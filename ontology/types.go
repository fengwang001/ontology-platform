// Package rlebitmap 是一个基于游程编码（RLE）的 uint32 全域集合。
//
// 集合的内部表示是一组按起点升序排列、互不相交且不相邻的“含 1 游程”：
// 每个游程 [lo, hi]（闭区间）表示从 lo 到 hi（含）的所有位均为 1，
// 其余所有位（包括未覆盖的全域前缀、间隙）均为 0。
//
// 该表示是规范（canonical）形式：
//   - 相邻且同值（都为 1）的游程必须合并，因此游程之间至少相隔一个 0 位；
   - 不存在长度为 0 的游程（lo <= hi 恒成立）；
   - 不保留尾部全 0 游程——只存 1 游程，故其不可能出现；
   - 空集表示为零个游程。
//
// 因为全集是有限且位置敏感的（Contains 必须能读出绝对位置），
// 我们不采用“起始值 + 交替游程长度”的锚点表示，而是让每个 1 游程
// 显式携带其绝对端点，从而保证编码唯一且可直接定位。
package rlebitmap

// run 表示一个含 1 的闭区间游程 [lo, hi]。
// 不变量：lo <= hi；同一 Set 内的游程按 lo 升序，
// 且相邻游程之间至少有一个 0 位（前一个.hi+1 < 后一个.lo）。
type run struct {
	lo uint32
	hi uint32
}

// Stats 报告最近一次“可计数操作”访问了多少个游程。
//
// 对于 Union/Intersect/Difference，Runs 是游标推进次数
// （每次消耗一个输入游程计 1），它只与输入游程数同阶，
// 与游程覆盖的位数无关。
//
// 对于 Count/Min/Max，Runs 是该次统计访问的游程数。
type Stats struct {
	Runs int
}
