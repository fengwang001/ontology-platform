# 设计推导

## 1. 空行与单列空值
空行（某记录无任何分隔符且唯一字段未加引号、值为空、字段起点等于行首字节）**跳过，不产生记录**：空输入零记录，末尾换行不产生多余记录，二者才一致可往返。
单列空值的记录无法靠跳过规则区分：解析器永不产生「未引号的单列空记录」，要表达「一条值为空的单列记录」必须写成 `""`（quoted 空）。故回写器对**单列 quoted 空字段**必须加引号；不加则写出空行，重新解析被跳过而丢记录。多行空行同理不产生记录。

## 2. 状态机
状态：`S0`字段开始 `SU`未引号 `SQ`引号内 `SQ2`引号内见引号 `SCR`行尾CR待定。字节类：`,` `"` `\n` `\r` 其他`x`。

| 状态 | `,` | `"` | `\n` | `\r` | `x` |
|---|---|---|---|---|---|
| S0 | 发空cell→S0 | →SQ | 空行/行结→S0 | →SCR | →SU |
| SU | 发cell→S0 | ErrBareQuote | 行结→S0 | →SCR | →SU |
| SQ | 追加 | →SQ2 | 追加 | 追加 | 追加→SQ |
| SQ2 | 发cell→S0 | 追加引号→SQ | 行结→S0 | →SCR | ErrQuoteClose |
| SCR | ErrLoneCR | ErrLoneCR | CRLF行结→S0 | ErrLoneCR | ErrLoneCR |

EOF 动作：SQ/SQ2 未闭合→ErrUnclosedQuote；SCR→ErrLoneCR；S0 在记录起点=无记录（末尾换行），否则=空行跳过；SU/SQ2 末字段落记录。引号字段起止偏移含包裹引号；`""` 在状态内仅 SQ2→SQ 时追加一个`"`，故每字节恰好处理一次。

## 3. CR 待定
流式：`\r` 不立刻判错，转 SCR 挂起；续传的下一字节决定：`\n`=CRLF 行尾，其他=ErrLoneCR（在该字节判）；Close 时仍挂 SCR 则 ErrLoneCR（偏移落在`\r`）。par：切点落在 `\r` 时入口态 SCR，由聚合器以该 `\r` 起做至多 3 字节微扫消化挂起，再接对应双假设序列（见 4），结果与挂起语义一致。

## 4. par 切点
buf 按任意偏移切 K 段。每段 worker 以两种入口态并行扫描：OUT(S0 起) 与 INQ(SQ 起)，各自产出「原始 cell/行结/错误事件」（仅带字节偏移，字段值后由 `cell.Decode` 从原 buf 按 quoted 标记解码，跨段拼接不拷值）。聚合器从左到右：段0入口=S0，依次确定每段真实入口；若入口为 SCR 或 SQ2，则从切点起做至多 3 字节微扫到达稳态，再把稳态对应的 worker 事件前缀（落在消化字节内者）丢弃后接续。段尾开 cell（SU/SQ/SQ2）跨到下段时，其首段已发 cell 被下段首 cell 合并：首段 raw span + 后续 span 联合解码，quoted 取首段标记。记录/字段号在聚合时按事件序重新编号，段内事件偏移均为全局绝对偏移，错误的字节偏移直接取绝对偏移，记录号/字段号=事件落在的第几个行结/逗号，与流式同源计数。每段最多两遍，微扫每段 ≤3 字节，总量 ≤2N+3K，长引号字段不引发重扫。

## 5. 上限的「立刻」
字段字节数在状态机内对**每个追加进字段值的字节**计数：普通字节 +1；引号内 `""` 转义对在 SQ2→SQ 时只 +1（追加一个`"`字符，包裹引号不计）。计数在 append 同一步检查，第一个超限字节即 ErrFieldTooLong（偏移指向它），进入终态；不缓冲整记录。ErrTooManyFields 在第 max+1 个字段起点（逗号/行首引号）判；ErrTooManyRecords 在第 max+1 条记录落定时判。

## 6. 规范输入
引号仅在 quoted 字段上出现；quoted 空只在多列或作为非空记录时合法（单列 quoted 空=空值记录，亦合法）；行尾一律 `\n`（引号内 `\r\n` 原样保留）；非空记录的行首字段若非空不加引号；末尾可选单个换行，无连续空行。规范输入上 Write(Parse(x))==x；最小引号条件：值含 `,` `"` `\r` `\n`，或单列表的 quoted 空。流式解析器单实例非并发安全；par 内 K goroutine 只读 buf，无共享可变状态。
