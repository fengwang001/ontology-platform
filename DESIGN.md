# 设计推导（规范化器）

## 1. 末尾换行策略

设规范化后（未施加末尾策略）的字节为 `b`，`t` = 末尾连续 `\n` 数。

- `Preserve`：输出 `b`（空入空出，不合成任何字节）。
- `EnsureOne`：`b==""→""`；否则输出去掉全部末尾 `\n` 后追加恰好一个 `\n`。
  空输入取 `""` 而非 `"\n"`：若取 `"\n"`，则与真实 `"\n"` 输入不可区分，无法各自稳定；
  取 `""` 后非空分支恒产出“内容+一个 `\n`”，空分支恒产出空，各自幂等。
- `TrimKeepOne`：`t==0→b`；`t>0→去尾换行后的内容 + "\n"`，内容为空则为 `"\n"`。
  该策略从不“无中生有”合成换行；三种策略的输出尾部均至多一个 `\n`，各自幂等。

| 输入 | Preserve | EnsureOne | TrimKeepOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

`ToOut` 单调不减且保留偏移必须满足 `ToOut(ToOrig(o))==o`，删除只能把区间折叠到同一输出偏移。
被删行尾空白与 `\r\n` 中的 `\r`，折叠点取其后随换行符的输出位置（即 `\n` 自身偏移）：
删除区间之后最近的保留锚点是 `\n`；光标落在被删空白里语义为“行尾”，应在 `\n`
**之前**（左闭右开下就是 `\n` 的偏移），而非跳到下一行。流末无换行终止时折叠到 `len(out)`。
合成换行无原文对应字节，其偏移映射为 `len(orig)`（钳制约定）：合成时两个不同输出点共享同一
原文终点，严格恒等式在 EnsureOne 给“无尾换行的非空输入”补换行时存在不可避免的单点例外；
TrimKeepOne 从不合成换行，无例外。

## 3. 空白缓冲超限

强制把待定空白判为“非行尾”并输出，违反第 5 条（再规范化会被删除）、“输出不含行尾空白”及第 6
条折叠语义。故选择拒绝：写入第 limit+1 个待定空白字节时返回带原文偏移的 `ErrWSOverflow`，
已输出保留，实例进入终态（再写返回 `ErrClosed`，`Close` 幂等）。输出上限同理返回
`ErrOutputLimit` 并进入终态。

## 4. par 拼接

各段独立以 `Preserve` 规范化（末尾策略只能最后施加一次），拼接处做两类有界修正，再全局施加策略：

1. 前段末为待定 `\r` 且本段首为 `\n`：删除本段输出首字节（`\r\n` 合成一个行尾）。
2. 跨切点的待定空白：其后随行尾或流末则删除（含前段尾、本段首两侧）；否则各自还原为逐字节保留。

修正有界：每侧只需检查至多 1 个待定 `\r` 加一个连续空白区间，即每段至多一次映射区间手术。
段映射表按段内坐标记录 `(origStart,origEnd,outStart,outEnd)`，拼接时整体平移
`(ΣorigLen, ΣoutLen)` 得全局坐标；超 `WSLimit` 即 `ErrWSOverflow`，与流式一致。
