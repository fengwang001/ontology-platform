# CSV 方言解析器设计（RFC4180 子集）

## 1. 空行与单列空值
选择：**空行被跳过**，不产生记录；多列行（如 `a,,b`）中的空字段照常保留。单列表里「一条空记录」与「空行」必须可区分：文件整体为空 = 0 条记录；无换行的空字段（EOF 处于 FS）= 1 条含 1 个未引号空字段的记录；文件 `""` = 1 条含 1 个加引号空字段的记录；`\n` = 空行跳过 = 0 条；`a\n\n` 只产生 a 一条。
往返推论：单列表中唯一字段为空的记录若不加引号，回写为空串，再解析得到 0 条记录，数据丢失。故 writer 规则：**记录仅 1 个字段且该字段为空时强制输出 `""`**（未加引号的也改写为加引号，引号标记虽变，但解析结果仍为 1 条空记录，满足「逐字段相等」——值相等；引号标记对该形态做文档化例外）。多列记录中的空字段写空串即可（逗号提供结构，不丢数据）。

## 2. par 切点：双假设 + 2 字节前缀
worker 无法仅凭本段字节知道起点是否在引号内。切点为任意偏移 i 时，段用两种入口各跑一遍：H0「引号外/字段开始」、H1「引号字段内」；并各带**切点前至多 2 字节前缀**重跑。
入口共 6 种，均能由左段尾 carry + ≤2 前缀确定：干净记录边界/逗号后/未引号字段中段 → H0；引号字段中段 → H1；`""` 的第二字节待定(qseen) → 带前 1 字节 `"` 重跑后取 H1；引号闭合后或未引号字段后的 `\r` 待定(crPending) → 带前 2 字节（`"\r` 或 `x\r`）重跑区分。未引号中段入口用 H0 即正确：该位置若出现 `"` 本就是 ErrBareQuote。
选定入口那一遍后，**丢弃绝对偏移 < i 的全部事件与错误**，只回放 ≥ i 的事件。跨段未闭合字段 carry：段首在字段中时，丢弃该 run 在 i 前发出的 cell-open/value，其后 value 追加到 carry；段尾未闭合字段成为下一段 carry。事件携带绝对偏移（段内偏移+段基址），记录号/字段号在全局 builder 回放时重新计数，错误坐标天然为全局坐标。
处理次数：每字节被本段两遍处理，加上每切点 ≤2 前缀字节 ×2 遍，总计 ≤ 2N + 4(K−1)，不随长引号字段退化。

## 3. CR 待定状态
crPending：未引号字段见 `\r`（含 FS 空态）或引号闭合后见 `\r` 时进入，暂不判定。下一字节为 `\n`：确认行尾（记录结束）；为其它任意字节：孤立 CR，引号外报 ErrBareCR（引号字段内的 `\r` 不进此态，直接作内容保留，`\r\n` 原样保留）。续传时该状态（连同字段是否已闭合引号）驻留解析器内存，跨 Feed 不丢，切在 `\r`/`\n` 中间亦成立；par 切点落此态时由 2 字节前缀重跑重建。EOF 仍在 crPending：报 ErrBareCR。

## 4. 上限的「立刻」
字段计数按**逻辑字符**逐字节递增并立即比较：未引号态每普通字节 +1；引号态普通字节 +1，转义对 `""` 在第二个 `"` 到达（QS 态见 `"`）时 +1，计数发生在该字节处理瞬间而非记录完成时。计数到上限+1 的字节即刻 ErrFieldTooLong，已产出的完整记录保留，fatal 被缓存，解析器进入终态，此后 Feed/Close 恒返回同一错误。字段数：每开始一个字段即计数 → ErrTooManyFields；记录数：每完成一条记录即计数 → ErrTooManyRecords。

## 5. 状态转移表
状态 FS 字段开始 / UQ 未引号中 / Q 引号中 / QS 引号中刚见引号 / CR CR待定；输入类 `,` `"` `\r` `\n` 普通c。
| 状态 | `,` | `"` | `\r` | `\n` | 普通c |
|---|---|---|---|---|---|
| FS | 收空cell→FS | cell-open,进Q | 进CR | 空行跳过→FS | 开cell,进UQ,c入值 |
| UQ | 收cell→FS | ErrBareQuote | 进CR | 收记录→FS | c入值 |
| Q | c入值 | 进QS | c入值 | c入值 | c入值 |
| QS | 收cell→FS | 转义:c入值,进Q | 进CR | 收记录→FS | ErrQuoteAfterClose |
| CR | ErrBareCR | ErrBareCR | ErrBareCR | 收记录→FS | ErrBareCR |
EOF：FS 收末记录（流首 FS 且无内容=0 条）；UQ/QS 收末记录；Q=ErrUnclosedQuote；CR=ErrBareCR。偏移：start=字段首字节偏移（引号字段为开引号位置），end=末内容字节偏移+1（未引号空字段 end=start；引号字段 end=闭引号后偏移）。

## 6. 规范输入
规范输入：记录分隔只用 `\n`；字段仅在含 `,` `"` `\r` `\n` 或为「单列表唯一空字段」时加引号；引号转义为 `""`；文件以一条无尾换行的记录或以 `\n` 结束（最后换行不产生空记录）。此形态下 Write(Parse(x)) == x 逐字节成立。流式解析器实例非并发安全；par 内部每 worker 独立状态机，无共享可变状态。
