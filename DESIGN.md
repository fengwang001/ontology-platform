# 严格 Base64 规范尾部推导

Base64 以每 3 个输入字节为 24 位，并按 6 位切成 4 个字母。最后不足 3 字节时，编码方在 24 位缓冲右侧补 0：

- 1 字节输出形如 `XY==`：只允许出现 1 个数据字母 `X` 之外，`Y` 只有高 6 位中的低 2 位属于原字节；`Y` 剩余的低 4 位必须全为 0。
- 2 字节输出形如 `XYZ=`：前两个字母承载 12 位；`Z` 的高 4 位属于原字节，最低 2 位必须全为 0。

因此严格判定检查的不是解码值，而是**最后一个非填充字母中不属于输入、由编码补零产生的尾部比特**：

- 1 字节余数：检查第 2 个字母值 `v & 0b001111 == 0`。
- 2 字节余数：检查第 3 个字母值 `v & 0b000011 == 0`。

`QR==` 可宽松解码成 `A`，但 `R` 的 6 位值是 17，低 4 位为 `0001`，不是规范编码 `QQ==` 中的全零。接受它会给同一字节串 `A` 两个不同编码，破坏“任意字节串的编码结果唯一”。严格模式只接受编码器真实输出，故必须拒绝。

填充与组长度也由编码形式唯一确定：无余数为 0 个 `=`，余 1 字节为 `==`，余 2 字节为 `=`；完整流必须由 4 字符组组成，填充只能在最后一组末尾。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待补 | 待补 |
