# DESIGN：流式行尾/空白规范化与双向偏移映射

## 1. 末尾换行策略（推导）
策略：Keep=保留原样；EnsureOne=非空输入保证恰好一个末尾 `\n`，空输入输出 `""`；TrimAll=删除全部末尾空行但保留一个换行（无尾换行的非空输入不补换行）。
若空输入 EnsureOne 输出 `"\n"`，则 N(N(""))=N("\n")="\n" 看似幂等，但规范化凭空创造了一行，编辑器会得到原文不存在的内容，违反「无删除点时区间数为常数」的精神；故选择空→空，规则表述为「空→空，否则以恰好一个 \n 结尾」，三策略均幂等（规范化结果已无 `\r` 与行尾空白，再规范化恒等）。

| 输入 | Keep | EnsureOne | TrimAll |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut（推导）
设原轴 i、输出轴 o，映射为分段线性的 piece：match(两轴同长)、drop(只占原轴)、ins(只占输出轴)。不变量 `ToOut(ToOrig(o))==o` 要求每个输出位置都有原像，故 ins（生成的 `\n`）锚定在生成点（末插换行锚定 len）。
drop 区间没有任何输出位置必须指向它，其 ToOut 取值只受「单调不减」约束：取 drop 之后首个输出位置（drop 起点的 o 坐标，即右边界）。于是 `\r\n` 的 `\r` 与行尾空白都映射到该行换行符 `\n` 的位置（光标在被删空白里 → 落在换行符处，即行尾之后，与编辑器「删空白后光标停在换行处」直觉一致）。ToOrig 单调不减；ToOut 在 piece 边界允许相等（drop 内恒定），故单调不减。

## 3. 缓冲超限（推导）
强制把待定空白判为「非行尾」并输出，若下一字节恰是行尾则输出错误（含行尾空白），第 2、5 条被破坏；改为延迟输出则违反流式边界且与切分点有关，破坏第 4 条。故选择拒绝：待判定空白字节数超过 `MaxTrail` 立即返回带原文偏移的可判定错误 `ErrTrailLimit`，已输出内容保留，进入终态（再写返回 `ErrClosed`）。拒绝瞬间映射仍自洽（缓冲内容未落任何 piece），第 4/5/6 条不受影响。输出超 `MaxOutput` 同理立即拒绝（`ErrOutputLimit`）。

## 4. par 拼接（推导）
各段以「非终态」独立规范化（Close 之外的挂起语义）：已确定的 piece 立即提交；段尾只能挂起一串待判定空白 + 至多一个待定 `\r`（eol 中 `\r` 必须等下一字节），故挂起内容有上界 `MaxTrail+1`，修正范围有界。
拼接按段顺序进行：段 k 的挂起空白交由段 k+1 的开头决定——查看 k+1 第一个原始字节：是 `\n`/`\r` 或在末段遇 EOF，则整串空白 drop（其后待定 `\r` 再与下一 `\n` 配对成 ins `\n`，否则自身成 ins `\n`）；否则整串空白 match 原样存活。该判定只依赖下一段第一个字节，且与段内逐字节处理同构，故任意切点（含 `\r|\n`、空白中间）结果与单流逐字节一致。
坐标平移：每段 piece 的原轴坐标加 `inStart`、输出坐标加当前 `outLen`；查询计数器/二分结构在合并后的全局 span.Map 上重建。末段按全局策略做一次尾部裁剪/补换行（span.ApplyPolicy）。NUL 严格检查仅在全局流上按序执行一次（段偏移+段内偏移）。

## 5. span 复杂度
piece 序列只在删除点增长；无删除的纯 `\n` 文本全程 match，区间数为常数。查询用 sort.Search 二分（含终点边界），非导出计数器 `lastChecks` 记录比较触及的区间数，断言 ≤ 2·log2(n)+4。
