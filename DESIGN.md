# 自定 LZ77 流格式设计

## 1. 记录布局

流：`头 + (记录)* + 尾`。除固定头外，整数均为无符号 LEB128 变长整数（varint）。

| 记录 | 首字节/布局 | 字段含义 |
|---|---|---|
| 流头 | `"OLZ" 01` 4 字节 | 魔数 3 字节，版本 1 字节 |
| 字面量 | tag=0,len,bytes | `len>0`，后接原文字节 |
| 回指 | tag=1,dist,len | `dist>=1`，`len>=3` |
| 刷新 | tag=2 | 边界提示；解码器无输出 |
| 流尾 | tag=3,origLen,checksum | FNV-1a-64，按原文字节序计算 |

varint 最多接受 10 字节；第 10 字节只能是最低位，溢出或超长即损坏。

## 2. 第三节推导

### 2.1 Write 切法无关

在当前已知数据末端，一个“最长匹配”可能被下一次 Write 延长。若立即发射，1 字节 Write 与整段 Write 会产生不同回指长度。压缩器因此保留一个未决后缀：每次循环只在某个起点检查窗口哈希链；若最长匹配到达当前已知数据末端，就停止发射，把该起点及其后所有字节留给下次。下一字节到来后重新考察同一候选链；该匹配要么继续延长，要么在确定结束处发射。任何已发射字面量或回指，其最优性都不会被未来数据改变：字面量起点可确认无长度至少 3 的匹配；回指后的下一起点尚未发射。Flush 强制完成未决后缀，因此此刻可发射当前能确定的回指、必要字面量和刷新标记；Close 不发射刷新，只发射流尾。连续空 Flush 没有新记录，故零字节。

### 2.2 重叠回指复制

块复制语义读取整个源区间；当 `dist<len` 时，源区间后半部在复制开始时尚不存在，直接复制会读到旧数据或与重叠方向不一致。正确 LZ77 展开等价于 `out[i]=out[i-dist]` 按 i 递增。实现固定逐字节循环；普通匹配长度由压缩上限约束，巨型恶意匹配的成本就是其申请产生的输出量，不构成额外放大，故无需复杂 memmove。

### 2.3 两类距离错误

“超过窗口容量”是格式/配置类错误：合法编码器的距离永不大于构造窗口 W。“超过已输出字节数”是流状态类错误：距离虽然不大于 W，但解码器历史尚浅。二者必须分开，不能合并成笼统坏距离。正常压缩器只插入窗口中的实际历史位置，因此不可能产生超过 W 的回指。并行块把上一块末尾至多 W 字节作为本块预置字典；全局串行解码时这些字节已由前一块输出，所以“已输出字节数”仍按全局流计数，预置字典不改变距离合法性，只改变块内匹配器的初始状态。

### 2.4 并行确定性

第 i 块匹配器状态仅由本块原文与上一块末尾至多 W 字节决定；上一块原文在分块后即可读，不等待其压缩结果，也不使用完成顺序、时间或共享可变匹配器。每块采用相同顺序算法，因此 workers=1/2/4/8 的记录逐字节相同。各块输出直接拼接，不加刷新标记；块边界只是输入分块，解码状态本来连续。最后由单一汇总阶段追加一个流尾，校验原文长度与 FNV 校验和。

## 3. 复杂度与资源

窗口为定长环形缓冲；哈希链每位置最多考察 chainLimit 个候选，计数器在每考察一个候选时递增，故上界为 `chainLimit×输入字节数`。全同字节链中候选很快受限于匹配上界，单位成本不随窗口增长。解码器在每次实际写入前检查输出上限，先拒绝长度声明，不预分配 `len` 大小空间。
