# 带撤回的增量聚合视图维护器 — 设计推导

## 1. 数据模型

- 变更 `Change`：版本 `Ver`（单调递增）、操作（Insert/Delete/Update）、记录键 `Key`、
  分组 `Group`（空串合法；缺失为非法）、值 `Val`；Update 另带原组 `OldGroup`、原值 `OldVal`。
- 视图为每个分组保存：成员表 `map[key]float64`，以及每个聚合器各自的聚合状态。
- Update 语义 = 从 `OldGroup` 撤回旧 (Key,OldVal)，再向 `Group` 应用新 (Key,Val)，
  二者在一次提交内完成，崩溃后整体重放，不存在半迁移。

## 2. 聚合器撤回策略推导

记删除值为 x，聚合状态为 S，成员集合为 M。

- **Count**：S 是基数 |M|，标量且对每条贡献恒为 1。删除：S' = S − 1，
  无需成员。可增量撤回。
- **Sum**：每条贡献独立可加（x），S = ΣM。删除：S' = S − x，
  贡献可从被删记录本身取得，无需其余成员。可增量撤回。
  浮点按位比较；−0 与 +0 在 Min/等值判定中归一为同一键值。
- **Min**：S = min M。若 x > S，删的不是极值，S 不变；若 x == S，
  新极值是 min(M\\{x})，而标量 S 不含“次小值”信息，无法反推
  （并列极值也无法只靠标量计数）。必须访问成员重算。
- **Max**：对称于 Min，删到极大值时标量状态不足，必须重算。
- **DistinctCount**：S = |{distinct v : v∈M}|。删除 x 后必须知道
  成员中是否仍有其他记录等于 x；标量计数不携带“每个值的持有者数”。
  故聚合状态额外维护各值出现次数 `map[value]count`：此时删除可纯增量
  （x 计数减到 0 才让 distinct 数减 1）。实现即采用该映射，因此
  DistinctCount 的 `NeedsMembers=false`；若只保留 distinct 标量，则与
  Min/Max 同属“删到关键值必须重算”。设计文档记录这一等价：映射是把
  “成员信息”内聚进聚合状态，触发条件判据不变。

聚合器接口显式声明 `NeedsMembers() bool`。删除流程：

1. 若该组所有聚合器都 `NeedsMembers=false`：直接各自 `Remove(x)`，O(1)；
2. 否则先各自尝试增量移除；对声明需要成员且删到关键值的聚合器（Min/Max
   的 `Remove` 返回 `NeedsRecompute=true`），只重算**该组**，重算输入
   恰好是该组删除后的成员表，不扫描其他组。

重算是例外：Count/Sum 永不触发；Min/Max 仅在被删值等于当前极值时触发。
非导出统计：`recompute[aggName]` 次数、`recomputeTouched` 访问成员总数、
`rejected`（乱序/缺组/NaN 拒绝数）。

## 3. 三阶段编排 Apply → Recompute → Commit

- **Apply**：校验（版本、分组存在、非 NaN）→ 追加日志；在内存改动
  成员表与所有聚合器（增量）。
- **Recompute**：仅当有聚合器报告需要成员，用该组改动后的成员表全量
  重建该聚合器（重算状态先放局部变量，完成后再换入）。
- **Commit**：推进 `maxVersion` 并释放快照。
- 并发：单把 RWMutex 串行化写者；读者持读锁看到的要么是旧组要么是新组，
  不会出现 Count 已变而 Sum 未变。Recompute 与同组写入同在写锁临界区，
  不会丢失。

## 4. 版本幂等与乱序

- `maxVersion` 为已提交最大版本。`ver > max`：接受；`ver == max`：
  幂等，直接返回不改任何字段（同一变更重复投递）；`ver < max`：
  返回可判定错误 `ErrStaleVersion` 并计入 `rejected`，视图不被污染
  （校验先于一切改动）。非递增到达即落入此类。

## 5. 日志格式与恢复

- 文件头：魔数 `"ONTJNL01"`（8B）+ 大端 uint64 条数（8B），自描述。
- 每条记录：大端 uint32 载荷长度 + 载荷（二进制编码 Change）+ CRC32(IEEE)。
- 截断四类可判定错误：头不完整 `<16B`；长度前缀不完整（不足 4B）；
  记录体不完整（声明长度未到齐，含 CRC 尾部缺失）；CRC 不匹配
  （长度齐但校验错）。重放遇到任一损坏即停，已完整记录全部生效，
  半截记录绝不生效。
- 三个崩溃点（Apply 中途/Recompute 中途/Commit 前）：日志已含的记录
  在重放时被重新走完整三阶段；因版本幂等与“先日志后内存”，恢复结果
  与无崩溃逐字段相同。崩溃注入由一次性 panic 钩子实现。

## 6. 边界语义

- 组内删空：删除组成员表为空后移除整个组；查询返回
  `(zero,false)`，`Groups()` 不含该组。
- 空串是合法分组；分组缺失（编码中 absent 标志）拒绝计数。
- NaN 值拒绝并计数（成员表无 NaN，Sum 位级比较因此良定义）。
- −0/+0：Min 比较与 distinct 键均按 IEEE 位归一（`Float64bits` 后
  把 −0 映射为 +0），视为相等。
