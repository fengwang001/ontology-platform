# RLE 设计说明

不变量：严格解码器接受的每个 `t` 都满足 `Encode(Decode(t)) == t`（编码唯一）。

## 拒绝清单的推导（若接受会怎样违反不变量）

1. 显式次数 1：接受 `1a` 则 `Decode="a"`，而 `Encode("a")="a" != "1a"`。
2. 次数 0：接受 `0a` 则输出为空，不含该游程，`Encode(...)` 无法产生 `0a`。
3. 前导零：接受 `012a` 则 `Encode` 重写为 `12a`；`01a` 重写为 `a`。
4. 相邻同符号游程：接受 `2a3a` 则输出 `aaaaa`，最长游程重编码为 `5a`。
5. 非法转义 `\x`（x 非数字/非 `\`）：该序列不是任何符号的合法写出方式。
6. 末尾孤立 `\`：缺少被转义字符，不是完整游程。
7. 只有次数没有符号：`12` 在末尾无法解码出符号，`Encode` 永不产生它。
8. 非法 UTF-8：解码结果必须是合法字符串；原样字节也无法经 `Encode` 复现。
9. 次数溢出上限（见下）：拒绝，避免静默错误。

错误彼此可区分（不同哨兵），并带字节偏移；偏移指向该游程次数首数字，
其余情形（转义、UTF-8）指向非法字节起点；偏移与输入切分方式无关。

## 大次数的取舍

选择拒绝超过 `math.MaxInt64` 的次数。次数的十进制表示本就可能任意长，
解析按位累加、每一位都做溢出判定，绝不使用固定宽度整数回绕；
且在拿到完整游程前不按次数分配内存。输出时按上限分块追加，
输出总字节数有可配置上限，超限返回可判定错误（`ErrOutputLimit`）。

## 语义与测试对照表

<!-- 最后补 -->
