# 自定格式 LZ77 流式压缩/解压 — 设计推导

## 流格式（全部整数为 LEB128 无符号变长整数）
| 记录 | 字节布局 |
| --- | --- |
| 流头 | magic(3B `LZ7`) + version(1B=1) |
| 字面量段 | tag=0(varint) + n(varint) + n 字节原文 |
| 回指 | tag=1 + distance(varint>=1) + length(varint>=3) |
| 刷新标记 | tag=2 |
| 流尾 | tag=3 + 原文总长(varint) + FNV-1a 64 校验和(varint) |

varint：每字节低 7 位为载荷、最高位表示后续还有；最多 10 字节，第 10 字节只允许 1 个有效位，否则判超长/溢出。

## 1. 压缩结果与 Write 切法无关（lazy 缓存）
任何位置 p 的最优输出取决于 p 之后是否还能延长匹配，因此 p 在“之后还有未处理字节”时不可决。压缩器把全部 Write 数据原样追加进未决缓冲，不在 Write 内产出任何记录；只有 Flush/Close 声明后面暂无/没有数据时，才在完整缓冲上跑一遍确定的 lazy 匹配器。算法只看同一份完整字节序列，1 字节、7 字节或整段写入产生的记录逐字节相同；Flush 位置（以累计偏移计）相同，则参与每次编码的子序列相同，输出相同。Flush 必须强制结束当前匹配：边界处的匹配本可被未来输入延长，承诺要求解压器此刻还原全部输入，故 Flush 把未决缓冲全部排空并追加刷新标记；之后的匹配仍从共享窗口取历史，故可越过 Flush 边界回指。

## 2. 重叠回指的复制
距离 d 小于长度 l 时，输出位置 i 的字节等于输出位置 i-d 的字节，后者可能正是本次复制刚产生的。一次性块复制读到的是旧缓冲，对周期性串会错。正确写法是按 k 递增逐字节 out[k]=out[k-d]。本实现直接逐字节循环：回指路径上每字节一次窗口取数，数据全在内存、无分配，复杂度仍 O(输出长度)，在解压炸弹语义下也天然逐字节可检查上限，故可接受。

## 3. 距离超过已输出 与 距离超过窗口 为何分开
已输出数是解压进度（前缀长度），窗口容量是压缩器保留的历史长度，二者是不同不变量。d 大于已输出：引用了从未存在过的数据，流一定是伪造的；d 不大于已输出但 d 大于窗口：数据真实存在却已滑出本格式允许引用的范围。压缩器只在容量 W 的窗口里找候选，故绝不产生后者。并行块预置字典把上一块末尾至多 W 字节在本块开始前装入窗口，但这些字节在同一流里此前都已由解压器输出，因此已输出字节数含义不变，两个检查都仍然必要且可区分（先查 d 大于 W，再查 d 大于已输出）。

## 4. 并行块压缩的确定性
块 i 的匹配器只依赖两块数据：本块原文 data[k*i:(k+1)*i] 与上一块原文末尾 W 字节（预置字典）；不读其他块、不读其他块的压缩输出，也不依赖 goroutine 完成顺序，因此块 i 的压缩结果是其输入的纯函数，与 workers 数无关。各块用同一压缩流顺序编码：上一块原文先进入窗口（预置为历史），再编码本块；除最后一块外每块末尾发刷新标记；最后一块写流尾，全流只此一个流头/流尾，长度与 FNV 校验和覆盖拼接后的完整原文。组装仅按块序拼接，故 workers 取 1、2、4、8 时输出逐字节相同，且可被普通流式解压器直接解开。
