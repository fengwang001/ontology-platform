# 严格 JSON 字符串编解码器设计推导

## 代理对错误偏移（对应语义 2）

结论：高代理单元不是"自足错误"，它是对下一个单元的**约束**；
因此一旦发现该约束无法满足，错误偏移回指**约束的起点**，即该高代理 `\u` 的反斜杠字节。

- `"\uD83D"`：读到结尾引号时约束未满足，报错于高代理起点（offset 1），错误为孤立高代理。
- `"\uDE00"`：低代理无前置高代理，它自身即非法单元，报错于自身起点（offset 1）。
- `"\uD83Dx"`：高代理后的下一单元必须是 `\uDxxx`，但下一字符是 `x`，约束在高代理起点即注定失败 → offset 1。
- `"\uD83DA"`：下一字符是 `A` 而非低代理转义，同理 → offset 1。
  它不能解释成"替换字符 + A"：RFC 8259 要求高代理后**必须**紧跟低代理，解码器无权把未兑现的高代理降级为
  U+FFFD 再继续；降级是宽客解析器的选择，会丢失"输入非法"这一事实，破坏严格性与编码单射。
- `"\uDE00\uD83D"`：首个单元即孤立低代理 → offset 1（首个错误优先，不等待后面的高代理）。

规则统一为：从左向右扫描，报第一个非法单元的起点；高代理非法性在"发现后继不是低代理"的瞬间成立，
位置记在高代理 `\` 上。

## 最小转义与唯一性（对应语义 4）

转义集合只取 RFC 8259 强制项：引号、反斜杠、U+0000–U+001F。

- 为何 U+001F 必须转义而 U+007F 不必：U+001F 属于 0x00–0x1F 控制段，RFC 8259 禁止其在字面量中裸出现；
  U+007F（DEL）不属于该段，引号外裸出现完全合法，故原样输出。非 ASCII（含 U+2028/U+2029）同理原样。
- 编码是确定性函数：每个码点的输出唯一——短形式只给 `\b \f \n \r \t`，其余控制字符唯一映射为
  小写定长 `\u00XX`；其余码点唯一映射为其 UTF-8 字节。不同码点的输出首字节互不重合（`\`、普通字节），
  且严格解码器与编码规则互为逆运算，因此 `Encode` 单射、输出唯一。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类严格错误可区分且带偏移 | （收尾时补填） |
| 2 代理对组合/拒绝/偏移 | （收尾时补填） |
| 3 非法 UTF-8 两个方向拒绝 | （收尾时补填） |
| 4 最小转义编码 | （收尾时补填） |
| 5 往返一致 | （收尾时补填） |
| 6 任意切分点流式一致 | （收尾时补填） |
| 复杂度 计数 ≤ 2N | （收尾时补填） |
