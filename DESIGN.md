# DESIGN

## 推导

1. 空行与单列空值：空行选择“跳过”。否则 `a\n\n` 会凭空多出记录；单列空值也无法和它区分。单列记录的唯一字段为空时，回写必须写成 `""`；不加引号会变成空行并被下一轮跳过，数据丢失。多列空字段仍写空串。
2. 并行切点：先由一个不保存值的轻量词法扫描整段一次，记录每段右端的快照（起始状态、记录是否活动、引号待定、CR 待定、计数和偏移）。K 个 worker 从对应快照开始只扫本段一次，输出字段片段、行结束或错误。若切点在引号内，快照状态为引号中；在 `""` 中间为“引号后待定”；在 `\r\n` 中间为 CR 待定。随后从左到右拼接片段并组装记录。因为词法状态完全来自确定性快照，每字节在预扫和 worker 中各至多一次，结果等于同一状态机的流式执行。错误的段内记录号、字段号加上段起点快照计数；字节偏移加段起点。
3. CR 待定：未引号字段见到 `\r` 后进入 CR 待定，不下发字段或行；下一字节是 `\n` 才提交行，否则报孤立 CR。半包时状态保留即可；并行切点直接保存/恢复该状态。流在 `\r` 结束调用 Close 时按孤立 CR 报错。引号内的 CR 永远是内容。
4. 立即触发上限：维护当前字段已接受字节数；逗号、换行、引号转义都不占字段长度，但转义结果 `""` 在第二个引号到达时只增加一个字节。新内容字节到达并被判定属于字段后，先令计数加一；若计数超过上限，在该字节偏移立即报错，不等待分隔符，也先不提交记录。记录数在完整记录提交前检查；字段数在字段提交导致数量越界时检查。

## 状态转移

| 状态 | 普通字节 | `,` | `"` | `\r` | `\n` | 结束 |
|---|---|---|---|---|---|---|
| 字段开始 | 未引号；追加 | 提交空字段 | 引号字段 | CR 待定 | 提交行/跳空行 | 提交最后字段（有活动记录） |
| 未引号字段中 | 追加 | 提交字段 | 裸字段引号错误 | CR 待定 | 提交行 | 提交最后字段 |
| 引号字段中 | 追加 | 追加 | 引号待定 | 追加 | 追加 | 引号未闭合 |
| 引号待定 | 闭合后杂字符错误 | 闭合并提交字段 | 追加一个引号 | 闭合；CR 待定 | 闭合并提交行 | 闭合并提交最后字段 |
| 行尾 CR 待定 | 孤立 CR | 孤立 CR | 孤立 CR | 孤立 CR | 提交行 | 孤立 CR |

## 规范输入

每条记录都以 `\n` 结束；记录分隔符写 `\r\n`；仅在字段含 `,`、`"`、`\r`、`\n`，或单列表中字段为空且加引号时使用引号；引号内的 `"` 写为 `""`。字段值中的 CRLF 原样保留。流式解析器实例非并发安全；不同实例可并发使用。
