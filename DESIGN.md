# 严格模式流式 Base64 设计说明

## 规范尾部的推导（语义第 2 条）

Base64 把 3 字节（24 bit）编码为 4 个字符（每字符 6 bit）。末尾不足 3 字节时：

- 剩 1 字节（8 bit）→ 2 个字符（12 bit）+ `==`：第 2 个字符只承载高 2 bit，**低 4 bit 未使用**；
- 剩 2 字节（16 bit）→ 3 个字符（18 bit）+ `=`：第 3 个字符只承载高 4 bit，**低 2 bit 未使用**。

编码器在右对齐补零时，这些未使用的低位必然为 0。宽松解码器直接丢弃这些位，
于是 `QR==`（R = 字母表第 17 号 = `010001`，低 4 bit = `0001`）也能解出 `A`。
但这样一来字节串 `A` 同时有 `QQ==` 与 `QR==` 两个"合法"编码，`Decode` 不再是
`Encode` 的逆，`Encode(Decode(y)) == y` 不再成立，编码失去唯一性。

**统一规则**：最后一个数据字符中"未承载输入数据"的低位必须全为 0。

- 判定用的比特：`pad==2`（形如 `XX==`）时检查第 2 个字符值的**低 4 bit**（`v[1]&15`）；
  `pad==1`（形如 `XXX=`）时检查第 3 个字符值的**低 2 bit**（`v[2]&3`）。
- 由此 `Encode` 是单射且在合法输入上 `Decode∘Encode = Encode∘Decode = id`，
  任意字节串的编码唯一；`QR==`、`QUJ=` 因低位非零被拒。
- 填充另有位置规则：`=` 只能作为最后一个组的**后缀**出现，且个数只能是 0、1、2；
  含填充的组之后只允许换行（MIME 模式），不得再出现任何数据字符。
  故 `QQ`、`QQ=`（长度非 4 的倍数）、`QQ===`、`QQ==QQ==`（填充不在流末尾）均被拒。
- 规范形式 = 本题编码器的输出；对任意被接受的 y，`Encode(Decode(y))` 是 y 的
  规范化结果，y 为规范形式（如无换行、或换行严格每 76 字符）时等式逐项成立。

## 语义与测试对照表

（实现完成后补齐）
