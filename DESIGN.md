# 媒体类型内容协商器 — 设计推导

## 1. 为什么先比 q，再比特异性

q 是客户端显式表达的偏好，特异性只是「同一偏好下」的消歧规则；
特异性绝不能覆盖客户端的明确取舍。

反例（先比特异性会选错）：

- Accept: `text/*;q=0.1, text/plain;q=1`，候选 `[text/plain]`
  先比特异性会因 `text/plain` 出现在被低权重的通配比较中而得出相反结论；
  正确结果必须是客户端给了 q=1 的那一项胜出。
- 更直观的反例：Accept: `*/*;q=1, text/plain;q=0.5`，候选 `[text/plain]`。
  `text/plain` 比特异于 `*/*`；若特异性优先，会选 q=0.5 的 `text/plain`，
  但客户端对该类型明确只给 0.5，通配方反而是 1 —— 违背 q 的语义。

因此比较键固定为 `(q, specificity)`，且只使用「与该候选实际匹配上的那一项」
的 q；Accept 书写顺序不参与比较，这是第 10 条可复现性的基础。

## 2. q=0 的语义与非法 q 的处理

`q=0` 是「明确拒绝」（RFC 9110：q=0 means not acceptable），不是最低优先级。
若把它当作最低优先级，在 Accept: `text/plain;q=0`、候选 `[text/plain]`
（服务端只有这一种）时，协商器会返回该候选，即把客户端声明「绝不接受」
的类型发给它。正确行为是返回「无法接受」。

q 用 0–1000 的整数表示（分母 1000，最多三位小数），天然避免浮点误差；
合法形式为 `0`、`1`、或带 1–3 位小数的 `0.xxx`/`1.000`；
`1.5`、`abc`、`0.1234` 均为非法。

**设计决定：任一 Accept 项非法 → 整条 Accept 头部解析失败（报错，不协商）。**
理由：协商器无法可靠判断客户端意图，静默丢弃会把「发错头的客户端」
悄悄降级为另一套偏好，属危险的静默语义；失败可判定、可由调用方按
400/解析错误处理，比「猜一个」更安全。非法项错误带项序号，便于定位。

## 3. 同分时为什么取服务端顺序

q 与特异性都相同，意味着客户端对这些类型「无差别地同等接受」。
客户端既已表达中立，决定权应交给提供资源的一方：服务端清单顺序代表
服务端实现成本/默认格式偏好（如服务端更愿意给 JSON 而非 XML）。
因此同分取**候选清单中最先出现者**，Accept 顺序一律不看。
对应两组对照：交换 Accept 项顺序结果不变；交换候选顺序结果改变。

## 4. 缺失 Accept 与空串为什么必须区分

- 头部缺失：客户端未作任何限制，按规范等价于 `*/*`（全部接受，q=1）。
- 空串：客户端发送了头但值为空，这是一个显式的、空的偏好集合，
  表示「不接受任何类型」，结果必须是无法接受。

若合并处理（把空串也当 `*/*`），会把一个明确表达「什么都不要」的请求
当成「什么都行」，从而在候选 `[application/json]` 下返回内容 ——
这正是第 6 条禁止的回退。二者在 API 层用 `acceptPresent bool` 区分：
`Negotiate(offers, header)` 中 `header==""` 表示缺失→`*/*`；
空串场景由调用方语义无法与缺失区分，故入口显式提供
`NegotiatePresent(offers, value, present bool)`，空串传
`value="", present=true` → 无法接受。

## 附：解析与错误

- 媒体类型：`type/subtype`；type/subtype 小写化；参数名小写、值保持原大小写；
  参数按名字典序排序后比较，顺序不影响相等；`q` 不是匹配参数。
- 参数值支持引号串：引号内 `;`、`=` 只是普通字符；`\"` 为转义引号，
  反斜杠仅在引号内作转义符。
- 五类可区分错误（均实现 `Unwrap` 哨兵、带 0 基项序号 `Index`）：
  `ErrNoSlash`、`ErrEmptySubtype`、`ErrParamNoEquals`、`ErrUnclosedQuote`、
  `ErrBadQ`。
- 无可接受候选返回哨兵 `ErrNotAcceptable`，绝不回退第一个候选；
  协商只读，不修改候选切片。
