# 游标分页器（keyset pagination）设计

## 1. 排序模型与复合键

行 = 排序值 `Value float64` + 全局唯一 `ID string`。定义复合键 `K = (Value, ID)`，
总序（tie-breaker）为：先比 `Value`，相等再按字符串字典序比 `ID`。

```
(a.v, a.id) < (b.v, b.id)  iff  a.v < b.v || (a.v == b.v && a.id < b.id)
```

`ID` 唯一保证该序是全序，每行位置确定，分页不重不漏。

## 2. 为什么游标不能只记排序值

反例：六行排序值都是 `1.0`，页大小 3，第一页取 `v <= 1.0` 的前三行，边界游标只记 `v=1.0`。

- 下一页取 `v > 1.0`：剩余三个等值行被整批跳过（**漏**）。
- 下一页取 `v >= 1.0`：第一页三行再次返回（**重**）。

正确做法：游标记录上一页**最后一行的复合键** `K`，下一页边界是复合键的**严格大于**：

- 正向：`K_row > K_cur`，取前 `n` 个；
- 反向：`K_row < K_cur`，取后 `n` 个（边界严格小于）。

等值行靠 `ID` 区分，因此既不重也不漏。

## 3. 游标编码（不透明、O(1)）

字节布局，定长头部 + 变长 ID：

```
0x01(版本) | dir(1: forward / 2: backward) | value(8B, binary.BigEndian)
           | idLen(2B BE) | id(idLen B) | crc32-IEEE(4B BE)
```

空字节串（nil 或长度 0）合法，表示从头（正向）或从尾（反向）开始。解码顺序：
长度不足/截断 → `ErrCursorTruncated`；CRC32 不符 → `ErrCursorChecksum`；
方向字节非 1/2 → `ErrCursorDirection`。三类与「跨方向复用」`ErrWrongDirection`
共同构成四类可 `errors.Is` 判定的错误。编解码仅操作定长缓冲，堆分配为常数。

## 4. 取页规则

底层为有序切片，`sort.Search` 二分定位边界游标，顺序读取 `limit` 行，
单次翻页代价 O(log N + limit)，比较次数有上界 `4*(limit+ceil(log2 N))`，绝不从头全扫。

- 正向：`Search` 找首个 `K > Kcur` 的下标 `i`，页 = `s[i:i+limit]`，正向排列；
  `Next` 指向本页末行（继续向后），`Prev` 指向本页首行（可反向回退）。
- 反向：`Search` 找首个 `K >= Kcur` 的下标 `i`，候选 = `s[max(0,i-limit):i]`，
  即最大的至多 `limit` 个严格小于 `Kcur` 的键；**页内仍按正向排列返回**。
  `Next` 指向本页首行（继续向更早），`Prev` 指向本页末行。

反向翻页不是把正向结果倒序：比较方向与取数方向都反，但输出页内顺序保持正向，
故「正向第三页游标反向一页」得到的正是正向第二页、逐行相同。

## 5. 并发增删下的语义

存储用 `sync.RWMutex` 保护；翻页是关于「调用瞬间快照」的纯函数，游标只含不可变键，
不持有连接或推进位置：同一游标并发翻页，写者静止时结果逐行相同。

- 不提供可重复读快照：翻页途中写入已扫区间的行允许不出现在后续结果中。
- **绝不重复**：边界是严格复合键序，已返回行的键不越过游标，不会再次满足。
- **绝不跳过未删行**：任何未删行只要其键落在 `(Kcur, +∞)`（反向同理）区间内，
  二分边界只依赖键值、不依赖该行是否存在，必在某页被取到。
- 插入到「第四页区间」的新行，按其复合键自然插入排序位置被取到；
  删除行直接消失，指向已删行的游标仍以键值定位、可继续翻页。

## 6. 自检（audit）

独立审计器：正向连续翻页收集全部 ID，断言与一次性全量排序结果的多重集一致
（不重不漏）、顺序一致；反向同理；并提供「已返回集合不与新页相交」的增量检查。
