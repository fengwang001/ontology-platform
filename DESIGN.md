# 熔断 + 舱壁调用保护器设计

分层（自外向内）：`stat.Executor` → `breaker.Allow` → `bulkhead.Acquire` → `timeout.Do(fn)` → `classify.Of` → `breaker.Report` → 归还额度。

## 1. 熔断计数只算真实调用的推导

设失败率 = 失败数 / 样本数。若把「被熔断拒绝」计入样本，则熔断打开后每次拒绝都使失败数与样本数同增：当拒绝占比趋于 1，失败率趋于 1，永远 ≥ 阈值，熔断永远不可能关闭——保护器自我锁死。同理，被舱壁拒绝的调用根本没有触及下游，其结果是本地容量决策而非下游健康信号，计入同样污染口径。因此：**样本只来自真实到达下游的调用**（成功、失败、超时、panic 转换的失败），两类拒绝只计入各自的拒绝计数器，不进失败率分子分母。推论：熔断打开后任意多次拒绝，失败率统计不变；冷却结束进入半开后，一次探测成功即可关闭。

## 2. 熔断在舱壁之前的论证

两种顺序对比：

- 熔断在前：下游已知故障时，请求在 O(1) 内以 `ErrOpen` 拒绝，不占并发额度、不进等待队列，额度留给其他仍健康的逻辑路径，拒绝延迟为零。
- 舱壁在前：同样的拒绝要先占额度甚至排队，打开期间额度被「注定失败的请求」占满，既增加延迟又可能把健康调用挤出队列。

故规定**熔断在前**。可观测判据：熔断打开期间，舱壁在途数与历史峰值恒为 0。

## 3. 额度必须在四条路径上归还

额度是有限令牌（容量 N 的 channel）。获取后只有四条出路：成功、业务失败、超时、panic。任何一条漏还，令牌单调减少，最终为 0，此后所有请求在队列满后被拒——保护器从「隔离故障」退化为「制造故障」。实现上用 `defer release()` 保证成功/失败/超时同路径归还；panic 路径用 `defer recover` 转为错误后仍走同一 defer 归还。判据：四条路径各跑 1000 次后可用额度回到满值 N。

## 4. 不可重试错误是否计入熔断

定义：**不可重试错误（如参数非法、404 类）不计入熔断样本**，既不增失败数也不重置连续失败计数；可重试错误、超时、panic 计入失败。论证：熔断的目的是探测下游健康度。不可重试错误是调用方契约问题，下游处理它是「正常履职」，重试同一请求必然重现同一错误，熔断它既不能保护下游也会误伤其他正常调用方；而可重试错误/超时/panic 都指向下游处理能力受损，正是熔断要隔离的信号。

## 5. 熔断状态机

- 关闭→打开：连续失败 ≥ `ConsecutiveFailures`，或（样本数 ≥ `MinSamples` 且失败率 ≥ `FailureRate`）。两条件独立成立即触发。
- 打开→半开：`now ≥ openedAt + cooldown`（注入时钟）。`now < openedAt` 判为时钟回拨：返回 `ErrClockRollback`，状态不变，绝不提前半开。
- 半开→关闭：探测成功数达到 `HalfOpenProbes`（即允许的探测全部成功），冷却重置为基准值。
- 半开→打开：任一探测失败立即打开，冷却加倍（`min(2*cooldown, MaxCooldown)`）。观测序列应为 c0, 2c0, 4c0, …, 封顶。
- 半开并发：在途探测数 < `HalfOpenProbes` 才放行，其余以 `ErrOpen` 拒绝。
- 并发迁移：所有状态迁移在互斥锁内完成，迁移回调只触发一次；并发失败不会重复加倍冷却。

## 6. 舱壁精确语义

- 令牌 channel（容量 N）+ 等待名额计数（上限 Q）。先非阻塞抢令牌；抢不到且等待名额未满则登记等待（select 令牌或 ctx 取消）；名额已满立即返回带时间戳的拒绝错误（`errors.Is` 命中 `ErrRejected`），时间戳取注入时钟，可断言拒绝未推进时间。
- 等待中被 ctx 取消：归还等待名额，返回 `ctx.Err()`（执行器把它计入舱壁拒绝口径，因为它未触及下游）。
- 非导出 `peak` 字段记录历史峰值在途数，仅在持锁时更新；`Peak()` 只读暴露。
- 不同下游各持一个 `Bulkhead` 实例，令牌与队列天然隔离。
- 构造校验：`Concurrency ≤ 0` 或 `Queue < 0` 报错；`Concurrency = 1` 自然退化为串行。

## 7. 统计口径

`stat.Recorder` 用原子计数：总请求、真实调用、熔断拒绝（含时钟回拨拒绝）、舱壁拒绝（含等待被取消）、成功、按 `classify.Kind` 细分的失败。任意时刻满足：

- 总请求 = 成功 + 失败 + 熔断拒绝 + 舱壁拒绝（每个请求在 Executor 中只有一条出路，各落一个计数器）
- 真实调用 = 总请求 − 熔断拒绝 − 舱壁拒绝
- 按类别失败之和 = 失败总数

四类哨兵错误可用 `errors.Is` 区分：`breaker.ErrOpen`、`breaker.ErrClockRollback`、`bulkhead.ErrRejected`、`classify.ErrTimeout`（另有 `classify.ErrNonRetryable`、`classify.ErrPanic`）。
