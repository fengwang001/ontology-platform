# Properties 解析/回写设计推导

## 推导一：续行接上“只含空白或为空”的物理行（语义 3）

对照 `java.util.Properties` 的 LineReader：行尾反斜杠置位 `skipWhiteSpace`，
随后它进入“跳过空白”循环（`==' '||=='\t'||=='\f'`），遇到换行也只是把
`skipWhiteSpace` 重新置位并继续；只有读到非空白字符才退出该循环并入缓冲。
因此被续行接上的纯空白行的全部内容（包括那个换行）都被吞掉，继续向再下一行
剥前导空白并拼接。即：

```
k=v\


   w
```
等价于 `k=vw`，中间有多少个空行/纯空白行结果都一样。

这与“续行首部空白被剥掉”是同一条规则的推论，而非两个独立机制：
`skipWhiteSpace` 状态只在遇到换行时被保持、遇到真正数据字符时才清除，所以
“被剥的空白”可以跨越任意多个纯空白物理行。文件末尾单个反斜杠时没有下一字符，
该反斜杠被丢弃，不产生任何拼接内容。

实现上 logical 层在读取过程中维护“行尾连续反斜杠计数”，遇普通字符清零，
遇 `\n` 时按计数奇偶决定是否续行——不从行首重扫，故每字节只检查一次。

## 推导二：Store 只转义必要字符（语义 6）

Load 一行的结构是：行首空白 → 可能的 `#`/`!` 注释 → 键 → 分隔符(`=`/`:`/空白)
→ 值。要保证任意串可逆，转义只需破坏这些“语法位置”：

- 键首为 `#`/`!`：否则整行被当注释，必须转义；值首不存在注释判定，值开头的
  `#`/`!` 永远安全，不转义。
- 键中任意位置的空格、`\t`、`\f`、`=`、`:`：它们都是键的分隔符候选（键扫描
  在此终止），不转义就无法回到原键。
- 键/值开头的空白：Load 会剥行首/分隔符后空白，必须转义；值**中间**的空格在
  值扫描里被原样保留，故不转义。
- 反斜杠与控制字符：`\\`、`\t`、`\n`、`\r`、`\f` 必须转义，否则会被当作
  转义引导或破坏单行结构；其他 `\x` 在 Load 中丢掉反斜杠，故不转义就无法往返。
- 非 ASCII：Load 按 Reader(UTF-8 解码) 读取，原样写出即可读回，不写 `\uXXXX`。
- 空键直接以 `=` 开头；空值写 `键=` 加换行；末尾保留换行保证末行完整。

## 语义—测试对照表

（实现测试后回填）
