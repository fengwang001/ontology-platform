# 设计推导

## 3. 空白续行

读入物理行后，先检查行尾反斜杠数；只有最后一个反斜杠前面有连续偶数个反斜杠（总计奇数）时，它才是续行符。
此时续行符本身被删除，再读取下一物理行，并删除该续行首部所有 ASCII 空白（空格、制表、换页）。
若下一物理行为空或只含空白，剥离后就是空串：它仍作为续行被消费并拼接到当前逻辑行，但不增加任何字符。
拼接后的行不再以反斜杠结尾，所以续行链结束；前一行已经删除的续行符不会恢复。
这与 `Properties` 的 `readProperty`/`readContinuedLine` 行为一致：空白剥离发生在“下一物理行已被读为续行”之后。

## 6. Store 最小转义

Load 先跳过逻辑行首部空白，再在键中寻找第一个未转义的 `=`、`:` 或空白；值从分隔符后开始，并继续跳过一段空白。
因此键中的所有反斜杠、空白、`=`、`:` 都必须转义，否则会改变边界；行首的 `#`、`!` 必须转义，否则整行成为注释。
值位于第一个分隔符之后，其中间的空格、`=`、`:`、`#`、`!` 都只是普通字符，所以不转义；但值开头的空白会被“分隔符后跳空白”规则吃掉，第一个开头空白必须转义。
反斜杠必须始终转义；`\n`、`\r`、`\f` 必须转义，避免引入新物理行或续行首部空白。非 ASCII 原样输出。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 分隔符 | 待补 |
| 2. 注释 | 待补 |
| 3. 续行 | 待补 |
| 4. 转义 | 待补 |
| 5. 重复键 | 待补 |
| 6. 回写 | 待补 |
| 7. 随机往返 | 待补 |
