# 设计推导

## 3. 只含空白的续行

续行判定只看物理行行尾连续反斜杠数量的奇偶性：扫描到行结束符时维持一个奇偶状态，奇数表示最后一个反斜杠转义了行结束符，因此丢弃该反斜杠并进入下一物理行；偶数则没有转义行结束符，逻辑行在本行结束。

进入续行后，读取器先处于“跳过首部空白”状态。空格、制表符等首部空白只被跳过，不加入逻辑行；遇到第一个非空白字符才恢复拼接。若下一物理行只含空白或为空，则从续行开始到行结束符之间没有任何字符能关闭跳过状态，这一整行除行结束符外都被消费，且不会向逻辑行加入空字符串、换行或空白。

所以 `k=v\` 后接空白行时结果仍是 `k=v`；它不会产生第三个键，也不会给值追加空格。空白续行不是独立规则，正是“续行首部空白被剥掉”在整行皆为空白时的边界表现：剥掉首部空白后没有剩余内容。若该空白行后还有普通行，普通行不再属于前一逻辑行，会作为新逻辑行解析。

## 6. Store 的最小必要转义

回写统一使用 `key=value`，每个映射占一个物理行，因此键和值都必须转义反斜杠以及会截断物理行的 `\n`、`\r`。

解析键时，未转义的 `=`、`:` 和空白都会结束键，所以键中的这些字符必须转义。逻辑行开头的非空白字符若是 `#` 或 `!` 会被识别为注释，所以仅当这两个字符出现在键首时需要转义；键内部的 `#`、`!` 不会触发注释。键首空白也必须转义，否则它在键开始前就被跳过；键内部空白会成为分隔符，同样必须转义。

值在分隔符确定后才开始解析，其中的 `=`、`:`、`#`、`!` 都是普通字符，内部和尾部空白也原样保留，所以无需转义。但解析器在消耗分隔符后会先跳过一段空白；若值以空白开头，不转义就会被当成分隔符后的填充而丢失，因此只需转义值开头的连续空白，之后的空格保留字面形式。非 ASCII 字符按 UTF-8 原样写出，即可被 `Reader` 读回。
