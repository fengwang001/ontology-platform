# 自然排序比较规则（DESIGN）

## 切分

`chunk` 把字符串切成数字段（仅 ASCII `0`–`9`）与非数字段交替的序列，给出每段原文。

## 前导零规则（第 3 条推导）

比较规则分两层，逐段并行扫描：

1. **主序（立即决定）**
   - 非数字段 vs 非数字段：字节序，短前缀更小。
   - 数字段 vs 数字段：比数值（去前导零的规范形，全零规范为 `0`；先比长度再比字典序），不转整数，任意长度不溢出。
   - 数值相等：**不决定**，继续比后面的段。
   - 一段已结束：短串更小。类型不一致（数字段对非数字段）：比两段首字节的字节序。
2. **次序（主序全程相等才用）**：按数字段出现顺序，逐段比「多余前导零个数」（原长 − 规范形长），个数少的更小；仍短前缀更小。

为什么不能立刻比前导零个数：`a01b` vs `a1c`：`a` 相同；`01` 与 `1` 数值都为 1，若立刻比零个数会判 `a01 > a1`，即 `a01b > a1c`；但下一段 `b < c`（0x62<0x63）明确要求 `a01b < a1c`，矛盾。故数值相等必须延后。

演算 `a01b` vs `a1c`：`a=a` → `01` 与 `1` 数值相等，挂起 → `b<c`，故 **`a01b < a1c`**。

演算 `a1b01` vs `a01b1`：`a=a`；`1` 与 `01` 数值相等，挂起；`b=b`；`01` 与 `1` 数值相等，挂起；同时结束，主序全等。次序零个数向量为 `(0,1)` vs `(1,0)`，首位 0<1，故 **`a1b01 < a01b1`**。

`x0<x00<x000<x1`：数值 0=0=0<1；零个数 1<2<3。`a1<a01<a001`：零个数 0<1<2。

## 传递性（第 4 条）

主序是「带类型的段序列」上的字典序（有序字母表 + 短前缀规则），是严格全序；次序是整数序列上的字典序，也是严格全序。整体键 =（主键, 零个数向量），两个全序的字典复合仍是严格全序：反对称、传递，且相等当且仅当两个键都相等。规范形序列加零个数可唯一还原原串，故键相等当且仅当 `a==b`。

## 非 ASCII（第 6 条）

字节 ≥ 0x80 都不是 ASCII 数字，归入非数字段按字节参与字节序；不做 UTF-8 解码故不 panic，全角数字 `１２` 也只是普通字节。切分仍是确定的划分，字节序在字节串上本就是全序，复合规则不变。

## 复杂度（第三条约束）

两串各只扫描一遍：逐段并行推进，内部计数器对每个被消费字节计 1，总计 ≤ len(a)+len(b) ≤ 2(len(a)+len(b))，不预切切片。

## 语义 ↔ 测试对照表

（实现完成后补齐）
