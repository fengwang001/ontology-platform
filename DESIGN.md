# 严格模式 Base64：非规范尾部推导

标准 Base64 把每 3 字节（24 bit）编码成 4 个 6-bit 字符；字节数不是 3 的倍数时，最后一组不足 24 bit，用 `=` 补齐：

- 余 1 字节：8 bit 装进 2 个字符（共 12 bit），组形如 `XX==`。第 2 个字符只有高 2 bit 承载源字节，**低 4 bit（6-bit 索引的 bit3..bit0）必须全为 0**。
- 余 2 字节：16 bit 装进 3 个字符（共 18 bit），组形如 `XXX=`。第 3 个字符只有高 4 bit 承载源字节，**低 2 bit（bit1..bit0）必须全为 0**。
- 余 0：4 个字符全部承载数据，不允许出现 `=`。

统一规则：**最后一个携带数据的字符，凡是没有被源字节占用的低位填充比特，必须全部为 0；且填充符只能是末尾的 `==` 或单个 `=`。**

样例判定：

- `QQ==`：Q=16=`010000`，低 4 位为 0 → 合法，字节 0x41（`A`）。
- `QR==`：R=17=`010001`，低 4 位为 1 → 拒绝。宽松解码器丢弃这 4 bit 也得到 `A`，但 `QR==` 不是任何字节串编码的产物。
- `QUI=`：I=8=`001000`，低 2 位为 0 → 合法，`AB`；`QUJ=` 中 J=9=`001001`，低 2 位为 1 → 拒绝。
- `QQ`/`QQ=`/`QQ===`：组长度/填充数量不属于上述三种形状 → 拒绝；`QQ==QQ==`：填充只允许出现在整个流的最后一组 → 拒绝。

与唯一性的关系：`Encode` 是字节串到字符串的单射。宽松解码把同一字节（如 `A`）映射出的 16 个串 `Q?==`（`?` 的低 4 位任意）全部映回 `A`，即接受集大于编码像集。严格模式只接受像集内的串，因此对任意被接受的 y，`Encode(Decode(y)) == y`，编解码互为逆映射。

## 语义与测试对照

| 语义条目 | 钉住它的测试函数 |
| --- | --- |
| 1 标准字母表与 `=` 填充（含 MIME 76 列） | `TestEncodeGolden` |
| 2 规范形式（非规范尾部按低填充比特判定） | `TestCanonicalTail` |
| 3 换行位置规则 | `TestNewlines` |
| 4 五类错误可区分且带字节偏移 | `TestErrorKinds` |
| 5 跨切分点一致 | `TestAllSplits` |
| 6 双向往返（MIME 开/关） | `TestRoundTrip` |
| 7 输出上限在组边界停下并进入终态 | `TestOutputLimit` |
| 计数器 == 输入字节数（不回扫） | `TestCheckedCounter` |
