# 属性级权限过滤器 — 设计推导

问题：按角色授予列可见性。当过滤谓词引用了对该角色不可见的列时，
有三种处理方案，必须选出唯一不泄露信息的一种。

## 甲：把不可见列的比较当作假 —— 泄露

探针查询：`NOT (secret = 1)`。
甲把 `secret = 1` 当假，于是 `NOT(false) = true`，**所有行都被返回**。
攻击者再发 `NOT (secret = 2)`、`NOT (secret = 3)`……：
当某个取值 v 使目标行从结果集中消失时，即推出该行的 `secret = v`。
逐值（或按范围二分）重复即可完整重建不可见列。结果集内容/行数
本身就是信息通道，"当作假"让谓词的求值结果依赖于秘密值。

## 乙：把不可见列当作 NULL 参与三值逻辑 —— 泄露

探针查询：`secret IS NULL`。
乙把不可见列读作 NULL，于是 `secret IS NULL` 恒真，**全部行被返回**；
而调用方对真正不含该列的查询也会得到同样的"恒真"——`IS NULL`
成为探测"某列是否存在且被隐藏"的探针，泄露列的存在性。
同时 `NOT (secret = 1)` 在三值逻辑下恒为 NULL、恒不返回行，
与"列不存在"或"没有匹配行"不可区分，语义被污染。

## 丙：整条查询拒绝执行并指出是哪一列 —— 不泄露（采用）

任何让谓词**求值结果**依赖于不可见列取值的方案都构成信息通道；
唯一安全的做法是根本不求值：返回可判定错误，指出列名与其在谓词
树中的路径。错误只回显调用方自己提供的列名（schema 中列的存在性
视为公开元数据，秘密是列的**值**），因此不泄露任何值信息。
本实现规定：谓词经常量折叠后仍引用任一不可见列，即整条拒绝，
错误可用 `errors.Is(err, filter.ErrInvisibleColumn)` 判定。

## OR 短路例外的定义与理由

定义：**先对谓词做常量折叠，再检查折叠后的树**。仅当折叠后的树
仍引用不可见列时才拒绝。被折叠消除的分支不再受检查。

理由：若 `OR` 的一支在常量折叠后可证为永真（如 `OR(TRUE, secret=1)`
→ `TRUE`），或一支永假使 `AND` 整体永假（如 `AND(FALSE, secret=1)`
→ `FALSE`），则该分支在求值中**永远不会被读取**，查询结果对
`secret` 的任何取值都完全相同——不存在信息通道，拒绝它是误报。
反之，折叠后仍残留的引用（如 `OR(FALSE, secret=1)` → `secret=1`）
位于某个求值路径上，其结果依赖秘密值，必须拒绝。
即：拒绝 ⟺ 不可见列出现在折叠后的谓词树中。

## 行裁剪

结果行只保留可见列：不可见列的键**从 map 中删除**，不置零值、
不置 NULL——否则"列不可见"与"列值为空"不可区分（乙的污染会
借此重新混入）。裁剪按可见列清单逐项拷贝，代价与可见列数成正比，
与行的总列数无关。

## 审计报告

报告含三项：被裁掉的列名集合（字典序）、被拒绝的谓词引用
（列名 + 全部路径，同列只报一次）、本次查询是否被整条拒绝。
所有集合在构造时排序去重，因此报告字节与构造顺序无关。

## 复杂度

- 谓词检查：常量折叠后对树做**一遍**遍历，访问节点数等于折叠后
  树的节点总数（无常量时即原树），不重复遍历。
- 行裁剪：拷贝次数 = 可见列数 ≤ 4 × 可见列数，与总列数无关。
