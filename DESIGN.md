# 设计推导

## 1. 非法单元切分（统一规则）
首字节立即确定期望长度 n；读第 2 字节时先判其是否落在该首字节专属区间：不合法则非法单元只含首字节 1 个，触发字节退回重做新首字节；第 2 合法后第 3 非法（须 80..BF）则单元含 2 字节、第 3 字节退回；第 3 合法后第 4 非法则单元含 3 字节、第 4 字节退回。流结束时仍未完成的合法前缀整体算 1 个单元（故 `E2 82`+EOF 是 1 个不是 2 个）。续行字节与 C0/C1/F5..FF 各自独立成 1 字节单元。
因此 `F0 90 80 41`：90 在 F0 的[90,BF]合法，80 不在第 3 字节区间[90,BF]，吞 3 字节，41 退回成 A；`E0 80 80`：80 不属[A0,BF]吞 1 并退回，后两个 80 各吞 1，共三个；`ED A0 80` 同理（A0 不属[80,9F]）；`C0 AF`、`F4 90 80 80` 逐字节退回共 2/4 个；`80 80` 两个。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 第2非法吞1(退回)；第2合法后EOF吞2 |
| E0 | A0..BF | 第2非法吞1(退回)，后续通用 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F（排除代理 D800..DFFF） | 同上 |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF（排除非最短） | 第3非法吞2退回 |
| F1..F3 | 80..BF | 4字节：第3非法吞2、第4非法吞3(均退回)；EOF吞已缓存数 |
| F4 | 80..8F（排除 >10FFFF） | 同 F0 |
| 80..BF | 无（孤立续行） | 吞1 |
| C0 C1 F5..FF | 无（非法首字节/非最短/越界） | 吞1 |

## 2. 缓存上限与提前放弃
最多缓存 3 字节：首字节加 2 个续行；第 4 字节到达时序列必有结论。看到越出专属区间的第 2 字节（E0 后 80..9F、ED 后 A0..BF、F0 后 80..8F、F4 后 90..BF）、续行位出现非续行、首字节为 C0/C1/F5..FF 时立即判非法，不等流结束，触发字节退回重判。只有“合法前缀遇 EOF”留给 Close，成为与非法字节可区分的截断。

## 3. par 切点对齐
切点 o 为字节偏移。从 o 向左回看常数字节：UTF-8 最多 3 字节内找到首字节 L 且 o-L 小于其声明长度，则段真正起点退到 L；UTF-16 先偶对齐，偶偏移处为低代理、或前 2 字节为高代理，则退 2。找不到覆盖者，o 即单元起点。工作者吃 [s,e) 外加 e 后最多 4 字节前瞻使跨尾单元闭合，配置 stopAt=e：起点偏移 <e 的单元归本段，≥e 静默停止不报截断。例 `C3|80 80`：回看命中 C3（声明 2），整字符归前段；后段独立从 80 开始，该 80 自成 1 个 FFFD，全局 1 合法 +1 非法，与单线程相同。前瞻字节由下一段重新处理，stopAt 保证归属唯一，每边界只看常数字节。UTF-16 高代理跨切点退 2 与低代理同段。BOM 仅第 0 段识别。

## 4. 上限断点
上限按编码后字节计（UTF-16 每标量 2 或 4，代理对容量不足 4 时整体不输出）。每标量编码前检查余量，放不下则不编码不吞单元，返回 ErrOutputLimit；n 恰为已完整输出单元覆盖的输入字节数。前次 Write 留在切分缓存的字节算“已消费未输出”，在闭合那次 Write 计入 n。调用方从 total+n 续传新实例，标量边界拼接，与一次转完逐字节相同。

## 5. 复杂度计数
解码器每读一个字节检查计数 +1，退回重判字节至多被检查 2 次，故任意切分 ≤2N；par 每边界前瞻 ≤4 字节（共 8 个边界），总量 ≤ N+8×常数。
