# DESIGN

## 流格式（整数均为无符号 LEB128 varint）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | magic=`0xC0FFEE`(varint) + version=`1`(varint) |
| 字面量段 | tag=0 + len(varint) + len 个原始字节 |
| 回指 | tag=1 + dist(varint, ≥1) + length(varint, ≥3) |
| 刷新标记 | tag=2（无载荷，块边界 / Flush） |
| 流尾 | tag=3 + origLen(varint) + checksum(varint, FNV-1a 64 位原文摘要) |

## 1. 切法无关（语义 3/4）

压缩器保留一段未决字节 `pend`，其字节尚未写入任何记录。每次 Write 把数据追加进 `pend`，
在 `pend` 上取位置 0 让匹配器给最长匹配 `(d,l)`。决策规则：只有当匹配不能被后续字节延长时才提交，
即 `l < len(pend)`（终点已被“失配”钉死），或达到窗口允许的最大长度；触及 `pend` 末尾的匹配一律延后。
提交一个位置后该字节离开 `pend`（字面量单独提交；回指一次性提交 `l` 字节），窗口随之前移。
因为“某位置最终能否匹配、匹配多长”只取决于已确定的前缀与未来追加的字节，与 Write 在哪里切开无关，
故输出字节流相同。`Flush` 强制把 `pend` 全部按“已知”处理（结尾匹配不再延长）并追加一个刷新标记，
故解压器能精确还原截至该点的输入；之后新数据仍能匹配已进窗口的旧数据。无新输入时 `pend` 为空，
仅抑制重复刷新标记即可做到第二次 Flush 零字节。

## 2. 重叠回指（语义 2）

块复制要求源区间与目标区间不重叠；当 dist<length 时，一次性 copy 会读到尚未写入的目标内存。
正确语义是“逐字节向前复制”：out[n]=out[n-d]，n 递增——产出的是周期串。实现上直接用该字节循环；
解压总字节受输出上限钳制，热点只在长回指，逐循环即一次 append/赋值，性能与内存都可接受，
也天然避免重叠 bug。

## 3. 两种“距离超限”（语义 7）

“超过已输出字节数”指流前缀里可引用的历史还不存在，是截断/伪造流的特征；
“超过窗口容量”指历史存在但按约定早已滑出环形缓冲，即便合法压缩器在“无限记忆”下也可能引用它，
所以必须是两类错误。压缩器只从窗口内取候选，故绝不会产生后者。并行块带预置字典时，
解压端在块开始前就把上一块末尾 ≤窗口容量 的字节装入窗口；“已输出字节数”指本次调用实际解压产出的计数
（炸弹上限按它计），而可引用距离 = 已输出 + 预置字典长度，二者语义都不混淆。

## 4. 并行确定性（语义 9）

块 i 的压缩输入只有“本块原文”与“上一块末尾窗口容量字节”（预置字典，只读）；匹配器是纯函数式状态，
不读取其他块、压缩结果或 goroutine 调度顺序，故 workers=1/2/4/8 产出逐字节相同。
拼接方式：整个流只有一个流头；每块压缩为若干记录（允许首块外的回指跨过块起点引用预置字典），
除紧邻流尾的最后一块外每块后写一个刷新标记；最后写一个流尾。刷新标记只影响解码器边界判定，
不改变输出字节，因此流式解压器能直接解开。
