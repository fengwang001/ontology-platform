# 可分裂二维网格索引 — 设计推导

## 1. 单元格与边界归属

每个格是轴对齐矩形，采用**左闭右开**：`[x0,x1) × [y0,y1)`。
点 `p` 属于格 `c` 当且仅当 `c.x0 <= p.x < c.x1 && c.y0 <= p.y < c.y1`。

- 分裂线 `mx=(x0+x1)/2` 上的点：`p.x == mx` 满足右子格的 `mx <= p.x`，不满足
  左子格的 `p.x < mx`，故只进右子格；y 方向同理，线上点只进上子格。
- 四个角点：左下角 `(x0,y0)` 属于本格；右上角 `(x1,y1)` 不属于本格
  （属于右侧/上侧相邻格，或越界拒绝）；左上、右下角按 x/y 各自的半开规则判定。
- 查询矩形 `r=[a,b)×[c,d)`：`p.x==a` 命中，`p.x==b` 不命中；y 同理。
  退化为线/点（`a==b` 或 `c==d`）时矩形无内点，按半开定义查不到点，合法返回空。
- `+0.0` 与 `-0.0` 经 `==` 判定相等，视为同一坐标。
- NaN 与任何边界比较均为 false，归属无定义，插入被拒绝并计入跳过数；±Inf 拒绝。

## 2. 分裂与递归终止

格内点数 `len > capacity` 时沿中点 `mx,my` 一分为四，原有点按下式全部下沉：
右 = `p.x >= mx`，上 = `p.y >= my`。父格标记 split，自身不再存点。

**为什么坐标全同会无限递归**：若所有点坐标完全相同，每次中点分裂后它们在比较中
永远落入同一个子格（同坐标点对 `mx` 的大小关系完全一致），该子格仍超容量，
若无条件继续分裂就永不终止、栈溢出。

**终止条件**（满足任一即停止继续下沉，允许叶格超容量）：
1. 中点不可分：`mx == x0 || mx == x1`（浮点耗尽，宽度为 0 或舍入后中点贴边），
   或 y 方向同理；
2. 深度达到 `maxDepth`（固定 48，远早于 float64 约 52 位尾数耗尽）。

因此 1000 个同坐标点最终停在一个“过满叶格”里，不丢不重，仍可点查与范围查到。
分裂前后整树点总数不变（点从父数组移入唯一子格，互斥归属）。

## 3. 删除与不合并

删除按点的唯一 id 在所属叶格移除。删除后**不合并**子格：合并需要在阈值附近反复
搬迁全部点，插入/删除交替会产生合并-再分裂抖动，并使并发查询的一致性快照更复杂。
不合并只损失少量剪枝效率，不影响正确性，故本实现不做合并。

## 4. 范围查询剪枝

DFS 树，对每格：相离（矩形不相交）→ 跳过；格完全被查询矩形包含 → 整取其全部点，
不做逐点判定；相交 → 叶格逐点判定，分裂格继续下钻。导出计数器记录访问格数与
逐点判定点数。

## 5. 并发

整树用一把 `sync.RWMutex`：查询持 RLock，插入/分裂/删除持 Lock。分裂在锁内
原子完成（建子格 → 搬点 → 置 split），查询看到的要么是分裂前要么是分裂后的完整
树，不会看到“点被搬走但子格不可见”的中间态，故不漏点；`-race` 干净。

## 6. 落盘格式

文件 = 固定头 + 逐格记录（DFS 先序）。
头：magic `"GRIDIDX1"`(8) + 根界 4×float64(32) + 容量 uint16(2) + 格数 uint32(4) = 46B，
头后跟头 CRC32(4)，头区共 50B。
格记录（定长头 20B + 点数据 + 两段 CRC32）：
depth u16(2)、flags u8(1，bit0=split)、nPts u32(4)、minX/minY/maxX/maxY 4×f64(32，
叶格为点集包围盒、分裂格为声明界)=39B；头 CRC32(4)=43B；点数据
`nPts×(u64 id + f64 x + f64 y)`；点数据 CRC32(4)。

**截断分类**（单文件逐字节截断 1..len-1，依据当前解析位置）：
- 落在文件前 50B 内 → 头不完整（ErrShortHeader）；
- 落在某格前 39B（格定长头）→ 格记录头不完整（ErrShortCellHeader）；
- 恰好落在格头 CRC 的 4B（39..42）→ 头 CRC 不匹配（ErrCRC，覆盖“CRC 不匹配”类）；
- 落在点数据内（43 .. 43+24n-1）→ 点数据不完整（ErrShortPoints）；
- 落在点数据 CRC 的最后 4B → CRC 不匹配（ErrCRC）。

恢复：返回已完整读回格构成的最大前缀（缺失子格的父格降级为空叶），并带分类错误。

## 7. 结构不变量

读回逐格校验：父格 split 时四个子格必须存在，否则 ErrMissingChild（指出格路径）；
子格声明界必须在父界内（逐维 `x0>=父x0 && x1<=父x1`，y 同），否则
ErrChildBounds（指出格 id 与越界维 x/y）。

## 8. 原子写与崩溃

先写 `name + ".tmp"`（含全部内容并 Sync），再 `os.Rename` 原子改名；启动时发现
残留 `*.tmp` 视为上次写盘中途崩溃，识别并删除清理。
