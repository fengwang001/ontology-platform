# 规范化器设计推导

策略记：`preserve`（原样，仅去 CR 与行尾空白）、`ensure`（末尾恰好一个 \n）、`trim`（空→""，非空→末尾恰好一个 \n）。

## 1 空输入与只含换行输入

不变量：N 幂等，输出无 CR、无行尾空白。

`ensure` 选「始终恰好一个 \n」：N("")="\n"。若选空输入→""，则定义变成「空或已以 \n 结尾则不动」，也幂等，但它无法把纯空白文件压成一个换行，且与 trim 对 "" 的行为重叠；选强制一个换行语义最单一，且 N("\n")="\n" 故幂等。

`trim` 选「空→""；非空→一个 \n」：空白全部落入行尾被删，"  \r\n" 整体被删空故为 ""；"\n"、"\n\n" 去掉末行后无内容，亦为 ""。N("")=""，N 作用于非空结果（均以 \n 结尾）保持不变，故幂等。

`preserve` 保留换行的"个数与有无"：空→""；"\n"→"\n"；"\n\n"→"\n\n"；"  \r\n"→"\n"（空白是行尾空白）。结果只含 \n，再作用不变。

| 输入 | preserve | ensure | trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2 被删字节的 ToOut

设输出区间与原文区间由段（等长段/纯删除段）覆盖。要求：ToOrig 单调不减、ToOut 单调不减，且对所有输出偏移 o，ToOut(ToOrig(o))=o。

`\r\n`→`\n`：\r 与 \n 都在原文指向唯一输出字节（\n 的位置 q）。ToOrig(q)=原 \n 偏移 j，故 ToOut(j)=q；\r 偏移 i<j 不能取 >q（会越过 \n 的映射），取 q 则 ToOut(i)=ToOut(j)=q，满足单调不减（被删字节是"坍缩点"，允许与下一字节同值）。

行尾空白同理：整串空白后面的 \n 在 q，整串空白（含其中每个偏移与终点）ToOut 都坍缩到 q。编辑器把光标置于被删空白中时，语义上它仍在"换行之前"（下一字符是换行），故映射到换行符位置而非其后；行尾之后的原偏移映射到 q+1。

规则统一：被删字节区间 [a,b) 的 ToOut 取"紧随其后第一个存活字节"的输出偏移；其后无存活字节（整段被策略删空）则取 len(out)。

## 3 空白缓冲超限

强制把长空白判定为"非行尾"并吐出：若后续真出现换行，这些空白本应删除却已输出，违反行尾空白语义（§2）、且破坏幂等（§5：输出中不能有行尾空白）与切分点一致（§4：整段输入走另一条判定路径）。故选择**拒绝**：返回哨兵 `ErrSpaceLimit`（带原文偏移），已输出保留，实例进入终态；再写返回 `ErrClosed`。拒绝是关于"全部输入"的确定事实——任何喂法下相同的超长空白串得到同一拒绝点，故不违反 §4/§5/§6（无结果需满足）。

## 4 par 拼接

每段用 fragment 模式独立规范化：Close 时不执行末尾策略，末尾待定 \r 与未判定空白作为"尾"原样保留（TailWS 个空白 + 可选 \r），其余为已决"头"；输出上限按段长放宽，缓冲上限按段长放宽，因此段内除 NUL 外不触发资源错误。

顺序拼接时维护 carry：前段尾部的空白。处理新段时：①其首字节为 \n 且 carry 后紧跟待定 \r（前段尾 \r），则 \r\n 配对——删 \r（原坐标段）并吞掉该 \n（删该段首字节，只删这一个：`\r\r\n` 里前一 \r 是独立行尾）；②否则待定 \r 独立成 \n。③carry 空白看"其后第一个非空白字节"：是 \r/\n/EOF 则删，否则原样吐回。该判定只需扫过当前段开头的空白，加一段尾，扫描量有上界（相邻两段尾+头）；K 个 goroutine 只写各自结果，拼接单线程做。

映射：每段头是规范化映射（等长/删除段），平移其原/输偏移；被吞的 \r 与 \n、被删 carry 空白插入对应原坐标的纯删除段。全局仍为段表，删除点总数与单线程一致。末尾策略在拼接完整体上执行一次。
