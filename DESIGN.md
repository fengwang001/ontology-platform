# 属性级权限过滤器：设计推导

## 1. 问题

角色只能看到部分列。过滤谓词树（列比较、`AND`/`OR`/`NOT`）可能引用不可见列。
目标：裁剪结果行，并在谓词"碰到"不可见列时安全拒绝，且全程可审计。

## 2. 三种处理方式

- **甲：不可见列的比较一律当假。** 泄露路径：`NOT (secret = 1)`。
  `secret = 1` 被替换成假，外层 `NOT` 翻转为真，查询照常返回行。
  攻击者据此可区分"列存在但不可见"与"列不存在"，并用 `NOT` 对隐藏列做探针；
  配合 `OR` 还可逐位推断隐藏值的布尔结构。不可取。
- **乙：不可见列当 NULL，走三值逻辑。** 泄露路径有两条：
  (1) `secret = 1` 变成 UNKNOWN，`NOT UNKNOWN` 仍是 UNKNOWN，行被过滤，
  攻击者通过"行消失"确认该列存在且被策略隐藏（存在性泄露）；
  (2) `secret IS NULL` 直接为真、返回行，成为对隐藏列的确定性探针。不可取。
- **丙：整条查询拒绝，并指出列名与路径。** 攻击者只能观察到"拒绝"这一个
  比特，无法区分列是否存在、值是什么；错误信息只暴露调用方本已写出的列名。
  **本实现采用丙**：谓词经常量折叠后仍引用任何不可见列，即返回
  `ErrInvisibleColumn`，错误中带列名与从根开始的子节点下标路径。

## 3. OR 短路例外（及一般化的常量折叠例外）

**定义**：对谓词做一遍自底向上的常量折叠；凡整个子树折叠成常量真/假，
该子树内的列引用视为"未被读取"，不报、不拒绝。折叠后仍残留的引用若指向
不可见列，则拒绝。该定义不限于 `OR`：

- `FALSE AND (secret = 1)` 折叠成 `FALSE`：放行。
- `TRUE OR (secret = 1)` 折叠成 `TRUE`：放行（`OR` 短路，右支不求值）。
- `FALSE OR (secret = 1)` 残留 `secret = 1`：拒绝（假支被丢弃后隐藏支仍需求值）。
- `NOT (FALSE AND (secret = 1))` 折叠成 `NOT FALSE = TRUE`：放行。

**理由**：常量折叠与 SQL 执行器的短路/矛盾化简语义一致；这些列在任何行上
都不会被实际读取，拒绝反而暴露了"该列存在且受保护"这一信息，比放行泄露更多。
例外必须以"可证明为常量"为前提，绝不基于统计或代价猜测。

## 4. 行裁剪

- 结果行只保留可见列；不可见列的键**整体删除**，不置零值、不置 NULL，
  使"不可见"（无此键）与"可见但值为 NULL"（键存在、值为 NULL）可区分。
- 谓词只在裁剪后的可见行上求值；投影按可见列清单遍历，代价与可见列数成正比，
  与行的总列数无关。

## 5. 报告与确定性

报告含：被裁列名集合（字典序）、被拒绝引用（列名+路径，按列名、再按路径排序，
同列多次出现只报一个列名但保留全部路径）、是否整条拒绝。输出为排序后的
规范化文本，因此输入 map 的构造/迭代顺序不影响字节结果。

## 6. 复杂度

- 谓词分析为一遍后序遍历：每个节点访问一次，同时完成折叠、引用收集与可见性判定；
  非导出计数器 `nodeVisits` 必须等于节点总数。
- 行投影仅遍历可见列清单，`copies` 等于实际拷出的键值数；千列五可见时
  `copies <= 4*5 = 20`。

## 7. 边界与穷举定义

空可见集（除纯常量谓词外一律拒绝）、全集（从不因列拒绝）、空谓词（不过滤）、
纯常量谓词（折叠后放行）、同列多次出现（列名去重、路径全留）。

三列 `c1 c2 c3`，四种谓词形态，各枚举八种可见性组合：

| 形态 | 模板 | 折叠后引用 | 放行条件 |
|---|---|---|---|
| `=` | `c1 = 1` | `{c1}` | c1 可见 |
| `NOT` | `NOT (c1 = 1)` | `{c1}` | c1 可见 |
| `AND` | `(c1=1) AND (c2=1) AND (c3=1)` | `{c1,c2,c3}` | 三列均可见 |
| `OR` | `(c1=1 AND FALSE) OR (c2=1) OR (c3=1)` | `{c2,c3}` | c2,c3 可见（c1 在折叠掉的假支中，不计） |

故各形态放行/拒绝数为 4/4、4/4、1/7、4/4，合计 **13 放行 / 19 拒绝**。
