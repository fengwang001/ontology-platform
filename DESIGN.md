# 可撤销批量重命名与冲突解析器 — 设计

## 1. 模型
- 命名空间 `name.Namespace`：名字到实体的集合（`map[string]struct{}`），空串是合法名字，
  `/` 等路径分隔符按普通字符处理；唯一非法情形见冲突 C3。
- 请求 `Req{Old, New}`；旧名必须当前存在；新名若已存在，则必须是本批另一个请求的旧名
  （它会被搬走），否则冲突。
- 命名空间自带 `sync.Mutex`；执行（含校验期间）全程持锁，任何并发修改在执行窗口内阻塞，
  由此定义“不允许并发修改”：并发调用等待，不产生交错写入。

## 2. 冲突分类（执行任何修改之前全部检测完）
- C1 `ErrTargetExists`：新名已存在，且不是本批任何请求的旧名。
- C2 `ErrDuplicateTarget`：两个请求 New 相同，错误列出两个 Old。
- C3 `ErrDuplicateSource`：同一 Old 出现两次，错误列出两个 New。
- C4 `ErrSourceMissing`：Old 在命名空间中不存在。
四类错误均为哨兵错误，用 `errors.Is` 区分；检测阶段只做只读查询，命名空间逐元素不变。

## 3. 排序推导（依赖图与拓扑序）
把每个请求看成有向边 Old→New。请求 x 可安全执行，当且仅当它的新名 New 不是任何
“尚未执行请求”的旧名；否则 x 会覆盖那个还没搬走的名字（a→b,b→c 先跑 a→b 即覆盖 b）。
因此：若请求 u 的 New 等于请求 v 的 Old，则 v 必须先于 u，即依赖边 u→v（u 依赖 v）。
对依赖图做拓扑排序；同一层（同为就绪）按旧名字典序排序，保证打乱输入后序列确定。
故 {a→b,b→c} 的顺序是 b→c, a→b。

## 4. 环的破解
环上每个节点的新名都被环内后继占用，拓扑序不存在。通用做法：对每个有向环任选一个节点
x（入边来自 prev，即 prev→x），取一个不冲突的临时名 t，把 x 的请求改写为 x→t 并追加
t→x.New：
- 二元环 a→b,b→a：a→t，b→a，t→b。
- 三元环 a→b,b→c,c→a：a→t，c→a，b→c，t→b（同层字典序）。
每一步执行时目标名都不存在，故中途无覆盖。
临时名数量：每个连通环恰有一个节点被拆，拆完该环变为无环链，故临时名数 = 环数。
临时名生成：形如 `.rename-tmp-N`，逐个 N 重试，要求既不在当前名字集合中，也不等于任何
请求的新名；超过 100000 次尝试返回 `ErrTempNameExhausted`（可判定错误，绝不覆盖）。

## 5. 自环语义
a→a：旧名存在、新名是自身（会被“搬走”再占回），定义为无操作（不进入执行步骤、
不写日志）。它不与任何其它请求形成有效占用关系（新名指向自身）。

## 6. 复杂度
冲突检测与排序：名字集合 O(名字数) 建好后，每个请求 O(1) 均摊，整体
O(请求数 + 涉及名字数)。`plan` 用非导出计数器 `lookups` 统计集合/索引查找次数，
约束 `lookups <= 4*(R+N)`。破环遍历每条边一次，临时名重试另计（非名字数规模）。

## 7. 执行、日志与撤销
- `apply.Executor` 持命名空间锁；第 k 步可注入失败（`FailAt`）：失败点不执行写入，
  随后按已完成步骤逆序自动回滚，结果与执行前逐元素相同。
- 日志写 `os.CreateTemp` 所在临时目录，二进制格式：
  魔数 4B `RLOG`、记录数 4B 大端；每条记录 = 长度前缀 4B 的载荷 + CRC32(IEEE) 4B；
  载荷为 `old\x00new\x00tmpFlag(0/1)`。
- `undo` 顺序读完全部记录得“最大可恢复前缀”：
  头不足 8 字节 → `ErrLogHeaderTruncated`；
  长度前缀/载荷被截断 → `ErrLogRecordTruncated`（含前缀正常但尾记录不全）；
  CRC 不符 → `ErrLogCRC`。
  只按可恢复前缀逆序撤销；临时步骤的逆序由 tmpFlag + 相邻记录配对还原
  （x→tmp 与 tmp→x.New 成对，逆序后仍还原原集合）。
- 重复撤销幂等：日志文件撤销成功后重命名为 `.done`，再次撤销无操作。
