# 属性级权限过滤器 — 设计推导

范围：按角色授予列可见性；谓词引用不可见列时的安全语义；行裁剪与审计报告。
不实现存储、索引、查询解析、HTTP、认证。状态全部在进程内存。

## 一、不可见列出现在谓词里：三种做法的推导

安全不变式：**响应只能是「查询 + 策略 + 可见数据」的函数，且不得对隐藏数据作出任何求值断言**。

### 甲：把不可见列的比较当作假 —— 泄露，弃用

- 探针 1：`NOT (secret = 1)`。甲下 `secret = 1` 恒假，故 NOT 恒真，**全部行被返回**，
  包括 secret 真为 1 的行。结果集的成员资格等价于系统对外断言「这些行 secret≠1」，
  其中对 secret=1 的行是假断言。调用方若把「被返回」当作事实记录或关联，即消费了
  关于隐藏列的伪造求值结果——响应不再是「查询+策略+可见数据」的纯函数，而是混入了
  对隐藏数据的断言。
- 探针 2：`secret = 1`（恒空集）与 `NOT (secret = 1)`（恒全集）配对，交集为空、并集
  为全表，调用方据此确知谓词被置假，机制暴露。
- 探针 3（存在性枚举）：`nosuchcol = 1` 报「列不存在」而 `secret = 1` 静默为假，
  两种响应可区分，调用方逐个试探即可枚举出全部「存在但不可见」的列名。

### 乙：把不可见列当作 NULL 参与三值逻辑 —— 泄露，弃用

- 探针 1：`secret IS NULL`。乙下不可见列读作 NULL，IS NULL 恒真，**返回全部行**；
  而 `nosuchcol IS NULL` 报「列不存在」。IS NULL 成为直接的存在性探针，泄露 schema。
- 探针 2：`secret = 1` → NULL → 空集；`NOT (secret = 1)` → NULL → 空集。两个互补
  探针同返空集，这一指纹同样暴露「置 NULL」机制，且 `secret IS NOT NULL` 恒空集，
  与 IS NULL 配对再次确认列存在。
- 三值逻辑把「不可见」与「值为 NULL」混为一谈，违反「不可见 ≠ 空值」的可区分性要求。

### 丙：整条查询拒绝并指名列 —— 采用

- 谓词引用任何一个对该角色不可见的列，即返回可判定错误，指出列名及其在谓词树中的
  路径，**不求值、不返回任何行**。
- 论证：错误响应只依赖「查询 + 策略」——两者皆为调用方已知（列名是调用方自己写进
  查询的）；响应不接触任何行数据，因而不可能编码隐藏数据的信息。既无甲的假断言，
  也无乙的存在性探针。指名列名不泄露调用方未知的信息。

## 二、OR 短路例外的定义与理由

定义：**先做常量折叠，再检查可见性**。折叠规则：`OR` 任一支为 TRUE 则整式 TRUE、
任一支为 FALSE 则化去该支；`AND` 对偶；`NOT` 常量取反。折叠后谓词树中仍出现的
不可见列引用才报错；被折叠整支消去的引用不再报错。

- 放行例：`TRUE OR (secret = 1)` → 折叠为 `TRUE`，secret 实际未被读取，结果恒真、
  与隐藏数据无关，放行。`FALSE AND (secret = 1)` → `FALSE`，同理放行。
- 仍拒例：`FALSE OR (secret = 1)` → 折叠为 `secret = 1`，引用仍在，拒绝；
  `TRUE AND (secret = 1)` 同理。
- 理由：安全不变式针对的是「求值实际读取的数据」。折叠后的树才是求值语义，
  其中无不可见列引用 ⇒ 响应是「查询+策略+可见数据」的纯函数，不变式成立。
  反之若不做此例外，会把可静态证明不读隐藏列的查询也拒绝，可用性受损而无安全收益。

## 三、行裁剪与报告

- 行裁剪：结果行只保留可见列，不可见列**从 map 中删除**（不置零值、不置 NULL），
  使「不可见」与「值为空」可区分。实现按可见列集合迭代查表，代价与可见列数成正比，
  与总列数无关。
- 谓词检查：一遍遍历，非导出计数器记录访问节点数，等于（折叠后）谓词树节点总数。
- 报告：被裁列名集合（字典序）、被拒谓词引用（列名 + 全部路径，同列只报一次）、
  是否整条拒绝；输出前统一排序，保证与构造顺序无关、逐字节确定。
- 三类错误用 `errors.Is` 区分：`ErrUnknownRole`、`ErrInvisibleColumn`、
  `ErrInvalidPredicate`。
