# 设计：只追加日志的段索引与范围回放

## 1. 磁盘格式（全部信息来自段文件）
- 段文件 `seg-NNNN`：段头 = 魔数(4) + 版本(1) + 首序号(uvarint) + 事件数(uvarint)。
  事件数是写入提示；活动段可为旧值，顺序读以实际可解出的记录为准。
- 每条记录：载荷长度(uvarint) + 序号(uvarint) + 载荷 + CRC32(IEEE，覆盖序号+载荷)，4 字节大端。
  长度前缀只计载荷长度；序号冗余写入使每条记录自描述，可校验段内连续、可重建索引。
- 索引文件 `seg-NNNN.idx`：魔数(4) + 锚点间隔 N(uvarint)，重复 {序号 uvarint, 段内字节偏移 uvarint}。
  索引不含任何段里没有的信息，故可逐字节重建。

## 2. 定位规则：为什么是「不大于 from 的最大锚点」
锚点把段切成区间：锚点 a_k 指向 seq a_k 那条记录的起点。要找 from，
必须从某个 seq ≤ from 的锚点起步再向后顺序扫描，否则会越过目标。
- 「最接近 from 的锚点」可能是 a_{k+1} > from：它位于目标之后，段文件只能
  顺序向前读，无法后退，从那里扫会漏掉 from 及之前的事件。故取
  `max{ a | a.seq <= from }`。
- from == 某锚点：直接定位，跳过 0 条。
- from 在两锚点之间：从前一个锚点扫，跳过事件数 ∈ [1, N-1]，严格 < N。
- from < 首个锚点：无锚点可用，从段头后偏移 0 开始全段扫描（仍 < N，
  因为第一个锚点在第 N 条即下标 N-1，之前最多 N-1 条；视为未用上索引）。

## 3. 索引重建与失效回退
重建：从头顺序读段，每读出第 i 条（i 从 0 起）且 i%N==0，
追加 {该记录序号, 该记录起始字节偏移}。输出格式与写时完全一致，
因此与原索引逐字节相同。
失效检测：按锚点偏移 seek 后，必须解出合法长度前缀且记录 CRC 正确、
序号与锚点一致。任一不成立即判定索引被篡改/过期，丢弃索引改用
「从段首顺序扫描」定位，并在结果报告里置 `IndexStale=true`。

## 4. 段边界与跨段回放
段按首序号排序。不变量：`seg[k].first + seg[k].count == seg[k+1].first`；
违反则 repair 报告缺口区间 `[first+count, nextFirst-1]`。
回放 [from,to]：选首序号 ≤ from 的最后一段为起点，逐段顺序读，
收集 from≤seq≤to，超过 to 即停；段尾自然衔接下一段首，不重不漏。
边界语义：from>to 返回 ErrInvalidRange；from<最小序号从头开始不报错；
to>最大序号放到末尾不报错；空日志回放为空；空载荷合法。

## 5. 复杂度计数
- `skipped`：定位阶段从锚点（或段首）到 from 之前丢弃的事件数，断言 < N。
- `bytesRead`：通过每段独立 Reader 实际读取的字节计数；回放结束点不超过
  「to 所在记录尾」与「from 所在锚点区间尾」二者较大值，故
  `bytesRead ≤ 区间记录字节和 + 一个锚点区间(N 条记录)字节`。

## 6. 损坏分类（四类，errors.Is 可区分）
按截断点字节位置分类：
- 头部区间 → ErrTruncatedHeader（头不完整）。
- 某记录长度前缀 uvarint 未完成 → ErrTruncatedLength。
- 长度已知但载荷/序号/CRC 不足 → ErrTruncatedBody。
- 完整记录但 CRC 不符（翻转字节，非单纯截断）→ ErrCRCMismatch。
截断恢复：顺序读到第一处坏帧即停，得到最大可恢复前缀；
repair 把段头事件数改写为实际可读条数并截断尾部垃圾，改后顺序读无错。

## 7. 并发
追加写对「写头事件数 + 追加帧」加 mutex；回放只依赖 OS 对已落盘前缀的
读，且在帧边界由长度+CRC 自校验：半帧/尾帧直接当作段尾（活动段宽松读），
因此任何时刻回放看到的都是某个一致前缀。索引重建/读取同样持锁。
