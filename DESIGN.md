# 设计推导

规范化是字节级单调函数：输出字节全部来自原文字节（孤立 `\r`→`\n` 复用该 `\r`；`\r\n` 复用原 `\n`），不合成新字节；行尾空白仅删除。故映射 = 若干等长「身份段」+ 删除点。

## 1. 末尾换行策略（样例表）

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

- **Keep**：尾部不做任何事。规范化后输出已无 `\r`、无尾随空白，再规范化逐字节不变，幂等。
- **EnsureOne**：空输入取 `""` 而非 `"\n"`。推导：若 `N("")="\n"`，幂等要求 `N("\n")=N(N(""))="\n"` 尚可，但 `""→"\n"` 凭空创造了一个行，且任何只含空白的行尾串（如 `" \n"`）都规范化为 `"\n"`，于是 `N` 把两个不同的幂等候选压到一起，空文件无法区分；更关键的是迭代下 `"\n"` 是不动点而 `""` 不是，空文件是合法规范文件，应自映射。定义：输出非空则末尾恰好一个 `\n`（删多余尾 `\n`，无则补一个），空则空。验证：`"abc"→"abc\n"`，`N("abc\n")="abc\n"`，两步后为不动点；`"\n\n"→"\n"`，同样收敛。
- **TrimBlank**：删除末尾全部连续 `\n`；删后若还有内容则补恰好一个 `\n`，否则 `""`。输出的末尾要么无 `\n`（前面是非 `\n` 字节且补零个）、要么恰好一个且其前一字节非 `\n`，第二次 Trim 不删除任何东西，幂等。与 EnsureOne 的唯一差别是无尾换行的非空输入（`"abc"`）：TrimBlank 不补。

## 2. 被删字节的 ToOut

不变量只要求 `ToOut(ToOrig(o))==o`（仅约束输出位置的原像）与双方单调不减，被删原偏移的取值需由单调性补全。

定义：被删位置 `i` 的 `ToOut(i)` = 其后第一个未删字节 `j` 的 `ToOut(j)`；若无后继（删除点在文件末尾），取 `len(out)`（终点）。

- 这是删除区间上唯一自然的「右缘吸附」：删除区间内所有点共享后继锚点，保证区间内平台、区间外严格身份，整体单调不减；取左缘会在 CRLF 处给出 `\n` 之前的位置，而该位置是上一行的尾，光标会错行，故排除。
- `\r\n` 的 `\r`：吸附到原 `\n` 的输出位置，即行尾换行符（也是下一行起点）。
- 行尾空白：吸附到行尾 `\n` 的输出位置——光标落在换行符之前的行尾，即下一逻辑行起点，符合「删掉尾随空白后光标跳到行尾」。
- 文件末尾被删空白：无后继，吸附到输出终点。
- 终点按定义域显式支持：`ToOrig(len(out))=len(in)`，`ToOut(len(in))=len(out)`。

## 3. 空白缓冲超限

选择**拒绝**：返回哨兵错误（具原偏移），该 `norm` 进入终态，后续 Write 返回终态错误；已输出内容与映射保留（等于「已确认前缀」的规范化结果），Close 同样得到该前缀结果。

排除「强制判定为非行尾并输出」：一旦空白后面真出现行尾，这些字节本应删除却已输出，违反语义 2 与幂等（输出再规范化会变短），且决策不可撤回。拒绝不违反 4/5/6——它不产出错误的规范化结果；截断遍历中 Close 的语义是「对已喂入前缀整体规范化」，而超限发生在尚未判定的待定缓冲上，缓冲区从未进入输出，故已确认输出与映射仍自洽可解释。

## 4. par 拼接

- 每块独立用 Keep 规范化（Keep 无尾部歧义），并暴露 `Residue()`：块末待定数据，长度有上界——至多 1 个待定 `\r`，或 ≤ bufCap 的尾随空白（二者不共存）。
- 顺序拼接：把上一块 residue 与当前块第 1 个字节重新做一次行尾/空白判定（只需当前块首字节即可决定待定 `\r` 是否与 `\n` 配对、待定空白是否被行尾终结），修正范围 ≤ bufCap+1，与块大小无关；其余已输出字节与身份段原样平移。
- 坐标平移：身份段记为 `(orig0,out0,len)`，块贡献原始区间 `[start,end)`；拼接时 `orig0 += start + 段内偏移修正`、`out0 += 输出累计`，删除点随段边界自然表达。修正区丢弃旧段、换入重判定产生的段，K、切点任意结果相同。
- 全部块拼完后再应用末尾策略（只删尾 `\n`，不合成字节），故空输入三策略仍按第 1 节。
