# Quoted-Printable 设计

## 行尾空白推导

输入中的空格或制表符先暂存；只有看到后续普通字节时，才能证明它不在逻辑行末尾。
遇到 `\n` 或输入结束时，暂存空白位于换行前或 EOF 前，因此必须编码为 `=20` 或 `=09`。
`\r\n` 整体识别为换行；单独 `\r` 没有换行语义，按普通字节编码成 `=0D`。

## 软换行推导

每个输出 token 的宽度是 1 或 3：普通字符宽 1，`=XX` 宽 3。若当前列加 token 宽度超过 76，
先输出 `=\r\n`，再输出完整 token，因而不会拆开 `=XX`。软换行的 `=` 位于第 76 列，计入宽度。

若软换行恰好插在一个字面空格之后，解码只删除 `=\r\n` 三个字节，不会删除前面的空格；
该空格会留在一个物理行末尾，严格解码器又必须拒绝行末未转义空白。因此该空格不能字面输出，
必须写成 `=20`（制表符写成 `=09`）。这不是多余转义，而是严格解码所要求的最小转义。

## 往返等式

设 `N(x)` 把输入中的 `\n` 和 `\r\n` 统一规范化为 `\r\n`，单独 `\r` 保留为字节 `\r`，则：

`Decode(Encode(x)) == N(x)`

对不含裸 `\n` 的输入，或把“换行规范化”计入语义时，编码再解码保持信息不变。

编码器只转义 `=`、33–126 之外的字节、逻辑行末空白，以及为避免落在物理行末而必须转义的空白；
其余可打印字符与空白均原样输出，所以是最小转义。严格输入中若有额外 `=XX`，解码后再编码只会
因这些多余转义而不同。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 转义与换行规范化 | `TestEncoding` |
| 2. 76 列与软换行等号计数 | `TestLineLimit` |
| 3. 行末空白与转义序列不可拆分 | `TestWhitespaceAndTokens` |
| 4. 五类严格解码错误及偏移 | `TestDecodeErrors` |
| 5. 任意切分点一致 | `TestDecoderSplits` |
| 6. 往返与严格输出 | `TestRoundTrip` |
| 7. 幂等与最小转义 | `TestIdempotence` |
| 复杂度：输入检查计数 | `TestCheckCounter` |
