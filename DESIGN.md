# 游标分页器设计（keyset pagination）

## 1. 复合游标边界的推导

行 = `(Score float64, ID string)`，ID 全局唯一。全量顺序按
`(Score, ID)` 字典序：先比 Score，相等比 ID（字符串字节序）。

反例（10 行，6 行 Score=1，ID a..f，每页 3 行）：
- 游标只记 Score：第一页取到 Score=1 的 `a,b,c` 后，下一页若取
  `Score > 1`，则 `d,e,f` 被整批跳过（漏）；若取 `Score >= 1`，
  则 `a,b,c` 又出现（重）。单值游标无法区分"同值但已翻过"的行。

正确做法：游标记录上一页最后一行的复合键 `(Score, ID)`，下一页
取严格复合大于它的前 N 行：

```
(s,id) > (s0,id0)  ⟺  s > s0 ∨ (s = s0 ∧ id > id0)
```

因为 ID 唯一，复合键是全序，严格大于的切分既不重也不漏。

## 2. 正向与反向翻页

- 正向：从游标键之后按正向序取 N 行，返回的游标指本页最后一行，
  方向标记 forward；页内顺序即正向序。
- 反向：从游标键之前按反向序取 N 行（即取游标之前最小的 N 行，
  等价于"正向取小于游标的 N 行后整体保持正向排列"），再把这 N
  行按正向序输出。返回游标指本页最前一行，方向标记 backward。
- 反向不是把正向结果倒序：比较方向与取数方向反转，但**页内顺序
  仍为正向序**。正向第 3 页的游标反向取一页，逐行得到正向第 2 页。
- 游标自带方向字节；forward 游标用于 backward 取页（或反之）报
  `ErrWrongDirection`，绝不静默按另一方向处理。
- 每页结果同时带两个导航游标：`Next`（forward，指本页末行）与
  `Prev`（backward，指本页首行）。反向导航使用上一页结果提供的
  `Prev`；因此"正向第 3 页"的 `Prev` 反向取页逐行得到第 2 页。

## 3. 游标编码（不透明）

```
ver(1) | dir(1) | raw(8=Score IEEE754) | idLen(2 BE) | id | checksum(4)
checksum = crc32(castagnoli) 覆盖 ver..id
```

空字节串合法，表示从数据集起点开始（方向取调用方指定方向）。
解码按顺序区分：版本/长度不符 → `ErrCursorMalformed`；dir 非法 →
`ErrBadDirection`；crc 不符 → `ErrChecksum`；跨方向使用 →
`ErrWrongDirection`。四类错误均可用 `errors.Is` 判定。

## 4. 并发增删下的语义

存储用 `sync.RWMutex` 保护，元素为 `(Score,ID)` 全序切片，靠
`sort.Search` 做 O(log n + 页大小) 的键集定位，绝不从头全扫。

翻页是纯函数：输入数据集快照 + 游标，输出一页；游标不可变，
同一游标被多协程并发使用得到逐行相同的页，无隐藏推进状态。
- 行插入到已翻过的键区间：允许它不出现（不承诺快照隔离）。
- 已返回过的行绝不会因翻页再次出现（严格键边界）。
- 未删除的行不会在其复合键区间内被跳过；游标只描述键位置，
  指向的行已删除时仍可正常续翻（不依赖该行存在）。
- 被删除的行不再出现。

## 5. 可观测与自检

`page` 用非导出计数器记录本次取页实际比较过的行数，仅按精确
比较计数，供 `audit` 读取并与 `4*(N+ceil(log2(n)))` 上界对比。
`audit` 用连续翻页对数据集做全量遍历，校验不重不漏，并提供
并发增删期间"已返回行不重复"的检查。
