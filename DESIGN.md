# DESIGN

## 1. 零计数 hunk 头的行号

unified diff 头 `-a,b` 里 a 是该 hunk 在旧文件中的**起始行号（1 起）**，b 是旧侧行数；
新侧同理。插入发生在旧文件第 g 行与第 g+1 行之间，hunk 旧侧为空（b=0），
GNU 规定 a 取"前一行"的行号 g；文件最开头插入时没有前一行，取 0。
三个样例：文件头插入 → `-0,0`；在第 2 行后插入 → `-2,0`；整文件删除 → `+0,0`。
Apply 看到 b=0 时把插入点放在旧文件第 a 行**之后**（a=0 即最前面），不从第 a 行删任何行。

| 旧侧 | 书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`，a=上一行行号（无则 0） | 在第 a 行之后插入，不删除 |
| b=1 | `-a`（省略 `,1`） | 替换/删除第 a 行 |
| b≥2 | `-a,b` | 覆盖第 a..a+b-1 行 |

## 2. hunk 合并阈值

阈值是 **2C**（间隔的纯上下文行数 g ≤ 2C 则合并）。每个 hunk 两侧各带 C 行上下文，
两 hunk 间隔 g 行未改动内容；各自的 C 行上下文合计 2C：g ≤ 2C 时上下文窗口相接或重叠，
合并为一个 hunk（中间只出现一次）；g = 2C+1 时中间至少剩 1 行不属于任一侧窗口，必须分开。
反例：取阈值 C，则 GNU `-U3` 对相隔 4 行的两改动会输出两个 hunk，而 C 规则错误地分开不了
（GNU 实际合并）；取 2C+1，则相隔 2C+1 行时错误合并，与 GNU 的两个 hunk 不符。
实现上先算出每个改动的覆盖区间 [start-C, end+C]，相邻区间重叠/相接即合并。

## 3. 并列最短脚本的选择

Myers 每条 furthest-reaching 路径上，D 为奇数时可"向下（删除）"或"向右（插入）"。
本实现固定：同一对角线上两候选到达位置相同时**优先删除（向下，k-1）**，即
`k == -d || (k != d && V[k-1] < V[k+1])` 时取 k+1（插入），否则取 k-1（删除）。
因此 `a b → b a` 的 hunk 内固定排列为先 `-a` 后 `+a`（删除行整体排在前），
不依赖 map 遍历顺序，补丁文本逐字节确定。

## 4. 偏移应用与后续 hunk 的平移

在记录位置附近 [-F,+F] 行内按"上下文+删除行"精确匹配候选；距离取 |实际起点−记录起点|，
最近优先，距离相同取靠前（offset 较小）。某 hunk 以偏移 k 应用、且其新侧比旧侧多 s 行
（s=插入数−删除数）后，旧文件坐标到当前文本坐标的总漂移为 k+s：其中 k 是该 hunk 匹配
位置的偏移，s 是应用它造成的行数变化。故下一 hunk 的查找中心 = 其记录起点 + 上一 hunk 的
(k+s)；搜索窗口仍是中心 ±F。这样若前一 hunk 因偏移 +5 找到、且自身增 2 行，下一 hunk 的
记录位置在当前文本中实际位于 +7 处，平移后才能在窗口内命中。

## 5. 其它约定

行保留原始行尾：`\n`、`\r\n`、末行无尾；`\r` 视为内容，渲染时补 `\n`，往返逐字节。
计数器统计 Myers 在对角线上前进的总步数（含 snake 逐行比较），超距离上限立即停止。
