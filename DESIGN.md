# 设计推导

## 1. 死信边界：第 m 次投递超时后立刻进死信

约定：投递次数 = 消息被 `Receive` 取出的次数；`MaxReceives = m` 表示最多投递 m 次。

第 m 次投递超时的那一刻，投递计数已经是 m。

- 方案 A（采用）：截止刻直接 `在途 -> 死信`，不再回到可见队列。实际投递恰好 m 次；
  进死信刻 = 第 m 次投递刻 + visibility；`Stats` 投递计数 = m。
- 方案 B（否决）：先变回可见，等第 m+1 次 `Receive` 才判超额。这要么真的发生了第
  m+1 次投递（违反"最多 m 次"，且投递计数变成 m+1），要么需要在 Receive 里拒绝它
  （此时它仍占可见槽位、破坏 FIFO，且进死信刻被推迟到下一次 Receive）。

m = 1,2,3 的时序（R=Receive，T=超时刻，D=进死信刻；从刻 0 发送、刻 1 起每次取出、
visibility=2）：

| m | 投递刻 | 进死信刻 |
|---|---|---|
| 1 | R1@1 | D@3 |
| 2 | R1@1,T@3,R2@4 | D@6 |
| 3 | R1@1,T@3,R2@4,T@6,R3@7 | D@9 |

## 2. 迟到 Ack 与超时的同刻竞态：先超时，后 Ack

可见性区间为左闭右开：收据在 [Receive刻, Receive刻+visibility) 内有效，截止刻本身
已失效（截止刻必须能被取到，见语义 3）。

时钟是离散注入的：`Tick()` 把时钟推进一刻并在该刻先统一处理所有 `deadline <= now`
的在途消息（重投或进死信），收据随状态迁移作废旧世代。此后在同一刻执行的
`Ack/Extend` 只按"当前世代"校验，必然拿到 `ErrStaleReceipt`。

反序（先 Ack 后超时）会让截止刻的 Ack 成功：消息被确认，而它本应在该刻重新可见——
对 m=1 的消息这等于静默丢消息（既不重投也不进死信），违反守恒之外的可见性保证；
即便允许它确认，也与"截止那一刻已可被取到"矛盾。故固定顺序为：**先到期迁移，再
处理同刻操作**。
