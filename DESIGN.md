# 通配模式匹配器设计推导

## 0. 表示约定

路径与模式都**只按字节切分、不做任何规范化**：`split(s)` 按 `/` 切成段，
保留空段。故 `""`→`[""]`，`"x/"`→`["x",""]`，`"//"`→`["","",""]`。
码点是匹配原子：非法 UTF-8 字节记为码点 U+FFFD，但比较时用**原字节**，
故非法字节只与自身相等；区间按 U+FFFD 的码点序处理。

## 1. `**` 与边界推导

定义：普通段内 `*` 匹配**零个或多个非 `/` 码点**，因此能匹配空段 `""`，
但段内没有 `/`，故不跨段。

`**` 仅当独占一段时表示「零个或多个整段」。把整段 `**` 定义为：它吃掉
**任意数目（含 0）的相邻段，每个被吃段都必须可被单个 `*` 匹配**，即由
非 `/` 码点组成（空段也算，因为 `*` 可匹配空串）。模式与路径都已是段
序列，匹配就是段序列匹配，且不做规范化。

由这一条规则推出：

- `a/**/b` 吃 0 段即 `a/b`，吃 n 段即 `a/x/.../b`，故 `a/x/y/b` 命中。
- `x/**`：吃 0 段时模式要求路径恰好是 `["x"]`，故匹配 `x`；`x/` 是
  `["x",""]`，末尾空段可被吃（它可由 `*` 匹配），故**匹配 `x/`**；
  `x/y` 吃 1 段，匹配。
- `**/x`：吃 0 段即 `x`，匹配；`/x` 是 `["","x"]`，首空段可被吃，匹配。
- 空路径 `""`：`*` 是一段、匹配空段，**匹配**；`?` 需恰好 1 码点，不
  匹配；独占段的 `**` 吃 0 段，**匹配**（任意段数含 0）；与他字相邻的
  `**` 退化为单个 `*`，仍不跨段。

## 2. 结果表

| 模式＼路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `*` | 是 | 是 | 否(跨段) | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |

## 3. 无回溯算法

**段内**（无 `*` 时）顺序对齐；遇到 `*` 记一个回退点 `(star,p)`：先试
零宽，失配则让最近星号多吃一个码点再重试。只保留**最后一个**星号的回
退点即可：若存在 `P1*..P2*..X` 且 X 失配，任何可行匹配都可把 X 前面那
个码点改由更近的 `P2*` 吃掉（`*` 可吃任意码点、两星之间的部分在旧回退
尝试中已对齐），从而存在一个只用最近星号回退的可行匹配——丢弃更早星号
不会漏解。每个码点至多触发一次前进，故段内 O(模式×路径)。

该论证**不能照搬到 `**`**：`*` 吃的是同段码点、单位同质；`**` 吃的是
整段，而相邻非星段要与路径段做**整段相等/模式匹配**，被吃段数不同会
改变后续所有段的对齐，单一回退点无法覆盖（例如多段都含相同前缀时）。
故 `**` 用**段级 DP**：`ok[i]` = 处理完前 i 个路径段是否可达；普通段做
一次转移，`**` 段做「保留自身 + 向下一模式段传递」的闭包。每个
`(模式段,路径段)` 组合至多计算一次段内匹配，总体 O(P·N)（P=模式字节，
N=路径码点）。引擎计数器每完成一次「原子 vs 码点」比较加 1。

## 4. 规则集版本一致

`set` 持有不可变的 `*ruleset` 指针，替换时在锁外编译新集（含上限校验），
成功后加锁一次性原子换指针；编译失败直接返回错误，旧集从未被触碰。
每次查询/Explain 开始时取一次快照指针，之后全程只用该快照，故结论、
序号、原文必属同一版本。

## 5. 错误与上限

四类可区分错误：`ErrUnclosedClass`（偏移 `[`）、`ErrReversedRange`（偏移
起端码点）、`ErrTrailingSlash`（偏移 `\`）、`ErrEmptyClass`（偏移 `[`）。
可配置：`MaxPatternBytes`、`MaxDoubleStarSegments`、`MaxRules`，超限各自
哨兵错误；规则集替换超限/编译失败保留旧版本。
