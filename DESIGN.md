# 带依赖的配置解析与环境覆盖器 — 设计推导

## 1. 分层与优先级

四层，优先级从低到高：`Default(1) < File(2) < Env(3) < Flag(4)`。
每层产出规范化条目：`{Key 键路径, Raw 原始字符串, Layer 层标记}`。
键路径用 `.` 分段；空键、空段（`a..b`）在规范化阶段即可判定错误；
深度 100 的路径合法。空串值合法，且必须与"键不存在"可区分（条目存在 vs map 无键）。

## 2. 先合并还是先展开（核心）

反例（先展开后合并）：文件层 `greeting=hello ${name}`、`name=file`，
环境层只覆盖 `name=env`。若在每层内先展开，文件层会先把 greeting 固化成
`hello file`；合并时环境层没有 greeting，于是最终值是过期的 `hello file`。

正确顺序：**先合并出最终的键→原始值表（每层只保留最高优先级原始值），
再对最终表统一做引用展开**。这样 `${name}` 在展开期看到的就是 env 的 `env`，
得到 `hello env`。溯源记录在合并之后、展开之前落定，因此报告同时持有
原始值（`hello ${name}`）与展开值（`hello env`）。

## 3. 引用语法与转义

扫描原始字节：
- `$${` → 字面量 `${`（`$$` 始终吞掉一个 `$`）；
- `${path}` → 引用，`path` 为非空键路径，引用到不存在的键是可判定错误；
- `${` 后找不到闭合 `}` → 可判定错误，错误中给出起始 `$` 的字节偏移；
- 其余字节原样保留。`${a}${b}` 连续两处引用各自展开。

## 4. 环检测

以"引用"为有向边做 DFS，维护当前递归栈。进入键压栈，离开弹栈；遇到已在
栈中的键即成环，环路径取栈中该键到栈尾再补该键（首尾闭合），覆盖
`a=${b},b=${a}` 与自引用 `a=${a}`。

## 5. 展开缓存（避免指数爆炸）

每个键只在"首次访问"时完整展开一次并缓存结果；后续引用直接取缓存。
替换计数只统计首次展开期间执行的引用替换。对
`k0=x`、`k(i)=${k(i-1)}${k(i-1)}` 到 k20，总替换数恰为
`1+2+...+20=210`，朴素重算为 2^20 量级。上界：
`4 * 键数 * 平均引用数`（平均引用数=引用边总数/键数）。

## 6. 溯源

合并时为每个键保留它出现过的所有层（含每层原始值），按优先级从低到高
输出；最高层即最终来源。键按字典序输出，保证逐字节确定性：
层集合固定排序、map 遍历一律排序，打乱四层来源构造顺序结果不变。

## 7. 环境变量名映射与歧义

映射：键路径每段转大写、`.` → `_`（`a.b.c` → `A_B_C`）。
逆映射：`_` 是段分隔符；字面下划线在键段中写作 `__`，即 `__` 逆映射为
段内单个 `_`。于是 `A__B_C` 也映射到键 `a.b.c`。若同一键的两种编码
同时存在于环境中，则来源有歧义，报可判定错误（AmbiguousEnvName）。

## 8. 文件格式与截断分类

UTF-8 文本，行格式 `key=value`，以 `\n`（或 `\r\n`）结尾；`#` 起始为注释。
对前缀截断 `content[:n]`（1 ≤ n < len），在最后一个不完整行上分类：
- 该行既无行尾、也无 `=`：KeyIncomplete（键不完整）；
- 有 `=` 但无行尾：ValueIncomplete（值不完整）；
- 有行尾但其后还有残留字节（最后一行被拦腰截断）：LineIncomplete（行不完整）。
行号从 1 起。未截断时解析成功。

## 9. 错误分类（errors.Is）

四个哨兵：`ErrTypeMismatch`（键名/期望类型/实际值/来源层四要素）、
`ErrMissingRequired`（一次报全部缺失必填键）、`ErrAmbiguousEnvName`、
`ErrTruncated`（附带 Kind：line/key/value 与行号）。
另有未闭合引用、坏键路径等可判定解析错误。

## 10. 复杂度

合并一遍扫描：每条目一次 map 判定，四层各 1 万键时查找次数
≤ `4 * 总键数`（包内非导出计数器，白盒测试读取）。
