# 组提交与顺序保证器设计

## 1. 范围

把并发写请求攒成批，每批一次写入文件并 `fsync` 一次；调用方拿到本条目
结果与全局序号。仅标准库，日志在临时目录。

包划分（5 个实现包 + demo）：
- `req`：写请求 / 结果 / 哨兵错误。
- `batcher`：按条数上限、字节上限、等待时长（先到者触发）攒批。
- `wal`：批次自描述落盘（批头 + 长度前缀条目 + 批级 CRC32）与读回。
- `commit`：组提交编排、序号分配与回滚、同步计数。
- `recover`：回放日志，恢复到最后一个完整批次。

## 2. 序号分配时机（推导）

三种候选：
1. 请求到达即分配：落盘失败会留下永久序号空洞，破坏“日志序号连续”。
2. 落盘成功后分配：调用方在等待期间无法知道自己的先后次序，且序号要在
   成功临界区内再发，逻辑上等价于先分配再提交，无额外收益。
3. **批次组装完成、落盘之前分配**：本方案。

规则：批次从攒批器吐出、leader 调 `wal.Write` 之前，顺序计数器
`nextSeq` 一次性预占该批条目数（条目在批内的顺序即其序号顺序）。
- 落盘与同步成功：序号生效，按批内位置回填各调用方结果。
- 落盘或同步失败：整批作废，`nextSeq` 回滚到分配前的值，批内所有调用方
  收到同一个错误。因此下一批（第 3 批）的首序号紧接成功批（第 1 批）末
  序号，无空洞。

## 3. 批次原子性

采用**批级 CRC32（IEEE）**，不做条级 CRC：
```
magic(4)="WAL1" | len(4,BE,不含自身8字节) | seqStart(8,BE) | count(4,BE)
| [ itemLen(4,BE) | payload ]* | crc32(4,BE, 覆盖 magic..payload 全部)
```
读回时顺序消费字节：
- 头部（文件前 8 字节）不完整 → ErrHeaderPartial。
- 可用字节少于批头声明长度 → 按截断位置归类 ErrBatchHeaderPartial /
  ErrItemPartial（见 §6），该批整体丢弃并停止回放。
- 长度足够但 CRC 不符 → ErrBatchCRC，整批丢弃。
绝不只丢批内某一条：一个批次要么整批可见，要么整批不可见。

文件开头固定写 8 字节文件头（magic + version）。零请求 Close 时只产生
含文件头的 8 字节文件（定义如此），回放得 0 批、末序号 0。

## 4. 攒批与 leader / 唤醒

`batcher` 内部单 goroutine 串行消费请求，故批次切分与序号天然有序。
触发条件三者先到：批内条数达到 MaxEntries、批内字节（条目载荷字节和）
达到 MaxBytes、距首条等待达到 FlushInterval（时钟可注入）。
FlushInterval=0 表示每条到达即成批；MaxEntries=1 退化为逐条提交。
单条载荷超过 MaxBytes 时**不拒绝**：若当前批非空先冲掉，该条单独成批
（批字节可超过上限，历史最大批字节按实际记录，上限只约束普通攒批）。

组提交 leader：每批恰好由 batcher 循环 goroutine 作为唯一 leader 调
`wal.Write`（写+同步一次），批内所有调用方阻塞在各自的**每请求独立
result channel**（cap=1）上。leader 落盘后按批内槽位把“该条序号 +
错误”写入对应 channel，故唤醒后调用方拿到的必是自己的结果，不串台。
Close 与请求竞争：请求要么进入最后一批并随其落盘，要么收到
ErrClosed；不会出现“成功但未落盘”。Close 后不再接受新请求。

## 5. recover

扫描文件，逐批校验，返回全部完整 CRC 正确批次与末序号。中途崩溃写出的
半个批次长度不足，按 §3 整体丢弃；其后若有数据也不会出现（写入是顺序
追加）。新提交器以恢复出的末序号初始化 `nextSeq`，序号从其后继续。

## 6. 截断点分类

对完整日志逐字节截断（保留前 n 字节，n=1..len-1）：
- `1 <= n < 8`：ErrHeaderPartial（文件头不完整）。
- `8 <= n < 8+20`：ErrBatchHeaderPartial（第一批 20 字节定长批头不完整）。
- 批头完整但 `n < 批记录结束位置(含CRC)`：若截断落在条目区为
  ErrItemPartial，落在 CRC 区为 ErrItemPartial（CRC 字节不全，等价于
  最后一个条目后的记录不完整）；分类以“条目是否全部齐全”为准，CRC
  尾部缺失归入 ErrItemPartial。
- 长度完整（n 为批边界）但字节被篡改致 CRC 不符：ErrBatchCRC。
回放结果均为“最后一个完整批次”，可见批序号连续无空洞。

四类错误均为 `wal` 包哨兵错误，用 `errors.Is` 区分：
ErrHeaderPartial、ErrBatchHeaderPartial、ErrItemPartial、ErrBatchCRC；
另加 ErrClosed（commit 包）。

## 7. 资源约束

同步次数 = 落盘批次数（每批恰好一次 fsync）。10000 请求、批上限 100 时
同步次数 ≤ 200。历史最大批字节由计数器暴露，普通批不越 MaxBytes。
