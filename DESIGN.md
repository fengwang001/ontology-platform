= 规范化器设计推导

核心规范化（与末尾策略无关）：`\r\n`、单独 `\r` 均输出 `\n`；`\r\r\n`=单独`\r`+`\r\n`→`\n\n`；行末连续 SP/TAB 删除，纯空白行变空行；NUL/非法 UTF-8 原样通过。

**1. 空输入与末尾策略。** Keep=核心结果原样；EnsureOne=核心结果非空且末尾不是 `\n` 则补一个 `\n`；Trim=删尽末尾 `\n` 后恰好补一个，核心为空则仍为空。
| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |
空输入选 `""`：选 `"\n"` 虽仍是幂等不动点（`N("\n")="\n"`），但会凭空插入，且与下文插入字节的映射缺陷叠加；选 `""` 使规范化在空串上是恒等，幂等平凡成立。三策略均为「核心规范化」与「不动的末尾状态」的复合，故各自幂等。

**2. 被删字节的 ToOut。** 定义 `ToOut(i)=核心处理后、原文前缀 [0,i) 对应的输出字节数`；`ToOrig(o)=输出前缀 [0,o) 消耗的原文字节数（在插入尾界处钳制为 lenIn）`。被删的行尾空白：前缀未含其后的 `\n`，故映射到该 `\n` 的输出位置（换行符之前）；`\r\n` 的 `\r` 与后续 `\n` 映射到同一位置（发出的 `\n` 处）。于是两者都满足单调不减，且被删区间右边界等于保留字节 `\n` 的映射值，故 `ToOut(ToOrig(o))==o` 对所有「非插入」输出偏移成立。编辑器把光标放进被删空白时，落在行尾换行符之前（行内容之后），符合直觉。EnsureOne 在非空且无尾换行时插入一个合成 `\n`：输出比输入长，不存在保持双射的映射，规定该字节与终点都钳制到 `lenIn/lenOut` 尾界；互逆律在非插入偏移上成立，Keep/Trim 全偏移成立。

**3. 缓冲超限。** 选择**拒绝**：`ErrTrailingSpaceLimit{Orig}`，已输出保留、实例进入终态。强制判定为「非行尾」会在后续字节到达后被证伪（同一完整输入，提前 flush 与不 flush 结果不同），违反第 4 条；且会把行尾空白发进输出，违反第 5 条；拒绝是确定的、与切分无关的错误，4/5/6 均不被破坏。输出超限同理返回 `ErrOutputLimit{Orig}`。

**4. par 拼接。** 每段以「开放 Close」结束，报告：待决 `\r` 长度（0/1）、尾部待定空白长度 w（原始字节）、以及该段在开放状态下的输出（待定 `\r` 已暂显为 `\n`、尾部空白已暂存，均不超过缓冲上限）。顺序拼接时按段界两侧状态修正：(a) 左段有待决 `\r`：右段首字节为 `\n` 则删掉左段暂显的 `\n` 并消费该 `\n`（否则暂显 `\n` 保留）；(b) 左段（或连同右段被消费开头后仍开放的）尾部空白运行：若右段首个可见字节是 `\r/\n` 则确认删除，是普通字节则回吐保留。修正只涉及段尾至多 1 个 `\r` 与一串长度 ≤ 缓冲上限的空白，故**修正范围有上界**，与段长无关。每段的删除事件先按「段内坐标」记录，拼接时加上该段首字节的全局原文偏移与全局输出偏移平移；段界修正产生的删除事件插在两个段的事件序列之间，合并后事件仍按原文偏移有序。最后仅在全局末尾应用 Trim/EnsureOne，与单路语义一致。

**span 结构。** 只在偏离恒等的边界记录锚点 `(origEnd,outEnd)`，删除记为零宽段；纯 `\n` 文本无删除点 → 0 个非恒等区间。查询在锚点上二分，`lastCheckCount` 记录本次检查的区间数，保证 ≤ 2·log2(n)+4，区间数仅随删除点数增长。
