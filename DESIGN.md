# DESIGN

## 1. `**` 边界推导

不变量：`**` 单独成段时匹配「零个或多个整段」。把路径按 `/` 切成段，
`x/**` 即前缀段 `x` + 一个 `**` 通配段。

- 切分规则：按 `/` 切段，空串为零段；`x/` 是段 `["x",""]`，`/x` 是 `["","x"]`。
  空段也是「整段」，可被 `**` 吃掉。
- `**` 取零段时前缀 `x` 独自耗尽路径，故 `x` 匹配；`x/` 中 `**` 吃掉空尾段，
  匹配；`x/y` 吃掉 `y`，匹配。
- 同理 `**/x`：零段时恰为 `x`，匹配；`/x` 中 `**` 吃掉空首段，匹配。
- `a/**/b` 匹配 `a/b`：`**` 取零段，两个 `/` 在「零段」处重合，
  与 `x/**` 匹配 `x` 是同一条「`**` 可取零段且不产生 `/`」规则。

## 2. 空串路径

空串切成零个段。`*` 是**段内**原子，必须落在某个段内才有意义，
零段路径中没有段可供它匹配，故 `*` 不匹配空串。`**` 是**段级**原子，
显式语义为「零个或多个整段」，零段正是其合法取值，故 `**` 匹配空串。
`?` 需消费一个码点，不匹配。

## 3. 无回溯算法

- 段内（`*`/`?`/类/字面）：线性扫描，只记录「最后一个 `*`」及其回退点；
  失配时回到该点，让该 `*` 多吃一码点继续。正确性：若存在成功匹配，
  最后一个 `*` 之后的全部模式后缀是固定的；先前任意 `*` 的成功吃入，
  都可改由最后一个 `*` 吃下（`*` 之间互不为边界），故旧回退点不可能
  产生最后回退点无法复现的解。每码点至多被该机制重访常数次，O(p×n)。
- 该论证**不能**照搬到 `**`：段是有边界的离散单位，早出现的 `**` 多吃一段
  会改变后续 `**` 的可行段数，多个 `**` 的段数分配无法由「最后一个」单点
  回退枚举。故跨段用段级 DP：reachable 为「当前段可被哪些 pattern 段消费」
  的布尔集合，`**` 段产生自环（吃本段）与跃迁（吃零段），逐段推进，
  每段内部仍走段内线性匹配，总体 O(模式原子数 × 路径码点数)，无指数回溯。

## 4. 规则集版本一致

`set.Set` 内只存一个不可变版本（规则与其编译结果同在一个结构体），
以 `atomic.Pointer` 持有。查询时在入口 Load 一次，全程只用该指针，
故结论、序号、原文必属同一版本。替换：先在旧版本之外编译并校验全部新规则
（含上限），任一失败则直接返回错误，旧指针原封不动；全部成功后一次
原子 Store 发布。

## 5. 匹配结果表

| 模式 | "" | x | x/ | /x | x/y | a/b | a/x/y/b |
|---|---|---|---|---|---|---|---|
| `x/**` | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `*` | 否 | 是(单段) | 否 | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |

注：`*` 对 "" 为否、对任何不含 `/` 的非空单段（含 `x`）为是。
