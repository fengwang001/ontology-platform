# DESIGN — 结构化日志字段脱敏与一致性采样器

模块划分：`record`（记录模型/编解码/校验）→ `rule`（规则编译）→ `mask`（脱敏执行，依赖 record+rule）
→ `sample`（采样决策，依赖 record）→ `sink`（落盘与损坏检测，依赖 record）→ `cmd/demo`（演示）。

## 1. 一致性采样的推导

目标：同一追踪链（同一 trace ID）的全部记录，要么全留、要么全丢。

- 反例一：逐条记录独立掷骰（`rand() < rate`）。链上 N 条记录全留的概率为 rate^N，
  链几乎必然断裂，且重放同一批数据结果不同，不可复现。
- 反例二：按到达顺序/时间/计数器决策（如每第 K 条）。结果依赖运行时调度与到达顺序，
  并发下不可复现，同一 trace ID 的记录仍会被拆散。
- 结论：决策函数只能是 `decision = f(traceID, rate)`，其中 f 是**确定性**函数，
  不读取记录内容、到达顺序、时间或任何可变状态。满足该约束的最简构造：
  `keep = uint64(SHA-256(traceID)[0:8]) / 2^64 < rate`（标准库 `crypto/sha256`）。
  - 确定性：同一 trace ID 永远得到同一哈希，决策与记录本身、顺序、时间无关。
  - 均匀性：需要雪崩充分的哈希。实测 FNV-1a 对 `chain-aX` 这类仅末字节不同的短 ID
    哈希值聚集（50 条链全部落在 0.40 附近，0.5 采样率下全留），故改用 SHA-256 取前 8 字节。
  - O(1)：每次决策恰好 1 次哈希计算（用计数器断言），无状态、无需锁（计数器用 atomic）。
  - 链级一致性是免费的：决策只依赖 trace ID，同 ID 必然同决策。

## 2. 错误级别强制保留与「链不完整」标记

- 规则：`level >= Error` 的记录无条件保留，即使其链被采样丢弃。
- 代价：链被丢弃时，强制保留的 error 记录成为孤儿，链不完整。
- 标记触发条件（严格定义）：**当且仅当** `hash(traceID)` 判定丢弃 ∧ `level >= Error`
  而强制保留时，给输出记录打上 `chain_incomplete: true` 字段。
  链本身被保留时 error 记录不打标（链完整）；非 error 记录被丢弃时不存在输出，无从打标。

## 3. 字段名含点号与嵌套路径的消歧

规则路径与记录字段路径都定义为**段序列**，点号只是序列的书写分隔符：

- 规则语法：`a.b.c` 解析为段 `[a b c]`；段本身含点号时必须用双引号书写，如 `"a.b".c` 解析为 `[a.b c]`。
- 记录侧：字段 `m["a"]["b"]` 的路径段为 `[a b]`，规范形 `a.b`；字段 `m["a.b"]` 的路径段为 `[a.b]`，规范形 `"a.b"`。
- 匹配按**规范形字符串精确相等**。因此规则 `a.b` 只匹配嵌套 `a→b`，永不匹配字面字段 `a.b`；
  规则 `"a.b"` 只匹配字面字段。二者在同一记录中可共存，互不干扰——歧义在解析期消除。
- 路径不存在、路径某层不是映射（如 `a` 是字符串而规则为 `a.b`）：不匹配，跳过，不报错。

## 4. 无追踪 ID 的采样策略

trace ID 缺失或为空串时，不做特殊分支：空串照常参与哈希（FNV-1a("") 是常数），
因此所有无 ID 记录构成一条确定的「空链」，共享同一决策，可复现。rate=0 时全丢（error 仍保留），rate=1 时全留。

## 5. 脱敏不变量

- 替换：目标值变为固定串（默认 `***`），不保留原长度。
- 哈希：`sha256:` + 十六进制摘要。同值同结果、异值异结果、输出不含原值（不可逆）。
- 截断：保留前 N 个**字符**（按 rune），追加 `…(truncated)` 标注；不超过 N 则原样。
- 彻底性：按值模式（正则）规则作用于记录中**所有**字符串叶子（含数组元素、任意嵌套层），
  因此同一敏感值出现在多处会被全部处理；按路径规则作用于规范路径命中的叶子。
- 非目标字段不变：脱敏在深拷贝上进行，只重写命中叶子，其余字段逐字节不变（序列化比对验证）。

## 6. 复杂度约束（可证明）

- 规则集编译期：路径规则解析为规范形存入 `map[string]pathRule`；模式规则编译为 `regexp`。
- 运行期：对每条记录做一次字段遍历，每个节点恰好 1 次 map 查询（计数器 `matches` 记录），
  总匹配次数 = 记录数 × 每条记录节点数 ≤ 记录数 × (字段数 + 常数)，与规则数无关。
  测试用 100 条规则 × 10 万条记录断言计数精确等于 记录数 × 节点数。
- 采样：每次决策 1 次 FNV 哈希，计数器断言恰为 1。

## 7. 记录校验：深度与循环

- 构造/解码时校验：嵌套深度上限 32，超限返回 `ErrDepth`（可判定，不栈溢出——校验本身是递归但先检查深度）。
- 循环引用：遍历时用 `reflect.Value.Pointer` 记录访问中的 map/slice 指针（DFS 进出栈维护集合），
  再次遇到即返回 `ErrCycle`，不会死循环。

## 8. 落盘格式与损坏分类

布局：`MAGIC(8B="ONTSINK1") | [len(4B LE) crc32(4B LE) payload(JSON)]*`。自描述头 + 长度前缀 + CRC32。
逐字节截断的分类（读取器按序解析，错误均为可 `errors.Is` 判定的哨兵）：

- 剩余 < 8 且一条记录都未开始 → `ErrHeader`（头部不完整）；
- 记录已开始但前缀不足 8 字节 → `ErrPrefix`（长度前缀不完整）；
- 前缀完整但 payload 不足 len → `ErrBody`（记录体不完整）；
- payload 完整但 CRC32 校验不符 → `ErrCRC`（CRC 不匹配，位翻转注入触发）；
- 截断点恰落在记录边界 → 无错误，恢复记录数 = 最大可恢复前缀。

## 9. 并发

编译后的规则集不可变、采样器无共享可变状态（计数器均 atomic）、脱敏作用于深拷贝，
因此多协程处理与串行处理输出集合逐元素相同；`-race` 干净。
