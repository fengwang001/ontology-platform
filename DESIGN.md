# DESIGN

流：`Header = 魔数"OLZ" + 版本1`，随后记录，`Tail = tag4, varint(原长), varint(crc32-IEEE)`。
记录：`Lit = tag0, varint(n), n字节`；`Ref = tag1, varint(dist), varint(len)`；`Flush = tag2`。
varint = 无符号 LEB128，最多 10 字节且第 10 字节高位必须为 0，否则 ErrVarint。

## 1 与 Write 切法无关（语义3）
压缩器维护 `win`（已决历史）+ `pend`（未决尾部）+ 匹配器。每次 Write 只把字节追加到
pend，然后在「末尾保留 maxMatch-1 字节」之外的范围取决策点 i：在 i 处查最长匹配；匹配只
引用 win（已决历史），新到的尾部字节只可能延长匹配长度，所以 i 只要还能被延长就不能定夺。
规则：保留末尾 maxMatch-1 字节不定；其余位置按「有匹配(len>=minMatch)则发 Ref 并跳过 len
字节，否则发一个字面量」推进，被跳过/发出的位置从 pend 移入 win。决策只依赖累计字节序列，
与 Write 边界无关。
Flush：强制把 pend 全部决掉（在最后一个可查位置决策，其余作字面量）并写 Flush 标记；匹配
本来可能被未来字节延长，故 Flush 必然终止当前匹配——这正是承诺4要求的边界。无新输入再
Flush：记录上次 flush 时的总长度，未变则什么也不写（承诺4）。

## 2 重叠回指（语义2）
块复制 `copy(dst, src)` 对重叠区按 memmove 处理，dist<len 时只复制已有历史，结果被截断重复。
采用逐字节循环：`for j := range out { out[j] = out[n-dist+j] }`，右端刚写的字节立即可被后续
引用，天然实现周期展开。长度有上限（maxMatch=1<<20），每字节只是一次索引读写，开销与字面量
同阶，可接受；且匹配长度在实际复制前先对照输出上限校验（语义8，不预分配）。

## 3 两类越界距离（语义7）
「距离>已输出字节数」=引用尚未产生的数据，任何合法流（含预置字典）都不可能出现：压缩器只
对 win 中真实存在的位置发 Ref。「距离>窗口容量」=引用的是已存在但被滑出窗口的历史：字节真实
存在，但本协议窗口有界，故单独成类。检查顺序：dist==0 → dist>已输出 → dist>窗口，三类可区分。
并行块：块 k 以块 k-1 原文末尾（至多窗口容量）预置字典。解压器视角这些字节是此前真实输出，
「已输出字节数」从流起点累计，含义不变；块首 Ref 可合法指向上一块。

## 4 并行确定性（语义9）
块 k 的压缩输入只有：本块原文 + 上一块原文（预置窗口）；哈希链初始只由预置字典建立，不读
其他块的压缩结果，也不读共享可变状态。块输出是纯函数，与 worker 数、调度与完成顺序无关。
汇总按块序号拼接：共用一个 Header，块间不写 Flush（Ref 天然跨块，见 §3），最后统一写一个
Tail（全流原长、全流 crc32）。worker=1/2/4/8 输出逐字节相同。

## 记录布局

| 记录 | 布局 |
|---|---|
| Header | `4F 4C 5A` ("OLZ") `01` |
| Lit | `00` uvarint(n) n 个明文字节 |
| Ref | `01` uvarint(dist) uvarint(len) |
| Flush | `02` |
| Tail | `04` uvarint(origLen) uvarint(crc32IEEE) |
