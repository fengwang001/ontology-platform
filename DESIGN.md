# 有序键值多层段合并读取器 — 设计推导

## 1. 段文件格式（segment）

单文件布局：`[header 32B][entries...][sparse index][footer 8B]`

- header(32B 定长)：magic 8B(`ONTOSEG\x01`)、version u16、entryCount u32、indexOffset u64、
  保留 10B、headerCRC u32（对前 28B 的 CRC32-IEEE）。
- entry：`klen u32 | vlen u32(最高位=删除标记，删除时 vlen=0 且标志位为1) | key bytes | value bytes | crc u32`，
  crc 覆盖 `klen,vlen,key,value`。段内 key 严格升序；空 key 合法；空 value 合法（标志位为 0）。
- sparse index：`blockCount u32`，随后每条 `klen u32|key bytes|offset u64`，
  每 16 个条目记录该块首条的文件偏移。offset 指向条目起始。
- footer(8B)：indexCRC u32（覆盖整个 index 段含 blockCount）+ 魔尾 u32 标记 `0x5345474d`。

稀疏索引点查：对块首 key 二分找到最后一个 `<= key` 的块（比较数 ≤ ceil(log2(N/16+1))），
再顺序扫该块（≤16 条）。总比较数上界远小于 `4*ceil(log2(100000))`。

## 2. 版本胜出规则（自行推导）

数据从旧到新分布在多层多段：
1. **层号更小者更新**（L0 最新，L2 最旧）。点查按层从小到大，第一次命中即胜出。
2. **同层内仅 L0 可能多段**（L1+ 键范围不重叠）；L0 中段序号（加入顺序）更大者更新。
归并迭代器比较键时按 `(key, 层号小优先, L0 段号大优先)` 决定同键谁在前。

### 三种键状态
存在（返回值）；已删除（命中删除标记）；从未写过（任何层段都无此 key）。
三者必须可区分：`Get` 返回 `(value, present, deleted)` 或等价三态。

## 3. 删除标记何时可丢弃（完整推导）

删除标记的唯一作用是**遮蔽更旧数据**。若合并产出的新段仍留在某个层级 L，
则比 L 更旧的层级中可能存在同 key 的旧值；一旦丢掉删除标记，该旧值在点查时
会「复活」。因此：

> 只有本次合并的输入覆盖了该 key **可能出现的所有层级**（即参与段来自
> L0..Lmax 的全部当前层级，且这些层中该 key 只会出现在参与段里——L1+ 不重叠，
> 所以「覆盖某层」即包含该层全部段）时，删除标记才能丢弃。

推论：把**全部现有层的全部段**做一次全量 compaction（输出成为唯一层）时，
没有任何更旧数据可遮蔽，删除标记一律丢弃且段体积变小。部分层合并必须保留。

## 4. 迭代与归并（iter）

段迭代器从给定偏移顺序读条目。多路归并用最小堆，堆元素携带 (key, level, seq) 序；
每吐一条 M：弹出 O(log S)、同键旧版本被吸收（每个参与段至多再弹一次，仍 O(S log S)
摊还到键组），故比较数 ≤ `4*M*ceil(log2(S+1))`。同键取最新版本，其余跳过。

## 5. 合并（compact）

流式 k 路归并，每路始终只持 1 条在内存 → 同时驻留条目数 ≤ S（外加输出缓冲 1 条），
由计数器断言不超过给定上限。新段先写临时文件 `name.tmp.<pid>`，校验完整后
`os.Rename` 原子替换，再删除旧段。

### 崩溃恢复顺序
写 tmp → fsync → rename → 删旧。崩溃后：存在的 `*.tmp.*` 必为未提交产物，直接删除；
正式段按头部/CRC 校验，旧段未被 rename 触及则逐字节保持可用。因 rename 原子，
不会出现「旧段已删新段未就位」。

### 读不看到空窗
读取持有当前快照（段描述符列表）；替换序列为「先建新段、原子改名、再删旧段」。
读者通过已打开的文件描述符访问旧段（rename/unlink 不影响已打开 fd），
新读者拿到新快照，任意时刻点查均有数据。-race 下并发读写测试验证。

## 6. 损坏分类（verify / segment 截断）

对长度 L、截断点 p（1..L-1）逐字节截断，分类：
- 头部不完整：p < 32 或 headerCRC 不符。
- 条目不完整：数据区内某条目定长字段/key/value 字节不足。
- CRC 不匹配：条目结构完整但 crc 落在被截掉的尾部（截点位于末尾 CRC4B 内）。
- 索引段不完整：全部条目完整后，index/footer 不足或 indexCRC/魔尾不符
  （含恰好截在条目边界）。
恢复：从头顺序读，遇到上述任一异常即停，返回此前完整条目作为「最大可恢复前缀」。

## 7. 层级不变量（verify）
L0 段键范围允许重叠；L1 及以上同层段键范围必须互不相交。违反时报告两个段的
标识与重叠区间 `[max(start), min(end))`。另校验每段内部 key 有序、CRC 全通过。

## 8. 边界语义
空段（仅头+空索引）、单条目、全同键多版本、空 key、空 value（区别于删除）、
范围扫描 `[start,end)` 左闭右开、start>end 返回可判定错误、区间与所有段不相交
返回空结果而非错误。
