# DESIGN: 常量折叠与谓词下推重写器

语义域为三值逻辑（Kleene 强三值）：`T`（真）、`F`（假）、`U`（未知，由 NULL 产生）。
连接词真值表要点：`F AND U = F`，`T OR U = T`，`U AND U = U OR U = NOT U = U`；
比较只要任一操作数为 U 结果就是 U。

## 一、三条关键重写的推导

1. `x = x` 不能折叠为真。取赋值 `x = U`：`U = U` 的比较结果按定义为 U，而非 T。
   真值集合 `{v | x=x 为 T} = {T,F}`，而常量 `true` 的真值集合是全部赋值，两者不等。
   即使在顶层（WHERE 只按"为真"放行元组），折叠后 NULL 行会被错误保留，故该规则
   **在任何位置都不采用**。
2. `A OR NOT A` 不能折叠为真。取 `A = U`：`U OR U = U`。真值集合为 `{T,F}`，同理不等于
   `true` 的真值集合，任何位置都不采用。
3. `NOT (A AND B)` → `NOT A OR NOT B` 在三值下仍成立（对全部 3^2 种赋值逐点验证，
   德摩根律在 Kleene 逻辑中保持），实现为规则 `de-morgan-and`（及对称的 `de-morgan-or`）。
   但 `A AND NOT A` 不能折叠为假：`A = U` 时 `U AND U = U` 而非 F。

## 二、何时可以按二值优化（位置条件）

WHERE 顶层只区分"为 T 放行 / 否则过滤"，即 F 与 U 效果相同。定义**过滤等价**：
`e =f= e'` 当且仅当两式的真值集合（取 T 的赋值集）相等。逐点三值相等记为 `e =3= e'`。

- 若替换发生在 `NOT`（或 CASE 分支值）之下，U 会被 `NOT` 原样透出（`NOT U = U`），
  必须要求逐点相等。反例：`NOT (A AND NOT A)` 的真值集合是 `{T,F}`，
  而 `NOT false = true` 的真值集合是全部赋值——把内层折成 F 会改变结果。
- 若子表达式处于**顶层合取项**位置（从根出发只经过 AND 链可达，未被 NOT/OR 包裹），
  整个谓词的真值集合是各合取项真值集合的交，故只需该合取项过滤等价。
- `A AND NOT A` 的真值集合为空集（A=T→F，A=F→F，A=U→U），与 `false` 相同，
  因此在顶层合取项位置可折叠为 F（规则 `contradiction`，仅在此位置启用）。
- `x = x → true`、`A OR NOT A → true` 的真值集合从 `{T,F}` 变成全集，连过滤等价
  都不满足，所以即使顶层也不安全——本重写器不提供这两条规则。

## 三、规则集与终止性

局部规则（任意位置、逐点等价）：`const-eval`、`and-false`、`and-true`、`or-false`、
`or-true`、`not-not`、`de-morgan-and`、`de-morgan-or`、`single-kid`、`dup-kid`。
顶层规则（仅根部 AND 链）：`contradiction`（仅过滤等价）。

终止性论证：定义字典序测度 `mu = (M, S)`，其中
`M = 每个 NOT 节点 v 的 2^|子树(v)| 之和`，`S` 为节点总数。
`de-morgan-*` 使 `M` 严格下降（`2^(a+b+2) > 2^(a+1) + 2^(b+1)`，NOT 被推向更小子树）；
其余规则不增加 `M` 且使 `S` 严格下降。故每轮不动点迭代 `mu` 严格下降，必终止。
德摩根链每轮最多把 NOT 下推一层，轮数 ≤ 深度 + 1 ≤ 节点数 + 1 ≤ 4×节点数，
实现以 `4 × 节点数` 为硬上界，超出即报可判定错误。
收敛性另由历史指纹保证：每轮记录规范化打印串，重复即判震荡（互逆规则）报错。
每轮每节点访问一次（FoldTop 再一遍），访问总数 ≤ 轮数 × 节点数 × 2 < 上界常数 4。

## 四、确定性

`rules.Fixpoint` 先把规则按名称字典序排序再应用；AND/OR 子节点在规范化打印时按
文本序排序。因此重写结果与调用方给出的规则顺序逐字节无关。

## 五、谓词下推

计划树为 `Scan(table)` / `Join(l, r)`。把顶层 AND 拆成合取项，每项推到覆盖其
引用表集合的最深节点：单表项落到该 Scan，跨表项留在 Join。引用不存在的表属
可判定错误。`Check` 自检：每个 Scan 上谓词引用的表必须是该 Scan 的表。

## 六、包结构

`ast`（树、三值求值、规范化打印、校验）→ `equiv`（3^n 穷举等价，`Equal` 逐点 /
`FilterEqual` 按真值集合，失败给出反例赋值）→ `fold`（规则与顶层折叠）→
`rules`（不动点、计数器、震荡检测、随机树生成）→ `push`（下推与自检）→ `cmd/demo`。
