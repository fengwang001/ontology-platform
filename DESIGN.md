# DESIGN：自定 LZ77 流式压缩

## 1. 流格式（整数均为 unsigned LEB128；CRC32 为 IEEE 小端追加 4 字节）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `"LZ77ONTO"`(8B) + 版本 varint=1 + 窗口容量 varint |
| 字面量 | 标签 0 + 长度 varint + 原始字节 |
| 回指 | 标签 1 + 距离 varint(≥1) + 长度 varint(≥3) |
| 刷新 | 单字节标签 2 |
| 流尾 | 标签 3 + 原文总长 varint + CRC32(4B, 原生小端序) |

标签字节复用 LEB128 首位：标签值 <64，不会与 varint 续位混淆。

## 2. 总体结构
`window` 环形缓冲保存已决历史；`match` 在其上用 3 字节哈希链找候选，
链只挂在窗口内位置，每位置最多顺链查 chainLimit 个候选。
`enc` 产出记录字节；`dec` 解析记录并从窗口逐字节回指复制。

## 3. 四处推导

### 3.1 为什么压缩结果与 Write 切法无关
压缩器维护"未决区"：新到字节先追加到缓冲，循环中仅当 `pos+maxMatch`
**严格小于缓冲长度**时才在 pos 决策——该位置能取到的最长匹配只依赖
已到达字节，将来字节不会改变它（匹配只看固定窗口内历史，历史已固定）。
匹配则输出回指并把匹配段并入历史；不匹配则输出 1 字面量。
`pos+maxMatch == len(buf)` 的尾部一律保留不动，于是跨 Write 边界的
匹配在拿到后续字节前不被截断：1/7/整段三种喂法得到同一字节序列。
`Flush` 强制把未决尾全部决掉并输出刷新标记，因为调用方要求"此刻所有
输入可解压"，而未决匹配的终点尚未确定，必须收尾；收尾后这些字节已
进历史，Flush 之后的匹配仍可回指它们。连续两次 Flush 间无输入，
未决区为空，第二次只产生零字节。

### 3.2 距离 < 长度时的复制
块复制（如 copy(dst,src)）允许源目的重叠但语义是**从前往后覆盖式**，
恰等于 LZ77 周期性展开：`abc` 以距离 3 复制 30 字节得到 abcabc…。
但为避免对 overlap 大小与 copy 行为的隐含依赖，实现取
`copy(out, out[len-d:])` 在剩余长度上循环（每次至多 copy 距离字节），
首次复制后源已包含新展开字节；这是 O(长度)、无额外分配、语义显式
逐字节向前的写法，与逐字节循环等价且不更慢（每字节仍摊 O(1)）。

### 3.3 "距离超过已输出" 与 "距离超过窗口" 为何分开
两者是不同不变量的违反：前者说明引用了从未存在过的字节（流伪造/
截断），后者说明引用了压缩器配置不可能保留的字节（版本/配置不符）。
即使距离 ≤ 已输出，也可能 > 窗口（早期历史已被环形缓冲丢弃），
故检查顺序为 0、>已输出、>窗口 三个独立哨兵错误。压缩器只在
窗口容量内做匹配，故其输出中距离 ≤ min(已输出, 窗口)，绝不可能
产生第三类。并行块预置字典：第 N 块把第 N-1 块原文末窗口容量字节
灌入自己的窗口/哈希链；该字典在全局解码视角本就是已输出历史，
"已输出字节数"语义不变，仍做同样三检查。

### 3.4 并行压缩的确定性与块衔接
块 i 的匹配只读取"块 i 原文 + 块 i-1 原文"（预置字典是上一块原文
的确定性后缀），不读取任何压缩结果、不与其他 goroutine 共享可变
状态；相同输入在任何 worker 数、任何完成顺序下产出相同记录字节。
主 goroutine 按块序拼接：先写流头，再依次写各块记录，块间插刷新
标记（块边界即 Flush 点），最后写流尾。刷新标记是纯边界，解压器
无须知道并行切块，按第 5 条普通流解析即可。worker 数只切分任务，
不改变每块输入与输出。

## 4. 复杂度
每位置候选数 ≤ chainLimit，总考察数 ≤ chainLimit × 输入字节数；
哈希链只含窗口内位置，全同字节最坏时每位置也只查 chainLimit 个，
每字节考察数不随输入规模增长。

## 5. 并发契约
单实例（一个 Writer/Reader）不要求并发安全；不同实例相互独立。
