# DESIGN

依赖方向（单向）：`lines` ← `edit` ← `hunk` ← `udiff` ← `patch`。

## 1. 零计数 hunk 头的行号（题 4）

- `x\n` → `y\nx\n`：旧文件最前面插入，插入点之前有 0 行 → `-0,0`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`：在第 2 行之后插入，之前有 2 行 → `-2,0`。
- `a\n` → 空：新文件在第 0 行之后为空，之前有 0 行 → `+0,0`。

结论：count=0 时，数字 = **插入点之前已存在行的行数**（即“前一行”的 1-based
行号；前面没有行时为 0），也就是 0-based 插入下标。它不是“将要插入的位置
（1-based）”：样例 1 按该解释应为 1，实际输出 0——该数字指向一条已存在的
行（第 0 行表示“无前行”），与 count>0 时指向片段首行在语义上统一。
Apply 解析：count==0 → `start = 数字`（直接是 0-based 插入下标）；
count>0 → `start = 数字 - 1`（1-based 行号转 0-based）。

| 头书写 | 含义 | Apply 插入点（0-based） |
| --- | --- | --- |
| `-a,0` | a 行之后有空片段（a=之前行数） | `a` |
| `-a`（即 `,1` 省略） | 第 a 行起 1 行 | `a-1` |
| `-a,b`，b≥2 | 第 a 行起 b 行 | `a-1` |

## 2. hunk 合并阈值（题 5）

阈值 = **2C**。前一改动向后延 C 行上下文，后一改动向前延 C 行：相隔 g 行
未改动行时，g ≤ 2C 两段上下文相接或重叠，GNU `diff -U C` 输出一个 hunk；
g = 2C+1 时中间恰有一行两段上下文都覆盖不到，输出两个 hunk。
- 若阈值取 C：g 满足 C < g ≤ 2C 时上下文已重叠却仍拆分（例 C=3,g=5），
  GNU 实为一个 hunk，矛盾。
- 若取 2C+1：g = 2C+1 也合并，则 hunk 要包含超过 2C 行上下文、中间行两侧
  上下文均不足 C（例 C=3，改第 5 行与第 13 行），GNU 实为两个 hunk，矛盾。

## 3. 并列最短脚本的选择（题 3）

Myers 贪心步：在对角线 k 上，当 `V[k-1] >= V[k+1]` 时选右移（删除一行），
否则选下移（插入一行）——**并列时优先删除**，回溯时用同一判定。效果：相邻
改动块内 `-` 行排在 `+` 行之前。例 `a b` → `b a` 固定输出
`-a`、` b`、`+a`，而不是 `+b`、` a`、`-b`。选择固定 ⇒ 补丁文本确定。

## 4. 偏移后的后续 hunk（题 7）

前一 hunk 以偏移 k（实际位置 − 记录位置）应用后，后续 hunk 的查找中心 =
记录位置 **+ k**，每应用一个 hunk 就把 k 更新为实测偏移。k 度量的是目标
文本相对补丁记录位置的整体漂移（如文件头被第三方增删行），该漂移对后续
所有 hunk 一致，故沿用。实现上在旧文本的不可变快照上定位、另建输出缓冲，
自身增删行不会改动快照坐标，因此无需再叠加本补丁的行数变化。
