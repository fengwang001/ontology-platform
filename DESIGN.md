# 设计推导

## 1. 空行与单列空值
选择：**跳过空行**（空白行不产生记录）。`` 等字节也不产生记录；`\n\n` 中间不产生记录；文件可解析为 0 条记录。
于是单列空值记录必须能与空行区分：记录 `[空字段]` 若不加引号写成 `\n`，再解析时该空行被跳过，记录丢失。
故单列表里唯一字段为空的记录**必须**回写为 `""\n`（引用空字段）。未引用空字段只在多列记录（如 `a,\n`）中安全。
规范输入定义：`\n` 行尾；除“单列空字段”写成 `""` 外不使用多余引号；不跳过空行的前提是无裸空行；末尾带一个 `\n`（0 条记录时为空串）。

## 2. par 切点
切点可落在引号字段、`""`、`\r\n` 中间，下段无法自证状态。方案两阶段，均按段并行：
1. **分类扫描**：每段仅做“引号翻转”扫描——引号外的 `"` 翻转进引号态，引号内的 `""` 成对吞掉，统计段内“引号翻转奇偶性”及“`\r` 结尾”。该扫描不校验裸引号，故对任意前置状态都成立；段 i 起点在引号内 = 前 i-1 段翻转数为奇。
2. 由各段起点真实状态（外/内）构造 lexer 快照，K 个 goroutine 各解析一段（`\r\n` 边界由分类器的 trailing-CR 标记并入下段初态）。
拼接：段 0 从全新状态开始；段 i 起始字段若未在段内闭合，其单元格事件只在最后一个覆盖它的段发出（快照带 pending，前段对该字段零输出）；段内事件经偏移（段起点 + 段内偏移、记录/字段号加快照基数）后顺序回放给同一个 table sink。
错误随检测段报告，偏移即“段起点+段内偏移”；记录号/字段号同样加基数。首个错误与单线程相同：错误点之前字节决定的状态与单线程完全一致，故首个错误点、错误类、坐标一致；出错后解析器终态。
复杂度：每字节被分类扫描 1 次 + 正常词法分析 1 次，总计 ≤ 2N + O(K)，不随长引号字段退化。

## 3. CR 待定
`\r` 在引号内一律是内容。引号外见到 `\r` 进入 CR 状态（不发事件、不判错）：下一 Feed 首字节为 `\n` 则行尾；为其他字节则在该 `\r` 偏移报“孤立 CR”。
半包：该状态跨 Feed 保留，故切在 `\r`/`\n` 之间结果不变。par：分类器对以 `\r` 结尾的段置 trailingCR；该 `\r` 留给下段：下段首字节 `\n` 则行尾，否则错。`Close` 时处于 CR 态：判孤立 CR。

## 4. 上限“立刻”
字段字节计数在每个成为内容的字节被接收时递增（引号内 `""` 两个引号字节只产生 1 个内容字节，计数加 1；开/闭引号字节不计），在加 1 **之前**与上限比较，超限立即报错——无需缓冲整字段（值本身仍为输出而增量累积，错误即终态）。
字段数在每次字段开始时计数；记录数在记录完成时计数。任何错误后 Feed/Close 返回同一哨兵。

## 5. 状态转移表
状态：S 字段开始，U 未引用，Q 引用中，A 引用后刚见引号，C 行尾 CR 待定。
字节类：`,` `\n` `\r` `"` 普通 x。

| 状态 | , | \n | \r | " | x |
|---|---|---|---|---|---|
| S | 发空字段;S | 跳过空行;S | C | 进Q(字段起点) | 记x;U |
| U | 发字段;S | 记录完;S | C | **裸引号错** | 记x;U |
| Q | 记,;Q | 记\n;Q | 记\r;Q | A | 记x;Q |
| A | 发字段;S | 记录完;S | C | 记引号;Q | **引号后杂字符错** |
| C | **孤立CR错** | 记录完;S | **孤立CR错** | **孤立CR错** | **孤立CR错** |

EOF：S/U/A 发待定字段/记录（armed 才有）；Q **引号未闭合**；C **孤立 CR**。
错误位置均为触发字节的全局偏移、当前记录号、字段号（从 1 起）。
