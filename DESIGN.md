# DESIGN：自定格式 LZ77 流式压缩

流布局：头 `LZ77` + varint 版本(=1) + varint 窗口容量 + varint 校验；其后 0+ 条记录；尾记录带总长与校验和。校验和为 FNV-1a 32 位。

| 记录 | 字节布局 |
|---|---|
| 字面量 | tag=0；varint 字节数 N；N 个明文字节 |
| 回指 | tag=1；varint(距离)；varint(长度) |
| 刷新 | tag=2（边界，无数据） |
| 流尾 | tag=3；varint(原文总长)；varint 校验和 |

varint：无符号 LEB128；解码器最多读 10 字节，第 10 字节只允许 bit0，否则报超长/溢出。

## 推导 1：Write 切法无关（语义 3）

编码器维护窗口 W 与一段「未决缓冲」P（已 append 进窗口、但尚未发出任何记录的字节）。在 p 点是否选回指，取决于未来字节：匹配必须能继续与不能继续两种证据都到齐后才定案。
规则：每次 `Write` 只把字节追加进窗口；随后在「最后一个字节之前」推进决策——当已选匹配的下一字节仍相等则延长；一旦不等，该匹配（或字面量）定案发出，并把起点移到定案位置。只要至少还有一个字节未被检查，就暂停。
因此决策只依赖字节内容，与字节由几次 `Write` 到达无关；`Flush` 强制对 P 中全部字节做完决策并补一个刷新标记，这正是 Flush 必须「强制结束当前匹配」的原因。

## 推导 2：重叠回指（语义 2）

回指语义是从已输出序列中逐字节取：`out[i]=out[i-dist]`，对 dist<len 时这是重复展开（dist=1 为 RLE）。一次性 `copy(dst, src)` 若 src、dst 重叠且 dst 在前，标准库 copy 按前向非重叠切片处理，不能产生周期重复，会得到错误结果。
结论：必须按 `for k:=0; k<length; k++ { emit(out[n-dist]) }` 逐字节 emit；emit 同时推进输出与校验和。4MB 全同字节仅由极少记录产生，逐字节循环开销可接受且它就是定义本身。

## 推导 3：两种「距离超限」的区分（语义 7）

`dist > 已输出字节数`：引用尚不存在的位置，任何合法流都不可能出现（并行块也不行，见下）。
`dist <= 已输出 但 dist > 窗口容量`：引用了编码器已丢弃的远古历史；合法编码器在该配置下不可能产生（它的窗口只留 capacity 个字节，匹配器只在其中找候选）。
二者是不同的不合法性质：前者流在任何窗口下都无法解；后者仅在当前窗口配置下无法解。预置字典不改变含义：字典字节在处理第一块记录前就逐字节写入解码器窗口和输出前缀（校验和同步计算），所以「已输出字节数」始终包含字典，校验和/总长仍只统计真实原文。

## 推导 4：并行块压缩的确定性（语义 9）

按固定 blockSize 切块。块 i 的压缩输入只有：本块原文 + 上一块末尾不超过窗口容量的一段原文（预置字典）。匹配器是纯函数：哈希链从同一字典状态出发，喂同一批字节，考察序列与贪心选择完全确定，与 goroutine 数、调度顺序无关；块 0 字典为空。
块间不需要刷新标记：LZ 记录流本就可衔接，块 i+1 的回指可落入由「字典+本块」构成的窗口；各块输出记录直接拼接，仅最后一块后追加流尾。主 goroutine 先算全文校验和，再收集记录，故 workers=1/2/4/8 逐字节相同。
