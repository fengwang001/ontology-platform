# 设计推导

## 1. 空输入 / 只含换行（三种末尾策略）

定义：`Keep`=不动；`One`=有内容则末尾恰好一个 `\n`；`Trim`=删全部末尾空行，内容非空则留一个 `\n`。
关键决策：`One`/`Trim` 对**空输入（规范后无任何行）**输出 `""` 而非 `"\n"`。空文件没有“行”需要终止符；
且 `N("")=""` 显然幂等；若选 `"\n"`，则再次规范化时 `"\n"` 被视为“一行空内容”，为保持幂等也必须产出 `"\n"`，
会导致 `""` 与 `"\n"` 两个不同原文被映射成同一输出、且凭空造出一行。选 `""` 后两种策略分别以“输出中是否出现过 `\n`/非空行”为判据，各自构成幂等函数。

| 原文 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

映射用左闭右开区间表示，被删原文字节区间 `[a,b)` 宽 0 落在输出边界 `o` 上。由单调性，删除段内部各字节只能取同一值 `o`。
`ToOrig(o)` 必须是“边界之后第一个保留字节”的原偏移，才能保证 `ToOut(ToOrig(o))==o`（否则会回落到删除段）。
因此行尾空白 `ws\r\n`：空白各字节映射到 `\n` 的输出位置（换行**之后**），即光标放在被删空白里落到下一行行首；
`\r\n` 的 `\r` 同样映射到 `\n` 的位置；单独 `\r` 原地变 `\n`（1:1，不删除）。边界之后无保留字节（流末删除）时取原文长度。

## 3. 空白缓冲超限

选择**拒绝**（确定性错误 `ErrPendingLimit`，带原文偏移，保留已输出，进入终态）。强制输出会把“尚未判定的行尾空白”
提前当作行中空白吐出，一旦随后出现行尾，输出与映射就无法回退，违反第 4、5、6 条；
拒绝则与切分方式无关：同一串超长空白在任何喂入方式下都在同一原文偏移触发，语义一致、状态可解释。

## 4. par 拼接修正

每段独立用同一 `norm` 处理。段首缺少前一段待定上下文，只可能造成两类边界误判，修正范围有上界：
1. 前一段末字节为 `\r`、后一段首字节为 `\n`：独立处理会把 `\r` 当单独行尾，`\n` 再输出一个 → 删去后段首字节输出，
   后一段整体输出前缀平移 -1。修正范围 1 字节。
2. 前一段以空格/制表符结尾：后向扫描该空白串（长度 ≤ 缓冲上限，否则与整体一样确定性报错），
   看其后首字节是否为行尾/EOF；据此恢复（删掉前一段末尾被误吐的空白）或保留；后一段内部不受影响。
映射拼接：取各段区间，把 orig 起点平移 `offI`、out 起点平移 `offO`，交给 `span.Builder` 合并（相邻同向区间自动合并，
故无删除的 10MB 只有一个区间）。平移是纯坐标变换，不改变段内判定。
插入合成换行（`One` 且内容不以换行结尾）时插入点处 `ToOrig`/`ToOut` 在终点相遇：约定插入位置回映到原文长度，
该唯一插入点上 `ToOut(ToOrig(o))` 钳制为输出长度，其余所有位置严格互逆；两映射仍单调不减。

## 5. 复杂度

区间仅在删除点产生（每次删除至多新增 2 个边界，区间数 O(删除点数)）；查询对 kept 区间/全区间二分，
计数器 `checked` 记录实际检查的区间数，保证 O(log n)。
