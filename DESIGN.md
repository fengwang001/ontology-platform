# DESIGN

## 1. 空行与单列空值
结论：空行（两个行尾紧邻，或文件开头就是行尾）**跳过**，不产生记录；单列表里一条「值为空」的记录必须用加引号空字段 `""` 表示。
推导：空行没有任何字段，连「一个空字段」都无法与「连续分隔符」体系自洽——单列时它恰为零字节记录，若当成 `[空]` 记录，则无法区分「真正想表达的空值」与「文本里多余的空行」。故解析器只在看到字段内容（含显式 `""`）或分隔符时才开记录。单列 `""\n` 是一条含一个加引号空字段的记录。
回写：未加引号的空字段在单列表里回写为「空」会与空行（跳过）混淆，破坏逐字节往返；故单列记录的唯一空字段必须写成 `""`。多列表中未引号空字段写空即可（逗号提供结构）。空行不参与表模型，故规范输入不含空行。
规范输入定义：无开头空行、无连续行尾、必须以 `\n` 结尾（最后一条记录也有换行）、无多余 CR、除「单列空值用 `""`」外仅在必要时加引号的字节序列。

## 2. par 切点
方案：每段在两个假设下各跑一遍状态机——H0=段起点在引号外且无 CR 待定；H1=段起点在引号内（沿用前一字段，引号数继续）。另有第三态 CR 待定（前段以裸 `\r` 结束），由拼接层在**后段开头只补一个字节的判定**解决（见第 3 节），不重扫整段。
确定真值：从左向右。第 0 段真实起点=H0。每段结束时记录（引号开合奇偶、是否以裸 CR 结束）。前段结束在引号内，则后段取 H1 的结果，否则取 H0。段内坐标=全局偏移减段起点；记录号/字段号从段首事件按 1 起累加（部分字段不算），拼接时加上前段末的全局记录/字段基数。错误在 worker 内按段内坐标报 `Err`，拼接层把偏移加段起点、记录号加基数；被选中假设若在段内报错即全局终态，后续段不产出记录。
字段跨切点：被选中轨迹在段首处于引号内（H1）时，其首个字段是前段末部分字段的延续，拼接层只合并值/引号标记/起始偏移，不另开字段。
字节数：每段两遍 ⇒ ≤2N；仅当切点恰在 `\r` 后时对后段首字节做 O(1) 判定，故 ≤2N+O(K)。

## 3. CR 待定
未引号字段末遇 `\r` 进入 CR 待定：不发字段、不发记录尾，等下一字节。`\n`→行尾（发字段+记录尾）；其它（含 EOF、引号、逗号、另一个 `\r`）→孤立 CR 错误。
半包：状态机可暂停，CR 待定作为持久状态跨 `Feed` 保留；`Close` 时仍待定即报孤立 CR。
par：切点落在 `\r` 与 `\n` 之间时，前段以 CR 待定结束。拼接层看后段首字节：为 `\n` 则在两段事件流之间补「字段+记录尾」并让后段从第二字节起按 H0 接（后段 H0 流里该首 `\n` 产生的空字段事件被丢弃，仅差首字节，O(1) 调整）；否则按孤立 CR 定错。切点落在引号内 `\r` 后无此问题（引号内 `\r` 是内容，不产生待定态）。

## 4. 上限的「立刻」
字段字节计数在字节被接纳进字段值的那一刻自增；引号内 `""` 的两个引号字节合意为一个 `"` 字符，但**字节上限按原始输入字节计数**：开引号本身不计入值，闭引号不计，转义对的第二字节（合意引号）到达时计 1——即每到达一个属于字段值的原始字节就计 1，第一个越界字节到达立即返回 `ErrFieldTooLong`，不等行尾。字段数在开新字段（遇逗号）时计；记录数在一条记录提交时计。触发即终态，错误被解析器缓存，后续 `Feed`/`Close` 返回同一错误。

## 5. 状态转移表
状态：F=字段开始，B=未引号字段中，Q=引号字段中，QE=引号中刚见引号，CR=行尾CR待定。字节类：`,` `"` `\r` `\n` 其它 C。
| 状态 | , | " | \r | \n | C |
|---|---|---|---|---|---|
| F | 发空字段→F | 开引号→Q | →CR | 空行:不发字段直接→F | 值+C→B |
| B | 发字段→F | ErrBareQuote | →CR | 发字段+记录尾→F | 值+C→B |
| Q | 值+,→Q | →QE | 值+\r→Q | 值+\n→Q | 值+C→Q |
| QE| 发字段→F | 值+"→Q | →CR(已闭) | 发字段+记录尾→F | ErrQuoteAfterClose |
| CR | 孤立CR | 孤立CR | 孤立CR | 发字段+记录尾→F | 孤立CR |
QE 态的字段视为已闭合（有引号标记）；CR 态遇 `\n` 时提交该字段。EOF：F→空输入无记录；B/QE→发字段+记录尾（无尾换行合法）；Q→ErrUnclosedQuote；CR→孤立CR。
