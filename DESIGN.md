# DESIGN

## 零计数 hunk 头

统一 diff 的区间描述的是该片段在旧/新文件中覆盖的行。普通区间首行号是 1-based；计数为 0 时没有“被覆盖的行”，所以首行号表示插入点**前面一行**的行号；在文件开头时为 0。

- 空文件插入 `y`：旧文件插入点前没有行，得 `-0,0`；新片段是第 1 行，得 `+1`。
- 在第 2 行 `b` 后、第 3 行 `c` 前插入 `X`：旧位置前一行是 2，得 `-2,0`；新插入行是 3，得 `+3`。
- 删除唯一一行：旧片段是第 1 行，得 `-1`；新文件插入点前没有行，得 `+0,0`。

| 旧区间 | 书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0` | 不消费旧行；插入点在旧第 a 行与 a+1 行之间，a=0 表示文件开头 |
| b=1 | `-a` | 消费旧第 a 行；`,1` 按统一格式省略 |
| b≥2 | `-a,b` | 从旧第 a 行开始消费 b 行 |

新区间 `+c,d` 规则相同；它主要供渲染和反向补丁校验，正向应用的定位以旧区间和上下文为准。

## hunk 合并阈值

阈值是 `2C`。两个 hunk 各自最多带 C 行前缀和 C 行后缀；当间隔 `g=2C` 时，前一 hunk 的 C 行后缀与后一 hunk 的 C 行前缀正好覆盖全部间隔行，GNU `diff -U C` 输出一个连续 hunk。若 `g=2C+1`，中间至少剩 1 行完全不属于任一边上下文，必须分开。

以 C=1 为例：`g=2` 时两行间隔分别作为前 hunk 后缀和后 hunk 前缀，合并；`g=3` 时中间一行无法被任一侧覆盖，分开。若阈值取 C，g=2 会被错误分开；若取 2C+1，g=3 会被错误合并，均与 GNU 不一致。

## Myers 并列选择

Myers 每条对角线上只保留能到达的最远 x。距离相同的路径采用固定回溯规则：可向两个方向到达同一终点时，优先选择更早删除的路径，即在回溯中优先取“插入边”作为最后一步。因此 `a b -> b a` 固定渲染为先 `-a`、上下文 `b`、后 `+a`，而不是先 `+b` 后 `-b`。所有 map 不参与文本决策，行序列均为切片且按同一规则遍历。

## 偏移后的 hunk 平移

设某 hunk 记录的旧起点为 p，实际匹配起点为 q，偏移 `k=q-p`。该 hunk 成功后，其消耗的旧行数与补丁声明一致，但净增减行数已经同时体现在新旧坐标差中。后续 hunk 的搜索中心从其记录起点平移 k，因为前一次偏移说明后续上下文也整体位于相同位移处；搜索半径仍为 F。若后续 hunk 再次找到不同位移，则以新位移继续更新。
