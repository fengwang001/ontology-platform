# DESIGN — 自定义 LZ77 流式压缩

## 流格式（wire 包）

所有整数均为 LEB128 无符号变长编码：每字节低 7 位有效、最高位为续位；
最多 10 字节，第 10 字节不得大于 1，否则判「变长整数溢出」。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 4 字节魔数 `"LZ77"`（`4C 5A 37 37`）+ 1 字节版本 `0x01` |
| 字面量段 | `0x01` + uvarint 长度 n + n 字节原文 |
| 回指 | `0x02` + uvarint 距离 dist + uvarint 长度 len |
| 刷新标记 | `0x03`（无负载） |
| 流尾 | `0x04` + uvarint 原文总长 + uvarint FNV-1a/64 校验和 |

记录自定界，可任意切分喂给解压器；刷新标记为空操作，仅标记 Flush 点。

## 推导一：Write 切法无关（语义 3）与 Flush 的含义

位置 pos 的贪心决策只依赖两类信息：(a) 哈希链候选，全部来自**已处理**且
**小于 pos** 的位置；(b) 从 pos 到第一个失配字节之间的原文。风险在于：
当匹配延伸到当前缓冲末尾时，后续到达的数据可能让它更长——提前提交就会
依赖切法。规则：在 pos 找到长度 L 的匹配，仅当 `pos+L < 已缓冲长度`
（失配字节已出现）、或 `L == maxMatch`（硬上限，不可能再延长）、或
Flush/Close 强制定稿时才提交；否则挂起等待更多数据。字面量决策仅当
pos 之后还有 ≥ minMatch(3) 字节时提交：此时哈希探针完整，且「无匹配」
结论由缓冲内失配或候选链耗尽得出，二者都只依赖 < pos 的历史，不会被
新数据推翻。每次 Find 前先把插入指针补齐到已处理位置，保证候选集恰好是
「全部已处理且 < pos 的位置」，与数据分几次到达无关。因此同一份字节流
任意切分 Write，只要 Flush 点（按累计输入偏移）相同，输出逐字节相同。
推论：Flush 必须**强制结束当前匹配**——它承诺「截至此时的全部输入已可
解码」，不允许再等更多数据，挂起的匹配按当前长度定稿输出。

## 推导二：重叠回指的复制语义（语义 2）

距离 < 长度时，回指的源区间与目标区间重叠：逐字节向前复制
`out[i] = out[i-dist]` 时，刚写出的字节立刻成为后续源字节，于是
`abc`+回指(3,30) 正确展开为 `abcabc…`。若用一次性块复制
（`copy(dst, src)`，memmove 语义相当于先把源整体拷到临时区），源区间里
尚未写出的字节会被当作旧值复制，结果错误。本实现解压时按字节循环
`win.At(dist)` 取字节并立即写回窗口：每次迭代恰好产出一个输出字节，
总代价 O(输出长度)，与任何解压器必须付出的输出代价同阶，不会渐进退化，
因此逐字节循环在这里可以接受，且天然正确。

## 推导三：「距离超过已输出」与「距离超过窗口」为何区分（语义 7）

窗口容量是格式参数：合法压缩器的匹配器只登记距离 ≤ 容量的候选，因此
**绝不可能**产生 dist > 窗口容量的回指；解码端看到它必属损坏，是一类
独立错误。而 dist ≤ 容量但 > 已输出字节数只会出现在流的前容量个字节内，
是另一类损坏（例如流被截掉开头）。两者原因与排查方式不同，必须可分。
并行块压缩中，「预置字典」只是编码器视角：块 N 对上一块末尾的引用落到
流上就是普通的、指向已解码字节的回指，解码器统计的「已输出字节数」是
**全流**已解码字节数，含义不变，dist ≤ 已输出 在解码端依旧成立。

## 推导四：并行块压缩的确定性（语义 9）

块 i 的压缩结果是纯函数 f(块 i 原文, 块 i-1 末尾 ≤ 窗口容量的字节,
配置)：字典在块压缩前一次性装入哈希链，之后只读；不引用其他块的压缩
结果，也无共享可变状态。每个 goroutine 只写自己下标的结果槽，主 goroutine
Wait 后按下标顺序拼接，因此与 workers 数量、调度顺序无关，输出确定。
衔接：记录自定界，直接拼接即合法；块与块之间额外写入一个刷新标记
（最后一块之后除外）使块边界显式可见；流头只写一次，流尾的总长与校验
和覆盖完整输入。块内匹配器容量 = 窗口容量，故 dist ≤ 容量恒成立。
