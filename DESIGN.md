# DESIGN — 带撤回的增量聚合视图维护器

## 0. 问题与约束

基表变更以流式到达（Insert/Delete/Update，带单调递增版本号），视图按分组键维护
Count/Sum/Min/Max/DistinctCount 五个聚合。关键约束：**聚合器只保存 O(1) 级别的聚合
状态（聚合值本身，DistinctCount 另持有一张插入侧计数表），不保存组成员全集**；组成员
全集只由 `view` 层为 Recompute 而维护。撤回能力的推导都在这个约束下进行。

## 1. 各聚合器的撤回策略推导

记聚合状态为 S，被删除记录的值为 v。

- **Count**：S = n。插入 n+1，删除 n-1。删除的贡献与被删记录内容无关，可直接减。
  结论：插入可增量、删除可增量、删除不需要成员。
- **Sum**：S = s。插入 s+v，删除 s-v。加法有逆元，贡献可精确对冲（IEEE754 下按相同
  操作序列回放时位级一致，见 §5）。结论：同 Count。
- **Min**：S = 当前最小值 m。插入：m' = min(m, v)，可增量。删除分两种：
  - v ≠ m：被删记录不是极值，m 不变，无需任何动作；
  - v = m：新最小值是「剩余成员中的最小者」，但 S 里只有 m 一个数，**次小值的任何
    信息都不在状态中**。反证：若只凭 S 就能推出新极值，则 S 必须编码整个有序多重集
    （否则构造两个 S 相同但次小值不同的成员集即矛盾），违背 O(1) 约束。故必须回到
    该组成员重算。Max 完全对称。
- **DistinctCount**：S = 去重计数 d（外加插入侧的 value→refcount 表）。插入：查表知
  v 是否已存在，可增量。删除：「被删值是否仍被组内其他记录持有」是**成员级信息**——
  与 Min 同理，d 一个数回答不了。若插入侧计数表恰好在手且显示 refs[v]>1，则计数
  必然不变，可增量撤回；若是最后持有者（refs[v]==1），计数必然变化，而删除路径
  不承诺保留该表（状态可被裁剪/置换），必须回成员重算确认。

汇总（即 FINDINGS.md 表①的核对目标）：

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
| --- | --- | --- | --- |
| Count | 是 | 是 | 否 |
| Sum | 是 | 是 | 否 |
| Min | 是 | 仅当被删值≠当前极值 | 是 |
| Max | 是 | 仅当被删值≠当前极值 | 是 |
| DistinctCount | 是 | 仅当仍被他人持有 | 是 |

## 2. Recompute 触发条件

`agg.Aggregator` 用两个方法显式声明撤回能力：

- `NeedsMembers() bool`：静态声明「删除时是否需要该组成员」（Count/Sum=false，
  Min/Max/DistinctCount=true）；
- `Retract(v) bool`：尝试增量撤回，返回 false 表示本聚合器需要重算。Min/Max 仅当
  v == 当前极值时返回 false；DistinctCount 仅当被删值是最后持有者时返回 false；
  Count/Sum 恒 true。

`view` 对每个受影响分组逐个调用 `Retract`，凡返回 false 者进入 Recompute 阶段，用该组
当前成员切片重建该聚合器。重算是例外而非常态：Min/Max 只在删到极值时触发，
Count/Sum 永不触发。`view` 内非导出计数器 `recomputes`（按聚合器名）与 `scans`
（重算访问的成员条数）经 `Stats()` 读出；一次重算的扫描数恰为该组当前成员数，不扫
其他组（成员切片按组构造，物理上不可能越组）。

## 3. 多阶段提交与崩溃恢复

`Apply` 分三阶段，全部在写锁内完成：

1. **Apply**：校验（版本、分组键缺失、NaN）后做增量变异（Add/Retract、成员表更新）；
2. **Recompute**：对标记的聚合器按组成员重算；空组整体删除（不留 Count=0 的空组）；
3. **Commit**：先把变更追加到 journal（写盘+Sync），再推进 `lastVersion`。

journal 追加是唯一的提交点。崩溃只能落在「已提交前缀」与「未提交后缀」之间：恢复时
丢弃内存态，用 `journal.Replay` 重放完整记录前缀重建视图，因此 Apply 中途、Recompute
中途、Commit 之前三个崩溃点恢复后的视图都恰等于已提交前缀的全量结果，与不崩溃时
逐字段相同。`ErrCrash`（测试/演示注入）返回后该 View 必须丢弃。

## 4. 版本单调与幂等判定依据

视图记录已应用的最大版本号 `lastVersion`。判定规则：

- `v == lastVersion`：视为同一变更的重复投递，幂等空操作（视图逐字段不变，不计拒绝）；
- `v < lastVersion`：版本回退/乱序，返回 `ErrVersionRegression` 并计入 `rejected`；
- `v > lastVersion`：正常应用。

依据：版本号由上游单调分配，等号只会来自重发（at-least-once），严格小于只会来自
乱序或回退；二者必须区别对待，前者幂等后者拒绝，才能既不污染视图又不丢重发语义。

## 5. Sum 的位级相等与审计

IEEE754 加法不可结合，「增量维护的和」与「任意顺序全量重算的和」在位级上可能不同。
因此 `audit` 的核对分两类：Min/Max/DistinctCount 与顺序无关，由审计器从自建成员集
**全量重算**；Sum/Count 由审计器按相同操作序列独立回放（独立代码路径，仍能抓出丢
更新、串组、版本错乱），比对用 `math.Float64bits` 位级相等。

## 6. 日志格式与截断分类

```
header: magic[8]="ONTJ0001" | version uint32 LE        (12 字节)
record: len uint32 LE | payload[len] | crc32 IEEE LE   (len+8 字节)
```

payload 为 `change.Encode` 的输出。重放按偏移解析，截断点 p 的分类是确定的：

- p < 12 → 头部不完整；
- 落在某记录的 len 字段内 → 长度前缀不完整；
- 落在 payload 内 → 记录体不完整；
- 落在 crc 字段内（含 payload 完整但 crc 缺失/残缺）→ CRC 不匹配；
- p 恰在记录边界 → 合法短日志，生效记录数 = 完整记录数。

半截记录绝不生效：重放只返回完整记录，视图恢复结果等于完整前缀的全量重算。

## 7. 并发模型

`View` 内一把 `sync.RWMutex`：Apply 全程持写锁，读者（`Groups`/`Group`）持读锁且一次
返回整组 `Values` 快照，因此读者不可能观察到「Count 已改 Sum 未改」的半更新态；
Recompute 在写锁内进行，同组并发写入只会串行排队，不会丢失。journal 追加也在同一
写锁内，天然串行。

## 8. 边界语义

- 分组键：`nil`（缺失）→ 拒绝并计数；空串 `""` 是合法键；
- 值为 NaN → 拒绝并计数；±0 在入库时规范化为 +0（`change.Canonicalize`），视为相等；
- Update = 原组撤回 + 新组插入，同组则组内换值；删除/更新未知 ID、重复插入 → 空操作；
- 组内最后一条记录被删 → 整组从 `Groups()` 消失，`Group(key)` 返回不存在。
