# 设计推导（严格 RLE）

不变量：解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（编码唯一）。

## 拒绝清单（逐条：若接受，哪个 t 违反不变量）

1. 显式次数 1：接受 `1a`，则 `Decode` 得 `a`，规范编码为 `a`，`a != 1a`。
2. 次数 0：`0a` 解出空串，规范编码为空串 `"" != 0a`；次数必须 >=1。
3. 前导零：`01a` 解出 `a`，规范编码为 `a != 01a`；`00a` 又命中零次计数。
4. 相邻同符号游程：`2a3a` 解出 `aaaaa`，最长游程规则下规范编码为 `5a`，不等于 `2a3a`。
5. `\` 后非数字非反斜杠：如 `\a`、`\世`。符号仅数字与反斜杠需要转义；
   若把 `\a` 解成 `a`，规范编码是 `a != \a`；若解成别的更无定义，故拒绝。
6. 末尾孤立 `\`：`a\` 后无被转义字符，缺少一个符号，无法构成游程。
7. 末尾只有次数没有符号：`12` 没有符号无法构成游程；若按 0 符号解则又违反第 2 条。
8. 非法 UTF-8：原语只能是码点。`Decode` 目标是合法 UTF-8 字符串，非法字节无对应码点；
   且其编码结果根本无法写回原字节，必违反不变量。

## 大次数取舍（第 3 条）

选择设上限拒绝：次数上限 = `math.MaxUint64`（`18446744073709551615`）。
理由：十进制累加用溢出哨兵判定，超过上限即返回 `ErrCountTooLarge`，
绝不回绕成小数；不支持任意精度整数以保持实现简单且次数无需随长度预分配。
解码不按次数预分配：游程先暂存，仅在其后续符号确认合法后分块写出。
另设可配置的输出总字节上限（`DecodeLimit` / `NewDecoderLimit`，默认 64 MiB），
超限在写出前返回 `ErrLimit`，故 `999...a` 不会撑爆内存。

## 语义与测试对照

| 第三节语义 | 钉住它的测试函数 |
| --- | --- |
| 1 往返 | `TestRoundTrip` / `TestRoundTripRandom` |
| 2 规范严格（8 类拒绝+偏移） | `TestRejectTable` / `TestErrorsDistinct` |
| 3 大次数与输出上限 | `TestHugeCount` / `TestOutputLimit` |
| 4 最长游程/不规范化 | `TestEncode` / `TestCombining` |
| 5 跨切分点一致 | `TestAllSplits` |
| 四 输入字节计数 | `TestInspectCounter` |
