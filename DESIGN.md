# DESIGN

## 1. 零计数 hunk 头的行号

统一格式里 `-a,b` 的 a 是该 hunk 在旧文件中的起始行（1 起），b 是覆盖行数。纯插入时 b=0，
hunk 不覆盖任何旧行，"起始行"必须退化为一个具体位置，GNU 取**插入点前一行**的行号：
插入在文件开头 → 前面没有行 → a=0（样例 `@@ -0,0 +1 @@`）；插在第 2 行之后 → a=2
（样例 `@@ -2,0 +3 @@`）。即 a = 插入点前最后一个旧行号（0..N），而"将要插入的位置"
是 a+1。Apply 解析时：b=0 的 hunk 不消费旧行，输出插入行的位置是**已消费旧行数达到 a 之后**
（消费 a 行旧内容后插入）；d=0（纯删除）对称，新文件起始 c = 删除点前最后一个新行号。

| 情形 | 书写 | Apply 解释 |
|---|---|---|
| b=0（纯插入） | `-a,0`，a=插入点前一行行号（开头为0） | 旧侧消费 a 行后放入 `+` 行，再继续 |
| b=1 | `-a`（省略 `,1`） | 消费从 a 起的 1 行 |
| b≥2 | `-a,b` | 消费从 a 起的 b 行 |

## 2. hunk 合并阈值

每个 hunk 取改动两侧各 C 行上下文。两 hunk 间有 g 行未改动行时：前 hunk 保留后 C 行、
后 hunk 保留前 C 行。g≤2C 时两份上下文重叠或相接（g=2C 时恰好无间隙地拼成 2C 行），
GNU 合并；g=2C+1 时中间剩 1 行不属于任何 hunk，分开。故阈值 **g ≤ 2C 合并**。
反证：C=1，g=2，改动相隔 2 行。阈值若为 C(=1) 则拆成两个 hunk，但 GNU -U1 输出一个
hunk（上下文正好相接）；阈值若为 2C+1(=3) 则 g=3 也合并，而 GNU 在 g=3 时两 hunk 中间
留 1 行未覆盖、输出两个 hunk。只有 2C 两种边界都对。

## 3. 并列最短脚本的选择

Myers 在同一条对角线上同一轮可"向下（删）"或"向右（插）"。本实现**优先删除（向下）**：
新对角线 k 先由 k-1（删除）到达，仅当 k+1（插入）给出更大的 x 时才插入。因此
`a b → b a` 的脚本为 `-a`、` b`、`+a`（删除块排在插入块之前），所有并列输入输出固定、
可重复。hunk 内 `-` 行成组排在相邻 `+` 行之前（标准 unified 形态）。

## 4. 偏移后后续 hunk 的起点平移

hunk i 在记录位置之外以偏移 δ 应用成功，说明目标文本相对补丁整体平移了 δ 行；后续 hunk
的记录位置是基于"前序 hunk 按原位置应用"编号的，故其查找起点同样平移 δ（累计偏移），
搜索窗口为 [记录位置+累计偏移−F, 记录位置+累计偏移+F]。每个 hunk 独立选最近候选；
失败则整补丁回滚。

## 其他约定

- 行：保留原行尾 `\n`/`\r\n`/无；补丁行体去掉行尾后加首字符标记，无行尾行后跟
  `\ No newline at end of file`。
- 计数器：Myers 每次对角前进记 1，snake 中每次逐行比较记 1（含失配那次）。
