# CSV 方言解析器设计

## 1. 空行与单列空值（推导）
空字节即 0 条记录。`\n` 前无字节 = 空行。两种读法：(a) 跳过；(b) 一条含一个未引号空字段的记录。选 (a)：空行不含任何逗号，无法与「字段间留白」区分，跳过是唯一确定且不产生合成数据的选择。于是单列记录 `""` 是合法的「一条空值记录」，而 `a\n\nb` 中间空行被跳过。回写推导：单列表中记录 `(unquoted "")` 若写为空串再加 `\n`，重新解析时该「行」为空行被跳过 → 丢记录；故**单列记录的未引号空字段必须加引号**。这正是「空行跳过」迫使回写器额外必须加引号的情形（另：含 `, " \r \n` 必加引号）。末尾最后一个换行不产生记录；`a`（无尾换行）也是合法记录。

## 2. 半包续传
纯字节状态机，状态与挂起的字段缓冲跨 Feed 保留；字段偏移在「该字段首个内容字节/开引号」时按全局 consumed 计数记录，故切分点落在 `""` 中间、`\r` 与 `\n` 之间都不影响结果。CR 待定：未引号态见 `\r` 进 `crU`，不发事件；下一字节是 `\n` 则行结束（CR 不算字段内容），否则在该字节处报 `ErrBareCR`（引号字段内 `\r` 是内容，另设 `crQ`：其后 `\n` 整体作为两字节内容保留，否则该 `\r` 仅作内容）。流在 `\r` 处 Close：未引号态报 `ErrBareCR`；引号态把 `\r` 作内容并报未闭合。

## 3. par 切点（双假设）
段 worker 无法得知起点是否在引号内，故每段各跑两版：A=假设起点在 FRESH（引号外、字段首），B=假设起点在 QUOTED 内。真实状态从左到右由前段末状态唯一确定（状态仅 FS/UNQ/QUOT/AQ/CR 类），据此在 A/B 间选一版。关键特例：前段末为 AQ（刚收到闭合引号）时，后段首字节可能是 `, \n \r "` 或其它——`"` 表示转义引号（回 QUOTED），其它若非法则错在「闭合引号之后」；用 B 版处理首字节 `"` 的语义、用 A 版处理分隔符/行尾/非法，二者在该字节之后状态一致，故选择无需回扫，每字节最多两遍。
拼接：被选版本的字段偏移加段起点即全局偏移；跨段字段把右段片段 append 到左段挂起字段（`""` 被切分时左右各做转义计数，还原出的恰好一个 `"` 不变）。段内错误的局部记录/字段号加左段选出版本已结束的记录/字段序号基数为全局号；字节偏移直接加段起点（consumed 即全局坐标）。

## 4. 上限「立刻」
字段计数在**内容字节到达时**递增：未引号态每个普通字节 +1；引号态内每个实际字符 +1，其中 `""` 在见到第二个 `"` 时整体算 1 个字符（第一个 `"` 处于 QUOTE_SEEN 不计数）。计数先于缓冲写入，超限字节当次 Feed 立即报 `ErrFieldTooLarge`。记录字段数在发出每字段（含行末空字段）时检查；总记录数在记录结束时检查。终态后 Feed/Close 返回同一错误。

## 5. 状态转移表
字节类别：`C` 普通字节（非 `,"\r\n`）、`,` `"` `\r` `\n`。动作：emit=发字段，row=行结束，+n=计数。

| 状态 | C | , | " | \r | \n |
|---|---|---|---|---|---|
| FS 字段首 | →UNQ,+1（记首偏移） | emit 空字段→FS | →QUOT（偏移=本字节） | →crU | 空行：不发记录→FS |
| UNQ 未引号 | →UNQ,+1 | emit→FS | **ErrQuoteInField** | →crU | row→FS |
| QUOT 引号内 | →QUOT,+1 | →QUOT,+1 | →AQ | →crQ(内容,+1) | 内容,+1 |
| AQ 见引号 | →QUOT,+1（`""`仅+1） | emit→FS | **ErrCharsAfterQuote** | →crA | row→FS |
| crU CR待定 | 先 **ErrBareCR** | 同左 | 同左 | 同左 | （不发生，`\r`已吞） |
| crA | 先 **ErrCharsAfterQuote** | 同左 | 同左 | 同左 | row→FS（CRLF行尾） |
| crQ 引号内CR | 内容→QUOT,+1 | 同左 | →AQ | 内容,+1 | 内容,+1（CRLF两字节全留） |

Close 时：QUOT/crQ → ErrUnclosedQuote；crU/crA → ErrBareCR；FS/UNQ/AQ 的挂起字段作无尾换行记录发出（空文件不发）。
