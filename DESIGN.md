# 统一格式 diff 工具设计推导

包依赖：`lines` ← `edit` ← `hunk` ← `udiff` ← `patch`，单向无反向。

## 1. hunk 头零计数时的行号（语义 4）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0`；`a\n`→空 得 `+0,0`。
三例中计数为 0 的一侧没有「第一行」可编号，数字恰好等于插入点**之前**的行数：
插入点前有 0 行 → `0`，有 2 行 → `2`。因此规则是：**计数为 0 时，写的是前一行的行号
（= 0 基插入下标），而不是将要插入的位置**。若写成「插入位置」（1 基），则 `-0,0` 无法
表达「插在第 1 行之前」（位置 1 与「插在第 1 行之后」冲突），而「前一行行号」对文件
开头（第 0 行）有自然取值 0，无歧义。`Apply` 解析：0 基起点 = b>0 ? a-1 : a。

| 情形 | 头 `-a,b` 写法 | Apply 对起点的解释（0 基） |
|------|----------------|----------------------------|
| b=0  | a = 插入点前的行数（前一行行号，可含 0） | 起点 = a（插在第 a 行之后） |
| b=1  | a = 首行行号，省略 `,1` | 起点 = a-1 |
| b>=2 | a = 首行行号，写 `,b` | 起点 = a-1 |

## 2. hunk 合并阈值 = 2C（语义 5）

两段改动间隔 g 行未改动行。若拆成两个 hunk，前 hunk 带 C 行尾部上下文、后 hunk 带 C 行
头部上下文，共覆盖 2C 行。g <= 2C 时两段上下文相触或重叠，GNU 必合并；g > 2C 时至少
有 g-2C 行无法展示，必须拆分。实测（C=3，改动第 1、8 行，g=6=2C）输出单个
`@@ -1,11 +1,11 @@`；g=7=2C+1 时拆成两个 hunk。
- 若阈值取 C：g 在 (C, 2C] 时被拆开，但两段上下文区域重叠，拆分后同一行会出现在两个
  hunk 里或上下文不足 C 行，与 GNU 输出不符。
- 若阈值取 2C+1：g=2C 时被拆开，两个 hunk 的上下文恰好首尾相接、中间无任何省略行，
  GNU 从不产生这种「相邻无间隔」的两个 hunk（实测为单 hunk）。

## 3. 最短脚本并列选择（语义 3）

Myers 前向推进时，对角线 k 上若 `V[k-1] >= V[k+1]` 选右移（删除），否则选下移（插入），
即**并列时删除优先**。效果：同一连续改动段内所有 `-` 行排在 `+` 行之前。例 `a b`→`b a`
生成「删 a（行1）、留 b、插 a（行2 后）」，C=0 时为 `-1 +0,0` 与 `-2,0 +2` 两个 hunk，
与 GNU diff 一致；若插入优先则得到先插后删的镜像脚本，hunk 内 `+` 先于 `-`。

## 4. 偏移应用后后续 hunk 的查找起点（语义 7）

匹配在**原始目标行序列**上进行，因此已成功 hunk 的增删行数差不影响后续 hunk 的查找
起点（行号都相对旧文件）；但前一个 hunk 的实际落点偏移 k = 落点 - 记录起点要传播：
后一个 hunk 的查找基准 = 记录起点 + k。理由：目标文本若在补丁区域之前被整体插入/删除
了若干行，所有后续 hunk 会带有相同的整体位移，带上 k 才不会浪费偏移预算、也避免在
窗口内误选更近的错误候选。GNU patch 的 `last_offset` 机制与此相同。
