# Float64 最短往返文本编解码设计

## 1. 位分解与精确值

- 用 `math.Float64bits` 取 64 位，再拆成 1 位符号、11 位阶码、52 位尾数。
- 正规数尾数补回隐含前导 1：值为 `sign * 2^e2 * m`；次正规数阶码固定为最小值，无隐含位。
- 全程用 `math/big` 大整数（分子/分母）保存精确有理数，避免浮点乘除产生二次误差。

## 2. 为什么不是固定十七位

- 17 位充分：`2^53 = 9007199254740992 < 10^17`，任意 binary64 的 17 位十进制舍入值
  落在唯一舍入区间内，必然逐位往返；但这是全局上界，不是每个数的最短答案。
- 17 位不最短：`0.1` 的真值是 `0.1000000000000000055511151231257827…`，1 位即往返；
  `1/3` 需要 16 位。
- 15 位不充分：`10^15 < 2^53`，存在必须 16 甚至 17 位的值（测试中有对拍实例）。
- 结论：不能按固定位数截断，只能从 1 位开始逐位试探。

## 3. 逐位递增加精确往返检查

对非零有限数，从 1 位有效数字开始：

1. 将精确有理数在十进制下舍入为当前 `n` 位有效数字，得到候选 `floor` 与 `ceil` 两个
   （它们是离原值最近的两个 n 位十进制数；解回的舍入区间是含原值的凸区间，故只要存在
   可往返的 n 位候选，这两个中至少一个也可往返——检查它们即保证最短性）。
2. 用精确算术把候选解回：候选是有理数 `p/q`，用 `BitLen` 定二进制阶，
   再以「比较 `2*余数` 与除数、恰半取偶」的大整数舍入得到 53 位（或次正规）尾数，
   重组为 float64 位模式，与原值逐位比较。
3. 若两个候选都失败，令 `n++`；第一个成功的 `n` 即最短位数，最大不超过 17。

全程只有大整数运算，不用任何浮点乘除回代（浮点回代会二次舍入：
测试中固定了一个 16 位候选浮点回代看似相等、精确比较不等的值）。

## 4. 十进制候选生成

先求 `e10 = floor(log10(|x|))`（浮点 `Log10` 估值后用大整数比较精确修正），再令缩放系数 `s = n-1-e10`。候选系数取 `floor(|x|*10^s)` 与 `ceil(|x|*10^s)` 两个，先试较近者；进位到 `10^n` 时规范化为系数 1。命中后把系数十进制串的尾随零去掉并相应调整指数，因此输出只含有效数字。

## 5. 文本形式选择

最短有效数字确定后，设首数字的十进制位权为 `E`（值约为 `d.ddd × 10^E`）。规则固定为：

- `-4 <= E < 21`：定点形式；`E >= 0` 在整数后补零或嵌入小数点，`E < 0` 补前导零和小数点。
- `E < -4` 或 `E >= 21`：科学记数形式 `d.dddde±X`，只有一位整数；只有一位有效数字时省略小数点。
- 正零输出 `0`，负零输出 `-0`。

该规则无随机性、无全局状态；同一位模式永远得到同一文本。

## 6. 解析与错误

解析支持可选符号、数字与小数点、可选十进制指数。尾数与指数先化为 `big.Rat`，再按最近偶数规则舍入为 binary64。解析独立于编码器，不接受 17 位以上的有限文本，以免调用者误以为超长近似仍保证身份往返。

错误可用 `errors.Is` 区分：空串、非法字符（错误位置附在错误中）、指数溢出、有效数字过多。`NaN` 与 `±Inf` 不定义文本：它们没有唯一最短十进制身份，静默生成字符串会把"不能编码的值"伪装成成功往返。

## 7. 确定性与计数

所有判定都是输入位模式的纯函数，不读写影响结果的全局状态。`dec` 包内的非导出计数器（配导出读取器）仅记录精确往返检查的执行次数，属诊断信息；`0.1` 在 1 位成功，计数不超过 3，证明算法没有无条件尝试 17 次。
