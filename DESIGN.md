# 严格 Base64 规范尾部推导

标准 Base64 的每个字符表示 6 个比特，4 个字符共表示 24 个比特。

- 无填充：4 个字符还原 3 个字节，24 个比特全部有效。
- 一个 `=`：前 3 个字符还原 2 个字节，只用前 16 个比特；第 3 个字符最低 2 位必须为 `00`。
- 两个 `=`：前 2 个字符还原 1 个字节，只用前 8 个比特；第 2 个字符最低 4 位必须为 `0000`。

因此，规范尾部不是比较宽松解码后的字节，而是检查最后一个有效字符中“落入已丢弃比特区”的后缀：

- `QQ==`：第二个 `Q` 为 `010000`，低 4 位是 `0000`，合法。
- `QR==`：`R` 为 `010001`，低 4 位是 `0001`。这些 1 不会进入输出字节，却携带了非零信息，所以拒绝。
- `QUI=`：第三个 `I` 为 `001000`，低 2 位是 `00`，合法。
- `QUJ=`：`J` 为 `001001`，低 2 位是 `01`，所以拒绝。

编码时先在字节流末尾补零到 24 位，再把这些补零形成的字符与固定数量的 `=` 一起输出。严格解码要求所有被补出来、且不属于原始字节的比特必须仍为零。由于任意字节串只会产生这一个零后缀表示，编码结果唯一；接受非零后缀会让多个输入文本解码成同一字节串，破坏 `Decode` 与 `Encode` 的互逆关系。

## 测试对照

待实现后补齐。
