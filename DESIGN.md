# 严格 Base64 尾部规则

一个 4 字符标准组携带 24 bit；严格模式先把 4 个 6 bit 值拼成
`v0<<18 | v1<<12 | v2<<6 | v3`。

- 无填充：输出全部 3 字节，四个字符任意合法值都代表唯一 3 字节。
- `xx==`：只输出第 1 字节，信息只来自 `v0` 的 6 bit 与 `v1` 高 2 bit，即第 7、8 bit。因此 `v1` 的低 4 bit 必须全为 0；`QQ==` 合法，`QR==` 中该 4 bit 非零，拒绝。
- `xxx=`：输出前 2 字节，第 3 字节不输出；信息来自 `v0,v1` 全部 12 bit 和 `v2` 高 4 bit，即第 13–16 bit。因此 `v2` 的低 2 bit 必须全为 0；`QUI=` 合法，`QUJ=` 中该 2 bit 非零，拒绝。

统一规则：填充组中不参与输出字节的尾部 bit 必须全部为零。被检查的就是 `xx==` 时 `v1&0b1111`，和 `xxx=` 时 `v2&0b11`。

同时，无填充组不得含 `=`；一个组只允许精确为 `xxxx`、`xxx=`、`xx==`，填充后不得再有数据。于是每个字节串有唯一长度余数、唯一填充形状和唯一规范字符组，任何非规范宽松别名都会被拒绝，保证 `Encode(Decode(y))=y`。

## 语义与测试对照

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1. 标准字母表与 `=` 填充 | 待实现 |
| 2. 规范形式样例 | 待实现 |
| 3. 换行位置 | 待实现 |
| 4. 五类错误与偏移 | 待实现 |
| 5. 跨切分点一致 | 待实现 |
| 6. 编码解码往返 | 待实现 |
| 7. 输出上限 | 待实现 |

计数器复杂度：待实现。
