# 设计：带转义的文本 RLE 编解码器

## 严格解码的拒绝清单（由 Encode(Decode(t)) == t 逐条推导）

合法编码唯一，等价于：任何被接受的 t 都必须是 Decode(t) 的规范编码。
以下每一类若被接受，都存在一个 t 使该等式被破坏，故必须拒绝：

- 显式次数 1：`1a` 若解码为 `a`，则 `Encode("a")="a"` ≠ `1a`。
- 次数 0：`0a` 若解码为 `""`，则 `Encode("")=""` ≠ `0a`；解码为任何含 a 的串同样不成立。
- 前导零：`01a` 若解码为 `a`，则 `Encode("a")="a"` ≠ `01a`。
- 相邻同符号：`2a3a` 若解码为 `aaaaa`，则 `Encode("aaaaa")="5a"` ≠ `2a3a`（Encode 取最长游程）。
- `\` 后非数字非反斜杠：`\q` 若解码为 `q`，则 `Encode("q")="q"` ≠ `\q`；`\` 的唯一合法用途是转义数字与 `\`。
- 末尾孤立 `\`：`a\` 若解码为 `a`，则 `Encode("a")="a"` ≠ `a\`。
- 末尾只有次数：`12` 没有符号，任何解码结果再编码都会带上符号，必 ≠ `12`。
- 非法 UTF-8：Decode 的输出必须是合法 UTF-8 字符串（Encode 的定义域），无法表示，拒绝。

每类对应一个彼此可区分的哨兵错误（`ErrExplicitOne` 等），包装在 `*rle.Error`
中携带字节偏移，调用方用 `errors.Is`/`errors.As` 判定。

## 大次数的取舍

拒绝超过 `uint64` 的次数：逐位用 `runs.AddDigit` 做溢出检测，溢出即报
`ErrCountOverflow`（带偏移），不会静默回绕，也不按次数分配内存。理由：任何
大于 2^64-1 的次数都超出可寻址、可计费的输出规模，"支持"它没有可实现的语义。
`uint64` 范围内的次数仍可能巨大：解码输出字节上限可配置（`NewDecoder` 的
`maxOut`，`Decode` 用 `DefaultMaxOutput`），超限报 `ErrOutputTooLarge`；
发射按固定小块循环写入，内存占用与次数无关（O(1)）。

## 语义与测试对照

（待测试落定后补齐）
