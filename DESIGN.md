# DESIGN：带路径语义的通配模式

## 1. `x/**` 与 `**/x` 的边界推导

唯一规则：`/` 是段分隔符；单独成段的 `**` 表示「零个或多个完整段」；其他段必须逐段字面/原子匹配。

- `x/**`：`x` 段后接一个 `**` 段。`**` 匹配 0 段 → `x`（命中）。`x/` 有两个段：`x` 与空段，`**` 可匹配 1 个空段 → 命中（空段也是段）。`x/y` 命中。
- `**/x`：`**` 匹配 0 段 → `x`（命中）。`/x` 是 `[空段, x]`，`**` 可匹配那个空段 → 命中。
- `a/**/b` 对 `a/b`：`**` 匹配 0 段，两侧 `a`、`b` 直接相邻命中。以上全部是同一条「0 段也合法、空段也是段」的推论。

## 2. 空串路径

路径按 `/` 切段：`""` 是恰好一个空段；段内通配只在段内工作。

- `*` 是单段模式，其段内 `*` 可匹配 0 个码点，故匹配空段 → 匹配 `""`。
- `**` 是「零个或多个段」，0 段合法，路径 `""` 视为 1 个空段也合法 → 匹配 `""`。
- `?` 要求恰好一个码点，空段 0 码点 → 不匹配。

## 3. 无回溯算法

段内：把一个模式段编译成「字面/`?`/字符类」原子与若干 `*`。执行用位级 NFA 模拟：每读入一个码点做一次 `(状态 ∪ 星闭包)` 的位转移，命中集合非空即成功。星号给状态集合加闭包，本质是「只保留最后一个 `*` 的回退点」的集合化表述。

为何「只记最后一个 `*` 回退点」正确：若较早星号的某次让步能导向成功，则较晚星号出现后，较早星号多吃的前缀可改由较晚星号吃掉，从较晚回退点重试必不劣；较早星号的独立信息被较晚星号支配（支配性论证），故单点回退不漏匹配，且每个码点只做常数次转移 → O(模式原子数 × 段长)。

为何不能照搬到跨段 `**`：段内重试代价是一个 O(1) 码点比较，让步廉价；而 `**` 的一次「回退」是整段位置的回退，且 `**` 之后的段谓词（含字符类/`*`）匹配代价非 O(1)，多个 `**` 的让步无法证明彼此支配。故跨段改用段级 DP：可达段集合按「同段原子前进」与「经 `**` 前进任意段」两条边传播，每 (模式段, 路径段) 对至多求一次段谓词，整体 O(模式长度 × 路径码点数)，无指数回溯。

## 4. 规则集版本一致

`set` 只持有一个不可变 `*ruleset` 指针，用 `atomic.Pointer` 发布。一次查询在入口读取一次指针，之后全程使用该快照：命中结论与 `Explain` 的序号、原文天然同版本。整体替换先在指针外编译并校验全部规则（含上限），任一失败即返回错误且不动旧指针，旧版本字节不变，正在进行的查询无感知。

## 5. 匹配结果表（✓ 匹配，✗ 不匹配）

| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x` | ✗ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ |
| `*` | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b` | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |

说明：`x/**` 对 `""`：字面段 `x` 不匹配空段；`**/x` 对 `x/`：末段为空 ≠ `x`；`**` 对任意路径都成立（零段或由 `**` 吃掉全部段，含空段）。
