# 设计：可撤销的批量重命名与冲突解析器

仅用标准库。五个包：`name`（名字集合）、`plan`（冲突检测+排序）、
`cycle`（破环临时名）、`apply`（执行/回滚）、`undo`（日志逆序撤销）。

## 1. 依赖图与排序规则

把每个请求 `old -> new` 视为一条有向边；若 `new` 同时是另一个尚未执行
请求的 `old`（点的入度与出度都非零），则二者相邻。

依赖定义：请求 `old -> new` 依赖于「把 `new` 这个名字腾出来」的请求
`new -> x`。即边方向 `old -> new` 给出依赖 `old->new` 依赖 `new->x`。
对依赖图做拓扑序，被依赖者先执行：

- `{a->b, b->c}`：`a->b` 依赖 `b->c`，顺序为 `b->c, a->b`。
- 每条边执行的充要条件是其目标名当前不存在，因此拓扑序下中途绝不覆盖。
- 同层（无先后约束）按 `old` 名字典序，保证顺序确定。
- 自环 `a->a`：旧名与新名相同，移动后集合不变，定义为**无操作（noop）**，
  不参与冲突、排序与执行（不计临时名）。
- 空串是合法名字；路径分隔符 `/`、`\` 按普通字符处理，不做任何路径语义。

图中每个顶点出度、入度至多为 1（冲突排除后），分量只有链与简单环。

## 2. 破环通用做法与临时名数量

环 `x1->x2, x2->x3, ..., xk->x1` 无论怎么排都先覆盖，必须借临时名。
取环中字典序最小者 `m = min(xi)`，生成一个与现存名及所有目标名都不冲突的
临时名 `t`，顺序：

1. `m -> t`（腾出 `m`）
2. 沿入边方向依次搬：`pred(m) -> m`, ...，最后 `m` 的后继链闭合 `t -> x2`。

二元环 `{a->b,b->a}`：`a->t, b->a, t->b`。
三元环 `{a->b,b->c,c->a}`：`a->t, c->a, b->c, t->b`。

**一个环只需一个临时名**：第 1 步腾出一个缺口后，环其余 `k` 步都搬到
「刚腾出的空位」，最后一步把临时名搬到已腾出的目标位。故 10 个互不相交
的环恰好产生 10 个临时名。

临时名生成：候选 `tmp, tmp1, tmp2, ...`，每个候选都校验「不属于现存名集合
且不等于任何请求的新名」，冲突则递增重试；超过合理上界（如 100 万）返回
`ErrTempNameExhausted`（可判定错误），绝不覆盖数据。

## 3. 四类冲突（均可用 `errors.Is` 区分，检测先于任何修改）

| 错误 | 判定条件 |
| --- | --- |
| `ErrTargetExists` | `new` 已存在，且本批没有以该名为 `old` 的请求（不会被搬走） |
| `ErrDuplicateTarget` | 两个不同 `old` 指向同一 `new`，报出两个旧名 |
| `ErrDuplicateOld` | 同一 `old` 出现两次，报出两个新名 |
| `ErrOldMissing` | `old` 不在命名空间中 |

检测只做只读遍历，收集完所有冲突后再返回；有冲突则不进入排序/执行，
命名空间逐元素不变。冲突优先级固定（缺失旧名 → 重复旧名 → 目标已存在 →
重复目标），便于确定性测试。

## 4. 复杂度

冲突检测与排序均为单次哈希遍历 + Kahn 拓扑，代价
`O(R + N)`（R 请求数，N 涉及名字数）。`plan` 内非导出计数器
`lookups` 记录哈希查找次数，断言 `lookups <= 4*(R+N)`（5 万请求）。

## 5. 执行、回滚与并发

- `Namespace` 内含 `sync.RWMutex`；批量执行期间持写锁，外部
  `Rename` 用 `TryLock` 语义测试并发修改被阻塞/拒绝。
- 执行序列中每一步先校验「目标名不存在」（快照检查器断言），再原子搬运。
- 注入失败：第 k 步返回注入错误后，已执行步骤按**逆序**撤销（把名字搬回），
  命名空间恢复到执行前逐元素相同；k 取 1、中间、最后一步。
- 执行前记录前镜像到日志，供 `undo` 使用。

## 6. 日志格式与截断分类

二进制追加日志：魔数+版本头（`ONTJNL1\n`，16 字节定长头），其后每条：

`[type:1][olen:2][nlen:2][old][new][crc32:4]`

三类解析错误，均可 `errors.Is`：

| 错误 | 字节区间（文件长度 L） | 含义 |
| --- | --- | --- |
| `ErrTruncatedHeader` | `0 .. <16` | 头部不完整 |
| `ErrTruncatedRecord` | 头部之后、某条记录声明长度未写满 | 记录不完整 |
| `ErrCRCMismatch` | 记录完整但 CRC32 校验失败 | 数据损坏 |

撤销按**最大可恢复前缀**（第一条无法解析/校验的记录之前）逆序执行，
并返回无法撤销的步骤数；对长度 1..L-1 逐字节截断循环分类。
重复撤销幂等：成功撤销后日志头部标记 `done`/重写为已完成态，第二次为无操作。

## 7. 确定性

拓扑同层按字典序、破环选最小名、冲突按固定优先级。请求集合构造顺序打乱
20 次，规划出的步骤序列逐元素相同。
