# RLE 设计说明

## 严格解码拒绝清单（不变量：接受的每个 t 必须满足 Encode(Decode(t)) == t）

记 D(t)=Decode(t)，E(s)=Encode(s)。

1. 显式次数 1：t=`1a` 时 D(t)=`a`，而 E(`a`)=`a`≠t。规范编码省略 1，拒绝，偏移指向该游程起点。
2. 次数 0：t=`0a` 时该游程贡献为空，D(t)=``，E(``)=``≠t；E 从不产生 0，拒绝。
3. 前导零：t=`01a` 与 t=`1a`/`a` 解码结果同为 `a`，E(`a`)=`a`≠`01a`，同一结果有多种编码即破坏唯一性，拒绝。
4. 相邻同符号游程：t=`2a3a` 时 D(t)=`aaaaa`，E(`aaaaa`)=`5a`≠t；最长游程必须合并，须看到下一游程符号才能判定，拒绝时偏移指向下一游程起点。
5. 非法转义：t=`\a` 时 D(t)=`a`，E(`a`)=`a`≠`\a`；规范编码只转义数字与反斜杠，拒绝，偏移指向 `\`。
6. 末尾孤立 `\`：游程缺符号，不存在可解码的串；且任何 E(s) 都以符号结尾（`\` 必与后随字符合成转义），拒绝，偏移指向该 `\`。
7. 末尾只有次数：t=`12` 缺符号，游程不完整；E 的输出永远以符号结尾，拒绝，偏移指向次数起点。
8. 非法 UTF-8：E 的输出恒为合法 UTF-8（原样写出的均为合法码点，转义只针对 ASCII），非法字节不可能出现在规范输出中，拒绝，偏移指向该字节。

各类错误为彼此可区分的哨兵错误，统一包在具名 `DecodeError`（含绝对字节偏移 `Offset`）中，可用 `errors.Is` 判定。

## 大次数（语义第 3 条）的取舍

选择**拒绝超限次数**，上限两级：

- 数字累加按位进行，超过 `math.MaxInt64` 即返回 `ErrCountTooLarge`（Go 字符串长度受 `int` 约束，任何被接受的游程最终都要在 `string` 输出中物化，超过该值在物理上无法表示），如 `99999999999999999999a` 在读到数字途中即拒绝，不溢出、不按次数分配。
- 输出总字节数有可配置上限（`WithMaxOutputBytes`，默认 64 MiB）；每次准备刷出一个已闭合游程前，先按 `次数×码点字节宽` 做乘法上溢与余量检查，超限返回 `ErrOutputLimit`，输出按游程增量写入，从不在解码前预分配与次数成正比的内存。

## 语义—测试对照表

（实现完成后补齐）
