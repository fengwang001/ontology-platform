# 只读快照一致性导出器 — 设计推导

## 1. 写时复制（COW）的触发点

快照 S 在版本 v 创建，必须永远看到 v 时刻的键值集合。
导出遍历期间，若 `Put(k,new)` 直接覆盖存储，导出读到 new，快照被污染。
正确做法：写入前，若存在未关闭快照且其版本 v 可见旧值 old，
先把 old 复制进该快照的保留表，再覆盖主存储。读取顺序：
快照保留表命中则用保留值，否则读主存储当前值。
只保留“被覆盖”的键，故保留项数 = 导出期间被改写的不同键数，与总键数无关。

## 2. 保留值的释放条件（为什么按最老快照版本判断）

每个快照持自己的保留表时，关闭即可释放本表，多个重叠快照各自独立。
关键是写入时决定“为谁保留”：旧值 old 只对版本 ≤ 当前写版本、
且尚未看到本次写入的快照可见。所有存活快照中，版本最低（最老）的一个
可见性最弱——若连它都已在新值之后创建，则没有任何存活快照需要 old。
因此按最老存活快照版本判断：被改键在最老快照中存在/可见才需保留；
为避免重复内存，旧值挂到最老快照上即可被所有更年轻快照共享读取
（年轻快照读保留表时沿链回退到老快照）。最老快照关闭后链由次老者接管，
只有所有相关快照都关闭，保留值才最终释放。

## 3. 导出顺序与一致性无关

块的物理切分依赖顺序，但总校验和只对“快照 v 的逻辑键值集合”计算：
键按字典序排序后逐个写入 hash，与导出遍历顺序无关。
故字典序/逆序导出产生不同分块，却有相同总校验和；
恢复时按块号重组逻辑集合即可比对。

## 4. 导出文件布局与续传

单文件：每块 = 块头(magic,块号,键数,体长度,体CRC32) + 自描述记录体
(每条记录: 键长,键,值长,值) + 4 字节体 CRC 尾。块头自带块体长度与 CRC，
读取无需清单即可在任意字节流中定位块边界并校验。
文件尾部追加清单尾：magic + 清单长度 + JSON(版本、块大小、顺序、
块号列表、总校验和、SHA256) + 尾CRC。清单只在全部块写完后追加，
因此“无清单尾”天然等于导出未完成，可凭块头从最后完整块的下一块续传。

## 5. 截断分类与可判定错误

逐字节截断 1..len-1，按落点分类（偏移见 FINDINGS 表）：
切在清单尾区间 → 清单不完整（块已全，可从最后一块续传/直接补尾）；
切在某块头区间 → 块头不完整；切在块体（含CRC尾）且未到声明长度
→ 块体不完整；块长完整但 CRC 不符（翻转/截断在 CRC 尾字节）
→ CRC 不匹配。四类各有哨兵错误，`errors.Is` 可区分。
可续传起始块 = 第一个不完整/校验失败的块号。

## 6. 快照过期为何必须拒绝续传

清单记录快照版本 v。续传本质是继续导出“同一个 v 的视图”。
若原快照已关闭，其保留值已释放，被改写键只能读到当前新值，
与前半段（旧值）拼接会得到跨时刻、总校验和必然矛盾的文件。
因此续传要求持有/校验存活快照句柄；快照关闭（过期）立即返回
ErrSnapshotExpired，且不删除已导出的块文件（用户可换快照重导）。
关闭与导出并发：快照用 RWMutex，导出持读锁，Close 等读锁释放，
保证不会有半截完整文件；进行中的导出要么做完，要么收到过期错误。

## 7. 资源与边界

导出逐块流式读写，内存峰值 ≤ 一块（块头+体缓冲）；每键只读一次（计数）。
空存储、单键、全键改写（保留=键数）、块大于总量（单块）、
块大小 0（ErrInvalidBlockSize）、空键、空值（值长0，与不存在区分）均合法/可判定。
