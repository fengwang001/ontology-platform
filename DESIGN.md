# 设计推导

## 1. 非法单元切分规则（从七样例推出）

规则（WHATWG 式）：解码出错的那个字节 b，若它是「第二字节且违反 E0/ED/F0/F4 的特殊区间」，
则非法单元只含此前已收的首字节（吞 1），b 归还给下一轮重新解码；其他续字节非法（含非续字节
41）时，非法单元 = 首字节起的已收前缀 + b（整条吞掉，b 不归还）。无序列时 80..BF 自成单元。
EOF 残留前缀整体是一个单元（截断/一个 FFFD）。验证：
- F0 90 80 41：F0 第二字节 90∈90..BF 合法；第三字节 80 非法（非特殊）→ 吞整条 F0 90 80
  一个 FFFD；41 因不是续字节而归还重处理 → FFFD 41。
- E0 80 80：E0 的第二字节 80∉A0..BF（特殊违例）→ 只吞 E0 一个，80 归还；80 无序列自成
  单元，第二个 80 同样 → 共 3 个。
- ED A0 80：A0∉80..9F（特殊违例）→ 吞 ED，A0 归还自成单元，80 自成单元 → 3 个。
- C0 AF：C0 非法首字节自成单元；AF 无序列自成单元 → 2 个。
- F4 90 80 80：90∉80..8F（特殊违例）→ 吞 F4；90、80、80 均无序列各成单元 → 4 个。
- E2 82+流结束：82∈80..BF 是合法前缀，第三字节缺失；EOF 不产生归还，整体一个单元 → 1 个。

第二字节合法区间表（80..BF 单独=无序列续字节；C0 C1 F5..FF=非法首字节）：

| 首字节 | 第二字节区间 | 第二字节违例吞 | 期望续字节 |
|---|---|---|---|
| C2..DF | 80..BF | 吞1、b归还（b非续字节时） | 1 |
| E0 | A0..BF | 吞1、b归还 | 2 |
| E1..EC | 80..BF | 吞1、b归还 | 2 |
| ED | 80..9F | 吞1、b归还 | 2 |
| EE..EF | 80..BF | 吞1、b归还 | 2 |
| F0 | 90..BF | 吞1、b归还 | 3 |
| F1..F3 | 80..BF | 吞1、b归还 | 3 |
| F4 | 80..8F | 吞1、b归还 | 3 |
| 80..BF | — | 1（各自成单元） | 0 |
| C0 C1 F5..FF | — | 1（非法首字节） | 0 |

统一细化：任何位置出错时，当前字节 b 是续字节 80..BF 才计入本单元；b 不是续字节（如 41、
新首字节）则归还重处理。区别只在 UnitLen：第二字节特殊违例（E0 80、ED A0、F0 80、F4 90 等）
时已收前缀只有首字节，UnitLen=1；第三/四字节处续字节非法时 UnitLen=已收前缀+1（如
F0 90 80 → 3）。这同时解释 F0 90 80 41：80 续字节非法、单元 F0 90 80 结束，41 归还。

## 2. 缓存上限与可立即判定点

UTF-8 最长 4 字节：等待下一字节时缓存 ≤3 字节。UTF-16 一个码元 2 字节、代理对 4 字节，缓存 ≤3。
任何续字节非法的瞬间前缀即非法，立即出 FFFD（无需等 EOF）：即上列第二字节区间违例，以及后续
续字节不在 80..BF。只有「到 EOF 仍在期望续字节」才是截断（可区分的 ErrTruncated）。

## 3. par 切点对齐

每个 worker 从切点 s 向前回看：UTF-8 最多回看 3 字节，找到第一个「不在 80..BF」的字节作为真正
起点（包含该序列首字节）；UTF-16 回看到前一个偶偏移（代理对最多回看 2）。worker 解析到越过自己
末边界为止，但只统计/输出「单元起始绝对偏移 ≥ s」的单元。跨切点的字符（及非法单元）恰好被一个
worker 完整产出。样例 C3|80 80：切点 worker 看到 C3 80 为一单元、第二个 80 属下一单元。回看总量
≤ 8×4 字节，检查次数 ≤ 输入字节数 + 8×常数。

## 4. 输出上限断点

Write 逐个完整单元追加输出；某单元会超 Limit 时，已完整输出的保留，当前单元一个字节都不输出
（UTF-16 不输出半个代理对），返回 ErrTooLarge{Offset=本单元在整个输入流中的起始偏移}。Write
返回的 n 只覆盖「本次调用中已彻底结束的单元」所占的本次输入字节；残留在切分缓存里的字节不算
已消费，由 stream 内部保留等下次 Write。调用方续传：新建实例从 ErrTooLarge.Offset 重喂，拼字节
与无限流逐字节相同。
