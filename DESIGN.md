# 快照读可见性判定器设计

## 核心推导：为什么「提交号 < 水位」不够

快照 S 在时刻 t0 创建，水位 H = 创建时已分配的最大提交号，活跃集 A = t0 时仍在进行的事务集合。
可见性的语义是「版本在 t0 之前已提交」。提交号 < H 只是「提交发生在某个 <= t0 的序列位置」的必要条件，
不是充分条件：提交号可能被预占，于是存在事务 T 满足 commitSeq(T) < H 但 T 在 t0 尚未提交。

反例构造：T1 先拿到（预占的）提交号 5，但迟迟不提交；此时创建快照 S（H = 10，A 含 T1）；随后 T1 真正提交。
朴素规则「commitSeq < H」判 T1 的版本可见，但 S 的读者在 t0 之后读到了一个 t0 时尚未提交的版本，矛盾。

正确规则（充分必要）：版本对 S 可见 当且仅当
  写入事务已提交 且 commitSeq < H 且 写入事务 ∉ A（左闭右开，等于 H 不可见）；
例外：写入事务是 S 自己的属主事务时，自己写的自己可见；已回滚事务永不可见。

## 复杂度

单次判定 = 1 次事务表查找 + 至多 1 次活跃集查找，常数时间，不遍历事务表。
`visible.Judge` 内嵌非导出计数器记录本次判定查找次数，每个 Judge 实例独立，并发互不串台。

## 错误与边界

- `txn.ErrUnknownTxn`：未知事务号；`snapshot.ErrReleased`：快照释放后使用；
  `visible.ErrCorruptChain`：版本链提交号非递增（损坏）。均可 `errors.Is` 判定。
- 活跃集上限 `snapshot.MaxActive`，超限构造返回 `snapshot.ErrTooManyActive`；
  快照内存项数 = 活跃集大小，与事务总数无关。
