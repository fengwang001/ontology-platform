# 设计文档：随机超平面 LSH 分桶检索器

## 1. 方案概览

固定维度 d 的 float64 向量，建 L 张独立哈希表，每表用 b 个随机超平面。
查询时取查询签名在 L 个桶中的并集作为候选，对候选做精确欧氏距离精排，返回 TopK。
所有随机数来自显式种子（`math/rand/v2` 的 `NewPCG(seed, seed)`），无系统熵。

## 2. 签名计算规则

对表 l、位 j，法向量 n[l][j] 的每个分量由确定性 PRNG 生成（标准正态）。

 sig(v)[j] = 1 当且仅当 dot(v, n) > 0
 sig(v)[j] = 0 当且仅当 dot(v, n) <= 0

**内积恰为 0 的约定**：取 0（落到负侧）。零向量与任意法向量内积恒为 0，
故零向量签名恒为全 0，始终落在每个表的 0 号桶，语义确定、可复现。
NaN/Inf 向量不参与签名，建索引时拒绝并计入跳过数。

## 3. 位数 b、表数 L 与候选数/召回的关系（推导）

设两向量夹角为 θ，超平面法向方向均匀随机，单个超平面将二者分到同侧的概率：

 p(θ) = 1 - θ/π

单表 b 位全部相同（落入同一桶）的概率：

 P_collision = p(θ)^b = (1 - θ/π)^b

L 张独立表中至少一表同桶（成为候选）的概率：

 P_cand = 1 - (1 - p^b)^L

对非近邻 θ 大，p 小，故被检索概率趋近 0——这是剪枝来源。

### 位数 b 的权衡（L 固定）

- b 增大：p^b 指数下降。近邻（θ 小、p 接近 1）仍有较高同桶概率，
  但非近邻几乎不可能 b 位全等。桶更细 → 每桶向量更少 → **候选数单调下降**。
- b 过大时，近邻同桶概率也被压低，单表召回下降。
- b 过小时（如 b=4），桶很粗，非近邻大量混入，候选数逼近全量 N。

### 表数 L 的权衡（b 固定）

- 任一向量被覆盖的概率随 L 单调上升（1-(1-q)^L 关于 L 单调），
  故 **TopK 召回率关于 L 单调不降（期望意义）**；实测中用共享前缀超平面
  （L 增大时只新增表，已有表不变）保证候选集严格嵌套，使实测召回逐点单调。
- 代价：L 增大时候选并集增大，哈希计算量也线性增长。

### 哈希计算次数

每个向量在每张表要算 b 个内积（b 位），故签名哈希计算次数恒为

 N_hash = N × L × b

不多不少，由计数器精确断言。

### 参数与目标

数据集 d=16、N=5000 的高斯向量。真近邻夹角约 0.3–0.6 rad。
取 b=8、L=8：p≈0.81–0.90，单表同桶概率约 0.19–0.43，
8 表覆盖概率约 0.82–0.98，top10 平均召回 ≥0.6；候选期望
约为 L·N/2^b ≈ 156，远小于 500 上限。b=12 时单表桶更细、
候选约 L·N/4096≈10，b=4 时接近全量，候选随 b 单调下降。

## 4. 各包职责

- `vec`：`Vec`、`Distance`（欧氏平方距离）、`Dot`、`Validate`（维度/NaN/±Inf）。
- `hyper`：按种子生成 `[L][b]d` 法向量族；`Signature`；同种子逐字节可复现。
- `bucket`：签名→向量 ID 列表的桶表；L 表并存；查询取 L 桶并集。
- `search`：建索引（原子整体可见）、候选生成 + 精排 TopK、暴力基准、
  距离精排计数与哈希计数；只读并发安全。
- `persist`：自描述二进制落盘/读回（魔数+头+法向量+向量+桶表+CRC32），
  截断分类、最大前缀恢复（剔除悬挂 ID 并计数）、退化法向量与维度错误。

## 5. 可复现性

种子决定全部法向量分量（PCG 确定性序列，生成顺序固定：表→位→维度），
桶为 map 不影响签名。同种子两次建索引，签名表逐字节相同；
不同种子法向量不同，签名以高概率不同，测试断言存在差异。

## 6. 持久化格式（大端，CRC32-IEEE 覆盖其前全部字节）

头 40B：magic(4)="LSH1" | version u16 | dim u32 | L u32 | b u32 |
Nvec u32 | skipped u32 | hlen u32（CRC 段长度，便于定位）
法向量段：L*b*d 个 float64。
向量段：每个向量 dim 个 float64（ID 即段内序号）。
桶段：每表 u32 桶数；每桶 u64 签名 + u32 条目数 + u32 ID 序列。
尾部 u32 CRC。

截断分类（按文件实际长度 n 与各段结束偏移）：
 n<=40 头部不完整；<=法向量段尾 超平面参数不完整；
 <=桶段尾 桶表不完整；CRC 段存在但校验失败 CRC 不匹配。
恢复：扫描桶段内完整的表/桶/条目，引用 ID >= 实际向量数者剔除计数，
其余原样恢复，保证无悬挂 ID。
