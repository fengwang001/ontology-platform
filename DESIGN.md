# DESIGN — 统一格式差分器的四处推导

## 1. hunk 头零计数时的行号（语义第 4 条）

三个样例：

- `x\n` → `y\nx\n`（C=0）：`@@ -0,0 +1 @@`。插入点在第 1 行之前，数字是 0。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）：`@@ -2,0 +3 @@`。插入点在第 3 行之前，数字是 2。
- `a\n` → 空（C=3）：`@@ -1 +0,0 @@`。新文件插入点在第 1 行之前，数字是 0。

推导：计数为 0 时该侧不占有任何行，头里的数字无法是「第一行的行号」（没有第一行）。
若把它解释为「将要插入的位置（1 基）」，样例 1 应写 `-1,0`、样例 2 应写 `-3,0`，与 GNU 输出矛盾。
若解释为「插入点之前最后一行的行号」（= 插入点前的行数），三个样例全部吻合：
插入点前分别有 0、2、0 行。故规则为：

> 计数 > 0：数字 = 该侧第一行的 1 基行号；计数 = 0：数字 = 插入点之前的行数（前一行的行号）。

`Apply` 的解释：0 基起始下标 `s = a-1`（b>0）或 `s = a`（b=0），即统一为「前面跳过 s 行，在 s 处删除 b 行、插入新侧各行」。

| b 值 | 头 `-a,b` 的写法 | Apply 的插入/删除点（0 基） |
|------|------------------|------------------------------|
| b=0  | a = 插入点前行数（可为 0） | 在下标 a 处插入，不删行 |
| b=1  | `-a`（省略 `,1`），a = 该行行号 | 从下标 a-1 起替换 1 行 |
| b≥2  | `-a,b`，a = 首行行号 | 从下标 a-1 起替换 b 行 |

## 2. hunk 合并阈值 = 2C（语义第 5 条）

每段改动向两侧各吸收 C 行上下文。两段改动间隔 g 行未改动行时：
前段的尾部上下文占 g 中的前 C 行，后段的头部上下文占后 C 行。

- g ≤ 2C：两块上下文重叠或恰好相接（g=2C 时正好无缝填满），合成一个 hunk 不丢行也不重复行，与 `diff -U C` 一致。
- g = 2C+1：中间恰有 1 行不属于任何一侧的上下文，若合并会多输出这 1 行（且 hunk 头计数与 GNU 不符），必须拆开。

反例：C=3 时若阈值取 C，g=4 的两段改动会被拆开，但 GNU 输出一个 hunk（4 ≤ 6 上下文相接）；
若取 2C+1，g=7 会被合并，但 GNU 输出两个 hunk（中间 1 行够不着）。故阈值恰为 2C。

## 3. 最短脚本的并列选择（语义第 3 条）

Myers 贪心每步在 k 线上取「向右（删除）」或「向下（插入）」中 x 更远者；
当 `V[k-1] == V[k+1]`（并列）时本实现固定取**向右，即优先删除**（条件写成 `V[k-1] < V[k+1]` 才向下）。
效果：同一 hunk 内删除行倾向于排在插入行之前。例：`a b` → `b a` 的最短脚本为
`-a`、` b`、`+a`（先删 a，b 保留，末尾插 a），而非 `+b`、` a`、`-b`。
该规则写死在比较式里，与输入无关，保证同一输入 100 次生成逐字节相同。

## 4. 偏移应用后后续 hunk 的查找起点（语义第 7 条）

后一个 hunk 的查找中心 = 头中记录的旧侧位置 + **累计行数差 Δ**（已成功 hunk 的 插入行数−删除行数 之和），
而**不**叠加前一个 hunk 的模糊偏移 k。理由：

- hunk 头的行号是在「b 文件」坐标系里写的；每应用一段净增 Δ 行，目标文件中后续内容的实际位置就整体平移 Δ，这是系统性漂移，必须补偿，否则漂移随 hunk 数线性累积、很快超出 ±F。
- k 是目标文件与预期之间的局部意外差异，不是系统漂移；把 k 也加进起点等于盲目信任「后面每一段都偏了 k 行」，会让查找中心跟着错误走。残余的 k 由 ±F 窗口吸收：|k| ≤ F 时仍能精确匹配，超过 F 判「超出偏移范围」正是题意要求的失败语义。
