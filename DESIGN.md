# 只读快照一致性导出器 — 设计推导

目标：数据持续变更时，导出某一时刻的一致快照；支持分块续传与完整性校验。仅用标准库。

## 1. 版本链与写时复制（COW）触发点

- `store` 中每个键维护一条版本链：写入不原地覆盖，而是把新版本追加到链头。
- 快照句柄记录创建时的版本水位 `v`；按版本读取沿版本链取第一个 `ver <= v` 的值。
- 推导：导出到键 k 的时刻可能晚于快照时刻。若原地覆盖，沿链读取将读到快照之后写入的
  新版本，破坏快照一致性。因此正确做法是——**任何写入都必须保留“最老活快照仍可能可见”
  的旧值**，导出读取永远走“快照版本可见值”而不是当前值。
- 实现采用惰性 GC：写入只追加；旧版本的释放推迟到最老快照推进/关闭后扫描脏键完成。
  故导出期间被改写的键，其快照旧值必然仍在链中，第 80 键即使在第 50 键导出时被改写，
  导出读到的仍是快照时刻的旧值。
- 快照结束时若读取到中途关闭，返回可判定错误 `ErrSnapshotClosed`，不产出被当成完整的文件。

## 2. 保留值释放条件：为什么按最老快照判断

版本 v_old 可释放，当且仅当不存在任何仍可见它的活快照。若按“写入时最新快照”或单个
快照判断，另一个更早（版本水位更低）的重叠快照可能仍需要该旧值，提前释放会让它读到
新版本——不一致。因此只有当**当前所有活快照中水位最低者（最老快照）**都不再需要
某版本时，它才可释放。两个重叠快照 s1(v=10)、s2(v=20)：s2 关闭后，旧版本对 s1 仍
可见；只有 s1 也关闭（活快照集合为空）后，保留值计数才归零。

## 3. 导出顺序无关性

清单中的**总校验和**对每条规范化记录（`len(key)||key||len(value)||value`）独立
计算 SHA-256，再将所有记录摘要按 32 字节字逐位 XOR 折叠。XOR 满足交换/结合律，
键集合与每键的快照版本可见值由版本水位唯一确定，故字典序、逆序或任意顺序导出，
块切分位置可能不同，但总校验和与“导出的内容集合”必然相同；且摘要可在唯一一次
遍历时增量更新，不需要第二次读取。

## 4. 文件格式

- 分块文件：重复若干块，块 = 8B 魔数 `ONTOBLK` + 8B 块号(binary.BigEndian uint64)
  + 8B 体长度 + 体 + 4B CRC32(IEEE，覆盖块号、体长度与体)。
- 体编码：重复 `uvarint(len key) || key || uvarint(len value) || value`，
  空值长度 0 与“键不存在”天然可区分（不存在即不出现该记录）。
- 块大小 > 0；块大小为 0 返回 `ErrBlockSize`。每块体不超过块大小，内存峰值 ≤ 一块。
- 清单（manifest，JSON）：快照版本、块大小、记录数、有序总校验和、块号→(偏移,长度,CRC)。
  清单在全部块写完后落盘；续传仅凭已有清单 + 校验已存在块，从第 k 块继续追加。

## 5. 续传与截断分类

- 续传起始块 k = 数据文件中第一个缺失/不完整块；逐块重算 CRC 定位，1..块数 任意 k 均可续。
- 截断点分类（在期望偏移上逐字节判定）：
  清单缺失 → `ErrManifestIncomplete`；块头不足 24B 或魔数错 → `ErrBlockHeader`；
  头后字节 < 体长度+4 → `ErrBlockBody`；体完整而 CRC 不符（含 CRC 尾被截断）→ `ErrCRC`。
- 块乱序/缺失：按块号顺序核对偏移与编号，明确报出缺失/错位块号（`ErrBlockOrder`），
  不止报总校验和。

## 6. 快照过期为何必须拒绝续传

续传必须用**同一版本水位**补完剩余块。快照关闭后其保留旧值已释放，若用当前数据续写，
前半块是时刻 t1 的数据、后半块是时刻 t2 的数据，拼出跨时刻文件，总校验和也失去意义。
故续传入口先校验快照仍开放（导出器持有快照的活跃标记），关闭则返回 `ErrSnapshotExpired`；
已导出的部分文件与清单原样保留，不删除，便于重新导出或排查。

## 7. 资源约束

- 保留旧值数 = 导出期间被改写（且被最老活快照持有）的键数，与总键数无关。
- 每个键在一次导出中恰好被读取一次（读取计数器精确断言）。
- 导出逐块构造，单块写盘后即丢弃，驻留内存 ≤ 一块大小（峰值计数器断言）。
- 并发：版本链追加与水位读取互斥；多导出各持独立快照；`-race` 干净。
