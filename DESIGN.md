# 设计推导

## 1. 行尾与行尾空白

`eol` 是三态扫描器：看到 `\r` 进入待定，下一字节为 `\n` 时合并为一个 CRLF，否则把待定 `\r` 作为独立行尾先吐出，再处理当前字节。因此 `\r\r\n` = 独立 `\r` + CRLF = 两个行尾。CRLF 的 `\r` 被删，单独 `\r` 用一个插入的 `\n` 表示（原区间长度 1，新区间长度 1）。

`ws` 只缓冲空格和制表符；收到行尾或 Close 时判定为行尾空白并删除，收到其他字节时判定为行中空白，原样输出。纯空白行因此变成空行。

## 2. 末尾换行策略（样例表）

| 输入 | Keep | EnsureOne | TrimEmptyThenOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

EnsureOne 对空输入选择 `""` 而不是 `"\n"`：若空输入产生 `"\n"`，再次规范化会得到 `""`，破坏幂等；选择 `""` 后，规范化输出只可能是 `""` 或以单个 `\n` 结尾，三种策略各自幂等。Keep 不动末尾；EnsureOne 对非空内容删到最多一个末尾换行；TrimEmptyThenOne 先删全部空行再补一个，空内容仍为空。

## 3. 被删字节的 ToOut

映射由不重叠区间 `[origLo,origHi) -> [outLo,outHi)` 组成，长度可分别为 0/1，连续恒等片段合并，故区间数只随删除/插入点增长。删除片段长度为 0：`ToOut(i)` 对落在删除区间内的任意 i 返回该点的 `outLo`。CRLF 的 `\r` 映射到新产生的 `\n` 所在位置；行尾空白映射到行尾 `\n` 的位置，即光标落在换行符之前（同一输出点），不会越到下一行。`ToOrig(o)` 用二分定位第一个 `outHi>o` 的区间；输出点反查到未删除字节，因此 `ToOut(ToOrig(o))==o`，两函数均单调不减。终点 `len` 同样合法。

## 4. 缓冲与上限

未判定空白必须缓冲。超过 `MaxPendingWS` 选择拒绝（`ErrPendingWSLimit`，带已消费的原文偏移），不强制判定：强制输出会在后续真出现行尾时产生错误删除，并破坏跨切分一致与幂等。拒绝后进入终态，已输出内容与映射保留，可解释为“该点之前的前缀”。输出超过 `MaxOutput` 立即 `ErrOutputLimit`。严格模式遇到 NUL 返回 `ErrNUL`（带原文偏移），已输出保留并终态；非严格模式 NUL 与非法 UTF-8 原样通过。

## 5. par 拼接

段首无法知道前段末尾状态，因此先把切点从危险区移开再并行：切点若在 `\r|\n` 之间，前移到 `\r` 前；若落在“后面不接行尾的空白串”中，前移到该空白串起点。移到安全点后，每段独立规范化，最后一段使用配置策略，其余段使用 Keep（策略只取决于全局文件末尾；Keep 段的输出拼接等于整体 Keep 前缀）。段映射先加段内原文基址，再加“前段输出长度”的输出平移；删除点数量不重叠，平移后仍单调。修正只需查看切点附近一次待定 `\r`（≤1 字节）和一个连续空白串，修正范围有上界 O(限制+1)。K=1 时退化为直接规范化。
