# 属性级权限过滤器设计

在空 module ontology（Go 1.26）内实现：谓词树（列比较 / AND / OR / NOT）+ 角色列可见性策略，单遍裁剪，进程内存，仅标准库。

**模型**
- 角色 -> 可见列集合；列域取各角色授予列的并集，未被授予即不可见。
- 谓词节点：常量 / 列比较（Eq、IsNull）/ 逻辑 NOT/AND/OR。路径为从根起的子节点下标，根为空串，如 "0.1"。
- 行：map[string]string；键不存在即 NULL（存在性与空值可区分），裁剪只复制可见键。

**不可见列参与谓词：三种做法**
- 甲（当假）：泄露。探针 `NOT (secret = 1)`：secret=1 的行被滤掉、其它行返回，按行差异即可反推 secret。
- 乙（当 NULL 三值逻辑）：仍泄露存在性；且 `secret IS NULL` 对不可见列恒为真，直接成为探针，隐藏行全部被选中。
- 丙（整条拒绝并点名列与路径）：不泄露。无论谓词多复杂、如何加 NOT/IS NULL，输出只有"拒绝"，无可观察行差异。
故采用丙：谓词引用任何对该角色不可见的列，返回可判定错误 filter.ErrInvisibleColumn（errors.Is 可分），给出列名与路径，且不执行行裁剪/求值。

**OR 短路例外（定义与理由）**
定义：先做常量折叠。常量使 OR 恒真 / AND 恒假 / NOT 恒值时，被消去分支运行时不会被读取，其中不可见列引用不拒绝；折叠后仍存活的分支含不可见列即拒绝。
理由：被折叠消去的分支不产生运行时数据依赖，放行等价于放行纯常量谓词，无行差异可观察；存活分支参与求值，必须拒绝。例：`OR(TRUE, secret=1)` 折叠为 TRUE 放行；`OR(FALSE, secret=1)` 折叠为 `secret=1`，拒绝并点名。

**算法**
- 谓词检查为一遍后序 DFS：每节点恰好访问一次（nodesVisited，测试断言等于节点总数）。子节点先折叠，父节点据此常量折叠；被消去分支的引用丢弃，存活分支引用向上并。
- 通过后按三值逻辑（true/false/unknown）求值过滤；NULL 比较为 unknown。
- 行裁剪只遍历可见列集合，代价 O(可见列数)，与总列数无关；计 copies。
- 报告：裁掉列（字典序）、被拒引用（列名+路径；同列多次出现列只列一次但路径全列、去重）、是否整拒；确定性文本序列化，打乱构造顺序结果逐字节相同。

**边界**
可见空集（任何引用引用即拒；空谓词放行得空投影行）、可见全集（无拒绝）、空谓词（不过滤）、常量谓词、同列多次出现（列一次、路径全列）。

**穷举**
三列 a/b/c，八种可见性掩码 × 四种形态（a=1、NOT(a=1)、AND(a=1,b=1,c=1)、OR(a=1,b=1,c=1)）= 32。形态一、二仅引用 a：a 可见即放行（各 4 放行 / 4 拒绝）；形态三、四引用三列：全可见才放行（各 1 / 7）。合计 10 放行、22 拒绝。

**三类错误**
policy.ErrUnknownRole、predicate.ErrMalformed、filter.ErrInvisibleColumn，均可用 errors.Is 区分。
