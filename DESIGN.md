# DESIGN — 带路径语义的通配匹配器

## 段模型

模式与路径都按 `/` 切分为段序列：`"a/b"` → `[a b]`，`"a/"` → `[a ""]`，`""` → `[""]`（一个空段），`"/"` → `["" ""]`。
不做任何规范化：不折叠 `//`、不处理 `.`/`..`，所以 `x` 与 `x/` 是不同的段序列（1 段 vs 2 段）。
`**` 只有单独成段时是「零个或多个整段」；与其他字符相邻（`a**b`、`**.go`）按普通 `*` 处理。

## 推导 1：`x/**` 与 `**/x` 的边界

唯一规则：**`**` 消费 k≥0 个整段，消费后其前后段直接相邻**。

- `x/**` = 段序列 `[x, **]`。路径 `x` = `[x]`：`**` 取 k=0 → 匹配。路径 `x/` = `[x, ""]`：`**` 取 k=1 消费空段 → 匹配。路径 `x/y`：k=1 → 匹配。
- `a/**/b` 匹配 `a/b`：`**` 取 k=0 后 `a` 与 `b` 相邻 —— 与 `x/**` 匹配 `x` 是同一条规则（k=0），无任何特例。
- `**/x` = `[**, x]`。路径 `x` = `[x]`：k=0 → 匹配。路径 `/x` = `["", x]`：k=1 消费空段 → 匹配。

## 推导 2：空串路径

`""` 是 1 个空段 `[""]`。空模式 = `[""]`，字面空段匹配空段，且只匹配 `""`。
`*` 匹配段内零个或多个码点 → 匹配空段 → `*` 匹配 `""`。
`**` 取 k=1 消费这个空段 → `**` 匹配 `""`。
`?` 需要恰好一个码点 → 不匹配 `""`。

## 推导 3：无回溯算法

段内：编译期把段拆成「星号分隔的固定块」A0 `*` A1 `*` … `*` An（Ai 内无 `*`，只有字面/`?`/类，每个原子恰好消费 1 码点）。匹配 = 在段文本上依次找 A1..An 的出现。运行时只记录**最后一个 `*` 的回退点**（文本位置单调前进）。
正确性：设 Ai 之前的部分已匹配。若 Ai 能在位置 j1 < j2 匹配，且尾部模式（以 `*` 开头）能匹配 text[j2+len(Ai):]，则尾部也能匹配 text[j1+len(Ai):]——因为前者是后者的后缀，而尾部以 `*` 开头，`X` 匹配某串的某后缀 ⇒ `*X` 匹配该串。故选最左的 j1 绝不劣于 j2；归纳到每一块，贪心最左匹配完备，单回退点足够。每码点最多被「当前块」重扫一次，段内 O(段模式长 × 段文本长)。
不能照搬到 `**`：该论证要求「失配后文本位置还能逐码点前进、且回退目标唯一」。跨段时失配可能发生在**块中间**（某段只匹配了一半），此时既要知道退到哪一级 `**`，又要知道该级 `**` 重试时段文本已消耗到哪——单个回退点无法同时记录这两个维度，级联回退会指数化。故跨段用**段级 NFA 状态集**：状态 = 模式位置（段下标, 段内原子下标），`**` 段带自环 ε 边，逐路径段计算可达集合并集，天然多项式 O(模式原子数 × 路径段数)，病态输入无回溯。

## 推导 4：规则集版本一致

规则集编译产物（规则切片 + 版本号）放入不可变结构体，用 `atomic.Pointer` 持有。查询只 `Load` 一次，本次结论与 `Explain` 的序号/原文/版本号全部来自这同一次 Load，不会半新半旧。替换 = 完整编译新规则集，**全部成功才** `Store`；任一规则编译失败或超上限则不触碰旧指针，直接返回错误，旧版本不受影响。

## 匹配结果表（行=模式，列=路径；✓=匹配）

| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x` | ✗ | ✓ | ✗ | ✓ | ✓ | ✗ | ✗ |
| `*` | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b` | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |
