# Quoted-Printable 设计说明

## 1. 行末空白的判定（语义 3 之一）

解码后，物理行末尾的未转义空格/制表符语义不可靠：严格解码器应拒绝它。
因此编码时对空白做一次前瞻：若某个空格/制表符之后遇到的第一个边界是
换行（`\n`，其前面可带 `\r`）或输入结束，它就是“行末空白”，必须输出
`=20`/`=09`；否则原样输出。

- `"a \n"`：空格后是换行 → `"a=20\r\n"`
- `"a b"`：空格后是 `b` → `"a b"`
- `"a "`（EOF）：空格后是结束 → `"a=20"`

## 2. `=XX` 不被软换行拆开（语义 3 之二）

物理行长上限 76，软换行的 `=` 也计入 76。输出一个 token 前先算其宽度
（原字节宽 1，转义宽 3）：若 `当前列 + 宽度 > 76`，先输出 `=\r\n` 再输出
token，列归零。已有 74 列时来 `=3D`：74+3=77>76，故先软换行再输出
`=3D`，绝不会出现半段 `=3`。

## 3. 软换行恰好插在空格之后

解码器遇到 `=\r\n` 会把这三个字符整体删除并把前后拼回。若软换行的 `=`
前是一个原样空格，该空格就成为某物理行的最后一个可见字符，即“行末未
转义空白”，严格解码器必须报错（普通解码器还可能直接剥掉它）。故结论：
**软换行前的空格按行末空白处理，必须写成 `=20`**（制表符同理 `=09`）。
实现：决定软换行时，把本行末尾连续的原样空白回退，改以 `=20`/`=09`
放到新行；这些字节不重新前瞻，不增加额外扫描复杂度。

## 4. 往返等式（语义 6）

设换行规范化 `N(x)`：每个前面不是 `\r` 的 `\n` 替换为 `\r\n`，已有
`\r\n` 保持，孤立 `\r` 原样保留。则

`Decode(Encode(x)) == N(x)`

即除 `\n` 规范化为 `\r\n` 外逐字节相等。

## 5. 幂等与最小转义（语义 7）

编码器只转义四类字节：`=`、33–126 之外的字节、孤立 `\r`、行末空白；
其余可打印字符与行中空白一律原样，软换行只在列宽溢出这一确定条件下
插入。因此对合法 y，再编码与 y 的差异只能来自 y 中“本可原样却写成
`=XX`”的多余转义。比较时先删除 y 与再编码结果中的全部软换行（软换行
不含任何数据，位置由 76 列算法唯一重算），剩下的差异点必须都是
`=XX`→原字符。

## 6. 语义与测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 转义与换行规范化 | （测试完成后补） |
| 2 行长 76 | （测试完成后补） |
| 3 行末空白 / 不拆 =XX / 软换行前空格 | （测试完成后补） |
| 4 严格解码五类错误 | （测试完成后补） |
| 5 跨切分点一致 | （测试完成后补） |
| 6 往返 | （测试完成后补） |
| 7 幂等 / 最小转义 | （测试完成后补） |
| 计数器 ≤2n | （测试完成后补） |
