# 设计推导

## 代理对的错误偏移（语义 2）

规则：高代理 `\uD800–\uDBFF` 本身不是错误，它表示「我需要紧随其后的一个
`\uDC00–\uDFFF`」。因此错误不归于高代理，而归于**应当成为低代理却不是的那个单元的起点**：

- `"\uD83D"`：后面是结尾引号，报错偏移 = 结尾引号的位置（7）。
- `"\uD83Dx"`：`x` 不是转义单元，偏移 = `x` 的位置（7）。
- `"\uD83DA"`：`A` 不是 `\uXXXX`，偏移 = `A` 的位置（7）。
- `"\uDE00"`、`"\uDE00\uD83D"`：低代理出现在没有待匹配高代理的位置，它自身就是
  非法单元，偏移 = 该转义序列反斜杠的位置（1）。
- 输入直接结束（高代理后无任何字节）时偏移 = 输入长度。

理由：高代理是合法的「挂起」状态，解码器无法在看到它时判定错误；真正违反语法的是
后继单元。把偏移指向后继单元，恰好标出需要修改/补全的位置；孤立低代理则没有前置
挂起状态可归咎，只能指向自身。

`"\uD83DA"` 不能解释成「U+FFFD + A」：严格解码器全有或全无，任何错误都返回 error、
不产出部分字符串，也从不静默替换；语法上高代理强制定位紧跟的低代理转义，`A` 使该
定位失败，故整体拒绝。

## 最小转义、单射与唯一性（语义 4）

RFC 8259 对字符串体内**必须**转义的只有：引号 `"`、反斜杠 `\`、控制字符
U+0000–U+001F。U+007F（DEL）不在该区间，它不是 JSON 语法字符，作为合法 UTF-8
原样输出对任何 JSON 解析器都无歧义，故不必转义；而 U+001F 属于强制区间必须转义。
`/` 无需转义（`\/` 仅是合法别名）；非 ASCII 一律输出原 UTF-8 字节；U+2028/U+2029
只在 JavaScript 源码层面敏感，JSON 文本不受影响。

单射证明：每个码点的输出唯一确定——`"`/`\` 与八个短转义字符各有固定转义形式，
其余 U+0000–U+001F 唯一编码为小写 `\u00XX`，其余合法码点唯一输出其 UTF-8 字节。
反方向上，原始字节永远不以 `\` 开头（`\` 只可能来自转义序列），转义序列首字节
`\` 之后的形式互不冲突且定界明确，因此任意输出字节流只有一种切分与还原方式，
编码可判定、无歧义，故 `Encode` 单射且输出唯一。

## 语义—测试对照表

<!-- 测试完成后补齐 -->

