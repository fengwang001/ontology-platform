# 三方合并冲突检测器：设计推导

## 模型

- 记录集合 doc.Set：map[键]记录，记录 = map[字段名]值，拍照式全量快照（非增量）。
- 值类型为 any（string/int/int64/float64/bool/nil），相等用 reflect.DeepEqual，类型不同必不相等。
- 记号：对键 K（或字段 F），祖先值 a、左值 l、右值 r；用 _|_ 表示不存在。

## 冲突判定表的推导

基本原则：冲突 = 两侧的意图无法同时满足。「一侧是否动过」不是判据，
「两侧意图是否互相排斥」才是判据。对三元组 (a, l, r) 逐情况枚举：

1. l == a：左未动，取 r。右未动对称。单侧改动永远无冲突。
2. l == r（含 l == r == _|_，即两侧同删）：两侧意图相同，
   应用一次即同时满足两侧，取 l，无冲突。
   「两侧改成相同值不是冲突」的理由：合并是变更的复合，相同变更
   复合一次与复合两次结果一致（幂等），不存在无法满足的一方。
   朴素规则「两侧都改过就冲突」把「动过」误当「分歧」，会把
   完全一致的意图误报为冲突，故判据必须是 l != r 而非「都动过」。
3. l != a 且 r != a 且 l != r：两侧给出不同意图，冲突。

记录级「删除 vs 修改」可归约到字段级：删除 = 把全部字段改为 _|_。
一侧删除、另一侧把字段改为非祖先值，展开后正是情况 3 的
(a, _|_, r')：复活记录违背删除方，删除记录丢失修改方，无法调和。
但它单列一类 DeleteModify，与 FieldValue 区分，因为解决方式
不同（决定记录存亡 vs 决定字段取值），报告语义也不同。

字段级粒度：合并以字段为最小单位。左改 a、右改 b 时，两个变更
作用于不相交字段，可同时生效，c 保持祖先值。只有同一字段
l != r 且都偏离 a 才冲突。

新增：键不在祖先中（a == _|_）而两侧都有，视为两侧各自新增，
仍按上表：内容相同（情况 2）不冲突，不同（情况 3）为 AddAdd
冲突。祖先缺失不是错误，只是 a == _|_ 的常态。

类型不一致：值类型不同则 l != r，落入情况 3，单列
TypeMismatch 类并在报告中携带两侧类型名。

## 「某侧为空」的语义定义

输入是全量快照而非增量，因此空集合 = 该侧删除了祖先中的全部
记录，不是「未做任何改动」。「未做改动」应传祖先的副本。
由此：左空 + 右未动，全部删除无冲突；左空 + 右修改，每条记录
一个 DeleteModify 冲突。

## 边界

- 三集合皆空：空结果、零冲突。祖先空、两侧有内容：按新增处理。
- 空串键、空串字段名均合法（map 不拒绝）。
- 字段值为空串与字段不存在可区分：map presence + DeepEqual，
  f="" 与 f 缺失不相等，走情况 3 判冲突。

## 复杂度

- 键并集：各 map 迭代一次收集，排序（确定性需要）O(k log k)。
- 每个键对三个 map 各查 1 次，查找总数 = 3*|并集| <= 4*|并集|。
  merge3 内置非导出计数器（LookupCount() 导出读数）。
- 冲突在唯一一遍扫描中收集完毕；conflict.Report 只访问冲突
  本身，访问计数 == 冲突数（ReportAccess() 读数），不二次遍历。

## 确定性

所有输出（合并结果、冲突清单、序列化字节）按键字典序、键内按
字段名字典序排列，仅依赖集合内容，与插入顺序、map 迭代序无关。

## 序列化格式

magic(4B "OM3\x01") | payloadLen(uint32 LE) | payload | crc32(payload, 4B LE)。
payload = 合并结果编码 + 冲突清单编码（均为长度前缀自描述）。
截断分类：小于头长为头部不完整；小于头长+payloadLen 为记录不完整；
再短于全长（CRC 被切）为 CRC 不匹配。三类错误可用 errors.Is 区分。
