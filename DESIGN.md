# 设计推导

基础规范化 K(x)：行尾 `\r\n`/`\r`/`\n` 统一为 `\n`；删除每行末连续空格与制表符（EOF 前的尾随空白同删）；NUL/非法 UTF-8 原样保留。三种末尾策略作用于 K：

- `Keep`：N=K。
- `EnsureOne`：`N(x) = trimr(K(x)) + "\n"`，其中 trimr 删掉全部末尾 `\n`；特别地 x 为空时 N="\n"。
- `TrimEmpty`：若 K(x) 末尾有 `\n`，则压成恰好一个；无则不添加；x 为空时 N=""。

样例表：

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 空输入推导

EnsureOne 对 "" 有两种选择：输出 "" 或 "\n"。若输出 ""，则 "" 是不动点，但 "" 又不含末尾换行，与“确保恰好一个”矛盾，语义退化成“空文件例外”。选择 "\n" 后：空集与非空集各自有不动点（"" 经一次映射进入非空集，此后 N("\n")="\n"），不变量“非空输出恰好以一个 \n 结尾”无例外，因此选 "\n"。TrimEmpty 的语义是“规范化已有的末尾换行，缺则不补”，故 ""→"" 是不动点。三种策略输出均无 `\r`、无尾随空白，逐行检查即得幂等。

## 被删字节的 ToOut

映射由分段区间给出：保留段 1:1，删除段（origLen>0,outLen=0），插入段（0,>0）。设输出侧换行在位置 o（保留的 `\n`）。`ToOrig(o)` 必须是原 `\n` 的偏移，故 `ToOrig` 在删除间隙取其后缘（删除段之后第一个保留位置）。由 `ToOut(ToOrig(o))=o` 与单调不减反推：删除段 `[a,b)` 内 `ToOut(i)` 只能取 a 之前最后一个输出位置，即被删间隙的**前缘**；`ToOut(b)` 取后缘 o。具体地：行尾空白 `[wsStart, nlOrig)` 与 `\r\n` 中的 `\r`，`ToOut(i)=nlOut`（该 `\n` 的输出偏移），即**换行符之前**的输出位置；而换行符自身 `ToOut(nlOrig)=nlOut`，换行符之后跳到 nlOut+1。光标放在被删空白/`\r` 上映到换行前，视觉上落在本行末；要落到下一行行首需显式用下一个有效位置。`ToOrig` 在删除间隙返回后缘原偏移（即 `\n`），保证 `ToOut(ToOrig(o))=o`。插入的 `\n`（仅 EnsureOne 且 K 无末尾换行）：`ToOrig` 取插入点原偏移=原长，`ToOut(原长)` 取插入后位置（后缘），保持右闭区间定义一致。

## 缓冲超限

强制判定为“非行尾”并吐出空白，在第 7 字节才触发换行时已经违规输出：若换行在限额外到达，已吐空白无法收回，输出含尾随空白，违反第 2、5 条（也产生错误的幂等性）。因此选择**拒绝**：挂起空白（及挂起的 `\r`）达到 `MaxPending` 时立即返回 `ErrPendingLimit`（带原偏移），已输出内容保留；此后 Close/Write 返回 `ErrClosed`，状态可解释（终态，Map/Output 反映已确认前缀）。输出超 `MaxOutput` 返回 `ErrOutputLimit`（带输出偏移）。NUL 严格模式返回 `ErrNUL`（带原偏移），非严格模式原样通过。三者与 `ErrClosed` 均为可 `errors.Is` 判定的哨兵，`OffsetError.Offset()` 给偏移。

## par 拼接

第 i 段单独用 Keep 规范化。段边界两侧最多需要回看：边界前的行尾空白串（可能含一个待配 `\n` 的 `\r`）与边界后的决定字节（下一个非空白字节或下一个换行）。故每个边界取窗口 `[i-回退, i+前进]`，回退=边界前连续 `[\t ]\r?` 的长度，前进=边界后连续 `[\t ]` 加至多一个 `\r\n?` 的长度；窗口只含“尚未判定”的字节，故有上界 MaxPending+3，重叠窗口合并。窗口区间与未覆盖间隔互不相交：间隔整体规范化（其内无跨边界待定量），窗口对原文做一次联合 Keep 规范化，替换覆盖到的各段结果，平移坐标（+原文基址 / +输出基址）后按序拼接。段内表一律先平移为段基址坐标；窗口内任何跨边界删除/CRLF 合并都在联合规范化的表中正确表达，消除“两段各判一次”的偏差。K 结果拼接后整体应用末尾策略（对 span 做与流式一致的尾部改写），得到与单路完全相同的输出与表。
