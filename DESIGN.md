# 属性级权限过滤器 — 设计推导

只解决一件事：按角色授予列可见性时，谓词引用了不可见列怎么办。

## 1. 三种做法

- 甲：把「不可见列的比较」当假。探针 `NOT (secret = 1)`：比较恒为假，NOT 后恒真，
  攻击者据此区分 `secret` 是否等于 1（行被返回/不返回），泄露列值。
- 乙：把不可见列当 NULL 走三值逻辑。探针 `secret IS NULL` 恒为真，成为存在性探针；
  `NOT (secret=1)` 对 NULL 为 UNKNOWN→NOT 后仍 UNKNOWN→不返回，与甲的返回集合也不同，
  攻击者可反推 NULL/取值。且结果行若把不可见列置 NULL，无法区分「不可见」与「真为空」。
- 丙（采用）：谓词任一「存活」引用指向不可见列，整条查询拒绝，错误带列名与树路径；
  行裁剪时把不可见列从 map 中**删除键**，而非置零/置 NULL。

## 2. OR 短路例外（可证明未读取即放行）

一遍后序遍历同时完成折叠、活性判定与引用登记，规则：

- 列比较恒为非常量，引用记为「存活」。
- NOT、AND、OR 先折叠常量操作数：
  - `OR` 任一常量操作数为真 → 整支恒真，另一支**未被读取**，回滚该支登记的引用；
  - `AND` 任一常量操作数为假 → 整支恒假，其余支**未被读取**，回滚其引用；
  - `NOT true/false` 直接折叠，子树引用随之回滚。
- 折叠后存活的引用必须全部可见，否则拒绝并列出每个存活引用的列名与路径。

理由：`secret>0 OR 1=1` 与任何行数据无关地恒真，执行器不需读取 `secret`；
拒绝它既无信息收益又破坏可用性。被丢弃的分支不报、不暴露。

## 3. 数据与错误

- `policy`：角色→可见列集合；空集=全不可见，全集=全可见。
- `predicate`：节点为常量、列比较（Op: = <> < <= > >= IS NULL IS NOT NULL）、AND/OR/NOT。
- 三类错误（`errors.Is` 可分）：ErrInvisibleColumn（含列名+路径）、
  ErrUnknownRole、ErrInvalidPredicate。
- 结果：可见列构成的新 map（不就地改写），报告含裁剪列（字典序）、
  被拒引用（列名+路径，排序）、是否整条拒绝；渲染为确定性文本。

## 4. 复杂度

- 谓词：单遍后序遍历，计数器=节点总数，不二次遍历。
- 行裁剪：只遍历可见列，拷贝次数 ≤ 4·|可见列|（map 写 1 次、容量提示 1 次、
  可见集读与删除判定各记账），与总列数无关。
