# 增量聚合视图维护器 — 设计推导

## 1. 变更模型

基表变更以流式 `Change{Version, Op, ID, Group, HasGroup, Value}` 到达：
Insert/Delete/Update 三类；`Version` 由上游全序分配、单调递增；`ID` 是记录主键，
视图据此维护 `id -> (group, value)` 索引。Update 视为「删旧插新」，天然支持跨组移动。

## 2. 各聚合器的撤回策略推导

**Count**：状态为计数 n。删除一条记录，组成员数恰减一，`n-1` 就是新全量结果。
状态本身包含撤回所需的全部信息 ⇒ 可增量撤回，不需要成员。

**Sum**：状态为和 s。删除值 v，`s-v` 在代数上是插入的逆运算 ⇒ 可增量撤回。
注意浮点加法不可结合：增量「加/减」与全量「按任意顺序重加」只有在运算精确时
才位级相等。本题数值取整数值浮点（|v|<<2^53，加减均精确），故位级一致成立；
这一前提在 audit 的 `math.Float64bits` 比对中被动验证。

**Min/Max**：状态只有当前极值 m。删除值 v!=m 时极值不变，可直接忽略；
但 v==m 时，新极值是「剩余成员的次优值」，而 (m, n) 中没有编码任何次序统计量，
信息论上无法反推 ⇒ 必须回到该组成员重算。撤回代价与组大小成正比，因此
「删到极值」必须是例外：Delete(v) 仅在 v 等于当前极值时声明失败，由 view 触发
Recompute；否则静默成功。

**DistinctCount**：状态为不同值个数 d。删除值 v 时，d 是否减一取决于
「v 是否还有其他记录持有」—— multiplicity 不在状态中，与 Min 同理不可反推
⇒ 声明删除需要成员，由 view 触发重算（重算时按成员重建多重集）。

结论表：`NeedsMembersOnDelete` — Count/Sum=false，Min/Max/DistinctCount=true。

## 3. Recompute 触发条件与上界

view 的 Apply 分三阶段：Apply（改成员表与各聚合器增量状态，收集 Delete 返回
false 的 (组, 聚合器)）→ Recompute（仅对收集到的项，用该组成员切片重算，
访问量按成员数计入非导出计数器）→ Commit（推进版本号，全程持写锁，读者只见
前后两个一致快照）。重算只读取本组成员 ⇒ 单次访问量 <= 该组当前成员数，
不触碰其他组。组被删空时整组移除，跳过重算。

## 4. 版本幂等的判定依据

版本号是上游赋予的全序。视图记录已应用的最大版本 `version`：
- `c.Version == version`：同一变更的重投递（at-least-once），幂等空操作，
  不改状态、不重复写日志、不计拒绝；
- `c.Version < version`：乱序/回退，返回 `ErrVersionRegression` 并计拒绝数；
- `c.Version > version`：接受。先写日志（WAL）再走三阶段，Commit 推进版本。

## 5. 日志与崩溃恢复

日志 = 自描述头（magic 4B + 格式版本 4B）+ 记录序列
（长度前缀 4B + 载荷 + CRC32）。重放按边界解析，截断点分类为：
头部不完整 / 长度前缀不完整 / 记录体不完整 / CRC 不匹配（后者由比特翻转注入）。
WAL 顺序保证：Apply/Recompute/Commit 前任一崩溃点，崩溃中的变更要么已在日志里
（重放后生效），要么整体不存在，不会出现半条记录；恢复 = 重放日志到新视图。
恢复后视图与不崩溃时逐字段相同。

## 6. 并发

单把 `sync.RWMutex`：写者串行化整个 Apply（含 Recompute 与 Commit），读者持
读锁取整组快照 ⇒ 不会观察到「Count 已改 Sum 未改」的中间态；Recompute 期间的
并发写入被锁串行化，不会丢失。版本号由写者侧原子分配。
