# 只读快照一致性导出器 — 设计推导

## 1. 数据模型
- `store`：键 → `(版本, 值)`，单调版本号，每次写入 +1。键为空串、值为空字节切片均合法；值存在用独立标志位区分，空值不等于键不存在。
- `snapshot`：句柄持有版本水位 `V`（取拍时刻当前版本）。快照读键时，返回“版本 ≤ V 中最新的那一条”。

## 2. 写时复制（COW）的触发点
导出进行中存在水位为 V 的活跃快照。写键 K 时，若直接覆盖当前项，快照随后读 K 只会看到新值，等于把新时刻数据污染进旧时刻快照。
因此写入前：
1. 取出 K 当前项 `e=(ver,val)`；
2. 若存在活跃快照且 `ver ≤ V_oldest`（当前最老活跃快照的水位），该项是某个活跃快照可见的旧值，必须把它追加进 K 的历史链，而不是丢弃；
3. 再把当前项更新为新版本。
快照读：先在历史链里找 `ver ≤ V` 的最大版本项，找不到再看当前项（当前项 `ver ≤ V` 才可见，否则键在 V 时不存在）。
导出到第 50 个键时第 80 个键被改写：第 80 键旧值在写入瞬间已入历史链，导出读到的仍是旧值。

## 3. 保留值何时释放（为什么按最老快照判断）
历史链里 `ver ≤ M`（M = 最老活跃快照水位）的项可能被 M 或任何水位 ≥ M 的快照看到，不能删。
设最老快照关闭，新最老水位为 M'（无活跃快照则 M'=∞）：凡 `ver < M'` 的历史项，已没有任何活跃快照需要它——需要它的快照水位只会 ≥ M'，而这些快照能看到更新的项；反之 `ver ≥ M'` 的项仍可能是 M' 可见的最新项，必须保留。
所以关闭快照时：对每个键，历史链只保留“M' 可见项之后”的部分，删掉所有比 M' 可见项更旧的项。两个重叠快照 M1<M2：关 M2 不动（最老仍是 M1）；只有 M1、M2 都关闭，M'=∞ 时整条历史链才清空，保留计数归零。
内存只与“导出期间被覆盖、且旧版本落在最老水位之后的键数”成正比，与总键数无关。

## 4. 导出顺序无关性
每条记录编码为定长前缀 `uvarint(len(key))|key|uvarint(len(value))|value`，另有 1 字节存在标志（0=不存在/墓碑，1=存在），使空值与缺失可区分。
清单总校验 = 所有记录载荷 CRC32 的异或（CRC 可交换、可结合），与遍历顺序无关；总长度 = Σ记录长度，亦与顺序无关。
分块按顺序切流，块 CRC 只用于定位块级损坏；清单总校验对字典序/逆序导出相同，故内容集合相同 ⇒ 总校验相同。

## 5. 文件布局、续传
```
[4B manifestLen LE][manifest JSON（manifestLen 字节）][chunk0][chunk1]...
```
manifest 一次预留足够槽位（按数据量上界估算，≥1MiB），单遍流式导出：manifest 先写 `Complete:false`、空 Chunks，随后逐块追加；每块头 16 字节 `[magic 2B][index 4B LE][bodyLen 4B LE][bodyCRC 4B LE]`，体内为连续记录帧。
每完成一块即把 Chunks 与 TotalCRC 重写回槽位（槽位固定不移动，后续块位置稳定）。
续传：解析清单 → 校验已存在块头/体/CRC → 从 `len(Chunks)` 块继续，严格重放同一键顺序。

## 6. 快照过期为何必须拒绝续传
续传的数据必须全部来自同一水位 V。快照关闭后旧值即被释放，若用当前 store 补后续块，前半是 V 时刻、后半是更晚时刻，拼出跨时刻文件，语义已错且无法事后察觉。
因此 Export/Resume 每次操作前检查 `snapshot.Closed()`，过期返回 `ErrSnapshotExpired`，且不触碰已写文件（已导出部分原样保留）。判定错误：`ErrIncompleteManifest / ErrIncompleteChunkHeader / ErrIncompleteChunkBody / ErrCRCMismatch / ErrChunkMissing / ErrChunkOutOfOrder / ErrSnapshotExpired / ErrChunkSizeZero`，均可用 `errors.Is` 区分。

## 7. 资源计数
- 保留值计数：历史链每入一条 +1，每释放一条 -1。
- 读计数：快照每次读键 +1（含键不存在），导出每键恰好读一次。
- 分块驻留峰值：导出器内只保留当前块缓冲，记录峰值长度，不超过一块大小。

## 8. 关闭与导出竞争
snapshot 内部 `WaitGroup`：导出开始 `Add(1)`、结束 `Done()`；`Close` 先置关闭标志再 `Wait`。进行中的导出要么读完，要么在下一次操作前看到关闭标志返回错误；`Complete` 只在全部块写完后置位，半截文件清单永远为 incomplete，不会被当成完整文件。
