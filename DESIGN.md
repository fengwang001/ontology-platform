# 带转义 RLE 编解码器：设计推导

## 严格解码的拒绝清单（由不变量 Encode(Decode(t)) == t 反推）

合法编码唯一：凡不可能由 Encode 产出的 t 都必须拒绝，否则 Decode(t) 再
Encode 回去会得到更短或不同的规范形。逐条推导（若接受，哪个 t 违反不变量）：

1. 显式次数 1：`1a` → Decode=`a`，Encode(`a`)=`a` ≠ `1a`。
2. 次数 0：`0a` → Decode 为空串，Encode 为空串 ≠ `0a`。
3. 前导零：`02a` → Decode=`aa`，Encode=`2a` ≠ `02a`。
4. 相邻同符号：`2a3a` → Decode=`aaaaa`，Encode 取最长游程得 `5a` ≠ `2a3a`。
5. `\` 后非数字非反斜杠：`\a` 不是合法符号写法；若当作字面 `a`，
   Encode(`a`)=`a` ≠ `\a`。
6. 末尾孤立 `\`：`a\` 的转义无对象；若丢弃，Encode(`a`)=`a` ≠ `a\`。
7. 末尾只有次数：`a12` 的次数无符号；若丢弃，Encode(`a`)=`a` ≠ `a12`。
8. 非法 UTF-8：Decode 须产出合法 UTF-8 字符串；非法字节会被规范化成
   U+FFFD，再 Encode 不可能等于原输入。

每类一个哨兵错误（`errors.Is` 可判定），包在 `*rle.Error` 里带字节偏移：
次数三类→次数首数字；相邻同符号→后一游程首字节；转义两类→`\` 本身；
缺符号→次数首数字；非法 UTF-8→非法序列首字节；超限→当前游程首字节。

## 大次数的取舍

选择支持任意大次数：次数用 `math/big.Int` 解析与比较，不会溢出
（`99999999999999999999a` 是合法输入）。解码按 8 KB 块流式写出，内存
O(1)、与次数无关，绝不在解码前按次数分配。输出总字节由
`Decoder.MaxOutputBytes` 限制（0 表示不限）；`Decode` 用
`DefaultMaxOutput`（64 MiB）。超限返回 `ErrOutputLimit`（可判定），
已写出部分不回滚。

## 语义与测试对照表

（待补：实现与测试完成后填写）
