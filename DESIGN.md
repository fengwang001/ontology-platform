# 统一格式差分器设计推导

## 1. 零计数时的行号（语义第 4 条）

样例（均经 GNU diffutils 3.10 实测复核）：

- `x\n` → `y\nx\n`（C=0）：头部为 `@@ -0,0 +1 @@`，插入发生在旧文件第 1 行之前。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）：头部为 `@@ -2,0 +3 @@`，插入在第 2 行之后。
- `a\n` → 空（C=3）：头部为 `@@ -1 +0,0 @@`，新文件侧为空。

推导：计数 ≥1 时数字是区间首行的 1 基行号；计数为 0 时区间为空，没有"首行"，
GNU 选择写**空区间之前最后一行的行号**（即"插入点前一行的行号"，文件开头则为 0），
而不是"将要插入的位置"（后者在三个样例中应分别为 1、3、1，与实测 0、2、0 矛盾）。
统一规则：设 0 基起始下标为 s（区间前有 s 行），则 count=0 时写 s，count≥1 时写 s+1。
`Apply` 解析 `-a,b` 时：b=0 → 0 基插入点 s=a（消耗 0 行，在第 a 行之后插入）；b≥1 → s=a-1。

| 情形 | 头部写法 | Apply 的解释 |
|---|---|---|
| b=0 | `-s,0`（s=前一行的行号，文件头为 0） | 插入点在第 s 行之后，消耗 0 行 |
| b=1 | `-a`（省略 `,1`） | 从第 a 行（0 基 a-1）起消耗 1 行 |
| b≥2 | `-a,b` | 从第 a 行（0 基 a-1）起消耗 b 行 |

## 2. hunk 合并阈值（语义第 5 条）

实测：C=3 时间隔 g=6 仍合并、g=7 分裂；C=1 时 g=2 合并、g=3 分裂；C=0 时 g=0 合并、g=1 分裂。
结论：阈值为 **2C**，即 g ≤ 2C 合并，g ≥ 2C+1 分裂。
原因：每个 hunk 在改动两侧各带 C 行上下文，两段改动间的 g 行若被两侧上下文
覆盖或恰好相接（g ≤ 2C），分成两个 hunk 会产生重叠或相邻的上下文，GNU 选择合并；
g = 2C+1 时中间恰有 1 行不属于任何一侧上下文，两个 hunk 才真正独立。
若取阈值 C：C=3、g=4 时 GNU 输出 1 个 hunk，按阈值 C 会输出 2 个，不一致；
若取阈值 2C+1：C=3、g=7 时 GNU 输出 2 个 hunk，按 2C+1 会合并成 1 个，不一致。

## 3. 并列最短脚本的选择（语义第 3 条）

Myers 前向搜索中，当 `V[k-1] >= V[k+1]`（删除不劣于插入）时优先向右走，即**优先删除**；
回溯用同一比较方向，结果确定。效果：同一连续改动块内所有 `-` 行排在 `+` 行之前。
实测 `a b` → `b a`：GNU 输出 `-a`、` b`、`+a`（先删开头的 a、末尾补 a），
与"优先删除"一致；若优先插入会得到 `+b`、` a`、`-b`，不符。

## 4. 偏移应用后的后续 hunk（语义第 7 条）

要平移。维护累积量 `shift`，初始 0；第 i 个 hunk 在 `pos_i` 处应用成功后
`shift = pos_i - OldStart_i + (newCount_i - oldCount_i)`，下一个 hunk 的查找中心为
`OldStart_{i+1} + shift`。理由：偏移 `pos_i - OldStart_i` 反映目标文本在该 hunk
之前与补丁旧文本相差的行数（如目标多出 k 行），该差异位于 hunk i 之前，
对之后的所有位置同样成立；而 hunk i 自身应用后净增 `newCount-oldCount` 行，
也会等量平移后续位置。这与 GNU patch 用上一 hunk 的偏移作为下一 hunk 起点一致。
反向应用（Reverse）时交换新旧两侧角色，按 NewStart 作同样累积。
