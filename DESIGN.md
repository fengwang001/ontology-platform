# 属性级权限过滤器设计

## 1. 安全目标

角色只能看到策略授予的列。谓词和返回行都不得让调用者观察到不可见列的值、是否存在，或借由布尔结果推断其统计特征。系统只做内存中的策略判定、谓词裁剪、行投影和审计报告，不实现存储、解析、网络或认证。

## 2. 三种处理方式

- 甲：把不可见列比较当作假。探针 `NOT (secret = 1)` 会在 `secret != 1` 时返回行，在 `secret = 1` 时不返回，调用者可反推不可见值。
- 乙：把不可见列当作 NULL 并使用三值逻辑。`NOT (secret = 1)` 的结果会随真实值在假/未知之间变化；`secret IS NULL` 更直接成为存在性和空值探针。把列“视为 NULL”也不能证明真实列不存在。
- 丙：拒绝整条查询，并报告不可见列名及谓词树路径。调用者得到的是固定的权限错误，不能从行集差异推断数据，因此本项目采用丙。

## 3. OR 短路例外

裁剪先对纯常量子树求值折叠，再做一次权限遍历。`OR` 的子支若折叠为常量真，则整个 OR 恒真；若折叠为常量假，则该支可删除。两种情况下该子树中的列在最终谓词里都不会被读取，因此不拒绝，也不报告这些列。

豁免只适用于“不依赖任何列即可证明为常量”的子树。像 `secret = secret`、`secret IS NULL` 或包含不可见列的非常量表达式不折叠；运行时短路也不豁免，因为数据库可能调整求值顺序，且计划本身仍引用敏感列。

## 4. 谓词与路径

谓词节点包括常量、列等于常量、列 `IS NULL`、`AND`、`OR`、`NOT`。路径从根开始：根为 `$`；子节点追加 `/and[0]`、`/or[1]`、`/not`、`/eq`、`/is-null`。同一列出现多次时，去重列名只报一次，但保留全部引用路径。

## 5. 裁剪算法

1. 递归折叠常量节点，记录原树节点总数；折叠后的逻辑树用于权限检查和后续执行。
2. 单次后序遍历检查列可见性；遇到不可见列时收集 `{列, 路径}`，并标记整条查询拒绝。
3. 对 OR 使用上述常量死支豁免；其他节点中的不可见引用一律拒绝。
4. 拒绝时不返回任何数据行，只返回错误和审计报告。
5. 放行时，每行按可见列集合构造新 map，只复制可见键值；不可见键物理缺失，而不是零值或 NULL。

## 6. 三类错误

- `ErrDeniedColumn`：谓词引用不可见列；错误详情包含列和路径。
- `ErrInvisibleColumn`：调用方显式请求选择或检查一个不可见列。
- `ErrInvalidPredicate`：谓词结构或操作数非法。

三者均可通过 `errors.Is` 区分。

## 7. 复杂度

谓词折叠与权限遍历各自至多访问每个节点一次；过滤器的非导出节点计数器断言权限遍历访问数等于输入谓词节点总数。行投影以策略中的可见列迭代，复制次数不超过可见列数；一千列、五列可见时上界为 `4 * 5 = 20` 次。

## 8. 边界定义

空可见集访问任何列都拒绝，但常量谓词和空谓词放行；全可见集不会因列权限拒绝；空谓词表示不过滤。三列模板穷举八种可见性组合与 `=`、`NOT`、`AND`、`OR` 四种形态：单引用形态各拒绝 4 次、放行 4 次；含同名列两次的 `NOT`/`AND`/`OR` 形态也各拒绝 4 次、放行 4 次。只有常量折叠证明的 OR 死支不计入拒绝。
