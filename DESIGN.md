# 设计与推导

## 1. 零计数时的 hunk 头行号

统一规则：`-a,b` 中 a 恒为「hunk 覆盖的第一个旧行（含上下文）的 1 基行号」，
b 为旧行数；当 b=0 时没有覆盖行，a 取「插入点前一个旧行」的行号；插入点位于文件
最前面时前一行不存在，取 0。验证三样例（C=0）：

- `x` → `y,x`：纯插入点在文件首，旧侧 0 行 → `-0,0`，新插入占新第 1 行 → `+1`。
- `a,b,c` → `a,b,X,c`：插入点在旧第 2 行之后，前一行行号 2 → `-2,0`，新第 3 行 → `+3`。
- 删除整文件：旧第 1 行被覆盖 b=1，新侧 0 行且插入点在文件首，前一新行行号 0 → `+0,0`。

故「前一行行号」而非「插入位置」：GNU 把 a 定义为区间起点（first old line），
空区间的起点退化为插入点所在的间隙左侧行号。Apply 时：b>0 用匹配到的旧区间；
b=0 且 a=0 插到文件首；b=0 且 a>0 插到旧第 a 行之后。

| 情形 | 书写 | a 的值 | Apply 的插入/替换点 |
| --- | --- | --- | --- |
| b=0 | `-a,0` | 前一旧行行号，无则 0 | 第 a 行之后（a=0 即文件首） |
| b=1 | `-a`（省 ,1） | 该行 1 基行号 | 替换旧第 a 行 |
| b≥2 | `-a,b` | 首行 1 基行号 | 替换旧第 a..a+b-1 行 |

## 2. hunk 合并阈值

每个改动两侧各带 C 行上下文。两段改动间隔 g 个未改动行：合并需要
g ≤ C+C = 2C；g = 2C+1 时相邻上下文之间还隔 1 行，必须分开。结论：阈值 2C。

反例（C=1，改动在第 1、4 行）：g=2 应合并（阈值 2C=2，GNU 输出一个 hunk）。
若取 C=1，则 g=2 会被错误拆开；若取 2C+1=3，则 g=3（改动在第 1、5 行）
会被错误合并，而 GNU `-U1` 输出两个 hunk。测试钉住 g=2 合并、g=3 分开。

## 3. 并列最短脚本的选择

Myers 每条 furthest-reaching D 路径上，水平边（删除）与竖直边（插入）都可达时
规定**优先删除**：先取向下（k-1，删除）候选再取向上（k+1，插入）。
于是 `a b → b a` 的脚本为 `-a`、` b`、`+a`（先删 a，公共 b 居中，再插 a），
而不是 `+b` 在前。规则固定 ⇒ 同一输入 100 次输出逐字节相同；hunk 内同一位置的
`-` 行整体排在对应 `+` 行之前（删除优先的自然结果）。

## 4. 偏移后后续 hunk 的查找起点

第 i 个 hunk 以偏移 o_i（实际匹配旧起点 − 头中旧起点）应用后，工作文本中该 hunk
之后所有内容的行坐标整体平移 o_i。设累计偏移 O_i=Σo（从同一快照串行判定），
第 i+1 个 hunk 的查找中心 = 头中旧起点 + O_i：前一个 hunk 多匹配到 k 行之外，
后续内容就整体多移动 k。搜索窗口仍为中心 ±F；多候选取 |距中心| 最小，并列取靠前。
所有 hunk 在同一原快照上先判定、后提交，任一失败整补丁零生效。
