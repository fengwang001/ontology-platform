# CSV 方言解析器设计推导

## 1. 空行与单列空值

选择：**空行被跳过**，不产生记录。推理：记录由换行结束，两个连续换行之间的零字节段若算记录，则“末尾换行”也会是一条记录，与第 4 条“末尾换行不产生额外空记录”冲突——要消掉末尾记录就得对“空记录”特殊处理，规则不自洽。故解析器对“记录仅含一个未加引号空字段”的行一律跳过（多列的 `a,,` 不会被跳）。

后果：单列表里一条值为空且未加引号的记录**无法用不加引号的方式回写**——写空串就是空行，会被解析器跳过，数据丢失。因此 writer 遇到该情形必须写 `""`（加引号的空字段），引号字段即使为空也构成真实字段，不会被跳过。

## 2. par 的切点问题

每个切点都是普通字节偏移（不找引号外安全点）。每段 worker 用两个候选起始假设各跑一遍：OUT（起点在引号外）与 IN（起点在引号内，可能处于“刚见引号”子态）。第 0 段真实态恒为 OUT。确定第 i 段真实态：看第 i-1 段末字节落在的词法状态——引号字段内（含 sawQuote）则 IN，否则 OUT。OUT 子态由前段末字节推导：`\r`→crPending，`"` 且其后无逗号/换行（前段末字节恰为 `"`，由该段在 OUT 下的末态判定）→closeQuoted。首字节是从段基偏移开始的字节；仅判定子态时回看的 1 字节不计入该段计数，因此每字节至多被两个 worker 各处理一次，总量 ≤ 2N（段尾状态字节不重读）。

拼接：段 i 按真实态选出事件流，事件偏移加上段基偏移即为全局偏移；记录号/字段号由 assembler 从全局事件流重放得出，所以错误的字节偏移直接来自全局坐标，记录号、字段号天然与单线程相同。先在选定流上出现的错误胜出，顺序按全局偏移确定。

## 3. CR 待定状态

未引号字段中 `\r` 不能立即判错（可能是 `\r\n` 行尾），进入 crPending：下一字符是 `\n` 则发 Nl 完成记录；是其他字符则报孤立 CR（位置为该 `\r`）。半包续传时 crPending 只是状态机的一个持久状态，`\r` 可单独在某次 Feed 末尾，下次 Feed 首字节裁决；流在 `\r` 处 Close 则报孤立 CR。par 中切点可落在 `\r`/`\n` 之间：该 `\r` 是前段末字节，下一段 OUT 起始子态按上表推导为 crPending，首字节 `\n` 正常结束记录；若 `\n` 不在下一段（即 `\r` 是全缓冲末字节）则 IN/OUT 两个假设流收尾均报孤立 CR。

## 4. 上限的“立刻”计数

引号字段内 `""` 表示一个引号字符，但这是**解码后**的值长度；上限针对**原始字节**计数（字段值就是原始字节，解码只发生在 writer，解析值保留 `""` 原样二字节，CRLF 原样保留）。因此每进入一个字段内容字节就 +1 并立即与上限比较，第一个超限字节到达即报错；不需要等闭合引号。字段数在每个逗号（字段数 +1）与记录结束处立即检查；记录数在每条完整记录提交时检查。终态后 Feed 返回包裹同一哨兵的错误。

## 5. 状态转移表

字节类别：C=逗号, Q=`"`, L=`\n`, R=`\r`, O=其他。事件：D=数据, Qo/Qc=引号开闭, Co=逗号, Nl=行尾, E=错误。

| 状态 | C | Q | L | R | O |
|---|---|---|---|---|---|
| fstart | Co→fstart | Qo→inQuoted | Nl→fstart | →crPending | D→inField |
| inField | Co→fstart | ErrQuote | Nl→fstart | →crPending | D→inField |
| inQuoted | D | D(Q→sawQuote) | D | D | D |
| sawQuote | Co→fstart | D→inQuoted | Nl→fstart | →crPending | ErrAfterQuote |
| crPending | ErrBareCR | ErrBareCR | Nl→fstart | ErrBareCR | ErrBareCR |

收尾：inQuoted/sawQuote→ErrUnclosed；crPending→ErrBareCR；其余正常结束。

## 6. 规范输入与往返

规范输入：记录间仅用 `\n`；除下列情形外字段不加引号——含逗号/`"`/`\r`/`\n` 的字段、以及单列表中为空的字段（写 `""`）；引号字段内嵌引号按 `""` 转义；无末尾多余空行。writer 额外保留原引号标记（已加引号的字段仍加引号输出），故任意解析成功的表回写后引号标记逐字段相同。
