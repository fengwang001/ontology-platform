# 设计推导

## 代理对错误偏移

`\uXXXX` 在看到四个十六进制数字前不是完整词法单元；因此数字缺失或非法时，偏移指向 `\u` 起点。
高代理（0xD800–0xDBFF）不是完整码点，它的继续符只能是紧随其后的低代理转义（0xDC00–0xDFFF）。
若高代理后直接遇到结尾引号、普通字节或另一个高代理，错误偏移仍指向该高代理的 `\`：
高代理声明了“半个码点”却无法完成，出错根因在它；后续字节本身无须被指责。
因此 `"\uD83Dx"`、`"\uD83DA"` 与孤立 `"\uD83D"` 的偏移相同（均为 1，跳过开引号）。
孤立低代理本身非法，因为没有待完成的高代理，偏移指向该低代理的 `\`；
所以 `"\uDE00\uD83D"` 拒绝在偏移 1，而不是后面的高代理，顺序颠倒不能被解析器原谅。
`"\uD83DA"` 不能解释成“替换字符加 A”：输入语法合法但码点孤立时，严格解码器必须报错；
U+FFFD 是宽容解码策略，会吞掉结构错误并破坏错误偏移与往返唯一性。

## 最小转义与唯一性

编码只转义 JSON 字符串语法强制禁止或需要消除歧义的字符：`"` 与 `\`，以及 U+0000–U+001F。
控制字符优先采用唯一短形式（`\b\f\n\r\t`），其余固定输出小写 `\u00XX`。
U+001F 必须转义，因为 RFC 8259 禁止未转义的 U+0000–U+001F 出现在字面量中；
U+007F 不在该范围，且没有引号或反斜杠的语法含义，所以原样输出仍是合法 JSON。
`/`、非 ASCII（含 U+2028/U+2029）与 U+007F 均原样以 UTF-8 输出。
每个输入码点被确定性映射到一个原字节串或一个转义串；反斜杠开头的输出只可能来自转义，
严格解码又能将每种转义还原成唯一码点，所以编码是单射，且同一输入的输出唯一。

## 语义测试对照

待实现完成后填写。
