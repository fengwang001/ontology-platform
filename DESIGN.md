# DESIGN

模型：规范化=一串区间。每段斜率 0（删除）或 1（原样复制）；末尾策略只做尾部
`\n` 的删除或追加。故 ToOrig/ToOut 只需在段表上二分，区间数仅随删除点增长。

## 1 末尾换行策略与样例表
三种策略：Keep=保留原样；One=非空时末尾恰好一个 `\n`，空输入输出 `""`；
Trim=非空时删掉全部末尾空行再补一个 `\n`（结果至多一个末尾 `\n`），空→`""`。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

空输入在 One/Trim 下取 `""` 而非 `"\n"`：若空→`"\n"`，对结果再规范化（非空
且末尾恰好一个换行）仍得 `"\n"`，也幂等；但它给“不存在的行”凭空造行，且
`""` 与 `"\n"` 被混同、映射表出现无原文对应的输出偏移。取 `""` 同样幂等
（N(`""`)=`""`）且无凭空字节，故选 `""`。Keep 对空显然是 `""`。

## 2 被删字节的 ToOut
设删除段原文 [a,b)（斜率 0，贴在其后的输出偏移为 p）。不变量要求
ToOut(ToOrig(o))=o：输出侧不存在 (p-1,p) 之间的点，故删除段上 ToOut 只能取端
点值。取 a 会违反单调不减（前一复制段末尾已映射到 p）。故：
ToOut(i)=p（i∈[a,b)）；ToOrig(p)=b。

- `\r\n`：a=CR 位置, b=a+2, p=输出 `\n` 偏移。光标放在被删 `\r` 上 → p，即
  换行符**之上**（其行的行末位置）。
- 行尾空白串 [a,b) 被删后紧跟 `\n`，p=该 `\n` 输出偏移 → 光标落在换行符上，
  即行尾换行符**之前的行末位置**（换行符左缘）。

## 3 缓冲超限（配置 MaxWS）
待定空白只可能延续到下一个行尾定案字节；超过 MaxWS 仍未见行尾则无界增长。方
案：**拒绝**，返回 ErrWSLimit（具名，带当前原文偏移）。强制判定为“非行尾”并
输出，若后续字节恰是 `\n`，已输出的空白本应被删却无法收回，直接违反语义
2/5/6，故不可取。拒绝时已输出内容与映射保留，进入终态（再写返回 ErrClosed），
状态可解释。输出超限同理：emit 前检查，越界即 ErrOutLimit 终态。MaxWS<=0 不限。

## 4 流式状态机（跨切分点一致的来源）
状态仅两个：crPending（上字节是 `\r`）与待定空白串。每个字节：
1) crPending 且本字节 `\n`：落删除段 [crPos,crPos+1)，复制 `\n`，清 cr。
2) crPending 且本字节非 `\n`：先把 `\r` 当孤立行尾落删除段+输出 `\n`，再处理本字节。
3) 本字节 `\r`：先冻结待定空白为行内空白，再置 crPending/crPos。
4) `\n`：丢弃待定空白（落删除段），复制 `\n`。
5) 空格/制表：加入待定缓冲（超限即 ErrWSLimit）。
6) 其他（含 NUL/非法 UTF-8；严格模式仅 NUL 终止）：冲洗待定空白后复制。
Close：crPending 按孤立 `\r` 落一个 `\n`；再按策略处理尾部：Keep 冲洗待定空
白；One/Trim 丢弃待定空白（最后一行尾空白总是删），One 在有输出且末尾非 `\n`
时追加 `\n`（斜率 0 的“插入段”，orig 宽 0，ToOrig 指向上一个原文末尾）；Trim
以最后一次“行冲洗点”为界截掉尾部多余 `\n`（同时截断 out 与段表）再保证一个。

截断不变量：Close 在任意字节后调用，效果=对已收到前缀整体规范化（孤立 `\r`
在 Close 升级为行尾不丢；待定空白按流结束判定）。

## 5 par 拼接
K 段在 [s_j,s_{j+1}) 上各自独立跑（内部用 Keep，不在中途应用尾策略）。每段产
出 (已定 out/map, 未定尾 U：至多 1 个 `\r` + 一串待定空白)。拼接时把 U 与下段
前缀一起重放进干净 normalizer，直到 U 全部定案（见到 `\n` 或非空白字节，或到
全局末尾），用重放结果替换下段开头对应产出。段 map 先按 orig/out 起点整体平
移，再合并相邻同斜率段。仅最后一段应用 One/Trim 尾策略。修正范围上界 =
1+待定空白长度（MaxWS 配置时上界 1+MaxWS；无界时 U 必被本段首个非空白/换行字
节终结，重放不超过一个未定区），故有上界且不随 K 变化。K=1..8、切点遍历全部
偏移因此逐位等价。

## 6 复杂度
段表相邻同斜率段合并（orig 宽 0 的插入段不合并）。纯 `\n` 文本无删除段也无插
入段 → 全表仅 1 段。查询 sort.Search 二分，探针计数 lastProbes，断言 ≤
2⌈log2(n+1)⌉+4。区间数只随删除点（+至多一个插入点）增长。
