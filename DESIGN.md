# RLE 编解码器设计

## 唯一合法形式与拒绝清单

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。规范编码规则：
相邻同码点必须合并为一个最长游程；次数 1 省略；次数 >=2 为无符号、无前导零
十进制；符号仅当为 ASCII 数字或 `\` 时用 `\` 转义。由此逐条推导：

- 显式次数 1（`1a`）：解码得 `a`，重编码为 `a != "1a"`，拒绝，错误 CountOne。
- 次数 0（`0a`、`00a`）：游程不存在，无法作为任何串的编码；拒绝，CountZero。
- 前导零（`03a`）：重编码规范化为 `3a`，与输入不同；拒绝，LeadingZero。
- 相邻游程符号相同（`2a3a`）：重编码合并为 `5a`；拒绝，AdjacentSame。
  该判定必须延迟到下一游程符号出现；`2a` 本身合法。
- `\` 后非数字非反斜杠（如 `\a`、`\n`）：不是合法符号转义，且不能原样解释；
  拒绝，BadEscape。多字节码点跟在 `\` 后同样不允许。
- 末尾孤立 `\`：缺少被转义符号，游程不完整，拒绝，TrailingBackslash。
- 末尾只有次数没有符号（`12`）：游程缺符号，拒绝，TrailingCount。
- 非法 UTF-8（含残缺多字节序列）：符号必须是 Unicode 码点，拒绝，InvalidUTF8。
- 解码输出超过上限：拒绝，OutputLimitExceeded（可判定、不按次数预分配）。

所有错误以具名错误类型携带输入字节偏移（从 0 开始），同类不同处偏移可能相同：
前导零/次数错锚在首位数字；BadEscape 与 TrailingBackslash 锚在反斜杠；
TrailingCount 锚在首位数字；InvalidUTF8 锚在坏字节。

## 大次数取舍

选择支持超过 `int64` 的次数：次数按任意精度十进制读取（runs 层用 big.Int），
从而不发生任何整数溢出。写入采用「小块重复、逐块检查输出总量」，绝不预分配
与次数成正比的内存。输出字节总量可配置（`LimitWriter`，`Decode` 给保守默认
上限），超限返回 OutputLimitExceeded。次数为规范十进制文本，其位数本身受输入
长度限制，因此解析器的内存只与输入有关。

## 语义与测试对照

| 语义（第三节） | 测试函数 |
| --- | --- |
| 1 往返（含数字/反斜杠/多字节/长游程/随机） | （收尾时补） |
| 2 规范形式与严格解码拒绝清单 | （收尾时补） |
| 3 大次数与输出上限 | （收尾时补） |
| 4 最长游程、组合字符不合并 | （收尾时补） |
| 5 全切分点流式一致 | （收尾时补） |
| 四、输入字节计数 | （收尾时补） |
