# DESIGN：并行任务图调度与失败传播

## 1. 模型
- `graph.Graph`：节点集合 + 有向边 A→B（A 是 B 的依赖，A 先于 B）。重复边幂等。
- 调度前校验：未知依赖节点 → 可判定错误；环（含自环）→ DFS 三色法报真实闭合路径。
- 拓扑分层仅用于校验/展示；执行本身用 Kahn 就绪算法。

## 2. 四类最终状态的判定推导
以任务生命周期划界，失败发生时刻为 t：

1. **成功 Succeeded**：t 之前已完成且无错。终态不可变，绝不回滚/改标。
2. **失败 Failed**：确实开始执行并返回 error（panic 被 recover 转成 `PanicError`）。
   记录原始错误；多个任务失败时全部记录。
3. **跳过 Skipped**：**从未开始**，因为其某个祖先失败。跳过原因用「最早失败上游」：
   沿依赖链找到的第一个 Failed 祖先（传播时首个被记录的失败源）。
   故 A→B→C 中 A 失败，C 的跳过原因指向 A 而非 B。
4. **取消 Canceled**：t 时刻**已经开始执行**（在途 goroutine），因 ctx 被撤而中止。
   与跳过的唯一区分依据是「是否调用过任务函数」：取消发生在开始之后、完成之前；
   跳过的任务函数从未被调用。取消后若任务仍写回结果，一律丢弃，状态保持 Canceled。

## 3. 两种模式
- **FastFail（默认）**：首个失败即取消在途任务（Canceled），所有不可达的
  未开始任务标 Skipped（原因指向最早失败上游）；已成功的保持成功。
- **BestEffort**：不取消在途任务、不主动跳过；只要某任务的依赖全部成功就继续跑，
  依赖含失败/跳过的任务才标 Skipped。失败集可能 >1，各自传播各自的下游。

## 4. 调度与复杂度
- 就绪队列：同时就绪多个时按 ID 升序弹出（堆）。每个节点恰好入队一次。
- 并发上限 maxRunning（<1 视为 1）：令牌门控，非导出 peakRunning 记历史峰值。
- 就绪判定次数 decisions：仅在「初始入队」与「某任务结束结算其出边」时计数，
  总计 = V + E 量级，保证 ≤ 4(V+E)；不存在全图扫描，O(V+E) 时间。
- 每个任务结束（成功/失败/取消/跳过）都幂等结算出边，活跃计数归零时收尾，
  保证失败与取消路径也无 goroutine 残留。

## 5. 确定性
- 报告按任务 ID 字典序输出；同图同函数，边的添加顺序打乱与随机完成延迟
  均不影响报告内容（共享状态只在锁内变更，终态由依赖关系决定）。

## 6. 边界
空图（报告为空）、单任务、全独立、1000 层链（迭代不递归）、重复边、
未知依赖、max=1 串行（报告与并行逐字节一致）。

## 7. 错误区分
环：`ErrCycle`（携带路径）；未知依赖：`ErrUnknownDependency`；
缺任务函数：`ErrMissingTask`；panic：`*PanicError`（As 识别，带原始信息）；
状态用 `State` 枚举（Succeeded/Failed/Skipped/Canceled）区分。
