# 只读快照一致性导出器 — 设计推导

## 1. 模型
- `store`：单调递增版本号的 KV，`Put(k,v)` 产生新版本；无删除。每个键只存当前值与当前版本。
- `snapshot`：`Begin` 记录版本水位 `v` 与该时刻键集合 `keys(v)`（Begin 时在 store 锁内拷贝）。
- 导出 = 按 `keys(v)` 把每键在版本 `v` 可见的值序列化、分块写盘，并出清单。

## 2. 写时复制（COW）触发点
覆盖会让导出到该键时读到新版本。正确做法：**写入生效前，在同一把 store 锁内，对每个活跃且版本 `v < newVersion` 的快照，把该键当前旧值交给它保留一次**（每快照每键至多一次）。读取顺序为：先查快照保留表，未命中再查 store 当前值。
为何「写前」而非「写后」：写后旧值已不可得。触发点必须与版本推进原子，否则写入与导出之间存在窗口。
导出到第 50 键时第 80 键被改写：第 80 键旧值已在 Put 时进保留表，稍后导出读到旧值；第 1..49 键若也被改写同理，与导出进度无关。

## 3. 保留值释放：为什么按最老快照判断
旧值对快照 `s` 有用 ⇔ `s.v >= old.ver` 且 `s.v < new.ver`。多个重叠快照可能共用同一份旧值；释放条件不是「某个快照关闭」，而是「所有版本落在 `[old.ver,new.ver)` 的快照都关闭」。实现为 store 持有旧值的**引用计数**：Put 时给每个相关快照计一次引用，快照 Close 时按其保留项逐个减一，归零即释放。最老快照版本 `min v` 是上界：它未关闭，凡是它需要的旧值必然不能释放；比它更老的区间无快照，相关旧值早已归零。故「两个重叠快照都关闭后才释放」。
内存 ∝ 导出期间被改写且命中快照区间的键数（每快照一份），与总键数无关。

## 4. 导出顺序无关性
分块是字节布局，顺序会影响块边界；但清单里的**总校验和**定义为按键字典序对每条目 `FNV1a64(len,key,value)` 求可交换的模 2^64 和，与遍历顺序无关。测试用字典序/逆序两种导出顺序，断言总校验和相等、条目集合相同。

## 5. 文件布局
- `manifest`：固定头（magic、snapVersion、entryCount、chunkSize、chunkCount、totalSum、done 标志）+ 每块 CRC32 记录。先以 done=0 原子落盘，全部数据块写完后重写为 done=1。
- 每块：16 字节头（magic "CHNK"、index、bodyLen、headerCRC）+ body。条目帧：`[klen 4B][vlen 4B][key][value]`，帧可跨块。
- 单块内存：chunker 缓冲恰为 `chunkSize`，按字节流式切片，单条超大条目也不整体复制（峰值=chunkSize）。

## 6. 续传与快照过期
数据文件只追加；续传时按清单把已有数据文件截到「完整块边界」，从块 k 重放条目流（跳过已写字节）继续。
拒绝过期续传的论证：续传读出的块 k.. 必须仍是版本 v 的值；快照关闭后保留值释放，旧版本永久不可读，若用当前数据补块会拼出跨时刻文件且总校验和可能恰好不可判别语义。因此导出器持有快照引用，续传时 `snap.Closed()` 为真即返回 `ErrSnapshotExpired`，**不截断、不删除**已导出部分（只有成功续上才截到边界）。
Close 与导出竞争：Close 设置 closed 标志并等待 in-flight 计数器归零（或导出检测到标志返回 `ErrSnapshotExpired`）；done=0 的文件 verify 判「清单不完整」，半截文件不可能被当成完整。

## 7. 四类错误
`ErrManifestIncomplete` / `ErrChunkHeaderIncomplete` / `ErrChunkBodyIncomplete` / `ErrCRC`，外加缺块/乱序 `ErrChunkMissing`、`ErrChunkOrder`、过期 `ErrSnapshotExpired`、块大小 0 `ErrInvalidChunkSize`，均为哨兵错误，支持 `errors.Is`（可包裹路径/块号）。
逐字节截断（1..len-1）：落在清单头/记录区 → 清单不完整；数据区中块头不足 16B 或头 CRC 坏 → 块头不完整；头完整但 body 不足 bodyLen → 块体不完整；恰好在块边界结束而后续块缺失由缺块/校验路径检出；CRC 不匹配由翻转/篡改字节产生（截断不会伪造出合法长度的坏块）。
