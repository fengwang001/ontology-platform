# 带背压的流式聚合管线 — 设计推导

## 1. 链路与角色
- `source`：可注入数据源（条数、速率、提前报错/结束），输出字节记录。
- `parse`：`key,value` 记录；无逗号或 value 非整数记为坏记录，计数但不进组；空 key 合法。
- `stage`：泛型有界队列骨架。`Send` 在队列满时阻塞 → 阻塞沿调用栈反压到 source。
- `sink`：先写 `output.tmp` 再原子改名 `output`；尾行放全内容 FNV-1a 校验和。
- `ckpt`：序列化快照（offset、各组部分和、坏记录数），保留最近两个：`ckpt.0/ckpt.1`。
- 根包：装配 source→parse→aggregate→sink、屏障提交、崩溃恢复。

## 2. 核心推导：「已消费位置」记在哪一条

管线内任一时刻记录可能处于四类位置：① source 刚读出 ② 某个有界队列里
③ 正被某阶段处理 ④ 已在 `output` 中。

### 2.1 为什么 source 读取位置不对
source offset = N 只保证前 N 条已离开 source；②③ 中的记录尚未落盘。若此刻提交
offset=N 后崩溃，恢复从 N 续读，在途记录永久丢失（少记录）。

### 2.2 为什么「sink 落盘位置」也不对
sink 落盘是快照式输出（每个检查点重写全量聚合结果），落盘与 offset、分组状态
来自不同时刻。若只按 sink 落盘位置推进 offset，而队列里还压着记录：
- 恢复后从旧 offset 重放，这些记录被第二次聚合 → 多/重复；
- 或用了较新的 offset 配较旧的聚合状态 → 丢更新。
offset、聚合 map、输出文件三者必须属于**同一个一致时刻**，不能各取各的水位。

### 2.3 正确定义：全局屏障（barrier）的静止点
周期性注入屏障标记（不占序号）。屏障流经每一级：
1. source 在 offset=B 处发 barrier；
2. parse 排空队列后原样转发；
3. 聚合器排空队列，固化聚合状态 `(B, groups)`，发提交事件；
4. sink 排空队列：写 tmp（聚合快照+checksum）→ rename → 写 ckpt(B,groups)。

当屏障到达 sink 时，**所有在途集合（队列 + 处理槽）对该屏障之前的记录均为空**：
每条 offset<B 的记录要么已在输出+检查点里，要么 offset 还没推进。

**提交语义（同时生效）**：ckpt{offset:B, groups:G} 与含 G 的 output 在一次
屏障提交内共同完成（先 rename 输出，再落检查点）。

### 2.4 崩溃恢复的不丢不重
- 崩在 ckpt 落盘前：旧 ckpt(B') 有效，tmp 被清理，从 B' 重放；B' 之后记录
  尚未提交，重放不算重复（输出里也没有它们）。
- 崩在 ckpt 落盘后：新 ckpt(B) 有效，输出已是 G；从 B 续读。
- offset 与聚合状态同源同刻，重放集合恰好等于缺失集合。聚合为 sum（交换律
  结合律）、输出按 key 排序，故恢复后输出与不崩溃**逐字节相同**。
- ckpt 损坏（checksum 不符）：丢弃该文件，读另一个完好文件，报告回退。

## 3. 资源约束
- 背压度量：各级有界队列共用「总在途计数」（仅计已入队未出队）。任一时刻
  总数 ≤ Σ 各队列容量（每条记录只占一个队列）。stage 记录历史最大值；source
  每次因下游满而阻塞时阻塞计数 +1。
- 分组硬上限：聚合器见到新 key 使组数超过 MaxGroups 即返回哨兵错误并停止，
  绝不静默丢组。
- sink 节流：最多按 1000 条/秒提交（每条 ≤1ms 的批次节流），保证测试可控且
  背压真实发生。

## 4. 优雅停止
每阶段一个 goroutine，归并 ctx 取消与 Stop：先关闭入口、排空或终止、关闭出口，
Wait 等待全部退出。Stop 经 sync.Once 幂等。测试取启动前 goroutine 基线，停止后
重数断言回到基线。崩溃注入 = 取消 ctx 模拟进程中止：入口全断、goroutine 随
Wait 回收，磁盘只留下一个完好检查点与可能的 tmp 残文件。

## 5. 崩溃注入三时刻
- A：source 读完（发完数据）但队列非空 → ctx 取消；
- B：聚合中途（处理到指定 offset，且 map 非空）→ ctx 取消；
- C：sink 已写完 tmp、rename 之前 → ctx 取消（留下 tmp 残文件）。
恢复后继续或重放，与无崩溃运行逐字节比对。
