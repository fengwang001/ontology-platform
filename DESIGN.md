# RLE 设计

严格解码器只接受自身编码的规范形式。编码总是使用最长游程，且次数 1 省略，因此接受任一非规范输入都会使 `Encode(Decode(t)) != t`。

## 必须拒绝的输入

- 显式次数 1：`1a` 解码为 `a`，重新编码为 `a`，不是 `1a`。
- 次数 0：`0a` 不表示任何码点；若解为空串，重新编码为空串。
- 前导零：`01a` 解码为 `a`，重新编码为 `a`，数字写法也不是最短十进制。
- 相邻同符号游程：`2a3a` 解码为五个 `a`，最长游程编码为 `5a`。
- 非法转义：`\x` 若解为 `x`，重新编码为 `x`，反斜杠消失；数字和反斜杠之外也无需转义。
- 末尾孤立反斜杠：`a\` 没有完整符号，不能由编码器产生。
- 次数后缺符号：`12` 只有次数没有游程符号，不能由编码器产生。
- 非法 UTF-8：替换字符会把无效字节变成有效码点，重新编码不能复原原字节。

所有语法错误都返回包装哨兵错误的 `DecodeError`，其 `Offset` 是从 0 开始的字节偏移；同类错误可用 `errors.Is` 判断。

## 大次数

选择支持任意精度次数：`runs` 用 `math/big.Int` 逐位累加，避免固定宽度整数溢出。解码器不会先按次数展开；一个游程只在与前一符号不同或关闭时输出，输出前用次数乘以该码点的 UTF-8 字节宽度检查剩余配额。输出总字节上限可配置，超限返回 `ErrOutputLimit`，且不分配与次数同比例的内存。

## 语义与测试对照

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 往返 | 待补 |
| 规范形式与严格解码 | 待补 |
| 大次数与输出上限 | 待补 |
| 最长游程且不规范化 | 待补 |
| 跨切分点一致 | 待补 |
