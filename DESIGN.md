# 设计推导

规范化 = 行尾统一为 `\n` + 删除每行末尾的空格/制表符，再按末尾策略收尾。

## 1. 空输入与只含换行的输入（末尾策略）

规范化主体只产生两类字节：内容行字节、行尾 `\n`。因此输出必形如
`内容\n ... 内容`，末尾 `\n` 个数即「空行数」。三策略只决定最终尾部：

- `Keep`：不改尾部。
- `EnsureOne`：无尾换行则追加一个；有则只保留最后一个。
- `TrimEmpty`：与 EnsureOne 相同的保留/追加规则，再加「只含空行时清空」。

空输入取 `EnsureOne("") = "\n"`。另一选择 `""` 也幂等（N("")==""），
但它让「确保」策略对空文件不产出换行，与非空无尾行文件待遇不一致；
取 `"\n"` 后三种策略对空输入各不相同且各自幂等。
幂等性：规范化后的文本无 `\r`、无行尾空白，三策略作用于自身尾部均是不动点。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

映射表由三类区间组成，全程保持原/输出坐标连续不重不漏：
`Keep n`（orig→out 等长）、`Del n`（orig 走 n、out 不动）、`Ins`（orig 不动、out 走 1，
仅末尾追加换行时出现）。对区间边界点采用「落在区间起点」约定：

- `ToOut(i)`：找到 `oStart ≤ i` 的最后一个区间（二分）。`Keep` 段内线性换算；
  `Del` 段（含其起点）恒为该删除点输出坐标，即**之后**首个 `\n` 之前；
  `Ins` 段起点取该点（追加场景）。
- `ToOrig(o)`：找到 `oStart ≤ o` 的最后区间。`Keep` 段内线性换算；
  `Del` 段恒为删除点原文坐标。

于是被删行尾空白的光标映射到行尾换行符**之前**（仍在本行末，编辑器语义自然）；
`\r\n` 中的 `\r` 映射到输出 `\n` 的位置。`Keep` 紧随 `Del` 之后时，
两者共享的边界点取 `Keep` 一侧（更早的输出坐标），故 `ToOut(ToOrig(o))==o` 成立；
两函数逐段单调不减。

## 3. 空白缓冲超限

选择**拒绝**：待定空白超限时返回哨兵错误 `ErrWhitespaceLimit`（带原文偏移），
不输出任何字节（该串空白本就一个字节未输出），实例进入终态。
强制输出虽不违反 4/6，但会把最终证明是行尾空白的字节永久写进输出，
违反第 2、5 条；拒绝则与第 7 条 NUL 的处理同构（可判定、带偏移、已输出保留、终态），
第 4 条在任何未拒绝的喂入方式下仍逐字节一致。

## 4. par 拼接

每段独立跑 `norm`，边界只有两种跨段不确定：

1. 前段末尾是待定 `\r`、后段首字节是 `\n`：段内 `\r` 记成了 `Keep`+`\n` 输出，
   拼接时把该 `\r` 的 Keep 改记为 Del（输出不动，`\n` 仍由后段提供）。
2. 前段末尾是未判定空白且后段首字节是内容：段内该空白被当成行尾删除，
   拼接时把对应 `Del` 改回 `Keep`（输出补回这些空白）。

这两种修正只需检查段边界处常数个区间，且只依赖后段首字节；
若后段也以 `\r`/空白开头，不确定状态继续右移，最多推迟到后段首个确定字节，
故每次拼接修正范围有上界（1 个 CR + 至多 wbLimit 个空白区间合并成的两个区间）。
坐标平移：各段区间的 orig/out 起点加该段在拼接结果中的累计偏移；
末尾策略只对拼接完成的整体应用一次。
