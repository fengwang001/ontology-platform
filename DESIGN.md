# 带转义的 RLE 编解码器设计

## 严格解码的拒绝清单（由不变量 Encode(Decode(t)) == t 推导）

不变量：严格解码器接受的每个 t 必须满足 Encode(Decode(t)) == t，即合法编码唯一
（规范形式）。Encode 只产出一种写法：最长游程、次数 1 省略、次数 >=2 写无前导零
十进制、数字与反斜杠必转义。凡接受后会导致再编码结果 != t 的输入都必须拒绝：

1. 显式次数 1（如 `1a`）：Decode 得 `a`，Encode(a)=`a` != `1a`。→ ErrCountOne。
2. 次数 0（如 `0a`）：0 次游程解码为空，Encode 永不产生 `0` 前缀。→ ErrCountZero。
3. 前导零次数（如 `01a`、`007a`）：Encode 写十进制无前导零，再编码会去掉零。
   → ErrLeadingZero。
4. 相邻游程同符号（如 `2a3a`、`aa`、`\1\1`）：Decode 得连续同码点，Encode 会合并
   成最长游程（`5a`、`2a`、`2\1`），!= 原串。→ ErrAdjacent。
5. `\` 后不是数字也不是反斜杠（如 `\a`）：`\` 只作转义前缀，Encode 从不单独输出
   反斜杠后跟非数字字符。→ ErrBadEscape。
6. 末尾孤立的 `\`（如 `a\`）：转义不完整，同 5 的理据，且无法对应任何码点。
   → ErrTrailingBackslash。
7. 末尾只有次数没有符号（如 `a12`）：Encode 的输出必以符号结尾，次数只是前缀。
   → ErrMissingSymbol。
8. 非法 UTF-8（孤立续字节、过长编码、截断序列等）：码点序列无法表示，Encode 只
   产生合法 UTF-8，往返不可能成立。→ ErrInvalidUTF8。

以上八类两两正交，各自返回可判定的哨兵错误，并携带出错 token 的字节偏移。
另有一类与编码形式无关的资源错误：输出超限 → ErrTooLarge。

## 大次数的取舍（第 3 条）

选择**支持**任意大的次数：次数用 math/big.Int 解析（runs.ParseCount），不存在
整数溢出，也就不会静默得到错误结果。但解码输出可能天文数字般大，因此解码器带
可配置的输出字节上限（NewDecoder(maxBytes)，Decode 用 DefaultMaxBytes）：在展开
游程**之前**用 big.Int 比较「次数 × 符号字节数」与剩余预算，超限即返回
ErrTooLarge，内存分配从不与次数成正比，只与实际上限成正比。

## 流式一致性

解码器是逐字节状态机（转义态 / 次数数字累积 / 多字节 rune 缓冲），每个输入字节
恰好检查一次（非导出计数器 checked 记录），状态与切分点无关，故任意切分喂入的
结果与错误逐位相同；`2a3a` 这类相邻性在第二个游程的符号到达时判定。

## 测试对照表

（待测试写完后补充）
