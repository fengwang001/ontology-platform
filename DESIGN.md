
## DESIGN.md — 规范化器推导与结论

### 1. 状态机（norm 核心，Keep 策略）
原语：`eol.Decoder`（待定 `\r`：遇 `\n` 成 CRLF，否则成单独 CR 并把该字节重喂）；`ws.Buf`（缓冲行尾空白候选 W=`[ \t]*`）。
norm 只持有 `W 缓冲` 与 `crPending`。逐字节：空格/Tab→入缓冲；`\n`→丢弃 W，输出 `\n`（CRLF 时 `\r` 记为删除）；`\r`→若已有待定 CR，先把旧 CR 结为单独行尾（丢 W、输出 `\n`），再置待定；其他字节→若有待定 CR 先结行尾，再把 W 作为普通内容冲刷后输出该字节。EOF：待定 CR 结为 `\n`；残留 W 删除。NUL/非法 UTF-8 当普通字节。

### 2. 末尾换行策略表（结论，幂等见括号）
| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

推导：Ensure 对空输入选 `""` 而非 `"\n"`：两者都可幂等（`"\n"` 是 Ensure 不动点），但选 `"\n"` 会让空文件凭空长出一行，且插入字节无原文对应，破坏偏移互逆（见 5），故选 `""`。规则：核心输出非空时，删掉末尾全部 `\n` 再补一个；核心空则输出空。Trim：删尽末尾空行，若仍有内容则补一个 `\n`，纯空白文件输出 `""`。两策略输出集合互为不动点，故幂等。

### 3. 被删字节的 ToOut
映射是分段线性单调函数，删除段 `[o0,o1)` 锚定在输出点 u（其后第一个保留字节的输出偏移），段内 `ToOut(i)=u`。由单调性：被删空白前的内容映射 `<u`，其后换行映射 `≥u`，唯一可取 u。编辑器把光标置于被删行尾空白中，应落在换行符**之前**（行内容末端，即该 `\n` 的偏移 u）；CRLF 的 `\r` 同理取 `\n` 的偏移；EOF 处被删 W 锚定输出输出终点。

### 4. 缓冲超限：选择拒绝
强制输出把 W 提前定为非行尾，若下一字节真是行尾，则输出多出空白，违反语义 2/5 且不可撤销。拒绝只发生在 W 增长瞬间，此时无字节依赖该判定，已输出内容与映射自洽，进入终态；语义 4/6 对任何被接受的输入成立，错误带原文偏移、可判定。故取拒绝（`ErrWSLimit`）。

### 5. par 拼接与插入的数学边界
段 i 携带前段尾部原文 T=`W*(\r)?`（仅 1 字节 CR 加一个 W 串描述符，**状态有上界**）处理 `T+chunk`，从 `ToOut(len(T))` 起采用输出与映射；前段在同一原坐标切开，丢弃其尾部待定结果。若 W 最终被保留（下段首字节为普通字节），该 W 输出随下段片段带出，原始坐标仍记在前段，拼接时按真实原文坐标合并，不重不漏。坐标平移：orig 加 `start-len(T)`，out 加全局已输出长度。
Ensure 对无换行内容追加的 `\n` 是纯插入：输出长 n+1、原文长 n，`ToOrig` 把 n+2 个输出偏移映入 n+1 个原文偏移，鸽巢原理下严格互逆不可能；约定插入 `\n` 与输出终点均映到原文终点，`ToOut(终点)=输出终点`；互逆律在所有非插入点成立，Keep/Trim 的删除情形严格成立。

### 6. span 复杂度
段只有 copy/drop/insert 三种，相邻同类合并：无删除的 10MB 文本只有 1 段。查询二分定位，计数器记本次检查的段数，保证 ≤ 2·log2(段数)+4。
