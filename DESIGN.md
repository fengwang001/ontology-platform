# DESIGN：自定格式 LZ77 流式压缩

## 流格式（所有整数为无符号 LEB128 varint；校验和为原文 CRC32 IEEE）

流头：`"OLZ" | version=1`。记录标签：0 字面量段、1 回指、2 刷新、3 流尾。

| 记录 | 字节布局 |
|---|---|
| 流头 | 0x4F 0x4C 0x5A varint(1) |
| 字面量段 | tag=0, varint(n), n 字节原文 |
| 回指 | tag=1, varint(dist), varint(length) |
| 刷新 | tag=2 |
| 流尾 | tag=3, varint(原文总长), varint(CRC32) |

刷新标记是边界声明：解压器见到即可保证此前输入都已还原，但输出无需停顿。

## 推导 1：压缩结果与 Write 切法无关

压缩器把未消费输入存在一个缓冲 `buf`，并在末尾保留 1 个「未决字节」`hold=1`。
只有 `len(buf) > hold` 时才允许在位置 `p` 做匹配决策：此时 `p+1` 处的字节已知，
若 `p` 处匹配因字符不一致而结束，则后续字节不可能延长它，该匹配长度已最终确定；
若没有匹配，`p` 可直接作为字面量输出，因为更晚到达的字节只会开启更晚位置的匹配，
不能回头让已输出的位置 `p` 进入一条回指（回指起点只能是当前位置）。因此每个输出
决策只依赖已到达的字节序列本身，与这些字节分几次 Write 到达无关。
未决的 1 字节在更多输入到来前不作结论。`Flush/Close` 时取消 hold：剩余字节全部
按「尽力匹配、否则字面量」处理——Flush 强制结束当前（可能本可跨边界延长的）匹配，
故 Flush 位置即解压边界；但窗口保留全部历史，Flush 之后仍可回指之前的数据。
无新输入时 Flush 直接输出空（连刷新标记也不重复发），故连续 Flush 第二次零字节。

## 推导 2：重叠回指（dist < length）

一次性块复制 `copy(out, out[i-d:i-d+len])` 的源区可能含尚未写入的目标位置，
Go 的 copy 向前覆盖，源被自身覆盖后内容错乱。正确语义是逐字节：
`out = append(out, out[len(out)-dist])` 重复 length 次，新写的字节可作后续源，
于是距离 1 复制出全同字节、距离 3 复制出周期串。实现直接用逐字节循环：
解压本身每字节 O(1)，循环不改变复杂度量级；为防炸弹先做 `长度 > 上限-已输出`
检查再复制，不预分配、不写后补检。

## 推导 3：两种「距离超限」的区分

dist>窗口容量：记录本身非法，即使输出历史里有那么多字节，压缩器也不可能引用
窗口外位置（match 只在环形缓冲现有内容中查找），故与切分无关地必为损坏流。
dist≤窗口但 >已输出字节数：该回指要求的历史尚未产生，在正常顺序流中不可能，
通常意味着流被截断/拼接错位。二者成因与定位不同，必须分别报错（均带字节偏移）。
并行块预置字典：块 b 压缩时把上一块末尾至多窗口容量字节装入窗口作字典，
但压缩出的回指仍按整份输入的连续顺序解释——解压器只有一条连续输出流，
字典字节就是已经输出的前缀字节，所以「已输出字节数」的含义不变，不需要特殊标记。

## 推导 4：并行块压缩的确定性

块 b 的压缩输入只有「本块原文 + 上一块末尾 ≤窗口容量 的原文」，哈希链的插入
顺序因此也完全确定（先插字典再插本块），与 goroutine 调度、完成顺序无关；
块 b（b>0）不依赖更前的块，也不依赖任何块的压缩结果。各块只输出记录流
（字面量/回指/必要时刷新），不输出流头流尾：由主线程先写流头，按块号顺序
拼接各块结果，再写一个刷新标记（保证块边界可解压）与流尾。记录天然自界定
（tag+varint 长度），故无需额外衔接字段。块间字典通过共享原文传递，无共享
可变状态；worker=1/2/4/8 的字节输出完全相同。
