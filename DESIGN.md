# LZ77 自定义流格式设计（推导）

## 流格式（全部 varint = LEB128 无符号，≤10 字节）

| 记录 | 标签字节 | 后续字段 | 说明 |
|---|---|---|---|
| 流头 | `4C 5A 37 01`（"LZ7"+版本1） | `windowCap:uvar` | 固定 4 魔数+版本，再变长窗口容量 |
| 字面量段 | `0x00` | `len:uvar` `bytes[len]` | len≥1；连续字面量合并为一段 |
| 回指 | `0x01` | `dist:uvar` `len:uvar` | 实际距离=dist+1，长度=len+3（最短匹配 3） |
| 刷新标记 | `0x02` | 无 | 语义边界，保证此前全部输入可被解压 |
| 流尾 | `0x03` | `rawLen:uvar` `crc32:uvar` | IEEE CRC32 覆盖全部原文；其后再有字节即多余 |

解析器状态机：固定头(5+var) → 循环读标签与字段 → 尾记录。未知标签即非法。

## 推导 1：为什么压缩输出与 Write 切法无关

在位置 i 能否发起匹配，依赖 `data[i..i+2]` 是否还能再向后延伸。若 i 是某次 Write 的最后一个
字节，此刻拿不到 i+1..i+2，直接判定只能发字面量会在「1 字节一段」时退化为不压缩。
规则：**只对「后面至少还留 minMatch(=3) 个未决字节」的位置做最终决策**；末尾不足 3 字节留作
pending，下次 Write 拼接后再判定。对位置 i 做最终决策时，哈希链查找与最长延伸读的都是同一份
拼接后原文，延伸到 pending 末尾自然终止，与字节在哪次 Write 到达无关，因此提交序列逐字节确定。
Flush 要求此刻就能还原全部输入，所以 Flush 必须**强制结束当前未决区间**：把 pending 当作「后面
不会再有字节」处理（延伸照样在末尾终止），并追加刷新标记；此后历史窗口保留全部已提交字节，
跨 Flush 仍可回指。第二次 Flush 无新输入：pending 为空且上次的刷新标记已发出，故零字节。

## 推导 2：重叠回指的复制

距离 d<长度 L 时，被引用的历史区间与输出区间重叠。块复制 `copy(out, hist[pos:pos+L])` 在 Go
（memmove 语义）下对 d<L 会读到还没复制过去的旧尾部，产出错误字节；正确语义是
`for k:=0;k<L;k++ { b := out[len(out)-d]; out=append(out,b) }`，每次读「当前输出」的 d 字节前，
于是周期自我复制（距离 1 产生填充、距离 3 复制 abc 模式）。L 可能巨大（2^40），逐字节 append 仍是
O(L)——而正确输出本来就是 L 字节，解压不可能优于 O(输出)；且循环不产生额外分配，故逐字节循环
在复杂度上无损，直接采用，避免重叠块复制的特殊分支。

## 推导 3：「距离 > 已输出」与「距离 > 窗口容量」为何分开

两者在合法流中都不该出现，但失败时刻不同：压缩器只向仍在窗口内的位置发回指，窗口容量是流头
声明的构造参数，**距离 > 窗口容量意味着引用了压缩器按构造绝不可能保留的字节**（头损坏或伪造流）；
而距离 ≤ 窗口容量却 > 已输出字节数，意味着引用「逻辑上存在于窗口、但本流尚未产出」的字节，
典型是截断或块字典被破坏。先判容量（ErrDistOverflow）再判已输出（ErrDistTooFar），错误可区分且
都带字节偏移。压缩器保证不产生前者的条件：候选只从窗口现存（未被环形覆盖、位置 ≥ total-cap）
的历史链中取。并行块的预置字典改变了「已输出」吗？**没有**：解压器只有一个线性输出序列，预置
字典字节是上一块的原文，已被上一块正常解压输出；块间在流格式上只是普通记录首尾相接（无额外
块标记，块边界处发刷新标记保证可还原），所以解压器无需知道分块。

## 推导 4：并行块压缩的确定性

`CompressParallel(data, blockSize, workers)`：第 k 块的输入是
`data[ks:(k+1)s]`，预置字典是上一块原文末尾 `min(cap, ks)` 字节。匹配器只对「本块原文 + 该字典」
做纯函数式查找，不读任何其他块的输出，也不读共享可变状态；故结果与 goroutine 调度、workers
数无关（workers 只改并发度，块的切分与字典固定）。实现上每个块用独立 encoder（独立窗口/哈希链），
只把第 0 块写流头、只在末块写流尾；块尾强制 Flush，刷新标记提供确定的边界衔接。主 goroutine 按
块序号收结果（结果先存入下标数组，不按完成顺序拼接），CRC/总长在最终 Close 时由全量输入算出，
因此四种 workers 输出逐字节相同；`-race` 下共享只读 data、各自独立编码器。

## 其他要点

- 解压 Output() 采用 drain 语义：取走并清空内部缓冲；单实例非并发安全，文档明示。
- varint 解码：>10 字节或第 10 字节超 1 位 → ErrVarint；任何在读完必需字节前缺字节 → ErrTruncated。
- 输出上限在「分配/追加之前」检查 n+L > max（含回指声明长度），超限进入终态。
- 候选计数器只增于「实际取窗口字节比较的候选」；同哈希按链头逆序走，深度达链上限即停。
