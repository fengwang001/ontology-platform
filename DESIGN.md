# DESIGN

## 1. 末尾换行策略（空输入与只含换行）

规范化主体：删除行尾空白；`\r\n`、`\r`、`\n` 均输出一个 `\n`。末尾策略只看主体输出 `y`：
- Keep：原样，输出 `y`。
- ExactlyOne：若 `y` 不含 `\n`（空主体），输出 `"\n"`；否则输出 `y`。即空输入补一个换行，已有任意末尾换行不增删。
- Trim：删除全部末尾空行；空主体输出 `""`，否则输出「去掉末尾所有 `\n` 后再加一个 `\n`」。

幂等：三种策略的输出都以 `\n` 结尾或为空，且不再含可删内容，再规范化一次主体不变；Keep 的不动点是不以 `\n` 结尾的串（含 `""`），ExactlyOne 的不动点恰是 `"\n"` 结尾的非空串，Trim 的不动点无尾部连续 `\n`。ExactlyOne 对空输入若选 `""`，则 `N("")=""` 而 `"\n"` 仍是不动点，两种选择各自幂等，区别只在空文件是否被视为需要换行；选 `"\n"` 让「非空文本必以换行结束」单一规则成立，故采用。

| 输入 | Keep | ExactlyOne | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

映射是两个单调不减阶跃函数，span 表按原文区间顺序存 `Copy/Del/Ins`。规则：被删原文字节 `ToOut(i)` 取其所属删除区间**之后第一个输出字节的偏移**；若删除位于输出末尾（EOF 处删掉的行尾空白），取输出长度。即光标落在被删空白里，映射到该行 `\n` 之前（空白删除后换行前移，光标准确落在行尾换行之前；EOF 处空白消失则映射到输出末尾）。`\r\n` 的 `\r` 同理：映射到 `\n` 的偏移。删除区间内各 i 取值相同；为保 `ToOut(ToOrig(o))==o`，令 `ToOrig` 对该输出偏移取删除区间**之后**第一个原文偏移（查询用「首个 oEnd>o 的段」，终点偏移归后段）。被删位置本来就没有输出字节，互逆只对输出偏移成立。

## 3. 缓冲超限

若强制判定为非行尾并输出，而后续真出现行尾，已输出的空白无法回收，违反第 2 条与幂等（输出残留行尾空白），且映射按两种未来分叉，违反第 4、6 条。故选择**拒绝**：返回可判定错误 `ErrSpaceLimit`（带原文偏移），已输出内容与映射保留，写入器进入终态（后续 Write 返回 `ErrClosed`），状态等同「截断在该位置再 Close」，完全可解释。输出上限同理：写入即拒绝 `ErrOutputLimit`，保留已产出内容。

## 4. par 拼接

各段独立流式规范化（含 Close），得到主体输出、span 表（段内坐标）与尾部状态（`none/cr/spaces`，由 eol/ws 自然暴露）。顺序拼接时每个边界只看上一段尾部与本段首字节，修正范围至多一个待定 `\r` 和一串空白：
- 上段尾 `cr`、本段首 `\n`：本是 `\r\n`；上段 Close 已产出一次 `\n`，本段首字节又产出一次，删掉上段那个换行（删 1 输出字节与对应插入段），`\n` 归属本段。
- 上段尾 `cr`、本段首非 `\n`：上段 Close 已正确产出 `\n`，无需改。
- 上段尾 `spaces`：本段首为行尾（`\n`/`\r`）或本段为空（最后段）时空白确实在行尾，保持删除；否则把空白原样插回两段之间（加一段 Copy，映射指回原文）。

段表坐标整体平移到全局后拼接、修正段就地改；每段修正只动段首/段尾常数个区间，故有上界。空格上限按整段长度放宽（单段内部不可能真正超限），空白的最终判定恰由上述边界修正完成。
