# 属性级权限过滤器 — 设计推导

## 问题

角色策略只规定「哪些列可见」。当过滤谓词引用了对该角色不可见的列时，
执行器该怎么办？三种做法：甲（不可见比较当假）、乙（不可见列当 NULL 走
三值逻辑）、丙（整条查询拒绝并指名列）。本题论证只有丙不泄露，并采用丙。

## 甲的泄露路径：不可见比较当假

探针查询：`WHERE NOT (secret = 1)`，角色看不到 `secret`。
设数据中有行 R1(secret=1)、R2(secret=2)，可见列相同。

- 对 R1：`secret = 1` 被当作假，`NOT false = true`，R1 被返回。
- 对 R2：同样被当作假，R2 也被返回。
- 但调用方再发 `WHERE secret = 1`（当假）返回空集；两相对照，
  「NOT 形式返回全部、肯定形式返回空」这一不对称本身就泄露了
  「存在 secret=1 的行」——因为若不存在这样的行，两种形式在
  「当假」语义下会给出一致结果。更直接地：调用方逐值探测
  `NOT (secret = v)` 与 `secret = v` 的返回集差异，即可逐比特
  反推出整列 secret 的值分布。返回集的内容依赖了不可见列的真值，
  这就是泄露信道。

## 乙的泄露路径：不可见列当 NULL（三值逻辑）

探针查询：`WHERE secret IS NULL`。

- 若 secret 被当作 NULL，则 `IS NULL` 对所有行恒真，返回全表。
- 若该列根本不存在，同样的查询应报「未知列」错误。
- 两种可区分的结果让调用方确认「secret 列存在且被隐藏」——
  列的存在性本身就是要保护的信息。此外 `secret = 1` 在三值逻辑下
  得 UNKNOWN，WHERE 过滤 UNKNOWN 与过滤 FALSE 的可观测行为与甲
  相同，甲的逐值反推信道在乙下依然存在。

## 丙：整条拒绝并指名列与路径

拒绝错误只依赖「谓词文本 + 角色策略」，二者对调用方都是已知的，
不依赖任何行数据，因此错误本身不携带数据信道。任何含有不可见列
引用的谓词一律拒绝，错误中指名列名与其在谓词树中的路径（可审计、
可定位），调用方只能改写查询，无法逐值探测。故采用丙。

## OR 短路例外的定义与理由

定义：先对谓词树做一遍常量折叠——常量节点求值；`TRUE OR x → TRUE`、
`FALSE AND x → FALSE`（被短路掉的子树 x 整体移除）；`NOT` 作用于
常量时折叠。折叠后仍然存活的不可见列引用才触发拒绝；被折叠消除的
子树中的引用不拒绝。

理由：被短路移除的子树在任何一行上都不会被求值，其列引用对结果集
没有任何数据依赖，拒绝它不会提升机密性，只会误伤可用性（例如
`WHERE 1=1 OR secret = 1` 恒真，secret 从未被读取）。反之，只要
引用在折叠后的树中存活，就存在某行使该引用被求值，信道就存在，
必须拒绝。报告中的路径一律指向原始谓词树中的位置，保证可审计。

实现：后序一遍遍历同时完成常量折叠与存活引用收集，每个节点恰好
访问一次（filter 包内非导出计数器可断言此性质）。

## 行裁剪

结果行只拷贝可见列的键值对：不可见列从行中移除（不是置零值/NULL，
否则「不可见」与「值为空」不可区分）。裁剪按可见列集合迭代而非按行
迭代，代价与可见列数成正比、与总列数无关。
