# 设计推导（DESIGN）

## 1. 非法 UTF-8 单元的切分（WHATWG 规则的重推）

标量区为 `[0,0xD7FF]∪[0xE000,0x10FFFF]`；代码点按最短形式编码：1/2/3/4 字节，首字节
`00-7F / C2-DF / E0-EF / F0-F4`，后继字节在 `80-BF`。非法单元用统一状态机切出：

1. 从位置 i 取首字节。ASCII 消费 1 个；合法首字节给出序列长 need。
2. 若剩余（含跨 Write 拼入的缓存）不足 need：这是**未定前缀**，缓存等待；流结束才判截断。
3. 逐字节校验后继，每个首字节对应的第二字节合法区间（用于排除非最短/越界/代理）：

| 首字节 | 第二字节区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 首 1 字节 |
| E0 | A0..BF | 首 1 字节 |
| E1..EC | 80..BF | 首 1 字节 |
| ED | 80..9F | 首 1 字节 |
| EE..EF | 80..BF | 首 1 字节 |
| F0 | 90..BF | 首 1 字节 |
| F1..F3 | 80..BF | 首 1 字节 |
| F4 | 80..8F | 首 1 字节 |
| 80..BF（游离） | — | 1 字节 |
| C0 C1 F5..FF | —（非法首字节） | 1 字节 |

4. 一旦某个期望后继不在 `80-BF`，或第二字节越上表区间：该单元**只吞首字节**，吐出一个
   U+FFFD，未消费的坏字节从下一个单元重新解析。后继全部合法但码点落入代理/越界（被区间表
   排除，不会发生；区间表就是为此而设）。

样例验证：`F0 90 80 41`：F0 期望四字节、第三字节 80 是合法后继，直到 41 不是后继 →
只吞 F0，一个 FFFD，41 重新解析为 A；`E0 80 80`：E0 的第二字节须 A0-BF，80 非法 →
只吞 E0；80、80 各为游离后继 → 共三个；`ED A0 80`：ED 第二字节须 80-9F，A0 非法 →
三；`C0 AF`：C0 非法首字节只吞自身，AF 游离 → 两；`F4 90 80 80`：F4 第二字节须
80-8F → 四；`E2 82`+EOF：两字节都是未定前缀（未见到违规字节），整体是一个被截断的
合法前缀 → 一个 FFFD；`80 80`：两个游离后继 → 两个。

## 2. 缓存上限与何时立即放弃

UTF-8 最长 4 字节，未定前缀至多保留首字节加其后继 = 3 字节缓存（硬上限 3）；UTF-16 单
字符至多 4 字节，未定部分（奇数尾字节或高代理）至多 2 字节（硬上限 2）。立即判定的
时机：看到任一「期望后继」字节却不合法（非 80-BF，或第二字节越区间表），或首字节本身
是 C0/C1/F5-FF，当拍即吐 FFFD，不必等到流结束；只有「字节数不够」才缓存等待。

检查次数：解码器每个字节恰在「进入某位置」时检查一次；被吐回重解析的坏字节在下一拍再
查一次，最坏 2n，与切分无关。

## 3. par 切点对齐

UTF-8：段 k（k>0）起点 i 向回看最多 3 字节：若 i-1 是合法首字节且其长度越过 i，则从该
首字节开始解析（左段吐出的标量截到该起点为止，字节区间不重叠：左段输出对应
[start,i')，右段 [i',end)）；否则 i 即为起点。回看只在「第二字节是否落在区间内」失败
时才不移动，因此 `C3 | 80 80`：C3 是合法首字节且越过切点 → 右段从 C3 开始，C3 80 解
出 U+00C0，剩下 80 是一个 FFFD，与单线程一致；`E0 80 | 80`：E0 长度 3 越过切点，但
第二字节 80 不在 A0-BF → E0 单元留在左段（左段一个 FFFD），右段从 80,80 得两个 FFFD。
UTF-16：段起点对齐到偶数边界；若左段最后一个 code unit 是高代理且右段首单元是低代理，
起点再前移 2 字节。只有第 0 段允许识别/丢弃 BOM；其余段中 FEFF 一律是普通字符。各段
用严格模式并带全局偏移，错误取最小偏移；替换模式拼接输出与统计。回看是 O(K·3) 常数。

## 4. 上限断点

事件按整标量（含完整代理对）提交输出。超限时在提交前停止：`Write` 返回的 n = 已提交
标量覆盖的输入字节数（BOM 字节计入）；仍在缓存中的未定前缀字节**未被消费**，且必然是
本次 p 的一个后缀，调用方把 `p[n:]` 喂给新实例即可续传，拼接与一次转完逐字节相同。
