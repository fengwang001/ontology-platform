# DESIGN.md — 基于版本向量的多副本冲突检测器

仅用标准库；状态全在进程内存。包：`vv`（向量与偏序）、`store`（本地存储）、`sync`（副本同步）、`conflict`（兄弟版本）、`prune`（裁剪）、`cmd/demo`。

## 1. 版本向量与偏序四关系

向量为函数 V: 副本ID → uint64 计数器，**缺失分量语义为 0**。设两向量在并集 U 上逐分量比较：
- `lt`：存在 i 使 V1[i] < V2[i]；`gt`：存在 i 使 V1[i] > V2[i]。
- 四关系：相等（!lt && !gt）；严格小于 V1≺V2（lt && !gt，即各分量 ≤ 且至少一个 <）；严格大于（!lt && gt）；**并发 V1∥V2（lt && gt，同时存在更小与更大分量）**。
- 关键：并发必须同时检查两个方向，不能只用「全部分量 ≤」；`{A:1,B:0}` 与 `{A:0,B:1}` 两方向各有一个严格不等，故并发。
- 缺失分量：`{A:1}` 与 `{A:1,B:0}` 在并集 {A,B} 上各分量相等 → 相等，非并发。

只遍历两向量分量的并集（归并排序 key），比较次数 ≤ 2|U|（每分量至多读取两边各一次），不遍历全部历史副本。用 `Counter` 记录读取次数供断言。

## 2. 存储与本地写入

`store.Store` 持有 registry（登记副本）、自身 ID、每个键的兄弟集合 `[]conflict.Sibling{Value, Vector}`。本地写：自身分量 +1（`MaxUint64` 时返回 `ErrOverflow`，不回绕）；同一副本连续写入严格递增（单副本任意两版本总可比）。

## 3. 同步合并与兄弟保留

把 X 的某键兄弟集合并入 Y，逐兄弟对做偏序比较：
- sx≺sy：忽略 sx；sx≻sy：sx 覆盖 sy；相等：去重保留一个；**并发：两者都保留为兄弟**。
- 合并对单个兄弟是交换、结合、幂等的；兄弟按 `(Value 字典序, 向量规范化字节序)` 全序规范化排序，保证两副本集合逐元素相同。
- 回退检测：Store 记录每个对等副本已见向量水位 Wp；收到向量某分量小于水位对应分量 → `ErrCounterRollback`，拒绝整次并入。
- 未知副本：**严格登记制**——向量出现 registry 未登记 ID 即 `ErrUnknownReplica`；已登记但分量缺失仍按 0。
- 每对副本双向各同步一次后，双方兄弟集合 = 双方写入集合的并集（最大元集）。三副本每对至少同步一次后三者状态为同一并集，逐字节相同；同步幂等、交换。

## 4. 兄弟上限

`conflict.Set` 带容量 cap（≥1）。超限时按确定性规则丢弃**最旧**：优先丢弃被另一兄弟严格支配（≺）者；无可支配者时按全序 key（向量规范化序, value）丢最靠前者，计数器记录丢弃数。cap=1 退化为最后写入者胜：新兄弟严格大于才替换，否则丢弃新者。

## 5. 裁剪安全性

退役副本集合 R 从所有向量移除。安全条件：**裁剪后活跃分量上任意两向量的四关系不得改变**。判定：对存储中全部向量对比较活跃投影关系，与原关系不一致（如严格小于变并发）即返回 `ErrPruneUnsafe` 拒绝，不修改状态。全零分量向量天然安全。

## 6. 序列化与截断分类

编码 = `magic(1)|kind(1)|count(4,BE)|项...|crc32(IEEE)`，项为 `idLen(2)|id|counter(8,BE)`（条目追加 `keyLen(2)|key|valLen(4)|value`）。按长度分类：
- < 头部 6 字节 → `ErrHeaderTruncated`；
- 项未读完/字段越界 → `ErrEntryTruncated`；
- 项读完但 CRC 4 字节不全 → `ErrCRCTruncated`；
- CRC 完整但不符 → `ErrCRCMismatch`；完全一致 → OK。

四类截断 + 回退/溢出/未知副本/裁剪均为哨兵错误，`errors.Is` 可区分。

## 7. 边界语义

零副本：空 registry、空向量两两相等，本地写报 `ErrUnknownReplica`。所有分量为 0 判相等。20 种同步顺序：对 3 副本的有向同步步序列做确定性枚举（每对至少一次），共 20 条，断言整库规范化哈希全部相同。
