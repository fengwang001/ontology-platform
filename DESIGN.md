# Set-Cookie 解析与适用性判定 — 设计推导

## 1. 默认路径推导（规范第 2 条）

规则：取来源 URL 路径，若不以 `/` 开头则为 `/`；否则取最后一个 `/`
之前的部分，结果为空则为 `/`。

- `/a/b/c` → 最后一个 `/` 在 `c` 之前 → `/a/b`
- `/a` → 最后一个 `/` 是首字符，其前为空串 → 空则回退 `/`
- `/` → 同上 → `/`

为什么 `/a` 推出 `/` 而不是 `/a`：默认路径的语义是「设置者所在的
目录」。`/a` 这个资源位于根目录下，它的「目录」是 `/`；若错误地推出
`/a`，则该 cookie 会被路径匹配规则限定到 `/a` 子树，导致同目录的
兄弟资源（如 `/b`）收不到它，而 `/a/...` 下的子资源反而收到——
既漏发又错发。RFC 6265 §5.1.4 的算法正是「去掉最后一段」，最后一段
是资源名而非目录。

## 2. 路径匹配为何不能是朴素前缀（规范第 3 条）

朴素前缀匹配下，cookie 路径 `/a` 会命中请求路径 `/ab`，因为
`strings.HasPrefix("/ab", "/a")` 为真。越权场景：站点把
`/account` 的会话 cookie 设在路径 `/acc` 上（或攻击者在可控路径
`/a` 下设 cookie），朴素前缀会让它随请求发往 `/ab`、`/admin` 等
毫不相关的应用，造成跨应用会话泄露与会话固定。正确判据（RFC 6265
§5.1.4）：相等，或 cookie 路径是请求路径前缀 **且** cookie 路径以
`/` 结尾，或前缀后请求路径的下一字符是 `/`。故 `/a` 匹配
`/a`、`/a/`、`/a/b`，不匹配 `/ab`。

## 3. Domain 越权判据（规范第 4 条）

判据：规范化后的 Domain（去前导点、小写）必须等于来源主机，或是来源
主机的父域（即来源主机以 `.`+Domain 结尾）；否则拒绝。这保证 cookie
只能「向上」委托到祖先域，不能横向或跨域设置：`b.example.com` 设
`Domain=example.org` 时，`b.example.com` 既不等于 `example.org`
也不以其为后缀，拒绝。

单标签域（如 `com`）必须拒绝：它是公共后缀级别，`b.example.com` 以
`.com` 结尾会满足后缀判据，若放行则任意站点都能设置对整个 `com`
顶级域生效的 cookie，等于向全网广播会话。工程上无公共后缀表，故以
「Domain 至少含一个点（两标签以上）」作为保守近似判据，单标签一律
拒绝。

## 4. 覆盖时为何保留最早创建时间（规范第 8 条）

发送顺序规则是「路径长的在前；等长时创建时间早的在前」。创建时间
表达的是「这个 (name, domain, path) 身份何时首次进入罐子」，覆盖
只是刷新值与属性，身份未变，故时间应保留最早那次。若改成覆盖时刷新，
则每次续期/重设都会把该 cookie 排到等长路径组的末尾：两个等长路径
的 cookie 每被覆盖一次，相对顺序就可能对调，发送顺序随写入时序抖动，
不再具备确定性，也让「先建立的会话优先」这一可预期性质失效。
