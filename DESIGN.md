# DESIGN

## 流格式（wire 包，整数一律 uvarint）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 3 字节魔数 `LZ7` + 1 字节版本 `0x01` |
| 字面量段 | tag=0 + 长度 n + n 字节原文 |
| 回指 | tag=1 + 距离 dist + 长度 len |
| 刷新标记 | tag=2（无负载，解压语义为空操作） |
| 流尾 | tag=3 + 原文总长 + FNV-1a-64 校验和 |

tag 本身是 1 字节 uvarint（值 0..3）。流尾之后任何字节都是错误。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

位置 p 的最长匹配长度 L 由 `data[p:]` 决定；而 L 无上界（全同字节
输入一步匹配可达数 MB）。因此在 `Write` 边界上，任何**定长**的未决
缓冲都不足以判定当前匹配是否结束——唯一安全的判定时刻是「数据不可
再变多」的时刻，即 `Flush`/`Close`。结论：`Write` 只追加到内部缓冲、
不做任何解析决定；解析（贪心最长匹配 + 哈希链）推迟到 `Flush`/`Close`，
且解析只看到截至本次 Flush 偏移的字节。于是输出只依赖「字节内容 +
Flush 偏移序列」，与 Write 切法无关。推论：`Flush` 必须强制结束当前
匹配与当前字面量段——跨过 Flush 偏移的匹配在偏移处被截断（匹配器
此刻根本看不到偏移之后的字节），否则 Flush 的承诺（已写字节足以还
原截至 Flush 的全部输入）无法满足。

## 推导二：距离 < 长度的重叠回指（第 2 条）

块复制 `copy(dst, src)` 在 dist < len 时，源区间包含尚未写出的字节，
结果错误（除非按 ≤dist 的小块反复复制）。逐字节向前复制则每步从窗
口固定距离处读，读到的可以是上一步刚写入的字节，语义天然正确。本
实现解压侧用环形窗口 `At(dist)` 逐字节复制：每字节 O(1)，总代价
O(输出长度)——而任何解压器都必须写出每个输出字节，故渐近上已最
优，不构成退化；常数小（一次取模索引），可接受。压缩侧同理：匹配
器比较第 l 字节时，l < dist 读窗口历史，l ≥ dist 读本块内已匹配的前
缀，等价于解压侧的语义，因此重叠匹配可被正确发现与还原。

## 推导三：「距离超已输出」与「距离超窗口」为何分开（第 7 条）

「超窗口」是资源/格式约束：解压器只保留 W 字节历史，dist > W 的字
节物理上不存在。「超已输出」是流逻辑错误：dist ≤ W 但流开头还没输
出过这么多字节，窗口里对应位置是陈旧内容。压缩器只从窗口中存在的
候选产生回指，故 dist ≤ min(W, 已输出) 恒成立，两种错误它都不可能
产生——它们只用于拦截损坏流。并行块压缩中，预置字典就是上一块原
文的尾部，在拼接后的流里这些字节恰好是解码器已输出的字节，因此
「已输出」的含义不变，dist ≤ 已输出 在解码时天然成立。

## 推导四：并行块压缩的确定性（第 9 条）

块 i 的压缩 = 纯函数(上一块末尾 ≤W 字节字典, 本块原文)：每块用全新
的匹配器与窗口，字典经 `LoadRange` 预置，不读任何其他块的压缩结果，
块间无共享可变状态，因此与 goroutine 数量、完成顺序无关；workers 只
影响调度。汇总时按块下标顺序拼接记录，块间**不需要**刷新标记——刷
新标记对解压器是空操作，省略后拼出的流与单流压缩器可能产出的流同
构，流式解压器直接可解。块边界处匹配被截断（同推导一的 Flush 语义），
代价是边界处略低的压缩率，换来确定性与可并行性。
