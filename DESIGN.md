# 三方合并冲突检测器 — 设计推导

## 模型

- 记录集合 `Set = map[key]Record`，`Record = map[field]Value`。
- `Value` 带类型：`Str(s)` 或 `Num(n)`；比较时类型与值都参与相等性。
- 三个输入均为**全量快照**：祖先 A、左 L、右 R。某侧缺少键 = 该侧删除了该记录。

## 变更的判定（相对祖先）

对任意键 k，比较 A[k] 与 L[k]（R 同理）：

- 两侧都不存在：无变更。
- A 有、侧无：该侧**删除**。
- A 无、侧有：该侧**新增**。
- 都有：逐字段比较，字段集合不同的部分构成**字段级修改**（含字段增删）。

「未改动」定义为与祖先逐字节相等（规范化编码一致），与构造顺序无关。

## 冲突判定表推导

对键 k 枚举两侧变更形态（X=未改, M=修改, D=删除, N=新增）：

| L\R | X | M | D | N |
|-----|---|---|---|---|
| X | 取祖先 | 取 R | 取删除 | 取 R |
| M | 取 L | 见下（字段级） | **冲突** | 不可能* |
| D | 取删除 | **冲突** | 取删除 | 不可能* |
| N | 取 L | 不可能* | 不可能* | 内容同则取 L，异则**冲突** |

*A 无 k 时两侧都只能是 X 或 N；A 有 k 时两侧都只能是 X/M/D，故 M/N、D/N 组合不存在。

### 关键条目理由

1. **两侧改成相同值不是冲突**：合并的目的是汇聚双方意图；两侧表达了同一意图（F: 1→2），取 2 不丢失任何一方的意愿。按「都改过即冲突」的朴素规则会把一致意见误报为分歧，故必须先在字段级比较**结果值**，相同则静默采用。
2. **删除 vs 修改是冲突且类型独立**：删除方意图是「该记录不应存在」，修改方意图是「该记录应存在且字段更新」，两者互斥、无法机械调和（保留则丢失删除意图，删除则丢失修改内容）。它与「同字段改不同值」性质不同——后者是内容分歧、记录存续无争议——故用独立冲突类型 `DeleteVsModify`，便于上层分别处理。
3. **字段级粒度**：记录是字段的合取，两侧改不同字段时意图正交，两个修改都生效即可同时满足，无冲突。只有同一字段被改为不同值（或一侧删字段一侧改字段）才冲突。
4. **两侧同删**：意图一致，结果删除，无冲突。
5. **新增冲突**：A 无 k 时两侧各自新增视为独立创作；内容相同则意图一致取其一，不同则 `AddAdd` 冲突，结果确定性地取左侧（文档化规则，冲突已上报）。
6. **类型不一致**：同字段两侧分别为字符串与数值时，即使文本相同也判冲突（`TypeMismatch`），报告两侧类型——类型是值语义的一部分，静默选边会改变数据解释。

## 边界语义定义

- **某侧为空集**：按快照语义 = 该侧删除了祖先中的全部记录（不是「未改动」）。「未改动」的表示是与祖先内容相等，而非空。
- 三集合皆空 → 空结果、零冲突。
- 祖先为空、两侧有内容 → 全部按新增处理（第 5 条）。
- 空串键、空串字段名：合法，长度前缀编码天然支持。
- 字段值为空串 `Str("")` 与字段不存在：由 map 的存在性区分，编码时存在性显式落盘。

## 算法与复杂度

- 一遍扫描：先求三集合键并集并排序，逐键各做一次 map 查找（3 次/键），字段合并只访问该键的三条记录。查找计数 ≤ 3N < 4N（N=并集大小）。
- 冲突在扫描中就地收集；报告阶段只遍历冲突列表，访问记录数 = 冲突数，不二次遍历全集。
- 确定性：键、字段均按字典序处理，输出与 map 迭代/构造顺序无关。

## 序列化

`magic(4) | version(1) | payloadLen(4) | payload | crc32(payload)(4)`。
截断分类：`<13` 头部不完整；`<13+payloadLen` 记录不完整；缺尾部 CRC 或校验不符 → CRC 不匹配。三类错误均可 `errors.Is` 判定。
