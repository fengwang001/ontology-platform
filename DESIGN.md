# DESIGN：游标分页器（keyset pagination）

## 1. 复合游标的必要性

数据集按 `(Score float64, ID string)` 升序排列，ID 全局唯一，故复合键全序。

- 只记 Score，下一页取 `Score > v`：若 6 行同分 v，本页取走 3 行，其余 3 行被整批跳过（漏）。
- 下一页取 `Score >= v`：本页已返回的 3 行再次出现（重）。
- 结论：游标必须记录复合键 `(v, id)`，下一页边界取复合键**严格大于**：
  `Score > v || (Score == v && ID > id)`。等值的同分行由 ID 次序逐个带走，不重不漏。

## 2. 正向与反向翻页

正向页 `[(s0,id0) .. (sn,idn)]` 同时返回两个游标：

- `Next = (sn,idn)`，方向 Fwd；继续正向取 `Key > Next`。
- `Prev = (s0,id0)`，方向 Back；继续反向取 `Key < Prev`，再按正向顺序排列。

反向取数时比较方向与取数方向都取反，但**页内顺序仍为正向**：从末尾方向截取 n 行后，把结果反转。
因此对正向第三页使用其 `Prev` 反向翻一页，得到的正是第二页，且逐行顺序相同，不是倒序。

游标自带方向标记，请求方向与游标方向不一致时返回可判定错误，绝不静默按另一方向处理。
空字节串是合法游标，表示从起始端开始（正向从头，反向从尾）。

## 3. 游标语义

不透明字节串，固定结构：`Version(1) | Dir(1) | ScoreBE(8) | IDLenBE(2) | ID | FNV1a校验和(8)`。
空串=起始。校验和覆盖前面全部字节。解码失败分四类（`errors.Is` 可区分）：
校验失败、字段不完整、方向标记非法、跨方向复用。游标指向的行被删除不影响续翻——
定位只以复合键比较为准（二分查找），不依赖该行存在。

## 4. 并发增删语义

数据用切片有序存储，读操作持 RLock 期间对切片快照并复制结果行，写操作持 Lock 重建顺序。
翻页是纯函数：游标不可变、无隐藏推进状态；同一游标多协程并发翻页，结果逐行相同。

语义承诺（无快照隔离）：
- 已返回过的行绝不会因后续翻页再次出现（边界是严格大于，单调推进）。
- 未被删除、且其复合键落在尚未翻过区间内的行，绝不会被跳过。
- 新行插入到已翻过区间，允许不出现；插入到未翻区间则必须能被后续页取出。
- 被删除的行不再出现；删除游标所指行后游标依然有效。

## 5. 效率

有序切片 + `sort.Search` 二分定位起始下标，每页至多顺序读取 pageSize 行，
比较次数上界为 `ceil(log2(N)) + pageSize`；约束要求上界 `4*(pageSize+ceil(log2(N)))`。
游标编解码为定长字段操作，时间与堆分配均为 O(1)（ID 拷贝一次，分配常数次）。

## 6. 自检（audit）

记录每次翻页返回的 ID：同一 ID 出现两次即为"重"；翻页结束后，对当前数据集做一次
全量排序，凡复合键落在已遍历区间（首键..末键之间）但未被访问到的存活行即为"漏"。
