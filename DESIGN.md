# 批量导入幂等与断点续传器 — 设计推导

## 1. 进度粒度的推导

写入按业务键幂等（同一 key 重复写同一 value 是 no-op），这是粗粒度进度的前提：

- 若写入不幂等，崩溃后无法区分「已写未刷进度」的记录，重放会产生重复/冲突，
  只能每写一条刷一次进度（细粒度），吞吐塌方。
- 若写入幂等，重放任何已写记录都安全。于是可以每写满 N 条（一个 chunk）刷一次
  进度。崩溃时最近不足 N 条的记录状态不明，但续传时只需从「最后一个已刷新区间
  端点」重放，重放条数 <= N，与批次总量无关。

结论：进度文件记录已刷新的已写区间序列（顺序写入下退化为前沿 frontier），
粒度取 N=chunk 大小。崩溃损失上界 = N 条重放，换来刷盘次数降低 N 倍。
续传起点 = min(进度前沿, 存储实际前沿)，重放区间内的 Put 全部命中幂等路径。

## 2. 重复导入的幂等判定

- 记录值确定性：`value = batchID + "\x00" + key`，不含时间戳/随机量，
  因此同一批次重放后存储逐字节相同（存储序列化按 key 排序输出）。
- `Commit` 幂等：提交后在进度目录写 `committed-<id>` 标记。再次导入同批次时
  Prepare 阶段先查标记，直接返回 `ErrAlreadyCommitted`（可判定、非失败），
  不写任何记录，存储保持逐字节相同。

## 3. 为什么以存储为准、不以进度为准

进度文件是「提示」，存储才是「事实」。进度可能超前于存储（崩溃发生在写盘后、
刷进度前的窗口，或人为构造的不一致）。若盲信进度，缺口区间的记录将永远丢失。
因此续传时对存储做存在性前缀判定：顺序写入保证「已写」是清单前缀，用二分查找
O(log n) 定位存储实际前沿，取 `min(进度前沿, 存储前沿)` 为续传起点；Commit 前
还有一次 O(n) 全量 Verify 兜底。这样进度损坏/超前只会导致安全重放，不会丢数据。

## 4. 进度文件格式与截断分类

```
P1 <batchID> <total>\n        头部
I <start> <end>\n             每个 flush 追加一条区间
E <crc32(此前全部字节)>\n      整体校验行（每次 flush 重写全文件，tmp+rename 原子替换）
```

截断分类（对截断点 m 可判定）：

- m 落在头部行内（无完整首行）→ `ErrHeader`（头部不完整），恢复进度 = 空；
- m 落在某条区间行内（末尾出现无换行的 `I ...` 残行）→ `ErrInterval`
  （区间记录不完整），丢弃残行，恢复此前的完整区间；
- 其余（切点在行边界、或落在 E 行内、或 E 缺失/校验不符）→ `ErrCRC`
  （CRC 不匹配），恢复全部完整区间。

恢复结果恒为「最大可恢复前缀」；进度文件不存在视为从头开始，不是错误。

## 5. 占用标记（锁）与崩溃接管

`lock-<id>` 文件以 `O_CREATE|O_EXCL` 创建，内容为 `持有者 时间戳`。
已存在时读时间戳：未过期（注入时钟判定，TTL 外部位）→ `ErrBusy` 立即返回；
已过期 → 删除并重建（安全接管）。导入器正常退出即释放；进程崩溃留下残锁，
靠 TTL 过期被后续导入器接管。锁按批次分文件，不同批次可并发。

## 6. 资源与复杂度上界

- 内存：Write 阶段按 chunk（<=N 条）驻留，计数器记录历史峰值，峰值 <= N。
- 续传：二分定位存储前沿 O(log n) 次 Get，重写数 <= N，不重头扫描。
- 对账：先建清单键集合（内存），再对存储一遍 Visit，再对清单下标一遍扫描
  合并区间，存储访问次数 = 存储条目数 <= 3n，无 O(n^2)。
