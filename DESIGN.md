# 设计说明

## 规范编码推导

严格解码的不变量是：接受的每个 `t` 都满足 `Encode(Decode(t)) == t`。

1. 显式 `1a`：解码为 `a`，再编码为 `a`，不等于 `1a`。
2. `0a`：次数 0 不产生符号，无法由任何输出编码出来。
3. 前导零（`01a`、`00a`）：规范十进制无此形式，再编码会删去前导零。
4. 同符号相邻（`2a3a`）：输出为 `aaaaa`，最长游程要求再编码为 `5a`。
5. `\x`：格式只允许转义数字或反斜杠；接受它会发明一种编码器不会产生的表示。
6. 末尾 `\`：缺少被转义符号，不是完整游程。
7. 末尾只有次数（`12`）：缺少符号，不是完整游程。
8. 非法 UTF-8：解码目标必须是合法 `string`，且编码器不会发出该字节序列。
9. 次数超过 `int64` 上限：见下节，超出格式允许的计数范围。

## 大次数取舍

选择把单游程次数限制为 `math.MaxInt64`。理由：输出长度必须可在本机用 `int` 安全表示；`Decode` 只在计数解析完成后计算 `n*utf8.RuneLen(sym)`，且先检查总输出上限，成功后才写入，因此不会预分配与巨大次数成比例的内存。超过上限返回可判定的溢出错误；默认输出上限为 64 MiB，流式解码器可配置。空输入正常产生空串。

## 语义与测试对照

待实现后补齐。
