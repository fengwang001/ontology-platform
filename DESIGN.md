# 设计推导

## 非法单元

在首字节处确定一个期望长度；只有字节数足够时才判断后续。先判定越界/代理，再判定后续字节；尾字节错误时只消费已存在的合法前缀并停止，所以 `F0 90 80 41` 吞掉 3 字节前缀，`41` 另成合法字符。首字节非法（`80..BF`、`C0 C1`、`F5..FF`）只吞 1 字节，因此 `80 80`、`E0 80 80`、`ED A0 80`、`C0 AF` 都是逐字节替换；`F4 90 80 80` 的第二字节已非法，`F4` 独立成单元，后续 3 字节也各成单元。

| 首字节 | 第二字节合法区间 | 非法时吞掉字节 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 仅首字节，或首字节+已有尾字节 |
| `E0` | `A0..BF` | 同上；第三字节也按尾字节规则 |
| `E1..EC` | `80..BF` | 同上 |
| `ED` | `80..9F` | 同上 |
| `EE..EF` | `80..BF` | 同上 |
| `F0` | `90..BF` | 同上；另检查第三、四字节 |
| `F1..F3` | `80..BF` | 同上 |
| `F4` | `80..8F` | 同上 |
| `80..BF` | 无（尾字节出现在开头） | 1 |
| `C0 C1 F5..FF` | 无（非最短/越界/非首字节） | 1 |

第二、三、四字节必须在 `80..BF`。任何非法后续字节都从其自身重新开始；若流在合法前缀后结束，前缀整体只产生一个单元，所以 `E2 82`+EOF 是一个 `FFFD`。

## 缓存与即时放弃

UTF-8 最长 4 字节，故任意时刻缓存硬上限为 3；UTF-16 单元为 2 字节，另需缓存可能的高代理，按输出前暂存 1 个 code unit。看到非法首字节可立即拒绝；长度足够但第二字节非法时，立即拒绝前缀，且该非法第二字节成为下一单元起点。第三、四字节同理。只有“首字节合法、已见尾字节也合法、但字节不足”才必须等待；EOF 时作为一个截断/非法前缀处理。

## 并行切点

每段读取从“切点前最多 3 字节”开始的窗口，但只发射起点落在本段 `[start,end)` 的单元；尾字节的归属跟随其单元起点。3 字节足以覆盖任何 UTF-8 前缀。若回看字节本身能作为新起点（ASCII 或新首字节），它及之后字节不再属于旧前缀。于是 `C3|80 80` 中跨切点的 `C3 80` 归前段；剩余开头 `80` 是独立非法单元。`F0 90|80 41` 会在前段窗口中看到完整非法 4 字节并归前段；`E2 82|` 末段在 EOF 得到一个截断前缀。各段覆盖窗口总重叠至多 `8*3`，无整段重扫。

## 超限断点

`Write` 每次先拼接上次 pending 与新字节；pending 不是“已消费输出”，只有形成完整单元才推进逻辑消费位置。若下一个完整标量放不下，立即停止并返回超限错误；返回的 n 只统计本次新数据中已并入完整输出单元的字节，pending 仍保留在转码器内。调用方把同一新缓冲区从 n 处继续传给新实例即可；调用方也可调用 `Pending()` 取残留自行续传。已输出结果、统计和错误偏移均以完整单元为边界，因此 UTF-16 不会留下半个代理对。
