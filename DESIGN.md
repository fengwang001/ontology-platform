# 批量导入：幂等与断点续传设计

## 1. 模型

- 批次 Batch：ID 非空；记录 = 位置 i∈[0,n) + 业务键 Key（允许空串）+ 载荷。同批次 Key 必须唯一，否则整批拒绝（报错含重复键与两处位置）。
- store：内存 map[key]value。Write 按业务键 upsert，天然幂等；带唯一键约束（重复写相同键返回原值/不产生第二条）；可注入第 k 条失败。
- progress：临时目录内 `<bid>.prog`，记录已确认区间 [0, high) 与 CRC32；另含占用标记（持有者 PID + 过期时间）。

## 2. 进度粒度的推导

写入是否幂等决定进度能否粗粒度：

- 若写入非幂等（append/自增），重放已写记录会产生重复，进度必须逐条 fsync，吞吐塌方。
- 本题 Write 按业务键 upsert，重放任意已写记录的结果与只写一次逐字节相同。因此进度可每 N 条（及结束时）粗粒度刷盘。
- 崩溃恢复：进度只承诺 [0,high) 已写；[high, 上次实际写入点) 状态不明。重放从 high 开始——可能重复写若干条，但因幂等而安全。
- 结论：正常路径每 N 条刷一次；崩溃后重放上限为 N 条（计数器 Replayed 可证）。

## 3. 四阶段

Prepare：校验批次（空 ID 拒绝；零记录合法；键重复拒绝）；获取占用标记（活跃则 ErrBusy，过期可接管）；读进度，不存在视为 0。
Write：从 high 起顺序 upsert；每 N 条刷进度；注入失败则停在失败点之前（high 严格小于失败位置）。
Verify：以**存储实际内容为准**——核对存储中归属本批的键是否覆盖清单前缀，得出实际连续前缀 actualHigh。进度只作提示：actualHigh 不得超过存储实证；进度声称 5 万而存储仅 3 万时回退到 3 万。随后从 actualHigh 重放至末尾。
Commit：全键齐备且逐字节一致后写 committed 标记；重复导入直接返回状态 Completed（非错误）。

## 4. 为何以存储为准而非以进度为准

进度与存储可能不一致（进度写成功但部分写入丢失/外部篡改：声称 5 万实际 3 万）。存储是数据唯一事实来源，进度仅是可丢失的缓存。相信进度会跳过缺失记录导致静默丢数据；故 Verify 用存储内容重算 high，不一致即回退重放，幂等写入保证结果不变。

## 5. 部分批次可见性

未 Commit 的已写键带在途归属标记（bid, committed=false）。reconcile 分三段：已写入区间（存储实际连续前缀）、缺口区间（清单应有而存储没有，精确到端点）、多余记录（存储中有而清单没有/属他批）。在途键不计入「已提交」统计。一遍扫描存储 + 一遍清单 + 归并，访问 ≤ 3n，O(n)。

## 6. 截断恢复

进度文件 = 定长头(含 high/体长度/CRC32) + 区间记录体。逐字节截断分类：短于头部 → 头部不完整；头部完整但体不足 → 区间记录不完整；长度够但 CRC 不符 → CRC 不匹配。三类均回退到「最大可恢复前缀」（头部可解析且被体证实的最后边界，否则 0），重放幂等，续传结果不变。

## 7. 资源约束

记录流式提供，内存同时驻留 ≤ 上限（峰值计数器断言）。续传不重头扫描：直接从 high 开始，Replayed ≤ N。
