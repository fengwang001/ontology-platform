# 可分裂二维网格索引 — 设计推导

## 1. 模型
- 点：`Point{ID uint64, X, Y float64}`；ID 唯一，故完全相同坐标的点也可区分、可删。
- 格：四叉树节点，矩形 `[x0,x1) × [y0,y1)`，左闭右开；根格默认 `[-1e9,1e9)`²。
- 叶子持有切片 `pts`；内部节点持有 4 个子格（左下/右下/左上/右上，记序 0..3）。
- 容量上限 `cap`：叶子点数超过 `cap` 时尝试分裂；深度上限 `maxDepth=32`。

## 2. 边界归属（左闭右开的唯一化）
对中点 `mx=(x0+x1)/2`、`my`，孩子选择：
- `x < mx` → 左列，`x >= mx` → 右列；`y < my` → 下行，`y >= my` → 上行。
于是分裂线（含过分裂线的角点）上的点只进右/上子格，全平面归属唯一、无缝无重。
格角点：`(x0,y0)` 属本格；`(x1,y1)` 不属本格（属右/上邻格）。
矩形查询同样左闭右开：点 `p` 命中 `r` 当且仅当 `r.X0<=p.X<r.X1 && r.Y0<=p.Y<r.Y1`。
故点恰在查询左/下边界命中，恰在右/上边界不命中。
矩形退化为线或点时 `X0>=X1` 或 `Y0>=Y1`，左闭右开区间为空，命中集必为空（合法）。
`-0.0 == +0.0`（IEEE754），天然视为同一点；`NaN`、`±Inf` 拒绝插入并计入 `Skipped`。

## 3. 分裂、重新归属与终止条件
插入后叶子 `len(pts) > cap` 则尝试分裂：
1. 深度已达 `maxDepth`，或 `mx==x0 || mx==x1 || my==y0 || my==y1`（中点在浮点上不可再分），
   则保持为超载叶子，不分裂；
2. 否则建 4 个子格，把 `pts` 全部重新下沉，清空自身点集。
为什么全同坐标会无限递归：所有点坐标相同时，每次下沉都落入同一个孩子，
该孩子插入后立即再次超载，若无终止条件则沿树无限分裂直至栈溢出。
终止依据：当深度到上限，或格子宽度在 float64 上已不可二分
（相邻浮点数间隔 `ulp`，`(x0+x1)/2` 会舍回端点），几何上已无法把重合点分开，
继续分裂毫无收益。此时叶子可持有任意多个点（超载是正确且有界的状态，深度 ≤ 32）。

## 4. 不合并的理由
删除后不合并子格。理由：① 合并需把四子格点重新上提并在再插入时重新分裂，
在阈值附近反复增删会产生分裂/合并抖动；② 查询剪枝只依赖树结构，超载/稀疏叶子
不影响正确性；③ 省略合并使“点总数不变、不丢不重”的不变量更易维持。

## 5. 范围查询与剪枝
DFS，每访问一个格计数 `cellsVisited`：
- 格矩形与查询相离 → 剪枝返回；
- 格矩形被查询完全包含 → 整取该格全部点（叶子直接追加，内部递归但不再逐点判定），
  `pointTests` 不增加；
- 仅相交 → 叶子逐点做左闭右开判定（每点计数），内部递归孩子。
完全包含的内部节点其后代也被包含，整取保证每点至多一次判定。

## 6. 并发
网格持 `sync.RWMutex`：查询取读锁可并发；插入/分裂/删除取写锁。
“查询进行中发生分裂”在锁语义下等价于查询看到分裂前或分裂后的一致快照，
移动的点不会漏：分裂在写锁内完成下沉后才提交可见。

## 7. 落盘格式（小端）
头：魔数 8B `"OGRDIDX1"` + 版本 1B + 容量 u32 + 最大深度 u32 +
跳过点数 u64 + 根界 x0,y0,x1,y1 各 f64 + 头 CRC32 u32（算前述全部字节）。
记录（先序遍历，仅写叶子；内部节点由恢复时的边界/层级关系重建不适用——
实际写每个叶子一条记录）：
标志 u8（bit0=叶子）+ 深度 u8 + 保留 u16 + x0,y0,x1,y1 f64 +
点数 u32 + n×(ID u64,X f64,Y f64) + 记录 CRC32 u32（CRC 覆盖该条此前所有字节）。
记录按深度优先、先序顺序写出叶子路径（每个内部节点先写一个 0 点内部标记，
再按 0..3 序递归），恢复时据此重建整棵树。
写盘：写 `name+".tmp"` 后 `fsync` 再 `Rename` 原子替换；启动时发现遗留 `.tmp` 即清理。
