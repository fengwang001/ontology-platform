# CSV 方言解析器设计

方言：逗号分隔；记录以 `\n` 或 `\r\n` 结束；引号字段内 `""` 为一个 `"`。位置：字节偏移从 0 起，记录号、字段号从 1 起。

## 1. 空行与单列空值

选择：**空行被跳过，不产生记录**。理由：末尾换行本就不产生记录，若把空行当作 `[未引号空]`，则 `a\n\n` 无法区分「记录 a 后一个空行」与「记录 a、记录空」，规范输入逐字节往返（不变量 5）不可能成立。跳过空行后：单列记录的唯一字段为空时，未引号写法 `\n` 会与空行无法区分而被丢弃，因此回写器**必须写成 `""`**（带引号空字段）。引号只在下列情形必要：字段含 `,`、`"`、`\r`、`\n`；或字段为空且所在表只有一列（空 unquoted 会被读作跳过的空行）。此外，为保证 `Parse(Write(T))` 引号标记相同，原本带引号的字段一律保留引号。规范输入定义：无空行；行尾仅 `\n`；记录均非空；引号仅出现在必要位置或单列表空字段写成 `""`。规范输入满足 `Write(Parse(x))==x`。

## 2. par 切点

每段 worker 无法独立知道入口是否在引号内，故对同一段**按两个假设各解析一次**：H0=起点在引号外（fieldStart），H1=起点在引号内的普通引号字段中。两段扫描各做一遍，故处理次数 ≤ 2N+2K。从左到右拼接：前一段真实结束状态决定下一段入口假设（out/bare→H0；in→H1；pending CR 由切点重叠 1 字节消解，见第 3 节）。段在引号字段中结束时，段尾未闭合字段是该字段的延续，与下一段 H1 的字段体拼接（含偏移与值；`""` 只在真实相邻处成对折，切点恰在 `""` 两字节间时，两侧各保留一个 `"` 原样拼接即可，无需特判）。拼接时：事件局部偏移加段起点；记录号加前段已完整记录数；字段号沿未闭合字段延续。错误取第一个按全局坐标最小者（错误令解析终止），其坐标同样加段起点与记录基数。

## 3. CR 待定

流式状态机含 pending-CR 状态：引号外字段末尾见到 `\r` 后，下一字节是 `\n`→行尾；是其他字节→错误（孤立 CR）。半包：CR 留在 lexer 状态里跨 Feed；流在 `\r` 处结束（Close）→报孤立 CR。par：切点可能落在 `\r\n` 之间。令每段解析的字节范围为 `[start,end+1)`（末段除外），即**向右多看 1 字节**作为重叠；拼接只采用落在本区间 `[start,end)` 的事件与字节计数，多看字节产生的事件（如行尾或错误）丢弃或推迟到右段。这样 pending-CR 永不需要跨段传递。同理，切点落在引号内 `"` 与下一字节之间（quotePending）也由这 1 字节重叠消解：状态判定只用于决定下一段入口假设，事件归属按区间裁剪。

## 4. 字段上限「立刻」

维护当前字段**值字节计数**（引号内 `""` 折叠成一个 `"`，故折叠后的 1 字节计 1）；同时维护原始跨度用于偏移。每接收一个应计入值的字节（引号外任意非分隔字节、引号内普通字节、`""` 折叠出的一个 `"`）后立即 `count+1` 并与上限比较，超限立即返回错误，不等待记录结束。字段数在每个逗号发出新字段时检查；记录数在每条完整记录（含末条无换行记录）加入表时检查。进入终态后 Feed/Close 返回同一错误。

## 5. 状态转移表

字节类别：c=逗号，q=`"`，n=`\n`，r=`\r`，o=其他。状态：S=字段开始，B=未引号字段中，Q=引号字段中，P=引号中刚见引号（quotePending），R=未引号末尾 CR 待定。emit=发字段；rec=发记录；err=错误类型（QuoteInBare 未引号中 `"`；CharsAfterQuote 闭合后杂字符；UnterminatedQuote；LoneCR）。

| 状态 | c | q | n | r | o |
|---|---|---|---|---|---|
| S | emit空,S | →Q | rec,S | →R | 字节入值,→B |
| B | emit,S | err QuoteInBare | rec,S | →R | 入值,B |
| Q | 入值,Q | →P | 入值,Q | 入值,Q | 入值,Q |
| P | emit,S | 入值 `"`,→Q | rec,S | rec(CRLF),S | err CharsAfterQuote |
| R | err LoneCR | err LoneCR | rec(CRLF),S | err LoneCR | err LoneCR |

Close 时：Q/P→UnterminatedQuote；R→LoneCR；S 为纯空行（跳过）；B 或 P 已 emit 的挂起字段→末记录无换行，合法。每字节恰好处理一次；跨 Feed 仅靠状态与挂起值续传。
