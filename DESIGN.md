# 设计：随机超平面 LSH 分桶近似最近邻检索器

## 1. 签名规则

固定维度 `d`。第 `l` 张表有 `b` 个随机单位超平面法向量 `h_{l,k}`，分量由
显式种子经 `math/rand/v2`（`NewPCG(seed, seed)`）按 `(l,k,i)` 顺序独立采样
标准正态并归一化得到。向量 `x` 的第 `k` 位：

    s_k = 1 若 <x, h_{l,k}> > 0；否则（含内积恰为 0）s_k = 0

内积为 0 固定归到 0 位：规则确定、可复现，且不影响“近向量大概率同位”的
性质（零向量与任意向量在此位只以先验概率一致，已在召回推导中体现）。
`b` 位打包成整数签名，`b<=64`。

## 2. 位数 b、表数 L 与候选数、召回的关系

余弦角为 θ 的两点，单次投影同号概率 `p = 1 - θ/π`。
在一张表的 b 位全部相同的概率 `p^b`（签名桶碰撞）。
- 单表命中概率：`P1(b) = p^b`。
- L 张独立表至少进一张候选集：`P(b,L) = 1 - (1 - p^b)^L`。
- 单桶期望候选数：`N * 2^-b`（签名近似均匀）；去重后候选 `≈ N(1-(1-2^-b)^L)`。

b 增大 → 桶变细 → 候选数单调下降，但近邻的 `p^b` 下降、单表召回下降；
L 增大 → 同一近邻被任一表捕获概率单调不降（趋向 1），候选数随 L 上升。
故候选/召回由 `L/2^b` 共同决定：增大 L 买回召回，增大 b 压低候选。

## 3. 可复现性

种子唯一决定 RNG 序列；超平面以 `(l,k,i)` 固定顺序生成，无 goroutine 写入
顺序依赖。同种子生成的法向量与每个签名逐字节相同；不同种子序列不同，签名
存在差异（概率 1）。

## 4. 精排与计数

候选 = 各表同签名桶 ID 的并集；对候选按欧氏距离平方精排取 TopK。
- `hashCalls`：建索引时每向量每张表算 b 个内积，恰好 `n*L*b`。
- `distCalls`：仅对去重后候选计算距离。

## 5. 落盘格式与恢复

小端；头 `magic(4)="LSH1"|version u16|d u32|b u32|L u32|nValid u32|
nSkip u32|bucketCount u32`；超平面段长度前缀 + `d*b*L` 个 float64；
有效向量段长度前缀 + `(id u32, d 个 float64)`；桶段 = bucketCount 个
`(table u32,sig u64,count u32,id...)`；末尾 CRC32（IEEE，覆盖此前全部字节）。
截断按字节位置归类：头不完整 / 超平面段不完整 / 桶表不完整 /（内容完整但
CRC 不符）CRC 不匹配。恢复：头+超平面完整即可读入，桶段按可解析单元取
最大前缀，剔除引用不存在 ID 的条目并计数，恢复结果无悬挂 ID。
全零法向量是退化超平面，读回逐 `(table,bit)` 检出。

## 6. 并发与边界

索引不可变；建索引在私有结构完成后原子替换可见指针，查询只见整组旧表或
新表。NaN/±Inf 向量拒绝并计入 skip；零向量合法（签名按第 1 节为全 0）；
维度不符、K 越界等返回可 `errors.Is` 判定错误。
