# 带背压的流式聚合管线 — 设计

## 1. 链路与包划分

`source`(可注入脚本数据源) → `stage`(parse 阶段) → `stage`(聚合阶段，逻辑在 `pipeline`) →
`stage`(sink 阶段) → `sink`(落盘)；`ckpt` 负责位置与中间聚合的持久化。仅标准库，状态在内存。

帧 `Frame{Seq, Offset int64, Raw []byte}`；记录 `Record{Key string, Val int64}`（行格式 `key:number`）。
每 E 条帧在聚合器边界注入 `Barrier{Pos}`，Pos 为 source 字节偏移。

## 2. 核心推导：「已消费位置」记到哪

管线中有三级有界队列，一条帧可能已被 source 读出、在队列中、或已参与聚合但尚未落盘。

- **取 source 读取位置 —— 错（丢）**：该位置之前可能有帧仍在途（队列/聚合内存里），
  崩溃后从该位置重放，这些帧既未进输出也未进检查点，永久丢失。
- **取 sink「已写出的最后一条」位置 —— 错（重/无法定义）**：sink 写出的是**整组聚合快照**，
  不与单条帧一一对应；且「写输出文件」与「写检查点」是两个动作，中间存在崩溃窗口：
  输出已新、检查点仍旧时，单看 sink 位置无法判断该从哪重放，重放会重复计入。

**正确定义**：已消费位置 = 最后一个**完整提交屏障**的位置 P。屏障要求其之前的全部帧
「聚合结果已持久进输出文件」**且**「位置与聚合已持久进检查点」两件事都完成。

提交协议（崩溃安全顺序）：
1. sink 把截止 P 的**全量聚合快照**写临时文件 → fsync → 原子 rename；
2. 成功后 ckpt 原子写 `{P, snapshot}`（两代轮换 + 校验和）。

提交协议保证：存在检查点 P ⇒ 输出文件一定已包含 `[0,P)` 的全部效果。

**恢复**：读检查点 `{P,S}`，source seek 到 P，只重放 `(P,N)`。
输出文件是全量快照**覆盖式**写出，重放天然幂等；分组按 key 排序，输出逐字节确定。
崩溃窗口：rename 前崩 → 旧输出 + 旧 ckpt，从 P 重放，tmp 残留启动时清理；
ckpt 写一半 → 校验和失败，回退上一代完好检查点。故不丢、不重。

## 3. 背压与资源约束

每级 stage = 有界 channel（容量 C）+ 同容量信号量：投递前取令牌，下游处理完才归还。
全管线在途记录数 ≤ 三级容量之和；`stage` 用非导出原子量记录历史最大值 `maxInFlight`，
`source` 投递阻塞次数 `Blocks` 可读出。组数量超 `maxGroups` 返回 `ErrTooManyGroups` 并停止。

## 4. 故障语义

- 坏记录（无冒号/数字非法/空 key 之外的缺键）：`Bad` 计数，不进任何分组，管线继续；空 key 合法。
- source 中途报错 / sink 报错：已提交屏障的部分保证落盘并写检查点，错误如实返回。
- sink 临时文件：固定名 `输出名 + .tmp`，启动恢复时识别并删除，绝不作为有效输出读回。
- 检查点两代 `ckpt.1`（新）/`ckpt.0`（旧），任一损坏则回退另一代，报告标记 `FellBack`。

## 5. 并发与优雅停止

各阶段独立 goroutine，经 context 联动；Start 前到达 Stop 则空跑结束；运行中 Stop 取消上下文、
把在途帧排空到最近屏障后退出；`Stop` 用 `sync.Once` 幂等。
