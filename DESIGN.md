# 属性级权限过滤器 — 设计推导

范围：按角色授予列可见性；当过滤谓词引用不可见列时如何处置；行裁剪与可审计报告。
不实现存储、索引、查询解析、HTTP、认证。状态全部在进程内存。

## 一、不可见列出现在谓词里：三种做法的泄露分析

设角色 R 可见 `{id, name}`，不可见 `secret`。攻击者已知列名（列名是调用方自己
写进查询的，不是秘密），想推出 `secret` 的**值**或**存在性**。

### 甲：不可见列的比较当作 false —— 泄露

探针：`secret = 1` 返回空，`NOT (secret = 1)` 返回全表。
两个互补谓词的结果不再互补（一个空、一个全集），攻击者据此即可确认「存在一个
被隐藏的列且结果被篡改」，并能区分「列不存在」与「列被隐藏」。更严重的是语义
被证伪：返回的行不再满足谓词，调用方会基于错误前提继续推理——例如用
`NOT (secret = 1)` 命中的行做统计分母，反推 `secret` 的分布。故甲泄露。

### 乙：不可见列当作 NULL 参与三值逻辑 —— 泄露

探针：`secret IS NULL` 恒为真，返回全表；`secret IS NOT NULL` 恒空。
`IS NULL` 直接成为存在性探针：攻击者确认该列存在且被隐藏。同时三值逻辑下
`secret = 1` 恒 UNKNOWN，与「列里全是真 NULL」无法区分，「不可见」与「值为空」
被混为一谈，下游任何基于空值的统计都被污染。故乙泄露。

### 丙：整条查询拒绝执行并指出列名 —— 不泄露（本题采用）

错误内容只含调用方自己提供的标识符（列名 + 谓词树路径），响应是**输入的函数、
不是数据的函数**：无论 `secret` 真实取值如何、是否存在该列的数据，响应完全
相同。因此不存在数据依赖的侧信道。论证：一个判定器若对所有可能的数据库状态
给出同一输出，则其输出关于数据库状态的信息量为零。甲、乙的输出随「列的真实
内容/存在性」变化，丙不随其变化（只随查询文本变化），故只有丙不泄露。

## 二、OR 短路例外的定义与理由

定义（求值模型）：谓词按**从左到右短路求值**进行常量折叠——

- `OR`：左支折叠为真 ⇒ 整体为真，右支不被读取；左支折叠为假 ⇒ 丢弃左支；
- `AND`：左支折叠为假 ⇒ 整体为假，右支不被读取；左支折叠为真 ⇒ 丢弃左支；
- `NOT`、比较：正常递归；只含常量的子树整体折叠。

「读取」的定义：求值到达该比较节点。若不可见列所在的子树在常量折叠后**从未被
求值到达**（如 `OR(TRUE, secret = 1)`、`OR(AND(FALSE, secret = 1), id = 5)` 中
的 `secret`），则该列实际未被读取，查询**放行**；否则**拒绝**并指名列与路径。

理由：(1) 信息论上，未被读取的列不可能泄露——查询结果完全由常量与可见列决定，
与 `secret` 的任何取值无关；(2) 该模型与真实执行器的读取行为一一对应，检查器
是执行器的忠实模拟，拒绝集合 = 执行时会真正触碰不可见列的查询集合，无假阳性
也无漏检；(3) 位置敏感性（`OR(secret=1, TRUE)` 仍拒绝）是短路求值的自然结果，
已在本文档明示，行为可预测、可测试。

## 三、行裁剪与报告

- 行裁剪：结果行只保留可见列；不可见列**从 map 中删除**（不置零值/NULL），
  使「不可见」与「值为空」可区分（用 `_, ok := row[col]` 判定）。
- 报告：被裁列名集合（字典序）、被拒谓词引用（列名 + 全部路径，同列只报一次）、
  是否整条拒绝。构造时排序去重，保证与构造顺序无关、逐字节确定。

## 四、复杂度约束

- 谓词检查是一遍从左到右的遍历：`filter.Engine` 内非导出计数器记录访问过的
  节点数；对无短路发生的谓词，访问数 == 节点总数（`predicate.Count`）。
- 行裁剪只遍历可见列集合做 map 查找与拷贝，拷贝次数 == 可见且存在的列数，
  与总行数列数无关；1000 列 5 可见时拷贝 ≤ 4*5 = 20。

## 五、错误分类（errors.Is 可区分）

- `policy.ErrUnknownRole`：角色无策略条目；
- `filter.ErrInvisibleColumn`：谓词引用不可见列（携带列名与全部路径）；
- `predicate.ErrInvalid`：谓词本身非法（空列名、未知算符、nil 子节点）。

## 六、穷举结论定义（32 组合）

模板：`=`: `a=1`；`NOT`: `NOT(a=1)`；`AND`: `(a=1)AND(b=2)AND(c=3)`；
`OR`: `(a=1)OR(b=2)OR(c=3)`。可见性 8 组合（a/b/c 各自可见与否）。
结论规则：拒绝 ⟺ 模板**引用到**的列中存在不可见列（模板不含常量，无短路）。
即 `=`/`NOT`：仅 a 不可见时拒绝（4 放 4 拒）；`AND`/`OR`：全可见才放行（1 放 7 拒）。
