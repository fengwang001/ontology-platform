# 设计推导

## 1. `x/**` 与边界

段按字节 `/` 分割，结果保留空段；绝不规范化。`**` 只在单独占一段时表示“零个或多个完整段”。
因此模式前缀 `x/` 必须先消耗字面 `x/`，随后 `**` 可消耗零段：

- `x/**` 不匹配 `x`（缺少前缀中的 `/`）；匹配 `x/`（零段，后随空段）；匹配 `x/y`、`x/y/z`。
- `**/x` 匹配 `x`（零段，零段后允许直接进入 `x`，不要求额外 `/`）；不匹配 `/x`（首段为空，无法由整段 `**` 生成）。
- `a/**/b` 匹配 `a/b`，是因为同一零段规则使 `a/` 后的 `**/` 折叠为空；同一规则也推出以上结论。
- 与普通字符相邻的 `**` 只是普通 `*`，且 `*` 不跨 `/`。

## 2. 空串路径

空路径没有任何码点，也没有任何段。

- 空模式匹配空串：两者段序列同为一个空段。
- `?` 需要恰好一个码点，不匹配。
- `*` 可匹配零个码点，因此匹配空串；它仍受段边界限制，空串中没有 `/`，不产生冲突。
- 独立段 `**` 匹配零个完整段，因此也匹配空串；模式 `**/x` 则还要求字面 `x`，不匹配空串。

匹配表：行是模式，列是路径，`+` 为匹配。

| 模式 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | - | - | + | - | + | - | - |
| `**/x` | - | + | - | - | - | - | - |
| `*` | + | + | - | - | - | - | - |
| `**` | + | + | + | + | + | + | + |
| `a/**/b` | - | - | - | - | - | + | + |

## 3. 无回溯算法

段内把通配项与字面项编成有序原子，使用 NFA 位集模拟：状态表示“已提交到某原子之后”。每读入路径的一个码点，先推进零宽的 `*` 闭包，再用当前码点消费 `?`、字符类或字面量；`*` 下一位置闭包中同时保留和前进。每个码点只扫描原子列表固定次数，所以段内为 O(原子数×码点数)，且天然包含经典“只记录最后一个 `*` 回退点”的所有状态：较早星号能到达的位置，一定被较晚星号的零宽闭包覆盖；较晚星号不会要求更少的字面提交，故不会产生假阳性。

该论证不能照搬到跨段 `**`：段边界 `/` 是必须显式存在的字符。跨一个段要同时消费 `/`、空段内容和下一个 `/` 的对齐方式不同，旧星号位置记录的是码点回退点，不能表达“在哪些段边界之后对齐”。因此跨段同样使用段级 NFA 位集：状态为已匹配的模式段；普通段用段内匹配器一次性判断，`**` 段在段闭包中可停留或越过下一段边界。复杂度为 O(模式段数×路径段数)，内部再乘段内原子×码点，总体 O(模式长度×路径长度)，没有递归或指数分支。

## 4. 规则集版本一致

所有新规则先在临时数据上完整编译；任一规则超限或语法错误，立即返回错误且不触碰当前版本。全部成功后用一次 `atomic.Pointer[compiledSet]` 替换。查询开始时读取一次指针，之后只使用该快照；因此结论、命中序号、原文必属同一版本，替换也与正在进行的查询无数据竞争。
