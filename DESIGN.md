# 设计推导

模型：字节流按状态机处理。`eol` 负责 `\r` 的待定（下个字节是 `\n` 则合并，否则立即判为行尾）；`ws` 缓冲行尾待定的空格/制表符，见到行尾丢弃、见到非空白原样吐出。`span` 只记录“原文区间→输出区间”的成对端点（seg：origStart/origLen,outStart/outLen），删除即两 seg 间的空隙，合成字节（Ensure 补的 `\n`）origLen=0。seg 数量只随删除点增长。

## 1. 空输入与末尾换行策略

三策略：Keep=规范化主体后不动末尾；Ensure=末尾恰一个 `\n`；Trim=删掉全部末尾空行再留一个 `\n`。
主体规范化对 `\n\n`、`  \r\n` 已得 `\n\n`、`\n`，且空串得空串。Ensure 对空输入我选输出 `""` 而非 `\n`：幂等要求 `N(N(x))=N(x)`，若 `N("")="\n"` 虽也自洽，但它对“无内容”凭空造字节，且与 Trim 对空输入的结果冲突；选 `""` 后三种策略对空输入一致，Ensure(`""`) 作用于自身仍为 `""`，幂等成立。由本规范器输出的任何文本必以单个 `\n` 或非空白字节结尾或为空，故 Ensure/Trim 再施加一次结果不变（Trim 删除的“末尾空行”只可能由输入侧连续 `\n` 产生，规范输出不会再产生）。在本规范器定义域内 Trim 与 Ensure 输出相同；差异仅在概念（Trim 声明会压缩既有末尾空行，而主体步骤已保证这一点）。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

不变量：`ToOut` 单调不减；对任意输出偏移 o，`ToOut(ToOrig(o))=o`。`ToOrig(o)` 取覆盖 o 的 seg 的对应端点（seg 内 `orig=o-outStart+origStart`），故输出每个 o 必须落在某 seg 覆盖范围，seg 覆盖在输出轴上无缝。被删原文字节不对应任何输出位置，其 `ToOut(i)` 只能取删除空隙之后第一个输出位置（即后续第一个 seg 的 outStart；空隙在流末则取 lenOut）。这样单调不减成立：空隙内及边界左沿都等于同一值，之后随原文严格递增。光标语义：被删的行尾空白、`\r\n` 中的 `\r` 都映射到该行 `\n` 的**之前边界**（即该 `\n` 的偏移），编辑器把光标放到被删空白处时落在换行符前（行尾逻辑位置），而不是下一行。

## 3. 缓冲超限

待定空白必须缓冲，因为后续字节决定它是行尾（删）还是行内（留）。两个选项：拒绝（返回带原文偏移的可判定错误，已输出保留，进入终态）；强制判为“非行尾”吐出。后者一旦后续真来行尾，这些空白本应删除却已输出，直接违反语义 2、5（输出含行尾空白、二次结果不同）与映射互逆；为满足幂等只能回滚重写，流式接口下已被取走的输出无法收回。故选择**拒绝**：错误类型携带起始原文偏移，终态后任何 Write/Close 返回同一终态错误。这不违反 4（任何切分方式在同一上限下同一点触发同一错误）、5、6（错误前的输出与映射仍满足全部不变量）。

## 4. par 拼接

每段独立规范化的盲区只有段交界：前段末尾可能有 (a) 待定 `\r`、(b) 一串待定空白、(c) 空白后再跟待定 `\r`。做法：段处理器不在段末施加尾部策略，而是产出 out、span 与 `resid`（未判定尾巴：空白字节及其原文起点，以及可能的 1 个 `\r`）。拼接时把 resid 前缀拼到下一段原始输入重跑，丢弃该段独立结果里对应的前缀部分——因为修正只涉及交界，且修正范围有上界：resid ≤ 1 个 `\r` + 连续空白串，重跑状态机一旦吃下一个非空白/非 `\r` 字节即与段独立结果逐字节汇合（有界同步）。实现上等价地携带一个有界重叠窗口（前段末尾 resid 与后段首部），在窗口内用全局状态顺序重算并替换两段交界 seg。映射随 seg 端点平移：`origStart += baseIn`、`outStart += baseOut`。最后一段才施加末尾换行策略。K∈1..8、任意切点因此与单流逐字节相同。
