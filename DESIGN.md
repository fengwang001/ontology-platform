# 设计说明：带转置的编辑距离与对齐脚本

## 1. 无限制转置的递推（推导）

采用 Lowrance–Wagner 的**无限制 Damerau–Levenshtein**距离。串先按码点解码为
`a[1..m]`、`b[1..n]`。`D[i][j]` 表示 `a[1..i] → b[1..j]` 的最小代价：

```
D[i][0]=i  D[0][j]=j
D[i][j] = min(
  D[i-1][j-1] + [a[i]!=b[j]],   // 匹配/替换
  D[i-1][j] + 1,                // 删除
  D[i][j-1] + 1)                // 插入
若 k=lastA[b[j]]>0, l=lastB[a[i]]>0：
  D[k-1][l-1] + (i-k-1) + (j-l-1) + 1   // 转置候选
```

其中 `k` 是 b[j] 在 a 中上次出现位置，`l` 是 a[i] 在 b 中上次出现位置。转置项的
含义：把 `a[k](=b[j])` 与 `a[i](=b[l])` 交换到对齐位置，中间 `i-k-1` 个 a 字符、
`j-l-1` 个 b 字符先删除/插入。**同一段子串在转置前后都可继续编辑**，因为转置项
从更左上的 `D[k-1][l-1]` 起跳，且中间的删除/插入使转置后区域不再被锁定。

**常见错误递推**（最优串对齐 OSA / restricted Damerau–Levenshtein）：
`D[i][j]=min(..., a[i-1]==b[j] && a[i]==b[j-1] ? D[i-2][j-2]+1 : ∞)`。
它只看紧邻的一对，且转置发生在所有其他编辑的"外层"，被转置的两格无法再编辑。

**`CA → ABC` 逐格差异**（m=2,n=3；OSA 得 3，无限制得 2）：

- i=2,j=2（`CA→AB`）两法均得 2（替换 C→A、A→B）。
- i=2,j=3：OSA 检查 `a[1]=C` 是否等于 `b[3]=C`？要求 `a[2]=A==b[2]=B`，
  为假，转置项不可用，故 OSA 给 `min(D1,2+1, D2,2+1, D1,2+1)=3`。
- 无限制法：b[3]='C' 在 a 中上次出现在 k=1；a[2]='A' 在 b 中上次出现在 l=1。
  转置项 = `D[0][0] + (2-1-1) + (3-1-1) + 1 = 0+0+1+1 = 2`。
  即：先把 A 与 C 转置（CA→AC，此时"CA 段"仍可继续编辑），再在 A 后插入 B
  得 ABC。OSA 之所以错，是因为它强制转置必须是作用于该子问题的最后一步且
  只能紧邻换位，无法表达"先转置、后插入"。

## 2. 码点与非法 UTF-8

自行逐字节按 RFC 3629 前缀解码（不依赖 `utf8.DecodeRune` 的归并行为）：合法
序列解码为正常 rune；**每个**非法字节单独产出一个私有哨兵 rune
`0x1000000 + 字节值`（超出 Unicode 范围，绝不与真实码点或 U+FFFD 碰撞），
因此两个不同非法字节（`\xff`、`\xfe`）是不同码点，距离为 1；合法 U+FFFD 仍是
正常码点 U+FFFD。

## 3. 复杂度与"上次出现位置"索引

"上次出现位置"是两个 `map[rune]int`（分别按 a、b 中已见码点为键），容量只随
输入中**实际出现的不同码点数**增长，与字母表总大小无关；不使用任何定长数组。
单元格计数 `cellsFilled` 为非导出计数器；每格 O(1)，总时间 O(mn)，内存 O(mn)。

## 4. 度量性质

Lowrance–Wagner 距离是 1/1/1/1 代价下的度量：正定性（空脚本仅当两串相同）、
对称性（交换两串时转置项对称，k/l 同时互换）、三角不等式（把两份最优脚本在
中间串上拼接即可得到上界脚本，代价为两段之和）。

## 5. 回溯与并列选择规则

回溯时按固定优先级判定来源，优先级为：
**匹配 > 替换 > 删除 > 插入 > 转置**。
即先判等值匹配（代价 0），再按顺序比较四种代价 1 的来源，取第一个达到当前
`D[i][j]` 的来源。该规则全局固定，故同一输入 100 次结果逐项一致。

## 6. 脚本操作

操作类型：`Match`、`Replace`、`Delete`、`Insert`、`Transpose`。转置携带当时
相邻的两个码点；回溯到转置来源时，中间字符生成 `Delete`（光标后删）与
`Insert`，`Apply` 在可变码点切片上按光标执行，转置即交换当前两个相邻码点。

## 7. 上限错误

`dist.Distance(a, b, maxProduct)`：先比较 `码点数乘积`，超限立即返回
`dist.ErrLimitExceeded`（哨兵错误），发生在任何 DP 分配之前。

## 8. 语义 → 测试函数对照表

<!-- TABLE -->

