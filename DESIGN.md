# 严格模式流式 Base64 设计

## 规范尾部的推导（语义 2）

把一个 4 字符组（quantum）里的 4 个 6-bit 值记为 a,b,c,d，拼接成 24 bit：
`aaaaaabb bbbbcccc ccdddddd`。三组输出字节数分别为 3、2、1。

编码时不足 3 字节的尾部先补 0 bit 再分组：
- 1 字节（形如 `QQ==`）：只用到 a 的 6 bit 与 b 的高 2 bit；b 的低 4 bit 是补位。
- 2 字节（形如 `QUI=`）：用到 a,b 全部 12 bit 与 c 的高 4 bit；c 的低 2 bit 是补位。

统一规则：**解码时，承载输出字节之外的补位 bit 必须全为 0；且填充符 `=`
只能是 0 个、2 个（`xx==`）或 1 个（`xxx=`）**。

判定所用的 bit：1 字节尾部查 `b & 0b1111 == 0`；2 字节尾部查 `c & 0b11 == 0`。

`QR==` 中 Q=16(a=010000)，R=17(b=010001)，低 4 bit 非 0，故拒绝；它携带的
8 bit 与 `QQ==` 完全相同，只是补位 bit 被塞进了信息。若接受它，则同一字节串
`A` 会对应两种编码，破坏「任意字节串的编码结果唯一」，严格模式因此拒绝。

## 测试对照表

| 语义 | 测试函数 |
|---|---|
| 1 标准字母表/填充/MIME 行 | （后补） |
| 2 规范尾部样例 | （后补） |
| 3 换行位置 | （后补） |
| 4 五类错误可区分+偏移 | （后补） |
| 5 跨切分点一致 | （后补） |
| 6 往返 | （后补） |
| 7 输出上限 | （后补） |
| 计数器 == 输入字节数 | （后补） |
