# 并行任务图调度与失败传播 — 设计推导

## 1. 目标与边界

输入：有向无环图（边 `prereq -> task`，前置完成后 task 才可开始）、每个任务的执行函数、并发上限、
模式（快速失败 / 尽力而为）。输出：每个任务唯一终态 + 原因的确定性报告。仅标准库，状态在进程内存。

包划分（无环依赖）：`graph`（图结构/环检测/分层）；`sched`（并发闸与就绪队列）；
`fail`（状态枚举/原因追踪/取消）；`report`（确定性文本）；`exec`（引擎，组合以上四包）；`cmd/demo`。

## 2. 四类终态的判定推导

设任务生命周期只有 未开始 / 运行中 / 已结束 三态；结束原因再分类。判定按“结束时刻”与“是否真的开始过”：

1. **成功 Success**：函数返回 nil。终态一旦写入即冻结，任何后续取消/迟到结果不得改写。
2. **失败 Failed**：函数返回 error，或函数 panic（在任务 goroutine 内 recover，包装为
   `PanicError`，错误可被 `errors.As` 识别并保留原始信息）。失败任务记录其原始错误。
3. **被跳过 Skipped**：任务**从未开始**，因为它（经任意路径）依赖某个失败任务。
   跳过原因必须指向“根失败任务”——沿依赖边向上找，取该任务所依赖的**失败节点中按 ID 字典序最小者**
   （确定性规则）。因此 `A→B→C` 中 A 失败时，C 的原因指向 A，而非直接上游 B。
4. **被取消 Canceled**：失败信号到达的瞬间该任务**已经在运行中**（属于失败任务的旁支）。
   区分依据是“有没有被调度器真正启动过”：取消是运行时对在跑任务的 ctx 撤销；跳过是入队前的静态阻断。
   被取消任务即使 ctx 已撤销仍返回结果，该结果一律丢弃，终态保持 Canceled。

任务是否开始过以“引擎是否从就绪队列取出并放入运行槽”为准；这使 Skipped 与 Canceled 互斥可枚举。

## 3. 两种模式语义

- **FastFail（默认，快速失败）**：首个失败落定即取消整个引擎（ctx cancel）。
  所有在跑任务 → Canceled；所有尚未开始的任务（含与失败无关的旁支）→ Skipped，原因指向根失败。
- **BestEffort（尽力而为）**：不取消 ctx。只跳过“能到达失败任务”的下游；与失败无关的分支继续跑完，
  可成功也可自己失败。某任务同时依赖多个失败任务时，根失败取失败祖先中 ID 最小者（各自最早上游的确定化解）。

两种模式下“成功/失败/取消/跳过”四类均可出现与区分；差异只在旁支是否被取消与无关分支是否继续。

## 4. 并发、复杂度与无泄漏

调度不用“每完成一个任务全图扫描”：维护每个节点的 `剩余未完成前置数`（Kahn 入度），
任务完成时只遍历其**出边**更新后继。就绪判定（某后继是否可入队）每条边恰好发生一次，加节点入队一次，
总计 ≤ V+E，远小于要求的 `4(V+E)`。并发由带容量 semaphore 的闸（`sched.Limiter`）保证同时运行 ≤ 上限；
计数器记录历史峰值。队列在多个任务同时就绪时按 ID 升序弹出（小顶堆/有序插入），保证决策确定。

每个任务恰好一个 goroutine；引擎 `Wait` 等待全部 goroutine 退出（包括被取消后才返回的慢任务），
因此返回时所有任务 goroutine 都已结束，无泄漏，可用 `runtime.NumGoroutine` 回到基线验证。

## 5. 确定性

完成顺序天然不确定，但：终态写入加锁；根原因用 ID 最小的确定规则；报告按 ID 字典序渲染，
故同图同函数的报告字节恒定。图边的插入顺序只影响邻接表，不影响结果（环检测路径也做规范化）。

## 6. 环与异常输入

执行前 `graph.Validate`：未知依赖 → `ErrUnknownDep`；有环（含自环）→ `CycleError`，
携带一条真实闭合路径（相邻 ID 对都是输入边，首尾相同），用 DFS 着色得到，路径节点排序后取最小以确定。
重复加同一条边幂等（入度与邻接不重复计数）。空图合法，直接产生空报告；链式 1000 层用迭代 Kahn，无递归栈风险。

## 7. 错误与状态的可区分性

`fail.Status` 枚举 + `errors.Is`：`ErrUnknownDep`；`errors.As`：`CycleError`（带 Path）、
`PanicError`（带原 panic 值与任务 ID）。报告每行：`taskID status reason`（reason 为根任务 ID 或错误文本）。
