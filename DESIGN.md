# Design

路径先按字节 `/` 切段，段是 `/` 之间的码点序列；`""` 路径有 0 个段，`"x/"` 是两段（第二段空）。

推导 1：`**` 语义与 `x/**`、`**/x` 边界。
定义：只有整段恰为 `**` 时它是「跨段星号段」D，展开为 0..n 个完整段（n≥0），
其中 n=0 时 `**` 连同相邻的 `/` 一起被吸收。由此：
- `x/**`：0 段 => `x`（吸收尾部斜杠，故不匹配 `x/`，后者是 `x`+空段）；n≥1 => `x/y`、`x/y/z`。
- `**/x`：0 段 => `x`；n≥1 => `a/x`、`a/b/x`。`/x` 以 `/` 开头，前面是一个空前导段，n=1 即 `""+/+x`，故匹配。
- `a/**/b`：0 段 => 吸收中间斜杠得 `a/b`；n≥1 => `a/x/b`、`a/x/y/b`。三者同一条「0..n 个整段」规则。

推导 2：空串路径。
`*` 是「段内」星号，只能在一个已存在的段内匹配码点；空路径没有任何段（连空段都没有），故 `*` 不匹配 `""`。
单独模式 `**` 是 0..n 个整段，n=0 合法，故 `**` 匹配 `""`；它也匹配 `"x/"`（两段：x、空）等任意路径。
`?` 需要一个段内码点，不匹配 `""`。空模式只匹配空串。

推导 3：无回溯算法。
段内：模式是字面量/`?`/字符类（每原子吃 1 码点）与 `*`（吃段内任意码点序列）。维护「当前可达的原子位置集合」
（布尔切片）。每读入一个码点，从可达位置走字符转移，再对落在 `*` 上的位置做闭包传播（`*` 可空移到下一原子，
也可自环）。每个码点只扫描原子表一次：O(段内原子×段内码点)，无重放、无指数回溯。
「只记最后一个 `*` 的回退点」正确的原因：旧星号重放能产生的每一个候选切分，最后一个 `*` 都能产生（它的区间
更靠右、剩余后缀更短），前缀 `p[0:lastStar)` 已被固定确认；等价地贪心失配时只把最后星号的终点右移一格即可。
该论证不能照搬到 `**`：跨段星号的「回退点」是整段边界，两个 `**` 之间夹着要求匹配多个整段的字面段（如
`**/x/**/y`），旧 `**` 多吃的段里可能包含后一个 `**` 必须用来满足其相邻关系的边界；段内唯一前缀序关系
在段的层面不成立，因为段边界是离散且两侧模式都有结构约束的。故跨段用段级 DP：对每个模式段计算它能匹配路径段
区间的哪些起点，D 段对「段数」作闭包传播，同样每对（模式段, 路径段）只算一次，O(模式原子×路径码点)。

推导 4：规则集版本一致。
`Set` 内只存一个不可变指针 `*ruleset`（规则切片编译后不再变更），用 `atomic.Pointer[ruleset]` 持有。
每次查询入口 `Load()` 一次得到本查询的版本快照，Match 与 Explain 全程只用该快照，故序号/原文/结论同版本。
替换先在新分配的结构上编译全部规则；任一条失败即返回错误、从不 Store，旧快照原封不动；全部成功才一次 `Store` 发布。

匹配结果表（Y=是，N=否）
| 模式 \ 路径 | "" | x | x/ | /x | x/y | a/b | a/x/y/b |
|---|---|---|---|---|---|---|---|
| x/**        | N  | Y | N  | N  | Y   | N   | N       |
| **/x        | N  | Y | N  | Y  | N   | N   | N       |
| *           | N  | Y | N  | N  | N   | N   | N       |
| **          | Y  | Y | Y  | Y  | Y   | Y   | Y       |
| a/**/b      | N  | N | N  | N  | N   | Y   | Y       |

资源上限：Limits{MaxPatternBytes, MaxDoubleStarSegments, MaxRules}，超限返回哨兵错误，替换保持旧版本。
步数：engine 内非导出计数器记录基本比较（每个「原子×码点」探测及 D 段传播一步），详见 AUDIT.md。
