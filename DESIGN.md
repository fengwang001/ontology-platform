# LZ77 流式压缩设计

## 流格式（所有整数为 uvarint/LEB128，字面量数据除外）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | magic=0x4C5A, version=1 |
| 字面量段 | tag=0, n(≥1), 随后 n 字节原文 |
| 回指 | tag=1, dist(≥1), len |
| 刷新标记 | tag=2（无负载） |
| 流尾 | tag=3, totalLen, crc64(ECMA) 校验和 |

流尾之后任何字节均为错误。变长整数超过 10 字节或第 10 字节 >1（溢出 64 位）为错误。

## 推导一：压缩结果与 Write 切法无关（语义 3）

一个位置上的最长匹配会向「未来」延伸，而 Write 边界处的未来字节尚未到达；
若在 Write 返回前定型任何 token，输出就会编码进「调用方怎么切」这一信息。
因此本设计让 `Write` 只做一件事：把字节追加进未决缓冲 `pending`，不定型任何
token。所有编码决策推迟到 `Flush`/`Close`：此时对 `pending` 整体跑一遍贪心最长
匹配（匹配可回指窗口内已定型历史，也可在 pending 内部自交叠）。于是压缩输出
是「全部输入字节 + Flush 发生的累计偏移」的纯函数，与 Write 切法无关。
代价是未决字节数 = 上次 Flush 以来的全部输入（上界由调用方用 Flush 频率控制），
这是换取切法无关性的必要条件：任何更早的定型点本身就会成为一个可见的「切」。
推论：`Flush` 必须强制结束当前匹配——它承诺「截至目前的输入全部可解码」，
不能再等未来字节；进行中的匹配在当前末尾截断发出，剩余部分下一轮作为新匹配
（可回指 Flush 前的数据），故不同 Flush 位置可产生不同输出，但同位置必然相同。

## 推导二：重叠回指的复制语义（语义 2）

距离 d < 长度 L 时，回指语义是逐字节前向复制：第 i 字节取自「当前」输出末尾
前 d 字节，源区与目的区重叠。一次性块复制（memcpy L 字节）会读到尚未写出的
字节，结果错误；纯逐字节循环正确但常数大。本实现按块复制但块长 ≤ d：每轮取
`n = min(d, 剩余)`，因为 n ≤ d 时源区间完全落在已写出的字节内（归纳：起始时
dist ≤ 已输出数保证成立，每轮追加 n 字节后不变式保持），用 memmove 一次复制
n 字节，共 ⌈L/d⌉ 轮。既非逐字节，又严格等价于逐字节前向复制。

## 推导三：两类距离错误为何区分（语义 7）

「距离 > 窗口容量」是对格式的静态违反：编码器在任何输入、任何 Flush 位置下都
不可能产生，因为匹配器在链上按 `pos-p ≤ 窗口容量` 剪枝，候选距离天然 ≤ 容量；
解码器据此把它当作流被损坏/伪造的证据。「距离 > 已输出字节数」是动态违反：
只在流的前「窗口容量」个字节内可能发生（之后已输出数 ≥ 容量，前者蕴含后者）。
二者成因与可恢复含义不同，故用不同哨兵错误。并行块压缩中预置字典 = 上一块
原文尾部，这些字节在拼接后的流里就是「已输出」字节，所以「已输出字节数」的
含义不变——仍是解码器的全局输出计数，而非本块内计数。

## 推导四：并行块压缩的确定性（语义 9）

块 i 的压缩输出 = g(字典_i, 块原文_i)，其中字典_i 与块原文_i 都是原始输入的
切片，不依赖任何其他块的压缩结果、完成顺序或 goroutine 调度；worker 只把结果
写入按块号索引的槽位，主 goroutine 按块号顺序拼接，故 workers 取任意值输出
逐字节相同。块间不需要刷新标记：解码器是纯粹的记录流状态机，回指距离相对
全局输出计数，块 i 的字典在流中恰好是已输出的前一块字节，距离天然合法；
流头只写一次，流尾（总长 + 校验和）覆盖全部输入。

## 复杂度

匹配器为哈希链（4 字节哈希 → 位置链），每位置最多考察 `ChainLen` 个候选，
故候选考察总数 ≤ ChainLen × 输入字节数，与窗口规模无关；匹配长度上限 4096，
找到等长候选即提前终止，保证全同字节串每字节考察数不随规模增长。
