# DESIGN

统一格式差分/补丁器的关键规则推导（题面第三节）。

## 1. 零计数 hunk 头的行号（语义 4）

样例一：`x\n` 变为 `y\nx\n`（C=0）得头 `@@ -0,0 +1 @@`。
样例二：`a\nb\nc\n` 变为 `a\nb\nX\nc\n`（C=0）得头 `@@ -2,0 +3 @@`。
样例三：`a\n` 变为空文件（C=3）得头 `@@ -1 +0,0 @@`。

推导：计数 b=0 表示该侧在 hunk 中不消费任何行，区间是空区间，没有"首行"可写。
若把数字解释成"将要插入的位置（1 基）"，样例一的插入位置是第 1 行，应写
`-1,0`，但实际是 `-0,0`；样例二的插入位置是第 3 行，应写 `-3,0`，但实际是
`-2,0`。两个样例的数字都比"插入位置"小 1，恰好等于插入点**前一行的行号**
（文件首之前没有行，记 0）。样例三新文件为空，空区间在"第 0 行之后"，故
`+0,0`。结论：b=0 时 a 是空区间之前那一行的行号；b≥1 时 a 是区间首行的 1 基
行号。`Apply` 解析：b=0 时 0 基插入点 = a（在第 a 行之后插入）；b≥1 时 0 基
起点 = a-1。

| 情形 | 头部书写 | Apply 对插入点的解释 |
|---|---|---|
| b=0 | `-a,0`，a=插入点前一行的行号（文件首为 0） | 0 基插入点 = a |
| b=1 | `-a`（省略 `,1`），a=该行 1 基行号 | 0 基起点 = a-1，消费 1 行 |
| b≥2 | `-a,b`，a=区间首行 1 基行号 | 0 基起点 = a-1，消费 b 行 |

## 2. hunk 合并阈值 = 2C（语义 5）

每段改动向两侧各延伸 C 行上下文。两段改动之间隔 g 行未改动行：前一段占用其
后 C 行，后一段占用其前 C 行。g ≤ 2C 时两段的上下文重叠或相接，GNU diff 输
出单个 hunk；g ≥ 2C+1 时中间至少有一行不属于任何一段的上下文，输出两个
hunk。故阈值是 2C：g ≤ 2C 合并，g = 2C+1 起拆分。

反证另两个取值：若阈值是 C，取 C=1、g=2（改动第 2、5 行），按 C 会拆成两个
hunk，但 `diff -U1` 实际输出一个 hunk（第一段的尾上下文到第 3 行，第二段的
首上下文从第 4 行起，两者相接必合并）。若阈值是 2C+1，取 C=1、g=3，按 2C+1
会合并，但 GNU 输出两个 hunk（中间第 4 行不属于任何上下文）。测试覆盖
g=2C（合并）与 g=2C+1（拆分）两侧。

## 3. 最短脚本的并列选择：优先删除（语义 3）

Myers 每步在"向右（删除一行）/向下（插入一行）"之间选择；当
V[k-1] == V[k+1] 并列时固定选删除（水平移动）。例：`a b` 变为 `b a` 的最短
脚本是 `-a`、` b`、`+a`，而不是 `+b`、` a`、`-b`。效果：同一位置上的删除行
在 hunk 内排在插入行之前，与 GNU diff 的 `-` 先于 `+` 一致；选择规则固定，
故同一输入的补丁文本逐字节确定。

## 4. 偏移应用后后续 hunk 的平移（语义 7）

hunk i 以偏移 off_i = 实际位置 − 记录位置 应用成功后，hunk i+1 的查找中心
= 记录位置 + off_i（沿用上**一个** hunk 的偏移，不是累积行数差）。理由：同
一补丁的所有 hunk 针对同一版本的目标文件，前序 hunk 发现的位移（文件头部被
插入或删除了若干行）通常同样作用于后续位置；记录位置本身已含补丁生成时的行
号信息，再叠加累积差会重复计数。这与 GNU patch 的 in_offset 行为一致。
