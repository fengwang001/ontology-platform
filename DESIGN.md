# DESIGN — 基于 LSM 思路的有序键值合并读取器

范围：多层有序段文件上的点查、范围扫描与段合并（compaction）。不含写缓冲、WAL、压缩算法。仅用标准库，数据落本地临时目录。

## 1. 段文件格式（segment）

```
[header][entry]*[index entry]*[footerCRC]
header  (36B): magic"OKV1" u16ver u16stride u32entryCount u64indexOffset u64indexLen u32indexCount u32headerCRC
entry        : u32keyLen i32valLen key val u32crc   (valLen==-1 表示删除标记；valLen==0 是合法空值)
index entry  : u32keyLen key u64offset              (每 stride=16 条取一条稀疏索引)
footerCRC    : u32，对 header 之后、footer 之前全部字节的 CRC32（头由 headerCRC 自保）
```

- 自描述头：版本、条目数、索引位置与长度都在头里，头自身带 CRC。
- 稀疏索引常驻内存，点查先二分索引定位到 ≤stride 条的块，再块内顺序扫，绝不线性扫全段。
- 每条 entry 自带 CRC，可区分「截断」与「位损坏」。

## 2. 同键多版本胜出规则（推导）

同一键可出现在多个段。新数据总是写在层号更小的层；同层（仅 L0，段间键范围可重叠）后产生的段序号更大。因此「新」的偏序为：

**层号更小者更新；同层时（只可能是 L0）段序号更大者更新。**

点查按此偏序找第一个命中：先按段序号降序查 L0 各段，再依次查 L1、L2…；L1+ 层内段不重叠，每层至多一个段可能含该键。归并迭代器用堆，键相同者按 (level 升序, seq 降序) 排优先级，堆顶即胜者，其余同键条目丢弃。

三种键状态必须可区分：
- **有值**：胜者条目 valLen ≥ 0（含空字节串 valLen==0）。
- **已删除**：胜者条目是删除标记（valLen==-1）。点查返回「已删除」，**不得**回退去更旧层取值，也**不得**与从未写过混淆。
- **从未写过**：所有层都无该键。

## 3. 删除标记可丢弃条件（推导）

设合并输入为若干层段的集合 C，目标层为 L。某键 k 的删除标记在合并输出中可被物理丢弃，当且仅当：**合并后不存在任何未参与合并的段可能含有 k 的更旧版本**。若存在这样的段 S（在比 L 更深的层，或同层但未参与合并），丢弃删除标记后，S 中 k 的旧值会在点查时「复活」——因为删除标记本是唯一压过它的版本。

充分可判定的实现条件（按层粒度）：**本次合并覆盖了当前所有更深层，即目标层 L 是存储中的最深层，且 L 层的全部段都参与合并**。此时 k 若不在 C 中，则任何层都不可能有 k 的更旧版本，删除标记可安全丢弃；否则必须保留。测试构造：合并 L0+L1（L2 仍有旧值）→ 标记保留、点查仍返回已删除；合并 L0+L1+L2 → 标记丢弃、段体积变小。

## 4. 合并期间的文件替换顺序（无读空窗）

1. 新段先写到 `<name>.tmp`，写满后 fsync；
2. `rename` 为正式 `.seg` 名（同目录 rename 是原子的，读者要么看到旧文件要么看到新文件）；
3. 在写锁内原子切换内存中的层级状态（旧段集合 → 新段集合）；
4. 释放锁后再关闭并删除旧段文件。

读者全程持读锁完成一次点查/扫描，拿到的层级快照必然自洽：切换前是旧段全集，切换后新段已就位。因此任意时刻都不会出现「旧段已删、新段未就位」的空窗。合并中途崩溃时，目录里只剩 `.tmp` 残骸，旧段原封未动；打开存储时扫描目录删除所有 `.tmp` 即完成恢复，读取结果与合并前逐字节相同。

## 5. 复杂度约束（可证明上界）

- 点查：二分稀疏索引（⌈log2(n/stride)⌉ 次比较）+ 块内至多 stride 条顺序比较。n=100000、stride=16 时 ≤ 13+16=29 < 4·⌈log2 100000⌉=68。segment 内置非导出比较计数器，测试断言。
- 归并：堆大小 ≤ S（参与段数），每吐出 1 条至多 2·⌈log2(S+1)⌉ 次堆比较，M 条总计 ≤ 4·M·⌈log2(S+1)⌉。归并迭代器内置比较计数器，测试断言。
- 合并内存驻留：每段迭代器同一时刻只持当前 1 条，驻留上限 = S 条。归并迭代器内置驻留峰值计数器，测试断言。

## 6. 截断分类（故障注入判定规则）

设文件实际长度 n，头 36B，索引区 [indexOffset, indexOffset+indexLen)，footer 4B：

| 截断后长度 n | 分类 |
|---|---|
| n < 36 | 头部不完整 |
| 36 ≤ n < indexOffset | 条目不完整 |
| indexOffset ≤ n < indexOffset+indexLen | 索引段不完整 |
| indexOffset+indexLen ≤ n < 全长（或 CRC 校验失败） | CRC 不匹配 |

截断段可读出「最大可恢复前缀」：从头后顺序解码，遇半截条目即停，半截一条不留；索引区或 footer 受损时全部条目均可恢复。

## 7. 层级不变量与边界语义

- L0 段间键范围可重叠；L1+ 层内段按键区间排序且两两不重叠，verify 检出违例并报告两段与重叠区间。
- 空段（仅头）、单条目段、空串键、空字节串值（≠删除标记）均合法。
- 范围扫描左闭右开 [start, end)；start > end 为可判定错误；区间与所有段不相交返回空。
