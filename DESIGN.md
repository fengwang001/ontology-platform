# 快照读可见性判定

## 唯一核心推导：只看提交时刻不够

朴素规则：版本写入事务 T 的提交号 < 快照水位 ⇒ 可见。

反例：快照水位 = 10，创建快照时 T1 尚未提交，故 T1 ∈ 活跃集；
随后 T1 提交，提交号 = 5（5 < 10，提交号可在水位编号之后才被读者观察到，
例如提交号取自较早分配的序号、水位按较新的序号推进）。
按朴素规则判可见，但取快照那一刻 T1 并未提交，快照不应看到它。

正确规则（两条件取与）：

1. 提交号 < 水位（左闭右开，等于不可见）；
2. 写入事务不在快照的活跃集中（自己写自己可见，故 owner 豁免）。

已回滚事务永不可见；未知事务、释放后使用、版本链提交号非递增均为错误。

## 复杂度

单次判定只做：事务表 1 次状态查找 + 活跃集 map 1 次成员查找，≤ 2 次，
用非导出计数器在测试中断言，不随事务总数或活跃集大小增长。
