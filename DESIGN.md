# DESIGN — 可撤销的批量重命名与冲突解析器

## 0. 问题形式化

命名空间 `Space` 是名字的集合（内存 `map[string]struct{}` 加互斥锁）。一批请求
`R = {old_i -> new_i}` 要求把每个旧名原子地换成新名。难点：执行中途任何时刻都
不得覆盖现存名字；任何冲突必须在第一次修改之前被判定；失败可完整回滚；可凭
日志整体撤销。

## 1. 排序规则的推导

把每个请求看成有向边 `old -> new`。若请求 X 的新名恰是请求 Y 的旧名
（`X.new == Y.old`），则 X 执行时 Y 的旧名还被占用，先执行 X 会覆盖数据，
故 **Y 必须先于 X 执行**。沿边反向追溯，得到依赖图上的拓扑序：对每个连通
分量，从「新名不再是任何请求旧名」的末端开始，逆着边方向依次执行。

关键观察：合法批次中每个旧名至多出现一次（否则是第三类冲突）、每个新名至多
被一个请求指向（否则是第二类冲突），所以该依赖图是**入度 ≤1、出度 ≤1 的函数
图**，其连通分量只有两种形态：

- **链**：`a1->a2->...->ak->t`，`t` 不在旧名集合中（是自由名）。执行顺序为
  从末端向首端：`ak->t, ..., a1->a2`。每步的目标名要么自由、要么刚被上一步
  腾空，绝不覆盖。
- **环**：`a1->a2->...->ak->a1`。链上不存在自由端点，任何排序都会覆盖，
  必须借临时名破环（见第 2 节）。

同层（互相无依赖的分量之间）按分量最小旧名的字典序排列，保证执行序列确定。

## 2. 破环的通用做法与临时名数量

对 k 元环 `a1->a2->...->ak->a1`，引入一个临时名 `tmp`，执行：

```
a1->tmp,  ak->a1,  a(k-1)->ak,  ...,  a2->a3,  tmp->a2
```

正确性：`a1->tmp` 后 `a1` 自由；随后逆序执行 `ai->a(i+1)` 时 `a(i+1)` 恰被
上一步腾空；最后 `tmp->a2`，`a2` 已自由。全程无覆盖，共 `k+1` 步、1 个临时名。

**临时名数量 = 环的个数**：每个环只需一个临时名（破一点即成链，链按第 1 节
处理）；且分量互不相交，各环的临时名各自独立，故总数恰为环数，可由计数器
精确断言。

临时名生成：形如 `#rename-tmp-N`，N 从 0 递增，校验「不在命名空间、不在本批
旧名集合、不在本批新名集合」，冲突则 N+1 重试。命名空间有限而 N 无上界，必在
有限步内成功；即使所有同形名字被预先占用也能找到可用者（可判定成功，而非覆
盖数据）。

## 3. 四类冲突的判定条件（全部先于任何修改）

按请求逐一遍历，用两个映射 `old->new`、`new->old` 与命名空间查询判定：

1. **目标名已存在且不在本批被搬走**：`new in Space` 且 `new notin olds`
   → `ErrTargetExists`，拒绝整批。
2. **两个请求指向同一新名**：`new` 已在 `new->old` 中 → `ErrDuplicateTarget`，
   报出两个旧名。
3. **同一旧名出现两次**：`old` 已在 `old->new` 中 → `ErrDuplicateSource`，
   报出两个新名。
4. **旧名不存在**：`old notin Space` → `ErrSourceMissing`，指出该名。

四个哨兵错误可用 `errors.Is` 区分。plan 只读不写，冲突时命名空间逐元素不变。

## 4. 复杂度论证

每个请求只做常数次哈希查找（登记 old/new、查 Space、链上走一步），总查找数
与「请求数 + 涉及名字数」成正比。plan 内置非导出计数器 `lookups`，测试用
5 万请求断言 `lookups <= 4*(请求数+名字数)`。

## 5. 边界语义定义

- **空请求集**：合法，产生 0 步，命名空间不变。
- **单请求**：退化为一步。
- **自环 `a->a`**：定义为**无操作**（不是冲突）。它不移动任何名字，plan 直接
  丢弃、产生 0 步；执行与撤销后命名空间逐元素不变。
- **空串名字**：合法名字，按普通名字处理。
- **路径分隔符**：`/`、`\` 按普通字符处理，无目录语义。
- **非法名字**：仅含 NUL（`\x00`）的名字非法（避免与日志二进制格式混淆），
  由 name 包 `ErrIllegalName` 拒绝。
- **请求数远大于名字数**：必然出现重复旧名，被第三类冲突拦住。

## 6. 执行、回滚与撤销日志

apply 持命名空间锁执行整批（**并发语义：批量执行期间并发修改被阻塞**，直至
本批结束），逐步骤把记录追加到本地临时目录的日志。第 k 步注入失败时，已执行
步骤就地逆序回滚（`new->old`），命名空间恢复逐元素相同，日志删除。

日志格式（定长头 + 记录区 + 全局 CRC）：

```
header: magic "ONRL"(4) | version(1)=1 | reserved(3) | count uint32 | bodyLen uint32  (16 字节)
body:   count 条记录，每条 oldLen uint16 | newLen uint16 | old | new
footer: crc32(4)，覆盖 header+body
```

截断分类（设文件全长 L，body 区为 `[16, 16+bodyLen)`）：

- 截断点 c 在 `[1,15]`：**头部不完整** `ErrHeaderIncomplete`；
- c 在 `[16, 16+bodyLen-1]`：**记录不完整** `ErrRecordIncomplete`；
- c 在 `[16+bodyLen, L-1]`：**CRC 不匹配**（footer 缺失或残缺）`ErrCRCMismatch`。

三类错误可用 `errors.Is` 区分。undo 解析到错误时按**最大可恢复前缀**撤销
（完整记录逆序回放 `new->old`），并报告无法撤销的步骤下标；头部不完整时无法
恢复任何记录。撤销成功后在日志旁写 `.done` 标记，**重复撤销是幂等无操作**。

## 7. 包划分

`name`（集合与校验）→ `cycle`（破环与临时名）→ `plan`（冲突检测与拓扑编排，
依赖 name/cycle）→ `apply`（持锁执行、故障回滚、写日志）→ `undo`（读日志、
截断分类、逆序撤销）→ `cmd/demo`（演示判定）。
