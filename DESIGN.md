# DESIGN

模块 `ontology`，Go 1.26，仅标准库。五个包单向依赖：wire → window → match；enc、dec 分别依赖底层包。

## 流格式（全部无符号 LEB128 变长整数）

| 记录 | 字节布局 |
|---|---|
| 流头 | `0x4F 0x4E`("ON") + 版本`1` + 窗口容量(varint) + 链长上限(varint) |
| 字面量 | 标签`0` + 长度n(varint) + n 个原始字节 |
| 回指 | 标签`1` + 距离(varint,≥1) + 长度(varint,≥1) |
| 刷新 | 标签`2`（无载荷） |
| 流尾 | 标签`3` + 原文总长(varint) + FNV-1a 64 校验和(varint) |

varint 单字节 7 位载荷，最高位为续位；超过 10 字节或第 10 字节使 64 位溢出即报错。

## 推导 1：压缩结果与 Write 切法无关

贪心 LZ77 的“当前最长匹配”只有在看到**下一个输入字节**后才能判定它不再延长。因此压缩器把一个刷新区间（两次 Flush/Close 之间）的全部字节先收集到段缓冲，再对该段做一次性确定性编码：在位置 p 用哈希链求最长匹配，命中则发回指并整体前进，否则发字面量前进。匹配判定只读取已收集的段内容，不依赖调用方把段切成几次 Write。故同样的累计字节 + 同样的 Flush 偏移产生逐字节相同的输出。代价是单次 Flush/Close 之前不产出该段字节；`Flush` 必须强制结束当前未决匹配：它把已缓冲内容立即编码并追加刷新标记，保证解压端在此点恰好得到全部已交输入，同时不清空窗口，故后续匹配仍可跨过 Flush 点回指历史。无新输入的 Flush 不写任何字节（不产生重复刷新标记）。

## 推导 2：重叠回指的复制

块复制 `copy(out, out[start:start+length])` 在 Go 中语义是不重叠的 memmove，距离 d<长度 L 时源区尚未被填满，结果错误。正确语义是“以已输出流为无限源，位置 outLen-d+i 处读第 i 个字节”，这些位置在 i≥d 时正是本轮刚写出的字节（周期展开）。实现为逐字节 `out=append(out, out[len(out)-dist])`，每次读都发生在上一字节写出之后。回指长度上限受输出上限约束，最坏为不可压缩随机数据，逐字节 append 摊还 O(1)，复杂度可接受，故直接采用逐字节循环。

## 推导 3：两类“距离过大”错误

“距离>已输出字节数”引用了从不存在的历史（对端/空流也可构造），属于内容截断或伪造；“距离>窗口容量”则引用了即使流完整、窗口未满也永远不可达的位置。两者成因不同，压缩端在窗口容量 W 内查找，dist≤W 且 dist≤已提交字节数，**绝不可能**产生第二种；故分开报以便定位。并行预置字典时，解压器不预置任何数据：第 k 块(k≥1)在刷新点之后，此前已逐块解压出全部前序原文，窗口里自然包含上一块末尾 ≥W 字节；该块回指这些字节时“已输出字节数”照常累计，含义不变，无需特殊字典帧。

## 推导 4：并行块压缩的确定性与衔接

`CompressParallel(data, blockSize, workers)` 切成定长块。块 0 用空字典；块 k 的匹配只读取“本块原文 + 上一块原文的末尾 W 字节（预置字典，不重复输出）”。哈希链匹配仅由字节内容决定，与 goroutine 调度、完成顺序、其他块无关；每块用相同配置新建独立压缩上下文，故同一切块在 workers=1/2/4/8 下逐字节相同（workers 只改并发度）。衔接：块间插入刷新标签作为边界，块的回指可越过它；流头只写一次（含 W 与链长），各块载荷顺序拼接，最后只写一个含全文总长与全文校验和的流尾。

## 校验、错误与上限

校验和对原文做 FNV-1a 64，随段推进增量更新。所有解码错误带流内字节偏移并互异：魔数/版本、距离 0、距离>已输出、距离>窗口、varint 超长/溢出、尾长度不符、校验和不符、尾后多余字节、截断。长度在任何复制/分配之前对照可配置输出上限检查，命中即终态，后续 Write 返回同一错误。窗口容量与链长上限必须 ≥1。
