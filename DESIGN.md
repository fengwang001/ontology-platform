# 设计推导

## 1. 空行与单列空值
状态 FS 见到 `\n`：若当前记录尚无字段则**跳过该行**（空行）；否则结束记录。
因此单列空值若不加引号：输入 `a\n\nb` 中间空行被跳过，`x\n` 末尾的 `x` 之后无记录；
更关键的是裸空流解析出 0 条记录，而 `""`（无换行）才是「一条空记录」。
故单列空字段**必须**写成 `""`，否则回写后再解析记录直接丢失。多列中的空字段有兄弟字段锚定记录，无需引号。
规范输入定义：回写器自身的输出——行分隔 `\n`、无尾换行、引号最小化。

## 2. par 切点
每段并行跑**两个假设**：起点在引号外（状态 FS，前置 1 个幻影字节 `buf[s-1]`）
与起点在引号内（状态 QF）。幻影把 UF/QP/CR 三种边界态用 1 字节精确重建：
FS+普通字节→UF，FS+`"`→QP，FS+`\r`→CR，FS+`,`/`\n`→FS；故每段恰好两遍完整扫描，
幻影总数 ≤K。段 0 只能取外假设；其后每段依据**前一段被选中扫描的终态**二选一：
终态 QF 取内假设，其余取外假设。扫描产出「字段内容片段 + 分隔符 + 记录结束」token 流，
片段为原始字节区间（含未转换的 `""`），引号外层标记 openAt/closeAt。
拼接：丢弃完全位于段首之前的幻影 token；跨界片段裁剪到段首后与前段未闭合片段拼接，
统一反转义 `""`→`"`。分界符在切点左侧时左段已发出、右段幻影副本被丢弃，故不会重复。
所有偏移在扫描时即加段基址，是全局坐标；记录号/字段号由拼接后的单一顺序流编号，
错误偏移天然全局，记录号/字段号无需换算。

## 3. CR 待定
未引号态（含引号刚闭合 QP）见到 `\r` 进入 CR：不结束记录、不报错。
续传：CR 是持久状态，可跨任意次 `Feed`；下一字节为 `\n` 则结束记录，
否则（含 EOF）在该 `\r` 偏移报 ErrBareCR。par：CR 由幻影 `\r` 在下一段重建，
终态 CR 的段选外假设；EOF 仍处 CR 报 ErrBareCR。

## 4. 上限「立刻」
字段长度按**反转义后**计数：QF 中非 `"` 字节 +1；QP 见 `"`（即 `""` 转义）+1。
在每个字节的计数动作中立即与 MaxFieldBytes 比较，超限即在当前字节偏移报错，
不等分隔符、不缓冲整条记录。字段数在每个逗号处检查，记录数在记录交组装器时检查。
错误进入终态，后续 Feed 返回同一错误，已产出记录保留。

## 5. 状态转移表
字节类：`"`=Q，`,`=C，`\n`=N，`\r`=R，其他=O；动作 F=发字段片段，S=分隔，E=记录结束。

| 状态 | Q | C | N | R | O | EOF |
|---|---|---|---|---|---|---|
| FS 字段开始 | QF | F(空),S | 空记录:跳过; 否则F,E | CR | UF | 有开放记录:F,E; 否则空流 |
| UF 未引号 | ErrBareQuote | F,S→FS | F,E→FS | CR | UF | F,E |
| QF 引号中 | QP | QF | QF | QF | QF | ErrUnclosedQuote |
| QP 见引号 | QF(计1) | F,S→FS | F,E→FS | CR | ErrQuoteAfterClose | F,E |
| CR 待CRLF | — | — | F,E→FS | — | ErrBareCR(@\r) | ErrBareCR |

## 6. 其他约定
单一流式实例非并发安全；多实例、par 内 K goroutine 并发安全。
cell 记录 Value/Quoted/Start/End（含引号语法的半开区间）。
字节计数：流式每真实字节恰 1；par 每字节 ≤2 加 ≤K 幻影字节。
