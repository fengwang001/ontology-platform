# 设计：可撤销批量重命名与冲突解析器

## 1. 数据模型与自环语义
- 命名空间 N 是名字集合（map，顺序无关）；合法名字 = 任意字符串，空串合法，路径分隔符是普通字符。
- 请求 (old→new)：old 必须属于 N。自环 old==new 定义为**无操作（no-op）**：不排序、不执行、不占目标名，不视为冲突。
- 每个操作只做一件事：把名字 old 改名 new，要求执行瞬间 new∉N 且 old∈N。

## 2. 排序规则推导（拓扑序）
- 先忽略自环。把每个有效请求看成有向边 old→new，顶点为请求涉及的全部名字。
- 冲突（覆盖）发生在「new 当前仍在 N 中」。new 在 N 中只有两种可能：
  1. new 是某个尚未执行请求的旧名：加依赖 new→old（必须先把 new 搬走）；
  2. new 不是任何请求的旧名：它是外部已存在名，冲突，整批拒绝（冲突甲）。
- 依赖边含义：若有请求 x→new，则执行前必须先执行 x 为源的请求。对依赖图做拓扑序即可。
- 确定性：同一入度层内按名字字典序出队，故步骤序列只依赖请求集合，与输入顺序无关。
- 校验：{a→b,b→c} 的依赖为 b→a，拓扑序先 b→c 再 a→b，中途目标名恒不存在。

## 3. 环的通用破法与临时名数量
- 纯重命名图中每个顶点入/出度至多 1（冲突丙已拒绝重复旧名、冲突乙已拒绝重复新名），分量只有链或简单环。
- 链靠第 2 节拓扑序解决；环上每个 new 都是某边的旧名，依赖图也是环，无拓扑序，必须借临时名。
- 破环：取环 v0→v1→…→v{k-1}→v0，生成临时名 t（t∉N，t 不是任何请求新名），步骤为
  v0→t，v{k-1}→v0，v{k-2}→v{k-1}，…，v1→v2，t→v1。
  逐步验证：每步目标名均不在当前集合（第 1 步腾空 v0，之后逆向搬入，最后 t→v1 时 v1 已腾空）。
- 临时名数 = 环数：环之间顶点不相交，每环恰好引入一个新顶点 t；链与自环不需要。10 个不相交环恰用 10 个。
- 取临时名：固定前缀生成候选并逐一校验「不在现有名集合、不与任何请求新名冲突」，冲突则递增重试；
  候选耗尽返回可判定错误（绝不覆盖）。

## 4. 四类冲突（errors.Is 可分，全部先于任何修改）
- 甲 ErrTargetExists：new∈N 且 new 不是任一请求的旧名（目标已存在且不会被搬走）。
- 乙 ErrDupTarget：两个不同旧名指向同一 new；错误报出两个旧名。
- 丙 ErrDupSource：同一旧名出现两次；错误报出两个新名。
- 丁 ErrMissing：old∉N。
- Detect 为纯函数，只读取集合；冲突一次性在执行前检出，命名空间逐元素不变。

## 5. 复杂度
- 冲突检测与排序只做均摊 O(1) 的 map 查找，总计 O(R+V)；包内非导出计数器 lookups 统计查找次数，
  导出只读访问器供测试断言 ≤ 4(R+V)。

## 6. 执行、故障注入与撤销
- 执行按编排步骤序列进行，每步先断言目标名不存在；每完成一步向日志追加一条记录（old、new、flag、CRC32）。
- failAt 让第 k 步返回错误；已完成步骤按日志逆序撤销，命名空间回到执行前。
- 撤销幂等：撤销翻转记录 flag 并重写 CRC；第二次撤销无操作。

## 7. 日志截断的三种分类
- 文件 = 魔数头(8B) + 若干记录(2B oldLen + old + 2B newLen + new + 1B flag + 4B CRC)。
- 截断偏移 p∈[1,len-1]：p<8 → ErrHeaderIncomplete；越过上一记录边界后凑不齐一条完整记录
  → ErrRecordIncomplete；能凑出完整记录但 CRC 不符 → ErrCRC；p 恰为记录边界视为干净。
  撤销只按最大可恢复前缀进行，并报告无法撤销的步数。

## 8. 并发语义
- Namespace 用 sync.Mutex 保护；批量执行期间持锁。并发修改者 TryLock 失败即被拒绝（ErrLocked），
  故执行期间不存在并发修改；测试断言该拒绝语义。

## 9. 边界
- 空请求集合法（零步骤）；单请求直接执行；名字 "" 合法；"/" 按普通字符；大量重复旧名由冲突丙在 O(R) 拦截。
