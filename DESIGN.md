# DESIGN.md — 作用域符号表设计推导

## 1. 同层重复声明：可判定错误（ErrDuplicate）

推导：不变量 2 要求任何一次 `Ref` 解析到唯一声明并可归属，SelfCheck
要求所有已记录引用仍解析到同一声明。若采用「后者覆盖前者」：引用 R
在覆盖前已解析到声明 D1；覆盖后同名再解析必命中 D2，已记录的 R 无法
「仍解析到同一声明」→ 直接违反不变量 2，且 Captures 的历史记录失真，
不变量 4 无法核验。若允许「两者共存」：同名两条声明使解析结果不唯一，
同样违反不变量 2。结论：同层重复声明必须拒绝；拒绝不改变任何可观测
状态（不变量 5），于是已解析到前者的引用永远稳定，不变量 2 与
SelfCheck 的重解析核验才成立。

## 2. 遮蔽 × 前向引用（核心推导）

情形：内层将声明允许前向引用的 `x`（位置 10），引用点在位置 5，外层
也有一个 `x`。

选择 A：解析到内层那个声明——遮蔽对整个作用域外延生效，声明位置只
决定引用的合法性（Forward 放行 / Plain 报错），不决定遮蔽与否。
- 后果：内层程序片段的含义自包含，外层同名声明无法劫持内层解析；
  不变量 2 的「可归属」与不变量 4 的捕获集合只取决于本层文本，可组合。
- 代价：若调用方在 `Declare` 之前就 `Ref`（调用序先于声明），该引用
  会解析到外层 x；待内层 `Declare` 完成后，SelfCheck 的重解析会发现
  记录失真并报错——不一致是可判定的，而非静默通过。

选择 B：解析到外层已可见的 x——内层声明只遮蔽其位置之后的引用。
- 后果：同一段内层文本，包在不同的外层里解析结果不同；捕获集合
  （不变量 4 的「恰好」）随外部环境变化，同一程序失去确定答案；
  前向引用机制形同虚设——任何外层同名声明都会截胡内层引用。
- 它不直接违反不变量 1/3/5，但使不变量 2、4 的判定依赖外部环境，
  不可组合，且与「捕获判定边界」无法给出稳定答案。

结论：选 A。实现：沿作用域链向外逐层哈希查找，第一个含该名字的层即
命中层，不再外溢；位置早于声明时 Forward 种类放行、Plain 报
ErrUseBeforeDecl（同样不外溢到外层）。

## 3. 捕获的判定边界

与选择 A 保持一致：名字在本层有声明（即使位置在引用点之后、种类为
Forward）→ 解析终止于本层 → 不算捕获外层。只有解析落到严格外层
（命中深度 < 引用所在深度）的引用才计入 `Captures`；同一外层声明被
引用多次按 (声明, 深度) 去重，只算一条。

## 4. 最外层 Leave：可判定错误（ErrLeaveRoot）

推导：不变量 5 要求被拒操作不改变任何可观测状态。若静默忽略，则
「配对错误的 Leave」与「成功的 Leave」不可区分，调用方无法发现
Enter/Leave 失衡，SelfCheck 对配对数的核验也失去意义。返回
ErrLeaveRoot 且不弹栈：既满足不变量 5（零状态变更），又让失衡
立即可判定，且与 ErrEmptyName 是互不相同的两类错误。
