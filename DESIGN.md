# 带转义 RLE 编解码器设计

不变量：严格解码器接受 t 当且仅当 `Encode(Decode(t)) == t`（合法编码唯一）。

## 第 2 条：拒绝清单逐条推导

设解码器接受了下述 t，则 `Decode(t)` 再 `Encode` 必得到更短/不同的规范串，矛盾：

1. 显式次数 1：`1a` → Decode=`a` → Encode=`a` ≠ `1a`。次数 1 必须省略。
2. 次数 0：`0a` → Decode=`` → Encode=`` ≠ `0a`。次数 0 的游程不产生输出。
3. 前导零：`01a`/`007a` → Encode 结果为 `a`/`7a` ≠ 原串。十进制不得有前导零。
4. 相邻同符号：`2a3a` → Decode=`aaaaa` → Encode=`5a` ≠ `2a3a`。最长游程必须合并，故 `a3a`、`aa`（两个 1 次游程）同样拒绝。
5. `\` 后非数字非反斜杠：`\a` 若接受则 Decode 含 `a`，Encode 产出 `a` ≠ `\a`；`\` 仅定义为转义数字与 `\`。
6. 末尾孤立 `\`：`a\` 不对应任何符号，任何 Decode 结果再 Encode 都不会以孤立 `\` 结尾。
7. 末尾只有次数：`3` 若接受，Decode 结果再 Encode 末位必为符号而非裸数字。
8. 非法 UTF-8：码点序列无定义，Encode 的输入是合法 UTF-8 串，往返无从谈起。

每类对应一个哨兵错误（`ErrCountOne` 等），包装在 `*rle.Error{Kind, Offset}` 中，
Offset 为违规 token（次数/转义/符号/坏字节）的起始字节偏移，`errors.Is` 可判定。

## 第 3 条：大次数的取舍

选择「支持到输出上限为止」：次数用饱和解析（`runs.ParseCount`，超 ceiling 即截断为
ceiling，绝不溢出）；解码器边解析边流出，内存 O(1)，绝不按次数预分配。输出总字节上限
`Decoder.SetMaxOutput` 可配（默认 1 GiB），次数折算的输出超限时返回可判定的
`ErrOutputLimit`。`99999999999999999999a` 因此得到确定错误而非崩溃或静默错误结果。

## 测试对照表

（待补）
