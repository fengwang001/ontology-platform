# DESIGN

依赖方向：lines → edit → hunk → udiff → patch（仅允许此方向）。
Line = 内容(不含行尾) + EOL："\n"、"\r\n"、或 ""（末行无换行）；切分保留行尾，Join 原样拼回。

## 1. 零计数 hunk 头的行号（语义 4）

`-a,b` 中 a 是该 hunk 覆盖的旧文件行范围起点之前的旧文件行数（1 基），b 是旧行数；
b=0 表示插入发生在某行"之后的间隙"，此时 a = 间隙前一行的行号（没有前一行则为 0）。

| 情形 | 书写 | Apply 的解释（插入点，0 基索引） |
|---|---|---|
| b=0 | `-a,0` | a=0 → 点 0（文件最前）；a>0 → 第 a 行之后（索引 a） |
| b=1 | `-a`（省略 ,1） | 第 a 行（索引 a-1），该行必须出现在旧文中 |
| b>=2 | `-a,b` | 从第 a 行起共 b 行（索引 a-1..a+b-2） |

样例：`x\n`→`y\nx\n` C=0：纯插入在文件最前，前面有 0 行 → `-0,0 +1`（新侧有 1 行）。
`a\nb\nc\n`→`a\nb\nX\nc\n` C=0：插入在第 2 行之后 → `-2,0 +3`（X 是新文件第 3 行）。
`a\n`→空 C=3：hunk 仅含删除行 a，新侧 0 行且在新文件最前 → `-1 +0,0`。
a 是"前行号"而非"插入位置"：插入点（0 基）恰为 a，数值相同但含义不同；1 基位置会是 a+1
（第二例应为 3 而非 2），证明 a 计的是前行数。

## 2. hunk 合并阈值（语义 5）

两段改动相隔未改动行数 g。合并则中间 g 行全作上下文；分开则各带 C 行，共需 2C 行上下文。
g <= 2C 时两个 C 行窗口在物理上放不下（重叠/越界），GNU diff -U C 必输出一个 hunk；
g = 2C+1 时两窗口间恰好剩 1 行不在上下文中，GNU 输出两个 hunk。
故阈值 **2C**：g <= 2C 合并，g >= 2C+1 分开。
反证：阈值为 C 时，C=3、g=5（应合并）会被错误分开；阈值为 2C+1 时，g=7（GNU 分开）会被错误合并。

## 3. Myers 并列最短路径的选择（语义 3）

每条 furthest-reaching 对角线上，k 奇（偶）数 d 时取 down（删除）优先，k 偶（奇）取 right
（插入）优先，即经典 Myers 的 down-on-odd 规则。`a b`→`b a`（d=2）：k=1 奇 → down，
先删 a，d=2 经 right 插入 a，hunk 内顺序为 `-a`、` b`、`+a`（删除块排前）。
规则确定，与重复次数无关，保证 100 次输出逐字节相同。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

前一 hunk 记录起点 p0、实际匹配 p0+k（k 为累计偏移），其头声明旧跨度 L 行；应用后旧文消费点为
p0+k+L。后一 hunk 记录起点 q0 与 p0 的相对距离在原文件中固定，故查找基准平移为 **q0+k**：
平移量等于已确认偏移 k，不再加 L——L 已包含在 q0-p0 的相对距离里。±F 窗口内取
|候选-(q0+k)| 最小、并列取最前者。
