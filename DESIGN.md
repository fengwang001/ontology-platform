# DESIGN: 游标分页器（keyset pagination）

## 1. 排序值重复时为什么必须用复合键

数据集按 `(score float64 升序, id string 升序)` 全序排列。

- 游标只记 `score=v`、下一页取 `score > v`：上一页末尾 `v` 之后剩余的同分行会被整批跳过（漏）。
- 取 `score >= v`：上一页已经返回过的同分行再次出现（重）。

根因是单值键在重复值上不是全序，无法定位「同分行内部」的位置。

**结论**：游标记录复合键 `K=(v,id)`，下一页边界是复合键的严格大于：

```
K' > K  iff  K'.score > K.score 或 (K'.score == K.score 且 K'.id > K.id)
```

float64 用 IEEE-754 位序（`math.Float64bits` 后符号位翻转）比较，NaN 不允许写入。这样每个键唯一、严格全序，翻页不重不漏。

## 2. 正向 / 反向翻页规则

游标编码 `Kfirst`（本页第一行键）与 `Klast`（本页最后一行键），加方向位。

- 正向（Forward）：从 `Klast` 之后取严格大于的前 `n` 行；首页为空游标。
- 反向（Backward）：从 `Kfirst` 之前取严格小于的 `n` 行（倒取），**取出后反转**，页内仍按正向排列。

因此「第 3 页游标反向一页」得到的正是第 2 页，且逐行顺序相同，而非其倒序。

## 3. 并发增删下的语义

数据存于内存有序切片 + RWMutex；翻页是**纯函数**：入参为快照边界键，游标不含可变推进状态，同一游标并发翻页结果逐行相同。

- 行只按复合键区间定位，游标指向的行被删除不影响续翻（不要求该行存在）。
- 已返回过的行，其键小于当前边界，严格大于边界永不会再次返回它（即使并发重排）。
- 未删除的行落在 `(边界, +inf)` 区间内必被后续页取到，不会被跳过。
- 新行插入到已翻过区间（键 ≤ 当前边界）允许不出现：不做快照保证。
- 插入到未翻区间、删除未翻页中的行，对后续翻页即时生效。

## 4. 游标编码与错误分类

字节布局：`version(1) | direction(1) | lastScore(8) | lastIDLen(2 BE) | lastID | firstScore(8) | firstIDLen(2 BE) | firstID | checksum(4 BE)`。

- checksum = FNV-1a 覆盖此前全部字节；空字节串 = 从头开始（合法）。
- 解码校验顺序：长度不足 → 字段长度越界（均归 `ErrIncomplete`）→ 方向位（`ErrDirection`）→ checksum（`ErrChecksum`）；方向与翻页方向不符由取页入口判为 `ErrMismatch`。
- 四类错误均为哨兵错误，用 `errors.Is` 判定。

## 5. 效率

- 存储为有序切片，`sort.Search` 二分定位边界（O(log N)），再顺序取至多页长（含被删检查），单次翻页检查行数上界 `页长 + ceil(log2 N)`，实现中以本地闭包计数器观测，断言 ≤ `4*(20+ceil(log2 10000))`。
- 解码固定次数 `binary.Read`/拷贝，`testing.AllocsPerOp` 断言为常数（与游标长度无关）。

## 6. 包划分

`row`（键与全序比较）、`cursor`（编解码/校验/错误）、`page`（内存有序集与双向取页）、`audit`（遍历自检：不重不漏、顺序一致）、`cmd/demo`（8 条判定）。
