# 设计推导

## 1. 末尾换行策略

设输入非空。规范器先逐行处理（删行尾空白、统一 `\n`），末尾未终结的最后一行的空白同样删除。三策略只改“最终字节流的尾部”：

- `Keep`：原样。无换行就不加；有几个留几个。
- `EnsureOne`：保证“最后一行被终结”：末尾没有 `\n` 则补一个；已有则原样（`\n\n` 仍为 `\n\n`，不额外删）。
- `Trim`：删全部末尾空行，但保留一个换行，即尾部折叠为单个 `\n`。

空输入三策略都输出 `""`：不为空内容凭空造字符；否则 `EnsureOne("")="\n"` 会把“终结符”当成内容（该方案虽也可幂等，但我们不采用）。上述定义下三策略均幂等：输出只以 0 或多个 `\n` 结尾，再次规范不删行内空白、不再新增、`Trim` 把尾部折叠到一个后不动。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

映射是“区间映射”：保留段 `[a,b)→[c,d)` 逐字节对齐，删除段 `[a,b)→[c,c)`（输出坍缩点 c）。

- 保留字节 i：`ToOut(i)` = 其输出位置。
- 被删字节 i：取包含它的删除段的坍缩点 c，即“紧随其后的下一个保留字节的输出起点”；删除段延伸到流末尾时 c = 输出长度。

`ToOrig` 在输出点 o 返回该位置对应原文点；坍缩点的 `ToOrig(c)` 取删除段之后第一个保留原文偏移（或 len(orig)）。于是 `ToOut(ToOrig(c))=c`，且两函数单调不减。光标落在被删的行尾空白或 `\r\n` 的 `\r` 上时，映射到换行符**之前**的行尾点（坍缩点正是该 `\n` 的输出起点），符合“清掉尾随空白后光标停在行尾”。

## 3. 空白缓冲超限

待判定空白必须缓冲：在看到行尾前无法知道是行内空白还是尾随空白。可选拒绝或强制输出。强制输出会在“实际是行尾”时错误保留尾随空白，违反第 2 条且破坏幂等；而拒绝不违反 4/5/6 任何一条——拒绝只依赖已扫描内容，与切分方式无关（第 4 条仍成立），不产生输出故无需映射。故选择**拒绝**：返回带原文偏移的哨兵错误，已输出内容与映射保留，规范化器进入终态，再写返回“终态后写入”错误。

## 4. par 拼接

段首不知道上段是否留有待定 `\r`/空白。安全切点只取“某行尾之后”的偏移 s：s=0、len，或字节 s-1 是某完整行尾（`\n`，含 `\r\n`）之后。此时 s 前所有行已终结，没有任何待定状态；段间互不影响。先用一次线性扫描求出所有安全点，再在每段要求的字节范围内选尽量靠近的安全点作为该段实际左右界（不选上一段右界以免空段），相邻段界共享同一安全点。

每段在其 `[L,R)` 上独立规范：非末段用 `Keep`（其末字节必为 `\n`，无待定尾空白），末段用配置策略。子输出在拼接点（恰为 `\n` 之后）首尾相接，无重叠无空洞；子映射先加 `(L, 全局输出起点)` 平移，再用 span.Builder 追加。修正范围有上界：切点需移动到最近安全点，移动不超过“该切点所在的那一整行”的长度，故每段最多多读其左右各一行；最坏单行极长时上界为段长，重复计算量仍 O(输入)。
