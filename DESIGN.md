# 设计推导：带补偿与断点续跑的 DAG 工作流编排器

本文档只写推导过程，不重复结论清单。代码分为 `graph` / `journal` /
`step` / `policy` / `orchestrate` 五个包，依赖严格单向。

## 1. 为什么补偿必须是逆拓扑序，且必须等前一步补偿完成

**从依赖方向推导。** 边 `A → B` 的语义是"B 依赖 A"：调度器保证 A 完成
之后 B 才开始。于是 B 的执行可以合法地读取、引用、扩展 A 产生的一切
状态——A 是 B 的**前提**。撤销（补偿）是执行的镜像：要安全地撤销 A，
必须保证已经没有任何"以 A 为前提"的状态存活，也就是说所有依赖 A 的
步骤必须**先**被撤销。沿依赖边反方向走，正是拓扑序的逆序：逆拓扑序中
每个节点都排在它的所有依赖之前，因此补偿到 A 时，A 的所有下游已被
补偿完毕，A 的前提不变式重新成立。

**为什么必须等 B 补偿完成，而不是并发补偿。** 逆拓扑序只规定了"发起
顺序"，如果并发执行，A 的补偿可能与 B 的补偿在时间上重叠，重叠期间
"B 已撤销"这个前提对 A 不成立。具体场景：

- A 的 Run 创建工作区目录 `/job`，A 的补偿是 `rm -rf /job`；
- B 的 Run 在 `/job` 下写入结果文件，B 的补偿是逐个删除自己写的文件
  并校验目录为空。

并发补偿时，A 的 `rm -rf /job` 与 B 的"删除 `/job/out` 并校验"同时进
行：B 可能删除失败（目录已被 A 删走，open 返回 ENOENT 之外的竞态错
误），也可能 B 校验通过的瞬间 A 尚未删除，留下语义上"补偿成功"但实
际残留的状态。无论哪种，都违反了"补偿完成后系统回到执行前状态"的
约定。因此 `orchestrate.compensate` 沿 `ReverseTopoOrder` **串行**执
行：B 的补偿记录 `CompOK` 落 journal 之后才发起 A 的补偿——是"完
成"在前，不是"发起"在前。

## 2. 崩溃点枚举与半条记录的识别

**崩溃点枚举。** 崩溃只能发生在"journal 追加一条记录"这个原子点的
两侧，因为所有状态变更都先落 journal 再生效（机器状态是 journal 的
纯函数，`orchestrate.append` 先写记录再 apply）。journal 的钩子对每
条记录触发 `Before` / `After` 两个事件，因此一次运行的崩溃点全集
= `2 × 记录数` 个。测试与 demo 对两个场景（全成功的菱形 DAG，16 个
点；A→B→C 且 C 失败触发补偿的链，20 个点）**逐点**注入崩溃。

**每个点的 journal 形态与恢复判定。** 在任意点崩溃后，journal 都是
一个合法前缀（钩子边界不会撕裂记录），重放规则是确定性的：

| 崩溃位置 | journal 前缀形态 | 重放判定 |
| --- | --- | --- |
| `Start` 前 | 步骤无任何记录 | 状态 Pending，正常调度执行 |
| `Start` 后 / `Retry` 后 | 有 `Start` 无终态记录 | `ResetAfterCrash`：Running→Pending，按幂等语义重跑 |
| `Success` 后 | 有 `Success` | 状态 Succeeded，调度器跳过，**绝不重跑** |
| `Failure` 后 | 有终态 `Failure` | 编排器进入补偿模式，补偿所有 Succeeded 步骤 |
| `CompStart` 后 | 有 `CompStart` 无 `CompOK` | `ResetAfterCrash`：Compensating→Succeeded，重新补偿 |
| `CompOK` 后 | 有 `CompOK` | 状态 Compensated，补偿循环跳过，**绝不重复补偿** |
| `CompFail` 后 | 有 `CompFail` | 记入聚合错误，不再重试该步 |

恢复只读 journal 字节（`Recover(cfg, bytes)` 构造全新编排器，不接触
旧进程任何内存），因此"已完成的步骤执行计数不增"由"journal 中有
`Success` ⇒ 状态机为 Succeeded ⇒ 调度器跳过"直接推出。

**半条记录的识别。** 每条记录的物理帧为
`uvarint 长度 | 载荷 | CRC32(载荷)`。崩溃可能把帧撕裂在任意字节。
重放时逐帧解码，遇到以下任一情况即判定为撕裂尾部并**丢弃**：长度前
缀不完整、声明的长度超出剩余字节、CRC 校验失败、载荷内部字段截断。
`FromBytes` 只保留合法前缀作为新 journal 的底稿，之后的追加都接在
合法前缀之后，所以丢弃后日志自洽、可继续追加。被撕掉的记录对应的
步骤按上表"记录未写入"的分支处理（重跑或重补偿），由步骤幂等性保
证安全。

## 3. 重放的复杂度：为什么节点访问次数与轨迹长度无关

**数据结构。** 重放分两步：

1. 顺序扫描 L 条记录，每条记录做一次**哈希表查找**
   （`map[stepID]*Machine`，O(1)）并 `Machine.Apply`（O(1) 的状态
   转移）。这一步完全不接触图结构——不查邻接表、不做遍历。
2. 扫描结束后，对所有 N 个节点做**恰好一次**收尾遍历：dangling
   状态复位（`ResetAfterCrash`）并判定是否需要进入补偿模式。

非导出计数器 `replayNodeVisits` 只在第 2 步自增，因此其值恒为 N。
测试固定 N=2，分别构造 L=100 与 L=10000 的轨迹（靠单步骤的重试记
录把轨迹拉长），断言两次计数**相等**——计数不随 L 线性增长，甚至
根本不随 L 变化。

**最坏情况上界。** 重放总代价 = L 次哈希查找 + N 次节点访问 =
**O(L + N)**，与轨迹长度严格线性，且不含任何每记录一次的图遍历
（朴素实现若在每条记录时重新计算拓扑或可达性，将是 O(L·(N+E))，
这正是计数器要证伪的形态）。

## 4. 对外只有两种终态：补偿中间态如何对查询隐藏

**终态集合。** `Run` 的相图只有两条出路：所有层执行完 →
`PhaseCompleted`；任一步终态失败 → 进入补偿，补偿循环跑完（无论有
无补偿失败）→ `PhaseCompensated`。不存在第三条路径：补偿失败不会毒
化出"部分补偿"的终态，而是聚合成 `CompensationError` 附在
`FailureError` 上，相位仍然是 `Compensated`。

**临界区边界。** 关键在 `orchestrate.compensate` 的锁范围：

```
compensate():
    o.mu.Lock()            // 写锁，整个补偿临界区开始
    defer o.mu.Unlock()    // 临界区结束（含崩溃 panic 的展开路径）
    for id in reverseTopo:
        ...写 CompStart/CompOK/CompFail，推进各步骤状态机...
    o.phase = PhaseCompensated
```

`Snapshot` / `StepInfo` 走 `o.mu.RLock()`。读写锁的语义保证：补偿临
界区持写锁期间，任何查询都**阻塞**，直到锁释放——而锁释放时相位已
是 `PhaseCompensated`、所有步骤状态机已到达各自的终态。因此观察者
要么在补偿开始前看到 `Running`（所有已完成步骤都是 Succeeded，没有
任何"已补偿"混入），要么在补偿结束后看到 `Compensated`，**永远看不
到"一部分完成、一部分已补偿"的中间混合态**。正常执行阶段不持写
锁，查询可以并发读到 `Running` 下的逐步推进，这不违反终态约束——
约束只针对终态与补偿过程。崩溃导致补偿中断时，`defer` 解锁，旧编
排器被丢弃，恢复出的新编排器重走同一临界区，不变式不变。
