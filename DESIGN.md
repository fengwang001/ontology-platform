# 列式存储段与谓词下推扫描器 — 设计推导

包依赖单向：`bitpack`、`dict`、`zone` 互不依赖；`segment` 依赖前三者；`scan` 依赖 `segment` 与 `zone`。

## 第 2 条：空值的三态表示与统计排除

**编码层落实方式：位图，而不是哨兵值。**
每个行组带一个空值位图（第 i 位为 1 表示第 i 行是空值），数据块里只存非空值。
读取结果是 `(value, isNull)` 二元组：空值 = `isNull=true`；数值 0 = `isNull=false, value=0`；
非零值 = `isNull=false, value≠0`。三者两两可判定，因为"是不是空"这个信息根本不经过数值通道。

**为什么不能用哨兵值。**
int64 的全部 2^64 个取值都是合法数据（测试里就包含 `math.MinInt64`/`MaxInt64`）。
若挪用某个值（比如 `MinInt64`）当"空"，那么真实数据等于该值时就无法区分——
三态被压进二态，必然不可判定。任何"值域内编码空值"的方案都会与某个合法值冲突，
所以空值必须走独立通道（位图），这是信息论层面的结论，不是实现偏好。

**统计为什么必须排除空值。**
min/max 的用途是裁剪：谓词 `= v` 在 `v < min || v > max` 时排除行组。
若把空值当作 0 计入 min/max，一个全空行组会得到 `min=max=0`，
于是谓词 `= 0` 无法排除它（只是多解码，不漏）；但反过来更危险的是：
若空值以 0 参与 min，真实最小值是 5 的组会记录 `min=0`，
这不会误杀；真正会误杀的是另一方向——若空值"拉高"了 max 或"压低"了 min 之外的任何
数值化方案，都可能让某个本该命中的边界谓词被统计排除。
更根本地：空值不命中任何数值谓词（第 4 条语义），所以它对数值裁剪判定
**不提供任何信息**；把它算进 min/max 只会让区间变宽（多解码）或在哨兵方案下
直接算错（误杀）。因此 min/max 只统计非空值，空值只进 `nulls` 计数；
全空行组用 `HasMinMax=false` 显式表示"无 min/max"，与"min/max 恰好是 0"严格区分。

## 第 3、4 条：裁剪判定式与形式化论证

设行组统计为 `S = (rows, nulls, min, max, hasMinMax)`，其中 min/max 仅取自非空值。
判定式 `MayMatch(S, p)`：

```
IS NULL      → nulls > 0
IS NOT NULL  → rows - nulls > 0
数值谓词      → 若 ¬hasMinMax 则 false（组内没有非空值，而空值不命中数值谓词）
  = v        → min ≤ v ≤ max
  < v        → min < v
  ≤ v        → min ≤ v
  > v        → max > v
  ≥ v        → max ≥ v
  IN (vs)    → ∃u∈vs: min ≤ u ≤ max
AND(p1..pn)  → 全部 MayMatch 为真才保留
```

**不会误排除的论证（对每种数值谓词，证明"组内存在命中行 ⇒ MayMatch 为真"）。**
设组内存在非空值 x 命中谓词 p。由统计构造知 `min ≤ x ≤ max`（min/max 是非空值的
真实最小/最大，归纳可证）。

- `p = (= v)`：x = v，故 `min ≤ v ≤ max`，判定式为真。∎
- `p = (< v)`：x < v 且 min ≤ x，故 min < v。∎
- `p = (≤ v)`：min ≤ x ≤ v。∎
- `p = (> v)`：x > v 且 max ≥ x，故 max > v。∎
- `p = (≥ v)`：max ≥ x ≥ v。∎
- `p = IN(vs)`：x = 某个 u，故 `min ≤ u ≤ max`，存在性条件满足。∎
- `IS NULL`：组内有空行 ⇔ nulls > 0，判定式是充要的。∎
- `IS NOT NULL`：组内有非空行 ⇔ rows-nulls > 0，同为充要。∎
- AND：组命中合取 ⇒ 组命中每个合取项 ⇒ 每个判定式为真。∎

于是 `MayMatch = false ⇒ 组内无任何命中行`，排除是安全的；反向不保证
（`= 15` 对 `[10,20]` 判真但组内可能没有 15），即**只会漏排除、不会误排除**。

**边界谓词为何不能排除。**
`= min`：min 本身是组内真实存在的非空值，它命中 `= min`，排除即误杀；
判定式 `min ≤ v ≤ max` 在 v=min、v=max 处取等号正是为此。
同理 `≥ max`、`≤ min` 必须保留（x=max、x=min 命中），
而 `> max`、`< min` 可安全排除（上面对偶方向的证明）。
测试用穷举对照（下推结果 vs 全解码逐行过滤）覆盖这五种边界，逐行一致。

## 第 5 条：游标编码与跨批等价

**游标内容。** Scanner 的游标是二元组 `(group, rowInGroup)`——当前行组号与组内行号，
外加一个"当前组解码缓存"（`cachedGroup` 标记属于哪一组）。
不需要存谓词进度或全局行号：谓词是无状态的纯函数，行组间相互独立。

**为何能穿过被裁剪的组且不重不漏。**
扫描循环对每个组先做 `MayMatchAll`：为假则 `group++` 直接跳过——
**不调用 DecodeRowGroup**，这是"跳过真实发生"的实现点，计数器
（`decodedGroups`/`decodedValues`，非导出字段、原子更新）可证明 1000 组只解码 1 组。
为真则解码、从 `rowInGroup` 起逐行过滤，组内耗尽才 `group++, rowInGroup=0`。
游标关于行的全序（先组后行）严格单调递增，且每次 `Next(limit)` 从游标精确续走，
因此任何行至多被考察一次（不重）、每个组要么被裁剪（已证无命中）要么被完整扫描（不漏）。

**跨批等价论证。**
定义抽象扫描流为按 `(group, row)` 顺序输出的命中行序列。`Next(limit)` 的性质是：
返回流中接下来的至多 limit 个元素，且游标推进到"下一个未考察行"。
由于游标推进与 limit 无关（同样的状态机，只是循环上限不同），
对任意批大小序列 b1, b2, ...，拼接结果都是同一抽象流的前缀序列，
故与一次性扫描（limit=∞）完全相等。测试对 1..总行数的每个批大小逐一验证。

## 第 6 条：截断可判定的长度校验点

段格式：`header(magic|组数|总行数)` + 每组
`统计(25B) | 编码(1B) | 位图长度(4B)+位图 | 数据块(宽度|长度|字节，字典另含基数+字典值)`。

校验位置（每一处 `take(n)` 都先查 `len(buf)-pos >= n`，不足即返回
`CorruptError{组号, 阶段}`，阶段 ∈ header/stats/null-bitmap/data）：

1. **Open 时（骨架解析）**：头部 magic/组数/行数；逐组读统计（长度、nulls≤rows、
   min≤max、无 min/max 必须全空）、编码字节合法性、位图长度必须恰为
   `⌈rows/8⌉`、数据块的宽度∈[1,64] 与长度前缀，并跳过相应字节。
   任何截断都会让某个 `take` 失败，错误精确到组号与阶段。
2. **DecodeRowGroup 时（二次校验后解码）**：位图展开后非空位数必须等于
   `rows-nulls`；打包数据长度必须恰为 `⌈n·width/8⌉`；字典码必须 `< 基数`；
   组块必须恰好消费完（无尾部垃圾）。

**为何任意截断点都不会读出半截值。**
所有值的长度都来自"先校验、后解码"的长度前缀，且 Open 的骨架解析会完整走一遍
全部组块——只要 Open 成功，每个组的字节区间 `[off,end)` 都完整落在输入内，
解码时的二次校验再次确认。因此结果只有两种：完整解码成功，或返回
`CorruptError`；不存在"返回前 k 个值"的路径。测试遍历 0..len 的每个截断点，
断言要么报错（且是 `*CorruptError`）要么全部组可完整解码，并用 recover 证明无 panic。

## 第 8 条：字典降级与资源上限

**三类上限。** `Config{MaxRows, MaxRowGroups, MaxDictCard}`。
`AddRowGroup` 在任何状态修改**之前**检查行数与行组数，超限分别返回
`ErrTooManyRows` / `ErrTooManyRowGroups`（彼此可判定的不同哨兵错误），
此时未触碰缓冲区与计数器——测试断言拒绝前后 `Bytes()` 逐字节相同。
字典上限由 `dict.Encode` 返回可判定的 `ErrCardinality` 表达，且该失败调用
对字典零修改（先算码、后提交，见下）。

**降级触发条件与回退过程。**
行组编码选择：`dict.New(MaxDictCard)` 后尝试 `Encode(非空值)`。
`Encode` 分两阶段——先在临时结构里试分配码字，发现第 `MaxDictCard+1` 个
不同值时返回 `ErrCardinality`，**不改字典**；只有全部值试分配成功才把新值
提交进字典。segment 层捕获 `ErrCardinality` 后走位打包路径：
存 `v - min` 的定宽增量（uint64 环绕减法天然支持负数与 MinInt64/MaxInt64），
宽度取 `MinWidth(max-min)`。

**为何回退不影响已写入的行。**
降级发生在**当前组尚未序列化**的时刻：前面已提交的组块字节在缓冲区里，
当前组只是还没选编码。回退只是把当前组按位打包编码后追加，
字节流中每组自带独立的编码标记、统计与数据块，组间无任何共享状态
（字典是每组一个，不是段级共享），所以已写入的组不受任何影响，
同一段内不同组可以是不同编码。
