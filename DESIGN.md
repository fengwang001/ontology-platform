# 近似最近邻分桶检索器设计（module ontology）

目标：固定维度 `d` 的 float64 向量，用随机超平面 LSH（SimHash 风格）建桶，
多表并发生成候选、精排取 TopK；提供暴力基准与可核对召回率；索引落盘并可检测损坏。

## 1. 签名规则

每张表有 `b` 条随机超平面法向量 `r_j ∈ R^d`，分量由显式种子（`splitmix64` 流 +
Box–Muller 生成标准正态）确定，与输入无关，故同种子逐字节可复现。

对向量 `x`，第 `j` 位：

```
dot(x, r_j) > 0  -> 1
dot(x, r_j) <= 0 -> 0   // 含内积恰为 0：约定取 0（零向量与超平面内积恒 0，因此全 0 签名）
```

`b` 个 0/1 位打包成一个 `uint64`（位 j 放在 bit j，j<b≤64）。
拒绝 `NaN`（任意分量 `!=` 自身）与 `±Inf`（`IsInf`）向量；它们不进桶，计入跳过数。

## 2. 候选生成与精排

`L` 张独立（种子 `seed+tableIndex`）表，每张表把向量挂到其签名桶。
查询：对每张表算查询签名，取该桶向量的并集为候选，再用欧氏距离精排取前 K。
建索引哈希计算次数恒等于 `N·L·b`：每个向量在每张表对 `b` 条超平面各算一次内积。

## 3. 位数 b、表数 L 与候选数、召回的关系

设两向量余弦角为 θ，随机超平面给出同符号位的概率 `p(θ)=1−θ/π`，
则 **b 位全同（落同桶）的单表碰撞概率** `P1(θ)=p(θ)^b`。

- `L` 张独立表至少一次碰撞：`PL(θ)=1−(1−P1)^L`。
- b 增大 → `P1` 按 b 次幂下降 → 桶更细 → 单桶候选数下降，召回（命中近邻概率）下降。
- L 增大 → `PL` 单调上升（对 `0≤P1≤1`，`(1−P1)^L` 随 L 单调不降地减小）
  → 召回单调不降；候选数随之上升（多表并集）。
- b 不变、L→∞ 召回→1，候选→全集；b→大、L=1 候选→稀疏，召回→0。
召回率定义：查询的近似 Top10 与暴力精确 Top10 交集大小 / 10，对查询取平均。

## 4. 复杂度

建索引 `O(N·L·b·d)`；查询 `O(L·b·d + C·d)`，`C` 为候选数（精排距离计数）。
检索器内保留非导出计数器：哈希内积次数、精排距离次数，测试精确/上界断言。

## 5. 并发

表整体 `atomic.Pointer[[]*bucket.Table]` 发布：`Build` 完成全部表后一次性原子替换，
查询只读快照，未完成表永不可见；`-race` 下安全。

## 6. 持久化（小端二进制，自描述）

布局：`[Header][HyperPlanes][Buckets][CRC32]`

- Header 24B：magic(4)=`OLSH`、version(1)、dim(uint16)、bits(uint8)、tables(uint8)、
  nVectors(uint32)、hyperLen(uint32)、bucketLen(uint32)。
- HyperPlanes：`L*b*d` 个 float64 原始字节（IEEE754，跨同构架构逐字节一致）。
- Buckets：N 个向量 ID（uint32，0..N-1，按表/桶顺序平铺），每条记录
  `table u32 | sig u64 | count u32 | id[count] u32`，bucketLen 为该区总字节。
- CRC32（IEEE）覆盖 Header 起的全部字节。

读取严格按区长度切分，截断按位置归类为：`ErrHeaderTrunc` / `ErrHyperTrunc` /
`ErrBucketTrunc` / `ErrCRC`，彼此与 `ErrDegenerateHyperplane`（某法向量全零，
指出表号、位号）、`ErrDimMismatch`（期望/实际维度）互不包裹，`errors.Is` 可区分。
恢复模式取 CRC 之前可完整解码的最大桶记录前缀；剔除引用 `id>=N` 的悬挂 ID 并计数，
因此恢复结果自洽（桶表引用的 ID 均存在）。

## 7. 边界

空集合查询返回空 TopK；单向量；K>N 返回全部；全同向量（内积同号）落同一桶，
候选=N、召回=1；零向量签名全 0，合法；维度 1 合法。
