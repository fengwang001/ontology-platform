# 设计推导

## 1. 死信边界：第 m 次投递超时后立刻进死信

`MaxReceives = m` 要求“最多被投递 m 次”。投递次数只在消息真正被 `Receive` 取走时加一。

- 方案 A：第 m 次投递超时后**立刻**进死信（在超时处理刻）。
- 方案 B：超时后重新可见，等第 m+1 次 `Receive` 才发现超额再进死信。

方案 B 有两个矛盾：第 m+1 次 `Receive` 若返回消息就已经构成第 m+1 次投递，违反“最多 m 次”；
若不返回消息，则这次 `Receive` 必须零副作用地拒绝，消费者看到的是“无消息”，死信进入时刻被
推迟到一次偶然的接收，且投递计数与死信时刻都依赖消费者节奏。方案 A 在“第 m 次投递的截止刻”
由 `Tick` 判定 `receives == m` 即转死信：投递次数恒 ≤ m，死信时刻确定（= 第 m 次截止刻），
`Stats().Delivered` 恰好等于实际投递次数。故选 A。

设首次 `Receive` 在刻 0、可见性为 1，且每次都超时：

| m | 每次投递刻 | 进死信刻 |
|---|---|---|
| 1 | d1@0 | 1（d1 超时即死信，不再可见） |
| 2 | d1@0, d2@1 | 2 |
| 3 | d1@0, d2@1, d3@2 | 3 |

规则：第 k 次投递在刻 k-1；其截止刻为 k。k < m 超时 → 重新可见（世代号 +1）；
k = m 超时 → 死信。`Ack` 成功则止于已确认，不进死信。

## 2. 迟到 Ack 与超时同刻：先超时，后 Ack

语义 3 规定左闭右开：消息在“Receive 刻 + visibility”**这一刻已可被取到**。
因此该刻到达时，在途租约必须先被视为到期。`Tick(now)` 的处理顺序固定为：

1. 弹出所有 `deadline <= now` 的在途消息：`receives < m` → 重新可见（新世代号），
   `receives == m` → 死信；旧收据随世代号推进而作废。
2. 之后同刻到达的 `Ack(r)` 按当前状态校验收据：世代号已不匹配 → `ErrStaleReceipt`，零副作用。

这与左闭右开一致：到期刻消息已不属于原持有者，其 Ack 必然是迟到 Ack。
若反过来“先 Ack 后超时”，到期刻的 Ack 会成功删除消息，随后超时重投被抑制——
消息既不会重投也（在 m=1 时）不进死信，且与“到期刻已可取走”矛盾，破坏守恒与 FIFO。

## 3. 数据结构与检查计数

- 在途按 deadline 维护最小堆；可见消息按 (becameVisible, id) 排序，id 单调分配，
  重新可见时追加即保持 (时刻, id) 有序，从队首出队。
- `Tick` 每比较一次堆顶计一次检查；所有到期项弹出后，再多看一个未到期堆顶即停止，
  故检查数 ≤ 本次到期数 + 1。
- `Ack`/`Extend` 只做一次 map 查找 + 世代号比较即定成败，恒为常数检查；
  `Extend` 成功后以当前刻重算 deadline 并调整堆，堆重排不计入“检查数”。
