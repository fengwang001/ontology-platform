# 设计推导

## 1. 末尾换行策略（Trailing）

记号：N 为「行尾归一 + 去行尾空白」后的文本（未做末尾处理）。
- `Keep`：输出 N，空串仍是空串。
- `EnsureOne`：N 以 `\n` 结尾则原样；非空且无尾换行则补一个 `\n`；空串输出 `""`。
  选空串→`""` 而非 `"\n"`：若补成 `"\n"`，再跑一次得到 `"\n"`，幂等也成立；
  但空输入无任何行却凭空造行，且 1 字节输出无原文锚点、`ToOrig(0)` 只能造假位置，
  破坏「输出偏移均对应真实原文字节」的直觉。故选 `""`。
- `Trim`：删掉全部末尾空行（所有尾部 `\n`），非空时补回恰好一个 `\n`；全空白行计入「空」→空串。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

EnsureOne 保留多个尾换行时再跑一次不变；Trim/Keep 显然为不动点，故三者幂等。

## 2. 被删字节的 ToOut

删除区间 D=[a,b)（行尾空白，或行尾空白连同 CRLF 的 `\r` 组成的连续删除段），前一存活字节 a−1 映射 o−1，
后一存活字节 b 映射 o。由 `ToOut` 单调不减且 `ToOut(ToOrig(o))==o`
（每个输出位置都必须被取到），区间内不能整体塌到 o−1：
故取 `ToOut(i)=o`（i∈D），即「下一个存活位置」。光标落在被删空白里映射到换行符起点
（**换行符之前**，即换行符所在偏移），语义为「本行之后、新行内容之前」。CRLF 的 `\r` 与前面
的行尾空白同属一个连续删除段，同样映射到该 `\n` 的输出偏移。

## 3. 空白缓冲上限：选择拒绝

强制判定为「非行尾」会先输出空白；若后续真出现换行，正确结果应删除它们 → 输出错误，
违反语义 2/4/5 且不可回滚。拒绝（`ErrWhitespaceLimit`，带当前原文偏移）不产出相关字节，
结果恰等于「在上限点截断原文后规范化」，截断语义本就被定义并被故障注入钉住，不违反 4/5/6。
拒绝后进入终态，再写返回 `ErrClosed`。

## 4. par 拼接

段首不知前段是否有待定 `\r`、段尾空白是否在行尾。第 k 段处理拥有区间
`[start,end)` 加右端上下文 `[end,min(end+C,n))`，C=wsLimit+2（覆盖任意整段空白+换行判定）。
流式状态机本就保证「切分一致」，worker 在拥有字节末位之后多看 C 字节即可对段尾待定状态
做出与全局相同的判定；再按**拥有原文区间的命运**裁切输出与映射：输出裁到最后一个拥有字节
对应的输出位置（拥有字节全被删时等于下一段首输出位置），span 做坐标平移
（orig/out 加该段拥有起点与已提交输出长度）；被删缝跨段删除区两端取相同映射值（§2），
拼接无缝。修正范围有上界 C：待定状态只有「一个 `\r`」或「≤wsLimit 个空白 + 其后两字节」。
