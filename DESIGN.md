# 设计推导

## 1. 空行与单列空值
空行（两个换行紧邻，或文件开头的换行）视为**记录分隔符相邻**，按 RFC4180 习惯**跳过**，不产出记录；否则单列文件无法区分“分隔符”与“数据”。那么单列中“唯一字段为空的记录”必须与空行可区分：用**加引号空字段 `""`** 表示数据，未引号空字段（长度 0）在单列中等价于空行被跳过。多列中的未引号空字段照常产出。
回写推论：单列空值记录若写成空串加换行，会被下次解析成空行而丢失，故必须写 `""\n`。多列不需要引号也能往返。
**规范输入**：行尾仅用 `\n`（引号内可含 `\r\n`）；不允许引号外 `\r\n` 行尾；无空行；单列空值必须写作 `""`。此集合满足 `Write(Parse(x))==x`。引号内 `\r\n` 原样保留；引号外 `\r\n` 行尾规范化输出为 `\n`，故不属于规范输入。

## 2. par 切点
每段无法知道自己是否在引号内，故对每段做**两次模拟**：OUT（假设起点在引号外、字段开头）与 IN（假设起点在引号内，且段首前一状态是“刚见引号”，因为歧义只在闭合引号被切开时产生）。两段均携带全局字节基址 base，事件偏移为绝对偏移，记录号/字段号用段内相对号。
拼接从第 0 段（确定 OUT）开始：上一段 EOF 状态决定下一段选哪条假设。
- 边界在引号外：选 OUT；上一段未闭合字段与本段首字段是同一字段，丢弃 OUT 首 cell，把字节区间并入前一字段。
- 边界在引号内：选 IN；段首开引号在上一段，IN 首个 cell 是同一延续字段，丢弃并合并。
- 边界恰在 `""` 中间：上一段结束于“引号内刚见引号”，下一字节若为 `"` 需判定转义还是闭合。IN 先数本段开头连续 `"` 个数：奇则转义（留引号内），偶则闭合，状态在首个非 `"` 前确定；只重扫该 quote-run，长字段不重扫，无双倍退化。
- 边界在 `\r\n` 中间：上一段为 CR 待定。本段首字节 `\n` 则行尾成立（拼接时合成 CRLF 事件），否则孤立 CR 错误；不重扫整段。
坐标换算：绝对偏移 = 段内偏移 + base；段先产生 rec0（切前的记录）相对号，其后记录全局号 = 相对号 + 已闭合记录数；字段号随全局字段计数延续。宽度错误只由最终组装器统一判，段内不判，避免跨段重复。

## 3. CR 待定
未引号字段中 `\r` 不立即判错：记录 CR 待定态与该 `\r` 偏移。续传下一字节：`\n`→行尾（行尾偏移记 `\r` 处，长度 2）；其他→孤立 CR 错误。Close 仍在 CR 态→孤立 CR（流不能以裸 CR 结束）。引号内 `\r` 是普通内容，无此态。par 处理见第 2 节。

## 4. 上限“立刻”
字段按**逻辑字节**计数：进入引号后 `""` 两个输入字节只让计数加 1（第二引号处加 0）；未引号字段每字节加 1。每个字节到达立即检查，超过 Max 即错。字段数在字段开始时检查；记录数在记录闭合时检查。命中即终态，后续 Feed 返回同一错误。

## 5. 状态转移表
状态：S 字段开始 / B 未引号中 / Q 引号中 / G 引号内刚见引号 / C CR 待定。输入类：逗号、引号、CR、LF、普通。
| 现态 | 输入 | 次态/动作/错 |
|---|---|---|
| S | 普通 | B，开未引号字段 |
| S | 引号 | Q，开引号字段 |
| S | 逗号 | S，发空字段 |
| S | LF | S，空行：跳过不发记录 |
| S | CR | C |
| B | 普通 | B |
| B | 逗号 | S，发字段 |
| B | 引号 | 错：未引号字段中出现引号 |
| B | LF | S，发字段+发记录 |
| B | CR | C |
| Q | 普通/逗号/LF/CR | Q，内容（CRLF 原样） |
| Q | 引号 | G |
| G | 引号 | Q，追加一个引号（计数+0） |
| G | 逗号 | S，发字段 |
| G | LF | S，发字段+发记录 |
| G | CR | C（引号字段行尾待定） |
| G | 普通 | 错：闭合引号后跟非法字符 |
| C | LF | S，发字段+发记录（CRLF） |
| C | 其他 | 错：孤立 CR |
EOF：S→无输出（空流）；B/G→发字段+发记录（无尾换行）；Q→错：引号未闭合；C→错：孤立 CR。

## 6. 复杂度
流式 bytesProcessed 每字节恰好 +1。par：每段字节被 OUT/IN 各处理一次（合计 2N），另加仅 quote-run 边界的重扫（每段至多一段，长度受切法影响，不超过常数级段数），断言上界 ≤2N+8K。
并发说明：单个流式解析器实例非并发安全；par 内部 K 个 goroutine 只写各自独立结果，拼接在全部完成后单线程进行。
