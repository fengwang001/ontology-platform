# 设计推导（流式 UTF-8⇄UTF-16 转码）

## 1. 非法单元切分规则（从七样例推出）

首字节决定本单元期望长度 n(2/3/4) 与第二字节合法区间 [lo,hi]。逐字节读：

- 第二字节不是 continuation，或虽是 continuation 但不在 [lo,hi]：**只吞首字节 1 个**，第二字节重新解析（独立 cont 各成一个非法单元）。
- 第二字节在区间内，但其后某字节不是 continuation（含 EOF 缺失）：非法前缀整体丢弃，长度 = 已收字节数（`E2 82`+EOF：2 字节→1 个 FFFD）。
- 读满 n 字节则为一个合法标量。

统一规则：**首字节非法（`C0 C1 F5..FF` 或独立 `80..BF`）、或第二字节不在窄区间时，只吞 1 字节；前缀已合法建立后中途断开时，整个已收前缀作为一个非法单元**。
校验七样例：`F0 90 80 41`：F0 90 在区间、80 合法，第 4 字节 41 非 cont→前缀 3 字节整体 1 FFFD，41 保留；`E0 80 80`：E0 要求 A0..BF，80 越界→只吞 E0，两个 80 各自独立→3；`ED A0 80`：ED 要求 ≤9F→只吞 ED，A0、80 各独立→3；`C0 AF`：C0 只吞 1，AF 独立→2；`F4 90 80 80`：F4 要求 ≤8F→只吞 F4，90/80/80 各独立→4；`80 80` 各 1→2；`E2 82`+EOF：合法前缀断开→2 字节 1 个。

## 2. 第二字节合法区间与吞字节表

| 首字节 | 第二字节区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | （无，独立） | 1 |
| C0 C1 F5..FF | （无） | 1 |

注：表中「非法时吞掉」指首字节/第二字节判定失败时只吞 1；合法前缀建立后中途断开则吞掉整个前缀（2..4 字节），是仅有的多字节非法单元。

E0/ED/F0/F4 的窄区间分别排除非最短（E0 80..9F、F0 80..8F）、代理（ED A0..BF）、越界（F4 90..BF）。

## 3. 切分缓存上限与何时立即判非法

UTF-8 最长 4 字节，状态机最多缓存 1 个首字节 + 2 个已收 cont（第 4 字节到达即结案），**硬上限 3 字节**。第二字节到达即查区间，非法立即结案并吞掉后续 cont，不等 EOF；只有「首字节（+合法第二字节）后再无字节」才必须等 Close。UTF-16 缓存硬上限 1 字节（奇偶），外加至多 1 个待配高低代理。

## 4. par 切点对齐

重开始点只有两种字节：ASCII（<80）或首字节（>=C2）。段从 off 起，若落在 cont(80..BF) 上则推进到首个 ASCII/首字节 r。`[off,r)` 是前一段未结案单元的尾巴：前段把解析范围延到 r；其 next 严格递增，若 r≤前段 next 则本段为空（输出归前段）。每个单元恰好覆盖一次，故单元数、FFFD 数与单线程相同。
`C3 | 80 80`：切点后 r 越过两个 80 到段尾，C3 80 被前段整体解析为 é，最后 80 独立→1 FFFD，与单线程一致。BOM 只在 offset 0（段 0）识别；严格错误按全局 offset 取最早。检查次数：除边界至多回看 3 字节外每字节只查一次，故 ≤ 输入字节数 + 8×3。

## 5. 第 9 条断点

输出前先按编码长度（UTF-8 1..4，UTF-16 2 或 4）判上限：放不下则不结案该标量，返回 ErrOutputLimit。`Write` 返回的 n = 本次输入中已结案字节数；切分缓存里的字节（上次遗留或本次末尾未结案）**不算已消费**，调用方需把 n 之后的全部字节重投新实例。严格错误同理；错误 offset 基于「已喂入流的总字节（含遗留缓存）」。
