# DESIGN：结构化日志脱敏与一致性采样

仅用标准库；状态仅存进程内存；输出为自描述长度前缀文件。五个包：`record` `rule` `mask` `sample` `sink`，外加 `cmd/demo`。

## 1. 一致性采样推导

目标：同一 trace 的记录要么全留要么全丢。若对每条记录独立掷骰（随机数、时间、到达顺序），同 trace 两次决策相互独立，链必断。
因此决策函数只能依赖 trace ID：`keep = hash64(traceID) mod 10000 < rate*10000`，FNV-1a 一次哈希，与记录内容、顺序、时间无关；天然 O(1)，
`sample.Sampler` 内非导出计数器记录哈希次数，单次 `Decide` 恒为 1。rate=0 全丢、rate=1 全留。
无/空 trace ID 无法归链，采用保守策略：**无条件保留**（宁可多留，不丢无法归属的记录），文档化并测试。

## 2. error 强制保留与「链不完整」标记

先算链决策 D（仍需哈希一次以判定完整性），再按级别：
- level >= error：Keep 恒为 true；当且仅当 D=丢 时置 `Incomplete=true`（链被采样丢弃、仅错误记录被捞回，链不完整）。
- level < error：Keep=D，不带标记。
故标记只出现在「链丢 + error 强制保留」一种情形。

## 3. 嵌套路径与点号消歧

路径按 `.` 分段，沿映射逐层下降；数组对「剩余路径」广播到每个元素；值规则扫描全部叶子。
路径不存在：`(nil,false,nil)`；某层中间值不是映射：返回 `ErrPathNotMap`（该规则不适用，非致命）。
**消歧规则（长字面量优先）**：在任一层，若当前映射存在与「剩余整串」完全相等的键，则按字面键命中并终止；
否则按第一个 `.` 切分下降。即根映射同时存在键 `"a.b"` 与嵌套 `a.b` 时，规则 `a.b` 命中字面键 `"a.b"`。
该规则确定、可测，消除「字段名含点」与「嵌套路径」的歧义。

## 4. 脱敏

三种动作：Replace（固定串 `***REDACTED***`，不保留长度）；Hash（SHA-256 十六进制，同值同果、不同值不同果、不可逆）；
Truncate（保留前 N 个 rune，附 `...<truncated>`，未超限不标注）。
值规则（编译后的正则）对所有字符串叶子生效（含数组元素、被引用字段），故同一敏感值出现在多处会全部命中。
处理在深拷贝上进行，非目标字段类型与值不动，序列化后逐字节一致。

## 5. 规则预编译与复杂度

路径规则编译成路径段 trie（根节点额外挂整串字面量表）。遍历记录时每个字段只在当前 trie 节点做一次 map 查找，
路径匹配次数 = 被访问字段数 + 常数，界为 `记录数*(字段数+常数)`，与规则数无关。非导出计数器 + 导出只读访问器供测试核验。
同一路径/同一值模式配置不同动作 = 编译期 `ErrConflict`，错误指出两条规则名。

## 6. 记录校验

`record.New` 遍历构造时：深度（根映射为 1，嵌套映射/数组叠加）超过 `MaxDepth=32` 返回 `ErrDepthExceeded`；
用 map 指针集合检测自引用/环，命中返回 `ErrCycle`。均不递归爆栈、不死循环。

## 7. 落盘格式与损坏分类

文件 = 头 `"ONTLOG1\n"`（8 字节）+ 逐记录 `[4字节BE长度][JSON体][4字节BE CRC32-IEEE(体)]`。
读取按最大可恢复前缀恢复，截断点分四类（errors.Is 判定）：
头不足/魔数错 `ErrHeader`；长度前缀不足 4 字节 `ErrLenPrefix`；体不足 `ErrBody`；CRC 字节不足或校验不符 `ErrCRC`。

## 8. 并发

匹配/脱敏为纯函数式深拷贝处理；唯一可变状态是计数器，用原子操作。同批记录并发与串行输出按规范 JSON 排序后逐元素相同。

## 9. 错误体系

哨兵错误：`record.ErrDepthExceeded`、`record.ErrCycle`、`rule.ErrConflict`、
`sink.ErrHeader/ErrLenPrefix/ErrBody/ErrCRC`，全部可用 `errors.Is` 区分。

## 10. 文件预算

6 个产物 .go（5 包 + demo）+ 3 个按包合并的表驱动 _test.go（mask 测试并入 record 侧、sample 并入 rule 侧、
并发端到端在 sink 侧），共 9 个 .go 文件，满足 5–9 上限；每文件 ≤200 行。
