# DESIGN — 流式行尾/空白规范化

## 1. 整体模型

五个包单向依赖：`eol`/`ws`/`span` 无依赖；`norm` 依赖三者；`par` 依赖 `norm`/`span`。
规范化器逐字节吃输入，输出只追加。映射用「片段链」记录，片段三种：
identity(orig→out 等长平移)、delete(原文有/输出无)、insert(原文无/输出有)。
相邻同类型片段合并，故片段数只随「删除点」个数增长。

## 2. 空输入与只含换行的输入

三策略记 K=保留原样、E=确保恰好一个、T=删全部末尾空行但保留一个换行。

| 输入 | K | E | T |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

推导：规范化后内容只可能是「非空且以 `\n` 结尾」或空串。
- E 对空输入选 `""` 而非 `"\n"`：若空→`"\n"`，则对 `"\n"` 再跑仍 `"\n"`（幂等成立），
  但会凭空制造一行，且 N("")≠"" 破坏「空是规范化态」这一自然期望。选 `""` 后，
  E 仅在内容非空时把末尾压成恰好一个 `\n`，三种输出 `""`/`…\n` 均为不动点，故幂等。
- K：末尾整体（全是 `\n` 的尾段）原样；内部规则照常（去尾空白、`\r`→`\n`）。
- T：末尾连续的仅由 `\n` 组成的空行全删，再保证恰好一个 `\n`；空串保持空串。
  E 与 T 的唯一差异正是 `"a\n\n"`→E=`"a\n\n"`、T=`"a\n"`；样例四者中二者相同。

## 3. 被删字节的 ToOut

不变量：ToOut 单调不减；对每个输出偏移 o 有 ToOut(ToOrig(o))==o。
一串被删字节在输出里没有任何位置，只能夹在两侧保留内容之间：设输出换行符偏移为 o。
- 若给删除串取严格小于 o 的值（ToOrig 后落在前面 identity 内），随 i 增大 ToOut
  会先升后降（例 `a   \n`：0→0,1→?），破坏单调不减。
- 故删除串上所有 i 都取「删除后最近的输出位置」o；`\r\n` 的 `\r` 同理取后续 `\n`
  的偏移 o。于是 ToOrig(o) 必须取该输出字节真正对应的原文位置（`\n` 的位置），
  round-trip 为 o。编辑器光标落在被删空白里 → 映射到行尾换行符**之上**（之后），
  符合「换行后开始新行」的直觉；被删空白紧贴 EOF 时无后续输出字节，取 len(out)。
ToOrig 在 delete 片段上回退到片段起点的原文位置（删除前最后保留字节），单调不减。

## 4. 缓冲超限（第 8 条）

待定空白若「强制判定为非行尾」输出，则之后出现 `\n` 时这些空白无法收回，
破坏语义 2/幂等（第 5 条），且映射被迫把 identity 改判，违反切分一致性（第 4 条）。
故选择**拒绝**：返回哨兵错误 `ErrSpaceLimit`（带触发字节的原文偏移），
已输出内容与映射保留，实例进入终态，再写返回 `ErrClosed`——错误可判定、状态可解释，
第 4/5/6 条均不受影响。输出总字节超限同理：`ErrOutputLimit`，立刻拒绝并进终态。

## 5. par 拼接

段首不知道上段末尾是否悬着 `\r` 或一串尾空白。让每段独立用 `Keep` 策略规范化，
只传入两个边界提示：`prevLast`（上段末字节，仅取 `\r` 才有意义）与
`nextFirst`（下段首字节）。段内据此：①段首 `\n` 且 `prevLast=='\r'` 时该 `\n`
属对端 `\r\n`，删之；②段末判定悬空白时，若 `nextFirst` 不是行尾字节则提前放行
（identity），是行尾则照常删除（删除在本段完成）。修正只看 1 个边界字节
（`\r\n` 只跨 2 字节；空白是否行尾只需看下一字节），故修正范围上界 = 1 字节。
段映射平移：orig 加段起点偏移、out 加此前各段输出长度之和（含 insert/delete 差）。
拼接后对全局输出与合并后的映射统一执行一次 K/E/T 末尾策略，即与单流逐位一致。
