# LZ77 流式格式设计

## 关键推导

1. **Write 切法无关**：压缩器只把“已被后一字节证明结束”的匹配交给编码层。若当前匹配一直延伸到暂存区末尾，后续字节可能继续延长它，因此从该位置起全部暂缓；下次 `Write` 看到新字节后再比较。无匹配位置可逐字节升格为字面量，故正常只缓存末尾少量字节，周期数据会缓存从当前长匹配开始的后缀。边界本身不产生记录，所以任何切法得到同一状态机轨迹。`Flush`/`Close` 表示再无立即到来的字节，必须强制结束并输出当前匹配；否则接收方无法还原完整前缀。
2. **重叠复制**：距离 `d`、长度 `n>d` 时，源区间与目标区间重叠。块复制要求两块不重叠，可能读到尚未搬运的新值；逐字节 `out[t]=out[t-d]` 会自然复用刚写出的字节，形成周期展开。这里每个回指本来就要逐字节更新窗口、计数、校验和及输出上限，单次复制代价就是输出规模本身；因此直接采用逐字节复制，不额外引入向量化快路径。
3. **两类距离错误**：`距离>已输出`说明回指尚无生命周期内的源字节；`距离<=已输出 但>窗口容量`说明字节曾经存在却已超出固定记忆。二者安全含义不同，故分开。压缩器只向仍在环形窗口中的候选位置发回指，且候选链会淘汰越界位置，所以正常流绝不可能产生第二类。并行块的预置字典在该块开始前已经进入窗口与匹配器；对解压端它也是前面块真实输出的字节，“已输出字节数”含义不变。
4. **并行确定性**：块 `k` 的状态只由本块原文和上一块末尾至多一个窗口的原文构造，不读取任何块的压缩记录、哈希选择或完成顺序；哈希、链上限、切块边界均固定。各 worker 只返回编号结果，主 goroutine 按编号拼接。块间无需刷新标记：记录流连续，解压窗口自然跨越块边界；整个文件只写一个流头和一个汇总流尾。

## 字节布局

| 记录 | 布局 |
| --- | --- |
| 流头 | `"ONLZ"` + 版本 `1`，共 6 字节 |
| 字面量 | tag=`0` uvarint，长度 uvarint，原始字节 |
| 回指 | tag=`1` uvarint，距离 uvarint，长度 uvarint |
| 刷新 | tag=`2` uvarint |
| 流尾 | tag=`3` uvarint，原文总长 uvarint，FNV-1a 64 校验和 uvarint |

所有整数使用无符号 LEB128/uvarint；解码器在第 11 个续接字节或 64 位溢出时报变长整数错误，在流提前结束时报截断错误。
