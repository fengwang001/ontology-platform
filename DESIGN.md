# DESIGN

## 1. Write 切法无关（语义 3）
压缩器把 `Write` 的字节只追加进未决缓冲与历史窗口，**不**立即产出。
只有 `Flush` 时，才把“上次 Flush 之后的整段区间”交给匹配器做一次完整的
贪心扫描：在位置 i 向窗口要当前最长匹配，取它（minMatch=3）或输出一字面量。
于是一个 Flush 段内的全部决策都基于同一份完整段原文，按字节位置确定，
与 `Write` 被切成几段、每段多大完全无关。边界问题因此不存在：扫描时位置 i
之后的字节都已在缓冲里，匹配可任意跨越历史 `Write` 边界。
代价是 Flush 之前没有压缩输出；`Flush` 的承诺正好要求“截至此刻的全部输入
此刻可还原”，所以 Flush 必须强制结束当前匹配、把未决段整体编码出去，
并追加一个刷新标记，解压器见到标记即把已解码数据交给输出。

## 2. 重叠回指的复制（语义 2）
距离 d < 长度 L 时，源字节位于“尚未复制完的目标区”，源随复制推进而增长。
块复制按地址不重叠语义只搬运一次：abc + d=3 + L=6 会复制出
abcabc??（??是旧内存），应为 abcabcabc。逐字节循环 `dst[j]=src[j-d]`
每写一个目标，下一次读就能读到它，天然得到周期展开。本项目输出落在环形
窗口里，直接对每字节调 `Put(b)`，再从窗口按距离 `ByteAt(d-1)` 读下一个
源；全程无额外大分配，逐字节读写窗口即可，这里逐字节是正确且可接受的。

## 3. 两类“距离过大”（语义 7）
距离有两个独立上界：① d ≤ 已输出字节数（流前缀里有没有那么多历史）；
② d ≤ 窗口容量（历史还在不在窗口里）。先判 d==0，再判 ①，再判 ②，
给出三个不同错误。压缩器在“当前绝对位置 - 候选绝对位置 ≤ 容量”时才发出
回指，故自身流中 ② 永不发生；但解析不可信流必须独立拦截。并行块以
“上一块原文末尾窗口容量”预置窗口：解压仍是单条线性流，预置字典只是
压缩侧的优化，输出仍逐字节增长，因此“已输出字节数”的含义不变，
上一块的历史同时存在于窗口与已输出流中，两类检查照常成立。

## 4. 并行块的确定性（语义 9）
块 k 的输入仅为 block[k-1] 的末尾窗口（预置字典，无则空）与 block[k]
原文；匹配是对这两段的纯函数，不读任何其他块的输出，也不读共享可变状态
（每块独立 window/matcher/编码器，无全局可变表）。因此结果与 worker 数、
完成顺序无关；块按序拼接即可。块间**不需要**刷新标记：块只是压缩器内部
的调度单位，流仍是一个段，回指自然跨块。流头只写一次，流尾在全部块后写一次。

## 5. 流格式（多字节整数一律 LEB128 无符号变长整数）
| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `LZ7` `7A`，版本 `01`，varint 窗口容量 |
| 字面量 | tag `00`，varint n，随后 n 个原文字节 |
| 回指 | tag `01`，varint 距离(≥1)，varint 长度(≥1) |
| 刷新标记 | tag `02`（无载荷） |
| 流尾 | tag `03`，varint 原文总长度，varint CRC32(IEEE) |
校验和覆盖原文全部字节（含重叠展开后的字节），增量维护。
