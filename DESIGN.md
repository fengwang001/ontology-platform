# RLE 设计说明

## 严格解码器拒绝清单推导

不变量：所有被接受的 `t` 都必须满足 `Encode(Decode(t)) == t`。

1. 显式次数 `1a`：解码为 `a`，重编码为 `a`，不等于 `t`。
2. 次数 `0a`：游程必须至少有一个符号；重编码也不可能产生 `0`。
3. 前导零 `01a`：合法十进制只写 `1` 且 `1` 省略，重编码为 `a`。
4. 相邻同符号 `2a3a`：解码得到 `aaaaa`，最长游程重编码为 `5a`。
5. 非法转义 `\x`：格式只允许 `\\` 与 `\数字`，且解码后没有可往返的码点。
6. 末尾孤立 `\`：转义字符缺失，不是完整游程。
7. 末尾只有次数 `12`：次数后必须有符号，流结束说明编码截断。
8. 非法 UTF-8：解码输出要求是合法 Go 字符串；替换字节会改变输入，拒绝才能保持往返。

所有错误都报告**输入编码文本中的字节偏移**；流式输入偏移从第一次 `Write` 起累计。

## 大次数取舍

选择拒绝超过 `int` 上限的次数：`MaxInt = strconv.IntSize` 位有符号最大值。

理由：次数表示输出中实际码点个数，任何超过地址空间的游程都不可能在本进程中物化为 `string` 或交给 `io.Writer`；在 `int64` 可表示但仍过大时，也由输出总字节上限拒绝。数字逐位更新并在乘法前检查 `n > (MaxInt-d)/10`，杜绝静默溢出。输出扩容前检查 `len(rune 字节)*n + 已产生字节`，默认上限 1 MiB，可用 `MaxOutputBytes` 配置；因此不会按次数预分配。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
