# DESIGN

统一格式差分/渲染/应用器的四处推导（题面第三节）。

## 1. 零计数 hunk 头的行号规则

从三个样例推导：

- `x\n` → `y\nx\n`（C=0）头为 `-0,0 +1`：插入发生在旧文件第 1 行**之前**，此时插入点之前没有任何行，记 0。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）头为 `-2,0 +3`：插入发生在第 2、3 行**之间**，记 2。
- `a\n` → 空（C=3）头为 `-1 +0,0`：新文件侧同理，删除后"插入点"在文件头，记 0。

结论：计数为 0 时，数字是**插入点之前最后一行的 1-based 行号**，而不是将要写入的位置——因为"行与行之间的间隙"本身没有行号，GNU diff 用间隙前一行的行号来指代它；间隙在文件头时没有前一行，用 0。计数 ≥1 时，数字是区间首行的 1-based 行号。`Apply` 解析这种头时：计数为 0 则插入点 = 0-based 下标 `a`（即第 `a` 行之后），计数 >0 则起点 = 下标 `a-1`。

| 情形  | 头 `-a,b` 的写法               | `Apply` 对插入点的解释（0-based） |
| ----- | ------------------------------ | --------------------------------- |
| b = 0 | a = 前一行的行号（文件头为 0） | 下标 a（第 a 行之后）             |
| b = 1 | 写 `-a`（省略 `,1`）           | 下标 a-1                          |
| b ≥ 2 | 写 `-a,b`，a = 首行行号        | 下标 a-1                          |

## 2. hunk 合并阈值 = 2C

推导：前一段改动带 C 行尾上下文，后一段改动带 C 行头上下文，两段改动之间隔 g 行未改动行。当 g ≤ 2C 时，尾上下文与头上下文相接或重叠，GNU diff 输出**一个** hunk；当 g = 2C+1 时，中间恰有 1 行不属于任何一侧的上下文，输出**两个** hunk。故阈值为 2C。

反证另外两个取值：若阈值取 C，则 g 在 C+1..2C 之间时两侧上下文已经相接却仍被拆成两个 hunk，与 GNU 输出不符；若取 2C+1，则 g = 2C+1 时两段上下文之间存在未被覆盖的行，GNU 输出两个 hunk 而实现却合并，同样不符。例：C=3、g=6 时尾上下文覆盖 g1..g3、头上下文覆盖 g4..g6，正好相接 → 合并；g=7 时 g4 无人覆盖 → 拆分。

## 3. 最短脚本的并列选择：优先删除

Myers 算法中，当 `V[k-1] == V[k+1]`（向下走与向右走同样短）时，固定选择从 k-1 走来，即**优先删除、后插入**。效果：同一 hunk 内所有 `-` 行排在 `+` 行之前。例：`a b` → `b a` 输出 `-a`、` b`、`+a`，而非 `+b`、` b`、`-b`。该规则写死在 `edit.Diff` 的分支条件（`<` 而非 `<=`）里，回溯时用同一条件，因此同一输入永远得到同一份补丁文本。

## 4. 偏移应用后的查找起点平移

维护 `shift`，初始 0。第 i 个 hunk 的记录位置为 r_i，实际应用位置为 p_i，则

```
shift_{i+1} = (p_i - r_i) + (new_i - old_i)
```

其中 old_i/new_i 是该 hunk 旧侧/新侧行数。理由：后续 hunk 的行号都是相对**原文件**的；之前 hunk 的净增行数（new-old）会把后续内容整体后移，必须累积；而 `p_i - r_i` 是目标文件相对补丁假设的系统性漂移（例如目标文件头部多了几行），上一个 hunk 的实测漂移是下一个 hunk 位置的最好先验。因此下一个 hunk 以 `r_{i+1} + shift_{i+1}` 为中心、在 ±F 行内搜索精确匹配。
