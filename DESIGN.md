# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（合法编码唯一、即规范形式）。

## 拒绝清单（若接受，则该 t 违反不变量）

1. 显式次数 1：`1a` 解码为 `a`，而 `Encode("a")="a" != "1a"`。
2. 次数 0：`0a` 解码为空片段；且规范编码从不产生 0 次游程，`Encode("")="" != "0a"`。
3. 前导零：`01a`（或 `001a`）的值为 1，其规范写法是 `a`；`12a` 的 0 次重复不存在，`012a` 规范为 `12a`。故任何 `0` 开头的多位/单位数字均非法。
4. 相邻同符号游程：`2a3a` 解码为 `aaaaa`，规范编码是 `5a`；最长游程要求合并，故必须在看到下一游程符号时拒绝（偏移指向下一符号）。
5. 非法转义：`\x` 中 x 非数字非 `\`，符号无定义；编码端只产生 `\`+数字/`\`+`\`，故不规范（偏移指向 x）。
6. 末尾孤立 `\`：后面没有符号，游程不完整（偏移指向 `\`）。
7. 末尾只有次数：`12` 缺符号，不是完整游程（偏移指向首个数字）。
8. 非法 UTF-8：解码目标是合法 Go 字符串，但 Encode 遍历非法字节会得到 RuneError，无法重现原字节（偏移指向非法首字节）。

## 大次数取舍

选择**支持任意大次数**（内部用 `math/big.Int` 累加十进制，不经过固定宽整数，杜绝溢出静默）。
理由：数字串长度受输入本身限制（1 MB 输入至多 1 MB 数字），big.Int 只在输入规模上增长；输出侧不预分配，按当前输出长度逐块倍增、每块前检查配额，绝不分配与次数成正比的整块内存。
输出总字节上限可配置（`WithLimit`，默认 `math.MaxInt`），超限返回哨兵 `ErrLimit`（可判定）且不产生输出。

## 语义—测试对照表

| 语义（第三节） | 测试函数 |
| --- | --- |
| 1 往返 | `TestRoundTrip` |
| 2 规范形式/严格解码 | `TestRejectTable` |
| 3 大次数/上限 | `TestBigCount` |
| 4 最长游程/不规范化 | `TestLongestRuns` |
| 5 跨切分点一致 | `TestAllSplits` |
| 计数器 == 输入字节数 | `TestByteCounter` |
