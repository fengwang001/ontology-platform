# DESIGN.md 属性级权限过滤器

## 1. 不可见列出现在谓词中的三种做法

谓词引用当前角色不可见的列时，有三种常见处理方式。

- 甲：把不可见列的比较当假。泄露路径：谓词 `NOT (secret = 1)` 中比较被当假，
  `NOT` 后为真，返回行。当真实数据 `secret=1` 时该谓词本应为假，调用方据此反推出
  secret 的值/分布。比较当假本身就是对不可见数据的断言。
- 乙：把不可见列当 NULL 走三值逻辑。泄露路径：`secret IS NULL` 被求值为真并返回行，
  等于声明「该列存在」；配合 `NOT (secret = 1)`（NULL 比较为 UNKNOWN，NOT 后仍
  UNKNOWN，即不返回），真假两种返回都能当探针，逐行区分 secret 是否为 NULL。
- 丙：整条查询拒绝，并指出列名与谓词树路径。查询不执行，任何行都不返回，调用方除
  「存在此列、策略不允许」外学不到任何数据值。故本实现采用丙。

列名与路径本身不属数据值（它们是查询与策略的产物），写进错误与报告不构成数据泄露。

## 2. OR 短路例外（定义与理由）

定义：常量折叠与拒绝检查在同一遍后序遍历中完成。节点折叠成常量（true/false/unknown）
后记录该常量子树引用过的不可见列。当且仅当某不可见列**仅**出现在 OR 的一支中、而该
支折叠为常量 true 或常量 false 时，豁免该引用：

- 支折叠为 false：`false OR X` 恒等于 X，该支对结果无贡献，列未被读取。
- 支折叠为 true：`true OR X` 恒等于 true，另一支不求值，列未被读取。

例外严格限定为「OR 的一支」：AND 下的常量 false 不豁免（拒绝策略以整查询是否表达了
对不可见列的引用为准，而非仅看最终相关性；这样规则可审计、一遍可算，且不放宽权限）。
`OR(secret, TRUE)` 中 secret 支不是常量，仍拒绝；不做常量提升/重排，只按语法位置折叠，
保证行为可预测。

## 3. 数据模型与路径

- Policy：角色 -> 可见列集合。未知角色视为空集（最小权限）。
- Predicate 树：Cmp（列、算子 OpEq/OpNotEq/OpIsNull/OpIsNotNull、可选常量值）、
  Bool（AND/OR/NOT）、Const（三值 true/false/unknown）。
- 路径：从根到节点，段为 `and[0]`、`or[1]`、`not`、`cmp(secret)`、`const`；
  根路径为 `$`。

检查阶段不持有数据行，因此只有纯常量子树能折叠成 true/false；豁免在「常量折叠后」的
含义是：OR 一支为纯常量表达式时，该支内不可见列引用被豁免（此时谓词语法上可不含数据
列；为使例外可测，允许常量子树中夹带不可见列引用——折叠为常量意味着它对结果无贡献）。

## 4. 错误（errors.Is 可区分三类）

- `ErrQueryRejected`：整查询被拒（丙）。
- `ErrInvisibleColumn`：谓词引用不可见列，错误文本含列名与全部路径。
- `ErrInvalidPredicate`：谓词树结构非法。
拒绝错误由前两者 Join 而成，故 `errors.Is(err, ErrQueryRejected)` 与
`errors.Is(err, ErrInvisibleColumn)` 同时成立。

## 5. 行裁剪与报告

- 结果行只输出可见列：遍历可见列集合，从源行取值拷贝，不可见列物理上不出现在结果中
  （不是置零/置 NULL，使「不可见」与「值为空」可区分）。
- Report：被移除列名集合（字典序）、拒绝引用（列名 -> 全部出现路径，按列名排序）、
  是否整条拒绝；`Text()` 确定性序列化。

## 6. 复杂度

- 谓词检查一遍后序遍历，每节点恰好访问一次；filter 持有非导出计数器 `visited`，测试
  断言其等于树节点总数。
- 行裁剪只迭代可见列：1000 列、5 可见时拷贝计数 ≤ `4*5=20`（实测 5）。
