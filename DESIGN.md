# 设计：带背压的流式聚合管线

链路：source -> parse(过滤坏记录) -> group(分组聚合) -> sink(落盘)。
多级有界队列承载在途记录，状态全在进程内存，只用标准库。

## 一、「已消费位置」的定义（核心推导）

设 source 读取位置为 r（下一条待读偏移），sink 已原子落盘位置为 d。

- 若检查点记 r：崩溃后从 r 重放，已被 source 读出、压在各级队列却未
  落盘的在途记录（d..r 间尚未持久化的部分）永久丢失。
- 若检查点记 d：恢复后从 d 重放，但 d..r 间已经落盘的部分会被再次
  聚合/输出，产生重复；单凭「落盘位置」分不清队列里哪些已持久化。

正确做法：引入屏障 barrier（携带 source 偏移 b），按 FIFO 流经全部
队列。屏障到 sink 时保证：偏移 < b 的记录要么已落盘，要么此刻在
sink 手中同批原子写出，即「b 之前各阶段在途集合为空」。检查点记为

    checkpoint = (consumed=b, groups=各组中间聚合, bad=坏记录数)

提交顺序：先原子改名输出文件，fsync 后再写检查点。三种崩溃：
- 改名前：输出与检查点都是旧的，整体重放，结果幂等；
- 改名后、检查点前：输出超前检查点，恢复时从输出文件尾行的 barrier
  标记与各组聚合值重建 groups、把 consumed 提到 b，不重放不重复；
- 检查点后：检查点已新，直接续跑。
检查点只在屏障点（每 B 条及结束时）生成；优雅停止也注入结束屏障，
排空全部在途记录。

## 二、背压与资源约束
每级是容量 c 的有界 channel，上游发送阻塞即背压。历史最大在途数只
统计 channel 驻留数（worker 手中在制品不计），故峰值 <= 各级容量之和。
分组 map 键数超上限立即返回哨兵错误并优雅停止，不静默丢组。

## 三、故障处理
- 坏记录：解析失败 bad++ 且不进任何分组，管线继续；缺键即坏记录。
- source 中途报错：已排空部分照常落盘+检查点，错误如实上报。
- sink 半截临时文件：*.tmp 启动即按未完成清理；合法输出带 checksum，
  任何字节截断都判无效，绝不当有效输出读回。
- 检查点损坏：保留最近两份（ckpt.1 新、ckpt.2 旧），校验和不符回退
  旧份，并在 Report.FellBack 标注。

## 四、并发与停止
每级一个 worker goroutine。Stop 用 sync.Once 幂等：注入结束屏障 ->
逐级关闭 -> WaitGroup 等待，不留悬挂 goroutine。故障注入的「崩溃」
走独立 kill 通道，模拟进程突然死亡，不排空。

## 五、文件格式（确定性，便于逐字节比对）
输出按 key 字典序的文本行 `<key>\t<sum>\t<count>\n`，尾行
`#barrier <offset> <bad> <sha256hex16>\n`；先写同名 .tmp（含校验），
fsync 后原子 rename。检查点为 JSON{consumed,bad,groups} 加换行与
sha256(hex) 尾行。

## 六、包划分
stage（有界队列骨架/计数/停止）、source（可注入脚本源）、parse、
sink（临时文件+改名+校验）、ckpt（双份检查点+回退）、pipeline
（组装与恢复）、cmd/demo（8 条判定演示）。.go 文件总计 8 个实现文件，
测试按包合并成表驱动文件（同包同类断言一张表一个测试函数）。
