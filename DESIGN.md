# 设计推导

## 非法 UTF-8 单元

采用 WHATWG 式最大子部分：若首字节合法，先按其长度收集尽可能长的合法前缀；任一后续字节落入下面的第二字节区间外，或本应是 `80..BF` 却不是，非法单元立即在该字节结束，之后从下一字节重新开始。因此：

- `F0 90 80 41`：`41` 不是 continuation，单元是前三字节，`41` 另成新字符。
- `E0 80 80`：`E0` 的第二字节必须是 `A0..BF`，`80` 立刻终止；两个 `80` 又各自非法。
- `ED A0 80`：`ED` 的第二字节必须是 `80..9F`，`A0` 立刻终止；随后两个字节各自非法。
- `C0 AF`：`C0` 非法且不允许吞后续；`AF` 独立非法，所以两个替换符。
- `F4 90 80 80`：`90` 立刻终止 `F4`，余下三个 continuation 各自非法。
- `E2 82` 在 EOF：二者构成仍可能合法的 3 字节前缀，故整体是一个截断非法单元。

| 首字节 | 第二字节合法区间 | 非法第二字节吞掉量 |
|---|---|---|
| `C2..DF` | `80..BF` | 首字节+非法第二字节；再从下一字节解析 |
| `E0` | `A0..BF` | 同上 |
| `E1..EC` | `80..BF` | 同上 |
| `ED` | `80..9F` | 同上 |
| `EE..EF` | `80..BF` | 同上 |
| `F0` | `90..BF` | 同上 |
| `F1..F3` | `80..BF` | 同上 |
| `F4` | `80..8F` | 首字节+非法第二字节 |
| `80..BF` | 不允许为首字节 | 仅当前 1 字节 |
| `C0 C1 F5..FF` | 永不合法 | 仅当前 1 字节 |

后续第 3、4 字节均须为 `80..BF`。非最短形式、代理区 `D800..DFFF`、越界都在上述区间或最终标量检查中拒绝；合法区间是 `0..D7FF` 与 `E000..10FFFF`。

## 流式缓存

只缓存“仍可能扩展为合法字符”的前缀。UTF-8 最长 4 字节，故缓存硬上限为 3；UTF-16 最长一个 code unit 加一个高代理，按字节缓存硬上限也是 3。合法前缀后的下一个字节若不能延续，无需等 EOF：立即把该前缀连同触发字节作为一个非法单元输出，触发字节之后重新解析。EOF 时仍有前缀才是“截断”，与首字节/continuation 构成的“非法字节”错误分开。

## 并行切点

每段从切点向前最多回看 3 字节，找最后一个能在切点形成待续前缀的 lead：UTF-8 可跳过最多 3 个 `80..BF`；UTF-16 可跳过 1 个 code unit 或一个高代理。该前缀不属上一段：上一段在切点抑制尚未闭合的事件；本段从回看起点解析，但只有起始或触发位置不小于切点的事件才计入本段输出。首个触发事件会消费切点前的前缀。这样 `C3|80 80` 中：下段从 `C3` 开始，先输出由 `C3 80` 构成的合法字符，再输出 `80` 的替换符；上段不输出前缀。任何跨切点的合法、非法或截断单元都只有唯一归属，单元数因此与单线程相同。

## 上限断点

lead 进入缓存时先按目标最坏大小预留输出空间：UTF-8 目标 4 字节，UTF-16 目标 4 字节；BOM 与 code unit 也先预留。事件真正写入时按实际长度提交，未达标量边界绝不写输出。`Write` 只返回本调用中既已形成事件的字节；跨调用缓存和上一段遗留前缀不算已消费。超限时命名错误带全局续传偏移；新实例从该偏移继续，输出直接拼接即等价。

## 复杂度

每读入一个新字节检查一次；旧前缀只保存状态，不重新扫描。单流检查数不超过 2N（分类判断和区间判断分开计数）。并行时每段最多回看 3 字节，总额外检查不超过 `8×常数`；每个 worker 独立写输出缓冲，顺序拼接。
