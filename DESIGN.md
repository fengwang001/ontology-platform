# 设计推导（先于代码完成）

## 1. 非法单元切分（WHAAWG 规则的字节版推导）

首字节决定该字符期望的续字节数 `n`（C2..DF=1, E0..EF=2, F0..F4=3）。
处理首字节后，对后续字节逐一检查：
- 首字节为 C0/C1/F5..FF 或非首字节(80..BF)：自身即一个非法单元，吞 1。
- 合法首字节后，第二字节必须落在下表区间；越界立刻判定前缀非法：
  只吞当前前缀（1 字节），**拒绝的那个字节重新当下一个首字节**。
- 第二字节合法、后续续字节落在 80..BF 之外：吞掉整个已接收前缀
  （2 或 3 字节，越界字节重新解析）。
- 到流结束仍缺续字节：残留整体是 1 个“截断”单元（区别于非法）。

| 首字节类 | 第二字节区间 | 第二字节非法 | 第 3/4 续字节非法 |
|---|---|---|---|
| C2..DF | 80..BF | 吞1 | — |
| E0 | A0..BF | 吞1 | 吞2 |
| E1..EC | 80..BF | 吞1 | 吞2 |
| ED | 80..9F | 吞1 | 吞2 |
| EE..EF | 80..BF | 吞1 | 吞2 |
| F0 | 90..BF | 吞1 | 吞2/吞3 |
| F1..F3 | 80..BF | 吞1 | 吞2/吞3 |
| F4 | 80..8F | 吞1 | 吞2/吞3 |
| 80..BF | — | 自身非法，吞1 | |
| C0 C1 F5..FF | — | 自身非法，吞1 | |

样例解释：F0 90 80 41：第二字节 90、第三字节 80 均在各自区间内，
41 不是续字节，前缀在 41 处关闭成一个非法单元（吞 3），41 重解析
→ FFFD+41。E0 80 80：80 不在 A0..BF，
吞 E0；80、80 各为 stray → 三个。ED A0 80：A0 不在 80..9F（代理区），
吞 ED，A0、80 各 stray → 三个。C0 AF：C0 吞 1，AF stray → 两个。
F4 90 80 80：90 不在 80..8F（超 U+10FFFF），吞 F4，90/80/80 stray
→ 四个。E2 82 + EOF：第二字节合法，残留 2 字节为一个截断 → 一个。
80 80：两个 stray。

## 2. 缓存上限与“立即放弃”

最长合法字符 4 字节，故缓存（已收首字节与合法续字节）硬上限 3。
看到第二字节越界（E0 后跟 <A0、ED 后跟 >9F、F0 后跟 <90、
F4 后跟 >8F）时前缀已不可能再变合法，立即出非法单元，无需等 EOF；
但被拒字节必须重放。只有“前缀仍合法却到 EOF”才是截断。
UTF-16：缓存上限 3 字节（半 code unit + 高代理 code unit）。

## 3. par 切点对齐

段 i 从切点 c 开始时，向前回看最多 3 字节（UTF-8；UTF-16 为 4 字节，
且按 code unit 奇偶取整）作为前缀 pre=c-m..c-1，段实际输入为
pre 加本段。解码器初始冻结（freeze=m 字节）：pre 期间只推进状态、
不出事件不计统计；冻结解除后，事件按其**起始偏移**归属——
起始 < c 的单元属于上一段（只在输入含后续段尾巴时出现：为此每段再
向后带 3(UTF-16:4) 字节“尾巴”，让跨界单元能解析完整；尾巴期间
起始 ≥ c+len 的事件丢弃）。故一个跨界单元恰由一段完整计一次。
例：C3|80 80。段0 带尾巴 80：解析出 U+0080，起始<c+1，保留。
段1 回看 1 得 C3 80（freeze=1）解出 U+0080，起始 c-1<c 丢弃；
随后 80 stray 起始≥c 归段1。合计一个合法标量 + 一个非法，与单线程同。
BOM 只在第 0 段 offset 0 检测；其余段 BOM 字节按普通前缀冻结。

## 4. 第 9 条断点

Write 按字节驱动；在处理一个**新标量首字节之前**先检查剩余容量是否
够该标量最坏输出（UTF-8 4；UTF-16 3：高代理可能凑对），不够则
停在该字节之前，返回 ErrLimit，已消费数恰好等于已完整输出标量的
输入字节。切分缓存里的字节尚未成标量，不算已消费（不进返回值，
保留在实例内）。调用方从返回偏移续传：续传实例关闭 BOM 检测
（BOM 只在流首有效），拼接与无上限逐字节相同。
