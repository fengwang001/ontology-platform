# 设计推导

模型：逐字节流式机。两个待定缓冲：pendCR（上一字节是 `\r`，等看下一字节是否 `\n`）、pendWS（行尾一串空格/Tab，等行尾或EOF才知是否删除）。
span 只记录"逐字节复制段"：段 [outStart,outEnd) ↔ [origStart,origEnd)，长度相等。删除点不记段，故段数=2×删除点数+1，与输出字节数无关。被删字节的 ToOut 落在它后面第一个复制段的 outStart（删除空隙吸附到右侧边界）。

## 1. 空输入 / 只含换行 与幂等
关键约束：ToOut(ToOrig(o))==o 要求每个输出字节都由原文字节复制而来，故策略只能删除字节、绝不能凭空追加（追加字节没有原文前驱，破坏互逆）。因此"确保恰好一个"对空输入输出 ""（无法把不存在的字节变成 `\n`）；N("")="" 幂等。
- Keep：不动末尾；Ensure：末尾 `\n` 数压成 1，无则不加；Trim：删全部末尾空行；全文非空则保留恰好一个 `\n`，全文为空白/空则输出 ""。
三种策略都只删除；N(x) 不含 `\r` 与行尾空白，故 N(N(x))=N(x)。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| "" | "" | "" | "" |
| "\n" | "\n" | "\n" | "" |
| "\n\n" | "\n\n" | "\n" | "" |
| "  \r\n" | "\n" | "\n" | "" |

## 2. 被删字节的 ToOut
复制段之间是删除空隙 (a,b)（原文 [a,b) 被删）。ToOrig 只定义在输出偏移，值域是复制段，天然互逆。ToOut 须单调不减且为 ToOrig 的左逆：空隙内每个 i 映射到空隙右侧复制段起点 o（吸附右边界）；空隙前最后保留字节映射 o-1。
`\r\n`：删 `\r`、复制 `\n`，`\r` 与 `\n` 的原文位置都映射到输出 `\n` 偏移 o；ToOrig(o)=`\n` 位置，互逆成立。光标放在被删 `\r`/行尾空白里 → 映射到换行符（行尾）而非下一行行首。

## 3. 空白缓冲超限（第 8 条）
强制判定为"非行尾"会输出本应删除的空白：破坏幂等（第 5 条，再次规范化会删掉）、破坏跨切分一致（第 4 条，切在空白中间会改变何时触发上限），故不可取。
选择拒绝：待定空白达到上限即返回带原文偏移的 ErrTrailingRunLimit，已输出保留，机进终态（再写返回 ErrClosed）。拒绝只取决于"自上个行尾以来连续空白长度"，与切分无关，第 4/5/6 条不被违反。输出上限同理：写入会使 outLen 越界即返回 ErrOutputLimit（本次字节不落机）。

## 4. par 拼接
段 j 独立跑 Open（不做EOF末尾决策），得到：已决复制输出/映射 + 尾部状态 pendCR、pendWS（原文连续空白）。拼接段 j 与 j+1 时只看下一段原文首字节 c：
- 段尾 pendCR：c=='\n' → 删掉该 `\n`（`\r` 复制成 `\n`，CRLF）；否则 `\r` 按独行尾决成 `\n`（复制段），再正常处理 c。
- 段尾 pendWS：c 是空白 → 并入 pendWS；c 是 `\n`（或经 pendCR 决成的换行）→ 整串空白删除；c 是其他字节 → 整串作为行中空白复制输出。
修正只需 1 字节前瞻 + 两段待定缓冲，修正范围有上界 O(段尾缓冲)，与段长无关。最后一段再用所选策略做全局 EOF 决策。
映射平移：段 j 的 span 段整体加 (Δorig,Δout) 导入全局 Mapper；拼接修正的删除/复制段以全局坐标补记，段总数 O(删除点数+K)。

严格模式 NUL：返回带原文偏移的 ErrNUL，已输出保留，进终态。
并发：单个 norm 实例非并发安全；par 每段独立实例 + goroutine。
