# DESIGN

## 1. 抢占额度
叶子 L：保障 g，占用 u，请求 r，全局闲置 f（总容量 − 全部占用 − 抢占在途）。
公式：`q = max(0, min(r − f, g − u))`；任务不可切分。
- 减 f：闲置可直接分配，每 1 单位闲置就少抢 1 单位；在途容量在 Ack/超时前不是闲置，不能计入 f。
- 以 g−u 封顶：L 只能强制要回属于自己保障、当前被别人占用的部分；L 占用已达 g 时无权抢占任何人。
- r 中超过 g−u 的部分属于 L 自己的借用需求，别人无义务让出，只能等闲置；抢占该部分即违反语义 4。
- 若 f+q < r，任务整体挂起（不许先分一部分，语义 6）；待其触发的抢占全部释放后，保障内部分必然满足（f+q≥r），超出部分继续等闲置。

| g | u | r | f | q | 结论 |
|--|--|--|--|--|--|
| 10 | 4 | 3 | 1 | 2 | 立即满足（1 闲置 + 抢 2） |
| 10 | 7 | 5 | 0 | 3 | 整体挂起；释放 3 后仍差 2，等闲置 |
| 10 | 10 | 4 | 2 | 0 | 整体挂起，只等闲置 |
| 10 | 2 | 8 | 5 | 3 | 立即满足（5 闲置 + 抢 3） |
| 10 | 0 | 10 | 10 | 0 | 全部用闲置，立即满足 |
| 10 | 5 | 6 | 2 | 5 | 立即满足（2 闲置 + 抢 5） |

## 2. 受害者选择
候选 = 占用 > Min 的叶子（借入者索引，不全扫）。任务粒度：只选 `size ≤ 该受害者当前借入余量` 的任务；选中后余量减去 size。规则：出借方同层兄弟优先（借用局部性），其余借入者其次；同档按借入量降序、ID 升序；任务在所有可行任务中按 size 降序贪心选取（并列取启动更早者），使被抢任务数最少，直到累计 ≥ q。
反例：q=3，受害者借入 4，任务 [2(早), 5(晚)]：错误的「最晚启动优先」选 5 → 受害者降到 Min−1，违反语义 5；本规则选 2。若受害者只有任务 5，则无可行任务 → 返回不可行，按语义 6 一个通知都不发，请求继续等闲置。

## 3. 跨层借用归属
部门 P 下 A 借 B 的闲置 Min：P 的占用 = A+B 占用之和不变，对 P 的兄弟部门而言 P **没有**借入；借用只记在真正超出自身 Min 的叶子上。回收必须先在 P 内部平衡（抢 A），不够再跨部门。
两层反例：C=24；P=10（A5/B5），Q=10（D，Max24）。A 占 8（借 B 3），B 占 2，D 占 12（借根未覆盖的 4）。B 请求 3：内部 A 恰有 3 单位借入可回收。错误归属把 A 的借入算成「P 对外借入」而先抢 D，结果外部任务被无谓抢占、P 内部 A/B 仍失衡，违反语义 3 的同层优先。

## 4. 在途容量与挂起顺序
在途容量被冻结：计入 `C − 占用 − 在途` 之前不可见。抢占只可能由队首挂起请求发起，通知与该请求绑定；释放后的容量归该请求（发起抢占者），其他挂起请求看不到也花不掉。一般闲置按提交顺序 FIFO 服务。两者一致：在途释放归发起者（最迟在其全部抢占确认/超时后满足其 Min 内部分，语义 7），无主闲置归最早挂起请求；超时与迟到 Ack 都不产生第二次释放。
