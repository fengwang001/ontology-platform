# DESIGN

## 推导

- 零计数行号：unified diff 的普通行号是 1 基首行行号；范围为空时没有首行，`a` 取插入点前一行的 1 基行号，插入点在文件开头为 0。旧 `x\n` 前插入时旧空范围在第 0 行后，故 `-0,0`；在 `a\nb\n` 后插入时前一行是 2，故 `-2,0`。`Apply` 把该点解释为“消费 0 个旧行，在已匹配的 a 行之后插入”，不是消费第 a+1 行。
- hunk 合并：相邻改动间有 g 个未改动行。两个 hunk 各最多带 C 行上下文；g≤2C 时上下文相交或相接，GNU diff 合并；g=2C+1 时中间至少剩一个不属于任一 hunk 的公共行，故分开。若阈值取 C，则 g=C+1 时 GNU 仍共享上下文却被拆开；取 2C+1，则 g=2C+1 时会吞掉两 hunk 间本应保留的公共行。
- 并列最短脚本：Myers 固定在每个 d、k 上先尝试删除（k-1），再尝试插入（k+1）；V 只保留到达同对角线的最远端点。因此 `a b → b a` 的脚本为 `-a,b -b,+b +a`，即同 D 并列时删除优先，补丁内删除块排在插入块前。
- 偏移后续 hunk：hunk i 记录起点是 oi，实际在 oi+ki 应用。其净增行数已包含在后续 hunk 的新坐标差中，但“目标文本相对补丁基线的整体位移”不会；所以 hunk i+1 的查找原点平移 `sum(kj)`。各 hunk 独立选最近候选；后续平移只使用已接受候选，失败则整体回滚。

## 零计数头表

| 旧范围 | 书写 | Apply 解释 |
| --- | --- | --- |
| b=0 | `-a,0` | a 是插入点前一行的 1 基行号，文件开头为 0；不消费旧行，在第 a 行后插入 |
| b=1 | `-a` | 从第 a 行开始消费 1 行 |
| b≥2 | `-a,b` | 从第 a 行开始消费 b 行 |

新文件侧 `+c,d` 规则相同：空范围 c 为插入点前一行；应用逆补丁时据此定位。
