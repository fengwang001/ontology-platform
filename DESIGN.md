# 快照读可见性判定器设计

## 核心推导：为什么只看提交时刻不够

设快照 S = (水位 H, 活跃集 A)，版本由事务 T 写入，提交号 C(T)。

朴素规则：`C(T) < H` 即可见。它只比较了两个"时刻"，却忽略了
"快照创建那一刻 T 是否已经提交"这一事实。提交号的全序和水位的推进
并不能推出活跃集为空：水位是单调计数器，活跃集是快照创建时的瞬时集合，
二者来自不同的时间源，可能交错。

反例构造：T1 在快照创建时仍在进行中，故 T1 ∈ A；随后 T1 提交，
恰好拿到提交号 C(T1)=5；水位之后推进到 H=10。此时对快照 S 判定
T1 写的版本：朴素规则看 5 < 10 判可见，但 S 创建时 T1 尚未提交，
读者看到了一个"在它取快照那一刻尚未提交"的版本，违反快照语义。

正确规则（本实现）：

    visible(T) = committed(T) && C(T) < H && T ∉ A

即「提交号小于水位 且 写入事务不在快照的活跃集中」。两个条件缺一不可：
前者排除晚于快照的提交，后者排除"提交号虽小但快照时仍未提交"的交错。

## 复杂度与系统性约束

- 单次判定 = 1 次事务表查找 + 至多 1 次活跃集查找，O(1)，不遍历事务表。
- 三类可判定错误（`errors.Is`）：未知事务号、快照已释放、提交号非递增（损坏）。
- 活跃集大小有上限；快照内存项数 = 活跃集大小，与事务总数无关。
- 快照创建后只读，可并发判定；每个判定器持有各自的查找计数器，互不串台。
