# DESIGN - 推导与结论

## 1. 空输入 / 纯换行输入 × 三种策略

策略定义：Keep=原样；EnsureOne=输出恰好以一个 `\n` 结尾；Strip=删除全部末尾空行，有内容时保留一个换行。

| 输入       | Keep     | EnsureOne | Strip  |
|------------|----------|-----------|--------|
| `""`       | `""`     | `"\n"`    | `""`   |
| `"\n"`     | `"\n"`   | `"\n"`    | `""`   |
| `"\n\n"`   | `"\n\n"` | `"\n"`    | `""`   |
| `"  \r\n"` | `"\n"`   | `"\n"`    | `""`   |

推导：EnsureOne 对空输入若输出 `""`，则「恰好一个末尾换行」的契约对空输出不成立，下游无法依赖；选 `"\n"` 后 `N("")="\n"`、`N("\n")="\n"`，幂等成立。若选 `""` 也幂等，但契约有例外，故取 `"\n"`。Strip 若对全空输入保留一个换行（`"\n\n"`→`"\n"`），则 `N("\n")=""` 而 `N("")=""`，`N(N("\n\n"))=""≠"\n"` 破坏幂等；故全空白输入必须归约为 `""`，即「保留一个换行」只在删除后仍有内容时适用。Strip 实现为：去掉输出末尾所有 `\n`，非空则补一个 `\n`。

## 2. 被删字节的 ToOut

不变量：`ToOut(ToOrig(o))==o`、`ToOut` 单调不减。设被删字节 d 落在相邻保留字节 a=ToOrig(o)、b=ToOrig(o+1) 之间，则 `ToOut(a)=o ≤ ToOut(d) ≤ ToOut(b)=o+1`，只有 o 与 o+1 两种合法取值。取**前向**约定 `ToOut(d)=o+1`（映射到下一个被保留输出字节的位置；无后继保留字节时为 `outLen`）：编辑器光标落在被删行尾空白里时，映射后落在行尾换行符的位置上（即行内容末尾、换行之前），符合「行尾」直觉；取后向 o 会把光标弹回行内最后一个字符上。`\r\n` 的 `\r` 同理映射到 `\n` 的输出位置。实现上只存保留区间，被删字节即区间间隙，查询时取「下一个区间的 OutLo」自然得到前向语义。

推论（合成字节的例外）：EnsureOne/Strip 可能追加一个原文中不存在来源的 `\n`。此时输出末尾两个位置（合成字节与终点）都需要原像，而原文只剩 `origLen` 一个偏移，`ToOut` 是函数，数学上不可兼得。故规定：合成字节的 `ToOrig = origLen`，互逆不变量对**除合成字节外**的所有输出偏移成立（合成字节至多 1 个，Map.Synth 暴露其存在）；幂等后的二次规范化不再产生合成字节。

## 3. 空白缓冲超限：拒绝 vs 强制判定

强制判定为「非行尾」并输出：若该串空白其后真的遇到行尾，输出将含行尾空白，违反语义 5（输出无行尾空白），且二次规范化会删除它，`N(N(x))≠N(x)` 破坏幂等。拒绝：返回带原文偏移的可判定错误（KindWS，偏移=超限字节处），进入终态；错误位置只取决于字节流前缀，与切分方式无关，不违反语义 4；不产生错误输出，不违反 5、6。**选择拒绝**。

## 4. par 的拼接修正

每段用 `norm`（Policy=Keep，Base=段全局起点）处理，**不 Close**，取出已判定的输出与区间，以及段尾待定态。待定态互斥地只有两种：待定 `\r`（1 字节）或未判定空白串 (start,len)。修正规则（合并时从左到右顺序做，段结果并行算）：

- 待定 `\r`：看下一段首字节（原始输入，par 持有），是 `\n` 则 `\r` 被吸收（删除，留间隙），否则补发一个源自该 `\r` 的 `\n`。
- 待定空白：若已是末段（EOF）或下一字节是 `\r`/`\n` → 删除；若下一段整段仍是空白（其待定空白起点=段起点）→ 并入进位继续传递；若下一段首区间起点=段起点（前导空白已被判为内容）→ 进位空白也是内容，补发；否则删除。
- 映射平移：段内区间用全局原文偏移（Base），输出坐标在合并时按当前全局 `len(out)` 平移后追加进全局 Builder，自动合并相邻区间。
- 末尾换行策略只在全局输出上应用一次（norm.Finalize），保证与单线程一致。

修正范围有上界：每段边界只需看下一段首字节与首区间起点，状态是 O(1) 的「CR 或空白串 (start,len)」；空白串跨段时以 (start,len) 整体传递不展开，每段只被合并一次，修正工作量 O(K)，总复杂度 O(n)。
