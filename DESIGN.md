# 可撤销批量重命名：设计推导

模块：`name`（名字集合/锁/校验）、`plan`（冲突检测+排序，含复杂度计数器）、
`cycle`（破环+临时名生成）、`apply`（执行/故障注入/日志）、`undo`（读日志逆序撤销）、`cmd/demo`。
依赖方向：apply → plan → cycle、apply → name、undo → apply（共用日志格式）。

## 1. 执行顺序：依赖图与拓扑序

请求 x→y 表示“把 x 搬走并腾出 y”。若 y 同时是某条请求 z→y? 不，关键是：
y 是另一条请求的旧名（y→z 且 y≠z），则必须先执行 y→z 腾出 y，再执行 x→y。
建边 x→y（在所有“非自环移动”构成的图上）：u→v 表示 v 必须先于 u 执行，
因此执行序是图的**逆拓扑序**。链 {a→b,b→c} 得 b→c 先、a→b 后；
按“单元（点或环）”做 Kahn，就绪层内按名字字典序，保证确定性。

## 2. 环与破环

每个点出度≤1、入度≤1（冲突已排除），故非自环环彼此点不相交，每个环只需 1 个临时名。
环 (v1→v2→…→vk→v1)（vi 字典序，v1 最小）先取出最小元 v1：
`v1→tmp`，随后按入度逆序 vk→v1, v_{k-1}→vk, …, v2→v3，最后 `tmp→v2`。
k=2 即 v1→tmp, v2→v1, tmp→v2。每步执行时其目标名必然已腾空（快照检查器可断言）。
临时名候选形如 `.renametmp-<v1>-<i>`，i=0,1,2,…；每个候选都校验
“不在现有名集合 ∪ 全部新名集合 ∪ 已选临时名集合”中，冲突即 i++ 重试；
重试耗尽（预算=名字总数+256）返回 `ErrTempExhausted`（可 `errors.Is`），绝不覆盖数据。

## 3. 四类冲突（修改前一次性检测，均支持 errors.Is）

1. `ErrSrcMissing`：旧名不在命名空间。
2. `ErrDupSrc`：同一旧名出现两次，错误中报两个新名（自环也占用旧名键）。
3. `ErrDupDst`：两个不同旧名指向同一新名，报两个旧名。
4. `ErrDstOccupied`：新名已存在，且它不是本批某条请求的旧名（即不会被搬走）。
全部用集合/映射一次性扫描；任一冲突即整体拒绝，命名空间逐元素不变。

## 4. 自环与边界语义

`a→a` 是**无操作**：不产生任何执行步骤、不产生环、不需临时名、不与自身构成目标占用
（目标 a 正是本批旧名，被豁免）。但旧名 a 仍被占用：`a→a` 与 `a→b` 属重复旧名冲突；
`b→a` 与 `a→a` 属重复目标冲突。空串是合法名字；路径分隔符 `/` 按普通字符处理。
空请求集：合法，执行与撤销均为 0 步；单请求：直接执行。请求数 ≫ 名字数时由重复旧名拦截。

## 5. 复杂度

冲突检测与排序均为哈希集合上的线性扫描。`plan.Counter` 累计所有存在性/映射查找次数，
保证 ≤ 4·(请求数+涉及名字数)；5 万请求断言之。临时名数 = 环数：10 个不相交环断言恰为 10。

## 6. 并发与确定性

`name.Space` 内部 `sync.RWMutex`；`apply` 执行整批前 `Lock()`，结束（含失败回滚）后解锁，
期间另一写 goroutine 被阻塞（测试用“尝试在执行中途加写锁会被阻塞至执行结束”证明）。
确定性：请求构造顺序打乱 20 次，步骤序列（旧名,新名）逐元素相同（同层字典序，见第 1 节）。

## 7. 执行、日志与撤销

执行把每个已执行步骤以定长帧追加到日志：
魔数 8 字节 `"RNMLOG01"`，随后每帧
`[4B 载荷长度 LE][载荷 = 旧名\0新名][4B IEEE CRC32(载荷) LE]`。
第 k 步可注入失败：已执行帧的步骤按逆序补偿重命名（b→a），整批返回错误且命名空间复原。
`undo` 从文件头顺序解析，按帧逆序执行补偿重命名，幂等：成功撤销后重命名头为空文件，
再次撤销为无操作（返回 0 步）。
截断分类（`errors.Is`，对字节位置 1..len-1 逐点循环）：位置在魔数区内 →
`ErrBadHeader`；位置落在某帧长度+载荷区内 → `ErrTruncated`；位置落入该帧 CRC 区 →
`ErrCRC`（CRC 存在但不匹配）。可恢复部分 = 此前所有完整帧，按其逆序撤销，
并通过返回结果报告无法撤销的帧（坏帧及其后全部）。
