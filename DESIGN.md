# CSV 解析器设计推导

## 1. 空行与单列空值

空行（`\n\n` 相邻）与「单列表中一条值为空的记录」在字节层面同形，二者只能选其一。
若把空行当记录，则空文件与 `\n` 解析结果相同，且空行无法被回写（写空行会产生一条
未引号空记录，多列表里还触发列数错误）。因此选择：**空行被跳过，不产生记录**。
推论：单列表中「唯一字段为空」的记录若不加引号回写，会得到一个空行，再解析时被跳过，
记录数减一——丢数据。故回写器必须把它写成 `""`。由此得「必要加引号」全集：
字段含 `,` `"` `\r` `\n`，或字段原本带引号（保引号标记），或单列空字段。
规范输入定义：每条记录以 `\n` 结尾、无空行、引号使用满足上述最小规则（含 `""` 单列空），
引号字段内的 `\r\n` 原样保留。此时 `Write(Parse(x))` 逐字节等于 x。

## 2. par 切点

切点可能落在引号字段内，后段无法自知起点状态。方案：每段用两个假设各跑一遍
lexer——OUT（假设起点在引号外、记录/字段边界）与 INQ（假设起点在引号字段内，
段首前置一个合成 `"` 开引号，其偏移记为 base-1）。然后从左到右解析真实状态：
第 0 段必为 OUT；若第 i 段所选解析的结束快照处于引号内（INQ/QQ/CR-in-quote），
第 i+1 段取 INQ，否则取 OUT。OUT 与 INQ 在首个完整记录结束后必汇合（引号字段
内外的解析在记录边界状态唯一），故每段只需保留「首条记录结束之前」的事件差集：
INQ 的首字段是跨段字段的后半段，其值前缀由前一段快照携带，拼接后重编字段号；
首条记录结束后两假设事件完全一致，直接采用。错误位置换算：段内偏移 +base
（INQ 因前置字节减 1），记录号/字段号加上已确认的全局基数。每段恰好两遍，
总处理次数 ≤ 2n+2K，不退化。

## 3. CR 待定状态

未引号字段末尾见 `\r` 进入 CR 待定：下一字节是 `\n` 则为行尾（CRLF），否则该 `\r`
是孤立 CR，报 ErrBareCR（偏移指向 `\r`）。半包续传：`\r` 后 Feed 返回，状态挂起，
下次 Feed 首字节先走 CR 分支；流在 `\r` 处结束按孤立 CR 报错。par：CR 待定属于
「引号外」结束快照，下一段取 OUT；若 `\r` 是段尾、`\n` 在下一段首，OUT 假设天然
覆盖（段首 `\n` 即行尾），INQ 假设在首记录结束前被丢弃，不影响结果。

## 4. 上限的「立刻」

字段字节数按解码后计数：引号字段内 `""` 只算 1（第二个 `"` 入值时计），引号本身不计。
每接收一个内容字节即 `n+1` 并与上限比较，超限当场返回 ErrFieldTooLarge，指向该字节，
不缓冲整条记录。字段数上限在字段闭合（见 `,` 或行尾）时检查；记录数上限在记录完成
时检查。超限后解析器进入终态，已产出完整记录保留，后续 Feed 返回同一错误。

## 5. 状态转移表（状态 × 字节类别 → 下一状态 / 动作 / 错误）

| 状态 | `,` | `\n` | `\r` | `"` | 其他 | EOF |
|---|---|---|---|---|---|---|
| 字段开始 FS | 出空字段→FS | 空行跳过→FS | →CR | →INQ | 入值→U | 结束 |
| 未引号 U | 出字段→FS | 出字段+出行→FS | →CR | ErrBareQuote | 入值→U | 出字段+出行 |
| 引号内 INQ | 入值 | 入值 | 入值 | →QQ | 入值 | ErrUnterminated |
| 闭合待判 QQ | 出字段→FS | 出字段+出行→FS | 出字段→CR | `"`入值→INQ | ErrQuoteAfterClose | 出字段+出行 |
| CR 待定 CR | ErrBareCR | 出字段+出行→FS | ErrBareCR | ErrBareCR | ErrBareCR | ErrBareCR |

记录号、字段号均从 1 起；字节偏移从 0 起；CRLF 行尾的偏移取 `\r` 位置。
