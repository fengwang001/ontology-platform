# DESIGN

统一 diff 实现。每行按字节保留原行尾；换行状态由 `lines` 包承载，补丁正文去掉行尾，
用 `\ No newline at end of file`（记 N）表示对应源行无 `\n`。

## 1. 零计数时的行号（语义 4）

`@@ -a,b +c,d @@` 中 `a` 是 hunk 覆盖旧区间的首行号（1 起），旧区间为 `[a, a+b-1]`。
- `b>=1`：区间非空，`a` 即首行（或前导上下文）行号。
- `b==0`：旧区间为空。空区间 `[a+1, a]` 位于旧行 `a` 与 `a+1` 之间，故 `a`=插入点前一行的
  行号；文件开头插入前没有行，取 `a=0`。

三个样例统一：空旧文件开头插入→`-0,0`；旧第 2 行与第 3 行间纯插入→`-2,0`；
删除唯一一行→新侧为空、`c=0`（删除点前的新行数为 0）。
Apply：旧侧插入点（0 起行索引）为 `a`（跳过 `a` 个旧行），新侧为 `c`；b=0 的锚点是
「旧行 a 之后」，不是「旧行 a+1」。

| 旧计数 | 头部书写 | Apply 旧侧插入/覆盖（0 起） |
| --- | --- | --- |
| b=0 | `-a,0`，a=前一行行号（文件头为 0） | 索引 a（第 a 行之后） |
| b=1 | `-a`（省略 `,1`） | 覆盖旧行索引 a-1 |
| b>=2 | `-a,b` | 覆盖旧行索引 [a-1, a+b-2] |

## 2. hunk 合并阈值（语义 5）

前 hunk 延伸改动后 C 行上下文，后 hunk 回溯改动前 C 行；间隔未改动行 g。
GNU `diff -U C` 规则：**g <= 2C 合并，g > 2C 分开**。g=2C 时两窗口重叠 1 行、上下文连续；
g=2C+1 时两窗口间剩 1 行不属于任何 hunk，必须分开。
取 C：g=C（C>0）本应合并却被分开，错；取 2C+1：g=2C+1 被错误合并成跨整行的单 hunk，错。
文件首尾不足 C 行的窗口按实际截断，不改变判定式。

## 3. 并列最短路径的选择（语义 3）

Myers 对每条目标对角线 k 取两个前驱：k-1（向下=插入，新侧前进）、k+1（向右=删除，旧侧前进）。
固定 **删除优先**：tie 取 k+1。于是同一 hunk 内并列位置上所有 `-` 行排在 `+` 行之前。
`a b → b a` 的 LCS 可取 a 或 b；删除优先使脚本固定保留 b：
`-a`、` b`、`+a`（先删 a，保留 b，再插 a），而不是保留 a 的另一并列脚本；
在同一并列位置上 `-` 行先于 `+` 行，反向链沿唯一父链输出，故 100 次生成逐字节相同。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

hunk i 记录旧起始 r_i（1 起），实际匹配 s_i，偏移 o_i=s_i-r_i。hunk 顺序应用，前面已应用
区域的净行数变化（插入-删除；上下文匹配行净变化为 0）把后续旧文本整体挪动，其位移恰等于
最近一次偏移 o_i。故 hunk i+1 的查找中心 = r_{i+1}+o_i（继承上一成功 hunk 偏移，
同 GNU patch），不以原 r 为中心，也不另行累计。搜索范围=中心±F；多候选取 |位移| 最小，
并列取行号靠前。
