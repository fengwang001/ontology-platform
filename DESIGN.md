# 多租户配额公平队列调度器 — 设计推导

## 1. 为什么是 `vt += c / 权重` 而不是 `c * 权重`

目标：长期看，租户 i 获得的执行代价份额应等于其权重占比 `w_i / Σw`。

设某租户连续执行了总代价 C 的任务，其虚拟时间前进 `Δvt`。调度规则是「每
次取 vt 最小者」，稳态下所有活跃租户的 vt 必然大致齐头并进（否则 vt 落后者
会立刻被选中），即任意两个活跃租户 i、j 在相同墙钟区间内前进的虚拟时间
相等：`Δvt_i = Δvt_j`。

- 若取 `Δvt = C / w`：由 `C_i / w_i = C_j / w_j` 得 `C_i / C_j = w_i / w_j`，
  即执行代价之比等于权重之比，正是目标。权重越大的租户 vt 前进越慢，被
  选中的频率越高，份额越大 —— 方向正确。
- 若取 `Δvt = C * w`：则 `C_i * w_i = C_j * w_j`，得 `C_i / C_j = w_j / w_i`，
  执行份额与权重成反比，权重大的租户反而被饿死 —— 方向恰好颠倒。

故必须采用 `vt += c / w`。直观记忆：虚拟时间是「按权重折算后的服务量」，
权重是折算率，代价除以权重才得到可横向比较的刻度。

## 2. 空闲租户重新加入时的 vt 抬升

系统维护「当前系统虚拟时间」`sysVT`：每次选择时，被选租户（堆顶，即当前
最小 vt）在选择前的 vt 就是所有活跃租户虚拟时间的下界，`sysVT` 取其最大
值，单调不减。

设租户 X 空闲期间，租户 Y（权重 1、任务代价 1）连续执行了 N=1000 个任务，
则 `sysVT ≈ vt_Y ≈ 1000`，而 X 的 vt 仍停在旧值 `vt_X ≈ 0`。

- **不抬升**：X 重新入队后 vt=0 为全场最小，之后每执行一个任务 vt 只 +1，
  在追上 1000 之前 X 始终是堆顶。若 X 补充了足够多的任务，它将**连续独占
  约 1000 步**（精确说约 `sysVT - vt_X` 步，按 `c/w` 折算），Y 在此期间
  完全得不到服务 —— 空闲反而获得奖励，违反公平。
- **抬升**：租户从空转为非空（或新到达）时执行
  `vt = max(vt, sysVT)`。X 以 `vt ≈ 1000` 重新入队，与 Y 几乎并列，至多
  因为并列打破规则先走一两步，随后立即与 Y 交替。空闲不奖励、不惩罚。

推导结论：抬升量恰好抵消「欠账」`sysVT - vt_X`，把独占步数从 O(欠账)
压到 O(1)。

## 3. 代价为 0 的任务

若允许 `c = 0` 原样参与，则 `vt += 0 / w = 0`，该租户 vt 不前进，只要它
还有零代价任务就永远是堆顶，造成无限独占。处理：设最小有效代价下界
`minCost = 1e-9`，推进时取 `c_eff = max(c, minCost)`。零代价任务合法入队、
合法执行，但每执行一个 vt 至少前进 `1e-9 / w`，独占步数必然有限；同时
下界远小于正常代价，对正常任务的公平性影响可忽略。

## 4. 同虚拟时间的并列打破

堆的比较函数先比 `vt`，`vt` 相等时按租户 ID 的字典序。规则固定且与插入
顺序无关，因此同一份确定性任务流必然产生逐元素相同的调度序列（可复现）。

## 5. 数值表示：float64

权重与代价均为 float64。极大权重（如 1e9）与极小权重（如 1e-9）并存时，
单步推进量在 `1e-9 ~ 1e9` 之间，float64 的有效范围（约 1e-308 ~ 1e308）
与 53 位尾数足以避免溢出；长期累加的相对误差在 10 万步量级下远低于 5%
的公平性容差。相比有理数（big.Rat）省去任意精度开销，且堆比较为常数时间。

## 6. 背压、移除与错误

- 每租户队列上限 L：满时入队返回 `ErrQueueFull`，只影响该租户。
- 权重 <= 0：`AddTenant` 返回 `ErrInvalidWeight`。
- 移除仍有排队任务的租户：拒绝，返回 `ErrTenantBusy`（任务去向 = 留在
  队列中继续被调度，租户对象不被删除）；仅当队列为空时可移除。
- 未知租户：`ErrNoSuchTenant`。四类错误均可用 `errors.Is` 区分。

## 7. 并发与确定性的关系

`admit.Limiter` 用一把互斥锁串行化所有入队/出队。并发提交时，「提交顺序」
定义为**获取入队锁的串行化顺序**：任务进入各自租户队列的先后由锁的获得
次序决定，调度器本身始终是单线程视角的确定性状态机。因此同一种子、同一
串行化顺序下调度序列可复现；跨 goroutine 的真实交错顺序不在确定性保证
范围内（也无法定义）。

## 8. 复杂度

选择 = 一次堆弹出 + （若仍非空）一次堆压入，比较次数上界约
`3*ceil(log2(k))`（k 为非空租户数），测试以 `4*ceil(log2(k))` 为断言上界。
堆中只放非空租户，空闲租户不占用任何选择代价。
