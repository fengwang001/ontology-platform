# LZ77 流格式与实现推导

## 流格式

定长流头 `4C 5A 37 37`("LZ77") + 版本 `01`。其后为记录序列，首字节高 2 位为标签，载荷均为无符号 varint（LEB128，≤10 字节，第 10 字节只允许低 1 位）。流尾必为 END 记录。

| 记录 | 标签 | 字节布局 |
|---|---|---|
| 流头 | — | `"LZ77"` `0x01`（5 字节定长） |
| LIT | `00` | tag + n(varint) + n 个原文字节 |
| MATCH | `01` | tag + dist(varint) + length(varint)，dist≥1 |
| FLUSH | `10` | 仅 tag，无载荷 |
| END | `11` | tag + totalLen(varint) + CRC32-IEEE(原文)(varint)；其后禁止任何字节 |

## 推导 1：压缩输出与 Write 切法无关

匹配器看到的历史会影响最长匹配，故切法无关要求：每个输入位置被考察时，它之前的全部历史必须已入库；且某位置是否发出 MATCH 只能依赖到当前已缓存末尾为止能确认的事实。维护未决缓冲，每次有新数据只推进到 len(buf)-1：最后一个字节的匹配可能被未来字节延长，暂不裁决。能完整匹配到缓冲末尾的候选也延后（长度未定），该位置先按字面量保留在未决缓冲中；下次数据到达重算，结果只可能更长或相同，不会改变更靠前记录的类型与内容。因此 1 字节/7 字节/整段写入产生完全相同的记录流。Flush/Close 时强制把缓冲裁决到末尾：此刻匹配长度已确定，FLUSH 点之前全部落盘；FLUSH 后历史窗口不动，之后的匹配仍可回指 FLUSH 之前。

## 推导 2：重叠回指的复制

块复制 copy(out[pos:], out[pos-dist:pos-dist+length]) 的 src 与 dst 在 dist<length 时重叠，Go 的 copy 从头到尾复制且读区可能含尚未写入的旧数据；而 LZ 语义是 RLE 式重复模板：out[i]=out[i-dist]，其中右侧可能是本次回指刚写出的字节。实现为每轮复制 n=min(剩余length, dist) 字节，每轮源、目标不重叠且源全部位于已输出区；dist=1 时每轮 1 字节，整体 O(length)，最多 length/dist 次 copy，不退化为每字节一次函数调用，又与逐字节循环严格等价。

## 推导 3：两种距离越界

dist 大于已输出字节数：引用了从未存在的内容，属流损坏（截断/伪造）；dist 不超过已输出但大于 windowCap：引用合法历史，却超出本流声明的窗口能力，属配置/流不一致，必须单独报错，因为二者排查方向不同。压缩器只在窗口内查表，Find 只返回 dist 不超过 windowCap 的候选，故正常编码绝不可能产生后者。并行块以上一块原文末尾 windowCap 字节预置新块窗口；这些字节在解压端就是紧接此前已解压输出的末尾，解码端距离判断口径不变——已输出始终指全局已还原字节，预置字典只是压缩端提前知道了解压端必然已有的历史。

## 推导 4：并行压缩的确定性与衔接

块边界固定为 blockSize。块 k 的预置字典 = data[k*bs-W : k*bs] 的原文（不足则取全），本块输入 = data[k*bs:(k+1)*bs]。压缩块 k 所需信息（本块原文 + 紧前 windowCap 字节原文）全部来自静态输入 data，与块 k-1 的压缩记录、完成时刻无关；匹配器、哈希链、候选上限均为确定算法，故 goroutine 数与调度不影响各块记录字节。主 goroutine 按 k 顺序收集 channel 结果拼接：流头 + 块0记录 + 块1记录 + … + END，块间不需要 FLUSH：记录流无前缀状态，解压端逐记录消费，MATCH 的 dist 天然可跨块边界回指（这些字节已是全局已输出）。块 k 内部匹配可跨其字典边界，因此跨块长回指同样生效。
