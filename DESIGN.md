# DESIGN

## 流格式

流头 `LZ77` + 版本 1，共 5 字节；之后 0..n 条记录；流尾含原文总长与 64 位 FNV-1a 校验和。
所有整数用 LEB128 无符号变长编码（与 binary.PutUvarint 同形，但自实现以给出字节偏移错误）。

| 记录 | 布局 |
| --- | --- |
| 流头 | `4C 5A 37 37 01` |
| 字面量 | tag=0(varint), len(varint), len 字节原文 |
| 回指 | tag=1, dist(varint, 1..cap), length(varint) |
| 刷新 | tag=2，无负载；表示截至此处全部输入可被解压器输出 |
| 流尾 | tag=3, origLen(varint), checksum(varint FNV-1a-64)，其后禁止任何字节 |

## 第三节四处推导

### 1. 压缩结果与 Write 切法无关

压缩器在 pending 缓冲保留所有尚未定稿的字节，窗口只含已定稿字节。位置 i 的匹配判定只依赖
已定稿窗口 + pending[i:i+3]，与 pending 分几次 Write 进来无关（字节序列相同）。匹配必须
等到“确认无法再延长”才提交：已知尾部至少比匹配长多 1 字节时才能确定其结束，因此匹配长度
到达常量上限 M=32768 立即提交（绝不可能更长）；否则等待更多输入。Flush/Close 把 pending
全部强制定稿：能匹配则按当前长度提交，剩余不足 3 字节走字面量。故 Flush 强制结束当前匹配，
正常 Write 边界不结束任何东西——同一字节序列与同一组 Flush 偏移必产生同一记录流。

### 2. 重叠回指的复制

dist<length 时源区域与目标区域重叠：一次性块复制会把“尚未复制到的新字节”当旧字节读。
逐字节复制 `dst[j]=src[j-dist]` 时源偏移随写指针前进，天然实现 RLE/周期展开（dist=1 即重复）。
解压端直接逐字节写，不追求块复制；匹配比较候选时同理按字节比较。回指展开成本就是输出本身
的成本（O(输出)），不构成额外渐近开销，故逐字节循环可接受。

### 3. “距离超过已输出”与“距离超过窗口”为何分开

二者是不同不变量：dist>窗口容量说明流不可能由本压缩器产生（压缩器引用前必先插入，超容量
候选早已被环形缓冲覆盖，Match 也不会返回它）——结构性伪造；dist<=容量但>已输出字节数只可能
是流被篡改、引用“未来”位置，是时序性伪造，故分别报错（先判后者再判前者，偏移明确）。并行
块的预置字典是“上一块原文末尾 <= 容量的字节”，这些字节在上一块已被解压器实际输出、仍在
解压窗口内，所以“已输出字节数”含义不变，无需在流里另传字典。

### 4. 并行块压缩的确定性

第 k 块匹配只查哈希链，链中候选全部来自“本块已处理前缀 + 预置字典（上一块末尾）”，字典按
原文字节重建，与第 k-1 块压成什么记录、goroutine 何时完成无关；匹配算法本身是确定性函数。
各块输出首尾相接（记录可跨块边界连续，块间不插刷新标记——刷新标记给调用方交付点用，不是块
分隔符），最后统一追加一个流尾（总长与校验和拼块后一次计算）。因此 workers=1/2/4/8 输出
逐字节相同，可安全并发。

## 复杂度

哈希链：head[65536] 链头、环形 prev[cap] 前驱；每位置沿链至多走 maxChain 步，非导出计数器
candidates 累计考察数 => 上界 maxChain×输入字节数。全同字节时每位置考察 1 个候选即确认
最大匹配并整体跳过，单位考察数不随规模增长。
