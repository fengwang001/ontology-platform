# DESIGN

## 流格式（全部 LEB128 无符号变长整数，最多 10 字节）

| 记录 | 首字节 | 后续字段 |
|---|---|---|
| 流头 | 魔数 `0x4C 0x37` | 版本 varint=1，窗口容量 varint |
| 字面量 | 标记 0 | 长度 varint，原始字节 |
| 回指 | 标记 1 | 距离 varint(>=1)，长度 varint(>=3) |
| 刷新 | 标记 2 | 无 |
| 流尾 | 标记 3 | 原文总长 varint，FNV-1a 64 校验和 varint |

## 推导 1：为什么输出与 Write 切法无关（语义 3/4）
压缩器维护「已确认前缀」与 `buf`（未确认字节）。在全局偏移 `base+64KiB` 处设块边界。
循环解析位置 i（base 为已确认长度）：
- 若 `len(buf)-i < 3`：剩余不足最小匹配，停留（非 flush 时保留）。
- 找匹配（距离 <= min(窗口, base+i)，长度 >=3）。匹配可延长到 `len(buf)` 才发——
  但发之前先截到「不越过下一块边界」。
  - 若匹配在块边界被截断：发回指，跳到边界，边界后字节继续循环。
  - 否则若匹配到达 `len(buf)`（未到块边界）：停留等待，因为再来字节可能更长。
- 无匹配：只有当位置之后还留着 >=2 字节时才把它确认为字面量（否则停留）。
所有判定只依赖「已确认前缀 + 当前 buf + 固定块边界」，不依赖 Write 切法；
切法不同只影响停留点，停留字节不产生输出。Flush 把块上限放开并解析到
`len(buf)`，强制结束当前匹配，随后发刷新标记，因此已输出字节恰好还原全部输入；
无新输入时没有新记录，第二次 Flush 零字节。

## 推导 2：重叠回指解压（语义 2）
距离 d < 长度 l 时一次性 `copy(dst, src)` 只复制到现有历史末尾，后续字节读不到
新写出的数据。解法：`for i:=0; i<l; i++ { out[p+i] = out[p+i-d] }`。每字节一次
内存读写，现代 CPU 下完全可接受；也可按「先扩 l 字节再按 d 步长 copy」加速，
本实现采用逐字节，正确性最直接。

## 推导 3：两种距离错误为何不同（语义 7）
「距离 > 已输出字节数」：引用了从未存在的数据，任何压缩器都不可能产生（含空流）。
「距离 > 窗口容量」：数据可能在很久以前存在但已被滑出环形窗口，老压缩器/更大
窗口流可能产生；头部声明窗口后，引用超出该容量即违反格式契约，故单独报错。
压缩器只在当前窗口内取候选，故永不产生后者。并行块预置字典时，块的 base
（已确认长度）包含上一块全文，窗口预载上一块末尾窗口容量字节，因此对解码器
而言「已输出字节数」含义不变：字典字节此前已作为前一块输出，距离计算天然连续。

## 推导 4：并行块压缩的确定性（语义 9）
块 k 的压缩只读取 `data[k*B:(k+1)*B]` 与上一块末尾窗口容量字节（预置字典，
仅填窗口/哈希链，不计入本块输出），不读取任何压缩结果，与 goroutine 完成顺序
无关；块边界天然是推导 1 的 64KiB 边界，故块内输出与流式压缩同一前缀的输出
逐字节一致。流上衔接只需每块结尾一个刷新标记（强制完成悬空匹配），块 k>0
不再写流头；最后发一次流尾。

## 复杂度与安全
哈希链：每位置追链最多 chainLimit 步，候选计数 = O(chainLimit·n)，与窗口大小
无关，故全同字节每字节考察数不随规模增长。炸弹：回指长度先加进「拟输出长度」
与上限比较，超限立即进入终态错误，不分配、不写出。
