# 批量导入幂等与断点续传器 — 设计

## 1. 进度粒度的推导

写入接口按业务键幂等：`Put(key, value)` 在同一键上重复执行结果相同（内存 store 用
map 赋值语义）。因此“进度已记录但记录尚未写入”与“记录已写入但进度未记录”两种崩溃
时序都可安全重放：

- 前者：重放完成缺失写入；
- 后者：重放一次幂等写，结果不变。

结论：进度无需逐条刷盘。进度文件只记录**一个高水位 H（已完成的连续前缀条数）**，
按粒度 N（如 1000）周期性刷盘。崩溃后进度可能滞后最多 N 条，续传从 `H-(H mod N)`
（或 store 实际前缀，见 §3）开始重放，重放条数 ≤ N，吞吐不塌方且语义正确。

## 2. 重复导入的幂等判定

四阶段：Prepare（校验/取锁）→ Write（幂等逐条写+粗粒度进度）→ Verify（store 前缀
等于批次全部记录）→ Commit（批次标为 committed）。Commit 是状态机上的一个显式状态。
若开始时发现该批次已处于 committed 且 store 内容与清单逐字节一致，直接返回哨兵错误
`ErrBatchComplete`（可判定的“已完成”，非失败）；写入与提交阶段全部幂等，所以同一批
次重复导入后 store 与第一次结束逐字节相同。

## 3. 以存储为准，而非以进度为准

进度文件只是缓存，可被篡改/截断/落后；store 才是事实来源。Prepare 阶段读取进度
`Hp` 后，向 store 询问“批次前缀实际连续匹配到第几条”得 `Hs`，以
`start = min(Hp, Hs)` 起续传（进度超过存储时回退到存储）。这保证“进度声称 5 万、
存储只有 3 万”时从 3 万处重写。对账器以 store 快照为基准独立比对清单，不采信进度。

## 4. 在途可见性

store 中每条带 `committed bool`。Write 阶段写入的记录 committed=false（在途），
Commit 时原子翻转该批次全部记录。reconcile 对未提交批次输出：已写入区间（前缀连续
部分）、缺口区间（清单位于前缀之后但存储缺失/不符的区间）、多余记录（store 中有、
清单中无的键）。在途记录不计入“已提交”统计。

## 5. 进度文件格式、CRC 与截断分类

文本行格式，末尾 CRC32（IEEE）覆盖此前全部字节：

```
BID v1
S <batchID> <H>
I <lo> <hi>      （可重复，表示已完成区间；本实现为单前缀，区间即 [0,H)）
C <crc32 hex>
```

截断点分类（逐字节截断，缺文件视为空进度从头开始）：

- 头部不完整：切断点落在前两行（magic/状态行）范围内，或得到的不是完整合法头；
- 区间记录不完整：头完整、某 `I` 行被截断（行尾换行缺失）；
- CRC 不匹配：头与所有区间行完整（均以换行结束）但 CRC 行缺失/残缺/不匹配。

恢复到“最大可恢复前缀”：头部不完整 → H=0；区间不完整 → 丢弃最后不完整行，保留其
前完整区间；CRC 不匹配 → CRC 之前的完整区间行均可采信（损坏无法定位，前缀语义保证
区间按序）。错误以哨兵 `ErrHeaderTruncated/ErrIntervalTruncated/ErrCRCMismatch`
返回，续传结果与不崩溃相同（幂等重放）。

## 6. 并发占用与接管

同目录写锁文件 `<id>.lock`，内容为 `pid 起始时间 TTL`。获取锁时：无锁→持有；有锁
且未过期（now < start+TTL）→ `ErrBatchBusy` 立刻返回；已过期→原子接管（时钟可注入，
便于测试活跃/过期）。不同批次锁文件不同，可并发。Commit/中止后删除锁。

## 7. 复杂度与资源约束

- 续传不重头扫描：`start` 由进度+store 前缀查询直接给出；非导出计数器 `rewrites`
  记录实际重写条数，≤ N。
- 内存：清单流式编解码（长度前缀），Importer 每次只持有一条记录，另维护已见键集合
  做批内重复检测；`maxResident` 计数器统计同时驻留记录条数的历史峰值。
- 对账线性一遍：清单键建 map（1 次/条），store 全量快照一次性取得（快照按 1 次计），
  再对清单/快照各线性扫描一次；访问计数 ≤ 3n，非 O(n²)。

## 8. 边界

空批次：Prepare/Verify 空转，Commit 标记完成。单记录正常。批内业务键重复：Prepare
拒绝整批，错误指出重复键与两处下标。空键合法。空批次 ID 拒绝。进度文件不存在 →
从头开始。

## 9. 包划分（6 实现文件 / 3 测试文件）

batch（批次+清单编解码）、store（内存存储+失败注入）、progress（进度+CRC+锁）、
importer（四阶段编排）、reconcile（线性对账）、cmd/demo。表驱动测试按包合并：
progress_test、importer_test（含 batch/store 判定）、reconcile_test。
