# Properties 解析/回写设计推导

## 第 3 条：被续行接上的行只含空白（或为空）

推导：
1. 行尾为奇数个反斜杠时，`Properties` 的 LineReader 把 `\` 与换行一起吃掉，并进入
   “跳过续行首部空白”的状态；该状态跳过 `' ' '\t' '\f'`（字节实现连 `\r \n` 也跳）。
2. 若整行只有空白（或为空行），跳过动作会一路吃掉这一行及其换行，状态仍停留在“找续行
   起点”，于是继续在下一物理行找第一个非空白字符。
3. 因为跳过的全是空白，没有任何字符追加进逻辑行；随后的终止符也被跳过，所以它等价于
   “这条续行对逻辑行贡献为空，且不结束逻辑行”。

结论：`k=a\` + 空行/纯空白行 + 下一行 `b` 得到 `k`→`ab`；多行纯空白同样被整体吞掉。
它与“首部空白被剥掉”是同一条规则的极端情形——整行都属于被剥离的前导空白。这也解释了
`k=a\`（EOF）：反斜杠被丢，逻辑行以 `a` 结束。

## 第 6 条：Store 的最小转义推导

回写格式固定为 `key=value`，Load 时只有以下判定会吃掉字符，逐一反推必须转义项：
1. 物理行首的前导空白被丢弃 → 键、值的**首个字符**若为空白必须写 `\ `。
2. 行首（跳过空白后）为 `#`/`!` 整行被判为注释 → 仅**键首**的 `#`/`!` 必须转义；
   值首的 `#` 前面有 `=`，永不处在行首，不必转义。
3. 未转义的 `=`/`:`/空白会终结键 → 键内部的这三类字符必须转义（值内部的 `=`/`:`/空白、
   `#`/`!` 都不会被再解释，值仅首字符空白需要转义）。
4. `\\` 自身是转义引导符；`\t \n \r \f` 让往返保持字符级相等（否则 `\n` 会断成两行）。

| 字符 | 键 | 值 |
|---|---|---|
| 行首空白 / `#` / `!` | 转义 | 仅空白转义 |
| 内部空格 `=` `:` `#` `!` | 空格/`=`/`:` 转义 | 原样 |
| `\`、`\t`、`\n`、`\r`、`\f` | 转义 | 转义 |
| 非 ASCII | UTF-8 原样 | UTF-8 原样 |

## 七条语义的测试对照

| 语义 | 测试函数 |
|---|---|
| 1 分隔符与空白 | `TestSeparators` |
| 2 注释 | `TestComments` |
| 3 续行 / 空白续行 / EOF | `TestContinuations`、`TestBlankContinuation` |
| 4 转义与 `\u` 错误行列 | `TestEscapes`、`TestUnicodeErrorPosition` |
| 5 重复键覆盖与首次序 | `TestDuplicateKeysOrder` |
| 6 Store 最小转义 | `TestStoreEscaping` |
| 7 1000 组随机往返 | `TestRoundTripRandom` |
| 计数器 ≤ 2N | `TestCheckCount` |
