# 设计推导（语义对齐 java.util.Properties）

## 第 3 条：只含空白的续行
基础规则只有两条：行尾奇数个反斜杠 → 去掉一个并拼接下一物理行；拼接前剥掉该行的首部空白。
推论：若被接上的行只含空白或为空，剥离后得到空串——既无可拼接内容，也没有新的行尾
反斜杠，于是逻辑行在此自然终止；该空白行被吞掉，再下一行开启新的逻辑行。
例：`k=v\` 换行 `   ` 换行 `x=y` → 逻辑行 `k=v` 与 `x=y`，即 k→v、x→y。
依据：OpenJDK `Properties.LineReader.readLine` 在续行后置 skipWhiteSpace=true 且
appendedLineBegin=true；此时行尾不会被当作空行跳过（!appendedLineBegin 不成立），
而是直接结束当前逻辑行，空白行本身不贡献任何字符。可见它不是额外规则，而是
「首部空白被剥掉」与「奇偶反斜杠」两条规则联立的必然结果，本实现用这两条规则推导即可。

## 第 6 条：Store 的最小转义集（由 Load 语法反推）
- 键：未转义的空白（空格/制表/换页）与 `=` `:` 会终止键 → 键内这些字符必须转义；
  `\` 是转义符本身，必须转义为 `\\`；`\n` `\r` 会截断逻辑行，必须转义。
- 键首的 `#`/`!`：键总在行首，行首第一个非空白字符是 `#`/`!` 时整行变注释，
  故键首的 `#`/`!` 必须转义；键中部的 `#`/`!` 不会被当作注释，不必转义。
- 值：分隔符之后、值开始之前的空白会被吃掉 → 只有值首的空格必须转义（`\t` `\f`
  已一律转义）；值中部的空格不被特殊解析，故不必转义，尾部空格加载时本就保留。
- 值中的 `#`/`!` 永不在行首（前面至少有 `key=`），不构成注释，不必转义。
- 非 ASCII：加载按字节透传 UTF-8，故原样输出即可，无需 `XXXX` 转义。

## 测试对照表
（待测试写完后补齐）
