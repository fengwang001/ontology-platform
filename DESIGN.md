# 严格流式 Base64 设计说明

## 第 2 条：规范形式的推导

Base64 把每 3 个字节编码为 4 个 6-bit 字符。输入尾部不足 3 字节时：

- 剩 1 字节（8 bit）：只需 2 个字符。第 2 个字符只承载 2 个有效 bit，
  其 6-bit 值的**低 4 bit** 是不参与解码的填充位。
- 剩 2 字节（16 bit）：只需 3 个字符。第 3 个字符只承载 4 个有效 bit，
  其 6-bit 值的**低 2 bit** 是填充位。

宽松解码器直接丢弃填充位，于是同一个字节串 `A` 有 16 种写法
（`QQ==`、`QR==`……第 2 字符低 4 bit 任意取值解码结果都一样）。
这破坏了「对任意字节串，编码结果唯一」：编码不再是单射，
`Encode(Decode(y)) == y` 不成立，编码文本也无法安全地做相等比较。

严格模式的统一规则：**尾部组的填充位必须全为 0**。

- `XX==` 组：检查第 2 个字符 6-bit 值的低 4 bit，即 `v[1] & 0x0F == 0`。
- `XXX=` 组：检查第 3 个字符 6-bit 值的低 2 bit，即 `v[2] & 0x03 == 0`。

`QR==` 中 `R` 的值为 17，`17 & 0x0F = 1 != 0`，即其二进制 `010001`
低 4 bit `0001` 是解码时被丢弃的填充位却非零，因此必须拒绝。
该规则是编码唯一性的充要条件：填充位全 0 时，每个字节串恰好对应
一个合法编码，严格解码是编码的逆映射，往返两条性质同时成立。

## 语义 → 测试对照表

（待测试写完后补齐）
