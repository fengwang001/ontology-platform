# DESIGN：无限制转置编辑距离

## 1. 推导：常见错误与正确递推

常见实现是「受限 OSA」：仅当 `a[i-1]==b[j-2] 且 a[i-2]==b[j-1]` 时允许
`d[i][j] = min(..., d[i-2][j-2]+1)`。它要求转置后两字符**立刻各自配对**，
转置完的片段不许再被编辑。

正确的「无限制」Lowrance–Wagner 递推（码点 1-基，`d[i][0]=i, d[0][j]=j`，
`k` = 上一个满足 `a[k]==b[j]` 的行，`l` = 当时与 `a[i]==b[l]` 匹配的列）：

```
d[i][j] = min(
  d[i-1][j]+1,                    // 删除 a[i]
  d[i][j-1]+1,                    // 插入 b[j]
  d[i-1][j-1] + [a[i]!=b[j]],     // 匹配/替换
  d[k-1][l-1] + (i-k-1) + (j-l-1) + 1) // 转置 a[k]a[i]
```
末项含义：转置成 `a[i]a[k]` 后，中间 `(i-k-1)+(j-l-1)` 个字符仍可继续编辑，
前缀代价是 `d[k-1][l-1]`——所以同一段转置后能再被编辑。

`CA -> ABC` 逐格（只列关键格）：

| 格 | OSA | 无限制 | 说明 |
|---|---|---|---|
| (1,3) C==C | 2 | 2 | |
| (2,1) A==A | 1 | 1 | |
| (2,2) | 2 | 2 | |
| (2,3) | **3** | **2** | OSA 无格可走；无限制取 k=1,l=3：d[0][2]+0+(-1)+1=2 |

路径：`CA --转置--> AC --插入B--> ABC`，共 2。OSA 因转置后 C 还要参与插入而
禁止该路径，错给 3。

## 2. 并列时的固定选择规则

回溯时按固定优先级取第一项：**匹配 > 替换 > 删除 > 插入 > 转置**。
（DP 取最小值不依赖优先级；优先级只决定多条等长脚本中输出哪一条。）

## 3. 码点

自写 UTF-8 解码：合法序列给正常 rune；每个非法字节给互不相同的**负**伪码点，
从而与 U+FFFD 及彼此都不同。

## 4. 语义–测试对照表

（最后一步补齐）
