# 多租户加权公平队列设计

## 模型
- 任务包含租户 ID、代价 `c`、全局提交序号；序号在入队临界区内分配，因此并发提交以拿到入队锁的顺序为准。
- 每个非空租户是一个 FIFO 队列，并维护虚拟时间 `vt`；调度堆始终只包含非空租户。
- 时钟通过 `task.Clock` 注入，仅记录提交时间；公平性由确定性的虚拟时间和序号决定。

## 为什么执行后增加 `c/w`
设权重为 `w`，两个租户在同一虚拟时间开始。租户 A 执行总代价 `C_A` 后，虚拟时间为 `C_A/w_A`，B 同理。调度器总是选择较小虚拟时间，均衡时有 `C_A/w_A = C_B/w_B`，所以 `C_A:C_B = w_A:w_B`。若使用 `c*w`，均衡会给出 `C_A w_A = C_B w_B`，份额与权重成反比。因此一次代价为 `c` 的任务后更新：

`vt <- vt + c/w`

权重 1:3 且任务等代价时，长期任务次数比例因此收敛到 1:3。

## 空闲租户重新加入
系统虚拟时间 `svt` 取当前活动租户的最小 `vt`；堆为空时保持上一次值。租户从空变为非空时：

`vt <- max(vt, svt)`

若不抬升，空闲租户 X 保留旧值，而 Y 已执行 1000 个等代价任务。权重为 1 时，X 与 Y 的虚拟时间差距是 1000 个单位；X 重新入队后会连续被选择，直到其 `vt` 追上 Y，最多独占约 1000 步。抬升后 X 与 Y 从同一虚拟时间开始，按权重和并列规则正常竞争，Y 不会被饿死。

## 零代价与数值边界
零代价任务合法，实际统计代价仍为 0；但虚拟推进使用 `max(c, epsilon)`，其中 `epsilon=1e-9`。否则零代价任务执行后 `vt` 不变，会无限小于其他租户而独占 CPU。虚拟时间使用 float64：权重范围限定为 `(0, 1e12]`，单任务代价限定为 `[0, 1e12]`，使 `epsilon/w` 正规化且正常有限；极大、极小权重并存时用 `math.IsInf/IsNaN` 防御。

## 并列、移除与复杂度
- `vt` 相等时按租户 ID 字典序选择，保证相同输入得到相同序列。
- 堆节点是“非空租户”，空闲租户不在堆中；因此选择代价与非空租户数 `n` 成对数关系。
- 提取最小节点及恢复堆序的比较次数上界为 `4*ceil(log2(n))`；1 个元素时上界取 0。
- 移除仍有排队任务的租户返回 `ErrTenantBusy`，任务不迁移、不静默丢弃；队列为空后才允许移除。
- 队列满返回 `ErrQueueFull`，只影响该租户，其他租户的映射、堆和队列不变。
