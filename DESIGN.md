# 设计推导：熔断 + 舱壁调用保护器

## 0. 分层与调用顺序

一次受保护调用的完整路径（顺序固定）：

```
请求 → breaker.Allow → bulkhead.Acquire → timeout.Do(fn) → classify.KindOf(err)
     → stat.Record* → breaker.Report(err) → bulkhead.Release(defer)
```

包划分：`classify`（错误分类）、`timeout`（单次时限 + panic 捕获）、
`bulkhead`（每下游并发额度 + 等待队列）、`breaker`（状态机）、
`stat`（统计与口径自洽）。组合逻辑很薄，由调用方（demo / 测试）按上面
的顺序串联，不单独设 "protector" 包。

## 1. 推导：被熔断拒绝的调用不计入失败率

失败率的意义是「下游健康度的估计」。设真实调用的失败率为 p，熔断打开
后到达的请求全部被拒。若把拒绝计入失败数，则打开期间每个请求都记为
一次失败，分母分子同步膨胀，失败率被推向 100%——这是熔断器自己制造
的样本，与下游是否恢复毫无关系。后果是正反馈：打开 → 拒绝计入失败 →
失败率更高 → 更没有理由关闭，熔断永远打不开。

正确口径：**只有「真实打到下游的调用」（真实调用数）才进入熔断的样本
与失败率统计**；被熔断拒绝、被舱壁拒绝的请求只计入各自的拒绝计数器，
不进入样本。这样半开期放行少量探测，用探测的真实结果决定关闭或重新
打开，系统才能收敛。`stat` 的恒等式 `真实调用数 = 总请求数 − 熔断拒绝
− 舱壁拒绝` 正是这一口径的代数表达。

## 2. 论证：熔断在舱壁之前

两种顺序对比：

- **熔断在前**：下游已知故障时，请求在第一步就被拒绝，不占用并发额度、
  不进等待队列。舱壁额度全部留给「熔断器认为有希望」的请求；拒绝是
  O(1) 的，拒绝本身零排队。
- **舱壁在前**：注定要失败的请求也会先占额度、占满后还要排队，等轮到
  了才被熔断拒绝。故障期间额度被死请求占满，半开探测可能排不上队，
  恢复被延迟；拒绝路径也变成阻塞路径，违背「快速失败」。

故规定**熔断在前**。可观测推论：熔断打开期间舱壁在途数恒为 0（demo
与测试均断言这一点）。

## 3. 论证：四条路径都必须归还额度

额度是有限资源（容量 N 的 channel 充当信号量）。Acquire 之后有且仅有
四条出路：成功、失败（业务错误）、超时（`timeout.Do` 先返回，fn 的
goroutine 可能还在跑，但从保护器视角调用已结束）、panic（在
`timeout.Do` 内 recover 转为普通错误返回）。任何一条路径漏还，额度就
单调泄漏；N 次泄漏后在途数恒为 N，所有后续请求要么排队要么被拒，
且永远无法自愈——比下游故障本身更致命。因此 Acquire 成功后立刻
`defer Release()`，使四条路径（包括 panic 被 recover 后的返回路径）
共用同一归还点。测试对四条路径各跑 1000 次断言额度回满。

## 4. 定义：不可重试错误是否计入熔断

定义：**不计入**。不可重试错误（参数非法、权限不足等 4xx 类）反映的是
「调用方发了坏请求」，重试同一请求必然再败，且它与下游健康度无关——
下游可能完全正常，只是如实拒绝了坏输入。若计入，一个发坏请求的调用方
就能把熔断打爆，让全体调用方被拒（错误的归因而又惩罚无辜者）。因此
`classify` 把错误分为可重试 / 不可重试 / 超时三类，`breaker.Report`
只把可重试与超时计入连续失败数与失败率样本；不可重试错误既不进分子
也不进分母。但它仍是「失败」，计入 `stat` 的失败总数与分类细分，
保证统计恒等式完整。

## 5. 其他关键决策

- **时钟注入**：`breaker` 的冷却判定、`bulkhead` 的拒绝时间戳都通过
  注入的 `Now()` 获取，测试用假时钟精确推进，断言「队列满立即拒绝时
  时钟未推进」「冷却到点才进半开」。
- **时钟回拨**：`Allow` 发现 `now < lastNow` 时返回可判定的
  `ErrClockBackward`，状态不变——回拨不得让冷却「提前到期」。
- **冷却退避**：半开期任一失败立即回到打开，冷却时长加倍并封顶
  `MaxCooldown`；回到关闭后冷却复位为基础值。加倍只发生在状态迁移
  内（持锁），并发触发只迁移一次，不会重复加倍。
- **半开并发**：半开期用计数器限制在途探测数 ≤ `HalfOpenProbes`，
  超出立即拒绝；探测全部成功才关闭。
- **panic**：在 `timeout.Do` 的 goroutine 内 recover，包装为
  `PanicError` 作为失败上报，包装器不被击穿。
- **四类可判定错误**：`breaker.ErrOpen`、`breaker.ErrClockBackward`、
  `bulkhead.ErrFull`、`timeout.ErrTimeout`（包装
  `context.DeadlineExceeded`），均可 `errors.Is` 区分。
