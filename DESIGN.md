# DESIGN — 自定格式 LZ77 流式压缩

## 流格式（wire 包）

所有多字节整数为 uvarint（LEB128，每字节 7 位，最高位续位；最多 10 字节，第 10 字节值 ≤1，否则报「变长整数超长」）。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4F 4C 01`：魔数 'O''L' + 版本 1 |
| 字面量段 | uvarint `tag`（`tag&3==0`，长度 `n=tag>>2`）+ `n` 个原始字节 |
| 回指 | uvarint `tag`（`tag&3==1`）+ uvarint `dist` + uvarint `len` |
| 刷新标记 | uvarint `tag`（`tag&3==2`），无负载，解码器视为空操作 |
| 流尾 | uvarint `tag`（`tag&3==3`）+ uvarint 原文总长 + uvarint FNV-1a64 校验和；其后不允许任何字节 |

记录类型编码在首 varint 低 2 位，因此任意字节序列都能被解析为某种记录——不存在「未知 tag」，损坏只表现为第 7 条列出的八类错误之一。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

- 位置 `pos` 处的「无匹配→字面量」判定只依赖 `pos` 之前的窗口内容与 `pos` 起 3 字节；窗口内容在 `pos` 之前已固定，故只要缓冲里 `pos` 后还有 ≥3 字节 lookahead，该判定就是终局。
- 「匹配长度 L」若 `pos+L` 恰好等于当前缓冲末尾，则未来数据可能让匹配继续变长，**未决**；若 `pos+L` 在缓冲末尾之前（已知下一字节失配）或达到 `maxMatch` 上限，则**终局**。
- 因此压缩器缓存从解析位置到缓冲末尾的未决尾巴（最多一个匹配的长度），只对已终局的决定发记录；哈希链只按解析顺序插入 `< pos` 的位置，候选集合同样只依赖数据本身。决定序列 = f（全部输入， Flush 点），与 Write 切法无关。
- 推论：`Flush` 声明「此处之后暂无数据」，必须以 final 语义强制结束当前匹配并吐出挂起字面量，否则未决状态永远无法被解码端还原——这正是 Flush 承诺的实现方式。

## 推导二：重叠回指的复制语义（第 2 条）

- `dist < len` 时源区间与目标区间重叠：一次性块复制读到的是「复制前」的字节，结果错误（`abc`+回指（3,30) 应得 30 个 `abc` 循环）。
- 逐字节向前复制使刚写出的字节立刻成为后续源，语义正确；总工作量 O（输出字节数），每字节 O(1)，不退化（相对输出的渐近复杂度已最优），故实现采用环形窗口上的逐字节 `At(dist)` 复制，距离在复制过程中保持不变。

## 推导三：「距离超已输出」与「距离超窗口」为何分开（第 7 条）

- 窗口容量是格式/资源上限（环形缓冲大小），「超已输出」是逻辑错误（引用了从未产生过的字节）。二者可独立违反：流开头 `dist=100`、已输出 50、容量 1MB → 只犯后者；`dist=2MB`、容量 1MB、已输出 10MB → 只犯前者。
- 压缩器只产生 `dist ≤ min（窗口容量， 当前位置）` 的回指，故两种错误都不可能出自合法压缩器。
- 并行块压缩的预置字典就是前若干块的原文，解码端这些字节**已经输出**，故「已输出字节数」仍是全局输出计数，含义不变。

## 推导四：并行块压缩的确定性（第 9 条）

- 块 i 的编码 = f（块 i 原文， 块 i 之前窗口容量以内的原文尾巴）：匹配器与窗口只用这两部分预置，不读其他块的压缩结果，与 goroutine 调度、完成顺序无关；结果按下标拼接即确定。
- 衔接无需刷新标记：记录自定界、回指距离是相对全局输出位置的绝对语义，解码器是纯正向记录解释器，块间直接相连；流头只写一次，流尾含全局总长与校验和。刷新标记仅为流式 `Flush` 承诺存在。

## 复杂度

每次 `Find` 考察 ≤ 链长上限 C 个候选，解析位置数 ≤ 输入字节数 n，故候选考察总数 ≤ C·n，与窗口大小无关（链随位置严格递减、越窗即停，不会随规模膨胀）。
