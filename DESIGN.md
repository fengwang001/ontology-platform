# 设计推导

## 1. 空行与单列空值
选择：**空行被跳过，不产生记录**。空行定义为不含任何字段字节（引号、字符、逗号）就遇到行尾：
状态机在 `start` 态见到 `\n`/`\r\n` 时若本行未产生任何字段，则不发记录。这样多列 CSV 里的
`a,b\n\nc,d\n` 只有两条记录，不会凭空冒出单列记录。

往返推导：回写器记录间不补尾随换行。单列记录的唯一字段为空时，若不加引号写成 `\n`
（两条记录之间），其字节形态就是空行，再解析时按上条规则被跳过——记录丢失；若该空记录是
末条记录，写成零字节则连"流结束"都无法表达字段存在。因此**单列空字段必须写成 `""`**，
这是引号标记必须保留的根本原因。多列中的空字段仍可保持不加引号（逗号确定字段存在）。

规范输入定义：字段仅在含 `,`、`"`、`\r`、`\n`，或它是单列记录的唯一且为空的字段时才加引号；
引号转义为 `""`；行尾一律 `\n`；文件末尾无换行；无空行。规范输入满足 `Write(Parse(x))==x`。

## 2. par 切点
每段在两种入口假设下各跑一次：A0=段首在引号外字段起点（含跨段续接的未引号态），
A1=段首位于引号字段内部（首字节按引号内内容处理）。段间真实引号状态只需知道一个布尔量：
自段首起引号外的裸 `"` 奇偶性——这在段内即可独立算出，故 K 段完全并行、零回扫。
拼接从左到右：段 i 选哪个假设由段 i-1 出口决定（出口在引号内→选 A1，否则 A0）；首段必为 A0。
微状态待定（`\r` 后、引号字段中刚见引号）只用段间 1 字节解决：看下一段首字节即可定性，
不需重扫；`\r` 是流尾则按孤立 CR 报错。
坐标换算：事件带绝对字节偏移（段基址+段内偏移）；记录号/字段号拼接时维护全局已完成记录数
`R`、当前记录已开始字段数 `F`，段内坐标分别加 `R`、`F`；错误若是首段过滤掉的空行，计数为 0。
每字节最多两次处理，总次数 ≤ 2N+O(K)，长引号字段不会引发重扫。

## 3. CR 待定状态
未引号字段中见 `\r` 进 `cr` 态，不立即入值：下一字节是 `\n`→行尾（`\r` 不入值、不发字符）；
是其他字节→孤立 CR，在该 `\r` 的偏移报 `ErrLoneCR`。流式半包：`\r` 后 `Feed` 返回可继续，
`Close` 仍处 `cr` 态则按孤立 CR 报错。引号字段内 `\r` 永远是普通内容，无待定态；其后的 `\n`
同样保留，故字段内 `\r\n` 原样保存。par 切点落在 `\r` 与 `\n` 之间时，`\r` 作为"段尾待定微状态"
由拼接方持有，用下段首字节（或确认流尾）定性，两段机器都不把它写入字段值。

## 4. 上限的"立刻"
字段字节数在每个内容字节（引号转义 `""` 只计 1 个逻辑字符，即第二引号到达时计 1）到达 sink
时计数，到达 `MaxFieldBytes+1` 的那一字节立即返回 `ErrFieldTooLarge`，字段缓冲只保留允许长度。
字段数在字段**开始**事件处计数（含逗号产生的字段），第 `MaxFields+1` 个字段开始即
`ErrTooManyFields`；记录数在每条记录完成时计数，第 `MaxRecords+1` 条完成即 `ErrTooManyRecords`。
出错即终态：机器记死错误，后续 `Feed` 原样返回同一错误值。

## 5. 状态转移表
状态：S=字段起点 U=未引号中 Q=引号中 E=引号中刚见引号 C=CR待定。

| 状态 | `,` | `"` | `\n` | `\r` | 其他 | EOF |
|---|---|---|---|---|---|---|
| S | 发空字段/下个 S | 入Q,标quoted | 空行则跳过否则发记录→S | 入C(本行无字段) | 字节入值→U | 本行无字段:结束;否则发记录 |
| U | 发字段/下个 S | **ErrBareQuote** | 发记录→S | 入C | 字节入值 | 发记录,结束 |
| Q | 入值 | 入值(1字符) | 入值 | 入值 | 入值 | **ErrUnclosedQuote** |
| E | 发字段/下个 S | 入值→Q | 发记录→S | 入C | **ErrCharsAfterQuote** | 发记录,结束 |
| C | 发字段/下个 S | ErrLoneCR(在\r处) | 发记录→S | ErrLoneCR | ErrLoneCR | ErrLoneCR |

错误位置均为触发字节偏移；`ErrLoneCR` 指向前导 `\r`。记录号、字段号从 1 起，字节偏移从 0 起。
列数不符 `ErrColumnCount` 在记录完成（含 EOF 记录）时判定。
