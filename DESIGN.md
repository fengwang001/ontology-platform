# 设计推导

## 1. 非法单元切分（WHATWG 式）
规则：读首字节 b0 得到期望长度 n。n 非法（80..BF、C0..C1、F5..FF）→ 单元 1 字节。
否则继续读后续字节；**第二字节 b1 必须落在首字节专属区间**，不合法则该单元只吞 b0 一个字节，
b1 作为新单元首字节重新解析；第 3、4 字节不在 80..BF 则单元吞掉此前已读字节（2 或 3 个）。
故 `F0 90 80 41`：b1=90 对 F0 合法、b2=80 非法续字节 → 吞 `F0 90 80` 三个，41 独立；
`E0 80 80`：b1=80 不在 [A0,BF]，E0 单字节成单元，80、80 各自成单元，共 3 个 FFFD；
`ED A0 80`：b1=A0 不在 [80,9F]，同理 3 个；`C0 AF`：b1=AF 对 C0 无合法区间，2 个；
`F4 90 80 80`：b1=90 超出 F4 的 [80,8F]，F4 单独后 3 个独立续字节，共 4 个；
`E2 82` 后流结束：两字节已属于同一未完成序列 → Close 时整个前缀算 1 个截断/替换；
`80 80`：两个游离续字节各成单元。

| 首字节 | 第二字节合法区间 | b1 非法时吞 |
|---|---|---|
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | （无首字节资格） | 1 |
| C0 C1 F5..FF | （永非法） | 1 |

第 3/4 字节非法时单元长度为已读字节数（2 或 3）；Close 残留按已缓存字节数。

## 2. 切分缓存上限与即时判定
UTF-8 最长 4 字节：状态机只缓存「首字节 + 已确认合法的续字节」，上限 3 字节
（第 4 个字节到达即可裁决，无需缓存 4 个）。判定时机：收到 b1 立即用专属区间判定，
不合法立刻把 b0 定为长度 1 的单元并回退 b1；b2/b3 非续字节立即把当前前缀整体定为单元。
UTF-16 状态最多缓存 1 个奇数字节或 1 个高代理码元。BOM 识别用极小前缀状态，不额外占缓存。

## 3. par 切点对齐
段 k 负责发射「起始偏移落在 [cut[k], cut[k+1])」的单元（末段含 Close 截断）。
解析起点为 cut-4：UTF-8 回看 3 字节即可覆盖任何越过切点的 4 字节单元；UTF-16 回退
4 字节（2 码元）可处理奇偶与代理对，故统一回看 4。起点前的单元只用于同步
（吞掉其字节与产出），不计统计、不写输出。合法性判定是前缀确定性的：从单元首字节
开始的裁决与全局一致；若回看起点落在某单元中间，WHATWG 规则会把残缺前缀拆成更小的
非法单元，且全部在 floor 之前被吞，到 floor 处必已重新同步（最坏 3 次单字节失败）。
如 `C3 | 80 80`：C3 属上段（末段 Close 时为 1 个截断单元），下段两个 80 各成单元，
总数与单线程相同。字节守恒：每个全局单元的全部字节恰好计入拥有其首字节的那一段。

## 4. 输出上限断点
每次发射标量前检查「现有输出 + 该标量编码长度 ≤ 上限」，放不下即停在标量边界并返回
ErrLimit 与已消费字节数 n。n 只包含**已完整裁决并输出**的单元字节；缓存中未裁决的
字节不算已消费（n 不含它们）。调用方把 `p[n:]` 喂给新实例：首字节状态干净，
逐字节结果等于无上限一次转完。UTF-16 代理对整体检查，绝不只写一个代理。
