# Quoted-Printable 设计说明

## 行末空白（空格/TAB）

解码器会在行尾（硬换行 `\r\n` 或输入结束处）把空格(0x20)与 TAB(0x09)当作普通
字节保留；但 RFC 2045 规定它们在传输行尾可能被剥离。因此：空白字节出现在输入
中间时原样输出；只有当其「后面紧跟换行或输入结束」时编码为 `=20` / `=09`。
判定靠前瞻一个输入字节完成，不回退。

## `=XX` 不被拆开 + 软换行插入点

每个待输出词元的宽度已知：字面 1，转义 3，软换行的 `=` 本身占 1（计入 76）。
设当前行已占 `n`，下一词元宽 `w`，且其后还有同一逻辑行的内容（`more`）。
当 `n + w > 76`（放不下）或 `n == 76 && more`（本行已满但内容未结束，不插入
软换行就会在后面产生第 77 列）时，先输出 `=\r\n` 再输出该词元。因此转义序列
整体落在同一行；最后的行若恰为 76 列则合法（其后无内容，无需续行）。

## 软换行前的空格要不要写 =20？

不要。解码器在还原时首先删除 `=\r\n`（连同其前的 `=`），软换行前那个空格解码后
位于输入中间，前后都有内容，并非行末空白，不会丢失。编码器仅在「空格后紧跟真正
的 `\r\n` 或输入结束」时转义空格；软换行是编码器自己插入的，插入前的空格保持
字面。若误写成 `=20=\r\n` 反而多余（仍合法，但非最小转义）。

## 往返等式

`D(E(x))` 与 `x` 的唯一差异是换行规范化：输入中的每个 `\n`（含 `\r\n` 中的）
都按 `\r\n` 输出，孤立 `\r` 作为普通字节转义。令
`norm(x)` = 将 `\r\n` 与孤立 `\n` 全部替换为 `\r\n`（孤立 `\r` 不动），则
`D(E(x)) == norm(x)`。对不含孤立 `\n` 的输入即 `D(E(x)) == x`。

## 最小转义（幂等性）

编码器只在两类位置转义：(a) 非字面可打印字节（33–126 且非 `=`，空白另行处理）；
(b) 行末空白。软换行只在几何上不可避免（上面两个条件）时插入。所以合法输入若
已无冗余转义，`E(D(y)) == y`；其余差异仅可能来自 y 对本可直出字节的多余转义。

## 测试对照表

<!-- 最后补齐 -->
