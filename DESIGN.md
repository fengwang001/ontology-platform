# DESIGN — 自定格式 LZ77 流式压缩

## 1. 流格式（整数均为小端 base-128 变长；长度/距离先减最小合法值）

| 记录 | 字节布局 |
|---|---|
| 流头 | `LZ7`(魔数3B) + 版本`1`(1B) + 窗口容量(varuint) |
| 字面量 | tag`0x00` + 字节数 n(varuint，n≥1) + n 个原文字节 |
| 回指 | tag`0x01` + (distance-1)(varuint) + (length-4)(varuint)，minmatch=4 |
| 刷新 | tag`0x02`（无负载） |
| 流尾 | tag`0x03` + 原文总长(varuint) + CRC32-IEEE(varuint) |

varuint 规则：每字节低 7 位为数据、最高位为续位，最多 10 字节；第 10 字节只能承载第 64 位，否则溢出。空输入产出「流头+流尾」。流尾必须是最后一个记录，其后任何字节都算多余。

## 2. 第三节四处推导

### 2.1 Write 切法无关（语义 3）
Write 边界处最长匹配可能尚未结束：若 `Write("abcab")` 就立即决定匹配/字面量，与随后到的 `"c..."` 一次写入的结果不同。
结论：压缩器把**未刷新的输入整体缓存**，Write 只追加不定稿；定稿只发生在 `Flush`/`Close`。定稿时对累计缓冲区从偏移 0 重放一次确定的贪心匹配。
因此输出只依赖「各 Flush 时刻的累计输入偏移」这个序列，与中间 Write 怎么切完全无关。Flush 必须强制定稿（结束当前匹配并发射刷新标记）：解压器见到刷新标记即保证此前记录可还原到该偏移；无新输入的 Flush 不发射任何字节（含刷新标记）。Close 等价于最后一次定稿后只写流尾，不写多余刷新标记。

### 2.2 重叠回指（距离 d < 长度 L）
块复制 `copy(out, out[len-d:])` 的语义取决于目标与源是否重叠：Go 的 `copy` 虽按正向逐字节搬运看似可用，但语义上不可依赖且在抽象的「先取整块再写整块」模型下，源会被自己刚写入的字节污染。
正确语义是**逐字节向前复制**：`for k:=0; k<L; k++ { out=append(out, out[len-d+k]) }`，当 k≥d 时读到的正是本轮刚复制出的字节，形成周期展开（距离 3、长度 30 得到 10 个周期）。
本实现直接采用逐字节循环：解压本身是 IO 路径，每字节一次追加无可避免，循环开销不改变渐近复杂度，且最不易错。

### 2.3 两类距离错误为何可区分
「距离 > 窗口容量」是**格式/配置级**非法：压缩器只在窗口容量 W 内寻找匹配，match 包的候选来自环形窗口，故合法压缩器（含预置字典）永不产生该记录；它与历史多少无关，可在解析记录时立即判。
「距离 ≤ W 但 > 已输出字节数」是**截断/越界级**非法：流本身可能合法，但当前历史不足（例如丢了前缀、并行块字典缺失）。流式解压器只能逐字节看到输出历史，故二者必须分别报错。
并行预置字典不改变判定：块 i 压缩时允许回指上一块末尾 W 字节，但这些字节在**流的更早位置**，顺序解压器早已输出。块间不产生独立流、不发射刷新标记，记录就是连续普通记录，「已输出字节数」仍是整条流解压到当前的累计长度。

### 2.4 并行块压缩的确定性（语义 9）
块 i 的匹配器初始窗口只预置 `data[i*B-W : i*B]`（不足则从头），候选链只由该字典 + 本块原文构造；它不读其他块的压缩记录、哈希链或完成顺序，只依赖原文两段，因此块输出是输入的纯函数。
衔接：块 0 写流头；各块把本块记录（字面量/回指）顺序追加，**块间无刷新标记**（刷新只由用户 Flush 决定）；最后一块追加流尾（总长=整段、CRC 覆盖整段）。故 workers 取 1/2/4/8 仅改变执行顺序与耗时，拼出的字节流逐字节相同，并能被普通流式解压器直接解开。

## 3. 复杂度

哈希链：每位置 p 插入 `prev[h(p)&mask]=p`（按插入序形成链），查找时沿链最多走 `maxChain` 个候选，故候选考察数 ≤ maxChain × 位置数；与窗口大小无关。环形窗口定容 O(W)。
