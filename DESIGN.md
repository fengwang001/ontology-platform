# 只读快照一致性导出器 — 设计推导

## 1. 不变量

`store.Store` 持有当前版本号 `ver`（每次 `Put` 原子自增）和当前映射。
`snapshot.Snapshot` 在创建时记录水位 `snapVer`。快照导出要求：每个键读到的值
**恰好是 `snapVer` 时刻可见的那一个**，与导出开始后发生的所有写入无关。

## 2. 写时复制（COW）的触发点

写入路径不能直接覆盖。`Put(k, v)` 时先取出旧值 `old`（及其可见版本 `oldVer`），
然后对每个活跃快照 `s`（按创建时间排序）：若 `oldVer <= s.snapVer`
（旧值是 `s` 的快照时刻可见值），则把 `old` 写入 `s.old[k]`，并立即停止对
更年轻快照的处理。

**为什么只保留给“最老的、仍可见该旧值”的快照**：快照水位单调，可见集合嵌套。
旧值若对最老候选 `s0` 不可见，则对任何更新的快照也不可见；一旦旧值进入某个
快照的保留表，该快照关闭时可把条目前传给更年轻的快照（第 3 节）。因此每次
写入只需按“最老快照版本”判断一次，保留条目总数恰好等于“导出期间被改写且
覆盖了某快照旧值的不同键数”，与总键数无关。

读路径：`s.Get(k)` 先查保留表 `s.old[k]`（含“已删除”墓碑，区别于键不存在
与空值），未命中再读 store 当前值；该键在快照之后未被改写时，当前值即快照值。
每个键全流程只读一次。

## 3. 保留值的释放（重叠快照）

关闭 `s` 时不能简单丢弃保留表：可能存在更年轻但仍活着的快照 `t`
（`t.snapVer > s.snapVer`）也需要该旧值。规则：对每个保留条目 `k->e`，若存在
比 `s` 更新的活跃快照 `t` 且 `e.ver <= t.snapVer`，把它并入 `t.old[k]`（仅当
`t` 尚未持有更新版本），随后清空并注销 `s`。故两个重叠快照各自持有所需旧值，
**只有两个都关闭后**保留值才全部释放、计数归零；这正是“按最老快照版本判断”
的闭环。

## 4. 导出顺序无关性

导出只是把快照可见 K/V 集合序列化。清单总校验和对**内容集合**计算
（每个条目 `crc32(keylen||key||vallen||val)` 异或，与顺序无关）。故字典序与
逆序导出总校验和相同、K/V 集合相同；块写出前统一按 key 排序，文件字节也确定。

## 5. 文件格式

块（自描述）：`["SNAP" 4][index u32][keylen u32][key][vallen u32][val]
[bodyCRC u32]`，bodyCRC 覆盖序号与载荷。块按 key 排序生成，内存只驻留当前
一个块；块大小 0 在构造时返回可判定错误。

组合文件 `name.snap`：依次放块，末尾放清单段
`["MANI" 4][json manifest][maniLen u32]["MANI" 4]`。清单含快照版本、块数、
每块偏移/长度/CRC、总校验和、块字节大小。校验时定位尾部 MANI：尾部缺失/
长度被截 = 清单不完整；块头字节不足 = 块头不完整；块体字节不足 =
块体不完整；bodyCRC 不符 = CRC 不匹配。纯截断不可能造成 CRC 不符，故 CRC
类用单字节翻转注入，FINDINGS 表 ② 注明。

## 6. 续传与快照过期

`Resume` 读清单得到已完整落盘的块数，只追加后续块，故对每个 k 续传结果与
一次性导出逐字节相同。续传前必须让 store 确认快照仍注册：快照关闭即过期，
返回 `ErrSnapshotExpired`（`errors.Is` 可判），**拒绝用当前数据续写**，否则
会拼出跨时刻的不一致文件；已导出部分原样保留。中断只表现为“块已写但最终
清单未提交”，不会有半截文件被当作完整文件。

错误：`ErrManifestIncomplete/ErrBlockHeader/ErrBlockBody/ErrCRC`，
`ErrBlockOrder`（携带缺失/错位块号）、`ErrSnapshotExpired`、`ErrBlockSize`，
全部 `errors.Is` 可判。

## 7. 并发

store 用一把 `sync.RWMutex` 保护当前映射、版本与快照注册表；保留表变更在
写锁内完成，`Get` 走读锁。快照关闭先在锁内摘除自己并前传条目，再等待活跃
导出计数归零（计数+条件变量登记），从而“关闭与导出竞争”要么导出先完成、
要么关闭后导出得到过期错误，绝不产生被误认完整的半截文件。
