# 设计推导

## 1. 为什么压缩结果与 Write 切法无关

压缩器只把字节追加到一个连续缓冲。普通处理在末尾保留 1 个未决字节：考察位置 p 时，先求当前最长匹配，再考察 p+1 的候选；只有后者没有更长匹配，才在 p 输出当前匹配或字面量。因此一个匹配是否开始取决于后续字节是否出现更长候选，而不取决于调用方是否在边界处返回。Write 只追加并循环处理，不截断匹配。Flush 会取消“延后 1 字节”策略，强制决定全部缓冲；所以刷新边界由累计输入偏移决定，和如何分包无关。

## 2. 重叠回指如何复制

块复制 `copy(dst,src)` 在重叠区可能一次读到旧数据，也可能读到新数据，不能表达 LZ77 的重复语义。正确语义是：每输出一个字节，它立即成为后续字节可引用的历史。解压器维护定长环形窗口，循环执行 `b=window.Get(dist); append(b); window.Put(b)`。长度在写入前先检查输出上限和 int 边界；距离 1 时这自然产生 RLE。每字节一次 O(1) 索引，总体仍线性，复杂度可接受。

## 3. 两类“距离过大”及预置字典

“距离超过已输出字节数”说明引用了尚不存在的逻辑位置；解码器即使窗口数组有旧垃圾也不能读。“距离超过窗口容量”说明引用超出压缩器承诺可保留的历史，是格式/配置错误。二者在检查总输出数后再检查容量，因此可区分。压缩器的候选来自固定容量窗口，且只输出 Get 成功的距离，绝不可能产生超容量距离。并行块的预置字典只供匹配器选择回指；它仍是上一块的原文，在流中位于本块之前，解压器逐字节解压时已经输出。故“已输出字节数”包含字典，含义不变，也不需要特殊记录。

## 4. 并行块的确定性与衔接

块 i 的候选只能来自其预置字典（上一块原文）和本块已扫描前缀；匹配算法、配置、哈希和链上限均固定，不读取任何其他块的压缩结果，也不依赖 goroutine 完成顺序。各任务完成后严格按块编号拼接。流中只写一次头和尾，块间不写刷新标记；块边界只是压缩器状态复位为“预置字典窗口”，解压器并不知道块边界，因此得到普通连续流。相同输入对任意 workers 只改变调度，不改变每块字节。

## 记录布局

| 记录 | 字节布局 |
|---|---|
| 流头 | magic `4C 5A 37` + version `1` |
| 字面量 | tag=0（varint） + 长度 varint + 原始字节 |
| 回指 | tag=1（varint） + distance varint + length varint，均从 1 计 |
| 刷新 | 单字节 tag=2 |
| 流尾 | tag=3 + 原始长度 varint + CRC32(IEEE) varint |

varint 使用 Go 无符号 base-128：低 7 位载荷，最高位表示后续；超过 10 字节或第 10 字节超出 64 位即错误。错误记录绝对字节偏移。
