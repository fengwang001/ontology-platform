# 设计推导：JSON 字符串字面量严格编解码器

## 第 2 条：代理对错误偏移

规则：**凡代理相关错误，偏移一律指向该代理单元自身起始的 `\`**。

- 高代理（如 `\uD83D`）一旦出现，解码器即承诺其后必须紧跟一个低代理
  `\uDC00`–`\uDFFF`；若未兑现，缺陷在于高代理无法成对，而不在后面
  的字节——`x`、`A`、`"`、`\u0041` 各自单独看都是合法内容，把偏移
  指向它们会诬告合法数据。故 `"\uD83D"`、`"\uD83Dx"`、`"\uD83DA"`、
  `"\uD83D\u0041"` 的错误偏移都是高代理的 `\`（偏移 1）。
- 孤立低代理（`\uDE00`）永远不能开启一个字符，错误即其自身起点；
  `"\uDE00\uD83D"` 在第一个单元就已失败，偏移 1，与顺序颠倒无关。
- `"\uD83DA"` 不能解释成「U+FFFD + `A`」：静默替换是有损修复，多个
  不同的非法输入会映到同一输出，使 Decode 非单射，第 5 条往返律
  立即破产；且 RFC 8259 对孤立代理的行为明确未定义，严格编解码器
  只能拒绝，不能替输入发明一个它从未包含的字符。

## 第 4 条：最小转义与唯一性

- RFC 8259 语法 `unescaped = %x20-21 / %x23-5B / %x5D-10FFFF`：必须
  转义的只有 U+0000–U+001F、`"`(0x22)、`\`(0x5C)。U+007F ≥ 0x5D，
  落在合法区间，原样输出即可；U+001F < 0x20，语法上禁止裸出现，必须
  转义。「必要」由语法界定，与字符是否可打印无关，故 U+2028/U+2029
  同样原样。
- 唯一性：Encode 是逐码点的全函数，每个码点恰命中一条规则（5 个短
  转义 / 其余控制字符 `\u00XX` 小写 / `\"`、`\\` / 原样），分支互不
  重叠且无自由参数，故同一输入的输出唯一。
- 单射：设 `Encode(s1) = Encode(s2) = y`。y 合法且最小，严格 Decode
  必成功；由往返律 `s1 = Decode(y) = s2`。故不同输入必有不同输出。

## 测试对照表

（待实现完成后补齐）
