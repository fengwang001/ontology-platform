# 结构化日志脱敏与采样设计

## 1. 边界与包

仅使用 Go 标准库；规则和采样状态在进程内存，sink 可写普通文件。拆为：

- `record`：记录值模型、深度/循环检查、确定性 JSON 编解码。
- `rule`：规则编译、路径/值规则、预编译集合与匹配计数器。
- `mask`：对匹配结果执行替换、SHA-256 哈希、截断。
- `sample`：追踪链一致性采样、error 强制保留、链不完整标记。
- `sink`：自描述二进制日志、长度前缀、CRC32、损坏分类。
- `cmd/demo`：无需参数的端到端验收演示。

## 2. 记录与嵌套路径

字段值仅支持 `nil`、`bool`、字符串、float64、[]any、`map[string]any`，JSON 编解码使用相同的确定性映射键序。最大嵌套深度固定为 8；深度指路径段数，根字段深度为 1。构造时做深度和循环检查，循环包括直接/间接 map 自引用；数组只遍历，不按数组元素产生路径段。

路径匹配同时维护两类键：

1. 优先把完整名称作为单个字面量字段名匹配；因此顶层字段 `a.b` 的路径就是 `a.b`，不会拆成 `a -> b`。
2. 若完整名称不存在，且名称包含点号，再按点号拆分为嵌套路径逐段进入映射。

该规则保证字面量字段优先，消除“字段名含点”和“嵌套路径”的歧义。任一路径段不是映射，或最终键不存在，均为未命中，不报错。进入数组时保留当前路径，对每个元素继续匹配；数组内映射再按其字段名继续。

## 3. 规则编译与复杂度

规则由路径、可选 Go RE2 正则、动作组成。路径规则编译进按路径段组织的 trie；值模式按记录深度优先遍历扫描值字符串。同一具体路径最多一个动作；同时配置替换和哈希等冲突在编译期返回错误，并指出两条规则。

匹配记录时，每个字段沿 trie 至多查询一次，路径规则匹配次数上界为“记录字段数 + 数组重复展开次数 + 常数”，与规则数无关。值模式线性扫描字符串值，数量在集合中显式保存；计数器供测试读取。

## 4. 脱敏语义

- 替换：写入固定串，不保留原长度。
- 哈希：输出 `sha256:` 加 64 个十六进制字符；同值相同，不同值以哈希区分，结果不等于原值且无法从输出还原。
- 截断：保留前 N 个 rune，并追加固定标记 `…[truncated]`；N<=0 时只保留标记。

匹配结果包含 map 容器与键，原地修改副本。记录先深拷贝再脱敏；未命中字段不改写，确定性 JSON 应逐字节相同。值若同时被路径规则、数组规则或值模式发现，所有匹配位置都处理。

## 5. 一致性采样推导

若每条记录独立随机决策，同一 `trace_id` 会同时出现保留和丢弃，调用链被截断。时间、顺序、级别、字段内容也会让重放结果漂移。正确决策只能是：

`keep = hash(namespace + "\x00" + trace_id) % denominator < numerator`。

使用 FNV-1a 64 位，一次哈希完成，单次决策哈希计数为 1。空追踪 ID 和缺失追踪 ID 统一为字面量 `__empty_trace__`，仍按同一确定性键决策；文档与测试固定该策略。采样率 0 时普通记录全丢，1 时全留。

`error` 及以上无条件保留。若其链决策为丢弃，记录保留并设置 `incomplete_trace=true`；链决策保留、非 error 或非强制保留时均不得设置该标记。

## 6. sink 格式与恢复

文件为：魔数 `ONTLOG1\n` + 版本字节 1 + uint32 主体 CRC32。其后逐记录写入 uint32 大端长度、确定性 JSON 主体、uint32 主体 CRC32。

恢复按最大可修复前缀读取：头部不完整、长度前缀不完整、记录体不完整均停止；长度完整但 CRC 不符返回 CRC 错误并停止，前序记录仍恢复。额外构造末字节翻转样例覆盖 CRC 不匹配。四类错误均为哨兵错误，支持 `errors.Is`。

## 7. 并发与验收

采样器和规则集内部计数器用 mutex 保护。演示把同一输入分别串行、并发处理，再按稳定序列化结果排序比较集合。深度超限、循环引用、规则冲突、sink 损坏分别使用可判定错误。demo 每步输出一行 `OK`/`FAIL`，总计不超过 20 行且退出 0。
