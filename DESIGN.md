# DESIGN：消息头部集合的解析、规范化与安全回写

本设计只覆盖头部块（header block）：若干 `Name: value` 行，以一个空行终止。
规范线分隔为 CRLF；规范形态为 `Canonical-Name: value`，值无首尾空白、无折行，
块以 `\r\n\r\n` 结束。五个包单向依赖：
`token` ← `fold` / `listval` / `policy` ← `headers`。

## 1. 数据模型与处理管线

- `headers.Set` 内部是有序切片 `[]entry{name, value}`，名字一律以规范形态存储；
  另有 `map[string][]int` 名字→位置列表，位置列表按出现顺序 append，故多重映射保序、不去重。
- 管线：字节 → 逐行解析（名字 / 冒号 / 值 / 折行 / 终止空行五阶段）→
  折行展开（`fold`）→ 值字符合法性（`token`）→ 规范化入表 →
  查询/增删改（受 `policy` 约束）→ 重折行（`fold`）→ 安全回写。
- 解析先写入本地构建器，全部成功后一次性提交；任何错误都不改变接收者已有状态。
- 并发：`sync.RWMutex`，只读操作持 RLock 可并发；增删改持 Lock，**不支持与读并发之外的并发写**
  （写者互斥，实际可并发但文档不承诺无锁扩展；调用方应串行组织写操作）。

## 2. 折行（语义 2）

续行是以 SP/HTAB 开头的行。展开 = 删掉续行前导的全部空白，压成**一个** SP 接到上一值后；
因此解析后值里永远不会出现行断点。块的第一行就是续行时，没有可依附的上一行，
判为 `StageFold` 语法错误。

回写时 `FoldWidth > 0` 且值超长则重折：**只允许在值中已有的 SP 处把该 SP 替换为
`"\r\n "`**，绝不在无空格处硬切。硬切会凭空插入一个 SP，再展开时无法还原。
若存在长度超过宽度的不可切分 token，该行允许超长——宽度是尽力目标，无损优先。
因为折点替换的 SP 与展开恢复的 SP 一一对应，`解析→回写→再解析` 的值逐字节相同。

## 3. 名字与冒号之间为什么必须报错（语义 3）

规范形态规定冒号紧跟名字。宽容地"去掉中间空白"看似兼容，实际打开走私面：

1. 上游网关与本组件的空白容忍度不同，会对同一字节流产生**不同的头部集合视图**。
   攻击者发 `X : Evil`，严格网关把整行当作非法/忽略，宽容后端却得到 `X: Evil`，
   于是注入了网关看不见的头。
2. 反向同理：`X\t: 1` 在一处被当作名为 `"X "` 的陌生头丢弃，在另一处变成 `X`，
   可绕过按名做的鉴权与缓存键。
3. 空白是不可见字符，日志与人工审计中不可见，差异无法被发现。

安全边界必须由"拒绝"而不是"猜测修正"构成，因此名字段遇到 SP/HTAB 后再遇冒号，
返回确定性的 `ErrNameColonSpace`，不做任何修正。

## 4. 注入防御（语义 4，核心）

禁止字符集合按字节定义：值只允许 HTAB(0x09) 与 0x20–0x7E（VCHAR 加空格）；
CR(0x0D)、LF(0x0A)、NUL(0x00) 以及其余所有 <0x20 的控制字符、DEL(0x7F)、
非 ASCII 字节一律拒绝。控制字符在回写格式里就是结构性字符（CR/LF 起新行、NUL 截断 C 串），
允许任何一个就等于允许追加任意头部。

**拒绝而非静默删除**：删除改变了数据含义却让发送方以为成功，
`a\r\nEvil: 1` 被删成 `aEvil: 1` 后，安全策略（签名、完整性校验、审计）全部对着
一份被篡改的数据生效，且无任何信号；拒绝则把冲突暴露给调用方，由其决定丢弃还是报警。

编码绕过不能依赖"下游会解码"才安全，本层做两道扫描：

1. 原始字节直扫，命中禁止字节即拒；
2. 编码形态扫描：`%HH` 百分号解码、`\xHH`、`\uXXXX`、`\r \n \0` 转义解码后再验一遍，
   解码结果含禁止字节即拒。

实测拦截变体（全部在 `Set`/`Parse` 入口拒绝，回写字节断言不含 `Evil`）：
`a\r\nEvil: 1`、`a\nEvil: 1`、`a\rEvil: 1`、`a\x00b`、
`a%0d%0aEvil:%201`、`a\rEvil: 1`（反斜杠转义）、`a\u000dEvil`。
名字同样只允许 tchar，CR/LF/SP/冒号均不在集合内，名字通道无注入面。

## 5. 列表与引号（语义 5）

逗号只在**引号字符串外**作为分隔符；引号内反斜杠转义下一字符（`\"` 不闭合字符串）。
若不实现该规则，带参数的标准头会被切碎，例如
`Accept: text/html, text/plain;level="1,2"` 会把一个参数值 `"1,2"` 切成两个元素，
后续按元素匹配/合并得到伪造的媒体类型。空项（`a,,b`）选择**判为语法错误**
（确定、可测，不静默吞数据）；元素首尾 OWS 去除。合并用 `", "` 连接，
引号原样保留，因此再切分得到同一元素序列（语义等价，空白归一不改变列表语义）。

## 6. 回写等价与幂等（语义 6）

输入已规范（CRLF、名字规范大小写、值无首尾 OWS、无折行）时，回写是纯重组装，
逐字节等于输入。输入不规范（LF 行尾、名字大小写、首尾 OWS、折行）时，
"正确输出"定义为上述规范形态的唯一表示，并置 `Normalized()=true`。
幂等论证：规范化是一个**取代表元**的函数——每个规范化步骤（大小写映射、trim、
空白压缩、CRLF、折行展开）作用于已规范输入都是恒等函数，故规范形态是该函数的不动点，
再解析再回写不再产生差异，`Normalized()` 保持为真且字节不变。

## 7. 截断、资源上限与索引（语义 7、8、10）

解析是显式五阶段状态机，EOF 出现在哪一阶段就返回带头部序号与
`StageName/StageColon/StageValue/StageFold/StageEnd` 的错误；每个前缀只可能得到
成功或该错误，无 panic、无半截集合（成功才提交）。
四类上限（条数 / 名字长 / 值长 / 总字节）对应四个可判定的哨兵错误。

索引为 `map[规范名][]int`。按名查找先做一次 map 定位（O(1) 平均），
再按位置列表顺序取条目；计数器 `findCompares` 只统计**同名桶内**逐条比较，
N 个互不相同的头时单次查找计数为常数（0–1），不随 N 线性增长（50 与 5000 对照相等）。
保序不依赖索引：权威顺序始终是切片，索引只存位置；新增 append、删除后统一重建位置，
故索引更新不改变切片顺序。最坏情况（全部同名）单次比较上界为该名字的出现次数 K ≤ N。
