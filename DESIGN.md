# 严格 JSON 字符串编解码器 — 设计推导

## 1. 代理对错误偏移（语义 2）

规则：错误偏移永远指向「第一个不合法码元单元」的起始字节，即该单元反斜杠 `\` 的位置。

- `"\uD83D"`：高代理在结尾引号前仍等不到低代理。高代理本身是一个不完整的语法单元
  （它的合法产生式是 `\uXXXX\uXXXX`），错误归属于提出需求的它，偏移 = 高代理的 `\`。
  若指向结尾引号，则 `"\uD83D"` 与普通缺引号无法区分，违背五类错误可区分的要求。
- `"\uDE00"`：低代理不允许作单元开头，非法单元就是它自己，偏移 = 该 `\`。
- `"\uD83Dx"` / `"\uD83DA"`：紧跟高代理的字节不是新 `\u` 单元（`x` 是普通字符、
  `A` 也是），高代理配对失败，偏移 = 高代理的 `\`。
- `"\uDE00\uD83D"`：第一个单元即非法低代理，偏移 = 第一个 `\`，扫描在更早位置中止。

为什么 `"\uD83DA"` 不能解释成「替换字符 U+FFFD + A」：U+FFFD 替换是非法 UTF-8 字节流的
恢复策略，处理的是字节层；而 `\uD83D` 是结构完整、语义非法（孤立代理）的转义单元，
属于语法层错误。严格解码器的职责是拒绝而非恢复；一旦容忍，合法/非法输入将得到同一输出，
违反唯一性与「不得静默替换」。因此解析为 A 是普通字面字符的同时，高代理配对失败即报错。

## 2. 最小转义与唯一性（语义 4）

RFC 8259 §7 规定：字面量中只有引号、反斜杠、控制字符 U+0000–U+001F **必须**转义；
其余一切字符（含 `/`、DEL U+007F、非 ASCII、U+2028/U+2029）原样合法。

- U+001F 必须转义：它是 C0 控制字符，未转义出现在字面量内是语法错误（五类错误之一）。
- U+007F 不必转义：DEL 不属于 0x00–0x1F 区间，RFC 不要求，转义反而不是最小形式。

单射性：若 `Encode(a)=Encode(b)`，对输出逐字节反转义即可唯一还原，故 `a=b`。
输出唯一性：每个码点的编码分支互斥且确定——`"`/`\\` 固定短转义；
`\b\f\n\r\t` 固定短形式；其余 0x00–0x1F 固定 `\u00XX` 小写十六进制；
其他码点固定原样 UTF-8。码点序列又由字符串唯一确定，故整体输出唯一。

## 3. 语义—测试对照表

（实现完成后回填）
