# DESIGN — 统一格式差分/补丁器的设计推导

## 1. hunk 头零计数时的行号（语义 4）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0`；`a\n`→空 得 `+0,0`。
推导：计数 b≥1 时 a 是区间首行的 1 基行号；b=0 时区间为空，"首行"无定义。
三个样例中插入点分别在第 0 行之后、第 2 行之后、第 0 行之后（新文件侧），
数字恰好等于"插入点之前已有的行数"，即**前一行的行号**（文件开头视为虚构的第 0 行）。
若把它解释成"将要插入的位置"（1 基），开头插入应得 1 而非 0，与样例矛盾；
解释成前一行行号则三个样例全部吻合，且与 GNU diff 一致。
Apply 解释：b=0 时插入点 = 0 基下标 a（前 a 行之后）；b≥1 时区间起点 = 0 基下标 a-1。

| b 取值 | 头写法 | Apply 对旧侧的解释 |
|---|---|---|
| b=0 | `-a,0`，a=前行行号 | 插入点在下标 a，不删除 |
| b=1 | `-a`（省略 `,1`） | 替换从下标 a-1 起的 1 行 |
| b≥2 | `-a,b` | 替换从下标 a-1 起的 b 行 |

（`+c,d` 侧同理，d=0 表示新文件在该处为空。）

## 2. hunk 合并阈值 = 2C（语义 5）

每个 hunk 首尾各带至多 C 行上下文。两段改动间隔 g 行未改动行：
若拆开，前 hunk 占尾部 min(g,C) 行、后 hunk 占头部 min(g,C) 行。
g ≤ 2C 时两段上下文相接或重叠，GNU diff 输出单个 hunk（间隔行全部作为上下文）；
g = 2C+1 时正中间有 1 行不属于任一 hunk，输出两个 hunk。故阈值为 **g ≤ 2C 合并**。
反例：若阈值取 C，则 C=3、g=5 会被拆开，但 GNU 输出单 hunk（5 行间隔全为上下文）；
若取 2C+1，则 g=7 会被合并，但 GNU 输出两个 hunk（中间行不在任何 hunk 中）。

## 3. 最短脚本的并列选择（语义 3）

Myers 贪心每一步在"删除（右移）"与"插入（下移）"间选更远者；
本实现规定：**V[k-1] ≥ V[k+1] 时优先删除**（仅严格小于才插入），回溯用同一规则。
效果：同一改动块内所有 `-` 行排在 `+` 行之前（与 GNU diff 一致）。
例：`a b`→`b a` 生成 `-a`、（保留 `b`）、`+a`；`x`→`y` 生成 `-x` 后 `+y`。
规则固定，故同一输入重复生成结果逐字节相同。

## 4. 偏移应用后后续 hunk 的查找起点（语义 7）

Apply 始终在**目标文本的原始坐标**中工作：hunk 依次应用，已应用部分用游标
切出，位置匹配都在原文下标上进行，因此本补丁自身造成的行数增减不影响后续
查找。需要补偿的是目标文本相对补丁记录位置的**整体漂移**：若第 i 个 hunk 在
记录位置 s_i 偏移 k 处匹配成功，说明目标文本在该区域附近系统性平移了 k 行，
故第 i+1 个 hunk 的查找中心取 s_{i+1}+k（沿用最近一次偏移，不累加），
再在其上下 F 行内按"距离最近、同距取前"选候选。漂移是目标文本的局部属性，
用最近一次观测值作预测最优；若后续区域漂移不同，±F 搜索会纠正。

## 5. 其他约定

- 行比较含行尾：`x\n` 与 `x` 是不同行，故"仅末尾换行变化"产生 `-`/`+` 各一行。
- 渲染：无行尾的行输出后补 `\n` 再加 `\ No newline at end of file`。
- 错误四类可判定：`udiff.ErrFormat`、`patch.ErrContext`、`patch.ErrOffset`、
  `edit.ErrTooLarge`，均可用 `errors.Is` 区分；hunk 失败带序号。
