# DESIGN

## 1. 流格式（所有整数均为无符号 LEB128 varint）

| 记录 | 首字节 | 载荷布局 |
|---|---|---|
| 流头 | `0xFF 0x4C 0x01` | 魔数两字节 + 版本 1，共 3 字节 |
| 字面量段 | `tagLit=0` | tag, varint n, n 个原文字节 |
| 回指 | `tagRef=1` | tag, varint distance(>=1), varint length(>=3) |
| 刷新标记 | `tagFlush=2` | 仅 tag；承诺此前全部输入可还原 |
| 流尾 | `tagEnd=3` | tag, varint 原文总长, varint CRC32(IEEE) |

流 = 头 {字面量|回指|刷新} 尾；尾后不得有字节。窗口容量/链长不写进流：distance 合法性靠「≤窗口容量、≤已输出」两条。

## 2. Write 切法无关（语义 3、4）

压缩器是确定性的「贪心最长匹配」函数：输出只依赖完整输入序列与 flush 偏移集合。做法是保留未决尾 `pend`。每次 Write 追加 `pend` 后，只提交那些「未来一字节已确定」的位置：

- 位置 i 的最长匹配能否再延长取决于 i+L 处字节；当 i+L < len(已见总输入) 时答案已确定（候选链是历史位置的纯函数），于是提交该 token，i 前进。
- 提交匹配后，i..i+L 的哈希插入在提交时统一完成；提交字面量则插入 i。
- 只要后续字节不变，已提交 token 永不改变，切法不影响输出；最后一个字节在 Close 前永远留在 `pend`。
- Flush 强制把全部 `pend` 按同一规则提交完（末尾匹配按当前长度截断），再发刷新标记。无新输入时没有未决字节、不重复发标记（flushed 标志），故连续 Flush 第二次零字节。Flush 不清空窗口，之后匹配可跨 flush 边界。

## 3. 重叠回指的解压（语义 2）

distance<length 时源区间与目标区间重叠，一次性块复制会从「还没生成的新位置」读到旧数据（d=1 时只能复制一个字节）。采用逐字节循环 `out[j]=out[j-d]`：第 j 字节写出后立刻可作为 j+d、j+2d 的来源，语义正是逐字节向前复制。热路径只访问定长窗口数组，且长回指数量少、复制总量是原文线性规模，逐字节完全可接受。

## 4. 两类距离错误与预置字典（语义 7、9）

「distance>已输出字节数」指逻辑上无此历史（开头就回指）；「distance>窗口容量」指历史虽长但超规格引用。压缩器候选距离恒 ≤W，正常流不可能产生后者。并行块把「上一块原文末尾 W 字节」同时填入本块窗口与哈希链，但这些字节不是本块解压输出：它们在本块流中必须以字面量或更早回指实际出现，故解码端无需特殊处理，distance 仍只与「本解压器已输出字节」比较。块间不插刷新标记：标记是调用方的 flush 承诺，块边界不是；仅第一块写头、仅最后一块写尾。

## 5. 并行确定性与复杂度（语义 9、复杂度约束）

块 k 的状态 = 预置字典(块 k-1 原文后缀) + 本块原文，worker 开始前都已知；哈希插入顺序、链上限、贪心规则全部确定，故块输出与调度、workers 数无关，按序号拼接即可。匹配长度封顶 maxMatch=16384：全同字节每 token 至少前进 3，链候选最多考察 chain 个（命中长度触顶即跳出），单字节考察数被夹在与规模无关的常数内，64KB→4MB 每字节考察数相差 ≤2×；4MB 输出仅约每 16KB 一条回指（各约 4 字节），远小于 64KB。候选计数由非导出字段 `examined` 累加。
