# 最短往返十进制编解码器设计

## 1. 问题与判据

目标：float64 → 十进制文本 → float64，逐位相同（`math.Float64bits` 相等），且文本有效数字最少。

**为什么不是固定 17 位**：17 位一定能往返。证明：相邻两个 double 的相对间隔
≥ 2^-53 ≈ 1.11e-16，半 ulp ≥ 2^-54 ≈ 5.55e-17；而保留 17 位有效数字的舍入相对误差
≤ 5e-17 < 2^-54，故 17 位最近十进制数必落回原浮点的舍入区间。但多数值用更少位即可
（如 `0.1` 只需 1 位），17 位不是最短。**为什么 15 位不够**：15 位舍入相对误差可达
5e-15 ≫ 半 ulp，大量值（如 `1/3` 需要 16 位）无法往返。

**正确判据（逐位递增 + 精确往返检查）**：对 k = 1, 2, …, 17，把原值的精确值舍入为
k 位有效十进制数字，再用精确算术把该十进制数解回 float64，逐位比对；第一个能往返的
k 即答案。k=17 必成功，循环必有终止。`0.1` 在 k=1 即成功，`1/3` 在 k=16 成功。

## 2. 精确算术做法（不用浮点回代）

- `bits`：`math.Float64bits` 拆出符号 s、阶码 E、尾数 f。正规数值 = (-1)^s · m · 2^e2，
  其中 m = 2^52|f，e2 = E-1075；次正规 m = f，e2 = -1074。m、e2 都是整数，值精确。
- `dec`：用 `math/big.Rat` 表示精确值 m·2^e2。对给定 k：先与 10 的整数幂（big.Int）
  比较定出十进制阶 e10（10^e10 ≤ r < 10^(e10+1)），再把 r·10^(k-1-e10) 按
  「商 + 余数×2 与除数比较、平局取偶」舍成整数 n（半偶舍入，确定性）。若 n 进位为
  10^k 则 n/=10、e10++。去掉尾部零得到数字串 digits 与阶 e10。
- 往返检查：由 digits·10^(e10-k+1) 重建 big.Rat，调 `(*big.Rat).Float64()`——它按
  IEEE 最近偶舍入给出精确结果——与原始 bits 比较。全程无浮点乘除，无二次误差。
  浮点回代（Horner 浮点累乘）会把累计舍入误差当成「相等」，测试中有反例：某 16 位
  十进制浮点回代看似等于原值，精确比较却落在相邻浮点上，正确答案是 17 位。
- `dec` 内置非导出原子计数器记录精确往返检查次数（仅诊断用，不影响输出，`-race`
  干净）；`Checks()`/`ResetChecks()` 供测试与 demo 读取。对 `0.1` 恰为 1 次（≤3）。

## 3. 文本形式（fmtf，确定性规则）

设科学计数阶为 E（值 = d.ddd…×10^E，E = e10，digits 去尾零后长度 n）：

- **定点形式** 当且仅当 -4 ≤ E < 21（与 Go `%g` 同界）：如 `0.1`、`123.45`、
  `100000000000000000000`（1e20，E=20）。
- **指数形式** 否则：`d[.ddd]e±E`，如 `1e+21`（E=21）、`5e-324`（E=-324）。
- 符号：负值前缀 `-`；`+0`/`-0` 分别编成 `0` / `-0`，文本不同、各自往返。
- 同一值的 E 与 digits 唯一，故输出唯一、可复现；编码是纯函数，无全局状态。

## 4. NaN 与 ±Inf 不给文本形式

十进制文本无法表达它们；若静默输出 `"NaN"`/`"Inf"` 字符串，则 `parse(fmtf(x))` 必然
失败或与合法输入冲突，违反「编码成功即可往返」的契约。故 `fmtf.Encode` 对它们返回
可判定哨兵错误（`ErrNaN`/`ErrInf`），由调用方显式处理。

## 5. parse 契约与错误分类

只接受 fmtf 能产出的文法：`[-]d[.ddd][e±E]`，有效数字 ≤ 17，合成后十进制阶
|e10| ≤ 400（覆盖 5e-324 到 1.8e+308 所需范围）。解析用 big.Int 收 digits、big.Rat
合成精确值，再 `Float64()` 最近偶舍入，最后施加符号（含 `-0`）。错误均可 `errors.Is`：

- `ErrEmpty`：空串；
- `ErrInvalidChar`：非法字符，错误信息含字节位置；
- `ErrExpOverflow`：指数越界（|e10| > 400 或指数字段本身溢出 int）；
- `ErrTooManyDigits`：有效数字 > 17，无法保证按本契约往返。

## 6. 包划分

`bits`（拆分）→ `dec`（最短有效数字，精确算术）→ `fmtf`（组装文本）→ `parse`
（文本解回）→ `cmd/demo`（验收演示）。仅标准库；核心路径不调用
`strconv.FormatFloat`/`ParseFloat`，仅在测试与对拍中作参考。
