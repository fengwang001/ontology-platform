# DESIGN

## 末尾策略
规范化主体先把所有行尾转为 `\n`、删除行尾空白；策略只作用于最终输出尾部。
`Preserve` 保留主体尾部；`EnsureOne` 去掉尾部多个 `\n`，非空主体补一个 `\n`，空主体仍为 `""`；`TrimTrailing` 删除末尾空行，非空主体保留一个 `\n`，空主体为 `""`。
选择空输入输出 `""`：若输出 `"\n"`，再次规范化时 `"\n"` 不是空输入，无法回到 `"\n"` 而不依赖历史，破坏朴素幂等。

| 输入 | Preserve | EnsureOne | TrimTrailing |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 被删字节映射
`ToOrig` 必须单调不减且 `ToOut(ToOrig(o))==o`。输出只含保留点和新增点；保留点取删除区间后的下一个原文字节作为其代表点。
因此被删字节统一 `ToOut(i)=后继保留点`；若它是行尾 `\n`，光标落在换行符上（之后），因为删除发生在逻辑行边界，而不是行内空白位置。
末尾删除无后继输出点时夹到输出终点。新增终点无原文后继，代表原文终点。

## 缓冲超限
状态机只缓冲两种跨片待定物：一个 `\r`，以及连续空格/制表符。空白可能无限长。
选择“拒绝”：返回带原文起点的 `ErrPendingLimit`，已输出字节和映射保留；再写返回 `ErrClosed`，状态可解释。
若强制判定为非行尾，之后若看到行尾，真实结果应删除这些字节，已输出内容无法收回，违反切分一致性、幂等和映射互逆。

## par 拼接
各段用未结束的流式规范化器处理到字节边界。片段只有两类非干净前缀：待定 `\r`、待定空白。
拼接器先让相邻片段共享边界字节：若前段末字节是 `\r`，后段重放该 `\r` 后再吃自身内容；随后只截断后段开头被该 `\r` 吸收的部分，并丢弃其空输出。
空白不跨段提前决策：每段关闭时段尾空白已按其当时的截断文本判定；但并行结果不能依赖该判定，所以切点不直接落在“非换行后的空白串”内部——以最近 `\n` 后为安全边界，最多给 worker 一个有界前缀，取安全边界后的输出与映射。坐标统一加全局偏移。
全是单个无换行长空白时只有一个 worker；因此修正只检查段边界附近，并受重放一个字节和安全边界提取限制。
