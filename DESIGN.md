# 媒体类型内容协商器 — 设计推导

包划分：`mtype`（解析/规范化，零依赖）→ `rank`（单项匹配与特异性评分）→ `negotiate`（对外入口）。依赖方向单向，无反向依赖。仅标准库，状态全在内存，解析器手写（不用 `mime.ParseMediaType`）。

## 数据模型

- `mtype.Type{Type, Subtype string; Params map[string]string}`：类型/子类型与参数名一律小写，参数值保留原始大小写（语义要求值大小写敏感）。`q` 在 `negotiate` 层从参数中剥离，不参与匹配。
- q 值用千分整数（0–1000）表示，避免浮点比较误差。

## 第 1 条：为什么先比 q 再比特异性

q 是客户端声明的意愿强度，特异性只是"这条声明有多精确"的平局裁决。若反过来先比特异性，输入 `Accept: text/*;q=0.9, text/plain;q=0.1`、候选 `[text/plain]`：先比特异性会选中 q=0.1 的精确项，按 q=0.1 的意愿返回；而客户端对 `text/*` 明确给了 0.9。再例：`Accept: text/plain;q=0, */*;q=1`、候选 `[text/plain]`，先比特异性会让 q=0 的精确项压过 q=1 的通配，与规则冲突。因此主序是 q，次序才是特异性（精确 3 > 类型通配 2 > `*/*` 1）。

## 第 2 条：q=0 是拒绝；非法 q 整条报错

若把 `q=0` 当"最低优先级"：输入 `Accept: text/html;q=0`、候选 `[text/html]`，"最低优先级"解释下它是唯一匹配项，会被兜底选中——返回了一个客户端明确说不要的类型。故规则：某候选的最佳匹配项 q=0 时，该候选被排除，即使它是唯一候选，结果为「无法接受」。

非法 q（`q=1.5`、`q=abc`、`q=0.1234`）选择**整条 Accept 报错**而非丢弃该项：非法 q 说明头部构造有 bug，该项意图不可知——它可能本想表达 `q=0`（拒绝），丢弃后该项失效，候选可能被 `*/*` 兜底接受，恰好返回客户端想拒绝的类型。静默纠错会把客户端的 bug 变成服务端的错误响应，fail-fast 让调用方能判定并记录。此选择在 `TestInvalidQ` 中固定。

## 第 4 条：同分为什么取服务端顺序

q 与特异性都相同时，客户端对这些候选无差别，决定权应交给内容的生产者：服务端候选清单的顺序就是服务端偏好（如把压缩率更高、渲染更便宜的格式放前面）。若取 Accept 书写顺序，同一服务端在不同客户端书写习惯下会返回不同表示，缓存键与行为都不稳定。故平局按候选清单下标升序取第一个。

## 第 5 条：缺失与空串必须区分

HTTP 语义里「头部缺失」= 客户端没表达偏好，按 RFC 7231 等价于 `*/*`，应正常协商；「`Accept:` 空串」= 客户端表达了偏好但列表为空，即"没有任何可接受的类型"，必须判无法接受。若合并处理（都当 `*/*`），空串会被错误地协商成功，调用方无法区分"客户端无所谓"与"客户端什么都不想要"，后者本应触发 406。API 用 `*string` 承载：`nil` = 缺失，指向 `""` = 空串，二者可判定地区分。

## 解析错误五类（彼此可区分，带项序号）

`mtype` 定义哨兵错误：`ErrNoSlash`、`ErrEmptySubtype`、`ErrParamSyntax`、`ErrUnclosedQuote`、`ErrBadQ`；`negotiate` 包装为 `mtype.Error{Index, Item, Kind}`，`errors.Is` 可判定类别，`Index` 给出问题项在 Accept 列表中的 0 基序号。

## 只读与可复现

协商只读 `offered` 切片，择优用纯函数遍历，无全局状态、无 map 迭代顺序外泄（参数相等用集合比较，不依赖顺序），故重复调用与随机打乱 Accept 项顺序结果一致。
