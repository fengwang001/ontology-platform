# DESIGN

## 1. 非法 UTF-8 单元

从首字节确定理论长度；每收到一个必需续字节就验证其区间。某字节一旦证伪前缀：已通过的“合法最大前缀”组成一个非法单元，输出一个 U+FFFD；证伪字节本身不吞入，立即作为新首字节重放。流结束时仍缺字节，已有前缀只输出一个 U+FFFD。

因此 `F0 90 80 41` 的前三字节是合法最大前缀，`41` 证伪并另起字符；`E0 80 80` 中第二字节已证伪，只吞 `E0`，两个 `80` 各自是 stray continuation；`E2 82`+EOF 是一个未完成前缀，只替换一次。`ED A0 80` 在第二字节离开 surrogate 合法区间，三字节各自成单元；`C0 AF`、`F4 90 80 80` 同理逐字节重放。

| 首字节 | 第二字节合法区间 | 第二字节非法 | 后随续字节非法/EOF |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 吞 1 | 吞 1（EOF） |
| `E0` | `A0..BF` | 吞 1 | 吞 2 |
| `E1..EC` | `80..BF` | 吞 1 | 吞 2 |
| `ED` | `80..9F` | 吞 1 | 吞 2 |
| `EE..EF` | `80..BF` | 吞 1 | 吞 2 |
| `F0` | `90..BF` | 吞 1 | 吞 2 或 3 |
| `F1..F3` | `80..BF` | 吞 1 | 吞 2 或 3 |
| `F4` | `80..8F` | 吞 1 | 吞 2 或 3 |
| `80..BF` | 无 | stray，吞 1 | — |
| `C0 C1 F5..FF` | 无 | 非法首字节，吞 1 | — |

## 2. 流式缓存

UTF-8 最长 4 字节，尚未判定时至多保存首字节后的 3 个续字节；UTF-16 代码单元为 2 字节，代理对至多再缓存 2 字节，故硬上限统一为 3 字节。看到第二字节超出首字节专属区间、第三/四字节不是 `80..BF`、数值越过 surrogate/U+10FFFF，或 UTF-16 出现孤立代理，即可立刻判定；只有“仍匹配某个合法前缀”才等待。UTF-16 奇数字节或高代理后 EOF 是截断，不是非法字节。

## 3. 并行切点

对原始切点 `c`，从 `max(0,c-3)` 重同步：向后跨过至多 3 个紧邻续字节，若前一字节是能覆盖这些续字节的 lead，则再包含 lead。从该点按全局状态解析，跳过起点小于 `c` 的第一个单元，返回下一单元起点；若文件在单元中结束则返回 EOF。这些调整边界把输入分成完整单元段，边界数 K 固定、每边界只看常数字节。

于是 `C3|80 80` 的切点被移到第二个 `80` 前：上段解析合法 `C3 80`，下段解析 stray `80`；切点落在非法单元中时，整个单元归给包含其起点的相邻段，另一边界跳过它。每段独立流式解析自己的完整边界区间，首尾相拼的输出、非法单元数和吞掉字节数必然等于单流。

## 4. 上限与断点

输出前先检查整个标量的目标长度；空间不足则不消费该标量、不写半个目标字符，立即返回 `ErrLimit`。本次 `Write` 的 `n` 只计入在本次调用中已经进入完整输出标量的字节；切分缓存中仍未形成标量的字节不算输出消费，可由 `Pending()` 取出。续传时把 `Pending()` 与 `p[n:]` 交给新实例，拼接结果等价。正常 EOF 后，`Close` 再统一把缓存记为截断或一个非法单元。
