# 严格 JSON 字符串编解码设计

## 1. 代理对错误偏移

JSON 把非 BMP 字符编码成紧邻的高代理 `\uD800-\uDBFF` 与低代理 `\uDC00-\uDFFF`。
解码器读到高代理时，不立即输出 U+FFFD，而是进入“必须匹配低代理”的状态。

错误偏移指向第一个不可能让当前语法单元成立的字节：

- 孤立低代理：指向其反斜杠，因为它没有待匹配的高代理。
- 高代理后遇 EOF、结束引号，或下一字符不是 `\u`：指向高代理的反斜杠，因为这个代理单元从起点就无法闭合。
- 高代理后有 `\uXXXX`，但该单元不是低代理：指向后一单元的反斜杠，因为高代理本身合法，失败发生在它要求的匹配位置。
- 高代理后出现 `\u` 但 EOF 或十六进制不足：仍指向高代理起点，因为整个待配对语法单元不完整。

因此 `"\uD83Dx"` 在偏移 1 拒绝，`"\uD83DA"` 也在偏移 1 拒绝：
`A` 不是新转义单元的开始，不能回溯地把尚未闭合的高代理改写成 U+FFFD 再接受 `A`。
`"\uDE00\uD83D"` 的第一个错误是偏移 1 的孤立低代理；顺序错误不会延迟到第二个单元。

## 2. 最小转义与唯一性

RFC 8259 强制字符串中的引号、反斜杠和控制字符 U+0000-U+001F 必须转义。
U+001F 是控制字符；U+007F 虽有 DEL 的历史含义，却不在 JSON 的控制字符范围内，原样 UTF-8 输出合法。
`/`、U+2028、U+2029 及其他非 ASCII 字符同样无需转义。

编码映射是固定的：`"`→`\"`，`\`→`\\`，`\b\f\n\r\t` 使用短形式，其余 27 个控制字符使用唯一的小写 `\u00XX`。
每个源码点的首字节可区分两类输出：以反斜杠开始的一定是转义，其他字节原样输出；短形式和四位十六进制形式互不重叠。
所以解码结果可唯一切分并恢复源串，`Encode` 是单射；同一码点没有第二种合法选择，输出也唯一。

## 3. 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
