# 设计推导

## 1. UTF-8 非法单元

首字节决定期望长度；非法首字节自身是一个单元。合法首字节后顺序吸收续节，只吸收能构成合法字符的最短必要前缀；续节不在该首字节允许区间时，立即终止：非法续节留给下一轮，首字节及此前已吸收续节共成一个单元。故 `F0 90 80 41` 为 `F0 90 80|41`；`E0 80 80` 为 `E0|80|80`；截断的 `E2 82` 在 EOF 成一个单元。

| 首字节 | 第二字节区间 | 非法续节吞掉 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 非法节留下，首字节 1 个成单元 |
| `E0` | `A0..BF` | 非法节留下，首字节 1 个 |
| `E1..EC` | `80..BF` | 非法节留下，首字节 1 个 |
| `ED` | `80..9F` | 非法节留下，首字节 1 个 |
| `EE..EF` | `80..BF` | 非法节留下，首字节 1 个 |
| `F0` | `90..BF` | 非法节留下；首字节 1 个 |
| `F1..F3` | `80..BF` | 非法节留下；首字节+第一续节 2 个 |
| `F4` | `80..8F` | 非法节留下；首字节+第一续节 2 个 |
| `80..BF` | 无 | 自身 1 个 |
| `C0 C1 F5..FF` | 无 | 自身 1 个 |

## 2. 切分缓存

最多缓存 3 字节。看到非法首字节立即处理；合法首字节看到非法第二字节即失败；三、四字节序列若第二字节合法、第三字节非法，失败长度分别为 2、3。只有“目前仍可能补成合法字符”的前缀等待；EOF 时缓存整体是一个截断/非法单元。UTF-16 最多缓存 1 个奇数字节；高代理最多缓存 1 个 code unit。

## 3. 并行切点

每段只从起点回看最多 3 个 UTF-8 字节来判断该点是否属于前一个四字节序列：若为四字节首字节、三字节且已带一个合法续节、或合法续节且其前两个字节构成四字节合法前缀，则跳过相应 1..3 字节。该判定需要后续 1 个字节参与；恰在边界截断时由前一段 EOF 接管。UTF-16 回退到偶数码元边界；高代理后的切点跨段携带一个码元。这样被跳过的尾巴与前段前缀同属一次解码，非法单元不会跨段重复。例：`C3|<80><80>` 中首个 `80` 属于前段 `C3 80`，后段从第二个 `80` 开始。

## 4. 输出上限断点

编码一个完整标量前先检查容量；不足即停止，不写半个标量或代理对。`Write` 返回的 n 只包括已完整输出标量覆盖的输入字节；尚在缓存中的合法前缀和未判定字节不算消费。调用方从 n 续传到新实例；旧实例不再使用，因此边界上不会重复或等待。
