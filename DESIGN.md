# DESIGN — 带遮蔽与前向引用的作用域符号表

## 模型约定

- 声明与引用都携带调用方给定的源码位置序号 `pos`（`Declare(n,k,pos)`、`Ref(n,pos)`），
  位置序号表达源码先后，而非 API 调用先后；这是前向引用可被表达的前提。
- 解析规则：从引用点所在作用域沿父链向外，逐层用哈希表定位名字；本层命中即裁决，
  不再向外。`KindForward` 命中即成功；`KindStrict` 要求 `ref.pos >= decl.pos`，
  否则报 `ErrUseBeforeDeclare`；整条链无该名字报 `ErrUndefined`。

## 推导一：同层重复声明 —— 覆盖还是错误？

- 选项 A「后者覆盖前者」：若已有引用解析到前者，覆盖后重解析同一引用点会得到
  后者，`SelfCheck` 的「已记录引用仍解析到同一声明」立即被违反；前者成为不可达
  垃圾，同一引用点的解析结果随时间漂移，违反不变量 2（唯一且可归属）的稳定性。
- 选项 B「可判定错误」：声明一旦创建即不可变，任何已记录引用永久稳定。
- **结论：B，哨兵错误 `scope.ErrDuplicateDeclare`。** 当重复声明发生在已有引用
  解析到前者之后时，因为拒绝，前者仍是该名字在本层的唯一声明，已记录引用重解析
  结果逐字段不变，不变量 2 与 SelfCheck 都保持成立。

## 推导二：遮蔽 × 前向引用（核心）

场景：内层声明 `x`（允许前向，pos=5），引用 `x` 在 pos=2，外层也有 `x`（pos=1）。

- 选项 A「解析到内层 x@5」：遮蔽以「本层声明集合」为单位整体生效，位置序号只
  决定 strict 种类是否报错。不变量 3 无条件成立；捕获集合不依赖外层内容。
- 选项 B「解析到外层 x@1」：内层的 forward 声明在同名外层存在时，对它之前位置
  的引用永远不可达 —— 不变量 3 在遮蔽场景下被直接违反。且内层明明声明了 `x`，
  引用却逃逸到外层：遮蔽不完整（违背不变量 1 的精神），捕获集合混入「本层已
  声明」的名字使不变量 4 的精确性依赖外层内容；同一作用域内的同名引用按位置
  分裂到两个不同声明，解析语义割裂。
- **结论：A。** 对称地，若内层 `x` 是 strict 种类，pos=2 的引用报
  `ErrUseBeforeDeclare` 而不是落到外层 —— 名字在本层有声明即本地裁决。

## 推导三：捕获的判定边界

与推导二一致：名字在本层有声明 ⇒ 本地裁决。forward 种类命中本层（不算捕获）；
strict 种类且引用在声明之前 ⇒ 解析失败，失败的引用不捕获任何东西。
因此捕获集合 = 本层记录的、成功解析到**严格外层深度**的引用所命中的声明，
按（深度, 名字）去重。本层声明在引用点之后的外层同名名字绝不进入捕获集合。

## 推导四：最外层 Leave

静默忽略与成功无法区分，调用方无法发现 Enter/Leave 不配对，「被拒绝的操作」
变得不可判定，直接削弱不变量 5。**结论：返回哨兵错误 `table.ErrLeaveRoot`，
且不改变任何可观测状态**（当前指针、各层声明表、引用日志全部不动）。

## 附：查看条目计数器

`resolve.Resolver` 的非导出字段 `looked`（`atomic.Int64`）：每访问一个作用域的
声明表计 1（哈希定位，不遍历该层）。演示程序需要打印实测值，故提供只读访问器
`Looked()`；字段本身不导出、外部无法修改，公开接口不接受也不返回该字段。
