# Properties 解析器设计说明

## 第 3 条：空白续行的推导

依据 `java.util.Properties`（`LineReader.readLine`）参考逻辑逐条推导：
1. 行尾是奇数个 `\` 时，该反斜杠（连同换行符）被丢弃，逻辑行继续到下一物理行。
2. 续接行首若为空白（` `、`\t`、`\f`），先把前导空白整体跳过，再追加正文。
3. 关键在于该“跳空白”是按自然行执行的：若整个续接行只有空白（或为空行），跳完后没有任何字符可追加，但续行链并没有终止——终止逻辑行的只能是“行尾反斜杠为偶数/无反斜杠且已有非空白内容”。空白行行尾不含反斜杠，所以循环继续读取下一自然行。
4. 结论：`k=v\` + 换行 + `   ` + 换行 + `w` 中，空白行被整体吞掉，结果为 `k`→`vw`；空行同理。注释也只在“自然行行首”判定：`k=v\` + 换行 + `#b` 中 `#` 属续行追加（结果 `a#b`）；被吞的空白行之后、真正位于自然行行首的 `#` 才是注释。
5. 与“续行首部空白被剥掉”的关系：同一规则的两种表现——剥空白剥到出现非空白字符为止；整行剥光，就等价于该行（连同换行）不产生任何内容，读取继续。

## 第 6 条：Store 转义规则推导

原则：只转义会让回读歧义的字符。回读规则是“自然行行首 `#`/`!` 为注释；键中未转义的 `=`/`:`/空白是分隔符；值前导空白被吃掉；`\` 是转义引导”。
- 键：行首的 `#`/`!` 会使整行变注释 → 必须转义；`=`、`:`、空白（行首与中间）会被当作分隔符 → 必须转义；`\`、`\n`、`\r`、`\t`、`\f` 必须转义。
- 值：行首的空格/`\t`/`\f` 会在分隔符后作为“值前导空白”被吃掉 → 值开头空白必须转义；值中间与结尾的空白保留，故不必转义；值里的 `=`/`:` 及中间的 `#`/`!` 回读时都是普通字符 → 不转义；只需转义 `\` 与控制字符 `\n \r \t \f`。
- 空键允许（直接写 `=value`）；空值允许（只写键）；统一以 `=` 作分隔符。
- 非 ASCII 原样按 UTF-8 输出（`Load(Reader)` 按 rune 解码，无需 `\uXXXX`）。

## 测试对照表（语义 → 测试函数）

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | props_test.go: TestSeparators |
| 2 注释 | logical_test.go: TestComments |
| 3 续行（奇偶/空白续行/注释/EOF） | logical_test.go: TestContinuation |
| 4 转义与 \uXXXX 行列号 | props_test.go: TestEscapes |
| 5 重复键覆盖与首次顺序 | props_test.go: TestDuplicateKeys |
| 6 Store 特殊字符回写 | props_test.go: TestStore |
| 7 1000 组随机往返 | props_test.go: TestRoundTripRandom |
| 复杂度计数器 ≤ 2N | logical_test.go: TestCheckCount |
