# LZ77 流式压缩设计

## 流格式（wire 包）

多字节整数一律为 uvarint（LEB128：每字节 7 位有效载荷，最高位为续位；超过 10 字节或第 10 字节大于 1 判为溢出 `ErrVarintOverflow`）。

| 记录 | 字节布局 |
|---|---|
| 流头 | `4F 4C 5A 01`（"OLZ" + 版本号 1） |
| 字面量段 | `00` + uvarint(n) + n 字节原文 |
| 回指 | `01` + uvarint(dist≥1) + uvarint(len≥1) |
| 刷新标记 | `02` |
| 流尾 | `03` + uvarint(原文总长) + uvarint(FNV-1a 64 校验和) |

## 推导一：压缩结果与 Write 切法无关（语义 3）

位置 p 的决策只依赖两类信息：p 之前的全部字节（决定窗口内容与哈希链）和 p 之后有限的前瞻。据此定规则：
- p 处的匹配在「匹配结束于已有数据之内」（p+len < total，失配字节已存在）或「长度达到 maxMatch 上限」时即确定，更多输入不可能再改变它；
- 若匹配恰好延伸到当前数据末尾且未达上限，它可能随新输入变长，必须挂起，等更多数据或 Flush/Close；
- p 处无匹配（字面量）在 p+minMatch ≤ total 时即确定（窗口与 p 处字节此后都不再变），但字面量段先缓存在 lit 中，直到遇到一个确定的匹配、达到 maxLiteral 上限（按个数切，与切法无关）或 Flush/Close 才落盘，因此不会在 Write 边界上提前截断字面量段；
- 哈希插入按位置单调推进（inserted 指针），位置 q 必在任何 Find(p>q) 之前插入，与 Write 边界无关。
于是任意切法下产出的记录序列逐项相同。未决字节至多 minMatch+maxMatch 个，缓冲有界。Flush 必须强制结束当前匹配（final=true）：Flush 承诺「截至目前的输入全部可还原」，挂起的匹配只能按现状落盘，否则这些字节无法被解码——这正是 Flush 的语义来源。

## 推导二：重叠回指的逐字节复制（语义 2）

dist < len 时回指的源与目标重叠：第 i 个输出字节会被第 i+dist 个字节引用。一次性块复制（memmove 语义，按复制前的快照读）会把尚未还原的字节当作源，得到错误结果；逐字节向前复制（读 Back(dist) → 追加 → 再读）读到的总是已还原字节，天然正确。本实现保留逐字节循环并论证其可接受：任何解压器的下界都是 Θ(输出长度)，块复制只能压低常数因子；该循环无分配、顺序访问环形缓冲，实测 4MB 还原为毫秒级，正确性则免费获得。

## 推导三：「距离超过已输出」与「距离超过窗口」为何区分（语义 7）

- 「超过窗口容量」是结构性错误：dist > cap 的回指在任何输出进度下都非法。压缩器绝不可能产生它——匹配器只在窗口容量内考察候选（链上 c < total-cap 即断链），dist ≤ cap 恒成立。
- 「超过已输出」是语义性错误：dist ≤ cap 但 dist > 已输出字节数，即引用尚不存在的字节，只有损坏或手工构造的流才会有。
二者成因与定位不同，故分为两个哨兵错误，解码器先查窗口、再查已输出。并行块压缩中预置字典让块内回指可指向上一块原文，但对解码器而言那只是「已输出字节」的自然延续——流是连续的，「已输出」的含义不变，无需特殊处理。

## 推导四：并行块压缩的确定性（语义 9）

块 i 的压缩记录 = f(块 i 原文, 其前 windowCap 字节的原文)。字典是上一块末尾的**原文**（预置进窗口与哈希链），不是任何其他块的压缩结果，因此：
- 每块输出与其他块是否完成、以何顺序完成无关，workers 只影响调度，结果对 workers=1/2/4/8 逐字节相同；
- 块内 dist ≤ windowCap，而字典对应的字节在流中先于本块输出，衔接处天然满足 dist ≤ 已输出；
- 流格式上块间无需刷新标记：解码器是连续状态机，流头 + 各块记录依次拼接 + 流尾即为合法流；Flush 标记只服务于流式 Flush 承诺，与分块无关。
