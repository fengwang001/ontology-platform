# 自然排序比较器设计

## 比较规则的分层
1. 切分：字符串切成「数字段 / 非数字段」交替序列（仅 ASCII 0-9 为数字）。
2. 主键：逐段比较。数字段按数值（去前导零后先比长度再比字典序，任意长、不转整数、不溢出）；
   非数字段按字节序；一段为数字、另一段为非数字时，比较当前位置两个字节（数字字节必小于
   大于 9 的非数字字节、大于小于 0 的非数字字节，与写法无关）。一方序列先耗尽则短者小。
3. 次键（前导零）：数值相等但文本不同的数字段不立即判定，只记录第一处差异
   （文本短者小，即前导零少者小），继续向后比较；整串主键相等时才用该记录分胜负；
   主键、次键皆同则两串完全相同，Compare 返回 0。

## 第 3 条推导
- 假设「立刻比较前导零」：a01b vs a1c 首段 01/1 数值相等即按前导零判 a1c < a01b，
  与样例 a01b < a1c 矛盾。故前导零不能立刻参与，必须让位于后续内容。
- 假设「前导零完全不参与」：则 a1 与 a01 判等，违反「非完全相同不返回 0」。
- 唯一出路：前导零差异降级为末位决胜——主键全部相等时才启用，且只取第一处差异。
- 演算 a01b vs a1c：a=a；01/1 数值相等，记录次键 01>1（待定）；b<c 主键分出胜负，次键丢弃。
- 演算 a1b01 vs a01b1：a=a；1/01 数值相等，记录次键 1<01；b=b；01/1 数值相等（次键
  已记录，不覆盖）；主键耗尽相等，用次键，得 a1b01 < a01b1。
- 验证其余样例：a1<a01<a001（主键全等，次键文本短者小）；x0<x00<x000<x1（前三者
  主键等、次键定；000 数值 0<1 主键直接定）。

## 第 4 条传递性论证
令 K(s)=(P(s), Z(s))：P(s) 为记号序列（数字段映射为数值，非数字段映射为字节串），Z(s) 为数字
段原文序列。记号间序：数值序 / 字节序 / 数字-非数字按首字节类别（非数字字节必整体小于 0 或大
于 9，故该序与前导零写法无关），是全预序；序列的字典序复合仍是全预序；(P, Z) 再作字典序复合仍
是全预序。K(s)=K(t) 当且仅当两段序列逐段相同，当且仅当 s=t（段拼接唯一还原原串），故该预序实
为串上的严格全序，反对称、传递、Compare(a,b)=0 当且仅当 a=b 同时成立。非 ASCII 字节（含 UTF-8
多字节、全角数字）只是普通非数字字节，上述论证不依赖其取值范围，故全序不被破坏、也不会 panic。

## 测试对照表
| 语义 | 钉住它的测试 |
| --- | --- |
| 1 基本（file2<file10 等） | natural.TestCompare（basic-*） |
| 2 大数不溢出（40 位） | natural.TestCompare（bignum-40-digit） |
| 3 前导零分层规则 | natural.TestCompare（zeros-*） |
| 4 严格全序（三元组穷举） | natural.TestTotalOrder |
| 5 打乱 50 次排序结果唯一 | natural.TestSortDeterministic |
| 6 非 ASCII 不 panic、按字节 | natural.TestCompare（nonascii-*）、chunk.TestNext |
| 复杂度：扫描字节数 ≤ 2(len a+len b) | natural.TestLastCompareBytes |
