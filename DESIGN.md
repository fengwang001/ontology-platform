# DESIGN：批量导入的幂等与断点续传

## 1. 进度粒度推导（核心）

写入分两类：

1. **非幂等写入**（如 `INSERT`、计数器自增）：重放一条已写记录会产生重复或副作用，
   进度文件必须在**每条写入被确认持久化后**刷盘，粒度只能为 1。
   fsync 次数 = 记录数，吞吐塌方，且仍无法区分「写成功但进度未落盘」的歧义。
2. **按业务键幂等写入**（本题前提）：`Write(batchID, key, value)` 对同一
   `(batchID,key)` 重复执行结果恒等——同值返回成功，异值报键冲突。于是
   「进度尚未记录、但记录已落盘」的窗口不再致命：续传时重放这段记录是安全的，
   最多得到一次无害的幂等命中。

结论：进度采用**粗粒度**，每写满 N（默认 1000）条刷一次高水位，记为若干半开区间
`[start,end)`。崩溃丢失的只有最后不足 N 条的进度，续传时重放窗口
`[max(0,hwm-N),hwm)`，**重放条数 ≤ N，与已导入总量无关**，故无需每条刷盘。

## 2. 断点续传：以存储为准，而非以进度为准

进度文件可能丢失/截断/撒谎（本题强制构造「声称 5 万、实存 3 万」）。
进度只是**提示**，存储才是事实。续传探测：设进度恢复出的高水位为 C，
用二分在存储中判定 `[0,x)` 是否连续存在（每次判定 O(1) 哈希查询，
整体 O(log n)，不重头扫描），得到实际连续前缀 P。续传起点
`r = min(max(0,C-N), P)`。

- P ≥ C：存储领先于进度，从 C-N 重放 ≤ N 条幂等命中；
- P < C：进度撒谎，起点回退到 P，缺口 `[P,C)` 全部重写。

因此重写数上界恒为 N，且绝不会基于虚假进度跳过未写记录。

## 3. 重复导入的幂等判定

批次状态写入进度头部：`prepared|committed`。Commit 成功后为 committed。
再次导入时若读到 committed 且存储中该批记录齐全，立即返回可判定错误
`ErrAlreadyComplete`（语义为「本批次已完成」，不是失败），存储逐字节不变。
零记录批次同样在 Commit 后置 committed。

## 4. 部分批次的可见性

存储为每个键维护 `value + state(inflight/committed)`。未 Commit 的记录处于
inflight，reconcile 明确标注：已提交键集与在途键集分开统计，在途不计入已提交。
对中断批次以半开区间输出三段：已写入区间、缺口区间、多余记录（存储有但清单
无），端点精确到记录下标。提交时该批全部 inflight 原子提升为 committed；
不同批次键空间独立，异批撞键触发唯一约束错误。

## 5. 进度文件格式与截断恢复

文本格式：4 行定长头部（魔数/批次ID/记录数/状态），随后每行一个区间
`<start>-<end>`，末行是其上方全部字节的 CRC32-IEEE（8 位十六进制）。
截断逐字节分类：

- 截断点落在前 4 行任一完整行之前/之内 → 头部不完整 `ErrHeaderTruncated`；
- 落在区间行内（缺换行）→ 区间记录不完整 `ErrRecordTruncated`；
- 文件完整但 CRC 不符 → `ErrCRCMismatch`。

恢复一律回退到**最大可恢复前缀**：不完整的最后一行整体丢弃，保留其前所有
完整区间。加载返回（恢复出的进度, 可判定错误），导入器据错误继续而非中止。
正常保存走临时文件 + rename，不自行产生截断。

## 6. 并发占用

`<dir>/<batchID>.lock` 存持有者 token + 注入时钟给出的过期时间。获取：
文件不存在则创建；存在且未过期 → `ErrBatchBusy` 立即拒绝；已过期 → 接管
（token 比对保证旧持有者不会误删新锁）。不同批次锁文件不同，可并行导入；
锁 TTL 由注入时钟判定，崩溃进程的锁到期后安全接管。

## 7. 对账线性一遍

清单按 key 有序、存储键集排序后做归并扫描：游标各自只前进，存储哈希查询仅用于
构造成员表与状态标注，每条记录对存储与清单各至多一次访问，访问次数 ≤ 3n，
禁止嵌套查询。

## 8. 资源约束与边界

- 记录按需经 `Source` 产生，Importer 设窗口上限 M（默认 128），
  峰值在途记录 ≤ M，历史峰值由计数器记录。
- 失败注入 `Store.FailAt(globalIndex)`：第 k 条写入返回错误，进度停在失败点
  之前；k=1 / 中间 / 末条位置效果一致。
- 边界：空串业务键合法；空批次 ID 拒绝；同批重复业务键拒绝并给出键与两处
  位置；空批次与单记录批次正常提交；进度文件不存在视为从头开始。
