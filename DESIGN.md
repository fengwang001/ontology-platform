# 增量聚合视图维护器设计

## 1. 撤回策略推导

- Count 的状态只是成员条数，删除一条必然减 1，不需要知道其他成员。
- Sum 的状态是所有成员值的代数和，删除时减去该值即可；+0 与 -0 在 IEEE754 数值集合语义上相等。
- Min/Max 的状态只保存当前极值。删除非极值时新极值不变；但删除的恰为当前极值时，聚合状态中没有第二小/第二大的信息，必须枚举该组现存成员重新比较。
- DistinctCount 的状态只有去重后基数。删除某值时无法判断该值是否仍被另一条成员持有，必须检查该组成员中该值的剩余出现次数。
- 因此聚合器声明 `NeedsMembersOnDelete(value, wasExtreme)`；Count/Sum 恒为 false，Min/Max 仅在删除值等于当前极值时为 true，DistinctCount 恒为 true。

## 2. 阶段与状态

- 视图按 `group -> 成员ID -> 值` 保存成员，按组保存五个聚合器状态。
- Apply：校验变更并修改成员表与可增量聚合；版本必须严格增大，重复版本幂等。
- Recompute：当删除策略要求成员时，仅扫描该组当前成员重建该组全部聚合器；用计数器记录次数与访问成员数，绝不扫描其他组。
- Commit：成员与聚合已经一致后推进已应用版本；删除到空组时删除整组，查询返回 ErrNotFound。
- 更新等价于从旧组删除旧记录，再向新组插入新记录；组变化时两组都维护。

## 3. 日志与恢复

- 文件头为魔数、格式版本、记录宽度和保留字节，长度固定且自描述。
- 每条记录采用 `4 字节大端长度 + payload + 4 字节 CRC32(IEEE)`。
- 重放按可读字节分类：头不足、长度前缀不足、记录体不足、CRC 不匹配；只有通过 CRC 的完整记录生效。
- 日志先追加并 fsync，再更新内存。崩溃恢复重放完整前缀，因此 Apply、Recompute、Commit 前崩溃都得到同一确定性结果。

## 4. 版本与拒绝

- 版本是单调非减序号；`v < applied` 返回 ErrVersionRegression，`v == applied` 直接视为已应用且不修改任何字段。
- 缺失分组键、NaN 值在进入状态前拒绝并计数；乱序版本同样拒绝并计数。

## 5. 并发与审计

- 同一把互斥锁保护日志追加、成员表、聚合器和版本；提交过程外不暴露可变内部状态。
- `Groups()` 返回深拷贝快照，读者只能看到一次提交后的一致状态。
- audit 独立维护成员集并全量重算，再按组、按聚合器比较；浮点使用 `math.Float64bits` 做位级比较。
