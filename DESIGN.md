# RLE 编解码器设计

不变量：严格解码器接受的每个 t 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 拒绝清单逐条推导（第三节第 2 条）

- 显式次数 1：若接受 `1a`，Decode 得 `a`，而 `Encode("a") = "a" != "1a"`。次数 1 必须省略。
- 次数 0：若接受 `0a`，只能解码为空串，`Encode("") = "" != "0a"`；且 0 次游程无对应编码形式。
- 前导零：若接受 `01a`，Decode 得 `a`，`Encode("a") = "a" != "01a"`；`007a` 同理（应为 `7a`）。
- 相邻同符号：若接受 `2a3a`，Decode 得 `aaaaa`，`Encode("aaaaa") = "5a" != "2a3a"`。`aa` 同理（应为 `2a`）。
- `\` 后非数字非反斜杠：若接受 `\a` 表示 `a`，`Encode("a") = "a" != "\a"`；转义仅用于数字与反斜杠。
- 末尾孤立 `\`：若接受 `a\` 并忽略 `\`，`Encode("a") = "a" != "a\"`；`\` 必须引出一个符号。
- 末尾只有次数：若接受 `a3` 并忽略 `3`，`Encode("a") = "a" != "a3"`；次数必须跟随符号。
- 非法 UTF-8：Decode 输出须为合法 UTF-8 字符串，Encode 按码点工作，非法字节序列无法往返。

每类各配一个哨兵错误（`ErrCountOne` 等），包在带字节偏移的 `*rle.Error` 中，彼此可用 `errors.Is` 区分。

## 大次数取舍（第三节第 3 条）

- 拒绝超过 `uint64` 的次数：解析时逐位 `AddDigit` 溢出即报 `ErrCountOverflow`，不静默回绕。
- 理由：Encode 产生的次数 ≤ 输入串长，合法往返所需次数必在 `uint64` 内；更大次数无实际用途，引入 big.Int 不值得。
- 解码输出总字节上限可配置（`NewDecoder(maxOut)`，默认 1 GiB）：写入前先做除法判定，超限报 `ErrOutputTooLarge`，绝不按次数预分配内存，故 `99999999999999999999a` 之类不会撑爆内存。

## 测试对照表

（待补）
