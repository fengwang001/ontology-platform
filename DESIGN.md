# DESIGN

## 1. 空行与单列空值（推导）
设空行为「跳过」。则单列空记录唯一存在方式是 `""`（带引号空字段）；若回写时去掉引号写成空行，会被跳过而丢记录。
故选定：**空行跳过；记录只在「本记录已激活」（收到过任意字节）时提交**，惰性开字段。单列表空值记录必为带引号空字段，
回写必须保留 `""`，否则丢数据；多列空字段（`a,,b`）保持不加引号。最后换行靠「惰性 + Close 丢弃零宽活动记录」不产生额外记录。

## 2. 切点推导（par）
段 worker 无法自证引号状态，故每段跑两个假设：O=起点在引号外（起点状态由 seam 前一字节决定：`\r`→crPending，否则 fieldStart），
I=起点在引号内 quoted。从左到右：上段选中末态为 quoted→取下段 I；否则取 O。I 段首字段与上段开放字段拼接（值相加、Start 取上段）。
上段末态为 quotePending（仅当 seam 前字节是 `"`）：顺扫连续 `"` 前缀到首个非 `"` 字节 b：b 为 `,`/`\n`/`\r` 则闭合后等价于
「O 假设从 b+1 起的事件」，直接复用 O 结果，不重扫；b 为其他字节则 pending 链立即出错，取 I 链。故每字节至多两遍。
事件携带相对段内偏移，加段基址即全局偏移；记录号/字段号只在拼接后的组装阶段计数，天然为全局坐标。

## 3. CR 待定（推导）
未引号态见 `\r` 入 crPending：下字节 `\n`→CRLF 行尾（CR 不进值）；其他字节→ErrStrayCR（偏移指向该 `\r`）；Close 仍待定→同样错误。
半包：crPending 是持久状态，跨 Feed 不丢。par：O 假设以 seam 前一字节 `\r` 决定是否以 crPending 开局（引号内 `\r` 是内容，走 I 链不受影响）。
引号内 `\r\n` 原样进值，不规范化。

## 4. 上限「立刻」（推导）
字段计数：未引号按到达字节数；引号内每个内容字节计 1，`""` 在第二引号到达时计 1（故超长在该字节即拒绝，不等闭合）。
字段数在字段开始的那个字节（逗号/引号/首字节）检查；记录数在记录激活的首字节检查。超限即错、进终态，后续 Feed 返回同一错误；
已提交记录保留。错误为具名类型，含 Kind、Offset、Record、Field。

## 5. 状态转移表
状态：FS=字段开始，B=未引号中，Q=引号中，QP=刚见引号，CR=行尾CR待定。类别：c=普通字节，`"`，`,`，`\n`，`\r`。

| 状态 | 普通 c | `"` | `,` | `\n` | `\r` |
|---|---|---|---|---|---|
| FS | →B 记起点 | →Q 记起点 | 发空字段→FS | 发空字段,行结束→FS | →CR |
| B | 累积 | **ErrBareQuote** | 发字段→FS | 发字段,行结束→FS | →CR |
| Q | 累积(含,\r,\n) | →QP | 累积 | 累积 | 累积 |
| QP | **ErrQuoteClosed** | →Q 累积`"` | 发引号字段→FS | 发字段,行结束→FS | →CR |
| CR | **ErrStrayCR** | **ErrStrayCR** | **ErrStrayCR** | CRLF 行结束→FS | **ErrStrayCR** |

Close：Q→ErrUnclosedQuote（偏移指向开引号）；CR→ErrStrayCR；B/QP/已激活FS→发末字段并提交末记录；空 FS→无记录。

## 6. 规范输入与往返
规范输入：每条记录均以 `\n` 结束（含末条），无 `\r`；引号仅在必要时出现（值含 `,` `"` `\n`，或单列带引号空值）；
`"` 转义为 `""`。Write 对任意表最小引号回写（含逗号/引号/CR/LF 或保留引号标记者才加引号），满足两种往返。
计数：lexer 内非导出 `n` 每处理一字节 +1；流式恒等于输入字节数；par 为各段两遍之和，≤2N+O(K)。
