# DESIGN — 流式行尾/空白规范化与双向偏移映射

策略记为 Preserve（保留原样）、EnsureOne（恰好一个末尾换行）、TrimBlank（删末尾空行但保留一个换行；全空则无）。
`N0(x)` 表示只做行尾与行尾空白规范化、尚未应用末尾策略。

## 1. 空输入与只含换行的输入

末尾策略只看“已输出的尾部”：Preserve 不动；EnsureOne 在输出为空时发一个合成 `\n`，否则保证最后一个字节是 `\n`（已有则不补）；TrimBlank 先删去尾部全部 `\n`，再补回一个（删完为空则不补）。
EnsureOne 对 `""` 必须给 `"\n"`：若给 `""`，再规范化一次仍可幂等，但它与“非空无尾换行要补 `\n`”产生不连续规则，且空文件无法区分“零行”与“一行空行”；选择 `"\n"` 后 `N("\n")="\n"`，幂等成立，规则统一为“输出恰以一个 `\n` 结尾”。三种策略均幂等：Preserve 尾部处理是恒等式；EnsureOne 输出必以 `\n` 结尾，再次执行不变；TrimBlank 输出形如 `内容\n`（或空），尾部至多一个 `\n`，再次执行不变。

| 输入 | Preserve | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

不变量：`ToOut` 单调不减；对每个输出位置 o，`ToOut(ToOrig(o))==o`。保留段给出恒等平移；删除段 `[a,b)` 不对应输出，其内部所有 i 只能取同一值 k，而两侧锚点迫使 k 等于“紧随删除段的输出字节”的偏移：`\r\n` 中的 `\r` 映射到输出 `\n` 的偏移；行尾空白映射到同一行尾 `\n` 的偏移；流尾被删空白之后无输出字节，k=输出长度 len。
因此编辑器把光标放在被删空白中时，映射到行尾换行符**之前**（该 `\n` 的起点，光标在 `\n` 左侧），即“回跳到行末”。`ToOrig` 对合成 `\n`（空输入下 EnsureOne）映射到原文终点 len，对普通输出 `\n` 映射到其原文偏移，互逆等式成立。

## 3. 空白缓冲超限

延迟空白若“强制判定为非行尾并输出”，输出会随切分/截断方式变化（不同切点缓冲剩余不同），违反第 4 条；且强制吐出的空白下次规范化时可能被当行尾空白删除，违反第 5 条。故选择**拒绝**：返回哨兵 `ErrWhitespaceLimit`（带触发时原文偏移），已输出内容保留，进入终态（再写返回 `ErrClosed`）。该结果与切点无关：界限只取决于输入字节序列本身。输出上限同理返回 `ErrOutputLimit`。

## 4. par 的拼接修正

按字节偏移切成 K 段。段间只有两种跨段依赖：前段末尾待定的 `\r`（下一段首字节若为 `\n` 则合并为一个行尾）与前段尾部未判定的空白（下一段若以行尾接续，则这些空白属行尾空白）。
做法：段 k 以小初始状态 `(prevCR, pendingOrigStart)` 独立流式处理；该状态由拼接器沿边界串行扫描得到（前段尾部向后看待定 CR 与连续空白，本段起点向前看连续 `[ \t]`）。修正范围有上界：1 个待定 CR + 一段连续空白（其长度受缓冲上限约束）。各段映射在段内坐标生成，拼接时把 orig/out 起点整体平移为全局偏移（`span.Concat`）；跨段被判为行尾空白的空白在拥有段内删除，平移后即全局正确。末段 Close 后由 par 统一施加末尾策略（改写尾部 span），单线程与并行共用同一尾部逻辑，结果逐字节相同。
