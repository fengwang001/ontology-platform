# 设计推导

## 流格式记录布局（wire）
| 记录 | 标记字节 | 载荷 | 说明 |
|---|---|---|---|
| 流头 | `H`=0x48 | ver(uvar)=1,winCap(uvar) | 必须最先出现 |
| 字面量段 | `L`=0x4C | n(uvar),后接 n 个原文字节 | n≥1 |
| 回指 | `R`=0x52 | dist(uvar,≥1),len(uvar,≥3) | dist 可小于 len（重叠） |
| 刷新 | `F`=0x46 | 无 | 此前全部输入可被还原 |
| 流尾 | `T`=0x54 | total(uvar),checksum(uvar) | FNV-1a 64；流尾后不得有字节 |

整数用 LEB128 无符号变长；读到第 10 字节仍续位或第 10 字节超 1 位即“超长/溢出”。

## 推导 1：与 Write 切法无关
压缩器把未提交字节留在内部缓冲，逐位置贪心：在 pos 调用匹配器；只有当匹配长度 `len < 剩余字节数` 时它的终点已被确认（终点之后那个字节已在手中），才提交该回指并前进 len；否则仅提交 pos 处 1 个字面量。若匹配恰好吃满缓冲，其“能否再延长一个字节”未知，先不提交，等待下次 Write。因此任何切法下，同一累计字节序列产生完全相同的决策序列。`Flush` 必须强制把缓冲排空：剩余不足延长验证的部分只可能是回指吃满尾部的情形，排空前把该回指按当前长度接受即可——即 Flush 强制结束当前匹配，然后发 `F`。连续两次 Flush 间无新输入，缓冲为空且已有 `F`，故第二次输出 0 字节。

## 推导 2：重叠回指的复制
块复制 `copy(dst,src)` 在 Go 中保证 src 不被中途改写，距离<长度时会重复原 src 的旧字节，得到错误结果（如 dist=1 应无限重复同一字节）。正确语义是“此刻输出序列”向前取字节：循环 `out[i]=out[i-dist]`，i 递增，新写的字节可被后续步读取。每次迭代都是 1 字节、成本在输出总字节数内（解压本来就是 O(n)），故直接逐字节循环，无额外复杂度代价。

## 推导 3：两种距离错误
“超过窗口容量”是格式与配置层面的硬不变量：压缩器只在容量 winCap 的窗口里搜索，且 Find 要求 dist≤窗口内历史长度≤winCap，因此合法压缩流绝不可能产生它；它应先于其他检查判出。“超过已输出字节数”是状态层面的错误：回指指向了还不存在的输出。两者不同——预置字典场景下窗口里有从未作为流输出的字典字节。解压器按本块窗口判定，维护窗口内有效字节计数（预置字典预填），故并行流中 dist 可指向字典而不违反“不超过已有数据”；对纯流解压该计数就是实际输出字节数，dist 超过它即越界。判定顺序：先 winCap（ErrDistWindow），再有效计数（ErrDistOutput），错误携带字节偏移。

## 推导 4：并行块的确定性
块 i 的压缩输入只有两样：本块原文 data[i*b:(i+1)*b]，以及上一块末尾至多 winCap 字节作为预置字典（预填窗口与哈希链，但不产出任何流记录）。压缩是纯函数：决策只依赖这两段字节，与 goroutine 调度、完成顺序、其他块无关。哈希表、链均为各 worker 私有。各块产出的记录（不含流头/流尾/刷新）按块序号顺序拼接：字典预置使块边界无需任何刷新标记，回指天然跨越边界，解压端逐字节重建窗口即等价重建了预置字典。流头由主线程写一次，块记录按序拼接，最后统一写流尾（total 与 FNV 校验为全文字节），故 workers=1/2/4/8 输出逐字节相同；块大小固定，worker 数不改变切块。

## 其他要点
- 哈希链：16384 槽头表 + 每位置一项 prev 链；每插入一个三元组最多令一次查询候选数+1，故每位置考察≤链长上限，总量≤上限×n，与窗口大小无关。
- 输出上限：回指先做 `total+len` 溢出与上限判定，再写任何字节；字面量段先按 n 判定再读字节。拒绝后进入终态，错误粘滞。
