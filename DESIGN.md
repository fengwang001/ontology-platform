# 快照读可见性判定

## 一、核心推导：只看提交时刻是不够的

朴素规则：写入事务 T 的提交号 commitTS(T) < 快照水位 watermark，即判可见。

反例构造：
1. T1 启动较早，活跃事务集 active = {T1}。
2. T1 最终提交，分配到的提交号为 5（提交号在提交时才分配）。
3. 快照 S 在 T1 提交之后创建：watermark = 10（水位是“快照创建时已提交事务的上界 + 1”之类的连续推进值，不按活跃集裁剪），但 S 记录的活跃集仍含 T1，即 S.active = {T1}。
4. 朴素规则：5 < 10 ⇒ 判可见；可在 S 创建那一刻 T1 对 S 而言仍在进行中，读者于是看到了一个“取快照时尚未提交”的版本。

正确规则（左闭右开 + 活跃集排除 + 自身例外）：

- T 是快照自身事务：自己写的自己可见（即使 T 在活跃集中）。
- 否则可见当且仅当：T 已提交 **且** commitTS(T) < watermark **且** T ∉ active。

含义：水位只给出“提交号下界裁剪”，活跃集才给出“快照时刻是否已提交”的精确信息，两者必须同时满足。

## 二、复杂度

单次判定只做：提交状态 1 次查找（事务表）+ 活跃集 membership 1 次哈希查找，共 ≤ 2 次，O(1)，不遍历事务表。

## 三、错误

- ErrUnknownTxn：事务号未注册；ErrReleased：快照已释放；ErrCorruptChain：版本链提交号非递增。
- ErrActiveLimit：活跃集超过上限。

## 四、边界

水位 0、活跃集为空、活跃集含自身事务、提交号等于水位（不可见）、已回滚（永不可见）。
