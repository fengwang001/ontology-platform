# RLE 设计推导

不变量：严格解码接受的每个 t 都满足 `Encode(Decode(t)) == t`。Encode 的输出必然是
「无前置零、省略次数 1、相邻游程符号互不相同、数字与反斜杠一律转义、合法 UTF-8」，
故不满足其中任一条的 t 若被接受，重编码后必不等于 t，必须拒绝（错误彼此可区分，带字节偏移）。

## 拒绝清单（每条给出接受即违反不变量的 t）

1. 次数显式为 1，如 `1a`：解码得 `a`，重编码为 `a` ≠ `1a`。
2. 次数为 0，如 `0a`：解码得空串，重编码为空 ≠ `0a`；且 0 次符号本不该出现。
3. 次数带前导零，如 `03a`：解码得 `aaa`，重编码为 `3a` ≠ `03a`。
4. 相邻游程符号相同，如 `2a3a`（含省略次数的 `aa`）：解码得 `aaaaa`/`aa`，
   最长游程要求重编码合并为 `5a`/`2a` ≠ 原串。偏移取后一游程起点。
5. `\` 后不是数字也不是 `\`，如 `\x`：转义只定义于数字与 `\`；无法唯一解码，
   且非数字非转义字符不可能出现在 Encode 输出中。
6. 末尾孤立 `\`：游程缺符号，且该字节不是任何合法编码的结尾。
7. 末尾只有次数没有符号，如 `12`：一个游程必须以符号收尾。
8. 非法 UTF-8（非法引导字节、残缺多字节序列、surrogate 等）：Encode 只产生合法
   UTF-8；拒绝以保证输出与错误逐字节确定，不依赖 `utf8.DecodeRune` 的宽松替换。

## 大次数取舍（第 3 条）

选择**限制次数上限为 `math.MaxInt`**。理由：次数是游程重复出现的次数，解码输出
长度必然 ≥ 次数；Go 中任何字节串都不可能达到 `MaxInt+1`（地址空间与 `len` 均为
`int`），故超过该值的次数在本平台没有可表示的结果，无法“真正支持”。解析用 uint64
逐位累加（`runs.AddCountDigit`），在乘 10 前做 `n > (MaxInt-d)/10` 判定，溢出与超
上限一律返回 `runs.ErrCountOverflow`，绝不静默回绕。解码器在真正写出游程前才检查
输出上限（`MaxOutputBytes`，0 表示不限制，默认 64 MiB），按不超过 4 KiB 的分块重复
写入，从不按次数预分配；因此 `99999999999999999999a` 在常数内存下被拒，大但合法
的次数也不依赖一次性分配。

## 流式一致性（第 5 条）

解码是纯字节状态机（起始态 / 次数态 / 转义态 / 多字节收集中），不保留“已消费即可
丢弃”之外的任何输入；`Write` 可缓冲任意末尾片段（多字节中间、`\` 与字符之间、数字
中间）。相邻同符号只在后一游程符号确定时判定，结果与错误完全由字节序列决定，与
切分方式无关。非导出计数器 `examined` 对每个进入状态机的输入字节恰好加 1。

## 语义—测试对照表

<!-- TABLE -->
