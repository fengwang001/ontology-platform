# 设计推导

## 流格式（每条记录首字节为定长 tag，其后为 uvarint 字段）

| 记录 | tag | 布局 |
| --- | --- | --- |
| 流头 | — | `O1Z7` + version(1) + windowCap(var) + maxChain(var) |
| 字面量 | 0 | tag + len(var) + len 个原始字节 |
| 回指 | 1 | tag + dist(var) + length(var)（dist>=1，length>=minMatch=3） |
| 刷新 | 2 | tag（单字节） |
| 流尾 | 3 | tag + totalLen(var) + CRC32-IEEE(var) |

uvarint：LEB128；超 10 字节或第 10 字节使 64 位溢出即非法。CRC32 用标准库 `hash/crc32` IEEE 表（非压缩库）。

## 1. Write 切法无关（语义 3、4）

编码器从当前未匹配位置 `p` 起只在「已知字节」上做匹配判定。若 `p` 处匹配长度顶到当前缓存末尾，它可能被未来 Write 的字节延长——此时不输出任何记录，保留 p 不动，等下次 Write 补齐后重新判定。让 p 前进的只有：(a) 匹配在某已知字节处终结（输出回指）；(b) p 处确认无法匹配（1 字节并入待定字面量）；(c) Flush 强制把未决区按当前确定的匹配/字面量落盘。

「顶到末尾就悬置」对任何切法产生同一条决策序列（决策只依赖累计字节流与偏移），输出逐字节相同。Flush 强制终结当前匹配并写 tag 2，故解压端可还原恰好到该偏移；窗口不清空，之后仍可回指 Flush 前。两次 Flush 间无新输入时未决区为空、无新记录，第二次不写字节（dirty 位判定）。

## 2. 重叠回指复制（语义 2）

`copy` 在 src/dst 重叠且 dist<length 时从低地址起拷，恰能重复展开；但为不依赖该细节、并能逐字节做上限检查，解压器用逐字节循环从环形窗口取 `At(emitted-dist+off)`。总复制量 O(输出长度)，这些字节本就要写入输出缓冲，逐字节循环不增加渐近成本，全同字节 4MB 实测远小于预算。

## 3. 两种"距离越界"（语义 7、9）

「距离超过已输出字节数」引用从未存在的偏移，是流内容损坏（截断/伪造）；「距离超过窗口容量」违反流头声明的配置（合法压缩器永不产生，match 只在 cap 环形窗口内搜索）。成因不同，故用两个哨兵错误。

并行块压缩把「上一块原文末尾 cap 字节」预置进本块窗口与哈希链：这些字节在本块首字节输出前已在逻辑流更早偏移输出过（上一块是同一连续原文前缀），解压端无需预置，其「已输出字节」天然包含它们，规则不变。

## 4. 并行确定性（语义 9）

块 i 的压缩只读本块原文与上一块末尾 cap 字节（预置字典），不读其他块输出；匹配结果由这两段字节与固定参数唯一决定。各 goroutine 输出按块号收集后顺序拼接（块间不插刷新标记：刷新服务于"未来字节未知"的流式场景，此处未来已知，拼接处允许跨块匹配）。流头只写一次，totalLen 为各块长度之和，CRC32 按原文顺序链式更新。worker 数只改变调度，输出逐字节相同。

## 错误与上限

`wire.CorruptError{Kind, Off}` 带压缩流绝对字节偏移；Kind 为哨兵：ErrMagic/ErrVarint/ErrZeroDistance/ErrDistanceOutput/ErrDistanceWindow/ErrLengthMismatch/ErrChecksum/ErrTrailing/ErrTruncated。输出上限在回指 length 解码后、写任何字节前检查（ErrOutputLimit），命中即终态，后续 Write 返回同一错误。
