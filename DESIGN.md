# 组提交与顺序保证器 设计

## 1. 目标与包划分
- `req`：写请求 `Request{Payload}` 与结果 `Result{Seq, Err}`；序号 `Seq` 为全局单调序号，0 表示无效。
- `batcher`：按「条数上限 / 字节上限 / 等待时长」三条件先到者成批；时钟可注入。
- `wal`：批次编码落盘与逐条读回，批级 CRC32；只追加写并 `Sync`。
- `commit`：单一 leader 协程攒批、分配序号、落盘；调用方在各自 channel 上等待。
- `recover`：扫描日志，恢复到最后一个完整批次，返回已提交请求与下一序号。

## 2. 序号分配时机（核心推导）
方案对比：
1. 到达即分配：落盘失败会留下永久空洞，破坏「序号区间连续」。
2. 落盘成功后才分配：调用方无法在等待期间得知自己的相对次序，且分配与可见性分离易产生竞态。
3. 本题规则：**批次在 batcher 封口后、调用 Write 之前由 leader 一次性分配连续序号**。
   - 成功：序号随结果返回，天然连续无重叠。
   - 失败（写或 Sync 错误）：**整批序号作废，计数器回滚到分配前的值**，下一批从旧值继续；本批全部收到错误。
因此「第 2 批失败后，第 3 批序号紧接第 1 批」。

## 3. 批次原子性：批级 CRC
磁盘布局（全部大端）：
- 文件头 8 字节魔数 `ONTOWAL1`。
- 每批为一帧：帧头 20 字节 = `payloadLen(4) | firstSeq(8) | crc32(4)`，其后 payload；
  payload = 每条 `len(4)+data` 拼接，`crc32 = IEEE(firstSeq 的 8 字节 ‖ payload)`。
帧头里 CRC 位于条目之前，使截断分类无歧义。**CRC 覆盖整批而非单条**：
批内任一字节损坏都使整批 CRC 不符，回放时整批丢弃，绝不只丢坏条目。
截断点分类（n = 当前文件长度）：
- `n < 8`：头部不完整 `ErrHeaderIncomplete`。
- `8 <= n < 28`：批头不完整 `ErrBatchHeaderIncomplete`。
- `n >= 28` 但读完帧头后剩余 < payloadLen：条目不完整 `ErrEntryIncomplete`。
- 帧完整但 CRC 不符：`ErrCRCMismatch`。
回放遇到任一错误即在该帧停止（恢复到最后一个完整批次）。

## 4. leader 选择与唤醒不串台
`commit.New` 启动唯一长期 leader goroutine；其余调用方从不接触磁盘。
每个请求带专属 `chan req.Result`（容量 1）。leader 把一个批的等待者按批内顺序保存，
落盘后按相同顺序把各自的 `Result{Seq: firstSeq+i}` 发回各自 channel。
channel 一一对应且异步发送不阻塞，故唤醒后拿到的必是自己那条，不会串台。

## 5. 资源与复杂度
- 一次成批只产生一次写与一次 Sync：10000 请求 / 每批 ≤100 ⇒ 同步 ≤100（断言放宽到 ≤200）。
- 成批三条件：`len==maxCount`、`字节>=maxBytes`（仅在多于 1 条时触发）、定时器到点。
- **超大单条**：单条载荷 `len > maxBytes` 时不拒绝；它作为单条批立即放行（该批可超字节上限），
  以免一条巨消息永久卡住流水线；常规多批仍严格不越界（计数器记录历史最大批字节）。

## 6. 故障与关闭语义
- 写一半崩溃 / 截断：对应不完整帧被丢弃，下一序号 = 最后完整批 firstSeq + count。
- Sync 失败：整批失败、序号回滚。
- Close 与请求竞争：leader 退出排空循环；竞态中的请求要么进入最后一批落盘成功，
  要么收到 ErrClosed。不存在「成功但没落盘」。
- 零请求 Close：仍写出 8 字节文件头（定义为「只有头的文件」，不含任何批帧），回放得 0 条、下一序号 0。
- 边界：maxCount=1 逐条提交；maxWait=0 每请求立即成批；空载荷合法。

## 7. demo 判定项（共 8 条，逐行 OK/FAIL）
1) 第 2 批失败后第 3 批序号紧接第 1 批；
2) 坏批整批不可见、前后批正常；
3) 200 协程序号唯一且与载荷一一对应；
4) 10000 请求同步次数 ≤ 200；
5) 条数/字节/超时三种触发各一例；
6) 头部/批头/条目/CRC 四类截断各一例；
7) 回放序号连续无空洞；
8) Close 后所有成功请求都在日志里。
