# 设计推导

## 1. 零计数行号

统一格式用 1 基、闭区间描述 hunk；空区间没有“其中第一行”，所以起始号表示插入点之前的旧行号。若 `b=0`，插入点在旧第 `a` 行之后；`a=0` 表示文件开头。解析时，新文件一侧同理：`d=0` 时插入点在新第 `c` 行之后，但生成补丁只把插入挂到旧坐标。

| 旧侧形式 | 含义 | Apply 的插入点 |
|---|---|---|
| `-a,0` | 不删除旧行，a 是前一旧行行号 | 旧索引 `a` 之后；`a=0` 即开头 |
| `-a` | 等同于 `-a,1`，删除旧第 a 行 | 先匹配第 a 行开始的上下文/删除段 |
| `-a,b`, b≥2 | 删除旧第 a 至 a+b-1 行 | 在匹配段位置替换 |

样例：空文件插入得到 `-0,0`；`x` 前插入 `y` 得到 `-0,0 +1`；在旧第 2 行后插入得到 `-2,0 +3`；删除唯一第 1 行得到 `-1 +0,0`。

## 2. hunk 合并阈值

上下文为 C 时，前一 hunk 尾部保留 C 行，后一 hunk 头部保留 C 行。若间隔未改动行数为 g：

- `g <= 2C`：两段上下文重叠或相邻，必须合并；g=2C 时恰好用完整 2C 行连接。
- `g = 2C+1`：中间至少 1 行不属于任何 hunk，GNU `diff -U C` 分开。

所以阈值是 `2C`，不是 C（会过度合并 g=2C 的 GNU 单 hunk），也不是 2C+1（会错误合并 g=2C+1 的两个 GNU hunk）。C=0 时阈值为 0，任意未改动间隔都分开。

## 3. 最短脚本并列选择

实现采用 Myers 对角线路径。每个 D、k 的转移固定为：能向下（删除，k-1→k）时优先删除；只有必须向右（插入，k+1→k）时才插入。蛇形前进只消费相等行，不改变 D。因此 `a b -> b a` 的 hunk 内顺序固定为 `-a`、` b`、`+a`；重复生成逐字节相同。

## 4. 偏移与后续 hunk

第 i 个 hunk 的记录起点是 `oldStart`，在 `[origin-F, origin+F]` 内找精确匹配；距离相同选靠前候选。若实际匹配点相对记录起点偏移 o，替换还会让后续文本整体平移 `ins-del` 行。因此下一 hunk 的查找起点改为其原始记录起点加累计量 `Σ(o+ins-del)`。这保持“同一补丁在当前已变形文本中的实际位置”，而不是重复使用陈旧坐标。
