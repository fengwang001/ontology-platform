# 设计推导

状态（S0..S4）：FStart=字段开始；FBare=未引号字段；FQuote=引号字段；FQuoteQ=引号内刚见引号；FCR=未引号字段后 CR 待定。字节类别：`,` `"` `\r` `\n` 其他。

## 1 空行与单列空值
空行不产生记录：记录必须“含字段或已闭合引号”，裸 `\n` 仅结束“无字段”状态并被跳过，因此连续两个换行是空行而非空记录。
否则单列表 `a\n\n` 无法区分“空行”与“值为空的记录”，且 `\n` 后的 EOF 又无第三个换行可消歧。
单列唯一字段为空且需要成记录时，回写器必须输出 `""`：未引号空串无法与空行/EOF 区分（`""` 使记录“含已闭合引号”而成为记录）。

## 2 par 切点
每段在两种入口假设下各解析一次：A=FStart（引号外）、B=FQuote 且重放切点前 1 字节（引号内）。
仅 1 字节重放即可覆盖全部入口：入口 FQuote 用 B；入口 FQuoteQ 用 B（重放的 `"` 立刻闭合，等价）；入口 FCR 用 A 并预置 CR 待定；入口 FStart/FBare 用 A。
按段从左到右，前段末态决定本段取哪次结果；首段入口 FStart。事件为（开字段起点、关字段、关记录），入口在记录中间时首个关字段并入前段携带记录。
段内坐标加段基址即全局偏移；记录号、字段号按拼接顺序累加。引号内切点（含 `""` 中、`\r\n` 中）与 CR 待定由此无状态依赖，每字节至多两遍，切点开销 O(K)。

## 3 CR 待定
FBare 见 `\r` 入 FCR 且不产出；见 `\n` 为 CRLF 行尾，见其他字节（含 EOF）为孤立 CR 错误，错误偏移指向该 `\r`。
半包续传时 FCR 跨 Feed 保持；切点在 `\r\n` 之间：下一段入口 FCR，首字节 `\n` 关记录，否则按孤立 CR 报错。引号内 CR 是内容，无待定态。

## 4 上限“立刻”
字段字节数在字节进入字段内容时计数：FBare 每接收 1 字节计 1；FQuote 内每字节计 1，`""` 在第二字节才成为 1 个引号字符——按字节计数在第一超限字节即拒绝，与字符计数同样立刻且不回扫。
字段数在每次字段关闭时检查 `>max`，记录数在记录关闭时检查；超限即终态，后续 Feed 返回同一错误。

## 状态转移表

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| FStart | 关空字段；S0 | ErrBareQuote | S4 | 无字段：跳过；有：关记录；S0 | S1，内容1 |
| FBare | 关字段；S0 | ErrBareQuote | S4 | 关记录；S0 | S1，内容+1 |
| FQuote | S2，内容+1 | S3 | S2，内容+1 | S2，内容+1 | S2，内容+1 |
| FQuoteQ | 关字段；S0 | S2，内容+1（转义"） | ErrAfterQuote | ErrAfterQuote | ErrAfterQuote |
| FCR | （关字段后）关记录；S0 | ErrLoneCR | ErrLoneCR | 关记录；S0 | ErrLoneCR |

FCR 行逗号：先按 CR 前字段关闭再关记录。Close：FQuote/FQuoteQ→ErrUnclosedQuote；FCR→ErrLoneCR；有活动字段则关闭成记录；末尾换行不产生额外记录。
