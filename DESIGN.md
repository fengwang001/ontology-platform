# 设计推导：空白续行（语义 3）与 Store 转义（语义 6）

## 3. 续行行只含空白 / 为空

对齐 `java.util.Properties` 的 `LineReader.readLine()` 状态机：行尾奇数个 `\`
转义掉行终止符后，先「跳过下一物理行首部空白」，该循环遇到非空白字节或行终止符即停。
若下一物理行只有空白（或空行），跳过循环一路吃到行终止符，逻辑行就在此终止符结束。

结论：该续行行不贡献任何字符；不会级联到第三行（只含空白的行不可能以非空白的 `\`
结尾，跳过循环也不越过终止符）。即 `k=a\`+空行+`z` ⇒ `k→a`，`z` 是新物理行。
它与「首部空白剥离」是同一规则的两面：剥离范围是续行行首到首个非空白字符之间；
不存在非空白字符时整行空白（含终止符前全部空白）被吃掉，续行段为空，逻辑行结束。

## 6. Store 转义规则

原则：逐字符对照 Load 词法，只转义「会让 Load 读出不同结果」的字符。

键：行首空格 / Tab / `\f`（被首部剥离）与键中间空白（未转义即终止键）都要转义；
首字符 `#`/`!` 要转义（整行首字符是二者则变注释，值从不位于行首故值中的 `#!` 不转义）；
`=` `:` 是分隔符要转义；`\` 写 `\\`；`\n` `\r` 写 `\n` `\r`（否则产生新物理行）。

值：回写形如 `key=value`，值首空白若不转义会被当成分隔符右侧前导空白吞掉，必须转义；
值中间的空格既不是分隔符也不被剥离，故不转义；`\` 与 `\n` `\r` 必须转义；其余——
含中间与尾部空白、`=`、`:`、`#`、`!`、非 ASCII——原样以 UTF-8 输出（不写 `\uXXXX`）。
空键写 `=value`，空值写 `key`（无分隔符），Load 均可还原，往返成立且顺序保留。

## 语义—测试对照

<!-- TABLE -->
