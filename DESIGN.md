# LZ77 流式压缩设计

## 记录字节布局（整数均为无符号 LEB128；tag 为 1 字节）

| 记录 | tag | 后续字段 |
|---|---|---|
| 流头 | 固定魔数 `"LZL1"`(4B) + 版本 `1`(1B) | 窗口容量、链长上限（各 1 个 varint） |
| 字面量段 | `0` | 长度 n(varint)，随后 n 个原文字节 |
| 回指 | `1` | 距离 d(varint，≥1)、长度 l(varint，≥3) |
| 刷新标记 | `2` | 无；此前记录恰好覆盖到一次 Flush 的输入偏移 |
| 流尾 | `3` | 原文总长(varint)、FNV-1a 64 位校验和(varint) |

## 推导一：压缩结果与 Write 切法无关（语义 3、4）

每次 Write 的数据追加到编码器内部输入缓冲后，从已确认位置做贪心匹配。
困难：在位置 i 找到向后匹配长度 m 时，m 的真值取决于 i+m 处字节是否仍相等，
而该字节可能尚未被任何 Write 送达。若此刻发出 (d,m)，下次 Write 可能送来
使匹配延长的字节，按 1 字节喂入的调用方会发出更长匹配——输出分叉。
规则：**只有匹配末端 i+m 之后至少还有 1 个已收到字节时，长度 m 才被确认**；
确认前这些字节作为未决区挂起，不产生输出。确认只依赖"已收到字节内容"这一事实，
与分几次 Write 到达无关，故输出逐字节相同。Flush 时没有未来字节可等，必须强制
确认：把未决区按当前匹配状态全部排空（够长则回指，否则字面量），再写刷新标记。
故 Flush 后解码器凭已有记录可还原全部已提交输入；Flush 不清窗口，后续匹配可跨越。
连续两次 Flush 间无新输入：未决区为空、记录边界已对齐，第二次不产生任何字节。

## 推导二：重叠回指的解压复制（语义 2）

回指语义是把距当前输出末尾 d 处开始的 l 个字节复制到末尾。d<l 时源、目标重叠：
正确序列是周期自复制，如 d=3、"abc" 产生 "abcabc…"。一次性块复制（memmove 或
先取源切片再追加）会读到旧数据，结果错误。实现维护定长环形缓冲，k=0..l-1 逐字节
从"当前末尾往前 d 字节"读取——该位置恒为已写字节（k 增大时可能正是本轮刚写出的
字节），写 1 字节、环推进 1 格。单循环每字节一次定长切片索引，编译器可做边界消除，
4MB 最坏情形毫秒级；结论：逐字节循环在此可接受，无需向量化。

## 推导三：两类"距离越界"为何可区分（语义 7）

解码回指前依次检查：d==0（编码非法）；d>窗口容量 W（即便历史足够长，按本格式
也不可能被编码——压缩器发距离前恒有 d≤min(W,已输入字节数)，候选只取自窗口内存活
的哈希链）；d>已输出字节数（历史尚短，属相对当前位置的截断/错乱）。三者为不同
哨兵错误并携带字节偏移。并行块以"上一块末尾 W 字节"为预置字典：解码本块记录时
这些字节已在窗口中，"已输出字节数"单调包含预置字典，含义不变；d>已输出 与
d>W 依旧是两类不同故障。

## 推导四：并行块压缩的确定性与衔接（语义 9）

按固定 blockSize 切分。块 k 是纯函数 f(本块原文, 上一块原文末尾至多 W 字节)：
匹配器只读这两者构造的窗口与哈希链，无全局状态、不读其他块结果；同一步贪心面对
同一候选集合与同一链长上限，故 f 无随机性、与调度和完成顺序无关，workers 取
1/2/4/8 输出逐字节一致。衔接：块 k(k≥1) 编码前把上一块末尾 W 字节装入窗口与
哈希链作为只读预置字典；解码器顺序读流，天然先重建这些字节，块边界无需刷新标记、
在字节流中不可见。全流一份流头（第一块前）与一份流尾（长度、校验按全文）。

## 其余要点

- varint 上限 10 字节且结果 ≤ math.MaxUint64，否则报溢出错误并带偏移。
- 解压炸弹：回指与字面量均在写任何字节前预判 已输出+l > 上限；超限即终态，
  后续 Write 重复返回同一错误。
- 截断与尾随：记录不完整即截断；流尾之后再有字节即尾随错误。
- 单实例非并发安全；多个独立实例可并发使用。
