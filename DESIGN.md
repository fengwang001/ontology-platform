# 设计推导

## 非法单元
统一从首字节向前解析，逐字节做“首字节、长度、第二字节区间、后续续字节”检查。
第二字节一落入下表非法区间，当前非法单元只含首字节；第二字节重新开始，
故 `F0 90 80 41` 中 `F0` 单独替换，`90 80 41` 各成新单元；`E0 80`、`ED A0` 同理。
第二字节合法而后续缺字节或遇到非续字节时，已收下的合法前缀整体是一个单元，
故流末 `E2 82` 只替换一次。`C0/C1/F5..FF` 和游离续字节永非法，各吞 1 字节。

| 首字节 | 合法第二字节 | 非法第二字节吞掉 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 1 |
| `E0` | `A0..BF` | 1 |
| `E1..EC` | `80..BF` | 1 |
| `ED` | `80..9F` | 1 |
| `EE..EF` | `80..BF` | 1 |
| `F0` | `90..BF` | 1 |
| `F1..F3` | `80..BF` | 1 |
| `F4` | `80..8F` | 1 |
| `80..BF` | 无（非首字节） | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

## 切分缓存
UTF-8 最长 4 字节；只在“首字节长度已声明、已收字节仍是该序列合法前缀”时缓存，
所以硬上限 3。第二字节非法或合法前缀后出现非续字节，立刻结束当前单元，
把非续字节作为新首字节；无需等流结束。只有 `Close` 时仍缺续字节才是截断。
UTF-16 单单元 2 字节、代理对再 2 字节；缓存硬上限 3（1 字节加完整高代理）。

## 并行切点
UTF-8 中只有 ASCII 首字节，或多字节首字节本身可作为同步点。切点若非同步点，
向前最多回看 3 字节，把同步点之后到切点的尾巴交给下一段；尾巴长度 1..3，
不会改变任何字节归属。`C3|<80 80>`：切点在 `C3` 后，边界回退到 `C3`，
上段输出该完整字符，下段从两个游离续字节开始，仍是两个非法单元。
`E0|<80...` 不回退，因为 `E0+80` 第二字节非法，两段各自产生一个非法单元，
与单线程一致。首段保留 BOM 处理；其他段从普通字节同步开始。

## 上限断点
编码下一个完整标量前先检查输出长度；空间不足则不写任何字节，返回
`ErrOutputLimit`。`Write` 的 `n` 只统计已形成完整输出标量的输入字节；
BOM 字节计入已消费；仍在缓存中的合法前缀未被判定，既不在 `n` 内，也不修改输出。
调用方用 `Pending()` 取回这些缓存字节，放在剩余输入前传给新实例；
因此边界停在标量，拼接与无上限一致。
