# 设计推导：带转义的 RLE

不变量：严格解码器接受的每个 `t` 都满足 `Encode(Decode(t)) == t`。据此逐条推导必须拒绝的输入（`t` 写成 Go 字符串字面量）。

| 情形 | 代表输入 | 若接受，`Decode(t)` → `Encode(...)` | 矛盾 |
|---|---|---|---|
| 显式次数 1 | `"1a"` | `"a"` → `"a"` | 次数 1 规定省略，`"a" != "1a"` |
| 次数 0 | `"0a"` | `""` → `""` | 编码器从不产生 0 次游程，`"" != "0a"` |
| 前导零 | `"01a"` | `"a"` → `"a"` | 规定无前导零，`"a" != "01a"` |
| 相邻同符号 | `"2a3a"` / `"aa"` | `"aaaaa"` / `"aa"` → `"5a"` / `"2a"` | 规定最长游程，输出被合并 |
| 错误转义 | `"\q"` | `"q"` → `"q"` | 编码器只转义数字与 `\`，`q` 不写反斜杠 |
| 末尾孤立 `\` | `"a\"` | —— | 编码器写出的 `\` 必带后续字符，无法再生成以 `\` 结尾的串 |
| 末尾只有次数 | `"a3"` | —— | 编码器输出必以符号结尾，无法再生成以数字结尾的串 |
| 非法 UTF-8 | `"a\xff"`、`"\xc3"` | —— | 不是码点序列；按替换字符编码后字节不一致，往返失败 |

上述 8 类各有独立哨兵错误（`ErrCountOne`、`ErrCountZero`、`ErrLeadingZero`、`ErrAdjacentSame`、`ErrBadEscape`、`ErrDanglingEscape`、`ErrMissingSymbol`、`ErrInvalidUTF8`），统一包在带字节偏移的 `*rle.Error` 中。

## 大次数取舍（第 3 条）

选择：**解析支持任意大次数，但发射受输出上限约束**。

- 次数用 `runs.Count`（内部 `math/big.Int`）按位累加，位数与输入长度成正比，永不溢出；`99999999999999999999a` 可被完整解析。
- 发射游程前只做判定 `count <= 剩余预算/符号字节宽`（整数除法，不发生乘法溢出）；不满足则返回 `ErrOutputLimit`，且不写任何字节，故不会按次数预分配内存。
- 上限可配置：`NewDecoder(w, maxOut)` / `DecodeLimit(t, maxOut)`；`Decode` 使用 `DefaultMaxOutput = 1<<30`。

## 语义与测试对照

（实现完成后补齐）
