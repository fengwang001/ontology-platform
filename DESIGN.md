# DESIGN — unified diff / patch

## 1. 零计数时 hunk 头行号（规则推导）

GNU 头中 `-a,b` 表示旧文件第 a 行起、共 b 行；统一格式行号从 1 开始，且 a 是
**该 hunk 覆盖范围第一行的行号**。空范围不含任何行，于是取“范围前一行的行号”：

| 情形 | 书写 | Apply 的插入点（旧文件行索引，0 基） |
|---|---|---|
| b=0 | `-a,0`，a=前文最后旧行行号（最前插入为 0） | 插在旧行 a 与 a+1 之间 |
| b=1 | `-a`（省略 `,1`） | 替换旧行 a |
| b≥2 | `-a,b` | 替换旧行 a..a+b-1 |

新侧 `+c,d` 同规则。样例：`x\n`→`y\nx\n` 在最前插入，旧范围空且前文无行，故 `-0,0`；
`a\nb\nc\n`→`a\nb\nX\nc\n` 插入点在旧行 2 之后、3 之前，故 `-2,0 +3`；删空 `a\n`
时旧侧唯一行被删 `-1`，新侧空、新文件 0 行，故 `+0,0`。a/c 是前一行行号而非“插入
位置”；Apply 把插入点解释为旧索引 a 与 a+1 的缝隙。

## 2. hunk 合并阈值

每个 hunk 两侧各带至多 C 行上下文。两改动相隔未改动行数 g 时，前 hunk 尾上下文最多
吃 C 行、后 hunk 头上下文最多吃 C 行：g ≤ 2C 时上下文覆盖相交或相邻，GNU 合并；
g=2C+1 时中间至少剩 1 行不属于任何 hunk，拆分。**阈值 = 2C**。C=0 时阈值 0：
1 行间隔即拆；取 C 或 2C+1 都会在 g=2C 或 C=0 边界与 `diff -U C` 不符。

## 3. 并列最短脚本选择

Myers 前向 V 迭代：对角 k 上向下（删除）来自 k+1，向右（插入）来自 k-1。二者给出
相同 x 时本实现选择 **删除优先**（`x >= V[k-1]` 取向下）。故 `a b`→`b a` 的 hunk
行序为 `-a -b +b +a`（同对角删除排在插入前），该选择固定；确定性整数运算保证同输入
重复生成补丁逐字节相同。

## 4. 偏移后后续 hunk 的起点平移

hunk 记录起点是“原文本旧位置”。若前一 hunk 在偏移 Δ 处应用成功，说明目标文本相对
原文本整体错位 Δ：后一 hunk 查找中心也平移 Δ，搜索范围仍是中心 ±F；多候选取 |偏移|
最小，相同距离取靠前（仅严格更近时更新）。错位只确认一次，不被后续 hunk 重复支付。

## 5. 计数器与差异上限

`edit.steps` 统计 snake 每次行比较（含失败的一次）与每次前进。第 d 轮遍历 2d+1 个
对角、snake 总长 O(N+M)，故 steps=O((N+M)(D+1))；MaxD 截断同样有界，超限立即返回
ErrTooDifferent（差异过大），不回溯。
