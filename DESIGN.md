# 设计推导

核心规范化 `N0`：逐字节扫描。`\r\n`→`\n`，单独 `\r`→`\n`，`\n`→`\n`；
一串空白在遇到行尾/流结束时才判定：行尾则整串删除，否则原样输出。
`\r\r\n`：第一个 `\r` 后紧跟 `\r`（非 `\n`）立即判为单独 `\r`，第二个才与 `\n` 配对 → `\n\n`。

## 1. 末尾换行策略（幂等推导）
- `Keep`：输出 `N0(x)`，不动末尾。
- `EnsureOne`：N0 后若 `len==0` 或末尾不是 `\n`，追加一个 `\n`；否则删掉末尾所有 `\n` 后补一个。
  空输入我选输出 `"\n"`：空文件被视为"一行无终止符"，补一个终止符。另一选择输出 `""` 也幂等，
  但非空无终止符文件（如 `"a"`）仍要补 `"\n"`，会造成空/非空规则不对称，故选补 `\n`。
- `Trim`：N0 后仅当以 `\n` 结尾时，把末尾连续 `\n` 收敛为一个；无终止符则不补（保持"无末行"信息）。

幂等：三策略的输出都不含 `\r`、不含行尾空白；再跑一次 N0 恒等。EnsureOne 的输出恰以一个 `\n` 结尾
或为空（实际不会空），再应用仍为自身；Trim 的输出要么恰一个末尾 `\n`，要么不以 `\n` 结尾，再应用不变。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut
要求 `ToOrig` 单调不减、`ToOut(ToOrig(o))==o`。删除点两侧被映射成同一个输出位置（删除产生的
"缝隙"，即后续换行符的输出偏移）。规则：被删原文字节映射到**其后第一个存活输出位置**，即行尾
`\n` 的输出偏移；删除区间末端（紧挨 `\n` 的原偏移）同样取该位置。编辑器光标落在被删空白中时落在
换行符**之前**（该缝隙恰为换行符的起点，光标在视觉上位于行尾换行之前）。流末尾被删的空白后无
存活字节时取输出长度（终点缝隙）。

## 3. 空白缓冲超限
强制判定为非行尾并输出：若后续真出现行尾，这些已输出空白本应删除 → 违反语义 2、5。故选择
**拒绝**：返回哨兵 `ErrWSBuffer`（带原偏移），进入终态，已输出内容保留；终态后再写返回
`ErrClosed`。拒绝不违反 4（任何切分方式下同一串空白的字节数相同，超限点一致）、5、6（根本无输出承诺，
错误可判定）。输出上限同理选择立即拒绝（`ErrOutputLimit`）。

## 4. par 拼接
段独立处理时开头可能继承上段末的待定 `\r` 或一长串待定空白，且空白长度无界，不能只回看 1 字节。
做法：切点 `cuts[k]` 向左扩展窗口起点 `start[k]`：回退到最近一个"硬边界"（非空白的 `\n`/普通字节
之后）；回退途中若末字符是 `\r` 再左移 1（覆盖 `\r|\n`）。第 k 段在原文字符串 `[start[k], end[k])`
上跑**完整**的单段规范化（窗口以硬边界开头，状态干净），只取输出/映射中落在 `[cuts[k-1], cuts[k])`
（首段从 0、末段至 len）的部分。修正范围有上界：待判定空白受 `WSBufferLimit` 约束，再加 1 个
`\r`；未配置上限时回退量本身有界于最长空白串（题目输入确定，故确定）。坐标平移：span 用
`Add(origDelta,outDelta)` 平移，拼接后按 `(orig,out)` 合并相邻等长（同速）区间。
NUL 以原文绝对偏移报告，属于哪个窗口就在哪段报错，整批失败。
