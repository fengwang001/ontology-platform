# 多版本可见性存储：读视图与回收器设计推导

本文档逐条推导四条核心语义的设计依据。代码分为五个包，依赖方向单向：
`txid ← version`、`txid ← snapshot`、`txid + snapshot ← reclaim`、以上全部 `← store`。

关键全局约定：

- 事务号只从注入的 `txid.Source` 分配，单调、不回绕、零值非法；代码不读真实时钟。
- 写事务在**开始时**分配事务号并原子登记为活跃写事务（`snapshot.Manager.BeginWriter`，
  分配与登记在同一把锁内完成）。
- 读快照在**建立时**取 `快照点 = Current() + 1`（不消耗事务号），并冻结当时的活跃写事务集合。
- 版本在**提交时**才进入版本链，版本上盖的是事务开始时分配的事务号。

## 1. 第 4 条：可见性判定规则的推导（为什么是左闭右开）

从「快照隔离 + 可重复读」出发：快照 S 必须恰好看到「S 建立那一刻已提交」的版本集合，
且之后无论谁提交，S 的读结果不变。把「S 建立那一刻已提交」翻译成事务号比较：

设 S 建立时事务号源的当前值为 `cur`（即已分配的最大事务号），快照点 `point = cur + 1`。

- **在 S 建立前已开始的事务**，其事务号 `c ≤ cur`，即 `c < point`。其中：
  - 建立前已提交的 → 必须可见；
  - 建立时仍未提交的（在活跃集合 `active` 中）→ 当时未提交，必须不可见；
    即使它之后提交，可重复读要求它对 S **永远**不可见，而 `active` 在建立时冻结，天然保证。
- **在 S 建立后开始的事务**，其事务号 `c ≥ cur + 1 = point`，即 `c ≥ point`，必须不可见。

于是可见性充要条件为：

> `c < point` 且 `c ∉ active` —— 左闭右开。

**为什么右边界必须开（`c < point` 而非 `c ≤ point`）**：快照点本身不消耗事务号，
`point = cur + 1` 正是**下一个将被分配的事务号**。若边界取闭，则 S 建立后第一个开始的
事务（恰好拿到 `c = point`）提交后会对 S 可见，S 的两轮读之间结果就会变化，直接破坏
可重复读。取开区间后，「等于快照点的提交」被精确排除，且该情形可以确定性构造
（先建快照、再让下一个事务提交），对应测试
`store/boundary_test.go: TestCommitExactlyAtSnapshotPointInvisible`。

**为什么左边界之外还需要活跃集合子句**：`c < point` 只说明事务在 S 建立前开始，
不说明它当时已提交。长事务可能建立前开始、建立后才提交；没有 `c ∉ active` 子句，
它的版本会"事后"变得可见，同样破坏可重复读。两个子句合起来恰好等价于
「S 建立那一刻已提交」，不多不少。对应测试 `TestActiveSetClause`。

## 2. 第 5、6 条：回收安全条件与增量策略

**安全条件推导**。版本 v（提交号 c）可以被回收，当且仅当任何活跃快照都不可能再读到它。
快照 S 读到 v 的条件是：v 是链上满足 `c < S.point 且 c ∉ S.active` 的最新版本。
因此只要链上存在一个**更新的已提交版本** v'（提交号 c' > c）满足
`c' < S.point 且 c' ∉ S.active`，S 就只会看到 v' 或更新者，永远轮不到 v。

对每个活跃快照 S 定义**回收视野** `h(S) = min(S.point, min(S.active))`。
若遮蔽者提交号 `c' < h(S)`，则 `c' < S.point` 且 `c'` 小于活跃集合的每个成员
（故 `c' ∉ S.active`），两个子句同时满足。取所有活跃快照视野的最小值作为水位
`W = min_S h(S)`（无活跃快照时 W 无界，一切被遮蔽版本都可回收），得到：

> **回收规则**：v 可回收 ⟺ 同链存在更新的已提交版本 v'，其提交号 `c' < W`。

**增量策略**。回收器不扫描键空间，而是维护一个按 `c'`（遮蔽事务号 shadow）排序的
**最小堆**（`reclaim.Reclaimer`）：

- 提交路径上，新版本遮蔽链上原最新版本时，把 `{key, 旧版本提交号, 新版本提交号}`
  压入堆，O(log n)，每个版本一生只入堆一次；
- 回收时只从堆顶弹出 `shadow < W` 的候选，遇到第一个未就绪候选即停；
- 弹出的候选在版本链上执行**安全移除**（`Chain.Remove` 拒绝移除链上最新版本，
  这道保险使崩溃恢复撤销提交后遗留的失效候选也绝对安全）。

**考察数上界**。设本次回收弹出 R 个候选，则考察数 = R（堆顶那次只读比较不计入），
与键总数 N、版本总数完全无关。最坏情况（全部候选就绪）为 O(候选总数)，
即 O(已被遮蔽版本数)，仍与 N 无关。对照测试
`store/reclaim_test.go: TestReclaimExaminedIndependentOfN`：
N=100 与 N=10000 时考察数同为 45（5 个热键 × 每键 9 个被遮蔽版本）。

**回收不改变活跃快照读结果（第 5 条）**：被回收版本对每个活跃快照都被
`c' < W ≤ h(S)` 的版本遮蔽，移除前后 `VisibleAt` 选中的版本相同。
测试用「回收前记录全部读结果 → 回收 → 逐字节比对」复现
（`TestReclaimPreservesActiveSnapshotReads`）。

## 3. 第 8 条：崩溃点枚举与半可见状态的排除

提交流水线为「追加版本体 → 记入索引 → 打提交标记」，对 K 个键共 3K+2 个中途点
（测试以 K=2 枚举出全部 8 个点并逐一注入崩溃）：

| 崩溃点 | 现场 | 恢复后状态 |
| --- | --- | --- |
| `BeforeAppend(k)` | 无任何变更 | 完全不可见 |
| `AfterAppend(k)` | 有版本体、无索引槽 | 撤销版本体，完全不可见 |
| `AfterIndex(k)` | 有版本体+索引、无提交标记 | 撤销两者，完全不可见 |
| `BeforeMark` | 全部键写完、无提交标记 | 全部撤销，完全不可见 |
| `AfterMark` | 提交标记已落 | 完全可见，恢复无需动作 |

**为什么不会半可见**，三层机制：

1. **意图记录（WAL intent）**：事务开始时先建 `txRecord`，流水线每步在记录中
   追加「已追加版本体的键」「已记入索引的键」。`Recover` 只需遍历在途记录，
   凡无提交标记者按记录精确撤销，不依赖任何猜测。
2. **提交标记是全有/全无分界**：标记之前的崩溃一律回滚到「完全不可见」；
   标记之后事务记录已从在途表删除，恢复不触碰，即「完全可见」。
3. **崩溃窗口内对读者也安全**：提交期间事务号仍在活跃写事务集合中，
   依据第 1 节的活跃集合子句，已追加但未提交的版本体对任何新快照都不可见，
   不存在"索引指向无版本体槽位"被读到的路径。恢复后 `checkConsistency`
   校验索引槽位与版本体一一对应，测试逐点断言。

## 4. 第 7 条：水位推进与新快照建立的竞态排除

**临界区边界**。`snapshot.Manager` 内部一把互斥锁保护三个操作：
`Begin`（读 `Current()`、冻结活跃集合、登记快照）、`BeginWriter`
（分配事务号 + 登记活跃写事务）、`Horizon`（回收器取水位）。
回收器计算水位与新快照建立因此**互斥**，不存在"读到一半的水位"。

**为什么互斥之外仍然安全**（核心论证）：设回收器在临界区内算得水位 W，
弹出的候选满足遮蔽提交号 `c' < W`。对任何**之后**建立的新快照 S_new：

- `c'` 是已提交事务号，而 `S_new.point = Current() + 1` 大于一切已分配事务号，
  故 `c' < S_new.point`；
- `c'` 对应的事务已提交并注销，不可能出现在 `S_new.active` 中。

于是 S_new 对那条链一定看到 v' 或更新者，永远不需要被回收的 v。
同理，回收时**已活跃**的快照由 `c' < W ≤ h(S)` 保证看不到 v。
两类读者都被覆盖，故"水位刚好推过 + 新快照刚好建立"的交错不会读到已回收版本。

**水位只升不降**：瞬时视野 `min_S h(S)` 可能因新快照建立而下降，
因此发布水位取历史最大值 `water = max(water, 本次有效水位)`，单调不减；
无活跃快照时有效水位取 `Current()`（一切已提交皆安全），同样单调。
对应测试 `TestWaterLevelMonotonicAndNoRaceWithNewSnapshots` 与
`store/concurrency_test.go` 中并发开关快照 + 并发回收的 `-race` 用例。
