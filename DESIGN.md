# 第 3 条推导：续行后紧跟只含空白/为空的物理行

依据 JDK 8 `java.util.Properties`（`LineReader.readLine`，Reader 版本）的状态机：

1. 读到行尾反斜杠时，该反斜杠被丢弃，置 `skipWhite=true`，越过一个行结束符，
   继续向同一逻辑行追加字符。
2. `skipWhite` 为真时，循环开头的空白吞噬段吃掉前导空格/制表，直到遇到非空白
   字符才置 `skipWhite=false`。
3. 空行检查（`len==0` 且遇行结束符）在空白吞噬之后执行：纯空白行上 `len`
   始终为 0，行结束符只结束该自然行；但此时处于续行状态，循环继续读下一自然
   行。关键是 `skipWhite` 只在“读到非空白”时才清零，空行/纯空白行里没有任何
   非空白字符，所以该标志保持为真。

结论：续行接上的物理行只含空白或为空时，整行（含换行）被吞掉、不追加任何
字符，也不终止逻辑行；并且“续行首部空白被剥掉”的效力继续传递给再下一物理行，
即其前导空白同样被剥掉。即 `k=a\` + `\n` + `\n` + `  z` 得到 `k -> az`，
`k=a\` + `\n` + `   ` + `\n` + `  z` 同样得到 `k -> az`。它与“首部空白
剥离”是同一机制：剥离不是“每续一行剥一次”，而是持续到首个非空白字符为止的
状态；纯空白行只消费空白，不消费该状态。

# 第 6 条推导：Store 各字符为何这样转义

Load 的读入规则决定 Store 的最小转义集合，逐字符对照：

- 行结束 `\n`/`\r`：Load 中终止逻辑行，必须写成 `\n`、`\r`，否则条目被拆开。
- 反斜杠：Load 中 `\x` 一律吃掉反斜杠，故必须写成 `\\`。
- 值开头的空格/制表：续行首部剥离会吃掉值侧前导空白，故值第一个空白必须写成
  `\ `（或 `\t`）；值中间的空白 Load 原样保留，不转义。
- 键开头的空白：Load 忽略键侧前导空白，故键首空白必须转义；键中间的裸空格、
  `=`、`:` 会被当作键值分隔符，必须写成 `\ `、`\=`、`\:`。
- 键首字符为 `#`/`!`：Load 在剥去前导空白后的行首见到二者即判注释，整行丢失，
  故以它们开头的键必须转义；值在分隔符之后，行中 `#`/`!` 不触发注释，无需转义。
- `\t`、`\f`：Load 把 `\t`/`\f` 还原为 TAB/FF；二者不终止逻辑行，原样输出也可
  往返，但本实现统一写成转义形式。
- 非 ASCII（如中文）：Load(Reader) 按 UTF-16 读、Store(Writer) 原样输出，非
  ASCII 无语法含义，原样写 UTF-8，不写成 `\uXXXX`。
- 空键、空值：空键直接以分隔符起行（`=v`））；空值写作 `k=`，无需额外字符。

# 七条语义与测试函数对照表

| 语义 | 钉住它的测试函数 |
|---|---|
| 1 分隔符（=、:、空白、两侧空白、==、空键、裸键） | props 包 `TestSeparators` |
| 2 注释（行首 #/!、行中 # 保留） | props 包 `TestComments` |
| 3 续行（奇偶反斜杠、空白剥离、#、EOF、空白续行传递） | logical 包 `TestContinuation`，props 包 `TestContinuation` |
| 4 转义（控制字符、\x、键中 \= \: \空格、\uXXXX 行列号错误） | props 包 `TestEscapes`，`TestUnicodeErrorPosition` |
| 5 重复键覆盖且顺序按首次出现 | props 包 `TestDuplicateOrder` |
| 6 Store 最小转义往返（空键空值空白 # ! = : \ 换行 TAB 非 ASCII） | props 包 `TestStoreRoundTrip` |
| 7 随机 1000 组往返 | props 包 `TestRandomRoundTrip` |
| 复杂度：计数 ≤ 2× 字节数 | logical 包 `TestByteCheckBudget` |
