# Quoted-Printable 设计说明

## 推导一：行末空白

解码器把硬换行还原为 `\r\n`，无法区分字面空格与“空格后紧跟换行”；RFC 2045 规定
行末空白在传输中不可靠，因此空格/制表符若紧跟硬换行或输入结束，必须编码为 `=20`/`=09`，
其余位置原样输出。样例：`"a \n"→"a=20\r\n"`、`"a b"→"a b"`、`"a "→"a=20"`。

## 推导二：`=XX` 不跨软换行

输出单位（token）长度为 1（字面字节）或 3（`=XX`）。若当前列 `c` 加上 token 长度后
超过 76，或 `c==76` 且后面还有 token（本行不能是最后一行内容），先输出 `=\r\n` 再输出
token。因此 token 永远完整落在同一行；75 列后只能接软换行，软换行的 `=` 计入 76。

## 软换行前的空格

空格写成字面 `' '` 后立即插软换行，输出为 `" =\r\n"`。解码器在 `=\r\n` 处删除的只是
`=\r\n` 三个字节，空格保留；而“行末空白”只由**硬换行或输入结束**定义，软换行不是硬换行。
结论：该空格不算行末空白，不写 `=20`。它也不会造成多余长度：软换行前最多 75 列。

## 往返等式

设 `N(x)` 把 x 中的 `\n` 与 `\r\n` 统一规范化为 `\r\n`（孤立 `\r` 保持字节 0x0D），则
对任意字节串 x：`Decode(Encode(x)) == N(x)`。即换行被规范化，其余字节（含孤立 `\r`、
二进制、行末空白）逐字节还原；且 `Encode(x)` 必被严格解码器接受。

## 最小转义（幂等性）

编码器仅转义 `=`（0x3D）、33–126 之外的字节，以及硬换行/输入结束前的空白；空格制表符
在其他位置均原样保留，软换行只在“下一 token 放不下（含 `c==76` 且其后非空）”时插入。
因此对严格解码器接受的 y，`Encode(Decode(y))` 与 y 的差异只能来自 y 中对可打印字节的
冗余 `=XX` 转义（软换行位置由同一长度判据确定，不会产生差异）。

## 复杂度

扫描输入一次切行并记录各行末空白长度（每字节 1 次检查），编码时每字节再检查 1 次，
非导出计数 `checked ≤ 2·len(input)`；插软换行只重置列计数，不回退重扫。

## 语义—测试对照

<!-- TODO: 测试函数命名定稿后回填 -->
