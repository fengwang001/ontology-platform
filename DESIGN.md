# 规范化器设计推导

## 推导 1：末尾策略与空/仅换行输入
行尾与行尾空白先做逐行规范化（每行至多一个 `\n`，尾随 ` \t` 删除），末尾策略只作用于尾部换行块。
- Keep：逐行结果原样保留，不新增也不补。
- Ensure：结果为 `""` 时保持 `""`；否则去掉所有尾部空行后以唯一 `\n` 结尾。
- Trim：全空白字符输入（含 `""`）为 `""`；否则原文含任一换行则去尾部空行保留一个 `\n`，原文无换行则不加。
空输入选 `""` 而非 `"\n"`：若 Ensure(`""`)=`"\n"`，则 `""` 与 `"\n"` 被映射到同一输出，N 在空集上凭空造字节，合成 `\n` 无原文锚点，且输出可长于输入；选 `""` 使三策略空输入一致、合成换行只在非空结果发生，幂等证明最简单。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等：输出不含 `\r`、不含尾随空白，再规范化每行恒等；Ensure/Trim 输出尾部至多一个 `\n`，其尾部块为不动点，故 N(N(x))=N(x)。

## 推导 2：被删字节的 ToOut
规范化是单调满射 f: 原文偏移域 [0,n] → 输出偏移域 [0,m]；定义 ToOrig(o)=min{i:f(i)=o}（最左原偏移），故 ToOrig 单调不减且 f(ToOrig(o))=o，即 ToOut(ToOrig(o))=o。被删区间内无输出点可归属，f 在被删串上必须取常量；单调性强制该常量等于被删串右侧第一个保留字节的输出位置（删除点之后输出坐标不前进）。
- `\r\n` 的 `\r`：f(i)= 该 `\n` 的输出偏移；光标落在 `\r` 上吸附到换行符位置（行边界，`\n` 前一侧）。
- 行尾 `"  "`（后接 `\n`）：两个空格的 f 都等于该 `\n` 的输出偏移；光标在被删空白内吸附到换行符之前的行尾边界。
- 删除发生在文件最末（Trim 截尾）：常量为 m（输出终点）。

## 推导 3：空白缓冲超限
待定空白必须整串缓冲：提前输出违反尾随空白删除与幂等；提前丢弃会在后续为非行尾时丢数据。选择**拒绝**：强制判定为非行尾并输出，若其后真到行尾则输出含尾随空白，违反语义 2 与幂等（语义 5）。拒绝返回可判定错误 `ErrPendingOverflow{Orig}`，只在输入真实含超上限连续空白时发生，与切分方式无关（跨切点 carry 不改变判定），不违反语义 4；错误时缓冲内容未提交、已输出保留，状态为终态+已提交前缀，可解释。输出上限在写入前检查，超限返回 `ErrOutputLimit`，已输出保留。

## 推导 4：par 拼接
切分点处至多残留两种待定态：单个待定 `\r`（长度 1）或一串未判定尾随空白（长度 ≤ 缓冲上限 B），二者互斥，故每段对下一段的 carry 长度 ≤ B+1，为常数上界，与输入规模和 K 无关。
做法：段 k 用绝对原文基址 base 初始化，以 `Suspend()` 关闭（不应用末尾策略，原样返回待定 carry 的字节与长度）；段 k+1 先把 carry 字节以 base 绝对偏移重新喂入（这些字节尚未提交映射），再喂自身分片。carry 随后续字符被判定：后接 `\n` 则 `\r` 记入被删区间；空白后接换行则整串记入被删区间；否则保留输出。映射全部以绝对原文偏移记录，拼接时只需顺序合并各段 span（坐标已全局化；段输出按序追加，输出坐标天然连续）。最后一段普通 Close 得 Keep 结果，再由 span 变换统一施加 Ensure/Trim（截断尾部区间或追加合成 `\n` 区间）。逐段结果与单流状态机逐字节相同，故任意切点、任意 K 一致；错误携带全局偏移，最先出错者胜出。

## 其他
NUL 严格模式在该字节提交前返回 `ErrNUL{Orig}`，非严格原样通过；非法 UTF-8 视为普通字节原样通过。span 仅在输出推进方式变化时产生区间（保留段与删除段相邻合并），区间数 O(删除点数)，纯 `\n` 文本为单区间；查询二分（sort.Search），检查数 ≤ ⌈log2 R⌉+1 ≤ 2log2 R+4，由非导出计数器 lastProbe 观测。单个 norm 实例非并发安全；par 内部并发安全。
