# DESIGN — 结构化日志字段脱敏与一致性采样器

模块 `ontology`，仅标准库，状态全在进程内存，输出经 `sink` 落盘。
包划分：`record`（记录模型/编解码）→ `rule`（规则编译）→ `mask`（脱敏执行）→
`sample`（采样决策）→ `sink`（落盘与损坏检测）→ `cmd/demo`（演示判定）。

## 1. 一致性采样的推导

目标：同一追踪链（同一 TraceID）的全部记录，要么全留、要么全丢。

- 记录逐条到达，顺序任意、时间任意、条数任意；若对每条记录独立掷骰，
  同链记录会以概率 (1-p)^k 出现部分丢失，链必断。故决策**不能依赖记录本身**。
- 决策也不能依赖到达顺序或时间（重放、乱序、多副本下不可复现）。
- 链上所有记录唯一共享且不变的属性是 TraceID。因此决策只能是
  `D = hash(TraceID) ∈ [0,1)` 与采样率 `rate` 的确定性比较：`keep ⟺ D < rate`。
  同 ID ⇒ 同哈希 ⇒ 同决策，天然一致；与记录内容、顺序、时间、并发均无关。
- 实现：FNV-1a 64 位哈希 / 2^64 得到 [0,1) 均匀值。单次决策恰好 1 次哈希（O(1)），
  用非导出原子计数器 `hashCount` 供测试断言。
- **无 TraceID（含空串）策略**：无法归入任何链，保守起见**一律保留**，
  不消耗哈希（决策与 rate 无关）。已在测试中固定该语义。
- `rate=0`：除 error 强制保留外全丢；`rate=1`：全留。两者都是上式的自然端点。

## 2. error 强制保留与「链不完整」标记

- 级别 ≥ error 的记录无条件保留（可观测性优先于采样率）。
- 触发「链不完整」标记的**唯一**条件：`decide(TraceID)=丢 且 level≥error`。
  此时记录被强制保留，但它所在的链已被采样丢弃，链必然残缺，
  故写入保留字段 `_chain_incomplete: true`（`record.ChainIncompleteKey`）。
  链被保留、或无 TraceID（始终保留）时，error 记录**不得**打标。

## 3. 字段路径与点号消歧

- 规则路径 `a.b.c` 表示嵌套映射字段 a→b→c；`.` 是唯一的层级分隔符。
- **消歧规则**：字段名中字面的 `.` 必须在规则路径里写成 `\.`（反斜杠转义），
  未转义的 `.` 一律视为分隔符。故字段名 `a.b` 用路径 `a\.b` 引用；
  路径 `a.b` 永远只匹配嵌套结构，绝不匹配字面字段 `a.b`。解析期校验：
  空段、孤立末尾反斜杠、非法转义均为编译期错误。
- 路径中某层不是映射（是标量/数组）⇒ 该路径在此记录上不匹配，跳过，不报错。
- 路径不存在 ⇒ 不匹配，静默跳过。
- **数组透明**：数组不消耗路径段，路径规则作用于数组的每个元素
  （`users.pw` 命中 `users` 数组每个元素映射的 `pw`）。
- 规则命中某节点即整体处理该子树，不再下钻。

## 4. 脱敏三种方式的不变量

- 替换：固定串 `[REDACTED]`，不保留原长度。
- 哈希：`hex(sha256(json(值))[:8])`。同值同结果、不同值不同结果、输出≠原值、
  单向不可逆；无盐、跨记录跨进程稳定（便于链上关联）。
- 截断：仅作用于字符串，保留前 N 个字符（rune）并追加 `…[truncated]`；
  长度 ≤ N 原样保留；非字符串不处理。
- 彻底性：按值模式（正则）的规则对**所有**字符串叶子求值，同一敏感值出现在
  任意多条路径/数组元素中都会被同一规则命中；路径规则经数组透明性覆盖元素。
- 脱敏在记录的深拷贝上进行，原记录与非目标字段逐字节不变
  （测试用 JSON 序列化逐字段比对）。

## 5. 复杂度论证

- 规则集编译期为路径建 trie。匹配时对记录做一次 DFS：每访问一个字段节点
  做一次 trie 查找，非导出计数器 `matchCount` +1。故每条记录的匹配次数
  ≤ 字段节点数（+常数），与规则条数无关；上界 `记录数*(字段数+常数)`，
  远优于 `记录数*规则数*字段数`。测试用 100 规则 × 10 万记录断言。
- 采样决策 O(1)：单次 `Decide` 恰好 1 次哈希，计数器断言。
- 脱敏与采样均为纯函数（作用于拷贝、无可变共享状态），天然线程安全；
  计数器用 `sync/atomic`，`-race` 干净。

## 6. record 的防御性约束

- 嵌套深度上限 `MaxDepth=32`（构造与解码时校验），超限拒绝并计数
  （`ErrDepth`，`DepthRejections()` 可读）。
- 循环引用：构造时 DFS，用活跃路径上的 map 指针集合检测自引用，
  命中即报 `ErrCycle`，不会死循环/栈溢出。

## 7. sink 落盘格式与截断分类

布局：`magic(8B "ONTSINK1")` + 重复 `[len uint32 LE][payload][crc32 IEEE uint32 LE]`。

- 自描述头：魔数识别格式；头不足 8 字节或魔数不符 ⇒ `ErrHeader`。
- 截断点落在长度前缀（不足 4 字节）⇒ `ErrLength`。
- 长度可读但 payload 不足 ⇒ `ErrBody`。
- payload 完整但 CRC 不足 4 字节或校验不符 ⇒ `ErrCRC`。
- `ReadAll` 返回最后一个完整记录之前的全部记录 + 上述可判定错误
  （`errors.Is` 可区分四类）；恰好截在记录边界 ⇒ 无错误、前缀全部恢复。
- 故障注入测试对 1..len-1 每个截断点断言分类与可恢复记录数。

## 8. 错误分类（均可 `errors.Is` 区分）

`record.ErrDepth`、`record.ErrCycle`、`rule.ErrConflict`（同路径不同方式，
编译期检出并指明冲突的两条规则编号）、`sink.ErrHeader/ErrLength/ErrBody/ErrCRC`。

## 9. 边界语义汇总

空记录（nil 字段表→空表，合法）；无/空 TraceID（一律保留）；空串字段值
（截断不变、可哈希）；深度 1（平铺记录，合法）；数组内嵌套映射（路径透明命中）；
rate 0/1 见 §1。
