Derivation 1: Empty Lines
空行选择为跳过，不作为记录：记录结束后若下一字节直接是换行，该行没有任何字段。
因此单列表中唯一字段为空时必须写成 `""\n`；若写成 `\n`，解析器只能判为空行，数据丢失。
末尾换行只结束上一条记录，不产生新记录；EOF 处已有开口字段才补出记录。

Derivation 2: Parallel Cuts
每个 chunk 第一遍只跑裸 DFA：用 5 位掩码同时从 5 个入口状态求值，并按入口记录字段/记录计数。
前一个 chunk 的确定结束状态是后一个 chunk 的真实入口；掩码使全部 worker 一次并行启动。
入口状态确定后，K 个 worker 第二遍并行完整 lex，事件坐标加段起点；再从左到右拼接。
切点可在 `""` 或 `\r\n` 中间：入口携带 escQ 或 crOut 微状态，不回扫任何字节。
跨段字段先给片段事件，由 table 按字段号拼接；段内偏移加段起点即全局字节偏移、记录号加前缀记录数。

Derivation 3: Pending CR
未引号态见到 `\r` 进入 crOut，只表示待定，尚未结束字段。
下一字节为 `\n` 才提交字段和记录；为逗号则在 CR 处形成单字段后开始新字段；其他字节或 EOF 均为孤立 CR。
半包续传允许自然停在 crOut。并行切点若停在 `\r` 后，下一段入口即 crOut；切在 CR 前则由下一段重新见到 CR。
引号态 CR 是普通内容，不进入待定态。

Derivation 4: Immediate Limits
字段长度在内容字节加入前检查：未引号态每个普通字节 +1；引号态普通字节 +1，转义对 `""` 在第二个引号到达时只 +1。
上限 N 时，第 N+1 个内容字节到达立刻报错，不等字段结束。
字段数在分隔符创建下一字段前检查；记录数在提交记录、保留完整记录后检查。
终态错误被缓存，后续 Feed/Close 返回同一错误。

State Table
状态：F=字段开始，U=未引号，Q=引号内，E=引号内刚见引号，C=未引号CR待定，X=引号后CR待定。
类别：`,`、`"`、`\n`、`\r`、普通、EOF。
| 状态 | , | " | \n | \r | 普通 | EOF |
|---|---|---|---|---|---|---|
| F | 空未引号字段,F | Q | 空行,F | C | U | 开口字段则提交 |
| U | 提交字段,F | ErrQuote | 提交记录 | C | 追加,U | 提交记录 |
| Q | 追加,Q | E | 追加,Q | 追加,Q | 追加,Q | ErrUnterminated |
| E | 提交字段,F | 追加引号,Q | 提交记录 | X | ErrAfterQuote | 提交记录 |
| C | CR处交字段后新字段,F | ErrLoneCR | 提交记录 | ErrLoneCR | ErrLoneCR | ErrLoneCR |
| X | CR处交字段后新字段,F | ErrAfterQuote | 提交记录 | ErrAfterQuote | ErrAfterQuote | 提交记录 |

规范输入：仅以 `\n` 结束记录；普通空字段不加引号；单列表唯一空字段写 `""`；含逗号、双引号、CR、LF 的字段加引号且内部引号双写。
