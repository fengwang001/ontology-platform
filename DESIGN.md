# DESIGN.md

## 末尾换行策略

规范化主体先把所有行尾转为 `\n` 并删除行尾空白；末尾空行指末尾连续的空行（多个 `\n`）。三策略：
`Keep` 保留主体结果；`Ensure` 在主体结果为空时保留空，否则确保以一个 `\n` 结束；`Collapse` 先 `Ensure`，再把末尾多个 `\n` 压成一个。

| 输入 | Keep | Ensure | Collapse |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

选择空输入仍输出空，而不是造 `\n`：三种策略都只重排已存在文本，不凭空造字节，且空、`\n` 均为策略不动点，因此幂等。

## 被删字节的 ToOut

映射表示字节边界，`[0,len]` 均合法；输出侧右逆要求先对每个输出边界找一个原像，再让被删边界复用相邻原像。
采用右连续规则：被删空白和 `\r\n` 中的 `\r` 映射到其删除区间后的第一个输出边界。
若其后是行尾 `\n`，该边界是换行符左边界，因此编辑器光标落在换行符之前；行尾符之后的列位置只能由下一行第一个边界映射得到。
`ToOrig(o)` 用输出边界反查其原像；终点用最后一个原像。于是对输出域有 `ToOut(ToOrig(o))==o`，两函数均单调不减。

## 空白缓冲与超限

扫描器保留一串待定的空格/制表符：遇到普通字节判定为行中空白并整体放出，遇到行尾判定为行尾空白并整体删除，Close 时按流结束判定。
若强制把超长空白判定为行中空白，下一块出现 `\n` 时会产生不可回退的错误输出，破坏任意切分一致和幂等。
因此选择拒绝：未决空白数达到 `MaxTrailingRun` 时返回带原文偏移的 `ErrTrailingOverflow`，已输出字节和映射保留，实例进入终态；较小上限只会更早拒绝，不改变未拒绝前缀。

## par 拼接

按任意边界切 K 段时，每段并行执行到“主体规范化但未应用末尾策略”的状态。段边界只可能遗留两类有界状态：一个待定 `\r`（1 字节）和一串未判定空白（上限大小）。
主流程先给第 0 段续入一段共享前导：`\n`、一个非空白普通字节，或末尾空白上限加 2 字节，取较小值；足够判定 `\r|\n` 与空白运行。
随后按原顺序把每段自身窗口和下一段前导喂给对应 worker，得到 `(输出,span,主体消耗数,主体输出结束)`；前导造成的尾部输出与区间丢弃，再按主体结束位置平移拼接。
映射平移只给区间两端加全局字节偏移，二分查找仍在合并后的删除区间上；修正状态大小至多 `MaxTrailingRun+2`，与总长度和 K 无关。主体全部完成后仅应用一次末尾策略，所以结果与单流完全一致。
