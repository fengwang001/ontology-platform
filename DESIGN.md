## 推导一：切法无关（语义 3）
匹配只允许在“已确定的字节”上决策。压缩器始终保留 maxMatchLen 个未决字节（maxMatchLen=32768）
在 pending 中：仅当 pending 长度 > maxMatchLen 时处理最前一个位置，因为该位置最长匹配不超过
maxMatchLen，后续 Write 无论带来什么字节都不会改变该位置决策。采用贪心解析（取窗口内最长候选，
平手取最近距离），决策只依赖“已提交字节+窗口”这一确定状态，故与 Write 切法无关。Flush 时
pending 全部强制确定：可能跨过 Flush 点的匹配截断到 Flush 前，随后发刷新标记；Flush 不重置窗口，
之后匹配仍可回指历史。

## 推导二：重叠回指复制（语义 2）
块复制（一次 copy 整段）在 d<len 时只取旧窗口快照，重复段不会被新写入字节填充；如 aaa 用 d=1、
len=5 会得到错误结果。正确语义是 out[o]=out[o-d]（o 递增）。实现用 copy 但每次最多复制 d 字节：
先 copy min(d, 剩余) 个已存在字节再循环。Go 的 copy 对前向重叠（dst>src）按字节推进，故 d>=1 时
与逐字节等价，且每次至少推进 1 字节、无额外大缓冲。压缩端匹配比较同样按字节 rle 式比较。

## 推导三：两类距离错误（语义 7）
“距离>窗口容量”是格式/配置不变量错误：压缩器只登记窗口内候选，窗口未填满时按已产生字节数限制，
合法流永不产生。“距离>已输出字节数”是状态错误：流起始窗口未填够时可能引用不存在的历史，成因
不同必须可区分。并行预置字典不改含义：上一块原文作为窗口初始历史，解压器此刻确已还原这些字节，
块视角下“已输出”含预置字典长度，上限恒为 min(已输出, 窗口容量)。

## 推导四：并行确定性（语义 9）
块 i 的匹配器输入仅为“块 i 原文 + 块 i-1 原文末 windowCap 字节”，决策是纯函数：不读块 i-2、
不读任何压缩输出、不读完成顺序与调度，故 workers=1/2/4/8 按块序汇总后逐字节一致。块间无需刷新
标记：预置字典保证块 i 回指在流式解压时都指向已输出字节；刷新标记只在用户 Flush 处。整体一个
流头一个流尾，总长=各块长度和，CRC32 对全原文连续计算。

## 流格式记录布局
魔数 "LZ7" + 版本 1（4 字节流头）。整数均为 LEB128 无符号变长整数（≤10 字节，第 10 字节仅允许
1 位，否则溢出 64 位）。

| 记录 | 布局 |
|------|------|
| 字面量段 | tag=0；varint n；随后 n 个原始字节 |
| 回指 | tag=1；varint distance；varint length |
| 刷新标记 | tag=2（无载荷） |
| 流尾 | tag=3；varint 原文总长；varint CRC32(IEEE) |

约束：3<=length<=32768，1<=distance<=windowCap；字面量段与回指相邻交替，空段不发。
