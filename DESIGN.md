# 排序合并连接溢出/回退执行器 设计

## 1. 范围
两路已按连接键升序排列的输入做等值内连接。单侧同键分组超过内存阈值时溢出到
本地临时目录，连接主循环对溢出组回退重扫。仅用标准库。

## 2. 组起点的两种表示
推进右侧时记录「当前组起点」，左组每个新行都要从该起点重扫右组。
- 右组完全在内存：起点是切片下标 `startIdx`，重扫即 `idx=startIdx`，O(1)。
- 右组已溢出：行是变长的（键长、值长都不定），**不能用 行号×固定长度**
  推位置；起点必须是该组第一行帧在文件内的**字节偏移**，重扫用
  `io.ReadSeeker.Seek(off, Start)`。帧格式见 §5。

错误实现的典型症状：按行号推算偏移，组内前几行恰好等长时看似正常，
随后错位、读出半截行或 CRC 失败。测试构造右组恰好跨越阈值，断言
「全内存」与「溢出」两次结果集逐字节相同。

## 3. 输出顺序
按键升序；同键内笛卡尔展开，次序为「左行到达序为主序，右行到达序为次序」
（外层循环遍历左组，每个左行依次重扫整个右组）。同一份输入多次执行结果
逐字节一致（测试跑 3 次）。打乱同键行相对到达序会改变输出，这是定义使然。

## 4. 主循环与计数器
两侧各维护当前行。键小的一侧前进；键相等时收集左右两个同键组，然后
`for 每行 in 左组 { 从右组起点重扫右组，逐对输出 }`。
计数器：
- `reads`：两侧 `Next` 返回成功行的总数（不含报错调用）。
- `rescans`：重扫从右组读出的行数之和。
等式：`reads == 左总行数 + 右总行数`（每个输入行恰好到达一次），
`rescans == Σ_键k (左组k行数 × 右组k行数)`（每个左行重扫其匹配右组一遍），
总推进 `reads+rescans`；不存在整表重扫。
内存峰值：`spillgrp` 内部计数器记历史最大驻留行数，阈值 T 下 ≤ T。

## 5. 溢出文件格式（自描述）
头部：magic `"SPLGRP01"`(8) + keyLen(uint32 BE) + key 字节。
其后若干行帧：`rowLen(uint32 BE) + 行编码(rowLen 字节) + CRC32(uint32 BE)`。
行编码：`keyLen(uint32)+key+valLen(uint32)+val`（同组键重复存储以自描述）。
CRC 覆盖该帧的行编码字节。损坏分类区间（按截断长度 n）：
1. n ≤ 头部长：头部不完整；2. 落在某帧前 4 字节：长度前缀不完整；
3. 落在行体内：行体不完整；4. 长度到了但 CRC 字节被改：CRC 不匹配。
截断文件只读出最大可恢复前缀（完整帧为止），半截行绝不输出；连接遇此
必须中止报错，不得少行假装成功。

## 6. 并发与状态
溢出由内部单写协程顺序写盘（有界 channel，容量保证驻留 ≤ T）。
组显式状态：`writing → complete`；重扫前必须等 `complete`（等 channel/
Wait，禁止 sleep），写出未完成时重扫阻塞等待而非读到半成品。

## 7. 故障与边界
四类可判定错误，`errors.Is` 区分：`ErrCorrupt`、`ErrStream`、
`ErrSeek`、`ErrUnsorted`（另 `ErrEmpty` 属损坏的子类标记）。
流中途报错：保留已产出结果，报告已消费到的键。Seek 失败：报告键与组。
输入逆序：检出并指出第一处逆序位置（侧别、下标）。
空键合法；两侧空/一侧空 → 空结果非错误；组大小 1；全同键；
大组笛卡尔结果数 = |Lk|×|Rk| 精确断言。

## 8. 包划分
row（行/键/编解码）、stream（流接口与假流）、spillgrp（缓冲/溢出/重扫）、
smj（主循环/计数器/错误）、verify（损坏分类、可恢复前缀、结果核对）、
cmd/demo（逐条判定，≤20 行输出，退出 0）。
