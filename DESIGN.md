# 设计推导

## 1. UTF-8 非法单元
从首字节期望长度开始；首字节非法则该字节单独成非法单元。合法首字节先等待第二字节：第二字节一旦不在专属区间，首字节立即单独成非法单元，当前字节重新作为新首字节。第二字节合法后，后续只接受 `80..BF`；若在收满前遇到非续字节或流结束，已收下的前缀作为一个单元：流中为非法单元，流末为截断。故 `F0 90 80 41` 已形成 3 字节残缺前缀，只替换一次；`E0 80 80` 在第二字节即判定 `E0` 非法，随后两个续字节各自非法；`E2 82`+EOF 是一个截断前缀。

| 首字节 | 第二字节区间 | 非法时吞掉字节 |
|---|---|---|
| `C2..DF` | `80..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `E0` | `A0..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `E1..EC` | `80..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `ED` | `80..9F` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `EE..EF` | `80..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `F0` | `90..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `F1..F3` | `80..BF` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `F4` | `80..8F` | 第二字节非法吞 1；之后残缺吞已收前缀 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

## 2. 流式缓存
UTF-8 只缓存一个合法首字节及其已接受的续字节，最多 3 字节；第二字节非法时不缓存、立即结束首字节，并把当前字节留在同一扫描位置重新判定。看到非法第二字节可立即判首字节非法；看到合法第二字节后，只要遇到非续字节也可立即判残缺前缀非法，只有数据不足才等待到 `Close`。UTF-16 最多缓存 1 个奇数字节；高代理最多再等待一个 2 字节 code unit。高代理后不是低代理时，只吞高代理 2 字节，当前 code unit 重新解析。

## 3. par 切点
每个内部切点只向回看至多 4 字节并用独立 UTF-8 解码器探测：结束时若仍有合法前缀，返回该前缀首字节地址，否则返回切点本身。第 `i` 段处理“本切点续传地址”到“下一切点续传地址”。这样上一段绝不输出跨切点字符；拥有该前缀的下一段从首字节重放。例：`C3|80 80` 中第一段不拥有 `C3`，第二段从 `C3 80` 解出合法标量，再把第二个 `80` 解为非法。每个前缀只在相邻一段中存在一次，故非法单元数与顺序扫描相同。探测每点固定 ≤4 字节，复杂度为 `n+8K` 量级。

## 4. 上限断点
编码输出前先计算完整标量的目标字节数（UTF-16 代理对按 4 字节），容不下就停在该标量前并返回 `ErrOutputLimit`。`Write` 的 `n` 只统计本次 `p` 中已随完整单元消费的字节；上次残留缓存不属于本次 `p`，不计入本次 `n`。触发上限时解析器不保留半截标量：若旧缓存与本次前部凑成完整单元，它必须已完整输出，之后才可能在下一标量前停下；调用方从返回的 `p[n:]` 新建实例即可拼接。字节守恒中的“已消费”另由解码器累计，包含此前已进入缓存的字节。
