# 设计文档：结构化日志脱敏与采样器

## 1. 范围与结构

仅标准库；状态在进程内存；记录可落盘。包划分：
- `record`：记录 = 级别 + 追踪 ID + 嵌套字段映射（`map[string]any`，值为 map/[]any/标量），JSON 编解码。
- `rule`：脱敏规则与规则集预编译（路径 Trie + 值精确匹配索引）。
- `mask`：执行 replace/hash/truncate 三种脱敏。
- `sample`：按追踪 ID 的一致性哈希采样 + error 强制保留。
- `sink`：自描述二进制帧落盘与损坏分类恢复。

## 2. 一致性采样推导

一条追踪链的多条记录若各自独立掷骰，则同链有的留、有的丢，链断裂。
因此采样决策**只能是追踪 ID 的纯函数**：

  keep(T) = h(T) mod 10000 < 采样率万分比

其中 h 为 FNV-1a 64 位哈希。它与记录内容、到达顺序、时间、并发协程均无关，
故同链必然同决策；同一 ID 重复决策结果确定。单次决策恰好 1 次哈希计算（O(1)）。

无追踪 ID / 空串：归一为常量链 ID `"\x00no-trace"`，即所有无链记录共享一条
「虚拟链」，在给定采样率下仍整体一致地留或丢；error 仍按第 3 节强制保留。
采样率 0：keep 恒假（全丢，error 除外）；采样率 1：keep 恒真（全留）。

## 3. error 强制保留与「链不完整」标记

级别 >= error 无条件保留。若该链的哈希决策本为「丢」，则本记录是该链唯一可能
出现的成员，链必然不完整：此时给记录打 `Incomplete=true`。标记出现的充要条件是
`链决策=丢 且 级别强制保留`；链决策为留时不打标。

## 4. 嵌套路径与点号消歧

路径按 `.` 切分逐层深入；键名中的字面点号用 `\.` 转义，故 `a\.b` 表示键
`"a.b"`，`a.b` 表示嵌套 `a`→`b`，二者在规则语法层即无歧义。匹配某层时：
1. 当前映射存在该段键（转义还原后）即进入；键含点号按其字面整体匹配，不拆分；
2. 路径不存在 → 未命中（无操作）；
3. 某层既不是映射（数组按数字下标进入）也找不到该键 → 未命中（不报错、不改值）。
数组用元素下标段（如 `a.0.k`）进入，数组内嵌套映射同样受路径与值规则覆盖。

## 5. 脱敏语义

- replace：替换为固定串（不保留长度）。
- hash：FNV-1a 的十六进制摘要；同值同结果、不同值不同结果、不可逆（输出不含原值）。
- truncate：保留前 N 个字符并加固定后缀 `"...<truncated>"`；N<=0 只给后缀。

路径规则命中其路径节点；值规则在整棵树（含各数组元素）逐标量节点比对，故同一
敏感值出现在任意多处都被处理。脱敏在深拷贝上进行；非目标字段与原序列化结果逐字节
一致（JSON 序列化后按字节比对）。

## 6. 预编译与复杂度

规则集编译为 Trie。处理一条记录时按其字段树导航 Trie：每个字段节点最多做一次
Trie 转移，路径匹配次数 ≤ 字段数 + C（C 为常数），与规则数无关；非
`记录数*规则数*字段数`。值规则放入 map 精确索引，查表 O(1)。

## 7. 规则冲突

同一路径不得配置两种不同方式；编译期返回包含冲突两条规则的错误。四类可判定错误
（`errors.Is` 区分）：`ErrConflict`、`ErrTooDeep`、`ErrCycle`、`sink.ErrCorrupt`
（并细分为头/长度前缀/记录体/CRC 不完整或不匹配）。

## 8. 记录构造安全

构造时遍历字段树：检测深度 > MaxDepth 报 `ErrTooDeep`；用访问指针集合检测映射
自引用/共享环报 `ErrCycle`；均在递归进入前判定，不栈溢出、不死循环。

## 9. 落盘帧格式

文件 = 头 `ONTLOG1\n`（8B）+ 若干帧；帧 = 4B 大端长度 L + L 字节 JSON 记录体 +
4B 大端 CRC32(IEEE)。截断分类：不足 8B=头不完整；某帧不足 4B=长度前缀不完整；
有 L 但不足 L+4=记录体不完整；CRC 不符=CRC 不匹配。恢复返回最大完整前缀内全部记录。

## 10. 并发

编译后的规则集与采样器只读，无需加锁；脱敏在各自记录的拷贝上进行。故并发处理
结果与串行一致（按追踪 ID 排序后逐元素相同）。
