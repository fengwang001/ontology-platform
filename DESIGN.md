# DESIGN

## 流格式

整数一律为 uvarint（7bit/字节，小端序，>10 字节或溢出 64 位即非法）。

| 记录 | 字节布局 |
|---|---|
| 流头 | `0x4C 0x5A 0x01`（魔数 "LZ" + 版本 1），定长 3 字节 |
| 字面量段 | `0x00` + uvarint n + n 个原始字节 |
| 回指 | `0x01` + uvarint dist + uvarint len |
| 刷新标记 | `0x02`（无负载，仅同步点） |
| 流尾 | `0x03` + uvarint 原文总长 + uvarint CRC32(IEEE) |

## 推导 1：压缩结果与 Write 切法无关（语义 3）

位置 i 的令牌（字面量/回指）何时能被「确定」？贪心最长匹配在 i 处的长度
只取决于「历史 + 从 i 起的输入」。若当前缓冲中从 i 起有 avail 字节、找到
的最长匹配长 L：当 L < avail（缓冲内已出现失配字节）或 L 达到 MaxMatch
上限时，匹配不可能再变长，令牌被确定；若 L == avail 且未到上限，匹配可能
随后续字节延长，此时 i 处令牌**不得定案**。同理 avail < MinMatch(4) 时
连哈希都凑不齐，也不定案。因此压缩器缓存「未决字节」：上界为
MaxMatch（等待匹配收尾）+ litCap（已定案但未发的字面量，到量即发出），
与 Write 怎么切无关——每个位置定案所依据的字节集合相同，决策序列便相同。
Flush 的代价由此显形：Flush 把缓冲末尾当作流尾强制定案（final=true），
正在生长的匹配被截断在当前位置，故 Flush 必须强制结束当前匹配；Flush 之后
窗口不清空，后续匹配仍可回指 Flush 之前的数据。

## 推导 2：距离 < 长度的重叠回指（语义 2）

块复制（memmove 语义）按「源快照」复制：dist=3、len=9 时源只有 3 字节
有效，其余是未定义数据，得到的是垃圾而非 `abcabcabc`。LZ77 的语义是
**逐字节向前复制**：每复制一个字节，它就成为后续复制可引用的历史。实现
用固定距离 dist 的逐字节循环：第 j 步读窗口中距离恒为 dist 的字节，该字
节必然已经物化（j < dist 时读的是旧历史，j >= dist 时读的正是本次循环
此前写入的字节），故正确。总量 O(len)，环形缓冲访问 O(1)，常数小，在
解压路径上可以接受，无需引入分块复杂度。

## 推导 3：「距离超过已输出」与「距离超过窗口」为何是两个错误（语义 7）

窗口容量是格式的**静态**上限：dist > 窗口容量的记录在任何一个解压时刻都
非法，与已输出多少无关；已输出字节数是**动态**状态：dist <= 窗口容量但
dist > 已输出，说明记录出现在流中「还不够历史」的位置。混为一谈会让
「窗口配错」与「流本身损坏」不可区分。压缩器只从窗口内取候选（链上
dist > 容量即剪枝），故正常压缩器绝不可能产生 dist > 窗口容量，该错误
只可能来自损坏或伪造的流。并行块压缩中，预置字典就是上一块原文的尾部，
而这些字节在拼接后的流里**已经作为前一块的输出存在**，所以「已输出字节
数」的含义不变——解压器根本不知道块的存在，校验逻辑零改动。

## 推导 4：并行块压缩的确定性（语义 9）

块 i 的匹配器仅用「块 i 原文 + 原文中紧邻其前的窗口容量字节（预置字典）」
初始化，令牌流是这两者的纯函数：不读其他块的压缩结果，不与别的块共享
任何状态，故与 worker 数、调度顺序无关；结果按块下标写入切片再顺序拼接，
天然有序。流格式上无需块界标记：回指的距离本来就可以落在任何已输出字节
上，块 i 发出的回指指向前一块尾部，对解压器而言与跨任意位置的回指毫无
区别，因此块间**不需要刷新标记**，直接拼接令牌流、首尾各加一个流头/流尾
即是合法流。
