# DESIGN: unified diff / patch

包：`lines`（切行，保留行尾）→ `edit`（Myers O(ND)，依赖 lines）→ `hunk`（分组，依赖 edit）
→ `udiff`（渲染/解析，依赖 hunk/lines）→ `patch`（应用/反向/并发，依赖 udiff/lines）。

## 1. 零计数 hunk 头的行号（题 4）

`-a,b` 中 a 是该 hunk 在旧文件中覆盖的**第一行的 1 基行号**，b 是覆盖行数。b=0（纯插入）时
「第一行」不存在，统一取「插入点前一行」的行号：
- `x\n` → `y\nx\n`，插入点在文件首，前一行不存在 → a=0，头 H1=`@@ -0,0 +1 @@`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`，插入点在旧第 2 行之后 → a=2，头 H2=`@@ -2,0 +3 @@`。
- `a\n` → 空（C=3），删除旧第 1 行、新侧 0 行、删除点后无行 → `+0,0`，头 H3=`@@ -1 +0,0 @@`。

规则表（`+c,d` 对称）：

| 情形 | 书写 | a 的含义 | Apply 的解释 |
|---|---|---|---|
| b=0 | `-a,0` | 插入点前一行的 1 基行号（可为 0） | 在旧行 a 之后（0 基偏移 a 处）插入 |
| b=1 | `-a`（省略 `,1`） | 唯一覆盖行的行号 | 锚定旧行 a |
| b≥2 | `-a,b` | 首行行号 | 覆盖旧行 a..a+b-1 |

## 2. hunk 合并阈值（题 5）

结论：**g ≤ 2C 合并，g ≥ 2C+1 分开**，与 `diff -U C` 一致。
每处改动两侧各带 C 行上下文；前段的后 C 行与后段的前 C 行之间若不足两个完整上下文窗口
（即 g<2C，或 g=2C 时两窗口首尾相接、中间无多余未显示行），GNU 输出为一个 hunk；
g=2C+1 时两窗口间还剩 1 行不显示的未改动行，GNU 输出为两个 hunk。
阈值取 C：g=2C（C≥1 时 g>C）会被错误拆开，与 GNU 合并输出矛盾。
阈值取 2C+1：g=2C+1 会被错误合并，与 GNU 的两个 hunk 矛盾。故唯一取值 2C。

## 3. Myers 并列最短路径的选择（题 3）

同一 D 上同时可向下（删除）与向右（插入）时，**优先删除（down）**，并先吃完所有 snake。
`a b → b a` 时脚本为 `-a / ' b / +a`（删除排前），而非插入优先的 `+b / ' a / -b`。
渲染严格按脚本序，故同一 hunk 内 `-`/`+` 块的相对位置由该规则固定，输出逐字节确定。

## 4. 偏移应用后后续 hunk 的起点（题 7）

补丁中每个 hunk 的行号都相对**原始文件**。设先前 hunk 在原文上造成的净行数变化累加为
s = Σ(插入行数 − 删除行数)，则下一个 hunk 的查找中心平移 s：记录旧行号 a 的期望位置变为
a+s（0 基索引 a+s-1），模糊窗口 F 以该点为中心。理由：目标文本是「原始文本 + 先前 hunk
造成的整体平移」，同一物理上下文随之平移 s；平移后再独立选最近候选，并列取靠前。

## 5. 其他固定语义

- 行模型：`Line{Data []byte}`，Data 原样含 `\n` 或 `\r\n` 或无尾；`Join` 原样拼回。
- 计数器：Myers 每次对角线上的逐行比较（含 snake）计 1；超过 MaxD 立即返回 `ErrTooDifferent`。
- 无换行：行尾缺失由 `\ No newline at end of file` 标记；仅尾换行差异时补成「删旧行+加新行」。
- 应用：所有 hunk 先在同一快照上定位校验，全部成功后才拼接输出（原子拒绝）。
