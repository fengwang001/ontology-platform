# 多区间 Range 响应组装器 — 设计推导

五个包单向依赖：`rangespec` → `coalesce`；`source` 独立；`multipart` 依赖
`coalesce` 的区间类型；`serve` 依赖全部。反向依赖不存在。

## 1. `bytes=-0` 为什么是不可满足而不是语法错误

`-n` 三种写法之一的语义是「资源末尾的 n 字节」：合法区间为
`[size-n, size-1]`，且按题意 `n > size` 时裁剪为整个资源（不报错）。

语法层面 `-0` 完全合法：有前导 `-`、其后是一个能正常解析的十进制整数 `0`，
因此它能通过词法/语法分析，偏移定位不到任何非法字节。代入语义公式后，它请求
的是「末尾 0 字节」，即空区间，不覆盖资源中的任何字节。空集合不是资源的一个
有效字节范围（Range 至少选中一个字节），所以它在「结合资源长度归一化」这一步
才失败，属于不可满足，且必须带上资源总长 `size`，供调用方生成
`416 Range Not Satisfiable` 与 `Content-Range: bytes */size`。

结论：语法错误 = 词法/文法不通过（带出错字节偏移，由 `rangespec` 报）；
不可满足 = 语法通过但结合长度后选不出字节（带总长，由 `coalesce` 报）。
`-0` 属于后者。同理 `Start > size-1` 的 `a-b`/`a-` 也归入不可满足。

## 2. 为什么相邻区间也必须合并

归一化输出要求互不重叠且**互不相邻**：区间按起点排序后，若后一个的起点等于
前一个终点 +1（首尾相接），两者必须并成一个连续区间。

合并只影响「表示」，不影响覆盖集合：`[a,b]` 与 `[b+1,c]` 覆盖的字节并集就是
`[a,c]`，逐字节完全相同。若不合并，会破坏第 7 条。第 7 条规定「合并后只有一
个区间时退化为裸字节响应」。典型情形：客户端发两个范围 `0-99` 与 `100-199`，
它们不重叠、但首尾相接；若只合重叠、不合相邻，组装器看到的是两个区间，会错误
地套 multipart 封装；而它们实际覆盖一段连续字节，正确行为是单个区间、裸字节。
所以「相邻合并」是「合并后区间个数决定封装与否」的必要前提。

## 3. 归一化算法与比较次数上界

算法（全部为闭式区间 `[Start,End]`）：
1. 逐个区间做越界裁剪：`Start>=size` → 不可满足（带 size）；`End>=size` 时
   令 `End=size-1`（`a-` 同样裁到末尾）。裁剪不丢任何真实字节。
2. 按 `Start` 升序排序。使用**自带的自顶向下归并排序**，排序器每做一次区间
   起点比较就让非导出计数器 `comparisons++`。不用标准库 `sort` 正是为了让比较
   次数可证明、可测试。
3. 线性扫描合并：与当前最后一段比较，若 `Start <= last.End+1`（重叠或相邻）
   则并入（`End` 取较大者），这一步每个区间恰好触发 1 次比较，共 n-1 次。

比较次数上界：归并排序对长度 n 的数组至多 `n*ceil(log2 n)` 次比较（每层归并
每元素至多参与一次比较，共 ceil(log2 n) 层）；加线性合并的 n-1 次，总计

    C(n) <= n*ceil(log2 n) + (n-1) = O(n log n)

因此不是 O(n²)。n=100 与 n=10000 的对照测试直接读取计数器，并验证
`C(10000)/C(100) <= 2*(10000 log 10000)/(100 log 100)`（留 2 倍常数余量，
约 168），实测数字与比值在测试/demo 中输出。

## 4. 断点续写的状态与不变量

响应字节在组装成功时一次性物化为一个确定的字节序列 `body`（各区间先经数据源
循环读满，再按封装格式拼接）。写出端 `WriteTo(w)` 只持有一个状态：
`sent int64`（已确认被接收方接受的字节数）。每次写 `body[sent:]`，短写时用
`w.Write` 的返回值 `k` 推进 `sent += k`（k<=len 且不前进就报错，防死循环），
下一轮从同一偏移继续；调用方不断调用直到 `sent==len(body)`。

不变量：**任意时刻已进入接收方的字节恒等于 `body[:sent]`，未来只可能追加
`body[sent:]`，永不回退、永不重排**。所以无论 Write 被切成多少段、切点在哪
个字节位置，接收方拼接结果恒为 `body`，与一次写完整段逐字节相同。测试枚举切点
`1..len(body)`（每次让 Write 恰好只接受 t 个字节），逐字节比对。
`Snapshot()` 只读不写、不推进 `sent`，连查两次结果必然一致。

## 5. 边界串规避策略与失败概率上界

边界从 `crypto/rand` 生成（标准库，进程内，无网络）：固定前缀 `ont` + 24 个
随机十六进制字符（96 比特熵）。封装前在**全部待传输字节**（所有区间内容以及
每段头部）中搜索该边界串是否作为子串出现：出现则重新生成，最多重试
`BoundaryRetries` 次，仍冲突则以可判定错误拒绝（本次拒绝不改变组装器已有状
态）。空内容不会冲突。

概率上界：对任一固定字节串，它在长度 L 的内容中出现的位置至多 L 个；在某位置
出现的概率为 16^-24 = 2^-96（十六进制字符相互独立）。故一次生成的冲突概率
`p <= L*2^-96`。重试 R 次仍冲突的概率

    P <= (L*2^-96)^R

取 L 为响应总字节上限（即便取 1GiB=2^30、R=8），P <= 2^((30-96)*8)
= 2^-528，可视为零；测试不依赖随机性，而是构造内容里恰好含候选边界样子串的
资源，驱动重试并断言最终边界不与内容冲突。
