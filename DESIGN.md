# 设计推导

## 1. hunk 头零计数时的行号

GNU 规则：a 是 hunk 第一行在旧文件中的行号；空区间不占行，取「插入点前一行」
的行号，插在文件开头则取 0。新侧同理：删除整个文件时 d=0，c 取删除点前一行号，
末尾删除即文件末行号。
* `x\n`→`y\nx\n`（C=0）：新行插在开头，旧侧无行 a=0,b=0；新侧首行行号 1，头为 `@@ -0,0 +1 @@`。
* `a\nb\nc\n`→`a\nb\nX\nc\n`：X 插在旧第 2 行之后，故 a=2,b=0；新侧行号 3，头为 `@@ -2,0 +3 @@`。
* `a\n`→空：旧侧是第 1 行 `-1`；新侧为空、删除点前没有新行，c=0,d=0，头为 `@@ -1 +0,0 @@`。

Apply 时以「第 a 行之后」为插入点：a=0 表示文件开头；删除侧同理定位删除区间。

| 旧侧计数 | 头书写 | 例子 | Apply 解释 |
|---|---|---|---|
| b=0 | `-a,0`，a=前一行号，开头为 0 | `@@ -2,0 +3 @@` | 插到第 a 行之后（a=0 即开头） |
| b=1 | `-a`（省略 ,1） | `-1 +0,0` | 区间恰为第 a 行 |
| b≥2 | `-a,b` | `-3,4 +3,4` | 区间为第 a 行起共 b 行 |

## 2. hunk 合并阈值

阈值 = 2C（合并条件 g ≤ 2C，等价地 g < 2C+1）。每个 hunk 在改动两侧各带 C
行上下文；两 hunk 共需 2C 行上下文，当 g ≤ 2C 时 GNU 输出一个 hunk；
g = 2C+1 时拆成两个 hunk。例 C=1，改动间 2 行未改动：合并，中间上下文为
`[c, x, y, c]`；若阈值取 C：g=2>1 会拆，错（GNU 合并）；若按 g ≤ 2C+1
合并：g=3 时中间行无法同时充当两侧上下文仍被强行合并，多输出一行上下文，错。
故实现为 g ≤ 2C 合并，g = 2C+1 分开。

## 3. 并列最短脚本的选择

Myers 前向 V 数组每条对角线上只保留最远终点；出现并列时一律沿「删除边」先到
达者保留（计算 k 时按 k 降序，使 down/删除优先），回溯时同样优先删除。故
`a b`→`b a` 的 hunk 行顺序固定为先全部 `-` 行再全部 `+` 行（-a、-b、+b、+a），
重复运行逐字节一致。

## 4. 偏移应用与后续 hunk

在记录位置上下各 F 行内枚举候选，候选必须使整个 hunk 的上下文与删除行精确
匹配；多个候选取 |偏移| 最小者，距离相同取靠前（负偏移先于正偏移）。前一个
hunk 以偏移 off 应用后，该点之后内容整体平移 delta=(新侧行数)-(旧侧行数)，
故后续 hunk 查找中心 = 记录行号 + 前次 off，扫描窗口仍为 ±F；一旦某 hunk
在窗口内偏移为 0 精确命中，记忆偏移归零（与 GNU patch 一致）。窗口内有候选
位但内容不匹配 → 上下文不匹配；窗口内根本没有候选位 → 超出偏移范围。所有
hunk 先在副本上全部校验通过才提交，任一失败原文逐字节不变。
