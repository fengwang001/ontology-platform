# DESIGN — 统一差分器的关键规则推导

## 1. hunk 头零计数时的行号（语义第 4 条）

三个样例：`x\n`→`y\nx\n` 得 `-0,0 +1`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0 +3`；`a\n`→空 得 `-1 +0,0`。

- 样例 1 的插入点在旧文件第 1 行**之前**，若头里写的是「插入位置（下一行的 1-based 行号）」应为 `-1,0`，实际却是 `-0,0`；若写的是「插入点之前已有多少行」，恰好是 0。
- 样例 2 的插入点在旧文件第 2、3 行之间，「之前已有的行数」为 2，与 `-2,0` 一致；而「插入位置」解释会给 3，矛盾。
- 样例 3 新文件为空，`+0,0` 的 0 同样是「hunk 之前的新文件行数」。

结论：计数为 0 时，行号 = 该文件中位于插入缝隙**之前的行数**（即前一行的行号，开头插入时为 0）；计数 ≥1 时，行号 = 范围首行的 1-based 行号。`Apply` 的插入点（0-based 下标）：`count==0` 取 `a`，`count≥1` 取 `a-1`。

| 情形 | 头写法 | a 的含义 | Apply 插入点（0-based） |
|---|---|---|---|
| b=0 | `-a,0` | 缝隙前的旧行数 | `a`（在第 a 行之后插入） |
| b=1 | `-a`（省略 `,1`） | 该行的 1-based 行号 | `a-1` |
| b≥2 | `-a,b` | 首行 1-based 行号 | `a-1`，共匹配 b 行 |

（`+c,d` 一侧完全对称。）

## 2. hunk 合并阈值（语义第 5 条）

每个 hunk 在首尾改动之外各带 C 行上下文。两段改动间隔 g 行未改动行时，前 hunk 的尾部上下文覆盖 `min(g,C)` 行，后 hunk 的头部上下文覆盖 `min(g,C)` 行。两段上下文相接或重叠，当且仅当 `C + C >= g`，即 **g <= 2C 合并，g >= 2C+1 拆分**。

- 若阈值取 C：g = C+1（仍 <= 2C）时两段上下文重叠，GNU diff 输出一个 hunk，阈值 C 会错误拆分。例：C=1，改动行间隔 2 行，`diff -U1` 只产一个 hunk。
- 若阈值取 2C+1：g = 2C+1 时两段上下文之间还剩 1 行谁也覆盖不到，GNU diff 产两个 hunk，阈值 2C+1 会错误合并。例：C=1，间隔 3 行，`diff -U1` 产两个 hunk。

测试覆盖 g = 2C（合并）与 g = 2C+1（拆分）两侧。

## 3. Myers 并列最短路径的选择（语义第 3 条）

同一 D 上有多条最短路径（如 `a b`→`b a`，D=2）。本实现在贪心推进与回溯中，当 `V[k-1] == V[k+1]` 时一律选择「向右」（删除）而非「向下」（插入），即**删除优先**。效果：一个改动块内所有 `-` 行排在 `+` 行之前（`a b`→`b a` 产出 `-a`、` b`、`+a`，而非 `+b`、` a`、`-b`），与 GNU diff 的输出习惯一致，且规则固定保证确定性。

## 4. 偏移应用后后续 hunk 的查找起点（语义第 7 条）

补丁记录的行号是**原文件坐标**。前一个 hunk 应用后，若其净增删行数为 d（新侧行数 − 旧侧行数），其后所有内容在新文本中平移 d 行。因此后一个 hunk 的查找起点 = 记录位置 + 此前所有已成功 hunk 的累计 d。注意：只累计**行数变化量 d**，不累计前一个 hunk 的偏移 k——每个 hunk 的记录位置相互独立，k 只是对单个 hunk 记录误差的修正，把它传递下去会把无偏移的后续 hunk 反而拉偏。找不到匹配时区分：全文件无匹配 → 上下文不匹配；有匹配但超出 ±F → 超出偏移范围。
