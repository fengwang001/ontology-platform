# 双时间查询器设计

每条记录带两条时间轴：有效时间（事实在现实世界成立的区间）与事务时间（数据库知道该事实的区间）。查询 `AsOf(有效时刻, 事务时刻)` 回答：「在事务时刻我们以为、在有效时刻成立的值是什么」。

## 一、两条时间轴的不对称性

- 事务时间只能追加：事务区间记录的是「我们当时以为的事实」。若更正历史时直接改写已有记录的事务区间或值，等于篡改「过去某个事务时刻系统所相信的内容」，于是 `AsOf(有效=t, 事务=过去)` 无法再复现当时的答案，审计与回放全部失效。
- 有效时间可以回填：有效区间描述现实世界，允许事后补登「其实 2020 年薪资应是 120」。
- 因此更正的正确做法：把旧记录的事务区间在当前事务时刻截止（`Tx.End = now`），再追加一条 `Tx = [now, ∞)` 的新记录。旧记录不动，两个事务时刻各看到各的答案。

## 二、逻辑删除的表示

删除不抹记录，而是把「有效区间在某点终止」作为一条新事实追加：旧记录事务区间截止于删除事务时刻，再追加一条有效区间被截断为 `[Valid.Start, 删除点)`、事务区间 `[删除事务时刻, ∞)` 的记录。于是删除前的有效时刻在删除后仍可查到原值，删除点之后则查无记录。「不存在」以 `found bool` 表达，与「值为零值」天然可区分。

## 三、同键矩形不相交不变量

把每条记录看作「有效区间 × 事务区间」的矩形。同一键下任意两条记录的矩形必须互不相交，否则同一 `(有效时刻, 事务时刻)` 命中两条记录，查询结果不唯一。维持方式：

- 写入：拒绝与任何「事务区间覆盖当前事务时刻」的同键记录有效区间重叠的写入（`ErrOverlap`）。
- 更正/删除：先把被覆盖的旧记录事务区间在当前事务时刻截止，再追加新记录；新事务区间 `[now, ∞)` 与一切已截止区间（`End ≤ now`）不相交。
- 时钟单调：每次变更取严格递增的事务时刻，保证截止与追加之间无空隙重叠。
- `store` 提供非导出自检 `checkDisjoint`：遍历同键全部记录对，断言矩形两两不相交。

## 四、无穷远终点

`interval.Infinity = math.MaxInt64` 表示「至今有效」。它是普通 `int64`，与有限终点共用同一套 `<` 比较，左闭右开判定无需特判。

## 五、边界与错误

- 区间一律左闭右开 `[Start, End)`；`Start >= End` 为空，构造与写入均拒绝（`interval.ErrEmpty`）。
- 三类错误可用 `errors.Is` 区分：`interval.ErrEmpty`（空区间）、`store.ErrOverlap`（写入冲突）、`store.ErrNotFound`（更正/删除目标不存在）。
- 查询时刻早于该键最早事务时刻：返回 `found=false`，不是错误。

## 六、复杂度

记录按键分桶（`map[string][]Record`），点查只扫描该键版本链：检查数 ≤ 4 × 版本数。更正同样只触及该键版本链：触及数 ≤ 版本数 + 2（扫描一遍 + 截止一条 + 追加一条）。查询为纯函数，对快照计数，重复查询结果与检查数均一致。读写由 `sync.RWMutex` 保护。
