# DESIGN

## 推导与结论

**空输入/末尾策略。** 规范化不得凭空插入字节：若 `"" -> "\n"`，输出偏移 0 的 `\n` 无原文字节可对应，无法满足 `ToOut(ToOrig(0))==0`，且再规范化时该换行无来源；为同时满足幂等和双向不变量，所有策略都规定 `"" -> ""`。对非空输入：`Keep` 保留原末尾换行序列（仅做 EOL/空白规范化）；`One` 折叠末尾所有换行为至多一个，已有换行则保留最后一个（因此不会插入）；`Trim` 删除全部末尾空行；若删后仍有内容，再保留一个已有换行，同样不插入。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

三者输出均无 `\r`、无行尾空白；`One`/`Trim` 的输出已至多一个末尾换行，故幂等。

**被删字节的 ToOut。** 对任一输出偏移 o，`ToOrig(o)` 必须命中保留字节，且再映射回 o。把“输出游标”看作只在保留字节上前进，则被删原文位置不能获得新的、独占的输出位置；连续删除区间整体吸附到删除后的下一个输出边界（行尾空白吸附到其换行；换行序列中多出的被删换行吸附到保留换行）；若删除发生在流末且后面没有输出，则吸附到输出终点。因此 `\r\n` 的 `\r` 映射到该 `\n` 的起点；行尾空白也映射到换行起点，光标放在其中会落在行尾换行符之前；流末被删空白映射到输出末尾。ToOrig 对删除区间无定义需求，查询时吸附到最近保留位置，保证互逆。

**缓冲超限。** 选择拒绝。强制把未判定空白输出会产生两种错误：后来遇到换行时，已输出空白违反“删除行尾空白”；后来遇到普通字符虽可保留，却使结果依赖内部上限/切分，违反跨切分一致与幂等。拒绝使用带原文起始偏移的哨兵错误，已输出内容保留，后续 Write/Close 返回终态错误；因此上限只截断 API，不改变任何已接受前缀的规范化结果。

**par 拼接。** 段首无法知道前段是否留下 `\r` 或未判定空白。处理法：每个逻辑段接收 `[start,end)`，若尾部是 CR 或“空白游程（其前还可能有一个 CR）”，把该待定尾巴移到下一段；边界只回退一个 CR（CRCRLF 的首个 CR 已可确定是独立换行），故修正长度有上界。各段从 `b_k` 到 `b_{k+1}` 互不重叠覆盖全文，独立以全局 `norm` 处理，仅最后一段启用末尾策略；中间段使用 `Keep` 且不关闭尾部策略，可自然保留跨边界状态。拼接输出；span 段做坐标平移并合并相邻同类区间。平移是线性坐标变换，修正只涉及相邻段、常量长度。

## span 设计

span 保存非重叠区间 `(orig0,orig1,out0,out1)`，删除区间长度为 1、输出长度为 0，保留区间二者相等。二分定位区间，记录检查区间数。区间数随删除点增长；连续文本可合并为一个区间。
