# 结构化日志脱敏与采样设计

## 1. 数据模型
- 记录由级别、追踪 ID 和字段森林组成；字段值限定为字符串、布尔、整数、浮点、nil、切片与有序映射。
- 映射为 `map[string]any`，处理时排序，保证序列化和并发/串行结果可逐字节比较。
- 深度从最外层映射计为 1；切片不增加深度，切片内映射继续增加。默认上限 8，构造时检测，防止恶意深层输入和自引用死循环。
- 空记录合法；空字符串值合法。

## 2. 路径消歧
规则路径使用点号分层。字段名可原生含点号，为消除 `a.b` 是两级还是单键的歧义，路径采用转义语法：
- `a.b`：先查嵌套 `a` 下的 `b`；不存在嵌套时再查字面键 `a.b`。
- `a\.b`：只查当前映射里的字面键 `a.b`。
- `\\` 表示字面反斜杠。
- 路径任一层存在但不是映射：该路径不可达，不修改内部值；其字面键兜底仅在当前层尝试。
数组不作为路径层级；路径规则只命中映射字段，值规则会递归处理数组元素。

## 3. 规则编译
路径规则编译成 trie：一次深度优先记录遍历沿当前 trie 节点下降，路径匹配量为字段数加常数，不随规则数增长。
值规则编译为正则。编译期校验：同一精确字段路径不得配置多个脱敏动作；正则必须可编译。错误指出两条冲突规则。
动作包括替换、SHA-256 哈希、按 rune 截断。哈希输出十六进制；截断保留前 N 个字符并追加 `…(truncated)`。
为彻底脱敏，值规则命中的敏感字符串会记入集合；第二遍递归时，任何路径或数组位置出现相同字符串都执行同样动作。

## 4. 一致性采样
每记录独立随机采样会让同一 trace 分裂。正确决策只能是 trace ID 的纯函数：
`h=SHA256(namespace + "\x00" + traceID)`，取首 8 字节大端与阈值 `rate*2^64` 比较。
因此结果与记录内容、级别、时间、到达顺序、并发调度无关；每次决策只计算一次哈希，复杂度 O(1)。
采样率 0 全丢，1 全留，0.5 由哈希阈值决定。空或缺失 trace ID 无法代表真实链，按独立伪链 `empty-trace-id` 决策；error 仍强制保留。
级别低于 error：链保留则输出，链丢弃则丢弃。error 及以上始终输出；若其链采样结果为丢弃，附加 `trace_incomplete=true`。该标记不会出现在普通保留链上。

## 5. 并发
规则集编译后不可变。采样器无表状态，仅维护哈希次数计数器；计数器使用原子操作。
演示/批处理先固定输入顺序，再并行得到按序号放置的结果，因此集合与逐元素顺序均和串行一致。

## 6. 文件格式与恢复
文件使用显式版本和小端长度前缀，文本头不参与记录 CRC：
`ONTLOG1\n` + JSON 描述 + `\n`；记录为 4 字节体长度、JSON 体、4 字节 IEEE CRC32。
读取按边界推进。文件截短分类：
- 结束位置小于完整头：头部不完整；
- 头完整但不足 4 字节：长度前缀不完整；
- 长度声明的体或 CRC 字节不足：记录体不完整；
- 边界完整但 CRC 不等于存储值：CRC 不匹配。
逐字节截断只会产生前三类；CRC 类由翻转记录字节但保持长度的故障注入覆盖。恢复在第一个坏边界停止，返回最大可恢复前缀和可判定错误。

## 7. 错误与测试边界
四类错误分别为深度超限、循环引用、规则冲突、存储损坏，并支持 `errors.Is`。
测试覆盖空记录、空 trace、0/0.5/1 采样率、深度 1、数组嵌套映射、1000 记录/50 链乱序、同 ID 20 次重复、同值三处传播、非目标字段字节不变、100 规则/10 万记录复杂度和全部截断点。
