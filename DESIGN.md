# Quoted-Printable 设计说明

## 1. 行末空白的判定

判定基于一个 token 的前瞻：只有「紧贴硬换行 `\r\n` 或输入结束」的空格/制表符才是行末空白，
必须写成 `=20`/`=09`。前面仍有内容的空格（如 `"a  \n"` 的第一个空格）原样输出，
因为它在编码行中不紧贴行尾，严格解码器也不会报错，往返结果不变。

**软换行前的空格**：软换行插在空格之后时（编码行形如 `… =\r\n`），该空格**不算**行末空白，
**不转义**。理由：
1. 解码器删除的是完整的 `=\r\n`，删除后空格在逻辑行中间，解码结果与原始输入逐字节相同；
2. 编码行以 `=` 结尾、不是以空白结尾，不会触发「行末未转义空白」错误；
3. 最小转义：只转义「不转就会改变语义或违反约束」的字节，该空格两条都不违反。

## 2. `=XX` 不被拆行的推导

把每个字节看成原子 token：可打印字符长 1，转义序列 `=XX` 长 3。设当前行长为 n，
token 长 L，`more` 表示逻辑行在该 token 之后是否还有内容。换行条件：

- `n+L > 76`：放不下，先软换行；
- `n+L == 76 且 more`：本 token 之后若还要插软换行，`=` 将占第 77 位，无处可放，
  因此也必须在本 token **之前**软换行。

不变式：任何 token 到达时 n ≤ 75（n 达到 76 只可能发生在行末 token），
所以软换行的 `=` 总能放在第 76 位以内。样例 n=74、token=`=3D`：74+3=77>76，
先输出 `=\r\n` 再输出 `=3D`，绝不会出现 `=3\r\n`。

## 3. 往返等式（准确形式）

设 norm(x) 为：将 x 中的每个 `\n` 与 `\r\n` 都替换为 `\r\n`，其余字节（含单独的 `\r`，
它按普通字节编码成 `=0D`）保持不变，则

	Decode(Encode(x)) == norm(x)

注意：x 里的 `\n` 往返后变成 `\r\n`；单独的 `\r` 往返后仍是 `\r`。

## 4. 幂等与最小转义

编码器只在三种情况下转义：(a) `=`；(b) 33–126 之外的字节；(c) 紧贴行末的空白。
除此之外一律原样输出，因此不存在可避免的 `=XX`。对编码输出 y 有
`Encode(Decode(y)) == y`（确定性 + 最小转义）。对任意严格可解的 y，差异只可能来自
y 把可打印字符多余地转义了（如 `=41` 被规范化回 `A`；小写十六进制被规范化为大写）。

## 5. 语义与测试对照表

<!-- 待测试定稿后补齐 -->
