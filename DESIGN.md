# Quoted-Printable（RFC 2045）设计

## 推导一：行末空白

解码器会把硬换行 `\r\n` 当作数据的一部分保留；若行尾留下裸空格/TAB，
传输过程中该空白常被网关剥离，信息就丢了。因此编码时必须预判“下一 token
是硬换行或输入结束”：仅当空格/TAB 是逻辑行最后一个裸字节时改写为
`=20`/`=09`。判定依据是 lookahead 一个字节（`\n`，或其前导 `\r`，或 EOF），
不回扫行，故每个输入字节只检查一次。

## 推导二：`=XX` 不被拆开

软换行 `=\r\n` 与后续字节都占列宽。先计算下一 token 宽度（1 或 3），
再比较：`col+w > 75` 且后面还有 token 时先插软换行（保留软换行 `=` 的位置，
使含 `=` 总长 ≤76）；逻辑行最后一个 token 放宽到 `col+w > 76`。
比较在写入前完成，因此 `=3D` 要么整组落在本行，要么整组移到下一行。

## 软换行恰好插在空格之后

该空格**不算行末空白，保持裸空格**。理由：解码器删除的是软换行序列
`=\r\n` 本身，其前一个字节不被删除，空格后面还有后续数据行；它并非真正
的行末。而且空白判断只看“逻辑行最后一个裸字节”，软换行已提前结束该行。

## 往返等式

`Norm(Decode(Encode(x))) == Norm(x)`，其中 `Norm` 把每个 `\n` 以及每个
独立 `\r`（不跟 `\n` 之外——即 `\r\n` 归并为一个换行）规范化：
`Norm` 将 `\r\n` 与 `\n` 都映射为 `\r\n`，孤立 `\r` 仍是被转义的普通字节。
即 `Decode(Encode(x)) == x`，只是 x 中每个裸 `\n` 在结果里变成 `\r\n`。

## 最小转义 / 幂等

编码器只在四种情况转义：`=`、非 33–126 字节（含裸 `\r`、TAB 除外）、
行末空格/TAB、以及软换行所需的 `=`。其余可打印字符与行中空白一律原样，
因此不存在“可打印字符被多余转义”。对合法 y，`Encode(Decode(y))` 与 y
去掉所有软换行、再把残余的 `=XX`（其值可打印）展开后逐字节相同；二者差异
只可能来自 y 中对可打印字符的多余转义与可选软换行位置。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 转义与换行规范化 | `TestEscapeAndNewlines` |
| 2 行长上限 76（软换行 `=` 计入） | `TestLineLength` |
| 3 行末空白 + `=XX` 不拆 + 软换行前空格 | `TestTrailingWhitespace` / `TestAtomicEscape` |
| 4 五类可区分错误及偏移 | `TestDecodeErrors` |
| 5 任意切分点一致 | `TestSplitConsistency` |
| 6 往返（含 75/76/77、二进制、`\r`） | `TestRoundTrip` |
| 7 幂等 / 最小转义 | `TestIdempotence` |

计数器约束由 `TestCheckedCounter` 钉住：`checked ≤ 2×len(x)`。
