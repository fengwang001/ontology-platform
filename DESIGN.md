# 设计推导

## 1. 空输入 / 纯换行（末尾策略）

策略：Keep=保留原样；One=非空则恰好一个末尾换行（空→空）；Trim=删除全部末尾空行，但保留一个换行（空→空）。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等论证：规范化主体保证输出无 `\r`、无行尾空白。Keep 保留的全部是 `\n`，再规范化主体不变；One 对非空文本保证“恰一个尾换行”，该结果再应用 One 不变；空在 One 下保持空（若让空→`\n`，则 One(`\n`)=`\n` 也幂等，但会凭空制造内容且 Trim 无法对称，故选空→空）。Trim 把“内容之后的连续空行”压成一个换行；其结果内容之后恰一个换行，再 Trim 不变。

## 2. 被删字节的 ToOut

定义：`ToOut(i)` = 原文前缀 `[0,i)` 在输出中的长度；`ToOrig(o)` = 输出偏移 o 对应的原文偏移（输出位置前一个字符的源位置；o=0 时为 0）。删除区间 `[a,b)`：ToOut 在该区间内恒等于删除位置处输出偏移（左侧贴靠）；被删 `\r`（`\r\n`）也贴靠到行首/前文末尾，即换行符 `\n` 之前。光标落在被删空白/`\r` 上映射到**换行符之前**，符合编辑器“跳到行尾内容处”的直觉，且保证单调不减与 `ToOut(ToOrig(o))==o`（每个输出字符仍一一对应到保留源字节）。

## 3. 缓冲超限

选择**拒绝**：待定空白（可能是行尾空白）一旦超过 `WSLimit`，立刻返回带原文偏移的可判定错误；已输出内容与映射保留，进入终态（再写/再关返回终态错误）。强制输出会在随后出现换行时把本应删除的空白固化进输出，违反语义 2/5（不可逆）。拒绝不产生任何输出字节，故不违反 4/5/6。

## 4. par 拼接

切点问题：段尾可能是待定 `\r` 或待定空白，段头也可能是空白。解法两趟：
1. 顺序扫描，用每段首部信息解析切点——若 `seg[k]` 首字节是 `\n` 而 `seg[k-1]` 尾为 `\r`，则该 `\n` 属于上一段 CRLF：令下一段“吞掉”首字节（其起点与所有坐标 +1）。
2. 关段时只决定“是否按 EOF”：末段按 EOF（待定空白删除）；非末段恒按“后面还有内容”处理——其尾随空白是否提交取决于下一段（吞掉首字节后的）首个非空白字节；下一段整体为空白时继续向后传播。

坐标平移：每段保存原始起点 `base`、吞字节数 `drop`；段内输出长度、span 区间按 `base+drop - outOffset` 平移合并（span 只存增量段，合并即拼接）。策略（Keep/One/Trim）只在全部拼接后应用一次。修正范围有上界：每个切点只向前后各看一个字节/一串连续空白（空白整体随段传播，状态为单段 + 一个悬留白串），与 K、输入长度无关。
