# 游标分页器设计推导

## 1. 为什么游标必须是复合键 (排序值, ID)

反例（只记排序值 v）：数据集按键排序为
`a(k=5), b(k=5), c(k=5), d(k=6)`，页大小 2。

- 第一页返回 `a, b`，游标只记 `v=5`。
- 若下一页取「排序值 > v」：`c(k=5)` 被整批跳过 —— **漏**。
- 若下一页取「排序值 >= v」：`a, b` 再次出现 —— **重**。

结论：排序值不唯一时，任何只含排序值的边界都必然漏或重。
正确做法：游标记录上一页最后一行的**复合键 `(key, id)`**，
比较规则为字典序（先比 `key`，相等再比唯一 `id`），
下一页边界为该复合键的**严格大于**。因为 `id` 唯一，
严格大于不会漏掉同键行，也不会重复已返回行。

## 2. 反向翻页规则

反向不是「正向结果倒序」。规则：

- 比较方向反转：取复合键**严格小于**游标的行；
- 取数方向反转：从游标位置**向前**取满一页；
- 页内顺序仍按正向（升序）排列：取到的窗口整体翻转一次后返回。

因此反向游标指向本页**第一行**的复合键，方向标记为 backward。
对同一数据集先正向翻到第三页、再用其反向翻一页，
得到的是第二页本身（逐行相同），而非第二页的倒序。

## 3. 并发增删下的语义承诺

翻页与增删并发，不做快照隔离。承诺且仅承诺：

- 插入到**已翻过区间**的行：允许不出现（不保证快照）；
- **已返回过的行绝不再次出现**：游标边界严格推进，单调不回退；
- **未被删除的行在其复合键区间内绝不跳过**：边界是复合键
  「点」，从该点继续扫描，与区间内其它行的增删无关；
- 游标指向的行**已被删除**仍可续翻：边界只依赖复合键值，
  不依赖该行是否存在。

## 4. 效率约束

- 存储为有序切片 + 二分定位：单次翻页比较次数上界
  `4 * (pageSize + ceil(log2(n)))`，绝不从头全扫；
- 游标为定长头 + ID 的不透明字节串，编解码 O(1) 分配；
- 翻页是纯函数：无隐藏推进状态，同一游标并发使用结果一致。

## 5. 游标格式与错误分类

字节布局：`[dir:1][key:8][idLen:2][id:idLen][crc32:4]`。
解码顺序：最小长度 → 方向标记 → 字段完整性 → CRC 校验。
四类错误可用 `errors.Is` 区分：

- `ErrTruncated`：字段不完整；
- `ErrDirection`：方向标记非法；
- `ErrChecksum`：校验失败；
- `ErrWrongDirection`：跨方向复用（正向游标用于反向翻页等）。

空字节串是合法的「从头开始」，不属于错误。
