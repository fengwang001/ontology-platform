# DESIGN

统一格式差分器设计。依赖方向 lines → edit → hunk → udiff → patch，单向。

## 1. 零计数 hunk 头的行号（语义 4）

GNU 头中 `-a,b` 的 a 是「该 hunk 首行在旧文件中的 1 基行号」；当 b=0 时 hunk
不占任何旧行，a 取「插入点前一行的行号」，插入点在文件开头时为 0。
推导：样例 `x\n`→`y\nx\n` 头为 `-0,0 +1`，新行落在第 0 行之后即文件开头，
故 a=0 而不是「将要插入的位置 1」；样例 `a\nb\nc\n`→`a\nb\nX\nc\n` 头为
`-2,0 +3`，插入点在旧第 2 行之后、第 3 行之前，a=2 正是前一行行号。
`+c,d` 对称：d=0 时 c 为删除点前一行在新序列中的行号（`a\n`→空，C=3 得
`+0,0`，旧 1 行删除后它「前面的新行」为第 0 行）。Apply 解析 b=0 时把插入点
放在旧行号 a 与 a+1 之间（即在第 a 行之后拼接），d=0 同理定位删除点。

| 旧侧写法 | 含义 | Apply 的插入/定位解释 |
|---|---|---|
| `-0,0` | b=0，插入点在文件开头 | 在第 0 行后、第 1 行前插入 |
| `-a,0` (a≥1) | b=0，插入点在旧第 a 行后 | 在第 a 行与第 a+1 行之间插入 |
| `-a` | b=1（省略 `,1`） | 恰好覆盖旧第 a 行 |
| `-a,b` (b≥2) | 覆盖旧第 a..a+b-1 行 | 整段删除后替换为新侧行 |

## 2. Hunk 合并阈值（语义 5）

两改动间未改动行数为 g，各自上下文 C 行。GNU diff -U C 规则：g ≤ 2C 合并，
g ≥ 2C+1 分开。前一改动尾部可吃 C 行、后一改动头部可吃 C 行，共容纳 2C 行
上下文而不重叠；g=2C 时两侧上下文恰好相接仍为同一 hunk，多出第 2C+1 行时
两侧各自只取 C 行、中间留出未覆盖行，才拆成两个 hunk。
反证其他取值：C=1，改动间 g=2（序列 改,同,同,改）。GNU 实际输出一个 hunk。
若阈值是 C(=1)，g=2 > 1 会错误地拆开；若阈值是 2C+1(=3)，则 g=3 时也合并，
而 GNU 对 g=3 输出两个独立 hunk，错误。故阈值只能是 2C。

## 3. 并列最短脚本的选择（语义 3）

Myers 沿同一条对角线、同一 D 出现「删一行或插一行都能进入同一 snake」的并列
最短路径时，本实现固定**优先删除（down 步）再插入（right 步）**，即在 V 迭代
中对 k=-D 的对角线只能 down、k=D 只能 right，其余优先继承 down 的端点。
效果：`a b`→`b a` 输出 `-a`、` b`、`-b`、`+a`、`+b` 等价脚本中的规范排列，
hunk 内删除块整体排在对应插入块之前（先全部 `-` 再全部 `+`），不交错。脚本只
依赖输入字节与算法，不接触 map 迭代序等随机源，因此重复生成逐字节相同。

## 4. 偏移应用与后续 hunk 的平移（语义 7）

第 i 个 hunk 的名义位置为 hunk 头旧行号 A_i。在前序 hunk 未发生偏移时，在目标
文本 [A_i-F, A_i+F]（1 基、裁剪到文件范围）内按「上下文+删除行整体精确匹配」
枚举候选，取 |实际起点-A_i| 最小者，相等取靠前者；窗口内无候选→上下文不匹配
错误，窗口外若理论上仍有匹配→超出偏移范围错误。
前序 hunk 以累计位移 Δ 应用后，后续 hunk 的查找中心平移为 A_i+Δ：Δ 等于此前
每个已应用 hunk 的（实际起点-名义起点）之和。原因是后续 hunk 的名义坐标是相对
旧文件的，旧文件中位于它之前的所有内容整体被挪动了 Δ；平移后「最近候选」仍以
平移后的中心度量，保证多 hunk 连续偏移时行为与 `patch(1)` 的漂移一致。

## 5. 数据与错误

lines.Line 保留原始字节 Content 与行尾 EOL（"\n"/"\r\n"/""），Split 后 Join 逐
字节还原。edit 维护 steps 计数器（每次蛇形行比较与对角前进一步各计 1），超
MaxDistance 返回 ErrTooDifferent。四类哨兵错误：ErrFormat、ErrContext、
ErrOffset、ErrTooDifferent，彼此可用 errors.Is 区分。
