# 严格 Base64：非规范尾部推导

标准字母表把每 3 字节（24 bit）编成 4 个 6-bit 字符；字节数不是 3 的倍数时，
在末尾补零比特到 24 的倍数，并用 `=` 表示填充。

设原始尾部有 r 字节，最后一个 4 字符组的布局为：

- r=1：`c0 c1 = =`。第 1 字节填进 c0 的 6 bit 与 c1 的高 2 bit，
  c1 还剩低 4 bit 是为对齐补入的零。
- r=2：`c0 c1 c2 =`。两字节填进 c0、c1 与 c2 的高 4 bit，
  c2 还剩低 2 bit 是补入的零。
- r=3：无 `=`，没有补入比特。

统一规则：**一个组合法，当且仅当填充形状合法（`xxxx` / `xxx=` / `xx==`），
且所有"仅为对齐补入"的比特全部为 0。**

因此被检查的比特是：

- r=1：第 2 个字符（index 1）的低 4 bit；
- r=2：第 3 个字符（index 2）的低 2 bit。

`QQ==`：Q=16（010000），c1=Q=16 低 4 bit 为 0000，合法，解码得 `A`。
`QR==`：R=17（010001），c1 低 4 bit 为 0001 ≠ 0，非规范，拒绝。
`QUI=`：U=20（010100），c2=I=8（001000）低 2 bit 为 00，合法，得 `AB`；
`QUJ=`：J=9（001001）低 2 bit 为 01，拒绝。

该规则保证编解码互逆：编码时补入的比特恒为 0，解码时只接受补入比特为 0 的组，
故每个被接受的串唯一对应一个字节串，且对任意字节串 x 有
`Decode(Encode(x)) = x`、对合法 y 有 `Encode(Decode(y)) = y`。
若放行 `QR==`，则 `QQ==` 与 `QR==` 两个不同输入解码为同一字节串，编码结果
`QQ==` 无法还原原输入，唯一性被破坏。

填充只允许出现在流的最末组；组的长度恒为 4（MIME 换行只能在组间），
故流结束时缓冲不足 4 字符即长度错误。

## 语义与测试对照

<!-- 测试完成后补齐 -->
