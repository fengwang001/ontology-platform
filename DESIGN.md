# 设计推导

## 1. 空行与单列空值
选择：**空行被跳过**。词法在「字段开始」状态直接遇到 `\n`（或 `\r\n`）时只发出一个行尾事件，
组装层见到「本行 0 个字段」就丢弃。推导：若空行算记录，则 `a\n\nb\n` 与文件末尾换行、连续换行
都无法区分，末尾换行又必须不产生额外记录，两种语义直接冲突；跳过空行后无歧义。
后果：单列里**未加引号的空字段无法作为记录存在**（`,\n` 是列数错误）。单列空记录必须写成 `""`。
回写器因此在「单列、值为空、未加引号」时**必须加引号**：若裸写为空字节，读端按空行跳过，
记录整条消失，即丢数据。一般规则：凡写出后会被读端解释成「空行跳过」的单元格都必须加引号。

## 2. par 切点
切在引号字段内时，下段无法自证是否在引号内。方案：每段跑**两个假设**——H0 起点在引号外，
H1 起点在引号字段内（状态 Q）；段 0 只跑 H0。worker 从切点回退 1 个字节（段 0 除外）重放该字节：
待定状态（`""` 的第一个引号、`\r`）都只由**紧邻前一字节**决定，重放 1 字节即可重建 QQ/CR，
多处理的字节每段仅 1 个（常数）。回退字节偏移 < 切点时只走状态转移、不追加内容、不产出事件。
拼接从左到右：上段选定轨迹的退出状态为 Q/QQ ⇒ 下段选 H1，否则 H0。跨切点未闭合字段作为
「携带字段」（起始偏移、引号标记、已积累内容）并入下段轨迹的第一个单元格（或继续开放），
故 H0 以 S 起步也无妨：首个单元格的 start/值在拼接时被携带值覆盖；H0 首字节是 `"` 而上段退出
为 U（未引号字段中）时，真相是裸引号，直接发 BareQuote。所有偏移在 worker 内就用「段基址+段内
偏移」的绝对坐标记录；记录号、字段号由共用的组装器在重放时按已完成记录数/当前字段计数给出，
天然是全局坐标。每字节在两个假设中至多处理 2 次 + 2(K−1) 个回退字节。

## 3. CR 待定
引号字段内 `\r` 是内容，立刻追加，无待定。引号外见到 `\r` 进入 CR 待定：下一字节为 `\n` 则
CRLF 行尾；为其他字节（含逗号、引号）则该 `\r` 孤立，报 LoneCR，位置指 `\r`。半包续传时该状态
原样保存在解析器状态里等待下一包。par 切点落在 `\r` 之后时，下段回退重放该 `\r` 重建待定态；
流在 `\r` 处结束（Close 时仍待定）即无 `\n` 相随，判 LoneCR。

## 4. 上限的「立刻」
字段计数 = 该字段的**逻辑字符流字节数**：未引号字段每追加 1 字节计 1；引号字段内逗号/CR/LF/
普通字节各计 1，转义对 `""` 在见到第二个引号、确定逻辑上是 1 个 `"` 字符时计 1。每次计数前先
比对上限，第一个超限字节到达即返回 FieldTooLarge，不缓冲整条记录。字段数在逗号开启新字段那一刻
检查；记录数在行尾事件、记录入表前检查。错误后解析器进入终态，后续 Feed 返回同一错误。

## 5. 状态转移表
状态：S=字段开始 U=未引号字段 Q=引号字段 QQ=引号内刚见引号 CR=引号外CR待定。
动作：a=追加字符 c=完成字段并遇逗号 e=行尾（发 recordEnd） q=开始引号字段 A=追加一个引号字符。

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| S  | c→S | q→Q | →CR | e→S | a→U |
| U  | c→S | **BareQuote** | →CR | e→S | a→U |
| Q  | a→Q | →QQ | a→Q | a→Q | a→Q |
| QQ | c→S | A→Q | →CR | e→S | **AfterQuote** |
| CR | **LoneCR** | **LoneCR** | **LoneCR** | e→S | **LoneCR** |

Close：处于 Q/QQ ⇒ UnclosedQuote；处于 CR ⇒ LoneCR；处于 S 且无开放字段 ⇒ 正常结束；
处于 U 或 S 有开放字段 ⇒ 完成最后一个字段（无尾换行合法）。

## 6. 规范输入与往返
规范输入：UTF-8 字节；每条记录（含最后一条）以 `\n` 结束；引号当且仅当「必要」时出现
（值含 `,`、`"`、`\r`、`\n`，或单列空值）。回写器对 Quoted 标记为真或必要的字段加引号，
故 `Parse(Write(T))` 的值与引号标记与 T 相同；规范输入无多余引号，逐字节往返成立。
单元格偏移：[Start,End) 为原文跨度，含外围引号。
单流式解析器实例非并发安全；par 内部 K 个 goroutine 只读各自轨迹、结果按序归并。
