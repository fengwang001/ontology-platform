# 设计推导

先定义核心规范化 C(x)（不做末尾策略）：把 `\r\n`/`\r` 都变成 `\n`，删除每个换行前
紧邻的连续 ` \t`。末尾策略只处理尾部：
Keep（保留）：输出 C(x) 原样。
EnsureOne（恰好一个）：x 去空白后非空 ⇒ 去掉全部末尾换行后追加一个 `\n`；x 全空 ⇒ ""。
Trim（删空行留一换行）：去掉全部末尾换行后：原本至少有一个换行 ⇒ 追加 `\n`；否则输出截断内容。

## 空输入与换行输入
- ""：C=""。三种策略均为 ""。EnsureOne 对空输入有 "" 与 "\n" 两个候选：选 "\n" 也幂等
  （N("")="\n"，再 N 仍 "\n"），但凭空创造一行且与 Trim 不可分（Trim("")必为""）；
  选 "" 同样幂等且不造行，本文采用 ""。
- "\n"：Keep/EnsureOne/Trim 均为 "\n"。
- "\n\n"：Keep→"\n\n"；EnsureOne→"\n"；Trim→"\n"。
- "  \r\n"：C 得 "\n"（空白行保留为空行）；三策略均 "\n"。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| "" | "" | "" | "" |
| "\n" | "\n" | "\n" | "\n" |
| "\n\n" | "\n\n" | "\n" | "\n" |
| "  \r\n" | "\n" | "\n" | "\n" |

幂等：三策略输出均以 0 或 1 个 `\n` 结尾、无 `\r`、无行尾空白，再规范化逐字节不动。

## 被删字节的 ToOut
ToOrig 单调不减且 ToOut(ToOrig(o))==o。设被删区间 D=[a,b) 之后第一个存活原文位置
对应输出 o'。被删位置在输出中无点可指：若 D 内某 i 的 ToOut(i)<o'，则必须存在输出点
p<o' 使 ToOrig(p)∈D，但 D 在输出中不存在对应字节，矛盾。故整条 D 的 ToOut 恒为 o'
（删除区间塌缩到其后第一个输出位置），D 之前取旧值；ToOrig(o')=a，互逆成立，两端
为常数/逐点映射故单调不减。含义：光标落在被删行尾空白或 CRLF 的 `\r` 上时吸附到紧随
其后的 `\n`（换行符本身），不是换行之前；被删字节位于流末尾（如 "a "）时吸附到输出
终点 len。

## 缓冲超限（规则 8）
1) 强制判定为「非行尾」并输出：之后若真出现换行，这些空白本应删除却已输出 ⇒ 违反
语义 2、5（不再幂等）与 6，不可取。
2) 拒绝：返回可判定错误（带触发位置原文偏移），已输出内容与映射保留，实例进入终态，
后续 Write 返回终态错误。状态可解释为「前缀已提交，扫描停在偏移 p」；任意切分方式都在
同一逻辑位置触发同样错误（不违反 4），5/6 只约束成功结果。本文选择拒绝。

## par 拼接
每段先按「开放模式」规范化：Close 时不应用末尾策略，把扫描停在段尾的未定状态（末尾
待定 `\r`；其前未判定空白串）原样留在 hold 缓冲，既不输出也不丢弃。只有「`\r` 为最后
一字节」可能待定，未判定空白只可能是结尾一串 ` \t`，故 hold 长度 ≤ MaxPending，与输入
规模无关。从左到右 fold：把上段 hold 作前缀 P 拼到本段原始输入前重跑开放规范化，
`\r\n` 是否成对、空白是否在行尾由真实后续决定，与切点无关；新段输出/区间整体加「已提交
全局偏移」平移（orig 与 out 各加常数，形状不变）。最后一段关闭时才应用末尾策略。
修正范围有上界：每段只重看 ≤ MaxPending 个 hold 字节，段内其余部分不重算；K=1 退化为
单流。

## 复杂度与并发
映射只记录保留区间并自动合并；删除只发生在删除点（行尾空白串、`\r`、末尾策略删的
换行），区间数 = O(删除点数+1)，与输出字节数无关。ToOrig/ToOut 二分，检查区间数
≤ 2⌈log2(n+1)⌉+4，计数器 span.checked 暴露。单个 norm 实例非并发安全；par 内每段独立
实例，K 个 goroutine 并发运行后顺序 fold。
