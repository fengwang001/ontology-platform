# 设计推导

## 1. UTF-8 非法单元

统一规则：首字节给出期望长度 `n`。若第二字节不在该首字节允许区间，首字节单独成为一个非法单元，并从第二字节重新开始；否则前缀是“合法前缀”。合法前缀后再遇到非法续字节，该非法续字节及其前导合法前缀合成一个非法单元，长度等于已经看到的期望序列字节数，下一字节重新开始。流结束时未完成的合法前缀合成一个非法单元；因此 `E2 82` 截断只替换一次。

`E0 80 80` 中 `80` 对 `E0` 非法，故 `E0` 一个单元，后两个裸续字节各一个单元。`F0 90 80 41` 中第二字节合法、第三字节非法，前三字节是一个单元，`41` 重新解析。该规则也解释 `ED A0 80`、`C0 AF`、`F4 90 80 80`。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 | 合法前缀后非法/截断 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 1 | 已见 2 字节 |
| `E0` | `A0..BF` | 1 | 已见 2/3 字节 |
| `E1..EC` | `80..BF` | 1 | 已见 2/3 字节 |
| `ED` | `80..9F` | 1 | 已见 2/3 字节 |
| `EE..EF` | `80..BF` | 1 | 已见 2/3 字节 |
| `F0` | `90..BF` | 1 | 已见 2/3/4 字节 |
| `F1..F3` | `80..BF` | 1 | 已见 2/3/4 字节 |
| `F4` | `80..8F` | 1 | 已见 2/3/4 字节 |
| `80..BF` | 无（裸续字节） | 1 | 不适用 |
| `C0 C1 F5..FF` | 无（非法首字节） | 1 | 不适用 |

## 2. 流式缓存与立即判定

UTF-8 最长 4 字节，看到首字节后最多再缓存 3 字节；一旦第二字节非法，立即只判首字节并把当前字节放回解析，不等待流结束。合法前缀在第三、四字节非法时立即判定整个已见前缀；只有“仍可能由未来续字节补全”的 1..3 字节合法前缀能留到 `Close`。因此硬上限为 3。UTF-16 完整码元需 2 字节，代理对需两个码元；物理待判定字节仍不超过 2（高代理存在状态中，不占待解析缓存），实现统一使用 3 的上限。

## 3. par 切点对齐

每段不能从任意字节盲启：盲启会把切点前的 `C3` 丢掉，并把本属于同一字符的 `80` 当裸续字节。对切点 `c`，只回看最多 3 字节，用与单线程相同的有限前缀自动机求“第一个不小于 `c` 的标量/非法单元边界” `b`：

- UTF-8 从可能的首字节跑到 `c`；若 `c` 落在完整单元后，`b=c`；若落在单元内部，`b` 为该单元结束位置。
- 第二字节非法的首字节在其自身后结束；合法前缀后的非法单元在已吞掉字节后结束。
- UTF-16 以偶字节码元边界和代理对结束位置对齐，回看常数仍为 3。

前段处理 `[0,b)`，后段从 `b` 开始；因此 `C3|80 80` 中 `b=c+1`，前段拥有 `C3 80`，后段只拥有一个裸 `80`，非法单元数为 1，不是 2。各段范围互不重叠且覆盖全输入，错误偏移加段起点；BOM 只可能由起点为 0 的段见到。有限回看保证检查量不超过 `n+8C`。

## 4. 上限与统计

`Write` 先只追加字节到缓存或推进状态机；一个完整标量编码到输出前先检查剩余容量，放不下就不写该标量、不吞掉它的任何输入。返回值只包含此前已经完整输出的标量（及已完整处理的非法单元/BOM）所对应的输入字节；仍在切分缓存中的字节不算已消费。调用方把 `p[n:]` 交给新实例即可续传，故输出在标量边界拼接。检查计数随状态机每个判定显式增加；每个输入字节进入状态机一次，BOM/边界回看是常数，不随切分次数重扫。
