# 推导一：压缩与 Write 切法无关（语义 3/4）
编码器维护窗口 w（已决历史）与未决缓冲 pend。不变量：
- 位置 i 的决策（字面量或某条回指）只在“该决策之后还有 1 个新字节”时落定：
  即循环处理 pend 中除最后 1 字节外的每个位置，在 w+pend 视图里做匹配。
- 匹配最长只允许延到 pend 末尾；若匹配正好顶到末尾，它可能随新 Write 变长，
  因此不输出任何记录，整体返回等待；下一字节到来后从同一起点重新匹配。
- 失配或匹配结束（末端仍有字节确认其长度）才输出字面量/回指，
  字面量与哈希插入都按“已决字节”推进，故决策序列只取决于字节流本身。
Flush 强制把 pend 清空（顶到末尾的匹配此刻长度已确定，必须结算），
输出刷新标记；无新增输入时重复 Flush 不产字节。历史窗口不被 Flush 清空，
故后续匹配仍可越过 Flush 边界。

# 推导二：重叠回指的复制（语义 2）
距离 d < 长度 L 时目标与源区间重叠：一次性块复制（从旧数据取源）会把末尾
未定义字节当成源。正确语义是 out[k]=out[k-d]，按 k 递增逐字节，源字节可能
就是本次复制刚写出的字节。实现定长展开：d>=4 时每次 4 字节、按索引 j=0..3
逐个赋值（Go 数组元素赋值按索引顺序求值，重叠安全），d<4 时逐字节。
语义与逐字节循环完全等价，均摊操作数约 1/4，4MB 全同字节下耗时可忽略。

# 推导三：两种“距离过大”为何可区分（语义 7）
- d>窗口容量 W：格式本身非法（引用不可能存在的历史），即使原文很短也必须
  按格式错误拒绝；压缩器任何路径都不会发出，因为匹配器只在窗口内取候选。
- d<=W 但 d>已输出字节数：格式合法但此刻没有那么多历史，属截断/伪造流。
并行块用“上一块末尾 W 字节”作预置字典：它只存在于压缩端（块独立压缩、记录
原样拼接），流本身不携带它；流式解码器的“已输出”已自然包含这些字节（它们
位于上一块输出记录中），故解码端两种判定的含义不变。

# 推导四：并行块的确定性（语义 9）
块 i 是状态纯函数：输入 = 本块原文 + 上一块原文末尾 W 字节（预置字典）。
块结果与其他块、其他块的压缩结果、调度顺序均无关；边界由固定 blockSize
唯一切定。各块用私有匹配器/窗口压缩，记录直接拼接：字面量/回指记录自描述，
块间不需要刷新标记；最后统一写一个流尾。故 workers=1/2/4/8 输出逐字节相同。

# 流格式（整数均为 ULEB128；回指 distance、length >= 1）
| 记录 | 布局 |
|---|---|
| 流头 | magic "ONTLZ" + version=1 |
| 字面量 | tag=0, len + len 个原始字节 |
| 回指 | tag=1, distance, length |
| 刷新 | tag=2（无负载） |
| 流尾 | tag=3, origLen, checksum(FNV-1a 64)；其后不允许任何字节 |

varint 上限 10 字节且不得溢出 uint64。
