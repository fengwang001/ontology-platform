# 并行任务图调度器设计

## 1. 包结构
- `graph`：加边（幂等）、依赖不存在校验、环检测（含自环）、拓扑分层。
- `sched`：信号量限并发；就绪用最小堆按 ID 升序；维护峰值并发与就绪判定计数。
- `fail`：状态枚举、两种模式、错误哨兵（环/panic 等），状态判定规则集中于此。
- `exec`：单 dispatcher + 完成 channel 驱动状态机，持有图/调度器/任务表。
- `report`：按 ID 字典序生成确定性报告文本。

## 2. 四类最终状态及推导
1. 成功：任务函数返回 nil 且其完成事件在"终态锁定"之前被处理。成功一经记录不可改。
2. 失败：真正执行且返回错误（含 panic 被 recover 包装）。报告保留原始错误。
3. 跳过：从未开始。某祖先失败使其永不可调度；原因指向**最早**失败祖先
   （分层最小、同层 ID 最小）。A→B→C 中 A 失败，则 C 的原因是 A 而非 B。
4. 取消：已经开始执行，因旁支失败触发 ctx 取消。与跳过的区分依据是
   "是否有过启动记录"：有启动记录→取消；无→跳过。

## 3. 两种模式
快速失败（FailFast）：首个失败 F 发生即取消 ctx；
- 在跑任务：ctx 取消后结束 → 取消；
- 尚未启动的全部任务（含无关分支）→ 跳过，原因：祖先失败的指向最早失败祖先，
  无关任务指向 F；
尽力而为（BestEffort）：不取消；依赖（含传递依赖）失败任务的任务跳过，
其余分支继续跑完，因此不存在"取消"状态。

## 4. 调度与复杂度
- dispatcher 单点驱动：事件只有"任务完成"与"入队重判"两种，每入度减为 0
  入堆一次，就绪判定（Pop/尝试获取令牌）总次数 = N + 完成相关重检次数，
  有界于 O(N+E)，断言 ≤ 4(N+E)；禁止全图扫描。
- 并发：容量 C 的带缓冲 channel 作信号量；峰值计数在获取/释放处更新，非导出。

## 5. 取消与迟到结果
失败锁定终态：dispatcher 在 F 失败后立刻把所有"在跑"条目标为取消，
ctx 取消；这些任务之后写回的完成事件见终态非空即丢弃，终态不再改变。
panic：任务 goroutine 内 recover，包装为 ErrPanic（含原始信息）上报为失败。

## 6. 确定性
就绪堆按 ID 升序；报告按 ID 字典序；模式/图/任务函数确定则报告逐字节相同，
与完成先后、加边顺序、随机延迟无关。

## 7. demo 判定项（11）
1 C 跳过原因指向 A；2 慢任务为取消非跳过；3 两模式状态分布差异；
4 并发峰值≤8；5 就绪判定次数≤4(N+E)；6 打乱边序 20 次报告一致；
7 环路径逐边真实闭合；8 panic 被捕获；9 多任务同时失败全部记录；
10 取消后写回被丢弃；11 goroutine 回基线。

## 8. 边界
空图返回空报告；单任务；1000 层链（迭代入度算法，无递归）；重复边幂等；
依赖不存在返回 ErrMissingNode；并发度 1 退化为串行，报告与并行一致。
