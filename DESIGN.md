# 严格 Base64 规范尾部推导

标准 Base64 把每 3 字节（24 bit）切成 4 个 6 比特字符。

- 3 字节：4 个字符均承载信息，无填充。
- 2 字节：共 16 bit；前两个字符承载 12 bit，第三个字符只承载高 4 bit，
  因此第三个字符最低 2 bit 必须为 `00`，第四个字符为 `=`。
- 1 字节：共 8 bit；第一个字符承载高 6 bit，第二个字符只承载剩余 2 bit，
  这 2 bit 位于第二个字符的最高 2 位；因此第二个字符最低 4 bit 必须为 `0000`，
  后两个字符为 `==`。

所以判定非规范尾部时检查：

- `xx==`：检查第二个字符的低 4 位（字符值 `& 0x0f`）。
- `xxx=`：检查第三个字符的低 2 位（字符值 `& 0x03`）。

`QR==` 中 `Q=16`、`R=17`，第二个字符低 4 位是 `0001`，不为零。
宽松解码会把这些尾部位丢弃并得到 `A`；严格模式必须拒绝，因为 `QQ==` 与
`QR==` 会解码成同一字节串，破坏“任意字节串的编码结果唯一”。
规范形式要求解码后重新编码得到完全相同的四字符组：尾部位必须全部为零。

同理，`QUI=` 中 `I=8`，低 2 位为零，合法；`QUJ=` 中 `J=9`，
低 2 位为一，必须拒绝。填充还必须具有精确形状（`xx==` 或 `xxx=`），
且带填充的组只能是整个流的最后一组。

## 测试对照表

（实现完成后补入。）
