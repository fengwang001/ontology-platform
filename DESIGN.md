# DESIGN：有预算的确定性 BFS 遍历器

## 1. 预算单位：为什么是「访问的节点数」

- 预算 `b` 计量从队头**弹出并记录为已访问**的节点数；结果序列长度 = `min(b, 可达节点数)`。
- 若改用「考察过的边数」做单位，截断点会随图的**存储形态**漂移：同一逻辑图只要把一条邻接关系存成
  重复边、或在邻接表里提前插入一条连向已访问节点的边，考察边计数就提前耗尽，访问到的节点集合改变。
  节点数是图的逻辑语义量，与边去重方式、邻接表内部排列无关，因此同图同预算必截在同一节点。

## 2. 截断确定性：必须固定的三处顺序

1. **出边顺序**：每个节点的出边按目标 ID 字典序展开（graph 负责排序去重，不暴露 map 迭代序）。
2. **同层入队顺序**：BFS 用 FIFO 队列；同层节点严格按「被发现的先后」入队，即按其前驱在本层的
   先后、再按出边字典序，全序确定。
3. **补队时机**：不一次性展开。仅当待访问队列已空时，才从「展开游标」继续展开下一个已访问节点，
   一次展开其一段出边（块大小 = 剩余预算）。这保证标准 BFS 层次序（上一层全部展开完才见下一层），
   同时游标位置确定，与预算切分无关。

不固定以上任一处（例如直接 `range map`），两次遍历可能截在不同节点，破坏可续传等价性。

## 3. 续点必须包含什么

BFS 的状态不是「最后一个节点」，而是整组中间量。只记最后节点会丢掉同层尚未访问的兄弟节点，
续传后无法恢复 FIFO 次序。续点（resume.State）包含：

- `frontier`：待访问的 FIFO 队列（未访问但已发现），按第 2 节确定顺序排列；
- `seen`：已访问/已发现集合（去重，环、自环、重复边靠它）；
- `cursor`：已访问但出边尚未展开完的节点；`offset` 为其下一条待考察出边下标；
- `tail`：游标之后、其余已访问但完全未展开的节点（决定展开先后）；
- `done`：整图可达部分已遍历完的标记。

`walk` 每段结束都把上述五部分冻结进续点；下一段从同一状态继续，故任一切分点
`a` 都有 `walk(a) ++ resume(walk(a), n-a) == walk(n)`。

## 4. 资源界

- 惰性展开：任何时刻入队的未访问节点不超过本段预算；星形图（中心连一万叶子）预算 `b` 时，
  先访问中心，再分批入队叶子，峰值队列 ≤ `b`（实现上界断言用 `4*b`），不会把一万出边一次入队。
- 每条边最多考察一次：节点一旦完整展开即离开 `cursor/tail`，不会再展开；重复边只考察不重复入队。

## 5. 续点编码与错误分类

- 编码：各字段长度前缀 + 大端序，队列/集合按确定顺序落盘（seen 按加入序），末尾附载荷 SHA-256
  截断校验和。解码分类顺序固定为：**结构（长度前缀/字段不完整）→ 引用（节点必须存在于图）→ 校验和**。
- 对合法续点逐比特翻转：结构损坏报 `ErrCorrupt`/`ErrIncomplete`；结构仍成立但 ID 指向不存在节点报
  `ErrMissingNode`（续传间删节点也走此项并指名节点）；其余载荷改动一律校验失败，绝无误接受。
- `done` 续点再续传返回空序列、`done` 续点与 nil 错误（幂等完成，不是错误）。

## 6. 并发

`walk.Walk` 为纯函数：输入图与不可变续点，输出新序列与新续点，计数器是每调用独立的 Stats，
无包级可变状态；graph 只读且无加锁之外的隐藏状态，故并发续传结果逐元素相同，`-race` 干净。
