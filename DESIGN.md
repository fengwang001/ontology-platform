# DESIGN

## 1. 零计数 hunk 头的行号规则

旧侧头 `a,b` 中 a 是 1 基的旧文件起始行号；统一规则：a = 旧 hunk 首行的行号，hunk 为空(b=0)时取“插入点前一行”的行号，插入点在文件开头则为 0。
- 空文件头部插入：插入点前无行，故 `-0,0` 而新侧起始为 `+1`。
- 在旧第 2、3 行之间插入：前一行是 2，故 `-2,0`；新插入行前有 2 行，故 `+3`。
- 删除旧第 1 行且新文件为空：新侧空、插入点在开头，故 `+0,0`；旧侧首行为 1，故 `-1`。
Apply 解释：b=0 时删除区间为空，锚点是“第 a 行之后”（a=0 即文件开头）；d=0 时同理锚点为“第 c 行之后”。

| 旧计数 | 书写 | a 的含义 | Apply 操作点 |
|---|---|---|---|
| b=0 | `-a,0`（0 不可省） | 插入点前一行行号，开头为 0 | 旧第 a 行之后，a=0 为开头 |
| b=1 | `-a`（省略 ,1） | 唯一涉及行的行号 | 第 a 行 |
| b>=2 | `-a,b` | hunk 首行行号 | 第 a 行起共 b 行 |

## 2. hunk 合并阈值

阈值是 **2C**：两改动相隔 g 个未改动行，`g <= 2C` 合并，`g > 2C` 分开。前 hunk 尾上下文 C 行、后 hunk 头上下文 C 行，上下文能相接当且仅当 g <= 2C。
C=1、g=2 时合并成一个含全部 2 个间隔行的 hunk，与 `diff -U1` 一致；阈值取 C 会在 g=2 误拆（GNU 不拆）；取 2C+1 会在 g=3 误并，而 GNU 此时输出两个 hunk、中间留 1 行不属于任何 hunk。

## 3. 最短脚本并列选择

Myers 在对角线更新 k 时固定父对角线顺序：k==-D 只能向下(插入)，k==D 只能向右(删除)，其余并列一律取向右即**优先删除**。
故 `a b → b a` 的 hunk 内顺序为 `-a`、` b`、`+a`（删除整体靠前），而非优先插入的 `+b`、` a`、`-b`。选择写死保证确定性。

## 4. 偏移后后续 hunk 的查找起点

要平移。前 hunk 记录位置 p、实际命中 q，累计 `delta += q-p`；后 hunk 查找中心 = 记录位置 + delta（hunk 自身删插造成的行号差已在记录行号中，delta 只承载模糊命中位移）。
错位通常来自上下文上方同一处增删导致的整体位移，后续 hunk 受同一位移；平移后“最近候选”才对应真实位置，同时每个 hunk 仍在 ±F 内独立判定，不放大单 hunk 容差。

## 5. 行模型与包结构

行保留原始行尾（`\n`/`\r\n`/无），相等性含行尾，故“仅末尾换行变化”表现为旧无尾行替换为有尾行（或反之），配 No-newline 标记。
依赖单向：lines ← edit ← hunk ← udiff ← patch。计数器统计对角前进的逐行 snake 比较次数；Myers 每 d 轮扫描 O(d) 条对角线、每条至多走 N+M 步，总量 O((N+M)(D+1))，实测常数 ≤4。
