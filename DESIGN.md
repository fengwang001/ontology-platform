# 设计推导

## 末尾换行

映射坐标均为左闭右开，并同时包含终点。若规范化新增一个原文不存在的字节，输出新增字节和输出终点都会自然对应原文终点；于是 `ToOut(ToOrig(o))=o` 无法同时对新增字节和输出终点成立。长度增加也会使非终点源字节失去可插入的唯一位置。因此本实现严格保证偏移互逆，`EnsureOne` 的含义是“已有末尾换行时恰好保留一个；没有换行不凭空新增”。`TrimEmpty` 则删除所有末尾空行，并保留最后一个内容行已有的换行；空输入仍为空。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三种策略的输出都不含 CR 与行尾空白；再次规范化不会改变末尾换行数量，故幂等。

## 被删字节映射

映射表只记录“连续保留区间”和删除造成的坐标差。对删除区间 `[a,b)`：

- 后面还有保留字节时，所有 `i∈[a,b]` 映射到下一个保留字节的输出偏移；
- 删除位于流末尾时，映射到当前输出终点。

所以 CRLF 中的 CR 映射到规范 LF 的偏移；行尾空格/Tab 也映射到 LF 前。这样编辑器光标落在被删空白中时，会定位到换行之前的行尾，而不是下一行。LF 自身仍映射 LF，保证输出每个偏移可由原文唯一定位。

## 缓冲超限

选择拒绝，而不是强制输出未判定空白。强制输出若随后看到换行，会永久留下行尾空白，违反无行尾空白和幂等；若再回退又不是流式接口可撤销的输出。空白计数达到上限时返回带首个空白原文偏移的终态错误，已输出内容保留，后续 Write/Close 返回终态错误。该选择不改变任何成功路径的字节或映射，因此不破坏切分一致性、幂等与映射互逆。

## 并行拼接

每个分段独立产生稳定输出、映射、末尾 CR 标记和末尾未判定空白。拼接 A、B 时只检查 A 的挂起后缀：CR 可能与 B 首字节合成 LF；未判定空白可能延续到 B，并由 B 中第一个换行或非空白字节决定。修正窗口至多为“一个 CR 或一串未判定空白”加 B 的 CRLF/空白起始，其大小受缓冲上界和 2 字节 CR LF 约束。

段内映射先记录局部原始/输出坐标；拼接时给所有坐标加上分段起始偏移和已提交全局输出长度。被替换的局部挂起区间从映射表切除，再用同一状态机在修正窗口上重放，得到与单段完全相同的新提交。全局末尾只在最后一个分段应用配置的末尾策略。重复切分点因重放规则确定而得到逐位相同结果。
