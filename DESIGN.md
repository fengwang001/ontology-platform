# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 必须拒绝的输入（逐条推导）

1. 显式次数 1（如 `1a`）：`Decode("1a")="a"`，而 `Encode("a")="a"`，不等于 `1a`。
2. 次数 0（如 `0a`）：游程长度必 ≥1，`0a` 无法表示任何串；若输出空串，`Encode("")=""` 也不等于 `0a`。
3. 前导零（如 `01a`、`00a`）：规范十进制无符号无前导零；`01a` 若表示 a，则其规范形是 `a`，回编不一致。
4. 相邻游程符号相同（如 `2a3a`）：合并后是 5 个 a，回编为 `5a != "2a3a"`。
   流式下该错误在看到后一游程符号时才能判定。
5. `\` 后非数字非 `\`（如 `\x`）：转义只允许这两类 ASCII；`\x` 无法对应任何符号。
6. 末尾孤立 `\`：后面没有符号字节，游程不完整。
7. 末尾只有次数没有符号（如 `12`）：游程 = 次数+符号，缺符号不完整。
8. 非法 UTF-8：原始符号按 UTF-8 码点写出；残缺序列、续字节错位、超长编码（`C0/80` 等）
   及代理码点 U+D800–U+DFFF（`ED A0 80` 等）都不构成码点，必须拒绝。

## 大次数取舍

选择**拒绝超过 `runs.MaxCount`（= `math.MaxInt64`）的次数**。理由：Go 的 int 为 64 位，
任何实际存在的字符串长度都不超过 MaxInt64，故接受更大值不可能通过往返；解析时逐位
`n = 10n+d` 并做溢出预检，绝不静默回绕。输出总字节上限可配置（`WithMaxOutput`，
默认 64 MiB），逐游程在写入前检查，超限返回 `ErrOutputLimit`，绝不按次数预分配内存。

## 语义-测试对照表

<!-- TABLE -->
