# POSIX 风格分词器设计

## 规则 2：双引号内反斜杠何时是转义符（推导）

POSIX：双引号内反斜杠仅在后随 `$`、`` ` ``、`"`、`\`、`<换行>` 时保留其转义语义，
其余字符前反斜杠无特殊功能，必须原样保留。
因此需要两个不同状态：`DQ`（双引号普通态）与 `DQS`（刚消费掉 `\` 的待定态）。
- `DQ` 见到 `\`：进入 `DQS`，该反斜杠本身**先不输出**（去留取决于下一字节）。
- `DQS` 见到五字符之一：只输出该字符本身，`\` 被吞掉（`<换行>` 两字符全吞，即续行）。
- `DQS` 见到其他字节：先补输出 `\`，再输出该字节，回到 `DQ`。
无引号态同理设 `ES`，但 `ES` 中**任意**下一字节都被转义（吞 `\`；换行连同吞掉）。
单引号态 `SQ` 中 `\` 无任何特殊性，直接当字面量输出，所以不需要待定态。

## 规则 4：空词（推导）

词的存在条件不是「输出过非空内容」，而是「进入过一次词」。
无引号空白只做分隔、不产生词；而引号一旦开启（即使为空）就表示一个词已开始，
因此 `''`、`""` 各贡献一个（可能为空的）词。
结论：`""''` 中两个空引号相邻拼接 → **1 个空词**；
`a""` 是普通部分与空引号拼接 → **1 个词 `a`**；
`\<换行>` 是续行，两个字节全部被吞、什么也没进入词，之后也无内容 → **0 个词**。
实现上以 `inWord` 标志记录当前词是否已开始；分隔符或 Close 时按标志落词。

## 状态转移表（S=空格/制表/换行类空白；Q=`'`；D=`"`；B=`\`；N=换行；O=其他）

| 状态 | S | Q | D | B | N（在B后见下） | O |
|---|---|---|---|---|---|---|
| OUT 词外 | 留 OUT | →SQ | →DQ | →ES | （空白）留 OUT | 出字面量→IN |
| IN 词内 | 落词→OUT | →SQ | →DQ | →ES | — | 出字面量 |
| SQ 单引号 | 字面量 | →回原态 | 字面量 | 字面量 | 字面量 | 字面量 |
| DQ 双引号 | 字面量 | 字面量 | →回原态 | →DQS（吞\） | — | 字面量 |
| DQS 双引待定 | 出 `\`+字节→DQ | 同左 | 出 `"`→DQ | 出 `\`→DQ | （见N行） | 出`\`+字节→DQ |
| DQS 遇 N | 两字符全吞（续行）→DQ ||||||
| ES 无引转义 | 出该字节→IN | 出该字节→IN | 出该字节→IN | （不会出现） | 全吞→回原态 | 出该字节→IN |

## 语义 ↔ 测试对照表

| 语义 | 钉住它的测试函数 |
|---|---|
| 1 单引号字面量（含 `\`） | TestSingleQuote |
| 2 双引号五种转义/保留/续行 | TestDoubleQuote |
| 3 无引号转义与续行 | TestUnquotedEscape |
| 4 拼接与空词 | TestJoiningAndEmptyWords |
| 5 三类可区分错误及偏移 | TestErrors |
| 6 跨切分点一致 | TestChunking |
| 7 往返 Quote/Split | TestQuoteRoundTrip |
| 计数器 == 字节数 | TestByteCounter |
