# DESIGN — 统一格式差分器的四处推导

## 1. hunk 头零计数时的行号（第二节第 4 条）

样例（均已用 GNU diff 复核）：

- 旧 `x\n` → 新 `y\nx\n`（C=0）：`@@ -0,0 +1 @@`，插入发生在旧文件第 1 行**之前**。
- 旧 `a\nb\nc\n` → 新 `a\nb\nX\nc\n`（C=0）：`@@ -2,0 +3 @@`，插入发生在旧文件第 2 行**之后**。
- 旧 `a\n` → 空（C=3）：`@@ -1 +0,0 @@`，新文件为空，删除后内容落在第 1 行**之前**。

统一规则：**计数为 0 时，头部数字是"紧邻的前一行的行号"**（文件开头没有前一行，记 0），
而不是"将要插入的位置"。若把它解释成插入位置，样例 1 会要求写 `-1,0`（插到第 1 行处），
但 GNU 写 `-0,0`；样例 2 若写插入位置应是 `-3,0`，GNU 写 `-2,0`。两个样例共同排除了
"插入位置"解释，且彼此一致地指向"前一行行号"。`Apply` 解析 `-a,0` 时把插入点放在
0 基索引 `a` 处（即第 a 行之后；a=0 即文件最前）。计数 ≥1 时数字是该段首行的 1 基行号。

| 情形 | 头部写法 | Apply 的解释 |
| --- | --- | --- |
| b=0 | `-a,0`，a=前一行行号（开头为 0） | 在 0 基索引 a 处插入，不匹配/删除任何行 |
| b=1 | `-a`（省略 `,1`） | 从 1 基第 a 行起匹配/替换 1 行 |
| b≥2 | `-a,b` | 从 1 基第 a 行起匹配/替换 b 行 |

## 2. hunk 合并阈值（第二节第 5 条）

结论：**两段改动间未改动行数 g ≤ 2C 时合并，g ≥ 2C+1 时拆分**（实测：C=1 时 g=2 合、
g=3 分；C=3 时 g=6 合、g=7 分）。

- 阈值若取 C：g=C+1 时两段各自带 C 行上下文，前 hunk 的尾部上下文与后 hunk 的头部
  上下文合计 2C > g，必然重叠，同一源行要出现在两个 hunk 里，补丁无法自洽，故 C 太小。
- 阈值若取 2C+1：g=2C 时前 hunk 尾上下文（C 行）与后 hunk 头上下文（C 行）恰好相接、
  中间不留任何未覆盖行，两个 hunk 紧邻无间隔；GNU 此时输出单个合并 hunk
  （实测 C=1、g=2 输出 `@@ -1,4 +1,4 @@` 一个 hunk），故 2C+1 太大。

## 3. Myers 并列最短路径的选择（第二节第 3 条）

同一 D 上有多条最短路径时，**优先删除**（k 线上 `V[k-1] >= V[k+1]` 时向右走，即先删后插）。
效果：一个变更块内所有 `-` 行排在 `+` 行之前。例：`a b` → `b a` 的最短脚本取
`-a`、` b`、`+a`（删 a、留 b、插 a），与 GNU diff 输出一致；若优先插入则得到
`+b`、` a`、`-b`，hunk 内行序不同。该规则固定，保证同一输入 100 次生成逐字节相同。

## 4. 偏移应用后后续 hunk 的查找起点（第三节第 4 条）

**要跟着平移。** 第 i 个 hunk 的期望位置 = 记录的旧侧起点 + 之前各 hunk 的净行数变化
（Σ(插入−删除)）+ 前一个 hunk 实际应用位置相对其期望位置的偏移 k。原因：目标文本若在
补丁记录位置之前多出/少了若干行，这份位移对其后所有内容同样成立，前一个 hunk 实际找到
的偏移 k 正是对这份位移的测量；不传递则后续 hunk 会以错误起点为中心浪费 ±F 的查找预算，
甚至在本可成功时误判失败。GNU patch 同样把已应用 hunk 的偏移累积进后续 hunk 的定位。
