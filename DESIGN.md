# 带背压的流式聚合管线：设计推导

## 1. 链路与阶段
source(可注入/限速/报错) → parse(坏记录计数不中断) → agg(分组求和,分组数硬上限)
→ sink(临时文件+原子改名+校验和)；ckpt 独立负责快照写出/恢复。
每两个阶段之间是**有界 channel**；另有一个容量为 C0=各级容量之和的全局在途信号量。
记录入第一条队列前 acquire、sink 处理完 release。因此全局在途数恒 ≤ C0，
source 在通道满/信号量满时必然阻塞——背压直接传导回上游。

## 2. 核心推导：「已消费位置」记到哪一条
记录生命周期：`read → queued(parse) → queued(agg) → agg更新内存 → queued(sink) → sunk`。

- **记 source 读取位置**：崩溃时 read 但未 sunk 的在途记录不会被重放 → 丢失。
- **记 sink 落盘位置**：队列里可能压着位置更小的记录还没被 agg 处理；
  从该位置重放会把「位置更大、已 sunk」的记录再聚合一遍 → 重复。
- 关键事实：单消费者 FIFO 下，记录到达 sink，意味着位置 ≤ 它的记录**都已离开
  上游所有在途集合并已作用于 agg 内存**。但取「任意记录到达 sink」的瞬间，
  仍有位置更小的记录停在 sink 自己的队列里尚未 sunk——不能据此 finalize。

**结论（排空屏障模型）**：在 source 读到批次边界时注入 barrier，barrier 随记录
FIFO 通过每一级。barrier 到达 agg 时 agg 状态即「位置 ≤ p 全部聚合完毕」的一致
快照（此刻上游在途集合为空）；barrier 到达 sink 且 sink 排空时，p 之前的结果已
全部 durable。检查点 CP = (pos=p, agg快照, sink输出摘要)，只在此时写出。
- 崩溃恢复：源 seek 到 CP.pos 重放；agg 从 CP 的中间聚合继续，不丢已计入组的值；
  sink 回滚到 CP 的输出摘要。CP 之后落盘的任何字节都在恢复时被裁掉，
  因此 CP 之后、崩溃之前 sunk 的记录会重放并重新聚合重写——不丢也不重。
- 正常结束等价于最后一道 barrier 排空：此时一次性把确定性的最终聚合
  （组键排序，行格式 `key\tsum\n`，尾附 sha256）写临时文件再 rename。

## 3. 三个崩溃时刻的语义
1. source 读完、队列非空崩溃：CP.pos 之前已 durable，之后全在途 → 重放补齐。
2. 聚合中途崩溃：agg 内存丢失，但有上一 CP 快照 → 从 CP.pos 重放。
3. sink 写临时文件未改名崩溃：目录里只可能残留 `*.tmp`；有效输出只有 rename
   之后的文件。恢复时删除所有 `*.tmp`，从 CP 的输出摘要重建输出文件。

## 4. ckpt 格式与损坏回退
每次写 `ckpt-N.tmp`(内容 json+sha256 校验行) 后 rename 覆盖两个滚动槽
`ckpt.1`/`ckpt.2`：新写 1，旧 1 → 2。恢复依次尝试 1、2，校验和不符或解析失败
则尝试下一槽，并在报告里置 fellBack=true；两槽全坏则从空状态起步。

## 5. sink 原子性
有效输出文件末尾自带 sha256 行；`*.tmp` 一律视为未完成。恢复时逐字节校验：
凡不是「完整格式且校验和匹配」的临时文件一律删除。

## 6. 优雅停止
ctx 取消：source 停止读数，各阶段排空在途记录后关闭下游；Stop 幂等
（sync.Once + 关闭标记）。停止前/后用 runtime.NumGoroutine 比对回到基线。

## 7. 边界与资源
零/单记录、全同组、cap=1、空串键合法、缺分隔符=坏记录、sink/source 立即报错、
未启动即 Stop；分组数超 MaxGroups 返回哨兵错误并整体停止；source 阻塞次数由
注入源计数，测试断言 >0。演示按比例缩小数据量（5k 条、sink 每条 1ms 触发阻塞，
背压机制与 5 万条测试完全一致）。
