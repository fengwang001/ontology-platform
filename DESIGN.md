# 随机超平面 LSH 分桶检索器设计

## 1. 签名计算

每张表有 `b` 个随机单位法向量 `r_i`，来自可复现 PRNG（种子 `seed` 与表号、位号派生）。
对向量 `x`，第 `i` 位：

```
dot(x, r_i) >= 0  -> 1
dot(x, r_i) <  0  -> 0
```

**内积恰为 0（含零向量）的约定**：取 `>= 0` 分支，位为 1。
该约定固定、对称实现于建索引与查询两侧，因此零向量之间仍互相碰撞，
签名完全确定（CharHash 对 `sign(0)` 取 +1 的 Go 版约定）。
位打包成 `b` 位无符号整数（b<=64），同签名落入同一桶。

## 2. 位数 b 与表数 L 对候选数、召回的影响

两向量夹角为 `θ`，单一位相等概率（Char 相似度）：

```
p = 1 - θ/π          （θ∈[0,π]）
```

单表 b 位全相等（同桶）概率 `p^b`；`L` 张独立表中至少一张同桶（成为候选）概率：

```
P_hit = 1 - (1 - p^b)^L
```

- **b 增大**：单桶区分度提高，桶变细 -> 桶内候选数下降；但 `p^b` 指数下降，
  单向量被召回概率降低。故 b=4→8→12 候选数单调下降，召回也趋降。
- **L 增大**：更多独立分桶机会，`P_hit` 单调上升（召回单调不降），
  代价是存储与候选并集线性增长；`L=1/2/4/8` 召回单调不降。
- 候选并集去重后精排，精排距离计算数即候选集合大小，必须远小于 N。

测试用数据：高斯聚类（相近向量 θ 小，p 接近 1），使 top10 召回可达 >=0.6。

## 3. 建索引与查询

- 建索引：对每个向量在每张表算 b 个内积（哈希内积计算次数恰为 `N*L*b`，
  由计数器精确统计），写入 `签名 -> ID 列表` 桶表。
- 查询：算查询的 `L*b` 个签名，按签名取各表桶，ID 并集为候选；
  候选数由非导出计数器 `rerankCount` 记录；对候选精排欧氏距离取 TopK。
- 基准：全量暴力 TopK；召回 = |近似TopK ∩ 精确TopK| / K 的跨查询均值。
- 并发：表切片采用「全量构建后一次发布」（不可变快照指针），
  查询只读已发布快照，故未完成表对查询不可见（全可见或全不可见）。

## 4. 可复现性

PRNG 使用 `math/rand` 的 `rand.New(rand.NewSource(seed))`，
每个法向量分量按固定顺序消耗随机数；表/位编号通过以
`(seed, table, bit)` 派生的独立 source 生成。
因此同种子两次构建：法向量逐元素相同 -> 签名表逐字节相同；
改任一种子 -> 至少有签名不同。

## 5. 持久化格式（小端序）

```
magic(4)="LSH1" | ver u16 | dim u32 | bits u8 | tables u8 | N u32 | CRC32 u32
[每个法向量: dim 个 float64]            // 超平面参数
[每表每桶: sig u64, count u32, ids u32*] // 桶表
```

CRC32（IEEE 表）覆盖其之前全部字节，追加于文件末尾。
截断分类（字节偏移区间）：
`[0,头长)` 头部不完整；`[头长, +平面参数)` 超平面参数不完整；
`[.., +桶表)` 桶表不完整；总长满但 CRC 不符 -> CRC 不匹配。
恢复取「最大可恢复前缀」：已完整解析的桶保留，跨截断点的半条桶丢弃，
桶中引用的不存在 ID 剔除并计数（不产生悬挂 ID）。
法向量全零为退化超平面，读回时检出并指明第几张表第几位。

## 6. 边界语义

输入向量含 NaN 或 ±Inf：拒绝建索引，计入可读跳过数 `Skipped`；
零向量合法（签名全 1）；空集合、单向量、K>N、dim=1 均定义明确；
查询维度不符返回可判定错误（指期望/实际维度）。四类错误均可用 `errors.Is` 区分。
