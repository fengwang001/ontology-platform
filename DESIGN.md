# 设计：只追加日志的段索引与范围回放

仅标准库。一个日志目录下含若干段文件 `seg-NNNN`（NNNN 为 0 基段号）与
同名索引 `seg-NNNN.idx`。序号即事件在整条日志中的全局 0 基连续序号。

## 1. 磁盘格式

段头固定 32 字节，大端：
- magic(4)=`OSEG`，version(1)=1，flags(1)=0
- firstSeq(8 uint64)：本段首事件全局序号
- count(8 uint64)：本段事件数（repair 可改写）
- headerCRC(4 uint32)：上述前 24 字节的 CRC32-IEEE

事件记录（头之后逐帧，顺序排列）：
- length(4 uint32)：载荷字节数（允许 0）
- payload(length)
- crc(4 uint32)：CRC32(length 字节 || payload)

索引头固定 16 字节：magic(4)=`OSID`，version(1)=1，interval(2 uint16)=N，
reserved(1)=0，headerCRC(4)；其后每 16 字节一个锚点：
seq(8 uint64)、offset(8 uint64，相对段文件起点的事件帧偏移)。
锚点只记「段中序号为 N-1、2N-1、3N-1…」（即全局序号 ≡ N-1 mod N）的事件。
锚点的序号可由「段首序号 + 锚点序数」推出，偏移通过扫描帧得到，因此索引中
的一切都能从段重新推出：索引不含任何只在写入时可知的信息。

## 2. 定位规则

回放 `[from,to]` 时，对每个与区间相交的段，选择「序号不大于 from 的最大锚点」
作为起点；无此锚点（from 小于首锚点序号）时从段头后开始。

为什么不能选「最接近 from 的锚点」：锚点可能恰好落在 from 之后，从该锚点向
后扫描只会看到序号更大的事件，from 与该锚点之间（含 from 自身）的事件会被
永久漏掉。取不大于 from 的最大锚点保证起点序号 ≤ from，再顺序跳过
`< from` 的事件即可，不重不漏。锚点为每 N 条一锚，故跳过数严格小于 N
（等于锚点时跳过 0；两锚点之间最多 N-1；小于首锚点时首锚点之前最多 N-1 条）。

## 3. 索引重建

删除 idx 后：读段头得到 firstSeq，从 32 字节起逐帧顺序扫描；每读完一条合法
帧，若其段内位置 ≡ N-1 mod N，则记录 (firstSeq+pos, 当前帧起始偏移)。
文件结束判定：读到 EOF 即正常结束（truncate 到帧边界仍合法）；读到帧中途
EOF 才是损坏。因锚点序号由规则推出、偏移由扫描得到、N 由参数给定且写入索引
头，重建结果与原索引逐字节相同。

## 4. 范围回放与读字节上界

段按 firstSeq 排序，选与 `[from,to]` 相交者依次处理；段内序号连续
（首序号 + count == 下段首序号）。从锚点偏移起用「精确读」逐帧读取（每次
io.ReadFull 恰好 length+8 字节，不预读），按序号过滤。读取字节数 ≤
区间内事件帧长之和 + 一个锚点区间（起点帧到首条命中帧，至多 N 帧）。

## 5. 失效回退

从锚点偏移解帧失败（偏移处 length 非法、length+8 越界或 CRC 不符）即判定
索引失效：丢弃锚点、从段头后全段扫描，并在报告中标记 `IndexStale`。
回放先校验段头 CRC 与相邻段序号连续性。

## 6. repair

截断点分类（f 为截断后长度）：
`f<32` 头不完整；32≤f<36 长度前缀不完整；其余按帧推进，落在某帧
`[L, L+4)` 为长度前缀不完整，`[L+4, L+ln)` 为事件体不完整，
`[L+ln, L+ln+4)` 为 CRC 不完整（CRC 字节残缺或校验不符）。
恢复最大可恢复前缀：回退到最后一个完整且 CRC 正确的帧，改写段头 count 为
实际条数（保留魔数并重算 headerCRC）。缺口检测：相邻段 firstSeq 必须等于
上段 firstSeq+count，否则报告缺失序号区间。四类判定错误可经 errors.Is 区分：
ErrHeader/ErrLength/ErrBody/ErrCRC（另 ErrRange、ErrGap、ErrIndexStale）。

## 7. 边界与并发

空日志合法；from>to 返回 ErrRange；from<最小序号从段首开始（不报错）；
to>最大序号回放到末尾（不报错）；空载荷合法。Log 以 RWMutex 串行化追加
（写锁）与快照列举/回放（读锁）；回放打开独立只读 fd 并对一致性快照工作，
追加对回放可见的始终是某个完整前缀，-race 干净。
