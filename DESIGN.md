# 带背压的流式聚合管线 — 设计推导

## 1. 链路与组件

source（可注入：速率/报错/提前结束）→ parse（坏记录计数不中断）
→ group（分组求和，组数量有硬上限）→ sink（临时文件 + 原子 rename，带校验和）。
所有有界队列由 stage 包提供；ckpt 负责检查点写出/恢复；pipeline 负责驱动。

## 2. 核心推导：「已消费位置」记到哪

记录序号 seq 单调递增。管线里有多级有界队列，任意时刻一条记录可能处于：
已被 source 读出 / 在队列里 / 已聚合但未落盘 / 已落盘。

- 若记 **source 读取位置**：崩溃后在途（队列中）记录不会被重放 → **丢失**。
- 若记 **sink 已落盘位置** 且队列中仍压着更早的记录：恢复后重放区间会覆盖
  已落盘部分 → **重复**。

两种朴素定义都不满足「不丢不重」。关键观察：**只有当某序号之前的全部记录
都已排空所有阶段（各阶段在途集合为空）时，该位置才可安全记为已消费**。

本设计用周期性 barrier（屏障）实现，等价于 Chandy–Lamport 的流标记：

1. source 每 N 条注入一条带 seq=B 的 barrier，随数据经过同一队列（FIFO 保序）。
2. parse/聚合 遇到 barrier 不计数、不改状态，原样下传。
3. sink 遇到 seq=B 的 barrier 时，保证 ≤B 的所有数据帧均已 fsync 落盘，
   随即原子写出检查点：`consumed=B` + 各组当前聚合值 + 已输出字节偏移。
4. 检查点原子 rename 后，barrier 之前的记录才正式「既不丢也不重」。

恢复语义：从最后一个完好检查点（consumed=B）重放 source 中 seq>B 的记录；
sink 以保存的偏移续写到同一临时文件，末尾 barrier 时原子 rename。崩溃发生在
检查点 rename 前则整个屏障周期重做：重放的数据帧会**逐字节覆盖**同一偏移，
输出字节序列与不崩溃时完全一致 → 不重（rename 恰好一次）不丢（重放从 B+1）。

## 3. 背压与资源约束

有界队列 send 在满时阻塞，阻塞次数计入 gauge。source 因此被下游拖慢。
各级队列容量统一为 C 时，系统在途记录数硬上界为 3C（三条队列）+ 各阶段
正在处理的 1 条；测试取 C 相等，断言历史最大在途 ≤ 3C。
内存分组数超过 maxGroups 返回 `ErrTooManyGroups` 并停止管线。

## 4. 停止与故障

- Stop 幂等（sync.Once）：关闭源并排空在途记录后各 goroutine 退出，
  不留悬挂 goroutine；停止前先排空，保证不丢。
- 坏记录（空行/无分隔符/空键值）计入 bad，不进入任何分组。
- source 报错：已处理部分照常过 barrier 落盘+检查点，错误如实返回。
- sink 崩溃：临时文件以 `.tmp` 标识；恢复时若正式文件不存在，删除残留
  tmp，从检查点偏移重放（逐字节截断点均因长度不足/校验和不符被识别）。
- 检查点损坏：保留最近两份（ckpt.json、ckpt.prev.json），校验和不匹配则
  回退上一份并在报告中标记 rolledBack。

## 5. 文件格式

输出帧：`seq<tab>key<tab>value<newline>`，整文件末尾附
`sha256:<hex>  <长度>\n`（校验行参与自身之前全部字节的摘要）。
检查点为 JSON：consumed、offset、groups(map)、sha256(其之前字节)。
