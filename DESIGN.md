# 设计推导

## 1. 非法单元切分（WHATWG 消费规则，由七样例反推）
首字节定期望长度 n：C2..DF→2，E0/E1..EC/ED/EE..EF→3，F0/F1..F3/F4→4；C0 C1 F5..FF 不可能编码标量，单字节成一非法单元；80..BF 是孤立尾字节，各成一非法单元（故 80 80 两个）。
第二字节按"非最短/代理/越界"设区间，区间外则**第二字节本身另起解析**（只吞 1 字节），故 E0 80、ED A0、C0 AF、F4 90 的尾字节都被释放重解析，得到三/三/两/四个 FFFD。第二字节合法时，把本序列期望的全部尾字节一次吞掉（无论后续字节是什么）：F0 90 80 41 中第二字节 90 合法（F0 区间 90..BF），41 被一起吞 → 一个 FFFD。EOF 时未凑齐只吞实际存在的前缀 → E2 82 是一个 FFFD。

| 首字节 | 第二字节合法区间 | 非法时吞 |
|---|---|---|
| C2..DF | 80..BF | 1（首字节） |
| E0 | A0..BF | 1 |
| ED | 80..9F | 1 |
| E1..EC | 80..BF | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | —（孤立） | 1 |
| C0 C1 F5..FF | —（不可能） | 1 |

第二字节合法后若第三/四字节不是尾字节：按期望长度吞，整序列只算一个非法单元（80..BF 在序列内不被释放）。

## 2. 切分缓存：缓存几字节、何时不等
只在"看到合法首字节且第二字节已在区间内"时才必须等（序列可能合法），此时期望最长 4 字节，缓存上限 **3 字节**（F0 90 80）。第二字节一落入区间外即可立即判首字节非法、释放第二字节重解析，不必等 EOF。C0 C1 F5..FF、孤立 80..BF 立即判定，从不缓存。EOF 残留前缀计 1 个截断非法单元，替换模式 1 个 FFFD，严格模式报截断。

## 3. par 切点对齐
切点 s 不保证在边界。左侧在 s 前回扫：UTF-16 先按偶数对齐，再退过一个高代理（最多退 4 字节）；UTF-8 回扫规则：s 处为尾字节则向前吞连续尾字节，若其前是首字节，按首字节期望长度 L 决定——已凑齐 L-1 个尾字节则切点在单元之后（不退），否则把这些字节划归右段（起点退 1+尾字节数；F0 后 3 个尾字节时退 3 仍足以区分）。回扫至多 4 字节。左段解析到新起点为止（可能在序列中途，截断前缀的 FFFD 被左段丢弃，因其首字节已归右段），右段从该字节以全新状态机解析。每字节恰属一段，C3|80 80 中 C3 归右段，与单线程的"截断 C3 + 孤立 80"两个 FFFD 完全一致。仅第 0 段识别 BOM。

## 4. 输出上限断点
Write 逐单元编码；编码前若剩余容量放不下一个完整单元（UTF-16 代理对要 4 字节），立即终止并返回 ErrLimit，此时返回的 n 只计"已完整输出单元"所占输入字节；切分缓存中的字节从未离开调用方，不计入 n，下次原样喂给新实例。中段 FFFD 同样占完整单元位置。故按 n 续传拼接 == 不限长结果。
