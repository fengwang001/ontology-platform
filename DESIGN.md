# 快照读可见性判定

## 推导：为什么只看提交时刻不够

1. 设快照水位为 10，活跃集为 `{T1}`，表示快照创建时 T1 尚未提交。
2. T1 之后以提交号 5 提交；这个提交号小于水位，可能来自序列号缓存或乱序完成。
3. 朴素规则只比较 `commit(T1) < 10`，因此错误地把 T1 的版本暴露给该快照。
4. 正确规则是：`commit(T) < watermark` 且 `T ∉ active(snapshot)`。
5. 活跃集命中代表 T 在快照创建瞬间仍在进行中；无论它后来拿到多小的提交号，都必须排除。
6. 已回滚事务没有有效提交号，永远不可见。
7. 边界采用左闭右开：提交号等于水位也不可见。
8. 若写版本的事务是快照自身事务，则该版本对当前读者可见。
9. 单次判定先查一次活跃集，再查一次提交状态；计数器上界为 2，不遍历事务表。
10. 版本链只能沿提交号严格递增方向访问；发现非递增即数据损坏。
