# 自定 LZ77 流式压缩 — 设计推导

## 流格式（全部无符号 LEB128 变长整数，上限 10 字节，超 10 字节或 64 位溢出即非法）

| 记录 | 布局 |
|---|---|
| 流头 | 魔数 `ONTO` + 版本 `0x01` |
| 字面量 | tag `0x00`, varint n, n 个明文字节 |
| 回指 | tag `0x01`, varint distance, varint length（length≥3，1≤distance≤窗口容量） |
| 刷新 | tag `0x02` |
| 流尾 | tag `0x03`, varint 原文总长, varint FNV-1a64 校验和；其后不得再有字节 |

## 1. 与 Write 切法无关（语义 3、4）
压缩器收到的新字节全部追加进一个「未决」缓冲，绝不在 Write 边界做任何提交。
主循环每步只在「决策点之后还有 ≥1 字节 lookahead」时决策：
匹配起点在 pos，可确认的匹配长度取到**第一个不匹配字节之前**；不匹配字节本身成为下一决策点。
因为匹配长度由「后续实际字节」决定而非由缓冲边界决定，无论字节分几次 Write 到达，
同一原文产生同一条匹配/字面量序列。唯一无法越过的是「缓冲末端」——
此时停止，等待更多字节。Flush 强制把缓冲末端也当作终止符：当前匹配必须在此结束
（后面没有字节可再延长它），随后输出 FLUSH 记录，所以解码器能还原恰好截至 Flush 的全部输入。
无新输入再次 Flush 时没有未决数据也没有新匹配，输出 0 字节。

## 2. 重叠回指（语义 2）
distance<length 时，源区间与目标区间重叠。块复制（如 copy(dst,src)）语义是
「源先被读完」，重叠时会把未初始化/旧字节当作源；LZ77 要求的是已复制出的新字节
立刻可作为后续源。实现：长度按 min(length, windowCap-distance) 分段，每段内源已全部
是旧数据，可一次 `copy`；每段结束后窗口已前进，下一段源自然含新复制字节。
分段次数为 length/(w-distance)+1，大块时远少于逐字节；distance=1 时每段可复制 w-1 字节，
语义 8 的「声明长度先检查」在复制发生前完成。

## 3. 两种越界距离（语义 7）
「超过已输出字节数」发生在流的很早处（输出尚短），属于数据自相矛盾；
「超过窗口容量」在任何位置都不可能合法——窗口是环形、容量固定，编码器只能对
窗口内现存字节发回指。二者错误类型不同：前者状态依赖，后者恒为非法。
匹配器只在 window 内枚举候选，故编码器不可能产生后者。并行块的预置字典在块开始前
就已物理写入该块自己的 window，因此对该块解码器而言这些字节就是「已输出」，
「已输出字节数」的含义不变，不需要特殊计数。

## 4. 并行确定性（语义 9）
块 i 的压缩只读取：本块原文 + 上一块末尾至多 windowCap 字节（预置字典）。
块之间不共享哈希链、不共享窗口实例，也不读取任何已产生的压缩字节，
所以输出是 (prevTail, blockData) 的纯函数，与 goroutine 调度、worker 数无关。
衔接：每块在块末 Flush，产生 `…记录…FLUSH`；块 0 前写一次流头，
所有块记录顺序拼接，最后写一次流尾。FLUSH 是常规记录，解码器无需知道块的存在，
故同一流可被流式解压器直接解开，workers=1/2/4/8 输出逐字节相同。
