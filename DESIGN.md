# 流式聚合管线设计（module ontology）

链路：source → parse → group/aggregate → sink。每级是 `stage` 骨架的有界通道（容量 N 表示最多 N 条在途：N-1 在缓冲、1 在 worker）。

## 1. 「已消费位置」记到哪一条

- 记录位置 = source 给出的单调序号（0 基，坏记录也占位，重放时字节一致）。
- **source 位置不对**：source 读得最快，记它会把「已读出但还在队列里」的记录当作已消费，崩溃后丢在途记录。
- **sink 位置单独也不对**：sink 落盘超前于检查点时，记 sink 位置而检查点未持久化，崩溃后从旧检查点重放，重复落盘。
- **正确定义**：已消费位置 `P` 是一个屏障（barrier）边界，且**在该时刻之前所有阶段的在途集合为空**——屏障是控制标记，随数据流向下游，
  每个阶段把屏障之前的数据全部处理完再转发，因此「屏障到达 sink」严格等价于各级在途集合为空（无早于屏障的记录残留）。

## 2. 检查点时机与崩溃无损顺序

sink 把输出写成**只追加的分段文件**（segment），段名含位置区间，段尾含 CRC32：

1. 屏障到达 sink：把当前 spool `rename` 为 `seg-{lo:08d}-{hi:08d}`（原子；崩溃前不存在、崩溃后完整存在）。
2. 原子写检查点 `P=hi`（`tmp → rename`，保留最近两份完好文件）。

崩溃点分析：rename 前崩溃→临时文件 CRC 不全，恢复时删除，从旧检查点重放整段；rename 后、ckpt 前崩溃→恢复时
发现孤儿段：CRC 完整且其 `lo == 检查点位置`，则校验段内记录并采纳（推进内存位置），相当于替我们完成了这次提交；
否则删除重放。段是 append-only，旧段从不改写，因此恢复后的最终输出按段名拼接，与不崩溃**逐字节相同**。

三个注入崩溃点：A source 已读满但队列非空（屏障前）；B 聚合中途（worker 内 panic/exit）；C spool 写完未 rename。
恢复后均整段重放到下一个完整屏障，无丢失无重复。

## 3. 背压与资源约束

- 有界通道自然传导：sink 慢→其通道满→聚合 worker 发送阻塞→parse 通道满→source `Send` 阻塞。
- `stage` 内非导出 `maxInFlight`：用 try-send 采样历史最大在途数；source 侧记录阻塞次数（try-send 失败即计一次）。
- 三级容量各 N 时，同时在途上限 = 3N，断言 `maxInFlight <= 3N`。
- 分组数硬上限 `MaxGroups`：超限返回哨兵错误 `ErrTooManyGroups` 并停止管线，不丢组、不静默。

## 4. 故障与恢复

- 坏记录（缺键/解析失败）：计入 `BadCount`，不进任何分组，位置照常占位。
- source 报错：屏障排空已处理部分，落盘+检查点后把错误上报到 `Run()` 返回值。
- sink 立即报错：管线停止，错误上报。
- 临时文件逐字节截断：CRC/长度校验必然失败 ⇒ 判定未完成临时文件并删除（恢复扫描与逐字节遍历测试）。
- 检查点损坏（CRC 不符）：回退最近一份完好检查点，报告 `fellBack=true`。

## 5. 并发与优雅停止

`Stop()` 关闭输入、等待所有 goroutine 退出（WaitGroup），幂等（sync.Once）；未启动时调用也安全。
测试以 `runtime.NumGoroutine` 停止前后差值回到基线（给调度退出宽限）为准，`-race` 干净。

## 6. 输出确定性

最终聚合快照按 key 排序后逐行输出；段按位置拼接；记录格式 `key|val|pos\n`，坏记录为 `!bad|0|pos\n`。
聚合为可交换结合的求和，任意屏障频率/崩溃时刻的最终输出逐字节一致。
