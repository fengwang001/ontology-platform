# 确定性跳表设计推导

约定：键类型为 `key.Key = int64`；头哨兵 `MinInt64`、尾哨兵 `MaxInt64`，高度恒为最大层。
层从 1（底层）起编号；跨度 `span` 定义为该指针跨过的底层数据元素个数（不含源、含目标若目标是数据节点）。

## 1. 层高的确定性导出

导出函数只接收键，不接收任何结构状态：

	h(k, H) = 1 + min(ctz(mix(uint64(k))), H-1)

其中 `mix` 是 SplitMix64 最终化（xorshift-multiply 三段，常量公开固定，无随机源），
`ctz` 为低位连续 0 的个数，`H` 为配置的最大层数（默认 20）。

- 同键同高：`mix` 是纯函数，`h` 只依赖键字节，与插入顺序、已有节点完全无关 → 不变量 2 的前提。
- 分布合理：对均匀的 `mix` 输出，`P(h>=r)=2^{-(r-1)}`，期望层高 2，最高层节点数约 n/2^(H-1)，
  与随机抛硬币跳表同分布，但对「同一份输入」逐次可复现。SplitMix64 的雪崩性保证连续整数键也均匀，
  不会因为键本身有规律而退化成链表或集中到最高层。
- 重复元素：副本键相同故层高相同。同一逻辑键的多个副本是各自独立的节点，各层相邻成链；
  跨度按底层节点计数，副本与普通节点没有任何区别，因此重复不破坏跨度不变量。

实测分布（键 0..n-1，H=20，列出第 1~10 层节点数）：

| n | L1 | L2 | L3 | L4 | L5 | L6 | L7 | L8 | L9 | L10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1000 | 510 | 259 | 115 | 51 | 30 | 20 | 10 | 1 | 1 | 2 |
| 10000 | 4971 | 2552 | 1258 | 599 | 311 | 169 | 78 | 31 | 15 | 9 |
| 100000 | 49843 | 25032 | 12439 | 6314 | 3196 | 1600 | 795 | 388 | 205 | 86 |

每升一层约减半，L8 以上合计占比极小，无退化。

## 2. 删除时跨度的回收

设某层上 `p -> v -> s`，对应跨度 `a = p.span`、`b = v.span`。删除 v 后该层变为 `p -> s`。
被删指针跨过的底层元素并入**前驱**：新指针 `p.span = a + b - 1`（减 1 是 v 自身消失）。
论证：原两条路径共跨过 `a+b` 个底层数据元素，其中恰好含 v 一次；删除 v 后该层只剩一条等价路径，
跨过的应是原集合去掉 v，即 `a+b-1`。当 v 是某层最后一个数据节点时 `s` 是尾哨兵：
`b` 恰为 v 到尾的跨度（0，因为中间无数据元素），公式仍给出 `a-1`，即把 v 从前驱跨度里扣掉，
尾指针无需特判。v 不出现的层上，前驱跨度只减 1（跨过的元素少了 v）。

## 3. RankOf 对不存在的键

选择：`RankOf(x)` 返回 `(小于 x 的元素个数, ErrNotFound)`；存在时返回第一个副本的 0 基序号。
即它本质是下界（lower-bound）位置。由不变量 3 推导：若不存在时也返回一个位置，
该位置必须正好是「插进去会落的位置」，否则 `At(RankOf(x))` 与插入语义矛盾。
`Range(lo,hi)` 直接用两个下界计数之差 `lower(hi) - lower(lo)`，其中 `lower(b)` 为 `< b` 的元素数；
lo、hi 是否真实存在都不影响——区间计数只依赖严格小于各自端点的元素个数，缺失端点天然被排除。

## 4. 迭代器失效策略

选择**快照**：`Iterate` 在读锁内沿底层把数据节点复制成一个切片后立即释放锁，迭代器只遍历该切片。
因此迭代期间任何写入都得到「迭代开始那一刻」的完整序列，不会返回 ErrStale，也不会半新半旧。
论证删除当前指向元素的情形：游标是快照内的下标，指向的是复制出来的值，底层节点被删不改变切片长度与内容，
游标继续返回后续快照元素，自洽；写入只影响新迭代器。
