# Properties 解析与回写设计

## 续行遇到空白行

1. Java 规则先找逻辑行终止符：行尾反斜杠数为奇数才续行，偶数表示最后一个反斜杠是普通字符。
2. 发生续行时，终止反斜杠、换行符和下一物理行开头的空白一起被丢弃。
3. 下一行若为空，剥掉零个空白后仍贡献空字符串，因此只是不再追加内容，逻辑行继续等待后续行。
4. 下一行若全是空白，这些空白正好全部属于“续行首部空白”，贡献也为空；它不会变成独立空行，也不会给逻辑行加空格。
5. 所以 `a=b\`、空白行、`   c` 的结果仍是 `bc`；空白续行与“剥掉首部空白”是同一条规则，不是特殊情况。
6. EOF 前的单个终止反斜杠只表示逻辑行结束，反斜杠本身不进入内容。

## Store 的最小转义

1. 回写统一使用 `key=value`，Load 必须恢复精确字符串和首次键序，因此只转义会改变词法的字符。
2. 值开头的空格、制表、换页会被 Load 当作分隔符后的前导空白吃掉，必须写成 `\ `、`\t`、`\f`；值中间和结尾空白不是分隔空白，原样保留。
3. 键的首尾空白会在寻找键时被吃掉，必须转义；键中间空格也会成为键值分隔符，同样必须转义。
4. 键开头为 `#` 或 `!` 时，整行会被识别为注释，所以要转义；值中的这些字符永远没有注释含义，不必转义。
5. `=`、`:` 在键中会终止键，在值中只是普通字符；反斜杠和换行、回车、制表、换页在键值中均需转义。
6. `\uXXXX` 仅作为 Load 的输入转义；Store 直接输出 UTF-8 字节，避免不必要转义并保证非 ASCII 原样往返。
7. 空键写成 `=value`；空值写成 `key=` 或空键空值写成 `=`。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 分隔符与空白 | 待补充 |
| 2. 注释 | 待补充 |
| 3. 续行与空白续行 | 待补充 |
| 4. 转义与 Unicode 错误 | 待补充 |
| 5. 重复键顺序 | 待补充 |
| 6. Store 最小转义 | 待补充 |
| 7. 1000 组随机往返 | 待补充 |
