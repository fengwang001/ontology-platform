# 设计推导

## 1. 代理对错误偏移（语义 2）

`\uXXXX` 是一个 6 字节词法单元；高代理（D800–DBFF）本身不完整，它声明「下一个单元必须是低代理」。

- 规则：扫描器读到高代理时，先缓存、不产出；随后一个字符都不能少。
  - 后续不是 `\u` 转义（如 `x`、`A`、结尾引号、输入结束）：错误偏移指向**该不合格单元的起点**
    （`"\uD83Dx"` 与 `"\uD83DA"` 均偏移 6，孤立高代理偏移 6）。
  - 后续是 `\u` 但值不在 DC00–DFFF：偏移指向**后一个 `\u` 的起点**（`"\uDE00\uD83D"` 偏移 6）。
  - 孤立低代理：其自身就是非法单元，偏移指向它的 `\`（偏移 1）。
- 理由：高代理没有错，错的是它后面的单元；偏移指向「被期望却不合格」的位置最利于定位。
  若指向高代理起点，则无法区分「孤立高代理」与「高代理后接错单元」。
- 为什么 `"\uD83DA"` 不能是替换字符 + `A`：严格模式不做替换。`\uD83D` 的语义是「半个码点」，
  一旦见到高代理，唯一合法续体就是低代理；`A` 从一开始就没有作为字面量内容的资格，
  允许 U+FFFD 回退等于静默丢信息，违反语义 3 的不替换原则。

## 2. 最小转义与唯一性（语义 4）

- 必须转义的集合：引号（语法定界）、反斜杠（转义引导符）、U+0000–U+001F（RFC 8259 禁止未转义出现）。
- U+001F 必须转义：它属于控制字符区间，未转义出现即非法。
  U+007F 不必：它是 DEL 但不属于 00–1F，RFC 对其无限制，原样合法。
- `/` 不是定界符；非 ASCII（含 U+2028/U+2029）以合法 UTF-8 原样输出即合法 JSON。
- 单射：编码逐码点进行，输出是引号 + 各码点的唯一像 + 引号。
  原样字符的像是自身（不是任何反斜杠序列），转义字符的像以 `\` 开头且短形式与 `\u00XX` 互不复现；
  不同码点像互不相交，且任一字面字节能无歧义切回词法单元，故映射一一对应，规范输出唯一。

## 3. 语义 ↔ 测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 五类严格错误可区分且带偏移 | TestDecodeErrors |
| 2 代理对组合与校验 | TestSurrogates |
| 3 非法 UTF-8（解码/编码两方向） | TestInvalidUTF8 |
| 4 最小转义编码 | TestEncodeMinimal |
| 5 双向往返 | TestRoundTrip |
| 6 跨切分点一致 | TestStreamingSplits |
| 复杂度：检查计数 ≤ 2N | TestCheckCount |
