# 设计推导

## 包与数据流

`lines`（行=内容+行尾原样保留）→ `edit`（Myers O(ND) 最短脚本）→ `hunk`（上下文 C 分组）
→ `udiff`（渲染/解析）→ `patch`（偏移应用、原子拒绝、反向、并发存储）。依赖只沿此方向。

## 推导 1：零计数时 hunk 头的行号

unified 格式约定：`a` 是该 hunk 覆盖范围在旧文件中**第一行的行号**；当覆盖范围为空（`b=0`），
它表示「插入点之前那一行」的行号；插入点在文件最开头时没有前一行，取 0。
- `x\n` → `y\nx\n`（纯插入于文件头）：旧范围空且在第 1 行之前，故 `-0,0`，新范围从第 1 行起 `+1`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（插入于 b、c 之间）：旧范围空，前一行是第 2 行，故 `-2,0 +3`。
- `a\n` → 空（删除唯一一行，C=3 越界截断）：旧范围第 1 行 `-1`；新文件范围空且在第 1 行之前，`+0,0`。

Apply 读到 `b=0` 时，在旧文件第 `a` 行**之后**（即第 a 与 a+1 行之间；a=0 即文件头）插入。

| 旧侧 | 书写 | 语义 / 插入点 |
|---|---|---|
| b=0 | `-a,0` | 不消费旧行；插入点在第 a 行之后，a=0 表示文件头 |
| b=1 | `-a`（省略 `,1`） | 消费第 a 行 |
| b≥2 | `-a,b` | 消费第 a..a+b-1 行 |

## 推导 2：hunk 合并阈值

相邻两段改动之间有 g 行未改动行。合并成一个 hunk 时，中间这些行全部成为上下文；分成两个
hunk 时，每段需要各取最多 C 行上下文，共需 2C 行。故：`g ≤ 2C` 合并（中间行够用/有重叠），
`g ≥ 2C+1` 分开（中间至少剩 1 行不出现在任何 hunk 中）。阈值 = **2C**。
反例（C=1，g=2）：若阈值为 C，g=2 应分开，但 GNU `diff -U1` 输出单个 hunk（两行间隔都被
上下文覆盖）；若阈值为 2C+1，g=3 应合并，但 GNU 输出两个 hunk，中间留下 1 行未出现。

## 推导 3：并列最短脚本的选择

Myers 单向前向，编辑图 k 线，`V[k-1] < V[k+1]` 时横移（插入），否则下移（删除）；
k=0 取下移，k=d 取横移。即并列时**优先删除（down）**，回溯全程记录每轮 V 快照。
影响：`ab → ba` 选 LCS=`b`，脚本为 删a、等b、插a；hunk 内排列为删除块在前、插入块在后
（`-a`、` b`、`+a` 形式，具体由等价行决定）。该选择固定，故重复生成逐字节相同。

## 推导 4：偏移后后续 hunk 的查找起点

第 i 个 hunk 记录起点 `r_i`。前面 hunk 实际在偏移 Δ 处应用，则其插入/删除造成的后续行整体
平移 Δ。第 i 个 hunk 的查找中心取 `r_i + Δ`（Δ 为各已应用 hunk 的「插入数-删除数」累加，
偏移应用本身平移的是后续所有行号），再在 ±F 内按距离近、距离相同取靠前的顺序匹配。
不跟随平移会在连续漂移时让本可命中的 hunk 超出 F 窗口而误拒。

## 换行标记

仅每段最后一行可能无 `\n`。渲染时该行（` `/`-`/`+`）后紧跟
`\ No newline at end of file`。最后一行无换行的「相等」行也发出标记，保证往返。
