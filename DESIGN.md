# 设计推导

## 1. 非法单元切分规则（由七个样例推出）

规则：**最大子序列（maximal subpart）**。从首字节出发，按该首字节规定
的逐位置合法区间逐字节匹配；若匹配出完整序列则为合法标量，否则已匹配
的最长前缀（至少含首字节）整体作为**一个**非法单元，输出一个 U+FFFD；
导致匹配失败的那个字节**不被吞掉**，作为下一单元的起点重新判定。

样例验证：`F0 90 80 41` 中 F0 要求第 2 字节 90..BF、第 3/4 字节 80..BF，
90✓ 80✓ 41✗ → 前缀 `F0 90 80` 是一个单元，41 重判 → `FFFD 41`。
`E0 80 80`：E0 要求第 2 字节 A0..BF，80✗ → 单元仅 `E0`；两个 80 是游离
延续字节各成一单元 → 3 个 FFFD。`ED A0 80`（ED 要求 80..9F）、
`C0 AF`（C0 非法首字节）、`F4 90 80 80`（F4 要求 80..8F）同理。
`E2 82`+流结束：前缀全合法但长度不足，整个前缀算一个单元 → 1 个 FFFD。

第二字节合法区间表（非法时吞掉字节数 = 已匹配前缀长度）：

| 首字节 | 第 2 字节区间 | 第 2 字节非法时吞掉 |
| --- | --- | --- |
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC, EE..EF | 80..BF | 1 |
| ED | 80..9F | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF, C0, C1, F5..FF | 无（非法首字节） | 1 |

第 3、4 字节区间恒为 80..BF；第 k 字节非法时吞掉前 k-1 字节。

## 2. 切分缓存上限与放弃等待时机

缓存只需保存当前未决单元的字节。合法序列最长 4 字节，未决前缀最长
3 字节（如 `F0 90 80` 等第 4 字节），故 UTF-8 方向缓存硬上限 = 3。
UTF-16 方向未决状态最多为 1 个奇数字节 + 1 个高代理（2 字节）= 3，
上限同为 3。放弃等待：每个位置有独立合法区间，读到的字节落在区间外
即可**立即**判定前缀非法，无需等后续字节；只有「前缀全合法但长度不足」
时才等待，且最多再等 3 个字节。

## 3. par 的切点对齐

单元长度 ≤4，故位置 p 是否单元边界只取决于 `src[p-3 .. p+2]`：对每个
s ∈ [p-3, p-1]，用 `u8.Decode` 从 s 起算单元长度 L（不完整视为延伸到
末尾）；若某 s 满足 s+L > p，则 p 在单元内部，不是边界，否则是边界。
`align(c)` = ≥c 的最近边界，关于 c 单调，故各段 `[align_i, align_{i+1})`
不重不漏。单元切分只依赖单元自身字节（确定性、无跨单元状态），从任何
真边界起解析与单线程逐段相同，故非法单元数与输出完全一致。例：
`C3 | 80 80`：切点处 s=p-1 的 `C3 80` 是长度为 2 的合法单元且覆盖 p →
p 非边界，p+1 才是 → 上段得 U+00C0，下段 80 → FFFD，与单线程相同。
UTF-16：par 先读头 2 字节确定 BOM/字节序并分发给各段；边界条件为
`(p-bomLen)` 为偶且前一 u16 单元非高代理，回看 ≤2 字节。

## 4. 输出超限断点

`Write` 返回的 n = 本次从 p 取走的字节数；进入切分缓存的字节计入 n。
但超限错误**只在缓存为空（单元边界）处**产生：每开始一个单元前，按
首字节算出该单元输出字节数的上界（UTF-8 由首字节精确决定；UTF-16 首
字节保守取 4，BE 可按高字节收紧），剩余额度不足则在消费任何字节前
停下，返回 `ErrLimit` 与已消费的 n。此时「已消费 == 已解析 == 已输出
标量对应的输入字节」，调用方按 sum(n) 切开、把后半段喂给新实例，拼接
结果与不设上限一次转完逐字节相同。残留在缓存里的字节虽计入此前某次
n，但断点处缓存必为空，故断点等式不受其影响。
