# DESIGN

流按字节顺序：流头 `4C 5A 01`（魔数 LZ、版本 1），随后零或多条记录，最后流尾。

| 记录 | 标签 | 布局（整数均为 base-128 uvarint，最长 10 字节） |
| --- | --- | --- |
| 字面量 | `00` | tag, len, len 个原文字节 |
| 回指 | `01` | tag, dist, length |
| 刷新 | `02` | tag（无载荷） |
| 流尾 | `03` | tag, totalLen, checksum(FNV-1a64 原文) |

## 1. Write 切法无关（语义 3）

匹配长度有上界 `M`（65536）。处理位置 p 时，若 p 之后可用字节 `< M`，则「最长匹配（截断到 M）」可能被未来的 Write 延长，此刻任何决定都会泄漏 Write 边界。因此压缩器缓存未处理字节，**仅当 p 之后已有 ≥ M 字节时才决定 p 的词元**；不到 M 就等。这样 p 的判定只依赖 `input[:p]`（决定哈希链状态）和从 p 起的至多 M 个字节（决定匹配长），两者都只是字节流和 flush 偏移的函数，与 Write 次数无关。连续字面量先攒着、在遇到回指/flush/close 时合成一条字面量记录，所以字面量段边界同样不依赖 Write。Flush 时调用方承诺之后暂时无数据可等，故 Flush 强制用「当前可见的全部后缀」把当前匹配截断并落盘（匹配终点恰为 flush 偏移）；这正是切法无关所必需的：flush 偏移相同则截断点相同。

## 2. 重叠回指（语义 2）

块复制 `dst=src..src+length` 在 length>dist 时读到的是复制前的旧缓冲，或因 src/dst 重叠而错乱；逐字节 `b=get(dist); put(b)` 中每一步的 get 都可能命中刚写出的字节，天然产生周期展开。`window.CopyFrom` 不退化为逐字节：每轮取 `k=min(remaining,dist)`，这 k 个源字节全在「上一轮结束位置之后」、与目标不重叠，可用 `copy` 整块搬（环形绕回时按连续段再切），随后把 dist 固定、继续下一轮。k 轮数 = ceil(length/dist)，大距离时基本一次完成；正确性等价于逐字节，因为每个字节被引用时它距离当前末尾恒为 dist。

## 3. 两类距离错误（语义 7）

二者是不同不变量：`dist>已输出字节数` 是「引用不存在的过去」，流在历史还很短时损坏；`dist>窗口容量` 是「引用了解压器根本不留的远古字节」，即使已输出很多也永远无法恢复。压缩器在候选遍历时硬性丢弃 `p-cand>窗口容量` 的候选，故合法流不可能出现后者；前者在正常流里也不可能，但预置字典外的手改流可能触发，故分别报错。并行块压缩时，块 i 的预置字典 = 块 i-1 原文末尾至多窗口容量的字节，而解压器此刻的「已输出」恰好包含全部前序块（字典是其中最后一段），所以「已输出字节数」语义不变，距离检查与顺序流完全相同。

## 4. 并行确定性（语义 9）

块 i 的压缩输入仅为 `(data[(i-1)块末尾..i块起点] 作字典, data[i块])`：窗口和哈希链只由该字典初始化，不读取任何其他块的压缩产物；压缩是纯函数，故完成顺序与 worker 数不影响结果，主协程按块序拼接即可。块间不需要刷新标记：解压器历史连续，块 i 的回指所指字典字节就是它已输出字节的最后一段；块边界只可能产生两条相邻字面量/回指记录，均为合法流。只有流头（一次）与流尾（含全长、全量校验和，主协程统一计算）是全局的。
