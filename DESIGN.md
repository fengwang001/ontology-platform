# 设计推导

## 严格解码拒绝清单（不变量：`Encode(Decode(t)) == t`）

1. 显式次数 `1`：`1a` 被接受则 `Decode` 得 `a`，再编码为 `a != 1a`。拒绝。
2. 次数 `0`：`0a` 得空，编码为 `` != `0a`；且游程语义上次数必 ≥1。拒绝。
3. 前导零：`01a`/`00a` 得 `a`/空，编码为 `a`/``，不等于原串。拒绝（偏移指向首个 `0`）。
4. 相邻同符号游程：`2a3a` 得 `aaaaa`，最长游程被合并，编码为 `5a != 2a3a`。拒绝。
5. `\` 后非数字非反斜杠：如 `\x` 不是合法符号转义（只有 `0-9`、`\` 需转义），拒绝。
6. 末尾孤立 `\`：后面无码点，游程无符号，拒绝。
7. 末尾只有次数无符号：如 `12`，游程不完整，再编码必补符号，拒绝。
8. 非法 UTF-8：符号必须是 Unicode 码点；字节非法时拒绝，不做宽松替换。

## 大次数（第 3 条）取舍

选择**支持任意大次数**：次数用 `math/big.Int` 逐位累加，永不整数溢出。
为避免 `9999…a` 导致按次数分配内存，输出采用**分块**写出（每块固定缓冲），
并设**可配置的输出总字节上限**（默认 1 GiB），超限返回可判定的 `ErrOutputLimit`，
此时不产生与次数成比例的分配。

## 测试对照表

| 语义（第三节） | 测试函数 |
| --- | --- |
| 1 往返（数字/反斜杠/多字节/长游程，含随机） | `TestRoundTrip` |
| 2 规范形式与严格解码（八类拒绝+偏移） | `TestStrictRejects` |
| 3 大次数不溢出 / 输出上限 | `TestLargeCount` |
| 4 最长游程 / 组合字符不规范化 | `TestLongestRun` |
| 5 全切分点一致（含 `2a3a`） | `TestAllSplits` |
| 四 字节检查计数器 | `TestExaminedCount` |
