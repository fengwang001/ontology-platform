# Set-Cookie 解析与适用性判定器 — 设计推导

包划分：`attr`（单条 Set-Cookie 文本解析）→ `match`（域/路径匹配规则）→ `jar`（罐：存储、覆盖、惰性过期、适用性排序）。依赖单向，反向依赖不存在。时间全部来自注入时钟 `now func() time.Time`，不读真实时钟、不碰网络与文件系统。

## 第 2 条：默认路径推导

规则（RFC 6265 §5.1.4）：取来源 URL 路径中**最后一个 `/` 之前（不含）的部分**；若来源路径不以 `/` 开头，或该部分为空，则默认路径为 `/`。

- `/a/b/c` → `/a/b`：最后一段 `c` 是"资源名"而非目录，cookie 的作用域应是承载该资源的目录。
- `/a` → `/`：路径里唯一的 `/` 就是开头那个，它之前是空串，按规则退化为 `/`。若错误地推出 `/a`，等于把"资源名"当成了"目录"，会让设置在 `/a` 的 cookie 被发送到 `/a/b` 等本不该携带它的子路径，作用域被悄悄放大。
- `/` → `/`：同样只有一个 `/`，退化到根。

## 第 3 条：为什么路径匹配不能是朴素前缀匹配

朴素前缀匹配下，cookie 路径 `/a` 会命中请求路径 `/ab`。越权实例：同一主机上 `/a` 是应用甲、`/ab` 是应用乙（现实中如 `/mail` 与 `/mailadmin`、`/app` 与 `/app-internal`），甲在 `/a` 下设置的会话 cookie 会被浏览器发给乙，乙即可冒用甲用户的会话——同主机不同应用之间发生会话泄漏。

正确规则（RFC 6265 §5.1.4）：两路径相等；或 cookie 路径是请求路径的前缀**且** cookie 路径以 `/` 结尾；或是前缀**且**请求路径中紧随其后的字符是 `/`。于是 `/a` 命中 `/a`、`/a/`、`/a/b`，但不命中 `/ab`。

## 第 4 条：Domain 越权的判据

判据两条，同时满足才允许设置：

1. `Domain` 值（忽略前导点、统一小写后）必须是来源主机自身或其上级域，即 `host == domain` 或 `host` 以 `"."+domain` 结尾。`b.example.com` 设 `Domain=example.org` 不满足后缀关系，拒绝。
2. `Domain` 不得是单标签域（必须含 `.`）。`com`、`org` 这类单标签是公共后缀级别：若允许，任意站点都能种下对整个 `.com` 生效的 cookie，等于跨站越权。在没有公共后缀列表（PSL）可用的约束下，"拒绝单标签"是安全的最低近似——它恰好挡住 `com` 这类顶级域，且不会误伤正常场景（正常场景设的域至少是自己站点的可注册域，含点）。

两条任一不满足，整条 Set-Cookie 被拒绝，cookie 不入罐。

## 第 8 条：为什么覆盖时保留最早创建时间

发送排序规则是"路径更长者优先；路径等长时创建时间更早者优先"。创建时间必须对"覆盖"免疫：

- 若覆盖时刷新创建时间，那么每次续期/重设同名 cookie（如会话滑动过期）都会改变它在等长路径组内的相对顺序，服务器端观察到的 Cookie 头顺序会随时间漂移。凡依赖接收顺序做冲突消解的应用（同名 cookie 取第一个）会出现非确定性行为。
- 保留最早创建时间后，排序只随"罐内成员集合"变化，不随"刷新动作"变化；同一集合在同一时刻的两次查询结果必然一致，排序是集合的稳定函数。

这也与 RFC 6265 §5.3 一致：覆盖时新 cookie 继承旧 cookie 的创建时间。
