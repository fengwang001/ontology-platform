# 设计推导：消息头部集合的解析、规范化与安全回写

范围限定：仅内存中的头部块（RFC 9110 风格的 `Name: value CRLF`，以空行终止）。
不涉及 HTTP 服务端、网络、存储、Link/Action。只用标准库。

## 1. 分层与数据模型

五个包，依赖单向：`token` ← `fold`、`listval`、`policy` ← `headers`。

- `token`：纯字符判定，无状态。名字字符=RFC tchar；值允许 SP、HTAB、VCHAR(0x21–0x7E)。
- `fold`：把字节按物理行切成逻辑行，处理续行；回写时按宽度重折。
- `listval`：逗号列表的切分/合并，识别 DQUOTE 与反斜杠转义。
- `policy`：每名字一份策略 `{是否列表型, 重复策略, 大小写敏感性}`。
- `headers`：有序多重映射；追加式存储（entry 带 live 墓碑，不做就地重排）+
  `name -> []*entry` 索引 + 规范化名字键。

回写帧格式严格为 `Name: value\r\n`，块末必须有空行 `\r\n`。裸 `\n`、裸 `\r`
不属于规范帧，出现在解析输入中一律判语法错误（它们正是注入载体）。

## 2. 第 3 条：名字与冒号之间为什么必须报错

`X : 1`（冒号前有空格）不能宽容地去掉空格后当 `X: 1`，原因是存在**前后端
对空白理解不一致**的走私面：

- 前端 A（本解析器）若宽容剥离，看到的是头部 `X`；后端 B（下游/缓存/日志/
  审计）若严格按 RFC，把整行视为非法行、头部名 `X ` 或直接透传，就会对
  “这是谁设置的、是否命中缓存键、是否越过 `X` 的白名单”产生分歧。
- 攻击者可借 `X :` 让宽松链路放行、严格链路拒绝（请求走私/缓存投毒），
  或借尾部空格制造 `X` 与 `X ` 两个名字绕过“只允许一个 X”的约束。
- 规范化只能作用于**语义明确无歧义**的部分（名字大小写）。`X ` 与 `X`
  是否同一名字并非跨实现共识，因此正确动作是拒绝，而不是猜测。

名字大小写无歧义（RFC 规定不敏感），故规范为 `X-Header-Name` 形态。

## 3. 第 4 条：禁止字符集合与“拒绝优于删除”

禁止集合（名字和值都按各自规则校验）：

- 值中的硬禁止：NUL(0x00)、CR、LF，以及除 HTAB 外的全部 C0 控制字符
  (0x01–0x08, 0x0B, 0x0C, 0x0E–0x1F)、DEL(0x7F)。
- 名字的硬禁止：非 tchar 的一切字节，包含空格、冒号、控制字符、高位字节。
- 编码绕过：帧边界只由原始字节 CR/LF 决定，任何“编码后”的换行同样禁止——
  对 `%0d`、`%0a`、`%0D%0A`、`\r`、`\n`、`\u000d`、`\x0a` 这些序列，
  `ValidateValue` 在**入口单点**（解析后值落库前、Add/Set 前）模式匹配拒绝。
  这些序列对头部值不是必需语法（值是不透明 token，不含转义约定）；宁可误伤
  含字面 `%0d` 的合法 opaque 值，也不保留一个多实现可能二次解码的走私面。
  拦截点只有一个：值进入集合之前，因此解析与程序化写入共用同一条防线。

静默删除比拒绝危险：删除会**改变发送方意图却返回成功**。调用方以为写入了
`a\r\nEvil: 1`，实际写成 `aEvil: 1` 或别的东西——数据损坏、审计失真、
且下游换一种解码策略时仍可能复活注入。拒绝把“这不是一个合法值”显式传回
调用方，由调用方决定，库绝不替他吞掉。

## 4. 第 5/6 条：列表切分与回写等价

引号内逗号不切分的典型受害头部是带引号参数的列表，例如：

`Accept: text/html;level=1, text/plain;charset="a,b", */*`

若无视引号，第二项会被切成 `text/plain;charset="a` 和 `b"` 两个非法媒体类型，
进而错误地多协商出一种类型；`Warning`、`Accept-Encoding;title="a,b"` 等同理。
转义引号 `\"` 用反斜杠状态机处理（`\\` 与 `\"`）。切分项首尾空白去除。
空项（`a,,b`）语义选择：**判错拒绝**——空 token 无对应协议元素，宽容跳过
会让往返静默丢项，合并无法还原，故确定性报错。

回写等价：输入本身规范（规范名、值首尾无空白、无折行或仅库风格折行、
CRLF、终止空行）时，解析→回写逐字节相同。不规范输入的“正确输出”定义为
应用规范化映射后的唯一形态：名字定形、值 trim、折行展开/重折、统一 CRLF。
幂等论证：规范化是**投影**（projection）——它只删除或替换被允许的冗余
（大小写、空白、折点），不引入新的冗余；投影后冗余集为空，再投影无变化，
故 `Write(Parse(x)) == Write(Parse(Write(Parse(x))))`。重折只在“空格后跟
非空白”处断开，续行前缀恰为一个空格，展开时压回一个空格，折行往返无损；
宽度内放不下的无空格长 token 无法合法折叠，报确定错误而不是硬切（硬切会
破坏往返）。

## 5. 第 10 条：索引结构、保序与比较数上界

存储：`entries []*entry` 仅追加（删除置 `live=false`，迭代时跳过），
天然保持插入顺序，索引只存指针，永不重排 entries。索引
`index map[string][]*entry` 指向同名 entry 链；规范化只改 name 字段不改指针。
因此索引的增删改不改变遍历顺序。

按名查找成本 = 一次 map 哈希（与 N 无关）+ 扫描该名字的桶（k 个同名）。
最坏单次比较数上界 = 该名字出现次数 k，与集合总条数 N 无关。计数器
`lookupCompares`（非导出，单测可注入读取钩子）记录每次 Get 实际比较的条目数：
N=50 与 N=5000 且每名字仅一次时，两次查找的比较数同为 1，不随 N 增长。

并发：一把 `sync.RWMutex`，读操作持 RLock 可并发；写操作持 Lock，故写与
读、写与写互斥——全部操作并发安全，但写不与读并行。截断错误按阶段
（name/colon/value/fold/terminator）返回并带头部序号；超限在构造期增量
校验，超限即整体返回错误、集合不交付（调用方拿不到半截状态）。
