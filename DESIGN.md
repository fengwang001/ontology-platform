# 设计：带租期的分布式序列号分配器

目标：多个分配器实例从共享持久计数器批量租用号段 `[s, s+n)`，在进程内存中
O(1) 分发，保证全局唯一、单调不回退。仅用标准库，时钟可注入，持久化写本地
临时目录（文件 + CRC32，写临时文件后原子改名）。

## 1. 为什么持久化必须先于分发

租到号段 `[s, s+n)` 后，必须**先**把持久计数器推进到 `s+n` 并落盘，**再**开
始分发该段内的任何号。反证：若「边分发边推进」，设租得 `[100,200)`，分发到
150 时持久值只推进到 150。此刻崩溃：已发给调用方的 150..199 中可能有在途响
应（调用方已收到号但分配器尚未落盘）。重启后从持久值 150 继续租段，150 之
后的号会被再次发出 → 全局重号，违反唯一性。反过来，先推进到 200 再分发，
崩溃的最坏后果只是 150..199 成为空洞——空洞可接受，重号不可接受。因此不变
量：**任何被分发的号 x 必满足 x < 崩溃前已持久化的计数器值**；重启后首个新
号 >= 上次租段的末尾 `s+n`。

## 2. 租约到期与作废语义

租约 = `{Start, Length, Expiry}`。本地分发仅在 `now < Expiry` 且段内还有剩余
时进行。时钟跨过 Expiry 后，该租约**作废**：剩余号永不分发（成为空洞），下
一次分配触发重新租用（持久计数器再次原子推进）。租期为 0 是合法配置：租约
立即到期，每次分配都重新租用、每次都持久写——正确但慢，仅用于演示边界。

## 3. 时钟回拨

分配器记录上次见到的时钟值 `last`。若 `Now() < last`，判定时钟回拨，返回可
判定错误 `ErrClockBackwards` 并拒绝分配。绝不因回拨让已到期租约"复活"：到
期判定只在时钟单调前进时进行，回拨直接被拒，不参与续租。

## 4. 段长自适应（AIMD）

段长 `seg` 初始为 `minSeg`（默认 1）。每次重新租用前按上一段的结局调整：

- 上一段被**耗尽**（剩余为 0，说明需求旺盛）：`seg = min(seg*2, maxSeg)`。
- 上一段**到期仍有剩余**（说明空闲，号被浪费成空洞）：`seg = max(seg/2, minSeg)`。

效果：高频阶段段长指数爬升到封顶 `maxSeg`（默认 1024），持久写次数约等于
分发数 / maxSeg；低频阶段段长回落到 `minSeg`，空洞浪费有界。10 万分发、
maxSeg 1000 时持久写 <= 120（增长期约 11 次 + 稳态约 100 次）。

## 5. 持久化格式与损坏分类

文件 16 字节：`4B magic | 8B 计数器值(大端) | 4B CRC32(前 12 字节)`。
加载时按长度与校验分类（均可 `errors.Is` 判定）：

- 1..3 字节：`ErrHeaderIncomplete`（头部不完整）
- 4..11 字节：`ErrValueIncomplete`（值不完整）
- 12..15 字节或 CRC 不符：`ErrCRCMismatch`

**损坏时拒绝启动，绝不从 0 重建**：持久值是全局唯一性的唯一权威来源，损坏
后从 0 重启会把整段历史号段重新发出，造成大规模重号。宁可不可用，不可重号
（可用性可由人工恢复备份解决，重号无法挽回）。

## 6. 原子推进与并发

`Counter.Next(n)` 在互斥锁内完成「读当前值 -> 校验溢出 -> 编码 -> 写临时文件
-> fsync -> 原子 rename -> 更新内存值」，对调用方表现为单次原子推进。溢出检
查 `value > MaxUint64-n` 时返回 `ErrOverflow`，绝不回绕。同一进程内多个分
配器共享同一 `*Counter`（状态本就在进程内存），跨协程租用由此串行化，号段
不重叠。写失败（注入）时 `Next` 返回错误、内存值与磁盘值均不变，租用失败
且不分发任何号。

## 7. 边界语义

- 段长 <= 0：`lease.New` 返回 `ErrInvalidLength`。
- 段长 = 1：每次分配都触发租用与持久写，正确但写放大为 1:1。
- 首次启动：无持久文件，从配置起点 `start` 建立文件。
- 计数器接近 `uint64` 上界：`ErrOverflow`，拒绝而非回绕。
- 不要求无空洞：崩溃与租约作废产生空洞；`audit` 报告空洞区间数与总长度，
  且总长度精确等于「作废剩余 + 崩溃丢弃」之和。

## 8. 包划分

`clock`（可注入时钟）-> `durable`（持久计数器）-> `lease`（号段租约值类型）
-> `alloc`（本地分发、续租、自适应、回拨检测）-> `audit`（唯一性/单调性/空
洞自检）-> `cmd/demo`（九项判定演示）。
