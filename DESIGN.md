# 分词器设计

## 第 2 条推导：双引号内反斜杠

POSIX：双引号内反斜杠仅在后随 `$`、`` ` ``、`"`、`\`、换行时保留转义义；
对其他字符失去特殊含义，两字节原样保留。故 `\` 进入「待定」状态：下一字节属
五符则吞掉 `\`（遇换行二者皆删，其余输出被转义字符）；否则 `\` 与下一字均输出。

## 第 4 条推导：空词

词的产生条件是「出现过构成词的事件」：进入引号即开词（即使内部为空）；
裸空白只切词、不开词。故 `""''` = 1 个空词（`""` 开词、`''` 拼入同词）；
`a""` = 1 个词 `a`；`\<换行>` 续行后无字面值事件 = 0 个词。

## 状态转移表

状态：`N` 无引号、`Q1` 单引号内、`Q2` 双引号内、`BN` 无引号待转义、`BQ` 双引号待转义；字节类：`sp` 空格/Tab、`nl` 换行、`q1` `'`、`q2` `"`、`bs` `\`、`o` 其他。

| 状态\字节 | sp | nl | q1 | q2 | bs | o |
|---|---|---|---|---|---|---|
| N | 切词→N | 切词→N | →Q1（开词） | →Q2（开词） | →BN | 开词并输出→N |
| Q1 | 输出 | 输出 | →N | 输出 | 输出 | 输出 |
| Q2 | 输出 | 输出 | 输出 | →N | →BQ | 输出 |
| BN | 输出空格→N | 删除二者→N（续行） | 输出'→N | 输出"→N | 输出\→N | 输出→N |
| BQ | 输出空格→Q2 | 删除二者→Q2（续行） | 输出'→Q2 | 输出"→Q2 | 输出\→Q2 | 输出\再输出该字→Q2 |

EOF：`N` 正常；`Q1` 报单引号未闭合（偏移=开引号）；`Q2/BQ` 报双引号未闭合（偏移=开引号，BQ 悬挂 `\` 属引号内）；`BN` 报行尾反斜杠（偏移=该 `\`）。

## 语义—测试对照

| 语义 | 测试 |
|---|---|
| 1 单引号内反斜杠 | `TestSplitTable/single*`、demo1 |
| 2 双引号内五种转义/保留 | `TestSplitTable/double*`、demo2 |
| 3 无引号转义与续行 | `TestSplitTable/unquoted*`、demo3 |
| 4 拼接与空词 | `TestSplitTable/concat*`、`empty*`、demo4 |
| 5 三类错误及偏移 | `words.TestErrorsTable`、`lex.TestMachineEOFErrors`、demo5 |
| 6 任意切分点一致 | `words.TestEveryChunkBoundary`、demo6 |
| 7 往返 | `words.TestQuoteRoundtripRandom`、demo7 |
| 字节计数==输入字节数 | `lex.TestByteCounter`（1 MB、逐字节喂入）、demo8 |
