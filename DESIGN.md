# 严格模式 Base64：非规范尾部的推导

一个 4 字符组承载 24 比特。末组按输入字节数分三种余数：

- 余 0：4 个字符，无填充，24 比特全部是数据。
- 余 2（2 字节 → 16 数据比特）：编码为 3 个字符加一个 `=`。
- 余 1（1 字节 → 8 数据比特）：编码为 2 个字符加 `==`。

设四个字符解码出的 6 比特值为 s0,s1,s2,s3，拼成
`s0<<18 | s1<<12 | s2<<6 | s3`。

关键在于：字符数少于 4 时，低 6 位（再低 6 位）在真正的编码过程中
**从未被赋值**，编码器是把一个 16/8 位整数左移后用 0 补齐的。
因此规范编码必须满足：

- 余 2（形如 `abc=`）：低 6 位 `s2&0x3F` 必须全为 0，即 `s2 & 0x3F == 0`。
- 余 1（形如 `ab==`）：低 12 位必须全为 0，即 `s1 & 0x0F == 0`
  （s1 的高 4 位是数据，低 4 位与整个 s2 都是补零）。

所以 `QR==`：R 的 6 比特值是 17，`17 & 0x0F = 1 != 0`，低 4 位里
竟携带了数据，这些比特在重新编码时无处可去，必须拒绝。
而 `QQ==` 中 Q 的值是 16，低 4 位为 0，合法，解码得 `A`。

统一规则：**末组带填充时，被填掉的字符之外、24 位载荷中超出真实字节
边界的后缀比特必须全部为 0，且填充形态只能是无填充 / 一个 `=` / 两个
`=`，填充组只允许出现在整个流的最末尾。**

这正好保证编码是双射：每个字节串的编码结果唯一；任何合法严格串的
后缀补零全部来自编码器自身的左移补零，故 `Encode(Decode(y)) == y`，
不会有两个不同合法串解码成同一字节串（宽松解码器会把 `QQ==`、`QR==`
都解成 `A`，破坏唯一性）。

## 语义与测试对照

| 语义（第二节条目） | 钉住它的测试函数 |
| --- | --- |
| 1 标准字母表与 `=` 填充 | TBD |
| 2 规范形式（本文件推导） | TBD |
| 3 换行位置限制 | TBD |
| 4 五类错误可区分且带偏移 | TBD |
| 5 跨切分点一致 | TBD |
| 6 往返（含 MIME 开关） | TBD |
| 7 输出上限在组边界终止 | TBD |
