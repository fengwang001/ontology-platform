# 倒排索引：短语查询与增量合并 — 设计推导

位置从 **0** 开始。每个位置至多一个词元（多词元情形见第 5 节）。

## 1. 倒排链与增量编码（posting）

- 每词一条链：`Doc{ID, Positions []uint32}`，按文档号升序；每篇文档内位置升序、去重。
- 同一文档同一位置重复添加：拒绝（忽略重复），由 Builder 累计 `DupCount`。
- 链编码（全部大端）：先写 uvarint 文档数；每个文档项写
  `uvarint(docID-上一docID, 首项基0)`、`uvarint(位置数)`、
  每个位置写 `uvarint(pos-上一pos, 首项基0)`。
- 解码逆过程；增量只做编码压缩，不影响查询对齐逻辑。

## 2. 段文件布局（segment）

```
头      "ONSE"(4) + u32 version(1) + u32 dictLen(4)          = 9 字节
词典    dictBlob：每个词 [u32 len][bytes term][u64 off][u64 chainLen]
倒排区  每词一块 [u64 n][chain 编码字节]
尾      u32(crc32(IEEE, 头+词典+倒排区))
```

词典按词排序写出，链偏移相对于倒排区起点。写出在临时文件上完成，
Close 时再拼尾并 `Rename` 为目标文件，保证崩溃不留“已提交”的半截段。

截断分类（按原长度确定区间边界，headLen/dictLen/chainsLen 全部已知）：
截断长度 n（1..len-1）落在 `[1,8]` 头不完整；`[9, 9+dictLen-1]`
词典不完整；`[9+dictLen, 9+dictLen+chainsLen-1]` 倒排链不完整；
`[9+dictLen+chainsLen, len-1]` 只剩尾部缺失 ⇒ CRC 不匹配。
四类错误均为哨兵错误，`errors.Is` 可判。

**最大可恢复前缀**：在“链不完整”档，按词典顺序逐块解析
（每块自带 u64 n 与链 CRC 无关——单链以 `n` 个文档项完整解码为准）；
能完整解码 n 个文档项的链保留，其余词连同之后全部词从恢复出的词典剔除。
因此恢复结果中：词典声称存在的词，其链必完整可读，不存在悬挂指针。

## 3. 短语匹配的位置对齐（phrase）

词序列 t0..tk-1 的位置表（已跨段按文档归并、同文档内排序去重）
p0..pk-1，每表各持游标 i0..ik-1，初值 0：

1. 取基准 doc：取**最大游标文档号** `d = max_j doc(ij)`；任何表越过 d
   后 d 立即重算（等价于“最小文档号驱动”：游标文档号 < d 的表
   用下界跳跃推进到 ≥d）。三表文档号相同才进入位置对齐，否则重复本步。
2. 位置对齐（同文档内）：若 `p1[i1] == p0[i0]+1 && ... &&
   pk-1[iik-1] == p0[i0]+k-1`，命中一次，记录起点 p0[i0]，
   然后**仅 i0 推进 1**（关键：不跳过任何“已用”位置）。
3. 否则取缺口最大的一端推进：计算各表期望值 `base+j`（base=p0[i0]），
   若某表当前位置 < 期望，则该表推进到“≥ 期望”的第一个位置
   （galloping 下界跳跃）；若 > 期望，则基准过小，i0 推进到
   使 `base = pj[ij]-j` 可达的位置（同样下界跳跃）。每次只动必须动的游标。

每次比较至少让一个游标单向前进，游标总前进次数 ≤ 各表长度之和；
下界跳跃内部比较数为 O(log n + 前进跨度摊销)，整体比较次数 O(各表长度和)，
断言上界取保守的 `4*sum(len)`。k=1 退化为单词查询：每个位置都是一次命中。

**重叠计数推导**：命中后只推进 i0，t1..tk-1 的游标不越过任何位置，
因此连续相同词不会漏掉相邻起点。`A A A` 查 `A A`：base=0 时
p1=1=0+1 命中，i0→1；base=1，p1=2=1+1 命中 ⇒ 2 次。`A A A A` ⇒ 3；
`A B A B A` 查 `A B A`：起点 0（0,1,2）与起点 2（2,3,4）⇒ 2。

## 4. 布尔 AND

先选最短链为驱动链；对驱动链的每个文档号，在其余链做下界跳跃探测，
全在则命中。驱动文档逐个处理（≤ minLen），每条被驱动链每文档一次
`O(log)` 跳跃，比较次数上界保守取 `4*minLen*链数`。

## 5. 同位置多词元时的调整（本题不实现，仅说明）

若一个位置可挂多个词元，位置表变成“位置可重复”，需两处修改：
对齐时相等判定改为：对每个 j 存在某游标位置 = base+j（一个位置匹配
多个模式词时游标不能共享，需逐 j 独立选择）；命中后所有游标只前进到
“下一个 ≥ 当前基准位置”的元素，重复位置保留以枚举同一基准的不同词元
组合，重叠计数语义改为对所有词元组合计数。

## 6. 多路归并合并（merge）

k 段每词取链头（文档号最小者），用最小堆归并；同一文档号在多段出现时
合并其位置集合并去重（段不可变 ⇒ 跨段同文档才需合并）。删除位图中的
文档在归并时直接跳过。每条倒排项（一个 Doc）恰好被弹出一次、写入一次，
计数器断言 读次数==写次数==总 Doc 项数。新段写临时文件，成功 Rename，
崩溃则删除半成品并保留旧段。

## 7. 并发快照

段集合以不可变切片保存，`atomic.Pointer[[]*segment.Reader]` 替换；
查询取一次快照、全程使用，合并替换后新查询见新集合，绝不混合。
