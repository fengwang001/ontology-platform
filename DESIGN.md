# DESIGN: Java .properties parser/writer

## 1. 空白续行的推导（语义第 3 条）

依据 `java.util.Properties.LineReader.readLine` 的控制流推导：

1. 行尾奇数个 `\` 触发续行时，readLine 删掉该 `\`，置 `skipWhiteSpace=true`
   且 `appendedLineBegin=true`，继续读下一物理行。
2. 续行首部：空白（空格/`\t`/`\f`）被跳过；若紧接着就是行尾符，
   因 `appendedLineBegin=true`，这个行尾符**不被忽略**，走正常 EOL 分支。
3. EOL 分支中 `precedingBackslash=false` 且已累积内容非空，直接返回逻辑行。

结论：**只含空白或为空的续行不贡献任何字符，并且终止续行链**，
该物理行被消费掉，再下一行开始新的逻辑行。
例：`k=v\` + `   ` + `w=x` 得到 `k`→`v`、`w`→`x`。
与「续行首部空白被剥掉」的关系：剥离后该行为空段，空段没有行尾反斜杠，
按奇偶规则自然不再续行——空白剥离是原因，链终止是推论。

## 2. Store 转义的推导（语义第 6 条）

只转义「不转义就会被 Load 读丢」的字符，由 Load 端规则反推：

- 键：行首空白被忽略、键遇空白即结束，故键中**所有**空格/`\t`/`\f` 必须转义；
  `=` `:` 是分隔符，键中必须转义为 `\=` `\:`；
  `\` 是转义引导符，必须写 `\\`；
  行首 `#`/`!` 会让整行变注释，故**仅键首**的 `#`/`!` 必须转义（键总在行首，
  只需看键的第 0 字节），键中间的 `#`/`!` 无特殊含义不必转义；
  `\n` `\r` 是行结构符，必须写 `\n` `\r`。
- 值：分隔符后的前导空白被跳过，故**仅值开头**的空格/`\t`/`\f` 必须转义；
  值从起点一直延伸到逻辑行尾，中间与尾部空白都被原样保留，
  故值中间的空格不必转义；`#`/`!`/`=`/`:` 在值中无任何特殊含义，一律不转义；
  `\` 写 `\\`，`\n` `\r` 写 `\n` `\r`（否则破坏行结构）。
- 非 ASCII：Load 按 UTF-8 读回即可往返，故原样输出，不写 `\uXXXX`。

## 3. 语义-测试对照表

（待实现完成后补齐）
