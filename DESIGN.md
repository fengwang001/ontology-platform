# LSH 分桶检索器设计

## 1. 签名计算规则

对 d 维向量 x，第 t 张表有 b 个随机超平面，法向量 n_i（i=0..b-1）。
第 i 位签名位：

- `dot(x, n_i) > 0` → 1
- `dot(x, n_i) < 0` → 0
- `dot(x, n_i) == 0`（含零向量）→ 约定取 0（确定性、可复现）

位序：第 i 位写入 sig 的第 i 位（低位起），签名用 uint64（b ≤ 64）。
零向量合法：与任何超平面内积恒为 0，故其签名恒为全 0，全部落入 0 号桶。
含 NaN / ±Inf 的向量拒绝入库，计数为 skipped，可读回。

## 2. 可复现性

- 超平面由 `rand.New(rand.NewSource(seed))` 顺序生成，逐表逐位取标准正态
  （NormFloat64），种子显式传入，不读时钟、不读全局随机源。
- 同种子两次建索引：超平面逐字节相同 ⇒ 每个向量每张表签名逐字节相同（测试断言）。
- 不同种子：正态采样的法向量几乎必然不同 ⇒ 签名表存在差异（测试断言）。

## 3. 位数 b、表数 L 与候选数、召回的推导

设两向量角距离为 θ，单个超平面把二者分到同侧的概率 p = 1 - θ/π。

- 单表同桶概率：p^b。b 越大桶越细：候选数 ↓，但近邻同桶概率 ↓ ⇒ 单表召回 ↓。
- L 张表"或"起来，至少一张同桶：P = 1 - (1 - p^b)^L。L 越大召回 ↑，
  代价是候选并集变大（上界约 L·N·E[p^b]）、内存与哈希计算量线性增长。
- 候选数期望：E[C] ≈ N·(1-(1-E[p̄^b])^L)，p̄ 为全体向量对平均同侧概率。
  固定 L：b ↑ ⇒ 候选数单调 ↓（测试对 b=4/8/12 实测断言单调下降）。
  固定 b：L ↑ ⇒ 召回单调不降（测试对 L=1/2/4/8 实测断言单调不降）。
- 工程甜点：选 b 使桶规模远小于 N，再用 L 把召回抬到目标。
  测试断言精排距离计算数 ≤ 10%·N，保证不退化为暴力扫描。
- 哈希计算总量恰好 N×L×b（每向量每表算 b 次内积），计数器精确断言。

## 4. 落盘格式（自描述，全部小端）

```
[Header]   magic(4B "LSH1") dim(u32) L(u32) b(u32) nVec(u64) nBuckets(u64) seed(u64)
[Hyper]    L×b×d 个 float64（表序、位序、维度序）
[Data]     nVec 个 {id(u64), dim(u32), dim 个 float64}
[Buckets]  nBuckets 个 {table(u32), sig(u64), cnt(u32), cnt 个 id(u64)}
[CRC]      以上全部字节的 CRC32(IEEE) u32
```

截断分类（按落点区间）：

- 落在 Header 内 → ErrTruncHeader（头部不完整）
- 落在 Hyper 内 → ErrTruncHyper（超平面参数不完整）
- 落在 Data/Buckets 内 → ErrTruncBucket（桶表/数据不完整）
- 落在 CRC 内或 CRC 值不符 → ErrCRCMismatch

恢复：截断文件走 Recover，取"最大可恢复前缀"——头部与超平面必须完整，
数据区逐条解析到出错为止，桶表逐条解析到出错为止；桶引用了不存在的向量 ID
一律剔除并计数 dropped，保证恢复结果自洽、无悬挂 ID。
读回时校验每个法向量非全零，否则报 ErrDegenerate 并指出第几表第几位。
查询维度不符报 ErrDimMismatch，含期望与实际维度。四类错误均可 errors.Is 区分。

## 5. 并发

- 索引构建完成后整体发布（atomic.Pointer 交换），查询只读已发布快照：
  未完成的表要么全可见要么全不可见。
- 查询可多协程并发，共享只读桶表与向量存储，-race 干净。

## 6. 边界语义

- 空向量集：合法，查询返回空结果。
- 单向量 / K > N：返回 min(K,N) 个结果。
- 全同向量：全落一个桶，候选数 = N，召回 = 1（测试断言）。
- 维度为 1：合法，超平面退化为正负半轴划分。
- 零向量：合法，签名全 0（见 §1）。
- NaN / ±Inf：拒绝入库并计数 skipped。
