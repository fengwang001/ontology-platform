# 设计推导（CSV RFC4180 方言）

## 1. 空行与单列空值
记录只由换行终止：`a\nb\n` 是两条记录，故 `a\n\n` 是两条记录，第二条含 1 个**未引号空字段**；末尾换行不产生额外记录。
单列下 `Write([]{""})` 若写 `""+"\n"` 再 Parse 得到的是「未引号空字段」，引号标记丢失、且与空行无法区分。因此单列空字段**必须加引号**输出 `""+"\n"`；多列空字段不加引号（列数已消歧）。
**规范输入**定义：记录以 `\n` 终止（引号内 `\r\n` 原样）；单列空值必为 `""`；不含连续两个换行（无空行）。此集合上 Write(Parse(x))==x 逐字节。

## 2. 引号必要条件（最小引号）
满足任一即必须引号：含 `,`、`"`、`\r`、`\n`；或「该字段原文带引号」（保持标记）；或单列下为空。引号输出时 `"`→`""`。

## 3. par 切点
每段以两个假设各跑一遍同一状态机：OUT(段首在引号外、字段起始) 与 IN(段首在引号字段内)。逐字节输出事件流；段末再补一个「OUT+首字节 r」变体与「IN+首字节 q」变体，仅用于上一段恰以 CR-pending / 刚见引号收口时接续，二者只多读 1 个常数字节，总量仍 ≤2N+常数。
从左到右：上一段真实末态（outside / inside / afterquote / crpending）决定本段选哪条假设——outside→OUT，inside→IN；后两者分别在段首注入虚拟 `"`（喂给 OUT）或虚拟 `\r`（喂给 OUT）以复现 pending 分支；注入字节是常数、其产生事件按真实情况在拼接层改写坐标。
跨段引号字段：拼接层记录字段全局起点；内部段的 IN 事件流不产出新 Cell（处于同一字段），收口时把各段原始字节拼为 Raw，在最终 Cell 处一次性反转义。
坐标换算：每段维护本段内已完成记录数 cR、已完成 Cell 数 cF；段内事件坐标加上「前序段完成记录数」得全局记录号；跨段字段的字段号 = 字段起点所在记录在起点处已有的 Cell 数+1。错误随选中假设的事件流携带段内坐标，按同一基底换算，故与单线程完全一致。

## 4. CR pending
状态 P（CR 待定）仅在引号外见到 `\r` 进入：下一字节 `\n`→正常收尾；`,`→字段收尾；EOF→ErrBareCR（`\r` 位置）；其它→ErrBareCR（错误指向 `\r`，因孤立 `\r` 此刻已可判定）。半包 Feed 把 P 留在状态里等待下一片；par 段末为 P 时不做判定，由下段首字节按注入法决定。EOF 落在 P：记录（若有内容）先收尾，再报 ErrBareCR，已完成记录保留。

## 5. 上限「立刻」
字段字节计数：未引号每个到达字节 +1；引号内每个原始字节 +1（含 `""` 的两个引号、含字段内 `\r\n` 各 +1），不计外围两个引号；每加 1 立即与 MaxFieldBytes 比较，超限字节处即 ErrFieldTooLarge。记录字段数在每个 Cell 收口时计数并立即比较。记录总数在每条记录收口后立即比较。任何上限错误使解析器终态，后续 Feed/Close 返回同一错误。

## 6. 状态转移表（字节类别：C `,`｜Q `"`｜R `\r`｜N `\n`｜O 其它）
| 状态 | C | Q | R | N | O | EOF |
|---|---|---|---|---|---|---|
| F 字段起始 | emit空,F | go Q | go P | close | 存,B | emit空+close |
| B 未引号中 | emit,F | ErrQuote | go P | close | 存,B | emit+close |
| Q 引号字段中 | 存 | go A | 存 | 存 | 存 | ErrUnterminated |
| A 引号内刚见引号 | emit,F | 存",go Q | go P | close | ErrAfterQuote | emit+close |
| P CR待定 | emit,F | ErrAfterQuote | ErrBareCR | close | ErrAfterQuote | emit+close+ErrBareCR |

emit=收口当前 Cell（F 时空未引号 Cell；A 时带引号标记）；close=记录收口；引号 Cell 的 Start/End 记录原文起止（含两个外围引号）。记录号、字段号、字段号、字节偏移均从事件收口时的全局计数取得。单流实例非并发安全。
