# 多读者环形缓冲区设计推导

约定：记录序号从 1 开始，全局单调递增、永不复用。`head`=仍在环内的最旧记录序号；
`tail`=下一条将写入的序号（即“最新序号+1”）。容量 N。读者游标 `next`=它下一次要读的序号。

## 1. 满与空如何区分

不用单写指针与读指针相等来区分，而是额外维护两个单调量：已写入总数 `tail` 与已覆盖数 `head`
（二者都是逻辑序号而非槽位下标）。
- 一条没写：`tail==0`，于是必有 `head==0` 且 `tail==head`，可读区间为空。
- 刚好写满 N 条且未覆盖：`tail==N`、`head==1`，`tail-head==N-1`，区间含 N 条。
- 已开始覆盖：始终有 `tail-head==N-1`（N 条满），靠 `tail` 是否到达 N 判定是否发生过覆盖。
结论：空判据 `tail==0`；满判据 `tail-head==N-1 && tail>0`。槽位定位纯函数 `slot(seq)=(seq-1)%N`，
`slotring` 只做下标换算与槽位读写，不持有 head/tail，因此自身不判断覆盖。

## 2. 掉队判定边界

读者位置与写者的相对差记 `d=tail-next`（尚未读的条数）。写入第 `tail` 条时，被覆盖的是序号
`tail-N` 这一条。
- `next > tail-N`（即 `d<N`）：游标指向的记录还在环内，未掉队；追平是 `next==tail`（d==0）。
- `next == tail-N`（即 `d==N`）：游标指向“即将被覆盖的那一条”。本次写入完成后该槽被新记录
  覆盖，若仍读该槽会得到新数据——错乱。故按“写入完成后的环状态”判定，此刻起算掉队。
- `d==N+1`：游标已落后整整一圈多，错过不止一条，同样掉队。
分界：差 N 条是掉队起点（错过恰好 1 条），差 N+1 条错过 2 条，错过条数 `missed=head-next`。
`d<N` 永不错乱。为避免读者与覆盖同槽产生数据竞争，写者在每次写入后立即把 `next<=head-1`
的读者标记掉队（eager 标记），Read 只做只读核对。

## 3. 恢复到哪一条

恢复目标是“当前最旧可读记录” `head`。它应当被读到，而不是跳过：设掉队时报错过
`[next, head)` 共 `head-next` 条。恢复后把游标重置为 `next=head`，第一次 Read 返回序号 `head`，
此后 `head, head+1, …` 连续；报错前最后读到的是 `next-1`，恢复后首读是 `head`，中间
`[next, head)` 恰好等于报错告知的错过条数，语义连续。若跳过 head 从 head+1 读，则又静默丢一条，
违反不变量 3。

## 4. 注销读者对统计的影响

最慢落后量定义为“当前存活读者集合”上的统计。注销后该读者不再参与资源占用与后续核对，
保留其历史值会让统计与逐读者核对结果不一致（无法核对一个不存在的读者），违反不变量 5。
故注销必须从增量数据结构中立即摘除并重算（堆式增量维护下摘除为 O(log R)，不是全量遍历）。

## 写入位置与读取位置相对关系对照表（写入完成后的状态，N=容量，d=tail-next）

| 条件 | 状态 | Read 结果 |
|---|---|---|
| tail==0（环空） | 空 | ErrNoData |
| next==tail（d==0） | 已追平，暂无可读 | ErrNoData |
| 0 < d < N | 可读 | 返回序号 next 的记录，游标 +1 |
| d==N（next==tail-N，指向刚被覆盖条） | 已掉队（边界，missed=1） | ErrFellBehind |
| d>N | 已掉队（missed=head-next≥2） | ErrFellBehind |
| tail-head==N-1 且读者均 d<N | 满但未覆盖任何存活读者 | 正常可读，写入继续覆盖最旧槽 |
