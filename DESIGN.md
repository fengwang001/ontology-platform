# 规范化器设计推导

## 1. 两阶段模型
先做与策略无关的“基础规范化” B(x)：`\r\n` 与孤立 `\r` → `\n`；删除每行末的空格/制表符（纯空白行变空行，行保留）。
末尾策略只在 `Close` 时作用于最终输出（不改变前中间节，故不破坏幂等性）。
- Keep = B(x)。
- EnsureOne：B(x) 去掉全部末尾 `\n` 后加一个 `\n`。
- TrimTrailingBlank：去掉“末尾空行”——即末尾连续 `\n\n…`（最后一个内容行之后的多余换行）——再保证一个 `\n`；
  即输出最后一个非换行字节之后恰好一个 `\n`。等价地保留最后一行内容 + `\n`。

## 2. 空输入 / 只含换行（样例 × 策略）
| 输入 | Keep | EnsureOne | TrimTrailingBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `"\n"` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |
推导：EnsureOne 对 `""` 可选 `""` 或 `"\n"`。选 `""` 则 `N("\n")="\n"` 而 `N("")=""`，虽幂等但“确保一个”名不副实，
且空文档与单空行不可区分；选 `"\n"` 时 `N("\n")=N(N("\n"))="\n"`，三策略各自幂等，故选 `"\n"`。
TrimTrailingBlank 对空内容同样给 `"\n"`：空文档视为零个内容行，仍补一个收尾换行；`N("\n")="\n"` 幂等成立。

## 3. 被删字节的 ToOut
映射用三类区间（互不重叠，覆盖 [0,len]）：identity `[a,b)→[c,d)` 同长；delete `[a,b)` 落向单点 `c`；
insert `[d,e)` 纯输出。端点约定：所有映射在区间端点连续。被删原文区间 `[a,b)` 的每个字节 `i∈[a,b)` 取 `ToOut(i)=c`，
`ToOut(b)=d`（删除之后的输出位置）；其中 `\r\n` 的 `\r` 是长度 1 的删除区间，c=d=该 `\n` 的输出偏移。
由单调性：删除区间内不能映射到区间之前（会回退）也不能越过之后（会跳过保留字节），唯一选择是塌缩到同一点 c。
光标语义：行尾空白整体落在“换行符之前”（c=换行偏移），编辑器把删除空白中的光标放到行尾、换行符之前；
`\r\n` 的 `\r` 映射到 `\n` 自身位置（d=c+1 处的起点 c），即光标落在换行符上。
ToOrig：删除区间前的输出端点用“左倾”规则解析为该删除区间起点 a（光标优先落入被删空白的开头而非上一行末），
保证 `ToOut(ToOrig(o))=o`：identity/insert 的输出位置精确反解；端点由左右区间一致给出。

## 4. 缓冲超限（第 8 条）
待定空白链若无限缓冲，则恶意输入（10MB 无换行空白）撑爆内存；选择“拒绝”：
挂起空白字节数（自上一刷新点起）超过 MaxPendingWS 时返回带原文偏移的哨兵错误 ErrPendingLimit，
已 flush 内容保留，Normalizer 进入终态（再写返回 ErrClosed）。
强制判定为“非行尾”会把行尾空白输出出去，违反第 5 条（输出仍含行尾空白，二次规范化会变化），且若下一字节真是换行
则已无法撤回，也违反第 4 条（切点改变结果）——故拒绝是唯一同时保全 4/5/6 的选择。
输出超 MaxOutput：在准备追加前检查，超限即 ErrOutputLimit（带原文偏移），内容保留、终态。

## 5. 流式一致性
eol 为一个小状态机：Push 字节产出 0/1/2 个事件（NL,pos；其中 pos 为 `\r` 或 `\r\n` 起点；TEXT,pos）。
`\r` 先挂起；下一字节是 `\n` 则合并为一个 CRLF 行尾，否则挂起 `\r` 先以孤立行尾刷新。ws 只做分类：
空格/制表符挂起，见到非空白（先刷空白为文本）或行尾（丢弃挂起空白）才判定；Close 时挂起空白视为行中保留。
因此任意切分喂入得到同一事件序列（事件带原文偏移），满足第 4 条。

## 6. par 拼接
各段独立规范化只暴露“稳定前缀”：自段首到最后一个已确定行尾之后的位置 s（Keep 策略；最终段 s=len）。
段尾 [s,len) 只能是“最后一行”：待定 `\r` 或一串未判定空白，结构上有界于最后一行长度。
拼接时把上段残留 carry 与下段 [0,s') 合并重新规范化：能被换行确定的部分并入，新的残留继续传给下一段；
事件的 orig/out 坐标按 carry 起点、已累积输出长度整体平移。最后一段应用末尾策略。
修正范围每刀只触及“两相邻段最后一行”，与总长度无关（上界=2×最长行，K≤8 时总返工 ≤2·输入规模），
故映射区间数与单路一致、逐字节相同。

## 7. 映射查询
span 只在删除点/插入点打断 identity 区间，纯 `\n` 文本只有 1 个区间。ToOrig/ToOut 二分（sort.Search），
记录检查区间数于非导出计数器 lastChecks（含端点回退常数步）。
