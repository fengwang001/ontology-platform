# 设计推导

## 1. UTF-8 非法单元

从首字节期望长度 `k` 开始：续字节是 `80..BF`。第二字节同时承担越界与代理区约束；它不合法时，只把首字节判成一个非法单元，随后把第二字节作为新起点重新判定。第二字节合法后，后续位置若遇到非续字节，当前“合法但未完成”的前缀整体是一个非法单元，非续字节重新解析；流结束时残留前缀也整体算一个。

因此 `F0 90 80 41` 的前 3 字节是合法前缀后遇非续字节，合成 1 个单元；`E0 80 80` 中 `E0` 的第二字节不在 `A0..BF`，`E0`、`80`、`80` 各成 1 个；`ED A0 80`、`F4 90 80 80` 同理。`C0 AF` 中非法首字节 `C0` 独立，`AF` 作为游离续字节独立。`E2 82` 结束时是同一个未完成前缀，所以只 1 个。

| 首字节 | 第二字节合法区间 | 非法时吞掉字节 |
|---|---|---|
| `C2..DF` | `80..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `E0` | `A0..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `E1..EC` | `80..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `ED` | `80..9F` | 第二字节坏：1；后段坏：已积累前缀 |
| `EE..EF` | `80..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `F0` | `90..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `F1..F3` | `80..BF` | 第二字节坏：1；后段坏：已积累前缀 |
| `F4` | `80..8F` | 第二字节坏：1；后段坏：已积累前缀 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

## 2. 流式缓存

缓存上限是 3：一个首字节加至多两个已验证的续字节。非法首字节、非法第二字节、非续第二字节都能立刻判定，不等待；只有“首字节和已见续字节均合法但长度未满”时缓存。流结束把该前缀作为一个截断/非法单元。合法多字节字符未集齐前绝不产出，所以任意切分结果相同。

## 3. 并行切点

每段从 `max(0,start-3)` 建独立解析器，最多回看 3 字节恢复 UTF-8 状态；UTF-16 回看 2 字节。解析器产生的单元按“起始偏移”归属：只统计且输出起点位于 `[start,end)` 的单元。前一段可越界读到完成单元所需的固定尾字节，后一段遇到起点小于本段起点的单元则丢弃。

全局输入让边界两侧都能看到必要的有限邻域，因此切点在字符或非法单元中间不会复制或遗漏。`C3|80 80` 中前一段借看第一个 `80` 形成合法两字节字符；后一段回看同一字符后丢弃，再把第二个 `80` 判成游离续字节。非法第二字节只吞首字节，故后续字节都有明确起始归属。

## 4. 上限断点与检查次数

输出在每个完整标量或完整代理对之后提交。越限时保留此前输出，返回 `ErrLimit` 和“最后已输出边界”的绝对字节偏移；本次残留在切分缓存的字节不属于已提交边界，调用方从该偏移开新实例续传。错误中同时报告 `Write` 的相对 `n`，便于实现 `io.Writer` 约定。

每个新入流字节只检查一次；缓存保存已验证状态，不重新扫描。复杂度为 O(n)，复杂度为 O(n)，检查数 ≤ 2n。并行各段只回看固定 3（UTF-16 为 2）字节，总量 ≤ n+8C。
