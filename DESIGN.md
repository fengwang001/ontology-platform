# DESIGN — Set-Cookie 解析与适用性判定器

包依赖单向：`attr`（解析）与 `match`（匹配规则）互不依赖，`jar` 依赖两者。只用标准库，状态全在进程内存，时间只走注入时钟 `func() time.Time`。

## 1. 默认路径推导（对应语义第 2 条）

RFC 6265 §5.1.4：无 `Path` 属性时，默认路径取自来源 URL 路径中**最后一个 `/` 之前（不含）的部分**；若来源路径不以 `/` 开头，或最后一个 `/` 就是首字符（即之前部分为空），则默认路径为 `/`。

- `/a/b/c` → 最后一个 `/` 前是 `/a/b` → 默认 `/a/b`。
- `/a` → 最后一个 `/` 是首字符，前面为空 → `/`。
- `/` → 同上 → `/`。

为什么 `/a` 推出 `/` 而不是 `/a`：默认路径的语义是"设置者所在的目录"。`/a` 这个资源本身位于根目录下，它无权代表 `/a` 目录下的其他资源（如 `/a/admin`）设定 cookie 的作用范围；只有路径里至少还有一层 `/`（如 `/a/b`），才能说明设置者确实处于 `/a` 目录内。因此"最后一个 `/` 之前"为空时必须回落到 `/`。

## 2. 路径匹配不是前缀匹配（对应语义第 3 条）

朴素前缀匹配下，cookie 路径 `/a` 会命中请求路径 `/ab`——但 `/ab` 与 `/a` 目录毫无关系。越权实例：某应用在 `/a` 下存放了普通会话 cookie，攻击者若能控制 `/ab` 下的响应（或反之 `/ab` 是另一个信任级别更低的应用），就会收到本不该发给它的会话凭证，造成跨应用会话泄露。正确判据（RFC 6265 §5.1.4）：`reqPath == cookiePath`，或 `cookiePath` 是 `reqPath` 的前缀**且**（`cookiePath` 以 `/` 结尾，或 `reqPath` 在 `cookiePath` 之后的第一个字符是 `/`）。于是 `/a` 匹配 `/a`、`/a/`、`/a/b`，不匹配 `/ab`。

## 3. Domain 越权判定（对应语义第 4 条）

判据：设来源主机为 `H`，声明域为 `D`（忽略前导点、统一小写）。合法当且仅当 `H == D` 或 `H` 是 `D` 的子域（`strings.HasSuffix(H, "."+D)`），**且** `D` 含至少一个 `.`（即不是单标签域）。

- `b.example.com` 设 `Domain=example.org`：`H` 不以 `.example.org` 结尾也不相等 → 拒绝。
- 单标签域（如 `com`、`localhost`）必须拒绝：单标签意味着公共后缀级别（或内网裸主机名级别），允许它等于让任意站点设置覆盖整个 TLD 的 cookie，所有该后缀下的站点都会收到它，这是经典的会话固定/注入面。工程上不维护公共后缀表，"至少含一个点"是可判定且足够保守的近似。

无 `Domain` 属性时为 host-only：只匹配来源主机自身，子域不匹配。

## 4. 覆盖时保留最早创建时间（对应语义第 6、8 条）

身份三元组为 (name, domain, path)，同身份再设置是覆盖：值、属性、过期时间全部更新，但 `Created` 保留最早那次。发送排序规则是"路径更长在前，等长则创建时间更早在前"。若覆盖时刷新创建时间，则排序不再稳定：同一个 cookie 每次被服务端续期（覆盖）后都会排到等长路径组的末尾，发送顺序随刷新历史抖动；攻击者或后端只要反复重设某个 cookie，就能改变客户端的请求头顺序，依赖顺序的下游（日志、签名校验、调试复现）全部失效。保留最早时间让排序只取决于"这条身份首次出现的时间"，覆盖不改变相对顺序，结果可复现。

## 5. 其他关键决策

- `Max-Age` 与 `Expires` 同时出现以 `Max-Age` 为准（RFC 6265 §5.3）；`Max-Age <= 0` 表示立即过期，存入时直接删除该身份而非存一条死数据。
- 过期惰性剔除：查询适用 cookie 时按注入时钟判定并**真正从 map 中删除**，查询前后条数变化可观测；同一时刻连查两次结果一致（第二次没有可剔的）。
- 解析错误分类可区分：`ErrNoEqual`、`ErrEmptyName`、`ErrControlChar`、`ErrBadMaxAge`、`ErrBadExpires`；未知属性名一律忽略。
- 前缀约束：`__Host-` 需同时满足安全来源 + 无 Domain + Path 恰为 `/`；`__Secure-` 需安全来源。拒绝原因以可判定的错误值区分。
