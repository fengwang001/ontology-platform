# 设计推导

## 1. 非法单元切分（推导自七样例）
首字节声明长度 n（2..4）。先尽量收「合法前缀」：下一字节必须落在该首字节的**第二字节合法区间**（见下表）；其后每字节必须是 80..BF。
- 前缀合法、期望内出现非法字节：此前缀整体是**一个**非法单元（吞掉前缀全部字节），非法字节**退回重解析**。
- 首字节本身非法（80..BF、C0 C1 F5..FF）：单独一个非法单元（吞 1）。
- 前缀合法但流结束：残留前缀算一个单元（吞掉全部前缀字节），错误类型为「截断」。

验证：F0 90 80 41 中 41 不在 80..BF，前缀 F0 90 80 一个单元、41 合法 ⇒ FFFD 41。E0 80：80 不在 E0 区间，E0 一单元，80 退回为散落续字节一单元；下一个 80 再一单元 ⇒ 3。ED A0 同理（A0 不在 ED 区间）⇒ 3。C0 AF：C0 非法 1 个，AF 续字节 1 个 ⇒ 2。F4 90：90 超出 F4 区间，F4 与 3 个续字节各自成单元 ⇒ 4。E2 82+EOF：第二字节合法、仅缺第三字节，前缀是一个截断单元 ⇒ 1（不会把 82 拆开）。80 80：两个散落续字节 ⇒ 2。

| 首字节 | 第二字节合法区间 | 第二字节非法吞字节 |
|---|---|---|
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | —（散落续字节） | 1 |
| C0 C1 F5..FF | —（非法首字节） | 1 |

第二字节合法后：三字节第 3 字节 / 四字节第 3 字节非续字节时吞 2；四字节第 4 字节非续字节时吞 3（非法字节均退回）。

## 2. 切分缓存上限与即时判定
合法前缀至多 3 个续字节，故任何时刻缓存 ≤ **3 字节**（测试钉死该上限）。判定时机：首字节非法立即成单元；前缀收到区间外字节立即成单元并退回该字节——**不必等流结束**。只有「每一步仍合法、只缺长度」时才继续等待；等待期间绝不输出 FFFD。

## 3. par 切点对齐
切点任意。对内部切点，从切点向上回看：越过紧邻的续字节，再吞掉其首字节（回看 ≤ 3 续字节 + 1 首字节，常数）。前段在对齐点干净收尾，被回看的完整单元交给后段从头重解析。
重解析逐字节一致的依据：每个非续字节都是全局某单元的起点，且第二字节区间只依赖首字节，故重放出同样的「前缀+非法退回」。例：C3|80 80 回看后后段得 C3 80 80：C3 80 为合法字符、余 80 为一个非法单元，与单线程完全一致。切点落在非法单元中间（E0|80 80）时 E0 80 重放为一个单元，个数不变。

## 4. 第 9 条断点
输出在写每个标量前检查上限；超限则该标量（含代理对、FFFD）整体不输出，返回输出超限错误，解码器状态回滚、该字节退回输入。Write 返回的已消费数 = 该次输入中**已落定**（合法标量或非法单元）的最后字节下标；切分缓存里的待续字节未落定，**不算已消费**。调用方拼接：新实例先喂 Pending()（残留前缀），再从「输入起点 + 返回数 + 该次新增缓存数」处续喂，逐字节等价于不设上限。
