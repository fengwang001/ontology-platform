# 设计：半包续传与并行切分 CSV（RFC4180 方言）

## 1. 空行与单列空值
选择：**空行跳过**（不产生记录），末尾换行不产生记录。理由：CRLF/换行可能跨行
拼接，把空行当记录无法在半包边界稳定定义；跳过使「以换行结尾」幂等。
于是单列输入 `""\n`（唯一字段是加引号空字段）与未引号写法 `\n`（即空行）
必须可区分：前者是一条记录，后者被跳过。故单列表中唯一字段为空时，回写器
**必须写 `""`**——不写则输出空行，重新解析得到 0 条记录，丢数据。
规范输入定义：每字段仅在必要时加引号；字段内 `"` 写为 `""`；字段用逗号连接；
每条记录（含最后一条）以 `\n` 结束。引号字段内 `\r\n`/`\n` 原样保留。

## 2. par 切点
每段按「起点在引号外字段开始」(W0) 与「起点在引号字段内」(W1) 两种假设各
完整解析一遍（O(2N)，无回扫）。从左到右由前段结束状态选后段真实假设：
前段在引号内→本段取 W1（去掉其开头 FieldStart）；前段闭合引号后结束→本段
取 W0 但丢掉开头的「字段开始+空字段结束」对（该字段在前段已结束）；其余→
取 W0 原样。段内字节偏移、记录号、字段号均以段首为 0；偏移回放时加段首偏移；
记录号/字段号由 `table` 累加器在回放事件时统一计数，故错误天然落全局坐标。
段边界错误（如孤立 `\r`、末段未闭合引号）由终态/前段事件判定，位置即切点-1
或段内首字节，同样经累加器定位。

## 3. CR 待定（CR）
未引号字段（含字段开始与引号闭合后）见 `\r` 进 CR：仅当下一字节为 `\n` 才是
行尾；其后为其他任意字节（含逗号、引号）或流结束，一律 ErrLoneCR。半包：
状态机保留 CR，`Feed` 边界不丢字符。par：CR 若落在段尾，前段报「CR 结束」
终态；后段首字节决定按上述规则回放，无后段（流末）则孤立 `\r` 报错。
引号内 `\r` 一律为内容，不进 CR。

## 4. 上限的「立刻」
字段字节计数在每一个进入字段值的字节到达时自增并比较：引号字段内 `""` 两字节
只在闭合引号被识别后计 **1** 个值字节（实现：进入转义时暂缓计入第 2 字节）；
字段一旦结束立即复位，不缓冲整条记录。字段数在每个字段开始时计数；记录数在
每条记录落定时计数。任一超限即刻返回哨兵错误，已产出记录保留，解析器进入
终态，后续 Feed 返回同一错误。

## 5. 状态转移表
字节类：`,` `\n` `\r` `"` 其他 C。动作：F=字段结束(逗号)/R=记录结束/
S=字段开始/Q=入引号内容/E=转义引号出值；err 标注见 §6。

| 状态 | , | \n | \r | " | C |
|---|---|---|---|---|---|
| S 字段开始 | S, F+S | S, R | CR | Q | U |
| U 未引号中 | S, F+S | S, R | CR | err BadQuote(偏移) | U |
| Q 引号中 | Q(内容) | Q(内容) | Q(内容) | X | Q(内容) |
| X 引号中刚见引号 | S, F+S | S, R | CR | Q, E | err TailQuote |
| CR 行尾待定 | err LoneCR | S, R | err LoneCR | err LoneCR | err LoneCR |

EOF：Q/X→ErrUnclosedQuote；CR→ErrLoneCR；S/U→收尾最后字段与记录
（空流不收）。`""` 空字段在 Q→X 即标记 quoted 且值为空。

## 6. 四类语法错误（可判定哨兵）
ErrBadQuote 未引号字段中出现 `"`；ErrTailQuote 引号闭合后紧跟非法字符；
ErrUnclosedQuote 流末引号未闭合；ErrLoneCR 孤立 `\r`。
上限：ErrFieldTooLarge / ErrTooManyFields / ErrTooManyRecords。
错误带字节偏移(0起)、记录号、字段号(1起)。单流式实例非并发安全。
