# 设计说明

## 1. 数据模型

- `name.Namespace`：内存名字集合，`sync.RWMutex` 保护；持锁期间完成整批执行，
  并发修改**阻塞等待**（非拒绝），因此批量执行期间不可能有并发写入。
- 名字为任意非 nil 字符串：空串合法；`/`、`\0` 等按普通字符处理，不做路径语义。
- 请求 `Req{From, To}` 先按 `(From,To)` 归一化；`From==To` 为**自环**。

## 2. 四类冲突（全部在任何修改之前检出）

1. 目标名已存在且本批不会搬走它：`ErrTargetExists`（指出 To）。
2. 两个不同 From 指向同一 To：`ErrDupTarget`（报两个 From）。
3. 同一 From 出现两次且 To 不同：`ErrDupSource`（报两个 To）。
4. From 当前不存在：`ErrMissingSource`（指出 From）。

自环 `a→a` 定义为**无操作**：直接保留，不参与排序、不计步骤、不触碰命名空间，
也不参与上述冲突（它既不需要 a 存在之后“搬走”，也不占用目标）。

## 3. 排序规则推导（拓扑序）

对非自环请求，边 `From → To`。若 `To` 仍是某条**尚未执行**请求的 From，
先执行本步会覆盖该名字，故本步依赖 `To → …` 先执行。即依赖边为
`From→To ⇒ To 那条边先跑`，对依赖图做拓扑序。

每个 From 至多一条出边、每个 To 至多一条入边（冲突 2/3 已排除），
故图只由不相交的**链**与**环**构成：

- 链 `a→b→c`：从终点侧开始，`b→c` 先，`a→b` 后。
  Kahn 算法：把“To 不是任何未执行 From”的边放入就绪队列，输出后移除。
- 同一就绪层按 From 字典序，保证打乱输入后步骤序列逐元素相同。

查找均在 map 上 O(1) 完成，计数器每次 map 读 +1；总读次数 ≤
4(R+N)（R=请求数，N=涉及名字数）：建图 2R、就绪判定 2R、输出 R、查环 ≤N。

## 4. 破环通用做法与临时名数量

环上任何一步先执行都会覆盖。通用做法，每个环只需 1 个临时名 t：

`a→t, x_k→…→x_1→…, t→x_k`

即任选环上一个节点 a：先把 a 移到 t（腾出 a），再沿环逆序搬完其余节点，
最后 t 搬到 a 的目标。二圆环：`a→t, b→a, t→b`；
三元环 `a→b,b→c,c→a`：`a→t, c→a, b→c, t→b`。

每步的目标名在执行前都不存在（t 从未出现；逆序搬时目标已被上一步腾空），
故中途零覆盖。每个环独立、只需一个临时名，c 个互不相交环恰好 c 个临时名。
临时名由 `cycle.TempName` 生成，候选集为两套前缀族
`.rename-tmp-<n>`、`.rename-tmp2-<n>`（n 从 0 递增，各 65536 个），
每次对“现有名 ∪ 所有 To”集合校验，冲突即重试；全部耗尽才返回 `ErrNoTempName`。

## 5. 执行、日志与撤销

日志文件格式（`os.CreateTemp` 建在临时目录）：

- 头部：`RNLG1`(5) + 大端 uint32 总步数(4) + CRC32(4，覆盖头9字节与全部帧)
- 每帧：0x01(1) + 大端 uint16 长度(2) + JSON{from,to,ord}(len)
- 尾部：CRC32(4) + 魔数 `ENDLOG1`(6)

执行：持写锁 → 冲突检测（此时命名空间可还原）→ 排序/破环 →
逐步原子改名并追加帧。注入失败时已执行步按日志逆序回滚，命名空间恢复原状，
删除日志文件。成功后提交日志路径。

撤销：读帧逆序执行 `to→from` 强制改名。重复撤销幂等：日志改名加 `.done`，
已 `.done` 即无操作返回。截断分类（逐字节）：头部 13 字节不齐 →
`ErrShortHeader`；某帧 0x01+len+JSON 不齐 → `ErrBadRecord`；
帧齐但尾部 CRC/魔数缺失或不符 → `ErrCRC`。始终按“最大可恢复前缀”撤销，
返回 `Result{Recovered, Total, Missing []int}` 报告无法撤销的步骤序号。
