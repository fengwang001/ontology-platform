# 设计：连接顺序的代价模型与计划选择器

模块 `ontology`，仅标准库，状态全在进程内存。包结构：`stats`（统计与选择率）、
`cost`（代价公式）、`plan`（计划表示 + 子集 DP）、`explain`（文本渲染）、
`catalog`（登记与异常检测）、`cmd/demo`（演示）。

## 1. 并列打破（等代价判定）的推导

代价是 float64，由加法（扫描代价、连接代价累加）与乘法（基数 = 左基数 × 右基数 ×
选择率）构成。IEEE 754 双精度 `ulp(1) = 2^-52 ≈ 2.22e-16`。一次 n 表计划的代价最多
经历 `2n-1` 次加法与 `n-1` 次乘法；每步相对舍入 ≤ 2^-53（就近舍入），n ≤ 16 时累计
相对误差上界约 `3n · 2^-53 ≈ 5.3e-15`。因此数学上相等的两个代价，朴素累加的结果差
可能达到若干 ULP（约 1e-15 相对量级），直接用 `==` 比较会让选择随枚举顺序漂移。

判定策略（`plan.costEqual` + `plan.better`）：

1. 先按相对误差判等：`|a-b| <= 1e-9 * max(1, |a|, |b|)` 视为等代价。
2. 等代价时比较计划的中序叶子表名序列（`Node.Key`），字典序小者胜。

阈值依据：1e-9 比累计舍入上界（约 1e-14）高 5 个数量级，足以吸收一切重结合误差；
同时统计信息本身是估计值（行数、NDV 的测量误差远大于 1e-9 相对），在 1e-9 之内区分
两个计划属于虚假精度，判等不损失语义。该比较与"名字序打破"合起来构成严格弱序：
任何两个候选都有唯一确定的胜者，与 DP 枚举顺序、表登记顺序无关，故结果可复现。
测试构造：星型谓词 A-B、A-C，取 `rows(B)=3, NDV(AB)=3, rows(C)=7, NDV(AC)=7`，
则 `(A⋈B)⋈C` 与 `(A⋈C)⋈B` 数学上同代价，但 `fl(3·(1/3)) ≠ fl(7·(1/7))` 差约 1 ULP；
断言选中名字序更小者，且登记顺序打乱 20 次后 explain 输出逐字节相同。

## 2. 笛卡尔积推迟规则

对谓词图（表为点、谓词为边）在每个子集 S 上求连通分量：

- S 有 k > 1 个分量：S 的最优计划 = 各分量最优计划按分量最小表名字典序依次做
  笛卡尔积（k-1 次），不再考察任何划分。笛卡尔积只可能出现在这里——即"剩余表与
  已选集合之间无任何谓词"的情形，且必然位于计划树最顶端（最后一步）。
- S 连通：只考察这样的划分 (S1, S2)：S1 含锚点（S 中最小下标表，避免重复划分）、
  S1 与 S2 各自在谓词图上连通、且 S1 与 S2 之间至少有一条谓词。两侧均不连通子集
  的划分被排除，因此连通子集的最终计划里绝不出现笛卡尔积。

可终止性：连通图删去生成树的任一叶结点，得到"叶子 | 其余"的合法划分，故连通子集
必有候选，DP 不会卡死。链式图 A-B-C-D 全集连通 ⇒ 无笛卡尔积；{A,B}、{C,D} 两段
不连通 ⇒ 全集恰有 2 个分量 ⇒ 恰好 1 次笛卡尔积且在根（最后一步）。

## 3. 基数估计公式与独立性假设

- 扫描代价：`scanCost = rows`；连接代价：`joinCost = leftCost + rightCost +
  leftCard · rightCard`（无索引嵌套循环模型）。
- 连接基数：`card(S1 ⋈ S2) = card(S1) · card(S2) · Π sel(p)`，p 取遍横跨 S1/S2 的
  谓词；无横跨谓词（笛卡尔积）时 Π 为空积 = 1。
- 等值谓词选择率：`sel = 1 / max(NDV_l, NDV_r)`（标准 containedness 假设）；任一列
  缺统计时回退到写死的 `DefaultSelectivity = 0.1` 并在 explain 标注"估计不可靠"。
- 独立性假设：同一对集合间有多个谓词时选择率直接相乘，explain 标注
  "independence assumed"。适用范围：各谓词列近似不相关时估计合理；列相关（如
  城市/邮编）时会低估基数，本模型不处理相关性，仅在输出中显式声明该假设。
- 估计只读取 `stats` 中的聚合量（行数、NDV、直方图），不接触行数据；
  `stats.RowAccesses()` 计数器恒为 0，测试据此断言。

## 4. 复杂度与资源上限

按子集 DP：子集数 ≤ 2^n，划分数 ≤ 3^n（每表三态：左/右/不选），远小于 n!。
硬上限 `MaxTables = 16`：2^16 个计划槽位，内存有界（约数 MB）；n = 20 直接返回
`ErrTooManyTables`，不分配 DP 表。`Optimize` 每次调用使用独立状态，共享只读
catalog，可多协程并发，`-race` 干净。

## 5. 统计异常与边界语义

- 缺失：列无统计 ⇒ 选择率回退 0.1，计划节点标 `Unreliable`，不中止。
- 过期：`|statsRows - catalogRows| / catalogRows > 0.1` ⇒ 标 `Stale` 并按目录行数
  校正；阈值 10% 是经验值，小于该比例的行数漂移对连接顺序影响可忽略。
- 损坏：直方图桶计数和 ≠ 声称总行数，或桶边界非严格递增 ⇒ 返回包装
  `ErrCorruptStats` 的错误，含表名与列名。三类异常分别包装
  `ErrMissingStats / ErrStaleStats / ErrCorruptStats`，可用 `errors.Is` 区分。
- 边界：零表 ⇒ `ErrNoTables`；单表 ⇒ 直接返回扫描计划；自连接谓词 ⇒ 登记时
  `ErrSelfJoin` 明确拒绝（同一物理表的多副本应以别名登记，本模型不支持别名）；
  行数为 0 或 1 均按公式正常计算。
