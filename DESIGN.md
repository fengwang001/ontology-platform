# DESIGN

## 1. 零计数时的行号

GNU 统一格式的 hunk 头用该 hunk 在旧文件中覆盖的行范围的 1 基起点 a 与行数 b。
hunk 可能覆盖空范围，空范围落在某两行之间时无法用范围起点表达，GNU 的约定是：
起点写成空范围前一行的行号；空范围在文件开头之前写 0。

- x 变为 y,x：插入点在第 1 行之前，前面没有行，旧侧 -0,0，新侧从第 1 行开始 +1。
- a,b,c 变为 a,b,X,c：插入点在第 2 行之后、第 3 行之前，空范围前一行是第 2 行，旧侧 -2,0，新侧首行 X 是第 3 行，+3。
- a 变为空：删除最后一行后新文件为空，新侧空范围位于第 0 行之后，+0,0，旧侧为 -1。

统一规则：b=0 时 a 等于插入点之前最后一行的行号（开头为 0）；b>=1 时
a 等于第一行（上下文或删除行）的 1 基行号。Apply 看到 b=0 时，把插入点放在
第 a 行之后（a=0 即文件开头）。

| 旧侧 | 书写 | Apply 插入/匹配点 |
| --- | --- | --- |
| b=0 | -a,0，a 为前一行行号（开头 0） | 在第 a 行之后插入 |
| b=1 | -a（省略 ,1） | 匹配第 a 行这一行 |
| b>=2 | -a,b | 从第 a 行起匹配 b 行 |

新侧 c,d 规则完全对称，仅用于渲染；Apply 只依据旧侧定位。

## 2. hunk 合并阈值

阈值是 2C：相邻两段改动间未改动行数为 g，g <= 2C 合并，g > 2C（即 g>=2C+1）分开。

推导：每个 hunk 在改动两侧各带最多 C 行上下文。合并后中间共享的未改动段是
后 hunk 的前文加前 hunk 的后文。若 g=2C+1，分开时中间段被切成 C / 1 / C，
中间那 1 行两边都收不下，因此必须分开；g=2C 时恰好每侧 C 行、无剩余，必须合并。
例 C=3：g=6 合并（中间正好 3+3），g=7 分开（剩 1 行无归属），与 diff -U3 一致。
若取阈值 C，则 g=4..6 本应合并却被分开；若取 2C+1，则 g=2C+1 本应分开却被合并。

## 3. Myers 并列最短脚本的选择

固定选择优先删除（down 边）：同一 (V,d,k) 上 down 与 right 等长时，先取 down。
例 a b 变为 b a（D=2）：优先删除得到 -a、空格 b、+a；优先插入会得到
+b、空格 a、-b。两者同为最短，本实现永远输出前者，因此补丁逐字节确定。
实现上用标准 Myers：正向 V[k]，d 从 0 递增；每步 k 先按 down 规则取值，
随后沿 snake 前进。编辑距离上限 Dmax 触发时返回 ErrDistanceExceeded。

## 4. 偏移后的后续 hunk

第 i 个 hunk 在记录位置之外以偏移 delta（实际匹配起点减记录起点）成功后，
第 i+1 个 hunk 的查找中心整体平移 delta：搜索中心 = 原记录起点 + 已累计偏移。
原因：偏移说明目标文本相对基准整体插入/删除了若干行，后续 hunk 的真实位置随之
整体移动；累计平移使搜索窗口跟随真实位置，而搜索半径 F 仍只覆盖相对上一 hunk
落点的额外抖动。多候选取离中心最近，等距取靠前（行号小）。

## 5. 其余约定

- 行：Split 保留原始行尾（LF/CRLF/无），内容含行尾；Join 原样拼回。
- 编辑脚本为操作序列 Equal/Delete/Insert，每操作携带整行（含行尾）。
- 行尾标记：旧/新最后一行无 LF 时，在对应空格/减号/加号行后输出
  反斜杠空格 No newline at end of file。仅行尾差异（a 与 a+LF）因此非空。
- 计数器：Myers snake 中逐行相等比较的次数（每次比较计 1，含失败比较）。
- 错误四类可判定：ErrMalformed、ErrContextMismatch、ErrOffsetExceeded、
  ErrDistanceExceeded；另有限额错误 ErrTooLarge。
