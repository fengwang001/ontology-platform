# DESIGN.md：可分裂二维网格索引

## 1. 坐标与边界归属（左闭右开）

- 点 `p` 属于格 `[x0,x1)×[y0,y1)` 当且仅当 `x0<=p.X<x1 && y0<=p.Y<y1`。
- 左/下边界上的点属于本格；右/上边界上的点**不属于**本格（属右邻/上邻格；落在整个根格右/上界的点越界拒绝）。
- 四个角点：左下角 `(x0,y0)` 属本格；左上/右下角有一维是上界，不属本格；右上角两维皆上界，不属本格。
- 分裂线 `mx=(x0+x1)/2`、`my=(y0+y1)/2`：`p.X==mx` 时 `p.X<mx` 为假，点落右侧子格（NE/SE）；`p.Y==my` 落上侧子格（NW/NE）。
  因此恰在横竖分裂线交点上的点唯一归属 NE（右上子格）。
- 查询矩形同样左闭右开：点在查询左/下边界→命中；在右/上边界→不命中。退化查询 `q.x0==q.x1`（竖线）或两维都相等（点）时，没有任何点满足 `<` 上界，结果恒为空，合法。
- `+0.0` 与 `-0.0` 按 IEEE754 `==` 视为相等，归属相同。`NaN` 与任何坐标比较都为假，无法归属，插入时拒绝并计入 `Skipped`；`±Inf` 同样拒绝（根格为有限矩形）。

## 2. 格结构与分裂终止条件

每格保存：边界、点切片、四个子格（分裂后非 nil）、深度、点数缓存。
叶子格点数超过容量 C（C=32）时在两维中点分裂成 SW/SE/NW/NE 四子格，点按第 1 节规则全部下沉。

全同坐标无限递归的推导：若所有点坐标相同，中点规则会把它们全部放进同一个子格（`x==mx`→右侧；最终 NE），其它三子格为空；该子格仍有 n 个点，再次超容量，于是无限递归。
终止条件（任一即不再分裂）：

1. 数值不可分：`mx==x0 || mx==x1` 或 y 同理（中点舍入后已等于端点，浮点宽度耗尽）；
2. 深度上限 D（D=48）：D 次二分使宽度约缩小 2^48，超过 float64 在该量级的分辨力，条件 1 通常先触发；D 为防御性硬上限。

达到终止条件的叶子成为“溢出叶”，可容纳超过 C 个点；查询与持久化均不依赖容量上限。

## 3. 网格与一致性

根格为固定有限矩形（构造给定）。插入：定位叶子→追加点→超容量且可分时分裂并把该叶全部点重新归属。删除：定位叶子删除同 ID 点。
并发用 `sync.RWMutex`：查询持读锁遍历整树，插入/分裂/删除持写锁。分裂（点移动）在临界区内一步完成，读者要么看到分裂前整叶、要么看到分裂后四子格，点不会在移动中途被漏读。

## 4. 范围查询剪枝与复杂度

自根 DFS（计数器：访问格数 `cells`、逐点判定数 `points`）：

- 格矩形与查询相离→跳过（不计 cells）；
- 格矩形完全包含于查询→整取该格全部点（叶子全收；内部格递归整取），**0 次逐点判定**；
- 相交但不包含→叶子逐点判定，内部格递归。

10 万点均匀分布、查询约 1% 面积：叶宽 ≈ (C/n)^0.5 ≈ 0.0179；与查询相交叶数 ≈ (0.1+2·0.0179)^2/0.0179² ≈ 总格数的 1.7%（<10%）；
被逐点判定的点 ≈ n·(0.136²) ≈ 1.9%（<5%）。

## 5. 删除不合并的理由

合并需等四个子格都变叶子且总点数低于阈值；插入/删除抖动会反复触发分裂—合并，代价高且产生延迟尖峰；题目不要求。故只分裂不合并，点数缓存始终准确。

## 6. 持久化格式（小端；CRC32-IEEE）

```
Header: magic "ONTGRID1"(8) version u8(=1) cap u16 maxdepth u8
        root x0,y0,x1,y1 float64(32) skipped u64 = 49 字节
Record: tag u8(1=leaf,2=node) depth u8 x0,y0,x1,y1 float64(32) count u32 crc u32 = 42 字节头
        leaf 紧跟 count 点：x,y float64 + id u64 = 24 字节/点
node 的四个子记录按 SW,SE,NW,NE 前序紧随。
文件尾：eof u8=0xFF + 整文件 CRC32 u32（不含其自身 4 字节）。
```

截断分类（k=被截掉的尾部字节数）：

- `1<=k<=4`：尾 CRC/eof 被截 → CRC 不匹配；
- `5<=k<=49`：头部不完整（不足 49 字节）；
- k 落在某记录 42 字节头内→格记录头不完整；落在点数据内→点数据不完整；落在记录 CRC 4 字节→CRC 不匹配；
- k 恰好截在记录边界→有效完整前缀，恢复成功。

恢复语义：解析到第一个不完整/损坏记录为止，此前前序完整读回的格构成“最大可恢复前缀”；其查询等于“只含这些格”的树。
结构不变量（CRC 通过后校验，错误指出格路径与维度）：node 必须四子格齐全；子格边界必须精确等于父格中点划分（越界报 x/y 维）。
崩溃安全：写 `name.tmp`→fsync→`rename`；加载时发现残留 `*.tmp` 识别为未完成写入并清理。
