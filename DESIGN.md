# 基于版本向量的多副本冲突检测器 — 设计

## 1. 版本向量与偏序
向量 V: 副本ID → uint64 计数器。缺失分量语义为 0：`{A:1}` 与 `{A:1,B:0}` 是同一向量。
比较取两向量 key 的并集 U（不遍历全部历史副本）。对每个 k∈U 取 a=V1[k], b=V2[k]（缺失记 0），
记 lt = ∃(a<b)，gt = ∃(a>b)，计数恰好 |U| 次分量读取（≤ 2·|U|）：

| 条件 | 关系 |
|---|---|
| !lt && !gt | Equal（含两向量全 0） |
| lt && !gt | Less（V1 因果在前） |
| !lt && gt | Greater（V1 因果在后） |
| lt && gt | Concurrent（必须同时存在更小与更大分量） |

并发不是“不相等”：`{A:1,B:0}` vs `{A:0,B:1}` 同时有更小和更大分量 → Concurrent；
`{A:1}` vs `{A:2}` 只有更小分量 → Less。

## 2. 存储与写入
每个 key 持有一组兄弟 Entry{Vec, Value}，并维护 watermark W（已并入条目的逐分量最大值）。
副本 r 本地写入：其计数器 r 分量 +1（`W[r]+1`），结果向量严格大于本副本任何旧版本。

## 3. 同步合并规则（逐条 key，对入条 e）
先对 e.Vec 做回退检查：对每个 k 满足 e[k] < W[k] 的分量，要求 e 在其余分量上不超过 W
（即 e 是 W 的因果前缀），否则说明计数器相对已知水位回退 → ErrCounterRollback。
然后将 e 与现有兄弟集合 S 逐一比较（以并集语义）：
- 存在 s∈S 使 e < s：忽略（严格小于）；e 等于某个 s：无操作。
- 存在 s∈S 使 s < e（且无 s 使 e < s）：丢弃该 s；最终 S 只保留与 e 并发的兄弟并入 e。
- 即：合并后 S 中任意两个版本必须并发；祖先版本被严格更大的版本取代。

## 4. 兄弟上限与确定性丢弃
上限 m（构造给定，m≥1）。兄弟始终按 Value 字典序升序存放。
并入新兄弟后若 |S|>m，丢弃 Value 最小的最旧兄弟，每丢弃一次 dropped++。
m=1 时只保留 Value 字典序最大者，即确定性的最后写入者胜（LWW，平局由 Value 字典序决定）。

## 5. 收敛性
合并是集合上的确定性函数（输入=并集，输出排序去祖先，超限按同一规则丢），故交换、结合、幂等。
只要每对副本之间双向同步各至少一次，三方最终拥有同一组兄弟，状态逐字节相同。

## 6. 裁剪退役副本
Prune(V, retired) 删除退役分量。安全性判定：对任意一对待保留向量 Vi,Vj，
关系必须满足 rel(Vi,Vj)==rel(prune(Vi),prune(Vj))；若有一对关系改变 → ErrPruneUnsafe，拒绝裁剪。
直觉：仅当被删分量从未在这组向量中提供过 lt/gt 判定信息时裁剪才安全。

## 7. 故障与错误（errors.Is 可区分）
- ErrUnknownReplica：登记集合非空时，向量/条目含未登记副本 ID（文档定义为报错，而非按 0 处理）。
- ErrOverflow：`W[r]` 已为 math.MaxUint64 时递增，拒绝，不回绕。
- ErrCounterRollback：见 §3 回退检查。
- ErrPruneUnsafe：见 §6。
- 编解码：CRC32(IEEE) 校验；截断点三类——字节不足读头部/长度 = ErrHeader；长度声明超出体 = ErrEntry；
CRC 不符 = ErrCRC。

## 8. 边界
零副本：无任何 key；单副本：其任意两个版本只有 Equal/Less/Greater；全 0 向量两两 Equal；
同副本连续写入严格递增；m=1 见 §4。
