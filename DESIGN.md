# 版本约束求解器设计推导

## 1. 选哪个版本（不变量 2 的推导）

要求：同输入重复求解逐位相同，且与登记顺序、约束声明顺序无关。
登记顺序是外部、任意的输入，不能作为 tie-break；唯一对所有输入天然存在
且与登记无关的全序是「版本号自身的顺序」与「包名的字典序」。
结论：
- 候选版本集合登记后立即按版本号**升序**规范化存储（抹掉登记顺序）；
  求解时在可行域内按**版本号降序**尝试，即固定选「满足约束的最高版本」。
- 选包顺序固定为包名字典序；约束在传播前按 `(包名, 约束字符串)` 排序。
任何同构输入（仅顺序不同）经规范化后状态完全一致，故输出逐位相同。
「最高可行版本」也是包管理器通行语义；最高版本与其下游冲突时回溯到更低
版本，选择规则仍是函数式确定的。

## 2. 预发布版本

比较：`主.次.修` 相同的版本，无预发布标识 > 任意预发布；预发布标识按点分
标识符比较：纯数字按数值比且数字标识符 < 混合/字母标识符，否则按 ASCII
字典序，段数少的更小。即
`1.0.0-alpha < 1.0.0-alpha.2 < 1.0.0-beta < 1.0.0-rc.1 < 1.0.0`，
而任意 `1.0.0-... < 1.0.1`（数字段优先）。

是否参与约束：**参与**。区间只按端点闭开判定，不为预发布开后门。
`>=1.0.0` 下端是闭端点 1.0.0，而 `1.0.0-beta < 1.0.0`，故不满足；
`>0.9.0 <1.0.0` 则能容纳 `1.0.0-beta`。规则只有一条（比较 + 闭开），
比较与判定自洽，无需「正式版才匹配」这类特例。

## 3. 区间端点闭开规则

单条约束是半（无）界区间：`>` 开、`>=` 闭下端；`<` 开、`<=` 闭上端。
求交：下端取「更严」者（值更大，值相同则开优先于闭），上端同理
（值更小，值相同开优先于闭）。空：下端点 > 上端点，或两端点为同一版本
且至少一端为开。

| 区间 A | 区间 B | 交 | 理由 |
|---|---|---|---|
| `>=1.2.0` | `<1.2.0` | 空 | 同点，上端开 |
| `>=1.2.0` | `<=1.2.0` | `{1.2.0}` | 两端皆闭，恰含一点 |
| `>1.2.0` | `<1.2.1` | 数学非空；有候选（如 `1.2.1-rc1`）才可行 | 预发布落在两点之间 |

注意区分：区间非空（存在版本点）与图中无候选版本是两件事；
`1.2.1-rc1 < 1.2.1` 且 `> 1.2.0`，注册了它才有可行候选。

## 4. 冲突解释选哪一条

搜索是确定性的（字典序选包、降序选版本、约束排序后传播），失败分支唯一。
在每个包的域上维护其下界/上界的**来源约束链**：根需求来源为空，传播某版本
的依赖时，来源前缀为「父包@版本 → 该依赖」。域变空时，相撞的上下界来源
链拼接即冲突链，随 `ConflictError` 上抛；回溯只展开选择、不丢弃已构造的链，
最终报告**第一次把根域整体判空的分支**上按排序最先出现的空域冲突。
遍历顺序确定，故同输入报告逐位相同。

## 5. 搜索与剪枝

回溯 + 前向检查（传播）：每确定一个包@版本，把其约束全部并入相关包的可行域
（区间求交），任一域立即为空则剪枝；域随递归快照/回滚，撤销的选择不留痕迹。
计数器只统计「真正尝试过的包@版本组合」。可达性：从根需求做闭包，只在可达
集合内求解与出解。循环依赖按「域」传播而非按访问 DFS，天然收敛、不死循环。

## 6. 错误分类（互不相同、可判定）

- 版本/约束语法非法：`ver.ErrSyntax`、`rng.ErrSyntax`（不同哨兵）。
- 登记重复：`graph.ErrDuplicateVersion`，失败不改既有数据。
- 约束引用未登记包：求解期 `solve.UnknownPackageError`（具名类型）。
- 无解：`*solve.ConflictError`（含冲突链）；超限：`solve.ErrSearchBudget`。
