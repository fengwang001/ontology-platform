# 设计推导（行尾/空白流式规范化 + 双向偏移映射）

规范化只做三件事：行尾统一为 `\n`；删除每行末尾的 ` \t`；按策略处理文件末尾换行。永不新增字节。

## 1. 空输入与只含换行的输入

策略：`Keep` 保留原样；`EnsureOne` 已有末尾换行则压成恰好一个，无则不新增；`TrimBlank` 删除全部末尾空行但保留至多一个换行。

关键约束是第 6 条 `ToOut(ToOrig(o))==o` 对所有 `o∈[0,len(out)]` 成立。若规范化**新增**一个换行，该输出字节没有任何原文字节可对应：任何 `ToOrig` 取值 j 都使 `ToOut(j)` 落在旧区间内，无法等于新终点；任何「钳到终点」的约定又使旧终点失去互逆性。故三种策略都不能新增字节。因此 `EnsureOne("")=""`（而非 `"\n"`）：`""→"\n"` 虽幂等却破坏映射互逆；`""→""` 两性质同时成立。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等：输出只含 `\n`、无行尾空白；`EnsureOne` 输出末尾换行数恰为 1（或 0），再规范化不变；`TrimBlank` 输出不含连续末尾空行，再规范化不变。

## 2. 被删字节的 ToOut

`\r\n`→`\n` 时，`\r` 被删、`\n` 保留，输出 `\n` 必须来自原 `\n`（否则上面的互逆终点论证失败）。行尾空白整体被删，其右侧最近保留字节是行尾 `\n`。

要求 `ToOrig` 单调不减且互逆：每个输出偏移 o 必须是某个 `ToOut(i)`；而 o 右侧的保留字节（换行符）必须独占一个 i。于是被删区间的合法像只有「换行符之前的输出位置」。结论：
- `ToOut` 落在被删空白或被删 `\r` 上时，取该删除点右侧最近保留字节的输出位置之前（即行尾 `\n` 的输出偏移，未保留则为输出长度）。
- `ToOrig` 对应该输出位置给出删除点起点（被删区间的左闭端）。
光标语义：光标在被删空白里 = 光标在换行符**之前**（行内容一侧），而不是下一行。

span 只在删除点存区间条目（drop `[a,b)`→输出位置 p；其后的保留段合并成一条），故区间数 = O(删除点数)，纯保留文本为 0 条。查询二分，计数器记录探针数。

## 3. 缓冲超限

强制把未判定空白当「非行尾」输出：若其后真出现行尾，输出已含本应删除的空白 → 违反语义 2 与幂等（语义 5），且映射无法事后收回。

选择**拒绝**：返回带原文偏移的哨兵错误 `ErrWSLimit`，此前已确定内容保留，进入终态（再写返回 `ErrClosed`）。该状态可解释为「前缀已规范化，自某偏移起被拒绝」。拒绝不违反 4（确定性：每次切分在同一点失败且前缀相同）、5/6（前缀仍满足）。

## 4. par 拼接

每段独立 `Write` 但**不 Close**，取出「已确定前缀」（committed 字节、span 条目）与「待定尾巴」（末尾至多一个 `\r` 加其后未判定空白，有界：≤1+缓冲上限）。顺序拼接：前缀直接平移入全局；把前段尾巴与下段已确定前缀的开头一起重新判定（`\r`+`\n` 配对、空白遇行尾则删、遇内容则留），被改判定的少数条目在全局 span 上重放；坐标按段起点平移。最后一段再用单实例的 Close 走末尾策略。修正范围上界 = 每段尾巴长度 ≤ 1+缓冲上限，故每接缝只重判常数大小区域；K≤8 并发，span 在各自实例内，无共享。

## 5. 其他

- NUL/非法 UTF-8 视为普通字节；严格模式遇 NUL 返回 `ErrNUL`（带偏移），保留输出，进终态。
- 输出超限返回 `ErrOutLimit`（带偏移），立刻拒绝。
- 单个 `norm` 实例非并发安全；`par` 内部并发、对外结果确定。
