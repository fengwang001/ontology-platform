# 带背压的流式聚合管线 — 设计推导

链路：`source → parse → 聚合(stage) → sink`。多级有界队列，状态仅在进程内存，
崩溃后从 `ckpt` 恢复。只用标准库。

## 1. 「已消费位置」记到哪一条

记录生命周期上有三种位置：(A) source 已读出位置；(B) 当前在某级队列/worker 在途；
(C) sink 已原子落盘。

- 以 A 为检查点：B 中的在途记录崩溃后全部丢失（丢）。
- 以 C 为检查点且 B 非空：C 之后、A 之前的记录会重放，已落盘的部分被重复处理（重）。

结论：检查点位置 = 一条 **屏障(barrier)** 记录的序号 N，且仅当「该屏障已串行穿过
每一级、屏障之前（含屏障）的所有记录都已离开全部在途集合（各阶段在途集合为空），
其聚合中间态已写入检查点文件并 fsync+rename 完成」时，N 才成为已消费位置。

依据：每个 stage 单 worker FIFO；barrier 之前的 item 先于 barrier 出队；barrier 到达
sink 时各级队列中屏障前的在途必为 0。此时 checkpoint = (N, 各组中间聚合) 是一个
「无在途」的一致快照。恢复时 source 从 N+1 重放：N 及之前已被快照覆盖（不重），
N+1 起全部重放（不丢）。

检查点时机：source 每产出 K 条注入一个屏障；sink 处理屏障即落快照。正常结束、
source 报错、优雅停止时同样走一次屏障。屏障点之间崩溃：丢掉最后一个未落盘快照，
从上个完好检查点重放，输出仍确定且逐字节一致（输出按组键排序生成）。

## 2. 背压与资源约束

- 每级 stage = 容量 C 的有界 channel + 单 worker；channel 满则上游 send 阻塞，
  压力逐级传回 source（注入 source 用的 `beforeEmit` 钩子记录阻塞次数）。
- 共享信号量式 gauge 记录全管线历史最大在途数；非导出计数，经方法读出。
  在途上界 = 各级容量之和（每级「channel + 1 个正在处理」，测试取容量含该余量）。
- 分组数硬上限：聚合出现新键且 map 长度达上限 → 哨兵错误，管线停止。

## 3. 落盘 / 检查点 / 故障

- sink：临时文件 `*.tmp`，内容尾附 sha256；fsync 后原子 rename 覆盖正式文件。
- ckpt：同样 tmp+rename；保留最近两份 `ckpt.1/ckpt.2`，校验和不匹配或解析失败则
  回退另一份，并在 Report 标记 `fellBack`。
- 启动时发现 `*.tmp` 一律视为未完成临时文件删除（正式文件含校验和，逐字节截断必
  在长度/校验和上失败，不会被读回）。
- 坏记录（缺字段/键缺失/值非整数）计数后跳过，不进任何分组。

## 4. 并发与停止

硬取消 ctx（崩溃）与优雅停止分离：Stop 关闭 stopCh，source 停拉，队列排空，屏障
落盘后退出；重复 Stop 幂等（sync.Once）。崩溃则 cancel，丢弃未完成快照，靠恢复重放。
