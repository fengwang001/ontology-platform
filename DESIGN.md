# Cookie 解析与适用性判定 — 设计推导

本设计分三个单向依赖包：`attr`（单条 Set-Cookie 解析）、`match`（域/路径判定）、
`jar`（存储、覆盖、过期、适用性），依赖方向只允许 `jar → attr/match`。

## 1. 默认 Path 的推导（需求 2）

没有 `Path` 属性时，默认路径只由**来源 URL 的路径**推导，步骤：

1. 若来源路径为空、或首字符不是 `/`，默认 `/`。
2. 否则取路径中**最后一个 `/` 之前**的部分（不含该 `/` 本身）。
3. 若该部分为空，默认 `/`；否则该部分就是默认路径。

推导：

- `/a/b/c`：最后一个 `/` 在 `c` 前，之前是 `/a/b` → 默认 `/a/b`。
- `/a`：路径里唯一的 `/` 在位置 0，它之前的部分为空 → 默认 `/`，
  而不是 `/a`。原因是 cookie 作用于“生成它的资源所在的目录”，
  `/a` 是一个具体资源名而非目录；若把 `/a` 当默认路径，`/a` 响应种下的
  cookie 会被错误地只发给路径恰好以 `/a` 开头的请求，同级页面 `/b`
  及目录页 `/` 都收不到，违背“默认覆盖整个来源目录前缀”的意图。
- `/`：唯一的 `/` 之前为空 → `/`。

## 2. 路径匹配为何不能是朴素前缀（需求 3）

朴素前缀匹配（`strings.HasPrefix(reqPath, cookiePath)`）会让
cookie 路径 `/a` 命中请求路径 `/ab`。这是越权：

- `/a` 这个 cookie 的意图作用域是“资源 `/a` 本身”与“目录 `/a/` 之下”
  （即 `/a`、`/a/`、`/a/b`）。
- `/ab` 是与 `/a` 平级的另一个资源，只是字符串前缀恰好相同。
- 若 `/login` 种下一个作用于自身的会话 cookie，朴素前缀会把它发给
  `/login-admin-hook`、`/loginance/report` 等无关甚至敏感端点，
  等于把凭据泄露给了同主机上的不同应用路径。

正确判据：请求路径等于 cookie 路径，或以 `cookiePath + "/"` 为前缀。

## 3. Domain 越权的判据（需求 4）

设来源主机为 host，请求属性域名（忽略前导点、转小写）为 dom。

- 无 `Domain`：host-only，只有 `host == dom` 才匹配，子域不匹配。
- 有 `Domain`：必须满足
  `host == dom` 或 `host` 以 `"." + dom` 结尾（即 dom 是 host 本身或上级域）。
  不满足则该 cookie **整条拒绝**。

据此：

- 来源 `b.example.com` 设 `Domain=example.org`：host 既不等于
  `example.org`，也不以 `.example.org` 结尾 → 越权拒绝。
- 单标签域（如 `com`）必须拒绝：单标签是公共后缀（public suffix）级，
  不属于任何单一方，允许 `Domain=com` 会让该 cookie 被发往 `.com` 下
  所有站点，构成跨站 cookie 注入；且通常它也不会通过上面的尾缀判据。
  判据为：`Domain` 去掉前导点后不含 `.` 即拒绝。

## 4. 覆盖时为何保留最早创建时间（需求 6、8）

cookie 身份是三元组 (name, domain, path)；同身份再设置是覆盖值与属性，
但创建时间保留为第一次写入时刻。发送顺序是“路径更长在前；路径等长时
创建更早在前”。若覆盖时刷新创建时间，则：

- 某个高频被服务端重写的 cookie 会随每次刷新不断“变年轻”，
  在等长路径组里的发送位置反复向后漂移；
- 同一罐里 cookie 的相对顺序会被与路径无关的刷新动作改变，
  客户端两次等价请求发出的 `Cookie` 头顺序不稳定，
  服务端若依赖顺序（例如同名多值取第一个）会观察到抖动甚至取错值。

保留最早时间使顺序只由“作用域（路径长度）+ 首次出现顺序”决定，确定且稳定。
