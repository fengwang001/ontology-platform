# 严格模式流式 Base64 设计

## 规范形式（语义第 2 条）的推导

一个 4 字符组解码出 24 比特。带填充时最后一组不足 24 比特：

- `XX==`（输出 1 字节 = 8 比特）：第 1 个字符贡献 6 比特，第 2 个字符
  只有高 2 比特被使用，其**低 4 比特是未用比特**。
- `XXX=`（输出 2 字节 = 16 比特）：第 3 个字符只有高 4 比特被使用，
  其**低 2 比特是未用比特**。

编码器是一个函数：对每个字节串 x，`Encode(x)` 唯一确定，且编码器在
生成最后一组时总是把未用比特写成 0（它们来自不存在的输入字节，按
RFC 4648 必须置 0）。因此「编码结果唯一」等价于「合法输入的未用比特
全为 0」。

若宽松地接受非零未用比特，则 `QR==` 与 `QQ==` 都解码为 `A`：同一个
字节串有了两个编码表示，`Encode(Decode("QR==")) == "QQ==" != "QR=="`，
往返不变量被破坏，编码不再是唯一规范形式。所以严格模式必须拒绝。

**判定规则**：解码最后一组时——

- 形如 `XX==`：检查第 2 个字符（组内下标 1）的 6 比特值 `v1`，
  要求 `v1 & 0x0F == 0`，否则报「非规范尾部」。
- 形如 `XXX=`：检查第 3 个字符（组内下标 2）的值 `v2`，
  要求 `v2 & 0x03 == 0`，否则报「非规范尾部」。

例：`R` 的值为 17 = 0b010001，低 4 比特为 0001 ≠ 0，故 `QR==` 拒绝；
`J` 的值为 9 = 0b001001，低 2 比特为 01 ≠ 0，故 `QUJ=` 拒绝。

由此推出统一规则：**严格模式接受的串恰好是编码器的值域**——字母表
合法、长度是 4 的倍数、填充只在末尾且数量正确、未用比特全为 0、
换行（仅 MIME 模式）只出现在组之间。这同时解释了 `QQ`（长度非 4 的
倍数）、`QQ=`（同上）、`QQ===`（填充后还有内容）、`QQ==QQ==`（填充
不在流末尾）为何必须拒绝。

## 语义-测试对照表

（待测试写完后补齐）
