# 多区间 Range 响应组装器 — 设计推导

本文档只写推导过程，不重复代码。对应需求条目：第 1、3、4、6、8 条各一节。

## 1. 为什么 `bytes=-0` 是不可满足，而不是语法错误

从语义定义出发推导：

1. `-n` 的语义是「资源的末尾 n 字节」。
2. 当 `n = 0` 时，「末尾 0 字节」是一个**空字节集合**。空集合在语法上完全合法：
   `-` 后跟一串十进制数字，`0` 是合法数字，词法与语法都没有任何可指摘之处。
   若在解析层拒绝它，就等于宣称「语法合法的请求里有一类要被语法层拒绝」，
   这会让语法层背负语义判断，层次错乱。
3. 因此正确归类是：语法接受，语义上该区间**覆盖 0 个字节**。归一化时它被裁剪为空并丢弃；
   当它（或与它并列的所有区间）裁剪后一个不剩时，整个请求**没有任何可返回的字节**，
   这正是「不可满足」的定义 —— 对应 HTTP 语义里的 416，而不是 400。
4. 工程后果：`coalesce.UnsatisfiableError` 携带资源总长 `Size`，调用方据此生成
   `Content-Range: bytes */Size` 的 416 响应；而 `rangespec.SyntaxError` 携带的是
   **头部串内的字节偏移**。两类错误用 `errors.As` 即可互相判定，测试
   `TestMinusZeroIsUnsatisfiableWithSize` 单独复现这一条。

## 2. 为什么「相邻」也必须合并（第 3 条）

1. 合并的不变量是「覆盖字节集合不变」。对相邻区间 `[a,b]` 与 `[b+1,c]`，
   并集是连续区间 `[a,c]`，合并前后字节集合逐元素相等，所以合并相邻区间**不违反**任何语义。
2. 反过来看不合并会怎样：第 7 条规定「归一化后只剩一个区间时，响应体必须是裸字节，
   两个及以上才封装 multipart」。若相邻不合并，请求 `bytes=2-4, 5-7` 归一化后仍是两个区间，
   于是被封装成两段的 multipart；但这两段拼起来就是连续字节 `2-7`，与直接请求
   `bytes=2-7` 的**字节内容完全相同**，响应形式却一个是 multipart、一个是裸字节 ——
   同一字节集合对应两种响应形态，第 7 条的「单区间裸字节」就退化成一个依赖请求写法、
   而非依赖语义的偶然结果。更具体地说：两个区间合并成一个之后应当退化为裸字节
   （测试 `TestMergedRangesDegenerateToBareBytes` 复现的情形）只有在「相邻也合并」的
   规则下才成立。
3. 实现上相邻与重叠用同一个谓词处理：排序后扫描，若 `next.From <= cur.To + 1`
   （`+1` 即相邻情形）就并入当前区间，否则开启新区间。归一化结果互不重叠、互不相邻，
   且「合并前后字节集合相同」由 `TestByteSetPreserved` 用随机区间集穷举对照证明：
   把裁剪后的 spec 逐个展开成字节集合，与归一化结果展开的字节集合逐一比对。

## 3. 归一化算法与比较次数上界（第 4 条）

算法（`coalesce.Normalize`）：

1. **裁剪**：对每个 spec 常数时间映射到 `[0, size-1]`，丢弃空区间。O(n)，无区间间比较。
2. **排序**：按 `(From, To)` 用 `sort.Slice`（introsort，比较次数 O(n log n)）排序。
   每次比较把非导出计数器 `cmpCount` 加一。
3. **扫描合并**：一趟线性扫描，每个区间只与「当前合并结果末尾」比较一次
   （`next.From <= cur.To+1`），n-1 次比较。

比较次数上界：排序的比较次数至多为 c·n·⌈log2 n⌉（introsort 最坏情形，c 为小常数，
实测约 1.1），合并扫描恰好 n-1 次，故

    C(n) <= c·n·⌈log2 n⌉ + (n-1) = O(n log n)

关键是**不存在任何两两比较**：合并正确性依赖排序后的顺序性 —— 若区间 i 不与当前
末尾区间重叠/相邻，则它也不可能与任何更早的区间重叠/相邻（更早区间的 To 更小），
因此每个区间只需一次比较。这正是 O(n^2) 被排除的原因。

实测（`TestComparisonCountIsNLogN`，种子固定）：n=100 时 C=770，n=10000 时 C=147962，
比值 192.2；而 n·log2(n) 的标度比值为 (10000·log2 10000)/(100·log2 100) ≈ 200，
测试取 2 倍常数裕量（上界 400）断言通过。若是 O(n^2) 算法，该比值将是 100^2=10000，
与上界相差两个数量级，测试能干净地区分两类复杂度。

## 4. 断点续写的状态与不变量（第 6 条）

续写状态只有两个字段：

- `body []byte`：组装完成后**不可变**的完整响应字节（Assemble 一次性构建，
  之后任何操作都不修改它）；
- `written int64`：已成功写出的字节数，即「断点」。

不变量：**所有 WriteTo 调用已经吐出的字节拼接起来，恒等于 `body[:written]`**。
证明：每次调用只写 `body[written:]` 这一段；写出端接受 n 字节后 `written += n`，
而接受方拿到的正是 `body[oldWritten : oldWritten+n]`。归纳即得：无论写出端每次
接受多少字节（短写）、无论调用被切成多少次、无论中途换多少个 writer、也无论
中途返回过什么错误（错误返回时 `written` 只按实际接受的字节数推进），下次调用
都从 `body[written:]` 继续，拼接结果恒为 `body` 的前缀，最终恒等于整个 `body`。
因此「任意切分点都能拼回同一串字节」不是测试运气，而是该不变量的直接推论。
测试 `TestAllSplitPointsIdentical` 遍历 1 到总长的全部切分长度逐字节比对，
`TestResumeAcrossWriters` 遍历全部断点位置并跨 writer 续写。

## 5. 边界串规避策略与失败概率上界（第 8 条）

策略（`serve.pickBoundary` + `multipart.NewBoundary`）：

1. 候选边界 = 固定前缀 `ontology-` + 128 bit 密码学随机数的 hex 编码（32 字符），
   总长 41 字符。
2. 生成后**显式验证**：用 `bytes.Contains` 检查候选串是否出现在将要传输的
   payload（所有区间字节拼接）中。出现即丢弃重试，最多 `MaxBoundaryTries` 次，
   耗尽则返回 `ErrBoundaryExhausted` 拒绝响应（拒绝不改动任何已有状态）。
3. 检查裸 token 而非 `--token` 是保守且充分的：封装行都以 `--` 开头拼接 token，
   任何危险的分隔串都包含裸 token 作为子串，故裸 token 不冲突 ⇒ 所有封装行不冲突。

失败概率上界：单次随机候选与长度为 L 的 payload 冲突的概率，是「L 字节的串中
出现某个特定 41 字节串」的概率，至多为

    p <= (L - 41 + 1) · 2^-128  ≈  L · 2^-128

（payload 中至多 L-40 个起始位置，每个位置命中特定 41 字节的概率为 2^-128；
这是 union bound，已是最松上界。）k 次独立重试全部失败的概率 <= p^k。
代入实际量级：L = 16 MiB（默认响应上限）时 p ≈ 2^-104，默认 8 次重试后
不超过 2^-832 —— 远低于硬件出错的概率。而「显式验证」这一步把残余概率从
「依赖随机性不撞车」降为「撞了也一定被发现并重试」：即使随机源被攻击者
预测（测试里用脚本化 Rand 复现），结果也只是重试或干净地报错，
**永远不会**产出边界与内容冲突的响应。
