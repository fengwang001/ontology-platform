# 分片扇出查询：部分失败合并器设计
## 1. 模型
分片接口 `Query(ctx) Response`；`Response{ID, Claimed, Records, UpperBound, Err}`。
成功分片满足 `Err==nil && len(Records)==Claimed`；记录数不符即损坏。
每个成功分片可给 `UpperBound`：该分片内任何记录得分/值的上界（未知给 -1）。
全部成功=精确；至少一个成功但非全部=部分；全部失败或零分片=错误（区别见第 5 节）。
## 2. 扇出与资源约束
固定 worker 池，令牌信号量限并发；非导出计数器在持锁下记历史峰值在飞数。
整体截止时间用带 deadline 的 context：获取令牌前先 select ctx，故截止后新发起数为 0；
在飞请求通过 ctx 取消传播。每个分片只登记首次响应（按分片 ID 去重），重复返回丢弃。
## 3. 五种聚合的部分失败可信度（核心推导）
设成功分片集合 S 的值为 v，缺失集合 M。
- Count：真实值 = v + 非负缺失计数 ≥ v ⇒ v 是下界，标注 `count >= v`。
- Sum：同理，缺失贡献非负 ⇒ 标注 `sum >= v`。
- Min：成功部分的最小值 m 只是已观测最小值；缺失分片可能含更小值，
  故真实 min ≤ m，标注 `min <= m`（方向与 Count/Sum 相反，易写反）。
- Max：缺失分片可能含更大值，故真实 max ≥ m，标注 `max >= m`。
- TopK：令 B = Σ 缺失分片所给 UpperBound（任一未知上界则 B=+∞）。
  缺失分片最多注入总分不超过 B 的条目。对当前榜单中得分 s 的条目：若 s > B，
  任何新条目得分 ≤ B < s，无法把它挤出前 K ⇒ 它必定入选（条目间名次不保证，只保证入选）。
  榜单按得分降序、ID 升序排列，可信前缀长度 p = 满足 score_j > B 的最长前缀长度（截到 K）；
  B=+∞ 时 p=0。
全部成功时五种聚合一律 `exact`。
## 4. 区间估计
Count/Sum：[v,+∞)；Min：(-∞,v]；Max：[v,+∞)。用可空上下界表示，无穷端=nil。
## 5. 边界与可区分错误
零分片：`ErrNoShards`；全部失败：`ErrAllShardsFailed`（绝不返回零值标精确）；
所有成功分片均空：正常精确零结果（OK），与全失败靠错误与状态枚举区分。
损坏/超时/取消映射状态 Corrupt/TimedOut/Canceled，正常 OK，错误用哨兵 error 可 errors.Is。
单分片、K>总条目数（返回全部，前缀仍可判定）、部分字段（缺失按零值）、空 ID（合法）均支持。
## 6. 确定性
合并前响应与缺失清单按分片 ID 升序排序；TopK 并列按条目 ID 升序，与到达顺序无关。
## 7. demo 判定项（10 条，输出≤20 行）
全成功五聚合 exact；缺片 Count/Sum 下界；Min 上界方向；Max 下界方向；
小上界 TopK 前缀=K；大上界前缀=0；200 片峰值≤8；重复返回不重复计数；
全失败返回错误；打乱 20 种顺序结果一致。
