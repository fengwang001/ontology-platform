# 双时间查询器设计

## 两条时间轴的不对称性

- 事务时间（Tx）回答「系统什么时候知道了这件事」，有效时间（Valid）回答「这件事在现实中什么时候成立」。
- 事务时间只能追加：一条记录的事务区间代表「在那段时间里，我们以为这就是事实」。若更正历史时直接修改已有记录的事务区间，等于篡改「我们当时以为的事实」——过去某个事务时刻的查询结果会凭空改变，审计无法复现，「我们什么时候发现自己错了」这一信息永久丢失。
- 有效时间可以回填：新获得的信息完全可以是关于过去的（「其实 2020 年薪资是 120」）。因此更正的正确做法是：在当前事务时刻把旧记录的事务区间**截止**（它仍是「t1 之前我们以为的事实」），再追加一条携带新有效区间、事务区间从当前时刻开始的新记录。于是 `AsOf(2020, t1)=100` 与 `AsOf(2020, t2)=120` 同时成立——同一有效时刻因事务时刻不同而得到不同答案，这正是双时间存在的意义；若只做单时间轴，历史答案 100 会被覆盖丢失。

## 逻辑删除的表示

删除不是抹掉记录，而是「自某事务时刻起，我们认为该键的有效区间在某点终止」。实现上：把当前事务开放的记录在 `now` 处截止事务区间，再追加一条有效区间被截断为 `[原起点, 删除点)` 的新记录。于是 `AsOf(有效=删除前, 事务=删除后)` 仍返回原值，`AsOf(有效=删除后, 事务=删除后)` 返回不存在。查询用 `(value, found bool)` 返回，「不存在」与「值为零值」天然可区分。

## 同键矩形不相交不变量

同一键的每条记录占据「有效区间 × 事务区间」平面上的一个矩形。任意查询点 `(validAt, txAt)` 要得到唯一答案，充要条件是这些矩形两两不相交。维持方式：写入/更正/删除时，先把该键所有事务开放且有效区间与新区间重叠的记录在 `now` 处截止事务区间，再把这些记录有效区间落在新区间之外的**剩余片段**以原值重新追加（事务区间自 `now` 起），最后追加新记录。旧矩形被截短、剩余部分平移到新事务区间，故任意两个矩形要么事务区间不交，要么有效区间不交。`store` 提供非导出方法 `checkInvariants`（导出包装 `Check`）全量校验该不变量。

## 无穷远终点

`interval.Infinity = math.MaxInt64` 表示「至今有效」。它只是一个普通的大整数，`Contains`/`Overlaps` 无需任何特判，与有限终点在比较上行为完全一致。

## 区间语义

所有区间左闭右开 `[Start, End)`：`Contains(Start)` 命中，`Contains(End)` 不命中。`Start >= End` 为空区间，构造时拒绝并返回可用 `errors.Is` 判定的 `interval.ErrEmpty`。三类哨兵错误：`interval.ErrEmpty`（空区间）、`store.ErrNotFound`（更正/删除不存在的键）、`store.ErrInvalid`（删除点不在当前有效区间内等非法操作）。

## 复杂度

存储按键分链（`map[key][]Record`）。点查只遍历该键版本链：检查数 ≤ 版本数，上界 `4 × 每键版本数`。更正只触及该键链：扫描一遍 + 截止一条 + 追加一条，触及数 ≤ 版本数 + 2。两者均不随总记录数增长。
