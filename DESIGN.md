# DESIGN：带替换的流式 UTF-8 ⇄ UTF-16 转码

## 1. 非法单元切分规则（WHATWG「替换消费」规则的统一推导）

状态机：看到首字节即知期望长度 N（2/3/4）与第二字节合法区间；续字节合法区间恒为 `80..BF`。
在第 k 个位置（k=2..N）第一次看到字节 b：
- b 合法：继续；
- b 非法：当前首字节**单独**成为一个非法单元（吞 1 字节），b 作为新起点重判；
- 流在第 k 个字节前结束：首字节及已跟随的全部合法续字节（N-2..N 中合法的部分）一起算**一个截断非法单元**。
首字节本身为 `80..BF`/`C0 C1`/`F5..FF`：非法单元就是它自己（吞 1）。
推导样例：`F0 90 80 41`：F0 要求第二字节 90..BF，90、80 都合法（3 字节合法前缀），第 4 字节 41
非法→F0 这 3 字节只算 1 个单元，41 重判合法 ⇒ FFFD 41。`E0 80 80`：E0 第二字节要求 A0..BF，
80 非法→E0 吞 1；80 为游离续字节吞 1；下一个 80 再吞 1 ⇒ 三个 FFFD。`ED A0 80`：ED 要求 80..9F，
A0 非法→ED、A0、80 各 1 ⇒ 三个（同时挡住代理码点）。`C0 AF`：C0 非法首字节吞 1，AF
作为续字节无首可跟再吞 1 ⇒ 两个。`F4 90 80 80`：F4 要求 80..8F，90 非法→4 字节各 1。
`E2 82`+EOF：E2 合法、80..BF 中的 82 合法，流结束 ⇒ 这 2 字节是**一个**截断单元（不是两个）。
`80 80`：两个游离续字节，各 1。

## 2. 第二字节合法区间表（续字节一律 80..BF；长度由首字节决定）

| 首字节 | 长度 | 第二字节区间 | 第二字节非法时吞 | 首字节非法时吞 |
|---|---|---|---|---|
| C2..DF | 2 | 80..BF | 首字节 1，违规字节重判 | — |
| E0 | 3 | A0..BF | 1 | — |
| E1..EC | 3 | 80..BF | 1 | — |
| ED | 3 | 80..9F | 1 | — |
| EE..EF | 3 | 80..BF | 1 | — |
| F0 | 4 | 90..BF | 1 | — |
| F1..F3 | 4 | 80..BF | 1 | — |
| F4 | 4 | 80..8F | 1 | — |
| 80..BF | — | （无首可跟） | — | 1 |
| C0 C1 F5..FF | — | （越界/非最短） | — | 1 |

## 3. 切分缓存上限与即时放弃

UTF-8 最长 4 字节，UTF-16 最长 4 字节（一个代理对），统一缓存上限 = **3 字节**（已收首字节+至多 2
个续字节/码元）。收到合法续字节只追加；收到违规字节，立即把当前前缀按规则判非法并 emit，违规字节
回到输入重判——**只有「已收全部字节都合法但长度未满」才等待**，因此绝不跨切分点提前出 FFFD，
且每个字节在其进入的那次 Write 里被检查一次，不回扫。EOF 时残留 → 一个截断单元。

## 4. par 切点对齐

UTF-8 序列 ≤4 字节：切点 c 若落在序列中间，其首字节必 ≥ c-3。每段从 **start=max(0,c-3)**
（UTF-16 取 c-4，常量 `lead`）开始喂一个独立转码器，丢弃所有「单元起始 < c」的输出单元（非法单元
也按单元起始偏移过滤），从第一个起始 ≥ c 的单元开始保留。上一段正常跑到物理末尾并在 Close 时吐出
截断 FFFD——该截断单元起始 < c，被下一段的重放覆盖（下一段重放时它后面接上了后续字节，不再截断），
最终拼接时只保留段 0..K-2 在 c 之前、段 K-1 到结尾的字节，重复覆盖区结果天然相同。
例：切在 `C3|80 80`：上段截断单元(C3) 被丢；下段从 C3 起解析得到 é 与一个游离 80，计数与单线程一致。
检查次数：(Σ 段长) + K·lead ≤ n + 8·常数。

## 5. 上限断点（语义 9）

输出前先按目标编码计算该单元长度（UTF-8 1..4、FFFD=3；UTF-16 2 或 4），放不下则**不输出、不吞该
单元**，停在标量边界返回 ErrOutputLimit。Write 返回的 n = 已进入已提交单元的字节数；仍在 3 字节
缓存里的合法前缀**不算**已消费（调用方下次必须连同它一起重传，故续传时把缓存字节前置到 p 即可，
拼起来与一次转完逐字节相同）。代理对作为整体原子提交。

## 6. BOM、UTF-16 代理、错误

仅流首（跨切分点由缓存自然支持）识别：UTF-16 无显式序时由 `FEFF`/`FFFE` 定序；KeepBOM=false 丢弃，
=true 输出 U+FEFF；其后的 BOM 是普通字符。孤立高/低代理、高代理后非低代理（后者退回重判）各一个
FFFD。错误四分类哨兵：`ErrInvalid`（*InvalidError 带偏移/长度）、`ErrTruncated`（*TruncatedError
带偏移）、`ErrOutputLimit`、`ErrClosed`；严格模式首错即终态，输出冻结。`stream.Transcoder` 非并发安全。
