# DESIGN

## 1. Write 切法无关（语义 3）与 Flush（语义 4）

贪心匹配在位置 i 的决定只依赖「已定前缀 + 从 i 起的未决字节」，与历史 Write 边界无关。
编码器把尚未定稿的字节留在 hold 尾部：
- hold[0] 找不到 ≥3 长度匹配：立即输出字面量并前进 1 字节（字面量决定不依赖未来字节）。
- 找到长度 L：仅当 `L < len(hold)`（已知下一字节不匹配）时匹配才定稿并输出；
  `L == len(hold)` 时继续等后续字节。后续扩展失败则用当前 hold 全量重扫一次候选，
  与「整段一次写入」在该长度下看到的候选集合完全一致，故选定结果一致。
- Flush/Close 强制把 hold 当前最大扩展定稿：该位置以累计输入偏移定义，切法不同也相同。
连续 Flush 时 hold 为空，直接不输出任何字节。

## 2. 重叠回指（语义 2）

回指语义是 out[k] = out[k-k-dist] 逐字节递推（dist<len 时引用本次刚复制的字节）。
块复制对重叠区读到的是旧内容，dist=1 的 run 会复制错。
解压一律逐字节 `w.At`/`w.Add` 循环；解压本身每输出字节至少 O(1)，不增加复杂度阶。

## 3. 两类距离错误（语义 7）与并行预置字典（语义 9）

- dist>已输出：解码器历史里根本没有该字节，是「引用未来/空洞」。
- dist>窗口容量：即使历史够长，dist 越过窗口是独立的格式不变量。
压缩器只在 window 内取候选且并行块预置字典恰为上一块末尾 cap 字节，故永不产生后者。
并行块中预置字典就是解压器已正常输出的上一块原文尾部，已输出计数天然包含它，
「已输出」语义不变，块间无需刷新标记。

## 4. 并行确定性（语义 9）

块 i 的压缩输入 = 本块原文 + 上一块原文末尾 cap 字节（仅 i 与 data 的函数），
不读取其他块的压缩结果或完成顺序。worker 结果按块下标拼接；流头一次、流尾一次，
中间是连续记录，确定性与 workers 数无关。

## 5. 流格式（全部 LEB128 uvarint，uvarint 最多 10 字节，第 10 字节只允许 ≤1）

| 记录 | 字节布局 |
|---|---|
| 流头 | 固定 4 字节 `L Z 7 1`（魔数 3 + 版本 1） |
| 字面量 | tag=0x00；uvarint n；n 个明文字节 |
| 回指 | tag=0x01；uvarint dist；uvarint length（均 ≥1，length≥3） |
| 刷新 | tag=0x02（无负载） |
| 流尾 | tag=0x03；uvarint 原文总长；uvarint FNV-1a64 校验和（对原文滚动） |

窗口容量默认 32768，链长上限默认 64，最小匹配 3。
