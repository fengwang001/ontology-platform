# 一致性快照导出器 设计推导

## 0. 总体结构

- `store`：MVCC 键值存储。每个键保留一条版本链 `[]entry{ver,val}`，全局单调递增版本号。
- `snapshot`：快照句柄。创建时记录版本水位 `v` 与当时的键集合，并向 store 钉住（pin）`v`。
- `export`：把快照按键序分块写入单个导出文件，块自描述（magic/序号/长度/CRC32）。
- `manifest`：文件尾部的清单（块表、总校验和、快照版本），JSON + 长度 + CRC 帧。
- `verify`：对导出文件做结构 + CRC + 顺序 + 总校验和的判定性校验。
- 文件布局：`[manifest帧][chunk0][chunk1]...[chunkN-1]`（清单在头部，
  否则任何后缀截断都会先毁掉清单，无法区分块头/块体截断）。

## 1. 写时复制：触发点与释放条件

**触发点推导**：导出读取的是「版本 ≤ v 的最后一个值」。若 `Put` 直接覆盖，
版本 ≤ v 的旧值就消失了，导出到该键时只能读到新值，快照被污染。
因此正确做法是：旧值根本不删——`Put` 只在版本链上**追加**新 entry；
等价地描述为「覆盖发生前，把快照可见的旧值保留下来」。导出读取时
沿版本链取 `floor(v)`，即优先读到这份保留值。只要 store 里还有任何
活跃快照，被取代的旧 entry 就不得物理删除。

**释放条件推导（为什么按最老快照版本）**：设活跃快照版本集合为
`{v1 < v2 < ...}`。快照 vi 需要且仅需要每个键的 `floor(vi)` 那份旧值。
任何 entry 若比 `floor(min{vi})` 还旧，则对所有活跃快照都已被取代，
无人可读，可以删除；反之 `floor(v1)` 之后（含）的 entry 至少被某一个
快照或当前读需要。因此 GC 的充要判据是：**丢弃所有严格旧于
floor(最老快照版本) 的 entry**。只按最老版本判断即可覆盖全部更年轻的
快照——它们需要的值要么等于、要么新于最老快照的 floor，必然被保留。
推论：两个重叠快照时，关掉年轻的一个不会释放任何东西（最老版本不变），
只有两个都关闭（活跃集合为空）后，才把所有键裁剪到只剩最新值。
实现：store 维护 `pins map[ver]count`，Pin/取消钉时触发裁剪，
`Retained()` 计数 = Σ(每键链长 − 1)，即被保留的旧值个数。

**内存上界**：保留值个数 = 导出期间被改写的键数（每改写一次链长 +1），
与总键数无关。10 万键只改 100 个 ⇒ 保留 ≤ 100。

## 2. 导出顺序无关性

快照内容 = 固定版本水位下的「键→值」集合，与枚举顺序无关。
分块字节流确实随顺序变化（正序/逆序的块划分不同），因此清单里的
**总校验和**定义为顺序无关的交换聚合：`total = Σ crc32(key‖value) (mod 2^32)`。
加法可交换，故任意导出顺序得到相同的总校验和与相同的内容集合；
测试用字典序与逆序各导出一次，断言清单总校验和相等。

## 3. 续传与快照过期

分块只取决于「快照键集合 + 每键值 + 块大小」，三者对同一快照恒定，
故分块是**确定性**的：重放导出跳过前 k−1 块，第 k 块起的字节与一次性
导出逐字节相同，这就是续传的正确性依据。续传时逐块比对清单里的 CRC，
可发现已有部分被篡改。

**为什么过期快照必须拒绝续传**：快照关闭后，为它保留的旧值已被 GC
释放，store 里只剩最新值。此时若「用当前数据续上」，文件前 k−1 块来自
旧时刻、后面的块来自新时刻，拼出的是一个**跨时刻的不一致文件**，且
CRC 自检无法发现（每块自身都合法）。唯一安全的策略是：续传前检查
快照句柄，已关闭则返回可判定错误 `ErrSnapshotExpired`，且**不删除**
已导出的部分文件（其中的块本身完好，重开新快照可整体重导）。

## 4. 关闭与导出的竞争

快照提供 `Acquire/Release` 引用计数。`Close` 先置 closed（此后的
Acquire/续传一律拒绝），再**等待在飞的导出读完**才解除版本钉、
触发 GC。因此在飞导出要么完整读完旧值，要么根本没开始就收到
`ErrSnapshotExpired`，不存在读到一半被抽走旧值的窗口。

## 5. 截断分类的说明

后缀截断不会篡改保留的字节，故截断点只会落在三类：截入清单帧 →
清单不完整；截入某块 14 字节头 → 块头不完整；截入块体 → 块体不完整
（续传起始块 = 被截块号）。CRC 不匹配只能由**比特篡改**产生
（清单完整、块结构完整但内容 CRC 不符），作为第四类可判定错误
单独注入验证。四类错误均为 `errors.Is` 可区分的哨兵错误。
