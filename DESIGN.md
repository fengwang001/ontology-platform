# 设计推导

## 1. 零计数 hunk 头的行号

统一头 `-a,b`：a 是该 hunk 旧侧第一行的 1 基行号；b=1 时省略 `,1`。hunk 全为插入（b=0）时旧侧没有“第一行”，GNU 用**插入点前一行**的行号，没有前一行则为 0；全为删除（d=0）时 c 同理。

- `x\n`→`y\nx\n`（C=0）：插入点在文件最前，其前一行行号为 0，故 `-0,0 +1`。
- `a\nb\nc\n`→`a\nb\nX\nc\n`：X 插在第 2 行之后，前一行为第 2 行，故 `-2,0 +3`。
- `a\n`→空：新侧为空，删除点前的新行号为 0，故 `-1 +0,0`。

Apply 解释：b=0 时插入点 = 旧文件第 a 行之后（a=0 即文件最前）；d=0 时删除后该位置即新文件第 c 行之前（c=0 即文件末尾）。

| 旧侧 | 书写 | Apply 的插入点 |
| --- | --- | --- |
| b=0 | `-a,0`（`-0,0` 允许） | 第 a 行之后；a=0 为文件最前 |
| b=1 | `-a`（省略 `,1`） | 从第 a 行开始 |
| b≥2 | `-a,b` | 从第 a 行开始共 b 行 |

## 2. hunk 合并阈值

C=1，相邻改动间有 g 行共同行。前 hunk 尾部取 min(C,g) 行、后 hunk 首部取 min(C,g) 行。
- g=2（=2C）：前后各取 1 行，两行上下文全部出现，GNU 合并为单 hunk。
- g=3（=2C+1）：中间至少 1 行共同行不进任何 hunk，GNU 拆成两个 hunk。

故阈值是 **g ≤ 2C 合并，g > 2C 分开**。若取 C：g=2 时会误拆（GNU 实为单 hunk）；若取 2C+1：g=3 时会误合并（GNU 实为两个 hunk）。

## 3. Myers 并列路径的选择

正向 Myers 主循环按 d=0..D、k=-d,-d+2..d 递推 V：能向上走（插入）则 x=V[k-1]，否则向左走（删除）x=V[k+1]+1。在 (k-1) 与 (k+1) 等深的并列点，条件取“能向下（插入）则插”，等价于回溯时优先删除。

本实现固定并列时**优先删除**：`ab→ba` 的 hunk 固定为 `-a`、` `、`b`、`+a`（先删后插），永不出现先插后删。确定性由此保证（蛇形比较无随机），重复生成逐字节相同。

## 4. 偏移后后续 hunk 的查找

第 i 个 hunk 记录的旧起始行 s_i。维护已应用 hunk 的净位移 shift=Σ(ins−del) 与上一个 hunk 实际偏移 lastOff。下一个 hunk 的查找中心：

    center = s_i + shift + lastOff

在 [center−F, center+F] 内取与“上下文+删除行”精确匹配的位置；多个候选取 |p−center| 最小，并列取靠前。

shift 反映行数净增减、lastOff 反映已发生的漂移；二者叠加后再 ±F，等价于 GNU patch“前一块钉住后一块跟着走”。只用 shift 不用 lastOff，会在整文件被平移（净行数不变）时漏配；平移成功后 lastOff 继续传播，保证查找中心跟随提交结果。

## 5. 计数器与复杂度

`edit.steps` 统计对角线上前进的每一步（蛇形逐行比较各计 1）。标准 Myers 为 O((N+M)D)：每个 (d,k) 状态至多推进一次蛇；测试以 4×(N+M)×(D+1) 为常数余量断言，并以两档规模比值（100000 行档计数 ≤ 1000 行档 150 倍）验证近线性。
