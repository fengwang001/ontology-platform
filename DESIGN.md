# 版本约束求解器设计推导

## 1. 选哪个版本（由不变量 2 确定性推导）

输入里唯一与登记顺序无关的全序是版本号自身的全序（以及包名的字典序）。因此：
- 选包顺序：按包名升序取下一个「域内仍有多个候选」的包；包名是输入身份，与登记顺序无关。
- 选版本顺序：候选版本**从高到低**逐个尝试，第一个令约束传播后仍可行的即选定。
版本在登记时即按版本号排序存储，故登记顺序不参与任何决策，同输入结果逐位相同。
选高优先：在 `>=1.0.0` 之类约束下，高版本通常向后兼容，高优先得到的是「最新可行解」。

## 2. 预发布版本是否参与

采用 SemVer 比较序：`主.次.修-预发布` 小于同三元组的正式版，即
`1.0.0-beta < 1.0.0`；预发布之间按标识符点分序列比较（数字标识符按数值、
非数字按字典序、数字段 < 非数字段、短前缀序列更小）。
结论：**预发布是普通版本，平等参与约束**。故 `1.0.0-beta` 满足 `>=1.0.0-beta`
但**不**满足 `>=1.0.0`（因为它小于 1.0.0）。不做 SemVer 官方那种
「正式约束隐藏预发布」的特判——那会破坏「比较与判定同一张全序表」的自洽性：
满足谓词必须且仅由区间端点的全序比较决定。解析规则跟随 SemVer：
禁止前导零（`01`）、禁止空标识符、拒绝 build 元数据（`+meta`）。

## 3. 约束求交的边界（闭开规则）

区间 = 下界 `[或(` 与上界 `)或]` 的合取；求交 = 下界取严、上界取严
（同端点时开比闭更严）。空判定：
`下界 > 上界`，或 `下界 == 上界 且任一端点开`。

| 区间 A | 区间 B | 交 | 说明 |
|---|---|---|---|
| `>=1.2.0` | `<1.2.0` | 空 | 同端点，上端点开 |
| `>=1.2.0` | `<=1.2.0` | `{1.2.0}` | 两端皆闭，恰好一点 |
| `>1.2.0` | `<1.2.1` | 结构非空 | 无版本登记则域为空；有 `1.2.1-rc1` 等预发布则可落入 |
| `>1.2.0` | `<=1.2.0` | 空 | 同端点，下端点开 |

第三行论证：`1.2.1-rc1` 在全序中严格位于 `1.2.0` 与 `1.2.1` 之间。
「区间结构非空」由端点序判定；「某包有候选落入」由域过滤判定，两者分开，
避免区间语义依赖登记内容。

## 4. 冲突解释取哪一条（确定性）

搜索失败时可能存在多条互斥链。规则完全由全序/字典序决定：
- 报告**包名升序的第一个**被清空的包；
- 其清空原因取**版本最低的候选**被排除时命中的那两条互斥约束；
- 互斥对按来源 `(包名,版本,目标包)` 排序后取最小的一对；
  根需求视为来源 `<root>`，且固定排在依赖约束之前；
- 解释链沿约束来源回溯到根：`<root> → A@a.v (约束) → B@b.v (约束) → ✗`。
冲突是数据的纯函数（候选与约束均按全序/字典序存储，不保留登记与声明顺序），
故同一输入解释逐位相同。
