# DESIGN

## 流格式（全部变长整数，小端 base128 varint，<=10 字节）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `magic=0x4F4E`(varint), `version=1`(varint), `windowCap`(varint) |
| 字面量段 | tag=1(varint), `n`(varint), 后接 n 个原始字节（不加 varint 前缀） |
| 回指 | tag=2(varint), `dist`(varint,>=1), `length`(varint,>=3) |
| 刷新标记 | tag=3(varint)。边界标记，不带数据，仅保证此前全部输入可还原 |
| 流尾 | tag=4(varint), `origLen`(varint), `crc32`(IEEE,varint) |

流 = 流头 (字面量段|回指|刷新标记)* 流尾。流尾后再有字节即“多余字节”。

## 推导 1：为何压缩输出与 Write 切法无关

单次 Write 到达时，末尾字节的最长匹配可能延伸到“将来”的 Write。若在 Write 边界
立即提交，1 字节一段与整段一次会产生不同决策。规则：压缩以“段”为最小提交单位，
一个段在其终点可知（Flush/Close，或内部缓冲达 cap 时整段一起压缩）。段内 LZ 扫描
使用全部段字节，匹配可跨段内任意 Write 边界；决策只取决于段的完整字节序列，故切
法不同、段内容相同则输出逐字节相同。未决字节缓存到段终点才扫描。Flush 强制结束
当前段：必须把缓冲全部提交，否则解码器无法在该点还原全部输入；同时 Flush 输出
刷新标记。无新输入时第二次 Flush 不写任何字节。匹配器/窗口跨段保留，故 Flush 后
仍可回指 Flush 之前的数据。

## 推导 2：重叠回指（dist < length）的复制

块复制（非 memmove 的“从旧快照拷贝”）在 dist<length 时会读到未复制区域。解压
维护环形窗口，第 k 个输出字节 = 窗口中距当前输出头 dist 的历史字节，且每写一字节
立刻回卷进窗口。length 很大（如 30, dist=3）时自然周期性重复 abc。实现即逐字节
循环：每字节一次环索引读写，O(length)，而 LZ 输出本身就是 length 个字节，复制已
是输出的下界工作，没有额外渐近开销，故可接受。

## 推导 3：“距离超过已输出”与“距离超过窗口容量”为何分开

二者语义不同：dist>已输出总数指引用了从未出现过的位置（流自始就不可能合法，也
包括预置字典之前）；dist<=总数但 dist>windowCap 指数据存在过但已滑出可引用范围。
压缩器只在当前 windowCap 环内登记候选，候选位置必在容量内，故绝不会产生后者。
并行块的预置字典是“上一块末尾 windowCap 字节”，被预置进同一环内；解压端该字典
就是它此前已输出的同一段字节，故“已输出字节数”含义不变，引用字典里的字节同时
满足 dist<=已输出 且 dist<=windowCap。

## 推导 4：并行块压缩的确定性

块 b 仅依赖：本块原文 data[b] 与上一块原文 data[b-1] 的末尾 windowCap 字节（预置
字典）。哈希链匹配器在每块从空链开始、以该预置构造，扫描是纯函数，不读其他块的
输出，也不读共享可变状态；块 b-1 的压缩结果不影响块 b 的决策。因此无论
workers=1/2/4/8、完成顺序如何，每块编码字节固定，拼接顺序固定 => 输出逐字节相同。
块间衔接无需刷新标记：块切分只影响压缩端字典预置，解压端看到的是普通回指流，
回指天然落在已输出（=上一块）的窗口内。全流只写一次流头、末尾一个流尾。

## 其他决定

- 最短匹配 3；字面量累计到下次匹配或段末成段输出。
- varint 第 10 字节后若仍有续位或值溢出 64 位 => 超长错误；记录中途缺字节 => 截断。
- 输出上限在声明 length 处、复制前检查（含字面量长度），拒绝即终态、保留已输出。
- 校验和 CRC32-IEEE（标准库 hash/crc32，仅校验非压缩）；单实例非并发安全。
