# 设计推导

不变量：严格解码器接受的每个 `t` 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 必须拒绝的输入（逐条推导）

1. 显式次数 1（`1a`）：`Decode` 得 `a`，而 `Encode("a") == "a" != "1a"` → `ErrCountOne`。
2. 次数 0（`0a`）：0 个 `a` 解码为空串，`Encode("") == "" != "0a"` → `ErrCountZero`。
3. 前导零（`01a`、`007a`）：解码为 `a`，规范编码是 `a`（次数省略）；即便非 1 也无前导零写法 → `ErrLeadingZero`。
4. 相邻同符号游程（`2a3a`、`aa`）：解码为 5 个/2 个相同码点，最长游程要求合成一个，`Encode` 得 `5a`/`2a`，不等于原串 → `ErrAdjacentSame`。
5. `\` 后既非数字也非反斜杠（`\a`、`\é`）：非数字非反斜杠的码点 `Encode` 原样写出，若把 `\a` 接受成 `a`，再编码得 `a != "\a"` → `ErrBadEscape`。
6. 末尾孤立的 `\`：合法编码中每个 `\` 必是转义前缀且后随字符；该输入不可能由 `Encode` 产生 → `ErrTrailingEscape`。
7. 末尾只有次数没有符号（`3`、`ab12`）：合法编码中次数必后随一个符号；该输入不可能由 `Encode` 产生 → `ErrMissingSymbol`。
8. 非法 UTF-8（`\xff`、`a\xc3`）：`Encode` 的输出全部由合法码点组成，必为合法 UTF-8；接受非法字节会得到无法再编码回原串的序列 → `ErrInvalidUTF8`。

## 大次数的取舍

选择**支持任意大次数**，但解码输出总字节数设上限（`DefaultLimit = 64 MiB`，可经 `DecodeLimit` / `NewDecoder` 配置）：

- 次数由 `math/big.Int` 精确解析（`runs.ReadCount`），不产生整数溢出。
- 每写一个游程前比较 `n <= (limit-已输出)/符号字节数`，超限立即返回 `ErrOutputLimit`；不预先按次数分配内存（`99999999999999999999a` 直接判超限）。

## 流式解码

`Write` 只追加缓冲（内存与**输入**字节成正比），`Close` 时做单趟严格解码。因此任意切分方式的输出与错误逐位相同；成功路径每个输入字节恰好被检查一次，非导出计数器 `checked` 自增总量 == 输入字节数。

## 语义 → 测试对照

（测试写完后补此表。）
