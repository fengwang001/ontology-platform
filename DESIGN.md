# 严格 JSON 字符串编解码器设计

## 代理对错误偏移

代理对是一个六步词法单元：`\uD800–\uDBFF` 后必须紧跟 `\uDC00–\uDFFF`。
错误偏移指向**第一个使序列无法继续合法的词法单元的起点**（即反斜杠位置）：

- 高代理后 EOF、直接字符、普通短转义、非法 `\u`：偏移指向高代理起点；因为高代理自身开启了必须完成的承诺。
- 高代理后是另一个高代理：偏移指向第二个高代理；第一个高代理的期待落空点从这里开始。
- 高代理后是低代理才组合成一个码点；然后该码点必然位于 U+10000–U+10FFFF，可安全编码为 UTF-8。
- 孤立低代理不等待任何内容：偏移指向低代理自身。

因此 `"\uD83Dx"`、`"\uD83DA"` 都在高代理起点报错；后者不能当作“替换字符 + A”，因为 RFC 8259 的转义是结构化词法，不能在高代理承诺尚未判定时先消费它并把后续字节独立解释。严格解码器也不得用 U+FFFD 掩盖词法错误。

## 最小转义与唯一性

JSON 字符串只要求引号、反斜杠和未转义 U+0000–U+001F 不能原样出现。U+001F 属于该控制字符区间，所以必须转义；U+007F 是 DEL 而非传输或终端约定下的 ASCII 控制字符，RFC 8259 允许原样出现，故不转义。

编码固定选择：`"`→`\"`，`\`→`\\`，退格/换页/换行/回车/制表符使用规定短形式；其余控制字符使用唯一的小写 `\u00xx`。其他所有合法 Unicode 码点（含 `/`、U+007F、U+2028、U+2029 与各平面字符）原样编码。

该映射以码点为单位：每个码点只有一种输出，且转义形式与原样 UTF-8 字节不可能混淆（转义形式总以反斜杠开始，合法原样字节不可能产生这种词法替代）。所以合法输入的输出唯一；严格最小转义解码后再编码，会逐字节恢复原字面量，`Encode` 因而单射。
