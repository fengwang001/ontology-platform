# DESIGN.md — 随机超平面 LSH 分桶检索器

## 1. 签名规则

- 向量维度 `d`；表数 `L`；每表位数 `b`。表 `t` 第 `j` 位的法向量 `r_{t,j} ∈ R^d`。
- 法向量用显式种子 `seed` 经 `math/rand/v2` PCG 流生成：按 `(t,j,k)` 顺序消费随机数，
  Box–Muller 产生标准正态分量。同种子 → 字节级相同的法向量 → 相同签名（确定性，无 map 迭代）。
- 位值：`bit = 1` 当 `<v,r> > 0`；否则（`< 0` 或恰好 `= 0`，含零向量）取 `0`。
  即零点约定为负侧，零向量签名恒为全 0，合法且确定。
- 签名 = 该表 `b` 位拼成的 uint64（位 j 放第 j bit）。
- 随机超平面 LSH 碰撞概率：`Pr(签名相同) = 1 - θ/π`，`θ` 为两向量夹角。
  距离越近 θ 越小，越可能同桶；不同种子给出独立划分。

## 2. b、L 与候选数、召回的关系

- 单表单候选点对碰撞概率 `p(b) = (1 - θ/π)^b`（b 位全一致），随 b 指数下降。
- 点对在 `L` 张表中至少同桶一次：`P_L = 1 - (1 - p(b))^L`，随 L 单调不降。
- 期望候选数（去重前）随 b 指数减少（桶更细），随 L 线性增多；故：
  - b↑：候选数↓、单点对召回↓；b=4/8/12 三档实测候选数应单调下降。
  - L↑：召回单调不降；本实现令查询签名取建索引签名前 `b_q` 位、按前缀匹配，
    且第 L 张表的法向量是 L+1 张表法向量流的前缀，故 1/2/4/8 表召回严格单调不降。
- 期望召回（TopK 近似与精确集合重合比例 / K）≈ 各近邻点 `P_L` 的平均。

## 3. 检索与复杂度

- 建索引：对每个向量、每表、每位算一次内积，哈希计算次数恰为 `N·L·b`（精确计数）。
- 查询：在每张表按查询签名前缀取桶，并集去重得候选，再做欧氏距离精排取 TopK。
- 计数器记录精排距离计算数；设计目标在 5000 向量上 ≤500（≤10%），否则退化为暴力扫描。
- 基准：暴力精确最近邻对全量向量算距离，作为召回率 ground truth。

## 4. 并发

- 建索引在构建器内完成全部表后一次性原子发布（不可变 Index）；查询只读，无锁并发安全。
- 构建中的表对外不可见，满足“全可见或全不可见”。`-race` 下安全。

## 5. 边界

- 空集合：查询返回空 TopK 与 0 候选；单向量；K>N 时返回全部。
- 全同向量：全部落入同一桶，候选数=N，近似与精确结果同为这些 ID，召回=1。
- NaN / ±Inf：拒绝入库，计入可读出的跳过数；零向量合法（签名全 0）；维度 1 合法。
- 维度不符：查询维度 ≠ 索引维度返回可判定错误（errors.Is），指出期望/实际维度。

## 6. 落盘格式（自描述 + CRC32）

```
magic "ONLSHIDX" | u16 version | u32 seed | u16 dim | u8 L | u8 b |
[L 张表法向量：每表 b×d 个 float64，IEEE754 小端] |
[L 张表桶：u32 桶数；每桶 u64 签名 + u32 数量 + u32[] ID] |
u32 CRC32(IEEE) 覆盖以上全部字节
```

- 损坏分类按截断点相对“头/法向量/桶/CRC”区间：头不完整、超平面参数不完整、
  桶表不完整、CRC 不匹配，四类均可 errors.Is 区分。
- 恢复模式返回最大可恢复前缀：桶中引用不存在 ID 的条目被剔除并计数，不残留悬挂 ID。
- 全零法向量为退化超平面，读回时检出并指出第几张表第几位。
