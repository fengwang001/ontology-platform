# DESIGN

## 1. 零计数 hunk 头的行号

unified diff 中区间号是「第一行的 1 基行号」，空区间没有第一行，故借用其边界处
已存在行的行号；文件开头前的位置行号为 0。

| b 值 | 书写 | Apply 对旧侧的解释（插入点：删除/替换前的原行行界） |
|---|---|---|
| b=0 | -a,0，a=锚点行号；插入到文件头时 a=0 | 插入点在第 a 行**之后**（a=0 即第 1 行之前） |
| b=1 | -a（省略 ,1） | 仅作用于第 a 行（或在 a 行后的行界删除其行尾） |
| b≥2 | -a,b | 作用于第 a..a+b-1 行 |

新侧 +c,d 完全对称。样例：文件头插入锚点行不存在 → -0,0；在第 2 行后插入
（a\nb\nc\n → 在 b 与 c 间）→ -2,0；删光唯一一行，新侧为空，删除后行界在文件
头 → +0,0。解析时把 -a,0 的插入点放在第 a 行行界之后，而不是第 a 行之前，否则
文件头样例必须写成 -1,0，与 GNU diff 矛盾。

## 2. hunk 合并阈值

阈值是 **2C**：两簇改动之间未改动行数 g ≤ 2C 则合并（各自带 C 行上下文时相邻上下文
恰好相接），g ≥ 2C+1 时分隔，中间至少留 1 个不出现在补丁里的未改动行。

以 C=1、改动在第 1、g+2 行为例：g=2 时 GNU diff 输出单个 hunk（两段 1 行上下文
相接），故阈值必须 ≥ 2C=2，取 C 会错误分开；g=3 时 GNU diff 输出两个 hunk，故阈值
必须 < 2C+1，取 2C+1 会错误合并。唯一解为 2C。

## 3. 并列最短脚本的选择

Myers 每条对角线上：删除后 x 较大；插入后 x 较小。本实现固定取
down = (k == -D || (k != D && V[k-1] < V[k+1]))，即能向右（删除）就向右，
**优先删除**。因此 a b → b a 的脚本排列为 -a、空格 b、+a（删除行排在前），
而非先插入。该选择对相同输入恒定，保证补丁逐字节确定。

## 4. 偏移后后续 hunk 的查找起点

hunk i 以记录位置为中心、半径 F 行搜索；实际应用点偏移 δ（实际旧侧起点 − 记录起点）
后，已应用的净行数变化为 Δ=(新侧行数−旧侧行数)。后续 hunk 的坐标应整体平移：
hunk i+1 的记录起点先加 Δ（前序改动造成的文档漂移），再以平移后的位置为中心、半径
F 搜索，距离相同取靠前候选。Δ 随每个已应用 hunk 累加。

## 5. 其余约定

- lines：按 \n 切分，行尾（\n、\r\n、无）随文本保留，Join 原样拼回。
- hunk 体内逐行记录旧/新侧文本与「该行无行尾」标记；No newline 标记仅渲染产物。
- 应用按候选位置精确匹配全部上下文行与删除行；任一 hunk 失败则整体放弃、原文不变。
- 计数器：snake 内每做一次行比较 +1（V 推进的基本步为 O(D²) 量级，不计入）。
