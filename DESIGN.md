# 设计：结构化日志字段脱敏与一致性采样

仅标准库；状态只在进程内存；输出为带自描述头的二进制文件。

## 1. 包划分（5+1）

- `record`：记录模型（嵌套 map + TraceID + Level）、规范化编解码、深度/环检测。
- `rule`：脱敏规则集预编译（路径 trie + 值模式），编译期冲突检测，匹配计数。
- `mask`：替换 / 哈希 / 截断三种动作，递归深拷贝后原地改写。
- `sample`：由 TraceID 决定的确定性一致性采样 + error 强制保留与不完整标记。
- `sink`：自描述头 + 4 字节长度前缀 + 载荷 + CRC32 的落盘/读回/损坏分类。
- `cmd/demo`：逐条判定的可运行演示。

## 2. 一致性采样推导（核心）

要求：同一条追踪链的所有记录要么全留、要么全丢，且与到达顺序、时间、机器无关。

1. 记录级随机掷骰会让同一链的记录决策不同 → 链断裂，排除。
2. 决策输入若含记录内容/时间/随机种子，同一 TraceID 会得到不同结果，排除。
3. 因此决策只能是 `f(TraceID)` 的确定性函数。取 `h = FNV-1a-64(TraceID)`，
   `h % 10000 < rate*10000` 时保留。单次决策恰好 1 次哈希计算，O(1)。
4. rate=0 全丢；rate=1 全留。
5. TraceID 缺失或空串：无法把多条记录关联成链，定义为“各自独立的单元素链”，
   按固定空输入哈希一次，结果确定且与顺序无关（空串通常落入丢弃桶）。
6. 重放同一 TraceID 任意次结果相同（无状态、无 RNG、无时钟）。

## 3. error 强制保留与“链不完整”标记

error 及以上级别无条件保留，即使 `f(TraceID)=丢弃`。此时该链的其他记录已被丢弃，
单独保留的 error 无法看到完整上下文：仅在“链采样决策=丢弃 且 记录被强制保留”时
置 `log.incomplete_chain=true`。链被保留的 error 不打标记；非 error 记录从不打标记。

## 4. 嵌套路径与含点号字段名的消歧

路径段切分后逐段下钻。消歧规则（确定、无歧义）：

1. 每一层先按嵌套映射查找：当前值是 map 且存在该段键 → 下钻。
2. 否则，仅当当前层存在键为“剩余完整路径原文”的字段（字面点号键）→ 命中该字段。
3. 字面点号键只在叶子判定时考虑，绝不在中途当作下钻入口。
4. 路径某层不是 map：路径不匹配（不报错）。路径不存在：不匹配。
5. 数组不参与路径下钻；数组内值由值模式规则（第 5 节）覆盖。

例：字段 `{"a":{"b":"x"},"a.b":"y"}`，规则 `a.b` 命中嵌套的 x，不命中字面键 y；
规则只写 `a` 则按 a 整体处理。

## 5. 脱敏规则与三种动作的不变量

- 路径规则：编译进 trie；值模式规则：固定子串列表（子串命中即视为敏感值）。
- 替换：输出固定串 `***REDACTED***`，长度与原值无关。
- 哈希：`sha256` 十六进制；同值同结果、不同值不同结果、结果不等于原值、不可逆。
- 截断：保留前 N 个 rune 并追加 `…(truncated)`；N=0 时只剩标注。
- 彻底性：值模式规则在整棵树（含所有路径、数组元素）扫描每个字符串叶子；
  同一敏感值无论出现在几处都被改写。
- 非目标不变：深拷贝后只改命中叶子，未命中字段序列化字节与原记录一致。
- 冲突：同一路径同时配置两种动作 → 编译期 `ErrConflict`，指出两条规则；
  同路径同动作视为重复（去重，不报错）。

## 6. 复杂度

- 规则集预编译为 trie：每记录每字段至多一次节点访问，路径匹配次数
  ≤ `记录数 × (字段数 + C)`，不是 `记录数 × 规则数 × 字段数`；
  非导出计数器供包内测试断言。
- 采样：每次决策 1 次 FNV 哈希；非导出计数器断言哈希次数 = 调用次数。

## 7. 记录健壮性

- 深度上限 32（map/slice 嵌套层数）；构造时超限返回 `ErrTooDeep` 并计数，
  迭代式遍历保证不栈溢出。
- 自引用/共享引用成环：构造时迭代式 DFS 检测 map 引用环，`ErrCyclic`，不死循环。
- 四类可判定错误（`errors.Is`）：`ErrConflict`、`ErrTooDeep`、`ErrCyclic`，
  以及 sink 的 `ErrHeader/ErrLength/ErrBody/ErrCRC`。

## 8. sink 文件格式与损坏分类

头：`ONTLOGv1\n`（8 字节，含版本）。帧：`uint32 BE 长度 | JSON 记录载荷 | CRC32 IEEE(载荷)`。
逐字节截断文件的分类：

- `< 8`：头不完整 `ErrHeader`，恢复 0 条。
- 长度前缀不足 4 字节：`ErrLength`，恢复此前完整帧。
- 有前缀但载荷字节不足：`ErrBody`，恢复此前完整帧。
- 载荷齐但 CRC 字节不足或校验不过：`ErrCRC`（帧不可信，不恢复该帧）。

最大可恢复前缀 = 头 + 所有此前完整帧。

## 9. 并发

规则集/采样器构造后不可变；计数器为 atomic；mask 在每记录私有深拷贝上进行；
sink 写入由互斥锁串行化。并发批处理的输出多重集与串行结果逐元素相同。

## 10. 边界

空记录（无字段）正常编码为 `{}`；空串值按普通字符串处理；深度 1 为仅顶层；
数组内嵌套映射可被值模式规则扫描到。
