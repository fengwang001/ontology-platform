# 设计推导

规范化只产生两类删除：行尾空格/Tab、`\r\n` 或单独 `\r` 中的 `\r`；另在“恰好一个”策略下可在末尾插入一个 `\n`。CRLF 与独立 CR 在读入下一个字节前都保持待定，因此任意切分得到同一状态迁移。

## 末尾换行

三种策略只在 Close 时作用于最终行流：`Keep` 保留末尾连续换行数；`EnsureOne` 有换行则压成一个，没有则补一个；`TrimBlank` 删除最后一个非空行之后的所有空行；空输入没有行可保留，仍为空。

选择给空输入补 `\n` 而不是输出空，是因为“EnsureOne”的命名不变量是“输出恰好以一个换行结束”。幂等性不受影响：`N("")="\n"`，再次规范化时已是一个末尾换行，仍为 `"\n"`。插入换行映射到原文终点这个零长度区间：其起始输出位置满足互逆，输出终点也回到原文终点。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 被删字节的 ToOut

映射采用左闭右开区间。保留字节在两侧等长；删除区间在输出端长度为 0，插入区间在原文端长度为 0。被删除区间内部没有对应的唯一输出字节，`ToOut` 取该删除点之后最近的保留输出位置；若删除一直延伸到 EOF，则取输出终点。

因此 `"a  \n"` 中两个空白的原文偏移都映射到 `\n` 的起始位置，CRLF 中的 CR 也映射到输出 LF 的起始位置：编辑器光标会落在行尾换行符之前。选择“之后”会让 CR 的映射大于 LF，破坏单调不减；删除区间内部各点取相同值，右侧 LF 的值也相同，刚好满足闭区间折叠与 `ToOut(ToOrig(o))==o`。

## 缓冲超限

选择拒绝，而不是把待定空白强制判成非行尾。一串空白只有看到行尾或 EOF 才能判定；若先输出，之后看到换行就要删除已输出字节，破坏流式输出上限，也会产生与无上限运行不同的结果。若强制丢弃，之后看到普通字节又违反“行中间空白保留”。

限制为待判定空白长度，CR 本身只占一个待定字节。尝试加入第 `limit+1` 个空白时，在该原文偏移返回可判定错误；已输出内容和映射保留，规范化器进入终态，后续 Write/Close 返回终态错误。limit=0 表示禁止任何需要延迟判定的空白。

## 并行拼接

段不能从待定序列中间开始。对每个内部切点，若段首是空格/Tab，就向右越过整串空白；随后若紧跟 `LF`、独立 `CR` 或 `CRLF`，再越过完整行尾。若空白后是普通字节，说明它属于行中间，可安全切开。于是切点只会落在完整行尾之后。

除最后一段外，各段都使用 Keep，避免提前删除跨段的末尾空行；最后一段使用配置策略。每段 span 的原始终点和输出终点都加上前序累计长度；段间共享的是不可变结果，不共享 normalizer 状态。修正范围至多为“待判定空白上限 + 两个行尾字节”；禁用上限时其上界自然是剩余输入长度。
