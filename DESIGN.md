# 批量导入：幂等与断点续传 — 设计

## 1. 范围与包划分

把一个大批次（批 ID + N 条带业务键的记录）导入目标存储，支持崩溃续传、
重复导入幂等、对账。仅用标准库，数据与进度均写本地临时目录。

- `batch`：批次定义、清单文本编解码、重复键/空 ID 校验、流式顺序读取。
- `store`：内存 map 存储，业务键唯一约束，区分 staged（在途）/committed，
  可注入一次性写入失败，统计访问次数。
- `progress`：进度文件（已成功区间 `[0,done)` + CRC32）与占用锁文件。
- `importer`：Prepare → Write → Verify → Commit 四阶段编排。
- `reconcile`：清单 vs 存储的线性一遍对账。
- `cmd/demo`：不读参数不联网的验收演示。

记录存储内容确定性地等于其业务键；故「逐字节相同」即 map 内容集合相同。

## 2. 进度粒度的推导（核心）

- 若写入**非幂等**：重放已写记录会产生重复/副作用，进度必须每条都刷，
  崩溃点之后一条都不能重放，吞吐随 fsync 次数塌方。
- 本题写入**按业务键幂等**（`Put` 是 upsert，同键同值重复写结果不变），
  因此「进度比真实进度旧」是无害的：续传时把一个区间内的记录再 `Put`
  一遍，存储结果不变。于是进度可以**粗粒度周期刷盘**（每 N 条一次）。
- 崩溃丢失的只是「最后不足 N 条」的进度声明，代价是续传至多重写 N 条，
  而不是每条一次 fsync。N 即内存窗口 `maxLive`，同时满足内存上限。
- 进度只允许回退到一个**前缀区间** `[0,done)`，不记录稀疏区间：崩溃语义
  天然简单，且重放安全。

## 3. 重复导入的幂等判定

Prepare 先读清单与进度：
- 进度头含 `done=true` 提交标记 → 不做任何写入，`Commit` 返回
  `ErrAlreadyComplete`（可判定，表示「本批次已完成」，非错误路径）。
- 否则以存储实际内容确定续传起点（见第 4 节），重放窗口内记录；
  因 upsert 幂等，第二次结束后存储与第一次逐字节相同。

边界：N=0 的批次经 Commit 写 `done` 标记，同样判为已完成。

## 4. 以存储为准，而非以进度为准

进度文件可能比存储**新**（进度刷盘后、对应写入因故障未持久化/被外部清掉）。
若相信进度声称的 `done=50000` 而存储只有 30000 条，会永久缺失 20000 条。
故 Prepare 时用「存储中按索引连续存在的 staged 前缀」重算续传起点，
进度文件仅作为**上界提示**：实际起点取 min(进度 done, 存储连续前缀)。
reconcile 同样以存储扫描结果为事实，能检出「进度虚报」并给出精确缺口区间。

## 5. 部分批次（在途）的可见性

存储为每键保存 staged 与 committed 两个集合。未 Commit 时记录只在 staged，
对 reconcile 可见但标注 in-flight，绝不计入「已提交」统计。对账输出三段：
- Written：清单索引中存储实际存在（staged）的连续区间；
- Gap：清单内应存在但缺失的区间（端点精确到索引）；
- Extra：存储有而清单没有的键（排序输出）。
Commit 把本批 staged 键原子晋升为 committed。

## 6. 资源与复杂度

- 续传不重头扫描：重写起点来自存储连续前缀；重写计数（非导出）断言
  10 万条在第 7 万中断时，重写 ≤ N（而非 7 万）。
- 内存：清单流式读取，通道窗口 = maxLive；计数器记历史峰值并断言不越界。
- 对账线性一遍：一次顺序读清单 + 一次存储全量 Range（map 内部排序保证
  确定性），两侧归并；存储访问计数 ≤ 3n。

## 7. 进度文件与故障分类

布局（小端二进制）：`"PPROG1"` magic(7)+batchLen(4)+batch+done(1)+
doneIdx(8) 为头部；其后每条 `keyLen(2)+key`；末尾 CRC32(4) 覆盖前面全部。
截断点逐字节分类：
- 长度不足完整头部 → 头部不完整；
- 头部后某条 keyLen/key 未写完 → 区间记录不完整；
- 记录齐而末尾 4 字节 CRC 不足/不符 → CRC 不匹配。
均返回带 `Kind` 的可判定 `CorruptError`，并回退到「最大可恢复前缀」
（头部不完整→0；记录不完整→该条之前；CRC 不符→全部记录之前），续传结果
仍与不崩溃一致。写入失败时进度停在失败点前的最后刷盘点，续传从该点重放。

## 8. 并发占用锁

与进度文件并列的 `.lock`：内容为持有者令牌 + 获取时间戳。获取用 O_CREATE|
O_EXCL 原子创建；持有者存活时第二个导入器立即得 `ErrBusy`。崩溃后锁文件
残留：时钟可注入，now-stamp ≥ TTL 判为过期，删除后接管（compare-and-delete
校验令牌）。不同批次用不同锁文件，可并发；-race 下保持干净。

## 9. 边界语义

空批 ID 拒绝；同批次重复业务键拒绝整批并报键与两处位置；空串业务键合法；
单记录、零记录正常；进度文件不存在视为从头开始（非错误）。
