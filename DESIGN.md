# 自然排序：前导零与全序推导

## 比较规则（自左向右单趟扫描，一次只看一段）

1. 数字段 vs 数字段：先比**数值**；数值不同即定论（无符号整数逐位比较，不转整型，任意长度）。
2. 非数字段 vs 非数字段：逐字节按字节序比较，首个不同字节即定论。
3. 数字段 vs 非数字段（同一位置类型不一致，如 `a1` 与 `a10` 的 `1|尾` 边界）：
   比较当前字节，ASCII 数字（0x30–0x39）一律小于其他字节；不等即定论。
4. 一侧耗尽：较短者为小（`x < x1`）。
5. **数值相等的两个数字段不得立刻相等，也不得立刻比较前导零个数**：
   把“前导零个数”之差**挂起（pending）**，继续比较后续内容。
   - 后续任意一步产生定论（规则 2/3/4），以该定论为准，挂起作废；
   - 仅当两串到结尾都完全一致（无任何定论），才在最末尾取首个（最靠左的）挂起点：
     前导零少者为小。

## 为什么不能“立刻比较前导零个数”

`a01b < a1c`：首个数字段数值相等（01 = 1），若立刻按零个数定论会得 `a1c < a01b`，
随后的 `b < c` 永远没有发言权，与样例矛盾。因此后续内容必须**优先于**前导零参与比较。

## 为什么末尾兜底是全序（传递性）

挂起规则等价于“两级键的字典序”：
一级键 = 把每个数字段折叠成其**数值**后的段/字节序列（规则 1–4）；
仅当两串一级键完全相同时，二级键才生效——取最靠左挂起点的零个数之差。
一级键自身的字典序是严格全序；“一级键相等才比二级键”是标准字典序分层，
故反对称、传递，且 `Compare==0 ⇔ a==b`。边界规则 3 是固定的单字节判定，同样传递。

## 演算

- `a01b` vs `a1c`：`a` 平；数字 01=1 挂起（零数 1 vs 0）；非数字段 `b` vs `c` → b<c，
  定论 `a01b < a1c`，挂起作废。
- `a1b01` vs `a01b1`：`a` 平；1=01 挂起（0 vs 1）；`b` 平；01=1 再挂起（1 vs 0）；
  同时到尾，无定论；取最左挂起点：0 个零 < 1 个零，故 `a1b01 < a01b1`。
- `x0 < x00 < x000 < x1`：前三者一级键相同，到尾后按零个数 1<2<3；
  `x000` 与 `x1` 一级键数值 0<1，直接定论。

## 非 ASCII

仅 ASCII `0`–`9` 是数字；全角数字等 UTF-8 字节全部走规则 2/3 的普通字节比较，
逐字节确定、不依赖 rune 解码，故不 panic 且仍是确定的严格全序。

## 复杂度

双指针单趟扫描，每个字节至多被触达两次（扫描边界一次、数值比较一次），
`checked <= 2*(len(a)+len(b))`，由 `natural.CheckedBytes()` 读出。

<!-- TABLE -->
