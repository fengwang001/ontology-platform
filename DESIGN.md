# 快照读可见性判定 设计
## 1. 为什么只看提交时刻不够
朴素规则：版本的提交号 < 快照水位 即可见。
漏洞：快照创建时 T1 尚未提交，被记入活跃集；之后 T1 才提交，
并发提交分配到的提交号可能小于水位。于是 commitNo < watermark
成立，朴素规则把「取快照时还不存在」的版本暴露给读者。
反例：T1 开始 -> 建快照（水位 10，活跃集 {T1}）-> T1 提交号 5。
朴素规则 5 < 10 判可见；正确答案不可见。
正确规则（写入事务 T 的版本）：
- T 是快照自身事务：可见（自己写的自己可见）；
- 否则要求 commitNo(T) < 水位，且 T 不在活跃集中；
- T 已回滚：永不可见；commitNo == 水位：不可见（左闭右开）。
水位与活跃集各做一次常数时间查找，不遍历事务表。
## 2. 包划分
- txn：内存事务表，维护事务号与状态（进行中/已提交/已回滚）及提交号。
- snapshot：不可变快照，持有水位、自身事务号、活跃集 map，可释放。
- visible：按上述规则判定，非导出计数器记录查找次数；
  另校验版本链提交号严格递增。
## 3. 错误与上限
- 哨兵错误 ErrUnknownTxn、ErrSnapshotReleased、ErrCorruptChain
  可用 errors.Is 区分；活跃集超限为 ErrActiveSetTooLarge。
- 快照只持有活跃集 map，内存项数 == 活跃事务数，与事务总数无关。
