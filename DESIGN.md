# DESIGN — Java .properties 解析器/回写器
包划分：`logical`（物理行→逻辑行，含非导出字节检查计数器）← `props`（键值拆分、反转义、有序保存、Store）。

## 推导 1：只含空白（或为空）的续行（语义第 3 条）
续行规则是两条独立判断的组合：(a) 当前物理行尾有奇数个反斜杠 → 丢弃一个并拼接下一行；
(b) 被拼接行先剥掉前导空白，再参与拼接，拼接后仍按 (a) 检查其行尾。
设 `k=a\` 后接一行 `   `（或空行）：按 (b) 剥离后得到空串，拼上等于没拼；
再按 (a) 检查该行的行尾——空白不是反斜杠，奇偶计数为 0（偶）→ 链条在此终止。
结论：`k=a\` + `   ` + `b=c` → `k`→`a`、`b`→`c`；空续行既**不贡献字符**，也**不延续链条**。
这与 `java.util.Properties` 的 `readLine` 一致：续行后读入的下一行只做前导空白跳过，
是否继续续行完全由该行自身行尾的反斜杠奇偶决定，空行自然结束逻辑行。
推论：「首部空白被剥掉」与「空白续行终止链条」是同一条规则的两面——先剥、再看剩什么；
若剥完剩下 `\`（如行 `  \`），奇数个反斜杠仍会让链条继续，因为判断发生在剥离之后。

## 推导 2：Store 的最小转义集（语义第 6 条）
从 Load 端反推哪些字符会被误读：行首空白被剥离；行首 `#`/`!` 触发注释；
`=`、`:`、空白是键的终止符；`\` 是转义引导符；`\n`/`\r` 是行终止符。
- 键：必须转义 `\`（否则被当作转义序列）、`=` `:`（否则提前终止键）、
  空白（空格/制表/换页在任何位置都会终止键，故键中所有空格都要转义为 `\ `，
  `\t` `\f` 及换行符写成 `\t` `\f` `\n` `\r`）；`#`/`!` 只在键首必须转义，
  因为注释判定只看行首第一个非空白字符，键中间的 `#` 落在键扫描里，天然安全。
- 值：必须转义 `\` 与控制字符（`\n`/`\r` 会截断行，`\t` `\f` 一并处理）；
  值**开头**的空格必须转义，因为 Load 在分隔符后会跳过空白，开头的空格会丢；
  值**中间**的空格不必转义，因为值扫描从第一个非空白字符起直到行尾原样保留（尾部空白都保留）；
  值中的 `#`/`!`/`=`/`:` 都不必转义：注释判定只发生在行首，而值前面必有键与分隔符，
  分隔符判定在第一个分隔符之后就结束了。
- 非 ASCII：本实现按 UTF-8 读写（不做 ISO-8859-1 转换），原样输出，不写 `\uXXXX`。

## 语义 → 测试对照表（待补）
