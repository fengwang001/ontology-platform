# 设计推导

## 1. 零计数时的 hunk 头行号

统一格式行号表示「该块覆盖区域前的旧/新文件行号」，1 起编号；空块（计数 0）用「前一行的行号」定位插入点。

· 旧 `x\n` 新 `y\nx\n`（C=0）：新增行插在旧第 1 行之前，空旧块前面有 0 行 → 旧侧头 `-0,0`；新块是新文件第 1 行 → 新侧头 `+1`。
· 旧 `a\nb\nc\n` 新 `a\nb\nX\nc\n`（C=0）：插在旧第 2 行之后、旧第 3 行之前，空旧块前有 2 行 → 旧侧头 `-2,0`；新行是新文件第 3 行 → 新侧头 `+3`。
· 旧 `a\n` 新为空（C=3）：删掉旧第 1 行 → 旧侧头 `-1`（b=1 省略）；空新块前有 0 行 → 新侧头 `+0,0`。

统一规则：`a` = 旧块前一行的行号，空块时即「插入点前已有行数」（可为 0）；`c` 同理由新文件计算。Apply 把 `a` 解释为插入点在旧文件第 `a` 行之后（`a=0` 即文件开头）。

| b | 头书写 | Apply 的插入点解释 |
| --- | --- | --- |
| b=0 | `-a,0`，a=插入点前行数（可为 0） | 不删行；新行插在旧第 a 行之后 |
| b=1 | `-a`（省略逗号与 1） | 从第 a 行起替换 1 行（a≥1） |
| b≥2 | `-a,b` | 从第 a 行起替换 b 行 |

新侧（加号侧）完全对称。

## 2. hunk 合并阈值

设相邻改动间隔 g 行未改动行。前块取 C 行下文、后块取 C 行上文：g ≤ 2C 时两侧上下文相接或重叠，每个未改动行至少被一侧覆盖，GNU diff -U C 输出一个 hunk；g = 2C+1 时恰好剩 1 行两侧都不覆盖，拆成两个 hunk。故阈值为 2C：g ≤ 2C 合并，g ≥ 2C+1 分开。

反例（C=2）：g=4 时前块 2 行下文与后块 2 行上文恰好覆盖全部 4 行，GNU 输出一个 hunk（阈值若取 C 会误拆，取 2C+1 会在 g=5 误合）；g=5 时第 3 行两侧均不覆盖，GNU 拆两个 hunk。实现：相邻 hunk 旧区间相隔行数 ≤ 2C 即合并。

## 3. 并列最短脚本的选择

Myers 每条 furthest-reaching 对角线在删除与插入给出相同最远端点时，固定优先删除（先取 k+1 端点再取 k−1，先向下走）。因此 `a b` 变为 `b a` 的脚本为删 a、删 b、插 b、插 a：hunk 内删除行整体排在插入行之前。该规则逐对角线固定，脚本唯一，补丁字节唯一。

## 4. 偏移后后续 hunk 的起点平移

hunk i 记录旧起点 p_i，实际在 p_i+Δ 处应用；下一个 hunk 的搜索中心平移同样的 Δ，即以 p_{i+1}+Δ 为中心、半径 F 搜索。原因：补丁中各 hunk 行号基于同一份旧文件，前一 hunk 的净增行（插入数减删除数）使其后所有绝对行号整体平移，偏移须累计；不平移会让文件前部一处偏移导致后部本可成功的 hunk 被误判为超出范围。
