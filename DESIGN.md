# 设计：多租户加权公平队列调度器

模块 `ontology`，仅标准库，状态全在进程内存。包划分：`task`（任务值类型）、
`tenant`（租户队列与虚拟时间账本）、`sched`（堆上加权公平选择）、`admit`
（入队背压）、`stat`（份额统计）、`cmd/demo`（演示）。时钟可注入：调度器
不读真实时钟，所有"时间"都是虚拟时间，测试用确定性任务流驱动。

## 1. 为什么是 vt += c/w 而不是 c*w

目标：长期看租户 i 获得的执行代价份额 = w_i / Σw。设租户 i 被执行了
总代价 C_i，定义其虚拟时间 vt_i = C_i / w_i，每次执行一个代价 c 的任务
后 vt_i += c / w_i。调度器每次选 vt 最小者，等价于不断把各租户的 vt 拉平：
稳态下所有活跃租户 vt_i ≈ V（同一系统虚拟时间），于是 C_i ≈ V·w_i，即
C_i ∝ w_i，份额恰好正比于权重。若错误地用 c*w，则 vt_i = C_i·w_i，拉平后
C_i ≈ V / w_i，份额与权重成反比——权重越大被调度得越少，完全颠倒。直观
记忆：vt 是"已消费服务折算成权重为 1 的标准租户的服务量"，权重大的租户
同样的真实服务只折算成更少的虚拟服务，所以显得"吃得少"、优先被选中。

## 2. 空闲租户重新加入时的 vt 抬升

系统虚拟时间 sysVT 单调不减，取值为最近一次被选中租户在选中时刻的 vt
（活跃租户 vt 的下界近似）。租户 X 在 sysVT = v0 时排空队列离开堆，vt_X
停在 v0；期间 Y 独占执行了 1000 个单位代价，sysVT 推进到 V = v0 + 1000。
若 X 重新入队时不抬升，它相对其他活跃租户持有 V - v0 的"虚拟欠账"：调度
器为拉平 vt 会连续只选 X，直到 vt_X 追上 V。按单位代价、权重 1 计算，X
会连续独占约 (V - v0)·w_X / c = 1000 步（X 排队的任务够多的话）。空闲期
间 X 并未参与竞争，这段欠账不是它应得的服务，只是账本没跟上。正确做法：
从空转为非空（或新到达）时执行 vt_X = max(vt_X, sysVT)，即放弃过期欠账，
从"当前起跑线"重新参与公平竞争。抬升后 X 与 Y 的 vt 相同，按权重交替，
X 最多连续执行 1 步，Y 也不会被饿死。本实现把抬升放在 `sched.Submit`
中（租户不在堆中且由空转非空时），保证抬升与入队在同一临界区完成。

## 3. 代价为 0 的任务

零代价任务合法，但若 vt += 0/w = 0，只提交零代价任务的租户 vt 永不推进，
将无限独占调度器。处理：引入最小有效代价下界 MinCost = 1，推进时取
eff = max(cost, MinCost)，即 vt += eff / w。这样每个任务至少推进 1/w，
零代价租户与提交单位代价任务的同权重租户严格交替，不会独占。代价：真实
代价在 (0,1) 区间的任务会被按 1 记账，属于有意的保守近似，文档化即可。

## 4. 同 vt 并列与确定性

堆的比较函数：vt 小者优先；vt 相等时按租户 ID 字典序。堆形状与弹出顺序
因此只取决于操作序列，与内存地址、map 迭代顺序无关，同一确定性任务流跑
任意多次调度序列逐元素相同。并发提交时，"提交顺序"定义为获取入队互斥锁
的串行化顺序：序号 Seq 在锁内分配，先拿到锁者序号小。

## 5. 数值表示：float64 + 范围校验

选 float64 而非有理数：比较有减法即可、无需大数库，且 double 有 53 位尾
数与约 1.8e308 的量级。配合构造期校验把权重限制在 [1e-6, 1e6]（越界、
NaN、非正一律报 ErrInvalidWeight），单次推进 eff/w <= 1 / 1e-6 = 1e6，
即使 1e12 次调度 vt 也不到 1e18，远低于溢出阈值；相对舍入误差约 1e-16
量级，对 5% 的公平性容差完全无影响。权重极大（1e6）与极小（1e-6）并存
时，大权重租户单次推进 1e-6、小权重租户推进 1e6，各自有限且可比较。

## 6. 选择复杂度与计数

活跃租户放二叉堆（container/heap），单次选择 = 堆顶调整或弹出，比较次数
约 2·log2(n)（下滤每层比较两个孩子再比较父子）加可能的重新上滤，实测上
界取 4·ceil(log2(n))。堆中只放非空租户：队列排空即弹出、重新入队才压入，
空闲租户不占用选择代价。调度器内部用非导出计数器累计单次 Next 的比较次
数（Less 回调内自增），测试在同包内直接断言，对外经 Compares() 暴露只读
快照。

## 7. 背压、移除与错误分类

每租户队列上限由 `admit.Limiter` 在自己的互斥锁内做"检查-入队"两步，超
限返回 ErrQueueFull，只影响该租户。四类错误均可用 errors.Is 区分：
tenant.ErrInvalidWeight（权重非法）、admit.ErrQueueFull（队列满）、
sched.ErrTenantBusy（队列非空时移除租户）、sched.ErrNoTenant（未注册租
户的提交/移除）。租户在仍有排队任务时被移除一律拒绝（ErrTenantBusy），
不静默丢弃任务；队列为空时移除立即生效，租户不在堆中无需额外清理。

## 8. 并发模型

sched 内一把互斥锁保护堆、账本与序号分配；admit 自己的锁保护各租户深度
计数，锁序固定 admit -> sched，不会死锁。每个任务恰好被取出一次：出队在
锁内完成，Seq 全局唯一且单调，消费者按 Seq 去重即可验证不丢不重。
