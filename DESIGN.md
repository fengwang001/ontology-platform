# DESIGN

## 1. 流格式

流头 5 字节：魔数 `LZ7`（3 字节）+ 版本 `1` + 窗口容量 varint（仅声明，供解压预校验）。
其后是 0..n 条记录，最后为流尾。记录首字节为 tag：

| 记录 | 首字节 | 后续布局 | 说明 |
|---|---|---|---|
| 字面量 | `0x00` | varint 长度 L，随后 L 个原文字节 | |
| 回指 | `0x01` | varint 距离 D，varint 长度 L | D>=1，L>=3 |
| 刷新 | `0x02` | 无 | 边界语义，不产生输出 |
| 流尾 | `0x7E` | varint 原文总长，varint CRC32-IEEE | 末记录 |

所有整数为无符号 LEB128 varint（最多 10 字节，第 10 字节只允许最低位）。
校验和按原文字节递增计算。空输入 => 流头 + 流尾（非空、可解压、长度 0）。

## 2. 第三节推导

**Write 切法无关。** 压缩器内部保存连续输入缓冲，维护已决位置 `d` 与已插入哈希位置 `i`。
在偏移 p 判定：先把 `[i,p)` 的 3 字节头插入哈希链，再查链取贪婪最长匹配 m。
仅当 m 存在且 m.len 恰好等于「当前缓冲剩余字节」时无法判定它是否在未来字节上继续，
保留到下次 Write 重查；否则在 p 立即决：输出 m（或 1 个字面量），d 前进。
于是每个决定只依赖"已到达的全部原文"这一前缀，且除"匹配顶到缓冲末端"外不等待未来，
不因 Write 边界截断匹配或提前吐字面量，输出与切法无关。

**Flush。** Flush 强制把所有未决字节按字面量决完（顶到末端的匹配在此刻不可能继续，
按字面量收尾），写刷新标记。因此刷新点之前的全部原文均可被解压还原；
没有新输入时没有未决字节，只发标记会造成重复，故第二次 Flush 直接写 0 字节。
Flush 不清窗口，后续匹配仍可引用更早数据。

**重叠回指解压。** 块复制（copy 一次性）在 D<L 时读到的是目标缓冲尚未写入的区域，
语义错误。正确语义是 out[t]=out[t-D] 逐字节推进。实现采用
`for k:=D; k<L; k++ { b[k]=b[k-D] }` 后一次性 append：环/窗口中前 D 个源字节先按
out[t-D] 读出，后续字节从上一轮刚写的位置取，等价逐字节复制，却只有一次 append。

**距离两类越界。** "超过已输出字节数"是流自身矛盾（历史根本不存在）；
"超过窗口容量"违反格式约定（即使历史更长也不允许引用，压缩器受容量约束绝不会发出）。
压缩器任何回指都来自其环形窗口内现存位置，故后者不可能由合法压缩器产生。
并行块用"上一块末尾 N 字节"预置字典：解压是单流顺序进行的，这些字节就是此前已输出
字节的一部分，"已输出字节数"含义不变，两类校验仍然成立。

**并行确定性。** 块 k 的压缩输入 = 本块原文 + 上一块原文末尾 N 字节，算法纯函数，
不依赖其他块的压缩结果或 goroutine 完成顺序，故 workers=1/2/4/8 输出逐字节相同。
块间不插刷新标记：记录流首尾相接，回指自然引用预置字典（即流中更早已解压字节），
流式解压器无需知道块边界即可解开。

## 3. 复杂度

哈希链每位置只考察至多 `chainLimit` 个候选，计数为非导出 `examined`；
全同字节虽每位置都走满链长，总数恰为 chainLimit×n（两档每字节相同，不随规模增长）；
随机串链很快断裂，远小于上界。长回指使 4MB 全同字节压缩后仅数十字节。
