# DESIGN：properties 解析器/回写器的关键推导

## 1. 空白续行（语义第 3 条）的推导

问题：续行接上的那一行只含空白或为空时，结果是什么？

推导（对照 `java.util.Properties.LineReader.readLine`）：
- 行尾奇数个反斜杠触发续行时，JDK 丢掉该反斜杠，并置
  `skipWhiteSpace=true, appendedLineBegin=true`，继续读下一物理行。
- 下一行前导的 `' ' '\t' '\f'` 被跳过；但跳过空白的循环里，
  换行符仅当 `!appendedLineBegin` 时才被跳过。续行后的第一个换行符
  （`appendedLineBegin=true`）**不被跳过**，它正常终止当前逻辑行。
- 结论：「剥掉续行首部空白」只剥空白**字符**，从不剥该行的行终止符。
  因此只含空白（或为空）的续行贡献空内容，且其换行符**结束**当前逻辑行，
  续行链在此断开。例：`k=a\` + 换行 + `   ` + 换行 + `b=c` → `k`→`a`、`b`→`c`。
- 与「续行首部空白被剥掉」的关系：两者是同一条规则的两半——
  剥的是字符级前导空白；行终止符不是字符内容，始终保留其语义。

## 2. Store 转义规则（语义第 6 条）的推导

从 Load 的逆向约束推导「只转义必要字符」：
- Load 在**原始行**上找第一个未转义的 `=` `:` 或空白作为键尾，
  再跳过分隔符两侧空白，值延伸到行尾。
- 键：任何 ` ` `\t` `\f` 会被当作键尾、行首空白会被逻辑行剥离，
  故键中**所有**空白必须转义；`=` `:` 会被当分隔符，键中必须转义。
- 键写在行首：行首第一个非空白字符是 `#`/`!` 时整行成注释，
  故**键开头**的 `#`/`!` 必须转义；键中间的 `#`/`!` 不在行首，不必。
  值前面至少有 `=`，永不在行首，故值中 `#`/`!` 一律不必转义。
- 值：分隔符只找一次，值中间的 `=` `:` 不再特殊，不必转义；
  值中间/尾部空格在值起点之后，Load 原样保留，不必转义；
  但**值开头**的空格会被「跳过分隔符后空白」吃掉，必须转义为 `\ `。
- `\` 是转义与续行字符，键值中所有 `\` 必须写成 `\\`；
  `\n` `\r` 是行终止符，必须写成 `\n` `\r`（`\t` `\f` 统一写成 `\t` `\f`）。
- 非 ASCII 字节高位为 1，不与任何 ASCII 特殊字符冲突，原样 UTF-8 输出。

## 3. 语义-测试对照表

（待测试写完后补齐）
