# LZ77 流式格式设计

## 记录布局

所有整数使用 LEB128 无符号变长整数；多字节记录按下列顺序排列。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 `LZ77O1` + version(`1`) |
| 字面量 | tag=`0` + length + 原始字节 |
| 回指 | tag=`1` + distance + length |
| 刷新 | tag=`2` |
| 流尾 | tag=`3` + originalLength + fnv1a64(原文) |

## 关键推导

1. **Write 切法无关**：最长匹配只有看到失配字节，或达到固定 `maxMatchLength=65535`，或收到 Flush/Close 时才结束。若当前匹配一直延伸到待处理缓冲区末尾，后续字节可能继续延长它，所以不能输出；没有至少 3 个待处理字节也不能判定无三字前缀匹配。因此压缩器缓存至多 `maxMatchLength+2` 个未决字节。Write 只追加并运行同一台确定性状态机，不从调用边界切 token；Flush 是显式不可再等待未来的边界，强制当前匹配/字面量结束并写刷新标记。
2. **重叠回指**：距离 `d`、长度 `l` 且 `l>d` 时，源区间与目标区间重叠。一次性复制会读到尚未复制的目标内存；正确语义是输出第 `k` 字节时，若 `k<d` 读历史窗口，否则读本次回指中第 `k-d` 个已经生成的字节。解压器直接逐字节执行该规则。解压本身必须逐字节产生输出，且要更新窗口与校验，逐字节循环没有增加复杂度，常数开销可接受。
3. **两类距离错误**：距离为 0 没有源字节；距离超过窗口容量说明编码引用了压缩器不可能保留的历史；距离不超窗口但超过已输出字节数，说明引用早于流起点。压缩器只把当前窗口内、位置严格早于当前指针的候选加入哈希链，因此绝不会产生超窗口引用。并行块的预置字典只是匹配器的非输出历史；标准流中的解压器没有预置字典，上一块字节已经成为“已输出字节”，所以判定含义不变。
4. **并行确定性**：第 `i` 块只读取本块原文和上一块末尾至多一个窗口的原文，用这些字节构建私有窗口和哈希链；不读取其他块、其他块输出或 goroutine 完成顺序。块内采用相同的确定性最长匹配与候选平局取最近距离规则，所以 workers 改变只影响调度。块结束被强制为 token 边界；块记录直接拼接，不加刷新标记，因为解压器窗口连续，下一块的跨块回指自然引用已经输出的上一块字节。整个流只有一个流头和一个流尾。

## 状态与上限

- 三字哈希、链式候选；候选链长度和滑动窗口容量均可配置，构造时拒绝 0。
- 回指长度在复制前先加总并检查输出上限，超限立即进入终态；不按声明长度分配内存。
- 单个 Compressor/Decoder 实例不要求并发安全；并行压缩使用彼此独立的匹配器。
