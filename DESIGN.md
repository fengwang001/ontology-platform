# 差分同步校验与增量传输器 — 设计推导

模型：目标端有数据 T，源端有数据 S。目标端把 T 的签名发给源端；源端用签名对 S 做匹配，产出补丁；目标端应用补丁后得到与 S 逐字节一致的结果。双方数据与补丁均写本地临时目录。

## 1. 弱/强校验和的分工推导

- 弱校验和 `W`：加法型滚动校验和，`W(B) = Σ B[k] (mod 2^32)`。O(1) 滚动，用于在每个偏移快速筛选候选块。
- 强校验和 `H`：SHA-256，用于确认候选块内容真的相同。
- **不能只用弱**：`W` 是加法型，碰撞极易构造（如 `{1,2,3,4}` 与 `{4,3,2,1}` 和均为 10）。若只凭弱匹配就发「复用块 j」指令，目标端会把内容不同的块当成相同块复用，补丁应用后数据与源端不一致，且全程无报错——静默错误。
- **不能只用强**：强校验和没有滚动性质，在偏移 i+1 处必须对整块重算，每个偏移一次 SHA-256，总代价 O(n·块大小)，失去滚动的意义，退化为逐块暴力比对。
- 结论：弱命中才算强（强校验计算次数 ≤ 弱匹配次数），强命中才复用。两级缺一不可。

## 2. 滚动推进公式

窗口 `[i, i+B)` 的弱校验和 `W(i) = Σ_{k=i}^{i+B-1} data[k]`。
推进到 i+1：`W(i+1) = W(i) - data[i] + data[i+B]`（uint32 自然回绕，减法无需特判）。
代价：初始窗口 B 次加法，之后每步 1 加 1 减共 2 次基本运算，全程 `B + 2(n-B) ≤ 2n ≤ 4n`，与数据长度成正比。`chunk` 包用非导出原子计数器记录基本运算次数，测试断言不超过 `4*n`；若每个偏移重算则为 `n*B` 量级，会被该断言挡下。
匹配命中后跳到 `i+B`，窗口在新偏移从头重算（重算总代价 ≤ 匹配数×B ≤ n，预算仍成立）。

## 3. 签名与补丁格式

签名（`sig`）：`magic"OSIG" | blockSize u32 | totalLen u64 | count u32 | count*(weak u32 | strong 32B)`。末块不满时以其真实长度参与，长度由 `totalLen`/`blockSize`/下标推出。

补丁指令（`diff` 产出）：`Reuse{块号}`（复用目标端第 j 块）或 `Data{字节}`（新数据，按序拼接进数据段）。

补丁文件（`patch` 编码）：
`magic"OSYN"(4) | blockSize u32 | srcLen u64 | srcHash 32B | instrCount u32 | 指令区 | 数据段 | CRC32 u32`
- 指令区每条：类型 u8（0=复用/1=数据）+ u32（块号 或 数据长度）。
- 数据段：所有 Data 指令的字节按序拼接，解码时按指令长度依次切分。
- `srcHash` 是源端整体 SHA-256，供目标端最终校验（不计入匹配统计）。
- CRC32 覆盖其前全部字节，防截断与比特损坏。

## 4. 截断分类与「不得部分应用」

解码按区间判定截断点（长度记为 L，指令区终点 I，数据段终点 D）：
- `L < 头长(52)` → `ErrTruncatedHeader`
- `头长 ≤ L < I` → `ErrTruncatedInstr`
- `I ≤ L < D` → `ErrTruncatedData`
- `D ≤ L`（CRC 缺失或不符）→ `ErrCRCMismatch`

实现方式：**先校验整份再落地**。`Apply` 先把全部指令校验一遍（块号越界、块大小非法一律报错），再在内存中构建完整结果，只有全部成功才返回；目标端原有数据从头到尾不被修改（结果写入新切片），因此任何失败都保证目标端逐字节不变。`verify.Final` 再比对结果长度与整体 SHA-256，检出「签名生成后目标数据被改动」这类静默不一致。

## 5. 错误分类（均可 `errors.Is` 判定）

`ErrTruncatedHeader` / `ErrTruncatedInstr` / `ErrTruncatedData` / `ErrCRCMismatch`（patch 包），`ErrBlockOutOfRange`（patch 包），`ErrChecksumMismatch`（verify 包），`ErrZeroBlockSize`（sig 包）。

## 6. 确定性与并发

diff 只通过「弱校验和 → 候选下标列表」做 map 查找，从不遍历 map，指令序列只依赖输入字节，故同一（源，目标）对补丁逐字节确定。计数器用 `sync/atomic`，多协程并发 diff 互不干扰，`-race` 干净。

## 7. 边界语义

源空→补丁零指令；目标空→全部为 Data；两者相同→全复用（含不满的末块，新数据 0 字节）；前缀关系→前缀部分复用、其余为 Data；块大小 > 数据长→单块（不满）处理；块大小 ≤ 0→`ErrZeroBlockSize`；长度为块大小整数倍/非整数倍均正确，末块不满按真实长度匹配。
