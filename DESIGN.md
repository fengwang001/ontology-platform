# 严格模式流式 Base64 设计说明

## 规范形式的推导（语义 2）

Base64 把每 3 字节编码为 4 个 6 bit 字符，共 24 bit，恰好无剩余。
最后一组不足 3 字节时，字符承载的比特数超过数据比特数：

- 剩 1 字节：需 2 个字符 = 12 bit，数据只占高 8 bit，
  第 2 个字符的低 4 bit 不对应任何输出字节（无主比特）；
- 剩 2 字节：需 3 个字符 = 18 bit，数据只占高 16 bit，
  第 3 个字符的低 2 bit 是无主比特。

宽松解码器直接丢弃无主比特。Q=16=0b010000，R=17=0b010001：
QR== 与 QQ== 的高 8 bit 都是 01000001='A'，于是两个不同的
编码串解出同一字节串，编码不再唯一。严格模式的统一规则由此得出：

> 所有无主比特必须为 0。即 pad=2 时第 2 字符的值 & 0x0F == 0，
> pad=1 时第 3 字符的值 & 0x03 == 0。

判定只用这几个比特：pad=2 查第 2 字符值的低 4 bit，pad=1 查第 3
字符值的低 2 bit。该规则等价于「Encode 是单射」：每个字节串只有
唯一合法编码，因此 Decode(Encode(x)) == x，且对被接受的 y 有
Encode(Decode(y)) == y。QR==（低 4 bit=0001）与 QUJ=（J=9，
低 2 bit=01）违反此规则，必须拒绝。

填充的唯一性同理：长度必须恰好补到 4 的倍数、= 只能出现在整个流
的末尾一组，否则同一字节串会有多种合法写法。故 QQ/QQ=（长度错）、
QQ===、QQ==QQ==（填充位置错）均拒绝。

## 错误分类

五类解码错误为可判定的哨兵错误：ErrInvalidChar、ErrNonCanonical、
ErrPadding、ErrLength、ErrNewline，加上限 ErrLimit；均包装在
*stream.Error 中，携带整个输入流的绝对字节偏移。

## 语义与测试对照表

（待测试写完后补充）
