# DESIGN

## 末尾换行

规范化先做两件局部事：CRLF/独立 CR 都变为 LF；LF 前及 EOF 前连续的空格、制表符删除。之后再处理末尾 LF：

- `Keep`：保留已有末尾 LF 序列，不新增字节。
- `One`：已有末尾 LF 序列压缩为一个；没有 LF 时不新增。
- `TrimBlank`：删除末尾空行并保留最后一个已有 LF；没有 LF 时不新增。

不新增 LF 是映射恒等式推出的硬约束：`ToOut∘ToOrig=id` 且两者单调，要求输出位置能单射到原文位置，故输出长度不能超过原文长度。给 `a` 新增 LF 后，新增 LF 与输出终点都只能锚到原文终点，函数无法同时返回两个输出偏移。因此空输入在任何策略下均为 `""`；这也天然幂等。

| 输入 | Keep | One | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三种策略的输出仍只以零个或一个 LF 序列结尾，重复规范化不会再改字节或映射。

## 被删字节的 ToOut

位置采用左闭右开游标。保留段一一对应；删除段的输出宽度为 0，其输出坐标取“紧随该删除段的保留位置”。

- CRLF 的 CR 指向下一个字符，即输出 LF 的游标之前：`"a\r\n"` 中 CR 的原文偏移 2 映射到输出偏移 2。
- LF 前被删空白也指向输出 LF 之前，而不是 LF 之后；编辑器光标落在这些空白中时，会停在逻辑行尾、换行符之前。
- EOF 或末尾策略造成的尾部删除没有后继字符，统一指向输出终点。

该约定下单调不减成立；每个输出位置都选择其后的保留字节，因而 `ToOut(ToOrig(o))==o`。

## 缓冲与输出上限

未判定空白必须缓存，因为后续普通字会令其保留，后续 CR/LF/EOF 才令其删除。若选择“强制输出”，之后到达 LF 时已无法撤销输出，会破坏行尾空白语义和幂等；所以本实现选择拒绝。待判定空白达到上限时，在当前原文偏移返回可判定的 `ErrPendingLimit`，保留此前输出并进入终态。输出上限在追加前检查，超限返回 `OutputLimitError`；严格 NUL 在消费该字节前返回 `NULLError`。

## 流式与并行

CR 只延迟一个字节：下一字节是 LF 则删 CR，否则先把旧 CR 变成 LF。空白零个或多个连续缓存；任何切分方式都只让 CR 或一串尾随空白悬而未决。

`par` 在启动 worker 前顺序调整切点：切点左侧是 CR 时左移 1 字节；左侧是空格/制表符时继续左移到该串起点。这样整串空白及其判定字（普通字、CR 或 LF）总在同一 worker。CR 前的空白即使留在左段，其 Close 也会按尾部空白删除，与右段产生的 LF 位于同一拼接输出坐标。修正范围上界是待定空白长度，受 `MaxPending` 限制，CR 本身上界为 1。

各 worker 使用调整后的 `[start,end)` 独立规范化。拼接输出时，段内映射的原文起点加 `start`、输出起点加前几段输出总长；删除段宽度仍为 0，所以跨边界映射自然落在拼接点。空段产生空输出和恒等空映射。
