# 设计推导

## 1 空行与单列空值
记录以行终止符结束；终止符若在本记录**尚未开始**（无字段、无内容）时到达，则整行是空行，词法层不产出任何事件，直接跳过。因此空行 ≠ 一条空记录；流末换行也不产生幻影记录。
单列表里 `a\n\n` 的第二行是空行（跳过）；而 `a\nb\n` 是两条记录，未结束的流 `a\nb` 与 `""` 行（`a\n""\n`）都合法。单列记录的唯一字段为「未引号空」时，其字节形态只能是零字节字段紧跟换行，即 `\n`——它与空行**无法在词法上区分**，必然被跳过。故回写器在**列数为 1** 且字段为未引号空时必须写成 `""`（加引号后该行有内容，不再是空白行）；不加引号会丢一条记录。列数 ≥2 时空字段两侧有逗号，零字节可唯一表示，无需引号。

## 2 par 切点
切点可落在引号字段、`""` 或 `\r\n` 中间。每段在**同一遍字节循环**内并行推进两台机器：O（假设段起点在引号外、记录刚开始）与 I（假设段起点在引号字段内部，且继承已写字节计数）。两台机器对同一段字节各处理一遍，共 2N 次，不存在重扫；段间不共享内存。
从左到右串行拼接：第 0 段用 O。前一段 O 的结尾状态决定下一段选哪台——结尾在引号外用 O；结尾在引号内（Q）则选 I 的对应结果，并做两处修正：(a) 若 I 的首个字段事件/尾缓冲落在本段，其值要**前缀拼接**前一段未闭合字段已积累的内容，字段号沿用全局值；(b) O 在段首裸字符上可能报的「裸引号」错误一律丢弃（该引号在真实轨迹中是字段内字符）。继承状态为 CR 待定时，下一段首字节若不是 `\n`，立即在**前一段尾偏移**处生成 dangling-CR 错误。
坐标换算：段内字节偏移加段起点得全局偏移；记录号、字段号加前段已完成计数（字段号仅在记录边界清零）。错误在第一个段确定后，后续段不再执行。

## 3 CR 待定
未引号字段见到 `\r` 进入 CR 状态但不结束记录：下一字符是 `\n` 则提交记录；是其他字节则在 `\r` 自身偏移报 dangling-CR。半包续传时该状态随解析器挂起，`\r` 可停在流末；`Close` 在 CR 状态报 dangling-CR。par 段把 `\r` 留在段尾时，下一段首字节作同样判定（见 §2）。引号字段内的 `\r` 永远是普通内容，不进 CR 状态。

## 4 上限「立刻」
字段大小按**解码后字节**计：普通字符 +1；引号字段内连续两个引号 `""` 解码为一个引号字符，在见到第二个引号时 +1（不是 +2）。每接受一个字符立即比较，超限在该字节（`""` 为第二个引号）处报错，不缓冲整条记录。最大字段数在接受每个字段时检查；记录上限在记录完成时检查。终态后 `Feed/Close` 返回同一错误。

## 5 状态转移表（5 状态）
S=字段开始 B=未引号字段中 Q=引号字段中 A=引号内刚见引号 C=CR待定。类别：`,` `"` `\n` `\r` 其他 o
| 状态 | , | " | \n | \r | o |
|---|---|---|---|---|---|
| S | 空字段→S | →Q | 空行(跳过)→S | →C | 追加→B |
| B | 提交字段→S | ErrBareQuote | 提交记录→S | →C | 追加→B |
| Q | 追加 | →A(暂不追加) | 追加 | 追加 | 追加 |
| A | 提交字段→S | 追加(+1)→Q | 提交记录→S | 提交记录→S | ErrAfterQuote |
| C | ErrDanglingCR | ErrDanglingCR | 提交记录→S | ErrDanglingCR | ErrDanglingCR |
EOF 处：Q→ErrUnclosedQuote；C→ErrDanglingCR；其余提交未完成字段/记录。错误偏移指向被拒绝字节（dangling-CR 指向 `\r`）。
