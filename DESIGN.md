# 连接顺序代价模型与计划选择器 — 设计推导

## 1. 代价模型与基数估计

- 扫描代价：`scanCost(t) = rows(t)`（顺序扫描，每行单位代价）。
- 连接代价（哈希连接，build+probe）：`joinCost(L,R) = card(L) + card(R)`。
- 计划总代价递归：`cost(J) = cost(L) + cost(R) + card(L) + card(R)`，叶子为扫描代价。
- 连接基数：`card(J) = card(L) * card(R) * Π sel(p)`，p 为横跨两侧的等值谓词；
  无谓词时退化为笛卡尔积 `card(L) * card(R)`。
- 等值连接选择率：`sel(a=b) = 1 / max(ndv(a), ndv(b))`（经典包含假设）。
- 同一对表间多个谓词：选择率相乘，并在 explain 标注「独立性假设」。适用范围：
  各谓词列分布在统计上近似独立时估计无偏；列相关时会低估基数，仅用于计划间
  相对比较，不做绝对承诺。
- 缺统计的列：选择率回退到写死默认值 `0.1`，标注「估计不可靠」，不中止。
- 估计只读取目录/统计聚合值，不触碰行数据（`stats.RowDataReads` 计数器恒为 0）。

## 2. 并列打破（tie-break）的推导

问题：代价是 float64，数学上等价的两个计划因浮点求值顺序不同会差若干 ULP
（如 `(x+y)+z != x+(y+z)`），直接用 `==` 或 `<` 会让选择随枚举顺序漂移。

推导：
- double 尾数 53 位，机器精度 `eps = 2^-52 ≈ 2.2e-16`。一次加法相对误差 ≤ eps。
- 一个计划的代价由 ≤ 2n 次加法/乘法累积（n ≤ 16），累积相对误差上界约
  `2n * eps ≈ 7e-15`。取阈值 `relEps = 1e-9`，比累积误差高 5 个数量级，
  足以吸收一切舍入漂移；同时真实不同的计划（行数不同）代价差通常远大于
  1e-9 相对量级，被误判为并列的概率可忽略。低于阈值的差异对计划选择
  本无意义，视为并列是可接受的近似。
- 判定规则：`|a-b| <= 1e-9 * max(1,|a|,|b|)` 判为等代价；等代价时比较
  **计划叶子的中序表名序列**（字典序小者胜）；再并列则比较规范化树形串
  （左深/右深的结构序列），保证比较是确定性的全序，与枚举顺序无关。
- 同一子集的两个计划参与表集合相同，故「参与表名序列」只能取叶子中序序列
  才有区分度；这是该规则的关键观察。

## 3. 笛卡尔积推迟规则

按子集 DP：对子集 S，先求谓词图在 S 上的连通分量（并查集）。

- 若 S 连通（k=1）：只考察「两侧之间有谓词横跨」的划分，笛卡尔划分一律跳过。
- 若 S 不连通（k>1）：只考察「每侧都是完整连通分量之并」的划分，这些划分
  必为笛卡尔积（分量间定义上就无谓词）。注意：若划分切开了某个分量，连通性
  保证必有一条谓词边横跨，因此「无横跨谓词」⟺「两侧都是分量之并」。

效果：链式图 A-B-C-D 的任何子集都连通或退化为孤立点，最优计划绝不出现
笛卡尔积；A-B、C-D 双分量图在顶层只能以分量并划分，笛卡尔积恰好一次且
位于最后一步。该规则即「存在可用谓词时绝不先做笛卡尔积；仅当剩余表与
已选集合之间无任何谓词时才允许」。

## 4. 复杂度与资源上限

- 子集数 ≤ 2^n，划分数 ≤ 3^n（每表三态：左/右/不在），远小于 n!。
- 硬上限 `MaxTables = 16`（状态表 2^16 项），超限返回 `ErrTooManyTables`，
  在枚举前拒绝，计数器保持 0，内存有界。
- 选择器每次调用独立持有 DP 表，共享只读目录视图，并发安全。

## 5. 统计异常判定

- 缺失：谓词引用的列无统计 → 回退默认选择率 0.1，警告包装 `ErrMissingStats`。
- 过期：统计行数与目录行数相对偏差 > 20% → 警告包装 `ErrStaleStats`，
  基数估计按目录行数校正。阈值依据：20% 是常见统计刷新周期的正常漂移上限。
- 损坏：直方图桶计数和 ≠ 声称总行数，或桶边界非递增 → 硬错误包装
  `stats.ErrCorruptStats`，消息含表名与列名，计划选择中止。

## 6. 边界语义

- 零表：`ErrNoTables`；单表：直接返回扫描计划，代价 = 扫描代价。
- 自连接谓词（两侧同表）：登记时拒绝，返回 `ErrSelfJoin`（本设计不支持
  同表谓词，避免叶子基数与连接基数语义混淆）。
- 全表行数为 0 / 行数为 1：公式自然成立，等代价由并列打破规则兜底。
