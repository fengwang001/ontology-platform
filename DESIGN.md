# 多版本可见性存储：读视图与回收器设计推导

模块划分：`txid`（事务号分配）→ `version`（单键版本链）→ `snapshot`（活跃事务集合与可见性）→
`reclaim`（水位与候选堆）→ `store`（对外存储）。依赖严格单向，反向依赖不存在。

事务号即提交号：事务在 `Begin` 时从分配器取号，提交时不再另取提交号。分配器单调递增、不回绕、
零值非法（`txid.Allocator` 从 1 开始）。快照点 `P` 定义为快照建立那一刻分配器的「下一个待分配号」
（`Curr`，不消费）。时间只来自注入的 `Options.Now`，代码不直接调用 `time.Now`。

## 1. 可见性判定（需求 4）：为什么是左闭右开

快照隔离 + 可重复读要求：同一快照对同一键的任意次读返回完全相同的结果。由此推出可见性必须是
「快照元数据 × 版本元数据」的**纯函数**，不得依赖读取发生的时刻，也不得依赖「未来是否还有提交」。

快照元数据只有两类信息可用：快照点 `P` 与建立时仍在活跃的写事务集合 `A`。于是版本 v 对快照 S
可见的判定只能是 `commitTx(v)` 与 `P`、`A` 的比较：

    visible(v, S)  <=>  commitTx(v) < P  且  commitTx(v) ∉ A
    读结果 = 链上满足 visible 的版本中 commitTx 最大者

推导边界的开闭：`P` 是建立快照时的 next-id。任何满足 `commitTx == P` 的事务必然是在快照建立**之后**
才被分配号的（否则 `P` 不会仍是 next-id），即它建立快照时尚未开始。若把边界取成闭区间
（`commitTx <= P`），那么「号为 P 的事务在读取前是否已提交」就会进入判定——可见性将依赖读取时刻
之后发生的事件，可重复读被破坏。因此上界必须开：`commitTx < P`。下界无界（任何足够老的已提交版本
都可见），故区间天然左闭（趋向 0）右开。`commitTx ∈ A` 排除「快照建立时尚未提交」的事务：它们当时
未提交，之后是否提交、何时提交都属于快照建立后的事件，同样不能进入纯函数。这两条合在一起恰好是
「快照建立那一刻已提交」的严格形式化。

## 2. 回收安全与增量策略（需求 5、6）

**安全条件推导。** 版本 v 可被回收，当且仅当不存在任何现在或未来的快照会选中它。

- 未来快照：其快照点 ≥ 当前 next-id > 一切已提交号，活跃集为空，因此它必然选中链上 commitTx 最大的
  已提交版本（链头）。任何非链头版本对未来快照恒不可见，无需检查。
- 当前快照：设 v 的「链上最近的已提交上位版本」为 `Above`（链按 commitTx 有序）。S 选中 v 当且仅当
  `Eligible_S(v.Tx)` 成立且 v 之上没有任何对 S 可见的版本。若 `Eligible_S(Above)` 成立，则 S 会选中
  Above 或更高者，绝不会选中 v。故充分安全条件为：

      reclaimable(v)  <=>  对每个活跃快照 S： ¬Eligible_S(v.Tx)  ∨  Eligible_S(v.Above)

  该条件偏保守：存在「Above 在 S 活跃集内、但更高处有对 S 可见版本」的罕见情形，此时 S 实际看不到
  v，条件仍判定保留。保守方向是安全的（宁可多留，不可误删），且只延迟回收，不影响正确性。

**增量数据结构。** 候选集合用「以 `Tx` 为键的最小堆 + `(Key,Tx)→堆节点` 索引」维护。关键观察：一个
已提交版本只有在「它上方落下新的已提交版本」那一刻才从可见头变成候选——这是唯一需要入堆的事件。
因此提交时只为每个被追加版本找到链上紧邻下方的已提交版本，把它入堆（或用它更新既有候选的 `Above`，
取更小者），O(log C)。回收时从堆顶弹出：若堆顶可回收则弹出、推进水位、从链上摘除，继续看新堆顶；
否则停止。每次回收考察数 = 本次实际回收数 + 1（堆顶不可回收时的最后一次检查），与总键数 N、总版本
数完全无关。最坏情况上界 = 当前堆内候选数 + 1，而候选数 ≤ 上次回收以来的提交产生的遮蔽数，稳态下
为 O（两次回收之间的提交数）。N=100 与 N=10000 的对照测试验证考察数不随 N 增长。

## 3. 崩溃点枚举（需求 8）

提交路径划分为三个阶段，共 4 个可注入崩溃点（`Options.CrashHook(stage)` 在边界处回调，可 panic 模拟
崩溃；写事务的写入先落在事务私有缓冲区，提交前不触碰任何共享结构）：

- 点 0：阶段 A（追加版本入链）之前。共享状态零改动，回滚缓冲区即可，无任何残留。
- 点 1：阶段 A 之后、阶段 B（候选记入回收索引）之前。链上可能已有该事务部分版本，但它们不属于任何
  「已终结事务」集合；恢复时按事务的追加记录逐条摘除，链恢复原状。
- 点 2：阶段 B 之后、阶段 C（终结：注销活跃事务、关闭内部快照）之前。索引里可能已存在以崩溃事务号
  为 `Above` 的候选，指向即将被摘除的版本体——这正是「索引指向无版本体槽位」的半可见形态。恢复时
  对受影响键执行 `RemoveKey` 后**从链上现存版本重新推导**全部候选（相邻已提交对），索引与链重新一致。
- 点 3：阶段 C 之后。事务已终结，完全可见，恢复无操作。

恢复不变式：**可见性的原子边界是阶段 C 的终结动作**。`Recover` 将所有未终结事务整体回滚（摘除已追加
版本、修复挂起计数、重建受影响键的候选索引、注销事务），因此任意崩溃点后重启，事务要么完全可见
（点 3），要么完全不可见且无残留（点 0–2），不存在第三种状态。测试用循环遍历全部 4 个点。

## 4. 水位推进与新快照建立的竞态（需求 7）

两个临界区，均由同一把 `store.mu` 保护（`snapshot.Manager` 内部的 `mgr.mu` 是叶锁，锁序恒为
store → manager，不会死锁）：

1. `OpenSnapshot`：在临界区内原子地完成「读 next-id 作为快照点 + 拷贝活跃事务集 + 注册快照」。
2. `Reclaim`：在临界区内原子地完成「取活跃快照列表 + 逐个判定弹出候选 + 推进水位」。

两者互斥，故只有两种交错：(a) 快照先于回收注册——它出现在回收的快照列表里，凡它可能看到的版本
都不会被回收；(b) 快照在回收之后注册——其快照点 ≥ 当前 next-id，大于一切已提交号，活跃集为空，
它只见各链链头，而链头（commitTx 最大的已提交版本）永远不会成为候选，故它绝不可能读到已被回收的
版本。水位 `watermark` 只在回收临界区内按弹出顺序单调更新（弹出键 `Tx` 严格递增），只升不降；任何
读路径都不依赖水位做可见性判定（水位仅是查询指标与不变式锚点），因此水位推进本身不可能让任何快照
读到已回收版本。
