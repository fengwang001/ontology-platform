目标：按角色授予列可见性；谓词引用不可见列时给出不泄露信息的处理，并产出可审计裁剪报告。仅用标准库，状态在内存。

## 1. 不可见列出现在谓词中：三种做法

- 甲（把不可见列比较当作假）：泄露。探针 `NOT (secret = 1)`：比较恒假，NOT 后恒真，攻击者凭“行被返回”反推 secret 存在且被策略隐藏。
- 乙（把不可见列当 NULL 走三值逻辑）：仍泄露。`NOT (secret = 1)` 中 NULL=1 为 UNKNOWN，NOT UNKNOWN=UNKNOWN，行被滤掉——“空结果”本身就是探针信号；且 `secret IS NULL` 恒真，攻击者直接确认隐藏列存在（若列真实存在但非 NULL 结果应不同，差异即可观测）。
- 丙（整条查询拒绝，指名列与路径）：不泄露。拒绝发生在行数据被求值之前，攻击者无法从放行/滤除差异推断任何值或存在性语义（拒绝信号是安全的：它只暴露“此角色不能用此列”，而这本就是角色已知的策略事实）。

本实现采用丙：一次自底向上遍历谓词树，折叠常量并收集“被实际读取的不可见列引用”。存在任一此类引用即返回可判定错误（列名 + 路径），不返回任何行。

## 2. OR 短路例外

定义：自底向上常量折叠。若某子树折叠为常量真/常量假，则该子树内的不可见列引用视为**未被读取**，不计入拒绝集合；兄弟分支引用不可见列仍拒绝。

理由：常量折叠是纯语法变换，不触碰行值。`OR` 的一支折叠为常量假时等价于不存在（`FALSE OR p == p`），该支运行时不求值，不可见列从未被读取，拒绝反而误杀；折叠为常量真时整个 OR 恒真，其余支同样不求值。对称地，`AND` 中常量真支被吸收、`NOT` 包裹的纯常量也整体消失。例外只授予“可证明为常量”的子树，无法证明时一律拒绝（安全默认）。

## 3. 数据模型与路径

- 谓词节点：`Const{Value}`、`Cmp{Col, Op}`（Op: =, !=, <, <=, >, >=, IsNull）、`Not{X}`、`And{Xs}`、`Or{Xs}`。
- 路径：从根到引用点的字符串，`$` 起，AND/OR 第 i 个子节点追加 `/and[i]`、`/or[i]`，NOT 追加 `/not`，比较追加 `/cmp`。例：`NOT (secret = 1)` → `$/not/cmp`；`a=1 OR secret=2` 第二支 → `$/or[1]/cmp`。
- 行：`map[string]any`。裁剪只遍历“可见列”，从行中取键构造新 map；不可见列既不出现也不置零（不可见 ≠ 空）。
- 策略：角色 → 有序列全集（字典序）+ 可见集合；不可见列 = 全集差集，保证裁剪报告与构造顺序无关。

## 4. 一遍遍历与复杂度

- 遍历自底向上，一次完成：节点计数、常量折叠、不可见引用收集。每个节点恰访问一次，计数器 = 节点总数，不重复遍历。
- 行裁剪拷贝次数 = 实际存在于行中的可见列数（每列至多常数次 map 操作），与行的总列数无关。千列五可见时拷贝次数上界 `4*5`。

## 5. 三类错误（errors.Is 可分）

- `ErrHiddenColumn`：谓词读取了不可见列（拒绝执行；详情含列名+路径）。
- `ErrUnknownColumn`：谓词引用了策略全集之外的列。
- `ErrInvalidPredicate`：结构非法（如 AND/OR 无子节点）。

## 6. 边界与穷举定义

- 空可见集：任何列引用都拒绝；无列引用（纯常量/空谓词）放行，行裁剪为空 map。
- 全集可见：永不因隐藏拒绝；引用未知列仍报 `ErrUnknownColumn`。
- 空谓词（nil）：无过滤，所有行仅按可见列投影。
- 同列多次出现：列名集合去重；拒绝引用列表保留全部路径。
- 穷举：列 a,b,c；可见性 8 种 × 形态 4 种（`=` 单引用 a；`NOT(a=…)` 引用 a；`AND(a=…,b=…)`；`OR(a=…,b=…)`）共 32 种。非折叠的非常量形态：引用列与不可见集相交即拒绝，否则放行。预期：`=`/`NOT` 各放行 4 拒绝 4；`AND`/`OR` 各放行 2 拒绝 6（b 始终可见时 OR 也拒绝——例外仅对折叠为常量的支生效，穷举模板的支均为非常量比较）。
