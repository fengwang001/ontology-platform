
# 只读快照一致性导出器 — 设计推导

## 1. 模型
- `store`：`map[key]{value, ver}`，一把 `RWMutex`；`Put(k,v)` 给键分配单调递增版本。
- `snapshot`：句柄记录版本水位 `v` 与所属 store。读取语义是「版本 ≤ v 的最近值」。

## 2. 写时复制（COW）的触发点
若导出直接读 live map，导出中途被改写的键会读到新值，快照不一致。
推导：快照 `s@v` 对键 k 应返回「v 时可见值」。当 `Put(k)` 携带新版本 w>v 发生时，
live 值将被覆盖，必须在覆盖**之前**把 k 的旧 entry（含 absent 标记，
用以区分「不存在」与「值为空字节串」）保存进 s 的保留表；s 读 k 时先查保留表，
未保留再读 live（live 版本必 ≤ v，因为任何 >v 的写入都会先触发保留）。
store 上可能有多个重叠快照，一次 Put 需通知所有水位 < w 的快照各自保留；
保留表挂在快照对象上，故「为某快照保留的值」数量就是该快照的 COW 内存量。

## 3. 保留值何时释放 — 为什么按最老快照版本判断
store 维护已注册快照集合。新 Put 的键版本 w 只需被「水位 < w」的快照保留。
等价地：键的旧 live 值对所有版本 ≥ 最老存活快照水位(minV) 的读取仍安全地留在 live；
一旦 minV 前移（最老快照关闭），其保留表整体释放；其余快照各自只持有自己仍需要的项。
两个重叠快照 s1@v1、s2@v2(v1<v2)：s1 关闭后，仅 s1 的保留释放；
在 (v1,v2] 间被改、被 s2 保留的键仍由 s2 持有，直到 s2 也关闭才归零。
因此释放判定看「最老存活快照版本」，且每个快照关闭只丢弃自己的保留表。

## 4. 导出顺序无关性
总校验和对**内容集合**求和：每条记录贡献 `sha256(len(k)||k||v)`，
manifest 存 `xor` 汇总（异或可交换、可结合）。块的物理排列只影响块文件布局，
不影响集合哈希；故字典序/逆序导出 manifest 的 RootHash 相同（逐字节文件不必相同，
比较的是内容集合与总校验和）。

## 5. 分块格式、续传与内存
文件 = 一行 JSON manifest + 顺序块。块：`MAGIC(4)|index(4 BE)|dlen(4)|crc(4 BE)|body`，
crc = IEEE CRC32(body)。manifest 含块数、块体偏移/长度、快照版本、键数、RootHash。
记录自描述：`klen(4 BE)|vlen(4 BE)|k|v`；一块顺序打包多条记录（贪心，单条可超块大小成一块）。
续传：用快照键的规范顺序重算计划块；已存在文件按 manifest 偏移逐块校验 CRC，
从第一个缺失/损坏块 k 继续重写其后内容（截断续写，末尾原子 rename）。
驻留内存峰值 = 当前块 body（≤ 一块大小，外加一条溢出记录），用计数器在 put/reset 处测峰。
每个键恰好 `Snapshot.Get` 一次（每键仅在打包它的块时读取）。

## 6. 四类可判定错误（errors.Is）
`ErrManifestIncomplete`（manifest 行未以换行结束/JSON 不完整）、
`ErrBlockHeaderIncomplete`（块头 16 字节被截断）、
`ErrBlockBodyIncomplete`（声明的 body 或 crc 尾被截断）、
`ErrCRC`（crc 不符，含单字节损坏）；另有 `ErrSnapshotExpired`、
`ErrBlockOutOfOrder`/`ErrMissingBlock`（携带块号）、`ErrBadBlockSize`。
截断点分类：解析到清单行被截→清单不完整；块头区间被截→块头不完整；
块体或 crc 尾被截→块体不完整；长度完整但字节被改→CRC 不匹配
（纯长度截断改不动已完整块的内容，CRC 类由等长损坏注入覆盖）。

## 7. 快照过期为何必须拒绝续传
续传 = 旧块（时刻 v）+ 新读块。若快照已关闭，其保留值已释放，
新读块来自当前 live，拼出跨时刻文件：集合哈希可能巧合通过但语义已撕裂。
故续传前检查快照存活性（关闭即标记），不存活直接 `ErrSnapshotExpired`，
且不动磁盘上已导出部分（先判活再开文件，错误路径不删除任何文件）。

## 8. 并发与关闭竞争
store 读写经 RWMutex；快照保留表有独立锁。
导出开始 `Begin()` 占一个活跃读者计数，`Close()` 等全部导出结束后再释放保留并标记过期；
此后任何续传得到 `ErrSnapshotExpired`。关闭与导出不会并发撕裂保留数据，
也不会把半截文件当作完整（manifest 只在全部块写完后才写）。
