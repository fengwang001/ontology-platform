# Quoted-Printable 设计（RFC 2045 严格子集）

## 推导一：行末空白

解码器按硬行（`\r\n`）与 EOF 界定行；行末裸空白若原样保留，会被传输链路或按行
处理的下游悄悄裁掉，数据就无法往返。因此空格/制表符是否转义取决于**它之后的下一个
输入字节是什么**：下一个是换行符，或之后再无字节，则输出 `=20`/`=09`；否则原样。
扫描时向前看 1 字节即可判定，无需回退重扫。

样例：`"a \n" -> "a=20\r\n"`；`"a b" -> "a b"`；`"a "`(EOF) `-> "a=20"`。

## 推导二：`=XX` 原子与软换行位置

每个 token 是 1 个原样字节或 3 字符的 `=XX`，token 不可跨行。行上限 76（不含
`\r\n`），软换行写作 `=\r\n`，其中 `=` 占用行内 1 格。设当前列号为 col，
token 长度为 k，本行之后是否还有 token 为 more：

- more 且 `col+k+1 > 76`：先输出 `=\r\n`，列号清零，再输出 token；
- 非 more 且 `col+k > 76`：先软换行再输出 token（末 token 不再需要占位）。

col=74、下一个为 `=3D`(k=3)、more：74+3+1=78>76，故先软换行再写 `=3D`。

## 软换行前的空格

结论：**不转义，仍写成裸空格**。软换行的语义是“本行物理折行，逻辑上无此断行”，
解码器删除的是 `=\r\n` 这三个字符整体；空格位于软换行 `=` 之前，不是逻辑行的末尾
（其后还有下一个 token），不存在“行末空白被裁剪”的风险。编码器只在“下一输入字节是
硬换行或 EOF”时转义空白，软换行由 token 布局产生，不改变该判定。

## 往返等式

令 `N(x)` 为换行规范化：`\r\n` 与单独 `\n` 都换成 `\r\n`，单独 `\r` 保留为字节。
则对任意 x：

`Decode(Encode(x)) == N(x)`。

严格解码接受的编码输出均以 `\r\n` 表示行结束，故其 N 是恒等；对严格合法的 y，
`Encode(Decode(y))` 与 y 的差别只可能是 y 中多转义了可打印字符——编码器是
**最小转义**：仅在 `=`、不可打印字节、以及“后随硬换行/EOF 的空白”三类位置转义，
绝不多写 `=XX`。

## 测试对照表

| 语义条目 | 钉住它的测试函数 |
| --- | --- |
| 1 转义与换行规范化 | `TestEscapeAndNewlines` |
| 2 行长 76 与软换行 `=` 占位 | `TestLineLimitSoftBreak` |
| 3 行末空白 / token 不拆分 / 软换行前空格 | `TestTrailingWhitespace`、`TestTokenNotSplit` |
| 4 五类可区分错误与偏移 | `TestStrictErrors` |
| 5 跨切分点一致 | `TestAllSplits` |
| 6 往返等式 | `TestRoundTrip` |
| 7 幂等与最小转义 | `TestIdempotent` |
| 计数器 ≤ 2N | `TestInspectionCounter` |
