# Properties 解析与回写设计

## 3. 空白续行的推导

`java.util.Properties` 逐物理行读入：先去掉行终止符，再检查行尾连续反斜杠。
只有反斜杠数量为奇数时，最后一个反斜杠才承担续行转义；此前偶数个反斜杠仍保留给键值转义阶段处理。
续行时，读取下一物理行，并把该行开头的空白（空格、制表、换页）整体跳过，再把剩余字符接在已积累文本后。
因此，若续行只含空白或为空，跳过首部空白后剩余长度为零：这一物理行不贡献任何字符。
它不是空注释/空键行，因为注释与空行判定只发生在逻辑行的开头；当前逻辑行已经开始并处于续行状态。
解析器会继续检查这个已变空的缓冲是否还以反斜杠结尾；答案是否定的，所以继续读下一物理行。
结论：空白/空续行被吸收并丢弃；若下一物理行非空白，则其首部空白也按同规则剥除后拼接。
这正说明“首部空白剥离”不是只缩进一个固定级别，而是跳过整个连续空白段；全空白行的整段都被剥掉。

## 6. Store 最小必要转义

Load 先按物理行拼接逻辑行，再寻找第一个未转义的分隔符；回写必须只破坏会改变这两个阶段的字符。
键开头若为 `#` 或 `!`，会被新逻辑行识别为注释，所以必须转义；该字符在键或值其他位置不是行首注释。
键中的 `=`、`:`、空白若不转义，会被选为键值分隔符或从键尾吃掉；键首部空白也会被忽略，所以这些位置要转义。
值开头的空格必须转义：分隔符后、值开始处的空白会被跳过，不转义会丢失。
值中间和末尾空格不转义：分隔符已经唯一确定，Load 只跳过值前导空白，保留其余空白；尾部空白按样例保留。
值中的 `=`、`:`、`#`、`!` 都不是分隔符或注释，因为它们不位于键区或逻辑行行首，无需转义。
键和值中的反斜杠必须写成 `\\`，否则会转义后续字符；`\t`、`\n`、`\r`、`\f` 用标准短转义保证单行可读。
Store 总是输出 `key=value`；空键、空值仍可由等号和空字符串重建，故无需特殊标记。
非 ASCII 字符直接以 UTF-8 输出；Go 的 `Reader` 输入按 rune 解码，天然匹配 `load(Reader)` 而非 ISO-8859-1 流语义。

## 语义与测试对照

待实现完成后补齐。
