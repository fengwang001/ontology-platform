# 规则推导（题面只给不变量）

## 1. 空输入 / 只含换行

先对主体做行规范化（删行尾空白、三种行尾→`\n`），再应用末尾策略。
- Preserve：原样，空即 `""`（无字节即无输出，幂等）。
- EnsureOne：仅当主体非空时裁掉末尾所有 `\n` 再加一个 `\n`；空主体保持 `""`。
  若空输入也补 `\n`，则 `N("\n")="\n"` 幂等仍成立，但会无中生有字节，下游无法区分「真空文件」；
  选 `""` 后 `N("")=""`，且任何非空输出都恰以一个 `\n` 结尾；两种选择都幂等，选 `""`。
- TrimBlanks：裁掉末尾所有空行；空主体无非空行，保持 `""`；否则保留至多一个结尾 `\n`。
  即 TrimBlanks 与 EnsureOne 仅在「全文为空白行」时有差别。

| 输入 | Preserve | EnsureOne | TrimBlanks |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

输出偏移 o 必落在保留字节上，故 `ToOrig` 只命中保留段；被删原偏移 i 的值必须使
`ToOrig(ToOut(i)) == ToOut(i)`（ToOut(i) 是合法输出点），且 ToOut 单调不减。
合法取值是「删段前最后一个输出点」或「删段后第一个输出点」，二者都满足单调性。
取**后界**：删串每个 i 映射到紧随删段之后保留字节的输出偏移（删段在 EOF 则=输出长度）。
行尾空白的后界即该行 `\n` 的输出偏移；`\r\n` 中的 `\r` 也映射到 `\n` 的输出偏移。
光标置于被删空白时应落在行尾换行符**之前的行末**；后界取该 `\n` 偏移，实现统一、严格非减。

## 3. 空白缓冲超限

强制把长空白判为「非行尾」会输出本应删除的尾部空白，违反第 2、5 条，且使第 6 条
映射依赖未来输入、无法自洽。故选择**拒绝**：空白运行长度超过上限时立即返回
`ErrWhitespaceLimit`（带运行起始原文偏移），已产出字节与映射保留，进入终态
（此后 Write 返回 `ErrClosed`）。运行长度有界 ⇒ 状态可解释；拒绝点只依赖已见输入，
不影响第 4 条切分一致性。

## 4. par 拼接与修正上界

每段独立用 norm 处理，但**不在段尾做最终判定**（保留待定 `\r` 与待定空白运行）。
段尾悬尾长度有界：至多 1 个 `\r` 加 ≤ 空白上限 个空白。拼接时把上一段悬尾与下一段
开头一起重判：悬尾 `\r`+下段首字节 `\n` 则该 `\n` 并入删段（CRLF 合一），否则 `\r`
先单独成行；悬尾空白遇行尾则整体删除、遇普通字节则整体保留——与一次流处理一致。
映射段以 (origStart,outStart) 平移后追加，连续同类段合并；删段 origLen>0/outLen=0。
坐标平移是纯加法，拼接映射与单流映射逐区间相同。末尾策略只在拼接后应用一次。
重判只发生在段边界、范围 ≤ 空白上限+2，故有上界。
