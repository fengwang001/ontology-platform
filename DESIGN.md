# 差分数据同步：校验与增量传输器设计

## 1. 弱/强校验和的分工推导

目标：源端不必把整份数据发给目标端，只发「差异」。做法：目标端把本地数据
切成定长块，每块算一对校验和（弱 + 强）作为签名发给源端；源端用滑动窗口
在源数据上逐偏移滚动弱校验和，命中签名后再用强校验和确认。

- **为什么不能只用弱校验和**：弱校验和必须可滚动（O(1) 推进），因此只能是
  少量字节的线性组合，熵极低，碰撞必然存在（本文第 2 节给出实测构造）。
  一旦碰撞，源端会把目标端某块**错误复用**进结果，补丁应用后数据与源端
  不一致，而且全程无报错——这是静默数据损坏，不可接受。
- **为什么不能只用强校验和**：强校验和（SHA-256）抗碰撞，但不可滚动。
  每个偏移都重算一次强校验，代价是 O(长度 × 块大小)，且密码学哈希本身
  昂贵，滚动的意义完全丧失，退化为暴力比对。
- **结论**：弱校验和做快速筛选（可滚动、会碰撞、只负责「可能相等」），
  强校验和做确认（只在弱命中时计算，负责「确实相等」）。两者缺一不可。

## 2. 弱校验和定义与滚动公式

取块 x[i..i+n)，定义（仿 rsync，模 2^16）：

    a = Σ x[j]            (mod 2^16)
    b = Σ (i+n-j) · x[j]  (mod 2^16)   即位置加权，首字节权重 n
    weak = a | (b << 16)  （32 位）

从偏移 i 推进到 i+1（移出 x[i]，移入 x[i+n]）：

    a' = a - x[i] + x[i+n]
    b' = b - n·x[i] + a'

每步 O(1)。正确性测试：随机数据上对每个偏移，滚动结果必须等于在该偏移
从头重算的结果。

**碰撞构造**：n=4 时，块 [1,0,0,1] 与 [0,1,1,0] 的 a 都是 2、b 都是
4·1+1·1 = 3·1+2·1 = 5，弱校验和相同但内容不同。测试中用它验证强校验和
挡住了错误复用。

## 3. 复杂度与传输量上界

- 滚动总代价：从头算一次 O(n)，之后每个偏移 O(1)，全程 O(长度)。
  chunk 包用非导出计数器记基本运算次数（从头算记 n，滚动记 2），
  断言不超过 4 × 数据长度；若每偏移重算则是 长度 × 块大小。
- 强校验和只在弱命中时计算：diff 统计 StrongComputed ≤ WeakMatches。
- 补丁最小化：只差中间一块时，新数据字节数 ≤ 2 × 块大小；完全相同则为 0。

## 4. 补丁格式（大端）

    magic "ONTP" | version u8 | blockSize u32 | srcLen u64
    srcStrong [16]byte (SHA-256 截断) | numInstr u32 | instrLen u32
    指令 × numInstr：
      op=0 复用块：blockIndex u32
      op=1 新数据：dataLen u32 + data 字节
    crc32 u32（IEEE，覆盖之前全部字节）

instrLen 记录指令区总字节数，使解码器在补丁被截断时能精确判断截断点
落在头部、指令还是数据段。签名格式同理：magic、blockSize、块数，然后
每块 weak u32 + strong [16]byte。源数据尾部若与目标端末块（可能不满）
等长且校验相同，则直接复用末块，避免块大小大于数据长度时整份重传。

## 5. 截断分类与「不得部分应用」

解码顺序即分类依据：头部不足 37 字节 → ErrTruncatedHeader；指令定长部分
读不全 → ErrTruncatedInstruction；新数据负载读不全 → ErrTruncatedData；
指令读完但 CRC 缺失或不符 → ErrCRCMismatch。四类均可用 errors.Is 判定。

**原子应用**：patch.Apply 先把整份补丁解码、CRC 校验、指令越界检查全部
做完，在**新缓冲区**里拼装结果，最后比对结果整体强校验和与头部 srcStrong，
全部通过才返回；任何一步失败都直接返回错误。目标端数据从不被就地修改，
因此截断或损坏的补丁绝不可能被部分应用。

## 6. 确定性与并发

diff 只按偏移顺序扫描、按切片顺序查表，不依赖 map 迭代顺序，同一对
（源、目标）产出逐字节相同的补丁。计数器用 atomic，多协程并发 diff
互不干扰，-race 干净。
