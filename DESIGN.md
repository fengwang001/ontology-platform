# DESIGN：并行任务图调度与失败传播

仅标准库，状态全在进程内存。包：`graph`（图/环/分层）、`fail`（状态与传播）、
`exec`（执行封装）、`report`（报告）、`sched`（调度器，组合其余各包）。

## 1. 四类最终状态的判定推导

设失败时刻为 t。任务按生命周期分四种处境，状态由「是否开始过」与「是否有失败祖先」两轴决定：

| 处境 | 开始过？ | 失败祖先？ | 最终状态 |
|---|---|---|---|
| t 前函数已返回 nil | 是 | 无（下游未失败时不可能完成） | 成功 Success |
| 函数返回 error / panic | 是 | —— | 失败 Failed，带原始错误 |
| t 时 ctx 已发出、函数仍在跑 | 是 | 无（旁支） | 被取消 Canceled |
| 从未被派发，且某祖先失败 | 否 | 有 | 被跳过 Skipped |

关键区分：**开始执行**是唯一判据——状态在「派发瞬间」从 Pending 翻成 Running。
因此取消永远不会命中尚未派发的任务；尚未派发的任务只能是 Skipped（有失败祖先）
或因并发上限暂挂（Pending 暂挂在失败传播时也从未开始，按旁支取消语义仍应与
Skipped 区分，见下）。

**Skipped 的原因溯源**：传播沿依赖边向下。一个未开始任务可能同时收到多个祖先
失败，其 Reason 必须指向**最初失败的任务**（最早 Failed；同时失败按 ID 升序
破平）。实现：失败只记录事件，跳过判定在调度器处理事件时，对每个新就绪任务做
可达性检查——若任一已失败任务是其祖先则跳过，origin 取失败时间最早者。
`A→B→C`、A 失败时 B 尚未开始：B 被跳过（origin A），C 的祖先集合含 A 与 B，
但 B 从未失败，故 C 的 origin 也是 A，而非直接上游 B。

**Canceled 与「未开始暂挂」**：快速失败时，在并发上限外等待、无失败祖先的任务
从未开始，规范四类状态要求它既不是 Skipped 也不能跑——归入 Canceled
（语义：被失败事件中止），报告 Started=false 仍可与 Skipped 区分；典型被取消的
慢任务 Started=true。Skipped 严格要求存在失败祖先。

**取消后写回**：任务函数仅收到 ctx，框架在派发槽位上登记 token；取消后迟到的
结果按任务当前状态校验，只有 Running 接受，已 Canceled 的写回被丢弃。

## 2. 两种模式语义

- FastFail（默认）：首个失败事件出现后，不再派发任何无失败祖先的新任务；
  在跑任务 ctx 取消；失败祖先的下游全部 Skipped（origin 指向最早失败）；
  无失败祖先但未派发/在跑者 Canceled。已完成的成功、失败本身保持不变。
- BestEffort：不取消任何在跑任务，继续派发**不依赖任何失败任务**的分支直到全部
  结束；只有失败祖先的下游 Skipped。因此可能继续产生新的 Failed，各自向下游传播，
  每个 Skipped 的 origin 仍是其最早的失败祖先。不存在 Canceled。

## 3. 调度算法与复杂度

单调度循环 + 容量为 limit 的信号量；就绪用最小堆（ID 升序），保证同就绪时按 ID
取，报告按 ID 排序输出 → 确定性，与完成顺序、加边顺序无关。

每个任务完成时只做局部更新：遍历其出边，对后继做一次「入度（未完成祖先数）
归零」判定，归零即入就绪堆或做失败祖先检查。就绪判定总次数 = 每条边恰好检查
一次 + 每节点入堆一次 ≤ 2(V+E)，上界 4(V+E)。计数器 `decideCount` 记录。

失败祖先检查需要可达性：事件处理时仅对「刚归零的后继」沿图向上查（memo 缓存
失败可达集），不做全图扫描；1000 层链为迭代实现，无递归。

并发：信号量保证同时在跑 ≤ limit；计数器 `maxRunning` 在派发/完成时更新，
测试直接读包内字段。所有任务 goroutine 经 WaitGroup 回收，调度循环自身也是
唯一长生命周期 goroutine，结束即退出。

## 4. 错误模型

`fail.Status` 枚举（Pending/Running/Success/Failed/Skipped/Canceled）区分状态；
错误侧 sentinel：`ErrCycle`（环，错误文本含真实环路径）、`ErrMissingDep`、
`ErrPanic`（panic 包装，`errors.Is`/`errors.As` 可取原始信息）、`ErrCanceled`、
`ErrSkipped`。报告每条目含 ID、Status、Started、Reason（失败/跳过的源头 ID 与
错误文本）。多任务同时失败：每失败独立落条目，全部保留。

## 5. 边界

空图直接出空报告；重复加边幂等（集合去重，入度只计一次）；依赖不存在的节点在
`Build` 时返回 ErrMissingDep；自环/任意环在执行前由 DFS 着色检出并还原路径；
limit=1 退化为串行，确定性输出不变。
