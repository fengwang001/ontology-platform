# 设计推导

## 第 2 条：代理对错误偏移

规则：解码器以「转义单元」为单位推进，`\uXXXX` 占 6 字节，错误偏移一律指向**不合格单元的起点**（其反斜杠处）。
- 孤立高代理（如 `"\uD83D"`、`"\uD83Dx"`、`"\uD83DA"`）：高代理本单元语法合法，但其后不是紧跟的、形式合法的低代理；在 EOF/非 `\u` 单元前无法形成对，错误落在高代理自身起点（它是唯一可见的不合格位置，其后的字符并非可定位的转义）。
- 孤立低代理（`"\uDE00"`）：低代理不得作为对的首单元，错误落在该低代理起点。
- 顺序颠倒（`"\uDE00\uD83D"`）：第一个单元是低代理，立即拒绝，错误落在它的起点。
- 两个高代理相邻：第一个高代理后看到的是高代理而非低代理，错误落在**第二个单元**起点（那个本应是低代理却不是的单元）。

理由：偏移应指向「使文本无法继续被接受的最小单元的起点」。UTF-16 代理对是 (high, low) 二元序列；读到 high 时文本仍是合法前缀，须前瞻下一个单元再判定，故缺陷归于实际出现的那个后继单元（或 high 自身当输入在此结束）。`"\uD83DA"` 不能解释成「替换字符 + A」：严格解码（RFC 8259）没有替换字符恢复规则，`A` 是字面内容而非低代理，high 在闭合引号前始终未配对，整体非法；静默替换会造成信息丢失与一对多歧义，也破坏严格往返。

## 第 4 条：最小转义、单射与唯一性

JSON 字符串中必须转义的只有：引号 `"`（定界符）、反斜杠 `\`（转义引导符）、控制字符 U+0000–U+001F（RFC 8259 规定未转义控制字符非法）。
- U+001F 属于该区间，必须转义；U+007F（DEL）不在 U+0000–U+001F 内，规范允许原样出现，故不转义。
- `/` 不是定界符，`\/` 仅是可选转义，取原样；非 ASCII（含 U+2028/U+2029，JSON 字符串允许原义换行分隔符）原样输出。
- 短形式优先：`\b \f \n \r \t`；其余控制字符唯一写成小写 `\u00XX`。

单射与唯一：编码是逐码点、逐位置的无状态确定性映射，每个码点恰有一种输出形式（原样或唯一转义串），转义形式集合（`\u00XX` 与六个短形式）互不相交且均不同于任何原样码点；因此不同输入必产生不同输出（单射），同一输入只有唯一字节序列。该映射可逆且无重叠，最小转义字面量满足 `Encode(Decode(y)) == y`。

## 六条语义的测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类错误可区分且带偏移 | （收尾时填写） |
| 2 代理对组合与校验 | （收尾时填写） |
| 3 非法 UTF-8（解码/编码两方向） | （收尾时填写） |
| 4 最小转义编码 | （收尾时填写） |
| 5 往返一致 | （收尾时填写） |
| 6 跨切分点一致 | （收尾时填写） |
