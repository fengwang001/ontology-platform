# 设计推导

## 1. 末尾换行策略（样例表）

规则：Keep=原样；One=折叠为至多一个；Trim=删掉全部末尾空行（空白行规范化后即空行）。三种策略都**只删不插**。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

推导：`ToOut(ToOrig(o))==o` 要求每个输出字节对应一个真实存在的原文字节（映射是满射），故规范化器**永不插入字节**。因此 One 对 `""` 只能给 `""`；若给 `"\n"`，`ToOrig(0)` 找不到源字节，且再规范化会删它，违反幂等。只删不插时三种策略的输出都是各自的不动点（无 `\r`、无尾随空白、尾随 `\n` 数符合策略），故 N(N(x))==N(x)。

## 2. 被删字节的 `ToOut`

映射表把文本切成若干片段，每片段 `[so,eo)`→`[sn,en)`，等长；删除片段 `en==sn`。`ToOrig(o)=so+(o-sn)`。`ToOut(i)` 用二分找最后一个 `so<=i` 的片段：存活片段内线性映射；落在删除片段（或删除片段与下一片段的边界）时取**紧随其后的存活片段的 `sn`**。

推导：单调性只允许取常数；`ToOrig(sn)` 必须等于下一片段起点，反解 `ToOut(ToOrig(sn))=sn`，故该常数只能是 `sn`，没有第二个选择。行尾空白被删时光标映射到该行 `\n` 的**位置 0**（换行符之前）；`\r\n` 的 `\r` 同理映射到 `\n` 之前——直觉上「被删的字符合并进换行符的左缘」。

## 3. 空白缓冲超限

强制把未定空白当作「非行尾」输出会违反第 5 条（输出出现尾随空白，二次规范化结果不同）与第 4 条（切分方式不同则判定不同）。故选择**拒绝**：返回带原文偏移的可判定哨兵错误 `ErrSpaceLimit`，已输出内容与映射保留，实例进入终态，再写返回 `ErrClosed`。拒绝不违反 4/5/6——错误前的输出是输入某前缀的规范化结果。NUL 严格模式 `ErrNUL`、输出超限 `ErrOutputLimit` 同理。

## 4. par 拼接

难点：段尾可能悬着 `\r` 或未定空白，其判定依赖下一段开头。解法是**先挪切点再处理**：从原始切点向后找至多 2 个「安全切点」——安全点=`\n` 后，或既非空白也非 `\r` 的字节后（该字节不是 `\r`）。在安全点处，段内状态必为「行中、无待定空白」，唯一遗留是上一段末尾的孤立 `\r`；安全点规则保证段首字节不会是该 `\r` 的 `\n`，故该 `\r` 在该段 Close 时被正确当作单独行尾。修正范围有上界：悬而未决的内容只能是「一个 `\r`」或「一串空白（可能再加一个 `\r`）」，遇到下一个普通字节即终止；`LimitSpace` 给空白串长度设了上限。每段独立规范化后，把片段坐标加段内全局起点偏移平移，段尾片段与下段首片段可合并（删除片段零长不影响）。拼接结果与单流等价，从而满足第 9 条。
