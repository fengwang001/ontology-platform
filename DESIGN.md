# 设计推导

## 流格式记录布局（全部整数为无符号 LEB128 varint）

| 记录 | 字节布局 | 说明 |
|---|---|---|
| 流头 | `4F 4E 54 31` ("ONT1") | 魔数+版本，固定 4 字节 |
| 字面量 | tag(0xxxxxxx) + payload | tag 低 7 位=长度 1..128（128 记为 0），其后原样字节 |
| 回指 | `0x80` + dist(varint) + len(varint) | dist≥1，len≥1，dist≤窗口容量 |
| 刷新 | `0x81` | 块边界，无数据 |
| 流尾 | `0x82` + totalLen(varint) + checksum(varint) | checksum=全体原文 FNV-1a 64 位 |

## 推导 1：压缩结果为何与 Write 切法无关

贪心 LZ77 在位置 p 取最长匹配后，决定「从 p 发出回指还是字面量」必须先知道匹配候选；
而要判定匹配（含延迟一槽的 lazy 判定）是否真的在此结束，必须等到其后第 2 个字节到来。
若提前在 Write 边界截断，1 字节喂入与整段喂入就会发出不同记录。因此压缩器内部保存
全部未决字节 pending，只在两种情况下推进决策：
(a) pending 在当前候选判定所需字节之外还有富余，即未来字节已确定；
(b) Flush/Close：强制按已知全部字节做最终决策并发出刷新标记。
决策函数是「累计字节流 + flush 偏移」的纯函数，故任何 Write 切法、只要 flush 偏移相同，
输出逐字节一致。Flush 强制结束当前匹配：此后没有未来字节，lazy 比较不能再等待，必须落盘。
空 Flush 时无未决输入，不发任何记录（连刷新标记也不发），故连续 Flush 零字节。

## 推导 2：重叠回指的解压复制

距离 d<长度 L 时，源区与目标区重叠。逐字节语义是 r[i]=r[i-d]，新字节可再作源；
一次性 copy 重叠区间会取到尚未回填的旧数据。实现：先追加历史末尾 d 字节填出一个完整
周期，再对剩余 L-d 字节执行 append(dst, dst[len-2*d : len-d]...)：源区间此时已是
完整的 d 字节周期，源与目标不再重叠，一次 copy 完成，等价于逐字节前向复制。

## 推导 3：两类「距离越界」为何分开

「dist>已输出字节」是流前缀错误：即使窗口无限大也无此历史，例如首条记录就是回指。
「dist>窗口容量」是协议错误：历史可能存在但超出本流声明的可引用范围。压缩器只在
dist≤min(匹配起点, 窗口容量) 时发回指，故其绝不可能产生后者；预置字典也先真实写入
窗口，dist 永远 ≤ 窗口内现有字节。并行块中解压流是连续的：预置字典只影响压缩器匹配，
解压器看到的仍是从头累计的输出，故「已输出字节数」的含义不变，两类检查都保留。

## 推导 4：并行块压缩的确定性与衔接

块 k 的匹配器只读取两块数据：本块原文 data[k] 与其预置字典（data[k-1] 的末尾 W 字节）。
它不读其他块的压缩记录，也不读任何汇总状态；纯输入→纯输出，与 goroutine 完成顺序无关，
因此 workers=1/2/4/8 结果逐字节相同，race 干净。块 k 先独立压缩为一段正常记录流（
字面量/回指），主序 goroutine 按块序拼接：块 0 之前写一次流头；每块（除最后一块）末尾
追加一个刷新标记；全部块结束后写一次流尾（总长与校验和按完整原文计算）。刷新标记即块
间衔接，解压器无需知道块边界即可连续还原。

## 复杂度与防护

哈希链每位置沿链最多探测 chainLimit 个候选，`candidates` 非导出计数器累计，上界
chainLimit×输入字节，与窗口大小无关。解压在回指长度展开前先比较「已输出+len>上限」，
超限立即返回错误且不写任何字节；错误后解压器进入终态。
