# 设计推导

规范化 N(x)：行尾 `\r\n`/`\r`/`\n` → `\n`；删除每个 `\n` 之前（以及文件末尾）连续的空格/制表符；NUL 与非法 UTF-8 原样通过。

## 1. 空输入与末尾换行策略

先做"主体规范化"（keep 策略：不改动末尾换行），再应用：
- `KeepOriginal`：主体结果原样返回。
- `EnsureOne`：把末尾全部 `\n` 折叠为恰好一个；若主体为空（输入为 `""` 或全空白被删光）则补一个 `\n`。
- `EnsureOne`：把末尾全部 `\n` 折叠为恰好一个；主体非空但无末尾换行则补一个；**空原文输入输出 `""`**（注意：这是第 6 条恒等式的硬约束——凭空插入的 `\n` 没有任何原偏移可对应，`ToOut(ToOrig(1))==1` 无法成立；其他所有情况补 `\n` 时，锚点原文末尾偏移与插入点输出偏移一致，恒等式成立）。
- `Trim`：主体去掉末尾全部 `\n` 后若为空（`""`/全空白/只含空行），输出 `""`；否则删除全部末尾空行并保证恰好一个 `\n`（无则补）。
- `Trim`：若主体去掉末尾全部 `\n` 后为空（空或全空白行），输出 `""`；否则删除全部末尾空行再补恰好一个 `\n`。

| 输入 | KeepOriginal | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

幂等：三种策略的输出只含 `\n` 且无行尾空白，再次规范化主体不变；EnsureOne 输出恰以一个 `\n` 结尾，Trim 输出为 `""` 或"非空白结尾 + 一个 `\n`"，再次施加各自策略不动。

## 2. 被删字节的 ToOut

把变换看作一串连续片段：keep（1 原字节→1 出字节）、del（若干原字节→0）、ins（0→若干出字节）。
要求 `ToOrig` 单调不减、`ToOut(ToOrig(o))==o`。令 `o0` 为删除片段紧前输出位置（即其后换行符 `\n` 的偏移），该片段占用原区间 `[i0,i1)`。
- del 后的 ins 要求该区间所有原偏移映射到 `o0`（`ToOrig(o0)=i1`）；
- del 后无 ins 则只能映射到 `o0`（删尾空白且无换行：文件末尾）。

故 `ToOut(i)=o0`（i 落在被删空白或 `\r\n` 的 `\r` 上，含 `\r`）。单调不减：keep 段斜率 1，del 段平台。`ToOrig(o)` 对平台点取最右原偏移。光标放在被删空白里时落在换行符**之前**（行尾），而非下一行；编辑器的删除动作也因此保持在本行内。

## 3. 空白缓冲超限

强制把待定空白判为"非行尾"输出：若下一字节恰是换行，则该空白应删却已输出，违反语义 2、5（再规范化会变短）；且结果依赖"先输出后看到换行"的时序，违反语义 4。故选择**拒绝**：返回哨兵 `ErrSpaceLimit`（具名错误携带原文偏移），流进入终态，已输出内容与映射保留，状态 = 已提交输出 + 终态错误，可解释且与切分方式无关（超限判定只取决于"当前待判定空白串长度"，任何切分喂入都在同一原偏移触发）。输出上限同理立即拒绝 `ErrOutputLimit`。

## 4. par 拼接

段头不知道前段尾部是否有待定 `\r`/空白。做法：每段都按 `KeepOriginal`、放宽缓冲上限独立规范化（段内坐标）。拼接时只看**两切点邻域**：取切点左方至多 `SpaceLimit+2` 字节（尾部待定空白串加可能的 `\r`）与右方至多 2 字节（`\n` 或 `\r\n`），对该窗口原文用一个串行小 normalizer 重算，替换两段各自窗口输出。修正范围有上界：待定状态只能是"一个 `\r`"和"一串空白"，长度 ≤ `SpaceLimit+1`；右向只需看 2 字节即可判定行尾（`\r\n` 或其他）。每段片段坐标按"原文全局偏移 + 已提交输出偏移"平移，窗口内旧片段删除、新片段按全局坐标插入，再合并相邻同类片段。NUL/严格错误按最小原文偏移上报。最后对完整输出施加 EnsureOne/Trim（纯输出尾编辑 + 映射尾片段编辑，无歧义）。
