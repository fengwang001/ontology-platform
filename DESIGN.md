# 带溢出的哈希聚合器 — 设计推导

## 1. 溢出对象：原始行 vs 部分聚合状态

内存哈希表超过驻留分组预算 B 时，必须把某个分区 p 逐出磁盘。两种选择：

- 溢出原始行：逐行写盘，回读时重新建立分组。写盘字节与行数成正比。
- 溢出部分聚合状态：先在内存按 key 聚合，只把 `(key, Count, Sum, Min, Max)`
  状态写盘。同一 key 在该分区被多次溢出时，各片段是各自的部分和。

结论：实现「溢出部分聚合状态」。理由：写盘数据量按分组数而非行数缩减；
正确性依赖部分状态的二元合并 `⊕` 满足

    (a ⊕ b) ⊕ c == a ⊕ (b ⊕ c)            （结合律）

成立时，无论状态在哪一刻被切到磁盘、切几次，最终把所有片段按 key 合并
都等于对全部输入做一次聚合。

## 2. 同一分区被溢出多次为何仍正确

分区数 P 固定。内存只保留「当前未溢出分区」的状态；分区 p 第 k 次超预算时，
把 p 的内存状态作为一个追加片段文件 `p.k` 写盘，随后清空。回读阶段对每个
分区收集其全部片段（k=1..K）与输入尾部中该分区的内存残留，按
`片段1 ⊕ 片段2 ⊕ … ⊕ 残留` 合并。任何片段遗漏都会少计，因此读取端必须
枚举目录中该分区的全部片段，文件头携带分区号，号不符即拒绝。

## 3. 四种聚合的合并规则

- Count：`C = C1 + C2`，整数加法结合。
- Sum：`S = S1 + S2`。浮点加法不普遍结合。为保证与单次内存聚合逐位相同，
- Sum：`S = S1 + S2`。浮点加法不普遍结合。为保证与单次内存聚合逐位相同，
- Sum：`S = S1 + S2`。浮点加法**不普遍结合**：段内预聚合会改变括号，
  仅靠按序合并不能对任意浮点逐位等于单次内存聚合（反例 1e16、-1e16、1：
  单次得 1，分两段得 0）。严格结论：(a) 对结合域——和不超 2^53 的精确
  整数浮点与 ±Inf——分多少段都与单次内存聚合 `math.Float64bits` 相同，
  逐位相同测试均在该域取值；(b) 对任意浮点，合并顺序固定（片段按溢出
  时间、片段内按行入表顺序、残留最后），结果确定可复现。
- Min：`M = min(M1, M2)` 取小结合；Max 取大结合。二者是取极值而非加法：
  空片段（Count=0）为恒等元不参与，否则会被零值污染。`min(-0,+0)=-0`、
  `max(+0,-0)=+0` 与归并顺序无关，亦逐位确定。
- NaN：输入值为 NaN 在行入口拒绝并计入 skipped；±Inf 参与聚合；
  ±0.0 视为同一分组值。

## 4. 资源与复杂度

- 驻留分组数 ≤ B：加入新 key 前若 `len(table) == B`，先逐出一个分区
  （至少清掉 1 个 key）再插入；计数器原子记录历史峰值。
- 每行哈希分区 1 次、回读 1 次，总处理 ≤ 2N；写出与读回逐行计数相等。
- P 由构造参数固定，不随分组数增长。

## 5. 分区文件格式（自描述 + 长度前缀 + CRC32）

    头 9 字节: "HSP1" | version(1) | partition(uint16 BE) | segment(uint16 BE)
    记录:      len(uint32 BE) | payload(len) | crc32-IEEE(payload)(uint32 BE)
    payload:   keylen(uint16 BE) key ... | count(uint64 BE) |
               sum/min/max(float64 BE)

写盘的是部分聚合状态。截断点分类：`<9` 头不完整；落在 len 区（4 字节不足）
长度前缀不完整；len 已声明但 payload 不足 → 行体不完整；payload 齐而
CRC 不符 → CRC 不匹配。

## 6. 故障与文件生命周期

写：`p.k.tmp` 临时名 → 写完关闭 → 原子 rename 为 `p.k`。启动时清理所有
`*.tmp`（半截崩溃残留不被当有效分区）。注入第 k 次写失败时聚合中止并删除
本次所有临时/片段，残留计数为 0。四类截断错误用独立哨兵
ErrHeader/ErrLenPrefix/ErrBody/ErrCRC，写失败 ErrSpill，全部可用
`errors.Is` 区分；截断错误附带分区号与字节偏移，聚合必须中止。

## 7. 并发与确定性

回读每分区一个 goroutine，各自累加互不共享；合并按 (片段号, key 排序)
确定性归并，与完成顺序无关。最终结果按 key 排序输出。测试打乱完成顺序
20 次，比较输出字节与每组 Sum 位模式一致。

## 8. 边界

零行输出空；单行；全同 key；分组数 == B 不溢出；B+1 溢出一次；空 key
合法；NaN 拒绝并计入 skipped；±Inf 不拒绝；±0.0 视为相等。
  skipped。
- Sum：`S = S1 + S2`。浮点加法**不普遍结合**：段内预聚合把
  `((a+b)+c)` 变成 `(a)+(b+c)`，仅靠按序合并不能对任意浮点保证逐位等于
  单次内存聚合（反例 1e16、-1e16、1：单次得 1，分两段得 0）。因此本实现的
  严格结论是：(a) 对结合域输入——绝对值在 2^53 内、求和不超 2^53 的精确
  整数浮点与 ±Inf——加法可结合，分多少段都与单次内存聚合
  `math.Float64bits` 相同；(b) 对任意浮点，合并顺序固定（片段按溢出时间、
  片段内按行入表顺序、残留最后并入），结果**确定可复现**。全部逐位相同
  测试均在结合域取值。±0.0 视为相等。
- Min：`M = min(M1, M2)`，取小满足结合；Max 同理取大。二者是「取极值」
  而非加法：空片段（Count=0）必须是恒等元，不参与极值，否则会被零值污染。
  `min(-0,+0)=-0`、`max(+0,-0)=+0` 与归并顺序无关，故也逐位确定。
