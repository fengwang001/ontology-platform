# DESIGN

## 流格式（记录均为 tag 字节 + 变长整数，整数用 protobuf 式无符号 varint）

| 记录 | 布局 |
|---|---|
| 流头 | `tag=0x00` + 魔数 `"ONLZ"` + 版本 1 + varint(windowCap) + varint(chainLimit) |
| 字面量段 | `tag=0x01` + varint(n) + n 个原始字节 |
| 回指 | `tag=0x02` + varint(distance) + varint(length) |
| 刷新标记 | `tag=0x04`（无载荷） |
| 流尾 | `tag=0x03` + varint(totalLen) + varint(crc32IEEE) |

varint 最长 10 字节；第 11 字节或使 uint64 溢出即报超长。校验和用 IEEE CRC32 增量计算。

## 推导

### 1. 输出与 Write 切法无关

压缩器维护绝对输入偏移 `pos`、`w`（window，含全部历史）。在位置 i：

1. 若 `len(w)-i < 3`（连最小匹配都不可能形成），停下来等下一次 Write。
2. 否则在哈希链中找最优匹配（距离最近、最长；最短长度 3）。
3. 匹配只在「能与紧随其后的字节比较」时才算结束。若匹配一直延伸到 `len(w)-1`
   （最后一个被匹配字节是当前缓存的最后一个字节），它**可能**因下一次 Write 的新
   字节而变长，故不决定，直接停下；位置 i 保持不变，不插入哈希链、不发任何记录。
4. 匹配已确定结束（下一字节不同）或未找到匹配时，位置 i 才推进；推进时把 i 插入
   哈希链（未找到匹配插 1 步；找到则逐字节插入 match 覆盖的全部位置）。

决策仅依赖「w 的内容 + 位置 i」，而 w 是累计原文，与历史 Write 切法无关，所以
记录流逐字节相同。未决定的尾部不输出字面量，也就不会在 Write 边界截断匹配。
Flush 语义要求「已输出足以还原全部已写入输入」，因此 Flush 把当前 w 当作不会再
有后续：步骤 3 的悬挂匹配按当前长度定案，剩余字节全部作为字面量输出，再发刷新
标记。连续两次 Flush 之间没有新输入时 pos 已到末尾，不产生任何记录。

### 2. 重叠回指的解压

距离 d < 长度 l 时，源区与目标区重叠。块复制（memmove 语义对、但若是从独立缓冲
一次性 append 则错）必须按偏移复制。实现用 `append(dst, dst[len(dst)-d:]...)`
然后循环 append 至长度够：切片别名指向同一底层数组，append 逐个写回，写入的新字节
立刻可被后续读到；append 对重叠别名按顺序求值。这既正确又不手写逐字节循环。

### 3. 「距离超过已输出」与「距离超过窗口容量」

二者是不同错误：前者是流自相矛盾（引用尚未产生的输出），即使窗口无限大也不合法；
后者表示引用落在编/解码器约定的环形窗口之外，历史已不可寻址，即使该字节曾输出过。
解压校验顺序：先 d==0，再 d>已输出（ErrDistTooLarge，Offset 指 distance varint
起点），再 d>windowCap（ErrDistWindow，同偏移）。压缩器对哈希链候选一律要求
`pos-candidate < windowCap`，且 candidate 只来自已插入的位置（< pos），所以绝不可能
产生后者。并行块的预置字典：解压仍是严格串行的——块 2 解压时块 1 的原文已全部输出，
「已输出字节数」含义不变，只是「可寻址历史」由 windowCap 决定，天然包含上一块末尾。

### 4. 并行块压缩的确定性

按 blockSize 切块。块 k 的压缩只读取两段原文：本块 + 上一块末尾至多 windowCap 字节，
作为预置 window 与预置哈希链（预置位置只入链、不产生记录）。它不读其他块、不读其他
块的输出、也不等待其他 goroutine，故 workers=1/2/4/8 只改变完成顺序；块结果按 k
顺序拼回，记录流逐字节相同。块之间不需要刷新标记：块是压缩器内部的并行单元，不是
Flush 边界；回指记录可自由跨块界（距离指向预置字典）。流只有一个流头与一个流尾，
流式解压器无需知道切块方式。

## 复杂度

哈希链：head 按 3 字节哈希，prev 为按 windowCap 取模的环形链；每位置至多考察
chainLimit 个候选（计数器 candExamined 记录总数），故总考察 ≤ chainLimit×输入字节数。
全同字节串每位置都在 chainLimit 处停（再老的候选超出窗口被剔除），每字节考察数为
常数；随机串链迅速断裂，更省。4MB 全同字节用一条长回指表示，流远小于 64KB。
