# DESIGN

## 1. 非法单元切分规则（WHATWG 风格的单字节状态机）

状态机只有 `accept` 与 `need(n, lo, hi)`（还需 n 个续字节、允许码点区间）。accept 看到首字节 b：
`C2..DF→need1`；`E0→need(2,0x800,0x7FF)`（由第二字节 A0..BF 决定）；`E1..EC→need2`；
`ED→need2`（第二字节 80..9F，排除代理区）；`EE..EF→need2`；`F0→need3`（第二字节 90..BF）；
`F1..F3→need3`；`F4→need3`（第二字节 80..8F）；其余为非法首字节。

**统一规则**：need 状态收到字节 c：
- c 是该位置合法续字节：推进；
- c **非续字节**（`c&0xC0 != 0x80`）：等待序列立刻死亡，已缓存前缀（1..3 字节）算一个非法
  单元、输出一个 FFFD，c 不作为本单元成员，回到 accept 重新判定 c；
- c 是续字节但**越过本首字节在该位置的合法区间**（E0 第二字节<A0、ED>9F、F0<90、F4>8F）：
  已缓存前缀**连同 c** 算一个非法单元（单元长度 = 当前已收字节数，2 或 3），一个 FFFD，
  其后字节回 accept；第三/第四续字节位置的越界同理（吞到 c，含 c）。

样例：`F0 90 80 41` 前三个字节均满足区间，第四字节要求续字节却是 `41`（非续）→ 前缀
`F0 90 80` 一个单元，`41` 重解析 → FFFD A。`E0 80 80`：E0 第二字节要求 A0..BF，80 越界，
单元 `E0 80`→FFFD；剩下 80、80 是两个非法首字节 → 共三个。`ED A0 80`：A0 越界（ED 只许
80..9F），单元 `ED A0`；A0 之后的 80 回 accept 又是非法首字节 → 三个。`C0 AF`：C0 与 AF
均为非法首字节 → 两个。`F4 90 80 80`：90 越界 → 单元 `F4 90`，随后 80、80 各非法 → 四个。
`E2 82`+EOF：82 是 E2 的合法第二字节，残留的是"合法前缀被截断"→ 整个残留算一个单元 →
一个 FFFD。`80 80`：两个孤立续字节，各一个。

## 2. 第二字节合法区间与吞字节表

| 首字节 | 第二字节合法区间 | 非法时吞掉几个字节 |
|---|---|---|
| C2..DF | 80..BF | 第二字节非续：只吞首字节 1 个；非法首字节行同理 |
| E0 | A0..BF | 越界/非续均吞到当前字节为止（含），其后字节重解析 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F | 同上（A0..BF 越界） |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF | 同上 |
| F1..F3 | 80..BF | 同上 |
| F4 | 80..8F | 同上（90..BF 越界） |
| 80..BF | 无（非法首字节） | 1 |
| C0 C1 F5..FF | 无（非法首字节） | 1 |

一句话：期望中的续字节一旦非法（非续或越界），等待序列在该字节处立即作废，已缓存字节加该字节
即一个非法单元；该单元之后不消费任何字节。EOF 时的残留前缀整体算一个截断单元。

## 3. 缓存上限与何时立即放弃

UTF-8 最长 4 字节，挂起缓存至多 3 字节（F0 90 80 后等第 4 个）；UTF-16 一个字符至多 2 个
code unit，字节级挂起至多 1 字节、码元级至多 1 个高代理。任何新字节到达都立即判定：合法推进、
非法立即作废单元，绝不回扫已接受字节，故每个输入字节恰好被检查一次（切碎喂也不回扫）。

## 4. 跨切分、par 对齐、上限断点

挂起字节在单元完成前不产生任何输出，故切在字符中间与不切逐字节相同。
`par`：每段用全新状态机独立转码（不 Close）；顺序合并时持有上一段挂起前缀 p（UTF-8 ≤3、
UTF-16 ≤2 字节）。在段首取至多 4 字节重放区，用"承接 p 的联合机"与"fresh 机"并行推进，
至多 4 字节后二者必同在 accept（任何首字节起 4 字节内单元必然结束）；[0,s) 用联合机的
输出/统计替换 fresh 结果，s 之后两机同态直接沿用该段输出。切点后为 `80 80`、上段尾为
`C3` 时：联合机得合法标量（C3 80），fresh 机得两个非法单元，重放恰好修正，全局单元数与
单流一致。每边界只回看有限字节，检查次数 ≤ N+8K 量级。
上限断点：Write 在输出每个标量前检查容量，放不下即停；返回的 n 只含已完整输出标量所消费的
输入字节，挂起缓存里的字节不计 n（留给调用方从 n 处整体重传）。新实例从 n 续传，因状态只
依赖挂起前缀，拼接结果与不设上限逐字节相同。
