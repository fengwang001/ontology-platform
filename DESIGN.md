规则（分层，从先到后，首个非零层决定结果）
===========================================

把串切成「数字段 / 非数字段」交替的 token，按顺序配对比较：

1. 段类型：结束 < 数字段 < 非数字段。同为数字段时先比数值：去掉前导零后，长串（位数多）更大；等长则从首个非零位起按 ASCII 字节序比较，全程不转整数。
2. 数值相等时，不立即比前导零，继续比较「后续内容」：若后继 token 序列不同，由后续内容决定；只有整串其余部分全部相同，才用前导零数裁决，零越少越大。
3. 同为非数字段时逐字节按 ASCII 比较；较短的段（提前结束/进入另一类型）更小；一侧先耗尽即结束最小。

为什么不能「立刻」比前导零个数
==============================

a01b < a1c：首段 01 与 1 数值相等。若立刻比零数，则 1（零更少）反而更大，与样例矛盾。
样例要求先看后缀：b < c，故 a01b 小。只有后缀也相同（a01 vs a1）才轮到零数，此时零越少越大：a1 < a01 < a001。

演算
====

- a01b vs a1c：a=a；01≡1（值均 1），挂起零差继续；b<c ⇒ a01b < a1c。
- a1b01 vs a01b1：a=a；1≡01 挂起；b=b；01≡1 挂起；同时结束，逐层回放零差：末段 1 的零数 0 < 01 的零数 1，零越少越大 ⇒ a1b01 < a01b1。
- x0 < x00 < x000 < x1：x=x；值均 0（1 值为 1 故最大）；值 0 三者后缀皆空，零数 1<2<3，零越少越大。

传递性（第 4 条）
=================

比较等价于给串赋一个按 token 顺序排列的键：token 为 (类型标签, 比较键, 零数)；数字段键为 (去零后长度, 去零后字节)，非数字段键为字节序列，末尾补「结束」标签。
标签序、长度字典序、字节 ASCII 序、零数整数序各自是严格全序，按层 lex 组合仍是严格全序。
「挂起零差、后缀优先」正是把键 (…, 当前键, 零数, 后继…) 中的零数排在后继之后求值，与该键的 lex 顺序一致。
故反对称、传递，且键相同当且仅当原文逐字节相同：Compare==0 ⇔ a==b。

非 ASCII（第 6 条）
===================

仅 ASCII 0–9 是数字，其它字节（UTF-8 续字节、全角 １２）都进非数字段按字节比较。
键完全建立在字节与标签之上，不做解码、不设特例，故仍是良定义严格全序，不 panic。

复杂度（第三节）
================

Compare 单趟扫描；每个串的每个字节至多在「段内比较」和一次段边界处被检查，故检查字节数 ≤ 2(len(a)+len(b))。
包内非导出计数器 checked 记录最近一次 Compare 检查的字节数。

测试对照表（语义 → 测试函数）
==============================

<!-- TABLE -->
