# DESIGN

## 1. 规范化模型
按字节状态机切行：`\r\n` 与单独 `\r` 都是一个行尾，输出 `\n`；`\r\r\n` 中第一个 `\r` 先行闭合，故为两行。每行结尾连续的 `' '`/`'\t'` 删除，其余字节（含 NUL、非法 UTF-8）原样。先规范化，再套用末尾策略。

## 2. 空输入 / 只含换行（推导）
策略作用于规范化结果 `y`：EnsureOne = `y=="" → ""`；否则无尾换行则补一个合成 `\n`，多个则删到一个。TrimBlank = 若所有行均空白则 `""`，否则删掉全部末尾空行、恰好留一个 `\n`。

| 输入 | Preserve | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

EnsureOne 对空输入取 `""`：`N("")=""` 平凡幂等。另一选择 `N("")="\n"` 配合 `N("\n")="\n"` 也幂等（空被视作"应有终止符的文档"），但会凭空制造原文不存在的字节；取 `""` 使三策略在空输入一致、映射无需合成点，故选它。

## 3. 被删字节的 ToOut（推导）
设被删原文区间 `[a,b)`（行尾空白，或 `\r\n` 的 `\r`），紧随其后保留字节（`\n`）在输出位置 `p`。不变量要求 `ToOrig(p)=b` 且 `ToOut(b)=p`；单调性要求 `a≤i<b ⇒ ToOut(i)≤p`，且映射像必须落在输出定义域。唯一一致选择是平台：`ToOut(i)=p`（`a≤i<b`），`ToOrig` 在 `p` 处跳到 `b`。因此编辑器把光标放在被删空白或 `\r` 上时，定位到 `p`——即换行符**之前**（行尾），不会跳到下一行行首。

## 4. 缓冲超限（推导）
待定空白串超过上限时有两条路。强制判为"非行尾"并输出：若下一字节真是行尾，已输出的空白无法收回，违反语义 2；且判定结果依赖喂入块大小，违反语义 4/5。拒绝：返回哨兵错误、报告原文偏移、保留已输出、实例进入终态，被接受的输入全部不变量照常成立。故选择**拒绝**（`ErrWSTooLong`）。输出超上限同理拒绝（`ErrOutputLimit`）。

## 5. par 拼接（推导）
切点处的未知信息只有两种：结尾待定的 `\r`（需向后看 1 字节）与结尾未判定空白串（后续可能是 `\r\n`，需向后看 2 字节）。每个 worker 处理把本段向**左**扩 `1+左侧空白串长度`、向**右**扩 2 字节得到的扩展区间（修正范围有上界 `wsLimit+3`），用 Preserve 策略、关闭上限独立规范化。
各保留字节按其**原文偏移归属**固定切分段；worker 用自己映射表的 `ToOut(段起点)`/`ToOut(段终点)`（删除串内是平台值，两侧取值相同）切出本段输出区间，区间首尾恰好相接、不重不漏。span 事件按输出区间截取后平移 `(Δout, Δorig)` 拼接。最后对全局结果统一套用末尾策略（EnsureOne 的合成 `\n` 只此一处产生，其 ToOrig 指向原文终点）。

## 6. 映射实现
事件列表：`del{a,b,out}` 表示原文 `[a,b)` 删除、平台指向 `out`；`ins{orig,out}` 仅用于末尾合成 `\n`。事件位置严格有序，查询用 `sort.Search` 二分并由非导出计数器记录比较次数；无删除的纯 `\n` 文本事件数为 0。合成字节：`ToOrig(L)=n`，`ToOut(n)=L`；终点钳制为 `ToOrig(L+1)=n`。

## 7. 错误与并发
哨兵：`ErrNUL`（携带原文偏移）、`ErrWSTooLong`、`ErrOutputLimit`、`ErrClosed`，均为终态；错误可 `errors.Is` 判定。单个 `norm.Normalizer` 非并发安全；`par` 每段独立实例，K goroutine，仅汇合阶段按序收集。
