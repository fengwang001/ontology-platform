# 快照读可见性判定 — 设计

## 核心推导：为什么只看提交时刻不够

朴素规则：版本由事务 T 写入，若 T 的提交号 < 快照水位，则可见。

关键事实：水位是「已分配提交号的上界」，不是「已完成提交的下界」。
提交号在提交开始时分配，提交完成（对读者生效）在其后；两者之间有窗口。

反例构造：
- T1 开始，提交开始时分配到提交号 5，提交尚未完成。
- 此时创建快照 S：水位已被其他事务推进到 10，T1 仍在进行中，进入 S 的活跃集。
- 朴素规则：5 < 10，判可见。读者于是看到一个「取快照那一刻尚未提交」的版本 —— 错。

正确规则（本实现）：

  可见 ⟺ T 已提交 ∧ 提交号 < 水位 ∧ T ∉ 快照活跃集

活跃集正是「取快照瞬间仍在进行中」的精确记录；提交号比较无法表达这一信息，
两者取交才能排除上述反例。补充边界：T 为快照属主 ⇒ 自己的写自己可见；
T 已回滚 ⇒ 永不可见；提交号 == 水位 ⇒ 不可见（左闭右开）。

## 复杂度

单次判定 = 1 次事务表查找 + 1 次活跃集查找，常数次，不遍历事务表。
每次判定的查找计数随调用返回（每调用独立，无共享状态，并发不串台）。

## 错误与资源

- 可判定错误：未知事务号、快照已释放、提交号非递增（损坏）。
- 活跃集大小有上限；快照内存项数 == 活跃集大小，与事务总数无关。
