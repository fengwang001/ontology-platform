# 设计：多租户配额的公平队列调度器

模块 `ontology`，仅标准库，状态全在进程内存。包划分：`task`（任务）、
`tenant`（队列与虚拟时间账本）、`sched`（加权公平选择）、`admit`（入队背压）、
`stat`（份额统计，时钟可注入）、`cmd/demo`（演示）。

## 1. 为什么是 vt += c/权重，而不是 c*权重

目标：长期下来租户 i 获得的执行代价 C_i 满足 C_i / C_j = w_i / w_j。

调度器每次取 vt 最小者服务，因此所有活跃租户的 vt 始终被压在一个有界区间
内（区间宽度不超过 max(c_i / w_i)：被服务的租户 vt 最多超前一个任务的步长，
下一步就轮到别人）。于是稳态下 vt_i ≈ vt_j，即

    C_i / w_i ≈ C_j / w_j   ⇒   C_i / C_j ≈ w_i / w_j

若改用 c*权重：权重大的租户 vt 推进更快，反而更少被选中，份额与权重成反比，
正好相反。若用 c（不带权重）：所有租户份额相等，权重失效。只有 c/权重 让
「权重大的租户 vt 推进慢、被选中更频繁」，份额正比于权重。

## 2. 空闲租户重新加入：vt 必须抬升

定义系统虚拟时间 sysVT 为「当前活跃（非空）租户的最小 vt」，实现中取每次
出队租户被服务前的 vt，单调不减。

租户 X 空闲期间不参与调度，vt_X 停在旧值。Y 执行 1000 个代价 1、权重 1 的
任务后，sysVT ≈ vt_Y = 1000，而 vt_X 仍 ≈ 1。若 X 直接带着旧 vt 重新入堆，
它是全场最小 vt，会被连续选中，直到 vt_X 追上 vt_Y：

    独占步数 k = ceil((vt_Y - vt_X) * w_X / c) = ceil(999 * 1 / 1) ≈ 1000

即 X 连续独占约 1000 步，Y 被饿死 1000 步——这正是要消除的行为。

正确做法：租户从空转为非空（或新到达）时，

    vt_X ← max(vt_X, sysVT)

抬升后 X 与 Y 处于同一虚拟时间水平，X 在一个轮转周期内（活跃租户数步内）
被服务，随后按权重正常交替；Y 的待执行任务只被推迟常数步，不会饿死。
空闲期间 X 没有排队任务，本就不该积累「欠账」，抬升 vt 正是丢弃这段虚假
欠账。

## 3. 代价为 0 的任务

c = 0 时 vt 不推进，该租户永远是最小 vt，会无限独占。处理：计费代价取下界
MinCost = 1（代价类型为 int64），即 vt += max(c, 1) / w。零代价任务合法入队、
正常执行，但每个任务至少计 1 的虚拟代价，保证 vt 单调推进、租户间交替。

## 4. 同虚拟时间的并列

堆的比较函数：vt 小者优先；vt 相等时按租户 ID 字典序小者优先。规则对全部
租户构成全序，与插入顺序无关，保证同一任务流的调度序列完全可复现。

## 5. 数值表示

vt 用 float64，权重 float64，代价 int64。权重极值并存（如 1e-9 与 1e9）时
单步推进量 ∈ [1e-9, 1e12]，float64 上限约 1.8e308，10^9 步累加也远低于溢出；
公平性判定用相对偏差（5%），float64 的 53 位尾数精度足够。未选用有理数：
标准库 big.Rat 每步开销大且无必要。

## 6. 复杂度

选择用二叉堆（container/heap），单次选择 = 弹出堆顶（下沉 ≤ 2·log2(n) 次
比较）+ 必要时重新压入（上浮 ≤ log2(n) 次），合计 ≤ 3·log2(n) 次比较，
测试上界取 4·ceil(log2(n))。堆中只放非空租户，空闲租户不占选择代价。

## 7. 背压、移除与错误

每租户队列上限由 admit 在入队时检查，超限返回 ErrQueueFull，只影响该租户。
权重 ≤ 0 → ErrInvalidWeight；代价 < 0 → ErrInvalidCost；队列非空时移除租户
→ ErrTenantNotEmpty（拒绝移除，任务保留在队列中继续可被调度）。四类错误
均为哨兵错误，可用 errors.Is 区分。

## 8. 并发与确定性

sched 内一把互斥锁保护全部状态；admit 再加一把锁把「查长度 + 入队」做成
原子操作。每个任务恰好出队一次：出队即从队列头移除，堆中同一租户只有一份。
确定性仅对「同一任务流」成立：并发提交时，提交顺序定义为获取入队锁的串行
化顺序；锁的获取顺序本身不确定，因此并发场景只保证不丢不重，不保证跨运行
的序列一致。固定串行任务流重放任意次，调度序列逐元素相同。

## 9. 时钟注入

stat.Tracker 接受 now func() time.Time（nil 用 time.Now），记录每租户累计
执行代价与最近被服务时刻；测试注入假时钟验证，调度逻辑本身不依赖墙钟。
