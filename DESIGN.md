# 设计推导

## 第 3 条：只含空白的续行会怎样

依据 `java.util.Properties` 的 LineReader：遇到奇数个行尾反斜杠时进入续行状态，
读下一物理行时先持续跳过行首空白（空格/`\t`/`\f`）；跳过过程中一旦读到换行，
该续行被当作"无更多字符"处理，当前逻辑行直接结束。

结论：下一物理行若为空或只含空白，空白被"续行首部剥离"规则吃掉后该行没有剩余字符，
于是该物理行成为当前逻辑行的结尾；其后的物理行开启**新的**逻辑行，不会被拼接。
即空白续行既不追加内容，也不会把再下一行继续连起来。这正是"首部空白剥离"的边界：
剥离一直进行到第一个非空白字符或换行，碰到换行意味着没有任何可追加字符，续行终止。

## 第 6 条：Store 必须转义哪些字符

Load 规则决定 Store 规则（只转义会被 Load 误解的字符）：

- 键中 `\` 必须转义（否则被当成转义引导符）；`=`、`:`、空格/`\t`/`\f` 必须转义，
  否则会被当成键值分隔符或键分隔符（键结束后紧跟分隔符的歧义）。
- 值中 `\` 必须转义；但 `=`、`:`、`#`、`!`、内部空格都不转义——值一旦开始，
  这些字符都是值的普通内容（`k=v # x` 中的 `#` 不是注释即证）。
- 值**开头**的空白必须转义：Load 在找值起点时会跳过分隔符后的连续空白，不转义则丢失；
  值中间/结尾空格是值正文，原样输出即可（这也是 `key = value  ` 尾空格保留的对偶）。
- 键若以 `#`/`!` 开头必须转义：Load 在逻辑行首部看到它们会把整行判为注释，转义后首字符
  不再是注释符；值开头的 `#`/`!` 无需转义，因为值的位置不参与注释判定。
- `\n` `\r` 一律转义，否则物理行被截断、可能触发续行；`\t`、`\f` 用 `\t`/`\f`。
- 非 ASCII 原样按 UTF-8 输出：Load 按字节透传非 ASCII，无需 `\uXXXX`。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | （最后补齐） |
| 2 注释 | （最后补齐） |
| 3 续行（含空白续行） | （最后补齐） |
| 4 转义与 `\u` 错误行列 | （最后补齐） |
| 5 重复键首次顺序 | （最后补齐） |
| 6 Store 最小转义 | （最后补齐） |
| 7 1000 组随机往返 | （最后补齐） |
| 计数器 ≤2N | （最后补齐） |
