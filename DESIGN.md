# 设计推导

## 1. 空输入 / 只含换行（三种末尾策略）

规范化逐行得到：删除行尾空白、行尾统一为 `\n`。末尾策略只决定「最终输出尾部」：

- `Keep`：保留规范化结果的原始末尾状态。
- `EnsureOne`：输出非空时尾部恰好一个 `\n`。
- `TrimBlank`：先删除全部末尾空行（连续 `\n`），非空时补恰好一个 `\n`。

空输入下我选择三种策略都输出 `""`。理由：规范化不应凭空造出原文本不存在的行；且
`N("")=""` 天然幂等。另一选择「EnsureOne 输出 `\n`」也幂等（`N("\n")="\n"`），但会把
「无内容」与「一个空行」混淆，故不取。`"  \r\n"` 是一个只含空白的行：空白被删、行保留。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等：所有输出只含 `\n`、无行尾空白；再规范化不产生删除；EnsureOne/TrimBlank 对
`""`→`""`、对以单 `\n` 结尾的非空文本不动，故 `N(N(x))=N(x))=N(x)`。

## 2. 被删字节的 ToOut

把字节流切成「保留段（orig→out 等长平移）」与「删除段（仅原文区间，长度 m→0）」。
保留段满足 `ToOut(i)=i-d`（d 为之前累计删除量）。要求 `ToOut` 单调不减、且对所有输出
偏移 o 有 `ToOut(ToOrig(o))=o`。设一个删除段原文为 `[s,s+m)`，它右侧保留段第一个字节
的输出偏移为 o，则段内任意 `i∈[s,s+m]` 只能取 o：取更小则落在前一保留段、再映不回 o；
取更大则越过 o。故**删除段（含右端点）整体映射到该删除点之后的输出位置 o**，即
`ToOrig(o)=s`（边界左闭归属删除段）。编辑器把光标准入被删空白时，落在换行符之前
（空白在 `\n` 之前被删；`\r\n` 的 `\r` 被删则光标落在 `\n` 自身位置）。查询用二分。

## 3. 空白缓冲超限

流式处理时，一串空格/制表符在看到行尾或 EOF 前不能判定，必须整块缓冲。两个选项：

- 强制判定为「非行尾」并输出：一旦后续其实是 `\r\n`，已输出的空白无法收回，违反
  语义 2、5，也破坏语义 4 的切分一致性。
- 拒绝：返回带原文偏移的可判定错误，已输出内容保留，进入终态（之后写入返回
  `ErrClosed`）。拒绝不产生错误结果，映射仍自洽，不违反 4/5/6。

故选**拒绝**（`ErrWSBufferLimit`）。输出字节上限同理立即拒绝（`ErrOutputLimit`）。

## 4. par 拼接

各段从任意字节偏移独立处理，采用「开放收尾」：不应用末尾策略、不在段尾解析 EOF。
段结果 = 已定稿前缀 + 未定稿尾部（至多 1 个待定 `\r` + 不超过上限的一串空白，
有常数上界）。拼接第 k 段时，把上一段未定稿尾部与本段原文重新过一遍规范化器：

- 前段空尾：直接采用本段定稿前缀与（按段起点平移原文坐标后的）映射区间。
- 前段有尾：重放 `tail + seg[k]`，定稿前缀成为新结果；每段最多重放一次，总线性。

映射平移：段内区间 orig 坐标加 `segStart`；out 坐标按累计定稿输出长度平移。最后对
全局流做一次 EOF 收尾（待定 `\r`→`\n`、悬空白删除）再应用末尾策略（EnsureOne 可能
插入一个 `\n`；TrimBlank 截掉尾部连续 `\n` 并记为删除段）。切点在 `\r|\n`、空白中间、
「空白+`\r`」中间时，相关字节都在未定稿尾部内被重放统一解决，故任意切分与单流相同。
