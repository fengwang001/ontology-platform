# DESIGN — 带撤回的增量聚合视图维护器

模块 `ontology`，仅用标准库，状态在进程内存，变更日志写本地临时目录。
包划分：`change`（变更记录与编解码）、`agg`（聚合器族）、`journal`（日志追加/重放）、
`view`（分组视图与多阶段编排）、`audit`（全量重算核对）、`cmd/demo`（演示）。

## 1. 变更模型

`change.Change{Version, Op, ID, HasGroup, Group, Value}`：

- `Version` 单调递增，是幂等与乱序判定的唯一依据。
- `ID` 是记录身份：Insert 建立 ID→(Group,Value)，Delete/Update 按 ID 定位旧记录，
  因此 Update 可以把记录移出原组并入新组（旧组删、新组插，两组聚合都维护）。
- `HasGroup` 区分「空串分组键（合法）」与「缺失分组键（拒绝并计数）」。
- 值为 NaN 拒绝并计数；`+0`/`-0` 归一化为 `+0`（IEEE754 中 `-0==+0`，
  归一化保证 Sum 位级一致、Distinct 的 map 键一致）。

## 2. 各聚合器的撤回策略推导

设组 G 的成员多重集为 M，删除记录 x（值 v）后 M' = M \ {x}。

- **Count**：|M'| = |M| − 1。只依赖旧聚合值与被删元素个数，可直接减。**可增量撤回**。
- **Sum**：ΣM' = ΣM − v。加法关于被删元素可逆（减去同一个 v），与集合其余元素无关。
  **可增量撤回**（浮点下与全量重算可能差舍入，但核对器对同一变更序列重放，
  运算次序一致，故位级相等）。
- **Min/Max**：若 v ≠ min(M)，则 min(M') = min(M)，无需动作；若 v == min(M)，
  新极值是 M 中第二小值，而聚合状态只保存了最小值这一个标量，
  **从状态无法反推出次小值**，信息已丢失，必须回到成员重算。
  触发条件是「被删值等于当前极值」，是例外而非常态。**条件性需要成员**。
- **DistinctCount**：|distinct(M')| 是否减 1 取决于 v 在 M 中是否还有其他持有者；
  聚合状态只有一个计数标量，不含每个值的重数，**无法从状态判定**，需要成员。
  （若把状态扩成「值→重数」映射则可增量，但那就把成员信息存进了状态，
  本题按「状态仅为聚合标量」的语义声明需要成员。）

结论表（`agg.Aggregator.NeedsMembersOnDelete()` 显式声明）：

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
|---|---|---|---|
| Count | 是 | 是 | 否 |
| Sum | 是 | 是 | 否 |
| Min | 是 | 条件（删到极值才重算） | 是 |
| Max | 是 | 条件（删到极值才重算） | 是 |
| DistinctCount | 是 | 否 | 是 |

## 3. 多阶段编排与 Recompute 触发条件

`view.Apply` 分三阶段（同一把写锁内完成，读者只见 Commit 后的完整状态）：

1. **Apply**：校验（版本、HasGroup、NaN、ID 存在性）→ 写前向日志（WAL）→
   更新成员表与可增量聚合（Count/Sum 直接加减；Min/Max 尝试增量更新）。
2. **Recompute**：仅当某聚合器声明 `NeedsMembersOnDelete` 且增量路径无法定论时触发：
   Min/Max 被删值==当前极值；DistinctCount 的任何删除。只扫该组成员，
   访问数 ≤ 组当前成员数，用非导出计数器记录重算次数与成员访问量。
3. **Commit**：发布新状态、推进 `lastVersion`。组内成员为空时整组删除
   （`Groups()` 不再出现，查询返回「不存在」而非零值）。

## 4. 崩溃恢复

WAL 先行：变更先完整追加到 `journal`（自描述头 + 长度前缀 + CRC32 校验的记录体），
再改内存。崩溃点在 Apply 中途 / Recompute 中途 / Commit 之前时，日志中该条已完整，
恢复 = 打开日志重放全部完整记录并重放内存编排，因此三种崩溃点恢复后的视图
与不崩溃时逐字段相同。日志重放遇截断只取「完整记录前缀」，半截记录不生效。

## 5. 版本幂等的判定依据

视图记录 `lastVersion` 与最后一条变更的指纹：

- `v > lastVersion`：正常应用。
- `v == lastVersion` 且变更内容与最后一条完全相同：判定为重复投递，幂等跳过，
  视图逐字段不变，不计拒绝。
- `v == lastVersion` 但内容不同，或 `v < lastVersion`：乱序/回退，
  返回可判定错误并计入 `Rejected()`，视图不被污染。

依据：版本号由上游单调分配，同版本同内容只能是重发；同版本不同内容或更小版本
说明流已乱序，应用会破坏「增量==全量」不变量，必须拒绝。

## 6. 日志格式与截断分类

头部 8 字节：magic(4)="OVJ1" + version(2) + headerLen(2)。记录：`len(4) | body | crc32(body)(4)`。
对长度 L 的文件截断到 n（1 ≤ n < L）分类：

- n < 8 → 头部不完整；
- 落在某记录的 len 字段内（余 1..3 字节）→ 长度前缀不完整；
- 落在 body 内 → 记录体不完整；
- body 完整但 crc 字段不足 4 字节或校验不符 → CRC 不匹配；
- 恰好落在记录边界 → 合法前缀，重放成功，生效记录数 = 前缀内完整记录数。

## 7. 并发

单把 `sync.RWMutex`：写者串行完成 Apply→Recompute→Commit 全阶段，
读者只读 Commit 后状态，故不会观察到「Count 已改 Sum 未改」的半更新；
Recompute 在写锁内进行，同组并发写入不会丢失。`-race` 干净。
