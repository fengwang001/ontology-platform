# 设计推导

## 1. 空行与单列空值
选择：**空行（紧邻的两个行尾、或开头的行尾）被跳过，不是记录**。于是"单列空值记录"在字节流里无法用裸空串表示（会与空行混淆），必须写成 `""`。
往返推导：单列表的记录 `[]`（唯一字段为空）若写裸空串+换行，则空行会被跳过，记录丢失；故 writer 在「单列且该字段为空且未加引号」时必须输出 `""`。多列中的空字段不受影响（结构逗号已证明其存在），裸写即可。解析器规则：一条记录在未 emit 任何字段时遇到行尾→跳过；流末尾未 emit 字段→不产生记录。
**规范输入**：字段仅在必要时加引号（含 `, " \r \n`，或单列空值）；引号内转义为 `""`；记录以 `\n` 分隔；文件不以换行结尾。则 `Write(Parse(x))==x`。

## 2. par 切点
每段各跑两台机器：`outside`（段首在引号外、字段开始态）与 `quoted`（段首已在引号内）。`quoted` 机把**段首字节**记入字段、`""` 正常转义；其首个引号即关闭引号（段起始在引号内）。段 0 只信任 outside。从左至右按前段结束态选右段结果：
- 结束在未引号字段中（含 CR 待定前的裸字段）→ 用 outside 结果，其首段事件（段首到首个结构字节）与左段尾事件**合并为同一字段**（偏移取左事件起点）。
- 结束在引号内/刚见关闭引号 → 用 quoted 结果，首个字段事件同样与左段尾事件合并。
- 结束在 CR 待定：右段首字节为 `\n`→换行（用 outside，字段已闭合，正常重放）；否则→孤立 `\r` 错误（偏移为 `\r` 全局偏移）。
段结果只产「片段」：字段片段（值、引号标记、起止偏移）、记录结束。拼接器把片段交 table 重放；偏移本就是全局绝对偏移，记录号/字段号由 table 统一定，错误坐标与单线程一致。`""` 被切开时前段以引号内结束→选 quoted 机，其首字节 `"` 恰为关闭引号，天然正确。每段 ≤2 遍，总量 ≤2N+O(K)。

## 3. CR 待定
未引号字段中 `\r` 后可能是 `\n`（行尾）或孤立 `\r`（错误）。状态 `cr` 记录「该 CR 前引号是否已闭合」：下一 `\n`→行尾；其他字节/EOF→错误，位置为 `\r` 偏移。半包：状态持久化，跨 Feed 无差别。par：按右段首字节裁决（见第 2 节）。引号内的 `\r` 是普通内容（`\r\n` 原样保留）。EOF 停在 `cr`→孤立 CR 错误。

## 4. 上限"立刻"
字段计数：未引号字段每个内容字节 +1（结构字节不计）；引号字段起始引号之后、关闭引号之前每个字节 +1，`""` 两字节计 1（转义后一字符）——引号态见到引号先 +1 再看下一引号。在字节处理入口递增，超 `MaxFieldBytes` 当场返回，不等记录结束。`MaxFields` 在字段闭合时检查；`MaxRecords` 在记录闭合时检查。错误缓存于解析器，后续 Feed/Close 返回同一错误（终态）。

## 状态转移表
状态：`fs`字段开始 `bare`未引号字段 `q`引号内 `qs`引号内刚见引号 `cr`行尾CR待定(带closed标记)。类别：`, " \r \n o=其他 e=EOF`

| 状态 | `,` | `"` | `\r` | `\n` | 其他 | EOF |
|---|---|---|---|---|---|---|
| fs | emit空字段→fs | →q | →cr(F) | 空行:重置→fs | 存字节→bare | 无记录；已有字段则emit空字段收尾 |
| bare | emit字段→fs | 错误:裸引号 | →cr(F) | emit+记录结束→fs | 存字节→bare | emit字段+记录结束 |
| q | 存字节→q | →qs | 存字节→q | 存字节→q | 存字节→q(计数超限即错) | 错误:引号未闭合 |
| qs | emit+→fs | 存'"'(+1)→q | →cr(T) | emit+记录结束→fs | 错误:闭合后杂字符 | emit字段+记录结束 |
| cr | 错误:孤立CR | 错误:孤立CR | 错误:孤立CR | 行尾(closed先emit)+记录结束 | 错误:孤立CR | 错误:孤立CR |

emit 携带值/引号标记/起止字节偏移；词法错误携带字节偏移，记录号字段号由 table 包装（均从 1；字节偏移从 0）。
