# DESIGN：严格模式 Base64 规范尾部推导

一个 4 字符组 `c0 c1 c2 c3` 承载 24 比特：`v0 v1 v2 v3` 为各字符的 6 比特值。

- 满组：24 比特全部是数据，输出 3 字节。
- `xx==`：只输出 1 字节，仅高 8 比特 `(v0<<6)|(v1>>0)` 的高 8 位是数据；
  v1 的低 2 比特不承载数据（2 字符 12 比特 = 8 数据 + 4 剩余？不，12-8=4 剩余，
  其中 v0 全用，v1 只用高 2 比特），因此 **v1 的低 4 比特必须为 0**。
- `xxx=`：输出 2 字节，用掉 16 比特；v0、v1 全用，v2 只用高 4 比特，
  因此 **v2 的低 2 比特必须为 0**。

统一规则：**解码后被丢弃的尾随比特必须全部为 0**。

为什么 `QR==` 必须拒绝：Q=16, R=17；v1=17 的低 4 比特为 0001 ≠ 0。
宽松解码器把这 1 比特丢弃后同样得到 `A`，于是 `QQ==` 与 `QR==` 解码结果相同，
编码-解码不再是双射，破坏「任意字节串的编码结果唯一」（同一明文字节串存在
多个可接受文本）。严格模式只保留尾随零的唯一形式 `QQ==`，其余一律判为
非规范尾部，从而保证 `Encode(Decode(y)) == y`。

判定所用比特：

- `xx==`：检查 v1 的 bit0..bit3（低 4 位）为 0；
- `xxx=`：检查 v2 的 bit0..bit1（低 2 位）为 0。

## 语义与测试对照

<!-- TODO: 测试写完后补表 -->
