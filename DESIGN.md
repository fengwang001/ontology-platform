# 设计：批量写入组提交与顺序保证器

只做一件事：并发写请求攒批 → 一次落盘一次 fsync → 每个调用方拿到自己那条的全局序号。

## 1. 包划分

- `req`：写请求与结果（序号 + error），请求自带独立回传 channel。
- `batcher`：纯攒批逻辑，三个触发条件（条数上限、字节上限、等待时长）先到先得；时钟可注入。
- `wal`：批次编码落盘与读回。
- `commit`：组提交编排，单个聚合协程是每批唯一 leader。
- `recover`：崩溃回放、截断点分类、部分批次丢弃。

## 2. 序号分配时机与回滚

到达即分配：落盘失败会留下永久空洞。
成功后再分配：调用方无法知道自己排在谁后面，且等待者结果难以构造。

采用规则：**批次在聚合协程中组装完成、调用写盘之前分配序号**
（`base = nextSeq`，条目按批内顺序得到 `base+0 … base+n-1`），
然后一次 `Write` + 一次 `Sync`。两者都成功才令 `nextSeq += n`；
任一失败则 `nextSeq` 不前进，即整批序号作废、计数器回滚到 `base`，
批内全部调用方收到错误。由于只有聚合协程写 `nextSeq`，回滚与后续批次天然串行，
下一批重新从同一个 `base` 取号，序号因此连续无空洞。

## 3. 批次原子性（批级 CRC，不是条级）

文件布局：

- 文件头：8 字节魔数 `ONTWAL01`，无任何批次时不创建文件。
- 批次头（小端 16 字节）：`baseSeq u64 | count u32 | entriesBytes u32`。
- 条目序列：逐条 `len u32 | payload`（空 payload 合法，`len=0`）。
- 批尾：`crc u32 = CRC32-IEEE(批次头 16 字节 ‖ 全部条目字节)`，覆盖整批。
  CRC 放在批尾，使“截断点落在 CRC 字段内”也归 CRC 类，四类截断分类均可由纯截断产生。

回放顺序读：头部给全长度，可定位下一批。
条目级 CRC 会造成“丢一条、其余可见”，破坏原子性，故不采用。
批级 CRC 不符 → **整批 5 条全部丢弃**；因头部自描述，可跳过坏批继续解析其后批次。
尾部结构不完整（截断）→ 停在该批之前，恢复“最后一个完整批次”。

## 4. leader 选择与唤醒不串台

只有一个聚合协程：它是所有批次唯一的 leader，负责写盘与同步；
`Submit` 的调用方只把请求放进队列，然后阻塞在**该请求自己的缓冲 size=1 的结果 channel**上。
leader 按“一个请求一个 channel”投递，结果永远直达本人，不存在共享变量串台。

## 5. 攒批规则

- 非空批中，再加入一条会使条数达上限或字节超上限：先冲刷当前批，再装新请求。
- 单条 payload 超过字节上限：不拒绝，它自己单独成批（批字节可超过上限，仅此例外）。
- 计时从批内第一条到达起算；`maxWait=0` 即立即成批。
- 历史最大批字节由 commit 内非导出计数器记录（测试只读），用于断言不越界。

## 6. 故障模型与错误分类

四类回放错误（哨兵，`errors.Is` 可区分）：
`ErrHeaderIncomplete`、`ErrBatchHeaderIncomplete`、`ErrEntryIncomplete`、`ErrCRCMismatch`；
另有提交侧 `ErrClosed`。

- 逐字节截断：截断点落在文件头/批头/条目区间分别归前三类；结构完整但 CRC 不符归 CRC 类。
- 写一半崩溃：尾部不完整批整批不可见，打开日志时截断到最后完整偏移，序号从其后继续。
- 同步失败：注入的 Sync 返回错误 → 整批报错、序号回滚。
- 并发 Close：请求要么在最后一批落盘成功，要么收到 `ErrClosed`；
  请求成功的唯一通道是 leader 投递，故不存在“成功但没落盘”。

## 7. demo 判定项（共 8 项）

1. 第 2 批同步失败后，第 3 批序号紧接第 1 批，无空洞。
2. 一批 5 条中第 3 条被改坏：该批全不可见，前后批正常。
3. 200 协程并发：序号互不相同且与内容一一对应。
4. 10000 请求、批上限 100：同步次数 ≤ 200。
5. 三种攒批触发各一例（条数 / 字节 / 超时）。
6. 四类截断分类各一例。
7. 回放序号连续无空洞。
8. Close 竞争后，所有成功请求都能在日志中回放出来。
