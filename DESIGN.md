# 统一差分设计

## 1. 零计数行号

统一格式头中的 1 基区间是“受影响范围”，不是普通 Go 切片下标。旧文件计数为 0 时，没有旧行可编号，所以 `a` 取插入点之前那一旧行的行号；文件开头之前为 0。

`x\n -> y\nx\n` 在开头插入，因此为 `-0,0 +1`。`a\nb\nc\n -> a\nb\nX\nc\n` 的插入点位于旧第 2 行之后、旧第 3 行之前，因此为 `-2,0 +3`。删除整文件时新计数为 0，`c=0`；`a\n -> 空` 为 `-1 +0,0`。解析时：`b=0` 的插入/零宽锚点是旧切片下标 `a`；普通范围起点下标为 `a-1`。

| 旧声明 | 书写条件 | Apply 解释 |
| --- | --- | --- |
| `-a,0` | 仅删除/插入锚点且旧计数为 0 | 锚点在旧下标 `a`；前面已有 `a` 行 |
| `-a` | b=1，省略 `,1` | 涉及旧下标 `a-1` 起 1 行 |
| `-a,b` | b>=2 | 涉及旧下标 `a-1` 起 b 行 |

## 2. Hunk 合并阈值

设两个相邻改动之间共有 `g` 个未改动行。前一个 hunk 最多吃入后 C 行上下文，后一个 hunk 最多吃入前 C 行上下文；当 `g<=2C` 时这些上下文集合接触或重叠，GNU `diff -U C` 输出一个 hunk。当 `g=2C+1` 时中间至少剩 1 行不属于任一方，输出两个 hunk。故阈值是 `2C`，不是 C 或 `2C+1`：C=1、g=2 应合并，g=3 应分开。

## 3. Myers 并列选择

Myers 在同一编辑距离的多个对角移动中，固定选择“优先删除”：求新的 `k` 时，插入候选来自 `k+1`、删除候选来自 `k-1`，两者并列时选 `k-1`，即先沿旧文件向下。`a b -> b a` 的 hunk 因而先输出 `-a`，再输出删除 `b`/插入 `b` 与插入 `a` 的最短脚本；所有边界条件仍由贪心规则确定，不依赖 map 遍历。

## 4. 偏移后的后续 hunk

前一 hunk 在记录位置偏移 `p` 行处应用后，后续旧文本的物理位置整体平移。若该 hunk 实际还带来 `delta=新增行-删除行`，则下一 hunk 的搜索中心从记录位置平移 `p+delta`。仍以新中心上下各 F 行为候选窗口，多候选按到记录目标位置的绝对距离排序，距离相同取靠前候选。这样后续 hunk 跟随已应用内容，同时不会把无关偏移无限放大。
