# LZ77 流式格式设计

## 流格式

| 记录 | 布局 |
|---|---|
| 流头 | 魔数 `L77` + 版本 varint(0) + 窗口容量 varint |
| 字面量 | tag `0`，长度 varint(n)，n 个原文字节 |
| 回指 | tag `1`，距离 varint(d)，长度 varint(l)；均 ≥1 |
| 刷新 | tag `2` |
| 流尾 | tag `3`，原文长度 varint，CRC-32(IEEE) varint |

varint 为标准无符号小端 7 位组；读端只接受规范、最多 10 字节且不溢出 uint64 的编码。

## 关键推导

1. **Write 无关性**：收到的数据先进入未决缓冲；只有在“当前匹配末端之后仍至少有一个字节”时才确定匹配不会延长。此时提交该匹配，或提交确定不再延伸的一个字面量字节。匹配末端在缓冲末尾时继续等待未来写入；因此跨 1 字节、7 字节或整段边界的候选完全相同。`Flush` 必须声明其后没有未知字节，所以把未决缓冲按已找到的最长匹配强制提交，并写刷新标记。连续刷新没有新字节可提交，只产生零字节。
2. **重叠复制**：块复制要求源和目标互不重叠；当 d<l 时，目标新字节本应成为后续源字节，块复制会读到旧缓冲或未初始化数据。解压器按输出位置逐字节 `out[i]=out[i-d]`。每个字节最多读写一次，复杂度线性；这里直接使用逐字节循环，不引入额外索引状态。
3. **两类距离错误**：d 必须先 ≤ 已产生的历史字节，否则引用不存在的时间点；再必须 ≤ 窗口容量，否则历史虽存在但该流的压缩器承诺无法寻址。压缩器只从当前窗口候选中发射回指，所以合法流不会出现第二类。并行预置字典只改变块压缩器初始化时“已可见历史”；流式解压器仍按全局输出计数，故校验含义不变。
4. **并行确定性**：块 i 的查找器初始窗口只装入上一块原文末尾 `windowSize` 字节，随后只追加本块；压缩输出只由这两段确定的字节与固定窗口、链参数决定，不读共享可变状态或完成通知。各块记录顺序拼接，块边界不需要刷新标记：回指可自然跨块，刷新仅表示调用方语义。

候选数由每个位置哈希链上最多 `maxChain` 个节点计入；全同数据很快匹配到链首并满长停止，不扫描整个窗口。
