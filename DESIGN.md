# 并行任务图调度与失败传播 — 设计

模块：`graph`（构图/环检测/拓扑分层）、`sched`（并发上限/就绪队列/计数器）、
`fail`（状态枚举、错误哨兵、传播模式）、`exec`（执行引擎、结果收集、传播）、
`report`（确定性报告），外加 `cmd/demo`。

## 1. 四类最终状态的判定推导

每个任务生命周期为：`pending → ready → running → terminal`，终态只有四种：

1. **已成功 Success**：进入 running 并正常返回 nil。终态一经写定不可改写。
   失败传播只影响「尚未终态」的任务，故先完成的成功任务保持成功。
2. **失败 Failed**：真正进入 running，且返回非 nil 错误（或 panic，见 §5）。
   报告携带原始错误；每个真实失败都独立记录，多失败全部保留。
3. **被跳过 Skipped**：**从未进入 running**，因依赖链上存在失败而不允许启动。
   判定依据：当某前驱失败时，对「不可运行的后继」做传递标记；标记时携带
   `root = 该失败任务 ID`，随传播逐跳向下传递（不重新指向上一跳）。
   因此 A→B→C 中 A 失败时，C 的跳过原因指向 **A（最初失败任务）**而非 B。
4. **被取消 Canceled**：失败发生瞬间已在 running 的旁支任务，其 `ctx` 被取消，
   引擎将其标记为取消。与 Skipped 的唯一区分依据是「是否已经开始执行」：
   以 `sched.Acquire` 成功（取得运行许可、任务函数已被 goroutine 启动）为界，
   界前终止=Skipped，界后终止=Canceled。被取消任务稍后若仍回写结果，
   终态比较时发现已 Canceled 则丢弃结果，不改状态。

传播规则：任务 X 失败 → 其每个未就绪后继 Y：若 Y 所有未完成前驱均已失败/
被标记不可运行，则 Y 记 Skipped 并继续向下传播；若 Y 仍有其他前驱可能成功，
则保持 pending（best-effort 下等待其完成）。在 ready 队列中尚未被 acquire
的就绪任务：快速失败时立即从队列摘除记 Skipped；尽力而为时不依赖失败任务的
分支照常运行。

## 2. 两种模式

- **FailFast（默认，快速失败）**：首个失败事件立即触发 abort：cancel 共享 ctx、
  标记全部 running 任务为 Canceled、其余未开始任务全部 Skipped，
  然后排空在途结果（终态已定，结果丢弃）。
- **BestEffort（尽力而为）**：不 cancel ctx，不标记旁支；只沿失败边做上述
  Skipped 传递。能成功的无关分支继续跑到完成，多个失败各自独立传播，
  各自 root 指向本分支最早失败任务。

root 选择需确定：任务因多个前驱失败而被跳过时，取「最早完成的失败任务」；
同一引擎事件内并列时取 ID 字典序较小者，保证报告确定。

## 3. 并发与复杂度

- 并发上限 N：带缓冲信号量（容量 N）+ ID 升序就绪小顶堆；`Acquire/Release`
  经同一通道，计数器在持锁下增减并记录历史峰值（非导出 peak）。
- 调度判定只在「任务入队」和「任务完成释放后继」时发生，沿边推进：
  每个节点入队/出队/终态判定各常数次，总计 ≤ 4(V+E)，不做全图扫描。
- 每任务一个 goroutine，全部经引擎 `done` 通道汇合；终态写定后 goroutine
  立即退出，无后台常驻协程 → 无泄漏（失败/取消路径同样排空）。

## 4. 图与确定性

- 边用集合去重（重复加边幂等）；引用未注册任务返回 `ErrUnknownTask`。
- 环检测用迭代三色 DFS 显式栈（1000 层不栈溢出），命中灰边时从显式栈
  切出真实环路径（每条边都在输入中、首尾闭合）；自环同样命中。
- 拓扑分层用迭代 Kahn，同层 ID 升序。
- 报告按任务 ID 字典序逐行输出；就绪堆同刻按 ID 升序取；完成顺序不影响报告。

## 5. panic 与错误区分

任务 panic 在该任务 goroutine 内 `recover`，包成带 `Panic: v` 文本的错误，
  任务记 Failed，调度器不崩溃。
四类判定：状态枚举 `fail.Status`（Success/Failed/Skipped/Canceled）直接区分；
错误用哨兵 `ErrCycle / ErrUnknownTask / ErrPanic`，支持 `errors.Is`，
跳过/取消原因用 `fail.RootCause(id)` 经 `errors.Is` 定位最初失败任务。
