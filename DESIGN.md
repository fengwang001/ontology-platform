# 多租户配额公平队列调度器 — 设计推导

## 1. 为什么虚拟时间按 `c/权重` 推进

目标：长期看，租户 i 获得的执行代价总量 `C_i` 应正比于其权重 `w_i`，即
`C_i / C_j = w_i / w_j`。

调度器每轮选虚拟时间 `vt` 最小者执行，等价于让所有活跃租户的 `vt` 尽量拉平
（每次把最小的往上抬）。稳态下有 `vt_i ≈ vt_j`。若执行代价 c 后令
`vt += c / w`，则 `vt_i = C_i / w_i`，拉平条件给出：

    C_i / w_i ≈ C_j / w_j  ⇒  C_i / C_j ≈ w_i / w_j

正是目标份额。反之若用 `c * w`，则 `vt_i = C_i * w_i`，拉平得
`C_i * w_i ≈ C_j * w_j ⇒ C_i / C_j ≈ w_j / w_i`：权重越大份额越小，方向完全
相反。因此必须是 `c / w`。

## 2. 空闲租户重新加入时的 vt 抬升

系统维护 `sysVT`：最近一次被选中任务开始执行时所在租户的 vt（单调不减）。
它代表「当前活跃租户群体大致推进到的虚拟时间」。

反例（不抬升）：X 的 vt 停在 0 后空闲，Y（等权、任务代价 1）独占执行 1000
步，`vt_Y ≈ 1000`，`sysVT ≈ 999`。此时 X 提交 1 个任务，若直接用旧 vt=0
入堆，X 将一直是堆最小者：每执行 1 步 vt 只 +1，要追上 `vt_Y` 需约 1000
步——X 连续独占约 1000 步，Y 被饿死 1000 步。空闲越久，独占越久，违反
「空闲不应积累未来份额」的公平语义。

正确做法：租户从空转为非空（或首次入队）时，令

    vt ← max(vt, sysVT)

即空闲期间不记账、也不补偿：重新加入者从「大家当前的位置」起跑。抬升后
X 与 Y 的 vt 几乎相等，X 的任务在 1–2 步内被执行，随后两者按权重交替，
Y 不会被饿死。FINDINGS.md 表②给出两种实现的实测对比。

## 3. 代价为 0 的任务

合法，但 `vt += 0 / w` 不推进，该租户会永远停在堆顶、无限独占。处理：给
代价设下界 `minCost`（默认 1，可注入），实际计费代价为
`max(cost, minCost)`。零代价任务按 minCost 记账，vt 正常推进，份额退化
为按权重分配，不会独占。

## 4. 同 vt 并列

堆比较键为 `(vt, tenantID)` 二元组：vt 相等时按租户 ID 字典序。比较全序
确定，同一输入流下调度序列可逐元素复现。

## 5. 数值表示：float64

权重限定 `(0, +Inf)` 开区间内的有限浮点数（构造时拒绝 ≤0、NaN、±Inf）。
vt 用 float64：每次增量 `c/w` 最大约 `maxCost/minWeight`，float64 有 53 位
尾数、量级到 1e308，10^5 次调度累积的相对误差远小于 5% 的公平性容差；
权重极大（1e12）与极小（1e-12）并存时 `c/w` 仍在 float64 正常范围内，不
溢出。有理数方案虽精确，但分子分母无界增长、需要大数库且每步要约分，
工程代价不值得。

## 6. 复杂度与堆

选择用二叉最小堆（`container/heap`），单次 `Next` = 弹顶 + vt 推进 +
（仍非空则）重新入堆，比较次数上界约 `3*ceil(log2 n)`，
测试按 `4*ceil(log2 n)` 断言。堆中只放**非空**租户：出队变空即移除，入队
由空转非空才加入，故空闲租户不占用选择代价，`Active()` 等于非空租户数。

## 7. 背压、移除与错误

- 每租户队列上限由 `admit` 在入队前检查，超限返回 `ErrQueueFull`，只影响
  该租户。
- 权重非法：`tenant.New` 返回 `ErrInvalidWeight`。
- 有排队任务的租户被移除：`sched.Remove` 拒绝并返回 `ErrBusy`，任务留在
  队列中继续参与调度（不静默丢弃）。
- 提交给未知租户：`ErrUnknown`。四类错误均可用 `errors.Is` 区分。

## 8. 并发与确定性

`sched.Scheduler` 内部一把互斥锁串行化所有操作；`admit` 再加一把锁保证
「查长度 + 入队」原子。并发提交时，任务的「提交顺序」定义为**获取入队锁
的串行化顺序**，序号在该临界区内分配，因此同一串行化顺序下调度序列确定；
固定顺序的任务流跑任意多次结果逐元素相同。时钟可注入
（`WithClock`），仅用于给每次决策打时间戳，不影响调度逻辑。
