# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（编码唯一）。

## 必须拒绝的输入（若接受则违反不变量）

1. 显式次数 1（`1a`）：Decode 得 `a`，Encode(`a`)=`a` ≠ `1a`。
2. 次数 0（`0a`）：0 次游程无对应输出；Encode 从不产生 `0`。
3. 前导零（`01a`、`00a`）：规范十进制无前导零；重编码只会是 `1a`/`0a` 形态，≠ 原串。
4. 相邻同符号游程（`2a3a`）：Decode 得 `aaaaa`，Encode 得 `5a` ≠ `2a3a`；故游程先挂起，看到下一符号才判定。
5. `\` 后非数字非反斜杠（`\x`）：Encode 对 x 原样写出，重编码无法还原反斜杠。
6. 末尾孤立 `\`：转义缺少符号，游程不完整，Encode 不会以此结尾。
7. 末尾只有次数无符号（`12`）：游程缺少符号，不是完整编码。
8. 非法 UTF-8：Encode 的符号只可能是合法码点；多字节符号必须合法且非 overlong。

各类对应彼此可区分的哨兵错误，包在带字节偏移的 `OffsetError` 中。

## 大次数取舍：超过 math.MaxInt 即拒绝

理由：Encode 的输入是 Go string，单个游程长度 ≤ len(string) ≤ MaxInt，
因此任何合法编码都不需要大于 MaxInt 的次数；接受更大值只会得到无法表示的输出。
逐位累加次数，每位先用除法预判溢出，溢出返回 ErrCountOverflow，不静默回绕。
输出长度上限 Decoder.Limit 可配置（默认 64MiB，<0 不限）；
写输出前先按 `次数×码点字节数` 校验，超限返回 ErrOutputLimit，
再以 ≤4KiB 的分块写出，绝不在校验前按次数分配内存。

## 语义—测试对照表

（实现完成后补齐）
