# 消息头部集合：解析、规范化与安全回写 — 设计推导

本工程只做一件事：从字节流解析头部集合，规范化，允许增删改，再安全回写。
五个包单向依赖：`token`（无依赖）→ `fold`、`listval`、`policy`（各依赖
`token`）→ `headers`（依赖全部）。禁止反向依赖：下层包不得 import 上层包，
`headers` 不实现 HTTP 服务端、网络、存储、Link、Action。

## 总体模型

- 头部集合是**保序多重映射**：`[]entry` 保存出现顺序，`map[string][]int`
  保存「规范名 → 下标序列」的索引。顺序语义只由切片决定，索引只是加速结构。
- 解析是全有或全无：任何错误都返回 `nil` 集合与可判定错误，绝不返回半截状态。
- 回写规范形：`Name: value\r\n` 逐条输出，值超宽按 `fold` 重折，最后跟一个
  `\r\n` 空行。名字规范形：每段（以 `-` 分隔）首字母大写其余小写。

## 第 3 条：为什么「名字与冒号之间的空白」必须判错

RFC 7230 的字段语法是 `field-name ":" OWS field-value`，冒号前**不允许**
任何空白。假设我们宽容地把 `Host : x` 的空白去掉再接受，会发生什么：

- 代理 A（宽容实现）把 `Content-Length : 5` 规范化为合法头并转发；
- 代理 B（严格实现）拒绝同一请求，或把该行当作**上一个头的折行续行**、
  或当作无名垃圾行丢弃；
- 于是 A 与 B 对「这个头是否存在、值是什么」得出不同结论。攻击者只要让
  前后两端解析分叉，就能让一端看到 `Content-Length: 5`、另一端看不到，
  这是经典的请求走私（request smuggling）入口，与 `Transfer-Encoding`
  分叉同理。

结论：空白出现在名字与冒号之间时，几种合理实现（去掉 / 当续行 / 丢弃）
语义互不兼容，唯一安全的共同点是**拒绝**。因此本库在 `StageColon` 阶段
返回 `ErrSpaceBeforeColon`，绝不宽容修复。同理，名字字符必须全部属于
tchar 集合，否则在 `StageName` 判错。

## 第 4 条：禁止字符集合与「拒绝优于静默删除」

**禁止集合的推导**。回写字节被接收方按行切分，行分隔符是 CR、LF；因此
值中任何 CR(0x0D)、LF(0x0A) 都直接制造额外头部，必须禁止。NUL(0x00)
会让 C 语言风格的接收方提前截断字符串，造成「两端看到的值不同」，同样
禁止。推广之：所有 C0 控制字符（0x00–0x1F）与 DEL(0x7F) 在接收方都可能
被当作某种分隔或被丢弃，只有 HTAB(0x09) 是 RFC 明确允许的字段内容，故：

- 允许：HTAB、SP、VCHAR(0x21–0x7E)、obs-text(0x80–0xFF，按字节透传不解码)；
- 禁止：0x00–0x08、0x0A–0x1F、0x7F；
- 额外禁止：**百分号编码的控制字符**。若下游任何环节做 percent-decode，
  `%0d`、`%0A`、`%00` 会还原成 CR/LF/NUL，等于把注入推迟到下游爆发。
  本库无法知道下游是否解码，故对「`%` 后跟两位十六进制且解码值 <0x20
  或 ==0x7F」的值一律拒绝；不形成合法百分号编码的 `%` 视为普通字符。

**为什么静默删除更危险**。若把非法字符删掉再写出：`a\r\nEvil: 1` 变成
`aEvil: 1`——写出成功，调用方以为值已生效，但实际生效的值与调用方意图
**不同**，且没有任何信号。安全系统的第一原则是「意图与效果必须一致，
否则必须报错」：静默修复让攻击面从「注入」变成「语义歧义」，排查时连
错误日志都没有。拒绝则把失败显式化、可判定化（`ErrInvalidValue`）。

**测过的绕过变体与拦截点**（见 `headers/inject_test.go`）：

- `a\r\nEvil: 1`、`a\nEvil: 1`、`a\rEvil: 1`：CR/LF，值校验拦截；
- 含 NUL、VT(0x0B)、FF(0x0C)、DEL(0x7F)：控制字符，值校验拦截；
- `a%0d%0aEvil:%201`、`a%0AEvil:1`、`a%00Evil:1`：百分号编码控制字符，
  同一校验函数拦截（解析与 Set/Add 共用 `token.IsValidValue`，无第二路径）。

## 第 5 条：引号内逗号不切分

列表型头部（`Accept`、`Cache-Control`、`Vary` 等）按逗号切分，但
quoted-string 内的逗号属于**数据**而非分隔符。不实现这条会解析错的
具体例子：

- `X-List: a; note="x,y", b` 若朴素按逗号切，得到 `a; note="x`、`y"`、
  `b` 三项，引号被劈开后参数语义全毁；
- 转义引号 `say \"hi,\" there` 中的 `\"` 不是字符串结束，其后逗号仍在
  引号内；若不处理 `\"` 就会提前结束引号区，把 `hi,` 的逗号当分隔符。

实现：单次扫描，状态机只有「在引号内 / 不在」两态，引号内 `\` 把下一字符
原样吸收（保留反斜杠本身，保证合并回去逐字节等价）。切出的每项做
`Trim(" ", "\t")`。**空项语义（明确选择）**：`a,,b` 中的空项直接**丢弃**
（与 RFC 9110 对空列表元素的处理一致），故 `Split` 结果永不含空字符串；
由此 `Join(Split(s))` 是幂等的规范形，往返测试即「再切一次结果相同」。
引号未闭合返回 `ErrUnterminatedQuote`，可判定。

## 第 6 条：回写字节等价与幂等性论证

**输入已规范时逐字节相同**：规范形定义为「名字是规范大小写、`: ` 恰好
一个空格、值无首尾空白、无折行或按当前宽度折行、CRLF 行尾、空行结尾」。
解析对规范输入不做任何改写（名字不变、值不剪、折行展开后按同宽度重折
得到同样断点），逐条 `Name: value\r\n` 输出，故与输入逐字节相同。

**输入不规范时的「正确输出」**：定义为对该输入语义（有序名字-值对序列）
应用规范化函数 N 的结果：名字 → 规范大小写；值 → 去首尾空白、续行展开
（前导空白压一格）；行尾 → CRLF；超长 → 按宽度重折。N 是**幂等函数**：
对任意已规范的输出再应用 N，名字已规范（N 的不动点）、值已无首尾空白
（Trim 幂等）、折行断点由确定性算法在同一宽度下重算得到相同结果、行尾
已是 CRLF。因此 `Write(Parse(Write(Parse(x)))) == Write(Parse(x))`，
测试对一组非规范输入验证此式。

## 第 10 条：索引结构、保序论证与比较数上界

- **结构**：`ents []entry` 是唯一权威顺序；`idx map[string][]int` 把规范名
  映射到 `ents` 下标的**升序**切片。每次写操作（Set/Add/Del）后整体重建
  索引（O(N)，写路径可接受），读路径零维护成本。
- **为何不破坏保序**：索引从不参与排序，只回答「哪些下标叫这个名字」；
  返回结果时按下标升序遍历，而重建时按下标 0..N-1 顺序追加，下标序列
  天然升序，故 `GetAll` 的输出顺序 == 插入顺序。测试在多次增删后断言
  顺序仍正确。
- **比较数上界**：按名查找 = 一次哈希表定位 + 对 k 个候选下标各做一次
  名字比对（k = 该名字的重复次数），与集合总条数 N 无关。故单次查找
  比较数上界为 **O(k)**，名字互不相同时 k=1，即 **O(1)**；最坏情况
  （全部 N 条同名）退化为 O(N)，但此时答案本身就有 N 项，任何实现都
  无法更优。非导出计数器 `compares` 记录每次查找比对的条目数，测试以
  N=50 与 N=5000（名字互不相同）对照，断言两次比较数相等且 <=1。

## 其他决策速查

- 截断：任意字节截断 → `*ParseError`，`Stage` 属于 {Name, Colon, Value,
  Fold, Terminator}，指出失败头部与偏移；测试循环覆盖全部截断点。
- 上限：条数 / 名字长 / 值长 / 总字节四类，各有独立哨兵错误
  （`ErrTooManyHeaders` 等），超限即拒；解析全有或全无，故状态零变化。
- 并发：读操作（Get/GetAll/Has/Count/Bytes/Write）持 RLock 可并发；
  写操作（Set/Add/Del）持 Lock 串行化，**写也是并发安全的**，但写与写、
  写与读之间的相对顺序由调度决定，调用方不应依赖跨操作的复合不变量。
- 每文件 <=200 行，测试全部表驱动。
