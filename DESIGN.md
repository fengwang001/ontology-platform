# DESIGN

## 1. 空输入与只含换行（三策略）
策略：`Keep` 原样保留；`EnsureOne` 若出现过换行则末尾恰好一个，否则不加；`TrimBlank` 删除全部末尾空白行后保留一个换行。
关键约束：`ToOrig:[0,n]→[0,m]` 必须单射，否则不存在 `ToOut` 使 `ToOut(ToOrig(o))==o`。
单射要求 `m<=n`，因此规范化器**绝不允许增加字节**：`EnsureOne` 不能给空输入补 `\n`（空→空，幂等成立；
若空→"\n" 则 m>n，第6条不可证），只把已有的一个或多个末尾换行折叠成一个。`TrimBlank` 同样只做折叠/删除。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

`EnsureOne("")==""` 且 `EnsureOne("\n")=="\n"`，两者各自再规范化都不变，幂等成立。

## 2. 被删字节的 ToOut
令 f=`ToOut`（单调不减），g=`ToOrig`（单调不减），要求 f(g(o))=o，故 g 单射、f 为 g 的左逆。
- 删除区间 `[a,b)`（长度 d>0）映射到同一个输出边界点 p：g(p)=a（删区之后的首输出位置），则必须 f(i)=p，i∈[a,b]。
  即被删字节的 `ToOut` 取**删除区间之后的输出偏移**。
- `\r\n`：`\r` 被删、`\n` 保留，故删区 `[i,i+1)`，p 为 `\n` 的输出偏移；`ToOut(\r位置)==ToOut(\n位置)`。
  光标落在被删 `\r` 或被删行尾空白时，跳到换行符**本身**；落在 `\n` 之后再前进。
- 因 m<=n，被删点共用 p 不破坏 f(g(o))=o；空输入 p=0。

## 3. 空白缓冲超限
强制把待定空白判定为「非行尾」会破坏第5条：一旦后续出现换行，真实答案应删除这些空白，
但字节已输出无法收回，违反 4/5/6。故选择**拒绝**：返回哨兵错误 `ErrWSLimit`（携带原文偏移），
已输出内容与映射保留，进入终态（此后 Write/Close 返回 `ErrClosed`）。该行为对任意切分一致：
缓冲长度只依赖累计逻辑输入，与切分无关，拒绝与否逐字节确定。

## 4. par 拼接
切点 c 左右各自流式规范化，但 c 可能落在：`\r|\n`、空白串中间、空白后是 `\r`。
统一把 c **向左修正到安全点**：先越过 c 左侧一串连续的 `[ \t]`；若再左边是 `\r` 则再退 1。
- 落在 `\r\n` 之间：退到 `\r` 左侧，整个 `\r\n` 归右段。
- 落在空白串中：退到空白串起点；该串随其后的字节（换行或非空白）整体归右段。
- 落在 `空白\r` 间：继续退过 `\r`，避免悬挂 CR。
修正后每段边界都在「非空白、非 CR」之后，段内独立 `norm` 与全局逐字节一致。
修正只是改切点坐标，不缓冲任意长空白串；K 段共修正 K-1 次，无额外缓冲区。
映射平移：段 j 的每个 span 区间原坐标 +OrigOffset、输出坐标 +OutOffset，用 `span.Builder.Add` 重放；
相邻 identity 区间自动合并。末尾策略只在拼接后对最终结果应用一次；
段内先按 `Keep` 规范化（不做末尾折叠），因此分段不引入额外折叠。
