# Quoted-Printable 设计说明

## 第 3 条推导一：行末空白必须转义

- 空格/制表符在 QP 里可原样输出，但解码侧与传输侧都可能删除物理
  行末尾的空白（RFC 2045 §6.7：解码时行尾空白必须丢弃）。
- 因此若空白之后紧跟换行或输入结束，原样输出会在往返中丢失，
  必须写成 `=20`/`=09`；行中间的空白不会被删，保持字面值。
- 判定方式：编码器把连续空白 run 缓冲起来，遇到非空白字节就把
  run 按字面值刷出；只有在行结束（换行或 EOF）时刷出的 run 才
  逐个转义。单遍完成，不回扫。

## 第 3 条推导二：软换行不得拆开 `=XX`

- `=XX` 是一个原子 token（3 字符），软换行只允许插在 token 边界。
- 实现：编码器始终缓存一个「待决 token」，新 token 到来时才按
  「当前行长 + token 长 > 75」决定是否先插 `=\r\n`；token 整体
  落盘，绝不按字符截断。行末（真换行/EOF）限额放宽为 76（因为
  后面没有软换行的 `=`）。

## 软换行前的空格算不算「行末空白」？——不算，保持字面值

- 推导：解码器遇到 `=\r\n` 会把这三个字符整体删除并把两行接回。
  空格写在软换行 `=` 之前时，物理行以 `=` 收尾，空格不在物理
  行尾，不会被「删行尾空白」的规则吃掉；接回逻辑行后它位于行
  中间。
- 结论：该空格不是行末空白，不写成 `=20`。这也正是最小转义的
  要求：多写一个 `=20` 属于多余转义，会破坏幂等性（第 7 条）。

## 第 6 条往返等式的准确形式

- 记 norm(x)：把 x 中每个 `\r\n` 与每个单独的 `\n` 都替换为
  `\r\n`（单独的 `\r` 不变，它被编码为 `=0D`）。则对任意字节
  串 x：`Decode(Encode(x)) == norm(x)`，且 Encode(x) 必被严格
  解码器接受。

## 第 7 条：本编码器是最小转义的

- 只转义三类：可打印范围(33–126)外的字节、`=` 本身、行末空白；
  其余一律字面值。因此重编码不会新增任何转义；对合法 y，
  `Encode(Decode(y))` 与 y 的差异只能来自 y 里「可打印字符被
  多余转义」（重编码后还原为字面值）以及软换行位置的规范化，
  二者都不改变解码结果。

## 测试对照表（最后补齐）

- 待补
