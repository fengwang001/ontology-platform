# DESIGN: 结构化日志脱敏与一致性采样

## 1. 一致性采样推导

同一追踪链（trace）的多条记录必须「要么全留、要么全丢」。

- 若每条记录独立掷骰（随机数、时间、到达顺序、记录内容参与决策），则同一条链
  的记录决策会互相独立，链必然断裂。
- 因此决策只能是追踪 ID 的纯函数：`keep(T, r) = h(T)/2^64 < r`，其中
  `h = FNV-1a(T)`。决策与记录内容、时间、顺序无关，只算一次哈希（O(1)）。
- 采样率为 1 时恒留；为 0 时链决策恒丢。比率语义：哈希空间 [0,1) 上的阈值比较。

无追踪 ID 与空串策略：

- `HasTrace=false`（无 ID）：记录无法归属任何链，按「不参与采样、无条件保留」处理，
  以避免静默丢失无法复查的记录；此时也不产生链不完整标记。
- `TraceID=""` 且 `HasTrace=true`：空串是合法 ID，所有空串记录同属一条退化链，
  正常走哈希决策（结果由 `h("")` 决定，确定性、可复现）。

## 2. 错误强制保留与链不完整标记

- `level >= error`：记录无条件保留，即使其链决策为丢弃。
- 链不完整标记（`broken_chain`）置位当且仅当：
  `HasTrace=true && 链决策=丢弃 && level>=error 被强制保留`。
  即该记录是一条被采样丢弃的链上唯一被「抠出来」的记录，读者必须知道它不成链。
- 无追踪 ID 的错误记录不打标记；链本身被保留时也不打标记。

## 3. 嵌套字段路径与点号消歧

- 路径 = 自根映射起各层字段名用 `.` 连接。规则按完整路径串做一次哈希表查找。
- 消歧规则：字段名本身允许含点号；`"a.b"` 既是字面字段名也是嵌套路径 `a → b`
  的拼接结果，二者等价，规则同时匹配两种解释（脱敏取「宁可多脱」的彻底语义，
  不允许因写法不同而漏脱）。数组不占路径层级：数组元素（含元素内嵌套映射）继承
  数组所在路径，如 `{list:[{secret:X}]}` 中元素字段路径仍为 `list.secret`。
- 路径不存在、或路径某层不是映射（如标量/数组中途）时该规则对该记录不生效，不报错。

## 4. 脱敏方式与彻底性

- 替换：写死固定串（默认 `***`，规则可配），长度不保留。
- 哈希：`sha256:` + SHA-256 值的十六进制；同值同结果、异值异结果、不可逆；
  非字符串值先做稳定 JSON 序列化再哈希。
- 截断：保留前 N 个 rune 并附固定标记 `…[truncated]`；不足 N 不截断。
- 执行器对整棵字段树递归一遍，路径规则与值模式规则（正则）在同一趟遍历中套用；
  同一值在任意路径、数组元素内出现都会被处理。返回脱敏后的深拷贝，
  非目标字段值与序列化结果保持不变。

## 5. 复杂度

- 规则集编译期建表；每字段仅一次路径查找，计数 `m = Σ 每记录访问字段数`，
  故 `m ≤ 记录数 × 字段数`，满足 `N×(F+常数)` 上界，与规则数无关。
- 采样每次决策恰好一次 FNV 哈希（有计数器，测试断言计数增量为 1）。

## 6. 落盘格式与错误分类

- 自描述头：魔数 `OSL1` + 头长(u32LE) + JSON 头（含 format/version/layout 说明）。
- 记录：体长度(u32LE) + JSON 体 + CRC32(IEEE)。
- 截断点分类（`errors.Is` 可判定，四类哨兵错误）：
  `ErrHeader`（头不完整）/ `ErrLenPrefix`（长度前缀不完整）/
  `ErrBody`（体不完整）/ `ErrCRC`（CRC 截断或校验不匹配）；
  读取器返回已恢复的最大完整前缀记录加首个错误。
- 规则冲突：同一路径配置两种不同脱敏方式，编译期报错并指明冲突的两条规则。
- 深度超限：构造/解码时深度 > `MaxDepth(32)` 拒绝并计数，迭代式校验不会栈溢出。
- 循环引用：以 hmap 指针做「进入/离开」标记，自引用映射报 `ErrCycle`（DAG 共享合法）。

## 7. 并发

编译后的规则集只读共享；采样器内部仅一个原子计数；脱敏不修改入参记录而是返回
新记录。多协程分片处理后按下标回收，结果与串行逐元素相同，`-race` 干净。
