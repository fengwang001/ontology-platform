# 前缀压缩有序字符串字典 — 设计推导

## 目标与总体结构

输入一批**严格递增**的字符串，切成若干块（block），块内做前缀压缩；
字典（dict）= 多块 + 每块的首末值索引；lookup 提供按序号取值与按值二分；
scan 做前缀范围扫描；verify 做损坏检测与不变量自检。字典可整体落盘、读回。

## 块格式（block）

```
magic u32 | k u32 | n u32 | numRestarts u32 | entriesLen u32   (20 字节头)
restartOffsets[numRestarts] u32   （相对 entries 区起点的偏移）
entries: 每条 = uvarint sharedLen | uvarint suffixLen | suffix 字节
crc32 u32（覆盖此前全部字节）
```

位置 `i%k==0` 的条目是重启点，`sharedLen` 恒为 0（存全量）。

## 一、重启点间隔 K 的权衡推导

- 无重启点（K=∞）：取第 i 条需从块首顺序解压 i 次，取值 O(块大小)。
- 有重启点：第 i 条落在重启区间 `r=i/K`，从重启点 `r*K` 起最多解压
  `i-r*K+1 <= K` 次，平均 `(K+1)/2` 次。**取值代价上界 = K**。
- 压缩率代价：每个重启点放弃与前一条的公共前缀（多存 sharedLen 字节），
  并多 4 字节偏移。每 K 条的开销约为 `4 + 平均公共前缀长`。
  K 越大压缩率越高、取值越慢；K=1 等价于不压缩（每条都是重启点，
  取值 1 次）。K 大于块内条数时只有第 0 条是重启点，合法。
- 结论：K 是「取值延迟」与「空间」的旋钮，实测见 FINDINGS.md 表①。

## 二、为什么二分只能在重启点上做

二分要求 O(1) 取出任意中间位置的比较对象。非重启点的条目只存了
「公共前缀长 + 后缀」，**不知道前一条就解不出完整串**，直接对它二分
会拿到错误的比较对象。因此二分分两段：

1. 在重启点数组（每条都是全量串，可独立解码）上二分，比较次数
   不超过 `ceil(log2(numRestarts))`；
2. 在命中的重启区间内顺序解压并比较，最多 K 次。

加上块级索引二分（`ceil(log2(numBlocks))` 次）与常数次边界比较，
总比较次数上界取 `4*ceil(log2(重启点数)) + K`（宽松上界，测试断言）。
未命中时返回应插入位置（全局序号）。

## 三、有序性检查的位置与前缀长度定义

- 有序性检查放在**构建期**：`block.Encode` 逐条检查严格递增
  （重复值、`ab` 后出现 `a` 这类都拒绝并报告位置），`dict.Build`
  额外检查跨块边界。查询期不再重复检查；故障恢复路径从块首顺序解码、
  遇到非法条目即停，天然保证恢复出的前缀仍严格有序。
- 公共前缀长度**按字节**计（Go string 是字节序列，`strings.HasPrefix`
  也是字节语义）。例：`café`（é 为 2 字节，共 5 字节）与 `cafés`
  的公共前缀按字节 = 5，按码点 = 4。测试对两种口径都给出断言，
  实现采用字节口径。空字符串作首条合法（重启点，sharedLen=0）。

## 四、字典整体格式（dict）

```
magic u32 | k u32 | numBlocks u32 | total u32
每块索引: firstLen u32 first | lastLen u32 last | off u32 | len u32 | count u32
之后顺序拼接各块字节
```

首末值索引全部驻留内存；按值查找先用首末值定位块，只解码目标块。

## 五、截断分类与恢复（verify）

按截断长度 L 分类（H=20，R=numRestarts，E=entriesLen）：

- `L < H` → 头部不完整 `ErrHeader`
- `H <= L < H+4R` → 重启点表不完整 `ErrRestartTable`
- `H+4R <= L < H+4R+E` → 条目不完整 `ErrEntry`
- `H+4R+E <= L < 全长` 或 CRC 校验失败 → CRC 不匹配 `ErrCRC`

恢复：忽略重启点表，从 entries 区起点顺序解码，遇不完整或非法条目即停，
得到「最大可恢复前缀」，单调性由顺序解码保证。

## 六、损坏自愈

- 前缀长度撒谎（sharedLen 大于前一条长度）：解码时检出 `ErrPrefixLen`，不 panic。
- 重启点偏移错位：从重启点解码时若重启位条目 sharedLen 非 0 或解码越界，
  记 `ErrRestartOffset`，回退为从块首顺序解压，结果仍正确，并标注
  `RestartBroken()`（索引失效）。四类截断错误与这两类错误均可用
  `errors.Is` 区分。

## 七、并发

Dict 构建后不可变，查询只读，多协程查找与扫描天然安全；构建与查询并发时
通过 `atomic.Pointer` 整体切换版本，未完成的块对外不可见。`-race` 验证。
