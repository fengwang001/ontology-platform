# 连接顺序代价模型 — 设计推导

## 1. 并列打破：可复现的“代价相等”判定

计划代价由若干 `float64` 项（扫描代价、连接代价）相加得到，加法不满足结合律：
`(a+b)+c` 与 `a+(b+c)` 最坏相差 1 ULP（约 `1e-16` 相对误差）。同一组两个数学上
等价的计划，仅因 DP 枚举的划分子集先后不同，朴素累加结果可能差 1 ULP，此时直接
`costNew == costOld` 会让选择随枚举/登记顺序漂移。

判定策略（`plan.cheaper`）：
1. 先用相对误差判等：
   `|a-b| <= epsRel*max(|a|,|b|, costFloor)`，其中 `epsRel = 1e-9`，`costFloor = 1`。
2. 阈值依据：真实代价差异来源于基数估计（选择率/直方图），这些估计本身是粗粒度的
   （直方图桶级近似、默认选择率等），相对误差在百分级；1 ULP 为 `2^-52 ≈ 2.2e-16`，
   取 `1e-9` 比 ULP 大约 7 个数量级，足以吞掉任何有限次累加的舍入（n≤20 时累加误差
   上界约 `n*ulp ≈ 4e-15` 相对量），同时远小于任何有意义的代价差异（至少 1e-2 量级）。
3. 判等后必须有定论：比较“参与表名序列”（计划叶子从左到右的表名），选字典序更小者；
   名字序也相同（同一逻辑计划）时保留先插入者，保证纯函数式确定。
4. 不判等时直接取较小值。整个比较不使用随机数、不依赖 map 迭代顺序。

登记顺序打乱 20 次：catalog 内部表按名字排序后再建索引，谓词列表也按
`(左表,左列,右表,右列)` 排序，因此 DP 输入逐字节一致。

## 2. 笛卡尔积推迟规则

把表当顶点、连接谓词当无向边。对子集 S 的二分 `(L,R)`：
- 若存在至少一条谓词横跨 `L,R`，允许该划分，谓词全部参与选择率；
- 若不存在横跨谓词（笛卡尔积），仅当 S 是“全局连通块的并”且 S 之外的顶点与 S
  之间也没有边时才允许（即 S 在全局图中无出边）。

推导：若 S 与外部顶点 X 有边，先在 S 内做笛卡尔积，之后仍必须与 X 连接；把笛卡尔积
推迟到 X 加入之后（或与其它连通块合并时）永远不更贵——设笛卡尔积两边为 P,Q，
`|P|*|Q|*|X 选择率修正|` 中选择率 ≤1，先连接 X 只会减小中间基数。因此推迟严格不劣，
规则可在不漏最优的同时保证：链式连通图中任何阶段都不出现笛卡尔积；两段不连通图
A-B、C-D 中，块内各走谓词连接，唯一一次笛卡尔积出现在根（最后一步）。

## 3. 基数估计与代价公式

记号：表 T 行数 `n(T)`；列 c 相异值数 `ndv(c)`；谓词 p 为等值谓词 `L.c = R.d`。

- 单表扫描基数：`card = n(T)`（本模型无基表过滤谓词）。
- 等值谓词选择率（两边统计都存在）：
  `sel(p) = 1 / max(ndv(L.c), ndv(R.d))`。
  依据均匀分布/包容假设：连接后基数约为 `|L|*|R|/max(ndv1,ndv2)`。
- 范围谓词：用等宽直方图，选择率 = 两侧落在重叠取值区间内的行比例之积按桶线性插值。
- 二元连接基数：
  `card = |L|*|R|*Π sel(p_i)`（p_i 为横跨该划分的全部谓词）。
- 多谓词独立性假设：各谓词选择率直接相乘，在 explain 中标注
  “独立性假设（谓词选择率相乘）”；相关列会低估基数，这是已知适用边界。
- 缺统计：`sel(p) = 0.1`（写死默认值），并在计划标注“估计不可靠（缺统计）”。
- 行数为 0：基数乘积自然为 0，后续代价为 0，扫描仍计入固定开销。
- 行数为 1：作为普通表参与，`max(ndv,...)` 至少为 1，不会除零。

代价（单位为抽象 I/O 权重，只用标准库）：
- 扫描：`scanCost(T) = seqPage + cpu*n(T)`，默认 `seqPage=1, cpu=0.01`。
- 连接：`joinCost = joinCpu * (|L| + |R| + card)`，默认 `joinCpu=0.02`；
  笛卡尔积加收 `cartPenalty=1`（仅为标注/区分，不改变推迟规则的最优性）。
- 计划总代价 = 左右子计划代价之和 + 本步连接代价（自底向上累加，固定相加顺序：左→右）。
基数估计只读取 catalog 中不可变的统计快照，不访问任何行数据；`catalog.statsReads`
记录估计期间读取的列数，行访问计数恒为 0。

## 4. 动态规划与计数器

按子集自底向上 DP：`best[mask]` 为该子集的最优计划。
- 枚举每个非空子集（单表直接给扫描计划）；
- 对每个真子集 `sub ⊊ mask`、`sub` 含最低位顶点（消除对称重复）考察划分 `(sub, mask^sub)`，
  应用第 2 节的谓词/笛卡尔规则。
- 计数器：`subsetsExamined ≤ 2^n`；`partitionsExamined ≤ 3^n`（每个顶点有
  “在左/在右/不在集合”三种状态，`3^n = Σ_k C(n,k)(2^k-1)/2*2` 的精确上界）。
  对比 `n! = 479001600`（n=12），`3^12 = 531441`，远小于全排列。
- 硬上限：DP 保留的计划（即已生成的 `best` 条目）超过 `MaxPlans`（默认 4096，
  n=12 恰好 4095 个非空子集可通过，n=20 必超）返回 `ErrTooManyPlans`；超限在写入
  下一条目前判定，map 不再增长。

## 5. 统计信息三种异常

均以哨兵错误表示，可用 `errors.Is` 区分：
- 缺失 `ErrStatsMissing`：列无统计。选择不中止，回退 `sel=0.1` 并在 explain 标注
  “估计不可靠（缺统计：表.列）”；查询 `Catalog.Validate` 时该错误可被显式检出。
- 过期 `ErrStatsStale`：统计行数与目录登记行数相对偏差
  `|nStat-nCat|/max(nCat,1) > 10%`。标注“统计已过期，按目录行数校正”，估计一律用
  目录行数（目录行数为权威值）。
- 损坏 `ErrStatsCorrupt`：直方图桶计数之和 ≠ 统计行数（相对偏差 >1e-6），或桶上界
  非严格递增。错误信息指出“表.列”，无法用于估计，Validate 直接返回该错误。

## 6. 边界与并发

- 单表：直接返回扫描计划，代价 = 扫描代价。
- 零表：`Choose` 返回 `ErrNoTables`。
- 自连接（谓词两侧同表）：明确拒绝，返回 `ErrSelfJoin`；自连接需要重命名/别名语义，
  超出“表集合上子集 DP”的模型范围。
- 并发：catalog 在构建后只读（表/谓词切片在构造时排序冻结），`Choose` 无共享可变
  状态，计数器在每次调用的局部 `Selector` 上；多协程并发调用安全，`-race` 干净。
