# 设计推导

## 1. 空行与单列空值
记录由换行界定，连续两个换行之间没有任何字节，无法承载字段值；因此**空行一律跳过，不产生记录**（末尾换行同理，不产生额外空记录）。
于是单列表若有一条「空值记录」，写成空行会被跳过 → 丢数据。故回写器对**单列记录的唯一空字段必须强制写 `""`**（引号是承载该值的唯一方式）。
规范输入：每条记录以 `\n` 结束；引号仅在内容含 `,`/`"`/`\r`/`\n`、或单列空字段时使用；`"` 转义为 `""`；引号内 `\r\n` 原样保留且不出现裸 `\r`。规范输入满足 `Write(Parse(x))==x`。
## 2. par 切点
切点可为任意字节。第 i 段无法独立知道起点状态，故每段在「起点在引号外 O」与「起点在引号内 Q」两个假设下各跑一次（每段字节计数最多 2 次）。
段 0 真实起点为 O；从左到右，由上一段真实末态决定本段真实假设：末态在引号内→Q，否则→O。
仅在切点恰落在 `""` 中间（携带 S：Q 后首字节已确认为引号）或 `\r` 后（携带 C）时，补一次从切点前 1 字节起步的**短修正扫描**；它至多处理到下一字段/记录边界即与 O 或 Q 结果汇合，与段长无关，每切点为常数。
拼接按全局字节偏移丢弃「闭合点 ≤ 切点」的边界事件，跨段字段片段直接相连（原始 run 切片）。段内坐标加段起点偏移即全局偏移；记录号/字段号由拼接后的事件流统一编号，故错误坐标与单线程完全一致。
## 3. CR 待定
见 `\r` 进入 CR 状态不发事件：下一字节是 `\n` 则记行尾（引号内两者都作为内容）；其他字节或 EOF，则该 `\r` 是孤立 CR，错误偏移指向 `\r` 本身。
半包续传时 CR 状态随状态机挂起，等下一次 Feed 的首字节裁决；Close 时仍在 CR 状态即判孤立 CR。par 切点落在 `\r` 后时按上节短修正扫描处理（携带 C，只裁决那一个 `\n`）。
## 4. 「立刻」上限
字段长度按**逻辑值字节数**计数：普通字节计 1；引号态遇到 `""` 的第二引号时只计 1（两个源字节→一个值字节），其余引号态字节计 1；开/闭引号本身不计。
每读一字节先按此规则累加，再检查 `MaxFieldBytes`，故第一个超限字节到达即返回错误，不等字段或记录闭合。字段数在每完成一字段时检查，记录数在每完成一记录时检查。

## 状态转移表
F=字段开始 U=未引号中 Q=引号中 S=Q后见引号(待裁决) C=CR待定；动作：a=追加普通run、q=追加引号字符、f=字段结束、r=记录结束(空行丢弃)。
| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| F | f | →Q(记开引号偏移) | r | →C | →U,a |
| U | f | 错误:裸引号 | r | →C | a |
| Q | a | →S | a | a | a |
| S | f | →Q,q(值+1) | r | →C(闭合引号止于`"`) | 错误:闭合后非法字符 |
| C 引号外 | f | 错误:孤立CR | r(CRLF) | 错误:孤立CR | 错误:孤立CR |
| C 引号内 | 先补`\r`入值,再同 S 行（`,`→f；`"`→q并→Q；`\n`→r；`\r`→a回C；其他→a回Q） |
EOF：F/U 有进行中字段→收尾；Q/S→引号未闭合；C→孤立 CR；无进行中字段→正常结束。
