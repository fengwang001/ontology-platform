# 只读快照一致性导出器 — 设计推导

## 1. 写时复制（COW）触发点

快照 s 在版本 vs 创建，它必须永远看到“vs 时刻每个键的值”。导出期间键 k 被写为新值 v2：
- 若直接覆盖，导出遍历到 k 时读到 v2，快照被污染。
- 因此写入路径必须在**覆盖前**检查：是否存在版本 ≤ vs 的活动快照。存在则把旧值 v1
  连同本次写入版本 wv 存入该快照的保留区（k→(v1,wv,tombstone)），再更新当前表。
- 读路径 `s.Get(k)`：在保留区里找 wv > vs 的**最小 wv** 条目（即 vs 之后第一次写入前
  的值）；没有则回退当前表。tombstone 区分“空值”与“键不存在”。

## 2. 保留值何时释放 — 为什么只看最老快照

一次写入的旧值，只对“创建于该写入之前”的快照有意义。设活动快照按版本升序
s1(最老), s2, …。一次写入 wv：
- 若 wv > s2 但 ≤ s1 不可能（版本单调、s1 更老）；关键情形：wv 对 s1 可见而对 s2
  不可见时，条目只存在 s1 的保留区；s1 关闭时若 s2 仍活着，必须把其中 wv ≤ s2.vs 的
  条目**转移**给 s2（这些旧值 s2 也需要），wv > s2.vs 的直接丢弃。
- 因此保留区只挂在最老快照上：新写入只要还有任意活动快照就只需保留一份。每关闭最老
  快照做一次转移/清理；最老快照关闭且没有更年轻快照时全部释放，计数归零。

## 3. 导出顺序无关

记录 = 长度前缀编码的 (k,v)。记录本身不携带位置信息，清单总校验和取每条记录 CRC32
的异或（XOR 满足交换律、结合律，与顺序无关）。所以字典序/逆序导出，记录集合相同，
总校验和必然相同；逐字节文件不同是正常的，一致性由“内容集合 + 总校验和”定义。

## 4. 文件格式

导出文件由若干块组成，每块自描述：
`[magic "ONEX"|index uvarint|bodyLen uvarint|crc32(body) uvarint|body]`，
body 为若干 `uvarint(len(k)), k, uvarint(len(v)), v`。块边界按 body 字节上限切，
内存驻留 ≤ 一块。清单（JSON，单独文件，原子重命名写入）：快照版本、顺序、键数、
`[]Chunk{Index, Offset, Length, Records, CRC}`、总校验和。

## 5. 续传与四类可判定错误（errors.Is）

凭清单扫描已有块；从第 k 块对应的**字节偏移**截断，用同一快照同一顺序重新生成后续块。
逐截断点分类：
- 文件在清单结束前缺失 → ErrManifestIncomplete，从块 0 重来；
- 偏移落在块头 16 字节内 → ErrChunkHeaderIncomplete，从该块重来；
- 头完整但 body 不足 → ErrChunkBodyIncomplete，从该块重来；
- body 满但 CRC 不符 → ErrCRC，从该块重来。
块乱序/缺失：比对每块 index 与期望，报出第一处错位/缺失块号（ErrChunkOrder）。

## 6. 快照过期拒绝续传

块内容是在快照版本 vs 上读出来的。快照关闭后保留区已释放，续传将读到**当前**数据，
拼出跨时刻文件。因此续传前必须检查快照存活句柄；已关闭立即返回 ErrSnapshotExpired，
且不触碰已写部分（先判定后开文件，保证半截文件原样保留）。

## 7. 关闭竞争

快照持有 wg：导出 Run/Resume 开始时 Add，结束 Done；Close 先标记关闭再 Wait。
故 Close 与导出并发时，Close 等待导出完整结束，不会有半截文件被当作完整（清单只在
全部块写完后原子写入）。

## 8. 资源约束

- COW 保留区每项仅在导出期间某键首次被改写时产生，故占用 = 被改写的不同键数，
  与总键数无关（10 万键改 100 个 → ≤100）。
- 每个键在一次导出中恰好 Get 一次，读计数器 = 键数。
- 导出逐块构造，body 达到上限即刷盘，峰值驻留 = 一块大小。
