# DESIGN

## 1. 空行与单列空值
选择：**空行被跳过，不产生记录**。推导：记录必须以换行结束，`\n\n` 中第一个 `\n` 结束一条记录，
第二个 `\n` 结束的是零字节记录；零字节记录与"流尾终止换行"在词法上不可区分（后者明确不产生记录），
故所有零字节记录一律跳过。后果：单列 CSV 里，未加引号的空字段无法被解析出来（它就是空行）。
因此单列空值要存在，只能写作 `""`。回写器对单列记录的唯一空字段**必须加引号**，否则写出空字节，
再解析时被当空行跳过，记录丢失（违反往返）。多列中的空字段写成裸空：如 `a,,b` 无歧义。

**规范输入**：记录以 `\r\n` 或 `\n` 结束（末记录可无换行）；无空行；引号仅在必要时出现
（值含 `,` `"` `\r` `\n`，或单列唯一空字段）。此集合上 `Write(Parse(x)) == x` 逐字节成立。

## 2. par 切点方案
buf 按任意偏移切成 K 段（含 0 长段）。每个 worker 对本段各跑**两遍**状态机：入口假设
(a) 引号外字段开始 F；(b) 引号字段内 Q。拼接从左到右：第 0 段真实入口=F；由选中那一遍的出口状态
决定下段入口。边界只可能是 F/B(未引号字段中)/Q/X(引号闭合待定界符)/C(CR 待定)：
- F、B：选 F 假设；B 时把本段首个 cell 与上段未闭合 cell 合并（值拼接，起始偏移取上段）。
- Q：选 Q 假设，同样合并首个 cell（`""` 跨段时第二遍入口 Q 会把孤立 `"` 正确判为转义引号）。
- X：选 F 假设，但先检查本段首字节：非 `,`/`\n`/`\r` 立即报 `QuoteAfterClosed`；是定界符则合并上段
  引号 cell（`\n`/`\r` 情形把 F 假设产生的"空行"事件重解释为上段记录的结束）。
- C：先看本段首字节：非 `\n` 即在该 `\r`（偏移 `segStart-1`）报 `LoneCR`；是 `\n` 则上段记录以
  CRLF 结束，丢弃 F 假设的首个空行事件后继续。
每段两遍、每遍每字节一次，故总处理次数 ≤ 2n（0 长段零开销），无重扫。错误坐标换算：
worker 错误带段内字节偏移 d、段内记录序号 r、字段序号 f；拼接时 `off=segStart+d`，
全局记录号/字段号 = 此前各段已刷记录数/当前记录已开字段数 + r/f（合并入口时 f 减 1）。

## 3. CR 待定
未引号态见到 `\r` 进 C：不发任何事件，保留待定。下字节 `\n`→记录结束（CRLF，`\r` 不入值）；
其它任意字节（含 EOF 后的 Close）→ `LoneCR`，错误偏移指向该 `\r`。引号内 `\r` 是普通内容原样保留。
半包续传：`\r` 作为最后一个字节时状态机暂停在 C，跨 `Feed` 与跨 par 切点同构（见第 2 节 C 边界）。

## 4. 上限的"立刻"
字段字节数按**解码后的值长度**计数：未引号每收 1 字节 +1；引号内每个内容字节 +1，开/闭引号 0，
`""` 折叠成 1 个引号字节（第二个 `"` 到达时 +1）。每次 +1 前先检查，首个超限字节到达即返回
`FieldTooLong`，不等定界符。字段数在每开一个字段时检查；记录数在每条非空记录刷出时检查。
终态后 `Feed`/`Close` 返回同一错误。

## 5. 状态转移表
状态 F=字段开始 B=未引号中 Q=引号内 X=闭合待定界 C=CR待定；动作 eF=发字段 eR=发记录。

| 状态 | `,` | `"` | `\n` | `\r` | 其它 | EOF |
|---|---|---|---|---|---|---|
| F | F,eF | Q | F(空行跳过) | C | B | 结束 |
| B | F,eF | **BareQuote 错** | F,eR | C | B | 结束,eR |
| Q | Q(收) | X | Q(收\n) | Q(收\r) | Q | **UnclosedQuote 错** |
| X | F,eF | Q(收"，""→1 字节) | F,eR | C | **QuoteAfterClosed 错** | 结束,eR |
| C | **LoneCR 错** | **LoneCR 错** | F,eR | **LoneCR 错** | **LoneCR 错** | **LoneCR 错** |

错误均带字节偏移、记录号、字段号（从 1/0 起）。单个流式解析器实例**非并发安全**；不同实例互不共享状态。
