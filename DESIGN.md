# 严格模式流式 Base64 设计

## 规范形式的推导（语义第 2 条）

一个 4 字符组装 24 bit = 3 字节。末尾不足 3 字节时用 `=` 补齐：
- `XX==`：2 个六比特组 = 12 bit，只用高 8 bit，**第 2 字符低 4 bit 是空闲位**；
- `XXX=`：3 个六比特组 = 18 bit，只用高 16 bit，**第 3 字符低 2 bit 是空闲位**。

宽松解码器会掩掉空闲位，于是 `QQ==` 与 `QR==` 都解出 `A`
（Q=16=0b010000，R=17=0b010001，差别只在空闲位）。
这意味着同一字节串存在多个编码，「编码结果唯一」被破坏，
`Encode(Decode(y)) == y` 也不再成立。严格模式因此要求：
**空闲位必须全零**，即只接受编码器自身会产生的唯一形式。

统一规则（规范形式 = 编码器输出的唯一形式）：
1. 输入长度为 4 的倍数（流结束时判定）；
2. `=` 只能出现在最后一个组，且个数与余数匹配（1 字节 → `==`，2 字节 → `=`）；
3. 非规范尾部判定用的比特：`XX==` 查第 2 个六比特组的**低 4 bit**，
   `XXX=` 查第 3 个六比特组的**低 2 bit**，非零即拒绝。

样例验证：`QR==` 中 R 的低 4 bit = 0001 ≠ 0 → 拒绝；
`QUJ=` 中 J=9=0b001001 的低 2 bit = 01 ≠ 0 → 拒绝；
`QQ==`/`QUI=` 空闲位全零 → 合法。`QQ`/`QQ=` 违反规则 1，
`QQ===`、`QQ==QQ==` 违反规则 2（填充不在流末尾）。

## 语义与测试对照表

（待测试写定后补齐）
