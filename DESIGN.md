# 设计推导

依赖方向：`lines → edit → hunk → udiff → patch`（patch 同时依赖 lines），反向依赖为零。

## 1. 零计数 hunk 头的行号（语义 4）

unified diff 的行号是 1 基的「该 hunk 第一行所在的行号」，且计数不含末行无换行标记。
关键事实：对纯插入（旧计数 b=0），GNU 给的是**插入点前一行**的行号，插入点在它后面。
- `x\n` → `y\nx\n`（C=0）：插在文件最前，前面没有行，故 `-0,0`；插入后 y 是新第 1 行，故 `+1`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）：插在旧第 2 行 b 之后、第 3 行 c 之前，故 `-2,0 +3`。
同理纯删除（新计数 d=0）时 `+c` 是删除点前一行的行号：
- `a\n` → 空（C=3）：hunk 只含旧第 1 行的删除，旧侧 `-1`；新文件 0 行，删除点前在新侧是 0 行，故 `+0,0`。
统一规则：**计数为 0 的一侧，行号取「锚点（前一条已保留内容/上一 hunk 末尾）之后尚未涉及的行号」，即 `锚点行号(可能为0)`**；计数≥1 时取该 hunk 该侧第一行的实际 1 基行号。
Apply 读到 b=0（头 `−a,0`）：插入点放在旧文件第 a 行之后（a=0 即文件最前）；读到 d=0（`+c,0`）：删除后该 hunk 对新侧不产生行，后续 hunk 的新侧偏移以 c 为锚。

| 旧侧头 | 书写 | Apply 的插入点解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一锚点行号(可为0) | 在旧第 a 行之后插入（a=0 → 文件开头） |
| b=1 | `-a`（省略 `,1`） | hunk 旧内容从旧第 a 行开始，占 1 行 |
| b≥2 | `-a,b` | hunk 旧内容从旧第 a 行开始，占 b 行 |

## 2. hunk 合并阈值（语义 5）

两个相邻改动块各向外取 C 行上下文。设两块之间未改动行数为 g。
- 若阈值取 **C**：g=C 时两块上下文之间还留 g−C=0 间隔却被合并，会比 GNU 多并；且 g=C+1 被错误分开（GNU 实际合并）。
- 若阈值取 **2C+1**：g=2C+1 时两个各长 C 的上下文仍相邻（中间剩 1 行），GNU 在此情况下**分开**（保持一个 hunk 上下文不超过 C 行的布局），故该阈值会错误合并。
- 正确阈值为 **2C**：g≤2C 合并（两个 C 行窗口相接或重叠），g=2C+1 分开。例 C=3，g=6：两块共享/相接上下文，输出一个 `@@`；g=7：两个 `@@`。C=0 时阈值 0，任何相邻改动都分开。

## 3. Myers 并列最短路径的取舍（语义 3）

`a b` → `b a` 的最短脚本有两条：先删 a 再插 a（`-a b`/`+b a` 形）或先插 b 再删 b，D 都为 2。
实现中前向 Myers 在每条对角线上选择上一步时，k=−d 只能来自向下（删除），k=d 只能来自向右（插入），其余 k 取「先尝试向下（删除，k−1），仅当其 V 不优于来自 k+1 时才向右」。即**固定优先删除**。
效果：`a b`→`b a` 的 hunk 内先输出全部可能的 `-` 行再输出 `+` 行（在同一并列段内删除排在插入之前）。该规则与输入无关、确定性可复现，故同一对输入生成 100 次补丁逐字节相同。

## 4. 偏移应用后后续 hunk 的平移（语义 7）

hunk 按顺序应用。设上一 hunk 记录位置为 oldPos，实际匹配在 oldPos+shift（shift 正为下移）。该 hunk 应用后文件长度变化 delta = 插入行数−删除行数。
下一 hunk 的**记录位置本身已是上一 hunk 之后的旧坐标**，故查找中心平移量只取上一 hunk 实际相对偏移 shift（不再加 delta：记录行号之间的间隔已隐含长度变化，delta 会被下一记录位置与文件游标同步吸收）。
实现维护「文件游标」：匹配成功后按实际位置和 delta 推进；下一 hunk 的搜索中心 = 其记录 oldStart + 累计 shift。窗口仍为中心 ±F。候选距离按与该中心的行差比较，并列取靠前。
