# 结构化日志脱敏与采样器设计

仅用标准库；状态全部在进程内存；sink 唯一副作用是写本地文件。

## 1. 数据模型（record）

- 字段值只能是 `string`、`int64`、`float64`、`bool`、`[]any`、`map[string]any`、`nil`。
- `Record{Level, TraceID, Fields}`；编解码用确定性 JSON（map 键排序），保证同记录字节稳定。
- 构造时递归校验：嵌套深度 > `MaxDepth(32)` 返回 `ErrDepth`；用 map/slice 指针集合检出自引用，返回 `ErrCycle`。四类错误哨兵：`ErrDepth`、`ErrCycle`、`ErrConflict`、`ErrCorrupt`，均可 `errors.Is`。

## 2. 路径语义与歧义消解（最关键点）

路径 `a.b.c` 的解释规则：
1. **字面键优先**：若当前层存在名字就叫整个剩余串 `"a.b.c"` 的键，按字面键命中，不再下钻。
2. 否则按 `.` 切分逐级下钻；中间层不是 map 或键缺失 → 路径不存在，不命中、不报错。
3. 数组：路径段若是十进制非负整数，命中该下标；越界/类型不符即不命中。
4. 字面含点键 `a.b` 与嵌套 `a → b` 同时存在时，规则 1 保证字面键优先，二者不再歧义。

规则两类：路径规则（精确命中某一路径的值）与值模式规则（正则，匹配**任意深度**出现的字符串，含数组元素和被引用值）。脱敏遍历时每个字段只在预编译 trie 上走一步，命中路径规则；字符串值再过编译好的值正则，保证同一敏感值在多处（不同路径/数组/引用）全部处理。

## 3. 脱敏（mask）

- 替换：整体换成固定串 `***REDACTED***`，不保留长度。
- 哈希：`FNV-1a-64` 十六进制；同值同结果、异值结果不同（测试断言），不可逆且不等于原值。
- 截断：保留前 N 个 rune，加后缀 `…(truncated)`；空串保持空串。
- 非目标字段逐字节不变：确定性 JSON 序列化前后逐字节比对（只允许命中位置变化）。
- 遍历复用深度/环检测，超限记录整体拒绝并计数，不处理、不输出。

## 4. 一致性采样（sample）

推导：同一 trace 的记录必须同留同丢。若按记录掷骰或混入时间/顺序/内容，同 trace 不同记录会得到不同决策 → 链断裂。因此决策输入只能是 traceID 本身：`h = FNV-1a-64(traceID)`，`keep = h mod 1e6 < rate*1e6`。决策与记录内容、到达顺序、时间无关，单次恰好 1 次哈希（O(1)）。

- 空 traceID（含空串）：无稳定链标识，按 rate 对**每条记录**独立判定；同记录重复判定仍确定；测试覆盖。
- rate=0：非 error 全丢；rate=1：全留。
- `LevelError` 及以上无条件保留；若其链本该被采样丢弃，记录打 `Incomplete=true`（链不完整），其余情况不打该标记。

## 5. 规则集预编译（rule）

- 规则编译成路径 trie + 正则切片。遍历一条记录时，每个字段在当前 trie 节点做 1 次“整串字面键”探测 + 至多 1 次分段子节点探测，匹配计数 ≈ 字段数 × 常数，与规则数无关。
- 上界：`matches <= 记录数 × (字段数 + 2)`，10 万记录实测断言。
- 矛盾规则：同一路径上既有 replace 又有 hash（动作不同）在 `Compile` 期报 `ErrConflict`，错误指出冲突的两条规则。

## 6. 落盘格式（sink）

文件 = 47 字节固定头 + 若干帧。
头：`"OLOG1\n"(6) | flags 1 | 保留 8 字节 0 | 头CRC32 4 | 载荷CRC32 4 | 记录数 8 | 创建时间 16`。
帧：`payloadLen(4,BE) | JSON payload | CRC32(payload)(4,BE)`。

截断/损坏分类（读取遇到的第一个不完整点即归类）：
- 截断在头内（< 47 或头 CRC 错）→ `ErrHeader`。
- 长度前缀不足 4 字节 → `ErrPrefix`。
- 声明长度超出剩余字节 → `ErrRecord`。
- 记录体够但 CRC 缺失/不符 → `ErrCorrupt`（纯尾部截断至 CRC 缺失属此类；另翻转一字节构造 CRC 不符）。
读取返回已恢复记录 + 错误；恢复数等于最大完整可解码前缀。

## 7. 并发

编译产物不可变；处理器只持原子计数器。多 goroutine 处理同一批记录，输出按确定性键排序后与串行结果逐元素相同；`-race` 干净。

## 8. demo 判定（每行 OK/FAIL）

全留全丢、20 次重复决策、error 强制保留+标记、三处同值脱敏、非目标字节不变、匹配次数上界、四类截断、规则冲突、深度拒绝、并发一致、总计。
