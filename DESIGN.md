# 并行任务图调度与失败传播 — 设计推导

## 1. 模块划分（5 包 + demo）
- graph：节点/边、幂等去重、环检测（Kahn 判环 + DFS 回溯环路径）、拓扑分层。
- fail：并发安全的状态机 Tracker、错误常量、跳过传播。
- sched：就绪队列（同就绪度按 ID 升序取）、信号量限并发、计数器（峰值/就绪判定）。
- exec：逐任务 recover panic，结果写回 Tracker；编排 Run（两种模式）。
- report：按 ID 字典序生成逐字节确定的报告。
- cmd/demo：12 条自判定，每行 OK/FAIL，≤20 行。

## 2. 四类最终状态的判定推导
终态互斥，判定时刻为任务 goroutine 返回或被传播标记时：
1. 成功 Success：任务函数在 ctx 取消前正常返回 nil。终态不可逆，绝不回滚。
2. 失败 Failed：任务函数返回非 nil 错误；panic 被 recover 后以
   fmt.Errorf("%w: %v", ErrTaskPanic, v) 记为失败，错误信息保留原始内容。
   多任务失败全部记录（各自一条 Failed）。
3. 跳过 Skipped：从未启动，且存在失败祖先（传递闭包）。由完成事件沿边 BFS
   传播：某任务的全部前置已结束且至少一个前置非成功，即标记 Skipped；传播是
   级联的（skipped 也会向下游传播）。根原因 = 失败祖先中 ID 字典序最小者，
   故 A→B→C 中 A 失败时 C 的原因指向 A 而非 B（快照时从 C 逆边 DFS 取最小
   Failed 祖先计算，与完成时序无关，保证确定性）。
4. 取消 Canceled：因全局中止而未启动的独立旁支，或已启动后 ctx 被 cancel
   才返回的任务。区分依据只有一条：Skipped 必然存在失败祖先且从未运行；
   Canceled 要么已经运行过（用 started 标志记录），要么与失败点无依赖关系，
   仅被全局中止波回。取消后迟到的成功/错误写回一律丢弃（终态先置 Canceled，
   CAS 语义拒绝改写）。

## 3. 两种模式
- FailFast（默认）：首个 Failed 出现即 cancel() 传播 ctx；失败/跳过祖先链按
  第 2 节规则标 Skipped；与失败点无祖先关系且尚未启动的任务标 Canceled（为让
  它们真正经历取消，调度器仍以原并发上限启动它们，ctx 已取消，立即返回）。
- BestEffort：不做全局 cancel；只有失败任务的传递下游标 Skipped，其余独立分支
  继续跑完。可能出现多个 Failed，报告全部保留。

## 4. 并发与复杂度
- 上限 C 的带缓冲信号量；峰值用非导出 peak（acquire 后 running+1 时取 max）。
- 就绪判定仅发生在初始入队与每次完成事件中，每节点至多扫描一次其出边：
  判定次数 ≤ V（初始）+ 每个完成事件触发的入度/边检查 ≤ 4(V+E)，不全图扫描。
- 每个任务恰好一个 goroutine，Run 经 WaitGroup 等全部结束才返回，无泄漏。

## 5. 确定性
- 图内部边集合化 + 排序，边添加顺序不影响结构。
- 就绪队列多任务同时就绪时按 ID 升序弹出。
- 报告按 ID 排序输出；跳过根原因由图结构静态计算（最小失败祖先 ID），
  与并行完成时序、随机延迟无关。

## 6. 边界
- 空图：直接返回空报告。链式 1000 层：Kahn 迭代判环，无递归栈风险。
- 重复边幂等（集合去重）。依赖不存在节点：AddEdge 返回 ErrNodeNotFound；
  环：Validate 返回 ErrCycle 并附真实环路径（首尾相同，每跳都是输入边）。
- 并发上限 1 退化为串行，报告与 C=8 逐字节一致。
