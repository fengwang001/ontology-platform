# LSH 分桶近似最近邻检索器 — 设计与推导

## 1. 签名计算规则

- 向量 `x ∈ R^d`，每个表含 `b` 个随机超平面，法向量 `r_j ~ N(0,1)^d`（由显式 `int64`
  种子经标准库 `math/rand` 的高斯源确定性生成）。
- 第 `j` 位：`s_j(x) = 1` 当 `dot(r_j, x) > 0`；`dot ≤ 0`（含内积恰为 0）时为 `0`。
  即内积为 0 归入负侧。零向量对所有法向量内积恒为 0，故其签名为全 0，确定且合法。
- 一个表产出一个 `b` 位签名（`uint64` 低位装填，`b ≤ 64`），作为桶键。
- 距离：欧氏距离与内积均在计算前校验维度并拒绝 NaN/±Inf。

## 2. 位数 b、表数 L 与候选数、召回的关系

- 两向量 `x,y` 碰撞于单个超平面位的概率（Charikar 2002，随机超平面 LSH）：
  `Pr[同一位相同] = 1 - θ/π`，`θ = arccos(cos(x,y))` 为夹角。
- 在一张 `b` 位表中同桶概率：`P1 = (1 - θ/π)^b`。
- `L` 张独立表（独立种子）至少一次同桶（成为候选）的概率：
  `P_L = 1 - (1 - P1)^L`。这就是单条真正近邻被召回概率的近似，查询 TopK 召回随之同向变化。

定性结论：
- **b 增大**：单桶体积约按 `2^{-b}` 收缩 → 每表命中的候选数指数级下降，精排更快；
  但 `P1` 减小 → 固定 `L` 下召回 `P_L` 下降。
- **L 增大**：召回 `P_L = 1-(1-P1)^L` 单调上升（逐步并集，只增不减）；
  候选数与哈希计算量（`N·L·b` 次内积）线性增长。
- 因此 b 控「桶粗细/候选规模」，L 控「召回/冗余」，二者独立权衡。
- 工程上查询取 L 张表命中桶的并集作为候选，故候选数随 L 单调增、随 b 单调降；
  候选按真实欧氏距离精排取 TopK。

## 3. 可复现性

- 法向量只依赖显式种子：`rand.New(rand.NewSource(seed))`，逐表逐位按固定顺序取
  `NormFloat64`。同种子 ⇒ 同法向量序列 ⇒ 同输入签名表逐字节相同；不同种子独立。
- 建索引哈希计算次数恰为 `向量数 × 表数 × 位数`（每向量每表每位一次内积），
  由非导出计数器精确统计。

## 4. 检索与复杂度

- 候选生成：查询在每张表算 1 次签名，取桶内 ID 并集；只对并集候选算欧氏距离精排。
- 非导出计数器记录精排距离计算数；5000 向量上要求 ≤500（10%），否则退化为暴力扫描。
- 精确基准：对全量向量算距离取 TopK，用于计算召回（近似∩精确 的 ID 数 / K）。

## 5. 并发可见性

- 所有表先在后台完整构建，再以一次 `atomic` 指针/切片整体发布；查询快照读到的要么是
  全部完成的表，要么一张也看不到（构建中查询返回「索引未就绪」可判定错误）。
  查询路径只读，`-race` 下安全。

## 6. 持久化与故障

- 文件布局（小端）：自描述魔数+版本头（维度 d、表数 L、位数 b、向量数 N）、
  超平面参数（L·b·d 个 float64）、向量表（ID+f64 分量）、桶表（表/签名/ID 列表，
  均带长度前缀）、末尾 4 字节 IEEE CRC32（覆盖此前全部字节）。
- 逐字节截断分类（恢复器顺序判定）：落在头区间→头部不完整；落在超平面区间→超平面参数
  不完整；落在向量/桶表区间→桶表不完整；体完整而末 4 字节缺失/不符→CRC 不匹配。
- 恢复取「最大可恢复前缀」：能解析多少张完整表/桶就恢复多少，并剔除引用了不存在
  向量 ID 的桶项（计数），保证无悬挂 ID。某法向量全零→退化超平面错误，指出表号与位号。
- 四类错误（维度不符、截断/不完整三型合并为 ErrTruncated 并以子错误细分、
  ErrCRC、ErrDegenerateHyperplane）均可用 `errors.Is` 判定。
