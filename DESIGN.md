# DESIGN - scoped symbol table derivations

模型：位置序号由调用方给出（编译器式：声明先全部登记，再对任意引用点查询）。
解析规则：从引用点所在作用域沿父链向外，命中第一个声明了该名字的作用域；
命中后由该声明的种类裁决：Forward 允许任意位置的引用；NoForward 要求
refPos >= decl.Pos，否则报 ErrUseBeforeDeclare，且不外溢到更外层。

## 1. 同一作用域内重复声明同一个名字

结论：可判定错误（scope.ErrDuplicate），不是后者覆盖前者。

推导：不变量 2 要求任何一次 Ref 解析唯一且可归属，且自检要求"所有已记录的
引用都仍能解析到同一个声明"。若允许覆盖：某引用点 R 已解析到前者 D1 并被记录；
覆盖后同一引用点 R 再解析会命中后者 D2——同一个引用点在不同时刻归属不同声明，
"唯一且可归属"退化为"取决于调用时机"，已记录的引用也无法再解析到原声明。覆盖
语义与不变量 2 不可调和，故必须拒绝。拒绝后，已解析到 D1 的引用永远指向 D1，
重复声明只是被拒的无效操作（不变量 5 保证它不留痕），不变量 2 保持。

## 2. 遮蔽与前向引用的交互（核心推导）

情形：内层声明了允许前向引用的 x；该声明之前的位置有一处对 x 的引用；
外层也有一个 x。

- 选择 A：解析到内层那个还没轮到的声明。遮蔽是"作用域级"性质：内层一旦
  声明 x，整个内层（含声明点之前的位置）都以该声明为准；前向引用种类只决定
  声明点之前的位置是解析成功（Forward）还是报 use-before-declare
  （NoForward）。外层 x 对整个内层不可见。
- 选择 B：解析到外层那个已经可见的声明。后果有三：
  (a) 同一个名字在同一作用域内按位置分裂为两个不同声明的绑定，"内层遮蔽外层"
      对内层前半段失效——遮蔽不再是作用域级性质，而只是"声明点之后"的局部现象；
  (b) 不变量 3 要求"允许前向引用的声明可以被它之前位置的引用解析到"，选择 B
      下只要外层恰好有同名声明，该能力就被架空：前向引用是否成立取决于外层
      环境，解析结果不再可组合；
  (c) 这处引用会被算作"捕获了外层 x"，但该名字在本层明明有声明——捕获集合
      混入了并未真正逃逸到外层使用的名字，不变量 4 的"不多"被推到边界上。

结论：选 A。它同时满足全部不变量；选择 B 虽不至于让单次 Ref 返回二义
结果，但会破坏遮蔽的作用域级语义、使不变量 3 形同虚设、并污染捕获判定。

## 3. 捕获的判定边界

与第 2 条保持一致：内层引用的名字若在本层有声明（哪怕声明位置在引用点
之后、哪怕因 NoForward 而报错），该引用都归属本层声明，不算捕获外层。
只有引用沿链解析到深度严格小于本层的声明时，才计入 Captures。

## 4. Leave 到最外层之后再 Leave

结论：可判定错误（scope.ErrLeaveRoot），不静默忽略。

推导：不变量 5 要求失败不留痕，因此这次 Leave 不得改变任何状态；在"不改变
状态"的前提下只剩两种选择——报错或静默。若静默忽略，调用者无法区分"成功弹出
一层"与"无层可弹"，Enter/Leave 的配对错误被掩盖，SelfCheck 所维护的"深度与
配对数一致"对调用者失去意义。故必须返回可判定错误，且与空名字等错误互不相同。
