# 设计推导

## 第 3 条：续行接到「只含空白/为空」的物理行

依据 JDK `Properties$LineReader#readLine`（8u 与 9+ 一致）：

1. 检测续行时，从行尾反向往回数连续反斜杠，奇数则末尾那个反斜杠被当作行连接符丢弃，下一行开头进入「跳过前导空白」状态。
2. 该跳空白循环在遇到换行/EOF 时不重新回到续行判断：空白行里没有任何可拼接字节，于是 `readLine` 直接返回当前已积累的逻辑行；空白行上的跳空白不会追加任何内容。
3. 下一次 `readLine` 调用中 newLine 被重置为 true，因此空白行的下一行被当作全新自然行（可注释、可空、正常解析）。

结论：空白（含 tab/form-feed）续行终止当前逻辑行且不追加字符；其后一行是新自然行。
例：`k=v\` 换行、空白行、`x=y` => `k=v` 与 `x=y` 两条。
它与「续行首部空白被剥掉」是同一条规则的两面：剥空白循环既消费续行前导空白，也消费整条空白续行；空行上永远遇不到非空白字节，连接符无后续可附着，行就此结束。

## 第 6 条：Store 各位置必须转义的字符

解析规则决定最小转义集（只转义会改变解析结果的字符）：

- 键：`#`/`!` 只在自然行行首被识别为注释，而键恰在行首，故键首 `#`/`!` 必须转义；值前面已有 `=`，其 `#`/`!` 永不在行首，不必转义。
- 键以未转义的空格/`=`/`:` 结束，故键中的空格、`=`、`:`、反斜杠必须转义。
- 值首空格会被「分隔符后剥空白」循环吞掉，故值首空白必须转义（写 `\ `）；值中间空格不在剥除窗口内，原样即可。
- 值中未转义的 `=`/`:` 不会再被当作分隔符（只找一个），故值无需转义它们；反斜杠统一转义。
- 控制字符 `\n \r \t \f` 转义为两字符；非 ASCII 原样 UTF-8 输出。
- 空键写 `=v`，空值写 `k` 加换行（无分隔符）；每条记录后跟换行，末条也能被 Load 读回。

## 语义—测试对照

| #   | 语义 | 测试函数 |
| --- | ---- | -------- |
| 1   | （填表） |  |
| 2   | （填表） |  |
| 3   | （填表） |  |
| 4   | （填表） |  |
| 5   | （填表） |  |
| 6   | （填表） |  |
| 7   | （填表） |  |
