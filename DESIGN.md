# RLE 设计说明

不变量：严格解码器接受的每个 `t` 都满足 `Encode(Decode(t)) == t`。
偏移量从整个流式输入的首字节起，按字节计数，指向非法结构的起始字节。

## 必须拒绝的编码

1. 显式次数 `1`：`1a` 解码为 `a`，但 `Encode("a")=="a"`，不等于 `1a`。
2. 次数 `0`：`0a` 不对应任何游程；若解码为空串，编码为 `""`，不等于 `0a`。
3. 前导零：`01a` 若表示一次 `a`，规范形式是 `a`；`00a` 同样不能由编码器产生。
4. 相邻同符号游程：`2a3a` 解码为 `aaaaa`，最长游程编码为 `5a`；违反唯一形式。
5. 非法转义：`\x` 若当作 `x`，其编码是 `x`；反斜杠只能转义数字或反斜杠。
6. 末尾孤立反斜杠：`a\` 没有符号，无法构成完整游程。
7. 只有次数没有符号：`a12` 中 `12` 后缺少码点，编码不会这样结束。
8. 非法 UTF-8：编码器输出始终是合法 UTF-8 与 ASCII 转义；非法字节不可能是规范编码。

## 大次数取舍

选择支持但限定在 `uint64`：次数按十进制逐位累加，每一步用 `n > (max-d)/10`
预判溢出，超出即返回 `runs.ErrCountTooLarge`，绝不回绕。看到符号前不按次数分配
内存；输出用定长小块重复写入，并在每次写入前检查 `已输出字节 + 次数*码点宽度`。
总输出上限可配置，超限返回 `rle.ErrOutputLimit`；`Decode` 使用 64 MiB 默认上限，
流式解码器在构造时给定上限，`limit<=0` 表示不限。

## 流式一致性

解码器是字节状态机：数字、反斜杠、UTF-8 续字节都只依赖保存的状态。一个已完整游程
先暂存，直到读到下一游程的符号并确认二者不同才输出；因此 `2a` 后跨任意切分再读到
`3a`，仍在第二个游程起点报 `ErrAdjacentRuns`。每个输入字节只令内部计数器加一。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 往返 | 待补 |
| 2 规范形式与严格解码 | 待补 |
| 3 大次数 | 待补 |
| 4 最长游程与不规范化 | 待补 |
| 5 跨切分点一致 | 待补 |
| 计数器复杂度 | 待补 |
