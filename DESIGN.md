# 严格 RLE 设计

不变量：解码器接受的每个 `t` 都必须满足 `Encode(Decode(t)) == t`。

## 必须拒绝的输入

1. 显式次数 `1`：`1a` 解码为 `a`，但规范编码是 `a`。
2. 次数 `0`：`0a` 不表示正长度游程；规范编码也从不产生 `0`。
3. 前导零：`01a` 解码为 `a`，但规范编码是 `a`；`010a` 也会与 `10a` 冲突。
4. 相邻同符号游程：`2a3a` 解码为五个 `a`，但规范编码是 `5a`。
5. 非法转义：`\x` 若解成 `x`，规范编码是 `x`，不是 `\x`；其他解释则无确定符号。
6. 末尾孤立反斜杠：`a\` 缺少被转义字符，不是完整符号。
7. 末尾只有次数：`a2` 的第二个游程没有符号，不能成为完整编码。
8. 非法 UTF-8：解码器只接受 Unicode 码点；非法字节经替换或保留都会产生无法由 `Encode` 生成的歧义。

所有错误均带字节偏移；偏移指第一个可证明违规的字节。

## 大次数

选择拒绝大于 `math.MaxInt` 的次数。Go 的字符串长度和索引均由 `int` 描述，超过该上限的值即使解析成功也无法在本机完整表示为字符串。十进制数字逐位累加，在乘 10 加数字前检查上限，因而不会溢出。

解码器不按次数预分配内存，只逐码点追加输出。输出总字节上限可配置，默认 1 MiB；超限返回带偏移的可判定错误。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 往返 | 待补 |
| 规范形式与严格解码 | 待补 |
| 大次数与输出上限 | 待补 |
| 最长游程与不做规范化 | 待补 |
| 跨切分点一致与输入检查计数 | 待补 |
