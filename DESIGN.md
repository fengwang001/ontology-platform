# Quoted-Printable（RFC 2045）设计

## 行末空白为何要转义
解码时行尾的空格/Tab 会被剥离（RFC 2045 6.7），若不转义则信息丢失。
因此：空格/Tab 一律原样输出，但当其后紧跟硬换行（`\n` 或 `\r\n`）或输入结束时，
必须改写为 `=20`/`=09`。编码器逐行缓冲，在「提交一行」时从行尾回扫空白改写。

## 软换行前的空格：结论是不转义
软换行 `=\r\n` 在解码时被整体删除，其前一个字节仍是新一行行首的内容，
并不处于任何一行的行末（提示所述：解码器只删除软换行本身，不删它前面的字节）。
所以软换行恰插在空格之后时，该空格是下一行语义的一部分，保持字面空格即可，
不写 `=20`。判定只在「硬换行/EOF」处进行。

## 不拆 `=XX`
每个输入字节先确定其输出 token：1 字符（字面）或 3 字符（`=XX`）。
当前列 + token 长度 > 76 时先输出 `=\r\n` 再写 token；`=` 计入列数，
故一行只可能在列 ≤75 时追加，`=` 落在第 76 列，token 永不跨行。
76 个可打印字符：第 76 个前插软换行，输出两行 `75 + =\\r\\n + 1`。

## 换行规则
输入 `\n` 与 `\r\n` 视为硬换行，输出 `\r\n`；孤立 `\r` 是普通字节 → `=0D`。

## 严格解码
状态机跨 Write 保留状态（`=`、`=X`、`=\r`、裸 `\r`），故任意切分等价。
五类错误均可判定且带出错字节绝对偏移：
非法转义（含末尾 `=`）、行过长（`\r\n` 前 >76）、不可打印裸字节、行末裸空白。

## 往返等式（准确形式）
令 N(x) 为换行规范化：x 中每个 `\n`（含 `\r\n` 中的）替换为 `\r\n`，
孤立 `\r` 保留。则 **Decode(Encode(x)) == N(x)**（而非恒等于 x）。
对不含 `\n` 的 x 才有 Decode(Encode(x)) == x。Encode 的输出必被严格解码器接受。

## 最小转义（幂等依据）
编码器只转义四类：`=`、33–126 之外字节（含孤立 `\r`）、硬换行符、
硬行末/EOF 前的空白；其余一律字面。即不存在「可打印字符被多余转义」。
对严格解码器接受的 y：先硬换行/软换行切分并校验，再做上述最小重编码，
得到 Encode(Decode(y))。与 y 的差别只可能是 y 中合法但多余的 `=XX`
（解码后是可打印非 `=` 字符）被规范化为字面字符；软换行位由长度规则重新决定。

## 测试对照表
<!-- 在实现完成后补齐：7 条语义 -> 测试函数 -->
