# 基于代价的缓存准入与淘汰器 — 设计推导

## 1. 目标与代价密度公式

缓存的稀缺资源是**字节容量**，收益是**避免重复构造的开销**。
条目 e 在缓存中待一个周期，期望收益 ≈ `构造代价 × 访问次数`，占用成本是 `大小` 字节的容量机会成本。
因此单位容量的收益率为：

```
density(e) = cost(e) × freq(e) / size(e)        (size 为 0 时按 1 计)
```

容量不足时，应淘汰 density 最小者——即「每字节能省下的构造代价」最少的条目。

### 为什么不能只按频率
反例：条目 X（大小 1、代价 100、频率 1）与条目 Y（大小 100、代价 1、频率 10）。
纯频率保留 Y（10 > 1）淘汰 X。但 Y 每字节只省 `1×10/100 = 0.1`，X 每字节省 `100×1/1 = 100`。
Y 占的 100 字节若留给 100 个 X 类条目，收益是 Y 的 1000 倍。频率忽略了「每次命中省多少钱」和「占多少地方」。

### 为什么不能只按大小
若优先淘汰大条目：一个大小为 1、代价为 0 的垃圾条目永久驻留，而大小 1000、代价 10^6、每秒被热点访问的条目被挤走。
若优先淘汰小条目：大量小高价条目被一个大低价条目换走，同上的容量浪费。大小本身不含任何收益信息。

### 为什么也不能只按代价
纯代价（如 LFU 的变体）保留构造贵的条目，但一个代价 10^6 却从不被访问的条目收益为 0。必须乘上频率。

## 2. 准入门槛

新条目 c 申请进入、需腾出 `need` 字节时：
1. 从密度最小端取受害者集合 V，直到 `Σ size(V) ≥ need`；
2. **准入条件**：`density(c) > Σ_{v∈V} density(v)`，否则拒绝准入（c 直接不缓存，返回 `ErrAdmissionDenied`）。

理由：c 进入的代价是永久失去 V 的全部收益率，故 c 的收益率必须盖过 V 之和。
若「无条件准入再淘汰」，一个巨大低价条目（密度 0.01、大小占半个缓存）会一次性冲掉几十个小高价条目，
之后它自己被命中次数极少，缓存整体收益率崩塌。门槛把这种「冲垮」挡在门外：原条目一个不少。

## 3. 频率老化

不老化时，早期热点的计数无限累积，即使它已不再被访问，密度仍虚高，永久占住容量（「缓存钉死」）。
策略：**每个老化周期 T，所有频率计数减半**（指数衰减，半衰期 = T）。

- 实现为惰性衰减：条目记录 `(freq, epoch)`，当前纪元为 E 时有效频率 = `freq >> (E - epoch)`，
  单条访问 O(1)；纪元推进时仅堆序重建（O(n)，周期性摊销）。sketch 的全局计数器数组同步减半。
- 选「减半」而非「清零」：保留近期历史，热度差 1 个数量级的条目约 log2(10)≈3.3 个周期后才拉平，过渡平滑。
- 时钟注入：所有时间判断走可注入时钟；检测到回拨返回 `ErrClockRewind`，当次不老化的同时频率保持不变，
  杜绝「回拨触发重复减半」或「回拨跳过该做的减半」。

## 4. 结构与复杂度

- `entry`：键、大小、代价、频率计数 + 纪元；密度用 `float64` 避免 int64 连乘溢出。
- `sketch`：固定 4096 个 uint32 计数器（16 KiB），哈希取模定位，内存与条目数无关；为未缓存键提供准入时的频率先验。
- `policy`：条目放**最小堆**（按密度），单次淘汰只沿堆下滤路径考察条目，
  考察数 ≈ 2·log2(n)，测试上界取 `4·log2(n) + 16`；绝不全表排序。
- `store`：互斥锁保护容量记账；`used ≤ cap` 为不变式，另设 `overCap` 计数器供测试断言从未超限；
  条目数另有上限 `maxEntries`，使大小为 0 的条目也无法无限准入。
- `metric`：原子计数（命中/未命中/节省代价/三类淘汰原因），并发安全。

## 5. 故障与边界决策

| 情形 | 决策 |
| --- | --- |
| 单条目 > 容量 | 拒绝准入，`ErrTooLarge`，不动任何已缓存条目 |
| 代价 < 0 | `ErrNegativeCost` |
| 代价 = 0 | 合法，密度为 0，淘汰时最先出局 |
| 大小 = 0 | 合法不占容量，密度按 size=1 计；受 maxEntries 约束，超出按密度淘汰 |
| 时钟回拨 | `ErrClockRewind`，频率不变、不老化 |
| 并发 Put 同键 | 锁内去重，只保留一份，容量只记一次 |

四类错误均为哨兵错误，可用 `errors.Is` 区分。
