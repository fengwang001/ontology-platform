# 规范化器设计推导

## 1. 空输入与末尾换行策略（含幂等）

先规范化行体（`\r`/`\r\n`→`\n`，删行尾空白），再应用策略；策略只看末尾的 `\n` 游程。
- **Keep**：原样保留末尾换行游程（含 0 个）。
- **EnsureOne**：末尾无换行则补一个；有则保留。
- **TrimBlank**：删除全部末尾空行后保留恰好一个换行。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

空输入的两种选择：EnsureOne 输出 `""` 则无法与「无末尾换行」区分，再跑一次仍被补成 `\n`，破坏幂等，故必须输出 `"\n"`。
TrimBlank 对空输入若补 `\n`，再跑时「删除末尾空行再保留一个」仍得 `\n`，幂等成立；但「全空文件规范化后凭空出现一行」不合理，且语义是「删空行」，空文件没有行可删，故选 `""`，再跑仍为 `""`。
三种策略的输出均无 `\r`、无行尾空白，故再规范化行体是恒等；末尾 `\n` 游程在各自策略下是不动点：EnsureOne 不动已有游程，TrimBlank 把任何非空末尾游程压成 1、空游程保持 0。

## 2. 被删字节的 `ToOut`

约定：删除区间 `[a,b)`（其后紧跟被保留区间 `[b,c)`）映射到同一输出位置，即**被删字节吸附到删除区右侧、下一保留内容的起点**；文件尾被删字节映射到输出长度。
- `\r\n` 的 `\r`：`ToOut(i)=o`，其中 `o` 是该 `\n` 在输出中的位置（光标落在行尾换行符**之前**，与编辑器「行尾」位置一致）。
- 行尾空白：吸附到其后 `\n` 的起点；文件尾被删空白吸附到输出终点。

由此映射单调不减（删除区是平台段）。`ToOrig(o)` 对有原文对应的输出位置返回该字节原偏移；纯合成位置（EnsureOne 对空输入补的 `\n`，无原文对应）`ToOrig` 取 0（锚点），`ToOut(0)=0` 时该例外成立（此唯一合成字节同时位于终点，`ToOut(0)` 自然等于输出长度）。
除该合成字节外，每个输出保留字节 o 满足 `ToOut(ToOrig(o))==o`。

## 3. 空白缓冲超限

选择**拒绝**。强制输出把「行尾空白」暂判为「行内空白」会永久写死错误结果，后续若真出现行尾/EOF，无法收回，直接违反幂等（第 5 条）与跨切分一致（第 4 条，切分疏密不同会导致结果不同）。
拒绝只违反「继续处理」，不违反 4/5/6：超限时返回带原文偏移的哨兵错误 `ErrPendingLimit`，已输出字节与映射保留，流进入终态，后续 Write/Close 返回 `ErrClosed`。由于拒绝点由输入内容与偏移唯一决定，与切分方式无关（缓冲在任何切分下都必须存满同样的空白游程），第 4 条仍成立。

## 4. par 拼接

每段独立用 **Keep** 规范化（段尾 `\n` 游程因此保留，只有行尾空白与被并掉的 `\r` 会丢），得到段输出与段内 span，再：
1. **跨段 `\r|\n` 修正**：段末为 `\r` 且下段首为 `\n` 时，删掉前段末 `\n`（该 `\n` 在段内本是由 `\r` 合成），等价于把它并入 `\r\n`；切点恰在原 `\r` 与 `\n` 间时偏移天然对齐，无需改段。
2. **跨段空白游程修正**：若上段删除了段尾空白游程（段内 EOF 决策），而下一段首字符是普通字节或 EOF（非行尾），把该游程原文插回拼接点；下段首为行尾则不插（删除正确）。
3. 全部分段用 Keep 拼接后，对整体**只应用一次**末尾策略，保证与单流一致、幂等。

修正只依赖每段末尾至多「一个待定 `\r` + 一条空白游程」与下段首字节，状态有上界，故为 O(K)。span 拼接：每段映射整体加段在全局原文/输出中的偏移量；插入/删除点用 `span.Map` 的 replay 追加一段 keep/delete 记录；最终策略同理 replay 其末尾 keep/delete。
