# 统一格式 diff：设计推导

行被定义为「内容 + 原始行尾」（`\n`、`\r\n`、末行空）；比较用整行，拼接原样还原。

## 1. 零计数时的行号

hunk 头 `-a,b` 里 b 是该 hunk 占用的旧文件行数。b=0 时占用 0 行，
`a` 必须是「插入点前一行的 1 基行号」；插入点在文件最前则 a=0。
这样 GNU 规则统一为：旧区间起点 = 第一条受影响行的行号（插入时受影响的是其后的行，
故用前一行号）；新区间同理。

样例核对：插入到文件头 → `-0,0 +1`；在第 2 行后插入（新内容成为新第 3 行）→ `-2,0 +3`；
删除唯一一行（新文件为空，插入点在新文件最前）→ `-1 +0,0`。
Apply 解析时插入点 = 已匹配的 a-1 条旧行之后（a=0 即在开头）。

| 旧侧 | 书写 | Apply 解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一行号（开头为 0） | 在第 a 行之后插入，不消费旧行 |
| b=1 | `-a`（省略 `,1`） | 消费旧第 a 行（删除或上下文） |
| b≥2 | `-a,b` | 从旧第 a 行起消费 b 行 |

## 2. hunk 合并阈值

两改动区间之间有 g 行未改动。上下文 C 时，左 hunk 最多右扩 C 行、右 hunk 最多左扩 C 行：
g ≤ 2C 时两侧上下文相交/相接，GNU 输出一个 hunk；g = 2C+1 时中间至少剩一行
不属于任一方，GNU 拆成两个 hunk。故阈值是 **2C**。
C=1 为例：g=2 时两 hunk 上下文相接（共享边界行），GNU 合并；
g=3 时中间留 1 行，GNU 拆开。阈值取 C 会在 g=2 时误拆，取 2C+1 会在 g=3 时误并。

## 3. 并列最短脚本的选择

Myers 在对角线 d 上枚举 k：取删（k-1）与插（k+1）两个前驱，删优先
（x 相等时取来自 k-1 的前驱，即先 `-` 后 `+`）。因此同一 hunk 内删除行排在插入行之前。
`a b → b a`：脚本为 `-a`、`b`（上下文）、`+a`，而不是先插后删。此选择全局固定，故输出确定。

## 4. 偏移后后续 hunk 的查找

hunk i 在记录位置偏移 δᵢ 处应用。前面已应用 hunk 的净位移
（插入行数−删除行数之和）改变后续 hunk 在当前文本中的期望位置；本 hunk 实际
落点相对记录位置的偏差为 δ。下一个 hunk 的查找中心 = 它的记录位置 + Σδ。
理由：记录位置基于原始文本，已应用 hunk 把其后所有行整体平移；已偏移的 hunk
多平移的部分同样累加。搜索仍在中心 ±F 行，候选取 |δ| 最小，相同 δ 取靠前者。
