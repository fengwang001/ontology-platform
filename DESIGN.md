# 并行任务图调度与失败传播设计

## 1. 数据模型与前置校验
- 节点由唯一字符串 ID 表示；边 `A->B` 表示 B 依赖 A，重复边在邻接表和入度中只生效一次。
- 构建时记录 `deps[B]` 和反向邻接 `down[A]`。引用未声明节点返回可判定错误。
- 执行前用 Kahn 算法校验 DAG；剩余节点上通过入度链回溯并闭合出一条真实路径。自环必然在首个位置闭合。
- 拓扑分层只用于校验与解释，不作为执行时反复扫描的依据。

## 2. 调度不变量
- 每个节点只经历：未开始、运行中、已终态三类内部阶段；外部只报告四类结果之一。
- 初始入度为 0 的节点进入就绪堆；多个节点同时就绪时按 ID 升序弹出，保证报告和调度选择确定。
- 工作信号量限制同时运行数为 `maxParallel`；上限为 1 时自然退化为串行。
- 某任务成功后只把其出边对应节点的剩余入度减一；每个节点、每条边仅被处理常数次，因此就绪判定次数不超过 `4(V+E)`。
- 完成事件由单个调度循环串行消费，因而无需在任务 goroutine 间共享可变状态；任务 goroutine 只负责执行并回传一次事件。

## 3. 四类最终状态的判定
- **成功**：终态只写入一次。失败开始传播前已经成功的节点保持成功，不允许之后改成跳过或取消。
- **失败**：任务函数实际开始执行并返回普通错误；panic 由执行边界 recover，并转换成带 panic 标记的失败错误。
- **被跳过**：直到图停止都从未开始的节点。其原因不是直接上游，而是它所有失败祖先中按任务 ID 排序的最早失败任务。
- **被取消**：失败开始传播时已经从就绪队列弹出、任务函数已经启动但尚未终态的节点。它收到 context 取消；是否尊重取消不影响最终状态。

关键区分依据是“是否曾经开始执行”：已开始为取消，从未开始为跳过。任务不能因为依赖失败而被跳过同时又被取消，因为调度器不会启动依赖未满足的节点。

## 4. 快速失败与尽力而为
- 快速失败：收到首个失败事件后进入失败态，取消所有运行中的旁支；尚未启动的节点最终跳过。其跳过原因通过失败传播闭包定位到最早失败祖先。
- 尽力而为：不取消与失败无依赖关系的旁支，允许它们运行并成功；只有失败节点的传递下游不启动并标记跳过。多个失败都会保留，下游原因取各自失败祖先中的 ID 最小值。
- 两模式都记录全部真实失败；模式只决定独立旁支是否继续，不影响已成功任务，也不改变失败定义。

## 5. 取消后写回
运行中任务在取消事件后进入逻辑终态“被取消”。执行函数可能忽略 context，稍后才返回结果或错误；调度循环通过终态表丢弃迟到事件，因此不会覆盖取消状态，也不会重复释放容量。容量在决定取消时立即回收，避免等待不合作任务。

## 6. 复杂度、确定性与生命周期
- 时间复杂度 `O((V+E)log V)`，主要来自就绪堆；外部就绪判定计数为线性。空间复杂度 `O(V+E)`；链式 1000 层使用迭代和堆，不递归遍历。
- 并发峰值由容量信号量保证；测试读取调度器内建的历史峰值和就绪判定计数。
- 报告按 ID 字典序渲染：状态、错误原因、跳过/取消原因；不包含时间、地址、随机完成顺序。
- 调度器为每个实际启动的任务建立一个可接收事件的完成入口，并等待其退出；panic、迟到写回和失败路径都不会留下 goroutine。
