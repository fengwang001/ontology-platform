# 设计：带撤回的增量聚合视图维护器

## 1. 撤回性推导

设某组的聚合状态只由已提交的变更累积而来。插入对所有聚合都是增量可做的；删除则取决于「减去被删值后能否唯一确定新结果」。

- **Count**：状态是成员条数 n，删除即 n-1，信息无损 → 可增量撤回，不需要成员。
- **Sum**：状态是标量和 S，删除即 S-v，减法唯一 → 可增量撤回，不需要成员。
- **Min**：状态仅是当前极值 m。若被删值 v>m，m 不变；若 v=m（极值被删），其余成员中可能存在多个 m 也可能没有，新极值无法由标量状态反推 → 必须扫描该组成员重算。
- **Max**：与 Min 对称，v=m 时必须重算。
- **DistinctCount**：状态是不同值集合（不持有频次即不完整）。删除 v 时必须知道是否还有其他成员持有 v；按「聚合器不持有成员」的统一约束，这等同于需要成员 → 标记为需要重算（实现上等价于每值频次减到 0 时移除，但触发判定走 Recompute）。

规则：每个聚合器实现 `NeedsMembersOnDelete() bool`。删除/移出某组时，只要该组任一聚合器声明需要成员且其状态受影响，view 触发一次该组的 **Recompute**（一次扫描同时重建所有需要重算的聚合器）。Count/Sum 直接撤回，永不重算；重算是例外而非常态。

## 2. 三阶段维护

`Apply → Recompute → Commit`：
1. Apply：校验变更（版本、字段），把变更追加进日志，然后更新逻辑成员表（按记录 ID 保存当前 group/value）。
2. Recompute：仅当有聚合器声明需要且删除命中极值/去重值时，扫描该组成员，用扫描结果整体替换对应聚合状态。计数：重算次数（按聚合器）、重算访问成员数（仅访问本组，上界=该组当前成员数）。
3. Commit：发布新状态。读写共用一把 RWMutex，读者要么看到旧整组要么看到新整组，看不到半更新；Recompute 与写入互斥，同组并发写入不会丢。

更新=旧组删除+新组插入（两阶段分别按上规则处理，可能触发一次重算）。组内成员清空时删除该组，`Get` 返回 ErrGroupNotFound（区分零值与不存在）。

## 3. 版本与幂等

view 记录已应用的最大版本 `maxVer`。
- `ver < maxVer`：回退，返回 ErrVersionRegression（可判定），拒绝计数+1，状态不动。
- `ver == maxVer`：同一变更重投（崩溃恢复/重试）视为幂等重放：字段必须与已应用记录一致，直接返回成功且不产生任何状态变化（幂等键=版本，同一版本不会出现两条不同变更）。
- `ver > maxVer`：正常应用。
乱序到达即落入回退分支：拒绝且计入 Rejected，视图不被污染。

## 4. 日志与恢复

文件格式：固定头（magic+版本，16 字节）后逐条帧：`uint32 载荷长度 | 载荷 | uint32 CRC32(IEEE)`。载荷是变更的自描述二进制（op/ver/id/group/value，group 缺失用标志位表示）。
重放从头顺序读：头不完整→ErrShortHeader；长度前缀不完整→ErrShortLength；载荷读不满→ErrShortBody；CRC 不符→ErrCRC。截断点之外的完整帧照常生效，半截帧丢弃（重放结果=完整前缀的全量重算）。
崩溃点注入：Apply 中途（日志只写半帧）、Recompute 中途（日志完整、聚合未发布）、Commit 前（成员表/聚合未发布）。三态都由「日志可重放 + 重放后重放已接受变更 + 崩溃的那条由调用方重投（幂等）」恢复，结果与不崩溃逐字段相同。

## 5. 边界

空视图合法；空串 group 合法；缺失 group、NaN 值拒绝并计数；+0/-0 经 `math.Float64bits` 归一化后视为相等（Min 比较与 distinct 集合）；更新跨组时两组分别维护；审计比对 Sum 用 Float64bits 位级相等。

## 6. 包划分

change（记录+编解码）、agg（5 个聚合器，显式声明撤回需求）、journal（追加写/分类重放）、view（成员表+编排+计数+并发）、audit（独立全量重算逐组比对）、cmd/demo（8 条判定）。每个实现文件 ≤200 行；测试表驱动、按包合并。
