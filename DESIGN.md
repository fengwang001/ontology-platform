# 设计推导

## 1. 非法单元切分规则（字节级）

从七个样例统一推出：一个首字节先按长度声明 `w` 向后取延续字节；
**第二字节受首字节的合法区间约束**，不在区间内则首字节单独成一个非法单元，
从那个字节起重新解析；后续延续字节若不是 `80..BF`，已合法吞掉的延续字节
与首字节合并成一个非法单元，坏字节重新解析（`E2 82`+EOF 只有一个，因为坏字节不存在）。
这样：`F0 90 80 41` 吞 3 个（90 不在 F0 区间，首字节 1 个，80/41 再各 1 个）；
`E0 80 80` 中 80 不在 E0 区间→3 个单字节；`ED A0 80` 同理（A0 落在代理区）；
`C0 AF`→2 个；`F4 90 80 80`→4 个；`80 80`→2 个。

| 首字节 | w | 第二字节合法区间 | 非法时吞字节数 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 1 |
| E0 | 3 | A0..BF | 1（后续坏延续则吞已收全部） |
| E1..EC | 3 | 80..BF | 1 / 已收全部 |
| ED | 3 | 80..9F | 1 / 已收全部 |
| EE..EF | 3 | 80..BF | 1 / 已收全部 |
| F0 | 4 | 90..BF | 1 / 已收全部 |
| F1..F3 | 4 | 80..BF | 1 / 已收全部 |
| F4 | 4 | 80..8F | 1 / 已收全部 |
| 80..BF | 1 | —（裸延续） | 1 |
| C0 C1 F5..FF | 1 | —（永不合法） | 1 |

## 2. 切分缓存上限与立即判定

最长合法序列 4 字节，已收首字节后最多再等 3 个延续字节，故缓存硬上限 **3**。
看到下列字节即可立即判非法，无需等流结束：第二字节越界、声明长度已收满仍坏、
遇到 ASCII/新首字节（它必须能作为新字符起点，不能被一起吞）。
只有「首字节+若干合法延续但尚未收满」才允许跨 Write 等待；`Close` 时仍残留即截断。

## 3. par 切点对齐

段切在字节 `c`。段真正起点 `a≤c`：从 c-1 向前最多找 3 个字节，寻找一个
满足 `lead+w > c`（声明序列跨过切点）且 `a..c-1` 全部是该 lead 下合法前缀的 lead；
找不到则 `a=c`。段对 `data[a:end]` 调用流式转码，但只报告起点 ≥c 的单元
（跨切点的那一个单元归前一段）。这正是流式规则本身：例如 `C3|80 80`，
C3 的单元跨过切点，前段在 Close 时得 1 个 FFFD（吞 C3…第一个80），
后段从 80 起步，再得 1 个 FFFD，合计与单线程相同。对齐每段至多回看 3 字节，
复杂度 O(n+8·3)。首段从 0 开始；尾段自然承担最后截断。

## 4. 上限断点

输出只在完整标量后追加；放不下下一标量即返回 ErrLimit。此时返回的已消费 n
只统计**已完整输出的标量**对应的输入字节；仍在缓存里的半截字符字节不算已消费，
调用方拿 `len(p)-n`（连同尾部）开新实例续传，拼接与无限额单次结果逐字节一致。

## 5. 检查计数

流状态机每输入字节只检查一次（缓存的是「合法前缀」状态而非原始字节重扫），
`checks ≤ 2n`（限额、统计等每字节至多再计一次）。par 额外成本仅为 8 段、每段
至多 3 字节回看与跨段单元的一次复查，`checks ≤ n + 8C`，C 为常数。

## 6. 并发与安全

单个 stream.Transcoder 非并发安全；par 内每段一个独立实例，K 个 goroutine
无共享可变状态，按段序拼接。
