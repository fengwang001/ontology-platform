# Design: Multi-tenant Weighted-Fair Queue Scheduler (ontology)

## 1. 模型

任务 = (租户 ID, 代价 c>=0, 全局提交序号 seq)。每租户一个 FIFO 队列，权重 w>0。
调度器在非空租户中选虚拟时间 vt 最小者，执行其队首任务，然后
`vt += c_eff / w`，其中 `c_eff = max(c, minCost)`，`minCost = 1e-9`。
虚拟时间用 float64（见 §5）。

## 2. 为什么增量是 c/w 而不是 c*w

公平目标：每个租户分到的实际处理速率 ∝ 权重。连续流体模型下，租户 i 已获
服务量 S_i，其虚拟时间定义为 vt_i = S_i / w_i。若两租户都持续非空，公平
意味着 vt_i 始终相等（落后者立即被优先），故 S_i / w_i = S_j / w_j，即
S_i : S_j = w_i : w_j，份额严格正比于权重。执行代价 c 后 S_i += c，所以
`vt += c/w`。若误用 c*w，权重越大 vt 涨得越快、越被推迟，份额变成反比，
方向完全错误。离散步度下，vt 较小者总是先跑，长期份额收敛到 w_i/Σw。

## 3. 空闲租户重新加入：vt 抬升

不抬升：X 空闲期间 Y 一直被服务，vt_Y 涨到约 N*c/w_Y，而 vt_X 停在旧值。
X 重新提交后它的 vt 最小，会**连续独占**直到 vt_X 追上 vt_Y；1:1 等代价
时 X 要独占约 1000 步（Y 刚跑的 1000 步被“补账”），Y 实际被饿死同样久。

正确规则：队列从空转非空（新租户首次到达 / 老租户新任务到达）时
`vt_i = max(vt_i, V)`，V = 当前所有非空租户 vt 的最小值（即“系统虚拟
时间”/基准）。含义：空闲期间不累积任何欠账，也不保留过时优势；回来即从
当前基准竞争。抬升后 X 至多经过 w_Y:w_X 量级的几步就会再次被选中，之后
X、Y 按权重交替，谁都不会被饿死。首次出现的租户以 V 为起点。

## 4. 并列与队列语义

vt 相等时按租户 ID 字典序选择，保证可复现；同一租户内 FIFO，seq 只做
观测/去重，不参与跨租户比较。堆只放非空租户，pop 后队列空则出堆，故
选择比较次数只与非空租户数有关：≤ 4*ceil(log2(n))（container/heap 单次
Pop 路径上浮+下沉，计数器逐条比较累计）。

## 5. 零代价、权重边界与数值

零代价合法，但 c/w=0 会让该租户 vt 永不前进而无限独占。处理：计费代价
`c_eff = max(c, 1e-9)`，即零代价也推进一个最小时间片，vt 前进极小但
严格为正，其他租户很快追上，零代价任务不能无限连发（它仍占一次调度）。
权重 w<=0 在注册时拒绝（ErrBadWeight）。采用 float64 而非有理数：比较
与除法直接、堆键简单；溢出/精度靠输入约束防护——w 限制在
[1e-9, 1e12]，c 限制在 [0, 1e9]，则 c_eff/w ∈ [1e-21, 1e18]，且单次
调度推进后 vt 仍远小于 float64 溢出阈值 1.8e308；极小增量相对大 vt 的
加法吸收由“抬升到当前基准、同基准租户 vt 相近”的结构缓解。极大/极小
权重并存的测试还断言所有 vt 始终 finite。

## 6. 背压、移除与错误

每租户队列上限 Q：超限返回 ErrQueueFull，任务不入队，状态不变，其他租户
不受影响。租户不存在/已移除时提交返回 ErrNoTenant（ErrQueueFull、
ErrBadWeight、ErrNoTenant、ErrBadCost 四类错误互相可用 errors.Is 区分）。
移除仍有排队任务的租户：明确拒绝，返回 ErrTenantBusy（可用 errors.Is
判为 ErrNoTenant 一族），调用方必须先排空；移除空租户立即生效。

## 7. 并发与确定性

sched 用一把互斥锁保护堆与账本；admit 在其上串行化入队。提交顺序定义为
“获取入队锁的串行化顺序”：拿到锁即分配全局 seq 并入队，故不同 goroutine
的实际先后以该锁顺序为准；给定同一串行化序列，调度输出逐元素确定。
8 消费者 * 10 万任务下每任务恰好出队一次（出队与移除同锁，不丢不重）。
统计包只记 executed[c]、execCost[c] 与总代价，份额 = execCost/总代价。
