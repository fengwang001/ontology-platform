# 自然排序比较器设计

## 比较规则（严格分层）

把字符串从左到右扫描成交替的「数字段 / 非数字段」，每次取两串当前段比较：

1. 一边已结束：结束的更小（短前缀小，故 `x < x1`）。
2. 段类型不同：比较两段首字节的字节序（数字字节 0x30–0x39 与其他字节天然可分）。
3. 都是非数字段：按字节序比较；相同则消费两段，继续。
4. 都是数字段：比较**数值**但绝不转整数——去掉前导零后，先比长度再按字节比（短lex序）；
   数值不同即定胜负。数值相同：记下该段「前导零个数」`p = 原长 - 去零后长度`，
   **先不比 `p`**，继续向后比较。
5. 仅当整串主比较（1–4）全部相等、而两串并非字节相同：按数字段出现顺序，
   比较首个 `p` 不同的段，`p` 小者更小。

## 前导零为何必须推迟（第 3 条推导）

若数值相等就「立刻」比前导零个数，则在数字段处 `a01` 已被判为大于 `a1`，
那么后续 `b` 与 `c` 的差异永远没有机会参与比较，必然得到 `a01b > a1c`，
与题设 `a01b < a1c` 矛盾。所以前导零只能作为**主序全部相等之后的末位 tie-break**，
且按段位置做字典序比较，而不是在某一段就地决定。

演算 `a01b` vs `a1c`：

- 段 `a` = `a`，继续；数字段 `01` 与 `1` 数值相等，记录 `p=1` 与 `p=0`，继续；
- 非数字段 `b` vs `c`，字节序 `b<c`，判定 `a01b < a1c`（`p` 未被使用）。

演算 `a1b01` vs `a01b1`：

- `a`=`a`；数字 `1` vs `01` 数值相等，记录 `(p=0)` vs `(p=1)`，继续；
- `b`=`b`；数字 `01` vs `1` 数值相等，追加 `(0,1)` vs `(1,0)`；
- 两串同时结束、主比较全等且串不同：逐段比 `p`，首位 `0<1`，
  判定 `a1b01 < a01b1`。

其余：`a1<a01<a001` 为 `p=0<1<2`；`x0,x00,x000,x1` 中前三数值同为 0 由
`p=0<1<2` 排序，而 `x1` 数值为 1，故 `x000 < x1`。

## 传递性与全序（第 4 条）

主比较是标准字典序：段序列来自一个全序「令牌字母表」——非数字段按字节序、
数字段按去零串的短lex序、串结束符小于任何段、异类型按首字节。故主序在
「主等价类」上是严格全序。同一主等价类内再以 `p` 向量做字典序（自然数上的严格
全序）。两个全序做字典序乘积仍是严格全序，故反对称、传递；同类且 `p` 向量全等
当且仅当两串字节相同，故 `Compare==0 iff a==b`。

## 非 ASCII（第 6 条）

仅 0x30–0x39 为数字；其余字节（含 UTF-8 续字节、全角 `１２`）一律进非数字段按
字节序处理，不查表不解码故不 panic。上述证明只依赖字节取值与确定性分段，结论不变。

## 复杂度与计数器（第三节）

流式游标比较，不预切段：每个字节最多被「数字判定/消费」扫描两次，计数器只统计
实际推进的字节数，故计数 ≤ 2(len(a)+len(b))。

## 语义与测试对照表

| 语义 | 钉住它的测试函数 |
| --- | --- |
| （末步补齐） | |
