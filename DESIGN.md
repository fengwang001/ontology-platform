# 无限制转置编辑距离：设计说明

## 1. 递推式与推导（核心）

用 D[i][j] 表示 a[:i]、b[:j] 的距离（按码点），D[0][j]=j，D[i][0]=i。

本文采用的递推（Lowrance-Wagner，转置后允许继续编辑）：

记 da = 字符 b[j] 在 a 中上一次出现的行 i'（无则 0）；
db = 字符 a[i] 在 b 中上一次出现的列 j'（无则 0）。

```
D[i][j] = min(
  D[i-1][j-1] + [a[i]!=b[j]],                  // 匹配 / 替换
  D[i-1][j] + 1,                                // 删除
  D[i][j-1] + 1,                                // 插入
  D[da-1][db-1] + (i-da-1) + 1 + (j-db-1)      // 转置
)
```
末式含义：删掉 a[da+1..i-1]（i-da-1 次），转置匹配 b[db]、b[j]（1 次），
再补出 b[db+1..j-1]（j-db-1 次）；转置的两个字符此前可被编辑过，故"无限制"。

常见错误递推（OSA/最优串对齐）只写
D[i][j] = min(..., D[i-2][j-2]+1 当 a[i-1]==b[j] 且 a[i]==b[j-1])，
把转置写死在"同一格、原串相邻的两位"，转置后该段冻结，禁止再编辑。

逐格差异，CA -> ABC（i 行=CA，j 列=ABC）：

| D | A | B | C |
|---|---|---|---|
| C | 1 | 2 | 2 |
| A | 2 | 2 | 2 |

正确路径：CA 转置成 AC（1），在 A、C 之间插入 B 得 ABC（1），共 2。
在 D[2][3] 处：a[i]=A=b[1] 且 b[j]=C=a[1]，da=1、db=1，
转置项 = D[0][0]+(2-1-1)+1+(3-1-1)=0+0+1+1=2。
OSA 在同格要求 a[i-1]==b[j]（C==C）与 a[i]==b[j-1]（A==B），
第二条件失败，只能走删/插/替换，得 3。
根源：OSA 的转置必须紧贴当前格末端，无法表达"先转置、再在中间插入 B"。

## 5. 并列时的固定选择规则（回溯方向）

同一格有多个等长前驱时，按下列优先级唯一选定：
匹配/替换 > 删除 > 插入 > 转置。
因此对同一对输入，100 次计算逐项相同。

## 6. 语义 - 测试函数对照表

| 语义 | 测试函数 |
|---|---|
| 1 转置距离样例（含 CA->ABC） | TestTranspositionSamples |
| 2 按码点 / 非法字节区分 | TestRuneAndInvalidBytes |
| 3 对称 / 同一 / 三角不等式 | TestMetricProperties |
| 4 脚本长度=距离且 Apply 还原 | TestScriptRoundTrip |
| 5 确定性 | TestDeterministic |
| 6 超限立即报错（不分配矩阵） | TestLimitError |
| 复杂度：计数器 <= 2(m+1)(n+1) | TestCellCounter |
