# DESIGN

## 1. 空行与单列空值
空行（连续两个行尾）选择**跳过**，不产生记录：它在文本编辑中只是行间隔；若当记录，则 `a\n\nb\n` 会凭空多出一行。因此解析器在「字段开始态且字段从未写入、行首」遇到行尾时直接丢弃。
后果：单列表里「值为空的记录」无法写成空字节串（那是空行/空流，会被跳过），**必须写成引号空字段 `""`**。故回写器规则：记录为单列且其唯一字段值为空时强制加引号。引号标记被保留：`""`（quoted）往返仍是 `""`；未引号空字段只可能出现在多列记录里。
规范输入定义：`\n` 行尾、仅必要时加引号（值含 `,` `"` `\r` `\n`，或单列空值）、每条记录（含末条）后有 `\n`、无空行。则 `Write(Parse(x))==x` 逐字节成立。

## 2. par 切点
按近似等长切成 K 段，切点任意。每段 worker 各跑**两遍** lexer：假设 A=段首在引号外字段开始态、假设 B=段首在引号内态。段 0 以 A 为真。从左到右：前段选定轨迹的**段末状态**若在引号内，本段取 B，否则取 A。
关键：lexer 不在引号边界停留——引号字段内见 `"` 时立即预读下一字节：`"`→输出一个引号留在引号内；其他→闭合。预读字节留在缓冲区不消费，半包时该字节再次到达，结果不变。故不存在「停在引号上」的切点，段末只可能是「引号内/外」二态，双假设恰好覆盖。
段内事件的字节偏移加段基址即全局偏移；记录号、字段号=全局已完成记录/字段数+段内序号。错误的三坐标（偏移、记录、字段）同样加基换算。
每字节最多被两个假设各处理一次，故总处理次数 ≤ 2N+O(K)。

## 3. CR 待定
引号外见 `\r` 不能立刻判定：其后可能是 `\n`（行尾）或其他（错误 ErrBareCR）。lexer 设态 sCR：**不把 `\r` 写入值**，暂停判定。下一字节：`\n`→行尾；EOF→ErrBareCR；其他→ErrBareCR（错误偏移指回该 `\r`）。半包时 `\r` 留在状态里，跨 `Feed` 与跨 par 切点语义一致（par 段末 sCR 等价「引号外」，下段 A 轨迹以 sCR 为入口态，用一次性入口槽注入，不需要第三种完整假设）。引号内 `\r` 一律是内容原样保留。

## 4. 上限「立刻」
字段字节计数在「值将被追加」前自增：每追加一个内容字节前检查 MaxFieldBytes。引号内 `""` 在见到第二个 `"` 时只追加一个 `"`，故仅计 1——第一个超限字节到达即返回 ErrFieldTooLong，不缓冲整行。MaxFields 在每开一个新字段（含行首首字段）前检查；MaxRecords 在每条记录完成（空白跳过行不计）前检查。出错即终态，记录同一错误，完整记录已全部交付。

## 5. 状态转移表
状态：F=字段开始，B=未引号字段中，Q=引号字段中，C=CR 待定。类别：`,` `\n` `\r` `"` 其他 o，EOF。

| 状态 | `,` | `\n` | `\r` | `"` | o | EOF |
|---|---|---|---|---|---|---|
| F | 交空字段→F | 空白跳过→F | →C | →Q | 追加→B | 无事件 |
| B | 交字段→F | 交记录→F | →C | ErrQuoteInBare | 追加→B | 交记录(末无换行) |
| Q | 追加 | 追加 | 追加 | 预读:`"`追加留Q / 否则→F | 追加→Q | ErrUnclosedQuote |
| C | ErrCharsAfterCR | 行尾→F | ErrCharsAfterCR（指回前 `\r`） | ErrCharsAfterCR | ErrCharsAfterCR | ErrBareCR |

Q 闭合后（回到 F 且字段已交付）再遇 `"` 或 o → ErrCharsAfterQuote；`,`/`\n`/`\r` 正常。Q 行 `"` 列预读：`"` 后无字节（半包或 EOF）时暂停于「引号内待读」，下一字节到达再判，EOF 终判为闭合。
