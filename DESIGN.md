# DESIGN.md

## 1. 可见性边界：为什么是左闭右开

快照隔离要求读事务在开始时刻 `Begin()` 冻结一个数据库状态。设注入的事务号源单调递增（不回绕、零值非法），事务号即全序提交序。建立快照 S 时：

- `point` = 分配器**即将发出的下一个**事务号（即"当前已用最大号 + 1"）；
- `active` = 建立瞬间尚未提交/回滚的写事务号集合。

版本 v 由提交事务号 `ct` 标记。冻结语义要求：S 只能看到"S 建立那一刻已提交"的版本。

- 若 `ct < point`：ct 在 S 建立前就已分配；但若 `ct ∈ active`，说明 S 建立时它还没提交，必须排除。
- 若 `ct >= point`：该事务在 S 建立之后才可能提交，无论物理提交何时发生，一律不可见。

所以规则是 `ct < point && ct ∉ active`，即 `[1, point)` 左闭右开。若误用闭区间 `ct <= point`，恰好在快照点的提交（其号等于 point）会泄漏进一个本应冻结在过去的快照，破坏可重复读——快照建立后该版本才提交，却能被读到，第 4 条边界用例专门钉死这一点。未提交版本没有 ct（用事务私有号标记且必在 active/未来区间），天然不可见；写事务读自己的链时走"读己之写"旁路：在普通可见判定之上，额外放行 `ct == 自己 txid` 的未提交版本。回滚只须把私有版本摘除，它从未获得 ct，不会进入任何已提交链，故无残留、回收器也不可能误判。

删除就是追加一个 `deleted=true` 的版本，可见性规则完全相同。查询结果因此有三态：链上无可见普通版本但可见版本是删除标记 = Deleted；键从未有过版本 = NotFound；命中普通版本 = Value。

## 2. 回收安全条件与增量数据结构

版本 v 可回收 ⟺ 两个条件同时成立：

1. **遮蔽**：同一键上存在比 v 更新的版本 v'，且 v' 对"所有还可能看到 v 的快照"都已可见。此时没有任何快照的查询结果还依赖 v。
2. **水位**：所有活跃（已建立、未关闭）快照 point 的最小值记为 `horizon`。快照 point 单调，故某快照能看到的最大号 `< horizon`。当遮蔽版本 v' 的提交号 `ct' < horizon` 时，每个现存快照都已能越过 v'，v 被完全遮蔽；水位只升不降，未来快照 point 更大，也不会回头要 v。

推导：若 `ct' >= horizon`，存在 point <= ct' 的活跃快照，它看不到 v' 却可能需要 v，回收即改变其读结果——故必须等 `ct' < horizon`。最新版本永远没有更新的遮蔽者，因此永不回收；删除标记作为最新版本时同样保留（Deleted 判定还要靠它）。

**增量结构（不全表扫描）**：

- 每个键一条已提交版本链（追加式，旧→新）。链维护候选下标 `gate`，指向下一个待考察的旧版本；被回收版本只推进该下标，不搬动后继。
- 回收器持有一个**每键候选最小堆**：堆项 = `(该键链首候选版本的 ct, key)`。每键只入堆一次（入堆集去重），之后推进只改堆项键值。
- `Advance(horizon)` 反复取堆顶最小 ct：若 `ct < horizon`，考察该键——沿 `gate` 回收所有"其后继提交号也 < horizon"的连续旧版本（连续遮蔽，一次截断一段），考察数 = 实际触碰的版本数（含决定停在最新版本/未越界版本的那一次），计入非导出字段 `inspected`；若该键仍剩 ≥2 个版本，用新候选 ct 更新堆顶，否则该键永无可回收物，直接出堆、不再入堆。堆顶 `ct >= horizon` 时立即停止（堆有序，其余键候选都更大）。

**最坏考察数上界**：一次 `Advance` 考察数 = 回收掉的版本数 + 被更新进堆的键数，二者都只属于"确有进展"的键，与总键数 N 无关。N=100 与 N=10000 而可回收键数相同的两组场景，考察数相等（不随 N 线性增长）。全程总考察数 = 版本生死次数 + 键入堆次数 = O(总版本数)，摊还 O(1)/版本。

## 3. 崩溃点枚举

一次写入的有序阶段为：a) 登记活跃事务 → b) 追加私有版本体（挂在事务未提交缓冲）→ c) 提交：临界区内给版本盖 ct、原子拼上键的已提交链、从事务表摘除 → d) 回滚：摘除事务并丢弃缓冲。注入钩子 `CrashAt` 在每点之后抛错模拟进程中止；重启 = 用同一份只追加内存 WAL（注入源，非真实文件系统）重放构造新 Store：

1. Begin 记录后崩溃：无键版本，重启后事务不存在 → 完全不可见。
2. 版本体记录后、commit 记录前崩溃：链未拼接，版本只属未决事务；重放时该事务无 commit 记录，按回滚丢弃 → 不可见，无悬空槽位。
3. commit 记录写完瞬间崩溃：重放看到 commit，盖戳拼接一次（按记录序号幂等）→ 完全可见。
4. 提交临界区之后崩溃：同 3，完全可见。

关键点："版本体挂链"在重放中由同一条 commit 记录驱动、单次完成；只有版本体没有 commit 的状态一律按未决丢弃。因此不存在"槽位指向没有版本体"或反之的半可见态。测试用循环遍历全部点，重启后断言每个键要么读到新值要么读旧值/不存在。

## 4. 水位推进与新快照的竞态

`Begin` 与 `Advance`（取 horizon、推进 gate）争用**同一把 store 互斥锁**，临界区边界：

- `Begin`：锁内读分配器当前值定 point、拷贝活跃集合、把新快照登记进活跃快照表，然后释放锁。快照登记完成前，其 point 不计入任何 horizon。
- 回收触发：锁内收集所有已登记快照 point 的最小值算 horizon，随后调用 `Advance(horizon)` 推进/回收，全程持锁。

反序 interleaving 逐一排除：若新快照先登记——它的 point 必进入 horizon，`ct' < horizon` 立刻挡住任何它看不到的遮蔽版本；若回收先跑完——新快照建立时被回收版本早已不在链上，而它的 point 更大，本来也只该看到更新版本，读结果与"版本从未被回收"一致。水位 `watermark` 只在新值更大时更新，故只升不降；回滚、超限拒绝都不碰水位。快照关闭移出集合只可能抬高 horizon。长事务只短暂持锁（版本选择是无锁纯计算），不阻塞其他键读写。

## 5. 资源上限与只读查询

三类上限在**任何状态变更之前**判定并返回彼此不同的哨兵错误：`ErrChainTooLong`（单链将超 `MaxChain`）、`ErrSnapshotsExhausted`（活跃快照将超 `MaxSnapshots`）、`ErrVersionsExhausted`（总版本将超 `MaxVersions`）。拒绝路径不分配号、不挂版本、不动水位与计数器。`Stats`（键可见版本数、活跃快照数、水位、总版本数）为锁内纯读，不改状态，连查两次一致；已回收或从未存在的键可见版本数为 0。时间与事务号只走注入源；不调用 `time.Now`，不碰文件系统、不联网。
