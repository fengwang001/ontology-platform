# 带转义的 RLE 编解码器设计说明

## 严格解码拒绝清单推导（不变量：Encode(Decode(t)) == t）

- 显式次数 1：`1a` 若接受，Decode 得 `a`，Encode(`a`) 得 `a` ≠ `1a`。次数 1 必须省略。
- 次数 0：`0a` 若接受，只能解为空串，Encode(空) 得空 ≠ `0a`。
- 前导零：`01a` 若接受，Decode 得 `a`，Encode 得 `a` ≠ `01a`。十进制必须无前导零。
- 相邻同符号游程：`2a3a` 若接受，Decode 得 `aaaaa`，Encode 得 `5a` ≠ `2a3a`；`aa` 同理应为 `2a`。游程必须最长。
- `\` 后非数字非反斜杠：`\q` 无语法意义；若按字面 `q` 解，Encode(`q`) 得 `q` ≠ `\q`。
- 末尾孤立 `\`：`a\` 若把 `\` 当字面反斜杠，Decode 得 `a\`，Encode 得 `a\\` ≠ `a\`。
- 末尾只有次数：`2` 若忽略次数解为空串，Encode(空) 得空 ≠ `2`。次数必须配符号。
- 非法 UTF-8：码点序列无定义，Encode 作用于码点，无法保证往返，解码器逐字节校验。

每类错误是可判定的哨兵错误（errors.Is），并携带字节偏移（*Error.Off）；
次数语法三类（前导零/零/一）的哨兵定义在 runs 包，其余在 rle 包。

## 大次数取舍：支持，不设次数上限

次数用 math/big 解析，任意位数不溢出（如 `99999999999999999999a`）。
展开前用 big 比较「次数 × 符号字节宽」与「剩余输出配额」，超限即报
ErrTooLarge，绝不按次数预分配内存。输出总字节上限可配置：
Decode 内置上限 1<<30，NewDecoder(limit) 自定义。

## 流式一致性

解码器逐字节消费输入：非导出计数器 checked 记录消费的字节数，
不完整 UTF-8 序列缓冲至多 4 字节，整体单遍 O(n)，与切分方式无关；
相邻同符号的判定在「一个游程完成时」进行，故 `2a|3a` 任意切分一致。

## 测试对照表

| 语义 | 钉住它的测试 |
|---|---|
| 1 往返 | TestRoundTrip（固定表 + 随机循环）、TestCanonical |
| 2 规范形式/严格解码 | TestReject（各类错误+偏移）、TestEncode |
| 3 大次数 | TestBigCount |
| 4 最长游程/不规范化 | TestEncode、TestCombining |
| 5 跨切分一致 | TestAllSplits（含 1 字节喂入与 `2a3a`） |
| 计数器（第四节） | TestCheckedCounter |
