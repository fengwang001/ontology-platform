# 设计：只追加日志的段索引与范围回放器

模块 `ontology`，仅标准库。日志与索引写本地临时目录。包划分：`event`（事件编解码）、
`segment`（段追加写/顺序读）、`sparse`（稀疏索引）、`replay`（范围回放）、`repair`（检测与修复）、
`cmd/demo`（演示判定）。

## 一、磁盘格式

事件体（event）：`seq uint64 BE ‖ payload`，即 8 字节序号 + 原始载荷，空载荷合法（体长 8）。

段文件（segment）：

```
header: magic "OSEG"(4) ‖ firstSeq uint64(8) ‖ count uint64(8)      = 20 字节
record: len uint32(4) ‖ body(len 字节) ‖ crc32-IEEE(body)(4)
```

- 头自描述：`firstSeq` 为本段首事件序号，`count` 为本段事件数；相邻段必须满足
  `firstSeq + count == 下一段 firstSeq`（序号连续，repair 负责校验缺口）。
- 每条记录长度前缀 + CRC32，截断/篡改可按「头不完整 / 长度前缀不完整 / 事件体不完整 /
  CRC 不匹配」四类判定（对应哨兵错误，`errors.Is` 可区分；另有「长度前缀非法」用于检出
  指向事件中间的篡改偏移）。
- 写者单实例 + 互斥锁，记录一次性 `WriteAt` 落盘后再回头改头里的 `count`。
  读者先读 `count` 再恰好读 `count` 条记录 ⇒ 永远只看到一个一致前缀，读不到半条事件。

索引文件（sparse）：`magic "OSIX"(4) ‖ interval N(8) ‖ firstSeq(8) ‖ 锚点数(8) ‖
锚点[seq uint64 ‖ offset uint64]…`，每 N 条记一个「序号 → 段内字节偏移」锚点
（含首事件，偏移 20）。

## 二、定位规则推导：为什么是「不大于 from 的最大锚点」

设锚点序号集合 A 单调递增。回放 `[from, to]` 需从某个偏移开始顺序扫描，扫描中跳过
`seq < from` 的事件。正确性要求：扫描起点的事件序号 `s ≤ from`，否则 `s` 与 `from` 之间
的事件被漏发；效率要求：`s` 尽量大以少扫。

- 候选规则「最接近 from 的锚点」会选中 `s > from` 的锚点（例如锚点在 0、128，from=100，
  最近锚点是 128）。从 128 开始扫，序号 [100,127] 的事件永远漏掉 ⇒ 错误。
- 「不大于 from 的最大锚点」`a* = max{a ∈ A : a ≤ from}` 必满足 `a* ≤ from`，不漏；
  且下一锚点 `> from`，故跳过数 `from - a* < N`，有严格上界。
- 若 `from` 小于首锚点（即 `from < firstSeq`），不存在 `≤ from` 的锚点，回退为从段首
  （偏移 20）扫；此时段内所有事件序号都 `≥ firstSeq > from`，跳过数为 0。

## 三、索引可丢弃、可从段逐字节重建

索引里每个字段的来源：锚点 `(seq, offset)` 全部可由「从头顺序扫段文件」观察到——
`seq` 在事件体里，`offset` 是扫描游标；`firstSeq` 在段头；`interval N` 是日志配置参数，
重建时由调用方显式传入（不是写入时才知道的秘密）。因此索引是纯加速结构：
删掉 `.idx` 后用同一 N 重扫段文件，得到的字节流与原索引逐字节相同。测试断言这一点，
它同时禁止实现把任何只存在于内存的信息塞进索引。

## 四、索引失效时的回退策略

定位时先按索引锚点跳转，然后在锚点偏移处试读一条记录并校验：长度前缀合法
（`8 ≤ len ≤ 上限` 且不越出文件）且 CRC 匹配且解出的 `seq` 等于锚点序号。任一不满足
即判定索引已失效：标注 `IndexInvalid`，丢弃索引，回退到从段首全段扫描，结果仍然正确。
索引文件缺失时同样回退全段扫描（不算失效，只是无加速）。

## 五、跨段回放

段按文件名序排列，各自范围 `[firstSeq, firstSeq+count)`。范围 `[from, to]` 与每段求交，
交非空则在该段内定位并回放交集部分，段间不重不漏；序号连续性由 repair 的缺口检查保证。

## 六、边界语义（文档定义，测试锁定）

- 空日志（无段）：返回空结果，不报错。
- 空段（count=0）：跳过，不报错。
- `from > to`：可判定错误 `replay.ErrFromGreaterThanTo`。
- `from` 小于最小序号：**从最小序号开始回放**（钳位），不报错。
- `to` 超过最大序号：回放到末尾为止，不报错。
- 区间恰好等于一整段、跨三段、空载荷：均为正常路径。

## 七、复杂度上界（可证明、有测试）

- 定位跳过事件数 `= from - a* < N`（N 为锚点间隔），replay 用非导出计数器记录并断言。
- 回放读取字节数 ≤ 区间事件编码长度之和 + 一个锚点区间（定位最多多扫 N-1 条）。
- 截断分类：设记录定长 R=4+(8+P)+4，头 20 字节。截断点 t<20 为头不完整；
  否则令 r=(t-20) mod R：r∈[0,3] 长度前缀不完整，r∈[4,11+P] 事件体不完整，
  r∈[12+P,15+P] CRC 不匹配。
