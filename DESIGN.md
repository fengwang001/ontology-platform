# CSV 方言解析器设计

## 1. 总体

五个包，依赖单向：`cell` ← `lexer` ← `table` ← `par`；`writer` ← `cell`。
lexer 是可暂停/续传的逐字节状态机，通过 Sink 回调吐事件；table 实现 Sink 组装记录；
par 把缓冲区按任意偏移切段，每段在两种起点假设下各跑一遍，再从左到右定真状态并拼接。

## 2. 空行与单列空值（推导）

RFC4180 下，换行本身是记录终止符而非记录分隔符：文件 `a\n` 只有一条记录。
因此 `a\n\n` 中第二个 `\n` 终止的记录字段数为 0——0 字段不能构成记录，故**空行一律跳过**，
不产出记录。于是「单列表中一条值为空的记录」只能写成 `""\n`：空行已被空记录占用并跳过，
不加引号就无法与空行区分，回写器会丢一条记录。结论：

- 空行（相邻换行，含流首换行、文件末尾换行）跳过。
- 单列、唯一字段为空且未加引号时，回写器必须写成 `""`。
- 字段含 `,` `"` `\r` `\n` 必须加引号；已标记 Quoted 的字段按原样加引号（保标记往返）。

「规范输入」定义：引号字段内 `\r\n`/`\n` 原样；无孤立 `\r`；每条记录以 `\n` 结束；
无冗余引号（只有上述必要情形才加引号）；无空行；文件以末尾 `\n` 结束。

## 3. par 切点（推导）

切在引号字段内时，下一段 worker 仅凭本段字节无法判断自己是否在引号内。方案：每段
用「起点在引号外」(A) 与「起点在引号内」(B) 两个 lexer 各解析一遍，各得事件流、段末状态、
错误。段内引号成对：段内 `"` 数为偶数时 A 末在引号外、B 末在引号内，奇数则相反。
拼接从左向右：首段取 A；前一段选定流若在引号内结束，本段取 B，否则取 A。B 的 lexer 以
「引号字段中段」状态启动（cell 已开始，首字节不发 CellStart）。被放弃的假设整段丢弃，
故每个非首段字节只被选定的那遍计入最终序列，总量 ≤ 2N + O(K)。段末处于「CR 待定」时，
worker 额外窥视下一段首字节（该字节只窥视不消费）：是 `\n` 则记录终止，否则报孤立 `\r`。
错误全局坐标 = 段基址 + 段内偏移；记录号 = 此前各选定段完整记录数 + 段内记录号；
字段号同理，跨段延续的字段在两段内字段号相同。错误一旦在某段选定流出现，该段成为最后
被采用的段，后续段不参与，故错误即全局首个错误。

## 4. CR 待定（推导）

未引号字段中 `\r` 后只能是 `\n`（行尾）才合法，其余（含 EOF）都是孤立 `\r`。
故见到 `\r` 进入 crPending 状态，不计数为字段内容、不结束记录：

- 半包续传：`\r` 后本批结束，状态机挂起；下批首字节若是 `\n` 结束记录，否则在该字节
  偏移处报 ErrLoneCR（流结束时在 `\r` 偏移报错）。
- par 切点：如上节，worker 窥视下段首字节，切点落在 `\r\n` 中间也能就地定案。
- 引号字段内没有该状态：`\r` 永远是内容（与下字节是否为 `\n` 无关）。

## 5. 上限的「立刻」（推导）

字段字节计数：未引号字段每个内容字节 +1；引号字段中，普通字节 +1，转义对 `""` 在见到
第二个 `"` 时 +1（一个引号字符）。计数发生在字节处理时，第 (MaxFieldBytes+1) 个计数字节
到达即返回 ErrFieldTooLong，不缓冲、不等待闭合。逗号分隔符触发 MaxFieldsPerRecord，
记录完成触发 MaxRecords；命中后进入终态，后续 Feed/Close 返回同一错误。

## 6. 状态转移表

状态：S=字段开始 U=未引号中 Q=引号中 A=引号内刚见引号 C=CR待定。
类别：c=普通字节 , `,` q=`"` l=`\n` r=`\r`；EOF 列。

| 状态 | c | , | q | l | r | EOF |
|---|---|---|---|---|---|---|
| S | →U 数据 | →S 末/首 | →Q 首引号 | →S 末记录 | →C | 末记录(若非流首) |
| U | →U 数据 | →S 末/首 | ErrQuote 偏移 | →S 末记录 | →C | 末记录 |
| Q | →Q 数据 | →Q 数据 | →A | →Q 数据 | →Q 数据 | ErrUnclosedQuote |
| A | →Q 数据(1字节) | →S 末/首 | →Q 数据 | →S 末记录 | ErrLoneCR | 末记录 |
| C | ErrLoneCR | ErrLoneCR | ErrLoneCR | →S 末记录 | ErrLoneCR | ErrLoneCR |

S 行 l 的「末记录」只在本记录已有 ≥1 字段时产出，否则跳过空行。错误分别为
ErrQuoteInBare、ErrCharsAfterQuote（A 态见 c 或 r）、ErrUnclosedQuote、ErrLoneCR；
列数不符为 ErrFieldCount，在记录完成时报。

## 7. 其他约束

单流式 Parser 实例**非并发安全**（多实例可并行）；par 内部 K goroutine 并发且无共享可变状态，
可通过 race。字段偏移为半开区间 [Start,End)（含引号时包含包裹引号），全局字节坐标。
