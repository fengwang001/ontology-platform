# 带撤回的增量聚合视图维护器 — 设计

包：`change`（变更编解码）、`agg`（聚合器族）、`journal`（日志/重放）、
`view`（分组视图与三阶段编排）、`audit`（全量核对）、`cmd/demo`。
仅标准库，状态在内存，日志在调用方指定的临时目录文件。

## 1. 撤回能力推导

设组 G 状态由成员多重集 M 导出。删除记录 x 后 M'=M\\{x}。

- Count：f(M)=|M|，f(M')=f(M)-1，只需计数本身 → 可增量减。
- Sum：f(M)=Σv，f(M')=f(M)-v(x)，只需被删值 → 可增量减
  （浮点按位回放，插入与删除使用同一 IEEE754 值，减法精确抵消；
  +0/-0 视为同一键值：成员表不存符号零差异，比较用值相等，+0==-0）。
- Min：若 v(x)≠当前 min，则 min 不变；若 v(x)==min，次小值不在
  聚合状态里（单值状态无法反推），必须遍历 M' 取最小值 → 删除需成员。
- Max：与 Min 对称，删到当前 max 必须重算。
- DistinctCount：删 x 后该值是否仍有其他持有者，单计数无法回答，
  需要「值 → 出现次数」的成员级信息 → 删除需成员（按成员表重算）。

因此每个聚合器显式声明 `NeedsMembersOnDelete bool`：
Count/Sum 为 false；Min/Max/DistinctCount 为 true。

## 2. 三阶段编排（Apply → Recompute → Commit）

对一条变更：
1. Apply：加锁后做版本判定，按操作改成员表；可增量聚合即时更新。
2. Recompute：仅当聚合器声明需要成员且删除命中条件
   （Min/Max 命中当前极值；DistinctCount 删的是某值最后一个持有者），
   遍历**该组当前成员**整体重算该聚合。重算是例外；非导出计数器
   `recomputeCount`（触发次数）与 `recomputeTouched`（访问成员数）记录代价。
3. Commit：更新 lastVersion、释放锁。更新操作先从旧组撤回再插入新组，
   两组成员表在同一次加锁内完成，读端看不到半更新。

组成员删空即整体删除；查询不存在返回 (零值,false)，不留 Count=0 空组。

## 3. 版本与幂等

变更带严格单调版本号 v。
- v<lastVersion：回退，返回 ErrVersionBackward（可 `errors.Is` 判定），不计入。
- v==lastVersion：视为重复投递，整体幂等跳过，视图逐字段不变，不算拒绝。
- v>lastVersion：正常应用，提交后 lastVersion=v。
乱序（非递增）属于业务拒绝：计入 `rejected`（连同空分组键、NaN 值），
返回可判定错误，视图不被污染。

## 4. 日志格式、截断分类与恢复

文件 = 头 + 若干帧。头 = `"ONTV1\n"`（7 字节）。
帧 = 4 字节大端载荷长度 n + 载荷 n 字节 + 4 字节 CRC32(IEEE) of 载荷。
载荷 = change 的二进制编码（自描述 op：1 字节操作；
Insert/Delete：8B 版本、2B 组键长+键、8B float64 值；
Update 再附 2B 新键长+新键）。

逐字节截断（文件总长 L，已提交前缀长度 p）分类：
- L<7：头不完整 ErrShortHeader（p=0）。
- 帧边界处：n 字段可读但剩余 <4：长度前缀不完整 ErrShortLen（按 n 声明判定）。
- 4≤剩余<n：记录体不完整 ErrShortRecord。
- 恰好读出 n 字节载荷但 CRC 4 字节不全：CRC 不完整按 ErrCRC 处理
  （无法校验的尾帧视同损坏）。
- 载荷齐但 CRC≠计算值：ErrCRC。
重放在第一个坏帧处停止，返回已完整帧数量与分类错误；坏帧绝不生效。
空文件（L=0）视为无日志、无错误。

崩溃恢复：三阶段每个崩溃点（Apply 写一半 / Recompute 中途 / Commit 前）
都在状态落盘前被日志边界截断，恢复结果 = 日志完整前缀的重放结果，
与不崩溃路径逐字段相同。

## 5. 并发与边界

`View` 用单一互斥锁串行化所有变更；查询在锁内复制快照返回。
Recompute 在锁内遍历同组成员，故并发写入不会与重算交错丢失。
边界：空视图查询返回不存在；空串 `""` 是合法分组键；缺失（nil）键拒绝；
NaN 用 math.IsNaN 拒绝；所有记录同组正常；跨组更新两组聚合同锁变更。

## 6. audit 核对

用重放得到的变更序列重建朴素成员多重集，全量重算每组聚合，
与 View 快照逐组比对；float64 用 math.Float64bits 位级比较；
组集合（含已删空组消失）也必须一致。
