# 事件流增量解析器设计推导

分层：`linescan`（字节块→逻辑行，三种行尾）→ `field`（行→字段）→
`stream`（字段→事件，内存态）。依赖单向：stream → {linescan, field}。

## 1. 块边界落在 CR 与 LF 之间

linescan 是字节状态机，只持有一个布尔状态 `crPending`：

- 初始 `crPending=false`。
- 见到 `\n`：若 `crPending=true`，说明它是上一块末尾 `\r` 的配对，
  仅清掉 `crPending`，**不发空行**；否则发出当前缓冲为一行。
- 见到 `\r`：先发出当前缓冲为一行，再置 `crPending=true`
  （此刻只产生一次行回调，不预判下一字符）。
- 见到普通字节：若 `crPending=true` 先复位，再追加到缓冲。

迁移（关键边）：`--\r--> crPending(已发一行)`；在该态 `--\n--> 复位/吞掉`，
`--其他--> 复位并正常积累`。因此 `\r\n` 在任何切分下都只产生一行尾，
`\r\n\r\n` 也只产生一个空行，绝不会变成两个。

## 2. 切分点无关需要保留的状态与不变量

跨块必须保留：① 行缓冲（未遇行尾的半行字节）；② `crPending`
（上一块是否以 `\r` 结束）；③ 流首标志（BOM 只能吃一次）。

不变量：linescan 的输出**只依赖到目前为止喂入字节的拼接结果**，
不依赖调用次数与每块长度。证明：输出由逐字节迁移决定，而上述三项
恰好是该字节自动机的全部状态；把同一段字节流在任意位置切成多段，
段间保存并恢复全部状态，等价于连续执行同一条状态路径，故回调序列
（行内容、行的个数、顺序）逐字节相同。测试对同一输入枚举
`k=1..len` 的首刀长度递归切分，断言事件序列完全一致。

## 4. 只有注释的块为何不能派发空事件

空行表示「一个事件结束」，但派发条件是该事件确有内容：数据缓冲非空
**或**显式设置过 `event`。`:comment` 按规范是注释，只更新行游标，
不触碰事件聚合态；`id`/`retry` 属于连接态、未知字段直接忽略，同样
不构成事件内容。若注释块也派发，下游会收到一个 `data==""`、
`event==""` 的幽灵事件：它没有任何业务含义，却可能被解释成
「心跳携带的空消息」，并在重连/统计中产生凭空多出的事件，破坏
第 2 条「同一字节流事件序列唯一」的语义。

## 9. 超限为何必须保证已聚合状态不变

限制（单行字节数、单事件 data 字节数）属于「拒绝整次喂入」的前置
校验：stream 对新行先判长、对 `data` 拼接先判总量，任一失败立即
返回两类可区分错误（`ErrLineTooLong` / `ErrDataTooLong`），
**不写入任何聚合字段**，错误前已确认的行照常保留。

若改成「截断后继续」，同一段字节流在不同切分点下会得到不同结果：
超限边界恰好对齐块边界的那次喂入会从截断点继续聚合，而落在块中间
的喂入则可能先拒后收，于是「任意切分点事件序列完全相同」（第 2 条
核心断言）被破坏；调用方也无法据错误做整包重放。因此拒绝必须是
原子的：查询值与派发序列在超限前后零变化。

## 其余约定

- 字段：无冒号→整行为名、值为空；有冒号→值去掉开头**恰好一个**
  空格；首字符为 `:` 是注释。多个 `data` 以 `\n` 连接，末尾不补。
- `id` 值含 NUL 则忽略；`retry` 须纯十进制，非法不改当前间隔。
- BOM 仅在整个字节流最开头被消费一次。
- 流末未遇空行的半个事件不自动派发，由调用方显式 `Finish` 派发；
  `Reconnect` 重置解析态但保留 `LastEventID` 与重连间隔。
