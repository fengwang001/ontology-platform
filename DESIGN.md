# 严格模式 Base64：规范尾部的推导

Base64 把 3 字节编码为 4 个 6-bit 字符；流末尾不足 3 字节时补零再编码，并用 `=` 标注填充。

- 1 字节余数：编码成 `XX==`。仅第 1 个字符的 6 bit 与第 2 个字符的高 2 bit 承载数据；
  第 2 个字符的低 4 bit 不承载任何数据，**规范要求它们必须全为 0**。
  `QQ==`：第 2 字符 `Q`=16=010000，低 4 位 0000，合法。
  `QR==`：第 2 字符 `R`=17=010001，最低位为 1，是“浪费”的比特，拒绝。
- 2 字节余数：编码成 `XXX=`。第 3 个字符只有高 4 bit 承载数据，其低 2 bit 必须全为 0。
  `QUI=`：`I`=8=001000，低 2 位 00，合法；`QUJ=`：`J`=9=001001，低 2 位 01，拒绝。

统一规则：被解码的数据由 `n` 个输出字节决定（无填充 n=3，`=`×1 时 n=2，`=`×2 时 n=1）；
合法组必须恰好是那 n 个字节经标准编码得到的 4 字符组，即所有不承载数据的尾比特（1 字节
余数查第 2 字符低 4 位；2 字节余数查第 3 字符低 2 位）必须为零，且填充形状只能是
`XXXX`/`XXX=`/`XX==`。这保证“任意字节串的编码结果唯一”：`QR==` 这类非规范形式携带了
永远无法由编码产生的比特，若接受它，解码后的字节将对应多个不同输入，破坏唯一性与往返
`Encode(Decode(y))==y`。

## 语义—测试对照

（实现完成后回填）
