# DESIGN: 流式 UTF-8 ⇄ UTF-16 转码器

## 1. 非法单元切分规则（WHATWG 风格）

首字节 L 决定期望长度。第二字节 c 的合法区间随 L 变化：

| 首字节 | 第二字节合法区间 | 第二字节非法时吞 | 第二字节合法后续字节非法时吞 |
|---|---|---|---|
| C2..DF | 80..BF | 1 | 1 |
| E0 | A0..BF | 1（违规字节重解析） | 吞掉已见合法前缀 |
| E1..EC | 80..BF | 1（违规字节重解析） | 吞掉已见合法前缀 |
| ED | 80..9F | 1（A0..BF 重解析为孤立续字节） | 吞掉已见合法前缀 |
| EE..EF | 80..BF | 1（违规字节重解析） | 吞掉已见合法前缀 |
| F0 | 90..BF | 1（违规字节重解析） | 吞掉已见合法前缀 |
| F1..F3 | 80..BF | 1（违规字节重解析） | 吞掉已见合法前缀 |
| F4 | 80..8F | 1（违规字节重解析） | 吞掉已见合法前缀 |
| 80..BF | —（孤立续字节） | 1 | — |
| C0 C1 F5..FF | —（恒非法首字节） | 1 | — |

推导：第二字节违规时，首字节 L 单独成为一个非法单元（len=1），违规字节被回推到流首重新解析。第二字节合法后，每个后续续字节只须在 80..BF；一旦违规，已见的整条合法前缀（不含违规字节）成为一个非法单元，违规字节重解析（ED 的第二字节为 A0..BF 时：ED 单吐，A0 重解析为孤立续字节单吐，故 `ED A0 80` 为三个 FFFD）。
据此：`F0 90 80 41` 中第三字节 41 非续字节，前缀 F0 90 80 一个单元（随后 41 正常）；`E0 80 80`：E0 单吐、两个 80 各单吐 = 3；`C0 AF`：C0 单吐、AF 单吐 = 2；`F4 90 80 80`：F4 单吐，90 80 80 三个孤立续字节 = 4；`80 80` 两个孤立续字节 = 2；`E2 82` 流结束：E2 82 是合法二字节前缀，属截断，替换时整段只产生一个 FFFD。

## 2. 缓存上限与何时立即判非法

UTF-8 最长 4 字节，已缓存合法前缀 + 当前输入即足够判定，故缓存硬上限 3 字节。
立即判非法（不等流结束）：恒非法首字节 C0/C1/F5..FF、孤立续字节 80..BF；第二字节落在表中非法区间；合法前缀之后遇到非续字节（单元在该字节前闭合为非法，新字节重入）。只有“已见字节全是某单元的合法前缀但长度不足”才等待；Close 仍等待 → 截断。
UTF-16：缓存上限 1 字节；按 code unit（2 字节）推进，高代理后必须看下一个 unit，非低代理则高代理单吐、该 unit 重新解析。

## 3. par 切点对齐

切点 b 处，向前回看至多 3 个续字节（80..BF）直到首字节 L。若 L..b-1 是 L 所启单元的合法未完成前缀（如 C3、E2 82、F0 90 80），则本单元整体归下一段：本段止于 L，下一段从 L 开始（该前缀字节两段都见到，但只由下一段产出、统计只记一次；常数级回看）。否则下一段从 b 开始（段内非法单元以 80..BF 开头时按孤立续字节处理，与流中情形一致）。
`C3 | 80 80`：C3 为合法未完成前缀 → 下段从 C3 开始，C3 80 解码成一个标量，剩 80 为 1 个非法单元，与单线程完全相同。段尾残留的合法前缀同理交给下一段，不会提前产出 FFFD。起始位置单调不减（被 C2..F4 阻挡），空段直接跳过。

## 4. 输出上限断点（第 9 条）

每个标量编码前先检查剩余容量：放不下就停下，返回 ErrLimit，此时该标量尚未产出。Write 返回的 n 只统计“已闭合并已输出”的标量所占字节；正在缓存里的半个字符字节不算已消费（留给下次 Write 或新实例）。调用方用返回的 n 切输入续传，新实例不接收 BOM，拼接逐字节等于不限长一次转完。

## 5. 检查次数计数

每从输入取出一个新字节（UTF-16 为 code unit 的两个字节各计一次）计 1 次；回推重解析的字节（高代理后的非低代理 unit、UTF-8 闭合后重入字节）不重复计数，因为该字节本属于下一次迭代取出。缓存重扫只发生在 ≤3 字节上，故整段喂入与逐字节喂入计数一致且 ≤ 2n。
