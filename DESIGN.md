# 设计：属性级权限过滤器

## 问题

角色决定可见列集合。当过滤谓词引用了对该角色不可见的列时，有三种做法，只有一种不泄露信息。

## 甲：不可见列的比较当作假（泄露）

把 `secret = 1` 直接判为 false。探针：`NOT (secret = 1)`。
若某行真实满足 `secret = 1`，甲对 `NOT(...)` 仍返回 true（内层被当假），
于是攻击者观察到「`secret = 1` 与 `NOT (secret = 1)` 命中同一批行」，
命中不随列值变化、只随可见性变化，这本身就是信息通道；再对 `secret = v`
逐值试探，命中集合不随 v 变化即暴露裁剪行为，与可见列对照即可反推取值分布。

## 乙：不可见列当作 NULL 参与三值逻辑（泄露）

探针：`secret IS NULL`。乙让它对每行都返回 true，攻击者由此确知
「存在一列 secret 且被隐藏」——列存在性被泄露。它成为通用探针：
对任意猜想列名发问，返回全集说明该列存在但不可见，空/报错说明不存在，
存在性被逐列枚举。三值逻辑下 `NOT (secret = 1)` 求值为 UNKNOWN，同样可区分。

## 丙：整条查询拒绝执行并指名列（不泄露，本题采用）

谓词引用任何一个对该角色不可见的列，整条查询返回可判定错误，
错误中指明列名与其在谓词树中的路径。结果集完全不被计算，
调用方得到的唯一信息是「该列不可见」，而可见性是策略对该角色公开的事实，
不引入任何关于数据的新信息，攻击者无法从行的有无反推列值。

## OR 短路例外

定义：先做常量折叠（`Const` 参与的 AND/OR/NOT 按布尔代数化简：
AND 遇 false、OR 遇 true 时整支删除并归约为 Const），
折叠后被整支删除的分支里的不可见列引用不再构成拒绝理由；
折叠后仍存活的引用一律拒绝。
理由：被折叠掉的分支语义上永不求值，不可见列从未被读取，
结果集与「该列不存在」时逐字节一致，无信息通道；拒绝它只损失可用性。
折叠先于可见性检查，二者在同一遍后序遍历完成：每节点恰好访问一次，
子节点先折叠，父节点再决定保留或删除整支，被删分支的引用随子树丢弃。

## 行裁剪与报告

结果行只保留可见列：不可见列从 map 移除（不置零值、不置 NULL），
使「不可见」与「值为空」可区分。报告含被裁列名集合（字典序）、
被拒绝引用（列名去重 + 全部路径，均排序）、是否整条拒绝。
报告序列化与构造顺序无关，逐字节确定。

## 复杂度

谓词检查为单遍后序遍历，访问节点数等于谓词树节点总数。
行裁剪按可见列集合迭代拷贝，代价 O(可见列数)，与总列数无关；
可见 5 列时每行拷贝 5 次，上界 4*5 恒成立。
