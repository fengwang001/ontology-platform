# DESIGN — JSON 字符串字面量严格编解码器

## 第 2 条：代理对错误偏移的推导

候选规则：(a) 指向高代理 `\uXXXX` 的起点；(b) 指向后面那个不合格单元的起点。

推导：一个高代理在写下的瞬间只是"条件合法"——它能否成立取决于后续 6 字节。
当后续单元不是低代理（或输入结束、或遇到引号）时，真正无法被任何合法
UTF-16 序列容纳的是**高代理本身**；后面的字节（如 `"\uD83DA"` 中的 `A`）
在别的位置完全合法，它不是病灶。孤立低代理 `"\uDE00"` 同理，错误只能
指向它自己。两案统一为一条规则：

> 偏移指向"第一个不可能出现在任何合法 UTF-16 编码中的码元"的转义起点。

因此 `"\uD83D"`、`"\uD83Dx"`、`"\uD83DA"`、`"\uDE00"`、`"\uDE00\uD83D"`
的错误偏移都是 1（各自问题码元的反斜杠处）。该偏移与后续内容无关，
无论后面是 EOF、引号还是垃圾字节都稳定，且正好指出修复点：删掉或补全
这个悬空码元，而不是动后面无辜的字节。

`"\uD83DA"` 不能解释成「U+FFFD + `A`」：严格解码器禁止静默替换。一旦做
有损恢复，无限多个不同的非法字面量会映射到同一字符串，Decode 不再单射，
往返性质（第 5 条）直接破产；且 RFC 8259 明确未配对代理的行为不可预测，
严格编解码器唯一可辩护的行为是拒绝，而不是猜测。

## 第 4 条：最小转义与唯一性的推导

U+001F 必须转义而 U+007F 不必：RFC 8259 的语法只禁止未转义的
U+0000–U+001F（外加 `"` 和 `\` 必须转义，否则无法定界/无法表示转义本身）。
U+007F (DEL) 不在必转义集合里，未转义的 DEL 是合法 JSON 字符串字符；
转义它仍然合法，但违反"最小"——而最小性正是唯一性的来源。

唯一性/单射：Encode 把每个输入字符按一张固定函数表映射到一个 token：
`"`→`\"`，`\`→`\\`，5 个具名控制符→短形式，其余 <0x20→`\u00xx`（小写），
其余字符（含 `/`、U+007F、U+2028/29、非 ASCII）→原样字节。每个 token
定长（2 或 6 字节）或以非 `\` 字节自成边界，token 流可无歧义切分；
两个不同字符串在首个不同字符处产生不同 token，故输出字节串不同——
Encode 是确定函数且单射，输出唯一。这也直接给出 Decode∘Encode = id。

## 测试对照表

（待测试写完后补齐）
