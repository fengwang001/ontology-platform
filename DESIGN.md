# DESIGN

## 第 2 条推导：双引号内反斜杠何时是转义
双引号内仍"活跃"的字符只有四类：`$` 与反引号触发展开、`"` 结束引号、`\` 自身。
反斜杠的职责仅是保护下一个仍具特殊性的字符；其余字符在双引号内本就是字面量，
无需保护，故反斜杠原样保留（`"a\b"`→`a\b`）。后跟换行是续行，两者一并删除。
结论：仅当下一字节属于 `$`、反引号、`"`、`\`、换行 时反斜杠才是转义符。

## 第 4 条推导：空词何时产生
一个词在"被触碰"时即已存在，与是否累积字符无关；触碰 = 写入字节或进入引号。
词边界（空白或 EOF）到达时，被触碰过的词即输出，哪怕内容为空。推论：
`""''` 相邻引号段拼成同一词 → 1 个空词；`a""` → 1 个词 `a`；
`a '' b` → 3 个词 `a`、``、`b`；`\<换行>` 单独一行：续行在分词前被删除，
未触碰任何词 → 0 个词；连续空白同理 → 0 个词。实现：started 标志记录触碰。

## 状态转移表（状态 × 字节类别 → 动作,下一状态）
状态：N=普通 S=单引号 D=双引号 EN=普通转义待定 ED=双引号转义待定。
动作：写=写入字节 留=保留反斜杠再写入 触=标记触碰 词=输出当前词 删=删除。

| 状态 | `'` | `"` | `\` | 换行 | 空白 | `$` 反引号 | 其他 |
|---|---|---|---|---|---|---|---|
| N  | 触,S | 触,D | EN | 词,N | 词,N | 写触,N | 写触,N |
| S  | N | 写,S | 写,S | 写,S | 写,S | 写,S | 写,S |
| D  | 写,D | N | ED | 写,D | 写,D | 写,D | 写,D |
| EN | 写触,N | 写触,N | 写触,N | 删,N | 写触,N | 写触,N | 写触,N |
| ED | 留,D | 写,D | 写,D | 删,D | 留,D | 写,D | 留,D |
EOF：S→单引号未闭合、D/ED→双引号未闭合（偏移=开引号）、EN→孤立反斜杠
（偏移=该反斜杠）、N→若被触碰则输出末词。非导出计数器 n 每字节 +1。

## 测试对照表
| 语义 | 钉住它的测试 |
|---|---|
| 1 单引号内全字面（含反斜杠） | TestSplitSemantics |
| 2 双引号内五种转义与保留 | TestSplitSemantics |
| 3 无引号转义与续行 | TestSplitSemantics |
| 4 拼接与空词（`""''` 等） | TestSplitSemantics |
| 5 三类错误可区分+偏移 | TestErrors |
| 6 任意切分点/逐字节一致 | TestSplitPoints |
| 7 Quote/Split 往返 | TestQuoteRoundTrip |
| 计数器 == 输入字节数 | TestProcessedCounter |
