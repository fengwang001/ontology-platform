# 带转义的 RLE 编解码器：设计推导

## 第 2 条：严格解码的拒绝清单（由 Encode(Decode(t)) == t 逐条反推）

不变量：解码器接受的每个 t 必须等于 Encode(Decode(t))，即 t 本身就是规范形。
以下每一类，若被接受，都给出违反不变量的具体 t：

- 显式次数 1：`1a` 解码为 "a"，而 Encode("a") = `a` ≠ `1a`。次数 1 必须省略。
- 次数 0：`0a` 若解码为 ""，Encode("") = 空 ≠ `0a`；解码为任何串等式都不成立。
- 前导零：`01a` 解码为 "a"，Encode("a") = `a` ≠ `01a`。十进制必须无前导零。
- 相邻同符号游程：`2a3a` 解码为 "aaaaa"，Encode 合并最长游程得 `5a` ≠ `2a3a`。
  同理 `aa`（两个省略次数的游程）必须拒绝，其规范形是 `2a`。
- `\` 后非数字非反斜杠：`\a` 中的 `\` 在 Encode 输出里永不出现（只有数字和
  反斜杠会被转义），故 Encode(Decode(`\a`)) 不可能含该 `\`，等式必不成立。
- 末尾孤立 `\`：Encode 的输出绝不以 `\` 结尾，故 `\` 后必须还有字符。
- 末尾只有次数：`12` 没有符号，而 Encode 输出必以符号结尾，故拒绝。
- 非法 UTF-8：输入按码点解释，非法字节序列不对应任何码点序列，
  而 Encode 只产生合法 UTF-8，等式无从成立。

每一类对应一个可 errors.Is 判定的哨兵错误，错误值携带字节偏移（游程、转义
或数字串的起始偏移；非法 UTF-8 为出错字节的偏移）。

## 第 3 条：大次数的取舍

选择支持任意大的次数：次数以十进制字符串解析进 big.Int（runs.ParseDecimal），
不存在整数溢出。但解码输出受字节上限约束：Decoder 携带 maxOut（Decode 用
DefaultMaxOutput = 1<<30，流式 NewDecoder 可配置）。每完成一个游程，先做
count > (maxOut - written) / width 的纯整数比较，超限返回 ErrOutputLimit；
只有通过检查后才按次数写出，因此任何时刻内存占用都不超过上限，与次数本身无关。
例如 `99999999999999999999a` 被安全拒绝为 ErrOutputLimit，`1000000a` 正常解码。

## 语义与测试对照表

（待测试写完后补齐）
