# DESIGN

统一格式差分器的设计推导。包依赖单向：`lines ← edit ← hunk ← udiff ← patch`。

## 1. 零计数 hunk 头的行号规则

样例：`x\n`→`y\nx\n`（C=0）得 `@@ -0,0 +1 @@`；`a\nb\nc\n`→`a\nb\nX\nc\n`（C=0）
得 `@@ -2,0 +3 @@`；`a\n`→空（C=3）得 `@@ -1 +0,0 @@`。

推导：计数非零时，起始号是 hunk 在该侧第一行的 1 基行号。计数为 0 时该侧
没有任何行，"第一行"不存在。三个样例中零计数侧的数字 0、2、0 恰好等于插入
点之前已有的行数，即**前一行的 1 基行号**：样例 1 插入点在文件开头（前面
0 行）→ `-0,0`；样例 2 插入点在 `b` 之后（前面 2 行）→ `-2,0`；样例 3 新
文件在删除点之后没有行 → `+0,0`。若把它解释成"将要插入的位置"（1 基），
样例 1 应写 `-1,0` 而非 `-0,0`，矛盾；解释成"前一行的行号"则三个样例全部
吻合，与 GNU diff 一致。

统一规则：设 0 基起始下标为 s、计数为 c。c>0 时头部写 s+1；c=0 时写 s。
c=1 省略 `,1`，其余情况（含 c=0）都写 `,c`。`Apply` 解析时反向：c>0 取
s=a-1；c=0 取 s=a，即插入/删除点在第 a 行（1 基）之后、第 a+1 行之前。

| b 取值 | 头部 `-a,b` 写法 | Apply 对插入点的解释 |
|--------|------------------|----------------------|
| b = 0  | a = 前一行行号（= 0 基下标 s），写 `-s,0` | 第 a 行之后，0 基 s = a |
| b = 1  | 省略 `,1`，写 `-a`，a = 首行 1 基行号 | 0 基 s = a-1 |
| b ≥ 2  | 写 `-a,b`，a = 首行 1 基行号 | 0 基 s = a-1 |

## 2. hunk 合并阈值

结论：两段改动间未改动行数 g ≤ 2C 时合并，g ≥ 2C+1 时分开。

推导：前一段改动向下带 C 行上下文，后一段向上带 C 行上下文。g = 2C 时两段
上下文恰好相接、中间无空缺，GNU diff 输出一个连续 hunk；g = 2C+1 时中间有
1 行不被任何 hunk 覆盖，无法合成一个连续 hunk。反证另两个取值：若阈值取 C，
C=1、改动第 2 与第 4 行（g=2）时 GNU 输出单个 `@@ -1,5 +1,5 @@`，阈值 C
会错拆成两个 hunk；若阈值取 2C+1，g=2C+1 时合并出的 hunk 必须包含中间那
行，但 GNU 实际输出两个 hunk 且中间行不出现，矛盾。故阈值 = 2C。

## 3. 最短脚本的并列选择

Myers 前向贪心在 `V[k-1] == V[k+1]` 时选择右移（删除），仅当
`V[k-1] < V[k+1]` 时下移（插入）；回溯用同一比较，保证脚本确定。效果：同
一位置先删后插，hunk 内 `-` 行排在 `+` 行之前。例：`a b`→`b a` 得到
`-a` / ` b` / `+a`（删 a、留 b、在 b 后插 a），与 GNU diff 一致；若并列时
优先插入，会得到另一最短脚本 `+b` / ` a` / `-b`，与 GNU 输出不符。

## 4. 偏移应用后的后续 hunk

要跟着平移。第 i 个 hunk 的查找中心为
`AStart_i + Σ_{j<i}(BCount_j − ACount_j) + drift_{i−1}`，其中 `drift_{i−1}`
是前一个 hunk 的实际应用位置减去它自己的查找中心。原因：补丁记录的行号是
旧文件编号，已应用 hunk 净增删的行数（Σ 项）必然改变其后所有位置；而目标
文件相对旧文件的整体位移（例如补丁区域之前被插入了一段）对每个后续 hunk
影响相同，故继承上一个 hunk 的实测漂移作为下一个的查找起点，再在 ±F 行内
搜索精确匹配，取距离最近、并列取靠前者。
