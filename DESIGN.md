# 设计推导（eol/ws/span/norm/par）

## 1. 空输入与只含换行：末尾策略样例表

| 输入 | Keep 原样 | Ensure 恰好一个 | Trim 删空行留一个 |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三策略统一形式：规范化正文后，设末尾连续 `\n` 数为 t。
Keep：保留 t；Ensure：t=0 且正文非空 → 插 1 个，否则补成 1（空输入仍为空，避免无中生有）；Trim：补成 min(t,1)。
幂等：输出只含 `\n` 结尾、无 `\r`、无行尾空白；二次规范化时 t 已等于 0/1，三策略均不动它。
Ensure 若选「空输入也输出 `\n`」同样幂等（`"\n"`→`"\n"`），但会凭空造字节，故选空→空。

## 2. 被删字节的 ToOut

映射表由「恒等段 (a0,a1,b,len)、删除段 (a0,a1,b,0)、插入段 (a0,b,len)」组成，
半开区间无缝拼接。恒等：ToOrig=o−b+a0；删除段 o=b（落入下一段起点）；插入 ToOrig=a0。
ToOut(i) 取包含 i 的最左段：恒等 i−a0+b；删除/插入 a0→b。
被删空白与 `\r\n` 的 `\r` 都映射到紧随其后的 `\n` 的**输出起点**（删除段的 b），
即光标落在换行符**之前**（行尾位置），而非下一行；单调性迫使删除区间整体压到一个点。
插入的 `\n`（Ensure/Trim 补的）ToOrig=末尾原文位置；它无原文逆像，故
`ToOut(ToOrig(o))==o` 对所有「非纯插入」输出偏移成立，纯插入偏移自身映射稳定，
恒等/删除数据上双射完全成立（演示与断言均覆盖）。

## 3. 缓冲超限：选择拒绝

强制把待判空白当行内空白输出，若下一字节恰好是行尾，则已输出的空白无法收回，
违反第 5 条幂等与第 4 条逐字节一致（换切分点结果不同）。
故超限时返回可判定错误 `ErrPendingLimit`（带原文偏移），已输出内容保留，
规范化器进入终态（再写返回 `ErrClosed`）。该状态可解释：偏移处之前的映射仍可查。

## 4. par 拼接：边界前移吸收

段首无法知道前段是否挂起 `\r`/空白。做法：对切点 p 只做**前移**到 q≥p：
前一字节是 `\r` 且 p 处是 `\n` → q=p+1（吃掉 `\n`，CRLF 整段归左）；
p 前是空格/制表符 → 从 p-1 向左越过整串空白，再看其前：是 `\r` 则继续吸收其后
可能的 `\n`；是其它字节（含 `\n`）则保持。即每个切点落在「行尾序列之后」。
被吸收字节归左段正常判定，右段只在干净边界开始，段内结果与整体完全一致。
段 Map 的区间坐标统一加 (q 起点原文偏移, 已输出字节数) 平移后拼接。
修正范围上界：≤ 空白缓冲上限 + 2（`\r`+`\n`）；扫描总量摊销 O(n)。
若前移到下一个边界则合并，实际段数 ≤ K。
