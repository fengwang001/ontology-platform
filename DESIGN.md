# 采样剖析与热点归因器 — 设计推导

## 1. self 与 total 的定义与恒等式

每条采样栈沿调用路径插入树：栈底为根方向，栈顶为叶子方向。
插入栈 `f0..fk` 时，路径上**每个节点** total 各 +1（该采样在它们的子树内），
只有**末端节点** self +1（采样实际停留处）。

- 恒等式①：所有节点 self 之和 == 总采样数（每条样本只贡献一次 self）。
- 恒等式②：所有节点 total 之和 >= 总采样数，且有任意栈深 >1 时严格大于；
  一条样本被路径上每个祖先重复计入 total，故 total 之和不是总采样数。

单样本栈深为 1 时两者恰好相等，属边界情形，不是矛盾。

## 2. 递归帧的函数级归因

递归使同一函数名出现在多个节点。若把所有名为 F 的节点 total 直接相加，
同一条样本会在“外层 F”和“内层 F”被重复计入（内层 F 的子树是外层 F 子树的
子集）。因此函数级 total 只取**不被另一个 F 节点包含的最外层 F 节点**：

- 遍历每条根→叶路径时维护“当前路径上是否已出现 F”，首个 F 为最外层；
- 函数 F 的 total = 各最外层 F 节点 total 之和（DFS 一次即可，每个节点判一次
  祖先名集合，O(节点数)）；函数 self = 所有 F 节点 self 之和（self 天然不重叠）。

对栈 `A→F→G→F→H`（单条样本）：节点 total 为 A=1,F(外)=1,G=1,F(内)=1,H=1；
F 的函数级 total = 1（最外层），不是 1+1=2。

## 3. 深度截断

规范深度上限 D。插入栈时只取前 D 帧：第 D 个节点（截断点）置 `Truncated=true`
标记，其后的帧丢弃但该样本仍计入树（截断点收到 self +1）。计数器
`TruncatedSamples++` 记录被截断样本数；空栈拒绝插入并计 `InvalidSamples`。
深度恰好 D 不截断，D+1 截断。

## 4. 复杂度计数

插入只沿单条路径下行，每帧一次 map 查找（+一次插入时的写，合计 ≤2 次哈希
比较），常数取 4 为上界：总比较数 ≤ 样本数 × 栈深 × 4，与既有节点总数无关。
归因是“一遍 DFS 扫描 + 一次 sort.Slice”，复用既有树、不重建；
`treeBuilds` 计数器恒为 0。

## 5. 采样器

采样协程按 ticker 固定间隔唤醒；时钟/栈源可注入。栈源返回 `(栈, dropped,
ok)`：busy 窗口返回 dropped 计数，累加进 `DroppedSamples` 且不入树。
应采样次数 = 有效 tick 数。恒等式：self 之和 + dropped == 应采样次数。
时钟回拨（nextTick ≤ prevTick 或间隔非正）时跳过该 tick 并计
`BadIntervals`，不占应采样次数。停止用 sync.Once，幂等。
并发：树操作以 RWMutex 保护，查询持读锁时写路径整体不可见，不存在
“子加父未加”的半更新态。

## 6. 落盘格式（自描述 + CRC32）

二进制布局：魔数 `PROF1`、版本 u16、深度上限 u32、节点数 u32、无效/截断
样本计数 u64 构成头部；其后为 DFS 先序节点记录；末尾 u32 CRC32（IEEE，
覆盖此前全部字节）。节点记录 = depth u32、truncated 单字节、name 长度 u16
+UTF8 字节、self u64、total u64。先序保证前缀恢复时绝不出现子在父前。

截断分类（三类用 errors.Is 判定 ErrHeaderIncomplete / ErrRecordIncomplete
/ ErrCRC）：尾部落在头部区→头部不完整；头部完整但某条节点记录未写完→
记录不完整，返回此前完整记录构成的最大可恢复前缀；记录全在而 CRC 缺失或
不符→CRC 不匹配，返回全部节点。恢复出的树按先序回填，父先于子，
self 之和 == 已恢复样本数恒成立。
