# 设计推导

## 1. 非法 UTF-8 单元的切分（WHATWG 规则推导）

读首字节 L 确定期望长度 n；之后每来一个字节：
- 是续字节(80..BF)：若该位置允许它（第二字节受 L 专属区间约束，见 §5 表），吞下继续；不允许则已缓存前缀（至少 L）立刻判为一个非法单元，该续字节作为新指令起点重新解析。
- 不是续字节：已缓存的 L（及若干续字节）立刻判为一个非法单元；新字节另解。
- 收满 n 个合法续字节且第二字节区间满足：整体为一个合法标量。
- 流结束仍未收满：已缓存字节合为一个非法单元（截断）。

样例：`F0 90 80 41`：90 在 F0 的区间(90..BF)内、80 也是续字节，41 非续字节 → F0 90 80 合为 1 个单元，41 另解。
`E0 80 80`：E0 第二字节须 A0..BF，80 不允许 → E0 为 1 个；80、80 作为孤立续字节各 1 个，共 3。
`ED A0 80`：ED 第二字节须 80..9F，A0 不允许（会进代理区）→ ED、A0、80 各 1，共 3。
`C0 AF`：C0 永不合法（非最短），AF 为续字节 → 各 1，共 2。
`F4 90 80 80`：F4 第二字节须 80..8F，90 不允许（越界 >10FFFF）→ 4 个各 1。
`E2 82`+EOF：E2 合法前缀但未收满 → 合为 1 个（截断，不逐字节拆）。
`80 80`：孤立续字节各 1，共 2。

## 2. 缓存上界与「立即放弃」

UTF-8 最长 4 字节，待决缓存（含首字节）≤3 个未决字节：每收到一个续字节立刻用 §5 区间检查，要么合法续上，要么立刻判非法并把该字节改作新起点——绝不等到流结束。立即判定时机：出现非续字节；或第二/后续字节越过该 L 的专属区间。UTF-16 只缓存 1 个奇数字节或 1 个高代理码元。

## 3. par 切点对齐

每段从切点 s 回看确定真正起点：UTF-8 若 s>0 且 data[s-1] 为多字节首字节，则按其长度前移（中间是 80..BF 续字节就继续前移），最多回看 3 字节；UTF-16 回退到偶偏移，若回退点是高代理再退 2 字节。各 worker 从对齐起点解析，只把输入字节偏移 >= 原切点 s 的产物计入本段（重叠前缀仅用于正确判定，不重复计入统计与输出）。尾部未完成字符不产出，自然落入下一段回看。
`C3|80 80`：段 1 的 C3 未收满、不产出；段 2 回看到 C3，组成合法 é 计入段 2，再处理孤立的 80。拼接与单流一致；任一非法单元的归属由其首字节所在段唯一决定，故单元总数不重不漏。

## 4. 输出上限的断点

Write 只在标量边界停：返回的 n 恰为「已完整输出标量对应的输入字节数」；残留在待决缓存里的字节不计入 n（未消费，调用方下次连同新数据重传）。超限即置终态返回 ErrLimit，已输出字节不变。调用方从 data[n:] 起换新实例，拼接与不限上限一次转完逐字节相同。UTF-16 代理对凑不齐全对不输出。

## 5. 首字节 → 第二字节区间与吞字

| 首字节 | 长度 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 首字节 1；续字节另起 |
| E0 | 3 | A0..BF | E0=1；越界续字节各 1 |
| E1..EC | 3 | 80..BF | 按 §1 续字节规则 |
| ED | 3 | 80..9F（避开 D800..DFFF） | ED=1；越界续字节各 1 |
| EE..EF | 3 | 80..BF | 按 §1 |
| F0 | 4 | 90..BF | F0=1；越界续字节各 1 |
| F1..F3 | 4 | 80..BF | 按 §1 |
| F4 | 4 | 80..8F（≤10FFFF） | F4=1；越界续字节各 1 |
| 80..BF | 1 | —（孤立续字节） | 自身 1 |
| C0 C1 F5..FF | — | 永不合法 | 自身 1 |

复杂度：每个字节最多被检查 2 次（作为续字节 1 次，越界后改作新首字节 1 次），由非导出计数器 checks 钉住；par 每段回看为 O(1) 常数字节，总额外检查 ≤ 8×常数。
