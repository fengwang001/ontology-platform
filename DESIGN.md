# DESIGN

## 1. 空行与单列空值

**选择**：空行被跳过（不产生记录）。连续两个换行中，第二个换行只终结一条 0 字节的“影子行”，它不携带字段；“无字段的记录”无法用字段序列表示，故跳过。末尾最后一个换行同理不产生记录；`""\n` 不是空行（有一个引号空字段），是一条记录。

**后果**：单列记录 `\n`（唯一字段为未引号空串）必须与空行区分。回写器在「记录恰好只有一个字段且其值为空串且未加引号」时必须写成 `""`；否则写成空串会输出一个裸换行，再解析时被当成空行跳过，数据丢失。故单列空值必要引号。多列中的空字段（如 `a,,b`）不会与空行混淆，保持裸写。

**规范输入（canonical）**：记录用 `\n` 结束；仅在必要时加引号（字段含 `,`、`"`、`\r`、`\n`，或上述单列空值）；引号字段内 `"` 转义为 `""`；末尾有换行；无空行。规范输入满足 `Write(Parse(x))==x`。引号内原始 `\r\n` 是内容，回写原样保留。

## 2. par 的切点

不做“双假设全量重算”，而是**规整切点**：从等分点向左回退，直到切点处前一字节既非 `"` 亦非 `\r`（保证切点不落在 `""`、闭合引号与后随字符之间、`\r\n` 中间）。回退量受限于“回退到上一段边界前 1 字节”；若整段候选只由 `"` 与 `\r` 构成（如 `""""""`），则把该段并入左段，段数退化、goroutine 数 ≤ K，不增加总处理量。

规整后，切点处只可能：①位于记录边界；②落在未引号字段内部。每段以 field-start 状态解析：情形①段内记录完整；情形②段的首尾各有一个“悬挂”未引号字段。拼接时，若左段末尾有悬挂字段，则把右段首个悬挂字段（以左段该字段起始偏移为准）与其拼接：合并字节、引号标记=未引号，随后右段其余字段照常归入同一记录。

引号字段跨段：切点不落在 `"` 字节上，一个引号字段只可能整段在某个 worker 内，或跨在一个不碰引号的切点上；后者左段以 quoted 结束、右段以 quoted 开始，拼接时把右段第一个字段的内容并入左段悬挂引号字段，不另开字段。

**坐标换算**：lexer 接收 `baseOff/baseRec/baseField`，段内偏移与记录号直接按全局值产出，拼接无需再换算。错误只在“真实链路”（field-start 起点、前序真实记录号）上判定；孤立假设不产生可外报错误。本方案无需双假设即可唯一确定真实状态，总处理量 ≤ 输入字节数 + 回退常数 ≤ 2N。

## 3. CR 待定状态

未引号字段中见到 `\r` 时进入 cr-pending：它可能是 `\r\n` 行尾，也可能是孤立 `\r`（错误）。续传时状态保存在 lexer 内存中，`Feed` 可任意次，`\r` 与 `\n` 分两片到达也正确。**Close 时仍处 cr-pending → 报孤立 `\r`**（偏移指向该 `\r`）。par 切点由第 2 条规整保证不落在 `\r` 上，worker 段末不存在 cr-pending；引号字段内的 `\r` 是普通内容，不进此状态。

## 4. 上限的“立刻”计数

- 字段字节计数只计**内容字节**（分隔符、换行、外层引号不计；`""` 转义只贡献 1 个 `"` 字节）。每接受一个内容字节前先判断 `len+1 > MaxFieldBytes`，超限即在该字节偏移报错，不缓冲。
- 字段数：每产出一个字段前判断 `count+1 > MaxFields`。
- 记录数：每完成一条记录前判断 `n+1 > MaxRecords`。

超限后 lexer 进入终态，返回 `ErrLimited`（包装可判定），此后每次 `Feed/Close` 返回同一哨兵 `ErrTerminal`；已产出的完整记录保留。

## 5. 状态转移表

状态：S=字段开始，B=未引号中，Q=引号中，R=引号中刚见引号，C=行尾CR待定。

| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| S | emit空字段→S | →Q | 空行:跳过→S | →C | 起字段→B |
| B | emit字段→S | 错误:裸引号 | emit字段+完成记录→S | →C | 追加→B |
| Q | 追加→Q | →R | 追加→Q | 追加→Q | 追加→Q |
| R | emit字段→S | 追加`"`→Q | emit+完成记录→S | 错误:引号后CR | 错误:引号后垃圾 |
| C | 错误:孤立CR | 错误:孤立CR | 完成记录→S | 错误:孤立CR | 错误:孤立CR |

EOF：Q→未闭合引号；R→emit+完成记录；C→孤立CR；S/B→若有在途字段则 emit+完成记录（末尾无换行合法），S 且无在途字段→正常结束。四类错误：`ErrBareQuote`、`ErrQuoteJunk`（含引号后 CR）、`ErrUnclosedQuote`、`ErrColumnCount`，均带 offset/record/field（从 1 起，偏移从 0 起）。
