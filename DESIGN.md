# 位图区间分配器：关键规则推导

## 1. 合并的方向与时机（核心）

`Free(s,k)` 先在有序空闲区间表里定位前驱 P（start<s）与后继 Q（start>s）。
新区间为 [s,s+k)，随后三种情形：

- **仅左邻**：位图初态（N=10）`1110000111`，空闲表 [3,7)。释放 [1,3)（槽1,2）。
  P=[3,7) 且 P.start==s+k=3，相接；Q 不相接。合并为 [1,7)。
  若不合并，表里留 [1,3)、[3,7) 两条相邻区间：不变量1“不相邻”立即被破坏；
  此后 `Alloc(5)` 只扫描单个区间，每条都 <5 便失败，尽管 [1,7) 连续可容——
  即不变量3 的假碎片。
- **仅右邻**：初态 `1110000111`，空闲表 [3,7)。释放 [7,9)（槽7,8）。
  Q=[3,7) 且 s==Q.end=7，合并为 [3,9)。
  若不合并，`Alloc(5)` 在两条 ≤4 的区间上失败，假碎片同样出现。
- **两侧同时**：初态 `1000100011`（N=10），空闲表 [1,4)、[5,8)。释放 [4,5)（槽4）。
  P.end==s 且 Q.start==s+k，三段必须一次合并成 [1,8)。
  只并左侧会留 [1,5) 与 [5,8) 相邻，`Alloc(6)` 被错误拒绝。

两侧都不接则直接插入；合并在插入时一次性完成，故不变量1恒成立。

## 2. 选哪一段（选段规则）

不变量4要求同一序列逐次同结果，因此选段必须是有序结构上的确定函数：
**最左适配（first-fit）——选 start 最小且长度≥k 的区间**。
起点有序、无并列歧义，结果可复现。
反例（碎片增多）：N=8，依次 `Alloc(2),Alloc(2),Alloc(2)` 占用 [0,2)[2,4)[4,6)，
`Free` 中间 [2,4)，再 `Alloc(2)`。first-fit 命中 [2,4)，布局复原；
若选“最右可容区间”会去切 [6,8)，使 [2,4) 永久成为内部空洞，碎片增多。

## 3. 切分剩余的位置

first-fit 从最左可容区间分配。为保持确定且与选段一致，**取区间头部 k 个**
（返回该区间起点），剩余 m-k 留在尾部（起点 += k）。
若取尾部，剩余会留在更小的起点，下次 first-fit 会优先切这段小头，
同一序列的落点将与“取头部”不同且无规范依据；取头部使分配位置单调确定，
保证不变量4。

## 4. k 恰好等于整个位图长度

**合法**。初态全部空闲时 `Alloc(N)` 返回 0：只有这一个区间、长度恰为 N，
成功后无任何两个未释放区间相交（不变量2成立）；位图全 1、空闲表为空，
不变量1/3均保持。由不变量5，只有 k>N 才是可判定错误，k=N 不得拒绝。

## 5. 非扫描定位（支撑复杂度约束）

空闲区间用随机 treap 按起点有序，每节点记录子树最大区间长度 mx。
`Alloc(k)` 自顶向下：左子树 mx≥k 才进左子树，否则考察本节点、再右子树。
“考察数”= 显式判定过能否容下 k 的节点数；碎片 100、10000 两档下，
最左可容区间在最左段，走左链即达，考察数均为 1（≤4），不随区间总数线性增长。
