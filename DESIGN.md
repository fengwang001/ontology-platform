# 媒体类型内容协商器 — 设计推导

包划分：`mtype`（解析/规范化，无依赖）→ `rank`（单项匹配与特异性评分，依赖 mtype）→ `negotiate`（协商入口，依赖前两者）。依赖单向，无反向引用。

## 评分模型

候选 c 的得分 = 所有匹配 Accept 项中 `(q, 特异性)` 的字典序最大值；q 以千分整数（0..1000）表示避免浮点误差。候选间比较同样先 q 后特异性，全平取服务端清单顺序。最大得分 q=0 的候选被丢弃；全部丢弃则返回 `ErrUnacceptable`。

## 1. 为什么先比 q 再比特异性

q 是客户端显式声明的意愿强度，特异性只是"这条声明写得多具体"的元信息。若反过来先比特异性：Accept 为 `text/*;q=0.9, text/plain;q=0.1`，候选为 `[text/html, text/plain]`。先比特异性时 text/plain（特异性 3）压过 text/html（特异性 2），于是返回 q=0.1 的 text/plain——客户端几乎不想要的类型；而客户端用 q=0.9 明确更想要的 `text/*`（覆盖 text/html）被丢弃。先比 q 时 text/html 以 0.9 胜出，符合客户端真实意愿。特异性只在 q 打平时作为平局裁决才有意义。

## 2. q=0 与非法 q 的处理

若把 `q=0` 当成"最低优先级"而非"拒绝"：Accept 为 `text/plain;q=0`，候选只有 `[text/plain]`。"最低优先级"语义下它是唯一可选，仍会被返回——但客户端已明确说不要这个类型，返回它直接违背声明。正确行为是返回"无法接受"。

非法 q（`q=1.5`、`q=abc`、`q=0.1234`）的处理选择：**整条 Accept 报错**，而非丢弃该项继续。理由：静默丢弃可能把一条本意是 `q=0` 的拒绝项（如笔误 `q=0.`）抹掉，使被客户端明确拒绝的类型变得可接受——这是静默改变安全语义的失败方向；报错让调用方显式感知并处理，失败方向是"少给"而非"给错"。该行为在 `TestErrorKinds` 中固定。

## 4. 同分为什么取服务端顺序

q 与特异性都打平时，客户端没有表达任何可区分的偏好（RFC 7231 中 Accept 项书写顺序不携带语义），此时唯一有信息量的偏好来源是服务端自己的候选清单顺序——服务端把最想给的放前面。若改用 Accept 顺序做平局裁决，结果会随客户端的书写习惯漂移，服务端无法控制自己的默认输出，同一服务对不同客户端产生不一致的内容形态。测试用两组对照固定：交换 Accept 项顺序结果不变，交换候选清单顺序结果改变。

## 5. 缺失与空串为什么必须区分

缺失 = 客户端没表态，HTTP 语义等价于 `*/*`（接受一切）；空串 = 客户端显式表态"我什么都不接受"。若合并处理：都当 `*/*` 会让显式拒绝被当成全盘接受，返回一个客户端声明无法处理的类型；都当拒绝则让未发送 Accept 的普通客户端全部失败。二者语义相反，必须可判定地区分：API 用 `*string`（nil=缺失）表达，空串直接返回 `ErrUnacceptable` 且不经解析。
