# 属性级权限过滤器 — 设计推导

## 1. 问题

角色只能看到部分列（属性）。过滤谓词可能引用其不可见的列。三种处理方案：

- 甲：不可见列的比较一律按假处理。
- 乙：不可见列按 NULL 参与三值逻辑（SQL 语义：UNKNOWN）。
- 丙：整条查询拒绝，报告列名与谓词路径。

## 2. 甲的泄露路径

探针 `NOT (secret = 1)`：甲把 `secret = 1` 替换为假，`NOT false` 恒真，
查询放行并返回全部行。攻击者据此确认「该列存在且系统在替我兜底」；
更一般地，攻击者可借助布尔盲注：`(secret=1)` 与 `NOT (secret=1)` 的
返回行集互补，逐位反推不可见列的值。谓词语义本身成了侧信道。

## 3. 乙的泄露路径

乙把不可见列当 NULL。探针 `secret IS NULL`：对 NULL 求值为真，
查询直接放行——`IS NULL` 成了存在性探针（真实列非空时本应返回空集，
现在返回全部行，存在性与「是否为 NULL」两种信息都可被区分）。
此外 `NOT (secret = 1)` 在三值逻辑下为 `NOT UNKNOWN = UNKNOWN`，
看似安全，但配合 `x OR secret=1`（x 已知为假）仍可区分 UNKNOWN 与 FALSE，
盲注通道依旧存在。

## 4. 采用丙的论证

权限判定必须在「查看任何数据值」之前完成，且输出只允许依赖
「角色可见列集合 + 谓词结构」这类攻击者已知的信息，不依赖不可见列的
真实值。甲、乙的输出都随不可见列的值或存在性变化，故必然泄露。
丙直接拒绝并指出列名（列名是授权元数据，审计需要，不属于列值），
对所有取值给出相同响应，无值相关侧信道。

## 5. OR 短路（常量折叠）例外

定义：谓词分析器一遍遍历并做常量折叠。若某 OR 节点折叠后整体成为
常量（真/假），则该子树在任何输入下都不会被运行时读取；其内部引用的
不可见列标记为 elided（消解）而非 rejected，查询放行。
仅 OR 边界触发该重分类（AND 无此例外：`false AND hidden()` 虽数学上
短路，但 SQL/三值逻辑与求值顺序下不保证不读取，保守拒绝）。
折叠只承认显式布尔常量，不猜测列间关系，保证保守可靠。

## 6. 其余定义

- 行裁剪：只按可见列拷贝，不可见列物理移除（不置零/NULL），输出键排序。
- 报告（确定性）：被移除列字典序；拒绝引用按列名、再按路径排序；
  同一列多次出现只归并为一条、路径全部保留；区分 rejected/elided。
- 三类错误：`ErrInvisibleColumn`、`ErrEmptyRole`、`ErrUnknownRole`，
  均支持 `errors.Is`；拒绝时错误可解出引用列表。
- 复杂度：谓词一遍遍历，访问计数 == 节点总数；行裁剪拷贝量
  O(可见列数)，与总列数无关（预算 4*visible）。
