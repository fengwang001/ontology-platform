设计推导
## 1. 非法单元切分规则（推导自七个样例）
合法 UTF-8：首字节给出长度 n；第二字节必须落在该首字节允许的连续字节区间；
后续每个字节必须是 80..BF。规则采用 WHATWG/UTS-#22 子表替换：
1. 首字节非法（C0 C1 F5..FF）：该字节自成一个非法单元（故 C0 AF 中 C0 先单吞，AF 再单吞 → 2 个 FFFD）。
2. 第二字节不在合法区间，或不是连续字节：只吞首字节本身，下一字节重新解析
   （E0 80：E0 要求第二字节 A0..BF，80 不合法，只吞 E0，剩 80 80 各为独立连续字节 → 3 个 FFFD；
   ED A0：ED 要求 80..9F，A0 不合法 → 3 个 FFFD；F4 90：F4 要求 80..8F → 4 个 FFFD）。
3. 第二字节合法、其后第 k 个必需连续字节缺失（流结束）或不是 80..BF：
   吞掉「首字节 + 已收到的合法连续字节」。故 F0 90 80 41 吞 F0 90 80 为 1 个单元、41 正常；
   E2 82 后流结束，吞 E2 82 为 1 个单元（不是两个）。
4. 孤立连续字节 80..BF：每个自成一个非法单元（80 80 → 2 个 FFFD）。
第二字节合法区间与吞字长度表：
| 首字节 | 第二字节区间 | 第二字节非法时 | 后续字节非法/EOF 时 |
|---|---|---|---|
| C2..DF | 80..BF | 吞 1 | 吞已收全部（最多2） |
| E0 | A0..BF | 吞 1 | 吞已收全部（最多3） |
| E1..EC | 80..BF | 吞 1 | 吞已收全部（最多3） |
| ED | 80..9F | 吞 1 | 吞已收全部（最多3） |
| EE..EF | 80..BF | 吞 1 | 吞已收全部（最多3） |
| F0 | 90..BF | 吞 1 | 吞已收全部（最多4） |
| F1..F3 | 80..BF | 吞 1 | 吞已收全部（最多4） |
| F4 | 80..8F | 吞 1 | 吞已收全部（最多4） |
| 80..BF | — | 自身为单元，吞1 | — |
| C0 C1 F5..FF | — | 自身为单元，吞1 | — |
## 2. 切分点缓存上限
DFA 只需记住「首字节+已收到的连续字节」。最长字符 4 字节，缓存硬上限 3：
- 缺第二字节缓存 1（首字节）；缺第三缓存 2；缺第四缓存 3。
- 第二字节一到即可判定合法/非法：非法时立即出一个单元，不等流结束（E0 80 立即吞 E0）。
- 首字节 C0/C1/F5..FF、连续字节 80..BF 立即出单元，从不缓存。
- 只有流结束时仍缓存的合法前缀才按规则 3 吞掉并报「截断」，故跨切分点绝不提前出 FFFD。
## 3. par 切点对齐
切点 c 可能落在任何字节上。从 c 向前最多回看 3 字节找最近的「非连续字节」j（0..7F 或 C2..FF）：
- j 处是 ASCII/非法首字节：单元长度 1 不跨切点，右段从 c 开始（head=0）。
- j 处是合法首字节：其单元可能跨 c；左段只负责到 j 之前，右段从 j 开始重解析
  （头部 c-j 字节仅驱动状态，不计统计）。
例：左段尾是 C3、切点在其后，右段以 80 80 开头。j 找到 C3：左段不负责 C3，
右段从 C3 解析，C3 80 合成 é，第二个 80 独立成单元，与单线程完全一致。
每个单元只属于一个段，故 FFFD 个数、统计必然相同。回看 ≤3 字节且每字节一次判定，
K=8 时总检查次数 ≤ n + 8×3。
## 4. 输出上限与断点
Write 以「单元」（标量/非法单元/BOM）为粒度推进。越限时：
- 当前单元及其后内容不输出、不计消费；n = 最后已提交单元结束偏移 − 已报告偏移；
- 残留在切分缓存（≤3 字节）里的字节不算已消费，n 恒非负；
- LimitError.Head 给出未提交的残留前缀（≤3 字节）；调用方用 Head + 未传完的尾部
  喂给新实例即可从断点续传，拼接与不限长逐字节相同。
检查次数：只有规则 1.2/1.3 的首字节会被重看一次（子表回扫，与切分无关），
缓存字节不重看，故 ≤2n，1 字节一段与整段喂入相同。
