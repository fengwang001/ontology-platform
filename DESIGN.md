# 条件请求前置判定器 — 设计推导

判定结果只有三种：继续执行（Proceed）、未修改（NotModified）、前置条件失败（Failed）。
依赖单向：`precond` → `etag` / `httpdate`，后两者互不依赖。

## 1. 为什么 If-Match 用强比较、If-None-Match 用弱比较

弱标签 `W/"v"` 只承诺语义等价，不承诺字节一致；强标签承诺字节逐位一致。
If-Match 服务于写请求（PUT/DELETE、断点续传），它要回答的是「你要覆盖的
就是我上次看到的那一份字节吗」。若两边都用弱比较，`W/"v"` 与 `"v"` 会被判等：
客户端基于旧字节算出的续传分片会被追加到一个语义等价但字节布局已变的新版本上，
服务端据此返回 412 的机会被错放成 2xx，文件被静默拼坏。
因此 If-Match 任一标签为弱即不等（强比较）。
If-None-Match 服务于读缓存，只需回答「表示是否还是语义上的同一份」，
弱比较即可，`W/"v"` 命中即 304，避免把语义未变的表示重复下发。

## 2. 为什么 If-Match 存在时忽略 If-Unmodified-Since

RFC 7232 §4.1/§4.2 规定：If-Match 存在时 If-Unmodified-Since 必须被忽略；
If-None-Match 存在时 If-Modified-Since 必须被忽略。两组之间 A 组
（If-Match / If-Unmodified-Since，失败→412）整体先于 B 组
（If-None-Match / If-Modified-Since，读→304，写→412）。
叠加判定的反例：客户端发 `If-Match: "new"`，资源当前确为 `"new"`
（ETag 命中），但 Last-Modified 晚于请求里的 If-Unmodified-Since。
叠加判定会因时间条件不满足给出 412，而规范要求 ETag 命中即通过——
客户端明确指定了「就是这个版本」，时间戳只应在没有 ETag 时充当后备。

## 3. 为什么同一「匹配」在读方法与写方法下结果不同

If-None-Match 匹配表示「你缓存里的那份仍然有效」。对 GET/HEAD，这正是
缓存校验成功的含义：服务器无需重发表示，返回 304 Not Modified。
对 PUT/POST/DELETE 等写方法，同一匹配表示「你要写入的对象已存在且
仍是那份」，继续写入会造成丢失更新/覆盖，必须以 412 Precondition Failed
拒绝（典型用法 `If-None-Match: *` 作「仅当不存在时创建」）。
因此 NotModified 与 Failed 是两种彼此可判定的不同结果，绝不能合并成同一个错误。

## 4. 相等时刻的归属

条件头里的时间戳精度只到秒，是客户端收到某版本的时刻 `t`。
If-Modified-Since 问的是「自 `t` 起**之后**有没有新版本」，
即 Last-Modified **严格晚于** `t` 才算已修改（`lm > t`，左闭右开）：
相等意味着该版本就是客户端已知的版本，返回 304。
若把相等算成已修改，秒级精度边界会让所有在同一秒内重验的缓存全部失效。
If-Unmodified-Since 问「版本是否不晚于 `t`」（`lm <= t`），
相等同样落在「未改动」一侧，因此通过。两条规则在相等点都把相等归入
「未修改」，只是后续动作（304 vs 继续）由头部用途决定。

## 附：解析失败与求值

- 格式非法的条件头部一律按「该头部缺失」处理（RFC 7232），不整体报错；
  解析错误本身仍可由 `httpdate` 包单独取得并区分四类。
- 无条件头部时直接 Proceed，判定器不调用注入时钟（时钟仅为未来过期策略预留）。
