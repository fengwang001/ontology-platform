# 条件请求前置判定设计

## 1. ETag 强、弱比较

强比较要求两个实体标签都表示字节级一致的同一实体。因此只要任一方带 `W/`，即使不透明值相同，强比较也必须失败。弱比较只要求两个实体在语义上等价，允许表示形式不同，所以双方弱标记组合不影响不透明值比较。

`If-Match` 用于防止“我基于的版本仍是当前版本”的写操作。若对弱标签也用弱比较，资源从 `W/"1"` 变成表示不同但弱值仍被视作相同的 `W/"1"` 时，断点续传或并发更新会被错误放行，导致字节范围接到不同实体上。故 `If-Match` 用强比较；`If-None-Match` 主要用于缓存再验证，只需判断语义版本是否仍是同一版本，用弱比较。

## 2. 条件组求值顺序

条件分为两组：`If-Match` / `If-Unmodified-Since` 是前置条件组；`If-None-Match` / `If-Modified-Since` 是缓存再验证组。先完整裁决前置条件组，再裁决再验证组。

同一组内，标签比时间更精确。若 `If-Match` 存在却再叠加 `If-Unmodified-Since`，当 ETag 不匹配但 Last-Modified 恰好未晚于 IUS 时，叠加法可能继续执行；规范要求 ETag 不匹配立即失败。反过来，ETag 匹配但时间戳因时钟精度、重新序列化或旧值而失败，叠加法又会错误拒绝。因此 `If-Match` 存在时必须忽略 `If-Unmodified-Since`；`If-None-Match` 存在时同理忽略 `If-Modified-Since`。

## 3. If-None-Match 的方法差异

`If-None-Match` 匹配表示“客户端持有的版本仍是当前版本”。GET/HEAD 的正常目标是获取当前表示；既然表示未变，服务器不应重复发送实体，返回“未修改”并可带验证头，这是成功的缓存再验证。

PUT/POST/DELETE/PATCH 等方法通常试图创建或改变资源。同一个“仍存在且版本匹配”的事实意味着防止丢失更新或避免重复创建的前置条件没有满足；继续执行会覆盖或重复变更。因此它必须是明确的“前置条件失败”，不能和读方法的“未修改”共用一个结果。

## 4. 修改时刻的相等边界

设资源最后修改时刻为 `M`，条件时刻为 `C`。

`If-Modified-Since` 问的是：自 `C` 表示的请求时点之后，资源是否发生过新的修改。修改集合应写作严格区间 `M > C`。当 `M == C` 时，该修改发生在客户端已知的时间边界上，而不是边界之后；HTTP 也将早于或等于该时刻的响应视为未修改。因此 GET/HEAD 可返回“未修改”。

`If-Unmodified-Since` 问的是：在 `C` 之后资源是否被修改。通过条件同样是 `M <= C`，所以 `M == C` 时没有“之后”的修改，前置条件通过。两条规则共享同一时间方向，只是分别服务于再验证和写前保护。
