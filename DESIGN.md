# DESIGN

## 1. 零计数 hunk 头的行号规则（语义第 4 条）

三个样例：`x\n`→`y\nx\n` 得 `-0,0`；`a b c`→`a b X c` 得 `-2,0`；`a\n`→空 得 `+0,0`。
若把数字解释为「将要插入的位置（1-based）」，样例二插入发生在第 3 行之前，应写 3，但 GNU 写 2，矛盾。
若解释为「插入点之前最后一行的行号」（即插入点前有 a 行）：样例一插入在文件头，前面 0 行 → `-0,0` ✓；
样例二插入在第 2 行之后，前面 2 行 → `-2,0` ✓；样例三新文件为空，插入点前 0 行 → `+0,0` ✓。
统一公式：设 hunk 在该侧的 0-based 起始下标为 s（消费掉的行数），则头中数字 a = s+1（b≥1，指向范围首行）或 a = s（b=0，指向前一行）。
b=0 时范围为空、没有「首行」，退化为「前一行的行号」与 b≥1 共用同一 s，规则无特例。

| b     | `-a,b` 写法                          | Apply 对起始下标的解释       |
|-------|--------------------------------------|------------------------------|
| b = 0 | a = 插入点前的行数（文件头插入为 0） | a（跳过 a 行，在其后插入）   |
| b = 1 | 省略 `,1`，a = 首行 1-based 行号     | a-1                          |
| b ≥ 2 | 写 `-a,b`，a = 首行 1-based 行号     | a-1                          |

## 2. hunk 合并阈值 = 2C（语义第 5 条）

每个 hunk 尾部保留 C 行上下文、头部保留 C 行上下文。设两段改动间未改动行数为 g。
g = 2C 时，前 hunk 的 C 行尾上下文与后 hunk 的 C 行头上下文恰好铺满间隙，合并后上下文连续、无重复无遗漏 → 必须合并。
g = 2C+1 时若合并，中间需显示 2C+1 行上下文，超出「每侧 C 行」的承诺；GNU 选择拆分，且中间 1 行在两个 hunk 中都不出现。
反证另两个取值：C=1、改动在第 1 与第 4 行（g=2），GNU `diff -U1` 输出单个 hunk `@@ -1,4 +1,4 @@`——若阈值是 C=1 会错误拆分；
g=3（=2C+1）时 GNU 输出两个 hunk 且中间行不显示——若阈值是 2C+1 会错误合并。故阈值为 2C：g ≤ 2C 合并，g ≥ 2C+1 拆分。

## 3. 最短脚本的并列选择（语义第 3 条）

Myers 每步在 k 对角线上取 x = max(V[k-1]+1, V[k+1])。并列（V[k-1]+1 == V[k+1]，即 V[k-1] ≥ V[k+1]）时固定取 V[k-1]+1，
即**优先删除**（向右走）；仅当 V[k-1] < V[k+1] 严格成立才插入（向下走）。回溯用同一不等式，保证正反一致。
效果：`a b`→`b a` 生成 `-a`、` b`、`+a`（保留 b），而非 `+b`、` a`、`-b`；hunk 内 `-` 行先于 `+` 行，与 GNU 输出一致。
规则不含任何随机或地图迭代，故同一输入 100 次生成逐字节相同。

## 4. 偏移应用后的后续 hunk（语义第 7 条）

要平移。补丁记录的 OldStart 是旧文件坐标；每应用一个 hunk，其后的行号都被改变。
平移量 delta 初始为 0；第 i 个 hunk 的查找中心 = OldStart_i + delta，再在该中心 ±F 内搜索。
若该 hunk 在偏移 k 处应用成功，则 delta += k + (newCount_i − oldCount_i)。
原因：k 补偿目标文本与旧文件的位置差，newCount−oldCount 补偿本 hunk 净增/净减的行数；
不平移时，前一个 hunk 净增 10 行会让后一个 hunk 在落后 10 行的位置附近搜索，超出 F 而误判失败。
