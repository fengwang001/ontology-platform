# DESIGN

## 1. 空行与单列空值
选择：**空行被跳过**，不作为记录；引号空行 `""` 是一条记录。理由：记录由「至少一个字段」构成，`\n` 单独出现时没有任何字段，把它当成 `[""]` 会让文件末尾换行不产生记录、中间却产生记录，自相矛盾。单列表里真正的空值记录写作 `""`（加引号空字段）。回写器因此必须：当表只有一列且该字段值为空（或字段本就带引号）时必须加引号——不加引号写出的 `\n` 重解析时会被当成空行跳过，记录丢失，往返失败。多列时空字段天然由逗号界定，无需引号。规范输入定义：行尾统一 `\n`、每条记录（含最后一条）都以 `\n` 结束、最小引号。

## 2. par 切点
每个切点左侧字节记为 `prev`。每段在两种假设下各跑一遍状态机（共 2 次/字节）：**OUT**=起点在引号外字段开始处（`prev=='\r'` 时为 CR 待定）；**IN**=起点在引号字段内（`prev=='"'` 时为「刚见引号」态，否则为引号态）。从左到右：第 0 段真相=OUT；用选中运行的末态（是否在引号内、末字节）查 2×3 表得到下一段真相（outside→start/unq/cr，inside→q/qs）。错误必由真实假设产生：在某段真实入口确定前其候选错误坐标不可能成为全局最小，确定后只保留选中运行；首字段跨段时按 carry 的字段内续接处理，其余事件字段号+carry 数、记录号+已完成记录数，字节偏移加段起点，即为全局坐标。非引号字段中段入口与 OUT 仅差「首字节为引号/裸 CR」两点，拼接时直接判定，不需第三次扫描。

## 3. CR 待定
未引号字段末见到 `\r` 进 `cr` 态但不发字段：下一字节是 `\n` 则结束记录；是其他字节则立即报裸 `\r`（位置=该 `\r`）。半包：`Feed` 结束于 `\r` 时状态挂起，下次 `Feed` 首字节完成判定；`Close` 仍在 `cr` 态则报裸 `\r`。par：`\r` 是段末字节时，OUT 运行以 CR 待定入口喂给下一段，判定由下一段首字节完成；`\r\n` 被切开时 IN 候选（若选中）把 `\r` 作引号内容、`\n` 进入 qs 判定，逻辑一致。引号内 `\r` 永远是内容，无待定。

## 4. 上限「立刻」
字段长度按**解码后**字节计数（引号内 `""` 只贡献 1 个字节）：未引号态每追加 1 字节后检查；引号态追加内容字节后检查、`qs` 态兑现转义引号的 1 字节后检查——第一个超限字节到达即报错，不等闭合也不缓冲整条记录。字段数在每发出一个字段后检查；记录数在每条记录开始时检查。终态后所有 `Feed` 返回同一错误。

## 5. 状态转移表（空白=追加到当前值；动作 end=发字段，rec=发记录）
| 状态 \ 字节 | `,` | `"` | `\n` | `\r` | 其他 | EOF |
|---|---|---|---|---|---|---|
| start 字段开始 | end→start | →q | rec→start | →cr | →unq | 结束 |
| unq 未引号中 | end→start | ErrQuote | rec→start | →cr | →unq | end/结束 |
| q 引号中 |  | →qs |  |  | (`"` 记转移义) | ErrUnclosed |
| qs 刚见引号 | end→start | append`"`→q | rec→start | append`"`→q | ErrJunk | append`"`+ErrUnclosed |
| cr 行尾CR待定 | (见下) | (见下) | rec→start | (按其他) | ErrBareCR | ErrBareCR |

`cr` 态除 `\n` 外的任意字节（含 `,` `"`）一律 ErrBareCR（逗号也属于「孤立 \r 后非 \n」）。所有错误位置=当前字节偏移，记录号/字段号为当前计数（从 1）。单线程计数器在状态机入口 +1；par 计数器为各 worker 两次运行之和，不含拼接。
