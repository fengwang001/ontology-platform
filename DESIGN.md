# CSV 方言设计

## 关键推导

1. **空行与单列空值**：空行是「无字段」，词法层直接跳过；`a\n\nb` 只有两条记录。单列空值必须至少有一个字段结束语义，写为 `""\n`：若写 `\n`，再解析会得到空行而非空值。多列空字段也用 `""` 标记，空字符串但无引号的字段只出现在非空记录中。
2. **CR 待定**：引号外见到 `\r` 只进入 CR 待定状态。续到 `\n` 则提交字段和记录；续到逗号或 EOF 是孤立 CR；续到普通字节可立即判定错误。引号内 `\r` 是普通内容；`\r\n` 原样入字段。半包在 CR 处暂停不报错，par 可把同步点放在 LF 后，从而不在段中继承 CR 待定状态。
3. **par 切点**：每段先从「引号外 / 引号内」两种初始假设扫描到段内首个完整行尾 LF，得到两个候选同步点；K 段按从左到右顺序，用上一段真实结束状态选择本段候选。再从同步点无重叠地词法解析。错误保留段内偏移，加上同步点全局基址；记录号、字段号由拼接器按完整记录累加。扫描总量至多两遍，解析再覆盖未跳区域，满足常数级额外开销。
4. **即时上限**：普通字节入字段前检查字段字节数；引号内每输出一个内容字节计数一次，转义对 `""` 只在第二个引号确认转义时增加一次。因此第 `MaxFieldBytes+1` 个逻辑字符到达时立即失败。字段数在接受新字段前检查；记录数在新记录首个字段出现前检查。

## 规范输入

空文件或仅含终结 LF 的文件为空表；每条记录以 LF 结束；不使用 CR、无前导空白保护；字段含 `,`、`"`、LF、CR 时加引号且 `"` 写成 `""`；无内容空行不出现；单列空值唯一字段写 `""`。

## 状态转移表

| 状态 | 逗号 | LF | CR | `"` | 普通字节 |
|---|---|---|---|---|---|
| 字段开始 S | 提交空字段，回 S | 跳过空行，回 S | 进 CR 待定 | 进引号字段，不存引号 | 开未引号字段并存字节 |
| 未引号 U | 提交字段，回 S | 提交字段和记录，回 S | 进 CR 待定 | 错误：裸引号 | 检查长度后存字节 |
| 引号 Q | 逗号、LF、CR 原样保存；CR 仍留 Q | 同左 | 同左 | 进引号待定 P | 检查长度后存字节 |
| 引号待定 P | 闭合并提交字段，回 S | 闭合并提交字段和记录，回 S | 错误：闭合后跟 CR | 输出一个 `"`，回 Q | 错误：闭合后杂字符 |
| CR 待定 C | 错误：孤立 CR | 提交字段和记录，回 S | 错误：孤立 CR | 错误：孤立 CR | 错误：孤立 CR |

EOF：S 且未开始记录不产生记录；U/P/C 提交最后字段并形成记录；Q 报未闭合；P 后的 CR 由 C 处理。闭合后逗号只表示该记录还有下一个字段。
