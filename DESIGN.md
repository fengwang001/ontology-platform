# POSIX Shell 分词设计

## 双引号内反斜杠

双引号保留大多数字符的字面形式，因此反斜杠默认也是普通字符。只有反斜杠紧邻仍保留特殊含义的 `$`、`` ` ``、`"`、`\` 时，它才消费自身并保留后一字符；紧邻换行时，`\<换行>` 是续行，两个字节都不进入词。其他组合（如 `\b`）同时保留反斜杠和后一字符。

## 空词与拼接

词不是由引号单独创建的“多个片段”，而是一个正在累积的词：无引号字面量、单引号片段、双引号片段都追加到同一词。遇到引号分隔符即标记词已开始；空白只在词已开始时结束词。因而：

- `""''` 是一个空词：第一个 `"` 开始词，后续引号只是继续拼接。
- `a""` 是一个词 `a`：空双引号不结束、分隔或新增词。
- 单独的 `\<换行>` 产生零个词：反斜杠只表示“下一字节待定”，换行随后作为续行被删除，自始至终没有字面量或引号开始词。

## 状态转移

状态：N 普通，S 单引号，D 双引号，E 普通反斜杠待定，DE 双引号反斜杠待定。类别中 B 表示空白，L 表示其他字节。

| 状态 | B | `'` | `"` | `\` | 换行 | L |
|---|---|---|---|---|---|---|
| N | 结束词 →N | 开始/续词 →N(S) | 开始/续词 →N(D) | 记待定 →E | 结束词 →N | 开始/输出 →N |
| S | 输出 →S | 关闭 →N | 输出 →S | 输出 →S | 输出 →S | 输出 →S |
| D | 输出 →D | 输出 →D | 关闭 →N | 记待定 →DE | 输出 →D | 输出 →D |
| E | 输出该字节 →N | 输出该字节 →N | 输出该字节 →N | 输出该字节 →N | 续行删除 →N | 输出该字节 →N |
| DE | 输出 `\` 和字节 →D | 同左 →D | 输出 `"` →D | 输出 `\` →D | 续行删除 →D | 特殊字节输出；否则输出 `\` 和字节 →D |

EOF 时：S 报单引号未闭合，D/DE 报双引号未闭合，E 报反斜杠悬置；偏移分别指向开引号或反斜杠。每个输入字节恰好令计数器加一。

## 语义测试对照

（实现测试后补齐。）
