# 设计推导

## 1. 规范化模型
输出是一串「原子」：Copy(n) 原样逐字节、Delete(n) 原文字节删除（输出零宽）、Insert(n) 输出新增（原文零宽）。
相邻 Copy 自动合并，因此区间数只随删除/插入点增长。

## 2. 四种样例 × 三种末尾策略
| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `"\n"` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

推导：EnsureOne 把「末尾零个或多个 `\n`」整体替换为一个。空输入若取 `""→""` 也幂等，
但无法保证「输出恰好以一个换行结尾」这一不变量；取 `""→"\n"` 时 `N("\n")="\n"`，仍幂等，故选后者。
TrimBlank 删除末尾空行（= 末尾连续换行除去最后一个）再保留一个换行；在第 2 条规则下
（纯空白行已变空行），它与 EnsureOne 对规范化结果等价，差别仅在配置语义，故两者输出列相同。

## 3. 被删字节的 ToOut
原子序列中 Delete 区间 `[p,p+D)` 对应输出中的一个点 `q`（下一个 Copy 原子的起点；文件末尾则为 `len(out)`）。
由「单调不减」：被删区间内所有 i 只能取同一个值，否则产生下降或人为跳跃。
由 `ToOut(ToOrig(o))==o`：点 q 同时是后续 Copy 的起点，规定 `ToOrig(q)=p+D`（删除点归属右侧字节），
则 `ToOut(p+D)=q` 成立；对被删字节统一规定 `ToOut(i)=q (p≤i<p+D)`。
编辑器语义：光标落在 `abc   \n` 的被删空白里，映射到输出中 `\n` 的位置（换行符之前、内容之后）；
`\r\n` 的 `\r` 同理映射到输出 `\n` 的位置。终点 `ToOut(len(orig))=len(out)`、`ToOrig(len(out))=len(orig)`。

## 4. 空白缓冲超限
强制把待定空白判为「非行尾」会让长空白串残留于输出，违反第 2、5 条（且结果依赖缓冲上限，破坏第 4 条）。
故选择**拒绝**：挂起空白达到上限即返回可判定的 `BufferLimitError`（带原文偏移），已输出内容保留，实例进入终态。
拒绝不产生结果，故不违反第 4/5/6 条；终态状态即「偏移 = 出错字节，错误类型可判定」。

## 5. par 拼接
各段独立规范化，非末段使用 OpenTail：段尾「连续空白 + 至多一个待定 `\r`」挂起不裁决、不产生原子。
段 j 的输入起点回退到上一段挂起后缀的起点（连续 `[ \t]`，再加紧前的一个 `\r`），
于是跨切点的 `\r\n`、被切开的空白串在段 j 内部被完整看到，裁决天然正确；
上一段恰好未输出这些字节，不重不漏，原子按 worker 顺序拼接即得全局原子流，坐标自动连续。
修正范围有上界：每个切点只涉及「1 个 `\r` 状态 + 1 串空白状态」共至多两个挂起状态（字节数虽可能很大，状态数有界）。
仅末段关闭 OpenTail 并套用末尾策略。

## 6. 复杂度与查询
span 内段按 orig、out 双坐标天然有序。`ToOrig`/`ToOut` 各自二分，比较次数记入非导出计数器。
纯 `\n` 文本无删除点，全表只有 1 个 Copy 段。
