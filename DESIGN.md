# DESIGN — 流式行尾/空白规范化 + 双向偏移映射

## 0. 记号与基本模型

字节流经过三层：`eol`（`\r\n`/`\r`/`\n` 统一产出 NL 事件，`\r` 后字节未到时处于 pending）、
`ws`（空格/制表符先存 pending，遇行尾丢弃、遇其他字节回吐）、末尾策略在 `Close` 应用。
映射以区间段 (origStart,outStart,length) 表示：length>0 为恒等平移；length=0 为插入点；
连续恒等平移合并，因此无删除的纯 `\n` 文本只有 1 段。

## 1. 末尾换行策略（空输入推导）

不变量：幂等 N(N(x))=N(x)，且幂等输出自身不再被删空白/`\r`。
- Keep：原样。空输入规范化无任何内容，输出 `""`（不会凭空造换行，否则再规范化会变化）。
- EnsureOne：目标态是「末尾恰好一个 `\n`」。空输入若输出 `"\n"`，则对空文件制造了内容；
  虽仍幂等，但「空→非空」是信息写入。选择：空输入例外输出 `""`；非空则缺则补、多则裁到一个。
- Trim：删全部末尾空行但保留一个换行。空行集只在文件含内容时有意义，故空输入输出 `""`。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

EnsureOne 与 Trim 规则统一为：先 Keep 规范化，空则空；否则裁掉尾部所有 `\n` 后恰补一个。

## 2. 被删字节的 ToOut（从不变量反推）

`ToOrig` 单调不减，且要求 `ToOut(ToOrig(o))==o`（每个输出偏移都要被映回）。
- `\r\n` 中的 `\r` 被删，`\n` 保留于输出位置 o。被删 `\r` 右侧最近的可映回原文点就是
  `\n` 的位置，故 ToOut(\r)=o（换行位置）。
- 行尾空白全部删除，其后换行在输出位置 o。空白串内任意偏移映射到 o；光标落在被删空白里时
  落在换行位置（编辑器按边界插入即换行前）。映到 o-1 会使换行位置失去唯一原像，违反互逆。
插入的尾换行（EnsureOne 补）无原文字节：ToOrig 映到 origLen，该点 ToOut=输出终值，仅 origLen
这一端点取终值（文档注明）。

## 3. 空白缓冲超限

强制把 pending 空白判为「非行尾」并输出，会违反语义 2（其后真到行尾则本应删除）与幂等
（已吐出的空白第二次运行会被删）。选择拒绝：返回哨兵 `ErrSpaceLimit`（带原文偏移），已输出
内容保留，实例进入终态（后续 Write/Close 返回 `ErrClosed`）。语义 2/4/5/6 均不受损：拒绝发生
在判定前，已提交前缀与映射自洽。输出上限同理返回 `ErrOutputLimit`。

## 4. par 拼接（为何修正范围有上界）

切点可能在 `\r|\n`、空白串中间。每段不独立截断，而按行对齐：worker j（除末段）处理到
「>=切点 j 的第一个 `\n` 的下一字节」；首段从 0 开始。每个输出段起点都落在 `\n` 之后，跨切点的
`\r\n`、空白整体归入唯一 worker，无待定状态泄漏。段内喂入哨兵字节（下一段首字节）阻止末尾被
提前判行尾，随后丢弃哨兵产出。末尾策略只作用于最后一个 worker。映射平移：段 (a,b,n) 加
(origBase,outBase)；段输出区间取 worker 输出中 [ToOut(start),ToOut(cut))。修正范围上界：每个切点
至多回退「切点所在行的那一行」，即受限于单个最长行，与段数无关；总工作量线性，K goroutine 并发，
各实例独立，`-race` 干净。
