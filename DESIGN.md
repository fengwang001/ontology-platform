# 有预算的 BFS 图遍历器 — 设计

## 1. 预算单位：为什么是「访问的节点数」

预算是遍历者向调用方承诺的输出规模，故以**实际访问（产出）的节点数**计量，
而不是「考察的边数」。

边数会随图的**存储形态**漂移：同一逻辑图，一条邻接关系可以存成一条边，
也可以因规范化方式不同（重复边、压缩边、附带属性行）产生不同的存储边数；
环与自环的去重时机不同，被考察的边数也不同。这样"预算 100 条边"在两份
等价数据上会停在不同的节点处，截断点不再由 `(图, 起点, 预算)` 唯一决定。
节点数是逻辑语义单位，与存储无关；且空图、自环、重复边在节点计数下行为一致。

## 2. 截断点确定性必须固定的三处顺序

1. **出边顺序**：邻接表按目标 ID 字典序排序（`graph` 在写入时排序）。
2. **同层内入队顺序**：严格 FIFO 队列；一个节点的出边按序扫描，新发现的
   子节点按该顺序入队，不得用 map 迭代。
3. **预算扣除时机**：仅在一个节点被确定访问（产出）时扣 1；展开边不扣预算。

三者固定后，输出序列是 `(图, 起点, 预算)` 的纯函数，重复执行逐元素相同。

## 3. 续点为什么不能只记最后一个节点

BFS 在截断瞬间的状态包括：

- **队列**：已发现、尚未访问的节点（同层剩余节点 + 已入队的下一层节点）；
- **已发现集合 seen**：已入队/已访问的节点，保证环、自环、重复边不重复访问。

只记"最后一个节点"会丢失同层尚未出队的兄弟节点，续传会从错误位置深搜下去，
无法再恢复 BFS 序。因此续点必须编码 `(队列(每项含其出边扫描游标), seen, done)`，
即一份完整的机器状态；done=true 表示图已遍历完。

## 4. 队列驻留有界：游标式展开

朴素做法是访问中心节点时一次性把全部出边入队，星形图（1 中心 + 10000 叶子，
预算 100）会立刻驻留 10000 项。本设计给每个队列项配**出边扫描游标**：
节点排在队首时每次只推进一小段，且**仅当还有剩余预算时才允许把新节点入队**，
预算用尽立即挂起。扫描到的边计入 edgesExamined 且绝不重扫。

每个预算名额最多对应队列中一个节点项，故峰值队列 ≤ 预算 + 常数（实现中
`4*budget` 是宽上界，星形图预算 100 实测峰值 ≤ 101）。

## 5. 复杂度与计数器（walk 包非导出）

- visited：精确等于 `min(预算, 可达节点数)`；
- peakQueue：队列驻留峰值；
- edgesExamined：被考察边数，≤ 已访问节点出度之和（每条边至多考察一次）。

## 6. 续点编码（resume 包，仅标准库）

二进制格式：`magic(4) | ver(1) | done(1) | seenCount(4) | seen{len,id}...
| qLen(4) | queue{len,id,cursor(4)}... | hmac(32)`

HMAC-SHA256 密钥由图内容派生（节点与有序邻接的 SHA256），因此图被改动后旧续点
校验失败；任何密钥外篡改也失败。比特翻转三分类（按检查顺序）：
**校验失败**（HMAC 不过）/ **字段不完整**（长度或结构解析不过）/
**引用不存在节点**（seen、队列项引用图中缺失 ID）。

## 7. 边界与错误

- 预算 0：返回空序列，续点 == 初始续点（幂等）；
- 预算 ≥ 可达规模：遍历完整图，done=true；对 done 续点再续传返回空且不报错；
- 起点不存在：walk 返回可 `errors.Is` 的错误；
- 续传间删除队列中节点：resume 解码/校验时返回引用缺失错误并指出节点 ID；
- 三类错误用哨兵错误 ErrChecksum / ErrIncomplete / ErrMissingNode 区分，
  walk 的起点缺失同样以 ErrMissingNode 归类。

## 8. 并发

Graph 构造后不可变；Walk 是纯函数：每次从 State 派生独立副本，计数器只存在于
本次返回的 State 上，协程间不共享可变状态，故并发续传结果一致、-race 干净。
