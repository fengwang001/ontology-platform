# 随机超平面 LSH 分桶检索器 — 设计与推导

## 1. 签名规则
对维度 d 的向量 x，第 l 张表第 j 位的法向量为单位化高斯向量 w（由种子
`seed + l*prime1 + j*prime2` 确定性生成）。令 v = <x,w>：
- v > 0 记 1；v < 0 记 0；
- **v 恰为 0**（含零向量）记 0（约定 `math.Copysign(1,v) >= 0 -> 0`）。
一张表 b 位拼成 64bit 签名（高位优先），b <= 64。

## 2. 碰撞概率与汉明距离
两向量夹角为 θ，P(某位相同) = 1 − θ/π。整签名相等的概率
`p(θ) = (1 − θ/π)^b`。期望候选数 C ≈ n·p：
- b ↑ → p ↓ → **候选数单调下降**；
- b ↑ → 真近邻（θ 小，p 接近 1）与远邻（θ 大，p 小）区分更好，但
  真近邻单表被命中的概率也下降，**单表召回随 b 下降**。

## 3. 表数 L 与召回
L 张独立表（种子不同 → 法向量独立），候选为各表桶并集。真近邻至少
被一张表命中的概率 `R_L = 1 − (1 − p_near)^L`：
- L ↑ → 召回单调不降，趋向 1；
- 候选数 C_L ≤ n·(1 − (1 − p_avg)^L)，次线性增长，远小于 L·n。
结论：b 控桶粒度（候选 vs 精度权衡），L 用并集补召回。测试以
b∈{4,8,12}、L∈{1,2,4,8} 实测验证上述两个单调方向。

## 4. 检索流程
查询 q：逐表算签名 → 取桶内 ID 并集（去重）→ 仅对候选算欧氏距离
精排 → TopK。精排距离计算数由非导出计数器记录；空候选时退化为
空结果（不回退全扫）。暴力基线 = 全量距离排序，用于算 top10 召回
`|近似∩精确|/10`。

## 5. 计数器与复杂度
建索引时每个 (向量, 表, 位) 恰好做一次内积符号判定，哈希计数
= N·L·b（精确断言）。精排候选数在 5000 向量数据集上断言 ≤ 500
（≤10%，证明未退化为暴力扫描）。

## 6. 可复现性
超平面 PRNG 为 `math/rand.New(rand.NewSource(seed))`，生成顺序、
单位化方式固定；同种子生成的法向量逐位相同，故同种子两次建索引
签名表逐字节相同；不同种子以高概率不同（测试断言存在差异）。

## 7. 并发
每张表用独立 RWMutex。建表在写锁内完成后整体替换该表；查询在
RLock 下读取，未建完的表对查询不可见（全有或全无语义）。

## 8. 边界语义
空集合返回空；K>N 返回全部；全同向量同桶，候选=N，召回=1；
含 NaN/±Inf 的向量被拒绝并计入 skipped（NaN 会污染距离）；
零向量合法（内积恒 0，签名全 0，见 §1）；d=1 合法。

## 9. 落盘格式（自描述）
固定 64 字节小端头：magic(4)="LSH1"、ver(u16)、flags(u16)、
dim(u32)、bits(u32)、tables(u32)、nvec(u32)、nhyper(u64)、
nbuck(u64)、nvecbytes(u64)、crc32(u32)、保留(u32)。
其后三段：hyper（表×位 个 float64 法向量，每个含 dim）、
buckets（每张表 u32 桶数，每桶 u64 签名 + u32 计数 + ID 列表）、
vectors（dim 个 float64/向量）。crc32(IEEE) 覆盖三段字节。
全零法向量为退化超平面，读回时报 `ErrDegenerateHyperplane` 并指出
表号/位号。维度不符报 `ErrDimMismatch`（期望/实际）。
四类截断错误：`ErrHeader`/`ErrHyper`/`ErrBuckets`/`ErrCRC`，
均可 `errors.Is`；恢复时剔除引用不存在向量的桶 ID 并计数。
