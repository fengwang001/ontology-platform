# DESIGN：带替换的流式 UTF-8 ⇄ UTF-16 转码器

## 1. 非法单元切分规则（由七样例推导）

逐字节扫描，每个字节归属于且仅归属于一个「单元」：
- `00..7F`：长度 1，合法。
- `80..BF`（孤立延续）、`C0 C1 F5..FF`（绝非法首字节）：长度 1，非法。
- `C2..DF F0/F4` 等首字节：期望 `n` 个延续字节；先校验紧邻的第二字节（`E0` 需 `A0..BF`，
  `ED` 需 `80..9F`，`F0` 需 `90..BF`，`F4` 需 `80..8F`，其余 `C2..DF/E1..EC/EE..EF/F1..F3`
  需 `80..BF`），不合法则只吞首字节 1 个；否则取满 `n` 字节，其中任一延续字节不是 `80..BF`
  或流提前结束，则**已取到的全部字节（首字节起）为一个非法单元**，在第一个坏字节处停止，
  坏字节留到下一单元。

据此：`F0 90 80 41` 第二字节合法、第三字节坏 → 吞 `F0 90 80` 共 1 单元，`41` 另算；
`E0 80 80` 第二字节超界 → 只吞 `E0`，两个 `80` 各自孤立 → 3 个；`ED A0 80`（代理）、
`C0 AF`（`C0` 非法首字节，1）、`F4 90 80 80`（第二字节超界）同理逐字节计；
`E2 82`+EOF：第二字节合法，第三字节缺失，已取到的 2 字节是一个单元 → 1 个 FFFD，不是 2。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 1（仅首字节） |
| `E0` | `A0..BF` | 1 |
| `E1..EC` | `80..BF` | 1 |
| `ED` | `80..9F` | 1 |
| `EE..EF` | `80..BF` | 1 |
| `F0` | `90..BF` | 1 |
| `F1..F3` | `80..BF` | 1 |
| `F4` | `80..8F` | 1 |
| `80..BF` | —（孤立延续） | 1 |
| `C0 C1 F5..FF` | —（永非法） | 1 |

## 2. 切分缓存上限与提前判定

最长字符 4 字节。只有「首字节+合法第二字节，且后续可能都是延续字节」时才需等待，最多再等
2 个字节，故缓存硬上限 = 3（首字节 + 至多 2 个延续字节）。第二字节一旦不合法可立即判首字节
非法、不必等 EOF；第三字节起一旦不是延续字节，已缓存前缀立即判为一个非法单元。UTF-16 同构：
需缓存至多半个单元（1 字节）或「高代理 + 半个下一单元」= 3 字节，上限同样 3。

## 3. par 切点对齐

段只在「同步锚点」开始：UTF-8 锚点 = 非延续字节（`<80` 或 `>=C0`）。给定字节切点 `c`，
段起点向前回退至多 3 字节找最近锚点；再用第 1 节规则从锚点本地判定：若锚点单元会越过 `c`
（坏尾或缺失尾），说明该单元被切开，起点再前移到该单元之后（即 `c` 本身附近的下一锚点）——
原则：左段一旦在某单元上输出（哪怕 FFFD），该单元归左段；拿不准就整单元让给右段。
`C3 | 80 80`：锚点 `C3` 在左，其单元跨过切点且缺/坏尾，左段不碰 `C3`，`C3 80` 整体交右段，
右段再独立处理剩下的 `80`，故 FFFD 总数（2）与单线程一致。回看恒 ≤3，检查次数只多常数。
UTF-16 锚点 = 偶数偏移；高代理跨切点时整对（或整个高代理）交右段，孤立代理不被错配。

## 4. 上限断点

`Write` 在**发射每个标量前**检查剩余额度，不够则停止，返回的 `n` = 已发射标量覆盖的输入字节数
（含其吞掉的字节与已丢弃 BOM）；仍在缓存里等待判定的字节不计入 `n`，原样交调用方续传。
因只在标量边界停，新实例从 `data[n:]` 续传，拼接逐字节等于无限流。

## 5. 其他

仅流首 BOM 参与定序（UTF-16 `FF FE/FE FF`，或配置固定序）/可丢弃；其后 `FEFF` 普通保留。
严格模式四类哨兵错误：非法字节（带偏移与长度）、截断（带偏移，与非法可区分）、超限、终态写入。
检查次数：每个输入字节在「离开缓存被定终局」时计 1 次，缓存中等待不计；故 ≤2n（UTF-8 坏前缀
至多 2 看，UTF-16 同），1 字节喂入不产生回扫。`stream.Transformer` 非并发安全。
