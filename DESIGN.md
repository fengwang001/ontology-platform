# 设计推导

统一格式（unified diff）差分/渲染/补丁应用器。行保留原始行尾：每行为
`内容+(\n|\r\n)`，末行可无行尾。

## 1. 零计数时的行号（语义 4）

hunk 头 `-a,b` 描述旧文件中从第 a 行（1 基）起的 b 行。纯插入（b=0）在旧文件
不占行，GNU 把 a 取为「插入点前一行的行号」，可为 0：
在文件头插入时前面没有行 → `-0,0`；在第 2 行后插入 → `-2,0`。
即 a 是前一条上下文行的旧行号（无上下文时 a=0）。`Apply` 读到 b=0 时把插入点
放在第 a 行之后（a=0 即文件头），不消费任何旧行。新文件侧对称：d=0 时 c 为
删除点前一行的新行号。

| b | 书写 | Apply 的解释 |
|---|------|--------------|
| 0 | `-a,0`（a 可为 0，必须显式写 0） | 插入点在旧第 a 行之后，不消费旧行 |
| 1 | `-a`（省略 `,1`） | 替换/校验旧第 a 行这 1 行 |
| >=2 | `-a,b` | 从旧第 a 行起消费 b 行 |

## 2. hunk 合并阈值（语义 5）

结论：相邻两段改动间未改动行数为 g，`g <= 2C` 时合并，否则分开。
GNU `diff -U C` 给每段改动各取 C 行上下文，两侧上下文重叠或相接（前段后 C 行
与后段前 C 行共需 2C 行，`g <= 2C`）时成一个 hunk。
反例 C=1，`g=3`：取 2C+1=3 会合并，但 GNU 输出两个 hunk；取 C=1 在 g=2
（C<2<=2C，GNU 合并）时会错误分开。故阈值恰为 2C。

## 3. Myers 并列选择（语义 3）

同一编辑距离 D 存在多条最短路径。固定规则：在每个 k 对角线推进 V[k] 时，
若「向下（删除）」与「向右（插入）」得到相同行号，优先向下（删除优先）。
因此 hunk 内并列情形的 `-` 行排在 `+` 行之前，如 `a b -> b a` 输出
`-a`、` b`、`+a`（而非先插入）。回溯由终点反向恢复，删除/插入顺序由该
优先级唯一确定，结果确定、可复现。

## 4. 偏移后后续 hunk 的平移（语义 7）

设前序 hunk 实际应用位置相对其记录位置的净偏移为 bias，且前序 hunk 使行数
变化 carry = 插入数−删除数。下一个 hunk 的查找中心 =
该 hunk 记录的旧起始行 + bias + 「记录中前序 hunk 之后本该发生的 carry」，
bias 在该 hunk 自身定位成功后更新为（实际起点−记录起点）。即每个 hunk
都基于「前面所有 hunk 真实落点」平移，避免已应用改动造成的漂移，同时只在
中心 ±F 行内搜索；多候选取 |偏移| 最小，相同取靠前。
