# 设计推导

## 1. 空行与单列空值
选择：**空行一律跳过，不产生记录**。`a\n\nb\n` 只有两条记录；流尾的最后一个换行也不产生记录。
原因：词法在「记录开始即遇行终止符」时无法区分「空白行」与「单字段空值 + 换行」，二者字节形态相同；若空行产生记录，则空表与单行空表无法分辨。
推论：单列表中「唯一字段为空」的记录，回写时**必须写成 `""` 再加换行**。若写成裸空（`\n`），重新解析时该空行会被跳过，记录丢失，违反往返。
规范输入定义：字段仅在必要时加引号——含 `,`、`""` 中的 `"`、`\r`、`\n`，或「单列记录的唯一字段为空」；行终止一律 `\n`；每条记录（含最后一条）后一个 `\n`。

## 2. par 切点
每段用两种初始假设各算一遍：`out`（起点在引号外、新字段开始）与 `in`（起点在引号内、延续上一段未闭合引号）。
真理由引号计数唯一确定：段内 `"` 的奇偶（`""` 成对，计数为 2）决定离开本段时的引号状态；前缀异或即得每段起点是否在引号内。故每段只有一种假设与全局一致，事件流逐字节相同。
拼接：按确定的假设重放事件；段界落在裸字段中时，上段末字段与下段首字段合并（值拼接、偏移取并集、引号标记取真）；落在 `\r` 与 `\n` 之间时，把下段首字节当作待定 CR 的判定字节。错误坐标：词法错误只带全局字节偏移（段偏移+段内偏移），记录号/字段号由全局 assembler 在交付时编号，无需换算。双假设每字节至多两遍，符合 ≤2N 预算。

## 3. CR 待定
未引号态遇 `\r` 进入 `crPending`，不产出、不报错。下一字节是 `\n`→行终止（消费该 `\n`）；否则→孤立 CR 错误（偏移指向 `\r`）。半包：`Feed` 可结束在 `\r`，状态跨调用保留，`Close` 时仍处 `crPending` 即报错。par：CR 是段最后字节时事件不闭合，按「段界待定」交给下段首字节裁决。引号字段内 `\r` 是普通内容，永不到达此状态。

## 4. 上限「立刻」
字段字节按**解码后内容**计数：普通字节 +1，引号态中的 `""` 作为一个 `"` +1；每读一个字节先增计数再与上限比较，首个超限字节即返回错误，不缓冲整条记录。字段数在字段闭合事件时计数；记录数在记录闭合事件时计数。超限即终态，错误被记忆，后续 `Feed` 返回同一错误。

## 5. 状态转移表
动作：F=发出字段，R=发出记录，内容=追加解码字节。

| 状态 \ 字节 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| fieldStart | F→fieldStart | →quoted | R→fieldStart | →crPending | →bare(内容) |
| bare | F→fieldStart | ErrBareQuote | R→fieldStart | →crPending | →bare(内容) |
| quoted | 内容 | →quoteSeen | 内容 | 内容 | →quoted(内容) |
| quoteSeen | F→fieldStart | 内容→quoted | R→fieldStart | →crPending | ErrAfterQuote |
| crPending | F→fieldStart | ErrLoneCR | R→fieldStart | ErrLoneCR | ErrLoneCR |

`Close`：quoted→ErrUnclosedQuote；crPending→ErrLoneCR；fieldStart 无字段→空（跳过尾换行/空行），有字段→F,R；bare/quoteSeen→F,R。
