# 三态熔断器设计推导

仅用标准库；时间单位毫秒，全部由构造注入的 `now func() int64` 提供。
依赖方向单向：`outcome <- tally <- state <- breaker`，无反向依赖。

## 1. 半开判定（推导过程）

设半开探测配额为 Q（同时在途探测的硬上限）。半开期每完成一个探测：

- 失败 1 次 → 立即重新断开。理由：半开的目的就是试探下游是否恢复，
  任何一个失败都证明尚未恢复，越早断开越安全；与 Q 无关，Q=1、Q=3 同样成立。
- 成功累计到 Q 次（且期间 0 失败）→ 恢复闭合。

不会卡死：Q=1 时，唯一探测成功即闭合、失败即断开，一步必有结论。
Q=3 时，最坏情况为 3 次成功后闭合；只要有一个失败立刻断开，永远不会
出现「配额耗尽但无结论」。被配额拒掉的调用不占探测、不计结果（见第 4 条），
因此不消耗这 Q 次机会。

## 2. 冷却边界（推导过程）

设断开时刻 t0、冷却时长 d。采用「到期即生效」的左闭右开：

- 断开区间为 [t0, t0+d)，即 now < t0+d 仍断开；
- now >= t0+d 的那一刻已进入半开。

结论：经过恰好 d 毫秒的那一拍，状态为半开。这样转移时刻对确定序列
可复算：每个外部事件先以当前 now 做惰性转移，再处理事件本身。

## 3. 统计窗口（推导过程）

在「最近 N 次调用」与「最近一段时间」之间选前者：定长计数窗口（环形桶）。

- 时间窗口在长时间无调用后突然来一次失败时，失败率=1/1=100%，
  样本只有 1 也可能触发断开，属误断开。
- 计数窗口只看最近 N 次，天然与时间解耦；为进一步防止样本太少误断开，
  约定「窗口未填满 N 个真实样本时不断开」。因此一次孤立失败的失败率
  虽为 100%，但样本数 < N，不断开。

tally 用环形桶 + 增量 success/failure 计数：记录时只覆盖被挤出的一个旧桶，
计算失败率为 O(1)，不遍历窗口（访问桶计数实测为 0，上限 4）。

## 4. 半开期被拒调用（推导）

不变量 3 要求被拒不计入成败。半开期超出在途配额的调用根本没有发出，
与断开期被拒性质相同，故：不计入 tally、不占探测槽、不计入成功/失败。

## 5. panic 与在途归还

探测函数 panic 视为一次真实失败（它确实到达下游并异常返回），然后原样
re-panic；探测槽用 defer 保证归还。panic 后 tally/在途均不留脏状态。

## 6. 状态转移表

当前状态 × 事件 → 新状态（事件按到达顺序，先按 now 做惰性到期转移）：

| 当前 | 事件 | 新状态 | 动作 |
| --- | --- | --- | --- |
| 闭合 | 真实结果成功/失败，窗口未满 N | 闭合 | 计入 tally |
| 闭合 | 窗口满且失败数/N > 阈值 | 断开 | 记录断开时刻 t0 |
| 闭合 | 窗口满但失败率未超阈值 | 闭合 | 计入 tally |
| 断开 | 任意 Call 且 now < t0+d | 断开 | 拒绝，不发调用，不计数 |
| 断开 | 任意 Call 且 now >= t0+d | 半开 | 消耗 1 个在途槽后按半开处理 |
| 半开 | 新 Call 且在途 < Q | 半开 | 发探测，在途+1 |
| 半开 | 新 Call 且在途 = Q | 半开 | 拒绝，不发调用，不计数 |
| 半开 | 探测失败（含 panic 结果） | 断开 | 在途-1，记录新 t0 |
| 半开 | 探测成功，成功数 < Q | 半开 | 在途-1，成功数+1 |
| 半开 | 探测成功，成功数 = Q | 闭合 | 在途-1，重置 tally（旧统计清空） |
