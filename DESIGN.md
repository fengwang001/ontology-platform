# Quoted-Printable 编码设计（RFC 2045 子集）

## 推导一：行末空白为何必须转义，以及判定方法

空格/Tab 本身可字面输出；但物理行尾的空白在传输中可能被增删，解码器无法区分
“行末空白后换行”与“换行”，因此空白位于硬换行（输入 `\n`，含 `\r\n` 中的）或
输入结束之前时必须写成 `=20`/`=09`。判定只需对输入向前看 1 字节：空白字节的
后继存在且不是 `\n` 时字面输出，否则转义。单独的 `\r` 是普通字节（编码为
`=0D`），不是换行，所以 ` \r` 中的空格后继不是 `\n`，仍字面输出。

## 推导二：软换行位置与“不得拆开 =XX”

每个输入字节映射为宽度 1（字面）或宽度 3（`=XX`）的 token；软换行 `=\r\n` 占 3
字符，但 `\r\n` 不计入行宽，故插入前当前列 c 必须满足 c≤75（插入后含 `=` 恰为
76）。规则：放宽度 w 的 token 前，若该逻辑行其后还有 token（后继不是硬换行也不是
EOF），要求 c+w≤75，否则先插软换行；行末（后继为硬换行/EOF）放宽到 c+w≤76。
这同时保证 `=XX` 三个字符永远相邻：c=74 遇到 w=3 的 `=3D` 时 74+3=77>75，先
`=\r\n` 再输出 `=3D`；也保证任何已输出行（不含 CRLF）不超过 76。

## 推导三：软换行前的空格算不算行末空白

结论：**不算，保持字面空格**。严格解码时 `=\r\n` 被整体删除，该空格后面紧跟下一
行的解码内容，并不处在解码结果的行末；解码器删除的是软换行的 `=`（连同
`\r\n`），从不删除空白。转义成 `=20` 不仅没必要，还会把 1 字符撑成 3 字符、提前
触发换行。故“行末空白”只依据后继是否为硬换行/EOF，与是否插软换行无关。该情形
确实会发生：74 个可打印字符后接空格再接需转义字节时，空格落在第 75 列、`=` 落
在第 76 列。

## 往返等式（语义 6 的准确形式）

令 N(x) 为把 x 中每个 `\n` 规范化为 `\r\n` 的结果（`\r\n` 只算一个换行，单独
`\r` 保留）。则对任意字节串 x：`Decode(Encode(x)) == N(x)`。编码器永不输出裸
`\n` 或裸 `\r`，故其输出必被严格解码器接受。

## 最小转义（语义 7 的理由）

编码器对每个字节只有三种转义理由：字节是 `=`；字节不在“可打印 ASCII 或
空格/Tab”集合；空白后继为硬换行/EOF。决策是单字节局部判定，字面字符宽度为 1 时
绝不写成 `=XX`，软换行也只在行宽约束要求时出现。因此 y 与
`Encode(Decode(y))` 的差异只能来自 y 中把可打印字符多余地写成了 `=XX`（解码后
重编码变回字面），以及这种 3→1 宽度变化连锁引起的软换行位置移动；软换行无解码
语义，移动不影响解码结果。

## 语义—测试对照表

<!-- 待实现完成后补齐 -->
