# 分层配额分配器：设计推导

记号：叶子 L 保障 g、占用 u，请求大小 r（任务不可切分），全局闲置 f。

## 1. 抢占额度

L 的合法占用上限是 g（保障线之内才有权要回；第 4 条）。先吃掉闲置：f 单位可直接借。
仍缺 d = max(0, r - f)。能抢的只有“别人占用超过它自己 Min”的借入部分，且 L 最多只缺到 g：

  need = min(d, g - u)

- 减 f：闲置容量本来就空闲，抢它只会制造无谓迁移，且不影响超发约束。
- 以 g-u 封顶：第 4 条，只有请求后占用 ≤ Min 的缺口可抢；超过 Min 即超额借用，无权驱逐任何人。
- r > g-u 时：整体挂起（任务不可切分，不能先分一部分）；按 need 发起抢占，容量凑齐 r 后才整体满足；
  不足部分只能等更多借入者出现，绝不驱逐到 Min 以下。

| g | u | r | f | need | 结果 |
|---|---|---|---|---|---|
|10| 0| 3| 5| 0 | 立即分配（全用闲置） |
|10| 0| 8| 5| 3 | 抢 3，凑齐 8 后分配 |
|10| 8| 4| 1| 2 | 抢 2（只回到 Min=10），整体挂起等再 1 |
|10|10| 5| 2| 0 | 不抢，挂起等闲置 |
| 5| 0| 9| 2| 3 | 抢 3 仍缺 4：挂起，不越 Min 抢 |

## 2. 受害者选择

候选=占用 > Min 的叶子，headroom = used - Min（该叶子最多可被抢到 Min）。
反例：要回 3，某叶子借入 4，但其最后启动任务 size=5：抢它后 used 降到 Min-1，违反第 5 条。
规则：在当前缺口下反复选“可整体抢下且 size ≤ headroom”的叶子内任务，先取 size 最大但不超过 headroom
者（一次满足、尽量少抢任务）；同值取启动更早（确定性）。每抢一个任务，从缺口与该叶子 headroom 减去
其 size。任一步没有可行任务而仍有缺口：返回不可行，按第 6 条一个通知都不发。
“只抢一部分任务”非法：任务不可切分，size>headroom 的任务对当前 headroom 永不可选。

## 3. 跨层借用归属

借入按节点逐层定义：节点 used>Min 才算该节点借入；父占用恒等子占用之和（第 2 条）。
部门 P 下 A 借给 B：A used<Min，B used>Min，但 P 的 used 未变，P 对兄弟部门不构成借入。
回收自内向外：先在最近公共祖先（LCA，叶子对自己即其本身）之内找借入叶子，不够再扩大到父级、跨部门。
两层反例：P(A Min10,B Min10)、Q(C Min10)，各用 5；B 多借 5。若把 P 整体记为“借入 5”，A 要回 5 时
会错误去抢 C（跨部门驱逐），而正确归属下 B 在 P 内、先抢 B，C 不受影响。

## 4. 在途容量与挂起请求

抢占通知发出即在途：该容量既不在受害者可用账上，也不可再分配（第 1 条）。在途释放（Ack 或超时，
超时左闭右开 now>=deadline 即强制回收）后并入全局闲置，然后按挂起队列 FIFO 重试：从队首开始，
队首可满足才分配并继续看下一个，队首不满足则停止——保证更早请求先拿到容量（第 7 条）；
在途归属触发它的请求的方式，就是“释放后统一按提交顺序重放”，请求最迟在其全部抢占确认/超时后满足。
