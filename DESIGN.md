# DESIGN — 统一格式 diff/patch 的四处推导

## 1. hunk 头零计数时的行号（语义 4）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0`；`a\n`→空得 `+0,0`。
三处插入/落空位置分别是「第 1 行前」「第 2 行后」「第 1 行前」，写出的数字是 0、2、0。
若把数字解释为「将要插入的位置（1 基）」，样例二应写 3 而非 2，矛盾；若解释为「插入点之前
最后一行的行号」（即该侧文件中位于 hunk 之前的行数），三个样例全部吻合，且文首插入自然得到 0。
结论：count>0 时 start 是首行的 1 基行号；count=0 时 start 是「前一行的行号」。
统一公式：`start = (hunk 之前已消耗的行数) + (count>0 ? 1 : 0)`。
`Apply` 的插入点（0 基下标）：count>0 取 `start-1`，count=0 取 `start`（即第 a 行之后）。

| 情形 | 头写法 | Apply 对插入点的解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一行行号（文首为 0） | 在第 a 行之后插入，下标 a |
| b=1 | `-a`（省略 `,1`） | 从第 a 行起，下标 a-1 |
| b≥2 | `-a,b` | 从第 a 行起共 b 行，下标 a-1 |

## 2. hunk 合并阈值（语义 5）

改动间距 g（中间未改动行数）。C 行上下文时，前一改动的尾上下文占 g 行中的 C 行，
后一改动的头上下文占 C 行；g ≤ 2C 时两段上下文相接或重叠，合成一个 hunk；
g = 2C+1 时中间恰有 1 行两边都够不到，必须拆开并省略该行。
实测 GNU diff（C=1）：g=2 输出 1 个 hunk（`@@ -1,4 +1,4 @@`），g=3 输出 2 个。
若阈值取 C：g=2 会被拆开，与实测矛盾；若取 2C+1：g=3 会被合并，合并后的 hunk
中间含 3 行上下文，其中间行距任一改动都超过 C，GNU 从不产生这种 hunk，矛盾。
结论：阈值 = 2C，即 g ≤ 2C 合并，g ≥ 2C+1 拆分。

## 3. 最短脚本的并列选择（语义 3）

Myers 前进时，对角线 k 上若 `V[k-1] ≥ V[k+1]` 选右移（删除），否则选下移（插入）；
相等（并列）归删除，即**优先删除**。效果：同一 hunk 内 `-` 行排在 `+` 行之前。
例：`a\nb\n`→`b\na\n` 得到 `-a`（删第 1 行）再 `+a`（第 2 行后插入），与 GNU diff
逐字节一致；若优先插入则会得到 `+b` 在前、`-b` 在后的另一种最短脚本。
该规则写死在 `edit.Diff` 的比较式里，故同一输入 100 次生成结果逐字节相同。

## 4. 偏移后的后续 hunk（语义 7）

要平移。前一个 hunk 以偏移 +k 应用成功，说明目标文本相对补丁记录位置整体漂移了 k
（通常源于文首增删行），其后的所有记录位置都带着同样的漂移；若后续 hunk 仍从原记录
位置起搜，漂移会累积，很快超出 F 而误报失败。故后一 hunk 的查找起点 =
`记录位置 + 已应用 hunk 的行数净变化 + 上一 hunk 的偏移`（行数净变化是因为工作文本
已被前面的 hunk 改长/改短；上一 hunk 的偏移是 GNU patch 同款的首猜值）。在此起点
上下各 F 行内搜索，多个候选取离起点最近者，距离相同取下标较小（靠前）者。
