# 熔断与舱壁保护器设计

## 1. 分层顺序：熔断在前，舱壁在后

请求路径：`breaker.Allow -> bulkhead.Acquire -> timeout 执行真实调用`。

论证：熔断掌握下游的全局故障结论。熔断打开时若先过舱壁，被拒请求仍要
占一个等待队列名额甚至一个并发额度，导致“拒绝也要排队”，且故障期间队列
被注定失败的请求塞满，拖慢恢复后的真实探测。熔断在前则打开期 O(1) 立即
拒绝、完全不触碰舱壁：熔断打开时舱壁在途占用恒为 0。

## 2. 统计口径：被拒绝不计失败率

熔断决策只能基于“真实调用”的结果。设真实调用样本集 S（成功 a、失败 b），
被熔断拒绝 r、被舱壁拒绝 q。

- 若把 r 计入失败：失败率 = (b+r)/(a+b+r)。熔断一旦打开，新样本几乎全是
  r，失败率被自身拒绝拉向 100%，即使下游已恢复也无法满足半开/关闭条件，
  形成自锁。正确口径：失败率 = b/|S|，分母只含真实调用。
- 舱壁拒绝 q 同理：它反映本端容量压力，不携带下游健康信息，计入失败率会
  让“自己容量小”被误判为“下游坏了”。
- 因此：总请求 = a + b + r + q；真实调用 = 总请求 - r - q；b 按错误类别
  细分之和恒等于 b。三条等式在任意操作序列后必须成立。

## 3. 不可重试错误是否计入熔断

不计入熔断失败，但计入统计的失败数。理由：熔断的意义是“暂停对一个暂时
不可用的下游的调用，给它恢复时间”；不可重试错误（如参数非法、资源确定
不存在）在下游恢复后依然失败，熔断它既无收益，又会让一个调用方的确定性
错误误伤所有调用方。可重试错误、超时、panic 才代表“下游此刻可能不健康”，
计入连续失败与失败率。所有真实失败都进统计，仅熔断计数口径不同。

## 4. 四条路径归还额度

额度在真实调用开始前获取，必须在成功、失败、超时、panic 四条路径上各归还
一次。panic 若不 recover 会沿调用栈击穿包装器，且 defer 若未覆盖该路径就
会永久占用一个额度；1000 次泄漏即把 N 个额度耗尽为 0，此后全部请求被舱壁
拒绝。实现用 `defer release()` 配合 panic 捕获，保证恰好一次归还。

## 5. 舱壁语义

容量 N、队列 Q：在途信号量容量 N，等待信号量容量 N+Q。
- 先占等待名额再等在途名额；等待名额满（第 N+Q+1 个）立即返回
  `ErrBulkheadRejected`，不阻塞、不推进时钟。
- ctx 在等待中取消：让出等待名额并退出，不获取在途额度。
- 获取在途额度后让回等待名额；Release 只还在途额度。内部峰值计数器仅在
  在途数增加时更新，保证历史峰值 <= N。
- 不同下游使用独立 Bulkhead 实例，X 耗尽不影响 Y。

## 6. 熔断状态机

配置：连续失败阈值 K、失败率阈值 F、最小样本 M、冷却 C、上限 Cmax、
探测数 P。阈值 <=0 构造报错。
- 关闭：滑动计数 (total, fail)；连续失败达 K，或 total>=M 且
  fail/total>F，则迁移到打开，冷却 = C。
- 打开：now-openedAt < 冷却则拒绝；到点迁移半开，重置探测窗口。
- 半开：仅 P 个请求可通过（探测令牌），其余立即拒绝；任一探测失败立即回
  打开，冷却 = min(2*当前冷却, Cmax)；P 个探测全部成功则回关闭并清零计数。
- 时钟回拨（now 早于已记录的决策时间）：返回 ErrClockRewind，状态不变，
  绝不提前进入半开。
- 所有迁移在互斥下完成，只发生一次；加倍冷却同样只执行一次。

## 7. 时钟与包划分

`Clock` 仅暴露 `Now()`，测试注入可控时钟，生产用 time.Now。
- classify：四类哨兵错误与分类（可重试/不可重试/超时），panic 归为可重试。
- bulkhead：容量、队列、峰值计数。
- timeout：单调用时限，ctx 取消即超时；recover panic 转为错误。
- breaker：状态机，Allow 返回许可，Complete 回报结果。
- stat：组合保护器、分组计数、快照与口径等式校验。

四类判定错误：ErrRetryable、ErrNonRetryable、ErrTimeout（classify），
ErrBulkheadRejected（bulkhead），ErrBreakerOpen、ErrClockRewind（breaker），
均可用 errors.Is 区分。
