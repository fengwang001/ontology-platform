# 带租期的分布式序列号分配器 — 设计

## 1. 包划分
- `clock`：`Clock` 接口（`Now`/`Sleep`）、`RealClock`、`FakeClock`（可推进/可回拨）。
- `durable`：目录内持久计数器。文件布局 `magic "ONTV1"(5B) | value(8B 大端) | CRC32(4B)`，共 17B；
  CRC 覆盖前 13B。推进 = 写同目录临时文件 → fsync → 原子 rename → fsync 目录；
  `flock` 串行化并发租用。无文件视为首次启动，按配置起点建文件。
- `lease`：租约 `[Start, Start+Len)` + `Expiry`；`ValidAt(t)`、`Contains(x)`。
- `alloc`：租号段并在本地 O(1) 分发；段长自适应；把作废区间报给 `audit`。
- `audit`：收集分发记录，校验全局唯一、实例内单调，汇总空洞区间。

## 2. 为什么持久化必须先于分发
租到 `[s,s+n)` 的瞬间先把持久计数器推进到 `s+n`，然后才允许发出第一个号。
反证「边发边推进」：设崩溃前已把号 k（响应在途）发给客户端，而持久值只推进到已确认位置 p<k。
重启后新实例读到 p，从 p 继续租用/分发，于是区间 (p,k] 中至少号 k 被第二次发出 → 重号。
先推进则崩溃无论发生在分发前还是分发中途：持久值已是 s+n，重启后第一段从 s+n 起，
`[s+n,…)` 绝不与在途号重叠，代价只是崩溃点之后的号成为空洞（允许）。

## 3. 租期语义
- 租约仅在 `now < Expiry` 且未耗尽时有效；到期瞬间即失效，剩余号作废、永不分发，
  作废区间 `[已发位置, 段末)` 计入空洞，然后重新租用。
- TTL=0：允许。此时每次 `Next` 都租新段（段长建议为 1），等于每号一次持久写。
- 时钟回拨：记录已观察到的最大时间；`now < max` 返回 `ErrClockRollback` 并拒绝分配，
  已到期租约不得因时间变小而复活。

## 4. 自适应段长
每实例维护当前段长 L（`[Min,Max]`，初值 Min）。
- 号段被**耗尽**（需求快于租期）→ 下一段 L=min(Max, 2L)。
- 号段**到期作废**（说明空闲）→ 下一段 L=max(Min, L/2)。
确定性序列（Min=1,Max=1000）：连续耗尽给出 1,2,4,…,512,1000,1000；
之后每号间推进时钟使段到期，给出 1000,500,250,…,1。高频平均段长远大于低频。

## 5. 性能约束
持久写次数 = 租用次数（+首次建文件 1 次）。10 万号、固定段长 1000 ⇒ 100 次，断言 ≤120。
本地分发仅一次取号与一次边界比较，循环迭代为常数，O(1)（测试内埋点计数断言）。

## 6. 损坏检测：绝不允许从 0 重启
若读到损坏文件却静默以 0 开始，新号段会与历史全部号重叠 → 大规模重号。因此：
| 字节长度 | 分类 | 处理 |
|---|---|---|
| 1..4 | ErrHeaderIncomplete | 拒绝启动 |
| 5..12 | ErrValueIncomplete | 拒绝启动 |
| 13..16 或 CRC 不符 | ErrCRCMismatch | 拒绝启动 |
三类错误均为哨兵错误，`errors.Is` 可判定。写失败返回 `ErrWriteFailed`，
临时文件改名前原文件不动，故不会出现部分覆盖。

## 7. 边界语义
- 段长 ≤0：`ErrInvalidLength`；段长 1 合法（每号一次持久写）。
- `value+Len` 溢出或超过 `math.MaxUint64`：`ErrOverflow`，拒绝租用，不回绕。
- 首次启动无文件：从配置起点初始化并持久化。
- 空洞不要求为零；`audit` 报区间数与总长度，总长度 = 到期作废剩余 + 崩溃丢弃长度。
