# DESIGN: 组提交与顺序保证器

仅用 Go 标准库；日志位于调用方给定目录，演示程序使用 `t.TempDir` 等价的系统临时目录。

## 1. 序号分配与回滚

- 请求到达时只排队，不分配全局序号；因此排队失败不会污染顺序。
- 一个批次满足「条数上限 / 字节上限 / 等待时长」任一条件后密封。
- 序号在密封之后、`write+fsync` 之前一次性赋给批内条目：批内按进入顺序获得连续序号。
- 若写或同步失败，整个批次不可见，序号计数器整体回滚到该批之前；等待者全部收到同一个落盘错误。
- 因而后续成功批次紧接前一成功批次，不产生空洞；调用方不会得到“成功但序号不可恢复”的结果。

## 2. 批次原子性

日志格式：

1. 文件头 16 字节：magic(4)+version(1)+flags(1)+保留(2)+创建时间戳(8)。
2. 批次头：魔数(4)+条目数(4)+载荷长度(4)+批级 CRC32(4)。
3. 条目序列：每条均为长度前缀(4 字节,大端)+原始载荷；空载荷是长度 0 的合法条目。
4. 批级 CRC 覆盖批次头固定字段及全部条目字节，但不包含 CRC 自身。

原子性来自“批次头声明完整范围 + 批级 CRC”。回放遇到以下任一情况即停止，并丢弃当前坏批及后续字节：

- 文件头不足或不匹配：`ErrHeaderIncomplete`；
- 批次头不足：`ErrBatchHeaderIncomplete`；
- 声明条目未读完：`ErrEntryIncomplete`；
- 完整长度已读完但 CRC 不符：`ErrCRC`。

不做条目级 CRC，也不恢复坏批内任何条目。打开日志时把文件截断到最后一个完整批次末尾，使下次写入从确定位置继续。

零请求直接 `Close`：不创建日志文件；单请求或空载荷在超时前到达也可正常成批。

## 3. 攒批规则

密封条件三者先到：条目数达到 `MaxEntries`；累计字节（含长度前缀）达到 `MaxBytes`；注入时钟的等待时长到期。超大单条不拒绝：当前批已有旧条目时先密封旧批，再让超大条目单独成批；因此单批字节可能是该单条自身大小，而 `MaxBytes` 约束的是普通聚合边界。等待时长为 0 表示队列一旦可检查即密封；条数上限为 1 退化为逐条提交。

## 4. leader、等待与关闭

每个 `GroupCommit` 只有一个调度协程，天然是每个已密封批次的唯一 leader；调用协程只把请求放进有缓冲队列并等待私有 `chan Result`。leader 按批内顺序写入响应 channel，响应携带调用方自己的载荷身份之外的独立序号；测试用请求 ID 与内容双向核对，杜绝串台。

`Close` 密封并提交当时已接收的请求，然后关闭队列。与 Close 竞争的请求只有两种结果：被纳入最后一批并成功落盘，或收到 `ErrClosed`；不存在成功返回但未进入日志。leader 总是先持久化、再通知等待者，故成功即可由回放观察。

## 5. 故障注入

WAL 内部持有逻辑长度。追加写失败或同步失败后回滚文件长度到本批前；若进程在中途死亡，回放依据不完整长度或 CRC 丢弃尾部。`FailNext(failSync bool)` 只令下一批的写或同步失败一次。同步通过 `f.Sync` 完成，并有非导出计数供测试读取。

## 6. 包划分

- `req`：`Request`、`Result` 与四类可 `errors.Is` 的错误。
- `batcher`：注入时钟下的密封策略，输出不可变批次。
- `wal`：编码、落盘、同步、截断、顺序读回与坏点分类。
- `recover`：打开并修复日志，输出最后连续状态。
- `commit`：组提交调度、序号、leader 唤醒、关闭与故障传播。
- `cmd/demo`：临时目录端到端自检，每行 OK/FAIL，总计一行。
