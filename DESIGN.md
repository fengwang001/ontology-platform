# CSV 方言解析器设计

## 1. 四处推导

**空行 vs 单列空值**：词法上 `a\n\nb\n` 中两个换行紧邻。把空行当成「单列未引号空字段记录」会使单列空值无法与分隔符区分（单列里永远没有逗号），多列表里空行又会制造一条 1 列记录。结论：**空行（记录体为空时遇到行尾）一律跳过**；单列要表达唯一字段为空，**必须写成 `""`**。否则回写器写出空串→一个空字节+换行→再解析为空行被跳过，记录丢失，往返失败。多列记录的未引号空字段照常保留（逗号可锚定其存在）。

**par 切点**：切点可能落在引号字段、`""`、`\r\n` 中间，段 worker 无法自知起点状态。每段在两种假设下各跑一次状态机：`H0=起点在引号外`、`H1=起点在引号内`，每字节恰好被两条状态线各吃一次（2N 上界）。段间用左段已确定的结束状态选择右段采用哪条线。关键不变量：对任意前缀，H0/H1 在吃同一个字节后，除「恰在结尾停在刚见引号」这一瞬时分歧外其余时刻状态一致——引号只会把两条线同时带入/带出引号态。故只需 2 个假设而非 3 个。首段 H0 为真；段长为 0 时沿用前段末态。词法事件（字段开始、字节、字段结束）按被选中的那条线重放；段首若处于未引号字段中段，丢弃其字段开始标记（由前一段的开始标记延续），字节照常拼接。记录号/字段号由 table 在重放事件流上统一计数，因此天然是全局的；字节偏移=段基址+段内偏移（事件携带段内绝对偏移，不随假设变）。

**CR 待定**：未引号字段末尾见 `\r` 不能立刻判行尾还是孤立 CR，进入 CRP 并记下该字节偏移，不提交字段。续传时下一字节为 `\n`→提交记录；为其他→在**该 `\r` 的偏移**报 BareCR。流在 `\r` 结束（Close/EOF）→同样报 BareCR。引号字段内 `\r` 是普通内容（包括其后不跟 `\n`）。par 段恰好切在 `\r` 后：本段以 CRP 结束时把「挂起 CR 的段内偏移」传给下段，下段首字节非 `\n`（含下段为空到 EOF）即在 基址+挂起偏移 报 BareCR；首字节为 `\n` 则正常行尾，`\n` 字节按普通字节被吃掉一次。

**上限的「立刻」**：词法每吃一个属于字段内容的字节就对 `fieldBytes` 加一并立即与上限比较，超限当场返回 ErrFieldTooLong，不缓冲整条记录。引号字段中 `""` 的两个引号在第二个引号被识别为转义后**只贡献 1 个内容字节**；开/闭引号本身不计。字段数在每次「字段开始」时立即计数并对 ErrTooManyFields 判定；记录数在一条记录提交时立即计数。任一上限触发即终态，错误被钉住，后续 Feed/Close 返回同一错误。

## 2. 词法状态转移表

状态：`START` 字段起点 `UNQ` 未引号字段 `QTE` 引号字段 `QQP` 引号内刚见引号 `CRP` 裸 CR 待定。动作：B=内容字节，F=开始字段，E=结束字段，R=提交记录。

| 当前 | `,` | `"` | `\r` | `\n` | 其他 | EOF |
|---|---|---|---|---|---|---|
| START | E,F→START | F→QTE | →CRP | (空记录:R) | F,B→UNQ | 无事件 |
| UNQ | E,F→START | **ErrQuote** | →CRP | E,R→START | B→UNQ | E,R |
| QTE | B→QTE | →QQP | B→QTE | B→QTE | B→QTE | **ErrUnclosed** |
| QQP | E,F→START | B→QTE(转义引号) | E→CRP | E,R→START | **ErrAfterQuote** | E,R |
| CRP | E,F→START | E,**ErrBareCR** | E,**ErrBareCR** | E,R→START | E,**ErrBareCR** | **ErrBareCR** |

空记录（START 遇 `\n`，或 QQP/UNQ 路径上记录体为空）交给 table：记录体为空则跳过（见推导 1）。QQP 遇 `\r` 先结束字段再待定，因此下一字节非 `\n` 报 BareCR 而非引号后杂字符。

## 3. 规范输入与回写

规范输入 = 记录以 `\r\n` 结束；字段仅在必要时加引号；单列空值写作 `""`；空行不作为记录。必要引号：内容含 `,`、`"`、`\r`、`\n`，或解析时该字段带引号标记（保持标记相等），或单列记录的空字段。引号转义为 `""`。

单流式解析器实例非并发安全；`par` 内部 K 个 goroutine 无共享可变状态，经 WaitGroup 汇合。
