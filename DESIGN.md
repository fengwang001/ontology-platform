# 列式存储段设计

## 1. 空值与零值

每个行组单独保存空值位图，位图第 `i` 位为 1 表示第 `i` 个槽位为空；数据块只编码非空值，且非空值仍保留其原始槽位。读取结果使用 `(value, null)` 表示，字符串同理使用 `(value, null)`，因此 `NULL`、`0`、`""` 是三种可判定状态：空值看位图，零和空串看类型化载荷。

不能选择哨兵值，因为 int64 的任何位组合都可能是合法用户值；字符串同理无法保证某个内容永不出现。哨兵还会让字典、最小值和最大值把空值误当业务值。

统计只遍历非空值。空行组保存 `HasMinMax=false`。若把空值算作零：全空数值组会得到 min=max=0，`=0` 本应不命中却无法裁剪；若列中真实值全为正数，`x<0` 本可裁剪却因假零必须解码。字符串空值若算作 `""`，也会错误命中 `=''`。

## 2. 行组裁剪

行组统计为非空最小值 `lo`、最大值 `hi`、空值数 `nulls`。数值比较谓词只在非空值上判定：

- `=v`：`lo <= v && v <= hi`
- `<v`：`lo < v`
- `<=v`：`lo <= v`
- `>v`：`hi > v`
- `>=v`：`hi >= v`
- `IN(S)`：存在 `v∈S` 满足 `lo <= v <= hi`

区间合取取各条件保留结果的逻辑与。这些判定式是“存在非空行可能为真”的必要条件：若范围内没有目标值、所有值都在阈值另一侧、或集合中没有值落入 `[lo,hi]`，就不可能命中。统计只给出边界，不给出成员关系，所以判定保留时可能没有真实命中，却不会排除真实命中；这保证只漏排除，不误排除。

等于 `lo` 或 `hi` 不能排除，因为边界值可能实际出现；`>max`、`<min` 必须排除。`IS NULL` 保留 `nulls>0` 的组，`IS NOT NULL` 保留非空计数大于 0 的组。任何普通比较遇空值都不是真，空值只由 `IS NULL` 命中。

## 3. 游标与等价性

游标保存 `(rowGroupIndex, offsetInGroup)`，指向下一条待检查的槽位，不保存已解码值或结果偏移量。续扫从该位置开始；若当前或后续行组被统计裁剪，扫描器直接把游标推进到下一个可能行组的起点。因此游标天然穿过被裁剪区域，且被跳过槽位不会再次成为候选。

每批只在实际返回达到 limit 时停止，并把游标停在结果后的第一个待检查槽位。无结果时游标也单调前进。把所有批的输出按顺序拼接，与从初始游标连续执行、不施加 limit 的扫描处理同一序列、同一谓词，因而不重不漏。

## 4. 截断与损坏

段格式依次为：段头、行组头、每个行组的统计、空值位图、数据块。所有读取均由有界字节缓冲支持：先校验固定长度，再按长度字段校验剩余容量，长度使用 uint64 varint 并拒绝超长和负义编码。

打开段时只完整解析头、统计和位图；数据块记录其起止偏移并再次核对段边界。真正扫描到某个行组时，在数据块阶段检查压缩后的位打包或字典块长度，再按声明的非空值数逐项解码。任何截断都会在固定头、varint、长度前缀、位图或声明值数对应的边界检查处失败，错误包装为 `group=<n> stage=<头部|统计|空值位图|数据块>`，不会依赖切片越界，也不会返回部分结果。

## 5. 资源上限与字典降级

限制包括最大行数、最大行组数、字典最大基数，均在写入器创建或行组提交前检查；超限直接返回互不相同的哨兵错误，提交失败时不移动行组切片、不追加段字节、不更新已提交计数。

行组先收集非空值并稳定去重。若去重值数量不大于字典最大基数，则写字典码字；码字宽度只需容纳字典大小。若提交时发现基数超限，丢弃临时字典，改用位打包路径重新编码同一批原始值。此前已提交行组的字节和统计不可变，不依赖本次临时结构，所以降级不影响它们。位宽 1–63 直接打包；int64 全范围需要 64 位，64 位值按连续 8 字节读写，仍遵守声明值数和末尾补位规则。
