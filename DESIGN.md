# DESIGN.md

## 1. 流格式记录布局（全部多字节整数为 ULEB128 varint）

| 记录 | 字节布局 | 说明 |
|---|---|---|
| 流头 | `'L','Z','7','O'` 4 魔数 + varint 版本(=1) | 仅出现在流首 |
| 字面量 | tag=1(varint) + varint 长度 n + n 字节 | n≥1 |
| 回指 | tag=2(varint) + varint 距离 d + varint 长度 l | d≥1, l≥3 |
| 刷新 | tag=3(varint) | 边界语义，非数据 |
| 流尾 | tag=4(varint) + varint 原文总长 + varint CRC32(IEEE) | 流必须以此结束 |

## 2. 推导一：压缩为何与 Write 切法无关（语义 3、4）

压缩器持有未决缓冲 `pend`。位置 i 的匹配只有两种确定性结局：命中长度 m 或确认无命中。但「m 个字节全部已到达」之前无法判定 m 是否就是最长——下一个 Write 的字节可能延长它。
规则：仅当 `len(pend)>=m+1`（m=0 时即 `len(pend)>=1`，额外字节用于尝试延长）才提交位置 i；否则等待更多输入。故每次决策只依赖已到的确定信息，与这些字节分几次到达无关，输出逐字节相同。
连续无匹配的位置先积压为字面量，直到某次命中才整体发出一个字面量段。
Flush 强制按当前信息提交全部 `pend`：最后一个匹配无法再被延长，此时确定结束；然后发刷新标记，解压器即可还原截至此刻的全部输入。Flush 不清窗口，后续匹配仍可引用其前数据。两次 Flush 间无输入时 `pend` 为空、无匹配可结束，故零字节输出。

## 3. 推导二：重叠回指的解压复制（语义 2）

d<l 时源区间与目标区间重叠。块复制 memmove 对「源在目标之前」的前向重叠恰好等价于逐字节向前复制，但依赖该巧合可读性差。因此统一用循环 `for k:=0;k<l;k++ { out=append(out,out[len(out)-d]) }`：每次从**已写出**的输出取字节，d=1 时自然产生字节重复，d=2/3 时自然产生周期。复杂度 O(l)，与块复制同阶，每字节仅一次索引+追加，可接受。

## 4. 推导三：两种越界距离的区分（语义 7、9）

d>窗口容量：格式/结构性非法——即使流无限长也永不可能被合法压缩器引用，预置字典再大也受窗口容量截断。
d>已输出字节数：当前历史不足，在流的更早位置或非预置场景本可合法，属「时序性损坏」。故先查容量（编码器静态契约）再查已输出量（动态状态），错误彼此可区分。
编码器只引用仍在环形窗口内的位置，d≤容量恒成立，故前者绝不产生。并行预置字典时，字典字节在块首已被压入同一窗口，解压端同样预置，它们计入「已输出」：本块解压输出的是块净增量，而内部历史包含字典（保留但不对外交付），「已输出字节数」语义未变，仍是窗口中全部历史。

## 5. 推导四：并行块压缩的确定性与衔接（语义 9）

第 k 块压缩器的输入 = 上一块末尾至多 W 字节作预置字典 + 本块原文。哈希链由确定性插入顺序决定，压缩是纯函数，结果只依赖这两段字节，与 goroutine 调度、完成顺序、其他块无关；故 workers=1/2/4/8 输出逐字节相同（块划分固定，仅执行并发度不同）。
块尾按 Flush 同样的强制规则结束本块匹配，但**不发刷新标记**：标记会让不同切块方案产生不同字节流，且解压无需边界信息——回指自然衔接，字典预置在解压端同样存在，回指自然可解。块输出 = 本块产生的字面量/回指记录；首块发流头，最后一块发流尾（总长为各块原文之和，CRC 顺序合并）。

## 6. 复杂度保证

哈希链：4096 桶头表 + 每位置一个链指针；`find` 每位置最多追溯 chainLimit 个候选，计数存于非导出字段 `examined`。每位置 O(chainLimit)，与窗口大小无关，故全同字节串每字节考察数不随规模增长。最小匹配 3，默认窗口 32768、链上限 64。
