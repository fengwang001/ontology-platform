# DESIGN: 结构化日志字段脱敏与一致性采样

## 1. 数据模型
- 记录 = 嵌套字段映射 `map[string]any`（值可为 string / map / []any）+ 追踪 ID + 级别。
- 编解码使用标准库 `encoding/json`，键按字节序排序，保证逐字节可比对。
- 深度上限 `MaxDepth=32`：从根映射开始，每进入一层嵌套映射或数组元素映射计 +1；超过即拒绝。
- 循环引用：构造时（`Build`）记录已访问映射指针（reflect 不可比较，用 `reflect.Value.Pointer()`），
  再次遇到同一映射即报 `ErrCyclic`，从根上杜绝死循环与栈溢出。

## 2. 一致性采样的推导
- 目标：同一追踪链的全部记录要么全留、要么全丢。
- 若对每条记录独立掷骰，同链不同记录结果不同，链必断裂；若依赖全局状态/到达顺序/时间，
  重放与并发结果不可复现，且无法跨进程一致。
- 正确做法：决策只能是追踪 ID 的纯函数。对 `traceID` 做一次 FNV-1a 哈希，
  `h mod 10000 < rate*10000` 则保留。决策与记录内容、顺序、时间无关（单次决策哈希次数 = 1，O(1)）。
- 空追踪 ID（缺失或空串）：无法标识一条链，统一按"独立记录"策略，对空串做同样的确定性哈希，
  即所有空 ID 记录共享同一个固定决策（rate>0 时全部保留或全部丢弃，行为可复现）。
- rate=0 全丢；rate=1 全留。
- error 及以上级别无条件保留（决策在前：采样判定 keep，但级别覆盖为保留）。

## 3. 链不完整标记
- 先按链做采样决策，再应用级别覆盖。当 `采样决策=丢 且 记录因 error 被强制保留` 时，
  该记录输出 `incomplete_chain=true`；其余情形（链保留、或非 error 被丢）不打标记。
- 语义：看到此标记即表示"同链其他记录未随本记录一起落盘，链不完整"。

## 4. 字段路径与含点号键的消歧
- 路径 `a.b.c` 表示逐层进入映射；空段非法。
- 歧义：键名本身可叫 `a.b`。消歧规则（固定、可判定）：
  1. 先按嵌套路径逐层精确匹配；
  2. 嵌套路径不存在（任一层缺失，或某层不是映射）时，再在**当前层**把整串剩余路径作为字面键名匹配一次；
  3. 两者都不命中 = 路径不存在。字面键命中后不再向更深层展开。
- 因此：路径中间层不是映射 => 嵌套匹配终止，仅尝试当前层字面键；不做类型强转。
- 规则里也可写 `a\.b` 表示"仅按字面键 `a.b` 匹配"，不走嵌套；未转义的点按嵌套分隔处理。

## 5. 脱敏
- 规则 = 路径规则（按第 4 节路径定位，action: replace/hash/truncate）+ 值模式规则（正则，命中任意字符串值即脱敏）。
- replace：固定串 `***REDACTED***`，不保留长度。
- hash：`sha256:` + 值的 SHA-256 十六进制；同值同结果、不同值不同结果、不等于原值、不可逆。
- truncate：保留前 N 个 rune，N 个 rune 后追加 `…(truncated)`；短于 N 不变（标注体现在超长时）。
- 彻底性：遍历整棵字段树（含数组元素、嵌套映射、被引用形态），每到一个字符串值既检查所在路径规则、
  也检查全部值模式规则；同一值出现在多处必然逐处处理。
- 非目标字段：先深拷贝，仅在命中位置替换；未命中值用 `reflect.DeepEqual` + JSON 逐字节比对保持不变。
- 规则集预编译为路径 trie（节点 map + 字面键槽位），每个字段只做常数次 map 查找；
  非导出计数器 `matchCount` 记录路径查找次数，上界 `记录数*(字段数+常数)`。
- 冲突：同一路径存在两种不同 action 即编译期报 `ErrConflict`，错误信息指出两条冲突规则。

## 6. 落盘格式与损坏检测
- 文件 = 魔数头 `"ONTLOG1"`(7B) + 版本 1B + 记录帧序列。
- 帧 = 4B 大端长度 L + L 字节记录体(JSON) + 4B CRC32(IEEE)。
- CRC 覆盖：记录体 + 1 字节延续标志 tag（本帧后还有帧则 tag=1，末帧 tag=0）。
  末帧 tag=0，使"在 CRC 之后截断/损坏"（末帧 CRC 被改坏）能被判为 CRC 不匹配而非正常 EOF。
- 截断点分类（逐字节 1..len-1）：header 不全 => ErrShortHeader；
  长度前缀落在前一帧 body/CRC 区 => 由前一帧判定（body 不全 / CRC 不匹配）；
  长度前缀本身被切掉 => ErrShortLen；body 字节不足 L => ErrShortBody；
  body 完整但算出 CRC 与读到的不符（含末帧 tag 参与的情况）=> ErrCRC。
- 恢复记录数 = 错误前连续完整且 CRC 正确的帧数（最大可恢复前缀）。

## 7. 并发与包划分
- 包：record（模型/编解码/深度/环）、rule（规则/编译/trie/计数）、mask（三动作执行）、
  sample（哈希采样/级别覆盖）、sink（帧写读/CRC/分类）、cmd/demo。
- 规则集编译后只读；采样只读 rate；处理无共享可变状态，多 goroutine 结果与串行逐元素一致。

## 8. 错误判定
- 四类错误均为哨兵：ErrCyclic、ErrTooDeep、ErrConflict、ErrShortHeader/ErrShortLen/ErrShortBody/ErrCRC，
  统一可用 errors.Is 区分。
