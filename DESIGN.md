# Properties 解析器设计推导

## 第 3 条：空白续行

规则回顾：行尾奇数个反斜杠 → 去掉一个反斜杠，剥掉下一物理行前导空白后拼接。
推导：设 `k=v\` 后接一行只含空白（或为空）的行，再接 `w=x`。续行先把下一行前导
空白剥光，剩下要拼接的内容是空串。逻辑行已无任何新输入字符，而「行尾反斜杠」这个
续行条件只能在非空的新内容上重新判断；空串没有行尾，无法再次触发续行。
对照 `java.util.Properties` 的 `LineReader.readLine()`：续行后 `skipWhiteSpace=true`
且 `appendedLineBegin=true`，若紧接着遇到行终止符（即续行行为空白/空行），代码直接
`return len`，逻辑行就此结束，不再链到下一行。
结论：`k=v\` + 空白行 + `w=x` ⇒ `k`→`v`，然后 `w`→`x`；空白续行**终止**逻辑行，
悬挂的反斜杠已被丢弃、不贡献任何字符。它与「续行首部空白被剥掉」是同一条规则的两
半：剥空白是动作，剥完为空则逻辑行自然闭合——空段既不能续也不能注释（注释判定只
发生在逻辑行首），所以 `k=a\` + `#b` ⇒ `k`→`a#b`（`#` 不在逻辑行首，按内容拼接）。

## 第 6 条：Store 转义集推导

目标：`Load(Store(m))` 逐项相等。只转义在 Load 路径上会被吃掉或改变语义的字符：
- `\`：转义引导符，键值中都必须写成 `\\`，否则会吞掉后一个字符；值尾奇数个 `\`
  还会触发续行。⇒ 键、值一律转义。
- 空格：键的结束符是未转义的空白或 `=`/`:`，故键中**每个**空格都要转义为 `\ `。
  值的分词是「跳过分隔符后的前导空白，其余原样保留」，故值中间的空格安全；但值
  **开头**的空格会被当作分隔符后空白跳过，必须转义。只转义第一个即可：`\ ` 使值
  以 `\` 开头（非空白），其后空格落入「其余原样保留」区。
- `\t` `\f`：同为空白，键中一律转义；值首也会被跳过，故值中一律转义（兼解决不可见
  问题）。`\n` `\r` 是行终止符，键值中都必须转义。
- `=` `:`：键的分隔符，键中一律转义；值中分隔符查找已结束，无需转义（`k==v` 解析
  出值 `=v` 可证）。
- `#` `!`：只在**逻辑行首**触发注释。键写在行首，故以 `#`/`!` 开头的键必须转义
  （`\#`）；键中间及值中永远不处于行首（值中的换行已被转义），无需转义。
- 非 ASCII：UTF-8 多字节序列不与任何 ASCII 特殊字符冲突，原样输出。

## 语义-测试对照表

（待补）
