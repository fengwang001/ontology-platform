# 排序合并连接：溢出与回退执行器设计

范围：两路已按连接键升序排列的输入做内等值连接；一侧同键分组超过内存阈值时溢出到
本地临时文件，后续左行以“回退重扫”该右组的方式完成笛卡尔展开。仅用标准库。

## 1. 组起点的两种表示

右侧遇到一个新键 K 时，需要把该键的全部右行收集成“右组”，之后该组要被每个
同键左行重扫一遍。组起点有两种表示：

- **完全驻留内存**：右组是 `[]row.Row`，组起点就是切片下标 `start = 0`；
  重扫 = 把遍历下标重置为 0，O(1)。
- **已溢出磁盘**：行是变长编码，组起点必须是**文件内字节偏移**
  （`dataStart = len(header)`，紧接自描述头之后第一条记录的起点）；
  重扫 = `Seek(dataStart, io.SeekStart)` 后顺序解码。
  **绝不能用“行号 × 固定长度”推算偏移**：每行 `4 字节长度前缀 + 变长行体 +
  4 字节 CRC32`，行体长度随内容变化，乘固定常数会落到长度前缀或行体中间，
  典型症状是组内前几行正常、后几行错位或读出半截行。

## 2. 溢出文件格式与截断分类

字节布局（小端）：

```
magic(4)="SPGR" | version(1)=1 | flags(1) | keyLen(4) | key 字节
重复记录：bodyLen(4) | body(bodyLen) | crc32(body, IEEE)(4)
```

以“文件实际长度 n”相对完整文件长度 N 逐字节截断，四类：

1. 头部不完整：`n <= len(header)`（magic/头字段/键被切掉）。
2. 长度前缀不完整：落在某条记录的前 4 字节内，`0 < n-p < 4`。
3. 行体不完整：前缀完整但 `p+4 <= n < p+4+bodyLen`。
4. CRC 不匹配：行体完整但 CRC 缺失/错误（截断点落在该记录 CRC 的 4 字节内，
   或 CRC 值被改写）。

读出“最大可恢复前缀”：从头顺序解码，CRC 校验通过的记录才算数；第一条不完整或
CRC 错误记录之后的一切字节全部丢弃，半截行一行不返回。连接在重扫遇到损坏时
必须以 `ErrCorrupt` 中止，不得用少行的结果假装成功。

## 3. 状态与并发

分组状态显式三态：`building`（仅构建方持有）→ `spilling`（异步落盘中，禁止重扫）
→ `ready`（fsync/关闭完成，内存或磁盘路径均可重扫）。`ready` 由落盘 goroutine
在写完并关闭文件后经 channel 一次性发布。重扫调用必须先确认 `ready`；
`spilling` 状态下立即重扫返回 `ErrGroupNotReady`（测试循环并发“写出+立即重扫”，
断言永远读不到未完成组），不使用 sleep。

## 4. 主循环与输出顺序

单协程推进：分别从两侧读取，收集同键全部行（读取时顺便校验
`key[i] >= key[i-1]`，逆序即返回 `ErrUnsorted` 并给出第一处逆序的流名/行号）。
- 左键 < 右键：推进左侧（内连接无输出）；右键 < 左键：推进右侧。
- 相等：对左组每一行 L（到达序）重扫整个右组（R 按到达序），输出 (L,R)。
  即键升序、同键内“左行到达序，再右行到达序”的字典序；同键行的相对顺序
  是输入序，打乱输入会改变输出，但同一份输入重复执行逐字节一致（跑三次断言）。

## 5. 读出行数等式

设左行数 L、右行数 R；主循环每个输入行恰好被“收集”读一次。键 Ki 的左组大小为
li、右组大小为 ri，则该键产生 li 次重扫、每次重扫读出 ri 行。

```
两侧读取行数之和 T = L + R + Σ li*ri
重扫读出行数  S = Σ li*ri = 每个左行 × 其匹配到的右组大小
```

测试精确断言 `T == L+R+S` 且 S 等于计数器实测值，证明不存在多余的全表重扫。

## 6. 内存上界

`spillgrp` 用非导出 `maxResident` 记录历史最大驻留行数。阈值 m 下，内存分组
达到 m 行即转为溢出，之后行边读边追加到文件、不常驻切片，故
`maxResident <= m`（50k 同键行、阈值 1000 时断言峰值 ≤ 1000 且溢出文件存在）。

## 7. 四类可判定错误（errors.Is）

`ErrCorrupt`（头部/前缀/行体/CRC，错误文本含分类）、`ErrInput`（输入流中途
报错，携带已消费到的键）、`ErrSeek`（重扫 Seek 失败，指出键与组）、
`ErrUnsorted`（第一处逆序位置）。
