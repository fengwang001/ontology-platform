# 区间重叠索引设计

## 0. 基础定义
区间 = `[L,R)`，左闭右开，含点集 {x | L<=x<R}，端点为 int64。点查等价于
单元素重叠判定；朴素扫描定义：x 命中 I 当且仅当 L<=x 且 x<R。

## 1. 相接算不算重叠（推导，不是结论）
重叠 = 交区间非空。交 = [max(L1,L2), min(R1,R2))，
在左闭右开下非空当且仅当 maxL < minR，即 L1<R2 且 L2<R1。
`[1,3)`、`[3,5)`：3<3 不成立，交为 `[3,3)`=空，故**相接不重叠**。
点查 3：3<3 为假，不命中第一个；3<=3 且 3<5，命中第二个。
这与朴素扫描逐点判定完全一致（扫描对第一个求值 3<R1 得假）。

## 2. 零长度区间 `[2,2)`
其点集为 {x|2<=x<2}=空，它是合法区间（L<=R 成立，允许插入）。
- 点查：任何 x 都不能同时满足 x<2 且 2<=x，**永不命中**。
- 被 `[1,5)` 重叠吗？两个点集的交为空，按“交非空”定义**不重叠**，
  任何重叠查询都不返回零长度区间，空集与任意集合的交集只能是空。
  判据统一为交点集非空：`max(L1,L2) < min(R1,R2)`。对普通非空区间
  它等价于 `L1<R2 && L2<R1`；出现 `[2,2)` 时 maxL=minR=2，判为否，
  从而与点查（空点集不含点）完全自洽。
零长度区间可插入、占用计数、可被删除，但任何查询都不返回它。

## 3. 结果排序规则
要求：逐位确定 + 与插入顺序无关。因此排序键不得包含插入序号或节点指针，
只能用区间自身数据。键取 `(L 升序, R 升序)`，对同一查询的结果做稳定
排序。完全相同的区间所有键相等，稳定排序保持它们在树中序序列中的相对
次序。树由区间值（不含插入次序）构造，不同插入顺序得到相同多重集；
删除一个相同区间后中序仍是同样的若干副本，故结果逐位一致。

## 4. 删除多重集里的哪一个
三个副本在比较器下不可区分（无载荷、无序号）。Delete 删去中序中该值
的**第一个**节点；副本对外部观测完全等价，删任意一个后剩余多重集都是
“两个副本”，查询结果只依赖多重集（第 3 条），故与删除次序无关。

## 5. 区间关系对照表
| 情形 | 例 | 重叠 |
|---|---|---|
| 相离 | [0,1) [2,3) | 否 |
| 相接 | [1,3) [3,5) | 否 |
| 部分重叠 | [1,4) [3,5) | 是 |
| 包含 | [1,9) [3,5) | 是 |
| 完全相同 | [2,4) [2,4) | 是（各算一个，多重集） |
| 零长度 | [2,2) 与任意 | 否（点集为空） |

## 6. 结构
AVL（键先 L 再 R），每节点缓存子树最大右端点 mR=max(R,left.mR,right.mR)；
旋转后自底向上重算 height 与 mR。重叠查询剪枝：节点 mR<=q.L 则右子树
必不相交；左子树 mR<=q.L 时不入左。点查沿 L<=x 的路径下行并剪枝，
互不相交数据下单次点查访问量 O(树高)。访问量由 query 层原子计数器记录。
并发：tree 用 RWMutex，查询走 RLock。
