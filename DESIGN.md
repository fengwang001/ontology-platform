# 设计推导：带租期的分布式序列号分配器

## 目标与不变量

多个分配器实例共享一个持久计数器（本地文件 + CRC32），批量租用号段
`[s, s+n)` 后在内存中 O(1) 分发。全局不变量：

1. **唯一性**：任意两个实例分发的号互不相同。
2. **单调性**：同一实例内分发的号严格递增。
3. **不回退**：持久计数器只增不减；崩溃重启后新号段起点 ≥ 崩溃前已租段末尾。
4. **允许空洞**：崩溃丢弃与租约作废会产生空洞，不要求无空洞，但 `audit`
   必须能精确报告空洞区间数与总长度。

## 一、为什么持久化必须先于分发

设实例租到号段 `[s, s+n)`。正确顺序是：

1. 持久计数器原子推进到 `s+n`（写临时文件 + fsync + rename）；
2. 推进成功后才开始在本地分发 `s, s+1, ...`。

**反证「边分发边推进」**：假设持久值只记录已分发位置 `d`（`s ≤ d < s+n`）。
分发动作与响应投递之间存在在途窗口：实例把号 `d+1` 发给客户端后、把
持久值推进到 `d+1` 之前崩溃，客户端已经持有了 `d+1`。重启后从持久值
`d` 继续，会再次发出 `d+1` —— 重号。即使把推进放在分发之前逐号执行，
持久写次数也等于分发数，失去批量租用的全部意义（见第四节性能约束）。
唯一两全的方案是**按段预付**：租用即把持久计数器推进到段末尾 `s+n`，
崩溃后重启从 `s+n` 起租新段，已发出的号 ≤ `s+n` 绝不会再出现。代价是
崩溃时已租未发的号成为空洞——这正是"允许空洞"的来源。

**推论**：崩溃时若崩溃点在"推进持久计数器"之前，则租用失败、一个号都
没发出，安全；若崩溃点在推进之后，则整个段已被持久化认领，重启跳过该
段，安全。两种崩溃点都不产生重号。

## 二、租约到期与作废语义

租约 = `{Start, Len, Expiry}`。分发条件：`now < Expiry` 且号未发完。
时钟跨过 `Expiry` 后，**该租约剩余号立即作废**，下一次分配必须重新租用
（持久计数器再推进一段）。旧段剩余号永不出现，成为空洞。作废是单向的：
租约不因任何后续事件复活。

## 三、时钟回拨

实例记录上次见到的时钟值 `lastNow`。每次分配前比较：若 `now < lastNow`
则返回可判定错误 `alloc.ErrClockRollback`，拒绝分配。由于回拨被直接拒
绝，不存在"回拨后旧租约看起来又未到期"的复活路径。时钟可注入
（`clock.Clock` 接口），测试用 `clock.Fake` 精确控制。

## 四、持久写次数上界

每次租用（含续租）恰好一次持久写。段长为 `n` 时可分发 `n` 个号，故
分发 `K` 个号的持久写次数 ≈ `K/n + 到期浪费次数`。段长 1000、分发 10 万
且时钟不到期时，写次数 ≈ 100，测试阈值取 120（留出余量但远小于 10 万）。

## 五、段长自适应策略

确定性规则（无随机、无浮点）：

- 初始段长 `MinSeg`（默认 64）。
- 租约在到期前被**发完**（说明需求旺盛）：下一段长度 ×2，封顶 `MaxSeg`
  （默认 4096）。
- 租约**到期仍有剩余**（说明空闲）：下一段长度 ÷2，下保底 `MinSeg`。

高频阶段段长单调升至封顶，低频阶段回落到下保底，测试用注入时钟构造
确定性的高/低频序列，断言两阶段平均段长有明确倍数差。

## 六、持久文件格式与损坏分类

文件定长 20 字节：`magic(8B "ONTCNTR1") | value(8B 大端 uint64) |
crc32(4B, IEEE, 覆盖前 16B)`。写入走临时文件 + rename，崩溃不会留下半
写文件（要么旧文件完整，要么新文件完整）。

加载时按长度/校验分类（`errors.Is` 可区分）：

- 长度 ∈ [1,7]：`ErrHeaderIncomplete`（头部不完整）
- 长度 ∈ [8,15]：`ErrValueIncomplete`（值不完整）
- 长度 ∈ [16,19] 或 CRC 校验失败：`ErrCRC`（CRC 不匹配）

**损坏时拒绝启动，绝不从 0 重建**：计数器从 0（或配置起点）重新开始，
意味着历史上已发出的所有号都在重发候选集中——大规模重号，比拒绝服务
严重得多。宁可不可用，不可不正确。只有"文件不存在"（首次启动）才允许
从配置起点初始化。

## 七、并发与边界

- 并发租用经 `AdvanceBy` 在单次持锁内「读当前值 + 推进」原子完成；
  若拆成「读值」与「推进」两步，两个实例可能基于同一旧值算出不同终点
  而双双推进成功，造成号段重叠（测试曾复现，已修复）。
- 段长 ≤ 0：`lease.ErrInvalidLength`；段长 1：每次分配都伴随一次持久写。
- `start + len` 溢出 uint64：`lease.ErrOverflow`，拒绝而非回绕。
- 租期 TTL = 0：允许，语义为"每次分配都重新租用"（租约立即到期）。
