# DESIGN — 自定义 LZ77 流式压缩

## 流格式（wire 包）

所有记录以 uvarint（7bit/字节、小端、至多 10 字节）编码，tag 低 2 位为类型。

| 记录 | 字节布局 |
|---|---|
| 流头 | `0x4C 0x5A 0x01`（魔数 2 字节 + 版本 1 字节） |
| 字面量段 | uvarint `(len<<2)|0`，随后 len 个原始字节 |
| 回指 | uvarint `(len<<2)|1`，随后 uvarint `dist` |
| 刷新标记 | 单字节 `0x02`（tag 值恰为 2） |
| 流尾 | 单字节 `0x03`，uvarint 原文总长，uvarint FNV-1a64(原文) |

## 推导 1：与 Write 切法无关（语义 3）

- 位置 p 的**字面量判定**只依赖 `data[p:p+4]` 与候选集（全部 < p）。一旦
  `p+4 <= len(buf)`，这 4 字节与候选集都不再变，故"无匹配"的判定是终局。
- 位置 p 的**匹配长度**取决于可用数据量：若匹配一直延伸到缓冲区末尾，更多
  数据到达后它可能变长。因此规则是：匹配终点 `< len(buf)`（存在失配字节）
  或长度达到 `MaxLen` 上限时，该匹配才终局，才允许发出。
- 字面量**记录**不能逐字节发（否则切法不同会产生不同分段），所以挂起中的
  字面量只计数不发出，直到遇到一个终局匹配（先发字面量再发回指）或
  Flush/Close 时才落盘。
- 结论：压缩器只需缓存"自上一个终局 token 起"的未决字节；token 序列是
  「原文 + Flush 偏移」的纯函数，与 Write 切法无关。
- 对 Flush 的含义：Flush 是调用者声明的"此处之后的数据暂不可知"，因此
  **必须强制截断**当前未决匹配（按现有数据截断长度）并发出全部挂起字面量，
  再加刷新标记。所有切法在相同 Flush 偏移做同样的截断，一致性保持。

## 推导 2：重叠回指的复制（语义 2）

- 距离 d < 长度 n 时，源区间与目标区间重叠。逐字节向前复制时，第 i 个输出
  字节读的是"已输出的第 i-d 个字节"，得到周期 d 的展开（`abc` 以 d=3 展开为
  `abcabc...`）。一次性块复制（如 `copy`）要求源已完整存在，重叠时语义不同。
- 实现：解压器维护输出窗口，`for i<n { b=win.Get(d); win.Add(b); out=append(out,b) }`。
- 为什么逐字节循环可接受：输出本身就有 n 个字节要产生，任何实现都是 Omega(n)；
  该循环是 O(n)，窗口 Get 是 O(1) 环形寻址、无 memmove，渐近最优且正确性直观。

## 推导 3：dist 超窗口 vs 超已输出（语义 7）

- 「超已输出」是**流内逻辑错误**：历史里根本没有这么多字节，任何压缩器都
  不该产生；「超窗口」是**能力错误**：历史存在但已滑出窗口，解压器无法取回。
  二者成因与修复方式不同（流损坏 vs 窗口配置不匹配），必须可区分。
- 压缩器侧：匹配器链式游走时 `pos-cand > win` 即断链，故 dist <= win 恒成立，
  绝不可能产生「超窗口」；dist <= pos 由候选定义保证，也不会「超已输出」。
- 并行块压缩：预置字典 = 上一块末尾 <= win 字节，块内位置 p 的候选满足
  `p-cand <= win`，dist <= win 不变。解压侧「已输出」= 截至当前已还原的全部
  字节（含前序各块），字典只是让压缩器提前知道这些字节，含义不变。

## 推导 4：并行块压缩的确定性（语义 9）

- 块 i 的输入 = 本块原文 + 上一块末尾 win 字节（字典），二者都是**输入数据
  的切片**，不依赖其他块的压缩结果、完成顺序或 goroutine 调度。
- 匹配器对同一（字典+块）是确定性的贪心最长匹配：哈希链按位置递增建立，
  候选考察顺序固定，故每块 token 序列唯一，workers=1/2/4/8 输出逐字节相同。
- 衔接：每块产出自洽的 token 序列（字面量段与回指都自带长度），按块序直接
  拼接即可，**不需要**块间刷新标记；回指天然跨块指向字典（已解码的上一块
  尾部）。流头写一次，流尾按全文总长与校验和写一次。
- 并发安全：每 goroutine 只写自己下标的结果槽，主 goroutine 在 `wg.Wait`
  后按序拼接，happens-before 由 WaitGroup 建立，`-race` 干净。

## 复杂度

匹配器每位置至多考察 `MaxChain` 个候选（非导出计数器 `cand` 记录），故
候选总数 <= MaxChain x 输入字节数，与窗口/规模无关；插入每位置 O(1)。
