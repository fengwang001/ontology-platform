# 带转义的文本 RLE 编解码器 — 设计推导

## 严格解码的拒绝清单（由 `Encode(Decode(t)) == t` 逐条推导）

不变量：合法编码唯一。凡接受后会导致某个 t 经解码再编码回不去的输入，都必须拒绝。

1. 显式次数 1：若接受 `1a`，Decode→`a`，Encode(`a`)=`a` ≠ `1a`（编码器省略次数 1）。拒绝。
2. 次数 0：若接受 `0a`，Decode→``（零个 a），Encode(``)=`` ≠ `0a`（编码器从不写 0）。拒绝。
3. 前导零：若接受 `01a`，Decode→`a`，Encode→`a` ≠ `01a`（编码器不写前导零）。拒绝。
4. 相邻游程同符号：若接受 `2a3a`，Decode→`aaaaa`，Encode→`5a` ≠ `2a3a`（编码器合并最长游程）。拒绝。
5. `\` 后非数字非反斜杠：若接受 `\a`，Decode→`a`，Encode→`a` ≠ `\a`（编码器只转义数字与 `\`）。拒绝。
6. 末尾孤立 `\`：若接受 `a\`，无论 Decode 产出什么（如 `a`），Encode 都不会以 `\` 结尾。拒绝。
7. 只有次数没有符号：若接受 `3`，没有任何码点可供重复，任何解码结果再编码都不等于 `3`。拒绝。
8. 非法 UTF-8：编码器输出恒为合法 UTF-8；含非法字节的 t 不可能等于任何 Encode 输出。拒绝。

## 大次数的取舍（第 3 条）

选择**支持**任意大的次数：`runs` 用 `math/big.Int` 读写十进制次数，杜绝整数溢出。
但解码输出字节数设可配置上限（`NewDecoder(limit)`，`Decode` 用 `DefaultLimit`）：
展开前先比较 `n*宽度 ≤ 剩余额度`（big.Int 比较，不溢出），超限返回哨兵错误
`ErrOutputLimit`；因此内存分配只与上限成正比，绝不与次数成正比。

## 测试对照表（第三节 5 条语义 → 测试函数）

（待测试写定后补齐）
