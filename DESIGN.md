# DESIGN：批量导入幂等与断点续传器

## 1. 进度粒度推导
写入按业务键幂等：`store.Put(key)` 对同一 key 多次写入结果逐字节相同（唯一键约束，已存在即覆盖为同值）。
因此「进度落后于真实写入」是安全的——重放只会重复执行幂等写，不产生重复数据。
结论：进度按区间粗粒度刷盘。每写满 `N`（区间长度，默认 1000）条刷一次，记录最大已确认区间
`[0, checkpoint)`。崩溃时末尾不足 N 条状态不明，续传从 checkpoint 重放至多 N-1 条，
吞吐与正确性兼得。若写入非幂等则必须每条刷盘（细粒度），本题不采用。

## 2. 阶段与文件布局（tempdir/<batchID>）
- `progress.bin`：魔数(8)+total(8)+checkpoint(8)+committed(1)，CRC32-IEEE 覆盖前述字节，追加于尾。
- `lock`：占用标记，内容为 `deadlineUnixNano`（由可注入时钟判定）。不同批次文件互不影响。
- Prepare：校验批次（空 ID 拒绝；批内重复业务键拒绝并给两处位置），获取锁；无进度则从头。
- Write：以分片（内存上限 M）迭代 `[checkpoint,total)`，幂等写入，满 N 刷进度。
- Verify：以存储实际内容为准核对（见第 4 节）。
- Commit：置 committed=1 并刷盘；重复导入时若已 committed，返回「本批次已完成」，不报错。

## 3. 重复导入幂等判定
存储为 `map[key]value`（值=业务键），整批内容是集合。第二次导入：Prepare 读到 committed=1
即短路返回 ErrAlreadyComplete；即便不短路，逐条幂等 Put 也使最终 map 与首次逐字节相同。

## 4. 以存储为准，而非以进度为准
进度文件可能因崩溃或人为篡改而「声称超前」（如称已写 5 万、实际仅 3 万）。进度只是提示，
存储才是事实源。Verify 线性扫描清单区间，逐键查 store，求出真实已写前缀与缺口区间，
并把进度回退到真实位置后续传。绝不相信未被存储证实的进度。

## 5. 在途可见性
未 Commit 的记录已物理写入 store，必须对 reconcile 可见但标注「在途」，不计入已提交统计。
reconcile 一次线性遍历：吸收进度快照后只遍历 store 一遍、清单一遍，访问次数 ≤ 3n。
输出三段：已写入区间 / 缺口区间（精确到端点）/ 多余记录（store 有而清单无）。

## 6. 故障注入
- 写失败：注入第 k 条 Put 失败，进度停在失败点之前（checkpoint≤k），续传从该点起，终态一致；测 k=1/中/末。
- 截断：对 progress.bin 逐字节截断 1..len-1，分类 头部不完整/体不完整/CRC不匹配，
  回退最大可恢复前缀；全截断点循环覆盖。
- 进度与存储不一致：人为写超前进度，Verify 检出缺失区间并以存储为准回退。

## 7. 并发占用
lock 存 deadline；第二个导入器立即返回 ErrBatchBusy。崩溃后 stale 锁可被接管：
时钟未到 deadline 判「仍活跃」拒绝；超过 deadline 判「已过期」可接管。不同批次独立，可并发。

## 8. 复杂度/资源
续传重写数为真实缺失前缀中被重放条数，≤ 粒度 N（10 万条在第 7 万中断场景）。
记录按分片加载，历史驻留峰值 ≤ M，计数器断言。reconcile 访问计数 ≤ 3n。

## 9. 边界
零记录批次 Commit 即完成；单记录；空业务键合法；空批次 ID 拒绝；批内重复键拒绝（键+两位置）；
进度文件不存在视为从头开始。
