# 设计推导

## 1. 空行与单列空值
选择“跳过空行”：空行没有字段数据；但 `""` 是一个显式加引号空字段，必须保留。因此单列表中的未引号空记录若写成空字节加换行，会被当成空行跳过；回写器必须把该唯一字段写成 `""`。多列记录中的空字段仍可写成空字节，因为逗号固定了字段位置。

规范输入定义：无开头/相邻行尾、无末尾行尾、记录间用 `\n`、只在必要时加引号；所以 `Write(Parse(x))` 可对规范输入逐字节还原。任意成功解析结果只保证语义与引号标记往返。

## 2. 并行切点
每段并行做两个假设：起点在引号外、起点在引号内。段尾输出词法事件、引号内/外状态、闭合引号后状态和 CR 待定状态。前一段选定状态从左到右决定后一段采用哪个假设；故每字节最多两遍。
切点在字段中间时，两段都产生“未闭合字段开始/文本”事件，拼接器合并相邻 Open/Text，采用最早起点和引号标记，并拼接文本。切点恰在闭合引号后时，下一段用“引号外”结果，并用其首字节修正：逗号或行尾只补一个字段/记录边界，其他字节立即报“闭合后杂字符”。切点在 `\r` 后时，下一段首字节必须是 `\n`；用引号外结果并去掉其为首个空字段产生的边界，否则在原 `\r` 偏移报孤立 CR。
段内记录号、字段号从 1 开始；拼接时加上此前已接受记录/字段数。跨段字段只计一个字段，偏移使用加过段起点的全局偏移。

## 3. CR 待定
未引号字段或字段开始处见到 `\r`，字段值先截止并进入 CR 待定；下一字节是 `\n` 才成行尾，否则在该 `\r` 偏移报错。半包结束于 `\r` 时，`Close` 报孤立 CR。引号内的 `\r` 永远是内容，随后的 `\n` 也是内容，因此 CRLF 原样保留。并行段尾为 CR 待定时按上一节只悬置一个字节，不重扫长字段。

## 4. 上限的即时性
字段上限统计“逻辑值字节”：未引号字段每个非分隔/非行尾字节计一；引号字段普通字节计一，转义 `""` 整体在第二个引号到达时计一；开引号和闭引号不计。计数在追加每个逻辑字节前检查，首个超限字节即终止。字段数在分隔符处、记录数在记录边界处立即判定。终态缓存原错误。

## 5. 状态转移表
| 状态 | 普通字节 | `,` | `"` | `\r` | `\n` | EOF |
|---|---|---|---|---|---|---|
|字段开始|入未引号/追加|空字段结束|入引号|CR 待定|结束记录|无输入则无记录|
|未引号字段|追加|字段结束|错误：未引号引号|CR 待定|结束记录|结束最后记录|
|引号字段|追加|追加|到“刚见引号”|追加 CR|追加 LF|错误：引号未闭合|
|刚见引号|错误：闭合后杂字符|字段结束|追加一个引号|字段结束且 CR 待定|结束记录|字段结束并结束记录|
|CR 待定|错误：孤立 CR|错误：孤立 CR|错误：孤立 CR|错误：孤立 CR|结束记录|错误：孤立 CR|

所有错误携带全局字节偏移、记录号、字段号；偏移从 0 开始，号码从 1 开始。
