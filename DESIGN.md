# 严格模式流式 Base64 设计

## 规范形式的推导（语义第 2 条）

Base64 把 3 个字节（24 bit）编码为 4 个 6 bit 字符。当输入长度不是 3 的
倍数时，最后一组不足 24 bit，编码器把剩余比特**左对齐**后放入若干个 6 bit
字符，未用到的低位补零，再补 `=` 凑满 4 字符：

- 剩 1 字节（8 bit）：产生 2 个字符。第 2 个字符只承载 2 个有效比特，
  其**低 4 bit 必须为零**。
- 剩 2 字节（16 bit）：产生 3 个字符。第 3 个字符只承载 4 个有效比特，
  其**低 2 bit 必须为零**。

宽松解码器只取有效比特、忽略这些填充位，因此 `QQ==` 与 `QR==` 都解出 `A`
（`Q`=16，`R`=17=0b010001，低 4 bit 为 0001）。但任何编码器对 `A` 的输出
唯一地是 `QQ==`：`QR==` 不可能由任何字节串编码产生。严格模式要求 Decode 是
Encode 的逆（即 `Encode(Decode(y)) == y` 对所有被接受的 y 成立），因此必须
拒绝一切「编码器不可能产生」的输入。结论——统一规则：

> 含填充的末组中，未被数据占用的尾部比特必须全为零。

判定只用两处比特：`xx==` 检查第 2 字符值的 `& 0x0F`，`xxx=` 检查第 3 字符
值的 `& 0x03`，非零即「非规范尾部」。填充本身还须满足：只出现在最后一组、
仅在该组末尾、个数为 1 或 2、其后（除流结束外）不得再有任何字节；无填充时
总长度必须是 4 的倍数。由此 `QR==`、`QUJ=`、`QQ`、`QQ=`、`QQ===`、
`QQ==QQ==` 全部被拒，而 `QQ==`、`QUI=` 与空输入合法。

## 语义与测试对照表

（待测试完成后补齐）
