# DESIGN

## 流格式（全部整数为无符号 LEB128 varint；最多 10 字节，溢出 64 位报错）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `LZ77O`（5B）+ 版本 `1`（1B） |
| 字面量段 | tag `0` + varint n + n 个原文字节 |
| 回指 | tag `1` + varint distance + varint length（length≥3） |
| 刷新标记 | tag `2`（无载荷） |
| 流尾 | tag `3` + varint 原文总长 + varint CRC32(IEEE) |

并行块：每块是一条独立完整流（头+记录+尾），直接首尾拼接；解压器见到流尾后可继续读下一条流头。

## 推导 1：压缩与 Write 切法无关
压缩器把两次 Flush 之间的 Write 全部追加进一个待决缓冲，不在 Write 边界做任何决策；
只有 Flush/Close 才对「累计原文」跑一次确定性的贪婪扫描。最长匹配是否跨过某次 Write
边界因此无从影响结果：扫描器看到的字节序列、哈希链状态（历史窗口在多次 Flush 间保留）
完全相同，输出字节就相同。代价是 Flush 之前不产出任何压缩字节；Flush 必须强制结束当前
匹配并发出 tag2，因为调用方要求此刻字节足以让解压端还原全部输入，而未决匹配的长度本可
随未来字节继续增长，不结束它就无法定长写出。

## 推导 2：重叠回指（distance < length）
块复制 `copy(dst, src)` 当 src、dst 重叠时，Go 的 copy 按正向逐字节搬运，`abc`+dist3
复制 30 字节恰好得到重复；但语义上规范写法是每输出一字节就从「已输出序列」取
`out[len(out)-dist]`，新字节立刻成为后续可取的历史。实现采用该逐字节 append 循环，
其代价是 O(输出长度)，而解压总输出受可配置上限约束（解压炸弹前置检查），故成本有界、
可接受；dist=1 时它自然表现为游程填充。

## 推导 3：两类距离错误为何分开
「distance > 已输出字节数」是相对错误：流本身引用了尚不存在的历史，任何解压器都无法还原；
「distance > 窗口容量」是配置错误：数据可能合法，但超出本解压器承诺保留的历史。压缩器只
从容量为 cap 的环形窗口取候选，故绝不可能产生第二类。并行块预置上一块末尾 cap 字节为字典，
「已输出」的语义扩展为「字典 + 本块已输出」：距离先与 cap 比较（超容量），再与
len(dict)+len(out) 比较（超历史），二者仍可区分；字典不计入本块流尾声明的长度与校验和。

## 推导 4：并行块的确定性
第 k 块的压缩输入只有「本块原文」与「上一块原文末尾 cap 字节」（预置字典）；哈希链初始只
插入字典，扫描仅由这些字节决定，与更早块、goroutine 完成顺序、并发度无关。块内匹配可伸入
字典，块间不允许跨两块（只给一块字典），因此无需任何调度协调，worker 数 1/2/4/8 产出逐字节
一致。衔接不需要刷新标记：每块自带流头/流尾，按固定偏移顺序拼接即可；解压端按连续多条流
解开，字典由前一条流还原出的原文自然提供。
