# 严格模式流式 Base64：规范尾部推导

一个 4 字符组 `c0 c1 c2 c3` 承载 24 个数据比特，各字符贡献 6 比特。
按输入字节余数分三种形状，只有这三种是合法编码形状：

- 3 字节余数 0：`XXXX`（无填充），24 比特全部是数据。
- 2 字节余数 1：`XXX=`，24 个槽位中只有前 16 个是数据，末 8 个由字符
  `c3` 的 6 比特和 `c2` 的低 2 比特组成。
- 1 字节余数 2：`XX==`，只有前 8 个是数据，末 16 个由 `c1` 的低 4 比特
  和整个 `c2` 组成。

非数据槽位不承载任何信息。若允许它们取任意值，同一字节串会有多个编码
（例如 `QQ==` 与 `QR==` 都解出 `A`：二者只有 `c1` 的最低位不同，而该位
正是 `XX==` 形状中不承载数据的槽位）。为保证「对任意字节串，编码结果
唯一」（编码与被接受编码一一对应），严格模式规定：

**所有不承载数据的槽位必须为 0。**

因此「非规范尾部」只检查以下比特：

- `XX==`：`c1` 的低 4 比特必须全为 0（`c2` 是 `=`，不参与）。
- `XXX=`：`c2` 的低 2 比特必须全为 0。
- `XXXX`：无需检查。

验证：`QQ==` 中 `Q=16=010000`，低 4 位为 0，合法；`QR==` 中 `R=17`，
低 4 位为 1，拒绝。`QUI=` 中 `I=8=001000`，低 2 位为 0，合法；`QUJ=`
中 `J=9`，低 2 位为 1，拒绝。填充形状本身也受限：`=` 只能作为结尾连续
出现，只允许 `=`/`==` 两种，且填充组只能是整个流的最后一个 4 字符组。
