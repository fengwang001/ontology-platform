# DESIGN

## 代理对的错误偏移（第 2 条推导）

规则：
- 孤立低代理（`\uDE00`）：偏移指向该单元自身的起点（反斜杠）。低代理
  永远不能作为一个字符的开始，错误就在它本身。
- 高代理后未跟合法低代理（`\uD83D"`、`\uD83DA`、`\uD83D\uD83D`）：偏移
  指向高代理单元之后、应当出现第二个 `\uXXXX` 的位置。高代理本身语法合法，
  错在“后继单元缺失或不合格”，偏移应指向可修复点，而不是高代理起点。
- 顺序颠倒的 `\uDE00\uD83D` 按第一条规则在第一个低代理处立即报错。

为什么 `"\uD83DA"` 不能解释成「U+FFFD 加 `A`」：严格解码要求每个被接受
的字面量具有唯一解析。高代理只是半个码点，本身不对应任何可输出字符；
若静默补一个替换符再继续，不同的非法输入会映射到同一输出，错误被吞掉，
调用方无法区分“数据本来就含 U+FFFD”与“数据损坏”，往返性质也随之破坏。
RFC 8259 规定高代理后必须紧跟 `\u` 加低代理，而 `A` 不是 `\`，错误即在该处。

## 最小转义与唯一性（第 4 条推导）

- U+0000–U+001F 必须转义：RFC 8259 的字符串语法直接禁止它们以未转义
  形式出现。U+007F（DEL）不在该区间，是语法合法、可无损传输的普通字符，
  转义它纯属冗余，故原样输出。
- 唯一性：每个码点的输出由一条确定规则给出——需要转义者有唯一形式
  （五个短转义优先，其余控制字符用小写 `\u00XX`），其余码点一律原样；
  不存在任何自由选择（不转义 `/`、不用大写十六进制、不对可原样字符写 `\u`）。
- 单射：不同输入的码点序列不同，经逐码点确定映射后拼接，输出必不相同；
  同时 `Decode` 是 `Encode` 的左逆（见往返测试），故可逆且输出唯一。
