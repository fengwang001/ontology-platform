# 设计推导

## 代理对错误偏移（语义 2）

规则：每个 `\uXXXX` 与普通字节一样，是一个从反斜杠起、长度固定为 6 的「词法单元」，
所有错误偏移都指向**第一个违规词法单元的起始字节**。

- `"\uD83D"`：高代理单元后必须紧跟低代理单元；后续是结束引号（无后续单元），
  故高代理无法闭合，偏移指向高代理起点（`\`）。
- `"\uDE00"`：低代理只能作为高代理的后续单元出现；它前面没有高代理，
  偏移指向这个低代理单元自身的起点。
- `"\uD83D"` 后紧跟普通字符（`x` 或 `A`）：高代理后唯一合法后续是低代理单元，
  违规的是「高代理缺少合法后续」，偏移指向高代理起点，而不是那个普通字符。
- `"\uD83DA"` 不能解释成「U+FFFD + A」：解码器没有「替换」分支，高代理是一个
  尚未完成的组合请求，除紧跟低代理外的任何后续（含结束引号）都使该高代理单元
  本身非法；允许替换就等于接受孤立代理，违反 RFC 8259。
- `"\uDE00\uD83D"`：第一个单元即非法低代理，偏移指向它；首错优先。

## 最小转义与唯一性（语义 4）

RFC 8259 对字符串体的唯一强制要求是 U+0000–U+001F 不可裸写；U+007F 不在该
区间，可原样输出，转义它无收益。`"` 与 `\` 必须转义，否则破坏字面量边界或
转义语义；`/` 不必转义。每个被转义码点有且仅有一种输出形式（短形式优先，
其余一律小写 `\u00XX`），未转义码点原样输出：同一输入的输出逐字节唯一；
且转义形式必以 `\` 开头而裸字符不会产出反斜杠转义，不同输入不可能同输出，
故 `Encode` 是单射。配合严格解码即得双向往返。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类严格错误可区分且带偏移 | TestStrictErrors |
| 2 代理对组合与拒绝 | TestSurrogates |
| 3 非法 UTF-8（解码/编码两方向） | TestInvalidUTF8 |
| 4 最小转义编码 | TestMinimalEscape |
| 5 往返 | TestRoundTrip |
| 6 跨切分点一致 | TestSplits |
| 复杂度：检查计数 ≤ 2n | TestInspectionBudget |
