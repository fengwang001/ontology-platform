# DESIGN

包依赖：`eol`、`ws`、`span` 互不依赖；`norm` 依赖前三者；`par` 依赖 `norm`、`span`。

## 推导 1：空输入与只含换行的输入

先证一个全局约束：第 6 条要求对所有输出偏移 o 有 `ToOut(ToOrig(o)) == o`，即
`ToOut` 必须满射到 `[0, outLen]`。`ToOut` 的定义域只有 `origLen+1` 个值且单调不减，
所以必须 `outLen <= origLen`：规范化不得插入任何原文字节。
因此「确保恰好一个」对空输入只能输出 `""` 而不是 `"\n"`——两者各自都满足幂等
（`N("")==""`；`N("\n")=="\n"` 因为已恰好一个），但 `"\n"` 里那个换行没有原文前驱，
`ToOut` 无法满射，直接违反第 6 条。同理 `"a"` 不能补成 `"a\n"`。

三种策略由此定义为（t = 规范化后输出末尾的 `\n` 游程长度）：

- Keep：不动。
- EnsureOne：`t >= 1` 时收缩为恰好 1 个；`t == 0`（含空输入）保持不变（不能插入，见上）。
- Strip：删除全部末尾空行；若删除后还有内容，内容行保留一个换行（即 `t>=1` 收缩为 1）；
  若整个输出全是换行（无内容行），则全部删去得 `""`——「保留一个换行」是为内容行保留，
  纯空行文件没有可保留换行的行。这也让 Strip 与 EnsureOne 可区分。

幂等：EnsureOne 的输出末尾 `\n` 游程为 0 或 1，再规范化不变；Strip 的输出要么为空，
要么以「内容 + 单个 `\n'`」结尾、无末尾空行，再规范化不变。

样例表（行尾空白先按第 2 条删除，`\r\n`→`\n`）：

| 输入        | Keep     | EnsureOne | Strip    |
|-------------|----------|-----------|----------|
| `""`        | `""`     | `""`      | `""`     |
| `"\n"`      | `"\n"`   | `"\n"`    | `""`     |
| `"\n\n"`    | `"\n\n"` | `"\n"`    | `""`     |
| `"  \r\n"`  | `"\n"`   | `"\n"`    | `""`     |

## 推导 2：被删字节的 ToOut

定义 `ToOut(i) = 由原文前缀 [0,i) 产生的输出字节数`。它天然单调不减；被保留字节
恰好在自己的输出位置上，故 `ToOut(ToOrig(o)) == o` 成立（`ToOrig(o)` 取产生输出位置 o
的那个原文偏移，终点互为 `len`）。推论：被删字节（行尾空白、`\r\n` 的 `\r`、被策略删掉的
换行）映射到删除点的输出位置，即下一个被保留输出字节之前。
编辑器把光标放在被删空白里时，映射后落在该行换行符之前（逻辑行尾），不会跳到下一行；
落在 `\r` 上的光标与该 `\n` 同位置（换行符前）。末尾被策略删掉的换行映射到输出终点。

## 推导 3：缓冲超限——拒绝还是强制输出

强制把待定空白判定为「非行尾」提前输出：若随后真出现行尾，输出就含行尾空白，
违反第 5 条（输出不得含行尾空白）且 `N(N(x))` 会把这串空白删掉、不等于 `N(x)`，
同时违反幂等；不可接受。选择拒绝：返回带原文偏移的可判定哨兵错误 `ErrBuf`
（偏移 = 空白游程起点 + 上限，即超限那一字节），已输出内容保留，实例进入终态
（与严格模式 NUL、输出超限、Close 后写入一致，错误彼此可用 `errors.Is` 区分）。
第 4 条不受影响：超限与否只取决于空白游程长度，与切分方式无关。

## 推导 4：par 的拼接

每段用 Open 模式规范化：段尾不结算，把「待定尾巴」原样交出——要么是 1 字节 `\r`
（`eol` 的待定态），要么是一串空/制表符（`ws` 的缓冲），二者互斥。拼接从左到右维护
`carry`（上一段的尾巴），对当前段：

- carry 是 `\r`：看当前段第一个原始字节，是 `\n` 则 `\r` 记为删除（`\r\n` 合体，
  该 `\n` 由本段正常输出），否则 `\r` 输出为 `\n`（单独 `\r`）。
- carry 是空白串 W：若当前段整段仍是空白（body 为空、尾巴仍是空白），把本段尾巴并入
  W 继续向后看（合并长度超 `MaxBuf` 时报与单线程相同的 `ErrBuf`，偏移一致）；否则在
  原文中扫到本段第一个非空白字节：是 `\r` 或 `\n` 则 W 是行尾空白、记删除，否则原样输出 W。
- 段内映射是全局原文坐标（`norm` 支持 `Base` 偏移），输出坐标由 `span.Map.Append`
  按段序平移拼接（追加时自动接在当前输出末尾并合并相邻同类区间）。

修正范围有上界：carry 本身不超过 max(1, MaxBuf)；合并空白游程超 MaxBuf 即报错
（与单线程逐字节行为一致）；扫描终结符的范围不超过下一段的行首空白游程
（该游程段内已缓冲，同样不超过 MaxBuf）。末尾换行策略在拼接后的整体输出上统一执行
（`norm.TrimPolicy`），被删换行的原文位置由 `Map.ToOrig` 反查，保证与单线程逐字节一致。
