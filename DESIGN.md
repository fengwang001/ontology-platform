# DESIGN：自定义 LZ77 流式压缩

## 记录布局（wire）

整数用 LEB128 无符号变长；超过 10 字节或第 10 字节 >1 即超长/溢出。

| 记录 | 布局 |
|---|---|
| 流头 | 魔数 `L7` + 版本 uvar=1 + 窗口容量 uvar + 链上限 uvar |
| 字面量 | tag=0 + 长度 uvar(>=1) + 原始字节 |
| 回指 | tag=1 + 距离 uvar(>=1) + 长度 uvar(>=1) |
| 刷新 | tag=2（无负载） |
| 流尾 | tag=3 + 原文总长 uvar + CRC32-IEEE(原文) uvar |

tag 为一个字节的低 2 位，区分四类记录（字面/回指的长度字段另用 uvar）。

## 推导 1：压缩结果与 Write 切法无关

在偏移 i 做贪心决策需要知道「从 i 起的匹配能延伸多远」。若当前缓冲尾部恰好匹配到末尾，下一次 Write 可能把匹配继续延长，此刻决策会依赖切法。因此压缩器维护未决缓冲：仅当满足下列任一条件才决策偏移 i：(a) i 处 3 字节哈希无候选（新增字节不可能凭空在 i 之前造候选，故可确定发字面量）；(b) 最长匹配长度 < 缓冲剩余字节（下一字节已证伪延长）；(c) 匹配达到全局匹配上限 M（不能再延长）。决策一旦做出即为全部切法共享的同一条解析，字面量只在「下一条匹配确定前」或块尾才成段输出，故字面量段边界也与切法无关。Flush 强制把缓冲排空：调用者承诺此刻不会再来字节延长当前匹配，故 Flush 必须终结当前匹配（条件 c 的人为版本）并立即输出刷新点。

## 推导 2：重叠回指的解压复制

距离 d、长度 L、d<L 时，源窗口内本没有 L 个独立字节；正确语义是输出序列以长度 d 为周期重复。一次性块复制只有在逐字节重叠复制时才偶然正确，且无法在写出前做整段超限判定。实现显式逐字节：out = append(out, out[len(out)-int(dist)])。该循环每字节只做一次下标与追加，解压总复杂度仍是输出字节数的线性量，常数极小；故不需要块复制技巧。

## 推导 3：两种「距离过大」为何分开

检查顺序：dist==0 -> dist>窗口容量 W -> dist>已产生字节数。dist>W 是结构性非法：任何合法流的距离永不超过构造时声明的 W（压缩器环形窗只保留 W 字节，匹配器返回距离 <=W；并行块预置字典也最多取上一块末尾 W 字节），它说明流头参数或回指被篡改，与「合法窗口但历史尚浅」语义不同，必须是不同哨兵错误。dist>已产生字节数是时序性非法（回指指向尚未输出的位置）。并行块里预置字典不算「本块已输出」：流是连续的，字典字节属于上一块，在全局偏移上早已产生。解压器只有一个连续输出缓冲，字典通过普通回指记录在流中自然重建——解压器根本不知道块边界，故「已输出字节数」含义不变，也不需要特殊字典记录。

## 推导 4：并行块压缩的确定性

第 k 块压缩所需状态只有「本块原文」与「上一块原文末尾至多 W 字节」（用于预置哈希链与环形窗）；它不读任何其他块的压缩字节、完成顺序或共享可变状态。故把数据按固定 blockSize 切块后，每块是纯函数 encBlock(block, prevBlock)，goroutine 数只影响调度、不影响字节。衔接方式：单流头 + 各块记录直接拼接 + 单流尾。块边界处不插刷新标记——块边界不是 Flush 语义边界，标记会随 worker 数改变输出；匹配记录本就自描述（距离/长度），跨块回指在解压端表现为对早已输出字节的普通回指。块 k 起点的哈希链只由 prev 末尾预置，故即使两块在同一 worker 顺序执行，解析也与「单压缩器吃整段」在该起点的状态一致。

## 关键常量与上限

- W 默认 16384（可配，构造时拒绝 0）；链上限默认 32（拒绝 0）。
- 匹配下限 3，匹配长度全局上限 M=1024：候选考察数为 链上限 x min(W/M, 规模)，两档规模每字节考察数之比 <=2；4MB 全同字节用 1024 长回指，压缩结果 <64KB。
- 解压炸弹：记录处理前先判 已输出+长度 > 上限，拒绝前不分配不复制；终态后 Write 永远返回同一错误。
- 单个 Encoder/Decoder 实例非并发安全；多实例可并发。
