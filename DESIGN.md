# DESIGN

## 1. 空行与单列空值
连续两个记录结束符之间没有任何字段分隔符，按 RFC4180 语义无法形成一条记录（记录至少含一个字段，且无法与「末尾换行不产生空记录」区分），故选择**空行一律跳过**（`""\n` 不跳过）。后果：单列表里一条「唯一字段为空且未加引号」的记录若写作 `\n`，会被解析器当成空行跳过——数据丢失。因此 writer 对**单列空值一律写成 `""`**（必要引号情形之一）。多列时空行天然畸形（列数不符），跳过空行不丢任何可表示的表。文件完全为空 = 0 条记录。

## 2. par 切点
记录结束符可能藏在引号字段内，故段边界只有两种可能状态：在引号外(out)或引号内(in)。每段在两种入口假设下各跑一次纯函数 `RunSegment`（每段常数次，总处理 ≤2N+8K），从左到右确定真实入口：段0为 out；段 i 的真实入口由段 i-1 在其真实假设下跑完时**是否在引号内**决定。切在 `\r`、`""` 中间时，重放前一段最后 1 字节作为 `lead`（不占全局偏移、不计字节计数），即可用同一状态机恢复 sCR/sQuoQ 待定状态。拼接：段首若是上一记录的残余，其 OpenField/Cell 追加到悬挂字段；引号内假设下引号闭合前的事件全部属于上一记录残余，闭合后事件转为本段新记录。记录/字段编号完全由 `table.Builder` 按全局顺序赋予，错误只需带全局字节偏移（段内事件偏移本就是绝对偏移）；未闭合等错误由最终段产生，位置天然全局。首个错误按全局字节偏移最小者为准。

## 3. CR 待定
sCR：引号外读到 `\r` 后不能判定。续传时它只是可暂停状态；下一字节是 `\n` 则结束记录（结束偏移不含 CRLF），否则立刻 ErrLoneCR（位置为该 `\r`）。Close 停在 sCR 也报 ErrLoneCR。par 中切点落在 `\r` 与 `\n` 之间：下一段以 lead='\r'、sCR 起步。引号字段内 `\r` 是普通内容，无待定状态，原样保留。

## 4. 上限「立刻」
字段字节数在字节**进入字段内容**时计数：普通字符、引号内字符（转义 `""` 两个输入字节只计 1——闭合 quote 后见到转义 quote 追加 1 字节时计 1），每加一字节前先比较，超限立刻 ErrFieldTooLong，不等记录结束。单记录字段数在每个逗号开始新字段时计数。总记录数在每条**保留**的记录完成时计数。命中即终态，后续 Feed/Close 返回同一错误。

## 5. 状态转移表（x=出错字节偏移；a=追加，+F=新字段，+R=结束记录，e=错误）
| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| sField | +F,sField | e QuoteInBare | +R,sField | sCR | a,sUnq |
| sUnq | +F,sField | e QuoteInBare | +R,sField | sCR | a,sUnq |
| sQuo | a,sQuo | sQuoQ | a,sQuo | a,sQuo | a,sQuo |
| sQuoQ | +F,sField | a,sQuo | +R,sField | sCR(引号外) | e GarbageAfterQuote |
| sCR | e LoneCR(x=\r) | e LoneCR | +R,sField | e LoneCR | e LoneCR |

EOF：sField/sUnq/sQuoQ→+R；sQuo→e UnclosedQuote；sCR→e LoneCR。空行（+R 时记录 0 字段）跳过；首条保留记录定义列数，不符 e ColumnCount。
