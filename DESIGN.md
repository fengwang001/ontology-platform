# LZ77 流格式设计推导

## 0. 记录字节布局

头:`4C 5A 37 37`("LZ77") + 版本 uvarint(=1)。其余整数均为小端 uvarint(<=10B)。

| 记录 | tag | 布局 |
|---|---|---|
| 字面量 | 0 | tag,长度 n,n 字节原文 |
| 回指 | 1 | tag,距离 d>=1,长度 L>=3 |
| 强制刷新 | 2 | tag |
| 流尾 | 3 | tag,原文总长,CRC32(IEEE) |

tag 是 1 字节,uvarint 解析到第 11 字节或溢出 64 位即报错。

## 1. 语义 3:输出与 Write 切法无关

结论:压缩器只认「累计已提交偏移」,不认 Write 边界。维护一段 `pend`(尚未决定的输入)。
- 新数据追加进窗口与 `pend` 后,在 `pend` 上贪心:位置 p 若能找到长度>=3 的匹配,提交 p 之前的字面量,记录开匹配并提交匹配段;之后每次新数据先尝试延长当前开匹配,失配才回到起点继续找。
- 关键:位置 p 一旦被「决定」(字面量或匹配),其长度只可能随未来输入**增大**,绝不可能改变形状(匹配的起点不会因后续字节变掉),因此任何切法产生的 token 流一致;`Flush(offset)` 位置固定,字面量分段点也就固定,字节流相同。
- Flush 必须强制结束开匹配:它承诺「到此为止的所有输入即刻可解」,于是把未决字节全部按当前形态提交并写 tag2。故 Flush 可能截短一个本可更长的匹配——这是允许的,因为 Flush 位置是契约的一部分。

## 2. 语义 2:重叠回指的复制

d<L 时块复制 `out[len:]=out[len-d:len-d+L]` 会把未初始化区域当历史(Go 越界 panic)。正确语义是
`out=append(out, out[len-d])` 重复 L 次,即「刚复制出的字节立刻成为后续源」。
实现:`for k:=0;k<L;k++{out=append(out,out[len-d+k])}`,切片越界访问始终指向已写位置。
4MB 最坏情形下它是线性的 O(输出),本身就是解压的渐近下界,不退化(不像匹配器那样随窗口平方增长),故直接逐字节复制可接受。

## 3. 语义 7:两种「距离超限」为何要区分

- 「距离>窗口容量」说明流不可能由本配置的压缩器产生:压缩器的窗口是全部历史,任何发出的 d 都<=窗口容量,且空窗时绝不发回指。它属于**格式/配置伪造**。
- 「距离<=窗口容量但>已输出字节数」说明流在当前解码点上引用了尚不存在的字节,属于**时序截断/损坏**。
并行预置字典不改变含义:解压端没有字典,字典内容已作为前一块真实输出存在。到任何回指时,窗口容量与「已输出前缀长度」的判定方式与单块完全一致;它只影响压缩器候选链的初值(预置前一块末尾 WindowCap 字节)。

## 4. 语义 9:并行块的确定性与衔接

固定块边界由 data 与 blockSize 唯一决定,与 workers 无关。块 i 的匹配器只预置「块 i-1 原文末尾 WindowCap 字节」并只压缩块 i 原文,故其输出只依赖这两段数据,不依赖其他块、不依赖 goroutine 完成顺序或他人的压缩结果。
衔接:各块产出不含流头/流尾的**裸 token**,按块序拼接。块间**不需要**刷新标记——块边界只是匹配的可选断点,解码器看到的仍是一条连续 token 流;流头由主 goroutine 写一次,流尾写一次(总长=len(data),CRC 对完整原文计算)。workers 数只改变调度,结果逐字节相同。
