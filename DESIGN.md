# 设计与推导

## 1. 包依赖与职责
eol（行尾识别）、ws（行尾空白延迟判定）、span（偏移映射）互不依赖；
norm 依赖三者；par 依赖 norm、span。依赖单向。单 norm 实例非并发安全。

## 2. 核心状态机（norm）
字节逐个到达。`\r` 暂存为 pendingCR：下一字符为 `\n` 则合成一个 `\r\n` 行尾，
否则 pendingCR 自身是一个行尾（`\r\r\n`：第二个 `\r` 后见 `\n`，故为 `\r`+`\r\n`）。
空格/制表符进入 ws 缓冲，紧跟其后的若为内容字节则整串判定为“非行尾”原样吐出，
若为行尾则整串判定为“行尾空白”删除。行尾统一输出一个 `\n`。
因此输出只依赖完整字节流，与 Write 切分无关（语义 4）。行尾空白永远被修剪，
故输出不含行尾空白、不含 `\r`，再规范化是恒等（语义 5，幂等）。

## 3. 四处推导

### 3.1 末尾换行策略、空输入（语义 3、5）
先得到“去掉末尾换行影响”的规范化结果 body。
- Keep：保留全部末尾换行（空 → `""`，`"a"` → `"a"`）。
- ExactOne：保证末尾恰好一个换行；空输入选择输出 `""` 而非 `"\n"`。
  选 `"\n"` 虽也幂等，但会凭空创造一行（“a”文件与“空文件”都收敛到换行，丢失空性）；
  选 `""` 把“空”当特殊态：N(空)=空，N(非空无换行)=加一换行，再作用显然不动。
- TrimBlank：删除全部末尾空行（空 → `""`，`"a"` → `"a"` 不造换行）。

| 输入 \ 策略 | Keep | ExactOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

（额外区分：`"a"` → Keep/TrimBlank `"a"`，ExactOne `"a\n"`。）
三策略再规范化：body 已无行尾空白/`\r`，末尾为零或多个 `\n`，
各策略对该形态不动，故幂等。

### 3.2 被删字节的 ToOut（语义 6）
映射以左闭右开区间记录：identity 段 orig==out 同长；delete 段 orig 长度 d、out 长度 0；
insert 段 orig 长度 0（仅 ExactOne 补的一个 `\n`）。
设输出位置 p 为被删串结束后下一个保留字节的输出起点。
- ToOrig(o)：identity 段内为 orig+(o-out0)；delete 段内任意 o 都映射到 orig0+d
  （间隙粘到右侧）；末尾/insert：orig1。单调不减。
- ToOut(i)：identity 段内为 out0+(i-orig0)；delete 段内任意 i（含其中与右端点）
  都取 p。于是 ToOut(ToOrig(o))==o 对所有输出 o 成立：删除间隙的 ToOrig 是间隙右边界，
  ToOut 该点仍为 p，而 p 恰是间隙后第一个输出位置。ToOut 单调不减。
编辑器语义：被删空白（含 `\r\n` 里的 `\r`）里的光标映射到换行符**之前**（p）；
`\r` 与尾随空白一起删除时 p 是下一行开头，放在换行之前符合“行内删除”直觉。
insert 段使输出严格变长时无原文字点可回映，ToOrig(该点)=orig 终点（粘右），
该点不满足互逆；互逆在输出不增长（Keep/Trim 主体）时由测试钉住。

### 3.3 空白缓冲超限（语义 8）
选择**拒绝**。强制把长空白判定为“非行尾”吐出会破坏跨切分一致：一旦后续到达行尾，
这些空白本应删除，却已不可撤回，违反 4/5/6；若改判则违反“原样吐中间空白”。
拒绝返回带原文偏移的哨兵错误 ErrTrailingLimit，保留已输出内容，实例进入终态，
状态可解释（已输出前缀确定、其后未消费）。输出上限同理拒绝 ErrOutputLimit。

### 3.4 par 拼接（语义 9）
段首无法知道前段是否有 pendingCR 或未判定空白。做法：第 j 段把前一段末尾
至多 maxPending（1 个 `\r` + 上限内空白）字节作为 carry 前置后再流式处理，
不 Close；记录“干净点” cp（自段首起最后一个行尾之后的位置，此前无任何挂起）。
第 j+1 段消费的 overlap = [cp_j, end_j) 被第 j 段重做。每段只提交 origEnd≤全局cp 的
输出；origEnd 之外为下一段重做区。span 段坐标整体平移 (origStart,outStart)。
策略收尾只对最后一段 Close 后做一次（策略为全局性质）。
修正范围上界 = 1 + 空白缓冲上限 + 合成段重复处理同量，与文件总长度无关。

## 4. span 复杂度
仅在“删除点/插入点”追加非 identity 段；相邻 identity 自动合并。
故无删除的 10MB 纯 `\n` 只有 1 段。查询二分定位段；维护非导出计数器 lastChecks
记录最近一次查询检查的区间数，断言其 ≤ 2·log2(段数)+4。

## 5. 错误（可判定、带原文偏移）
ErrNUL（严格模式遇 NUL，偏移）、ErrTrailingLimit、ErrOutputLimit、ErrClosed。
均为终态；已输出内容保留。NUL 非法 UTF-8 默认原样通过，严格模式只管 NUL。
