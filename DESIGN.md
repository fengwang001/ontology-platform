# 严格模式流式 Base64：规范尾部的推导

## 推导（语义 2）

一个完整组是 4 个字符，每字符携带 6 bit，共 24 bit = 3 字节。
末组不足 3 字节时，右侧先补 0 bit 凑满 24 bit，再用 `=` 标记被填充的
**字符位**：余数 1 字节 → 2 字符 + `==`；余数 2 字节 → 3 字符 + `=`。
被填充字符位之外、但属于最后一个数据字符的低位比特（"凑位比特"）
不携带任何输入字节，因此必须为 0：

- `XX==`：第 2 个字符的 bit 3..0（低 4 位）必须全 0。
  `Q`=16=`010000`，低 4 位为 0 → `QQ==` 合法（解码 0x41='A'）。
  `R`=17=`010001`，低 4 位为 1 → `QR==` 非规范，拒绝；宽松解码器会
  丢弃这 4 bit 同样得到 'A'，故拒绝它正是严格性所在。
- `XXX=`：第 3 个字符的 bit 0（最低位）必须为 0。
  `I`=8 末位 0 → `QUI=` 合法；`J`=9 末位 1 → `QUJ=` 拒绝。

统一规则：**解码后再用标准编码重编码，结果必须与输入逐字符相同**
（re-encode 恒等）。这等价于"凑位比特全 0 + 填充形状只能是 0/1/2 个
`=`"。于是合法串集合与任意字节串一一对应：`Encode` 天然满足该规则，
满足规则的串又唯一解码出字节，保证 `Encode(Decode(y)) == y`。
`QQ`/`QQ=`/`QQ===` 因形状非法拒绝；`QQ==QQ==` 因填充只允许出现在
整个流末尾而拒绝。

## 语义与测试对照表

（实现完成后补齐）
