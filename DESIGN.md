# Design：流式行尾/空白规范化与双向偏移映射

## 1. 行尾与空白的判定
- `eol` 是逐字节状态机：收到 `\r` 进入 Pending；下一字节是 `\n` → CRLF，否则 Pending 提交为 CR，再处理新字节。
- `ws` 延迟判定：`' '`/`\t` 仅在其后跟行尾或 EOF 时才是行尾空白；遇到其他字节立即作为正文吐出。
- 因此 `\r\r\n`：第一个 `\r` 见第二个 `\r` 提交成 `\n`，第二个 `\r` 见 `\n` 成 CRLF，输出 `\n\n`。
- 策略：Preserve=保留原末尾结构；Ensure=非空输出恰有一个结尾 `\n`，空输入仍为 `""`（保证幂等：若空输入产出 `\n`，再规范化得到 `\n` 也幂等，但会凭空造字节、破坏空文件语义；选 `""` 同样幂等且无凭空内容）；Trim=删掉全部空尾行，非空保留一个 `\n`。
- NUL/非法 UTF-8 原样通过；严格模式遇 NUL 返回带原文偏移的具名错误，已输出保留。

## 2. 空输入与只含换行输入（样例表）
| 输入 | Preserve | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

- 幂等：输出只含 `\n` 行尾、无行尾空白；Preserve 的输出末尾若有若干 `\n`，再跑仍保留同样个数（`""→""`，`"\n\n"→"\n\n"`）；Ensure/Trim 的输出只可能是 `""`、`"\n"` 或以非空白+单个 `\n` 结尾，再跑不变。

## 3. 被删字节的 ToOut
- 不变量要求对所有输出偏移 o：`ToOut(ToOrig(o))==o`，故 `ToOrig` 必须取到每个被保留字节，被删区间只能“共享”一个相邻输出偏移。
- 取**删除区间起始处的输出位置**（前一保留字节之后的间隙）：`\r\n` 的 `\r`、行尾空白的每个字节都映射到删除点（即行尾 `\n` 之前）。
- 编辑器把光标放在被删空白中，光标落在换行符**之前**（行的逻辑末尾），符合直觉；映射到之后会跳到下一行。
- `ToOrig` 用输出偏移选择源区间，单调不减；末尾终点 `len` 互相映射。

## 4. 缓冲超限与输出上限
- 未判定空白必须缓冲。选择**拒绝**：待判空白是连续 `' '`/`\t`，长度超 `MaxPendingWS` 立即返回具名错误（带当前原文偏移），已输出保留、进入终态。强制输出虽不致命，但需虚构“后面不是行尾”的判定，若后续真出现行尾会产生已吐字节无法回收，破坏第 4/5 条，故不选。
- 输出超过 `MaxOutput` 同样立即拒绝。终态后再 Write 返回 ErrClosed。

## 5. par 拼接
- 切点可能切开 CRLF 或跨段行尾空白。做法：对每个目标切点 p，取“p 处或其后第一个非 `' '`/`\t`/`\r`/`\n` 字节位置”为**安全切点**（其后不可能有待判 `\r`/空白回看；没有则取 len）。
- 第 j 段规范化范围 `[s_j, s_{j+1}+1)`（末段除外），再把输出与映射裁到原边界 `s_{j+1}`。重叠的那个非空白字节由两段各算一次但只保留一次（前段保留、后段裁掉）。
- 因安全切点保证 CRLF 不被切开、行尾空白整体落在其前行所在段内，无需回看、修正范围恒为 1 字节，有上界。
- 映射平移：span 锚点是“源/目标累计长度”的分段线性点表，拼接时把各段锚点加上全局原点偏移即可；目标偏移同步平移。
- 策略：Preserve=保留原末尾结构；Ensure/Trim=对非空输出保证恰好一个结尾 `\n`。关键推导：第 6 条要求每个输出偏移都有源（`ToOrig` 满射）且往返还原，若凭空插入 1 字节，输出侧有 O+1 个光标而源只有 I+1 个（O=I），鸽巢原理迫使两个输出偏移共指同一源偏移，其往返不可能都还原。故规范化器**只删不增**：Ensure 与 Trim 的唯一可实现语义都是“裁掉末尾多余空尾行后保留一个 `\n`”（二者等价）；空输入只能得 `""`。选 `""` 也避免凭空造字节。
