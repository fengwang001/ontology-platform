# 设计说明

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。据此推导必须拒绝的情形。

## 拒绝清单（每条给出反例 t 与理由）

1. 显式次数 1：t=`1a`，Decode=`a`，但 Encode(`a`)=`a`≠`1a`（次数 1 必须省略）。
2. 次数 0：t=`0a`，Decode 为空，Encode(``)=``≠`0a`，且游程不允许空。
3. 前导零：t=`01a`，Decode=`a`，Encode(`a`)=`a`≠`01a`（禁止前导零）。
4. 相邻同符号游程：t=`2a3a`，Decode=`aaaaa`，Encode=`5a`≠`2a3a`（必须切成最长游程）。
5. `\` 后非数字非反斜杠：t=`\a`，编码字母不转义，重编码不会产生 `\a`。
6. 末尾孤立 `\`：t=`a\`，无完整符号，不是任何 Encode 输出。
7. 只有次数无符号：t=`12`，末尾缺符号，编码每个游程必带符号。
8. 非法 UTF-8：t 含非法字节，它不可能是合法 string 的 Encode 结果。

每类错误为不同哨兵错误，包装在带字节偏移的 DecodeError 中，可断言、可区分。

## 大次数取舍

拒绝超过 `math.MaxInt64`（19 位十进制）的次数，返回 ErrCountTooLarge。
理由：Encode 无法从 Go string 产生 >int64 的游程（长度受 int 限制），
接受它会得到无法再编码回自身的 t，违反不变量；上限即 Encode 输出的天然上界。
解码不按次数预分配：输出仅在游标推进时按块写出；输出总字节设可配置上限，
超限返回 ErrOutputLimit，默认按 int 安全上界估算。次数累加逐位检测溢出，绝不回绕。

## 测试对照表

（测试写完后补填）
