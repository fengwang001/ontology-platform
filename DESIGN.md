# 设计推导

公共规范化核 K：`\r\n`、`\r`、`\n` → `\n`；删除每个 `\n`/EOF 前连续的空格与制表符；
其余字节（含 NUL 与非法 UTF-8）原样通过。记 K 后内容为 c、末尾换行个数为 t。

## 1. 空输入 / 只含换行：三种末尾策略

定义：`Keep` 不动尾部；`EnsureOne` 若 `c!=""` 则输出恰好一个尾部 `\n`，否则输出 `""`；
`TrimBlank` 删除全部尾部空行，若删后非空则保留一个 `\n`，否则输出 `""`。
推导：规范化核只删不增（删空白、删 `\r`），故输出长度 ≤ 输入长度，这是第 6 条
`ToOut(ToOrig(o))==o` 可满射的必要前提；若对空输入凭空追加一个 `\n`，输出比输入长，
该插入字节在原文没有任何位置，恒等映射必然失败。因此「确保恰好一个」选择
**空输入仍输出 `""`**：对 `""`、`"\n"`、`"\n\n"` 结果均为 `""`，再规范化仍为 `""`，
幂等成立；另一选择（空输入追加 `"\n"`）虽使 K 部分幂等，但插入字节违反第 6 条，故不取。
在「只删不增」约束下 EnsureOne 与 TrimBlank 对尾部的收敛规则相同（t→1 或整体为空），
二者保留为两个具名策略，但本实现输出一致；差异仅语义意图。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `""` | `""` |
| `"\n\n"` | `"\n\n"` | `""` | `""` |
| `"  \r\n"` | `"\n"` | `""` | `""` |

幂等：Keep 的输出无 `\r`、无行尾空白，K 为不动点；EnsureOne/TrimBlank 输出为
`""` 或恰一个尾换行的非空内容，再跑结果不变。

## 2. 被删字节的 ToOut

把流看作字节间的「间隙」：间隙 k 位于原文 k 与 k+1 之间，偏移 k 即间隙 k。
保留段 a→b 中，间隙 k 映射到间隙 `b-(a-k)`；删除段 a→b 所有间隙坍缩到间隙 b
（即后续保留内容的起点）。因此被删空白或 `\r\n` 中的 `\r` 上 `ToOut(i)` 取
**删除段之后的输出偏移（ceil/右边界）**，单调不减；ToOrig 取输出区间右边界，
使 `ToOut(ToOrig(o))==o`（插入不存在，恒等可逐段验证）。编辑器把光标放进被删
空白时，右边界即换行符本身之前的间隙，光标停在**换行符之前**（行的逻辑末端），
不会跨过换行。

## 3. 空白缓冲超限（第 8 条）

选择**拒绝**：连续未判定空白超过上限立即返回可判定错误 `ErrWSLimit`（带原文字节偏移），
已输出内容保留，实例进入终态，后续写入返回 `ErrClosed`，`Output`/`Map` 仍可读取
（与严格模式 NUL 同一终态模型）。论证：强制判定为「非行尾」会在后续真出现行尾时
产生本应删除却已输出的空白，破坏幂等（第 5 条）与映射正确性（第 6 条），且无法
跨切分点一致（第 4 条，判定依赖未来字节）；拒绝不产出任何错误内容，三条不变量
在错误路径上均不被触碰。输出总上限同理：写入前预估超限即 `ErrOutLimit`。

## 4. par 拼接

规范化是无状态函数，难点仅在段尾。切点 c 可能切断 `\r\n` 或行尾空白串，因此
段 j（0..K-1，仅末段除外）**不做 EOF 终判**：它把输入处理到切点，保留待定 `\r`
与缓冲空白在段内状态中（`Process(prefix, final=false)`），其输出片为
`out[0 : g.ToOut(cut))`，即截止切点在全局输出中位置的前缀；段 j+1 从同一切点起
重新处理后缀并做终判。每个切点只重做「待定 `\r` ＋ 一串空白」这一有界窗口
（被缓冲上限界定），修正范围有上界，不做全局重扫。映射先在各段坐标内构造
（orig/out 均从 0 起），平移 `(+origStart,+outStart)` 后按序 append：
段内区间互不重叠且边界连续，平移后与全局逐段区间完全一致；输出片直接拼接
即得全局输出。全部片段独立，可由 K 个 goroutine 并发，`sync.WaitGroup` 汇总，
无共享可变状态。
