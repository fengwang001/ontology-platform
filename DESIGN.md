# DESIGN（CSV RFC4180 方言解析器）

## 1. 空行与单列空值
空行（0 字节后即行尾）被**跳过**，不成为记录。推导：RFC4180 中文件末尾的换行不产生额外记录；若把空行记为「单列空记录」，则末尾换行无法与「最后一条记录为空」区分，逐字节往返必失败。故规则：行内有逗号或引号才算记录；纯空行跳过。推论：解析器**永远不会产出「单列、未引号、空」的 cell**——该输入只能是空行并被跳过。因此回写器遇到单列空记录时**必须写 `""`**（带引号空字段），写未引号空串会被下次解析当空行跳过而丢记录；这就是「必要引号」的第五种情形。规范输入定义：无首尾空行、无连续空行、单列空值一律写 `""`、其余仅在必要时加引号的 CSV 文本。

## 2. par 切点
每段 worker 在不知全局引号状态时，按两种入口假设各跑一遍同一状态机：`outside`（引号外，记录起始）与 `inside`（引号字段内，携带跨段前缀值与起始偏移）。第 0 段真实状态确定为 outside；此后由左段**真实出口状态**唯一选定右段要采纳的结果（出口在引号内→取 right 的 inside 解，否则取 outside 解），逐段递推。两条路径各只扫描本段一次，故总处理量 ≤ 2N。两假设在段内均为合法确定的机器推进，采纳的那条与单线程状态机在相同入口状态、相同字节序列下逐状态一致（归纳法），故拼接逐位相同。cell 跨切点时：inside 解以段内起点为开放引号字段、字段号/字段起始偏移沿用左段；拼接时该 cell 先不 emit，由协调器把相邻段开放字段值串接，到真正闭合时用全局起始偏移 emit 一条 cell。错误只可能出自被采纳解；其段内字节偏移加段起点即全局偏移，记录号/字段号加「本段之前已闭合的记录数 / 左段开放字段号」。切点在 `""` 与 `\r\n` 中间时同理：引号态/挂起 CR 作为入口假设的一部分。

## 3. CR 待定
未引号字段遇 `\r` 不能立即判定（可能是 `\r\n` 行尾或孤立 CR 错误），进入 `CR?` 挂起态，该字节暂存、不计数错误。续传：挂起可跨 `Feed`，下次字节是 `\n` 则行尾，是其他字节则先在该孤立 `\r` 偏移报 `ErrBareCR`，后续字节仍按正常状态处理一次（不回扫）；`Close` 时仍挂起则在 `\r` 偏移报 `ErrBareCR`。par：切点恰在 CR 后时，outside 解以「出口挂起 CR」记录该事实，选定右段时把挂起字节与偏移作为入口状态传入，规则与续传完全相同。引号内 CR 直接是内容，无挂起。

## 4. 上限「立刻」
字段字节计数：未引号字段每接收一个内容字节 +1；引号字段内每字节 +1，`""` 在**第二个引号到达并确认为转义时**计 1（每个输出内容字符计 1，不含外层引号）。字节一进入即累计，第 `max+1` 个内容字节到达的当拍返回 `ErrFieldTooLarge`，从不先攒整条记录。字段数在逗号当拍、记录数在行尾提交当拍立刻检查。已产出记录保留，解析器进终态，错误缓存，此后 `Feed/Close` 返回同一错误。

## 5. 状态转移表
字节类别：`,`=C，`\n`=Q逗号，`\r`=R，`"`=Q引号，其他=X。动作：emit=发字段，row=提交记录。

| 状态 | C 逗号 | N 换行 | R | Q 引号 | X |
|---|---|---|---|---|---|
| R 记录起始 | F,emit 未引号空 | R,空行跳过 | CR?(空行候选) | Q | U,X 入值 |
| F 字段起始 | F,emit 未引号空 | R,emit 空,row | CR?(字段待 emit) | Q | U,X 入值 |
| U 未引号中 | F,emit | R,emit,row | CR? | err BadQuote | U,入值 |
| Q 引号中 | Q,入值 | Q,入值 | Q,入值 | QQ | Q,入值 |
| QQ 刚见引号 | F,emit | R,emit,row | CR?(字段已 emit) | Q,入值一个引号 | err QuoteAfterClose |

补充：挂起 CR 后下一字节——N 则成行；其他字节先在 CR 处报 BareCR 再按该字节在 F 态处理；EOF 挂起报 BareCR。引号态 EOF 未闭合报 ErrUnclosedQuote。cell.Off 为字段**原始字节**区间（含外层引号），便于定位与并行拼接。
