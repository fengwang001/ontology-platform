# 设计：多租户配额的加权公平队列调度器

## 1. 虚拟时间为什么用 `c/权重` 而不是 `c*权重`

目标：长期看，租户 i 获得的执行代价份额应等于其权重占比 `w_i / Σw`。

设调度器始终选择虚拟时间最小的租户执行。定义租户 i 的虚拟时间
`vt_i = Σ 已执行代价 / w_i`（即每执行一个代价为 c 的任务，`vt_i += c / w_i`）。
稳态下所有活跃租户的 vt 必然近似相等（记为 V），否则 vt 偏小者会被优先选中
直到追上。于是租户 i 累计获得的执行代价 `C_i ≈ V * w_i`，即

    C_i / ΣC_j ≈ V*w_i / (V*Σw_j) = w_i / Σw_j

份额恰好等于权重占比，得证。

若改用 `c*权重`：稳态时 `C_i ≈ V / w_i`，权重大的租户反而拿到更少份额，
与配额语义完全相反。直观上，`c/w` 让权重大的租户 vt 推进得慢，于是被选中
得更频繁——这正是"权重即份额"的含义。

## 2. 空闲租户重新加入时 vt 必须抬升

系统虚拟时间 `sysVT` 定义为历次被选中租户执行前的 vt 的单调上界
（`sysVT = max(sysVT, 被选中者的 vt)`），它近似等于所有活跃租户的公共 vt。

反例（不抬升）：租户 X（权重 wX）在 vt = v0 时空闲，期间租户 Y（权重 wY）
独占执行了 N 个代价为 c 的任务，Y 的 vt 推进到 `v0 + N*c/wY`。此后 X 重新
提交任务，其 vt 仍停在 v0，于是 X 连续独占调度器，直到 vt 追上 Y：
需要步数 k 满足 `k*c/wX ≥ N*c/wY`，即 **k ≈ N·wX/wY**。等权重、N=1000 时
X 会连续独占约 1000 步，Y 被饿死近 1000 步——长期份额虽最终收敛，但短期
公平性被彻底破坏。

正确做法：租户从空转为非空（或新注册后首次入队）时，

    vt ← max(vt, sysVT)

即空闲期间视为"按系统节奏同步推进"，重新加入时从当前系统虚拟时间起跑。
抬升后 X 与 Y 处于同一 vt 起跑线，按权重交替，X 的任务在常数步内被执行，
Y 也不会被饿死。空闲租户不被奖励也不被惩罚。

## 3. 代价为 0 的任务

若允许 c=0 直接推进，则 `vt += 0`，该租户 vt 不变，将被无限连续选中
（零代价任务无限独占）。处理：引入最小计费代价下界 `MinCost = 1`，
推进时取 `c_eff = max(c, MinCost)`。零代价任务合法入队与执行，但每执行一个
至少推进 1 个单位虚拟时间，因此任何租户都无法靠零代价任务独占调度器。

## 4. 同虚拟时间的并列打破

多个租户 vt 完全相等时（新租户批量加入、等权重等代价场景必然出现），
按租户 ID 的字典序取最小者。堆的比较键为二元组 `(vt, id)`。
该规则与提交顺序、堆内部形状无关，保证同一任务流的调度序列完全可复现。

## 5. 数值表示与溢出

虚拟时间与代价一律用 float64。权重极大（如 1e6）与极小（如 1e-6）并存时，
`c/w` 最大约 `c*1e6`，float64 上限约 1.8e308，需连续执行约 1e302 个任务
才溢出，实际不可达；相对精度 2^-53 对 5% 的公平性容差绰绰有余。
未采用有理数：标准库 `math/big` 的 big.Rat 每次推进都要分配与约分，
10 万次调度的开销与复杂度均无必要。

## 6. 复杂度

选择 = 堆顶取出 + vt 推进后下滤（或删除空租户），比较次数上界约
`2*ceil(log2 n) + ceil(log2 n) < 4*ceil(log2 n)`（n 为堆中非空租户数）。
实现为手写二叉堆，比较回调内嵌非导出计数器，每次 Pick 清零，
测试直接断言单次选择的比较次数上界。空闲（空队列）租户不入堆，
故选择代价只与非空租户数相关，与总租户数无关。

## 7. 背压、错误与移除

- 每租户队列上限由 admit 层在入队前检查，超限返回 `ErrQueueFull`；
  检查按租户独立，不影响其他租户入队。
- 权重 ≤ 0 注册时返回 `ErrInvalidWeight`。
- 向未注册租户提交返回 `ErrUnknownTenant`。
- 移除仍有排队任务的租户返回 `ErrTenantBusy`（拒绝移除，任务保留），
  仅当队列为空时允许移除。四个错误均为哨兵值，可用 `errors.Is` 区分。

## 8. 并发与确定性

调度器全部状态（租户表、堆、sysVT）由一把互斥锁保护，提交与取出在同一
锁内串行化；每个任务在锁内从队列弹出，保证恰好被取出一次（不丢不重）。
确定性：同一确定性任务流（固定提交顺序与代价）产生逐元素相同的调度序列。
并发提交时，"提交顺序"定义为获取入队锁的串行化顺序——谁先拿到锁谁先入队，
该顺序一旦确定，后续调度序列即唯一确定。
