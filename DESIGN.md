# JSON 字符串严格编解码设计

## 代理对错误偏移

规则：`\u` 单元的偏移指向反斜杠。读到高代理后，只允许紧跟一个低代理单元；
若遇到结束引号、原始字节、非法转义，错误偏移指向高代理起点，因为它是未能闭合的不完整词法单元。
若下一个可识别 `\u` 不是低代理（含低代理、颠倒对、普通值），错误偏移也指向高代理起点：
高代理声明的是“半个码点”，后继单元在组合点不合格，应归责于该声明。
孤立低代理出现时，其自身就是非法单元，偏移指向该低代理起点。
因此 `\uD83D`、`\uD83Dx`、`\uD83DA` 都报偏移 1；`\uD83D\u...` 的不匹配均报偏移 1。
不能把 `\uD83DA` 解释成替换字符加 `A`：严格模式下代理不是普通码点，且 `A` 不是以 `\u` 开头的转义单元；
一旦容忍半个代理，解码器会偷偷改变词法边界与码点，违反 RFC 8259 和非法字节不静默替换的要求。

## 最小转义与唯一性

JSON 字符串中必须转义双引号和反斜杠，否则破坏定界或转义结构；
RFC 8259 要求 U+0000–U+001F 转义，防止控制字符混在文本中。
U+007F 虽是 ASCII 控制语义的 DEL，但不在该强制区间，也不影响 JSON 语法，故原样输出。
`/` 无需转义；非 ASCII（含 U+2028/U+2029）是合法 UTF-8 文本，均原样输出。
短转义只用于固定的退格、换页、换行、回车、制表；其余控制字符唯一写成 `\u00XX` 小写形式。
编码对每个码点采用确定的唯一输出：除上述字符原样复制，且 UTF-8 解码/编码保持恒等，没有二义分支。
解码只接受合法 UTF-8 和合法转义；最小转义集合又完全对应编码规则，所以编码是单射，往返输出唯一。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 严格解码五类错误及偏移 | 待补充 |
| 代理对组合与拒绝 | 待补充 |
| 非法 UTF-8 两个方向 | 待补充 |
| 最小转义编码 | 待补充 |
| 两类往返性质 | 待补充 |
| 跨切分点一致 | 待补充 |
