# 设计：可撤销的批量重命名与冲突解析器

模块 `ontology`，仅标准库。包划分：`name`（命名空间与合法性）、`plan`（冲突检测与排序）、
`cycle`（破环）、`apply`（执行与日志）、`undo`（逆序撤销）、`cmd/demo`（演示）。

## 1. 名字与合法性

- 名字是任意字符串：空串合法，路径分隔符按普通字符处理；`name.Valid` 恒为 true（保留校验钩子）。
- 命名空间 `name.Set` 是内存中的名字集合（map），内嵌互斥锁；批量执行期间整体持锁。

## 2. 冲突分类（检测必须先于任何修改）

四类冲突均可 `errors.Is` 区分，检测只读命名空间，不做任何修改：

1. `ErrDuplicateSource`：同一旧名出现两次。判定：构建 from→请求 映射时撞到重复键，报出两个新名。
2. `ErrDuplicateTarget`：两个请求指向同一新名。判定：构建 to→请求 映射时撞到重复键，报出两个旧名。
3. `ErrMissingSource`：旧名不在命名空间。判定：`from ∉ ns`，指出该名。
4. `ErrTargetExists`：新名已存在且不在本批被搬走。判定：`to ∈ ns 且 to ∉ fromSet`。

自环先被丢弃（见 §5），不参与上述判定。「请求数远大于名字数」必然产生重复旧名，被重复旧名类冲突拦住。

## 3. 排序规则推导

依赖关系：请求 R 的新名 `R.to` 若是另一请求 Q 的旧名 `Q.from`，则 Q 必须先于 R 执行，
否则 R 会把 Q 尚未搬走的内容覆盖。得依赖图：节点=请求，边 Q→R 当且仅当 `R.to = Q.from`。

- 旧名唯一 ⇒ 每节点入度 ≤ 1；新名唯一 ⇒ 每个名字至多是一个请求的目标。
- 无环部分按 Kahn 拓扑序执行即安全：执行 R 时占用 `R.to` 的旧名已搬走，且无其他请求以
  `R.to` 为目标，故目标名必不存在 ⇒ 中途任何时刻都不覆盖。
- 同时可执行的节点按旧名字典序弹出（最小堆），保证确定顺序。

例：`{a→b, b→c}`：`a→b` 依赖 `b→c`，执行顺序为 `b→c; a→b`。

## 4. 破环的通用做法与临时名数量

Kahn 结束后剩余节点入度均为 1；又因旧名、新名各自唯一，剩余图是若干互不相交的简单环
（若某非环节点指向环，则环上该入点有两个请求以其旧名为目标，与「新名唯一」矛盾，检测阶段已排除）。
环上每个新名都被环内旧名占用，任何顺序都会覆盖，必须借环外名字。

做法（每环一个临时名 t）：设环为 r0..r(k-1)，其中 r(i+1).from = ri.to：

    r0.from → t ;  逆序执行 r(k-1) .. r1 ;  t → r0.to

逐步验证无覆盖：t 是全新名；执行 ri 时其目标刚被 r(i+1) 腾空；最后 t 腾空自己。临时名数量
恰等于环数，且最优：k 个名字的环中 k 个目标全被占用，至少需 1 个环外名字才能启动。

临时名生成：`tmp~0, tmp~1, ...` 递增尝试，逐一校验 `∉ ns`、`∉ 本批全部旧名`、`∉ 本批全部新名`、
未被已用临时名占用，冲突则重试；超过上限报 `ErrNoTempName`（可判定错误，绝不覆盖数据）。

## 5. 自环语义

`a→a` 定义为**无操作**：内容已在目标处，冲突检测与排序前直接丢弃。丢弃后剩余请求照常判定
（如 `{a→a, b→a}` 中 `b→a` 因 `a ∈ ns 且 a ∉ fromSet` 报 `ErrTargetExists`）。

## 6. 执行、日志与撤销

- `apply` 先把整批步骤写入日志，再持锁逐步执行；每步前调 Hook（故障注入点）。第 k 步失败 ⇒
  逆序回滚已执行的 0..k-1 步（`to→from`），删除日志，返回 `ErrStepFailed`。
- 日志格式：头行 `ONT1 <count:%08x> <bodyCRC:%08x>\n`；每条记录一行 `hex(from) hex(to)\n`；
  `bodyCRC = crc32(全部记录字节)`。hex 编码使任意字符串（含空串、分隔符、换行）可安全落盘。
- 截断分类（截断长度 L）：L 落在头行内 ⇒ `ErrHeaderIncomplete`；头完整但末尾记录行无换行
  ⇒ `ErrRecordIncomplete`；截断点恰在记录边界 ⇒ 剩余行完整但 bodyCRC 不匹配 ⇒ `ErrCRCMismatch`。
  三类均可 `errors.Is` 区分；可恢复前缀 = 已完整解析的记录，撤销按该前缀逆序进行，
  缺失的尾部步骤 `[k, N)` 逐个报告为无法撤销。
- `undo` 成功后将日志改名为 `.done`；再次撤销看到 `.done` 直接返回（幂等无操作）。

## 7. 并发与确定性

- 执行期间持有 `name.Set` 的锁：并发修改被**阻塞**至批次结束（定义为阻塞而非拒绝）。
- 确定性：拓扑同层按旧名字典序；环按环首（最小旧名）字典序依次处理；临时名计数器从 0 递增。
  请求的构造顺序不影响输出步骤序列。

## 8. 复杂度

冲突检测 + 排序对每请求只做常数次 map/集合查找（from/to 建表、成员判定、依赖边、Kahn 各一次，
堆调整不查表），总代价 O(请求数 + 名字数)。`plan` 包内非导出计数器记录全部查找，
测试断言不超过 `4 * (请求数 + 涉及名字数)`。
