# 设计：带背压的流式聚合管线

链路：source → parse → 分组聚合 → sink 落盘；只用标准库，状态在进程内存 + 两个磁盘文件。

## 1. 「已消费位置」的推导

记录按 source 侧单调序号 n（字节偏移）流动。各阶段间是有界队列，队列满时发送方阻塞（背压）。

- 记 source 读取位置为 `R`（已从数据源取出），sink 落盘位置为 `D`（已原子改名）。
- 错误候选一：检查点记 `R`。崩溃时 `R>D` 的记录可能正躺在某个有界队列里，恢复后从 `R+1` 继续，在途记录永久丢失。
- 错误候选二：检查点记 `D`。sink 落盘是「全量快照 + 校验和」，若恢复从 `D` 重放而聚合器内存已含同批记录，会产生重复聚合；且 `D` 无法表达「解析坏记录」这种不产生输出的前进。

正确定义：**已消费位置 = 屏障序号 B，且所有阶段的在途集合（已入队未处理完）均为空时才允许把 B 写入检查点。**

实现方式是周期性「屏障」：屏障是与记录同序流动、不允许被越过的特殊消息。
1. source 每 K 条注入一个携带序号 B 的屏障；
2. 屏障经 parse 原样下传（坏记录只计数，不影响序），聚合器收到屏障时把「当前聚合结果 + B」打包为一个聚合快照事件；
3. sink 依次处理事件：收到快照事件时先把全量输出写临时文件、校验和、原子改名，再把检查点写临时文件、原子改名（保留最近两个）。

由于屏障与记录同序且队列 FIFO，sink 开始处理屏障 B 时，`n≤B` 的记录必然已全部离开各级队列（在途集合为空）。此刻崩溃：
- 输出文件与检查点要么都是旧状态、要么都是新状态（各自原子改名），不存在跨态；
- 重启从检查点位置 B+1 重放，`n≤B` 不在重放区间 → 不丢不重；
- 崩溃发生在两次屏障之间，则在途记录重放，输出仍是旧快照 + 重放，结果等价。

临时文件带 `.tmp` 后缀与内容校验和，启动时一律删除（绝不读回）；检查点保留 `ckpt` 与 `ckpt.prev`，当前校验和损坏则回退上一个并标记 `fellBack`。

## 2. 背压与资源约束

- 阶段骨架 `stage.New(cap)`：有界 channel，满则发送阻塞；`Send` 中记录阻塞次数；worker 每出队一条更新历史最大在途数（入队数减出队数的峰值）。
- 三级队列容量之和即理论在途上限，5 万条全速 source 对 1ms sink：断言峰值不超过容量和，且阻塞次数大于 0。
- 分组硬上限 `MaxGroups`：聚合器遇到新键且超限，返回 `ErrTooManyGroups`，管线停止入流、把在途排干落盘后如实返回错误。

## 3. 优雅停止

`Stop` 幂等（`sync.Once`）：关闭入口队列，worker 排空后退出，逐段等待，无在途丢失，goroutine 全部回收。source 报错同理走排空路径并如实返回该错误。

## 4. 崩溃恢复测试模型

以进程内「故障点」模拟硬崩溃（丢弃内存状态、重新构造管线、重新 Run）：source 读完队列非空（屏障前）、聚合中途（屏障入聚合器）、sink 写完 `.tmp` 未改名。对照一次无故障运行，断言恢复后输出文件逐字节相同。

## 5. 边界

零记录、单记录、全同组、容量 1、sink 立即错、source 立即结束、启动前 Stop、空串键合法、缺键算坏记录。
