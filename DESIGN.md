# 规范化器设计推导

规范化：每个行尾（`\r\n` 或单独 `\r`/`\n`）输出一个 `\n`；删除行尾连续空格/制表符（行中间保留）；纯空白行变空行。

## 1. 末尾换行策略

记 `Keep`=保留原样，`EnsureOne`=恰好一个，`Trim`=删全部末尾空行后留一个。规则（在规范化后的字节流尾部执行）：

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

EnsureOne 对空输入可选 `""` 或 `"\n"`：选 `"\n"` 时 `N("\n")="\n"` 仍幂等，但输出长度大于输入，与第 6 条冲突（见 2 末），故选 **空输入→`""`**。Keep 对无尾换行的末行不补换行；EnsureOne：非空且尾部无 `\n` 则补一个，多个尾部 `\n` 削成一个；Trim：非空时把尾部所有 `\n` 换成一个。输出本身无 `\r`、无行尾空白，三种策略重复执行均不动自身，故幂等。

## 2. 被删字节的 ToOut

`ToOrig(o)` 必须覆盖每个输出偏移；输出字节数 ≤ 输入字节数时，可令 `ToOut(ToOrig(o))==o` 处处成立。被删原字节 i 无法占独立输出槽，否则与相邻保留字节的槽冲突。推导：一串删除字节夹在「左侧输出偏移 a」与「右侧输出偏移 b」之间（`b≥a`），单调不减且不能占用 a 或 b（二者已被左右保留字节占用），故**整串删除字节取 `ToOut(i)=b`**（删除串右侧输出位置；删在 EOF 时 `b=len(out)`）。

光标在被删空白中：映射到删除串之后、即换行符 `\n` 所在位置（`b`），编辑器表现为跳到换行位置，方向自然。`\r\n` 的 `\r` 同理 `ToOut` 指向其后 `\n`。

**例外**：EnsureOne 对非空且无尾换行的输入会"插入"一个 `\n`（输出比输入多 1 字节）。由抽屉原理 `ToOut(ToOrig(o))==o` 不可能对所有 o 成立（定义域只有 len(x)+1 个点，值域要求 len(x)+2 个不同值）。采用约定：插入的 `\n` 在 `ToOrig` 上映射到原文终点 len(x)，且 `ToOut(len(x))=len(x)+1`（插入点之后）；此时仅插入点 `o=len(x)` 一处 `ToOut(ToOrig(o))=len(x)+1≠o`，两个映射仍单调不减。除插入策略外（恒有 len(out)≤len(x)）互逆处处成立。

## 3. 空白缓冲超限

选择**拒绝**（哨兵错误 `ErrPendingOverflow`，错误携带原文偏移）。若强制把待定空白判定为"非行尾"输出：当后续字节正是行尾时，本应删除的空白被永久输出，违反语义 2；且再规范化一次会删掉它们，违反幂等（语义 5）；强制输出时建立的映射也无法事后回收。拒绝时缓冲与输出均不推进该批字节、状态保留为"已缓冲空白+原文偏移"，可解释；配合更大缓冲重跑即可，且任意切分方式下失败点一致（失败只取决于累计空白长度），不违反 4/5/6。默认上限较大，`MaxPendingWS=0` 表示不限。

## 4. par 拼接修正

切点可能落在待定 `\r`、待定空白或"空白后跟 `\r`"中间。处理：第 k 段不从切点直接开始，而向前回退到最近的"安全点"（一个完整行尾之后；`\r|\n` 切点回退越过 `\r`；边界情形回退到 0），同时下一拥有者的起点向后延伸到最近安全点（空白后紧跟行尾则越过该行尾，否则走到首个非空白内容处）。相邻段共享的修正带由**拥有者区间**（按原始切点 `b[k]` 划分）去重：输出条目按其原文坐标落在 `[b[k-1],b[k])` 归属，`span.Slice` 过滤后把原文/输出坐标平移段基址拼接。修正范围上界 = 一个待判定空白串长度（受 `MaxPendingWS` 约束，不限时最坏 O(n)）+ 2 个行尾字节，与 K 无关。各 worker 以 raw 模式（不应用末尾策略）规范化，拼接后对完整结果统一应用一次末尾策略，故切点在 EOF 附近、K=1 与任意切点结果逐字节相同。
