# 设计推导

## 1. 非法单元切分（WHATWG 风格，逐字节机）
首字节 L：合法 lead 则等待其声明的 n-1 个续字节；续字节位置收到非 `80..BF`，或首字节本身非法，立即判一个非法单元：吞掉此前已收的待决字节 + 当前字节，再把当前字节当作新序列起点重新解析（重解析不增加检查计数）。未收满即 EOF：全部待决字节合为**一个**非法单元。
故 `F0 90 80 41`：F0 待决收 90、80，第 3 续字节位收到 41 → 一个单元吞 4 字节，41 重解析为 A。`E0 80 80`：E0 要求第 2 字节 ∈A0..BF，80 非法 → 单元(E0,80)，80 重解析为独立单元，末尾 80 再一个 = 三个。`E2 82`+EOF：待决未收满，整体一个。

第二字节合法区间与非法吞噬表（c1=首字节，c2=第二字节）：
| 首字节 | c2 合法区间 | 非法时吞字节 |
|---|---|---|
| C2..DF | 80..BF | 2（c1 与坏 c2 同死） |
| E0 | A0..BF | 2；其余续字节位非法吞到坏字节为止 |
| E1..EC | 80..BF | 2（第3字节位非法则吞3） |
| ED | 80..9F（A0..=代理区） | 同上 |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF（80..8F 非最短） | 2 |
| F1..F3 | 80..BF | 2 |
| F4 | 80..8F（90.. 越界 >10FFFF） | 2 |
| 80..BF | 无（独立非法单元） | 1 |
| C0 C1 F5..FF | 无（非最短/越界） | 1 |
任一合法 lead 之后 EOF：吞掉全部已收待决字节为 1 个单元（≤n-1 个）。

## 2. 缓存上限与提前放弃
UTF-8 最长 4 字节，待决缓存至多 3 字节（硬上限 3，测试断言 ≤3）。c1 非法或任一续字节位收到不在区间内的字节时立即判非法，不等 EOF（此时吞掉的是"待决+坏字节"，坏字节重解析）；只有"剩余字节全合法但流结束"才留到 Close 判截断/一个 FFFD。故检查次数 = 每字节至多首检一次 + 坏字节重解析一次 ≤ 2N。

## 3. par 切点对齐
段 i（i≥1）在切点 s 向前回看：从 min(3,s-1) 起取**最近**的位置 j，使 s[j] 为合法 lead 且 j+n>s（n=其声明长度）。j 存在则 carry=[j,s) 并入本段、body=[s,e)；不存在则 carry 为空，段从 s 开始。回看 ≤3 字节为常数。
论证：全局解析中唯一可能跨越切点的单元，其起点必是切点前某个 n 能跨过切点的合法 lead；取最近者即该单元真实起点（它会"吃掉"中间一切续字节）。无此 lead 则切点前字节不可能与切点后组成同一单元。特例 `C3|<80><80>`：C3 声明 n=2 且 j+2==s+1>s，carry=C3，本段解析 C3 80 成字符、80 独立——与单线程一致；`80 80|...` 无 lead 跨过，80 各自独立。`E2 82|` 段末不足：carry 后 body 为空即等价 EOF，得一个 FFFD。carry 字节只由本段检查一次，前段不检查，全局检查次数 ≤ N + 8×常数。UTF-16 切点强制偶数对齐（回看 ≤1 偶位置），高代理跨切点同理携带。

## 4. 上限断点（语义 9）
输出前若 `len(out)+编码长度 > MaxOut`：在标量边界停下，本次 Write 返回的 n 只含"已完整输出的标量"所消费的字节；尚在待决缓存中的字节从未消费（返回前退回输入计数，调用方下次原样重传）。返回 ErrLimit（终态）。调用方新建实例从 n 续传，拼接逐字节等于无限额一次转完。

## 5. 其他要点
BOM 仅在流首检测（u8 收满 EF BB BF；u16 首 u16=FEFF/FFFE 定序），切开喂入等价；之后 FEFF 为普通字符。UTF-16：高+低代理成标量；孤立高/低代理各一个 FFFD；高代理后非低代理时该非低代理单元重解析。错误四类可判定：ErrBadUnit(带偏移/长度)、ErrTruncated(带偏移)、ErrLimit、ErrClosed。u8↔u16 代理对编解码；统计守恒：合法字节+非法吞字节+BOM 字节=输入字节。
