# DESIGN — 统一格式差分/补丁器推导记录

包依赖单向：`lines ← edit ← hunk ← udiff ← patch`。仅标准库，状态在进程内存。

## 1. hunk 头零计数时的行号（第二节第 4 条）

样例：`-0,0 +1`（在 `x\n` 最前插入）、`-2,0 +3`（在第 2 行后插入）、`-1 +0,0`（删空新文件）。
设某侧计数为 0，则该侧在文件里不占有任何行，只对应一个「缝隙」。观察三个样例：
- 插入点在最前时写的是 `0`，而不是「将要插入的位置 1」；
- 在第 2 行之后插入时写的是 `2`，而不是 `3`（`3` 是插入内容在新文件里的行号，已写在 `+3` 侧）。
结论：计数为 0 时，写的数字是**缝隙前一行的行号**（0 表示文件头之前的缝隙），即 0 基插入下标本身；
计数 > 0 时，写的是区间首行的 1 基行号。这样空区间锚定在既有内容上，文件头/文件尾缝隙都能统一编址。
`Apply` 的解释：`b==0` 时插入点 = 0 基下标 `a`；`b>0` 时起点 = `a-1`。

| 情形 | 头 `-a,b` 写法 | Apply 对旧文件的解释 |
|---|---|---|
| b = 0 | a = 缝隙前一行行号（文件头缝隙为 0） | 0 基下标 a（在第 a 行之后插入） |
| b = 1 | a = 该行 1 基行号，省略 `,1` | 0 基起点 a-1，覆盖 1 行 |
| b ≥ 2 | a = 首行 1 基行号，写 `-a,b` | 0 基起点 a-1，覆盖 b 行 |

## 2. hunk 合并阈值（第二节第 5 条）

设两段改动间未改动行数为 g，上下文为 C。分开渲染时，前 hunk 带 C 行尾上下文、后 hunk 带 C 行头上下文，
共覆盖 2C 行间隔。若 g ≤ 2C，两段上下文重叠或恰好相接，GNU diff 合成一个 hunk；g ≥ 2C+1 时中间至少
有 1 行不被任何上下文覆盖，必须分开。实测 GNU diffutils 3.10：C=1 时 g=2 合并、g=3 分开；C=3 时 g=6 合并、
g=7 分开。**阈值 = 2C**。反证：若阈值取 C，则 C=1、g=2 会分成两个 hunk，与实测矛盾；若取 2C+1，则
C=1、g=3（或 C=3、g=7）会合并，同样与实测矛盾。故 `g ≤ 2C` 合并、`g > 2C` 分开。

## 3. 并列最短脚本的选择（第二节第 3 条）

Myers 前向贪心在 `V[k-1] >= V[k+1]` 时**优先向右走（删除）**，相等也取删除，即「删除优先于插入」。
效果：同一改动块内所有 `-` 行排在 `+` 行之前，与 GNU diff 一致。例 `a b` → `b a`（LCS=1，D=2）：
删除优先得到 `-a`、` b`、`+a`（先删开头的 a，再在 b 后插 a）；插入优先会得到 `+b`、` a`、`-b`。
两者皆最短，本实现固定前者，保证同一输入重复生成逐字节相同。

## 4. 偏移应用后后续 hunk 的查找起点（第二节第 7 条）

要平移。第 i 个 hunk 应用后，记录累计行差 Δ = Σ(插入行数 − 删除行数) 与本次偏移 k = 实际位置 − 期望位置；
下一个 hunk 的查找起点 = 头中记录位置 + Δ + k。原因：头里的行号相对**原始旧文件**，前面 hunk 每净增 Δ 行，
其后所有位置在结果里都平移 Δ；而目标文本若在补丁记录之外另有增删（正是偏移应用的场景），同一位移通常
贯穿后续 hunk，沿用 k 作为首猜可让查找落在 ±F 窗口内，否则后续 hunk 会无谓失败。GNU patch 同样用
上一 hunk 的偏移作为下一 hunk 的首猜。
