# 并行任务图调度设计

## 数据模型

- `graph.Graph` 保存节点集合、去重后的有向边、直接依赖与直接下游；重复加边幂等。
- `fail.RunFunc(ctx,id)` 是用户任务；只允许在 `ctx` 结束前写回的结果生效。
- `sched` 维护就绪最小堆、运行集合、剩余入度、并发槽和结果表。
- `report` 只按 ID 字典序读取终态，输出不受完成顺序影响。

## 执行前校验

1. 空图直接产生空报告。
2. 任一边依赖不存在节点，返回可 `errors.Is` 的依赖错误。
3. 合法图用 Kahn 算法分层；若处理节点数少于总数，用 DFS 沿输入边找环。
4. 自环是长度为 1 的环；返回路径首尾相同，且每段都是输入边。

## 四类终态推导

- `Success`：任务函数已在失败传播前返回 nil；终态不可覆盖、不可回滚。
- `Failed`：任务函数实际返回错误，或 panic 被 recover 后转成错误；保留原始错误/panic 信息。
- `Skipped`：任务从未开始。直接或传递依赖某个失败任务时跳过；快速失败下尚未开始的旁支也跳过。
- `Canceled`：失败发生时已经开始但未结束，随后收到 `ctx.Done()`；“开始过”是与 Skipped 的唯一判定边界。

传播原因只指向该任务可达的、最早确定的失败。多失败同层或同一时刻时，按失败任务 ID 字典序取根因，保证确定性。链式 `A→B→C` 中 A 失败时，C 的原因记录 A，而不是直接上游 B。

## 两种模式

- `FailFast`：首个失败进入终态后，立即取消所有仍在运行任务，并把所有未开始任务标记 Skipped；可从失败任务到达者的原因为该失败，其他未开始任务原因为中止任务。
- `BestEffort`：不取消全局 Context；失败只沿其下游传播。独立分支继续运行。多个独立分支可全部失败，报告记录全部 Failed，每个下游分别指向各自最早根因。

取消是协作式的：`ctx` 关闭后任务应退出。若任务忽略取消并稍后写回，写回事件到达时其终态已是 Canceled，结果和错误一律丢弃。

## 调度算法

1. 初始入度为 0 的节点按 ID 升序放入就绪堆。
2. 每次“就绪判定”只弹出当前能在并发上限内启动的就绪节点；启动后递减空闲槽，不扫描全图。
3. 任务结束时，仅遍历该任务的直接下游，将其剩余入度减一；变为 0 才入堆。
4. 失败后按模式取消运行集或标记失败下游；队列中未启动项最终成为 Skipped。
5. 每个节点入堆一次；每条边在其上游完成时检查一次，总调度决策为 O(V+E)，上界 `4(V+E)`。
6. 1000 层链只使用循环和邻接表，不做递归 DFS 处理终态；环路径查找仅在异常小图上 DFS。

## 并发与生命周期

每个启动任务一个 goroutine；事件经单一事件通道进入调度循环，因此状态迁移串行化，任务可并行阻塞。调度循环在运行集和就绪集都清空后关闭结果通道，所有 worker 已通过 WaitGroup 收敛。取消关闭同一 Context，不向任务通道发送数据，避免忽略取消的任务阻塞调度器。

## 确定性

就绪节点用最小堆按 ID 升序取；同 ID 终态只写一次；多失败根因用字典序破平局；报告按 ID 排序。随机延迟只影响完成事件时序，不改变图语义和报告字节。

## 复杂度

- 构图与校验：O(V+E) 期望时间（map 存边去重），O(V+E) 空间。
- 调度：O((V+E)log V) 堆时间，O(V+E) 空间。
- 峰值并发：受信号量/空闲槽严格限制为配置上限，上限 1 时退化为串行。
