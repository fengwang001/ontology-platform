# 多租户配额公平队列调度器 — 设计推导

## 1. 虚拟时间为什么用 c/权重 而不是 c*权重

目标：长期运行下，租户 i 获得的执行代价总量 C_i 与其权重 w_i 成正比，即
C_i / C_j = w_i / w_j。定义租户虚拟时间 vt_i 为「已获得的归一化服务」：

    vt_i = C_i / w_i

调度规则「永远服务 vt 最小者」会把各租户的 vt 拉平（最小者被服务后 vt 增大，
不再是最小）。vt 拉平即 C_i / w_i ≈ C_j / w_j，也就是 C_i ∝ w_i，正是目标。
因此执行一个代价为 c 的任务后必须 vt += c / w。

反证 c*w：若 vt += c*w，则权重大的租户 vt 增长更快、更常被判定为「非最小」，
获得服务反而更少，长期份额与权重成反比，方向完全错误。直觉：权重是「配额
面值」，同样 1 单位 vt 对高权重租户应折算成更多实际服务，所以除法而非乘法。

## 2. 系统虚拟时间与空闲租户重入的 vt 抬升

维护系统虚拟时间 sysVT = 最近一次被选中任务开始服务时的 vt（即弹出堆顶租户
当时的 vt，单调不减）。sysVT 近似等于「当前活跃租户的归一化服务进度」。

问题：租户 X 空闲期间，其他租户持续推进，sysVT 从 v0 涨到 v1。若 X 的 vt 仍
停在 v0，X 重新入队后 vt 远低于所有活跃者，按「vt 最小优先」X 会被连续选中，
直到 vt 追上 v1。设 X 权重 w、任务代价 c，则独占步数约为：

    N = (v1 - v0) * w / c

例：Y（w=1, c=1）在 X 空闲期执行 1000 步，v1 - v0 = 1000，则 X（w=1, c=1）
会连续独占约 1000 步——空闲反而换来超额补偿，破坏公平也饿死别人。

正确做法：租户从空转为非空（含首次到达）时，把 vt 抬到

    vt = max(vt, sysVT)

语义：空闲期间视为「已按份额领取过服务」，重入后只与当前活跃者平起平坐。
抬升后 X 最多在「活跃租户数」步内被服务（并列时按 ID 序），不会独占；Y 的
vt 不变，继续按份额被服务，不会饿死。

## 3. 代价为 0 的任务

合法，但若 vt += 0 则该租户 vt 永不推进，永远是最小者，无限独占。处理：vt
记账时代价取下界 MinCost = 1.0，即 vt += max(c, MinCost) / w。下界只作用于
虚拟时间推进，统计中仍记录真实代价。负代价同样被下界兜住。MinCost 取 1 而
非极小值：下界必须与其他租户的典型代价同量级，否则实践中仍是独占。

## 4. 并列打破

多个租户 vt 相同（常见于初始 sysVT=0 或同步重入）时，按租户 ID 字典序取
最小者。规则纯函数化、无随机源，同一确定性任务流必然产生逐元素相同的调度
序列，保证可复现。

## 5. 数值表示：float64

vt 与代价用 float64。权重极大（1e6）与极小（1e-6）并存时，单步推进量级
c/w 最高 1e12，float64 上限约 1.8e308，需 1e296 步才溢出，实际不可达；53 位
尾数在 1e5 步调度内的累积误差远低于 5% 的公平性容差。相比有理数（大数库）
不在标准库内且性能差，float64 是标准库约束下的正确选择。

## 6. 复杂度

选择用二叉最小堆（container/heap），元素仅为非空租户。单次选择 = 一次弹出
（下沉每层 <= 2 次比较）+ 一次压回（上浮每层 <= 1 次比较），总比较次数
约 3*ceil(log2 n)，测试以 4*ceil(log2 n) 为上界留足余量。sched 内部用非导出
计数器记录单次选择的比较次数，经 LastComparisons() 只读暴露。空闲租户不在
堆中，不占用选择代价。

## 7. 背压、移除与错误分类

admit 包在 sched 外加每租户队列上限：超限返回 ErrQueueFull，且只拒绝该租户
的本次入队，其他租户不受影响。租户有排队任务时拒绝移除（ErrTenantBusy），
不静默丢弃任务；空租户可安全移除（此时它本就不在堆中）。四类错误用
errors.Is 区分：ErrInvalidWeight（w<=0/NaN/Inf）、ErrQueueFull、
ErrTenantBusy、ErrUnknownTenant。

## 8. 并发与确定性

admit.Limiter 用一把互斥锁串行化 Submit/Next：每个任务恰好入队一次、被
Next 取出一次，不丢不重。并发提交时，「提交顺序」定义为获取入队锁的串行化
顺序；单一流水（固定提交顺序与代价）下调度序列完全确定，20 次重放逐元素
一致。
