# DESIGN：properties 解析器/回写器

## 第 3 条推导：续行接上全空白（或空）行会怎样

依据 `java.util.Properties.LineReader.readLine` 的状态机：行尾奇数个反斜杠时，
丢弃一个反斜杠，并置 `skipWhiteSpace=true`、`appendedLineBegin=true`。
下一物理行开头的 `' '`、`'\t'`、`'\f'` 会被逐个跳过；但「跳过空行」的分支是
`if (!appendedLineBegin && (c=='\r' || c=='\n')) continue;`——续行模式下
`appendedLineBegin` 为真，该分支不生效。于是：若被接上的那一行只含空白或为空，
跳过空白后遇到的第一个字符就是行尾符，它**不被跳过**，而是直接结束当前逻辑行。
结论：`k=v\` ⏎ `   ` ⏎ `w=x` 解析为 `k`→`v`，然后 `w`→`x`；续行**不会**跨过
一个全空白行继续往下链接。与「续行首部空白被剥掉」的关系：剥离只删空白**字符**，
并不删该行的行尾符；全空白行对内容贡献为空，但它的行尾符仍然生效（终止逻辑行）。
文件结尾以奇数个反斜杠收尾时（EOF 版的同一规则），该反斜杠被丢弃。

## 第 6 条推导：Store 只转义必要的字符

以 Load 的对称逆操作推导。键：` `、`\t`、`\f` 会被当作键的结尾或分隔符两侧空白
吃掉 → 全部转义；`=`、`:` 是分隔符 → 全部转义；`\` 会与后一字符组合、且在行尾
奇数个时会触发续行 → 全部转义；`\n`、`\r` 是行尾符 → 全部转义；`#`、`!` 只有在
逻辑行首（即键的第 0 个字符）才被识别为注释 → 仅键首需要转义，键中部不必。
值：`\`、`\n`、`\r` 理由同上 → 全部转义；**前导**的 ` `、`\t`、`\f` 会被 Load 中
「分隔符之后跳过空白定位 valueStart」的逻辑吃掉 → 仅前导连续空白段需要转义；
值中间与末尾的空格在 valueStart 确定之后原样进入值（值延伸到行尾，尾部空白保留）
→ 不必转义。`#`、`!` 在值中永远不在逻辑行首（前面至少有键或 `=`）→ 不必转义。
非 ASCII：load(Reader) 是字符语义，不依赖 `\uXXXX` → 原样以 UTF-8 输出。

## 测试对照表

（待测试写完后补齐）
