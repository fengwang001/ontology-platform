# 设计：有预算的图广度优先遍历（确定性截断 + 可续传）

## 1. 目标与范围

给定只读邻接表图、起点与访问预算 `B`（非负整数，单位为**访问到的节点数**），
执行确定性 BFS：返回已访问序列、截断续点、计数器。不做图存储、索引、解析、最短路。

## 2. 计量单位：为什么是「访问的节点数」而不是「展开的边数」

预算必须度量用户可观测的产出（输出序列元素），即「访问（出队并记录）的节点数」。
若以「展开的边数」为单位，同样的逻辑图在不同存储形态下截断点会漂移：

1. 邻接表是否去重：重复边（`a->b` 存两份）会多耗预算，截断位置随去重与否变化。
2. 是否为叶子存空出边表：把空切片实现成 `nil` 与显式空表，若边数包含空展开记账也会变。
3. 边的展开顺序、自环是否算边、反向索引是否额外存一份，都改变「为得到同样 N 个节点需要考察多少边」。
4. 节点预算直接给出资源承诺：访问数 = min(B, 可达数)，且与图的物理编码无关。

故预算只扣减在「记录一个已访问节点」这一刻，考察边不扣预算。

## 3. 确定性所需固定的三处顺序

1. **出边顺序**：每个节点的出边按目标 ID 字典序（去重后），不来自 map 迭代。
2. **同层内入队/产出顺序**：严格按父节点在当前层的先后、再按目标字典序展开；
   等价于「每个父节点取其一个子节点追加到下一层，轮询推进」，同层顺序与标准 BFS 完全一致，
   且任一父节点都不会一次性把全部出边物化（这是驻留上限的关键，见第 5 节）。
3. **预算扣减点固定**：每产出一个节点扣 1，预算用尽立即停止；恢复时从续点精确重放，无随机量。

于是同图、同起点、同预算两次遍历逐元素相同（测试重复 20 次）。

## 4. 续点必须包含什么（为什么不能只记最后一个节点）

BFS 在截断瞬间的状态不是单个游标，而是：

- **已访问集合 `visited`**：否则续传会把环、自环、跨层汇合边指向的节点重复访问/重复入队。
- **当前层剩余待展开的父节点游标**：每个父节点 `(node, nextEdgeIndex)`，
  表示其出边已消费到哪里；只记「最后访问的节点」会丢掉同层尚未展开的兄弟。
- **已生成但尚未访问的下一层节点队列 `frontier`**（有序、去重）：
  这些节点已被发现、必须先于更深层被访问，丢失它们会改变序列。

续点 = `{visited 列表(按首次访问序), expanders[]=父节点+边游标, frontier[]}`。
初始续点：`visited=[], expanders=[{start,0}], frontier=[]`。
完成续点：`done=true`（其余为空），对其续传返回空序列而非错误（幂等）。
编码采用带长度前缀的规范字节串 + SHA-256 校验和（标准库），字段顺序固定；
解码依次分类：结构性损坏→字段不完整；校验不符→校验失败；结构完好但引用节点缺失→节点不存在。
任何被翻转比特的变体最多落入上述三类之一，不可能被当作合法续点接受。

## 5. 资源界与算法

展开循环（frontier 为空且仍有预算时）：取队首 expander `(u,i)`，
从 `u` 的第 `i` 条边起逐边扫描，跳过已访问/已在 frontier 的目标；
找到一个新目标 `v` 即：标记 visited?——否，发现即标记（`seen`），
追加 `v` 到 frontier；若 `u` 还有剩余边，把 `(u,i+1)` **放回 expander 队尾**（轮询），否则丢弃。
frontier 非空时：弹出队首节点访问并扣预算。注意发现即标记保证每节点只入队一次、
每条边至多被考察一次（父节点游标单调，且只有被访问节点的边才会被考察）。

**驻留界**：expander 个数 ≤ 已访问且尚未展完的节点数 ≤ B；
frontier 长度 ≤ 剩余预算（每发现一个待访问节点，未来必消耗 1 预算才可能再发现新节点，
生成在预算用尽时即停），故峰值队列 ≤ 2B（星形图 B=100 时远小于 4B=400）。
**访问数**精确等于 min(B, 可达数)。**考察边数** ≤ Σ outdegree(已访问节点)。

## 6. 错误分类（errors.Is 三类）

- `resume.ErrTampered`：校验和不符（内容被改但结构仍可解析）。
- `resume.ErrMalformed`：字段不完整/长度前缀越界/版本非法。
- `graph.ErrNodeNotFound`：结构合法但续点引用了图中不存在的节点（含起点不存在、续传间删节点）。

续传前校验续点引用的每个节点仍存在；删除队列中节点则返回包裹该节点 ID 的 ErrNodeNotFound。

## 7. 并发

图构造后只读（出边存排序切片，读取不加写锁、无隐藏可变状态）；遍历是纯函数，
计数器在每次调用内部独立分配，多协程同图同续点并发续传结果逐元素相同。

## 8. 等价性自检（audit）

对总预算 n 与每个切分点 a=1..n-1：先以预算 a 遍历，编码续点，再以 n-a 续传（可多段），
拼接序列必须与一次性预算 n 的结果逐元素相同。
