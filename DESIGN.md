# 严格流式 Base64 设计

## 规范尾部推导

RFC 4648 把每 3 字节（24 bit）切成 4 个 6 bit 组，记为 `a b c d`。

- 3 字节：`a b c d` 均有效，无 `=`。
- 2 字节：只有 16 bit，补 2 个 0 形成 `a b c =`；`c` 的低 2 bit 必须全为 0。
- 1 字节：只有 8 bit，补 4 个 0 形成 `a b = =`；`b` 的低 4 bit 必须全为 0。

因此统一规则是：按填充形式确定最后一个**非填充字符**，只允许编码时为对齐组而补出的 0 bit；这些尾部位必须全为 0，且 `=` 只能按 0、1、2 个出现在整个输入末尾。

`QR==` 中 `Q=16(010000)`、`R=17(010001)`。一字节情形只使用 `b=R` 的高 2 bit（`01`），其低 4 bit 是 `0001`，不是编码产生的补零，所以拒绝；虽然宽松拼接得到 `A`，它不是 `A` 的规范编码。`Encode("A")` 固定选择补零字符，得到唯一的 `QQ==`。严格拒绝所有非零尾部位后，任意字节串只有一个合法编码结果，严格解码与编码互为逆映射。

## 测试对照

待实现后补齐。
