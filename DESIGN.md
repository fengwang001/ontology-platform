# URL 规范化与等价判定器 — 设计推导

模块划分：`pct`（无依赖）→ `segpath`/`query`（依赖 `pct`）；`host`（无依赖）；
`canon`（依赖以上全部）。依赖方向单向，无反向依赖。全工程只用标准库，
不使用 `net/url`，状态全部在进程内存。

## 第 2 条：哪些转义可以还原，哪些绝对不能

推导起点只有一条公理：**还原一个转义后，URL 的解析结构不得改变**。

URL 的解析结构由"分隔符"决定：`/` 切路径段、`?` 切查询、`&`/`=` 切查询项、
`:` 切端口、`#` 切片段、`%` 引入转义本身。RFC 3986 把字符分为
*unreserved*（`A-Z a-z 0-9 - . _ ~`）与 *reserved*（gen-delims + sub-delims）。
对 unreserved 字符，转义形式 `%41` 与字面量 `A` 在任何解析器下都落在同一个
token 内部，既不产生也不消灭任何分隔符——**还原是安全的**，而且 RFC 3986
§6.2.2.1 明确要求这样做才能达成"同一资源唯一形态"。

对 reserved 字符则相反：字面量形式是分隔符，转义形式是数据。若把路径段里的
`%2F` 还原成 `/`，一个段会变成两个段，`..` 回退的层级、缓存键、授权规则
（按路径前缀匹配）全部改变——`/a%2Fb` 是"段 `a/b`"，`/a/b` 是"段 `a` + 段
`b`"，二者**不是同一资源**。因此保留字符的转义一律保持转义，只做十六进制
字母大写归一（`%2f` → `%2F`），这是不改变语义的纯形式归一。

由此得到实现规则（`pct.Normalize`，单遍扫描）：

- `%XX` 解出的字节是 unreserved → 写字面量（`%41` → `A`，`%2e` → `.`）；
- 否则 → 原样保留转义、hex 字母大写（`%2f` → `%2F`，`%25` → `%25`，
  因为 `%` 自己也是结构字符）；
- 非 `%` 字节原样通过。

注意 `%2E`（`.`）会被还原成字面量 `.`，随后路径消解会把它当点段处理——
这正是安全所需：`/%2e%2e/x` 与 `/../x` 必须同等对待，否则授权检查可被
编码绕过。

## 第 3 条：路径消解的两条规则依据

**根之上的 `..` 为什么吞掉而不是报错。** `..` 的语义是"回退一级"，在根上
已经没有可回退的层级，此时有两种选择：报错，或定义为恒等（吞掉）。选择
吞掉的理由：(a) RFC 3986 §5.2.4 的 remove_dot_segments 算法就是这样定义
的——输出缓冲为空时 `..` 不产出任何内容，与所有主流实现（浏览器、反向
代理）一致；规范化器若在此处报错，就会与上下游对同一 URL 的解读产生分歧，
而分歧正是缓存穿透与越权的来源。(b) 安全上吞掉是**保守**的：`/../../x`
归一到 `/x`，攻击者无法借 `..` 逃逸到根之上——根本没有"根之上"可去。
(c) 吞掉保证幂等：`/x` 再规范化仍是 `/x`；若报错则 `canon(canon(x))` 的
定义都变得复杂。实现上，绝对路径的段栈底是根标记（首空段），弹栈条件
`canPop` 拒绝弹出根标记，天然实现"吞掉"。

**尾斜杠为什么保留语义。** `/a` 与 `/a/` 在服务器上可以是不同资源
（典型：目录索引 vs 文档本身；相对解析的基准段数也不同）。既然存在把
二者区分为不同资源的合法部署，规范化器就**无权**把它们合并——合并会
改变资源身份，违反第 2 条的公理。同理，连续斜杠产生的空段（`/a//b`）
也保留：某些框架把空段当合法路由参数。规范只做"一定不改变身份"的事：
删 `.`、回退 `..`，其余原样保留。尾斜杠在实现上体现为：末尾的
`.`/`..` 消解后补一个空段（`/a/.` → `/a/`，`/a/..` → `/`），普通尾空段
直接保留。
直接保留。

## 第 4 条：查询三态如何保持，混淆会怎样

`a`、`a=`、`a=%20` 在原始字节上就是三种不同形态，规范化只需**不引入
新的合并**即可保持可判定：

- `a` → `Item{Key:"a", HasValue:false}`，渲染回 `a`；
- `a=` → `Item{Key:"a", Value:"", HasValue:true}`，渲染回 `a=`；
- `a=%20` → 值是保留字节空格的转义，`%20` 不还原（空格不是 unreserved），
  渲染回 `a=%20`。

关键在于 `HasValue` 这个独立布尔位：若像某些解析器那样把"无等号"建模为
"空值"，三种形态就坍缩成两种。混淆的现实后果：许多框架（如 PHP 的
`$_GET`、若干路由中间件）对 `?a` 与 `?a=` 给出不同的解析结果（缺失键 vs
空字符串值），授权规则若写"参数 a 为空则放行"，把 `?a` 归一成 `?a=` 就会
让不带值的请求通过本不该通过的检查；缓存层若把三者混为一键，后端返回的
不同响应会互相污染（缓存穿透/错投）。重复键同理：`a=1&a=2` 不得去重，
因为"第一个生效"与"最后一个生效"的框架都存在，去重等于替服务器做决定。
`ModeSorted` 只重排不删除，排序键为 (Key, Value, HasValue) 的全序，
因此 `a=1&a=2` 与 `a=2&a=1` 在 Sorted 下等价、在 Ordered 下不等价——
两种模式都保持三态可判定。

## 第 7 条：等价判定为什么满足传递性

定义：`Equivalent(a, b) ⇔ canon(a) = canon(b)`（规范形式逐字节相同）。

论证：设 `~` 为上述关系，`C(x)` 为规范化函数（对合法输入是字符串上的
全函数，且幂等：`C(C(x)) = C(x)`，由 2000+ 随机输入测试大规模验证）。
则 `a ~ b ⇔ C(a) = C(b)`，即 `~` 是函数 `C` 的核（kernel）——
"像相同"关系。任何函数的核都是等价关系：

- 自反：`C(a) = C(a)`；
- 对称：`C(a) = C(b) ⇒ C(b) = C(a)`；
- 传递：`C(a) = C(b) ∧ C(b) = C(c) ⇒ C(a) = C(c)`，

三条都由字符串相等 `=` 本身是等价关系直接继承。测试用等价类穷举验证：
组内两两等价、跨组两两不等价，并对全集所有三元组显式检查传递性——
因为等价 ⇔ 规范形式相同，这些检查本质上是在验证 `C` 的行为与定义一致。

## 第 8 条：单遍扫描结构，为什么没有重复解码

规范化的数据流是一条直线，每个字节只被每个阶段各看一次：

1. **切分**（`canon.splitScheme/splitRest`）：对原始串做一次线性扫描，
   定位 `://`、`/`、`?`，切成 scheme/authority/path/query 四段。O(L)。
2. **各组件独立规范化**：`host.Normalize` 扫 authority 一次；
   `segpath.Normalize` 对 path 做一次 `strings.Split`，每段交给
   `pct.Normalize`；`query.Parse` 同理。所有段的长度之和 = 组件长度，
   因此这一阶段总量仍是 O(L)。
3. **`pct.Normalize` 内部单遍**：逐字节扫描，遇 `%` 取后两位，输出缓冲
   一次写入；解码流 `dec` 与输出同步构建，UTF-8 校验是对 `dec` 的一次
   线性 `utf8.Valid`（常数因子，不改变复杂度）。只有**出错路径**才为
   报告偏移而重扫——正常路径绝不重复解码。
4. **组装**：一次拼接输出。O(L)。

关键点：不存在"先整体解码再整体编码"的两遍结构，也没有任何组件被
pct 处理两次——`segpath`/`query` 拿到的段直接消费 `pct.Normalize` 的
输出，`canon` 不再对结果做任何再解码。点段消解在已解码的段上进行，
是同一遍里的栈操作，不回溯原始字节。

可证明性：`canon.Normalizer` 内置非导出计数器 `scans`（`atomic.Uint64`），
在切分、host、path、query 各阶段按组件长度累加。测试
`canon/complexity_test.go` 用 L≈1KB 与 L≈64KB 两组输入实测，实测数据：
L1=1027 字节扫描 3054 次，L64=65539 字节扫描 196590 次，扫描比 64.4 ≈
长度比 63.8（每字节约 3 次扫描的常数因子），并断言比值落在线性区间、
且总量不超过 8L——平方增长的实现在 64KB 组会超出该上界而被测试拒绝。
