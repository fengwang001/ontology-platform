# CSV 方言设计

## 关键推导

1. **空行与单列空值**：空行选择“跳过”，因为行首立即出现换行时没有任何字段证据；否则空文件与一条空记录无法区分。单列空值必须写成 `""\n`：若写成裸换行，回读时会按空行跳过，记录消失。多列中的空字段仍由逗号定位，保持未引号。
2. **并行切点**：每段分别以“起点在引号外（字段起点）”和“起点在引号内”两个假设解析。第 0 段只算外假设；之后从左到右依据上一段结尾的 `quoted/closed-quote/CR-pending` 等状态选一份。引号内结果的首字段与前段首字段拼接；closed-quote 后只允许逗号、CR、LF，否则在闭合引号处报错；CR-pending 后只允许 LF。每份输出都带全局绝对偏移；段内记录号在拼接已产出记录后换算，错误延迟到被选中的轨迹，坐标与流式相同。每字节最多进入两个假设，不随长引号字段重扫。
3. **CR 待定**：未引号态或 closed-quote 态见 CR，不立即放行，记录 CR 偏移；下一字节是 LF 才产生记录结束，否则在原 CR 处报孤立 CR。半包时该状态跨 `Feed` 保留；`Close` 仍待定则报错。`par` 切点在 CR/LF 间时，下段外假设会把 LF 看成空行，拼接器将其改配为上一记录结束；其它字节直接报原 CR。
4. **字段上限的立刻性**：计数的是解码后的字段字节：普通字节加一，引号内 `""` 在第二个引号确认转义后只加一。每收到一个 Data 事件先检查 `已计数+1 > 上限` 再写入，因此第一个超限字节即终止；错误前完整记录保留，解析器进入终态。

## 状态转移表

| 状态 | 逗号 | LF | CR | `"` | 其他字节 |
|---|---|---|---|---|---|
| field-start | 结束空字段，回 field-start | 空行：跳过，回 field-start | 进 CR-pending，记 CR | 进 quoted，发 Open | 进 unquoted，发 Data |
| unquoted | 结束字段，回 field-start | 结束记录，回 field-start | 进 CR-pending，记 CR | 裸引号错误 | 发 Data，留 unquoted |
| quoted | 发 Data(逗号) | 发 Data(LF) | 发 Data(CR) | 进 closed-quote，记引号偏移 | 发 Data，留 quoted |
| closed-quote | 结束字段，回 field-start | 结束记录，回 field-start | 进 CR-pending，记 CR | 发 Data(`"`)，回 quoted | 闭合后杂质错误 |
| CR-pending | 不应到达；先由待定检查拒绝 | 结束记录，回 field-start | 同左：仅 LF 合法 | 同左：孤立 CR 错误 | 同左：孤立 CR 错误 |

## 其他约定

- 规范输入：记录均以 LF 结尾；引号只用于包含逗号、双引号、CR、LF 的字段，或单列空字段；引号内 CRLF 原样保留。
- writer 保留已解析单元格的引号标记；仅上述必要情形会为原本无引号值新增引号。
- 单个流式解析器不是并发安全的；多个独立解析器可并发使用。
