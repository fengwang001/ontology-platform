# JSON 字符串严格编解码器 — 设计推导

## 代理对错误偏移（语义 2）

规则：任何无法组成合法代理对的代理码元，错误偏移指向它自己的 `\u` 的反斜杠。

- 孤立低代理（如 `"\uDE00"`）：它出现在普通字符位置上，自身即非法，立即在其反斜杠处报错，无需前瞻。
- 高代理（如 `"\uD83D"`）：合法性取决于随后 6 字节。解码器记下其反斜杠偏移 hiOff，若后续不是 `\u` + 4 位十六进制的低代理，则在 hiOff 处报错。理由：
  (a) 与 UTF-8 非法前导字节的报错惯例一致——错误归于发起者而非受害者；
  (b) 偏移与前瞻长度无关，`"\uD83D"`、`"\uD83Dx"`、`"\uD83DA"` 报同一偏移，可预测；
  (c) 流式切分下报错偏移不依赖尚未到达的字节，任意切分结果一致。
- `"\uDE00\uD83D"`（顺序颠倒）：第一个码元已是孤立低代理，在其自身起点报错，根本不会看到第二个码元。

为什么 `"\uD83DA"` 不能解码为「U+FFFD + A」：
(a) RFC 8259 明确孤立代理的行为不可预测，严格编解码器唯一安全的选择是拒绝；
(b) 若静默替换，`"\uD83DA"` 与 `"\uFFFDA"` 两个不同字面量解码结果相同，Decode 不再单射，且 `Encode(Decode(y)) == y` 对该输入不成立；
(c) 替换会抹掉「输入非法」这一事实，调用方无法再用哨兵错误判定。

## 最小转义与唯一性（语义 4）

转义是逐码点的确定映射：`"` 与 `\` 用反斜杠转义，`\b\f\n\r\t` 用短形式，其余 U+0000–U+001F 用 `\u00xx`（小写十六进制），其它一切码点原样输出。

- U+001F 必须转义：RFC 8259 的字符串文法明文禁止未转义的 U+0000–U+001F。
- U+007F 不必转义：它不在禁止集合内，是字符串中合法的 Unicode 标量值，原样传输不会被 JSON 文法误解；转义它纯属冗余，与最小原则矛盾。`/`、U+2028/U+2029 同理。
- 单射：每个码点的输出形式唯一；所有转义形式以 `\` 开头，而 `\` 本身必被转义，故转义序列与原文逐字节可区分、拼接可唯一解码，不同输入必得不同输出。
- 唯一性：上述映射是函数（每个码点恰匹配一条规则），故同一输入输出唯一。

## 测试对照表

（待测试写完后补充）
