# 设计推导

## 代理对错误偏移规则（第 2 条）

解码严格从左到右一次扫描；`\uXXXX` 在读到第 4 位十六进制的瞬间成为「完整
单元」，码位随即确定。规则：

- 孤立低代理（U+DC00–U+DFFF）读到其第 4 位即拒绝，偏移指向**该单元的 `\`**
  （如 `"\uDE00\uD83D"` 指向偏移 1，即顺序颠倒的第一个单元）。
- 高代理（U+D800–U+DBFF）必须紧邻一个低代理单元：在「高代理之后的第一个完整
  单元」判定；不是低代理（普通码位、高代理、引号、非转义字符、EOF）即拒绝，
  偏移指向**高代理自己的 `\`**。

理由：错误归属「无法被补全的那个转义单元」。孤立低代理自身即非法标量值；
孤立高代理的非法性在后续单元揭晓时才确定，但病灶是没有配对的高代理，偏移
始终回指其起点，规则单一、与切分无关。

`"\uD83DA"`：`A` 是字面量内容而非第二个 `\u` 单元，高代理无配对，必须拒绝；
不能解成「U+FFFD + 'A'」——那是静默修复，违反「不静默替换 U+FFFD」。

## 最小转义、单射与唯一性（第 4 条）

语法上必须转义的只有：`"`（否则提前闭合）、`\`（否则引发转义解析）、
U+0000–U+001F（RFC 8259 禁止未转义控制字符）。`/` 的短转义合法但非必须；
U+007F（DEL）不在禁令区间；U+2028/U+2029 在 JSON 中合法；非 ASCII 原样输出。
故 0x1F 必须转义而 0x7F 不必。

输出唯一：每个码点映射确定——`"`→`\"`、`\`→`\\`、`\b\f\n\r\t` 取短形式、
其余控制字符取唯一的 `\u00XX`、其余原样。单射：转义输出都以 `\` 开头，而
原样分支不产生 `\`（输入的 `\` 必走 `\\`），按 `\` 切分可无歧义还原；短/长
形式覆盖的码点集合互不相交，不存在两码点映射到同一输出。

## 语义 ↔ 测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类错误可区分、带偏移 | TestDecodeErrors |
| 2 代理对组合与拒绝 | TestSurrogates |
| 3 非法 UTF-8 两个方向 | TestInvalidUTF8 |
| 4 最小转义与规范输出 | TestEncodeMinimal |
| 5 双向往返（含随机） | TestRoundTrip |
| 6 任意切分点一致 | TestStreamingSplits |
| 计数器 ≤ 2n | TestByteCheckBudget |
