# DESIGN

## 零计数时的 hunk 头行号

unified diff 的 `-a,b` 是「1 基闭区间」：覆盖旧文件第 a..a+b-1 行。
插入点位于两段上下文之间，其上一行（旧文件最后一个被覆盖/相邻行）的 1 基行号为 p，则区间为空时 a=p：
插入发生在文件开头，前面没有行，p=0 → `-0,0`；在第 2 行与第 3 行之间插入，上一行是第 2 行 → `-2,0`；
文件非空且全部删除时，区间必须覆盖既有行，取 `-1`（b=1 省略计数）。
Apply 侧统一换算：旧区间起始的 0 基下标 = a-1（b=0 时即插入点下标，`-0,0`→0、`-2,0`→2，正确）；
新区间起始 0 基 = c-1（`+0,0`→0）。

| 情形 | 书写 | Apply 解释 |
|---|---|---|
| b=0 | `-a,0`，a=插入点上一行行号（开头为 0） | 在旧下标 a 处纯插入 |
| b=1 | `-a`（省略 `,1`） | 旧下标 a-1 起 1 行 |
| b≥2 | `-a,b` | 旧下标 a-1 起 b 行 |

## hunk 合并阈值

两段改动之间有 g 行未改动。合并后前一 hunk 的尾部上下文与后一 hunk 的头部上下文各需 C 行；
g≤2C 时两段上下文不缺（不足则共享全部 g 行），GNU `diff -U C` 输出一个 hunk；
g=2C+1 时中间恰好剩 1 行上下文余量，拆成两个 hunk。故阈值为 **2C**。
反例：阈值取 C 时，C=3、g=4（>3 但 ≤6）会错误拆分 GNU 合并的 hunk；
取 2C+1 时，C=3、g=7 会把中间恰有 1 行余量的两段错误合并。

## 并列最短脚本的选择

Myers V 迭代中同一条对角线上「向下（删除）」与「向右（插入）」并列时，固定优先**向下（先删除）**，
与 GNU diff 一致：`a b`→`b a` 输出 `-a`、` b`、`+a`（删除行排在对应插入行之前）。
实现上取 k=-D 起向下、并列分支（k 同时可达且 x 相同）也选向下。

## 偏移后后续 hunk 的平移

hunk i 在记录位置偏移 d 行处成功，则 hunk i+1 的查找中心从「其头中记录位置 +d」开始。
理由：一次成功的偏移说明文档整体在该点发生位移，记录坐标整体过时；
d = 实际匹配旧下标 − 头中旧下标。后续 hunk 只在新中心 ±F 内搜索，候选并列时取距离近、再取靠前者。
