# 三态熔断器设计

## 包依赖与时间模型

依赖方向单向：`outcome` ← `tally` ← `state` ← `breaker`。时间全部由 `now func() int64`（毫秒）注入；不启动任何后台定时器，所有「到期的转移」在 `Admit` / `Kind` 被调用时惰性求值。因此「转移时刻」= 第一个观察到时间已越过边界的调用的 `now`，对同一串（结果, 时刻）序列完全确定。

## 推导一：半开期间的判定规则

- 规则：半开期内**任一探测失败 → 立即重新断开**；累计**配额次探测成功 → 恢复闭合**。
- 与配额自洽：成功阈值取成配额，语义是「把全部探测名额都用成功填满」才恢复，等价于要求一批探测无一失败；失败阈值取 1，因为半开的目的是低成本探活，一次失败已足以判定下游未恢复，无需消耗更多配额。
- 配额=1：一次探测定生死——成功即闭合、失败即断开，状态机每来一个调用必离开半开，不会停留。
- 配额=3：在途 < 3 时新调用总会被放行补位；每个被放行的探测必然 resolve 恰好一次（panic 也先按失败 resolve 再原样上抛），于是半开期只有两种结局：集满 3 次成功 → 闭合，或某次失败 → 断开。超出配额的调用立即被拒、不排队，不会堆积成「永远等不到空位」。唯一看似停留的情形是根本没有新调用到来——那是静默而非卡死，下一个调用到来即继续探测直至分出胜负。

## 推导二：冷却时刻的边界

- 约定「到期即生效」、左闭右开：断开时刻 `t0`、冷却 `d`，则 `[t0, t0+d)` 内拒绝，`now >= t0+d` 起按半开处理。恰好经过冷却时长的那一刻**已进入半开**。
- 实现为 `now - openedAt >= cooldown` 即转入半开。
- 理由：冷却 `d` 的直觉语义是「等满 d 毫秒即可探测」；若边界仍算断开，实际冷却变成 `d+1` 毫秒，与配置值不符。左闭右开同时保证「断开区间长度恰好为 d」，便于推导与测试。

## 推导三：统计窗口的语义

- 选**计数窗口**（最近 N 次调用，环形桶），不选时间窗口。
- 时间窗口在「长时间无调用后突然来一次失败」时，窗口内只剩这一个样本，失败率 100%，一次失败即误断开；且窗口随时间自动清空，样本量随流量起伏，不可控。
- 计数窗口下，静默期的旧样本仍留在窗口内，单次失败被历史样本稀释，不会独自构成判决。
- 第二道闸：仅当窗口内样本总数 ≥ MinSamples 时才允许失败率触发断开。样本量有下界，「样本太少就误断开」在结构上被排除；窗口未攒满时任何失败率都只记录、不判决。

## 推导四：半开中途被拒的调用

- 由不变量 3：被拒调用不得影响统计——它根本没有发出，对下游健康状况的信息量为零。
- 推论：它也不是「半开期的一次结果」。不计入探测成功数、不计入失败数、不占用在途名额（在 `Admit` 阶段即被拒，从未进入 inFlight）。
- 因此「半开期的结果集合」恰好等于「被真实放行的探测的结果集合」，二者由 ticket 机制一一对应；上一半开期迟到的结果凭过期 ticket 被丢弃，不污染当前期。

## 转移表（当前状态 × 事件 → 新状态）

| 当前状态 | 事件 | 条件 | 新状态 | 动作 |
|---|---|---|---|---|
| 闭合 | 调用到达 | — | 闭合 | 放行 |
| 闭合 | 调用结果 | 样本 < MinSamples 或失败率 < 阈值 | 闭合 | 计入窗口 |
| 闭合 | 调用失败 | 样本 ≥ MinSamples 且失败率 ≥ 阈值 | 断开 | openedAt=now |
| 断开 | 调用到达/查询 | now < openedAt+冷却 | 断开 | 拒绝，不计统计 |
| 断开 | 调用到达/查询 | now ≥ openedAt+冷却 | 半开 | epoch+1，在途/成功数清零 |
| 断开 | 上一期迟到结果 | — | 断开 | 忽略 |
| 半开 | 调用到达 | 在途 < 配额 | 半开 | 在途+1，放行探测 |
| 半开 | 调用到达 | 在途 = 配额 | 半开 | 拒绝，不计统计 |
| 半开 | 探测成功 | 成功数+1 < 配额 | 半开 | 成功数+1，在途-1 |
| 半开 | 探测成功 | 成功数+1 = 配额 | 闭合 | 清空窗口与半开计数 |
| 半开 | 探测失败 | — | 断开 | openedAt=now，在途清零 |
| 半开 | 上一期迟到结果 | ticket 过期 | 半开 | 忽略 |

## 自检（Check）

一次性核验：0 ≤ 在途 ≤ 配额；非半开时在途 = 0；半开成功数 ∈ [0, 配额)；被拒次数 + 真实调用次数 = 总请求数；tally 总数 = 成功 + 失败且不超过容量。
