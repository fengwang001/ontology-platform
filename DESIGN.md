# 游标分页器设计（keyset pagination）

## 1. 排序模型

- 行 = `(Value float64, ID string)`，ID 全局唯一。
- 全序：先比 `Value`，相等再按字节比 `ID`，记为复合键 `k=(v,id)`，比较为严格字典序 `<`。
- 数据集始终按此全序维护；排序本身确定且稳定，不需要行号。

## 2. 为什么游标不能只记排序值

反例：6 行排序值同为 v，另有其他 4 行。页大小 3。

- 下一页取 `Value > v`：v 组剩余 3 行被整批跳过 → **漏**。
- 下一页取 `Value >= v`：上一页已返回的 3 行再次出现 → **重**。

正确做法：游标记录上一页最后一行的复合键 `(v,id)`，下一页边界为复合键
**严格大于**：`(Value,ID) > (v,id)`，即 `Value>v` 或 `Value==v && ID>id`。
同值行由此被 ID 逐一切开，任何一行都既不会被跳过也不会重复。

## 3. 反向翻页规则

反向不是把正向页倒序，而是同时翻转「比较方向」和「取数方向」，最后保持页内正向序：

- 正向（Fwd）：从游标键之后取最小的 n 行，升序；
- 反向（Bwd）：从游标键之前取最大的 n 行（扫描方向相反、比较为严格小于），
  取出后仍按 `(Value,ID)` 升序排列返回。

因此「正向第 3 页游标 → 反向一页」逐行等于正向第 2 页，而非其倒序。
游标自带方向标记；方向与请求不符返回 `ErrWrongDirection`，不静默处理。

## 4. 游标编码

不透明字节串，定长前缀：`版本(1) | 方向(1) | float64 值(8,大端) | ID长度(4,大端) | ID字节 | FNV-1a校验(8,大端)`。
校验和覆盖其前全部字节（含版本）。解码 O(1)（ID 仅一次拷贝）。
错误四类，皆可用 `errors.Is` 区分：

- `ErrEmpty`：空字节串，表示从头开始（合法，不是错误）；
- `ErrChecksum`：校验和不符（含版本翻转等内容篡改）；
- `ErrTruncated`：声明长度与实际不符 / 字段不完整 / ID 非法 UTF-8；
- `ErrBadDirection`：方向标记非 Fwd/Bwd。

判定顺序：先结构完整与长度，再方向标记，最后校验和；
任何比特翻转至多落入上述一类，且绝不被接受为合法游标。

## 5. 取数与效率

- 内存中按复合键有序的切片 + `sync.RWMutex`；增删后重排。
- 起点用 `sort.Search` 二分定位，再顺序取至多 n 行。
  单页比较次数上界 `n + ceil(log2 N)`；本实现计数含两侧边界比较，
  故约束为 `4*(n + ceil(log2 N))`（10000 行、n=20 时为 136）。
- 游标只携带不可变键，定位以复合键为准，不依赖该行是否仍存在：
  指向已删除行时，从该键在当前集合中的位置继续即可。

## 6. 并发增删下的语义承诺

- 翻页是游标 → 页的纯函数，不保存任何推进状态；同一游标并发使用，
  在同一数据快照下逐行相同（`-race` 干净）。
- 插入到「已翻过区间」的行允许不出现（无快照要求）；
  插入到「未翻区间」的行，会在到达其复合键位置时正常出现。
- 删除的行不再出现。
- 不变量：① 任一已返回行不会因后续翻页再次返回；
  ② 未被删除的行不会在其复合键区间内被跳过
  （单调严格边界保证；插入只在键空隙中加入新键，不移动旧键的相对次序）。

## 7. 包划分

`row`（键定义与比较）、`cursor`（编解码与错误分类）、
`page`（并发存储、正反向取页、比较计数）、`audit`（不重不漏自检）、`cmd/demo`（验收演示）。
