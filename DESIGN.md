# 自定 LZ77 流式压缩：推导与格式

## 1. Write 边界无关（语义 3/4）
贪心匹配在位置 p 的长度 L，只有在看到 p+L 处字节确实破坏匹配时才最终确定。压缩器因此把“未决”字节留在内存：至少 MinMatch-1 个尾部字节，外加正在增长的匹配体与一个探测字节。Write 只追加字节并运行结算循环，绝不因 Write 返回而输出记录；故 1/7/整段三种切法在相同累计偏移处达到相同的已决状态，输出逐字节相同。Flush 没有未来字节，故强制当前匹配以现长结束、把全部未决字节排干为记录，并发一个 flush 标记来固定解码边界；无新输入时第二次 Flush 无字节可排干，输出零字节。

## 2. 重叠回指（语义 2）
回指 (d,L) 在 L>d 时一次性 memcpy 会自覆盖而损坏；正确语义是 out[i]=out[i-d]，只用已产出字节。该段实质是周期 d 的重复：实现先从历史复制前 d 字节，再以“成倍增广”的 copy（dst=自身前缀）处理整倍数块；L<=d 时直接非重叠 copy。整体 O(L) 且无逐字节循环。

## 3. 距离超已输出 vs 距离超窗口（语义 7）
二者是不同偏移上的不同失败：流（块）早期可能 d 大于现存字节却不超容量；而伪造的 d 可能超过固定环容量。压缩器只发出绝对位置仍在活动环内（>=written-cap）的候选，故绝不会产生超容量距离。并行块把上一块末 cap 字节预置为字典，这些字节立即可用：块 n>1 解码器的可用量 = 字典长 + 本块已产出；先按可用量判，再按容量判。

## 4. 并行确定性（语义 9）
块 k 用全新匹配器，仅以块 k-1 原文末 cap 字节预置；其记录只依赖（块 k-1，块 k），与其他块的输出、完成顺序、同步均无关。拼接流为：块0 一个流头；每块原始记录；每块边界一个 flush 标记，使解码器边界与编码器一致；末块一个流尾。按块下标而非完成顺序拼装，故 workers 1/2/4/8 逐字节相同。

## 流格式
所有整数为无符号 LEB128 变长整数，最多 10 字节，超长或溢出 64 位即损坏。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4C 5A 37 01`（"LZ7"+版本1） |
| 字面量 tag=0 | tag, len(varint), len 个原文字节 |
| 回指 tag=1 | tag, dist(varint,>=1), len(varint,>=MinMatch=3) |
| 刷新 tag=2 | tag |
| 流尾 tag=3 | tag, 原文总长(varint), FNV-1a64 校验和(varint) |

空输入 => 流头+流尾(0, fnv64a(""))；0 字节输入判截断；有头无尾判截断。窗口为定容环+绝对位置计数；3 字节哈希链，每位置保存同哈希前驱，Find 每位置最多走 ChainLimit 个存活候选，包内非导出计数器记录考察候选总数；超龄位置被丢弃，故最坏每字节 O(ChainLimit)，不随规模增长，全同字节最坏情形立即链到紧邻前驱。压缩器/解压器单实例不要求并发安全，多实例相互独立。
