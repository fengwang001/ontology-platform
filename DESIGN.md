# DESIGN：游标分页器（keyset pagination）

## 1. 排序值重复时为什么必须用复合键

数据行是 `(score float64, id string)`，排序规则为：先按 `score` 升序，
`score` 相等时按 `id` 升序。因此全序键是复合键 `K=(score,id)`，`id` 唯一，
键空间是全序的。

反例：六行 `score=1`，id 为 a..f，页大小 3，第一页取走 a,b,c，末值 `v=1`。

- 下一页取 `score > v`：d,e,f 被整批跳过（**漏**）。
- 下一页取 `score >= v`：a,b,c 再次出现（**重**）。

正确做法：游标记录上一页末行的复合键 `(score,id)`，下一页边界是复合键的
**严格大于**：`(score > s) || (score == s && id > lastID)`。等值组因此被
键序逐一切开，不重不漏。空游标表示「从头开始」。

## 2. 反向翻页规则

反向不是把正向页倒序。设游标指向某行的键 `K`：

- 正向：扫描键序中 `key > K` 的后继，取最小的 `n` 个，页内保持升序，
  游标指向本页最后（最大）键。
- 反向：扫描键序中 `key < K` 的前驱，取最大的 `n` 个，再将这 `n` 个
  **反转为升序**输出；页内顺序仍是升序。返回的游标指向本页最大键，
  因此从第 3 页反向一页，逐行得到第 2 页（而非第 2 页的倒序）。

## 3. 并发增删下的语义

数据集用一个 `RWMutex` 保护不可变有序切片：增删时复制出新切片后整体替换
（copy-on-write），每次翻页持有读锁、在同一份不可变快照上完成定位与取数，
单次翻页原子、无撕裂读。

语义承诺（不做快照隔离）：

1. 翻页是纯函数：同一游标 + 同一份数据，并发调用得到逐行相同的页，
   游标对象不记录任何推进状态。
2. 已返回过的行不会再次出现：边界是严格键比较，已翻过的键区间不会重入。
3. 未删除的行不会在其键区间内被跳过：新行插入到**尚未翻过**的区间
   （键大于当前游标）会在后续页按复合键顺序出现；插入到已翻过区间允许
   不出现（无快照保证）。
4. 删除只影响数据，不影响边界：游标记录键值而非行位置/指针，指向的行被
   删除后仍可凭键继续翻页。

## 4. 游标格式（不透明字节串）

```
'ver'(1) | dir(1) | score(8 BE) | idLen(2 BE) | id(idLen) | fnv1a(4 BE)
```

- 首字节为版本魔数；dir=1 正向、dir=2 反向。
- 校验和覆盖其之前全部字节（FNV-1a 32 位）。空字节串合法，表示首页起点。
- 解码分类顺序：长度过短/截断 → `ErrCursorCorrupt`（字段不完整）；
  方向字节非 1/2 → `ErrInvalidDirection`；校验不符或版本错 → `ErrChecksum`。
  翻页方向不符 → `ErrDirectionMismatch`（均可用 `errors.Is` 判定）。

## 5. 效率

- 数据按复合键有序存放，用 `sort.Search` 二分定位边界，单次翻页只检查
  边界之后一个页大小的行；比较计数含二分比较，上界约
  `n + ceil(log2(N))`，测试断言 `<= 4*(n+ceil(log2(N)))`。
- 解码只做定长读取与一次 `string(b[...])`，分配次数为常数（测试断言）。

## 6. 包划分

`row`（行与复合键比较）、`cursor`（编解码与校验）、`page`（并发数据集与
正反向取页、比较计数）、`audit`（不重不漏自检）、`cmd/demo`（验收演示）。
