# DESIGN

列式存储段：编码 + 谓词下推扫描。五个包单向依赖：`bitpack`、`dict`、`zone`
不依赖任何包；`segment` 依赖三者；`scan` 依赖 `segment`、`zone`。段字节格式为
自定义定长头 + 长度前缀块（不用 gob/json，不碰真实文件系统，只在进程内存传递）。

## 1. 空值与 0 / 空串的三态区分（语义 2）

编码层不用哨兵值，而用一张**空值位图**（每个行组一个 bitpack 宽度 1 的位图），
另存非空值的紧密数组。读出结果是 `(value int64, valid bool)`（字符串列为
`(s string, present bool)`）：`valid=false` 即空；`valid=true, value=0` 是真实
零值；字符串列 `present=true && s==""` 是空串。三者由两个独立维度区分。

不能用哨兵（如 math.MinInt64 代空）的原因：int64 值域是满的，任何哨兵都是
可能出现的真实数据，往返必然碰撞；且哨兵会污染统计。统计（`zone.Stats`）在
逐行构建时只对 `valid` 的行更新 min/max、累加非空值；空值只计入 `Nulls`。
全空行组的 `HasMinMax=false`，绝不是 0。若把空值当 0 计入 min/max，例如
全空行组会得到 min=max=0，谓词 `=0` 的裁剪判定会把它判为"可能命中"而无法
跳过（漏排除，方向尚安全）；真实列若全为正数，假 min=0 会让区间谓词错误地
贴到数据下界；而用"越界"哨兵代空则会让本该保留的行组被排除（误杀）。所以
空值必须完全游离于 min/max 之外。

## 2. 裁剪判定式与"只漏排、不误排"（语义 3、4）

设行组统计区间 `[lo,hi]`（仅非空行），谓词是 int64 上的射线/点/有限集。判定
规则：**存在满足谓词的值落在 [lo,hi] 内（或组内可能有空值且谓词为 IS NULL）
时才保留**。各判定式（`zone.Stats` 的判定方法）：

- `=v`：保留 ⇔ lo ≤ v ≤ hi（等于 lo 或 hi 必须保留，端点值真实存在于组内）
- `< v`：保留 ⇔ lo < v；`<= v`：保留 ⇔ lo ≤ v
- `> v`：保留 ⇔ hi > v；`>= v`：保留 ⇔ hi ≥ v
- `IN(S)`：保留 ⇔ ∃v∈S，lo ≤ v ≤ hi
- `IS NULL`：保留 ⇔ Nulls>0；`IS NOT NULL`：保留 ⇔ (行数-Nulls)>0
- `AND`：每个合取支各自判定，全保留才保留

形式化论证（只漏排、不误排）：组内每个非空值 x 都满足 lo ≤ x ≤ hi。
对 `=v`，若实际有 x=v 命中，则 lo ≤ x=v ≤ hi，判定必为保留——即命中 ⇒ 保留，
其逆否命题"排除 ⇒ 无命中"成立，故永不误排。`< v` 等射线谓词同理：命中 x 满足
x<v ⇒ lo≤x<v ⇒ lo<v。判定式都是"命中条件在整个区间上的松弛"（只要求区间与
命中域相交，不要求端点真命中），所以可能在端点不真命中时保留组（漏排除），
这是安全的浪费，随后逐行解码过滤兜底。等于 min/max 不能排除，因为 min/max
是组内真实出现过的值，等值谓词恰好命中它们。AND 取下界/上界合取：x 命中合取
⇒ x 命中每一支 ⇒ 每支都保留。空值不参与任何数值谓词：行级求值先查 valid，
无效即不命中，只由 IS NULL 命中（三值逻辑的"非真即丢弃"）。

## 3. 游标编码与跨批等价（语义 5）

游标只存两个整数 `(groupIdx, rowIdx)`：下一个候选值的全局位置。扫描器每次只
在游标所指行组处惰性解码（解码计数 +1），逐行判定谓词、填满 limit 即返回并
把游标推进到下一未读行；已解码组缓存在扫描器内，续扫同组不重复解码；组内
读完或组被裁剪，游标直接把 groupIdx 加一——裁剪组根本不解码，自然"穿过"。
不重：游标单调前进，已返回位置不再访问。不漏：推进是逐行/逐组的满射，所有
未读位置按序可达。跨批等价：一次性扫描 = 批大小极大时的特例，两者都是同一
有序序列上的游标单调消费，limit 只切分长度、不改顺序与判定，故任意批大小
1..N 拼接结果与一次性结果逐行相同。

## 4. 截断点的长度校验（语义 6）

每个段/行组分四个阶段解析，顺序固定：①段头与行组表（魔数、版本、行数、
行组数、各组分界偏移，均为定宽并校验魔数/版本/偏移单调与上界）；②统计
（标志位 + 两个 zigzag varint + 空值计数，`HasMinMax=false` 时无 min/max
字节）；③空值位图（bitpack 宽度 1，精确 ceil(rows/8) 字节）；④数据块
（1 字节编码标签 + 4 字节长度 + 负载）。统一用有界读取器：任何字段读取前检查
剩余字节数，不足即返回带阶段与行组下标的错误（`group %d <stage>: unexpected
EOF`）；长度前缀声明的负载必须完整落在段内，否则按数据块截断报错。因此读到
的每个字段都有完整字节支撑，不可能读到半截值；扫描器遇解析错误不返回任何行。
遍历 0..len 全部截断点测试，并 recover 兜底证明无 panic。

## 5. 资源上限与字典降级（语义 8）

三个可配置上限：`MaxRows`、`MaxGroups`、字典 `MaxDictCard`，对应三个哨兵
错误 `ErrTooManyRows`、`ErrTooManyGroups`、`ErrDictTooLarge`，彼此可用
`errors.Is` 判定。Writer 对每次追加/封组先检查、后改状态：超限立即返回错误且
不触碰任何已写入切片（追加在检查通过后才执行），故"拒绝不改变已写入状态"。

字典降级：封一个行组时先收集去重值；若不同值数量 > `MaxDictCard`（0 表示
不限），这一组**不报错**，改用位打包（码字宽度按值全域的 bit-width 计算），
并在该组头的编码标签写 `encBitpack`；未超限的组仍写 `encDict`。同一指标
签随组持久化，读取端按标签选择解码器，故回退只影响本组布局，此前已封组的
字典块字节原封不动，往返无损。writer 记录 `DictFallbacks()` 计数以暴露降级
事件，测试断言触发过降级且该组按位打包往返正确。
