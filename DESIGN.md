# 多租户加权公平队列调度器 — 设计与推导

仅用标准库；状态在进程内存；时钟以 `submitSeq`（提交序号）这一逻辑序号注入，
因此同一份确定性任务流可逐元素复现。包划分：`task` / `tenant` / `sched` /
`admit` / `stat` / `cmd/demo`。

## 1. 虚拟时间推进：为什么是 c/权重

每个租户维护虚拟时间 `vt`，执行代价为 `c` 的任务后 `vt += c / w`，其中 `w` 为权重。
调度器始终选择 `vt` 最小的非空租户。

推导：在墙钟时间内，租户 i 完成的总代价记为 `S_i`，则其 `vt_i = S_i / w_i`
（设初值相同）。最小虚拟时间优先迫使所有活跃租户的 `vt` 相互贴近，
即 `S_i / w_i ≈ S_j / w_j`，于是 `S_i / S_j ≈ w_i / w_j`：
实际得到的处理代价与权重成正比。

若误用 `vt += c * w`，则 `vt_i ≈ S_i * w_i`，贴近条件给出
`S_i / S_j ≈ w_j / w_i`，权重越大反而拿得越少，与语义相反。

## 2. 空闲租户重新加入：为什么必须抬升 vt

设系统虚拟时间 SVT 为“全体活跃租户虚拟时间的前沿（最小值）”。
租户 X 空闲后停留在旧值 `vt_X`，而 Y 以权重 1、代价 1 连续执行 1000 个任务，
则 `vt_Y` 前进 1000，SVT 也前进 1000。

若 X 带着 `vt_X = 旧SVT` 直接入队而不抬升，它与前沿相差 1000。
此后每执行 X 一个代价 1 的任务，差距只缩小 1（同权重下），
因此 X 将**连续独占约 1000 步**才追平——空闲者反过来霸占调度器，
Y 在这期间被饿死。

正确做法：租户由空转非空（新到达或重新提交）时，
`vt = max(vt, SVT)`。这样 X 从当前前沿重新开始排队，
最多在“同 vt 并列”时按 ID 顺序得到一次执行，随即与 Y 交替推进，
不独占、Y 不饿死。

SVT 维护规则：堆非空时 SVT = 堆顶 vt；堆空时 SVT 冻结在最后值
（首个/唯一租户空转后回归时抬升到该值，等价于从 0 起步，无副作用）。

## 3. 并列打破

堆按 `(vt, tenantID)` 排序：vt 相同则 ID 字典序小者先出，全序、可复现。

## 4. 代价为 0 的任务

若 0 代价任务不推进 vt，该租户会永远停在最小 vt 上无限独占。
处理：记账时使用有效代价 `max(c, MinCost)`，`MinCost = 1e-9` 为最小计费下界；
统计的“实际执行代价”仍记真实 `c`。于是 0 代价任务也让 vt 前进一个微小量，
连续执行约 `1/MinCost` 个才使 vt 推进 1，即不会长期独占；
权重比例由真实代价统计检验，不受下界影响。

## 5. 数值选择与溢出

vt 用 `float64`（有理数会让堆比较与长期累加实现复杂，且 1e5 次调度、
代价/权重受限下 float64 尾差远低于 5% 容差）。入参校验：
权重须为有限正数且 `≤ 1e100`；代价须为有限数且 `0 ≤ c ≤ 1e100`。
为防溢出权重下限取 `1e-6`，故单步 vt 增量 `≤ 1e106`，
远低于 `math.MaxFloat64 ≈ 1.8e308`。

## 6. 入队、背压与移除

`admit` 在 `sched` 之上做每租户队列上限：超限返回 `ErrQueueFull`，
不入队、不改变任何 vt，且不影响其他租户。
权重非法返回 `ErrBadWeight`；代价非法返回 `ErrBadCost`；
对仍有排队任务的租户执行移除返回 `ErrTenantBusy`（拒绝移除，
任务去向明确：原样保留在该租户队列中；排空后才可移除）。
四类错误均可通过 `errors.Is` 区分。

## 7. 并发与确定性

调度器用一把 `sync.Mutex` 保护租户表、堆与 SVT，提交与取出串行化。
并发提交时，“提交顺序”定义为**获取入队锁的串行化顺序**，
submitSeq 由调度器在锁内分配；因此同一串行化序列必产生同一调度序列。
并发验收：10 万任务多协程提交、8 协程取出，每个任务恰好被取出一次。
确定性验收：固定的提交序列与代价，单协程跑 20 次，调度结果逐元素相同。

## 8. 复杂度

选择下一个任务 = 堆 Pop：`O(log n)`，`n` 为**非空租户数**
（空租户不入堆，故空闲租户不占选择代价）。`sched` 内置非导出比较计数器，
在堆比较键函数内自增；每次选择前清零。以 1000 租户口测，
sift 路径上界为 `4*ceil(log2(1000)) = 40` 次比较。
