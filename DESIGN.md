# 严格模式流式 Base64：规范尾部的推导

## 1. 组与有效载荷

Base64 把字节流切成 3 字节组，编码为 4 个六比特字符（A–Z a–z 0–9 + /）。
末尾不足 3 字节时用 `=` 填充，整组仍是 4 字符：

- 3 字节 → 4 字符，无填充，输出 3 字节。
- 2 字节 → `XXX=`，输出 2 字节。
- 1 字节 → `XX==`，输出 1 字节。

## 2. 非规范尾部为什么存在（推导）

六比特字符有 64 种取值，而最后一个有效字符携带的“被解码”比特数少于 6。
多出来的比特在解码时被丢弃，于是多组不同输入可能解码出同一字节串。
严格模式要求编码结果唯一，因此这些被丢弃的比特必须全为 0：

- `XX==`：第 2 个字符只有高 4 位（bit 5..2）进入输出字节，低 2 位（bit 1..0）必须为 0。
  `Q`=16=`010000`，末两位为 0，故 `QQ==` 合法；`R`=17=`010001`，末位为 1，故 `QR==` 拒绝。
- `XXX=`：第 3 个字符只有高 2 位（bit 5..4）进入输出，低 4 位（bit 3..0）必须为 0。
  故 `QUI=` 合法而 `QUJ=` 拒绝（`I`=8=`001000` 低四位为 0；`J`=9=`001001` 非 0）。

统一规则：**尾部被丢弃的比特必须全部为 0；且填充形态只能是无填充、`=`、`==`，
填充只允许出现在整个输入流的最后一个组。** 在此规则下每组长度（3/2/1）唯一确定
其规范编码，从而 `Encode` 是单射：任意字节串的编码结果唯一，严格解码恰为其逆，
非规范串（`QR==` 等）一律拒绝。

## 3. 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 标准字母表与 `=` 填充、76 列 MIME | （测试完成后回填） |
| 2 规范尾部样例 | （测试完成后回填） |
| 3 换行只在组间、仅 MIME | （测试完成后回填） |
| 4 五类错误可区分且带偏移 | （测试完成后回填） |
| 5 任意切分点一致 | （测试完成后回填） |
| 6 编码/解码往返（含 MIME） | （测试完成后回填） |
| 7 输出上限在组边界终止 | （测试完成后回填） |
| 复杂度：检查计数 == 输入字节数 | （测试完成后回填） |
