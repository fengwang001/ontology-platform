# 有预算的图遍历器 — 设计推导

范围：在给定访问预算下做广度优先遍历，预算耗尽时确定性地截断并给出可续传的续点。不实现索引、查询解析、最短路。状态全部在进程内存，仅用标准库。

## 1. 预算的计量单位：访问的节点数

本题规定预算单位是「访问的节点数」，推导如下：

- 遍历的对外承诺是「已访问序列」。以节点数计量时，截断点只取决于已访问序列的前缀长度，而前缀是 BFS 的唯一良定义输出，与图的存储形态无关。
- 若以「展开的边数」计量，截断点会随存储形态漂移：同一逻辑图，邻接表中出现重复边、自环、或边的排列不同，展开同样数量的边所覆盖的节点集合就不同。重复边被考察时消耗预算却不产生新节点，于是「预算 k」在两种存储下截在不同节点上，确定性承诺失效。边数是实现细节，节点数才是语义单位。

## 2. 截断确定性：必须固定的三处顺序

同图、同起点、同预算，两次遍历必须截在同一处且序列逐元素相同。需要固定：

1. **出边顺序**：邻接表按目标 ID 字典序排序，构造时插入排序，读取不依赖 map 迭代顺序（Go map 迭代顺序随机，是主要不确定性来源）。
2. **同层入队顺序**：发现即入队、先进先出；已发现集合只做成员判定，从不被迭代，因此 map 的随机序不会泄漏进遍历顺序。
3. **预算计量单位**：固定为访问节点数（见第 1 节），而非展开边数。

三处固定后，BFS 的「发现流」是纯函数：visited 序列 = 按访问顺序消费发现流的前 k 项，k = min(预算, 可达节点数)。

## 3. 续点必须包含什么

只记「最后一个访问的节点」无法恢复 BFS 状态：同层尚未访问的兄弟节点、以及已访问但出边尚未展开的祖先都会丢失。BFS 的完整状态是：

- **已访问集合**（按访问序）：既是输出前缀，也是「已发现」判重集合的主体；
- **待访问队列**（FIFO）：已发现、未访问的节点，顺序决定后续访问顺序；
- **展开游标队列**：已访问但出边未流完的节点及其下一条待考察边的下标。

为什么需要展开游标（资源核心）：若访问节点时把全部出边目标一次性入队，星形图（1 中心 + 1 万叶子）在预算 100 下队列峰值会达到 1 万。改为「懒展开」：发现流按需从队首游标逐条流出，待访问队列长度 <= 1，游标队列只保留已访问节点，峰值队列长度 O(预算) 而非 O(出度)。每条边最多被考察一次，游标随续点持久化，跨段不重复考察。

续点 = {已访问序列, 待访问队列, 展开游标队列}。三者都是切片（顺序确定），序列化无需遍历任何 map，编码天然确定。

## 4. 续点编码与篡改分类

编码：版本字节 + 三段长度前缀的字符串表（uvarint 长度 + 字节）+ CRC32。解码依次检查：CRC 校验（ErrChecksum）-> 结构完整（ErrIncomplete）-> 引用节点在图中存在（ErrUnknownNode）。CRC32 可检测全部单比特错误，故逐比特翻转必然落入校验失败类；另两类由截断与换图解码的定向测试覆盖。三类错误均可用 errors.Is 区分。

## 5. 分段等价与故障语义

分段续传等价于一次遍历，因为续点完整携带第 3 节的状态，恢复后发现流逐比特一致。推论：续传之间若图中删除了仍在队列（待访问或展开游标）里的节点，状态已无法忠实恢复，必须返回指名该节点的错误（walk.ErrMissingNode），不得静默跳过。预算为零直接返回输入续点（幂等）；队列与游标均空即「已完成」，再续传返回空序列而非报错。

## 6. 复杂度

- 访问节点数 = min(预算, 可达节点数)，每轮循环恰好访问一个节点；
- 峰值队列驻留 = 待访问(<=1) + 游标队列(<=已访问数) = O(预算)，星形图预算 b 下峰值 <= 4*b；
- 每条边最多考察一次：考察边数 <= 已访问节点出度之和；
- 遍历为纯函数，不持有任何遍历间状态，多协程并发续传互不串台。
