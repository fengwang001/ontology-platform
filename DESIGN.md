1. 行末空格/制表符：解码器保留普通空白，只删除 `=\r\n`，且严格解码拒绝行末未转义空白；因此空白后若紧跟硬换行或输入结束，必须编码为 `=20`/`=09`，否则空白会丢失或被严格模式拒绝。
2. 软换行前的空格仍在物理行末，必须写成 `=20`。若保留原空格再写 `=\r\n`，解码器删除软换行后会保留该空格，却无法区分它是否原本处于逻辑行末；严格解码器也会拒绝该物理行末空白。
3. `=XX` 占三列且是一个原子 token。若当前列加三列超过 76，先输出 `=\r\n`，再输出三列 token；软换行的 `=` 计入 76 列，因此 74 列后不能直接放 `=3D`。
4. 编码把输入 `\n` 和 `\r\n` 都规范化为硬换行 `\r\n`；单独 `\r` 是普通字节，编码为 `=0D`，所以不会与换行混淆。
5. 准确往返式：`D(E(x)) = normalize(x)`，其中 `normalize` 只把每个独立 `\n` 或 `\r\n` 替换为 `\r\n`，其他字节保持不变。
6. 编码器对非空白只转义 `=` 及不可打印字节；对空白仅在其后是硬换行、软换行行界或输入结束时转义；可打印普通字符永不转义，因此是最小转义。严格可解码的 y 中多余 `=XX` 解码后再编码会规范成原样可打印形式，差异只源于这些多余转义。
7. 语义测试对照（待测试实现后补齐函数名）。
