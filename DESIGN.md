# 排序合并连接：溢出与回退执行器设计

仅标准库。内连接，两路输入均已按连接键升序排列，键相等（含合法空串）。

## 1. 主循环
- 每次取左右当前组（同键所有行，到达序保留）。
- `lk<rk` 推进左；`lk>rk` 推进右；相等则产出笛卡尔积后两侧同时推进。
- 推进时若 `key < 前一key`，报告第一处逆序位置（流名、行序号、前后键）。
- 输出顺序：键升序；同键内按左行到达序优先，再右行到达序：
  对每个左行依次配全部右行。输入确定则输出逐字节确定。

## 2. 组起点的两种表示（核心）
右侧同键多行：每处理一个左行，右组须从起点重扫。
- **在内存**：起点 = 切片下标 0，重扫 = 下标重置为 0。
- **已溢出**：起点 = 文件内行区的**字节偏移**（固定头之后），
  重扫 = `Seek(偏移, 0)` 后顺序读。行是变长，**禁止用行号乘固定长度**；
  只能在打开/首读时记录真实偏移，否则后半组错位、读出半截行。
左侧同键多行顺序天然保留在内存左组中。

## 3. 溢出文件格式（自描述）
```
头 32B: magic "ONTSPILL"(8) ver(1) reserved(7) nframes uint64 BE(8)
        dataLen uint64 BE(8) 头CRC32 IEEE uint32 BE(4)
行帧 ×n: len(uint32 BE) + row 编码(见 row 包)
尾 4B : 组CRC32 IEEE（覆盖全部行帧字节）
```
组在阈值内：驻留内存；首行使计数超阈值即建临时文件逐帧流式写出，
写完覆盖头部填 nframes/dataLen/CRC。

## 4. 资源与推进复杂度
- 驻留峰值：超阈值后内存只留"当前读/写窗口"（0 行常驻），峰值 ≤ 阈值+1。
- 推进计数 `readRows = 两侧首读行数 + 重扫读出行数`。
  首读恰好 = `L + R`（每行从源流取一次，绝不全表重扫）。
  重扫发生在键 k 匹配时：每个左行重扫该右组一次，
  故 `重扫 = Σ_k |Lk|·|Rk|`，即"每个左行 × 其匹配到的右组大小"。
  输出行数 = 同一求和。

## 5. 并发与状态
溢出写与重扫可能并发。分组有显式状态 `writing → ready`，
由 mutex + channel 保护：重扫先 `Wait()` 阻塞至 `Finish` 完成，
禁止 sleep/轮询读到半成品；就绪后可并发只读重扫。

## 6. 损坏分类（截断点按剩余字节判定）
文件原长 H+D+T（H=32 头，D=帧数据，T=4 尾CRC）。
- `<H`：头不完整（或头CRC坏）→ ErrCorruptHeader。
- 落在帧区长度前缀 4 字节窗口内：ErrTruncLenPrefix。
- 帧体内（含进入尾CRC 4 字节窗口）：ErrTruncBody。
- 恰在帧边界且尾CRC缺失/不符：ErrCRCMismatch。
按帧顺序解码，先成帧先恢复；半截行一律不返回（最大可恢复前缀）。
四类错误 + 流错误 ErrStream + Seek错误 ErrSeekFail 均用 `errors.Is` 判定。
损坏/Seek 失败：连接中止报错，不产出残缺结果；流中途出错：
已输出结果保留，错误中带已消费到的键。
