# DESIGN.md — 统一格式 diff/patch 设计推导

## 1. 零计数 hunk 头的行号规则（第二节第 4 条）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0`；`a\n`→空 得 `+0,0`。

推导：计数 ≥1 时 `a` 是区间首行的 1-based 行号，区间覆盖 `a..a+b-1`。
计数为 0 时区间为空，没有"首行"可言。样例 1 中插入点在第 1 行之前，
若 a 表示"插入位置"应写 1，实际写 0；样例 2 中插入点在第 2、3 行之间，
若 a 表示插入位置（插入后新行占据的行号）应写 3，实际写 2。两个样例
共同表明：**b=0 时 a = hunk 之前已消耗的旧行数，即插入点前一行的行号**
（文件开头为 0），而不是将要插入的位置。`+0,0` 同理（新文件一侧
一行都没有，前一"行"是 0）。这也与 b≥1 的规则自洽：空区间位于第 a 行
之后、第 a+1 行之前，用前一行的行号指代。

Apply 的解释：b=0 时插入点 = 第 a 行之后（0-based 下标即 a）；
b≥1 时替换第 a..a+b-1 行（0-based 起点 a-1）。

| 情形 | `-a` 中 a 的写法 | Apply 对插入点的解释 |
|---|---|---|
| b=0 | a = 前一旧行号（hunk 前已消耗旧行数） | 在第 a 行之后插入（0-based 下标 a） |
| b=1 | a = 该行 1-based 行号，省略 `,1` | 替换第 a 行 |
| b≥2 | a = 区间首行 1-based 行号，写 `-a,b` | 替换第 a..a+b-1 行 |

## 2. hunk 合并阈值（第二节第 5 条）

结论：**阈值 = 2C**，即间隔 g ≤ 2C 合并，g ≥ 2C+1 分开。

推导：每个 hunk 向改动两侧各延伸 C 行上下文。两段改动间隔 g 行时，
前一段的尾部上下文占 min(C,g) 行，后一段的头部上下文占 min(C,g) 行。
g ≤ 2C 时两段上下文相接或重叠，中间没有任何一行"不属于任一 hunk"，
GNU diff 将其并为一段；g = 2C+1 时恰有 1 行谁都覆盖不到，必须分开。
反例：若阈值取 C，C=1、g=2（第 1、4 行改动）时 GNU 输出单个
`@@ -1,5 +1,5 @@`（上下文 2+2 行恰好相接），取 C 会错误拆开；
若阈值取 2C+1，C=1、g=3（第 1、5 行改动，共 6 行）时 GNU 输出两段
（第 3 行不在任何上下文中），取 2C+1 会错误合并。故只能是 2C。

## 3. 最短脚本的并列选择（第二节第 3 条）

Myers 贪心推进中，当 `V[k-1] == V[k+1]`（删除与插入收益相同）时
**固定选择删除**（沿对角线右移，即取 `x = V[k-1]+1`），回溯时用同一
比较规则，结果确定。效果：一个改动块内所有 `-` 行排在 `+` 行之前。
例：`a b`→`b a` 产出 `-a`、` b`、`+a`（与 GNU diff 一致），
而非 `+b`、` a`、`-b`。

## 4. 偏移应用后后续 hunk 的查找起点（第三节第 4 条）

要平移。维护位移 `delta = 本 hunk 实际应用位置 - 记录位置`，下一个
hunk 的查找基准 = 其记录旧行位置 + delta（"最近距离"相对该基准度量，
F 行窗口也以基准为中心）。理由：偏移几乎总是由补丁生成后、目标文本
在补丁区域之前增删了内容造成的，这一增删对其后所有行产生**恒定**
位移，因此前一个 hunk 实测到的位移是后一个 hunk 位置的最佳预测；
不跟着平移会把 F 浪费在已经不可能的位置上，甚至把 hunk 应用到
偏移前的错误候选上。
