# 严格模式流式 Base64 设计

## 规范尾部推导

每个 Base64 四元组承载 24 bit。完整组中 4 个 6 bit 值全部由 3 个输入字节确定。

- 1 个输入字节只有 8 bit：第 1 个字符使用高 6 bit；第 2 个字符的最高 2 bit 使用剩余 2 bit，其低 4 bit 没有信息，编码时必须为 0，随后是 `==`。
- 2 个输入字节共有 16 bit：第 1、2 个字符使用前 12 bit；第 3 个字符的最高 4 bit 使用剩余 4 bit，其低 2 bit 没有信息，编码时必须为 0，随后是 `=`。

因此，`QQ==` 中第二个 `Q` 的值为 `010000`，低 4 bit 为 0；`QR==` 中第二个 `R` 的值为 `010001`，低 4 bit 为 1，这 1 bit 不来自任何输入字节，必须拒绝。同理，`QUI=` 的第三个 `I` 为 `001000`，低 2 bit 为 0；`QUJ=` 的第三个 `J` 为 `001001`，低 2 bit 为 1。

统一规则是：按填充数缩短最后一组后，所有“不承载输入字节”的尾部位必须全部为零；并且填充模式只能是无填充、一个 `=` 或两个 `=`。该规则使每组严格合法文本都恰好是编码器对某个唯一字节串的输出，故任意字节串的编码结果唯一，严格解码与严格编码互为逆操作。
