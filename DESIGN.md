# 带背压的流式聚合管线 — 设计推导

## 链路与包划分

source（可注入数据源）→ parse（字节→记录）→ 聚合分组（pipeline 内）→
sink（临时文件+原子改名+校验和）。stage 提供各阶段通用骨架：有界 channel 队列、
满则阻塞（背压）、优雅停止；ckpt 负责检查点双文件轮换与恢复。

## 核心推导：检查点的「已消费位置」记到哪

管线在任意时刻把记录分成四段：

1. 已被 source 读出；
2. 在某级有界队列里在途；
3. 已被聚合进内存分组但未落盘；
4. 已被 sink 原子改名、带校验和完整落盘。

两种朴素定义都错：

- 记 source 读取位置（段1之前）：段2、3 的记录崩溃后既不会重放也不在输出里 → **丢**。
- 记 sink 落盘位置（段4之前）：恢复时从该位置重放，但段2、3 的记录中可能已有
  部分与落盘内容重叠，且聚合中间态丢失 → **重**（且聚合值翻倍）。

正确定义：**已消费位置 = 某条屏障（barrier）记录的位置，且该屏障之前的所有记录
已经 sink 原子落盘；此刻 source→sink 之间的在途集合为空，source 位置 == sink 位置。**

实现手段是周期性屏障：

1. source 每 N 条注入一条带序号的 barrier；barrier 随记录流顺序流过全部队列，
   天然「排在它之前的记录都已处理完」；
2. barrier 到达聚合器：对当前各组的中间聚合做深拷贝快照；
3. barrier 到达 sink：把当前完整输出写临时文件、fsync、原子改名（内容带校验和，
   旧完整文件被原子替换），随后把「屏障位置 + 分组快照 + 输出记录数」写入检查点。

关键顺序：**先原子改名输出，再写检查点**（两步各自原子）。
崩溃落在两步之间：输出已含 B，检查点还停在 A。恢复时读出输出尾部的记录数，
重放区间 `(A.pos, 输出条数]` 即可，屏障机制保证此区间与上次重复执行结果幂等，
逐字节一致。反之先写检查点会让「检查点声称 B、输出只有 A」无法弥补地丢数据。

恢复算法：删除所有 `*.tmp`（未改名即未完成）→ 取最近两个检查点中校验和最新的
完好文件（损坏则回退并标记）→ 校验输出完整性，以输出实际记录数为准重置 source
偏移与分组快照（检查点分组仅用于审计对齐）。

## 背压与资源约束

- 每级 stage 用容量 C 的有界 channel；Push 满即阻塞，source 因而被阻塞，
  阻塞次数在 source 侧计数。
- 在途数 = 各 stage `len(ch)+(worker 忙?1:0)`；全局最大在途 ≤ Σ(C+1)。
- 分组数达硬上限：聚合器返回可判定错误 `ErrTooManyGroups`，管线停止。

## 故障与停止语义

坏记录（无 `=`、键缺失、值非负整数失败）计数后跳过；source 报错则已处理部分
先在最近屏障落盘再上报错误；Stop 幂等，排空在途记录后关闭各 stage，不留 goroutine。

## 边界

零记录、单记录、全同组、队列容量 1、空串键合法、sink/source 立即报错、
启动前 Stop，均有表驱动覆盖。
