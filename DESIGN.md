# 设计说明：带转义的 RLE 编解码器

不变量：严格解码器接受的每个 t 都满足 Encode(Decode(t)) == t（合法编码唯一）。

## 拒绝清单推导（第三节第 2 条）

1. 显式次数 1（`1a`）：Decode 得 `a`，Encode 得 `a` ≠ `1a`。拒绝。
2. 次数 0（`0a`）：0 次游程解码为空串，Encode 得空串 ≠ `0a`。拒绝。
3. 前导零（`01a`）：Decode 得 `a`，Encode 得 `a` ≠ `01a`。拒绝。
4. 相邻同符号（`2a3a`、`aa`）：Decode 得 `aaaaa` 或 `aa`，Encode 合并最长游程得 `5a` 或 `2a` ≠ 原串。拒绝。
5. `\` 后非数字非反斜杠（`\a`）：Encode 对普通码点原样输出，永不产生 `\a`。拒绝。
6. 末尾孤立 `\`：转义缺目标，Encode 永不以 `\` 结尾。拒绝。
7. 末尾只有次数（`12`）：次数后必有符号，Encode 永不产生。拒绝。
8. 非法 UTF-8：输出须为合法字符串，Encode 只重写合法码点，永不产生。拒绝。

每类对应一个哨兵错误（rle.Err*，即 Kind 常量，errors.Is 可判定），
*rle.Error 携带 Kind 与字节偏移 Offset，彼此可区分。

## 大次数取舍（第三节第 3 条）

选择**支持**任意大次数：次数以十进制字符串累积、math/big.Int 解析
（runs.ParseDecimal），不会整数溢出。解码前不按次数分配内存：游程按
码点逐个写入输出，额外内存 O(1)。输出总字节上限可配置
（NewDecoder(limit)；Decode 用 DefaultLimit），超限返回
ErrTooLarge，属可判定错误。

## 唯一性前提与流式

Encode 经 runs.All 合并最长游程，不做 Unicode 规范化
（é 与 e+U+0301 是不同码点序列）。解码器是逐字节状态机，每字节
恰好检查一次（计数器 inspected 佐证），与切分方式无关；`2a|3a` 这类
相邻同符号靠跨 Write 保留的上一符号状态在第二游程符号处报错。

## 测试对照表

（待补）
