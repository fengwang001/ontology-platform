# DESIGN.md — 自定 LZ77 流式压缩

## 流格式（全部变长整数：LEB128 无符号，≤10 字节）

| 记录 | 标签 | 载荷布局 | 说明 |
|---|---|---|---|
| 流头 | 无 | magic `OLZ` + ver=1（定长 4 字节） | 流必须以它开头 |
| 字面量 | `0` | tag(1B) + len(varint) + len 字节 | len≥1 |
| 回指 | `1` | tag(1B) + dist(varint) + len(varint) | dist≥1；len 可 > dist（重叠） |
| 刷新 | `2` | tag(1B) | 仅边界提示，无数据语义 |
| 流尾 | `3` | tag(1B) + totalLen(varint) + checksum(varint) | FNV-1a-64(原文)；其后禁止再有字节 |

标签字节固定为 0/1/2/3，使逐字节状态机解析无歧义。所有错误携带流内字节偏移。

## 第三节：四处推导

1. **与 Write 切法无关**：Write 到达时无法预知后续字节，立刻发匹配会把恰好落在边界的长匹配截断。压缩器把自上次 Flush 以来收到的全部字节留在未决缓冲，延迟决定：匹配只在“下一位置无法继续延伸”时才确定结束，该判定只依赖输入字节本身，不依赖到达时刻，故任何切法产出同一令牌流；本实现在 Flush/Close 前缓存全部未决输入，再做一次确定的贪心扫描。代价是未决输入驻留内存。Flush 强制结束当前未决匹配：立即编码全部缓冲并补发刷新标记，使解压器此刻已能还原全部输入；Flush 不清窗口，后续匹配仍可跨过 Flush 点回指。连续 Flush 无新输入 => 无令牌；仅在本次确实编码出数据时写刷新标记，第二次零字节。

2. **重叠回指复制**：`dist<len` 时 `dst[i]=src[i-dist]` 逐字节向前；一次性块复制 `copy(dst, src)` 会复制“旧的”历史缓冲，重复图案只得到 dist 个模式字节而非展开 len 字节（如 dist=1 时结果只重复一次）。解压器采用逐字节循环；每字节一次索引写环形缓冲，O(输出) 无额外放大，故逐字节循环可接受。

3. **两类距离错误**：“超过已输出”是时序错误（引用了还没产生的字节，任何窗口容量下都不可能合法）；“超过窗口容量”是策略错误（字节已产生，但已滑出双方约定容量）。压缩器只在环形窗口保留的最近 W 字节中发候选，距离恒 ≤ W，故绝不可能产生后者；先判已输出、再判容量，二者可区分。并行块预置字典时，块 i 把上一块末尾 W 字节以预置历史装入窗口；在统一流里这些字节就是此前已输出字节，解压器“已输出计数”从流头累计，含义不变，无需特殊通道。

4. **并行确定性**：块 i 的匹配只看本块原文与块 i-1 原文（预置字典），不看任何块的压缩输出；各块使用相同确定性哈希链、无随机、无共享可变状态，故 worker 数与完成顺序不影响令牌。衔接不用刷新标记：各块令牌顺序拼接，回指天然跨块边界引用上一块末尾；只写一次流头，全部块完成后写一次流尾（总长、整文 FNV-1a 校验和），输出即普通合法流，流式解压器无感知。

## 复杂度与防护
哈希链每位置最多考察 chainLimit 个候选 => 候选总数 ≤ chainLimit × 输入字节；候选计数为 `match` 包非导出计数器。解码在任何实际写出前检查输出上限，超限即终态。
