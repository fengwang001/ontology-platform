# DESIGN

## 1. 零计数 hunk 头行号（推导）

unified diff 的行号始终是 **1 基的旧/新行号**；hunk 覆盖的旧行区间是
`[a, a+b-1]`。当 `b=0` 时区间为空，为让空区间有唯一坐标，采用"前一行"约定：
`a = hunk 前最后一条已存在旧行的行号`；若 hunk 在文件最开头，则没有前一行，
`a=0`。样例逐一核对：

- `x\n` → `y\nx\n`，插入点在文件开头，前面没有旧行，故 `-0,0 +1`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`，插入点在旧第 2 行之后、第 3 行之前，
  前一条旧行是第 2 行，故 `-2,0 +3`（新侧插入点恰为新第 3 行）。
- `a\n` → 空，删除唯一一行，新侧为空且在文件开头，故 `-1 +0,0`。

关键：`-0,0/-2,0` 描述的是**插入位置之前**的状态，不是"新内容将落在第几行"。
`Apply` 把该 hunk 的插入点放在旧文件第 `a` 行**之后**（a=0 即文件开头），
即槽位索引 `a`（0 基）。新侧同理：纯删除块的锚点在新第 `c` 行之后。

| 旧侧 `b` | 书写 | Apply 解释（0 基插入/锚点） |
|---|---|---|
| b=0 | `-a,0`，a=前一条旧行行号，开头为 0 | 在旧行 a 之后（槽位 a）插入 |
| b=1 | `-a`（省略 `,1`） | 替换/删除旧行 a（1 基） |
| b≥2 | `-a,b` | 覆盖旧行 a..a+b-1（1 基） |

新侧 `d` 完全对称。

## 2. hunk 合并阈值（推导）

阈值 = **2C**：两改动间未改动行数 g ≤ 2C 时合并，g > 2C 时分拆。

理由：每个 hunk 在改动两侧各取至多 C 行上下文。若合并，则后段上文与前段下文
必须首尾相接或重叠，即 `g ≤ C + C = 2C`。实测 GNU diff：C=2、g=4 得单个
`@@ -1,8 +1,8 @@`；g=5 得两个 hunk `@@ -1,4 @@` 与 `@@ -6,3 @@`。
若阈值取 C：g=4,C=2 会被错误分拆，与 GNU 矛盾；若取 2C+1：g=5,C=2
会被错误合并，也与 GNU 矛盾。C=0 时阈值 0：相邻改动之间只要有 ≥1 行未改动就
分拆，与 `diff -U0` 一致。

## 3. 并列最短路径选择（推导）

Myers 按 d（编辑数）逐层扫描 V 对角线。同层更新对角线 k 时，规则固定为：
`k==-d || (k!=d && V[k-1] < V[k+1])` 时取 k+1（向下=删除），否则取
k-1（向右=插入）。即**并列时优先删除**。故 `a b` → `b a` 输出
`-a`、` `b`、`+a`（删除成块在前），并规定同型编辑连续成块，`-` 行永远
排在对应 `+` 行之前。该选择与 GNU diff 默认输出一致，且逐字节确定。

## 4. 偏移后后续 hunk 的平移（推导）

hunk i 以偏移 +k（实际匹配旧行号 − 记录旧行号）应用后，它实际插入/删除造成
的新行数差为 `len(add)-len(del)`，记为 Δ。后续 hunk 的**上下文仍属于旧文本**，
其查找窗口中心应整体平移"已发生偏移"：新记录起点 = 原记录起点 + k，搜索半径仍
为 F（±F）。Δ 只影响最终行号渲染，不影响后续 hunk 在当前文本中的查找，因为
所有 hunk 的模式都针对原始旧行；实现上依次把已接受的累计偏移 k 作为下一片中心
修正量。多候选取 |偏移| 最小，相同取靠前。

## 5. 复杂度

Myers 为 O((N+M)D)，计数器只统计对角线前进步（含 snake 逐行比较），
每 d 层每对角线 snake 总步数 ≤ N+M，故总计数 ≤ (N+M)(D+1)；实现给 4 倍余量。
