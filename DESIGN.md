# DESIGN

## 1. 零计数 hunk 头的行号（b=0/d=0）
1 基行号 a 表示「hunk 前一条旧行」，hunk 第一行实际位置是 a+1；
b>=1 时第一行是 hunk 首行，a 即其行号；b=0 时 hunk 无旧行，a 取插入点
前一行的行号（文件开头插入则为 0）。样例：`y\nx\n` 相对 `x\n` 开头插入，
没有前一行，旧头为 `-0,0`、新头 `+1`；`a\nb\nX\nc\n` 在第 2 行后插入，
前一行是第 2 行，旧头 `-2,0`、新头 `+3`；删空 `a\n` 时新侧无前一行，
新头 `+0,0`。所以该数不是「将要插入的位置」（那是 a+1），而是 GNU diff
规定的「前一行行号」。Apply 插入点 = 0 基下标 a（行 a 之后；a=0 即开头）。

| 情形 | 旧头书写 `-a,b` | Apply 解释（0 基） |
| --- | --- | --- |
| b=0 | a=前一行行号，开头为 0；`,0` 不省略 | 在下标 a（行 a 之后）插入 |
| b=1 | a=该行行号，省略 `,1` | 从下标 a-1 起取 1 行 |
| b>=2 | a=首行行号，写 `,b` | 从下标 a-1 起取 b 行 |

## 2. hunk 合并阈值
C=上下文行数，两个 hunk 间隔 g 条未改动行，各带 C 行上下文。合并后
共享段长度为 g，要每侧仍保留 C 行上下文必须 g<=2C；g>2C 合并会吃掉
应有上下文。故阈值为 **2C**：g=2C 合并（共享段恰 2C），g=2C+1 分开
（中间留 1 行不进补丁），与 `diff -U C` 一致。取 C：g=2C 时错误分开，
多出一对 hunk 头；取 2C+1：g=2C+1 时错误合并，把本应省略的行纳入补丁。

## 3. 并列最短脚本的选择
Myers 每条 furthest-reaching 路径上，对同一对角线优先「向下（删除）」
再「向右（插入）」：实现中 k 从大到小更新，删除来源 V[k+1]+1 优先于
插入来源 V[k-1]。回溯同序：能删除就先删除。因此 `a b` 转 `b a` 生成
`-a`、` b`、`+a`（删除排在插入前），而非 `+b`、` a`、`-b`。该选择
全局固定，输出确定，重复 100 次逐字节相同。

## 4. 偏移应用与后续 hunk 的平移
每 hunk 期望中心 = 记录位置(0 基) + 累计 delta，delta=已应用 hunk 的
净行数变化（插入数-删除数）之和。记录位置基于原文件，前 hunk 偏移 +k
只代表它在当前文本错位，不改变文本行数，故 k 不累加进 delta；真实行
位移只来自已应用编辑。在 [center-F, center+F] 内枚举起点，要求上下文
与删除行逐字节精确匹配；多候选取 |start-center| 最小，相同距离取起点
较小（靠前）。最近距离>F 报「超出偏移范围」；范围内无精确匹配报
「上下文不匹配」。
