# 有预算的广度优先遍历：设计推导

## 1. 预算为什么以「访问的节点数」计量
预算刻画的是调用方愿意接收多少个结果节点，与结果序列一一对应，截断位置天然落在两个节点之间。
若以「展开的边数」计量，访问到多少节点取决于每张邻接表的形态：同样的逻辑图，把一条边存成
重复边、压缩成一条、或换一种邻接表切分方式，第 n 条边落在哪个节点上都会变；自环、重复边、
已访问边是否计入也会改变落点。于是截断点随存储形态漂移，无法保证「同图同起点同预算两次结果
逐元素相同」。故预算 = 访问的节点数，`visited = min(budget, reachable)`。

## 2. 截断确定性需要固定的三处顺序
1. 出边顺序：每个节点的出边按目标 ID 字典序（构入时排序去重）。
2. 同层内的处理顺序：按节点被首次发现的先后（BFS 入队顺序）处理。
3. 同层内的展开顺序：一层内各父节点轮流各取一条下一条未处理出边（round-robin），
   游标小的先取；这同时给出确定的发现顺序，并保证不会把高度数节点的出边一次性入队。

层间严格先进先出：当前层处理完才进入下一层。以上全部只用 slice，不依赖任何 map 迭代序。

## 3. 资源约束：边懒展开
不能把一个节点的全部出边一次性入队（星形图会瞬间入队一万条）。每个待展开节点保存一个
「帧 = (节点, 下一条出边下标)」，每轮只从该帧取一条边。设预算 B、活动层节点数 ≤ B：
每轮新增入队 ≤ 活动帧数 ≤ B，活动层规模至多 2B（帧与已入队的下一层节点合计），
故峰值队列 ≤ 2B+1；B=100 时远小于 4B=400。每条边只被其源帧取一次，考察边数
≤ 已访问节点出度之和。

## 4. 续点为什么必须包含整个队列与已访问集合
只记「最后一个访问的节点」无法恢复 BFS：同一层中排在它后面、以及下一层中已发现但尚未
访问的节点都会丢失；已访问集合丢失则续传会重复访问、无法去环。续点 State 必须包含：
- `Visited`：已发现（含已访问）节点集合，用于去环、自环、重复边；
- `Frontier`：当前层的懒展开帧 `(节点, 出边游标)`，保留同层剩余节点与各自展开进度；
- `Next`：已从当前层发现、将在下层访问的节点有序列表（发现顺序即后续访问顺序）；
- `Done`：无可展开内容且无下层节点时置真。
续传 = 用同一 State 再调用 Walk，语义与一次性大预算完全相同。

## 5. 续点编码与错误分类
编码 = `版本 | uint32(载荷长) | 载荷 | 载荷前16字节SHA-256`。载荷为长度前缀文本：
done 标志、visited 行数、frontier 帧行数（节点+游标）、next 行数。解码三类错误：
校验失败（校验和不符）、字段不完整（截断/长度非法/游标越界）、引用不存在节点
（校验完整但节点不在图中）。另用哨兵错误表达起点不存在等。已完成续点再续传返回空序列、
nil 错误（幂等）；预算 0 不改状态、返回空序列。

## 6. 分段等价与并发
`Walk` 是纯函数：入参 Graph 只读、State 按值克隆进出，计数器挂在每次调用返回的私有
State 上；不含任何包级可变状态，故多协程并发续传结果逐元素相同，`-race` 干净。
预算 a 与 n-a 两段拼接必等于一次预算 n：每段都从确定状态、按第 2 节固定顺序推进确定步数。
