# DESIGN：带替换的流式 UTF-8 ⇄ UTF-16 转码

## 1. 非法单元切分规则（推导）

解码采用「最长合法前缀」状态机：首字节 `b0` 声明长度 `L`（2/3/4），随后每到一个续字节，
先检查它是否落在「此前缀下允许的第二（第三、第四）字节区间」。关键事实是：只要当前字节
落在 `80..BF` 且满足该位置的区间约束，前缀就是某合法标量的前缀，后续字节可能救活它，
因此必须继续等待；一旦续字节 `∉ 80..BF`，或落在 `80..BF` 但违反该位置专属区间，则该字节
绝不可能救活前缀，前缀立刻终结：已吃下的字节整体算 1 个非法单元，**而当前字节原封不动
退回，从 ground 重新当首字节处理**（WHATWG 风格 reprocess）。

- `F0 90 80 41`：`F0` 的第二字节须 `90..BF`，`90` 合法；第三字节 `80` 须 `80..BF`，合法；
  然后 `41∉80..BF`，立刻终结，`F0 90 80` 整体 1 单元，`41` 重处理 → `41`：`FFFD 41`。
- `E0 80 80`：`E0` 第二字节须 `A0..BF`，`80` 立即违规 → 1 单元（仅 `E0`），`80` 重处理；
  `80` 在 ground 是独立非法首字节 → 1 单元；第二个 `80` 同理 → 共 3 个。
- `ED A0 80`：`ED` 第二字节须 `80..9F`（`A0` 会编码代理），`A0` 违规 → `ED` 1 单元，
  两个 `A0 80` 在 ground 各自非法 → 共 3。
- `C0 AF`：`C0` 第二字节须 `80..BF`（区间合法但值必 <U+0080，属非最短），实际约束在编码值
  层面：`C0/C1` 开头任何序列都非最短，故 ground 即把 `C0` 判为独立非法首字节，`AF` 再 1 个。
- `F4 90 80 80`：`F4` 第二字节须 `80..8F`（`90` 编码 >U+10FFFF），`90` 立即违规 →
  `F4` 1 单元，剩余三字节在 ground 各非法 → 4。
- `E2 82`+EOF：`E2` 的第二字节 `82∈80..BF` 合法，等待第三字节时流结束，未完成前缀只产
  **1 个**替换（截断/未完成，区别于已终结的非法）。
- `80 80`：ground 上每个续字节是独立非法首字节 → 2。

第二字节区间表（`cc` 为第二字节；续字节公共区间为 `80..BF`）：

| 首字节 | 第二字节区间 | 违规时吞掉的字节 |
|---|---|---|
| `C2..DF` | `80..BF` | 仅首字节（违规字节重处理） |
| `E0` | `A0..BF` | 仅 `E0` |
| `E1..EC` | `80..BF` | 仅首字节 |
| `ED` | `80..9F` | 仅 `ED` |
| `EE..EF` | `80..BF` | 仅首字节 |
| `F0` | `90..BF` | 仅 `F0` |
| `F1..F3` | `80..BF` | 仅首字节 |
| `F4` | `80..8F` | 仅 `F4` |
| `80..BF` | 无（ground 非法首字节） | 1 |
| `C0 C1 F5..FF` | 无（非最短/越界首字节） | 1 |

第三、四字节公共区间均 `80..BF`（`F0..F4` 的越界已全部由第二字节区间拦死）。

## 2. 缓存上限与放弃等待的时机

最长合法 UTF-8 序列 4 字节，故「等待中前缀」至多 3 字节；UTF-16 一个代码单元 2 字节，
缓存至多 1 字节。解码器内部另有 1 字节 reprocess 槽（被退回的字节），stream 层缓存硬上限
因此为 UTF-8 4 字节、UTF-16 2 字节。可以立刻判定前缀非法而不等 EOF 的时刻：读到非
`80..BF` 字节，或读到违反专属第二字节区间（`E0 A0`、`ED A0..BF`、`F0 80..8F`、
`F4 90..BF`）的字节。只有「切分点处自然结束且前缀仍合法」才需要等待下一次 Write/EOF。

## 3. par 的切点对齐

切点可在任意字节。每段开工时只做有限回看：从切点往回扫至多 3（UTF-16 为 1）字节，找
「唯一可能跨切点的未完成前缀」：最近一个首字节，其声明长度能覆盖切点，且它到切点之间
（不含切点）的每个字节都满足各位置区间（即单线程解析到切点时必然停留的 ground/等待态）。
该前缀的归并方式是**状态交接而非重扫**：第 i 段在自己的末尾切点调用 `Suspend`，把等待
中的前缀（≤3B）交给第 i+1 段，第 i+1 段以 `Adopt(prefix)` 开局，使 prefix 之后的字节立刻
进入同一段序列。这样：被交接的字节只被检查一次（第 i 段的检查次数），全任务检查次数 =
输入字节数 + 每切点至多 3 字节回看，即 `≤ n + 8·3`。

为什么非法单元个数与单线程一致：单线程语义由「ground 首字节 + 确定性状态转移」决定。
Suspend 只可能发生在 ground 或「合法前缀等待中」；非法前缀在造成它的那个字节上已当场终结，
该字节使解析器回到 ground 并被 reprocess，不可能跨过切点。例：`C3 | 80 80`，切点处 `C3`
是合法等待前缀，交给下段；下段 `80` 补全成 U+00C0（合法标量），再一个 `80` 非法 →
与单线程完全相同。切点落在非法单元中间的情形不存在：非法单元在其终结字节处即闭合。

## 4. 上限断点（第 9 条）

`Write` 内部按「标量事件」推进：只有当一个标量（或替换）已完整编码进输出缓冲后才推进
`consumed`，consumed 精确等于「已闭合事件覆盖的输入字节数」。写入前若剩余容量不足以放下
下一个标量（UTF-16 下含整个代理对 4 字节），则在该标量前停止并返回 `ErrOutputLimit`；
等待前缀中的字节尚未被任何事件覆盖，**不算已消费**，留在缓存里。调用方按 `n` 续传时，
新实例从 ground 解析同一段字节，逐标量边界衔接，拼接结果与无限额转码逐字节相同。
严格模式的非法/截断错误同样只在事件闭合时返回，已输出内容冻结，转码器进入终态。
