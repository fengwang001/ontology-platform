# DESIGN：自定 LZ77 流

## 流格式（记录布局）
魔数 `LZ77`+版本`1`；所有整数用无符号 LEB128 varint（≤10 字节，否则报超长/溢出）。

| 记录 | 字节布局 |
|---|---|
| 流头 | `"LZ77" 0x01`（固定5字节） |
| 字面量 | tag=0, varint n, n 个原始字节 |
| 回指 | tag=1, varint dist, varint length（均≥1） |
| 刷新 | tag=2，单字节，无负载 |
| 流尾 | tag=3, varint origLen, varint checksum(FNV-1a 64) |

## 1. Write 切法无关（语义3）
压缩器只对“已成定局”的数据发记录：保留至多 `maxMatch-1` 个未决尾字节。每次 Write 追加到 pending，
随后反复取起点 s：在 (窗口 ∪ pending[s:]) 中求最长匹配。关键判定：若匹配终点不是 pending 末尾，
未来字节无法再延长/改变它（更长匹配必须覆盖到末尾新字节），该匹配为最终结果；同理未匹配字节在
“其后方已有 ≥maxMatch 字节”或其后存在一个已确定的匹配时才输出。不能确定就停在 pending 末尾，
等下次 Write。发匹配时才把 [s,s+len) 插入窗口并前进；字面量则插入并前进 1 字节。
因此同一累计偏移处可见历史完全相同，输出只依赖已收到的字节流，与切分无关。
Flush/Close 需要强制收尾：把所有未决尾字节按最终结果处理（允许匹配结束于末尾），发刷新标记；
所以 Flush 必然强制结束当前可延长的匹配。窗口不随 Flush 清空，后续匹配可回指 Flush 之前。
无新输入的第二次 Flush：pending 为空且已发过刷新，输出零字节。

## 2. 重叠回指复制（语义2）
块复制（copy(dst,src)）在 dist<len 且 src/dst 重叠时按实现决定行为，不能保证重复模式。
解压器维护输出环形窗口，回指展开为 length 次 `b := window.at(outPos-dist+1); emit(b)` 的逐字节循环：
每次读的是“当前历史”，dist=1 即重复末字节、dist=3 即周期 abc。复制量有输出上限（默认 GiB 级），
逐字节循环的成本就是解压本身的 O(输出)，不改变复杂度，可接受；不做投机的整块 copy。

## 3. 两种“距离越界”（语义7）
`dist > 已输出字节数`：引用了尚不存在的数据——截断/伪造流在早期就可能出现。
`dist ≤ 已输出但 dist > 窗口容量`：引用的字节即使存在也已滑出窗口，压缩器结构上不可能产生：
编码器窗口容量固定，且并行块预置字典后候选同样被窗口容量截断；只有损坏流才会出现。二者必须分开报。
并行块：预置字典只是解码器端窗口的初始内容；对单条流式解码器而言这些字节就是“已输出”，
dist 校验相对窗口总长度，语义不变。块流里**不发刷新标记**，块边界对解码透明（刷新仅流式 API 使用）。

## 4. 并行确定性（语义9）
块 i 的字典 = 上一块末尾至多 capWin 字节（更早历史距块首已超过整个窗口容量，不可能被引用）。
块 i 的匹配器初始窗口=该字典，压缩输入=块 i 原文，只依赖 (块 i-1 原文, 块 i 原文)，
与其他块、goroutine 调度、workers 数无关；workers 只改变调度。各块输出按索引顺序拼接：
先统一写一个流头，各块输出各自的字面量/回指记录（块内不带头尾、不发刷新），最后统一写一个流尾，
总长与 FNV 校验和对整个原文计算。故 workers=1/2/4/8 输出逐字节一致，且可被普通流式解码器解开。

## 复杂度与防护
哈希链：表 2^16（键=3字节 FNV 风格哈希），每位置插入链头，候选沿 prev 链回溯，至多 chainLimit 个；
非导出计数器 `examined` 每考察一个候选加一。全同字节下同哈希，每位置恰好 ≤ chainLimit 次，
每字节考察数不随窗口/输入规模增长。解码器：每个 varint 限 10 字节且第10字节≤1（溢出另报）；
回指先算“展开后总长”并与输出上限比较，超限直接进入终态错误，不写任何字节；终态后写入返回同一错误。
