# 最短往返十进制编解码设计

## 1. 问题与判据

给定 float64 `x`（有限值），求最短十进制文本 `s`，使得「按 IEEE 754 最近舍入（round-to-even）把 `s` 解回 float64」逐位等于 `x`。

- 17 位有效数字一定够：double 尾数 53 bit，`10^17 > 2^53`，任意 double 都可由 17 位十进制有效数字唯一区分；但多数值用更少位即可，故 17 位不是最短。
- 15 位不一定够：`10^15 < 2^53`，存在相邻 double 共享同一个 15 位舍入值，例如 `1/3` 的 double 需要 17 位。
- 正确判据：从 `k=1` 起逐位增加有效位数，每次用精确算术检查「`k` 位最近十进制值解回是否逐位等于 `x`」，第一个通过的 `k` 即最短。检查次数被计数，对 `0.1` 不超过 3 次。

## 2. 精确算术做法（不用浮点回代）

全部用 `math/big` 的有理数，无任何浮点中间运算：

- `dec.Exact(x)`：由 `math.Float64bits` 拆出符号、阶码、尾数，构造精确有理数 `m * 2^e`（`big.Rat`，`m` 为尾数整数，`e` 可负，次正规 `e = -1074`）。
- `dec.Candidate(v, k)`：十进制指数 `q = floor(log10(v))`（用高精度 `big.Float` 估算再校正），计算 `n = round(v / 10^(q-k+1))`，舍入用「比较 2 倍余数与除数」的整数比较，平局取偶；`n` 进位到 `10^k` 时 `q` 加一重算。候选值即 `n * 10^(q-k+1)`。
- 往返检查：把候选有理数 `r` 用 `nearestFloat(r)` 精确舍入到 float64，比对 bits。`nearestFloat` 先用高精度 `big.Float` 取近似，再枚举候选及其相邻值，用精确中点 `(lo+hi)/2` 的 `big.Rat` 比较决定归属，平局取偶；上溢得 ±Inf，下溢得 ±0。
- 浮点回代（如 `n*10^p` 的 float64 连乘）会二次舍入，把本不能往返的 16 位值误判为可往返；本实现全程有理数，杜绝该误差。

## 3. 文本组装规则（fmtf）

- 记候选为 `n * 10^(q-k+1)`，`n` 为 `k` 位有效数字，`q` 为十进制指数。
- 定点/指数选择（确定且可复现）：`-4 <= q <= 20` 用定点形式，否则用指数形式 `d[.ddd]e±q`（指数至少两位数字，与标准库风格一致）。
- 定点：按 `q` 与 `k` 的关系补零或插小数点，不输出多余字符。
- `+0.0`/`-0.0` 分别编成 `0` / `-0`；NaN 与 ±Inf 返回 `ErrNonFinite`，不静默给字符串：若允许 `NaN`/`Inf` 文本，调用方会误以为它们是合法数值数据，且 NaN 不等于自身的语义会在后续比较中埋雷，故显式报错。

## 4. 解析规则（parse）

- 语法：`[+-]? ( digits [. digits?] | . digits ) ( [eE] [+-]? digits )?`，前导 `+` 允许。
- 解析出整数 `n` 与指数 `p` 后，构造精确有理数 `n * 10^p`，复用 `dec` 的精确舍入；`-0` 保留符号位。
- 错误分类（`errors.Is` 可判定）：`ErrEmpty`（空串）、`ErrSyntax`（非法字符，错误信息含字节位置）、`ErrExpOverflow`（指数绝对值 > 10000）、`ErrTooManyDigits`（有效数字 > 1000，超出可精确往返的合理规模）。
