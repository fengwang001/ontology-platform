# DESIGN

## 1. 空行与单列空值
解析在词法层做**空记录抑制**：一条仅由行尾组成、且从未产生过字段的记录不触发回调。因此空行（`\n\n`、文件以 `\n` 结尾）被跳过，不产生记录。这使「文件末尾换行」天然不产生额外空记录。但 `a\n\nb\n` 里的空行同样被跳过——它无法与单列表里「值为空的记录」区分，故单列空值必须显式加引号：回写器遇到单列记录的唯一字段为空时写 `""`。不加引号时 `Write` 出来是空行，再解析被抑制，记录丢失，往返失败；写成 `""` 则产生一个 quoted 空字段，逐字段与引号标记均还原。

## 2. par 切点
每段 worker 不知道起点是否在引号内，故**双假设**各跑一遍：H0=起点在引号外（正常 FSTART）；H1=起点在引号内（QFIELD，且 H1 仅在前一段末尾为 `\n` 时才可能成立，因为引号不能跨行尾外开启）。每段还需知道上一段是否以 `\r` 结束：若是，则本段首字节必须是 `\n`，否则前一段在该 `\r` 处即错（CR 不跨段延判，见 §3）。
左到右确定：段 i 的真实状态由段 i-1 的选定结果末状态决定（段 0 恒为 FSTART）。段 i-1 选定为 FSTART 则段 i 取 H0；为 QFIELD 取 H1。段间不完整记录拼接：前段若停在引号字段或未引号字段（非 CR、非行尾）中，其后段选定结果的首个字段即为同一字段的延续；拼接时丢弃前段该未闭合字段（其值只用于上限/错误判定，不进结果），采用后段版本——偏移区间覆盖整段，值一致。
错误换算：段内错误带段内字节偏移 b、段内记录号 r、字段号 f；全局偏移=段起点+b；全局记录号=之前各段选定结果中完整记录条数之和 + r（抑制空记录不计）；字段号不变（字段不跨「记录」界，只跨段界）。每段至多两遍，故总处理 ≤2N+O(K)，与引号字段长度无关。

## 3. CR 待定
FFIELD 中见 `\r`：不立即判错也不立即提交行尾，进入 CR 状态。续传时：下一字节是 `\n` → 行结束（行尾算两字节，但不产生 `\r` 字符）；否则在该 `\r` 处报 ErrBareCR，位置记 `\r` 自己。QFIELD 中的 `\r` 是普通内容字节；其后若遇 `\n` 仍是引号内 CRLF 内容，**原样保留**两字节。`Close` 时处于 CR 状态（流在 `\r` 处结束）→ ErrBareCR；处于 QFIELD → ErrUnclosedQuote。par 中 `\r` 恰好为段末：不延判，直接在该偏移报 ErrBareCR（`\n` 不可能属于另一段后再改变判定——实际可能，故特例：段末 `\r` 且非末段时，由拼接层查后段首字节，为 `\n` 则两字节并作行尾，否则错）。

## 4. 上限「立刻」
字段字节计数在字节被接受进字段值的瞬间递增：未引号字段每个普通字节 +1；引号字段中，普通字节 +1，`""` 转义对在见到第二个 `"` 时 +1（一个引号字符、一个字节的输出值）。计数到达上限后下一字节即返回 ErrFieldTooLong（位置为该超限字节；在 QQUOTE 见第二个 `"` 且超限时位置为该 `"`）。不等记录结束、不先缓冲。字段数每开启一个新字段计数并立即对 ErrTooManyFields；记录数每提交一条记录立即对 ErrTooManyRecords。出错即终态，记录已提交部分保留，之后 Feed 恒返回同一错误。

## 5. 规范输入（canonical）
定义为：记录间恰好一个 `\n`；文件不以换行结尾；需要引号的字段一律用引号且内部引号写 `""`；不需要引号的字段一律裸写；单列空值写 `""`。此形式下 `Write(Parse(x))==x` 逐字节成立。回写器必要引号情形：含 `,`、`"`、`\r`、`\n`，及单列唯一空字段；其余（含多列空字段）裸写。

## 6. 状态转移表
字节类别：`c`=普通(非 `,"\r\n`)；`,` `"` `\r` `\n`；EOF 由 Close 处理。

| 状态 | `,` | `"` | `\r` | `\n` | `c` | Close/EOF |
|---|---|---|---|---|---|---|
| FSTART | emit空字段→FSTART | →QFIELD | →CR | 提交记录(空则抑制) | →FFIELD | 无字段:结束; 否则提交单字段记录 |
| FFIELD | emit字段→FSTART | ErrBareQuote | →CR | 提交记录→FSTART | 累加 | 提交当前记录 |
| QFIELD | 累加 | →QQUOTE | 累加CR | 累加LF | 累加 | ErrUnclosedQuote |
| QQUOTE | emit字段→FSTART | 累加`"`→QFIELD | ErrAfterQuote | ErrAfterQuote(行尾) | ErrAfterQuote | 提交记录 |
| CR | (不会先于\n: 非\n已错) | ErrBareCR | ErrBareCR | 提交记录→FSTART | ErrBareCR | ErrBareCR |

注：CR 状态见任意非 `\n` 字节（含 `,` `"` `c` `\r`）均在原 `\r` 偏移报 ErrBareCR。
错误四类：ErrBareQuote（裸 `"`）、ErrAfterQuote（闭引号后杂质，如 `"ab"c`）、ErrUnclosedQuote、ErrColumnCount（table 层，记录列数≠首记录）。
词法器对每个输入字节恰好处理一次（无回扫），计数器 `n` 导出测试用。
