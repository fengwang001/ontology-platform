# DESIGN.md — 消息头部集合：解析 / 规范化 / 安全回写

仅标准库，进程内存。依赖单向：`token` → `fold` / `listval` / `policy` → `headers`。

## 1. 名字与冒号之间的空白为何必须判错（语义第 3 条）

若宽容地把 `Name : value` 当作 `Name: value`，解析层会把"名字中的尾随空白"吞掉，
而不同中继/网关可能选择不吞。于是同一字节流经两个实现产生不同的头部集合：

- 前端宽容解析看到 `Name`，把它当作可信策略白名单成员放行；
- 后端严格解析（或按字面 `Name ` 建索引）看不到该白名单名，策略落空；
- 攻击者还可用 `Name : value` 与 `Name: value` 两个名字在去重/合并阶段造成
  "一个被策略处理、另一个绕过策略直连后端"的分歧（header smuggling）。

宽容等于把一个字节位置的解释权交给链路里的每一跳。因此语法直接拒绝，
不产生"规范化后看起来无害"的假象。续行（行首空白）仍合法，但首行不得是续行。

## 2. 注入防御：禁止字符集合与"拒绝优于删除"（语义第 4 条）

禁止集合（值字节）：

- `\r`(0x0D)、`\n`(0x0A)：物理行分隔符，出现即可制造额外头部行；
- `NUL`(0x00) 与其余 C0 控制字符（0x00–0x08、0x0B、0x0C、0x0E–0x1F）：
  RFC 7230 字段值只允许 VCHAR / SP / HTAB / obs-text，控制字符一律非法，
  且 NUL 在 C 风格后端会截断比较，属于经典伪装面；
- 0x7F（DEL）一并拒绝（控制字符）。

为什么"静默删除"更危险：删除会改变语义却不报错。`a\r\nEvil: 1` 被删成 `aEvil: 1`
后，调用方拿到成功，以为值按原样写出；审计、签名、长度统计全部基于另一份数据。
而"拒绝"是可判定的：写入操作返回错误，集合状态不变，回写路径永远拿不到脏值。

编码绕过：解析器看到的是原始字节，百分号编码只有在某个"下游再解码一层"时才变成
换行。为防多跳分歧，除原始控制字符外，另拦截"解码后为 CR/LF/NUL"的常见编码：
`%0d %0a %0d%0a %00`（大小写不敏感）以及反斜杠转义 `\r \n \0`，命中即拒绝。
拦截点统一在 `token.ValidateValue`，被 `Set`/`Add` 与解析路径共用，保证只有一道门。

测试的五类变体：①`a\r\nEvil: 1` ②`a\nEvil: 1` ③`a\rEvil: 1` ④含 NUL
⑤编码变体（`a%0d%0aEvil: 1`、`a\rEvil` 等），全部断言被拒且回写不含 `Evil`。

## 3. 列表型头部：引号内逗号不切分（语义第 5 条）

不实现则会切错所有带 quoted-string 参数的头部。例如：

`Warning: 299 - "hardware, disk offline"` —— 引号内逗号是文案，
切错后第二项 `disk offline"` 被当成独立警告值，参数归属与个数全错。
类似还有 `Accept: text/plain; level="1,2"` 等带引号参数的头部。

切分规则：只在**引号深度为 0** 的逗号处切分；`"` 由前导奇数个 `\` 判定是否转义
（`\"` 不切换深度）；每项首尾 OWS 去除，项内容（含引号）原样保留。
空项语义：**空项判错**（`a,,b`、`,a`、`a,` 都返回错误）——空元素在标准列表
头部中无合法语义，容忍它会让"缺值"与"空值"不可区分。
合并用 `, ` 连接；往返断言：`Split` 后 `Join` 再 `Split` 得到完全相同的元素序列。

## 4. 回写等价与幂等（语义第 6 条）

输入已规范时回写逐字节相同（原样保留：字段值不折叠、行尾 CRLF、终止 CRLF）。
输入不规范时，"正确输出"定义为确定的规范形态：

- 名字规范化：首字母及每个 `-` 后字母大写，其余小写（`cOnTeNt-TyPe`→`Content-Type`）；
- 续行并入上一行，前导空白压成一个 SP；值首尾 OWS 去除，内部 OWS 保留；
- 行尾统一 CRLF；宽度超限时在 OWS 边界折行（见 `fold.Refold`）；
- 列表型头部的值本身不重排、不去重（保序硬约束优先）。

幂等性论证：规范函数 N 是"语法糖消除"——折行展开、trim、大小写统一，
这些变换的输出中再无对应输入模式（规范值不以 SP/HTAB 开头结尾、不含 CRLF、
名字已全规范），故 N(N(x))=N(x)。用测试对规范输出再解析再回写逐字节断言。
折行往返：Refold 只在原本就是 OWS 的位置插入 CRLF+SP，Unfold 恰好把
"CRLF+前导空白"压回一个 SP，因此折行/展开互逆。

## 5. 索引结构与保序（语义第 10 条）

存储为 `[]entry` 追加切片，顺序即出现顺序，永不重排。
索引为 `map[string][]int`：键是规范化小写名，值是该名字在切片中的下标列表
（按下标递增追加，多重映射的顺序也由此保留）。删除采用"标记墓碑 + 压缩"：
压缩时按新位置重建整个索引，故索引与切片始终一致，保序不受索引更新影响。

按名查找只在目标桶内比较：先一次 map 命中（O(1) 平均），再比较桶内条目。
比较计数器只统计桶内逐条目比较；最坏上界 = 该名字的出现次数 k，与总条数 N 无关。
测试对照 N=50 与 N=5000（每名 2 次）时单次 GetAll 的比较数同为 2。

其他：截断解析到临时副本，成功才整体替换，失败零状态改变；四条上限
（条数/名长/值长/总字节）返回四个可判定的哨兵错误。并发用 `sync.RWMutex`
保护——读写都串行安全，文档与测试（`-race`）以此为准。
