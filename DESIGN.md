# CSV 方言设计

## 关键推导
1. **空行与单列空值**：两个换行之间没有字节时，无法同时表达“空行分隔”和“单字段空记录”。本实现选择**跳过空行**；因此单列表中的空记录必须写成引号空字段 `""`。若写成空字节，下一次解析只能得到空行并跳过，记录丢失。多列记录中的空字段可由相邻逗号定位，仍保持未引号。
2. **回写**：逗号、`\r`、`\n`、`"` 必须加引号；`"` 转义为 `""`；单列唯一空字段必须加引号。原字段已加引号时也必须保留引号，否则引号标记无法往返。规范输入定义为：无空行、引号字段外只用 LF、引号满足上述最小条件、非空表以 LF 结束。
3. **并行切点**：每个目标段先用两个假设（起点在引号外字段起点 / 引号字段内）跑轻量状态机，得到段尾状态、首个错误、引号外记录尾。再从左到右用真实起点选择结果，把各 worker 的实际起点回退到该目标切点前最近的记录尾。任意请求切点仍被使用；回退只改变区间，不改变字节。首个记录可能跨多个目标切点，对应 worker 得到空区间。每段因此从完整记录边界启动，普通词法分析结果直接拼接；错误偏移加段基址，记录/字段号加段前缀计数。
4. **CR 待定**：引号外见 `\r` 进入 CR 待定，下一字符必须为 LF；EOF 或其他字符是孤立 CR。续传时该状态随解析器保留。并行轻量状态也保留它，切点在 CR/LF 中间时，下一段 LF 才提交记录。引号内 CR 永远是普通内容。
5. **字段上限立即性**：未引号字节到达即计数；引号内普通字节到达即计数，转义对 `""` 只在第二个引号到达时增加一个逻辑字符。因此第一个超限逻辑字符尚未缓冲即可失败。记录字段数在每个字段开始前检查，记录数在完整记录提交前检查。

## 状态转移
| 状态 | 逗号 | LF | CR | `"` | 其他字节 |
|---|---|---|---|---|---|
| S 字段开始 | 提交空字段，S | 提交记录(空行跳过)，S | P | 进入 Q | 开始未引号字段 U |
| U 未引号字段 | 提交字段，S | 提交记录，S | P | 错误：杂散引号 | 追加并计数 |
| Q 引号字段 | 内容 | 内容 | 内容 | QE | 内容并计数 |
| QE 引号后引号 | 提交字段，S | 提交记录，S | P | 追加一个引号，Q | 错误：闭合后有字符 |
| P CR 待定 | 错误：孤立 CR | 按 U/QE 语义提交记录，S | 错误：孤立 CR | 错误：孤立 CR/闭合后字符 | 错误：孤立 CR |

EOF：Q/QE 为未闭合引号；P 为孤立 CR；S/U/QE 的活动字段按规则提交，且不额外制造记录。
