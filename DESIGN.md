推导一：执行完一个任务后键去哪

做法A（同 worker 直接续跑该键下一任务，不经过环）：
n=1 时唯一 worker 一旦开始续跑热点 H，就再也不回环取键；之后提交的冷键 C
永远等不到 worker，等待上界为 ∞（题目事故的直接成因）。n>1 且只有单热点时
其余 n-1 个 worker 可服务冷键，上界约 ceil(k/(n-1))+1，但推广到 n 个热点时
各 worker 全被粘住，冷键仍可饿死，故 A 不提供统一上界。

做法B（任务结束后一律把 H 重新放回环尾，worker 回环重新竞争）：
设冷键 C 提交时前方至多有 H 与 k-1 个冷键。环中冷键被取到即离环（只出现一次），
H 每轮恰好出现一次。C 入环后，每经过一次 H 执行，环就推进完整一轮、前方冷键
减少一批；C 最晚在第 k+1 次 H 之前被取出执行。故冷键等待上界 = k+1 个 H 任务，
与 n 无关，n=1 时同样成立。结论：采用做法B。

推导二：键何时离环、何时入环

每键三态：IDLE（不在环、无 worker）、QUEUED（在环）、RUNNING（不在环、
有且仅有一个 worker）。全部转换在同一把互斥锁下完成。
1. Submit 追加任务后按状态：IDLE→QUEUED（入环尾）；QUEUED 保持；RUNNING 保持
   （只追加，不入环）。
2. worker 从环取到键：QUEUED→RUNNING（出环），只执行该键一个任务。
3. 任务结束（含 panic；recover 在锁外执行，收尾判定回锁内）：队列空→IDLE；
   非空→QUEUED（环尾）。
不被两个 worker 取到：环成员集合中每键唯一，QUEUED→RUNNING 是持锁原子出环，
RUNNING 期间 Submit 不入环，故第二个 worker 无法再取到该键。
不丢任务：最后一个任务执行期间新任务到达只有两种交错——
(a) Submit 先于 worker 收尾临界区：worker 看到队列非空，把键放回环；
(b) Submit 晚于该临界区：worker 已定 IDLE，Submit 看到 IDLE 自行入环。
“追加任务”与“查空+定态”在同一把锁上互斥，不存在“任务已在队列而状态为 IDLE”
的中间窗口。回环责任明确：先到者由收尾 worker 负责，后到者由 Submit 负责，
二选一恰好一次。
复杂度：环用“尾切片+成员 map”实现，入环/出环均摊 O(1)，空闲键（IDLE）从不
留在环里；取键扫描最多跳过 1 个被并发抽走的槽位，即检查键数 ≤ 2。
