# 熔断 + 舱壁调用保护器：设计推导

## 0. 术语与分层

一次请求的生命周期：`总请求 → (熔断闸门) → (舱壁闸门) → 真实调用 → 成功|失败`。
调用链固定为 **熔断在前、舱壁在后、时限包最内层**，统计在每一道闸门分流记账。

## 1. 口径推导：被拒绝的调用为何不计入失败率

- 失败率的定义是「真实调用中失败所占的比例」，即 `失败数 / 真实调用数`，
  其中 `真实调用数 = 总请求数 − 熔断拒绝数 − 舱壁拒绝数`。
- 反证：若把熔断拒绝计入失败，熔断一旦打开，打开期内所有请求都被拒绝，
  失败率被自己的拒绝拉满（分子分母同步膨胀且失败率→100%），
  冷却结束进入半开的条件虽与失败率无关，但关闭期重新累积的样本会被
  「打开期的伪失败」污染，熔断将反复误开甚至永远打不开——统计不再描述
  下游健康度，而是描述熔断器自身的行为，口径失去意义。
- 因此熔断拒绝、舱壁拒绝各自单列计数，绝不进入成功/失败账本；
  由此得到三条恒等式（见 `stat.Snapshot.Check`）：
  `总 = 成功 + 失败 + 熔断拒绝 + 舱壁拒绝`，
  `真实 = 总 − 熔断拒绝 − 舱壁拒绝`，`Σ分类失败 = 失败`。

## 2. 顺序论证：熔断为何置于舱壁之前

- 熔断打开意味着下游已被判定故障，此时最优行为是 fail-fast：
  立即返回 `breaker.ErrOpen`，不占用任何并发额度、不进入等待队列。
- 若舱壁在前，被拒请求也要先排队占名额：打开期内舱壁会被注定失败的
  请求塞满，真正恢复后半开探测与正常流量反而排不上队；
  且排队等待会放大上游延迟，违背熔断「快速止损」的目的。
- 推论（测试断言）：熔断打开期间，舱壁在途数与历史峰值恒为 0。

## 3. 额度归还：四条路径一个都不能漏

- 真实调用只有四种结局：成功、失败、超时、panic。
- 额度泄漏是单调恶化的：每漏一个，可用额度永久减一，漏到 0 后舱壁
  永久全拒——比任何下游故障都糟，且无法自愈。
- 实现上获取额度后立即 `defer release()`，四条路径统一经 defer 归还；
  panic 由包装器 recover 转为错误后同样走 defer 路径。

## 4. 不可重试错误是否计入熔断：不计入

- 定义：不可重试错误是调用方缺陷（参数非法、协议错误、调用 panic），
  用相同输入重试必然同样失败，且不表征下游健康度。
- 熔断是下游保护机制，触发语义应为「下游可能病了」；把调用方缺陷计入
  会让一个坏调用方把全体调用方熔断掉，属于误伤。
- 故：可重试错误与超时计入熔断计数；不可重试错误只计入统计失败数
  （口径等式仍覆盖它），不触碰熔断状态机。

## 5. 熔断状态机语义

- 关闭→打开：连续失败数 ≥ 阈值，**或**（样本数 ≥ 下限 且 失败率 ≥ 阈值）。
- 打开→半开：注入时钟前进 ≥ 当前冷却时长。
- 半开→关闭：配置的探测次数内全部成功，冷却时长复位为基础值。
- 半开→打开：任一探测失败立即回开，冷却时长 ×2（封顶 MaxCooldown）。
- 半开并发：在途探测 ≤ 配置探测数，超出立即拒绝（`ErrOpen`）。
- 时钟回拨：`Now()` 早于上次观测值时返回 `ErrClockBackwards`，
  状态与冷却判定一律不变，杜绝回拨导致的提前半开。

## 6. 错误分类与可判定性

- 四类框架错误均可 `errors.Is` 判定：`breaker.ErrOpen`、
  `bulkhead.ErrFull`、`timeout.ErrTimeout`、`guard.ErrPanic`。
- 业务错误经 `classify.Mark` 打标；未打标的默认「可重试」，
  `context.DeadlineExceeded` 一族归为超时。
- panic 由 `guard` recover 后包装为 `ErrPanic` 并标记不可重试：
  它是调用方代码缺陷，不计入熔断（同第 4 节论证），但计入统计失败。
