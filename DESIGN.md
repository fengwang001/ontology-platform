# 多区间 Range 响应组装器 — 设计推导

工程结构：`rangespec`（词法/语法解析）← `coalesce`（归一化）← `multipart`（封装），
`source`（字节源抽象，无依赖），`serve`（组装器，依赖以上全部）。依赖方向单向。
状态全部在进程内存；只用标准库；不使用 `net/http` 的 Range 实现与 `mime/multipart`。

## 第 1 条：`bytes=-0` 为什么是不可满足而不是语法错误

- 语法层面：`-n` 写法的产生式是 `"-" 1*DIGIT`。`0` 是合法的 DIGIT 序列，
  所以 `bytes=-0` 在词法与语法上**完全成立**，判语法错误会违背文法。
- 语义层面：`-n` 的含义是"资源末尾的 n 个字节"。当 n=0 时，它指称的字节集合是
  **空集**——不是"位置非法"，而是"这个位置区间里没有任何字节可返回"。
- 空字节集合无法构成一个 206 的部分内容响应体（长度为零的区间没有意义），
  这与"起点越过资源末尾"同属一类：语法正确、但按当前资源长度**取不到任何字节**。
- 因此归一化阶段把 `-0` 判为不可满足（unsatisfiable），错误中携带资源总长，
  供调用方生成 416（`Content-Range: bytes */total`）。语法错误与不可满足是
  两个可判定的错误类型：`rangespec.SyntaxError`（带字节偏移）与
  `rangespec.UnsatisfiableError`（带总长），调用方用 `errors.As` 区分。

## 第 3 条：为什么"相邻"也必须合并

- 区间 `[0-9]` 与 `[10-19]` 不重叠，但首尾相接，覆盖的字节集合与 `[0-19]`
  **完全相同**。归一化的职责是产出规范形：凡是覆盖集合相同的区间集，
  归一化结果必须唯一，否则"归一化"这个词不成立。
- 若不合并相邻区间，会直接违反第 7 条的退化情形：客户端请求
  `bytes=0-9, 10-19`，两个区间相邻，应合并为一个 `[0-19]`；按第 7 条
  "只有一个区间时响应体是裸字节"，此时必须退化为裸字节响应。
  若只合并重叠不合并相邻，归一化结果仍是两个区间，响应会被错误地封装成
  multipart——同样的字节集合、同样的语义，却产出两种不同形态的响应，
  这正是第 7 条要求"两个区间合并成一个之后应当退化为裸字节"要堵的漏洞。
- 合并条件因此统一为：`next.Start <= cur.End + 1`（重叠或相邻都合并）。
  合并后列表两两之间既无重叠也无相邻，满足规范形。

## 第 4 条：归一化算法与比较次数上界

- 算法分三步，不做任何两两配对比较：
  1. **解析裁剪**（O(n)，0 次区间间比较）：每个 spec 独立解析成具体闭区间
     `[start, end]`：`-n` → `[max(0,total-n), total-1]`；`a-b`/`a-` 的 end
     裁到 `total-1`；起点 `>= total`、`b < a`、`-0`、空资源 → 丢弃。
     全部丢弃则返回 `UnsatisfiableError{Total}`。
  2. **按起点排序**：`sort.Slice`（pdqsort，最坏 O(n log n)），
     每次 less 调用计 1 次比较，比较次数 ≤ c·n·log2(n)，c 为小常数。
  3. **单趟扫描合并**：当前区间与下一区间只做 1 次相邻/重叠判断，
     共 n-1 次比较。
- 比较次数上界：`C(n) ≤ c·n·log2(n) + (n-1)`，即 O(n log n)，不是 O(n²)。
- 计数器是 `coalesce` 包内**非导出**的 `atomic.Int64`，经 `CompareCount()`/
  `ResetCompareCount()` 读取。测试取 n=100 与 n=10000 两组实测：
  n·log2(n) 的理论比值为 100·(log2(10000)/log2(100)) ≈ 200，
  断言实测比值 ≤ 2×200 = 400（留常数因子余量），从而排除 O(n²)
  （O(n²) 下比值应接近 100² = 10000）。

## 第 6 条：断点续写的状态与不变量

- 组装器状态只有两块：不可变的 `body []byte`（组装完成后不再修改）和
  游标 `off`（已写出字节数）。`Build` 把所有区间字节读齐、封装完毕、
  通过全部上限检查后**一次性提交**这两个字段；此前任何失败都不触碰状态，
  这也是第 9 条"拒绝不改变任何已有状态"的实现方式。
- **不变量**：任意时刻，调用方已收到的字节序列 == `body[:off]`，
  尚未写出的 == `body[off:]`。`Write(p)` 只做
  `n := copy(p, body[off:]); off += n`，返回 `n` 让调用方感知短写。
- 由不变量直接推出：无论写出被切成多少段、切在哪里，所有段的拼接
  == `body[off0:]`，与一次性写出完全相同——因为 `body` 提交后不可变，
  切分只影响 `copy` 的次数，不影响被拷贝的字节内容。测试用
  切分点 1..总长 的循环逐字节比对验证该不变量。

## 第 8 条：边界串规避策略与失败概率上界

- 策略：**随机生成 + 显式校验 + 有限重试**。边界串 = 固定前缀
  `ontology-` + 16 字节 `crypto/rand` 随机数的 hex（32 字符，128 bit 熵）。
  生成后用 `bytes.Contains(avoid, boundary)` 检查它是否出现在**全部待传输
  的内容字节**里（各区间数据拼接），命中则重摇，重试次数上限可配置，
  超限返回可判定的 `ErrBoundaryTries` 并拒绝组装（状态零变化）。
- 显式校验使"边界串出现在内容里"成为**确定性不可能**（检出即重摇），
  随机性只影响需要重摇的概率，不影响正确性。
- 失败概率上界：内容长 L 字节时，单次生成的边界串命中内容的概率
  ≤ L·2^-128（L 个起始位置 × 随机串等概率 2^-128）。即使 L = 2^40（1 TiB），
  单次命中概率 ≤ 2^-88；t 次独立重试全部命中的概率 ≤ (L·2^-128)^t，
  取 t = 8 时远低于 2^-700，实际不可达。重试上限存在的意义是防御
  调用方注入的对抗性内容与随机源故障，而非应对真实碰撞。

## 并发与查询约定（第 10、11 条）

- 单个 `serve.Assembler` **不要求并发写出**：`Build`/`Write` 须由同一
  goroutine 串行调用（文档注释写明）。多 goroutine 各持独立 Assembler
  组装不同响应是安全的；包级比较计数器用 `atomic.Int64`，`-race` 干净。
- `Stats()` 返回快照（区间列表为拷贝），不推进游标、不修改任何字段；
  同一时刻连查两次结果完全相同；未 `Build` 时为零值。
