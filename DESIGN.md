# Java `.properties` 解析/回写 设计推导

## 第 3 条：续行遇到“空白行 / 空行”

依据 `java.util.Properties` 底层行读取器的状态机：读物理行时，先剥掉行首
空白（`skipWhiteSpace`）；随后若该行**剩余长度为 0**，逻辑行直接结束（空行
被当作续行链的终止，而非再次续行）；若剩余非空且行尾是奇数个 `\`，才拼接
下一物理行。注意：JDK 在剥首空白时遇到行尾奇数反斜杠仍会继续，因此
"`\`+换行" 形式（空白后跟反斜杠）仍算续行，但"完全没有非空白字符"的一行
（空行或纯空白行）会让当前逻辑行终止。

结论：
- `k=v\` + 换行 + 空行（或 `"   "`）→ 逻辑行 = `k=v`，该空白行内容全部丢弃，
  且它后面的物理行不再属于此逻辑行（空行本身被吸收为终止符）。
- 与"续行首部空白被剥掉"的关系：剥离发生在判断"该行是否还有内容"**之前**；
  剥完为空即终止，剥完非空才拼接剩余部分。所以剥首空白既是拼接的前置动作，
  也决定了空白续行不贡献任何字符。

## 第 6 条：Store 转义推导

Load 规则决定了 Store 的最小转义集：
- 键：行首 `#`/`!` 会让整行变注释 → 键首字符为 `#`/`!` 时必须转义；未转义的
  `=`/`:`/空白是分隔符，且键两侧空白被吃 → 键内所有 `=`、`:`、空白必须转义；
  `\` 是转义引导符必须转义；换行/制表等用 `\n \t \r \f` 写出。
- 值：值只在"首个分隔符之后"开始解析，故值内 `=`、`:`、`#`、`!` 均普通字符，
  **不必转义**；值首空白会被剥除 → **只有值的第一个字符为空白时必须转义**
  （中间与尾部空格原样保留，故 `v # x`、`v  ` 安全）；`\` 必须转义；
  换行等控制字符转义；非 ASCII 按 UTF-8 原样输出。
- 空键直接写 `=value`；空值写 `key`；每映射一行，无注释头，保证
  `Load(Store(m))` 逐项、顺序恒等。

## 七条语义与测试对照表

（实现完成后补齐）
