# 随机超平面 LSH 分桶检索器设计

## 1. 数据与度量

- 向量：`Vec []float64`，维度 `d` 在建索引时固定，所有向量/查询必须与之一致。
- 欧氏距离 `dist(a,b)=sqrt(sum (a_i-b_i)^2)`；精排只需比较大小，故实际比较平方距离。
- 内积 `dot(a,b)=sum a_i*b_i`。
- `NaN`、`+Inf`、`-Inf` 一律拒绝（`NaN` 计入可读出的跳过数）；零向量合法。

## 2. 随机超平面族（hyper 包）

- 第 `l` 张表、第 `j` 位的法向量 w 由种子流确定性生成：
  `rng = rand.New(rand.NewSource(seed + uint64(l)*1_000_003 + uint64(j)))`，
  w 的每个分量独立取标准正态分布。种子相同 => 法向量字节级相同 => 签名表逐字节相同。
- 一位：`dot(w,x) >= 0` 记 1，否则记 0。**内积恰为 0（含零向量）约定记 1**。
- 一张表 b 位拼成 uint64，位 j 对应 `1<<j`；b <= 64。
- 对一个向量计算 b 位签名，哈希计算计数恰为 **b** 次（每位一次内积判定）。

## 3. 桶表（bucket 包）

- 每张表是 `map[uint64][]int`：签名 -> 向量 ID 列表；多表为 `[]Table`。
- 插入按位计数累加，故全库哈希计算次数恰为 `N*L*b`。

## 4. 检索（search 包）

- 查询时把 L 张表同桶 ID 去重合并为候选；对每个候选计算一次平方距离（精排计数），取 TopK。
- K>N 返回全部 N；空库返回空结果；无候选（桶空）返回空结果。
- 精确基线：与全部 N 个向量比距离取 TopK，用于召回核对。
- 召回率 `|ANN Top10 ∩ 精确 Top10| / 10`（K>N 时分母按精确结果数）。
- 表通过 RWMutex 下的一次性替换发布：查询要么看到 L 张全部表，要么一张也看不到。

## 5. 位数 b 与表数 L 的推导

- 每表碰撞概率随 b 下降：b 越大单桶越细，桶内期望候选 ~ `N/2^b`；
  L 张表并集使期望候选约随 L 线性增长、随 b 指数下降。
- 召回：单表漏检概率 `p`，L 张独立表后漏检 ~`p^L`，故 b 升召回降、L 升召回升。
- 结论：**候选数随 b 单调下降**（4>8>12 档），**召回随 L 单调不降**（1/2/4/8）。
- 多表实现上第 l 张表复用第 0 张的前若干位再扩展，保证“多表”是“少表”的
  超集路径，实测召回 1<=2<=4<=8 单调；不同档位 b 用固定法向量前缀，候选随 b 下降。

## 6. 持久化（persist 包）

文件布局（小端）：
1. 头：magic `"LSH1"`、d、L、b、seed（定长 28 字节，含长度域）。
2. 向量区：N + N 个 (id, d 个 float64)。
3. 超平面区：L*b*d 个 float64 法向量分量。
4. 桶表区：L 张表，逐表 `条目数` + `(签名, 计数, id...)`。
5. 尾部 4 字节 CRC32(IEEE) 覆盖其前全部字节。

- 截断分类（按长度落在的区间）：`ErrHeaderTrunc` / `ErrPlanesTrunc` /
  `ErrBucketsTrunc`（向量区计入桶表体不完整）/ `ErrCRC`（全长度在但 CRC 不符）。
- 损坏恢复：按长度恢复“最大可恢复前缀”，丢弃不完整的最后条目/表；
  桶内引用了不存在 ID 的条目被剔除并计数，保证无悬挂 ID。
- 某法向量全零 => `ErrZeroNormal`，指出表号、位号；维度不符 => `ErrDimMismatch`。

## 7. 可复现性

种子是唯一随机源，数据与 LSH 种子均显式给定；同种子两次建索引签名逐字节一致，
不同种子（极大概率）签名不同并有测试断言存在差异。
