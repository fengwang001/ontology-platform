# 快照读可见性

版本 `v` 由写入事务 `T` 产生，记录提交号 `c(T)`；读快照 `s` 记录水位 `h(s)` 与创建时的活跃事务集 `A(s)`。

朴素规则只判断 `c(T) < h(s)`，只使用了提交号，却丢失了“快照创建时 T 是否仍活跃”这一并发事实。

分情况推导：

1. `c(T) >= h(s)`：提交点不在水位左侧，左闭右开，不可见。
2. `c(T) < h(s)` 且 `T ∈ A(s)`：快照创建时 T 尚未提交；即使之后提交，对该快照仍不可见。
3. `c(T) < h(s)` 且 `T ∉ A(s)`：快照创建时已提交，可见。
4. T 已回滚：任何快照都不可见。

因此正确规则是：**已提交、提交号小于水位、写入事务不在活跃集中，三者同时成立才可见**。

反例：T1 取得提交号 5；读者创建快照时水位为 10，活跃集为 `{5}`，此时 T1 尚未提交；T1 随后提交。朴素规则看到 `5 < 10` 就判可见；正确规则还会发现 `5 ∈ {5}`，必须判不可见。

实现中事务状态存为一次 map 查找，活跃集存为 map；正常判定最多执行“状态查找 + 活跃集查找”两次。

快照复制活跃集后不再依赖事务表变化；其内存项数等于活跃集大小，与总事务数无关。多个 goroutine 只读同一快照时不修改快照，计数放在每次判定的本地结果对象中。

三类错误分别为：事务号未注册、快照已释放、版本链相邻提交号不递增。
