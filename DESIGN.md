# DESIGN

流式处理：逐字节喂入状态机。状态=行内已确定字节 + 待定空白串 `ws`（起始原文偏移）+ 待定的行首 `\r`（布尔）。空格/TAB 入待定缓冲；`\r`/`\n` 先冲洗行为：丢弃待定空白、产出一个 `\n`；普通字节把待定空白先释放。

## 1 末尾策略
- `Keep`：Close 时若有待定空白则释放（未见行尾，属行中）；待判定 `\r` 成一个 `\n`。原样保留末尾结构。
- `EnsureOne`：Close 后把末尾连续 `\n` 折叠为至多一个；无核心内容则输出空。
- `Trim`：有非换行核心时末尾恰好一个 `\n`；全空白/全换行输入输出空。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `""` | `""` |
| `"\n\n"` | `"\n\n"` | `""` | `""` |
| `"  \r\n"` | `"\n"` | `""` | `""` |

幂等：空输入是三种策略的不动点（`EnsureOne("")=""` 而非 `"\n"`，否则再规范化仍 `"\n"` 却要凭空造字节）。所有输出均无 `\r`、无行尾空白，再规范化时每字节均为 keep，策略不再改变它：EnsureOne/Trim 的输出末尾至多一个 `\n` 且核心非空。

## 2 被删字节的 ToOut
映射由 keep/delete 段构成。设输出偏移轴 o、原文轴 i。对 keep 段 `i∈[a,b)` 有 `o=i+c`。delete 段 `i∈[a,b)` 不占输出。
- 行尾空白被删：取**删除点之后**的输出位置（即行尾 `\n` 的位置），`ToOut(i)=ToOut(b)`。这样 `ToOrig` 单调不减；光标放在被删空白里映射到换行符**之前**（编辑器不会跳到下一行）。
- `\r\n` 的 `\r` 被删、`\n` keep：`ToOut(i_of_\r)` 同样取后值，即输出 `\n` 的偏移。
对所有输出偏移 o 有 `ToOut(ToOrig(o))==o`：`ToOrig(o)` 只落在 keep 段（取该输出位置对应的原文起点），故回到 o；delete 段不在 `ToOrig` 像集中，不参与该恒等式。两函数均按段单调不减。

## 3 缓冲超限
选择**拒绝**（可判定 `ErrPendingOverflow`，带原文偏移）。强制输出待定空白会把"将来可能是行尾空白"提前固化为行中空白：若随后出现行尾，单次流与切分流的差异（切分点恰在空白中）会违反语义4，且已写出的字节无法收回，幂等(5)与映射(6)被破坏。拒绝时已输出内容保留，状态进入终态，再写返回 `ErrClosed`；整体映射仍对已输出前缀成立。输出总字节超限同理返回 `ErrOutputLimit`（偏移=当前原文偏移）。

## 4 par 拼接
按给定切点分 K 段，每段独立 `norm`，产出事件流（keep/delete，带段内起止）。顺序拼接时在缝上做**有界修正**（每缝只看相邻段边界，修正范围 ≤ 待定空白长度+1）：
- `\r|\n`：前段把孤立 `\r` 判成 `\n`；丢弃该输出 `\n`，改把 `\r` 记为 delete，保留后段 `\n`。
- 前段末待定空白：空白在前段 Close(Keep) 时被释放为行中；若后段首字节是行尾，则改为删除这些空白并统一换行；若后段首字节普通或后段为空则保持 keep。
事件坐标：段 k 事件加 `baseOrig[k]`（原文）与 `baseOut[k]`（拼接后输出）；修正删除输出字节时 baseOut 相应收缩。每缝仅检查前段末尾与后段开头的待定序列，长度受 `PendingLimit` 上界约束，故总修正 O(K·limit)。各段用 Keep 规范化，拼接完事件再统一施加一次末尾策略，与单流结果逐字节一致。
