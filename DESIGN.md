# DESIGN: work-stealing 执行器

## 1. 未完成计数何时 +1 / -1

规则：任务被接受时（入队**前**）+1；任务走到终态（执行完成 / 跳过 / 失败）**且**其全部子任务都已被接受入队之后 -1。

若在「开始执行」时 +1，存在漏洞交错：P1 从队列 Pop 出 T（队列变空）→ P1 即将执行 T 但还没加计数 → 此时 Wait 看到计数 0 提前返回。
更本质的提前终止交错（即使接受时 +1，若在 fn 返回瞬间、子任务入队前 -1）：
1. T 是在途唯一任务，计数=1，T 内部要派生 A、B；
2. T 的 fn 已执行完最后一行用户代码，调度器先做 -1（计数=0）；
3. 此时所有队列都空、没有 worker 在执行用户 fn；
4. Wait 判定终止返回；而 A、B 此刻才被接受入队 → Wait 在任务尚未跑完时返回。

因此 -1 必须在「fn 已返回 **且** fn 内所有 Spawn 已完成入队（各已 +1）」之后，先 + 后 - 使计数永不为 0 的窗口消失；跳过/失败路径同样在终态时 -1。判据永远不能是「队列空且无人执行」。

## 2. StealHalf 取整

| n | 偷 ⌊n/2⌋ | 偷 ⌈n/2⌉ |
|---|---|---|
| 0 | 0（空） | 0 |
| 1 | **0**：唯一的长任务偷不走，其余 worker 永久闲置 | **1**：能偷走，但见下 |
| 2 | 1 | 1 |
| 3 | 1 | 2 |

n=1 必须能偷走（8 任务屏障要求最后一个任务也可迁移）→ 需要 ⌈n/2⌉。但两 worker 互偷时，n=2 ⌈2/2⌉=1 会整块在两方间来回搬、无进展且制造活锁流量。折中：**n=1 偷 1，n≥2 偷 ⌊n/2⌋**（至少给受害者留 1 个，杜绝互偷清空）。这正是「按错误规则实现就会失败」的测试点。

## 3. 取消如何到达被偷走的任务

取消时遍历所有 worker 队列是 O(任务数)（违反第四节：Cancel 访问数在 100/100000 两档必须相等），且持锁删除与窃取竞争。方案：组树节点存取消标记，`Cancel(g)` 只沿子组树 DFS 置位（O(子组数)）；任务出队后、调用用户 fn **之前**沿 parent 链向上检查任一祖先是否已取消，是则跳过，否则执行。
判定点即 exec worker 中的一行：`if t.g.canceled() {跳过} else {t.fn()}`。「已开始照常跑完」成立，因为检查只在 fn 调用前发生一次；fn 开始后取消不再影响它。被偷到任何队列都不影响——取消状态在组树上，不在队列里。

## 4. 本地队列满

拒绝会把反压直接暴露给正常递归负载；**就地执行**最坏：worker 深度递归 Spawn，每层队列满 → fn 嵌套 fn，栈深随递归线性增长（深树直接栈爆），且破坏窃取公平。
选择：**满时把本队列底部一半任务转移给随机其他 worker 的队列顶部**（腾空间、顺序不变）；所有 worker 都满才返回 `ErrOverloaded`。全局 `MaxPending` 用计数器在接受前原子判定，超限 `ErrOverloaded` 计入 rejected，不加未完成计数。

## 5. 复杂度计数器

- `deque.moves`：仅环形扩容（倍增）时搬移，摊还 O(1)，断言 ≤3×ops。
- `group.cancelVisits`：每次 Cancel DFS 访问的组节点数（包级原子，测试读取）。
- `group.ancestorSteps`：每次取消检查上行步数（包级原子），深度 3 与 30 两档断言。
