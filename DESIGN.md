# 事件流增量解析器设计推导

包依赖单向：`linescan`（字节块→逻辑行）← `stream`，`field`（行→字段）← `stream`。`linescan` 与 `field` 互不依赖，均只用标准库。

## 1. 块边界落在 `\r` 与 `\n` 之间

行切分状态机只有两个状态：

- `inLine`：正在累积一行；遇 `\n` 直接成行；遇 `\r` 成行并迁移到 `afterCR`。
- `afterCR`：上一行以 `\r` 结束，尚未看到下一字节。此时若来 `\n`，**丢弃**该 `\n`（它是同一个 `\r\n` 行尾的后半），迁回 `inLine`；若来其他字节，说明那个 `\r` 是独立的 `\r` 行尾，当前字节属于新行，迁回 `inLine` 并正常处理该字节。

关键：`\r` 一旦使一行产出，状态机进入 `afterCR` 并**挂起**，等下一个字节裁决。因此 `\r`、`\n` 分处两个块时，第二个块的首字节 `\n` 命中 `afterCR` 分支被吞掉，`\r\n` 永远只产生一次行结束，绝不会被当成「一行 + 一个空行」。空行只能由 `inLine` 状态下缓冲为空时遇到行尾产生，而 `afterCR` 不产生任何行。

## 2. 切分点无关性

跨块必须保留的状态（解析器全部可变状态）：

- 半行缓冲 `buf`（未遇行尾的字节）；
- 行尾状态 `afterCR`（上节的一位状态）；
- 流首 BOM 标志（仅当尚未消费任何字节时为真）；
- 事件聚合状态：data 缓冲、event 名、待提交的 lastEventID；
- 重连状态：lastEventID、retry 间隔。

不变量：**对任意字节流 S 与任意切分 S = c1|c2|...|cn，逐块喂入与一次性喂入，状态机消费的字节序列完全相同**。因为状态机的迁移只依赖「当前状态 + 下一个字节」，不依赖块边界；块边界处唯一可能丢失的信息（半行、`\r` 待裁决、BOM 待裁决）全部显式存在上述状态里。对喂入字节数做归纳：0 字节时平凡成立；设前 k 字节后状态相同，则第 k+1 字节触发相同迁移，故任意切分点下产出的事件序列完全相同。

## 4. 为什么只有注释的块不能派发

派发判定：空行到达时，仅当 data 缓冲非空**或**设置过 event 名才派发。注释行（`:` 开头）按规范被忽略，不写入任何缓冲；未知字段同理。若「只有注释的块」也派发，下游会观察到 data 为空、event 为空的幽灵事件：心跳注释（代理每 15s 发一行 `:ping` 保活）会变成源源不断的空事件，触发下游的空消息处理逻辑（空推送、空入库），违背「注释仅用于保活」的契约。因此派发条件必须只看 data/event 两个语义载体，注释与未知字段不得计入。

## 9. 超限为何必须状态不变

单行超长、单事件 data 超长时，解析器立即返回可区分错误（`ErrLineTooLong` / `ErrDataTooLong`），且**不提交任何部分结果**：半行缓冲、data 缓冲、event 名、lastEventID、retry 全部保持超限前的值。

若改为「截断后继续」，被截断的行会以残缺内容进入 data 缓冲并参与派发，于是同一字节流 S：切分点使超限发生在块 A 内时下游看到截断事件，而调用方在超限后选择丢弃剩余块时下游看不到——**派发出的事件序列开始依赖调用方在错误后的处理方式与切块方式**，直接破坏第 2 条「同一字节流任意切分产出相同事件序列」的不变量（错误后的字节是否被消费成为新的隐式状态）。「拒绝且状态不变」把错误变成纯查询：调用方可安全地检查状态、决定重连，重连后从保留的 lastEventID 继续，语义唯一。
