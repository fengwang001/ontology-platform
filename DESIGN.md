# 带转义 RLE：设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（合法编码唯一）。
据此逐条推导必须拒绝的输入（错误均带字节偏移，彼此可区分）：

1. 显式次数 1：接受 `1a` 则 Decode 得 `a`，Encode 得 `a` ≠ `1a`。拒绝。
2. 次数 0：`0a` 解码为空游程，Encode 不含该游程，无法还原 `0a`。拒绝。
3. 前导零：`01a` 的数值 1 被规范化为 `a`；`002a` 被规范化为 `2a`，均不可逆。拒绝。
4. 相邻同符号游程：`2a3a` 解码为 `aaaaa`，最长游程编码为 `5a` ≠ 原串。拒绝（偏移指向下一游程符号处）。
5. `\` 后非数字/反斜杠：如 `\a` 不是任何合法输出（规范编码永不产生），拒绝。
6. 末尾孤立 `\`：转义缺少被转义字符，且不可能是任何 Encode 输出。拒绝。
7. 末尾只有次数：`12` 缺符号；Encode 输出永远以符号结尾。拒绝（偏移指向首位数字）。
8. 非法 UTF-8：Decode 结果是 Go 字符串；原始字节经 Encode 无法得到该字节序列。拒绝（偏移指向坏字节）。

大次数取舍：选择设上限拒绝。理由：次数以 int64 承载，strconv 逐位解析可检测溢出，绝不静默截断；
接受超过 MaxInt64 的次数在主流 64 位平台无法物理解开且收益为零。
在第 19 位检测到越过 9223372036854775807 即返回 ErrCountTooLarge；
解码前不分配与次数成正比的内存，输出按 Write 逐次增长并受输出预算约束（默认 64MiB，超限 ErrOutputLimit）。

切分一致性：流式 Writer 是一台字节状态机，每个输入字节恰被检查一次（计数器记录），
不完整前缀留在内部缓冲；相邻同符号（如 `2a3a`）在符号边界处才判定，故任意切分结果一致。

## 语义—测试对照

| 语义（第三节） | 测试函数 |
| --- | --- |
| 1 往返 | （测试写完后补） |
| 2 规范形式/严格解码 | |
| 3 大次数 | |
| 4 最长游程 | |
| 5 跨切分点一致 | |
| 四、计数器 | |
