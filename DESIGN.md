# DESIGN

## 1. 总体

流式流水线：字节 → `eol` 解码器（产出事件，\r 暂存待定）→ 事件驱动 `ws` 状态机（行尾空白延迟判定）
→ `norm` 落盘输出并调用 `span` 记账。`par` 切段并发跑 `norm`，顺序做有界边界修正后拼接。
映射按「游程」记账：一条 Run 表示原 `[A,B)` 连续映射到输出 `[a,b)`，长度可不等（删除点长度 1）。

## 2. 规范化分两阶段

阶段 P（逐行、与末尾策略无关）：`\r\n` 与孤 `\r` → `\n`；删每行尾部连续空格/制表符；空白行变空行。
对 P 有 `P(P(x))=P(x)`：P 的输出无 `\r` 且每行尾无空白。
阶段 F（末尾策略，只作用结尾）：Keep 原样；One：非空且不以 `\n` 结尾则补一个 `\n`，否则不动；
Trim：删全部结尾 `\n` 后补一个 `\n`；空输入特例 `""`（不凭空造行）。

## 3. 空输入与只含换行（推导）

幂等要求 `F(F(x))=F(x)`。One 对 `""`：若输出 `"\n"`，再次 One 输入 `"\n"` 已结尾故不变，幂等成立；
但它对"无内容"凭空造行，且与 Trim 无法区分。取 One 对 `""`→`""`（无行可保），对非空保证恰好一个尾换行，
再施加 One：已恰好一个，不动，幂等成立。Trim 对 `""`→`""`；其余剥到最后一个非 `\n` 之后再补一个，
再施加 Trim：结尾恰好一个、剥 0 个再补，不动。Keep 恒等。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 4. 被删字节的 ToOut（推导）

要求 ToOut 单调不减且对所有输出偏移 o 有 ToOut(ToOrig(o))=o。故被删原偏移不能映射到新值，
只能复用某真实输出偏移。一串被删空白位于换行符之前：取该删串起点（行尾）对应的输出位置，
即**换行符之前**（下一个非删输出的起点）。整串删字节都映射到该位置 p：
ToOrig(p)=删串起点、ToOrig 单调不减、ToOut(ToOrig(p))=p，全部成立。
`\r\n` 的 `\r` 同理映射到输出 `\n` 的位置（删串紧接 `\n`，p 即 `\n` 位置）——光标停在被删的 `\r`
上等价于停在换行上；停在行尾被删空白里则落在换行**之前**，符合"删掉的尾随空白"的编辑直觉。
文件尾被删空白（其后无换行）映射到输出末尾 len(out)。

## 5. 缓冲超限（推导）

ws 必须缓冲"未定空白串"才能在看到换行时删除、看到非空白时原样吐出。强制判定为非行尾会把
`"a   \n"` 在超长空白串上输出空白，若随后真出现换行，输出含行尾空白，违反语义 2/5；
若不输出又无法继续，状态不可解释。故选择**拒绝**：哨兵 `ErrPendingLimit`，带当前原文偏移；
已输出内容保留，Normalizer 进入终态（再写返 `ErrClosed`）。切分一致性不受影响：拒绝只取决于
到该点为止的完整输入前缀，与喂入方式无关。输出超限：哨兵 `ErrOutputLimit`，
超限字节不写入、不落 span，立即终态。

## 6. par 拼接（推导）

段 `[s_k,s_{k+1})` 各自用独立 `norm` 并发（阶段 P，末尾 Keep、宽松上限、NUL 严格）。
段间只有两类跨边界悬而未决状态：段尾一个待定 `\r`（eol）、一串未判空白（ws）。
故顺序修正时有界：取 A 段尾部 `L+2` 字节（L=WsLimit）与 B 段头部 `L+2` 字节覆盖的 zone
（尾部还含一个可能的待定 `\r`），对该 zone 用全局切分点重新规范化，替换 A 输出前缀到切分点、
B 输出从切分点起的部分；A 若有终态错误则整体失败。zone 大小 ≤ 2(L+2)，与输入总长、段数无关，
故修正范围有上界。span 按原文切点切表（Run 在切点处分裂），平移段内坐标：orig、out 各加段起点
累计值；zone 替换后整体游程合并。最后对拼接结果施加一次阶段 F（策略可配），F 只改结尾且只新增/删除
结尾 `\n`，幂等且与单路一致。所有切点（含 `\r|\n`、空白串中间、空白后接 `\r`）zone 都覆盖到。
