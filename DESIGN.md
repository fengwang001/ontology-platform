# DESIGN

## 代理对错误偏移（语义 2）的推导

规则：偏移指向「值违反代理语法的那个 `\u` 单元的反斜杠」。

- 高代理后不是 `\u` 转义（`"\uD83Dx"`、`"\uD83DA"`、`"\uD83D"`）：
  后续字节本身是合法内容，唯一无法配对的单元是高代理 → 指向高代理起点（偏移 1）。
- 高代理后确是 `\uXXXX` 但 XXXX ∉ [DC00,DFFF]（`"\uD83D𐀀"`）：
  低代理槽位已被占据，占据者的值不合格 → 指向第二个单元起点（偏移 7）。
- 低代理没有先行高代理（`"\uDE00"`、`"\uDE00\uD83D"`）→ 指向它自身起点（偏移 1）。

理由：偏移应指向「值非法的单元」而非「发现错误的位置」。`"\uD83DA"`
报在 1，用户改的是 `\uD83D` 而不是无辜的 `A`；`"\uD83D𐀀"` 报在 7，
因为 `\uD83D` 本身写法良构，是槽位占据者的值不合格。

`"\uD83DA"` 不能解释成「U+FFFD + `A`」：U+D83D 不是 Unicode 标量值，
不存在它的任何 UTF-8 编码，严格解码器对它只有「配对」或「拒绝」两条路。
静默替换为 U+FFFD 会让不同非法输入（`\uD83DA`、`\uD800A`…）映射到同一
输出，解码不再可逆、错误被吞没，违反本编解码器「拒绝而非修复」的约定。

## 最小转义与唯一性（语义 4）的推导

RFC 8259 的 unescaped = %x20-21 / %x23-5B / %x5D-10FFFF：语法禁止原样
出现的只有 U+0000–U+001F、`"`、`\`。U+001F 属 C0 控制字符，原样出现会
破坏以控制字符为分隔的传输与日志，故语法禁止；U+007F(DEL) 不在禁止集
（它可安全出现在任何能承载 JSON 的上下文中），所以最小转义不碰它。
最小性 = 只转义语法禁止的，故 `/`、非 ASCII、U+007F、U+2028/29 原样。

单射且唯一：编码是逐码点的纯函数，每个码点恰有一条输出 token
（短形式 / `\u00xx` 小写 / 原样，无任何选择余地）→ 输出唯一；
token 流可无歧义切分（`\` 开头的 token 定长 2 或 6，其余为单码点，
而原始字节中 `\` 必被转义）→ 解码唯一还原 → Encode 是单射。

## 测试对照表

（待测试写完后补齐）
