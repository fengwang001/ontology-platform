# 设计推导

## UTF-8 非法单元

规则：每个 lead 先吞入其声明长度内的后续字节，遇到非 continuation、越界、非最短、代理或流结束即结束；该 lead 到断点前的字节算一个非法单元。断点后的字节从自身重新开始。独立 continuation、`C0/C1/F5..FF` 各吞 1 字节。这就是 maximal subpart：`F0 90 80 41` 中前三字节是一个合法前缀；`E0 80 80` 第二字节越界后，剩下两个 continuation 各自独立；`E2 82`+EOF 的前缀只成一个单元。

第二字节窗口如下；其他 lead 为 `80..BF`，表内“非法”指第二字节不在窗口：

| lead | 第二字节窗口 | 非法吞掉字节数 |
|---|---|---|
| `C2..DF` | `80..BF` | 2；若第二字节非法，则 1 |
| `E0` | `A0..BF` | 3；若第二字节非法，则 1 |
| `E1..EC` | `80..BF` | 3；若第二字节非法，则 1 |
| `ED` | `80..9F` | 3；若第二字节非法，则 1 |
| `EE..EF` | `80..BF` | 3；若第二字节非法，则 1 |
| `F0` | `90..BF` | 4；若第二字节非法，则 1 |
| `F1..F3` | `80..BF` | 4；若第二字节非法，则 1 |
| `F4` | `80..8F` | 4；若第二字节非法，则 1 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

第二字节非法后立即放弃前缀，不等到 EOF；后续第三、四字节再非法才继续扩大断点后的非法单元。

## 流式缓存

UTF-8 最长 4 字节，只需缓存未完成 lead 加最多 2 个 continuation；若第二字节窗口非法，立即终止该单元。因此缓存硬上限为 3（尚未等待下一字节的当前 lead 不计为“等待缓存”）。UTF-16 硬上限为 3：一个残缺 code unit 的 1 字节加高代理 code unit 的 2 字节。每次字节检查后立即改变状态，不回扫，碎切不增加检查次数。

## 并行切点

任意切点可能落在单元中部。每段从切点前最多 3 字节（UTF-16 为 3）开始，找到最近的非 continuation（UTF-16 对齐 code unit 边界及可判定 lead 边界），在本地重放前缀，只接受起点不早于本段切点的单元；尾部同样把“安全尾界”交给下一段重放。前缀输出丢弃，但其标量/非法统计仍属拥有该单元起点的段。例：`C3|<80 80>` 从上段末的 `C3` 重放，首 continuation 属于上段一个合法标量，下一段独立的 `80` 才是一个非法单元，不会误报两个非法单元。

## 上限断点

开始一个新单元前先确认输出容量；容量不足则不消费任何字节，返回 `ErrLimit` 与 `Consumed`。已在缓存中的字节不算本次新消费；恢复者先把缓存字节写入新实例，再从 `Consumed` 续传，即可逐字节拼接。若编码替换符导致容量不足，在写入前拒绝整个单元，不留半个标量或半个代理对。
