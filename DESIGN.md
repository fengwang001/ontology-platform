# DESIGN：流式 UTF-8 ⇄ UTF-16 转码器推导

## 推导 1：非法单元切分规则
从流首字节 b0 起：
- b0∈80..BF / C0,C1 / F5..FF：单元仅 1 字节 → `80 80`、`C0 AF` 逐字节各替换；`F4 90 80 80` 中 F4 的 b1=90 不在 F4 区间，F4 独立成单元，其后 3 个 80 各自成单元 = 4 个。
- 否则期望长度 n=2/3/4。检查 b1 是否在下表第二字节区间；不在则单元=b0 单字节（立即判定，不等后续）→ `F0 90 80 41`：F0 要 b1∈90..BF 而 90 非法，F0 单独替换，`80 41` 再各自处理 = FFFD+41；`E0 80 80`：E0 要 A0..BF，E0 独立，余两 80 独立 = 3 个。
- b1 合法后：位置 k=2..n-1 上首个不在 80..BF 的字节，使单元长度=k（合法前缀整体替换，长度可能短于 n）→ `E2 82`+EOF：k=2 且流尽，单元长 2，只 1 个替换；`ED A0 80`：若按通用区间 b1=A0 合法，则 b2=80 会编码出代理码点，须在标量层拒绝、单元长 3 只 1 个 FFFD——与题面样例（3 个）矛盾。故 ED 的第二字节区间必须收窄为 80..9F，在 b1 即判非法：ED 独立、A0 独立、80 独立 = 3 个。

第二字节合法区间与非法时吞掉字节数：
| 首字节 | b1 合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | b0 一个 |
| E0 | A0..BF | b0 一个 |
| E1..EC | 80..BF | b0 一个 |
| ED | 80..9F | b0 一个 |
| EE..EF | 80..BF | b0 一个 |
| F0 | 90..BF | b0 一个 |
| F1..F3 | 80..BF | b0 一个 |
| F4 | 80..8F | b0 一个 |
| 80..BF | —（首字节即非法） | 1 |
| C0 C1 F5..FF | —（首字节即非法） | 1 |

b1 合法后的后续非法/截断：吞掉「b0..上一个延续字节」（2..n-1 个）。

## 推导 2：缓存上限与何时放弃等待
UTF-8 最长 4 字节，pending 只需存 1 个首字节 + 至多 2 个已接受延续字节（第 4 字节到达即决），硬上限 3。UTF-16 最长代理对 4 字节，pending 上限 3（奇数字节或高代理+1 字节）。立即判定不等 EOF 的情形：b1 落在区间外时 b0 立刻成单元；首字节本身非法立刻成单元。仅「已接受延续字节但后续字节缺失」才必须留到 Close（半个合法字符）。跨切分时 pending 先拼新数据再解析，故切在字符中间不提前出 FFFD。

## 推导 3：par 切点对齐
UTF-8 单元边界可由字节局部判定：延续字节 80..BF 永不开启新单元。段 i（i>0）原始起点 s：取段内前 a=min(4,剩余) 字节，回退越过其中所有连续前导 80..BF（至多 3 个）得真正起点 t，段 i 只解析 [t,end)，BaseOffset=s+t。每个段解析到片段末尾时，未完成前缀不立即计 FFFD，而是把尾部至多 3 字节（首字节+已见延续）原样移交下一段；下一段回退跳过的字节恰是交接口，不重不漏。例 `C3|80 80`：上段交 C3，下段回退 1 个 80 后从 C3 解析 → C3 80 合法（é），余 80 独立 1 个 FFFD，与单线程相同。切在非法单元中间（`E0|80`）：E0 无 b1 时不预输出，交接后 E0 80 判非法，仅 1 个 FFFD。统计按全局偏移累加，字节数守恒。

## 推导 4：上限断点
Write 在每个完整单元输出前检查剩余容量；放不下整个编码（UTF-8 的 1..3 字节 FFFD、UTF-16 的 2 或 4 字节，代理对不拆）即返回 ErrLimit，n=已完整输出单元对应的输入字节数；pending 中的半个字符字节不算已消费，经 Pending() 取回，调用方把 Pending()+p[n:] 喂给新实例，拼接即等于无上限结果。Close 的截断 FFFD 同样受上限约束。
