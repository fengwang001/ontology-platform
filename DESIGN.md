# 带转义的 RLE 编解码器设计

## 第 2 条：严格解码拒绝清单的推导

不变量：解码器接受的每个 t 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。
对每一类输入，若接受它，则存在某个 t 使不变量被破坏，故必须拒绝：

1. 显式次数 1：`1a` 解码为 `a`，而 `Encode("a") == "a" != "1a"`。拒绝（ErrExplicitOne）。
2. 次数 0：`0a` 若解码为空串，`Encode("") == "" != "0a"`；次数 0 永不产生。拒绝（ErrZeroCount）。
3. 前导零：`01a` 解码为 `a`，`Encode("a") == "a" != "01a"`；`007a` 同理。拒绝（ErrLeadingZero）。
4. 相邻同符号游程：`2a3a` 解码为 `aaaaa`，`Encode("aaaaa") == "5a" != "2a3a"`；`aa` 同理（应为 `2a`）。拒绝（ErrAdjacentSame）。
5. `\` 后非数字非反斜杠：`\a` 若解码为 `a`，`Encode("a") == "a" != "\a"`；裸 `a` 才是 `a` 的编码。拒绝（ErrBadEscape）。
6. 末尾孤立 `\`：`\` 不是任何码点的编码（反斜杠必须转义为 `\\`），无对应明文。拒绝（ErrDanglingEscape）。
7. 末尾只有次数：`a3` 中 `3` 无符号可修饰，任何解码结果的编码都不含裸尾部数字。拒绝（ErrMissingSymbol）。
8. 非法 UTF-8：输出必须是合法 UTF-8 字符串，非法字节序列无法表示为码点游程。拒绝（ErrInvalidUTF8）。

每类错误是可判定的哨兵错误（`errors.Is`），并包装在携带字节偏移的 `*rle.Error` 中。

## 第 3 条：大次数的取舍

选择**有界支持**：次数按十进制解析时以剩余输出预算饱和（`runs.ParseCount` 数学上不会溢出，
超过预算即返回 预算+1），因此 `99999999999999999999a` 这类超大次数不会溢出、也不会按次数预分配
内存；只有在实际写出解码字节时才占用内存。解码输出总字节上限由 `NewDecoder(max)` 配置
（`Decode` 用 `DefaultMaxOutput`），超限返回可判定的 `ErrTooLarge`。被接受的 t 其次数必然
不超过上限，故饱和解析不影响 `Encode(Decode(t)) == t`：上限内的次数被精确解析。

## 语义 → 测试对照表

（待补）
