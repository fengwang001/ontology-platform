# 严格模式流式 Base64 设计说明

## 第 2 条「规范形式」的推导

一个 4 字符组承载 4×6 = 24 bit，对应 3 字节输出。带 `=` 填充时输出不足
3 字节，六比特组里就有不参与任何输出字节的冗余比特：

- `xx==`：2 个六比特组共 12 bit，只输出 1 字节（8 bit），冗余 4 bit，
  即**第 2 个字符取值的低 4 bit**；
- `xxx=`：3 个六比特组共 18 bit，只输出 2 字节（16 bit），冗余 2 bit，
  即**第 3 个字符取值的低 2 bit**。

编码是一个函数：对任意字节串 x，`Encode(x)` 的结果必须唯一。宽松解码器
把 `QR==` 也映射到 `A`（冗余位被直接丢弃），于是 `QQ==` 与 `QR==` 两个
不同的编码对应同一字节串，`Encode(Decode("QR==")) == "QQ==" != "QR=="`，
「编码唯一」与「往返」同时被破坏。因此严格模式要求冗余比特全为 0：
`QR==`（R = 17 = 0b010001，低 4 bit = 0001 ≠ 0）必须拒绝；同理
`QUJ=`（J = 9 = 0b001001，低 2 bit = 01 ≠ 0）必须拒绝。判定只检查上述
4 bit / 2 bit，与其余比特无关。

统一规则：一个流被严格模式接受，当且仅当它恰好是某个字节串的标准编码
输出——长度是 4 的倍数、填充只出现在整个流的末尾、尾部冗余比特为零、
换行只出现在组之间（且仅 MIME 模式）。由此 `QQ`、`QQ=`（长度不足）、
`QQ===`、`QQ==QQ==`（填充不在末尾）均被拒绝，空流合法。

## 语义 ↔ 测试对照表

（待测试落地后补齐）
