# 分层时间轮调度器 — 设计推导

时间完全外部注入：构造时给定起始时刻 `start`，之后只能经 `Advance(ticks)` 推进。
调度器内部维护累计推进量 `now`（从 `start` 起算），代码不出现任何真实时钟调用。

## 结构

L 层轮，每层 S 个槽。层 i 的粒度 `g(i) = S^i`，游标不变量 `pos[i] = (now / S^i) % S`。
剩余 `r = deadline - now` 的元素落在最小满足 `r < S^(i+1)` 的层 i，槽号 `(deadline / S^i) % S`。

量程表（S=64，槽号/延迟以 tick 计）：

| 层 | 粒度 g(i)=S^i | 本层覆盖剩余 r 的范围 | 累计可表示最大延迟 |
|----|---------------|------------------------|---------------------|
| 0  | 1             | [1, 63]                | 63                  |
| 1  | 64            | [64, 4095]             | 4095                |
| 2  | 4096          | [4096, 262143]         | 262143              |
| 3  | 262144        | [262144, 16777215]     | 16777215            |

L 层总量程 `S^L - 1`；`MaxDelay` 配置不得超过它，构造时校验。

## 推导一：到期判定用 `>=`，延迟 0 在首次 Advance 触发

不变量 1 要求「累计推进量首次达到或超过 d 时触发」。若用 `>`，则 d 与累计量相等
时不触发，直接违反「达到……时」。故判定为 `now >= deadline`，其中
`deadline = 注册时刻 + d`。推论：d=0 时 deadline 等于注册时刻，注册瞬间条件已
成立；但触发只发生在 `Advance` 内，且 `Advance(0)` 是参数错误，所以延迟 0 的
定时器在注册后的**第一次 Advance** 触发——这与 `>=` 自洽，且无观察窗口能违反
不变量 1。实现上 deadline <= now 的新注册进入 due 队列，下一个 tick 触发。

## 推导二：同 tick 触发顺序 = 注册序号顺序

约束 a：同一输入序列重复执行结果逐位相同 → 顺序只能是状态的确定性函数。
约束 b：顺序不得因元素恰好落在高层轮而改变 → 顺序不能依赖层号/槽号/降级路径。
满足 a、b 且可用的唯一信息是注册先后：每次 `Add`/`Reset` 分配单调递增序号
`seq`，同一 tick 收集完所有到期元素（含各层降级下来的）后按 `seq` 排序再触发。
`Reset` 视为重新注册，分配新序号——否则同一句柄的新旧身份会让「先后」产生歧义。

## 推导三：高层元素在推进到其所在槽时才降级（惰性降级）

候选 A（惰性）：层 i 游标转到某槽时，取走整槽，按剩余 tick 重新定位到低层。
候选 B（提前）：推进若干 tick 后把「即将进入低层量程」的元素预先搬下。
在「一次跨多 tick 推进」下两者会产生不同的槽位中间状态，但只要触发序列定义为
「到期集合 + seq 排序」，两者最终序列相同——元素是否已降级不改变其 deadline
与 seq。选 A，理由：
1. `Advance(n)` 内部就是 n 个单 tick 的循环，每 tick 只动当前游标槽，触碰槽数
   O(n + 降级元素数)，与总定时器数 N 无关（不变量 2 与复杂度约束同时满足）；
2. 降级发生在 `now = (deadline/S^i)*S^i`，此时剩余 `deadline mod S^i < S^i`，
   必然落入更低层，数学上不会迷路；剩余为 0 时落入层 0 当前槽，同 tick 触发；
3. 候选 B 需要扫描或索引未来元素，触碰数随 N 增长，直接违反复杂度约束。

## 推导四：`Reset` 从当前时刻起算

候选 A（当前时刻 now 起算）：`deadline = now + d`。
候选 B（原注册时刻起算）：`deadline = 原注册时刻 + d`。
选 A。理由：B 下若新 d 小于已流逝时间，deadline 落在过去，要么立即触发（违背
调用者「再过 d 个 tick」的直觉），要么需要额外语义补丁；且 B 使 Reset 的结果依
赖于调用方已无法观察的历史时刻，可判定性差。A 与不变量 1 的共存方式：把
`Reset(d)` 定义为「在 now 时刻的一次新注册」，不变量 1 作用于这次注册——累计
推进量首次 `>= now + d` 时触发，恰好是不变量 1 本身的形式，无需任何例外条款。

## 关键机制备忘

- 取走整槽后统一按 seq 排序再触发；触发前逐句柄复查状态，「已取出待触发但被
  取消」的句柄在回调前被拦截（不变量 4）。
- 回调在不持锁的情况下执行，回调内可安全调用 `Cancel`/`Reset`/`Add`。
- 每次 `Advance` 重置非导出计数器 `touchedSlots`/`touchedTimers`，统计本次推进
  触碰的槽数与定时器数。
