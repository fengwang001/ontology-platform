# DESIGN：统一格式 diff 的四处推导

行模型：每行字节保留原行尾（`\n`/`\r\n`/无），内容=去尾缀。行号 1-based，索引 0-based。

## 1. 零计数时的行号（规则 4）

hunk 头形如 `-a,b +c,d`，其中 a/c 是 hunk **起始旧/新行号**。含行时 a=第一行行号（1-based）。
纯插入（b=0）时 hunk 不含旧行，约定起始行号 =「插入点之前最后一行」的行号；插在文件开头则 0。
逐样例：`x\n`→`y\nx\n` 插在开头，旧无前行 → `-0,0 +1`；`a\nb\nc\n`→`a\nb\nX\nc\n`，
插入点在旧第 2 行之后 → `-2,0 +3`；`a\n`→空 为纯删除（d=0），新侧起始行号=0（新无前行）
且旧行号=1 → `-1 +0,0`。GNU 取「前行行号」而非插入位置，是因为 diff 坐标描述「hunk 覆盖的行」，
空区间 0..0 的起点自然落在前行上，与 `ed` 地址习惯一致，也保证 a 永不指向不存在的行。
Apply 解释：b=0 时插入点 = 旧文件索引 a（a 行之后，即 `orig[:a]` 与 `orig[a:]` 之间）；a=0 开头。

| b | 旧侧书写 | Apply 插入/起始位置 |
|---|---|---|
| 0 | `-a,0`，a=前行行号（开头为0） | 插入点在旧索引 a 处（a 行之后） |
| 1 | `-a`（省略 `,1`） | 首行旧行号 a（索引 a-1） |
| ≥2 | `-a,b` | 首行旧行号 a，共 b 个旧行 |

新侧 `+c,d` 完全对称；渲染时 b/d 为 1 省略计数，为 0 显式写 `,0`。

## 2. hunk 合并阈值（规则 5）

每 hunk 两侧各取 C 行上下文。两改动间未改行数为 g。合并后中间共享上下文不重复：
合并 hunk 的上下文总量 = 2C+g，分开为 4C，输出文本行数合并比分开少 `4C-(2C+g)=2C-g`。
当且仅当 g ≤ 2C 时合并不增加输出（g=2C 持平），故阈值 **g ≤ 2C 合并，g ≥ 2C+1 分开**。
反证：C=3、g=6：`diff -U3` 把两改动合并成一个 hunk（2C=6）；若阈值取 C 则错误分开。
阈值取 2C+1 则 g=7 也会合并，但 GNU 对 g=7 输出两个 hunk（合并需 2·3+7=13 旧行，
而分开共 12 行，更短，故分开）。实现：遍历脚本中的改动块，间隔上下文行数 ≤2C 即并入。

## 3. 并列最短脚本的选择（规则 3）

Myers 按 D 递增、每条对角线 k 从大到小（先向下=删除，再向右=插入）扫描；
取 `x = k==-D || (k!=D && V[k-1] < V[k+1]) ? V[k+1] : V[k-1]`，即并列时**优先删除**。
例 `a b`→`b a`：D=2，先删 `a`（-行在前）再插 `a`（+行在后），hunk 内排列
`-a`、` b`、`+a`，与 GNU diff 一致。全部平局由「先删后插 + k 降序」唯一确定，故输出确定。

## 4. 偏移后后续 hunk 的查找起点（规则 7）

hunk i 以偏移 δ=实际旧索引−记录旧索引应用后，文档行已平移 δ。hunk i+1 的记录位置
针对的是原文件坐标，因此查找中心**跟随平移 δ**：中心 = 记录起始索引 + δ，搜索窗仍 ±F 行。
多 hunk 时 δ 累积（每次成功偏移叠加）。这样同一处插入导致后续所有 hunk 整体位移时仍能命中；
若后续改动与前 hunk 无关，±F 窗口照常工作。候选排序：|实际−中心| 最小，相等取靠前。
