# 设计推导

约定：逗号分隔；记录终结为 `\n` 或 `\r\n`；引号内 `""` 为一个 `"`；引号内字节（含 `\r\n`）原样保留。

## 1. 空行与单列空值

选择：**空行是一条记录**，含一个未加引号的空字段。推导：状态机只在见到记录终结时产出记录；两个相邻 `\n` 之间的空字段序列长度为 1（字段数 = 分隔符数 + 1），故空行 = `[""]`。文件末尾的换行不产生额外记录，因为「记录」在终结符处闭合，空文件记录数为 0。

回写后果：单列表若某记录唯一字段为空，写出的字节是 `\n`；若漏写记录终结，记录就不存在了。因此 writer 对**每条记录都写记录终结**，单列空值写作 `\n` 而非空串，数据不丢。引号内空值必须写 `""`，否则与未引号空值不可区分。

规范输入定义：非空文件每条记录都以 `\n` 结尾（最后一条也有 `\n`）；记录终结统一用 `\n`；仅含逗号/引号/CR/LF 的字段按第 4 节最小规则加引号。此集合上 `Write(Parse(x)) == x`。

## 2. par 切点

切点前后的唯一隐藏状态是「是否在引号内」。每段 worker 以**两种假设**各跑一次：H0 起点在引号外、H1 起点在引号内（段首即引号体内）。段间真实状态从左向右确定：第一段真实状态为 H0；段内引号奇偶决定下一段入口的真实假设，从而选定下一段应采用的结果。

跨段残尾（段切在一个字段中间）分两类携带：引号体内（段结束于奇数个未转义引号之后）携带 `qopen`；未引号字段末尾段以 `\r` 结束携带 `cr`（见第 3 节）。拼接时残尾字段与下段首字段合并；被否定假设下的事件全部丢弃，其错误也丢弃，只有最终提交路径上的错误保留。

位置换算：段内记录/字段号从 1 起；维护跨段「已闭合记录数」，全局记录号 = 偏移 + 段内号；字段号仅在残尾字段合并时顺延；字节偏移 = 段起点 + 段内偏移，合并字段的起点取残尾起点。

复杂度：每字节在 H0/H1 各处理一次，总计 ≤ 2N + O(K)；段间拼接为 O(K)，长引号字段不引发重扫。

## 3. CR 待定

未引号字段见到 `\r` 不能立刻判定：下一字节为 `\n` 则是行尾，否则是孤立 CR 错误。故设独立状态 `crPending`，该 `\r` 不进字段值。流式续传：`\r` 可作为最后一个字节留在状态里等待下次 Feed，不报错。par 切点在 `\r\n` 之间：worker 以 `cr` 残尾结束本段、不计行尾；下段首字节为 `\n` 时确认行尾，否则在该字节处报孤立 CR（位置指向 `\r`）。Close 时仍处 `crPending`：按孤立 CR 报错（位置为该 `\r`）。引号内的 `\r` 是内容，无待定状态，原样保存。

## 4. 「立刻」拒绝的计数

字段字节计数：未引号字段对每个内容字节 +1；引号字段从开引号后的第一个字节起计，`""` 两字节只产出一个 `"` 字符，**按产出的一个字符计 1**（即第二字节不再加），但该判定发生在第二字节到达时，于是超限定位于第二字节。计数在追加前判断：一旦计数将超过上限，立即返回字段超限错误，解析器进入终态，已产出记录保留，之后 Feed 返回同一错误。记录字段数在上限检查放在字段产出瞬间；记录数检查放在记录闭合瞬间，均不等整段缓冲。

## 5. 状态转移表

字节类别：`c`=逗号，`q`=`"`，`n`=`\n`，`r`=`\r`，`o`=其他。

| 状态 | c | q | n | r | o |
|---|---|---|---|---|---|
| fieldStart | 发空字段→fieldStart | fieldQuoted | 发空字段+记录→fieldStart | crPending | 起未引号字段→fieldBare |
| fieldBare | 发字段→fieldStart | ErrBareQuote | 发字段+记录→fieldStart | crPending | 追加 |
| fieldQuoted | 追加 | fieldQuoteEnd | 追加 | 追加 | 追加 |
| fieldQuoteEnd | 发字段→fieldStart | 追加 `"`→fieldQuoted | 发字段+记录→fieldStart | 发字段→crPending | ErrQuoteJunk |
| crPending | （仅续传）发字段→fieldStart | ErrBareCR | 发字段+记录→fieldStart | ErrBareCR | ErrBareCR |

Close：fieldStart 且无悬挂字段→正常结束；fieldBare/fieldQuoteEnd→补发字段；fieldQuoted→ErrUnclosedQuote；crPending→ErrBareCR。

## 6. 最小引号必要性

字段含 `,` `"` `\r` `\n` 必须加引号；`Cell.Quoted==true` 的空字段必须写 `""`（保留标记）；其余不加引号。每条记录都写终结 `\n`，故单列未引号空值也能往返。
