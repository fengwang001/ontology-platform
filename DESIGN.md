# DESIGN

## 1. 末尾换行策略
先做核心规范化 C：行尾（`\r\n`、`\r`、`\n`）→`\n`；删除每行尾连续空格/制表符。C 幂等且输出无 `\r`、无行尾空白。策略只调整末尾：
- Keep：保留 C(x) 原样。
- EnsureOne：C(x)=="" → `""`（若空输入造 `"\n"`，则 "" 与 "\n" 被强制合并为同一输出，N 不再是幂等投影；选 "" 则 N 是纯投影，N∘N=N）。非空时：末尾无 `\n` 则补一个，多个则压成一个。
- TrimEmpty：剥掉全部末尾空行；仍有非空内容才补一个 `\n`；空→""。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

三策略输出都属于 C 的不动点集，故各自幂等；EnsureOne 对无换行内容（如 "a"）补尾换行，补后再归一化仍只一个换行。

## 2. 被删字节的 ToOut
设删除区间（按原文序，合并相邻）为 [s,e)，前缀删除总字节 c_k。幸存字节：ToOut(i)=i−D(i)，D(i)= 原文偏移 <i 的被删字节数。
被删偏移 i∈[s,e)：取其后第一个幸存字节的输出偏移，即 ToOut(i)=ToOut(e)（其后无幸存则为 L）。
理由：要求 ToOut 单调不减（区间内取常数、贴右边界）且 ToOut(ToOrig(o))=o——下一个幸存字节 s'=e 满足 ToOrig(其输出位)=e，ToOut(e)=同一输出位，互逆成立；若贴左边界则落到删除区间自身，ToOrig 不返回它，右逆失败。
因此 `\r\n` 的 `\r` 与行尾空白都映射到紧随的 `\n` 的输出偏移：编辑器光标落在被删字节上时，定位到换行符所在位置（换行**之前**的间隙），而不是换行之后的下一行行首。流末被删空白映射到终点 L。
ToOrig(o)：求最大 k 使 o+c_k ≥ e_k（c_k 为前 k 段删除总字节），二分；ToOrig=o+c_k，o=L 时为 n。

## 3. 空白缓冲超限
待定空白串在见到行尾/EOF 前无法判定，必须整串缓冲。
强制判定为"非行尾"立即输出：若其后真出现换行，该空白实为行尾空白，输出将含行尾空白 → 违反语义 5（N(N(x))≠N(x)）与"输出不含行尾空白"。
故选择**拒绝**：挂起长度与 Write 切分无关（跨 Write 累积、不中途刷出），任何喂入方式都在同一原文偏移触发 ErrWSPending，已输出内容保留，实例进入终态（再写 ErrTerminal），故不违反语义 4；拒绝路径不产生"成功输出"，语义 5/6 只约束成功路径。默认上限足够大，可配置；0 表示不限。
截断 Close：挂起 `\r` 单独成行→输出 `\n`；挂起空白后无行尾→是流末行尾空白，删除。与"对截断原文整体规范化"逐字节一致。

## 4. par 拼接
各段 worker 以非终态（stage）规范化：段末状态只有两种挂起——待定空白串，或末字节单独 `\r`（`\r` 后若来空白，`\r` 已判为孤行尾，故二者不并存），挂起原文长度 ≤ 空白上限+1，有上界。
拼接按段序折叠：上一段挂起字节 carry 作为前缀喂给本段的修正规范化器，状态在本段**第一个行尾之后**立即与"干净起点"同步；carry 为空则直接采用 worker 结果。仅最后一段套用全局末尾策略。映射为每段坐标加全局平移（原文=段起点，输出=前段输出总长）；修正器产生的区间按 carry 长度 c 分段平移（<c 用 carry 的原文基址，≥c 用段起点）。每段至多重算到首个同步点（含 carry ≤ 上限+1 字节），故修正总量有上界；错误偏移同理平移，严格 NUL/超限在两种执行路径上报同一全局偏移。
