# 设计推导

## 1 空行与单列空值
空行（紧邻的两个行终止符）**跳过**，不产生记录：否则回写器无法表达“这里本来没有记录”，
且 EOF 前最后一个终止符必须不产生记录，规则统一为“终止符只结束已开始的记录”。
记录开始的标志：段首第一个字节，或上一条记录结束后见到的任意非终止符字节。
推论：单列表里一条值为空的记录无法写成裸空串（`\n` 只会被当成空行跳过），
**必须写成 `""`**（带引号空字段）；多列中的未引号空字段不受影响。

## 2 par 切点
每段在两种入口假设下各跑一次词法：E=记录起点、Q=引号字段内（`""` 中、裸字段内不是独立入口，
因为它们由段内首字节即可判定并在进入前归一：裸字段内等价于“续接一条开记录”，首字节 `,`/终止符照常）。
第 0 段真实入口为 E。段 i 的真实入口由段 i-1 在其真实入口下跑完后的末态唯一决定：
末态为引号中（含刚见引号）→ Q，否则 → E。因此每个字节最多被扫描两次，总量 ≤2N+O(K)。
记录号/字段号在拼接时按“段首已结束记录数/下条记录已有字段数”重编号；字节偏移直接加段起点
（词法器全程吃绝对偏移）。Q 入口的首字段与上段末开字段合并（值、引号位、起始偏移取前者）。
语法错误在选定假设后即为全局首个错误，坐标随之换算；上限错误在拼接器选定假设后按同样的
每字节增长判定，位置与流式完全一致。

## 3 CR 待定
状态 CR 只在“引号外、字段中见到 \r”进入。Feed 在 CR 结束时保留该状态（不报错、不结束记录）；
下一字节为 `\n` 则结束记录，否则立刻报裸字段 CR 错误（孤立 \r 是数据损坏，不可吞）。
Close 时仍在 CR → 同样报孤立 CR。切点恰在 \r 与 \n 之间时，上段末态为 CR（非引号中），
下段入口判为 E 假设；E 假设扫描器提供 CR 续接入口，首字节 `\n` 正常收尾，其余字节报错。
引号字段内的 \r 是普通内容，不进 CR 态。

## 4 上限“立刻”
字段计数 = 写进该字段的内容字节数，引号转义 `""` 计 1（两字节只 append 一个 `"`），
字段内 \r\n 计 2 并原样保留。词法器在**每次 append 前**比较 `len==max`：第 max+1 个内容字节
到达的同一字节即返回 ErrFieldTooLong，不缓冲后续；记录数在记录闭合瞬间计数并检查；
字段数在字段闭合（逗号）瞬间检查。报错即终态，记录已闭合的保留，开记录丢弃，之后 Feed 回同一错误。

## 5 状态转移表
状态：S0 记录起点；B 裸字段；Q 引号内；QQ 引号后刚见引号；CR \r 待定。
字节类：c 逗号 q 引号 r \r n \n x 其他。动作用 +=追加、f=字段闭合、R=记录闭合、skip=空行跳过。

| 状态 | c | q | r | n | x |
|---|---|---|---|---|---|
| S0 | f→B(空字段) | →Q(引号字段起) | →CR | R+skip→S0 | +=x→B |
| B | f→S0 | Err 裸引号 | →CR | R→S0 | +=x→B |
| Q | +=c→Q | →QQ | +=\r→Q | +=\n→Q | +=x→Q |
| QQ | f→S0 | +=“→Q | →CR(待判定) | R→S0 | Err 闭后杂字符 |
| CR | Err 孤立\r | Err | Err 孤立\r | R→S0 | Err 孤立\r |

QQ 行细节：QQ+c=字段闭合；QQ+q=转义引号追加并回 Q；QQ+r 进 CR（字段已闭合待判定）；
QQ+n 结束记录；QQ+其他（含 x）报“闭合后杂字符”。CR+c：\r 已确认为孤立 CR，先报 ErrBareCR
（逗号不能救活它）。S0 表示“当前记录尚无字段开始”，故 n 为空行跳过；B 中 n 结束一条含字段记录。
