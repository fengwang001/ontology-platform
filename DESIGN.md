# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`，因此合法编码唯一。

## 严格解码拒绝清单（逐条推导）

1. 显式次数 1（`1a`）：Decode=`a`，Encode=`a` ≠ `1a`，拒绝。
2. 次数 0（`0a`）：次数必须 ≥1；Decode 为空但输入非空，重新编码为 `` ≠ `0a`，拒绝。
3. 前导零（`01a`、`00a`）：Decode=`a`/``，规范编码为 `a`/``，短形式不带前导零，拒绝。
4. 相邻同符号游程（`2a3a`）：Decode=`aaaaa`，最长游程合并且 Encode=`5a` ≠ 原串，拒绝。
5. `\` 后非数字/非反斜杠（如 `\n`、`\中`）：该转义不是合法符号，无可定义语义，拒绝。
6. 末尾孤立 `\`：没有符号，游程不完整；无法重新编码，拒绝。
7. 末尾只有次数（`12`）：游程缺符号，拒绝。
8. 非法 UTF-8（裸 `\x80`、残缺多字节序列）：输出必须是合法码点序列，拒绝。

各类错误均可 `errors.Is` 判定，并带字节偏移（定位到该游程起点/违规字节）。

## 大次数取舍

不设硬性次数上限，按**输出字节预算**处理：默认 1GiB，可通过 `Option` 配置。
次数解析以 `max(uint64)` 为界逐位检测溢出，绝不静默回绕；完成游程前计算
`次数×符号UTF8长度`，超过剩余预算则返回 `ErrOutputLimit`，此前不按次数分配内存，
输出只按实际已接受字节追加。因此 `99999999999999999999a` 得到可判定错误而非崩溃。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 往返 | TestRoundTrip |
| 2 拒绝清单与偏移 | TestDecodeRejects |
| 3 大次数/预算 | TestLargeCount |
| 4 最长游程/不规范化 | TestEncode、TestRoundTrip |
| 5 跨切分点一致 | TestSplits |
| 计数器复杂度 | TestCheckedBytes |
