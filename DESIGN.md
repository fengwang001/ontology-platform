# DESIGN — 统一格式差分/渲染/补丁器设计推导

## 1. 零计数 hunk 头的行号（语义第 4 条）

三个样例：

- `x\n` → `y\nx\n`：头为 `-0,0 +1`。插入发生在旧文件第 1 行**之前**，写的却是 0。
- `a\nb\nc\n` → `a\nb\nX\nc\n`：头为 `-2,0 +3`。插入发生在旧文件第 3 行之前，写的是 2。
- `a\n` → 空文件：头为 `-1 +0,0`。新文件为空，位置在"第 1 行之前"，写 0。

推导：计数为 0 时该侧不存在"首行"，GNU diff 写的是 **hunk 之前最后一行的行号**
（即 1-based 首行行号减 1；在文件首之前则为 0）。它不是"将要插入的位置"：
样例 2 若写插入位置应为 3，实际为 2；而"前一行行号"恰等于插入点的 0-based 下标
（第 2 行之后 = 下标 2），说明语义是"前一行"。`Apply` 解析 `-a,0` 时把插入点放在
旧文本第 a 行**之后**（0-based 下标 a 处）；`+c,0` 同理描述新文本侧。

| b     | 头 `-a,b` 的写法                       | Apply 对插入点的解释           |
|-------|----------------------------------------|--------------------------------|
| b = 0 | a = 前一行行号（文件首之前为 0），`-a,0` | 在旧文本 0-based 下标 a 处插入 |
| b = 1 | a = 该行行号，写 `-a`（省略 `,1`）      | 从 0-based 下标 a-1 起匹配     |
| b ≥ 2 | a = 首行行号，写 `-a,b`                 | 从 0-based 下标 a-1 起匹配     |

## 2. hunk 合并阈值 = 2C（语义第 5 条）

每个 hunk 向改动之间的空隙各延伸 C 行上下文，两侧共覆盖 2C 行。空隙为 g 行时：
g ≤ 2C 则两侧上下文相接或重叠，GNU diff 合并为一个 hunk；g = 2C+1 时中间恰有
1 行不属于任何一侧的上下文，必须拆分。例：C=3、g=6 时上文 3 行加下文 3 行恰好
铺满空隙，合并；g=7 时第 4 行悬空，拆成两个 hunk。
若阈值取 C：g 在 C+1..2C 时上下文已经重叠却仍拆分，与 GNU 不符；
若取 2C+1：g = 2C+1 时中间有未覆盖行却仍合并，同样与 GNU 不符。故阈值为 2C。

## 3. 最短脚本的并列选择（语义第 3 条）

Myers 贪心每一步在 `V[k-1] == V[k+1]` 的并列情形下选择**向右走（删除）**，
即优先删除、后插入（代码中用严格小于号 `<` 实现，相等时落入删除分支）。
效果：同一 hunk 内相邻的 `-`/`+` 行中 `-` 行排在前。例：`a b` → `b a`
渲染为 `-a`、` b`、`+a`，与 GNU diff 一致；若优先插入会得到 `+b`、` a`、`-b`。

## 4. 偏移应用后后续 hunk 的平移（语义第 7 条）

要平移，平移量 = 已应用 hunk 的实际位置 − 记录位置（即偏移 k），**不**再累加
该 hunk 的行数差。原因：匹配是在**原始文本**上进行的，后续 hunk 记录的行号同样
针对原始文本；偏移 k 反映的是目标文件相对补丁基线的整体位移，该位移对后续
位置同样成立。行数差只影响输出文件的行号，不影响在原文中的查找起点。
