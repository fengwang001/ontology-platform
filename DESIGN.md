# Quoted-Printable 设计说明

## 行末空白（语义 3 之一）
空格/TAB 先按字面处理；仅当前瞻到的下一个字节是换行（`\n`，其前可带 `\r`）
或输入结束时，它成为行末空白，改写为 `=20` / `=09`。
故 `"a \n"→"a=20\r\n"`、`"a "→"a=20"`、`"a b"→"a b"`。

## `=XX` 不拆行（语义 3 之二）
输出以令牌为单位：普通字节 1 字符、`=XX` 3 字符、硬换行 `\r\n`、软换行 `=\r\n`。
提交令牌前若 `列位置+len(令牌)>76`，先发 `=\r\n` 再发令牌。
因此 col=74 遇 `=`（输出 `=3D`）时 74+3>76，先软换行，`=3D` 整体落新行。
单字符令牌在 col=75 时：其后是硬换行/EOF 则正常收尾成 76 字符合法行，
否则先软换行，使下一行以该字符开头；该判定仅前瞻一次。

## 软换行前的空格
结论：保持字面空格，不写 `=20`。理由：解码器删除的是软换行序列 `=\r\n`
（等号连同其后的 CRLF），删除后该空格直接与续行内容相连，已不再处于行末。
只有面向硬换行 `\r\n` 或输入结束的空白才必须编码；若此处也写 `=20` 则属多余转义。

## 往返等式（语义 6）
令 `N(x)` 为把每个 `\n`（无论是否有前导 `\r`）规范化为 `\r\n`、独立 `\r` 保留
（编码时它是 `=0D`）的变换，则准确等式为：
`Decode(Encode(x)) == N(x)`。仅当 `x` 不含裸 `\n`（行终止已是 `\r\n` 或无换行）时才等于 `x`。

## 最小转义与幂等（语义 7）
编码器只转义：`=`；33–126 之外的字节（空格/TAB 与换行除外）；硬换行/EOF 前的空白。
软换行只在 76 字符上限处插入，无任何额外转义。故对合法文本 y 重编码时，
`Encode(Decode(y))` 与 y 的差异只能来自：(a) y 多余转义了可打印字符；
(b) y 含与长度无关的多余软换行——它被解码删除，而规范重编码不会重新产生。

## 语义—测试对照表
（最后补齐）
