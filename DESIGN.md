# 设计推导

## 1. 零计数时的行号（语义 4）

hunk 头里 -a,b 与 +c,d 的计数都按「该 hunk 涉及的行」算，而行号是
**涉及范围内第一行的行号（1 起）；范围为空时，取空范围所夹位置的前一行行号**。

三个样例：

1. 空到 y 换行 x 换行 在 C=0 下纯插入，插入点在第 1 行之前，空范围夹在
   「文件开头」与第 1 行之间，前面没有行，故旧侧为 -0,0。
2. a/b/c 三行到 a/b/X/c，在 C=0 下插入点在第 2、3 行之间，旧侧空范围
   的前一行是第 2 行，故为 -2,0，新侧第一行是 3，故 +3。
3. 仅 a 一行到空，在 C=3 下旧侧涉及唯一的第 1 行（删除），故 -1（计数 1
   省略 ,1）；新侧空范围前面没有行，故 +0,0。

所以它不是「将要插入的位置」（位置语义在两行间没有唯一行号），而是 GNU 的
约定：空范围行号 = 其前一行行号（开头处为 0）。Apply 读到计数 0 时，插入点
取「行号 a 之后」：a=0 插在文件开头；a>0 插在第 a 行之后。

| 旧侧 | 书写 | Apply 解释 |
| --- | --- | --- |
| b=0 | -a,0（a=前一行行号，开头为 0） | 在第 a 行之后空插入点放置新行 |
| b=1 | -a（省略 ,1） | 从第 a 行起替换/删除 1 行 |
| b>=2 | -a,b | 从第 a 行起涉及 b 行 |

新侧 +c,d 完全同理（样例 3 的 +0,0）。

## 2. hunk 合并阈值（语义 5）

相邻两段改动间隔 g 行未改动行。合并后需要的上下文是「前改动尾部 C 行 + 间隔
+ 后改动头部 C 行」。当 g <= 2C 时，两段各取 C 行上下文已经相接或重叠
（g=2C 时间隔恰好 2C 行，两侧上下文正好各占一半、无多余），GNU diff -U C
输出**一个** hunk；g=2C+1 时中间至少剩 1 行纯粹未改动行，拆成两个 hunk。
阈值即 **2C**：g<=2C 合并，g>2C 拆分。

反例取 C=1：若阈值是 C=1，则 g=2 会被错误合并，而 GNU diff -U1 对 g=2
输出两个 hunk；若阈值是 2C+1=3，则 g=3 会被合并，但 diff -U1 对 g=3 仍
输出两个 hunk。故只有 2C 正确（C=0 时阈值 0，即间隔 0 行才合并，与
GNU diff -U0 一致）。

## 3. 并列最短脚本的选择（语义 3）

Myers 沿 k 线推进。同一 d、同一 k 上，来自向下（删除，k-1）与向右（插入，
k+1）的代价相同时，**固定优先向下（先删除）**。回溯时采用同一规则，得到
a b 到 b a 的脚本：删 a、删 b、增 b、增 a（删除整体在前、插入整体在后），
而不是交错排列。hunk 内同位置的删除行总是排在插入行之前。该选择写死，故
同一输入任意次渲染逐字节相同。

## 4. 偏移后后续 hunk 的起点（语义 7）

hunk i 以相对记录位置 +k 应用成功后，hunk i+1 的查找起点平移 **+k**：
记录的行号编码的是补丁生成时的绝对期望位置，前一 hunk 净增减（插入行数减
删除行数）已体现在观察到的位移 +k 中，平移量就是该位移本身。搜索范围 =
平移后起点加减 F；候选按与平移后起点的距离取最近，并列取靠前。
