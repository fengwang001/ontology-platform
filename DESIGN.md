# DESIGN — 确定性跳表与排名查询

## 1. 层高的确定性导出

导出函数：`Level(k) = 1 + TrailingZeros64(FNV1a64(k))`，空哈希时把最高位置 1 兜底，
故返回值域为 `[1, 64]`；`list` 再用可配置的 `maxLevel` 上限拒绝超限键。

- **分布合理性**：FNV-1a 雪崩良好，其低位比特近似独立均匀。`tz(h) >= j` 的概率为
  `2^-j`，故 `P(Level >= j) = 2^(1-j)`：每层期望节点数减半，期望层高为 2，n 个元素的
  最高层期望约 `log2(n)`。既不退化成纯链表，也不会全是高层。实测分布见文末表。
- **同键同层**：`Level` 是键的纯函数，不读随机源、不读结构状态、不读插入顺序，
  因此同一键在任何构建过程中层高恒定——这是不变量 2 的前提。
- **重复元素**：同一键的所有副本得到相同层高，塔形一致；插入时用严格 `<` 定位，
  等键副本在底层相邻，相邻等键节点在任意层的跨度恒为 1，与副本插入先后无关，
  故重复元素不破坏跨度不变量，也不破坏结构唯一性。

## 2. 删除时的跨度回收

删除节点 x 时，对 x 出现的每一层 i：令前驱 `update[i]` 吸收 x 的跨度，
`update[i].span += x.span - 1`（-1 即回收 x 自身占的那个底层位置），前驱指针改指
x 的后继。**选择并入前驱而非后继**：排名语义是「指针跨过多少底层元素」，前驱的指针
现在跨过了原来 x 跨过的元素，加法自然闭合；若并入后继，后继的 span（从后继起算）
无法表达「向前多跨」。**x 恰为某层最后一个节点**时：约定空指针跨度为 0（不跨任何
元素），此时 `x.span = 0`，机械套用公式会给前驱 -1，故特判——前驱指针置空、跨度
置 0，与「空指针跨 0 个元素」的约定自洽。x 不出现的层：前驱 span 减 1（空指针不动）。

## 3. RankOf 对不存在的键

返回「它若插入会在的位置」，即**严格小于 k 的元素个数**，而非报错。推导：不变量 3
要求 `Range(lo, hi)` 的计数等于 `RankOf` 差值且三者不矛盾。若 `RankOf` 对缺失键报错，
则 `Range` 在 lo/hi 不存在时无法定义，违反「lo/hi 都不存在时仍正确」。取插入位置后：

- 存在的键：`RankOf` = 第一个副本的下标（等键相邻且插入位置在等键段之前），与 `At` 互逆；
- 缺失的键：`RankOf` 单调可减，`Range(lo,hi) = RankOf(hi) - RankOf(lo)` 恰为
  `{x : lo <= x < hi}` 的计数，对存在/缺失的 lo、hi 一致成立。

## 4. 迭代器失效判定

选择**立即失效**（fail-fast），不做快照。`list` 维护单调递增的版本号，每次写
（Insert/Delete 成功）加 1；迭代器创建时记录版本，每次 `Next` 先比对版本，
不一致即返回哨兵错误 `iter.ErrInvalidated`。最坏情形「迭代中删除的恰是迭代器当前
指向的元素」：删除必升版本号，迭代器下一次 `Next` 在任何解引用之前先发现版本不一致
而报错，因此绝不会沿已脱离结构的节点继续走，也不会交出半新半旧的序列；版本号单调
不归零，不存在「改回去就检测不到」的 ABA 漏洞。已返回的元素属于旧快照语义、未返回
的一律不返回，行为可判定。

## 5. 层高分布实测（键为 "k%06d"，FNV-1a）

| 元素数 | L1 | L2 | L3 | L4 | L5 | L6 | L7 | L8 | L9+ |
|---|---|---|---|---|---|---|---|---|---|
| 1000 | 1000 | 500 | 250 | 125 | 64 | 34 | 17 | 10 | 11 |
| 100000 | 100000 | 50000 | 25000 | 12500 | 6250 | 3115 | 1576 | 794 | 792 |

（L9+ 为 9 层及以上合计；逐层减半符合 `P(L>=j)=2^(1-j)`，最高层 12/16 随 log2(n) 增长，未退化。）
