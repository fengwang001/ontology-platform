# 设计说明

## 1. 无限制转置距离：推导与结论

**常见错误递推式（受限转置 OSA）**：

```
d[i][j] = min(
  d[i-1][j] + 1, d[i][j-1] + 1,
  d[i-1][j-1] + [a[i]!=b[j]],
  若 a[i]==b[j-1] 且 a[i-1]==b[j]: d[i-2][j-2] + 1)
```

错误点：它只把转置当成「当前相邻两格的一步交换」，交换后的两个码点被冻结、不能再被编辑，每个位置至多参与一次转置。

**本文采用的无限制递推式（Lowrance–Wagner）**。另维护两个索引：
`i1 = lastA[b[j]]`：码点 b[j] 在 a 中上一次出现的行；`j1 = lastB[a[i]]` 同理。

```
d[i][j] = min(
  d[i-1][j] + 1,                            // 删除
  d[i][j-1] + 1,                            // 插入
  d[i-1][j-1] + [a[i]!=b[j]],               // 匹配/替换
  d[i1-1][j1-1] + (i-i1-1) + 1 + (j-j1-1))  // 转置
```

转置项含义：在 i1 与 i 之间删除 i-i1-1 个中间源码点，交换两端两码点（代价 1），
再在 j1 与 j 之间插入 j-j1-1 个中间目标码点。交换后的码点仍可继续编辑，故为无限制。

**逐格说明 CA → ABC（a 为行方向）**：格 (i=2, j=3)，a[2]=A、b[3]=C：
i1=lastA[C]=1，j1=lastB[A]=1，转置项 = d[0][0]+(2-1-1)+1+(3-1-1) = 2，
即 CA 交换为 AC 再插入 B 得 ABC——交换之后仍允许插入 B，答案 2。
OSA 在此格要求 a[2]=b[2]（A=B）才允许转置，条件不成立，只能走代价 3 的路径，故错误给出 3。

## 5. 并列时的固定选择规则

DP 取 min 与回溯一律按下列优先级取第一个达到最小值的来源：
**匹配/替换（对角） > 删除 > 插入 > 转置**。等长最优脚本因此唯一、确定、可复现。

## 语义—测试对照表

（实现完成后补填）
