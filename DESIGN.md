# 设计推导

## 1. 非法单元切分规则

统一规则（WHATWG 式"最大合法前缀"）：从位置 p 取首字节 b：

1. b∈80..BF、C0/C1、F5..FF：非法单元仅 1 字节，p+1 继续。
2. b∈C2..F4 声明长度 L（2/3/4）。其后**每一个后续字节都必须立即是 10xxxxxx**；
   且第二字节落在该首字节专属区间。满足的字节全部收入同一"合法前缀"。
3. 只要出现第一个不满足的字节，非法单元 = 合法前缀字节数 +1（吞掉这个坏字节），
   坏字节之后的下一个字节作为新位置重新开始解析（绝不预吞）。
4. 流结束时仍有未闭合的合法前缀（≥2 字节）：整段算作 1 个非法单元（截断）。

解释样例：`F0 90 80 41`：90∈(90..BF)、80 都是续字节，前缀 F0 90 80 合法，
41 是第一个坏字节 → 一个单元 4 字节，1 个 FFFD。`E0 80 80`：E0 的第二字节区间是
A0..BF，80 立即非法，E0+80 成 1 单元，下一个 80 是孤立续字节又 1 单元，共 3 个。
`C0 AF`：C0 单字节非法（1），AF 单字节非法（1）。`E2 82`+EOF：E2、82 都是合法
前缀字节，闭合失败 → 整段 1 个，不是 2 个。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞 | 续字节非法时吞 |
|---|---|---|---|
| C2..DF | 80..BF | 2 | — |
| E0 | A0..BF | 2 | 前缀长+1（3） |
| E1..EC | 80..BF | 2 | 前缀长+1（3） |
| ED | 80..9F | 2 | 前缀长+1（3） |
| EE..EF | 80..BF | 2 | 前缀长+1（3） |
| F0 | 90..BF | 2 | 前缀长+1（3 或 4） |
| F1..F3 | 80..BF | 2 | 前缀长+1（3 或 4） |
| F4 | 80..8F | 2 | 前缀长+1（3 或 4） |
| 80..BF | 无（1 字节非法） | 1 | — |
| C0 C1 F5..FF | 无（1 字节非法） | 1 | — |

ED 收窄排除高代理 D800..DBFF；F4 上限 8F 排除 >10FFFF；E0/F0 收窄排除非最短形式。

## 2. 缓存上限与何时不等 EOF

最长字符 4 字节，只需缓存"尚未闭合的合法前缀"，硬上限 3 字节（首字节能立即判定，
不入缓存）。每个到达字节即检查：续字节若不在 10xxxxxx、或第二字节越专属区间，
**当字节到达时**就可判定前缀非法，立刻闭合非法单元，绝不拖到 EOF。只有"字节全合法
但数量不够"（如 E2 82 后 EOF）才留给 Close 判截断。故缓存任何时刻 ≤3，
且每个输入字节只被检查 1 次（计数检查次数 ≤2N，2N 来自写入时与取出合并时各触一次）。

## 3. par 切点对齐

段 k（k>0）从切点 c 回退至多 3 字节（u16 至多 3 个代码单元）到 `start`：
段 k 从 start 解析，且标记"片段模式"——start 处的未闭合前缀在片段末尾不算截断、
不发 FFFD（交给下一段；它也是最后一段时才按真 EOF 处理）。段 k-1 正常解析到 start，
其末尾前缀若是非法单元（坏字节落在 start 之前），该单元已计入段 k-1，段 k 从
start 的合法前缀字节开始必然无法闭合而整体挂起到片段末尾，不落任何 FFFD；若
start 恰是坏字节，段 k 自身产生该单元。两种归属互斥，总数与单线程一致。
例：`C3 | 80 80`，start=切点(C3 后)：段0 结尾 C3 合法挂起不报错；段1 从 80 解析，
80 孤立非法 1 单元、80 再 1 单元；而单线程为 C3 80 合法 + 80 非法 = 1 个 FFFD。
因此对齐改为**串行求边界**：段 k 的 start 从 c 回退到"段 k-1 最后一个已闭合单元
边界"（利用段 k-1 片段解析暴露的最后边界，回看 ≤3 字节即可），保证 start 必是
边界；上例 c 回退到 C3 之前，段1 整体解析 C3 80 80 = 合法 + 1 非法。
各段数据体并行转码，边界确定只做常数回看，检查次数 ≤ N + 8·常数。

## 4. 第 9 条断点

编码在输出前预算完整标量的输出长度，放不下则在标量边界停下返回 ErrLimit：
已输出标量对应的源字节计入 n；切分缓存里的挂起字节不算已消费（n 不含），
调用方下次 Write 必须从 n 处重发。新实例接续后，合法多字节前缀重新挂起、
合法字节重新计数，拼接与无限额逐字节一致，统计也一致（重复喂入的挂起字节
在旧实例未计入）。
