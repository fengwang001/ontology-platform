# 多版本可见性存储：读视图与回收器设计推导

状态全在进程内存；事务号只走注入的 `txid.Source`（单调、不回绕、零值非法）。
依赖方向单向：`txid <- version <- store`、`txid <- snapshot <- reclaim <- store`。

## 1. 可见性判定：为什么是左闭右开（对应要求 1、4）

快照隔离的两条公理：

- (a) 快照 S 只能看到「S 建立那一刻已提交」的版本；
- (b) 同一快照的重复读结果必须完全相同（可重复读）。

事务号由注入源单调分配，事务 T 在 `Begin` 时拿到 `id(T)`。快照 S 建立时
取 `point(S) = 源当前将分配的下一个号`。由单调性：任何在 S 建立后才开始的
事务 T 满足 `id(T) >= point(S)`；任何已提交事务 C 满足 `id(C) < point(S)`。

由 (a)，「已提交」的必要条件是 `id(C) < point(S)`，故右边界必须**开**：
恰好等于 `point(S)` 的号属于「S 建立后才分配」的事务，其提交一律不可见。
左边界天然闭：号最小的已提交事务（id=1）必须可见，没有更小的非法界。
但 `id(C) < point(S)` 不充分：事务可能「开始得早、提交得晚」，即 S 建立时
它仍在活跃集合 `active(S)` 里，当时尚未提交。由 (a) 它也不可见。于是：

```
visible(S, C)  <=>  id(C) < point(S)  AND  id(C) not in active(S)
```

该判定只依赖 S 建立时固化的 `(point, active)` 二元组，不随时间变化，
因此 (b) 可重复读自动成立：同一 S 对同一版本的判定是纯函数。
对单键版本链，可见版本 = 从新到旧扫描、第一个满足 `visible` 的版本；
删除标记是普通版本的一种载荷，命中即「不存在」，与「链中无任何可见
版本」（从未存在）是两种可区分结果。

## 2. 回收安全与增量策略（对应要求 5、6）

版本 v（提交号 c）被同键更新版本 v2（提交号 c2 > c）**完全遮蔽**后，
快照 S 仍可能看到 v，当且仅当 S 能看到 c 却看不到 c2。回收安全条件：

```
safe(v)  <=>  对所有活跃快照 S：visible(S, c2)  OR  not visible(S, c)
```

直接逐快照判定太贵，取充分保守条件。对每个快照定义
`floor(S) = min(point(S), min(active(S)))`。若 `c2 < floor(S)`，则
`c2 < point(S)` 且 `c2` 小于一切活跃号（故 `c2 not in active(S)`），即
`visible(S, c2)` 成立。于是令水位

```
W = min over 活跃快照 S of floor(S)   （无活跃快照时取当前 point）
```

则「`c2 < W` ⇒ 所有活跃快照都看到 v2 ⇒ v 可回收」。

**增量结构**：候选 `(key, c, c2)` 在「新版本提交遮蔽旧版本」的那一刻产生，
压入以 `c2` 为键的最小堆（`container/heap`）。单次回收只反复看堆顶：
`c2 < W` 则弹出并摘除版本，否则停止。考察数 = 弹出数 + 1（停堆那一次），
上界为 `O(可回收候选数)`，与键总数 N、版本总数无关——N=100 与 N=10000
对照测试中考察数完全相同。堆中候选总量 ≤ 被遮蔽版本总数，但单次回收
绝不触碰 `c2 >= W` 的候选，更不扫描任何键。

**最坏情况考察数上界**：`min(堆中候选数, 可回收数 + 1)`。

## 3. 崩溃点枚举（对应要求 8）

提交临界区（全程持锁）被拆成四个可注入崩溃的原子步，钩子按序触发：

```
P0  写索引前            —— 无任何痕迹
P1  索引已写、版本未追加 —— pending 索引指向不存在的版本体
P2  版本已追加（未提交标记）、提交记录未写 —— 链上有尸体版本
    （含多键写一半的中点：每次追加前都插钩子，测试逐次遍历）
P3  提交记录已写、索引未清理 —— 事务已完全可见
```

可见性有**唯一判定点**：版本的 committed 标记（P3 的提交记录）。P0–P2
崩溃时该标记未写，版本对一切快照天然不可见，不存在「半可见」；P3 崩溃时
标记已写，事务完全可见。重启后 `Recover` 遍历 pending 索引（只扫未决
事务，非全表）：无提交记录 ⇒ 按索引逐键摘除尸体版本（索引先于版本写入，
故尸体一定被索引覆盖，不会漏）；有提交记录 ⇒ 仅丢弃索引。两种结局都不
留残留，测试对全部崩溃点逐一注入并验证「全可见或全不可见」。

## 4. 水位推进与新快照建立的竞态（对应要求 7）

竞态：水位刚推过 `c2`，同一时刻新快照 S 建立，S 是否可能看不到 `c2`
却已读不到被回收的 v？排除方式是把临界区收缩到**同一把互斥锁**内：

- 快照建立（`BeginView`）：持锁读 `point`、拷贝活跃事务集、注册进
  `Registry`，三步原子完成；
- 水位推进（`Collect`）：持锁从 `Registry` 计算 `W` 再回收。

因此任意候选出堆时，「当时活跃的快照集合」是确定快照。分两种情形：

1. S 在出堆前已注册：`floor(S) >= W > c2`，由第 2 节 S 必见 `c2`；
2. S 在出堆后才注册：`c2` 在 S 建立前已提交（候选只在提交时产生），
   故 `c2 < point(S)` 且 `c2 not in active(S)`，S 仍见 `c2`。

两情形之外不存在第三个时序（锁把「注册」与「出堆」排成全序），故新
快照永远读不到已回收版本。水位本身只升不降：`Advance` 取
`max(旧水位, 新 horizon)`。单调性安全：候选的 `c2` 必为已提交号，任何
未来快照的 `point` 都大于它且它不在其活跃集，故「`c2 < W` 曾经成立」
⇒「对所有未来快照依然安全」，水位无需回退去保护后来者。
