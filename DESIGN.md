# 设计说明

## 规则 2：双引号内反斜杠何时是转义（推导）

POSIX 规定双引号内反斜杠仅在后随 `` $ ` " \ <换行> `` 时保留其转义意义：该反斜杠被删除，下一个字符成为字面量；
对其他跟随字符，反斜杠不特殊，原样保留。故进入 PD（双引号转义待定）态后，PD 只对上述 5 类字节"删除 `\` + 字面输出下一字节"，
其余字节"先补输出 `\`，再按 D 态处理当前字节"。特例：跟随换行时两字符均删除（续行），不输出、不标记词开始。

## 规则 4：空词（推导）

词由空白分隔；"一个词是否存在"取决于是否出现过任何字面内容**或**一次引号开闭——引号表示此处显式放置了内容（哪怕为空）。
因此：开引号即标记词开始（mark）。

- `""''` → **1 个空词**：`""` 标记词开始并闭合，`''` 与之相邻、中间无空白，按拼接规则并入同一词。
- `a""` → **1 个词 `a`**：空引号拼接到已有词，不新增词。
- 单独 `\<换行>` → **0 个词**：续行删除两字符，既无字面输出也不触发 mark，状态机回到"词未开始"，结尾无词可收。
- 对比 `a '' b`：空白分隔 + 中间的引号 mark，得到 `a`、`` ``、`b` 三个词；纯空白不 mark，故不产生空词。

## 状态转移表

状态：F=普通，S=单引号，D=双引号，P=F 中遇 `\`，PD=D 中遇 `\`。
动作：lit=字面输出(并隐含 mark)，mark=仅标记词开始，sep=分词，drop=删除。

| 状态 | 空白 | `'` | `"` | `\` | 换行 | 其他 |
|---|---|---|---|---|---|---|
| F | sep | →S,mark | →D,mark | →P | lit | lit |
| S | lit | →F | lit | lit | lit | lit |
| D | lit | lit | →F | →PD | lit | lit |
| P | drop+lit | drop+lit | drop+lit | drop+lit | drop+drop→F | drop+lit→F |
| PD | drop+lit→D | drop+lit→D | drop+lit→D | drop+lit→D | drop+drop→D | lit(`\`)+lit(b)→D |

收尾：F 收束当前词；S/D 未闭合 → 报错（偏移=开引号）；P → 结尾反斜杠错误；PD 视为 D 未闭合。

## 语义—测试对照表（最后补齐）

<!-- 待实现完成后回填 -->
