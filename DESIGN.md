# DESIGN — 批量写入的组提交与顺序保证器

## 包划分

- `req`：写请求 `Request` 与结果 `Result{Seq, Err}` 的类型定义。
- `batcher`：攒批。条数上限 / 字节上限 / 等待时长三者先到者触发，时钟可注入。
- `wal`：批次落盘与读回。自描述文件头 + 逐条长度前缀 + 批级 CRC32。
- `commit`：组提交编排。leader 协程落盘，其余等待，序号分配与回滚在此。
- `recover`：崩溃回放、截断分类、部分批次丢弃。
- `cmd/demo`：可运行演示，逐条打印判定。

## 一、序号分配时机（核心推导）

候选方案：
1. 到达即分配：批次落盘失败时序号已发出，产生空洞，且失败请求无法回收序号。
2. 落盘成功后分配：调用方在提交前无法知道自己与他人的相对顺序，且
   「序号」与「落盘内容」的对应关系要在日志上另行记录，复杂且易错。

推导出的规则：**序号在批次组装完成、落盘之前分配**。批次在内存中定稿
（成员、顺序、字节数都确定）后，leader 为其分配一段连续序号
`[base, base+n)`，再整批落盘。这样：

- 落盘成功：序号与日志内容一一对应，调用方拿到的序号就是它在日志中的位置。
- 落盘失败：**整批序号作废，计数器回滚到 base**，所有等待者收到错误。
  下一批从 base 重新开始，因此日志中序号区间连续无空洞（测试：第 2 批
  注入同步失败，第 3 批序号紧接第 1 批）。

回滚安全的前提：序号计数器只在 leader 协程内读写（提交循环为单协程），
无需锁，也不会与并发批次交错。

## 二、批次原子性

磁盘格式：

```
文件头: magic(4B "OWAL") | version(uint32)
批次:   count(uint32) | totalLen(uint32) |
        count 条 { len(uint32) | payload } | crc32(uint32, 覆盖 crc 之前的全部批次字节)
```

- CRC 是**批级**而非条级：一批要么整体通过校验、要么整体丢弃。条级 CRC
  无法表达「整批原子」，坏一条丢一条会破坏序号连续性。
- 回放时按批读取：任何一步读不完整（批头 / 条目 / CRC）或 CRC 不匹配，
  都丢弃该批及其后全部内容，恢复到「最后一个完整批次」。
- 单条请求超过批字节上限时**单独成批**（不被拒绝）：上限约束的是「攒」
  的行为，不约束单条的合法性；此时该批线上字节数可超过上限，文档明示。

## 三、leader 选择与唤醒不串台

- `batcher` 内部单协程循环：从通道收请求、按三个触发条件组批，把批次
  （含每条请求的结果通道）交给 `commit` 的提交循环。
- `commit` 的提交循环是**唯一的 leader**：只有它调用 `wal.WriteBatch`、
  分配与回滚序号、递增同步计数。其余协程只是把请求塞进通道后阻塞在
  自己的结果通道上，天然不存在多个落盘者。
- 不串台：每条请求携带**自己的**结果通道。leader 按批次内下标逐条回写
  结果，通道与请求一一绑定，唤醒后拿到的必然是自己那条（200 协程并发
  测试断言序号互不相同且与内容对应）。

## 四、资源与复杂度约束

- 同步次数 = 落盘批次数，远小于请求数。`commit` 内非导出计数器
  `syncs` 记录 `WriteBatch` 调用次数（每次落盘一次 write+fsync），
  由 `SyncCount()` 暴露。10000 请求 / 批上限 100 → 同步 ≤ 200。
- 批字节数按「线上字节数」（批头 + 长度前缀 + 载荷 + CRC）统计，
  `commit` 记录历史最大值 `MaxBatchBytes()`，常规批次不超上限；
  唯一例外是超限单条单独成批（见二）。
- 攒批三触发：条数达上限、累计字节达上限、首条到达后等待超时
  （注入时钟测试）。

## 五、故障语义

四类可判定错误（`errors.Is` 区分）：

- `ErrHeaderTruncated`：文件头不完整（截断点 < 8）。
- `ErrBatchHeaderTruncated`：批头（count|totalLen，8B）不完整。
- `ErrEntryTruncated`：条目区或 CRC 字段不完整（含写一半崩溃的批）。
- `ErrCRCMismatch`：字节数够但 CRC 不符（含位翻转）。

回放规则：遇到上述任一错误即停，可见数据 = 之前全部完整批次；
序号连续无空洞。下次写入从「最后完整批次的末序号 + 1」继续
（`commit` 打开已有日志时先回放恢复计数器）。

- 同步失败：注入 fsync 返回错误 → 整批调用方收错、序号回滚，
  且文件截回批前偏移，日志保持「只有完整批次」的不变量。
- 并发关闭：`Close` 与在途请求竞争时，已被受理的请求纳入最后一批
  落盘；未受理的收到 `ErrClosed`。测试断言：所有收到成功的请求
  都出现在回放结果里，不存在「成功但未落盘」。

## 六、边界语义（定义并测试）

- 零请求 Close：产生**只有文件头**的日志文件（8B），无空批次。
  选择「建文件即写头」使「文件存在 ⇒ 头合法」成为不变量，回放更简单。
- 单请求、批上限 1（逐条提交）、等待时长 0（立即成批）、空载荷
  （合法，长度前缀为 0）、超大单请求（单独成批，见二）。
