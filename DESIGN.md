设计推导（SAGA 编排器，module ontology）

1. 正向返回「超时/未知」错误：算成功还是失败？
前提：错误分两类——明确失败（确定无效果）与未知（超时，效果未知）；补偿动作必须幂等。
推导：
- 若算「明确失败」而不补偿：实际已生效时效果永久残留，违反不变量1的精神（已产生效果却
  永不补偿），且日志永久记为失败，Resume 也不会补偿。
- 若按「推定成功」处理：立即记 forwardSuccess 并进入逆序补偿。
  * 实际已生效：幂等补偿恰好撤销该效果，正确。
  * 实际未生效：对空效果补偿，幂等补偿必须把空结果定义为成功，正确。
结论：未知错误=推定成功，记 forwardSuccess（unknown 标记），参与逆序补偿；未知不重试
（重试可能在已生效后造成二次效果）。两种真实情况都收敛到正确终态，代价仅是未生效时
多调用一次幂等补偿——这是必要的保守选择。

2. 可重试步骤：重试几次？补偿是否重试？
- 正向：仅当步骤 Retryable 且错误为「明确失败」时重试，最多 MaxRetries 次尝试（含首次）；
  达上限仍失败则记一次 forwardFail 并开始补偿。未知错误不重试（见 1）。
- 补偿：同一套规则（Retryable 且明确失败才重试）。最终仍失败则记 compensateFail，
  继续执行下一个补偿，状态为「补偿带失败」。
- 与不变量3一致：重试发生在「日志尚未记录成功」之前，日志只追加一次结果；已记录成功的
  动作永不重放。Resume 对日志里 compensateFail 的步骤再试一轮属于"未完成工作"而非重放
  成功动作；全补偿成功后任意次 Resume 的调用次数不再变化。

3. Resume 起点（末条决定方向；全文只用于枚举失败补偿）
- 末条=第 k 步 forwardSuccess：若 k 是最后一步则已成功、空操作；否则从 k+1 步正向继续。
- 末条=第 k 步 forwardFail：k 未生效，从 k 开始逆序补偿 k-1..1（k 本身不补偿，不变量1）。
- 末条=compensateFail/Success(k)：补偿阶段，成功集合固定；对仍未 compensateSuccess 的
  已成功步骤逆序重试；compensateFail 步骤先重试，其后续（更早的）补偿已执行完，不重放。

日志最后一条记录          Resume 续跑起点
forwardSuccess(k), k<n    第 k+1 步正向
forwardSuccess(n)         空操作（实例已成功）
forwardFail(k)            补偿 k-1, k-2, ..., 1（k 不补偿）
compensateFail(k)         重试 k 的补偿，其余维持
compensateSuccess(1)      空操作（已完全补偿；带失败时先重试失败补偿再判空）

4. 已完全补偿的实例再次 Resume
选择空操作：每个成功步骤都有 compensateSuccess，无待执行动作；不报错（Resume 是幂等的
"推进到终态"操作）、不重跑（重跑违反不变量3）。状态为「补偿带失败」时 Resume 只重试
失败补偿；全部补偿成功后，任意次 Resume 间动作总次数不再变化，与不变量3一致。

复杂度：Resume 判定终态只读末条记录（journal.Last），读取条数 O(1)，不随日志长度增长。
是否存在未成功补偿由内存归约集合判断（在线 Run 已持有归约状态）。resumeReads 仅统计
本次 Resume 读取的日志条数（终态实例恒为 1）。

事实来源：归约函数 reconstruct(records, n) 同时供在线 Run 与离线重建调用；在线状态不保留
任何归约之外的私有事实。
