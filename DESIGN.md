# 设计推导

## 第 3 条：续行行只含空白时会怎样

`java.util.Properties` 的状态机里，遇到 `\`+行终止符会令 `precedingBackslash=true`，
随后重新进入主循环。主循环首先判断 `precedingBackslash`：若当前字符是空白
（space/tab/form-feed），就停在 `while` 里**连续丢弃空白**。该行被丢空后读到
换行，走到 `if (c == '\n')` 分支：`lineLen=0`，该逻辑行直接终止。

结论：续接行若整行只有空白（或为空行），它是逻辑行的**终止符**，且不追加任何
内容，也不会成为独立空行；其后下一行是新逻辑行。

这正是“续行首部空白被剥掉”规则的极端情形：规则不是“剥掉前导空白后若有内容就
追加”，而是“前导空白被丢弃；当空白耗尽遇到数据字符才继续拼接”。纯空白行上数据
字符永远不出现，于是换行关闭逻辑行。即“剥离首部空白”是状态机内联动作，剥离
本身不保证后面一定有内容。

## 第 6 条：Store 的最小必要转义

回写只转义“会改变 Load 结果”的字符。物理行每行是一个条目，所以键值中的
`\r` `\n` 必须转义（`\r`/`\n`），否则会拆成多行；`\` 必须转义（`\\`），否则会被
当成转义/续行引导。`\t`、`\f` 转义以避免被 Load 当空白处理。

键额外需要：`=`、`:`、空白（任意位置）必须转义，因为 Load 在键区遇到第一个未
转义的 `=`/`:`/空白就当作分隔符；键若以 `#` 或 `!` 开头必须转义，因为物理行首
（去行首空白后）的 `#`/`!` 会让整行被判成注释——只有**行首位置**有此风险，所以
键中间的 `#`/`!` 不用转义。空键直接以 `=` 开头（Load 读到分隔符即得空键）。

值额外需要：值以空白开头时，该空白必须转义——Load 在分隔符后会吞掉一段前导
空白，只转义首个空白即可让其后的空白作为值内容保留；值**中间**的空格不必转义，
因为分隔符判定早已结束，`#`/`!`/`=`/`:` 在值区也无特殊含义，故值从不转义这些
字符。非 ASCII 原样输出 UTF-8。

## 语义—测试对照表

<!-- 最后补 -->
