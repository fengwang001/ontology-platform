# URL 规范化与等价判定器 — 设计推导

模块划分：`pct`（百分号编解码）被 `segpath`、`query` 依赖；`host` 独立；
`canon` 依赖以上全部。依赖方向单向，无反向依赖。不使用 `net/url`。

## 第 2 条：哪些转义可以还原，哪些绝对不能

**推导起点**：一条转义可以还原为字面量，当且仅当还原后**不改变 URL 的
解析结构**——即切分 scheme/authority/path/query 的边界不变、路径段的数量
与边界不变、查询参数的键值切分不变。

URL 的结构由两类字符决定：

- **保留字符**（`/` `?` `#` `&` `=` `:` `@` 等结构分隔符）：解析器按它们
  的**字面出现**切分组件。若把 `%2F` 还原为 `/`，路径段数量就从 1 变成
  2——`/a%2Fb` 是"一个名为 `a/b` 的段"，`/a/b` 是"a 下的 b"两个段，
  对应不同的资源路径。还原它会改变段结构，按段做的 ACL 会被绕过
  （例如 `/admin%2F..%2Fsecret` 还原后段结构完全改变）。因此**一切保留
  字符的转义都必须保持转义**，只把十六进制字母统一成大写（`%2f` →
  `%2F`），因为 `%2f` 与 `%2F` 解码结果相同，只是同一转义的两种拼写。
- **非保留字符**（`A-Z a-z 0-9 - . _ ~`）：它们在任何组件里都没有结构
  含义，写为字面量还是 `%41` 完全等价（RFC 3986 §6.2.2.1 明确规定这种
  归一化）。不还原它们反而会造成**缓存穿透**：`/A` 与 `/%41` 是同一
  资源，若不合并，同一资源在缓存里占两个键。因此非保留字符的转义
  **必须还原**。

结论（`pct.Normalize` 实现）：逐字节扫描，`%XX` 解码得字节 b——
`IsUnreserved(b)` 则输出字面量 b，否则输出大写形式的 `%XX`；字面字节
原样通过。这样 `%2F` 永远保持 `%2F`，`/a%2Fb` 与 `/a/b` 规范形不同、
判定不等价；`%41` 永远变成 `A`。

## 第 3 条：`..` 吞掉与尾斜杠语义

**根之上的 `..` 为什么吞掉而不是报错**：`..` 的语义是"回退一级"。在根
位置已经没有可回退的层级，此时有报错与吞掉两种选择。`..` 回退是**相对
计算**，结果不依赖"从哪来"：`/../../x` 无论从哪一级开始算，落在根之下
的结果都是 `/x`。RFC 3986 §5.2.4 的 remove_dot_segments 算法正是这样
定义的——输出缓冲为空时遇到的 `..` 直接丢弃。把它当作错误会把"写法的
笨拙"与"资源的非法"混为一谈：规范化的职责是把同一资源的不同写法归一，
而不是校验调用方的路径算术。吞掉还有安全上的必然性：若允许 `..` 逃逸
到根之上，规范化结果就无法表示为合法 URL 路径，等于给路径穿越开门。
吞掉是**既不放大也不拒绝**的中性选择。

**尾斜杠为什么必须保留**：`/a` 与 `/a/` 的区别在于后者末尾多一个空段。
在大量真实系统里，`/a` 指向资源 a，而 `/a/` 指向"a 这个集合/目录的默认
视图"（静态服务器对目录与文件的区别、REST 里集合与成员的惯例）。规范化
器无法证明某个部署里两者相同，而判错等价会导致缓存穿透或越权，所以
**存疑时一律区分**：空段（连续斜杠）与尾斜杠都原样保留。同理 `/a/.`
消解后留下 `/a/`（`.` 删除但尾部的段位置留下空段），与 RFC 3986 的
参考算法一致。

## 第 4 条：`a`、`a=`、`a=%20` 三态如何保持

三者的比特级区别：

- `a`：参数里**没有等号**。解析为 `Item{Key:"a", HasValue:false}`。
- `a=`：有等号、值为**空串**。`Item{Key:"a", HasValue:true, Value:""}`。
- `a=%20`：有等号、值为**一个空格**。`Item{Key:"a", HasValue:true, Value:"%20"}`。

保持手段：`Item.HasValue` 记录等号是否存在；值经过 `pct.Normalize`，
空格不是非保留字符，`%20` 保持转义（不会变成空串，也不会变成 `+`——
本系统不采用 form 编码的 `+` 约定）。重建查询串时按 `HasValue` 决定
是否输出 `=`。于是三种形态两两规范形不同，`Equivalent` 判定彼此不等价。

**混淆的后果**：许多框架把 `?a` 解析为"标志位存在"、把 `?a=` 解析为
"参数 a 被显式置空"、把 `?a=%20` 解析为"参数 a 是空白字符串"，三者在
应用层可能分别映射到"使用默认值 / 清空 / 非法输入"。若规范化把它们
合并，缓存层会把"清空某字段"的响应回给"查询默认值"的请求（缓存污染），
鉴权层也可能把"显式空值拒绝"的规则错配到无等号形态上（越权）。

## 第 7 条：等价判定为什么满足传递性

定义：`Equivalent(a, b) ⇔ canon(a) == canon(b)`（字符串逐字节相等）。

`canon` 是一个**函数**：同一输入永远产生同一输出——无随机、无时间、
无可变状态参与结果；唯一共享状态是只增的字节扫描计数器，不进入输出。
字符串相等 `==` 是标准的等价关系（自反、对称、传递）。"经函数映射后
相等"仍是等价关系：

- 自反：`canon(a) == canon(a)` 恒真。
- 对称：`canon(a) == canon(b)` ⇒ `canon(b) == canon(a)`。
- 传递：`canon(a) == canon(b)` 且 `canon(b) == canon(c)` ⇒
  `canon(a) == canon(c)`，由 `==` 的传递性直接得到。

因此 `Equivalent` 把全部 URL 划分成等价类，每个等价类恰好对应一个规范
形字符串。测试用等价类穷举验证：组内任意两两等价、跨组任意两两不等价，
并单独验证自反、对称与一条等价链的传递闭包。

## 第 8 条：单遍扫描结构

管线是严格的**分层单遍**结构，每层对输入只做常数遍扫描，且**解码不
重复**：

1. **结构切分**（`canon.splitURL`）：对原始字节串一次顺序扫描，按
   `:` `//` `#` `?` `/` 切出五个组件，不做任何解码。
2. **组件处理**：authority 交给 `host`（一次扫描：小写、尾点、IPv6
   压缩、端口归一）；path 交给 `segpath`——按字面 `/` 切成段，每段交给
   `pct.Normalize` **恰好一次**（该函数内部单遍完成：转义校验、大小写
   归一、非保留还原、UTF-8 校验，同一字节不会被解码两次）；query 交给
   `query.Parse`——按 `&` 切分后每个键/值同样只过一次 `pct.Normalize`。
3. **重组**：把规范化后的组件拼接输出，长度不超过输入的常数倍。

每个字节被扫描的总次数 = 结构切分 1 次 + 所属组件处理 1~2 次 = **常数
遍**，总量 O(L)。不存在"对前缀反复重扫"或"对同一转义重复解码"的结构
（段切分在**未解码**的原文上进行，解码只在段内发生一次，没有先整体
解码再整体重编码的两遍流程）。

实测（`TestScanCountIsLinear`，非导出计数器经 `ScanCount()` 观察）：

- L = 1 028 字节：扫描 1 021 次
- L = 65 544 字节：扫描 65 537 次
- 长度比 63.6x，扫描比 **64.1x** —— 与长度成正比；若是平方复杂度，
  比值应接近 4 096x。

## 附：其余设计要点

- **幂等性**：规范形的每个组件都已是"终态"——转义只剩大写且仅用于
  非保留之外的字节、点段已消解、端口已归一、查询已按模式排序；对终态
  再跑一遍同一管线，每一步都是恒等映射。5000 个随机输入（含畸形）验证
  `canon(canon(x))` 与 `canon(x)` 逐字节相同。
- **空路径归一为 `/`**：带 authority 的 URL 空路径与 `/` 同指
  （RFC 3986 §6.2.3），合并它们是安全的。
- **资源上限**：长度、路径段数、查询参数数三类上限在**任何计数与状态
  推进之前**检查，超限即返回彼此可判定的错误（`ErrTooLong` /
  `ErrTooManySegments` / `ErrTooManyParams`），扫描计数器与既有结果
  零变化。
- **并发**：`Normalizer` 无可变共享状态（计数器为 `atomic.Int64`，仅
  观测用），`Inspect` 完全不触碰共享状态。
