# 设计推导

## 1. 空输入与末尾换行策略
规范化把输出切成「完整行 + 末尾残余（不含 `\n`）」。残余中的行尾空白也删除；残余为空表示文件以换行结束。
- Keep：保留末尾换行结构，残余保持原样（去尾空白）。
- EnsureOne：末尾恰好一个 `\n`；残余非空则补一个 `\n`，残余为空则不动。
- TrimBlank：删除全部末尾空行（连续 `\n` 收敛为 0），但非空行保留一个 `\n`；空文件仍为空。
幂等性来自：三种策略都只产出 `\n` 且无行尾空白，再次规范化时完整行集合不变；EnsureOne 的 `"\n"` 残余为空不补不减；空输入再规范化仍为空（选空而非 `"\n"`，否则 EnsureOne 会把空文件永久变成 `"\n"`；两种选择都幂等，选空更保守）。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut
要求 ToOrig 单调不减、ToOut(ToOrig(o))==o。输出偏移只对应保留字节，删除区间必须吸附到端点：删除段吸附到其后第一个保留字节的输出偏移（行尾空白与 `\r\n` 中的 `\r` 都吸附到该换行 `\n`）；末尾无后继则吸附到输出终点。
故被删空白与 `\r` 的 ToOut 等于该换行 `\n` 的输出偏移——光标落在被删空白时落在换行符之前（行尾位置），符合编辑器直觉。删除造成的输出空洞不存在，ToOrig 无需选择。

## 3. 空白缓冲超限
强制判定为「非行尾」会吐出空白：若其后真有换行，这批空白将永久残留为行尾空白，违反语义 5 且不可恢复；拒绝则不产生错误输出，调用方得到确切原文偏移，状态进入终态，已输出与映射保留可解释。故选拒绝（ErrWhitespaceLimit），拒绝前输出恰为已确定前缀的规范化结果，不违反 4/5/6。输出上限同理拒绝（ErrOutputLimit）。

## 4. par 拼接
段 k 区间 [b,e)。跨段唯一歧义是前段挂起的 `\r` 或未判定空白。段 k 额外获得 1 字节前瞻字节 data[e]（若存在）且 final=false；末段无前瞻、final=true。前瞻 1 字节足够：判 `\r|\n` 需 1 字节；判空白是否行尾只需「下一字节是否行尾起始符」，若是 `\r` 由其自身前瞻决定，链式传递每跳恰 1 字节，有界。
归属：非末段处理 [b,e]（含前瞻字节），末段 [b,n)，区间无缝、每字节恰属一段。拼接时把段 k 映射中 orig 端 > e 的部分（前瞻字节结果，至多开头数个 span）切下，按段长度差平移后并入段 k+1 开头（前瞻字节在属段 local 0）。span 只随删除点增长，总区间数有界于删除点数 + K。
