# DESIGN

## 1. 模型
路径按字节 `/` 切段，空段保留（不做任何规范化）。非空路径至少 1 段；空路径 `""`
是唯一的「0 段」路径。模式同样切段：普通段是原子（字面码点、`?`、`*`、字符类）
序列；整段恰好为 `**` 的段是 globstar 段，匹配「0 个或多个完整段」。

## 2. `x/**` 与 `**/x` 的推导
统一规则只有一条：普通段必须与路径段**一一对应**，globstar 段消耗「连续若干个
完整段」，段数可以是 0。于是 `/` 只是段边界，相邻边界自然塌缩：
- `a/**/b` 匹配 `a/b`：globstar 消耗 0 段，`a` 对 `a`、`b` 对 `b`。
- 同一条规则下 `x/**` 匹配 `x`（globstar 0 段）；匹配 `x/`（路径尾段是空段，
  globstar 消耗 1 个空段）；匹配 `x/y`。不匹配 `/x`（首段是空段而非 `x`）。
- `**/x` 匹配 `x`（0 段）；匹配 `/x`（globstar 消耗首空段，尾段对 `x`）；
  不匹配 `x/`（尾段是空段）。

## 3. 空串路径
空路径是 0 段。`*` 是「某一个段内」的量词，它必须绑定一个实际存在的段，段不
存在则不匹配，故 `*` 不匹配空串（它只匹配「恰好 1 段」的路径，段内容可为空）。
`?` 同理不匹配。`**` 的论域是段序列，允许 0 段，故 `**` 匹配空串。空模式只
匹配 0 段路径，即只匹配空串。

## 4. 无回溯算法
段内：经典「最后一个 `*` 单回退点」扫描。记最后星号位置 sp 与对应文本位置
si；失配时把 sp 之后的模式重新对到 si+1。正确性来自交换论证：对任意两个相邻
`*`，它们吃掉的文本逐字符重新分配不影响「剩余固定原子能否对上」——若存在一种
星号长度分配使后缀成立，则让更靠后的星号多吞一个字符、靠前星号少吃一个，匹配
性不变；可达文本位置恒为连续区间，故只需保存最后一个回退点。复杂度 O(段长)。
不能照搬到跨段：globstar 除「消耗 ≥1 段」外还有「消耗 0 段」转移（`a/**/b`
对 `a/b` 的塌缩），同一(模式位置,边界)上零消耗与正消耗重入并存，且普通段自带
内部结构、不是单码点谓词，回退状态不再是一个指针，而是「一组可达路径段位置」。
故跨段用段级可达集合 DP：逐模式段推进，普通段要求恰好消耗 1 段且段内匹配，
globstar 段做集合闭包（保留 0 段转移并沿段链扩张）。每个 (i,j) 摊还 O(1)，
总体 O(模式段数×路径段数)，含段内匹配为 O(模式字节×路径码点)，无回溯。

## 5. 规则集版本一致
规则全部编译成一个不可变快照（已编译匹配器切片 + 原文 + 版本号），存入
`atomic.Pointer`。查询开始时取一次快照指针，整次判定与 `Explain` 只用该快照，
故结论、序号、原文必属同一版本。替换先完整编译新快照：任一规则编译失败或超过
上限就直接返回错误，旧指针不动；成功才 `Store`。

## 6. 结果表（✓ 匹配 / ✗ 不匹配）
| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**`  | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x`  | ✗ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ |
| `*`     | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**`    | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b`| ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |

## 7. 错误与上限
四类可区分语法错误（具名哨兵，携带字节偏移）：未闭合类 `ErrUnclosedClass`、
颠倒区间 `ErrReversedRange`、末尾单反斜杠 `ErrTrailingBackslash`、空类
`ErrEmptyClass`。上限：模式最大字节数、最大 `**` 段数、规则集最大规则数。
