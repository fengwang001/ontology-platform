# 段索引与范围回放 设计

## 1. 布局
- 一个日志 = 一个目录，内含按序命名的段文件 `seg-000000` 与可选索引 `seg-000000.idx`。
- 段格式（自描述）：
  - 头：魔数(4) `ONSE`、版本(1)=1、首序号(8 大端)、事件数(4 大端)，共 17 字节。
  - 每条事件：长度前缀(4 大端，事件帧字节数) + 事件帧。
  - 事件帧：序号(8) + 载荷长度(4) + 载荷 + CRC32(4)，CRC 覆盖「序号+载荷长度+载荷」，IEEE 表。
- 追加只写活动段；轮转后该段只读。头里的事件数在轮转/Close 时回写；读到文件尾时
  以实际完整帧数为准（见 §5）。

## 2. 稀疏索引与定位推导
- 锚点：每 N 条事件记一个「序号 → 该事件长度前缀在段内的字节偏移」，首条必为锚点。
  索引文件 = 锚点数(4) + N(4) + 若干 (序号 8, 偏移 8)。
- 回放 [from,to] 必须选「不大于 from 的最大锚点」，从它的偏移顺序读到 from。
  - 不能选「最接近 from 的锚点」：最近锚点可能序号 > from，落在目标之后；从其后扫只会
    看到更大序号，区间开头的事件永久漏掉，且没有更早已读内容可回退（顺序文件、稀疏索引
    不保证更早锚点之间的逐条边界）。
  - from 恰好等于锚点：跳过 0 条，直接命中。
  - from 在两锚点之间：跳过 (from-anchorSeq) 条，1 ≤ 跳过 < N。
  - from 小于首锚点（首锚点即段首序号）：from 被夹到段首，跳过 0 条。
- 因此定位阶段跳过事件数恒满足 0 ≤ skip < N，证明索引确实被用上。

## 3. 索引可从段重建
- 索引里的每个字段都能从段重新推出：锚点序号即每 N 条帧解码出的序号；偏移即读该帧前
  文件光标的位置（长度前缀起点）；N 存于索引头，重建由调用方给定（对同一份段恒等）。
- 即索引不含任何「只在写入时可知」的信息（无时间戳、无随机量、无内存指针），所以
  删除 .idx 后顺序扫段可重建出与原文件逐字节相同的索引。

## 4. 段边界与跨段回放
- 段头记首序号 first 与事件数 count；合法日志满足
  下一段 first == 本段 first + count，序号全局连续无重叠无缺口。
- 回放按序打开覆盖 [from,to] 的段：中段读整段；首段定位到 from；末段止于 to；
  to 超过最大序号则读到末尾（不报错）。段间由连续性保证不重不漏。

## 5. 损坏分类与 repair
- 段从偏移 c（1 ≤ c < 文件长）截断后，顺序解码，按 c 落点分四类（哨兵错误，errors.Is）：
  - 头部不完整 `ErrTruncatedHeader`：c < 17。
  - 长度前缀不完整 `ErrTruncatedLength`：某 4 字节长度前缀读不满。
  - 事件体不完整 `ErrTruncatedBody`：长度前缀完整，但其后事件帧不足声明长度。
  - CRC 不匹配 `ErrCRC`：帧满但 CRC 校验失败（CRC 被截断也算此类，残帧校验必失败）。
- repair 顺序读到第一个上述错误，截掉残尾，把头中事件数回写为实际完整帧数；
  头部不整无法判身份，返回 ErrTruncatedHeader 不自动改。
- 索引锚点偏移若被改到事件中间：该偏移读不到合法长度前缀（越界/声明长度超界）或
  CRC 失败。定位检出后废弃该索引、从段首全段扫描，并在统计里标记 IndexStale=true。
- 序号缺口：repair 扫描所有段头，若 next.first != cur.first+cur.count，报
  `ErrSequenceGap` 并附缺口区间 [cur.first+cur.count, next.first-1]。

## 6. 边界语义
- 空日志（0 段）回放返回空结果；单事件、空载荷均为合法帧。
- from > to：`ErrBadRange`。from < 最小序号：夹到最小序号（从头放，不报错）。
- to ≥ 最大序号：放到末尾。区间可恰好一整段或跨任意多段。

## 7. 并发与读取计费
- 活动段写者与读者可并发：写者追加用一把互斥锁并在每条事件后 f.Sync；读者用
  os.File.ReadAt（独立 offset），长度前缀先按声明长度读取，读不满即视为「到一致前缀
  末尾」，因此绝不可能读到半条事件。
- replay 统计：SkipEvents 为定位跳过条数（< N）；EventBytes 为完整事件帧字节之和；
  上限约束：EventBytes ≤ 区间内帧长之和 + 一个锚点区间（N 条）帧长。
