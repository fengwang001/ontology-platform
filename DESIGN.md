# 设计说明

严格解码只接受唯一规范编码，判定不变量为 `Encode(Decode(t)) == t`。

| 拒绝情形 | 推导 |
| --- | --- |
| 显式次数 `1` | 接受 `1a` 会解码为 `a`，再编码得到省略次数的 `a`，不等于 `1a`。 |
| 次数 `0` | 接受 `0a` 会得到零个 `a`，该符号在再编码中消失，不可能回到 `0a`。 |
| 前导零 | 接受 `01a` 或 `00a` 会得到一个或零个 `a`，规范结果只能是 `a` 或空串。 |
| 相邻同符号游程 | 接受 `2a3a` 会得到 `aaaaa`，最长游程再编码为 `5a`。 |
| 非法转义 | 接受 `\x` 无法确定其符号；转义只规范表示数字和反斜杠。 |
| 末尾孤立 `\` | 该转义没有符号，解码结果不可能再产生这个结尾字节。 |
| 只有次数 | `12` 没有符号，不是完整游程；解码结果无法再编码回次数。 |
| 非法 UTF-8 | 编码只由码点产生；RuneError 会再编码成实际替换字符，而非原字节。 |

次数上限选择 `math.MaxUint64`。更大的十进制次数无法用标准库固定宽度整数无溢出表示；拒绝可在逐位累加时立刻发生，不按次数分配内存。`Decoder.SetMaxOutput` 限制解码输出总字节数，默认 1 MiB；超限在实际写出前返回可判定错误，因而 `99999999999999999999a` 安全失败。

流式解码器先暂存当前游程。遇到下一个符号或 `Close` 时，比较相邻符号并立即输出已确定游程；切分点只影响到达时间，不改变状态机和字节偏移。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 往返 | 待补 |
| 2 规范形式与严格解码 | 待补 |
| 3 大次数与输出上限 | 待补 |
| 4 最长游程与不规范化 | 待补 |
| 5 跨切分点一致 | 待补 |
