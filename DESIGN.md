# DESIGN

## 1. hunk 头零计数时的行号

`@@ -a,b +c,d @@` 中，普通段的 a 是「本段第一行在旧文件中的 1 基行号」。
当 b=0 时本段不含任何旧行，a 无法再表示第一行，统一定义为
**插入点之前已有的旧文件行数**（即插入点的 0 基行索引）：

- `x\n`→`y\nx\n`（C=0）：在第 0 行之前插入，故 `-0,0`；新侧第一行是第 1 行，d=1 省略，故 `+1`。
- `a\nb\nc\n`→`a\nb\nX\nc\n`：在旧第 2 行之后、第 3 行之前插入，故 `-2,0`，新侧为 `+3`。
- `a\n`→空（C=3）：反向情形，新侧 0 行，插入点前有 0 个新行，故 `+0,0`；旧侧第一行是第 1 行且 b=1，省略为 `-1`。

这解释了 `-0,0/-2,0` 的数字是「前一行的行号」（前面有几行），而不是「将要插入的位置（第几行）」。
Apply 遇到 b=0 时，把插入点放在旧文件第 a 行与第 a+1 行之间（a=0 即文件首，a=行数即文件尾）。

| 情形 | 书写 | Apply 解释 |
|---|---|---|
| b=0 | `-a,0`（始终显式） | a=前导旧行数；在第 a 行后插入 |
| b=1 | `-a`（省略 `,1`） | a=该行 1 基行号，替换/上下文中的这一行 |
| b≥2 | `-a,b` | a=首行 1 基行号，共 b 行（含上下文） |

新侧规则完全对称。

## 2. hunk 合并阈值

阈值是 **2C**：两段改动相隔未改动行数 g，g ≤ 2C 合并，g ≥ 2C+1 分开。
每个改动携带 C 行前置与 C 行后置上下文；g ≤ 2C 时两个上下文窗口相接或重叠，
中间不存在「未被任何窗口覆盖的行」，GNU `diff -U C` 输出一个 hunk。
C=3 时 g=6：`@@ -1,7 +1,7 @@`（合并）；g=7：两段之间留下 1 行完全不出现的行，分成两个 hunk。
若阈值取 C：g=4（≤6）会被错误拆开；若取 2C+1：g=7 会被错误合并（GNU 在此分开）。

## 3. 最短脚本的并列选择

Myers 对角递推 `x = max(down, right)` 遇到并列时固定选择 **right（先删除旧行）**。
`a b`→`b a` 因此产生 `-a / ' 'b / +a`（与 GNU diff 默认输出一致），
对称为「先插入」会产生 `+b / ' 'a / -b`。全局固定先删除保证确定性；
hunk 内行序即脚本的线性化顺序：同一步的右移（`-`）先于下移（`+`）配对时，`-` 块排在 `+` 块前。

## 4. 偏移后后续 hunk 的查找起点

第 i 个 hunk 的查找中心 = 记录起点 + 累计漂移。第 j 个 hunk 实际匹配点相对
「仅按长度差推算的预测点」的偏差 δj 全部累加：
`center_i = oldStart_i + Σ(Δlen_j) + Σ(δj)`。
必须平移：前一个 hunk 偏移 ±k 后，其上方/下方所有行在目标文件中整体平移了 k
（插入删除还带来长度差 Δlen，已计入），不平移会让后续本可成功的 hunk 白白越界。
距离仍按 |候选起点 − center| 比较，相等取靠前候选。
