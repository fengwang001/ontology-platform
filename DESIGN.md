# 通配符模式匹配器设计

范围：把含 `%`（任意串，含空串）与 `_`（恰好一个码点）的 SQL LIKE 模式匹配到文本。
仅标准库、进程内存、纯函数。包划分：`pattern`（解析/规范化）、`budget`（步数）、
`match`（匹配）、`audit`（朴素递归对拍）、`cmd/demo`（演示）。

## 1. 线性匹配：单向扫描 + 单回溯锚点

朴素递归把每个 `%` 当成“匹配长度 0..n”的分支，多个 `%` 时分支相乘
（`%a%a…%b` 对 20 个 `a` 呈 2^k 级退化）。

线性做法只维护一个锚点 `(starPat, starText)`：最近一个 `%` 的模式位置，
以及它当时“暂时吃掉”的文本位置。主循环单向推进：

1. 当前模式位是 `%`：记锚点为当前位置，模式前进一格，文本不动（先尝试吃 0 个）。
2. 普通字面量/`_` 与当前文本码点相容：两边各进一格（每次前进前计一步）。
3. 失配且存在锚点：模式回到 `starPat+1`，文本回到 `starText+1`，
   并把 `starText` 推进一格（让该 `%` 多吃一个码点），继续扫描。
4. 失配且无锚点：不匹配。模式耗尽后，剩余模式位必须全是 `%`。

**为什么一个锚点就够**：更早的 `%` 都是“贪婪可伸缩”的。设最近锚点为 S，
失配时只需要让 S 多吃一个码点；S 之前的更早 `%` 的任何长度分配，都不会改变
“S 之后这段固定模式前缀”与当前文本后缀的相对局面——那些前缀已经在锚点记录时
被成功匹配并固定下来。若延长 S 仍失败，说明 S 之后的固定段在文本中更靠前的
起点也已被同样的回溯逐一尝试过；要让更早的 `%` 多吃，只会把 S 的起点推后，
而推后起点等价于 S 自身多吃，已被“锚点文本逐格推进”覆盖。

**为什么不会漏解**：锚点每次失配只把文本起点推进一格，即按起点升序枚举
该 `%` 之后固定模式段的所有可能起点；起点存在则必被枚举到。因此这是完整的
迭代加深，而不是剪枝。无递归，迭代次数为 O(模式码点数 × 文本码点数)，
步数上界取 `4 * P * T`（P=折叠后模式长度，T=文本码点数）。

## 2. 相邻 `%` 折叠

解析时把连续的一个或多个 `%` 规范成单个 `%`：token 流层面
`% %+ -> %`，折叠发生在转义处理之后，故 `\%` 是字面量不参与折叠。

语义不变：`x%y` 表示“前后两段之间存在任意串（含空）”。两个相邻 `%`
的约束是“存在 s1、s2 任意”，拼接 s=s1+s2 仍是任意串（含空），
即 `%(%)* ≡ %`。归纳得任意连续 `%` 等价于一个。`%%%a` 与 `%a`
匹配同一集合，规范化后长度严格变短（空模式与无相邻 `%` 的模式除外）。

## 3. 单位是码点，不是字节

模式与文本都按 Unicode 码点（rune）对齐：`_` 匹配恰好一个码点
（`caf_` 匹配 `café`；一个四字节 emoji 也只算一个）；
字面量按码点比较；锚点推进按码点边界。字节只用于**报错位置**：
用 `utf8.DecodeRuneInString` 逐段解码，遇到 `RuneError` 即非法 UTF-8，
返回带字节偏移的可判定错误，并标明来自模式还是文本
（`errors.Is(err, pattern.ErrPattern)` / `ErrText`），绝不静默替换 U+FFFD。

## 4. 转义与错误分类

`\%`、`\_`、`\\` 为三个字面量；`\` + 其他字符、尾随单个 `\` 为模式错误。
三类哨兵错误：`ErrPattern`（模式侧：转义/UTF-8）、`ErrText`（文本非法 UTF-8）、
`ErrBudget`（步数超限），均可用 `errors.Is` 区分，错误值携带字节位置。

## 5. 确定性与纯度

步数只来自匹配过程中的确定性循环计数（无随机、无缓存、无全局状态）；
不写输入字符串；每次匹配各自持有独立 `*budget.Counter`，并发互不串台。
`audit` 另含朴素递归参考实现（仅供测试/对拍，不受步数约束）。
