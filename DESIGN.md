# 只追加日志：段索引与范围回放 —— 设计推导

## 1. 物理布局

- 段文件 `seg_XXXX`：自描述头（magic8 + version u16 + 首序号 u64 + 事件数 u32，共 22B）+ 逐事件记录。
- 记录：长度前缀 u32（记录体字节数）+ 记录体（序号 u64 + 载荷字节串）+ CRC32（IEEE，覆盖长度前缀与记录体）。
- 索引文件 `seg_XXXX.idx`：magic4 + 锚点间隔 N(u32)，其后每 16B 一个锚点（序号 u64 + 段内绝对字节偏移 u64）。
- 事件 0 基序号；段按序号连续切分；`首序号 + 事件数 == 下一段首序号`。

## 2. 为什么锚点必须是「不大于 from 的最大锚点」

回放 `[from,to]` 必须不漏前缀。锚点是扫描检查点：从锚点起顺序读，读不到就会漏。
- 取「最接近 from 的锚点」可能是大于 from 的锚点，该锚点落在 from 之后，
  从它开始会漏掉位于 (上一个锚点, from] 的事件。
- 唯一安全选择是 `max{a | a.seq <= from}`，从它向前顺序扫，跳过 seq<from 的记录后正好落到 from。
- from 恰好等于锚点：跳过 0，直接命中；from 在两锚点之间：跳过数 1..N-1；
  from 小于首锚点：从段头偏移 0 扫描，跳过数 < N。三种情况跳过数都严格小于 N。
- 无任何 `<=from` 锚点时，回退到段首偏移 0；对单段日志这等价于全段扫，仍正确。

## 3. 索引可丢弃、可逐字节重建

索引是纯加速结构，其每个锚点（序号、偏移）都必须能从段文件重新得到：
偏移即记录起始位置，序号在记录体内。重建规则与写时一致：
第 N、2N、3N… 条事件各记一个锚点。因此索引禁止保存「只在写入时可知」的信息；
删掉 `.idx` 后扫描整段重建，与原索引逐字节相同（含固定头）。

## 4. 定位与回放的复杂度

- 定位：一次二分（O(log 锚点数)）+ 至多 N-1 条顺序扫描；计数器记录跳过事件数，断言 `< N`。
- 回放：从锚点偏移开始读，经过的字节上界 = 一个锚点区间的记录字节 + 区间内事件字节；
  计数器记录读取字节数，断言 `<= 区间内事件编码长度之和 + 一个锚点区间长度`。
- 跨段：先定位含 from 的段，段内读到段尾或 to；下一段首序号连续，直接从其首事件续读，
  不重不漏，直到某段首序号 > to。

## 5. 损坏分类（按截断字节位置）

四类可 `errors.Is` 判定错误：
- `ErrHeaderTruncated`：保留字节 < 22，头部不完整。
- `ErrLenTruncated`：某条记录不足 4B 长度前缀。
- `ErrBodyTruncated`：长度前缀完整，但剩余字节 < 4+len（体或 CRC 不完整）。
- `ErrCRC`：记录完整但 CRC32 不符。
恢复语义：保留最大可恢复前缀；repair 重写头部事件数为实际可读条数，
并截断尾部残字节，改正后顺序读不再报错。

## 6. 索引失效与序号缺口

- 索引锚点偏移被改成事件中间时，该偏移处解不出合法长度前缀或 CRC 不符。
  replay/repair 检出后丢弃索引，回退全段扫描（从偏移 0），仍给出正确结果，
  并在报告中以 `IndexStale` 标注。
- 相邻段违反 `首+数 == 下一首` 即序号不连续，repair 报告缺口区间 `[应有起点, 下一段首序号)`，
  返回 `ErrGap`。四类错误另有 `ErrRange`（from>to）。

## 7. 边界与并发

- 空日志：回放得空结果不报错；单事件、空载荷均合法。
- from>to：`ErrRange`；from 小于最小序号：从最小事件开始（裁剪，不报错）；
  to 超过最大序号：回放到末尾；区间恰好整段、跨三段均连续。
- 并发：追加用文件互斥锁串行化，每条记录一次完整 `Write`（单次写追加）；
  回放只读已完整可见的记录，CRC/长度自检拒绝半条事件，因此每次回放都是某个一致前缀。
