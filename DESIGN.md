# Quoted-Printable 设计说明

## 关键推导

1. 空格、制表符在解码后可能成为硬行尾；严格解码会拒绝硬行尾的未转义空白，因此编码器只有在确认下一输入事件是换行（`\n` 或 `\r\n`）或 EOF 时，才把空白写成 `=20` / `=09`。其后仍有普通字节时，空白位于解码文本内部，必须原样输出。
2. `=XX` 是不可分割的三字符词元。若当前列宽加词元宽度超过 76，必须先输出软换行；不能逐字符写出后再判断。
3. 软换行前的空格仍写字面空格。解码器只删除软换行的 `=`、`\r`、`\n`，会保留该空格并与下一行首字节拼接，所以它不是硬行尾空白。若写成 `=20`，会产生严格上不必要的转义，破坏最小转义幂等性。
4. 软换行的 `=` 占用第 76 列，故普通内容在还有后续词元时至多放到第 75 列；第 76 列只允许留给硬换行或 EOF 前的最后内容。

## 往返等式

令 `N(x)` 将输入中的每个 `\n` 与 `\r\n` 规范成 `\r\n`，单独的 `\r` 保持为普通字节，则对任意字节串：

`Decode(Encode(x)) == N(x)`。

编码器是最小转义的：仅转义 `=`、不可打印字节、单独 `\r`，以及经前瞻确认位于硬换行或 EOF 前的空白；其他 33–126 字节、内部空格和制表符均原样输出。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 转义与换行规范 | 待补 |
| 76 字符行长与软换行 `=` | 待补 |
| 行末空白推导 | 待补 |
| `=XX` 原子性与软换行前空格 | 待补 |
| 五类严格解码错误及偏移 | 待补 |
| 跨切分点一致 | 待补 |
| 往返、幂等与计数器复杂度 | 待补 |
