# 差分数据同步：校验与增量传输器

模型：源端与目标端各持一份记录集合（实现为 `[]byte`，记录按定长块切分）。
目标端发签名 → 源端滚动比对生成补丁 → 目标端校验整份补丁后原子落地。

## 1. 弱校验和与强校验和的分工

弱校验和：`w = (Σ b) mod 65536`（16 位，极易碰撞，碰撞对可手工构造）。
强校验和：SHA-256（密码学哈希，碰撞概率可忽略），用于确认。

- 只用弱和：`{0x10,0x00}` 与 `{0x00,0x10}` 弱和同为 16，会被判为同一块而
  **错误复用**，补丁应用后数据不一致且不报错。故弱命中必须再算强和确认。
- 只用强和：每个偏移都要对整个块做一次 SHA-256，代价 O(数据长 × 块大小)，
  完全失去滚动意义。故强和只在弱命中时计算。

## 2. 滚动推进公式

窗口从偏移 i 推进到 i+1，块大小为 n：

`w(i+1) = (w(i) − b[i] + b[i+n]) mod 65536`

首块用循环初始化；之后每个偏移做 3 次基本运算（减、加、取模），
总代价 ≤ 4·len；而逐偏移重算是 len·n 次加法。模 65536 使窗口
和不会无限增长，且弱和值域小、匹配多，便于验证强和的拦截作用。

## 3. 签名（sig 包）

目标端把数据按块大小 n 切成若干块（末块可不满），每块输出
`(块号, 弱和, 强和)`。编码为二进制：魔数 `ONSIG1`，随后每块
`uint32 块号 | uint16 弱和 | 32B SHA-256`。

## 4. 匹配与补丁指令（diff 包）

源端维护窗口：每个偏移先滚动弱和，查签名中是否有块共享该弱和；
有候选才对当前窗口算一次强和，与候选强和比较，命中则发
`REUSE 块号` 并整体前移 n；否则前移 1 字节。未被复用覆盖的连续
源字节聚成 `LITERAL 数据`。弱和只取窗口低 16 位和，因此任何
「内容不同但和相同」的窗口都会产生弱命中——强和在此负责拦截。
强和计算计数器 ≤ 弱命中计数器。

补丁文件布局：魔数 `ONPTC1`；头部 `uint32 块大小 | 32B 源整体
SHA-256`；指令段为 `uint32 段长` + 若干条
`tag(1B) + uint32 参数`（REUSE 参数=块号，LITERAL 参数=数据长度）；
数据段按 LITERAL 顺序拼接原始字节；尾部 4B IEEE CRC32（覆盖此前
全部字节）。

## 5. 截断分类与「不得部分应用」

verify 先解析整份补丁并按长度分类（四类，均为可判定错误，errors.Is）：
头部不足 → ErrHeaderTruncated；指令段不足 → ErrInstTruncated；
数据段不足 → ErrDataTruncated；结构完整但尾部 CRC 不符 →
ErrCRCMismatch（逐字节翻转/越界改块号都落在此类）。

应用流程：**先校验整份**（结构、CRC、块号范围全部通过），再在
内存中拼出完整结果并核对源整体 SHA-256，全部通过后才一次写入
目标文件。任何一步失败立即返回、不触碰目标文件 → 要么全应用，
要么完全不变。

## 6. 边界

n==0 返回 ErrBlockSize；源/目标为空、相同、互为前缀、n 大于数据长、
长度恰为 n 整数倍与否、末块不满，均由统一的窗口循环 + 末尾
literal 自然覆盖。补丁只依赖块号与内容，天然确定；各次 diff 使用
独立计数器与独立 map，可并发。
