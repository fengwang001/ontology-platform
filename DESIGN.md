# 属性级权限过滤器 — 设计推导

## 问题

角色只能看到部分列。当过滤谓词引用了对该角色不可见的列时，如何处
理才能不泄露信息？三种候选做法：甲（不可见比较当作假）、乙（不可见列
当作 NULL 走三值逻辑）、丙（整条查询拒绝并指名列）。

## 甲的泄露路径：不可见比较当作 false

设可见列 `name`，不可见列 `secret`，真实数据只有一行 `secret = 1`。
攻击者发两个查询：

- `name = 'x' AND secret = 1` → `secret = 1` 按甲折为 false → 空集；
- `name = 'x' AND NOT (secret = 1)` → false 取反为 true → 返回该行。

两次结果之差直接编码了 `secret ≠ 1` 这一事实。把 `1` 换成 `2, 3, …`
逐值枚举，即可把整列 `secret` 完整读出。根因：谓词的真值在甲下仍然
是真实数据的函数，只是被粗粒化了，差分攻击把它还原。

## 乙的泄露路径：不可见列当作 NULL

把 `secret` 替换为 NULL 走三值逻辑，则：

- `secret IS NULL` 恒为 true → 返回全部行。这本身就是探针：若列不存
  在会报「未知列」，若存在但不可见则恒真，于是「存在某被隐藏的列」
  这一元信息被泄露；
- `secret IS NOT NULL` 恒假、`secret = 1` 恒 UNKNOWN 过滤掉所有行，
  攻击者可用同样的二分枚举区分「列不存在」与「列存在但不可见」。

即使值本身读不出，存在性泄露已违反「不可见即不可知」。

## 采用丙的论证

丙：谓词引用任何对当前角色不可见的列，整条查询拒绝执行，错误中指明
列名及其在谓词树中的路径。在丙下，任何被执行的查询其结果都只是可见
列的函数：两个只在不可见列上不同的数据库状态，对同一角色产生逐字节
相同的输出（同样的行裁剪、同样的错误）。因此不存在任何输出通道能把
不可见列的信息编码给调用方。甲、乙都让输出依赖于不可见列（甲依赖其
真值、乙依赖其存在性），只有丙切断依赖。故采用丙。

## OR 短路例外的定义与理由

先做**常量折叠**：不含任何列引用的子树直接求值；`OR` 一支折为 true
则整树折为 true（其余分支被丢弃），一支折为 false 则丢弃该支；
`AND` 对偶处理；`NOT` 常量取反。折叠是语义保持的求值前变换。

定义：拒绝判定作用于**折叠后**的谓词树。被折叠丢弃的子树中的列引用
从未被读取，不参与拒绝判定。例：

- `(1 = 1) OR secret = 1` → 折叠为 `true` → 放行；
- `(1 = 2) OR secret = 1` → 折叠为 `secret = 1` → 拒绝并指名 `secret`。

理由：被消除的分支在任意行上都不会被求值，其信息论效果与调用方从未
写出该分支完全等价；拒绝它不会多保护任何信息，只会误伤合法查询。

## 判定与裁剪规则

- 对折叠后的谓词树做**一遍**遍历：凡引用角色不可见列即拒绝，报列名
  与路径（如 `root.or.right`）；同一列多处引用只报一次列名，但路径
  全部列出。
- 三类错误可用 `errors.Is` 区分：`ErrUnknownRole`（角色未注册）、
  `ErrUnknownColumn`（列不在 schema）、`ErrInvisibleColumn`（列存在
  但不可见）。
- 行裁剪：只拷贝可见键到新 map，不可见键被**移除**而非置零值，使
  「不可见」与「值为空」可区分；拷贝次数 ≤ 可见列数，与总列数无关。
- 报告确定性：被裁列名、被拒引用（列名+路径）均按字典序排序后渲
  染，构造顺序不影响输出字节。

## 穷举判定定义（32 组合）

三列 `a, b, c`，可见性 8 种组合 × 谓词形态 4 种：

- `=`：`a = 1`，引用集 `{a}`
- `NOT`：`NOT (b = 1)`，引用集 `{b}`
- `AND`：`(a = 1) AND (b = 1)`，引用集 `{a,b}`
- `OR`：`(b = 1) OR (c = 1)`，引用集 `{b,c}`

放行当且仅当 引用集 ∩ 不可见集 = ∅；否则拒绝并指名全部不可见引用列。
