# 推断一·空行与单列空值：空行=跳过；单列表的空值必须写成 `""`
解析层规则：状态机在「字段开始」见到 `\n` 时，若当前记录尚未开始，不产出任何记录（空行跳过）。
于是输入 `a\n\nb`（单列）产出两条记录 `a`、`b`；`a\nb` 也是两条；只有 `""\n` 才是「空值记录」。
矛盾：单列空值记录若不加引号，序列化只能得到空行，再解析就被跳过——丢数据。
结论：回写器在「单列记录、唯一字段为空（不论原引号标记）」时必须写 `""`。
规范输入定义：引号外只用 `\n` 结束每条记录（含最后一条）；不含裸 `\r`；
除「空值必引号」与内容含 `, " \r \n` 外一律不加引号；引号转义为 `""`。
# 推断二·par 切点：两种入场假设各算一遍，从左到右钉死真实状态。
段首可能状态只有：FS（新字段）、MU（未引号续传）、MQ（引号续传，距上一引号>1 字节）、
MQS（续传且本段首字节即闭合引号），外加 carryCR（前段以裸 `\r` 挂起）。
每段只跑两遍：runA 覆盖 {FS,MU}，runB 覆盖 {MQ,MQS}，字节处理量 ≤ 2N，不随切点退化。
前段出口唯一决定选择：FS/MU→runA，MQ/MQS→runB；carryCR 用本段首字节裁决
（`\n` 则关闭 carry 记录并令被选 run 跳过该字节；否则立刻报孤立 `\r`）。
FS 与 MU 的差别仅在「首个续传字节并入 carry 单元格、空首格事件丢弃」，由拼接层按 carry 处理。
坐标换算：全局偏移=段起点+段内偏移；全局记录号=已提交记录数+段内记录序号
（carry 合并记录沿用 carry 记录号）。两假设在本段首个记录结束换行后自然合流，
故每段至多首记录需拼接，其后与单线程逐字节一致，结果与错误位置完全相同。
# 推断三·CR 待定：状态 CR（未引号末遇 `\r`）挂起。
续传：下一字节 `\n`→关闭记录；非 `\n`→孤立 CR 错误，该字节按类别重判（`"`→裸引号，其余并入新字段）。
流在 `\r` 处结束（Close）：后无 `\n`，判孤立 CR（位置在该 `\r`），不产出记录。
par：carryCR 只在段边界出现，裁决见推断二；错误坐标落在前段末字节。
# 推断四·上限「立刻」：
字段字节数按「字段实际内容字节」计数——引号字段中 `""` 在第二个 `"` 被确认成内容的那一拍计 1；
每接受一个内容字节立即比较 MaxFieldBytes，超限即在当前字节偏移报 ErrFieldTooLong，不缓冲整条记录。
字段数在产出字段时计数超限即 ErrTooManyFields；记录数在记录闭合时即 ErrTooManyRecords。
# 状态转移表（ef=产出字段；er=产出记录；skip=空行跳过；+c 内容计数）
状态\输入 | , | " | \n | \r | 普通字节
FS 字段开始 | ef→FS | →Q | 已开始:ef,er→FS；未开始:skip→FS | →CR | +c→U
U 未引号中 | ef→FS | BareQuote | ef,er→FS | →CR | +c→U
Q 引号中 | +c→Q | →QS | +c→Q | +c→Q | +c→Q
QS 见引号 | ef→FS | +c→Q | ef,er→FS | +c→Q | AfterQuote
CR 待定 | OrphanCR | OrphanCR | er→FS | OrphanCR | OrphanCR(该字节随后重判)
EOF：Q/QS→UnclosedQuote；CR→OrphanCR；其余正常收尾（已开始的字段与记录产出）。
列数≠首条记录→ErrColumnMismatch，由 table 判定。单实例非并发安全；多实例互不共享状态。
