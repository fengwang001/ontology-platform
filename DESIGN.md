# 严格流式 Base64 设计说明

## 规范形式（第 2 条）的推导

Base64 把每 3 字节（24 bit）编码为 4 个 6-bit 字符（sextet）。
输入长度不是 3 的倍数时，最后一个组（quantum）不足 24 bit：

- 剩 1 字节 = 8 bit，需要 2 个 sextet（12 bit），**多出 4 bit**；
- 剩 2 字节 = 16 bit，需要 3 个 sextet（18 bit），**多出 2 bit**。

编码器在右端补的是 0 bit（移位得到），所以**任何编码器的真实输出，
其最后一个数据 sextet 的低位冗余 bit 必然全为 0**。据此得到统一规则：

> 尾组中超出解码字节长度的那些 bit 必须全为 0；具体判定只用两处比特：
> `xx==` 形看第 2 个 sextet 的低 4 bit，`xxx=` 形看第 3 个 sextet 的低 2 bit。

样例验证：`QR==` 中 `R`=17=`0b010001`，低 4 bit 为 `0001`≠0，说明它不
可能是任何字节串的编码结果——宽松解码器把多余的 `0001` 悄悄丢弃才得到
`A`，但 `Encode("A")="QQ=="≠"QR=="`。同理 `QUJ=` 中 `J`=9=`0b001001`，
低 2 bit 为 `01`≠0，而 `QUI=` 中 `I`=8=`0b001000`，低 2 bit 为 `00`，合法。

与「编码结果唯一」的关系：严格解码器接受的集合必须恰好是 Encode 的像，
这样 Encode 是字节串到合法编码的双射，`Decode(Encode(x))==x` 且对被接受
的 `y` 有 `Encode(Decode(y))==y`。若接受 `QR==`，`A` 就有两个编码，唯一性
与重编码往返同时被破坏——所以 `QR==` 必须拒绝。填充规则（`=` 只能出现在
整个流的末尾、长度必须为 4 的倍数）同理：它们也是编码器输出的必然性质。

## 语义 ↔ 测试对照表

（待测试写完后补齐）
