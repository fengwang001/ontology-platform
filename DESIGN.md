# 设计推导

包依赖单向：lines → edit → hunk → udiff → patch；patch 另依赖 lines。
行 = 含行尾的字节段（`\n` / `\r\n` / 末行无尾），`Split` 与 `Join` 互逆。

## 1. hunk 头零计数时的行号

统一 diff 的区间采用半开区间，头里的数是**从 1 起的闭区间编号**：
普通段 `[a, a+b-1]` 覆盖旧行 a..a+b-1；`b=0` 时该区间为空，
起点 a 表示「插入点紧接在旧第 a 行之后」，即 a = 相邻上下文/锚点行号，
插入位置在新序列索引 a（0-based）处。文件开头前没有行，编号为 0。

| b | 书写 | Apply 解释（0-based 插入/作用点） |
|---|---|---|
| b=0 | `-a,0`，a=前一锚点行号（文件首为 0） | 在旧索引 a 处（第 a 行之后）插入 |
| b=1 | `-a`（省略 `,1`） | 作用于旧第 a 行（索引 a-1） |
| b≥2 | `-a,b` | 覆盖旧索引 a-1 .. a+b-2 |

样例核对：空→首行插入，前面无行故 `a=0`：`-0,0 +1`；
`a\nb\n` 中在第 2 行后插入，前一锚点是第 2 行：`-2,0 +3`；
删最后一行，新区间空、锚点是新第 0 行（文件首）：`-1 +0,0`。
`+c,d` 同构。

## 2. hunk 合并阈值

两改动间未改动行数为 g。若分开成两个 `-U C` hunk，相邻上下文各占 C 行；
合并要求中间上下文只出现一次，故 `g ≤ 2C` 时合并（g=2C 时中间恰好放得下
C+C 行上下文且无重复），`g ≥ 2C+1` 时分开。阈值 = **2C**。
反例：C=1、g=2 时 2C=2，合并后头跨度正好覆盖全部 2 行间隔，与 GNU 一致；
若阈值取 C 或 2C+1：取 C 会把 g=2 错分成两个 hunk（中间上下文重复输出）；
取 2C+1 会把 g=3 错合并（GNU 在 g=3、C=1 时输出两个 hunk）。

## 3. Myers 并列路径选择

前向 Myers 中同一条 furthest-reaching 路径有多个 D 相同的走法。固定规则：
每个对角线 d，若向下（删除，k-1）与向右（插入，k+1）到达同一 x，**优先删除**
（取 `x = max(V[d-1]+1, V[d+1])`，并列时前者胜）。因此 `a b → b a` 的脚本固定为
`-a`、` b`、`+a`（删除排前），渲染字节确定，重复 100 次逐字节相同。

## 4. 偏移后后续 hunk 的起点平移

hunk i 记录起点为 s_i，实际在 t_i 命中，偏移 δ_i = t_i − s_i。
hunk i+1 的记录起点 s_{i+1} 必须整体平移 δ_i：在 s_{i+1}+δ_i 附近 ±F 搜索，
因为前一 hunk 的插入/删除已使其后所有旧侧行号平移；不平移则对本来合法的
补丁会误报超出范围。每 hunk 的平移量由**当前已应用的累计行差**独立推出，
因此多 hunk 链与最终命中位置一致；距离仍按「命中点 − 平移后的记录起点」比较，
多候选取最近、并列取靠前。

## 5. 其他固定约定

最短性：删除数+插入数 = N+M−2·LCS，Myers 保证 D 最小；超过 MaxEdits 返回
`ErrTooDifferent`。计数器 `steps` 记录每条对角线路径的前进与 snake 逐行比较数。
错误四类哨兵：`ErrFormat`（带补丁行号）、`ErrContext`、`ErrOffset`、
`ErrTooDifferent`，互不包裹、可 `errors.Is` 判定。应用先整份试算再落盘，
任何 hunk 失败则目标字节不变（原子拒绝）。
