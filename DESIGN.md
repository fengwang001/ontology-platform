# DESIGN

## 推导

1. Write 切法无关：压缩器只把字节追加到连续未刷新缓冲；匹配扫描总从同一累计偏移观察同一份字节，不在 Write 返回处决策。Flush/Close 前才对整个未决区间运行同一确定性贪心算法，因此最长匹配不会跨调用边界被截断，字面量分区也相同。Flush 强制结束当前匹配，并输出刷新标记，因为解码器必须在该点交出恰好这些输入；之后写入仍使用刷新前历史，所以匹配可跨越刷新点。
2. 重叠复制：距离 `d`、长度 `n>d` 时，块复制会读尚未复制的新槽位；语义是每复制一字节，该字节立即成为后续历史。实现对每字节调用环形窗口尾部取回；重复周期短，Go 编译器可展开边界，复杂度仍是 O(n)，语义最直接。
3. 距离校验：`d>窗口容量` 是编码格式/配置不可能引用的历史；压缩器只在窗口已存字节中产生候选。`d>已输出字节数` 虽也越界，但可能来自截断/伪造且早于窗口判断，二者分开能定位不同不变量。并行预置字典把上一块末尾字节先放入窗口但不计入当前已解压输出；解码器流仍是单一流，回指发生在这些字典字节已经按流输出之后，所以“已输出”始终指流开始后的总输出，含义不变。
4. 并行确定性：第 k 块算法只读块 k 和块 k-1 原文：窗口/哈希由上一块末尾预置，候选链有固定上限，不读压缩结果、goroutine 状态或完成顺序。块按原顺序拼接；非末块仅压缩本块并写 flush，末块写 tail，故无需特殊块记录，workers 只改变执行时机。

## 流格式

| 记录 | 字节布局 |
| --- | --- |
| 流头 | magic `LZL1` + varint version=1 |
| 字面量 | tag=1，varint 长度，后接原文字节 |
| 回指 | tag=2，varint distance，varint length |
| 刷新 | tag=3 |
| 流尾 | tag=4，varint 原文总长，varint IEEE CRC-32 |

所有整数用无符号 LEB128；最多 10 字节且第 10 字节只允许 bit0。空输入仍输出头和尾。
