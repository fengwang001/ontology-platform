# DESIGN

基线 N0（固定变换，三策略只改尾部）：`\r\n`、`\r`、`\n` 统一为 `\n`；删除每行末尾连续空格/制表符。
`Keep`=N0；`EnsureOne`：非空则末尾恰好一个 `\n`（空串→`""`，见下）；`Trim`：去掉末尾全部空行后加一个 `\n`。
删除空行使其先被 N0 变成空行再统一处理，故两策略在本语义下结果相同（实现保留两套独立尾部逻辑）。

## 1. 空输入与换行输入
`""` 下：Keep→`""`；EnsureOne 若输出 `"\n"`，再规范化 `"\n"` 仍得 `"\n"`（幂等成立），但制造了原文不存在的行，
且与 Trim 不对称。选 `""→""`（"确保恰好一个"解释为"至多一个且在有内容时必有一个"）。
`"\n"` 是一行空内容+换行；`"\n\n"` 两空行；`"  \r\n"` 空白行→空行+换行。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三种输出均无 `\r`、无行尾空白，且每格等于自身的规范化 → 幂等。

## 2. 被删字节的 ToOut
要求 ToOrig 单调不减、且 `ToOut(ToOrig(o))==o`。被删原文区间在输出中没有对应字节，ToOut 必须把整段被删区间
压到同一个输出偏移。`\r\n` 的 `\r` 被删、`\n` 保留为输出 `\n`，故 `ToOut(pos(\r))=o`（换行符之前）；
行尾空白同理压到其后面换行符之前：`"a  \n"` 中两个空格的 ToOut 都是 `a` 之后、`\n` 之前的偏移。
编辑器把光标放进被删空白时落在行尾换行符**之前**（行内容的末端），符合直觉。终点 `ToOut(len(src))=len(out)`
（含合成换行：合成 `\n` 无原文字节，全局终点即输出终点，这是唯一无原文对应的输出字节）。

## 3. 缓冲超限
ws 缓冲保存"尚不知是否在行尾"的空白。若强制判定为非行尾输出，则当后续出现换行时本应删除的空白已输出，
直接违反语义 2、破坏幂等（语义 5）与映射可解释性；拒绝只违反"本次调用可用性"，不违反 4/5/6：
流切分无关性来自状态机本身，拒绝是可判定错误。选**拒绝**：返回 `ErrWSBuffer`（带原文偏移），进入终态，
已输出内容与映射保持、可查询，之后 Write/Close 返回 `ErrClosed`。输出上限同理：写前预检，超限即
`ErrOutputLimit` 终态。NUL 严格模式：`ErrNUL` 带偏移、终态、保留已输出。

## 4. par 拼接
段边界可能切开 `\r|\n` 或一行尾空白。每段独立 N0（Keep）规范化后只可能在**接缝**处不同，待定状态互斥
（待定 `\r` 与空白缓冲不会同时非空，见 norm 状态机），修正量有上界：
- 前段 Close 后末尾为独立 `\r`（其 ToOrig 段终点 == ToOut 段终点，可由映射差值识别），且后段首字节是 `\n`：
  该 `\n` 与 `\r` 合并，删除前段末尾的 `\n`（及其 span）。
- 前段末尾有被删行尾空白（段以 `\n` 结束且其前有 ToOut 相同的被删区间）：仅当后段第一个非空白字节不是行尾
  （即非 `\r`/`\n`）时，被删空白"复活"——把该空白串原样插回接缝（1:1 span）；后段以行尾开头则维持删除。
段映射先按各段 `(origBase,outBase)` 平移，再随接缝修正调整其后所有区间坐标；最后在全局尾部统一应用一次
非 Keep 策略（单段合成）。每段只回看接缝处一个待定项，判定只需后段首字节/首非空白字节，修正区间 O(1)。
