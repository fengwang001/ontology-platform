# 分片扇出查询部分失败合并器 — 设计推导

只依赖标准库；分片由接口 `Shard.Fetch(ctx, id) <-chan shard.Event` 模拟（事件流，可推多条以模拟重试叠加）；状态只在进程内存。

## 1. 模型
- `Record{ID string; Count int64; Value int64}`：`Count` 供 Count 聚合，`Value` 供 Sum/Min/Max/TopK。只给部分字段即零值，合法。分片 ID 允许空串。
- 每个成功分片自报 `Bound`（该分片任何记录 `Value` 不超过此上界）与 `Count` 字段；事件携带的记录数必须等于分片声称条数，否则判损坏。
- `fanout` 并发向 N 个分片发起，受并发上限 `maxInFlight` 与整体截止时间约束；同一分片第二次到达的结果丢弃（去重，按分片维度）。
- 分片状态枚举：`OK / Timeout / Corrupt / Duplicate / Failed / Canceled`，对应哨兵错误可用 `errors.Is` 区分。

## 2. 五种聚合在部分失败下还能说什么
记成功分片集合为 S，缺失集合为 M（M=∅ 时全部精确）。

### Count 与 Sum：成功部分是下界
每条记录对 Count/Sum 只贡献非负数（Count≥0，Value 可为负；Sum 的“下界”依赖 Value≥0 的约定，本实现统计语义取非负贡献；Sum 若允许负值则同样是“已观测值”，方向不再保证，故 DESIGN 明确 Sum 输入非负）。
缺失分片的真实贡献 ∈ [0, +∞)，故真实值 T ≥ 观测值 v：标注「≥ v」（下界）。M=∅ 时 T=v，精确。

### Min：方向相反的单边界
已观测最小值 v = min_S。缺失分片可能出现更小值，但不可能让真实最小值变大：T = min(v, min_M) ≤ v。
故只能断言「真实 Min ≤ v」。M=∅ 时 T=v，精确。

### Max：单边界另一方向
T = max(v, max_M) ≥ v：只能断言「真实 Max ≥ v」。M=∅ 时精确。
注意 Min/Max 与 Count/Sum 的边界方向相反，写反即错。

### TopK：可信前缀
缺失分片最多再放入 (B = Σ_{m∈M} Bound_m) 这个“总上界水平”的分数竞争（任何缺失记录得分 ≤ 对应分片 Bound，故缺失侧最强竞争者得分 ≤ B）。
当前合并榜按得分降序、ID 升序打破并列，第 j 名得分 s_j。
- s_j > B：最多 K−1 个其它已知条目比它高（榜为严格名次序），缺失侧没有任何条目得分能达到 s_j，因此该条目确定入选 TopK → 前 j 名全部满足时前 j 名“入选确定”。
- s_j ≤ B：缺失侧可能出现严格更高分条目把它挤出（即使 s_j = B，缺失侧可用同 Bound 配合更小 ID 在并列序中排前），从 j 起整条不可信。
故可信前缀长度 L = 最大的 j（j ≤ K）使 s_1..s_j 均 > B；榜单在 L 处截断标注；若任何缺失分片未给上界（BoundUnknown），无法定界，L=0。
“入选确定”不等于“排名确定”：缺失条目可插到前缀中间改变名次，故只保证前 L 个条目属于真实 TopK 集合，不保证次序。
用例：K 名得分均 > 很小的 B ⇒ L=K；B ≥ 头名得分 ⇒ L=0。

### 全部成功
M=∅ 且无失败：五种聚合一律 Exact，不得降级。

## 3. 故障判定
- 超时/取消：ctx 截止后未交付，状态 Timeout（截止）或 Canceled（ctx 取消）。
- 损坏：事件结束时实交记录数 ≠ 声称条数，丢弃该分片全部记录，状态 Corrupt。
- 重复：同一分片在已成功结束后再推完整结果，丢弃后续，状态 Duplicate；Count/Sum 与单次逐位相同。
- 全失败：无任何 OK 分片 ⇒ 返回 ErrAllFailed，禁止返回零值标精确。零分片 ⇒ ErrNoShards。所有分片成功但均空 ⇒ 合法空结果，精确，与全失败可区分。

## 4. 资源约束证明
并发执行体由容量 `maxInFlight` 的信号量闸控：发起前获取、结束后释放，故在飞数恒 ≤ 上限；非导出 `peak` 在持锁下记录历史最大值。
整体截止由 `context.WithDeadline` 控制：调度循环每次取信号量前检查截止，到期则停止发起（之后新发起数为 0），已在飞请求随 ctx 取消；`Wait` 在全部工作者退出后返回。

## 5. 确定性
合并只依赖成功分片的记录多重集：Count/Sum 可加且可交换；Min/Max 可结合可交换；TopK 全局排序（得分降、ID 升）。缺失清单按分片 ID 排序。重复按分片首次结果去重。故任意到达顺序输出逐字节相同。

## 6. 包划分
`shard` 类型与假分片；`fanout` 并发扇出；`combine` 纯聚合；`confidence` 判定与区间；`report` 汇总；`cmd/demo` 演示。
