# 设计推导（非法字节替换 / 流式 UTF-8⇄UTF-16）

## 1. UTF-8 非法单元切分（统一规则，WHATWG 最大子部分）

规则：从首字节起按长度 L(1/2/3/4) 读续字节。若第 k 个位置的字节不在该区间，
则：k==1 且该字节是续字节(80..BF)时，**只吞首字节**；否则吞掉"首字节+已确认的续字节"
（k>=2 时那个坏字节不吞，重新作为新字符起点）。EOF 时只吞已收到的前缀。

验证样例：`F0 90 80 41`：F0 要求第2字节 90..BF，90 合法；第3字节要求 80..BF，41 非法，
且 k=3≠1，吞 `F0 90 80`→1 个 FFFD，41 重解析→`41`。`E0 80 80`：E0 第2字节要求 A0..BF，
80 是续字节但越界，k=2 吞 `E0 80`，剩 80 独立成单元→3 个 FFFD。`ED A0 80`：ED 第2字节
要求 80..9F（排除代理 D800..DFFF），A0 越界，同理 3 个。`C0 AF`：C0 第2字节要求 C2 才有的
A0..BF（C0/C1 无任何合法第2字节，区间空），k=2 吞 C0，AF 独立→2 个。`F4 90 80 80`：
F4 第2字节要求 80..8F（排除 >10FFFF），90 越界→4 个。`E2 82`+EOF：第2字节 82 合法，
正等第3字节，EOF 只吞这 2 字节→1 个（不是 2 个）。`80 80`：续字节开头 k=1 只吞自己→2 个。

| 首字节 | 第2字节合法区间 | 非法时吞几个 |
|---|---|---|
| C2..DF | 80..BF | 见下 |
| E0 | A0..BF | 1(坏字节是续)或2(其他,重解析坏字节) |
| E1..EC | 80..BF | 1 或 2 |
| ED | 80..9F | 1 或 2 |
| EE..EF | 80..BF | 1 或 2 |
| F0 | 90..BF | 1 或 2 |
| F1..F3 | 80..BF | 1 或 2 |
| F4 | 80..8F | 1 或 2 |
| 80..BF | （无，自身即非法单元） | 1 |
| C0 C1 F5..FF | 空（永不合法） | 1 |

第3/4字节区间一律 80..BF；非法时（k>=2）吞"首+已确认续字节"即 k-1 个，坏字节重解析。

## 2. 切分点缓存

UTF-8 合法首字节至多预告 3 个后续字节，故待决前缀缓存硬上限 **3 字节**（含未完成的
BOM 前缀；任一时刻二者合一不超过 3）。看到与已确认长度不符的字节立即判非法、不等
EOF（上表）；只有"已收续字节全部合法但尚未收齐"时才等待，且等待字节数 ≤2。
UTF-16 待决缓存 ≤3：奇数尾字节(1)或高代理+奇数尾(3)；BOM 前缀另占 ≤1。

## 3. par 切点对齐

段 i 不盲从自己的起点切。从切点 b 向回看（UTF-8 ≤3、UTF-16 ≤3），找**最后一个"可能跨越
b 的单元"的首字节**：UTF-8 即 b 前最近的前导字节（非 80..BF），若它预告的长度能跨过 b
则采用其偏移，否则不采用；被跳过的续字节必然属于前一个单元。UTF-16 中跨单元只有代理对
（4 字节），回看 3 字节，检查偏移是否为码元偶对齐起点、且首码元是高代理。
每个单元在"做最终判定的最后一个字节（决策字节）"所在的段输出。这样跨切点的完整/非法
单元只输出一次；段尾未决单元（flush=false）不输出。例：切点在 C3 后、下段以 80 80 开始：
回看找到 C3（预告 2 字节且跨过切点），下段从 C3 开始重解析，`C3 80` 的决策字节 80 在
下段→非法单元归下段；末尾 80 归再下一个单元起点。计数与单线程完全相同。回看常数字节，
总检查次数 ≤ N+8C（C 为小常数）。

## 4. 第9条断点的消费计数

逐单元推进。Write 返回的 n = 已进入"已决单元"的字节数（含非法单元吞掉的字节、BOM
字节）；仍在待决缓存里的前缀（半个标量）**不算已消费**，n 不计它们，因此调用方
`p[n:]` 续传时半个字符随新数据重新进入。输出只在整个标量（或整个 FFFD；UTF-16 为
整个码元/代理对）写得下时才写，否则在标量边界停并返回超限错误。
