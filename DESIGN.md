# CSV 方言解析器设计

## 1. 空行与单列空值

**选择：空行（两换行间 0 字节）一律跳过，不作为记录。** 解析器在 `fieldStart`
遇到 `\n`（或 `\r\n`）时只消费换行、不发射记录；因此单列输入里"一条空值记录"
（`a\n\nb` 中间那行）**无法**用未引号空字段表示——它与空行不可区分，只能写成
引号空字段 `""`。这是引号的第 5 种必要情形。回写规则：单列表中唯一字段为空时
必须写 `""`；多列表中未引号空字段照写（逗号已消除歧义）。

**规范输入**（满足 `Write(Parse(x))==x`）：逐记录拼接，每行以 `\n` 结尾
（末行也有换行），引号字段原样保留；空行不出现（无法由表表达）；记录间无额外字节。

## 2. par 切点

每段从两个假设起跑：`S`=起点在字段开始（引号外）；`Q`=起点在引号字段内。
段末输出：终止状态、事件流（Cell 带全局偏移/引号标记、EOR、Err）。从左到右，
前一段选定假设的终态决定下一段用哪条：字段开始/未引号中→S；引号中/刚见引号→Q。
被跳过假设的段内前缀事件丢弃：S 选中而 Q 被弃时 Q 事件全弃（下一段重新 Q）；
Q 选中而 S 被弃时，S 若先遇到顶层换行（切点恰在跨段记录中，S 凭空拆出一条），
丢弃到该 EOR 为止；切点正落在 `\r` 后（`\r` 已被前段记为裸 CR 或待决）：后段首字节
为 `\n` 时不发射 EOR（前段待决已闭包该记录），否则裸 CR 错误以 `\r` 的偏移为准；
切点落在 `""` 中间（前段为"刚见引号"）：后段首字节 `"` 按转义处理、Q 事件自此保留，
首字节为逗号/换行时闭合在该字节。S/Q 均无错而终态不同是不可能的：二者只在首个真实
记录边界之前分叉，边界后完全同流。错误坐标=段内已处理字节数+段起点偏移；记录号=
前段完整记录数+段内计数（被弃前缀的伪记录不计）。

## 3. CR 待决

未引号字段中见 `\r` 进入 `crPending`：已确定它不属于任何字段内容（裸 CR 在未引号
上下文恒为错）。下一字节 `\n`→闭记录；其它→`ErrBareCR`（偏移为 `\r` 处）；EOF→
`ErrBareCR`（流在 `\r` 结束不是合法行尾）。半包：该状态随解析器挂起，续传字节照常
决策。par：切点在 `\r`/`\n` 之间时由上节拼接规则处理，不依赖跨段状态。引号内
`\r` 是普通内容字节，无待决。

## 4. 上限的"立刻"

字段字节在"进入字段内容"的同一字节计数：未引号字段每个普通字节 +1（逗号、换行不
计）；引号字段内每个内容字节 +1，转义对 `""` 在第二个 `"` 到达、确定它是内容时
+1——超限当字节即报 `ErrFieldTooLarge`，不等闭引号。字段数在每个字段开始（含首字段）
计数，第 N+1 个开始字节即报 `ErrTooManyFields`；记录数在记录完整（EOR/EOF 闭包）时
计数，第 N+1 条完整即报 `ErrTooManyRecords`。报错后解析器终态，后续 Feed 返回同一
错误。par 中 worker 先不带上限计算；拼接选路后做轻量汇总（字段字节/字段数/记录数），
发现超限再用带上限的流式解析器重放至该点，得到逐字节相同的错误位置；合法输入不触发
重放。

## 5. 状态转移表

字节类别：`,`=C `\n`=N `\r`=R `"`=Q 其它=X。动作：cell=发射字段，eor=闭记录，
err=报错（终态）。

| 状态 | C | N | R | Q | X |
|---|---|---|---|---|---|
| fieldStart | cell+eor准备/新字段 | eor（空行跳过） | crPending | quoted | unquoted |
| unquoted | cell,→fieldStart | cell+eor | crPending | err BadQuote | unquoted |
| quoted | 内容 | 内容 | 内容 | quoteSeen | 内容 |
| quoteSeen | cell→fieldStart | cell+eor | crPending | 内容(+1引号) | err QuoteAfterClose |
| crPending | （不可达，先决策）| cell+eor | err BareCR | err BareCR | err BareCR |

注：fieldStart 下 C 发射当前（空）字段并进入新字段；N 发射字段+EOR，但若该字段是
本记录首字段且记录为空则跳过。EOF：fieldStart 且本记录无任何字节→结束（末尾换行不
产生空记录）；其余→发射字段+EOR（无换行结尾合法）；quoted→err UnclosedQuote。

## 6. 复杂度

每字节恰好执行一次状态转移：无回扫，流式计数==len(buf)。par 每段 S/Q 各一遍
（2N 字节）；仅当切点落在跨段引号记录（选中路径含待拼接前缀）时，该段额外以真实起点
重放一次，故总处理 ≤ 3N；设计的规模测试（1MB/16MB，引号巨字段跨切点）下选中段均为
纯 Q，实测 ≤ 2N+O(K)。
