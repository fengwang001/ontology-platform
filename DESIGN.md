# RLE 设计推导

## 严格解码拒绝清单（不变量：接受的 t 必须满足 Encode(Decode(t)) == t）

1. **显式次数 1（`1a`）**：Decode 得 `a`，但 Encode(`a`)=`a` ≠ `1a`，编码不唯一 → ErrCountOne。
2. **次数 0（`0a`）**：得到零长度游程，`0a` 非空却解码为空；Encode 永远不产生非空的零次游程 → ErrCountZero。
3. **前导零（`01a`、`00a`）**：规范形式十进制无前导零；Encode(`a`)=`a`、Encode 任何游程都不以 `0` 开头，无法回写 → ErrLeadingZero。
4. **相邻同符号游程（`2a3a`）**：解码为 `aaaaa`，再编码为 `5a` ≠ 原文；最长游程要求同码点必须合并 → ErrAdjacentSame，偏移取后一游程起点。
5. **非法转义（`\x`，x 非数字非 `\`）**：`\` 只允许转义数字/反斜杠；字面 `x` 的规范编码是 `x`，任何 t 被接受后都无法由 Encode 复现该反斜杠 → ErrBadEscape，偏移取反斜杠后第一个字节。
6. **末尾孤立反斜杠（`a\`）**：转义缺少符号，游程不完整，EOF 时仍无符号 → ErrLoneBackslash，偏移取该 `\`。
7. **只有次数没有符号（`a3`）**：游程 = 次数 + 符号，缺一不可；Encode 的输出绝不会以裸数字结尾 → ErrCountNoSymbol，偏移取次数首字节。
8. **非法 UTF-8（裸 0x80、0xFF、截断多字节、超长编码、代理码点）**：Encode 的输出恒为合法 UTF-8，非法序列不可能是任何字符串的规范编码 → ErrInvalidUTF8，偏移取非法首字节。

所有错误包装为 ParseError{Offset, Err}，哨兵错误互不相同，可用 errors.Is 判定。

## 大次数取舍

**支持任意大小次数，不设次数位数上限**：runs 用 `math/big.Int` 逐位累加十进制（每位 O(当前长度)，不溢出）。
但解码输出受**可配置字节上限 MaxOutput**（默认 64 MiB，DecodeLimit 可覆盖；0 用默认值）约束：
数字累加过程中一旦次数本身已超过 MaxOutput（此时即使每码点 1 字节也必然超限），立即在符号到达前返回 ErrOutputLimit；
符号确定后再校验 次数×码点字节数 ≤ MaxOutput。写盘按 64 KiB 分块重复，绝不按次数分配内存。
因此不会整数溢出、不会因天文次数崩溃，且超限是可判定错误。

## 语义 → 测试对照表

| 语义（第三节） | 钉住它的测试函数 |
| --- | --- |
| 1 往返 | （收尾时补） |
| 2 规范形式/严格解码 | （收尾时补） |
| 3 大次数 | （收尾时补） |
| 4 最长游程 | （收尾时补） |
| 5 跨切分点一致 | （收尾时补） |
| 四 计数器 | （收尾时补） |
