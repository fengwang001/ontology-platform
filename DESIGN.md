# DESIGN — 带依赖的配置解析与环境覆盖器

## 1. 先合并后展开（核心推导）

四层优先级：默认值 < 文件 < 环境变量 < 命令行。每个键的最终原始值由最高优先层决定。

**反例（先展开后合并为何错）**：文件层含 `greeting = hello ${name}` 与 `name = file`，
环境层只覆盖 `name = env`。若先在文件层内展开，`greeting` 被固化为 `hello file`，
合并时环境层没有 `greeting`，最终结果错误地使用旧 `name`。
展开引用的解析必须看到**最终的** `name`，而最终结果只有合并后才知道。

**结论**：先按优先级合并出最终键值表（值保持未展开的原始形态），再对整张表统一展开。
溯源同样在展开前记录各层原始值，展开只产生派生的"展开值"，不回写原始记录。

## 2. 来源与文件格式

- 统一中间表示：`Entry{Key, Value, Layer}`，`Layer` 为 default/file/env/cli 四级。
- 文件格式：每行 `key = value`，`=` 两侧空白裁剪；值可含 `=`（按第一个 `=` 切分）；
  空值合法（`key =` 得到 `""`，与键不存在可区分）；文件必须以单独一行 `.` 结束（终止行）。
- 终止行存在的理由：让"文件被截断"成为可判定错误，而非静默得到一份更短的合法配置。
- 键校验：空键报 `ErrEmptyKey`；路径段为空（`a..b`）报 `ErrEmptySegment`；段数不限（深度 100 合法）。

## 3. 截断分类（故障注入）

解析到不完整的最后一行（无 `\n` 收尾）或缺失终止行时，按切点位置分类，均带行号：

- 切点在行边界（内容以 `\n` 结尾但无终止行）→ `ErrLineIncomplete`，行号 = 已有行数+1。
- 切点在键区（末行无 `=`）→ `ErrKeyIncomplete`，行号 = 末行行号。
- 切点在值区（末行已含 `=`）→ `ErrValueIncomplete`，行号 = 末行行号。

逐字节截断 1..len-1 的每个切点必落入且仅落入其中一类（终止行本身不完整时末行无 `=`，归入键不完整）。

## 4. 引用展开

- 语法：值中 `${path}` 引用其他键；`$$` 是 `$` 的转义，故 `$${a}` 展开为字面量 `${a}`。
- `${` 后无 `}`：`ErrUnclosedRef`，报 `${` 的字节偏移。
- 引用不存在的键：`ErrUndefinedRef`。
- 环检测：展开栈上重复出现的键即环，`ErrCycle` 报闭合路径（如 `a -> b -> a`），自引用同法。
- 性能：每个键的展开结果缓存（memoize），沿引用链每条边只做一次替换；
  替换总次数 ≤ 引用总边数 ≤ 4×键数×平均引用数。朴素递归对 `k1=${k0}${k0}` 型链是 2^n，必须避免。

## 5. 环境变量名映射与歧义

- 正向（键→名）：大写、`.`→`_`，如 `a.b.c` → `A_B_C`。
- 反向（名→键）：小写、**连续下划线折叠为一个 `.`**，如 `A_B_C`→`a.b.c`、`A__B_C`→`a.b.c`。
- 歧义判定：两个**不同**的环境变量名规范化到同一个键即 `ErrAmbiguousEnv`，报出两个原名与键。
  同名重复不算歧义（后者覆盖前者）。可选前缀参数用于只取 `PREFIX_` 开头的变量。

## 6. 合并与溯源

- 合并：单层循环，按层从低到高逐条写入 map；已存在则把旧值压入该键的覆盖列表。一遍扫描，
  查找次数 ≤ 2×条目数（读+写），上界取 4×总键数。非导出计数器 `lookups` 供包内测试断言。
- 溯源：每个最终键记录 `Final{Layer,Raw}` 与 `Overridden[]`（按优先级从低到高），
  展开后补 `Expanded`。报告按键字典序输出，四层来源构造顺序不影响结果（确定性）。

## 7. 校验

- 类型：`string`/`int`/`bool`，按合并后的最终值校验；类型错误报键名、期望类型、实际值、来源层，
  可由 `errors.Is(ErrTypeMismatch)` 判定。
- 必填：收集**全部**缺失键，排序后一次性报出，`errors.Is(ErrRequired)` 判定。
- 多个校验失败用 `errors.Join` 聚合，`errors.Is` 仍可分别命中。

## 8. 确定性

所有输出（最终配置、溯源报告）按键字典序生成；map 迭代不参与输出顺序；
打乱四层来源的构造顺序 20 次，输出逐字节一致。
