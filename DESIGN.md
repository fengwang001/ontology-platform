# 属性级权限过滤器 — 设计

## 1. 不可见列出现在过滤谓词中

谓词只用于决定「哪些行返回」。若谓词引用了当前角色不可见的列，任何「猜测一个值继续算」的做法都会把该列的信息编码进结果集。

- **甲：不可见列比较视为假。** 探针 `NOT (secret = 1)`：内层恒假，取真，于是 `secret <> 1` 的行被返回、`secret = 1` 的行被排除；调用方逐行差分即可反推 secret。
- **乙：不可见列视为 NULL（三值逻辑）。** 探针 `secret IS NULL`：NULL IS NULL 恒真，不可见行全部返回，直接成为存在性/取值探针；普通比较的 UNKNOWN 结果同样可差分。
- **丙（采用）：整条查询拒绝。** 常量折叠后仍需读取不可见列才能判定谓词，则返回可判定错误，指出列名与其在谓词树中的路径（子节点下标序列），不返回任何行。结果集与该列取值无关，无可差分通道。

## 2. OR 短路例外（折叠后读取分析）

定义：一遍后序遍历同时做常量折叠与「读取集合」推导。对每个子树返回 `(折叠后节点, 该子树实际需要读取的列集合, 拒绝项)`，规则：

- 常量、空谓词：读取集为空。
- 列比较：读取集 = {该列}（列须在宇宙内）。
- NOT：透传子树读取集（NOT 不短路）。
- AND：任一常量子树为假 → 整树常量假，读取集空；否则读取集 = 各非恒真子树读取集之并（恒真支被吸收）。
- OR：任一常量子树为真 → 整树常量真，读取集空；否则读取集 = 各非恒假子树读取集之并（恒假支被吸收）。

仅当最终读取集与角色不可见列相交时才拒绝。理由：`secret = 1 OR TRUE` 之类分支在折叠后根本不会被求值，引擎无需读取该列；拒绝它是假阳性。例外严格限定于「折叠即可证明」，不做任何关于列值或行数据的假设。被吸收分支内的**未知列名**仍报错（构造错误与权限无关）。同一列多次出现：拒绝项去重为一条，路径列表保留全部出现位置。

## 3. 行裁剪

结果行只保留可见列：按可见列集合从源行读取并写入新 map，不可见列既不置零也不置 NULL，键物理消失，使「不可见」与「值为空」可区分。裁剪代价只与可见列数有关，与行的总宽度无关；每次目标键赋值计一次拷贝。

## 4. 复杂度

- 谓词检查：一遍后序遍历，每个节点恰好访问一次；filter 内部非导出计数器 `nodesVisited` 记录访问数，调用方可与谓词节点总数比对。
- 行裁剪：赋值次数 = 可见列中实际存在于行内的列数 ≤ 可见列数。

## 5. 包与错误模型

- `policy`：列宇宙 + 角色→可见列集合；提供并集/查询，未知角色可见集为空。
- `predicate`：节点类型为常量、列比较（含 `Is`/`NotIs` 表示 IS NULL / IS NOT NULL）、AND/OR/NOT；路径为子节点下标切片。
- `filter`：折叠+读取分析、拒绝判定、行裁剪；维护 `nodesVisited` 与 `copies` 计数器。
- `report`：被裁剪列名（字典序）、拒绝引用（列名+全部路径）、是否整条拒绝；JSON 序列化，键与数组均排序，确定性逐字节可比。

三类错误用 `errors.Is` 区分：`ErrInvisibleColumn`（权限拒绝，包装为可含多列的 `*DenyError`）、`ErrUnknownColumn`（谓词引用了宇宙外列）、`ErrUnknownRole`（按名查询未定义角色时）。
