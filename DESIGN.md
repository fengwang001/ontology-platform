# 设计推导

## 1. 零计数 hunk 头的行号

统一规则：`@@ -a,b +c,d @@` 覆盖旧文件 1 基行区间 `[a, a+b-1]`；`b=0` 时区间为空，
`a` 表示插入点**之前**的旧文件行数（0 = 文件开头），而不是“新行将占据的行号”。

- 空文件前插入：插入点前有 0 行 → `-0,0`；新侧第 1 行 → `+1`（`,1` 省略）。
- 在旧第 2、3 行之间插入：插入点前有 2 行 → `-2,0`；新行在新文件中是第 3 行 → `+3`。
- 删除唯一的第 1 行：旧侧 `-1`；新侧插入点前 0 行 → `+0,0`。

`Apply` 解释：`b=0` 时插入点在旧第 `a` 行与第 `a+1` 行之间（`a=0` 即文件开头）；
`d=0` 同理。

| b | 书写 | a 的含义 / Apply 插入点 |
|---|---|---|
| 0 | `-a,0` | a=插入点前的旧行数；在第 a 行之后插入（a=0 为开头） |
| 1 | `-a`（省略 `,1`） | 第 a 行起 1 行 |
| ≥2 | `-a,b` | 第 a 行到第 a+b-1 行 |

## 2. hunk 合并阈值

结论：间隔未改动行数 `g ≤ 2C` 合并，`g ≥ 2C+1` 分开。每个改动两侧各带最多 C 行上下文，
两段上下文在 `g=2C` 时恰好首尾相接，GNU 输出一个 hunk；`g=2C+1` 时正中间一行落在
两段上下文之外，分成两个 hunk。反例（C=1，g=2）：若阈值是 C，则 GNU 实际合并而规则
要求分开；若阈值是 2C+1=3，则 g=3 时 GNU 分开而规则要求合并。故仅 2C 一致。

## 3. 最短脚本的并列选择

Myers 按 d 层扫描 k，顺序为 d, d-2, …, -d。向下（删除）使 k 减 1，向右（插入）使 k 加 1；
k 取最大值时走删除。并列时固定**优先删除**。例 `a b → b a` 产生
`-a` / ` b` / `+a`（snake 共享行让删除尽量排在插入之前）；优先插入则会得到
`+b` / ` a` / `-b`。选择固定后补丁文本逐字节确定。

## 4. 偏移后后续 hunk 的查找起点

偏移 δ 是“定位误差”，不是文本位移，不向后传递。真正改变后续行号的是已应用 hunk 的
行数差 `Σ(新行数-旧行数)`。故第 i 个 hunk 的查找起点 = 其记录起点 + 之前各 hunk 的
累计行数差；再在 ±F 内按距离（同距取靠前）枚举候选。每个 hunk 独立在自身记录位置
附近匹配，避免一个定位误差被放大成全部平移。

## 其他约定

- 行：`\n`、`\r\n` 或末行无行尾均原样切分与拼回；无行尾通过行内的 no-newline 标记表达。
- 复杂度：Myers O(ND)，蛇行逐行比较计入非导出计数器；上限 maxD 超出即返回差异过大。
- 计数上界：每层各蛇互不相接，同层前进总数 ≤ N+M，另加每层至多 d+1 次失配比较，
总计 ≤ (D+1)(N+M) + D(D+1)/2 ≤ 4(N+M)(D+1)（本题 D≤6 的两档规模）。
- 资源上限：补丁总字节数、hunk 数、偏移 F、maxD 均可配置；超限零状态变化。
