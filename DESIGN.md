Design: 半包续传 CSV 方言（RFC4180）

1. 空行与单列空值
选择：空行不是"跳过"，而是一条含 1 个未引号空字段的记录（RFC4180 的空记录）。
空输入才产生 0 条记录；"a\n\n" 是两条记录（a, 空）。多列文件夹杂空行因此报
ErrFieldCount，是严格性的一部分。回写难题：单列末记录为未引号空字段且无末尾换行
时，文本必须是空串，而空串解析出 0 条记录——数据丢失。故 writer 对"最后一条、
唯一、未引号、空"字段必须写 `""`；非末尾位置由换行承载，无需引号。此时引号标记
被迫翻转，是"保数据"与"保标记"不可兼得时的唯一例外，AUDIT 有测试钉住。
规范输入：记录间用 \n；除上述特例（末单列空值写 "" 且不加尾换行）外，记录末不
加额外尾随换行；字段仅在必要时加引号。此时 Write(Parse(x))==x 逐字节成立。

2. par 切点
切点可在任意字节。worker 无法自知起点是否在引号内，故每段以 4 种起始假设各算一遍：
0 字段外 / 1 引号字段中 / 2 引号内刚见引号 / 3 CR待定（总处理量约 2N+8K：每个内部
切点被左右段各覆盖一次≈2，段数为常数项）。段结果带"首字节是否延续字段"与"段末状态"。
拼接：第 0 段取假设 0；其后用上一段段末真实状态选本段假设，延续字段并入上段已开字段。
段内偏移 + 段基址 = 全局偏移；字段号/记录号从拼接时累计的全局计数起编。只有真实路径
上的错误生效，故错误坐标与单线程完全一致。

3. CR 待定（stateCR）
未引号字段遇 \r 入 stateCR，不定字段：下一字节 \n 则行结束；EOF 则 ErrDanglingCR
（偏移指向 \r）；其他字节也是 dangling CR（\r 后必须紧跟 \n）。半包续传靠 stateCR
跨 Feed 保持。par 中 \r 为段尾时段末状态为 stateCR，下段首字节裁决；\r 为全 buffer
末字节时末段 EOF 收尾判 dangling CR。

4. 上限"立刻"
字段字节计数对引号字段只累计内容字节：开/闭引号不计数，`""` 转义对的第二个引号
计 1 字节，故 `""` 恰计 1 个内容字符。逐字节 +1 后立即比较 MaxFieldBytes，第一个
超限字节即返回，从不预缓冲整记录。MaxFieldsPerRecord 在逗号确定新字段时立即检查；
MaxRecords 在记录关闭时检查。终态后 Feed/Close 返回同一错误。

5. 状态转移表（输入类别：, " \r \n 普通 c）
Start: ,→Start+发空字段; "→InQ(开引号,开引号偏移记录); c→InF; \n→关记录; \r→CR
InF:    ,→Start+发字段; "→ErrBareQuote; c→InF(+1); \n→关记录; \r→CR
InQ:    ,/c→内容(+1); \r→内容(+1,原样保留); \n→内容(+1,原样保留); "→QSeen
QSeen:  ,→Start+发字段; "→InQ(+1转义); \r→CR; \n→关记录; c→ErrQuoteClose
CR:     \n→关记录(\r不计入); EOF/其他→ErrDanglingCR
EOF：Start 且记录已开→关闭末记录；InQ→ErrUnclosedQuote；其余收尾合法。末尾换行
不产生额外记录。错误为 *PosError{Byte(0起),Record,Field(1起)}，四类语法错误加
列数不一致（table 层）与 dangling CR 均哨兵可判定。
