# 游标分页器设计

仅标准库；数据为内存中按排序键有序的 Row 集合（float64 排序值 + 唯一字符串 ID），
支持并发增删。翻页是纯函数：入参为数据集快照 + 不透明游标 + 方向 + 页大小。

## 1. 排序值重复时为什么必须用复合键

假设六行排序值都为 5.0，页大小为 3。若游标只记排序值 v：

- 下一页取 `score > v`：与 v 相等的其余 3 行被整批跳过（漏）。
- 下一页取 `score >= v`：上一页返回过的 3 行再次返回（重）。

根因：排序值不是唯一键，`v` 无法在并列组内定位。正确做法：

- 游标记录复合键 `(score, ID)`，排序键为 `(score asc, ID asc)` 的全序。
- 下一页边界是复合键的**严格大于**：`(s,id) > (s',id')` 当且仅当
  `s > s'`，或 `s == s'` 且 `id > id'`。严格大于保证既不重也不漏：
  上一页最后一行严格小于边界故不重复，其余并列行按 ID 序严格大于边界故不跳过。

十行（六行 score=5）按每页 3 行翻页，每页的最后一行复合键即下一页边界，
十行恰好各出现一次，顺序与一次性全量排序一致（见 FINDINGS.md 表①）。

## 2. 反向翻页规则

反向不是「先正向取再 reverse」。规则：

- 比较方向取反：候选为复合键**严格小于**游标的行；
- 取数方向取反：从游标位置向小键方向取 `limit` 行（逆向扫描）；
- **页内顺序仍正向排列**：把逆向取到的行整体倒序后返回。

随页返回的游标约定：`Prev` 锚定本页第一行、编码为反向；`Next` 锚定本页最后一行、
编码为正向。第三页的 Prev 游标 = 锚定第三页首行的反向游标，反向取 3 行：
取 `(s,id) < (第三页首行)` 的最后 3 行并正序排列，结果正是第二页且逐行相同，
而非第二页的倒序。

## 3. 并发增删下的语义

Pager 以「不可变有序快照」(copy-on-write) 保存数据；一次 Get 只观察某一个快照，
与增删完全并发、无锁读取。承诺：

1. **不重**：边界是复合键严格序。翻页只沿一个方向推进，严格大于/小于的谓词
   永不包含已返回的键，行不会再次出现。
2. **未删行不跳过**：每页从复合键边界起在快照上连续取满，键区间内未被删除的行
   必被某一页取到；游标只含复合键，不依赖指针/下标，被锚定的行被删后仍可从该
   复合键继续翻页（其后的行依然满足严格大于谓词）。
3. 插入到**已翻过区间**的行允许不出现（不做跨快照快照隔离）；插入到**未翻区间**
   的行按复合键顺序在后续页出现。删除使该行在后续快照消失，不会报错。
4. 翻页无隐藏推进状态：同一游标被多协程同时使用，各自得到逐行相同的页。

## 4. 效率

- 数据集有序，定位边界用二分（sort.Search），随后顺序取 limit 行。
  单次翻页比较次数 ≤ `limit + ceil(log2(n))`；测试上界为
  `4 * (limit + ceil(log2(n)))`，n=10000、limit=20 时为 136。
  比较次数由 page 包内非导出计数器 `compared` 记录（每页清零）。
- 游标定长头 + ID 段，解码为 O(1) 次内存操作；测试断言解码分配数 ≤ 2。

## 5. 游标格式与错误分类

格式（大端）：`dir(1) | f64(8) | idLen(2) | id(idLen) | crc32-IEEE(4)`，
最少 15 字节；dir=1 正向，=2 反向；CRC 覆盖前 11+idLen 字节。

四类可 `errors.Is` 区分的错误：`ErrChecksum`、`ErrMalformed`、
`ErrBadDirection`、`ErrDirectionMismatch`。校验顺序与分类：

1. 总长度不足、idLen 超出剩余长度 → `ErrMalformed`；
2. CRC 不符 → `ErrChecksum`；
3. dir 非 1/2 → `ErrBadDirection`。

对合法游标逐比特翻转（18 字节 = 144 位）：结构判定先于 CRC，故

- 翻转 idLen 任一位（16 位）→ 字段长度失效 → ErrMalformed；
- 翻转 CRC 段（32 位）→ 只破坏校验 → ErrChecksum；
- 翻转 dir 段：7 位变 0/3..255 → ErrBadDirection，1 位 1↔2 → ErrChecksum；
- 翻转 score（64 位）与 ID（16 位）→ ErrChecksum。

空字节串是合法的「从头开始」，不进入解码；非空但方向与请求相反 →
`ErrDirectionMismatch`，不静默按另一方向处理。
