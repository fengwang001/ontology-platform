# 属性级权限过滤器 — 设计推导

## 模型

- `policy`：角色 → 可见列集合；另维护一个「已知列全集」（声明 + 授权之并），用于区分「列不存在」与「列存在但不可见」。
- `predicate`：谓词树，节点为 `Const` / `Compare`(列=值) / `IsNull` / `Not` / `And` / `Or`。
- `filter`：单遍、从左到右、带常量折叠地检查谓词；按可见集裁剪行。
- `report`：裁剪审计报告（被裁列、被拒引用、是否整条拒绝），输出确定性字节流。

安全不变式：**一行被返回，当且仅当过滤谓词对该行的真实数据为真**。任何让「谓词为假的行」被返回、或让答案依赖攻击者可推理的替代值的做法，都会把结果集变成关于隐藏值的预言机。

## 甲：不可见列的比较当作假 —— 泄露

设攻击者已知（或经由其他角色写入）一条金丝雀行：`id=7` 且 `secret=1`。
探针查询：`id = 7 AND NOT (secret = 1)`。

- 甲把 `secret = 1` 重写为 `false`，于是 `NOT(false)=true`，**id=7 被返回**。
- 但该行真实 `secret=1`，谓词对它的真值是假。系统「证实了一个关于数据的假命题」。
- 攻击者由此获得每查询 1 比特的确认预言机：先构造假设「行 r 的 secret=v」，再发 `id=r AND NOT(secret=v)`；甲恒返回 r，于是任何外部旁证（应用层据谓词语义展示的徽标、计数差 `count(visible=x)` 恒为 N 与 0 的签名）都被坐实。谓词语义与数据脱钩，下游一切由谓词含义推出的结论都在泄露差值。

## 乙：不可见列当作 NULL 参与三值逻辑 —— 泄露

探针查询：`secret IS NULL`。

- 乙把 `secret` 替换为 NULL，`IS NULL` 恒真，**返回全部行**；而真实数据里该列可能全非 NULL。同样违反不变式。
- 更糟的是它泄露**存在性**并提供**探针**：对任意列名 `x` 发 `x IS NULL`，返回行 ⇒「x 存在且对你不可见」，报错 ⇒「x 不存在」。攻击者可字典式枚举隐藏列名。`secret = 1` → NULL → 空集、`NOT(secret=1)` → NULL → 空集、`secret IS NULL` → 全集，这一组答案签名本身就是可识别的信息通道。

## 丙：整条拒绝并指名 —— 采用

谓词引用任一不可见列即返回可判定错误（`errors.Is` 可区分），错误携带列名与谓词树路径，**不返回任何行**。

- 唯一可观测的是「拒绝」这一事实，它与行数据无关，不携带任何数据比特。
- 代价是泄露一条策略元数据（该列存在且不可见）；这是可审计、显式的设计取舍，且远弱于甲乙的数据级泄露：甲乙让**行集合的成员资格**成为隐藏值的函数，丙让任何行都无法越界。
- 实现为一遍从左到右遍历：收集全部不可见引用（同一列多处出现时每处记一条 (列, 路径)，不去重路径），遍历结束后一次性报错。

## OR 短路例外：定义与理由

**定义**：求值语义为从左到右、常量折叠短路——`And` 的某个左子树折叠为 `false`、或 `Or` 的某个左子树折叠为 `true` 时，其余兄弟子树**不被求值**；`Not`/`And`/`Or` 的常量子树就地折叠。不可见列若只出现在因短路而未被求值的子树中，则**不视为被读取，放行**；只要它所在子树会被求值，必须拒绝。

**理由**：安全性来自「答案不依赖任何不可见列的真实值」。被短路掉的子树对任意行值都不影响结果（`OR(TRUE, …)` 恒真、`AND(FALSE, …)` 恒假），放行不泄露任何比特；反之拒绝它们属于误伤（如 `visible = 1 OR TRUE`）。该定义是确定性的、单遍可判定的，且与「访问节点数」计数器一致：被跳过的子树不计入访问。

推论（测试按此断言）：
`OR(TRUE, secret=1)` 放行；`AND(FALSE, secret=1)` 放行；
`OR(FALSE, secret=1)` 拒绝；`AND(TRUE, secret=1)` 拒绝；
`OR(secret=1, TRUE)` 拒绝（secret 在短路前已被求值）。

## 复杂度约束

- 谓词检查严格单遍：`filter` 内部非导出计数器记录访问节点数，对无常量折叠的谓词等于 `predicate.Count(树)`。
- 行裁剪只拷贝可见列：结果行新建 map，逐可见单元格拷贝一次；拷贝计数 ≤ 4 × 可见列数（实测 = 可见列数），与总行宽无关。

## 三类可区分错误

- `ErrInvisibleColumn`：引用了存在但对该角色不可见的列（携带全部 (列, 路径)）。
- `ErrUnknownColumn`：引用了全集中不存在的列。
- `ErrInvalidPredicate`：树结构非法（空节点、空 And/Or）。
