# 设计推导

## 1. 非法单元切分（推导）
首字节决定期望长度。第二字节 c 必须落在区间 I2(lead)；不合法时：
- 若 c 是**续写字节**(80..BF)，它被吞进当前非法单元（WHATWG「prepend」），从 c 的下一字节起解析；
- 若 c 是**新首字节**(C0..FF)，只吞首字节，c 作为新首字节重新解析；
随后续写字节缺失/越界时，已缓存续写逐个成立（见下），故单元吞掉 1+k 字节。
验证：F0 90 80 41：90∈I2(F0)=[90,BF]，80∈[80,BF]，第四字节 41 非续写→吞 F0 90 80，41 重解析 => 1 个 FFFD+41。
E0 80 80：80∉I2(E0)=[A0,BF] 且 80 是续写，吞 E0 80；剩 80 单独成单元、EOF 吞它 => 3 个。C0 AF：C0 非法首立即吞；AF 续写单独 =>2。ED A0 80 三个（A0 是续写被吞）。F4 90 80 80 四个。80 80 两个。E2 82 +EOF：82 是续写被吞，EOF 后整体一个 =>1。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | [80,BF] | 1（+1 若 c 是续写） |
| E0 | [A0,BF] | 1（+1 若 c∈80..9F） |
| E1..EC | [80,BF] | 1（+1 若 c 是续写） |
| ED | [80,9F] | 1（+1 若 c∈A0..BF） |
| EE..EF | [80,BF] | 1（+1 若 c 是续写） |
| F0 | [90,BF] | 1（+1 若 c∈80..8F） |
| F1..F3 | [80,BF] | 1（+1 若 c 是续写） |
| F4 | [80,8F] | 1（+1 若 c∈90..BF） |
| 80..BF | —（本就是孤立续写） | 1 |
| C0 C1 F5..FF | 无（首字节即非法） | 1 |
第二字节合法后，第三/四字节必须∈[80,BF]：非续写只作重解析起点（不吞）；EOF 时已见续写随单元被吞。

## 2. 缓存上限与可立即判非法的时机
最长序列 4 字节。状态只需 (need, seen, cp, bad)，缓存已见续写**字节数**（编码端可由 cp 重建，无需存原字节）。seen≤need-1≤3，缓存硬上限 3。
看到第二字节落在区间外即可立即判首字节单元失败（不等 EOF）；看到第三/四字节非续写，立即结束当前单元（该字节不吞）。仅当 EOF 时 need>seen 才是「截断」：该前缀在严格模式属截断而非非法——因为在更多字节到达时它仍可能合法（样例 E2 82 不允许提前 FFFD 同理）。

## 3. par 切点对齐（推导）
UTF-8 在任意切点 cut 处：
- cut 字节是续写(80..BF)：向前最多找 3 字节内的首字节 L。
  - L 存在且 L+need(L)>cut：cut 落在某序列中。该序列整段交给包含 L 的段（前一段），前段结束点延到该序列结束（按 need，最多 cut+3，绝不整段重扫）；
  - L 不存在（前 3 字节内无首字节）：这些是孤立续写，各自成非法单元，下一段从 cut 开始，cut 字节单独成单元。
- cut 字节非续写：边界就是 cut。
对齐时每个字节只被「规划」检查 1 次、被解析 1 次，故检查数 ≤ n+8·C（C 为回看常数=3）。
C3|80 80：cut 前 L=C3 need2>cut，序列 C3 80 交给前段（EOF 时成单元），第二 80 归后段单独成单元——非法单元个数与单流相同（前段 1 个、后段 1 个）。

## 4. 输出上限断点（推导）
Write 以「基础偏移 base+已消费 n」为单元起点记账。仅当某**完整**单元（非法=1 个 FFFD；合法=其目标编码字节）能整体放入上限才提交 n 与输出，否则返回 ErrOutputLimit 且 n 停在最后一个已提交单元后，缓存字节不计入 n。调用方用 data[n:] 喂新实例，拼接逐字节相同。UTF-16 不提交半个代理对。

## 并发
par 起 K goroutine，各段独立 stream.Transcoder，经 channel 收集；纯函数无共享可变状态。单 stream 实例**非并发安全**。
