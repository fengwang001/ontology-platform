# DESIGN

## 1. 零计数 hunk 头的行号规则（题面第 4 条）

三个样例：`x\n`→`y\nx\n`（C=0）头为 `@@ -0,0 +1 @@`；`a\nb\nc\n`→`a\nb\nX\nc\n`
（C=0）头为 `@@ -2,0 +3 @@`；`a\n`→空（C=3）头为 `@@ -1 +0,0 @@`。

推导：若把数字理解成「将要插入的位置（1 基）」，样例 1 应写 `-1,0`（在第 1 行处
插入），样例 2 应写 `-3,0`（在第 3 行处插入），都与 GNU 输出矛盾。三个样例中该
数字分别等于缺口之前该侧文件的行数：样例 1 插入点前有 0 行、样例 2 插入点前有
2 行（a、b）、样例 3 新文件共 0 行。所以它是「前一行的行号」（文件开头没有前一
行时为 0），不是插入位置。统一规则：设 idx 为 hunk 在该侧文件中的 0 基起始行号，
`count>0` 时写 `idx+1`（1 基首行号），`count=0` 时写 `idx`（= 缺口前的行数）。
`Apply` 解析：`count>0` → 起始行 `idx=start-1`；`count=0` → 插入点 `idx=start`，
即「在第 idx 行（0 基）之前插入」，不消费任何旧行。

| 情形 | 头部 `-a,b` 写法 | Apply 对插入点的解释 |
|------|------------------|----------------------|
| b=0  | `-a,0`，a=插入点前的行数（前一行行号，开头为 0） | 插入点=a（0 基），在第 a 行之前插入 |
| b=1  | `-a`（省略 `,1`），a=首行 1 基行号 | 起始 0 基=a-1，共 1 行 |
| b≥2  | `-a,b`，a=首行 1 基行号 | 起始 0 基=a-1，共 b 行 |

## 2. hunk 合并阈值（题面第 5 条）

结论：两段改动间未改动行数 `g ≤ 2C` 时合并，`g ≥ 2C+1` 时拆分。

推导：每个 hunk 向改动两侧各延伸 C 行上下文。两段改动相隔 g 行时，前 hunk 的
后 C 行加上后 hunk 的前 C 行共 2C 行：`g ≤ 2C` 时两侧上下文覆盖或重叠全部 g 行，
GNU 输出一个 hunk；`g = 2C+1` 时中间恰有 1 行不属于任何一侧的上下文，GNU 拆成
两个 hunk。若阈值取 C：`g=C+1..2C` 时 GNU 仍合并，矛盾；若取 2C+1：`g=2C+1` 时
GNU 拆分而阈值要求合并，矛盾。例：C=1 时 g=2 得一个 hunk，g=3 得两个 hunk。

## 3. 并列最短脚本的选择（题面第 3 条）

Myers 每步在 `V[k-1]`（右走=删除）与 `V[k+1]`（下走=插入）之间选择。本实现采用
严格小于比较：`V[k-1] ≥ V[k+1]` 时优先右走，即**平局时删除优先**。效果：同一位置
上 `-` 行排在 `+` 行之前。例：`a b`→`b a` 得到 `-a`、` b`、`+a`（先删 a、在 b 后
插 a），而非 `+b`、` a`、`-b`。规则固定，故同一输入的输出逐字节确定。

## 4. 偏移应用后后续 hunk 的平移（题面第 7 条）

前一个 hunk 以偏移 `k = 实际位置 - 记录位置` 应用成功后，后续 hunk 的查找中心跟
着平移 k：`guess_i = OldIdx_i + k`。理由：造成偏移的原因（目标文件头部增删了行）
通常对全文持续存在，不平移会把本可匹配的后续 hunk 推出 ±F 窗口导致误拒；GNU
patch 同样携带最近一次偏移。注意只需平移查找中心：各 hunk 的 OldIdx 都是旧文件
坐标，匹配始终在原文行序列上进行、输出按序拼接，前序 hunk 造成的行数差不影响
后续 hunk 的坐标系。
