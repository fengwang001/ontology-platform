# Quoted-Printable 设计说明（RFC 2045 严格子集）

## 1. 行末空白

空格/制表符只在「该逻辑行的最后一个字符」时转义（`=20`/`=09`），行内原样输出。
软换行 `=\r\n` 不是逻辑行结束：解码器删除 `=\r\n` 后，其前的字节与后一个字节属于同一逻辑行，
因此软换行前的空格是**行内空格**，必须原样输出而非 `=20`。
否则严格解码器会把 `=20=\r\n` 解成普通空格（合法），但编码不再最小；且解码后无法区分
「真行末空白」与「软换行处空白」。结论：只按硬换行（`\r\n`）和输入结束判定行末。

## 2. 软换行不拆 `=XX`

每个输入字节先映射为 token：普通字符宽 1，需转义字节宽 3（`=XX` 是原子单元）。
贪心装填，当前列 `c`：
- token 不是逻辑行最后一个 token 时，要求 `c+w <= 75`（给后续至少留 1 列）；
- 最后一个 token 要求 `c+w <= 76`；
不满足就先输出 `=\r\n`（`=` 占 1 列，计入 76），列置 0，再写 token。
例：列 74 + `=3D`（宽 3）= 77 > 76 → 先软换行，再写 `=3D`。

## 3. 往返等式

设 N 把裸 `\n` 与 `\r\n` 都规范化为 `\r\n`（单独 `\r` 保留），则
`Decode(Encode(x)) = N(x)`。即行尾被规范化，其余字节（含单独 `\r`、二进制、行末空白）逐字节还原。

## 4. 最小转义

编码器对每个字节做二选一：仅当「非可打印 ASCII」或「`=`」或「逻辑行行末空白」时转义；
这三类若不转义，严格解码器必拒绝或语义改变，故每一次转义都不可省，输出为最小转义。
因此对合法 y，`Encode(Decode(y))` 与 y 的差异只可能是 y 中多余的转义被规范化。

## 5. 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 转义与换行规范化 | `TestEscapeAndNewlines` |
| 2 76 列上限（软换行 `=` 计列） | `TestLineLength76` |
| 3 行末空白/不拆 `=XX` | `TestTrailingSpace`、`TestSoftBreakAtomicToken` |
| 4 严格解码五类错误与偏移 | `TestDecodeErrors` |
| 5 跨切分点一致 | `TestSplitConsistency` |
| 6 往返 | `TestRoundTrip` |
| 7 幂等/最小转义 | `TestIdempotentMinimal` |
| 计数器 ≤ 2n | `TestInspectionCounter` |
