# 可撤销批量重命名与冲突解析器 — 设计

## 1. 模型与合法性
- `name.Space` 为内存名字集合：`map[string]struct{}` + `sync.RWMutex`。批量执行期间
  持有写锁，外部并发修改（Add/Delete/Rename）阻塞到批量结束（语义选择：阻塞，不拒绝）。
- 合法性：**任意字符串都是合法名字**，包括空串与含路径分隔符（`/`、`\`）的串；
  分隔符只是普通字符，不做任何路径解析。

## 2. 冲突（修改前全量检测，四类用 errors.Is 区分）
对请求列表（旧名 o → 新名 n）：
1. `ErrOldMissing`：o 不在命名空间。
2. `ErrOldDup`：同一 o 出现两次，错误携带两个新名。
3. `ErrNewDup`：两个不同 o 指向同一 n，错误携带两个旧名。
4. `ErrTargetExists`：n 已存在，且 n 不在本批旧名集合中（即不会被搬走）。
自环 o→o 的 n==o 满足“目标存在但 o 在旧名集合中”，不属第 4 类。
四类检测在任何修改之前完成；冲突时命名空间逐元素不变。

## 3. 排序推导（依赖图 + 拓扑序）
请求 o→n 表示 n 必须先“腾空”。若 n 同时是某请求 m→… 的旧名，则 o→n 依赖该请求
先执行：建边 o→n→m，等价于依赖图中节点 o 指向 m。无环时：其新名不是任何待办旧名
（入度为 0）的请求先执行；同层按名字字典序。故 {a→b,b→c} 先 b→c 再 a→b，
每步目标位均已腾空，绝不覆盖。检测与排序只做哈希查找，O(R+N)。

## 4. 破环（每环恰好一个临时名）
残留入度>0 的节点全部落在不相交的有向环上（功能图每节点出度≤1）。对每个环：
1. 选环中字典序最小者 p（环 p→…→p），生成临时名 t；
2. 执行 p→t（p 腾空）；
3. 沿边执行其余边：x→p、…（每步目标都已腾空）；
4. 最后 t→目标，归位。
二元环：a→t, b→a, t→b；三元环：a→t, c→a, b→c, t→b。
每环一步腾位即足以解开，故临时名数 == 环数。临时名格式 `tpm-<k>`（k=0,1,…），
生成时在「现有名 ∪ 所有新名」集合中校验，冲突则 k++ 重试；占用集合有限，持续递增
必然找到空位（否则返回可判定的 ErrNoTempName，绝不覆盖数据）。

## 5. 自环语义
o→o 定义为**无操作**：两条 a→a 仍按 ErrOldDup 处理；唯一自环不进入计划、不产生
日志、不参与环检测。

## 6. 执行、日志与撤销
- 帧格式（小端）：`oldLen(4) newLen(4) old new crc32(4, IEEE)`；文件头魔数 `RNML1\n`
  6 字节。每步成功后 fsync 追加。
- 注入：执行器在第 k 步（1 基）可注入失败；失败后已执行步骤按日志逆序撤销，
  命名空间与执行前逐元素相同。
- 撤销：逆序读完整帧并反向改名；每帧成功即把日志截断为其前缀，故对同一日志再撤销为
  无操作（幂等）。
- 截断分类（帧头 H=8，载荷 L=ol+nl，CRC 占末尾 4 字节）：保留 0..5 字节 →
  `ErrTruncHeader`；保留 ≥6 但 < H+L → `ErrTruncRecord`；保留 ≥ H+L 且 < H+L+4
  （CRC 被切）→ `ErrTruncCRC`。撤销只执行最大可恢复前缀，并报告无法撤销的步数。

## 7. 确定性与复杂度
同层按名字字典序、环选字典序最小者，计划完全确定；打乱请求构造顺序 20 次，步骤序列
逐元素相同。plan 内部计数器记录哈希查找次数，断言 ≤ 4(R+涉及名字数)。

## 8. 边界
空请求集 → 空计划、直接成功；单请求 → 单步；大量重复旧名 → ErrOldDup；
非法情形不存在（名字恒合法）。
