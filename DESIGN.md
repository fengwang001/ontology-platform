# LZ77 流式压缩设计

## 1. 流格式（全部 little-endian varint）
| 记录 | 首字节 | 后续字段 | 说明 |
|---|---|---|---|
| 流头 | `L` `Z` | version=1 | 3 字节魔数+版本 |
| 字面量 | 0x00 | n, bytes | 1≤n；空段不输出 |
| 回指 | 0x01 | distance, length | 1≤dist≤窗口；1≤length |
| 刷新 | 0x02 | 无 | 一个 Flush 仅一条 |
| 流尾 | 0x03 | origLen, checksum | FNV-1a 64 位校验 |

## 2. 核心参数
窗口 65536；最小匹配 4；单条回指长度上限 4096；哈希链长 64；FNV-1a 与窗口无关。

## 3. 推导
### 3.1 Write 切法无关
压缩器把每次 Write 仅追加到内部缓冲；只有“从已决策位置到结尾至少保留 maxMatch-1 字节后”的前缀可决策。对每个起点取哈希链上的最长匹配；匹配取 4096 上限。保留区内的字面/匹配可能被后续字节延长，故不提前发射。于是记录只依赖完整前缀，与 Write 边界无关。Flush 将保留量强制设为 0，先决策全部输入再发射刷新标记，因此它会结束当前未决匹配；重复 Flush 时无新输入且位置未推进，输出零字节。Close 同样清空未决区，再写流尾。

### 3.2 重叠回指
块复制要求源、目标区间不重叠；dist<len 时刚复制出的字节本就应成为后续模板（dist=1 即 RLE）。实现按目标位置逐字节读取环形窗口；环形窗口每次 WriteByte 后即可读新字节。输出上限按每条记录声明长度在写入前一次性检查，再执行有界逐字节循环，避免超大重复且复杂度仍是输出长度。

### 3.3 两类越界距离
“超过已输出”说明回指指向尚无任何历史，是结构性截断/伪造；“超过窗口”说明历史存在但编码器按约定不可引用。压缩器的距离取自已插入环形缓冲的位置，且插入不会早于当前位置-窗口容量，因此不可能产生后者。并行块以“上一块末尾窗口字节预置窗口”预置窗口，但这些字节不会进入解压输出；解压器维护独立的逻辑历史长度参与上限判断，回指只影响本块输出，故“已输出”不改变，窗口从预置字典起算。

### 3.4 并行确定性
块 i 的匹配器状态=上一块末尾窗口字节预置窗口 + 本块原文；决策不读取任何压缩记录、未来块或 goroutine 完成顺序。各块独立压缩并省略所有内部刷新记录；按块序号拼接字面/回指，仅在流末尾写一条流尾。块衔接点的窗口预置保证引用可解，不需要刷新标记：刷新只表示调用方交付点，不是字典边界。workers 只改变调度，因此 1/2/4/8 字节相同；worker 多于块数时空闲。

## 4. 复杂度
每起点最多沿链检查 64 个候选；同字节串候选很快失配终止，每字节考察数有界且不随输入规模增长。`CandidatesExamined()` 只读非导出计数。

## 5. 并发与状态
单 Encoder/Decoder 实例非并发安全；多个独立实例可并发。错误均为哨兵或带偏移的 `DecodeError`，零长度流、缺尾、尾后字节分别判错。
