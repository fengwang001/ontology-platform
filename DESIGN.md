# 设计推导

## 1. 非法单元切分规则（WHATWG 风格，逐字节状态机）

首字节 L 决定本单元长度 n 与第二字节区间；每收到一个期望的延续字节就做一次区间校验：
- 校验失败：**立即**判非法，当前字节成为单元最后一个字节，且该字节重新作为新单元首字节处理。
- 未失败但输入结束：未凑齐 n 个字节时，未完成前缀整体算一个非法单元（截断时也是一个）。

`F0 90 80 41`：F0 要求第二字节 90..BF，90 合法，第三字节 80 合法，第四字节应为 80..BF 却收到
41 → 单元在 41 处终结，仅 1 个 FFFD，41 重新解析。`E0 80 80`：E0 要求第二字节 A0..BF，80 立即
非法（单元1），该 80 重解析为裸延续（单元2），第二个 80 同理（单元3）。`ED A0 80`：ED 要求第二
字节 80..9F（避开代理），A0 立即非法，三个字节各成一个单元。`C0 AF`：C0 永不合法（非最短），
C0、AF 各一。`F4 90 80 80`：F4 要求 80..8F，90 立即非法，四个各一。`E2 82`+EOF：首字节与第二
字节都合法（E2 区间 80..BF），是未完成前缀，整体恰好一个单元，故只一个 FFFD。`80 80`：裸延续
无首字节可依附，各自独立，两个单元。

| 首字节 | 长度 | 第二字节合法区间 | 非法时吞掉字节 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 1（首字节非法即止） |
| E0 | 3 | A0..BF | 1 或 2（失败点即止） |
| E1..EC | 3 | 80..BF | 1 或 2 |
| ED | 3 | 80..9F | 1 或 2 |
| EE..EF | 3 | 80..BF | 1 或 2 |
| F0 | 4 | 90..BF | 1..3 |
| F1..F3 | 4 | 80..BF | 1..3 |
| F4 | 4 | 80..8F | 1..3 |
| 80..BF | 1（裸延续） | — | 1 |
| C0 C1 F5..FF | 1（永不合法） | — | 1 |

## 2. 切分点缓存

最长合法字符 4 字节，进入字符后至多再等 3 个字节，缓存硬上限 = 3。首字节进入缓存；后续每字节
若落在当前合法区间则留在缓存，落在区间外立即终结当前单元（无需等 EOF）并把该字节退回主循环重
解析；只有"区间合法但字节数未凑齐"才继续等待。故跨切分绝不会提前吐 FFFD。

## 3. par 切点对齐

UTF-8：从段尾向前扫，遇非延续字节即停，最多回看 3 字节；尾串为「一个 C2..F4 引导字节 + 若干
延续字节」且短于其期望长度时整体移给下一段，否则段尾闭合。UTF-16：先回退奇数尾字节对齐偶边界，
再看最后一个码元：高代理则连同 2 字节一起移走。段 i 输入 = 段主体 + 段 i+1 的尾巴；段 0 偏移 0
启用 BOM，其余段起始偏移非 0 不识别 BOM；移走字节不计入本段统计。`C3 | 80 80` 时 C3 作为未成
前缀移交，段 1 见到 `C3 80 80`，得一个合法字符加一个裸延续，与单线程相同；凡判定字节落在尾中，
其首字节也在尾中，判定原子性整体移交，不会切成两个 FFFD。

## 4. 上限断点

字节逐个送入状态机；只有完整标量（或一个 FFFD）的编码能完整放入输出缓冲（UTF-16 代理对视为
整体）时才编码计数。放不下即返回 `ErrOutputLimit`，返回的 n 只含此前已完整输出标量对应的字节，
缓存内字节不算已消费。用相同配置新建实例从 `p[n:]` 续传，拼接与无限流逐字节相同。

## 检查次数

每个送入状态机的字节自增一次 `checks`（含 BOM）；非法终结点字节被退回重解析一次，故
`checks ≤ 2n`，与切分粒度无关。par 每字节属于恰好一段，额外仅 K-1 段回看，每段 ≤3（UTF-16
≤4），`checks ≤ n + 8·C`。
