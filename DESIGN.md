# 设计推导

## 代理对错误偏移（语义 2）

规则：凡是"代理对结构不完整/顺序颠倒"，错误偏移一律指向**当前这个不合法单元的起点**：

- 孤立高代理：`\uD83D` 后要求紧跟低代理，若遇到引号、普通字节或 EOF，偏移指向**高代理 `\` 的起点**。
- 孤立低代理 `\uDE00`：低代理前没有待匹配高代理，偏移指向**该低代理 `\` 的起点**。
- 顺序颠倒 `\uDE00\uD83D`：第一个 `\uDE00` 即非法，偏移指向它的起点；流提前终止，后面的高代理不检查。

理由：RFC 8259 规定 `\uD800–\uDBFF` 必须与紧随其后的 `\uDC00–\uDFFF` 成对出现。高代理只"向前看"，它与下一个单元共同构成一个不可分割的语法单元；当期望落空时，这个语法单元的起点就是高代理自身。低代理在没有待匹配高代理时本身就是非法 token，起点就是它自己。两类情况统一为"不合法代理单元的起点"。

因此 `\uD83DA` 不能解释成"替换字符 + A"：读到 `\uD83D` 时，解码器进入"必须接收低代理"的确定状态，`A` 不是 `\uXXXX` 单元的开头，错误在高代理起点即被确定。解码器从不在中途把一个高代理降级为 U+FFFD——那会静默吞掉本应报错的结构错误（非法 UTF-8/代理都不得静默替换为 U+FFFD）。

## 最小转义与唯一性（语义 4）

U+001F 属于 RFC 8259 明确禁止在未转义文本中出现的 C0 控制字符（U+0000–U+001F），必须转义；U+007F 是 DEL，不在该区间内，JSON 允许其原样出现，故不转义。`/` 无前置反斜杠也合法，非 ASCII（含 U+2028/U+2029，这是 JSON 不是 JS 源码）以 UTF-8 原样输出即可。

单射与唯一编码：

1. 原样字节与转义形式不冲突——反斜杠只由转义产生，而 `\` 自身被转义为 `\\`，解码时每个反斜杠都必然开始一个转义序列，故解码无歧义。
2. 每个可转义码点只有唯一选择：5 个短形式字符（`\b \f \n \r \t`）用短形式，其余控制字符用 `\u00XX`（固定 4 位小写），`"` 与 `\` 各有唯一形式。
3. 其他所有码点逐字节原样输出，选择同样唯一。

故每个字符串恰有一种编码（存在唯一性）；解码又能逐位恢复原文（可逆），故 Encode 为单射，且 Decode(Encode(s)) == s。对只用这套最小转义写成的字面量，其转义形式与 Encode 的选择完全一致，故 Encode(Decode(y)) == y。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类严格解码错误可区分且带偏移 | `TestDecodeErrors` |
| 2 代理对组合与拒绝 | `TestSurrogates` |
| 3 非法 UTF-8（解码拒绝/编码报错） | `TestInvalidUTF8` |
| 4 最小转义编码 | `TestEncodeMinimal` |
| 5 双向往返 | `TestRoundTrip` |
| 6 任意切分点一致 + 计数器 ≤ 2n | `TestStreamSplits` |
