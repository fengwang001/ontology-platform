# DESIGN

## 1. 空输入与只含换行的输入
策略：Keep=保留原样；EnsureOne=输出恰以一个 `\n` 结尾（缺则补、多则删）；TrimBlankOne=删全部末尾空行再补一个 `\n`。
对空输入选 `""`（而非 `"\n"`）。理由：N 必须幂等。若 EnsureOne("")="\n"，则 N("\n")="\n"，N(N(""))="\n"≠N("")，幂等破坏。
选 `""` 时三策略对 `""` 都给 `""`；对任何非空文本都以恰好一个 `\n` 结尾，该形态经 EOL/空白规范化与策略处理后不变，故幂等成立。
`"\n"`、`"\n\n"` 不含非空白内容：EnsureOne 视为「需要恰好一个结尾换行」；TrimBlankOne 删末尾空行后补一个；
在空白规范化后的文本空间里二者产出相同，故样例表两列一致；实现仍独立（对一般输入如 `"a\n\n"` 描述不同、结果相同；仅含空行时无差异）。

| 输入 | Keep | EnsureOne | TrimBlankOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

补充：`"a\n"` 三策略均 `"a\n"`；`"a\n\n"` → Keep `"a\n\n"`，另两个 `"a\n"`；`"a"` → Keep `"a"`，另两个 `"a\n"`。

## 2. 被删字节的 ToOut
设删除区间 `[ds,de)`（行尾空白或 `\r\n` 中的 `\r`）在输出位置 `o` 处。删除点左侧身份字节映到 `o` 之前，右侧映到 `o` 之后；
删除区间内字节没有自己的输出位置，只能取边界。为保持 `ToOut`、`ToOrig` 均单调不减且 `ToOut(ToOrig(o))==o`，
令删除区间内所有 `i` 满足 `ToOut(i)=o`（整段映到删除点的右侧边界），并令 `ToOrig(o)=de`（删除后第一个保留字节，即 `\n`），
于是 `ToOut(de)=o` 互逆成立。光标停在被删空白中时落在行尾换行符 `\n` 本身（换行之前、行尾位置），落点稳定且唯一。

## 3. 空白缓冲超限
延迟判定必须缓冲整串待定空白，上限 M。若强制判定为「非行尾」并提前输出，而其后真出现行尾，输出与无上限结果不同，违反 4/5。
故选择拒绝：第 M+1 个空白字节处返回哨兵 `ErrPendingOverflow`（带原文偏移），已输出保留，实例进入终态（再写返 `ErrClosed`）。
已提交输出即此前确定内容，错误偏移即溢出空白位置，状态可解释。输出总字节上限同理：提交前检查，超限返 `ErrOutputLimit`（带原文偏移），终态、内容保留。

## 4. par 拼接
段独立处理时段尾携带未定状态：P（待定空白串，|P|≤M）与 CR（待定 `\r`，≤1 字节），故修正范围上界为 M+1 字节的常数状态。
拼接只检查相邻边界：(a) 前段以 CR 结束且后段首字节为 `\n`：合并为一个 `\n`，后段该 CR 的删除点平移到全局；
(b) 后段首个确定 EOL：前段 carry 的 P（含孤立 `\r`）判为行尾空白/`\r`，删除；(c) 否则 P 原样补出，孤立 `\r`→`\n`。
段映射先做坐标平移（orig 加段在原文件起点并按 carry 来源修正，out 加已拼接输出长度），再插入 carry 产生的删除/身份区间。
末尾策略仅对全局文本应用一次（段级 Close 不应用策略），故与单流逐位一致。

包依赖：`eol`、`ws`、`span` 无依赖；`norm`→三者；`par`→`norm`、`span`。单个 `norm` 实例非并发安全。
`span` 只存删除点区间（orig 区间 + 对应 out 位置），纯 `\n` 文本无删除点 ⇒ 区间数为常数；查询二分。
NUL/非法 UTF-8 默认原样通过；严格模式遇 NUL 立即 `ErrNUL`（带原文偏移），已输出保留、终态。
