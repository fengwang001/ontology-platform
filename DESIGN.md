# DESIGN — 可分裂二维网格索引

## 1. 边界归属规则（左闭右开）

每个单元格占据半开矩形 `[x0,x1) × [y0,y1)`。点 `p` 属于格 `c`
当且仅当 `x0 <= p.X < x1` 且 `y0 <= p.Y < y1`。

推论（全部可直接由定义推出，无需额外约定）：

- **分裂线归属**：分裂取中点 `mx=(x0+x1)/2, my=(y0+y1)/2`。
  左半为 `[x0,mx)`，右半为 `[mx,x1)`。故 `p.X == mx` 的点**只**进右侧子格；
  `p.Y == my` 的点**只**进上侧子格。四个子格两两不相交且并集覆盖父格，
  归属唯一。
- **格的四个角**：`(x0,y0)`（左下）在格内；`(x1,y0)`、`(x0,y1)`、
  `(x1,y1)` 均在格外（右/上边界为开），分别属于右邻、上邻、右上邻格。
- **查询矩形边界**：查询矩形同样左闭右开。落在查询矩形**左/下边界**上的
  点命中；落在**右/上边界**上的点不命中（`p.X < x1` 不成立）。
- **退化查询矩形**：`x0==x1` 或 `y0==y1` 时区间为空，合法但查不到任何点。
- **±0.0**：Go 中 `0.0 == -0.0` 为真，比较语义天然视为相等，无需特判。
- **NaN / ±Inf**：NaN 使一切比较为假，会静默丢失；Inf 无法定位子格。
  插入时拒绝，并计入可读的 `Skipped()` 计数。

## 2. 分裂终止条件推导

分裂把格按中点分成四个子格，原有点全部下沉到子格。

**为什么坐标全同会无限递归**：设所有点坐标均为 `q`。分裂后 `q` 唯一落入
包含它的那个子格，其余三个子格为空。该子格仍超容量则再分裂，`q` 仍唯一
落入下一层某个子格——任一层的单子格点数都不减少，"超容量即分裂"的朴素
规则永不满足停止条件，递归不收敛（最终栈溢出）。

**终止条件（两道闸门，任一触发即停止分裂）**：

1. **深度上限 `maxDepth`**：每次分裂子格深度 +1，深度达到 `maxDepth`
   （取 48）的格不再分裂，超容量的点直接留在该叶格。递归深度被常数
   限制，故必终止。
2. **坐标不可再分**：浮点精度耗尽时 `(x0+x1)/2 == x0` 或 `== x1`
   （y 同理），分裂产生的子格与父格相同，分裂无意义，拒绝分裂。

两道闸门都只在"分裂无法取得进展"时触发；正常分布的点远在到达上限前
就已把每格点数降到容量以下。分裂前后点总数不变：分裂只是把父格
`points` 逐点重新插入子格并清空父格，不新增不丢弃。

## 3. 删除后不合并的理由

- **避免抖动**：若删除到阈值即合并，在容量附近交替插入/删除会引起
  分裂-合并反复震荡，每次搬运整格子树，均摊复杂度变差。消除震荡需
  引入迟滞（合并阈值远低于分裂阈值），换来实现复杂度。
- **删除保持 O(depth)**：不合并时删除只需定位叶格并移除点。
- **空子格代价有界**：深度上限 48 且只有超容量才分裂，空壳子格数量
  受历史峰值点数约束，查询剪枝（相离即跳过）使其对查询几乎无影响。

## 4. 持久化格式（自描述）

```
header:  magic[8] "OGRIDv1\n" | version u32 | cellCount u32 | headerCRC u32
record:  x0,y0,x1,y1 f64×4 | flags u8(bit0=divided) | nPoints u32
         | points f64×2×nPoints | recordCRC u32
```

记录按先序排列：父格之后紧跟其四个子格子树。读回时校验：魔数/版本、
头 CRC、每条记录 CRC、先序结构（已分裂格必须恰好跟随 4 个子格）、
子格边界必须含于父格。截断按切口位置分类：头部不完整 / 格记录头
不完整 / 点数据不完整 / CRC 不匹配；恢复时取最大完整记录前缀重建。
写盘先写 `path.tmp` 再 `rename` 原子替换；发现残留 `.tmp` 即视为
崩溃残骸并清理。
