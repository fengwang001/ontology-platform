# DESIGN — 只读快照一致性导出器

## 1. 问题与模型

存储在导出期间持续被写入。要求：导出结果等于「快照时刻」的逻辑状态，
支持分块、断点续传、完整性校验。只允许标准库，导出文件写本地临时目录。

核心机制是 MVCC + 写时复制（COW）：

- `store` 为每个键保存按版本升序的值链 `[(ver, val)...]`，全局版本号单调递增。
- `snapshot` 句柄记录水位 `W`（获取时刻的全局版本），读 `k` 即读链上
  「≤ W 的最大版本」对应的值。导出只通过快照读，因此导出的永远是同一逻辑时刻。

## 2. 写时复制的触发点（推导）

假设不用 COW：写入直接覆盖旧值，则导出扫到该键时读到新值，结果混入
快照时刻之后的数据 —— 不一致。因此**触发点必须在写入路径上**：

- `Put(k,v)` 时，若存在活跃快照，则该键的旧版本必须保留（不能就地覆盖），
  新值以新版本追加到值链；导出经快照读到的仍是 ≤ W 的旧值。
- 若没有任何活跃快照，旧版本对任何人都不可见，`Put` 时立即折叠只留最新值。

所以 COW 的内存开销 ∝ 导出期间被改写的键数（每个被改写键多留一份旧版本），
与总键数无关。`store.Retained()` = Σ(每键版本数 − 1)，即为此开销的精确计数。

## 3. 保留值的释放条件（为什么按最老快照判断）

快照 S 的水位为 W(S)。对某个键，S 可见的版本是「≤ W(S) 的最大版本」。
多个快照并存时，一个旧版本只要对**任一**活跃快照可见就必须保留；
而「≤ 最老水位 Wmin 的最大版本」一旦被丢弃，最老快照立即读到错误数据，
故保留集合的下界由**最老快照水位**决定。GC 规则（快照关闭时触发）：

- 每键必留最新版本；
- 对每个活跃水位 W，留下「≤ W 的最大版本」；
- 其余版本全部释放。

推论：两个重叠快照 S1⊂S2（W1<W2），关闭 S2 后 Wmin 不变，旧值仍被 S1
钉住；只有 S1 也关闭、活跃集为空时，旧版本才全部释放，`Retained()` 归零。

## 4. 导出顺序无关性（论证）

导出结果 = 快照时刻的键值集合 {(k, v_W(k))}，这是一个**集合**，与枚举
顺序无关。为让「总校验和」可比较，清单中的 `TotalCRC` 定义为逐条目
`crc32(k‖v)` 的**求和**（加法可交换、可结合），因此字典序与逆序导出得到
相同的 `TotalCRC` 与相同的内容集合。分块边界由「固定顺序 + 固定块大小」
决定，同一快照同一参数下分块完全确定 —— 这是续传逐字节一致的基础。

## 5. 文件格式（自描述分块）

```
[CHK idx:4 len:4 crc32(body):4][body] ...   若干块
[MAN mlen:4][manifest JSON]                  清单段
[crc32(清单段):4]                            尾部校验
```

- 块头自描述（序号、体长、体 CRC），清单含块列表、总校验和、快照版本、
  块大小、导出顺序。
- 尾部 CRC 只覆盖清单段：续传时凭清单即可确定性重建「剩余块 + 清单段 +
  尾部 CRC」，与一次性导出逐字节相同（若尾部 CRC 覆盖全文件，续传方没有
  前缀字节就无法重建，故如此设计）。

## 6. 截断分类（可判定错误，均可 errors.Is 区分）

按截断点落位分类：

- 落在清单段（含块边界处清单缺失）→ `ErrManifestIncomplete`
- 落在某块头内 → `ErrChunkHeader`（带块号）
- 落在某块体内 → `ErrChunkBody`（带块号）
- 落在尾部 4 字节 → 清单完整但尾部校验缺失/不符 → `ErrCRCMismatch`

块乱序/缺失：解析到期望块号 i 处读到其他块号 → `ErrChunkMissing(i)`，
指名块号，不退化为「总校验和不符」。

## 7. 快照过期拒绝续传（论证）

续传拼出的文件 = 已导出的前 k−1 块（时刻 T）+ 新导出的剩余块。若原快照
已关闭，剩余块只能来自「当前数据」（时刻 T′>T），拼出的是跨时刻文件，
违反一致性且无法被任何校验发现（每块自身 CRC 都合法）。因此 `Resume`
必须校验快照句柄仍活跃且版本等于清单记录版本，否则返回
`ErrSnapshotExpired`；已导出部分留在原地不删除（它本身合法，可凭
仍活跃的同版本快照重试）。关闭与导出竞争时，关闭立即使后续 `Get`
返回 `ErrSnapshotExpired`，导出以错误终止，绝不产出被当成完整的半截文件。

## 8. 资源约束（可证明）

- COW 内存 ∝ 改写键数：见 §2，`Retained()` 精确计数。
- 读取次数 = 键数：导出对每个键恰好 `Get` 一次，键列表走索引不计读。
- 驻留 ≤ 一块：导出器只缓冲当前块，写满即落盘，`Peak()` 记录缓冲峰值。
