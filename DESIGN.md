# 设计推导：带双向偏移映射的流式行尾与空白规范化

## 1. 末尾换行策略：空输入与只含换行输入

先做行尾与行尾空白规范化（与策略无关），再处理末尾 `\n` 个数 t。

- Keep：不动末尾。
- One（确保恰好一个）：t>1 裁到 1；t=1 不动；t=0 且输出非空则补一个 `\n`；t=0 且输出为空则保持 `""`。
  关于空输入的两种选择：若 `"" -> "\n"`，则 N 把“无行”变成“一个空行”，但因 `N("\n")=="\n"`，仍然幂等；
  若 `"" -> ""`，空输出是不动点，同样幂等。两种都满足第 5 条。选 `"" -> ""`：不凭空制造内容，
  且空文件与单空行文件不被混同。非空且缺尾换行时补 `\n`，`N("abc")=="abc\n"`，而 `N("abc\n")=="abc\n"`，幂等。
- Strip（删末尾空行留一个换行）：t>=1 裁到 1；t=0 不动（没有可删的末尾空行，也不新增）。
  `N("abc\n\n")=="abc\n"` 与 `N("abc")=="abc"` 都是不动点，幂等。

| 输入 | Keep | One | Strip |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

设删除区间 [a,b)（行尾空白，或 `\r\n` 中的 `\r`），删除发生时已输出 L 个字节，删除点之后第一保留字节输出位置为 L。
ToOut 必须单调不减，且对任意输出 o 有 `ToOut(ToOrig(o))==o`。ToOrig(o) 取“产生 o 的原文首字节”，
故保留字节的原位置必须映回自身位置。删除区间内无任何输出字节，其 i 若取小于 L 的值会破坏
`ToOut(a-1) <= ToOut(a)` 的左侧关系空间并使后续保留字节错位；唯一自洽选择是

  对所有 i ∈ [a,b)：ToOut(i) = L。

于是：行尾空白里的光标映到行尾 `\n` 的位置之前（即行内容末尾、换行符上）；
`\r\n` 中的 `\r` 映到 `\n` 自身的输出偏移，与 `\n` 同位。编辑器语义：被删字符上的光标吸附到删除点右缘，
不跳过换行符。ToOrig(o) = o +（outPos <= o 的删除长度之和），区间内恒为 b；代入得 ToOut(b)=L，互逆成立。

## 3. 空白缓冲超限

强制判定为“非行尾”并输出：当该串实际位于行尾时，输出残留行尾空白，直接违反第 2 条，
且违反第 5 条“输出不含行尾空白”（第二次规范化结果可能不同），映射虽可构造但语义已破。
选择**拒绝**：返回可判定错误 `ErrWS`（带原文偏移），已输出内容保留，规范化器进入终态，
之后 Write 返回既有终态错误。拒绝不违反第 4/5/6 条：截断点（超限点）之前的输出与映射与正常处理完全一致。

## 4. par 拼接修正

每段独立用 Keep 策略规范化。跨段危险只有两类：

1. `\r`|`\n`：左段在自身 EOF 把尾 `\r` 当独立行尾输出了一个 `\n`。修正：删除左段输出末尾这 1 字节，
   并在合并映射中补一条删除记录（原文 `\r`，输出位置为该字节被删前的位置，长度 1）；右段的 `\n` 保持。
2. 空白串跨段：段在自身 EOF 一律把尾部空白判为行尾并删除。向右扫描“整段全空白”的后续段，
   找到空白串后的第一个非空白字节：是 `\r`/`\n` 或到达 EOF，则原判正确；否则撤销涉及段的末条删除记录，
   从原始缓冲区把这些空白字节补发到该段输出末尾（承载该串后继的右段原本就正确保留了自己的前导空白）。

修正范围有上界：每段至多动末尾 1 字节、末 1 条删除记录、并补发一段属于空白串本身的字节，
与段内数据量无关，修正总量 O(K)。映射平移：段 s 记录 (orig,out,n) 平移为
(orig+lo_s, out+base_s, n)，base_s 是此前各段**修正后**输出长度之和；记录按原文序喂入合并 Map，
相邻删除自动合并，与单线程记录逐条一致。最后在拼接输出上统一执行末尾换行策略，
裁剪尾部 `\n` 时用合并映射的 ToOrig 取得其原文源偏移。
