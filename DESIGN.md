# 三方合并冲突检测器 — 设计文档

## 1. 数据模型与规范化编码（doc 包）
- 记录：`Record = map[string]Value`；集合：`Set = map[string]Record`（键 → 字段名 → 值）。
- 值类型只有两种：`String(s)` 与 `Number(float64)`；类型不同的值不可能相等（即使 `1` vs `"1"`）。
- 规范化编码（字节级确定性）：键按字典序；同键内字段按字典序；字符串用 `strconv.AppendQuote`
  转义；空键、空字段名、空串值均合法，长度前缀保证「空串值」与「字段不存在」可区分。
  布局：每条记录 `r <keylen:4> <key> <nf:4>`，每字段 `f <namelen:4> <name> <t:1> ...`
  （字符串给长度+字节，数值给 float64 的 8 字节大端）。

## 2. 单侧变更集（delta 包）
对一侧集合与祖先逐键逐字段比较：
- 新增 Added（整键祖先不存在）；删除 Deleted（整键侧上不存在）；
- 修改 Modified：逐字段得到 Added/Changed/Removed（字段粒度，保留祖先值便于三方合并）。
- 校验：同一键同时出现在删除集合与新增/修改集合中 → `ErrContradiction`（带键名）。

## 3. 冲突判定表（merge3 包）
按键在祖先 A / 左 L / 右 R 的存在状态分类（`-`=不存在，`+`=存在）：

| A | L | R | 情形 | 结果 | 冲突 |
|---|---|---|------|------|------|
| + | + | + | 两侧均未改 | A | 无 |
| + | + | + | 仅一侧改 | 取改动侧 | 无（规则1） |
| + | + | + | 两侧改同字段为相同值 | 该值 | 无（规则2） |
| + | + | + | 两侧改同字段为不同值 | 保留冲突字段 | FieldConflict |
| + | + | + | 两侧改不同字段 | 字段级合并，其余取 A | 无（规则5） |
| + | + | - | 仅右删 | 删除 | 无 |
| + | - | + | 仅左删（对称） | 删除 | 无 |
| + | + | - | 右删、左改（或反之） | — | DeleteModifyConflict（规则3） |
| + | - | - | 两侧同删 | 删除 | 无（规则4） |
| - | + | - / - | + | 仅一侧新增 | 新增内容 | 无 |
| - | + | + | 两侧同键新增，规范化编码相同 | 任一侧 | 无（规则6） |
| - | + | + | 两侧同键新增，内容不同 | — | AddAddConflict（逐字段附字段冲突） |

关键推导：
- **规则2**：冲突的定义是「两侧意图不可同时满足」。两侧把同一字段从同一祖先值改成同一新值，
  两个意图完全一致，自动满足，故不是冲突；判定必须比较新值，而非「是否都动过」。
- **规则3**：修改方的意图依赖记录继续存在（字段挂在记录上），删除方意图是记录消失，
  两个意图互斥；且与「两个新值二选一」的 FieldConflict 语义不同，故单列 DeleteModify。
- **祖先缺失**（`- + +`）：不报错，按两侧各自新增处理；内容一致则自动合并。
- **类型不一致**（字符串 vs 数值）：新值不同的一种，产生 FieldConflict，报告中列出两侧类型。

## 4. 复杂度与确定性
- 合并只扫描键并集一遍：用三张存在/记录位图表（构建时每键 3 次 map 查找），
  之后全部按索引访问；查找计数上界 = `3 × 键并集大小 ≤ 4 × 键并集大小`。
- 冲突报告按冲突清单逐条取证（每冲突最多访问一条记录），不二次遍历全部记录。
- 所有输出按键字典序、同键字段名字典序排序；结果不含时间戳，与 map 迭代/插入顺序无关。

## 5. 空侧语义（边界）
「某侧集合为空」表示该侧**未做任何改动**（identity 操作），不是「删除全部记录」。
删除必须显式：某键在祖先存在而该侧不存在才是对该键的删除；空集合无法表达删除任何具体键。
因此祖先非空、一侧为空时合并结果恒等于另一侧。

## 6. 序列化（serialize 包）
文件 = 15 字节定长头 + body：头含 magic(4)、CRC32 of body(4)、bodyLen(4)、version(1)、flags(1)。
读回时按失败位置分类：头部字节不足 → `ErrHeaderIncomplete`；body 不足 bodyLen 或内部记录截断
 → `ErrRecordIncomplete`；长度够但 CRC 不符 → `ErrCRCMismatch`（与 `errors.Is` 区分）。
