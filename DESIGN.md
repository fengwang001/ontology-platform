# 设计推导

## 1. 零计数 hunk 头

统一 diff 的 `-a,b`/`+c,d` 用 1 起行号，区间是连续的旧/新行。若区间为空，给
定的“行号”表示该空区间前面最后一行的行号；在文件开头插入时为 0。

- 旧文件 `x\n`，新文件 `y\nx\n`：空旧区间在开头，故 `-0,0 +1 @@`。
- 旧文件 `a\nb\nc\n`，新文件 `a\nb\nX\nc\n`：空旧区间紧跟旧第 2 行，
  故 `-2,0 +3 @@`。
- 新文件为空且删除旧第 1 行时，新区间紧跟新第 0 行，故 `+0,0`。

Apply 把空旧区间的插入点放在逻辑行索引 `a`：`a=0` 插到开头，`a=2` 插到旧第
2 行之后。空新区间只用于定位删除区间结束处。

| 旧侧计数 | 书写 | 区间/插入点 |
| --- | --- | --- |
| b=0 | `-a,0` | 空区间；点在索引 a（旧行 a 之后） |
| b=1 | `-a` | 旧行 a |
| b>=2 | `-a,b` | 从旧行 a 起的 b 行 |

新侧 `+c,d` 规则相同；删除整个文件时为 `+0,0`。

## 2. hunk 合并阈值

相邻两处改动间有 g 个未改动行。每个 hunk 最多在两端各取 C 行上下文；若
`g <= 2C`，两边上下文重叠或相接，GNU diff 合并；若 `g = 2C+1`，前一处只取
前 C 行、后一处只取后 C 行，中间至少剩 1 行，故分开。

例：C=1、g=2 时合并，改动为 `X/g1/g2/Y`，输出含 `X/g1/g2/Y`；阈值若取 C，
会错误分开。g=3 时必须分开为 `X/g1` 与 `g3/Y`；阈值若取 2C+1，会错误合并。

## 3. Myers 并列选择

Myers 每条对角线选择 `down`（删除）和 `right`（插入）的较优者；分数相同采用
`down` 优先，即优先删除。脚本按从左到右保留行，先输出全部相邻删除，再输出
插入。因此 `a b -> b a` 的脚本固定为 `-a`、` b`、`+a`，而不是先插入。

## 4. 偏移应用与后续 hunk

候选位置以 hunk 记录的旧起点为中心，绝对距离 0..F，距离相同取较前位置。
已应用 hunk 会把净行数 `ins-del` 加到后续文本中，所以后续 hunk 的中心也平移
所有已应用 hunk 的累计净变化；搜索半径仍为 F。这样比较的是当前文档坐标，而
不会让一次插入固定地推动所有后续 hunk 超出容差。

所有 hunk 先只试选位置并校验上下文/删除行，全部成功后再从后向前替换，保证
原子性。
