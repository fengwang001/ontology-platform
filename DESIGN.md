# DESIGN 推导（与 AUDIT.md 合计不超过 100 行）

1. 零计数 hunk 头行号。GNU unified diff 规则：`-a,b` 的 a 永远是「该 hunk 覆盖的旧区间起点的 1 基行号」，
   覆盖为空（b=0）时起点取「插入点前一行」的行号，文件头处无前行则为 0；`+c,d` 对称。
   - `x` → `y x`（C=0）：纯插入，旧区间为空，在第 0 行之后插入 ⇒ `-0,0`，新区间是第 1 行 ⇒ `+1`。
   - `a b c` → `a b X c`：在旧第 2 行之后、第 3 行之前插入 ⇒ `-2,0 +3`。
   - `a` → 空：删旧第 1 行，新区间为空，删除点前有 0 行 ⇒ `-1 +0,0`。
   解析时插入点 = 目标序列下标 a（0 基，即第 a 行之后）；b=0 不校验删除行；d=0 替换后写入 0 行。

   | 情形 | 书写 | a 含义 | Apply 插入/锚点（0 基） |
   |---|---|---|---|
   | b=0 | `-a,0`（a 可为 0） | 插入点前一行行号 | 在目标下标 a 处（第 a 行后）插入 |
   | b=1 | `-a`（省略 ,1） | 唯一行的行号 | 校验下标 a-1，替换 [a-1,a) |
   | b>=2 | `-a,b` | 首行行号 | 校验 [a-1,a-1+b) 后替换 |

2. 合并阈值。C 行上下文时两 hunk 间隔未改动行 g。GNU diff -U C：前段尾部带 min(C,...) 行、后段头部
   带 min(C,...) 行，两段上下文恰好相接即合并，相接条件 g <= C+C = 2C。故 g <= 2C 合并、g >= 2C+1 分开。
   反例 C=1、两改动隔 g=3 行（改 `1 X . . . Y 7` 中 X、Y）：GNU 输出两个 hunk——前段带走 1 行、后段
   带走 1 行后仍剩 1 行无上下文覆盖，无法同 hunk；阈值取 C(=1) 或 2C+1(=3) 都会错误合并 g=3，只有
   2C 正确（边界：g=2 合并、g=3 分开）。C=0 时阈值 0，相邻改动全分开。

3. 并列最短路径选择。Myers 更新同一 k 时规定 **优先删除**：到达同一点的两条等长路径取来自 k-1（向下=
   删除）者；V 用 1 基索引、先取 k+1 再取 k-1 实现。于是 `a b` → `b a` 生成「删 a、b 公共、插 a」，
   hunk 内排列为 `-a`、` b`、`+a`（删除块在插入块之前，公共 snake 行夹在中间），而非插入优先的
   `+b`、` b`、`-b`。偏好固定，故同一输入重复生成补丁逐字节相同。

4. 偏移后后续 hunk 的平移。hunk i 记录起点 p_i、实际应用点 q_i，偏移 o_i=q_i-p_i。hunk i+1 的查找中心
   平移为 p_{i+1}+Σo_j（j<=i 累计位移）。补丁头行号相对原始文本，而目标已被前面 hunk 的
   delta（插入数−删除数）整体推后/拉前，累计位移恰为 Σ(插入−删除)。搜索仍限中心 ±F 行；多候选取
   |候选−中心| 最小，平手取靠前者（下标小者）。
