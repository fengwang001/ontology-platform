# 设计推导

## 1. 空输入与末尾换行策略

策略定义：`Keep` 原样；`One` 输出为空→给 `"\n"`，非空且末字节非 `\n`→补一个，其余不动；
`Trim` 先删全部末尾 `\n`，结果非空再补一个。
幂等关键：规范化输出保证每行（除可能的最后一行）都以 `\n` 结束、无 `\r`、无行尾空白，
故再次规范化时「核心内容」不变，末尾策略在其自身输出上为不动点。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

空输入选 `One→"\n"`：则 `N("\n")="\n"`，幂等。若选 `""`，则 `N("\n")="\n"≠""=N("")`，
违反「等价类收敛」且使 One 在 `""`/`"\n"` 上振荡，故选 `"\n"`。Trim 对空输入若也补
`\n` 就与 One 完全相同、失去三选一意义，故 Trim 空输入保持 `""`（删 0 个、结果空、不补）。

## 2. 被删字节的 ToOut

不变量要求 `ToOrig`/`ToOut` 单调不减，且 `ToOut(ToOrig(o))==o`。
区间为左闭右开 `[0,len)` 加终点。删除区间 `[a,b)`（其后保留内容从输出偏移 o 开始）：
保留侧锚点是 o（下一个存活字节）。令删除字节统一映射 `ToOut(i)=o (a≤i≤b)`，
含终点 b；`ToOrig(o)=a`（删除区间起点），于是 `ToOut(ToOrig(o))=ToOut(a)=o` 成立；
两侧 `ToOut` 均不减。`[a,b)` 不是合法输出偏移，故 ToOrig 无需在其上取值。
`\r\n`：`\r` 被删、`\n` 保留在 o，故 `ToOut(off(\r))=o`（指向该 `\n`）。
行尾空白 `[a,b)` 后接 `\n` 于 o：光标落在被删空白里映射到换行符位置（行尾），
编辑器据此停在换行前；映射到「换行之后」会跳到下一行行首，错误。

## 3. 空白缓冲超限

强制判定为「非行尾」并输出，等价于把空格/制表符保留：若后续出现真正行尾，则与
语义 2（行尾空白必删）矛盾，且再规范化会删掉这些字节，破坏幂等（语义 5）；
映射虽可建表，但 `N(N(x))≠N(x)`。故选择**拒绝**：返回带原文起始偏移的
`ErrPendingLimit`，已输出内容与映射保留，normalizer 进入终态，后续 Write/Close 返回
`ErrClosed`。终态可解释：输出 = 该偏移之前前缀的规范化结果。
输出上限同理：追加会超限即返回 `ErrOutputLimit`（带原文偏移），不追加，进终态。

## 4. par 拼接

段独立规范化时，只有段尾可能受下一段影响：「最后一个已发出 `\n` 之后」的尾巴只可能由
空格、制表符、至多一个待定 `\r` 组成（再出现 `\r\n`/`\n` 即发出新行，边界归零）。
故每段产出 = 已定前缀（输出+span）+ 悬尾（原文一段连续字节）。
拼接：第 k 段悬尾与第 k+1 段原文合并重跑；段内 span 为段局部坐标，平移量
= 全局原文起点（orig 侧）、此前已接受前缀的输出长度（out 侧），段表仅在 cut 点截断。
修正范围有上界：每个接缝只重扫「上一段悬尾 + 下一段直到首个换行（含）」，
无换行时悬尾并入下一段悬尾，总重扫量 O(n)。切点任意（含 `\r|\n`、空白串中间）一致。
