# 设计推导

## 1. 末尾换行策略（空输入推导）
规范化主体（行尾归一 + 删行尾空白）后，策略只作用于结尾：
`Keep` 原样；`EnsureOne` 把结尾连续多个 `\n` 折叠为一个；`TrimBlank` 删除全部结尾空行后若有内容则保留一个 `\n`。

关键推导：策略**绝不能凭空合成 `\n`**。设 x="a"，若 N(x)="a\n"，合成的 `\n`（输出偏移 1）必须有 ToOrig(1)∈{1,2}。
但终点必须 ToOut(2)=2（输出长度），而 "a" 的原始终点 1 同样只能映到 2，于是 ToOut(ToOrig(1)) 为 2，不可能等于 1，违反第 6 条互逆。
故空输入与无尾换行的非空输入都不补换行；`EnsureOne` 只折叠、不合成。三种策略对空文档输出均为 ""，且都幂等。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut
不变量要求 ToOut 单调不减，且每个输出偏移 o 都能被 ToOrig 命中（ToOut(ToOrig(o))==o）。
删除在输出轴上不占位置，删除区间 [a,b) 内的原偏移 i 没有任何输出字节可指。
唯一不破坏单调与互逆的取值是该删除间隙的**左边界输出偏移** c（即后续幸存字节的起始位置）：ToOut(i)=c（a≤i<b）。
对 `\r\n`：`\r` 删除、`\n` 保留，c 即输出 `\n` 的位置。编辑器光标落在被删空白或 `\r` 上时落在行尾 `\n` **之前**，符合“删除发生在换行符之前”的直觉；终点 ToOut(len)=len。

## 3. 空白缓冲超限
延迟空白在见到行尾前无法判定。强制输出会违反第 2 条（下一字节恰为 `\n` 时本应删除，字节已不可收回），
进而违反幂等（第 5）、切分一致（第 4，不同切分点超限位置不同）与映射互逆（第 6）。
故选择**拒绝**：返回带原文偏移的哨兵错误 ErrPendingOverflow，此前输出保留，待定空白仍在缓冲中、可解释，实例进入终态（再写返回 ErrClosed）。
上限约束的是“单个待定空白串”的长度。

## 4. par 拼接
每段用 Keep 独立流式规范化，得到：输出、映射事件、段尾是否有待定 `\r`、段尾待定空白串（长度≤上限，有上界）。
拼接按全局段起点平移坐标，并在接缝做有界修正（修正量 ≤ 空白上限 + 1，与输入大小无关）：
- 前段待定空白并入 carry；后段首字节为行尾 → carry 整体删除；为普通字节（含 NUL/非法 UTF-8）→ carry 原样保留。
- 前段结尾待定 `\r`：后段首字节为 `\n` 则该 `\n` 删除、`\r` 以 `\n` 输出（前段坐标）；否则 `\r` 在接缝处输出为 `\n`。
- 全部段消费后做一次全局收尾（等同单次 Close 的待定处理），最后统一施加结尾策略（仅删除尾部，映射记删除区间，绝不合成）。
carry 与 1 个待定 `\r` 即跨段全部状态，故修正范围有上界；1..8 段、任意切点与单流逐字节一致。
