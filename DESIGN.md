# 只读快照一致性导出设计

## 目标

在 KV 持续变化期间，导出某一逻辑版本的只读快照；导出文件落在本地临时目录，支持按块续传、乱序/缺失检测、逐字节完整性校验。只使用标准库。

## 1. 版本与写时复制

存储每次提交把版本号加一；快照持有创建时的版本水位 `v`。

写新值前，若当前存在不晚于新版本的活跃快照，必须先复制该快照可见的旧值，而不是先原地覆盖：

1. 快照读取键 `k` 时，先查该快照的保留表。
2. 保留表有 `k`，说明导出期间它被覆盖，读取保留的旧值。
3. 没有 `k`，说明快照版本后它未被覆盖，读取当前表即可。

因此“导出到第 50 个键时第 80 个键被改写”不会影响结果：第 80 个键虽尚未导出，但其快照旧值已在写入提交前复制，之后扫描会读到旧值。

## 2. 何时保留与释放

新写入只需满足仍可能读取该版本的最老活跃快照。若最老快照版本为 `oldest`，新版本号大于等于 `oldest` 时，被覆盖键的旧值对该快照仍可见，必须保留。

按最老快照判断即可：较新的快照也从其水位读取；某键若在最老快照后被覆盖，保留的正是覆盖前值。若写入版本早于所有活跃快照，则没有活跃快照需要该旧值，无需复制。

快照关闭时注销并等待进行中的该快照读操作结束，然后清空自己的保留表。多个重叠快照各自持有自己需要的保留值；引用由注册表协调。最后一个快照关闭后，注册表不再要求保留旧值，保留计数归零。

## 3. 导出顺序无关性

逻辑快照是固定版本下的键值集合。导出只是枚举这个不可变集合；字典序、逆序只改变块内/块间排列，不改变键值集合。

清单中的总校验和是每条自描述记录 CRC32 的异或：`total ^= crc32(record)`。异或满足交换律，所以块顺序和枚举顺序都不影响结果，又无需缓存全部值；空键与空值正常编码，不存在的键不产生记录。

## 4. 文件格式与续传

文件由若干数据块和末尾清单块组成，均小端编码：

- 数据块：magic `DATA`、块号、条目数、头部 CRC、体长度、体 CRC、体。
- 块体依次编码每条记录：键长、值长、键、值。
- 清单块：magic `MANI`、快照版本、总条目数、总校验和、块数、每块条目数、体长度/CRC、头部 CRC、体 CRC、体。

块号从 0 开始；清单独立记录每块条目数，所以可定位任意块的第一条记录。续传从第 `k` 块时保留现有前 `k` 块，从同一快照重新枚举并跳过前 `k` 块的记录，覆盖写后续块；输出计划由版本、顺序和块大小唯一确定，因此与一次性导出逐字节相同。

## 5. 快照过期

续传只凭旧文件和清单还不够；必须重新提供同一活跃快照句柄。若快照已关闭，其保留值已释放，继续扫描可能读到当前数据，并把旧前缀与新后缀拼成跨版本文件。

所以续传开始先校验快照仍注册且版本等于清单版本；过期立即返回 `ErrSnapshotExpired`，函数不截断、不重写原路径。调用方可稍后删除文件，但不能把它当完整文件。

## 6. 完整性分类

解析顺序提供四类可判定错误：

- 末尾没有完整清单块：`ErrManifestIncomplete`。
- 数据块固定头不足：`ErrBlockHeaderIncomplete`。
- 头完整但声明的体不足：`ErrBlockBodyIncomplete`。
- 长度足够但头部或体 CRC 不符：`ErrCRC`。

校验还比对块号连续、偏移和每块 CRC；乱序返回带实际/期望块号的错位错误，缺失返回带缺失块号的错误。

## 7. 资源约束

- COW 只保存导出期间被覆盖且活跃快照需要的旧值，空间随被改键数增长，不随总键数增长。
- 快照读接口每键调用一次；导出顺序只遍历一次。
- 每块序列化为一个不超过块大小的缓冲，写完即可复用；不累积全量键值。
- 空存储、单键、空键、空值、块大于总数据均合法；块大小为 0 返回 `ErrInvalidBlockSize`。
