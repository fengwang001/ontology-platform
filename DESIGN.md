# Properties 解析器设计推导

## 一、空白续行的行为（对应语义第 3 条）

依据 `java.util.Properties` 内部 LineReader 的单遍状态机推导（OpenJDK 源码）：

1. 每个物理行先跳过前导空白（空格/制表/换页）；跳过后若直接遇到行尾，则判定为“空物理行”，
   直接结束当前正在拼装的逻辑行，**不消费挂起的续行反斜杠状态**，该物理行也不参与拼接。
2. 续行只发生在“行尾恰有奇数个反斜杠”时：反斜杠被消费、换行被丢弃，下一物理行的前导空白
   被剥掉后追加到同一逻辑行。
3. 因此：若被接上的下一物理行只含空白或为空，第 1 步使它在“剥光前导空白”后立即触发行尾，
   当前逻辑行就此终止；再下一物理行是全新逻辑行（可成为注释或新键）。
   例：`k=a\` + 空行 + `w=b` 得到 `k=a` 与 `w=b`，而不是 `k=aw=b`。
4. 与“续行首部空白被剥掉”的关系：空白剥离是逐物理行无条件执行的；当且仅当剥完后该行还有
   内容时，它才作为续行内容追加。剥离使空白行“坍缩为空”，空则不能提供续行内容，链终止。
5. EOF 特例：最后一行以单个反斜杠结尾时没有下一物理行可拼，load0 的反转义循环遇到行尾
   悬空反斜杠直接丢弃它（不保留反斜杠）。

## 二、回写转义规则的推导（对应语义第 6 条）

回写行形如 `key=value`，Load 的判定规则决定了哪些字符必须转义：

- 键：Load 先剥键的前导空白，再找第一个未转义的 `=`/`:`/空白作分隔符，且行首 `#`/`!`
  使整行变注释。故键必须转义：行首空白（否则被剥）、行首 `#`/`!`（否则变注释）、
  任何 `=`、`:` 与空白（否则成为分隔符）。
- 值：Load 找到分隔符后剥“紧邻值的”前导空白，之后所有字符（含中间/结尾空白、`#`、`!`、
  `=`、`:`）都原样属于值。故值中只有：行首空白必须转义（否则被剥），以及控制字符
  （`\n` `\r` `\t` `\f` 会破坏物理行或语义）与反斜杠本身必须转义。
- 值中间空格不转义：分隔符一旦确定，扫描不再以空白切分，中间空格是值内容。
- 值开头的 `#`/`!` 不转义：注释只在“物理行首”判定；它们出现在分隔符之后，永不成为注释。
- 非 ASCII 以 UTF-8 原样输出（本实现刻意不做 Java store 的 `\uXXXX` 转义）。
- 统一以 `=` 作分隔符；空键写成 `=value`；空值写成 `key=`。

## 三、语义与测试函数对照表

| # | 语义 | 测试函数 |
|---|------|----------|
| 1 | 三种分隔符与空白 | `TestDelimiters` |
| 2 | 注释行与行内 # | `TestComments` |
| 3 | 续行奇偶/EOF/空白续行 | `TestContinuation` |
| 4 | 转义与 \\uXXXX 行列号 | `TestEscapes` / `TestUnicodeErrorPosition` |
| 5 | 重复键覆盖且顺序按首次 | `TestDuplicateKeys` |
| 6 | Store 必要转义与往返 | `TestStoreEscape` |
| 7 | 1000 组随机往返 | `TestRoundTripRandom` |
| 复杂度 | 字节检查计数 ≤ 2n | `TestBytesChecked` |
