# Quoted-Printable 设计推导

## 推导 1：行尾空白必须转义

空格/制表符是可打印的，本应原样输出。但传输代理可能剥离物理行末尾
的空白，且严格解码器把「行末未转义空白」列为错误（语义 4）。编码输出
必须被严格解码器接受（语义 6），所以编码器在「空白位于输出行末尾」时
一律写 `=20`/`=09`：即空白是输入行最后一字节，或下一个 token 会迫使
软换行紧跟其后（此时列号 col+1==75，下一个 token 必然放不下）。

## 推导 2：`=XX` 不可拆分 + 软换行前的空格

编码按 token（1 列字面量或 3 列 `=XX`）贪心填行：当前行已有 col 列、
下一个 token 宽 w，若 col+w 超过上限（非行末 token 上限 75，给软换行
的 `=` 留 1 列；行末 token 上限 76），先插 `=\r\n` 再输出该 token。
token 是原子单位，故 `=XX` 永不拆开；插入点只由 col+w 决定，不回退。

**软换行恰好插在一个空格之后时，那个空格算不算行末空白？** 算，必须写
`=20`。推导：解码器删除的是软换行的 `=\r\n` 本身，空格物理上仍位于该
传输行的末尾；严格解码器对「行末未转义空白」报错不区分硬/软换行，否则
语义 6（编码输出必须被严格解码器接受）被违反。因此编码器在 col+1==75
且后面还有 token 时就把该空白转义（转义后宽 3 列放不下，改为先软换行
再在新行写 `=20`）。

## 往返等式的准确形式

`Decode(Encode(x)) == norm(x)`，其中 `norm` 把 x 中每个 `\r\n` 和每个
落单 `\n` 都替换为 `\r\n`，其余字节（含落单 `\r`）原样保留。即输入里
的换行在往返后统一为 `\r\n`，这是语义 1 的直接推论。

## 幂等与最小转义

编码器是**最小转义**的：字节 b 被转义当且仅当 b==`=`、b 不在 33–126、
或 b 是行末空白——正好是 RFC 2045 的必需集，不多转义任何可打印字节。
因此对严格解码器接受的任意 y，`Encode(Decode(y))` 与 y 的差异只可能
是：(a) y 里对可打印字节（含非行末空白）的多余转义被还原为字面量；
(b) 十六进制统一为大写（同一转义的拼写归一，不改变"转义了哪些字节"）。
除此之外无差异：软换行位置不变，因为严格解码器额外要求软换行是必要的
（贪心满行，否则报 `ErrBadSoftBreak`），而编码器恰好按同一贪心规则断行。

## 语义 ↔ 测试对照表

（实现完成后补齐）
