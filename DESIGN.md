# 设计推导

记窗口长 w，最小/大块长 min/max。滚动哈希取 Rabin-Karp：
H = Σ b[i]·B^(w-1-i) mod 2^64；推进一字节 H = H·B + 进 − 出·B^w，O(1)。
边界谓词 P：从喂满 w 字节起每个位置都计算 H，当前块起点之后喂满 min 字节起才允许判定，
H mod M == 0 即内容边界（M=2^6，平均 64 字节一边界）。

## 1. 最小块长：甲 vs 乙

甲：起点后前 min 字节不喂哈希；乙：照常喂哈希，只是前 min 个字节内不判定。
两者不等价：甲在偏移 min 处才建窗，第一个满窗要到偏移 min+w−2，乙在该区间早有满窗，
故甲会漏掉乙能命中的第一个内容边界；二者哈希序列从偏移 min+w−1 起才逐点相同。

下面所有算式均取 w=3, min=4, B=7，谓词 H(x,y,z)=49x+7y+z ≡ 0 (mod 11)，
字节按 ASCII 值；块长 ≥min=4 即可切，即窗口结束偏移 ≥3 起允许判定。
取流 S = `abcd0,f8`（偏移0..7；各模 11 值：a9,b10,c0,d1,'0'4,','0,f3,'8'1）。
- H("abc")=1·…=5≠0，H("bcd")=7≠0；
- H("cd0")=49·0+7·1+4=11≡0（结束于偏移4）；
- H("d0,")=49·1+7·4+0=77≡0（结束于偏移5）；
- H("0,f")=49·4+7·0+3=199≡1≠0（结束6）；
- H(",f8")=49·0+7·3+1=22≡0（结束7）。

原始分块：
- 乙：偏移4 "cd0" 命中且块长5≥min ⇒ 第1块边界在偏移4之后，第1块=`abcd0`，
  第2块从偏移5开始（偏移5 的命中对第2块而言块长1，被 min 抑制）。
- 甲：偏移4 才喂第一个字节，偏移6 才有满窗："0,f" 不命中，偏移7 ",f8" 命中
  ⇒ 甲的第1块=`abcd0,f8`（8 字节）。**同一段内容，切点不同**。

插入位置改为删除：删掉偏移0 的 `a`，得 S'=`bcd0,f8`。
- 乙：窗口照常推进，"cd0" 移到结束偏移3，块长恰为 min=4 ⇒ 第1块'=`bcd0`，
  其后各块与原流逐块同址。**受影响旧块仅 1 个**（第1块）。
- 甲：从新偏移4 才开始喂，偏移6 才有满窗；"cd0"(新偏移3)、"d0,"(新偏移4)
  对甲都不可见，"0,f" 不命中，直到 ",f8"(新偏移7) 才切 ⇒ 第1块'=`bcd0,f8`，
  吞入了原第2块的内容 `f8`。**受影响旧块有 2 个**，且切点退到更远。

甲仍满足不变量 1（建窗规则只依赖字节流本身），但它的不变量 2 常数严格比乙差
（删除禁区内 1 字节即多吃掉一个整块），且实现上还要特殊处理建窗期，无收益。
结论：采用乙——每个字节都恰好进一次滚动哈希，min 只抑制谓词、不抑制窗口推进。

## 2. 最大块长强制切分

走到 max 仍无内容边界时必须在偏移 max 处强制切。这一刀的位置只取决于"距块起点偏移 = max"，
与字节内容无关，破坏了切点完全由内容决定的性质：强制点之后所有块的起点被这个非内容锚点
重新标定。若其后一直没有内容边界（对抗性周期输入），强制点按 max 周期出现；在这条纯强制
链上插入 k 字节且 k 不是 max 的倍数，则链上每块内容整体平移，远处块地址全部不同——
局部性常数在此退化为无界。一旦常数距离内出现新的内容边界，链在该内容点立即重新对齐，
影响恢复有界。故第 3 条的常数以"附近存在内容边界"为前提；测试用 1/64 高命中谓词+固定
种子随机数据，并单独构造 max 强制切演示这一刀。

## 3. 局部性常数

设插入/删除点落在旧块 B0 内，其后第一个命中内容边界为 e。乙方案：
- e 之前的窗口若与插入点相距 ≥ w，则窗口与旧流逐字节相同，哈希相同，e 必然仍命中；
- 最近情形是 e 距插入点 < w，此时 e 可能错过，滑到下一个命中点 e'。
旧侧：被改变的旧块为 B0；若 e 在相邻旧块 B1 内且恰好错过，再加 B1 ⇒ 至多 2 个旧块。
新侧：插入点到重新对齐点之间至多 2 个新块，对齐后与旧 B2… 逐块同址 ⇒ 至多 3 个新块。
min 禁区只会使"新边界距上一边界不足 min"时把边界再推迟一次，推迟点仍在上述范围内，
常数不增大。测试断言：旧块受影响数 ≤2、新块 ≤3（独立于流长，10^4/10^5 两档同断言）。

## 4. 末尾不足一块

候选：单独成块（末块可 <min）或并入前块（合并后长度 ≤ max+min−1，可能超 max）。
由不变量 3，两者都能逐字节重组；由不变量 1，决定只能依赖完整字节流，故 Flush 时把剩余
r（0<r<min）并入前一块：前块地址在 Flush 前不发布，不产生"半块"。空流（块数 0、总字节 0）
与零长 Write（总字节仍 0、块数仍 0）行为一致，但与"有内容"可按块数/总字节区分；
整个流长度 < min 时没有前块可并入，剩余作为唯一一个块发布（≥1 且 <min），SelfCheck
允许"最后一个块短于 min"这唯一例外，短流情形因此自洽。
