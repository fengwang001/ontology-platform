# CSV 方言解析器设计推导

## 1. 空行与单列空值

规则：连续两个换行之间不含任何字节，既没有字段也没有逗号，结构上**不是一条记录**（记录至少含 1 个字段），因此空行一律**跳过**，不计记录号；文件末尾换行同理不产生记录。由此单列空值记录无法写为空串：空串加末尾换行会被当作"末尾换行后无记录"而丢失。

结论：单列表里唯一字段为空的记录**必须写成 `""`**（加引号空字段）。回写器的"必要引号"集合由此推出：字段含 `,`、`"`、`\r`、`\n`，**或字段为空串**。多列中的空字段不加引号（`,,` 无歧义）。解析侧因此能区分未引号空字段与 `""`。

## 2. par 切点：双假设 + 确定性串接

每个段无法仅凭本段字节判断起点是否在引号内。令段以"起点在引号外、字段开始位"（OUT）与"起点在引号字段内、段首引号不计内容"（IN）两种起点状态各跑一遍同一状态机，处理量 ≤ 2n。段以 `""` 中第二个 `"` 开始时 IN 的起点态为"刚见过一个引号"：实现上给该段前补一个虚拟 `"` 字节（全局偏移 -1）再跑 IN，额外开销每段 1 字节，总计 ≤ 2n+K。

段边界处的真实状态由左段唯一决定，从左到右确定：若左段结束于"引号内/引号内刚见引号"，右段取 IN；否则取 OUT（CR 待定见 §3）。逐段选出的版本与"从全局字节 0 一次跑过"的状态轨迹逐字节相同，故拼接语义、错误完全相同。

拼接：状态机产出的不是整字段而是 `(start,end)` 片段（指向原缓冲区，不拷贝）。跨切点的同一字段，左段尾片与右段 IN 的首片拼成一个 cell（引号标记由该字段真实起点决定：新字段首字节是 `"` 才 quoted；跨段时左段知道该未决字段是否 quoted）。IN 版本段首引号不计内容、不计进任何片段。字段值在需要时（错误诊断/回写）才按 quoted 标志解码，`""` 解码为一个 `"`。

坐标换算：状态机一切偏移均为绝对字节偏移（段调用时传入 base，切点任意都不特殊），错误偏移天然是全局值；记录号/字段号按段内计数，串接时左段完成的完整记录数与当前字段号作为右段的加基数；引号未闭合错误只可能出现在最后选中的尾段，位置直接可用。

## 3. CR 待定状态

未引号字段见到 `\r` 不能立刻区分行尾与孤立 CR：进入 CR 待定态，`\r` 不计入字段；下一字节是 `\n` → 行结束；是其它字节 → 在该 `\r` 的偏移报 ErrBareCR。半包续传时该状态随解析器挂起，下一次 Feed 自然判定；流在 `\r` 处 Close → 在该偏移报 ErrBareCR（未看到行尾）。

par 切点落在 `\r` 与 `\n` 之间：左段结束态为 CR 待定，右段真实起点是"待定后下一字节"——OUT 与 IN 都不够，需要第三种子状态。为保持 ≤2n：右段 OUT 结果在首字节为 `\n` 时做 O(1) 改写（丢弃待定 `\r` 已产生的任何片段尾，把该 `\n` 当行尾，该字节不重复计数）；首字节非 `\n` 时在左段 `\r` 偏移直接报 ErrBareCR，右段从字段开始位正常起算。引号字段内 `\r` 是普通内容，无待定态；引号闭合后出现 `\r` 同未引号字段走待定。

## 4. 上限的"立刻"

字节计数器记录每个被状态机消费一次的输入字节（IN 前补的虚拟引号也计 1，故 par 有常数项）。单字段字节数在**字节进入字段内容的那一刻**累加：未引号态每消费 1 字节 +1；引号态普通字节 +1，成对 `""` 在第二字节（确认是转义而非闭合）+1（一对引号 = 一个逻辑字符）；到达上限的字节当场返回 ErrFieldTooLong，不再消费后续字节。字段数在每个新字段（首字段随新记录、之后每个逗号）开始时检查 ErrTooManyFields；记录数在每条**非空行**记录开始时检查 ErrTooManyRecords。错误后解析器进入终态冻结错误，已产出完整记录保留。

## 5. 状态转移表

状态：F=字段开始；U=未引号字段中；Q=引号字段中；X=引号内刚见引号；C=CR 待定。类别：逗号、引号、CR、LF、O=其它、EOF。

| 现态 | 输入 | 次态/动作/错误 |
|---|---|---|
| F | O | U，开片段，字节计入 |
| F | , | F，结束空字段(unquoted) |
| F | " | Q（字段 quoted，内容片段延后） |
| F | CR | C |
| F | LF | F，行尾；无任何字段则空行跳过 |
| U | O | U |
| U | , | F，结束字段 |
| U | " | **ErrQuoteInField**（偏移=该字节） |
| U | CR | C |
| U | LF | F，行尾 |
| Q | " | X |
| Q | O/CR/LF/, | Q，原样为内容 |
| X | " | Q，转义，逻辑长度 +1 |
| X | , | F，结束字段 |
| X | CR | C |
| X | LF | F，行尾 |
| X | O | **ErrCharsAfterQuote**（偏移=闭合引号） |
| C | LF | F，行尾 |
| C | O/,/"/CR | **ErrBareCR**（偏移=CR 偏移） |
| F | EOF | 正常结束 |
| U/X | EOF | 结束末字段，正常结束 |
| Q | EOF | **ErrUnclosedQuote**（偏移=起始引号） |
| C | EOF | **ErrBareCR**（偏移=CR 偏移） |

片段边界：unquoted 片段为内容字节区；quoted 片段含开闭引号，解码时剥外层引号并把 `""`→`"`。记录号、字段号从 1 起，字节偏移从 0 起。流式解析器实例非并发安全；par 内部各段用独立状态机并发。
