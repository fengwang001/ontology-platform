# 组提交 / 顺序保证器 设计

模块 `ontology`，仅用标准库。包：`req` `batcher` `wal` `commit` `recover` `cmd/demo`。

## 1. 序号分配时机

- 到达即分配：批落盘失败会留下空洞，回放也无法重建连续序号。
- 成功后才分配：等待者无法在落盘前得知“我排在谁后面”，且要再次同步唤醒。
- 规则：**批次在 batcher 封口后、交给 WAL 落盘之前，由唯一的提交协程一次性分配**
  `base=计数器+1 … base+n-1`（全局序号从 1 起）。落盘（含 fsync）失败则
  **整批序号作废，计数器回滚到 base-1**，全批调用方收到同一个错误。
  因此成功批次的序号必然连续、无重叠；坏批不消耗序号，下一批紧接上一成功批。

## 2. 批次原子性：批级 CRC

磁盘文件格式：

- 文件头 5 字节：magic `GWAL1`（无批次时也创建该头文件，见 §7）。
- 每个批次一条记录（大端）：
  `baseSeq u64 | count u32 | payloadLen u32 | payload | crc u32`
  payload = 逐条 `len u32 | data`；crc 覆盖「批头 16 字节 + payload」。

CRC 是**批级**而非条级：回放逐批解析，任一字节损坏/缺失只表现为整批失败，
不可能“只丢一条”。回放遇到第一个坏批即停止（崩溃点之后的字节也不可信），
已完整通过校验的前缀批次全部可见。

截断点分类（文件长度 L，按长度 t=1..L-1 截断后回放）：

- 头不完整：`t < 5`。
- 批头不完整：`5 ≤ t < 某批起始+16`（连 16 字节定长头都没写完）。
- 条目不完整：批头完整但 `t < 起始+16+payloadLen+4`（payload/CRC 缺失）。
- 批 CRC 不匹配：长度完整但 CRC 校验失败（只能由字节改坏触发，纯截断不产生）。

## 3. leader 选择与唤醒不串台

- batcher 用单提交协程作为每个封口批次唯一的 leader：leader 分配序号、调 WAL
  追加+同步；其余协程把请求投入通道后在**各自的 `req.Result` 通道**上等待。
- 批内每条请求携带批内下标 index；leader 成功后按 index 逐一发送
  `{Seq: base+index, Err: nil}`，失败则全部发送同一错误。结果通道为每请求独立、
  容量 1，故 leader 永不阻塞，等待者也只会读到自己那条，不会串台。
- 批次经队列串行提交，天然保证落盘顺序 = 封口顺序 = 序号顺序。

## 4. 攒批触发（三者先到）

`maxCount` 条、`maxBytes` 字节（payload 口径 = 每条 4+len(data)）、
`maxWait` 超时。计时器可注入（`Clock` 接口：now/newTimer）。
单条 `4+len(data) > maxBytes` 时**不拒绝**：立即以其独占一批封口（该批允许
超过字节上限；条数上限恒为 ≥1，单条不会超过条数上限）。
空载荷合法（占 4 字节）。maxWait=0 时每请求立即成批。

## 5. 资源与故障

- 同步次数 = 成功批次数（每批恰好一次 fsync）；10000 请求 / 上限 100 ⇒ ≤200。
- 故障注入：WAL option 可注入 sync 错误；写一半崩溃用「截断文件」模拟；
  CRC 坏批用「改写一字节」模拟。
- 错误哨兵（均可 `errors.Is`）：`ErrClosed`（commit）、`ErrHeaderTruncated`、
  `ErrBatchHeaderTruncated`、`ErrEntryTruncated`、`ErrCRCMismatch`（wal）。

## 6. Close 并发语义

Close 停止接收：竞态中的请求要么已进入最后一批并随 Close 同步落盘（成功必在日志
中），要么收到 `ErrClosed`。不存在“成功但未落盘”。Close 后文件仅含完整批次。

## 7. 边界语义

- 零请求 Close：产生仅含 5 字节文件头的文件（定义为合法空日志，回放得 0 批）。
- 单请求；maxCount=1（逐条提交）；maxWait=0（立即成批）；空载荷；超大单条独占批。
- 回放序号从 1 连续递增；批内条目顺序 = 序号顺序。
