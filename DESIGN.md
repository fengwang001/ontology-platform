# RLE 设计推导

## 严格解码拒绝清单（不变量：Encode(Decode(t)) == t）

1. 显式次数 `1`（如 `1a`）：Encode 规定次数 1 必须省略，重编码得 `a`，与 `1a` 不等。
2. 次数 `0`（如 `0a`）：Encode 不产生 0；且 0 次游程语义为空，无法用任何游程重编码出 `0a`。
3. 前导零（如 `01a`、`00a`）：Encode 写无前导零十进制，重编码最多得 `1a`（其本身也非法），永不等于 t。
4. 相邻同符号游程（如 `2a3a`）：Encode 取最长游程，二者必合并为 `5a`；只有看到下一游程首符号才能判定。
5. `\` 后非数字非反斜杠（如 `\x`、`\é`）：该符号无合法转义写法；Encode 不会产生这样的 t。
6. 末尾孤立 `\`：转义不完整，不构成任何游程，无对应编码。
7. 末尾只有次数没有符号（如 `12`）：游程必须以符号收尾，无法重编码（`12` 会被当作一次新游程的开头）。
8. 非法 UTF-8：解码输出必须是合法 UTF-8 字符串，多字节序列跨切分到达时先缓存，见到确定无效字节即报错。

以上每类各有哨兵错误，统一包成具名 `Error{Kind, Offset}`，Offset 为输入字节偏移。

## 大次数（语义 3）的取舍

选择**设上限拒绝**：次数上限 `runs.MaxCount = math.MaxInt64`。
逐位累加（`n = n*10 + d`），每次做溢出检查，超出立即返回 `ErrCountTooLarge`，不回绕、不静默出错。
理由：次数是游程长度，输出字节数与之同阶；64 位上限远大于任何可实际写出的输出，
放开到任意精度只会使解码端可被 `999…a` 触发无界分配。`Decoder.Limit`（默认 64 MiB）
在实际写出前做乘法溢出检查与总量检查，超限返回 `ErrOutputLimit`，因此不会按次数预分配。

## 语义—测试对照表

| 语义 | 钉住的测试函数 |
| --- | --- |
| 1 往返 | `TestRoundTripRandom`（随机）/ `TestEncodeSamples`（样例） |
| 2 规范形式/严格拒绝 | `TestRejects`（八类各带偏移） |
| 3 大次数/输出上限 | `TestLargeCount` / `TestLimit` |
| 4 最长游程、不做规范化 | `TestEncodeSamples`（`2a3a` 样例由拒绝项钉合并） |
| 5 跨切分点一致 | `TestAllSplits`（逐字节切分，含 `2a3a`） |
| 计数器 == 输入字节数 | `TestScannedCounter` |
