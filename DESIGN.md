# 带背压的流式聚合管线：设计与推导

链路：source（按 offset 产出）→ parse（字节→记录）→ 聚合（按 key 分组 sum）→ sink（落盘）。
各级之间是有界 channel；每级单 goroutine 顺序处理，offset 单调，天然 FIFO。

## 1. 「已消费位置」记到哪一条（核心推导）

设记录 r 的 offset 为全局单调位置 n。定义三个位置：
- R(n)：source 已读出 n（可能还在某级队列里在途）；
- A(n)：n 的值已并入内存分组（但输出未必落盘）；
- D(n)：n 已随某份输出快照原子改名落盘。

错误的两种定义：
- 记 R：崩溃恢复后从 n+1 继续，但 ≤n 中可能有记录仍卡在队列、从未落盘 → **丢**。
- 记 D 却不约束在途：D 之前的记录虽落盘，但队列里还压着 offset 更大的记录；
  D 的推进若早于「全部更小 offset 在途清空」，快照与位置不构成原子边界，
  恢复点可能落在中间状态 → **重/丢不可判定**。

正确定义：已消费位置 C = 最大的屏障 offset b，使得 **b 及其之前每一级在途集合都为空**，
即 R、A、D 对所有 n≤b 都已完成，且各级队列中不存在 n≤b 的记录。

实现：周期性在 source 流中注入屏障 B(b)，随记录流过 parse→聚合→sink。
聚合遇到 B 时产出一份完整快照；sink 把快照写临时文件、fsync、原子改名，
随后才写检查点 C=b。因为单 goroutine + FIFO，B 到达 sink 时，
所有 offset≤b 的记录必然已全部并入该快照，各级对 ≤b 的在途集合为空。
崩溃点只可能在：改名前（无新输出，C 未动）→ 从旧 C 重放，旧快照仍完整；
改名后、写检查点前 → 新输出已存在但 C 仍旧值，恢复时从旧 C 重放并用同名原子改名
覆盖该快照。快照为确定性全量，故恢复后输出与一次顺跑逐字节相同。

## 2. 背压与资源约束
- 有界 channel 满即阻塞上游；source 包装层统计阻塞次数，限流器统计当前/历史最大在途。
- 可证明在途数 ≤ 各队列容量之和（每个在途记录恰占一个队列槽，各容量 Q_i）。
- 分组 map 超过 MaxGroups 返回 ErrTooManyGroups 并停止。

## 3. 故障与恢复
- sink：快照写 `out.tmp`，校验和不匹配/存在残留即视为未完成，恢复时先删除。
- ckpt：双文件轮转（.1/.2），内容含 SHA256；损坏则取另一个完好文件并标记回退。
- 坏记录：缺 key 或解析失败计入 bad，不进任何分组，不中断。
- source 报错：先过屏障使已处理部分落盘+检查点，再如实返回错误。

## 4. 优雅停止
context 取消后各级排空在途记录再退出；Stop 幂等；测试用 goroutine 计数钉死无泄漏。
