# 只读快照一致性导出器 — 设计推导

目标：键值数据持续变更的同时，导出某一时刻 T 的一致快照；支持按块续传与完整性校验。

## 1. 写时复制（COW）触发点与释放

存储维护单调递增版本号：每次 Put 后版本 +1。快照 S 持有创建时的版本水位
`snapVer`，且只可见版本号 <= snapVer 的已提交值。

**触发点**：导出读取 key 与并发写竞争时，若直接覆盖，后读的导出会拿到新值，
与已导出的旧值拼成跨时刻文件。因此写入不能原地覆盖：每次 Put 前，必须把
"当前所有活跃快照中最需要旧值的那个" 尚不可见新值的旧值保留下来。

活跃快照按版本从老到新形成链 S1 < S2。每条记录包含 `(value, ver)`，
`ver` 是该值被写入时的提交版本。保留项存放在某个快照的 keep 区，记录其
提交版本 wv。判定规则（Put 时从最新快照向最老快照遍历）：
- 先在较新快照 keep 区找该 key 且 wv <= 当前快照 snapVer 的保留项；
- 若找到，当前快照可见它，无需再保留；
- 若找不到，则当前快照必须保留旧值（它没有任何可看到旧值的来源）。

于是每次 Put 至多在最老的"需要旧值"的快照里存一份。导出读取时沿
keep 链从新到老查 wv <= snapVer 的保留项，命中则用保留值，否则读当前值
（当前值版本必 <= snapVer，否则前面必有命中）。

**释放条件**：关闭最老快照 S1 时，把它 keep 区里"下一个快照 S2 还需要"
的项转移给 S2：S2 已有同 key 保留项则丢弃（S2 那份更新且它可见），否则
转移。没有下一个快照时全部丢弃。这正是"按最老快照版本判断"的理由：
保留项只为覆盖 `(最老快照版本, 当前版本]` 区间的覆盖而存在；最老快照
消失后，该区间收缩到次老快照，多余版本区间的保留项随之失去用途。
内存开销因此只与"导出期间被改写的不同键数"成正比，与总键数无关。

## 2. 导出顺序无关性

每条记录的字节编码为 `len(key) 8字节大端 + key + len(value) 8字节大端 + value`
（长度恒等，编码无歧义）。清单总校验和取全部记录 CRC32 的异或。
异或满足交换律，故按字典序或逆序导出，记录集合相同 => 总校验和相同。
块切分顺序影响各块 CRC，但每块自描述，重排时按块号寻址即可；
最终一致性判定基于集合校验和而非字节拼接顺序。

## 3. 文件格式

导出产生两个文件：`data.bin` 与 `manifest.json`（清单）。
块格式（小端二进制头 + 载荷 + 尾部 CRC）：
`[index u32][count u32][bodyLen u64][body][crc32 u32 over body]`。
清单含快照版本、块大小、各块 index/偏移/长度/CRC、记录总数与总校验和。

截断分类（verify 自前向后扫描，错误用哨兵，errors.Is 可区分）：
- 数据文件短于清单声明的某块 => 头不足(ErrTruncHeader)/体不足(ErrTruncBody)；
- 体完整但 CRC 不符 => ErrCRCMismatch；
- 清单 JSON 不完整/缺失 => ErrManifestIncomplete。

## 4. 快照过期必须拒绝续传

续传复用的是旧版本水位。若原快照已关闭，其 keep 区已释放，后续读取只能
拿到当前数据——与续传前已导出的部分拼出跨时刻文件。因此清单记录
snapVer 与单调 token；续传时 Store 校验该快照仍存活（token 未释放），
否则返回 ErrSnapshotExpired，已导出的 data 与 manifest 不删除、不覆盖，
交由调用方决定。

## 5. 资源与并发

- 导出逐块写盘，内存驻留 <= 一块（计数器峰值断言）。
- 每个键每轮导出只读一次（读取计数器断言）。
- Store 用单一 RWMutex 保护 map 与快照链；导出持 RLock 逐键读取，
  与并发 Put 互斥的临界区只覆盖一次读拷贝；Close 与导出竞争时，
  导出读到的要么是完整旧视图，要么快照标记关闭后导出返回
  ErrSnapshotExpired，不会写出被当作完整的半截文件（完成以清单
  落盘且 data 长度/CRC 全部校验通过为准）。
