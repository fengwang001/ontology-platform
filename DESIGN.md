# 自定 LZ77 流格式设计与推导

## 记录字节布局

流头固定 `LZ\x01`，其后每条记录 1 标签字节 + 若干 Uvarint（无符号 LEB128，最多 10 字节）。

| 记录 | 标签 | 字段布局 |
|---|---|---|
| 流头 | 字面 | `4C 5A 01` |
| 字面量 | 0 | tag, len, data |
| 回指 | 1 | tag, distance, length |
| 刷新 | 2 | tag（解压器视为输出分界，无数据） |
| 流尾 | 3 | tag, origLen, crc32(IEEE 全文) |

## 1. 压缩结果与 Write 切法无关

编码器内部保留一段「未决」缓冲：任何已发出的记录都不得触及未决缓冲的最后一个字节。
扫描当前可提交前缀（`buf[:n-1]`）时，匹配长度上限就是该前缀长度；匹配未达最小长度 3
则该字节只能作字面量。逐字节提交后，下一个字节从同一逻辑偏移、同一窗口/哈希链状态
继续，而窗口与链只依赖已提交的历史，与这些字节当初由几次 Write 到达无关。因此任意
Write 切法产生逐字节相同的记录流。`Flush` 必须把未决字节全部强制提交（长度上限放开
到缓冲全长）并写刷新标记：否则解压器尚缺这些字节，承诺不成立；强制结束也意味着
Flush 处的匹配不再延伸到之后数据，这正是「分界」语义。

## 2. 重叠回指的复制

`dst := src` 一次性块复制按字节语义读源区：d<l 时读端会读到本轮刚写、尚未完成复制的
位置，结果由运行时/架构决定，不能依赖。正确语义是「输出第 i 字节 = 已输出序列距当前
末尾 d 处」，等价于 `out = append(out, out[len(out)-d])` 的逐字节复制。
本实现直接用逐字节循环：解压侧本就是逐字节记账（计数、校验、窗口、炸弹上限），循环
无额外量级开销；长度先经上限检查，不会因巨大长度先分配。

## 3. 「距离超过已输出」与「距离超过窗口容量」

二者是独立不变量：d>已输出数时历史中根本不存在该字节（流损坏或越界引用）；
d≤已输出但 d>windowSize 时引用了被环形窗口淘汰的字节（编码窗口与解码窗口不一致）。
必须分别报错。正常编码器只在窗口内插入/查询，d 恒 ≤ min(已提交, windowSize)，
故绝不可能产生后者。并行块预置字典：解码是一条连续流，d 永远相对「截至目前实际
输出的总字节」，字典在该块首条记录之前已在输出序列中，语义没有任何改变，只是这些
前缀字节不属于「本块」。

## 4. 并行块压缩的确定性

块 k 的编码器以 `data[max(0,k*B-W):k*B]`（至多上一块原文的末尾窗口容量）预置窗口与
哈希链，压缩 `data[k*B:(k+1)*B]`。其决策只依赖这两段确定的原文，不读其他块的输出、
不共享状态、与 goroutine 完成顺序无关；各块用独立匹配器实例，workers 只改变调度。
每块在边界强制结束匹配（同 Flush 语义），块记录直接顺序拼接：块间不写刷新标记——
刷新标记只承诺「已输出到此处」，连续流始终满足该承诺，标记只会平白改变输出；
解压器也无需知道块边界。最后统一写一条流尾（全文长度 + 全文 CRC32）。
