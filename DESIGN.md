# 设计：可撤销批量重命名与冲突解析器

## 1. 模型与命名合法性
- 命名空间是内存中的名字集合（`map[string]struct{}`），带 `sync.RWMutex`。
- 名字为任意字符串，**空串合法**，路径分隔符（`/`、`\`）按普通字符处理。
- 唯一非法情形：含 NUL 字节或长度 > 63（NUL 不能写入定长日志帧）。

## 2. 冲突判定（全部在任何修改之前完成，四类用哨兵错误 + `errors.Is` 区分）
1. `ErrMissing`：旧名不存在。
2. `ErrNotMoving`：新名已存在，且不是本批任何请求的旧名。
3. `ErrDupTarget`：两个不同旧名指向同一新名，报出两个旧名。
4. `ErrDupSource`：同一旧名出现两次，报出两个新名。
判定顺序：先查 4（同旧名双请求）→ 3（同新名双请求）→ 1（旧名不存在）
→ 2（新名静止占用）。错误携带冲突名字。检测期间不获取写锁，命名空间逐元素不变。

## 3. 执行顺序的推导
把每个请求看成有向边 `旧名 -> 新名`。执行 `x->y` 的前提是 `y` 当前不被占用。
若 `y` 同时是某个尚未执行请求的旧名，则必须等 `y` 先搬走。因此依赖关系为
「`y->*` 必须先于 `x->y`」，即按依赖图的**拓扑序**执行：新名无人占用（出度为 0
的剥离端）的请求最先执行，逐步剥离。`{a→b,b→c}` 先 `b→c` 再 `a→b`。
同层按旧名字典序，保证确定性。入度表 + 剥离总代价为 O(请求数+名字数)。

## 4. 环的通用破法与临时名数量
剥离后剩下的就是环（每个剩余顶点入度=出度=1）。对环
`v0→v1→…→v_{k-1}→v0`（取 `v0` 为字典序最小成员），序列化为：
`v0→T, v_{k-1}→v0, v_{k-2}→v_{k-1}, …, v1→v2, T→v1`。
每步目标名都不存在：`T` 全新；其后每个旧名已在上一步腾空；最后 `T` 被腾空。
**每个环恰好引入 1 个临时名**（只需腾出一个顶点），故临时名总数 = 环数。
多个环互相独立，按各自最小成员字典序排列。
临时名形如 `".rename-tmp-<n>"`（n 为 16 进制计数），生成时校验：不与任何现有名、
任何目标名、已生成临时名冲突，冲突则 n++ 重试；64 次内无法取得则返回
`ErrTempExhausted`（可判定，绝不覆盖）。

## 5. 自环语义
`a→a` 定义为**无操作**：合法、不产生步骤、不参与排序与破环，也不影响其他请求
（它不会把名字“搬走”）。

## 6. 执行、日志与撤销
- 执行全程持有命名空间写锁；命名空间所有变更只经包内方法进行，并发 `ns.Lock()`
  抢锁修改会被阻塞到批处理结束；测试用抢锁 goroutine 验证期间阻塞、结束后成功。
- 每步成功后向临时目录日志追加**定长 132 字节帧**：8 字节头（魔数+版本）+
  `old[63]` + `new[63]` + 4 字节 CRC32(IEEE)。
- 撤销按日志**逆序**执行 `new->old`；日志不存在视为空日志（无操作），
  故重复撤销幂等（第二次无操作）。
- 中途失败（注入第 k 步失败）：已执行帧逆序回滚，命名空间与执行前逐元素相同。

## 7. 日志截断的三类错误（`errors.Is` 区分）
文件按帧解析：前 8 字节不完整 → `ErrHeaderIncomplete`；尾部不足 132 字节
→ `ErrRecordIncomplete`；帧长足够但 CRC 不匹配 → `ErrCRCMismatch`。
截断后只恢复「最大可恢复前缀」（最后一个完整且 CRC 正确的帧之前的全部帧），
其后的步骤无法撤销，由撤销结果报告无法撤销的步数与名字。逐字节遍历全部截断点。

## 8. 复杂度
冲突检测与排序只对集合/表做平均 O(1) 查找，包内计数器记录查找次数，
预算 `4*(请求数+涉及名字数)`。
