# DESIGN

## 1. 流格式

头：`"LZ77"` 4 魔数 + 1 版本字节(=1)。其后 0..n 条记录，最后流尾。

| 记录 | 首字节 | 后续 |
|---|---|---|
| 字面量段 | tag 0 | uvar 长度 n，n 个原文字节 |
| 回指 | tag 1 | uvar 距离 d>=1，uvar 长度 l>=3 |
| 刷新 | tag 2 | 无 |
| 流尾 | tag 3 | uvar 原文总长，uvar FNV-1a64 校验和 |

uvar 为无符号 LEB128，最多 10 字节；第 10 字节只允许最低位为 1，否则为溢出。

## 2. 第三节四处推导

1. 与 Write 切法无关：压缩是「整段决定、块输出」函数。Write 只把字节追加进待决缓冲，
不立即发记录；Flush/Close 才对「从游标到末尾」整体跑确定性贪心匹配。
匹配器内部位置为流绝对位置，与切分无关，故输出字节相同。代价是 Flush 前缓冲可见数据。
Flush 强制把当前匹配在游标处收尾并全部落盘，否则解码端拿不到未决字节；空 Flush 不发任何字节。

2. 重叠回指：d<l 时被复制区间与目标重叠。一次性块复制会读到尚未回填的旧值。
本实现按输出序逐字节 `out[t]=out[t-d]`，t 递增时源位置 t-d 已在本轮写过，天然实现周期展开。
每字节一次环形取址，亚秒级，无需特判；d==1 即 RLE，d==3 展开 `abc` 周期。

3. 距离两错区分：d<=cap 但 d>已输出，引用「从未存在」字节（逻辑损坏）；
d>cap 连存储结构都无法寻址（违背窗口结构）。压缩器只插入真实输入且 d 取自现存槽位，
故正常流永不出后者。并行块预置字典使块首「已输出」含上一块全部字节（解码端确已输出），
含义不变；d 仍受 cap 限制，故块大小可大于 cap。

4. 并行确定性：块 i 的匹配器仅由「块 i 原文 + 块 i-1 原文预置字典」决定，
不读任何块压缩结果、不依赖完成序；worker 私有匹配器，结果按块序号拼接，无共享可变状态，
所以 workers=1/2/4/8 输出逐字节一致。块间不需要刷新标记：均为完整记录边界，回指仅跨一块且不超 cap。

## 3. 复杂度

匹配器为 3 字节哈希 + 每位置单链（`next[pos & mask]`），每位置链探测数 <= 链长上限，
总候选数 <= 上限 × 输入字节数，与窗口规模无关；全同字节每位置恰好探测上限次，每字节考察数恒定。
