# 有预算的图遍历器 — 设计推导

范围：给定访问预算下的确定性广度优先遍历（BFS），预算耗尽可截断、可续传。
不含图存储、索引、查询解析、最短路。状态仅在进程内存。

## 1. 预算计量单位：为什么是「访问的节点数」

预算规定为**新访问（输出）的节点数**。

- 若以「考察的边数」为单位，同一逻辑图、同一起点、同一预算，截断位置会随
  图的物理存储形态漂移：重复边是否被去重、自环是否保留、邻接表是否压缩、
  环上的边在第几轮才被考察，都会改变「预算内看到了多少个新节点」，于是
  不同存储形态下输出序列的长度与最后一个节点都不同，截断点不确定。
- 节点数是图的逻辑性质，与存储无关：预算 N 精确表示「输出 N 个新节点」，
  且可验证 `访问数 == min(预算, 可达节点数)`。

## 2. 截断确定性：固定三处顺序

1. **出边顺序**：每个节点的出边按目标 ID 字典序（去重），不依赖 map 迭代。
2. **同层入队顺序**：BFS 队列先进先出；节点在「首次发现」时入队并标记已访问，
   保证环、自环、重复边都不会重复输出。
3. **消费顺序**：每消耗 1 个预算，先输出当前队首节点，再按序扫描其出边、
   把未访问邻居依次追加到队尾。因此输出序列本身就是确定的 BFS 顺序，
   两次遍历（含同图同预算重复 20 次）逐元素相同。

## 3. 队列驻留上限：游标帧，不整体展开

队列不能把中心节点的一万条出边一次性入队，否则驻留与图最大出度同阶。
每个队列元素是一个**帧** `(node, cursor)`：帧被处理时只扫描到「下一个未访问
邻居」，发现一个就追加一个新帧并把游标前移。对一万叶子星形图、预算 100：
第 2 次消费发现 1 个叶子后帧仍在队首，之后每次消费产生至多 1 个新帧，
故峰值帧数 ≤ 预算 + 常量（实测见 FINDINGS，断言 ≤ 4*预算）。

## 4. 续点必须包含完整 BFS 状态

只记「最后一个访问的节点」无法恢复：同层可能还有已发现未输出的节点，
已访问集合也丢失，续传会走错分支或重复访问。续点因此包含：

- `visited`：已发现节点集合（编码为字典序 ID 列表）；
- `frames`：整个游标帧队列 `(node, cursor)`，含队首未消费完的帧；
- `headEmitted`：队首帧的节点本轮是否已输出；
- `done`：是否已完成（空队列）。

续传 = 用该状态对**同一图**继续执行同样的消费循环，故
「预算 a + 预算 n-a 两段」与「一次预算 n」逐元素等价（对所有 1..n-1 切点）。
预算 0：不消费，返回空序列且续点等于初始续点（幂等）。
已完成续点再续传：返回空序列、续点仍为 done，不报错。

## 5. 续点编码与故障分类

布局（全部小端）：
`magic(4) | version(1) | done(1) | headEmitted(1) | payloadLen(4) |
 payload | sha256(前述全部字节)(32)`。
payload = 各字符串的长度前缀拼接（visited、每帧 node/cursor）。

解码三类错误，用 `errors.Is` 区分：
- `ErrChecksum`：32 字节校验和不符（覆盖 magic/version/len/payload/尾签翻转）；
- `ErrMalformed`：字段不完整（长度前缀越界、游标非法）；
- `ErrUnknownNode`：语法合法但引用了图中不存在的节点
  （含两段之间图被删改：队列中节点消失，必须指名报错，禁止静默跳过）。
校验和先于内容判定，保证逐比特翻转无一被误接受。

## 6. 并发

遍历是纯函数：图只读、状态来自续点、计数器每次调用新建于返回值中，
无包级可变状态。多协程以同一续点并发续传结果逐元素相同；图读取不需加锁。
