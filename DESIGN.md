# 计数草图设计推导

结构为 Count-Min Sketch：d×w 矩阵，行 i 用哈希族第 i 个函数把键映到 [0,w)；
`Add` 对 d 个格子各加 n，`Estimate` 取 d 个格子最小值。

## 1. 能否支持「减计数」Sub

结论：**不能**。`Sub` 会破坏不变量 1（绝不低估），因此不提供该操作。

具体操作序列（取 d=2，两个不同键 x、y 在两行里分别碰撞）：

1. `Add(x,1)`、`Add(x,1)`：x 真实计数=2，x 的两个格子均为 2，`Estimate(x)=2`。
2. `Add(y,1)`，且 y 在第 1 行与 x 碰撞、第 2 行不碰撞：
   第 1 行 x/y 共享格子=3，第 2 行 x 格子=2、y 格子=1。
3. `Sub(y,1)`（把 y 的两个格子各减 1）：第 1 行共享格子 3→2，第 2 行 y 格子 1→0。
4. 此时 x 真实计数仍是 **2**，但 x 的第 2 行格子=2、第 1 行格子=2，看起来正常；
   再 `Add(y,1)` 后 `Sub(y,2)`（撤销超过 y 自己的份额，格子可为负）：
   第 1 行共享格 2+1-2=1，第 2 行 y 格 0+1-2=-1。
5. `Estimate(x)=min(1,2)=1 < 真实值 2`——低估发生，不变量 1 被破坏。

根因：格子是「共享账户」，减法无法只撤回 y 自己的贡献，会连带删掉 x 的计数。
调用方若要「撤销」一个键，应**保留输入日志，用相同参数从头重建草图**；
需要精确去重/删除语义时应改用支持删除的结构（如带符号草图），其保证不同，不在本题范围。

## 2. 保守更新是否保持绝不低估

规则：加 n 前先取 d 个格子最小值 m，只把小于 m+n 的格子提到 m+n。

结论：**保持不变量 1**。归纳证明：更新前若每个格子 ≥ 该格真实归属计数，
设键 x 真实计数由 T 变为 T+n，m=min(更新前 d 格) ≥ T；
更新后每个格子要么不动（原值 ≥ m+n ≥ T+n），要么被提到 m+n ≥ T+n，
故每格仍 ≥ T+n，最小值即估计值仍 ≥ 真实值。

但它**破坏不变量 2（合并同构）**。反例（d=2,w=2，行1 x、y 同格，行2 不同格）：

- A 保守加 x：[[1,0],[1,0]]；B 保守加 y：[[1,0],[0,1]]；
  Merge 得 [[2,0],[1,1]]。
- 单草图按 x 后 y 保守更新：加 x 得 [[1,0],[1,0]]；加 y 时 m=min(1,0)=0，
  提到 1：第 1 行格 1 已 ≥1 不动，第 2 行格 0→1，得 [[1,0],[1,1]]。

两者第 1 行不同（2≠1）。标准 `Add/Merge` 保持线性、可合并；保守更新只作为
独立方法 `AddConservative` 提供，且不承诺可合并。

## 3. (ε,δ) 与 (w,d) 换算

采用 Count-Min 经典界：w=⌈e/ε⌉，d=⌈ln(1/δ)⌉。

来历：任一行中，另一键与 x 落入同一格的概率为 1/w（哈希由参数确定性导出，
随机性指针对任意输入的分析保证）；该行碰撞总载荷的期望 ≤ (N−f_x)/w。
由 Markov，单行使估计偏高超过 εN 的概率 ≤ 1/(εw) ≤ 1/e；d 个独立行同时偏高的
概率 ≤ e^{−d}，取 d=⌈ln(1/δ)⌉ 即 ≤ δ。常数 e≈2.71828 来自 Markov 界的 1/e。

反向换算（调用方直接给 w,d）：ε'=e/w，δ'=e^{−d}，
必须表述为「**误差不超过 ε' 的概率至少为 1−δ'**」。
由于取整放宽（⌈⌉ 只增不减），这是对实际保证的保守描述，不得反过来宣称
比 e/w、e^{−d} 更紧的界。

## 4. HeavyHitters 阈值该比谁

设阈值 φ，候选键真实计数 f。不漏报要求 f>φ ⇒ 必被报告。

采用「`Estimate(x)` 直接与 φ 比较」：因 Estimate≥f（不变量 1），
f>φ 时 Estimate≥f>φ，必然入选，满足不漏报；只会因碰撞多报。

错误选择是「`Estimate−εN` 与 φ 比」（等价于把阈值抬到 φ+εN）：
当 f∈(φ, φ+εN] 时，Estimate 最小可能只有 f（未发生碰撞时），
于是 Estimate−εN<φ，真实超阈值的键被漏掉，错在**漏报侧**。
因此误差界只能用来事后剔除「估计值−εN≤φ」的候选以减少多报；
本实现以零漏报为优先，直接用 Estimate 比较，结果按键排序保证顺序确定。
