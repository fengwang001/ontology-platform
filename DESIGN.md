# CSV 方言解析器设计

## 1 空行与单列空值
选择：**空行跳过**（不产生记录）。尾随换行不产生空记录，则两个紧挨的换行同理：第二条“记录”零字段，零字段不成记录。
单列空值记录若不加引号，写出的字节只有一个换行，回读即被当作空行消失，违反往返，故**必须写成 `""`**。
writer 最小引号规则：`Quoted==true`（单列表空值借此保留）或值含 `,`、`"`、`\r`、`\n` 时加引号；引号内 `"` 双写；`\r\n` 原样。
规范输入：记录以 LF 或 CRLF 结束（末记录可无换行）；空行只作分隔；需要引号处才加引号且引号最小。规范输入满足 `Write(Parse(x))==x`；任意成功表 T 满足 `Parse(Write(T))` 与 T 字段值、引号标记全同。

## 2 par 的切点问题
方案：先做**一次顺序预扫描**——同一个字节状态机只推进状态、不产事件，在每个切点记录入口快照（状态、未发出的半截值、单元格数、记录数、字节基址）。随后 K 个 worker 各以该快照起步，只解析本段（末段含 EOF），并发放大。
拼接按字节序：段尾悬空的半截 cell 与下段首个 cell 直接拼 `Value`、偏移取并集，其余 cell 与记录边界顺序喂入 table。
一致性论证：字节机对任一字节的转移只依赖“当前状态 + 该字节”；预扫描在切点的状态正是单线程跑到该点的状态，所以各 worker 产出的事件流按字节序拼接后与单线程事件流逐事件相同。
错误坐标：快照中的字节位置、记录号、字段号本就是全局值（worker 从切点基址起算并继承计数），无需段内换算；语法错误即终态，拼接遇首错即止，保留此前完整记录。
处理次数：预扫描 ≤n 次，K 段合计 n 次，总计 ≤2n+O(K)，与引号字段长度无关。

## 3 CR 待定状态
未引号侧（F/U，或 A 闭字段后）遇 `\r` 进入 C：字节已计入，但 `\r` 不入值，并记录该记录是否零字段（blank）。
续传：下一字节为 `\n` 则关闭记录（blank 时按空行跳过）；为其他字节则在该 `\r` 的偏移报 `ErrBareCR`。`Close` 时停在 C 同样报 `ErrBareCR`。
引号字段内 `\r` 是内容，直接入值，不进 C。par 切点落在 `\r` 与 `\n` 之间时，快照原样保存 C 与 blank 标志，下段 worker 的判定与流式续传完全同源。

## 4 上限的“立刻”
`fieldMax`：每个**值字节**入值前检查；引号内每个字节计 1（`""` 两个定界引号只产生 1 个值字节，在第二个 `"` 入值前检查），未引号普通字节同理；分隔符、定界引号、`\r` 行尾不计。首超限字节当刻报 `ErrFieldTooLarge`，不缓冲整字段。
`fieldsMax`：每关闭一个 cell（逗号或行尾）即查；`recordsMax`：每关闭一条记录即查。超限即终态，其后 `Feed` 返回同一错误。

## 5 状态转移表
状态：F=字段开始 U=未引号中 Q=引号中 A=引号后刚见引号 C=CR 待定。

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| F | 发未引空 cell→F | →Q | →C(blank 记 ncells) | ncells=0 跳过；否则关记录→F | →U 入值 |
| U | 发 cell→F | ErrQuoteInBare | →C | 关记录→F | 入值 |
| Q | 入值 | →A | 入值 | 入值 | 入值 |
| A | 发引号 cell→F | →Q，值加 `"` | →C | 关记录→F | ErrCharsAfterQuote |
| C | ErrBareCR | ErrBareCR | ErrBareCR | blank 跳过/否则关记录→F | ErrBareCR |

EOF：Q→ErrUnclosedQuote；C→ErrBareCR；F 且 ncells=0→无事件；其余→关闭末记录（F 时末 cell 为空）。
错误位置均为出错字节偏移、当前记录号、字段号（从 1 起，偏移从 0 起）。
