# CSV 方言解析与回写设计

## 推导一：空行与单列空值
空行（两个行终止符相邻，或流首即换行）**跳过**，不产生记录：它不含任何逗号，无法证明是「一条记录」还是排版空行。
因此单列表中唯一字段为空的记录必须写成 `""`：解析器对该 Cell 置 `Quoted=true`。
若不加引号，回写产物是零字节后紧跟换行，正是会被跳过的空行，往返丢数据。多列表中的空字段可裸写（逗号已证明记录存在）。
**规范输入**：行尾一律 `\n`；仅含 `,` `"` `\r` `\n` 的字段加引号，`"` 翻倍；单列空值写 `""`；末尾恰一个 `\n`。

## 推导二：par 切点
切点可落在引号字段、`""`、`\r\n` 中间，worker 无法仅看本段判定引号状态。
故每段（首段除外）分别以「段首在引号外」「段首在引号内」两个初始假设各跑一次词法状态机，各产出带**段内坐标**的事件流（字段、记录结束、错误）。
从左到右：左段终态若处于引号内（含刚见引号/待定 CR 的边界情形），右段取「引号内」假设，否则取「引号外」假设。
段间粘合：右段起始于引号字段内时，其第一个字段是左段开口字段的延续，前缀置为左段已累积内容；右段首字段 Start 改用左段开口字段的 Start。
左段终止于待定 CR、右段首字节为 `\n` 时，左段补一条「记录结束」并让右段吃掉该 `\n`。
拼接后把事件喂给唯一的 table，错误的段内坐标加段起点偏移即全局字节偏移；记录号/字段号由 table 按合并事件序列重新编号，天然与单线程一致。
每段最多算两遍，总处理 ≤ 2N+O(K)，不随长引号字段退化。

## 推导三：CR 待定状态
未引号态见 `\r` 进入 CR 待定：暂不结束字段。下一字节是 `\n` → 记录结束（偏移记在 `\r` 处）；其他字节 → 孤立 CR 错误。
半包续传时该状态随解析器挂起，跨 Feed 与同段处理逐字节结果一致；流在 `\r` 处 Close → 孤立 CR 错误。
par 中左段停在待定 CR 时，由右段首字节裁决（见推导二）。引号态内的 `\r` 是普通内容。

## 推导四：上限的「立刻」
字段字节计数器在每个被追加到字段值的字节到达时递增并立即检查：未引号态每个普通字节计 1；
引号态普通字节计 1；一对 `""` 在「引号后见引号」状态确认为转义时计 1（两个源字节计一个内容字节），一旦超限立即返回错误，不缓冲整记录。
记录字段数在每个逗号分隔出新字段时检查；总记录数在 table 收到记录结束时检查。终态后 Feed 恒返回同一错误。

## 状态转移表（S=字段开始，B=未引号中，Q=引号中，A=引号后见引号，R=CR待定；emitF=发字段，endR=结束记录）
| 状态 | `,` | `"` | `\n` | `\r` | 其他 | EOF |
|---|---|---|---|---|---|---|
| S | emitF→S | →Q | endR→S | →R | →B | 空流，结束 |
| B | emitF→S | err裸引号 | endR→S | →R | 追加→B | endR(无终止符) |
| Q | 追加→Q | →A | 追加→Q | 追加→Q | 追加→Q | err未闭合 |
| A | emitF→S | 追加引号→Q | endR→S | →R* | err闭合后杂字 | endR |
| R | emitF→S | err孤立CR | endR→S（吃\n） | err孤立CR | err孤立CR | err孤立CR |

\* A 态见 `\r`：字段已闭合，按记录尾待定处理（与 R 同裁决）。四类错误：裸引号、闭合后杂字、未闭合、列数不符（table）；孤立 CR 为带位置的可判定错误。
