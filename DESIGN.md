# 多租户配额公平队列调度器 — 设计推导

仅用标准库；状态在进程内存；时钟可注入（测试构造确定性任务流，不依赖墙钟）。

## 1. 为什么推进量是 `c/w` 而不是 `c*w`

租户 i 维护虚拟时间 `vt_i`，执行代价 c 的任务后 `vt_i += c/w_i`，每次选 `vt` 最小的非空租户。

反证（乘权重）：若 `vt += c*w`，权重大的租户 vt 涨得更快、被选得更少，与"权重大份额大"相反。

正解（除权重）：两租户 A(w=1)、B(w=3)、任务等代价 c。交替选取最小 vt：
A 执行后 vt_A=c；B 连选 3 次到 vt_B=3c/3=c，与 A 持平；然后 A 再选一次，B 再选三次。
故执行次数比例 `n_A:n_B = 1:3 = w_A:w_B`，且累计实际代价占比也按此比例收敛。
直观含义：vt 度量"按权重摊薄后该租户消耗的服务量"，每个租户 vt 增速相同即公平。

## 2. 空闲租户重新加入：vt 抬升

系统虚拟时间 `gvt = min(所有非空租户 vt)`（堆顶；堆空时冻结）。
某租户从空变非空（新到达/重新提交）时，先做

    vt_i = max(vt_i, gvt)

再把它加入候选堆。

推导（不抬升会独占多少步）：设等代价 c、A 权重 1、B 权重 3。
若 B 空闲期间 A 连续执行 m=1000 个任务，则 gvt = vt_A = 1000c，而 B 的 vt 停在旧值 0。
不抬升：B 一回来 vt_B=0 远小于 vt_A，B 每步涨 c/3，须连续执行
`1000c/(c/3)=3000` 步才追平 A —— 即回归租户独占约 `m*w_B/w_A`（本例 3000）步，
A 在此期间被饿死。
抬升：vt_B=max(0,1000c)=1000c 入堆即与 A 持平，按 ID 并列规则立刻可被选，之后正常按 1:3 交替，
不独占，A 也不饿死。

## 3. 并列打破

vt 相等（浮点精确相等）时按租户 ID 字典序选，保证可复现；同租户队列内 FIFO（提交序号）。

## 4. 代价为 0 与权重边界

代价采用 float64：float64 表示范围约 ±1.8e308，权重 1e-300 与 1e300 并存时
`c/w` 仍在有限范围；精确整数公平用等代价任务即可，不引入有理数复杂度。
零代价合法，但 `c=0` 会使 vt 不推进，同租户可无限被选中。处理：有效推进代价
`e = max(c, eps)`，eps 为可配置最小代价（默认 1e-9）。统计仍记录真实代价 c，
仅虚拟时间按 e 推进，保证零代价租户每步也前进、不独占。
权重必须 >0，否则返回错误（见第 6 节）。

## 5. 复杂度

非空租户放最小堆，按 (vt, id) 排序。单次选择：堆顶取值 O(1) + 堆维护 O(log n)；
比较计数器记录上浮/下沉及 Pop 的父子比较次数。上界
`4*ceil(log2 n)`（n=1000 时为 40）。空闲租户不在堆中，堆大小 = 非空租户数。

## 6. 背压与租户移除（四类可 errors.Is 区分的错误）

- `ErrQueueFull`：每租户队列有上限，超限拒绝该入队且不影响其他租户。
- `ErrInvalidWeight`：权重 <=0。
- `ErrInvalidCost`：代价 <0。
- `ErrTenantGone`：移除租户仅允许在其队列排空后进行；有排队任务时移除即拒绝，
  任务留在原队列、不静默丢弃，语义明确。

## 7. 并发与确定性

sched 内一把互斥锁保护租户主表与堆，条件变量在无任务可执行时阻塞消费者。
入队、出队均串行化：并发提交时"提交顺序"以拿到入队锁的串行化顺序为准（序号在锁内分配）。
每个任务只被一个消费者 Pop 走（出堆即从队列摘除），不丢不重；-race 干净。
固定串行化任务流（相同提交顺序与代价）→ 相同 (vt,id) 排序 → 调度序列逐元素确定。

## 8. 份额度量

统计每租户被执行任务的真实代价之和，份额 = 该和 / 总代价；
与权重占比 w_i/Σw 比较，相对偏差 |实际-理论|/理论 < 5%。
