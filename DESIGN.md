# DESIGN

包依赖单向：lines → edit → hunk → udiff → patch；udiff、patch 另依赖 lines。

## 1. 零计数 hunk 头的行号规则（语义 4）

统一定义：hunk 头里的 a 是「该 hunk 第一行在旧文件中的 1 基行号」；hunk 没有旧行（b=0）
时，a = 紧邻其上的旧行行号（插入点在第 a 行之后）；文件最顶端插入时上面没有行，a=0。
新侧 c 同理（b↔d、旧↔新）。
- 样例 `x`→`y x`（C=0）：旧侧空 hunk 位于文件顶端，a=0，故 `-0,0`；新侧第一行是 y，c=1，
  且 d=1 省略计数，故 `+1`。
- 样例 `a b`→`a b X c`（C=0）：插入点在旧第 2 行之后，a=2 故 `-2,0`；新侧 X 是第 3 行，
  故 `+3`。
- 样例 `a`→空（C=3）：旧侧 hunk 第一行是第 1 行，b=1 省略为 `-1`；新侧空且位于文件顶端，
  c=0，故 `+0,0`。
说明：a 不是「将要插入的位置」（位置语义在 0 基/1 基间有歧义），而是「上一行的行号」，
这样 0 唯一表示文件顶端。Apply 解析 b=0 时把插入点放在旧第 a 行与第 a+1 行之间；实现上
每个 hunk 的前缀/后缀相等行是必须匹配的锚点，b=0 时锚点全部是插入点前后的相等行。

| 旧计数 | 书写 | Apply 对起点的解释 |
|---|---|---|
| b=0 | `-a,0`，a=上一行行号（顶端为0） | 在旧第 a 行之后插入，不改任何旧行 |
| b=1 | `-a`（省略 `,1`） | 替换/删除从旧第 a 行开始的 1 行 |
| b≥2 | `-a,b` | 替换/删除从旧第 a 行开始的 b 行 |

## 2. hunk 合并阈值（语义 5）

两段改动间隔 g 行未改动行。GNU `diff -U C` 给每段改动各取 C 行上下文：两段上下文
共 2C 行；当 g ≤ 2C 时上下文重叠或相邻（g=2C 时恰好首尾相接，无间隙），GNU 输出一个
hunk；g ≥ 2C+1 时中间至少剩 1 行不出现在任何 hunk，输出两个 hunk。故阈值为 **2C**：
g≤2C 合并，g≥2C+1 分开。若取阈值 C，则 g=C+1 时 GNU 仍合并（2C 行上下文仍覆盖），错误
拆分；若取 2C+1，则 g=2C+1 时 GNU 已分开（如 C=1 两改动隔 3 行，`diff -U1` 出两个
hunk），错误合并。测试覆盖 g=2C 与 g=2C+1 两侧。

## 3. 并列最短脚本的选择（语义 3）

Myers 在 V 数组上迭代：每个 d 沿 k 从大到小计算。取 k 有两个来源——k+1（向下走=删除）
与 k-1（向右走=插入）。**并列时选删除**（实现：k==-d 或（非 k==d 且 V[k+1]≥V[k-1]）
时取删除）。因此 `a b`→`b a` 的脚本固定为 `-a`、` b`、`+a`（先删后插），同 D 的另一
脚本 ` b`、`-a`、`+a` 被排除。hunk 内同一位置的 `-` 行整体排在对应 `+` 行之前。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

前一 hunk 以偏移 s（实际匹配起点减记录起点，行单位）应用成功后，后一 hunk 的记录起点
整体平移 s：搜索中心 = 记录起点 + s，再在 ±F 行内找候选。原因：补丁头行号基于旧文件，
一处净行数变化（新增行数减删除行数）会使其后所有位置整体移位；以「已应用 hunk 的累计
净行数变化」为平移量，正是补丁在当前文本中应在的位置。候选间取离中心最近、等距靠前。

## 5. 复杂度与计数（语义 2、第四节）

edit 维护计数器 steps：每做一次对角线蛇形逐行比较加 1（含比较失败的那一次）。
Myers 外循环 d 至多 D+1 轮、每轮至多 2d+1 个 k、每个 k 的蛇形至多 N+M 次比较，
朴素上界 (D+1)²(N+M)；实现中 k 只在 [-M,N] 有效且各蛇形从前沿位置出发，整体满足
4(N+M)(D+1) 的断言界，D=3 的改动实测近似线性。距离上限 MaxD 可配，超过返回
ErrTooDifferent。
