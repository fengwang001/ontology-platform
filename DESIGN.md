# DESIGN

## 1. 零计数 hunk 头的行号（语义 4）

统一规则：`-a,b` 的 a 表示"该 hunk 旧侧**第一条上下文行**的 1 基行号；hunk 完全由插入构成（b=0）时没有上下文行，a 退化为插入点**前一行**的行号（文件开头插入为 0）"。这与 `+c,d`（c 为新侧第一条上下文行行号；纯删除 d=0 时 c 为删除点前一行行号）完全对称。
- 样例1 旧`x\n`→新`y\nx\n` C=0：旧侧纯插入，插入点在文件开头、其前一行不存在，故 `-0,0`；新侧插入了第 1 行，单条故省略 `,1`：`+1`。
- 样例2 旧`a\nb\nc\n`→`a\nb\nX\nc\n` C=0：在旧第 2 行之后插入 X，插入点前一行是旧第 2 行，故 `-2,0 +3`。
- 样例3 旧`a\n`→空 C=3：旧侧删掉第 1 行，故 `-1`（b=1 省略 `,1`）；新侧为空、删除点前一行在新文件中不存在，故 `+0,0`。
- Apply 解释：b=0 时把插入点放在旧文件第 a 行**之后**（a=0 即文件开头）；d=0 时把删除后位置理解为新文件第 c 行之后，反向应用同理。

| 旧侧 | 书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`（0 不省略） | 纯插入；插入点在旧第 a 行之后，a=0 为文件开头 |
| b=1 | `-a`（省略 `,1`） | hunk 旧侧只占第 a 行（1 条上下文或删除行） |
| b≥2 | `-a,b` | 旧侧从第 a 行起共 b 行（含上下文/删除） |

## 2. hunk 合并阈值（语义 5）

结论：**g ≤ 2C 时合并，g > 2C 时分隔**（g 为两段改动之间未改动行数）。
GNU diff 每个 hunk 左右各带至多 C 行上下文；g≤C 时两侧上下文重叠，必然合并；C<g≤2C 时两段上下文虽不重叠但**直接相接**，也会被 GNU diff 并入同一 hunk；g=2C+1 时中间至少有 1 行无法纳入任何一侧的上下文，必须分开。
反例（C=3）：若阈值取 C，则 g=4（=C+1≤2C）本应像 GNU diff 一样同 hunk 却被错误分开；若取 2C+1，则 g=7 时 GNU diff 会保留 1 行公共行并输出两个 hunk，该阈值却强行合并，多出 2C+1−g=0 行上下文边界仍不对，而 g=2C+2 时又错误合并 2 行。故 C 与 2C+1 都与 `diff -U C` 不一致，只有 2C 正确。

## 3. 并列最短脚本的选择（语义 3）

Myers 沿对角线 k 的更新 `V[k] = max(V[k-1]+1, V[k-1]-1)`：取**第一项**（向下，即先删除）打破并列。即距离相同的路径中优先删除、后插入；`a b → b a` 的脚本固定渲染为删 `a`、删 `b`、插 `b`、插 `a`（两个 `-` 行在前、两个 `+` 行在后），而不是交错形式。每个 (d,k) 的 V 快照完整保存，回溯后整体反转，保证逐字节确定。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

前一 hunk 在偏移 +k（实际位置减记录位置）应用后，后续 hunk 的记录位置**平移 +k**：因为前一 hunk 若含 net 行数变化，其后所有内容在目标文件中的物理位置已整体移动，平移量恰是"实际旧侧起点 − 记录旧侧起点"，而不是插入净增量（fuzz 命中可能两者不同）。搜索窗口 `[center−F, center+F]` 随之平移；候选仍按距新中心最近、并列靠前者选取。

## 5. 算法与结构

`lines`：Line{Content 不含行尾, NL 为原始行尾字节（"\n"/"\r\n"/""）}；Join 原样拼回，且 Split(Join(x))==x 对空文件成立（0 行）。
`edit`：Myers O(ND)，V 全快照回溯；ops 为 Eql/Del/Ins 行序列；非导出 `steps` 统计对角线步与 snake 逐行比较的总次数；距离上限超限返回 ErrTooDifferent。
仅末尾换行变化（无行差异）时，在 `hunk` 层合成"末行 Del+Ins（内容相同、NL 不同）"，保证补丁非空且往返。
无换行标记只允许出现在整个文件的最后一行之后（旧/新侧各自），严格解析。
