# 多租户配额公平队列调度器 — 设计推导

## 1. 虚拟时间为什么用 c/权重 而不是 c*权重

目标：长期看租户 i 分到的执行代价份额正比于其权重 w_i。
定义虚拟时间 vt_i = 该租户已被服务的代价 / w_i，即 vt_i = served_i / w_i。
调度不变式：每次选择 vt 最小的租户服务，使各活跃租户的 vt 趋于相等。
若所有活跃租户 vt 相等，则 served_i / w_i = 常数，即 served_i 正比于 w_i，正是目标。
因此每服务一个代价为 c 的任务，应令 vt += c / w：权重越大，同样实际代价换来的
vt 增量越小，被再次选中的优先级越高，从而拿到更大份额。
反证 c*w：若 vt += c*w，则权重大的租户 vt 涨得更快、更靠后被选，份额与权重成反比，
与目标恰好相反。直观记忆：权重是"速率"，vt 是"已消费的配额时间"。

## 2. 空闲租户重新加入时的 vt 抬升

系统虚拟时间 sysVT 定义为最近一次被 Pop 的租户在服务前的 vt。由于堆顶恒为最小 vt、
且新加入者被抬到不低于 sysVT，Pop 出的 vt 单调不减，故 sysVT 单调不减。

不抬升会怎样：租户 X 的 vt 停在旧值 v0，期间 Y（权重 w_y、任务代价 c）执行了 k 个任务，
系统 vt 推进到 V ≈ v0 + k*c/w_y。X 重新入队后 vt 仍为 v0，恒为堆最小者；
X 每执行一个任务 vt 前进 c/w_x，需 (V-v0)*w_x/c = k*w_x/w_y 步才能追上 V。
等权重时即 k 步——Y 跑 1000 步，X 就独占约 1000 步，Y 被饿死。这就是经典 bug。

正确做法：租户队列从空转为非空时（admit.Submit 内）执行
vt = max(vt, sysVT)。抬升后 X 至多比当前堆最小值领先一个服务间隔，
至多连续被服务 2 次（抬升值可能略低于堆顶、并列时触发 ID 次序）即回到公平轮换，
X 的任务在 1~2 步内被执行，Y 不会饿死。

## 3. 代价为 0 的任务

c=0 时 vt 不前进，该租户 vt 永远最小，将无限独占调度器。
处理：有效代价 eff(c) = max(c, MinCost)，MinCost = 1（task.Effective）。
零代价任务合法入队、可被调度，但按最小代价推进 vt，份额仍受权重约束。

## 4. 同 vt 并列打破

堆比较器按 (vt, 租户ID) 字典序：vt 相等时租户 ID 字典序小者优先。
比较规则是纯函数，同一确定性任务流必产生逐元素相同的调度序列。

## 5. 数值表示：float64

vt 用 float64。权重可跨 1e-9 ~ 1e9 量级，float64 上限约 1.8e308，不会溢出；
1e5 次调度的累加相对误差约 1e-13，远低于 5% 的公平性容差。
未选用有理数：标准库 math/big 代价高且无必要。

## 6. 复杂度

选择用二叉堆，单次 Pop 的比较次数 <= 2*ceil(log2 n)（每层比较左右孩子各一次），
测试上界取 4*ceil(log2 n)。堆中只放队列非空的租户，空闲租户不占用选择代价。

## 7. 背压、移除与错误

- 每租户队列上限 cap：超限 Submit 返回 ErrQueueFull，只影响该租户。
- 权重 <= 0：tenant.New 返回 ErrInvalidWeight。
- 有排队任务时 Remove：拒绝并返回 ErrTenantBusy（任务保留在队列中，去向不变）。
- 未注册租户：ErrUnknownTenant。
四类错误均为独立哨兵错误，可用 errors.Is 区分。

## 8. 并发与确定性

admit.Limiter 用一把互斥锁串行化 Submit/Next/Remove。
并发提交时，"提交顺序"定义为获取入队锁的串行化顺序；
同一确定性任务流（固定提交顺序与代价）必产生同一调度序列。
并发下每个任务恰好被取出一次：出队与堆操作在同一临界区内完成。
