# properties 解析/回写设计要点

## 第 3 条：只含空白（或空）的续行

依据 `java.util.Properties$LineReader.readLine` 的状态机逐字节模拟：

1. 行尾反斜杠仅把"待续行"置位；遇到 `\n`/`\r`/EOF 时才决定逻辑行是否结束。
2. 下一物理行开头先剥空白（`inWhiteSpace` 状态），随后读到行尾符。
3. 此刻"待续行"标志来自的是**当前这行**的反斜杠；空白行/空行本身没有反斜杠，
   读到行尾符时续行标志为假，逻辑行立即终止，且空白行未贡献任何字符。

结论：`a\` + 换行 + 空行（或纯空白行）得到 `a`，而非无限等待。续行**只延续一行**；
"剥前导空白"与"判断是否继续"用的是同一遍扫描——空白被丢弃后若紧接行尾符，
该符就是逻辑行的结束符，而不是再等下一行。仅当空白行自己也以反斜杠结尾时才会继续。

## 第 6 条：Store 转义规则推导

从 `Load(Store(m)) == m` 的必要性反推，按读取阶段逐条分析：

- 行级识别：逻辑行只在**行首**判定 `#`/`!` 注释与前导空白。故键的行首字符若是
  `#`/`!` 或空白必须转义；值永远位于分隔符之后，行首的 `#`/`!`/空白对值无意义，
  因此值开头的 `#`/`!` 不转义。
- 值开头的空白必须转义：读取时键与分隔符之后的**前导空白被整体吃掉**，不转义会丢失；
  值中间/末尾空白不会被吃（样例 `key = value  ` 尾空格保留），故不转义。
- 键中间的 `=`/`:`/空白是分隔候选，必须转义；值里的 `=`/`:` 只是普通字符，不转义。
- 反斜杠一律转义成 `\\`；`\n \r \t \f` 转成控制字符转义，否则会变成行尾/空白被改写。
- 非 ASCII 原样 UTF-8 输出（对应 `store(Writer)`，不做 native2ascii）。
- 分隔符统一用 `=`；空键输出 `=v`，空值输出 `k=`，均可原样读回。

## 语义 → 测试对照表

<!-- 最后补齐 -->
