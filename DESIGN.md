# DESIGN

## 1. Write 切法无关
压缩器维护一个未决窗口：每个输入偏移在其位置调用哈希链，只有当它后面还存在
`maxMatch` 个可用字节（或 Flush/Close 已知输入结束）时，匹配是否可继续延长才被
完全确定。因此新 `Write` 的字节先缓存，已确定的尾部以前字节才提交为 token；提交
只依赖累计字节序列和固定偏移，不依赖它们来自几次 `Write`。Flush/Close 强制声明
“后面暂时/永远没有字节”，此刻把未决字节按“匹配最多延长到当前末尾”全部提交，并
输出刷新标记；所以 Flush 会结束跨边界匹配，但位置只由累计偏移决定。

## 2. 重叠回指复制
设距离 d、长度 l。块复制语义只取一次源区间，d<l 时读到的是旧字节而不是刚复制的
新字节。正确语义是输出序列 `out[n]=out[n-d]` 依赖已生成的输出。实现先做
`copy` 不重叠部分（最多 d 字节），再循环复制后续 d 字节块；每个目标块的源区间
在复制前已经生成。Go 的 `copy` 对重叠切片按 memmove 处理，所以每轮只复制不超过
d 字节即可。

## 3. 两类越界距离
“超过已输出”说明回指指向流开始之前；即使窗口足够大也没有该历史。“超过窗口容量”
说明历史存在但编码器约定已经遗忘。先判 `dist>windowCap`（容量为 0 的非法配置也在
构造时拒绝），再判 `dist>produced`，错误可区分。压缩器只从环形缓冲现存位置取候选，
故绝不产生超过容量的距离。并行块以“上一块末尾 windowCap 字节”预置窗口：这些字节
尚未作为本块输出，但已经是本块解压器可见的字典，因此判定距离时基数是
“预置字典 + 本块已输出”；流格式不需字典标记，因为块边界对解压器不可见，预置只改
变匹配选择，复制语义不变。

## 4. 并行确定性
第 k 块的候选只来自本块字节与上一块末尾 windowCap 字节；哈希链初始状态也仅由该
预置字典确定，不读取其他块的 token、输出或 goroutine 完成顺序。块大小、偏移和
贪心规则固定，因此每块 token 序列确定。worker 只负责计算，主 goroutine 按块号收
集；块之间直接连接 token，不插入刷新标记（块边界不是 Flush 语义），最后统一写头、
全部 token、刷新记录和带总长/CRC 的尾。workers=1/2/4/8 只改变调度。

## 5. 记录布局
所有整数为无符号 LEB128（最多 10 字节，第 10 字节只能为 1）。

| 记录 | 布局 |
| --- | --- |
| 流头 | magic=`LZ77`，version=uvarint(1) |
| 字面量 | tag=0，len=uvarint，随后 len 个原文字节 |
| 回指 | tag=1，distance=uvarint，length=uvarint（均 >=1） |
| 刷新 | tag=2 |
| 流尾 | tag=3，originalLength=uvarint，crc32IEEE=uvarint |

字面量与回指交错；tag 为记录首字节，不属于后续整数。
