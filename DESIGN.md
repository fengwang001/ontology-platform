# 设计推导

## 第 3 条：续行与“空白续行”

`Properties.load` 判定续行只看物理行行尾：自最后一个非空白字符向前数连续反斜杠，奇数个则反斜杠与行尾空白一并丢弃，下一物理行拼入同一条逻辑行。续行判定只依赖上一次扫描时已计的“尾部连续反斜杠数”，无需回扫，因此每个字节检查次数有常数上界。

拼入时，下一物理行开头的空白（空格、制表、换页等）被跳过。若该续行行只含空白或为空，则“跳过首部空白”会吃掉整行：它不贡献任何字符，也不终止逻辑行，解析器继续读再下一物理行（该非空行同样跳过首部空白）。依据是 JDK 中“续行后跳过 leading whitespace”的循环作用在整条物理行上，空行只是把该循环走到行尾。关系：空白续行并非特殊规则，而是“首部空白剥离”作用于整行空白时的自然结果；它不结束逻辑行，只有遇到非空续行或非续行行才结束。

文件结尾单个反斜杠：EOF 充当终止，反斜杠被丢弃（无后续字符可转义/拼接）。

## 第 6 条：Store 的最小转义

Load 的分词规则决定回写时必须转义什么。输出形如 `key=value`，每条记录一行：

- 键起始：行首若为 `#`/`!` 会被当作注释，故以这两个字符开头的键必须转义；值在分隔符之后，行首规则永不适用，所以值中的 `#`/`!` 一律不转义。
- 键中的 `=`/`:`/空格：未转义的这三个字符会终止键（分隔符），故键中任意位置都要转义。值位于第一个分隔符之后，`=`/`:`/空格不再有分隔含义，故值中的这些字符（含中间空格）原样输出。
- 值起始的空格（含制表等空白）：Load 会吃掉分隔符之后的值首部空白，故值开头空白必须转义为 `\ ` / `\t`；中间与尾部空白保留原样，不必转义。
- 反斜杠一律写成 `\\`；换行、回车、制表、换页写成 `\n \r \t \f`，其他不可打印控制字符写成 `\uXXXX`。
- 非 ASCII 字符按 UTF-8 原样输出（Load 以 Reader/UTF-8 读取），不写 `\uXXXX`。
- 空键：行直接以 `=` 开头，Load 即得到空键；空值即行尾为空。

转义 `#`/`!`/空格用“前加反斜杠”（Load 对未知转义丢弃反斜杠），保证只转义必要字符且可逆。
