# Ontology 属性级访问控制设计

模块目标：对 Ontology 对象属性做**按主体（用户/组）的读/写分离授权**，
读时投影（不可见即移除）、写时强制（不可写即拒绝），默认拒绝，
谓词引用不可见列时整条查询拒绝，杜绝 `NOT` / `IS NULL` 探针泄露。

包结构：`acl`（主体与授权）、`access`（权限求值）、`project`（读时投影）、
`api`（写时强制与组装，唯一入口），`cmd/demo` 打印语义判定。

## 推导点 1：默认拒绝 vs 默认允许

问题：属性对某主体没有任何显式授权时，默认可见/可写，还是不可见/不可写？

- 口径 A：默认允许。后果：任何新建属性、任何漏配授权都立即对全体主体
  泄露，安全上不可接受；且「撤销授权」无法真正收回——只要忘了删某条
  grant，默认允许就继续放行，权限收回依赖运维零失误，违反 fail-closed。
- 口径 B：默认拒绝。后果：安全，但引入一个新问题——读投影会把「schema
  必填属性」也移除，投影后的对象不再满足 schema。必须处置：投影产物被
  定义为**读取视图（View）**而非存储对象，允许缺必填列，标记为「部分可
  见」（`Partial=true`）；它只影响本次读取的呈现，不回写、不影响存储中
  对象的完整性校验。存储层完整性仍按完整 schema 校验，与读取视图正交。

**结论：默认拒绝。** 无显式授权即不可读、不可写。投影后的对象允许不满足
schema（读取视图允许缺必填列），以 `Partial` 标记显式表达「这是降级视图」，
而非把残缺对象冒充合法对象。

## 推导点 2：无权限字段写入——拒绝 vs 忽略

问题：调用者对属性 b 无写权限，却提交了含 b 的写入，如何处理？

- 口径 A：静默忽略 b、只写有权限的字段。后果：调用者收到成功响应，以为
  b 已写入，实际被丢弃——静默数据丢失；下一次读回发现 b 未变，行为无法
  解释。这是**静默降级**，写入路径上最危险的失败形态。
- 口径 B：整条拒绝。后果：fail-closed，调用者明确知道失败。需进一步回答
  「更新已有对象时部分字段无权限是否也整条拒绝」——是。更新是部分字段
  写入，若只写有权限字段，同样构成静默丢弃；调用者必须永远能区分「全部
  写入成功」与「哪些字段没写进去」。

**结论：创建与更新一律整条拒绝。** 创建（整对象）含无权限字段 → 拒绝；
更新（部分字段）含无权限字段 → 同样拒绝，且错误中**列出全部无权限字段名**
（哨兵错误 `ErrWriteDenied`，可 `errors.Is` 判定）。校验先于任何物化：
任一次失败的写入，物化计数器为 0，对象无任何属性被部分写入（原子性）。

## 推导点 3：谓词引用不可见列——拒绝 vs 静默代换

问题：查询谓词引用调用者无读权限的属性（如 `WHERE secret > 10`），如何
求值？

- 口径 A：把 secret 当 NULL 继续求值。后果：`NOT(secret > 10)` 中，
  `secret > 10` 对不可见行求值为 UNKNOWN，`NOT UNKNOWN` 在投影语义下
  等价于「通过」，攻击者构造正反两个谓词对比结果集大小，即可推断
  「secret 是否存在、是否大于 10」——存在性探针泄露。
- 口径 B：把 secret 当假值继续求值。后果：`secret IS NULL` 探针对所有
  不可见列恒真，攻击者直接确认「该对象存在 secret 列且对我不可见」，
  同样泄露列存在性与取值分布。

两种代换都泄露，区别只在泄露通道。任何「替调用者猜一个值」的语义都不可
接受。

**结论：谓词一旦引用不可见列，整条查询拒绝**，哨兵错误
`ErrPredicateDenied` 并指出列名；绝不做 NULL 代换、假值代换或任何静默
降级。查询前先收集谓词引用的全部列，逐列校验读权限，全部通过才执行。

## 跨模块不变量

1. **读投影确定性**：投影按属性名排序遍历，同一主体、同一对象、任意输入
   遍历顺序，输出逐字节相同；不可见属性被**移除**（键不存在），而非置零
   值——`_, ok := view["k"]` 中 `ok=false` 与 `view["k"]==nil` 可区分。
2. **授权并集可判定**：有效授权 = 主体直接授权 ∪ 传递闭包内所有祖先组的
   授权；直接授权优先判定（正授权体系下与并集结论一致，且短路求值）。组
   展开按主体记忆化，每个组在一次求值周期内至多访问一次，重复求值的组访
   问计数为 0（100 / 10000 组两档验证，访问数不随求值次数线性增长）。
3. **写时零静默丢弃**：写入前先对全部字段做写权限校验，任一字段无权限
   即返回含字段名的 `ErrWriteDenied`，本次调用物化计数器增量为 0；成功
   时增量等于提交字段数（全量物化）。
