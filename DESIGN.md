# 组提交与顺序保证器 — 设计

## 1. 序号分配时机
- 到达即分配：落盘失败会留下永久序号空洞。
- 成功后分配：调用方在等待期间无法知道自己的相对位置，且 leader 唤醒后需二次发布。
- 本设计规则：**批次组装完成、调用 `Write+Sync` 之前**由 leader 一次性给整批分配连续序号
  `[base, base+n)`；落盘或同步返回错误时整批序号作废，计数器回滚到 `base`，整批调用方收错。
  成功后序号才对外可见，因此已提交序号空间永远无空洞。

## 2. 批次原子性（批级 CRC32）
日志布局：
- 文件头 8 字节：`GRPWAL01`。
- 每批一帧：`magic(4)=GRPB | baseSeq uint64 | count uint32 | dataLen uint32 | crc32 IEEE(帧其余全部字节) | data`；
  data 内每条目为 `len uint32 + payload`。
CRC 覆盖整批（头字段 + 全部条目），是批级而非条级：回放时一帧 CRC 不符 → **整批全丢**，
坏批之后的完好批次仍正常读出（自描述长度前缀可跳到下一帧 magic）。
帧头/长度不完整（崩溃写了一半）→ 直接终止回放，停在最后一个完整批次。

## 3. leader 选择与唤醒不串台
唯一 leader goroutine 从通道取请求攒批；其余协程把自带 `chan result` 的请求投入通道后阻塞。
结果按批内位置逐一写回**请求自带的通道**，唤醒的永远是调用方本人，物理上不可能串台。

## 4. 攒批三触发（batcher，注入时钟）
条数达 `MaxCount`、未同步字节达 `MaxBytes`、距首条等待达 `MaxWait`（用注入的 timeSource
判定，测试可零等待推进），三者先到先触发。**单条超过 MaxBytes 不拒绝**：立即单独成批，
故单批字节可能等于该条长度（唯一允许超 MaxBytes 的情形）。

## 5. 资源复杂度
每批恰好一次 `Write` + 一次 `Sync`；10000 请求 / 批 100 ⇒ 同步 ≤ 200（含超时尾批余量）。
leader 串行写，文件偏移单调递增 ⇒ 序号区间连续不重叠，批内条目顺序与序号一致。

## 6. 故障与四类错误
`ErrIncompleteHeader`（文件头不足）、`ErrIncompleteBatch`（批头不足）、
`ErrIncompleteEntry`（某条目长度/载荷不足）、`ErrCRC`（CRC 不符），均可用 `errors.Is` 区分。
同步/写失败：SyncFailer/截断写注入，整批收错、序号回滚。
半截帧（崩溃）视为 `ErrIncomplete*`，该批不可见，重启后 StartSeq 从最后完好批次继续。

## 7. 边界语义
- 零请求 Close：文件仅含 8 字节文件头，无批帧、无额外同步。
- 批上限 1：退化为逐条提交；等待时长 0：首条立即成批。
- 空载荷 `[]byte{}` 合法（len=0）；超大单条单独成批。
- Close 竞争：leader drain 通道期间到达的请求要么进入最后一批落盘成功，要么收
  `ErrClosed`；不存在“成功但未落盘”。
