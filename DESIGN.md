# 设计：三值逻辑谓词的常量折叠与谓词下推

值域：每列为小整数或 NULL；比较产生三值 T/F/U（未知）。逻辑真值表：
NOT T=F, NOT F=T, NOT U=U；AND 取最小值、OR 取最大值，序 F<U<T。
WHERE 的过滤条件是「求值为 T 才保留」，故 U 与 F 过滤效果相同。

## 1. 三条在三值逻辑下不成立的重写（推导）

1. `x = x` → 真：x 非 NULL 时确为 T；但 x=NULL 时 `NULL=NULL` 为 U，不是 T。
   故只能折叠为「布尔存在性」断言：T 当且仅当 x 非 NULL，即 `x IS NOT NULL`。
2. `A OR NOT A` → 真：A=T 或 F 时为 T；A=U 时 `U OR NOT U = U OR U = U`。
3. 德摩根 `NOT(A AND B) = NOT A OR NOT B`：对 a,b∈{F,U,T} 枚举 9 组均成立
   （两边都等于 max(inv(a),inv(b))），NOT 对 min/max 自对偶。但
   `A AND NOT A` → 假不成立：A=U 时 `U AND NOT U = U AND U = U ≠ F`；
   它等价于 `A IS FALSE`（A 为 F 时 T，否则 F）。

## 2. 何时可按二值优化（位置极性推导）

WHERE 只保留 T，于是在「顶层」把 U 与 F 视为同一过滤结果是安全的。
形式化：记表达式 e 的三值结果为 v；谓词被保留当且仅当 v=T。
- 若 e 处于**正极性且是顶层合取项**（从根到 e 路径上 NOT 层数为偶数、
  且不含 OR 分支：根为 e，或只穿过 AND），则过滤判定等价于二值判定，
  可应用 `x=x→true`、`A OR NOT A→true`、`A AND NOT A→false`：
  这些规则唯一改变的赋值正是 v=U 的赋值，而这些行在顶层本就被过滤。
- e 在 NOT 之下（极性反转）：外层 `NOT e=T` 要求 e=F，此时 e=U 与 e=F
  行为不同（NOT U=U 被过滤，NOT F=T 保留），禁止折叠。
- e 在 OR 分支下：OR 取 max，该分支的 U/F 会影响上层结果（再被 NOT
  包裹时差异显现），统一保守禁止。
实现：折叠携带 ctx（negDepth、underOr、isTopConjunct）；二值化规则仅在
`negDepth 偶 && !underOr && isTopConjunct` 时触发；NOT 内部全部保持原样。

## 3. 可做的安全重写

- 常量三值表达式按真值表直接折叠（比较/AND/OR/NOT）。
- 恒等吸收（三值下枚举成立）：`X AND T→X`、`X OR F→X`；
  湮灭：`X AND F→F`、`X OR T→T`（不依赖列值，恒成立）。
- 合取/析取展平、单子退化（AND{x}→x、OR{x}→x）。
- 德摩根：`NOT(AND xs)→OR(NOT xs)`、`NOT(OR xs)→AND(NOT xs)`、`NOT NOT x→x`。
以上每条均由 equiv 在 3^4=81 组赋值上全量验证。

## 4. 不动点终止性

每条规则严格减小一个有界度量 M（子项序上的三元组）：
M = (节点数, NOT 深度和, 非常量节点数)，按字典序。常量折叠与单子收缩减小
节点数；德摩根 + 双 NOT 消除使每个 NOT 逐层下移并与叶子处的 NOT 成对消除，
NOT 深度和单调下降；规则集**不含互逆规则对**（无规则把 OR 改回
`NOT AND`，无规则重新包裹 NOT）。故每轮至少一个节点的 M 下降，
迭代轮数 ≤ 4·节点数（保守常数，测试中强断言）。
震荡检测：每轮记录规范化打印；同一字符串在「非连续」两轮重复即存在环
（单调系统不应回退），返回可判定错误 ErrOscillation。
规则顺序固定：自底向上，先常量、再恒等/单子、再位置敏感二值化、最后
德摩根——德摩根放最后避免与上层规则竞争；顺序敏感故显式固定，不随
外部切片顺序改变（demo 打乱只重排外部传入次序，内部按优先级排序）。

## 5. 谓词下推

计划节点：Scan(table)、Filter(input, pred)。对 Filter：
- 比较只引用单表 → 并入该 Scan 的谓词；引用多表则留在 Filter。
- AND：各子谓词独立下推（分别按其引用表）。
- OR/NOT：仅当全部叶子引用同一张表时整体下推，否则保留在 Filter
  （跨表 OR 拆分不保持三值语义，保守处理）。
完成后自检：每个 Scan 谓词的自由列只属于该表，否则 ErrCrossTable。

## 6. 等价性验证

给定 n 列与每列值域（NULL+若干小整数，默认 {NULL,1,2}），枚举 3^n 个
赋值，分别三值求值两树，全等才等价；失败报告规则名与首个分歧赋值。
计划节点等价：投影为「扫描表行满足全部 Scan 谓词且满足 Filter 谓词」。

## 7. 边界

nil 树、列引用未知表、空 AND/OR → 可判定错误；单子 AND/OR 退化处理；
深度 1000 的链以显式栈思路处理（Go 增长栈下 1000 层递归亦安全）；
全 NULL 常量按 U 折叠；重复子表达式原样处理，不做 CSE。
