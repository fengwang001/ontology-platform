# DESIGN：带路径语义的通配模式匹配器

## 模型

路径与模式都按 `/` 切分为段，不做任何规范化（不折叠 `//`、不处理 `.`/`..`），
空段是合法段。空串切分为「一个空段」。段内原子：`?`（恰好一个码点）、`*`（段内零个或多个码点）、
`[...]`（一个码点）、字面量（`\` 转义）。`**` 只有单独成段时才是「零个或多个整段」，
否则段内相邻的多个 `*` 坍缩为一个 `*`（`a**b` ≡ `a*b`，`**.go` ≡ `*.go`）。

## 推导一：`x/**` 与 `**/x` 的边界

不变量：`**` = 零个或多个整段；**当它取零段时，两侧的分隔符随之消失**。
这正是 `a/**/b` 匹配 `a/b` 的解释：零段时 `a/**/b` 退化为 `a/b`。同一条规则向两端推广：

- `x/**` 取零段 → 退化为 `x`，故匹配 `x`；取一段（空段）→ 匹配 `x/`；取一段 → 匹配 `x/y`。
- `**/x` 取零段 → 退化为 `x`，故匹配 `x`；`/x` 的首段是空段，`**` 取空段 → 匹配 `/x`。
- `x/**` 不匹配 `/x`（首段 `""` ≠ `x`）；`**/x` 不匹配 `x/`（`x` 段之后还剩一个空段无模式可配）。

## 推导二：空串路径

空串 = 单个空段。`*` 匹配段内零个码点 ⇒ 匹配空段 ⇒ **`*` 匹配空串**。
`**` 取零段（或取一个空段）⇒ **`**` 匹配空串**。`?` 需恰好一个码点 ⇒ 不匹配。
空模式 = 单个无原子段 ⇒ 只匹配空段 ⇒ 只匹配空串。

## 推导三：无回溯算法

**段内**：`?`、字符类、字面量每个原子恰好消费一个码点，所以无星原子序列的匹配位置由起点唯一确定，
唯一自由度是每个 `*` 的跨度终点。失配时只回退到**最后一个** `*` 并让它多吞一个码点即可，不漏匹配：
若某成功对齐要求更早的 `*` 跨度更大，则最后一个 `*` 之后的无星序列必然从更晚的位置 q 开始匹配，
那么让最后一个 `*` 直接吸收到 q 之前的全部文本（星号可吞任意码点）仍是合法对齐——
即任何「早星加大」的对齐都可改写为「末星加大」的对齐。重试点 ≤ 段内码点数，每点重扫 ≤ 段内原子数，
段内代价 O(原子数 × 码点数)。

**不能照搬到 `**`**：`/` 是硬边界，段模式不能吞 `/`；两个 `**` 之间隔着多个整段模式，
「后一个 `**` 多吞一段」只能整段吸收，无法重现「前一个 `**` 多吞一段、中间段模式整体右移后重新匹配」
的对齐（如 `**/a/**/b`）。故跨段用段级 NFA 子集构造：状态 = 模式段下标集合，
`**` 状态消费任意段后自留，并经 ε-闭包把后继状态带入；每消费一个路径段做一次子集转移，
总代价 O(模式段数 × 路径段数 × 段内代价)，无指数回溯。

## 推导四：规则集版本一致

规则集整体编译为不可变的 version 结构（规则切片 + 版本号），`atomic.Pointer` 指向当前版本。
每次查询只 `Load` 一次，结论、`Explain` 的序号/原文/版本号全部来自同一份 version ⇒ 不会半新半旧。
`Replace` 在互斥锁内先把新规则**全部编译成功**才 `Store` 新指针；任一规则编译失败直接返回错误，
旧指针不被触碰，进行中的查询持有的旧 version 引用不受影响。

## 匹配结果总表（✓=匹配）

| 模式 | "" | x | x/ | /x | x/y | a/b | a/x/y/b |
|---|---|---|---|---|---|---|---|
| `x/**` | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x` | ✗ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ |
| `*` | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b` | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |
