# RLE 设计推导

唯一不变量：严格解码器接受的每个 t 必须满足 Encode(Decode(t)) == t。

## 必须拒绝的编码（逐条：若接受，哪个 t 违反不变量）

1. 显式次数 1：t=`1a` 解码得 `a`，重编码为 `a`，与 `1a` 不同。
2. 次数 0：t=`0a` 解码为空串，重编码为空串；空游程不是合法编码。
3. 前导零次数：t=`03a` 解码为 `aaa`，重编码为 `3a`；同一串出现两种写法。
4. 相邻同符号游程：t=`2a3a` 解码为 `aaaaa`，重编码为 `5a`；Encode 总是取最长游程。
5. 非法转义 `\x`（x 非数字非反斜杠）：解码无对应码点，且任何 Encode 输出都不含该序列。
6. 末尾孤立 `\`：`\` 后必须紧跟被转义符号，t 不是完整游程。
7. 末尾只有次数没有符号：t=`12` 不是完整游程，无法重编码。
8. 非法 UTF-8：Encode 只由合法 Go 字符串产生，符号是码点；故严格输出必为合法 UTF-8。

每类错误哨兵不同，错误携带字节偏移：次数类错误指向游程首字节，非法转义指向 `\`，
孤立 `\` 指向该字节，缺符号指向末尾，非法 UTF-8 指向首个坏字节。

## 大次数的取舍：拒绝，上限为 math.MaxInt

次数的语义是"展开为该数个码点"，任意整数精度没有收益：输出本身不可能超过
进程可寻址内存，而 64 位平台 MaxInt 已远超任何可构造的输出。故逐位累加，一旦
n > (MaxInt-d)/10 判定溢出，返回可区分的 ErrCountTooLarge（指向游程首字节），
绝不回绕。展开也不按次数一次性分配：流式解码每段最多缓冲 8 字节，写满即 flush，
并以可配置的输出字节上限 DefaultLimit=256MiB 预检（乘前先做除法上界检查），
超限返回 ErrOutputLimit。

## 语义与测试对照表

（实现完成后补齐）
