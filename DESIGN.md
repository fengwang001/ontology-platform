# 设计推导

## 代理对错误偏移

`\uD800`–`\uDBFF` 不表示字符，只表示一个尚未完成的码点前缀；它必须与紧邻的、无其他词法单元插入的 `\uDC00`–`\uDFFF` 组成代理对。

因此，孤立高代理读到后续词法单元时已经失败，偏移指向该高代理 `\u` 的起点：它是不可单独完成的无效单元起点。孤立低代理或颠倒顺序中的低代理可立即判定，偏移指向低代理自身的 `\u`。

`\uD83DA` 不能解释为“替换字符加 A”：`\u` 声明的是固定四十六进制转义；读到 `A` 时遇到引号，说明声明不完整。严格词法必须优先报“hex 不足”，不能重解释或静默替换。

## 最小转义与唯一性

JSON 字符串必须转义引号和反斜杠，因为二者承担词法边界或转义前缀；U+0000–U+001F 必须转义，因为 RFC 8259 禁止未转义控制字符。

U+007F 不是控制字符范围 C0，也不是 JSON 分隔符，所以原样输出；U+002F、U+2028、U+2029 和其他非 ASCII 均无强制 JSON 转义要求，也原样输出。

每个输入码点只有一种确定输出：固定短转义用于 `\b\f\n\r\t`，其余控制字符固定为小写 `\u00XX`，其他所有合法码点固定原样 UTF-8。故不同输入的编码结果不同，且同一输入的结果唯一。

## 测试对照

| 语义 | 测试函数 |
| --- | --- |
| 严格解码五类错误与偏移 | 待补 |
| 代理对组合、拒绝与偏移 | 待补 |
| 非法 UTF-8 两个方向 | 待补 |
| 最小转义编码 | 待补 |
| 两类往返性质 | 待补 |
| 任意切分点一致 | 待补 |
