# 游标分页器设计（keyset pagination）

只依赖标准库，状态存进程内存；`row`/`cursor`/`page`/`audit` 四个包 + `cmd/demo`。

## 1. 复合键边界：只记排序值会漏或会重

行 = 排序值 `Score float64` + 唯一 `ID string`，全序键 `K = (Score, ID)`：
`(s1,id1) < (s2,id2) ⇔ s1<s2 ∨ (s1==s2 ∧ id1<id2)`。

反例：三行 `(1,"a") (1,"b") (1,"c")`，页大小 2。
- 游标只记值 `v=1`，下页取 `Score > 1`：`(1,"c")` 被整批跳过（漏）。
- 下页取 `Score >= 1`：`(1,"a")(1,"b")` 再次返回（重）。

正确做法：游标记录上页末行的复合键 `K`，下页边界是 **严格大于** `K`：
正向 `WHERE K > Kcur ORDER BY K ASC LIMIT n`；首页 `Kcur = -∞`。
相等排序值由 ID 定序，因此每一行在全序列中位置唯一，不重不漏。

## 2. 反向翻页

反向不是把正向结果倒置。规则：比较方向与取数方向同时反转，页内再正向排列。
- 取数：`WHERE K < Kcur ORDER BY K DESC LIMIT n`（靠近游标的前 n 行）；
- 页内：将结果反转为 ASC 再返回；
- 新游标用「本次页内最远离 Kcur 的那一行」生成，使往返对称：
  正向用本页最后一行，反向用本页第一行。
故在同一数据集上正向到第 3 页后反向 1 页，逐行等于第 2 页（非其倒序）。
游标带方向标记；方向与请求不符返回 `ErrDirectionMismatch`，不静默处理。

## 3. 并发增删下的语义

翻页是纯函数：入参（数据集快照引用、游标、页大小、方向），不推进任何隐藏状态，
同一游标并发调用结果逐行相同。数据集内部用 `sync.RWMutex` 保护有序切片。

承诺（无快照隔离，只做 keyset 区间承诺）：
- 插入到已翻过区间（`K <= Kcur`）：允许不出现（迟到行）；
- 插入到未翻区间（`K > Kcur`）：将来翻到该区间时必按序出现，不跳过；
- 已返回过的行不会再次返回——边界是复合键而非位移，新插入不改变旧键相对次序；
- 删除：被删行不出现；游标不依赖该行存在，定位用键比较做二分，删除后照常续翻；
- 未删除行不会在其键区间内被跳过（边界严格、按全序键比较）。

## 4. 效率

数据集为有序切片 + id 索引。定位边界用二分 `sort.Search`，O(log N) 次比较，
取页 O(pageSize)，单页比较次数 ≤ `pageSize + ⌈log2 N⌉ + 1`。
游标编码：`dir(1) | score(8 BE) | idlen(2 BE) | id | crc32(4)`，长度级 O(1)，
解码为定长解析、常量次分配；空字节串 = 从头开始。

## 5. 校验与错误分类

解码顺序：长度不足或 ID 长度字段越界 → `ErrTruncated`；方向字节非法 →
`ErrBadDirection`；CRC32（IEEE，覆盖方向字节起的载荷）不符 → `ErrChecksum`。
页级另有 `ErrDirectionMismatch`（正向游标用于反向或反之）。四类错误均可用
`errors.Is` 判定。比特翻转必落在三类之一：末 4 字节翻转 → 校验失败；
方向字节翻成 0/2 → 方向非法；长度字段翻坏 → 字段不完整；其余载荷翻转 → 校验失败。
