# 差分数据同步：校验与增量传输器设计

## 1. 模型与流程
数据建模为字节切片（记录即字节）。块大小 `n`：目标端按 `n` 切块；源端用滑动窗口
（任意偏移）寻找目标块。流程：目标端 `sig` 生成签名 → 源端 `diff` 产补丁 →
`patch` 校验后原子落地 → `verify` 完整性判定。

## 2. 弱校验和（滚动）及其推进公式
对窗口 `x = x[k..k+n-1]`（1-based 下标 j=1..n）：

```
a(x) = 1 + Σ x[k+j-1]            (mod 2^16)
b(x) = n + Σ (n-j+1)·x[k+j-1]    (mod 2^16)
weak = a | (b << 16)              (uint32)
```

从偏移 k 滚动到 k+1（离开 out=x[k]，进入 inc=x[k+n]）：

```
a' = a - out + inc
b' = b - n·out + a'
```

代入即可验证 `b'` 等于在 k+1 处从头求和：旧 b 减去 `Σ x[...] = a` 后，
剩余部分恰好是新窗口按递减权重的和，`+a'` 完成权重归位。
因此每个偏移只需 4 次基本加减运算，弱和总代价 `O(L)`；若每偏移重算则是 `O(L·n)`。

## 3. 为什么必须两级校验（碰撞与分工）
- 只用弱和：弱和仅 32 位且加法可交换（`a` 对排列不敏感、值域仅 16 位），
  必然存在碰撞。碰撞会让不同字节块被误判为「同一块」并复用，
  补丁应用后数据错误却无任何报错——静默损坏。
- 只用强和：SHA-256 无法廉价地从窗口 k 推导窗口 k+1，每个偏移都要重算整窗，
  代价 `O(L·n)`，丧失滚动的意义。
- 分工：弱和负责 `O(1)/偏移` 的粗筛候选；仅在弱和命中时才算一次强和确认。

## 4. 签名格式（`sig`，小端）
`"OSIG" | n(u32) | 重复{index(u32) weak(u32) strong([32])}`。
强和为块的 SHA-256。`n=0` 返回可判定错误。

## 5. 补丁指令与文件格式（`diff`/`patch`）
指令：`COPY idx`（复用目标第 idx 块）、`LIT len + data`（新数据）。
文件：

```
"OPAT" | blockSize(u32) | srcLen(u64) | srcHash([32])
| count(u32) | 指令区：COPY=0|idx(u32)，LIT=1|len(u32)|原始数据
| crc(u32) = 此前全部字节的 IEEE CRC32
```

确定性：指令只依赖输入；弱和表冲突时取最小 index；无时间戳/map 遍历。

## 6. 截断分类与「不得部分应用」
解析补丁时按字节位置分类（四类，均可用 `errors.Is` 区分）：
1. 头部不完整：拿不满头（8+32 字节）。
2. 指令不完整：指令区中途截断（tag/idx/len/data 不足）或 count 不符。
3. 数据段不完整：指令区完整后、CRC 前没有恰好 4 字节。
4. CRC 不匹配：结构完整但 CRC 不符。
应用前先在内存中完成：完整解析 → 逐条重放（COPY 越界立即报错）→
比对结果长度与整体 SHA-256。任一步失败都不触碰目标文件；
全部通过才经临时文件 `rename` 原子替换。故要么全部应用，要么逐字节不变。

## 7. 最小性与计数约束
- 完全相同：每个窗口命中块，0 条 LIT，新数据 0 字节。
- 中间差一块：仅差异窗口无法命中，产生不超过约 `2n` 字节 LIT（含边界未命中窗口）。
- chunk 计数器：弱和基本运算次数 ≤ `4L`；强和计算次数 ≤ 弱和命中次数。

## 8. 边界
源/目标为空、前缀关系、`n>len`、`n=0`（错误）、长度为 n 整数倍/非整数倍、
末块不满：末块按其真实长度建签名与窗口匹配，空集合只含「源也为空」时 0 指令。

## 9. 错误集合
`ErrBadBlockSize`、`ErrHeaderTruncated`、`ErrInstrTruncated`、`ErrDataTruncated`、
`ErrCRC`、`ErrBlockIndex`、`ErrResultMismatch`、`ErrSignature`。
