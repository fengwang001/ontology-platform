# DESIGN（CSV 方言解析 / 并行切分 / 最小引号回写）

## 1. 空行与单列空值

选择：**空行跳过，不产生记录**。判定：行结束时若该记录尚未产出任何字段，
跳过（`\n`、`\n\n`、`\r\n` 开头均无记录）；一旦出现逗号（`a,`）或引号字段
（`""`），记录结构已存在，正常提交。
推导：单列「未引号空值」记录的字节形态只能是零长度，与空文件不可区分；若把
空行算作记录，它只能回写成空串，必与引号空 `""` 的引号标记冲突，往返第 5 条
不成立。故单列空记录的唯一表示是 `""`：回写器在「单列表、唯一字段值为空」
时必须加引号，否则该记录再解析时按空行被跳过，丢整条记录。多列记录的未引号
空字段（如 `a,`）有逗号定界，不需要引号。

## 2. par 切点

每段 worker 不知起点是否在引号内。对每段跑**双假设**片段解析：F 假设
（起点在引号外、字段中，无 carry）与 Q 假设（起点在引号内，carry 一个未
闭合引号）。左段在切点的真实状态 S ∈ {FIELD, Q, QQ, CR}（FIELD 并入 F
假设）由段尾状态直接推出。右段按 S 选用：Q 用 Q 跑的首格；QQ 与 F 在
「跳过首字节后」完全等价（QQ 见 `"`→Q，见其它→与 F 相同的报错/内容），
故用 F 结果加 O(1) 首格分类；CR 与 F 在首字节为逗号时相同，其它字节按 CR
规则判定。切点落在 `""` 中间：左段 QQ、右段首字节 `"`，拼回为一个引号字符。
每片段带段内偏移，加段起点得全局偏移；记录号、字段号从左到右累加（carry
占字段号，首片段不另起字段）。错误取最早全局偏移，坐标换算后与流式一致。
每字节在 F、Q 各处理一次，总量 ≤ 2n+常数。

## 3. CR 待定

状态 CR：未引号字段末见 `\r`。下一字节 `\n`→行结束；逗号→字段结束（均不含
CR）；引号或其它→ErrBareCR。半包：CR 留状态跨 Feed，不提交字段，下一 Feed
首字节判定。par：切点在 CR 后时左段停 CR（不发记录），右段首字节在拼接点
判定。流恰在 `\r` 结束：Close 时 CR 未决 → ErrBareCR。引号字段内 CR 是内容。

## 4. 上限「立刻」

字段字节计数 = 最终值字节数：未引号内容逐字节 +1；引号内内容逐字节 +1，
转义对 `""` 在第二个 `"` 确认时只 +1。字节到达时先增计数再转移，第一超限
字节即 ErrFieldTooLarge，不缓冲整记录。记录字段数：每见分隔符立即 +1 检查；
总记录数：行结束提交时立即检查。终态错误后 Feed/Close 返回同一错误。

## 5. 状态转移表

类别：`,` `"` `\n` `\r` 其它 o；动作 F=发字段 R=发记录。
| 状态 | `,` | `"` | `\n` | `\r` | o |
|---|---|---|---|---|---|
| START | F,START | →Q | R,START | →CR | 内容,FIELD |
| FIELD | F,START | ErrBareQuote | R,START | →CR | 内容 |
| Q | 内容 | →QQ | 内容 | 内容 | 内容 |
| QQ | F,START | 内容+1,Q | R,START | →CR | ErrQuoteClose |
| CR | F,START | ErrBareCR | R,START | ErrBareCR | ErrBareCR |
Close：Q/QQ→ErrUnclosedQuote；CR→ErrBareCR；否则提交末尾记录（0 字段不发）。
引号字段起点偏移为开引号位置；未引号字段起点为字段开始位置（含空字段）。

## 6. 规范输入与最小引号

必要引号：值含 `,` `"` `\r` `\n`；单列唯一空字段（见 §1）；原字段带引号
标记。规范输入 = 满足上述任一条件处加引号、`""` 转义、行分隔统一 `\n`（
字段内 CRLF 原样保留）、末尾恰好一个 `\n`、无冗余引号的 CSV。非规范但合法
的输入保证 Parse(Write(T)) 逐字段/标记相等，不保证字节还原。

## 7. 并发说明

单个流式解析器实例非并发安全；par 内部 K 个 goroutine 各自独立状态机，
仅通过局部切片通信，拼接在主 goroutine 完成。
