# 设计推导

流式状态机：每个字节先过 `eol`（CR 等待下一字节，LF/CR 都产生一个边界），
再在 `ws` 上动作。规则：普通字节→先 flush 待定空白再输出；边界→丢弃待定空白并
输出一个 `\n`。于是 `\r\r\n` 产生两次边界（`\r` 一次、`\r\n` 一次）= 两个行尾。
EOF 时残留 CR 算边界、残留空白是末行尾空白，丢弃。最后应用末尾策略。

## 1 末尾换行策略（样例表）
设输入末尾连续换行个数 t（无内容时另计）。Keep=原样；Ensure=无内容→`""`，
否则恰好一个；Trim=t==0→不加，t>0→恰好一个。选「空输入→空」而非 `"\n"`：
若空→`"\n"`，则 `N("")="\n"` 而 `N("\n")="\n"`，仍幂等但把空文件改成非空，
编辑器无中生有；空→空使 `N` 在空串上是恒等，最自然。

| 输入 \ 策略 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等论证：输出只含 `\n`、无行尾空白；再规范化时无 `\r`、无可删空白，唯一变化
来自末尾策略，而三策略对「已规范形态」（尾换行数 0/1/>1）都是不动点。

## 2 被删字节的 ToOut
令删除段原文区间 [a,b) 紧随其后的保留映射起点为输出 p（被删尾空白后即行尾 `\n`
的位置；`\r\n` 的 `\r` 后即 `\n`）。单调性与 `ToOut(ToOrig(p))==p` 要求：
若 ToOut(i)<p，则 ToOrig 必须能回到 i，但 p 之前没有输出点可承载，矛盾；
若 >p 则破坏紧随关系。故对所有 i∈[a,b) 取 ToOut(i)=p（坍缩到保留端）。
终点 b（下一个保留字节）也映射 p，整段坍缩为一个输出点。编辑器光标落在被删
尾空白里时映射到 `\n` 的位置（行尾换行符），即换行符之前的行末位置，不跨行。

## 3 缓冲超限（选拒绝）
选择**拒绝**：待定空白达到上限且仍未见到行尾时，返回带原文偏移的哨兵错误，
已输出内容与映射保留，实例进入终态（再写返回 ErrClosed）。
强制输出违反幂等第 5 条：先判定为「非行尾」输出空白，随后真的来了换行，
这些空白本应删除却留在输出，且第二次规范化会删掉它们，N(N(x))!=N(x)；
它也使结果依赖切分/上限，违反第 4 条。拒绝不产生任何输出副作用，
不影响 4/5/6；上限为 0 表示不允许行尾空白。

## 4 par 拼接与修正上界
切点可能切在待定 CR 或行尾空白中。可携带状态只可能是切点前连续的
`[ \t]*\r?`：空白受缓冲上界 W 约束、CR 至多 1 个，故修正范围 ≤ W+1，有上界。
做法：按偏移切 K 段；段 j 起点回退 carry（前段尾部连续空白与可选 CR，
仅由原文确定性扫描得出），把该块连同 carry 前缀交给一个独立 norm（Keep 末尾
策略；除最后段外在结束点再产生一个虚拟边界，使该块结束于行边界，从而无残留
CR/空白/末尾策略改动）。各块输出顺序拼接；映射先把段内坐标整体平移
（原文平移 base-carry、输出累加前段输出长度），再合并相邻区间。这样每字节恰在
一个块处理，块边界是行边界，跨块状态等价于单流，输出与映射逐位相同。
