# 设计推导

## 非法 UTF-8 单元

从首字节立即知道期望长度。第二字节必须同时是 `10xxxxxx`，且落在该首字节的合法区间；否则该首字节单独成为一个非法单元，后续字节重新解析。后续 continuation 若缺失，Close 时已缓存前缀整体是一个非法单元；若等待中先看到非法 continuation，则只终结当前前缀，不吞那个新字节。因此 `F0 90 80 41` 的前 3 字节是“4 字节序列截断/遇非 continuation”的一个单元；`E0 80` 第二字节越界后，`80` 再作为孤立 continuation，第三个 `80` 同理，共 3 个；`ED A0 80` 同理；`C0 AF` 首字节非法后 `AF` 孤立；`F4 90 80 80` 为 4 个；`E2 82`+EOF 是同一个截断前缀；`80 80` 是两个孤立 continuation。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 首字节；若已收到非法第二字节，第二字节重解析 |
| `E0` | `A0..BF` | 已缓存前缀（1 或 2/3 个）；下一字节重解析 |
| `E1..EC` | `80..BF` | 同上 |
| `ED` | `80..9F` | 同上 |
| `EE..EF` | `80..BF` | 同上 |
| `F0` | `90..BF` | 同上 |
| `F1..F3` | `80..BF` | 同上 |
| `F4` | `80..8F` | 同上 |
| `80..BF` | 无，孤立 continuation | 1 字节 |
| `C0 C1 F5..FF` | 无，非法首字节 | 1 字节 |

非最短形式由 `E0/A0..BF`、`F0/90..BF` 排除；越界由 `ED/80..9F`、`F4/80..8F` 和 `F5..FF` 排除。

## 切分缓存与提前放弃

UTF-8 最长 4 字节，最多缓存首字节后的 3 个 continuation；在收到决定非法性的第二字节、后续遇到非 continuation、或首字节本身非法时立刻产出一个非法单元，不等待 EOF。只有“到 EOF 仍是合法形状前缀”才按截断处理。因此任何时刻待决字节不超过 3；输出端也不允许半成品留在输出缓冲。

UTF-16 最多缓存 1 个奇数字节，或 1 个待配对高代理；高代理后的下一 code unit 若不是低代理，高代理先成为非法单元，该 code unit 重新解析。EOF 奇数尾或高代理尾是可区分的截断错误。

## par 切点对齐

切点在第 `p` 字节前。每段从指定起点扫描；为避免段中字符的首字节落在前一段，起点向前最多回看 3 字节：若 `p-i` 是 lead 且其期望长度跨过 `p`，则把起点移到该 lead；continuation 不自行开新序列。上一段结束于这个新起点，形成不重叠、无空隙的覆盖。

例：`C3 | 80 80`，左段吃到整个 `C3 80`，右段从最后一个 `80` 开始并报 1 个孤立 continuation，和单线程一致。切点在非法前缀中间时，持有其首字节的左段负责吞掉整个非法单元，右段只从下一新单元开始。统计按原始输入偏移归并，因此输出、错误和计数均一致。

## 上限断点

Write 先把输入交给解码器；完整标量先编码到临时输出，只有完整放入上限才追加。放不下时返回 `ErrOutputLimit`，本标量及之后字节、以及仍在解码器中的半个字符，都不算该次 Write 已消费；调用方可从返回偏移续传到新实例。已经是非法单元时同理：替换符放不下则该非法单元不消费。残留在切分缓存里的字节只有在已经归并成一个完整合法/非法单元时才算消费；EOF 截断错误中的残余前缀报告其起始偏移和长度。

## 复杂度

stream 与 par 各维护非导出 `checks`：每次“检查一个输入字节”加一；缓存 continuation 不回扫，对齐只向后看常数 3 字节。故流式检查次数不超过 `2n`（EOF 前缀最多额外常数），K=8 的 par 不超过 `n+8*C`。
