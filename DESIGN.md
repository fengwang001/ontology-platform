# 设计推导（题面第三节）

## 1. 非法单元切分规则

统一规则（WHATWG 风格，但全部手写判定）：在当前边界取首字节 `b`。
- 续行字节 `80..BF`、非法首字节 `C0 C1 F5..FF`：吞 1 字节，立即非法。
- `b∈C2..DF`：期望 2 字节；第二字节必须 `80..BF`。第二字节不合法：吞 1；合法但其后（到流结束）缺续行：吞全部已到字节，算 1 个单元。
- `b∈E0/ED/E1..EC/EE..EF`：期望 3 字节，第二字节区间如下表，第三字节恒为 `80..BF`；第二字节越界只吞 1，第二合法而第三缺失/非法则吞全部（首+第二+0/1）。
- `b∈F0/F4/F1..F3`：期望 4 字节，第二字节区间如下表，第三、四字节恒为 `80..BF`；同样“第二越界吞 1，之后缺一吞全部已到字节”。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 | 后续缺失/非法时吞掉 |
|---|---|---|---|
| C2..DF | 80..BF | 1 | 期望长度内全部已到字节 |
| E0 | A0..BF | 1 | 同上 |
| E1..EC | 80..BF | 1 | 同上 |
| ED | 80..9F | 1 | 同上 |
| EE..EF | 80..BF | 1 | 同上 |
| F0 | 90..BF | 1 | 同上 |
| F1..F3 | 80..BF | 1 | 同上 |
| F4 | 80..8F | 1 | 同上 |
| 80..BF | —（首字节即非法） | 1 | — |
| C0 C1 F5..FF | —（首字节即非法） | 1 | — |

由此：`F0 90 80 41` 第二字节 90 合法、第四字节 41 非法 → 一个单元；`E0 80 80`
第二字节 80∉A0..BF，首字节 E0 单独成单元，随后 80、80 各为续行非法 → 三个；
`ED A0 80` 同理（A0∉80..9F，且编码即代理）→ 三个；`C0 AF` 中 C0、AF 各吞 1；
`F4 90 80 80` 第二字节 90∉80..8F → 四个；`E2 82` 后流结束属于“合法前缀被截断”，
只吞这两个字节成一个单元；`80 80` 两个首字节非法 → 两个。严格模式据此区分
“非法字节”与 Close 时的“截断”：单元在非法字节出现那一刻定性，缺字节只在 EOF 定性。

## 2. 切分缓存上限与放弃等待的时机

UTF-8 合法前缀最多 4 字节，缓存 `[4]byte` + 长度 + 已验续行数即可（硬上限 4）。
一旦第 k 个续行字节不在 `80..BF`（或落在第二字节受限区间外），前缀立刻定性为
非法单元，绝不等待流结束；该字节本身不被本单元吞掉，作为下一个单元首字节重解析。
仅当缓存是“仍可合法延长”的前缀时才继续等待；`Close` 时残留 → 截断（严格）
或一个 U+FFFD（替换）。UTF-16 同理：码元 2 字节、代理对 4 字节，缓存上限 4
（奇数 1 字节或孤单高代理 2 字节时等待）；高代理后出现非低代理码元，立刻定性，
且该非低代理码元原样作为新码元重解析，不被吞掉。

## 3. par 的切点对齐

每段起点先回看：若切点前一字节是续行字节，则继续向前，最多越过 3 个连续续行字节
（UTF-8 最长 4 字节），回看到的字节全部并入后一段；否则从切点开始。前一段则从
其起点解析到“被让给后段”之前。这样任何跨切点的序列——完整字符或非法单元
（例如前半 `C3`、后半 `80 80`：切点落在 C3 之后，80 被并入后段，整段作为
`C3 80`+孤立 80 解析，与单线程的“一个合法标量 + 一个续行非法”完全一致；
`F0 90 80` 型同理，含第二字节受限的判定所需字节也一并让渡）——都恰好由后段解析
一次，前段不产生任何针对它的替换，故标量数、非法单元数、字节守恒均与单线程相同。
回看只触达切点前至多 3 字节，不整段重扫。输出拼接时前段丢弃被让渡字节对应输出
（这些字节尚未形成完整标量，仍在其切分缓存中），后段原样从回看点启动普通流式
转码，统计合并即得单线程结果。K∈1..8 与所有切点均同理。

## 4. 第 9 条断点

编码每个标量前先确认目标侧整段编码（UTF-16 代理对占 4 字节，视为整体）放得进
剩余额度；放不下则在编码前停止，绝不写半个标量/半个代理对。`Write` 返回的
已消费数只包含“已形成并输出的标量”对应的输入字节；仍在切分缓存里的前缀字节
不算已消费（调用方从返回偏移继续喂给新实例即可）。上限错误为独立哨兵错误，与
非法字节、截断、终态写入互可区分。新实例从断点续传，两段输出拼接等于一次转完。
