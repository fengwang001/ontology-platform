# 属性级权限过滤器设计

## 1. 威胁模型与三种处理

谓词中引用了对当前角色不可见的列。三种常见处理：

- 甲（不可见列比较一律为假）。探针 `NOT (secret = 1)`：比较为假，NOT 后为真，
  返回全部行；攻击者逐值枚举 `NOT (secret = v)`，命中值时结果集变化，逐值反推 secret。
  即使 secret 完全不出现在结果列里，值仍通过“返回哪些行”泄露。
- 乙（不可见列当作 NULL，三值逻辑）。探针 `secret IS NULL`：替换后为 NULL IS NULL =
  TRUE，返回全部行；而 `secret = 1` 对不可见调用方恒为 UNKNOWN、永不放行。
  行集的有无差异本身就是探针，且 `IS NULL` 直接返回成功，泄露列存在性与行集信息。
- 丙（整条查询拒绝，指明列名与谓词路径）。不可见列从不参与求值，输出只有错误，
  错误内容只包含用户已知的列名与树结构，无任何数据依赖，故不泄露。

本实现采用丙。三类错误用 `errors.Is` 区分：
`ErrInvisibleColumn`、`ErrUnknownColumn`、`ErrInvalidPredicate`。

## 2. 列引用与路径

路径为从根到该列比较节点的操作数序号序列，如 `[0,1]` 表示根的第 0 支里的第 1 支。
同一列多次出现只报一次列名，但保留全部路径。

## 3. OR 短路例外

常量折叠先于授权检查，在同一遍后序遍历中完成：

- 常量子树折叠为 `Const`；
- `AND` 任一支折叠为常量假：整支恒假，其余支被吸收，其列引用一律不上报；
- `OR` 任一支折叠为常量真：整支恒真，其余支被吸收，其列引用一律不上报；
- `NOT` 不吸收任何支。

定义：折叠后子树为常量时，其中不可见列在运行时不会被读取（没有任何数据行能改变
结果），故不拒绝，且不出现在审计报告中。这是“证明未读取即放行”，不是“猜测不可见
列的值”；无法证明为常量的引用一律拒绝。反例：`OR` 另一支是普通可见列比较时，
不可见列仍可能被读取，必须拒绝。

## 4. 行裁剪

结果行只保留可见列：按可见集合分配新 map 并逐个拷贝。不可见列直接缺失，不置零值、
不置 NULL——“不存在该键”与“键存在但值为空”可区分。拷贝次数等于可见列数，
与行的总列数无关。

## 5. 复杂度与确定性

- 谓词检查：一遍后序遍历，访问节点计数等于谓词树节点总数，不重复遍历。
- 行裁剪：O(可见列数)，与总列数无关。
- 报告：被裁列名字典序；拒绝引用按路径、再按列名排序；与输入构造顺序无关。
- 无谓词（nil）与只含常量的谓词：无任何列引用，恒放行。

## 6. 穷举口径

三列 a、b、c，八种可见性掩码；四种谓词形态 `=`、`NOT`、`AND`、`OR`。
`=` 与 `NOT` 只对列 a 做一次比较（NOT 作用于该比较）；`AND`/`OR` 连接 a、b、c
各一次比较。放行当且仅当谓词中每一处未被常量吸收的列引用均可见。本例无常量，
`AND`/`OR` 不发生吸收；单引用形态在 a 可见的 4 个掩码下放行，三引用形态只在
全可见 1 个掩码下放行：`EQ/NOT/AND/OR = 4/4/1/1` 放行，拒绝数 `4/4/7/7`。
