# 设计推导

## 1. UTF-8 非法单元

规则：从首字节进入长度为 1/2/3/4 的候选序列；只吞入“相对当前位置合法”的后续字节，得到最长合法前缀。前缀完整则接受；遇到流结束或第一个不合规字节时，这个最长合法前缀就是一个非法单元，不合规字节不被吞入，而是作为新候选重新判定。

- `F0 90 80 41`：前三字节是四位序列的合法前缀，缺第四位，故三字节一个单元，`41` 重新开始。
- `E0 80 80`：第二位对 `E0` 必须是 `A0..BF`，`80` 不合规；最长前缀只有 `E0`，两个 `80` 各自重判，共三个。
- `ED A0 80`：第二位对 `ED` 必须是 `80..9F`，故 `ED`、`A0`、`80` 依次各成单元。
- `C0 AF`、`F4 90 80 80`：非法首字节或第二位越界后立即收束，后续 continuation 各自重判。
- `E2 82`+EOF：两位均合法但缺第三位，故截断时只产生一个单元。

| 首字节 | 第二字节区间 | 第二位非法 | 后续位置非法 |
|---|---|---:|---:|
| `C2..DF` | `80..BF` | 吞1 | 吞已有合法前缀 |
| `E0` | `A0..BF` | 吞1 | 第3位非法吞2 |
| `E1..EC` | `80..BF` | 吞1 | 第3位非法吞2 |
| `ED` | `80..9F` | 吞1 | 第3位非法吞2 |
| `EE..EF` | `80..BF` | 吞1 | 第3位非法吞2 |
| `F0` | `90..BF` | 吞1 | 依次吞2或3 |
| `F1..F3` | `80..BF` | 吞1 | 依次吞2或3 |
| `F4` | `80..8F` | 吞1 | 依次吞2或3 |
| `80..BF` | 无首字节资格 | 吞1 | - |
| `C0 C1 F5..FF` | 永不合法 | 吞1 | - |

## 2. 流式缓存

UTF-8 最长 4 字节，等待下一位时至多缓存 3 个已确认合法的前缀字节；UTF-16LE/BE 一个代码单元 2 字节，高代理后再等一个低代理，故总缓存上限也取 3 字节。每来一个字节只检查一次：不合规的结束字节先触发前缀收束，再作为新首字节检查，因此总检查次数不超过 `2n`。第二位非法时立刻收束，不必等流结束；只有“目前每一位都合法但长度不足”的前缀才等待。

## 3. par 切点

边界可能切开字符或非法单元。第 `i` 段从 `start_i-3`（不小于0）开始解码，重叠区最多 3 字节；worker 只认领“单元起始偏移 ≥ start_i”的结果，前一个 worker 用同样的最多 3 字节前瞻认领跨界单元。非最后段不执行 EOF 收束。因为任意单元的回溯距离不超过 3，重叠解析在边界处必然到达同一状态，且每个单元只由起始偏移所属 worker 统计一次。`C3|80 80` 中，前段借到 `80` 完成合法双字节并由前段认领；后段从重叠区重放但过滤该单元，剩余独立 `80` 成为一个非法单元。

## 4. 上限断点

转换器只在完整标量边界检查输出上限。当前缓存中的不完整前缀尚未构成标量，不计入本次 `n`；`Write` 返回的 `n` 只包含已经形成输出或错误单元的字节。超限时最后一个完整标量不写出，返回错误和 `ResumePrefix() []byte`：其内容是内部未完成前缀追加本次未消费后缀。调用方把它作为新实例的首次写入，即可从标量边界续传；新旧输出拼接等于无限额结果。
