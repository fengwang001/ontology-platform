# 属性级权限过滤器设计

## 目标与边界

输入为角色到可见列集合的策略、内存中的行、布尔谓词树。输出只包含可见列的行、可能被常量折叠后的谓词，以及确定性审计报告。不实现存储、索引、SQL 解析、HTTP 或认证。

谓词节点为：常量、列比较（`=`、`IS NULL`）、`NOT`、`AND`、`OR`。路径使用从根开始的 1-based 子节点下标；根比较的路径为空，例如 `NOT(secret = 1)` 中的比较路径是 `[1]`。

## 不可见列谓词的三种处理

- 甲：把不可见列比较当假。探针 `NOT (secret = 1)` 会在 `secret` 不等于 1 时放行，攻击者可据此反推受保护值。
- 乙：把不可见列当 `NULL` 参与三值逻辑。`NOT (secret = 1)` 的结果为 `NULL`，其“既非真也非假”的存在性仍可被聚合或分支观察；`secret IS NULL` 更会直接返回真，成为稳定探针。
- 丙：谓词引用当前角色不可见的列时，整条查询拒绝，并在错误和审计报告中给出列名与原始谓词路径。该回答只依赖权限元数据，不依赖受保护单元格，因此不泄露值或存在性。

本实现采用丙。三类可判定错误分别是未知角色、非法谓词、不可见谓词引用，均可用 `errors.Is` 区分。空可见列集不是错误；它只允许常量谓词。

## OR 短路例外

可见性检查与一遍常量折叠同时进行：先在逻辑上把子表达式证明为常量，再决定它是否仍会被求值。凡折叠后不属于运行时表达式的引用，均视为未读取。

- `OR` 的一支折叠为永假：该支删除，其中不可见列不拒绝。
- `OR` 的任一支折叠为永真：整个 `OR` 折叠为真，其余支短路删除；折叠支和被短路支中的不可见列均不拒绝。
- `OR` 保留的非常量支中只要出现不可见列，仍整条拒绝。原因是该行相关支必须读取受保护列才能生成布尔结果。

同一规则也自然适用于 `AND` 中的永假支、永真支和被折叠成常量的整个子树。例外依据是“常量折叠后该列运行时不可达”，而不是语法上恰好位于 `OR` 下方。

## 行裁剪与报告

行使用 `map[string]*string`：键不存在表示列被移除，键存在且指针为 `nil` 表示可见值就是 SQL `NULL`。裁剪只复制可见且该行实际存在的列；不可见列不会被写成空字符串或 `NULL`。

报告包含字典序去重的被移除列、按列名字典序去重的拒绝引用（每列保留全部路径），以及查询是否被整条拒绝。报告只记录集合和固定路径，不依赖行或 map 的构造顺序。

## 复杂度

谓词分析是一次后序遍历：每个节点完成合法性检查、常量折叠、可见引用收集和节点计数。折叠结果复用于行过滤，不再次遍历原树。计数器在 `filter` 包内非导出，测试断言其等于节点总数。

行裁剪按可见列迭代，并只在源行存在该键时执行一次目标赋值。因此单行列拷贝次数不超过可见列数；一千列、五列可见时不超过 `4 * 5` 的测试上界。
