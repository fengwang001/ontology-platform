# 设计推导

## 代理对错误偏移

规则：偏移始终指向被拒绝的最小词法单元起点，即反斜杠位置。

- 孤立低代理如 `"\uDE00"`：该单元自身非法，偏移指向其 `\`。
- 高代理必须继续检查紧邻的下一个转义单元；`"\uD83Dx"` 中下一单元不是转义，偏移指向高代理起点。
- `"\uD83DA"` 同样缺少必需的低代理；`A` 已是另一个普通内容，不能把高代理替换成 U+FFFD 后继续。
- `"\uD83D\u0041"` 中下一单元不是低代理，偏移仍指向高代理，因为完整要求“高代理后紧跟低代理”在此失败。
- `"\uDE00\uD83D"`：第一个单元已是非法低代理，偏移指向第一个 `\`，不越过首个错误。
- 结尾或引号截断高代理时，高代理就是缺失配对的失败单元，偏移指向高代理 `\`。

理由：JSON 字符串不是“逐 rune 容错”的 UTF-16；`\uD83D` 不是完整码点，而是尚未闭合的半个词法产生式。严格解码器必须拒绝整个输入，且最早错误位置最利于定位。把它改成 U+FFFD 会改变长度和语义，也会把本应错误的 `\uD83DA` 静默解释成两个合法 token。

## 最小转义与唯一性

编码只把 JSON 字符串中必须转义的字节写成转义：引号、反斜杠和 C0 控制字符 U+0000 至 U+001F。控制字符中五个有 RFC 短形式：`\b`、`\f`、`\n`、`\r`、`\t`；其余固定为四位小写 `\u00XX`。

U+001F 必须转义，因为 RFC 8259 明确禁止未转义的 U+0000 至 U+001F 出现在字符串中。U+007F 虽属控制字符，但不在该禁止区间；它是合法 Unicode 标量，UTF-8 表示也合法，因此原样输出即可。`/`、非 ASCII、U+2028/U+2029 同理均不要求 JSON 转义。

单射性：对任一码点，规则在“原样输出”和唯一转义形式之间二选一，不存在同一码点两种编码。解码端看到反斜杠只会得到该转义规定的码点；普通 UTF-8 字节则严格解码为其自身码点。不同输入若编码结果相同，逐位置反推出的码点必相同。每个码点的输出形式固定，码点序列顺序不变，所以编码结果唯一，并满足严格最小转义字面量的往返。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待补 | 待补 |
