# DESIGN — 租借池关键规则推导

## 1. 取消与交接同时发生
不变量：任意交错下 `借出+空闲+创建中+已销毁 == 已创建`，且每个资源同一时刻至多一个持有者。
推导：一次交接有三个事件——池把等待者**出队**、池把资源**投递**进等待者的缓冲通道、等待者
的 ctx 触发**取消**。出队与投递在同一把锁内完成，是一个原子提交点。对取消方而言只有两种
本质顺序：
- 取消时自己仍在队列中：把自己从链表 O(1) 摘除，返回 `ctx.Err()`；资源从未动过，守恒成立。
- 取消时自己已被出队：资源已经提交进通道。若此时仍返回 `ctx.Err()`，通道里的资源无人接收，
  它不在借出/空闲/创建中任何一态，也不会被 Destroy——守恒等式被打破，资源凭空消失。
  「取消时把自己从队列里删掉就完事」错在假装提交点可以无视：删除动作无法撤销已发生的投递。
规则（提交点规则）：**出队即提交**。取消方发现自己已被出队时，必须接收交付物并返回成功，
由调用方走正常 Release；只有仍在队列中的等待者才允许以 `ctx.Err()` 退出。于是任意交错下
每个资源恰有一个持有者，等式成立。测试 `TestCancelHandoffRace` 用 channel 对齐取消与归还
跑 10000 轮逐轮断言守恒；若按错误规则实现，被丢弃的资源会让等式在某一轮失效。

## 2. 复用顺序：LIFO 还是 FIFO
场景：10 个空闲、单调用方每时钟刻借一次还一次、`MaxIdleTime`=50 刻、`MinIdle`=1、1000 刻。
- LIFO（取最近归还）：同一槽位每刻被复用，归还时刻不断刷新；其余 9 个空闲时长单调增长，
  第 50 刻起陆续过期，被回收到 `MinIdle` 下限为止 → 最终空闲 = **1**。
- FIFO（取最久空闲）：10 个槽位轮转，每个空闲时长最多 10 刻 < 50，永不过期 → 最终空闲 = **10**。
结论：选 **LIFO**。FIFO 复用会让轻负载下的空闲回收永远触发不了（正是线上事故根因），
第 9 条形同虚设；LIFO 让冷资源自然老化被回收，热资源保持复用。

| 复用顺序 | 最终空闲数 |
|----------|-----------|
| LIFO     | 1         |
| FIFO     | 10        |

测试 `TestLightLoad` 构造该场景断言 1000 刻后空闲 == 1。

## 3. 名额交接：谁调用 Create
推导：Create 可能慢甚至阻塞；若由归还方代建，Release 被拖慢，且失败时错误无法归属到具体
的 Acquire。规则：**等待者自己的 goroutine 调用 Create**。没有空闲但有空余名额时，归还方/
丢弃方/创建失败方只在锁内把预占的「创建中」名额（permit）随唤醒**交接**给队首等待者；
被唤醒者持 permit 在自己的 goroutine 里重新走获取流程并自己 Create。创建失败时：错误返回
给发起创建的那个 Acquire；permit 不直接释放，而是交接给下一个等待者（链式重试，无人永久
挂起）；队列已空才真正 AbortCreate 释放名额。新来者见队列非空必须排队，抢不走该名额。

## 4. 空闲全坏
选择：**不报错，销毁坏资源后转去创建新资源**。论证：Validate 失败是资源质量问题而非容量
问题；每销毁一个坏空闲就释放一个名额，转去创建既不超发，也符合调用方「拿到可用资源」的
预期；若直接报错，调用方只会立刻重试并走同一条路，错误没有信息量。上界：单次 Acquire 的
Validate 次数 ≤ 进入时的空闲数（每个坏资源随即被销毁、不会被复查），故 ≤ `Max`。
测试 `TestAllIdleBad`：3 个空闲全坏时 Validate 恰 3 次且 Acquire 成功拿到新建资源。
