# LZ77 流式压缩设计

## 流格式（所有整数为 ULEB128 变长编码，限 10 字节）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 4 字节魔数 `OLZ7` + varint 版本号（=1） |
| 字面量段 | varint(n<<1)（n≥1）+ 紧随 n 个原始字节 |
| 回指 | varint(len<<1\|1)（len≥1）+ varint(dist)（dist≥1） |
| 刷新标记 | varint(0)，无负载，解码器视为无操作 |
| 流尾 | varint(1) + varint(原文总长) + varint(FNV-1a/64 校验和) |

首 varint 为 0 是刷新、为 1 是流尾、≥2 偶数是字面量、≥3 奇数是回指，因此
任意 varint 都落在四种记录之一，不存在「未知标记」这一额外错误类。

## 推导一：压缩结果与 Write 切法无关（语义 3）

- 设最大匹配长为常量 M=65536。位置 pos 的最长匹配若被截断在「当前已收到
  数据的末尾」，则未来 Write 可能让它继续变长，此时下结论就会依赖切法。
- 因此规定：只有当匹配长度 L 满足 `L < 待处理字节数`（被失配字节终止，未来
  数据无法改变它）或 `L == M`（撞到上限，未来数据同样无法改变）时，该匹配
  才是「已确定」的，才允许发出 token；否则把 pos 之后的字节留在缓冲区等待。
- 推论：缓冲区积压（pending）恒小于 M——一旦 pending ≥ M，匹配必被 M 截断
  而可发出。故内存有界，且每个 token 的边界只取决于输入字节序列本身与
  Flush 位置，与 Write 如何切分无关。字面量是否可发同理（L<MinMatch 且已
  确定时发字面量）；字面量段按固定 32768 字节切块，也是输入的确定函数。
- 对 Flush 的含义：Flush 是对「未来没有更多数据」的临时承诺，必须按
  final 语义强制结束当前匹配（允许 L==pending 的匹配直接发出）、清空字面
  量缓冲并写刷新标记，否则 Flush 前的数据无法被完整还原，违背语义 4。

## 推导二：距离 < 长度的回指复制（语义 2）

- 块复制（copy 一整段）在 dist<len 时源与目标重叠，读到的是尚未写出的旧
  值，结果错误；逐字节向前复制则每写一个字节就成为后续可读的源，语义正确。
- 实现：`Window.AppendBack(dist,n)` 每次复制 `min(dist,剩余)` 字节——因为
  模式以 dist 为周期，复制 dist 字节后末尾 dist 字节仍是同一模式，可继续按
  dist 为块长复制。迭代次数为 ceil(n/dist)，每次是一次真正 memmove；
  仅 dist=1 退化为逐字节填充，总工作量仍是 O(n)，可以接受。

## 推导三：「距离超过已输出」与「距离超过窗口」为何是两个错误（语义 7）

- 窗口容量 W 是格式层面的硬限制（dist>W 的流永远无法解码，因为更早的字节
  已被挤出环形缓冲）；「已输出字节数」是解码进度的软状态（流开头 dist≤W
  但历史尚不足）。二者混淆会把「合法的流开头小回指」误判，或把「超出窗口」
  拖到中后期才暴露。
- 压缩器只从哈希链取候选，且链上位置 c 满足 `pos-W ≤ c < pos`，故
  `1 ≤ dist ≤ W ≤ 已输出`，两类错误我们的编码器都不可能产生；它们只为防
  御外部/损坏流。并行块压缩把上一块尾部作为预置字典载入窗口，「已输出」
  仍是绝对输出偏移（字典字节本就是前一块的真实输出），含义不变。

## 推导四：并行块压缩的确定性（语义 9）

- 块 i 的编码器 = 哈希链匹配器 + 预置字典（上一块末尾 ≤W 字节原文）。匹配
  候选只来自「本块已处理前缀 + 字典」，二者都是输入数据的确定切片，与其
  他块的压缩结果、goroutine 调度、完成顺序无关，故 workers 取任意值输出
  逐字节相同。
- 衔接：各块只产出一串 token（无流头流尾），按块序拼接，块间写一个刷新
  标记（与流式 Flush 的输出形态统一，解码器视为无操作，窗口跨块保留，
  因此跨块回指天然合法）；最后追加整流流尾（总长 + 整体校验和）。
