# DESIGN：自定格式 LZ77 流式压缩

## 1. 流格式（所有字段均为 unsigned varint，小端 7 位一组；原始字节直接内联）

| 记录 | 字节布局 |
|---|---|
| 流头 | varint 魔数 `0x4C5A3737`，varint 版本 `1` |
| 字面量 | varint `(len<<3)|0`，后接 len 个原文原始字节 |
| 回指 | varint `1`，varint distance(≥1)，varint length(≥3) |
| 刷新 | varint `2`（无负载） |
| 流尾 | varint `3`，varint 原文总长，varint FNV-1a 64 校验和 |

流 = 流头 记录* 流尾；刷新标记对解压状态无影响，仅承诺字节边界。

## 2. 第三节四处推导

### 2.1 Write 切法无关（语义 3）

压缩器把未决策字节全部留在缓存，按绝对偏移做贪心匹配。在位置 p 令 avail=已知未处理字节数，Find 给出已知数据内最长匹配 L。
关键引理：若 L<avail，则 L 也是「未来全部数据」下的最长匹配。反证：若真有长度 L'>L 的匹配，则 min(L',avail)≥L+1 个字节在当前已知数据内本就相等，Find 必然找到，矛盾。
因此当且仅当 L<avail（或达到长度上限）时才能拍板一个回指；L==avail 表示匹配顶到缓存末端、未来可能延长，必须挂起等待下一次 Write。
字面量同理：avail≥3 时哈希已知、候选只来自已定稿历史，若连 3 字节匹配都没有则永不可能有，可定稿；avail<3 时连三字节哈希都算不出，挂起。
定稿只依赖「绝对偏移与已写入历史」，不依赖一次 Write 给了多少字节，所以输出与切法无关。
Flush 的含义是「此刻之后不会立即有更多数据」：它用 final 模式把顶到末端的匹配强制按当前 avail 定稿（匹配本可以更长，但被人为截断），并追加刷新标记；故 Flush 必须结束当前匹配。

### 2.2 重叠回指的解压（语义 2）

块复制 `copy(out[k:k+L], out[k-D:])` 在 L>D 时会读入「尚未被复制」的新位置（Go 的 copy 按字节序实现，语言也不保证它的重叠语义）。
正确做法是逐字节循环：每写出一个字节都从「当前输出倒数第 D 个」读取，于是 D=1 产生整段重复，D=3 产生 abcabc… 的周期展开——这正是 LZ77 的复制规则。
成本分析：展开 L 个字节本身就必须产生 L 个输出，逐字节循环相对块复制不增加渐近成本（同为 O(L)），只少常数；输出总量又被 MaxOutput 封顶，故可接受。实现固定距离 `win.At(D)` 逐字节 append，天然正确。

### 2.3 两种「距离过大」必须区分（语义 7）

dist>WindowCap：历史早已滑出环形缓冲，按格式定义这个回指永远无法满足，是结构性损坏；压缩器只向窗口内（最近 WindowCap 字节）插入哈希链位置，Find 返回的 dist≤WindowCap，故正常压缩器绝不可能产生它。
dist>已输出但 dist≤WindowCap：窗口装得下，只是流还没走到那个位置，典型是流被错位/截断后的误解析，语义不同，单独报错。
校验顺序：0 → 超窗口 → 超已输出 → 长度超上限。并行块的预置字典就是上一块末尾的真实原文；拼接成单一流后，这些字节恰是本块之前的已输出字节，因此「已输出字节数」在全局流里含义不变，解压器不需要知道分块。

### 2.4 并行块压缩的确定性（语义 9）

块 i 的压缩器状态 = 空状态 + 预置 data[max(0,start_i-Cap):start_i] 到窗口与哈希链 + 块内原文。Find 的结果只由「块内字节 + 这些预置字节」决定（2.1 的定稿规则在块尾以 final 运行），与别的块如何压缩、何时完成无关，也不读任何共享状态（每块独立 matcher/window/缓冲）。
输出拼接：流头一次 + 各块的字面量/回指记录顺序拼接 + 末尾一个流尾。块间不需要刷新标记——对解压器而言块边界不可见，回指距离按全局已输出来解释，预置字典保证其合法。收集按块序号排序后拼接，故 workers=1/2/4/8 逐字节相同。

## 3. 复杂度

哈希链每位置最多跟 maxChain 个候选，match.Matcher 的非导出计数器 checked 统计候选总数，上界 maxChain×输入字节数；链按位置环形覆盖窗口内位置，不随窗口整体扫描。
