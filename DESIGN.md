# 设计推导（unified diff 五个包）

## 1. hunk 头零计数时的行号

行号统一为「该 hunk 第一行之前已存在的行数」。b≥1 时它就是 1 基起始行；b=0 时 hunk 不含旧行，
只能数它前面的旧行数（即前一行的行号），开头插入时为 0。

| b | 书写 | a | Apply 的插入点（旧文件字节流中的 0 基行位置） |
|---|------|---|--------------------------------------------------|
| 0 | `-a,0` | 前一旧行的行号，开头为 0 | 第 a 行之后（a=0 即文件最前） |
| 1 | `-a`（省 `,1`） | 该行 1 基行号 | 第 a 行（1 基）本身参与匹配 |
| ≥2 | `-a,b` | 首行 1 基行号 | 从第 a 行起取 b 行匹配 |

新侧 `+c,d` 同理。样例：新文件在开头插入 `y`，旧侧前面有 0 行 → `-0,0 +1`；
在旧行 `b`(第2行) 后、`c`(第3行) 前插入 `X` → `-2,0 +3`；删光唯一一行时新侧前面 0 行 → `-1 +0,0`。

## 2. hunk 合并阈值

阈值是 **2C**：两段改动之间未改动行数 g ≤ 2C 时合并，g ≥ 2C+1 时分开。
依据 GNU diff -U C 每个 hunk 各取至多 C 行上下文。C=0 时阈值 0，中间隔任意未改动行都分离
（若阈值是 2C+1=1，则相隔 1 行也会合并，与 `diff -U0` 矛盾；若阈值是 C，只在 C=0 与 2C 等价，
C=1 时相隔 2 行 GNU 会合并（两边上下文各 1 行恰好相接），阈值 C 却会分开）。测试钉住 g=2C 与 g=2C+1。

## 3. 最短脚本的并列选择

用前向 Myers，`V[k]` 沿蛇前进后更新。同一 d 步 k 的新值由 k−1（下移=插入）与 k+1（横移=删除）汇聚；
取横移（删除）优先：k==-d 或非边界且 `V[k-1]>=V[k+1]` 时 x=V[k-1]+1，否则 x=V[k+1]。
回溯同样横移优先，并据此恢复 `-`/`+` 序列。因此 `a b → b a` 的脚本固定为 `-a`、` b`、`+a`
（先删后插，删除块排在前）。确定性行为：每次运行字节一致；相同行内容比较为值相等。

## 4. 偏移与后续 hunk 的平移

每个 hunk 以记录起点（原行号，0 基）为中心在 ±F 行窗口内寻找使上下文+删除行逐行精确匹配的位置，
取距离最小、并列取最靠前。前一 hunk 实际在偏移 δ 处落地（实际起点−记录起点）后，把该 δ 作为
累计偏移加到后续每个 hunk 的记录起点上再开窗口：前文 hunk 整体平移 δ 行，后续行号也整体平移；
再允许该 hunk 自己 ±F 搜索。逐字节匹配失败 → 上下文不匹配；窗口内无候选 → 超出偏移范围。

## 其他约定

- `lines.Split` 保留行尾（`\n`、`\r\n` 或末行无尾），Join 即原字节。
- 计数器 `steps`：Myers 中蛇与横/斜移动每比较一对行（含逐行 snake）加 1；D 超上限立即返回 ErrTooDifferent。
- 无换行标记：渲染时在旧/新最后一条内容行后输出 `\ No newline at end of file`；
  应用时该标记表示对应侧末行无 `\n`，匹配到文件尾才校验；只改尾换行也生成 hunk（相等内容一删一插）。
- 错误四类可判定：ErrMalformed(*LineError)、ErrContext、ErrOffset、ErrTooDifferent。
- 并发 Store：每个文档一把锁，Apply 在锁内对当前版本快照判定并整体替换，成功版本+1 并追加提交日志。
