推导与结论（DESIGN）
1. 非法单元切分（WHATWG 式）：从起点读首字节 L，按其声明长度 k。第二字节必须是续字节且落在专属区间；若越界，首字节单独成 1 个非法单元（其后字节重解析）——故 F0 90 80 41 的第二字节 90 对 F0 非法→1 个 FFFD 后 41 重解析；而 E0 80 80 中 E0 对 80 非法（1 个），随后两个 80 各自非法，共 3 个。后续续字节若被非续字节打断，前缀整体成 1 个非法单元，打断字节退回重解析；故 E2 82+EOF 是 1 个截断单元。
2. 第二字节区间与吞掉字节数：
| 首字节 | 第二字节合法区间 | 非法时本单元吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1（首字节） |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | （无） | 1 |
| C0 C1 F5..FF | （无） | 1 |
其余已收续字节在被非续字节打断时随前缀一起吞掉（退回打断字节）；EOF 时残留前缀整体成 1 个截断单元。
3. 缓存：合法前缀最长是 F0 的 3 个待收续字节，故硬上限 3。看到第二字节越界可立刻判首字节非法；等待第 3/4 字节时，任何非续字节立刻判前缀为 1 个非法单元并退回该字节，不必等流结束。UTF-16 最长残留 1 个高代理（外加 1 个奇数字节）。
4. par 对齐：worker i 只处理 [S_i,E_i) 内首字节落在该区间的单元。起点 S_i 向回最多扫 3 字节（UTF-8；UTF-16 为 2+1）找到可重解析起点 S'：从 S_i 向上，续字节跳过，首个非续字节若能和右侧组成序列则纳入。例如 C3|80 80：从切点回看纳入 C3，C3+80 合法，token 首字节归上一段；80 被退回重解析为非法。|C3 80 80 同理。token 可越过右边界解析（用末尾哨兵），但只有首字节在段内才输出，故非法单元计数与单流恒同；末尾只追加哨兵而不 Close，真实截断由末段 Close 检测。回看/越界都是 ≤3 常数，检查次数 ≤ n+8c。
5. 上限断点：Write 在取一个完整单元前先判输出空间；不够则在标量边界停下，返回该单元首字节在 p 中的下标（不消费它）。若单元首字节在内部 carry 中，返回 0，调用方改喂 Pending()+p[ret:] 给新实例；残留在 carry 的字节从未在本次 Write 返回为已消费。拼起来等价：新实例从该单元首字节重放。
错误四类：ErrIllegal（带偏移/长度）、ErrTruncated（带偏移）、ErrLimit、ErrClosedWrite；用 errors.Is 区分；非法与截断为具名 *Error。
