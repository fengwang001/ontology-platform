# DESIGN

## 推导与结论

1. **空行与单列空值**：空行（`\n\n` 或开头/末尾的空行）跳过，否则它无法与“一条仅含未引号空字段的记录”区分。单列里要表达空值必须写 `""`；若写空串，回写结果就是空行，再次解析会被跳过而丢记录。多列中的未引号空字段由逗号定位，不受影响。回写器在单列且值长度为 0 时必须加引号；此外为保持解析出的引号标记，`Quoted=true` 也必须回写引号。规范输入定义为：非空记录均以 `\n` 结束；无空行；引号只在内容含 `,`、`"`、`\r`、`\n`，或单列空值时出现；`"`  doubling；引号字段保留其中原始 `\r\n`。
2. **并行切点**：每段从切点前 1 字节分别以“引号外”和“引号内”两种状态试跑。该预热字节把状态细化成普通字段中、引号中、转义引号后、CR 待定等；随后只处理本段一次。第一段真状态为引号外；之后若前段选中状态在引号内（含刚见一个引号）就选引号内假设，否则选引号外假设。段首未闭合字段作为尾字段与前段尾字段拼接，取最早起点和 OR 的引号标记。记录号、字段号用切点处计数初始化；字节偏移直接使用全局偏移，故错误无需二次换算。预热只增加 K 个以内重复字节，双假设总量 ≤2N+2K。
3. **CR 待定**：引号外见到 `\r` 进入 CR 待定，不立即落字段或结束记录；下一字节是 `\n` 才按记录结束，否则报孤立 CR。流式 `Feed` 自然保留该状态，因此可跨调用。并行切点在 `\r`、`\n` 之间时，后段预热的正是该 `\r`，选中后以 CR 待定状态消费 `\n`。流在 `\r` 结束时 `Close` 报孤立 CR。
4. **字段上限的立刻性**：字段计数包含所有成为字段值的字节；引号字段中 `""` 两个输入字节只输出一个 `"`，但在逐入两个引号时各计 1，并在第二个导致超长的输入字节处报错。引号本身不计入字段值长度。逗号、记录结束和未成为值的预热字节不计入。记录字段数在产生字段分隔时立即检查；记录数在记录真正闭合时立即检查。

## 状态转移表

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| S 字段开始 | 落空字段，留 S | 错误：裸引号 | 入 CR | 空行：留 S | 入 B |
| B 未引号字段 | 落字段，留 S | 错误：裸引号 | 入 CR | 落字段并结行，留 S | 追加，留 B |
| Q 引号字段 | 追加逗号 | 入 E | 追加 CR | 追加 LF | 追加，留 Q |
| E 引号后 | 落字段，留 S | 追加引号，入 Q | 入 CR（字段已定） | 结行，留 S | 错误：引号后杂符 |
| C CR 待定 | — | — | — | 落字段/结行，留 S | — |

C 状态只等待下一字节：下一字节非 `\n` 即在该字节报孤立 CR；EOF 报孤立 CR。引号字段内不进入 C，CR 始终是内容。字段闭合后的 `\r\n` 与未引号字段共用 C。
