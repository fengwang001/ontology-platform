# 差分数据同步：校验与增量传输器设计

## 1. 两级校验和的分工推导

源端与目标端各持有一个**记录集合**：记录为 `(Key, Value []byte)`，按键排序后序列化为字节流，
随后按定长块切分。匹配采用 rsync 式滑动窗口。

- 弱校验和（滚动）：`a = Σxi mod M`，`b = Σ((L-i)*xi) mod M`，`M=65521`，`weak=(a<<16)|b`。
  加法型结构可在 O(1) 内随窗口滑动更新，用于在每个偏移快速筛选候选块。
- 强校验和：SHA-256（256 bit），仅在弱校验命中候选时计算一次并与签名逐一比较，做最终确认。

**不能只用弱校验和**：加法型弱校验空间小、碰撞易构造。碰撞会让源端一段不同数据被误判为
目标端已有块，生成"错误复用"指令；目标端照常拼装，结果与源不一致且无任何报错——静默损坏。
**不能只用强校验和**：强哈希不可滚动更新，若在源的每个偏移都从头算一次 SHA-256，
总代价为 O(数据长度 × 块大小)，滚动算法退化为暴力匹配，失去意义。故必须弱筛强认。

碰撞实例（块大小 3，M=65521）：`[3,0,0]` 与 `[1,1,1]` 均有 a=3、b=9，弱值相同，
强校验和不同，匹配必须被拒绝。

## 2. 滚动推进公式

窗口 `xi..x(i+L-1)`：`a = Σxj mod M`，`b = Σ((L-(j-i))*xj) mod M`。
移出最左 `xi`、加入最右 `x(i+L)` 时：

```
a' = a - xi + xnew
b' = b - L*xi + a'
```

所有运算对 M 取模并归一化到 `[0,M)`。推进结果必须与在 i+1 处从头计算逐字节相等。
开销：初始化与每次推进的基本运算由计数器记录，总计 ≤ 4N。

## 3. 签名与补丁指令格式

记录集合序列化：每条记录写 4 字节大端 Key 长度、Key、4 字节 Value 长度、Value；Key 唯一、
整体按键升序；空集合为空字节串。

签名（目标端生成）：头部 `SIG\n` + 4 字节块大小 + 4 字节块数；每块 4 字节 weak + 32 字节 SHA-256。

补丁指令两类：`COPY idx`（复用目标第 idx 块）、`DATA n`（后随 n 字节新数据）。源端扫描：
当前偏移弱值命中签名 → 算一次强哈希 → 候选全部不一致则窗口右移 1 字节（懒惰合并连续 DATA）；
一致则发 COPY 并前进一个块长；扫描结束补尾。
补丁文件：头部 `PTCH` + 4 字节块大小 + 32 字节期望结果哈希 + 4 字节指令数 + 4 字节数据段长度，
随后数据段（各 DATA 负载按序拼接）、指令段（每条 1 字节操作码 + 4 字节参数：COPY 为块号，
DATA 为长度），末尾 4 字节对之前全部字节的 CRC-32（IEEE）。

## 4. 复杂度与确定性

滚动总基本运算 ≤ 4N；强哈希每发生一次弱匹配只计算一次（缓存当前窗口哈希），
故强计算次数 ≤ 弱匹配次数，由计数器断言。匹配表为 weak→块索引列表，冲突全部强比较。
算法无随机依赖：指令与数据均顺序构造，同一输入 20 次生成补丁逐字节相同；
状态均为函数内局部变量，多协程并发互不干扰。

## 5. 故障与原子应用

补丁解析先对整份文件校验再落地，任一失败目标文件保持原状（不写任何字节）：

| 截断区间（截断后长度 k） | 分类 |
| --- | --- |
| 0..45 | 头部不完整 ErrHeader |
| 46..46+8n-1 | 指令不完整 ErrTruncated（指令段） |
| 某条 DATA 取数越过数据段 | 数据段不完整 ErrData |
| 完整内容但末尾 CRC 缺失/不符 | CRC 不匹配 ErrCRC |

指令校验：COPY 块号 ≥ 块数 → ErrIndex；DATA 长度累加超出数据段 → ErrData。
应用流程：解析 → CRC 校验 → 在**内存缓冲**重建全部字节 → COPY 的目标块逐一强校验
（签名与目标数据不符时块被改动，ErrCorrupt）→ 结果整体哈希比对（ErrMismatch）→
全部通过后写临时文件并 rename 覆盖目标。任何失败均不落盘，目标逐字节不变。
块大小为 0 返回 ErrBlockSize。错误哨兵均定义在 verify 包，四类截断错误可用 errors.Is 区分。
