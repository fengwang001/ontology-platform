# 连接顺序代价模型设计

## 1. 范围与结构

输入是进程内存中的只读目录：表、行数、列的相异值数（NDV）、等宽直方图和连接谓词。
包职责为：`stats` 估计选择率，`catalog` 登记并校验元数据，`cost` 估计扫描/连接代价，
`plan` 用子集动态规划选计划，`explain` 渲染计划，`cmd/demo` 做无参数自检。

计划是二叉树：叶子为表扫描，内部节点为二元连接。表用稳定 ID 表示；展示、并列比较和
异常信息都使用用户提供的表名。登记后目录不可变，所有选择只读取目录与统计，因此可并发复用。

## 2. 并列打破

计划先按估计代价比较。float64 不能用朴素 `==` 判定数学等号：同一和式的不同结合顺序可能相差
一个 ULP，枚举顺序变化会改变胜者。判定为：

```text
scale = max(1, |costA|, |costB|)
equal := |costA-costB| <= scale * 1e-12
```

阈值依据：本模型所有输入是非负行数，单步最大相对误差来自少量 IEEE-754 基本运算，量级约
`1e-16`；`1e-12` 至少保留四个数量级的舍入余量，同时远小于 0.1% 的真实代价差，不会吞掉实际
更优计划。判等后，对计划叶子做从左到右遍历，比较参与表名序列，选择字典序较小者；序列长度
相同。若仍相同，用子集切分位掩码给出确定结果，但正常不同树形不会走到这里。

## 3. 基数与选择率

扫描基数为目录当前行数。若统计声称行数与目录行数相差超过 10%，视为过期，按目录行数校正。

等值连接 `R.a = S.b` 使用：

```text
sel = 1 / max(NDV(a), NDV(b))
card = maxRows(scan(R), scan(S)) * sel
```

同一对输入有多个谓词时，选择率相乘，并在该连接标注“独立性假设”；这只在谓词列没有函数依赖
证据时成立。若列统计缺失，该谓词选择率回退为固定 `0.1`，节点标注“估计不可靠”，不报错中止。

范围/比较谓词使用等宽直方图：把比较常量映射到桶，按桶内均匀分布估计比例；等值谓词也可用
`桶内行数/总行数` 与 NDV 估计融合。中间结果只保存基数、输入 NDV 和警告，不访问任何行数据。

## 4. 代价模型

扫描代价等于扫描基数。连接代价为两个输入代价之和，加上本次连接输出基数；笛卡尔积选择率为
`1`，故输出基数等于两输入基数之积。该模型偏好尽早使用高选择率谓词。行数为 0 时结果基数为
0；行数为 1 会自然降低选择率和代价。同表两侧的自连接在本版本明确拒绝，返回可判定错误。

## 5. 子集 DP 与笛卡尔积

对非空子集 `S` 保存一个最优计划。单表子集直接生成扫描。大小至少 2 的子集枚举非空真子集
`A`，取 `B=S\\A`，强制 `min(S)∈A` 以避免左右镜像重复。

普通连接要求存在至少一个一端在 `A`、一端在 `B` 的谓词，因此只要存在可连接切分，就绝不选择
笛卡尔积。若谓词图恰有两个连通分量，只有 `S` 是全部表且 `A/B` 正好是两个分量时允许一次
笛卡尔积；它必然是根，即最后一步。三个及以上互不连通分量无法用一次二元连接串成一个关系，
返回可判定错误。

非空子集数上界 `2^n-1`；划分数上界 `(3^n-2^(n+1)+1)/2 < 3^n`，均远小于 `n!`。包内计数器
记录实际考察数。基数估计调用另由计数器记录，且没有行扫描接口。

## 6. 统计异常、上限与边界

三类哨兵错误可用 `errors.Is` 区分：`ErrStatsCorrupt`（桶计数和不等于声称行数，或桶边界不递增）、
`ErrStatsStale`（行数偏差超过 10%）、`ErrStatsMissing`（规划时需要但没有列统计）。缺失和过期
是可规划的软异常：错误可判定，同时计划带警告继续；损坏是硬错误，错误标明表和列。

另定义零表、自连接、不可连通和中间计划数超限的可判定错误。默认中间计划硬上限为 4096；
n=20 会在远早于 `2^20` 个计划时停止并返回 `ErrIntermediateLimit`，不预分配指数级切片。
