# DESIGN: 严格模式流式 Base64

## 规范尾部的推导（统一规则）

每 3 个输入字节拼成 24 bit，再切成 4 个 6-bit 字符 c1c2c3c4。
输入字节数模 3 的余数只可能是 0/1/2，因此一个完整 4 字符组只有三种形态：

- 余 0：`c1c2c3c4`，24 bit 全部承载数据，无 `=`。
- 余 1：`c1c2==`。数据只有 8 bit：c1 的 6 bit 全用，c2 只用其高 2 bit，
  低 4 bit 在编码时被补零。规范要求这 4 bit 必须仍为 0，
  即 `c2 & 0b001111 == 0`。
- 余 2：`c1c2c3=`。数据有 16 bit：c3 只用其高 4 bit，低 2 bit 被补零。
  规范要求 `c3 & 0b000011 == 0`。

统一规则：**填充组中，最后一个数据字符里不属于任何输入字节的低位补零比特，
必须全部为 0。** 判定只用到这几个比特：`==` 组查 c2 的低 4 位，`=` 组查 c3 的低 2 位。

`QR==`：Q=16、R=17，R 的低 4 位为 0001，非零；这 4 bit 不承载任何字节数据，
宽松解码器丢弃它也得到 `A`，但 `A` 的规范编码只能是 `QQ==`（c2=16）。
若接受非零尾位，则 `QR==`、`QS==` 等多个字符串都解码为 `A`，
`Encode(Decode(y)) != y`，违反「对任意字节串，编码结果唯一」与严格往返。
同理 `QUJ=` 中 J=9 低 2 位为 01，拒绝；规范的 `AB` 是 `QUI=`（I=8）。

## 七项语义与测试对照

| # | 语义 | 测试函数 |
|---|------|----------|
| 1 | 标准字母表、`=` 填充、76 字符 MIME 换行且末行无换行 | （待补） |
| 2 | 规范尾部（上表样例）与空输入 | （待补） |
| 3 | 换行只在组间、仅 MIME 模式允许 | （待补） |
| 4 | 五类错误可区分且带字节偏移 | （待补） |
| 5 | 任意切分点（含 `\r|\n`、`=|=`）结果一致 | （待补） |
| 6 | 双向严格往返，MIME 开/关 | （待补） |
| 7 | 输出上限：组边界停下、保留完整组、终态 | （待补） |

计数器（被检查字节数 == 输入字节数）由单独测试钉住。
