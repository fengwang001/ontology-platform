推导与结论
================

1. 非法单元切分规则（由七样例反推）
读首字节 b0 得期望长度 L（2/3/4）。第二字节必须落在 b0 专属区间；
越界则 b0 单独成一个非法单元，该第二字节按下一个新单元重新解析。
第二字节合法才继续；此后每个后续字节只要求落在 80..BF，一旦看到非
续接字节，已经收下的「b0..最后一个续接字节」整体算一个非法单元
（长度 < L），看到的非续接字节重新解析。流结束时同理：已缓存前缀
整体一个单元。代入样例：
- F0 90 80 41：90 在 F0 区间，41 非续接 → F0 90 80 一个单元 + 41。
- E0 80：80 不在 E0 区间 → E0 一个；80 是游离续接字节，一个。
- ED A0：A0 不在 ED 区间 → ED 一个；A0 游离、80 游离，共三个。
- C0 AF：C0 非法首字节一个；AF 游离一个。
- F4 90：90 不在 F4 区间 → F4 一个；其余 3 个续接字节各一个。
- E2 82 截断：82 合法，残留 E2 82 整体一个，不是两个。
- 80 80：两个游离续接字节，各一个。
与 WHATWG「最大合法子前缀 + 每个残余续接字节各一个替换」一致。

第二字节合法区间与非法吞除：
| 首字节            | 第二字节区间 | 第二字节非法时吞掉 |
| C2..DF            | 80..BF       | 1（仅首字节）      |
| E0                | A0..BF       | 1                  |
| E1..EC            | 80..BF       | 1                  |
| ED                | 80..9F       | 1                  |
| EE..EF            | 80..BF       | 1                  |
| F0                | 90..BF       | 1                  |
| F1..F3            | 80..BF       | 1                  |
| F4                | 80..8F       | 1                  |
| 80..BF（游离）    | 无           | 1（其自身）        |
| C0 C1 F5..FF      | 无           | 1（其自身）        |
第二字节合法后、第 3/4 字节非续接时：吞掉已收的全部前缀（2 或 3 字节）。

2. 缓存上限与提前判定
只需缓存「b0 + 已确认的续接字节」，最多 3 字节（4 字节序列在拿到第 4
个续接字节时必已完成，不进缓存）。第二字节一越界即可立刻判 b0 非法、
立即吐回该字节重解析，无需等流结束；只有第二字节合法而后续未凑齐时
才必须跨 Write 等待。首字节后的每字节恰检查 1 次，重解析只发生在被
吐回的「新首字节」上，故总检查 ≤ 2N。

3. par 切点对齐
段 i 不从切点盲解析。顺序决定每段的「种子」：顺序扫描时，落在切点仍
未决的只有一段结构合法前缀（UTF-8 ≤3 字节、UTF-16 ≤2 字节）。做法：
段 0 正常启动；段 i 以「上一段结束时仍处于等待的前缀」为种子回退到
输入中，并继承解码状态（need、b0、第二字节是否已合法）。该前缀在上
一段不输出、不计数；在下一段被完整检查一次。非续接字节绝不可能留在
种子里（第 2 条保证它当场吐回），所以 C3|80 80：种子 = C3，下一段
解析 C3 80 成合法 é，再 80 一个非法单元；非法单元数与单线程相同。
种子长度有界 ⇒ 检查次数 ≤ N + 8·常数。

4. 第 9 条断点
Write 在发每个完整标量前先看输出上限；放不下则在发之前停下，错误为
ErrLimit（非终态）。Write 返回 n = 已被「已输出标量」消费的字节数；
处于等待缓存中的合法前缀字节不计入 n，但仍留在解码器内部，调用方从
输入[n:] 续传到新实例前必须先补喂 Pending()（或直接用同一实例继续
Write——两种方式结果逐字节相同）。ErrLimit 不损坏已输出内容。

UTF-16：单元为 code unit（2 字节）；高代理后非低代理时，高代理一个
非法单元，该非低代理 code unit 立即重解析。奇数残留与残留高代理在
Close 时报 ErrTruncated，与 ErrInvalid 区分。
BOM：仅流首 EF BB BF / FEFF 参与判定或按配置丢弃；中间 U+FEFF 原样。
