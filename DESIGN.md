# DESIGN — 自定格式流式 LZ77（包 ontology）

## 流格式记录布局（wire）
魔数 `OLZ` + 版本 `1`。所有整数均为小端变长整数（LEB128，最多 10 字节；第 10 字节仅允许最低位，否则超长/溢出）。

| 记录 | 布局 |
|---|---|
| 流头 | `"OLZ1"`（4 字节定长） |
| 字面量段 | tag=0，随后 LEB128 长度 n，再 n 个原文字节 |
| 回指 | tag=1，LEB128 距离 d（d>=1），LEB128 长度 l（l>=3） |
| 刷新标记 | tag=2（单字节），表示此前累计输入可被独立还原 |
| 流尾 | tag=3，LEB128 原文总长度，LEB128 CRC32(IEEE)，其后禁止再有字节 |

## 推导 1：为什么输出与 Write 切法无关
压缩器持有一块“未决缓冲”，容量无界增长直到下一次 `Flush`/`Close`：每次 `Write`
只追加字节，不立即发记录。压缩在整块未决数据上做一次确定的贪心扫描（同一起点、同一
窗口状态、同一哈希链只取决于历史内容），故输入相同则记录序列相同，与切点无关。
Flush 强制把未决数据全部扫描输出并补 tag=2；因为此刻已确知“后面没有字节”，当前匹配
必须在边界结束，边界之后（下次 Write）另起新匹配，但仍允许回指边界前的历史窗口。
若两次 Flush 间无新输入，未决缓冲为空：头只在首个非空事件惰性写出，第二次 Flush 不写字节。

## 推导 2：重叠回指的复制
块复制 `copy(out, out[len-d:])` 当 d<l 时，Go 的 copy 也是逐字节推进，源随目标增长，
因此对重叠区间天然产生周期重复语义（与 memmove 相同），本身正确。为完全不依赖该细节、
且让炸弹上限能在“写出前”判定，实现先校验 l<=剩余配额，再按 `copy(dst, src)` 分批，
每批至多 d 字节（首次批长 min(d, 剩余)），后续批次的源含刚写出的字节。复杂度 O(l/d) 次
调用；d=1 的最坏情形仍 O(l)，而逐字节本来就是输出量的下界，可接受。

## 推导 3：两类“距离越界”为何分开
`d > 已输出字节数`：流引用了尚不存在的历史，说明流损坏或预置字典缺失（ErrDistOutput）。
`d <= 已输出但 d > 窗口容量`：历史确实存在，却超出双方约定的可引用范围（ErrDistWindow）。
压缩器只在把字节加入窗口后才对其建哈希候选，且取字节时窗口拒绝 d>容量，故正规流不可能
产生后者。并行块以“上一块末尾 W 字节”预置窗口与历史计数：对解码器而言这些字节仍是逐块
真实输出的一部分，“已输出字节数”的含义不变（它包含本块之前全部块的字节），所以判定
规则对单流与块流完全一致；每块边界发 tag=2 衔接，块尾不发流尾。

## 推导 4：并行块压缩的确定性
块 i 的压缩只读取本块原文与块 i-1 原文末尾 W 字节（预置字典），不读取任何块的压缩结果、
共享可变状态或 goroutine 完成顺序；worker 各自新建独立 matcher/window。块 i 的记录仅依赖
这两份不可变输入，故 workers=1/2/4/8 产出的字节逐字节相同。主 goroutine 按索引收集结果
后顺序拼接：流头一次，随后每块记录，每块后追加 tag=2，最后统一写 tag=3+总长+校验和。

## 复杂度
每位置沿哈希链最多考察 ChainLimit 个候选，候选计数上限 ChainLimit×输入字节；窗口定容，
全同字节在每个位置的考察数恒定，不随窗口/规模增长。CRC32 在压缩与解压两侧各算一次。
