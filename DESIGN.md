# LZ77 流式压缩设计推导

## 流格式记录布局（除魔数/版本外一律 uvarint，LEB128，至多 10 字节）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `0x9A 0x01`（魔数、版本，各 1 字节定长） |
| 字面量段 | `tag=(len<<2)|0`，随后 `len` 个原始字节 |
| 回指 | `tag=(len<<2)|1`，随后 uvarint `dist` |
| 刷新标记 | `tag=2`（单字节，无负载） |
| 流尾 | `tag=3`，随后 uvarint 原文总长、uvarint FNV-1a64 校验和 |

tag 低 2 位为类型（0 字面量 / 1 回指 / 2 刷新 / 3 流尾），高位为长度参数。

## 推导一：压缩结果与 Write 切法无关（语义 3）

位置 p 上的编码决策分两类。字面量决策：p 处是否存在长度 ≥3 的匹配，
只取决于 `data[p:p+3]` 与已固定的历史 `data[:p]`；只要缓冲里已有 ≥3 个
未决字节（lookahead），该决策即终局，后续输入不可能推翻。匹配决策：
最长匹配可能延伸到当前缓冲末尾，若 `best == len(lookahead)`，匹配尚未
"闭合"——下一批输入可能让它更长，且更长的 lookahead 可能让链上靠后的
候选反超（A 候选在旧边界处并列最优但先失配，B 反而更长）。

结论：编码器缓存全部未决字节（pending），仅当匹配被"失配字节闭合"
（`best < len(lookahead)`）或 Flush/Close 时才发出回指；字面量只在
lookahead ≥3 且无匹配时发出。由于决策只依赖"截至某偏移的输入前缀"，
与 Write 如何切分无关，输出逐字节相同。代价：开放匹配期间 pending 无界
增长（最坏为全同字节流，O(n) 内存），这是切法无关性的必要代价；重新
查询时匹配器从同一位置重走哈希链，结果与一次性输入完全一致。

对 Flush 的推论：Flush 承诺"还原截至此时的全部输入"，而开放匹配的
字节尚未发出，故 Flush 必须以当前长度强制闭合匹配（等价于在该偏移处
临时令 final=true），再发刷新标记。Flush 不清窗口，故 Flush 之后的
匹配仍可回指 Flush 之前的数据。无新输入时 pending 与字面量缓冲均为空，
第二次 Flush 不写任何字节。

## 推导二：重叠回指的解压复制（语义 2）

距离 dist < 长度 len 时，回指引用的源区域与目标区域重叠：第 k 个输出
字节（k ≥ dist）复制的是本次回指自己刚写出的字节。一次性块复制
（`copy`/`memmove` 语义，源在复制前快照）会把未写出的区域读成旧值，
结果错误。逐字节向前复制天然正确：每步从窗口取"距当前末尾 dist"的字节，
写出后立即成为后续步骤的源。

本实现保留逐字节循环：环形窗口使每步 O(1)，总代价 O（输出长度），而
输出本身就要 Ω（输出长度），故没有渐进劣化；4 MB 最坏情形为毫秒级。
若要块复制，需按 `min(dist, 剩余)` 分块翻倍拷贝并特判边界，复杂度
不值得。结论：逐字节循环在此可接受，且是语义定义本身。

## 推导三：两类距离错误为何区分（语义 7）

"距离 > 已输出字节数"是流本身的逻辑错误（引用不存在的过去）；
"距离 > 窗口容量"是实现能力错误（窗口太小装不下该距离）。前者与
配置无关，后者随解压器窗口配置变化，合并会掩盖成因，故分两个哨兵
错误。压缩器侧：匹配器只在窗口现存字节中搜链（`dist ≤ win.Len()`），
而 `win.Len() ≤ cap`，故合法压缩器绝不可能产生"距离 > 窗口容量"，
该错误只会来自损坏流或解压器配置小于编码器窗口。

并行块压缩中，预置字典 = 上一块末尾 ≤cap 字节，这些字节在拼接后的
流里就是普通的"已输出字节"，解码器窗口内容与单流压缩完全一致，故
"已输出字节数"的含义没有变化，两类检查无需特判。

## 推导四：并行块压缩的确定性（语义 9）

块 i 的压缩输出 = f（块 i 原文， 块 i-1 末尾 ≤cap 字节， 配置）。三者都在
切分前确定，不依赖任何其他块的压缩结果、goroutine 调度或完成顺序；
workers 只改变计算的并发度，不改变每个 f 的输入，故 workers=1/2/4/8
输出逐字节相同，30 次重复亦相同。衔接：各块记录直接按序拼接，块间
**不需要**刷新标记——回指距离跨越块缝是连续的（字典即前块末尾），
解码器看到的就是一条逻辑流；流头只写一次，流尾的总长与校验和覆盖
全部原文。空输入时无块，输出 = 流头 + 流尾（0, seed)，仍是合法非空流。

## 复杂度

匹配器为每个位置建立一次哈希链节点（O(n)），每次查询最多考察
maxChain 个候选，故考察总数 ≤ maxChain × n，与窗口容量、输入规模
无关；最优候选提前命中（best 已满 lookahead）即停止。非导出计数器
`examined` 记录考察的候选位置总数，由 match 包内测试钉住。
