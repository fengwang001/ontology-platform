# DESIGN — 差分数据同步的校验与增量传输器

## 1. 弱/强校验和的分工推导

- 弱校验和采用纯加法型：`W(b) = Σ b[i] (mod 2^32)`。它可以在 O(1) 内滚动推进，
  但碰撞极易构造：块 `[1 2 3 4]` 与 `[4 3 2 1]` 的和都是 10，内容却完全不同。
- **只用弱校验和**：碰撞时源端会把目标端内容不同的块误判为"可复用"，补丁应用后
  结果与源端不一致，而且全程无任何报错——静默产生错误数据，不可接受。
- **只用强校验和**（SHA-256，取前 16 字节）：正确性没问题，但强校验和无法滚动，
  每个偏移都要对整整一个块重算一次 SHA-256，代价是 O(L·bs)，滚动窗口失去意义。
- **分工**：弱校验和做滚动快速筛选（每个偏移 O(1)），只有弱命中时才计算一次强
  校验和做确认。于是强校验计算次数 ≤ 弱命中次数，总代价仍是 O(L) 级别，且碰撞
  会被强校验挡住，错误复用不可能发生。

## 2. 滚动推进公式与运算量上界

- 定义 `S(i) = Σ_{k=0..bs-1} data[i+k]`，则 `S(i+1) = S(i) − data[i] + data[i+bs]`。
- 初始窗口 bs 次加法，之后每次推进 2 次基本运算（一减一加）。长度为 L 的数据
  全程基本运算次数 = bs + 2·(L−bs) = 2L − bs ≤ 4L。若每个偏移从头重算则是
  L·bs 量级。`chunk` 包内非导出计数器记录基本运算次数，测试断言 ≤ 4·L。

## 3. 签名与补丁的线上格式

- 签名（sig）：`magic "ONS1"(4) | blockSize(4) | count(4) | targetHash(16)`，
  随后每块 `weak(4) | strong(16)`，块号即数组下标。
- 补丁（patch）头部 36 字节：
  `magic "ONP1"(4) | blockSize(4) | srcLen(8) | instrCount(4) | srcHash(16)`。
- 指令：`op(1)`；`op=0` 复用 → `blockIndex(4)`；`op=1` 新数据 → `dataLen(4) + payload`。
  连续的新数据字节合并为一条指令。
- 尾部：`crc32-IEEE(4)`，覆盖此前全部字节。

## 4. 截断分类（按解析失败位置判定）

- 剩余不足 36 字节头 → `ErrHeaderIncomplete`；
- 指令定长部分（op / blockIndex / dataLen）不足 → `ErrInstrIncomplete`；
- 数据指令的 payload 不足 → `ErrDataIncomplete`；
- 尾部 CRC 缺失或校验不符 → `ErrCRCMismatch`。
- 四类错误与 `ErrOutOfRange`、`ErrMismatch` 均为独立哨兵错误，可用 `errors.Is` 区分。

## 5. "截断补丁不得部分应用"的实现方式

- `Apply` 是纯函数：先完整解析 + CRC 校验，再做全部块号越界检查，然后在内存里
  重建结果并核对 `srcLen` 与整体强校验和，全部通过才返回新数据；任何一步失败
  返回错误，目标数据零改动。
- `ApplyFile` 在 `Apply` 成功后先写 `target.tmp` 再 `rename` 覆盖目标文件，
  失败路径完全不触碰目标文件，文件层面同样全有或全无。

## 6. 复杂度与传输量

- diff：每偏移 1 次弱查询（O(1) 滚动），强校验只在弱命中时发生；期望总代价 O(L)。
- 补丁新数据只覆盖未匹配区域：源与目标只差中间一个块时，新数据 ≤ 2·bs；
  完全相同时为 0（全部为复用指令）。
