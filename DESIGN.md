# 设计推导

## 1. `x/**` 与 `x`、`x/`（推导过程）

`**` 只在独占整段时生效，语义是「匹配零个或多个完整段」。模式按 `/` 切段，路径也按 `/` 切段，`/` 始终是显式分隔符、永不被折叠。

- `x/**` 要求前缀 `x/` 中的分隔符实际存在；`**` 匹配零段后，消费的是 `x/` 之后的内容。故 `x/**`：不匹配 `x`（缺 `/`），匹配 `x/`（零段，空段视作零个完整段），匹配 `x/y`。
- `**/x`：`x` 匹配（`**`=零段，塌缩 `**/`），`/x` 匹配（`**`=一个空段），`x/` 不匹配（尾段为空不等于 x），`a/x` 匹配。
- `a/**/b` 匹配 `a/b`：`**`=零段时 `**/` 整体取零段展开，相邻分隔符塌缩为一次字面相邻，这是「零个完整段」的唯一特例；塌缩的是 `**/` 组合，不是任意 `/`。`x/**` 尾部没有后续段，零段留下的空段无法被同一条特例吸收，因此仍要求 `x/` 存在。

## 2. 空串路径

- 空模式只匹配空串；`?` 要求恰好一个码点，不匹配空串。
- `*` 是段内元字符：空串切成一段（空段），`*` 匹配该空段零个码点，故 `*` 匹配 `""`；它不匹配 `"/"`（两段空段）。
- 独占整段的 `**` 按段数计数：空串切成一段空段，`**` 匹配零个或多个段，故 `**` 匹配 `""`；`**/**` 不匹配 `""`（需要至少一个分隔符）。

## 3. 无回溯算法

- 段内：单趟扫描，维护 `starPat`/`starTxt`（最近一个 `*` 的回退点）。`*` 只移动模式指针；失配且无星号则失败，否则把模式重置到该星号之后、文本回退点前进一位。
  正确性：维护不变量「已确认前缀的最优匹配中，只需最近的星号」。若某前缀存在匹配，让最靠右的星号多吞一个码点即可重建该匹配，更早的星号不可能做到它做不到的事，因此丢弃更早回退点不漏解。
  该论证不能照搬到 `**`：星号作用域是同一线性段，文本回退点是全序的；而 `**` 的回退点是「段数」，多个 `**` 各自可展开 0..k 段，一个 `**` 多吞一整段会越过 `/` 边界、改变后续段与模式段的对齐，段级选择不存在单一全序回退点，贪心会漏解。
- 跨段：模式与路径都切成段；用段级 DP 集合 `cur`（当前路径段后模式可能到达的段下标），逐路径段转移。普通段要求精确相等（段内用上面的线性算法），`**` 段产生 ε 转移与停留转移。每对（模式段, 路径段）只求值一次，O(模式长度 × 路径长度)。

## 4. 规则集版本一致

每次替换先在锁外把全部规则编译成新的不可变快照；任一规则超限或语法错误则直接丢弃快照、返回错误，旧快照指针不动。成功后临界区内原子替换 `atomic.Pointer[snapshot]`。查询（含 `Explain`）只在入口 Load 一次快照，之后全程只读该快照，因此结论、序号、原文必属同一版本。

## 5. 匹配结果表

| 模式＼路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | 否 | 否 | 是 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `*` | 是 | 是 | 否 | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
