# DESIGN

## 推导

1. 零计数行号：unified diff 的范围是“文件中已有行”的 1 基闭区间；空范围只能锚定边界。开始锚点取前一条已有行号；文件开头前没有行时为 0。因此插入空文件是 `-0,0 +1`，在第 2 行后插入是 `-2,0 +3`。
   - b=0：范围为空；a=插入点前的旧行号，开头为 0。Apply 在第 a 行后插入。
   - b=1：书写 `-a`；匹配并消费第 a 行。
   - b>=2：书写 `-a,b`；匹配从 a 起的 b 行。
   - 新侧同理：d=0 时 c=插入点后的新行号，文件末尾为 0；d=1 省略 `,1`。
2. hunk 合并：每段改动各能向上、下取 C 行上下文。两 hunk 合并后至少保留 C 行中间上下文；故未改动间隔 g<=2C 时合并，g=2C+1 时分开。若阈值取 C，g=2C 会错误分开 GNU 输出；取 2C+1，g=2C+1 会错误合并且只剩 C-1 行间隔边界被破坏。
3. Myers 并列选择：前向填 V 时同 k 先取“向下（删除）”的前驱；反向重建也固定此偏好。`a b -> b a` 因而排列为 `-a`、空格 `b`、`+a`，不是先插入。
4. 偏移传播：后续 hunk 的记录位置先加上此前成功 hunk 的 `+ins-del` 净行数，再围绕新位置查 ±F。只加距离 k 与加净变化等价于用实际提交后的文件坐标，且支持 hunk 自身造成长度变化。

## 结构

- Line 保存不含行尾的 Body 和原 EOL（`\n`、`\r\n`、空），比较用整行原文，空串切成 0 行。
- hunk 内操作保留旧侧/新侧索引和 no-newline 标记；严格解析按声明计数消费。
- Apply 先模拟全部 hunk，再一次性提交；解析、上下文、距离、差异过大四类错误可判定。
