# 并行任务图调度与失败传播 — 设计推导

## 1. 包划分（5 个核心包，依赖单向无环）

- `graph`：建图、幂等去重边、依赖存在性校验、环检测（DFS，报真实闭合路径）、Kahn 拓扑分层。
- `sched`：就绪队列（同就绪按 ID 升序的小顶堆）、并发度上限、活跃计数、就绪判定计数。
- `exec`：单任务执行器，`recover` 捕获 panic，监听 `ctx.Done` 上报取消。
- `fail`：编排引擎（Run），串起 graph/sched/exec，负责传播、取消、根因计算。
- `report`：状态枚举、`Report`/`TaskResult`、原因类型、按 ID 字典序的确定性渲染。

## 2. 四类最终状态的判定推导

失败发生时，把所有任务按「与失败时刻的关系」切成互斥四类：

1. **成功 Succeeded**：失败发生前已 `nil` 完成。状态只允许 Pending→Running→Succeeded 单向迁移，
   任何后续信号（取消、迟到写回）都被 CAS 拒绝，故成功不被回滚、不改标。
2. **失败 Failed**：真正执行过且返回 error（含 panic 转换的 error，错误信息带 `[panic]`）。
   多任务可同时失败，全部保留；并发期间收到的首个错误仅决定取消时机，不遮蔽其余失败。
3. **跳过 Skipped**：失败发生时**尚未开始**（状态仍是 Pending）的任务。
   - 下游（存在失败祖先）：原因 = 其失败祖先集合中层数最小、同层 ID 最小者（结构根因，确定性，
     不依赖完成时序）。故 `A→B→C`、A 失败时，C 的原因指向 A 而非直接上游 B。
   - 快速失败模式下无失败祖先的独立 Pending 任务：原因 = 全图「首个」失败任务（同序：层,ID），
     Reason 标记为 `Aborted`，与依赖型 `DependencyFailed` 可由原因类型区分。
   - 尽力而为模式下不满足跳过条件的 Pending 任务继续执行，不会被跳过。
4. **取消 Canceled**：失败发生时**已经开始**（Running）的旁支任务。引擎 `cancel()` 其 context，
   exec 监听到 `ctx.Done` 后回传 canceled 事件。区分依据是**是否曾经开始执行**：
   Running→Canceled 与 Pending→Skipped 是两条不相交的迁移，报告可逐任务区分。

迟到写回：任务被取消后若无视 ctx 仍回传结果/错误，引擎发现其状态已是终态即丢弃，
不改变 Canceled 状态。

## 3. 快速失败 vs 尽力而为

- `ModeFailFast`（默认）：第一个失败落定即 `cancel()`：Running 旁支→Canceled；
  其余 Pending→Skipped（根因规则见上）。
- `ModeBestEffort`：不取消。仅「可到达某失败任务」的 Pending 任务永不就绪→Skipped；
  与失败无关的分支继续跑完（可能成功、也可能再失败，全部记录）。无失败祖先的 Pending 不跳过。

根因计算：按拓扑序 DP，`root(v)=v(若v失败)`，否则取 `min_rank(root(父))`，O(V+E)，不全图扫描。

## 4. 复杂度与并发约束

- 活跃任务数 = 弹出数−回收数，`launch` 前断言 `< limit`；记录 `peakActive`（非导出，测试读 accessor）。
- 每次「某任务完成后重算可启动者」沿其出边推进，每个节点入队一次、每条边松弛一次；
  `readyDecisions` 为调度器 `Next()` 判定次数，有界于 O(V+E) ≤ 4(V+E)。
- 主循环单 goroutine 驱动，任务 goroutine 只通过 channel 回传事件；引擎在所有任务 goroutine
  退出后才返回，故无 goroutine 泄漏（无视取消的用户函数除外，属用户违约）。

## 5. 确定性

输出只依赖结构不依赖时序：邻接表/入边表均排序；就绪堆按 ID 升序；根因用 (层,ID) 全序；
报告按 ID 字典序逐行渲染，字节稳定。打乱加边顺序、注入随机延迟均不影响报告。

## 6. 故障与边界

环：DFS 三色标记，回溯出闭合路径（自环即 `[v,v]`），执行前返回错误。
边界：空图返回空报告；缺依赖任务在执行前判定报错；重复边幂等；1000 层链为迭代算法不爆栈；
limit=1 退化为串行，报告内容与并行一致。
