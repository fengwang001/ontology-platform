# 设计推导：并行任务图调度与失败传播

模块 `ontology`，仅标准库，状态全在进程内存。包划分：`graph`（建图/环检测/分层）、
`sched`（调度主循环、并发上限、就绪队列）、`exec`（执行与 panic 捕获）、
`fail`（状态枚举与错误类型）、`report`（确定性报告）、`cmd/demo`（验收演示）。

## 一、失败后四类最终状态的判定推导

设任务在运行结束时只有两种可观测事实：是否启动过、启动后返回了什么。
由此推出四类互斥状态：

1. **已成功 Succeeded**：在首次失败被主循环处理之前结果已到达且 `err == nil`。
   一旦落定为成功，后续任何失败/取消事件都不改写它（终态不可逆，报告只写一次）。
2. **失败 Failed**：结果到达时 `err != nil`，且该错误不是由调度器取消上下文引起
   （即 `!errors.Is(err, context.Canceled)`）。panic 由 `exec.Run` recover 后包装成
   `*fail.PanicError`（`errors.Is(err, fail.ErrPanic)` 为真），同样走失败路径。
   多个任务同时失败时，每个失败结果独立到达、独立记录，报告保留全部失败。
3. **被跳过 Skipped**：直到运行结束都从未进入 Running。两种触发：
   快速失败模式下首个失败落定后所有 Pending 任务（含无关节点）整体跳过；
   尽力而为模式下某节点全部前驱落定且至少一个前驱为 Failed/Skipped 时跳过。
   跳过原因必须指向最初的失败根因而非直接上游：每个失败任务记录根因=自身与
   失败序号（按结果到达顺序编号），跳过沿边传播时继承根因；多前驱场景取失败
   序号最小（并列取 ID 字典序最小）的根因。故 `A→B→C` 中 A 失败时 C 的 cause 是 A。
4. **被取消 Canceled**：失败发生瞬间已经在跑（状态为 Running）的旁支任务。
   与 Skipped 的区分依据就是「是否启动过」：主循环只把 Running 任务当作取消
   候选，Pending 的一律跳过。被取消任务稍后写回的结果必须丢弃：取消生效
   （`aborted == true`）后到达的结果，若是 `nil` 或 `context.Canceled`，一律记
   Canceled 且不采用其返回值；若是任务自身的真实错误则记 Failed（保证同时
   发生的多个失败全部入报告）。

## 二、两种模式语义

- **快速失败（默认）**：首个失败落定 → `cancel()` 广播 → 所有 Pending 记 Skipped
  （cause=该失败根因）→ 在跑任务等结果返回后记 Canceled（或真实错误的 Failed）。
  主循环在在跑任务全部返回后才退出，保证无 goroutine 泄漏。
- **尽力而为**：不取消上下文；失败仅沿后继边传播跳过，独立分支照常调度到完成。
  同一节点在两种模式下可能分别得到 Skipped 与 Succeeded，测试与 demo 均断言
  这种分布差异。

## 三、并发与复杂度约束（可证明）

- 并发上限：仅当 `inFlight < limit` 才从就绪队列弹出并启动 goroutine；`sched`
  内非导出原子计数器记录运行数与历史峰值，测试断言峰值不超过上限。
- 就绪判定次数：只在两处计数——初始扫描每节点一次（共 N）、每条边在前驱落定
  时做一次入度递减（共 E）。总计 `N + E <= 4*(N+E)`，绝无全图重扫。
- 无泄漏：所有 worker 的结果经同一 channel 被主循环接收，主循环在「全部任务
  落定且在跑数归零」前不返回；`Run` 返回时所有 goroutine 已退出。

## 四、确定性

- 报告按任务 ID 字典序输出，状态/错误/原因均为纯文本，与完成顺序无关。
- 就绪队列弹出时总是取当前就绪集合中 ID 最小者（切片保持升序，队首即最小）。
- 边用集合存储（重复加边幂等），遍历前一律排序，故加边顺序不影响任何输出。

## 五、故障注入与边界

- 环：Kahn 分层后若有剩余节点，在剩余子图上做迭代 DFS（灰点回边）还原真实环
  路径，首尾闭合、每条边都来自输入；自环天然命中（节点到自身的灰边）。
- 链式 1000 层：拓扑、环检测、跳过传播全部用显式栈/工作表迭代，无递归。
- 空图直接返回空报告；并发度 <1 归一为 1（串行）；依赖不存在节点时
  `AddEdge` 返回可 `errors.Is` 判定的 `graph.ErrUnknownNode`。
