# 设计推导

## 1. 空行与单列空值

空行（两个行尾相邻）**跳过，不产生记录**。理由：若把空行当作「一个未引号空字段」的单列记录，则空文件与单列里一条空值记录无法区分；而回写器要保证往返，单列空值记录若写为不加引号的空串，输出是零字节（不是一行），再解析回来就是零条记录，数据丢失。因此单列记录唯一字段为空时，回写器**必须写成 `""` 加引号**（「必要引号」的一种：单列 + 空值）。一般列里未引号空字段写出仍为空串，其存在由相邻分隔符/换行保留。

**规范输入**：字段仅在含 `,` `"` `\r` `\n`，或「单列记录唯一字段为空」时加引号；引号写为 `""`；行尾一律 `\n`；文件末尾无换行。解析器接受更多输入（CRLF、非必要引号）。往返保证：解析器产出的表可往返且引号标记相同；规范输入逐字节恒等。

## 2. 状态机（每字节处理一次，不回扫）

状态：`F`字段开始 `U`未引号字段中 `Q`引号字段中 `A`引号内刚见引号 `C`CR待定。输入类别：`,` `"` `\r` `\n` 普通`x`。

| 状态 | x | , | " | \r | \n |
|---|---|---|---|---|---|
| F | U,攒字节 | emit空;F | Q | C | 行尾;F(空行跳过) |
| U | U,攒字节 | emit;F | 裸引号错误 | C | 行尾;F |
| Q | Q,攒字节 | Q,攒 | A | Q,原样攒`\r` | Q,原样攒`\n` |
| A | 引号后杂字符错误 | emit;F | Q,攒一个`"` | C | 行尾;F |
| C | 孤立\r错误 | 行尾;F | 裸引号错误 | 孤立\r错误 | 行尾;F(CRLF) |

`C` 见到下一字节才定性，是续传与切点唯一跨边界的状态；其 `\r` 暂存不落盘。EOF 时：`Q/A`→引号未闭合；`C`→孤立 `\r`；`F` 且本行无字段产出→正常结束不补记录；其余→emit 末字段与末行（无换行结尾合法）。

## 3. 半包续传

`Feed` 只跑转移表；`Close` 按 EOF 规则定性。切在 `""` 之间（`A`）、`\r\n` 之间（`C`）都与整体输入同构：每字节仅一次转移，事件只依赖状态与字节，错误坐标由单调计数器给出，故任意切分逐位相同。

## 4. par 切点

每段在两个假设下各解析一遍（处理次数 ≤ 2N+常数）：
- F 假设：段起点在某条已结束记录之后（`F` 态）。段内第一次行尾（含其跨边界的 `\r` 待定）之前的事件属于上一条跨界记录，丢弃；sync 点 = 该行尾之后。
- Q 假设：段起点位于一个引号字段内部，首字节不是开头的 `"`（必落在左段）。直接按 `Q` 态处理，不丢弃。

段末快照：结束状态、跨界字段已攒长度、当前记录字段数、已完成记录数。段 i+1 真起点由段 i 快照决定：`F`→F 假设；`Q`→Q 假设；`A`：下一段首字节为 `"` 则继续 Q 假设（`""` 跨段），否则与 F 假设一致（闭合后必为 `,/\r/\n`）；`U`→F 假设，而 F 假设首字节为 `"` 即「未引号字段中出现引号」（等价 U 假设）。

坐标换算：段内偏移加段起点；记录号加前段完成记录数；字段号加跨界记录已见字段数；字段长度携带跨界已攒字节（`""` 解为 1 字节，在第二个引号到达时计入）。上限从携带值继续计数，故在与单线程相同的第一个超限字节上报错。EOF 规则只对最后一段 `Close` 时套用。各段 goroutine 产出事件切片，主 goroutine 按快照顺序回放给 table，结果与流式完全相同。

## 5. 上限「立刻」

字段字节数每攒入一个内容字节后立即检查：普通字节 +1；`""` 在第二个引号到达时 +1。字段数每确定一个字段（逗号/行尾）时检查；总记录数每确定一条保留记录时检查。超限立即返回哨兵错误并进入终态，后续 Feed 返回同一错误。
