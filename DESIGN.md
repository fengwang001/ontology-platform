# 设计推导

## 1. 第3条：Write 切法无关

在位置 i 找到匹配时，匹配长度可能继续延伸到调用方尚未 Write 的字节；若立即定长发射，
下一段输入到来后就无法延长它，不同切法会产生不同结果。结论：**压缩器内部缓存全部未决输入**，
Write 只追加缓冲、不做任何编码决定；真正的压缩在 Flush/Close 时对「从上次冲刷偏移到当前末尾」
的完整区间跑一遍贪心 LZ77。于是发射只依赖累计字节流与冲刷偏移，不依赖 Write 边界。
Flush 必须强制结束当前匹配：它承诺冲刷点之前的数据可独立还原，跨过冲刷点的匹配会引用冲刷点
之后才到的字节，违反承诺。Flush 后窗口/哈希链保留冲刷点之前的历史，故后续匹配仍可回指。

## 2. 第2条：距离 < 长度的回指

块复制（如 copy(dst, src)）在源区间与目标区间重叠时按实现选择的方向覆盖，距离 1 长度 30
时源会被刚写出的字节污染或读到旧数据，结果错误。正确语义是**逐字节向前复制**：
`for k := 0; k < length; k++ { out = append(out, out[len(out)-distance]) }`。
每字节是一次定长索引+追加，O(长度) 且无额外内存；解压吞吐本就按输出字节计费，逐字节循环
与一次 copy 同阶，可接受。写之前逐字节做上限检查，天然满足炸弹防护（第8条）。

## 3. 第7条：两种「距离越界」

- 距离 > 窗口容量：格式非法。压缩器只在容量 W 的环形窗口内查表，候选位置距当前位置
  必 ≤ W，故正常压缩器绝不产生。
- 距离 ≤ W 但 > 已输出字节数：引用了尚不存在的数据（常见于损坏/伪造流、块起始处）。
二者检测时机与含义不同，必须分开报错。
并行块预置字典不改变判定：块可回指上一块末尾 W 字节，但这是**同一连续原文**的相邻字节；
输出流是唯一的连续字节流，解码到块内位置时，上一块输出早已在输出缓冲/窗口中，
「已输出字节数」即累计输出长度，判定规则完全不变，也无需任何特殊块标记。

## 4. 第9条：并行确定性

块 i 的压缩输入 = 块 i 原文 + 上一块末尾至多 W 字节作为预置字典；贪心 LZ77 是纯函数，
不读任何共享可变状态：每 worker 自建 window/matcher，预置字典只入窗口与哈希链但不发射。
因此块 i 输出只依赖块 i-1 与块 i 原文，与完成顺序、worker 数无关；按块序号拼接即与
workers=1 逐字节相同。块之间**不需要刷新标记**：块边界不允许发射跨边界引用（预置字典只
延长匹配到本块内），拼接点两侧记录各自合法；只有用户 Flush 处发射 flush 记录。

## 5. 流格式（全部记录为 tag + LEB128 无符号变长整数；小整数 1 字节）

整数用无符号 LEB128；第 10 个 continuation 字节只允许低 1 位，再长/溢出判超长。

| 记录 | 布局 | 说明 |
|---|---|---|
| 流头 | `tag=1, windowCap(uvarint)` | 仅出现一次；魔数固定为 tag 1 |
| 字面量段 | `tag=2, length(n), n 个原始字节` | n>=1 |
| 回指 | `tag=3, distance(d), length(n)` | d>=1，n>=最小匹配(3) |
| 刷新 | `tag=4` | Flush 点；解压器边界，无数据 |
| 流尾 | `tag=5, origLen(uvarint), checksum(uvarint)` | FNV-1a 64 位校验原文；其后再有字节即多余 |

非法 tag、长度 0 字面量、距离 0 等在解析时即判损坏。空输入 = 头 + 尾(origLen=0,
checksum=FNV空基值)，非空合法流。
