# 带背压的流式聚合管线设计

## 不变量

- 每条 source 记录有单调递增位置 `pos`，从 0 开始。
- 每个阶段只有一个 worker；阶段间是容量固定的 Go channel。
- 向上游传 item 的发送操作本身阻塞，因此 channel 满时压力直接传回 source。
- sink 对相同 `pos` 前缀的聚合结果幂等：聚合只做按组累加，输出按 key 排序。

## 为什么 source 位置不能作为已消费位置

source 返回只表示字节已读出；记录可能位于 source 与 parse、parse 与 aggregate、
aggregate 与 sink 的任一队列中，也可能已聚合但尚未落盘。此时若检查点记录 source
位置，崩溃恢复会从下一条开始，所有在途记录永久丢失。

## 为什么 sink 已落盘位置也不能直接作为已消费位置

sink 落盘位置只描述进入输出文件的记录，不描述其前面的全部记录都已离开队列。
若输出按聚合后的组写入而不是按原始记录写入，落盘集合甚至没有单一“记录位置”。
更关键的是：旧输出文件仍存在时，直接从 sink 位置恢复，无法保证检查点与输出文件
属于同一原子状态；先更新检查点或先改名输出，任一中途失败都可能造成重放。

## 正确定义：屏障提交位置

设屏障 `barrier(n)` 表示“处理并提交位置 `< n` 的全部记录”。屏障沿同一条工作链
逐级传递。worker 处理屏障时，先排空自己之前的所有数据 item，再转发屏障；
因此 sink 收到 `barrier(n)` 时，可以证明三个阶段间队列对这些位置均为空。

已消费位置定义为：**最新成功提交屏障的 n，即所有位置 `< n` 的记录都已完成
解析、聚合、进入检查点状态，并且对应输出已原子可见**。

提交必须按下面顺序：

1. source 在边界生成 `barrier(n)`，此后暂停越过该边界的推进。
2. 屏障经过 parse、aggregate；到达 sink 时，所有 `< n` 的数据都已在 sink 内存状态中。
3. sink 将内存聚合状态序列化到临时输出，fsync 后原子 rename。
4. ckpt 将 `{nextPos:n, groups:sink 状态}` 和输出校验和写入新临时检查点，再原子切换。
5. 屏障完成信号逆向/随链返回，source 才允许推进到下一边界。

第 3、4 步之间崩溃：有新输出但检查点仍旧。恢复使用旧检查点，并先删除不属于当前
检查点的输出；随后从旧 `nextPos` 重放。幂等 sink 会重新生成字节相同的输出。
第 4 步后崩溃：检查点与输出都新，恢复不重放。临时文件按固定前缀识别，任何未
rename 完成的文件都不是有效状态，恢复开始时删除。

因此提交点不是“source 读到哪”也不是“sink 随手写到哪”，而是所有在途集合为空后，
输出与检查点按顺序达成的原子逻辑边界。

## 背压与资源上限

- source → parse、parse → aggregate、aggregate → sink 均为有界 channel。
- source 发送前后用 try-send/超时探针无法准确统计；改为：发送若不能立即完成，
  原子增加 blocked 次数后再执行阻塞发送。
- 每个 item 持有一张跨阶段总令牌，入 source 前获取，sink 完成后释放；用信号量
  容量记录全局在途上限。历史最大在途数由进入/退出时更新的峰值计数器保存。
- 分组 map 达到 `MaxGroups` 时返回哨兵错误，管线取消，不静默丢组。

## 恢复

检查点保留当前、上一个两个文件，每个文件含 CRC64。启动时优先当前；校验失败则
尝试上一个，并在报告中置 `FellBack=true`。两者都坏则返回错误，不用空状态冒充。
输出按检查点中的路径与校验和核验；不匹配则删除并从 `nextPos` 重放。

## 优雅停止

`Stop` 关闭 once 保护的 stopCh；正在运行的管线排空已接收数据和屏障后退出。
尚未启动时该信号只令随后的 Run 立即返回 ErrStopped。所有 worker 与 source goroutine
都由 Run 的 WaitGroup 收口，重复 Stop 幂等。
