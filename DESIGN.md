# 多租户配额公平队列调度器 — 设计推导

只依赖标准库；状态全在进程内存；时钟由调用方驱动（提交序号 `Seq` 充当逻辑时钟），
因此调度结果完全确定、可复现。

## 1. 虚拟时间为什么推进 `c/w` 而不是 `c*w`

租户权重 `w` 表示它应得的长期服务速率份额。设租户 i 在墙钟时间内累计执行代价
`S_i = Σ c`，其虚拟时间 `vt_i` 每次执行后增加 `c/w_i`。

调度器始终执行 `vt` 最小的非空租户。对两个长期繁忙的租户 A、B，稳定状态下
二者 `vt` 交替领先、均值相等：

`S_A/w_A = S_B/w_B  =>  S_A/S_B = w_A/w_B`

即**累计服务量与权重成正比**，权重 1:3 时执行次数（等代价任务）比例收敛 1:3。

若错误地用 `vt += c*w`，则权重越大 `vt` 涨得越快、越频繁地“被让到最后”，
得到的是反比份额 `S_A/S_B = w_B/w_A`，与权重语义相反。故必须是 `c/w`。

## 2. 空闲租户重新加入：为什么必须抬升 vt

若租户 X 空闲期间 `vt_X` 不动，它会保留一个“远古时代”的极小值。重新提交时，
所有繁忙租户的 `vt` 已远高于它，X 将连续赢得选择，直到自己的 `vt` 追上系统当前
水平——在测试场景（X 空闲、Y 连续执行 1000 个单位代价、权重相同）下，X 会**连续
独占约 1000 步**，等价于把空闲期“欠”的服务一次性补发，并把 Y 饿死。

正确做法（WFQ 的标准 finish-time 校准）：队列**从空变非空**（新到达/重新激活）时

`vt_X = max(vt_X, SVT)`，其中 `SVT = min over 非空租户 j of vt_j`

`SVT` 是调度器当前的系统虚拟时间（最落后的在队租户）。抬升后 X 从“现在”开始
参与竞争，最多利用并列规则领先一步，而不是补发历史；Y 仍与 X 交替、不会被饿死。
注意只在“空→非空”那一刻抬升；队列仍非空时的后续提交不抬升（否则该租户会被
不合理地推后）。

## 3. 代价为 0 的任务

`c=0` 时 `vt += 0/w = 0`，该租户 `vt` 永不推进，若 0 代价任务源源不断，它将
无限独占。处理：给实际推进量设最小下界 `eps = 1`

`vt += max(c, eps)/w`

选择以整数序号为基本单位（代价也是同一单位的整数），`eps=1` 即“每次调度至少
消耗一个调度单位的虚拟时间”。0 代价任务合法且可执行，但连续执行有限个后必然
让出；`stat` 只统计真实代价 `c`（0 代价不增加服务量），份额结论不受影响。

## 4. 同虚拟时间并列

`vt` 相等（含浮点近似相等，见第 6 节）时按租户 ID 字典序选择，保证可复现。
堆序键为 `(vt, tenantID)`。

## 5. 背压、移除与错误

- 每租户有独立队列容量；超限返回 `ErrQueueFull`，不触碰其他租户队列。
- `w <= 0` 返回 `ErrBadWeight`；`c < 0`、`c=+Inf/NaN` 返回 `ErrBadCost`；
  向不存在/已移除租户提交返回 `ErrNoTenant`。四类错误可用 `errors.Is` 区分。
- 移除租户：仅允许在其队列空时移除（返回其剩余任务数）；队列非空时拒绝移除，
  返回 `ErrQueueNotEmpty`，任务去向无歧义（仍在原队列，排空后才能移除）。

## 6. 数值表示与并发

虚拟时间用 `float64`：有理数（大整数比）实现复杂且标准库无大有理堆支持。
权重限制在可安全表示的有限正数区间，`vt` 增量 `max(c,1)/w` 始终 ≥ 极小正值，
任务总量有限（题目 10 万量级）时 `float64` 精度（尾数 52 位）远不构成问题。
比较时仅在差值相对极小（`< 1e-12 * max(1,|a|,|b|)`）时视为相等，再用 ID 定序。

并发：`sched.Scheduler` 内部用互斥锁保护队列与堆；入队锁的串行化顺序即“提交
顺序”，同租户严格 FIFO（`Seq` 也在锁内分配），因此同一并发程序在给定交错下
确定；纯串行的确定性任务流跑 20 次调度序列逐元素相同。

## 7. 复杂度与包结构

非空租户放最小堆，选择 = 堆顶 + 下沉，比较次数 `O(log n)`，堆大小恰为非空租户数。
`sched` 导出 `LastCompares()` 供测试断言 `≤ 4·⌈log2 1000⌉ = 40`。

- `task`：任务值类型（租户、代价、提交序号）。
- `tenant`：每租户 FIFO 队列、权重、`vt` 账本与抬升/推进规则。
- `sched`：租户注册表 + 最小堆 + 比较计数 + 线程安全调度。
- `admit`：入队背压（容量校验）与移除守卫。
- `stat`：份额累计、理论占比、相对偏差与公平性度量。
- `cmd/demo`：逐条判定并打印 OK/FAIL。
