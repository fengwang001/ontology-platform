# 设计推导

## 续行后只含空白

`Properties.load` 先读到一个物理行；仅当行尾反斜杠数为奇数时，最后的续行反斜杠和换行才被移除。
随后它读取下一物理行，并先丢弃该续行行首的空白，再把剩余字符拼接到已有逻辑行。
因此，下一物理行为空或只含空白时，剥离后贡献的是空字符串：

- `k=a\` 后接空行，得到 `k=a`；
- `k=a\` 后接只含空格/Tab 的行，仍得到 `k=a`；
- 链接同时结束，因为续行反斜杠属于上一行，空白行剥离后没有新的行尾反斜杠。

这不是“空行被当注释/删除”，而是续行规则先执行，注释判定只针对一个逻辑行的第一个非空白字符。

## Store 的最小必要转义

回写统一使用 `key=value`；转义只用于避免下一次 `Load` 改变词法含义。

键需要转义：

- 反斜杠，以及 `\t`、`\n`、`\r`、`\f`，避免转义或物理行被改变；
- 行首的 `#`、`!`，否则整行会被识别成注释；
- 行首空白，否则还没开始取键就被跳过；
- `=`、`:` 和空白，否则它们会成为键值分隔符（包括键尾空白）。

值需要转义：

- 同样转义反斜杠和 `\t`、`\n`、`\r`、`\f`；
- 仅转义值的第一个前导空格；反斜杠空格会固定生成一个空格，其后的普通空格作为值内容保留，所以不必逐个转义；
- 不转义值中的 `=`、`:`、中间空格，因为分隔符已经确定；
- 不转义值开头或中间的 `#`、`!`，因为注释只可能从物理逻辑行的键首开始。

非 ASCII 字符按 UTF-8 原样写出；Go 的 `bufio.Writer` 可直接写字符串。

## 测试对照表

（实现完成后补齐）
