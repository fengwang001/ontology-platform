# Ontology 对象图遍历器 — 设计推导

模块：`ontology`（Go 1.26）。包划分：`graph`（有向图 + 三色环检测）、
`traverse`（遍历序）、`bounded`（深度/数量截断三态）、`api`（唯一对外入口）。

## 推导一：环检测的「灰 vs 黑」

问题：DFS 中从当前节点出发的边指向一个「已访问」节点时，是否就是环？

- **口径 A：只要指向已访问节点就报环**（二色：未访问/已访问）。
  后果：菱形依赖 A→B、A→C、B→D、C→D 中，D 先经 B 路径完成（黑），
  再从 C 路径被指向。二色法把「指向 D」误判为环。即把无环图判成有环，
  对本体里极常见的菱形（多对多汇合）必然误报，不可用。
- **口径 B：三色标记**（白=未访问，灰=在当前递归栈上，黑=已彻底完成）。
  只有指向**灰**节点的边（回边）才是环——灰节点是当前 DFS 路径上的祖先，
  边回到祖先必成环。指向**黑**节点不是环：黑节点的整个子图已完结且其中
  无环，当前路径只是「再次到达」一个已完结区域（菱形中的 D）。
- **选择：口径 B**。`HasCycle` 对每个白色节点启动 DFS（覆盖非连通图），
  灰→黑转换在邻边全部考察完后发生。菱形返回 false，A→B→C→A 返回 true。
  复杂度：每节点入栈一次、每条边恰被考察一次，O(V+E)；用非导出计数器
  累计「节点访问次数 + 边考察次数」，断言 ≤ V+E。

## 推导二：limit 截断与环的交互

问题：`Limit(n)` 输出满 n 个节点后，结果是 `Complete` 还是 `LimitCut`？

- **口径 A：已输出数 == n 即记 `LimitCut`**。
  后果：可达节点恰好 == n 时，遍历其实自然走完，却被误报为截断；
  反之环把 limit 占满（环上节点去重后不足 n）时又可能漏报。
  「输出满 n」与「图中还有没走的节点」是两个不同命题，不能互相替代。
- **口径 B：以「已发现但未展开的节点数 > 0」判定 `LimitCut`**。
  BFS 中输出第 n 个节点并将其邻接点入队后，若队列非空，说明还有已
  发现、未输出的节点 → `LimitCut`；若队列空，则可达集已全部输出 →
  `Complete`（即使此时输出数 == n）。环上的节点去重后只占一个名额，
  环不会把 limit「虚假占满」；环上尚未走的节点一旦被发现就计入未展开。
- **选择：口径 B**。为满足不变量「输出数 + 未展开数 == 可达数」，
  截断后做一次不计输出的 drain（沿用同一去重集、同一深度上界），
  把剩余可达节点全部计入未展开计数器；环上每个节点只贡献一次。

## 推导三：深度上限与环的交互

问题：`MaxDepth(d)`（起点深度 0）遇环时，截断语义是什么？

- **口径 A：节点按到达路径重复输出，深度超限即停**。
  后果：环 A→B→C→A 中 A 在深度 0、3、6…无限重现，任何 d 都走不完，
  且输出含重复节点，违反「节点即对象」的语义，不可用。
- **口径 B：节点只输出一次（按首次到达的最浅深度），深度限制的含义是
  「所有最短深度 ≤ d 的节点都必须被访问到」**。
  去重使环在 d ≥ 环上最长距离时自然走完（`Complete`），不会无限循环。
  当展开到深度 == d 的节点时，若它还有指向**未发现**节点的边（该邻居
  最短深度必为 d+1 > d），说明深度 d 处存在未展开的边 → `DepthCut`；
  若深度 d 处节点的出边全部指向已发现节点（含环回边指向浅层祖先），
  则没有遗漏 → `Complete`。
- **选择：口径 B**。判定要点是「深度 d 处是否还有指向未发现节点的边」，
  而不是「是否到达了深度 d」。环回边指向已发现节点，不触发 DepthCut，
  因此不会漏报也不会误报。

## 三态互斥与优先级

`bounded` 返回 `Result{Nodes, Err}`：`Err == nil` 即 `Complete`，
否则为哨兵错误 `ErrLimitCut` / `ErrDepthCut`，可 `errors.Is` 区分。
同一次遍历只落一态：先判 `LimitCut`（limit 先于深度触停），
未触 limit 再判 `DepthCut`，皆无则 `Complete`。

## 遍历策略

选 **BFS**：深度语义即「最短距离」，与 `MaxDepth` 天然对齐；首到即最浅，
去重时机简单。DFS 的深度是「路径深度」而非最短距离，与推导三冲突。
`dir ∈ {out, in, both}`：out 沿出边、in 沿入边（反向邻接表）、both 取并集。

## 参数语义（api.Traverse）

- 起始节点不存在 → 参数错误（非三态，直接返回校验错误）。
- 非法 dir → 参数错误。
- `MaxDepth <= 0` → 不限深度；`Limit <= 0` → 不限数量（文档化的「零值即无界」）。
