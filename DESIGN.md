# 设计：三值逻辑下的常量折叠与谓词下推重写器

## 1. 三值逻辑（3VL）语义

列值可为 NULL，谓词求值为三值：T（真）、F（假）、U（未知）。
Kleene 语义：AND=min、OR=max（序 F<U<T），NOT 为 T 与 F 互换、U 不变；
等值比较任一侧为 U 则结果为 U。WHERE 只保留求值为 T 的行，因此在顶层
过滤位置 U 与 F 效果相同（都滤掉），但 U 与 T 绝不等价。

## 2. 三条"常见但不成立"的重写推导

1. `x = x` → TRUE 不成立：x=U 时左式=U，右式=T。U 变 T 即使在顶层也改变
   结果集（U 滤掉、T 保留），故该折叠任何位置都不安全（除非列有
   NOT NULL 约束，本系统不假设）。
2. `A OR NOT A` → TRUE 不成立：A=U 时左式 = U OR U = U，不等于 T。
   同理顶层也不安全。
3. `NOT (A AND B)` → `NOT A OR NOT B`（德摩根）成立：对 9 种赋值逐一
   验证，两侧真值表完全相同，3VL 下是恒等式，任何位置安全。
   但 `A AND NOT A` → FALSE 在 3VL 下不成立：A=U 时左式 = U。不过 U 与
   F 在顶层过滤效果相同，故它属于"仅过滤位置安全"的一类。

## 3. "什么时候可以按二值优化"的条件推导

定义过滤位置：对该位置的子表达式做 U 到 F 的替换不改变最终过滤结果。
逐构造子分析（设子表达式 S 当前值为 U，替换为 F）：

- 顶层 `WHERE S`：U、F 都滤掉，安全。
- `P AND S`：P=T 时 U 变 F 都滤掉；P=F 时都是 F；P=U 时 U 变 F 都滤掉，安全。
- `P OR S`：P=T 时都是 T；P=F 时 U 变 F 都滤掉；P=U 时都是 U，安全。
- `NOT S`：NOT U = U，NOT F = T，U 变 T 改变结果，不安全。
- `S = P`（比较内）：U 传播为 U，而 F 参与比较得 T 或 F，不安全。

结论：过滤位置 = 从根到该节点的路径上只经过 AND/OR、不经过 NOT/比较。
U 到 F 类折叠（如 `A AND NOT A`→FALSE）只在过滤位置安全；U 到 T 类折叠
（`x=x`→TRUE、`A OR NOT A`→TRUE）连顶层也不安全，一律禁用。
实现：fold 自顶向下携带 filterPos 标志，AND/OR 透传，NOT/EQ 清零，
只在 filterPos 为真处做 `A AND NOT A`→FALSE。

## 4. 规则集与位置限制

精确规则（3VL 恒等，任意位置安全）：常量求值、AND 遇 FALSE 短路、
OR 遇 TRUE 短路、AND 去 TRUE、OR 去 FALSE、双重否定消除、德摩根两条。
位置受限规则：`A AND NOT A`→FALSE（仅过滤位置）。
禁用规则：`x=x`→TRUE、`A OR NOT A`→TRUE（见第 2 节）。

## 5. 不动点终止性论证

规则集不含互逆规则。定义测度 M(n) = 对每个 NOT 节点 v 求和 2^|v 的子树|，
配合字典序 (M, 节点数)：

- 德摩根 `NOT(AND(a,b))` → `OR(NOT a, NOT b)`：
  2^(2+|a|+|b|) 大于 2^(|a|+1) + 2^(|b|+1)，M 严格下降。
- 双重否定消除去掉两个 NOT，M 严格下降。
- 折叠类规则只删节点不增 NOT，节点数严格下降且 M 不增。

每轮至少一条规则生效，(M, size) 字典序严格下降且下有界，故必终止。
轮数上界取 4 倍节点数（实测远小于此），由计数器断言；另用规范化打印
出现重复来检测震荡：若规则集被加入互逆规则（如 a→NOT(NOT a) 与
NOT(NOT a)→a 并存），同一形态必然复现，报可判定错误 ErrOscillation。

## 6. 确定性与复杂度

- 规范化打印对 AND/OR/EQ 子节点按字典序排序，交换律下输出唯一；
  不动点收敛到唯一范式（否定范式 + 常量折叠），与规则顺序无关
  （测试打乱规则顺序 20 次验证）。
- 每轮对树做单遍遍历（每节点访问 1 次），总访问量不超过
  轮数 × 节点数，计数器断言不超过 轮数 × 节点数 × 4。
- 下推：顶层合取项按引用表拆分，单表项并入对应 Scan，多表项留在
  连接层，零表项（常量）留在顶层；自检保证每个 Scan 的谓词只引用本表。
