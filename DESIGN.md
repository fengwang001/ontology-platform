# 设计说明

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。

## 严格解码器拒绝清单（每条：若接受，哪个 t 破坏不变量）

1. 显式次数 1：接受 `1a`，但规范化编码是 `a`，重编码 != t。
2. 次数 0：接受 `0a`，解码出空串，重编码为空串 != `0a`；且游程至少含 1 个符号。
3. 前导零：接受 `03a`（或 `01a`），规范化写 `3a`（或 `a`），重编码 != t。
4. 相邻游程同符号：接受 `2a3a`，解码为 `aaaaa`，`Encode` 产出最长游程 `5a` != t。
   流式下必须看到下一个游程的符号才能判定，故需保留一个"待提交游程"。
5. `\` 后不是数字或 `\`：如 `\a` 中 `a` 是普通字符，其编码本就是 `a`；
   接受它等于允许同串的第二种表示，重编码丢反斜杠，不变量被破坏。
6. 末尾孤立 `\`：`a\` 无符号可解码，且其任何"补全"后的重编码都不含孤立 `\`。
7. 末尾只有次数：`a3` 缺符号，不是任何字符串的完整编码（次数必须后随符号）。
8. 非法 UTF-8：编码由数字、ASCII `\` 与原样 Unicode 码点构成；原样码点必须是
   合法 UTF-8，否则无法对应确定码点，重编码会替换/丢弃字节，不变量被破坏。

每类错误都有独立哨兵错误，`*rle.Error.Offset` 给出字节偏移（次数类指向首位数字，
重复符号指向下一游程符号，转义类指向 `\`，非法 UTF-8 指向首坏字节）。

## 大次数取舍

选择**支持任意大次数**：次数用 `math/big.Int` 解析，位数本身受输入长度约束，
不会静默溢出（`runs.ParseCount` 拒绝前导零与 0）。解码不预分配与次数成正比的
内存，而是按输出字节上限逐符号写入（`Decoder.MaxOutput`，默认 64 MiB），
超限返回哨兵 `ErrOutputLimit`，可判定且不崩溃。

## 语义—测试对照（最后补齐）

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1 往返 | `TestRoundTripRandom` |
| 2 规范形式/严格解码 | `TestRejectCases` |
| 3 大次数 | `TestLargeCount` |
| 4 最长游程/不规范化 | `TestEncodeRuns`、`TestRoundTripRandom` |
| 5 跨切分点一致 | `TestSplitsMatchWhole` |
| 复杂度：字节只检查一次 | `TestInspectCount` |
