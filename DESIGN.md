# DESIGN: 自定格式 LZ77 流式压缩

## 流格式（wire 包）

所有整数均为 uvarint（每字节 7 bit 小端，最高位续传，超 10 字节或溢出 64 位判错）。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4F 4C 5A 01`（"OLZ" + 版本号 1），定长 4 字节 |
| 字面量段 | `varint(0) varint(n) <n 字节原文>` |
| 回指 | `varint(1) varint(dist) varint(len)` |
| 刷新标记 | `varint(2)`，无负载 |
| 流尾 | `varint(3) varint(原文总长) varint(FNV-1a/64 校验和)` |

校验和为 FNV-1a 64（偏移基 14695981039346656037，素数 1099511628211），对原文逐字节滚动计算。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

位置 p 的最长匹配何时算"结束"？只有当 p 之后出现失配字节、或匹配长度撞到上限
（enc.maxMatch = 2^15）时才能定长。Write 边界处这两个信息都不存在——下一批字节
可能让匹配继续延长。因此在 Write 边界做任何输出决策都会泄露切法。
结论：`Write` 只把字节追加进未决缓冲 `pend`，不做任何匹配、不输出任何字节；
缓存量 = 自上次 Flush 以来的全部输入（匹配可能任意长，这是切法无关的必要代价，
长度上限只约束单次比较开销，不约束未决量）。只有 `Flush`/`Close` 才对 `pend`
整体跑一遍 LZ77：Flush 语义本身就是"此刻之后没有属于本段的更多字节"，所以
Flush 必须强制结束当前匹配——把已扫到的部分匹配按现有长度定长输出，否则
"Flush 后字节足以还原全部输入"不成立。输出因此只依赖（字节流, Flush 偏移序列），
与 Write 切法无关。Flush 不重置窗口与哈希链的历史（窗口照常滑入新字节），
故 Flush 之后的匹配仍可回指 Flush 之前的数据。

## 推导二：距离 < 长度的回指如何复制（第 2 条）

dist < len 时源区间与目标区间重叠。逐字节向前复制时，第 k 步（k > dist）读到的
"历史字节"其实是本回指第 k-dist 步刚写出的字节——这正是 `abc`×N 能用 dist=3
表示的原因。若用一次性块复制 `copy(out, out[len-dist:len-dist+len])`，源区间尾部
是尚未写入的字节，语义错误。实现：每次只复制 `min(dist, 剩余)` 字节——这段源在
复制前已全部确定（已落进输出与窗口），因此可以用块复制（切片 append）安全完成，
循环上取整(len/dist) 次即得逐字节语义，总工作量 O(len) 次 memmove，不退化成
逐字节循环。

## 推导三：两种"距离过大"为何区分（第 7 条）

"距离 > 窗口容量 W"是格式错误：解码器只保留 W 字节历史，超出者永远无法还原，
任何合法压缩器都不可能产生（匹配器候选只来自窗口，dist <= W 恒成立）。
"距离 > 已输出字节数"是流的前缀性错误：距离虽在窗口量程内，却引用了尚不存在的
历史（流开头附近才会发生）。二者成因与可恢复性不同，必须分开报错。
并行块压缩的预置字典不改变"已输出"的含义：字典恰是上一块（或更早）原文的尾部，
在全局流中位于本块之前、已经输出，故两条校验的语义与顺序流完全一致。

## 推导四：并行块压缩的确定性（第 9 条）

块 i 的压缩输入只有两部分：本块原文 `data[i*bs : (i+1)*bs]` 与预置字典
`data[max(0, i*bs-W) : i*bs]`（上一块末尾 W 字节以内的原文）。两者都是原文切片，
不读取任何其他块的压缩结果，与 goroutine 数量、调度、完成顺序无关；哈希链匹配器
对同一输入序列行为确定，故每块输出确定，workers=1/2/4/8 逐字节相同。
衔接：每块只产出字面量/回指记录，按块序直接拼接；记录自定界，解码器不知道也
不需要知道块界，因此块间不需要刷新标记；全流只有一份流头与流尾，总长与校验和
按整段原文计算。
