# DESIGN — deterministic skip list with rank queries

## 1. 层高的确定性导出

导出函数（`key` 包）：对键的 8 字节做 FNV-1a 64 位散列得 `h`，
`level = TrailingZeros64(h) + 1`（`h==0` 时取 64），上限 `MaxLevel=64`。

- 分布合理性：`h` 的最低位为 0 的概率约 1/2，故 `P(level>=k) ≈ 2^-(k-1)`，
  是标准跳表 p=1/2 的几何分布，期望层数 O(log n)，不退化为链表也不会全最高层。
- 同键同层：`level` 只是键的纯函数，与插入顺序、随机源、已有结构无关，
  这是「结构与插入顺序无关」的前提。
- 重复元素：重复键得到相同层高，多个副本在底层按键序依次相邻排列，
  每个副本是独立节点、各自携带完整跨度，跨度不变量按节点逐层维护，与副本数无关。

实测分布（本函数对键 0..n-1）：

| 元素数 | L1 | L2 | L3 | L4 | L5 | L6 | L7+ |
|---|---|---|---|---|---|---|---|
| 1000 | 500 | 250 | 125 | 62 | 32 | 15 | 16 |
| 100000 | 50000 | 25000 | 12500 | 6250 | 3125 | 1563 | 1468 |

各层节点数逐层减半，与理论分布一致，未退化。

## 2. 删除时跨度的回收

规则：被删节点 `x` 在第 `i` 层的跨度**并入其前驱** `update[i]`：
`update[i].span[i] += x.span[i] - 1`，再让前驱指向 `x` 的后继；
`x` 不出现的层（高于其层高）只把前驱跨度减 1。

论证：第 `i` 层上 `pred -> x -> succ` 原本跨过 `pred.span + x.span`
个底层元素；删除 `x` 后 `pred -> succ` 应跨过 `pred.span + x.span - 1`
（少掉 `x` 自己一个底层元素），公式精确成立。当 `x` 是该层最后一个
节点（`succ == nil`）时，`x.span[i]` 仍是它到表尾跨过的底层元素数，
前驱新跨度 = 前驱到表尾的元素数，不变量保持；表尾由「后继为 nil」
判定，不依赖特殊节点，故末位删除无特例。

## 3. RankOf 对不存在的键

选择：返回「它若插入会占的位置」，即 `lower_bound(key)`（严格小于 key
的元素个数），不报错。推导：不变量 3 要求 `Range(lo,hi)` 等于 `RankOf`
差值且三者不得矛盾；若 `RankOf` 对不存在的键报错，则 `Range` 在 lo/hi
不存在时无法定义，违背「lo/hi 都不存在时仍正确」。采用下界定义后：
`Range(lo,hi) = upper_bound(hi) - lower_bound(lo)`（闭区间计数），
对任意 lo/hi（存在与否、相等与否）都自洽；存在的键其 `lower_bound`
恰为第一个副本的序号，与多重集语义一致。

## 4. 迭代器失效的判定时机

选择：**快照语义**。`Iterate` 在调用瞬间把键序列物化为切片，迭代器只读
该快照，之后任意写入（增删）都不影响已发出的序列，也永不返回错乱数据。
论证「删除的恰是当前元素」：快照在迭代开始前已固定，当前元素即使随后被
删除，迭代器仍按开始那一刻的内容把它发出——这正是「完整反映迭代开始
那一刻」的定义，无半新半旧；且快照只读、不持有锁，不写共享结构，
并发只读下 `go test -race` 干净。代价是 O(n) 物化内存，换来零失效判定
与零锁，对本题（内存结构、正确性优先）是合理取舍。
