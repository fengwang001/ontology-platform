# 分块流式写出管线：设计推导

## 1. 为什么零长写入必须被吞掉

线路格式中每个数据块为一行 `十六进制长度(;扩展)*\r\n` + 载荷 + `\r\n`。
`0\r\n\r\n`（长度 0 + 空扩展行 + 无载荷）被保留为**结束标记**：下游一旦解析
到长度 0 的行，就立即判定整个流传输完成并停止接受后续字节。

因此 `Write(nil)`/`Write([]byte{})` 绝不能映射成一个长度 0 的数据块，否则
下游会在流真正结束前观察到 EOF，之后真实数据块到来时要么被丢弃，要么被当成
“结束后的垃圾”而报错。零长写入是合法的上游调用（例如 `io.Copy` 转发
`Read` 返回的 `(0,nil)`），唯一正确处理是：什么也不切块、不缓冲、不推进，
直接返回 `(0,nil)`。结束块只能由 `Close` 产出。

## 2. 第 3、5 条：断点状态记录哪些量

下游的一次 `Write(p)` 只有三种结果：完整接受 `len(p)`、短写 `n<len(p)`、
暂时拒绝（背压，等同 `n=0`）、硬错误（含断开）。唯一可信的进度量是下游
**返回的非负已消费字节数 n**，任何未被确认的字节都停留在管线内部。

管线维护一个帧（一个完整块的全部编码字节）序列 `frames`，以及游标
`(fi, off)`：当前帧序号与该帧内已确认偏移；`confirmed = 已确认帧总长 + off`。
不变量：

- I1 `accepted = confirmed + pending`（accepted=已入队编码字节，pending=游标
  之后未确认字节）；
- I2 游标之后的字节**逐字节等于**无故障一次成型时编码流的后缀
  `wire[confirmed:]`；
- I3 已确认前缀绝不重发（推进起点是游标而非缓冲头），未确认字节绝不跳过。

短写落在帧内任意位置只移动 `off`，故遍历切分点 k=1..n，游标恰好扫过每个字节
一次，拼接结果与一次写出相同。`resume.Checkpoint` 在硬错误/断开时持久化
`confirmed` 与帧游标；重建管线时把 `frames` 前缀按 confirmed 截断后重新排队，
由 I2 续写，最终字节不变。块内偏移（fi,off）是必须记录的，否则无法判断确认
位置落在帧头还是载荷，重连会错发半行长度行。

## 3. 第 6 条：块序列为何只由字节流与时钟决定

切块策略是字节流上的纯函数：输入是同一串字节和同一条注入时钟轨迹，输出的
（大小,扩展）块序列必须唯一。若策略看上游调用边界（如“每次 Write 收一次就
吐一个块”），那么同一字节流按 `[abc]` 与 `[a][bc]` 两种喂法会得到不同块
序列；而每种切分的编码字节流不同，第 3 条“最终字节序列与从不中断时逐字节
相同”这一断言就失去了参照基准（“正确答案”随调用方式漂移）。断开续传后
上游/下游的分片方式通常也会改变，策略依赖边界会让续传段无法与前缀拼接。

切块规则因此只在三种与调用无关的事件上吐块：

- 流式累积达到 MaxChunk：立即吐一个满块（超过 MaxChunk 的单次写入被切分）；
- 注入时钟推进且越过时间窗：聚合区中的零散字节吐为一个块（MinChunk 只决定
  是否可以提前吐，不改变字节归属）；
- Close：吐出剩余尾部。

聚合区记录“首字节到达时刻”，窗口判定只用注入时钟，测试通过同一条时钟轨迹
重放，保证任意调用切分下块序列相同。

## 4. 第 8 条：缓冲超限为何报背压

三类上限是不同层次的资源约束，必须可区分：

- 单块超 MaxChunk：属于“无法编码”的策略性拒绝，返回 ErrChunkTooLarge；
- 扩展编码总长超 MaxExtLen：属于头部元数据约束，返回 ErrExtTooLarge；
- 待写缓冲超 MaxPending：属于**瞬时**下游跟不上，数据本身完全合法。

缓冲超限若丢数据，违反第 4 条与 I1；若当成永久错误关闭流，则下游一旦短暂
变慢整个传输就失败，而分块传输本来就要容忍慢消费者。正确做法是把缓冲超限
映射为与下游“暂时不可写”同构的背压信号 ErrBackpressure：本次调用在**入队前**
判定超限，一块状态都不修改（chunker 先快照、拒绝则回滚），上游稍后重试同一
批字节即可，顺序与内容不变。

## 5. 第 11 条：缓冲结构与扫描复杂度

缓冲不是一个每次从头拷贝的大 `[]byte`，而是帧链表 `[]frame{raw []byte}` +
游标 `(fi,off)`。推进时从 `frames[fi].raw[off:]` 开始向下游提交：当前帧尾部
加后续完整帧可组成一次提交；无论已积压多少帧，单次推进触碰的只是
“本次实际能写出的那些字节”，已确认帧直接丢弃、起点永远是游标。

定义非导出计数器 `scanBytes`：每次推进累加“本次提交给下游的字节数”，而不是
“为寻找可写字节而跳过的字节数”——由于游标结构，后者恒为 0（无需跳过任何
前缀）。于是缓冲里积压 N=1KB 还是 N=1MB，只要下游每次只消费同样的 d 字节，
单次推进的 `scanBytes` 都等于 d（加一次长度为 0 的边界检查），不随 N 增长；
总体推进次数为 total/d，总工作量对每字节摊还 O(1)。
