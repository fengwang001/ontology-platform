# 只读快照一致性导出器 — 设计推导

## 1. 多版本存储（store）

每个键维护一条不可变的版本链，条目按版本号降序排列：
`(version, value)`；`nil` value 是墓碑，表示该版本起键不存在。
当前版本 `cur` 单调递增，每次 Put 产生一个新版本。
快照版本 `v` 读键时沿链找到第一条 `ver <= v`：
这就是“快照版本可见的值”，链本身不可变，读到的永远是旧值。
无活动快照时，Put 直接替换该键的唯一条目，不留历史。

## 2. 写时复制的触发点与释放（snapshot）

导出进行中键 K 被改写时，若覆盖旧条目，快照版本读到的就是新值。
规则：存在活动快照时，Put 不覆盖，而是在版本链头部插入新条目；
旧条目物理保留，快照沿链按版本过滤即可看到旧值，即“写入前保留
快照可见旧值，导出读取优先命中保留值”。

快照关闭时不能立刻删历史：可能还有另一个更老的快照。
设活动快照版本最小值为水位 `L`。只有满足 `ver <= L` 且不是
链中唯一可用条目的旧条目才可压缩删除，即“按最老快照版本判断”。
两个重叠快照 S1(v1)、S2(v2)，v1<v2：只关 S2 时 L 仍是 v1，
S1 可见旧值必须保留；两个都关闭（无活动快照）后历史归零，
只留当前值。保留条目数随最老快照关闭而下落，引用计数精确。

## 3. 导出顺序无关性

清单总校验和定义为每键 `CRC32(keylen,key,vallen,value)` 的 XOR。
XOR 满足交换律与结合律，与遍历顺序无关；单键因子含键长与内容，
空值与不存在可区分：不存在不参与，空值参与且因子只含键部分。
因此字典序/逆序导出的块字节流可不同，但总校验和相同、集合一致。

## 4. 文件格式、分块与续传

追加式：若干块 + 尾部清单。
块：`"CS"|no uint32BE|keyCount uint32BE|recCount uint32BE|
bodyLen uint32BE|CRC32(头+体) uint32BE|body`。
body 为定长记录 `klen|vlen|key|value`，顺序为该块指定的键序。
块自描述，可独立校验。尾部：`manifest JSON|len uint32BE|"MEND"`，
含 Version、BlockCount、BlockNos、Order、TotalXOR、ChunkSize、
FileLen（不含清单自身）。
续传先读尾部清单：无清单从块 0 写；有则定位 FileLen，
按 Order 只追加缺失块，完成后覆盖写新清单。可从任意块 k 续传。

## 5. 快照过期为何拒绝续传

续传要按清单 Order 重放剩余键，依据快照 v。快照关闭后版本链
可能已压缩，剩余键只能读到当前值，文件就拼成 t0 前段 + t1 后段
的跨时刻结果。因此 Exporter 持有快照引用：Close 后 Read/Resume
返回 `ErrSnapshotExpired`，已落盘部分原样保留。Close 与导出
并发时 Close 等待在导出计数器上，导出结束前历史不释放。

## 6. 截断点分类

从长度 1 截到 len-1，按前缀状态机分类：
- 清单不完整：截断落在尾部清单区域，MEND 定界无法解析；
- 块头不完整：能定位块起点但不足 22 字节头；
- 块体不完整：头完整但 body 不足或 CRC 域不全；
- CRC 不匹配：块完整但校验不符。纯截断不会产生此情形
  （区间为空，需字节翻转制造，演示单独给出一例）。
四类错误用哨兵错误、errors.Is 区分；另有 ErrBlockMissing /
ErrBlocksMisordered 指名块号、ErrChunkSizeZero。

## 7. 资源约束

COW 内存 = 导出期间被改写键新增的版本条目数，与总键数无关。
导出每键仅一次 Store.Read（计数器断言）。分块只用单块缓冲，
峰值驻留 = 一块大小（计数器断言）。
