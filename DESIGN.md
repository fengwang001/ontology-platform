# 设计推导

## 1. 取消与唤醒竞争
错误交错：1) Release 取互斥锁并选中队首 W；2) 若此时只发信号、不把 W 原子改成“已发放”，W 仍像等待者；3) Cancel 加锁把 W 移出队列并减等待数；4) Release 再把许可交给已离开的 W，许可无人接收而消失。
相反交错：1) Cancel 已判定取消；2) Release 未检查终态仍向 W 交付；3) W 同时带着取消结果和许可返回，调用方可能不归还。根因都是“选中”和“终态交付”之间有缝隙。
本实现让队列在同一把锁内完成 Pop、定位节点、状态改为 granted、写入凭证、关闭唤醒通道；Cancel 也在同一把锁内只处理 waiting 状态。granted 与 canceled 是互斥终态，先到者独占结果。
若 Acquire 已因 ctx.Done 醒来但发现终态是 granted，它不得向上层交出凭证，而立即按正常 Release 路径转交下一等待者或放回池，再返回取消错误；因此调用方永不持凭证，许可也不消失。

## 2. 放行顺序
采用严格 FIFO：Release 时只要有等待者，许可直接交给队首，绝不放回可用池。
若允许新来者抢刚释放的许可，调度结果取决于 goroutine 抢占、CPU 调度和系统负载；同一组操作重复执行可能得到不同顺序。FIFO 只依赖入队序号，顺序可复现。

## 3. TryAcquire 与队列
池空或队列非空时 TryAcquire 立即失败。它只消费无人等待的可用许可，不能绕过队首；这样与严格 FIFO 一致，也不产生“瞬时插队”。

## 4. 容量为 0
容量 0 合法。不变量要求 issued+available=capacity，初始状态即 0+0=0；TryAcquire 永远失败，Acquire 只能等待随后也不会出现的许可，可用带截止时间的上下文取消。负数无法拥有非负的 issued 与 available，故构造时返回 ErrNegativeCapacity。
