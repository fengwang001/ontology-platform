# 并行任务图调度与失败传播 - 设计推导

## 1. 问题与模型

输入：DAG（任务为节点，边 `from->to` 表示 to 依赖 from）、每个任务的执行函数
`func(ctx) error`、并发度上限、失败模式（快速失败 / 尽力而为）。
输出：每个任务的最终状态与原因组成的确定性报告。

## 2. 四类最终状态的判定推导

失败发生后，任意任务 T 必落在且仅落在以下四类之一。判定依据两条正交事实：
(a) T 是否已经开始执行；(b) T 是否返回了错误。

1. 已成功 Succeeded：已开始且返回 nil。失败发生前完成的任务属于此类。
   状态一旦进入 Succeeded 即终态，任何后续失败不得回滚或改标。
2. 失败 Failed：已开始且返回非 nil 错误（含被捕获的 panic，包装为
   `exec.PanicError`，`errors.Is(err, exec.ErrPanic)` 为真）。报告保留原始错误。
3. 被跳过 Skipped：从未开始。触发条件二选一：
   - 图下游：T 的某个（传递）上游失败，T 永远无法就绪；
   - 快速失败清扫：首个失败后调度器停止派发新任务，T 虽未依赖失败任务，
     但永远得不到执行机会。
   跳过原因（Cause）必须指向最初失败的任务，而非直接上游：
   传播时沿依赖边把「失败任务的 ID」原样下传，A->B->C 中 A 失败，
   B、C 的 Cause 都是 A。对多上游任务，取最先到达的那次失败（最早失败）。
4. 被取消 Canceled：已经开始执行、但失败发生时仍在运行的旁支任务。
   与 Skipped 的区分依据就是 (a)：调度器在触发取消的快照里把「当前运行集合」
   整体标记为待取消；这些任务之后无论返回什么（成功/失败/panic），结果一律
   丢弃，状态定格为 Canceled。Skipped 任务从未进入运行集合。

四者互斥且完备：Started x Err 真值表覆盖全部组合；「未开始」再按「是否因
失败而失去执行机会」归入 Skipped（无第五类：未开始的任务必然是因失败被拦下）。

## 3. 两种模式的语义差异

- FailFast（默认）：首个失败即 (1) 取消派生 context；(2) 当前运行集合
  标记为待取消；(3) 沿依赖边传播跳过；(4) 清扫所有未开始的 Pending 任务为
  Skipped（下游任务 Cause 已由传播设为各自上游失败，其余指向首个失败）。
  之后不再派发新任务，但仍回收所有在跑 goroutine 的结果（丢弃）。
- BestEffort：不取消 context、不清扫；仅失败任务的下游被跳过，其余分支
  继续跑完。多个失败全部记录；被跳过任务的 Cause 指向各自最早的上游失败。

## 4. 调度器结构与复杂度

单 goroutine 事件循环持有全部状态（无锁、无数据竞争）：

- 就绪判定用 Kahn 入度法：初始扫描 N 次；每条边只在前驱完成时松弛一次、
  失败传播时遍历一次，故就绪判定计数 <= N + 2E <= 4(N+E)，绝不全图重扫。
- 就绪队列为按 ID 升序的有序切片，同时就绪时取最小 ID，保证派发顺序确定。
- 每个被派发的任务一个 goroutine，结果写入容量 = 并发上限的带缓冲 channel；
  事件循环只有在所有任务进入终态（含被取消任务的结果回收完毕）后才退出，
  因此无 goroutine 泄漏。
- 历史最大并发计数器在每次派发后更新，测试可断言峰值 <= 上限。
- 拓扑分层与环检测均用迭代式 Kahn/DFS，1000 层链不递归、不爆栈。

## 5. 确定性

- 报告按任务 ID 字典序输出；就绪队列按 ID 升序弹出；边用集合存储，加边
  顺序不影响任何遍历顺序，故打乱加边顺序报告逐字节相同。
- 完成顺序的随机性只影响事件循环的处理次序，不影响终态判定规则本身；
  对确定性任务函数（含全成功图注入随机延迟），报告逐字节相同。
- 环检测：Kahn 残余子图中沿前驱边走直到重复，得到每条边都在输入中、
  首尾闭合的真实环路径；自环（A->A）残余入度不为 0，同样检出。

## 6. 包划分

- `graph`：建图、环检测（`CycleError`/`ErrCycle`）、拓扑分层、未知任务错误。
- `exec`：任务函数执行、panic 捕获（`PanicError`/`ErrPanic`）。
- `fail`：状态枚举、`Tracker`（状态标记、跳过传播、快速失败清扫）、模式。
- `sched`：事件循环调度器、并发上限、就绪判定/峰值计数器。
- `report`：按 ID 排序的确定性报告。
- `cmd/demo`：逐条判定演示。
