# DESIGN — 流式行尾/空白规范化器

策略：`Keep`（保留末尾换行原样）、`Ensure`（非空输入末尾恰好一个 `\n`，空输入仍为空）、`Trim`（删除全部末尾空行后保留至多一个 `\n`）。

## 1. 空输入与只含换行的输入（推导）

规范化先做行内变换：`\r\n`、`\r` → `\n`，删行尾空白。设该结果为 y。
- `Keep`：末尾就是 y 的末尾，不动。
- `Ensure`：若 y 非空则补/截成恰好一个尾部 `\n`。空输入若补成 `"\n"`，则 `N("")="\n"`，再作用一次仍是 `"\n"`，**也幂等**；但“对零字节输入产生一个字节”违反最小惊讶与输出上限直觉，故选 `N("")=""`：此时 `N(N(""))=N("")=""`，且 `Ensure("\n")="\n"`，幂等成立。
- `Trim`：删除 y 末尾全部空行（连续的尾部 `\n`）。删完若文档有实质内容则补一个 `\n`；若整体为空白/空，则为空或（有过换行）一个 `\n`。验证：`Trim(Trim(x))=Trim(x)`，因为其输出至多含一个尾部 `\n`，再次修剪不变。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut（推导）

映射表按“存活段 + 删除段”的仿射区间维护：存活段 orig→out 平移保长；删除段（`\r\n` 的 `\r`、行尾空白、末尾裁掉的 `\n`）orig 长度>0、out 长度 0；末尾策略补的 `\n` 为 orig 长度 0、out 长度 1 的纯插入段。

设删除段 D 为原文 [a,b)，其前一个输出位置为 p（段起点 out 坐标）。`ToOut` 在 D 上必须取常数：左极限要求 `ToOut(a)≥p`，右极限（后一存活段从 p 开始）要求 `ToOut(b)≤p`，故 `ToOut(i)=p` 对所有 i∈[a,b]。`ToOrig(p)` 取下一存活段首字节（无后继则取 a），于是 `ToOut(ToOrig(p))=p`，互逆成立；两侧取常数天然单调不减。

`\r\n` 中 `\r` 是长度 1 的删除段，其输出位置是其后 `\n` 的位置 p：`ToOut(i_of_\r)=p`，`ToOrig(p)=i_of_\n`，即光标落在**换行符上**。行尾空白段位于换行符之前，其输出位置也是该换行位置——光标位于被删空白中时映射到行尾换行符（逻辑上是该行内容结束处/换行之前的锚点；偏移数值即 `\n` 的下标，编辑器据此定位到行边界）。

## 3. 空白缓冲超限（推导）

两种选择：(a) 强制判为“非行尾”立即吐出空白——但下一字节若是 `\n`，这批空白其实是行尾空白，输出与一次性处理不一致，**违反第 4 条（跨切分点一致）与第 5 条幂等**；(b) 拒绝：返回具名哨兵 `ErrWSLimit`（带已消费的原文偏移），已输出内容保留，实例进入终态（后续写入返回 `ErrClosed`）。拒绝不产生任何错误输出，不改变“若成功则等价于一次性处理”的性质，故第 4/5/6 条都不被触及。选 (b)。上限 0 表示不限。输出上限同理：超限立即 `ErrOutLimit` 拒绝，不部分追加。

## 4. par 拼接（推导）

难点在切点落在待定结构内。调整切点（只左移、保持顺序、允许 0 长段）：切点前一字节是 `\r` 时，把切点左移到该 `\r` 之前（`\r\n?` 整体归前段，Close 语义等于看到下一段首字节后的判定：单独 `\r` 与 `\r\n` 各自规范，`\r\r\n` 在 `\r|\r\n` 处切开仍得 `\n\n`）；切点落在空白运行内时，左移到该空白运行起点（整段空白归前段——若其后是行尾则由前段删除，若是正文中途则由前段原样吐出，判定都不依赖切点之后）。调整范围上界：回扫经过 `\r` 至多 1 字节，回扫空白至多 `WSLimit`（无上限时空白运行可能很长，故并行前要求显式正上限；本实现默认 1<<20）。

各段以相同策略（`Trim`/`Ensure` 只作用于末段）独立规范化：前段一律用 `Keep`，末段用所选策略。段映射按 orig/out 长度平移拼接（删除段平移 orig 基址、out 基址），相邻同斜率段合并。故输出与映射与单流逐字节相同。
