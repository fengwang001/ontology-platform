# 带背压的流式聚合管线 — 设计推导

## 1. 模型

三级流水线，每级一个 goroutine，级间是**有界队列**（容量 Q1、Q2）：

`source -> [原始字节队列 Q1] -> parse -> [记录队列 Q2] -> aggregate -> sink`

记录在 source 中按偏移单调编号（offset = 该条之前的记录总数）。坏记录也占一个
offset，只是不进任何分组，仅累加坏计数。队列满时上游 `Push` 阻塞，压力逐级回传
到 source；source 用同一把阻塞计数器记录被卡次数。

## 2. 核心推导：检查点里的「已消费位置」记到哪一条

设某条记录 offset = k。它在管线中依次经历四个位点：
S(被 source 读出) → Q1/P(parse 完) → A(并入内存聚合) → D(sink 原子落盘)。

- 记成 source 的读取位置（max S）：崩溃时凡是 `S 已发生、D 未发生` 的在途记录，
  恢复后都从该位置之后重放，**这些记录永久丢失**。
- 记成 sink 的落盘位置（max D）且不附聚合状态：恢复时从 max D 重放，会把
  `D 已发生` 的记录再聚合、再落盘一次，**重复**。
- 仅在「管线中在途集合为空」的时刻记 sink 位置也不通用：流水线持续满载时这个
  时刻可能永不出现；且必须连同**那一刻的分组聚合快照**一起记。

正确定义：

> checkpoint.pos = 一个偏移上界 n：满足 offset < n 的**每一条**记录都已到达 D
> （有效记录已落盘，坏记录已计数）；offset >= n 的记录保证一条都没落过盘。
> checkpoint.groups = 在恰好处理完 offset n-1、且 n 尚未开始时的全部分组聚合值
> （含坏计数 badN）。

即 checkpoint 描述的是一个**同时切齐 source/parse/aggregate/sink 四者的串行
前缀边界**（drain point / 排水点）。单 goroutine 逐级处理时，提交一批 B 条后、
下一批进入前，队列恰好为空，四者位点重合，这个不变量自然成立：

- 从 n 重放：<n 的记录 D 已发生且不会重放，不丢不重；
- >=n 的记录 D 从未发生，连同 groups 快照继续聚合，结果与不崩溃完全一致。

**顺序约束（先 WAL 后 CKPT）**：每个边界必须先让 sink 把本批结果原子落盘
（fsync + rename 完成），再写 checkpoint；顺序反过来则崩溃后会用旧 groups 重放
已落盘数据，造成重复。

## 3. 三个崩溃时刻的恢复结论（测试钉死）

| 崩溃时刻 | 磁盘状态 | 从最近完好 checkpoint 重放 |
| --- | --- | --- |
| source 读完、Q1 非空 | 旧 ckpt，新数据均未落盘 | 整条在途区间重放，不丢不重 |
| 聚合中途（A 后 D 前） | 旧 ckpt，未落盘 | 同上，中间聚合随内存一起丢，由重放重建 |
| sink 临时文件已写未改名 | 输出目录残留 `*.tmp`，旧正式输出完好 | 启动清理残留 tmp，再重放该批，不丢不重 |

因每批输出是「该批各分组的追加段 + 段校验和」的确定性格式，重放生成字节完全
相同，恢复后输出与不崩溃**逐字节一致**。

## 4. 背压与资源约束

- 队列满则 `Push` 阻塞；source 在阻塞处计数。全局在途数上界为
  Q1 + Q2（每队列最多各满一次），不可能逼近总记录数。
- 分组数硬上限 MaxGroups：超限返回哨兵错误 `ErrTooManyGroups`，管线停止并
  如实上报，不静默丢组。

## 5. sink 与 checkpoint 的崩溃安全

- 输出：先写 `seg-N.tmp`（内容 + SHA-256 校验和），fsync 后原子 rename 成
  `seg-N`；正式段天然完好。启动时发现的任何 `*.tmp` 一律删除，且对任意字节
  截断点的 tmp：校验和必不匹配（或文件不成形），绝不会被当有效输出。
- 检查点：保留最近两个 `ckpt.0`（新）/`ckpt.1`（旧），写法 tmp+rename 交替；
  每份带校验和。新文件损坏（校验和不匹配）时回退旧文件，并在报告中标记 fellback。

## 6. 优雅停止

`Stop()` 幂等（sync.Once）；关闭后 source 在边界结束，各级排空在途数据、做完
最后一次提交与 checkpoint 后退出，三个 worker goroutine 全部回到基线。
