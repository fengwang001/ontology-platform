# DESIGN

## 推导与结论

1. **空行与单列空值**：末尾换行只终止前一条记录，不制造记录。连续两个换行之间没有任何字节，因而空行没有字段、不能成为记录，只能跳过；否则 `a\n\n` 会凭空多出尾记录。单列中的空值必须有形可序列化，所以规范写法是 `""`。它生成一个“加引号空字段”；若写成零字节，再解析会变成无记录，数据丢失。多列中的空字段可保持未引号，如 `a,\n`。
2. **半包续传**：词法状态只保存当前状态、字段缓冲、首尾偏移、引号标记、记录/字段序号、CR 偏移和终态错误。任何字节只在到达它的那次 `Feed` 中处理一次；`""` 与 `\r\n` 可跨块，由“闭合引号”和“CR 待定”状态保留判定依据。
3. **CR 待定**：未加引号内容或闭合引号后遇到 CR，记录其偏移但不收进字段。下一字节是 LF 才成行尾；是其他字节或 EOF，就在 CR 偏移报孤立 CR。引号字段内的 CR 立即原样入字段，后续 LF 也原样保留。par 段尾为 CR 时，把待定状态和偏移交给右段；右段首字节决定结果。
4. **并行切分**：每段分别按“段首在引号外 / 引号内”两种初始假设各跑一次，故每字节最多两遍。前一段的真实终态确定后，顺序选择下一段假设：终态在引号内选内假设，否则选外假设。段内事件先记段内记录/字段坐标和绝对字节偏移；拼接时加左段累计记录、字段号只在记录边界归零，因此错误换算为段内坐标加累计量。跨段字段由左段 carry 值、起始偏移和右段延续内容合成；`""`、CRLF 同理只靠边界状态续判。
5. **上限即时性**：字段长度按**解码后字节**计数。普通字节到达即计；转义引号在确认第二个 `"` 时计 1 字节（超限位置即该第二个引号）；闭合引号不计数。字段分隔符到达即增加下一字段计数；记录完成即增加记录计数。超限立即终态，已发出记录保留。

**规范输入**：无 BOM；记录间用 `\n`，文件无尾换行；需要引号的字符为逗号、双引号、CR、LF；单列唯一空字段必须写作 `""`；其余字段不加引号。

## 状态转移表

| 状态 | `,` | LF | CR | `"` | 其他 |
|---|---|---|---|---|---|
| 字段开始 | 发未引号空字段；开始下一字段 | 有字段则结束记录；无字段则跳过空行 | 进入 CR 待定 | 进入引号字段 | 追加，进入未引号字段 |
| 未引号字段 | 发字段；开始下一字段 | 发字段并结束记录 | 进入 CR 待定 | 错误：未引号字段出现引号 | 追加 |
| 引号字段 | 追加逗号 | 追加 LF | 追加 CR | 进入“刚见引号” | 追加 |
| 刚见引号 | 发字段；开始下一字段 | 发字段并结束记录 | 进入 CR 待定 | 追加一个引号，回引号字段 | 错误：闭合引号后有杂字符 |
| CR 待定 | 错误：孤立 CR | 成行尾（完成字段/记录或跳过空行） | 错误：孤立 CR | 错误：孤立 CR | 错误：孤立 CR |

EOF 时：引号字段报引号未闭合；CR 待定报孤立 CR；其他状态发出当前字段/记录，但零字节空行不产生记录。
