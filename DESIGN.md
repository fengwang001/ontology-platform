# Quoted-Printable 编码/严格解码设计（RFC 2045 子集）

## 1. 行末空白（推导）
严格解码器把「行末未转义空白」判错，故空格/制表若直接落在 `\r\n` 或输入末尾，产物将不被接受。判定规则按「下一输入字节」推导：空白的下一字节是 `\n`（其 `\r` 前缀可有可无）或 EOF 时写 `=20`/`=09`，否则原样输出。

## 2. 不拆散 `=XX`（推导）
`=XX` 是不可分割的 3 字符 token；字面字节是 1 字符 token。设当前列 col、下 token 宽 w∈{1,3}：若 col+w>76，先输出 `=\r\n` 再输出 token。col=74、w=3 时 74+3=77>76，故先软换行（其 `=` 占第 76 列）。当 col+w=76 且其后还有 token 时也提前软换行，否则下一个 token 必产生 77 字符的行。

## 3. 软换行恰好插在空格之后
该空格**不**算行末空白，原样输出。解码器删除的是 `=\r\n` 三字节本身，其前的空格原样保留，且它后面紧跟 `=` 而非换行/EOF，属于「空格+软换行」结构中的行内空格；写成 `=20` 反而使 token 宽变 3、改变折行位置。判定只依赖真实输入后继，不依赖编码器自插的软换行。

## 4. 往返等式（准确形式）
定义 N(x)：自左向右，把每个 `\r\n` 与每个单独 `\n` 都替换为 `\r\n`，孤立 `\r` 保持普通字节。则对任意字节串 x：`Decode(Encode(x)) == N(x)`。即 x 中 `\n` 往返后成 `\r\n`（`\r\n` 不变，孤立 `\r` 经 `=0D` 还原）；且 Encode(x) 恒被严格解码器接受。

## 5. 最小转义与幂等
编码器仅对 `=`、33–126 与 SP/TAB 之外的字节、以及行末/EOF 的 SP/TAB（不转义则非法）输出 3 字符形式；硬换行只在输入换行处，软换行只在 76 列上限强制时出现。每个转义都是严格合法性所必需；token 宽度最小化后贪心折行位置唯一。故对任意被接受的 y，`Encode(Decode(y))` 与 y 的差异只能来自 y 把本可原样输出的可打印字符（含行内 SP/TAB）多余地写成 `=XX`（及由此宽度变化带动的软换行位移）。

## 6. 语义—测试对照表
| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1 转义与换行规范化 | `TestEncoderEscape` |
| 2 每行 ≤76（含软换行 `=`） | `TestEncoderLineLength` |
| 3 行末空白/不拆 `=XX`/空格后软换行 | `TestEncoderDerivations` |
| 4 五类错误可区分及偏移 | `TestDecoderErrors` |
| 5 跨切分点一致 | `TestChunkConsistency` |
| 6 往返 Decode(Encode(x))=N(x) | `TestRoundTrip` |
| 7 幂等（最小转义） | `TestIdempotent` |
| 复杂度计数 ≤2n | `TestCheckCount` |
