# 设计推导

## 1. 空输入 / 纯换行输入与末尾策略

记规范化（去掉 `\r` 与行尾空白后）的字节流为 B。

- **Keep**：原样保留末尾。`""→""`、`"\n"→"\n"`、`"\n\n"→"\n\n"`、`"  \r\n"→"\n"`。
- **EnsureOne**：非空则末尾恰好一个 `\n`；**空输入输出 `""`**。
  备选 `""→"\n"` 不幂等：`N("\n")="\n"` 但 `N("")="\n"≠""`，违反第 5 条；
  取 `""→""` 后 `N(N(""))=N("")`，三种输出都稳定。
- **TrimBlank**：删除全部末尾空行（行的概念需要换行符），再补一个 `\n`。
  `""` 无行可言，输出 `""`；`"\n"`、`"\n\n"` 均为 0 内容行+空行 → `"\n"`。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三策略输出均只含 `\n`、无行尾空白，故再规范化不变；EnsureOne/TrimBlank 输出恒以
单个 `\n` 结尾，策略再施加仍是自身 → 幂等。

## 2. 被删字节的 ToOut

映射表是若干段 `[os,oe)←[is,ie)`（恒等段等长，删除段 oe=os，追加段 is=ie）。
不变量 `ToOut(ToOrig(o))=o` 决定输出侧每个 o 都必须是某原偏移的像，故删除字节只能
映射到相邻存活位置。`\r\n`→`\n` 时换行符像在原文 `\n` 处；行尾空白后紧跟换行。
统一规则：被删位置 i 映射到「删除区间右端之后下一个存活原偏移对应的输出偏移」，
即 `\r` 与行尾空白都映射到 `\n` 的输出位置（换行符之前的逻辑列）。
编辑器光标落在被删空白里时，跳转到行尾换行符前（列 = 行内保留内容长度），
而不是下一行行首——这是编辑器列保留的直觉，也使 `ToOrig(o)=i ⇒ ToOut(i)=o`
对输出偏移 o 全部成立；删除区间内部取相同像值，ToOut 单调不减。

## 3. 空白缓冲超限

强制判定为「非行尾」会输出空白，但若其后真出现换行，这些空白即行尾空白，被错误
保留，第 2、5 条均破坏（输出不稳定、再规范化结果不同），且映射也随之错误。
故选择拒绝：返回哨兵 `ErrWSBuffer`（带起始原文偏移的具名错误），已输出内容
保留、进入终态（再写返回 `ErrClosed`）。该状态可解释：输出是某合法前缀的规范化
结果，映射表仍自洽。截断到拒绝点之前不影响已有结果（故障注入要求天然满足）。

## 4. par 拼接

每段用 Keep 策略且 open 模式独立 Close：段尾待定的 `\r` 与未判定空白原样吐出
（并保留其段内映射），不做策略收尾。段映射先按「原文偏移 = 段起点+段内偏移、
输出偏移 = 前序段输出长度+段内偏移」平移，得到一张含多余恒等段的临时表。

然后按切点顺序扫描原文修正，只有两类缺陷，修正范围有上界：
1. 跨切点的 `\r|\n`：切点处原文是 `\r`、下一段首字节是 `\n`。前段 open 输出了
   `\r`，删掉该 `\r`（加入删除区间）即可，`\n` 保留 → 两段合计输出一个 `\n`。
2. 跨切点空白串：向左右扩展出切点处极大 `[ \t]+`（上界：缓冲上限 L，两侧合计
   ≤2L；更左/更右不可能属于同一串）。看串后第一个原文判定字节：EOF、`\n`、`\r`
   ⇒ 整串删除；否则保留。处理后越过该串，每个字节最多被扫一次。

全部删除区间（含 1、2 两类）排序合并后，对临时段表做一次 reflow：恒等段与删除区间
做差集、输出坐标按剩余长度重排；删除区间只可能命中恒等段（CRLF 中的 `\r`、空白在
open 结果中均为存活恒等字节）。EOF 处若切点空白串后无字节则按行尾删除。
最后在拼接好的完整字节流上施加 EnsureOne / TrimBlank（truncate 段尾 + 可选追加
零原长段）。段内 goroutine 不共享状态；拼接纯顺序、无 sleep。
