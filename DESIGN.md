# DESIGN

## 1. UTF-8 非法单元
首字节决定期望长度：2 字节 `C2..DF`，3 字节 `E0..EF`，4 字节 `F0..F4`。从起点尽可能按该长度切一个候选单元；若流内字节不足则等待，EOF 时把残前缀整体记为一个截断非法单元。续字节必须是 `80..BF`，且第二字节要在下表区间，才能满足最短性与标量上界。

候选一旦非法，只吞掉“首字节 + 已到达且仍属该候选的续字节”：下一非法续字节结束本单元，其后字节重新开始。故 `F0 90 80 41` 中 `41` 重开，得一个 `FFFD` 加 `A`；`E0 80 80` 的首字节后遇非法 `80` 立即成单元，后面两个 `80` 各自成单元，共三个。`E2 82` 后 EOF 仍是一个未完成候选，所以一个。

| 首字节 | 第二字节合法区间 | 第二字节非法吞掉 | 后续非法续字节 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 2 | 1 |
| `E0` | `A0..BF` | 2 | 1 |
| `E1..EC` | `80..BF` | 2 | 1 |
| `ED` | `80..9F` | 2 | 1 |
| `EE..EF` | `80..BF` | 2 | 1 |
| `F0` | `90..BF` | 2 | 1 |
| `F1..F3` | `80..BF` | 2 | 1 |
| `F4` | `80..8F` | 2 | 1 |
| `80..BF` | 无 | 1 | — |
| `C0 C1 F5..FF` | 无 | 1 | — |

## 2. 流式缓存
UTF-8 最长合法候选为 4 字节；缓存只存一个未完成首字节候选，硬上限 3 字节。看到非法第二字节可立即结束长度 2 的单元；看到之后任何非 `80..BF` 也立即结束当前单元，无需等 EOF。UTF-16 最长代码单元序列是代理对，BOM 检测最多缓存 1 个代码单元即 2 字节。

## 3. 并行对齐
并行只用于字节对齐的 UTF-8→UTF-8。每个 worker 取 `[start-3,end+3)` 的有限重叠并解码带全局起点的单元；单元归属于起点所在的 `[start,end)` 段。前置重叠用于找到跨过切点的单元，后置重叠让其知道该单元是否在更多字节后仍非法；所有段无遗漏、无重复。`C3 | 80 80` 中，单元 `C3 80` 因起点在前段归前段，孤立 `80` 起点在后段归后段。

## 4. 输出上限断点
编码在写完整标量前预留其完整输出长度，不足则不写该标量。`Write` 只返回已变成已输出标量的输入字节数；残留在切分缓存中的字节未被统计消费，由调用方将返回的未消费尾巴接到下次。这样新实例拼接与无限流式结果逐字节相同。
