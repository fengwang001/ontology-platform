# DESIGN

## 1. UTF-8 非法单元

从首字节进入期待状态；第二字节先判定该首字节专属区间。区间非法时，当前字节终结单元：`E0 80`、`ED A0`、`C0 AF`、`F4 90` 都一次吞 2 字节，随后字节重新开始，所以后接续字节会再成一个单元。若第二字节合法，后续续字节先累积；遇到非续字节，不吞该新字节，只把此前“合法但未完成”的前缀判成一个单元，故 `F0 90 80 41` 为 `FFFD 41`。流结束同理，残留前缀只报一个，故 `E2 82` EOF 是一个。

| 首字节 | 第二字节区间 | 非法第二字节吞掉 | 其他终止 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 2 | EOF 吞已缓存前缀 |
| `E0` | `A0..BF` | 2 | 同上 |
| `E1..EC` | `80..BF` | 2 | 同上 |
| `ED` | `80..9F` | 2 | 同上 |
| `EE..EF` | `80..BF` | 2 | 同上 |
| `F0` | `90..BF` | 2 | 同上 |
| `F1..F3` | `80..BF` | 2 | 同上 |
| `F4` | `80..8F` | 2 | 同上 |
| `80..BF` | 无 | 1 | 独立续字节 |
| `C0 C1 F5..FF` | 无 | 1 | 立即重同步 |

## 2. 缓存与即时放弃

UTF-8 最长 4 字节，读到合法第二、第三字节后仍可能等待下一字节，所以缓存硬上限为 3。看到非法首字节、独立续字节、或非法第二字节可立即判定；已有合法前缀后看到非续字节时，立即结束前缀并把该字节作为新首字节处理，无需等 EOF。UTF-16 只需一个奇数字节，或一个等待低代理的高代理；二者合计上限仍小于 3，统一硬上限取 3。

## 3. 并行切点

切点只可能落在 UTF-8 单元起点后 1..3 字节。worker 从切点向前最多回看 3 字节，用全局数据试解析；若某个单元的逻辑终点超过切点，该单元归上段，下段真实起点前移到其起点。`C3 | 80 80` 中双字节字符跨过切点，下段从 `C3` 开始，上段在 `C3` 前结束；随后第二个 `80` 才由下段判成独立非法单元。各真实边界首尾相接，单元归属和非法数与单流相同。UTF-16 在奇数字节处回退 1；若切点前是高代理且切点后是低代理，再回退到高代理起点。首段才识别 BOM。

## 4. 上限断点

输出前先检查目标长度；空间不足则本标量及代理对一个字节都不输出。缓存中的未完成字节不算已消费，普通 `Write` 返回本次已落到完整标量的字节数；超限错误另带全局 `Offset`，即未输出标量的起点。新实例从该偏移喂入，旧实例已停在标量边界，拼接逐字节一致。
