# 租借池设计推导

## 规则 1：取消与交接同时发生

状态空间：等待者有三态 `排队 / 已交接 / 已取消`，资源在池中只有四种归属（空闲、借出、创建中、销毁）。
交接与取消都要在同一把池锁内做一次原子的 CAS 式决策：
- 交接：仅当等待者仍为「排队」，才把资源提交给它并置「已交接」；此提交**不可撤销**。
- 取消：仅当等待者仍为「排队」，才把它移出队列并置「已取消」。

谁赢决策谁决定结果。若交接先赢：等待者即使立刻发现 ctx 已取消，也必须返回成功——资源已在它
名下，失败返回会让该资源既不在空闲队列也不在任何持有者名下（守恒式被破坏）。它随后可
`Release` 正常归还。若取消先赢：它不在队列里，交接方根本看不见它，资源继续交给下一个排队者，
没有资源悬空。

「取消时把自己从队列删掉就完事」错在：删除与归还方遍历队列是两个临界区。归还方可能已经选中
该等待者、正准备投递资源；此时删除成功会导致资源投给一个已离开的等待者而丢失。故取消与交接
必须竞争同一个节点状态位，而不是各做各的链表操作。

## 规则 2：复用顺序（LIFO vs FIFO）

轻负载场景：10 个空闲资源，1000 个时钟刻，每刻借一次还一次，`MaxIdleTime=50`，`MinIdle=1`。
- LIFO（取最近归还）：每刻借走的是上一刻刚归还的那个，它的空闲龄每刻归零；其余 9 个永远不被
  触碰，在第 50 刻全部过期。回收受 `MinIdle=1` 限制保留 1 个。之后唯一存活的就是被反复借用的
  那个，它每次都在被用，永不过期 → 最终空闲数（稳态）= 1。
- FIFO（取最久空闲）：每次借空闲龄最大的，10 个资源轮转使用，每个两次被借用间隔 10 刻 < 50，
  没有任何资源的连续空闲龄达到 50 → 永远回收不掉 → 最终空闲数 = 10。

结论：题目事故「轻负载下永远回收不掉」对应 FIFO。池采用 **FIFO 复用**（取最久空闲），
空闲集合按归还时刻有序；这恰好让回收也只需从最旧端顺序检查（见规则 4）。

| 复用顺序 | 轻负载 1000 刻后最终空闲数 |
|---|---|
| LIFO | 1（9 个在第 50 刻过期，1 个被反复借用） |
| FIFO | 10（轮转周期 10 < 50，无资源过期） |

「错误规则」测试：对同一序列用 LIFO 策略断言空闲数==1，证明选错顺序会得到相反结论。

## 规则 3：名额交接——由谁 Create

规则：**等待者自己的 goroutine 调用 `Create`**，归还方从不创建。归还/丢弃路径只负责把一个
「已存在的资源」沿 FIFO 队列交接出去；没有现成资源时，它唤醒队首等待者，由该等待者在自己的
goroutine 内占用一个「创建中」名额并调用工厂。

推导：
- 失败归属：创建失败是调用方语义错误（连不上后端），必须返回给发起 `Acquire` 的那个等待者。
  若由归还方 Create，错误无法自然回到等待者，且会污染无辜 `Release` 调用的返回值。
- panic 隔离：工厂 panic 在等待者 goroutine 内可被 `defer recover` 捕获；在归还方栈上抛出会
  炸掉无关调用方。
- 名额：等待者被选中去创建时已在锁内预占「创建中」名额；失败/panic 时由同一路径释放名额并
  唤醒下一个等待者（若有），后者可重试创建——因此后续等待者不会永久挂起。

## 规则 4：空闲全坏 + 回收检查复杂度

所有空闲 `Validate` 都失败时：**销毁全部坏空闲，转入创建新资源**（只要还有名额），不向调用方
报错。论证：`Validate` 失败是资源状态问题，不是调用方错误，调用方要的是「一个可用连接」；池
的职责就是透明替换。单次 `Acquire` 中 `Validate` 调用次数上界 = 调用开始时的空闲资源数
（每个空闲资源最多被检查一次，检查失败即销毁并移出空闲集合，不会重复）。

空闲集合按归还时刻有序（FIFO 取最旧端）。`Evict` 从最旧端开始检查：过期则回收，遇到第一个
未过期的即可停止（更新者都更新）；再受 `MinIdle` 截断。故检查数 ≤ 回收数 + 1。
