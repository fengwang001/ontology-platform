# DESIGN：流式行尾/空白规范化 + 双向偏移映射

## 1. 规范化规则

`\r\n`、单独 `\r`、`\n` 一律输出 `\n`；`\r\r\n` = 独立 `\r` + `\r\n` → `\n\n`。
行尾（含流结束）前的连续 `空格/Tab` 删除，其余字节（含 NUL、非法 UTF-8、原样通过。

## 2. 末尾换行策略（推导：空输入）

- Keep：不动末尾。空文档没有“最后一行”，加 `\n` 反而凭空造行 → `""→""`。
- EnsureOne：输出非空且末尾不是 `\n` 才补一个。`""→""`（若选 `"\n"`：`N("\n")="\n"` 仍是不动点，也幂等，但空文档被当成有一行；两种都满足幂等，本实现选 `""`，语义为“没有行就不终结行”）。
- TrimBlank：`""→""`；否则剥掉全部末尾 `\n`，剥完非空则补一个 `\n`，剥完为空（输入全是换行）保留单个 `\n`。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等：输出只含 `\n`、无 `\r`、无行尾空白；三种策略对各自值域都是不动点（EnsureOne 已满足“末尾恰一个或空”；TrimBlank 的输出只含 0/1 个末尾换行）。

## 3. 被删字节的 ToOut（推导）

要求：`ToOrig` 单调不减且 `ToOut(ToOrig(o))==o`。取 run 模型，相邻 run 共享边界锚点 `(orig,out)`，run 内线性插值；查询锚点时取“下一锚点 orig 更大”的 run（tie-break），使共享锚点只有唯一解释。

- 复制 run：`ToOrig(o)=orig+(o-out)`。
- 删除 run（行尾空白、CRLF 的 `\r`）：整段映射到其**右侧**输出锚点 `out`，即换行符 `\n` 之前。
- 插入 run（EnsureOne 补的 `\n`）：整段映射到右侧原文锚点。

故被删空白内 `ToOut(i)=` 换行符的输出偏移（行尾 `\n` 之前）。`\r\n` 中 `ToOut(off(\r))=off(\n)` 的输出位置。光标放在被删空白里 → 落在行尾换行符**之前**（该行逻辑末尾），不跳到下一行行首。右侧锚点 tie-break 保证 `ToOrig(该out)=orig(换行)`，于是 `ToOut(ToOrig(o))=o`；两侧均单调不减。

## 4. 缓冲超限：选择拒绝

强制把待定空白判为“非行尾”输出，下一个字节是换行时无法撤回 → 破坏幂等(5)、跨切分一致(4，切分点落在空白中间会改判)、互逆(6)。拒绝则输出冻结、进入终态，仍满足 4/5/6；返回带原文偏移的可判定错误 `ErrWSBuffer`。输出超限同理 `ErrOutputLimit`。严格模式 NUL：`ErrNUL` 携带原文偏移，已输出保留、终态。

## 5. par 拼接（推导）

各段独立跑 Keep 策略 norm（末尾策略全局统一施加，避免段边界被当文件尾删空白）。逐物理切点：

1. 切点在 `\r|\n`：左段把 `\r` 当独立行输出 `\n`，右段 `\n` 也输出 `\n` → 多一个；拼接删掉左段末尾 `\n`，映射对该位置改插值（落在右段复制的 `\n` 上）。
2. 切点穿过空白串且全局看其后**不是**行尾/EOF（向右跳过空白找第一个非空白，非 `\r`/`\n`）：左段误删，原样补回（长度=左段末尾连续空白数）；右侧是行尾/EOF 则不补。

修正范围有上界：每切点只向左看左段物理结果、向右到第一个非空白字节；增删常数次、补回长度即该空白串自身，不跨切点传播。映射拼接：各段锚点平移 `(+ΣorigLen,+ΣoutLen)`、同 orig 合并；接缝修正整体平移后续 `out` 并做单点插值修改；最后在全局字节与映射上施加一次末尾策略。

## 6. 查询复杂度

锚点只在删除/插入点增长；纯 `\n` 文本仅首尾 2 点。查询二分锚点，非导出计数器记录本次检查的区间数。
