# 分片扇出部分失败合并器设计

## 1. 模型
- `shard.Shard` 描述分片：`ID` 允许为空串；`Query(ctx)` 返回记录、自报条数和该分片可能出现的最大分值。
- 记录为 `{ID string, Value int64}`；未提供的标量字段按零值处理。记录 ID 在同一分片内用于重试去重。
- `fanout` 用带缓冲信号量限制并发，用整体 `context.Context` 控制截止；计数器只在持有/释放信号量时更新。
- `combine` 只合并成功响应，先按 `(分片ID, 记录ID)` 去重，再计算 Count、Sum、Min、Max、TopK。
- `confidence` 只依据成功值、缺失分片及其上界做判定；`report` 汇总值、标注、缺失分片和每分片状态。

## 2. 部分失败下的可证明结论
设成功分片的观察值为已确定事实；任何缺失记录的值均非负。令缺失集合为 M，分片 i 的上界为 u_i，B=Σu_i。

1. Count：缺失分片只能贡献非负条数，因此真实 Count ≥ 观察 Count。观察值是下界，标注 `at-least`。
2. Sum：缺失分片只能贡献非负和，因此真实 Sum ≥ 观察 Sum。观察值是下界，标注 `at-least`。
3. Min：成功集合是真实集合的子集。真实 Min 是在更大集合中取最小值，故真实 Min ≤ 观察 Min；观察值是上界，标注 `at-most`。若观察集合为空，则 Min 无定义。
4. Max：成功集合是真实集合的子集。真实 Max 是在更大集合中取最大值，故真实 Max ≥ 观察 Max；观察值是下界，标注 `at-least`。若观察集合为空，则 Max 无定义。
5. TopK：失败分片可能加入观察列表之外的候选。若当前第 j 名得分 s_j ≤ B，缺失数据可能产生达到或超过它的候选，将其挤出，因此不能从第 j 名开始继续担保。若 s_j > B，则任何缺失候选的得分都不超过其所属分片上界，更不可能达到严格大于 B 的条目；所以前 j 个观察条目必定入选。缺失条目仍可能以较低分插入，且分数相同者需按 ID 重排，故接口只担保“可信前缀长度”，不把前缀外排名当真。

因此可信前缀长度 p 是最大的 j∈[0,min(K,len(观察TopK))]，使前 j 条每条得分都严格大于 B。等价地，在观察 TopK 非零时检查最弱的第 j 名 s_j>B。全部成功时 B=0 且数据完整，五种聚合均为 `exact`，不因零值降级。

## 3. 故障与状态
- 超时：`ctx` 截止后不申请新信号量；在飞调用收到取消，结果状态为 `timeout`。
- 损坏：`DeclaredCount != len(Records)` 时不参与合并，状态为 `corrupt`。
- 重复：同一分片内相同记录 ID 只保留一次；状态为 `duplicate`，但合并可成功。
- 全部失败：不存在任何成功响应，返回 `ErrAllFailed`；零分片返回 `ErrNoShards`。
- 所有分片成功但记录为空是合法精确零结果，必须与全部失败区分。

四类状态通过枚举区分；零分片、全失败、损坏另可通过 `errors.Is` 识别。

## 4. 确定性与资源证明
- TopK 先按分值降序；分值相同按记录 ID 升序；因此只依赖多集内容，不依赖到达顺序。
- 合并前将响应按分片 ID 排序、记录按记录 ID 排序；缺失清单同样排序后生成，报告可逐字节复现。
- 每个请求开始前必须同时满足：父 `ctx` 未结束且信号量有空位；故历史峰值 ≤ 构造上限，截止后新发起数为 0。
- 所有在飞请求接收同一 context；Wait 等待这些请求随取消结束，因此能在截止后的短窗口内返回。

## 5. 边界
- K 大于总条目数：返回全部观察条目前提下的列表与可信前缀。
- 单分片：成功即精确；失败即全部失败。
- 空分片 ID：以空字符串作为正常键，不特判。
- 只返回部分字段：缺失的数值字段取零；自报条数仍必须与记录数一致。
