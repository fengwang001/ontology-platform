# 设计推导

三种末尾策略（规范化后的序列只含 `\n`）：
- `Keep`：保留全部末尾换行；
- `One`：末尾换行超过 1 个则收敛为 1 个，0 个仍为 0 个；
- `Blank`：删除全部末尾空行；文件在删除前含任意换行则留 1 个 `\n`，否则 0 个。

三者都只删除字节、从不合成新字节，故输出是原文的子序列，幂等时第二次无字节可删。

## 1 空输入与只含换行输入

若对空输入合成 `"\n"`，该 `\n` 没有原文源偏移，`ToOrig` 无法给出源点，且
`ToOut(ToOrig(o))==o` 在新增终点上无解（输出域比原文域多一个点）。因此任何策略
都不许合成新字节：`One("")==""`。`One` 的不变量准确表述为“至多一个末尾换行”。
样例表（`"  \r\n"` 先删行尾空白再决策末尾）：

| 输入 | Keep | One | Blank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

每种输出已无 `\r`、无行尾空白且不含多个末尾换行（One）或不含末尾空行（Blank），
再规范化不变，故三策略均幂等。

## 2 被删字节的 ToOut

设原偏移区间 `[a,b)` 被整体删除，其删除点后的下一个保留字节在输出位置 p；若无下一个
保留字节则 p=len(out)。规则：`i∈[a,b]` 时 `ToOut(i)=p`；`ToOrig(p)=b`。于是
`ToOut(ToOrig(p))=ToOut(b)=p`。被删空白与 `\r\n` 中的 `\r` 都属闭区间 `[a,b]`：
编辑器光标落在被删空白里时，映射到该行换行符**之前**（行尾位置 p，下一字节才是
`\n`）；光标向右跨过删除点（b）后才落到 `\n` 之后。`\r\r\n` 中第一个 `\r` 不删除
（它自己规范成一个 `\n`），仅 CRLF 的 `\r` 删除。

## 3 缓冲超限

一串待定空白在看到行尾前无法判定，若强制按“非行尾”输出，后续真出现 `\n` 时已无法
收回，违反语义 2、幂等与跨切分一致；而拒绝可以在任何切分方式下于同一全局偏移
确定触发，且拒绝后保留已确认输出、不写待定空白，状态完全可解释。故选择拒绝，返回
具名 `ErrWSOverflow`（附原文偏移），进入终态。输出上限同理在提交字节前拒绝
（`ErrOutputLimit`）。

## 4 par 拼接

每段独立 Close 无法知道跨段的 `\r\n` 与空白串。做法：段 j 处理扩展范围
`[start_j-pre, end_j+post)`，其中 pre/post 是从边界向两侧扫描到的连续 `[ \t\r]`
（再向前补看一个字节以决定 `\r` 是否配对）的长度，因此修正范围 = 待定空白上限内的
字节数，有上界。段首强制丢弃挂起 `\r`，段内按“无末尾策略”规范化；随后只保留输出
字节的**源坐标**落在本段归属 `[start_j,start_{j+1})` 内的部分（保留字节源坐标即自身，
用 `ToOrig` 判定）。K 段保留集合恰好划分全部保留源字节，拼接即全局基础输出。
最后一个 worker 额外取真正的末尾策略并关闭；其产生的末尾删除段经源区间并集替换全局
span 中落入该尾区间的项。各段删除区间源坐标平移到全局、排序合并，输出坐标由
`p = srcEnd - 已删字节累计` 得到。空白串超过上限时至少一个扩展窗口在同一全局偏移
报 `ErrWSOverflow`，与单流一致。
