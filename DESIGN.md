# 设计推导

## 1 非法单元切分（WHATWG 式）
首字节决定序列长度 L(2/3/4) 与第二字节合法区间。见下表。第二字节若在非法区间，则该首字节单独成一个非法单元，后续字节从下一字节重新解析；第二字节合法而其后的延续字节非法，则整个前缀（已吃的 2..L 字节）成一个非法单元，非法字节重新解析。`80..BF` 与 `C0/C1/F5..FF` 各吞 1 字节。流结束时残留的未完成前缀整体成一个单元（截断/替换）。
- `F0 90 80 41`：第3字节非法（应为延续却为 `41`），吞 `F0 90 80` 三字节，`41` 重解析 ⇒ 一个 FFFD + `A`。
- `E0 80 80`：`E0` 后第二字节区间 A0..BF，`80` 非法 ⇒ `E0` 一单元；`80 80` 各一单元 ⇒ 三个。`ED A0 80` 同理（ED 区间 80..9F，A0 非法）；`C0 AF`、`F4 90 80 80` 同理逐字节成单元。
- `E2 82`+EOF：第二字节合法，仅第3延续字节缺失，两字节是一个未完成前缀 ⇒ 一个 FFFD。

## 2 第二字节区间表（违规时吞 1，仅首字节；合规则吞到坏字节止）
| 首字节 | 第二字节区间 | 首字节即非法 |
|---|---|---|
| C2..DF | 80..BF | — |
| E0 | A0..BF | — |
| E1..EC | 80..BF | — |
| ED | 80..9F | — |
| EE..EF | 80..BF | — |
| F0 | 90..BF | — |
| F1..F3 | 80..BF | — |
| F4 | 80..8F | — |
| 80..BF | — | 吞 1（杂散延续） |
| C0 C1 F5..FF | — | 吞 1 |

## 3 缓存上限与可立即判非法的时机
UTF-8 最长 4 字节：合法前缀最多缓存 3 字节，故硬上限 4（含首字节）。当新读到的字节违反“首字节定长/第二字节区间/普通延续区间”中任一条，立刻在该字节处定单元（前缀）并把该字节置为 reprocess 重新解析，不等 EOF；只有缓冲区里的合法前缀在无新字节时才挂起。`E2 82` 因此在 Close 前一直合法挂起、绝不提前出 FFFD。UTF-16LE/BE 单元 2 字节、代理对 4 字节，缓存上限同为 4；奇数尾字节或孤立高代理仅在 Close 时判截断。检查用每个字节仅在“进入”和“reprocess 进入”各计一次，故 ≤2n。

## 4 par 切点对齐
段 i 起点 s_i=i*n/K。向回看最多 3（UTF-8；u16 为 2）字节定位真实起点 t：从 max(0,s-3) 起用“无尾截断”解码推进，取 ≤s 的最大单元结束位置；空单元时取 s（保证非递减）。段 0 恒 t=0，末段吃到文件尾并允许截断。中段输入为 [t,文件尾) 但只**发射起点 ≥ s_i 的单元**、且不允许尾截断（末尾未完成单元丢弃，由下一段重新解析）。每段独立 BOM 关闭，仅段 0 按配置处理 BOM。这样任何单元（含跨切点的非法单元）恰被一段发射：单元结束位置唯一决定归属；单元中间被丢弃、随后续段重放，故个数与单线程相同。`C3|80 80`：s 落在 `C3` 后，回看发现 `C3` 单元在 s 之后才结束，丢弃 `C3`，段以 `80 80` 重解析，恰为两个杂散延续 ⇒ 与单线程相同。偏移加 t 还原为全局偏移。回看定长 ⇒ 检查 ≤ n+8C（C 为小常数，实测见 AUDIT.md）。

## 5 第 9 条断点
Write 先缓冲挂起前缀（不计入返回的已消费），再逐单元推进：每个单元编码前检查输出余量，放不下立即停止，返回 n=已完整发射单元的字节数之和；挂起前缀留在缓存，对调用方属“未消费”，续传时从 n 处重喂新实例。n 之前无半个标量、无半个代理对；新旧实例输出拼接 == 不限长结果。
