# 设计推导（第三节四处推导 + 流格式表）

## 1. Write 切法无关（语义 3）
把输入流看成一条确定字节流，输出 token 序列只由「字节流 + 已确认的 Flush 点」决定。
做法：新字节先追加进窗口与一段 pending；每次只在 pending 的 **0..len(pending)-2** 上推进游标（最后 1 字节永不决）。
在位置 i 找到匹配则整段输出（若匹配触到 pending 末字节则停止），否则输出 1 个字面量。
原因：最长匹配是否延伸取决于下一个未到达字节，保留末字节意味着任何已发 token 的选取都只依赖已经看见且不再位于末位的字节，
所以按 1/7/整段切 Write，窗口内容与每个决策点所见前缀完全一致，输出逐字节相同。
为防 pending 无界：当 pending 达到窗口容量 W 时强制把 pending 当成「末尾」结算一次（清空 pending，窗口仍保留历史）；
该触发只取决于累计字节流，与切法无关。`Flush()` 也强制结算全部 pending 并发 FLUSH 标记，
因为调用方要求此刻所有输入可解，等待下一个字节已不可能，故 Flush 强制结束当前可能的匹配；此后匹配仍可命中窗口历史。

## 2. 重叠回指（语义 2）
块复制（`copy(dst, src)` 由 memmove 实现）在 distance<length 时源区与目标区重叠，结果错误。
逐字节 `for k:=0;k<length;k++ { out=append(out, out[n-distance+k]) }` 每轮都从**已生成**的输出取字节，
等价于周期复制，对 distance=1/2/3、length≫distance 正确。
回指长度上限就是 W（≤数万），循环代价与输出字节数同阶（本来就要逐个产生输出），故直接采用逐字节循环。

## 3. 「距离超过已输出」与「超过窗口」（语义 7）
- 距离 0：编码非法（0 个字节前不存在）。
- 距离 > 已输出（解压端含预置字典的已可见字节）：引用了从未存在过的数据。
- 已输出 ≥ 距离 > W：引用存在但已滑出窗口的数据；合法压缩器**绝不可能**产生它，因为匹配器只在容量 W 的环形窗口内找候选。
两者成因不同，必须分别报错并给出流内字节偏移。
并行块预置字典：块 0 之后，块 i 把「上一块末尾 W 字节」作为初始窗口，并对其建哈希链。
解压端解码该块前把同一字典预置进自己的窗口/已输出计数，故「已输出」对块 i 而言是「预置字典 + 本块已解压」，含义一致、判定不变。

## 4. 并行块确定性（语义 9）
块 i 的压缩输入 = 本块原文 + 上一块原文（仅取末尾 W），不含任何其他块、token 或调度信息；
因此块 i 的 token 序列与其完成顺序、goroutine 数量无关，workers=1/2/4/8 逐字节相同。
块间不需要 FLUSH：token 流天然连续，预置字典替代历史窗口，解压器不感知块边界。
衔接：块 0 的片段带流头，其余块不带头；只在全部数据末尾发一次 END（origLen、FNV-1a64 校验和覆盖全文）。

## 流格式（整数均为无符号 LEB128 变长；校验和 FNV-1a 64 手写）
| 记录 | 字节布局 |
|---|---|
| 流头 | `'O' 'N' 'T'` + version(varint=1) + windowCap(varint) |
| 字面量段 | tag0 与 length 共编码 + length 个原始字节 |
| 回指 | tag1 与 distance 共编码 + length(varint,≥3) |
| 刷新 | 单字节 tag 2 |
| 流尾 | 单字节 tag 3 + origLen(varint) + checksum(varint) |

tag 与第一个 varint 共占首字节：低 2 位为 tag，高 6 位为该 varint 首段；刷新/流尾为纯单字节 tag。
varint 超过 10 字节或溢出 uint64 判为「变长整数非法」。
