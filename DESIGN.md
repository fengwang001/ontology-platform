# 设计推导

## 1. 末尾换行策略（空输入 / 只含换行）

设核心规范化 C(x)：统一行尾、删行尾空白；策略只改输出末尾。
- `Keep`：保留解析结果（EOF 待定 `\r` 仍是一行之尾，输出一个 `\n`）。
- `EnsureOne`：无行尾则补一个；末尾若干 `\n` 收敛为一个。
- `TrimEmpty`：去掉末尾所有空行；非空且有换行则保留一个 `\n`；无换行文本不加；空输入不合成字节。

**空输入选 `""` 而非 `"\n"`**：若 EnsureOne(`` "" ``)=`` "\n" ``，则二次处理得到 `` "\n" ``，虽然也幂等，但凭空插入一个原文不存在的字节，破坏映射（出现无原文对应的输出区间，只能造插入区间），也违反「只做规范化」的直觉。选 `` "" `` 后输出是输入的子序列（除待定 `\r`→`\n` 的等长替换），映射全程有真实原文可指；三种策略对 `` "" `` 都给 `` "" ``，`N(N(""))=N("")` 仍成立。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

补充：`"abc"`→ Keep/Trim=`"abc"`、Ensure=`"abc\n"`；`"abc\n\n"` 三者均以恰一个 `\n` 结束。幂等：输出中无 `\r`、无行尾空白，C 是恒等；末尾 `\n` 数 ∈ {0,1}，三个策略在 {0,1} 上均为不动点。

## 2. 被删字节的 ToOut

映射以「行程」记录：copy(等长保留)、delete(原文有/输出无)、insert(仅 EnsureOne 补的换行)。坐标边界是公共点，必须有**一致的归属约定**：

`ToOrig(o)`（o∈[0,lenOut]）：落在删除边界的输出坐标归**右**，即 `o==` 某 delete 区间右端时返回该区间右边界（换行符起点）。这样每个输出坐标都指到保留字节或最右终点，且 `ToOut(ToOrig(o))==o`。

`ToOut(i)`（i∈[0,lenIn]）：被删区间内部的 i 取该 delete 的**输出终点**（其右侧第一条保留行程的起点；删除位于输出末尾时取 lenOut）。边界端点归右：delete 起点按前一 copy 行程末端映射。于是 `ToOut` 单调不减；`"ab  \n"` 中三个被删偏移都映射到 `3`（`\n` 的输出位置，含删除起点也归右）；`"a\r\nb"` 中 `\r` 映射到 `1`，即 `\n` 的位置。

编辑器语义：光标置于被删行尾空白中，映射到**行尾换行符之上（其之前的边界）**，而非换行之后的下一行，保证可见的列位置回跳最少、单调且互逆。

## 3. 空白缓冲超限

备选「强制判定为非行尾并输出」不可取：超长空白后若确实出现行尾，单次流会保留空白、而小缓冲/不同切分方式会拒绝或删除，违反第 4 条（切分一致，缓冲上限对切分敏感）；保留下来的行尾空白还违反第 5 条幂等（二次规范化会删掉，输出变化）。因此选择**拒绝**：消费该空白字节前返回具名错误 `ErrWhitespaceLimit`（携带该原文偏移），此前输出与映射保持有效并保留，实例进入终态（再写返回 `ErrClosed`）。错误可判定、状态可解释，且不产生违反 4/5/6 的输出。输出上限同理：追加前判定，超限返回 `ErrOutputLimit`（带原文偏移），不追加。

## 4. par 拼接修正

每段独立按 `Keep`+宽松模式规范化（EOF 待定 `\r` 当作行尾、段尾空白当作行尾空白删除），再按从左到右顺序修正两段边界，只有两类 carry：

- **待定 CR**：前段末是孤立 `\r`。后段首字节为 `\n` 时，前段的 `\n` 其实是 `\r\n` 对，删掉前段最后的输出 `\n`（delete 前一段那个由 `\r` 转换出的 copy），右段 `\n` 保留；否则前段 `\n` 正确。只看 1 个字节。
- **段尾空白**：前段以被删空白结尾。看后段首个非空白字节：是行尾（`\n`/`\r`）或后段整段空白且全局 EOF → 删除成立；否则空白属于行中间，把前段被删 blank 恢复成 copy，且后段若也删了自己的前导空白则一并恢复，由更右的判定统一裁决。需要回看的只是「直到第一个非空白字节」，即下一个 carry 判定点，跨段数与 K 同阶、不随行长增长；`\r|\n`、空白串中间切点都被同一规则覆盖。

坐标平移：段行程的 in/out 区间加该段之前各段的输入/输出累计长度即为全局坐标；修正只是把边界处至多一条 copy↔delete 行程翻转或丢掉末条行程。全部段成功后再在全局尾部应用一次最终策略（EnsureOne 可能加一个 insert 行程）。修正范围上界：每个切点至多检查首字节并翻转一条行程，总计 O(K)。
