# DESIGN：带依赖的配置解析与环境覆盖器

## 1. 包划分
- `source`：四类来源读取与规范化（键路径+原始值+层标记）；含文件截断分类、环境名映射。
- `merge`：默认值→文件→环境→命令行，按优先级合并，一遍扫描。
- `expand`：在合并后的最终键值表上统一做 `${key}` 展开、转义、环检测、缓存。
- `trace`：记录每个键被哪些层提供/覆盖，报告原始值与展开值。
- `validate`：整数类型校验、必填检查。
- `cmd/demo`：无参数、无网络，逐条判定打印。

## 2. 核心推导：先合并后展开
反例（先展开再合并）：文件层 `greeting = hello ${name}`、`name = file`；
环境层只覆盖 `name = env`。若在文件层先展开，greeting 被固化成
`hello file`，环境对 `name` 的覆盖无法传播到 greeting。
正确顺序：**先把四层合并成最终键值表（name=env 胜出），再在最终表上
统一展开**，greeting 读 `${name}` 时看到的是 env，得 `hello env`。
溯源在展开前记录原始值；展开仅产出最终值，不改写溯源。

## 3. 引用语法与转义
- 引用：`${` 起，到**第一个** `}` 止，中间非空且为合法键路径，否则可判定错误。
- `${` 未闭合：扫描到值末尾仍无 `}`，报字节偏移（0-based）位置。
- 转义：`$$` 折叠为字面 `$`。故 `$${a}` => 字面 `${a}` 不展开；
  `$$${a}` => `$` 后接对 a 的展开。扫描：遇 `$` 看下一字符：
  `$` 输出 `$` 前进 2；`{` 进引用解析；其它字符为字面 `$`。
- 部分引用：一值内多处引用逐段展开（`${a}${b}` 两处）。
- 环：调用栈中再次出现同一键即报错，路径为栈+闭合键，首尾相同
  （自引用 `a=${a}` 路径 `a -> a`）。
- 缓存：每个键只展开一次并记忆；“替换次数”=引用替换动作数，
  链式翻倍 `ki = k{i-1} k{i-1}` 时为 `sum(2^i, i=1..n)=2^(n+1)-2`；
  朴素不缓存会 2^n 次重算；缓存后总替换 ≤ 4·键数·平均引用数。

## 4. 合并复杂度
四层各一份 `map[string]string`。合并每层只遍历一次（总访问上限 4N），
不做层间交叉扫描。非导出计数器记录 map 访问，测试断言四层各 1 万键时
计数 ≤ 4·总键数。

## 5. 文件格式与截断分类
行格式 `key = value`（`=` 两侧空白裁剪；空值合法，区别于键不存在）；
`#` 起始为注释；末行必须以 `\n` 结束，否则行不完整。
逐字节截断到长度 L（1..len-1）分类（给行号）：
截断内容不以 `\n` 结尾时该行未结束——无 `=` 为**键不完整**，有 `=` 为
**值不完整**；其余（末行以换行结束但无法构成键值，如 `\r` 孤行、空白或
注释末行）为**行不完整**。判定只看截断后的字节。

## 6. 环境变量名映射与歧义
规则 P（点分隔）：键转大写、`.`→`_`，`a.b.c`→`A_B_C`。
规则 Q（双下划线分隔）：`.`→`__`，`a.b.c`→`A__B_C`。
同一逻辑键两种规则名同时存在即歧义（`errors.Is` 可辨）；不做跨键碰撞
猜测。空环境值合法，区别于未设置。

## 7. 键路径规则
键按 `.` 分段；空串键、空段（`a..b`）为可判定错误；段内允许
`[A-Za-z0-9_-]`。深度 100 合法。输出一律键字典序，保证确定性。

## 8. 错误与校验
哨兵错误均可用 `errors.Is` 区分：`ErrTypeMismatch`（含键名/期望类型/
实际值/来源层四要素）、`ErrMissingRequired`（一次报全所有缺失键）、
`ErrAmbiguousEnvName`、`ErrFileTruncated`（附分类与行号）；另有
`ErrSyntax`（未闭合/坏键）、`ErrCycle`（附环路径）。整数键值必须全为
数字（可选负号），否则类型错误。
