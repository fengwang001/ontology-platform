# DESIGN — 自定义 LZ77 流式压缩

## 流格式（wire 包）

所有整数均为 uvarint（LEB128，超 10 字节或溢出 64 位即判错）。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `'O' 'L' 'Z' 0x01`（3 字节魔数 + 1 字节版本） |
| 字面量段 | `0x01, uvarint n, n 字节原文`（n ≥ 1） |
| 回指 | `0x02, uvarint dist, uvarint len`（dist ≥ 1，允许 dist < len） |
| 刷新标记 | `0x03`（无负载，仅记录边界） |
| 流尾 | `0x04, uvarint totalLen, uvarint crc32(原文)` |

## 第三节推导

### 1. 压缩结果与 Write 切法无关（第 3 条）

`Write` 边界之后还有什么字节是未知的，位置 p 的最长匹配可能延伸到尚
未到达的数据；若在 `Write` 边界强行定界，匹配长度就依赖于调用方的切法。
推导：要让输出只是「输入字节序列 + Flush 偏移」的纯函数，分词动作就不
能发生在 `Write` 时刻。结论：`Write` 只把字节追加到未决缓冲区 `pend`，
**不做任何分词**；分词只在 `Flush`/`Close` 时对 `pend` 整体进行。缓存的
未决字节数 = 自上次 Flush 以来的全部输入（全同字节等极端情形下匹配一
直延伸，本就无法提前定界，这是不变量的必然代价，用 MaxMatch 上限强制
截断超长匹配）。一个匹配只有在看到「终止字节」（失配、达到 MaxMatch、
或 Flush/Close 点）时才算结束。由此 `Flush` 必须**强制结束**当前匹配：
Flush 承诺其前的全部输入已可解，不能等待一个可能永远不来的失配字节。

### 2. 重叠回指的解压复制（第 2 条）

`dist < len` 时源区间与目标区间重叠：一次性块复制（如 `copy`）读的源字
节有一部分尚未写出，结果错误；逐字节向前复制每写一个字节立即可供后续
读取，语义正确。实现：按 `min(dist, 剩余)` 为一步的**分块倍增复制**——
每步复制时源区间（已物化的输出尾部）完整可读，语义与逐字节循环逐点等
价，但每步是一次 memmove，总量 O(len)，不退化成逐字节循环。

### 3. 「距离超过已输出」与「距离超过窗口」为何分开（第 7 条）

窗口容量是**资源配置**（解压器可用与编码器不同的窗口重放同一格式）；
已输出字节数是**流结构合法性**（回指不能指向还不存在的字节）。二者违
反的是不同不变量，报错必须可区分。压缩器保证 `dist ≤ min(已输出, 窗口)`
（匹配器只考察窗口内、已消费位置的候选），所以合法流永不触发任一条。
并行块压缩中预置字典就是上一块的原文尾部，它在流里就是「已输出字节」，
「已输出」的含义不变，仍指整个流到当前为止解码出的总字节数。

### 4. 并行块压缩的确定性（第 9 条）

块 i 的分词只读「块 i 原文 + 块 i-1 末尾 min(窗口,块长) 字节的预置字典」，
不读任何其他块的压缩结果，也不依赖完成顺序，故输出是块内容的纯函数，
workers 取任意值结果相同。衔接：各块只产出记录序列（字面量/回指），按
块序直接拼接；解压器的窗口跨块连续，无需块边界信号，故块间**不需要**
刷新标记；流头由主 goroutine 写一次，流尾覆盖全部原文。
