# 设计推导

## 规则一：执行完一个任务后键去哪

设 n 为槽位数，热点键 H 预排 10000 个任务，之后提交 k 个冷键各 1 个任务。

- 做法 A：worker 执行完 H 一个任务且 H 队列非空时，**留在本地继续执行 H**，不经过环。
  H 一旦占住某槽，该槽永远只服务 H。冷键等待上界 = 无穷大：H 后面 9999 个任务先跑完。
  n=1 时唯一槽被 H 永久占据，冷键永远饿死（题述事故）。
- 做法 B（采纳）：每执行完一个任务，只要 H 队列非空就把 H **重新放回环尾**，worker 从环头取键。
  键在环上严格轮转：冷键提交时进入环尾，排在当时环上各键之后；H 每次只执行一个任务就要回环尾重排。
  某冷键 C 从其入环到出环被执行之间，环上任意其他键（含 H）至多被取到 1 次，
  即 C 等待期间 **H 至多再执行 2 个任务**（C 入环时 H 可能正占着槽执行 1 个，之后在 C 之前最多轮转 1 次）。
  该上界与 n、k 无关（始终为 2）；对比做法 A 上界无穷大、n=1 时冷键永不执行。

## 规则二：键何时离环、何时入环

事实：**任务执行期间，键不在环里**（离环），保证不会有第二个 worker 取到它（同键串行）。

用一把互斥锁原子地维护「每键队列 + 每键 running 标志 + 环 + 全局计数」，规则：

- Submit(key)：入键队列；全局 pending+1；仅当该键此前无任务且 `running==false` 才入环；
  若正在执行（最后一个任务执行时来了新任务），**不入环**，running 保持 true。
- worker 从环头取键：出环并置 `running=true`（出环与置标志同一临界区）。
- 任务执行在锁外；执行完后加锁弹出该任务：panic 计失败，随后
  若队列非空则把键**入环尾**（running 留 true，由下一个取到的 worker 承接），
  若队列空则置 `running=false`（键离环且空闲）。

检查过的交错：
1. 执行中新任务到达：Submit 见 running=true 不入环；worker 收尾见队列非空入环——不丢、不重复。
2. 空闲键提交：running=false 才入环；连续两次提交第二次不入环——同键不会被两个 worker 取到。
3. worker 收尾入环与新 Submit 竞争同一把锁：要么 Submit 先（running 仍 true 不入环，由 worker 入环），
   要么 worker 先置 running=false（Submit 见 false 入环）——恰好一次入环，键不丢不重。
