# DESIGN

## 1. 规范化算法
流式维护三种待定物：(a) 行尾空白串 ws；(b) 一个待定 `\r`（下一字节可能是 `\n`）；(c) 已定稿输出。
普通字节到来：先刷掉待定 `\r`（按独立行尾），把 ws 判为行内并原样输出，再输出本字节。
空白进入 ws 缓冲。换行成立（`\r\n` 或独立 `\r`/`\n`）时清空 ws 并追加一个 `\n`。
流结束：待定 `\r` 是独立行尾；未遇到行尾的 ws 是行内空白（保留）。即先做“保留原末尾”的规范化 P(x)。

## 2. 末尾策略（对 P(x) 做，仅删/加 `\n`）
- Keep：输出 P(x)。
- EnsureOne：非空输入保证恰好一个末尾 `\n`（空 ⇒ 空，多个压成一个，没有则补）。
- TrimAll：删掉全部末尾空行；若删后仍有非 `\n` 内容，补恰好一个 `\n`。

| 输入 | Keep | EnsureOne | TrimAll |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

空输入三策略都选 `""`：EnsureOne 若对空补 `\n`，则 N("")="\n"，二次 N 无法回到 ""，破坏幂等；选 "" 则恒等。
TrimAll 对 `"\n"` 给 `""`（“删空行后无内容”），对有内容文本（如 `"a\n\n"`→`"a\n"`）补一个换行。

## 3. 被删字节的 ToOut（光标语义）
维护锚点：保留字节 b（原文 i、输出 o）⇒ ToOut(i)=o；区间间用“后继锚点”填充。
被删字节 i 的 ToOut(i) = 紧跟其后的第一个保留字节的输出位置；已到结尾则为 len(out)。
于是 `\r\n` 的 `\r` 映射到该换行输出 `\n` 的位置（换行**之后**的光标需落在 `\n` 之后 o+1，
即 `\n` 自己的锚点）；行尾空白映射到行尾 `\n` 的位置（换行之前）。
ToOrig：输出 o 是保留字节则回其原文 i；是间隙（如被删段对应的同一输出位）则取后继原文字节。
两端恒等成立：ToOrig 的像恰为所有保留字节的原文位置，ToOut 在这些位置上为原值；两函数均单调不减。
策略补出的 `\n` 无原文字节，给哨兵锚点 (orig=len(in), out=k)：它只作为其后间隙的后继，
ToOrig(k)=len(in)；ToOut(len(in))=len(out)。删除末尾 `\n` 时截断到最后一个存活锚点即可。

## 4. 缓冲超限（选择拒绝）
选择**拒绝**：待定空白可能是行内空白（必须保留），强制输出等于猜对时可行，但若下一字符是换行，
已输出的空白无法撤回，违反“删除行尾空白”（语义2）且输出含行尾空白，进而破坏幂等（语义5）。
拒绝只损失可用性，不产生错误结果：返回哨兵 `ErrWSOverflow`（带已扫描原文偏移），
已输出内容与映射保留可用，Normalizer 进入终态、再写返回 `ErrClosed`。

## 5. par 拼接
每段独立跑流式器但不 Close：取“未定稿状态”（输出、span、待定 ws、待定 CR、段消费长度）。
顺序拼接，段间只可能有两类携带物：carryWS（前段末尾待定空白）或 carryCR（前段待定 `\r`）。
- 下一段首字节 `\n` 且有 carryCR：CRLF 合成，丢弃该 `\n` 原文字节（ToOut 指向后一锚点），发出一个 `\n`。
- 否则有 carryCR：先发出一个 `\n`（独立 `\r`）。
- 下一段首个被刷出行内的字节前：carryWS 原样补发；若该段自身也以待定空白开头且最终为行内，
  整段可能 0 定稿，则把两段待定空白合并继续携带（因此携带量有上界：最多跨到下一个行尾/流末）。
段内 span 按 (origBase, outBase) 平移；丢弃/补发的少量字节在接缝处用与 norm 相同的锚点规则记录。
最后对拼接整体施加一次末尾策略。修正范围有上界：每个接缝至多回看 1 个 `\r` 与一段待定空白，
该空白在遇到段内第一个行尾或行内字节时立即消解，故总修正量 O(K)，与段长无关。
