# DESIGN：unified diff 子系统

## 1. 零计数 hunk 头的行号规则（语义 4）

统一 diff 的 `-a,b` 表示旧文件第 a 行起的 b 行（1 基、闭区间）。当 b=0 时区间为空，
插入点必须定位到「旧文件中该位置前面那一行」：故 a 取**前一行的行号**；若插入点在文件
最开头，前面没有行，a=0。这解释了题面三个样例：
* `x` → `y,x`（C=0）：插入点在文件最开头，前面 0 行，头为 `-0,0 +1`。
* `a,b,c` → `a,b,X,c`（C=0）：插在 c 前，前一行是 b（第 2 行），头为 `-2,0 +3`。
* 删除整文件 `a`（C=3）：新侧为空，插入点在第 1 行「之后」，前一行是 a，头 `-1 +0,0`。

Apply 解析：旧区间覆盖索引 `[a-b, a-1]`（0 基、闭区间；b=0 时为空区间，锚点在第 a 行
之后）。新侧规则完全对称。

| 旧侧形式 | 书写 | Apply 插入点（0 基行索引） |
| --- | --- | --- |
| b=0 | `-a,0`（不省略 0） | 恰在第 a 行之后，即索引 a（a=0 表示文件开头） |
| b=1 | `-a`（省略 `,1`） | 锚定/替换第 a 行，即索引 a-1 |
| b≥2 | `-a,b` | 替换第 a..a+b-1 行，即索引 a-1..a+b-2 |

之所以是「前一行行号」而非「将要插入的位置」：该格式行号永远指被描述区间内真实存在的
行，空区间不含任何行，只能借用紧邻其前的那一行命名；这样新侧非空区间与旧侧空区间仍共享
同一个锚点语义，解析器只需按上表换算，无需猜测。

## 2. hunk 合并阈值（语义 5）

结论：**g ≤ 2C 时合并，g ≥ 2C+1 时分开**。

每个 hunk 在改动块上下各取最多 C 行上下文。前 hunk 的后 C 行与后 hunk 的前 C 行覆盖中间
全部 g 行；当且仅当 g ≤ C+C=2C 时两份上下文重叠或相接，合并后中间未改行恰为 g 行，输出
与 GNU `diff -U C` 一致。
* 若阈值取 C：g=C+1 时两个 C 行窗口仍相接（共覆盖 2C≥g 行），GNU 仍合并，取 C 会误拆。
* 若阈值取 2C+1：g=2C+1 时两窗口间还空 1 行，GNU 输出两个 hunk，取 2C+1 会误并。

## 3. 并列最短脚本的选择（语义 3）

Myers 前向 V 迭代中，对同一 d 的每条对角线 k 取
x =（k==-d 或 (k!=d 且 V[k-1] < V[k+1])）时向右（插入），否则向下（删除）：
即并列时**先向下走，先删除（delete-first）**。因此 `a b` → `b a` 输出
`` `-a` ``、`` ` b` ``、`` `+a` ``：删除块整体排在插入块之前。行相等性按**含行尾的整行
字节**比较，故仅末尾换行差异也是一次删除加一次插入。迭代顺序完全固定，同输入生成 100 次
补丁字节相同。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

hunk 的记录位置基于原文件行号。维护已应用 hunk 累计位移 shift = Σ(新行数-旧行数)，
后一 hunk 的搜索中心 = 记录起点 + shift：前一 hunk 只可能平移其**后续**行号，平移量恰为
其新旧行数之差，与它实际落地的偏移 k 无关。在中心上下 F 行窗口内选上下文与删除行精确
匹配的候选，距离最小、平手取靠前。

## 5. 其余约定

错误分四类且哨兵可判定：`edit.ErrTooLarge`、`udiff.ErrFormat`（携带补丁行号的具名类型）、
`patch.ErrContext`、`patch.ErrFuzz`；字节数与 hunk 数上限违例也归为 ErrFormat。Apply 为
函数式：不改写入参，任一 hunk 失败整体放弃。计数器统计前向搜索中 snake 的逐行比较次数，
标准 Myers 总量上界 O((N+M)(D+1))。
