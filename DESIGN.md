# DESIGN — 区间索引与重叠查询

包依赖方向（单向，无反向）：`ival` ← `node` ← `tree` ← `query`。
所有区间均为左闭右开 `[Lo, Hi)`，合法区间为 `Lo <= Hi`；`Lo > Hi` 是可判定错误。

## 一、相接算不算重叠

把区间看作点集：`[a,b) = {x | a <= x < b}`。定义**重叠 = 交集非空**：

```
Overlap(A,B)  <=>  max(A.Lo,B.Lo) < min(A.Hi,B.Hi)
```

- `[1,3)` 与 `[3,5)`：`max(1,3)=3`，`min(3,5)=3`，`3 < 3` 不成立 → **不重叠**。
- 点查 `3`：`3 ∈ [3,5)`（左闭），`3 ∉ [1,3)`（右开）→ 命中 `[3,5)`，且只命中它。

与「不漏不多」的一致性：朴素线性扫描与树查询使用**同一个** `Overlap` 谓词
（`ival.Overlaps`），树查询只是用子树最大右端点做剪枝，剪枝条件
（左子树 `MaxHi <= q.Lo` 时跳过；当前节点 `Lo >= q.Hi` 时跳过右子树）只排除
「按该谓词必然不重叠」的子树，因此返回集合与扫描结果恰好相等。

## 二、零长度区间

`[2,2) = {x | 2 <= x < 2} = ∅`，是空点集。三个问题的自洽回答：

1. **能插入吗？** 能。`Lo <= Hi` 合法，插入只改变多重集的成员计数，
   不参与任何查询结果。拒绝它反而会无端缩小合法域。
2. **能被点查命中吗？** 不能。`Contains(p)` 即 `Lo <= p < Hi`，空集不含任何点。
3. **能被 `[1,5)` 的重叠查询命中吗？** 不能。空集与任何集合交集为空：
   `max(2,1)=2 < min(2,5)=2` 不成立。注意它连与自己都不重叠。

三者自洽：插入/删除维护的是「多重集成员关系」，查询回答的是「点集关系」，
空点集在点集层面不可观测，在成员层面可计数、可删除。

## 三、结果排序规则

不变量 3 要求：(a) 同一查询重复执行逐位相同；(b) 与插入顺序无关。

推导：结果若与插入顺序无关，则它只能是**区间多重集本身的函数**，不能依赖
树的形状、插入历史或遍历顺序（AVL 中序遍历顺序依赖插入历史，排除）。
多重集的函数要逐位确定，最自然的做法是选定一个**全序**并排序输出。
取字典序 `(Lo, Hi, Payload)`：先比左端点，再比右端点，再比载荷字符串。
该序是 Go 可比较字段上的全序，排序结果唯一，天然满足 (a)(b)。

多重集中**完全相同**的两个区间：它们的三元组相等，排序后谁前谁后不可区分，
输出的字节序列与这个选择无关——交换两个相同元素不改变序列。故确定性保持。

## 四、删除哪一个

多重集中有三个完全相同的区间，`Delete` 删掉「搜索路径上遇到的第一个
等键节点」（具体是哪一个物理节点由树形决定，不对外承诺）。

论证不影响后续查询：查询结果是区间多重集的纯函数（见第三节），
三个相同区间删掉任何一个，剩下的多重集完全相同，故任何后续查询结果
逐位相同，与删除次序无关。自检方法 `Check` 只校验结构不变量
（MaxHi、树高、节点数），同样不依赖被删的是哪一个。

## 五、区间关系 → 是否重叠对照表

| 情形     | 示例 A  | 示例 B        | Overlap | 说明                 |
|----------|---------|---------------|---------|----------------------|
| 相离     | `[1,2)` | `[3,4)`       | 否      | 交集为空             |
| 相接     | `[1,3)` | `[3,5)`       | 否      | `3<3` 不成立         |
| 部分重叠 | `[1,4)` | `[3,5)`       | 是      | 交集 `[3,4)`         |
| 包含     | `[1,5)` | `[2,3)`       | 是      | 交集 `[2,3)`         |
| 完全相同 | `[2,4)` | `[2,4)`       | 是      | 非空时与自身重叠     |
| 零长度   | `[2,2)` | 任意（含自身）| 否      | 空点集与一切交集为空 |
