# LZ77 流式压缩设计

## 流格式
流 = 头 + 记录* + 尾。记录类型在首字节低 2 位：

| 记录 | 标签 | 布局 |
|---|---|---|
| 头 | `OH` | `'L','Z'` + uvar 版本(1) + uvar 窗口容量 |
| 字面量 | `00` | tag(00) \| len<<2；随后 len 个原始字节 |
| 回指 | `01` | tag(01) \| (len-3)<<2；随后 uvar distance（≥1） |
| 刷新 | `02` | 单字节，标记此前字节构成完整前缀，无数据 |
| 尾 | `03` | tag(03) \| (totalLen)<<2；随后 uvar CRC32(IEEE) |

所有整数为无符号 LEB128（10 字节上限，溢出报错）。len 字面量≥0；回指 len≥3，故编码 len-3。

## 第3条：与 Write 切法无关
Write 只追加到一个“未决缓冲”，不触发任何匹配判定；匹配只在 Flush/Close 时对自上次 Flush 起的
全部新字节做一次贪心扫描。扫描器从偏移 0 顺序推进：在位置 p 向 Matcher 查询“以 p 开头、相对
当前已纳入窗口前缀 p 的最长匹配”，取到匹配 (d,l) 就发回指并整体前进 l，否则发字面量前进 1。
任何匹配判定只依赖“位置 p 之前的全部字节（含更早 Flush 留下的历史）”和“从 p 起整块缓冲的
内容”，与这些字节分几次 Write 到达无关，因此输出逐字节相同。Flush 强制把未决缓冲全部扫描
并入窗口并写刷新标记：它强制结束的不是“半个匹配”（Write 从不产生半个匹配），而是保证此刻
所有输入都已被确定性地解释，解压器凭此得到完整前缀。

## 第2条：重叠回指
距离 d<长度 l 时，目标尚未被全部复制，源在“当前输出向前 d 字节”处随复制推进而变化。
Window.CopyFrom 逐字节语义：源索引随 copied 增长为 (w-d+copied)。实现上先取 k=min(l,d)，
把 [w-d,w-d+k) 一次拷到末尾并推进 w；此后剩余部分的源索引都已落在窗口已有区，可整段批量
拷贝。d≥l 时直接一次性拷贝。故最多 d 字节走重叠语义，其余为块拷贝，与朴素逐字节等价。

## 第7条：两种距离错误
“distance > 已输出字节数”：引用了根本不存在的数据，正确流在任何阶段（含块首）都不可能出现。
“已输出 ≥ distance > 窗口容量”：引用字节存在于逻辑历史但已滑出环形窗口，说明流是用更大
窗口压的或被篡改。压缩器只向窗口内（距离≤容量）发回指，故后者对合法流不可能；二者必须可
区分。并行块预置字典只是把上一块末尾 cap 字节预先放入本块窗口，本块发出的距离仍≤cap，且
解压端在块首前已经真实输出过这些字节，所以“已输出字节数”的含义不变：永远等于此前流中
字面量+回指产出的总字节。

## 第9条：并行确定性
块 i 的压缩只读取本块原文与块 i-1 原文末尾 cap 字节（构造独立 Matcher、预置字典），不读取
任何其他块，也不读取其他块的压缩输出；纯函数，故 goroutine 数、调度顺序不影响每块字节。
衔接：非末块压缩结果末尾追加刷新标记（块间边界即 Flush 边界，解压器在每个标记处输出完整
前缀），末块以尾记录结束；头只写一次。workers 仅影响并发度，输出按块序号拼接，四种值相同。

## 复杂度
Matcher 每位置沿哈希链最多考察 chainLimit 个候选，candidateProbes 计数；全同字节每位置至多
chainLimit 次且与窗口规模无关。回指可一次覆盖整块（len 不封顶），故 4MB 全同字节输出极小。
