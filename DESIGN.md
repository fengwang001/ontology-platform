# 设计推导

## 规则 A：队首取消后必须执行成组唤醒

事实：取消只让一个等待者离队，额度本身没有变化，但 FIFO 闸门变了。

反例（只在 `Release` 里唤醒），设 Cap=10：
1. A:Acquire(9) 立即满足，used=9，avail=1。
2. B:Acquire(5) 排队，成为队首（avail 1 < 5）。
3. C:Acquire(1) 排队；队首 B 未满足，FIFO 下 C 不得插队。
4. B 的 ctx 超时，B 离队。avail 仍为 1，但此刻已足够满足 C。
5. 此后无人 `Release`（A 长时间持有）。唤醒只挂在 Release 上，C 永远不会被检查，
   只能挂到自己的 ctx 超时——而额度其实一直够用。

结论：取消路径摘掉队首节点后，必须与 Release 走同一段「从队首顺序满足，
遇到第一个满足不了的就停」的唤醒流程。摘掉的是中间节点时，队首未变、
闸门未开，结果自然是「零个满足」，调用同一流程即可，不需要特判。

## 规则 B：取消与满足竞态的裁决

等待者被唤醒时执行：`grant(n)` 先在锁内把 used 加上 n 并关闭其 ready，
再释放锁；等待者从 select 醒来。于是 ctx.Done 与 ready 只有两种真实状态：

- 额度尚未划走：ctx 取消走取消分支，离队并触发规则 A 的唤醒，返回 `ctx.Err()`。
- 额度已经划走（ready 已关闭）：即使 ctx 同时处于 Done，也必须返回成功。

关键裁决：select 同时就绪时若选择「返回错误」，n 已经计入 used，
而调用方拿不到成功就不会 `Release(n)`，这 n 份额度永久泄漏（违反不超发的
账本一致性与「取消零副作用」）；若在错误分支再退回额度，则与另一分支可能的
成功返回形成双重计入/双重释放。因此裁决为：**以锁内原子状态为准——
已划额度就成功，未划额度才取消；成功后 ctx 是否已取消一律无关，
调用方负责之后 Release**。额度转移与离队在同一把锁内完成，
不存在「划了一半」的交错。
