# 条件请求前置判定器 — 设计推导

三个包单向依赖：`precond` → `etag` / `httpdate`，后两者互不依赖。
结果三态：`Continue` / `NotModified` / `Failed`（前置条件失败）。

## 1. 为什么 If-Match 用强比较、If-None-Match 用弱比较

`If-Match` 的典型用途是断点续传 / 范围请求与乐观并发写：客户端要断言
「服务器上的字节序列和我手里这份逐字节一致」。弱标签 `W/"x"` 只承诺
语义等价，不承诺字节一致（例如代理对同一资源做了 gzip 重编码，实体
标签保留弱形式但字节流已变）。若 `If-Match` 用弱比较，客户端拿着编码
A 的前 1MB 去续传编码 B 的后半段，两个字节流会被拼接成损坏文件——
弱比较放行了本应收到的 412。因此 `If-Match` 必须强比较：任一方为弱
标签即判不等。
`If-None-Match` 的用途是缓存再验证：客户端问的是「我这份还能不能当
缓存用」，语义等价就足够，弱标签表示的正是语义等价，故用弱比较。
若两边都用弱比较，断点续传场景就会静默拼接出损坏文件；若两边都用强
比较则会让缓存再验证大量误报未命中，浪费带宽但不出错——所以不对称
选型是「强给 If-Match、弱给 If-None-Match」。

## 2. 为什么 If-Match 存在时忽略 If-Unmodified-Since

两个头部都在守卫同一个「前置条件失败」出口，但 `If-Match` 是客户端
显式给出的精确断言，时间戳断言只是它的粗粒度替代品（秒级精度）。规范
（RFC 7232 §6）因此规定：`If-Match` 存在时 `If-Unmodified-Since` 必须
被忽略。若叠加判定（两者都判、任一失败即 412），反例输入：
`If-Match: "v"` 与当前标签强比较相等，但 `If-Unmodified-Since` 给的时刻
早于 LastModified。叠加判定返回 412，而规范要求放行（`If-Match` 单独
决定，已通过）——结果与规范相反。组间同理：`If-Match` /
`If-Unmodified-Since` 组整体优先于 `If-None-Match` / `If-Modified-Since`
组，因为前者的失败语义（412）优先于后者的缓存优化语义（304）。

## 3. 为什么同一「匹配」在读/写方法下结果不同

`If-None-Match` 匹配的含义是「资源的当前表示与客户端持有的一致」。
对 GET/HEAD，客户端只是想省流量：一致就意味着缓存可用，应答 304
（NotModified）即可，无需再传实体。对 PUT/POST/DELETE，客户端是在
做条件写：「仅当资源不再是我看到的那个版本才拒绝改」——匹配说明
资源自客户端上次读取以来没有按预期变化的前提不成立，必须 412 防止
丢失更新。同一条件、两种意图，因此必须是两种可区分的结果。

## 4. 相等时刻为什么算「未修改」

HTTP 日期只有秒级精度。`If-Modified-Since` 的语义是「自我手上这份的
时间戳之后，资源变过吗」。LastModified 与给定时刻相等，只表示「在我
能看到的最细粒度上没变」——同一秒内的变化根本无法用该头部表达。
若相等算已修改，客户端拿到 200 + Last-Modified 后立刻再验证，永远
得到「已修改」，缓存再验证机制失效。故相等归入未修改：
`If-Modified-Since` 仅当 LastModified **严格晚于**给定时刻才算已修改；
对偶地，`If-Unmodified-Since` 在相等时通过。

## 附：其他裁决

- 条件头部解析失败（含四类时间错误、标签语法错误）→ 忽略该头部，
  不整体报错；未来时刻的日期头部同样忽略。
- 时钟仅在日期头部存在且解析成功时调用一次（用于忽略未来时刻）；
  无条件头部时调用计数为 0。
- `Evaluate` 只读入参、纯函数，同一输入任意次求值结果相同。
