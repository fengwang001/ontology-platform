# 设计推导

## 1. 零计数 hunk 头的行号（语义 4）

unified diff 头 `@@ -a,b +c,d @@` 中 a、c 恒为 **1 基起始行号**；b、d 为计数，计数 1 时省略 `,1`，计数 0 时**不省略**。
计数 0 表示该侧不占任何行，没有「自己的行号」，统一规则是：**a（或 c）取该段紧邻的前一行的行号；文件开头之前没有行，则为 0**。

- `x\n` → `y\nx\n`（C=0）：纯插入，位于第 1 行之前，前一行不存在 ⇒ 旧侧 `-0,0`；新内容占第 1 行 ⇒ `+1`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）：插入位于旧第 2 行之后、第 3 行之前 ⇒ 前一行是 2 ⇒ `-2,0`，新侧 `+3`。
- `a\n` → 空（C=3）：纯删除占旧第 1 行 ⇒ `-1`；新文件为空，前一行不存在 ⇒ `+0,0`。

`Apply` 解释：旧侧 `-a,0` 时，插入/锚点位置取**基于 0 的索引 a**（即第 a 行之后）；新侧 `+c,0` 同理用于反向应用。

| 旧段计数 b | 书写 | Apply 锚点（0 基，指该行之后） |
|---|---|---|
| 0 | `-a,0`，a=前一行行号（开头为 0） | 索引 a，即第 a 行之后插入 |
| 1 | `-a`（省略 ,1） | 索引 a-1 起匹配 1 行 |
| ≥2 | `-a,b` | 索引 a-1 起匹配 b 行 |

## 2. hunk 合并阈值（语义 5）

GNU `diff -U C`：两个 hunk 各自带 C 行上下文，相隔未改动行数为 g。分开时两 hunk 间空隙为 g 行；合并只需中间保留一份 C 行上下文。
当 **g ≤ 2C** 时合并：此时相邻上下文（各 C 行）相接或重叠，GNU 必然合并；**g = 2C+1** 时中间恰有 1 行无法纳入任何 hunk，必须分开。
故阈值为 **2C**（与「g ≤ 2C+1」「g ≤ C」对照：前者在 g=2C+1 时会错误合并本应分开的两 hunk；后者在 g=C+1 时错误分开本应相邻的两 hunk，均与 GNU 不符）。

## 3. 并列最短脚本的选择（语义 3）

Myers 前向遍历中同一步可向右（插入）或向下（删除），回溯并列时**优先向下 = 优先删除**。
因此 `a b` → `b a` 固定得到 `-a`、` b`、`+a`（删除行排在对应插入行之前），而不是 `+b`、` a`、`-b`。重复运行逐字节一致。

## 4. 偏移后后续 hunk 的起点（语义 7）

维护累计漂移 `drift`，初值 0。第 i 个 hunk 的查找起点为「记录的旧起始 + drift」，在 ±F 行内取与上下文+删除行精确匹配处；多候选取距起点最近，同距取靠前。
hunk 在位移 off 处应用成功后：`drift = off + (新侧行数 − 旧侧行数)`。
理由：前一 hunk 实际比记录位置下移 off 行，且其插入/删除行数差改变了后续所有行号；后续 hunk 必须同时平移这两部分，否则同一补丁在均匀插入的文本上会逐 hunk 累积错位（GNU patch 的 line offset 语义相同）。
