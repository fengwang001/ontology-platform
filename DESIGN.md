# 并行任务图调度与失败传播 — 设计

## 1. 包结构（标准库，进程内存）

- `graph`：任务图构建、重复边幂等、环检测（DFS 染色，回溯出真实环）、Kahn 拓扑分层。
- `fail`：状态机 `Tracker`，四类状态、原因链、两种传播模式；错误哨兵与状态枚举。
- `sched`：有界信号量（并发上限）+ ID 升序就绪堆；非导出计数 `peakRunning`、`readyDecisions`。
- `exec`：单循环调度器（唯一调度决策点，O(V+E)），任务 `panic`/取消后写回由状态机丢弃。
- `report`：按任务 ID 字典序逐字节确定地渲染报告。
- `cmd/demo`：不读参数不联网，逐条判定并打印 OK/FAIL。

## 2. 四类最终状态的判定推导

对每个任务记生命周期：`Pending → Ready → Running → 终态`。

1. **成功 `Success`**：进入过 Running，且在全局首个失败被记录之前（或快速失败的取消信号对其生效之前）正常返回 nil。终态不可回滚、不可改标。
2. **失败 `Failed`**：进入过 Running，自身返回 error 或 panic（recover 包装为带原始信息的错误，`errors.Is(err, ErrPanic)` 成立）。保留原始错误。
3. **跳过 `Skipped`**：终局时仍处于 Pending/Ready、从未进入 Running 的任务。跳过原因必须指向**可达它的、ID 字典序最小的失败任务**（“最初/根源失败”；A→B→C 中 C 的原因是 A 而非 B）。这是按最终失败集做的确定性判定，与完成时序无关。
4. **取消 `Canceled`**：失败发生时已处于 Running、随后响应 ctx 被取消而中止的旁支任务。与 Skipped 的唯一区分依据是“是否进入过 Running”：Running 中收到取消为 Canceled；还没运行为 Skipped。任务在 ctx 取消后若仍写回，状态机发现自身已不在 Running，写回一律丢弃。

竞态说明：旁支任务若在取消信号生效前已写回成功，则记 Success；demo/测试用阻塞到取消的任务稳定制造 Canceled。

## 3. 两种模式

- **快速失败 FastFail（默认）**：记录首个失败后立刻 cancel 整个 ctx。失败任务的全部下游标 Skipped（原因取根源失败 ID）；其余正在运行的任务因 ctx 取消而 Canceled；尚未运行的独立任务也标 Skipped，原因同样指向根源失败（根源先于一切传播）。
- **尽力而为 BestEffort**：不取消全局 ctx。只有直接或间接依赖某失败任务的任务在轮到它时标 Skipped；不含失败祖先的分支继续跑到完成。跳过原因按可达失败任务中 ID 最小者确定，保证可复现。

多任务同时失败：两种模式都记录全部失败任务；每个被跳过任务的原因是其可达失败集合里 ID 最小者，即各自最早的上游根源。

## 4. 复杂度与并发证明

调度循环是单一 goroutine：从就绪堆（ID 升序）取任务，受信号量与完成事件驱动。每个入堆/出堆计一次“就绪判定”，总数 ≤ 2·完成数 + 2·入队数 ≤ 4(V+E)，不存在每次完成全图扫描。`peakRunning` 在同一决策点更新，≤ 并发上限。所有 worker 由唯一 WaitGroup 跟踪，循环终止于“终态任务数 == V”，此时全部 goroutine 已 Join，无泄漏。

## 5. 确定性

就绪堆按 ID 升序；报告按 ID 字典序；原因只依赖最终状态集合（不依赖完成先后）；跳过原因用 ID 最小的失败根源。故边添加顺序打乱、随机完成延迟都不影响报告字节。

## 6. 边界

空图得空报告；单任务；全独立；1000 层链用迭代 Kahn（无递归）；重复加边幂等；引用不存在节点返回可判定错误；并发上限 1 退化为串行且报告与并行一致；环（含自环）在执行前报出，每条边均在输入图中且首尾闭合。
