# DESIGN — 带背压的流式聚合管线

仅标准库、状态在进程内存，链路：source → parse → group/agg → sink。

## 1. 关键推导：检查点里的「已消费位置」记到哪

三级有界队列：parse 输入 `q0`、聚合输入 `q1`、sink 输入 `q2`。
记记录序号为单调递增的 `pos`。

- **记 source 读取位置（错）**：`pos` 已从 source 读出但可能还在任一队列里未落盘；
  崩溃后从 `pos+1` 重放，在途记录永久丢失。
- **记 sink 落盘位置（错）**：sink 已落盘到 `k`，但 `k+1..n` 可能也在队列中且其聚合
  结果尚未随检查点保存；若直接从 `k+1` 重放而旧输出不清理，会产生重复输出；
  若从更后的位置重放，又会丢。

**正确定义**：已消费位置 = 「全局屏障（barrier）水位」。管线按批次推进，批次边界插入
屏障标记。屏障穿过全部三个阶段、sink 把屏障之前所有结果原子落盘后，令在途集合为空
（`q0,q1,q2` 均无该屏障之前的记录），此刻才把检查点推进到屏障位置，并与当时各组的
中间聚合一起原子写出。

形式化：设屏障序号 `b`。当且仅当 sink 确认 `pos <= b` 的全部聚合结果已 fsync+改名，
且解析/聚合已把 `<= b` 的状态并入快照，检查点 `CP = {Pos: b, Groups: agg(1..b)}`。
恢复时：删除 sink 残留临时文件，从 `Pos+1` 向 source 请求续读，聚合状态初始化为
`CP.Groups`。输出文件也只在屏障点整文件原子替换，恢复后输出 = 上次屏障落盘内容 +
重放结果；屏障之后、崩溃之前的半成品（残留 tmp）一律不存在，故不丢不重。非屏障点的
sink 临时文件崩溃：临时文件被清理，从上一屏障重放。

## 2. 背压与资源约束

- 阶段用通用骨架 `stage.Stage`：有界 channel 作队列，发送方满则阻塞，阻塞沿
  parse→source 反向传导。非导出 `maxInFlight` 记历史最大在途数（本阶段队列+处理项），
  `blocks` 记发送阻塞次数。
- 三阶段队列容量各 `C`，任一屏障前在途总数上界 `3C+3`（每阶段至多 C 排队 + 1 处理）；
  source 全速 5 万、sink 每记录 sleep 1ms 时断言上界成立且 `blocks>0`。
- 分组数硬上限 `MaxGroups`：新键且已满即返回哨兵错误 `ErrTooManyGroups`，管线停止、
  已处理部分落盘并上报，绝不静默丢组。

## 3. 故障模型

- 坏记录（缺键/解析失败）计入 `Bad`，不进任何分组，管线继续。
- source 中途报错：屏障排空在途后落盘+写检查点，错误原样返回。
- sink 半成品：输出只走 `tmp → fsync → rename`；恢复时识别同目录固定前缀 `*.tmp`
  并删除。临时文件逐字节截断的每个截断点都不得被读为有效输出（读取只认最终文件名+
  校验和行）。
- 检查点保留最近两份 `ckpt.001/ckpt.000`，每份末尾附 SHA-256；校验失败回退较旧一份，
  报告 `Fallback=true`。

## 4. 并发与优雅停止

`Stop()` 关停止通道并等待全部 goroutine 退出，幂等（sync.Once）；未启动时调用安全。
在途记录排空后退出，测试以 `runtime.NumGoroutine` 停止前后回基线作准。

## 5. 记录与输出格式

记录行 `key|value`（整数 value）；空键合法，无 `|` 为坏记录。输出每屏障整体重写：
逐行 `key count=n sum=s`，按 key 排序，末行 `sha256=<hex>`。

## 6. 包划分（6 包，8 个 .go 文件）

source（可注入源：速率/报错/提前结束/起始位置）、parse、stage（骨架）、
sink（原子写+校验和+临时清理）、ckpt（双份检查点+回退）、pipeline（根包编排）、
cmd/demo。测试表驱动、按包合并，根包一张大表覆盖全链路。
