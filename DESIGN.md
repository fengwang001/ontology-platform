# 设计推导

## 1. 只含空白的续行

`java.util.Properties` 在读到“反斜杠 + 行终止符”后，会进入下一个物理行，并先循环跳过
空格、制表符和换页符，然后才继续读取逻辑行内容。

因此，续行首部空白不是被保留后再裁剪的普通值片段，而是在续行边界上执行的词法步骤：

- 下一物理行含有前导空白和普通字符时，只剥掉空白，普通字符接到上一行。
- 下一物理行为空或只含空白时，这一遍跳过会越过整行；该行又没有非空白字符，更没有可转义
  下一行终止符的反斜杠，所以逻辑行结束，追加内容为空。
- 若空白之后还有一个位于行尾的反斜杠（如 `"  \\"`），它不是“只含空白”的行；剥掉前导空白后
  仍能看到该反斜杠，于是续行链继续。

结论：`"k=a\\\n   "` 得到 `k -> a`；`"k=a\\\n  \\\\\nb"` 得到 `k -> ab`。

## 2. Store 的最小必要转义

回写每一项使用 `key=value\\n`，转义只用于避免 Load 改变词法边界：

- 反斜杠总是转义为 `\\\\`，因为它会引入转义或续行。
- `\\n`、`\\r` 不能原样出现，否则物理行被截断；写为 `\\n`、`\\r`。
- 键中的空格、制表符、换页符、`=`、`:` 会终止键，因此必须转义；键首字符为 `#` 或 `!` 时，
  整行会被识别成注释，因此只在首字符转义这两个字符。
- 值在分隔符之后：其中的空格、制表符、`=`、`:`、`#`、`!` 都是普通字符，所以不必转义；但
  Load 找到分隔符后会先跳过一段空白，故值的第一个空白字符必须写成 `\\ ` / `\\t` / `\\f`。
- 非 ASCII 字符按 UTF-8 原样输出；解析器按字节拼接后由字符串保留 UTF-8 编码。

## 3. 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待补齐 | 待补齐 |
