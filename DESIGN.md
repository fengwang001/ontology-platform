# 设计推导（先于代码）

## 1. 空行与单列空值
选择：**空行是一条记录**，含一个未引号空字段。RFC4180 允许末换行不产生记录，但两个相邻换行之间无法与“空值记录”区分；跳过空行会丢数据且不可往返。故 `\n` 一律结束当前记录（首字节即 `\n` 时记录含一个空字段），仅流尾的最后一个换行不产生多余记录（`Close` 时处于“记录起点且未消费字段”则不发记录）。
回写：单列空值记录写为「空内容 + 换行」，即两个连续换行。不加引号不会丢数据——空行本身就是它的表示；但加引号的空字段 `""` 必须写成 `""`，否则 `Parse` 得到未引号空字段，引号标记丢失（违反逐字段相等）。因此 writer 对 `Quoted=true` 的字段一律回写引号。

## 2. par 切点
切点可落在任意字节。每个段先在两种假设下各跑一遍同一状态机：A=段起点在引号外记录起点(FieldStart)；B=段起点在引号字段内(Quoted，且第一个引号视为内容)。A 运行时额外用 1 位记录「首个分隔符前是否出现裸 `"`」。从左到右由前段实际结束状态择一：
- 结束在引号内：取 B；段内坐标即全局 `off+segBase`，记录号、字段号以已完成数为种子。
- 结束在 CR 待定：下段首字节决定；`\n` 则丢弃 A 重复产出的空字段（首字段 Start==segBase 且为空且未引号），否则立即报 ErrBareCR。
- 结束在引号外：取 A；若前段有跨段未完成字段（midUnquoted），把 A 第一个字段与前段残部拼接（值拼接、Start 用残部起点）；若 A 在首分隔符前标记了裸 `"`，对残部非空的情形立即报 ErrBareQuote。
A/B 各扫一遍，故总处理量 ≤ 2N + 4K（CR 待定仅看 1 字节，不重扫）。错误位置 = 段内 off + segBase；记录号/字段号 = 段内号 + 前段已完成号。

## 3. CR 待定
未引号态见 `\r` 进入 sCR，不发字段：下个字节 `\n` 才发字段+记录；非 `\n`（含 EOF）报 ErrBareCR，偏移指向该 `\r`，其字段内容不含 `\r`。半包：`\r` 留在机内状态，`Feed` 返回后暂停，续传字节判定。par：CR 为段末时结束状态携带 CR 待定，由下段首字节按上节处理；`\r\n` 被切开时 `\n` 不产生空记录。引号态内 `\r` 是普通内容，无待定。

## 4. 上限「立刻」
字段计数 = 解码后内容字节数，引号态内 `""` 两个输入字节只追加一个 `"`，在追加后立即检查，故第 max+1 个内容字节（转义对即第二个引号）到达瞬间即 ErrFieldTooLong，不缓冲后续。字段数：每遇分隔符立即计数检查；记录数：一条记录完成（换行确定）瞬间检查。报错即终态：机内 frozen，后续 Feed/Close 返回同一错误；已发完整记录保留。

## 5. 规范输入（canonical）
定义：分隔符 `,`；记录以 `\r\n` 结束，末记录可有可无行尾；字段仅在包含 `,` `"` `\r` `\n` 或原本 Quoted 时加引号，引号写作 `""`；不加引号的字段不含上述字节；空行即单列空值记录。对此类输入 `Write(Parse(x))==x`。

## 6. 状态转移表（动作：f=发字段，r=发记录结束，+=累加，e=错误）
| 状态 \ 输入 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| FieldStart | f,→Unq | →Q(quoted) | →CR | f,r→Start | +→Unq |
| Unquoted | f,留Unq(新字段Start) | e BareQuote | →CR | f,r→Start | + |
| Quoted | + | →QQuote | +(保留) | + | + |
| QQuote | f,留Unq | +→Q | +(保留CR,→Q) | +(保留\n,→Q) | e QuoteNotClosed-after |
| CR-pending | (由下字节判定) | e BareCR | e BareCR | f,r→Start | e BareCR |
EOF：Q/QQuote→ErrQuoteNotClosed；CR→ErrBareCR；Start 且无字段→不发记录；其余 f 后结束。
