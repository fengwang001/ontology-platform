# 带背压的流式聚合管线 — 设计推导

## 1. 拓扑

`source → parse → group/agg → sink`，每两级之间一个**有界 channel**。
背压即 channel 满时发送方阻塞，压力自然传导回 source。source 记录被阻塞次数。

记录：`Key string; Val int64; Off int64`（source 单调偏移，1 起）。
source 记录格式 `key:value`；无冒号或缺 value 为坏记录；`:5` 是合法空键。坏记录消费偏移、计数，不入组。

## 2. 核心问题：「已消费位置」记到哪

记 source 读位置：崩溃时队列中在途记录尚未落盘，恢复从其后重放 → **丢失**。
记 sink 落盘位置：上游队列可能仍压着已落盘记录，恢复会重放它们 → **重复**。
两者都错。正确定义：

> 已消费位置 = 已落盘记录中**最大的、且其之前无在途记录**的偏移，即一次「在途集合清空」时确认的水位（barrier）。

在途集合只增不减，无法在流水线中实时观察「清空」，故采用**屏障（barrier）**：
周期性（每 B 条）注入带偏移的 barrier，它沿各级队列 FIFO 下行；
sink 收到 barrier 时，屏障之前的所有记录必然都已原子落盘（在途集合此刻为空）。
此时按顺序：fsync 输出 → 原子改名 → 写检查点(off, 各组聚合, bad)。
崩溃后：检查点偏移 c 之前的数据**恰好一次**在输出中；重放从 c+1 开始，不丢不重。

崩溃点分析：
- source 读完、队列非空：最新 barrier 未到 sink，检查点停在更早水位，在途全部重放，不丢。
- 聚合中途：组状态只在 barrier 提交后才进检查点，未提交状态重建，不重。
- sink 写临时文件未改名：正式输出仍是上一版；恢复删除残留 temp，从上一检查点重放。

## 3. 输出确定性（保证「逐字节相同」）

sink 缓冲本 barrier 周期内各组的 `(key,sum,count)`，提交时按 key 排序，
写**全量**快照（带格式版本与 CRC32）到 `out.tmp`，fsync 后原子 rename 为 `out`。
因此输出是「截至已提交偏移的完整确定性快照」，与崩溃点、重放次数无关。
EOF 时再做一次屏障提交，保证尾部落盘。

## 4. 检查点

保留 `ckpt.1`（最新）、`ckpt.2`（上一版），内容含版本、off、bad、map、CRC32。
先写 `ckpt.tmp`+fsync，再 rename 到 ckpt.1（旧 1 轮转为 2）。
恢复取第一个校验通过者；都坏则从 0 开始；发生回退在报告中标注 `FellBack`。

## 5. 资源与停止

- 历史最大在途数：非导出计数器按采样（各队列 len + 各组缓冲）记录 max，
  受 `len(q1)+len(q2)+len(q3)` 与「未满一 barrier 的缓冲」约束，不随总量增长。
- 组数硬上限：新 key 使组数超限时返回哨兵错误并停止管线（不静默丢组）。
- 优雅停止：收到信号后关闭输入、排空在途、提交最终 barrier、关闭各 goroutine；Stop 幂等。
- 崩溃模拟：hook 触发时立即 os.Exit（子进程模式），父进程重新 Run 并比对输出。
- temp 识别：只认 `out.tmp`/`ckpt.tmp` 为本轮未完成文件，恢复时先删除，绝不读回。

## 6. 包划分（共 6 实现文件 + 3 测试文件）

`source`（可注入数据源，速率/报错/提前结束）、`parse`（解析+坏计数）、
`stage`（有界队列、管线编排、屏障、停止、在途/阻塞计数）、
`sink`（temp+rename+CRC）、`ckpt`（双槽+回退）、`cmd/demo`（自判定演示）。
测试按包合并：`source_test`、`stage_test`（含崩溃恢复/停止）、`sink_test`（含截断/ckpt 回退）。
