# 有预算图遍历器设计

## 1. 预算计量单位：访问的节点数

预算 = 本次允许**访问（输出）的节点数**。原因：节点数是语义输出的长度，
与图如何存储无关。若以「考察的边数」为单位，截断点会随存储形态漂移：
同一个逻辑图，邻接表重复存一份边、多存若干死边、或改用不同的边压缩布局，
预算消耗速度就不同，从而在不同的节点处截断；自环/重复边也会吃掉预算却不
产出节点。节点是去重访问的，计数稳定，因此选节点。

## 2. 截断确定性必须固定的三处顺序

1. 出边顺序：邻接表按目标 ID 字典序排序，展开时按序扫描。
2. 同层入队顺序：按「父节点在本层的次序 × 其出边的字典序」入队，
   即标准 BFS 逐层从左到右。不依赖任何 map 迭代序（邻接表在建图时排序）。
3. 跨段顺序：续点序列化时 visited 与队列都按规范次序写出，解码后逐字节
   唯一；因此同图、同起点、同预算的任意分段，拼接结果唯一。

## 3. 续点必须包含整个队列 + visited 集合

只记「最后访问节点」无法恢复 BFS：BFS 的待办分散在整层——上一层未展开完
的游标、当前层尚未访问的兄弟节点，以及已入队未访问的下一层节点。因此
续点字段为：

- `cur`: 本层仍需继续展开的节点及其出边游标 `(node, cursor)`，按层序；
- `pending`: 已由 cur 入队、尚未访问的本层剩余节点，按层序；
- `next`: 展开 cur 时发现并已标记 visited、属于下一层的节点，按发现序；
- `visited`: 全部已访问节点集合（序列化时按字典序规范化）；
- `done`: 是否完成。

恢复后从游标继续，语义与从未中断完全一致。预算为 0 的续点即初始续点
（cur=[start@0], pending=[start], next=[], visited={}），续传它幂等。

## 4. 惰性展开与队列驻留上界

不能把一个节点的全部出边一次性入队（星形图会瞬间驻留 10000）。算法维持
「层界」：每次只把 cur 头部节点的出边游标推进到「够补足下一个待访问节点」
为止，再访问一个 pending 节点。同层所有 cur 展开完毕、pending 取空后才换
层（cur=next 并清空 next）。

驻留量：每一层节点数至多为已访问数 v，故 cur+next 合计不超过 2v，
pending 不超过 v，峰值 ≤ 3v；规范取上界 `4*budget`。

## 5. 复杂度与计数器

- 访问节点数 = min(预算, 可达节点数)，计数器 `visitedCount` 精确断言。
- 每条边最多被考察一次：游标单调前进，计数器 `edgesExamined` ≤
  Σ(已访问节点的出边条数)。
- 峰值队列计数器 `peakQueue`，星形图（1 中心 + 10000 叶，预算 100）
  断言 ≤ 400。

## 6. 续点编码与三类错误

编码为自定义紧凑二进制：magic + done + 三段有序节点序列 + 排序 visited，
尾部附 FNV-1a 64 位校验和。解码顺序：

1. 长度/结构解析失败 → `ErrCorruptCheckpoint`（字段不完整，归入篡改）；
2. 任一引用节点不在图中 → `ErrNodeMissing`（指名节点）；
3. 校验和不匹配 → `ErrChecksumMismatch`。

三者均为哨兵错误，`errors.Is` 可区分。续传中途图被删节点同样以
`ErrNodeMissing` 返回并指名，绝不静默跳过（否则分段等价性被破坏）。
「已完成」续点再续传返回空序列、nil 错误。

## 7. 并发与纯函数

遍历不修改图、不持有包级或图级隐藏状态，计数器存在每次调用私有的 Result
中。图读取仅访问只读切片；同一续点多协程并发续传结果逐元素相同，
`-race` 干净。
