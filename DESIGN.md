# DESIGN：自定 LZ77 流式压缩

## 1. 流格式（全部变长整数，uint64 little-endian base128，最多 10 字节）

流 = 流头 记录* 流尾。校验和 = CRC32-IEEE（标准库 `hash/crc32`，非压缩库），随字面量与回指展开的原文持续更新。

| 记录 | 字节布局 |
|---|---|
| 流头 | `4F 4E 54 4C`("ONTL") + varint 版本(=1) + varint 窗口容量 + varint 链长上限 |
| 字面量段 | tag=0 + varint 长度N + N 个原文字节 |
| 回指 | tag=1 + varint 距离D(≥1) + varint 长度L(≥3) |
| 刷新标记 | tag=2（无负载；连续空 Flush 不产出任何字节，故不写标记） |
| 流尾 | tag=3 + varint 原文总长 + 4 字节 CRC32 大端 |

## 2. 四处推导

**Write 切法无关。** 压缩器维护 `pending []byte`（已 Write 但未裁决）。每次只在 `len(pending)>=2` 时裁决其首字节：找最长匹配 M；若 `len(M) < len(pending)`，则 M 不会再被未来字节延长（右侧字节已全部在手），立即提交——M≥3 发回指，否则发 1 个字面量；剩余 0~1 字节必须挂起，因为 2 字节永远凑不成最小匹配 3，且可能是某匹配的开头。决策只依赖"已提交历史 + 当前已知 pending"，而历史由累计偏移决定，与 Write 边界无关，故输出逐字节相同。`Flush` 强制把挂起字节全部以字面量结案并写刷新标记：因为 Flush 承诺解压器此刻能还原全部输入，不能再等未来字节；这也强制结束可能跨过该点的匹配。Flush 后历史仍保留，后续匹配可回指 Flush 之前。

**重叠回指（D<L）。** 一次性块复制会把同一片旧缓冲读到自身、拿到未定义内容；语义必须是输出位置 i 取输出位置 i-D，即逐字节向前。解压器对回指执行 `for k:=0;k<L;k++{ b:=out[len(out)-D]; emit(b) }`。每字节一次索引+追加，L 与输出同阶，全程 O(输出)；压缩比再高也没有额外渐近成本，故直接逐字节即可，不退化。

**两类距离越界。** "D>已输出字节数"是引用了不存在的历史（流损坏/伪造）；"D≤已输出 但 D>窗口容量"是引用了合法存在、却超出协商窗口（流头声明）的历史。二者错误不同：压缩器只在窗口内取候选，D≤容量恒成立，故绝不可能产生后者。并行块以"上一块原文末尾窗口容量字节"预置字典：该字典在本块第一字节输出前已存在于解压侧窗口，因此"已输出字节数"从解压视角仍含预置字典（用虚拟偏移记账），两类检查的相对关系不变。

**并行确定性。** 块 i 的匹配器只装载 block[i-1] 的窗口（预置字典），只压缩 block[i]；不读其他块的压缩结果，且压缩是纯函数、无共享可变状态，故与 goroutine 完成顺序无关。固定块边界使 workers 数只改变调度、不改变切块与逐块输出，输出按块序拼接，故 workers=1/2/4/8 逐字节相同。块间不需要刷新标记：块边界不强制结案（除整块末尾），预置字典保证跨块回指可被流式解压器按普通记录解开；流仍是 头+记录+尾 的单一流。

## 3. 复杂度与防护

哈希链：每位置哈希头表 + 链指针，沿链最多考察 `chainLimit` 个候选；计数器记录考察总数，故 ≤ chainLimit×字节数，与窗口大小无关。解压炸弹：发回指前先判 `已输出+L > maxOut`，再逐字节展开，巨大 L 在写出前即拒绝。
