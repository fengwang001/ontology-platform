# DESIGN：内容定义分块与去重

## 0. 方案概览

- `roll`：Rabin-Karp 风格滚动哈希。窗口长 `w`，维护 `h = Σ b[i]*p^(w-1-i) mod m`。
  推进一个字节（出旧 `o`、进新 `c`）：`h = (h - o*p^(w-1))*p + c mod m`，O(1)。
- `split`：边界谓词 `h % 2^bits == 0`（期望块长 `2^bits`）。受 `min/max` 约束。
- `addr`：地址 = SHA-256（强哈希），相等即字节相等（概率忽略）。
- `store`：`map[Addr][]byte` + 引用计数。
- `stream`：先缓冲全流，Flush 时一次切分并原子提交，天然与写入切法无关。

## 1. 最小块长：甲、乙两法是否等价

**结论：不等价。乙（照常喂哈希、只禁止判边）保持全局滑动窗口的内容定义性；
甲（前 min 字节不喂）会把每次插入的位移"记忆"到块起点之后 min 个字节，级联破坏局部性。**

谓词取 `h%2==0`，窗口 `w=2`，左字节权 2、右字节权 1，模 3：
`h(AB)=0, h(BC)=2, h(CB)=0, h(eB)=2`。取 `min=3, max=8`。

原始流：`A B C B C B C`，块起点 0。

- **乙法**（窗口从流头连续滑动，位置<min 只不判边）：滑过每个窗口的哈希只由
  该两字节内容决定。在偏移 3 的 `B` 前插入 `e` 后，窗口序列为
  `AB,BC,Ce,eB,BC,CB,BC,...`；插入点之后的窗口内容仍是 `BC,CB,BC,...`，
  与原流同位置的窗口序列完全一致——故谓词命中位置整体后移 1，其后的切点
  全部复原，远处块地址不变。
- **甲法**（前 min 字节不喂，哈希在块起点重启）：原块从第 3 字节起喂入，
  窗口序列 `CB,BC,CB,...`，第一个满足谓词且位置≥3 的是偏移 4 的 `CB`，
  切点在偏移 5。插入 `e` 后块内喂入序列变成 `eB,BC,CB,...`：`eB`(2)、
  `BC`(2) 都不满足，第一个命中的 `CB` 出现在偏移 5，切点变成偏移 6。
  切点没有跟随内容平移，而是落到了**不同的内容位置**：甲法的哈希相位依赖
  "块起点在哪"，块起点恰被插入改变，于是该块边界改变后，下一个块起点也变、
  相位再变，切点变化级联传播到流尾，破坏不变量 2。

直观判据：乙法中谓词只依赖**窗口内容本身**，切点是流上的内容固有标记；
甲法中同一段字节在不同块起点相位下哈希序列不同，标记不再固有。
**本实现采用乙法。**

## 2. 最大块长强制切分的含义

走到 `max` 仍无内容边界时，在偏移 `max` 处强制切。这一刀不满足谓词、
**不依赖窗口内容**，破坏了"切点位置完全由内容决定"：插入发生在强制切点
之前时，强制边（距块起点固定 max）整体平移，平移后强制块内内容边界的相位
改变，必须再滑至多一个 max 才能重新同步到内容边界。故局部性常数出现
`ceil(max/min)` 项（见第 3 节）。

## 3. 局部性常数

插入 k 字节后，插入点前一切不变。插入点位于某块内；改动窗口至多 w-1 字节后
谓词输出恢复，但 min 禁止区与 max 强制边使重新同步可能推迟。从插入点到下一
个与原流一致的内容边界，距离 ≤ max（任意长度 max 的区间内必有边界：内容边
或末端强制边）。该区间内块数 ≤ `ceil(max/min)+1`；左侧被截断的块 1 个；
插入字节最多再造 `ceil(k/min)+1` 块。删除同理（k 取删除长度）。

上界：**受影响块数 ≤ 2·ceil(max/min) + ceil(k/min) + 3**，与流长无关。
测试取 min=64, max=256, k=1：上界 = 2·4+1+3 = **12**；断言受影响块
≤12，且其后第 1 个块起地址逐个相同。

## 4. 流末尾不足 min

选择：**末尾残留并入最后一个已切块**（合并块允许长到 max+min-1）；
整个流 < min 时单独成 1 块（≥1 字节）。

- 不变量 3 要求覆盖全部字节且偏移首尾相接；独立成小块会违反 min，
  并入前块只让最后一块特例化，不影响其它块的内容定义性。
- 空流：0 块，重组为空字节串；零长 `Write` 不产生块、不改偏移，
  与空流的区分靠写入次数（API 暴露 `Writes()`）。
- 流 < min 时无"前一块"可并，只能单独成块；它既是首块也是末块，
  长度 ∈[1,min-1]，自洽。
- SelfCheck 长度规则：除末块外每块 ∈[min,max]；流≥min 时末块
  ∈[1,max+min-1]；流<min 时唯一块 ∈[1,min-1]。
