# 设计推导

## 1. 零计数 hunk 头的行号（语义 4）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a b c`→`a b X c` 得 `-2,0`；`a\n`→空得 `+0,0`。
插入发生在新内容落点「之前没有旧行被消费」的情形。若把数字解释成「将要插入的位置
（1 基）」，三例应分别写 1、3、1，与实际输出 0、2、0 不符。实际数字恰好是插入点
**之前已有的行数**，即「前一行的行号」：例 1 插入点在第 1 行前，前面 0 行 → `-0,0`；
例 2 插入点在第 3 行前，前面 2 行 → `-2,0`；例 3 新文件为空，插入点前 0 行 → `+0,0`。
统一规则：计数 b≥1 时 a 为区间首行的 1 基行号；b=0 时 a 为插入点之前的行数（前一行
行号）。`Apply` 解析：b≥1 时从第 a 行（0 基下标 a-1）起消费 b 行；b=0 时不消费任何
行，插入点放在第 a 行之后（0 基下标 a 处）。

| b 取值 | 头部写法 | Apply 的插入点解释 |
|---|---|---|
| b=0 | `-a,0`，a=插入点前行数 | 不消费旧行，在 0 基下标 a 处插入 |
| b=1 | `-a`（省略 `,1`） | 从 0 基下标 a-1 起消费 1 行 |
| b≥2 | `-a,b` | 从 0 基下标 a-1 起消费 b 行 |

## 2. hunk 合并阈值（语义 5）

结论：两段改动间未改动行数 g ≤ 2C 时合并，g ≥ 2C+1 时拆分。
推导：前一段改动的 hunk 向后带 C 行上下文，后一段向前带 C 行上下文。g=2C 时两段
上下文恰好首尾相接（前段尾上下文占 g 行前 C 行，后段头上下文占后 C 行），中间不留
未带上下文的行，GNU diff 将其视为一个连续区段而合并；g=2C+1 时两上下文之间多出 1
行谁也不要的「空档」，区段断开，拆成两个 hunk。实证（GNU diffutils）：C=3 时 g=6
输出 1 个 hunk、g=7 输出 2 个；C=0 时 g=0 合并、g=1 拆分。若阈值取 C，则 C=3、g=4
会被错误拆分（GNU 为 1 个 hunk）；若取 2C+1，则 g=7 会被错误合并（GNU 为 2 个）。

## 3. 最短脚本的并列选择（语义 3）

Myers 前向贪心每步取 `x = max(V[k-1]+1, V[k+1])`。当 `V[k-1]+1 == V[k+1]` 并列时，
本实现固定取 `V[k-1]+1`，即**优先向右走（删除）**，回溯时镜像同一规则。效果：同一
改动块内所有 `-` 行排在 `+` 行之前，与 GNU diff 一致。例：`a b`→`b a` 的最短脚本
不唯一（LCS=1），本实现固定产出 `-a`、` b`、`+a`（删 a、保 b、插 a），而非
先插后删的镜像方案。规则固定 ⇒ 同一输入重复生成逐字节相同（语义 3 的确定性）。

## 4. 偏移应用后后续 hunk 的查找起点（语义 7）

维护漂移量 drift（初值 0）。第 i 个 hunk 的记录位置（0 基）为 p_i，查找起点取
`p_i + drift`，在 ±F 内找精确匹配，命中于 q。命中后更新
`drift += (q - (p_i + drift)) + (插入行数 - 删除行数)`。理由：q 相对期望位置的偏差
反映目标文件在该处之前多/少了若干行，后续内容大概率带着同样的偏差，故把本次偏移
叠加进 drift；同时本 hunk 净增/净删的行数使后续所有旧坐标平移，也必须计入。两者
相加正是「目标文件新坐标 − 补丁旧坐标」的最新估计。若每次都用裸 p_i，则第一个
偏移命中后，后续 hunk 仍会从不含漂移的起点重新搜索，既慢又可能在 ±F 内找不到本应
命中的位置。
