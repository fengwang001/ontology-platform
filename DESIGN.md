# DESIGN — unified diff 生成 / 渲染 / 应用

依赖方向（单向）：`lines → edit → hunk → udiff → patch`。

## 1. 零计数 hunk 头的行号规则（语义 4）

统一格式头是 `@@ -a,b +c,d @@`，其中 a、c 是**基于 1 的起始行号**，表示该 hunk 第一个被计数的行；
b、d 是该 hunk 在旧/新文件中所占的行数。GNU diff 规则：

- **b ≥ 1（含 b==1）**：`a` = hunk 第一行旧行号，按 `b` 原样写；b==1 时省略 `,1`。
- **b == 0（纯插入，旧文件不占行）**：没有“第一个被计数的行”，GNU 取插入点**前一行**的行号：
  插在文件开头→`-0,0`；插在旧第 n 行之后→`-(n),0`。这解释了 `x\n`→`y\nx\n` 得 `-0,0`
  （前零行），`a\nb\nc\n` 在 b、c 间插入得 `-2,0`（插入点前一行是第 2 行 b）。
  注意 `-2,0` 不是“新位置 3”，插入位置本身不唯一（第 2 行后 = 第 3 行前），
  故统一用“前一行行号”。Apply 解释：`a==0` → 插到最前面；`a>0,b==0` → 在旧行 a 之后、a+1 行之前插入。

| 情形 | 书写 | Apply 的插入/起始解释 |
|---|---|---|
| b==0 | `-a,0`（a 即前一行行号，可 0） | 新行插在旧第 a 行之后；a==0 插文件首 |
| b==1 | `-a`（省略 `,1`） | 仅覆盖旧第 a 行这一行 |
| b≥2 | `-a,b` | 覆盖旧第 a .. a+b-1 行 |

新侧 c、d 完全对称：`a\n`→空 得 `@@ -1 +0,0 @@`（删除后新文件 0 行，c=0）。

## 2. hunk 合并阈值（语义 5）

阈值是 **2C**：相邻两段改动之间未改动行间隔 g 满足 **g ≤ 2C 则合并**，g ≥ 2C+1 才分开。
推导：每段 hunk 默认各带 C 行上下文。若合并，中间间隔行全部成为 hunk 内上下文（共 g 行）；
若分开，前 hunk 取尾部 C 行、后 hunk 取头部 C 行。当 g ≤ 2C 时两段上下文必然相接或重叠
（g < 2C 重叠，g == 2C 恰好首尾相接，中间没有任何被排除的行），分开会在输出里产生两个
相邻/重叠的 hunk，与 `diff -U C` 矛盾，故 g == 2C 必须合并；g == 2C+1 时中间恰有 1 行
不属任一 hunk，两 hunk 可合法分开。例 C=3：g=6 合并（`diff -U3` 输出单个 hunk），
g=7 分开。取 C 会在 g=4、5 时错误拆开，取 2C+1 会在 g=7 时错误合并。

## 3. 最短脚本并列时的选择（语义 3）

Myers 在编辑图上每层 D 沿对角线 k 扩展：`k=-D,-D+2,…,D`。
到达 k 有两个前驱——自 k-1（向右 = 插入）或自 k+1（向下 = 删除）。
本实现的并列规则：**能向下（删除）时优先向下**，即
`if k == -D || (k != D && V[k-1] < V[k+1]) { 向下 } else { 向右 }`；
向下/向右后各自走 snake（对角线相等行）。因此 `a b → b a` 的脚本固定为
`-a`、` b`、`+a`（先删后插），渲染时 hunk 内 `-` 行整体排在对应 `+` 行之前，
重复生成 100 次字节完全一致。选择相反（优先插入）会得到 `+b`、` a`、`-b`，不合 GNU 习惯。

## 4. 偏移应用后后续 hunk 的起点平移（语义 7）

hunk i 记录的旧起点为 r_i；若它在偏移 ±F 内于实际行 p_i 处应用成功（位移 Δ_i = p_i - r_i），
则 hunk i+1 的搜索区间中心（记录位置）整体平移 **+Δ_i**：
即以 `r_{i+1} + Δ_i` 为中心在 ±F 行内找最佳匹配。原因：前一 hunk 的位移说明目标文本与
补丁基线之间存在整体性行号漂移，后一 hunk 记录的位置同样落在该漂移坐标系里；
不累积漂移会使近邻 hunk 平白消耗偏移预算甚至误拒。多候选时仍取距平移后中心最近者，
等距取靠前（行号较小）。

## 5. 其他决定

- 行：`lines.Split` 保留原行尾（`\n`/`\r\n`/无），`Join` 原样拼回；空串=0 行。
- 无换行：`\` 行不占 b/d 计数；解析时空文件上下文行“` `"不算错。
- 四类错误哨兵：`edit.ErrTooLarge`、`udiff.ErrFormat`、`patch.ErrContext`、`patch.ErrOutOfRange`，
  用 `errors.Is` 判定；格式错误带补丁文本行号，应用错误带第几个 hunk。
- 原子性：所有 hunk 先在副本上试应用，全部成功才提交；截断/翻转任何情形不得 panic。
