# 分片扇出查询的部分失败合并器 — 设计与推导

目标：把一个聚合查询并发扇出到 N 个内存假分片，部分分片失败时合并成功部分，
并为每种聚合给出**可信度标注**与区间。只用标准库。

## 1. 数据模型

- 记录 `Record{ID string; Value float64; HasValue bool}`：分片可只给部分字段，
  `HasValue=false` 的记录参与 Count，但不参与 Sum/Min/Max/TopK。
- 分片应答 `Response{ShardID, ClaimedCount int, Records []Record, ScoreBound float64, HasBound bool}`：
  `len(Records)!=ClaimedCount` 即**损坏**，整个分片判失败，数据不并入。
  `ScoreBound` 是该分片对「单条记录可能得分」给出的上界（供 TopK 推导）。
- 分片状态枚举：`OK / Timeout / Corrupt / Missing`；分别可用哨兵错误
  `ErrTimeout/ErrCorrupt/ErrAllFailed/ErrNoShards` 经 `errors.Is` 区分。
- 同一分片多次应答（重试叠加）：按 ShardID 去重，只接受第一次成功应答，
  后续应答一律丢弃，因此不会重复计数。

## 2. 五种聚合

Count=成功分片记录总数（含缺值记录）；Sum=有值记录之和；Min/Max=有值记录极值；
TopK 取分高者，分数相等按 ID 升序（确定性，与到达顺序无关）。

## 3. 部分失败下还能说什么（核心推导）

设成功分片给出的值为 v，存在至少一个失败分片。

**Count / Sum（只会偏小 → 下界）**
缺失分片的贡献非负（条数 ≥0；题目数值非负，Sum 贡献 ≥0），
故真实值 T ≥ v。标注「≥ v」，区间 Count=[v,+∞)、Sum=[v,+∞)。

**Min（方向易错）**
缺失分片可能含有比 v 更小的值，真实最小值只可能 ≤ 当前 v，不可能更大：
T ≤ v。标注「真实 Min ≤ v」，区间 (−∞,v]。

**Max（方向相反）**
缺失分片可能含更大值：T ≥ v。标注「真实 Max ≥ v」，区间 [v,+∞)。

**TopK — 可信前缀**
即使只缺一个分片，当前第 K 名也可能被挤掉，所以不能笼统信任整个列表。
设失败分片集合 F，分片 i 给出的单条得分上界为 u_i（有界时），
记 B = Σ_{i∈F} u_i。
- 失败分片最多能让「来自失败分片的得分总量」达到 B，等价地，任何一条来自
  缺失分片的新条目得分 ≤ B。
- 取合并后当前排好序的第 j 名，得分 s_j。若 s_j > B，则任何缺失条目都无法
  超过它：要把它挤出前 K，至少需要 K 个比它高的条目，而连一个得分 > B 的
  缺失条目都不存在。故前 j 名**入选确定**（严格分数差也固定了相对名次）。
- 于是从榜首往下数，满足 s_j > B 的连续前缀长度 P 即可信前缀；
  P 截断到 min(K, len(list))。其余位置标注「不可信（可能被挤掉）」。
- 若某失败分片无上界（HasBound=false），视为 B=+∞，P=0。

小上界场景（B 很小）⇒ 前 K 名全部满足 ⇒ P=K；大上界场景（B 很大）⇒ P=0。

全部成功：无缺失分片，五种聚合一律标注「精确」，不做任何降级。

## 4. 资源约束（可证明）

- 并发：容量为 limit 的信号量；每个 Fetch 前获取。`inFlight/maxInFlight` 由
  互斥锁保护，记录历史峰值。200 分片、上限 8 ⇒ 峰值恰为 8（有足量长任务时）。
- 截止：整体 `context.WithTimeout`；信号量获取放在 `ctx.Done()` 的 select 中，
  截止后不再发起新请求（`Started` 不再增长），在飞请求因 ctx 取消而返回，
  `Wait` 在截止后很短窗口内返回。

## 5. 确定性与边界

- 合并只依赖去重后的成功应答集合：缺失清单按 ShardID 升序；TopK 以
  （分数降序, ID 升序）排序。打乱到达顺序 ⇒ 逐字节相同。
- 零分片 ⇒ `ErrNoShards`；全部失败 ⇒ `ErrAllFailed`；
  全部分片成功但为空 ⇒ 合法的精确零结果（与全部失败可区分）。
- K>条目数 ⇒ 返回全部且标注精确/前缀按实际长度；空串 ShardID 合法。
