# 游标分页器设计（keyset pagination）

## 数据模型

每行是一个复合排序键 `(Key float64, ID string)`，ID 全局唯一。
全序关系：先比 `Key`，`Key` 相等再比 `ID`（字典序）。该全序是数据集唯一的排序依据。

## 一、复合游标边界的推导

**反例 1（只记排序值 + 严格大于）**：数据 `(1,a),(2,b),(2,c),(2,d),(3,e)`，页大小 2。
第一页取到 `(1,a),(2,b)`，游标只记 `v=2`。下一页取 `Key>2`，得 `(3,e)`：
`(2,c),(2,d)` 被整批跳过 —— **漏**。

**反例 2（只记排序值 + 大于等于）**：同样数据，下一页取 `Key>=2`，得
`(2,b),(2,c)`：`(2,b)` 再次出现 —— **重**。

**结论**：排序值不唯一时，任何只含排序值的边界都必然漏或重。
正确做法：游标记录上一页最后一行的**复合键 `(Key, ID)`**，下一页边界为该复合键的
**严格大于**：`(k,id) > (K0,ID0)` 当且仅当 `k>K0 || (k==K0 && id>ID0)`。
因为 ID 唯一，复合键全序无并列，严格大于既不漏（同 Key 的行按 ID 续取）也不重
（已返回行的复合键都 `<=` 边界）。游标不依赖该行是否仍存在，只取键值。

## 二、反向翻页规则

反向不是「正向结果倒序」。规则：比较方向与取数方向同时反转，**页内顺序仍按正向**：

- 正向 `Forward(after, n)`：取复合键 `> after` 的最前 n 行，升序返回。
- 反向 `Backward(before, n)`：取复合键 `< before` 的最后 n 行（降序扫描），
  再反转为升序返回。

每次翻页返回两个游标：`Next`（正向，指向本页末行）与 `Prev`（反向，指向本页首行）。
于是「正向翻到第 3 页后用 `Prev` 反向翻一页」恰好得到第 2 页且逐行相同。
游标内嵌方向标记，正向游标交给 `Backward`（或反之）报 `ErrDirection`，不静默处理。

## 三、并发增删下的语义承诺

- 单次翻页在读锁内完成，等价于对某一瞬间有序序列的纯函数：同游标同数据集
  必得同页，无隐藏推进状态，多协程共用同一游标结果逐行相同。
- 翻页间隙的增删不做快照隔离：插入到**已翻过区间**的行允许不再出现；
  插入到**未翻区间**的行会在后续页按复合键顺序出现；被删除的行不再出现。
- 两条硬保证：**已返回过的行绝不再次出现**（边界严格大于已返回的最大复合键）；
  **未被删除的行不会在其复合键区间内被跳过**（边界是键值比较，与物理位置无关，
  插入只改变位置不改变键序）。指向已删除行的游标仍可续翻。

## 四、效率约束

- 定位边界用二分查找（`O(log N)` 次比较），页内扫描 n 行，单次翻页比较次数
  上界 `4*(n+ceil(log2 N))`，绝不从头全扫。比较次数由包内非导出计数器统计。
- 游标编解码 O(1)：定长头 + 一次 ID 拷贝，分配次数为常数。

## 五、游标编码与故障分类

字节格式：`[dir:1]['F'|'B'][key:8 大端][idLen:1][id:idLen][crc32:4]`。
解码按序检查：长度下限 -> 方向标记 -> idLen 与总长一致 -> CRC 校验。
四类错误可用 `errors.Is` 区分：`ErrIncomplete`（字段不完整）、`ErrBadDirection`
（方向标记非法）、`ErrChecksum`（校验失败）、`ErrDirection`（跨方向复用）。
空字节串是合法游标，表示从头（正向）或从尾（反向）开始。
单比特翻转下：方向字节 8 个变体中 7 个落入非法方向、1 个落入另一合法方向但被
CRC 拦截；idLen 字节 8 个变体全部长度不一致；其余变体全部 CRC 失败。无一被接受。
