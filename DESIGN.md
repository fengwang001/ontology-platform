
# DESIGN — 可撤销批量重命名与冲突解析器

## 1. 模型
- `name.Space`：内存中名字集合，底层为 `map[string]struct{}`；持有 `sync.RWMutex`。
- `Move(a,b)`：原子地 `delete(a); add(b)`；`b` 已存在则失败（不覆盖）。
- 合法性：任意 Go 字符串均合法（空串合法；`/` 等路径分隔符按普通字符处理）。

## 2. 四类冲突（修改前一次性检测，可用 errors.Is 区分）
设请求集合 R，旧名集合 Src，新名集合 Dst，现有名字集合 N。
1. `ErrSrcMissing`：`a ∉ N`（旧名不存在）。
2. `ErrDestExists`：`b ∈ N` 且 `b ∉ Src`（目标已存在且本批不会被搬走）。
3. `ErrDupDest`：同一 `b` 对应两个不同 `a`（报出两个旧名）。
4. `ErrDupSrc`：同一 `a` 对应两个不同 `b`（报出两个新名）。

## 3. 排序规则推导（依赖图拓扑序）
规则 a→b 要安全执行，必须等「占用 b 的旧名」先搬走。
- 若存在请求 b→c，则 a→b 依赖 b→c：有向边 b→c → a→b。
- 一般地，对每个请求 a→b（a≠b），若 b ∈ Src，则添加依赖 `(b→?) → (a→b)`。
- 该图每个顶点入度至多 1、出度至多 1，非环部分是若干条链。
- Kahn 拓扑序（同层按名字字典序）即安全顺序。
- 链 {a→b,b→c}：a→b 依赖 b→c，顺序为 b→c, a→b；任意时刻目标名都不存在。

## 4. 环的破解（cycle 包）
环即新名闭合回旧名的请求环，任何线性顺序都必然覆盖。
通用做法（每个环只需一个临时名 t，故临时名数 = 环数）：
- 环 r0:v0→v1, …, r_{n-1}:v_{n-1}→v0：
  1. v0→t（腾出 v0，t 经校验不存在）；
  2. 依次 v_{n-1}→v0, v_{n-2}→v_{n-1}, …, v1→v2（沿环逆推）；
  3. t→v1。
- 二元环 {a→b,b→a}：a→t, b→a, t→b。
- 三元环 {a→b,b→c,c→a}：a→t, c→a, b→c, t→b。
临时名形如 `.rename-tmp-<n>`；必须 ∉ N ∉ Dst，冲突则 n 递增重试；
超过上限（1e9 次尝试）返回 `ErrNoTemp`（可判定，绝不覆盖数据）。

## 5. 自环语义
`a→a` 定义为**无操作**：从执行图与日志中剔除（仍参与冲突检测）。
重复的自环按第 3/4 类冲突处理。

## 6. 复杂度
冲突检测与排序只做哈希查找与入度维护，随「请求数 + 涉及名字数」线性；
`plan.lookups` 统计 map 查找次数，约束 `lookups ≤ 4*(|R|+|涉及名字数|)`。
破环对每个环常数次替换，临时名数恰为环数。

## 7. 执行、日志与撤销（apply / undo）
- 执行持 Space 写锁（串行化，并发修改被阻塞）；每步为一个原子 Move。
- 日志（临时目录）：5 字节头 `RNL01`，每条
  `[4 字节大端 len][JSON payload(from,to)][4 字节大端 CRC32(payload)]`。
- 撤销按日志逆序 Move(to,from)；完成后日志重命名为 `*.done`，重复撤销为无操作。
- 注入第 k 步失败（`ErrInjected`）：已执行步骤立即逆序回滚，逐元素还原。
- 截断分类（对字节前缀逐点截断，errors.Is 可区分）：
  - `ErrLogHeader`：前缀 < 5 或头不完整；
  - `ErrLogRecord`：长度字段在但 payload 不足 len 字节（记录不完整）；
  - `ErrLogCRC`：payload 完整但 CRC 不足 4 字节或校验不符；
  按「最大可恢复前缀」撤销，并报告无法撤销的步数。
  纯前缀截断下 CRC 类发生在「payload 完整、CRC 被截断」区间。

## 8. 确定性与并发
同层按名字字典序出队，请求构造顺序打乱 20 次，步骤序列逐元素相同。
批量执行全程持写锁，并发 Move 必须等待锁释放（阻塞后成功）。

## 9. 边界
空请求集：空计划直接成功；单请求：一步；
大量重复旧名：第 4 类冲突拦截；名字数远小于请求数同理。
