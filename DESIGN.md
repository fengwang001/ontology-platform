# DESIGN - 带溢出的哈希聚合器

## 1. 问题与总体方案
- 输入：行流（分组键 string，数值 float64）；输出：每组的 Count/Sum/Min/Max。
- 内存预算 B = 最多驻留分组数；分区数 P 构造时固定，与数据规模无关。
- 流程：行 -> FNV-1a(key) mod P 定分区 -> 入内存哈希表；表满时选一个分区，把该分区当前驻留的部分聚合状态写盘（溢出）并逐出；输入结束后逐分区回读全部溢出片段，与内存残余状态合并，按键排序输出。

## 2. 溢出什么：部分聚合状态，而非原始行
- 结论：溢出部分聚合状态。
- 论证 1（磁盘量）：溢出原始行需每行一条记录；溢出部分状态只需每个驻留分组一条记录，记录数 <= 行数，高重复键时远小于行数。
- 论证 2（有界性）：单次溢出写出量 <= 当前驻留分组数 <= B，与已见行数无关；原始行方案的写出量随行数与溢出次数线性膨胀。
- 论证 3（可合并性）：部分状态对 Merge 构成交换幺半群（见第 3 节），同分区可溢出任意多次，回读时把各片段与内存残余合并即等价于一次性聚合；原始行方案回读时仍需逐行重聚合，已做的聚合工作全部浪费。

## 3. 同一分区多次溢出仍正确的理由
- 设分区 p 的行按到达序为 r1..rn，在第 k1、k2 行后各溢出一次，则片段 S1=agg(r1..k1)，S2=agg(r(k1+1)..k2)，S3=剩余内存状态。
- Merge 满足结合律与交换律，且存在幺元 (Count=0, Sum=0, Min=+Inf, Max=-Inf)：故 Merge(S1,S2,S3)=agg(r1..rn)，与溢出次数、切分点无关。漏掉任一片段等式即不成立——回读必须合并该分区所有片段加内存残余。
- 浮点说明：Sum 的结合律对任意浮点仅近似成立；当中间和均可精确表示（如整数值 float64）时严格成立。正确性测试使用整数值，故结果与全内存聚合逐位相同；Count/Min/Max 对任意合法输入严格满足结合律。

## 4. Min/Max 与 Count/Sum 在合并上的差异
- Count/Sum：合并是累加（c1+c2、s1+s2），非幂等——同一状态合并两次会翻倍，故每段状态只能参与合并一次（每行只入表一次、每片段只读回一次，由计数器强制验证）。
- Min/Max：合并是取极值，幂等（merge(x,x)=x），重复合并无害；极值不携带次数信息，必须与 Count 分开维护。
- 幺元不同：Count/Sum 幺元为 0；Min 幺元为 +Inf，Max 幺元为 -Inf。
- NaN 在入表前拒绝并计入跳过数，合并律无需处理 NaN；±Inf 正常参与 Sum 与 Min/Max；-0.0 入表时归一化为 +0.0，保证位模式确定、±0 视为相等。

## 5. 分区文件格式与截断分类
- 头部 8 字节：magic "HAG1"(4) + 分区号 uint32(4)，自描述。
- 记录：长度前缀 uint32 + 载荷 + CRC32(载荷) 4 字节。
- 截断点 n 分类：[0,8) -> 头部不完整；记录起点 s 处 [s,s+4) -> 长度前缀不完整；[s+4,s+4+L) -> 行体不完整；[s+4+L,s+8+L) -> CRC 不匹配。错误均带分区号与字节偏移，四类可 errors.Is 区分。

## 6. 原子写与故障语义
- 先写 part-PPP-SSSSSS.tmp，关闭后原子 rename 为 .spill；启动时清理目录内全部 *.tmp，半截临时文件不会被当作有效分区读入。
- 注入第 k 次写分区失败：聚合立即中止，删除本次已产生的全部临时/分区文件，残留计数为 0。

## 7. 并发与确定性
- 回读阶段每分区一个 goroutine，聚合状态互不共享；结果按分区号定序合并（片段按序号升序、内存残余最后），最终按键排序输出。合并顺序与回读完成顺序无关，Sum 位模式逐字节确定。

## 8. 资源与复杂度约束
- 插入新键前若表已满（len==B）则先溢出驻留分组最多的分区，故驻留分组数 <= B，峰值由非导出计数器记录。
- 每行只哈希分区一次、只入表一次；每段溢出记录只写一次、读回一次；总行处理次数 <= 2N（N 入表一次，溢出记录数 <= N 且只读回一次）。
- P 固定，文件句柄数与回读并发度上界固定，与分组数、行数无关。
