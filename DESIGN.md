# DESIGN

## 记录布局（wire）

头部固定 6 字节：魔数 `LZ77X`（5B）+ 版本 1（1B）。其后 0..n 条记录，流尾结束。
整数一律 unsigned LEB128 varint；tag 字节高位为记录类型，低 7 位负载：

| 记录 | tag | 后续字段 | 说明 |
|---|---|---|---|
| 字面量 | 0xxxxxxx | x 个原始字节（x=0 时为 0 长占位） | 低 7 位为长度 0..127 |
| 回指 | 10xxxxxx | varint 距离, varint 长度 | 低 6 位为 (len-3) 的位 0..5 |
| 刷新 | 11000000 | 无 | Flush 边界 |
| 流尾 | 11000001 | varint 原文总长, varint FNV-1a 64 位校验和 | 之后不得再有字节 |

回指长度编码：长度 ≥3（最小匹配 3）；tag 低 6 位 = (len-3) 的位 0..5，
其后 varint = (len-3)>>6。距离 1..1<<20。

## 推导 1：压缩结果为何与 Write 切法无关

压缩器保留一个 `pending` 缓冲（新 Write 追加其尾），窗口/哈希表只吸入「已决」字节。
贪心决策循环每轮在 pending 头部位置 p 找匹配：

1. 匹配到达 `maxMatch` 上限：再延长也不可能，立即可决（输出回指）。
2. 匹配末端在 pending 末尾（仍可被未来字节延长）：不可决，停止处理并等待下一次 Write。
3. 否则匹配已被其后一个已存在的字节终止：长度确定，可决。
4. 无匹配：仅当 p 之后还至少有 1 个已存在字节时，p 才确定为字面量（否则下一字可能成为
   距离 1 匹配）；把累积字面量成段输出。

可决判据只依赖「已存在的字节内容」，不依赖它们来自哪次 Write，故同一累计偏移上的决策
逐字节相同。`Flush` 必须强制决尽 pending（force 模式：尾部字面量、未延长匹配一律落盘），
否则解压器无法还原截至该偏移的全部输入；决尽后写刷新记录。无新输入时不产生任何字节。

## 推导 2：重叠回指的还原

块复制（`copy(out[h:], out[h-d:h-d+l])` 一次取 l 字节）在 d<l 时读到的不是被复制写回
的新字节，只得到原有的 d 个旧值，输出长度被截断、内容错误。Go 内置 `copy` 语义是
「源随目标写入而变化」：对重叠区间，`copy(dst, src)` 实现保证按前进方向逐字节搬运，
等价于循环 `for i:=0;i<l;i++ { dst[i]=src[i] }`。因此 d=1 时天然生成 RLE，
d=2/3 生成周期展开。标准库文档保证该重叠行为，且 `copy` 走 memmove，性能不退化为
Go 层逐字节循环。回指先做输出上限校验再 copy，任何字节写出前即可拒绝。

## 推导 3：「距离超已输出」与「距离超窗口」为何分开

二者是不同不变量：d>已输出说明流引用了从未产生过的历史（损坏/伪造）；
d≤已输出但 d>窗口容量说明引用超出压缩器本可保留的范围（流必非本格式压缩器所产生，
或版本/参数不符）。压缩器只从固定容量窗口取候选，故绝不可能产生后者。
并行块预置字典不改变其含义：解压端不按块解压，流是一条普通流，「已输出字节数」就是
解压器实际写出的全部字节；字典中的字节在流里本就是前一块输出的字节，解码到跨块回指时
它们已在输出缓冲内，两类检查按原顺序、原含义进行。

## 推导 4：并行块压缩的确定性与衔接

块 k 的匹配器仅加载「上一块末尾 ≤ 窗口容量字节」作为预置字典（只进窗口与哈希链，
不计入本块输入偏移），本块决策只依赖该字典 + 本块原文，与其他块、其他 goroutine、
完成顺序均无关；块大小与输入固定后每块输出逐字节确定。各块输出是同一头部之后顺序
拼接的普通记录（无块标记、无刷新记录：刷新是 Flush 语义，切块是内部并行手段），
最后由主 goroutine 统一追加唯一流尾（总长/校验和在切块前一次算出）。因此 worker 数
只影响调度，workers=1/2/4/8 输出完全一致，且流可被普通流式解压器直接解开。

## 复杂度与防护

哈希链每位置至多考察 `maxChain` 个候选（链头推进，旧链被容量截断），故考察总数
≤ maxChain×输入字节数，与窗口大小无关。解压器状态机逐字节消费、同一字节不解释两次；
varint 超 10 字节或溢出 uint64 即错；错误带流内字节偏移且终态粘滞；输出上限在任何
分配/写出之前校验。单实例不并发安全；多个独立实例可并发使用。
