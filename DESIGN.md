# DESIGN

## 记录布局（wire）
头 5 字节魔数 `LZ7\x01` 后接记录。整数全用 LEB128 无符号 varint（上限 10 字节）。

| 记录 | 标签 | 载荷 |
|---|---|---|
| 字面量段 | 0x00 | varint 长度 n，随后 n 个原始字节 |
| 回指 | 0x01 | varint 距离 d（≥1，从新到旧），varint 长度 l（≥3） |
| 刷新 | 0x02 | 无；声明此前全部输入已成块、可还原 |
| 流尾 | 0x03 | varint 原文总字节数，varint CRC32(IEEE) 校验和 |

## 推导 1：与 Write 切法无关（语义 3/4）
编码器保留 `holdback = maxMatchLen` 个“未决”字节不下决定；仅当未决区超过 holdback 时，
才对最早位置取最长匹配 M。若 M 终点落在未决区内部（后面还有字节），匹配不可能被未来
字节延长（它本可继续延长却没继续），提交它是确定的；若 M 恰好延伸到未决区末尾，决策
延后，等下一字节——两种切法最终观察到的最长匹配相同，故输出逐字节相同。字面量同理：
每次只提交确定边界后的 1 字节再重试。`Flush` 把未决区全部当作“后面永无字节”处理，因此
强制结束当前匹配；之后窗口已含这些字节，后续匹配仍可跨过刷新点回指。空输入也产出
头+流尾。连续 Flush 无新输入：未决区为空，第二次不写字节。

## 推导 2：重叠回指复制（语义 2）
块复制（如 copy(dst, src)）在 d<l 时会读到尚未生成的目标字节而错乱；正确语义是
out[t] = out[t-d] 逐字节计算。Go 的 `copy` 对重叠切片不保证生成语义，故解码对回指逐
字节循环。该循环只触碰将成为输出的字节，无冗余拷贝，4MB 最坏情形也仅 400 万次简单
赋值，可接受；输出仍先进内部缓冲，不产生额外每字节分配。

## 推导 3：两类“距离过大”（语义 7/8）
- d=0：非法；d>窗口容量：引用必然永远无法满足，压缩器受窗口容量限制绝不可能产出
  （它只从窗口内取候选，且回指提交时其源字节仍在窗口内；匹配长度 ≤ 固定 256 ≪ 窗口）。
- d>已输出字节数：引用超出当前已生成的历史；并行块场景下块以“上一块原文末尾”为预置
  字典，解码器在块开始前已真实输出这些字节（块边界处发刷新标记、解码器逐块喂入），
  所以“已输出字节数”就是当前块之前全部已解压前缀长度，含义不变，两条件都按它判定。
顺序先判 d>已输出（数据/截断类语义），再判 d>窗口，错误可区分。输出上限在回指 l 落入
声明时先检查 total+l ≤ MaxOutput，再写任何字节，超限即终态。

## 推导 4：并行块确定性（语义 9）
块边界发刷新记录。块 i 的压缩器初始窗口预置上一块原文的末尾 min(len(prev), win) 字节，
匹配器哈希链也预置该字典；其全部决策只依赖（预置字典 + 本块原文），不读其他块的输出、
不依赖完成顺序，故 worker 数变化只改变调度不改变字节。各块流拼接后：解码器把上一块的
全部输出留在同一窗口，与块 i 编码端预置字典状态逐字节一致，刷新记录保证边界语义；单一
流式解码器即可解开。块大小固定使切块也与并发度无关。
