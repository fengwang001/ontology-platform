# 设计推导：并行任务图调度与失败传播

模块划分：`fail`（状态枚举/错误类型，叶子包）→ `graph`（DAG）→ `exec`（执行）→ `report`（报告）→ `sched`（调度，汇聚前四者）→ `cmd/demo`。

## 一、失败后四类最终状态的判定推导

调度器对每个任务维护两个事实：**是否已启动**（是否已为其分配 goroutine 并计入 running 计数）与**返回时的错误性质**。四类状态由这两个事实唯一确定：

1. **成功 Success**：已启动，且返回 `nil`，且返回时调度器未对其发出取消。失败发生前完成的任务天然落入此类；状态一旦落盘即不可改（调度器只在任务返回时写一次结果，之后无任何路径回写），故不会被回滚或改标。
2. **失败 Failed**：已启动，返回非 nil 错误，且该错误不是由取消引起（即不是 `context.Canceled`）。panic 由 `exec` recover 后包装为 `fail.PanicError`，同样走此路径，报告中保留原始 panic 值。
3. **被跳过 Skipped**：**从未启动**。判定依据：任务在就绪前，其某个（传递）依赖已被标记失败或被跳过。原因字段指向**根失败任务**：沿依赖链回溯，跳过原因逐跳继承——若直接上游是失败任务，原因即该上游；若直接上游本身是被跳过的，原因继承上游的原因，最终收敛到最初失败的任务。故 `A→B→C` 中 A 失败时，B、C 的原因都指向 A 而非直接上游。
4. **被取消 Cancelled**：**已启动**，但首个失败触发快速失败传播，调度器取消其 `context`；任务随后返回 `nil` 或 `ctx.Err()`。即使它在取消后仍写回结果（返回 nil 或成功值），该结果被丢弃，状态保持 Cancelled 不变。

**「被取消」与「被跳过」的区分依据**：唯一的判据是"失败发生时任务是否已启动"。调度器在启动任务前将其记入 running 集合；收到结果时，若该任务在 running 集合中且取消已发生、其返回为 nil 或取消错误，则判 Cancelled；从未进入 running 集合的未就绪任务只能判 Skipped。慢任务与快失败任务并行时，慢任务已进入 running 集合，故为 Cancelled；其未启动的下游为 Skipped。二者不可能混淆，因为启动与否是调度器本地的确定事实，不依赖时序猜测。

**多任务同时失败**：凡返回真实错误（非取消错误）的任务都记为 Failed，报告记录全部；跳过传播按各失败任务分别沿其下游进行，每个被跳过任务的原因指向其各自子图中最早解析出的根失败任务。

## 二、快速失败 vs 尽力而为

- **FailFast（默认）**：首个失败落盘即 `cancel()` 共享 context，不再启动新任务；已就绪与未就绪任务全部标记 Skipped（无失败祖先的旁支任务，原因指向首个失败任务）；已启动任务等待其返回后判 Cancelled/Failed。
- **BestEffort**：不取消 context。失败任务的下游按依赖传播立即标记 Skipped（原因指向根失败任务）；与失败无关的分支继续调度直至完成。
- 两种模式下「成功/失败」的判定完全一致，差异只在未启动任务的去向：FailFast 全部跳过，BestEffort 仅失败下游跳过。

## 三、确定性

- 就绪队列用按 ID 升序的有序插入，多任务同时就绪时取字典序最小者。
- 拓扑分层（Kahn）同样按 ID 升序取层内元素。
- 报告按任务 ID 字典序逐行序列化，行内字段固定；完成顺序的随机性只影响写入 map 的顺序，不影响输出字节。

## 四、并发与复杂度

- 并发上限：信号量式计数 `running < maxConcurrent` 才启动新任务；原子计数器记录历史峰值 `maxSeen`。
- 就绪判定：每条边只在源任务解析时做一次入度递减判定，加上初始一次全节点扫描，总数 ≤ `V + E`，远低于上界 `4*(V+E)`；绝不按完成事件全图扫描。
- 无泄漏：调度器在返回前必定排空结果通道（所有已启动 goroutine 都被等待），`defer cancel()` 释放 context 资源。
- 环检测：Kahn 分层后若有剩余节点，在剩余子图内沿边迭代行走（非递归，1000 层链不溢出）提取真实环路径，路径逐边存在于输入且首尾闭合；自环路径为 `[v, v]`。

## 五、边界语义

空图直接返回空报告；重复加边用集合幂等去重；加边时端点不存在即返回 `fail.ErrMissingDependency`；并发度 1 时就绪队列每次只取一个，退化为按字典序的串行，报告与并行完全一致。
