# 分片扇出部分失败合并设计

## 1. 模型与故障

一次查询发给有序分片集合。每个分片返回记录、记录数上报名 `Declared`、以及该分片对单条得分的上界 `UpperBound`。
记录只含公共必需字段时仍可参与 Count；含 `Value` 时参与 Sum/Min/Max/TopK。

状态分为：`OK`、`Timeout`、`Corrupt`、`Failed`。
返回数与 `Declared` 不一致判为损坏；上下文截止或永久阻塞记为超时；同一分片因重试再次返回时只接受首个成功应答。

## 2. 扇出

扇出器用带缓冲信号量限制并发，开始请求前先占用，结束后释放；启动前若整体上下文已结束则不再启动。
每个分片派生子上下文，截止后取消仍在飞行的请求。历史最大在飞数由互斥保护的计数器记录。

## 3. 五种聚合的可证明结论

设成功分片的结果为 `S`，缺失分片的真实但未知贡献为 `M`；所有字段和数值默认非负。

- Count：真值 `C = C(S)+C(M) >= C(S)`，成功结果是下界，标注 `>= C(S)`。
- Sum：真值 `V = V(S)+V(M) >= V(S)`，成功结果是下界，标注 `>= V(S)`。
- Min：缺失分片可能更小，故 `trueMin <= min(S)`；若成功集为空则不能给数值边界。
- Max：缺失分片可能更大，故 `trueMax >= max(S)`；若成功集为空则不能给数值边界。
- 全部成功时 `M=0/不存在`，以上不等式均变为等式，五种聚合标注精确。

## 4. TopK 可信前缀

缺失分片最多放入总分 `U`，其中 `U` 是每个缺失分片上报 `UpperBound` 之和。
当前列表按得分降序、ID 升序排序。考察当前第 `j` 名得分 `s_j`：

若 `s_j > U`，则前 `j` 名各自得分都至少为 `s_j > U`。缺失分片的全部候选总分不超过 `U`，
不可能产生任何高过第 `j` 名的条目，也不可能把前 `j` 名挤出 TopK，因此前 `j` 名入选确定。
它们彼此的相对名次由成功分片确定，故前缀的名次也确定。

若 `s_j <= U`，缺失分片理论上可产生达到或超过该分数的条目，第 `j` 名可能变化；因此不能把 `j` 纳入可信前缀。
可信前缀长度是最大的 `j<=K`，使当前第 `j` 名得分严格大于 `U`。
`U=0` 且前 K 名存在时前缀长度为 K；缺失上界很大时，首个当前分数不大于 U，前缀为 0。

## 5. 合并与确定性

先按分片 ID 去重，再按规范 ID 升序消费，保证与到达顺序无关。
TopK 先按 `(score desc, ID asc)` 排序；缺失清单按分片 ID 排序；并列时 ID 小者在前。
空 ID 是合法 ID。零分片是请求错误；全部分片成功但记录为空仍是成功，和全部失败严格区分。

## 6. 报告

报告包含五种聚合值与可信度、缺失分片、每分片状态和是否全部失败。
全部失败返回可判别的 `ErrAllShardsFailed`；零分片返回 `ErrNoShards`；损坏可用 `ErrCorrupt`，超时可用 `ErrTimeout`。
