# 倒排索引短语查询与增量合并 — 设计

## 基本约定

- 位置从 **0** 开始（文档第 1 个词元的位置为 0），全库一致。
- 文档号全局唯一、从 0 递增，由 `merge.Set` 分配；每个文档只属于一个段。
- 每个位置至多一个词元（同义词扩展见末节「同位置多词元」）。
- 词元可以是空串，合法，按普通词元处理。
- 同一文档同一位置重复添加：拒绝并计数（`posting.Builder.Dups`）。

## 倒排链与编码（posting）

- 倒排链 = 文档号严格升序的条目序列，每条目内含位置严格升序序列。
- 增量编码：文档号与位置均按「与前一项之差」做 uvarint 编码，前一项初值视为 -1，
  故所有 delta >= 1；解码时若 delta <= 0、varint 截断或有尾随字节，均判为数据损坏。

## 段文件格式（segment）

```
header: magic(8B "ONTSEG01") | version u32 | termCount u32 | bodyLen u64   (24B)
body:   record * termCount
record: termLen uvarint | term bytes | postingsLen uvarint | postings bytes
footer: crc32-IEEE(header+body) (4B)
```

- 词典与倒排链按 record 交错排列，词典即 record 序列本身，天然支持前缀恢复。
- 写入先写 `*.tmp` 再 rename，保证合并崩溃时旧段不受影响；打开目录时清理 `*.tmp`。

### 截断分类（四类错误，可用 errors.Is 区分）

按 1..len-1 逐字节截断，分类规则：

- `len < 24` 或 magic/version 不可读 → `ErrHeaderIncomplete`
- 顺序解析 record 时在 termLen/term 字节处遇 EOF → `ErrDictIncomplete`
- 在 postingsLen/postings 字节处遇 EOF → `ErrPostingsIncomplete`
- record 全部可读但 CRC 缺失或不一致 → `ErrCRCMismatch`

恢复语义：顺序解析，只保留完整 record；词典中保留的词其倒排链必然完整，
不完整的词被剔除，不留悬挂指针（恢复出的索引自洽）。

## 短语位置对齐（phrase）

对短语 `t1..tk`，先把各词倒排链按文档号归并求交（文档级 AND），
对公共文档内的 k 条位置表做**调整值归并**：

- 定义调整值 `v_j = pos_j - (j-1)`（第 j 个词的位置减去其在短语中的偏移）。
  短语在起始位置 p 命中 ⟺ 存在 `pos_j = p + j - 1` 对所有 j 成立 ⟺ 各表当前 v_j 全等。
- 推进规则：每轮取 `m = max(v_j)`；若所有 `v_j == m` 则命中一次；
  否则把所有 `v_j < m` 的指针各进一步（至少一个指针前进，保证终止）。
- **重叠计数**：命中后把**全部** k 个指针各进一步（不是只进一个、也不是跳过已用位置），
  因此 `A A A` 查 `"A A"` 命中 2 次（起始 0 与 1），`A A A A` 得 3，`A B A B A` 查 `"A B A"` 得 2。
- 复杂度：每轮至少推进一个指针，总轮数 <= 各表长度之和；每轮比较次数 <= 2(k-1)，
  故总比较次数 <= 2(k-1)·Σlen <= 4·Σlen（k=2 时实测 <= 2·Σlen）。与乘积无关。

推导（重叠）：`A A A` 的两条位置表都是 [0,1,2]，调整后表1=[0,1,2]、表2=[-1,0,1]。
归并过程：v=(0,-1)→进表2；v=(0,0) 命中 p=0，两指针同进；v=(1,0)→进表2；
v=(1,1) 命中 p=1，同进；表2 耗尽，结束。共 2 次。若命中后只进表1，则
(1,0)→进表2→(1,1) 命中→(2,1)→进表2 耗尽，也得 2；但 k>=3 时间隔词表会错位，
统一「命中后全进」规则最简单且对所有 k 正确。

### 同位置多词元（规定不实现，仅说明规则调整）

若允许同一位置挂多个词元（同义词），位置表变为 (pos, token) 序列且 pos 可重复：
先把每条表按 pos 去重为「该位置可接受词元集合」，对齐仍按调整值归并；
命中判定从「pos_j == p+j-1」改为「p+j-1 在表 j 的去重位置集合中」；
计数按**起始位置**计（同一 p 的多个词元组合只算 1 次），避免组合爆炸。

## 布尔 AND（phrase）

- 按最短链驱动：候选文档号取自最短链，其余链各持一个持久指针，
  候选到来时只前进不后退（归并式跳跃），相等则保留候选。
- 每条其他链的指针总推进量 <= 其链长，故总比较次数 <= Σlen + 最短链长×(k-1)，
  在链长可比的常规数据下 <= 4 × 最短链长 × 链数（测试按此断言）。

## 合并（merge）

- 多路归并：各段词典有序，用最小堆按词归并；同词的各段链按段龄顺序拼接
  （文档号全局递增，段龄序即文档号序，结果仍有序），每条倒排项读一次、写一次，
  计数器断言 reads == writes == 总项数（无删除时）。
- 删除：删除位图按段记录；未合并前查询用位图过滤；合并时跳过被删项（物理清除）。
- 崩溃安全：新段写 `*.tmp` 后 rename 才替换段集合；崩溃残留 tmp 在下次打开时清理，
  旧段始终可用，查询结果与合并前逐字节相同。
- 并发：段集合用 `atomic.Pointer` 存不可变快照，查询取一次快照即得一致视图；
  删除/合并用写时复制换快照，`-race` 干净。

## 边界语义

空索引查询返回空；查询词不存在返回空；短语长 1 退化为单词查询；
短语长超过文档词数必不命中；空串词元合法；重复 (doc,pos) 拒绝并计数。
