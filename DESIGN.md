# 采样剖析与热点归因器 — 设计推导

## 1. self 与 total

采样得到的是一条完整调用栈（根→叶）。每次插入沿路径给每个节点 `total++`，
仅给叶子节点（最内层帧，CPU 实际所在帧）`self++`。

- 节点定义：`total(n)` = 以 n 为子树根的采样数；`self(n)` = 仅终止于 n 的采样数。
- 恒等式 1：整棵树 `Σ self == N`（总采样数）。每条样本恰有一个叶子，被计数一次。
- 恒等式 2：`Σ total >= N`，单链深 1 时取等，存在祖先时严格大于，因为祖先样本被
  自身和每个后代各计一次，故不能把 total 当 self 使用。

## 2. 递归帧的函数级归因

同一函数 F 可出现在一条栈的多层：`A→F→G→F→H`。按调用路径建树时产生两个 F 节点，
外层 F 是内层 F 的祖先。外层 F 的 total 已包含内层 F 子树的所有样本；若把两个 F
节点的 total 相加，内层子树被计入两次。

因此函数 F 的函数级 total 只统计**最外层 F 节点**：遍历每条栈路径时用 depth 阈值
`seen[F]`，仅在“不被另一个 F 包含”的节点上计数一次；函数级 self 则可安全累加全部
F 节点的 self（self 只落在叶子，天然不重叠）。对 `A→F→G→F→H`（1 条样本）：
外层 F total=1，内层 F total=1，函数级 total(F)=1（不是 2）。

## 3. 深度截断

规范化时栈深超过 MaxDepth，保留前 MaxDepth 帧并将末帧标记 `Truncated=true`，
截断部分的样本数计入可读计数器；不丢弃整条样本。恰好等于上限不截断，上限加一才截断。

## 4. 复杂度

- 插入：用 map 存子节点，每帧一次哈希查找，代价 O(栈深)，与既有节点总数无关。
  `tree.lookupCount` 累计插入时的比较/查找次数，断言 ≤ 条数×深度×4。
- 归因：对已建好的快照做一遍扫描统计 + 一次 sort，重建次数计数器恒为 0。

## 5. 采样正确性

- 丢样：注入忙窗口时跳过该次采集，`dropped++`；`Σself + dropped == expected`。
- 时钟回拨/异常间隔：`now-prev <= 0` 时跳过本次 tick 并 `badInterval++`，不产生负值。
- 空栈：拒绝并 `invalid++`。
- 停止幂等：`Stop` 用 sync.Once，可重复调用。

## 6. 并发

采样 goroutine 在 RWMutex 写锁下更新树；查询取整树深拷贝快照后在锁外归因，
因此绝不会观察到“子加了父没加”的半更新状态。

## 7. 落盘格式与截断分类

格式：固定魔数+版本+MaxDepth+总样本/丢弃/无效/截断计数（定长头），随后逐个节点
preorder 记录（uvarint 深度、帧长+帧字节、self、total、截断标志），末尾整文 CRC32。
preorder + 深度保证恢复时父必先于子出现。截断点三类（errors.Is 可区分）：
头不完整 ErrHeader、记录不完整 ErrRecord、CRC 不匹配 ErrCRC。读到坏记录即停止，
此前记录构成最大可恢复前缀，其树仍满足 `Σself == 已恢复样本数`。
