# 设计推导（题面第三节）

## 1. 非法 UTF-8 单元的切分规则

采用 WHATWG 规范等价的「首字节决定吞入量、坏字节回退」规则：

1. 首字节 `00..7F`：1 字节 ASCII，合法。
2. 首字节是延续字节 `80..BF`（或 `C0/C1/F5..FF` 等非法首字节）：该**单字节**自身构成一个非法单元，吞 1 字节。
3. 首字节是合法前导 `C2..F4`：声明长度 n∈{2,3,4}，读后续字节；任一字节**不是延续字节 `80..BF`** 时，该前导（连同此前已确认的延续字节）构成一个非法单元，解析位置回退到那个坏字节（坏字节作为下一单元重新处理）。
4. 第二字节落在越界区间（`E0` 需 `A0..BF`、`ED` 需 `80..9F`、`F0` 需 `90..BF`、`F4` 需 `80..8F`）时同样在第二字节处判非法：只吞首字节，坏字节回退。
5. 到流结束字节都是合法延续但凑不齐：整个已收前缀在 Close 时算 1 个截断（替换模式 1 个 FFFD）。

验证：`F0 90 80 41` 中 `41` 非延续 → 回退，故 1 个 FFFD + `A`；`E0 80 80` 第二字节即越界，只吞 E0，随后 `80 80` 各为孤立延续 → 三个 FFFD；`ED A0 80`（A0 对 ED 非法）、`C0 AF`（C0 非法首字节）、`F4 90 80 80`（90 对 F4 非法）同理逐字节独立；`E2 82`+EOF 中 82 是 E2 合法第二字节，前缀整体合法但被截断 → 1 个 FFFD；`80 80` 两个孤立延续 → 两个。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 只吞首字节，坏字节重解析 |
| E0 | A0..BF | 只吞首字节，坏字节重解析 |
| E1..EC | 80..BF | 只吞首字节，坏字节重解析 |
| ED | 80..9F | 只吞首字节，坏字节重解析 |
| EE..EF | 80..BF | 只吞首字节，坏字节重解析 |
| F0 | 90..BF | 只吞首字节，坏字节重解析 |
| F1..F3 | 80..BF | 只吞首字节，坏字节重解析 |
| F4 | 80..8F | 只吞首字节，坏字节重解析 |
| 80..BF | 无（自身即非法首字节） | 吞自身 1 字节 |
| C0 C1 F5..FF | 无（非法前导） | 吞自身 1 字节 |

## 2. 切分缓存：缓存几个字节、何时立刻判非法

最长 UTF-8 序列 4 字节，等字节期间最多保留「首字节 + 2 个延续字节」= **3 字节**缓存（硬上限 3）。看到第二字节的瞬间即可判定：首字节非法、第二字节不是延续、或落在越界区间 → 立即判非法并把坏字节留在输入里，绝不等流结束。只有「字节都是合法延续但尚未凑齐」才继续等待，此时提前输出 FFFD 即为错误。UTF-16 缓存硬上限 2（偶数字节对齐），另加 1 个「高代理待配对」逻辑状态（不占字节缓存）。

## 3. par 的切点对齐

UTF-8 切点任意：段起点向前回看至多 3 个字节（常数），找最后一个非延续字节；若它是前导且声明序列跨过切点，段真正起点设为该前导位置（带上前缀重解析），否则段从切点开始。每段只输出「单元起始 ≥ 切点」的结果，统计也只计这些单元；前一段恰在切点处结束（不把未完成前缀算入），尾巴由此整体移交给下一段。例：`C3|80 80`：段0 在切点截断且不计 C3；段1 回看命中 C3 并从其开始，`C3 80` 合成一个标量，第二个 80 孤立——与单线程一致。落在非法单元中间（`E0 80|80`）：段0 在第二字节即判 E0、80 两个非法单元后到达切点；段1 回看虽命中 E0，但 E0 单元**已被前一段判定**（E0 后面紧跟的 80 已被前一段消费），规则是「回看只重解析仍在等待中的合法前缀」：判定方法为回看命中的前导必须满足「切点前没有任何字节曾使它完成或失败」——由于段0 在切点处只可能残留「全是合法延续的等待前缀」（其余情况位置早已前进），实现上段0 直接把 decoder 残留（≤3 字节且必为纯合法延续前缀）交给段1 即可，无需推断：交接的字节就是前一段的残留本身。段1 若未收到交接字节则从切点开始，收到则 prepend 交接字节。非法单元不会跨交接，因为它在第二字节确定非法的瞬间就被吞掉，decoder 中残留的永远只能是合法延续前缀。

UTF-16：第一段吃掉可能的 2 字节 BOM；其后代码单元边界恒为偶数偏移，切点向下对齐到偶数（回看 ≤1 字节）；高代理恰在段尾时作为 2 字节交接给下段配对。

## 4. 上限断点（Write 的 n 与输出的对应）

输出按完整标量追加（UTF-16 代理对两个单元一次追加，绝不出现半个）。逐单元推进；追加下一单元会越限时停止并返回 ErrOutputLimit。返回的 n = 自本次 Write 起已被完整单元消费的字节数；decoder 等待缓存中的字节尚未被消费、不计入 n，它们仍来自本次 p，调用方把 `p[n:]` 喂给新实例即可（残留在 p 末尾）。断点两侧统计各自守恒，拼接结果与不限上限逐字节相同。
