# LZ77 流式格式设计

## 推导与结论

1. **Write 切法无关**：压缩器把当前匹配起点 `p` 留在未决区间；每次新字节到达，若仍匹配就延长，失配时才提交完整匹配。匹配前的字节只有在发现匹配后才能安全并入字面量，因此编码器在内部累计输入而非在 `Write` 末尾提交。任意切法喂入同一累计序列，提交的匹配起点、长度与字面量完全相同。`Flush`/`Close` 不能再等待未来字节，所以强制结束当前匹配并吐出所有未决字节；它只改变提交时机，不改变后续字典。
2. **重叠回指**：设距离 `d<len`，第 `n` 个输出字节来自目标开始后 `n%d` 的位置；这些位置可能正是本轮新写出的字节。块复制若以重叠的 `src,dst` 调用，语义不由语言内存重叠保证。本实现始终按偏移 `out[len-1-d]` 逐字节计算；热路径只有索引和自赋值，复杂度仍是线性 O(长度)，因此不需要特殊向量化。
3. **两类距离错误**：距离超过窗口容量说明记录本身违反压缩配置，甚至引用从未保留的位置；距离不超过窗口但超过已输出字节，只说明流的当前历史不足，例如开头伪造回指。二者安全边界不同，必须返回不同错误。压缩器只从当前窗口中已插入的位置取候选，且匹配长度受剩余输入限制，所以不可能生成超窗口距离。并行块预置字典只影响块内匹配器；解压器看到的仍是一条连续输出流，回指引用的字节必须已经由前序记录输出，因此“已输出字节数”的含义不变。
4. **并行确定性**：第 `k>0` 块只读取固定切块得到的本块原文和上一块原文末尾至多窗口容量字节；匹配器初始状态由这段预置字典完全确定，不读取通道顺序、全局匹配器或其他块结果。每块按块号排序后拼接，块结束只强制提交未决数据，不改变字典；块间无需刷新标记。因此 workers 只改变执行时间，不改变字节流。

## 流记录布局

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 `0x4F4E5A37` 的 uvarint，版本 `1` 的 uvarint，窗口容量 uvarint |
| 字面量 | 标签 `1`，长度 uvarint，原始字节 |
| 回指 | 标签 `2`，距离 uvarint（从 1 开始），长度 uvarint（从 3 开始） |
| 刷新 | 标签 `3`，无载荷 |
| 流尾 | 标签 `4`，原文总长 uvarint，FNV-1a 64 位校验和 uvarint |

uvarint 为小端 base128；每个字节低 7 位有效，最高位表示后续还有字节。解码器最多接受 10 字节，且第 10 字节只允许位 0 为 1，否则为超长或 64 位溢出。压缩器只缓存一条未完整解析的尾记录，状态按字节偏移推进，所以压缩流的喂入切法不影响解码结果。
