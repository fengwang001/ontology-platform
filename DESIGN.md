# DESIGN — 只追加日志的段索引与范围回放器

## 文件格式（全部小端）

- 事件编码（event）：`seq uint64 | payloadLen uint32 | payload`，编码长 `12+len(payload)`。
- 段文件（segment）：24 字节自描述头 `magic "OSEG" | ver u16 | flags u16 | firstSeq u64 | count u64`，
  随后逐条记录：`len u32（事件编码字节数）| 事件编码 | crc32 IEEE u32（对事件编码）`。
  记录定长部分为 `len+4+4`，第 i 条记录起点 = `24 + Σ 前 i 条记录长`。
- 稀疏索引（sparse）：`magic "OSIX" | interval u32 N | numAnchors u32`，随后每条锚点
  `seq u64 | offset u64`。锚点对应段内第 0, N, 2N, … 条事件，offset 为其记录在段文件中的起点。

## 核心推导一：为什么定位必须取「不大于 from 的最大锚点」

锚点只在第 k·N 条事件上。设锚点序列 a[0]<a[1]<…（按序号严格递增）。
对任意 from，存在唯一 j 使 `a[j].seq <= from < a[j+1].seq`（或 from 超过最后锚点，取最后一个）。
从 a[j] 顺序扫描，跳过 `from - a[j].seq` 条事件即达 from，跳过数 `< N`，上界成立。
若改取「最接近 from 的锚点」：当 from 位于区间后半段，最近锚点是 a[j+1]，其序号 `> from`，
从 a[j+1] 开始扫描会永久漏掉 `[from, a[j+1].seq)` 内的事件——回放结果少数据，且无法事后检测。
反向扫描虽可补救，但要求记录可逆遍历，与「长度前缀 + 只追加」格式冲突。故唯一正确规则是
「不大于 from 的最大锚点，向前扫」。from 小于首锚点（即小于段最小序号）时无锚点可用，
从头（偏移 24）扫；按边界语义 from 被钳到最小序号，跳过 0 条。

## 核心推导二：索引为何可丢弃、可逐字节重建

索引内容 = {N, 锚点(seq, offset)}。N 是调用方参数（重建时同一 N）；
每条锚点的 seq 与 offset 完全由段文件决定：顺序扫描段，第 i 条事件的 seq 在事件头里，
offset 由记录长度累加得到，`i % N == 0` 时记一条锚点。numAnchors = `(count-1)/N + 1`（count>0）。
索引不含任何「只在写入时可知」的信息（不含时间戳、不含计数器快照），
因此删掉索引后对段重扫一遍重建，结果与原索引逐字节相同。测试直接断言字节相等。

## 核心推导三：索引失效的检测与回退

锚点 offset 被篡改（指向记录中间）时，在该偏移处：长度前缀解出的记录要么越界、
要么 CRC 不符、要么解出的事件 seq 与锚点 seq 不一致——三者必居其一，可判定检出。
检出后回退策略：放弃索引，从段头后（偏移 24）全段顺序扫描定位 from，结果仍然正确；
同时在回放报告中置 `IndexInvalid=true` 标注索引已失效，由 repair 重建。

## 段边界与跨段回放

段头记录 firstSeq 与 count，本段覆盖 `[firstSeq, firstSeq+count)`。
相邻段必须满足 `seg[k].firstSeq + seg[k].count == seg[k+1].firstSeq`（repair 校验，缺口报 ErrSeqGap）。
范围回放按段序遍历，与 `[from,to]` 求交，段间不重不漏：段 k 的最后序号 +1 = 段 k+1 的首序号。

## 截断分类（repair）

对截断到 t 字节的段：`t < 24` → 头部不完整；落在某记录的 `len` 字段内 → 长度前缀不完整；
落在事件编码内 → 事件体不完整；落在 CRC 字段内或 CRC 校验不符 → CRC 不匹配。
四类为可 `errors.Is` 区分的哨兵错误。修复 = 截到最后一条完整记录末尾，段头 count 改为实际条数。

## 边界语义（文档定义，测试锁定）

- 空日志：回放返回空结果，不报错。
- `from > to`：返回可判定错误 ErrInvalidRange。
- `from < 最小序号`：钳到最小序号从头回放，不报错。
- `to > 最大序号`：回放到末尾，不报错。
- 空载荷合法；区间恰等于一整段、跨三段均为正常路径。

## 并发一致性

写者持锁追加：先写完整记录，再更新内存与段头 count。读者不依赖 count，
顺序解码至「记录边界处干净 EOF」为止；遇到半条记录（长度/体/CRC 不全）即视为当前前缀终点。
故任意时刻回放看到的都是连续无缺口的一致前缀。`-race` 下写锁保护共享状态。
