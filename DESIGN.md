# 双时间查询器设计

每条记录携带两条时间轴：有效时间（事实在现实世界成立的区间）与事务时间（系统知道该事实的区间）。查询 `AsOf(有效时刻, 事务时刻)` 回答：「在事务时刻我们所知的、于有效时刻成立的值」。

## 一、两条时间轴为何不对称

- 事务时间只能追加。事务区间记录的是「我们当时以为的事实」，它是历史本身。若更正时直接修改已有记录的事务区间或值，等于篡改历史：此后任何「当时我们以为是什么」的查询都返回被改写后的答案，审计与回放不再可能。因此更正必须是：把旧记录的事务区间在当前事务时刻截止（`Tx.End = now`），再追加一条事务区间从 `now` 开始的新记录。旧记录仍在库中，`AsOf(有效, 旧事务时刻)` 依旧得到旧值。
- 有效时间可以回填。有效时间描述的是现实世界，允许「事后得知去年某月的事实」。所以新记录的有效区间可以落在任意过去或未来，只要事务区间从当前事务时刻开始即可。
- 推论：记录一旦写入，其事务区间起点与有效区间永不改变；唯一允许的变更是一次性的「事务区间截止」（`Forever -> now`），且只发生在当前事务时刻。

## 二、逻辑删除的表示

删除不是抹掉记录，而是表达「有效区间在某点终止」：把当前记录的事务区间在 `now` 截止，再追加一条有效区间为 `[原起点, 删除点)`、事务区间为 `[now, Forever)` 的记录。于是 `AsOf(删除前的有效时刻, 删除后的事务时刻)` 仍返回原值，而 `AsOf(删除点之后, 删除后)` 返回不存在。查询用 `(record, bool)` 返回，「不存在」与「值为零值」天然可区分。

## 三、同键矩形不相交不变量

把每条记录看作二维平面上的矩形 `有效区间 × 事务区间`。同一键的任意两条记录矩形必须互不相交，否则同一 `(有效时刻, 事务时刻)` 命中两条记录，查询结果不唯一。维持方式（全部在写锁内原子完成）：

- 写入：仅当新有效区间与该键所有「事务区间未截止」的记录都不重叠时才允许，否则报 `ErrOverlap`。
- 更正：先把匹配的旧当前记录事务区间截止，再追加新记录。旧矩形在事务轴上终止于 `now`，新矩形从事务轴 `now` 开始，二者在事务轴上相切不相交。
- 删除：同上，先截止再追加截短后的有效区间。
- 自检：`checkInvariant`（非导出）遍历全部记录，对同键记录两两断言矩形不相交；导出的 `CheckInvariant` 供 demo 调用。

## 四、无穷远终点

`interval.Forever = math.MaxInt64` 表示「至今有效」。它是普通整数终点，参与 `Contains`/`Overlaps` 比较时与有限终点行为完全一致，无需特判。区间一律左闭右开：`Contains(t) = Start <= t && t < End`，空区间（`Start >= End`）在构造与写入时被拒绝。

## 五、复杂度

- 记录按键分桶（`map[string][]record.Record`）。点查只遍历该键的版本链：N 键 × 每键 V 版时，单次检查数 `<= V`，上界取 `4V`。
- 更正只触及该键版本链：扫描 V 条 + 截止 1 条 + 追加 1 条，上界 `V + 2`。
- `asof` 包用非导出原子计数器记录每次查询检查的记录数，`store` 用非导出原子计数器记录每次变更触及的记录数，测试据此断言上界。

## 六、错误分类

三类错误可用 `errors.Is` 区分：`interval.ErrEmpty`（空/倒置区间）、`store.ErrOverlap`（写入与当前事实冲突）、`store.ErrNotFound`（更正/删除的目标不存在）。
