# 设计推导

规范化 = 逐字节「保留/删除」，输出每字节都来自原文同一字节；仅末尾策略在 `EnsureOne` 且输出为空时合成一个 `\n`。

## 1. 末尾换行策略
- `Keep`：不改动末尾。空输入输出 `""`（无字节可输出；`N("")=""` 幂等）。
- `EnsureOne`：输出非空则末尾恰有一个 `\n`（裁掉末尾空行后补一个）；输出为空也补 `"\n"`。
  选 `""` 虽也幂等，但三策略退化为两个，且「确保一个」对空文件通常即新建空行；`N("\n")="\n"` 仍幂等。
- `Trim`：非空裁掉全部末尾空行再保留一个 `\n`；空则 `""`（空文件没有「最后一行」可终止）。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

规范化输出无 `\r`、无行尾空白；末尾空行只含 `\n`，故三策略都满足 `N(N(x))=N(x)`。

## 2. 被删字节的 ToOut
输出与原文都是左闭右开区间，另加终点 `len`。把每段连续删除记为一个映射「缝」：缝前输出位置 `p`。
对缝内任一原文偏移 `i`，`ToOut(i)=p`（光标停在「顶替上来」的输出位置，即换行符**之前**）；
缝的终点 `i=end` 已属于缝后，映射 `p+保留长度`。理由：`ToOrig(p)` 取缝后首个保留字节（合成 `\n` 取输入终点），
则 `ToOut(ToOrig(p))=p`；缝内各点与缝前最后一个保留偏移共享 `p`，满足单调不减与互逆。
例 `"a \n"`：`ToOrig(1)=0(a)`、`ToOrig(2)=2(\n)`；`ToOut(1)=ToOut(2)=2`。
`"\r\n"` 同理：`ToOut(\r 偏移)=ToOut(\n 偏移)=1`。编辑器把光标放在被删空白里 → 落在行尾换行之前。

## 3. 空白缓冲超限
强制输出会把「可能的行尾空白」变永久保留：若其后真出现行尾，输出含行尾空白 → 违反语义 5（幂等/无行尾空白）。
故选择**拒绝**：返回可判定错误 `ErrPendingOverflow{OrigPos}`，已产出内容与映射保留，写入终止；
之后 `Close()` 仍按「流在错误点结束」产出最终结果（待定空白此时确为行尾，被裁掉），状态可解释，
不破坏 4/5/6。输出上限同理用 `ErrOutputLimit` 拒绝。

## 4. par 拼接
每段用 fragment 模式（`Close` 不裁末尾空白、不套末尾策略；末尾单独 `\r` 作为 carry 交右邻）规范化，
但切点若落在 `\r|\n` 或空白串中间，片段结果与整体不同。修正：对每个切点向两侧找到最近的「确定换行
（`\n` 或非空白非 `\r` 字节后的 `\r`）」作为锚点，两锚点之间的原文字节（ambiguous 区）整体重做规范化；
锚点外的各 gap 片段结果必然与整体一致（其边界已是确定行）。
修正范围有上界：一个 ambiguous 区 = 切点左侧「纯空白 + 至多一个 `\r`」到上一锚点 +
右侧同理到下一锚点；若整段无锚点则扩展并入相邻区（最坏覆盖全文，只发生在无换行输入，K 个段合并为 1 个区）。
映射拼接：每个片段/重做区产出 span 的段列表，按其原文起点平移 `nOrig`、按输出起点平移 `nOut` 后顺序合并；
合成 `\n`（仅 EnsureOne 且空输入）记为「零原文长度段」。
复杂度：正常输入每切点只重扫到最近换行，总重做量有界 O(K·行长)；无换行时合并为单区。
