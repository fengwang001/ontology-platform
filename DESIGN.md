# 设计推导

## 空行与单列空值
空行跳过，不产生记录；单列空值只能写作 `""`。两个换行相邻时中间没有任何字段，因此跳过；末尾换行结束上一条记录，EOF 也不补记录。单列表若写未引号空字段，词法上与空行不可区分，会丢失该记录，所以空值必须回写为 `""`。规范输入定义为：空字段只在单列表且为空值时加引号，其余字段仅在含 `,`、`"`、`\r`、`\n` 时加引号，引号转义为 `""`，行尾固定 `\n`（引号内 `\r\n` 原样保留）。

## 并行切点
每段都从本地零偏移运行一次“超状态”扫描：记录 outside 的 fieldStart/fieldMid、inside 的 quoteMid/quotePending 四种可能；同时记录段尾 CR。段 1 的真实状态是 outside+fieldStart；前一段段尾若在引号内，则本段真实状态取 inside 组，否则取 outside 组。段内逗号/引号/换行让 fieldStart 与 fieldMid 收敛；`"` 让 quoteMid 与 quotePending 收敛。首个分隔符前的事件先暂存，真实分支确定后提交。段边界字段通过相同记录/字段号拼接，全局偏移=段偏移+本地偏移；记录号按已提交行尾累加，故错误坐标随分支延后提交而自然换算。段尾 CR 若下一段首字节不是 LF，则在 CR 处判孤立 CR。每字节由唯一一次超状态步骤处理两个分支，扫描总量不超过 2N+O(K)。

## CR 待定
未引号字段见到 CR 进入 crPending，不立即结束字段：下一字符为 LF 才结束记录，其他字节则 CR 为非法孤立回车。半包时状态保留，EOF 时同样报孤立 CR。引号内 CR 原样入字段，不进入 crPending。并行段尾在引号外时，CR 的判定延迟到下一段首字节；段尾在引号内时，CR 是内容。

## 上限即时性
字段字节数在每个内容字节入值前递增并检查。引号字段中连续两个引号只表示一个内容引号：第一个进入 quotePending，第二个回到 quoteMid 并只增加 1；起始/结束引号不计数。字段数在产生新字段单元时检查，记录数在记录提交前检查。触发即保存终态错误并停止，后续 Feed/Close 返回同一错误。

## 状态转移
| 状态 | 普通字节 | `,` | `"` | `\r` | `\n` |
|---|---|---|---|---|---|
| fieldStart | ->fieldMid/内容 | 提交空字段/新 fieldStart | 错裸引号 | ->crPending | 提交记录 |
| fieldMid | ->fieldMid/内容 | 提交字段/新 fieldStart | 错裸引号 | ->crPending | 提交记录 |
| quoted | ->quoted/内容 | ->quoted/内容 | ->quotePending | ->quoted/内容 | ->quoted/内容 |
| quotePending | 错闭合后字符 | 提交字段/新 fieldStart | ->quoted/加一个引号 | 错闭合后字符 | 提交记录 |
| crPending | 错孤立 CR | 错孤立 CR | 错孤立 CR | 错孤立 CR | 提交记录 |

EOF：quoted 报未闭合；crPending 报孤立 CR；其余仅在本记录已有字段活动时提交挂起字段和记录；位于行首且无字段活动为空输入或空行，不产生记录。
