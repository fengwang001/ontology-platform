# 设计推导

## 1. UTF-8 非法单元切分规则

采用 WHATWG「遇错继续」规则：首字节 L 给出期望长度 k=2..4 与第二字节合法区间。
第二字节落区间外：单元=2 字节；落区间内后，每收到越界续接字节，单元=已缓冲字节数
（含越界字节，1..k-1）；字节不足到 EOF：单元=全部缓冲。首字节非法（80..BF、C0 C1
F5..FF）：单元=1 字节。合法序列完成则为一个标量。

- `F0 90 80 41`：F0 要求第二字节 90..BF，90 合法；第三字节应为 80..BF 却是 41，单元吞
  `F0 90 80` 三字节，41 重新解析为 ASCII → 一个 FFFD + 41。
- `E0 80 80`：E0 要求第二字节 A0..BF，80 越界，单元=`E0 80`；剩 80 是孤立续接字节，
  各为单元 → 三个 FFFD。
- `ED A0 80`：ED 要求第二字节 80..9F（排除代理码点），A0 越界，单元=`ED A0`；剩 80
  孤立 → 三个。
- `C0 AF`：C0 非法首字节吞 1 字节，AF 孤立续接 → 两个。
- `F4 90 80 80`：F4 要求 80..8F，90 越界吞 `F4 90`；两个 80 各孤立 → 四个。
- `E2 82`+EOF：首字节合法、第二字节 80..BF 合法，EOF 不足，吞全部缓冲 → 一个。
- `80 80`：两个孤立续接字节 → 两个。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 越界第二字节:2；非法首字节/孤立:1 |
| E0 | A0..BF | 越界:2；后续越界:缓冲长度；EOF:全部 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F | 同上 |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF | 同上 |
| F1..F3 | 80..BF | 同上 |
| F4 | 80..8F | 同上 |
| 80..BF | 无（非法首字节） | 1 |
| C0 C1 F5..FF | 无（非法首字节） | 1 |

## 2. 切分缓存上限与可提前判定点

UTF-8 最长 4 字节，最多缓冲 3 个续接字节；看到第 2 字节落特殊首字节（E0/ED/F0/F4）
区间外即可判 2 字节单元；后续位置出现非续接字节即可判缓冲长度单元；只有「合法前缀
但字节不足」才必须等到 Close。UTF-16LE/BE 最长 2 个 code unit（4 字节），最多缓存
3 字节（奇数尾字节 + 高代理后等待低代理的 2 字节）。统一硬上限 4 字节。

## 3. par 切点对齐

每段从切点 s 向前回看至多 3 字节（UTF-8 最长单元-1）到 c=s-3，从 c 跑流式解码，丢弃
所有「单元起点 < s」的单元（合法与非法都只归一段），第一个「单元起点 >= s」的单元起
正式计入本段；最后一段不限。因为解码是从字节位置确定的确定性函数，回看上下文足以
复现前缀判定，单元不会跨段重复或漏掉。`C3|80 80`：第二段回看 C3，解码单元 `C3 80`
起点<s 被丢弃，第二个 80 起点>=s 计为一个非法单元；第一段 `C3` 是截断前缀，因存在后
续段而不算截断、不算输出，拼起来恰为一个非法单元被跳过、一个 FFFD，与单线程相同。
UTF-16 切点回看到偶偏移（回看至多 3 字节）。

## 4. 上限断点

每个标量编码入输出前先检查容量，放不下立即停止：已编码的标量全部保留，当前标量与其
后字节一律不碰。Write 返回的 n 仅为已完成单元（已输出标量或已吞非法单元）的输入字节
数；还在续接缓存里的合法前缀字节不算已消费，但跨 Write 保留在实例内。调用方对同一
实例续传未消费后缀；超限后实例进入终态，续传需新实例，二者输出拼接等于无限额结果。
UTF-16 侧代理对与 BOM 同样整体检查容量。
