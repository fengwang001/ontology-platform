# 多租户配额公平队列调度器设计

## 1. 模型

任务属于某租户，含代价 `c>=0` 与租户内提交序号。每个租户有正权重 `w>0`
和一个 FIFO 队列；调度器从非空租户中选一个，执行其队首任务。

## 2. 为什么增量是 c/w 而不是 c*w

设每个租户维护虚拟时间 vt，每执行一个代价 c 的任务：

    vt_i <- vt_i + c / w_i

调度器总是选 vt 最小的非空租户。考察稳态下两租户 A、B（权重 wA、wB）
交替被选的条件：它们的 vt 必须交替成为最小值，即长期内 vt 增量相等：

    nA * (c / wA) = nB * (c / wB)   =>   nA/nB = wA/wB

即执行次数（等代价时）正比于权重。直观上权重是“消费速率”：高权租户
单位代价只让自己的 vt 前进 1/w，同样时间内可完成 w 倍的任务。
若用 c*w，同样推导给出 nA/nB = wB/wA，高权租户反而被惩罚，结论相反。

## 3. 空闲租户重新加入：vt 抬升

设系统当前所有活跃租户的 vt 已推进到约 V。租户 X 空闲很久，其 vt 仍停在
v0 << V，此刻它提交一个任务。

- 若不抬升：X 的 vt=v0 远小于所有人，它将连续被选，每次只前进 c/wX，
  直到 vt 追上 V。需要连续执行约 `(V-v0)*wX/c` 步——在本题构造里 Y 已执行
  1000 个等代价任务（w=1, c=1），即 V-v0=1000，X(w=1) 会连续独占约 1000
  步，等价于把空闲期攒成“债务”一次性补偿，独占调度器并饿死 Y。
- 正确做法：租户从空变为非空（首次到达 / 空闲后重新到达）时，
  `vt <- max(vt, now)`，其中 `now = max(所有非空租户 vt)`（无活跃租户时
  now=0）。于是 X 以与大家相同的起点参与竞争，最多在并列规则下先跑一步，
  随后与 Y 按权重公平交替，谁也不被饿死。

## 4. 代价为 0 的任务

c=0 时 vt 增量为 0，该租户的 vt 永不前进，若它队列非空就会无限被选而
独占。处理：设最小代价下界 `minCost = 1e-9`，记账增量按
`max(c, minCost)/w` 计；统计的“实际执行代价”仍按任务原始 c 计。
这样零代价任务只带来可忽略的有限推进，租户不会无限独占。

## 5. 并列打破

vt 相等（含极小浮点误差 1e-12 内视为相等）时，按租户 ID 字典序选较小者，
保证选择完全可复现。

## 6. 数值表示

vt 用 float64。极端权重下：w 极小（1e-9）时单次增量最大约 1/minW，
w 极大（1e12）时增量为 0 不会产生 NaN/Inf；权重在构造时校验
`0 < w 且非 Inf/NaN`，代价同理，避免溢出与非法除零。

## 7. 数据结构与复杂度

非空租户放入最小堆（键 vt，次序键 ID）。入堆/出堆 O(log n)，单次选择
含一次 Pop + 一次 Push，比较次数上界 `4*ceil(log2 n)`（上浮与下沉各至多
log2 n，系数 4 为保守上界）。空闲租户不在堆中，堆大小恒等于非空租户数。
`LastCmp()` 暴露单次选择的比较计数。

## 8. 背压与租户移除

每租户有队列上限，超限返回 ErrQueueFull 且不改动任何状态，其他租户不受
影响。四类错误（ErrQueueFull / ErrInvalidWeight / ErrInvalidCost /
ErrTenantBusy）均为哨兵错误，可用 errors.Is 区分。移除租户时若其队列
非空则拒绝（ErrTenantBusy），任务去向唯一且明确：任务仍属该租户，
待队列排空后才可移除。

## 9. 并发

一把互斥锁保护租户表、各队列、堆与 vt 账本；Submit / Next / 统计均串行
化。Next 在锁内完成“取队首 + 推进 vt + 堆调整”，故每个任务恰好被一个
消费者取出，不丢不重。

## 10. 确定性

同一进程内，固定的提交顺序与代价序列产生逐元素相同的调度序列。并发
提交时“提交顺序”定义为各 goroutine 获取入队锁后被串行化的顺序；demo 与
测试在单协程内按确定序列提交，再重复调度 20 次比对。
