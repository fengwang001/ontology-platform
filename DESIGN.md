# 多租户配额公平队列调度器设计

## 1. 虚拟时间为何按 c/权重 推进

目标：长期执行代价占比等于权重占比。给每个租户一条虚拟时钟 `vt`，
每执行代价为 c 的任务后 `vt += c/w`，调度器总是选 `vt` 最小的非空租户。

推导：设两租户权重 w1,w2，稳态下交替被选中，故两者 `vt` 长期相等。
在墙钟区间 T 内各自执行代价 C1,C2，则
`vt1 = C1/w1`，`vt2 = C2/w2`；稳态 `C1/w1 ≈ C2/w2`，即
`C1:C2 = w1:w2`。因此单位权重要分摊的虚拟时间是 c/w：权重大的租户
每单位代价让虚拟时钟走得慢，从而被选得更频繁。若改用 c*w，则
`C1*w1 = C2*w2`，份额与权重成反比，与目标相反。

## 2. 空闲租户重新加入：vt 抬升

调度器维护系统虚拟时间 `sysVt`：每次出队后取所有非空租户 vt 的最小值。

若租户 X 空闲很久，其 vt 停在旧小值；Y 连续执行 n 个代价 c 的任务后
`vtY` 前进了约 `n*c/wY`，而 `vtX` 不变。X 重新提交时若不抬升，
`vtX` 比 `vtY` 小 `n*c/wY`。等代价等权重下，X 每步追 `c/w`，
要连续被选约 n 步（量级等于 Y 缺席期间的执行步数，本题构造里即约 1000
步量级）才能追平，造成反向独占与尖峰。

做法：租户从空转非空（首次注册或新任务到达时队列为空）时，
`vt = max(vt, sysVt)`。于是 X 从当前系统水位开始，立即具备被选资格
（vt 不再落后上千步），且不会插队，随后按正常加权交错执行。

## 3. 零代价任务

c=0 合法。若直接 `vt += 0`，该租户 vt 永不前进，将无限独占。
处理：推进量取 `max(c, minCost)/w`，`minCost = 1e-9`。零代价任务只
推进一个极小量，既保证该租户很快让位，又不显著扰动份额统计
（统计仍记真实代价 c=0）。

## 4. 数值表示、权重范围与溢出

vt 用 float64（题目允许；有理数实现成本高且非需求）。入队校验权重
`w` 必须有限且 `minW <= w <= maxW`（`1e-9..1e9`），否则返回
ErrBadWeight，从源头避免除零、inf、NaN。单次推进量钳制到不超过
1e12，防止极端代价把 float64 推到 inf。

## 5. 并列打破与数据结构

多个租户 vt 相同时按租户 ID 字典序选最小者，保证可复现。用最小堆
（键 vt，次键 ID），堆中只放非空租户：租户空了出堆，重新非空再入堆，
故空闲租户不占选择代价。单次选择堆内比较次数受堆高约束。

## 6. 背压、移除与错误

每租户队列有上限 cap：超限返回 ErrQueueFull，该任务不进入队列，其他
租户不受影响。四类错误用哨兵错误，可 errors.Is 区分：ErrUnknownTenant、
ErrQueueFull、ErrBadWeight、ErrBadCost（c<0 或非有限）。
移除仍有排队任务的租户采用拒绝语义：Remove 返回 ErrTenantBusy，队列
原样保留；空队列才可移除。

## 7. 并发与确定性

sched 内部用一把互斥锁串行化入队与出队。并发提交时「提交顺序」定义为
获取入队锁的串行化顺序，同 ID 同 vt 并列再按提交序号 tie。因此给定确定
性任务流，调度序列逐元素一致；并发场景以锁顺序为唯一事实来源。

## 8. 复杂度

入队/出队 O(log n)（n=非空租户数），选择比较次数约为堆高
ceil(log2(n)) 的常数倍。统计 O(1) 摊销。
