# 可撤销批量重命名设计

## 模型
- `name.Namespace` 是内存中的名字集合；空串合法，`/` 等字符按普通字符处理。
- 请求为 `Old -> New`。检测阶段只读，四类冲突全部通过后才允许修改。
- 所有执行、撤销均持有同一把互斥锁；并发修改在批量期间阻塞，批量结束后继续。

## 冲突条件
四类错误用不同哨兵错误，均可 `errors.Is`：
1. 旧名当前不存在：`ErrMissingOld`。
2. 新名已存在，且它不是本批某个请求的旧名：`ErrTargetExists`。
3. 同一旧名出现多次：`ErrDuplicateOld`，记录两个新名。
4. 多个请求指向同一新名：`ErrDuplicateTarget`，记录两个旧名。
冲突检测只使用集合查找；计数器在哈希表访问处递增，因此为 `O(R+N)`。

## 自环与孤立目标
- `a -> a` 定义为无操作：不参与依赖图，也不产生日志步骤；它仍要求 `a` 当前存在。
- 有效非自环边满足每个旧名、每个新名至多一次，因而依赖图由不相交链和简单环构成。

## 安全顺序推导
把请求 `x -> y` 看作节点 x 到 y 的依赖边：执行 x 前，y 若是尚未搬走的旧名，必须先执行 `y -> ...`。
因此每条链从汇点开始，即沿边方向的逆序执行。`a -> b, b -> c` 的顺序为 `b -> c`，再 `a -> b`。
同一可执行层按新名/旧名字典序排序，保证输入打乱后步骤序列仍确定。

## 破环
环上每个节点的目标都仍存在，纯排序无法解除。对每个环：
1. 选择环内字典序最小成员 `s`，其边为 `s -> next(s)`。
2. 生成一个不属于当前命名空间、也不等于任何计划新名的临时名 `t`。
3. 追加 `s -> t`，原环变为以 `t` 为汇点的链。
4. 按逆拓扑执行，最终经 `t -> next(s)` 落位。
二、三元环分别产生 3、4 个执行步骤，但每个环只引入一个临时名。
临时名由可注入命名器生成；默认候选带序号，冲突即递增重试。尝试达到上限返回可判定错误，绝不覆盖。

## 执行与撤销
- 执行前对整批计划做完整校验并在命名空间锁内排序、破环。
- 每个成功步骤用 CRC 校验日志记录；注入失败时，按已成功步骤逆序调用撤销移动。
- 日志格式：每条记录依次为 4 字节 CRC、4 字节载荷长度、JSON 载荷。
- 撤销按记录逆序执行。解析在第一个损坏边界停止，只恢复最大完整前缀；头部或长度/载荷字节缺失为不完整，CRC 不符为损坏。
- 已完整撤销的日志再次撤销返回无操作结果，保证幂等。

## 复杂度
- 冲突检测、建图、拓扑排序均为线性；测试要求查找计数不超过 `4*(R+N)`。
- 环检测只访问有效边一次；每个弱连通环追加一个临时节点，所以临时名数精确等于环数。
