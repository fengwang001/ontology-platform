# DESIGN：结构化日志字段脱敏与采样器

仅标准库；状态在进程内存；五个包：`record` `rule` `mask` `sample` `sink` + `cmd/demo`。

## 1. 一致性采样推导

- 错误做法：对每条记录独立掷骰。同一 trace 的记录会被随机切开，链路断裂，无法还原单次请求。
- 决策只能依赖追踪 ID：`h = FNV-1a(traceID)`，`h mod 1000 < rate*1000` 则保留。
  该函数与记录内容、到达顺序、时间均无关：同一 traceID 永远映射同一桶，故同链全留或全丢。
- 单次决策恰好 1 次哈希计算（`sample.hashCount` 计数器断言），O(1)。
- 空追踪 ID：没有可归属的链，退化为按记录内容哈希（JSON），使该记录决策确定可复现；
  它天然是“单记录链”，不存在跨记录一致性问题。空串同此路径（JSON 含级别与字段，确定）。
- rate=0 全丢，rate=1 全留；error 及以上级别不受 rate 影响，强制保留。

## 2. 链不完整标记

- 规则：traceID 非空时先算链决策 `kept`；记录级别 >= error 时无条件保留。
- 仅当 `!kept && level>=error` 时置 `Record.Incomplete=true`：链被采样丢弃，只捞出了错误记录，
  该链在输出中必然不完整。链全留时 error 记录不打标记。

## 3. 嵌套路径与含点字段名的消歧

- 路径为点分序列；歧义通过转义解决：字面点写 `\.`，字面反斜杠写 `\\`。
  即 `a\.b` 表示键名 `a.b`；`a.b` 表示 map["a"]["b"]。规则与遍历共用同一解析器。
- 遍历规则：逐层求值；某层不是 map 且路径未走完 => 路径不匹配（安静跳过，非错误）；
  路径不存在同理。数组不作为路径段：数组内值按“值模式”规则处理（敏感值出现在数组也脱敏）。
- 规则两类：①路径规则，精确命中叶子键；②值模式规则，对所有叶子字符串值做正则匹配。
  值规则保证“同一敏感值在多处/数组/被引用处”全部脱敏（每棵子树只遍历一次）。
- 规则集预编译为路径 trie：每条记录按字段树走一遍 trie，路径匹配次数 ≤ 字段数 + 常数，
  与规则数无关（100 规则 × 10 万记录由 `rule.matchCount` 断言）。

## 4. 脱敏方式与不变量

- 替换：固定串 `***`，长度不保留；同值同结果。
- 哈希：`sha256(值)` 十六进制，同值同结果、不同值不同、不可逆；结果不等于原值。
- 截断：保留前 N 字符（按 rune）并加后缀 `…(+k)` 标注被截字符数；N>=len 时不截。
- 非目标字段逐字节不变：序列化原始记录，脱敏后再序列化，逐字节比对未命中路径。

## 5. record

- `Value`：`map[string]any | []any | string | int64 | float64 | bool | nil`；
  `Record{Level, TraceID, Fields, Incomplete}`。
- 深度上限 `MaxDepth=32`：构造/遍历超限返回 `ErrTooDeep` 并计数，深度参数守卫，不栈溢出。
- 循环引用：构造时沿 map/切片记录已访问指针，再次访问 => `ErrCycle`，不死循环。

## 6. sink 文件格式

- 自描述头 `"ONTLOGv1\n"`（9B）；之后每条记录：4B 大端长度前缀 + JSON 体 + 4B CRC32(IEEE)。
- 截断分类（哨兵错误，`errors.Is` 可判）：
  头不足/魔数错 => `ErrBadHeader`；长度前缀不足 => `ErrShortLength`；
  体不足 => `ErrShortRecord`；CRC 不符 => `ErrCRC`。
- 恢复语义：返回最大可恢复前缀记录数，首个损坏点之后不再读取（无边界同步）。

## 7. 错误、并发、边界

- 四类错误：`ErrConflict`（同路径两种不同脱敏方式，编译期报出冲突的两条规则）、`ErrTooDeep`、
  `ErrCycle`、sink 的 `ErrBadHeader/ErrShortLength/ErrShortRecord/ErrCRC`，均可 `errors.Is`。
- 规则集编译后只读，采样器/脱敏器无可变状态（计数器用原子操作），可多协程共享；
  并发输出按输入索引汇聚，与串行结果逐元素一致；`-race` 干净。
- 边界：空记录、无/空 traceID、rate 0/1、空串值、深度 1、数组内嵌 map 均有测试。

## 8. 测试组织

- 表驱动、每包一个 `*_test.go`、同类断言一张表一个测试函数；截断点用循环逐字节覆盖。
