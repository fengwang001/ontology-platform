# 设计推导

## 1. 代理对错误偏移（规则与理由）

规则：错误一律定位到**造成配对失败的那个 `\uXXXX` 单元的起始反斜杠**（字节偏移）。

- 孤立高代理 `"\uD83D"`：高代理后必须紧跟一个低代理才能确定码点，字面量结束，
  故高代理自身就是失败单元，偏移指向高代理的 `\`（偏移 1）。
- `"\uD83Dx"` / `"\uD83DA"`：下一单元不是合法低代理转义，失败仍在高代理起点。
  这两个串没有任何歧义：高代理不能独立解码；`x`/`A` 不是转义也不能充当低代理。
- `"\uDE00"`：低代理没有前置高代理，失败单元就是低代理自身（偏移 1）。
- `"\uDE00\uD83D"`：第一个单元已是失败单元，偏移 1；严格模式遇到错误即止，
  不会读到后面的高代理。

`"\uD83DA"` 不能解释成「U+FFFD + `A`」：RFC 8259 规定孤立代理非法，
**替换是容错策略，严格解码器禁止静默替换**；而且 `A` 是普通文本而非低代理，
无论如何都无法完成配对，只能拒绝。

## 2. 最小转义与唯一性

必须转义的集合 = `"`、`\`、U+0000–U+001F（八进制短转义优先，其余 `\u00XX`）。

- U+001F 必须转义：它属于 RFC 8259 禁止在字面量中裸出现的 C0 控制字符；
  U+007F（DEL）不在该集合内——JSON 语法只禁 0x00–0x1F，故 0x7F、
  U+2028/U+2029 及一切非 ASCII 原样输出（合法 UTF-8）。
- **单射**：解码每个 `\uXXXX`/短转义都唯一还原一个码点，且输出字符绝不可能
  来自另一种转义（例如 `n` 与 `\n` 产物不同；合法字面量里裸 `"`/`\`/控制字符
  不存在），因此不同输入不可能编码相同。
- **唯一**：每个码点的处理是确定的——短转义或六位小写 `\u00xx` 二选一，
  其余字符原样，没有可选形式（`/` 不转义、非 ASCII 不用 `\uXXXX`）。
  故最小转义字面量满足 `Encode(Decode(y)) == y`。

## 3. 语义与测试对照

| 语义（第二节） | 钉住它的测试函数 |
| --- | --- |
| 1 五类错误可区分且带偏移 | `TestDecodeErrors` |
| 2 代理对组合/校验与偏移 | `TestSurrogates` |
| 3 非法 UTF-8 双向拒绝 | `TestInvalidUTF8` |
| 4 最小转义编码 | `TestEncodeMinimal` |
| 5 往返（含随机与编码/json 对照） | `TestRoundTrip` |
| 6 跨切分点一致 | `TestStreamingSplits`、`TestStreamCounter` |
