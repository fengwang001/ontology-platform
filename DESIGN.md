# 计数草图设计推导

采用 Count-Min：矩阵 `d×w`，键的第 i 个哈希落到第 i 行。`Add` 给这 d 格各加 n；`Estimate` 取 d 格最小值。

## 1. 能不能支持减计数（Sub）

取 w=2、d=2，构造两个键 a、b：h1(a)=0,h2(a)=0；h1(b)=1,h2(b)=0（a 在第 2 行与 b 同格）。

操作序列（n 为每次计数）：
1. Add(a,5)：第1行=[5,0]，第2行=[5,0]，a 真实计数 5，Estimate(a)=min(5,5)=5。
2. Add(b,3)：第1行=[5,3]，第2行=[8,0]，Estimate(a)=min(5,8)=5。
3. Sub(b,4)：b 只被加过 3，却减 4（净计数 -1）。第1行c1 被夹到 0，第2行c0=8-4=4。
   矩阵为第1行=[5,0]，第2行=[4,0]。此时 a 真实计数仍为 5，而 Estimate(a)=min(5,4)=4 < 5。

不变量 1 被破坏：共享格被“别的键的超额减法”拉低，min 把这个污染值选了出来。
若强行保证“每个键净计数恒非负”，数学上反例消失（每格是若干非负净计数之和，必不小于 a 的净计数），
但草图无法廉价核验调用方对某键是否减过头——精确记录每键计数恰好是本结构要避免的开销。
结论：**不提供 Sub**。想“撤销”应按时段分桶：把近期数据放在独立草图里，撤销=丢弃整段草图后合并其余段，
绝不逐键减。

## 2. 保守更新是否保持绝不低估，以及对合并的影响

保守更新：先取 d 格最小值 m，只把小于 m+n 的格子提到 m+n。
不变量 1 仍成立（归纳）：加之前每格 ≥ 键的旧真实计数 t，故 m≥t；更新后被提到的格为 m+n≥t+n，
未动的格原本 ≥m，而 m 是旧最小值故有格等于 m 且必被提到 m+n，未动格 ≥m+n≥t+n。故每行仍有格 ≥ 新真值。

但它破坏不变量 2（合并同构）。例：S1 让 a 经过共享格把格子抬高，S2 单独喂时保守更新会跳过该格，
而合并后该格高度来自 S1，`max` 合并与“拼接后保守更新”逐格不再相等；点加合并又会大于保守结果。
三种结果互不相同，无法定义与单草图拼接一致的 Merge。结论：**不采用保守更新**，只用朴素点加，
以 Merge 的逐格同构为优先。

## 3. (ε,δ) 与 (w,d) 换算

标准 Count-Min 界：设 N 为总插入计数，单个键每次计数落在任一其他固定列的概率为 1/w，
该行碰撞总量的期望为 (N−c)/w ≤ e/w（令 e=N/w）。取 w=⌈e/ε⌉，则 Markov 给出单行过估 >εN 的概率 ≤1/e 量级；
d 行独立同分布地同时过估，概率 ≤(1/e)^d。取 d=⌈ln(1/δ)⌉（自然对数），过估 >εN 的概率 ≤δ。
实现中用 w=⌈e/ε⌉、d=⌈ln(1/δ)⌉，e 为自然对数底常数，误差上界表达为 `Estimate-true ≤ εN`，概率 ≥1−δ。

反向（调用方直给 w,d）：报 ε=e/w、δ=e^{−d}，必须明确这是“按上述界反解的名义参数”，
是对保证的**保守复述**，不夸大；真实误差通常更小，且非整数取整只会让实际界更紧。

## 4. HeavyHitters 阈值该比谁

直接用 `Estimate(x) ≥ threshold` 判定。Estimate 只可能偏大：真实计数 > threshold 的键，
其 Estimate ≥ 真实值 > threshold，必被选中——满足“零漏报，允许多报”。
若改用 `Estimate − εN ≥ threshold`，当真值在 (threshold, threshold+εN] 内时，
被上偏的估计减掉误差带后可能落到阈值下，造成**漏报**，违反不变量 4。
减误差带只能用于“想少报”的场景，与本不变量方向相反，故不采用。
候选集：所有 Add 过的非空键（确定性集合），HeavyHitters 仅对候选取 Estimate 并按键字典序输出。
