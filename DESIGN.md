# LZ77 流式压缩设计

## 记录布局（uvarint = 7 位/字节小端变长整数）
| 记录 | 首字节 tag | 后续字段 |
| --- | --- | --- |
| 流头 | 4 字节魔数 `L`,`Z`,`7`,`7` + 1 字节版本(=1) | 无 |
| 字面量段 | tag=0 | uvarint 长度 n，后接 n 个原文字节 |
| 回指 | tag=1 | uvarint 距离 d(≥1)，uvarint 长度 l(≥3) |
| 刷新标记 | tag=2 | uvarint 累计原文偏移 |
| 流尾 | tag=3 | uvarint 原文总长度，uvarint FNV-1a-64 校验和 |

## 推导一：与 Write 切法无关
匹配从位置 p 开始，长度 m 取决于 p+1..p+m-1 这些**已经确定的历史字节**与窗口内容；未来字节只决定匹配何时结束。
因此算法只要对每个已确定位置 p 做同一件事（贪心：插入哈希链，查当前可得的最长匹配），结果就是输入字节序列的纯函数，与到达方式无关。
压缩器缓存至多 minMatch-1=2 个“尾部未定”字节（它们可能与后来字节拼成 ≥3 的匹配），只在已知字节数足够（≥3；Flush/Close 时更少也要决策）时才对最老位置做一次决策并立即落盘。
决策一旦做出永不回改，所以 1 字节 / 7 字节 / 整段写入产出逐字节相同。
Flush 必须把所有缓存字节强制按“无未来字节”决策（强制结束当前匹配），并额外发刷新标记；之后新写入仍可引用旧历史，因为窗口不跨 Flush 清空。

## 推导二：重叠回指的复制
d<l 时一次性拷贝（如 copy(dst, src) 且两段重叠）语义依赖实现，正确语义是 RLE 式重复：out[i]=out[i-d]。
实现逐字节 `out[t]=out[t-d]` 即天然正确；复杂度为 Θ(输出长度)，而写出输出本身就要这么多工作量，且拷贝是顺序内存访问，故不额外退化、也无需先分配一块大缓冲。

## 推导三：距离两类越界为何不同
“距离>已输出字节数”是**时序错误**：流引用了尚不存在的字节，任何压缩器都不会产生，单独报错可定位损坏位置。
“距离>窗口容量”是**策略错误**：即使历史够长，编码器也只承诺在窗口容量内回指；压缩器在 match 阶段只用环形窗口内候选，绝不可能发出它。
并行块预置字典只改变块启动时窗口的**初始内容**（=上一块末尾 ≤窗口容量字节），解压侧不感知块边界：字典字节在流的线性时间线上就是“此前已输出字节”，所以“已输出字节数”的含义不变，仍是当前记录之前的全部原文。

## 推导四：并行块压缩的确定性
块 i 的匹配候选只来自“本块原文 + 上一块原文末尾窗口容量字节”，不读取任何其他块、也不读其他块的压缩产物；第 i-1 块完成只提供一段确定的字节切片。
故块输出是其输入的纯函数，与 goroutine 数及完成顺序无关；workers 只影响调度。
块边界不需要刷新标记：每块都以强制结束匹配收尾（同 Close 语义），各块记录首尾相接仍是一条记录序列，最后统一接一个含总长与校验和的流尾。

## 其他
校验和为标准库 `hash/fnv` 的 FNV-1a 64 位（非压缩库）。解压炸弹：读到回指长度后先用剩余额度 `limit-outputN` 校验，超限则在复制任何字节前失败。单个 Writer/Reader 非并发安全，仅多实例可并发。
