# Quoted-Printable 设计说明

## 1. 行末空白的推导（语义 3）

编码器逐 token 处理：原样字节为 1 字符 token，转义字节为 3 字符 `=XX`。
空格/制表符先按原样挂起（pending）。若下一个 token 到达，说明空白不在行末，
直接冲刷；若到达换行或输入结束，空白必在物理行末，改写为 `=20`/`=09`。
因此 `"a \n" -> "a=20\r\n"`，`"a "`（EOF）-> `"a=20"`，而 `"a b" -> "a b"`。

## 2. 不拆 `=XX` 与软换行位置（语义 2、3）

设当前列 col（0..76），下一个 token 长 n。当且仅当 col+n>76，或
col+n==76 且其后还会有 token 时，先输出 `=\r\n` 再输出该 token。
于是 `=` 恰好占据第 76 列；`=XX` 三字符要么同处一行要么整体到下一行。
“其后还有 token”用单遍前看实现：空白挂起、非空白到来即代表前一 token 之后仍有内容。

## 3. 软换行恰好跟在空格之后

空格保持原样，**不**写成 `=20`。严格解码器见到 `=\r\n` 时删除的是这三字节本身，
软换行前面的字节（含空格）原样保留；解码后两行重新拼接，该空格仍夹在字符之间，
并非行末空白。只有物理行真正结束于 CRLF 或输入 EOF 处的空白才需要转义。

## 4. 往返等式（语义 6）

设 N(x) 规范化换行：`\r\n` 与 `\n` 都变为 `\r\n`，孤立 `\r` 保留为字节 0x0D。则
`Decode(Encode(x)) == N(x)`。

## 5. 最小转义（语义 7）

编码器只对四类字节转义：`=`、33–126 以外字节（含孤立 `\r`）、物理行末空白、
以及为满足 76 列上限必须断开处；软换行只在 col+n>76 等位置插入，不提前断行。
故对合法 y，`Encode(Decode(y))` 与 y 的差异只能是 y 中多余转义的可打印字节
（如 `=41`）变为原样（如 `A`），或 y 提前放置的软换行被省略/重排。

## 6. 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 转义与换行 | 待补 |
| 2 行长 76 | 待补 |
| 3 行末空白/不拆=XX | 待补 |
| 4 严格解码错误 | 待补 |
| 5 跨切分点 | 待补 |
| 6 往返 | 待补 |
| 7 幂等最小转义 | 待补 |
| 复杂度计数器 | 待补 |
