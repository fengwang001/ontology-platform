# DESIGN：自定 LZ77 流式压缩

## 1. 流格式（全部变长整数 = LEB128，≤10 字节且不得溢出 uint64）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `LZ7`(3B) + 版本 `1`(1B) + winSize(varint) + maxChain(varint) |
| 字面量 | tag=0(1B) + len(varint,≥1) + data(len B) |
| 回指 | tag=1(1B) + dist(varint,≥1) + length(varint,≥minMatch) |
| 刷新 | tag=2(1B)；声明此前全部输入已可还原 |
| 流尾 | tag=3(1B) + 原始总长(varint) + CRC32-IEEE(varint) |

## 2. 第三节四处推导

**(a) 与 Write 切法无关（语义3）。** 在偏移 p 决定输出前，必须知道下一个字节是否延续当前匹配，因此压缩器始终保留 `maxMatch-1=257` 个"未决"字节（不足则等更多输入）。可判定：对已发出历史，未决区头部位置 p 取最长匹配 M；匹配只能在出现首个失配字节、或未决数据用尽时结束，两者都只取决于字节内容，不取决于 Write 边界。所以输出是输入流的纯函数；失配字节本身又成为下一段字面量/匹配的起点。字面量也不在边界提前发：只发"匹配确定后位于匹配之前"的字节。`Flush` 强制放弃观望、把全部未决字节按已有判定排空并发刷新标记——这正是 Flush 承诺（语义4）；无新输入时没有未决内容，第二次 Flush 零字节。

**(b) 重叠回指复制（语义2）。** dist<length 时目标区域与源区域重叠：一次性块复制读到的可能是未复制的旧数据或脏缓冲，结果错误；正确语义是"以输出流本身为源"逐字节 `out[i]=out[i-dist]`。实现采用 `copy(dst, dst[:len(dst)-dist])` 分轮复制：每轮 copy 读到的是此前轮次已写好的字节，等价逐字节前移；多数情况 1~2 轮完成，不退化成每字节一次的循环。

**(c) 两类距离错误。** "距离>已输出字节数"是**截断/引用不存在位置**（流损坏）；"距离>窗口容量"是**违反头声明的窗口约定**（压缩器配置无法表达的回指）。二者诱因不同、可观测性不同，必须分别报错。压缩器只在环形窗口 winSize 内取候选，且候选 dist≤已见字节，故正常流绝不可能产生后者。并行块的预置字典来自上一块原文末尾 winSize 字节：解码器在该块第一个字面量/回指之前窗口里已有这些字节，因此"已输出"按**窗口内可见历史**理解——dist 仍 ≤ 窗口现有字节数，语义不变，只是历史来自字典而非本块输出。

**(d) 并行确定性（语义9）。** 块 i 的匹配器状态 = 预置字典（块 i-1 原文末尾 winSize）+ 本块原文；匹配判定只依赖这段确定的字节内容与固定的哈希链插入顺序，不依赖块 i-1 的压缩结果、压缩耗时或完成顺序。故 worker 数只影响调度，不影响任何一块的字节输出。流衔接：整流只写一个流头；每块按块边界强制排空未决字节（块间不跨块匹配），块与块之间写一个刷新标记；最后写一个流尾（总长、全程 CRC32）。刷新标记天然承担边界，解码器无需其他信息。

## 3. 关键参数与复杂度

- minMatch=3，maxMatch=258；哈希链：head[hash] 存最新位置，prev[pos mod winSize] 存同哈希上一位置，链沿历史倒退，每位置最多考察 maxChain 个候选 → 每字节候选数 ≤ maxChain，与窗口大小、输入规模无关；总考察数 ≤ maxChain×n。
- 全同字节：每个哈希桶只有一条链、候选都在窗口内，命中率几乎 1，故每字节考察数为常数（两档相差 <2 倍），输出近似 `头+一次回指+流尾`，4 MB 远小于 64 KB。
