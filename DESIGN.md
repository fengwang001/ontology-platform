# 设计推导

## 2. 代理对与错误偏移

规则：低代理不能开始代理对，立即在该 `\u` 的反斜杠处报错；高代理只建立一个“必须由低代理完成”的待决状态。
因此，孤立高代理的错误偏移指向证明它无法完成的第一个字节：结尾引号，或其后的非 `\u` 字节。若后面是 `\u`，偏移指向第二个转义的起点：不足四位或不是低代理，都说明这个待决对失败。

所以 `"\uD83D"` 的偏移指向结尾引号；`"\uD83Dx"` 与 `"\uD83DA"` 都指向偏移 7 的 `x`/`A`；`"\uDE00\uD83D"` 在偏移 1 即拒绝。高代理的起点不是终态错误位置，因为当时仍可能收到合法低代理。

`"\uD83DA"` 不能解释成“替换字符加 A”：严格解码器不是宽容 Unicode 变换器。读到高代理时已经承诺下一个词法单元必须是低代理；原始 `A` 使整个代理对词法单元不合格。U+FFFD 会吞掉本应暴露的词法错误，也破坏严格往返。

## 4. 最小转义、单射与唯一性

JSON 字符串中必须避免原样出现的只有引号、反斜杠和 U+0000–U+001F。0x1F 是 C0 控制字符，RFC 8259 禁止未转义出现；0x7F（DEL）不是 C0 控制字符，且本身是合法 Unicode 码点，所以可随 UTF-8 原样输出。U+2028/U+2029 也是合法字符串内容；限制它们是 JavaScript 源码兼容问题，不是 JSON 语法要求。

编码对每个码点使用唯一固定表示：五个有短形式的控制字符用 `\b\f\n\r\t`，其余控制字符用小写 `\u00XX`，`"` 与 `\` 分别短转义，其他所有合法码点原样输出。转义结果都以反斜杠开头，而反斜杠本身绝不原样输出；不同字符的转义形式互不相同，原样字符与转义字符也不相交。因此映射没有歧义，`Encode` 是单射；每个输入又只有这一套确定规则，故输出唯一。

## 测试对照表

<!-- 最后补齐 -->
