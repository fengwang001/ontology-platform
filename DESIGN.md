# 批量导入幂等与断点续传器 — 设计推导

## 1. 进度粒度的推导

约束：每写一条刷一次进度则吞吐塌方；攒 N 条刷一次则崩溃时最后不足 N 条状态不明。

关键事实：本题要求写入按业务键幂等（store.Put 对已存在的相同键值是 no-op，
键同值不同则报冲突）。由此推导：

- 幂等写入意味着重放任意已写前缀是安全的，不产生重复数据、不改变存储终态。
- 因此进度允许粗粒度加回退重放：每写满 N 条（一个 chunk）刷一次区间
  [start,end)；崩溃丢失的是最后一次刷盘之后的进度，最多 N 条。
- 续传位置 pos = min(进度高水位 H, 存储实际前缀 M)：
  - 进度落后于存储（崩溃在刷盘前）：pos = H，重放 [H,M)，重写数 M-H 不超过 N。
  - 进度超前于存储（进度被保留而存储丢失或被构造不一致）：pos = M，
    以存储为准，绝不跳过进度声称存在而存储缺失的记录。
- 结论：粒度 N 只影响崩溃重放上限（不超过 N 条幂等重写）与刷盘频率，
  不影响正确性。本题取 N 为可配置参数（测试用 1000，小批次用 2~4 便于构造边界）。

若写入不幂等，则必须逐条刷进度（细粒度）才能精确保留崩溃点，吞吐受损——
这正是本题选择幂等写入的收益。

## 2. 重复导入的幂等判定

- Prepare 先查提交标记文件 <id>.commit（含记录数与清单 CRC32）。
- 已存在则直接返回 Result{AlreadyDone:true}，不写任何记录，存储逐字节不变。
- 未提交则走 Write、Verify、Commit；中途失败可任意重试，因写入幂等且
  续传位置由存储实际前缀决定，最终存储与一次性成功逐字节相同
  （测试用 store.Snapshot() 的字节比较断言）。

## 3. 为什么以存储为准而非以进度为准

- 进度文件是提示（hint），存储才是事实（source of truth）。进度可能
  超前（文件被保留而存储重建）、落后（崩溃在刷盘前）、损坏（截断）。
- 超前时相信进度则永久丢失记录；相信存储只多付一次 O(log n) 前缀探测。
- 实现：导入器顺序写入，存储中本批次记录恒为清单前缀 [0,M)，
  二分查找 M 只需 O(log n) 次探测，不重头扫描、不重写 7 万条；
  非导出计数器 rewrites 记录幂等重写条数，测试断言不超过 N。

## 4. 进度文件格式与截断恢复

    PGR1 <hex(batchID)> <crc32>\n        头部行
    I <start> <end> <crc32>\n            区间行，可多条，追加写

每行 CRC32 覆盖该行自身内容（不含 CRC 字段）。恢复规则（最大可恢复前缀）：

- 头部无换行或字段残缺：ErrHeaderIncomplete，恢复为空进度；
- 任一行语法完整但 CRC 不符：ErrCRCMismatch，丢弃该行及之后全部；
- 区间行字段残缺：ErrIntervalIncomplete，丢弃该行及之后全部；
- 截断恰好落在行边界：文件合法，区间数变少，属正常回退。
- 恢复后只保留最长合法前缀，续传从该前缀的高水位继续，结果与不崩溃一致。

## 5. 占用标记（锁）与崩溃接管

- <id>.lock 内容：holder token 加过期时间；创建用 O_EXCL。
- 已存在且未过期（可注入时钟判定）：立即返回 ErrInProgress（可判定错误）。
- 已过期：安全接管（删除重建）。正常结束释放；进程崩溃遗留的锁靠 TTL 过期。
- 锁按批次 ID 分文件，不同批次可并发；-race 干净。

## 6. 对账（reconcile）的线性性与在途语义

- 一遍扫清单（n 次 Has）加一次取存储键集（1 次 Keys），存储访问不超过 n+1，
  键集合用 map 去重，绝不逐条回查清单，整体 O(n)。
- 输出三段：已写入区间（清单下标在存储中出现的连续段）、缺口区间
  （缺失下标的连续段，精确到端点）、多余记录（存储有而清单无的键）。
- 未 Commit 批次的已写入记录计为在途，不计入已提交统计；
  已提交批次（有 .commit 标记）才计入 Committed。

## 7. 阶段划分与资源上界

Prepare（校验、加锁、载进度）到 Write（按不超过 N 的 chunk 写并刷区间）到
Verify（全量 Has 校验）到 Commit（写提交标记）。

- 导入器工作缓冲峰值等于 chunk 大小，不超过 N，计数器 peak 断言不越界；
  清单本体由调用方持有，可流式供给。
- 边界：零记录批次直接 Commit 为已完成；单记录正常；批内重复键整批拒绝并
  报键与两处下标；空串键合法；空批次 ID 拒绝；进度文件不存在视为从头开始。
