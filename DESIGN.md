# DESIGN

方言：逗号分隔；记录以 `\n` 或 `\r\n` 结束；引号字段内 `""` 表示一个字面引号，引号字段内的逗号、`\n`、`\r\n` 原样保留为内容。

## 推导一：空行与单列空值

空行（行首即遇换行）若解析为「一个未引号空字段的记录」，则单列文件里 `a\n\nb\n` 的中间空行会制造一条 1 列记录——看似无害，但多列文件中混入空行会得到列数 1 的记录，触发列数错误，语义上「空行」与「空记录」无法区分；更关键的是回写：若空行是记录，则单列空值记录可写成空行，反之单列空值记录也可以不写引号。但二者不能同时成立，否则 `""` 与空行同义，引号标记丢失。因此选择：**空行一律跳过，不产生记录**（空输入解析为空表）。推论：单列表中「唯一字段为空的记录」若不加引号写成空行，解析时会被跳过，整条记录丢失——所以回写器**必须**把它写成 `""`。于是「必要加引号」的完整集合为：字段含 `,`、`"`、`\r`、`\n`，或字段带引号标记（`""` 解析出的空字段），或「单列记录的唯一空字段」。由此规范输入定义为：记录均以 `\n` 结尾（含最后一条）；字段最小加引（仅上述必要情形）；引号字段内字节原样。对规范输入 `Write(Parse(x)) == x` 逐字节成立。

## 推导二：par 的切点与拼接

切点落在引号字段内时，后续段无法自判起点状态。方案：段 i>0 用两种假设各解析一遍——`In=false`（起点在引号外）与 `In=true`（起点在引号内）。续传快照只需 1 bit，因为上一段末字节（每段 worker 可读前一字节）确定了其余状态：末字节为 `"` 时 In=true 即引号字段内（`""` 的左半，下一字节按引号字段内容处理），In=false 即引号刚闭合（下一字节只接受 `,`/换行/EOF）；末字节为 `\r` 时按推导三在段首预挂 CR 待定；其余末字节下 In 直接区分「未引号字段中」与「引号字段中」。每段产出：完整记录、末端开放记录（已完成的字段列表 + 进行中的字段前缀）、末态（In、末字节、记录内已完成字段数）、首个错误。拼接从左到右：段 i 的真实入态 = 段 i-1 真实出态的 In，据此选取段 i 的对应假设结果；相邻段的开放字段/开放记录首尾相接（前段进行中字段前缀 + 后段字段 = 完整字段，字节偏移为各自绝对偏移，无需换算）；记录号 = 之前各段完整记录数 + 段内序号；错误的记录号/字段号同理由前缀段的状态累加得到。单线程流式解析在同一字节序列上经历的状态序列，与「真实入态」假设下的分段解析逐字节一致（状态机确定性 + 快照无损），故拼接结果逐位相同。每段最多解析 2 遍，总处理字节数 ≤ 2n + K。

## 推导三：CR 待定状态

未引号字段末尾（或引号刚闭合后）见到 `\r` 时，是 `\r\n` 行尾还是孤立 `\r` 错误，取决于下一字节，故进入「CR 待定」状态：半包续传时该状态原样保留在解析器内，下一次 `Feed` 的首字节（或 `Close`）来裁决——`\n` 则结束记录，否则报错（引号闭合后的待定 CR 同理，只是错误类型为「闭合后多余字符」）。`par` 中若上一段末字节是 `\r`，本段在解析前先把该 `\r` 按待定逻辑用本段首字节裁决（计入前段假设的处理次数），裁决结果再决定本段初始状态；末段在 `\r` 处结束（`Close`）时，按「其后无 `\n`」判孤立 `\r` 错误。引号字段内的 `\r` 一律是内容，不进待定状态。

## 推导四：上限的「立刻」

单字段字节上限按**写入值缓冲的字节**计数：未引号字段每字节计 1；引号字段内普通字节计 1，`""` 逃逸对只在确认第二个 `"` 时计 1（即引号字符本身），定界引号不计。每写入一个字节先检查 `count+1 > MaxFieldBytes`，超限立即返回错误，不缓冲后续字节。字段数上限在字段**完成**入列前检查（当前记录字段数已达上限即拒），记录数上限在记录**完成**时检查。任一超限或语法错误后解析器进入终态：已产出的完整记录保留，后续 `Feed`/`Close` 返回同一错误。

## 状态转移表

状态：FS=字段开始，UQ=未引号字段中，IQ=引号字段中，QC=引号字段中刚见引号，CR=CR 待定（未引号），CRC=CR 待定（引号已闭合）。

| 状态 | 字节 | 动作 | 下一状态 |
|---|---|---|---|
| FS | `,` | 产出未引号空字段 | FS |
| FS | `"` | 开始引号字段 | IQ |
| FS | `\n` | 空行：跳过（记录字段数=0 时）/ 结束记录 | FS |
| FS | `\r` | 同上但待定 | CR |
| FS | 其他 | 开始未引号字段，写入 | UQ |
| UQ | `,` | 完成字段 | FS |
| UQ | `\n` | 完成字段+记录 | FS |
| UQ | `\r` | 待定 | CR |
| UQ | `"` | 错误：未引号字段含引号 | — |
| UQ | 其他 | 写入 | UQ |
| IQ | `"` | 待定（闭合或逃逸） | QC |
| IQ | 其他（含 `\r` `\n` `,`） | 写入 | IQ |
| QC | `"` | 写入一个字面引号 | IQ |
| QC | `,` | 完成字段 | FS |
| QC | `\n` | 完成字段+记录 | FS |
| QC | `\r` | 待定 | CRC |
| QC | 其他 | 错误：闭合后多余字符 | — |
| CR | `\n` | 完成字段+记录（或跳过空行） | FS |
| CR | 其他 | 错误：孤立 CR | — |
| CRC | `\n` | 完成字段+记录 | FS |
| CRC | 其他 | 错误：闭合后多余字符 | — |

`Close` 时：IQ/QC 未闭合→引号未闭合错误；CR/CRC→孤立 CR / 闭合后多余字符；UQ→完成字段+记录；FS 且当前记录无字段→无操作（末尾换行不产生空记录）。

## 其他约定

- 字段偏移 `[Start,End)` 为该字段在原文中的字节区间（含定界引号），绝对偏移从 0 起。
- 记录号、字段号从 1 起；错误同时携带字节偏移（从 0 起）。
- 单个流式解析器实例不是并发安全的；`par` 内部 goroutine 只写各自段结果，主 goroutine 拼接。
