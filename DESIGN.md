# 设计推导（UTF-8⇄UTF-16 流式转码器）

## 1. 非法单元切分（WHATWG 风格，逐字节驱动）
首字节 b1 决定期望长度；随后逐字节喂入，一旦某字节使序列"无法再成为合法序列"，立即产出一个
U+FFFD：**该首字节被吞掉**，当前字节（若是续字节）作为新单元重新解析（退 1）；当前字节不是续字节则
不回退。若一直是"合法前缀"直到流结束，Close 时算一个截断，替换模式产 1 个 U+FFFD 并吞掉全部残留。

解释样例：`F0 90 80 41`：F0 第二字节须 90..BF，90 合法；第三字节 80 是续字节但缺第四字节，此刻前缀
仍合法（F0 90 80 是 4 字节序列的合法前 3 字节），不能判错；到非续字节 41 才确定失败，F0 90 80 一起
被吞成 1 个 FFFD，41 重新解析。`E0 80 80`：E0 第二字节须 A0..BF，80 立即非法，E0 吞成 1 个 FFFD，80
重新解析；80 是孤立续字节吞成第 2 个；第二个 80 吞成第 3 个。`ED A0 80` 同构（ED 第二字节须 80..9F）。
`C0 AF`：C0 是非法首字节（直接吞），AF 续字节再吞 →2 个。`F4 90 80 80`：F4 第二字节须 80..8F，90 非法
→1 个；之后每个续字节各 1 个，共 4。`E2 82`+EOF：E2 82 是 3 字节序列合法前缀，截断产 1 个（不是 2）。
`80 80`：两个孤立续字节各 1。

第二字节区间与吞掉字节数（cont=续字节 80..BF；"1,退1"=吞首字节并把当前续字节重解析）：
| 首字节 | 第二字节合法区间 | 第二字节非法时 | 第3/4字节非法时 |
|---|---|---|---|
| C2..DF | 80..BF | 非cont:吞1不回退；cont不会发生 | — |
| E0 | A0..BF | 非cont:吞1不回退；cont(80..9F):吞1退1 | 非cont:吞2不回退；cont到EOF:截断吞2 |
| E1..EC | 80..BF | 非cont:吞1不回退；cont不会发生 | 同上(吞2/3) |
| ED | 80..9F | 非cont:吞1不回退；cont(A0..BF):吞1退1 | 同上 |
| EE..EF | 80..BF | 同 E1..EC | 同上 |
| F0 | 90..BF | 非cont:吞1不回退；cont(80..8F):吞1退1 | 非cont:吞k不回退；cont到EOF:截断吞k |
| F1..F3 | 80..BF | 非cont:吞1不回退；cont不会发生 | 同上(k=3 或 4) |
| F4 | 80..8F | 非cont:吞1不回退；cont(90..BF):吞1退1 | 同上 |
| 80..BF（开头或退1重解析时） | — | 吞1（孤立续字节，不回退） |
| C0 C1 F5..FF | — | 吞1（非法首字节，不回退） |

## 2. 切分缓存上限与放弃等待的时机
缓存只存"仍可能扩展为合法序列"的前缀。合法序列最长 4 字节，缓存硬上限为 **3 字节**（首字节 + 至多
2 个后续字节；收到第 4 个合法字节必立刻解码清空）。何时可立即判错、不等 EOF：
- E0 收到 80..9F、ED 收到 A0..BF、F0 收到 80..8F、F4 收到 90..BF：第二字节即越界，立刻 1 个 FFFD 并退 1。
- 任一阶段收到非续字节：立即终结当前前缀（吞掉已缓存的合法前缀，产 1 个 FFFD）并重解析该字节。
只有"合法前缀但续字节不足"（如 E2 82 后 EOF）才必须等到 Close。故切分点落在合法字符中间时绝不会提前
输出 FFFD；任意分片拼装与整段喂入跑的是同一个逐字节状态机，结果一致。

## 3. par 的切点对齐
段 k 从偏移 s[k] 开始。取**前一段末尾最多 3 字节** pre 作"静默引导"：worker 先把 pre 喂入只推进 pending、
不输出、不计数的状态机，再正常处理本段 [s[k], s[k+1])。核心性质：距边界 3 字节以外不可能还有跨越边界
的待定前缀（4 字节序列若跨过切点，首字节必在切点前 3 字节内），故静默引导足以在切点重建与顺序解析逐
字节相同的 pending。段尾残留（如 `C3`）留在 pending，由下一段引导重放；FFFD 只在下一段看到决定性字节
（或末段 Close）时产出，全局 FFFD 个数与顺序版相同。
例：切点 C3|80 80。顺序：C3 80 合成字符，第二个 80 孤立→1 FFFD。并行：段尾 pending=C3；下段静默引导
C3 后喂入 80（合法字符，被前缀吸收）、再 80（孤立→FFFD），一致。首段从头解析（含 BOM）；仅末段
Close（截断只可能在整个流末尾）。吞掉字节按"决定性字节落在哪段"归属，总和与顺序版相同；每切点引导
≤3 字节且引导不计全局检查数，故 par 检查次数 ≤ N + 3K。

## 4. 输出上限断点
Write 在写每个已解析单元前检查剩余容量，不够则停在**标量边界**（此时 pending 必为空：不完整前缀从不
产出，停下点只在单元之间）。返回 n = 本次入参中已被完整解析单元消费的字节数；残留在 pending 的字节
**不算已消费**（调用方用 Pending() 取回，连同剩余数据喂给新实例）。"新实例(Pending()+剩余)"的拼接与
无上限一次转完逐字节相同；超限实例冻结。BOM 字节计入输入守恒但不产标量：合法标量字节+非法吞掉字节
+BOM 字节 == 已消费输入字节。

## 5. 复杂度计数
每个输入字节进入状态机恰好被检查一次（退 1 重解析发生在下一次状态机步，检查的是"该字节作为新单元
首字节"），整段与逐字节喂入检查次数都 == N，测试用 ≤2N 作宽松上界。par 不计静默引导，额外检查仅各段
≤3 字节引导：≤ N + 3K ≤ N + 8·常数。
