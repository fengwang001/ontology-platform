# LZ77 流式压缩格式设计

## 流格式（所有整数为 Uvarint，见 `wire`）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4C 5A 53`（"LZS" 魔数）+ 1 字节版本 `01` |
| 字面量段 | `00` + varint 长度 n(≥1) + n 字节原文 |
| 回指 | `01` + varint 距离 d(≥1) + varint 长度 n(≥1) |
| 刷新标记 | `02`（无负载） |
| 流尾 | `03` + varint 原文总长 + varint FNV-1a64 校验和 |

匹配长度上限 `MaxLen=1024`，最小匹配 3，哈希取 3 字节。

## 推导一：压缩结果与 Write 切法无关（语义 3）

位置 p 上的决策只依赖两类数据：(a) p 之前的历史字节（哈希链只含已
处理位置，未来字节永远不会进入 p 的历史）；(b) 从 p 开始的待压字节本身。
因此：(1) 字面量决策在做出时即最终——历史已固定，后续输入不会改变
「p 处无匹配」的事实。(2) 匹配决策只有当匹配终点已知时才最终：若匹配
长度 L 达到当前缓冲末尾（L == avail，只可能在 avail ≤ MaxLen 时发生），
更长的输入可能让匹配继续延长，此时必须**挂起**，把 p 起的数据留在未决
缓冲里等更多输入；若 L < avail（失配字节已在缓冲中，或被 MaxLen 截断），
则匹配终点已确定，立即输出。挂起的匹配在更多输入到达时**整体重算**
（不重算会引入候选选择顺序的差异）；因为历史未变，重算结果与一次性
喂入全量数据完全一致。推论：`Flush` 必须把「缓冲末尾」临时当作「数据
末尾」强制结束当前挂起匹配并输出，否则 Flush 之前的令牌会依赖 Flush
之后的输入，违背语义 4 的承诺；Flush 之后窗口保留，匹配仍可回指之前
的数据。缓存量上界：未决匹配最多 MaxLen 字节参与重扫，重扫成本有界。

## 推导二：重叠回指的解压复制（语义 2）

距离 d < 长度 n 时，源区间与目标区间重叠：块复制（memcpy 式）读到的是
尚未写出的字节，结果错误；逐字节向前复制则每写出一个字节立即可作为
后续源字节，语义正确。本实现用环形窗口 `Window.At(d)` 取一字节、
`Write` 写一字节，单字节 O(1)，总量 O（输出长度）。每个输出字节本来就
必须产生一次，O(n) 已是最优下界，所以逐字节循环在这里**没有渐进退化**，
无需引入「d ≥ 剩余长度时块复制」的快路径。

## 推导三：两个距离错误为何区分（语义 7）

「距离 > 窗口容量」是格式级不可能事件：编码器的匹配器只在窗口内搜索，
链遍历在距离超过容量时即停，因此合法流**绝不可能**包含这种回指，出现
即说明结构性损坏。「距离 > 已输出字节数」是状态级错误：距离本身可能在
容量内，只是当前输出还不够长（流前半段被篡改时常见）。两者违反的不变
量不同，必须可区分。并行块压缩中，预置字典只是让编码器窗口预先装填
上一块末尾字节；对解码器而言流是连续的，「已输出字节数」含义不变——
字典回指就是普通的、距离不超过累计输出的回指。

## 推导四：并行块压缩的确定性（语义 9）

块 i 的压缩输出是纯函数 f（块 i 原文， dict=data[start-wcap:start])：
dict 直接取自**原文**（上一块末尾窗口容量以内的字节），不取自任何其他
块的压缩结果，因此与块数、调度顺序、worker 数无关；各块结果按下标拼接
即确定。流格式上块间**不需要**刷新标记：记录全部自定界，解码器状态
（窗口、累计输出、校验和）天然连续，块边界在格式中不可见；预置字典
产生的回指对解码器就是普通回指（见推导三）。整个流只含一个流头与一
个流尾，流尾覆盖全部原文。
