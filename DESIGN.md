# 设计推导

## 3. 续行遇到空白行

物理行拼接只由“行尾反斜杠数为奇数”触发；该反斜杠本身属于转义状态，不作为内容保留。
`java.util.Properties` 找到续行标记后，会先读取下一物理行，并在追加前跳过其首部连续空白。
因此下一行为空或仅含空白时，跳过首部空白后剩余长度为 0：没有任何字符被追加，当前逻辑行立即结束。
该空白行也不会再作为独立空行处理，因为它已被前一逻辑行的续行请求消费。
这说明“首部空白剥离”发生在“是否还有续行内容”之前；普通空白行是空逻辑行，作为续行出现则只是终止续行链。
文件 EOF 处的单个结尾反斜杠同理：没有下一物理行可追加，反斜杠丢弃，逻辑行结束。

## 6. Store 的最小必要转义

回写每行固定为 `key=value\n`；即使键和值都为空也写 `=`，否则空行会被 Load 忽略。
键位于逻辑行行首：首字符若为空白、`#` 或 `!`，会分别被行首空白规则或注释规则吞掉；因此键的首个空白、`#`、`!` 必须转义。
键内部的空白、`=`、`:` 会终止键，必须转义；但键内部的 `#`、`!` 不可能再开始注释，不必转义。
值总在分隔符之后，行中的 `=`、`:`、`#`、`!` 均无语法意义，不必转义。
值开头的空白会被“分隔符后空白”规则吃掉，必须转义；一旦值已经开始，中间空格不再被跳过，因此不必转义。
反斜杠始终转义，否则会改变后继字符；`\n`、`\r` 写成 `\n`、`\r`，避免制造新物理行。
`\t`、`\f` 仅在键内或值开头（即仍可能被当作分隔空白的位置）转义；普通内容中原样保留。
非 ASCII 字符不是本格式语法字符，按 UTF-8 原样输出。
