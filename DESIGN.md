# 严格模式 Base64：设计推导

## 第 2 条：非规范尾部的统一规则（推导）

一个无填充 4 字符组 c0c1c2c3 携带 24 比特：
v0<<18 | v1<<12 | v2<<6 | v3，其中 vi 为字符的 6 比特值。
原始字节数 n 只能是 1 或 2 时（组尾分别为 `xx==`、`xxx=`），有效载荷不足
24 比特，Base64 规定「数据比特之外的比特必须为 0」：

- n=1（`c0c1==`）：只有高 8 比特是数据。c1 的 6 比特全部落在数据之外，
  故其低 4 位必须为 0，即 **v1 & 0b001111 == 0**；否则组内存在两种编码
  表示同一字节串。`QQ==`（v1=16，合法）；`QR==`（v1=17，低 4 位非 0，
  被拒）——宽松解码器丢掉这 4 位也得到 A，所以严格模式必须拒绝。
- n=2（`c0c1c2=`）：16 比特数据，超出的 2 比特在 c2 低端，故
  **v2 & 0b000011 == 0**。`QUI=`（v2=18，合法）；`QUJ=`（v2=19，被拒）。

统一规则：**有效载荷之外的所有尾部位必须为 0；填充恰好为 0（无 `=`）、
2（`==`，查 c1 的低 4 位）、1（`=`，查 c2 的低 2 位）个，其余皆非法；
`=` 只能出现在最末组**。它保证编码是单射：任意被接受的解码结果 y 满足
Encode(Decode(y)) == y，即「任意字节串的编码结果唯一」。

## 错误分类

1. 非法字符 ErrIllegalChar（含组内/裸 \r、空格、Tab）
2. 非规范尾部 ErrNonCanonical（尾部位非 0）
3. 填充位置错误 ErrPadding（`=` 的位置/数量非法，或填充后又有数据）
4. 长度错误 ErrLength（流结束时残留非空组）
5. 换行位置错误 ErrNewline（非 MIME 模式出现换行，或换行切断组）
另加 ErrLimit（超限，独立于五类编码错误）。所有错误经 DecodeError 携带
流内绝对字节偏移 Offset；规则只依赖已见到的字节，故与切分方式无关。

## 测试对照表（最后补齐）

| 语义 | 测试函数 |
| --- | --- |
| 1 标准字母表/填充/76 列 MIME | （待补） |
| 2 规范形式样例与 Encode/Decode 单射 | （待补） |
| 3 换行位置 | （待补） |
| 4 五类错误可区分 + 偏移 | （待补） |
| 5 跨切分点一致 | （待补） |
| 6 往返（含 MIME 两档、空串/余数） | （待补） |
| 7 输出上限在组边界停下并终态 | （待补） |
