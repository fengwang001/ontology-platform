# DESIGN — 统一差分/补丁

## 1. 零计数 hunk 头的行号（语义 4）

统一定义：hunk 头 ` -a,b` 的 a 为「该 hunk 覆盖的第一行旧行的 1 基行号」；
当 b=0（hunk 不含任何旧行，纯插入）时，取**插入点前一行旧行的行号**，
插入点在文件开头时取 0。对新文件侧 c 同理。

推导：头的含义是「从第 a 行开始的 b 行」。纯插入没有「第一行」，GNU 的
约定是报告插入点锚定的前一行号（行间间隙用其前一行编号）。样例 1
`x\n`→`y\nx\n` 在文件开头插入：前面没有行，故 `-0,0 +1`。样例 2
`a\nb\nc\n`→`a\nb\nX\nc\n` 在第 2 行与第 3 行之间插入：前一行是
第 2 行，故 `-2,0 +3`。样例 3 删除唯一一行，新侧为空，最后一行旧行是
第 1 行，删除点之后无行，故 `-1 +0,0`。Apply 解析时：b=0 把插入点放在
旧文件第 a 行**之后**（a=0 即文件开头），不消费任何旧行；d=0 时删除点
位于新文件第 c 行之后，不产生新行。

| 旧侧 | 书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一旧行行号（开头为 0） | 不消费旧行；插入点 = 第 a 行之后 |
| b=1 | `-a`（省略 `,1`） | 消费旧文件第 a 行这 1 行 |
| b≥2 | `-a,b` | 消费从第 a 行起连续 b 行 |

## 2. hunk 合并阈值（语义 5）

阈值 g = 2C：两段改动之间未改动行数 g ≤ 2C 时合并，g > 2C 时分开。
推导：每个 hunk 在改动两侧各取 C 行上下文。若合并后两改动之间保留
g 行上下文，则左改动需要其右侧 C 行、右改动需要其左侧 C 行；当
g ≤ 2C 时这两份上下文需求不冲突（中间行可同时充当两边上下文），
GNU `diff -U C` 输出一个 hunk；g = 2C+1 时中间必有至少 1 行不属于
任何 hunk，于是输出两个 hunk。反证其他取值：C=1、g=2 时 GNU 输出
一个 hunk（如 `a\nx\ny\nb\n`→`a\nX\nY\nb\n` 得一个 `-2,4` hunk），
故阈值不能是 C；C=1、g=3 时 GNU 输出两个 hunk
（`a\nx\n\n\ny\nb\n`→`a\nX\n\n\nY\nb\n` 为两个 hunk），故阈值不能
是 2C+1。结论：恰好 2C 合并，2C+1 分开。

## 3. 并列最短脚本的选择（语义 3）

Myers 沿 k 对角线填表，编辑图中向下=删除、向右=插入。同一编辑距离 D
存在多条最短路径时固定**优先删除（先向下）**：每轮从 k=D 到 k=-D
（步长 2）扫描，k=D 与 k=-D 仅有一个合法来源；其余 k 比较 k+1（向下）
与 k-1（向右）两个前驱，取 x 较大者，即优先来自 k+1 的向下步。效果：
`a b`→`b a` 的 hunk 内删除行全部排在插入行之前（`-a`、`-b`、`+b`、
`+a`），而不是 `-a`/`+b` 交错。该规则写死，输出逐字节确定。

## 4. 偏移应用后后续 hunk 的查找起点（语义 7）

前一 hunk 以偏移 +k（实际匹配点行号 − 记录行号）应用后，后续 hunk 的
查找中心整体平移 k：已应用部分造成文档漂移，后续内容相对原记录位置
恰好位移 k（本 hunk 若再含增删，其净行数变化只影响**再之后**的 hunk，
故累计平移 = 之前各 hunk 的实际偏移，而非净行数和）。每个 hunk 仍在
平移后中心 ±F 行内找精确匹配，多候选取 |距离| 最小，并列取靠前。

## 5. 其他约定

- 行 = 保留原行尾（`\n`/`\r\n`/无）的字节串，按原始字节比较；
  `\r` 属行内容，CRLF 不会被规范化。
- 无行尾行后输出 `\ No newline at end of file`，渲染时该行前缀后加 `\n`。
- 四类哨兵错误：`ErrMalformed`/`ErrContext`/`ErrOutOfFuzz`/`ErrTooDifferent`，
  均带 hunk 序号（格式错误另带补丁行号）。
- 解析严格校验头计数与体行种类/数量；空文件的「一个空格」行合法。
- 并发存储以每文档互斥锁串行化提交，日志记录成功补丁，结果等同重放。
