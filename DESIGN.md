# RLE 编解码器设计

不变量：严格解码器接受的每个 t，必须满足 `Encode(Decode(t)) == t`。

## 拒绝清单推导（给出反例 t 与规范化形式）

1. 显式次数 1（如 `1a`）：Decode 得 `a`，Encode 为 `a` ≠ `1a`。
2. 次数 0（如 `0a`）：0 次重复不是游程；且 `Encode("")` 为空串，无法产生 `0a`。
3. 前导零（如 `01a`、`00a`）：规范编码用无前导零十进制，重编码为 `a`/`0a`（后者本身非法）。
4. 相邻同符号游程（如 `2a3a`）：未取最长游程，重编码合并为 `5a`。
5. `\` 后既非数字也非反斜杠（如 `\x`、`\a`）：格式只允许转义数字与 `\`。
6. 末尾孤立 `\`：转义序列缺第二字符，无完整符号。
7. 末尾只有次数（如 `a3`、`12`）：游程必须以符号结尾。
8. 非法 UTF-8（含截断多字节序列、非法前导/续字节）：解码结果必须是合法 string，无法构成码点符号。

每类返回独立哨兵错误，错误携带输入字节偏移；重复违规取最早出现位置。

## 大次数取舍

选择支持任意大次数（用 `math/big` 逐位累加），不用定宽整数，杜绝静默溢出。
理由：格式本身对十进制位数无限制，拒绝需引入格式未规定的上限；支持不增加任何与次数成正比的
预分配。可配置的是**输出总字节上限**（默认 64 MiB）：每个游程发射前比较，超限返回可判定的
`ErrOutputLimit`，故超大次数（如 20 位）会被输出上限挡住而非撑爆内存。

## 语义—测试对照表

<!-- 最后补 -->
