# 设计推导

## 1. 非法 UTF-8 单元

统一采用 Unicode 最大合法子部分规则：从一个首字节开始，先按它声明的长度收集“截至失效点仍合法”的字节；下一字节若不能成为合法后续，这个前缀整体是一个非法单元，失效字节不被吞入并重新开始判定。独立续字节和 `C0/C1/F5..FF` 各自成一个非法单元。

- `F0 90 80 41`：`F0 90 80` 是四位序列的合法三位前缀，`41` 不是续字节，故吞 3 字节；`41` 另成字符。
- `E0 80 80`：`E0` 的第二字节必须是 `A0..BF`，`80` 立即失效；三个字节均独立成单元。
- `ED A0 80`：`ED` 的第二字节必须是 `80..9F`，同理为三个单元，也排除代理码点。
- `C0 AF`：`C0/C1` 永远非法，故两个字节各自替换；这排除非最短形式。
- `F4 90 80 80`：`F4` 的第二字节必须是 `80..8F`，故四个字节各自替换；这排除越界码点。
- `E2 82` 后结束：两位都是三位序列的合法前缀，截断时整体是一个非法单元。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 | 后续失效时吞掉 |
|---|---|---:|---:|
| `C2..DF` | `80..BF` | 1 | 完整合法前缀（最长 2） |
| `E0` | `A0..BF` | 1 | 完整合法前缀（最长 3） |
| `E1..EC` | `80..BF` | 1 | 完整合法前缀（最长 3） |
| `ED` | `80..9F` | 1 | 完整合法前缀（最长 3） |
| `EE..EF` | `80..BF` | 1 | 完整合法前缀（最长 3） |
| `F0` | `90..BF` | 1 | 完整合法前缀（最长 4） |
| `F1..F3` | `80..BF` | 1 | 完整合法前缀（最长 4） |
| `F4` | `80..8F` | 1 | 完整合法前缀（最长 4） |
| `80..BF` | 无 | 1 | 不适用 |
| `C0 C1 F5..FF` | 无 | 1 | 不适用 |

## 2. 流式缓存与判定时机

UTF-8 只需缓存 3 字节：合法首字节后，第二字节可确定总长是 3 还是 4；在第四字节到达前，最多已有首字节和两个续字节。第二字节非法时立刻结束首字节单元且不缓存非法字节；合法前缀后的非续字节到达时，立刻结束整个最大合法子部分，绝不等流结束。流关闭时仍未满足长度的合法前缀作为一个截断/替换单元。UTF-16 缓存上限为 1 个奇数尾字节；另有 1 个高代理等待低代理。

## 3. `par` 切点对齐

每个切点只回看 3 字节定位其所在单元，并顺序保证实际边界单调。若单元恰好结束在切点，边界就是其结束位置；若单元跨过切点：当该单元起点不早于上一实际边界，把整个单元交给下一段，边界移到单元起点；若它已属于当前段（多个切点落入同一 4 字节单元），边界移到单元结束点。中间段可以为空。

因此每个跨切单元只被某一段完整解析一次。例如切点在 `C3|80 80`：回看发现 `C3 80` 跨切点，交给后一段，前一段不产生替换；后一段依次得到合法字符和一个独立 `80`，与单线程的一个字符加一个非法单元完全相同。UTF-16 同理回看 2 字节并保持代理对完整。

## 4. 输出上限与消费偏移

每次输出前先检查整个标量的编码长度；空间不足即返回输出超限，不写入半个标量或半个代理对。`Write` 的 `n` 只表示本次 `p` 中已随完整标量消费的字节数；上一次调用留在切分缓存的字节在当时不算已消费，也不能让本次 `n` 追溯变大。绝对续传位置读取 `Consumed()`：它只统计已形成标量、非法单元或 BOM 的字节数；残留缓存字节不计入，因此新实例从该偏移重传即可。
