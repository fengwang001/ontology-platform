# 句柄表设计推导

句柄为 64 位：`[代号 gen:28][表标 tag:16][槽号 index:20]`。

## 1. 代号起始值与零值句柄

零值句柄要求「在任何表、任何时刻」无效（不变量 5）。若代号从 0 起，槽位 0 的
首代句柄编码恰好为 0，与 `var h Handle` 不可区分。故代号从 **1** 开始：槽位 0
第 1 代的编码低 20 位为 0、代号字段为 1，整体非 0；而零值句柄代号字段为 0。
校验时代号为 0 直接判零值错误，永不查表。

tag 是进程内全局单调计数器（回绕仅在生成 65536 张表之后，正常使用不会发生），
使 A 表句柄在 B 表必然解码出不同 tag，从而可判定为跨表错误。

## 2. 代号回绕

代号字段位宽 28，上限 G=2^28-1。若取模回绕到 0（或 1）复用，构造序列：

1. 在槽位 i 插入对象 O1，代号 1，发出句柄 H=(i,1)；2. Remove(H)，槽 i 代号变 2。
3. 在槽 i 上反复「Insert 新对象→Remove」，每轮代号 +1，共做 G-1 轮，代号回到 G。
4. 再 Remove 一次并令代号回绕为 1，随后 Insert，槽 i 代号再次为 1，发出 (i,1)。
5. 第 1 步的老句柄 H=(i,1) 现在与当前槽位的代号 1 相等，Get(H) 误判有效，
   老句柄读到复用者——不变量 1（ABA）被破坏。

处理办法：代号达到上限 G 的槽位，Remove 时**不还回空闲链表**，标记为「代号耗尽」，
永不复用。论证：该槽位代号不再变化（恒为 G 且空闲），任何历史句柄代号都 ≤ G；
最后一个被拒的老句柄代号恰为 G，但槽位已非在用态，校验仍失败；更早的句柄代号
< G，也失败。故不变量 1 成立。耗尽槽位不再属于空闲集，计入独立的「耗尽数」，
`在用 + 空闲 + 耗尽 = Cap`（不变量 3）仍逐操作成立；其余槽位的链表与代号均不受
影响，整表继续工作。

## 3. 头插还是尾插

两者都 O(1)。头插（LIFO）：每次释放的槽立刻成为下次分配的首选，热点槽位被反复
复用，其代号每轮 +1，**代号最先耗尽**。尾插（FIFO）：还回的槽排到链尾，槽位按
轮转复用，各槽代号增长均匀，任一槽耗尽都最晚。故选择**尾插**，与第 2 条
「尽量推迟耗尽、耗尽槽永不复用」一致。

## 4. 同一失效句柄连用两次

两次返回**同一个错误**（ErrStaleHandle）。失效句柄上的 Get/Remove 是只读判定：
不修改代号、不改动空闲链。由不变量 4（失败不留痕），第一次与第二次操作前的状态
逐字段相同，句柄同样不通过校验，因此结果必须相同；不存在「已处理过」的状态可记，
任何区分性错误本身都隐含了一次状态写入，违反失败不留痕。
