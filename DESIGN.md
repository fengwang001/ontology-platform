# DESIGN

## 流格式（所有整数为变长 varint：每字节 7 位、低位在前、最高位为续位，最长 10 字节且第 10 字节 ≤ 1）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `C7 71 01`（魔数 2 字节 + 版本 1 字节，定长） |
| 字面量段 | `00 varint(n) n 字节原文` |
| 回指 | `01 varint(dist) varint(len)` |
| 刷新标记 | `02`（无载荷） |
| 流尾 | `03 varint(原文总长) varint(FNV-1a64 校验和)` |

## 推导 1：压缩结果与 Write 切法无关（语义 3）

压缩器在位置 p 做决定时，决定结果只许依赖「不会随未来输入改变」的事实：
- 位置 p 的字典（p 之前的全部字节）已经固定，不随未来变化。
- 「p 处无匹配 → 字面量」只要已看到 ≥3 个前瞻字节即可判定，未来字节不改变它。
- 「p 处最长匹配长度 L」只有当匹配终点未到当前数据末尾时才确定：终点处的失配字节已经看到，未来字节无法让匹配更长。

因此压缩器缓存一段未决字节 `pend`，处理循环只在以下情况发出 token：匹配终点
在已有数据之内，或匹配长度达到上限 MaxMatch（不可能更长）。若匹配恰好延伸到
数据末尾且未满上限，则停住等待更多输入——这就是必须缓存的未决字节，上界为
MaxMatch+窗口量级。字面量先攒进运行缓冲，只在遇到回指、Flush、Close 或达到
固定容量上限时落盘，绝不因 Write 边界而截断。由于每个决定都只依赖字节流本身，
同一输入任意切法的输出逐字节相同。推论：`Flush` 必须把「末尾待定」强行按终态
处理（把已有数据当作流尾），否则已写出的字节不足以还原截至 Flush 的输入。

## 推导 2：距离 < 长度的回指如何复制（语义 2）

块复制（memmove 语义）先把源区快照再写目标；dist<len 时源区包含尚未写出的
字节，快照读到的是旧内容，结果错误。逐字节向前复制在第 k 步读 `total-dist`
处的字节，k≥dist 时读到的正是本次刚写出的字节，与 LZ77 的自引用语义一致。
代价方面：解压每个输出字节本就需要 Ω(1) 工作，逐字节循环总代价 Θ(输出长度)，
不存在渐进退化；环形窗口取字节是 O(1)，故实现直接采用逐字节循环，不做块复制。

## 推导 3：「距离超过已输出」与「距离超过窗口」为何是两个错误（语义 7）

窗口容量是解码器的静态结构限制（环形缓冲大小），已输出字节数是流的动态历史。
同一压缩流在容量不同的解码器下合法性不同，两类违例原因与排查方式都不同，必须
可区分。压缩器一侧：匹配器只在窗口内找候选（候选位置 ≥ total-cap），故发出的
距离恒 ≤ 窗口容量，绝不可能产生「距离超过窗口」。并行块压缩用上一块末尾作预置
字典时，编码器的局部 total = 字典长 + 本块已发，而解码器的已输出 = 全局已输出 ≥
局部 total，故「距离 ≤ 已输出」不变式保持；字典只是预填窗口，不改变「已输出」
的含义。

## 推导 4：并行块压缩的确定性（语义 9）

块 i 的压缩输入只有两块原文：`data[i*bs:(i+1)*bs]` 与字典
`data[max(0,i*bs-cap):i*bs]`（上一块末尾窗口容量以内的字节）。二者都是输入的
纯函数，不依赖任何其他块的压缩结果、goroutine 调度或完成顺序，故 workers 取
任意值输出逐字节相同。衔接上：记录自定界，块 i-1 的记录解完后，解码器的窗口、
已输出计数、校验和恰好等于块 i 编码器预置字典时的假设，直接拼接即可，块间
不需要刷新标记（刷新标记只对流式消费者有意义，对解码状态无影响）。流头由
协调者写一次，流尾的总长与校验和由协调者对整段原文顺序计算。
