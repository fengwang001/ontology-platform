# 严格模式流式 Base64 设计

## 非规范尾部的推导（语义 2）

三个字节按大端拆成 4 个 6-bit 值；输入不足 3 字节时，最后一个 6-bit 值由
「有效载荷比特 + 补零比特」拼成，编码端的补零比特必须全部为 0。

- 1 字节输入（`xx==`）：只有 8 个载荷比特，即 6 + 2；第 2 个字母值
  `v1` 的低 4 位全部是补零，故必须满足 `v1 & 0b001111 == 0`。
  `Q`=16，`QQ` 给出 0x41（'A'），补零位为 0 → 合法；
  `R`=17，`QR` 的数据位同样是 0x41，但 `v1` 低 4 位 = 1 ≠ 0 → 拒绝。
- 2 字节输入（`xxx=`）：有 16 个载荷比特，即 6 + 6 + 4；第 3 个字母值
  `v2` 的低 2 位是补零，故必须满足 `v2 & 0b11 == 0`。
  `QUI`：I=8，低 2 位 0 → 合法；`QUJ`：J=9，低 2 位 1 → 拒绝。

结论：严格模式要求「解码值等于其唯一规范编码再解码的值」。若接受非零补零
位（如 `QR==`），两个不同输入 `QQ==` 与 `QR==` 会解码成同一字节串，
破坏「任意字节串的编码结果唯一 / 编码解码互逆」。

填充长度也由此唯一确定：剩 1 字节必有两个 `=`，剩 2 字节必有一个 `=`，
无剩余则无填充；`=` 只能出现在整个流最末尾。无填充的截断流一律拒绝。

## 错误五分类（带输入字节偏移）

1. 非法字符（不在字母表且不是 `=`/换行）
2. 非规范尾部（补零位非 0）
3. 填充位置错误（`=` 个数或位置非法、填充后仍有数据）
4. 长度不是 4 的倍数（Close 时缓冲非空）
5. 换行位置错误（组内换行、非 MIME 换行、孤立 `\r`、流尾悬挂换行）

## 复杂度

解码器逐字节状态机：每个输入字节只检查一次，非导出计数器 `checked`
随字节递增；任意切分方式（含 `\r| \n`、`=|=`）行为完全一致。

## 七条语义的测试对照表

<!-- TABLE -->
