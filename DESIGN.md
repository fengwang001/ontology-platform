# 分片扇出查询的部分失败合并器 — 设计推导

## 1. 模型

- 查询扇出到 n 个分片，分片 i 贡献记录集合 R_i；记录有 ID、Score（float64）。
- 分片可静态给出自己可能贡献的最高分上界 B_i（ScoreBound）。
- 记成功分片集合 S、失败（缺失）集合 M。缺失分片可能贡献 0 条或任意 ≤B_i 分的记录。
- 五种聚合：Count、Sum（对 Score 求和，Score 非负）、Min、Max（对 Score）、TopK。
- 分片状态枚举：OK / Timeout / Corrupt / DuplicateReject / Canceled。

## 2. 五种聚合在部分失败下能说什么

设缺失分片对聚合量的未知贡献 X ≥ 0（Score 非负；缺失分片至少贡献 0）。

### Count

真实 Count = C_S + X，X ≥ 0 ⇒ 真实值 ≥ C_S。
成功部分是**下界**：标注「≥ C_S」，区间 [C_S, +∞)。全部成功才是精确。

### Sum

同理真实 Sum = V_S + X，X ≥ 0（缺失分片只有非负分记录）。
成功部分是**下界**：「≥ V_S」，区间 [V_S, +∞)。

### Min

缺失分片可能含有比当前最小更小的值，且最小不随"加入更多记录"而增大。
真实 Min = min(m_S, m_M) ≤ m_S；若 M 全部为空，真实 Min 才等于 m_S。
故只能说「真实 Min ≤ m_S」（**上界**方向）。区间 (-∞, m_S]。
全部失败或成功分片全空时 Min 不存在（区别于 0）。

### Max

缺失分片可能含更大的值，真实 Max = max(M_S, M_M) ≥ M_S。
只能说「真实 Max ≥ M_S」（**下界**方向）。区间 [M_S, +∞)。
方向易写反：Min 是"只会更小或相等"（≤），Max 是"只会更大或相等"（≥）。

### TopK 与可信前缀

合并 TopK = 成功分片记录按 (Score 降序, ID 升序) 排序取前 K。
缺失分片能塞进榜单的最高分：U = Σ_{i∈M} B_i（各缺失分片上界之和；任一分片上界未知则 U=+∞）。

论证：要把当前第 j 名（得分 s_j）挤出 TopK，缺失分片至少要贡献 j 条得分
**严格大于 s_j** 的记录（并列按 ID 升序打破，ID 更大者可能输掉并列，故只算严格更大）。
缺失分片总分最多 U，而 j 条严格大于 s_j 的记录总分 > j·s_j。
因此当 **j·s_j > U** 时，前 j 条中每一条都确定入选 TopK
（逐条论证：对任意 t≤j，t·s_t ≥ j·s_j > U，故前 t 名也确定入选）；
但它们的**排名**仍可能被缺失高分记录改变，所以只能保证"入选"这一前 j 名集合。

可信前缀长度 p = 最大的 j（1≤j≤min(K,len)）使 j·s_j > U；不存在则 p=0。
全部成功时 U=0 且无未知，TopK 整体精确（p=K，标注精确而非"可信前缀"）。
特例：s_j=0 时 j·s_j=0 不可能 > U≥0，故零分条目不进可信前缀，符合直觉。

## 3. 全部失败 / 空结果 / 零分片

- 零分片：无扇出目标，返回可判定错误 ErrNoShards。
- 所有分片都失败：无任何成功数据，返回 ErrAllShardsFailed（errors.Is 可判），
  不返回零值伪装精确；报告仍携带每分片失败状态供排障。
- 所有分片成功但各自为空：成功数据存在，Count/Sum=0，Min/Max=不存在，
  TopK 为空；全部标注精确。与"全部失败"通过是否存在成功分片区分。

## 4. 扇出与资源约束

- 并发上限 C：容量 C 的信号量；非导出原子计数记历史峰值 inFlightMax。
- 整体截止：context.WithTimeout；截止后不再获取信号量发起新请求，
  未启动的分片标 Canceled；在飞请求通过 ctx 取消，分片看到 ctx.Done 后返回。
- Wait 在所有 goroutine 结束后返回（截止后一个很短窗口内必然返回）。
- 故障区分（errors.Is/状态枚举）：超时 ErrShardTimeout；损坏 ErrCorrupt；
  同一分片重复返回：第二次响应与首次不同 → 判 Corrupt，相同 → 丢弃并标
  DuplicateReject（视为幂等重试），合并绝不重复计数。
- 损坏判据：响应声称条数 n 与实际记录数不符（含"部分字段"：缺 ID/Score 字段的
  记录计入不符）；空 ID 合法，不判损坏。

## 5. 确定性

每分片结果先落本地槽位，Wait 后按分片 ID（空串合法，排序自然落位）排序再合并；
TopK 并列按条目 ID 升序。故报告（值、缺失清单、状态）与到达顺序逐字节无关。

## 6. 包结构（≤24 个 .go 文件，每文件 ≤200 行）

shard（类型/接口/错误 + 假分片 + 测试）、fanout（扇出 + 测试）、
combine（聚合 + 测试）、confidence（标注 + 测试）、report（报告 + 确定性测试）、
cmd/demo（≤20 行自检输出，退出码 0）。
