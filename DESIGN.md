# 设计推导

## 代理对错误偏移（第 2 条）

规则：高代理 `\uD8xx–\uDBxx` 必须紧随一个低代理 `\uDCxx–\uDFFx`，二者共同构成一个
码点；任何不完整都把错误归给**高代理的起始反斜杠**。

- 孤立高代理 `\uD83D` 缺低代理（结尾引号/EOF/普通字符均算）：报在高代理 `\` 处。
  理由：高代理自身发出了“我要消费下一个单元”的承诺，承诺未兑现，责任在承诺方。
- 孤立低代理（出现在开头，或紧跟普通字符之后）：报在该低代理的 `\` 处——
  它前面没有高代理，自身就是错误单元。
- 顺序颠倒 `\uDE00\uD83D`：第一个单元是孤立低代理，报在其自身 `\`。
- `\uD83DA`：高代理之后是普通字符 `A` 而非 `\uXXXX`，承诺失败，报在高代理 `\`；
  不能读作“替换字符 + A”，因为解码器不做静默替换（与第 3 条一致），
  且 `A` 处于高代理的待选低代理位置，语义上不允许滑过去重解释。

偏移锚定在承诺起点，使流式任意切分下错误偏移恒定（只依赖已承诺序列，
不依赖缓冲边界）。

## 最小转义与唯一性（第 4 条）

RFC 8259 要求转义的只有引号、反斜杠与 U+0000–U+001F。U+007F 虽是 DEL，
但落在 0x1F 之外，字符串内原样 UTF-8 合法，故不转义；U+001F 属强制范围必须转义。
`/` 仅在可选转义列表中、非 ASCII 与 U+2028/2029 在 JSON 字符串中合法，均原样输出。

单射与唯一性：编码是**逐码点**的无状态映射，每个码点的输出唯一
（8 个短形式或 `\u00XX` 或自身 UTF-8，三者互斥），映射按码点划分互不重叠，
因此两个不同字符串的输出必不同，且同一字符串只有唯一输出；
解码按字面序列确定地还原码点，故最小转义字面量往返后逐字节不变。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 五类错误可区分且带偏移 | `TestStrictErrors` |
| 2 代理对组合与拒绝样例 | `TestSurrogates` |
| 3 非法 UTF-8（解码/编码两方向） | `TestInvalidUTF8` |
| 4 最小转义与原样输出 | `TestMinimalEscape` |
| 5 编解码往返（含随机） | `TestRoundTrip` |
| 6 流式任意切分一致 | `TestStreamSplits` |
| 计数器 ≤ 2N | `TestByteCounter` |
