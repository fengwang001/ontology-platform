# DESIGN：流式行尾/空白规范化

模型：按行处理，每步 = 输出内容字节（含行中空白）→ 丢弃行尾空白 → 把行尾（`\r\n`/`\r`/`\n`）变为 `\n`；
最后按末尾策略收尾。非法 UTF-8 与普通字节等价（按字节），仅严格模式拦 NUL。

## 1. 空输入/纯换行 与 末尾策略

先得到「保留策略」结果 y（不增删末尾换行），再：
Ensure 若 y 以换行结尾则去掉全部末尾换行、补一个，否则补一个；Trim 若非空则同 Ensure（空仍为空）。
表：

| 输入 | Preserve | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

Ensure 对空有两个幂等不动点可选：`""→""` 或 `""→"\n"`。选后者：
Ensure 恒以恰好一个换行结尾（含空输入，与多数文本工具「文件以换行结尾」一致），且 N(N(x))=N(x) 显然。
Trim 保持空为空；其余输入三策略输出均无连续末尾换行，故幂等。

## 2. 被删字节的 ToOut

不变量要求 ToOut 单调不减且对输出偏移 o 有 ToOut(ToOrig(o))=o。
映射分三类 piece：R 保留[oi,oi+n)↔[oo,oo+n)、D 删除[oi,oi+n) 恒指输出边界 oo、I 插入（仅 Ensure 末尾 1 字节）。
被删运行内所有 i 取「运行之后第一个输出位置」oo：`\r\n` 的 `\r` → 输出 `\n` 的起点（ToOrig=`\n`）；
行尾空白 → 行尾 `\n` 起点之前。编辑器把光标放在被删空白里即停在行尾换行之前；放在 `\r` 上停在 `\n`（而非跳过整行）。
注意数学事实：净插入（Ensure 对空/无换行文补 `\n`）时 |orig|<|out|，鸽子洞使「每个输出偏移（含终点）互逆」不可能；
约定插入段两端 ToOrig 均为 orig 终点、ToOut(orig终点)=插入段起点，于是 o<len(out) 全部互逆，仅输出终点例外，测试显式钉住。

## 3. 空白缓冲超限

强制判定为「非行尾」会在行尾空白长度>上限时输出本应删除的空白，违反语义 2 与幂等（语义 5），不可取。
选择拒绝：在第 MaxPending+1 个待定字节处返回 `ErrPendingLimit`（OffsetError，带原文偏移），
已输出内容与已记录映射保留，进入终态（此后 Write 报 `ErrClosed`）。错误由上限决定、与切分无关，语义 4/5/6 不受影响。
输出超限同理在写入导致越界的字节处返回 `ErrOutputLimit`。

## 4. par 拼接

每段以「片段模式」跑 Preserve：段内事件照常，但段尾把待定 `\r`（carryCR）与待定空白（carryWS）留在片段状态，
并标记首个字节是否已直接输出（headKept：首个字节是保留内容或空文本 `\n`；首字节为待定 `\r`/空白时为 false）。
拼接时只看片段首字节与上一段 carry，修正量 ≤ MaxPending+1：
carryCR + 首字节 `\n` → 该 CR 为删除片，`\n` 保留（`\r|\n`）；否则输出 `\n` 再处理首字节。
carryWS：headKept 或首字节为 `\r` 时 carryWS 判为行中、输出；否则（首字节 `\n` 或 EOF）判行尾、删除。
随后把片段 pieces 坐标（orig += 段起点，out += 当前输出长度）平移并入全局 Builder。
修正范围有上界：carry 只是一台 ws/eol 机器的状态，长度 ≤ MaxPending+1，与段数、段长无关。
全部片段拼完后在全局结果上统一应用 Ensure/Trim（仅末尾），与单流一致；错误（NUL/上限）原样上浮并带全局偏移。
