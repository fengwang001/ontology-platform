# 采样剖析与热点归因设计

## 数据模型

- 调用栈是从根到叶的帧序列；规范化只删除相邻同名帧，保留递归中的非相邻同名帧。
- 树节点保存 `Self`、`Total`、截断标记、父子边。根是合成节点，不参与热点和求和。
- 一条采样沿树下钻：路径上每个现存节点只做一次 map 查找；缺边则创建并写入 map。
- 叶子的 `Self += 1`；根到叶所有真实节点的 `Total += 1`。

## Self 与 Total

节点的 self 表示采样叶正好停在该节点的次数；total 表示采样经过或停在该节点的次数。

设总采样数为 `N`。每条采样恰有一个真实叶，所以：

`sum(node.self) == N`

但一条深度为 `d` 的采样会给路径上 `d` 个节点的 total 各加一次，所以：

`sum(node.total) == sum(sample.depth)`

当存在深度大于 1 的栈时，total 之和大于 N；祖先被后代采样重复计入，不能把 total 当 self。

## 递归帧的函数级归因

节点按完整调用路径区分，因此 `A→F→G→F→H` 中有两个 F 节点。

函数级 self 可汇总所有同名节点的 self，因为 self 只属于唯一叶子节点，不重叠。

函数 F 的 total 不能相加所有 F 节点：内层 F 子树本身已包含在外层 F 子树中，相加会重复计算。
只统计调用栈上“不被另一个 F 祖先包含”的最外层 F；同一函数的这些最外层子树互不相交。

遍历时维护“当前路径是否已见该函数”：第一次见到 F 时把该节点 total 计入函数 total，进入其后代后再遇到 F 则跳过。
排除某函数后的归因同理：一旦进入被排除函数的最外层节点，整棵子树都不计入。

## 深度截断

- `maxDepth <= 0` 表示不限制。
- 栈深等于上限：完整插入，不截断。
- 栈深超过上限：保留前 `maxDepth` 帧，末节点设置截断标记，并增加截断样本计数。
- 空栈拒绝插入，增加无效样本计数，不产生节点。

截断样本仍按保留路径贡献 self/total，因此 self 恒等式不被破坏；截断计数额外说明叶节点可能不是原始叶。

## 并发与复杂度

采样协程通过互斥锁串行修改整棵树；查询先在锁内复制不可变节点记录，再在锁外排序。
查询不会看到“子节点 total 已加而父节点未加”的中间状态。停止采样通过关闭循环标志实现，重复停止无副作用。

插入一条深度 `d` 的栈最多执行 `d` 次查找和 `d` 次插入，比较计数上界为 `4*d`；复杂度为 `O(d)`，与既有节点数无关。
归因是一次节点快照扫描加一次排序，树重建次数恒为 0。

## 采样器

时钟、采样次数和栈来源均可注入。每次时钟步进一个固定间隔；若时间未推进或发生回拨，跳过该 tick 并增加异常间隔数。
忙窗口返回 dropped=true 时增加丢样数；非丢样且取栈成功才交给树。停止是幂等操作。

因此应有采样 tick 数满足：

`有效样本 + 丢样 + 异常间隔 == 应采样次数`

有效样本再继续分成正常样本、截断样本和空栈等无效样本；树的 self 之和恒等针对成功插入的样本。

## 落盘格式

文件使用小端二进制和 IEEE CRC-32：

1. 16 字节头：魔数 `PROFv1\x00\x00`、版本 1、头 CRC、总采样、截断、无效、maxDepth。
2. 每条记录依次写入深度、截断标记、帧名长度和帧名。
3. 文件末尾 4 字节为全部节点记录的 CRC。

节点按深度优先、且父先于子写出。读回先验头 CRC，再按完整 4 字节 CRC 校验记录区。

截断分类：

- 短于 16 字节：头部不完整。
- 头完整但头 CRC 错误：头部 CRC 不匹配。
- 无法在记录区切出完整记录：节点记录不完整。
- 记录完整但末尾 CRC 缺失或不符：CRC 不匹配。

恢复时只接受能从根连续解析出的父先子后记录前缀；不会恢复父缺失的子节点。
