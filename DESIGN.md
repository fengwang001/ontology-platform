# CSV 解析器设计

方言：逗号分隔；记录以 `\n` 或 `\r\n` 结束；引号字段内 `""` 表示一个 `"`，其中的逗号、`\n`、`\r\n` 原样保留。单实例非并发安全；状态全在进程内存。

## 推导一：空行与单列空值

- 空行（两个换行紧挨着）**被跳过**，不产生记录。依据：若空行产生「一个未引号空字段的记录」，则单列空表（0 条记录）与「含一条空记录的单列表」都会写成空字节串，往返丢数据，违反不变量 5；跳过空行后二者不再混淆。
- 由此推出回写规则：单列表中某条记录的唯一字段为空时**必须**写成 `""`（加引号）。若不引，写出来是空行，解析时被跳过，记录丢失。故 writer 规则：字段为空、或含 `,""\r\n` 任一字符时加引号，其余不引。
- 引号标记往返：未引空字段回写仍不引（在单列场景与「跳过空行」规则配合仍往返）；引号空字段回写仍引。

## 推导二：par 的切点问题

- 切点可能落在引号字段内，worker 无法仅凭本段字节判定起点状态。方案：每段并行跑**两遍**——假设起点在引号内（Q）与假设起点在引号外（统一为「未引号字段中 U」）。起点其实是字段开始/引号后/CR 待定时，用「前缀增广」归约：在该段前虚拟拼接 1 字节（U 假设拼 `x`，Q 假设拼 `"`），事件偏移减 1 即还原；起点为字段开始（FS）时，U 遍的事件流与 FS 完全一致（FS 与 U 对首个字节的动作相同），仅当首字节是分隔符/换行导致字段边界差一个空字段时，拼接端按「起点是否恰在记录开始」丢弃或补一个空单元；起点为 QC/CR 时，把该段第一个字节并入上一段（上一段多解析 1 字节，O(1)），本段从第二字节起必然处于 FS/U/Q 之一。
- 拼接：从左到右，第 0 段起点状态已知（FS）；取该段对应假设的「结束状态」作为下一段起点状态，再选下一段对应假设的事件流接上。每段两遍的结束状态必然不同（引号奇偶性相反），故选择唯一。字段被切点劈开时，拼接端把相邻两段产生的首尾单元碎片合并为一个（值拼接、引号标记取左段、偏移取左段起点与右段终点）。
- 坐标换算：每段记录其全局基址 base 与全局起始记录号 recBase；段内事件偏移加 base、记录号加 recBase 即全局坐标。错误同理：段内错误（字节偏移、记录号、字段号）换算为全局后与单线程一致，且左段先错先报。
- 复杂度：每字节恰被处理 2 次（两遍假设），拼接只做 O(1) 归并与事件搬运，故总计 ≤ 2N + O(K)。

## 推导三：CR 待定状态

- 未引号字段末尾见 `\r` 进入 CR 态：下一字节是 `\n` 则按 `\r\n` 结束记录；是别的字节则报「孤立 CR」错误（错误位置指向该 `\r`）。
- 半包续传：`\r` 到达时只记状态不出错，等下一次 `Feed` 的第一个字节再判定；`Close()` 时若停在 CR 态，按「孤立 CR」报错（流末的 `\r` 不是合法行尾）。
- par 切点：切点落在 `\r` 与 `\n` 之间时，`\n` 所在段的第一个字节并入上一段（见推导二），CR 态的判定永远在持有 `\r` 的那段内完成，不跨段。
- 引号字段内的 `\r`（含 `\r\n`）一律是内容，原样保留。

## 推导四：上限的「立刻」

- 单字段上限按**解码后**字节数计：引号字段内 `""` 只算 1 个 `"`；每向当前字段追加一个字节就先检查 `len+1 > max`，超限当场返回 `ErrFieldTooLarge`，不缓冲整条记录。
- 单记录字段数上限：字段结束事件使字段数超过 max 时当场返回 `ErrTooManyFields`。
- 总记录数上限：第 max+1 条记录的第一个字节到达时当场返回 `ErrTooManyRecords`，已产出的完整记录保留。
- 三类上限触发后解析器进入终态，后续 `Feed` 返回同一个错误。

## 状态转移表（状态 × 字节类别 → 动作 / 下一状态 / 错误）

- FS（字段开始）：逗号=出空字段；引号=进 Q；换行=出空字段并结束记录；回车=进 CR；其他=入字段进 U；EOF=脏则出字段并结束记录。
- U（未引号字段中）：逗号=出字段进 FS；引号=ErrBareQuote；换行=出字段并结束记录；回车=进 CR；其他=追加；EOF=出字段并结束记录。
- Q（引号字段中）：逗号/换行/回车/其他=追加；引号=进 QC；EOF=ErrUnclosedQuote。
- QC（引号字段中刚见引号）：逗号=出字段进 FS；引号=追加一个引号回 Q；换行=出字段并结束记录；回车/其他=ErrTrailingGarbage；EOF=出字段并结束记录。
- CR（行尾回车待定）：换行=出字段并结束记录进 FS；其余字节与 EOF=ErrLoneCR（位置指向该回车）。

字段开始为「脏」（本记录已有字段或内容）时，换行与 EOF 规则同上；为「净」（记录尚无内容）时空行被跳过。引号字段内字节均计入字段上限。
