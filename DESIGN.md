# DESIGN: 计数草图与频繁项检测器的四条推导

## 1. 能不能支持 Sub(x, n)（减计数）？

结论：不能，实现不提供 Sub。

推导：普通更新下，每个格子的值恰好等于「所有哈希到该格的键的真实计数之和」。
若减量恰等于 x 的当前真实计数，数学上格子仍是精确和，不变量 1 似乎能保住。
问题在于：调用方拿不到真实计数，只能拿到 Estimate（>= 真实值）。一旦减量超过
真实计数，格子就被压到某些碰撞键的真实计数之下。

具体操作序列（w=1, d=1，所有键必碰撞）：

1. Add(a,1)：格子=1，true(a)=1。
2. Add(b,1)：格子=2，true(b)=1。
3. Estimate(a) 返回 2（碰撞导致高估，合法）。
4. 调用方想撤销 a，只能拿估计值当减量：Sub(a,2)：格子=0。
5. Estimate(b) 返回 0 < true(b)=1，不变量 1 被破坏。

即使限制 n <= Estimate(x) 也挡不住（上例 n=2 恰等于 Estimate(a)）。草图内部无法
区分「真实计数」与「碰撞噪声」，任何它能自己算出的减量都可能超限。结论：不提供
Sub。调用方想「撤销」时的替代：(a) 保留插入日志并重建草图（不变量 3 的确定性
保证重建结果逐格相同）；(b) 按时间窗维护多个草图，窗口内 Merge、整窗丢弃——
丢弃是整体放弃一个草图，而不是对活草图做减法。

## 2. 保守更新是否保持「绝不低估」？

结论：保持不变量 1，但破坏不变量 2，故不采用（注意与第 1 条答案不同）。

不变量 1 的归纳证明：设更新前 d 个格子都 >= true(x)（归纳基：全 0 >= 0）。取
m=min(格子)，则 m >= true_old(x)。保守更新把 < m+n 的格子提到 m+n >=
true_old(x)+n = true_new(x)；未动的格子本就 >= m+n >= true_new(x)。更新后所有
格子仍 >= true_new(x)，估计值 = min(格子) >= true_new(x)。证毕。

但它破坏合并同构。反例（d=2, w>=3；键 x,y 满足 h0(x)=h0(y)=0, h1(x)=1, h1(y)=2）：

- 合并喂入 [x,y]：加 x 后 r0[0]=1, r1[1]=1；加 y 时 m=min(r0[0],r1[2])=min(1,0)=0，
  只把 <1 的格子提到 1：r1[2]=1，r0[0] 不动。结果 r0[0]=1。
- 分开喂再 Merge：A=[x] 得 r0[0]=1；B=[y] 得 r0[0]=1；逐格相加得 r0[0]=2。
- 1 != 2：保守更新顺序相关，无法由两个独立草图的格子逐格还原。

因此实现只用普通更新（d 个格子各加 n），不提供保守更新开关。

## 3. (eps,delta) 与 (w,d) 的换算

采用 Count-Min 标准式：w=ceil(e/eps)，d=ceil(ln(1/delta))。

常数来历：单行误差的期望 <= N/w（误差是其他键落入同格的计数和，由 Markov 不等式，
P(单行误差 >= eps*N) <= 1/(w*eps)）；取 w=e/eps 使该概率 <= 1/e；d 行独立时全部
失败的概率 <= e^{-d}，令其 <= delta 得 d=ln(1/delta)。e 是此放缩下的最优底数。

反推（调用方直接给 (w,d)）：eps=e/w，delta=e^{-d}。表述时必须称为「名义」参数：
delta 的概率语义建立在哈希族独立随机的假设上；本实现的哈希族由参数确定导出，
对固定输入不存在真正的概率空间，所以 delta 只是设计目标，不得宣称「对任意输入
以概率 1-delta 成立」。

## 4. HeavyHitters 的阈值跟谁比

用 Estimate(x) >= T 判定。推导：不变量 1 给出 Estimate(x) >= true(x)，故
true(x) >= T 蕴含 Estimate(x) >= T，真实超阈值的键必然入选，零漏报；多报的键
也满足 true(x) >= T-eps*N，多报幅度有界。

若改用 Estimate(x)-eps*N >= T：一个 true(x) >= T 但 Estimate(x) < T+eps*N 的键
会被丢掉，即漏报，直接违反不变量 4。该变体错在「漏」的一侧，只能作为「确定超
阈值」的加强子集（topk.Certain）额外提供，不能作为 HeavyHitters 的判定条件。
