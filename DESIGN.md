# 带转义的 RLE 编解码器：设计说明

## 严格解码的拒绝清单推导

不变量：严格解码器接受的每个 t 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。
下面每一类，若被接受，给出的 t 都会违反该不变量，故必须拒绝（错误带字节偏移且彼此可区分）。

- 显式次数 1：`Decode("1a")` 必为 `"a"`，而 `Encode("a") == "a" != "1a"`（次数 1 按规定省略）。
- 次数 0：`Decode("0a")` 若为空串则 `Encode("") == ""`，若为 `"a"` 则 `Encode == "a"`，任何解释都不等于 `"0a"`。
- 前导零次数：`Decode("02a") == "aa"`，而 `Encode("aa") == "2a" != "02a"`（规定无前导零）。
- 相邻同符号游程：`Decode("2a3a") == "aaaaa"`，而 `Encode("aaaaa") == "5a" != "2a3a"`（Encode 只产生最长游程）。
- `\` 后非数字非反斜杠：只有数字与 `\` 需要转义；若 `Decode("\q") == "q"`，则 `Encode("q") == "q" != "\q"`。
- 末尾孤立 `\`：`\` 只能以 `\数字` 或 `\\` 的形式成对出现，任何合法串的 Encode 都不会以孤立 `\` 结尾。
- 末尾只有次数没有符号：次数必须修饰符号；若 `Decode("2a3")` 忽略尾部 `3` 得 `"aa"`，则 `Encode("aa") == "2a" != "2a3"`。
- 非法 UTF-8：Encode 只输出合法 rune，非法字节序列不可能出现在任何 `Encode(s)` 中。

## 大次数的取舍

选择**支持**任意大次数：`runs.ParseCount` 用 `math/big.Int` 解析十进制，无整数溢出。
解码器在输出前用 big.Int 计算该游程的字节数（次数 × 符号宽度）并与剩余额度比较，
绝不按次数预分配内存；输出总字节上限可配置（`NewDecoder` 的 `maxOut`，`Decode` 用
`DefaultMaxOut = 1<<30`），超限返回可用 `errors.Is` 判定的 `ErrTooLarge`。

## 语义与测试对照

（待测试写定后补齐）
