# 可撤销批量重命名：设计推导

## 1. 模型
命名空间是内存中的名字集合（`name.Namespace`，`map[string]struct{}` 包一把 `sync.RWMutex`）。
名字合法性：除 `\x00` 外的任意字符串都合法——空串合法；`/`、`\` 等路径分隔符按普通字符处理。
单步原子操作只有两种：`Add(x)`（目标不存在）、`Remove(x)`（源存在）。重命名一步 = Remove 旧名 + Add 新名。

## 2. 顺序规则（拓扑序推导）
把每个请求 `o->n` 视作有向边。请求 P 必须先于请求 Q，当且仅当 P.old == Q.new：
即"新名是另一个尚未执行请求的旧名"时，必须等那个请求先搬走，否则 Add(n) 会撞上仍在的 n，构成覆盖。
对依赖图（边 o->n，邻接含义"n 被腾出后 o 才能落位"）做 Kahn 拓扑排序，入度为 0 的先执行。
`{a->b,b->c}`：边 a->b、b->c，b 入度 0 先执行 `b->c`，再 `a->b`，全程无覆盖。
确定性：同层候选按旧名字典序排序。

## 3. 环的通用破解（每环一个临时名）
环上任意排序都覆盖（每个新名都仍被占）。取环中字典序最小节点 s（环 o0->o1->...->o(k-1)->o0）：
`o0 -> t`（先把 o0 挪到临时名 t，腾出 o0），随后 `o1 -> o0, o2 -> o1, ..., o(k-1)->o(k-2), t -> o(k-1)`。
每环恰好一个临时名（只破一个缺口）。临时名生成：候选串 `.rename-tmp-<old>-<n>`，
生成时在"现有名 ∪ 全部新名 ∪ 已用临时名"集合中校验，冲突则递增后缀重试；
重试有上界，耗尽则返回可判定错误 `cycle.ErrTempExhausted`（绝不覆盖数据）。
自环 `a->a`：定义为**无操作**（不报错、不产生步骤、不占临时名），因为源与目标相同，集合不发生任何变化。

## 4. 四类冲突（全部在任何修改之前检出）
1. `ErrMissingSource`：old 不在现有命名空间（指出该名）。自环豁免：a->a 时 a 必须存在。
2. `ErrDupSource`：同一 old 出现两次（报出两个 new）。
3. `ErrDupTarget`：两个不同请求指向同一 new（报出两个 old）。自环不参与目标占用。
4. `ErrTargetExists`：new 已存在，且 new 不是本批某个被搬走的 old（自环除外）。
检测顺序：dupSource -> dupTarget -> missingSource -> targetExists；四类均用包装错误支持 `errors.Is`。
冲突检出时命名空间零修改（只读 RLock）。

## 5. 复杂度
冲突检测与排序全部基于 map 查找：记请求数 R、涉及名字数 N（old∪new∪existing 规模）。
`plan` 内非导出计数器统计每次 map 访问，预算 `4*(R+N)`：建索引 O(R)、校验 O(R)、
建图 O(R)、拓扑 O(R+N)，均线性。5 万请求测试断言不超预算。
破环对每环常数次查找，临时名数由步骤类型精确计数，10 个不相交环恰为 10。

## 6. 执行、撤销与并发
`apply.Executor` 执行前对命名空间加全局写锁并持有到整批结束（含回滚），
期间任何外部并发修改在 Lock 上阻塞（文档语义：阻塞而非拒绝）。
每步前快照断言"目标名不存在、源存在"，再 Remove/Add；可注入 `failAfter(k)` 让第 k 步失败，
失败即按已执行步骤逆序回滚，命名空间逐元素还原。
日志写本地临时目录，顺序追加每条已完成步骤（可据此逆序撤销）。
撤销幂等：已处理日志标记/删除，第二次撤销为无操作。

## 7. 日志格式与三类截断错误
文件：魔数头 `RNLOG1\n`（7 字节）；每条记录 `BE32(len) + payload + BE32(crc)`，
payload = `BE32(olen)+old+BE32(nlen)+new`，crc 为 IEEE CRC-32 覆盖 len 与 payload。
解析分类（`errors.Is` 可分）：
- `ErrHeaderIncomplete`：前 7 字节不全；
- `ErrRecordIncomplete`：某条记录声明的长度/CRC 尾部读不全；
- `ErrCRCMismatch`：记录完整但 CRC 不符（仅靠逐字节截断不能产生——截断使尾部缺失归为 record-incomplete；
CRC 错误用翻转末 4 字节中的字节构造，截断循环另用翻转补出该类）。
撤销按"最大可恢复前缀"执行：头部/记录不完整或 CRC 不符处停止，返回前缀之外无法撤销的步骤数。
