# 设计：只追加日志的段索引与范围回放器

## 文件格式

- 段文件 `seg-NNNNNN.osl`：自描述头 + 逐事件记录。
  - 头（26B）：magic(4) + version(2) + firstSeq(8) + count(8) + headerCRC32(4，覆盖前 22B)。
  - 记录：`len(4, 事件体字节数)` + 事件体(`seq(8)` + payload) + CRC32(4，覆盖 len+事件体)。
- 索引文件 `seg-NNNNNN.osi`：magic(4) + every(4) + 锚点数(4) + 锚点序列`(seq(8), offset(8))`。
  每 every=N 条事件记一个锚点（段内第 0, N, 2N… 条），offset 为该记录相对段文件起始的字节偏移。
- 序号全局连续：段 i 的 `firstSeq + count == 段 i+1 的 firstSeq`，由写入器轮转时保证，repair 可校验。

## 定位规则推导：为什么是「不大于 from 的最大锚点」

段是只追加的，记录只能**向前**顺序扫描。设锚点集合 A 按序号递增。

- 若取「最接近 from 的锚点」a：当 `a.seq > from` 时，a 的字节偏移位于 from 所指记录**之后**，
  从 a 向前扫永远经过不到区间 `[from, a.seq)` 内的事件 → 漏事件，结果错误。
- 若取「不大于 from 的最大锚点」a*：`a*.seq <= from`，从 a* 偏移处向前扫，途中跳过
  `[a*.seq, from)` 的事件后必经过 from，不重不漏。
- 跳过量上界：相邻锚点序号差恰为 N，故 `from - a*.seq <= N-1`，即定位阶段跳过事件数**严格小于 N**。
  这给出可测试的复杂度断言（10 万事件、N=128、随机 from，skipped < 128）。
- 推论：回放读取字节数 ≤ 区间事件编码长度之和 + 一个锚点区间（最多 N-1 条被跳过的事件）。

## 索引可丢弃、可从段逐字节重建的理由

索引是纯加速结构，其全部内容必须能由段文件推导：

- 锚点 seq：来自事件体本身；offset：扫描段时记录位置的累积，二者都在段里。
- every=N：是调用方配置参数，重建时作为参数传入，不属于「只在写入时可知」的信息。
- 段头 firstSeq/count 也在段文件头里，不依赖索引。

因此索引中**不允许**写入任何段里没有的信息（如写入时间戳、内存状态）。
只要扫描逻辑确定、编码确定，重建出的索引与原索引逐字节相同。测试直接断言字节相等。

## 索引失效检测与回退策略

定位时对选中的锚点做**就地校验**：在该 offset 解码一条记录，要求长度前缀合法、CRC 通过、
且解出的事件序号等于锚点序号。任一不满足（如索引被篡改、指向事件中间）：

1. 判定索引失效，在报告中标注 `IndexInvalid=true`；
2. 自动回退为从段头之后全段顺序扫描，结果仍然正确（索引只是加速，不影响正确性）。

索引文件缺失时同样回退全段扫描（不标注失效，记 `IndexUsed=false`）。

## 损坏分类与修复

截断分类（可判定错误，`errors.Is` 可区分）：

- 剩余字节 < 26 → `ErrHeaderIncomplete`
- 记录起始处剩余 < 4 → `ErrLengthPrefixIncomplete`（含恰好落在记录边界、0 字节可读）
- 长度前缀完整但事件体不足 → `ErrBodyIncomplete`
- CRC 字段不足 4 字节或校验值不符 → `ErrCRCMismatch`

`repair` 扫描出最大可恢复前缀（连续合法记录 `[0,k)`），把文件截断到该前缀末尾，
并把段头 count 改正为 k；随后重建该段索引。序号连续性检查：
相邻段 `firstSeq+count != nextFirstSeq` 时报 `ErrSeqGap` 并给出缺口区间。

## 并发一致性

写者协议：先完整写入记录并落盘，再以单次写更新段头（头带 CRC）。
读者：读头时校验 headerCRC，不符则短暂重试；之后只读 count 条记录。
由于第 1..count 条记录在 count 更新前已完整落盘，读者只能看到某个一致前缀，
永远读不到半条事件。`-race` 下无数据竞争。

## 边界语义（文档定义，测试锁定）

- 空日志：回放返回空结果，不报错。
- `from > to`：返回 `ErrInvalidRange`。
- `from` 小于最小序号：**钳位到从头开始**，不报错（定义如此）。
- `to` 超过最大序号：回放到末尾，不报错。
- 空载荷：合法，正常编解码与回放。
