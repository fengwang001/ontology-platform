# Quoted-Printable 设计说明

## 行末空白（语义 3-a）

空格/TAB 在行中可原样输出，但解码端无法区分“行内空白”与“行尾空白”：
硬换行前的尾随空白会被传输层剥离。因此编码时必须向前看一个字节：
空格/TAB 若后随 `\n`（含 `\r\n`）或位于输入结尾，输出 `=20`/`=09`；
其余情况原样输出。单独的 `\r` 不是换行符，故 `\r` 前的空白不算行末空白。

## `=XX` 不被拆开（语义 3-b）

每个输入字节对应一个不可再分的 token：字面 1 字符，或 `=XX` 3 字符。
软换行 `=\r\n` 的 `=` 占 1 字符，故一行内容（不含 `\r\n`）上限为 76。
当 token 不是本行最后一个 token 时，其后必须还能容纳软换行 `=`，
即 `col+w <= 75`；是本行最后一个 token（下一个字节是换行或输入结束）时
允许 `col+w <= 76`。放不下就在 token 前输出 `=\r\n`，计数器不回退、不重扫。

## 软换行恰好插在空格之后

该空格**不算**行末空白，原样输出，不写 `=20`。理由：解码器删除的是
软换行的 `=\r\n` 三个字节，其前面的空格被保留并与下行拼接，
语义上它处于行的内部；只有真正的硬换行（或输入结束）前的空白才需要转义。

## 往返等式（语义 6）

定义 N(x)：把 x 中的 `\n` 与 `\r\n` 统一为 `\r\n`（单独 `\r` 保留）。
则对任意字节串 x：`Decode(Encode(x)) == N(x)`。编码保证：
硬换行只在输入换行处出现；行尾空白一律转义；每一行不超过 76 字符，
故其输出被严格解码器接受。

## 最小转义与幂等（语义 7）

编码器只转义三类：字节 `=`、非可打印字节（含单独 `\r`）、
硬换行/EOF 前的空白；其余一律字面。因此合法解码结果 y 再编码时，
逐字节不同只可能来自 y 中对可打印字符的多余 `=XX`（解码后按规则回到字面）。
软换行位置由同一贪心规则唯一确定（75/76 阈值如上），不产生差异。

## 语义—测试对照表

（测试写完后补齐）
