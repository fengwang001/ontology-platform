# DESIGN：带替换的流式 UTF-8 ⇄ UTF-16 转码

## 1. 非法单元切分规则（WHATWG「最大子串」的字节表述）

从首字节 L 起：按 L 声称的长度 N（C2-DF→2，E0-EF→3，F0-F4→4）读后续字节，
每读到一个 80-BF 继续字节就吞掉；一旦遇到「非继续字节」或流结束即停止：

- 若 L∈{C0,C1,F5..FF}：L 不可能开头，单元长度 1。
- 否则单元长度 = 1 + 已吃到的继续字节数（EOF 时不足 N 也全部吞掉）。
- 每个这样的单元恰好产生一个 U+FFFD；下一字节重新当首字节。

解释样例：`F0 90 80 41` 里 41 非继续，单元={F0,90,80}→1 个 FFFD，41 重解析；
`E0 80 80`：E0 后第二字节 80 不在其特殊区间，单元={E0}，80、80 各自是游离继续字节→3 个；
`ED A0 80` 同理 ED 不允许 A0→1+1+1；`C0 AF` 中 C0 长度 1、AF 游离→2；
`F4 90..`：F4 第二字节仅允许 80-8F，故 4 个字节各自成单元；
`E2 82`+EOF：一起被截断为 1 个非法单元（不是 2 个）；`80 80`：两个游离→2 个。

## 2. 首字节 → 第二字节合法区间与非法时吞掉的字节

| 首字节 | 第二字节区间 | 不合法时本单元吞掉 |
|---|---|---|
| C2..DF | 80..BF | 首字节 1 个 |
| E0 | A0..BF | E0 1 个（后续按首字节重解析） |
| E1..EC | 80..BF | E 字节 1 个 |
| ED | 80..9F | ED 1 个（防代理码点） |
| EE..EF | 80..BF | E 字节 1 个 |
| F0 | 90..BF | F0 1 个 |
| F1..F3 | 80..BF | F 字节 1 个 |
| F4 | 80..8F | F4 1 个 |
| 80..BF | （游离继续字节） | 1 个 |
| C0 C1 F5..FF | 不存在 | 1 个 |

第二字节合法后，第三/四字节只要求 80-BF（非最短与越界已被上表排除）。

## 3. 流式缓存：缓存几个字节、何时立刻放弃

最长序列 4 字节，最坏情形（F0 后流被逐字节切）一次最多暂存 **3** 个字节，硬上限即 3。
收到首字节后，只要下一字节**不是**继续字节，立即判该前缀为非法单元，不等 EOF。
暂存字节在被后续字节「确认非法并吐出」前不算已消费；只有已完整判定（合法标量或
非法单元吞掉）的字节计入 Write 返回值。EOF 时残留暂存字节被一次吞为 1 个非法单元（截断）。

## 4. par 的切点对齐

切点 b 可能落在任何位置。令 s=b：
1) 若 byte[s-1] 是继续字节 80-BF，则 s 不断回退越过连续的继续字节；
2) 回退后若 byte[s-1] 是首字节 L（C2-F4），且 b 到 s 的继续字节数 < L 的序列长度-1，
   再退一格把 L 也并入右段；否则 L 留在左段（其序列在左段内已完整或已被非法字节终止）。
段 i 解析区间 [s_{i-1}, s_i)，段间不重叠不遗漏；每段只在区间内「可立刻定案」时输出，
跨区间尾巴留给邻段。C3|80 80：切点前是 80，回退越过它，前一字节 41 非首字节，
故 80 留左段作游离字节、右段 C3… 独立解析，FFFD 计数与单线程完全相同。
仅回看 ≤3 字节，检查次数 ≤ 输入字节数 + 8×常数。只有最后一段按真 EOF 处理截断。

## 5. 第 9 条断点

输出在标量边界记账：先尝试为「下一个完整标量」预留输出空间，放不下就返回 ErrLimit，
该标量一个字节都不写、其输入字节不算已消费（Write 返回值不含它，也不含暂存字节）。
调用方用返回的 n 续传新实例（新实例不再发 BOM），拼接与无限额逐字节相同。
