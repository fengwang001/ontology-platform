# DESIGN

## 第 3 条：续行接上纯空白行的推导

- 依据 `java.util.Properties` 的 `LineReader.readLine`：行尾奇数个反斜杠触发
  续行时，吃掉一个反斜杠并置 `skipWhiteSpace=true`、`appendedLineBegin=true`。
- 下一物理行的前导空白被跳过；若跳过后立即遇到行尾（`appendedLineBegin`
  仍为真），该 EOL 按普通行尾处理：已累积内容非空且无待续反斜杠，逻辑行
  就此结束。
- 结论：`k=v\` + `   `（纯空白行）+ `x=y` → `k`→`v`、`x`→`y`。纯空白续行
  使续行链终止，而不是被当作空行跳过；其后的物理行开启新的逻辑行。
- 与「续行首部空白被剥掉」的关系：剥离先发生；剥完后该段为空、末尾不存在
  奇数个反斜杠，续行条件不再成立，逻辑行闭合。两条规则是同一流程的前后
  两步，并不矛盾。
- 边界：若累积内容仍为空（如输入仅 `\` + 空行），Java 的 `len==0` 分支把
  它整体丢弃；本实现同样不产出空逻辑行。

## 第 6 条：回写转义规则的推导

- 键：解析时键在遇到**未转义**的 `=`、`:`、空格、`\t`、`\f` 处结束，故这些
  字符在键内任何位置都必须转义；`\` 会改变后继字符含义，必须转义；`\n`、
  `\r` 会截断物理行，必须转义。注释判定只看逻辑行第一个非空白字符，故仅
  键首的 `#`、`!` 需要转义，键中间的不需要。
- 值：值永远不在行首（前面至少有分隔符 `=`），故 `#`、`!` 不必转义；分隔符
  之后的内容全部归值，`=`、`:` 也不必转义。
- 值的空格：加载只跳过「分隔符与值首之间」的空白且不修剪尾部，故值中间与
  尾部的空格原样保留、不必转义；值首的空格落在被跳过的区间，必须转义。
  只需转义第一个空格：反斜杠本身即终止空白跳过，其后的字面空格受到保护。
- 非 ASCII 对解析完全惰性，按 UTF-8 原样输出，不写成 `\uXXXX`。

## 测试对照表

（待实现完成后补齐）
