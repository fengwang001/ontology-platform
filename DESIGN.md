# 带溢出的哈希聚合器设计

## 1. 溢出内容：选择溢出「部分聚合状态」

每行入表时先在内存里做局部聚合（key → {Count,Sum,Min,Max}）。
驻留分组数达到预算时，选定一个分区，把该分区**当前内存中的聚合状态**
（每个 key 一条部分状态）写到磁盘片段，随后从内存表删除这些 key。

**为什么溢出状态而不是原始行**：
1. 同分区会被反复选中溢出（最多 O(输入/预算) 次）。溢原始行时，磁盘 IO
   与回读处理量随行数线性累积；溢部分状态时，每次写盘只写「当时驻留的
   去重后状态」，同 key 在内存内已先合并，写/读量通常更小。
2. 正确性的关键是部分状态**可合并**且合并**满足结合律**（见下）。

## 2. 同一分区多次溢出为何仍正确

对分区 p 的最终状态 =
merge(片段₁, 片段₂, …, 片段ₖ, 内存剩余状态ₚ)。
每次溢出只是把「已看到的那部分行的聚合结果」固化成一个操作数；
merge 是结合的，因此片段划分次数、回读完成先后都不改变最终值。
漏掉任一片段都会少算其中所有行，故回读必须合并该分区**全部**片段。

并行只以分区为单位（不同 key 的分区号不同，互不相交），状态不共享；
合并按固定顺序（片段序号 → 片段内 key 序 → 内存剩余 map 的 key 序），
因此调度顺序不影响输出行序与 Sum 位模式。

## 3. 四个聚合的合并规则（结合性论证）

| 聚合 | 部分状态 | 合并规则 | 结合性 |
|---|---|---|---|
| Count | 行数 n | n₁+n₂ | 整数加法，结合 |
| Sum | float64 s | s₁+s₂ | 实数/±Inf 上结合；位精确性见 §5 |
| Min | float64，含存在标志 | min(a,b) | min 结合且幂等，与 Count 不同 |
| Max | float64，含存在标志 | max(a,b) | max 结合且幂等 |

Min/Max 不能像 Count/Sum 那样「相加」：空状态没有中性值会污染结果
（若误用 +Inf/-Inf 会与真实 ±Inf 混淆），故用独立「存在」标志，
合并时一侧不存在直接取另一侧。±0.0 视为相等：归一化 -0 → +0，
min/max 与 map 查找（Go string key 不含值）均不受影响。

## 4. 文件格式（自描述头 + 长度前缀 + CRC32）

```
头 9 字节:  "OHPF" | version=1(uint8) | partition(uint32 BE) | seq(uint32 BE)
记录:  len(uint32 BE) | body(len 字节, row.Encode: keylen|key|float64 BE ×4)
       | crc32-IEEE(BE, 覆盖 len 前缀 + body)
```
片段原子落盘：`p<part>-<seq>.tmp` 写完 fsync 后 rename 为
`p<part>-<seq>.spill`；启动时清理目录内所有 `*.tmp`。
截断分类（n=文件长度，L=首条记录总长）：
n<9 头部不完整；9≤n<13 长度前缀不完整；13≤n<9+L 行体不完整；
n≥9+L 且 CRC 不符 → CRC 不匹配（首条记录起，逐记录推进）。

## 5. 数值语义

NaN 不可比较、不可聚合：入表前拒绝并计入 Skipped。±Inf 合法，参与
Sum/Min/Max（Inf 算术精确）。Sum 为 float64：本实现保证
「合并顺序确定」，在数值可精确表示时（如整数 |v|≤2^53）与全内存
逐行聚合逐位相同；verify 用此域构造数据并用 math.Float64bits 比对。

## 6. 资源约束

分区数构造时固定，不随分组数增长；插入新 key 前若驻留数==预算则先
溢出，故驻留峰值 ≤ 预算（预算边界恰好不溢出）。每行入表处理一次、
从片段回读一次，总处理次数 ≤ 2×输入行数（NaN 行只计数不处理）。
四类错误用哨兵错误并以 errors.Is 区分：ErrCorrupt / ErrHeader /
ErrTruncated（前缀或行体，附偏移）/ ErrWriteFail（注入）。
