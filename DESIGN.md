# DESIGN：流式 UTF-8/UTF-16 转码（替换 + 严格）

## 1. UTF-8 非法单元切分规则（推导）

逐字节运行一个"带头的状态机"。读到首字节 L 时，按其类别要求第 2 字节落在区间内；任何续字节一旦不满足要求：
**只把失败字节 b 退回输入重新解析，此前已缓存的该次尝试字节合并成一个非法单元（恰好一个 U+FFFD）**；
首字节非法（`C0/C1/F5..FF`）或孤立续字节（`80..BF`）本身为一个 1 字节单元；EOF 时残留的合法前缀整体为一个截断单元。

- `F0 90 80 41`：F0 要求 90..BF（90 合法），第 3 字节要求 80..BF，41 失败 → 退回 41，`F0 90 80` 合一单元 → `FFFD 41`。
- `E0 80 80`：E0 要求 A0..BF，80 失败退回；E0 单独一单元，之后 80、80 各为孤立续字节 → 三个。
- `ED A0 80`：ED 要求 80..9F，A0 失败退回；ED 一单元，A0/80 各一单元 → 三个（也是代理区 U+D800）。
- `C0 AF`：C0 非法首字节一单元；AF 孤立一单元 → 两个（非最短形式）。
- `F4 90 80 80`：F4 要求 80..8F，90 失败退回；F4 一单元，余下三个续字节各一 → 四个（越界 >U+10FFFF）。
- `E2 82`+EOF：E2 区间合法、82 也合法，仍是"未完成的合法前缀"，整体一个截断单元 → 一个 FFFD（不是两个）。
- `80 80`：两个孤立续字节 → 两个。

首字节 → 第二字节合法区间 / 失败时吞掉的字节数：

| 首字节 | 第2字节区间 | 非法时吞下 |
|---|---|---|
| C2..DF | 80..BF | 失败字节退回；已缓存前缀 1 单元 |
| E0 | A0..BF | 同上 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F | 同上（排除代理） |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF | 同上 |
| F1..F3 | 80..BF | 同上 |
| F4 | 80..8F | 同上 |
| 80..BF | （无首字节资格） | 1（孤立续字节） |
| C0 C1 F5..FF | （永非法） | 1 |

第 3/4 字节一律要求 80..BF，失败同样只退回失败字节。

## 2. 切分缓存上限与提前判定

UTF-8 一个字符最多 4 字节，尝试中最多保留首字节+2 个续字节=3 字节，故缓存硬上限 3（UTF-16 同理：1 个半字节 + 1 个高代理 = 2 字节）。
看到落入"不允许区间"的字节即可立刻判前缀非法并把该字节退回（重解析为新单元），无需等 EOF；只有所有已见字节都在合法区间但长度不足时，才必须继续等待（因此合法字符跨切分点绝不提前出 FFFD）。每个输入字节至多被"检查两次"：一次作为某次尝试的字节，失败退回后再作为新首字节一次，故检查数 ≤ 2N。

## 3. par 切点对齐

各段独立转码；非末段不 Close（合法前缀残留不算截断），仅末段 Close。切点回退对齐：UTF-8 从切点向前找首字节（最多 3 字节），仅当"首字节+切点前续字节"构成仍在合法区间内的未完成前缀时，本段起点前移到该首字节（该前缀不属任何完整字符）；否则从切点后的第一个字节开始，切点前的续字节/失败前缀都已计入上段。
例：`C3 | 80 80`：C3+80 是未完成合法前缀（C3 需 1 续字节，已见 0 个）→ 本段从 C3 开始，C3 80 成一字符、最后 80 为非法；与单线程一致，且 C3 不被输出两次。
`E0 80 | 80`：E0+80 不是合法前缀（E0 要求 ≥A0）→ 本段从切点后 80 开始；上段已产出 FFFD(E0)+FFFD(80)，本段再 FFFD(80)，共 3 个，与单线程相同。回退 ≤3 字节 ⇒ 检查数 ≤ N + 8×3 量级。各段消费从自身名义切点起计，字节守恒仍成立。

## 4. 上限断点

输出在标量边界写入；写入下一完整标量会越界即停，返回 ErrOutputLimit。
`Write` 的 n = 已"定稿"的输入字节数（已对应输出标量）；仍在 3 字节尝试缓存里的前缀未定稿、不计入 n，调用方从 `data[n:]` 重喂给新实例即可逐字节拼接。出错实例 `Resume()` 亦可返回全部未定稿字节。UTF-16 代理对整对输出，绝不只出高代理。
