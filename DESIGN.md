# 设计推导

## 1. 续行后只含空白

结论：若上一物理行以奇数个反斜杠结尾，下一物理行会无条件作为续行消费；即使该续行只含空白或为空，也不会另算空行或注释。

推导：续行判断只看上一逻辑行末尾反斜杠数量的奇偶。进入续行后，规则是“丢弃反斜杠加行终止符，并跳过续行首部空白”。跳过首部空白使用的是前缀消费：空格、制表、换页都属于空白；若整行都被该前缀匹配，剥离结果就是空串。由于该行没有新的行尾反斜杠，逻辑行在此结束。

因此 `k=a\`、换行、只含空白的下一物理行得到 `k=a`；再下一物理行已是新逻辑行。它与“首部空白被剥掉”是同一条规则：空白续行没有任何“至少保留一个空白”的特例，只是首个非空白字符不存在。

## 2. Store 的最小转义

Load 先识别逻辑行，再在逻辑行内解析。Store 不能拆断逻辑行，所以键和值中的 `\`、`\n`、`\r`、`\t`、`\f` 都必须转义；其中换行写成两字符 `\n` 或 `\r`，不能写成真实行尾反斜杠，否则会变成续行标记而被丢弃。

键位于分隔符之前：未转义的空格、制表、换页会结束键，`=` 和 `:` 会成为分隔符，所以这些字符在键中均需转义。逻辑行首字符若为 `#` 或 `!` 会被识别成注释，因此键首是二者之一时必须转义；位于键中则无注释含义，不必转义。

值位于分隔符之后：Load 只跳过值的首部空白，一旦读到第一个值字符，后续空格、`=`、`:`、`#`、`!` 都只是普通字符。因此只需转义值的首个空白；值中间或末尾空格、以 `#`/`!` 开头均不必转义。非 ASCII 原样按 UTF-8 写出即可。

## 3. 测试对照表

待测试函数确定后回填。
