# 严格模式流式 Base64：规范尾部推导

## 统一规则推导

标准字母表每个字符携带 6 比特。3 字节（24 比特）恰好拼成 4 个字符；
余数不足时用 `=` 补足 4 字符，但填充字符不携带原始数据，其位置上的 6 比特是“凑出来”的。

- 尾部 1 字节：形态 `XX==`。该字节 8 比特放入前两个字符，即 c0 的全部 6 比特与 c1 的高 2 比特。
  因此 **c1 的低 4 比特必须全为 0**。
  `QQ==`: c0=16,c1=0，合法；`QR==`: Q=16, R=17=010001，低 4 位=0001≠0，拒绝。
- 尾部 2 字节：形态 `XXX=`。16 比特放入前三个字符，用满 c0、c1 与 c2 的高 4 比特。
  因此 **c2 的低 2 比特必须全为 0**。`QUI=` 的 I=8=001000，低 2 位=0，合法；
  `QUJ=` 的 J=9=001001，低 2 位=01，拒绝。

统一规则：**填充字符对应的全部比特必须为 0**（`XX==` 查 c1 低 4 位；`XXX=` 查 c2 低 2 位）。
填充数只能是 0（`XXXX`）、1（`XXX=`）或 2（`XX==`），其余填充形态非法。

与唯一性的关系：若允许这些“残差比特”非 0，多组不同编码（如 `QQ==` 与 `QR==`）
会解码成同一字节串，解码不再是编码的逆映射。强制残差为 0 后每个字节串的编码唯一，
于是 `Encode(Decode(y)) == y` 对所有被严格模式接受的 y 成立。

## 语义与测试对照表

| 语义（第二节） | 钉住它的测试函数 |
| --- | --- |
| 1 标准字母表与 `=` 填充、76 列 MIME | （收尾填写） |
| 2 规范尾部（非 0 残差拒绝） | （收尾填写） |
| 3 换行位置与非法空白 | （收尾填写） |
| 4 五类错误可区分且带字节偏移 | （收尾填写） |
| 5 任意切分点结果一致 | （收尾填写） |
| 6 往返（含余数与 MIME 两档） | （收尾填写） |
| 7 输出上限在组边界停并进入终态 | （收尾填写） |
| 三、检查计数 == 输入字节数 | （收尾填写） |
