# 设计推导：多租户加权公平队列调度器

## 1. 为什么是 `vt += c / 权重` 而不是 `c * 权重`

目标：长期看租户 i 获得的服务量 S_i 与权重 w_i 成正比，即 S_i / S_j = w_i / w_j。

调度规则是「虚拟时间最小者优先」，稳态下所有活跃租户的 vt 被拉平到近似相等
（谁落后谁被选中，追平为止）。设租户 i 累计获得服务量 S_i 后 vt_i = f(S_i, w_i)，
拉平条件 vt_i ≈ vt_j 要求 f 关于 S 与 w 的商不变：

- 取 `vt += c / w`：vt_i = S_i / w_i。拉平得 S_i / w_i = S_j / w_j，即 S_i / S_j = w_i / w_j，
  正是目标比例。权重大的租户 vt 涨得慢，被选中更频繁，正确。
- 若取 `vt += c * w`：vt_i = S_i * w_i，拉平得 S_i * w_i = S_j * w_j，即 S_i / S_j = w_j / w_i，
  权重越大服务越少，公平性完全倒置。

直觉：`c / w` 把「真实代价」折算成「该租户以权重 w 独占一台速率正比于 w 的虚拟机时
消耗的时间」，权重是速率不是放大器，所以做除数。

## 2. 空闲租户重新加入时的 vt 抬升

系统维护系统虚拟时间 V = 最近一次被选中任务开始执行时其租户的 vt（随调度单调不减）。
租户 X 空闲期间，其他租户持续执行，V 从 V0 涨到 V1。若 X 的 vt 停在旧值 vt_X = V0，
重新入队后它落后所有活跃租户 L = V1 - V0。每次 X 被选中 vt 只前进 c/w_X，
在追平之前堆顶永远是 X：X 将连续独占约 `L * w_X / c` 步。本题测试中 Y 在 X 空闲期
执行 1000 个代价 1、权重 1 的任务，L ≈ 1000，X 若带 1500 个任务回归将连续独占
约 1000 步——这就是「不抬升会独占多少步」的定量结论：独占步数 ≈ 空闲期间系统
虚拟时间的推进量 ÷ 自身每步虚拟时间增量。

正确做法：租户从空转为非空（重新进入堆）时执行 `vt = max(vt, V)`。抬升后 X 与
活跃租户站在同一起跑线，偏差至多一个任务粒度，立即恢复按权重交错。
注意 V 取「任务开始时的 vt」而非「完成后的 vt」，保证 V 单调且不超过任何活跃
租户的当前 vt，抬升不会把新来租户压到队尾饿死。

## 3. 代价为 0 的任务

零代价合法，但若 vt 增量为 0，该租户 vt 不动，堆顶永远是它，无限独占。
处理：设最小代价下界 MinCost = 1.0，推进时取 `max(cost, MinCost)`。
零代价任务按 MinCost 计费，vt 每步至少前进 1/w，独占被限制在常数步内。
下界取 1.0 而非极小值：若下界远小于典型代价，独占步数（典型代价/下界）仍会失控。

## 4. 同虚拟时间的并列打破

vt 相等时按租户 ID 字典序取小者。堆的 Less 先比 vt 再比 ID，得到全序，
同一输入流下调度序列完全可复现（配合第 5 节，并发下以入队锁的串行化顺序为提交顺序）。

## 5. 其他决策

- 数值类型：vt、代价、权重均用 float64。权重极大（1e6）与极小（1e-6）并存时
  单步增量最大 1e6 量级，10 万次调度累计约 1e11，远低于 float64 上限（约 1.8e308），
  不会溢出；有理数（big.Rat）无必要且慢。
- 选择复杂度：活跃租户放二叉堆，单次选择比较次数约 2*log2(n)（弹出下沉每层
  两次比较 + 回插上浮常数次），实测上界取 4*ceil(log2(n))。空闲租户不在堆中，
  不占选择代价。
- 背压：每租户队列上限 limit，超限返回 ErrQueueFull，只影响该租户。
- 错误分类（errors.Is 可区分）：ErrQueueFull（超限）、ErrInvalidWeight（权重 <= 0）、
  ErrInvalidCost（代价 < 0）、ErrTenantBusy（有排队任务时移除租户）。
- 租户移除：有排队任务时拒绝（ErrTenantBusy）；队列为空时可安全移除，无任务去向问题。
- 并发：Admitter 用一把互斥锁串行化 Submit/Next；并发提交的「顺序」定义为
  获取该锁的串行化顺序，因此确定性只对固定的串行提交序列承诺。
