# Quoted-Printable 设计说明

## 关键推导

1. 可打印字节定义为十进制 33–126，但 `=` 是转义引导符，所以只有 33–126 且不等于 `=` 的字节可原样输出。
2. 空格和制表符本身可原样输出；判定“行末空白”不能只看物理输出位置，而要看解码后的逻辑行边界：下一个输入事件是硬换行（`\n` 或 `\r\n`）或输入结束。
3. 因而 `a \n`、`a \r\n` 与结尾的 `a ` 中的空格必须写成 `=20`；制表符同理写成 `=09`。而 `a b` 中空白后还有内容，必须保持原样。
4. 每个输入字节先得到长度为 1 或 3 的原子 token：普通字节长度 1，转义序列 `=XX` 长度 3。软换行只能插在两个原子之间。
5. 若当前列号加 token 长度大于 76，先输出 `=\r\n`，再输出 token。因此第 74 列后遇到 `=3D` 时，74+3>76，会先软换行，绝不会把 `=3D` 拆开。
6. 若软换行恰好插在一个空格之后，该空格仍写成字面空格，不写成 `=20`。原因是解码器只删除软换行序列 `=\r\n`，删除后空格与其后的内容重新连接，仍是逻辑行中间的空白；只有硬换行或输入结束前的空白才是行末空白。

## 往返等式

设 `N(x)` 把 x 中的每个 `\n` 及 `\r\n` 规范成 `\r\n`，单独的 `\r` 保持为普通字节，则：

`Decode(Encode(x)) == N(x)`

所以任意 x 中原本单独的 `\n`，往返后会变成 `\r\n`；单独的 `\r` 被保留为字节 CR。

编码器是最小转义：每个字节只在三类情况下转义：不是允许的字面字节、是 `=`、或位于逻辑硬行尾/输入结束的空白。除此之外均输出字面形式；软换行只在 token 无法放入剩余列宽时插入，不额外转义任何可打印字符。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 转义与换行规范 | 待补 |
| 2. 76 字符行长 | 待补 |
| 3. 行末空白与转义原子 | 待补 |
| 4. 严格解码错误与偏移 | 待补 |
| 5. 跨切分点一致 | 待补 |
| 6. 往返 | 待补 |
| 7. 幂等与最小转义 | 待补 |
