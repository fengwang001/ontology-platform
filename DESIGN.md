# 设计推导

严格解码的唯一不变量是：接受 `t` 当且仅当 `Encode(Decode(t)) == t`。

- 显式次数 `1`：`1a` 解码为 `a`，但 `Encode("a")=="a"`，故拒绝。
- 次数 `0`：`0a` 不产生符号；编码器不会输出零长游程，故拒绝。
- 前导零：`01a` 解码为 `a`，规范编码是 `a`；`001a` 同理，故拒绝。
- 相邻同符号：`2a3a` 解码为五个 `a`，最长游程编码为 `5a`，故拒绝。
- 非法转义：`\x` 不是“数字或反斜杠”的符号转义，不能由编码产生，故拒绝。
- 末尾孤立 `\`：没有被转义的符号，不是完整游程，故拒绝。
- 只有次数：`12` 末尾没有符号，不是完整游程，故拒绝。
- 非法 UTF-8：解码目标是合法 Go 字符串；非法序列不可能是任何合法输入的编码，故拒绝。

大次数选择“设上限并拒绝”。上限是 `math.MaxInt`：Go 的切片长度与索引受 `int` 约束，更大次数即便解析成功也无法在不依赖外部存储的情况下物化为字符串。数字逐位累加，仅当 `n > (MaxInt-d)/10` 或加数位后超限时报错，杜绝溢出；报错发生在输出前，不会按次数预分配内存。解码器另有总输出字节上限，超限返回可判定的 `ErrLimit`。

流式解码器在看到下一个游程的符号前只暂存上一游程；因此 `2a` 后紧跟 `3a` 要到第二个符号处才报“相邻同符号”。UTF-8 只保存状态和不完整尾字节，任意切分都得到同一全局偏移和错误。

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 往返 | `TestRoundTrip` |
| 规范形式与严格解码 | `TestStrictRejects` |
| 大次数与输出上限 | `TestHugeCountAndLimit` |
| 最长游程与组合字符 | `TestLongestRunsAndCombining` |
| 跨切分点一致 | `TestStreamingSplits` |
| 输入检查计数 | `TestExaminedCount` |
