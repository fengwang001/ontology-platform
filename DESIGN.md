# 设计推导

## 记录布局（均为无符号 LEB128；tag 本身是一字节 varint）
| 记录 | tag | 后续字段 |
| --- | --- | --- |
| 流头 | 1 | 版本=1(varint), 窗口容量(varint) |
| 字面量 | 2 | 长度 n, 随后 n 个原始字节 |
| 回指 | 3 | 距离 dist, 长度 length（dist>=1, length>=1） |
| 刷新 | 4 | 无（仅边界语义，本实现只在显式 Flush 产生） |
| 流尾 | 5 | 原文总长, FNV-1a 64 位校验和 |

魔数为 ASCII `LZ77`，写在流头 tag 之前。varint 解码：需要更多字节时返回“待续”，第 10 字节或 64 位溢出立即报错，偏移指向坏字节。

## 推导一：为什么输出与 Write 切法无关
压缩器永远不把 `Write` 当成边界。内部保留一个按累计偏移拼接的输入缓冲，并维持 `lag=1` 个未决字节：位置 i 是否能继续延长匹配，取决于位置 i+1 的字节，该字节可能在下一次 Write 才到达。因此每次写入只处理到 `len-1`：能确定结束的匹配/字面量才落流，最后一个字节继续等待。整段一次写、1 字节写或 7 字节写，喂给匹配器的逻辑字节序列与决策时刻完全相同，故输出逐字节相同。`Flush` 是唯一强制收口点：它要求“已输出足以还原全部全部已提交输入”，所以必须把 lag 字节按字面量强制落流并写刷新标记；无新输入时状态已干净，第二次 Flush 自然零字节。

## 推导二：距离 < 长度时的复制
块复制 `copy(out, src)` 依赖重叠内存的传播行为，而回指要求从“逻辑源”反复取值。dist=3、len=30 时正确结果是源 3 字节周期重复。我采用显式逐字节 `out=append(out, out[len(out)-dist])`。这里逐字节可接受：总工作量就是输出字节数，且为有界窗口内顺序访问；不引入额外缓冲，并保证炸弹回指在写第一个字节前就完成总量校验。

## 推导三：两类“距离越界”为何分开
解压器同时维护 emitted（已输出字节数）与固定窗口容量 W。dist>emitted 指向从未存在的数据，即使流不完整也必坏；dist<=emitted 但 dist>W 指向已被承诺丢弃的历史，违反协商窗口。二者可独立发生、修复含义不同，必须分成两个哨兵错误并报告字节偏移。压缩器只在容量 W 的环形窗口内取候选，故合法编码绝不可能产生 dist>W。并行块预置字典只改变每块编码时窗口的初始内容（上一块末 W 字节）；它仍是本块回指“已经存在的源”。进入标准流后解压侧 emitted 为全流累计字节，校验规则不变。

## 推导四：并行块压缩的确定性与衔接
块 k 的匹配候选只来自“本块原文 + 上一块末 W 字节字典”，哈希链是该纯输入的确定性函数；不读取其他块的压缩结果，goroutine 完成顺序不影响输出，最终按块序拼接，所以 workers=1/2/4/8 逐字节相同。块间不写刷新标记：记录流顺序自界定，刷新只表达调用方的 Flush 边界。流只写一个头与一个尾；每块回指长度上限取块大小，保证不越过本块，而其字典在解压时正是先前块已输出的字节。
