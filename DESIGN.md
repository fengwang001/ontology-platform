# 规范化设计推导

## 末尾换行

三种策略只在普通规范化完成后决定最终 `\n` 数：
- Keep：保留原文末尾换行数；幂等因输入已规范且策略不再改变它。
- One：非空输出必以恰一个 `\n` 结束，空输入仍为 `""`。若空输入产生 `\n`，再次规范会得到非空文本，不能保持空；选 `""` 后三次均为 `""`。
- Trim：删除全部末尾空行；非空内容保留一个终止 `\n`，空内容为 `""`。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `""` |

## 被删字节映射

每个输出偏移都必须有一个原文锚点，且 `ToOut(ToOrig(o))=o`。普通字节锚定自身；`\r\n` 合成的 `\n` 锚定 `\n`，所以 `\r` 的 `ToOut` 取后一个 `\n` 的位置。被删尾随空白位于行内容与换行之间，映射到行尾换行之前，即该空白串之后第一个保留输出偏移；EOF 处删除则映射到输出终点。该规则让 `ToOut` 单调不减，编辑器光标落在被删空白时停在行尾换行前，而不是跳到下一行。`ToOrig(len)` 返回原文终点，支持闭合区间终点。

## 缓冲超限

选择拒绝。强制把超长尾随空白改成行中空白会依赖未来字节，且若随后出现换行，已输出内容无法撤回，违反跨切分一致、幂等和映射正确性。拒绝前保留已确认输出，并返回带原文起始偏移的 `ErrPendingWhitespaceLimit`；对象进入终态，后续写入返回 `ErrClosed`，状态可解释。输出上限同样在写入前检查，报导致超限的原文偏移。

## 并行拼接

每段独立流式处理，切点只可能有两种未决状态：一个待配 `\n` 的 `\r`，或一串未决尾随空白。并行时先把逻辑切点移到最近的干净点：非空白、非 `\r` 且不紧跟 `\n` 的字节之后；移动最多受尾随空白上限加两个字节约束。干净点保证段尾无 `\r`、无尾随空白、无末尾策略影响。各段本地 span 加上全局原/输出偏移；删除只会造成间隙，拼接后仍保持单调。最后仅对完整输入应用一次末尾策略，因此 K、切点与并发时序不改变字节和映射。
