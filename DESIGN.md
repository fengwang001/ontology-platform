# 快照读可见性判定器 设计

## 一、核心推导：为什么只看提交时刻不够

目标谓词是「该版本在取快照那一刻已经提交」。朴素规则用
`commit(T) < watermark(S)` 作代理，但它只是必要条件而非充分条件：水位
与活跃集在快照创建瞬间一起采样，提交号却可能由另一条序列分配（如开始
时预占、提交日志乱序落盘），故快照创建时仍在进行中的事务，其提交号完
全可能小于水位。反例构造：T1 在快照 S 创建时仍在 active(S) 中；S 的水
位已推进到 10（其他事务已提交到 10）；T1 随后提交，拿到的提交号恰好
是 5。朴素规则：`5 < 10` ⇒ 可见——读者看到取快照时尚未提交的版本。

正确规则（两条缺一不可）：
    visible(T, S)  <=>  commit(T) < watermark(S)  且  T ∉ active(S)
提交号检查过滤掉快照之后的提交；活跃集检查过滤掉「提交号虽小于水位、
但取快照时尚未提交」的事务，合取才等价于「取快照时已提交」。唯一例外：
快照属主事务（owner）自己写的版本对自己恒可见。

## 二、复杂度

单次判定 = 1 次事务表查找 + 至多 1 次活跃集查找 = 常数（≤2），不遍历
事务表；每次判定持有独立的非导出计数器，并发互不串台。

## 三、结构与错误

- `txn`：事务号、提交状态表（只读无锁）；`snapshot`：水位 + 属主 +
  活跃集（上限 MaxActive）+ 释放标记；`visible`：判定。
- 版本链提交号严格递增（最旧在前），否则损坏。可判定错误：
  `txn.ErrUnknown`、`snapshot.ErrReleased`、`visible.ErrCorrupt`。
