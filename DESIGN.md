# 并行任务图调度与失败传播 — 设计

## 1. 模型
- DAG：节点=任务 ID（字符串）；边 `A->B` 表示 B 依赖 A。加边幂等（重复边去重）；引用未注册节点立即报错。
- 执行前校验：空图合法；自环与任意环都用 Kahn 剥离法检出，并从残留入度>0 的节点沿后继回溯，报一条首尾闭合、逐边真实存在的环路径。
- 任务函数 `func(ctx) error`。panic 由 goroutine 内 `recover()` 捕获，转成带 panic 标记的失败错误。

## 2. 四类终态及判定（核心推导）
调度器只允许一次状态迁移：`待执行 -> 运行中 -> 终态`。

| 状态 | 判定依据 | 是否执行过函数体 | 原因 |
|---|---|---|---|
| Success | 函数返回 nil（发生在任意取消信号落定之前） | 是 | — |
| Failed | 函数返回 error，或 panic 被 recover | 是 | 原始错误/panic 信息 |
| Skipped | 派发前发现：某（传递）依赖已 Failed，或快速失败模式已触发终止 | 否 | 指向**最早失败根因** |
| Canceled | 已被派发运行；取消信号落定后才结束，且结束时返回 nil | 是 | 触发取消的失败 |

区分 Skipped 与 Canceled 的唯一依据是「函数是否已经开始执行」：派发（goroutine 已启动）后一律不可能再变 Skipped。
因此慢旁支任务在旁支失败后被 ctx 取消：若它结束时返回 nil -> Canceled；若返回 err -> Failed（它自己也是真实失败）。
取消后迟到的任何写回（结果/nil）到达时状态已是终态，直接丢弃，不覆盖。

### 根因（跳过原因）指向推导
跳过原因必须指向"最初失败任务"而非直接上游。规则：
对每个 Skipped 节点，取其**失败祖先集合中 ID 字典序最小者**（确定性破并）；快速失败模式下，
与失败无祖先关系、仅因全局终止而未启动的独立节点，原因同样指向该触发失败（首个 Failed，ID 最小破并）。
故 `A->B->C`、A 失败：B、C 的原因都指向 A。该根因在全部结果回收后统一重算，
不依赖 goroutine 完成先后，保证逐字节确定性。

## 3. 两种模式
- FailFast（默认）：首个 Failed 落定 -> 取消 ctx；所有运行中任务收到取消；未派发任务全部 Skipped。
- BestEffort：不取消 ctx；失败只沿其下游传播（Failed 的严格后代 Skipped）；不依赖失败任务的旁支继续跑完，可成功。
两种模式下多个任务可同时失败，报告记录**全部** Failed；每个 Skipped 的根因按第 2 节规则确定。

## 4. 调度器与复杂度
- 就绪条件：全部前驱 Success。入度计数 + 每张「后继表」，节点完成时只更新其直接后继，不全图扫描。
- 就绪队列：ID 升序最小堆，同时就绪时按 ID 取，保证确定性。
- 并发上限 N：容量 N 的信号量；每个派发 goroutine 先获取令牌再运行。
  非导出计数器 `peakRunning` 在持锁区随 `running` 增减取 max；测试读峰值。
- 非导出 `readyDecisions`：每次"尝试派发就绪节点"的判定计数。每条边至多触发一次后继重估，
  每节点入队/派发各常数次，故 `readyDecisions <= 4*(V+E)`。
- 调度循环：单 goroutine 持 mutex 串行处理 `completeCh` 事件，事件携带 起始标记/结果/error/panic，
  锁内完成状态迁移与入队，无数据竞争。`Run` 等待所有已派发 goroutine 退出后返回 -> 无泄漏。

## 5. 确定性
- 完成顺序随机不影响结果：终态与根因只依赖图结构与错误集合；报告按 ID 字典序渲染，
  文本固定为 `ID 状态 原因`。故边添加顺序打乱、注入随机延迟，报告逐字节相同。

## 6. 包划分
graph(图/环/层) · sched(堆/信号量/计数器/调度循环) · exec(任务包装/recover/结果)
fail(取消/状态机/根因传播) · report(终态记录与渲染) · cmd/demo(验收演示)。
