# 属性级权限过滤器设计

## 目标与安全边界

按角色授予列可见性。谓词一旦读取不可见列，整条查询拒绝；行投影只保留可见列，键缺失即不可见，不用零值或 NULL 伪装。

## 三种谓词处理

- 甲：把不可见列比较当假。探针 `NOT (secret = 1)`：当 secret 为 1 时内层假、NOT 真，secret 非 1 时被过滤，调用方可反推该列值。
- 乙：把不可见列当 NULL。探针 `secret IS NULL` 恒真，可确认不可见列存在；其他比较经三值逻辑也会形成值相关信道。
- 丙：发现不可见列即拒绝，并返回列名与从根到节点的路径。调用方只能得到“权限不足”，不能观察数据或行是否匹配。

本实现采用丙。

## OR 的常量折叠例外

检查先做自底向上常量折叠，但不根据不可见列求值。定义：只有当某支在删除不可见引用后仍能独立折叠为常量时，才可短路：

- `FALSE OR p` 可折叠为 `p`，不可见列若只在已证明的 FALSE 支中，未被读取，允许执行。
- `TRUE OR p` 可折叠为 TRUE，不可见列若只在未求值的 p 中，未被读取，允许执行。
- 不可折叠支中的任何不可见列仍拒绝；`AND` 不采用对称宽松规则，避免把规则扩大到非需求场景。

同一列多次引用只在拒绝集合中出现一次，但保留每条引用路径。

## 结构

- `policy.RolePolicy`：角色到可见列集合。
- `predicate.Node`：常量、列比较（含 `IS NULL`）、AND、OR、NOT；路径用 `and/2`、`or/2`、`not`、子节点序号编码。
- `filter.Filter`：一次折叠和校验，一遍遍历节点；随后按可见列构造新行。
- `report.Report`：有序裁列、有序拒绝引用、拒绝状态；确定性 JSON 便于逐字节比较。

## 复杂度

谓词折叠与校验合并为一次后序遍历，访问计数等于节点总数。行裁剪遍历可见列并最多为每个可见列执行一次 map 读和一次 map 写，1000 列、5 可见时拷贝操作上界为 `4*5`。
