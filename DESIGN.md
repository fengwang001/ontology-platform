# 带转义 RLE 设计

不变量：严格解码器接受的每个 t 必须满足 `Encode(Decode(t)) == t`。据此推导拒绝清单。

## 拒绝清单推导

1. 显式次数 1（`1a`）：Decode 得 `a`，Encode 为 `a`，与 `1a` 不等。拒绝。
2. 次数 0（`0a`）：Decode 为空贡献，但游程必须含一个符号；Encode 不会产生任何对应形式。拒绝。
3. 前导零（`01a`、`00a`）：Decode 得 `a`，Encode 为 `a`；且规范十进制无前导零。拒绝。
4. 相邻同符号游程（`2a3a`）：Decode 得 `aaaaa`，Encode 为 `5a`（最长游程），与原串不等。拒绝。
5. 非法转义（`\x`）：转义只允许数字与反斜杠；该形式无法由 Encode 产生。拒绝。
6. 末尾孤立 `\`：转义不完整，无任何 Encode 输出以 `\` 结尾。拒绝。
7. 只有次数无符号（`12` 末尾）：游程必须以符号收尾，Encode 不会产出。拒绝。
8. 非法 UTF-8：符号必须是 Unicode 码点，Encode 的输出恒为合法 UTF-8。拒绝。
另：次数溢出（超过 math.MaxInt64）无法在不溢出的前提下计数与重放，拒绝（见下）。

## 大次数取舍

选择：拒绝次数超过 `math.MaxInt64`（`runs.MaxCount`）的游程，解析中一旦超限立即报错、绝不静默回绕；
理由：合法输入的重复次数在 64 位平台上不会达到该量级，拒绝比引入任意精度整数更简单且无溢出风险。
解码重放采用定长缓冲分块写出，绝不在解码前按次数分配内存；输出总字节数设上限
（`DefaultLimit`，可用 `WithLimit` 配置，`WithLimit(-1)` 不限），超限在写出前返回
`ErrLimitExceeded`。

## 语义 ↔ 测试对照

（最后补齐）
