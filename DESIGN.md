# 设计推导

## 1. 状态机
- `eol` 只记住上一字节是否为待定 `\r`。待定 `\r` 遇 `\n` 合成一个行尾；遇下一个 `\r` 先收束为单独行尾，再保留新 `\r` 的待定状态；遇普通字节或结束则收束为单独行尾。
- `ws` 只缓冲连续空格和制表符。遇到普通字节先原样冲刷；遇到行尾删除；流结束也按行尾空白删除。
- 连续被删区间合并为一个 `span` 区间，因此普通文本映射保持恒等，区间数只随删除点增长。

## 2. 偏移映射
映射采用左闭右开区间。删除区间记为原文 `[a,b)` 折叠到输出点 `p`；后续原文字节整体前移 `b-a`。
- `ToOrig(p)` 跳过删除区间，返回删除后第一个保留字节；所以 `\r\n` 的换行点映射到 `\n`，孤立 `\r\n` 才映射到 `\r`。
- 删除区间内部的 `ToOut(i)` 都等于 `p`。这是唯一同时满足单调不减且 `ToOut(ToOrig(p))=p` 的选择。
- 编辑器落在被删行尾空白或 `\r\n` 的 `\r` 上时，输出光标位于换行符之前；端点本身仍映射到 `\n`，不会落到下一字符。
- 流末被删空白折叠到输出终点，`ToOrig(输出终点)=原文终点`。

## 3. 末尾换行策略
三种策略都只允许折叠，不插入字节。原因：插入会让输出位置数多于原文位置，而 `ToOrig` 单调且每个输出位置都要被 `ToOut` 还原，无法给插入字节找到不同原像。故无末尾换行时三种策略均保留零个换行；有一个或多个换行时：
- `Preserve`：保留全部末尾换行。
- `EnsureOne`：已有换行则折叠为一个；没有则不插入。
- `TrimBlanks`：删除末尾空行，若原本有换行则留下一个；没有则保持无换行。

| 输入 | Preserve | EnsureOne | TrimBlanks |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

上述输出均不含 `\r` 或行尾空白，再次规范化保持不变，所以三种策略分别幂等。

## 4. 上限选择
空白延迟缓冲选择超限即拒绝，错误携带导致超限字节的原文偏移并进入终态。若强制把待定空白判定为“非行尾”，其后可能立刻看到行尾，将无法撤回已输出空白，违反行尾裁剪、幂等和映射互逆；拒绝不产生新输出，已有输出和映射仍可解释。输出上限在字节追加前检查，超限同样终态化。

## 5. 并行拼接
并行切点先回退到最近安全边界：从上一个字节向前越过待定 `\r` 与连续空格/制表符，直到已确认的 `\n` 或普通字节。每段从安全边界独立规范化，内部段使用 `Preserve`，末段使用配置策略，段间不存在待定 `\r` 或待定空白。
各段映射先记录段内坐标；收集后按原文起点与已拼接输出长度平移，并追加到总映射。安全回退只经过待定 `\r` 和一串待定空白；该空白串受缓冲上限约束，故修正范围至多为空白上限加少量行尾字节；无上限时最坏上界是输入总长。
