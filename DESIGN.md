# 严格 Base64 规范尾部推导

标准编码把 3 字节切成 4 个 6 位组；不足 3 字节时用 `=` 填充。
为保持任意字节串的编码结果唯一，解码器必须只接受编码器实际会产出的 4 字符组。

1 字节情形的四元组形如 `XY==`：
- `X` 使用前 6 位；`Y` 的高 2 位来自该字节最后 2 位。
- `Y` 的低 4 位没有源数据，必须全部为 0。
- 因而规范判定检查 `Y & 0b001111 == 0`。

2 字节情形的四元组形如 `XYZ=`：
- `X`、`Y` 承载第一字节；`Z` 的高 4 位来自第二字节。
- `Z` 的低 2 位没有源数据，必须全部为 0。
- 因而规范判定检查 `Z & 0b11 == 0`。

`QQ==` 中第二个符号 Q=16，低 4 位为 0，所以合法。
`QR==` 中 R=17，低 4 位非 0；这些比特不可能由标准编码器写入，宽松解码却会将其丢弃。
因此拒绝 `QR==`，才能保证每个被接受的填充尾部都唯一对应一次标准编码，避免“多个文本解码成同一字节串”。

合法填充仅限末尾完整组：`XY==` 或 `XYZ=`。无填充、一到三个等号、组间填充均为填充位置错误。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 标准字母表、`=` 填充、76 列 CRLF | 待补 |
| 2. 规范形式样例与唯一编码 | 待补 |
| 3. 换行位置规则 | 待补 |
| 4. 五类错误及字节偏移 | 待补 |
| 5. 任意切分点一致 | 待补 |
| 6. 严格解码与编码往返 | 待补 |
| 7. 输出上限与终态 | 待补 |
