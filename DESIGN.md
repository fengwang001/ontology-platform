# CSV 方言设计推导

## 1. 空行与单列空值
选择保留**所有物理行**：空行是一条记录，含一个未引号空字段。因此末尾换行只是终止最后一条记录，不额外生成记录；无换行 EOF 也可结束。

单列表中记录的唯一字段为空时，若回写成空字节串，Parse 无法区分「一条空值记录」与「零记录/末尾换行」。故该字段必须写成 `""`。普通空字段不加引号；含逗号、引号、CR、LF 时必须加引号，内部引号双写。回写保留已加引号的标记：最小引号是相对结构必要的上闭包，保留标记才能同时满足字段值与引号标记逐位往返。

## 2. 并行切点
每段都从两种入口假设独立状态机：`outside`（引号外）与 `inside`（引号内）。`inside` 入口还需前一字节是否为 `"`；只有前一字节是 `"` 才初始为「引号内刚见引号」，否则为普通引号内。段起点在 CR/LF 中间时，两假设都会把后续 LF 归入同一待定转移，状态一致。

前一段结束状态唯一确定后选择后一段对应假设。段内字段先偏移到：若入口在引号内，取段起点；否则取段首逗号/CRLF 后的字段起点。该字段在前一段已有值（引号内含段前内容），选择结果发出同一字段的后缀，拼接器只在引号内切点把后缀内容并入左段未完成字段；其他入口从新字段开始。事件流包含 Field/Record/Error，拼接时加段基址、起始记录号、字段号；错误只采用真实入口路径，故坐标与流式相同。

双假设逐字节同步推进，每字节最多处理两次；段数常数开销，不回扫长字段。

## 3. CR 待定与上限
引号外遇到 CR 进入 CR 待定：下一字节 LF 产生行边界；EOF 或其他字节产生孤立 CR 错误。半包时状态保留，跨 Feed 与 par 段边界规则相同。引号内 CR 是内容，直接进入字段值。

字段上限按字段值字节数计数：普通字节和 CR/LF 各按实际字节；引号内转义 `""` 仅产生一个值字节，但原始两个引号字节仍由机器处理一次。每读到第一个将使长度超限的值字节立即返回上限错误，不等待分隔符。记录字段数在字段定界时立即计数。

## 4. 状态转移表
| 状态 | 普通 | `,` | `"` | CR | LF | EOF |
|---|---|---|---|---|---|---|
| FStart 字段开始 | 转 FData | 发空字段 | 转 FQuote | 待定 | 发记录 | 发活动字段 |
| FData 无引号 | 留 FData | 发字段 | BareQuote 错 | 转 FCR | 发记录 | 发活动字段 |
| FQuote 引号内 | 留 FQuote | 内容 | 转 QSaw | 内容 | 内容 | UnclosedQuote 错 |
| QSaw 刚见引号 | 内容转 FQuote | 发字段 | 内容转 FQuote | 发字段后 CR | 发记录 | 发活动字段 |
| FCR CR待定 | LoneCR 错 | LoneCR 错 | LoneCR 错 | LoneCR 错 | 发记录 | LoneCR 错 |

引号闭合后再遇普通字节（上表 QSaw/普通）报 QuoteAfterClose。
