# 熔断与舱壁隔离调用保护器 — 设计推导

## 0. 总体结构

调用链固定为：`breaker.Allow → bulkhead.Acquire → timeout.Do(fn) → classify.Of → stat 记账 → breaker.OnSuccess/OnFailure`。
组合体 `stat.Protector` 是唯一入口，保证统计口径在每一条路径上都自洽。时钟通过 `breaker.Config.Now` 注入；测试用假时钟推进冷却、断言拒绝不推进时间。

## 1. 熔断计数只算「真实调用」的推导

定义：真实调用 = 通过熔断与舱壁、实际进入 `fn` 的调用。失败率 = 失败数 / 真实调用数。

反证：若被熔断拒绝的调用也计入失败（或计入分母），则熔断一旦打开，后续所有请求都被拒，
每次拒绝又抬高失败数/失败率 → 失败率恒为 100% → 永远满足打开条件 → 半开探测尚未发生，
打开状态就被自己的拒绝自我维持，熔断器永远无法闭合。因此：

- 被拒绝（熔断拒绝、舱壁拒绝）的调用**既不进分子也不进分母**；
- 失败率分母只含真实调用，即 `真实调用数 = 总请求数 - 熔断拒绝数 - 舱壁拒绝数`；
- 由此推出全局恒等式：`总请求数 = 成功数 + 失败数 + 熔断拒绝数 + 舱壁拒绝数`，
  且 `按类别细分的失败数之和 = 失败数`（每个真实调用非成功即失败，失败必属某一类别）。

测试构造「打开后连续 1000 次被拒」，断言失败率统计不变、半开后一次成功即关闭。

## 2. 熔断置于舱壁之前的论证

规定顺序：先熔断，后舱壁。理由：

- 熔断打开意味着下游已知故障，此时应立即拒绝，不占用任何并发额度与队列名额，
  把额度留给其他下游（舱壁按下游隔离）或状态翻转后的探测调用；
- 若舱壁在前，被拒请求也要先排队/占额度：下游故障期间队列被注定失败的请求塞满，
  产生队头阻塞，恢复信号（成功调用）反而挤不进来；
- 统计口径上，熔断拒绝与舱壁拒绝是两类不同事件，先熔断可让「熔断打开时舱壁占用恒为 0」
  成为可断言的不变量（测试逐次断言 `InFlight()==0`）。

## 3. 四条路径都必须归还额度

真实调用只有四种结局：成功、失败（error）、超时、panic。额度在 `Acquire` 成功后用
**单个 `defer Release`** 覆盖全部四条路径：panic 在 `timeout.Do` 内被 recover 转为普通 error，
不会击穿包装器，因此释放一定发生在同一栈帧。若任一路径漏还，额度单调泄漏至 0，
舱壁退化为全拒。测试在四条路径上各跑 1000 次，断言可用额度回到满值。

## 4. 不可重试错误是否计入熔断

定义：**计入**。论证：

- 口径自洽：每个真实调用非成功即失败，失败必属 {可重试, 不可重试, 超时} 之一；
  若不可重试失败不计入，则「分类失败数之和 = 失败数」断裂，统计等式不再成立；
- 语义上，不可重试 ≠ 下游健康（如下游确定性地返回 500 类错误），熔断衡量的是
  「真实调用的成功率」，与「是否值得重试」是两个正交维度；
- 本题不实现重试，可重试性只影响分类报告，不影响熔断计数。

## 5. 熔断状态机精确语义

- 关闭→打开：连续失败达阈值 **或**（失败率超阈值 且 样本数达下限），两条件独立实现、分别测试；
- 打开→半开：注入时钟 `now ≥ openedAt + cooldown`；冷却时长从基础值起，每次半开失败加倍，
  上限 `MaxCooldown`，回到关闭后重置为基础值；
- 半开→关闭：半开期允许的探测数内全部成功（成功数达探测数）；
- 半开→打开：任一探测失败立即打开，冷却加倍（有上限）；
- 半开并发：同时最多放行「探测数」个调用，其余直接拒绝；
- 时钟回拨：`now < openedAt` 时 `Allow` 返回 `ErrClockSkew`，状态保持不变；
- 并发迁移：所有状态迁移在互斥锁内完成并计数，多协程并发触发失败时迁移只发生一次、
  冷却只加倍一次。

## 6. 舱壁精确语义

- 额度 N：在途真实调用数恒 ≤ N，非导出峰值计数器记录历史峰值供测试断言；
- 队列上限 Q：等待者计数用原子加法占位，第 N+Q+1 个请求立即返回 `ErrBulkheadFull`，
  不睡眠、不推进注入时钟；
- 等待中被 ctx 取消：从等待计数移除并返回 `ctx.Err()`，名额自动让出；
- 额度 N=1 时退化为串行，语义不变；
- N≤0 或 Q<0 构造报错；熔断阈值 ≤0、探测数 ≤0、冷却 ≤0 同样构造报错。

## 7. 错误分类

四类哨兵错误可用 `errors.Is` 区分：`ErrTimeout`、`ErrBulkheadFull`、`ErrCircuitOpen`、
`ErrClockSkew`。`classify.Of` 把真实调用的错误分为 可重试 / 不可重试（`classify.Mark` 标记）/
超时 三类；panic 被 `timeout.Do` 捕获转为普通 error，按可重试归类。
