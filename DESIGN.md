# 设计推导

## 流格式记录布局（全部数值为无符号变长整数 LEB128，≤10 字节）

| 记录 | 标签字节 | 后随字段 |
|---|---|---|
| 流头 | `0x4F 0x4E`("ON") + 版本 `0x01` | 窗口容量(varint)、最小匹配长度(varint) |
| 字面量 | `0x10` | 长度 n(varint)、原始字节 n 个 |
| 回指 | `0x20` | 距离 d(varint, d≥1)、长度 l(varint, l≥3) |
| 刷新 | `0x30` | 无 |
| 流尾 | `0x40` | 原文总长(varint)、IEEE CRC-32(varint) |

校验和覆盖全部原文字节（与记录切分无关）。

## 推导一：压缩输出为何与 Write 切法无关（语义 3/4）

贪心匹配在位置 p 是否发回指，取决于从 p 开始能匹配多长。只有当下一个待判字节「失配」时，
当前匹配的终点才确定；失配可能发生在任意一次后续 Write 里。因此压缩器把收到但尚未判定的
字节全部缓存在内存（lookahead），决策循环每轮要求 lookahead 至少含 minMatch=3 字节才探测匹配：

- 探测到长度 L≥3：此刻还不能发——若 lookahead 正好耗尽，L 可能在下次 Write 后变长；
  只有当 L < lookahead 长度（第 L+1 字节失配，匹配终点被钉死）时才提交该回指；否则整段缓存等待。
- 探测不到匹配：p 字节在任意历史下都不可能成为匹配首字节（需要连续 3 字节才能建哈希），
  故可立即作为字面量提交。

所有判定只依赖「当前窗口 + 已缓存字节」的内容，与它们由几次 Write 写入无关，输出因此逐字节一致。
Flush 意味着调用方要求可还原性边界：必须把「尚未钉死的最长匹配」也强制当作终点提交
（force=true 时 L 等于 lookahead 长度也发），随后写刷新标记。窗口不清空，故之后仍可回指之前数据。
连续两次 Flush 间无输入时不脏，输出零字节。

## 推导二：重叠回指的复制（语义 2）

d<l 时，目标区间与源区间重叠。一次性块复制 `copy(dst[p:], src[p-d:])` 若被当作
不重叠拷贝会从头部搬运；语义正确的 LZ 要求源随输出增长，即重复长度为 d 的图案：
`out[p+k]=out[p+k-d]`，逐字节向前写。实现为循环「每轮复制 min(剩余长度, 距离) 字节，
源永远是已存在的输出末尾」：首轮复制 d 个历史字节，之后每轮的源都是上一轮刚写出的字节，
图案自然延展。既不退化成一次整块 copy，也不必真的逐字节循环。

## 推导三：「距离超过已输出」与「距离超过窗口」（语义 7）

d>窗口容量：窗口里根本没有该字节，压缩器在任何情况下都不可能产生——哈希链只索引窗口内
（寿命 ≤ 容量）的位置，候选距离被 Find 强制夹在 [1,容量]；故这是「格式/窗口不一致」类损坏。
d≤容量但 d>已输出字节数：窗口装得下，却指向尚未产生的字节，属「前向引用」损坏，发生在流被
篡改时。两类必须分开报，因为前者还意味着流头声明的窗口与记录矛盾。
并行预置字典不改变含义：字典字节在本块压缩前已逐字节 Insert 进窗口与哈希链，解压端则是
「上一块的实际输出」，二者在流上是同一段已输出字节；解码器只维护全局已输出计数与一个窗口，
d 永远指向已经还原出的字节。

## 推导四：并行块压缩的确定性（语义 9）

第 k 块的压缩输入是「本块原文 + 上一块末尾至多窗口容量字节」：窗口状态完全由这段原文经
固定插入顺序确定，哈希链与贪心决策都是纯函数；不读第 k-1 块的压缩记录，也不读任何其他块，
故与 goroutine 调度顺序、worker 数无关。块间不加刷新标记：刷新标记是给「调用方等待点」的
语义承诺，块边界并非等待点；各块记录首尾直接拼接，共享同一个流头与流尾（总长、CRC 在汇总时
统一计算）。最后一块按 force 收尾，保证无缓存残留。worker 数只改变并发度，结果字节一致。
