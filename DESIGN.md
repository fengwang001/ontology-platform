# 设计推导

## 1. 零计数 hunk 头的行号

行号语义是「hunk 之前已消费的同端行数」`p`。普通 hunk 首行是第 `p+1` 行，故写 `p+1`；
纯插入 hunk 没有旧端行可指，GNU 仍需一个行号表达插入点，便写前一行的编号 `p`（旧文件
里它前面有几行）。

- 样例 `x` -> `y,x`（C=0）：插入前旧端消费 0 行，`p=0` -> `-0,0`，即文件最顶端插入。
- 样例 `a,b,c` -> `a,b,X,c`（C=0）：插入点在第 2 行之后，`p=2` -> `-2,0`。
  这不是「插入位置 3」：编号 2 是新行 `X` 前面最后一条真实旧行。
- 样例 `a` -> 空（C=3）：新端在删除前消费 0 行 -> `+0,0`；旧端有 1 行 -> `-1`。

Apply 解释（旧端 `-a,b`，0 基切片下标）：`b==0` 时插入下标为 `a`（插在第 a 行之后，
`a=0` 即顶端）；否则替换起点下标为 `a-1`。

| 旧端计数 | 头部书写 | Apply 的起点（0 基） |
| --- | --- | --- |
| b=0 | `-a,0`，a=hunk 前消费行数 | 纯插入，插在下标 a |
| b=1 | `-a`（省略 `,1`），a=p+1 | 从下标 a-1 开始匹配/替换 1 行 |
| b>=2 | `-a,b`，a=p+1 | 从下标 a-1 开始匹配/替换 b 行 |

## 2. hunk 合并阈值

每个 hunk 在改动两侧各取至多 C 行上下文。两改动间未改动行数为 g：

- g <= C：两侧上下文重叠，必合并；
- C < g <= 2C：上下文不重叠但首尾相接（g=2C 时恰好接满，中间无剩余行），GNU 仍输出
  一个 hunk，故仍合并；
- g = 2C+1：两段上下文之间剩 1 行谁也不覆盖，GNU 拆成两个 hunk。

阈值是 **2C**（g <= 2C 合并）。C=1 反例：g=2 若按阈值 C 拆开会得到两个相邻 hunk，与
GNU 的单 hunk 不符；g=3 若按阈值 2C+1 合并，会把 1 行间隔吞进同一 hunk，也与 GNU 不符。

## 3. 并列最短脚本的选择

Myers 前向在对角线上选步时：能从下方（插入）或右方（删除）同值到达时，取右方，即
**优先删除**（回溯用同一判定）。例 `a b` -> `b a`，D=2 有两条最短路，固定输出
`-a`、` b`、`+a`：删除行排在插入行之前，与 GNU diff 默认输出一致。因此补丁文本对
相同输入逐字节确定。

## 4. 偏移后后续 hunk 的查找起点

第 i 个 hunk 记录位置为 r_i，在当前文本上实际落点为 q_i，且该 hunk 净增行数
d_i = 新端行数 - 旧端行数。第 i+1 个 hunk 的查找起点平移为
r_{i+1} + Σ(q_k - r_k) + Σ d_k：偏移 q_k-r_k 表示补丁与目标整体错位，后续 hunk 同样
错位；净增 d_k 改变其后所有行的绝对行号。候选仍按距该起点 0,-1,+1,-2,+2,... 搜索
（同距取靠前），超出 F 判定超出偏移范围。
