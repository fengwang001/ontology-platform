# 自定格式 LZ77 流式压缩 — 设计推导

模块 `ontology`，Go 1.26。窗口容量 W（默认 32768），链长上限 C（默认 32）。

## 流格式（全部变长整数 = 无符号 LEB128，≤10 字节，第 10 字节只允许最低位）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `LZ7` + 版本 `1` + varint W |
| 字面量 | tag=0 + varint n + n 个原文字节 |
| 回指 | tag=1 + varint(距离-1) + varint(长度-3)，长度∈[3, 1<<16] |
| 刷新 | tag=2（单独字节，无载荷） |
| 流尾 | tag=3 + varint 原文总长 + varint FNV-1a 32 校验和 |

tag 字节取值 0/1/2/3；其他首字节非法。空输入产出「头 + 尾」非空流。

## 推导一：压缩结果与 Write 切法无关（语义 3、4）

在偏移 i 找到匹配 [i,i+L) 时，还不能立刻输出：下一字节可能让匹配延长，且新位置
可能找到更长匹配。规则：**只在某位置的匹配结束边界被「确认」后才输出它**。

维护游标 pos 与链头表 head[hash]，每收到字节把该位置挂进链；在 pos 搜索时只沿链
考察 < pos 的候选（位置尚未插入，未来写入只是给链追加更老方向的节点，不改变当前
候选序列），取贪心最长匹配 M。匹配先悬置不发；处理下一字节前，若 M 的末端恰在当前
数据末尾则等待（后续 Write 可能延长/换更长链），否则 M 已被失配字节封顶，立即提交：
先发匹配前尚未提交的字面量段，再发回指，pos 跳到 M 末端。Flush/Close 时数据末
必然确定，强制提交悬置匹配并发刷新（Close 不发刷新，直接流尾）。

因每个偏移的候选序列与匹配长度只取决于「已到达的更早字节」，提交只取决于是否出现
封顶失配字节，故同一累计字节序列在不同 Write 切法下产生逐字节相同输出；Flush 位
置（累计偏移）相同则刷新记录位置也相同。Flush 强制结束悬置匹配，因此解压器此刻可
还原全部输入；之后仍可回指 Flush 前数据（窗口未清）。无新输入再 Flush 时没有任何
悬置/未提交内容，发 0 字节。

## 推导二：重叠回指（语义 2）

距离 d < 长度 L 时，输出 [p-d..] 的内容本身正在生成。逐字节复制：
`out = append(out, out[len(out)-d])` 循环 L 次，天然正确（如 d=1 复制出长串相同
字节）。一次性 copy 切片别名时虽部分库实现可重叠，但语义不保证且易错。L 上限
65536，最坏输入每回指至多 65536 次简单 append；实测 4MB 同字节在毫秒级，故直接
采用逐字节循环，换取确定正确、零额外分配。

## 推导三：两类「距离过大」错误（语义 7）

- dist==0：编码为(距离-1)，0 不可表达，必为损坏/伪造。
- dist > 已输出字节数：没有可引用的历史，流在该点就不合法。
- dist > W：即使历史够长，解码器只保留 W 环形窗口，取不回；且压缩器（含并行预
置字典）插入哈希的候选位置距当前永远 < W，**绝不可能产生** dist>W 的回指。
三者按此顺序判，错误各自具名并携带记录起始字节偏移。并行块把上一块末尾 W 字节预
置进窗口与哈希表（不产出、不计入本块输出），解压时这些字节已逐块解压并留在同一
环形窗口里，故「已输出字节数」是整个流的累计计数，含义不变，块间无需特殊距离
编码。流衔接靠每块末尾的刷新记录（最后一块除外）。

## 推导四：并行块压缩的确定性（语义 9）

块 k 的输入 = 本块原文 + 上一块原文末尾 W 字节（预置字典）。按推导一，其输出只
是这两段确定字节的函数：窗口初始只含预置字典，块内不读取更早块、不共享可变状态。
因此 goroutine 数与完成顺序不影响结果；workers=1 串行执行同一段代码，输出一致。
每块用独立编码模式（无流头），块尾发刷新；由调用方在最前补一个流头、最后补流尾
（总长、全流 FNV），拼成一个普通合法流，流式解压器无需感知分块。

## 复杂度

每位置沿哈希链至多考察 C 个候选，总考察数 ≤ C×字节数；同字节最坏情形被 C 截断，
不随 W 或输入规模增长（4MB 同字节：链上同 hash 极多但每位置只看 C 个）。
