# DESIGN

## 推导一：`x/**` 与 `x`、`x/`；`**/x` 与 `x`、`/x`
路径按字面 `/` 切段，空段保留：`x/` = 段 `[x,""]`，`/x` = 段 `["",x]`。
`**` 匹配「零个或多个**整段**」，零段时它不消耗任何段（连相邻的 `/` 都不产生）。
因此固定前缀 `x` 后接 `**` 的 `x/**`：
- `x`：前缀消耗完，`**` 取零段 => **匹配**；
- `x/`：需 `**` 匹配空段 `""`，空段也是整段 => **匹配**；
- `x/y`：`**` 匹配段 `y` => 匹配。
同一规则解释 `a/**/b` 匹配 `a/b`：中间 `**` 取零段，`a` 与 `b` 直接相邻。
`**/x`：`x` 时 `**` 零段 => 匹配；`/x` 时 `**` 匹配空段 => 匹配（不规范化，`""` 是合法段）。

## 推导二：空串路径
空串切成 0 段。`*` 是「段内」原子，必须有一段可作用；空路径没有段可作用 => 不匹配。
结论：`*` 不匹配空串；`**` 匹配零个整段 => 匹配空串；`?` 不匹配；空模式匹配空串。

## 推导三：无回溯算法
段内（无 `**`）用「最后星号回退点」法：记录最后一个 `*` 及其后 atom 的位置与段内偏移；
失配时只回退到最后一个 `*`，让它多吃一个码点。正确性：若存在任一匹配，把前面所有 `*`
的让渡全部移给最后一个 `*`（`*` 对任意子串都可吸收）仍得到匹配，故只记最后星号不漏配；
每一步要么前进、要么回退且回退点单调前进，O(原子数 × 码点数)。
不能照搬到 `**`：`**` 跨段时不同 `**` 的段分配相互耦合（`**/a/**` 中 `a` 的锚定位置
不唯一），让渡无法全部合并到最后一个。改用段级 DP：状态 `(模式段位置, 路径段位置)`，
普通段要求精确匹配该段，`**` 转移为「不消耗段」或「消耗一段」。状态数 O(P×N)，
每个普通段匹配本身线性，合计 O(模式原子数 × 路径码点数)。

## 推导四：规则集版本一致
`set` 用 `atomic.Pointer[version]` 持不可变版本（规则编译后冻结）。查询先取一次指针，
整次判定与 `Explain` 只用该快照，天然只属于一个版本。替换先在新切片上整体编译，
任一规则失败即返回错误、不触碰原子指针，旧版本继续服务。

## 匹配结果表
| 模式 | "" | x | x/ | /x | x/y | a/b | a/x/y/b |
|---|---|---|---|---|---|---|---|
| x/** | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| **/x | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| * | 否 | 是 | 否 | 否 | 否 | 否 | 否 |
| ** | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| a/**/b | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
