# 设计：多租户配额公平队列调度器

模型：租户 i 有权重 w_i，任务有代价 c。每个租户维护虚拟时间 vt，
调度器每次取 vt 最小者执行一个任务。状态全在进程内存，时钟可注入
（本实现不读墙钟，推进完全由调度事件驱动，测试可精确重放）。

## 1. 为什么是 vt += c/w 而不是 c*w

目标：长期每个租户获得的执行代价份额正比于其权重，即 S_i / S_j = w_i / w_j。
把 vt 定义为"已获得的归一化服务量"：vt_i = S_i / w_i。
调度器每轮取 vt 最小者，等价于让各租户的归一化服务量保持相等：
vt_i 约等于 vt_j 推出 S_i / w_i 约等于 S_j / w_j，即 S_i 正比于 w_i，正是目标。
因此执行一个代价为 c 的任务后必须 vt += c / w：
权重越大，vt 涨得越慢，下次仍容易被选中，于是拿到更多服务。
若反过来用 c*w，权重大的租户 vt 涨得更快、被惩罚，权重语义被倒置：
1:3 的两个租户会收敛到 3:1 而非 1:3。故只能是 c/w。

## 2. 空闲租户重新加入：vt 必须抬升

定义系统虚拟时间 sysVT = 当前堆中（非空租户）的最小 vt，单调不减，
每次取出堆顶时把 sysVT 推进到堆顶的 vt。
租户 X 空闲期间不执行任务，vt 停在旧值；若 Y 在此期间执行了 N 个
代价为 c、权重 w 的任务，sysVT 已推进约 N*c/w，X 落后约 N*c/w。
若 X 带着旧 vt 直接重新入堆，它每一轮都是最小者，直到 vt 追上 sysVT：
独占步数约为 落后量 * w_X / c_X。例：w=1、c=1、N=1000 则 X 连续独占
约 1000 步，Y 被饿死 1000 步。这正是"新来租户起点"的经典错误。
正确做法：从空转为非空（含首次注册后首次入队）时
vt = max(vt, sysVT)。X 被抬到当前服务前沿，立即与他人公平交替：
实测连续执行仅 2 步（抬升后与堆顶并列一次、按 ID 赢一次），Y 不饿死。
该规则同时防止"注销重注册刷低 vt"的作弊：新租户也从 sysVT 起步。

## 3. 代价为 0 的任务

c = 0 时 vt += 0，vt 不推进，该租户永远是最小者，导致无限独占。
处理：记账代价取 c_eff = max(c, MinCost)，MinCost = 1.0（最小调度量子）。
零代价任务合法入队与执行，但每次执行至少按 1 个代价单位记账，
vt 照常推进，与其他租户交替，不会独占；代价小于 1 的任务同理按 1 记账。

## 4. 同 vt 并列

堆的比较键为 (vt, 租户 ID)：vt 相等时按 ID 字典序升序。
比较键全序，同一输入流产生唯一调度序列，可复现。

## 5. 数值表示：float64

vt 用 float64。权重极大（1e12）与极小（1e-6）并存时，
单步增量 c/w 最大约 1e18 量级，float64 上限约 1.8e308，不会溢出；
10 万步累加的相对误差约 2^-53 量级，远小于 5% 的公平性容差。
不选用 big.Rat：每次加法都要约分与分配堆内存，10 万步调度开销不可接受，
而本题的公平性判据（5%）对 float64 的精度而言绰绰有余。

## 6. 复杂度

选择 = 堆顶弹出加（仍非空则）再入堆，均为 O(log n)，n 为非空租户数。
单次选择的比较次数上界：down 每层至多 2 次、up 每层 1 次，
合计不超过 3*ceil(log2 n)，小于 4*ceil(log2 n)，
测试以 4*ceil(log2(1000))=40 断言。
空闲租户不在堆中，不占选择代价；堆中元素数等于非空租户数。

## 7. 背压、错误与移除

每租户队列上限 L：入队时若队列长度已达 L，返回 ErrQueueFull；
按租户独立判定，一个租户满不影响其他租户入队。
四类错误可用 errors.Is 区分：ErrQueueFull（队列满）、
ErrInvalidWeight（权重不大于 0，注册时报错）、ErrTenantBusy（有排队任务时移除）、
ErrUnknownTenant（未注册的租户）。
移除语义：队列非空时拒绝移除（ErrTenantBusy），任务去向明确——
要么先消费完，要么租户继续存在；不允许静默丢弃他人任务。

## 8. 并发与确定性

所有共享状态（租户表、堆、sysVT、队列）由一把互斥锁保护，
提交与取出在锁内串行化：每个任务恰好出队一次，不丢不重。
确定性定义：同一确定性任务流（固定提交顺序与代价）推出调度序列逐元素相同。
并发提交时，"提交顺序"以获取入队锁的串行化顺序为准；
该顺序一旦确定，后续调度序列即唯一确定。
