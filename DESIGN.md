# Design

## 1. 零计数时的 hunk 行号（语义 4）

hunk 头表示旧文件从第 a 行起共 b 行、新文件从第 c 行起共 d 行。区间使用 GNU diff 的半开约定：

| b | 头书写 | 插入点解释（Apply） |
|---|---|---|
| 0 | 旧侧写作 a,0（0 不省略） | a = 前一未改行行号；插入点在第 a 行之后。文件首插入时 a=0 |
| 1 | 旧侧只写 a，省略逗号与 1 | 第 a 行本身参与（删除或上下文） |
| 不小于 2 | 旧侧写作 a,b | 旧文件第 a 至 a+b-1 行参与 |

新侧 c,d 规则相同。验证：x 文件在文件首插入 y，前一行不存在，故旧侧 0,0、新侧从 1 起；
a,b,c 中在第 2 行后插入 X，旧侧为 2,0，新侧插入成为第 3 行；删除唯一行时旧侧为 1，
新侧前一行不存在故为 0,0。a 是前一行行号而非插入位置，因为统一格式行号从 1
开始，位置 0 必须编码为 0；Apply 对 b=0 把插入点放在逻辑偏移 a 处（前 a 行保留不动），
对 b 不小于 1 时从偏移 a-1 起替换。

## 2. Hunk 合并阈值（语义 5）

阈值 g = 2C：两个 hunk 各自携带至多 C 行上下文，当间隔未改行数 g 不超过 2C 时上下文区间
相接或重叠，必须合并；g 至少 2C+1 时分隔。例 C=3：g=6 时前 hunk 吃到后 3 行、后 hunk
吃到前 3 行，中间无剩余行，GNU 输出单一 hunk；g=7 时中间剩 1 行不在任一上下文中，
输出两个 hunk。若阈值取 C，则 g=4..6 会被错误拆开（与 GNU 不符）；取 2C+1，则 g=7
会被错误合并。

## 3. 并列最短脚本的选择（语义 3）

Myers 在同一条 D 前沿上，对每个 k 先计算向下（删除）分支 x_down，再计算向右（插入）
分支 x_right；两者并列时取 x = max 并记录来自删除。回溯时同样先尝试向下边。因此
a,b 变成 b,a 的脚本固定为删除 a、上下文 b、插入 a（先删后插，删除块排在同序位插入块
之前）。相同输入重复 100 次输出逐字节相同由此保证。

## 4. 偏移应用后后续 hunk 的查找起点（语义 7）

设记录位置为 p，hunk i 以偏移 f_i 命中（实际行等于 p+f_i）。后续 hunk 的记录起点按其在
旧文件的逻辑位置差值平移：第 i+1 个 hunk 的查找中心等于 p_(i+1) 加此前各 hunk
实际删除区起点减记录删除区起点之和；搜索窗口仍为该中心上下 F 行。候选间取绝对偏移
最小，并列取靠前。原因：一个 hunk 提前或滞后 f 行，其后所有内容在当前文本中整体平移同样
的行数；平移量按命中点逐段累计，而非按 hunk 顺序号，避免把无关间距重复计算。
