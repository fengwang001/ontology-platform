# 多键稳定排序设计

## 1. 多趟排序的趟序：必须从「最次键」排到「主键」

稳定排序的语义是：相等元素保持此前相对顺序。一趟只确定「当前键 + 此前已定好的相对次序」。
所以主键必须最后一趟排——它决定最大分组，组内次序由先前各趟保留；若主键先排，次键趟会跨主键
重排，主键次序被破坏。

数字例（K1 主键升序，K2 次键升序），输入：

| 记录 | K1 | K2 |
|---|---|---|
| a | 1 | 9 |
| b | 1 | 1 |
| c | 2 | 5 |

正确（先 K2 后 K1）：K2 趟得 [b(1,1) c(2,5) a(1,9)]；K1 趟稳定后 [b(1,1) a(1,9) c(2,5)]，
相邻 (a,c)：K1 上 1<2，满足不变量 2。

错误（先 K1 后 K2）：K1 趟稳定后 [a(1,9) b(1,1) c(2,5)]；K2 趟稳定排序会跨 K1 比较：
按 K2 值整体排得 [b(1,1) c(2,5) a(1,9)]，相邻 (b,c) 在 K1 上 1<2 尚可，但相邻 (c,a)：
K1 上 2>1，违反不变量 2「第一个不相等的键上前者必须严格优先」。

## 2. 降序：对「单键比较结果取反」，不能整体反转

比较取反时相等仍判相等，稳定原语照样保留相等元素的输入次序，不变量 1、2 均成立。
「升序排完再整体反转」会把相等元素的相对次序也倒过来，破坏不变量 1；多键场景中还会
颠倒上一趟次键已排好的组内次序，破坏不变量 2。

## 3. 缺失值：定义为「比一切有值都小」，位置随方向翻转

定义严格全序 缺失 < 任意有值，方向只作用于比较符号。于是升序缺失在前、降序缺失在后，
两者互为严格镜像（同方向再取反必回原状），满足不变量 4 自洽。
「一律最前」：降序缺失仍压在最小值之前，与「降序是升序的逆序」矛盾，同数据升序再降序
不能互相还原；「一律最后」在升序下有同样矛盾。全键相等（含全缺）时比较恒为 0，
靠稳定排序保持输入序。
「字段名不存在」指所有记录都无此字段（模式错误），拒绝；「记录缺值」指字段合法但个别
记录没有该键，按缺失值规则参与排序。

## 4. 键清单重复：判定为错误

同一键声明两个方向时，无论「后者覆盖前者」还是「前者优先」，结果都取决于清单写法而非
数据，且两写法对同一数据给出不同结果，违反不变量 3 的确定性；同方向重复则是无意义趟。
故重复键与空键清单各自返回独立哨兵错误。所有拒绝都在移动任何元素前完成（失败不留痕）。

## 5. 确定性说明

「重复排序逐条相同」与「与初始排列无关」不矛盾：全部键相等时排序键提供的序信息完全
相同，稳定排序逐趟输出输入序，这正是被明确允许的唯一例外；一旦某键不等，该键强制决定
先后，初始排列无法影响结果。
