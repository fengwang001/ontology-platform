# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。

## 必须拒绝的输入（接受即违反不变量）

1. 显式次数 1：`1a` 解码为 a，但 `Encode("a")="a" != "1a"`。
2. 次数 0：`0a` 不产生任何符号，而 0 游程本就不存在；无对应编码。
3. 前导零：`01a`、`007a` 的规范形式是 `a`、`7a`，重编码不等于原文；`00a` 还含零次数。
4. 相邻同符号游程：`2a3a` 等于 aaaaa，最长游程编码为 `5a`，不等于 `2a3a`。
5. 非法转义 `\x`：Encode 只产出 `\` 加 ASCII 数字或 `\` 本身，无编码可生成它。
6. 末尾孤立 `\`：没有符号，且任何 Encode 结果都不会以 `\` 结尾。
7. 末尾只有次数无符号：`12` 不完整；Encode 的每个游程必带一个符号。
8. 非法 UTF-8：Decode 的输出必须是合法 UTF-8，Encode 永远不会产出此类字节序列。

偏移规则：次数类错误指向该次数首字节；非法转义指向 `\` 后字节；孤立 `\`
指向 `\`；缺符号指向输入末尾；非法 UTF-8 指向该序列首字节；相邻同符号指向
下一游程次数首字节（次数省略时为其符号字节）。

## 大次数取舍：拒绝

次数超过 int64 即报 `ErrCountTooLarge`。逐位累加 `n = n*10 + d`，累加前检查
`n > (MaxInt64-d)/10`，因此绝不静默回绕。理由：单次输出受 Go 切片长度（int）
与输出总字节上限约束，任何能被实际输出的游程都远小于 int64，支持更大数字没有
收益，只增加任意精度运算的复杂度。解码前不按次数预分配：计数合法后用固定
4KiB 缓冲分块写出；输出累计字节数可配置上限（默认 1<<30），超限报
`ErrOutputLimit`，故 `99999999999999999999a` 只扫描十几字节即返回错误。
