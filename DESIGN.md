# 只读快照一致性导出器 — 设计推导

模块 `ontology`，仅标准库。包：`store`（版本化 KV）、`snapshot`（快照句柄）、
`manifest`（清单编码）、`export`（分块导出/续传）、`verify`（校验）、`cmd/demo`。

## 1. 写时复制：触发点与保留值的读取

`store` 为每个键维护按版本递增的条目链 `[]entry{version, value}`，全局版本号随
每次 `Put` 单调递增。快照打开时在 `store` 注册当前版本水位 `v`。

**触发点推导**：设快照水位为 `v`，导出读取键 k 时应看到「版本 ≤ v 的最新条目」。
若 `Put(k)` 直接就地覆盖最新条目，则导出读到的是版本 > v 的新值，快照被污染。
因此正确做法是：`Put` 时若存在活跃快照，且被覆盖的旧最新条目对某快照可见
（存在快照水位 ≥ 该条目版本），就**不得就地覆盖**，而是把新条目追加到链尾，
旧条目成为「保留值」；读取时按水位二分查找 ≤ v 的最新条目，自然读到保留值。
若当前没有任何活跃快照，旧条目对无人可见，直接就地替换，不产生保留值。
由此得资源结论：保留值个数 ≤ 导出期间被改写的键数，与总键数无关。

## 2. 保留值的释放条件（为什么按最老快照版本判断）

快照关闭时从注册表移除其水位。设剩余活跃快照的最老水位为 `O`。
对任意键，任意活跃快照只能看到「≤ 其水位的最新条目」，而所有水位 ≥ O，
故版本 ≤ O 的条目里只有最新的那一条仍可能被读到，其余永不可见，可删；
版本 > O 的条目可能对较新的快照可见，必须保留。若无活跃快照，只留最新条目。
**为什么按最老版本**：可见性集合由所有活跃水位的并集决定，而最老水位是
「最保守的读者」——任何对最老快照不可见的旧条目，对更晚的快照也不可见
（它们看到的是同一条或更新的条目）。因此两个重叠快照时，只要最老的还开着，
旧值就必须保留；只有两个都关闭（注册表为空）后保留值才全部释放，计数归零。

## 3. 导出顺序无关性

导出结果是「键→快照可见值」的集合，该集合由快照水位唯一确定，与遍历顺序无关。
分块内容会随顺序变化，因此清单的**总校验和**定义为逐条目 CRC32(key‖0xFF‖value)
的 **XOR**——异或满足交换律与结合律，对顺序不变。故字典序与逆序两次导出，
内容集合相同、总校验和相同。而续传要求字节级可复现，故默认遍历顺序固定为
字典序，同一快照 + 同一块大小 ⇒ 分块边界与每块字节完全确定。

## 4. 文件格式与截断分类

文件 = 若干块 + 尾部清单。

- 块：`BLCK`(4) + 块号(4) + 载荷长(4) + 载荷 + CRC32(载荷)(4)。载荷为
  条目序列：`klen(4) key vlen(4) value`。
- 清单：`MANF`(4) + JSON 长(4) + JSON + CRC32(JSON)(4)。JSON 含快照版本、
  块大小、键数、总校验和、每块 {块号, 偏移, 长度, CRC}。

校验器从头顺序解析，任一截断点落入的区间唯一确定分类：

- 截在 `BLCK` 头（含头的部分前缀）→ 块头不完整 `ErrHeaderIncomplete`；
- 截在载荷内 → 块体不完整 `ErrBodyIncomplete`；
- 截在块尾 4 字节 CRC 内（载荷完整但 CRC 残缺/不符）→ CRC 不匹配 `ErrCRCMismatch`；
- 截在清单区（`MANF` 前缀、JSON、清单 CRC）→ 清单不完整 `ErrManifestIncomplete`。

块魔数 `BLCK` 与清单魔数 `MANF` 首字节即不同，故 1 字节残余也可判定归属。
四类错误均为哨兵错误，可用 `errors.Is` 区分。结构解析通过后再比对块号序列：
缺失报 `ErrBlockMissing(n)`、错位报 `ErrBlockOutOfOrder(n)`，指名块号，
不只报总校验和。最后重算各块 CRC、条目 XOR 总校验和并与清单逐项比对。

## 5. 续传与快照过期

清单记录每块偏移，续传 = 校验快照仍有效后，把文件截到第 k 块偏移，用同一
快照重放分块生成器，跳过前 k 块、续写其余块与清单。因第 3 节的确定性，
结果与一次性导出逐字节相同。

**为什么快照过期必须拒绝续传**：续传的正确性依赖「前 k 块」与「后续块」来自
同一水位。快照关闭后其保留值被释放，存储里只剩当前最新值；若此时用当前
数据续写，拼出的文件前半是时刻 T1、后半是时刻 T2，是跨时刻的不一致快照，
且清单上的版本号会撒谎。因此续传前检查快照未关闭且水位等于清单版本，
否则返回可判定错误 `snapshot.ErrClosed`；已导出的部分文件保留不动，
便于用仍存活的其他快照或重新导出处理。关闭与导出竞争的语义：导出每次
读键都经快照句柄，关闭后后续读返回 `ErrClosed`，导出以错误终止，
不会把半截文件当成完整产出（清单尚未写出，verify 必报清单不完整）。

## 6. 资源约束

- 保留值计数 = 非最新条目数，由 `store` 在 `Put`/关闭 GC 时维护，≤ 改写键数。
- 导出每键恰好一次 `GetAt`，读取计数器精确等于键数（列键不计数）。
- 导出按块流式写出：先凑满一块再落盘，驻留峰值 = 最大一块的字节数，
  计数器记录峰值，断言 ≤ 块大小（单条目大于块大小时以该条目为界）。
