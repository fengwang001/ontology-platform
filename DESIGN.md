# 设计推导

## 1. UTF-8 非法单元

首字节决定期望长度 `n`，随后只收 `80..BF`。收满 `n` 字节后一次性校验最短形式、代理区和上界：合法则输出一个标量；非法则每个字节都是一个非法单元。未收满时遇到非续字节或流结束，已收的连续前缀只算一个单元；前者是非法字节，后者在前缀仍可能合法时是截断。

因此 `F0 90 80 41` 只收满 3 个续字节就遇 `41`，整个前缀替换一次；`E0 80 80`、`ED A0 80`、`F4 90 80 80`、`C0 AF` 均已收满，校验失败后逐字节替换；`E2 82` 在流结束时仍是可合法前缀，替换一次；独立 `80` 各替换一次。

| 首字节 | 第二字节合法区间 | 非法时吞掉字节 |
|---|---|---|
| `C2..DF` | `80..BF` | 第二字节非法/缺失：1 或 2 |
| `E0` | `A0..BF` | 收满失败按 3；未收满按前缀 |
| `E1..EC` | `80..BF` | 收满失败按 3；未收满按前缀 |
| `ED` | `80..9F` | 收满失败按 3；未收满按前缀 |
| `EE..EF` | `80..BF` | 收满失败按 3；未收满按前缀 |
| `F0` | `90..BF` | 收满失败按 4；未收满按前缀 |
| `F1..F3` | `80..BF` | 收满失败按 4；未收满按前缀 |
| `F4` | `80..8F` | 收满失败按 4；未收满按前缀 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

## 2. 流式缓存

UTF-8 最长 4 字节，读到首字节后最多再缓存 3 字节；UTF-16 自动字节序最多缓存 1 字节，代理最多再缓存一个代码单元。硬上限取 3。每个字节只喂给状态机一次；第二字节落入非法区间时标记前缀已死，但仍继续区分“收满”和“被非续字节打断”。

## 3. 并行切点

`par` 从候选切点最多回看 3 字节：若切点被某个首字节的期望单元覆盖，就把段起点移到该单元之后；否则保持原切点。查找只检查至多 4 字节。于是 `C3 | 80 80` 中 `C3 80` 归上一段，第二个 `80` 归下一段；若切点在独立续字节中间则不跨越。各段覆盖区间首尾相接，非法单元不会被拆给两个转码器，计数与单线程相同。

## 4. 输出上限断点

转码器按完整标量写输出；UTF-16 在写代理对前预留 4 字节。`Write` 的常规返回值包含已经接管的当前输入字节（可能在缓存中）；触发上限时返回的 `n` 只包含此前已完整输出标量对应的字节，缓存前缀通过 `Pending()` 交给新实例。统计中的已消费字节在安全点结算，因而不会输出半个标量或半个代理对。
