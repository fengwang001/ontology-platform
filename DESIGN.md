# 设计推导

## 1. UTF-8 非法单元切分

规则（自顶向下应用）：首字节 b 决定期望长度 L=2/3/4。先判「第二字节合法区间」；区间不符则**首字节单独成一个非法单元**（长度 1），其余字节各自重新解析；区间相符则继续看后续字节是否为 10xxxxxx：一旦遇到非续字节，该非续字节**不被吞**，重投为新首字节；已吞的前缀产出一个 FFFD。80..BF、C0/C1、F5..FF 长度 1。流结束时残留的合法前缀整体产出一个 FFFD。

推导：F0 90 80 41 中 F0 的二字节区间是 90..BF（通过），第 3 字节 80 续接，第 4 字节 41 非续且重投，故 1 个 FFFD + 41。E0 80 80 中 E0 要求 2 字节为 A0..BF，80 不符→E0 单独非法，80、80 各自非法，共 3 个。ED A0 80 中 ED 要求 80..9F（拒代理），A0 不符→ED 单独，A0、80 各自非法。C0 AF 中 C0 永远非法（长度1），AF 非法。F4 90 80 80 中 F4 要求 80..8F（防越界），90 不符→4 个单字节。E2 82 区间 80..BF 通过后遇 EOF，整个前缀 1 个 FFFD。

| 首字节 | 第2字节区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 不匹配：1；匹配后坏续字节：前缀（2） |
| E0 | A0..BF | 同上，长度 1 或前缀 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F | 同上 |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF | 同上 |
| F1..F3 | 80..BF | 同上 |
| F4 | 80..8F | 同上 |
| 80..BF | 无 | 1 |
| C0 C1 F5..FF | 无 | 1 |

## 2. 缓存上限与即时判定

最长字符 4 字节，解码器缓存最多 3 字节（F0 90 80 等待第 4 字节），外加最多 3 个待重投续字节（判定前缀非法时已吞的续字节要逐个重投），故缓存硬上限 = 6。可立即判定前缀非法的时机：第二字节落区间外（首字节立刻发射，不等流结束）；后续出现非续字节（前缀立刻发射，非续字节重投）。只有「仍在等续字节」才必须跨 Write 等待，因此合法字符的中间切分点绝不提前出 FFFD；仅在 Close 时把残留前缀判为截断/FFFD。

## 3. par 切点对齐

段 k 从切点 c 回退最多 3 字节寻找覆盖 c 的序列起点（4 字节字符的首字节满足 p≤c<p+L，L 由首字节算出，非法/截断前缀 L 取到数据末尾）：找到则该段起点 = p（序列完整归前一段，本段靠喂入前瞻最多 3 字节自然判坏后丢弃越界单元）；找不到则从 c 开始。每段从起点喂到段尾+3 字节，仅发射起始偏移落在本段 [c,next) 的单元；末段喂到 EOF 并正常 Close。

C3|80 80：C3 在前段发射为完整字符；后段 80、80 各自非法，共 1 合法+2 非法，与单流相同。C3|EOF：C3 归最后一段并 Close→截断/FFFD。非法单元同理：单元起点决定归属，任何段不会只看到单元的后半段，所以 FFFD 个数逐字节一致。UTF-16 对齐到偶偏移，前瞻 2 字节，孤立高代理后跟非低代理时该非低代理重投为新单元。

## 4. 上限断点

Write 在喂每个字节前检查「输出当前事件是否超限」（事件大小已由解码器状态确定：1..3/4 字节，UTF-16 为 2/4），超限立即停下且不喂该字节；返回 n = 已让解码器终结的输入字节数（含被吞字节，不含切分缓存中尚未终结的字节，也不含未喂入字节）。调用方从原输入第 n 字节续传新实例，输出按标量边界拼接。事件大小在发射前确定，故不会出现半个标量/半个代理对。
