# Properties 解析/回写设计要点

## 续行接上空白行或空行（语义 3 的推导）

对照 `java.util.Properties` 的读入循环：某物理行在剥离尾部空白后以**奇数个** `\` 结尾时，
这些 `\` 与行终止符一并丢弃，续读下一物理行；续入行先剥掉一段前导空白（连续的空格/制表），
再无条件拼到当前逻辑行上——剥空白与拼接对任何续入行都发生，不看其内容。

故续入行只含空白或为空时：它的净贡献是空串，逻辑行内容不增长；若它仍以奇数个 `\` 结尾，续链继续。
它不是"被当作不存在"，而是"被吃掉一次前导空白后什么也没加上"。
例：`k=a\` + `\n   \n` + `b` 得 `k→ab`。
与首部剥离规则的关系：注释/空行判定只针对逻辑行的第一个物理段首字符，续入行的首字符永不参与判定，
所以空白续行不会产生空逻辑行，续入行中的 `#`/`!` 也只是普通字符（`k=a\` + `\n#b` 得 `k→a#b`）。

## Store 转义规则（语义 6 的推导）

从 Load 的判定反推必须转义的字符：

- 键：`\` 必须转义（否则启动转义）；空白必须转义（行首空白在逻辑行层被剥，键内未转义空白会提前结束键）；
  `=` `:` 必须转义（任意位置都可能充当分隔符，`k==v` 即为例证）；
  `#` `!` **仅当位于键首字符**时转义（注释判定只看逻辑行首第一个非空白字符，键中间的 `#!` 安全）。
- 值：`\` 必须转义；**开头空白**必须转义（分隔符确定后会跳过值的一段前导空白）；
  值中间及尾部空白不必转义（跳过动作只发生一次、在值首；`key = value  ` 尾部两空格本就保留）；
  `#!` 不必转义（值阶段从不做注释判定，`k=v # x` 原样保留）；`=` `:` 不必转义。
- 两边相同：`\n \r` 转短转义，否则切断物理行；`\t \f` 转义，避免被卷入首部空白剥离；非 ASCII 原样 UTF-8 输出。
- 空值写 `key=`；空键合法，写 `=value`。

## 七条语义与测试函数对照
