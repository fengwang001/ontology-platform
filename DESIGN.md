# 带前缀压缩的有序字符串字典 — 设计

## 1. 块格式（小端，uint32 长度/偏移）

```
header(24B): magic "ONDIC"=5, ver=1, k=u16, n=u32,
             restartCount=u32, dataLen=u32, reserved=u32(=0)
entries:   每条 varN: shared(u32) diffLen(u32) diff 字节
restarts:  restartCount 个 u32（相对 data 起点的条目偏移）
crc:       u32，CRC32-IEEE，覆盖 header 起至 restarts 末尾
```
重启点 shared 恒为 0。K=条目间隔（第 0 条及每 K 条为重启点）。

## 2. 重启点间隔 K 的权衡

- 取第 i 条：从其之前最近的重启点开始顺序解压，至多 K 次。
- 空间：每 K 条多存一个 u32 偏移 + 重启条目丢掉共享前缀（多存约平均前缀长 P 字节）。
  每条摊薄开销 ≈ 4/K + P/K 字节；K 越小压缩越差、取值越快；
  K 越大压缩越好、取值与区间内顺序比较越慢。K=1 等价不压缩。
- 故取值代价 O(K)，重启表开销 O(n/K)。

## 3. 为什么二分只能在重启点上

非重启条目只存「共享长度+后缀」，脱离前缀不可独立解出。
因此先在重启点数组（其值可 O(1) 独立解码）上二分，
比较次数 ⌈log2(R)⌉，定位到不超过 K 条的区间，再从该重启点
顺序解码比较（≤ K 次），总比较次数 ≤ ⌈log2(R)⌉ + K。
（实现中每次顺序比较前会重读重启锚点值，常数 ≤4 倍，
故满足上界 4⌈log2(R)⌉ + K。）
未命中返回应插入位置（首个更大值的全局序号，全小于则返回 n）。

## 4. 有序性检查的位置

构建时（写入每条之前）与 verify 时检查：要求严格递增
`s[i-1] < s[i]`（字节序）。在解压阶段再做一次防御：
- shared 必须 ≤ 前一条长度，否则 ErrSharedLen（不得越界）；
- shared 可为整条长度（重复），但严格字典在构建期拒绝重复；
- 后一条是前一条前缀（如 "ab"→"a"）违反有序性，构建即拒绝
  并报告位置；恢复模式下该半截/乱序条目被丢弃，输出仍严格有序。

## 5. 前缀长度按字节

共享长度按字节（编码无关、与磁盘一致）。
café（5B）/cafés（6B）：共享 5；按码点算则共享 4。
故按字节，多字节 UTF-8 天然正确，无需按 rune 对齐。

## 6. 故障分类与恢复

截断按解码器发现顺序分类：header 不完整(ErrHeader) /
重启表不完整(ErrRestart) / 条目不完整(ErrEntry) / CRC 不匹配(ErrCRC)。
纯截断在到达 CRC 前必先遇到长度不足，故不产生 ErrCRC；
ErrCRC 由翻改尾部 CRC 字节触发。另有 ErrSharedLen（前缀长度撒谎）、
ErrRestartOffset（重启偏移指向条目中间）。
恢复：忽略重启表从块首顺序解码，严格保留严格递增的最大前缀。
重启偏移错位时回退块首顺序解压，结果正确并置索引失效标记。
所有错误用 errors.Is 区分（哨兵错误）。

## 7. 并发与字典文件

构建期块不可见（append 后原子登记）；只读快照多 goroutine
安全。dict 文件 = header + 每块：blockLen(u32) + block 字节。

## 8. 边界

空字典、单条、空串首条、全同前缀、全无公共前缀、K=1、K>n
均需正确；前缀扫描先二分块首再顺序读块，只解压命中块。
