# DESIGN：只追加日志的段索引与范围回放

## 1. 盘上格式（全部小端，仅标准库）

- 事件：`Event{Seq uint64; Payload []byte}`，编码 `[seq:8][len:4][payload][crc32:4]`，
  CRC32(IEEE) 覆盖 seq、len、payload。
- 段文件（`.seg`）头 24 字节：`magic(4)="OSEG"|firstSeq(8)|count(8)`。
  首序号不可变；count 在每次 append 后就地改写（定长偏移写，崩溃只可能旧/新不会撕裂）。
- 索引文件（`.idx`，每段一个）头 16 字节：`magic(4)="OSPX"|firstSeq(8)`，
  其后锚点项 16 字节：`seq(8)|offset(8)`，offset 为该事件整条记录在段内的起始字节。
- 段命名 `000001.seg`，序号在段间连续：`本段 firstSeq + count == 下段 firstSeq`。

## 2. 为什么定位锚点必须是「不大于 from 的最大锚点」

锚点把事件流分成区间，锚点本身指向其锚定事件的起始字节，只能从该字节向后顺序扫。
若选「最接近 from」的锚点，当它落在 from 之后时，向前没有锚点可用、无法倒扫，
必然漏掉该锚点之前、from 之后的事件。因此只能选 `a.seq <= from` 的最大锚点，
从它开始顺序丢弃 `seq < from` 的事件。锚点间隔 N 时，跳过数严格 `< N`。
三种位置：from==锚点（跳过 0，直接命中）；两锚点之间（跳过 1..N-1）；
from 小于首锚点（无锚点可用，从段头之后偏移 0 扫起，跳过 `< N`）。

## 3. 索引可丢弃、可逐字节重建

索引是纯加速结构：锚点的 seq 与 offset 都能从段重新得到——顺序读每条记录可得 seq，
写入位置即读前的文件偏移。故索引头只放段里已有的 firstSeq，不存放任何仅写入时
可知的信息。重建算法：读段头后，从偏移 24 起逐条解码，第 0、N、2N… 条写锚点。
重建结果与原索引逐字节相同（确定性、无时间戳/无填充）。

## 4. 回放 [from,to]

按文件名排序段；firstSeq<=from 的最后一段为起点，后续段依序读到 firstSeq>to 为止。
起点段用 `floor(from)` 锚点定位；锚点偏移处解不出合法记录（长度越界/CRC 不符）即
判定索引失效，报告 `IndexStale=true` 并回退到段头顺序全段扫描。
边界语义：from>to 报错；from<最小序号从头开始（不报错）；to>最大序号放到末尾；
空日志合法；payload 为空字节串合法（len=0）。

## 5. 损坏分类与修复

读记录时按剩余字节分类：`<24` 头不完整（段头）；`8+4<=x` 之后 `<12` 长度前缀不完整；
长度声明 `L` 超出剩余 `<L+4` 事件体不完整；CRC 不符（含 CRC 尾被截断：此时无下一条
且长度虽够但校验失败）。四类均为哨兵错误，`errors.Is` 可区分。
修复：扫出最大可读前缀，截断残尾，把段头 count 改写为实际条数；重建该段索引；
检查相邻段 firstSeq 连续性，缺口报告为 `[prevFirst+prevCount, nextFirst-1]`。

## 6. 并发与读取上界

append 为「追加数据 → fsync → 就地更新 count」；回放独立打开文件、不看 count，
读到 CRC/长度不合法的尾部即停，因此只可能观察到一致前缀，不见半条事件。
读取字节上界：起点段从锚点偏移开始读，字节数 <= 区间内事件编码总长 + 一个锚点
区间（128 条）的编码量；后续段整段都在区间内。
