# `.properties` 解析器设计推导

## 第 3 条：续行后接「只含空白 / 为空」的物理行

推导依据：`java.util.Properties` 的物理→逻辑行拼接算法（`LineReader.readLine`）为
「读物理行 → 若行尾反斜杠成奇数则丢弃该反斜杠、剥掉下一物理行前导空白后继续拼接」。
剥前导空白的循环在遇到行结束（CR/LF/EOF）时才停止；因此：

- 下一物理行为空：没有任何字符可拼，且该行行尾**没有**反斜杠，续行链终止；
- 下一物理行全是空白：空白被前导剥离循环全部吃掉，同样无字符可拼、无行尾反斜杠，链终止。

结论：空白/空续行对逻辑行**贡献零个字符**，并终止续行链；它后面的下一行是一条全新逻辑行。
这正是「续行首部空白被剥掉」的极端情形——当整行都落在「首部空白」范围内时，整行被剥光。

## 第 6 条：Store 的最小转义推导

Load 时每个逻辑行的形态为：`[键前空白] 键 [键值间空白] [=|:] [值前空白] 值`。
回写必须保证重新切分得到同样的键与值，据此逐项推导：

- 反斜杠 `\`：它是转义引导符，键和值中都必须写成 `\\`，否则会吞掉下一字符。
- `\n`/`\r`/`\t`/`\f`：换行行会切断逻辑行；制表/换页属空白会在分隔符两侧被吃掉，
  故写为 `\n` `\r` `\t` `\f`（键值均如此）。
- 键的首字符为 `#` 或 `!`：行首（剥去键前空白后）的 `#`/`!` 使整行变注释，必须转义为 `\#`/`\!`。
  值里的 `#`/`!` 不在行首，永远不是注释，无需转义。
- 键中的 `=`、`:`、空白：未转义时会被识别成键值分隔符，必须转义为 `\=`/`\:`/`\ `。
  值中的 `=`/`:`/`#`/`!`/中间空格都不参与切分，原样输出即可。
- 值的**首字符为空格**：分隔符之后的前导空白会被剥除，故必须写成 `\ `；
  值中间、尾部的空格不属于前导空白，Load 原样保留，不必转义。
- 空键、空值：`=v` 即空键，`k=` 即空值，直接输出 `=v` / `k=`。
- 非 ASCII：Load 按 UTF-8 读取，直接原样输出即可无损往返，不写成 `\uXXXX`。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | （最后补齐） |
| 2 注释 | |
| 3 续行与空白续行 | |
| 4 转义与 `\u` 错误行列号 | |
| 5 重复键覆盖与首次顺序 | |
| 6 Store 特殊字符转义 | |
| 7 1000 组随机往返 | |
| 复杂度：计数器 ≤ 2N | |
