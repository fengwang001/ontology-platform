# DESIGN — 并行任务图调度与失败传播

仅标准库，状态在进程内存。包：`graph`（建图/环检/分层）、`fail`（状态与错误）、
`sched`（调度器：并发上限、就绪队列）、`exec`（执行+panic 恢复+结果）、`report`（报告）、
`cmd/demo`。

## 1. 四类最终状态的判定

任务状态机：`Pending → Ready → Running → {Success, Failed}`，旁路收敛到 `Skipped/Canceled`。

1. **Success**：函数在 ctx 未取消前返回 nil。终态，任何迟到写回不得覆盖。
2. **Failed**：函数真正执行且返回非 nil（含 panic 转换出的错误）。报告保留原始 error。
3. **Skipped**：**从未开始**。当其祖先（可达上游）中存在 Failed 时，调度器不再入队该任务。
   跳过原因必须指向**最早失败的根**：在所有能到达它的 Failed 任务里选 ID 字典序最小者
   （该任务本身没有失败的直接祖先，即根失败）。故 A→B→C 中 A 失败，C 的原因是 A，而非 B。
4. **Canceled**：**已经开始执行**，但在其返回前全局失败传播触发、ctx 被取消。
   判定依据是「是否已从就绪队列取出并启动」：启动过=Canceled，没启动=Skipped，二者由此严格可分。

**取消后写回丢弃**：任务以终态（Canceled/Skipped/Failed/Success）写在共享 state 上；
worker 拿到返回值后用 `CompareAndSwap(Running, ...)` 提交，状态已被传播器改写则丢弃，
因此慢任务即便忽略 ctx 稍后才返回，也不能改变 Canceled 状态与原因。

## 2. 两种传播模式

- **FailFast（默认）**：首个失败即 cancel 全局 ctx。正在运行的任务：配合 ctx 的以
  ctx 错误结束→Canceled（原因记为触发取消的失败任务）；不配合 ctx 的慢任务最终结果被丢弃。
  尚未启动的任务全部 Skipped。
- **BestEffort**：不 cancel ctx。已运行/可运行的分支继续跑完；仅当任务的祖先含 Failed
  （其任一前置最终是 Failed/Skipped）时，在「该前置完成」时被标记 Skipped，原因同样回溯到
  最早失败根。因此不依赖失败分支的任务保持 Success/Failed。

## 3. 并发模型与复杂度

- 固定 P 个 worker 从**就绪最小堆**（同时就绪按 ID 升序弹出）取任务；初始入度为 0 者入队。
- 事件驱动，无全图扫描：任务完成仅更新其后继入度，入度归零才做一次就绪判定。
  就绪判定次数 ≤ 入队事件数 ≤ 节点数，边遍历恰好 2E（建图/后继更新），合计 ≤ 4(V+E)。
- 峰值运行数由 worker 信号量/常驻 worker 数 P 天然限定为 ≤ P；记录历史最大值。
- dispatcher 与 P 个 worker 在所有路径结束后退出，无 goroutine 残留。

## 4. 确定性

报告按 ID 字典序输出；就绪弹出按 ID 升序；最早失败根用「最小 ID」消歧。
完成顺序与随机延迟只影响时序，不影响终态与原因；边插入顺序不影响邻接（邻接按 ID 排序），
故同图同函数报告逐字节相同。

## 5. 故障注入

- 环：执行前 DFS（三色）检出，自环即环；返回一条真实闭合路径，每条边均来自输入。
- panic：worker 内 recover，转成带 panic 标记的 Failed，错误含原始信息。
- 多失败：状态按任务独立记录，全部保留；各自回溯本分支最早失败根。
- 取消后写回：CAS 提交保证丢弃。
四类状态用枚举区分；panic 错误可用 `errors.Is(err, ErrPanic)` 判定。

## 6. 边界

空图→空报告；单任务；全无依赖；链式 1000 层用迭代不入栈；重复加边幂等；
引用不存在节点→可判定错误；P=1 退化为串行，报告与并行一致。
