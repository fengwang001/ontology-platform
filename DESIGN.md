# 消息头部集合解析、规范化与安全回写器 — 设计推导

## 0. 总览

五个包，依赖单向：`token`（字符合法性）← `fold`（折行展开/重折）← `listval`（列表切分/合并）；
`token` ← `policy`（每头语义策略）；`headers` 依赖以上全部，对外提供有序多重映射。
解析管线：字节流 → 按 CRLF 切行 → `fold` 展开续行 → 逐行切分 `名字: 值` → `token` 校验
→ 名字规范化、值去首尾空白 → 入有序多重映射并维护名字索引。回写为其逆过程。

## 1. 为什么"名字与冒号之间的空白"必须判错（对应要求 3）

RFC 9112 §5.1 规定 field-name 后必须紧跟冒号，名字与冒号之间出现空白是
**请求走私（request smuggling）的经典攻击面**。推导如下：

- 若本解析器"宽容地去掉"空白，把 `X-Forwarded-For : 1.2.3.4` 规范化为
  `X-Forwarded-For: 1.2.3.4`，而链路上另一个组件（代理、WAF、后端）把同一行
  判为**语法错误并整行丢弃**，或把整行当作**上一个头部的值的一部分**，
  则两个组件对"这条头部是否存在"产生分歧。
- 攻击者利用分歧：让安全设备看到"没有这条头"，让后端看到"有这条头"，
  即可绕过基于头部名的访问控制（如伪造 `X-Admin: true`）。
- 同理，首行即续行若被宽容地"当作无名头部"，不同实现对它的归属判断也不一致。

结论：凡出现"名字与冒号之间有空白"或"首行续行"，一律返回语法错误并拒绝整个
输入，不做任何修复性猜测。两个实现只有在"同样拒绝"时才不可能产生语义分歧。

## 2. 禁止字符集合与"拒绝优于静默删除"（对应要求 4）

**禁止集合的推导**：回写格式是 `Name: value\r\n`，接收方按 CR/LF 切行、按冒号
切名值。因此值中任何能改变"行结构"或"不可见地改变语义"的字符都必须禁止：

- `CR (0x0D)`、`LF (0x0A)`：直接制造新行 → 头部注入，绝对禁止。
- `NUL (0x00)`：许多 C 语言实现的下游按 NUL 截断字符串，可隐藏注入后缀。
- 其余 C0 控制字符（0x01-0x08、0x0B、0x0C、0x0E-0x1F）与 `DEL (0x7F)`：
  不可见、无语义，但可被某些终端/解析器解释为行终止或丢弃，造成分歧。
- 允许：HTAB（0x09，折行空白本身）、可见 ASCII（0x20-0x7E）、
  obs-text（0x80-0xFF，按字节透传，不做 UTF-8 解释——解释反而引入分歧）。

**为什么静默删除更危险**：若把 `a\r\nEvil: 1` 静默清洗为 `aEvil: 1` 再写出，
调用方以为写入失败/成功的语义与实际字节不一致，且清洗规则一旦与下游的清洗
规则不同（下游可能删 `\r` 留 `\n`），分歧依旧存在。**可判定地拒绝**把
"值不合法"这一事实显式暴露给调用方，调用方必须处理错误分支，不存在
"以为没写出去其实写出去了"的窗口。拒绝时集合状态零变化，可安全重试。

**测过的绕过变体与拦截点**（全部在 `headers.Set/Add` 与解析两处拦截）：

| 变体 | 拦截点 |
| --- | --- |
| `a\r\nEvil: 1`（CRLF 注入） | 值校验发现 CR/LF，拒绝 |
| `a\nEvil: 1`（裸 LF） | 同上 |
| `a\rEvil: 1`（裸 CR） | 同上 |
| `a\x00Evil`（NUL） | 控制字符检查，拒绝 |
| `a%0d%0aEvil: 1`、`a%0AEvil`、`a%00Evil`（百分号编码绕过） | 值校验识别可解码为禁止字节的百分号编码（%00-%1F、%7F，大小写不敏感），拒绝；其余百分号序列（如 %41、%25）按普通可见字符透传 |
| 其他编码（实体、Unicode 转义等） | 头部层从不解释这些编码；任何解码后的控制字符在 Set 时仍被值校验拒绝 |

## 3. 引号内逗号不切分（对应要求 5）

列表型头部按逗号切分，但 quoted-string 内的逗号是**数据**不是分隔符。
不实现这条规则会解析错的典型例子：

- `If-Match: "a,b", "c"` —— ETag 是引号字符串，逗号在标签内部；
  朴素切分得到 `"a` / `b"` / `"c"` 三个非法 ETag，条件请求全部误判。
- `Cache-Control: no-cache="Set-Cookie, Set-Cookie2"` —— 参数值里的逗号。
- `WWW-Authenticate: Digest realm="x, y", nonce="n"` —— 认证参数。

转义引号 `\"` 必须正确处理：`"a\"b,c"` 中 `\"` 不结束字符串，其后的逗号
仍不切分。切分出的每项去首尾空白；**空项语义（文档约定）**：`a,,b` 中的空项
在切分时被**丢弃**（等价于 RFC 9110 对列表空项"必须忽略"的规定），合并时
用 `", "` 连接，因此 `a,,b` 往返后变为 `a, b`——语义等价（空项无意义），
字节不必相同。往返测试断言"切分→合并→再切分"得到相同项序列。

## 4. 回写字节等价与幂等性（对应要求 6）

- 输入已规范（名字为规范形态、无多余空白、折行符合宽度、CRLF 行尾）时，
  解析→回写逐字节等于输入：因为解析只做"校验+记录"，不改动任何字节内容，
  回写按原顺序原样拼回。
- 输入不规范时，"正确输出"定义为**规范化函数 N 的不动点**：名字转为
  首字母与连字符后大写、值去首尾空白、续行展开后按配置宽度重折、
  统一 CRLF。幂等性论证：N 的每个子操作都是幂等的（大小写映射作用于
  已规范名不再变化；去空白作用于已无首尾空白的值不再变化；重折作用于
  已按同一宽度折行的输出不再变化——因为折行点选择是宽度的纯函数），
  幂等函数的复合仍幂等，故 N(N(x)) = N(x)。测试以"解析→回写→再解析→
  再回写，两次字节相同"验证。

## 5. 索引结构与查找复杂度（对应要求 10）

- 结构：`entries []entry` 保序存储全部条目（出现顺序即切片顺序，增删改
  只动这个切片，**顺序语义与索引无关**）；另维护
  `index map[string][]int`，键为规范化后的名字，值为该名字在 `entries`
  中的下标列表（升序）。
- 为什么索引不破坏保序：索引只是"名字 → 下标"的旁路指针，所有按序遍历
  （回写、Dump）都只走 `entries`；按名查找用索引拿到下标后仍按下标升序
  返回值，等价于线性扫描过滤同名项的结果。
- 复杂度：按名查找的比较数 = 该名字的**出现次数 k**（哈希定位视为 O(1)，
  不计入条目比较），与总条数 N 无关。最坏情况上界：全部 N 条同名时为
  O(N)，这是多重映射的固有下界（要返回 k 个值就得读 k 次）；异名查找为
  O(k) ≤ O(N)。非导出计数器 `cmpCount` 记录每次查找实际比较的条目数，
  测试以 N=50 / N=5000（各含 1 条目标名）对照，证明比较数不随 N 增长。
- 删除条目时采用"标记删除 + 惰性重建"：置空标记并作废索引（置 nil），
  下次按名查找时重建。重建是 O(N) 但摊还到写操作上，只读路径稳定 O(k)。

## 6. 并发与截断（对应要求 7、11）

- 截断：解析器按状态机推进（读名字 → 读冒号 → 读值 → 折行 → 终止空行），
  任一阶段遇到输入结束即返回带阶段与头部名的错误；错误类型可判定
  （`errors.As` 到 `*headers.ParseError`，含 `Stage` 与 `Header` 字段）。
  解析失败不返回半截集合，已解析的临时状态随函数返回一并丢弃。
- 并发：所有只读方法（Get/GetAll/Len/Count/Bytes/Normalized/Stats）持有
  `sync.RWMutex` 读锁，可并发；写方法（Set/Add/Del/解析入口）持写锁。
  文档约定：**写操作本身由锁保证安全，但写后读到的快照以锁序为准**；
  调用方若需"读-改-写"原子性须自行外加锁。
