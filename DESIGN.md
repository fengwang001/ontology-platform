# Quoted-Printable 设计说明（RFC 2045 子集）

## 1. 行末空白推导

解码规则：硬换行（裸 `\r\n`）解码为 `\n`，而软换行 `=\r\n` 整体删除。
若行末的空格/制表符原样保留（如 `a \r\n`），解码器无法区分它本来是
"空格后接硬换行"还是被行折行规则留在行尾的空白，且常见传输层会剥除
行尾空白。因此规定：空格(0x20)/制表符(0x09)在"紧跟 `\n`/`\r\n` 或
输入结束"时必须写成 `=20`/`=09`；不在行末时（如 `"a b"`）原样输出，
这就是最小转义。

## 2. `=XX` 不拆行推导

`=XX` 对解码器是不可分割的原子：若在 `=` 与 `X` 之间插入 `=\r\n`，
解码器会先看到裸 `=`，报"无效转义"。因此每个输入字节先确定其输出
记号（1 或 3 字符），当 `列数+记号长度 > 76` 时，**先**整体插入
`=\r\n`（列归 1），再写记号。阈值等价于：列到 75 时下一个记号必须
折行，从而每行（含软换行的 `=`，计入 76）绝不超过 76。

## 3. 软换行前的空格

**不转义。** 软换行前的空格不是行末空白：解码器在该处删除的是
`=\r\n`，空格属于下一逻辑行的开头，`a =` 中的空格解码后仍是空格，
语义无歧义；而硬换行前的空格才是行末空白，必须 `=20`。

## 4. 往返等式

设 `N(x)` 为把裸 `\n` 与 `\r\n` 规范化为 `\r\n`、单独 `\r` 保留
（普通字节）的结果，则对任意字节串 x：

    Decode(Encode(x)) == N(x)

当 x 不含裸 `\n` 时即为 `Decode(Encode(x)) == x`。

## 5. 幂等与最小转义

编码器只对三类字节转义：`=`、33–126 之外（含 0x20/0x09 仅在行末）、
行末的 0x20/0x09；每个记号只输出必要的一次，故为最小转义。严格
解码器接受的 y 若含多余转义（如 `=41`），`Encode(Decode(y))` 只在
这些位置把 `=41` 规范化成 `A`（折行位置因本编码器固定阈值而一致）。

## 6. 语义—测试对照表

<!-- TABLE -->
