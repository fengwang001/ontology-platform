# DESIGN — 可撤销批量重命名与冲突解析器

模块：`ontology`（仅标准库）。包划分：`name`（命名空间）、`plan`（冲突检测与排序）、
`cycle`（破环）、`apply`（执行+日志）、`undo`（逆序撤销）、`cmd/demo`（演示）。

## 1. 模型

命名空间是名字的集合（`map[string]struct{}`），重命名 `old->new` = 删除 `old`、加入 `new`。
**覆盖**定义为：执行某步时目标名 `new` 已存在于集合中 —— 这会静默吞掉一个名字，必须避免。
合法性：任意字符串都是合法名字（空串合法；`/`、`\` 等按普通字符处理），`name.Valid`
恒为真，校验点保留为单一入口。

## 2. 排序规则的推导

对请求 `r: old->new`，若执行 `r` 时 `new` 仍存在则覆盖。`new` 何时存在？两种情形：
(a) `new` 在命名空间里且没有任何请求把它搬走 —— 这是静态冲突（见第4节第一类），直接拒绝；
(b) `new` 是另一个尚未执行的请求 `s` 的 `old` —— 必须先执行 `s` 把名字腾出来。

由此得到依赖边：`r` 依赖于 `s` 当且仅当 `r.new == s.old`（`s` 先执行）。
每个 `old` 至多出现一次（第4节第三类保证），所以每个请求至多有一条依赖边，依赖图是
入度 <=1 的有向图，即若干条链汇入若干个环（函数图）。

无环部分用 Kahn 拓扑序：初始可执行集 = 所有「`new` 不是任何待定请求之 `old`」的请求；
每执行一个请求 `r`，唯一可能被解锁的是满足 `u.new == r.old` 的请求 `u`（用 `new->old`
反查表 O(1) 找到）。同层（同时可执行）按 `old` 字典序出队（小根堆），保证确定序。
例：`{a->b, b->c}` 中 `a->b` 的 `b` 是 `b->c` 的 `old`，故先 `b->c` 后 `a->b`。

## 3. 破环的通用做法与临时名数量论证

Kahn 结束后仍待定的请求满足：每个请求的 `new` 都是某个待定请求的 `old`。由于 `old`
唯一、`new` 唯一（第4节第二、三类），该子图中每点出度入度恰为 1，即若干个**互不相交的
简单环**。环上任何排序都无法开始：第一步的目标必被环上前驱占用。

做法：对每个环 `[n0->n1, n1->n2, ..., nk-1->n0]`（从环上字典序最小 old 起沿映射走得到，
保证确定性），借一个临时名 `tmp`，发出 `k+1` 步：

    n0->tmp,  nk-1->n0,  nk-2->nk-1,  ...,  n1->n2,  tmp->n1

正确性：`n0->tmp` 腾空 `n0`；随后逆序依次执行，每步目标恰是上一步刚腾空的名字；
最后 `tmp->n1` 时 `n1` 已被腾空。每步目标都不存在，零覆盖。

**临时名数量 = 环数**：充分性由上述构造（每环 1 个）。必要性：环上每个名字都被占用，
不借外部名字则环上任何一步都覆盖，故每个环至少需 1 个临时名；各环不相交，本实现为
每环各取一个临时名（步骤可独立撤销、日志可定位），总数恰等于环数。

临时名生成：候选 `.tmp-rename-0,1,2,...` 递增枚举，逐一与「命名空间 + 全部请求的
old/new + 已用临时名」谓词 `used` 校验，冲突则取下一个；候选无限，故必终止（调用方
也可设上限得到可判定错误），绝不靠覆盖「解决」冲突。

## 4. 四类冲突的判定条件

全部在 `plan.Plan` 中、任何修改之前完成（`Plan` 对命名空间只读）：

1. `ErrTargetExists`：`new` 在命名空间中存在，且 `new` 不是本批任何请求的 `old`。
2. `ErrDuplicateTarget`：两请求 `new` 相同，报错携带两个 `old`。
3. `ErrDuplicateSource`：两请求 `old` 相同，报错携带两个 `new`（重复旧名再多也在此拦住）。
4. `ErrSourceMissing`：`old` 不在命名空间中，报错携带该名。

四个哨兵错误用 `errors.Is` 区分。检查顺序：先批内（2、3，建表时顺带完成），后批外（4、1）。

## 5. 自环与边界语义

- 自环 `a->a`：**合法的无操作**。要求 `a` 存在（否则按第四类报错），通过校验后从计划中
  丢弃，不产生步骤、不算环、不引入临时名。
- 空请求集：合法，产生空计划与仅含头部的日志，撤销为无操作。
- 单请求：直接执行。空串名、`/` 分隔符名：普通名字，全流程无特判。

## 6. 复杂度论证

设请求数 `R`、涉及名字数 `N`。冲突检测：每请求常数次哈希表/集合查询（<=4 次）。
排序：建反查表 O(R)；Kahn 每请求入堆出堆各一次（时间 O(R log R)），但**查找次数**为
每请求常数次（入度计算 1 次、解锁反查 1 次）；破环每请求常数次。`plan` 内非导出计数器
`lookups` 记录全部命名空间查询与 map 查询，测试断言 `lookups <= 4*(R+N)`。

## 7. 执行、日志与撤销

`apply.Execute` 在 `name.Namespace.Transact` 的整把锁内执行全部步骤。并发语义定义：
**批量执行期间并发修改被阻塞至批结束**（测试验证）。每步执行后追加一条日志记录并落盘
到临时目录。第 k 步注入失败：在锁内把已执行的 0..k-1 步逆序改名回滚，删除日志，返回
错误 —— 命名空间逐元素复原。

日志格式（行式，便于截断分类）：

    {"v":1,"n":<总步数>}                      # 头部，n 用于报告不可撤销步数
    {"old":"a","new":"b"} <crc32-8hex>        # 记录 = JSON + 空格 + 对 JSON 字节的 CRC32

截断分类（`undo` 解析时，`errors.Is` 可区分）：

- `ErrHeaderIncomplete`：首行不完整或头部 JSON 无法解析；
- `ErrRecordIncomplete`：记录行 JSON 无法解析、缺 CRC、或完整记录数 < 头部声明的 n；
- `ErrCRCMismatch`：记录 JSON 可解析但 CRC 字段存在且校验失败（截断落在 CRC 数字内
  时触发）。

撤销按**最大可恢复前缀**：逆序撤销全部完整且 CRC 通过的记录；丢失的尾部记录、以及
与当前状态不符（源已不在/目标被占）而无法撤销的记录，都在 `Report.Unrecoverable`
中列出下标。撤销成功后删除日志，重复撤销时文件不存在 -> 无操作（幂等）。
