# LZ77 自定义流格式设计

## 流格式（全部字段为 LEB128 无符号变长整数；编码同 `binary.PutUvarint` 规则，超过 10 字节非法）

| 记录 | 布局 |
| --- | --- |
| 流头 | magic `'L','Z','Z','7'`(4 字节) + version=1(varint) |
| 字面量段 | tag=0(varint) + n(varint) + n 个明文字节 |
| 回指 | tag=1(varint) + dist(varint,≥1) + length(varint,≥0) |
| 刷新标记 | tag=2(varint)；承诺其前的全部输出可还原 |
| 流尾 | tag=3(varint) + 原文总长(varint) + FNV-1a64 校验和(varint)；其后不得再有字节 |

tag 是独立 varint。校验和按原文（解压结果）字节序计算。

## 第三节推导

### 1. 压缩与 Write 切法无关（语义 3、4）

压缩器维护未决数据 `buf`（全部未吐出的输入）与已确认历史 `window`。在位置 i 只做可判定的事：
在已知数据上找当前最长匹配 M(i)。若 `i+len(M)` 已抵达 `buf` 末尾，该匹配可能被未来 Write 延长，
不能定论——它后面的字节也不能提前当字面量输出（否则另一种切法会输出不同字节）。
于是只输出定论内容：匹配（或字面量）之后还跟着至少一个已确认不属它的字节时，它才无法再被延长、可以输出。
处理停在最后一个定论位置，剩余全部留在 `buf`。判定只依赖"已收到的完整字节序列"，与分几次 Write 无关，
故输出逐字节相同。`Flush` 表示调用方承诺此刻无后续字节：按已知匹配强制定论全部未决字节并追加 tag=2；
之后新 Write 仍可把刷新前数据当历史。无新输入时没有定论内容，第二次 Flush 零字节。
`Close` 强制定论全部未决字节并写流尾（不写刷新标记）。

### 2. 重叠回指的复制（语义 2）

回指语义是 out[k]=out[k-dist]：距离小于长度时，源字节在复制过程中才被写出。一次性块复制在
dst/src 重叠向后推进时读到的是旧字节，结果错误。实现为 length 次 `b=win.Byte(dist); 写输出; win.Push(b)`，
即定义本身；窗口读写 O(1)，解压总复杂度仍 O(输出)，故逐字节循环可接受，不做展开优化。

### 3. 距离的两类越界（语义 7）

"dist > 已输出字节数"：历史中不存在该字节，无限窗口也不可能合法。"dist > 窗口容量"：字节可能存在
但超出协议引用范围；压缩器只在容量 W 的窗口内选候选，故绝不可能产生后者。二者语义不同必须分别报错。
并行块压缩时，每块开始前用上一块末尾 W 字节预置窗口；该字典是更早块的输出、解压器到达本块前
已按序产出，故解压器视角的"已输出字节数"含义不变，无需特殊处理。

### 4. 并行压缩的确定性（语义 9）

块 b 是确定性贪心编码器，输入只有"本块原文 + 上一块原文末尾 W 字节"，不读其他块的压缩结果；
匹配判定、链顺序、链上限全确定，故无论哪个 goroutine、何种完成顺序，块 b 输出固定。
主 goroutine 按块号顺序拼接（worker 不写共享流），workers=1/2/4/8 输出逐字节相同。
块间不需要刷新标记：拼接处是普通记录边界，回指天然可引用预置字典；整流仅写一次流头/流尾。

## 配置

窗口 W（默认 1<<16）、链上限 L（默认 64）、最小匹配 3、最大匹配 1<<32（便于炸弹测试）、
解压输出上限可配。W=0 或 L=0 构造即返回错误。单实例非并发安全。
