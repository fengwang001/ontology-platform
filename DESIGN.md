# 规范化器设计推导

## 1. 末尾换行策略（空输入选择）

三策略：`Keep` 不改结尾；`Ensure` 输出非空且无尾换行时补一个；`Trim` 先剥全部尾换行，剥后仍有内容则保留一个，否则为空。
关键点：空输入若 Ensure 输出 `\n`，则 `N("\n")="\n"` 幂等成立，但语义上"空文件"被改成非空；选择 Ensure 对空输入输出 `""`，因为规范化器只删除/统一已有字节、不因 Ensure 凭空造内容，且 `N("")=""` 使三策略在空输入上一致、幂等显然成立。Trim 对 `"  \r\n"`（剥掉行尾空白与换行后无内容）输出 `""`。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |
幂等：规范化结果只含行内空白且每行以 `\n` 结尾；Keep 不改结尾、Ensure 仅在非空无尾换行时补（补后再处理无变化）、Trim 剥后最多一个（已是一个或为空），故 `N(N(x))=N(x)`。

## 2. 被删字节的 ToOut

设输出偏移 0..m。映射是保序的阶梯函数：被删区间内每个原文 i 必须取同一个输出值 k（夹在前一存活字节映射 k-1 与后一存活字节映射 k 之间，单调不减要求）。`ToOrig` 对每个输出偏移 o 必须选出唯一原文偏移使 `ToOut(ToOrig(o))=o`，因此 k 必须是"下一个存活输出位置"。
- `\r\n`：`\r` 删除、`\n` 保留且输出该 `\n`，故 `ToOut(i(\r))=ToOut(i(\n))`（换行符的输出位置），光标落到换行符上（之后）。
- 行尾空白：其后的存活字节是换行符，故整串空白都映射到换行符输出位置，光标位于行尾换行符**之前同一插入点**（编辑器视角即行末换行前）。
- 结尾待判定空白（Trim 删除、无后续换行）：映射到输出终点 m。

## 3. 空白缓冲超限

选择**拒绝**（`ErrWSBuffer`，带已消费原文偏移）。强制判定为"非行尾空白"会把空白原样吐出，若后续其实是行尾/EOF，则输出含行尾空白，违反语义 2 与幂等（语义 5）；拒绝不改任何已输出字节与映射，状态进入终态，可解释为"第 i 字节起的空白串超过上限"，且与切分无关（按原文总长度计数，语义 4 不受影响）。输出超限同理：在写出前判定，返回 `ErrOutputLimit`，已输出保留、终态。

## 4. par 拼接修正

切点 p 的不确定区只取决于局部字节：从 p 反向走过空白（空格/制表符）到 lo；正向从 p 起，跳过待定 `\r`（p-1 或 p 处，依切点）与 `\r\n`，再跳过其后空白，直到首个"决定性"字节（`\n` 或普通字符）到 hi。lo..hi 跨切点连续规范化；边界恰为已决换行符后则不确定区为空。把所有重叠不确定区合并为若干区间，与区间之间的"间隙"各自独立喂给单实例 `norm`，再按原文顺序拼接输出；映射表平移：段内 `(a,b)->(c,d)` 加上全局偏移 `(lo, 输出前缀长度)`。
修正区有上界：每个方向最多走过一个待定 `\r`/`\r\n` 加一串空白；空白串长度受 `WSBufferLimit` 限制（无上限时该行即为上界），且合并后间隙首字节必为已决状态，故并行结果与单机逐字节一致（含 K=1 与任意切点）。
