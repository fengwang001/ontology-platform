# DESIGN

## 流格式（全部字段为无符号 LEB128 varint）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `0x4F4E`（LEB：0xCF 0x9E）、版本 `1` |
| 字面量 | tag=0，长度 n，随后 n 个原始字节（n>=1） |
| 回指 | tag=1，距离 d（1..W），长度 l（>=MinMatch=3） |
| 刷新 | tag=2（边界标记，解压器无动作） |
| 流尾 | tag=3，原文总长，CRC32(IEEE) |

tag 与数值合在一个 varint 内：低 2 位为 tag。相邻字面量在输出前合并。

## 推导一：压缩与 Write 切法无关（语义 3/4）

匹配判定需要看下一个字节：最长匹配可能在某次 `Write` 之后才结束。压缩器始终只
提交到「已确认历史」为止的数据：维护缓冲 `pend`，仅当至少还剩 1 个未处理字节时才
提交前面的结果（该字节充当哨兵，证明匹配不会再延长）。规则：在 `pend` 中位置 i
求匹配 [i,i+l)；若该匹配能吃到 `pend` 末尾（i+l==len(pend)），它也许会被未来字节
延长，于是停止，把 i..末尾全部留待下次；否则提交字面量字节 i 或整个匹配并前进。
因为决定只依赖「当前位置 + 其后的字节内容」而不依赖这些字节属于哪次 Write，任意
切法产生完全相同的 token 流与字节流。`Flush` 没有未来字节，必须强制把 `pend` 全部
提交（匹配在边界终止），再发刷新 tag；此后继续 Write 时旧数据仍在窗口里，可被回指。
连续两次 Flush 间无新输入：第二次无 token、无刷新字节。

## 推导二：重叠回指的复制（语义 2）

块复制 `copy(dst, src)` 当 d<l 时，Go 的 copy 语义不保证读到刚写入的字节，会得到
重复源模式而非 RLE 展开。正确语义是「输出指针每走一步，历史也多一个字节」，即
`out = append(out, out[len(out)-d])` 逐字节前进。实现就用逐字节循环：每步一次索引
+ 一次 append，没有额外分配，复杂度 O(l)，与不重叠情形同阶，可接受。

## 推导三：两种距离越界（语义 7）

`d>已输出字节数`：引用了尚不存在的未来，任何合法流都不会有，说明流损坏/伪造。
`d>窗口容量 W`：引用存在于「逻辑历史」中但压缩器按配置本不该引用的远古字节；
压缩器只在容量 W 的环形窗口内取候选，绝不可能产生它，并行预置字典也只放 W 字节。
两者必须分开报错。预置字典不改变含义：字典字节先装入窗口与解压历史，回指的
「已输出」以「字典 + 本块输出」合计计数，与单流解压完全一致，无需特殊标记。

## 推导四：并行块压缩的确定性（语义 9）

块 k 的压缩只读取：本块原文、上一块末尾至多 W 字节（预置字典）。哈希链是压缩器
的私有状态，启动时用预置字典喂养，不读取任何其他块的输出或完成顺序；同一份
（字典+本块）经固定哈希与固定候选上限只产生唯一 token 序列。因此 workers=1/2/4/8
输出逐字节一致。衔接：块 0 的编码器发流头；各块压缩到块尾强制结束匹配（同 Flush
规则但不发刷新 tag），其回指在解压器看来就是普通回指；最后一块发流尾。块间不插入
刷新标记——刷新只承诺解压器可还原某偏移，块边界不向调用者暴露此承诺。
