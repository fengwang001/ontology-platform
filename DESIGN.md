# 设计推导

## 1. UTF-8 非法单元
首字节给出期望长度；只有已读字节仍是该长度的合法前缀时才继续等待。遇到第一个不可能的续字节即终止：该非法单元吞掉“首字节 + 此前已接受的合法续字节 + 这个坏字节”。若首字节本身非法，只吞 1 字节。因此 `F0 90 80 41` 在第三字节发现 `F0 90` 后第三字节应为 `90..BF`，吞 3 字节，只替换一次；`E0 80`、`ED A0`、`F4 90` 在第二字节即失败，只吞首字节，后续续字节各自再成非法单元。`C0 AF` 两个首字节均非法，故两个替换。`E2 82` 遇 EOF 仍是合法前缀，作为一个截断前缀替换一次。

| 首字节 | 第二字节合法区间 | 第二字节非法吞 | 后续非法吞 |
|---|---|---:|---|
| `C2..DF` | `80..BF` | 1 | - |
| `E0` | `A0..BF` | 1 | 第三字节非法吞 2 |
| `E1..EC` | `80..BF` | 1 | 第三字节非法吞 2 |
| `ED` | `80..9F` | 1 | 第三字节非法吞 2 |
| `EE..EF` | `80..BF` | 1 | 第三字节非法吞 2 |
| `F0` | `90..BF` | 1 | 三/四字节坏吞 2/3 |
| `F1..F3` | `80..BF` | 1 | 三/四字节坏吞 2/3 |
| `F4` | `80..8F` | 1 | 三/四字节坏吞 2/3 |
| `80..BF` | 无首字节 | 1 | - |
| `C0 C1 F5..FF` | 永非法 | 1 | - |

## 2. 流式缓存
只需缓存一个未完成标量的前缀。UTF-8 最长 4 字节，故在等待后续 `Write` 时最多留 3 字节；第四字节要么完成，要么当场与前三字节组成非法单元。UTF-16 代码单元 2 字节、代理对 4 字节，等待新数据时最多留 3 字节。看到非法续字节时立刻按第 1 节长度发一个替换并继续，绝不等 EOF；只有“目前仍合法但长度不足”才等。EOF 时仍等待属于截断，它与已定位的非法字节错误分开。

## 3. par 切点对齐
切点 `s>0` 时只向前回看一个字符：UTF-8 先越过切点前的续字节 `80..BF` 回到其首字节，工作起点是首字节；UTF-16 若落在代码单元中间则回 1 字节。从该起点用普通解码器求真实单元长度。若单元终点 `>s`，本段解析但在输出和统计中扣除这个跨段单元，它归前一段；否则不扣除。段尾同理读入至多 3 个 lookahead 字节，使所有起点在段尾之前的单元完整归属本段。
这样每个内部边界只让至多一个单元的有限字节（UTF-8≤3、UTF-16≤3）被相邻 worker 复查。`C3|80 80` 中后段回看到 `C3 80`，知其为终点在切点后的合法标量并扣除；随后独立解析第二个 `80`，所以非法单元数与单线程相同。中间段不启用 BOM 语义，只有全局偏移 0 的 worker 识别 BOM。

## 4. 上限与断点
字节先进入“当前单元”状态机；只有形成完整标量或非法单元后才计数为该单元的输入归属，并在写输出前检查剩余空间。输出不足时不写任何半标量或半个代理对，转码器进入终态，返回 `ErrLimit`；本次 `Write` 的 `n` 是本次实参中属于已完整输出单元的连续前缀字节数。若跨多次写入的未完成单元被拒，`Pending` 返回此前缓存的整个单元字节，调用方用 `Pending()+p[n:]` 续到新实例；残留在缓存、尚未形成单元的字节不冒充已输出标量。正常拼回后，输出按标量边界相接，与无上限一致。
