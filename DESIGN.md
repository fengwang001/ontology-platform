# OLZ 流式 LZ77 设计

## 流格式

所有整数使用 LEB128 无符号变长整数；偏移从 0 开始。

| 记录 | 字节布局 |
|---|---|
| 流头 | `magic=0x4f4c5a`、`version=1`、`window`，各为 uvarint |
| 字面量 | tag=`0` uvarint，`length` uvarint，随后 length 个原文字节 |
| 回指 | tag=`1` uvarint，`distance` uvarint，`length` uvarint |
| 刷新 | tag=`2` uvarint，无负载 |
| 流尾 | tag=`3` uvarint，`original_length` uvarint，FNV-1a 64 校验和 uvarint |

空输入为流头加流尾；流尾后的任意字节均非法。

## 关键推导

1. **Write 切法无关**：匹配起点只能引用已存在的历史；考察某起点时，未来字节只会延长或终止该匹配，不能增加新的历史候选。压缩器因此可立即确认“不足 3 字节且无三元候选”的字面量；一旦当前最长匹配吃到暂存区末尾，就保留这段未决匹配，等下一个字节证明它继续或失配。字面量按固定 64 KiB 边界成段，匹配按固定 64 KiB 上限成段；这些边界只由累计输入偏移决定，所以 1 字节、7 字节或整段写入得到同一字节流。`Flush` 是用户声明的知识边界：此时必须把未决匹配按当前长度结束并写刷新标记，否则解码器不能还原全部输入。
2. **重叠回指**：块复制 `copy(dst, dst[len-d:])` 在 Go 中可能一次搬运重叠区间，`d<len` 时是否逐周期展开依赖实现方式。解压器对回指执行 `out=append(out, out[len-d])`，一次只追加一个字节；下一轮源位置也可能位于本次回指已生成的字节中，因此距离 1、2、3 的周期展开天然正确。回指长度受输出上限约束，线性复制就是 LZ77 解压的必要工作量，没有额外渐近成本。
3. **两类距离错误**：`distance>window` 违反格式和编码器不变量：哈希链只保留最近 window 个位置，压缩器在插入候选与查找时都以窗口容量裁剪，故不可能发出。`distance<=window` 但 `distance>已输出字节数` 指向尚未产生的位置，只能由损坏流或截断的预置字典造成，必须单独判定。并行块以“上一块原文末尾 window 字节”为预置字典；在最终流中这些字节属于解码器已经输出的前一块，所以全局已输出字节数的含义不变，块内距离仍可换算为全局合法距离。
4. **并行确定性**：第 n 块的哈希链初始状态只由第 n-1 块末尾 window 字节决定，块内决策只读取该字典和本块原文；不读取其他块的压缩记录、channel 结果或 goroutine 完成顺序。各 worker 使用私有匹配器，产出带编号的不可变记录片，主 goroutine 按编号拼接并统一写流头、FNV 校验和与流尾。块间普通记录直接衔接；刷新标记只表示可解压边界，不是重置窗口，因此固定块边界不需要插入刷新标记。
