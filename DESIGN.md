# 连接顺序代价模型与计划选择器 — 设计推导

## 1. 并列打破（tie-break）判定策略

代价是 float64。朴素做法有两处不确定来源：

1. `==`：两个数学上相等的代价，因加法结合顺序不同可相差 1 ULP（如 `a+b+c` 与 `(a+c)+b`），
   微小差异会让选择随枚举/登记顺序漂移。
2. 严格小于号下"首个最优"的胜出者依赖表的登记顺序。

**策略（两阶段）**：

1. 先按**相对误差**判等：`|a-b| <= epsTie * max(1, |a|, |b|)`，其中 `epsTie = 1e-12`。
2. 判等则按"参与表名序列"的字典序选择：序列定义为计划前序遍历得到的表名切片
   （左子树在前、右子树在后），逐元素比较 `strings.Compare`，再比长度。

**阈值依据**：代价是 O(子计划数) 次加法累加，每项为基数/扫描代价。n<=20 时相对误差
约为 `n * 2^-52 ≈ 4.5e-15`；取 `1e-12` 为其上界并留约 200 倍裕量，既能吸收 ULP 级抖动，
又远小于真实不同计划间的有效代价差（基数按行相乘，差至少一个有效位）。非判等时仍取严格较小者。
为保证可复现，DP 内部所有子集/划分数组都先按"排序后的表名"规整，与登记顺序无关。

## 2. 笛卡尔积推迟规则

谓词构成无向图（表为顶点，谓词为边）。规则：

- 对每个子集 S，只考虑把 S 划分为 A、S\A 两个非空不交部分的划分。
- 若 A 与 S\A 之间**存在任何跨边谓词**，允许普通连接。
- 若**不存在跨边谓词**（即产生笛卡尔积），仅当 A 与 S\A 分别是图的完整连通分量
  （即二者内部都没有连向 S 之外顶点的边）且 S 之外**不存在**与 A 或 S\A 相连的顶点时才允许。

等价表述：只有把若干个已"封闭"的连通分量在顶层合并时才允许笛卡尔积。因此：
链式图（单连通分量）任何中间划分都有跨边或会破坏连通性，全程无笛卡尔积；
两段不连通图（两个分量）唯一合法的笛卡尔积发生在两分量都完成后的最后一步，恰好一次。

## 3. 基数与代价公式

设表 T 目录登记行数 `rows(T)`（权威值，过期统计以它校正）。

- 扫描基数：`card = rows(T)`；扫描代价：`scanCost = pages(T)`（无页数时取 `rows(T)`），
  另加固定每表启动常量 `1.0`。
- 等值连接 `L.a = R.b` 选择率（无直方图时）：`sel = 1/max(ndv(a), ndv(b))`。
- 直方图可用时用等宽桶重叠比例改进：
  `sel = Σ_bucket (overlap_fraction(a,b) * count_a/ndv 估计)`，保守取与 1/maxNDV 的较大者。
- 多谓词：`sel = Π sel_i`，标注"独立性假设"（仅在谓词引用不同列对时近似合理；
  相关谓词会低估基数，explain 中显式标注）。
- 连接基数：`card(L⋈R) = cardL*cardR*sel`；笛卡尔积 `sel=1`。
- 连接代价（嵌套循环基准 + 哈希探测项）：
  `joinCost = costL + costR + cardL*cardR*sel + 0.1*min(cardL,cardR)`。
基数估计只读取统计对象（行数/NDV/桶计数），从不访问任何行数据；cost/plan 不持有行迭代器。

## 4. 统计异常（均可用 errors.Is 区分）

- `ErrStatMissing`：列无统计。回退固定选择率 `1/10 = 0.1`，explain 标注"统计缺失：估计不可靠"。
- `ErrStatStale`：统计行数与目录行数偏差 > 10%（阈值 `StaleFraction`）。按目录行数校正基数，
  explain 标注"统计已过期：按目录行数校正"。
- `ErrStatCorrupt`：桶计数之和 != 统计行数（允许 0 行误差 0）或桶上界非严格递增。
  返回错误并指出表名.列名；损坏不回退，直接拒绝该目录（fail-fast）。

## 5. 复杂度与上限

DP 按位掩码子集枚举：子集数 ≤ `2^n`；对每个子集枚举划分 A⊊S，划分数 ≤ `3^n`
（每元素属于 A / S\A / S 之外三种状态），远小于 `n!`。计数器 `subsetsSeen/splitsSeen`
在 plan 包内非导出，仅暴露只读访问器。
硬上限 `MaxTables=16`：若 n > MaxTables 立即返回 `ErrTooManyTables`（不分配 DP 状态，
已用中间表数为 0），防止 3^20 级内存膨胀。

## 6. 边界语义

- 单表：直接返回扫描计划，代价 = 扫描代价。
- 0 行表：基数 0，连接基数 0（空关系传播）。
- 1 行表：NDV≤1，等值选择率 ≤1，公式自然成立。
- 自连接（谓词两侧同表）：明确拒绝，返回 `ErrSelfJoin` 并说明原因（别名解析超出本范围）。
目录构造后冻结为只读，所有选择调用共享同一份不可变统计，天然并发安全；计数器按每次
`Choose` 调用独立持有，无共享可变状态。
