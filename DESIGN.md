# 设计推导

## 1. 行尾与空白

`\r\n` 与单独 `\r`/`\n` 都产生一个 `\n`。处理状态机只保留一个待定位点：上一个字符是 `\r`
时，下一个 `\n` 与它合并（`\r` 删除、`\n` 保留）；否则 `\r` 立即成行尾。因此 `\r\r\n` 是
两个行尾：第一个 `\r` 先落定，第二个 `\r` 与 `\n` 合并，输出 `\n\n`。
行尾空白延迟：连续的空格/制表符先缓冲，遇到行尾整体丢弃，遇到其他字节整串回吐为
普通内容，流结束时视为「非行尾」回吐（EOF 不是行尾）。故只含空白的行：空白在 `\n` 前
被判为行尾空白而删除，行由保留的 `\n` 体现。

## 2. 偏移映射

映射表是原文与输出坐标上的「段」序列，每段形如 `(origStart,outStart,kind)`：
`Keep` 段两坐标等速（slope 1），`Drop` 段输出长度为 0（被删原文），`Add` 段原文长度 0。
相邻同类段合并。段数只随删除/新增点增长，纯 `\n` 文本只有一个 Keep 段。
查询在起点坐标上二分，计数器记录检查段数。

- `ToOrig(o)`：落在某段内的输出偏移按段内坐标换算。落在 `Add`（合成 `\n`）或 `Drop`
  段正后方的输出边界时，取**该删除段的原文起点**（光标落在行尾换行符上 → 原文里落在
  被删空白串的最左端、即换行之前，正是编辑器「行尾光标」的位置）。
- `ToOut(i)`：`Keep` 段内直接换算；`Drop` 段内任意原文偏移都取该删除段**之后**的输出
  坐标（删除点的右侧）。

验证：设删除段原文 `[a,b)`、输出点 p。删除段内及右端 `ToOut(i)=p`，删除段前 `p-(a-i)`；
`ToOrig(p)=a`，于是对输出点 p 有 `ToOut(ToOrig(p))=ToOut(a)=p`。两段函数都随坐标
单调不减。被删空白里的光标映射到行尾换行符（该行的右边界）。

## 3. 末尾策略（推导与样例表）

记规范化（去行尾空白、统一行尾、未施加末尾策略）的结果为 B，其末尾换行个数 t。
- `Keep`：输出 B，幂等：规范化不改 B。
- `EnsureOne`：输出空 → `\n`；否则 B 末尾恰补成一个 `\n`。选「空→`\n`」而非「空→`""`」：
  后者下 `N("")=""` 却 `N("\n")="\n"`，不违反幂等，但策略语义「确保恰好一个」对空结果
  也成立才一致；本选择下 `\n` 是不动点。
- `TrimBlank`：解释为「删除末尾多余空行」：t≤1 保留 B；t≥2 删到只剩一个 `\n`（保留
  最后一行换行）。`""→""`。该函数在自身输出上重复仍得自身（输出 t 只可能是 0 或 1）。

| 输入 \ 策略 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 4. 缓冲超限

两种候选：(A) 强制把待定空白判为「非行尾」输出——但后续字节可能是行尾，届时输出已
不可撤回，直接违反第 2 条与幂等；(B) 拒绝。选拒绝：返回带原文偏移的可判定错误，
已输出内容与映射保留，实例进入终态（再 Write/Close 返回「终态后写入」错误）。
拒绝语义独立于最终输入形态，故任意切分方式结果一致，不违反 4/5/6 条。输出总字节
超限同理：立刻拒绝并进入终态。

## 5. par 拼接

按字节硬切的段**不能各自独立规范化**：段首不知前段末尾的待定 `\r`/空白。做法：各段
先用 `Keep` 策略流式规范化（状态机把段末待定状态在 Close 时按「EOF 非行尾」落定），
再在拼接处做有界修正。拼接点只需查看前段尾部至多一个待定 `\r`、一串待定空白，以及
后段开头的判定字节序列（空白、`\r`、`\n`）：
1. 前段末 `\r` + 后段首 `\n`：前段把 `\r` 当独行尾多输出了一个 `\n`，删除该 `\n`
   （Drop），后段 `\n` 保留 —— 与整体 `\r\n` 一致。
2. 前段末空白串被 EOF 回吐为内容，但后段首个非空白字节是行尾：该空白串应整串删除，
   把对应 Keep 段改为 Drop。
其余情况回吐保持不变。修正范围只涉及拼接点两侧各一个「待定结构」，故有界；整体扫描
量 O(输入)。映射段坐标加段偏移平移，再按上面两条改写拼接处的段类型即可。
