# SAGA 编排器设计推导

## 1. 正向动作返回「超时/未知」时如何归类

正向结果分三类：`Success`、`Fail`（明确失败）、`Unknown`（超时/未知，可能已生效）。

- 明确失败：本步确定没有效果。由不变量 1，失败步**不进**补偿集合，且后续正向步骤不再执行。
- Unknown：无法确定是否生效，两种真实情况都必须安全。补偿动作被要求**幂等**：
  - 实际已生效：补偿把效果撤销，得到正确结果；
  - 实际未生效：幂等补偿为空操作，不产生副作用，仅多一次补偿调用。
  反之若选择不补偿，则「实际已生效」时效果永远悬挂，违反不变量 1 的精神（已产生效果必须可撤销）。

结论：Unknown 与 Fail 一样**终止正向阶段**，但与 Success 一样**计入补偿集合**。是否补偿只取决于「是否可能产生过效果」，与调用方的确定程度无关。

## 2. 重试规则（正向与补偿）

- 仅当步骤 `Retryable` 且结果为**明确失败**时重试；`Unknown` 一律不重试（再发一次正向请求可能造成双重效果，应交给幂等补偿）。
- 每个步骤每个方向最多真实调用 `MaxStepRetries` 次（含首次）；任一次返回 `Success` 立即停止。
- 补偿方向同一规则：明确失败且 `Retryable` 才重试；补偿 `Unknown` 记为补偿未成功（可能已撤销，幂等前提下 Resume 再补一次永远安全）。
- 每次尝试（含失败尝试）都在写「成功记录」**之前**落一条结果日志；是否继续完全由日志决定。

与不变量 3 的关系：`Resume` 重建状态后，已存在 `Success` 记录的 (步骤, 方向) 永不再次调用；重试循环只在同一轮执行内、尚无 `Success` 记录时发生。因此同一日志任意次 `Resume`，动作真实调用总次数恒等。

## 3. `Resume` 续跑起点（忽略尾部终态标记，看最后一条步骤记录）

设步骤总数 n，下标 1..n。

| 日志最后一条记录 | 续跑起点 |
|---|---|
| 第 k 步正向成功，k < n | 正向从第 k+1 步继续 |
| 第 n 步正向成功 | 终态 Completed，空操作 |
| 第 k 步正向明确失败 | 补偿 k-1 .. 1（第 k 步不补） |
| 第 k 步正向 Unknown | 补偿 k .. 1 |
| 第 k 步补偿失败/Unknown | 先重做第 k 步补偿（幂等），再继续 k-1 .. 1 |
| 第 k 步补偿成功，k > 1 | 继续补偿 k-1 |
| 第 1 步补偿成功 | 终态（Compensated 或 CompensationFailed） |
| 终态标记 End | 空操作，直接返回重建快照 |

重建先于执行：正向成功集合 = 有 FwdSuccess 或 FwdUnknown 的步骤；补偿成功集合同理。补偿顺序由 `compens` 包从「需补偿集合 − 已补偿成功集合」按**严格下标递减**算出，因此崩溃/Resume 任意次都不会漏补或乱序。

## 4. 已完全补偿的实例再次 `Resume`

选择**空操作**：返回终态快照，不调用任何动作、不追加日志。实例到达终态时写入一条 End 标记，`Resume` 只读尾部 1 条记录即可判定终态。与不变量 3 一致：没有新的事实，就没有新的调用。

## 5. 日志模型与状态重建

- 记录两种：`StepRecord`（下标、方向 Fwd/Comp、结果三态、错误、毫秒时间戳、幂等键）与 `EndRecord`（终态 Completed/Compensated/CompensationFailed）。
- 状态重建是只吃日志的纯函数：存在 End 用其终态；否则由步骤记录集合推导当前阶段；每个 (步骤, 方向) 的真实调用次数 = 该键的记录条数。在线状态与重建状态逐字段比较，即 `SelfCheck`。
- 合法性：每步至多一条正向成功且其后无正向记录；补偿记录必须晚于该步 FwdSuccess/FwdUnknown；补偿成功至多一条且在最后；正向下标严格递增，补偿下标全局严格递减。

## 6. 复杂度与故障注入

- `logsRead`（非导出）统计一次 `Resume` 为决策读取的记录数：终态实例读尾部 1 条 End 即返回，与日志长度无关（100 与 10000 条均为 1）；无 End 的崩溃实例才全量重建。
- 哨兵错误：空步骤列表、幂等键重复、nil 补偿（成功后需要补偿时触发）、超 MaxSteps/MaxRetries/MaxJournalEntries（追加前预检，不留半条）、实例不存在、实例运行中并发 Resume，互不相同。
- 每实例一把运行锁，`Run/Resume` 互斥，并发 Resume 得 `ErrInstanceBusy`；实例表与日志各自加锁，实例间无共享可变状态。
