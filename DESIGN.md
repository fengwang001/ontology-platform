# 设计推导：熔断 + 舱壁调用保护器

## 分层与调用顺序

请求路径：`stat(总请求) → breaker → bulkhead → timeout(真实调用) → stat(结果)`。
熔断在前、舱壁在后，理由：

- 熔断打开意味着下游已被判定故障，此时应立刻拒绝，不占用任何并发额度；
  若舱壁在前，连「拒绝」都要先拿额度甚至排队，故障期额度被无效请求耗尽，
  下游恢复后第一批真实调用反而进不来。
- 舱壁的职责是限制「真实在途调用」的并发，只应作用于已通过熔断放行的请求。

## 推导一：熔断计数只算真实调用

设失败率 = 失败数 / 样本数。若被熔断拒绝也计入样本且算失败，则熔断打开后
每次拒绝都把失败率推向 100%，冷却结束进入半开时失败率已被人为拉满，
任何探测结果都无法把它降回阈值之下，熔断将永远打不开（自我锁死）。
因此正确口径：**只有真正到达下游的调用（真实调用）才计入样本与失败率**；
被熔断拒绝、被舱壁拒绝都只计入各自的独立计数器，不进失败率分子分母。
推论：半开期一次探测成功后，失败率统计中只新增这一个成功样本，
熔断器可凭「半开探测全部成功」独立条件回到关闭，不被历史拒绝污染。

## 推导二：额度必须在全部四条路径归还

舱壁额度是计数信号量，获取后必须且只能归还一次。调用结局有四种：
成功返回、失败返回、超时（底层调用可能仍在跑，但包装层已放弃，额度属于
包装层的在途配额必须归还）、panic（recover 后转为失败）。任何一条路径漏还，
额度单调泄漏至 0，此后所有请求被拒，系统假死。实现上用 `defer` 在获取额度
成功后立即注册归还，panic 路径由同一 `defer` 覆盖，保证四条路径统一归还。

## 推导三：不可重试错误计入熔断

定义：熔断的目的是「下游持续故障时停止施压」。不可重试错误（如参数非法）
若来自下游响应，同样消耗下游资源且反映调用失败事实；把它排除在熔断计数外，
会让一个稳定返回 4xx 的故障下游永远触发不了熔断。因此规定：
**超时与可重试错误、不可重试错误都计入熔断失败**；三者的区别只在
`classify` 的分类标签与 `stat` 的细分口径，供调用方决定是否重试，
不影响熔断判定。panic 归为不可重试失败，同样计入。

## 关键语义决策

- 熔断打开 → 半开：注入时钟 `Now() - openedAt >= cooldown`。冷却退避：
  半开失败后 `cooldown = min(cooldown*2, maxCooldown)`，回到关闭时重置为初始值。
- 时钟回拨：`Now() < openedAt` 视为时钟异常，返回可判定错误 `ErrClockRewind`，
  状态保持打开不变，不提前进入半开。
- 半开并发：用计数器限制同时在途探测数 = `MaxProbes`，超出立即拒绝
  （计入熔断拒绝，不计失败率）。
- 状态迁移只发生一次：所有迁移在互斥锁内做 CAS 式检查（先验当前状态再迁移），
  并暴露迁移计数器供测试断言；冷却加倍只在「半开→打开」这一次迁移里发生。
- 舱壁等待队列：额度满时进入容量为 Q 的等待队列，第 N+Q+1 个请求立即返回
  `ErrQueueFull`，不推进时钟、不阻塞；等待中 `ctx.Done()` 触发则移除并返回
  `ctx.Err()`，名额让给下一个等待者。
- 参数校验：并发额度、队列上限、阈值、样本下限、探测数 <= 0 时构造报错。

## 统计口径（stat 的不变式）

- 总请求数 = 成功数 + 失败数 + 被熔断拒绝数 + 被舱壁拒绝数
- 真实调用数 = 总请求数 − 被熔断拒绝数 − 被舱壁拒绝数
- 各类别失败数之和 = 失败数

每个请求在入口计一次总数，结局只落入四个互斥桶之一，因此等式恒成立。
