# 设计推导

## 1. 续行接上空白行 / 空行（语义第 3 条）

对照 `java.util.Properties` 的 `LineReader.readLine()`：行尾奇数个 `\` 置 `appendedLineBegin=true`；
进入下一物理行时仅在该标志为真时，于行首跳过空白（空格、`\t`、`\f`），遇非空白字符后原样保留。

结论：下一物理行整行为空或只含空白时，其内容被全部跳过，逻辑行停在已累积的内容上，
反斜杠被丢弃且不插入任何字符（`a=b\` + 空白行 + EOF → `b`）。该空白行已作为独立物理行被消费
且末尾没有续行符，逻辑行就此终止；再后一行是全新逻辑行，不再续接。它与「续行首部空白被剥掉」
是同一条规则：整行空白只是“首部空白”延伸到行尾，剥完后无字符剩余，而不是被当作空逻辑行。

## 2. Store 的最小转义推导（语义第 6 条）

Load 端规则决定 Store 端必须转义什么：

- 键首 `#`/`!` 且其前无字符时会被识别为注释，故**键首** `#!` 必须转义；值位于分隔符之后，
  行内 `#!` 永不构成注释，故值的 `#!` 一律不转义。
- 读键前吞掉逻辑行首部空白、分隔符后再吞一次空白：故键与值的**前导**空白必须转义；
  值中间与值尾空白原样保留，不必转义。
- 键中未转义的 `=` `:` 空格是分隔符候选，必须转义；值中 `=` `:` 永不分隔，无需转义。
- `\` 转义为 `\\`；`\n` `\r` 转义（否则改变物理行结构）；`\t` 转义；其余控制字符用 `\uXXXX`。
- 非 ASCII 既非空白也非分隔符/注释符，原样 UTF-8 输出即可往返。

## 3. 语义 — 测试对照表

| 语义条目 | 测试函数 |
|---|---|
| 1 分隔符与空白 | _待补_ |
| 2 注释 | _待补_ |
| 3 续行 / 空白续行 / EOF | _待补_ |
| 4 转义与 \uXXXX 错误行列号 | _待补_ |
| 5 重复键覆盖且顺序按首次 | _待补_ |
| 6 Store 最小转义往返 | _待补_ |
| 7 1000 组随机往返 | _待补_ |
| 复杂度：检查计数 ≤ 2N | _待补_ |
