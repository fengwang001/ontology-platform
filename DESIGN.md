# 设计推导（Java .properties，`java.util.Properties` 语义）

## 3. 续行与“空白续行”

- 读行时只统计行尾连续反斜杠的奇偶：`\` 是转义符，`\\` 是被转义的字面反斜杠。
  因此必须从行尾（`\r`/`\n`/EOF 之前）向左配对计数——不能从行首重扫，每字节只过一次。
  奇数个：最外层 `\` 与行终止符一起被吞掉，内层配对还原成字面 `\`，与下一物理行拼接；
  偶数个：全部是配对转义，行正常结束。EOF 处单个 `\`：同样被吞掉，不续行。
- 拼接后下一物理行走 skipLeadingWhitespace：循环吃掉空格/`\t`/`\f`。
- **空白续行的结论**：下一物理行只含空白（或为空）时，前导空白循环把整行吃光，
  随后直接读到行终止符，于是当前逻辑行**就此结束**，不会再接再下一行。
  即 `k=a\`+NL+`   `+NL+`z` 的结果是 `k=a`，`z` 是独立的新逻辑行。
  依据：JDK `Properties.readLine` 的 skipWhiteSpace 标签在吃掉空白后若遇到换行符，
  就正常收行（空白行不产生续行；反斜杠只出现在物理行尾才有续行意义）。
  这与“续行首部空白被剥掉”是同一条规则的两面：被剥掉的前导空白只是分隔符，
  行在出现真正内容之前先遇到换行，则该续行不贡献任何内容。

## 6. Store 转义推导

- 回写形如 `key=value\n`，只转义“再解析会改变含义”的字符，非 ASCII 原样输出 UTF-8。
- **值**：Load 时 `\` 自身是转义符 → 转义 `\\`；CR/LF 会断行 → `\n`/`\r`；
  值起始的空白会被“分隔符后跳空白”吃掉 → 首字符空白必须转义（`\ `、`\t`）；
  值中间空格没有任何语法作用，故不转义；`#`/`!` 只在行首才算注释、`=`/`:` 只在键扫描时才算分隔符，
  因此值中这些字符一律不转义。
- **键**：Load 时键扫描遇空白、`=`、`:` 即结束，所以键中任何位置的空白与 `=`/`:` 都必须转义；
  以 `#`/`!` 开头会被整行判为注释，故首字符是 `#`/`!` 时必须转义，其余位置不必。

## 测试对照表

（测试函数落地后补齐）
