# 批量写入组提交设计

## 目标与边界

并发的单条写入先在内存中组批；一个批次仅执行一次写盘和一次同步，然后把批内结果分别返回给调用方。
日志放在本地目录；程序不联网、不使用第三方库。单条载荷允许为空，超大单条不拒绝。

## 序号分配

序号不能在请求刚到达时分配：该请求可能等待很久，且它所在批次若失败会留下空洞。
序号也不能在同步成功后才分配：leader 必须在落盘前写入批的全局区间，等待者也需要在同一批次上等待确定结果。

规则：批次组装完成后、写入前由 leader 原子预留连续区间。例如基础序号为 10、批内 3 条，则分配 11、12、13。
写盘或同步失败时，leader 将计数器回滚到基础序号，批内所有请求得到同一个失败结果，这些序号全部作废。
下一批成功后从同一基础序号重新分配，因此成功结果连续且无空洞。

## 批次原子性

WAL 不在每条记录后同步。磁盘格式先有 8 字节全局文件头，之后每个批次自行描述：

- 批次头：魔数、批次长度、首序号、条目数、批 CRC32；
- 条目：4 字节大端长度加原始载荷；
- 尾部：批头与全部条目内容的 CRC32。

CRC 是批级而不是条级：回放只接受完整通过 CRC 的批次。批次内第三条损坏时，无法证明其余条目属于一个完整已提交批次，因此五条全部丢弃。
读回在坏批处停止；上层截断到最后一个完整批次，后续写入追加新的原子批次。

## Leader 与唤醒

攒批器为每个批次创建一个等待组。第一个让批次满足触发条件、或在关闭时得到现成批次的协程成为 leader：

1. leader 从队列移除批次并关闭其 `ready` 通道；
2. 其他协程在 Add 中看到该批次已关闭，携带自己的请求引用进入等待者列表；
3. leader 分配序号并调用 WAL；
4. leader 按批内请求对象逐一写入对应结果，再关闭 `done`；
5. 等待者只从自己持有的 request 读取结果，不共享槽位、不按唤醒顺序猜测序号。

因此唤醒不会串台。批内条目顺序就是组批顺序，也是序号递增顺序。

## 攒批触发

三个条件先到先触发：条目数达到上限、估计批字节达到上限、等待时间达到上限。
字节数按载荷总和计数；达到字节上限时连同当前请求成批。单条载荷超过字节上限时立即单独成批；否则合法的大请求会被人为饿死或必须拒绝，均不符合语义。
批上限为 1 时退化为逐条提交；等待为 0 时请求到达即触发。零请求 Close 不产生空批次，也不创建日志文件。

## 故障模型

- 截断落在全局头、批头、条目或 CRC 区间时，分别返回可 `errors.Is` 的错误。
- 写一半崩溃属于尾部不完整或 CRC 不符：整个批次不可见。
- 同步返回错误时整个批次失败并回滚序号；调用方不得把该次写入视为成功。
- Close 与 Add 竞争只有两种结果：请求进入最后一批并成功落盘，或返回已关闭错误。Close 后回放可验证所有成功请求均持久化。

## 包职责

- `req`：请求、结果和四类哨兵错误。
- `batcher`：并发组批，触发条件由可注入时钟驱动。
- `wal`：文件头、编码、批 CRC、同步、读回与截断分类。
- `commit`：leader 编排、序号预留与回滚、同步计数。
- `recover`：扫描日志，报告最后完整批次和下一个序号。
- `cmd/demo`：在临时目录逐项构造并判定上述行为。
