# CSV 方言解析器设计

## 1. 空行与单列空值

记录由行尾分隔。`"a\n\nb\n"` 含三条记录，不能丢中间一条；而空行本身没有逗号，按语法它就是一条含一个**未引号空字段**的记录。因此选择：**空行 = 一条单字段记录（字段为空、未加引号），不跳过**。流末尾的换行只是最后一条记录的终止符，不产生记录；文件无尾换行时最后一条记录同样成立。

往返推论：单列表中一条记录唯一字段为空且带引号（`""`），若 writer 不加引号写成空行，重解析得到的是「未引号空字段」，引号标记丢失 → 丢数据。故单列记录唯一字段为空时**必须加引号**写为 `""`；未引号空字段（空行）才写成零字节。规范输入定义：无尾换行；空行（连续两个换行）允许；单列的引号空值必须写 `""`。此规范上 `Write(Parse(x)) == x` 逐字节成立；任意成功解析的表都满足 `Parse(Write(T))` 字段值与引号标记逐位相同。

## 2. par 切点

切点可落在任意字节。方案：K 段各算两遍假设——**O**（起点在引号外、字段开始）与 **I**（起点在引号内）；段0取 O，之后由上一段被选 trace 的出口态选择本段：出口在引号内选 I，否则选 O。

裸假设在两种切点失效，故**切点右移**（单调、绝不越过下一个切点；移动只是跳过不重复处理字节，计数仍每字节一次）：前一字节是 `\r` → 移到其后（CR/LF 不跨段，消除 CR 待定跨段）；前一字节是 `"` → 移到其后连续 `"` 串之后的第一个字节（引号双写不跨段）。此时「引号外」入口必落在字段边界或未引号字段中间，两假设足够：I trace 首个事件为 `cont`（承接悬置字段），O trace 首个事件为 `start`。选中 trace 的字节事件按全局偏移重放给 table 组装器；记录号、字段号、偏移 = 段基址 + 段内坐标，随 trace 线性累加，错误坐标同理换算。每字节至多在 O/I 中各处理一次 ⇒ 计数 ≤ 2N+O(K)。

## 3. CR 待定

未引号态见 `\r` 进入 `crPending`：下一字节是 `\n` 则行尾成立；否则 `\r` 是孤立 CR，立即报错（错误偏移指向 `\r`）。半包续传时状态保留在状态机内，`\r` 与 `\n` 分两次 Feed 结果一致；EOF 停在 `crPending` 报孤立 CR。par 中切点右移保证 `\r\n` 同段。引号字段内 `\r` 是普通内容，无待定；引号字段内 `\r\n` 原样保留，写回不规范化。

## 4. 上限「立刻」

字段字节数按**输出字节**计：普通字节 +1，`""` 合一时 +1，在该字符被追加前检查，超限即返回错误、不缓冲后续字节。记录字段数在第 max+1 个字段开始前拒绝；总记录数在第 max+1 条记录开始前拒绝。错误即时返回，解析器进入终态，已产出完整记录保留。

## 5. 状态转移表

状态：S=字段开始 U=未引号中 Q=引号中 P=刚见引号(闭合待定) C=CR待定；输入类：`,` `"` `\n` `\r` O=其他。

| 现态 | , | " | \n | \r | O |
|---|---|---|---|---|---|
| S | 发空字段→S | →Q(记引号) | 发空字段,行结束→S | →C | 起字段→U |
| U | 发字段→S | **ErrQuoteInBare** | 发字段,行结束→S | →C | 追加→U |
| Q | 追加逗号→Q | →P | 追加\n→Q | 追加\r→Q | 追加→Q |
| P | 发字段→S | 追加引号→Q | 发字段,行结束→S | **ErrBareQuote** | **ErrBareQuote** |
| C | 旧\r报错,分隔→S | **ErrBareCR** | 行尾成立→S | 旧\r报错,新\r→C | **ErrBareCR** |

EOF：S/U 发字段收记录（流中无任何字节则无记录）；Q → **ErrUnclosedQuote**；P 发字段收记录；C → **ErrBareCR**。
四类语法错误：ErrQuoteInBare / ErrBareQuote / ErrUnclosedQuote / ErrBareCR；表级 ErrColumnCount。均携带字节偏移、记录号、字段号（偏移从 0、编号从 1 起）。
