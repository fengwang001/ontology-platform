# CSV 解析器设计

## 1. 空行与单列空值
空文件不产生记录；末尾换行不产生额外记录（EOF 时若本条记录内尚无字段则不闭合记录）。
但“空行”（读到换行时本条记录内没有任何字段）是合法的**一条单列记录**：跳过它会让
`a\n\n` 变成 1 条记录，而回写 2 条记录时无法逐字节还原。因此选择：空行 = 一条只含
一个**未加引号**空字段的记录。回写单列空值时若写空串，再解析无法区分“空行记录”与
“流末尾换行不产生记录”，会丢数据；故单列记录的唯一字段为空时**必须写 `""`**
（加引号空字段），多列中的空字段写空串即可。规范输入定义：记录以 `\n` 结束（含最后
一条），分隔符逗号；单列空值写 `""`；含逗号/引号/CR/LF 的字段加引号，引号转义为
`""`；引号字段内 CRLF 原样保留。

## 2. par 切点
切点任意。每段用两种入口假设各算一遍：(A) 入口在引号外，入口状态仅由切点前一字节
决定——切于流首或前字节为逗号/`\n` 时取 FRESH，前字节为 `\r` 时取 CR，否则取 BARE；
(B) 入口在引号内，前字节为引号时取 QSEEN，否则取 QUOTED。段 0 真实状态为 FRESH。
顺序确定真实入口：用 (A) 出口是否在引号内判定下一切点是否在引号内；在引号内则采用
(B)，否则采用 (A)，其出口再向后传播。切在 `\r\n` 中间：上段出口为 CR，下段入口
即 CR，第一字节即可判定；在引号内则 `\r` 已是内容。拼接：被采用段的事件偏移加段起点
后喂给普通 table 组装器；跨段字段由 open/chunk 事件续接；最后一段出口状态用于 EOF
判终。记录号/字段号由组装器累计，段内只需换算字节偏移。每段最多两遍，处理次数
≤2N+常数，不因长引号字段重扫。

## 3. CR 待定
引号外见到 `\r` 进入 CR：下一字节 `\n` 则闭合记录；其他字节一律为孤立 CR 错误
（逗号前的 CR 也非法）；EOF 时 CR 非法。半包续传时 CR 是普通状态，跨 Feed 不丢。
par 切于 CR 后时下段入口为 CR。引号内 `\r` 直接作为内容字节，无待定。

## 4. “立刻”的上限
引号内每字节 +1；`""` 两字节各 +1（QSEEN 读引号时也 +1）。字段长度在每字节处理时
同步递增比较，第一个超限字节即报错，不等闭合；字段数在每个逗号检查；记录数在每条
记录闭合时检查。错误即终态，后续 Feed 返回同一错误。

## 5. 状态转移表
FRESH=记录内尚无字段；BARE=未引号字段；QUOTED=引号内；QSEEN=引号内见引号；
CR=引号外见 CR 待定。类别：c=普通字节（非 `, " \r \n`）。
动作：start=开始字段；open=开引号字段；chunk=追加字节；end=闭合字段；row=闭合记录。

| 状态 | c | , | " | \r | \n |
|---|---|---|---|---|---|
| FRESH | start/chunk→BARE | start+end→FRESH | open→QUOTED | →CR | row（单列空记录）→FRESH |
| BARE | chunk | end→FRESH | err 非法引号 | →CR | end+row→FRESH |
| QUOTED | chunk | chunk | esc→QSEEN | chunk | chunk |
| QSEEN | err 闭后杂字符 | end→FRESH | chunk+esc→QUOTED | err 闭后杂字符 | end+row→FRESH |
| CR | err 孤立 CR | err 孤立 CR | err 非法引号 | err 孤立 CR | row→FRESH（CRLF） |

EOF：QUOTED/QSEEN 报引号未闭合；CR 报孤立 CR；FRESH 不产生记录；BARE 或字段待闭合
时 end+row。
