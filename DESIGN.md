# 只读快照一致性导出器 — 设计推导

## 1. 模型
- `store`：每键一条版本链。最新单元是 (ver, value)；历史是按版本升序的
  保留段 (ver, value)，其中 tombstone（nil 段）表示“当时不存在”，与空值 []byte{} 可区分。
- `snapshot`：句柄记一个单调水位 `snapVer`，注册进 store 的活跃快照集合。
- 读 `snap.Get(k)`：先在该键版本链上二分找 `ver <= snapVer` 的最大单元；
  找到 tombstone 视为不存在。因此快照只读不超过自身水位的数据。

## 2. 写时复制（COW）触发点
- 写 `Put(k,v)` 时，在互斥区内：若该键当前单元 `cur.ver <= 最老活跃快照水位`，
  则它对至少一个快照仍应可见，必须先把 `cur`（含其版本号）压入该键的保留段，
  再安装新单元。删除同理，压入 tombstone。
- 为什么按**最老快照水位**判断：它是活跃快照中要求看见的最旧数据。
  `cur.ver <= oldest` 时，最老快照必然需要 cur；一旦 cur 对最老快照不再需要
  （cur.ver > oldest），则对所有更新快照也不需要。所以一个判定即可覆盖全部快照，
  且每个被覆盖单元至多保留一份——重叠快照共享旧值而非各存一份。
- 导出读 key=80 时走的是它版本链上 `ver<=snapVer` 的单元；导出途中第 80 键被改写，
  旧单元已被保留，故导出读到的仍是快照时刻的旧值。

## 3. 保留值释放条件（为何按最老快照）
- 快照 Close 时从活跃集合摘除；随后新的最老水位升高（或集合变空）。
  对每键保留段，丢弃 `ver > newOldest` 的段（它们只服务于刚关闭的更老快照），
  仅保留 `ver <= newOldest` 的最大一个段作为新锚点；无活跃快照则清空整链历史。
- 两个重叠快照 s1(v100)、s2(v110)：写在 v100 前的旧值被保留，s1 关闭后
  newOldest=v110，`ver<=100` 的旧值不再需要才释放；故只有**两个都关闭**
  （集合空）后计数归零。保留段只随最老水位推进而缩短，证明释放只依赖最老快照。
- 内存：保留单元仅在“被覆盖且被某快照需要”时产生，数量 ≤ 导出期间被改写键数，
  与总键数无关。

## 4. 导出顺序无关性
- 清单的总校验和定义为：对每条 (key,value) 记录按 key 升序规范化后做 CRC32，
  与字节写出顺序无关。字典序/逆序导出只改变块内字节布局，规范化校验和相同；
  一致性由快照版本水位保证，与遍历顺序无关。

## 5. 分块、续传与快照过期
- 文件布局：`MANIFEST(固定长度 JSON + 魔数)` 后接若干块。每块自描述：
  magic(4)+index(4 u32)+datalen(4)+crc32(body)(4) 头 16 字节，后接 body，
  body 由多条 `klen u32 | vlen u32 | k | v` 组成。块大小是 body 上限。
- 续传：凭清单与仍开放的快照，从第 k 块的起始键偏移继续写并截断其后内容；
  前 k-1 块字节不动。逐块格式确定 ⇒ 任意 k 续传结果与一次性导出逐字节相同。
- 快照过期：快照 Close 后水位不再受保护（版本链可能已被修剪）。若允许用当前
  数据续写，前缀来自 t0、后缀来自 t1，拼出跨时刻文件。故续传先查快照活跃性，
  关闭即返回 ErrSnapshotExpired，**不触碰已导出部分**（不重写/不删除文件）。
- Close 与导出竞争：Close 等待在引用计数 WaitGroup 上，所有进行中的导出结束
  才返回；故不会出现“句柄已释放而文件写一半被当成完整”。

## 6. 错误分类（均可用 errors.Is）
- ErrManifestIncomplete：清单区不足或魔数不符；ErrChunkHeaderIncomplete：头不足；
- ErrChunkBodyIncomplete：体不足声明长度；ErrCRC：在体后 4 字节 CRC 区内被截断
  （尾校验字节缺失，无法确认完整，保守按 CRC 不符）或 CRC 不符；
- ErrChunkMissing/ErrChunkOrder：块号缺号或与位置不符（指名块号）；
- ErrSnapshotExpired：续传时快照已关闭；ErrBadBlockSize：块大小为 0。

## 7. 资源约束
- 读取次数：每键仅一次 snap.Get，由计数器精确统计，等于键数。
- 内存驻留：导出只维护一块 body 缓冲，峰值计数器记录其最大容量，≤ 一块大小。
