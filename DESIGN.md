# 确定性跳表设计推导

## 1. 层高的确定性导出

导出函数（`key/key.go`）：对键做 splitmix64 风格的位混合 `mix64`，取混合值的二进制末尾零位数 `tz`，`Level = tz + 1`。

- **分布合理性**：`mix64` 是雪崩充分的 64 位混合器，输出近似均匀。`P(tz >= k) = 2^-k`，故 `P(Level >= k) = 2^(1-k)`：约 1/2 节点为 1 层、1/4 为 2 层……与随机跳表的几何分布一致，期望每上一层节点数减半，既不退化成链表，也不会全是最高层。
- **同键同层**：`mix64` 是纯函数，只依赖键的比特，不依赖插入顺序、随机源或已有结构，故同一键在任何构建过程中层高相同（不变量 2 的前提）。
- **重复元素**：同一键的重复副本必然得到相同层高，且在全序中彼此相邻、字段完全相同，因此它们在结构中的相对位置不影响"逐字段相同"的判定；跨度按"指针跨过的底层元素数"维护，与节点是否重复无关，不变量 1 不受破坏。

实测分布（顺序键 0..n-1，见 `cmd/demo` 与 `AUDIT.md`）：

| 元素数 | L1 | L2 | L3 | L4 | L5+ |
|---|---|---|---|---|---|
| 1000 | ~500 | ~250 | ~125 | ~62 | ~63 |
| 100000 | ~50000 | ~25000 | ~12500 | ~6250 | ~6250 |

## 2. 删除时跨度的回收

删除节点 x 时，在 x 出现的每一层 i 执行 `pred.Span[i] += x.Span[i] - 1; pred.Next[i] = x.Next[i]`（`list/list.go`），即 x 的跨度**并入前驱**：前驱新指针跨过"原来到 x 的元素"加"x 原来跨过的元素"，减去被删的 x 本身，恰好等于新指针跨过的底层元素数。我们额外维护**指向 nil 的指针的跨度**（= 该节点之后到底层末尾的元素数），因此当 x 恰好是某层最后一个节点时，`x.Span[i]` 就是 x 之后的底层元素数，并入前驱后前驱的 nil 跨度仍然精确，该边界情形自然成立，无需特判。

## 3. RankOf 对不存在的键

选择：返回**它若插入会在的位置**（= 严格小于该键的元素个数，即 lowerBound）外加一个 `found bool` 标记是否命中。推导：不变量 3 要求 `Range(lo, hi)` 的计数等于 `RankOf` 差值且三者自洽。若对不存在的键返回错误，则 lo/hi 不存在时 `Range` 无法定义，违反自洽要求。改为 lowerBound 语义后，`Range(lo,hi) = upperBound(hi) - lowerBound(lo)` 对任意 lo/hi（无论是否存在）都给出闭区间内的元素个数；对存在的键，lowerBound 恰为第一个副本的序号（不变量 4），与 `At` 互逆（不变量 3）。

## 4. 迭代器失效的判定时机

选择**立即失效**（fail-fast）：`list` 维护一个随每次成功写操作递增的版本号，迭代器创建时记录版本，每次 `Next` 先比对版本，不一致即返回 `ErrInvalidated`（`iter/iter.go`），绝不返回半新半旧的序列。论证边界情形：若迭代期间被删的恰是迭代器当前指向的元素，删除本身会使版本号递增，迭代器下一次 `Next` 在解引用任何指针之前先发现版本不一致并返回 `ErrInvalidated`，因此不会读到已删除节点，也不会跳过或重复任何元素——行为完全可判定。
