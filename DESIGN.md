# 只读快照一致性导出器设计

## 1. 目标与边界

在键值存储持续变更时，导出某逻辑时刻 `V` 的一致只读快照到本地文件；
支持按块续传与完整性校验。只用标准库。

- 每次写入单调产生新版本；快照句柄固定版本水位 `V`。
- 快照读取只允许看到 `v <= V` 的最新值，看不到 `V` 之后的写入。
- 导出是快照的纯函数：任意键顺序、任意时间开始/续传，结果相同。

## 2. 写时复制（COW）的触发点

设当前写入版本 `w`。写入键 `k`（版本 `w`，`w > V`）前，若直接覆盖：

1. 导出在 `w` 之前读到 `k`，得到旧值；
2. 导出在 `w` 之后读到 `k`，得到新值；

于是同一次导出按读取时刻不同会跨越版本边界，快照不一致。

正确做法：**写入前**判断是否存在 `V < w` 的活跃快照。若存在，把该键
“快照版本可见的旧值”（含“不存在”标记，区别于空值）保存到该快照的
保留表 `retained[k]`，再更新主存储。快照读 `k` 时优先读 `retained[k]`，
未保留才读主值且要求主值版本 `<= V`。这样导出无论何时读到 `k`，都得到
`V` 时刻的值。导出进行到第 50 个键时改写第 80 个键，旧值已先入保留表，
故第 80 个键仍导出旧值。

## 3. 保留值的释放：为什么按最老快照判断

每个快照只保留“比自己新”的写入所覆盖的旧值。对一次写入 `w`，所有
`V < w` 的快照都需要该旧值；只需在**最老活跃快照水位 `oldest`** 上判断：

- 若 `oldest < w`，则该旧值对最老快照应保留。更年轻快照的读取委托给最老
  快照的保留表（读链：先查自己，再查最老快照，最后查主存储并限版本）。
- 若最老快照都 `V >= w`，则所有活跃快照都 `V >= w`，主值本身对它们可见，
  无人需要旧值。

因此保留值只挂在最老快照上。关闭最老快照时，把其保留表中对“新的最老
快照”仍不可见（即新值版本 > 新最老水位）的条目移交过去，其余可释放；
关闭非最老快照不产生保留值。两个重叠快照只有都关闭后，保留计数才归零；
先关闭年轻者计数不变，再关闭年长者后归零。句柄全部关闭后存储可安全
推进，无悬挂旧值。

## 4. 导出顺序无关性

导出的逻辑内容是 `{(k, valueAt(V,k))}` 的集合。总校验和定义为每条记录
`CRC32(k ++ 0x00 ++ len64(v) ++ v)` 的整数和模 `2^32`，**与顺序无关**；
文件分块只影响字节排布，manifest 记录键序列与每块边界。字典序与逆序导出
产生不同布局，但记录集合与总校验和相同；以“同一固定顺序再导出”即可做
逐字节比对，以总校验和比对“内容集合”。

## 5. 文件格式

单一文件：`blocks ++ manifest ++ [u64 BE manifestLen]`。

- 块：`[u32 BE blockLen][u32 BE blockNo][payload][u32 BE crc]`，
  `crc = CRC32(blockNo ++ payload)`；payload 由多条
  `[u32 kLen][k][u32 vLen][v]` 组成，块 payload 不超过块大小。
- manifest（JSON，末尾 u64 定长）：版本水位、块号、键序列、每块偏移/长度、
  总记录数、总 payload 字节数、总校验和。

末尾定长 trailer 使任意截断点可判定（见下）。

## 6. 续传与快照过期拒绝

manifest 固定后，块集合就是固定函数。续传只做：校验 manifest 存在、
快照仍活跃、水位与 manifest 一致、已有块的头/体/CRC 完好且块号正确，
然后从指定块重写后续块，绝不重新读取“当前数据”。

若原快照已关闭，其保留值已按第 3 节释放，主存储只含当前值；此时用当前
数据补块会把 `V` 时刻与当前时刻拼接成跨时刻文件。故续传必须先获取快照
活跃锁，失败返回可判定错误 `ErrSnapshotExpired`，且**不触碰**已写块文件，
已导出部分原样保留。

## 7. 截断点的可判定分类

从长度 1 逐字节截断到 `L-1`，从尾部读 u64 manifestLen：

- 末尾剩余 `<8`，或 `8+manifestLen > L`：manifest 区域不完整
  → `ErrManifestIncomplete`。
- 已完整 manifest，但某块 `[len][no]` 不足：`ErrBlockHeaderIncomplete`。
- 头完整但 `payload+crc` 不足：`ErrBlockBodyIncomplete`。
- 整块字节都在但 CRC 不符（如仅截掉紧邻 manifest 的 1–3 字节，破坏末块
  CRC 而 manifest 仍完整）：`ErrCRCMismatch`。

每个截断点恰落入一类；错误均为哨兵错误，可用 `errors.Is` 区分。块缺失/
乱序单独返回 `ErrBlockMissing`/`ErrBlockOutOfOrder` 并指出块号。

## 8. 资源约束

- COW 保留项数 = 导出期间被改写的不同键数，与总键数无关。
- 每条记录只 `Get` 一次快照：读次数恰等于键数。
- 导出逐块编码，驻留缓冲不超过一个块大小（+ 定长头尾），峰值按块统计。

## 9. 并发与关闭竞争

store 用 RWMutex 保护 map 与活跃快照集合；快照关闭与写入在同一临界区完成
保留表移交。导出持有快照的活跃读引用，关闭等待在途导出结束；若快照已
关闭才开始续传，则立即得到 `ErrSnapshotExpired`，不会留下被误认为完整的
半截文件（无完整 manifest 即非完整导出）。
