# 只读快照一致性导出器设计

## 1. 目标与不变量
在键值数据持续变更的同时，导出某一时刻 T 的一致快照；导出文件分块、自描述、
可从任意块续传，并可逐字节校验。核心不变量：**导出结果只反映快照水位版本
`vsnap` 可见的键集合与值，与导出期间的任何写入无关，与导出顺序无关。**

## 2. 写时复制（COW）的触发点
存储维护 `data map[string]string` 与单调版本号。快照 S 在 `vsnap` 打开，并冻结该
版本可见的键集合 `keys`。此后写线程对键 k 执行 `Put(k,new)` 时，若直接覆盖
`data[k]`，而导出稍后才读到 k，就会读到新值——快照被污染。

正确做法：写入路径在**覆盖之前**通知所有“打开中且尚未见过该键新值”的快照，把
`vsnap` 可见的旧值（删除则保留墓碑 `(nil,true)`）拷贝进该快照的 `preserved` 表；
之后才更新 `data`。导出读 k 时优先查 `preserved`，未命中才读 `data[k]`。于是：
- 键在快照后第一次被改写：旧值被保留，导出读旧值；
- 同一键被反复改写：仅第一次触发保留，后续改写与该快照无关，保留表每键至多 1 项；
- 快照后新增的键不在冻结键集合中，导出根本不读它。

题设场景（导出到第 50 键时改写第 80 键）：第 80 键旧值在写入瞬间进入保留表，
导出到第 80 键时命中保留值，结果仍是快照旧值。

## 3. 保留值何时释放：为什么按最老快照判断
快照关闭时，其 `preserved` 整体丢弃、计数归零。多个重叠快照时，一个写入必须通知
所有仍打开、且水位早于该写入的快照。释放条件由**当前最老打开快照的水位
`voldest`** 决定：任何版本 `v >= voldest` 的写入，其旧值都可能被某个仍打开的快照
观察到，写入发生时必须保留；只有当最老快照也关闭后，它保留的旧值才整体释放，
之后更年轻的快照关闭即各自释放。两个重叠快照 S1、S2 中，S2 先关闭不影响
S1 的保留值；S1（最老）关闭后 S1 保留值释放，S2 仍持有的只与 S2 有关，
S2 关闭后归零。实现上每个快照独立持有保留表，并共用一个全局保留项计数器。

资源上界：保留项数 ≤ 导出期间被**不同**改写键的数量（每键每快照至多 1 项），与
总键数无关。10 万键中只改 100 个，则计数器 ≤ 100；全部键都改则等于键数。

## 4. 导出顺序无关性
总校验和定义为每条记录 CRC32（IEEE）的**异或**：`Σ = ⨁ crc32(record_i)`。
异或满足交换律与结合律，且记录内容只由 `(键, 快照可见值)` 决定，与遍历顺序无关。
因此字典序、逆序导出得到同一组记录，总校验和必然相同。分块只影响字节切分位置，
不影响记录集合与校验和。为保证续传重建出**逐字节**相同的文件，清单固定记录顺序
（字典序）与块切分参数。

## 5. 文件格式（单文件，追加 trailer 清单）
块：魔数 `COW1`(4) | 块号 uint32BE | 体长 uint32BE | body | crc32(头‖体) uint32BE。
记录（body 内串联）：keyLen uvarint | valLen uvarint | key | val；删除记录不导出，
valLen=0 即空值（与键不存在可区分）。
trailer：魔数 `MNF1`(4) | JSON 清单 | jsonLen uint32BE | 魔数 `MNF1`(4)。
清单含快照版本、键数、块大小、块数、各块 CRC、总异或校验和。末尾定界，无尾部
魔数即“清单不完整”。

## 6. 分块、内存上界与续传
逐键流式编码进一块缓冲，达块大小即落盘并重建缓冲，任一时刻驻留 ≤ 一块大小
（峰值计数器断言）；读取严格每键一次。块大小为 0 返回 `ErrInvalidBlockSize`。
续传凭清单定位：已存在文件中完整且 CRC 通过的块 k 被跳过，从 k+1 起重写后续块，
得到与一次性导出逐字节相同的文件。

## 7. 快照过期为何必须拒绝续传
续传本质是“用同一水位的旧视图补齐剩余块”。若原快照已关闭，保留值已释放，继续
读只能得到当前数据，会把 T 之后的新值拼进 T 时刻文件，拼出跨时刻的不一致文件。
因此续传句柄失效时返回 `ErrSnapshotStale`，且**不删除、不截断**已导出部分。
并发关闭与导出：关闭等待在途导出结束；关闭后开始/继续的导出收到该错误。无
trailer 的文件永远不会被当作完整文件。

## 8. 校验与四类可判定错误
`errors.Is` 区分：`ErrManifestIncomplete`、`ErrChunkHeader`、`ErrChunkBody`、
`ErrChunkCRC`、`ErrChunkOutOfOrder`（指名块号）、`ErrSnapshotStale`、
`ErrInvalidBlockSize`。截断点按字节位置落入：trailer 区→清单不完整；块头 12
字节内→块头不完整；块体内→块体不完整；块尾 CRC 4 字节→CRC 不匹配（校验值被
截断，读出 CRC 不可能等于实算值）。
