# CSV 解析器设计（module ontology / Go 1.26）

## 1. 关键决策（推导）

### 1.1 空行与单列空值
记录由「换行」结束。文件末尾的换行若已完整结束一条记录，不再凭空补记录；
两个相邻换行中间长度为 0：它既无字段也无引号标记，无法表达任何 `Cell`，
因此**空行被跳过**（`\n\n`、`\r\n\r\n`、首行空行同理）。
推论：单列 CSV 中 `a\n\nb\n` 只产出两条记录。那么单列空值记录（`""\n` 或
无尾换行的 EOF）必须与空行可区分——引号标记承担区分。但无尾换行且全文件为空
（长度 0）时，无法判断是「零记录」还是「一条未引号空字段记录」，约定为零记录。
为保证往返：单列表中唯一字段为空时，回写器**必须**写成 `""`（加引号）；
若不加引号，输出是空串（零记录）或空行（被跳过），数据丢失。多列记录中的空
字段保持未引号（如 `a,,b`），因为列结构已由逗号锚定，无需引号也能往返。
未引号的单列空记录只可能出现在无尾换行 EOF；解析器把该形态也判为合法空表
（零记录），故回写器侧不会产生需要它的输入。

### 1.2 par 切点：双假设
段 worker 无法知道起点是否在引号内。每段分别以「起点在引号外(S)」和
「起点在引号内(Q)」各解析一遍，得到两种终态、事件流、错误与字节计数。
段间真实状态由前一段终态决定：前段终态在引号外 → 本段取 S 分支；在引号内
（含 Q/A/C 等子态）→ 本段取 Q 分支。首段强制 S。
拼接时携带前段未闭合字段的（前缀值、起始偏移、引号标记）与段内首字段合并：
若段内首事件是字段结束，用前缀+其值、前段起始偏移生成一个字段事件，字段计数
只加一次；随后事件直接采用。记录数按「换行是否为真实行尾」逐段累计（空行
跳过不计数），得到本段相对记录基号；字段号按拼接后的记录内序号重排。
错误位置 = 段内偏移 + 段起点；记录号 = 相对记录号 + 全局记录基号；字段号同理。
引号内的 `\n`/`\r\n` 不结束记录，双假设终态天然把它们排除在行尾计数外，
因此拼接与单线程逐事件同构，结果完全相同；每字节最多被两套状态机处理，
总处理次数 ≤ 2N + O(K)。

### 1.3 CR 待定
未引号态见 `\r` 进入 CR 态：暂不发行尾。下一字节为 `\n` → 行尾；
为逗号 → 裸 CR 错误（孤立 `\r`）；其它 → 同错（`\r` 是字段内容仅在引号内）。
半包：CR 是状态机一个持久状态，`Feed` 边界正常挂起。par 切点落在 `\r` 后：
终态携带 CR 态，下一段首字节按上表判定；流恰在 `\r` 结束（Finish）→ 裸 CR
错误（无 `\n` 跟随）。引号内 `\r` 直接入值，A 态后见 `\r` 同未引号 CR 态。

### 1.4 上限「立刻」
字段字节在**追加前**计数：引号内每个到达字节（`""` 转义在第二个 `"` 处仅追加
1 个字节）使计数 +1，计数将超过 MaxFieldBytes 时，在该字节处理时立即报错，
不缓冲整条记录。字段数在每个 FieldEnd 时递增并立即对 MaxFieldsPerRecord 判定。
记录数在每条记录闭合（行尾/EOF）时立即对 MaxRecords 判定。报错即终态，
已产出的完整 FieldEnd/记录保留；后续 Feed/Close 返回同一错误。

## 2. 状态转移表

状态：F=字段开始 B=未引号中 Q=引号中 A=引号后见引号 C=CR待定。
字节类：`,` `\n` `\r` `"` 其它c。

| 状态 | , | \n | \r | " | c |
|---|---|---|---|---|---|
| F | FieldEnd→F | 空行跳过→F | →C | →Q | 存c→B |
| B | FieldEnd→F | RecEnd→F | →C | ErrQuote | 存c→B |
| Q | 存,→Q | 存\n→Q | 存\r→Q | →A | 存c→Q |
| A | FieldEnd→F | RecEnd→F | →C | 存"→Q | ErrCharsAfterQuote |
| C | ErrBareCR | RecEnd→F | ErrBareCR | ErrBareCR | ErrBareCR |

FieldEnd 发字段事件（值、起止偏移、引号标记）；RecEnd 先补 FieldEnd 再发记录边界。
EOF：Q → ErrUnclosedQuote；C → ErrBareCR；A/F 挂起字段在非空输入时补 FieldEnd；
F 且本记录未见过任何字段且无尾换行 → 零记录（空文件/空行尾），不发事件。

## 3. 包与 API 摘要
`cell.Cell{Value []byte; Quoted bool; Start,End int}`；End 为内容尾偏移（不含）。
`lexer` 导出 `Machine`（可拷贝快照）、`Event`、`Limits`、哨兵错误及位置错误类型；
`Feed/Close` 流式接口基于 Machine。`table` 组装 `[][]cell.Cell` + 表头，校验列数。
`writer` 最小引号回写（必要：含 `,` `"` `\r` `\n`，及单列空字段）。
`par.Parse(buf []byte, k int, lim Limits)` 并发双假设拼接。
单流式解析器实例非并发安全；不同实例互不共享状态。
