# DESIGN — 流式 UTF-8 ⇄ UTF-16 转码器推导

## 1. 非法单元切分规则（由七个样例反推）

规则：**maximal-subpart（最长合法前缀）**。从首字节查表得序列期望长度与第二字节合法区间；
逐字节校验，一旦某字节不在当前位置的合法区间内，已累积的合法前缀（至少 1 字节）即为一个
非法单元，输出一个 U+FFFD，**当前字节不被吞掉**，作为新单元的首字节重新判定；流结束时
未完成的合法前缀整体算一个非法单元。

验证样例：`F0 90 80 41`：F0 要求第二字节 90..BF，90、80 合法，41 不在 80..BF → 前缀
`F0 90 80` 是一个单元，41 重判为 ASCII → `FFFD 41`。`E0 80 80`：E0 要求第二字节 A0..BF，
80 越界 → E0 单独成单元；80、80 各自是非法首字节 → 三个。`ED A0 80`：ED 要求 80..9F，
A0 越界 → 三个。`C0 AF`：C0 恒非法 → 各一个。`F4 90 80 80`：F4 要求 80..8F，90 越界 →
四个。`E2 82`+EOF：合法前缀被截断 → 一个。`80 80`：两个非法首字节 → 两个。

第二字节合法区间表（其余后续字节恒为 80..BF；「吞」= 第二字节非法时本单元吞掉的字节数）：

| 首字节 | 第二字节区间 | 吞 | 首字节 | 第二字节区间 | 吞 |
|---|---|---|---|---|---|
| C2..DF | 80..BF | 1 | F0 | 90..BF | 1 |
| E0 | A0..BF | 1 | F1..F3 | 80..BF | 1 |
| E1..EC | 80..BF | 1 | F4 | 80..8F | 1 |
| ED | 80..9F | 1 | 80..BF | —（非法首字节） | 1 |
| EE..EF | 80..BF | 1 | C0 C1 F5..FF | —（非法首字节） | 1 |

区间不同正是为了排除非最短形式（E0<A0、F0<90）、代理区（ED>9F）与越界（F4>8F）。
后续第 i 字节越界时吞 i-1 字节（合法前缀），越界字节重判。

## 2. 切分缓存上限与放弃等待的时机

合法前缀最长 3 字节（四字节序列缺最后一字节），故 UTF-8 缓存上限 = **3 字节**；UTF-16
为 1 个奇数字节 + 1 个高代理（2 字节）= **3 字节**。任一新字节到达时若不在当前位置的合
法区间内，可**立即**判定前缀非法（区间表是充要条件，后续字节不可能使其变合法），不必
等流结束；只有「目前仍是合法前缀」时才需要等。

## 3. par 切点对齐

切点 s 的对齐：从 s-1 向前最多回看 3 字节，找到最近的首字节 q（跳过 80..BF）；q 不存在
或 q+len(q) ≤ s 则 s 本身是边界，否则边界取 q。任何单元内部只有首字节不是 80..BF，且单
元最长 4 字节，故 3 字节回看必然命中。对齐值单调（s1<s2 ⇒ align(s1)≤align(s2)）：若
align(s2)=q<s2，则 q..s2-1 间全是续字节，对任意 s1∈(q,s2] 回看也必命中同一 q。
对齐结果必是单线程解析的单元边界：边界两侧解析状态为空，而 UTF-8 单元的判定只需本单元
的字节，故每段 [align(s_i), align(s_{i+1})) 独立解析（段尾按 EOF 冲刷）与单线程逐单元
一致，拼接后输出与非法单元数完全相同。`C3|80 80`：切点 1 对齐到 0，C3 不会单独成单元，
`C3 80` 仍解析为合法字符，与单线程一致。末段边界强制为 N（截断由 Close 处理）。

## 4. 输出超限断点（第 9 条）

字节只在**单元完整输出后**才计入「已消费」：pending 缓存里的字节不算已消费。`Write` 返回
n = 本次调用中新完成单元所吞的 p 的字节数；全局断点 = `Stats().Consumed`（恰为被卡住单
元的起始偏移）。调用方以 `Resume` 配置开新实例、从该偏移续传，跳过的只是未消费字节，故
拼接与不设上限一次转完逐字节相同。超限判定在发射前做，UTF-16 代理对 4 字节作为一次发
射，不会输出半个标量。
