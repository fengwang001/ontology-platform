# 采样剖析与热点归因设计

## 1. 采样模型
- 采样器按注入时钟的固定间隔运行；栈来源返回一条自根到叶的帧序列。
- 每次 tick 只决定一次：正常入树、丢样、空栈无效、异常间隔跳过。
- 忙窗口来源返回 `ErrDropped`，计入 `Dropped`，不写树。
- 时钟回拨或非正间隔跳过，计入 `BadIntervals`。
- 停止通过关闭内部停止通道完成；重复停止不 panic、不重复关闭。

## 2. 栈规范化
- 空栈拒绝，计入无效样本；非空栈保留从根到叶顺序。
- 相邻同名帧去重：递归仍可隔着调用帧出现，但完全相同的连续重复不增加深度。
- 最大深度 `max` 对深度 `d` 生效：`d<=max` 不截断，`d=max+1` 截到 `max`。
- 被截断样本增加截断计数；保留的第 `max` 帧带截断标记，代表其后未知后缀。

## 3. 调用树与 self/total
树节点是“调用路径”，键为帧名。插入栈 `f1...fk` 时沿路径逐边查找并构造，每帧比较一次；因此一条栈只进行 O(k) 次比较，与既有节点总数无关。

对每条栈，路径上每个节点都代表此刻位于其调用之下：
- `total(N)+=1`：节点 N 及其全部子树出现的样本数。
- 仅叶节点 `self(N)+=1`：采样计数落在该节点本身。
- 截断标记节点作为保留栈的叶节点，因此也接收该样本的 self。

恒等式：
1. `sum(self(N)) == acceptedSamples`；每条正常栈恰好给一个叶节点加 self。
2. `sum(total(N)) > acceptedSamples`（非单帧栈时）；祖先和后代都计入，故重复。
单帧栈时第二式取等号，因此 demo 用多帧栈断言“大于”。

## 4. 递归的函数级归因
同一函数 F 可在不同路径节点出现。节点级 total 为：
- 外层 F：统计整段子树，包含内层 F。
- 内层 F：统计内层子树，已包含在外层 F 中。
所以函数 F 的 total 不能把所有 F 节点的 total 相加，否则内外层重复。规则是只加**不被同函数祖先包含的最外层 F 节点**。对 `A→F→G→F→H`：
- 若样本落在 H，最外层 F total=1，内层 F total=1；
- 函数 F total 仍为 1，不是 2。
函数级 self 可以相加，因为每次栈样本仍只有一个叶 self，不存在自我重叠。

## 5. 热点与排除归因
- self 榜和 total 榜都对不可变节点快照做一遍扫描收集，再一次排序。
- 函数榜建树后只做扫描/排序，不重建调用树；非导出 `rebuilds` 恒为 0。
- “排除帧 E”在函数层级计算：读取每条调用路径时，移除所有 E 帧；样本 self 归属于移除后最后一个非 E 函数。total 采用最外层同函数规则。这样 E 自身为 0，调用者可见其下游工作。

## 6. 并发一致性
采样协程插入时持写锁；查询协程持读锁，先在锁内复制不可变快照，再在锁外排序。样本沿路径先创建/定位，再从根到叶更新 total，最后更新叶 self；查询只能看到插入前或插入后的完整快照，不会观察到 total 已加而父节点未加。

## 7. 落盘格式
文件为小端二进制：
- 头：魔数 `OPRF0001`(8)、版本 uint8、最大深度 uint32、已接受 uint64、截断 uint64。
- 节点记录：父索引 int32、深度 uint16、截断 uint8、帧长 uint16、帧名 UTF-8、self uint64、total uint64。
- 尾部 CRC32 覆盖文件中此前全部字节。
节点按先根顺序写出，因此任意完整记录前缀都无父缺失。读回逐条原子解码。

## 8. 截断分类与恢复
设完整文件长度为 L，截断后长度 n：
- 头未读完：`ErrTruncatedHeader`。
- 头部之后遇到不完整记录：`ErrTruncatedRecord`。
- 所有记录完整但 CRC 尾部缺失：`ErrTruncatedCRC`。
- 恰好读到完整 CRC 但值不符：`ErrCRC`。
前三者可用 `errors.Is` 区分。返回已完整恢复的最大前缀；若 CRC 不匹配也仍返回前缀供调用方检查。恢复树满足“恢复样本的 self 之和等于已接受样本数”。

## 9. 边界
零样本：归因结果为空，不报错。单样本、同一栈、深度 1、空帧名均合法；空栈无效。
