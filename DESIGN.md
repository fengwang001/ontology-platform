# RLE 设计

不变量：严格解码器接受的每个 `t` 必须满足 `Encode(Decode(t)) == t`（编码唯一，即规范形式）。

## 拒绝清单（逐条推导）

1. 显式次数 1（如 `1a`）：解码得 `a`，再编码为 `a != 1a`。
2. 次数 0（如 `0a`）：产出 0 个符号，任何 `Encode` 结果都不含空游程，无法回写 `0a`。
3. 前导零（如 `01a`、`00a`）：规范十进制无前导零；`01a` 解码为 `a`，再编码为 `a`。
4. 相邻同符号游程（如 `2a3a`）：`Encode` 输出最长游程，再编码为 `5a != 2a3a`；须在看到下一游程符号时才能判定。
5. `\x`（x 非数字非 `\`，如 `\a`）：`a` 的规范写法是裸 `a`，转义写法无对应编码产出。
6. 末尾孤立 `\`：不是完整游程，无符号可产出，且任何 `Encode` 结果都不以裸 `\` 结尾。
7. 末尾只有次数（如 `12`）：游程必须以符号结束，缺符号；`Encode` 不会产出它。
8. 非法 UTF-8：符号必须是 Unicode 码点；多字节序列中途截断同样在 `Close` 时按非法拒绝。

## 大次数取舍

次数以 `uint64` 逐位累加，乘 10 加一位前做上溢检查（`v > (MaxInt64-d)/10`），
超过 `math.MaxInt64` 直接拒绝（`ErrCountTooLarge`），不静默截断。
理由：输出本身受可配置总字节上限约束（默认 1 GiB，`WithMaxOutput`），
任何能成功产出的游程次数都不会超过其字节数，故 63 位足够；拒绝比引入大整数更简单且同样安全。
解码时按 4 KiB 分块重复写出符号，绝不按次数一次性分配内存。

## 复杂度

输入每个字节恰好经过状态机一次，流式切分只影响在多少个 `Write` 里处理；
计数器 `checked` 因此对 1 字节切分严格等于输入字节数。

## 语义–测试对照

| 语义 | 测试函数 |
|---|---|
| 1 往返 | _填充中_ |
| 2 规范/拒绝清单 | _填充中_ |
| 3 大次数与输出上限 | _填充中_ |
| 4 最长游程/不规范化 | _填充中_ |
| 5 跨切分点一致 + 计数器 | _填充中_ |
