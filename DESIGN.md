# 分片扇出查询的部分失败合并器 — 设计

## 1. 范围与模型

一个聚合查询扇出到 N 个分片。每个分片返回记录列表，每条记录有 `ID`、数值 `V`，
以及该分片声明的条数。分片可注入：延迟、永不返回（超时）、损坏（实际条数与声明不符）、
重复返回（同一分片两次投递同一批记录，模拟重试叠加）。状态仅在进程内存。

包划分：`shard`（接口与假分片）、`fanout`（并发扇出、上限、截止）、
`combine`（五种聚合合并与去重）、`confidence`（可信度与区间）、
`report`（报告）、`cmd/demo`（演示）。

## 2. 五种聚合在部分失败下的可信度

设成功分片集合 S，失败（缺失）集合 M。缺失分片可能贡献任意记录。

### Count / Sum：单侧下界

缺失分片只可能增加计数/总和，不会减少。
- 真实 Count = Count(S) + Count(M) ≥ Count(S)，故当前值是**下界**，标注 `≥ 当前值`。
- 真实 Sum = Sum(S) + Sum(M) ≥ Sum(S)（记录非负；空集合记 0），同样标注 `≥ 当前值`。
- 全成功时 M 为空，差量恒为 0，标注「精确」。

### Min / Max：方向相反的单边界

缺失分片可能含更极端的值，但不可能让已观测到的极端值"消失"（它是真实数据的一部分）。
- Min：真实 Min = min(min(S), min(M)) ≤ min(S)。当前值是**上界**，标注 `真实 Min ≤ 当前值`。
- Max：真实 Max = max(max(S), max(M)) ≥ max(S)。当前值是**下界**，标注 `真实 Max ≥ 当前值`。

### TopK：可信前缀

设最终列表按分数降序、ID 升序取前 K。缺失分片 M 最多能贡献的总"挤入量"为
`U = Σ_{m∈M} UpperBound(m)`，UpperBound(m) 为分片 m 中任何一条记录的分数上界
（本实现中假分片配置项 `Upper`；缺失即取 +∞，此时前缀为 0）。

关键论证：缺失分片向全局 TopK 中插入的、分数高于阈值 x 的记录最多有 U 个
（每条记录至少占 1 个单位上界，上界和为 U）。因此，观测列表中分数严格 **> U**
的条目，在任何缺失数据下都必定留在前 K 之内——能把它挤出前 K 需要 K 个比它更高的
未知记录，而未知侧高于它的条数 < U < 该条目分数对应的容量。

更精确地：对观测有序列表第 j 项分数 s_j，其入选确定当且仅当缺失侧严格大于 s_j
的记录数 < K（并列以 ID 升序打破，ID 最不利时也可论证，工程上采用更保守的
`s_j > U` 充分条件，避免依赖未知 ID）。故：
- 可信前缀长度 j = 满足 `s_j > U` 的最长前缀长度（列表本身降序，前缀连续）；
- 但这 j 项的**排名**在缺失数据插入后仍可能改变，故只保证"入选"，不保证名次；
- j 之后（含被挤掉的第 K 名）整段不可信。
- 全成功：U=0，全部观测项分数 ≥ 0 > 0 不成立——采用 `U==0` 特判为精确（j=min(K,len)）。

边界：U 很大（+∞ 或 ≥ s_1）→ 前缀 0；U 很小（< 所有 K 项分数）→ 前缀 K。

## 3. 资源约束

- 扇出用带缓冲信号量（容量=并发上限 c）限并发；非导出 `peak` 在持锁区
  `inflight++; peak=max` 记录历史峰值，测试读 `Peak()`。
- 截止时间：`context.WithTimeout` 派生 ctx。每个请求"获得令牌后"检查
  `ctx.Err()`，到期则不再发起（计入 `unlaunched`，`Launched()` 可查）；
  已在飞请求通过 ctx 取消，假分片在 select 中监听 `ctx.Done()`。
  Wait 在所有 worker 退出后返回，截止后一个很短窗口内必返回。

## 4. 故障与状态

分片状态枚举：`OK / Timeout / Corrupt / Duplicate`。
- 超时：ctx 到期分片未返回 → `Timeout`，错误 `ErrTimeout`。
- 损坏：`len(records) != claimed` → `Corrupt`，错误 `ErrCorrupt`，数据不并入。
- 重复：同一分片成功投递两次 → 第二次识别为 `Duplicate`（`ErrDuplicate`），
  合并按 `(shardID, recordID)` 去重，Count/Sum 与单次投递逐位相同。
- 全部失败：合并器返回 `ErrAllShardsFailed`；零分片返回 `ErrNoShards`；
  全部分片成功但均为空：合法精确结果（Count=0 精确），与全部失败可区分。

## 5. 确定性

所有按分片的归约都是可交换结合的加法/取极值；TopK 收集全部候选后统一排序
（分数降序、ID 升序）。报告中缺失/状态清单按分片 ID 升序输出。
因此任意到达顺序产出逐字节相同。空串分片 ID 合法。

## 6. 边界语义

单分片即普通精确路径；K > 候选数时返回全部（全成功仍标注精确）；
部分字段记录（如 V=0、ID 为空）合法参与。
