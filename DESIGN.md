推导与设计（非法字节规则 / 缓存 / par 对齐 / 上限断点）

# 非法单元切分规则推导

状态机逐字节：首字节 L 决定期望长度 n 与第二字节区间；后续字节必须落在 80..BF。
关键：**第二字节一旦落在非法区间，立刻判非法，且只吞掉首字节这 1 字节，当前字节作为新起点重新处理**；
第二字节合法后若第三/第四字节非法或缺失，则吞掉**已累积的整个前缀**（2 或 3 字节）作为一个单元。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF（续字节开头） | — | 1 |
| C0 C1 F5..FF（永不合法） | — | 1 |

E0 取 A0、ED 取 9F、F0 取 90、F4 取 8F 正好排除非最短形式与越界。
样例：`E0 80 80` 中 80 立即失败只吞 E0，两个 80 再各自独立 → 3 个 FFFD；`ED A0 80`（代理区）、
`F4 90 80 80`（越界）同理 3/4 个；`C0 AF` 是 C0、AF 两个独立单元；`80 80` 同理。
`F0 90 80 41`：90、80 均合法，第四字节 41 不是续字节，已累积的 3 字节前缀整体算一个单元，
41 重新作为首字节 → 1 个 FFFD 后接 A。`E2 82` 遇流结束：82 合法、前缀在等待第三字节，
结束时已累积的 2 字节作为一个截断单元 → 恰好 1 个 FFFD。

# 切分缓存上限与可立即判定点

最长字符 4 字节，任一时刻「已读未决」字节数 ≤ 期望长度-1 ≤ 3：合法标量一完整就立刻输出；
非法字节到达即判非法并立即 reprocess，绝不等待流结束。只有仍是**合法前缀**时才等待下一字节，
故缓存硬上限 = 3 字节（UTF-16：1 个奇偶字节 + 1 个待配对高代理，合计 ≤ 3 字节）。

# par 切点对齐

UTF-8 在切点 c 处向回最多走 3 个续字节找到首字节 L：用第二字节区间判定 c 是否落在 L 的
合法序列覆盖范围内，是则本段真正起点为 L（整单元归本段），否则起点就是 c。
中间段解码时额外向**下一段借看 1 字节**：段尾残留的合法前缀若能被该字节补全则继续，
否则整体作为一个非法单元（仅最后一段按流结束处理为截断）。于是每个单元恰好被某一段完整解析一次。
`C3 | 80 80`：回看 C3 覆盖 80，首段从 C3 起解析得到合法字符，次段只剩孤立 80 → FFFD，
与单线程一致。每切点只回看 ≤3、前看 1，不整段重扫，故检查次数 ≤ n + K·常数。
UTF-16 对齐到偶字节（回看 ≤1），并保留跨切点的代理对。

# 第 9 条断点

事件只在「整单元」边界产生；编码后若越过输出上限，该单元一个字节都不输出，解码机回退到
该单元起点，`Write` 返回的已消费字节数恰好等于此前已永久解析（已输出或已计非法）的字节数；
切分缓存中尚未解决的前缀字节**不算**已消费，随返回值留给调用方续传到新实例，拼接逐字节一致。
