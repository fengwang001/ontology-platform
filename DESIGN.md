# 结构化日志脱敏与采样设计

## 1. 边界与数据模型

- 只使用 Go 标准库；规则状态、规则计数器和采样计数器均保存在进程内存。
- 记录包含 `Fields map[string]any`、`TraceID string`、`Level int`；字段值只支持映射、切片、标量及 `nil`。
- JSON 同时作为规范编码和落盘载荷；空记录是合法记录，编码为 `{}`。
- 最大嵌套深度为 32，按“根映射为第 1 层”计数；构造和遍历都会拒绝超限结构。
- 映射通过实例身份检测自引用；发现环立即返回 `ErrCycle`，不做递归展开。

## 2. 字段路径与消歧

- 规则路径使用点号分隔嵌套段，例如 `a.b.c` 表示根键 `a` 下的 `b` 下的 `c`。
- 字段名中的点号必须转义：`\.` 表示字面量点号，`\\` 表示字面量反斜杠。因此字段名 `a.b` 的规则路径是 `a\.b`。
- 未转义的 `a.b` 永远表示嵌套路径；转义后的 `a\.b` 永远表示字面量字段名。两者可在同一记录中并存，不存在二义性。
- 路径缺失：不匹配、不报错。路径中间节点不是映射：不匹配、不报错。路径终止于映射或数组：按该整体值执行规则。
- 路径规则编译为分段 trie；遍历记录时沿当前字段栈移动 trie 节点，每条字段只做常数次查表。
- 值模式是已编译正则；递归遍历字符串字段，命中即按动作脱敏。路径规则和值规则都不会按规则数重复扫描字段。

## 3. 脱敏动作

- 替换：写入固定串 `***`，不保留原长度。
- 哈希：对原字符串计算 SHA-256，输出 `sha256:` 加十六进制摘要；同值同结果，输出不可逆且不等于原值。
- 截断：按 rune 保留前 N 个字符并追加 `…<truncated>`；空串仍为空串。
- 脱敏采用深拷贝后修改；切片、映射和标量递归处理。同值出现在不同路径、数组元素或被其他字段引用时，值模式会全部处理。
- 同一路径编译出多个互斥动作时，编译期返回 `ErrConflict`，错误包装两条规则索引；规则集不生成可执行脱敏器。

## 4. 一致性采样

设采样率为 `r`，追踪 ID 为 `t`。决策只计算：

```text
h = FNV-1a-64(稳定命名空间字节 + t)
保留链当且仅当 h mod 10000 < r * 10000
```

决策输入不包含记录内容、序号、到达时间或随机数，所以同一 `t` 的任意记录在任意进程、任意顺序下结果相同。若每条记录独立掷骰，同一链会被随机撕裂，因此不能使用 `math/rand` 或时间种子。

- 采样率 0：普通记录全部丢弃；采样率 1：全部保留。
- 空追踪 ID 和缺失追踪 ID 统一视作一个确定性伪链，键为空串；它不享受特殊保留率，所有空 ID 记录共同得到一个决策。
- error 及以上级别无条件保留。若其链的哈希决策为丢弃，记录保留但设置 `incomplete_chain=true`；其他情况不得设置该标记。
- 无论采样率是否为 0 或 1 都计算一次哈希，单次决策哈希计数恰为 1，因此是 O(1)。

## 5. 落盘格式与恢复

字节流采用小端整数：

```text
header  = "ONTLOG\x01" + 1 字节标志位（固定 8 字节）
frame   = uint32(payload_len) + payload + uint32(crc32_ieee(payload))
```

读取器顺序恢复完整帧。前缀截断分类为：头部不足 `ErrHeaderIncomplete`、长度前缀不足 `ErrLengthIncomplete`、声明体不足 `ErrRecordIncomplete`、CRC 字节不足或校验失败 `ErrCRC`。恢复结果始终是文件的最大完整帧前缀，不跳过坏帧猜测后续数据。

## 6. 并发与错误

- 采样器不可变且无写状态；脱敏器无共享修改，只返回新记录。
- sink 持有互斥锁，保证多协程写入的帧不交错。
- 可判定哨兵错误：`ErrCycle`、`ErrTooDeep`、`ErrConflict`、`ErrHeaderIncomplete`、`ErrLengthIncomplete`、`ErrRecordIncomplete`、`ErrCRC`，均通过 `errors.Is` 区分。
