# 设计推导

规范化分四层。行尾：`\r\n` 与孤立 `\r`、`\n` 都产出一个 `\n`。`\r` 到达时处于待定（CR），下一字节是 `\n` 则配对，否则立刻判为行尾。空白判定（ws）：空格/制表符累积为待定空白，遇到非空白非行尾字节则整串转正输出，遇到行尾或 Close 则整串删除。span 用有序非重叠区间 `{ostart,oend,istart,d}` 记录输出区间→原文区间，相邻保留区间立即合并，故区间数只随删除点增长。

## 1. 空输入 / 只含换行（末尾策略）

三策略定义：Keep=保留末尾换行原样（仅做行尾与空白规范化）；EnsureOne=有内容则末尾恰好一个 `\n`，无内容则空；TrimEmpty=删掉全部末尾空行（连续末尾 `\n`），有非换行内容则补一个 `\n`。选择「EnsureOne 对空输入输出空」而非 `"\n"`：后者是扩张映射（输出无原文字节可对应），破坏「输出每个偏移都有原文前像」且让 N("")≠N(N("")) 之外的映射性质变复杂；前者下 N("")="" 是不动点。两种选择都能幂等，但空→`\n` 方案需为纯插入维护空区间，且 N(N("")) 必须再返回 `"\n"`，可成立却不自然。纯换行 `"  \r\n"` 规范化后只剩换行，视为「无内容」，EnsureOne 与 TrimEmpty 都产出空（该 `\n` 是被删空行的行尾）。Keep 保留换行个数。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `""` | `""` |
| `"\n\n"` | `"\n\n"` | `""` | `""` |
| `"  \r\n"` | `"\n"` | `""` | `""` |

幂等：三策略输出都不含 `\r`、不含行尾空白；末尾换行数 ∈ {0,1}（Keep 下末尾连续 `\n` 已无尾随空白可删，再规范化原样不动），故 N(N(x))=N(x)。

## 2. 被删字节的 ToOut

不变量：ToOrig 单调不减；ToOut 单调不减；ToOut(ToOrig(o))=o。被删字节在输出中没有自己的位置，只能取相邻输出偏移。`\r\n`→`\n` 时输出 `\n` 的原前像必须是 `\n`（i=1），故 `\r`（i=0）取其后的输出偏移 1；即被删字节统一取「该删除区间右端在输出中的位置」：尾随空白 `ab   \n` 中三个空白都映射到输出中 `\n` 的偏移（行尾换行之前？之后？——取换行符自身偏移，即光标落在换行符上，语义为「行尾之后」一侧但仍指向该行的换行，前像逆射回 `\n`）。EOF 处被删尾随空白映射到输出终点 len(out)。理由：若取左端会与被删区间右侧保留字节的 ToOut 相同而 ToOrig 回不来；取右端则 o 指向紧随删除区的保留字节（换行），ToOrig(o) 指回它，互逆成立，且单调性迫使整串共享同一值。

## 3. 缓冲超限

强制判定待定空白为「非行尾」会在得知其后是 `\n` 时产生错误输出，违反第 4、5 条（不同切分点结果不同、幂等破坏），且无法回退映射，违反第 6 条。故选择拒绝：待定空白（或待定 CR 之后到判定前的累积，长度计原始字节）达到 `MaxPending` 即返回哨兵错误 `ErrPendingLimit`，带当前原文偏移；已输出内容与映射保留，Normalizer 进入终态（closed），此后 Write 返回 `ErrClosed`。拒绝点只依赖「待定长度」这一前缀状态，与切分方式无关，故第 4 条对「未超限」输入仍逐字节一致。输出超 `MaxOutput` 返回 `ErrOutputLimit`（偏移为将写出界的原文偏移），同样终态。严格模式遇 NUL 返回 `ErrNUL`（偏移为该字节），NUL 本身不进入输出。

## 4. par 拼接

不在字节中间赌状态：把 K 个目标切点向后推到「安全边界」——跳过待定空白与待消费的 `\n`，使每段都结束于一个已闭合行尾之后（最后一段到输入末尾）。该段以 Close 结束时无待定 CR、无待定空白，Keep 策略下段结果即全局结果的对应切片（换行已全部输出，跨段不存在未决内容）。边界搜索每字节只访问一次，总修正范围 ≤ n（上界为输入总长；无空白行时每段代价 O(1)）。并行：K 个 goroutine 各自对 `data[lo:hi]` 用 norm（非末段 Keep、末段用用户策略），得输出 `outk` 与映射 Mk。拼接：输出直接连接；映射区间坐标平移——每条 `(ostart,oend,istart,d)` 平移 `(Σ|outj|, lo)` 后加入全局表，段间相邻保留区间合并。由于安全边界恰好是两边规范化结果的拼接边界，平移后的区间无缝衔接，映射与单流完全相同。K 个 goroutine 经 channel 回收，无共享可变状态，-race 干净。
