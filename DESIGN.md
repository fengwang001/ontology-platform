# DESIGN.md — 游标分页器（keyset pagination）

## 1. 为什么游标必须是复合键 (Sort, ID)

全局顺序定义为：先比 `Sort float64` 升序；`Sort` 相等再比 `ID string` 字典序升序。
`(Sort, ID)` 因此构成全序，且 ID 唯一 ⇒ 无并列。

反例：只记上一页末行排序值 v=5，六行 Sort 都为 5（ID=a..f），页大小 3。
- 下一页取 `Sort > 5`：b..f 中未返回的三行被整批跳过（漏）。
- 下一页取 `Sort >= 5`：a..c 再次返回（重）。

正确规则：游标记末行复合键 k=(v,id)，下一页边界为复合键**严格大于**：
`Sort > v || (Sort == v && ID > id)`。比较基于全序，边界行本身永不重复，
其同 Sort 的后续行不会漏。每页末行复合键即下一页游标，直到取不满一页。

## 2. 反向翻页规则

方向只改**遍历方向与边界方向**，页内顺序始终按正向（Sort,ID 升序）排列：
- 正向 Forward：取 key > cur 的前 n 行，升序返回；游标指向本页末行。
- 反向 Backward：取 key < cur 的前 n 行（沿全序反方向数 n 行），再**翻转回
  升序**返回；游标指向本页首行（key 最小者）。

故第三页首行游标向后一页，得到的集合与页内排列都与正向第二页逐行相同，
而非其倒序。

## 3. 并发增删下的语义承诺

翻页是纯函数：同一游标 + 同一页大小 ⇒ 同一结果，无隐藏推进状态，可并发复用。
底层集合可并发增删，调用方看到的是某次 RLock 窗口内的一致切片：
- 行被删除：不出现，不报错；游标只依赖复合键位置，不依赖该行仍存在。
- 插入到**已翻过**区间（key ≤ cur）：允许不出现（不保证快照隔离）。
- 插入到**未翻**区间（key > cur）：随后续翻页正常出现，不跳过。
- 不变量：任何未删除的行，只要其 key 落在某页的开区间内就一定被取到；
  严格大于的开边界保证已返回过的行绝不再次出现。
插入/删除只影响相邻两页，不会让区间中部的行被跳过。

## 4. 游标格式（不透明字节串）

```
dir(1) | sortBE(8) | idLen(2 BE) | id(idLen) | crc32IEEE(4, 覆盖前面全部字节)
```
dir=1 正向，2 反向；空字节串 ⇒ 从头（仅正向合法起点）。
解码按序分类：长度不足 ⇒ ErrIncomplete；dir 非法 ⇒ ErrDirection；
CRC 不符 ⇒ ErrChecksum；正向游标用于反向（及反向用于正向）⇒ ErrCrossDirection。
四类错误均可 errors.Is。比特翻转必落入前三类之一，无一被接受。

## 5. 效率

有序切片上二分定位首个 key > cur（O(log N) 次比较），随后顺序取至多 n 行。
比较计数器 = 二分比较数 + 逐行比较数，单次翻页上界 n + ceil(log2 N)
（验收阈值给 4 倍裕量）。解码仅在校验通过时复制一次 id，分配数为常数 1。
