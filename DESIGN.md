# 可分裂二维网格索引 — 设计推导

## 1. 坐标模型

- 点 `Point{ID uint64, X, Y float64}`：ID 由网格单调分配，使同坐标点可区分、可删除。
- 单元格为**左闭右开**矩形 `[x0,x1) × [y0,y1)`；包含判定：`x0 <= x && x < x1 && y0 <= y && y < y1`。
- 矩形相交（含边界接触）：`a.X0 < b.X1 && b.X0 < a.X1 && a.Y0 < b.Y1 && b.Y0 < a.Y1`。
- 矩形完全包含格：`r.X0 <= c.X0 && c.X1 <= r.X1`（Y 同理）。
- `-0.0 == +0.0`（IEEE754），天然视为同一点；`NaN` 用 `x != x` 检出，`±Inf` 检出，均拒绝插入并计数（`Grid.Skipped()`）。
- 查询矩形可退化（`X0==X1` 或 `Y0==Y1`）：左闭右开下面积为 0，含边界接触的相交判定保证它只可能与格**接触而不相交**，故必返回空。这是合法结果。

## 2. 边界点归属（唯一性推导）

分裂线为父格中线 `mx=(x0+x1)/2`、`my=(y0+y1)/2`。四象限子格仍为左闭右开：

- BL `[x0,mx)×[y0,my)`，BR `[mx,x1)×[y0,my)`，TL `[x0,mx)×[my,y1)`，TR `[mx,x1)×[my,y1)`。
- 点归类规则：`x < mx → 左，否则右；y < my → 下，否则上`。

因此任意边界点恰好落入一个子格：

- 垂直分裂线上的点（`x==mx`）→ 右子格（BR/TR，由 y 决定上下）。
- 水平分裂线上的点（`y==my`）→ 上子格（TL/TR）。
- 两线交点（格中心）→ TR。
- 父格角点：左上、左下在 `[x0,…)` 内属于本格；右上、右下因右开不属于本格（属于右侧邻格）。即一个点在整棵树里仍只属于一个格。

查询边界：查询 `R=[rx0,rx1)×[ry0,ry1)`，命中条件同包含式。落在**左/下边界**（`x==rx0` 或 `y==ry0`）命中；落在**右/上边界**（`x==rx1` 或 `y==ry1`）不命中。

## 3. 分裂与终止条件（核心推导）

- 触发：叶格点数 `> cap`（容量上限）。分裂为 4 子格，把点按第 2 节规则全部下沉，父格转为内部格（自身不再存点）。
- **为何全同坐标会无限递归**：若所有点坐标相同，每次中点分割后该点都进同一个子格（`x==mx` 恒为右、`y==my` 恒为上 → 总进 TR），该子格点数不减，再次超容量 → 无限分裂 → 栈溢出。
- **终止条件（双保险）**：
  1. 深度上限 `maxDepth`：达到后叶格不再分裂，标记 `oversized`，容量成为软上限。
  2. 中点不可再分：`mx==x0 || mx==x1 || my==y0 || my==y1`（float64 在该尺度下已无表示精度），分裂不能产生更小区间，停止。
- 推导：全同点在深度约 53（float64 尾数 52 位）即触发条件 2；深度上限（取 40）更早触发。1000 个同坐标点共存在一个 `oversized` 叶格中，不丢点、不重复、可被点查与范围查询找到。

## 4. 删除与不合并的理由

删除按 ID 在定位到的叶格中移除。删除后**不合并**，理由：
- 合并/再分裂在容量阈值附近会随反复增删**抖动**，且需把四点重新上浮下沉、代价高；
- 本任务查询剪枝靠「跳过相离格 + 整取内含格」，稀疏内部格对剪枝无实质损害；
- 简化并发：结构只单向变深，读写锁下不会失效正在遍历的路径。

## 5. 查询剪枝

自根 DFS，对每个格与查询矩形求三态关系：

- **相离**：跳过（计入 visited，不计 pointTests）。
- **完全包含**：整取——内部格继续递归子格，叶格把其点**全部直接加入结果**（pointTests 加 0）。
- **相交（边界穿越）**：内部格递归；叶格逐点用矩形包含判定（计入 pointTests）。

复杂度：N=1e5 均匀点、cap 较大时总格数约 `4N/cap`；1% 面积查询只触达 `O(sqrt(格数))` 量级边界叶格，pointTests 远小于 N。计数器为 `query` 包非导出字段，经 `Stats()` 读出。

## 6. 持久化格式（自描述 + CRC32）

文件 = 文件头 + 前序记录流（父先于子，子按 BL,BR,TL,TR）：

- 头（小端）：magic `"ONGRID01"`(8) + version u16 + cap u32 + maxDepth u32 + root 四边界 float64 + 头 CRC32(4)。
- 记录：除 `crc32(4)` 外依次为 depth u8、flags u8（bit0=split，bit1=oversized）、x0,y0,x1,y1 float64、npoints u32，然后 npoints×`{id u64, x,y float64}`。内部格后面紧跟其子树。
- 写盘：写 `name.tmp-<rand>` → Sync → `rename` 原子替换 → 目录 Sync。残留 tmp 在 `Open` 时识别并清理。

截断分类（截断长度 L）：L 小于完整头长度 → **头部不完整**；某记录定长字段读不满 → **格记录头不完整**；点数区读不满 → **点数据不完整**；字段完整但 CRC 不符 → **CRC 不匹配**（按“整记录缓冲 + 解析”区分数据不完整与 CRC 不符）。恢复语义：返回已完整记录构成的最大前缀（split 父的子树只有全部完整时才接受该父），并附带错误；前缀部分上的查询结果等同于“只含已完整读回的格”。

结构不变量（独立于截断，单独构造文件）：父声称 split 但子缺失/数量不足 → 指出该格 depth 与路径；子格边界超出父格 → 指出格路径与越界维（x 或 y）。

## 7. 并发

`Grid` 用单把 `sync.RWMutex`：查询持读锁走整棵树；插入/删除/分裂持写锁。分裂在一次写临界区内完成“下沉全部点 + 挂好子格”，查询要么看到分裂前的叶（含全部点）要么看到完整子树（点已全在子格），不存在中间态，故不漏点、不重复。

## 8. 包划分

`geom`（点/矩形/包含/相交）、`cell`（格结构与分裂辅助）、`grid`（树、定位、增删、跳过计数）、`query`（DFS 剪枝 + 非导出计数器）、`persist`（Save/Open、截断分类、结构校验、tmp 清理）、`cmd/demo`。
