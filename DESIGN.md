# 设计推导

## 1. 空行与单列空值
空行（终止符到达时本记录字段数为 0）跳过，不产记录：否则末尾换行会产生幽灵记录（语义 4 禁止）。
故单列、值为空的记录必须写成 `""\n`：解析器对加引号的空字段产 1 个字段，记录得以保留；
不加引号的 `\n` 即空行会被跳过，数据丢失。规范输入（canonical）= 无空行、仅在必要时加引号、
记录间 `\n`、末尾无 `\n`。writer 对「单列且值为空」的字段一律加引号（无论原标记），
解析器从不产生单列不加引号空字段，故对解析结果引号标记仍逐字段相同。

## 2. par 切点
每段在两种入口假设下各跑一次：B=起点在字段边界（引号外）、I=起点在引号字段内部。
段尾出口归约为 B / I / C（引号闭合后待分隔）/ R（裸 `\r` 待判定）/ Q（带引号裸 `\r`）。
从左到右：上段出口决定本段真实入口——B 取 B 跑，I/C/R/Q 取 I 跑（C/R/Q 先看本段首字节定夺：
`,` 续新字段；`\n` 或 `\r\n` 收记录，CR 偏移属上段；其他字符（I 假设下首个 `"` 后）按
`"x` 规则报错；Q 后非 `\n` 即孤立 CR 错，R 后非 `\n` 同样错）。两种特殊裁决只看常数个字节，
之后直接复用整段预算好的 B 跑事件（丢弃其首个合成事件），故每字节最多两遍。I 跑不发首字段
StartField，字段由首个 B 段开启；段缝拼接时把"挂起字段"与 I 跑的 EndField 值首尾相接，
偏移按段基址平移（段内事件偏移为绝对偏移，直接生成）。记录号/字段号由 table.Builder 在事件
重放时从 1 连续编号，因此无需换算；错误偏移为字节绝对偏移。

## 3. CR 待定
未引号尾的 `\r` 进入 R 态：下一字节是 `\n` 则行尾（错误时位置记 CR）；是其他字节（含 EOF）
则孤立 CR 错误。半包续传：R 是持久状态，跨 Feed 保存，EOF 时判错。par 切点：R/Q 是段出口
模式，下段首字节裁决，逻辑同上，CR 偏移保留在上段坐标。引号内 `\r` 一律为内容（包括其后无
`\n`）。

## 4. 上限「立刻」
字段内容字节（引号字段含转义后的 `"`，即 `""` 对只计 1 字节；外围引号不计）逐字节累加，
每个内容字节到达即与 MaxFieldBytes 比较，超限立刻在该字节偏移报错，不缓冲。MaxFields 在
字段开启瞬间判定，MaxRecords 在记录完成瞬间判定。错误粘滞：终态后 Feed/Close 返回同一错误。

## 5. 状态转移表
状态：S0 字段开始 / S1 未引号中 / S2 引号中 / S3 引号闭合后 / S4 未引号CR待定 / S5 引号CR待定。
输入类：`,` `"` `\r` `\n` 其他。动作：F=字段内容，T=收字段并按字段数决定记录/空行。

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| S0 | 收未引号空字段→S0 | S2(quoted) | S4 | 0字段跳过;否则T→S0 | F→S1 |
| S1 | 收字段→S0 | 错误:裸引号 | S4 | T→S0 | F→S1 |
| S2 | F | 见到引号→S3 | F | F | F |
| S3 | 收字段→S0 | F(`"`)→S2 | S5 | T→S0 | 错误:闭合后杂字符 |
| S4 | 错误:孤立CR | 同左 | 同左 | T(CRLF)→S0 | 错误:孤立CR |
| S5 | 错误:孤立CR | 同左 | 补发挂起CR再入S5 | T(CRLF)→S0 | 错误:孤立CR |

EOF：S2/S3 为引号未闭合，S4/S5 为孤立 CR；其余状态字段数>0 补收末记录，=0 为空流。
事件：StartField(quoted,off) / Append(b) / EndField(off) / EndRecord(off,blank) / Error。
