# DESIGN — 带路径语义的通配模式匹配器

## 1. `x/**` 与 `x`、`x/`；`**/x` 与 `x`、`/x`

不变量：`**` 单独成段 = 「零个或多个整段」。路径按 `/` 切分为段序列，不做任何规范化，因此 `x/` 是两段 `["x",""]`（尾部空段是真实存在的段），`/x` 是 `["","x"]`。

- `x/**` = 段序列 `[x, **]`。路径 `x` 只有一段：`**` 取「零个段」，匹配成立。路径 `x/` 的第二段是空段，`**` 取「一个段（空段）」，成立。`x/y` 同理成立。
- `a/**/b` 匹配 `a/b`：`**` 取零段，`b` 与 `b` 对齐——与上面「`**` 可取零段」是同一条规则，不是特例。
- `**/x` 对 `x`：`**` 取零段，成立。对 `/x`：`**` 取一个段（空段），`x` 对齐第二段，成立。

结论：`x/**` 匹配 `x`、`x/`、`x/y`；`**/x` 匹配 `x`、`/x`。

## 2. 空串路径

空串切分为一个空段 `[""]`。`*` 是「段内零个或多个码点」，空段含零个码点，故 `*` 匹配空串。`**` 是「零个或多个整段」，取零段即匹配空串（取一个空段也匹配）。`?` 要求恰好一个码点，不匹配空串。空模式（零个原子的单段）只匹配空串。

## 3. 无回溯算法

- 段内：线性 glob。扫描时只记录**最后一个 `*`** 的回退点（星号位置 + 文本位置）；失配时回退到该星号并让它多吞一个码点。正确性：段内各 `*` 之间只隔着必须连续匹配的字面原子，任意早先的 `*` 多吞的码点，改由最后一个 `*` 来吞，后续字面原子的对齐位置完全相同——早先星号能到达的文本位置是最后星号可达位置的子集。因此只留最后星号不漏解；且每次回退文本指针单调前进，段内复杂度 O(原子数 × 段长)。
- 该论证**不能**照搬到 `**`：两个 `**` 之间隔的是整段字面段，扩展较早的 `**` 会整体平移中间各段的对齐，产生较晚 `**` 无法复现的对齐（段不可被逐码点吸收）。故跨段用**段级 NFA 状态集合**：活跃状态 = 模式段下标集合，`**` 段产生自环（消费一段）与 ε 闭包（零段），逐段推进。状态数 ≤ 模式段数，总复杂度 O(模式原子数 × 路径码点数)。
- engine 内维护非导出原子计数器，记录最近一次匹配的原子比较步数。

## 4. 规则集版本一致

`set` 持有指向不可变版本结构（规则切片 + 版本号）的 `atomic.Pointer`。`Replace` 先把全部新规则编译成完整新版本，任一失败则不交换、旧版本原样保留并返回错误；成功才一次性 Store。每次查询只 Load 一次指针，结论、规则原文、序号、版本号全部来自同一版本，绝不半新半旧。

## 5. 匹配结果表（✓=匹配）

| 模式 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x` | ✗ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ |
| `*` | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b` | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |
