# 随机超平面 LSH 分桶检索器 — 设计文档

## 1. 签名规则

固定维度 `d` 的 float64 向量。第 `l` 张表、第 `j` 位的超平面由单位化随机法向量
`w_{lj}` 表示（各分量 i.i.d. 标准正态，由显式 `int64` 种子经拆分的
`math/rand/v2` 序列生成；表 l 位 j 用子流 `seed + l*256 + j`，保证可复现且
跨表/跨位独立）。签名第 j 位：

```
bit_j(v) = 1            若 <v, w_{lj}> > 0
         = 1            若 <v, w_{lj}> == 0   （零内积约定为 1）
         = 0            若 <v, w_{lj}> < 0
```

零向量与任意超平面内积恒为 0，因此在所有表上签名均为全 1 位串（0xFF...），
这是一个确定性合法桶；零向量之间互为最近邻，不受影响。

## 2. 碰撞概率与召回推导

随机超平面 LSH 的经典结论：两向量夹角为 θ 时单比特相同的概率
`p(θ) = 1 − θ/π`。长度为 b 的签名完全碰撞（落入同一桶）的概率：

```
P_collision(b, θ) = (1 − θ/π)^b
```

设查询与其第 k 个真最近邻的夹角为 θ*。L 张相互独立的表中**至少一张表同桶**
（成为候选）的概率即单点召回：

```
P_hit(b, L) = 1 − [1 − (1 − θ*/π)^b]^L
```

**位数 b 的权衡**：b 增大使 `(1−θ*/π)^b` 指数下降：
- 非近邻（θ 大）碰撞概率下降更快 → 桶更细、候选更少；
- 但真近邻（θ* 小但非 0）被同桶接纳的概率也下降 → 单表召回下降；
- 表数 L 增大按 `1−(1−p)^L` 补偿召回，代价是候选数近似随 L 线性增长。

因此：**候选数随 b 单调下降；召回随 L 单调不降（1−(1−p)^L 对 L 单调，
L=0 时为 0，L→∞ 时为 1）；增大 b 降低单表召回，需要更大 L 补偿。**

## 3. 检索流程与复杂度

建索引：对每个向量、每张表计算一次 b 位签名（哈希计算计数恰为
`N×L×b` 次内积，逐位计），放入对应桶。复杂度 `O(N·L·b·d)`。

查询：在 L 张表中查签名桶，取并集候选，对候选逐一算精确欧氏距离并精排，
返回 TopK。距离精排计数 = 并集大小。期望候选规模
`≈ N·L·(1−θ̄/π)^b`（θ̄ 为平均夹角），b=8/L=8 在聚类数据上实测远小于 N。
精确最近邻基准：全量 `O(N·d)` 扫描，用于核对召回。

## 4. 并发与可见性

索引采用快照结构：建索引完成后整体原子替换只读快照（`atomic.Pointer`）；
查询只读取已发布快照，因此“未完成的表”对查询全不可见，建/查并发安全。
快照内 map 与切片发布后不再修改，`-race` 干净。

## 5. 边界与数据卫生

空集合可建索引但查询返回空结果；单向量、K>N 返回全部；全同向量
（含全零）落同一桶，候选=N，召回=1；d=1 合法。含 NaN 或 ±Inf 的向量
在建索引时被拒绝并跳过，累计计入可读出的跳过计数。维度不符返回
`ErrDimMismatch`（带期望/实际维度）。

## 6. 持久化格式

单文件、小端序、自描述：

| 区间 | 内容 |
| --- | --- |
| `[0,8)` | magic `LSHIDX01` |
| `[8,40)` | 头：d(8) b(4) L(4) N(8) 共 24 字节 |
| `[40, 40+8L)` | 每张表的 int64 种子 |
| 种子段后 | 法向量系数，L×b×d 个 float64 |
| 系数段后 | 桶表：uvarint 表数循环，每表 uvarint 桶数，桶为定长签名+sleb128 ID 数+uvarint ID |
| 末 4 字节 | 前述全部字节的 IEEE CRC32 |

读回逐段校验长度并分类错误：头不完整 / 超平面参数不完整 / 桶表不完整 /
CRC 不匹配（四类 `errors.Is` 可判）。任一截断都给出“最大可恢复前缀”：
解析到该段前的完整内容，桶表中引用了不存在向量 ID 的条目被剔除并计数，
恢复结果无悬挂 ID。法向量全零在加载时检出并指出第几张表第几位。

## 7. 可复现性

同种子 ⇒ 同一 `rand/v2` 字节序列 ⇒ 法向量逐位相同 ⇒ 签名表逐字节相同；
不同种子以高概率产生不同法向量，签名存在差异。测试对 5000 向量逐向量
断言同种子重建签名一致、异种子存在差异。
