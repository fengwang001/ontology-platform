# 媒体类型内容协商器 — 设计推导

包划分（依赖单向）：`mtype`（解析/规范化）← `rank`（单项匹配与特异性）← `negotiate`（择优与拒绝）。

## 1. 为什么先比 q 再比特异性

q 是客户端声明的相对偏好，特异性只是「哪一条 Accept 项负责给候选定 q」的依据，
不是独立的排序键。反例：`Accept: text/*;q=0.9, text/plain;q=0.8`，候选
`[text/plain, text/html]`。客户端明确把 `text/plain` 降级到 0.8、其余 `text/*` 给 0.9。
若先比特异性：`text/plain`（特异性 2）压过 `text/html`（特异性 1），返回的恰是客户端
刻意降权的类型——把「匹配规则的精确程度」误当成了「客户端的意愿强度」。正确结果：
先比 q，`text/html`(0.9) > `text/plain`(0.8)，选 `text/html`。因此每个候选的得分 =
匹配项中最特异项的 q；候选间先比 q，q 平才比特异性。

## 2. q=0 是拒绝；非法 q 整条报错

若把 `q=0` 当「最低优先级」：`Accept: text/plain;q=0`，候选 `[text/plain]`（唯一候选）。
「最低优先级」逻辑在无其他候选时会返回 `text/plain`——正是客户端显式声明不接受的
类型（等价于该返回 406 时返回了 200 + 客户端无法处理的内容）。故 `q=0` 一律视为拒绝：
候选的有效 q 为 0 时不可选，即使它是唯一候选，也返回「无法接受」。

非法 q（`q=1.5`、`q=abc`、`q=0.1234`）的处理选择：**整条 Accept 报错**，不静默丢弃。
理由：丢弃该项会悄悄改写客户端意图——若被丢的是 `q=0` 拒绝项，可能返回客户端明确
拒绝的类型；若被丢的是唯一接受项，会把「请求有误」误报成「无法接受」。畸形头部是
调用方 bug，应以可判定错误（`*mtype.Error`，`Kind == KindBadQ`，带项序号）暴露，
而不是替调用方猜测。测试 `TestParseErrors` 固定此行为。

## 4. 同分为何取服务端顺序

q 与特异性都相同时，客户端对这几个候选**无偏好可言**——Accept 的书写顺序不承载
语义（RFC 7231 也未赋予其意义，且中间代理可能重排）。此时唯一有信息量的偏好是
服务端自己的候选清单顺序（服务端把更省、更稳、更想给的类型放前面）。取服务端顺序
还有两个工程收益：结果只取决于服务端可控的输入，可复现；调用方调整清单顺序即可
调整默认输出。若改用 Accept 顺序，结果会随客户端序列化细节漂移，且语义上无依据。

## 5. 缺失与空串为何必须区分

二者语义相反：头部**缺失** = 客户端未表达任何约束，按 HTTP 惯例等价于 `*/*`（接受
一切）；头部**为空串** = 客户端发了一个没有任何条目的 Accept，即「没有任何可接受的
类型」，应判定无法接受。若合并处理：把空串当缺失，会把一次显式的「什么都不接受」
变成「接受一切」，返回客户端不要的类型；把缺失当空串，则所有未设 Accept 的普通
客户端全部被拒绝。故接口用 `*string`：`nil` = 缺失 → 按 `*/*` 处理；非 nil 空串 →
解析出 0 个 Accept 项 → 无候选可接受 → `ErrNotAcceptable`。两条路径在测试中用同一
候选清单分别复现，结果必须不同。

## 错误模型

解析错误统一为 `*mtype.Error{Kind, Index, Item}`，五类可区分：`KindNoSlash`（缺 `/`）、
`KindEmptySubtype`（子类型空）、`KindBadParam`（参数缺 `=`）、`KindUnclosedQuote`
（引号未闭合）、`KindBadQ`（非法 q）；`Index` 为问题项在 Accept 列表中的序号（0 起）。
无法接受是独立结果：`negotiate.ErrNotAcceptable`（`errors.Is` 可判定），绝不回退到
第一个候选，也绝不返回零值冒充成功。
