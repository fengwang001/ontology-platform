Design: 流式 UTF-8 ⇄ UTF-16 转码（带 U+FFFD 替换）

# 1. 非法单元切分（WHATWG 规则）
首字节决定期望长度 n（C2..DF=2,E0..EF=3,F0..F4=4）。第二字节必须落在
首字节专属区间（见 §5 表）；不合法：只吞首字节 1 个，立即判非法，不等待。
第二字节合法而第 k（k>=3）字节不是 80..BF：吞掉前 k-1 字节为 1 个非法
单元，非续字节作为新字符重新解析。首字节非法（80..BF、C0/C1/F5..FF）：
1 字节一个非法单元。据此：F0 90 80 41 第三字节越界→吞3；E0 80 … 第二
字节越界只吞 E0，后续 80、80 各为非法首字节→3 个；ED A0 80 同理（A0
对 ED 越界）→3；C0 AF→C0 非法 1 个、AF 续字节无首→1 个；F4 90→首+
第二越界共吞2，再 80、80→共4；E2 82+EOF：第二合法、缺第三，截断 1 个；
80 80→2 个。

# 2. 缓存上限与何时放弃等待
最长字符 4 字节，carry 只存「仍合法的前缀」，故上限 3 字节。第二字节越界
或续字节位上出现非续字节时，当字节到达即可判非法，绝不入 carry；只有
「前缀全合法但长度不足」才等待，EOF 时按 1 个截断非法单元处理。

# 3. par 切点对齐（固定 3 字节重叠）
块 i 拥有 [t_{i-1}, t_i)，t_i 为原始切点；解析区间 [start_i, end_i]，
start_0=0，start_i=t_{i-1}，end_i=min(N-1, t_i+2)：每块至多向后多读
3 字节。正常流式解析 start_i..end_i，输出过滤：单元/标量首字节位置
<t_{i-1} 一律丢弃（前块已拥有）；首字节位置 >=t_i 的合法标量也丢弃
（尾块例外，t_K=N，拥有到 N）。非法单元起始 off<t_{i-1} 归前块；
off>=t_i 归后块；落在 [t_{i-1},t_i) 归本块。跨切序列最长 4 字节，3
字节 lookahead 足够。C3|80 80：后块从 C3 起解析，C3 80 合法但首
<t_i 丢弃；80 首>=t_i 且为孤立续字节→1 非法；前块 C3 未闭合→截断
1，拼得 FFFD FFFD，与单线程同。F0 90|80 41：后块 F0 90 80 单元首
<t_i 丢弃，80(孤立续)→FFFD，41 首>=t_i 合法→41，即 FFFD 41。

# 4. 第 9 条断点
输出按整标量追加；追加前若 len(out)+编码长度>cap 则停，返回 ErrLimit，
n=「已完整输出标量对应的输入字节数（全局偏移）」。carry 中字节属于
已消费（已校验前缀）但对应标量未输出；调用方在新实例从 n 续传时这些
字节会被重新喂入，故 n 只统计已闭合标量，不含 carry。两段拼接字节相同。

# 5. 第二字节合法区间表（续字节通用区间 80..BF）
首字节        | 第二字节区间 | 第二字节非法吞 | 第k字节非续吞
C2..DF        | 80..BF       | 1             | 无第3字节(2B)
E0            | A0..BF       | 1             | 吞2
E1..EC        | 80..BF       | 1             | 吞2
ED            | 80..9F       | 1             | 吞2
EE..EF        | 80..BF       | 1             | 吞2
F0            | 90..BF       | 1             | 吞3
F1..F3        | 80..BF       | 1             | 吞3
F4            | 80..8F       | 1             | 吞3
80..BF        | 无(独立非法) | 1             | —
C0 C1 F5..FF  | 无(独立非法) | 1             | —
「吞 k」后，终止解析的字节作为新首字节重新解析（不吞）。

# 6. 检查次数与并发
每个输入字节主循环检查一次；仅跨 Write 的 carry（<=3B）重看一次，
总量 <=2N，碎切分不回扫。par 每块固定重读 <=3 字节，总量 <=N+8*3
（K<=8）。stream 实例非并发安全；par 内部 goroutine 各自独立实例。
