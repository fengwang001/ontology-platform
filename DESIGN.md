# 多区间 Range 响应组装器 — 设计推导

包依赖单向：`rangespec` → `coalesce` → `multipart` → `serve`；`source` 独立；`serve` 依赖全部。

## 1. `bytes=-0` 为何是"不可满足"而非"语法错误"

后缀形式 `-n` 的语义（RFC 7233 §2.1）是"资源末尾的 n 个字节"。该写法在词法上完全合法：
连字符 + 数字，解析器无需也不应当把它判成语法错误。
令资源总长为 `size`，后缀区间换算为普通区间：

    first = max(0, size - n), last = size - 1，长度 = min(n, size)

当 `n = 0` 时长度恒为 `min(0, size) = 0`，即"末尾 0 个字节"是一个空选择。空区间无法用
206 响应表达（416 的语义正是 Range 语法可解析但没有任何可满足的区间），所以它必须走与
语法错误不同的分支。解析层只做词法/语法判断，不知道 `size`，因此 `-0` 在 `rangespec`
层合法；在 `coalesce` 归一化时才结合 `size` 判为不可满足，并把 `size` 带在错误里，供调用方
生成 416 的 `Content-Range: bytes */size`。其它"起点越过末尾"（`a >= size`）同理。

两类错误用不同具名错误类型（`SyntaxError{Offset}` 与 `UnsatisfiableError{Size}`），
调用方可用 `errors.As` 彼此判定。

## 2. 为何"相邻"也必须合并

相邻指 `[a,b]` 与 `[b+1,c]`：字节集合严格不相交，但首尾相接、中间没有任何空洞。
multipart 封装的每个段都携带固定头部开销，且第 7 条要求"区间数为 1 时退化为裸字节"。
若相邻不合并，则一个连续区间 `[a,c]` 被客户端表达成 `a-b,c+1-d`（等价改写）时，响应会从
一个裸字节区间变成两段封装——输出取决于请求的书写方式而不是字节集合本身，违反
"合并前后覆盖的字节集合必须完全相同"这一以集合为唯一基准的原则；更直接地，第 7 条
"两个区间合并成一个之后应当退化为裸字节"的情形只有在相邻也合并时才能发生：例如
`0-4,5-9` 覆盖的就是连续的 `[0,9]`，正确响应必须是 10 个裸字节，而不是 multipart。
故合并条件为 `next.Start <= prev.End+1`。

## 3. 归一化算法与比较次数上界

算法（区间裁剪与排序合并）：

1. 把每个 spec 结合 `size` 裁剪：`a-b`→`[a, min(b,size-1)]`；`a-`→`[a,size-1]`；
   `-n`→`[size-min(n,size), size-1]`，其中 `n=0` 或 `start>=size` 记为不可满足。
2. 标准库 `sort.Slice` 按起点升序（同起点按终点降序）排序，比较器每被调用一次计数器 +1。
3. 线性扫描：当前合并区间 `cur`，对后继 `s` 做一次比较：若 `s.Start <= cur.End+1` 则
   合并（只扩大右端），否则输出 `cur`。每个后继恰有一次比较。

总比较次数 `C(n) = S(n) + (n-1)`，其中 `S(n)` 是排序比较次数。Go 的 pdqsort 对所有输入
保证 `S(n) ≤ n·⌈log₂ n⌉`（元素数 ≤12 的插入排序分支同样不超过该界），因此

    C(n) ≤ n·⌈log₂ n⌉ + n = O(n log n)

且该界与输入是否重叠、是否已排序无关（无两两比较的 O(n²) 路径）。
n=100 时上界 `100·7+99=799`；n=10000 时 `10000·14+9999=149999`。
实测比值（测试中打印）应与 `(10000 log 10000)/(100 log 100) ≈ 200` 同阶，
断言 `C(10000)/C(100) ≤ 250`。计数器为非导出字段，经非导出辅助函数暴露给测试。

## 4. 断点续写的状态与不变量

`Prepare` 阶段就把**最终响应体序列化为一个确定的 `[]byte` `body`**（裸字节或 multipart
字节），此后只读不变。续写状态只有一个：`off int`，即 `body` 中已被接收方确认写出的字节数。

写出协议：每次调用 `WriteTo(w)`（或 `Write`）尝试 `w.Write(body[off:])`。短写时 `Write`
返回 `n < len(body[off:])` 且无错误是合法的；组装器只执行 `off += n`，绝不重发、绝不改写
任何已确认字节，也不重新生成边界串。

不变量：对任意切分点序列 `0=k0<k1<...<km=len(body)`（接收方每次接受任意正数个字节），
接收方拼接所得恒等于 `body[k0:km] = body`。因为每次写出的就是 `body[off:]` 的一个前缀，
各段按 `off` 相邻拼接恰好覆盖 `[0,len(body))`，无重无漏——这与切分位置和次数无关。
测试遍历切分点 1..len(body)-1 逐字节断言。

数据源短读同理：`readFull` 循环 `Read` 直到目标区间读满；若 `Read` 返回 `(0, EOF)` 而
尚不足，返回可判定的 `io.ErrUnexpectedEOF`（经 `errors.Is` 识别），不静默截断。

## 5. 边界串规避策略与失败概率上界

边界由加密随机源生成：固定前缀 `ont` + 24 个 base62 字符。选边界后，组装器在**所有
将要出现在响应里的字节**（全部区间数据 + 每段固定头部 + 结束边界文本）中做子串搜索；
一旦候选边界出现就丢弃重抽，最多重试 `Limit.BoundaryRetries`（默认 100）次，超限返回
具名错误且不修改组装器已有状态。

失败概率上界：把每个内容字节位置视作一次"命中"事件。24 个 base62 字符若均匀随机，
该精确串从任一固定位置出现的概率 ≤ 62^-24（头部含非 base62 字符处概率为 0，故 62^-24
是保守上界）。令响应总字节 ≤ `B`，并集界（union bound）给出一次抽取命中的概率
`p ≤ B·62^-24`。取极端 `B = 10^9`：

    p ≤ 10^9 / 62^24 ≈ 10^9 / 1.04·10^43 ≈ 9.6·10^-35

100 次重试全部失败 ≤ p^100，量级约 10^-3393，实际可视为 0。即便随机源不均匀，"扫描后
重抽"这一步是确定性兜底：内容有限时只要存在一个不出现的候选就必能选出；只有内容长到
包含所有候选（需要远超响应上限的字节数）重试上限才有意义。
测试构造内容中包含与最终边界仅差一字符、以及大量"伪边界前缀"的资源，断言最终边界绝不
与内容冲突。

## 附：资源上限与查询

- `Limits{MaxRanges, MaxTotalBytes, BoundaryRetries}`，零值 `New` 填默认值。
- 超限检查全部在状态提交之前完成（先算出最终 body 再一次性赋值），故拒绝后组装器仍是
  未 Prepare 的零值状态；三类超限是三个具名错误，可彼此判定。
- `Snapshot()` 返回归一化区间、响应总字节、已写字节、是否 multipart 的值拷贝；纯读、
  不推进状态，连查两次结果逐字段相等。
- 单组装器不支持并发 `WriteTo`（文档注释写明）；不同组装器之间无共享可变状态，
  `go test -race` 下并发组装互不干扰。
