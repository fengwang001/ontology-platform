# 设计推导

## 1. 末尾换行

先做与策略无关的规范化：删除行尾空格/制表符，把 `\r\n` 和单独 `\r` 变为 `\n`。
- `Preserve`：保留原文是否有 0、1、多个末尾换行。
- `EnsureOne`：非空输入最终恰好一个 `\n`；空输入仍为 `""`。若空输入也插入换行，虽仍幂等，但会把“零个字节”解释成“一行”，改变空文件长度；这里选择空文件不产生行。
- `TrimBlank`：仅在原文出现过行尾时保留一个 `\n`；没有行尾的非空文本不新增，末尾所有空行折叠为一个换行。

| 输入 | Preserve | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三种策略的输出都只含规范行，再次应用不会改变字节，故幂等。

## 2. 被删字节的映射

设输出为 `"a\n"`。被删的行尾空白和 `\r\n` 中的 `\r` 都位于换行符前。若把这些原文字节映射到换行符之后，`ToOut` 会越过 `ToOrig` 给出的换行偏移，破坏 `ToOut(ToOrig(o))==o`；映射到删除区起点又会使一串被删字节早于换行边界。因此被删区间采用零宽输出区间，其每个原文字节映射到该零宽区间的输出边界，即下一个换行符之前；EOF 处则映射到输出终点。编辑器光标落在被删空白中时，应停在行尾换行符之前。插入的末尾换行以原文终点为零宽原文锚点，保证其反向映射再正向映射仍回到该换行。

## 3. 空白缓冲超限

强制把待定空白输出为普通空白，会在后续看到行尾时无法收回，违反行尾空白删除与幂等。因此选择拒绝：空白串达到上限仍未判定时，在当前原文偏移返回 `ErrWSBuffer`，已输出字节和映射保留，规范化器进入终态；后续写入返回 `ErrClosed`。该状态表示“前缀已规范化、后续未接受”，可解释且不伪造结果。

## 4. par 拼接

任意切点可能落在一整段待定空白或 `\r|\n` 中，不能让工作段从任意字节独立开始。每个并发段先把自己的起点推进到“最近的已确认行边界之后”：即起点之后第一个 `\n` 之后；找不到则到 EOF。相邻段使用排序后的唯一边界，因而每段都由完整行组成，不会携带待定 `\r` 或空白。可能产生空段，也可能某段覆盖多个原始切点；K 个 goroutine 仍各自处理独立区间。修正只需要回退/前进到当前切点所属行的下一个换行，扫描量有段大小上界，总输入仍线性。拼接时把段内原文坐标加全局原文起点、输出坐标加前面各段输出长度；段间零宽删除区间自然相接。
