# 严格模式流式 Base64 设计说明

## 第 2 条推导：规范形式

Base64 把每 3 字节（24 bit）编成 4 个 6-bit 字符。末尾不足 3 字节时：

- 剩 1 字节（8 bit）：前 6 bit 为第 1 字符，**低 2 bit 补 0** 为第 2 字符，再补 `==`。
- 剩 2 字节（16 bit）：6+6 之后**低 4 bit 补 0** 为第 3 字符，再补 `=`。
- 剩 0 字节：无尾部组、无填充。

所以合法尾部组只有三种形态：`XXXX`、`XXX=`、`XX==`；且非规范尾部的判定比特是
**`XX==` 时第 2 个字符的低 2 位、`XXX=` 时第 3 个字符的低 4 位**，必须全为 0。

`QR==`：`R` 的值是 17 = 0b010001，低 2 位为 01 ≠ 0。宽松解码器直接丢弃这 2 个
填充位，仍解出 `A`——于是 `QR==` 与 `QQ==` 映射到同一字节串，解码不再单射，
「对任意字节串，编码结果唯一」被破坏，严格模式必须拒绝。`QUJ=` 同理（`J`=9，
低 4 位非 0）。`QQ==`/`QUI=` 的相应比特全为 0，合法。

统一规则：**输入必须等于其解码结果的唯一规范编码**。推论：填充只能出现在整个
流的末尾且数量由余数唯一决定（`QQ`、`QQ=`、`QQ===` 均非法）；填充组之后不得
再有任何内容（`QQ==QQ==` 非法）；空输入合法，输出长度 0。

## 语义与测试对照表

（待测试写完后补齐）
