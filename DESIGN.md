# 可撤销批量重命名设计

## 1. 数据模型与边界
- `name.Namespace` 是 `map[string]struct{}` 的加锁集合；空串是合法名字，`/`、`\`、NUL 之外不设业务字符。为保证日志可逆，日志层对字节做 base64 编码。
- `plan.Request{From,To}` 表示一次请求；`plan.Step{From,To}` 表示实际一步。破环后的临时名也是普通步骤。
- 批量执行期间持有命名空间互斥锁；并发 `Add/Remove/Move` 阻塞到批量结束，而不是看到中间态。

## 2. 四类冲突
设 S 为“真正搬走”的旧名集合，即排除 `x→x`；T 为所有目标名。
1. 旧名不存在：`From` 不在初始集合，包装 `ErrMissingSource`。
2. 目标已占用且不会搬走：目标在初始集合且不在 S，包装 `ErrTargetExists`。
3. 同一旧名两次：相同 `From` 对应不同 `To`，包装 `ErrDuplicateSource`，错误带两个新名。
4. 两个请求指向同一新名：相同 `To` 有两个不同 `From`，包装 `ErrDuplicateTarget`，错误带两个旧名。
检测只读取快照并构造 map，不执行任何增删；任一冲突都拒绝整批。

## 3. 自环语义
`a→a` 定义为无操作：它不占用临时名、不产生日志步骤，也不会把 `a` 视为“会搬走”。因此 `a→a` 单独存在总是成功；若另有 `x→a`，仍按“目标已存在且不会搬走”冲突处理。

## 4. 安全顺序推导
请求形成有向边 `From→To`。若边 `u→v` 的目标 `v` 也是另一条边 `v→w` 的旧名，则必须先搬走 `v`，即 `v→w` 先于 `u→v`。所以依赖方向是“目标的出站边在前”。
算法从当前占用集合开始：
1. 目标当前为空的步骤可执行；链尾最先执行。
2. 每步完成后释放 `From`、占用 `To`。
3. 因某个名字被释放而变得可执行的步骤进入最小堆，同层按 `From` 字典序选择。
因此 `{a→b,b→c}` 中 `b→c` 先执行，随后 `a→b`；任一步的目标在执行前都不存在。
每个请求、名字和步骤只进入 map、队列或堆常数次，复杂度为 O(R+N)，其中 N 是涉及名字数。

## 5. 环与临时名
每个有效旧名只有一条出站边，而目标冲突已排除，所以相关点出度、入度均不超过 1；非平凡分量要么是链，要么是单个有向环。
对环 `n0→n1→...→n(k-1)→n0`，取字典序最小点为 `n0`，展开为：
`n0→tmp`、`n(k-1)→n0`、`...`、`n1→n2`、`tmp→n1`。
第一步先释放 `n0`，之后沿逆环方向依次填空，最后释放临时名。每个这样的分量恰好有一个环，且只需第一步把一个环成员移入临时空间；故临时名数量等于环数。
候选名为 `__rename_tmp_<n>__`。它必须不在初始名、请求旧名、请求新名和已选临时名中；冲突则 n 递增重试。有限重试耗尽返回 `ErrNoTemp`，绝不覆盖数据。

## 6. 执行、故障与日志
执行前完成全部分析并加锁。每个成功步骤立即写一条仅追加日志：
`CRC32|长度|base64(序号\x00旧名\x00新名)\n`，CRC 覆盖编码后的记录体和结尾换行。
若第 k 步的业务移动失败，执行器按内存中的已执行列表逆序移动，关闭并删除本次日志，返回包装 `ErrExecutionFailed`。

## 7. 撤销与截断分类
撤销按有效记录的逆序执行 `To→From`；目标不存在且旧名已存在时视为该记录已撤销，因此重复撤销幂等。
截断点分类：
- 文件短于固定头部：`ErrHeaderTruncated`。
- 新记录缺少长度、载荷或结尾换行：`ErrRecordTruncated`。
- 记录长度已完整但缺少提交换行，导致含换行的 CRC 不一致：`ErrCRCMismatch`。
解析在第一个坏记录处停止，只撤销“最大可恢复前缀”，并报告后续无法识别的步骤数。

## 8. 确定性与复杂度验证
输入先进入 map，再以字典序枚举；安全队列使用按 `From` 排序的最小堆，所以打乱请求构造顺序不改变步骤。包内非导出计数器记录集合查找次数，5 万请求规模断言查找次数不超过 `4*(R+N)`。
