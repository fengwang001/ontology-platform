# RLE 设计说明

合法编码由最长游程组成：十进制次数（省略 1）紧跟一个符号；数字与 `\` 必须转义。

## 严格解码拒绝清单

严格解码器只接受满足 `Encode(Decode(t)) == t` 的唯一规范形式。

| 输入类别 | 拒绝理由 |
| --- | --- |
| 显式次数 1，如 `1a` | 解码为 `a`，重编码为 `a`，与 `1a` 不同。 |
| 次数 0，如 `0a` | 解码为空片段，重编码会删除该游程；0 也不是正数。 |
| 前导零，如 `01a`、`00a` | 解码结果对应 `a` 或空，重编码分别是 `a` 或删除，数字写法也非规范。 |
| 相邻同符号，如 `2a3a` | 解码为 `aaaaa`，因必须取最长游程，重编码为 `5a`。 |
| 非法转义，如 `\x` | 编码中 `\` 只能保护数字或反斜杠；重编码不会产生该字节序列。 |
| 末尾孤立 `\` | 转义符后缺少符号，流不是完整游程。 |
| 末尾只有次数，如 `12` | 次数后缺少符号，无法形成游程。 |
| 非法 UTF-8，如 `a\xff` | 符号必须是 Unicode 码点；合法字符串输入不可能重编码出该字节。 |

所有错误都报告全局字节偏移；流式输入中偏移从整个逻辑输入的首字节开始累计。

## 大次数取舍

选择上限 `math.MaxInt64`：次数在 64 位平台可直接作为 Go `int`，解码时按符号分块写入，先检查输出上限再写，不按次数预分配。十进制累加每步做溢出检查，超过上限返回可判定错误，绝不回绕。

输出上限可通过 `NewLimitDecoder` 配置；默认上限为 1 GiB。计算重复字节前用 `limit-written < count*utf8.RuneLen(symbol)` 的等价安全形式判断，避免乘法溢出。

## 语义测试对照

待实现后补齐。
