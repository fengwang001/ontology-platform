# 多副本缓存失效传播与回源协调器 — 设计推导

包依赖方向（单向，无反向依赖）：
`version` ← `entry` ← `single` ← `replica`，`version` ← `bus` ← `replica`。
所有时间判定只走注入时钟（`replica.Clock`），代码不调用 `time.Now` 等真实时间源。

## 1. 为什么"丢弃旧版本通知"能让乱序投递收敛（规则 1、8）

**单调量是每条目的版本下界 `Floor = max(持有数据版本, 已应用失效版本)`。**

失效通知 `{k, v}` 的生效条件只有一条：`v > Floor(k)`。生效时把
`Floor` 提升到 `v` 并把 `Valid` 降级为 `Stale`；否则整条丢弃，
不写任何字段。由此得到三个事实：

- **Floor 单调不减。** 唯一写 Floor 的操作是"取更大者"，不存在
  任何让 Floor 回退的路径（回源填充也是 `max` 语义：只有
  `res.ver >= Floor` 才写入，写入后 Floor = res.ver）。
- **通知效果是可交换的。** 对同一键的任意两条通知 v1、v2，
  无论谁先谁后，作用后的 Floor 都是 `max(Floor0, v1, v2)`；
  状态只取决于 Floor 与填充版本，与到达顺序无关。
- **重复通知是空操作。** 第二次到达时 `v <= Floor` 已成立，直接丢弃，
  因此"实际触发的失效次数"计数不增长（规则 2 随之成立）。

由可交换性，任意排列的同一组通知作用后的末态，等于按版本升序
作用后的末态——都等于"Floor 提升到全组最大版本"。这就是
"随机打乱 100 次末态逐字段相同"测试（`replica/order_test.go`）
所验证的不变量。注意失效**计数**随顺序不同而不同（先到大版本会
吸收后续小版本），因此末态比较只覆盖数据平面字段（状态、持有版本、
已应用失效版本、负缓存标记、剩余存活时长），计数器属于观测信息。

对规则 8：通知可能丢失，所以仅靠通知无法保证收敛。兜底是 TTL
（规则 3）：条目到期后（`now >= expiresAt`，左闭右开）无论是否
收到通知都必须重新回源，而回源拿到的是后端**当前**版本。于是
每个副本的可见版本最终都等于"max(收到的最大通知版本, 最后一次
回源时的后端版本)"，所有副本过期一轮后该值相同，收敛得证。

## 2. 回源结果的过期判定（规则 6）

**比较双方：回源结果携带的版本 `res.ver` 与写入时刻条目的
`Floor`（在 `replica.mu` 临界区内比较）。** 判定规则：
`res.ver >= Floor` 才允许写入，否则丢弃并重新回源。

为什么不能用"回源开始时的版本"：回源进行中可能有任意多条失效
通知到达（包括把未知键接纳为空洞条目并抬升 Floor 的通知），开始
时的版本对这些通知不可见，用它判定会把回源期间被失效的旧值写进
缓存——正是规则 6 禁止的覆盖。

为什么不能用"回源结束时的版本"单独判定：结束时读到的新 Floor
只能说明"现在失效到了哪里"，但无法区分"回源结果本身就满足该
版本"（后端已到 v2，通知也是 v2，结果可写）与"回源结果落后于
失效"（后端返回 v1，通知已到 v2，必须丢弃）。只有携带版本的
逐值比较 `res.ver >= Floor` 能同时表达这两种情况。

**临界区边界：** 后端调用（慢）在锁外执行，不阻塞失效应用与其他
键；失效应用与"比较-写入"都在同一把 `replica.mu` 下完成，因此
二者被串行化：要么失效先落地（Floor 抬升，随后的比较必然丢弃旧
结果），要么写入先落地（随后的失效按规则 1 正常把条目降级为
Stale，不会覆盖）。不存在"比较通过后又来了失效"的窗口。

## 3. 回源失败为什么不能缓存（规则 5）

单飞语义只合并"在途的一次执行"，执行返回后结果（含错误）不滞留：
`single.Group` 在 fn 返回后立即从在途表删除该键，等待者通过
通道拿到错误，表中不留任何记录。

如果缓存了失败，会同时违反两条语义：

- **规则 3（到期兜底）将被架空**：失败若作为条目内容被缓存，
  在 TTL 内后续读会命中这个"失败条目"而不再回源，等于把一次
  瞬时故障放大成整个存活期的故障；而规则 3 要求到期后"必须重新
  回源"，缓存失败会让"重新回源"推迟甚至（在失败被当作有效数据
  时）永不发生。
- **规则 5 自身的可重试性**：失败缓存会使"失败后立刻重试"命中
  缓存的错误，回源调用计数不增加，与"重试必须真正再回源一次"
  直接矛盾。

因此失败路径不写任何字段：条目保持进入回源前的状态（Hole 或
Stale），二者都是"可重新回源"的状态，下一次读立即触发新一次
单飞执行。

## 4. 失效应用的索引结构与复杂度（规则 9）

索引结构就是副本内的一张 **`map[string]*entry.Entry` 哈希表**，
以键为主索引。应用一条通知的路径是：一次哈希定位（O(1)）→
对定位到的唯一条目做版本比较（O(1)）。不遍历、不扫描、不维护
全局有序结构。

非导出计数器 `checkedEntries` 在每次应用通知时恒加 1——检查的
条目数就是被定位的那一个（未知键视为检查 0 个条目但仍计 1 次
查找）。因此每条通知的检查数与缓存总条目数 N 完全无关，最坏情况
上界是 **O(1)/条通知**（一次哈希查找 + 常数次比较）。
`replica/limit_test.go` 用 N=100 与 N=10000 两组对照断言
检查数都恰好为 1。

需要说明：条目数超限时的 LRU 淘汰是 O(N)，但它只发生在**接纳新
条目**时，不在通知应用路径上，不影响本条的界。

## 5. 条目数超限的淘汰策略及其与版本单调性的关系（规则 10）

**策略：LRU。** 接纳新条目时若已达上限，淘汰"最近最少使用且当前
不在回源中"的条目（每次读写用单调序号 `lastAccess` 打戳，取最小
者）。回源中的条目被单飞钉住不可淘汰；若全部条目都被钉住，则
返回 `ErrTooManyEntries` 拒绝，且拒绝前预检（单飞饱和检查在
计数与接纳之前）保证拒绝零副作用；竞态下仍被拒绝的调用会回滚
未命中计数并移除本次接纳的空洞条目。

**为什么不破坏规则 1 的版本单调性：** 单调性是按条目成立的
（Floor 单调不减）。淘汰是**删除**整个条目而不是修改它，存活
条目的 Floor 不受影响。被淘汰键的后续旧通知到达时，该键按未知
键处理：接纳一个只记录失效版本的空洞条目（Floor 从通知版本重新
开始单调），或（全钉住时）直接丢弃。两种处理都不会让任何**存活**
条目回退；而被淘汰键此后若重新回源，拿到的是后端当前版本，
不小于任何已丢弃的旧通知版本。因此全局不变量"任何条目的可见
版本不会因通知而倒退"在淘汰前后都成立。

三类超限错误是三个独立的哨兵错误（`replica.ErrTooManyEntries`、
`single.ErrTooManyInflight`、`bus.ErrQueueFull`），彼此
`errors.Is` 不可判定为同一，测试与演示均断言了这一点。
