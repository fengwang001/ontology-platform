# 设计推导

## 1. 空行与单列空值
选「空行 = 一条记录，唯一字段是未加引号空字段」。理由：跳过空行会丢行，且无法区分「无记录」与「有空记录」；
该选择使字节流是记录序列的无歧义拼接（每读到行结束就落一条记录）。
单列空值：若写为空串+换行，再解析时与"空行"不可区分，仍成立；但**加引号的空字段** `""` 必须带引号写出，
否则引号标记丢失。故回写规则：值含 `,` `"` `\r` `\n` 必须加引号；`Quoted==true` 必须加引号（保住引号标记）。
规范输入定义：UTF-8 字节；每条记录（含最后一条）均以 `\n` 结束；所有行尾只用 `\n`；
仅在必要时（上述六类）使用引号；引号字段内引号写成 `""`。则 `Write(Parse(x))==x` 逐字节成立。

## 2. par 切点
切点可落在任意字节。每个非首段 worker 分别按两种进入假设跑同一状态机：
A=起点在引号外字段开头；B=起点在引号字段中。worker 输出各自完整事件流、末态宏状态、字段/记录局部计数。
驱动从左到右：用上一段真实末态（OUT/IN/QESC/CR）在两假设里选一个；OUT 选 A，IN/QESC 选 B，
CR 或"A 起点但上一段字段未关闭"时顺序补跑（仅该段重算一次，且只重算字段首字节为界的常数开销）。
首个完整字段事件前的片段按"开字段续片"与左段挂起值拼接。全局偏移=段起点+局部偏移；
全局记录号/字段号=左段已关闭计数+局部计数（错误事件同法加基数）。故结果与流式逐位相同。
复杂度：两段假设各扫一遍 =2N；末态 CR 等罕见续跑每段至多 1 段，计入常数（切点总数 K）。

## 3. CR 待定
未引号态遇 `\r` 进 CR 态：不立即判错。下一字节是 `\n` 则行尾落记录；否则 ErrBareCR（位置在该 `\r`）。
半包续传：CR 态跨 Feed 保留，状态机可暂停恢复；流在 `\r` 处 Close，按"后非 \n"判 ErrBareCR。
引号态遇 `\r` 只是内容，保留原字节（不改成 `\n`）。par 段恰在 `\r` 后结束：末态=CR，下一段按上节补跑，
不产生双重行尾。

## 4. 上限的"立刻"
字段计数=逻辑字节数：未引号段每字节+1；引号内每内容字节+1，`""` 整体代表 1 个引号字符（读到第二个引号时+1）。
在消费该字节的同一步检查并立即返回 ErrFieldTooLong，不等字段或记录关闭。记录字段数在"字段开始"动作瞬间+1并查
ErrTooManyFields；记录数在记录关闭瞬间+1查 ErrTooManyRecords。超限后进入终态，后续 Feed 返回同一哨兵。

## 5. 状态转移表（宏状态 × 字节类 → 下一状态/动作/错误）
状态：S=字段开头(未引号,touched=0) B=未引号字段中 Q=引号字段中 E=引号中刚见引号(逃逸待定) C=CR待定。
| 状态 | , | " | \n | \r | 其他 |
|---|---|---|---|---|---|
| S | 落空字段→S | err BareQuote | 落记录→S | 进C(内容待定) | 追加→B |
| B | 落字段→S | err BareQuote | 落记录→S | 进C | 追加→B |
| Q | 追加 | 进E(暂不落) | 追加 | 追加 | 追加 |
| E | 落字段→S | 追加1个引号→Q | 落记录→S | err QuoteClose | err QuoteClose |
| C | (不可达,由外层裁决) | err BareCR | 落记录→S | 见\r后再\r→err BareCR | err BareCR |
说明：C 态把"待决的 \r"缓存，下一字节在 C 入口统一裁决；E 态后 `\r` 属"闭合后非分隔符非行尾"→QuoteClose。
EOF：Q/E→ErrUnclosedQuote；C→ErrBareCR；其余 flush 当前字段与记录（有任意字节或已开字段时）。
