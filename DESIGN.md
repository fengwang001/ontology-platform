# 设计推导

## 1. 末尾换行策略（空输入 / 只含换行）

核心流程（`Keep`）：`\r\n`、`\r`、`\n` 都产出一个 `\n`；删行尾空格/制表符。
EOF 时一个待定 `\r` 算一个行尾；末尾的 `\n` 保留，故多个末尾换行原样保留。
- `Keep`：空串输出空串；不创造换行，幂等（已是规范文本的输入恒等，空串自映）。
- `EnsureOne`：输出“至少一个实义内容或已有换行”时保证恰好一个末尾换行。空串输出
  `""` 而非 `"\n"`：若空串→`"\n"`，则 `N("\n")` 又得 `"\n"`，仍幂等；但“创建一个
  空文件本无行”与“有一行空行”在编辑器里语义不同，且会把无内容文件变成非空文件，
  故选空串。非空时裁掉末尾所有空行再补一个 `\n`，再喂一次输入已只有一个末尾换行，
  无空白可删，故幂等。
- `Trim`：删除全部末尾空行；结果为空串则输出空串。纯空白内容在核心流程里是
  “(空白→空行)+换行…”，属“无实义内容”，整体抹成空串。再规范化空串得空串，幂等。

样例表：
| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

不变量：`ToOut(ToOrig(o))==o`，两函数单调不减。每个被删原文字节是零长
（orig→零 out）区间，`ToOrig` 不可能落在它上面（其输出像为空），故它可以自由取值。
取“被删串之后第一个有效输出位置”（即下一个保留字节的输出偏移），保证单调不减；
连续被删字节取同一值（平台），`ToOrig` 在该点返回被删串前/后的边界，`ToOut` 仍是它，
互逆对边界成立。行尾空白删后，下一个输出是结尾的 `\n`：`ToOut` 指向该 `\n`，
编辑器把光标放在被删空白里 → 落在换行符上（该行可编辑内容之后、下一行之前）。
`\r\n` 的 `\r` 被删、`\n` 保留：`ToOut(iCR)=ToOut(iLF)`=该 `\n` 的输出偏移（指向之后）。

## 3. 空白缓冲超限

拒绝：错误可判定、不输出这批空白、状态进入终态，不违反任何语义，但调用方拿不到结果。
强制判定为“非行尾”并输出：空白立刻保留，若其后其实是行尾，输出就会带行尾空白，
违反语义 2、5（且无法映射回一致结果）。因此选择**拒绝**：超过 `MaxTrailingSpaces`
个连续待定空白即返回哨兵错误（带原文偏移），已输出内容保留，`Write/Close` 进入终态。
输出上限同理超限即拒绝。拒绝是确定性的，与切分无关（上限按原文字节计数），语义 4 不破。

## 4. par 拼接与修正上界

各段开头不知前一段末尾的 `\r`/空白。并发阶段每段用“未决模式”独立规范化：EOF 不把
待定 `\r` 判成行尾、不把尾随空白判成行尾，即一段尾部的 `\r`、空格、制表符原样待定。
顺序拼接阶段，把上一段的“待定后缀”与下一段开头能影响判定的字节（至多下一个换行前的
空白串 + 一个 `\r`/`\n`，大小上界 = `MaxTrailingSpaces+2`；`Keep` 无上界时该未决区
就是段尾连续的 `[\t \r]+` 到首个非空白为止）重新判一次：删去的段尾输出与映射用
`span.CutOrig` 按原始偏移切掉，重判结果以全局原坐标/累计输出坐标续接。修正只触及边界
两侧这个有上界的窗口，故总修正量 O(K)。`EnsureOne`/`Trim` 只改最末尾，作用于全局拼接
结果；严格 NUL 等错误的偏移加段起点平移。映射区间天然全局连续，二分查询不变。
