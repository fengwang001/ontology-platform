# 设计推导

## 1. 空行与单列空值
空行（两个行尾紧邻，包括 `\n\n`、`\r\n\r\n`、首行即空）**跳过，不产生记录**：它不含任何字段，无法与「一条含一个字段的记录」在字节层面区分事件以外的语义，而跳过规则使 `a\n\nb\n` 明确为两条记录。
单列表中「唯一字段为空」的记录必须由回写器输出 `""\n`。若不加引号输出 `\n`，读回时该记录正是被跳过的空行，数据丢失；加引号后引号标记区分了它与空行。规范输入中单列空值必须写 `""`。

## 2. par 切点
每段 worker 无法确定起点状态，故按两个假设各逐字节跑一遍：H0=起点在引号外的字段开始（N），H1=起点在引号字段内（Q）。双假设各处理段内每字节一次，无回扫，故总量 ≤2N+O(K)。
第 0 段真实起点为 N；确定某段结束真实状态 s 后，下一段直接选用以 s 起跑的那条假设的产出（N→H0，Q→H1）。若段在 CR（行尾待定）状态结束，真实状态依赖下段首字节：首字节为 `\n` 则行尾成立，否则该 CR 为孤立错误——选择推迟到看到下段首字节（首段则在 Close 时判错）。
若选中的假设在段首处于延续字段中（切点落在引号字段内），其字段开始偏移/值不是真实值：丢弃该假设在「本字段结束（逗号或行尾）」之前的字段级事件，只保留其字节计数与结束状态；延续字段的真实值来自前段结尾字段，拼接时把段内引号文本追加到该悬挂字段（`""` 已解码为 `"`）。记录/字段号在拼接时按全局顺序计数；段内错误偏移=段起点+段内偏移，记录号=已输出记录数+1。

## 3. CR 待定
状态 R：未引号字段中见到 `\r`，悬置该字节。续传下一字节为 `\n` → 行尾；否则该 `\r` 是孤立 CR 错误（引号字段内的 `\r` 是内容，不入 R）。流在 R 结束（Close 无更多字节）→ 孤立 CR 错误。par 中跨切点的 CR 处理同第 2 节。

## 4. 上限的「立刻」
字段字节计数在字节被接受为字段内容的同一转移点自增：未引号字段每字节 +1；引号字段中普通字节 +1，转义 `""` 的两个字节在第二个引号到达时整体作为一个内容字符 +1（即计数为「输出字符字节数」，`"` 占 1 字节）。计数达到上限后的下一个内容字节立即报错，不缓冲。字段数在每次 emit 字段时检查；记录数在每条完整记录落表时检查。

## 5. 状态转移表
字节类别：`,` `\n` `\r` `"` 普通 x。
| 状态 | `,` | `\n` | `\r` | `"` | x |
|---|---|---|---|---|---|
| N 字段开始 | emit字段→N | 空行→N(行首) | →R | →Q,记引号 | →U |
| U 未引号 | emit字段→N | 结束记录→N | →R | ErrBareQuote | 追加→U |
| Q 引号中 | 追加→Q | 追加→Q | 追加→Q | →QE | 追加→Q |
| QE 引号后 | emit字段→N | 结束记录→N | →R | 追加`"`→Q | ErrQuoteAfter |
| R 行尾CR待定 | ErrBareCR* | 结束记录→N | ErrBareCR* | ErrBareCR* | ErrBareCR* |

R 行任意非 `\n`：先判孤立 CR 错误（位置为该 CR 偏移）。EOF：N/U 结束悬挂字段与记录（无尾换行合法）；Q/QE 中 Q 为 ErrUnclosedQuote，QE 结束；R 为 ErrBareCR。末尾换行后不产生额外记录。
