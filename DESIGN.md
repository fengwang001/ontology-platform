# 只读快照一致性导出器 — 设计推导

## 1. 写时复制（COW）的触发点

问题：导出进行中某键被写入，若直接覆盖，导出到该键时读到新值，快照不再一致。

推导：快照在版本水位 `w` 处打开；每次写入产生新版本 `v`。对任意满足 `w < v` 的打开快照，被覆盖的旧值在区间 `[上次写入版本, v)` 内仍是"快照时刻的当前值"，必须在覆盖前保留。

- 触发点：`Put` 时若存在打开的快照，先把旧值以条目 `(ov=v, old)` 记入 `retained[key]`，再覆盖。
- 新建键也要记 `(v, existed=false)`，否则快照读会错把快照后新建的键当作可见。
- 读取规则：快照读 `key` 时，在 `retained[key]` 中找第一个 `ov > w` 的条目，即为水位 `w` 处的可见值；没有这样的条目则读当前值。

## 2. 保留值的释放条件（为什么按最老快照版本判断）

条目 `i`（覆盖版本 `ov_i`，前一条目版本 `ov_{i-1}`）的可见区间是 `[ov_{i-1}, ov_i)`。
它被需要 ⟺ 存在打开快照 `w ∈ [ov_{i-1}, ov_i)`。

推论：所有 `ov_i ≤ minW`（`minW` 为最老打开快照水位）的条目，对任何 `w ≥ minW` 的快照都不可见，可立即释放。只有最老快照能看到最旧的值，因此保留量由最老快照水位决定：两个重叠快照时，关掉较新的一个不会释放任何仍被最老快照需要的条目；只有当最老的也关闭、且无其他快照落在条目可见区间内时才释放。最后一个快照关闭后 `retained` 必为空。

每次 `Put` 后对同一键做同样的回收，保证每键保留条目数不超过需要它的快照数，与总键数无关 ⇒ COW 内存开销 ∝ 导出期间被改写的键数。

## 3. 导出顺序无关性

导出内容集合 = 快照时刻全部 `(k,v)`，与遍历顺序无关。清单总校验和定义为可交换聚合：`TotalCRC = Σ crc32(k‖0x00‖v) (mod 2^32)`，配合 `TotalKeys` 联合判定。求和可交换 ⇒ 任意导出顺序（字典序/逆序）得到相同总校验和。分块边界随顺序变化，但逐块 CRC 只校验本块自洽性，不影响总校验和。

## 4. 分块格式与续传

块文件自描述：`magic(4B) | index(4) | keys(4) | bodyLen(4)` + `body(klen,k,vlen,v …)` + `crc32(body)(4B)`。
清单 `manifest.json` 记录快照版本、块大小、每块 `{index, keys, bytes, crc, sum}`、`TotalKeys`、`TotalCRC`、`Complete`。

- 分块计划是 `(有序键列表, 块大小)` 的确定函数；清单记录每块键数 ⇒ 续传时无需重读已完成块的值即可定位第 `k` 块的键起点。
- 清单每写完一块落盘一次（写 tmp 再 rename 原子替换），`Complete=true` 只在最后写 ⇒ 中断的导出永远 `Complete=false`，半截文件不会被当成完整。
- 续传：加载清单 → 校验快照版本一致 → 校验已完成块 CRC → 从第 `k` 块继续 ⇒ 与一次性导出逐字节相同。

## 5. 快照过期拒绝续传的论证

续传的正确性依赖"剩余块的键集合与旧值仍来自同一版本水位"。快照关闭后保留值已释放，若用当前数据续传，会把时刻 T1 的前 `k` 块与时刻 T2 的后 `n-k` 块拼接成跨时刻文件，违反一致性且事后无法检测。因此续传前必须确认原快照仍打开；已关闭则返回可判定错误 `ErrSnapshotExpired`，已导出部分保留在磁盘上（不删除），供排查或从头重导。

关闭与导出的竞争：导出每次读都经快照句柄；快照关闭后读返回 `ErrClosed`，导出映射为 `ErrSnapshotExpired` 中止。结果只有两种：导出先完成（`Complete=true`），或导出失败且清单保持 `Complete=false` —— 不存在"半截被当完整"。

## 6. 可判定错误与截断分类

全部用 sentinel error + `errors.Is` 区分：`ErrManifestIncomplete / ErrHeaderIncomplete / ErrBodyIncomplete / ErrCRCMismatch / ErrChunkMissing / ErrChunkOrder / ErrTotalMismatch / ErrSnapshotExpired / ErrChunkSizeZero`。

设块体长 `B`，块文件长 `L = 16 + B + 4`，则截断到长度 `n` 的分类：

- 清单任意截断 → 清单不完整；
- `1 ≤ n ≤ 15` → 块头不完整；
- `16 ≤ n ≤ 16+B-1` → 块体不完整；
- `16+B ≤ n ≤ L-1`（截进 CRC 尾）→ CRC 不匹配。
