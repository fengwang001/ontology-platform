# 设计推导

## 3. 续行与「只含空白的续行」

物理行扫描从行尾向前数连续反斜杠：奇数个表示续行（最末 `\` 充当行连接符），
偶数个表示字面反斜杠、逻辑行结束。这只需在读取时维护「当前反斜杠连续计数」，
遇到非 `\` 即清零，因此每个字节检查次数为常数，计数器 ≤ 2× 输入字节数。

确认为续行后，读入下一物理行。Java `LineReader.readLine()` 的循环是：

1. `trimSpaces` 跳过该物理行全部前导空白；
2. 若跳过之后行内没有内容（只有空白或为空），`return` 当前已累积的逻辑行——
   即**空白续行不拼接任何东西，而是终止整条逻辑行**，后续物理行另起新逻辑行。

结论：`a\` + LF + `   ` + LF + `b` 得到逻辑行 `a` 与逻辑行 `b`，而不是 `ab`。
这与「续行首部空白被剥掉」是同一条规则的两面：剥前导空白是连接前的动作；
若剥离结果为空，说明这一行在「剥光」后不提供任何字符，JDK 直接把它作为
逻辑行结束的信号，而不是凭空产生空格。

## 6. Store 的最小转义集

回写格式固定为 `key=value`，逐字符审视 Load 端会怎么读回，只转义会误读的字符：

- 键：
  - 行首是空白（空格 / 制表符）会被当作缩进丢弃 → 转义第一个前导空白；
  - 行首是 `#`/`!` 会让整行变注释 → 转义；（值在 `=` 之后，`#`/`!` 不可能再触发注释）
  - `=` / `:` / 空格是键值分隔符 → 转义；
  - `\` 转义为 `\\`；`\t \n \r \f` 转义为控制形式（换行会物理断行，必须转义）。
- 值：
  - 只有**行首（即 `=` 后第一个）空白**会被 Load 吃掉 → 仅值开头空格写 `\ `，
    值中间/末尾空格本就是值内容，无需转义，末尾两个空格原样保留；
  - `\` 与 `\t \n \r \f` 同样必须转义；
  - `=` / `:` / `#` / `!` 在值中无特殊含义，原样输出。
- 非 ASCII 原样以 UTF-8 输出（Java 字符串即码点序列，Go 侧以 rune 处理等价）；
  只有 Load 端会解释的转义形式才需要书写。

据此：空键写 `=v`；空值写 `k=`；`Load(Store(m))` 与 `m` 逐项、同序相等。

## 语义—测试对照表

| 语义条目 | 测试函数 |
| --- | --- |
| （实现完成后补填） | |
