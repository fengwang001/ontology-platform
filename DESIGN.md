# DESIGN：自定义 LZ77 流式压缩格式

## 1. 流格式（变长整数均为无符号 LEB128，≤10 字节，>10 字节或第 10 字节越界即错误）

| 记录 | 字节布局 | 说明 |
|---|---|---|
| 流头 | `"LZ77ONTO"`(8) + ver(varint=1) | 魔数/版本错误在偏移 0/8 报出 |
| 字面量 | tag(varint, tag=0) + n(varint) + n 个原文字节 | 连续字面量合并成一段 |
| 回指 | tag(varint, 1\|(dist<<1)) + length(varint) | dist 从 1 起；tag 用低位放 1 |
| 刷新 | tag(varint, tag=2) | 无负载；前一段输出到此处可解 |
| 流尾 | tag(varint, tag=3) + totalLen(varint) + checksum(varint) | checksum=FNV-1a64；其后任何字节都算多余 |

## 2. 四处推导（第三节要求）

**(a) Write 切法无关（语义 3）。** 对每个起始位置 p 求最长匹配必须看“后面跟来的字节”：
在 Write 边界处，下一段输入可能让当前匹配继续，此刻输出任何字面量/回指都会被切法影响。
压缩器因此把尚未定稿的尾巴 `pend` 留到下次 Write 再比；主循环条件是“p 之后至少还有 1 字节”，
即在 p 能判定“p+1 是否还能延长匹配”时才定稿 p：匹配则发 (dist,len) 并跳过 len 字节，
不匹配则把 1 字节并入字面量段。能定稿的只取决于 `pend[0:len-1]` 的内容，与它来自几次 Write 无关，
故输出逐字节相同。`Flush` 必须知道“后面不会再有能延长匹配的字节”，于是强制把剩余
`pend` 按同样规则定稿（末尾不足扩展的位置若能成匹配则发回指，否则字面量），并写一个刷新标记。

**(b) 重叠回指复制（语义 2）。** 回指语义是重复模式：dist=3、len=30 时源窗口只有 3 字节，
结果是这 3 字节不断重复。一次性 `copy(dst, src)` 只能复制 src 当前长度（3 字节），其余字节读不到，
且当 src 与 dst 重叠时 copy 的覆盖语义也不等于“从已生成序列重复”。实现以
`out = append(out, out[len(out)-d])` 逐字节向前复制：每字节都从“已经生成的输出”取，
距离 1/2/3 自动产生周期展开。每字节一次 append（配合预扩容，无重复分配），相对窗口回放也是 O(len)，
不成为复杂度瓶颈，因此这里直接逐字节是可接受且可证明正确的。

**(c) 两种距离越界（语义 7）。** “超过窗口容量”是协议级不变量：匹配器只在容量 W 的环形窗口内
索引候选，合法压缩器（含并行块）绝不可能发出 dist>W，它意味着流损坏。“超过已输出字节数”是
顺序级错误：dist≤W 但当前位置之前还没有那么多字节（典型为流开头、或刷新点之后）。
二者条件独立、含义不同，必须分别报出。并行块用“上一块原文”作预置字典时，字典字节会像普通
历史一样进入解压器的窗口吗？不会——本格式没有字典记录，每块流衔接后解压器是从空输出开始的。
因此预置字典必须也由解压器看到：本设计让块之间不插任何标记，压缩块的字典部分天然就是流里
更早的原文，解压到该块时这些字节早已是“已输出”，故“已输出字节数”含义不变，dist 永远不超过
（已输出总数、W）二者中的小者；窗口满后旧字节淘汰，与单流完全一致。

**(d) 并行确定性（语义 9）。** 第 k 块的压缩输入只有 `data[k*B:(k+1)*B]` 与其字典
`data[k*B-B? : k*B]` 的最后 W 字节，二者都是原始 data 的确定切片；匹配器为纯函数式状态
（无随机、无时间、无 goroutine 间共享可变状态，每 goroutine 独立 enc），所以块输出只由这两段
字节决定，与 workers 数、调度顺序无关。流衔接：除第 0 块外，每块压缩前把字典原文灌进窗口与
哈希链（但不产生任何字面量/标记），随后各块记录直接首尾相接；块间不需要刷新标记——刷新标记只
服务“提前可读”的 Flush 承诺，固定块边界对正确性无意义。流头只写一次，最后统一写一个含全量
totalLen 与全量 FNV 的流尾。

## 3. 其余要点

- 窗口：定容环形缓冲，绝对位置单调递增；按 dist 取回即 `buf[(end-dist)%cap]`，dist>end 拒绝。
- 哈希链：hash(3字节) 头表 + `prev[pos%cap]` 链；每位置只沿链走 ≤ chainLen 个候选，
  故全同字节最坏也是每位置 chainLen 次比较，候选总数 ≤ chainLen×输入字节数，与规模无关。
- 错误：哨兵 + `DecodeError{Offset}` 包装；解压炸弹在追加输出前按 `len(out)+length>max` 拒绝，
  随后进入终态，同一错误持续返回。
- 单实例非并发安全；多实例可并发。空输入也产生“流头+流尾”的非空合法流。
