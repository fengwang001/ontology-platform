# 并行任务图设计

## 不变量

图是 `id -> 下游 id` 的有向无环图；重复边幂等，未知依赖在执行前报错。
环检测用 DFS 三色标记，发现灰边时沿父指针回溯，返回首尾相同且每条边均来自输入的路径；自环天然被覆盖。拓扑层只用于校验与说明，运行时不做全图扫描。

调度器维护入度、按 ID 升序的就绪集合、当前运行数和历史峰值。每个任务结束只触发一次局部就绪判定：把该任务的下游入度减一，入度为 0 才入队，因此判定次数与边数同阶。并发上限通过“先入就绪集、有空位才启动”保证。

## 四类最终状态

1. 成功：任务函数已启动且正常返回；终态一旦写入不再改变。
2. 失败：任务函数启动后返回错误或 panic；panic 包装为可类型断言的 panic 错误并保留原始信息。
3. 跳过：任务从未启动，且存在一个失败祖先。原因记录按拓扑/时间最早可达的失败任务。`A->B->C` 且 A 失败时，C 必须归因 A，而不是 B。
4. 取消：任务已经启动，失败传播发生时仍在运行；其 context 被取消。任务之后即使写回结果，CAS 终态也必须丢弃。

区分依据是“是否有启动事实”：有启动记录而后终止传播的为取消；没有启动记录且依赖链含失败的为跳过。成功和失败也已锁定终态，不会被后续取消改写。

## 失败模式

快速失败：第一个终态失败出现后立即取消其 context，停止派发新任务；仍在运行的旁支记为取消，未启动且依赖失败的记为跳过。不依赖任何失败任务的未启动任务也被取消派发，但因尚未启动记为跳过；旁支已启动任务才是取消。

尽力而为：失败不取消全局 context，也不阻止独立分支；只封锁失败任务的可达下游。已启动的独立任务继续运行，可成功或失败。所有失败都保留；每个跳过任务选择其最早失败祖先。

## 确定性与资源

就绪集合用最小堆按 ID 升序取任务。报告按 ID 字典序输出，完成顺序只影响并发时序，不影响终态表。任务、调度循环和收集器的生命周期均收敛：任务结束后 writer 退出，所有结果处理完成后调度器关闭内部通道；失败路径取消 context 并等待运行任务收尾。

取消后写回用 `sync.Once`/原子终态 CAS 丢弃。panic 在任务包装器内 recover。空图生成空报告；链式 1000 层只用迭代入度处理，不递归遍历。
