# 双缓冲快照发布器 — 设计推导

## 1. 回收时机怎么判（核心）

朴素方案「Publish 先切换当前指针，再检查旧版本借出计数，为 0 则回收」的错误交错：

1. 读者 A 执行 Acquire：读取当前指针，读到版本 v（尚未登记借出）。
2. 写者执行 Publish：构造 v+1，把当前指针切到 v+1。
3. 写者检查旧版本 v 的借出计数——此时仍是 0（A 还没登记），于是回收/复用 v 的缓冲。
4. 读者 A 这才登记借出并开始读，读到的已是被复用的 v+1 内容；不变量 1 被破坏。

办法：用**一把互斥锁把「读当前指针 + 借出计数 +1」与「切当前指针 + 判断回收」都包成临界区**，
并在同一临界区内决定回收：Publish 构造好新块后，在锁内置 current、加入活跃集，
旧块仅在 refs==0 时立即移出活跃集并回收，否则保留；Release 归还后在锁内 refs--，
若该块已非 current 且 refs 归 0，则在同一临界区回收。

论证：上面第 1、4 步之间不可能插入第 2、3 步——A 在锁内拿到 v 时 refs 已 +1，
写者拿到锁时看到的 v 要么 refs>0（保留），要么根本没有读者能再引用它（A 要么已登记，
要么还没进临界区，进临界区后读到的是 v+1）。Release 是 refs 归零的唯一路径，
故非当前块在最后一个持有者归还的同一步被回收；无人持有的非当前块 Publish 时立即回收。
不变量 1、2 同时成立。

## 2. Acquire 的原子边界

「读当前版本」与「登记借出」必须在同一临界区内完成。若分开，恰为第 1 节第 1→4 步的交错：
指针读到的是 v，登记前写者已把 refs==0 的 v 回收，读者登记到已回收块上。
原子化后，读者登记到的版本与它读到的当前版本必然是同一个、且必为完整发布的版本。

## 3. 内容相同也递增版本

版本号是读者判断「我手上的快照是否仍为最新」的唯一凭据。若内容相同复用旧号，
一次 Publish 已发生但 Acquire 返回旧号，读者无法区分「没有新发布」与「有新发布但内容相同」，
破坏不变量 4（版本号不再与发布事件一一对应，严格递增失效）。故每次 Publish 无条件 +1，不比较内容。

## 4. 尚无快照时的 Acquire

返回可判定错误 ErrNoSnapshot，而不是零值快照。零值无法自证「完整」，
读者可能把零版本的空内容当成一份合法快照读出，违反不变量 3「只能拿到完整版本」。
零值版本号保留为「尚无快照」的内部标记；读者用 errors.Is 判定后重试或等待。
