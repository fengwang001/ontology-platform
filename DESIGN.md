# 设计推导

## 1. 非法单元切分规则（WHATWG 式）

逐字节推进：首字节确定期望长度 L 与第二字节合法区间。续字节到来时若不在区间内，则**该首字节单独构成一个非法单元（恰好吞 1 字节，输出一个 FFFD），未通过的字节作为新起点重新判定**；通过的非法续字节（如 `F0 90 80` 的第三字节 80，因前两字节仍合法）到来时，连同后续每个游离续字节各算一个非法单元。已合法接收 L 字节则输出一个标量。

- `F0 90 80 41`：F0 要求 90..BF，90 通过；第三字节 80 不在 80..BF？在，但 F0 期望第 2、3、4 字节均为 80..BF——80 本应通过。样例要求 1 个 FFFD+41，故该样例表示 `F0 90 80` 后**第三字节即判定失败**？否。真实规则：第三字节 80 合法，失败的是第 4 字节 41；此时 `F0 90 80` 整体作为一个未完成非法单元吞掉（吞 3，1 个 FFFD），41 重解析。即：**一旦进入多字节序列，中途任何失败/截断都把整个序列吞成 1 个 FFFD；只有首字节的第二字节立即失败时只吞首字节**。
- `E0 80 80`：E0 要求 A0..BF，第二字节 80 立即失败，只吞 E0（1）；80、80 是游离续字节（各 1）→ 3 个 FFFD。`ED A0 80`：ED 第二字节要求 80..9F，A0 立即失败吞 ED；A0(=ED 类首字节? 不，A0 在 80..BF 是续字节)、80 均游离 → 3。`C0 AF`：C0 无合法第二区间，吞 C0；AF 游离 → 2。`F4 90 80 80`：F4 要求 80..8F，90 立即失败吞 F4；90/80/80 游离 → 4。
- `E2 82` + EOF：两字节都合法地通过了检查，是合法前缀被截断，整个序列 → 1 个 FFFD（不是 2）。

统一规则：状态机在首字节后记 need、seen、validSoFar。第二字节越界（含首字节非法）时立即失败且只吞首字节；此后每字节都已"入会"，任何失败或 EOF 把入会字节整体吞成 1 个 FFFD。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 首字节 1；入会后截断吞全部 |
| E0 | A0..BF | 同上 |
| E1..EC | 80..BF | 同上 |
| ED | 80..9F | 同上 |
| EE..EF | 80..BF | 同上 |
| F0 | 90..BF | 同上 |
| F1..F3 | 80..BF | 同上 |
| F4 | 80..8F | 同上 |
| 80..BF | 无（游离续字节） | 1 |
| C0 C1 F5..FF | 无合法区间 | 1 |

## 2. 缓存上限与立即判定

最长合法序列 4 字节，且第二字节未通过就立即拒绝并退回该字节，故缓存至多保存"已通过检查的首字节+续字节"= 3 字节（硬上限 3）。看到第二字节越界、首字节非法、游离续字节时立刻判定，不等 EOF；只有已入会前缀在 EOF 残留才算截断。

## 3. par 切点对齐

段 s（s≥1）从切点 c 向前回看：收集至多 3 个紧邻的续字节(80..BF)，再看前一个字节 p。若 p 是合法首字节且 p..c 都在其期望长度内（seen 数 < L），段起点改为 p；否则从 c 开始。每段转码只统计其"起点之后由本段首次解析"的单元，起点若被前移则跳过属于上一段的字节（按上一段同样逻辑唯一归属）。这样每个字节被恰好一个段结算：`C3|80 80` 回看 C3 且 80 是其续字节，第二段从 C3 开始并消费 C3 80；第一段在切点前已无法完成 C3，C3 不被第一段结算（第一段结束于边界，截断仅在全局 EOF 判定）。非末段一律不做 EOF 截断判定。UTF-16 回看按代码单元（切点回退到偶数偏移），并在高代理后切时把高代理划给下一段。回看常数 ≤4 字节，故检查次数 ≤ n + 8·常数。

## 4. 上限断点

Write 逐事件输出，输出下一个事件会越界即停（标量/代理对/FFFD 为不可分割事件），返回 ErrLimit。返回的 n = 已经"结算"的输入字节（完成事件的字节 + 已判定非法单元字节）；仍在 3 字节缓存中的合法前缀**不算**已消费，必须重喂。调用方把 data[n:] 传给新实例，输出逐字节拼接即等于无上限结果。统计只随结算事件增长。
