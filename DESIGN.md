# 带代号句柄表 — 设计推导

## 编码方案（推导的前提）

句柄为 64 位无符号整数，自高到低：`tag(16) | index(28) | gen(20)`。
`tag` 是表标签（进程内原子计数器分配，从 1 开始），`index` 是槽位号，
`gen` 是该槽位当前代号。代号位宽 m=20，上限 `M = 2^20 - 1`。

## 推导一：代号回绕会破坏不变量 1，故代号不得回绕

假设允许代号递进到 M 之后回绕（跳过 0，回到 1）。取容量为 1 的表，
为清晰起见把位宽缩小为 m=2（M=3），操作序列：

1. `Insert` → 槽位 0 代号 1，发出句柄 `h1=(tag,0,1)`；
2. `Remove(h1)` → 代号递进为 2；`Insert` → 发出 `h2=(tag,0,2)`；
3. `Remove(h2)` → 代号递进为 3；`Insert` → 发出 `h3=(tag,0,3)`；
4. `Remove(h3)` → 代号回绕为 1；`Insert` → 发出 `h4=(tag,0,1)`。

此时 `h4` 与第 1 步的 `h1` **逐位相同**：`Get(h1)` 校验通过，读到的是
`h4` 的对象。老句柄复活，不变量 1 被破坏。根因：回绕使同一槽位的代号
取值序列出现周期，老句柄的 `(index, gen)` 组合必然在 M-1 次复用后重新
变为合法。

**处理办法：代号只递进、永不回绕。** 槽位代号已达 M 时再被 `Remove`，
该槽位转为「代号耗尽」态：不还给空闲链表、永不再复用，由表单独计数。
论证：每槽位代号序列严格单调且取值不重复，任何已发出的 `(index, gen)`
组合此后永不合法，不变量 1 成立；耗尽槽位既不在用也不在空闲链表中，
不变量 3 的等式因显式计入「耗尽数」而继续成立：
`Len + 空闲数 + 耗尽数 == Cap`。代价是容量随耗尽缓慢收缩，这是用
有限位宽换取「老句柄绝不复活」的必要代价。

## 推导二：代号从 1 开始、tag 从 1 开始，零值句柄自然非法

不变量 5 要求 `var h handle.Handle`（全 0 比特）永不有效。全 0 解码为
`tag=0, index=0, gen=0`。只要保证：**表标签从 1 开始分配**（原子计数器
首值为 1），且**每个槽位的代号从 1 开始**（建表时初始化为 1，只增不减、
不回绕到 0），则「tag=0」或「gen=0」的句柄永远不可能是任何表发出的。
因此「槽位 0 的第 0 代」这个组合根本不存在——槽位 0 的首个句柄是
`(tag≥1, 0, 1)`，与零值 `(0,0,0)` 不同。零值句柄在解码前即被
`IsZero` 拦截，返回专有的 `ErrZeroHandle`。

## 推导三：空闲链表尾插（FIFO），延缓代号耗尽

头插（LIFO）：刚释放的槽位立刻被下一次分配拿走，热点槽位的代号以
每次操作 +1 的速度燃烧，容量为 N 的表在约 M 次「释放-分配」后即出现
第一个耗尽槽位。尾插（FIFO）：释放的槽位排到队尾，复用按轮转方式
均摊到全部空闲槽位，第一个耗尽槽位大约在 `M × 空闲数` 次复用后才
出现。两者都是 O(1)。由于推导一选择「耗尽即退役、永不复用」，耗尽
速度直接等于容量收缩速度，故选择**尾插**，与「代号不回绕」一致：
既然耗尽不可逆，就让单槽位代号燃烧速率降到最低。

## 推导四：同一失效句柄连用两次，必须返回相同错误

不变量 4 要求任何被拒绝的操作不得改变可观测状态。若第二次返回一个
可区分的「已处理过」错误，则表必须记录「该句柄已被查询过」这一事实，
即第一次（被拒绝的）操作改变了可观测状态，直接违反不变量 4。因此
错误只能是当前表状态的纯函数：`f(句柄, 表状态)`。同一失效句柄在状态
未变时连用两次，表状态相同，错误必然相同——均为 `ErrStaleHandle`。
