# LSH 分桶近似最近邻检索器 — 设计与推导

## 1. 签名计算规则

对 d 维向量 x，一张表含 b 条随机超平面，第 j 条法向量 r_j 的各分量由带显式种子的
Lemire 反变换（math/rand/v2 ChaCha8 + rand.NormFloat64）独立采样，均值 0、方差 1。

- 位 j = 1 当且仅当 内积 `<x, r_j> > 0`；
- 内积 `<x, r_j> <= 0`（含**恰为 0**，如零向量）时位 j = 0；
- 零向量与任何超平面内积恒为 0，故其签名为全 0，确定、可复现。

签名按小端位序打包成 `[]byte`（位 j 存入 byte j/8 的 bit j%8）。b=4/8/12 时
分别占 1/1/2 字节。一次签名恰好做 `b` 次内积（哈希计算计数即按此口径）。

## 2. 位数 b、表数 L 与候选数、召回的关系

随机超平面 LSH 中，两向量夹角为 θ 时**一位**相同的概率 p(θ) = 1 − θ/π
（Gordon 引理）。同一张表 b 位全同（落入同一桶）的概率：

  P_collision(b, θ) = p(θ)^b

L 张独立表中至少一次同桶（成为候选）的概率：

  P_candidate = 1 − (1 − p^b)^L

- **候选数**：向量在一个桶内的期望数量约为 N·p^b（按近邻距离分布加权），
  L 张表合计候选（去重前）约 `N·L·p^b`。b 增大时 p^b 指数下降 ⇒ 桶更细、
  候选更少；L 增大时候选近似线性增多。
- **召回率**：单表命中最近邻的概率为 p^b，随 b 指数下降；L 张表把它提升为
  `1−(1−p^b)^L`。因此 L 越多召回越高且单调不降（见下嵌套表约定），
  b 越大召回越低。这是「桶更细、候选更少但召回更低」的权衡。

**嵌套表约定（保证严格单调）**：同一族内 L=1/2/4/8 复用前 1、2、4、8 张表，
即表集合恒为 `T1 ⊂ T1..2 ⊂ T1..4 ⊂ T1..8`。候选集合随之严格包含，
故召回率随 L 单调不降，无随机抖动。

## 3. 可复现性

种子经 `rand.NewChaCha8(SHA-256(seed bytes))` 派生；按「表 j、位 k」顺序
逐分量采样正态分量并立即乘以 -1（rand.NormFloat64 的符号无关紧要，保留原值即可，
关键是采样序列确定）。同种子 ⇒ 超平面字节相同 ⇒ 每个向量签名逐字节相同。
不同种子以压倒性概率产生不同法向量，签名必有差异。NaN/±Inf 向量在签名前拒绝
（不计入索引，计入 Skipped）。

## 4. 精排与复杂度

候选 = 各表查询向量所在桶中向量 ID 的并集（map 去重）；对候选计算欧氏距离
精排，距离相等按 ID 升序打破并列，取 TopK。非导出计数器记录：
- `hashCalcs`：建索引时内积次数，恰为 `向量数 × L × b`；
- `distCalcs`：查询精排算过的距离条数（每次查询复位）。

## 5. 并发与可见性

建索引在私有副本上完成，再以一次 `atomic.Pointer` 整体替换发布；查询只读已发布
快照。未完成的表永不可见（全有或全无）。

## 6. 持久化格式（自描述 + CRC32）

小端字节，区段顺序（四个可截断区段）：
1. 头 28B：magic "LSHIDX01"(8)、version u16、dims u32、b u32、L u32、
   nVectors u32、nEntries u32。
2. 超平面参数：L·b 个法向量，每个为 dims 个 float64。
3. 数据：nVectors 个向量（id u32 + dims 个 f64），其后 nEntries 条桶项
   （table u32、sigLen u16、sig、nIDs u32、IDs u32…）。
4. CRC32（IEEE）对前 3 段的校验和，4B。

截断分类：头不完整 → ErrHeaderTruncated；超平面段不完整 → ErrHyperTruncated；
向量/桶段不完整 → ErrDataTruncated（已恢复项保留，构成最大可恢复前缀）；
文件到 CRC 末尾但校验失败 → ErrCRC。恢复时剔除引用不存在向量的桶 ID 并计数，
保证无悬挂 ID。某法向量全 0（退化）→ ErrDegenerateHyperplane（指出表号/位号）；
查询维度不符 → ErrDimMismatch。四类错误均可用 errors.Is 区分。

## 7. 边界语义

空集合：查询返回空结果、无错误；单向量：TopK 取该向量；K>N：返回全部；
全同向量：全落一个桶，候选=N，最近邻集合即全集，召回=1；NaN/±Inf：拒绝并计入
Skipped；dims=1：法向量为标量 ± 值，签名规则不变。
