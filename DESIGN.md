# 传递式优先级继承调度器：推导与设计

## 1. 有效优先级不动点

`eff(t) = max(base(t), max{ eff(w) : w 等待某把 t 持有的锁 })`。该方程在
"持有者→等待者"构成的依赖图上取最大不动点：从各任务 base 出发，沿
"等待者把自身 eff 抬给持有者"的边反复传播直到不变。等待图无环
（成环的 Lock 被拒绝），故不动点唯一且传播必终止。

## 2. 第三节四处推导

### 2.1 多把锁时的恢复

L 持 m1(等待者 eff 10)、m2(等待者 eff 7)，base=1，故 eff(L)=10。
释放 m1 只删除 m1 这条贡献边；m2 的贡献仍在：
**eff(L) = max(base=1, 7) = 7**，不是 1。

"直接恢复成 base"导致无界反转的序列：
1. L(base1) 锁 m1、锁 m2。
2. H(base10) 等 m2（L 被抬到 10）。
3. X(base7) 等 m1。
4. L 释放 m2（交给 H）。错误实现把 L 降回 1，尽管 X(7) 还在等 m1。
5. 此后大量 M(base5) 就绪：L=1 永远输给 M，X=7 卡在 L 持有的 m1 后——
   中优先级任务经 L 把 X(7) 无限推迟，正是优先级反转。正确实现 L=7，
   立即高于 M，临界区有界完成。

### 2.2 降级传播何时停止

超时/离开触发从该等待者原等待的锁的持有者开始做一次"局部重算"：
`eff'(v)=max(base(v), 剩余所有等待者的 eff)`。

- 若 **eff'(v) == eff(v)**：v 对它自己持有的每把锁的持有者 u 给出的
  贡献值没变，u 的输入集合不变 ⇒ u 的重算结果必然不变，再往下所有人
  的输入都不变，可立即停止。这是剪枝正确的充要条件。
- "降到 base 就停止"是错的：节点 v 可能仍被另一把锁上的等待者抬高
  （如 2.1 中 L 应为 7 而非 1）。在 base 处停会漏掉这条仍存在的贡献；
  反之 v 即便没降到 base，也可能恰好没变化而可以安全停止。判据只能是
  "值是否变化"，不能是"是否等于 base"。

### 2.3 持锁结束（Done 时仍持锁）

选择：**报错并强制释放全部锁给等待队列队首**，不允许等待者永久阻塞。
任务以失败状态结束；被交接者收到可判定标记：`ReceivedAborted(m, owner)`
（调度器在交接步把前任 ID 与 `aborted=true` 记入该任务的通知）。
理由：永久等待会把单个任务的编程错误放大成整链活锁，且无法满足
"无无界等待"；强制释放让资源存活，通知让后任能感知锁可能处于
不一致状态（类似进程崩溃后接手）。

### 2.4 交接与抢占的顺序

单步内严格顺序：
1. 执行当前任务 t 的一条指令；
2. Unlock：锁在同一步直接交给队首 w（无"无主"中间态）；
3. 重新计算继承：w 不再是等待者、成为持有者，沿链做 2.2 的重算/抬升；
4. 本步结束，再按 eff 选下一步任务——**下一步才发生抢占**，t 不会在
   一条指令执行中途被打断。
若交接后 eff(w) > eff(t)，下一步运行 w；否则同级/更高的 t 继续。
不违反无反转：被反转受害者 H 一旦成为等待者，其 eff 已在等待开始那
一步经继承链抬到持有者，持有者下一步即以 ≥H 的 eff 运行；交接当步
临界区已结束，H 最早下一步拿到 CPU，延迟只多常数步。

## 3. 无反转步数

H 开始等待后，持有者链上每个节点 eff≥H，故任何 eff<H 的 M 都不能
插在它们前面；H 等待步数 ≤ 持有者临界区剩余指令数 + 2（入队步、
交接步），与 M 数量/工作量无关。

| 实现 | M=1，H 等待步数 | M=100，H 等待步数 |
|---|---|---|
| 继承 | L 剩余临界区 + 2（有界） | L 剩余临界区 + 2（相同） |
| 不继承 | 可随 M 工作量增长 | 随 M 数量×工作量增长（无界） |
