# DESIGN

模块 `ontology` 只用 Go 标准库；所有状态在内存，落盘仅通过 `sink`。

## 1. 数据模型
- `record.Record` 为 `Level/TraceID/Fields`；字段是 `map[string]any`，值可为映射、`[]any`、标量。
- 编码用 `encoding/json`（键按字典序，输出确定），作为非目标字段逐字节比对基准。
- 构造时做 DFS：深度上限 8；父链记录已访问映射，自引用返回 `ErrCycle`；超限返回 `ErrDepth`。

## 2. 嵌套路径与含点字段名的消歧
规则路径以点切分。某层不是映射或层不存在，规则不命中且不报错。
为消歧 `a.b`：进入某映射时，先查整串字面键（如 `a.b`）；命中则该映射即叶子，不再下沉。
未命中字面键时，才按第一段下沉（先 `a`，再在子映射查 `b`）。
该规则递归用于所有层级，因此字面字段优先于嵌套解释，两种键并存时结果确定。

## 3. 脱敏
预编译规则集：精确路径放入 `map[string]Action`；为每个父前缀建 suffix 映射，使每条字段 O(1) 查找。
值模式规则在编译期转成 `regexp.Regexp`，扫描所有字符串；一个敏感值出现在任意路径/数组都会被处理。
同一逻辑字段不会因字面与嵌套双重解释被改两次。方式：
- replace：输出固定串，不保留长度；
- hash：SHA-256 hex；同值相同、异值概率上不同、不可逆；
- truncate：保留前 N 个 rune，追加 `…<truncated>`，N<=0 只保留标记。
同一路径的 replace/hash/truncate 冲突在编译期返回包装 `ErrConflict` 的错误，并指出两条规则。

## 4. 一致性采样
若每条记录独立随机，同一 trace 会分裂，故决策输入只能是 trace ID。
`h=SHA-256(traceID)`，取前 8 字节 big-endian 对 1e9 取模；`h < rate*1e9` 则保留。
该决策与记录内容、顺序、时间无关；率 0 全丢，率 1 全留。
空/缺失 trace ID 无法共享链身份：散列为空串，形成一个稳定的“无链”桶，结果确定且 O(1)，但不跨进程密钥加盐。
error 及以上始终保留。若其 trace 桶未被采样，记录另加 `_incomplete_chain=true`；
被采样链上的 error、或非 error 的强制情形均不加标记。

## 5. 落盘格式与损坏分类
文件 = 16 字节头 `ONTLOG1\n` + uint32(大端)记录数 + 保留 6 字节；其后为多条：
uint32 体长度、JSON 体、IEEE CRC32 checksum（4 字节）。
扫描按前缀恢复。截断分类：`<16` 头部不完整；`<20` 长度前缀不完整；
`<20+n` 记录体不完整；`<24+n` CRC 不完整；CRC 不等为 CRC 不匹配。
所有读取错误包装 `ErrCorrupt`，与 `ErrConflict/ErrDepth/ErrCycle` 均可 `errors.Is`。

## 6. 复杂度与并发
规则匹配计数器仅在规则集内递增；路径规则按预编译 map 查，值规则为字段数线性项。
100 条路径规则、10 万记录时上界：`记录数*(字段数+常数)`，不含规则数乘法。
采样每次恰好一次 SHA-256，用非导出计数验证；处理过程无共享可变状态，规则集只读，可并发。
