# DESIGN

## 1. 非法单元切分规则（u8）

规则：首字节按期望长度 n=2..4 起一个单元；随后只接受延续字节 80..BF。遇到第一个不满足该位置要求的字节时：
(a) 若此前没有收到过任何延续字节，则该单元只吞首字节，坏字节留给下一单元；
(b) 否则单元吞掉首字节到上一个延续字节为止，坏字节留给下一单元。
流结束时残留在单元内的所有字节（首字节+延续字节）合吞为一个截断单元。
因此 F0 90 80 41：F0 已收 2 个延续字节，41 不合格，吞 F0 90 80 一个单元，41 重解析；
E0 80 80 按 (b) 只得两个 FFFD，但样例要三个，故采用 WHATWG 精确修订：
第二字节非法时不吞并该延续字节（它本身必是 80..BF，独立成非法单元），其余位置的坏延续字节按 (a)/(b) 处理。于是 E0 单元只吞 E0，80、80 各一；ED/F4 同理三个；C0 首字节非法，C0 与 AF 各一；E2 82+EOF：82 合法但流断，残首+延续合吞为一个截断单元。

第二字节合法区间表（其它延续位置均为 80..BF）：
| 首字节 | 第二字节 | 第二字节非法时吞 | 期望长 |
|---|---|---|---|
| C2..DF | 80..BF | 只首字节，坏字节重读 | 2 |
| E0 | A0..BF | 只首字节，坏字节重读 | 3 |
| E1..EC | 80..BF | 只首字节，坏字节重读 | 3 |
| ED | 80..9F | 只首字节，坏字节重读 | 3 |
| EE..EF | 80..BF | 只首字节，坏字节重读 | 3 |
| F0 | 90..BF | 只首字节，坏字节重读 | 4 |
| F1..F3 | 80..BF | 只首字节，坏字节重读 | 4 |
| F4 | 80..8F | 只首字节，坏字节重读 | 4 |
| 80..BF（孤立） | — | 自身 1 字节 | 1 |
| C0 C1 F5..FF | — | 自身 1 字节 | 1 |
第 3/4 位延续非法：未收过延续则只吞首字节，否则吞到上一延续；坏字节总是重读。非最短（E0<A0、F0<90）与越界（ED>=A0 代理、F4>=90）由区间表统一排除。

## 2. 缓存上限与即时判定

u8 最长 4 字节，缓存上限 3（首字节之外的延续字节）；u16 一个 code unit 2 字节，外加待配对高代理，缓存上限 3 字节。
看到首字节即可知期望长度与第二字节区间：第二字节一到立刻判合法性，不合法马上吐 FFFD 并重读该字节，无须等流结束；只有字节数不足才拖到 Close，判为截断（与非法字节错误区分）。每个物理字节只过一次判定函数（坏字节重读走新单元但判定函数不重复计数），故检查次数 <= 输入字节数+常数。

## 3. par 切点对齐

每个非首段从切点 s 向前借 ctx 字节作为重叠上下文（u8: 3；u16: 4，并把起点向下取偶），从该重叠区启动解析，丢弃起始偏移落在 s 之前的单元，首段从 0 开始（只有它识别 BOM）。末段不设上界并正常处理截断；非末段设绝对上界=下一段切点，跨上界的未完成单元丢弃。
判定一个单元是否起于 s 之前只需 3 字节前瞻即可定其长度（首字节给出长度），因此无需整段重扫；重叠区字节被检查两次，总量 <= N + 8*常数。
C3 | 80 80：段一以 C3 起、上界在切点，2 字节序列跨上界被丢；段二借 C3 起解析 C3 80 合法（起始 <s 丢弃），随后孤立 80 计一个非法。与单线程（1 合法+1 非法）一致，非法个数不重不漏。

## 4. 上限断点

每个单元在写入输出前按编码后精确大小检查剩余额度；放不下则不写任何字节、不提交该单元。Write 返回 n = 截至上一个已提交单元末尾的输入字节数；该单元首字节起的所有字节（含已进入切分缓存的）都算未消费，调用方用 Pending() 取回后续传给新实例。残留在切分缓存里的字节不计入 n、不计入统计；额度错误与非法/截断/终态错误用不同哨兵区分。
