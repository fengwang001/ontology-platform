# DESIGN — JSON 字符串字面量严格编解码器

## 语义 2：代理对错误偏移的推导

规则：**凡"代理不配对"错误，偏移一律指向该代理单元自身 `\u` 转义的反斜杠起点**。

- 高代理后未紧跟合法低代理（串结束、非 `\u`、或 `\u` 后非低代理）：错在高代理本身，偏移指向高代理的 `\`。如 `"\uD83DA"`、`"\uD83D"`、`"\uD83D\u0041"` 均报在偏移 1。
- 孤立低代理（前面没有待配对的高代理）：偏移指向它自己的 `\`。`"\uDE00\uD83D"` 在第一个单元处即报错（偏移 1），因为解码器读到孤立低代理时已经能判定非法，无需往后看。

理由：偏移应指向"第一个不可能属于任何合法字面量的字节"。高代理不是合法标量值，其合法性悬置到后续单元；一旦后续不满足，最早的可判定点仍是高代理自身。若指向后面那个单元，会暗示"删掉它即可修复"，实则不然——`"\uD83DA"` 删掉 `A` 后依然非法。

`"\uD83DA"` 不能解释成「U+FFFD + `A`」：

1. RFC 8259 §8.2 明示孤立代理行为不可预测，严格解码器只能拒绝；
2. 若静默替换，则 `"\uD83DA"` 与 `"\uFFFDA"` 解码结果相同，Decode 失去单射性，语义 5 的往返性质被破坏；
3. 合法编码端永不产生孤立代理，解码端接受它就无法区分"原始数据"与"传输损坏"，等于静默改数据。

## 语义 4：最小转义与唯一性的推导

- U+001F 必须转义：RFC 8259 的 unescaped 产生式为 `%x20-21 / %x23-5B / %x5D-10FFFF`，U+0000–U+001F 不在其中，不转义则输出根本不是合法 JSON 字符串——这是合法性要求，不是风格选择。
- U+007F 不必转义：它落在 `%x5D-10FFFF` 内，原样出现即合法；转义它只是可选风格，最小编码选择不转义。U+2028/U+2029、`/` 同理（`\/` 只是被允许的写法，不是必需的）。

单射：若 `Encode(a) == Encode(b)`，由往返性质 `Decode(Encode(s)) == s` 立得 `a == b`。

唯一性：逐字符看，每个字符在给定规则下恰有一种写法——五个短形式、其余控制字符 `\u00XX`（小写）、其他一律原样；编码规则是确定函数，无自由参数，故输出是规范形（canonical form），唯一。

## Encode 签名说明

`Encode(s string) ([]byte, error)`：输入含非法 UTF-8 时返回 `jstr.ErrUTF8`（可用 `errors.Is` 判定），绝不静默替换为 U+FFFD。

## 测试对照表

（待测试写完后补齐）
