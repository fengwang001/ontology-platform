# 带队首阻塞的加权信号量 — 设计推导

## 规则一：队首取消后，取消路径也必须触成组唤醒

队列是严格 FIFO，且语义2规定「队列非空时即使额度够也不许越过队首」。
因此额度在队首阻塞期间会被「冻住」，唯一的解冻点就是成组唤醒
（从队首开始依次满足，遇第一个不满足者停止）。

反例（只在 `Release` 里唤醒）：
1. Cap=10。G1 申请 10，成功，占用=10。
2. G2（队首）申请 10，额度不足，入队等待。
3. G3 申请 1，队首阻塞，即便额度… 此时仍不足；关键下一步：
4. G1 `Release(10)`，唤醒扫描到队首 G2（需10）仍不满足吗？此刻 available=10，
   实际会满足 G2。为构造冻结，改为 G1 只 `Release(1)`：available=1，
   扫描遇队首 G2(10) 不满足，立即停止——G3(1) 被跳过，正确冻结。
5. G2 超时取消离队。此时 available=1 已足够 G3。
   若取消路径不触发唤醒，则唯一能再唤醒 G3 的事件是下一次 `Release`；
   在那之前 G3 一直挂起，直到自己超时——这正是线上事故。

结论：**取消路径移除等待者后，必须与 `Release` 走同一个唤醒循环**
（无论取消的是不是队首：中间/队尾取消也要调用，成本仅一次检查）。

## 规则二：取消与满足在同一瞬间竞态时的判定

满足（划额度+标记已满足）与取消（检查+移除）都在同一把 mutex 内完成，
二者对同一等待者只有两种互斥顺序：

- 先满足后取消：满足先把额度划给该 waiter 并标记 done；随后的取消路径
  发现已标记，**不移除、不退回额度**，`Acquire` 返回 `nil`。
- 先取消后满足：取消先把节点移除（零副作用，未划额度）；满足循环按
  队列指针推进，再也遇不到该节点，不会给它划额度，`Acquire` 返回
  `ctx.Err()`。

判定规则：**以「持锁排队」为唯一真相。拿锁时若发现自己已被满足就返回
成功；若还没被满足则取消生效、原子移除**。成功后额度由调用方后续
`Release` 归还；取消则从未持有额度。这样任何交错下占用都恰好被计一次，
语义1（不超发）与语义4（取消零副作用、不丢不重）同时成立。
