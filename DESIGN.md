# DESIGN

## 推导

1. Write 切法无关：minMatch=3。压缩器在内部缓冲尾部保留 2 个未决字节 `pend`；只在
`len(buf)>=minMatch` 的位置决策。若位置 i 无匹配，i 提交为字面量；若有匹配 (d,l)，
提交该回指。两种情况下决策只依赖「当前已读的确定字节 + 窗口历史」：未来字节最多把无匹配
变成长度 2（<3 不发回指，仍是字面量，语义不变），而匹配长度只在「匹配延伸到缓冲末尾」
时才可能增长——此时 i+l 必然落入最后 2 字节保护带，本轮停止，等下次 Write 再决定。
故输出只取决于累计输入，不取决于切分。Flush 强制排空 pend：此刻不可能再有未来字节，
未决字节按字面量提交，并打刷新标记。
2. 重叠回指：距离 d<长度 l 时，源区与目标区重叠，一次性 copy 会读到未写入内容。
每轮复制 `n=min(l, d)` 字节（块内不重叠），随后 l-=n；距离固定，新暴露的源字节正是
上一轮写出的字节。最多 l/d 次 memmove 而非 l 次，且 l 受输出上限前置检查保护。
3. 两种距离错误：「距离>已输出」是截断/伪造流（窗口里根本没有该字节）；「距离<=已输出但
>窗口容量」是引用了合法但已滑出窗口的历史。压缩器永不产生后者，因为匹配器只在环形窗口
内搜索。并行块用「上一块末尾 <=窗口容量 的原文」作预置字典：该字典在第 0 块的回指之前
已通过块首预置进入窗口，但不是解压输出，故需在流中携带每块字典长度；解压端同样预置，
「已输出字节数」不含字典，判定不变。
4. 并行确定性：块 k 压缩函数只读 data[k*B:(k+1)*B] 与
data[max(0,k*B-W):k*B]，匹配器为纯函数式局部状态，无共享、无完成顺序依赖；
worker 数只改变调度。块间不插刷新标记：上一块压缩结束时未决 2 字节已随其 Close 排空，
下一块以预置字典起压，记录首尾相接即合法流。

## 流记录布局（小端，整数均为 ULEB128 变长；x-1 表示减 1 后编码）

| 记录 | 布局 |
|---|---|
| 流头 | 魔数 `L`,`Z`,`7` ；版本=`1` ；窗口容量 ；链长上限 |
| 字面量 | tag=0 ；长度-1 ；原始字节序列 |
| 回指 | tag=1 ；距离-1 ；长度-1（长度≥3，距离≥1，距离≤窗口） |
| 刷新 | tag=2 （无负载；连续两次无输入时第二次不发） |
| 块字典 | tag=3 ；预置字典长度（仅块 k>0 块首出现；其后的回指可引用该字典） |
| 流尾 | tag=4 ；原文总长度 ；FNV-1a 64 校验和（对原文逐字节更新） |

tag 与第一个负载拼成一个变长整数：tag 存低 2 位。解析时 tag>4 即错误记录。
压缩流总长度为 0、或头/记录/尾不完整一律报截断；流尾后再出现字节报多余字节。
