# 游标分页器（keyset pagination）设计

数据模型：每行 = 排序值 `V float64` + 唯一 `ID string`。复合键 `K=(V,ID)`，
全序：先比 V，V 相等再按字符串字典序比 ID。数据集按 K 升序排列。

## 1. 排序值重复时，游标必须是复合键

反例：六行 V 全为 1.0（ID `a..f`），页大小 3。
- 若游标只记上一页末行的值 v=1.0 且下一页取 `V > v`：`b..f` 全部被跳过 → 漏。
- 若取 `V >= v`：第一页的 `a,b,c` 再次出现 → 重。

正确做法：游标记录最后一行的复合键 `(v,id)`，下一页边界为复合键严格大于：

    (V,ID) > (v,id)  ⇔  V > v  或 (V == v 且 ID > id)

同一 V 内由唯一 ID 决定全局顺序，每行恰好一个位置，故不重不漏。

## 2. 正向 / 反向翻页规则

- 正向（Forward）：取 `K > cursor` 的最小 limit 行，页内按 K 升序。
- 反向（Backward）：取 `K < cursor` 的最大 limit 行；取数方向反向，但返回前**重排为升序**，
  因此反向得到的页与正向翻到同位置的页逐行相同，而不是它的倒序。
- 游标带方向标记。方向标记与请求方向不一致返回 `ErrWrongDirection`（可 `errors.Is`），
  绝不静默按另一方向处理。

## 3. 并发增删下的语义

每次翻页从数据集取一个不可变快照；游标是纯位置 (V,ID)，推进状态只在调用方手里，
同一游标并发翻页结果逐行相同。语义承诺：
- 已返回过的行不会再次出现（后续页只取严格更大/更小的键）。
- 未被删除的行不会在其键区间内被跳过（边界是键谓词，与具体行无关）。
- 插入到已翻过区间的行允许不出现（无快照隔离承诺）；插入到未翻区间则按位置正常出现。
- 删除游标所指行后仍可续翻：边界是复合键谓词，不依赖该行存在。
- 空游标 = 从头（正向首页 / 反向末页），合法。

## 4. 游标的不透明编码与故障分类

字节布局：`magic(1) | dir(1) | checksum(8,FNV-1a,大端) | v(8,大端) | id(UTF-8)`。
解码按顺序分类拒绝（均可 `errors.Is`）：`ErrIncomplete`（长度不足/magic 不符）、
`ErrDirection`（方向字节非法）、`ErrChecksum`（校验不符）、`ErrWrongDirection`（跨方向复用）。
校验和覆盖 dir+载荷。

## 5. 效率

数据按 K 有序保存在切片中，定位用 `sort.Search` 二分，单页比较次数为
O(log n + pageSize)。计数器为 page 包内非导出变量，测试以白盒断言
n=10000、pageSize=20 时比较数 ≤ 4·(20+⌈log₂10000⌉)。游标的编解码与
n 无关，为 O(1)，用 `testing.AllocsPerRun` 断言解码分配为常数。
