# 设计推导

## 1. 与 Write 切法无关（语义 3、4）
每个 Flush 周期内，编码器只把输入追加到一段完整缓冲区，不保留任何跨周期状态决策；
随后从该段起点做一次贪婪匹配。任何 Write 只改变追加动作，不改变这段累计字节，
因而匹配、字面量与输出完全相同。匹配在“发现下一个位置不能再延长当前匹配”时才结束；
若提前在 Write 边界结算，后续字节可能延长该匹配，输出就依赖切法。Flush 代表调用方
要求该周期立即可解，因此强制以刷新标记结束当前周期并输出全部未决字面量/回指；
连续空 Flush 不产生周期，故第二帧零字节。

## 2. 重叠回指复制（语义 2）
设距离 d、长度 n。块复制要求源范围先固定，d<n 时会读到尚未生成的字节。逐字节复制中
第 i 个源字节是输出位置 p-d+i 当时的内容，天然实现周期展开。实现使用
`copy(out[p:], out[p-d:p])`，一次复制 min(n,d) 个已存在字节，随后 n 按 d 递减；
k 次后长度最多剩 0，复杂度 O(n)，同时每一步的新源都已生成，结果与逐字节循环相同。

## 3. 两类距离错误及预置字典（语义 7、9）
“超过已输出字节数”说明引用了尚不存在的逻辑历史；“超过窗口容量”说明引用虽可能存在，
却早于配置允许记忆的范围，二者恢复含义不同，必须分别报错。正常流中编码器只插入距当前
位置不超过窗口容量、且编号更小的候选，故不可能产生超窗口回指。并行块以“上一块原文末尾
至多一个窗口容量”预置本块匹配器：块内位置 0 的已输出逻辑历史已等于此前全部块长度，
因此解压器的已输出计数不变；预置只是让编码器选择距离≤窗口的合法回指，不会放宽校验。

## 4. 并行确定性与块衔接（语义 9）
块 i 只读取 data 中本块和块 i-1 末尾窗口，二者在调度前已固定；它不读取其他块的压缩
结果、goroutine 完成顺序或共享可变匹配器。每个块用同一配置、无块内刷新标记地压缩；
块尾不强制边界，下一块编码器的候选仅由预置原文得到。流中所有块记录顺序拼接，最后一个
流尾统一声明全文长度和 FNV-1a 校验和；因此 workers=1/2/4/8 仅改变执行时序，字节相同。

## 5. 记录布局
所有整数为无符号小端 base-128 varint（最高位表示后续字节）；偏移从流首字节起计。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 `LZ77SELF`（8）+ 版本 1（1）+ 窗口容量 varint + 链上限 varint |
| 字面量 | tag=0（1）+ 长度 varint + 原始字节（长度） |
| 回指 | tag=1（1）+ 距离 varint（≥1）+ 长度 varint（≥3） |
| 刷新 | tag=2（1） |
| 流尾 | tag=3（1）+ 原文总长 varint + FNV-1a 64 位校验和 varint |

tag 必须按 0..3 解释；流尾后出现任意字节均为多余数据。
