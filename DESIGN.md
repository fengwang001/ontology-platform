# 规范化器设计推导

## 1. 末尾换行策略：空输入与幂等

三种策略：`Keep`（原样）、`EnsureOne`（非空输出恰以一个 `\n` 结尾，空输出仍为空）、
`TrimEmpty`（删除全部末尾空行后保留一个换行：输入为空输出空；否则先裁掉末尾全部 `\n` 再补一个）。

空输入选 `""` 而非 `"\n"`：规范化是「等价改写」，空文件不是「缺末尾换行的文件」；
且 `""` 在三种策略下均是不动点。若 `EnsureOne("")="\n"`，则第二次进入时输入非空、已有一个换行，
结果仍 `"\n"`，看似幂等，但它把「有无内容」这一信息凭空造出来，`TrimEmpty` 也无法与它一致。
故约定：没有任何内容（规范化后长度为 0）时绝不追加换行。`EnsureOne` 仅在「有内容且无尾换行」时补一个。

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三个策略的输出都不含 `\r` 与行尾空白，再规范化时 EOL/空白无变化，结尾已是策略不动点，故幂等。

## 2. 被删字节的 ToOut

`span` 记录一串「run」：相邻运行合并。保留运行 orig==out；删除运行仅记 orig 区间，输出长 0；
末尾追加运行 orig 长 0。ToOrig(o)：二分定位 run，删除点取其后一个保留字节位置（无则取 orig 终点）。
ToOut(i)：i 在保留段按位移；在删除段取删除点之后的输出位置（后继约定）；i 为 orig 终点时，
若末尾有追加则取追加起点，否则取 out 终点。于是被删的行尾空白与 `\r\n` 中的 `\r` 都映射到
行尾 `\n` **之前**（光标停在行内逻辑行尾，而非跳到下一行），符合编辑器直觉。
ToOrig 单调不减；ToOut 单调不减。被删位置取后继使 ToOut(ToOrig(o))==o 对所有输出字节位置成立。
插入位置是左闭右开区间 [0,len] 的终点特例：该单点不是任何输出字节，对追加的 `\n` 取「前置约定」
（ToOrig 最后一个输出位置=orig 终点，ToOut(orig 终点)=追加起点），故除该单点外互逆恒成立。

## 3. 空白缓冲超限

待判空白必须缓冲（提前输出违反第 5 条幂等，提前丢弃违反第 2 条——流中途截断时它可能是行中空白）。
超限时两选：强制判定为「非行尾」输出，则当真实行尾在下一片到达时，本应删除的空白被永久保留，
第 5 条被破坏且输出不可恢复；拒绝则只是一次有界失败，不产生任何错误输出。选择**拒绝**：
返回带原文偏移的哨兵错误 `ErrWhitespaceLimit`，已输出内容保留，实例进入终态（再写返回
`ErrClosed`），状态完全可解释（偏移即那串空白的起点）。

## 4. par 拼接

段首可能继承前一段的待定 `\r`，段尾可能悬着 `\r` 或一串未判定空白。做法：切点（边界）先做
「吸附」修正——从切点向左回退，跨过空白串；若再前一个字节是 `\r`，再退一格把 `\r` 也归左段；
随后强制各边界单调不减（取 max(prev)），得到最终切段。这保证任何空白串与任何 `\r?\n` 序列都不
被切开。左段 [a,b) 用「内部结束」方式规范化：喂入段字节后再喂一个标记字节（非空白非换行），
令悬挂 `\r`/空白被强制按非行尾落定，然后把输出与映射表截到该标记之前；最后一段用正常 `Close`，
适用选定的末尾策略。每段 span 的 orig/out 坐标加上各自全局基址（前序段的原文与输出累计长度），
再顺序拼接。修正回退范围 ≤ 缓冲上限 + 1，故每段只需 O(限制) 额外回看；K 个 goroutine 只写各自
的局部结果，经 channel 归并，race 干净；结果与切分点无关，与单流一致。
