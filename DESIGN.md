# DESIGN

## 非法单元

采用 WHATWG「最大合法前缀」：从期望新字符处读首字节；随后只接收仍能延长合法 UTF-8 的 continuation。遇到不能延长前缀的字节时，前缀本身成为一个非法单元并输出一个 U+FFFD；终止字节不吞，重新当首字节。流结束时残留前缀整体成一个非法单元；严格模式则报截断。

因此 `F0 90 80 41` 中前三字节仍是 4 字节字符前缀，遇 `41` 一次替换；`E0 80 80` 的第二字节已不合法，`E0` 成单元，第二个 `80` 终止并重开，第三个 `80` 再成单元。`ED A0 80` 因第二字节越过 9F 同理三个；`C0 AF`、`F4 90 80 80` 同规则。`E2 82` 在 EOF 是一个未完成前缀，只替换一次。

| 首字节 | 第二字节区间 | 第二字节非法 | 后续 continuation 缺失/非法 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 吞首字节 | 缺则吞 1；第三字节前结束即无第三字节 |
| `E0` | `A0..BF` | 吞 1 | 第三字节非法吞 2 |
| `E1..EC` | `80..BF` | 吞 1 | 第三字节非法吞 2 |
| `ED` | `80..9F` | 吞 1 | 第三字节非法吞 2 |
| `EE..EF` | `80..BF` | 吞 1 | 第三字节非法吞 2 |
| `F0` | `90..BF` | 吞 1 | 第三字节非法吞 2，第四字节非法吞 3 |
| `F1..F3` | `80..BF` | 吞 1 | 同上 |
| `F4` | `80..8F` | 吞 1 | 同上 |
| `80..BF` | 无 | 单独吞 1 | 不适用 |
| `C0 C1 F5..FF` | 无 | 单独吞 1 | 不适用 |

## 缓存与复杂度

合法 UTF-8 最长 4 字节；提交给下一次 `Write` 前最多缓存 3 字节，读入终止字节的瞬间临时持有 4 字节，故硬上限为 4。任何 continuation 一违反上表即可立即判非法，只有仍满足区间的前缀需等待。UTF-16 最多缓存 1 个奇数字节；组成高代理后再缓存 2 字节等低代理。每个输入字节作为终止字节最多检查一次、作为下字符字节最多再检查一次，所以检查次数 ≤ 2N；残留在缓存中的字节不计入本次 `Write` 的返回值。

上限触发时只在完整标量边界停止：若目标缓冲不足以容纳 UTF-8 1..4 字节或 UTF-16 2/4 字节，就不读取该标量。返回值只含已完整输出标量对应的字节；旧缓存和本次未读字节都由调用方重传给新实例，因而拼接逐字节相同。

## par 对齐

仅并行 UTF-8 输入。第 0 段从 0 开始；每个原始切点先看上一字节是否为 lead，或切点处是否为 continuation 且其前 1..3 字节内有尚未满足长度的 lead。若是，新段起点移回该 lead；否则保持切点。上一段的分配结束点等于下一段分配起点。

移回保证一个多字节字符或最大非法前缀只归一段处理；不移回时，切点前若有非法前缀，已可在该段内由终止字节或段边界结算。`C3|80 80` 中第 1 段从 `C3` 开始，输出一个字符后再处理独立 `80`，总数为一个合法标量加一个非法单元，与单线程相同。各段关闭时的末尾替换只允许发生在全局 EOF 的最后一段。偏移按全局输入坐标统计。
