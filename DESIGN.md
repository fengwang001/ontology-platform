# 设计推导

## 1. 空行与单列空值
选择：空行（两个行尾紧邻，或流首就是行尾）**跳过**，不算记录。因为若算记录，则流末尾换行 `a\n` 也会多出一条记录，与"末尾换行不产生空记录"矛盾；跳过规则下 `a\n\n` 只有一条记录。
推导：单列 CSV 若某记录唯一字段是空且不写引号，回写结果就是空行，再次解析被跳过 → 丢记录。故 writer 对「单列、唯一字段为空、无引号标记」必须写成 `""`（引号只此一处，往返后引号标记仍相同）。多列中的空字段 `a,,b` 靠逗号定位，不需引号。
规范输入（canonical）：LF 行尾；最后一条记录无结尾换行；空行不出现；需保数据的空值用 `""`；CRLF 仅可出现在引号字段内并原样保留。规范输入逐字节往返。

## 2. par 切点
每段无法仅凭本段确定起点是否在引号内。方案：段 0 以"引号外"单跑；其余每段按"起点引号外(OUT)/引号内(IN)"两假设各跑一遍。段 i 的真实起点 = 段 i-1 被选变体跑到段末的词法状态：段末在引号内选 IN，否则 OUT；段末字节是 `\r` 时追加 CR 变体。从左到右逐段确定。
IN 变体种子 = OUT 机器快照置"左界已开引号"（深度1），并携带待续字段已写字节数与追加偏移日志。长度上限不做两遍：词法只在记录边界判定，字段在段内结束时用追加偏移日志换算第 limit+1 字节的全局偏移，超限位置与单线程一致。
坐标换算：事件携带段内记录号/字段号，拼接时加前段计数增量；字节偏移为全局绝对偏移（按全局偏移喂词法）。错误偏移同理，记录号/字段号加增量。两变体对引号外文本转移一致，被选变体事件流即单线程状态机在该段的事件流，拼接逐位相同。每段最多两遍，处理 ≤ 2n+O(K)。

## 3. CR 待定
词法设 CR 待定态：引号外见 `\r` 不终结记录，等下一字节——`\n` 则行尾；其它字节（含 EOF）报孤立 CR。半包续传时该态挂起在机器内，下次 Feed 首字节即决。par 切点落在 `\r` 后时，由左段末尾状态触发右段 CR 变体。流在 `\r` 处 Close → 孤立 CR。引号字段内 `\r` 是普通内容，无待定。

## 4. 上限"立刻"
字段按**最终值字节数**计数：引号内 `""` 两个引号合为一个内容字节。每写一个内容字节前检查，第 limit+1 字节到达即报错（偏移指向该字节），不等记录结束；EOF 时未闭合错误先于长度错误。字段数：每见逗号前检查。记录总数：记录终结产出前检查。错误后机器进入终态，后续 Feed 返回同一错误。

## 5. 状态转移表
状态：FS=字段开始，B=未引号中，Q=引号中，QS=刚见闭合引号，CR=行尾CR待定。
| 状态 | `,` | `"` | `\r` | `\n` | 普通 | EOF |
|---|---|---|---|---|---|---|
| FS | 出空字段→FS | →Q | →CR | 结束0字段记录→FS | 出首字节→B | 空流结束 |
| B | 结束字段→FS | 错:裸引号 | →CR | 结束记录→FS | →B | 结束最后记录 |
| Q | 追加 | →QS | 追加 | 追加 | 追加 | 错:未闭合 |
| QS | 结束字段→FS | 追加一个引号→Q | 结束字段→CR | 结束字段+记录→FS | 错:闭合后垃圾 | 结束最后记录 |
| CR | 错:孤立CR | 错:孤立CR | 错:孤立CR | 结束记录→FS | 错:孤立CR | 错:孤立CR |
注：0 字段记录（FS 直接遇换行）被 table 层按空行跳过；QS 遇 `\r` 为先结束字段再进 CR 待定。
