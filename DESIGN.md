# DESIGN

方言：逗号分隔；记录以 `\n` 或 `\r\n` 结束；引号字段内 `""` 表示一个 `"`，逗号与换行原样保留。偏移均为全局字节偏移（从 0），记录号/字段号从 1。

## 状态转移表（状态 × 字节类别 → 下一状态 / 动作 / 错误）

状态：FieldStart（字段开始）、Unquoted（未引号字段中）、Quoted（引号字段中）、QuoteSeen（引号字段内刚见 `"`）、CRPending（未引号字段末尾见 `\r` 待定）。

| 状态 \ 输入 | `,` | `"` | `\r` | `\n` | 其他 | EOF |
|---|---|---|---|---|---|---|
| FieldStart | 结束空字段(未引号)；FieldStart | Quoted；字段标记为引号 | CRPending | 结束空字段+记录；FieldStart | Unquoted；追加 | 结束空字段+记录（若已开始） |
| Unquoted | 结束字段；FieldStart | 错误 BareQuote | CRPending | 结束字段+记录；FieldStart | 追加 | 结束字段+记录 |
| Quoted | 追加 | QuoteSeen | 追加 | 追加 | 追加 | 错误 UnclosedQuote |
| QuoteSeen | 结束字段；FieldStart | Quoted；追加一个 `"` | CRPending | 结束字段+记录；FieldStart | 错误 JunkAfterQuote | 结束字段+记录 |
| CRPending | 错误 LoneCR | 错误 LoneCR | 错误 LoneCR | 结束字段+记录；FieldStart | 错误 LoneCR | 错误 LoneCR |

「结束字段+记录」在行首空字段且记录未开始时不产出记录（末尾换行不产生空记录，见下）。

## 推导一：空行与单列空值

把输入按行尾切分：每消费一个行尾产出一条记录；行尾之间零字节（空行）无法跳过——否则单列空值记录将无从表示，且 `a\n\n` 会丢行。故空行 = 一条「只含一个未引号空字段」的记录。文件只有末尾一个换行时，换行后没有新字节，记录未开始，Close 不产出幽灵记录（规则：Close 只冲刷「已开始」的记录）。推论：单列表中「未引号空字段」记录写出来就是空行，可逆；但「引号空字段 `""`」记录若写成空行，再解析会得到未引号空字段——引号标记丢失。故回写器对「单列表中唯一字段为空的记录」必须写成 `""`（无论原标记），这就是「必要引号」的第五种情形（另四种：字段含 `,` `"` `\r` `\n`）。多列记录中 `,` 本身已占位，空字段可不加引号。

## 推导二：par 的切点

段 i 的真实入口状态只可能是五种机器状态之一。为每个段并行预计算三个候选解析：A=FieldStart、B=Quoted、C=QuoteSeen。C 用「段前虚拟拼一个 `"`」实现（等价于 QuoteSeen 入口，仅多处理 1 字节，常数开销）。Unquoted/CRPending 入口不可能需要单独假设：切点若在字段中间，前段必停在 Quoted/QuoteSeen（引号字段内）或 Unquoted——而 Unquoted 入口与 FieldStart 入口对后续字节行为逐字节一致（都进入 Unquoted 追加），A 已覆盖；CRPending 入口意味着切点在 `\r` 之后，此时单线程在 `\r` 处已报错，或（`\r\n` 情形）`\r` 属前段、本段以 `\n` 开头，A 对 `\n` 的动作与 CRPending 一致（结束字段+记录）。拼接：从左到右，第 0 段取 A，其后每段取「入口状态 == 前段出口状态」的候选；段内错误即全局错误（段内坐标 + 段基址 = 全局字节偏移；记录号/字段号由重放已拼接事件流时的计数器得出），首个出错段之后的段丢弃。每段事件以「追加区间 [lo,hi) + 定界事件」记录，拼接后重放同一核心状态机，故与单线程逐位一致。字节处理总量 ≤ 2×n + 段数。

## 推导三：CR 待定

流式：见 `\r` 进入 CRPending 并挂起字段收尾，下一字节到达再判：`\n` → 行尾；其他 → LoneCR 错误；EOF → LoneCR（`\r` 不是合法记录结尾）。半包切在 `\r` 后天然安全：状态随解析器对象存续，下个 Feed 继续判。par：`\r` 永远属于前一段（切点落在 `\r\n` 中间时，后段以 `\n` 开头，其 A 候选在 FieldStart 态见 `\n` 的动作与 CRPending 见 `\n` 相同），故待定状态不跨段传递，拼接无需特判。

## 推导四：上限的「立刻」

字段字节计数在「值字节」产生时进行：引号字段内 `""` 只算 1（第二个 `"` 进入 Quoted 时计），引号字段内的换行、逗号各算 1。每追加一个字节就 `if n > max { 报错 }`，第一个超限字节到达即拒绝，不缓冲整条记录。字段数上限在「结束字段」使当前记录字段数超限时立即报；记录数上限在「结束记录」使总数超限时立即报。超限后解析器进入终态：已产出的完整记录保留，后续 Feed/Close 返回同一错误。
