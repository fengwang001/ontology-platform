# DESIGN：流式行尾/空白规范化器（仅标准库）

## 1. 末尾换行策略的推导

规范化核心（与策略无关）：`\r\n`、`\r` → `\n`；删除每行末的 ` \t`；不增删换行个数。
之后对**末尾空行**（结尾连续 `\n`）施加策略：
- Keep：原样保留。
- EnsureOne：裁剪为至多一个 `\n`。
- Trim：删除全部末尾空行；结果非空则补一个 `\n`。

空输入必须在 EnsureOne 下二选一：`""→""` 或 `""→"\n"`。
若 `""→"\n"`，则 `N("\n")="\n"`，幂等成立，但它对"无内容"凭空制造了内容，
且与 Trim 的直觉冲突。本实现选 `""→""`：只在"曾有内容"时保证恰好一个尾换行，
即先裁到 ≤1 个 `\n`，且输入核心输出非空才保留它。此时 `N("\n")="\n"`、`N("")=""`，三者皆幂等。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

补出的 `\n` 无对应原文字节：映射中是 orig 区间长度 0 的合成区间（见 §2 结尾）。

## 2. 被删字节 ToOut 的推导

不变量：ToOut 单调不减；对输出偏移 o 有 ToOut(ToOrig(o))=o。
映射表只保存"存活区间" `[os,oe) ↔ [is,ie)`（长度相等），删除处即存活区间之间的空隙。

`\r\n`：`\r` 被删、`\n` 存活（输出 `\n`）。
设空隙 orig 为 `[a,b)`、其后存活区间从 orig b 起、out 从 p 起（p 即输出换行位置）。
若令空隙内 ToOut(i)=p+（空隙后位置），则 ToOrig 对输出 `\n` 取的是其 orig 起点 b，
无法回到 p。故只能令空隙整体压到**空隙前**的输出位置：ToOut(i)= 空隙前输出末位
（首行空隙前为 0）。于是 ToOrig(p)=b、ToOut(b)=p，互逆成立；单调不减成立。

行尾空白同理：被删空白压到"其后换行的输出位置之前"——即该行内容的输出末位
（行内容为空时压到行首）。
编辑器光标落在被删空白里时，映射到**换行符之前**（行尾），语义为"行内容末尾"。

合成换行（orig 长度 0）无法严格满足全部互逆：输出中多出的字节没有唯一原像。
规定其 `\n` 的输出位置双向别名到原文终点 L：ToOrig(o)=L，且 ToOut(L)=o。
这是唯一的例外点，仅在 EnsureOne/Trim 对原本无尾换行输入补换行时出现；DESIGN 明示，
不影响其余所有偏移的单调与互逆。

## 3. 空白缓冲超限的选择

待定空白必须缓冲（看到行尾/EOS 才知道是否行尾空白）。方案：
(a) 拒绝：可判定错误，带原文偏移，已输出保留，进入终态——不改变任何已提交映射，
故第 4/5/6 条对"成功路径"完全不受影响，第 4 条的切分一致性也成立（同一逻辑空白串
长度与切分无关，超限与否确定性一致）。
(b) 强制判为"非行尾"输出：若随后真出现行尾，将产生本应删除的空白，违反第 2、5 条
（输出含行尾空白、再规范化结果不同）。
故选 **(a) 拒绝**：哨兵错误 ErrPendingLimit，错误记录起点原文偏移；关闭后实例不可再用。
输出超限同理为 ErrOutputLimit（带已达偏移），严格模式 NUL 为 ErrNUL（带原文偏移），
终态后写入为 ErrClosed，互不相同、可用 errors.Is 判定。

## 4. par 拼接的修正

各段用 Keep 策略独立规范化，再做一次全局尾部策略。段边界只可能有两类未决：
1. 前段以 `\r` 结束、后段以 `\n` 开始：两段各产一个 `\n`，必须删掉前段尾 `\n`，
   并把该输出位置在映射中并入后段 `\n`（即删一个合成空区间），输出少 1 字节。
2. 前段以一长串 ` \t` 结束：它们在段内被当行尾空白删除。拼接时看后段：
   - 后段首行在换行前出现非空白内容 → 那串空白其实是"行中间"空白，必须原样补回
     （输出与映射都恢复），补在两段交界处；
   - 后段首先是行尾（`\r`/`\n`）或后段为空 → 确属行尾空白，维持删除。

每段只需记录末尾一个 `\r` 标志、末尾连续 `[ \t]+` 串（受同一 pending 上限约束），
以及后段首行的"首个非空白字节是否先于行尾/EOF"这一 bool；故修正只涉及每段边界
**有界的**尾部前缀（一个 `\r` 加一串空白），不会回溯整段。
映射拼接：段内 span 坐标加"该段在全局 orig/out 中的基偏移"（out 基偏移随上述增删
逐段修正），再调用与单路相同的 span 合并；因此最终区间序列与单线程逐字节相同，
双向查询与第 6 条全部继承。K∈1..8、切点任意、goroutine 并发，无共享可变状态。
