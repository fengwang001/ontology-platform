# 设计推导

## 1. 非法单元切分（WHATWG 式「最大合法前缀」规则）

状态机从首字节 b0 起步，需要的续写长度 L=1/2/3。收到 b1 时先查 b0 的第二字节合法区间；b1 不合法则 b0 单独成一个非法单元（吞 1 字节），b1 退回主循环。b1 合法但其后任何字节不在 80..BF（或流结束）时，吞掉「已收到的该序列全部字节」（2..4 字节）成一个单元。合法则输出标量。

推导七样例：`F0 90 80 41`：b1=90 合法，b2=80 合法，b3=41 非续写 → 吞 3；`E0 80 80`：E0 的第二区间是 A0..BF，80 非法 → 吞 1，80 非法首字节吞 1，80 吞 1；`ED A0 80`：ED 区间 80..9F，A0 非法（落入代理区）→ 吞 1，剩 80、80 各 1；`C0 AF`：C0/C1 永不合法 → C0 吞 1，AF 续写字节吞 1；`F4 90 80 80`：F4 区间 80..8F，90 非法（越界 >U+10FFFF）→ F4 吞 1，90、80、80 各 1；`E2 82`+结束：b1 合法但续写缺失 → 吞已收到的 2 字节；`80 80`：续写字节各吞 1。规则同时解释了非最短（C0/C1、E0 80..9F）与越界（ED A0..BF、F4 90..BF、F5..FF）。

| 首字节 | 第二字节区间 | b1 非法吞 | b1 合法但后续坏/缺吞 |
|---|---|---|---|
| C2..DF | 80..BF | 1 | 2 |
| E0 | A0..BF | 1 | 2..3 |
| E1..EC | 80..BF | 1 | 2..3 |
| ED | 80..9F | 1 | 2..3 |
| EE..EF | 80..BF | 1 | 2..3 |
| F0 | 90..BF | 1 | 2..4 |
| F1..F3 | 80..BF | 1 | 2..4 |
| F4 | 80..8F | 1 | 2..4 |
| 80..BF | 无（续写出现在主循环） | 1 | — |
| C0 C1 F5..FF | 无（永不合法） | 1 | — |

## 2. 切分点缓存

UTF-8 最长序列 4 字节，进入多字节序列后最多再等 3 个字节，故待决缓存硬上限 3（首字节保留在解码器状态，缓存只放续写）。看到 b1 不在首字节对应区间，或 b2/b3 不在 80..BF 时立即判非法并回吐该字节，绝不等到流结束；只有「已收到的续写都合法、但在 80..BF 处流结束」才在 Close 判截断。UTF-16 待决缓存上限 1：半个 code unit（奇数字节）或一个高代理（等低代理，下一个 code unit 不是低代理时立即放行重解析）。

## 3. par 切点对齐

单元最长 4 字节。每段（除段 0）回看切点前至多 3 字节作为「前缀上下文」：该 worker 的流配 offset=-len(ctx)，从 ctx 起始解析，起始位置在 ctx 之前或之中的单元由本段负责（输出与统计都算本段）；段尾 Close 时抑制残尾（suppressTail），残尾由下一段的上下文负责。因此切点前最多 3 字节、切点后 0 字节构成重叠判定区，任何字节不可能被两段都算或都不算。

为什么不需要回看更多：在 ctx 的 3 字节内按状态机解析，合法的 E2/F0/F4 类首字节只可能是这 3 字节里的第一个；更前面的首字节若合法，其序列必然在切点前闭合。`C3|80 80`：切点前 C3 属段尾被抑制，段 1 ctx=[C3]，C3 后接 80 合法成一个标量，第二个 80 是非法续写——与单线程（一个合法字符+一个 FFFD）完全一致；若切点在 `E2|82 AC`，ctx=[E2] 后 82 合法，AC 闭合，字符归段 1。

## 4. 上限断点（Write 返回值）

Write 的 n 只计「已变成输出的完整单元」所占输入字节；尚在待决缓存中的字节不算已消费，调用方以 `pending + p[n:]` 续传新实例。越界在下一个完整单元发射前判定：已发射的标量保留、半单元（含半个代理对）绝不输出，返回 ErrOutputLimit。新实例首字节消费挂起的 pending，故拼接与不限上限逐字节相同。UTF-16 输入 n 同样以输入字节计（偶对齐）。

## 5. 复杂度

每字节至多：主循环查 1 次 + 作为续写被查 1 次 = 2 次（续写吸收后不再回主循环）。par 每段只额外检查 ≤3 字节前缀，8 段总检查 ≤ N+24。
