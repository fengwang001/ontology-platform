# 设计推导

## 1. 取消与唤醒撞上时许可去哪

设 R=Release 线程，C=取消线程，W=等待者，`mu` 为 sem 与 waitq 共用的临界区：

交错 A（许可凭空消失）：1) R 在 `mu` 下从队首摘出 W；2) R 释放 `mu`，尚未通知 W；3) C 进入 `mu`，发现 W 已离队，向 W 报“已取消”并减等待者数；4) R 再把许可交给 W——W 已自认取消，无人接收，许可消失，违反不变量 2。

交错 B（又取消又拿许可）：1) C 在 `mu` 下标记 W 取消、减等待者数；2) C 尚未让 W 醒来；3) R 在 `mu` 下按 FIFO 仍选中 W 并发许可；4) W 同时收到取消与许可，违反不变量 3。

处理办法：**摘队与状态判定在同一临界区内原子完成，离队即“已获得许可承诺”，取消只允许作用于仍在队的等待者。**
- R 在 `mu` 下调用 waitq.Pop：FIFO 摘首节点并返回其就绪通道，随后在同临界区 `issued++`、`avail` 不变（摘队即授权），出锁后 close 通道。
- C 进入 `mu` 调用 waitq.Cancel(凭证)：映射中查不到（已被 Pop 摘走）则返回 false，Acquire 只能走“已就绪”路径并必须 Release；查得到则摘节点、减等待者数、登记取消数、close 一个独立的取消通道。
交错 A 的步骤 3 中 C 查不到 W，无法报取消；交错 B 的步骤 3 中 R 已查不到 W，只能选下一个或把许可放回可用池。故取消者永不持有许可，许可要么被在队等待者原子摘走，要么回池，不变量 2、3 同时成立。

## 2. 放行顺序

采用**严格 FIFO**：Pop 永远取队首；Pop 成功（队列从非空变空）时，释放者许可直达队首，否则回可用池。若允许“任意一个正在调度的等待者”被唤醒，哪个 goroutine 先被调度由运行时决定，同一组操作序列重复执行会得到不同放行顺序，违反不变量 5。FIFO 只由入队次序决定，纯内存确定可复现。

## 3. TryAcquire 与队列的关系

队列非空时 TryAcquire 必须立即失败。它若能取池，就等于后来者在“刚有 Release、Pop 尚未执行”的窗口插到队首之前，与第 2 条 FIFO 矛盾，且该窗口是否出现取决于调度，使结果不可复现。规则：`avail>0 且 len(waitq)==0` 才成功。

## 4. 容量为 0

合法配置。不变量 1 为 `issued+avail==cap`，cap=0 时仅要求二者恒为 0：初始满足；池永空，Acquire 只能排队等待（可被 ctx 取消），不会发放；Release 只接受已发放凭证，无许可可发放故无凭证可 Release。负容量无法构造非负计数满足守恒，故 `New(-1)` 返回哨兵错误 `ErrNegativeCapacity`。
