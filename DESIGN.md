# 严格 Base64：非规范尾部推导

一个无填充 4 字符组表示 3 字节：把 4 个 6 比特值首尾相接成 24 比特，再切成 3 字节。
有填充时，明文长度模 3 只可能为 1 或 2，编码器必须把“没有明文对应的比特”补 0：

- `XX==`：只有第 1 字节有效；第 1 个字符给高 6 位，第 2 个字符只提供该字节低 2 位，
  因此第 2 个字符的低 4 位必须全为 0。
- `XXX=`：有 2 字节有效；第 3 个字符提供第 2 字节的低 4 位，其低 2 位必须全为 0。

所以严格判定是：按 24 比特还原后，被丢弃的尾比特必须等于编码时填入的 0。
`QQ==` 的第二值为 `Q=010000`，低 4 位为 0，合法；`QR==` 的 `R=010001`，低 4 位为
`0001`，这 4 个比特在宽松解码时被丢弃，却携带了额外信息，故拒绝。同理 `QUI=` 第三值
`I=001000` 的低 2 位为 0，而 `QUJ=` 中 `J=001001` 的低 2 位为 1。

这保证“合法且严格的串”恰好等于某个字节串经标准编码得到的串：若非零尾比特也被接受，
多个不同输入会解码成同一字节串，`Encode` 的结果不再是唯一规范表示，二次编码也无法还原
原串。要求尾 0 后，严格解码与标准编码互为逆运算。

填充还必须满足完整形态：无 `=`、仅一个末尾 `=` 但不在 3/4 位置、两个以上 `=`、
或在流末之前出现 `=`，都不是任何 Encode 结果，按填充位置错误拒绝。

## 七条语义测试对照

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1 标准字母表与 MIME 换行 | 最后补齐 |
| 2 规范形式 | 最后补齐 |
| 3 换行位置 | 最后补齐 |
| 4 五类错误与偏移 | 最后补齐 |
| 5 跨切分点一致 | 最后补齐 |
| 6 往返 | 最后补齐 |
| 7 输出上限 | 最后补齐 |
