# 规范化器设计推导

## 1. 空输入/换行样例 × 三种末尾策略

策略：`Keep` 原样；`EnsureOne` 非空且无最终 `\n` 则补一个；`Trim` 先删全部末尾空行再补一个 `\n`（空输入除外）。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

**推导**：EnsureOne 对空输入若输出 `"\n"`，N(N(""))="\n" 虽幂等，但补出的 `\n` 原文无对应字节，违反第 6 条双向映射（插入使该输出位无互逆原像）。故选 `""`，EnsureOne≡「输出若非空则以单个 `\n` 结尾」。Trim 同理：空输入输出 `""`，非空时仅保留一个末尾换行。三种策略输出都已无 `\r` 与行尾空白，再规范化为恒等，幂等成立。

## 2. 被删字节的 ToOut

输出区间左闭右开 `[0,len)`，逐字节映射：保留字节一一对应；删除点把输出坐标夹住不动。`"ab   \n"`→`"ab\n"`：被删空白的三个原偏移都映射到下一个产生字节（`\n`）的输出位 3，即落在换行符**之前**——光标停在被删空白里不应被推到下一行。`\r\n` 的 `\r` 同理映射到 `\n` 位。模型：保留段 `(o0,o1)->(p0,p1)` 满足 `o-o0=p-p0`；删除段输出长度 0。查询取最后一个起点 `<=x` 的段（同界取起点匹配的段）。于是保留字节上 `ToOut(ToOrig(o))=o`，两函数单调不减。唯一例外：EnsureOne 给无换行非空输入补的合成 `\n` 无原像，末点互逆不成立（基数 1>0 所限），AUDIT 标注为可判定特例。

## 3. 缓冲超限

未判定空白必须缓冲，设 `MaxPendingWS`（默认 1MiB），**选择拒绝**：返回具名 `*ErrPendingOverflow`（带已确认原文偏移）。若强制判定为非行尾并输出，后续出现 `\r`/`\n` 时已吐空白无法收回，违反第 2/5 条（输出带行尾空白、不幂等）与第 6 条（映射已固定却需改成删除）。拒绝不破坏任何不变量：被拒前前缀即该前缀整体规范化的结果（见截断语义），状态可解释。输出上限 `MaxOutput` 在产生超限字节前返回 `*ErrOutputLimit`。

## 4. par 拼接

顺序扫描，维护已确认前缀与尾部**未判定后缀** u：仅 0/1 个 `\r` 加若干 `[ \t]`（空白情形包含到本行起点的整串尾部空白），长度有上界 `1+MaxPendingWS`。切点 c 的实际段边界为 `c-len(u)`，段内容为 `u` 拼接下一段确认前缀。各段以 `Keep` 规范化（末尾策略只作用于全局最后一段），span 段按 `(origBase,outBase)` 平移；同界删除段与邻段合并（零长吸收），区间数只随删除点增长。每边界仅回看一个有界后缀，修正范围有上界；u 与右段首字节合并判定，消除 `\r|\n` 与空白中间切分歧义，任意切分/K 与单流逐字节一致。

## 其他

- 严格模式遇 NUL 返回 `*ErrNUL`（带原文偏移），已输出保留并进入终态；终态后写或重复关闭返回 `ErrClosed`。NUL、缓冲超限、输出超限均具名且带 `OrigOffset`，以 errors.As/Is 区分。
- 单个 `norm.Writer` 非并发安全；多实例可并发。`span` 用排序坐标二分，最近一次查询检查的区间数存于未导出计数器。
