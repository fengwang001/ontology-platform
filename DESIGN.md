# 设计：只追加日志的段索引与范围回放

## 1. 分层与文件布局
- `event`：事件 = 序号 `Seq int64` + 载荷 `Payload []byte`（允许空），定长头 + 原始载荷编解码。
- `segment`：一个段 = 一个自描述文件。固定 24 字节头（魔数 4、版本 2、首序号 8、事件数 8，小端），其后逐条追加帧。
- `sparse`：每 N 条记一个锚点 `(seq, offset)`，锚点文件自带间隔 N，可独立重建。
- `replay`：加载目录下所有段，按序号范围定位并顺序回放，暴露跳过事件数与读取字节数计数器。
- `repair`：段/索引的损坏检测、最大可恢复前缀截断、段头计数改正、索引重建、序号缺口检测。

段文件命名 `seg-%012d.log`，序号为段首序号，按名排序即按序号排序；索引为同名加 `.idx`。

## 2. 帧格式
每条事件一帧，全部小端：
`[len uint32][ event: seq int64 + payload ][crc uint32]`，帧总长 `4 + len + 4`。
- `len` = 事件编码字节数（8 + 载荷长度）。
- `crc` = IEEE CRC32（`crc32.ChecksumIEEE`）覆盖的是**事件编码**（seq + 载荷），放在帧尾。
把 CRC 放帧尾后，逐字节截断能稳定、确定地落入四类：
头 24 字节读不全 → 头部不完整；某帧长度 4 字节读不全 → 长度前缀不完整；
长度读全但事件体读不全（读到 `< len` 字节）→ 事件体不完整；
事件体完整但尾部 CRC 读不全或校验不符（或 len 非法）→ CRC 不匹配。
截断到某帧的整数边界时该帧完整可读，属于干净前缀（头计数可能偏大，由 repair 改正）。

## 3. 稀疏索引的定位推导
锚点是单调的 `(seq, offset)`，offset 指向该事件帧起点。
回放 `[from, to]` 必须从**不大于 from 的最大锚点**开始顺序扫描：
- 锚点是“扫描入口”，只允许从入口**向后**走，不能回退。
- 若选“数值最接近 from”的锚点，它可能大于 from（from 落在两锚点之间时最近点常在后方），
  从那里开始会漏掉 from 与该锚点之间的事件；而小于 from 的锚点至多跳过 N-1 条即可到达 from。
因此正确规则是 `max{ a | a.seq <= from }`（floor 锚点），绝不能用最近邻。
三种位置：
1. from == 某锚点序号：命中该锚点，跳过 0 条。
2. from 在两锚点之间：命中前一个锚点，跳过 `(from-a.seq)` 条，范围 1..N-1。
3. from < 第一个锚点：无 floor 锚点，从段/日志头开始扫（索引第一段首序号天然是锚点，
   故等价于从首事件锚点进入），跳过至多 N-1 条。
定位阶段跳过事件数因此严格 `< N`，这是索引确实被使用的可证明上界。

## 4. 索引可丢弃与逐字节重建
索引是纯加速结构，其中每个字节都必须能由段推导：
- 首锚点永远是段首事件，偏移为固定头长；
- 其后每数 N 条记一锚点，偏移 = 头长 + 此前各帧 `4+4+len` 之和；
- 间隔 N 写在索引头里（重建时显式给定，与建索引时一致）。
因此索引中不得存放任何“只在写入现场可知”的信息（如内存指针、时间戳、随机盐）。
删除 `.idx` 后顺序读段、按同一 N 重新生成，结果与原文件**逐字节相同**（确定性编码、固定字段顺序）。

## 5. 跨段与连续性
段头记录首序号与事件数。相邻段必须满足
`prev.firstSeq + prev.count == next.firstSeq`，否则存在序号缺口，repair 用
`errors.Is(..., ErrSeqGap)` 报出并给出缺口区间 `[prevEnd+1, nextFirst-1]`。
回放时依次打开 floor 锚点所在段及其后各段，跨段边界直接衔接下一文件首事件，
做到不重不漏；`to` 超过最大序号则自然在末段结束，不报错。

## 6. 边界语义
- 空日志（无段 / 零事件）：回放返回空结果，不报错。
- 单事件、空载荷（len=8）：合法。
- `from > to`：`ErrBadRange`。
- `from < 最小序号`：不报错，从头开始（floor 语义的自然延伸：没有比它大的事件要跳过）。
- `to > 最大序号`：回放到末尾。
- 区间恰等于一整段、跨三段：均由段迭代覆盖。

## 7. 索引失效与回退
锚点偏移可能被篡改（指向事件中间）。到达该偏移后：
- 读不出合法长度前缀、len 越界、或该帧 CRC 不符 → 判定索引失效（`ErrStaleIndex`）。
- 自动回退为**全段顺序扫描**定位，仍给出正确结果；结果报告里标注 `StaleIndex=true`。
段被截断但头计数偏大时同理：顺序读以“首个不完整/损坏帧”为界，返回其前的干净前缀。

## 8. 并发
追加写在单 `os.File` 上由段 Writer 内 mutex 串行化；帧以一次 `Write`（单 syscall）落盘，
读取方打开独立文件句柄，只会看到 0 字节或整条帧，看不到半条。
并发回放看到的始终是某个一致前缀，事件序号连续无缺口。

## 9. 复杂度与计数器
- 跳过计数器：定位阶段丢弃的事件数，严格 `< N`（10 万事件、N=128、200 随机 from 逐一断言）。
- 字节计数器：只统计定位锚点之后实际从段读取的帧字节（`8+len` 每帧），
  上界 = 区间内事件编码长度之和 + 一个锚点区间（N 条）的编码长度。

## 10. repair 结果
四类错误用 `errors.Is` 区分：`ErrTruncHead`、`ErrTruncLen`、`ErrTruncBody`、`ErrCRCMismatch`；
另有 `ErrStaleIndex`、`ErrSeqGap`、`ErrBadRange`。
截断段：定位最大可恢复前缀 → 物理截断文件 → 头计数改写为实际可读条数 → 顺序读不再报错。
