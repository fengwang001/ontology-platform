# Quoted-Printable 编码 / 严格解码设计（RFC 2045 子集）

## 规则 3 的两处推导

1. 行末空白（空格/制表符）：严格解码器规定「行末出现未转义的空白」是错误，
   因为这类空白在传输中容易被尾随空白清理程序删改，语义无法保证。因此编码器
   必须缓存自上一个非空白 token 之后的空白串：其后若来内容字节，说明不在行末，
   原样冲刷；其后若来换行（`\n` 或 `\r\n`）或输入结束，则按 `=20`/`=09`
   冲刷。样例 `a \n → a=20\r\n`、`a b → a b`、`a(结束) → a=20` 即此规则。
2. `=XX` 不可拆：多字节 token 原子输出。设当前行长度（不含 `\r\n`）为 c，
   token 长度为 k（可打印字符 k=1，`=XX` k=3）。软换行 `=` 也计入 76，故
   当 c+1+k > 76（即 c+k > 75）时先输出 `=\r\n` 再输出 token。c=74、k=3 时
   74+3>75，故先软换行，不会出现 `=3\r\nD`。

## 软换行恰好插在空格之后

该空格**不算行末空白，原样输出空格**，即输出形如 `… =\r\n…`。理由：解码器
删除的是软换行序列 `=\r\n` 整体（`=` 与其后的 `\r\n`），空格保留在物理行行尾
但其后紧跟的是软换行 `=`，并非真正的行结束；下一行是续行，该空格逻辑上位于
一行中间，且严格解码器在软换行路径上不校验尾随空白。若反而编码成 `=20` 则属
多余转义，违反幂等要求中的最小转义性质。实现上软换行只在冲刷内容 token 时
插入，而冲刷出的待决空白已是「被后续内容确认的中间空白」。

## 规则 6 往返等式（准确形式）

记 N(x) 为换行归一化：把每个 `\n`（含 `\r\n` 中的）变成 `\r\n`，单独的 `\r`
保持为普通字节。则对任意字节串 x：

  Decode(Encode(x)) == N(x)

即除 `\n → \r\n`（`\r\n` 亦归一化为同一个 `\r\n`）外逐字节相等；特别地，单独
的 `\r` 往返后仍是 `\r`（编码时写成 `=0D`）。

## 最小转义（规则 7）

编码器只转义：`=`；33–126 之外的字节（单独 `\r` 含在内，写成 `=0D`）；以及
行末空白。这三类正是严格解码器拒绝或语义不保留的情形，其余一律原样，故不存在
可删去的转义；对合法 y，Encode(Decode(y)) 与 y 的差异只可能是 y 中多余的
`=XX`（解出可打印字节后被重新原样输出）。

## 语义–测试对照表

<!-- TABLE -->
