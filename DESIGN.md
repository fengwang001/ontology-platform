# 只追加日志：段索引与范围回放设计

## 1. 存储布局
- 一个目录即一个日志：`seg-000001.log`、`seg-000002.log`……以及对应 `seg-000001.idx`。
- 段头固定 32 字节，小端：magic(4)=`SLOG`、version(2)、flags(2)、firstSeq(8)、count(8)。
- 每条记录：length(4,uint32，记录体字节数) + 记录体(序号 uint64 + 载荷) + CRC32(4,IEEE，覆盖 length 与记录体)。
- 事件编码放在 `event` 包：seq(8) + payload；段层再加 length/CRC。
- 段头 `count` 是提交点：写完整条记录（含 CRC）后才把新 count 写回头。读者只信头中 count，
  因此并发追加时要么看到旧 count（旧前缀），要么看到包含新记录的新 count，永不读到半条。

## 2. 稀疏索引与定位推导
- 索引每 N 条记一个锚点：`(seq, recordStartOffset)`，首条（段内下标 0）必为锚点。
- 回放 `[from,to]` 必须选「不大于 from 的最大锚点」，再顺序扫描跳过 `seq < from` 的记录。
- 为什么不能选「最接近 from 的锚点」：最近锚点可能在 from 之后，从它开始会漏掉它之前、
  from 之后的事件；也可能在 from 之前但并非最大者，浪费定位但仍可能越界假设。只有
  `<= from` 的最大锚点保证「起点序号 <= from」，从而从该点到目标之间至多跳过 N-1 条。
- from 小于本段首锚点（=段首序号）时，直接从段头后开始；跨段时从包含 from 的最早段开始。
- 跳过计数：锚点下标为 N 的倍数，目标在锚点后 0..N-1 条，故定位跳过数严格小于 N。

## 3. 索引可丢弃与重建
- 索引是纯加速结构，只含 `(seq,offset)` 两类信息，二者都能从段顺序读出：
  seq 来自事件体，offset 来自读取游标。因此删除 idx 后顺序扫段即可逐字节重建
  （写入锚点的判定规则固定：段内下标 mod N == 0，文件头含 magic 与 N）。
- 禁止把「仅写入时可知」的状态（时间戳、进程 id、随机填充）放进索引。

## 4. 段边界与跨段回放
- 段头记录 firstSeq 与 count；连续性约束：前段 `firstSeq + count == 下段 firstSeq`。
- 回放按段顺序拼接，只读取与 `[from,to]` 相交的段，段内再按 seq 过滤，保证不重不漏。
- 区间等于一整段、跨三段均为该规则的特例。

## 5. 损坏分类、修复与回退
- 截断点按字节分类：头不足 32B=头不完整；length 不足 4B=长度前缀不完整；
  length 声明的体+CRC 超出可读数据=事件体不完整；数据够但 CRC32 不符=CRC 不匹配。
- 修复保留最大可恢复前缀：顺序读出全部合法记录，把头 count 改为实际条数，并截断文件到
  最后一条合法记录末尾；之后重读不再报错。
- 索引锚点偏移可能被篡改到事件中间：在该偏移处解析，length 不合法或 CRC 不符即判定索引失效
  （`ErrStaleIndex`），自动回退到该段从头全扫描，结果仍正确并在报告中标注。
- 段间缺口：相邻段不满足 `firstSeqA+countA==firstSeqB` 时报 `ErrSeqGap`，报告给出缺口区间。
- 四类错误均为哨兵错误，用 `errors.Is` 区分：头/前缀/体不完整、CRC 不匹配、索引失效、序号缺口。

## 6. 边界语义
- 空日志回放返回空结果、计数为 0，不报错；`from > to` 返回 `ErrBadRange`。
- `from` 小于最小序号：从最小序号开始（夹紧，不报错）；`to` 超过最大序号：回放到末尾。
- 空载荷合法（length=0 的记录体仍含 8 字节 seq）。单事件、整段、跨三段均支持。

## 7. 读取量约束
- 回放每段只从所选锚点偏移开始读到最后一条 `seq<=to` 的记录末尾；读取字节数计入计数器。
- 上界：区间内事件编码总长（length+体+CRC）+ 至多一个锚点间隔（起点锚点到 from 之间 < N 条）。
