# CSV 解析器设计推导

## 1. 空行与单列空值
选择：**空行（两个行终止符相邻）一律跳过，不作为记录**。解析器无法区分「单列表里的空值记录」与「行间多余空行」：
单列表中 `a\n\nb` 若空行当记录，语义与误插空行不可辨，且跳过空行使多行表对空白更稳健。
推导回写：若单列表某记录唯一字段为**未引号空值**，`Write` 输出为 ``（空串），加行终止符后恰是空行，
再解析时被跳过 → 丢数据。因此单列表空值必须写成 `""`（带引号空字段）。一般规则：列数==1 且字段为空
（无论原引号标记）时必须加引号；此外字段含 `,` `"` `\r` `\n` 必须加引号；有引号标记的空字段加引号。
规范输入：`Write(Parse(x))==x` 对规范输入成立——即 `Write` 的输出（LF 行终止、最小引号、无空行、
末记录带 `\n`；`""` 在解析时保留 Quoted 标记）。

## 2. par 切点
每段无法自知起点是否在引号内。方案：每段在**两种入口假设**下各跑一次状态机：
`fresh`（起点在引号外的字段开头）与 `inQuote`（起点在引号字段内）。段内状态机逐字节一次、可暂停，
输出事件流（字段内容片段、起止偏移、引号标记、记录边界、错误）与出口状态。
从左到右串行确定真实入口：第 1 段用 `fresh`；前一段出口若在引号内，本段取 `inQuote`，否则取 `fresh`。
若入口假设错误（如出口是「引号内刚见引号」需看下段首字节），拼接时按下段首字节二选一，仅回看 1 字节，
不重扫整段（故总处理次数 ≤ 2N+K）。跨段字段：事件偏移为全局绝对偏移（段起点+段内偏移），
inQuote 段首字段的 `Start` 记在段起点，拼接时由前一段修正为字段真正起点，内容按偏移顺序拼接。
记录号/字段号：各段携带种子计数，拼接时按已确定的前缀段累计数平移；错误位置同样加段起点偏移，
故全局字节偏移、记录号、字段号与单线程逐位相同。

## 3. CR 待定状态
状态机有 `crPending`：未引号字段末尾见到 `\r`，不立即判错（可能是 `\r\n` 行尾），也不立即收尾。
半包续传：`\r` 存为待定，下一字节是 `\n` 则行尾成立；是其他字节则 `\r` 为孤立 CR → 错误（偏移指向 `\r`）。
par：切点恰在 `\r` 之后时，出口为 `crPending`，由下一段首字节判定，与半包同一机制；
`\r\n` 被切点分开时同理。流在 `\r` 处 `Close`：无后续字节可匹配 `\n` → 判孤立 `\r` 错误（仅当非空流）。
引号字段内的 `\r` 是普通内容，立即累积，不进入 `crPending`，故原样保留 `\r\n`。

## 4. 上限「立刻」
MaxFieldBytes 按字段**已确认的内容字节数**计数：普通字节到达即 +1；引号字段内 `""` 在第二引号到达、
确认其为转义引号时 +1（第一引号仅为标记，不计内容）。计数在追加前做：`n+1 > max` 即在该超限字节
（偏移指向它）返回 ErrFieldTooLarge，不缓冲后查。MaxFields 在字段开始（首个内容/引号到达）时计数，
超限立刻返回；MaxRecords 在记录完成时计数。终态后 Feed 返回同一错误。

## 5. 状态转移表

| 状态 \ 输入 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| fStart 字段开始 | →bare，开字段 | →quoted | →crPending | 结束记录 | →bare，累积 |
| bare 未引号字段 | 结束字段 | **ErrBareQuote** | →crPending | 结束记录 | 累积 |
| quoted 引号字段中 | 累积 | →qSeen | 累积(内容) | 累积(内容) | 累积 |
| qSeen 见引号 | 结束字段 | 累积`"`,→quoted | **ErrAfterQuote** | 结束记录 | **ErrAfterQuote** |
| crPending 行尾待定 | **ErrBareCR** | **ErrBareCR** | **ErrBareCR** | 结束记录(消耗\n) | **ErrBareCR** |

EOF：quoted/qSeen → **ErrUnclosedQuote**；crPending → **ErrBareCR**；其余收尾最后字段与记录。
列数不一致（ErrColumnCount）在 table 层记录闭合时判定，偏移指向行终止符。
