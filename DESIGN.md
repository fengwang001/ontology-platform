# 结构化日志脱敏与采样器 设计

## 1. 包与记录模型
- record：日志记录 = 嵌套字段映射（`map[string]any`，叶子仅 string/int/float/bool/nil）+ TraceID + Level。
  构造时校验深度（上限 `maxDepth=8`，深度=根为 1），并用路径集检测自引用映射/切片。
- rule：脱敏规则 `{Path, Pattern, Action(replace/hash/truncate), Arg}`；规则集编译为路径 trie +
  值模式正则数组。同路径出现两个不同 Action 即冲突，编译期返回 `ErrConflict`（含两条规则）。
- mask：按编译结果执行 replace/hash/truncate，可原地遍历。
- sample：一致性采样器；sink：长度前缀 + CRC32 的二进制落盘/恢复。

## 2. 一致性采样推导
要求：同一追踪链上的多条记录决策一致。逐记录掷骰会使链断裂；决策只能依赖在整条链上
恒定的输入——追踪 ID，并取其确定性哈希映射到采样区间：`h=FNV1a64(TraceID)`，
`keep = h%10000 < rate*10000`。结果与记录内容、顺序、时间、协程均无关，天然并发安全。
- 无追踪 ID / 空串：链只含自身，决策须可复现；用记录稳定序列化（规范化 JSON 字节）作为
  哈希输入，error 级别仍强制保留。
- rate=0 全丢（error 强制保留）；rate=1 全留。单次决策恰有一次哈希计算（计数器断言）。
- Level：Debug=10 Info=20 Warn=30 Error=40；Error 及以上无条件保留。
- 链不完整标记：`chainDropped && level<Error 不成立而记录仍保留` 时置位，即
  「该链被采样丢弃、但本条因 error 强制保留」；正常采样保留与采样丢弃均不置位。

## 3. 嵌套路径与点号消歧
路径用 `.` 分段，`*` 表示任意数组的一层下标。解析规则（每一层）：
1. 当前映射同时存在「字面全名键」与可继续下钻的子段时，**嵌套优先**；
2. 否则若存在把整段（含点）作为字面键的字段（字段名就叫 `a.b`），走字面键；
3. 否则路径不存在：读得 nil、ok=false；脱敏规则对不存在路径为无操作（不创建字段）；
4. 路径要求映射而实际是叶子/数组：同上视为不存在，不报错、不改动。
示例：映射只有键 `"a.b"`，路径 `a.b` 命中字面键；映射有 `"a":{"b":...}` 则命中嵌套。

## 4. 脱敏不变量
- replace：输出固定串（如 `***`），不保留长度。
- hash：`"h:" + base64(sha256(v))[:16]`；同值同结果、不同值不同结果、不等于原值、不可逆。
- truncate：保留前 N 个 rune，附加 `"…<trunc>"` 标注；N 对极短串给出可判定错误。
- 彻底性：值模式规则对整棵树所有叶子生效，因此同一敏感值无论出现在不同路径、数组元素、
  还是被其他字段引用（值相等），全部处理。
- 非目标不变：先取原始规范化 JSON，脱敏后逐字节比对，除规则命中叶子外字节必须一致。

## 5. 复杂度
路径规则编译进 trie；每条记录沿数据树与 trie 同步下钻，路径匹配次数
≤ 记录数 ×（字段数 + C），C 为常数，与规则数无关。100 规则 × 10 万记录压测断言该上界。
采样 O(1)：一次哈希。

## 6. 落盘格式与损坏分类
文件 = 头 `"OLOG1" + u32be(版本=1)`（9 字节），其后逐记录：
`u32be(负载长度) | 负载(JSON) | u32be(CRC32-IEEE)`。
截断分类（按偏移区间，对长度 1..len-1 逐字节截断）：
- 偏移 < 头长：头部不完整 `ErrBadHeader`；
- 落在某帧长度前缀 4 字节内：`ErrLenTrunc`；
- 已读完整长度、剩余字节 < 长度：`ErrBodyTrunc`；
- 负载完整但 CRC 字节不足或校验失败：`ErrCRC`。
四类错误可用 `errors.Is` 区分；恢复记录数 = 截断点前完整帧数量（最大可恢复前缀）。

## 7. 故障与并发
深度超限 `ErrTooDeep`（记录被拒，计数器递增，递归改显式栈，不溢出）；
自引用 `ErrCycle`（构造期白灰染色检测）；规则矛盾 `ErrConflict`；与 sink 三类共五类判定错误。
处理为只读编译规则 + 无共享写入；并发批处理输出按输入序收集，集合与逐元素均与串行一致。
