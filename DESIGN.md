# DESIGN

## 最短脚本的并列选择

Myers 在每条对角线上从两个前驱选择：删除来自 `d-1,k-1`，插入来自 `d-1,k+1`。
本实现固定在删除前驱与插入前驱的最远位置相等时选择删除，因此同 D 的并列脚本总是 `-` 行排在对应 `+` 行前；`ab → ba` 的 hunk 为 `-a`、` `b`、`+a`，而不是先插入。每次循环保存整轮 `V` 快照回溯，保证不沿非最优路径贪进。

## 零计数行号

GNU unified diff 的旧坐标始终是旧文件中的 1 基“上下文/删除起始行”；当旧计数 `b=0` 时，该范围不含旧行，起始号取插入点前一条旧行的行号。所以文件开头插入为 `-0,0`，在旧第 2、3 行间插入为 `-2,0`。解析时不能把它当作“新插入点的行号”直接切到第 0 或第 2 行；规则是把插入点放在旧行号 `a` 之后（`a=0` 即文件开头）。新侧同理：新计数为 0 时 `c` 是删除点前一条新行的行号，应用删除时删除新索引区间 `[c,c+d)` 对应的旧 hunk 内容。

| 旧计数 | 头部写法 | Apply 的解释 |
|---|---|---|
| `b=0` | `-a,0`，`a` 为插入点前一条旧行行号，开头为 0 | 在第 `a` 条旧行后、第 `a+1` 条旧行前插入 |
| `b=1` | `-a`（省略 `,1`） | 匹配从第 `a` 条旧行开始的 1 条上下文/删除行 |
| `b≥2` | `-a,b` | 匹配从第 `a` 条旧行开始的 `b` 条上下文/删除行 |

## Hunk 合并阈值

每个改动块向外取 C 行上下文。两块若合并，第一块的尾部上下文与第二块的头部上下文会覆盖间隔；实际重复的是中间上下文，因此合并条件是间隔 `g ≤ 2C`：C=0 时不同改动总分开；C=3 时相隔 6 行会合并，相隔 7 行分开。若阈值取 C，C=3、g=4 会错误分开 GNU 已合并的 hunk；若取 `2C+1`，C=0 会把相邻改动错误合并，C=3、g=7 也会多保留 1 行共同上下文而超出 `-U3`。

## 偏移后的后续 hunk

应用第 i 个 hunk 时，记录坐标来自原文件。此前净增减行数记为 delta；若上一 hunk 的匹配点相对其当时查找中心偏移 off，则后续查找中心平移为 `delta+off`：delta 补偿已应用编辑造成的文件位移，off 是仍残留在当前文件布局中的最新错版偏移。不重复累加旧 off，因为它们已经并入后续文本位置；这样多个同样错版 hunk 不会漂移。每个候选仍需上下文与全部删除/上下文行精确相等，多候选按到查找中心的绝对距离排序，同距取靠前。
