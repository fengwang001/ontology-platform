# 设计推导

1. UTF-8 首字节声明长度。续字节须在 `80..BF`；非续字节立即结束旧前缀。
2. `E0 80` 是非最短形式，`ED A0` 进代理区，`F4 90` 越界；看到第二字节即只判首字节非法，该第二字节从续字节位置重新按孤立续字节处理。
3. 其他首字节在合法第二区间内时前缀仍可能等后续；非续字节会终止一个此前合法的前缀。`F0 90 80 41` 中前三字节一直合法，直到 `41` 终止，故三字节共一个单元。
4. 因而 `E0 80 80`、`ED A0 80`、`F4 90 80 80` 均逐续字节失败；流末 `E2 82` 是尚未被新字符终止的合法前缀，整体一个单元。

| 首字节 | 第二字节合法区间 | 第二字节非法/越界时吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 首字节 1 |
| `E0` | `A0..BF` | 首字节 1 |
| `E1..EC` | `80..BF` | 首字节 1 |
| `ED` | `80..9F` | 首字节 1 |
| `EE..EF` | `80..BF` | 首字节 1 |
| `F0` | `90..BF` | 首字节 1 |
| `F1..F3` | `80..BF` | 首字节 1 |
| `F4` | `80..8F` | 首字节 1 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

5. 缓存上限 3：合法 UTF-8 前缀最多为“首字节+2 个已收续字节”；看到越界第二字节即放弃等待。流末只处理一次，不回扫历史输入。
6. `par` 仅切 UTF-8。对每个切点，用最多回看 3 字节找到包含它的单元；若该切点在单元内部，把该单元整段交给起点更靠左的段。每段只在切点附近做常量次检查，段内一次线性解码，单元归属互斥且覆盖全部字节。
7. `C3|80 80` 中切点不在首单元内：上段在 `C3` 截止并丢弃不完整前缀，下段从 `80` 开始产生两个孤立续字节；与单线程相同。
8. `Write` 的 `n` 只计已形成输出或已被替换吞掉的字节；切分缓存中的前缀不算，返回短写后必须连同缓存再传。超限时停在最后一个完整标量，残留下次重放；UTF-16 代理对必须两码元一起提交。
