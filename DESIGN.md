# 规范化器设计推导

规范化分两阶段：**核心规范化**（行尾 `\r\n`/`\r`→`\n`，删行尾 `[ \t]+`，其余原样）
先产出 `core`；`Close` 时再按末尾策略只改 `core` 的结尾。非法字节原样通过。

## 1. 空输入与只含换行（幂等推导）

策略：`Keep`=保留结尾原样；`EnsureOne`=非空 core 结尾必须恰有一个 `\n`，
`""`→`""`（不动）；`TrimTrailing`=删掉最后一个真实行之后的全部空行，
末行有内容才补一个 `\n`，无内容（空或全是被删空白/换行）则为 `""`。

核心结果 `core`：`""`→`""`，`"\n"`→`"\n"`，`"\n\n"`→`"\n\n"`，`"  \r\n"`→`"\n"`。

| 输入 | Keep | EnsureOne | TrimTrailing |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

`EnsureOne` 若对 `""` 造 `"\n"`，则 `N("\n")="\n"` 但 `N("")="\n"`，
映射起点（orig 0 个字节↔out 1 个字节）无法满足 `ToOut(ToOrig(0))==0`，
故选择不造换行；非空时折叠尾部 `\n+` 为一个，`N(N(x))` 仍是单 `\n` 结尾，幂等。
三策略输出均无 `\r`、无行尾空白，故二次规范化核心恒等、结尾策略再作用一次不变。

## 2. 被删字节的 `ToOut`

映射由区间段 `(origStart,origEnd,outStart,outEnd)` 组成，输出段长度 0（删除）或
与原文等长（恒等），换行 `\r\n`/`\r` 是长度 1 的恒等段（输出 `\n` 对应
原 `\n`；孤立 `\r` 对应其自身）。
要求 `ToOut(ToOrig(o))==o`：每个输出位置必须对应某个原文字节，故被删原位置
只能映射到删除段**之后**的第一个存活输出位置（该位置的 `ToOrig` 回到存活字节）。
`ToOut(i)`= 从原文位置 i 起（含 i）第一个存活字节的输出偏移；若到结尾无存活字节
则取 `len(out)`（如结尾被删空白，光标落 `\n` 之后的终点）。`\r\n` 的 `\r` 被删
而其后的 `\n` 存活，故 `ToOut(\r 位置)=` 输出 `\n` 的偏移，即**行尾换行符之后**；
被删行尾空白（`\n` 前）之后第一个存活字节正是 `\n`，故光标也落在换行符上/之后，
符合「删尾空白后光标跳到下一行行首」。`ToOrig` 同段恒等、删除段无输出位置，
天然单调不减；`ToOut` 删除段取平台值，单调不减。

## 3. 空白缓冲超限

未判定的 `[ \t]+` 必须缓冲：其后是行尾则删除，是其他字节则原样吐出。
两选一：**强制判定为「非行尾」**会在「空白实际在行尾」时输出本应删除的
空白，直接违反语义 2，且输出含行尾空白，违反语义 5 幂等。**拒绝**不产生任何
违规输出，状态可解释（已吐出前缀 + 保留缓冲，进入终态），故选择拒绝：
挂起空白串长度超过 `MaxPending` 即返回可判定的 `ErrPendingLimit`（带原偏移），
已输出内容保留，之后写入返回 `ErrClosed`。输出字节超 `MaxOutput` 同理拒绝。

## 4. par 拼接

核心规范化是确定的逐字节流变换，仅依赖**有界的过去**：一个待定 `\r`（1 字节）
和挂起空白（≤ `MaxPending` 字节）。对切点 p，令 safe 为 p 之前最后一个
「安全字节」（非 ` \t\r`）位置加 1（整段无安全字节时 safe=0）。
前缀 `[0,safe)` 只含 `\n` 或确定内容，各段独立规范化后直接有效；
重叠区 `[safe,end)` 归右侧段（末段除外，保留其结尾）重复规范化。
因此只需回看 ≤ `MaxPending+1` 字节，修正范围有上界；段永远不会切在
`\r|\n` 或空白串中间（切点被整体推过该区），故 `\r\r\n`、空白后接 `\r`
均与单流一致。每段映射是段内原坐标，按 safe 平移 orig、按累计输出平移 out
后合并；末尾策略只对**全局** core 应用一次。错误偏移同理平移（每段独立
`MaxOutput` 额度=全局剩余额度，使超限点与单流完全相同）。
