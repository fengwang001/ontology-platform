# 严格 JSON 字符串编解码设计

## 代理对错误偏移

解码器把 `\uD800`–`\uDBFF` 视为一个“尚未完成的代理对产生式”，而不是一个可先输出的码点。

- 输入结束仍只有高代理：没有后继单元，错误指向高代理 `\u` 的起点。
- 高代理后紧跟任意不能成为低代理 `\uDC00`–`\uDFFF` 的单元：错误指向该后继单元起点；原始字符指向其首字节，转义指向其反斜杠。
- 未等待高代理时遇到低代理：低代理本身就是不合格单元，错误指向该低代理 `\u` 起点。

因此 `\uD83Dx` 与 `\uD83DA` 都指向高代理之后的 `x`/`A`，`\uDE00\uD83D` 指向第一个低代理，孤立 `\uD83D` 在 EOF 指向自身。偏移始终指向“让语法确定失败的最小位置”：EOF 前保留唯一可疑的高代理；有后继时，高代理是否合法取决于后继，故不应提前判罪。

`\uD83DA` 不能解释成“替换字符加 A”：严格解码器只接受完整 JSON 文法；高代理的唯一下一个合法词法单元是低代理，`A` 已使组合失败。U+FFFD 是容错 UTF-8 解码策略，本题显式禁止静默替换。

## 最小转义与唯一性

RFC 8259 要求字符串中转义双引号、反斜杠和 U+0000–U+001F。短形式固定用于 `\b`、`\f`、`\n`、`\r`、`\t`；其余控制字符唯一写成小写 `\u00XX`。

U+007F 虽具控制语义（DEL），但不属于 JSON 必须转义的 C0 控制区，且在合法 UTF-8 中可原样出现，所以保留。U+001F 位于必须转义的 U+0000–U+001F 区间内。`/`、非 ASCII、U+2028/U+2029 同理均原样输出。

该编码是单射：解码时反斜杠只能由转义产生，而原始 `"`、`\` 和控制字符绝不可能以原样出现在输出中，所以两个不同输入不可能得到同一字面量。它也唯一：每个需转义字符只有规定的一种表示，其他字节均按唯一的合法 UTF-8 原文复制；函数完全确定。

## 测试对照

<!-- 实现完成后补齐：六条语义到测试函数的映射 -->
