# 设计推导

## 末尾换行

先逐行规范化：行尾连续空格/制表符删除；`\r\n` 与独立 `\r`、`\n` 都输出一个 `\n`。三种策略只在最终后缀上决策：

- `Keep`：保留规范化后原有末尾 `\n` 数量，不新增、不删除。
- `Ensure`：空文件也视为需要结束一行，因此总以恰好一个 `\n` 结束；其他情况先删尾部全部 `\n` 再补一个。
- `Trim`：不补缺失换行；若原本有一个或多个末尾换行，删到只剩一个。纯空白行也会被删成空行，因此它本身没有“内容行”和“空白行”的区别。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

`Ensure("")="\n"`：再规范化 `"\n"` 仍是 `"\n"`，幂等；若选 `""` 也幂等，但不符合“确保恰好一个”。补出的 `\n` 是零长度原文区间 `[len(x),len(x))`，双向终点仍可映射。

## 删除字节的偏移

设连续删除原文区间 `[a,b)` 后面第一个存活输出在输出偏移 `o`。对所有 `i∈[a,b]` 定义 `ToOut(i)=o`；其中终点 `b` 本来也指向同一位置。于是行尾空白里的光标和 `\r\n` 中的 `\r` 都落在规范换行符之前（即换行符所在偏移），不会越过换行到下一行。映射段保存存活段；连续存活段相邻即合并，删除区只改变一次输出坐标。`ToOrig(o)` 对存活偏移返回其原文字节；故 `ToOut(ToOrig(o))=o`。删除区的多对一不要求反向互逆。

## 缓冲上限

选择拒绝。待定空白只有在看到换行或 EOF 时才能证明位于行尾；提前强制输出，若之后到达换行，输出会含行尾空白，违反行尾清理和幂等。超过 `MaxTrailingWhitespace` 时返回带原文偏移的 `ErrWhitespaceLimit`，规范化器进入终态，已输出内容保留，超限字节及之后字节不接收。该失败确定、可重复，不产生不满足语义的输出或映射。

## 流式状态与并行修正

单流状态仅为：待定 CR、待定空白串、当前行已冲刷标志。任意输入分片都走同一状态机，所以 1 字节分片与整段输入逐字节一致；EOF 把待定 CR 判为独立换行，再应用末尾策略。

`par` 对第 k 段的起点向左借位：跳过前一段末尾连续 `[\t ]*`，若空白前是 `\r` 再借该 `\r`。借到的重叠前缀在本段结果中丢弃，但用来决定 CRLF 是否配对以及整串空白是否属于前一行尾；段起点相应后移可避免重复输出。每段独立运行后，把原文坐标与输出坐标都平移为全局坐标，再顺序拼接 span。需要回看的只有“一整串行尾空白加至多一个 CR”，长度由 `norm` 的同一缓冲上限界定，因此修正范围有上界；K=1 退化为普通规范化。
