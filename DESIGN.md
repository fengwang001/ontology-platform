# 结构化日志字段脱敏与采样器 — 设计

## 1. 目标与包划分

只做一件事：对结构化日志记录按规则脱敏、按规则采样，并保证同一追踪链采样决策一致，
结果可落盘并可做损坏检测。仅用标准库，状态全部在进程内存。

- `record`：记录模型。`Level`(Debug/Info/Warn/Error/Fatal)、`TraceID`、嵌套
  `map[string]any` 字段（叶值为 string/int64/float64/bool/nil）与数组。
- `rule`：规则集编译。路径规则（action=replace/hash/truncate）+ 值模式规则
  （正则，命中任意字符串值即脱敏）。
- `mask`：执行脱敏（替换 / SHA-256 哈希 / 截断），深拷贝后改写，不改原记录。
- `sample`：按追踪 ID 的一致性采样决策 + error 及以上强制保留。
- `sink`：自描述二进制落盘 `MLOG1|depth|frame...|8B footer`；帧=
  `u32BE(len)|json record|u32BE(crc32 IEEE of body)`；读取做损坏分类。

## 2. 一致性采样推导

同一条追踪链的多条记录必须同留同弃。若对每条记录独立掷骰，链就会断裂。
因此决策函数必须是追踪 ID 的纯函数：

    keep(t) = hash(t) mod M < keepNumerator(t)，其中 t 仅含 TraceID

- 只能由 TraceID 决定：与字段内容、级别、到达顺序、时间均无关（级别只在其后做
  强制保留修正）。这样同一链无论记录数多少、乱序到达、重复运行，结论都相同。
- 对 TraceID 求一次 SHA-256，取前 8 字节对 2^32 取模后与阈值比较；单次决策恰好
  1 次哈希（`hashCalls` 计数器可证）。选 SHA-256 而非 FNV：FNV 对 `chain-00`…
  `chain-49` 这类高度相似的短键会聚簇到同一侧，无法体现 0.5 采样率。采样率以分数
  `n/2^32` 表示：0=全丢，1=全留。
- **空 TraceID（含无 TraceID 字段）**：全部归入同一个空 ID 桶，即共享一个链决策
  （它们彼此无法区分，视为同一条匿名链）；error 记录仍强制保留。

## 3. 链不完整标记

正常采样先得到链决策 keep。Error/Fatal 无条件保留，会出现「链被采样丢弃、但某条
记录因级别被强行留下」——此时输出记录打 `_chain_incomplete=true`。标记当且仅当
`keep==false && level>=Error` 时出现；链被保留时 error 记录不打标记。

## 4. 字段路径与含点字段名的消歧

- 路径段按 `.` 分割，逐层进入 map；数组用段 `[i]` 表示该元素。
- **字面点转义**：字段名中的点写为 `\.`，反斜杠写为 `\\`。因此
  `a\.b` 表示顶层名为 `a.b` 的单个键；`a.b` 表示两层嵌套。解析在编译期完成，
  运行时以段序列匹配，两种写法不可能歧义。
- 路径某层不是 map/array（类型不符）或索引越界：视为「不匹配」，安静跳过
  （不是错误；规则可指向不存在的路径）。路径整体不存在同样跳过。

## 5. 脱敏语义

- replace：换成固定串 `***`，不保留长度。
- hash：`sha256` 的十六进制摘要；同值同结果、不同值不同（碰撞概率忽略）、不可逆，
  且结果不等于原值。
- truncate：保留前 N 个 rune，追加 `…(truncated)`；不足 N 则不加标注。
- 值模式规则对记录中**所有**字符串叶值（含数组元素、被嵌套引用处）生效，故同一
  敏感值无论出现在几个路径都被处理。脱敏深拷贝执行，非目标字段 JSON 逐字节不变
  （测试以原始 JSON 为基准，只替换命中值后比对）。

## 6. 编译与复杂度

规则集 `Compile` 时：路径段序列化为 `\x00` 连接键，存入 map；值模式预编译
`*regexp.Regexp`。遍历记录每个叶值一次：先按完整路径 O(1) 查路径规则，再对每个
字符串值跑值模式。非导出计数器 `matchCount` 每次「路径查表」加 1，故
`matchCount ≤ 记录数 × 叶字段数 + 0`（无「记录数 × 规则数」项）。

同一路径配置两个不同 action → 编译期返回 `ErrConflict`（包裹冲突的两条规则）。

## 7. 拒绝类错误（四类，errors.Is 可区分）

- `ErrDepthExceeded`：构造/遍历时嵌套深度 > `MaxDepth(32)`，拒绝并计数，
  迭代式遍历保证不栈溢出。
- `ErrCycle`：构造记录时同一 map 指针在同一条下降路径上再次出现即报错；
  兄弟分支共享同一 map 合法（非自引用下降）。
- `ErrConflict`：规则集自相矛盾（编译期）。
- 落盘损坏：`ErrHeader` / `ErrLengthPrefix`（含帧边界截断与尾部标记缺失）/
  `ErrBodyTruncated` / `ErrCRC`。

## 8. 并发与落盘

`Decider` 决策无写状态（计数器用 atomic），`Masker.Apply` 对每记录独立深拷贝，
多 goroutine 并发结果与串行逐元素相同，`-race` 干净。落盘格式读取为容错式：
逐帧解析，遇到损坏返回可判定错误及此前已恢复的全部记录。
