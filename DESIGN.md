# DESIGN

## 推导 1：续行接上的行只含空白或为空（语义 3）

`java.util.Properties` 的 `LineReader.readLine` 在行尾出现奇数个反斜杠时，
删掉一个反斜杠并置 `skipWhiteSpace`/`appendedLineBegin`，继续读下一物理行：
先剥掉其前导空白，再把余下字节追加进逻辑行。若该物理行全是空白（或为空），
剥离后没有任何字节被追加，`precedingBackslash` 为 false，于是这个物理行的
行尾不再满足续行条件——续行链在此终止，而不是继续向下蔓延。
结论：`k=a\` + `   ` + `b=c` 得到 `k`→`a`、`b`→`c`；纯空白/空续行贡献为空串，
且终结续行链。它与「续行首部空白被剥掉」是同一条规则的两面：正因为首部空白
被剥掉，纯空白行才退化为空行；而空行没有行尾反斜杠，自然无法延续链条。

## 推导 2：Store 的最小转义集（语义 6）

Store 要转义的字符由 Load 的识别规则反推：
- 键：未转义的 `=`、`:`、空白会结束键，`\` 是转义引导符，故键内一律转义；
  逻辑行首（即键首）的 `#`/`!` 会被判为注释，必须转义；`\n`/`\r` 会破坏物理
  行结构，与 `\t`/`\f`/空格一样写成 `\n`/`\r`/`\t`/`\f`/`\ `。
- 值：注释判定发生在逻辑行首、分隔符判定发生在值开始之前，故值中的
  `#`/`!`/`=`/`:` 都无需转义；值中间的空格也不必转义——load 只在分隔符后
  跳过一次空白，进入值之后逐字节照搬（尾部空白因此被保留，同样不用转义）。
  只有值开头的空白必须转义，否则会被那次跳过吃掉。`\` 必须转义（顺带避免
  行尾出现奇数反斜杠被误判为续行）；`\n`/`\r` 同理必须写成转义形式。
- 非 ASCII 按 UTF-8 原样写出（Reader 语义，非 ISO-8859-1 字节流）。

## 语义 ↔ 测试对照

（待测试写完后补充）
