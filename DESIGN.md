# 句柄表设计推导

## 1. 代号回绕

代号是有限位宽的非负整数。若对 `max` 加一后按模回绕到 0，则旧代号会重复。

具体 ABA 序列：槽位 `i` 中对象 A 的句柄为 `(i, 1)`；第 1 次删除 A 后代号变为 2 并分给 B；之后持续“删除并插入”，代号依次经过 3、…、`max`、0、1。当代号又回到 1 时，槽位恰好放入新对象 Z；校验只比较槽位号和代号，于是 A 的老句柄 `(i, 1)` 被误判为 Z 的句柄，不变量 1 被破坏。

更早地，回到 0 还会与零值句柄的编码空间冲突。因此本设计不回绕：删除在用槽位时，若代号已是 `max`，不把槽位归还空闲链，而把它标记为“代号耗尽”；否则代号加一后归还。耗尽槽位既不计入存活，也不计入空闲，但仍计入容量，所以 `Len + 空闲数 + 耗尽数 = Cap`。该槽位永久不可分配，老代号最多等于当前代号一次，而那时对象正是它本人；删除后只会递增或永久退役，不会再次出现。

## 2. 起始代号与零值句柄

`var h handle.Handle` 的所有编码字段都是 0，所以 `(槽位 0, 代号 0, 表 0)` 必须解释为无效。新表标识从 1 开始，槽位代号也从 1 开始；句柄编码包含表标识、槽位号、代号三部分，且解码后用显式谓词判定零值。这样槽位 0 的第一代是 `(表非 0, 槽位 0, 代号 1)`，不可能等于零值句柄。

表标识随新表单调分配，并编入句柄；A 表句柄拿到 B 表时，即使槽位号相同，也在校验代号前因表标识不同而失败。

## 3. 空闲链表插入方向

头插是 LIFO：刚释放的槽位下一次立刻被取走。若持续删除再插入同一对象集合，少数热点槽位会承担所有代号递增，最先耗尽。

尾插是 FIFO：取走链表头部、释放追加到尾部，分配会在空闲槽位间轮转，把代号递增摊开。本设计选择尾插；配合“不回绕、耗尽即退役”，它降低但不消除耗尽概率，耗尽槽位永久离开链表，因此不会破坏容量等式。

## 4. 同一失效句柄连用两次

第二次必须返回与第一次相同的失效错误，而不是“已处理过”。不变量 4 要求被拒绝操作不留痕；若第一次错误后记录“见过此句柄”，第二次才能返回不同错误，那么失败操作已经改变了可观察状态。所有失效句柄校验均为只读判定，不分配、不移动链表、不改代号，因此连续调用逐字段同态、错误相同。

## 5. 失败原子性与复杂度

所有操作先完成边界、表标识、零值和代号校验，再做唯一一次状态提交。`Insert` 的取槽、代号和值写入、长度递增组成临界区内的完整提交；若表满或取出的槽位并非空闲则直接返回。空闲链用头尾指针和长度维护，取、还均为 O(1)，不扫描槽位数组。
