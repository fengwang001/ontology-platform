# DESIGN.md

## 1. 末尾换行策略（空输入推导）
规范化主体：所有行尾→`\n`，删行尾 `[ \t]+`；结果是若干行，末尾可能挂 k 个 `\n`（k≥0，k≥2 表示末尾空行）。
三策略只改末尾：
- `Keep`：保留规范化主体的原末尾换行数 k。
- `Ensure`：把末尾 `\n` 串折叠成恰好 1 个（主体为空也给 1 个，即 `""→"\n"`）。
- `Trim`：主体为空输出 `""`；否则删掉全部末尾空行后补 1 个 `\n`。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

`Keep` 幂等：输出不含行尾空白/`\r`，再规范化末尾结构不变。`Ensure`：任意输出恒以单个 `\n` 结尾且无末尾空行，不动点。`Trim`：输出为 `""` 或以单 `\n` 结尾，不动点。
讨论空输入选 `""` 还是 `"\n"`（Ensure）：选 `"\n"` 时 `N("")="\n"`、`N("\n")="\n"`，幂等成立；若选 `""` 则 `N("\n")` 必须仍为 `"\n"`，空与非空行为不对称，故 Ensure 取 `"\n"`；Trim 为保持「删除全部末尾空行」语义取 `""`。

## 2. 被删字节的 ToOut
不变量：对所有输出偏移 o，`ToOut(ToOrig(o))==o`，故每个被保留的输出字符位 o 必须有原偏移 i 使 `ToOut(i)=o`。
被删区间是原文一段连续区间 `[a,b)`，其后第一个保留位置在原文 b、输出 o'（换行 `\n` 或行首内容）。区间内部没有输出位，只有 o' 左侧的「间隙」。由单调性，区间内 i 只能取常数；取 o' 会抢占保留位置，故 `ToOut(i)=o'` 仅给保留点 b，被删点取其左侧最近输出位置：
- `\r\n` 的 `\r`（i=a，b=a+1）：删除间隙位于换行符前，`ToOut(a)=o'`（`\n` 的输出位，即删除前位置 = 换行符位）。间隙长度 1，等价「贴向删除间隙右侧最近输出位」。
- 行尾空白 `[a,b)`：`ToOut(a..b-1)=o'`，o' 为该行 `\n` 或 EOF 处的输出位（Trim 下末尾空白行整段删除时 o' 为前一行换行位）。

编辑器光标落在被删空白中时，映射到**换行符之前**（紧贴行尾间隙左缘的行内容端 = 换行位本身，删除后换行符成为该行尾字符；语义上是换行之前的行末端），不跳到下一行行首，避免错误跨行。
`ToOrig(o)`：输出位 o 属于某保留原文区间时返回对应原文偏移；o=len 返回原文 len。

## 3. 缓冲超限
方案对比：
- 拒绝：返回可判定错误 `ErrSpaceLimit`（带原文偏移），已输出保留、实例进入终态；不影响无超限输入的 4/5/6，且截断语义仍由 Close 把待定空白当行尾（删除）给出。
- 强制判定为非行尾并输出：若其后真出现行尾，这些空白本应删除却已输出 → 违反 1/4/5（输出与无界情形不一致、再规范化会删空白），违反 6（映射目标改变）。
结论：**拒绝**。待定空白超 `SpaceLimit`（0=不限）即在写入该空白时报错。输出超 `OutLimit`（0=不限）在写入前判定，报 `ErrOutLimit`。终态后写入报 `ErrClosed`。严格模式遇 NUL 报 `*NULError`（带偏移）。四错误互相可判定。

## 4. par 拼接
朴素按切点分段会在边界误判：`a|\r\nb`（\r 单独成段→换行位置错）、`a | \nb`（空白归属错）。
做法：请求切点集 cuts 只作上界；处理段 s 前，从 cuts[s] 起向右扫描寻找**安全点** p：从 p 向左回看不跨越「长度 ≤ SpaceLimit 的尾部空白串（前导不是 \r）」与「结尾单个 \r」这两种待定结构，即 p 不落在 (空白run|\r)(\n?) 的歧义区内；安全点上 p 处字节（若存在）使前一段待定状态立即消解（如 p 为普通字节、\r 或 \n 时按整体规则结算）。每段实际区间 [prev,p]，独立规范化再拼接；修正扫描长度上界 = SpaceLimit+1（无界时给内部上限），故有界。段 s 用 `Close` 策略规范化：末段用全局策略；中间段一律 Keep（后续段内容继续同行）。
映射拼接：段内映射坐标加段起点 delta（orig/out 各加偏移量），段间区间合并；段尾 Close 删除在拼接映射中按该段输出结束点结算，再由后续段的首区间覆盖该输出位，保证 `ToOut(ToOrig(o))==o`。K∈1..8、切点取遍所有偏移即覆盖全部歧义位置，与单流逐字节一致（norm 状态机对 1 字节切分不变，故与整体一致）。
