# DESIGN

## 1 末尾换行策略（推导）
规范化只做两类改动：`\r`/`\r\n`→`\n`、删行尾空白。记 t = 输出末尾连续 `\n` 数。
- Keep：t 不动。
- EnsureOne：t>0 时折叠成 1；t=0 时不追加。
- TrimBlank：t>1 折叠成 1；t=0 或 t=1 不动。
空输入恒输出 ""：规范化是“删除/替换”，凭空追加 `\n` 会制造没有原文对应的输出字节，
而 `ToOut(ToOrig(o))==o` 要求每个输出偏移（含 o=1）能往返；插入字节无法满足，故不追加。
幂等：第二轮输入无 `\r`、无行尾空白，仅末尾 `\n` 数变化；EnsureOne 把 t>0 变 1、TrimBlank 把 t>1 变 1，再跑一次 t 不变，故幂等。

| 输入 \ 策略 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2 被删字节的 ToOut（推导）
设被删原文字节区间 [a,b)（行尾空白或 `\r\n` 中的 `\r`），其后继输出点为 p。
ToOrig 单调不减，ToOut 单调不减，要求 ToOut(ToOrig(o))==o。
ToOrig(p)=b（p 是后继保留字节的起点），故 ToOut(b)=p；
单调性迫使所有 i∈[a,b] 取 ToOut(i)=p。即被删字节映射到“删除区间之后”的输出位置 p。
语义：光标放在被删空白/`\r` 上，等价于光标在 `\n` **之前**（`\n` 是后继字节，p 是其偏移），
不越过换行；`\r\n` 里的 `\r` 映射到输出 `\n` 起点。

## 3 空白缓冲超限（推导）
选择：**拒绝**（可判定错误 `ErrSpaceOverflow`，带原文偏移，已输出保留，进入终态）。
强制判定为“非行尾”会把行尾空白原样吐出，违反语义 2、5（再规范化会继续删）；
判定为“行尾”而后文其实是普通字符则误删行中空白。两种强制输出都必然违反 4/5/6 之一，故拒绝。
拒绝时输出仍是某合法前缀的规范化结果，状态=终态，可解释、可判定（`errors.Is`）。

## 4 par 拼接（推导）
段在任意偏移切开后独立 Close 会把“段末未决空白/待决 `\r`”误当行尾。修正：切点向外吸附。
切点 s 左侧的不安全后缀 = 末尾连续空白（含 `\t`）加可能的一个结尾 `\r`；
右侧不安全前缀 = 开头连续空白，以及 `\r\n` 跨切点时的 `\n`。
扫描原文把每个切点吸附到左右均安全处；吸附后若与相邻点重合/回退则合并段。
这样每段边界必落在确定点，段独立 Close 与全局逐字节一致。
修正范围上界：≤ 单段长度，且 ≤ MaxWhitespace+2（空白受上限约束，`\r\n` 仅 2 字节）。
映射：各段 span 平移段起点的字节偏移，末尾策略只在拼接后对全局应用一次（段内用 Keep）。

## 5 span 结构
仅在“删除点”存区间 (out, orig, n 个被删字节)；其余为恒等。纯 `\n` 文本 0 删除点→1 个恒等区间。
查询对删除点数组二分；计数器记录本次检查的区间数。
ToOrig(o)：o 落在删除点之后则 +n（含终点）。
ToOut(i)：恒等 i；若 i 落入删除点覆盖的原文范围则取该删除点对应的输出位置 p（见 §2）。
