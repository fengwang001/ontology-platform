# RLE 设计

严格解码器只接受满足 `Encode(Decode(t)) == t` 的规范文本。每个游程为可选次数加一个符号；次数只能是首位 `2`–`9` 的十进制数，符号为非数字、非反斜杠码点，或 `\` 后接数字/反斜杠。

## 必须拒绝的输入

| 类别 | 若接受会违反不变量的原因 |
|---|---|
| 显式次数 1，如 `1a` | 解码为 `a`，但 `Encode("a") == "a"`，不是 `1a`。 |
| 次数 0，如 `0a` | 游程长度必须至少 1；不存在能重新编码出 `0a` 的字符串。 |
| 前导零，如 `01a`、`00a` | 即便解释成 1 或更大值，规范编码也省略前导零。 |
| 相邻同符号，如 `2a3a` | 解码为 5 个 `a`，最长游程再编码为 `5a`。 |
| 非法转义，如 `\a` | 若把转义符丢弃得到 `a`，再编码是 `a`；转义只允许数字和反斜杠。 |
| 末尾孤立 `\` | 转义必须引入一个符号，输入没有完整游程。 |
| 末尾只有次数，如 `12` | 次数后必须有符号，否则没有完整游程。 |
| 非法 UTF-8，如 `\xff` | 符号定义为 Unicode 码点，且规范输出只包含合法 UTF-8。 |
| 次数超过 `math.MaxInt`，如 20 位以上的 `9...a` | 见下节；不能在静默溢出后得到错误次数。 |

## 大次数与输出上限

选择拒绝 `count > math.MaxInt`：`Decode` 的结果是 Go 字符串，长度受 `int` 约束；流式接口也需要有界整数来累计输出。数字逐位累加，更新前检查 `n > (math.MaxInt-d)/10`，因此不会溢出。输出采用可配置的总字节上限；每次输出游程前检查 `已输出 + count*符号字节数`，超限直接返回可判定错误，不预先按次数分配内存。`Decode` 默认使用 `math.MaxInt` 作为上限。

## 测试对照

（实现后补）
