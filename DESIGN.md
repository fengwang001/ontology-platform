# DESIGN — CSV 解析器（RFC 4180 方言）

## 1. 空行与单列空值（推导）

不变量：`Parse(Write(T))` 与 T 逐字段相等且引号标记相同。
- 若规定「空行被跳过」：单列表中「唯一字段为未加引号空值」的记录，其唯一忠实写法是空行，回写后该记录消失 → 数据丢失，违反往返。要保住它只能把空字段写成 `""`，但引号标记从 false 变 true，仍违反「引号标记相同」。**该选择不可行。**
- 故选择：**空行 = 一条只含一个未加引号空字段的记录**。此时单列表空值记录直接写成空行即可忠实往返，回写器无需为它加引号。
- 结论：「必要加引号」仅四种——字段含 `,`、`"`、`\r`、`\n`。空字段永远不加引号；空行语义由「空行即记录」吸收。
- 推论：**规范输入**定义为——记录间一律 `\n` 分隔、最后一条记录以 `\n` 结尾、引号仅按上述四种必要情形出现。对规范输入 `Write(Parse(x))` 逐字节等于 x。

## 2. par 切点（推导）

切点落在引号字段内时，后续 worker 无法自判起点状态。方案：**每段按两种假设各解析一遍**——
- A 假设：起点在字段开始（词法状态 FieldStart）；
- D 假设：起点在引号字段内部（词法状态 InQuoted）。
逐字节状态机的下一状态只由当前状态与当前字节决定，故段 i 的真实出口快照只依赖段 i-1 的真实入口快照；从第 0 段（入口必为 FieldStart）**从左到右**选定每段的真实假设并拼接，结果与单线程逐位相同。
- 出口快照共 6 类：FieldStart / 未引号字段中 / InQuoted / InQuoted 刚见引号 / CR 待定 / 已有错误。前五类分别映射到下一段入口：FieldStart→A 直接用；未引号字段中→A 假设的首字段恰是续接（值与偏移天然衔接）；InQuoted→D 直接用；刚见引号→用 A 日志但**跳过首个空字段事件**（A 把 `"` 当开引号，真实语义是闭合引号，字段内容相同）；CR 待定→下一段若以 `\n` 开头，跳过 A 日志的首个空记录事件（该 `\n` 属于上一段行尾）。
- 入口为「刚见引号」且段首字节不是 `,`/`\n`/`\r` 时，单线程必报 JunkAfterQuote：拼接时按段首字节直接构造该错误，偏移=段起点，记录/字段号由拼接计数器换算。
- 错误位置换算：段内字节偏移 + 段起点 = 全局偏移；记录号 = 拼接时已产出记录数 + 段内记录号；字段号 = 拼接时当前行已收字段数 + 段内字段号。
- 字段值拼接：跨段字段 = 前段遗留值 + 本段首字段值，引号标记取首段，偏移取首段 Start 与末段 End。三类上限在拼接时统一检查（见第 4 节），字节计数 ≤ 2N。

## 3. CR 待定（推导）

未引号字段中见到 `\r` 时，`\r` 可能是 `\r\n` 行尾，也可能是孤立 `\r`（错误），必须等下一字节：
- 半包续传：状态机进入 CR 待定态即暂停，`Feed` 可在任意点返回；下一字节为 `\n` → 提交行尾；为 `,` → 报 DanglingCR（孤立 `\r` 后只能是行尾，逗号同样非法）；其他 → DanglingCR。
- `Close()` 时仍处于 CR 待定：流在 `\r` 处结束，`\r` 后无 `\n`，判 DanglingCR。
- par 切点落在 `\r` 与 `\n` 之间：`\r` 属前一段，前段出口快照即 CR 待定，按第 2 节规则丢弃后段由 `\n` 产生的空记录事件，行尾归属前段，不错不漏。
- 引号字段内的 `\r` 一律是内容，不进入待定态。

## 4. 上限的「立刻」（推导）

- 单字段字节上限：引号字段内 `""` 计 1 个字符（值语义），未引号字段逐字节计 1。状态机在**每次向字段值追加字节前**检查 `len+1 > Max`，超限当场报错，该字节即为第一个超限字节，不缓冲整条记录。
- 单记录字段数上限：每次要开启新字段前检查 `字段号+1 > Max`，当场报错。
- 总记录数上限：每产出一条完整记录即计数并检查，超限当场报错；已产出记录保留。
- 三类错误发生后解析器进入终态，后续 `Feed` 返回同一错误对象。
- par 中 worker 关闭字段上限（避免双假设重复计数字节），拼接时按原始字段区间用同一计数规则重放定位第一个超限字节，偏移与单线程一致。

## 5. 状态转移表

状态：S0=字段开始 S1=未引号字段中 S2=引号字段中 S3=引号字段中刚见引号 S4=CR 待定。

| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| S0 | 发空字段 | →S2（引号字段开始） | 发空字段+发记录 | →S4 | →S1，追加 |
| S1 | 发字段→S0 | 报 BareQuote | 发字段+发记录→S0 | →S4 | 追加 |
| S2 | 追加 | →S3 | 追加 | 追加 | 追加 |
| S3 | 发字段→S0 | 追加`"`→S2 | 发字段+发记录→S0 | 报 JunkAfterQuote | 报 JunkAfterQuote |
| S4 | 报 DanglingCR | 报 DanglingCR | 发字段+发记录→S0 | 报 DanglingCR | 报 DanglingCR |

EOF：S0 无动作（若本行已有内容则补发空字段+记录）；S1 发字段+记录；S2 报 UnclosedQuote；S3 发字段+记录；S4 报 DanglingCR。
「发记录」即行结束；列数一致性、记录数上限由 table 层在记录事件处检查。
