# 设计推导

## 第 3 条：续行接上的行只含空白/为空时

物理行拼接算法逐字节扫描：读到行尾时数连续反斜杠个数，奇数则丢弃这些
反斜杠和换行符，并把下一物理行**首部空白全部跳过**后接在当前逻辑行后，
然后继续扫描；偶数则逻辑行在此结束。

推导：被接上的行若整行都是空白（或空行），剥离前导空白后贡献的字节数为
0；扫描器不会在该行结束，而是带着“期待续行”状态继续到再下一物理行，其
前导空白同样被剥掉，直到某行提供非空白内容为止。例如：
`k=a\`、（空行）、`   `、`   b` 三行拼接为 `k=ab`。
依据：`java.util.Properties` 的 `LineReader#readLine` 中，奇数反斜杠后
执行 `skipLF` + `skipWhiteSpace()` 循环（跳过空格/TAB/FF，含整行空白行与
行终止符），空白行不产生输出也不终止逻辑行；该机制正是“续行首部空白被
剥掉”这条规则的同一段逻辑——剥空白本就逐行进行，剥完整行为空就自然
继续剥下一行。注释规则在逻辑行拼好之后才应用，所以续行里的 `#`/`!` 不
算注释。

## 第 6 条：Store 的最小转义推导

Load 解析逻辑行的规则决定哪些字符必须转义：

1. 行首（逻辑行第一个非空白字符）为 `#` 或 `!` 整行变注释。因此 **键** 以
   `#`/`!` 开头必须转义；键以空白开头也必须转义（该空白会被当行首空白
   吃掉）。值不位于行首，永不触发注释，故 `#`/`!` 与开头都无需特殊处理
   （除第 3 点）。
2. 第一个未转义的 `=`/`:`/空白是键值分隔符；其后空白被吃。因此键中的
   `=`、`:`、空白全部转义；值中的这些字符不作分隔符，无需转义。
3. 值开头的空白位于分隔符之后，会被“分隔符后吃空白”吞掉，故**值的第一
   个字符若是空白必须转义**（转一个即可，其后空白安全保留）；值中间的
   空白永远不是分隔符，原样输出。
4. `\` 是转义引导符，键值中一律转义成 `\\`；`\n` `\r` `\t` `\f` 必须写成
   反斜杠转义，否则换行终止逻辑行（且 CR/LF 会被行终止符逻辑吞并）。
5. 其余字符（含非 ASCII）原样以 UTF-8 输出，不写 `\uXXXX`。

统一输出 `key=value`：空键、空值都合法（`=v` 为空键，`k=` 为空值）。

## 七条语义与测试函数对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | （收尾时填写） |
| 2 注释 | |
| 3 续行 | |
| 4 转义/`\u` 错误行列 | |
| 5 重复键顺序 | |
| 6 回写 | |
| 7 随机往返 | |
