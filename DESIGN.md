# 设计推导

## 0. 记号

`B(x)` 为规范化主体：行尾统一成 `\n`、删除每行末尾的空格/制表符；空白行使整体保留为空行；
非法 UTF-8 与 NUL（非严格模式）原样透传。末尾换行策略只在 `B(x)` 的结果上做后缀调整。

## 1. 空输入 / 只含换行（推导）

设 `B` 之后末尾连续换行个数为 `t`，内容（去掉该后缀）为 `c`。

- Keep：原样保留 → 结果 = `c + t 个 \n`。
- EnsureOne：`t==0` 时行为必须由幂等决定。`""` 若输出 `"\n"`，再规范化时必须不动；
  即「无内容且恰好一个换行」是唯一稳定点。于是定义：`c=="" 且 t<=1` 时结果原样（`""`→`""`，`"\n"`→`"\n"`）；
  否则结果 = `c + 一个 \n`（`t==0` 补一个，`t>1` 折叠为一个）。输出再代入必有 `t<=1`，落入不动点。
- TrimOne：删除全部末尾空行但保留一个换行。定义：无内容（`c==""`，含纯空白输入，因空白全删）→ `""`；
  否则结果 = `c + 一个 \n`（去掉 `t-1` 个空行，留一个换行；`t==0` 也补一个）。输出再代入时内容不变、`t==1`，不动。

样例表：

| 输入 | Keep | EnsureOne | TrimOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut（推导）

设被删运行（行尾空白、或 `\r\n` 中的 `\r`）原文区间 `[a,b)`，其后第一个存活字节（输出 `\n`）
的原文偏移为 `b`、输出偏移为 `o`。被删位置上 `ToOrig` 无原像。由 `ToOut` 单调不减：
存活字节 `b` 映到 `o`，其前位置不得越过 `o`，故 `ToOut(i)=o`（`a<=i<=b`）——整串被删字节
（含其后边界点）都映到「下一个存活字节」的输出位置，即输出换行符上。因此编辑器光标落在被删
空白里，会落到行尾换行符**上（换行之前的位置）**。`ToOrig(o)=b`，故
`ToOut(ToOrig(o))=ToOut(b)=o`，互逆成立。

策略追加的合成 `\n` 无原文字节：`ToOrig` 钳到原文末尾 `L`；`ToOut(L)` 返回内容终点
（合成换行前的位置）。这是长度增长型映射唯一确定的约定，二者在终点各为边界，文档明示。

## 3. 缓冲超限（推导）

两选：(a) 强制把待定空白判为「非行尾」并输出——一旦后续真出现行尾，就留下了本应删除的
行尾空白，违反语义 2 与幂等（语义 5），且与「整段一次规范化」不一致，违反语义 4。
(b) 拒绝：返回具名错误 `ErrPendingLimit`（带超限开始的原文偏移），已输出保留、进入终态；
该决策只依赖已见输入，任何切分方式在同一点得到同一错误，不违反 4/5/6。故选 (b) 拒绝。
输出字节上限同理：写入前预判，超限返回 `ErrOutputLimit`（带该字节原文偏移），不写入，终态。

## 4. par 拼接（推导）

段尾可能挂着：1 个待定 `\r`、一串待定空白。判定只依赖下一个字节，故段间需重放的原始后缀
长度有上界（1 + 缓冲上限，为常数）。做法：除末段外每段以 Keep 流式跑到切点，返回「已提交
输出 + span + 未决原始后缀（含其原始偏移）」；处理第 k+1 段时把该后缀前置重放。已提交部分
按定义不可能再被后字节改变（新行已开始），直接拼接；span 区间按全局输入/输出偏移平移后追加，
相邻相同恒等区间合并。全部拼接后只在整体上应用一次末尾策略（Keep/EnsureOne/TrimOne），
故与单路逐字节结果逐位一致。
