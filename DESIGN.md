# 有预算的图遍历器：设计推导

## 1. 预算计量单位：访问的节点数（而非展开的边数）

预算定义为「本次遍历允许加入结果序列的节点数」。

若以「考察的边数」为单位，同一逻辑图在不同存储形态下截断点会漂移：
- 重复边、平行边是否去重，会改变到达第 n 个新节点前考察过的边数；
- 邻接表插入顺序、冗余边、内部缓存（如排序后写回）都会改变边序号；
- 自环、指向已访问节点的边消耗边预算却不产生访问结果，
  导致「预算 = 10」在两张等价图上访问到不同节点。
节点数是结果序列自身的长度，只取决于图的语义（节点集合与去重后的边关系），
与边的存储冗余无关，因此截断点稳定。

## 2. 截断确定性：必须固定的三处顺序

1. **出边顺序**：每个节点的出边按目标 ID 字典序展开，不依赖 map 迭代；
2. **同层内入队顺序**：按「父节点在本层的顺序 × 该父节点有序出边」的顺序入队，
   去重后顺序保持（先到者占位）；
3. **预算扣减时机**：仅在节点真正加入结果序列时扣 1，
   预算耗尽的瞬间立即停止，之后的任何边都不再考察。

三处都固定后，同图、同起点、同预算两次遍历必得到逐元素相同的序列。

## 3. 续点为什么必须包含整个队列与已访问集合

BFS 的状态不只是「最后访问的节点」。截断时：
- 本层可能还有若干节点**尚未展开**，且各自展开到第几条边（游标）；
- 已展开但尚未访问的子节点在**待访问积压**中，顺序必须保留；
- 已发现集合决定哪些节点会被去重，丢失它会导致重复访问与序列错乱。

只记最后一个节点无法恢复同层剩余节点，也无法判定后续候选是否已发现。
因此续点包含：已访问集合、已发现集合、当前层各展开器（节点 ID + 出边游标）、
待访问积压（有序）、已完成标志、起点。状态编码为长度前缀字段并追加 SHA-256 校验和。

## 4. 资源核心：有界积压

朴素做法在访问一个节点前就把它的全部出边入队，星形图中心节点会瞬间入队一万条。
改为：展开是惰性的，每次只为一个父节点产出至多一个下一个子节点，
且待访问积压长度封顶为剩余预算（实现上以总预算为帽）。
积压将满时先消费访问、再继续展开。
这样峰值队列规模为 O(预算) 而非 O(最大出度)：
当前层展开器 ≤ 已访问数，积压 ≤ 预算，已发现集合 ≤ 2×预算，
测试断言峰值队列 ≤ 4×预算。每条（去重后的）边至多被考察一次，游标只增不减。

## 5. 边界与错误分类（errors.Is 可区分）

- `ErrChecksum`：校验和不符（含任何被检测到的语义篡改）；
- `ErrIncomplete`：字段截断、长度不足、游标越界等结构性不完整；
- `ErrMissingNode`：续点引用的节点在当前图中不存在（含起点不存在、中途删节点）。
解码顺序：先结构长度 → 再校验和 → 最后对照图校验引用；
三类互斥，调用方用 `errors.Is` 区分。
- 预算 0：返回空序列，续点等于初始续点（幂等）。
- 预算 ≥ 可达规模：遍历完成，续点标记 done；对 done 续点续传返回空且无错误。

## 6. 并发

遍历是纯函数：图只读（邻接表构造完成后不再修改，读取无需加锁），
所有计数器与状态存在于单次调用的局部 Walker 中，不写包级或共享变量。
同一图与同一续点可被多协程并发续传，各自结果独立；`-race` 下干净。
