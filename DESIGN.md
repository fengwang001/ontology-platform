# DESIGN — 随机超平面 LSH 分桶检索器

## 1. 签名计算规则

对维度为 d 的向量 v，第 t 张表（共 L 张）使用 b 个随机超平面，法向量
h_{t,i} 的每个分量独立取自标准正态分布。签名为 b 位无符号整数：

    bit_i(v) = 1  当且仅当  <v, h_{t,i}> >= 0，否则为 0

约定：内积恰为 0 时取 1。因此零向量与任何超平面内积恒为 0，其签名恒为
全 1（2^b - 1），所有零向量落在同一个桶，行为确定且可复现。

## 2. 位数 b、表数 L 与候选数、召回的关系

两个夹角为 θ 的向量，在单个超平面下同侧的概率为 p(θ) = 1 - θ/π。
一张 b 位表内二者同桶的概率为 p(θ)^b。

- 候选数：设 q 与一个“随机无关”向量的夹角约 π/2，则单表期望候选约
  N·(1/2)^b；L 张表的并集期望候选约
      C ≈ N · (1 - (1 - 2^-b)^L) ≈ N · L · 2^-b   （b 较大时）
  即候选数随 b 指数下降、随 L 近似线性上升。
- 召回：一个夹角 θ 的真实近邻被至少一张表命中的概率为
      R(θ) = 1 - (1 - p(θ)^b)^L
  R 随 L 单调不降（加表只增候选），随 b 单调下降（p(θ)^b 衰减）。
- 权衡：b 越大桶越细、候选越少、精排越便宜，但召回越低；要补召回只能
  加表，代价是候选近似线性增多。工程上先取 b ≈ log2(N) 使单表候选
  O(1)，再用 L 把召回推到目标值。本题 N=5000，取 b=8、L=8 可让
  top10 平均召回 ≥ 0.6 且每查询精排候选 < N 的 10%。

## 3. 可复现性

每张表的超平面由 `rand.New(rand.NewSource(seed + t))` 重新生成，不共享
全局随机源；生成顺序固定（逐平面、逐分量）。同种子两次建索引得到逐字节
相同的签名表（测试按“签名排序后序列化”比较）；不同种子必产生差异。

## 4. 落盘格式（小端）

    头部(28B): magic "LSH1" | dim u32 | bits u32 | ntables u32 | nvecs u32 | seed u64
    超平面参数: ntables × bits × dim 个 float64
    桶表: 每表 nbuckets u32，随后每桶 sig u64 | count u32 | ids u32×count（sig 升序写出）
    尾部: CRC32(IEEE) u32，覆盖此前全部字节

截断分类按字节区间判定：落在头部区 → ErrHeaderTruncated；落在超平面区 →
ErrHyperTruncated；落在桶表区 → ErrBucketTruncated；落在 CRC 区或校验值
不符 → ErrCRCMismatch。结构校验先于 CRC：若某超平面法向量被改为全零
（退化、无法划分空间），读回时先报 ErrDegeneratePlane 并指出表号与位号，
再论完整性。恢复（Recover）解析最大可恢复前缀：头部完整才继续，超平面
完整才解析桶表，桶逐个解析、遇不完整即停；桶表中 ≥ nvecs 的悬挂 ID 一律
剔除并计数，保证恢复结果自洽。向量本体保留在内存索引中，落盘文件只描述
LSH 结构（自描述头 + 超平面 + 桶表）。

## 5. 并发语义

Add 只在互斥锁下追加向量；Build 用一致快照离线构建全部表后，通过
atomic.Pointer 一次性发布。查询只读已发布快照，因此并发建索引时查询
要么看到完整的旧索引、要么看到完整的新索引，绝不会看到半成品表。
精排距离计数与建索引哈希计数均用原子/锁保护，`-race` 干净。

## 6. 边界语义

NaN/±Inf 向量在 Add 时拒绝并计入可读的 skipped 计数；空索引、单向量、
K>N（返回全部候选）、维度为 1 均为合法输入；查询维度不符返回
vec.ErrDimMismatch（含期望与实际维度），四类落盘错误与
ErrDegeneratePlane 均可用 errors.Is 区分。
