# SAGA 编排器设计推导

时钟由构造器注入 `now func() int64`（毫秒），全代码无任何 `time.Now/After/Sleep`。

## 1. 正向「可能已生效」错误（推导，非结论先行）

错误分两类：`step.ErrDefiniteFailure`（明确失败，副作用保证未发生）与其他错误（超时/未知）。
约束只有两条：(a) 不变量 1 要求只补偿「已成功（产生效果）」的步骤；
(b) 补偿动作必须幂等。若把未知错误当失败不补偿：当真实情况是「已生效」时，
该效果永久泄漏，且没有任何记录可补救——违反 (a) 的意图。
若把它当成功补偿：真实「已生效」时补偿正确；真实「未生效」时，补偿对一个从未发生的
效果执行一次，幂等性保证这与「什么都不做」等价（补偿以幂等键 `IdemKey` 为参数）。
**结论：未知错误记为 `ForwardUncertain`（视为可能生效），停住正向并参与逆序补偿。**
只有明确失败记为 `ForwardFailed`，且该步不补偿。

## 2. 重试规则

`Step.Retries` 表示「首次调用之外」的额外尝试次数，受配置 `MaxStepRetries` 上限约束，
超限在 Run 入口立即拒绝。
- 正向：每次失败后重试，直到成功/未知或耗尽尝试；耗尽后最后一次错误若为明确失败则
  记 `ForwardFailed`，否则记 `ForwardUncertain`。
- 补偿：同样按 `Retries` 重试；补偿失败不影响后续步骤的补偿（不变量 2）。
不违反不变量 3：每次真实尝试追加 `Attempt` 记录，但「成功类终态」每步只有一条；
重放只依据终态记录判定是否跳过，已成功步的动作绝不再次执行。未知错误立即停试
（再试无法把「未知」变「明确」），同样由日志钉住。

## 3. Resume 起点对照表

恢复策略：先只取最后 1 条；若为终态标记直接判定，其余情况再读全量重建。
因此终态实例读取条数恒为 1，与日志长度无关。

| 日志最后一条记录 | 含义 | Resume 续跑起点 |
|---|---|---|
| 无记录 | 尚未开始 | 从第 0 步正向开始 |
| `ForwardOK(i)` / `ForwardUncertain(i)` | 第 i 步有了终态 | OK：从 i+1 步正向；Uncertain：停止正向，从 i 起逆序补偿 |
| `ForwardFailed(i)` | 第 i 步明确失败 | 不补偿 i；从 i-1 步开始逆序补偿 |
| `Compensated(i)` | i 补偿完成 | 对首个未补偿的更低已成功步继续补偿（逆序） |
| `CompFailed(i)` | i 补偿失败 | 重试 i 的补偿（仍逆序、计失败），完成后继续更低步骤 |
| `ForwardDone`（标记） | 正向全部成功 | 空操作（成功终态） |
| `CompDone`（标记） | 补偿流程走完 | 空操作（补偿终态，含部分失败） |

## 4. 已完全补偿后再次 Resume

选择**空操作并返回当前状态**，不报错也不重跑。理由：`CompDone` 是终态标记，
Resume 的契约是「使日志收敛到终态」，终态上收敛等于自身；重跑会让真实调用次数增长，
直接违反不变量 3；报错则把合法的重复投递变成调用方负担。不存在实例才报错。

## 5. 事实来源与重建

实例状态只由该实例的日志序列决定：扫描记录即可得到每步正向终态、每步补偿结果、
终态标记、失败补偿集合。`Replay(journal, instance)` 纯函数重建，与在线 `State` 逐字段
一致。在线状态仅作缓存，任何分支都不依赖缓存之外的信息。

## 6. 错误清单（互不相同、可判定）

`ErrNoSteps`（步骤为空）、`ErrDuplicateKey`（幂等键重复）、
`ErrNilCompensation`（步骤已成功却无补偿动作，记为该步补偿失败）、
`ErrMaxSteps`、`ErrMaxStepRetries`、`ErrJournalFull`（上限，追加前判定，不留半写）、
`ErrInstanceNotFound`、`ErrInstanceRunning`（并发 Resume 同一实例）。

## 7. 并发

journal 单实例内互斥追加与读回；编排器用 map 管理实例并加 RWMutex；
每个实例有独立运行标记，同一实例并发 Resume 第二次即得 `ErrInstanceRunning`；
不同实例无任何共享写状态。
