# DESIGN — LZ77O 自定格式流式压缩

## 流格式（全部无符号 LEB128 变长整数；偏移从流首字节 0 起）

| 记录 | 标记字节 | 后续字段（均 varint） |
|---|---|---|
| 流头 | 魔数 `LZ77O` 4B | 版本=1 |
| 字面量 | 0 | 长度 n | n 个原始字节 |
| 回指 | 1 | 距离 d（1..窗口） | 长度 l（≥3） |
| 刷新 | 2 | 无 |
| 流尾 | 3 | 原文总长 | FNV-1a 64 校验和 |

流头后是若干（字面量|回指）记录；刷新可插入任意位置；流尾之后必须无字节。

## 推导 1：压缩为何与 Write 切法无关

贪心匹配在位置 i 的决策只依赖「i 起的输入」与「i 之前的历史」。若在 Write 边界
立即输出，跨边界的更长匹配会被截短。解法：压缩器缓存自上次 Flush 以来的全部未决
数据，仅在 Flush/Close（或并行块边界）时对整段数据跑一次确定的贪心编码；历史窗口
只含此前已 Flush 的字节。因此任意 Write 切法、同一 Flush 偏移 → 同一段字节 →
同一编码。Flush 强制结束匹配：它必须让解码器此刻还原全部输入，故把缓存排空为
字面量/回指记录并写刷新标记；标记后历史仍保留，后续匹配可回指 Flush 之前数据。
无新数据时 Flush 不写任何字节（刷新标记只在真正排空后写一次）。

## 推导 2：重叠回指（d < l）的解压

块复制 `copy(dst, src)` 对重叠区间按平台 memmove 语义拷贝，d<l 时 src 后段尚未
生成，结果错误；而 LZ 语义是逐字节「输出[k]=输出[k-d]」，会重复刚产生的字节
（d=1 即符文）。实现中维护「已输出窗口」环形缓冲，循环 l 次取
`b := win[pos-d]; emit(b); win[pos]=b`，每次只复制 1 字节。每次迭代仅两次索引
与一次写，O(l) 不可避免（输出本身就 O(l)），常数极小，故逐字节循环可接受。

## 推导 3：两种「距离超限」为何不同

d>已输出字节数：引用了从未存在的内容，任何合法流（含预置字典流）都不可能，属
「前向引用」；d≤已输出但 d>窗口容量：压缩端窗口只保留最近 W 字节，绝不可能产生
此回指（并行块的预置字典字节同样在该块窗口内）。两类成因不同，分别报错并带偏移。

## 推导 4：并行块压缩的确定性与衔接

块 i 的贪心编码输入仅 = 本块原文 + 上一块末尾 W 字节（字典），不读取其他块、不
依赖 goroutine 完成顺序，故块编码是纯函数；按块序号顺序拼接即确定，workers 1/2/
4/8 输出逐字节相同。衔接：写一次流头；每块编码后紧跟一个刷新标记（最后一块也写，
随后流尾），解码器在刷新点把当前窗口末 W 字节预置为下一块字典；每块自身仍是合法
记录序列，流式解码器无需知道块大小。
并行块把上一块末尾 W 字节作为本块预置字典。解码端窗口在刷新标记处**不重置**：
那些字典字节本就是此前已输出并留在环形窗口里的，所以「d≤已输出」的检验对字典
位置天然成立，「已输出」含义不变；压缩端只可能引用窗口内（≤W）字节，故绝不可能
产生 d>W。
此回指（并行块的预置字典字节同样在该块窗口内）。两类成因不同，分别报错并带偏移。
并行块把上一块末尾 W 字节作为本块预置字典。解码端窗口在刷新标记处**不重置**：
那些字典字节本就是此前已输出并留在环形窗口里的，所以「d≤已输出」的检验对字典
位置天然成立，「已输出」含义不变；压缩端只可能引用窗口内（≤W）字节，故绝不可能
产生 d>W。
