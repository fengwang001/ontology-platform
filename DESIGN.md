# 设计推导：分片扇出查询的部分失败合并器

## 1. 模型

- 查询扇出到 N 个分片，分片由可注入接口模拟；每个分片返回 `Response{Records, Claimed, MaxScore}`。
- 分片状态：OK / Timeout / Corrupt / Canceled / Empty。Corrupt = `len(Records)!=Claimed`。
- 整体有截止时间 D 与并发上限 C；fanout 用信号量（容量 C 的缓冲通道）限并发。
- 记录以 `(ShardID, RecordID)` 全局去重，重复返回（重试叠加）不重复计数。
- 分片可给出 `MaxScore`（本分片任意单条记录得分上界）；缺失分片上界之和记为 M。

## 2. 五种聚合在部分失败下能说什么

设成功分片集合为 S，缺失集合为 F。

- **Count**：缺失分片只可能再增加计数。观测值 c=ΣS count，真实值 C*≥c。标注 `≥ c`（精确当 F 为空）。
- **Sum**：同理，真实 S*≥s。标注 `≥ s`。
- **Min**：缺失分片可能带来更小值。观测 m=min(S)，只能保证 m 在结果中出现过，
  真实最小值 m* 满足 **m* ≤ m**（缺失只会更小或相等）。标注 `≤ m`。方向切勿写反。
- **Max**：对称地，真实 x* 满足 **x* ≥ m**。标注 `≥ m`。
- **TopK**：
  - 缺失分片最多再贡献 `|F|` 条、每条得分不超过该分片 `MaxScore`，故外部最高分总和阈值
    `M = Σ_{f∈F} MaxScore_f`（缺失分片未给出上界则视为 +∞，可信前缀 0）。
  - 设当前 TopK 列表按 (Score 降序, ID 升序) 为 t1..tK。
    若前 j 条每条得分都 **严格大于 M**，则任何缺失记录都无法超过它们中的任一条，
    故这 j 条必定入选最终 TopK（入选确定；它们之间的相对排名也确定，因为内部顺序已定，
    外部得分 < t_j ≤ 前 j 条中的最小分，无法插入其间）。
    即「可信前缀长度」 j = 最长连续前缀使 min(t1..tj 的得分) > M。
    第 j+1 名及以后可能被挤出，不可信。
  - M=0（无缺失）时 j=K，标注精确；M 很大时 j=0，整表不可信。

## 3. 边界与可判定错误

- 零分片：返回 `ErrNoShards`，不产生零值「精确」报告。
- 全部失败：返回 `ErrAllShardsFailed`；与「全部分片成功但结果为空」（合法，精确零值）区分。
- K>总条目数：返回实际长度，前缀判定照常。单分片、空 ShardID（合法）、部分字段
  （Value 缺省 0 但仍计入 Count；Score 缺省 0）均不报错。
- 损坏（条数不符）判该分片 Corrupt，其数据不并入。

## 4. 资源约束（可证明）

- 每个请求启动前 `sem <- struct{}{}`，因此同时在飞数 ≤ C；计数器在持槽期间
  `inflight++` 并更新 `peak`（互斥保护），测试读 `Peak()`。
- 截止时间到达：调度循环不再启动 goroutine（新发起数为 0）；
  ctx 被取消，已在飞请求收到取消；Wait 关闭收集通道后返回，200 分片 C=8 时峰值恰 ≤ 8。

## 5. 确定性

- 去重后，所有聚合只依赖最终记录集合；到达顺序无关。
- TopK 并列按 RecordID 升序；缺失分片清单与每分片状态按 ShardID 升序输出，报告可逐字节复现。

## 6. 包划分（9 个 .go 文件上限内）

shard（接口+假分片）、fanout（调度）、combine（五聚合+去重）、
confidence（标注/区间/可信前缀）、report（报告装配）、cmd/demo。
测试按包合并为单文件单表：fanout_test（含故障注入/资源/顺序）、combine_test、confidence_test。

## 7. demo 判定行（共 10 条）

1 五聚合精确；2 Count/Sum 下界；3 Min 方向 ≤；4 Max 方向 ≥；5 TopK 前缀=K；
6 TopK 前缀=0；7 峰值≤上限；8 去重幂等；9 全失败错误；10 20 种乱序逐字节一致；末行总计。
