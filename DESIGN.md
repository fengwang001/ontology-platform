# DESIGN — LSH 分桶近似最近邻检索器

## 签名计算规则

- 每张表 t 有 b 个随机超平面，法向量 n[t][h] 为 dim 维、逐分量取标准正态。
- 向量 v 在表 t 的签名为 b 位整数：第 h 位 = 1 当且仅当 `dot(v, n[t][h]) > 0`。
- **内积恰为 0 的约定**：取 0 位（与 `< 0` 同侧）。因此零向量合法，其签名恒为全 0；
  全同向量（含零向量）必然全部落入同一桶，候选数等于总数，召回必为 1。
- 含 NaN / ±Inf 的向量在建索引时被拒绝并计入可读出的跳过数（`search.Index.Skipped`）。

## 可复现性

- 超平面只由显式种子生成：`rand.New(rand.NewSource(seed))`，按 (表, 位, 分量) 固定顺序
  调用 `NormFloat64()`。同种子 ⇒ 法向量逐字节相同 ⇒ 每个向量的签名逐字节相同；
  不同种子 ⇒ 签名序列几乎必然不同。测试对此逐向量断言。

## 位数 b、表数 L 与候选数、召回的关系

- 两向量夹角为 θ 时，单个超平面同侧概率 `p(θ) = 1 − θ/π`（随机超平面基本性质）。
- 一张 b 位表内碰撞概率为 `p(θ)^b`；L 张表至少碰撞一次的概率（= 该近邻被召回的概率）：
  `R = 1 − (1 − p(θ)^b)^L`。
- 候选数：对近似随机（θ≈π/2，p≈1/2）的非近邻对，单表期望候选 ≈ `N·2^(−b)`，
  L 张表去重并集 ≲ `L·N·2^(−b)`。
- 定性结论：
  - b ↑ ⇒ 桶更细，候选数按 `2^(−b)` 指数下降，但近邻碰撞率 `p^b` 也指数下降 ⇒ 召回降；
  - L ↑ ⇒ 召回按 `1−(1−p^b)^L` 单调上升并趋于 1，候选数近似线性上升；
  - 因此调参路径：先用 b 把候选压到远小于 N（本题要求 ≤10%），再用 L 把召回抬到阈值以上。
- 实测矩阵（b=4/8/12 × L=1/2/4/8 的候选数与 top10 召回）见 FINDINGS.md 表一。

## 复杂度约束（可证明）

- 哈希计算次数：每个向量对每张表算 b 次内积，建索引总次数恰为 `向量数 × L × b`，
  `hyper.Family` 内原子计数器精确记录，测试断言不多不少。
- 精排距离计算数 = 候选去重并集大小，`search.Index` 内非导出计数器记录；
  当 `L·N·2^(−b) ≪ N` 时远小于暴力扫描的 N，测试在 5000 向量上断言 ≤500（10%）。

## 落盘格式（persist，全部小端）

- 头（22B）：magic `"OLSH"`(4) | ver u16 | dim u32 | bits u32 | tables u32 | nvec u32。
- 超平面参数：tables×bits×dim 个 float64。
- 桶表：每表 nbuckets u32；每桶 sig u64 | cnt u32 | cnt 个 id u32。
- 尾部：CRC32(IEEE) u32，覆盖此前全部字节。
- 截断分类（按字节区间）：`<22` 头部不完整；`<22+超平面字节数` 超平面参数不完整；
  桶表区间内 桶表不完整；落入尾部 4B 或 CRC 校验不符 CRC 不匹配。四类错误均可 `errors.Is` 区分。
- 恢复：读出头与完整超平面后，顺序解析桶表，只保留完整解码的桶；引用 id ≥ nvec 的
  悬挂项剔除并计数（`Recovered.DroppedIDs`），保证恢复结果自洽。
- 退化检测：读回时逐表逐位检查法向量，全零则报 `DegenerateError{Table, Bit}`。

## 并发

- 索引查询只读，可多协程并发；`bucket.Index` 用 `sync.RWMutex`，一次 Add 对全部表
  原子可见（要么全可见要么全不可见）；计数器用 `sync/atomic`。`-race` 必须干净。
