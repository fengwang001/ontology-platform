# 设计推导

## 1. 空行与单列空值

结论：**空行被跳过**，不产生记录；单列中值为空的记录表示为 `""`（加引号空字段）。
推导：连续两个 `\n` 既可能是「一条单字段空记录」也可能是视觉空行，解析层无法消除二义性。
令空行被跳过，则单字段记录（无论空否）必须以行尾结束，单字段空记录的裸写就是 `\n`，
恰与空行不可区分。为消歧，单字段空值必须带引号：`""\n` 含非换行字节，不是空行。
回写规则：①单字段且值为空时写 `""`；②每条记录都写行尾（末记录无换行属非规范）。
不加引号会丢数据：单列中 `a\n\nb\n` 的第二条记录会被当空行跳过。
规范输入：每记录以 `\n` 结束；仅当字段含 `,`、`"`、`\n` 或规则①时加引号；无孤立 `\r`。
该定义下 `Write(Parse(x))==x`；引号内 `\r\n` 原样保留的输入属非规范（值级往返仍成立）。

## 2. par 切点

buf 按 K 个固定字节偏移切段，每段一个 goroutine 并发算 OUT 假设（入口态 F：引号外、字段起点）。
随后单线程从左到右选择拼接：第 0 段选 OUT；上段出口态为 F 时本段选 OUT；
上段在字段中途结束（U/Q/Q1/C）时，用「上段出口态」作精确入口态对本段补算一次 CONT，
每段补算至多一次 ⇒ 处理量 ≤ OUT 的 N + CONT 的 N = 2N+K，不因子段长短退化。
CONT 入口态由权威上段出口唯一确定，无需猜引号内外；切点前一字节仅作常量 peek（总计 ≤K 字节）。
CONT 首字段是开口字段：与上段开口字段值拼接、Start 取上段值、字段字节计数含前缀，
使上限判定时刻与单流一致。段解析器注入「已完成记录数、当前记录已完成字段数、开口字段已计字节数」，
事件号与错误号即全局值；字节偏移用段基址，恒为绝对偏移。
错误只在被选中的解析上生效。末段 OUT/CONT 都执行 Close（捕获未闭合等 EOF 错误），非末段不 Close。
状态机确定 + 选中的入口态序列与单流解析逐段一致 ⇒ 记录、字段、引号标记、偏移、错误逐位相同。

## 3. CR 待定

未引号字段末见 `\r` 进入 C 态：下一字节 `\n`→行尾；逗号或其他字节→孤立 CR 错误；EOF→孤立 CR 错误。
引号内 `\r` 直接入值；引号态中 `\r\n` 仍在引号内，原样保留；引号刚闭合（Q1）后的 `\r` 进入 C(Q1)，
若随后非 `\n` 则为孤立 CR 错误。C 是普通可持久化状态，半包挂起/恢复无需特殊处理；
`\r|\n` 切点：OUT 段以 C 出口触发下一段 CONT，入口态 C 配合 peek 判定，与单流一致。

## 4. 上限「立刻」

字段字节计数在字节被收入字段值时自增；引号态 `""` 的第二个 `"` 只产出一个值字节，在收入它时计数，
故第一个超限字节到达即 ErrFieldTooLarge，不缓冲整字段。字段数在每个字段完成时计（逗号/行尾触发），
记录数在记录完成时计。触发即终态：记录 terminalErr，后续 Feed 返回同一错误，已产出完整记录保留。

## 5. 状态转移表

状态：F=字段开始，U=未引号中，Q=引号中，Q1=引号中刚见引号，C=CR 待定。
动作：v=字节入值，f=完成字段，r=完成记录，z=跳过空行。输入类：`,` `"` `\n` `\r` 其他 o，EOF。

| 态 | , | " | \n | \r | o | EOF |
|---|---|---|---|---|---|---|
| F | f→F | →Q | r/z→F | →C | v→U | f+r（输入全空则不产记录） |
| U | f→F | 错误:裸引号 | f+r→F | →C | v→U | f+r |
| Q | v | →Q1 | v | v | v | 错误:未闭合 |
| Q1 | f→F | v→Q | f+r→F | →C(Q1) | 错误:引号后杂字符 | f+r |
| C | 错误:孤立CR | — | f+r→F | 按内容(§3) | 错误:孤立CR | 错误:孤立CR |
