# 配置解析与环境覆盖器 — 设计推导

## 1. 先合并后展开（顺序的核心推导）

反例：文件层 `greeting = hello ${name}`、`name = file`；环境层只覆盖 `name = env`。
- 若**先展开后合并**：文件层先把 `greeting` 展开成 `hello file`，合并时环境层没有
  `greeting` 键，于是最终值固化为 `hello file`——引用方没有随被引用方更新。
- 正确做法：**先按优先级合并出最终键值表，再对最终值统一做引用展开**。此时
  `name` 已解析为 `env`，`greeting` 展开为 `hello env`。
- 溯源也遵循同一顺序：先记录每键各层**原始值**，再在最终表上展开，因此原始值
  （`hello ${name}`）与展开值（`hello env`）可同时出现在报告里。

## 2. 引用语法、转义与错误

- 引用形式 `${dotted.key}`；一条值可含多处引用（`${a}${b}` 展开两处）。
- `$$` 为转义：`$${a}` 输出字面量 `${a}`，不展开。
- 遇到 `$` 后下一字符不是 `{`：`$` 按字面量处理。
- `${` 之后直到值末尾仍无 `}`：报「未闭合引用」并给出 `$` 的字节位置。
- 环：展开沿引用边做 DFS，栈上重复即环；报出环路径，首尾为同一键且每条边都
  来自输入。自引用 `a=${a}` 同样检出。
- 每个键只完整求值一次（memo），复杂度 O(节点+边)。构造
  `k0=x, k_i=${k_{i-1}}${k_{i-1}}` 到 k20 时朴素递归是 2^20，缓存后替换次数
  为各键对引用体求值时的替换数之和，上界 `4*键数*平均引用数`。

## 3. 合并

优先级（高到低）：命令行 > 环境 > 文件 > 默认。一遍扫描：按低到高遍历四层，
每个存在的键只做一次 map 写入/查找；总查找数 ≤ 4*总键数。层内键互不相同
（冲突属可判定错误）。输出按键名字典序，保证确定性。

## 4. 环境变量名映射与歧义

- 规则甲（扁平）：键路径按 `.` 分段，逐段大写，段间用单下划线连接：
  `a.b.c -> A_B_C`；段内原有下划线转双下划线避免冲突。
- 规则乙（前缀双分隔）：首个点映射为双下划线、其余点映射为单下划线，段内
  下划线保留：`a.b.c -> A__B_C`。
- 同一键若两种映射产物同时存在（`A_B_C` 与 `A__B_C` 都设置），则无法判定该值
  来自哪种规则，报「环境变量名歧义」错误。

## 5. 文件格式与逐字节截断分类

格式：首行是声明的行数 `N`，其后恰好 N 行 `key = "value"`，末行无换行。
任何真前缀都不是合法文件，因此每个截断点必可分类：
- **行不完整**：最后一行以换行结尾（文件止于行边界，还欠后续行；N 行全完整时
  行数不足也归此类）。
- **键不完整**：最后一行不含 `=`。
- **值不完整**：最后一行含 `=` 但缺结尾引号。
错误均带行号。四类错误（类型不符 / 必填缺失 / 环境歧义 / 文件截断）用独立
sentinel error，可 `errors.Is` 区分。

## 6. 校验

- 类型：int/string，类型不符报键名、期望类型、实际值、来源层四要素。
- int 解析：允许可选正负号后接十进制数字，空串与其他字符均非法。
- 必填：一次收集全部缺失键，不提前返回。

## 7. 包划分（均仅标准库，状态在进程内存）

`source`（四类来源读取/规范化、截断分类、env 映射）、`merge`（优先级合并+
计数）、`expand`（扫描器+memo 展开+环检测）、`trace`（逐键逐层溯源）、
`validate`（类型/必填）、`cmd/demo`（判定演示）。键路径空串、空段
（`a..b`）为可判定错误；空值合法且与「键不存在」可区分；空配置合法。
