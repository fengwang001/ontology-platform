# 带撤回的增量聚合视图维护器 — 设计推导

## 1. 数据模型

- `Record{Group string; Value float64}` 是一条基表记录；`Change{Op, Group, Value, Version}` 是一次变更。
- Op：Insert / Delete / Update。Update 语义为「把 `(Group,Value)` 旧记录改写为 `(NewGroup,NewValue)`」，
  实现上等价于先撤回旧贡献、再加新贡献，因此可跨组移动。
- Version 单调递增，是去重与排序的唯一依据。

## 2. 聚合器撤回策略推导

设聚合状态 S，删除值 v，问「S 能否仅靠自身与 v 得到新状态」。

- **Count**：S 是基数。删除确定存在的一条 → S-1，与值无关。可纯增量撤回，不需成员。
- **Sum**：S 是值的代数和，加法群可逆 → S-v。可纯增量撤回，不需成员。
  浮点严格按记录到达顺序做 IEEE754 减法；全量重算按同一记录序列求和，故位级一致
  （比对用 `math.Float64bits`）。+0/-0 位模式不同但数值相等，删除按值相等判定。
- **Min**：S 只是当前最小值。若 v > S，极值未受影响，无需动作；若 v == S，
  仅凭 S 无法知道是否还存在另一个 S，也不知道次小值 → 必须遍历该组成员重算。
  为避免「删一个等于最小值但仍有重复最小值」也触发重算，增量态额外记录当前最小值的出现次数，
  仅当被删值等于 Min 且其出现计数归零才重算。
- **Max**：与 Min 对称，记录最大值出现次数，归零才重算。
- **DistinctCount**：S 是不同值个数。删除 v 后无法从基数判断 v 是否还被别的记录持有，
  因此删除总是需要该组成员（以成员集合重算 distinct 集合）。

结论接口：每个聚合器声明 `NeedsMembersOnDelete() bool`。
Count/Sum 返回 false；Min/Max/DistinctCount 返回 true。
`view` 仅在该声明为 true 且满足上述触发条件时进入 Recompute，故重算是例外而非常态。

## 3. 三阶段编排 Apply → Recompute → Commit

- Apply：在分组状态上做可增量的加/减；标记受影响组。
- Recompute：仅对被标记组，遍历该组成员集合从空聚合器重算，
  访问成员数有上界（不超过该组当前成员数，绝不扫描别组）。
- Commit：发布新聚合态、推进已应用版本号。版本推进只在 Commit 发生，
  任一阶段崩溃，已应用版本都不会越过最后一个完整提交点。

组内成员为空时整体删除该组：`Groups()` 不含它，查询返回「不存在」而非零值。

## 4. 版本幂等判定

记已提交最大版本为 V：
- version < V → 回退，返回可判定错误 `ErrVersionBackward`，不改动任何状态；
- version == V → 幂等重复，直接视为成功但状态逐字段不变（不二次加/减）；
- version > V → 正常处理，提交后 V=version。
幂等依据：变更以版本为唯一键、聚合更新是对「该版本所描述的那一记录状态转移」的确定函数，
重复转移不应再作用一次。

## 5. Journal 格式与重放

文件 = 自描述头 + 若干帧。头含魔数与版本；帧 = `[4 字节大端体长][记录体][4 字节 IEEE CRC32]`，
CRC 覆盖记录体。记录体编码 Op/Group/Value/Version。
重放顺序读取，尾部损坏按下界长度分类为四类可判定错误：
头部不完整 / 长度前缀不完整 / 记录体不完整 / CRC 不匹配。
遇到损坏帧即停，已完整帧全部生效，半截帧不生效。

## 6. 恢复

重放日志 = 从空视图按序 Apply 完整帧。三阶段崩溃注入点（Apply 中途 / Recompute 中途 /
Commit 之前）使该次变更不产生提交点；恢复后视图等价于只重放到上一完整提交点，
与不崩溃运行在同一完整前缀下逐字段相同。

## 7. 并发

一把互斥锁串行化变更提交（含成员集合与聚合态），读者不可能看到
「Count 已改而 Sum 未改」的中间态；快照在锁内拷贝后释放。
Recompute 与后续写在同一临界区内顺序执行，同组并发写入不丢失。`-race` 干净。

## 8. 边界

空视图合法；分组键空串合法；缺失分组（`GroupSet=false`）拒绝并计数；
NaN 不是可聚合值（自身不等、破坏 Min 比较），拒绝并计数；+0/-0 视为相等。
