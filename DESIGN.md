# 多区间 Range 响应组装器 — 设计推导

工程结构：`rangespec`（词法/语法解析）→ `coalesce`（归一化）→ `multipart`（封装）→ `serve`（组装器），`source`（字节源抽象）独立，依赖方向单向。只用标准库，状态全在进程内存。

## 第 1 条：`bytes=-0` 为什么是不可满足而不是语法错误

- `-n` 的语义是"资源的末尾 n 字节"。语法层面 `-` 后跟一个非负整数即合法，`0` 是一个完全合法的整数词法单元，因此 `bytes=-0` 在**语法上没有任何错误**。
- 语义层面，"末尾 0 字节"是一个**空区间**：长度为 0，不覆盖任何字节。它不是"写错了"，而是"请求了一个合法但为空的集合"。
- 同理可推：`a-b` 中 `a > b`（如 `5-3`）也是空区间；起点 `a >= 总长` 是越出资源。三者共同点是：语法正确，但归一化后**不覆盖任何字节**。
- 结论：解析器（`rangespec`）对 `-0` 必须接受；归一化器（`coalesce`）把空区间/越界区间判为**不可满足**，并携带资源总长，供调用方生成 416。语法错误与不可满足是两类可判定的错误（`ParseError` vs `UnsatisfiableError`），`bytes=-0` 归入后者。

## 第 3 条：为什么"相邻"也必须合并

- 区间 `[a,b]` 与 `[b+1,c]` 首尾相接，覆盖的字节集合与 `[a,c]` **完全相同**——合并不改变语义，只减少段数。
- 若不合并相邻区间，会违反第 7 条"单区间不封装"的退化情形：客户端请求 `bytes=0-3,4-7`，实际覆盖一段连续字节，本应得到**裸字节**响应；不合并就会得到两段 multipart 封装，语义上等价的请求产生了两种不同形态的响应体，且 multipart 封装引入了边界串开销与解析负担，毫无收益。
- 因此归一化规则定为：排序后，凡 `next.Start <= cur.End+1`（重叠或相邻）一律并入当前区间。合并后列表互不重叠、互不相邻，是覆盖同一字节集合的**最简表示**。

## 第 4 条：归一化算法与比较次数上界

- 算法：先按起点升序**排序**，再一次线性扫描合并。扫描只做相邻两项比较，共 `n-1` 次；排序是唯一的非常数复杂度来源。
- 排序用**迭代式归并排序**（自底向上、宽度倍增），比较次数有确定上界：每一趟归并的总比较次数 `< n`，趟数为 `ceil(log2 n)`，故
  `C(n) <= n * ceil(log2 n)`，严格为 `O(n log n)`，与输入分布无关（不像快排有最坏 O(n²)）。
- 计数器：`coalesce` 包内一个**非导出**包级计数器，每次区间比较（归并比较与扫描比较）自增；测试通过导出的 `Compares()`/`ResetCompares()` 观测。
- 证明方式：测试对 n=100 与 n=10000 分别断言 `C(n) <= n*ceil(log2 n) + (n-1)`——这是严格的 O(n log n) 证明。两组实测数字与比值一并打印：实测比值约 200 余（归并排序平均比较数约为 `n*log2 n - 1.26n`，低阶项使小 n 的分母偏小，比值略高于 200 的整倍数），测试另断言比值 `<= 300` 以排除 O(n²)（其比值量级为 `100^2 = 10000`）。

## 第 6 条：断点续写的状态与不变量

- 组装器在 `Prepare` 阶段把**完整响应体**（裸字节或 multipart 字节流）一次性构建进内存缓冲区。这可行，因为第 9 条已对单次响应总字节数设上限，内存占用有界。
- 续写状态只有一个字段：`written int64`（已写出字节数）。`Write(p)` 从 `body[written:]` 拷贝，按实际拷贝量推进 `written`，返回短写计数；写满后返回 `io.EOF`。
- 不变量：**任意时刻 `body[:written]` 是已交付前缀，`body[written:]` 是待交付后缀**。调用方无论把写出切成多少段、切在哪里，拼接得到的都是 `body` 的前缀序列，最终必然等于 `body` 本身——与切分点无关。测试遍历所有切分点（1 到总长）逐字节比对验证。
- 单组装器不保证并发写出（`Write` 与 `Info` 用互斥锁保护内部一致性，但并发写出的分段语义由调用方串行化），此约束写入包文档。

## 第 8 条：边界串规避策略与失败概率上界

- 策略：边界串 = 固定可打印前缀 + 128 bit 密码学随机（`crypto/rand`，hex 编码 32 字符）。生成前先**扫描完整内容**（内容已在内存，见第 6 条），若候选边界串出现在内容中则重掷，重试次数有上限（可配置），超限返回可判定的 `ErrBoundaryRetries` 错误。
- 因为内容在生成边界串**之前**已全部确定，"扫描-冲突-重掷"循环必然终止于一个不冲突的边界串（除非随机源连续撞库），不存在流式场景下"内容后到、无法预知"的问题。
- 失败概率上界：设内容长 `L` 字节，单次随机候选与内容冲突的概率 `<= L / 2^128`（内容中至多 `L` 个起始位置，每个位置撞上 128 bit 随机值的概率为 `2^-128`）。`k` 次重试全失败的概率 `<= (L / 2^128)^k`。取 `L = 2^40`（1 TiB，远超实际上限）、`k = 8`，上界约为 `2^-704`，实际可视为零。
- 测试构造内容里恰好含"看起来像边界串"（同前缀、可打印）的字节，断言最终边界串不出现于内容中，且响应可正确分段。
