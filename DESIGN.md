# DESIGN — 流式 UTF-8 ⇄ UTF-16 转码器设计推导

## 1. 非法单元切分规则的推导

七个样例中，`E0 80 80`、`ED A0 80`、`C0 AF`、`F4 90 80 80`、`80 80` 都是「每个字节一个 U+FFFD」，
而 `F0 90 80 41` 是「一个 U+FFFD + 字符 A」，`E2 82`+EOF 是「一个 U+FFFD」。
观察差异：前一组里**第二个字节就已落在该首字节允许的第二字节区间之外**（或首字节本身非法），
此时若把后续字节并入同一单元，就无法解释为何 `F0 90 80`（二、三字节都合法）要整体只算一个单元。
由此推出统一规则（与 WHATWG Encoding 的 UTF-8 decoder 一致）：

- 首字节决定序列长度与**第二字节合法区间**（见第 5 节表）。
- 逐字节检查；在**第一个**落在允许区间外的字节处停止：该字节之前的已消费前缀构成一个非法单元，
  越界字节本身**不被吞掉**，作为新字符重新处理（`F0 90 80 41`：`41` 越界 → 吞 `F0 90 80` 一个单元，`41` 重解析为 `A`）。
- 首字节本身非法（`80..BF`、`C0 C1`、`F5..FF`）→ 单元长度 1，故 `E0 80 80`：`80` 越界 → 吞 `E0`，
  随后 `80`、`80` 各自是非法首字节 → 共三个单元。`ED A0 80`、`C0 AF`、`F4 90 80 80` 同理均为每字节一个。
- 流结束时残留的**合法前缀**整体算一个单元（`E2 82` → 一个 U+FFFD），因为前缀内部没有越界字节可切分。

「第二字节区间」在代码里的表达：首字节 `b` 给出 `(lo, hi)`，第二字节须在 `[lo,hi]`，第三、四字节须在 `[80,BF]`。

## 2. 切分点缓存上限与放弃等待的时机

UTF-8 最长序列 4 字节，故切分缓存（pending）**硬上限 4 字节**；UTF-16 侧同理上限 4 字节（一个奇数字节 + 一个高代理）。
「可以立刻判定非法、不必等流结束」的时机：

- 首字节落笔即知：非法首字节立即产出单元，不等后续。
- 每个后续字节落笔即与当前位置允许区间比较，**越界立即产出单元**并把越界字节重放，无需等齐整个序列长度。
- 只有「目前为止全部合法但还没凑齐长度」的前缀才需要等待；它在 EOF 时整体成为一个单元（截断）。

因此每字节最多被检查 2 次（一次作为当前序列的后续字节、一次作为重放后的新首字节），全程检查次数 ≤ 2n。

## 3. par 的切点对齐

段 `i` 的切点 `s` 可能落在多字节字符或非法单元中间。规则：

- **起点对齐**：从 `s` 向前最多回看 3 字节（`u8.AlignStart`），找到「以它为起点、按第 1 节规则推进后恰好跨过 `s`」的单元起点 `a`；
  找不到则 `a = s`。UTF-8 单元最长 4 字节，回看 3 字节必然足够。
- **归属**：起点落在段 `i` 范围 `[start_i, end_i)` 内的单元归段 `i` 输出与统计。段 `i` 用「非 EOF 段模式」转码
  `[a_i, end_i)`：段尾残留的未决前缀**不产出**（EOF 规则不适用，因为后面还有字节）；若 `a_i < start_i`，
  解码器恰好多产出一个起点在上一段的跨界单元（carry），段 `i` 只把它计入统计、不输出字节。
- 例：`C3 | 80 80`：段 0 在切点处留下 `C3` 不产出；段 1 回看到 `C3`，解出单元 `C3 80`（一个 U+FFFD，计入统计不输出），
  再解出 `80`（一个 U+FFFD，输出）。合计两个单元，与单线程一致。`E0 80 80` 跨切点时，回看解出的 `E0` 在切点前越界终止，
  起点在上一段 → 不输出，`80 80` 各自成单元 → 仍三个。
- 单线程总检查 ≤ 2n；对齐每段只回看 ≤ 3 字节，故 par 总检查 ≤ 2n + 3K，满足 n + 8·常数 的界（按每字节摊还）。

## 4. 输出超限断点的语义

`Write` 在编码每个标量**之前**检查输出上限：放不下整个标量（UTF-16 下代理对算一个标量 4 字节）就停，
返回 `ErrLimit`，`Write` 的返回值为**已完整转码并输出**的输入字节数。此时切分缓存里可能还有未决前缀，
这些字节**不算已消费**——调用方按返回值取输入后缀喂给新实例即可无缝续传，拼接结果与无上限一次转完逐字节相同。
EOF 截断错误（`ErrTruncated`）与非法字节错误（`ErrInvalid`）是不同哨兵；带偏移的错误用 `*stream.Error` 包装，
可用 `errors.Is` / `errors.As` 判定。

## 5. 首字节区间与吞字节数表

| 首字节 | 序列长度 | 第二字节合法区间 | 非法时吞掉字节数 |
|---|---|---|---|
| `00..7F` | 1 | —（无第二字节） | —（必合法） |
| `C2..DF` | 2 | `80..BF` | 越界前已消费的前缀（1） |
| `E0` | 3 | `A0..BF` | 同上（1 或 2） |
| `E1..EC` | 3 | `80..BF` | 同上 |
| `ED` | 3 | `80..9F` | 同上 |
| `EE..EF` | 3 | `80..BF` | 同上 |
| `F0` | 4 | `90..BF` | 同上（1..3） |
| `F1..F3` | 4 | `80..BF` | 同上 |
| `F4` | 4 | `80..8F` | 同上 |
| `80..BF` | — | —（非法首字节） | 1 |
| `C0 C1 F5..FF` | — | —（非法首字节） | 1 |

「吞掉字节数」= 第一个越界字节之前的合法前缀长度；越界字节重放。EOF 时未决前缀整体一个单元。
UTF-16 侧：高代理后须跟低代理，否则高代理自身一个非法单元、后跟单元重处理；孤立低代理一个单元。
