# DESIGN：可撤销的批量重命名与冲突解析器

模块 `ontology`，仅标准库。包划分：`name`（命名空间）、`plan`（冲突检测与排序）、
`cycle`（破环）、`apply`（执行与日志）、`undo`（按日志逆序撤销）、`cmd/demo`。

## 1. 命名空间与合法性（name）

- 命名空间是内存中的名字集合：`map[string]struct{}` 加互斥锁。
- 合法性定义：**任意字符串都是合法名字**，包括空串与含路径分隔符的名字
  （分隔符按普通字符处理，不做任何路径解释）。`Valid` 恒为真，校验集中在请求层。
- 批量执行期间禁止并发修改：`apply.Exec` 全程持有 `NS` 的互斥锁，
  并只通过 `RenameLocked`/`HasLocked`（调用方须持锁）操作；
  公开的 `Add`/`Remove`/`Has` 各自取锁，因此并发修改会被阻塞到批次结束。

## 2. 冲突分类（plan，全部在任何修改之前判定）

设请求集 R，命名空间当前名字集 S。先丢弃自环（见 §5），再按序判定：

1. `ErrDuplicateOld`：同一旧名出现两次（报出两个新名）。
2. `ErrOldMissing`：旧名不在 S 中（报出该名）。
3. `ErrDuplicateNew`：两个请求指向同一新名（报出两个旧名）。
4. `ErrTargetExists`：新名已在 S 中，且不是本批任何请求的旧名（即不会被搬走）。

四类错误都是 `*Conflict`，`Unwrap` 返回对应哨兵错误，可用 `errors.Is` 区分。
检测只做只读查询，发现冲突即整体拒绝，命名空间逐元素不变（测试断言）。

## 3. 执行顺序的推导（拓扑序）

请求 r: old→new。若 new 恰是另一请求 r2 的旧名且 r2 尚未执行，先执行 r 会覆盖
r2 尚未搬走的数据。因此 **r 依赖于 r2 ⇔ r.new == r2.old**，r2 必须先执行。

由于旧名唯一（冲突类 3）、新名唯一（冲突类 4），每个请求至多有一个依赖、
至多有一个依赖者：依赖图是**函数图**，即若干条互不相交的链加若干个纯环。

- 无依赖（r.new 不是任何请求的旧名）的节点可先执行；执行后解除其唯一依赖者
  （满足 r'.new == r.old 的 r'）的依赖。按层（Kahn BFS）推进，**同层按旧名字典序**
  输出，保证顺序与请求的构造顺序无关（确定性，打乱 20 次测试）。
- 例 `{a→b, b→c}`：dep(a→b)=(b→c)，故先 `b→c` 再 `a→b`，中途无覆盖。

## 4. 破环的通用做法与临时名数量（cycle）

环 `a1→a2, a2→a3, …, ak→a1` 无论怎样排序都互相阻塞。通用破环法：

1. 任取一环成员（取字典序最小者保证确定性），先 `a1→tmp`；
2. 此时 a1 已空，按逆序执行 `ak→a1, a(k-1)→ak, …, a2→a3`；
3. 最后 `tmp→a2`，完成 a1 的原请求。

k 元环产生 k+1 步，**恰好 1 个临时名**。环互不相交（函数图性质），
每个环独立破环，故 **临时名总数 == 环的个数**（10 个环恰好 10 个，计数器断言）。
每步的目标名在执行前都不存在：tmp 新生成、ai 刚被腾空，可用逐步快照检查器证明。

临时名生成：确定性候选 `\x00renametmp<N>`（N 从 0 递增），候选若与命名空间现有名、
本批任一旧名/新名、已用临时名冲突则重试；超过上限（1<<20 次）返回可判定的
`ErrNoTemp`，绝不覆盖数据。

## 5. 边界语义定义

- 空请求集：合法，产生空计划，执行与撤销均为无操作。
- 单请求：直接执行，无排序问题。
- 自环 `a→a`：**定义为无操作**，在冲突检测前丢弃，不参与任何冲突判定
  （`{a→a, a→b}` 等价于 `{a→b}`）。
- 空串名字、含 `/` 的名字：合法，按普通字符串处理。
- 请求数远大于名字数：必然重复旧名，被冲突类 3 拦截。

## 6. 复杂度论证

- 冲突检测：每个请求 5 次查找（重复旧名、重复新名、旧名存在、新名存在、
  新名是否在旧名集）；排序：每请求 2 次（求依赖、求依赖者）。合计 ≤ 7n 次，
  而合法批次涉及名字数 m ≥ n（环时取等），故 7n ≤ 4(n+m) 恒成立。
  `plan` 内非导出计数器 `lookups` 只统计这两个阶段的 map 读与命名空间查询
  （写入与破环阶段的临时名探测不计），测试用 5 万请求链断言上界。
- 排序按层归并，总代价 O(n log n)（排序主导），查找部分严格线性。

## 7. 执行、日志与撤销（apply / undo）

- `apply.Exec` 持锁执行；`FailAt=k` 注入第 k 步失败时，已执行的 k-1 步
  **立即逆序回滚**（内存逆操作），删除日志，返回 `ErrStepFailed`。
- 撤销日志格式（写本地临时目录）：
  - 头部 16 字节：magic `"RNLG"` + u32 版本 + u32 记录数 + u32 头部 CRC32；
  - 每条记录：u32 旧名长 + u32 新名长 + 旧名 + 新名 + u32 记录 CRC32。
- 截断分类（`undo.ReadLog` 返回最大可恢复前缀 + 可判定错误）：
  - 总长 < 16 或头部 CRC 不符 → `ErrHeaderIncomplete`；
  - 记录定长部分或名字数据被截断、或记录数不足头部声明 → `ErrRecordIncomplete`；
  - 记录数据完整但 CRC 字段缺失/不完整/不匹配 → `ErrCRCMismatch`。
- `undo.Undo` 按最大可恢复前缀**逆序**回滚，报告 `Undone` 与 `Total`
  （无法撤销的步数 = Total-Undone）；成功后删除日志，故**重复撤销幂等**
  （日志不存在时直接返回无操作）。撤销前后用排序快照做逐元素集合比对。
