# DESIGN

基础规则：每个字节在状态机中流转。普通字节直接输出；空格/制表符进入待定空白缓冲，
见到行尾则整串丢弃，见到非空白非行尾字节则整串回吐。`\r` 先记为待定 CR：
下一字节是 `\n` 则成对输出一个 `\n`；否则按单独 `\r` 输出 `\n`。因此
`\r\r\n` = 待定 CR 遇到 CR：第一个 CR 判为独行尾输出 `\n`，第二个与 `\n` 成对，得 `\n\n`。

## 1. 空输入与末尾换行策略

规范化主体 N0（不含末尾策略）删行尾空白、统一行尾、但不增删最末换行。三策略：

- Keep：输出 = N0(x)。空输入输出 `""`；`N("")=""`，幂等；`N("\n")="\n"`，`N("\n\n")="\n\n"`。
- EnsureOne：若 N0(x) 不以 `\n` 结尾则补一个 `\n`，否则不动。空输入补成 `"\n"`。
  再次规范化：`"\n"` 已以换行结尾，不再补，故幂等。若对空输入选 `""`，则
  「空」与「恰好一个换行」无法区分，`N("\n")="\n"` 而 `N(N(""))=N("")=""` 仍幂等，
  但策略语义退化为 Keep 的特例；本实现选补 `"\n"`，使「输出永远以换行结尾」成为不变量。
- TrimOne：取 N0(x) 删除全部末尾空行后再补一个换行；N0(x) 为空时输出 `""`（空文件
  无行可保留，补换行会凭空造出行，且 `""->"\n"->"\n"` 虽幂等但改变了空文件语义）。

| 输入 | Keep | EnsureOne | TrimOne |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

三者的输出均不含 CR 与行尾空白，故再次规范化结果不变，幂等成立。

## 2. 被删字节的 ToOut

映射用分段线性区间 `[a0,a1)->[b0,b1)`，允许零长输出区间。规则：被删原文字节所在区间
`[a0,a1)->[b1,b1)`（终点对齐，`b1` 是紧随其后的输出位置）。于是
- 行尾空白（如 `"a \n"` 中空白，原文偏移 1）映射到 `\n` 的输出偏移 1，即换行符**之前**；
- `"\r\n"` 的 `\r` 与 `\n` 同区间，`\r` 映射到 0、`\n` 映射到 0：`ToOut(cr)=ToOut(lf)`，
  光标落在行尾换行符上。编辑器把光标置于被删空白中时停在换行符前，不会跳到下一行。
终点对齐保证 ToOut 单调不减；ToOrig 对每个输出偏移 o 取 b0≤o<b1 的区间并线性反算，
ToOut 对落在区间内的原文偏移同式反算，对被删偏移取区间输出终点 b1，故
`ToOut(ToOrig(o))=o`，两向均单调不减。

## 3. 空白缓冲超限

选择「拒绝」：待定空白可能最终被删也可能回吐，在见到判定字节前提前输出会在「后续是
行尾」时产生无法撤回的错误输出（违反 4/5）；提前丢弃则在「后续是普通字节」时丢数据。
二者都破坏逐切分一致性。故超限时返回带原文偏移的哨兵错误 `ErrPendingLimit`，
已输出内容保留，Normalizer 进入终态，此后 Write 返回 `ErrClosed`，状态完全可解释。
输出字节上限同理：追加前检查，超限返回 `ErrOutputLimit`（带原文偏移）并终态化。

## 4. par 拼接

每段独立流式处理到「不应用末尾策略」的主体结果，并记录段尾状态：待定 CR 位置、
待定空白区间。顺序拼接时只看两段交界处有界信息（≤ 缓冲上限 + 1 字节）：

1. 前段尾有 CR、后段首字节是 `\n`：前段为该 CR 发出的 `\n` 与后段 `\n` 合并为
   `\r\n` 成对：删去前段那个 `\n` 的零长映射，CR 与 LF 同区间。
2. 前段尾有待定空白：依后段首字节判定——首字节是行尾则空白删除（零长区间），
   是空白/普通字节则空白存活（后段独立处理时首字节为空白也进待定缓冲，交界处按
   同一状态机重放即可）。

实现上不重放输出：每段产出的 span 区间坐标加上「段原文起点、段输出起点」平移量，
交界处用上述至多 1 个区间的插入/合并修正。修正范围有界，因为待定状态只含一个 CR 与
一串空白缓冲，长度分别 ≤1 与 ≤ 缓冲上限；与切几段无关。最后对全局结果应用一次末尾策略。
