# 自然排序比较器设计

## 分层规则（第 3 条推导）

切段：极大数字段（ASCII 0-9）与非数字段交替。比较分两层键：
1. 主键：段序列字典序。数字段按数值（去前导零后先比长度再比字节）；非数字段按字节序；数字段与非数字段相遇按当前字节比（数字字节 0x30-0x39 与非数字字节永不判等）。
2. 次键：主键全等时，各数字段前导零个数序列的字典序，少者小。
3. 主次皆等 ⟺ 两串完全相同（字节、数值、零个数共同定死原文）。

前导零不立刻比较，只在整串主键相等时作整串级决胜。若立刻判：a01b vs a1c 首段数值相等，按零个数得 a01 > a1 且与后续无关，会推出 a01b > a1c，与要求矛盾——故必须延后。

## 逐步演算

- a01b vs a1c：首段数值等，记决胜「a 侧多 1 零」；b < c 主键分胜负，决胜被丢弃 ⇒ a01b < a1c。
- a1b01 vs a01b1：首段 1 与 01 等，记决胜「a 侧零少则小」；b 与 b 等；尾段 01 与 1 又等（决胜不覆盖）；主键全等，启用首记决胜 ⇒ a1b01 < a01b1。
- a1<a01<a001、x0<x00<x000<x1：主键等（或数值 0<1），由零个数决胜。

## 传递性（第 4 条）

主键把串映为 token 序列（数字段→数值，非数字→字节），token 上为全预序（数字比值、字节比字节、混比由 0x30-0x39 区间唯一裁定），其字典序是全预序；次键（零个数序列字典序）亦然。两全预序的字典式复合仍是全预序，故反对称且传递；复合键等 ⟺ 两串逐字节同，故 Compare==0 ⟺ a==b，为严格全序。

## 非 ASCII（第 6 条）

仅 0x30-0x39 是数字，其余字节（UTF-8 多字节、全角 １２）一律进非数字段按字节比。字节序对任意字节串都是全预序，上述论证不依赖字节取值，故全序不破坏、不 panic。

## 测试对照表

| 语义 | 测试 |
|---|---|
| 1 基本 | TestCompareTable |
| 2 大数不溢出 | TestCompareTable（40 位行） |
| 3 前导零 | TestCompareTable（a1/a01/a001 等行） |
| 4 严格全序 | TestTotalOrderExhaustive（穷举三元组） |
| 5 排序结果唯一 | TestSortDeterministic（50 种打乱） |
| 6 非 ASCII | TestCompareTable（文件/全角行） |
| 字节计数界 | TestCompareByteBudget |
