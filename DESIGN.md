# 多租户配额公平队列调度器 — 设计推导

## 架构

- `task`：任务值类型（租户 ID、代价、提交序号 Seq）。
- `tenant`：租户（ID、权重、虚拟时间 VT、FIFO 队列）与 VT 推进规则。
- `sched`：加权公平选择。最小堆按 `(VT, ID)` 排序，堆中只放非空租户。
- `admit`：入队背压（每租户队列上限）、租户增删、四类哨兵错误。
- `stat`：份额统计与相对偏差度量。
- `cmd/demo`：九项判定的可运行演示。

## 为什么是 c/权重 而不是 c*权重

目标：长期看各租户获得的执行代价份额正比于权重，即 `C_i / C_j = w_i / w_j`。

定义虚拟时间为「已获得的归一化服务」：执行代价 c 后 `vt += c / w`。
选择器永远取 vt 最小者，于是所有活跃租户的 vt 被拉平到同一水平 V（落后者
立即被优先服务直到追上）。vt 拉平意味着 `C_i ≈ w_i · V`，即 `C_i ∝ w_i`，
正是加权公平。VT 是「每单位权重已消费的服务」，谁消费得少谁先来。

若改用 `c * w`：权重大的租户 vt 涨得更快，会被选择器判定为「已消费更多」
而压到队尾，结果是权重越大服务越少——与目标完全相反。故必须除以权重。

## 空闲租户重新加入时的 VT 抬升

设系统虚拟时间 `sysVT` 为最近一次被选中租户出队时的 vt（随选择单调不减，
即当前活跃租户 vt 的下界）。租户 X 空闲期间，其余租户把 sysVT 从 v0 推到 V。
若 X 重新入队时 vt 仍是 v0，则 X 一直是堆顶，直到 vt_X 追平 V：

- 欠款 `D = V - v0`；X 每执行一步 vt 前进 `c / w_X`；
- 独占步数 `= D · w_X / c`。例：w=1、c=1、Y 在 X 空闲期执行 1000 步，
  则 D=1000，X 将连续独占 1000 步（实测 1001，含一次 vt 并列），Y 被饿死。

正确做法：队列由空转非空时，`vt_X = max(vt_X, sysVT)`。抬升后 X 与在役租户
齐平，只按权重参与竞争，连续执行不超过 1 步（并列时按 ID 决定先后），
Y 立即恢复服务。抬升不损害公平：X 空闲期间本就没有需求，不欠它服务。

## 代价为 0 的任务

c=0 时 `vt += 0`，租户 vt 不变，永远停在堆顶 → 无限独占。处理：有效代价
取下界 `c_eff = max(c, MinCost)`，`MinCost = 1`。零代价任务合法入队，但每
执行一个仍推进 `1 / w`，与代价 1 的任务同等让位，不会独占。

## 并列打破

堆比较器用二元组 `(VT, ID)`：vt 相等时按租户 ID 字典序。比较全序确定，
同一任务流任意次重放得到逐元素相同的调度序列。

## 数值表示：float64

vt 用 float64：53 位尾数，10^5 次累加的相对误差 ~1e-11，远小于 5% 的公平
性容差；量程到 1e308，权重 1e-9 与 1e9 并存时 vt 在 [1e-9, 1e12] 量级，
不会溢出。有理数（big.Rat）精确但慢且内存无界，不必要。

## 背压、移除与错误

- 每租户队列上限 L：第 L+1 个入队返回 `ErrQueueFull`，只影响该租户。
- 权重 ≤ 0：`tenant.New` 返回 `ErrInvalidWeight`。
- 入队/移除不存在的租户：`ErrUnknownTenant`。
- 队列非空时移除租户：拒绝并返回 `ErrTenantBusy`；任务去向定义为「留在
  队列中直到被正常取出」，取空后方可移除。
- 四类错误均为哨兵错误，调用方用 `errors.Is` 区分。

## 并发与确定性

- `sched.Scheduler` 内部以互斥锁保护全部状态；`admit.Admitter` 自带互斥锁，
  「检查队长 + 入队」与「Next」在同一把锁下串行，故背压判定与取任务无竞态。
- 提交序号 Seq 在入队锁内分配。并发提交时，「提交顺序」定义为获取入队锁的
  串行化顺序；单线程固定流则完全确定，重放逐元素一致。
- 选择复杂度：单次 Next = 一次堆 Pop + 至多一次 Push，比较次数
  `≤ 4·ceil(log2(n))`（Pop 下沉每层 ≤2 次比较，Push 上浮每层 1 次），
  n 为非空租户数；空闲租户不进堆，不占选择代价。
