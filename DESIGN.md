# 连接顺序代价模型 — 设计推导

范围：给定表、统计、连接谓词，用按子集的 DP 选取代价最低的连接顺序。仅标准库，进程内存，只读统计可被多协程共享。

## 1. 并列打破（可复现判定）

DP 中每个计划代价是至多 `n-1` 次浮点乘加的结果。IEEE-754 双精度每次运算相对误差界 `u≈1.11e-16`，
两条数学上同代价的计划因求和/连乘结合顺序不同，误差至多约 `2·n·u`（n≤20 时 < 5e-15 的相对差），
但朴素 `==` 会把这 1 个 ULP 的差误判为不同，选择随枚举顺序漂移。

判定策略（`plan.equalCost`）：

1. `|a-b| <= ε·max(|a|,|b|,1)`，`ε=1e-12`。依据：该阈值比 20 层累加的误差界（~5e-15）大约 3 个数量级，
   足以吸收 ULP 漂移；又小于任何有意义的计划差距——真实差距来自选择率因子 `1/NDV`，
   即便 NDV=1e9（因子 1e-9），乘到基数上产生的相对差也在二阶量级，且此类计划不在"数学同代价"语境。
2. 判为同代价时，比较两计划从左至右的叶子表名序列（`leafNames`），取字典序更小者。
3. 进入 DP 前先把表按名排序，子集按掩码数值升序、划分按枚举顺序固定，故登记顺序不影响任何结果。

## 2. 笛卡尔积推迟

谓词图 G 以表为顶点、连接谓词为边。枚举子集 S 的划分 (L,R) 时，
若 L、R 之间**无任何谓词**，则仅当 `S = 全集`（最后一步）才允许该划分。
依据：非最后一步的笛卡尔积把无关节点并入中间表，其基数 `|L|·|R|` 不被任何选择率压缩，
再与剩余表连接时代价单调不减；在存在跨边可用时先连跨边总是不劣。
推论：链式图（连通）全程无笛卡尔积；两段不连通图各段内部正常连接，
仅在根步把两段做一次笛卡尔积——恰好一次且在最后。

## 3. 基数估计

叶子：`card = 当前目录行数`（统计过期时按目录行数校正，不扫数据）。
连接：`card(L⋈R) = card(L)·card(R)·Π sel(p)`，p 遍历跨 L、R 的全部谓词。
等号谓词选择率 `sel = 1/max(NDV(c1), NDV(c2))`（取值域较大者，连接后不同值不超过较小一侧，
`1/max` 是该上界下的标准估计）。多谓词选择率直接相乘 = 属性值独立、均匀分布假设；
谓词 ≥2 个时计划标注「独立性假设」。
某列无统计：sel 回退写死默认值 `0.1`，标注「估计不可靠」，不报错。
行数为 0 → 基数 0；行数为 1 正常参与公式。估计只读统计结构，计数器保证行数据零访问。

## 4. 代价模型

扫描：`scanCost = rows + 10·ceil(rows/100)`（每行 1 个 CPU 单位 + 每 100 行一个 I/O 页，页代价 10）。
哈希连接：`joinCost = costL+costR + 2·min(cardL,cardR) + max(cardL,cardR)`
（在较小侧建表 2 倍权重、较大侧探测 1 倍权重）。代价只依赖已选子集的估计基数。

## 5. 统计异常（errors.Is 可区分）

- 缺失：列无统计对象 → sel=0.1，警告随计划传播，explain 标注「估计不可靠」。
- 过期：`|statRows-catRows| > 0.1·catRows` → 警告，基数采用 catRows，标注「统计已过期」。
- 损坏：直方图桶计数之和不等于声称行数，或桶上界非严格递增 → `ErrCorruptStats`，
  错误信息含表名与列名，登记时直接返回，中止选择。
三类错误哨兵：`ErrMissingStats`（谓词引用未登记列时）、`ErrStaleStats`、`ErrCorruptStats`；
另有 `ErrEmpty`（零表）、`ErrLimit`（超子集上限）、`ErrSelfJoin`（谓词两侧同表，明确拒绝）。

## 6. DP、复杂度与上限

`best[mask]` 只保留每个子集的最优计划；单表子集为扫描叶子。枚举顺序：mask 升序，
对每个 mask 枚举真子集 `sub=(mask-1)&mask …`（只取 sub 含最小位的一半去重）。
计数器：`Subsets`（≤2^n）、`Partitions`（每子集划分数合计 ≤3^n，远小于 n!）。
硬上限 `MaxSubsets=4096`：n=12 非空子集 4095 不触发；n=20 为 1048575，插入前超限即返回 `ErrLimit`，
已分配条目不超过上限，不随枚举无限增长。

## 7. 自连接与并发

谓词引用同一张表的两个列时返回 `ErrSelfJoin`：当前模型以"表集合"为状态，
无法区分同一关系的两份相关副本与相关名修正，明确拒绝而非静默产生错计划。
所有输入结构只读；Choose 不修改 catalog，计数器位于每次调用独立的状态对象，`-race` 安全。
