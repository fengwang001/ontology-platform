# 设计推导

记号：E=行尾归一化+行尾空白删除后的中间结果（策略无关，逐字节稳定）。

## 1. 末尾换行策略（空输入推导）

- Keep：输出 E。空输入→`""`；无末尾换行的内容不加。
- One：去掉 E 全部末尾 `\n`，非空则补一个 `\n`；空→`""`。
- Trim：与 One 同式（先删全部末尾空行再保留一个），空→`""`。

备选「空输入也补 `\n`」：`N("")="\n"` 仍幂等（再规范不变），但违反“无可删之物”，
且 0 字节文件被改为 1 字节。故选空→空。该式下 N(E) 只有 0/1 个末尾换行，故幂等。
One 与 Trim 输出相同：纯空白行也是行，被保留在主体里，二者差异无法显现。

| 输入 \ 策略 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut（推导）

区间按 [o0,o1)/[i0,i1) 左闭右开存三类段：keep(等长)、drop(输出长度0)、insert(原文长度0)。
要求 ToOrig 单调不减且 `ToOut(ToOrig(o))==o`：每个输出偏移 o 必须落在某段输出区间内部，
不能被映射规则吞掉。因此边界归属必须「左归被删侧」：
- drop 段 `[i0,i1)→o0`：其所有原文偏移（含 `\r\n` 的 `\r`、被删空白）ToOut=o0；
  下一段（保留的 `\n`）的起点 ToOut=o0。ToOrig(o0)=i1，故 ToOut(i1)=o0，往返成立。
- insert 段（合成换行，i0 同点）：ToOrig(o0)=i0；ToOut(i0) 在共享点上左归前一段，
  insert 内部偏移 o0 映射回 i0，往返仍成立。
结论：被删空白里的光标映射到换行符**之前**（边界点 o0，ToOrig 解释为换行起点 i1），
编辑器按此跳转停在新行行首，符合“删除发生在换行之前”的直觉。

## 3. 空白缓冲超限（推导）

强制输出空白会制造假“中间空白”：若其后真出现行尾，这些字节本应删除却已输出，
违反幂等(5)与跨切分一致(4，切分点可制造超限差异)，映射(6)也无法回退。
故选择**拒绝**：返回哨兵错误，带当前原文偏移，保留全部已输出字节，进入终态，
之后 Write/Close 返回“终态后写入”错误。不变量只描述成功路径或显式错误路径，均可解释。
默认上限给大值；NUL 严格模式同构处理（记录偏移、保留输出、终态）。

## 4. par 拼接（推导）

每段独立以 Keep 规范化，得 E_k、局部 span 与段尾状态（是否待定 `\r`、尾部空白串）。
段间只有两种需要修正的情况，均只涉及切点两侧：
- 前段以待定 `\r` 结束、后段首字节是 `\n`：段内把 `\r` 当断行（输出1），
  全局应为 `\r\n` 一个断行。修正：把 `\r` 与 `\n` 两个段都改记为 drop，
  合并后输出长度−1（丢弃前段那个 `\n`）。
- 切点落在尾部空白中：前段视为行尾空白已 drop；后段会把该串当行中空白 keep。
  修正：将后段对应 keep 改为 drop；若该串后随（跨段）`\r\n`，按上一条再处理。
修正范围有上界：仅一个待定 `\r` 与至多「空白上限」长度的尾部空白串，不向段内扩散。
坐标平移：段 k 的所有段区间偏移加段 k 全局原文起点；输出坐标按已拼接 E 长度累加，
并随上述删除修正减去对应长度。最后对拼接整体施加 One/Trim（删尾部 `\n` 补一个，
用 insert/drop 段记账），空→空。严格模式下 NUL 取全局最小偏移报错。
