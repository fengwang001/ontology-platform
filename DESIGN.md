# 分块流式写出管线设计

## 1. 零长写入与结束语义

线路格式为：十六进制块大小行、可选扩展、CRLF、块体、CRLF；`0\r\n\r\n` 是唯一的结束标记。
因此，大小为 0 的普通块和结束块在字节层面无法区分。上游调用 `Write(nil)` 或空切片只表示“这次没有提交数据”，不能被翻译成 `0\r\n`；否则下游会立即读到结束块，并可能把之后的真实字节当作结束后的非法尾部。连续任意多次零长写入都必须在入口被吞掉：不移动输入计数、不创建块、不改变时钟窗口，也不影响 Close 幂等状态。

Close 只负责把仍在时间窗中的非空数据冲掉，再写入一次 `0\r\n\r\n`。结束状态由管线自身的 `closed/finished` 标志决定；重复 Close 直接返回，保证结束块在线路上只出现一次。Close 后的 Write 返回可判定的 `ErrClosed`。

## 2. 短写、背压与断点状态

管线把已形成的块编码成不可变 frame，并维护：

- `confirmed`：下游已确认接收的线路字节总数；
- frame 队列：已产生但尚未完全确认的块；
- `currentOffset`：队首 frame 内已确认的偏移；
- chunker 聚合缓冲：已接受但尚不足成块的原始字节及其起始时间；
- `closed/finished`：是否已计划或已确认写完结束块。

核心不变量是：重连后从 `queued[0][currentOffset:]` 继续，而不是从块开头重发。sink 的一次 `Write` 若返回 `(n, err)`，先把 `n` 计入确认位置，再处理 `ErrWouldBlock` 或断开错误。因而 `0<=n<=len(p)` 时，`[0,n)` 只发送一次，`[n,len(p))` 保留并在下一次发送；任意短写切分点都满足“不重不漏”。

背压与短写都不是数据丢失信号：管线保留队列并停止本次推进。背压期间上游追加的数据进入 chunker，随后形成的新 frame 只能排在队列尾部，所以不会丢弃或重排。断开时快照保存 chunker 缓冲、未确认 frame、当前 frame 偏移、confirmed 和关闭标志；用同一快照和新 sink 重建后，未来字节流由相同输入与相同注入时钟重新决定，已确认前缀不会再发送，最终拼接结果必然等于从未中断的输出。

## 3. 块边界与调用边界无关

切块器只面对一个逻辑字节流和一个注入时钟：内部维护连续缓冲、首字节时间、最小块、最大块和时间窗。Drain 时，超过最大块的数据按固定最大块大小切出；达到最小块的数据立即成块；不足最小块的数据在时间窗到达时成块；Close 冲掉剩余非空数据。

这些规则不读取单次 `Write` 的长度或调用次数。例如三次写入 `ab`、`c`、`defg` 与一次写入 `abcdefg`，在相同时钟下占用相同的逻辑时间点并产生相同负载块序列。若按调用边界成块，短写恢复后用相同逻辑输入重建管线就可能得到不同块头，破坏“任意切分点最终字节逐字节相同”的断言，也会让断点状态无法只依靠字节流位置描述。

## 4. 限制、拒绝与只读不变量

三类上限使用彼此可判定的错误：单块超限 `ErrChunkTooLarge`、扩展总长超限 `ErrExtensionTooLarge`、待写缓冲超限 `ErrBufferLimit`。拒绝先于任何状态修改执行：先检查扩展总长，再模拟加入本次输入后的待写字节数；任一失败都不复制字节、不推进时钟窗口、不改变 frame 队列。

缓冲达到上限说明下游速度慢，而不是数据无效。丢弃会破坏字节序列，普通致命错误会迫使上游丢失本次写入；因此缓冲超限同时满足 `errors.Is(err, ErrWouldBlock)`，让上游以后重试，同一批字节之后仍按原顺序进入管线。

查询只取互斥快照，不调用 sink、不成块、不推进时间。查询口径定义在线路写出侧：`accepted = confirmed + pending`，其中 pending 精确等于 chunker 非空缓冲、所有未确认 frame 字节和未确认结束块之和；所以 `AcceptedBytes() == ConfirmedBytes()+PendingBytes()` 恒成立。连续两次查询在无其他写者时必然相同。所有公开方法共用一把锁，写入、推进、查询并发运行时状态也保持一致。

## 5. frame 队列与复杂度

待写结构不是一个会被整体重扫的大 `[]byte`，而是 `container/list` 形式的不可变 frame 链表。每个 frame 保存一块已编码的连续字节；队首 frame 额外保存 `offset`。一次推进只把 `front.frame[front.offset:]` 交给 sink，返回后最多调整一个 offset、摘除一个已完成 frame。扫描计数器 `scanBytes` 记录本回合触碰的这一段长度，其上限是单块 frame 大小，不遍历后续 frame，也不扫描 chunker 中其余 N 字节。

因此，缓冲从 1KB 增加到 1MB、且每轮下游只接收少量字节时，单次推进的扫描量只取决于当前 frame 的固定最大块长度和当前短写长度，不随总缓冲 N 线性增长。恢复状态按 frame 负载和偏移保存；重建时重新编码这些负载，避免把历史已确认字节放入快照。
