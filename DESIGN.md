# 严格 Base64 尾部规范推导

标准 Base64 把每 3 字节拆成 4 个 6 比特值。最后不足 3 字节时，用 `=` 补足到 4 个字符。

- 余 1 字节：8 个信息比特放入前两个 6 比特组；第二个组最低 4 比特没有信息，必须为 `0000`。形式为 `xx==`。
  因此 `QQ==` 中第二个值为 `Q=16`，末 4 比特是 `0000`；`QR==` 中 `R=17`，末 4 比特是 `0001`，解码虽可截成 `A`，却不是任何字节串的编码结果，必须拒绝。
- 余 2 字节：16 个信息比特放入前三个 6 比特组；第三个组最低 2 比特没有信息，必须为 `00`。形式为 `xxx=`。
  所以 `QUI=` 的 `I=8` 末 2 比特为 `00`，而 `QUJ=` 的 `J=9` 末 2 比特为 `01`。
- 无填充的完整组必须总是解码为 3 字节；填充只能是末尾组的 `==` 或 `=`，其后不得再有任何字符。

结论：严格判定不只看字符和填充形状，还要检查“为对齐而补出的零比特”：两填充查第二个 6 比特值的低 4 位，一填充查第三个 6 比特值的低 2 位。该规则保证 `Encode` 与 `Decode` 一一对应：任一字节串的标准编码唯一；任何被接受的文本都必须正好是某个字节串的编码结果。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待补 | 待补 |
