# 并行任务图调度与失败传播器 — 设计推导

## 1. 问题与分层

输入：DAG（节点=任务，边 u→v 表示 v 依赖 u）、每任务一个执行函数、并发度上限、失败模式。
输出：每个任务的最终状态与原因的确定性报告。分五包：

- `graph`：建图、环检测（Kahn + 剩余子图上走父指针取真实环路径）、拓扑分层。
- `exec`：执行单个任务函数，recover panic 并包装为 `PanicError`（`errors.Is` 可达 `ErrPanic`）。
- `fail`：状态机。四类终态的判定、跳过原因的根因回溯、取消标记。
- `sched`：调度循环。就绪队列按 ID 升序、并发度上限、两个非导出计数器（峰值并行数、就绪判定次数）。
- `report`：按任务 ID 字典序渲染的逐字节确定性报告。

## 2. 四类终态的判定推导

每个任务的生命周期：`Pending → Running → 终态`。终态判定依据两条独立事实：
(a) 任务函数是否被调用过；(b) 它的依赖闭包里是否有失败任务。

1. **成功 Success**：函数被调用且在首次失败发生前已返回 nil。终态不可回滚——
   因为结果已被调度器消费并可能触发下游入队，改标会破坏已发生的调度决策。
2. **失败 Failed**：函数被调用且返回非 nil 错误（含 panic 经 recover 转换）。报告存原始错误。
3. **被跳过 Skipped**：函数**从未被调用**，且不再可能被入队。判定式：
   `state==Pending 且 存在失败祖先（或快速失败停机）`。
4. **被取消 Canceled**：函数**已被调用**（处于 Running），首次失败到达时 ctx 被取消。
   判定式：失败时刻 ∈ running 集合。

**Canceled 与 Skipped 的区分依据**：唯一判据是「失败发生的瞬间，该任务是否已进入
Running」。Skipped 的任务函数一次都没被调用；Canceled 的任务函数正在执行（或已开始
但尚未返回）。因此慢任务与快失败任务并行时，慢任务 = Canceled；而失败任务的下游
（从未入队）= Skipped。两者不能合并：Canceled 任务可能已产生副作用，审计语义不同。

**取消后写回**：Canceled 是终态。调度器在收到结果时先查状态，若为 Canceled 则丢弃结果，
状态与报告不变。这要求调度器 `wg.Wait()` 等所有已启动 goroutine 退出后才返回（也是
无 goroutine 泄漏的前提）。

## 3. 跳过原因的根因指向

跳过原因必须指向**最初失败的任务**而非直接上游。推导：失败沿边向下传播，B 因 A 失败
被跳过，C 因 B 被跳过而被跳过——若原因只记直接上游，C 的原因会指向 B（一个本身没有
失败的节点），丢失根因。因此对每个 Skipped 任务 s，根因 = s 的失败祖先中拓扑层深最小
者（同层取 ID 字典序最小）。层深最小 ⇔ 沿任意路径回溯到的「第一个真正执行并失败」的
任务。多任务同时失败时，各分支的被跳过任务各自回溯到本分支的根失败，互不污染。
原因在报告生成时惰性计算（此时失败集合已完备），保证确定性。

## 4. 两种失败模式的语义差异

- **FailFast（默认）**：首个失败到达即 (1) cancel 共享 ctx；(2) running 集合全部标记
  Canceled；(3) 失败任务的后代标记 Skipped（根因惰性回溯）；(4) 其余所有未启动的
  Pending 任务标记 Skipped，原因指向首个失败任务（停机语义：不是依赖失败，而是调度器
  已停机）。不再启动新任务，但已启动的会被等待退出。
- **BestEffort**：不取消 ctx。仅失败任务的后代被跳过；与失败无关的分支继续跑完。
  此模式下**不会出现 Canceled**——这是两种模式状态分布的可判定差异。

## 5. 并发与复杂度约束的构造

- 并发上限：调度循环只在 `running < maxConc` 时启动任务；`peak` 在每次启动后更新。
  启动循环先于任何结果接收执行，故 500 个无依赖任务、上限 8 时峰值恰为 8。
- 就绪判定次数：初始每节点 1 次（indegree==0 判定）+ 每条边在其父节点完成时恰好
  递减并判定 1 次，总计 `n + e ≤ 4(n+e)`。不存在「每完成一个任务全图扫描」。
- 无泄漏：结果通道容量 = maxConc（worker 发送永不阻塞）；调度循环在 running 清空前
  持续消费；返回前 `wg.Wait()`。三条路径（全成功/快速失败/尽力而为）同一代码路径退出。

## 6. 确定性

- 就绪队列：插入时保持按 ID 升序，弹出队首 ⇒ 同时就绪时取字典序最小。
- 报告：按任务 ID 字典序输出 `id state reason`，与边的添加顺序、完成顺序无关。
- BestEffort 下终态与完成时序无关（无取消），故注入随机延迟报告仍逐字节相同；
  FailFast 的确定性以「单失败源」场景断言（多失败源时谁先到达本就不可判定）。

## 7. 故障注入的处置

- 环：执行前由 `Layers()` 检出，`CycleError.Path` 为输入边上首尾闭合的真实路径
  （含自环 `A→A`）。
- panic：`exec.Run` defer recover，包装为 `PanicError`，走正常失败传播，调度器不崩。
- 多失败：BestEffort 下每个失败各自 `Fail()`，报告记录全部；传播原因各自回溯。
- 取消后写回：见 §2，结果按状态丢弃。
