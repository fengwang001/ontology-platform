# 设计推导：并行任务图调度与失败传播

模块 `ontology`，仅标准库，状态全在进程内存。包划分：`graph`（建图/环检测/拓扑分层）、
`sched`（调度器：并发上限、就绪队列）、`exec`（执行与 panic 捕获）、`fail`（状态枚举与哨兵错误）、
`report`（确定性报告）、`cmd/demo`（验收演示）。

## 一、失败后四类终态的判定推导

调度器对每个任务维护两个事实：**是否已派发**（goroutine 已进入任务函数）与**是否已回报结果**。
失败发生（快速失败模式下首个失败触发全局取消）后，任一任务的终态由这两个事实唯一推出：

1. **已成功 Succeeded**：已回报且错误为 nil。成功事实先于失败被记录，之后任何取消都只针对
   「已派发未回报」集合，故已成功任务永不回滚、不改标。
2. **失败 Failed**：已回报且错误非 nil（含 panic 被 `exec` 包 recover 后包装成的 `*fail.PanicError`，
   可用 `errors.Is(err, fail.ErrPanic)` 判定）。报告保留原始错误。
3. **被取消 Canceled**：**已派发但未回报**时取消发生。判定依据是「已开始跑过」——
   调度器在触发取消的瞬间把当前 running 集合整体标记为 canceled；这与是否依赖失败任务无关，
   只与「取消发生时它正在跑」有关。
4. **被跳过 Skipped**：**从未派发**。与 Canceled 的区分依据就是派发标志：跳过 ⇒ 未派发；
   取消 ⇒ 已派发未回报。二者互斥且完备（未派发 或 已派发）。

**跳过原因指向**：对跳过任务沿前驱边向上做迭代 DFS，收集所有「失败」祖先，取任务 ID 字典序最小者
作为原因（字典序是「最早失败」的确定性代理，保证报告逐字节可复现）。因此 `A→B→C` 中 A 失败时，
B、C 的原因都指向 A 而非直接上游。快速失败模式下与失败无关的未派发任务，原因指向触发取消的
首个失败任务。

**取消后写回**：被标记 canceled 的任务之后仍回报时，结果一律丢弃；唯一例外是「真实错误优先」——
若它回报的是自身的真实错误（非 `context.Canceled`、非 nil），则改记 Failed。这同时满足
「多任务同时失败须全部记录」：两个并行任务都真实失败时，第二个不会被误标为 Canceled。
成功结果的迟到写回不改变 Canceled 终态。

## 二、两种模式语义

- **快速失败 FailFast（默认）**：首个失败即 `cancel()`，running 集合 → Canceled，
  未派发任务全部 → Skipped，不再派发新任务；调度器仍等待在跑的 goroutine 全部退出后才返回
  （结果丢弃），保证无泄漏。
- **尽力而为 BestEffort**：不取消。只有「全部前驱成功」的任务才会就绪；某前驱失败的任务永远不就绪，
  循环结束时自然落入 Skipped；不依赖失败任务的分支继续跑完。此模式下不出现 Canceled。

## 三、并发与复杂度不变式

- 派发前检查 `running < limit`，`running` 只在调度主循环（单 goroutine）内增减，峰值计数
  `peak` 随之更新，故峰值 ≤ 上限是结构性保证。
- 就绪判定只在两处发生：初始化时每个节点查一次入度；任务成功时对其每条出边做一次入度递减。
  故 `readyChecks = 节点数 + 已松弛边数 ≤ n + e < 4(n+e)`，绝无全图扫描。
- 环检测用迭代 DFS（三色标记），拓扑分层用 Kahn，跳过原因用迭代 DFS，千层链不爆栈。

## 四、确定性

报告按任务 ID 字典序序列化 `id state cause err`；就绪队列弹出时取 ID 最小者；跳过原因取
字典序最小失败祖先。完成顺序的不确定性只影响到达顺序，不影响任何终态与输出字节。
