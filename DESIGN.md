# 严格模式流式 Base64 设计

## 规范形式的推导（语义 2）

Base64 把每 3 个字节编码为 4 个 6 比特组（sextet），共 24 比特，恰好无剩余。
带 `=` 填充时：

- 余 1 字节 → 2 个 sextet，提供 12 比特，输出只用 8 比特，**多 4 比特**；
- 余 2 字节 → 3 个 sextet，提供 18 比特，输出只用 16 比特，**多 2 比特**。

这些多出的比特不对应任何输出字节。若宽松地接受它们的任意取值，
同一个字节串就会有两个以上合法外观的编码（例如 `A` 既可写成 `QQ==`
也可写成 `QR==`），编码不再唯一，`Decode` 也不再是 `Encode` 的左逆，
`Encode(Decode(y)) == y` 无法成立。

因此严格模式的统一规则：**所有不参与输出的尾部比特必须为零**。
判定只用两处比特：

- `xx==`：第 2 个 sextet 的**低 4 比特**必须为 0。
  `QR==` 中 `R`=17=0b010001，低 4 位是 0001 ≠ 0，故拒绝。
- `xxx=`：第 3 个 sextet 的**低 2 比特**必须为 0。
  `QUJ=` 中 `J`=9=0b001001，低 2 位是 01 ≠ 0，故拒绝。

推论：接受该规则后，任意字节串的编码唯一；反之，凡被严格模式接受的
`y` 都满足 `Encode(Decode(y)) == y`（语义 6 后半句由此保证）。

配套规则：输入总长必须是 4 的倍数；`=` 只能以 `xx==` 或 `xxx=` 出现在
最后一个 4 字符组内；填充组之后只允许（MIME 模式下的）换行。

## 测试对照表

（实现完成后补齐）
