# 组提交设计

## 目标与边界

只做并发写请求的攒批、一次落盘同步、顺序结果返回。每个请求得到自己的全局序号或错误；不实现索引、查询和业务语义。

## 序号规则

请求到达时只进入待决批次，不分配序号。批次因条数、字节数或等待超时而封闭后，由 leader 在写盘前一次性预留连续序号。这样等待者在被唤醒前已经知道候选顺序，而失败时这些序号尚未对外可见。

若整批写文件或同步失败，leader 持锁回滚序号计数器到本批首序号，并向批内每个请求返回同一错误。因此第 2 批失败后，第 3 批首序号紧接第 1 批末序号，失败调用方不会观察到空洞。只有 CRC 完整的批次才能提交结果。

## 日志格式

文件创建时立即写入 8 字节文件头 `ONTWAL01`；零请求 Close 也保留只含文件头的文件，表示已初始化但没有批次。

每个批次先写 28 字节批头：4 字节 `BATC`、8 字节首序号、4 字节条数、8 字节载荷总字节、4 字节 CRC32。批头后逐条写 4 字节大端长度和载荷。CRC 覆盖批头前 24 字节和全部长度前缀条目，不使用条级 CRC。批次是读回复位边界：CRC 不符就丢弃整批，绝不丢弃其中单条。

## 攒批

三个触发条件先到者成批：达到 `MaxItems`；批内载荷总字节达到 `MaxBytes`；等待达到 `MaxWait`。时钟和定时器可注入。等待为 0 时首批立即封闭；批上限为 1 时退化为逐条提交；空载荷是合法请求。

单条载荷超过 `MaxBytes` 不拒绝。若批为空，它立即单独成批；若批中已有请求，先封闭原批，再让超大请求单独成批。因此限制的是普通攒批量，超大请求自身的实际批字节可以超过上限，历史普通批字节不越界。

## Leader 与唤醒

每个请求有自己的结果通道。第一个进入空批的协程成为 leader；后续协程只入队并阻塞在自己的通道。leader 收集整批、分配序号、预留序号、调用一次 WAL 写入和同步，然后按槽位逐条发送结果。请求引用与槽位一一绑定，唤醒结果不会串台。

Close 与提交互斥。Close 前进入队列的请求由最后一批落盘；Close 后到达的请求得到关闭错误。Close 等待 leader 完成，因此不存在返回成功但日志未同步的请求。

## 回放与故障分类

回放从文件头后顺序扫描，返回最后一个完整且 CRC 匹配的前缀。下一写入序号等于最后完整批次末序号加一；无批次时为 1。

截断点按当前扫描位置分类：文件头不足为头部不完整；批头不足为批头不完整；批头完整但任一条长度或载荷不足为条目不完整；所有条目完整但末尾 CRC 只有 1 至 3 字节，或 CRC 校验失败，为批 CRC 不匹配。前两类、条目不完整和 CRC 错误均有可 `errors.Is` 判定的哨兵错误。写盘中途崩溃等价于尾部不完整或 CRC 不匹配，整批不可见。
