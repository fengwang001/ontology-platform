# 可崩溃恢复外部排序 — 设计

## 1. 目标与边界

输入是并发到达的记录流 `Record{Key string, Value []byte, Seq uint64}`，记录数或总体积
可能超过给定内存预算。管线只负责：内存攒批 -> 排序溢写为 run -> K 路归并 ->
输出全局有序结果。所有状态、run、检查点都放在一个本地临时目录，只用标准库。

阶段固定为 `Ingest -> Spill -> Merge -> Finalize`，每个阶段边界都可崩溃并恢复。

## 2. 等键顺序：比较键推导

要求：相同 Key 的记录按到达顺序输出，且跨 run 成立。

一次溢写得到一个 run，run 内按 `(Key, 局部到达位置)` 排序。
局部位置只在该 run 内从 0 单调，不同 run 之间没有关系。若归并时用
`(Key, run内位置)` 比较，考虑同一 Key 的三条记录被切到三个 run：

- 全局到达顺序：A(run0, 局部 4)，B(run1, 局部 0)，C(run2, 局部 1)。
- 用局部位置比较会得到 B(0) < C(1) < A(4)，与真实到达顺序 A,B,C 相反。

原因是局部位置不携带“跨 run 的先后”，它只是批内下标。

正确做法是引入**全局单调到达序号** `Seq`：Ingest 入口在互斥区内给每条成功接收的
记录分配一个全局递增的 `Seq`，并随记录编进 run。这样无论记录落进哪个 run，
`Seq` 的相对大小都等于到达先后。全序比较键为

  `(Key 升序, Seq 升序)`

Key 不同按字典序；Key 相同 Seq 唯一且单调，因此顺序确定且等于到达顺序。
归并堆内每条光标只比较这个全序键，不需要 run 编号或局部下标。

## 3. 记录与 run 文件格式

记录按确定字节序编码，避免 `string/[]byte` 头部长度的平台差异：

  uint32 LE len(Key) | Key 字节 | uint32 LE len(Value) | Value 字节 | uint64 LE Seq

run 文件：

  Header: magic "ONTRUN01"(8) | uint64 LE Count
  随后每条记录一帧：uint32 LE len(rec) | rec | uint32 LE CRC32(IEEE, rec)
  零记录 run 在 Header 后写 `uint64 LE 0` 作为终止标记。

Header 自带 Count，因此“只有 Header(24B)”与“零记录 run(32B)”可区分：
前者 Header 不完整/无终止符，后者完整且 Count=0。

每帧独立携带长度与 CRC，所以崩溃留下的半截 run 可扫描出**最大可恢复前缀**：
完整帧全部保留，第一个不完整帧及之后全部丢弃。

## 4. 预算与溢写触发

`budget` 只记账当前内存驻留字节数 `resident` 与硬上限 `limit`：

- `Charge(n)`：若 `resident+n > limit` 返回超限错误（用于单条超大记录）。
- 正常攒批由管线判定：加入后若 `resident >= limit`，立即把整批移交给后台
  Spill；移交的同一把锁内从驻留账上扣除整批，新批次从 0 开始。

因此账上驻留严格 `< limit`（加上正在构造但尚未触发的一批的最后一条，达到
`limit` 时立刻溢写）；历史最大值由管线计数器记录，任何时刻都不越界。
20 万记录、预算为总量 1/50 时至少产生 40 个 run。

## 5. K 路归并与比较上界

用 container/heap，每个 run 一个当前光标，堆大小为 K。每条记录：
一次堆顶取出 + 一次尾部上浮替换，比较次数至多

  `2 * ceil(log2(K+1))`

N 条总计 `2*N*ceil(log2(K+1))`，实现上再按 N 条逐次累计，满足题目上界
`4*N*ceil(log2(K+1))`。朴素每轮线性扫描为 O(NK)，会超界，故不采用。

## 6. 并发

Ingest 持中心锁：分配全局 Seq、追加到内存批、记账；达到预算即把批放入容量为 1
的溢写通道并同步扣账。单个后台 goroutine 串行把批排序、写 run、rename 落盘，
再更新检查点。待处理批最多 1 个，加上锁内当前批，驻留仍不超过预算。
Close 在锁内标记关闭，之后 Ingest 返回 `ErrClosed`；Close 排空后台、溢写残余、
归并、Finalize。所有共享字段经锁或通道访问，满足 `-race`。

## 7. 检查点与恢复

run 先写 `*.tmp` 再原子 rename，rename 成功后才把该 run 追加到检查点。
检查点 `checkpoint.json` 同样用临时文件 rename 原子替换，记录：
阶段、已提交 run 列表、已接收记录数、全局 Seq 高水位、输出文件是否完成。
恢复时：

- run 文件按帧扫描，截断到最大可恢复前缀，并分类头部/长度/体/CRC 错误；
- 已提交 run 一定完整；未提交的 tmp 若含完整帧则抢救为 run；
- 从检查点阶段继续：未完成 Merge 则重跑（归并确定性，结果逐字节相同）。

崩溃点 Spill 后 / Merge 中 / Finalize 前，恢复后输出均与正常路径一致。
