# 设计推导

## 2. 代理对错误偏移

规则：把 `\uD800`–`\uDBFF` 视为一个**必须立即消费低代理的未完成词法单元**。

- 输入以低代理开头：低代理本身不能开始码点，偏移指向该 `\u` 的反斜杠。
- 高代理后不是完整、合法的 `\uDC00`–`\uDFFF`：偏移指向高代理的反斜杠。
- 因而 `\uD83Dx`、`\uD83DA`、高代理后 EOF、高代理后再遇高代理，都报在高代理起点。

理由：高代理不是独立码点；从看到它开始，解码器已承诺解析一个组合码点。错误根因是这个高代理未获得必需续体，而不是续体位置上的普通字节。偏移指向根因词法单元，可稳定定位且不受后续字节影响。

`\uD83DA` 不能变成“U+FFFD + A”：`\uXXXX` 精确消费且只消费四个十六进制位；`D83D` 已完整产生一个孤立高代理。RFC 8259 只允许代理成对出现，Go 的 UTF-8 字符串也不能把 UTF-16 代理当 Unicode 标量。替换为 U+FFFD 会静默篡改输入；剩余 `A` 只是下一个字面量字符，不能参与重新解释。

## 4. 最小转义与唯一性

JSON 字符串语法强制排除的只有未转义的 U+0000–U+001F、未转义引号和反斜杠。U+001F 是控制字符，原样出现会直接违反 RFC 8259；U+007F 虽有控制语义，但不在 JSON 的强制转义区间内，且是合法单字节 UTF-8，所以保持原样。`/` 没有前导反斜杠时无歧义；非 ASCII、U+2028/U+2029 也是合法 JSON 文本，均原样输出。

编码分支互不相交：`"` 与 `\` 各有短转义；`\b`、`\f`、`\n`、`\r`、`\t` 固定短形式；其余 U+0000–U+001F 固定小写 `\u00XX`；其他合法码点固定原样 UTF-8。每个码点只落入一个分支，解码器又能无歧义地区分原始字节与转义字节，所以编码结果唯一且 `Encode` 是单射。
