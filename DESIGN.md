# 设计推导：带背压与断点续传的分块流式写出管线

线上格式（自创，仿分块传输但不用任何库实现）：

```
<hex-size>[;k=v;k="v"][CRLF]
<payload>[CRLF]
```

结束块是 `0[CRLF][CRLF]`，即大小为 0 的块。依赖方向：
`sizeline`、`chunker`、`sink` 互不依赖；`resume` 依赖 `chunker`；
`pipeline` 依赖全部。所有时钟由外部注入，状态只在进程内存。

## 1. 为什么零长写入必须被吞掉

线上流里“大小为 0 的块”是**流结束标记**（结束块），下游解析器读到它就认为
整个传输已完成，后续字节应被忽略或视为协议错误。若上游一次 `Write(p []byte)`
其中 `len(p)==0` 就产生一个 0 大小块，下游会在流尚未结束时提前看到终止标记，
误判“传输完成”，真实数据与结束块将永远无法送达。因此零长写入在管线中必须是
纯空操作：不切块、不编码、不推进时钟窗口、不改变任何计数器。唯一允许写出 0
大小块的地方是 `Close`，且只写一次。

## 2. 第 3、5 条：断点状态必须记录什么，才能“不重不漏”

管线把**最终要写出的完整字节序列**组织成若干段（段 = 已编码块帧；结束块也是
一段）。设下游是“从位置 0 开始、严格按顺序消费字节”的流，则只需一个量即可
唯一定位：

- `confirmed`：已确认写出的绝对字节偏移（从 0 起）。

不变量 I1：`confirmed` 之前的每个字节下游都已收到；`confirmed` 起的每个字节
下游都还没确认。短写时 `confirmed += n`；背压 `n==0` 时不动；断开后用旧值
重建，仍从 `confirmed` 续写——既不重发（只发 `[confirmed,:]`），也不跳过
（从 `confirmed` 逐字节继续）。

为了在“任意字节位置”断开后能重建，快照还需携带：

- 已发出的块帧字节（按顺序保存，含帧边界元信息）；
- 当前进行中的段索引 `chunkIndex` 与段内偏移（由 `confirmed - frameStart`
  导出，即“块内偏移”）；
- `chunker` 的待聚合字节与首字节时间窗起点；
- `closed`（是否已进入收尾）。

因为段本身是幂等的静态字节串，重建后的管线从 `confirmed` 续写，与从不中断的
管线产出**同一字节流**。任意短写切分点只是让 `confirmed` 落在段内不同位置，
不改变最终序列；遍历所有切分点/断开点即枚举 `confirmed` 的所有取值。

## 3. 第 6 条：块序列为何只取决于字节流与时钟

切块策略的输入是“一条随时间到达的字节流”，不是“一串 `Write` 调用”。
`Write` 的调用边界是上游的偶然行为（一次发整包 vs 分多次发），若切块按调用
边界进行，同一份数据会因调用方式不同而产生不同块序列。这会直接破坏第 3 条的
断言——“最终字节序列与一次性写出逐字节相同”：块头里的长度行不同，整体字节
序列就不同。

因此 `chunker` 只维护：内部连续缓冲、缓冲首字节到达时刻（注入时钟给出）、
`min/max` 与时间窗。规则：

- 缓冲达到 `max`：按 `max` 切块（连续的大输入天然切成等长块，与调用边界无关）；
- 否则缓冲 `>= min`：立即成块；
- `0 < 缓冲 < min` 且距首字节超过时间窗：超时成块；
- `Close` 强制把残余成块（残余为 0 则不产块）。

时间也由参数显式注入，所以切块结果是 `(字节流, 到达时刻序列)` 的纯函数。

## 4. 第 8 条：缓冲超限为何报背压

下游慢时，数据必须停在管线里等下一次推进。若直接丢弃，第 4、5 条的“不丢”
被破坏；若当作致命错误关闭流，则下游暂时不可写本是可恢复状态，不该终止传输。
因此待写缓冲达到上限时，上游 `Write` 返回与“下游暂时不可写”同类的可判定
背压错误（`ErrBackpressure`），**调用前先完整校验、不接纳任何字节**，缓冲
状态零变化；上游稍后重试即可。三类上限——单次写入字节上限、单块扩展编码后
总长上限、待写缓冲上限——分别返回 `ErrWriteTooLarge`、`ErrExtTooLarge`、
`ErrBackpressure`，彼此可用 `errors.Is` 判定。

## 5. 第 11 条：缓冲结构与扫描复杂度

待写缓冲不是单一连续数组，而是**段链表**：每段是一块已编码帧（`[]byte`），
另存 `segIndex`（当前段）与 `segOff`（段内已确认偏移）。推进时：

1. 只取当前段的 `buf[segOff:]` 交下游写；
2. 短写后只移动 `segOff += n`；整段确认完才前进到下一链表节点。

单次推进检查/触碰的字节数只是“当前段剩余部分”，与历史已确认段和未来段都无关，
故为 O(本段长度)，**不随缓冲总字节 N 线性增长**。非导出计数器
`lastScan` 记录每次推进实际扫描的字节数；N=1KB 与 N=1MB、每次只写少量时，
`lastScan` 基本相同（只反映那一小口），以此证明。

## 关键不变量汇总

- I1（不重不漏）：见第 2 节。
- I2：零长写入不产生块；结束块仅由 `Close` 产生且仅一次，重复 Close 幂等。
- I3：`accepted == confirmed + pending`。三者均按“线上输出字节”计数：
  接纳一个块帧时 `accepted += 帧长`，确认时 `confirmed += n`，
  `pending = accepted - confirmed`。所有状态变更在同一把锁内完成，查询只读
  快照、不推进，故任意时刻连查两次一致，并发下恒等式恒成立。
