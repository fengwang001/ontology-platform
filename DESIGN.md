# DESIGN：有预算的图遍历器

范围：纯内存、标准库、BFS；预算以「访问节点数」计量；截断确定且可续传。

## 1. 预算的计量单位：为什么是节点数而不是边数

若预算以「展开的边数」计量，截断点会随图的存储形态漂移：

- 同一逻辑图，邻接表是否保留重复边、是否去重，出度就不同；同样预算下
  考察的边数相同，但触达的节点集合不同。
- 同一逻辑图，边拆分粒度不同（平行边、自环、冗余边），节点访问序列
  随之改变，截断位置不再是图的函数，而是存储布局的函数。

以「访问的节点数」计量时，预算是图的同构不变量：任何保持节点与可达性
不变的存储变换都不改变第 k 个被访问的节点。因此本题规定：预算 = 访问
（首次标记）的节点数，访问序列 = 标记顺序。访问数精确等于
min(预算, 可达节点数)。

## 2. 截断确定性：必须固定的三处顺序

1. 出边顺序：邻接表按目标 ID 字典序固定（graph 包插入时排序去重），
   遍历时绝不迭代 map。
2. 同层入队顺序：严格 FIFO；一个节点的出边按第 1 条顺序逐个考察，
   子节点按考察顺序入队，不允许乱序批量入队。
3. 计量与标记时机：节点首次被考察到时即标记 seen 并计入预算
   （mark-on-enqueue），同一节点不会重复入队、重复计数。

三处固定后，遍历是 (图, 起点, 预算) 的纯函数：同输入必同输出，
重复任意多次截在同一处。

## 3. 续点必须包含什么：推导

BFS 在任意截断时刻的状态不是「最后一个节点」，而是：

- frontier 队列（有序）：同层剩余节点与更深层已入队节点都在其中，
  只记最后节点会丢失同层兄弟，续传顺序即被破坏；
- 队首帧的出边游标：预算可能在展开某节点出边的中途耗尽，必须记录
  「下一条待考察出边的下标」，否则续传会重复或漏考察边。不变式：
  只有队首帧游标可能非零（展开总是从队首做到尽），故全队列只需
  一个游标值；
- seen 集合：已访问节点集合，用于去重（环、自环、重复边、菱形汇合
  都靠它防止二次访问），序列化为排序后的 ID 列表。

续点 = (done 标志, 队首游标, 队列 ID 序列, 排序 seen 列表)。
队列为空 当且仅当 done=1；对 done 续点再续传返回空序列而非报错。
预算为零时不消耗任何状态，返回的续点与输入续点逐字节相等（幂等）。

## 4. 续点编码与篡改分类（resume 包）

二进制布局：magic(2) | flags(1) | Q(4) | S(4) | 队首游标(4) | crc32(4)，
后接 Q 个队列项与 S 个 seen 项，每项为 idLen(1) | id字节。
CRC32 只覆盖头部 15 字节（含游标与计数），ID 负载不覆盖——这样
三类解码错误都能被真实触发且可用 errors.Is 区分：

- ErrChecksum：magic/flags/计数/游标/crc 字段被改（头部完整性失败）；
- ErrIncomplete：长度字节错乱导致结构解析越界/剩余/不一致
  （含 seen 未严格排序、Q=0 而 done 未置位等结构矛盾）；
- ErrUnknownNode：结构完好但引用了图中不存在的节点
  （ID 字节被改、或两段续传之间图被删节点）。

测试图的节点 ID 两两比特汉明距离 >= 2，故单比特翻转 ID 字节必然落到
「图中不存在的节点」，不会被误接受。解码时同时校验：队列项互不相同、
队首之外的所有队列节点必须已在 seen 中。

## 5. 复杂度与资源

- 访问节点数 == min(预算, 可达数)：循环只以「seq 满预算」或「队列空」退出。
- 峰值队列：出边逐条考察、子节点逐个入队，星形图（1 中心 + 1 万叶子）
  预算 b 的峰值 <= b+1（队首帧 + 已入队叶子），测试断言 <= 4*b。
- 每条边最多考察一次：每节点至多入队一次、队首游标单调递增，
  考察边数 <= 已访问节点出度之和。
- 遍历为纯函数：状态全部在栈上的 walker 实例内（计数器非导出），
  图只读，可并发续传同一续点而互不串台。
