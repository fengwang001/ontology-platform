# 快照读可见性判定器设计

## 核心推导：为什么只看提交时刻不够

版本由事务 T 写入。读快照 S 在创建时记录水位 W（已分配提交号的上界）
与活跃集 A（快照创建那一刻仍在进行中的事务集合）。

朴素规则：T 的提交号 < W 即可见。

反例构造：T1 在快照创建时仍在进行中，故 T1 ∈ A；之后 T1 提交，
恰好拿到提交号 5，而快照水位 W = 10。按朴素规则 5 < 10 判可见，
于是读者看到了一个在它取快照那一刻尚未提交的版本，违反快照语义。

正确规则：**提交号 < W 且 T ∉ A**。
补充：T 为快照自身事务时，自己写的版本对自己恒可见。

## 复杂度

单次判定 = 1 次事务表查找 + 至多 1 次活跃集查找，常数次，不遍历事务表。

## 三类可判定错误（errors.Is 可区分）

- `visible.ErrUnknownTxn`：未知事务号
- `visible.ErrSnapshotReleased`：快照已释放后继续使用
- `visible.ErrCorruptChain`：版本链中提交号非递增（损坏）

## 资源上限

活跃集大小超过 `snapshot.MaxActive` 时构造快照返回
`snapshot.ErrTooManyActive`；快照占用的内存项数等于活跃集大小，
不随事务总数增长。
