# 结构化日志脱敏与采样器设计

## 1. 范围与包划分
仅标准库，状态在进程内存。包：`record`（记录与编解码、深度/环检测）、`rule`（规则与编译）、
`mask`（替换/哈希/截断）、`sample`（按追踪链一致性采样）、`sink`（长度前缀+CRC32 落盘与损坏分类）、
`cmd/demo`（可运行判定）。字段值用 Go 基础类型：map/slice/string/数字/bool/nil。

## 2. 一致性采样推导
同一 trace 的多条记录必须同留同丢。若对每条记录独立掷骰（含时间、序号、map 迭代序等），
同一链上决策会分裂，链路断裂。故决策函数只能是 `f(traceID)` 的纯函数：
对 traceID 做一次确定性 64 位 FNV-1a 哈希，`h % denom < numer` 则保留；rate 0 全丢、1 全留。
决策与记录内容、到达顺序、时间无关 → 同链必然一致；同一 ID 重复决策结果相同；单次决策恰好 1 次哈希（O(1)）。
无 traceID（字段缺失）与空串等价：哈希空字节串，仍得到确定决策（不强制保留、也不报错），文档固定此策略。

## 3. 错误级强制保留与「链不完整」标记
level >= error 的记录无条件保留。若其所在链在采样意义上本应被丢弃（`f(traceID)=丢`），
该记录打标记 `incomplete=true`：链上普通记录已丢、只剩错误，链不完整。
链本就保留时的 error 记录不打标记（链完整）。即标记 ⇔ 采样判定丢 ∧ 级别强制保留。

## 4. 嵌套路径与点号歧义（消歧规则）
路径 `a.b.c` 自左向右逐层进入映射。规则：
1. 先按嵌套解析：依次取子键 `a`、`b`、`c`，要求中间各层为映射；中途某层不是映射（如字符串/数组/数字）
   → 该路径在该点判定「不匹配」，不做类型强转、不继续深入。
2. 路径缺失（任一键不存在）→ 不匹配。
3. 点号歧义：字段名本身可含点号（键字面量 `"a.b"`）。**嵌套优先，字面量兜底**：
   在某层用下一段键名取不到映射时，把「剩余全部段用点号拼回」作为单个字面键名再取一次。
   即 `a.b.c` 可命中字面键 `"b.c"`（在映射 `a` 下）或顶层字面键 `"a.b.c"`。
   嵌套键与字面键同时存在时嵌套胜出。
数组不按键名索引；数组中的映射元素仍对其内部路径应用规则（遍历时前缀路径不变）。

## 5. 脱敏语义与彻底性
替换：固定串 `***`，不保留长度。哈希：SHA-256 十六进制摘要（同值同果、异值异果、不可逆、不等于原值）。
截断：保留前 N 个 rune 并追加 `…(truncated)` 标注。执行器对整棵树做一次遍历，
凡路径命中规则即改值；同一敏感值出现在多路径、数组元素、被引用处都会被各自路径规则覆盖；
基于值模式（正则）的规则对每个字符串值尝试匹配。非目标字段不动，未命中部分序列化后逐字节不变。

## 6. 编译与复杂度
规则集预编译为路径 trie：每个映射节点只需按其实际键名沿 trie 下移，
路径匹配次数 ≤ 记录数字段数 + 常数（每条记录一次根查询），与规则总数无关，
即 O(记录数 × 字段数) 而非 O(记录数 × 规则数 × 字段数)。计数器 `matchCount` 记录下移次数，导出只读方法供测试断言。
冲突：同一路径（按规范化路径串）配置两种不同脱敏方式 → 编译期返回 `ErrConflict`，错误文本列出冲突两条规则。

## 7. 记录校验
`New` 构造时做深度优先遍历：深度（根映射为 1，嵌套映射/数组逐层 +1）超过 `MaxDepth=32` → `ErrTooDeep` 并计数，拒绝；
用访问序号标记 map 指针，第二次进入同一映射 → `ErrCycle`，拒绝（不死循环）。两类错误与规则冲突、文件损坏均可 `errors.Is` 区分。

## 8. 落盘格式与损坏分类
文件 = 固定头 `"ONTOLOGY-LOG v1\n"`（自描述、魔数+版本）+ 若干帧。
帧 = 4 字节大端长度 N + N 字节记录体（JSON：trace/level/fields/incomplete）+ 4 字节大端 CRC32(IEEE)。
截断分类（逐字节截断点遍历）：
- 头部不完整 `ErrHeader`：文件短于固定头或魔数不匹配；
- 长度前缀不完整 `ErrLenPrefix`：剩余字节 < 4；
- 记录体不完整 `ErrBody`：剩余字节 < 4+N；
- CRC 不匹配 `ErrCRC`：帧读全但 CRC 校验失败（CRC 尾部被截也归此类）。
遇损坏帧即停，已完整且校验通过的帧构成最大可恢复前缀，逐条返回记录数。

## 9. 并发
规则集、采样器均无写后变状态（计数器用原子/互斥保护），同批记录并发处理的输出集合与串行逐元素一致。
