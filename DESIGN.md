# 基于 LSM 思路的有序键值合并读取器 — 设计

仅标准库。段文件落本地目录。不做 memtable / WAL / 压缩。

## 1. 段文件格式（自描述）

```
+---------------- Header (33B) ----------------+
| magic "LSEG" (4) | ver uint16 (2) | flags u8 |
| level u32 | seq u64 | numEntries u64         |
| indexOffset u64 | indexLength u64           |  (大端)
+---------------- Data Frames ------------------+
| frame* : 每条一个定界帧                      |
+---------------- Sparse Index -----------------+
| indexEntry* : {key []byte, offset u64}       |
| 每 8 条记录一个锚点（首条必入索引）          |
+---------------- Index Trailer (8B) -----------+
| indexLen u64 大端 + magic "LIDX"(4)          |
+-----------------------------------------------+
```

Data frame 自定界：`frameLen u32 | keyLen uvarint | valueLen uvarint | del u8 |
key | value | crc32 u32`。`frameLen` 覆盖从 keyLen 到 crc（含）。`del=1` 且
valueLen=0 表示删除标记；`del=0,valueLen=0` 是合法空值，二者靠 del 区分。
crc32(IEEE) 覆盖 keyLen..value 的全部字节。

点查：先在稀疏索引上二分找最后一个 `anchorKey <= k` 的锚点，从其 offset 起顺序
读到 key>k。索引比较 + 数据比较都计入「比较过的条目数」。锚点间隔 8，数据段
最多顺序看 8 条；上界 `ceil(log2(N/8+1)) + 8 < 4*ceil(log2 N)`。

## 2. 版本胜出规则（推导）

写入次序 = 新到旧：先入 memtable 的更旧，合并产生新段。段用 (level, seq) 标识。
规则：**层号更小更新；同层仅 L0 可有重叠段，seq 更大更新**。因此对键 k，
点查按优先级 L0(seq 降序) → L1(seq 降序) → L2… 取第一个含 k 的条目即可。

点查三种返回状态互斥：`(value, Present)` / `Deleted` / `NotFound`。
删除标记是「显式写过但已删」，必须遮蔽更旧段的值，不能回退到旧值，也不同于
从未写过（找不到任何条目）。

## 3. 删除标记何时能丢弃（核心推导）

合并输入是若干段的集合，输出层 Lout。一个删除标记遮蔽的是「所有比该条目更旧的
位置上同键的值」。一个位置更旧当且仅当 level 更大，或 level 相同(L0)时 seq 更小。
合并只读取参与合并的段；合并后，输入段被删除，新段位于 Lout，而未参与合并的
更旧层（level > Lout，或 Lout==0 时未被选中的旧 L0 段）可能仍含同键旧值。

故丢弃 tombstone 的充要条件：**不存在任何比输出条目更旧、又未被本次合并消除的
位置**。实现判定：参与合并的段覆盖了 0..maxLevel 的每一层（每层至少一段）。
此时所有可能更旧值都已参与合并、loser 被丢弃，tombstone 是全库最旧可见状态，
可丢。反例：只合并 L0、L1，L2 仍有 k 的值——丢掉 tombstone 后 L2 的值复活，
故必须保留（测试构造此例）。覆盖全部层级时丢弃，文件变小。

## 4. 多路归并（堆）

堆元素为各源当前头条目，键取最小；键相等时按 (level, seq) 取「最新」者（level
小优先，同层 seq 大优先），其余同键条目不吐出。每吐出一条：1 次取顶 + 每补入一
个候选最多 log S 次比较，故每吐出条目 `O(log(S+1))`。同键全版本极端情形：该键
每源 1 条仍满足；若单源含同键多版本（单段内同键多版本仅 L0 顺序读取时可能相邻，
单段排序保证同键相邻），归并循环对该键额外推进计入常数。计数器由 merge iterator
持有，断言 `cmp <= 4*M*ceil(log2(S+1))`（另加建堆与同键推进的常数余量）。

## 5. Compaction 与内存上限

流式：每源一个迭代器，任何时刻每源至多驻留 1 条（已弹出未写入的胜出条加 1），
计数器断言同时驻留条目数 `<= S+1`，并受显式 cap 校验（cap<S 直接报错）。
loser（被覆盖旧版本、被遮蔽删除键）读到即丢，不写入新段。

## 6. 崩溃恢复与并发无空窗

合并写临时文件 `seg-*.tmp`：刷盘并 Open 校验通过后，`rename` 成正式新段，**然后**
才删旧段。顺序推导：读取方枚举目录时只接受正式名。① 新段 rename 前：旧段都在；
② rename 后、删旧前：新旧同在，新段更新，点查取新版本仍正确；③ 删旧后：只剩新
段。任何时刻键都可解析，杜绝「旧已删、新未就位」。`.tmp` 在打开/恢复时识别并
删除（半截新段清理），旧段从未在新段就位前被动过，故恢复后读取与合并前逐键相同。

## 7. 损坏分类

`Classify(path)` 对截断/损坏给出可判定错误：
- ErrHeader（头部不完整/魔数错）：长度 < headerSize
- ErrIndex（索引段不完整）：定位到 indexOffset 后尾部 trailer 魔数或长度不符
- ErrEntry（条目不完整）：frameLen 声明超出文件 / 帧内字段截断
- ErrCRC（CRC 不匹配）：帧完整读出但 crc 不符（纯尾部截断产生前三种；CRC 类由
  字节翻转注入，因截断无法产生完整帧）

截断段用 `RecoverPrefix` 流式读出最大可恢复前缀：逐帧解析，第一帧不完整或 CRC
错即止，半截条目绝不计入。

## 8. 层级不变量

L0 段键范围允许重叠；L>=1 各段非重叠（按最小键排序后相邻段不相交）。verify 报告
重叠的两段路径与重叠键区间 `[max(lo), min(hi)]`。

## 9. 边界语义

空段（0 条，仅头+索引尾）、单条目、全同键多版本、空键 ""、空值 []（区别于删除）、
范围扫描 `[start, limit)` 左闭右开、start>limit 返回 ErrReversed、区间与所有段不
相交得空结果。
