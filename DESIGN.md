# 只追加日志：段索引与范围回放 — 设计

## 1. 磁盘格式（全部大端）

段文件 `seg-%012d.log`（名字含首序号，保证按文件名排序即按序号排序）：

- 头 22 字节：魔数 4 + 版本 1 + 首序号 8 + 事件数 4 + 头 CRC32 4。
  头 CRC 覆盖前 17 字节。追加时以 `O_SYNC` 风格（写后 Sync）落盘，
  事件数在每次追加后原地改写并 fsync，因此它是「已提交前缀」的声明。
- 事件记录：`len(4) | seq(8) | payload(len) | crc(4)`。`len = 8+len(p)+4`，
  CRC32(IEEE) 覆盖 `seq+payload`。长度前缀自带边界，空 payload 合法。

索引文件 `seg-%012d.idx`（与段同目录、同首序号）：

- 头：魔数 4 + 版本 1 + 首序号 8 + 步长 N(4) + 锚点数 4 + 头 CRC(4)=25B。
- 锚点数组，每个 12B：序号 8 + 段内字节偏移 4（不含 22B 段头）。
- 每个锚点一条 CRC32(4)，使索引自身损坏可检；文件尾追加 CRC。

索引不包含任何只在写入时可知的信息：锚点序号与偏移都由「从头顺序读段」
唯一确定（每隔 N 个完整记录落一个锚点，第一条记录落 0 号锚点），
因此删除索引后重建结果与原文件逐字节相同。

## 2. 稀疏定位：为什么是「不大于 from 的最大锚点」

锚点保证的性质只有：**锚点处存在一条完整、已校验的记录，且从锚点顺序
扫描会经过其后所有记录**。它不保证锚点之后的偏移是什么。

- 若选「最接近 from 的锚点」而它落在 from 之后，从该偏移扫描只能看到
  更大的序号，from..锚点-1 这些事件永远丢失。
- 「不大于 from 的最大锚点」一定 <= from，从它顺序扫描只会越过（跳过）
  若干严格小于 from 的事件，区间起点不可能被漏。
- 跳过事件数上界：锚点步长 N，相邻锚点序号差 N，故最多跳过 N-1 < N。

三种 from 位置：等于锚点（跳过 0，用索引）；两锚点之间（跳过 d，
1<=d<N，用索引）；小于首锚点（首锚点即段首序号、跳过 0；在多段日志中
则落到更早的段）。

## 3. 跨段与边界语义

段头声明 `firstSeq` 与 `count`，合法日志满足
`seg[i].firstSeq + seg[i].count == seg[i+1].firstSeq`，违反即序号缺口。
回放按文件名排序顺序推进，段内按序号扫描，故跨段不重不漏。

- 空日志：合法；任意范围回放返回空结果（from>to 除外）。
- `from > to`：判定错误 `ErrInvalidRange`。
- `from < 最小序号`：夹取到最小序号，从头回放，不报错（前缀语义）。
- `to > 最大序号`：夹取到末尾，回放到最后，不报错。

## 4. 损坏分类与恢复

读段时按当前读到的字节位置分类，四类哨兵错误均可 `errors.Is`：
`ErrHeaderIncomplete`（不足 22B 或头 CRC 坏）、`ErrLenIncomplete`
（长度前缀不足 4B）、`ErrBodyIncomplete`（按 len 读体不足）、
`ErrCRCMismatch`（体完整但 CRC 不符）。

关键事实：**纯截断不可能产生 CRC 不匹配**——尾记录的 CRC 缺字节属于
「体不完整」；CRC 错只可能是原地改写（如被篡改/介质错误）。故故障注入
中 CRC 类用「翻转一个数据字节」构造，截断类逐字节遍历。

恢复策略：停在第一个坏记录之前，得到最大可恢复前缀；`repair` 把段头
count 改正为实际完整可读条数、截断文件到合法边界，并重建索引。

## 5. 索引失效与回退

索引偏移可能指向记录中间。定位时从锚点偏移尝试解码：若解不出合法长度
前缀或 CRC 不符，标记 `IndexStale=true` 并回退到从段首全段扫描，仍给出
正确结果。正常定位的读取字节上界：
`区间事件编码长度之和 + 一个锚点区间的字节数`（跳过的记录 + 定位记录）。

## 6. 并发

`Log` 用 `sync.RWMutex` 串行化追加、共享回放读；每个段单写者。
回放只读已 Sync 的前缀，记录以长度前缀+CRC 自界定，因此要么读到完整
记录，要么停在文件尾，绝不会读到半条。压测并发追加 + 回放循环，
要求每次回放都是连续无缺口前缀，`-race` 干净。
