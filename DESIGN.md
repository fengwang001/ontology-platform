# DESIGN：批量写入组提交与顺序保证器

## 1. 包划分（标准库）
`req`（请求/结果/序号）、`wal`（落盘与读回）、`batcher`（攒批）、`commit`
（组提交编排）、`recover`（回放）、`cmd/demo`（验收演示）。

## 2. 序号分配时机与回滚
候选：(a) 请求到达即分配——落盘失败会留下空洞；(b) 落盘成功后才分配——
调用方拿不到确定顺序。采用 (c)：**批次组装完成、调用 WriteBatch 之前，由
leader 一次性分配连续序号 [base, base+n)**。若写入或同步失败，整批序号
作废：计数器回退到 base-1，批内全部请求收到错误。故第 2 批失败后第 3 批
序号紧接第 1 批。

## 3. 批次原子性（批级 CRC）
文件：8B magic `GRPWAL01`；之后每批一帧：
`4B 本批条目数 | 4B 起始序号 | 逐条目(4B长度+载荷) | 4B IEEE CRC32`，
CRC 覆盖该帧除末尾 CRC 外全部字节。帧无外层长度，读取顺序即提交顺序。
CRC 是批级而非条级：条目 3 损坏 → CRC 不符 → 整批 5 条都不可见，绝不小
修式丢弃单条。回放从文件头顺序解析，遇到首个不完整/CRC 错帧即停止，已完
整解析的前序批次即为恢复集，序号天然连续无空洞。

截断点分类（四类可判定错误）：
`ErrHeader`（magic 头未满 8B）、`ErrBatchHeader`（批头未满 12B）、
`ErrEntry`（某条目 4B 长度或载荷不完整）、`ErrCRC`（帧完整但 CRC 不符，
含尾部 CRC 被截断/改写）。

## 4. 单条超大与边界
单条字节数超过批字节上限时**不拒绝**：当前批先按触发条件封批落盘，该超大
条单独成批（仅此批允许超过字节上限），保证不拆分、不静默截断。空载荷合法。
零请求 Close：产生仅含 8B magic 头的文件（不产生任何空批次），回放得 0 批。

## 5. leader 选择与唤醒不串台
`batcher`：首个进入空组的 goroutine 当选 leader 并负责落盘；后来者成为
follower，把自己的请求挂到批次并等待 leader 完成通知。leader 触发条件：
条数达上限 / 字节达上限（含超大单条立即单独成批）/ 等待时长到（注入
Clock）。leader 落盘后按条目逐个向各自 channel 投递**自己那条**的
Result{Seq, Err}，Channel 容量 1 且无接收者也不阻塞；调用方凭各自的
pending channel 取回，不存在共享槽位，故不串台。

## 6. 资源约束
每批恰好一次 Write + 一次 Sync；10000 请求、批上限 100 ⇒ 同步 ≤200
（非导出计数 `syncCount`）。落盘中途失败用写前偏移截断回滚文件；同步失败
同样截断并回滚序号。

## 7. 关闭与故障
Close 与在途请求竞争：请求要么进入最后一批落盘成功，要么收到
ErrClosed；绝不“返回成功但日志中没有”。故障注入：faultFile 可令第 k 次
Sync 返回错误；半截 Write 可直接用坏文件模拟崩溃。
