# 设计推导

## 第 3 条：续行只含空白时会怎样

依据 `Properties.load0`：它逐字符处理。行尾反斜杠若被判定为转义行终止符，则跳过换行并令 `precedingBackslash=false`，随后进入 `isWhiteSpace` 分支——该分支会**循环跳过所有空白直到下一个非空白字符或换行**。因此下一物理行开头空白被剥掉的规则，本质是“续行后自动开启的跳空白循环”。

若下一物理行只含空白（或为空行），跳空白循环会一直吃到该行末尾的换行；该换行前面没有反斜杠（空白不改变反斜杠奇偶），所以它是**未转义的行终止符**，当前逻辑行在此结束，空白续行不贡献任何字符。结论：

- `k=a\` + 换行 + 空行（或 `   `）+ 换行 + `b` → `k`→`a`，且 `b` 是另一条新逻辑行（`b`→`""`）。
- 它正是“续行首部空白被剥掉”的极端情形：整行都是被剥掉的首部空白，剥完即遇终止换行。
- 例外：空白行若以奇数个尾部反斜杠结束（如 `  \`），其换行被转义，逻辑行继续；剥光空白后连到再下一行。

## 第 6 条：Store 最小转义推导

解析视角决定转义需求：键在遇到第一个**未转义**的空格/制表/`=`/`:` 时结束；值从第一个非空白字符开始；仅逻辑行首字符为 `#`/`!` 时整行注释；反斜杠总是引入转义。

- 反斜杠：任何位置都必须转义为 `\\`，否则会吞掉下一字符或改变续行。
- 换行/回车：必须转义为 `\n`/`\r`，否则一个属性被拆成两行。
- 键中的空格与制表：转义为 `\ `/`\t`，否则被当作键终止符；`=`/`:` 同理转义为 `\=`/`\:`。
- 以 `#`/`!` 开头的**键**：该字符位于逻辑行行首，必须转义（`\#`），否则整行变注释。值开头不是行首（前面有键和 `=`），故值以 `#`/`!` 开头无需转义。
- 值开头的空格：键与分隔符之后解析器会吃掉一段空白，故必须转义为 `\ ` 才能保留；值**中间**的空格不在“键后跳空白”区间内，原样保留即可。制表同理转义首部。
- 非 ASCII 原样 UTF-8 输出，不写 `\uXXXX`；空键/空值仍输出 `=` 以固定分隔位置。

## 七条语义的测试对照

<!-- TABLE -->
