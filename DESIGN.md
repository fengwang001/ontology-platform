# 设计推导

## 1 非法单元切分规则（由七个样例推导）

规则（与样例完全一致）：解码以"首字节"为锚点推进。首字节声明长度 L(2..4)，
其后最多看 L-1 个后续字节：
- 续字节必须在 80..BF；否则首字节锚点在第一个不满足条件的字节之前结算，
  吞掉的字节数 = 已拿到的连续合法续字节数 + 1，该字节成为下一个新锚点。
- 受限首字节 E0/ED/F0/F4 的第二字节区间不同；第二字节非法但仍是普通续字节
  (80..BF) 时，锚点立即结算且只吞 1，并进入"续字节残尾吞吃"态：连续吞掉所有
  80..BF（只记一个 FFFD），直到遇到非续字节，该字节重新当锚点。这解释了
  `E0 80 80`→三？不：E0 吞 1 后，80、80 都被残尾吞吃只记 1，与样例"三个"矛盾。
  故残尾规则仅适用于样例 `F0 90 80 41`：F0 后 90、80 被并入同一残尾、遇 41 停止
  （41 重解析），整段一个 FFFD；而 `E0 80 80`、`ED A0 80`、`F4 90 80 80` 要求
  每个字节都独立成单元。统一区分：第二字节违规后，**其后紧跟的续字节，若构成
  某合法首字节声明序列则独立重解析，否则整体并入残尾**——90/80 不能作任何合法
  序列首字节，故在 F0 90 80 41 中并入；而 E0 80 80 中，样例规定按 WHATWG 语义：
  E0+80（非最短）吞 1，后续每个字节各成单元。

最终统一规则（逐条核对七样例）：
- 首字节非法（C0/C1/F5..FF）→ 1 个单元，吞 1；孤立续字节 80..BF → 1 个，吞 1。
- 普通首字节（C2..DF/E1..EC/EE..EF/F1..F3）：第二字节非法 → 锚点吞 1，
  该字节重解析（重解析规则同上，故 80 各自成单元）。
- 受限首字节（E0/ED/F0/F4）第二字节非法 → 锚点吞 1 成一单元；**此后处于残尾态，
  连续吞掉所有 80..BF（每个仍按重解析独立成单元），直到非续字节重解析**。
  E0 80 80：E0(1)+80(1)+80(1)=三；F4 90 80 80=四；F0 90 80 41：F0(1)+90、80
  本应各一，但样例要求只有一个 FFFD——故残尾态下这些续字节不独立计数，只吞不记。

七样例对受限 4 字节首字节（F0/F4）给出关键区分，统一规则：
- 第二字节违规（仍是 80..BF）：首字节与该第二字节先进入"待决残尾"，继续吞入
  连续续字节直到声明窗口结束或被非续字节截断。
- 若被**非续字节提前截断**（或 EOF 提前结束）：首字节 + 这些续字节整体**一个**
  非法单元（F0 90 80 41：F0/90/80 一个单元，41 重解析 ✓）。
- 若续字节**恰好填满声明窗口**：首字节单独一个非法单元，窗口内每个续字节按
  孤立续字节重解析、各自一个单元（F4 90 80 80 → 1+3=四 ✓；E0/ED 声明窗口为
  3，违规第二字节 + 1 个续字节即填满，故 E0 80 80、ED A0 80 均为三个 ✓）。
其余：非法首字节 C0/C1/F5..FF 自身一单元；孤立续字节 80..BF 自身一单元
（C0 AF→二、80 80→二 ✓）；普通首字节第二字节非续字节时锚点吞 1 后重解析；
E2 82+EOF 是"合法前缀+已验证续字节"的 EOF 残尾，整体一个单元 ✓。

## 2 第二字节合法区间与吞字节数

| 首字节 | 第二字节合法区间 | 非法时锚点吞 |
|---|---|---|
| C2..DF | 80..BF | 1（该字节重解析） |
| E0 | A0..BF | 1，闭合重解析 |
| E1..EC | 80..BF | 1，重解析 |
| ED | 80..9F | 1，闭合重解析 |
| EE..EF | 80..BF | 1，重解析 |
| F0 | 90..BF | 提前截断：首+续共一单元；填满窗口：各自成单元 |
| F1..F3 | 80..BF | 1，重解析 |
| F4 | 80..8F | 提前截断：首+续共一单元；填满窗口：各自成单元 |
| 80..BF | 无首字节，孤立续字节 | 自身 1 |
| C0 C1 F5..FF | 永不合法 | 自身 1 |

第三、四字节仅需 80..BF；非续字节令锚点结算（吞已验证连续续字节+1）。
EOF 残尾（首字节+k 个已验证续字节，k≥1）整体一个非法单元：E2 82 → 吞 2 记 1。
缓存上限 3 字节（4 字节序列后三字节）。可立即判定、不必等 EOF 的时机：非法首字节；
受限第二字节违规；普通锚点遇到非续字节；孤立续字节。只有"合法前缀+已验证续字节"
需等待，且等待期间每个字节只被检查一次（增量状态机，不回扫）。

## 3 par 切点对齐

切点 c 任意。段 0 从 0 正常解析，Close 时不结算 EOF 残尾（残尾必被段 1 覆盖；
K=1 例外）。段 i（i>0）把"上一段末尾 3 字节 + 从 c 开始的字节"喂给一个
SkipPrefix 转码器：前 3 字节驱动状态但不输出、不统计；每个解析出的单元只有
起始绝对偏移 ≥3 才被输出与计数。单元最长 4 字节，3 字节回看必包含完整锚点，
每个单元恰好被唯一一个"其起点落在自己半开区间内"的段认领。例：段尾 C3、切点、
下段 80 80：段 0 不结算；段 1 喂 C3 80 80，C3+80 成合法标量（段1 认领），第二个
80 是孤立续字节，与单线程逐单元一致。检查次数：每字节在本段查一次，仅 3(K-1)
个回看字节被查第二次，故 ≤ N + 3(K-1)（满足 N+8×常数）。

## 4 上限断点

解析出的完整单元先进就绪队列，再按目标编码长度写入输出；放不下则保留在队列并
立即返回 ErrOutputLimit。Write 返回的 n = 本次新结算（标量完成/非法单元闭合）的
输入字节增量；3 字节缓存内未结算的前缀不算已消费。同一实例随后继续 Write
（后续字节或 nil）即在标量边界续出；Resume() 返回当前缓存残尾，切到新实例时
append(Resume(), 未传部分...) 续传，两种方式拼出的字节都与不限上限逐字节相同。
