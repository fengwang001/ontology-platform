# 严格模式流式 Base64：设计说明

## 第 2 条「规范形式」的推导

编码方向是确定的：3 字节 → 4 个 6 比特字符。当输入不足 3 字节时，
编码器把最后一个不完整的 6 比特字符的高位填上真实数据、**低位补 0**：

- 剩 1 字节（8 bit）：c0 取高 6 bit，c1 取余下 2 bit 左移 4 位，
  即 **c1 的低 4 bit 恒为 0**，后补 `==`。
- 剩 2 字节（16 bit）：c0、c1 用满，c2 取余下 2 bit 左移 2 位，
  即 **c2 的低 2 bit 恒为 0**，后补 `=`。

因此解码时，尾部填充组里这些「编码器永远写 0」的比特必须真的是 0，
否则该字符串**不是任何字节串的编码**。统一规则：

> 最后一个含 `=` 的组中，被 `=` 挤掉的低位比特必须全为 0。
> 判定只看两类比特：`XX==` 看第 2 个字符的低 4 bit；
> `XXX=` 看第 3 个字符的低 2 bit。

`QR==`：`R` 的值是 17 = 0b010001，低 4 bit 为 0001 ≠ 0。
宽松解码器把这 4 个比特丢弃，碰巧也得到 `A`；但这样一来
`QQ==` 与 `QR==` 两个不同字符串解码出同一字节串，
`Encode(Decode(y)) == y` 不再成立，编码结果对同一字节串不再唯一。
严格模式拒绝一切「丢弃比特非 0」的尾部，等价于保证
**Decode 的每个输出都有唯一的合法编码**，即 Encode∘Decode = id
（在合法输入上），这正是「对任意字节串，编码结果唯一」的解码侧表述。

同理 `QUJ=`：`J` = 9 = 0b001001，低 2 bit 为 01 ≠ 0，拒绝；
`QUI=`：`I` = 8 = 0b001000，低 2 bit 为 00，合法。

## 语义 ↔ 测试对照表

（待测试写完后补齐）
