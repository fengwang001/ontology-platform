# DESIGN — 带依赖的配置解析与环境覆盖器

## 1. 先合并还是先展开（核心推导）

**结论：先按优先级合并出最终键值表，再统一展开引用。**

反例（先展开后合并）：文件层有 `greeting = hello ${name}`、`name = file`，
环境层只覆盖 `name = env`。若先在文件层内展开，`greeting` 被固化为
`hello file`；环境层没有 `greeting`，合并后仍是 `hello file`——但用户
语义上期望 `hello env`，因为 `name` 的最终值是 `env`。错误根源：展开
发生在「最终值尚未确定」的时刻，引用被绑定到了低优先级层的旧值。

先合并后展开则天然正确：合并后 `name = env`（环境层胜出）、
`greeting = hello ${name}`（仅文件层提供），展开时 `${name}` 解析到
最终表中的 `env`，得 `hello env`。因此流水线固定为：

```
defaults/file/env/args → source 规范化 → merge（一层一值）→ expand → validate
```

trace 在 merge 阶段记录各层**原始值**（展开前），展开后再补上展开值。

## 2. 引用语法与转义规则

- 引用：`${key.path}`，key 须为合法键路径（非空、无空段）。
- 部分引用：值中可出现多个引用，`${a}${b}` 依次展开。
- 转义：`$$` 表示一个字面量 `$`，故 `$${a}` 展开后是字面量 `${a}`，
  不发生引用。扫描时先识别 `$$`，再识别 `${`。
- 孤立 `$`（后既非 `$` 亦非 `{`）按字面量处理，不报错。
- `${` 后无配对 `}`：可判定错误 `ErrUnclosedRef`，报出 `${` 的字节位置。
- 环：展开按 DFS 沿引用边走，访问栈中再次出现某键即成环，
  报 `ErrCycle` 与闭合路径（如 `a -> b -> a`）；自引用 `a = ${a}` 同理。
- 引用不存在的键：可判定错误 `ErrUnknownRef`。

## 3. 环境变量名映射与歧义判定

两条映射规则（键段小写化）：

- 规则一（单下划线）：`a.b.c` ↔ `A_B_C`，`_` 是层级分隔符。
- 规则二（双下划线）：`a.b.c` ↔ `A__B_C`，`__` 是层级分隔符。

读取时对每个环境变量名分别按两条规则求候选键，丢弃非法键（空段等）。
若**两个不同的环境变量名**映射到同一个键（如 `A_B_C` 与 `A__B_C` 同时
存在且都指向 `a.b.c`），无法判定取舍，报 `ErrEnvAmbiguous` 并列出两个
原始名。同层内单名单映射不冲突。

## 4. 配置文件格式与截断分类

行格式：`key = value`，`#` 起注释，空行忽略；键值两侧空白修剪；
**完整行必须以 `\n` 结尾**。`key =`（有换行）是合法的空值，与「键不
存在」可区分（前者有条目、值为空串）。

文件被截断（末行缺 `\n`）时按末行内容分类，行号 = 已见 `\n` 数 + 1：

- 末行无 `=`：`ErrKeyIncomplete`（键不完整，键可能还有后续字节）。
- 末行有 `=` 但 `=` 后无值字节：`ErrValueIncomplete`（值不完整）。
- 末行有 `=` 且有部分值：`ErrLineIncomplete`（行不完整，值可能被截断）。

三者与 `ErrTypeMismatch`、`ErrMissingRequired`、`ErrEnvAmbiguous`、
`ErrCycle`、`ErrUnclosedRef`、`ErrEmptyKey`、`ErrEmptySegment` 均为
sentinel 错误，可用 `errors.Is` 区分。

## 5. 合并与展开的复杂度设计

- merge 单遍扫描：条目按层（低→高优先级）依次处理，每条目恰好一次
  map 查找（读），首次出现时一次写入；计数器只计查找。查找总数
  = Σ|层| ≤ 4·max|层| ≤ 4·最终键数，测试以四层各 1 万键验证。
- expand 逐键记忆化：每键展开结果缓存，重复引用直接取缓存。替换次数
  = 各键值中引用出现次数之和（每处恰好替换一次）。对
  `k0=x, k1=${k0}${k0}, …, k20`：替换 40 次 ≤ 4·21·(40/21)=160；
  朴素递归不缓存则为 2^20 量级。计数器随 Expander 暴露只读访问。

## 6. 溯源与确定性

- trace 在 merge 时按层记录每个键的原始值（低→高），最终层即值来源；
  展开后补最终展开值。报告键按字典序输出，层序固定，与来源构造顺序
  无关（merge 按层优先级而非构造顺序），故打乱构造顺序输出逐字节一致。
- 键校验：空键 `ErrEmptyKey`；含空段（`a..b`）`ErrEmptySegment`；
  深度不限（100 层可用）。值为空串合法。
- validate：类型（string/int/float/bool）对展开后的值解析，类型错误
  报键名、期望类型、实际值、来源层四要素；必填缺失一次性收集全部。
