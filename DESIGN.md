# 媒体类型内容协商器设计

包划分：`mtype`（解析/规范化，无依赖）→ `rank`（单项匹配与评分，依赖 mtype）→ `negotiate`（对外入口，依赖前两者）。依赖单向，无反向引用。

## 1. 为什么先比 q 再比特异性

q 是客户端声明的意愿强度，特异性只是打破平局的规则。若反过来先比特异性，
客户端的意愿会被忽略。反例：Accept 为 `text/*;q=0.9, text/plain;q=0.1`，
候选为 `[text/plain, text/html]`。客户端明显更想要任意 text（0.9），
对 text/plain 只给 0.1。若先比特异性，text/plain 以特异性 2 胜出，
返回的恰是客户端评分最低的具体类型；正确做法是先比 q：
text/html 命中 `text/*`(q=0.9) 胜过 text/plain 的 0.1，应选 text/html。
因此排序键为 (q 降序, 特异性降序, 候选序号升序)。

## 2. q=0 是拒绝；非法 q 的处理

q=0 在 RFC 7231 中意为"不可接受"。若当成"最低优先级"，则 Accept 为
`text/plain;q=0`、候选只有 `[text/plain]` 时会返回 text/plain，
这正是客户端明确说"不要"的类型，且此时并非无其他选择，而是应判定无法接受。
故 q=0 的项只用于否决，不产生匹配。

非法 q（`q=1.5`、`q=abc`、`q=0.1234`）的处理选择：丢弃该项，继续解析其余项，
而不是整条 Accept 报错。理由：Accept 来自不可信的远端，一处笔误不应让全部
有效声明失效；丢弃单项与 RFC"忽略无法理解的参数"的容错传统一致，且仍可通过
返回的解析告警被观测。该选择由测试固定：非法 q 项被丢弃后，
若因此无项匹配，结果为"无法接受"而非解析错误。

## 3. 同分时为什么取服务端顺序

Accept 的顺序不携带语义（RFC 7231 未赋予书写顺序意义），而服务端候选清单的
顺序是服务提供方的显式偏好（例如把成本最低的格式放前面）。同分时若看
Accept 顺序，结果会被客户端书写习惯左右且不可由服务端控制；取服务端顺序
则结果稳定、可复现，且让"服务端偏好"这一唯一有语义的顺序生效。

## 4. 缺失与空串为什么必须区分

RFC 7231：缺少 Accept 头表示客户端接受任何类型，等价于 `*/*`；
而 `Accept:` 空串是客户端显式声明不接受任何类型。若合并处理，
要么把"随便给"错判成拒绝（可用性 bug），要么把"明确拒绝"错判成放行
（语义 bug，违背第 6 条"拒绝必须可判定"）。因此入口用 `*string`
区分两者：nil 等价于 `*/*`；指向空串则无可接受项，判定无法接受。

## 解析与错误

解析器手写（不用 `mime.ParseMediaType`）：type/subtype 大小写不敏感、统一转小写；
参数名小写化、参数值保留大小写；支持引号值（内部分号不分隔、`\` 转义）。
五类错误各自有独立哨兵错误值（ErrMissingSlash、ErrEmptySubtype、
ErrParamMissingEq、ErrUnclosedQuote、ErrBadQ），可用 errors.Is 区分，
错误消息携带项序号（item N）。
