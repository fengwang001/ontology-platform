# DESIGN — 近似最近邻的分桶检索器（随机超平面 LSH）

## 1. 签名计算规则

- 每张表 `t` 有一族 `b` 个随机超平面，法向量 `h[t][i] ∈ R^d`，分量取自标准正态分布。
- 向量 `v` 在表 `t` 上的签名是 `b` 位整数：`bit_i = 1 当且仅当 <h[t][i], v> >= 0`，否则为 0。
- **内积恰为 0 的约定**：取 `>= 0` 即落入正半空间，bit 置 1。因此零向量（与任何超平面内积恒为 0）是合法输入，其签名恒为全 1（`2^b - 1`），所有零向量落同一个桶，互相成为候选，行为自洽。
- 含 NaN 或 ±Inf 的向量在 `Add` 时被拒绝并计入 `Skipped()`，不参与签名计算。
- 几何事实：两向量夹角为 `θ` 时，单个超平面把二者分到同侧的概率为 `p(θ) = 1 - θ/π`（方向越接近概率越高）。

## 2. 位数 b、表数 L 与候选数、召回的推导

- 单表同桶概率：两向量在一张 `b` 位表上签名相同的概率为 `p(θ)^b`。
  - 对"无关"向量（随机方向，`θ ≈ π/2`，`p ≈ 1/2`）：`≈ 2^-b`。
  - 对"真近邻"（`θ` 小，`p` 接近 1）：`p^b` 缓慢下降。
- 候选数期望（L 张表取并集、去重后）：
  `E[cand] ≈ N · (1 - (1 - 2^-b)^L)`（无关向量贡献）+ 真近邻贡献。
  - **b 增大**：每桶期望大小 `N·2^-b` 指数缩小 ⇒ 候选数单调下降；但真近邻同桶概率 `p^b` 也下降 ⇒ 召回下降。这就是"桶越细、候选越少、召回越低"的权衡。
  - **L 增大**：候选数近似线性上升（`1-(1-x)^L ≈ Lx`），同时真近邻至少在一表同桶的概率 `1-(1-p^b)^L` 单调上升 ⇒ 召回单调不降。
- 召回单调性的严格论证：若 L 增大时**沿用原有表再追加新表**（表集合嵌套），则候选集合嵌套 `C1 ⊆ C2 ⊆ ...`；精排是在候选集上取精确 topK，候选集变大只会让精确 topK 不差 ⇒ 实测召回单调不降（确定性成立，非统计意义）。本实现第 `t` 张表用种子 `seed+t` 生成，L=1/2/4/8 的配置共享前缀表，满足嵌套条件。
- 精排距离计算数 = 去重后的候选数，约束为 `≤ 10%·N`；选 b 使 `N·2^-b·L << N` 即可满足。
- 哈希计算次数：每个向量在每表算 b 个内积 ⇒ 建索引总量恰为 `N·L·b`，用计数器精确断言。

## 3. 可复现性

- 超平面只由显式种子生成：`rand.New(rand.NewSource(seed+t))`，逐表、逐位、逐维按固定顺序调用 `NormFloat64`，不依赖全局随机源与 map 迭代顺序。
- 同种子两次建索引：超平面逐分量相同 ⇒ 每个向量的签名表逐字节相同（测试断言）；不同种子 ⇒ 签名表必不同。
- 落盘序列化时桶按签名排序写出，文件字节亦可复现。

## 4. 落盘格式（persist）

```
[头 20B]  magic "LSH1" u32 | dims u32 | bits u32 | tables u32 | count u32(向量数)
[超平面]  tables × bits × dims × float64（小端，按 t,i,dim 顺序）
[桶表]    每表: nbuckets u32；每桶: sig u64 | nids u32 | ids u32×nids（按 sig 升序）
[CRC32]   以上全部字节的 CRC32(IEEE)，u32
```

- 截断分类（`errors.Is` 可判定）：头不足 20B → `ErrHeader`；超平面区不足 → `ErrHyper`；桶表区不足 → `ErrBuckets`；CRC 缺失或不匹配 → `ErrCRC`。
- 读回校验：任一法向量全零（退化，无法划分）→ `ErrDegenerate`，报出表号与位号。
- 恢复（`Recover`）：解析最大完整前缀的桶；引用 `id >= count` 的悬挂 ID 剔除并计数，保证恢复结果自洽。

## 5. 并发语义

- `Index` 内嵌 `sync.RWMutex`：`Add` 先在锁外算完全部 L 个签名，再持写锁一次性把向量追加到全部表 ⇒ 对查询而言每张表要么全可见要么全不可见。
- `Query` 持读锁，多协程并发查询安全；`-race` 干净。

## 6. 复杂度

- 建索引：`O(N·L·b·d)` 哈希计算；查询：`O(L·b·d + |候选|·d)`，`|候选| << N`（测试断言 ≤ N/10）。
