# Quoted-Printable 设计说明（RFC 2045 严格子集）

## 推导一：行末空白为何必须转义

MIME 传输允许在硬换行（`\r\n`）前剥离尾随空格与制表符；输入结束处的空白
同样没有后续字符保护。因此判定规则只看「后继」：空格/制表符之后紧跟硬换行
或 EOF 时输出 `=20`/`=09`，否则原样。故 `"a \n"`→`"a=20\r\n"`、
`"a b"`→`"a b"`、`"a "`（EOF）→`"a=20"`。

## 推导二：软换行与 `=XX` 不拆分

输出单元只有「原字符」（宽 1）与「`=XX`」（宽 3）；软换行写 `=\r\n`，
其中 `=` 占第 76 格，所以普通位置可用内容宽为 75；但输入行最后一个单元
之后不再有内容，不必软换行，可占满 76 格。规则：写每个单元前，若它不是
本行最后一个单元且 `cur+width>75`，先输出 `=\r\n`。于是第 74 格后遇到
`=3D` 时 74+3=77>75，先软换行再写 `=3D`，不可能出现 `=3` 跨行。

## 软换行恰好插在空格之后：空格保持原样，不写 `=20`

解码器删除的是整个 `=\r\n` 三字符序列。它前面的空格在删除后直接与下一行
首字符相邻，属于行中空白而非行末空白；写成 `=20` 反而是多余转义。由于软
换行总插在两个输出单元之间，该空格后面紧跟的是下一个单元，永不落入
「行末」判定，故原样输出。

## 往返等式（第 6 条准确形式）

设 N(x) 为：`\r\n` 与单独 `\n` 一律规范化为 `\r\n`，单独 `\r` 保留为
普通字节 0x0D。则对任意字节串 x：

`Decode(Encode(x)) == N(x)`，且 `Encode(x)` 必被严格解码器接受。

## 幂等与最小转义（第 7 条）

编码器只在四种必要处改动字节：`=` 及 33–126 以外字节转义、行末空白转义、
超过 76 格处插软换行；且软换行位置由 76 格边界唯一确定，无多余软换行。
因此对严格解码器接受的 y，`Encode(Decode(y))` 与 y 的差异只可能来自 y 中
对可打印字符多余的 `=XX` 被还原为原字符，编码器本身是最小转义的。
