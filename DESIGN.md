# 自然排序：前导零推导与全序论证

## 分段
ASCII `0`–`9` 连续字节为数字段，其余（含全角数字、UTF-8 续字节）为非数字段。

## 第 3 条推导
- 样例 `x0 < x00 < x1`、`a1 < a01 < a001`：数值相等时必须再分出先后，且零越多越大。
- 但 `a01b < a1c` 排除「立刻比前导零个数」：数字段 `01`/`1` 若马上比零，会判 `a1 < a01`，
  从而 `a1c < a01b`，与要求相反。故零的差异必须**推迟到其余全部比较完仍平局时**才参与。

## 完整分层（按序）
1. 同步取段。段类型不同：比该段首字节（数字字节 `0`–`9` < 任意非数字首字节，如 `1`<`a`）。
2. 非数字段：按字节序字典序比较。
3. 数字段：先比数值——长度（去零后有效位数）不同则位数大者大；等长再逐字节字典序（任意长，不转整数）。
4. 一边流先结束：段数少者小。
5. 仅当 1–4 全部平局（去零后的「规范化串」完全一致）：从左到右找第一个前导零个数不同的数字段，
   零少者小；全相同则两串字节全等，Compare 返回 0。

## 演算
- `a01b` vs `a1c`：段 `a`=`a`；数字段数值相等（1=1），按规则 4 推迟；非数字段 `b` vs `c` 已分出：前者小。
- `a1b01` vs `a01b1`：`a`=`a`，数字段 1=1 推迟；`b`=`b`；数字段 1=1 推迟；同时结束，落入规则 5：
  零向量 `(0,1)` vs `(1,0)`，第一处差异 0<1，故前者小。
- `x0`/`x00`/`x1`：规范化串同为 `x1`（末者数值为 1；`x0` 规范化为 `x0`——单独说明：全零段规范为 `0`），
  故 `x0,x00,x000` 零数 1<2<3，均小于 `x1`（数值 0<1，规则 3 即分出）。

## 第 4 条全序
规范化串比较是若干「固定段序上的全序」的字典序组合：每个分量（类型首字节 / 字节序 / 数值 / 段数）
各自为全序，故其字典序是全序（传递）。它只在两串规范化后完全相等时才平局；此时比较零数向量（
按数字段位置的自然数向量字典序），仍是全序；再平则字节全等。
两层全序按固定优先级组合，反对称且传递；相等当且仅当 `a==b`。
非 ASCII 只是普通非数字字节，规则与编码无关，故不破坏全序，也不会 panic。

## 语义—测试对照表
<!-- TODO 最后补 -->
