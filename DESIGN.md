# RLE 设计说明

## 严格解码拒绝清单（不变量：接受的 t 必须满足 `Encode(Decode(t)) == t`）

1. 显式次数 1（`1a`）：Decode 得 `a`，Encode 回写省略次数得 `a != "1a"`，拒绝。
2. 次数 0（`0a`、`00a`）：游程次数必须 >=1，重复 0 次不在编码像集内；回写也无法产生 0，拒绝。
3. 前导零（`01a`、`007a`）：规范十进制无前导零；Encode 回写为 `a`/`7a`，与原文不同，拒绝。
4. 相邻同符号游程（`2a3a`）：Encode 强制最长游程，会合并回写为 `5a != "2a3a"`，拒绝。
5. `\` 后既非数字也非反斜杠（如 `\n`、`\a`）：转义只允许数字与 `\`，否则该形式不可能由 Encode 产生。
6. 末尾孤立 `\`：反斜杠必须与被转义字符成对，缺少符号，不是任何 Encode 输出的闭合前缀。
7. 末尾只有次数没有符号（`12`）：游程必须以符号收尾，无法回写完整游程，拒绝。
8. 非法 UTF-8：Encode 的输出只含合法 UTF-8 与 ASCII，非法字节不可能出现在像集中，拒绝。

每条错误均为可判定的哨兵错误，包装在带字节偏移（0 基）的具名 `DecodeError` 中：
偏移定位到次数首数字（次数 1/0/前导零）、第二游程首数字（相邻同符号）、`\`（非法转义/孤立转义）、
末尾次数首数字（缺符号）、首个非法字节。

## 大次数取舍（语义第 3 条）

选择「支持任意大次数，但受可配置输出字节上限约束」。

- 次数用 `math/big.Int` 解析与比较：再大的十进制也不整数溢出，杜绝静默错误结果。
- 解码绝不按次数预分配：每个游程按 `count` 循环以常量大小分块写出；
  写前累加输出字节数并与 `Limit` 比较，超限立即返回哨兵 `ErrLimitExceeded`。
- 默认 `DefaultLimit = 64 MiB`；`NewDecoderLimit` 可配置。
- 即 `99999999999999999999a` 不会溢出或 OOM：上限内正常输出，超上限得到可判定错误。

## 语义 ↔ 测试对照表

| 语义条目 | 测试函数 |
| --- | --- |
| 1 往返（含数字/反斜杠/多字节/长游程） | `TestRoundTrip`（表驱动 + 随机） |
| 2 规范形式与八类拒绝（错误可区分、带偏移） | `TestRejects`（表驱动） |
| 3 大次数：不溢出、超限可判定 | `TestBigCount` |
| 4 最长游程；组合字符不规范化 | `TestEncode`（表驱动） |
| 5 任意切分（含 `2a|3a`）与单段逐位一致 | `TestStreamingSplits`（循环所有切点） |
| 复杂度：检查计数 == 输入字节数 | `TestInputByteCounter` |
