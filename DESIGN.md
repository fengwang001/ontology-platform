# 通配符匹配设计

## 范围

仅支持 SQL LIKE 风格的两个通配符：`%` 匹配任意长度文本（含空串），`_` 匹配恰好一个码点。
`\` 只可转义 `%`、`_`、`\`；不支持正则、字符集、查询、索引或持久化。

## 单锚点线性回溯

朴素递归把每个 `%` 分成“匹配空串”和“多吃一个码点”两个分支。多个 `%` 连续失配时，
旧分支会反复尝试不同的消费量，分支数可随 `%` 数量指数增长。

正式算法只保存最近一个 `%`：

1. 从左到右扫描模式和文本。
2. 普通字符要求相等，`_` 要求当前有一个码点。
3. 遇到 `%` 时记录锚点 `(模式位置, 文本位置)`，模式前进，文本暂不前进。
4. 后续失配且存在锚点时，回到“该 `%` 后的模式位置”，让该 `%` 比上次多吃一个码点。
5. 没有锚点，或锚点吃到文本末尾后仍失配，则匹配失败。

一个锚点足够：更早的 `%` 若需要多消费，必须先经过最近 `%` 所在的后缀；在最近锚点穷尽前，
调整旧锚点只会得到已被当前后缀覆盖的消费划分。若最近锚点已穷尽仍失配，再让旧锚点多吃一格，
等价于把新的边界再次交给最近锚点。因此回到最近锚点并推进文本不会漏解，也不需要递归。

每次主循环只产生当前位置的一次判定或一次锚点回退；模式位置与文本位置单调推进，回退后的
锚点文本位置也单调推进，故总成本为 `O(模式长度 * 文本码点数)`，预算上界按 `4*m*n` 预留常数。

## 相邻百分号折叠

解析时，若当前未转义 `%` 的上一个规范化词法单元也是未转义 `%`，则丢弃当前 `%`。
转义得到的字面量 `\%` 不是通配符，不能参与折叠。

语义上，`%%` 是两个“任意串（含空串）”的连接。任意文本 `t` 总能拆成 `t=t1+t2`
（含 `t1` 或 `t2` 为空），所以 `%%` 与 `%` 表示同一字符串集合；结合律可把任意连续
`%` 折叠为一个。`%%%a` 折叠为 `%a`，二者匹配集合相同且规范化词法单元数减少。

## 码点与字节

`_` 的长度单位是 Unicode 码点，不是字节。内部先校验 UTF-8，再以码点序列匹配：

- `caf_` 有四个码点，匹配同样四个码点的 `cafe` 或 `café`；最后一个码点可任意。
- 一个四字节表情符号是一个码点，可由一个 `_` 匹配。
- 非法 UTF-8 立即返回 `ErrInvalidUTF8`，错误携带 `pattern` 或 `text` 侧别及字节位置。
- 匹配器绝不把非法字节静默替换成 U+FFFD。

空模式只接受空文本；仅有 `_` 时要求文本码点数不少于 `_` 数量。文本中的 `%`、`_`、
`\` 默认都是字面量；模式中的这三个字符只有通过 `\` 转义后才按字面量处理。

## 包边界

- `pattern`：UTF-8 与转义校验、词法单元、相邻 `%` 折叠。
- `budget`：保存非导出步数计数和可选上限。
- `match`：单锚点匹配算法与文本 UTF-8 校验。
- `audit`：固定随机数据、朴素递归参考实现和结论对拍。
- `cmd/demo`：不读参数、不联网，逐条打印验收判定。
