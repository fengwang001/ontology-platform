# 设计：只追加日志的段索引与范围回放

目标：把只追加事件日志切成段，每段建稀疏偏移索引，按事件序号区间回放；只依赖标准库。

## 1. 布局

- 段文件 `seg-XXXXXX.log`：段头 + 若干事件记录。段头 24 字节：
  magic(4)="SEG1"，version(2)=1，firstSeq(8,u64 BE)，count(8,u64 BE)。
- 事件记录（自描述，长度前缀 + CRC32）：
  length(4,u32 BE，载荷字节数)，seq(8,u64 BE)，payload(length)，crc(4,u32 BE)。
  CRC 覆盖 length、seq、payload 三部分。seq 写入记录体内，保证仅靠段文件即可
  逐条恢复序号，索引无需保存任何“只在写入时可知”的信息。
- 索引文件 `seg-XXXXXX.idx`：每 N 条事件一个锚点，锚点 16 字节：
  seq(8,u64 BE)，offset(8,u64 BE)，offset 为该记录首字节（length 前缀处）。
  锚点序号按序排列。追加写每落 N 条追加一个锚点。
- count 仅在关闭/封存段时写入；存活段头部 count=0，读取以顺序扫描为准。

## 2. 稀疏索引定位推导

回放 `[from,to]`，记锚点序号为 a0<a1<…。必须选择“不大于 from 的最大锚点”
`a = max{ai | ai <= from}`，从其偏移开始顺序读，跳过 seq<from 的记录。

为什么不能选“最接近 from 的锚点”：若该锚点落在 from 之后（ai>from），从它开始
扫描会丢失区间 [from, ai) 内的全部事件；稀疏索引只保证锚点之后可顺序到达，不能
反向定位锚点之前的记录。选 floor 语义则起点保证 ≤ from，顺序扫描必然经过 from
本身，不重不漏。

from 小于首个锚点（或无锚点）时，起点取段头结束位置（offset=24），全段从头扫，
此时“用上索引”退化为“用段头定位”，跳过数最多为 N-1（因为第一条事件之后 N 条
内必出现首个锚点；首个锚点对应第 0 条，故实际跳过 0 条；该分支在跨段段首
from=段首序号时出现）。

跳过数上界：锚点每 N 条一个，floor 锚点之后到 from 之前至多有 N-1 条事件，
故定位阶段跳过事件数严格小于 N。

边界：from>to 返回 ErrBadRange。from<最小序号按区间裁剪，从最小序号开始，不报错；
to>最大序号回放到末尾，不报错；to<最小序号返回空区间。

## 3. 索引可丢弃与逐字节重建

索引是纯加速结构。重建 = 顺序扫描段文件：从 offset 24 起逐条读记录，第 0、N、
2N… 条（段内相对序号）写锚点。锚点全部信息（seq、offset）均可由段文件推导：
seq 来自记录体，offset 为扫描位置。因此重建结果与原索引逐字节相同。禁止把段内
不可推导的状态放入索引。

## 4. 索引失效回退

定位时先跳到 floor 锚点偏移，验证该偏移恰好对齐一条记录：能读出合法 length、
且记录 CRC 通过、且其 seq 等于锚点 seq。任一不成立（如偏移被改成指向事件中间）
即判定索引失效：忽略全部锚点，从 offset 24 全段扫描，结果仍正确，并在返回的
Report 中标记 IndexDegraded=true 与失效段。

## 5. 段边界

段头记录 firstSeq 与封存后的 count；相邻段必须满足
`firstSeq_i + count_i == firstSeq_{i+1}`。回放按 firstSeq 排序所有段，逐段顺序
处理，跨段无缝。repair 校验连续性，不连续则返回 ErrGap 及缺口区间
`[firstSeq_i+count_i, firstSeq_{i+1})`。

## 6. 损坏分类与截断恢复

扫描在每一步给出可判定错误（errors.Is 四类）：
ErrShortHeader（不足 24 字节头）、ErrShortLength（length 前缀不足 4 字节）、
ErrShortBody（记录体不足 length+8 字节，含 seq 与 payload）、
ErrBadCRC（CRC 字段不全或校验不符；截断点落在 CRC 四字节内归此类）。
另：ErrGap（序号缺口）、ErrBadRange（from>to）。

截断段恢复：扫描至第一条报错位置停止，已完整通过 CRC 的记录构成最大可恢复前缀；
repair 将 count 改写为实际可读条数并把文件截到该前缀结尾，再顺序读不再报错。

## 7. 并发

所有跨进程/协程可见性以操作系统 read/write 的字节流与 CRC 原子性保证为准：
Writer 每记录先组装完整字节再一次 Write，Reader 只承认 CRC 通过的记录，尾部半条
记录表现为“不完整”而被忽略，因此并发回放看到的始终是一致前缀。同目录 Writer
内部用 mutex 串行化追加；Reader 只读已落盘字节。

## 8. 读取字节计数

回放在每段从定位起点开始读；读取字节数上界为：区间内记录编码长度之和 +
floor 锚点到区间起点之间不足一个锚点区间（< N 条）的记录长度，且不读区间结束
之后的内容（读到 seq>to 即停）。
