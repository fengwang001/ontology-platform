# DESIGN: 自定格式 LZ77 流式压缩

## 1. 流格式

| 记录 | 字节布局 |
|---|---|
| 流头 | `4F 4E 54 4C`("ONTL") + varint(version=1) + varint(windowCap) |
| 字面量段 | tag=0 + varint(n) + n 个原始字节 |
| 回指 | tag=1 + varint(dist) + varint(length) |
| 刷新标记 | tag=2（解码器确认边界，无载荷） |
| 流尾 | tag=3 + varint(原始总长) + varint(crc32-IEEE) |
| 流尾 | tag=3 + varint(原始总长) + varint(crc32-IEEE) |

varint 最多 10 字节且第 10 字节只能用最低 1 位，否则报超长/溢出。错误携带流内字节偏移。

## 2. 固定参数

最大回指长度 `maxLen=1024`；最短匹配=3 字节起；哈希链用 3 字节哈希键。

## 3. 四处推导

### 3.1 压缩结果与 Write 切法无关（含 Flush）
贪心匹配只有在「匹配不可能再延长」时才能定案：在位置 i 找到匹配后，只有当第
i+length 字节存在时才知道匹配到此为止。压缩器缓存全部未定数据，每次只推进到
`len(buf)-1`（末字节参与延长匹配，本身先不发）；下一字节到达后，末字节是否延长
前一匹配只取决于它自身，与它来自哪次 Write 无关。为避免缓存无界，把匹配截断在
maxLen：每消费至多 maxLen 个「可延长」字节就必然定案一个回指，未定缓存 ≤
2·maxLen；4MB 全同输入只发约 4MB/1024 条回指，仍 <64KB。Flush 强制把全部未定缓
存定案（当前匹配立即结束、字面量立即发出）再写刷新标记，所以 Flush 边界决定输
出、Write 边界不决定；空 Flush 不发字节（边界未移动）。跨 Flush 不重建窗口/哈
希，新匹配仍可回指 Flush 之前。

### 3.2 重叠回指（distance < length）的复制
块复制 `copy(dst, src)` 在重叠区间按 memmove 语义处理，可能先移动整段，把「尚
未生成的输出」当成历史读走。LZ77 要求源随输出同步增长：`dst[j]=src[j]`，若
src[j] 落在本次复制范围内，读到的是本次刚写出的字节。实现逐字节执行
`out=append(out, out[len(out)-d])` 并同时写入环形窗口；每字节仅一次 slice 索引与
边界检查、无额外分配，4MB 量级可接受，故保留逐字节循环。

### 3.3 「距离超过已输出」与「距离超过窗口容量」为何区分
两者是不同不变量：前者违反 LZ 因果（解码器还没有那个字节，任何压缩器都不可能
合法产生）；后者只说明引用超出格式约定的窗口，压缩器仅在 windowCap 内选候选，
只要 windowCap 与流头声明一致、环形窗口实现正确，就绝不可能产生。并行预置字典
不改变「已输出字节数」：解码器对字典一无所知，它解压本块时，字典字节已经是它先
前真实输出的一部分，距离 d 仍 ≤ 累计输出长度；编码器 seed 上一块末尾 windowCap
字节、只把它们插入哈希（不产字面量），候选距离限制不变，也不可能引用越界字节。

### 3.4 并行块压缩的确定性
块 k 的匹配只读取「本块原文 + 上一块原文的末尾窗口」：窗口内容是 (上一块原文)
的纯函数，不读其他块、不读其他块的压缩字节；同一输入字节必然产出同一输出字节，
故与 goroutine 完成顺序无关，worker 完成后按块序拼接。格式衔接：整个流一个流头
（声明 windowCap），每块结尾写一次刷新标记，使解码器在块边界确认全部已解字节；
最后一块后写唯一一个流尾（总长/CRC 覆盖全部原文）。workers=1/2/4/8 输出逐字节
相同，且可直接喂给普通流式解码器。

## 4. 复杂度

每位置沿哈希链最多考察 chainLimit 个候选，计数器记录考察总数，总考察数 ≤
chainLimit×输入字节数，与窗口大小无关；全同输入每位置在首个候选即命中并延长到
maxLen，每字节考察数为常数。
