# 游标分页设计

## 1. 排序与边界

行由 `(Value float64, ID string)` 组成。数据按严格全序排列：

1. 先比较 `Value`；
2. `Value` 相等时按 `ID` 的字典序比较。

若游标只保存上一页最后一行的值 `v`：

- 下一页用 `Value > v`：其余所有 `Value == v` 的行会被跳过；
- 下一页用 `Value >= v`：上一页已经返回过的行又会重复。

例如三行是 `(1,"a")、(1,"b")、(1,"c")`，页大小为 2。第一页结束于 `(1,"b")`；只记录 `1` 无法区分 `a、b、c`。正确游标保存复合键 `(1,"b")`，下一页从严格大于它的 `(1,"c")` 开始，而不是从 `>1` 或 `>=1` 开始。

因此正向边界是：

```text
(Value,ID) > (cursor.Value,cursor.ID)
```

反向边界是：

```text
(Value,ID) < (cursor.Value,cursor.ID)
```

空游标在正向表示最小值之前，在反向表示最大值之后。

## 2. 反向翻页

反向不是把正向结果倒序。反向时：

1. 用小于游标锚点的键定位；
2. 从有序数组中向更小键的方向取 `limit` 行；
3. 取到后在页内重新按正向全序升序返回。

所以从正向第三页的首行反向取一页，会得到按正常顺序排列的第二页，而不是第二页的倒序。

## 3. 游标格式

游标是不透明字节串：

```text
direction(1) || float64 bits(8, big-endian) || id byte length(2, big-endian) || id || fnv-1a 64 checksum(8)
```

方向为 `F` 或 `B`。解码先检查最小长度与长度字段，再检查方向，最后校验 FNV-1a。这样字节截断可归类为字段不完整，方向字节损坏可归类为方向非法，正文或校验变化归类为校验失败。四类错误分别用哨兵错误暴露，支持 `errors.Is`。

解码只读取入参数组并使用固定宽度整数转换；不创建哈希器，也不复制游标。

## 4. 存储与并发

内存状态维护一份不可变有序切片。插入先检查唯一 ID，再通过二分找到位置，并复制生成新切片；删除也生成新切片。用 `sync.RWMutex` 保护当前切片指针。读页在锁内取得快照头后释放锁，因此翻页只依赖入参游标和该次快照，不推进任何共享状态。

同一游标并发使用会在同一快照存在期间得到逐行相同的页；即使写者切换了快照，每次调用自身仍基于一致快照。

## 5. 增删发生时的承诺

不提供快照事务或时间旅行。分页过程中：

- 插入到已翻过区间的行，允许直到重新从头遍历才出现；
- 插入到尚未翻过区间的行，只要属于该次后续键区间，就应出现；
- 删除的行不再出现；
- 已通过某个复合边界返回的行，后续正常使用返回游标时不会再次出现；
- 游标锚点对应的行被删除不影响继续翻页，因为边界只比较复合键，不查询该行是否仍存在；
- 未删除且键值落在当前边界之后的行不会因等值边界被跳过。

## 6. 效率

每页通过 `sort.Search` 对不可变有序切片做一次二分定位，再复制至多 `limit` 行。比较次数上界按 `limit + ceil(log2(n))` 计；演示与测试使用更严格的验收线 `4 * (limit + ceil(log2(n)))`。
