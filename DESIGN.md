# 设计推导

## 1. 空输入 / 纯换行输入 × 末尾策略

策略：`Keep` 保留原有末尾状态；`EnsureOne` 非空输出必须以恰好一个 `\n` 结尾；`Trim` 先删全部末尾空行，非空时再补恰好一个 `\n`。
空输入一律输出 `""`：若 `EnsureOne("")="\n"`，则空流凭空造字节，会让「无内容」与「一个空行」不可区分，也违反「不凭空插入」。
故选保守定义：只有输入（去空白判定后）非空时才补换行。此定义下三策略均幂等（输出已无 `\r`、无行尾空白，再规范化除末尾策略外为恒等；`Trim` 输出恒以单个 `\n` 结尾，再 `Trim` 不变）。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 `ToOut`

不变量：`ToOut` 单调不减；对输出位置 `o` 有 `ToOut(ToOrig(o))==o`。
一串被删原文字节 `[a,b)` 映射到删除点的输出位置 `p`：`ToOrig(p)` 必须落在删除段内（存活字节区间不能覆盖 `p`），于是 `ToOut` 在整个 `[a,b]`（含终点 `b`）取 `p`，保证 `ToOut(ToOrig(p))=p`。
具体：`\r\n` 的 `\r` → 输出 `\n` 的位置；行尾空白 `[a,b)`（`b` 为换行起点）→ `\n` 的位置（即换行符**之前**）。
编辑器语义：光标落在被删空白里时，逻辑位置仍在该行内容末尾、换行符之前（位置等于 `\n` 的位置）；视觉上恰在 `\n` 左侧，符合直觉，而不是跳到下一行。删除段终点（换行字节本身）映射到 `p+1`。

## 3. 空白缓冲超限（第 8 条）

选择：**拒绝**。强制判定为「非行尾」会立刻输出空白，但若后续真出现行尾，这些空白已不可收回，违反第 2、5 条（输出残留行尾空白、再规范化结果变化）与第 4 条（切分点不同则缓冲长度不同，结果分叉）。
拒绝则进入终态、返回带原文偏移的可判定错误 `ErrWSOverflow`，已输出字节与映射保留；错误前的结果等于对「错误偏移处截断的前缀」规范化的结果，第 4/5/6 条在成功域内不受影响。`WSLimit=0` 表示不限。输出上限 `MaxOut` 同理在越界写入时拒绝（`ErrOutputLimit`，带原文偏移），已输出保留。

## 4. `par` 拼接修正

段边界处只有两种未决状态：待定 `\r`（下一段首字节若是 `\n` 则配对）与一串尚不知是否行尾的空白。
段内独立规范化后记录边界事实：`endsCR`（输出末尾的 `\n` 是否来自孤立 `\r`）、`trailWS`（段尾被删空白的原文字节）、`startsLF`（段首是否消费了一个 `\n`）、`leadWS`（段首被删空白原文字节）。
拼接规则：①前一段 `endsCR` 且后一段 `startsLF` → 删除后段首 `\n`（本是一对 `\r\n`）；②若①不成立，则前段尾部被删空白其实是行中间空白（边界后紧跟非行尾字节）→ 原样重新插回；③后段首空白被删，仅当其后紧跟行尾时成立，否则（边界前非行尾）插回。
修正只触及每段边界处至多一个换行与两段空白串，故修正范围有上界（≤ 两段尾/首空白长度，不随段大小增长）。
映射拼接：段内 run 坐标为段局部偏移，按「已拼接的原文字节数 / 输出字节数」整体平移；插回/删除的修正直接改写边界 run。
