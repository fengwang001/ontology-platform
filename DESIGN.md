# 设计推导

## 1. 零计数 hunk 头的行号（语义 4）

样例：`x\n`→`y\nx\n` 得旧侧 `-0,0`、新侧 `+1`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0 +3`。
观察：旧侧行数 b=0 时，头里的 a 等于插入点**前一行**的旧文件行号（插入点在文件开头则前一行不存在，记 0）；新侧数字 a′=a+1 是插入位置在新文件中的行号。
结论：b=0 表示该 hunk 在旧文件不占行，a = 插入点之前的旧行数（即 0 基插入位置）；Apply 把插入点放在旧文件第 a 行**之后**（a=0 即在文件最前）。这解释了 `-0,0` 与 `-2,0`：若 a 表示“将插入的位置”，两者都应使用新坐标 1/3，无法同时成立；而“前一行行号”规则统一成立。d=0 对称：c=删除点前的新行数，反向应用时在第 c 行后插入。

| b | 头中 `-a,b` 的书写 | Apply 对旧文件位置的解释 |
|---|---|---|
| 0 | `-a,0 +c,d`（d=1 省略为 `+c`） | 不消耗旧行；在第 a 行后插入，a=0 即文件头 |
| 1 | `-a` 省略 `,1` | 从第 a 行起消耗 1 行 |
| ≥2 | `-a,b` 完整书写 | 从第 a 行起消耗 b 行 |

## 2. hunk 合并阈值（语义 5）

C 为每侧上下文行数。相邻两段改动的窗口为 [起点−C, 终点+C]。设两改动之间未改动行数为 g：两端各取 C 行后共需 2C 行上下文；g≤2C 时窗口相接或重叠（g=2C 时恰好首尾相接），GNU `diff -U C` 合并为一个 hunk；g=2C+1 时中间至少有 1 行不属于任何窗口，必须分开。故阈值为 **2C**。反证：若阈值是 C，取 C=0 会把相邻插入错误拆开；若是 2C+1，C=0、g=1 也会合并，但 GNU `diff -U0` 对相隔 1 行的两处改动输出两个 hunk。测试钉住 g=2C 合并、g=2C+1 分开。

## 3. 最短脚本并列选择（语义 3）

采用前向 Myers，对角线上只保留到达最远 x 的路径；每轮 d 保存 V 快照用于回溯。并列时采用标准 Myers 的确定性规则：`x = V[k-1] < V[k+1] ? V[k+1]（插入） : V[k-1]+1（删除）`，即 V 值相等时取删除；边界 k=−d 强制插入、k=d 强制删除。因此 `a b`→`b a` 在 d=1、k=1=d 上先删 `a`，脚本为 `-a`、` `b`、`+a`，删除排在插入之前。脚本仍为最短，且只依赖输入字节，故重复生成逐字节确定。

## 4. 偏移后后续 hunk 的平移（语义 7）

设第 i 个 hunk 记录起点 o_i、实际匹配起点 p_i，位移 δ=p_i−o_i；该 hunk 造成后续文本行号增量 Δ=插入行数−删除行数。第 i+1 个 hunk 的搜索中心取 o_{i+1}+δ（跟随最近一次位移），且匹配点必须位于前一 hunk 应用结束位置之后。原因：偏移一般源于更早处与目标的差异，对其后所有 hunk 的影响相同；匹配在“尚未应用本 hunk”的文本上进行，所以只平移 δ，不把 Δ 重复计入。
