# 设计推导

## 推导 1（Write 切法无关）

匹配长度只有在“无法继续延长”被证实时才确定。若在 Write 边界立刻吐出匹配，
下一次 Write 的字节本可把它延长，输出就依赖切法。压缩器维护自待决起点到
当前末尾的一段串：每扩展一字节查一次最长匹配，维护 bestLen 与起点 p；一旦
当前匹配长度 < bestLen，p 处匹配已闭合（后续字节无法使其变长），提交之并
整体越过 p+bestLen（途中不做哈希插入）；bestLen 为 0 则 p 成为字面量。整段
串上的决策只依赖串内容，与分几次 Write 拼出无关，故字节级确定。代价是
Flush 前最多缓存距上次 Flush 的全部新输入（Flush 是用户显式分界，有界）。
Flush 强制闭合当前待决匹配并写刷新标记，承诺 4 成立；被缓存字节的哈希插入
始终延迟到其决策之后，Flush 之后的匹配仍可回指它们。

## 推导 2（重叠回指解压）

d < L 时目标位置会读到本次回指已写出的字节（模式周期重复）。一次性块复制
`dst[0:L]=src[0:L]` 在重叠区读到陈旧/越界数据。实现用 `out[n]=out[n-d]` 逐
字节循环；这也天然满足炸弹防护：先做加法溢出与上限检查再开循环，长度 2^40
在任何写出前即被拒绝。逐字节循环可接受：每字节一次有界环形索引、零额外分
配，与匹配同阶；单条回指截断到 maxMatch=64KiB，成本有界。

## 推导 3（两类“距离过大”）

`dist>window` 表示引用了窗口从未保存的历史，是格式/配置矛盾：压缩端窗口容
量恒定且只插入最近 cap 字节，绝不可能产生（预置字典也不超过 cap）。
`dist>已输出` 表示引用在容量内但早于实际产出前缀（损坏使距离变大）。前者
意味配置不匹配，后者意味流损坏，必须分开。预置字典不改变判定：它是上一块
解压器已真实输出的尾部，天然满足 dist<=已输出 且 dist<=cap，无需任何豁免。

## 推导 4（并行块确定性与衔接）

块 k 的压缩输入 = 上块末尾至多 cap 字节预置字典（只插入窗口/哈希，不产
出）+ 本块原文；结果只依赖这两段原文，与完成顺序、worker 数无关，worker
只切任务。块间不插刷新标记：上块结束时其全部字节已在解压窗口中，即下块
预置字典。各块只发字面量/回指记录，全部块压缩后按块偏移排序拼接，最后发
唯一流尾（总长、CRC32）。块内用推导 1 的延迟决策、块尾强制闭合。

## 流格式（首字节 tag；整数均为无符号 LEB128 varint）

| 记录 | tag | 后续字段 |
|---|---|---|
| 流头 | 0x01 | version=1, windowCap, maxChain |
| 字面量 | 0x02 | length, length 个原始字节 |
| 回指 | 0x03 | distance(>=1), length(>=3) |
| 刷新 | 0x04 | 无 |
| 流尾 | 0x05 | 原文总长, CRC32-IEEE |

varint 至多 10 字节，第 10 字节只允许最低位；超长/溢出即损坏。minMatch=3。
CRC32 用 hash/crc32（非 compress/*，仅作校验和）。
