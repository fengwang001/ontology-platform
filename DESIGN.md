# DESIGN

## 记录布局

所有整数用小端 unsigned varint（LEB128）；长度/距离先减 1 再编码（字段写 `v-1`）。

| 记录 | 布局 |
|---|---|
| 流头 | magic `4C 5A 37 31`("LZ71") + version=1(varint) + winCap(varint) |
| 字面量 | tag=0(varint) + `len-1` + 原始字节 |
| 回指 | tag=1 + `distance-1` + `length-1` |
| 刷新 | tag=2（仅 Flush 时；连续空 Flush 不写） |
| 流尾 | tag=3 + 原文总长(varint) + CRC32IEEE(varint) |

常量：minMatch=3，maxMatch=1<<20，chainLimit=64，默认窗口=1<<16。

## 推导一：输出与 Write 切法无关

压缩器把输入追加进 `buf`，仅当 `len(buf) > holdback`（holdback=maxMatch）时才在 `buf[0]` 做决策。
此时该位置之后至少还有 maxMatch 个真实字节（来自任意多少次 Write 已无关），能取到的最长匹配就是确定值；
取匹配则发回指并整体跳过，否则发一个字面量。尾部 ≤maxMatch 字节只在 Flush/Close 时强制处理。
因此决策只依赖最终字节序列与累计偏移，不依赖 Write 边界。Flush 必须强制排空所有未决字节：
未决字节尚未编码，不排空就不满足“已输出字节足以还原全部输入”；排空后下一位置重新起链，不影响已写字节。

## 推导二：重叠回指

距离 d<长度 L 时，回指区间与输出区间重叠：第 d 个及以后的源字节其实是本次复制刚写出的字节。
块复制只保证源与目的不重叠，会重复读到旧内容而错误。实现按输出索引 i 逐字节 `out[i]=out[i-d]`，
天然形成周期展开（如 abc 以 d=3 重复）。热路径上是一次随机读+一次写、无额外分配；
解压上限也正好按 i 逐字节检查，故采用逐字节循环，既正确又不额外付出代价。

## 推导三：两类距离越界

“超过已输出字节数”表示引用了流开始（或并行块）之前的数据，是截断/伪造流的典型特征；
“超过窗口容量”表示引用比压缩器能记住的历史更久远，即使已输出很多也不可能出现。
压缩器只保留最近 winCap 字节、匹配器只在其中检索，故绝不会产生后者。
并行块以“上一块末尾 winCap 字节”预置字典后，该字典即成为本块的已输出历史：
预置时同步填充环形窗口，并把“已输出计数”置为字典长度。之后距离检查逻辑完全不变，
本块解出的字节追加到输出时该计数同样增长，故两类区分仍然成立。

## 推导四：并行确定性

每块为 `data[i*bs:(i+1)*bs]`，字典是上一块末尾至多 winCap 字节（即原文中紧邻本块之前的字节）。
本块哈希链初始只含该字典，压缩结果只依赖“字典原文+本块原文”，与 goroutine 数、调度顺序、
其他块的压缩产物无关，故 workers=1/2/4/8 输出逐字节相同。块间不需要刷新标记：
前一块 Close 已强制把尾部全部编码，块边界解压器处于干净状态，下一块直接以预置窗口继续解释回指；
流头只写一次，流尾只写一次（总长=全长、CRC=全量 CRC）。
