# 设计推导

## UTF-8 非法单元
首字节决定期望长度。先要求所有后续字节在 `80..BF`；再做最短形式与上界校验：
`E0` 的第二字节必须 `A0..BF`，`ED` 必须 `80..9F`，`F0` 必须 `90..BF`，`F4` 必须 `80..8F`。
若某字节不是期待续接字节，非法单元从首字节开始，结束于“该字节之前”：已读到的非法续接字节都随首字节吞掉，新字节另起解析。
因此 `F0 90 80 41` 是 3 字节一个单元；`E0 80 80` 中 `80` 终结 `E0`，随后两个独立 `80`，共三个。
`C0/C1`、`F5..FF` 必非法且只吞 1 字节；孤立 `80..BF` 也只吞 1 字节。
流结束时，仍满足“首字节+合法续接前缀”的残片是一个截断单元，只替换一个 FFFD；如残片内已有字节终结非法性，则立即切成一个非法单元，不等待 EOF。

| 首字节 | 第二字节合法区间 | 非法时吞掉字节数 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 1 + 其后连续 `80..BF` |
| `E0` | `A0..BF` | 1 + 其后连续 `80..BF` |
| `E1..EC` | `80..BF` | 1 + 其后连续 `80..BF` |
| `ED` | `80..9F` | 1 + 其后连续 `80..BF` |
| `EE..EF` | `80..BF` | 1 + 其后连续 `80..BF` |
| `F0` | `90..BF` | 1 + 其后连续 `80..BF` |
| `F1..F3` | `80..BF` | 1 + 其后连续 `80..BF` |
| `F4` | `80..8F` | 1 + 其后连续 `80..BF` |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

“吞掉的连续续接字节”在遇到非续接字节时停止，且不包含该非续接字节。第三、第四字节只需在 `80..BF`；在 EOF 前它们保持待定，不提前替换。

## 流缓存
缓存只存“首字节 + 尚未被否定的合法前缀”。UTF-8 最长 4 字节，所以硬上限为 3 个待决续接字节加下一次输入中的判定字节；任何时刻缓存不超过 3。
看到第二字节即可否定 `E0/ED/F0/F4` 的特殊前缀；若其仍在 `80..BF`，至少属于当前前缀，不会回退重扫。
看到非续接字节立即结束当前非法单元；看到序列最后一个合法续接字节立即输出标量。每个输入字节只从调用者缓冲区进入缓存一次，检查次数计入一次，故总计不超过 `2n`。
UTF-16 每单元 2 字节；缓存上限 2（LE/BE 首字节）。高代理必须再看 2 字节，低代理到来即提交，非低代理不被吞掉，立即成为下一单元首字节。

## par 切点
切点 `cut` 不直接作为边界。从 `cut` 向左最多回看 3 字节，找覆盖 `cut` 的 UTF-8 首字节；再用首字节的期望长度检查 `cut` 是否仍可能是其合法前缀。
若可能，段起点移到该首字节，使该完整待定字符（或非法单元）交给下一段；前一段在该首字节前结束，不做 EOF，因而不会提前 FFFD。
否则在 `cut` 切分：孤立续接字节或必非法首字节本身就是同步点。`C3|80 80` 中 `C3 80` 连同交给后段为一个合法字符，剩下 `80` 为一个非法单元，和流式相同。
若切点字节否定前缀，如 `F0 80|...`，前段只拥有 `F0`：该字节在其边界即一个非法单元；后段从 `80` 开始，仍产生同一个独立单元。
对齐只检查固定 3 字节窗口，成本 `O(K)`，所以 K=8 时总检查数为 `n+O(K)`。

## 输出上限断点
提交顺序是“先确定完整标量，再向输出追加其全部编码”。若追加会越界，保留此前完整标量并返回 ErrLimit。
本次 `Write` 的返回字节数只包含已终结字节：合法多字节字符的全部输入字节、或非法单元吞掉的字节；留在 1..3 字节跨写缓存中的残片不计入本次返回，而计入总 Consumed/统计。
调用方新实例续传时从“本次返回的输入位置”重喂这些残片；BOM 与输出缓存同理只在完整提交后计数。
UTF-16 代理对作为一个标量整体提交，绝不只输出高代理。
