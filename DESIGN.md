# 设计推导：严格 JSON 字符串编解码器

## 第 2 条：代理对的错误偏移

把 `\uXXXX` 视为最小词法单元，其起点是反斜杠处的绝对字节偏移
（偏移包含开头的 `"`，即整个输入从 0 开始计）。

规则：`\u` 读满 4 位 hex 得到码点 cp 后——

- cp 是高代理（D800–DBFF）：必须紧跟一个以 `\u` 开头、值为低代理
  （DC00–DFFF）的完整单元。紧随字符不是 `\`、不是合法 `\u`、或值不在
  低代理区间 → 报 ErrBadSurrogate，偏移指向**这个高代理单元自身的起点**。
- cp 是低代理（DC00–DFFF）但没有未配对的高代理在等待 → 报
  ErrBadSurrogate，偏移指向**该低代理单元的起点**。
- 高代理等待期间遇到 `\u` 截断、EOF 等 → 报 ErrShortUnicode，偏移指向
  那个残缺单元的起点（它首先是一个残缺的 `\u`）。

理由：孤立高代理的错误在“它要求配对却找不到”时才成立，但最靠近病灶、
且在按字节切分流式喂入时保持稳定的锚点是它自身起点——等待期遇到的每个
坏字符各有各的词法错误（如 `x` 是“未知转义”/非法内容，残缺 `\u` 是
“短 unicode”），不能让高代理掩盖下游单元自身的错误类型。反之孤立低代理
在其自身起点即可判定，故指向自身。`"\uD83DA"` 不能解释成“替换字符+A”：
RFC 8259 不允许静默替换；高代理后必须是 `\u`，`A` 只是普通内容，配对要求
未被满足，整体必须拒绝。

## 第 4 条：最小转义与唯一性

RFC 8259 对字符串内容只强制两类约束：引号/反斜杠会破坏词法，必须转义；
未转义的 C0 控制字符 U+0000–U+001F 禁止出现，必须转义。U+007F（DEL）
不在 C0 区间、不是引号也不是反斜杠，原样 UTF-8 输出仍然是合法 JSON 字符串
内容，故不必转义；U+001F 是 C0 控制区最后一个码点，必须转义。`/`、
非 ASCII（含 U+2028/U+2029）同理原样输出。短形式只取 RFC 指定的
`\b \f \n \r \t`，其余控制字符统一为小写 `\u00XX`。

唯一性/单射：编码是逐码点的确定性映射，且该映射是单射——
原样输出的码点产生其 UTF-8 字节（首字节 ≥0x20 且不为 `"`/`\`），
转义产生以 `\` 开头的两或六字节序列，二者首字节互不重合；短形式与
`\uXXXX` 形式也互不重合，且每个控制字符只选其中一种。因此不同码点的
输出首元素可区分，串接后任意两个不同字符串必有不同编码（单射）；每个码点
只有一种合法最小写法，故输出唯一。于是最小转义写成的字面量经 Decode 后
再 Encode 必然得到原字面量。

## 语义—测试对照表

（实现完成后补齐）
