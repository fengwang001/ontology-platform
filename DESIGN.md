# 设计推导

## 1. 末尾换行策略（样例表）

核心规范化 C(x)：`\r\n`、`\r`→`\n`；删每行行尾的空格/制表符。
策略（作用于 C(x)，t=尾部连续 `\n` 数）：
- Keep：原样，输出 C(x)。
- ExactlyOne：x 为空白输入 → `""`；否则 t=0 时**不插入**，t≥1 时压成 1 个 `\n`。
- TrimEmpty：C(x) 全是 `\n` → `""`；否则 t 压成 1；t=0 不插入。

| 输入 | Keep | ExactlyOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

幂等：三种策略的输出均无行尾空白、无 `\r`、尾部 `\n` 数 ∈ {0,1}（Keep 除外），
再规范化不变。ExactlyOne 对空输入选 `""` 而非 `"\n"`：选 `"\n"` 则
N("")="\n" 而 N("\n")="\n"，虽幂等，但要求“凭空插入 1 字节”，输出长大于
输入长，与第 6 条 `ToOut(ToOrig(o))==o`（该式使 ToOrig 必须为满射，输出长
不能超过输入长）矛盾。故空输入必须映射为 `""`；同理 t=0 不补换行。

## 2. 被删字节的 ToOut

ToOrig 必须单调不减且被 ToOut 左逆。被删字节不占据输出位置，故删除点两侧
输出偏移相同，被删原偏移 i 一律取该公共值 = 下一个保留字节的输出偏移
（无下一字节则取输出终点）。因此 `\r\n` 的 `\r`（i）与 `\n`（i+1）都映射到
该 `\n` 的输出偏移；行尾空白串中每个 i 映射到紧随其后的换行符偏移（EOF 处
则为输出终点）。语义：光标落在被删空白里，映射到行尾换行符**之前/其上**
（视觉上的行末），而非跳到下一行。

## 3. 空白缓冲超限

候选：(a) 强制判定为“非行尾”输出——但后续若真出现换行，这些空白应被删除，
一旦吐出无法收回，违反跨切分一致与幂等（第 4、5 条）。(b) 拒绝：返回
ErrPendingLimit（带原文偏移），保留已输出，进入终态；拒绝不产生任何错误
映射，第 4/5/6 条在已输出前缀上仍成立，状态可解释。选 (b)。

## 4. par 拼接

各段独立跑 Keep 核心规范化，段结果只携带两类边界状态：至多 1 个待定 `\r`、
至多 L+1 字节待定空白（L=缓冲上限），故修正范围有上界。左→右逐段拼接：
1) 左段尾 `\r` 遇下段首 `\n`：删掉左段那个 `\n`（合并为 1 行尾），否则
遇任何其他字节把 `\r` 确认为行尾（已发 `\n`）；2) 左段尾待定空白遇下段首：
首为 `\n`/`\r` → 空白永久删除；首为其他 → 空白原样补发，且若下段把自己
开头空白也标为“待定”（首字节是空白），则合并待定继续右传。映射用
span.Run 拼接：每段 Run 的 orig/out 起点加全局偏移；删除合并时把对应 Run
标记为删除。最后对整条结果应用一次末尾策略。每段只看相邻段边界、边界状态
有上界，故与单流完全一致且切点无关。
