# LZ77 流式压缩设计

## 流格式（所有整数均为 uvarint，LEB128，至多 10 字节且不得溢出 64 位）

| 记录 | 字节布局 |
|---|---|
| 流头 | `4F 4C 5A`（"OLZ" 魔数）+ 1 字节版本 `01` |
| 字面量段 | `01` + uvarint 长度 n(≥1) + n 字节原文 |
| 回指 | `02` + uvarint 距离 d(≥1) + uvarint 长度 n(≥1) |
| 刷新标记 | `03`（无负载） |
| 流尾 | `04` + uvarint 原文总长 + uvarint CRC32(IEEE) 校验和 |

流尾之后出现任何字节即「多余数据」错误。空输入的合法流 = 流头 + 流尾(长度 0)。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

问题：贪心匹配在位置 p 的最长匹配可能延伸到当前已收到输入的末尾，而下一个
`Write` 可能带来更长匹配；字面量段也可能被后续字节续长。因此凡是「吃到待定
缓冲区末尾」的决定都不能提交。

结论与规则：编码器维护一个未提交字节缓冲区，并给匹配长度设硬上限 `maxMatch`。
位置 p 处的决定（字面量，或距离 d、长度 n 的回指）**当且仅当**满足以下任一条件
时才提交并发出：
1. 匹配长度达到 `maxMatch`（不可能再变长）；
2. 匹配结束位置严格小于待定缓冲区末尾（后续字节只可能追加，不可能改变已确定的
   失配事实——贪心最长匹配在第 n+1 字节的失配已由现有字节证明）；
3. 位置 p 未命中任何匹配且 p 不是缓冲区最后一个字节（该字节必为字面量，但字面量
   段先不封口，继续累积）。

字面量段只在「即将发出回指 / Flush / Close」时封口，因此其边界只取决于匹配
位置，与 Write 切法无关。待定缓冲区的驻留量由匹配行为决定（至多一个待定匹配
的长度），关键在**提交判据只看匹配是否触及缓冲末尾**。推论：`Flush` 必须强制
结束当前匹配与字面量段——Flush 承诺「截至此时的全部输入可被还原」，不能再等
后续字节来证伪待定匹配，故 Flush 时把缓冲区内所有位置按「已到流尾」语义提交，
并写一个刷新标记。`Close` 同理再补流尾。无新输入时 Flush 只重放提交逻辑，缓冲区
为空，连刷新标记也不重复写，故第二次 Flush 零字节。

## 推导二：距离 < 长度的重叠复制（第 2 条）

块复制 `copy(dst, dst[len-d:])` 在 d < n 时源与目标重叠，`copy` 语义是「先快照
源再写」，结果是源的前 d 字节平铺而非周期延拓，与 LZ77 逐字节前向复制语义不同
（例：历史 `abc`，d=3、n=30 应得 `abcabc…`，整块复制则出错）。本实现仍用
`copy`，但按「每次最多复制 d 字节」分段：第 k 段复制时源区间已完全落在已还原
字节内，数学上等价于逐字节循环，而每段都是一次 memmove。d ≥ n 时退化为单次
`copy`；d=1 时退化为填充循环。正确性不依赖逐字节循环，摊销仍是 O(n)。

## 推导三：「距离超过已输出」与「距离超过窗口」为何分开（第 7 条）

二者是不同层面的损坏：超过已输出是「引用了不存在的历史」（逻辑错误，流早期
就可能出现）；超过窗口容量是「引用了实现不可能记住的历史」（编码器不变量被
破坏）。压缩器只向匹配器查询窗口内的位置，窗口容量即搜索范围，故合法压缩器
**绝不可能**产生距离 > 窗口容量的回指；但可能因 bug 或恶意构造产生距离 ≤ 容量
却 > 已输出的回指（流开头窗口未填满时）。并行块压缩中，块 i 以块 i-1 末尾
窗口容量字节为预置字典：对该块而言「已输出」= 预置字典 + 本块已解码字节，即
含义从「本流已解码字节数」扩展为「解码器窗口中可得字节数」。本格式让解码器
窗口跨块自然累积（块间不重置窗口），两种含义在拼接后的流里恰好一致。

## 推导四：并行块压缩的确定性（第 9 条）

块 i 的压缩输入只有两项：本块原文切片、上一块原文末尾 `windowCap` 字节（预置
字典）。两者都是**输入数据**的函数，与任何块的压缩结果、goroutine 调度、完成
顺序无关；workers 只改变并发度，不改变每块输入，故输出确定。每块用独立的
编码器状态（窗口以预置字典初始化、匹配器链表全新），块内不 Flush；块间在流
格式上**不需要刷新标记**——刷新标记只对「Write 边界的待定匹配」有意义，而块
边界处块 i 的匹配本来就可回指字典（上一块），与流式解码器跨段累积窗口的行为
天然一致。各块输出按序拼接，流头由块 0 写、流尾在最后一块后统一写。
