# 属性级权限过滤器 — 设计推导

## 问题

按角色授予列可见性。当过滤谓词引用了对调用角色不可见的列时，系统该怎么办？

## 候选做法与泄露分析

### 甲：把不可见列的比较当作假

谓词中 `secret <op> v` 一律求值为 false。泄露路径：

- 探针 `NOT (secret = 1)`：恒为真，返回**全部行**——包括真实 `secret = 1` 的行。
  攻击者再发 `secret = 1` 得到空集。两个答案与数据无关、恒为互补常量，
  攻击者由此断定：(1) 存在被隐藏的列 `secret`（模式泄露）；
  (2) 系统会对隐藏列说谎，于是 `secret = 1 OR secret != 1`（永真式）返回空集
  这一反常结果进一步确认了隐藏列的存在性。
- 更糟的是结果本身错误：本应按谓词排除的行被返回。

### 乙：把不可见列当作 NULL 参与三值逻辑

`secret <op> v` 求值为 UNKNOWN。泄露路径：

- 探针 `secret IS NULL`：恒为真，返回全部行。`IS NULL` 变成通用探针——
  攻击者可对任意猜测的列名探测其是否存在且被隐藏（存在性泄露），
  并白拿全表行数。
- 探针 `NOT (secret = 1)`：UNKNOWN 取反仍 UNKNOWN，行被滤掉返回空集；
  与 `secret IS NULL` 的全集对照，同样暴露「列被隐藏」这一事实。

### 丙：整条查询拒绝执行并指出是哪一列（本题采用）

谓词引用任一不可见列即返回可判定错误，给出列名与其在谓词树中的路径，
不返回任何行。论证：查询答案完全不依赖数据（恒为错误），
攻击者从答案中得不到关于数据或存在性的任何比特；
错误信息只泄露调用方本来就提供的列名，不泄露数据。
代价是可用性（合法但越界的查询被拒），这在权限场景下可接受，
且错误信息可审计、可指导调用方改写查询。

## OR 短路例外的定义

先做**常量折叠**（含吸收律），再对折叠后的树做可见性检查：

- `X OR true → true`、`X AND false → false`：兄弟分支被**吸收**，永不求值；
- `X OR false → X`、`X AND true → X`：常量分支被丢弃，`X` 仍要求值。

**定义**：仅当不可见列的引用位于折叠过程中被吸收的子树内时，不视为违规；
折叠后仍然存在的引用一律拒绝。理由：吸收发生后求值器根本不会读取该子树，
无任何信息可泄露，拒绝它只会误伤合法查询；反之只要引用在折叠后存活，
求值就必须读取该列，必须拒绝。`AND` 的对偶情形（`X AND false`）同理豁免。

## 行裁剪

结果行只保留可见列，不可见列**从 map 中删除**（不置零值/NULL），
使「不可见」与「值为空」可区分。裁剪按可见列集合迭代、回查行，
代价 O(可见列数)，与总列数无关；拷贝计数上界 4×可见列数。

## 谓词检查复杂度

折叠后的树做单遍 DFS，每个节点恰好访问一次；
非导出计数器记录访问数，测试断言其等于折叠后树的节点总数。

## 错误分类（errors.Is 可区分）

- `ErrInvisibleColumn`：谓词引用不可见列（附列名与路径）；
- `ErrUnknownRole`：角色不在策略中；
- `ErrInvalidPredicate`：谓词树畸形（如空子节点、未知节点类型）。
  注意：`nil` 谓词表示「无过滤」，是合法输入，不属于错误。

## 审计报告

报告含：被裁列名集合（字典序）、被拒谓词引用（列名 + 全部路径，
同列多次出现只报一次但路径列全）、整查拒绝标志。
序列化前对所有集合排序去重，保证与构造顺序无关、逐字节确定。
