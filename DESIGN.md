# DESIGN — 采样剖析与热点归因器

## 1. self 与 total 的定义与恒等式

对一棵按调用路径聚合的调用树，每个节点 N 对应一条根到 N 的帧路径：

- `total(N)` = 所有「调用路径经过 N」的采样数，即 N 及其整棵子树的采样数之和。
- `self(N)` = 调用路径恰好在 N 终止（N 是栈顶叶帧）的采样数。

每条采样栈在插入时，沿路径上每个节点 `total += 1`，仅栈顶节点 `self += 1`。
由此推出两条恒等式：

1. **Σ self(N) = 总采样数 S**：每条采样恰好有一个终止节点，只贡献一次 self。
2. **Σ total(N) ≥ S，且一般 > S**：深度为 d 的栈会让 d 个节点的 total 各加 1，
   祖先被重复计入。Σ total = Σ(每条栈的深度) ≥ S，等号仅当所有栈深度为 1。
   因此 total 之和绝不能当作样本总量使用，测试同时断言这两条防止混用。

## 2. 递归帧的函数级归因推导

同一函数 F 出现在栈的多个层级（如 `A→F→G→F→H`）时，按调用路径建树，
F 会出现在两个不同节点上：外层 F（A 之下）与内层 F（G 之下）。

若把「函数 F 的 total」定义为所有名为 F 的节点 total 之和，则内层 F 的 total
被重复计算：凡是经过内层 F 的采样必然也经过外层 F，已计入外层 F 的 total。
即 `total(F内) ⊆ total(F外)`，简单相加会把同一份采样数算两遍。

**正确推导**：函数级 total 只统计**不被另一个同名 F 节点包含（祖先链上无同名帧）的
最外层 F 节点**的 total 之和。这些最外层节点对应的采样集合两两不相交，
其并集恰为「所有经过任意 F 的采样」，故

- `FuncTotal(F) = Σ total(N)`，N 取遍名为 F 且祖先链无 F 的节点；
- `FuncSelf(F) = Σ self(N)`，N 取遍所有名为 F 的节点（self 互不相交，可直接相加）。

对 `A→F→G→F→H`（单条栈采样 n 次）：外层 F 的 total = n，内层 F 的 total = n，
`FuncTotal(F) = n`（只取最外层），而不是 2n。

## 3. 深度截断

栈深超过上限 `MaxDepth` 时，只保留前 `MaxDepth` 帧建树，并满足：

- 被截断的样本计入树级计数器 `TruncatedSamples`（可读出的「截断样本数」）；
- 截断点（保留下来的最深节点）打 `Truncated` 标记，表示其下方还有被丢弃的帧；
- 整个样本不丢弃，截断后的前缀照常计入 self/total，恒等式 Σself = S 仍成立；
- 深度恰好等于上限不截断，上限加一才截断。

## 4. 复杂度约束

- 插入一条深度为 d 的栈：沿树逐层在子节点表中查找/插入，比较次数 ≤ d × 子节点分支数，
  与已有节点总数无关。`tree` 用非导出计数器 `lookups` 记录比较/查找次数，
  测试以 10 万条深度 20 的栈断言总次数 ≤ 100000×20×4。
- 归因（排序、函数级汇总、排除帧）全部基于一次性快照做一遍扫描 + 一次排序，
  不重建树；计数器 `rebuilds` 恒为 0。

## 5. 采样正确性

- **丢样**：栈来源在「忙」窗口返回丢样信号，采样器计入 `Dropped`。
  恒等式：Σself + Dropped = 应采样次数。
- **时钟回拨**：相邻两次时钟读数之差 ≤ 0 视为异常间隔，跳过该次采样并计入
  `BadIntervals`，不产生负间隔或巨大间隔；恒等式不受回拨影响。
- **停止幂等**：`Stop` 多次调用效果等同一次（`sync.Once`）。

## 6. 落盘格式（dump）

自描述头 + 逐节点记录 + CRC32：

- 头部（固定 36 字节）：magic(8) `ONTPROF1`、版本 u16、头长 u16、
  节点数 u64、总采样数 u64、丢样数 u64。
- 节点记录（前序遍历，父先于子，保证恢复出的前缀不会出现父缺子在）：
  帧名长 u16 + 帧名字节 + self u64 + total u64 + 深度 u16 + 标志 u16（bit0=截断）。
- 尾部：对以上全部字节的 CRC32（IEEE）u32。

截断分类（`errors.Is` 可区分）：头部不完整 `ErrHeader`、节点记录不完整 `ErrRecord`、
CRC 不匹配 `ErrCRC`。任一截断点都能读出「最大可恢复前缀」：完整解析的节点记录
按前序重建子树，恢复出的树仍满足 Σself(已恢复) = 已恢复节点的样本数。

## 7. 并发

采样写协程与归因读协程之间用 `sync.RWMutex` 保护树；读方拿到的是一致性快照
（整树拷贝），不会观察到半更新（某节点 total 已加而父节点未加）的中间态。
`-race` 干净。
