# 设计推导：前缀压缩有序字符串字典

## 1. 重启点间隔 K 的权衡

块内条目按「与前一条的公共前缀长度 + 后缀」增量编码，只有每 K 条
设一个重启点存全量。设平均串长 L、相邻平均公共前缀长 p：

- 非重启条目开销 ≈ `2 个 varint + (L - p)` 字节；
- 重启条目开销 ≈ `2 个 varint + L` 字节，另加重启表 4 字节偏移。

块内 n 条的总字节数约为

    S(K) ≈ n*(L - p + 2) + (n/K)*(p + 4) + 24(头部) + 4(CRC)

即每 K 条多付出 `p+4` 字节（丢掉一次前缀复用 + 表项）。K 越大，
重启点越稀，S(K) 越小并趋近 `n*(L-p+2)`；K=1 时全部存全量，
等价于不压缩，S(1) ≈ n*(L+6)。

取值代价：第 i 条从重启点 `i/K` 起顺序解压，解压次数
`i - (i/K)*K + 1 ≤ K`，平均 `(K+1)/2`。K 越小取值越快。

结论：K 是「压缩率 ↔ 随机读延迟」的旋钮。K=16 时每个重启点
摊销 `(p+4)/16` 字节，压缩损失可忽略，取值最多 16 次解压，
是默认选择；测试用 K=1/4/16/64 实测对比（见 FINDINGS.md）。

## 2. 为什么二分只能在重启点上做

二分要求 O(1) 取出第 mid 条作为比较对象。非重启条目只存了
「相对前一条的增量」，不先解压前一条就得不到它的全量——它
不是独立可寻址的。若直接对全部条目二分，比较对象本身解不出来
（或要 O(mid) 解压，二分就失去意义）。因此：

1. 先在重启点数组（每个都可独立解码）上二分，定位候选区间
   `[r*K, (r+1)*K)`；
2. 在区间内从重启点顺序解压并比较，最多 K 次。

比较次数上界：重启点二分 `ceil(log2(R))` 次（R 为重启点数），
区间扫描 ≤ K 次，合计 `≤ 4*ceil(log2(R)) + K`（留足余量，
测试与 demo 均按此上界断言）。块间先用「块级首末值」二分定位
目标块，同一块内不重复解压。

## 3. 有序性检查的位置

有序性在**编码时**（`block.Encode` / `dict.Build`）强制检查：
要求输入严格递增（不允许重复值），违反时返回错误并指出下标。
典型反例：`ab` 之后跟 `a`（后一条是前一条的真前缀）必然乱序，
直接拒绝。选编码期而非解码期的原因：字典落盘后只读，构建期
一次性检查成本最低；解码期只做与编码格式直接相关的廉价校验
（`prefixLen ≤ len(前一条)`，重启点处 `prefixLen == 0`），
完整的顺序复查由 `verify` 包按需做。恢复截断块时只取完整条目，
而完整条目在写入前已严格有序，故恢复结果天然严格有序。

## 4. 前缀长度按字节计算

公共前缀长度按**字节**计，不按码点。理由：与 `len()` 一致、
无需 UTF-8 解码、对任意字节串都安全。以 `café` / `cafés` 为例
（é = U+00E9 = 0xC3 0xA9，占 2 字节）：

- 按字节：公共前缀 `café` = **5** 字节，后缀 `s`；
- 按码点：公共前缀 = **4** 个码点。

本实现取前者（5）。注意按字节算的公共前缀可能切断多字节字符
（如 `café` 与 `cafè` 共享 `caf\xC3` 共 4 字节），但因为后缀
按原始字节存储，解码结果仍然逐字节正确，测试覆盖此情形。

## 5. 格式与错误分类

块文件：`[24B 头部][重启表 nrestart*4B][条目区][CRC32]`。
头部含 magic、版本、K、条目数、重启点数、条目区偏移、CRC 偏移；
CRC 覆盖 CRC 之前的全部字节。条目：`varint 前缀长 + varint 后缀长
+ 后缀字节`。截断分类（`errors.Is` 可判定）：

- 长度 < 24 → `ErrHeaderIncomplete`；
- 长度 < 条目区偏移 → `ErrRestartIncomplete`；
- 长度 < CRC 偏移 → `ErrEntryIncomplete`；
- 落在 CRC 的 4 字节内或 CRC 校验不符 → `ErrCRCMismatch`。

另有 `ErrCorruptPrefix`（前缀长撒谎）与 `ErrNotSorted`。
重启点偏移错位时 `Get` 检出（目标处前缀长非 0 或越界）并回退
为从块首顺序解压，结果仍正确，`RestartOK()` 置 false。

字典文件：`PDIC` 头 + 块索引（偏移、长度、首值、末值）+ 块数据。
字典构建完成前不对外发布，故构建与查询并发时查不到未完成的块。
