# 倒排索引短语查询与增量合并 — 设计推导

## 0. 基本约定

- 位置从 **0** 开始（词元 i 的位置为 i），全库一致。
- 每个位置至多一个词元；空串词元合法，按普通词元处理。
- 倒排链：文档号严格升序；同一文档内位置严格升序。`(doc,pos)` 重复添加被拒绝并计数。
- 段不可变；删除用内存位图标记，合并时物理清除。

## 1. 短语位置对齐的推进规则

短语 `t1..tk` 命中 <=> 存在位置 x 使 ti 在 x+i-1。先按文档号求交（同 AND），
文档内对 k 条位置表做类归并同步推进，每条表一个游标，**游标只前进不后退**：

1. 锚定：令 a = 第 1 条表当前位置。
2. 对 i=2..k：推进第 i 条游标至第一个 `>= a+i-1` 的位置 v。
   - 若 `v > a+i-1`：对齐失败，把第 1 条游标推进到第一个 `>= v-(i-1)` 的位置，回到 1。
   - 若全部 `v == a+i-1`：在 a 处命中一次。
3. 命中后**只把第 1 条游标前进一格**（其余游标不动），回到 1。

正确性：任一游标之前的元素都不可能再属于任何命中（锚点 a 单调不减，
而命中要求其余位置为 a+i-1，也单调不减），故跳过安全；每步至少推进一个游标，必然终止。

## 2. 重叠短语的计数

规定：**允许重叠计数**。`A A A` 查 `"A A"` 命中 2 次（0-1、1-2）。
推导：规则 3 命中后仅锚表前进一格，其余表的游标仍停留在 `a+i-1`；
新锚 a' > a 时只需 `v >= a'+i-1`，旧位置 `a+i-1` 若等于 `a'+i-1`（即 a'=a+1）
会被直接复用而**不被跳过**，于是位置 1 既作前一命中尾、又作后一命中头。
若规则 3 改为"命中后所有游标都越过本次用到的位置"，则 `A A A` 只得 1——
这正是很多实现的错误。推得：`A A A A` 查 `"A A"` = 3；`A B A B A` 查 `"A B A"` = 2（锚 0、2）。

## 3. 若允许同一位置多词元

位置表将出现重复值（同一 doc 内同一 pos 多条）。调整：
- 推进比较仍用 `<` 找第一个 `>= 目标` 的元素，重复值天然对齐到同一位置；
- 命中后锚表须**跳过与 a 相等的全部重复值**再取新锚，否则同一锚被重复计数；
- 同一锚可能因多条重复位置产生组合式多重命中：若按"锚"计数需去重，
  若按"词元实例组合"计数则每组合算一次。本题每位置一词元，不存在此问题。

## 4. 复杂度界（测试断言）

- 短语：游标只前进，比较次数 = O(各词位置表长度之和)。1 万 A + 1 万 B 查 `"A B"`，
  断言比较次数 <= 4*(10000+10000)，而非 10^8 乘积。
- AND：以最短链为驱动，对其余链做指数跳跃（galloping）+ 窗口二分；
  断言比较次数 <= 4 * 最短链长 * 链数。
- 合并：一次多路归并，每条倒排项读一次写一次；计数器断言 读 == 写 == 总项数
  （有删除时 读 == 总项数，写 == 总项数 - 删除数）。

## 5. 段文件格式（自描述）

```
magic[4]="OSG1" | version u32 | termCount u32 | dictLen u32      -- 16 字节头
词典: 按词排序, 每项 = termLen u32 | term | offset u32 | length u32
倒排区: 各词编码链顺序拼接 (offset/length 相对倒排区起点)
crc32 u32 (IEEE, 覆盖之前全部字节)
```

链编码：docCount uvarint；每篇 docDelta uvarint、posCount uvarint、位置差分 uvarint。

截断分类（按切点 c，N=文件全长，均可 `errors.Is` 区分）：
- c < 16：头部不完整 `ErrHeaderIncomplete`
- 16 <= c < 16+dictLen：词典不完整 `ErrDictIncomplete`
- 16+dictLen <= c < N-4：倒排链不完整 `ErrPostingsIncomplete`
- N-4 <= c < N（或 CRC 校验错）：CRC 不匹配 `ErrCRCMismatch`

**最大可恢复前缀**：头与词典完整时，倒排区可用字节数 = c-4-(16+dictLen)；
词典中 `offset+length <= 可用字节数` 的词保留并解码，其余词**从词典剔除**，
不留悬挂指针。恢复结果可通过全部不变量自检。

## 6. 合并与崩溃恢复

- 多路归并：各段词典有序，堆/扫描取最小词；同词合并各链（按 doc 归并，
  同 doc 位置归并去重）；删除位图中文档的倒排项直接丢弃。
- 崩溃安全：新段先写 `*.tmp`，关闭后原子 rename 为 `*.seg`，再换内存视图、删旧段。
  启动时扫描目录，识别并删除残留 `*.tmp`；旧段始终可用，查询结果与合并前逐字节相同。
- 并发：查询持 RLock 取一致快照（段切片+删除位图），合并后持 Lock 一次性换入，
  查询结果永远对应某一个一致的段集合，不混合新旧段。

## 7. 边界语义

空索引/查询词不存在 -> 空结果；短语长 1 退化为单词查询；短语超过文档长度 -> 空；
空串词元合法；同 `(doc,pos)` 重复添加 -> 拒绝并计数。
