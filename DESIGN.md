# LSH 分桶近似最近邻检索器 — 设计与推导

## 1. 数据模型与包划分

- `vec`：`Vec = []float64`，欧氏距离、内积、维度校验（错误可用 `errors.Is` 判定）。
- `hyper`：显式种子的随机超平面族与签名计算。
- `bucket`：签名 → 向量 ID 列表的桶表，支持多张表。
- `search`：多表并集候选 + 欧氏距离精排 TopK；暴力精确最近邻基准。
- `persist`：自描述头 + 超平面 + 向量 + 桶表 + CRC32 的落盘/读回/前缀恢复。
- `cmd/demo`：不读参不联网，逐条判定输出。

## 2. 随机超平面 LSH（SimHash 式签名）

第 l 张表、第 k 位对应法向量 r_{lk}，各分量独立取自标准正态
`math.NormFloat64`，随机源为 `rand.New(rand.NewSource(seed))`。种子相同则
r 序列逐字节相同；不同种子以极大概率得到不同法向量，签名不同。
签名第 k 位规则：

	bit(k) = 1 当且仅当 <x, r_lk> > 0；内积 ≤ 0（含恰好为 0）时为 0。

内积恰为 0（含零向量）的约定：**取 0**。零向量与任意法向量内积恒为 0，
故其在所有表所有位签名均为 0，零向量之间同桶，行为确定。

对固定 r，碰撞概率（两位签名相等的概率，单比特一致即视为该位碰撞）：

	P[h(x)=h(y)] = 1 − θ(x,y)/π，

其中 θ 为两向量夹角；越相似夹角越小，碰撞概率越高。

## 3. 位数 b、表数 L 与候选数、召回的关系

单表 b 位全部一致的碰撞概率 p1 = (1−θ/π)^b。
L 张独立表至少一表碰撞的概率 pL = 1−(1−p1)^L。

- **位数 b**：b 增大 → 桶按签名切得更细（共 2^b 个可能桶）→ 期望落入
  同一桶的候选数约 N·2^{-b} 单调下降；但 p1 随 b 指数下降，单表召回降低。
- **表数 L**：多表取并集，候选数随 L 近似线性增加（去重后次线性），
  召回 pL 随 L 单调不降（1→2→4→8 表只会多出候选，不会丢失候选）。
- 权衡：b 负责「桶的粗细 / 候选预算」，L 负责「用冗余换召回」。

复杂度：建索引哈希计算次数恰好为 N·L·b（每向量每表逐位一次内积符号判定）；
查询为 L 次桶查表 + 对候选并集做精排，候选数 ≪ N 时远快于暴力 O(N·d)。

## 4. 召回度量

topK 召回 = |ANN TopK ∩ 精确 TopK| / K。精确基准为对全部向量算欧氏距离。
测试：5000 个 16 维向量、100 查询，取均值；断言 b=8 下 L=1/2/4/8
召回 ≥0.6（L=8）且单调不降；b=4/8/12 候选数随 b 单调下降。
数据采用多簇高斯分布（同簇向量夹角小），使理论碰撞概率与上述模型吻合。

## 5. 边界语义

- 空集合：查询返回空 TopK；单向量、K>N：返回全部。
- 全部相同向量：签名全同，候选 = 总数，召回必为 1。
- NaN：建索引时拒绝并计入 `Skipped`（可读出）；±Inf：拒绝并计入。
- 维度不符（含 d=1 之外的查询）：返回 `ErrDimMismatch`，含期望/实际维度。

## 6. 并发与可见性

索引不可变；多表通过一次 `atomic.Pointer` 整体替换发布，查询要么看到
完整的一组表，要么看到旧组，不会读到半成品表。查询天然并发安全，-race 干净。

## 7. 落盘格式（小端字节序）

	头部:   "LSHI" 魔术(4) | version u16 | dim u32 | bits u8 | tables u8
	        | seed u64 | nvec u32
	法向量: dim*bits*tables 个 float64
	向量段: nvec 个 [id u32 | dim 个 float64]
	桶表段: tables × [nb u32 | nb 个 [sig u64 | cnt u32 | cnt 个 id u32]]
	尾部:   CRC32(u32, IEEE) 覆盖以上全部字节

截断点分类（按文件长度 t，前缀完整所需长度）：

- t < len(头部)：头部不完整 `ErrHeaderTrunc`
- t < 头部+法向量段：超平面参数不完整 `ErrPlanesTrunc`
- t < 头部+法向量+向量段+桶表段：桶表（含向量段载体）不完整 `ErrBucketsTrunc`
- 完整但 CRC 不符：`ErrCRC`

读回时按结构校验恢复最大前缀，剔除桶中不存在的向量 ID 并计数（无悬挂 ID）；
法向量全零（退化）在 CRC 之前先按 (表, 位) 检出 `ErrZeroPlane`。
四类错误 + 维度错误互不包裹重叠，均可 `errors.Is` 区分。
