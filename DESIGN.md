# 设计推导

## 流格式（LEB128 无符号变长整数，小端）

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `OLZ1`(4B) + version(u8=1)，共 5B |
| 字面量 | tag=0(u8) + len(varint,≥1) + len 个原始字节 |
| 回指 | tag=1(u8) + distance(varint,≥1) + length(varint,≥3) |
| 刷新 | tag=2(u8)，无负载 |
| 流尾 | tag=3(u8) + originalLen(varint) + CRC32-IEEE(varint) |

varint 最多 10 字节且第 10 字节只允许最低位为 1，否则超长/溢出。CRC 覆盖全部原文字节。

## 1. 与 Write 切法无关（语义 3、4）
压缩器维护内部缓冲区，只在“能确定下一字节去向”时才提交。算法为贪心哈希链：在位置 i 找到匹配则 i 进入匹配，否则 i 作字面量。
匹配能否延伸依赖未来字节，因此保留至少 2 字节未决：每次只处理到 len(buf)-2，把新到的字节与当前候选再比较一次——匹配变长只取决于字节内容与累计偏移，与到达次数无关，故任意切法产出相同。
Flush 强制把未决字节全部提交并写刷新标记：匹配本可能靠未来字节继续延长，Flush 必须终止它，这正是“强制结束当前匹配”；无新输入时无可提交内容，第二次 Flush 输出零字节。

## 2. 重叠回指解压（语义 2）
距离 d、长度 l，输出端为循环缓冲，按“绝对位置”索引：第 p 个输出字节 = buf[(p-d) mod W]。
d<l 时源位置可能在本次回指中尚未生成，逐字节取时它已在更早一轮写好，故天然等价于逐字节向前复制；memmove 式整块拷贝会取到旧数据而错。
实现上仍是长度 l 的取-存循环；解压炸弹在写任何字节前只做计数器加法即拒绝，可接受。

## 3. 两类越界距离（语义 7）
“距离 > 已输出字节数”：引用了从未存在过的历史，任何流都不可能合法（预置字典也计入解压器窗口，并行块解码时字典就是本块之前已输出内容，含义不变）。
“距离 > 窗口容量 W”：历史可能存在，但已滑出双方约定的窗口。压缩器固定窗口 W，FindMatch 只返回 ≤W 的候选，故正常流绝不产生后者；二者成因不同，必须给不同错误，且都带压缩流字节偏移。

## 4. 并行确定性与块衔接（语义 9）
块 k 的压缩器以“上一块原文末尾 W 字节”为预置字典构建匹配器初始哈希链；它读到的所有字节（字典+本块）与单线程一致，输出只依赖这两块原文，不依赖 goroutine 完成顺序或他块结果。
块流直接首尾拼接，块间不加刷新标记：回指可越过块界指向前块（距离≤W），解压端看到的就是普通记录序列，唯一流头与流尾在最外层由组装者写。workers 只改变调度，故 1/2/4/8 输出逐字节相同。
