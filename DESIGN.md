# DESIGN：半包续传 / 并行切分 CSV（RFC4180 方言）

## 1. 空行与单列空值（推导）
词法上 `\n` 永远闭合当前记录；空字节段后遇 `\n` 仍是一条记录。故 `a\n\nb\n` = [a]、[]、[b]：**空行是一条含一个未引号空字段的记录，不跳过**。若跳过空行，则输入含多少记录不可由字段决定，且无法与 `Write` 互逆。末尾换行不产生额外记录：记录只在「字段被换行关闭」或「EOF 时字段已开始（见过字节或引号标记）」时产出；纯 `""` 因引号标记使字段「已开始」。
单列单条记录且唯一字段为空：写成 `\n` 会被解读为「零记录+末尾换行」→ 丢数据；因此该字段**必须加引号**：`""\n`（无尾换行时写 `""`）。引号标记是 cell 的一等属性，`Write` 对原输入已加引号的空字段保留引号，故往返引号标记也相同。

## 2. 最小引号（必要集合推导）
必要：值含 `,` `"` `\r` `\n`；空值且为单列表最后一条记录（否则与尾换行/零记录歧义，见 §1）；原文该字段已带引号（保留标记）。`""` 内 `\r\n` 原样。**规范输入**：记录以 `\n` 分隔、无尾换行、字段仅在必要时加引号的字节串；规范输入满足 `Write(Parse(x))==x`。

## 3. 状态转移表
状态 SFieldStart / SPlain / SQuoted / SQuoteSeen / SRCr（字节类：, " \r \n other）
| 状态 | , | " | \r | \n | other |
|---|---|---|---|---|---|
| FieldStart | 发空字段→Start | 进Quoted(开引号) | 进Cr | 关记录 | Plain,记字节 |
| Plain | 发字段→Start | **ErrQuoteInPlain** | 进Cr | 关记录 | Plain |
| Quoted | 追加 | 进QuoteSeen | 追加 | 追加 | 追加 |
| QuoteSeen | 发字段→Start | 追加"(转义),回Quoted | 进Cr | 关记录 | **ErrCharsAfterQuote** |
| Cr(未引号挂起) | 发字段(以\r前内容)→新行首→处理, | **ErrLoneCR** | 发字段→新行首→Cr | 关记录(CRLF) | **ErrLoneCR** |
EOF：Cr→**ErrLoneCR**；Quoted→**ErrUnclosedQuote**；字段已开始→隐式关记录。引号内 CR 是内容（Quoted 行追加），不进 Cr。

## 4. CR 待定（半包/切点）
SRCr 只缓存「挂起」布尔，不缓存字节；`\r` 计入偏移但**不计入字段值**，下一 Feed 首字节到达再判（`\n`=CRLF，其他=孤立 \r 错误，`,`=先关字段再处理逗号）。流在 `\r` 处 Close 即孤立 \r。par 段切在 `\r|\n`：outside 假设对本段前置一个合成 `\r`（虚拟字节，不计 counter），段内偏移 -1 基准；inside 假设中 `\r` 是引号内容，无需合成。

## 5. par 切点推导（双假设）
切点可落在引号字段内部，段 worker 不知起点内外。每段跑两遍词法：**outside**（起点在引号外、新字段起点）与 **inside**（起点在引号内，续接一个已开始字段）。第 0 段真实=outside。设前段末尾真实状态：在引号内→本段取 inside；否则 outside。各 worker 产出「事件流 + 尾状态」，编排者按真实尾状态顺序选择并拼接：
- 跨段同一字段（前段末字段未关闭且本段 outside 首字节非分隔符/行首，或本段取 inside）：合并值、偏移取前段起点；引号标记取真正开启该字段的那段（inside 首字段无开引号则继承前段标记）。
- 挂起 CR 跨段由 outside 的合成 `\r` 前置机制覆盖。
偏移：事件偏移=段起点+段内偏移（合成字节为 -1）。记录/字段号为段内局部序号，拼接时加「前段最终序号」基数；错误随事件流携带局部坐标，换算同一基数，即得全局字节偏移/记录号/字段号，与流式逐位一致。每段两遍 ⇒ 处理次数 ≤2N+O(K)。

## 6. 上限「立刻」
FieldStart 首个内容字节到达即记字段开始；字段字节计数在字节被接收的同一动作内自增并在超限瞬间返回 ErrFieldTooLarge（不先存整条记录）。引号内 `""`：第二个 `"` 转义为 1 个字符值，计数按**输入字节数**（每进状态机字节 +1，转义不减），故第一个超限字节到达即拒绝。字段数在每字段关闭时检查 ErrTooManyFields；记录数在每记录关闭时检查 ErrTooManyRecords。错误后终态：记录 err，后续 Feed/Close 返回同一错误。

## 7. 并发
流式 Parser 单实例**非并发安全**；par 内部 K goroutine 各自独立词法器/收集器，仅经 channel/切片回传，`go test -race` 干净。不用 sleep。
