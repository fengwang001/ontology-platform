# 严格流式 Base64 设计说明

## 规范尾部（第二节第 2 条）的推导

Base64 把每 3 字节（24 bit）编码为 4 个 6-bit 字符（sextet）。最后一组不足
3 字节时：

- 剩 1 字节：用 2 个字符（12 bit）承载 8 bit，低 4 bit 没有对应输入，补 `==`；
- 剩 2 字节：用 3 个字符（18 bit）承载 16 bit，低 2 bit 没有对应输入，补 `=`。

编码器写出这些“悬空比特”时必然填 0。宽松解码器直接丢弃它们，于是 `QQ==` 与
`QR==`（R = 010001，低 4 bit = 0001）会解出同一个字节 `A`：两个不同的编码串
映射到同一字节串，`Encode(Decode(y)) == y` 被击穿，编码不再是字节串 ↔ 编码串
之间的双射。严格模式要求“被接受的串必须是某个字节串的唯一编码”，因此尾部
悬空比特必须全 0。

统一规则：**填充 `=` 只能出现在整个流最后一个 4 字符组的末尾；末组若有填充，
其最后一个数据字符的悬空比特必须全 0；无填充时流长度必须是 4 的倍数。**判定
所用的比特是：

- `xx==` 形式：看第 2 个字符 sextet 的**低 4 bit**（v1 & 0x0f == 0）；
- `xxx=` 形式：看第 3 个字符 sextet 的**低 2 bit**（v2 & 0x03 == 0）。

`QR==` 因 v1 & 0x0f != 0 被拒；`QUJ=` 因 v2 & 0x03 != 0 被拒；`QQ==QQ==`
在带填充的组之后仍有字符，属填充位置错误。拒绝这些串后，严格解码器只接受
编码器的输出本身，`Decode` 是单射，`Encode ∘ Decode` 在合法串集合上为恒等。

## 错误分类

五类解码错误均为哨兵错误（`b64.ErrInvalidChar` 非法字符、`b64.ErrNonCanonical`
非规范尾部、`b64.ErrPadding` 填充位置错误、`b64.ErrLength` 长度非 4 倍数、
`b64.ErrNewline` 换行位置错误），另有 `b64.ErrLimit` 输出超限。流层统一包装为
`stream.Error{Kind, Off}`，`Off` 为错误字节在整个输入流中的偏移，`errors.Is`
可区分类别；换行只在 MIME 模式下允许出现在组间，`\r` 必须紧跟 `\n`。

## 语义 → 测试对照表

（测试实现完成后补齐）
