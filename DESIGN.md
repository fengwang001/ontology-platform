# JSON 字符串编解码器设计

## 1. 代理对的错误偏移

规则：错误位置指向“语法上不完整单元”的起点，而不是错误被发现的末尾。

- 孤立高代理：高代理 `\uXXXX` 自身声明还需要一个低代理，所以 EOF 处的错误偏移指向这个高代理的反斜杠。
- 高代理之后不是低代理：高代理加紧随其后的低代理才构成一个完整单元；因此偏移指向后继不合格单元的起点。
  - `"\uD83Dx"` 指向 `x`；`"\uD83DA"` 也指向 `A`。
  - 若后继是残缺转义（如 `\u12`），则指向第二个 `\`。
- 孤立低代理不期待后续码元，错误偏移直接指向该低代理的反斜杠；所以 `"\uDE00\uD83D"` 指向第一个低代理。

`"\uD83DA"` 不能解释成“替换字符加 A”：JSON 转义按语法组合，不是按 16 位数容错；高代理的唯一合法后继是低代理。且 `\u` 固定消费四个十六进制位，`A` 从一开始就是独立 token。替换会偷偷丢弃代理语义并改变字符边界，严格解码器必须拒绝。

## 2. 最小转义、单射与唯一性

RFC 8259 要求字符串中必须转义的只有：引号、反斜杠、未转义的 U+0000–U+001F。U+001F 属于 C0 控制字符，原样出现会违法；U+007F 是 DEL，不在该控制区间，且本身可由 UTF-8 合法表示，所以不必转义。U+2028、U+2029 和其他非 ASCII 字符也同样原样输出；HTML/JS 分隔顾虑不属于 JSON 标准要求。

每个码点有确定表示：`"`、`\` 与五个有短名的控制字符使用固定短形式；其余控制字符固定为小写 `\u00XX`；其他字节（包括 `/` 与 U+007F）原样输出。解码时，普通字节和反斜杠开头的转义互不重叠，每个转义又唯一还原为原码点。因此编码不会合并两个不同字符串，`Encode` 是单射；同一输入不存在第二种合规选择，最小转义输出唯一。

## 3. 语义与测试对照

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1. 五类严格解码错误及字节偏移 | `TestStrictErrors` |
| 2. 代理对组合、顺序与错误偏移 | `TestSurrogates` |
| 3. 双向拒绝非法 UTF-8 | `TestInvalidUTF8` |
| 4. 最小转义与原样字符 | `TestMinimalEncoding` |
| 5. 双向往返 | `TestRoundTrip` |
| 6. 任意流式切分点结果一致 | `TestStreamSplits` |
