# 设计推导

## 格式

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `"LZ7"` + 版本 `1` + varint 窗口容量 + varint 链长上限 |
| 字面量 | 标签 `0` + varint 长度 + 原始字节 |
| 回指 | 标签 `1` + varint 距离 + varint 长度 |
| 刷新 | 标签 `2` |
| 流尾 | 标签 `3` + varint 原文长度 + varint FNV-1a64 校验和 |

多字节整数只用小端 unsigned LEB128；超过 10 字节或末字节溢出即错误。

## 推导

1. **Write 无关**：编码器只在“字节已不再需要用于延长匹配”时提交。保留一个前瞻字节；每次匹配的最大长度为 `len(buf)-1`。若匹配吃到缓冲末尾，暂存它，等待后续字节证明能否延长或结束。任何 `Write` 只改变已确定前缀，不改变其决策，因此输出相同。`Flush` 没有未来字节，必须把最后一个未决字节纳入当前匹配或字面量，再写刷新标记。
2. **重叠复制**：块复制在 `distance<length` 时会复制尚未写入的目标数据；源码按目标位置逐字节取 `out[i-distance]`，天然重复周期。实现直接逐字节写；解压器通常受 I/O 与 varint 解析约束，距离 1 的长串还可用 `bytes.Repeat` 特判，不牺牲正确性。
3. **两种距离错误**：距离大于已输出字节数，说明引用了尚不存在的位置；它不大于窗口容量也仍非法。距离超过容量则引用了按流约定必然遗忘的历史，即使文件很长也不可能有效。压缩器只插入和查询当前窗口内位置，故不会产生后者。并行块的预置字典只是解码器初始窗口；其字节尚未成为“本流输出”，所以“已输出字节数”语义不变：首块不得引用它之后的位置，后续块引用的是上一块已输出内容。
4. **并行确定性**：第 n 块的匹配器只预置上一块原文，再只扫描本块；哈希链、候选顺序和 tie-break 均为纯函数，不使用 goroutine 完成顺序、时间或共享状态。块结果按序号拼接；块间不写刷新标记，普通回指连续衔接，只有调用方显式 `Flush` 才有标签 2。

## 资源策略

回指长度先做 `已输出+长度<=上限` 的加法安全检查，再写任何字节；拒绝后解压器进入终态。哈希链每位置最多考察链长上限个候选，复杂度为 O(链上限×输入)。
