# DESIGN — 统一格式差分器的四处推导

推导均用 GNU diffutils 3.10 实测对照（本实现不调用它，仅用于定规则）。

## 1. 零计数 hunk 头的行号（语义第 4 条）

实测：`x\n`→`y\nx\n`（C=0）得头 `-0,0 +1`；`a\nb\nc\n`→`a\nb\nX\nc\n`（C=0）
得头 `-2,0 +3`；`a\n`→空（C=3）得头 `-1 +0,0`。

规则：**计数为 0 时，起始字段写的是「插入点之前最后一行的 1-based 行号」，
而不是将要插入的位置**。样例 1 插入点在第 1 行之前，前面 0 行，故写 `-0,0`；
样例 2 插入点在第 3 行之前，前面 2 行，故写 `-2,0`；样例 3 新文件为空，插入点
前 0 行，故写 `+0,0`。若写成「将要插入的位置」样例 1 应为 `-1,0`，与实测矛盾，
故排除。统一解释：计数>0 时起始字段是首行的 1-based 行号；计数=0 时是前一
行的行号（即 0-based 插入下标）。`Apply` 解析：count==0 → 下标 = 起始字段；
count>0 → 下标 = 起始字段 − 1。

| b 值 | 头 `-a,b` 写法 | Apply 的插入点（0-based 下标） |
|------|----------------|--------------------------------|
| b=0  | a = 前一行行号（可=0） | 第 a 行之后，即下标 a |
| b=1  | `-a`（省略 `,1`），a = 该行行号 | 下标 a−1 |
| b≥2  | `-a,b`，a = 首行行号 | 下标 a−1 |

## 2. hunk 合并阈值（语义第 5 条）

实测 C=3：两处改动间隔 g 行未改动，g=5 → 1 个 hunk，g=6 → 1 个，g=7 → 2 个。
结论：**阈值为 2C，g ≤ 2C 合并，g ≥ 2C+1 拆分**。

反证：若阈值取 C，则 g=6（>C=3）应拆分，实测合并，矛盾；若取 2C+1，则
g=7（=2C+1）应合并，实测拆分，矛盾。直观理由：g=2C 时前 hunk 的 C 行尾
上下文与后 hunk 的 C 行首上下文恰好相接不重叠，GNU 选择合写；g=2C+1 时
两段上下文之间会多出 1 行不属于任何一侧的上下文，必须拆成两个 hunk。

## 3. 并列最短脚本的选择（语义第 3 条）

Myers 前向贪心在每步选边：当 `V[k-1] < V[k+1]` 时取下移（插入），否则取右移
（删除）。相等时走删除分支，即**删除优先于插入**。实测 `a b`→`b a` 的 GNU
输出为 `-a`、` b`、`+a`（删在插前），与该选择一致。效果：hunk 内同一段改动
的所有 `-` 行排在 `+` 行之前，补丁文本因此确定，重复生成逐字节相同。

## 4. 偏移应用后后续 hunk 的查找起点（语义第 7 条）

要平移。本实现始终按**原始文本坐标**在源行序列上查找（不就地改写，已应用
区域用只读游标跳过），因此行数差 Δ 不需要补偿——补丁里的 A 侧行号本来就
是相对原文的；需要传播的只有落点偏移 k：下一 hunk 的查找起点 =
记录位置 + 累计偏移 Σk。理由：某个 hunk 实际落点相对记录位置偏移 k，说明
目标文本在记录位置之前多/少了 k 行（例如目标比补丁期望多了 k 行），同一
平移大概率对后续 hunk 继续成立，故以 Σk 修正后再在 ±F 内精确匹配；这与
GNU patch 就地修改文件时维护的累积偏移 `last_offset` 作用相同（GNU 的
公式 记录位置+Δ+k 中的 Δ 只是因为它在演化后的文件上定位）。
