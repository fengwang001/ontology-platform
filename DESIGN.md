# 游标分页器设计（keyset pagination）

## 1. 排序模型与复合游标

行 = (score float64, id string)，id 全局唯一。全序：先比 score，
score 相等再比 id（字符串字典序）。记键 `k=(score,id)`。

**反例（只记 score 必错）**：六行 score 同为 1.0，id=a..f，页大小 3。
- 下一页取 `score > v`：d/e/f 整批被跳过（漏）。
- 下一页取 `score >= v`：a/b/c 再次出现（重）。

原因：排序值不具唯一性，等值行之间没有确定的「下一行」。

**正确做法**：游标携带本页边界行的复合键 `k`，下一页取严格 `> k`
（score 大于，或 score 相等且 id 大于）。`(score,id)` 唯一，等值行被
id 切开，因此不重不漏。首页用空游标（负无穷键）开始。

## 2. 正向与反向翻页

正向（next）：从游标键 k 出发，取严格大于 k 的最小 limit 行，
页内升序；返回的游标指向本页最后一行（下页边界）。

反向（prev）：从 k 出发，取严格小于 k 的最大 limit 行（降序取出），
**页内仍按正向升序返回**；游标指向本页第一行（上页边界）。
所以反向不是「正向结果倒序」：比较方向与遍历方向都反，但呈现顺序正。
在第 3 页首行游标上做反向，恰好逐行得到第 2 页。

## 3. 并发增删下的语义

翻页是关于游标键的纯函数：无隐藏推进状态，同一游标并发翻页结果
逐行相同。不承诺快照一致性：
- 行被插入到已翻过的（< 游标键）区间：允许不出现；
- 行被插入到未翻区间：后续页按复合键位置正常出现；
- 行被删除：不再出现，且**不影响其他行**（不依赖该行存在，游标
  只是一个比较锚点，指向已删行仍可续翻）；
- 承诺：已返回过的行不会因等值/插入/删除而再次出现；未删行在其
  复合键区间内不会被跳过。

## 4. 数据结构与效率

`page.Store` 用 treap（随机优先级 BST）按复合键全序存储，读写均
加 `sync.RWMutex`；行键在插入时拷贝，遍历时持锁，翻页为纯函数。
单次翻页沿树单向下行再顺序取 limit 个后继/前驱，比较次数
`<= 树高 + limit`，不从头全扫。计数器记录本次翻页的键比较数。
一万行期望树高约 `ceil(log2(10000))`，故上界
`4*(20+ceil(log2(10000)))` 充裕。NaN 拒绝插入（无全序）。

## 5. 不透明游标

线性格式：`0x4B 0x53 | dir(1) | 8字节 score | idLen(varint) |
id 字节 | checksum(8 FNV-64a，覆盖除魔数与校验外的全部字节)`。
dir: 1=正向 2=反向。解码先核完整性，再核方向，最后核校验和；
三类错误分别为 ErrTruncated / ErrBadDirection / ErrChecksum，
跨方向复用为 ErrDirectionMismatch，空字节串合法（从头开始）。
解码除游标自身外不分配，allocs/op 为常数 0（次切片不逃逸）。
