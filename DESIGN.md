# 设计推导

## 记录布局（wire）

魔数 `OLZ` + 版本 `1`。之后为记录序列，以 END 结束。

| 记录 | 首字节 | 后续布局 |
|---|---|---|
| 字面量 | `0x00` | varint 长度 n；n 个原文字节 |
| 回指 | `0x01` | varint 距离 d；varint 长度 l（d,l≥1） |
| 刷新 | `0x02` | 无 |
| 流尾 | `0x03` | varint 原文总长；varint CRC32(IEEE) |

varint 为小端 7 位/字节（LEB128）；超过 10 字节或第 10 字节超 1 位即溢出。

## 1. 压缩结果为何与 Write 切法无关（第 3、4 条）

匹配决策需要「未来」字节。压缩器维护未决缓冲 `buf`：仅当位置 `i` 之后还剩
至少一个字节时才决定 `i` 是字面量还是回指；否则 `i` 是当前潜在匹配的可延长
尾部，暂留。每次有新字节到达，原最后一个字节变为可决位置，决策只依赖已到达
的字节序列，与它们分几次 Write 到达无关。匹配一旦发出就不再改变（后续字节只
能延长尚未发出的匹配：每次新字节到达时重试当前可决位置的匹配）。Flush 强制把
`buf` 全部排空（按当前最优匹配/字面量发出并跳过已覆盖字节），再写刷新标记，
故解压器可还原截至 Flush 的全部输入且不多；空 Flush 不写任何字节。Flush 后窗口
保留全部历史，后续匹配仍可越界回指。

## 2. 重叠回指的复制（第 2 条）

d<l 时一次性拷贝 src 与 dst 重叠，块复制读到的是原始旧字节而非刚复制的新字节，
结果错误。正确语义等价于逐字节 `out=out[n-d]`。实现采用跳长拷贝：每次复制
`min(d, l-n)` 个字节（一块内部源在目标之前，不重叠），复制后下一块的源正好落
在上一块刚写出的区域内——得到与逐字节相同的重复展开效果，迭代次数 ≤ l/d 而非
l。对 d=1,2,3 有专门表驱动测试钉住。

## 3. 两种「距离过大」的区分（第 7 条）

d>窗口容量是格式级违约：合法压缩器永远不可能产生它，因为匹配器只检索仍在环形
窗口内的位置（预置字典也在窗口内）。d≤窗口但 d>已输出（+预置字典）字节数是
引用级违约：检查顺序上先判 d==0，再判 d>窗口，再判 d>可用历史，故三类可区分。
并行块预置字典不改变判定：解压仍是一个连续流，预置字典字节只是额外的可用历史
（它们既在窗口里也计入「可回指字节数」），判定恒为 `d ≤ min(窗口, 已输出+预置)`。

## 4. 并行块压缩的确定性（第 9 条）

块 k 的匹配器输入仅为「块 k 原文 + 块 k-1 原文（作为预置字典装入窗口）」，不读
其他任何块的压缩输出或状态；哈希链是该输入的纯函数，故块输出与执行顺序、
worker 数无关，同一分片方案下逐字节相同（含 END 仅由最后一块写出，块间不插刷新
标记：刷新只承诺解码器即时边界，块切分只是压缩内部的字典衔接，解码流连续）。
主写协程按块序汇接：流头 + 各块记录（末块含流尾）。workers 只改变并发度不改变
分片，1/2/4/8 结果一致。
