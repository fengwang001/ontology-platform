# 设计推导

## 1. 末尾换行策略与空输入

三策略：`Keep`（原样）、`One`（至多一个末尾换行）、`Trim`（全删末尾空行再补一个）。
关键约束：映射只能引用原文已存在的字节，合成字节会破坏 `ToOut(ToOrig(o))==o`
（该式要求 `ToOrig` 单射；若原文无末尾 `\n` 而输出追加一个，则输出新增位置
没有任何原文位置可映射，单射不可能成立）。
因此 `One` 不追加、只折叠：无末尾换行的输入保持无换行；有末尾换行则折叠为恰好一个。
`Trim` 仅当原文出现过任意 `\n` 时补回一个，且补回的 `\n` 映射到原文最后一个换行字节；
正文后无换行（原文无任何 `\n`）不追加。空输入不产生任何字节。三策略在各自像集上
均为不动点，故幂等。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

规范化把原文切成「保留段 R 与删除段 D 交替」。保留段内 `ToOut(i)=o0+(i-i0)`。
对删除段 D=`[a,b)`（其后输出位置为 p，即 D 原本会落入的输出边界），取
`ToOut(i)=p`（i∈[a,b)），并令 `ToOrig(p)=a`（删除段起点）。
于是保留侧：`ToOut(ToOrig(o))=o`；删除侧：`ToOut(ToOrig(p))=ToOut(a)=p`。
两侧 `ToOrig`、`ToOut` 均单调不减。被删的行尾空白映射到行尾换行符**之前**
（p 是该 `\n` 的位置），编辑器光标落在被删空白中时正好停在换行符前；
`\r\n` 的 `\r` 同样映射到输出 `\n` 的位置。

## 3. 空白缓冲超限

选择「拒绝」。强制输出空白会把后续可能证明是行尾的空白提前固化为正文，
一旦下一字节是 `\n`，输出就带上了行尾空白，违反语义 5（幂等/无行尾空白），
且该错误无法回滚；而拒绝是可判定错误 `ErrSpaceLimit`（携带原文偏移），
已输出内容保留，实例进入终态（再写返回 `ErrClosed`），状态完全可解释，
不违反 4/5/6，仅意味着该输入需调大上限后重放。输出字节超限同理拒绝
（`ErrOutputLimit`）。

## 4. par 拼接

切点两侧都独立做「raw 规范化」（不应用末尾策略；段尾待定 `\r` 折叠、
段尾待定空白按丢弃处理），产生 `(输出, span)`，再顺序修正拼接。每个边界只看
相邻两段交界处，修正分两类且互斥：
1. CRLF 跨界：前段末字节为孤立 `\r`（折叠成 `\n`）且后段首字节为 `\n`。
   跨界整体是一个 `\r\n`，但两段已产出 `\n\n`，故删除后段首字节：
   span 用 `DropHeadOne` 去掉后段首个原文位置，输出截掉首字节，再整体平移。
2. 空白跨界：前段关闭时其尾部待定空白被当作行尾丢弃，而后段首字节是普通
   非空白字节（说明这些空白其实在行中）。此时把前段被丢的空白（≤ 缓冲上限，
   数量有界）恢复为输出、并在 span 中改回保留（`KeepTailDropped`）。
   后段首字节为空白或换行时丢弃判定本就正确，无需修正。
段 span 用平移常量 `(inBase,outBase)` 合并；修正范围至多「1 字节 + 上限内空白」，
故有上界。拼接全部段后，在全局结果上应用一次末尾换行策略
（span 提供 `FoldTailNewlines`/`TrimTailNewlines`，合成映射指向原文最后一个换行）。
