# DESIGN — 版本约束求解器推导记录

本文记录题面第三节要求的四处推导。结论先行，论证随后。

## 1. 选哪个版本：候选中满足约束的最高版本

- 不变量 2 要求结果与登记顺序无关，因此选择规则只能依赖**版本自身的全序**，
  不能依赖任何外部顺序（登记序、map 迭代序、声明序）。
- `ver` 提供的比较是语义化版本的全序，任意两个不同版本可比较。全序集合中
  唯一与外部顺序无关的规范选择是「按该序取极值」。
- 取**最高**版本而非最低：与主流生态（cargo/npm 的 maximal 偏好）一致，
  且让「约束不变、新增更高版本」时解单调上移，行为可预期。
- 实现：候选版本在查询时按降序排序后逐个尝试，搜索找到的第一份解即为
  确定性解；排序在求解时进行，登记顺序因此被完全消除。

## 2. 预发布版本：参与，且 1.0.0-beta < 1.0.0

- 比较规则（semver §11）：主/次/修数值比较；同前三元组时，**带预发布的
  版本低于不带预发布的**；预发布标识逐段比较，数值段 < 字母段，数值按值、
  字母按 ASCII，前缀相同则段数少者低。
- `>=1.0.0` **不**包含 `1.0.0-beta`，因为按上述全序 `1.0.0-beta < 1.0.0`，
  区间成员判定必须直接使用这同一个比较函数。
- 理由：不变量 1 要求「解必须真的满足全部约束」且可被独立校验。若求解器
  与校验器各用一套成员规则（例如对预发布特殊豁免），两者会对同一份解给出
  不同结论。唯一自洽做法是**全序唯一、区间成员=比较结果**，预发布版本
  作为普通版本参与候选，不做任何特殊开关。

## 3. 约束求交的边界：端点闭开规则

- 每条约束归约为若干比较子（`>=` `<=` `>` `<` `=`），求交 = 比较子合并后
  取最紧下界 `(lo, loClosed)` 与最紧上界 `(hi, hiClosed)`。
- **空区间判定**：`lo > hi` 为空；`lo == hi` 且非「两端皆闭」为空；否则非空。
- 端点规则表：

  | 交 | 下界 | 上界 | 结果 |
  |---|---|---|---|
  | `>=1.2.0 ∩ <1.2.0` | [1.2.0 | 1.2.0) | 空（等端点非双闭） |
  | `>=1.2.0 ∩ <=1.2.0` | [1.2.0 | 1.2.0] | 非空，点区间 {1.2.0} |
  | `>1.2.0 ∩ <1.2.1` | (1.2.0 | 1.2.1) | 非空（如 1.2.1-alpha） |
  | `>1.2.0 ∩ <1.2.0` | (1.2.0 | 1.2.0) | 空 |

- 论证：版本全序是稠密性未知的离散集，但**区间是否为空只由端点决定**，
  与登记了哪些版本无关——`>1.2.0 ∩ <1.2.1` 作为区间非空（预发布的存在
  证明其间有版本，如 `1.2.1-alpha`）；「是否存在已登记版本落在区间内」
  是求解器的可行性检查，与区间空性分离，两层各自可判定、互不混淆。

## 4. 冲突解释取哪一条：规范序 + 字典序最小渲染

- 不变量 2 要求同一输入解释逐位相同。搜索顺序已确定，但「遇到的第一个
  冲突」依赖实现细节，不够稳；需要一个**只依赖输入**的规范规则。
- 规则分三层：
  1. 单包冲突内：该包被施加的全部约束按规范键排序
     `(来源包, 来源版本, 约束原文)`，根需求排在最前；按此序增量求交，
     取**第一个使交为空的前缀**。由一维区间的 Helly 性质，区间族交为空
     当且仅当存在一对不交区间，故前缀内必能定位到「最紧下界约束 vs
     最紧上界约束」这一对不相容约束作为链尾。
  2. 链身：从链尾约束的来源 `包@版本` 回溯——该包为何被选中？取其被施加
     约束中规范键最小者，递归至根需求；带 visited 集合防循环依赖死循环。
  3. 多分支冲突间：搜索中每次冲突都渲染完整链，最终报告**渲染文本字典序
     最小**者。字典序是解释文本上的全序，与搜索顺序无关，故重复求解、
     打乱登记/声明顺序，解释逐位相同。
