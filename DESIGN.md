# 设计推导

## 代理对错误偏移（严格解码器）

规则：一个高代理 `\uD800–\uDBFF` 必须与其**紧后**的低代理 `\uDC00–\uDFFF` 配对，共同表示一个
BMP 外命题点；两者之间不允许任何内容。任何违规都拒绝，偏移规则如下：

- 孤立低代理（高代理未出现就遇到低代理）：错误归属该低代理自身，偏移指向它的 `\`。
  它是一个独立的、自足的非法单元。
- 高代理之后不是紧接的合法低代理（EOF、普通字符、其他转义、十六进制位数不足等）：
  错误归属这个**高代理单元**，偏移指向它的 `\`。

理由：低代理是“自己即非法”；高代理是“开出了一张必须立即兑现的期票”，未兑现时错误根源在
期票本身。因此 `"\uD83Dx"`、`"\uD83DA"`、EOF 三种情形偏移都指向 `\uD83D` 起点；而
`"\uDE00\uD83D"` 的偏移指向前面的 `\uDE00`（低代理先违规，高代理在其后才被检查）。
该归属规则独立于输入切分方式，因此流式任意切分都给出同一偏移。

`"\uD83DA"` 不能解释为“替换字符加 A”：严格解码器在看到高代理后必须继续消费紧接的
`\uXXXX` 作为低代理候选；`A` 不是转义起始，期票无法兑现即报错。用 U+FFFD 静默降级属于
有损恢复，违反“非法输入必须显式拒绝、不得静默替换”的语义。

## 最小转义、单射与唯一性

`Encode` 只转义 `"`、`\`、U+0000–U+001F：前两者必须转义否则破坏字面量结构；控制字符按
RFC 8259 必须转义。`\b\f\n\r\t` 用短形式，其余控制字符用 `\u00XX`（小写）。

- U+001F 必须转义：未转义控制字符是语法错误，JSON 只豁免空白控制符 U+0009/0A/0D。
- U+007F 不必转义：它是 DEL 而非 C0 控制字符，不属于 U+0000–U+001F，原样合法。
- `/`、非 ASCII（含 U+2028/U+2029）原样：字符串内无需转义即合法。

单射且唯一：每个码点的输出由其值唯一决定（短形式集合与 `\u00XX` 不相交，四位小写十六进制
无歧义），且转义产物以 `\` 开头而未转义字节不可能是 `\`（`\` 必被转义为 `\\`），因此解码后
无法混淆“原样字符”与“转义展开”，码点级映射可逆，整个编码是单射；同一 s 只有一种输出，
故最小转义字面量满足 `Encode(Decode(y)) == y`。

## 测试对照表

<!-- 最后补 -->
