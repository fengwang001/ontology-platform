# DESIGN — 熔断与舱壁隔离调用保护器

## 1. 分层与调用顺序：熔断在前，舱壁在后

一次受保护调用依次经过：熔断判定 → 舱壁取额度 → 超时包装 → 真实调用 → 统计。

论证熔断必须在舱壁之前：

- 熔断打开意味着下游已知故障，此时应立刻拒绝。若舱壁在前，被拒请求也要先占用/排队并发额度，故障期间额度会被"注定要失败的请求"耗尽，反而拖垮尚未来得及熔断的健康流量。
- 熔断在前时，打开期所有拒绝都不触碰舱壁，额度占用恒为 0（测试断言），下游恢复后半开探测能立刻拿到额度。
- 代价是熔断判定成为每请求一次的临界区，但它只做状态读取与计数，远轻于真实调用，可接受。

## 2. 熔断计数只算"真实调用"的推导

失败率 = 失败数 / 真实调用数。真实调用 = 通过熔断判定且拿到舱壁额度、真正发起下游调用的请求。

若把熔断拒绝计入失败率：打开后每个被拒请求都推高失败数，失败率被自己的拒绝拉满到 100%，冷却结束进入半开时若按"失败率仍超阈值"判定会立即回到打开，永远无法恢复——这是自反馈死锁。

同理舱壁拒绝也不计入：它是本地过载信号，不代表下游故障。

因此正确口径：熔断拒绝与舱壁拒绝只计入各自的拒绝计数器，不进入失败率分子分母。测试构造打开后连续 1000 次被拒，断言失败率统计不变，且半开后一次成功即回关闭。

## 3. 额度必须在所有路径上归还

额度是有限资源（N 个），泄漏一个就少一个，泄漏到 0 后舱壁全拒、服务假死。真实调用有四条出口路径：

1. 成功返回；
2. 返回错误（可重试 / 不可重试）；
3. 超时（调用未在时限内完成，包装器先行返回）；
4. 调用 panic（捕获后转为失败）。

四条路径都必须归还额度。实现上用 `defer release()` 包住真实调用段，panic 在 defer 链中 recover，保证任何出口都只归还一次。测试在四条路径各跑 1000 次，断言结束后可用额度回满。

## 4. 不可重试错误是否计入熔断

定义：计入。论证：

- 熔断的目的是"下游不健康时停止施压"。不可重试错误（如参数错误 4xx 语义）虽然重试无意义，但持续大量出现同样说明调用方/下游契约已坏，继续放行没有收益。
- 若不计入，攻击面是：下游持续返回不可重试错误时熔断永不打开，流量无保护。
- 本题不实现重试，故只需在统计中按类别细分（可重试 / 不可重试 / 超时），三者都计入失败数与失败率。类别细分之和必须等于失败总数（口径自洽等式之一）。

## 5. 熔断状态机

- 关闭 → 打开：连续失败数 ≥ 阈值，或（样本数 ≥ 下限 且 失败率 > 阈值）。两条件独立实现、分别测试。
- 打开 → 半开：距打开时刻 ≥ 冷却时长（注入时钟判定）。时钟回拨（now < 打开时刻）返回可判定错误，状态不变。
- 半开 → 关闭：允许的探测次数内全部成功。
- 半开 → 打开：任一探测失败立即回到打开，冷却时长 ×2（有上限）。
- 半开并发控制：同时最多放行"探测数"个在途调用，其余直接拒绝，不计统计失败。
- 并发迁移只发生一次：状态迁移在互斥锁内做 compare-and-swap 式判定，迁移计数器断言为 1，冷却不重复加倍。

## 6. 舱壁精确语义

- 每个下游独立一个舱壁实例，互不影响。
- 并发额度 N：在途真实调用数恒 ≤ N，内部维护历史峰值计数器供测试断言。
- 等待队列上限 Q：第 N+Q+1 个请求立即返回可判定错误（注入时钟断言时间未推进）。
- 等待中被取消（context）：从队列移除、不占用额度。
- N=1 退化为串行，仍正确。
- N、Q ≤ 0 构造报错。

## 7. 统计口径自洽

`stat` 报：总请求数、真实调用数、熔断拒绝数、舱壁拒绝数、成功数、失败数（按可重试/不可重试/超时细分）。任意操作序列后恒成立：

- 总请求数 = 成功数 + 失败数 + 熔断拒绝数 + 舱壁拒绝数
- 真实调用数 = 总请求数 − 熔断拒绝数 − 舱壁拒绝数
- 各类别失败数之和 = 失败数

## 8. 错误分类与可判定错误

四类哨兵错误用 `errors.Is` 区分：`ErrOpen`（熔断拒绝）、`ErrBulkheadFull`（舱壁拒绝）、`ErrTimeout`（超时）、`ErrClockRewind`（时钟回拨）。`classify` 把调用返回的错误分为可重试 / 不可重试 / 超时三类。
