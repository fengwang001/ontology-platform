# 连接顺序代价模型与计划选择器 — 设计推导

## 1. 目标与范围

输入：n 张表（行数、各列 NDV、等宽直方图）与等值连接谓词；输出：按子集动态规划得到的
最优二元连接树，及其逐节点基数/代价与异常标注。仅用标准库，统计为只读内存对象。

## 2. 代价与基数公式

- 扫描：`scan(T) = R_T`（每行单位代价）；输出基数 `R_T`。
- 等值谓词 `L.x = R.y` 选择率：`sel = 1 / max(NDV_Lx, NDV_Ry)`（PK/FK 经典估计）。
- 两子树间 m 个谓词：`sel = Π sel_i`，输出基数 `|L|·|R|·Π sel_i`；
  m>1 时标注「独立性假设」，即假设谓词彼此不相关。
- 连接代价：`join = |L|·|R|`（与嵌套循环/哈希探测输入规模成正比，与谓词数无关）。
- 计划总代价 = 各扫描代价 + 各连接代价之和。所有运算只读取统计元数据，绝不读取行数据
  （目录提供行读取计数器，选择器调用后必须为 0）。
- 适用范围：等值内连接；非等值/外连接不在范围内。缺列统计时选择率回退为常量
  `0.1` 并标注「估计不可靠」，不中止。

## 3. 并列打破：相对误差判等 + 名字序

代价是 float64 上的连乘连加。单次四则运算相对误差不超过机器精度
`u = 2^-52 ≈ 2.22e-16`；一棵计划至多 O(n^2) 次乘加，n≤20 时累计相对误差上界
`γ ≈ k·u < 5e-14`。取判定阈值 `ε = 1e-12`：它比累计舍入上界大 20 倍以上
（吸收 ULP 级漂移），又远小于真实代价差——基数为整数行数，任何真实不同的计划
其相对差都 ≫1e-12。

判定：`equalCost(a,b) ⇔ |a-b| ≤ ε·max(|a|,|b|,1)`（加 1 覆盖零代价空表）。
等代价时取「叶子表名序列」字典序更小者：把计划左展开为表名切片，逐元素字典序比较；
序列完全相同则保留枚举到的前者。子集用掩码枚举，且掩码位号按**表名排序后**分配，
与登记顺序无关，因此打乱登记顺序结果逐字节一致。

## 4. 笛卡尔积推迟规则

对子集 S 的划分 S=L∪R，当 L、R 间无任何谓词时该连接为笛卡尔积。允许条件（闭包规则）：

> L 与 S 之外的任何表都没有谓词，且 R 与 S 之外的任何表都没有谓词。

即两侧都已是「封闭」连通分量。

- 链式 A-B-C-D（连通图）：任何 S⊊V 的笛卡尔切法都因外侧仍有邻边被封闭规则拒绝；
  S=V 时链式图的任意切分都有跨边，也不是笛卡尔。故全程零次笛卡尔。
- 两段 A-B、C-D（不连通）：S⊊V 的切法总有一侧向外连边（如 {ab}|{c} 时 c 还连 d），
  被拒绝；仅 S=V 且切分 {ab}|{cd} 满足封闭且无跨边，恰在最后一步出现一次。

## 5. 动态规划与复杂度

按子集自底向上：单例子集=扫描；对 |S|≥2 枚举「含 S 最小元素」的一半划分
（避免左右镜像重复），取两侧最优计划，按第 4 节放行连接，用第 2 节公式算代价，
按第 3 节规则保留最优。

- 考察子集数 ≤ 2^n；划分数 ≤ 3^n（每个元素属于 L/R/不在 S 三种状态）。
- n=12：2^12=4096，3^12=531441，而 12!=479001600，计数远小于全排列 n!。
- 表数硬上限 `MaxTables=18`（n=20 直接返回 ErrTooManyTables，中间表数不超过 2^18）。

## 6. 统计异常

哨兵错误 `ErrMissingStats`、`ErrStaleStats`、`ErrCorruptStats`，均可用 errors.Is 判定，
错误信息带表名/列名。

- 缺失：列无统计 → 选择率用 0.1，标注「估计不可靠」，不报错中止。
- 过期：`|目录行数-统计行数| > 10%·max(统计行数,1)` → 标注，基数按目录行数校正。
- 损坏：直方图桶计数之和 ≠ 统计行数，或桶上界非严格递增 → ErrCorruptStats，选择中止。
- 自连接：谓词两侧同表 → 明确拒绝（ErrSelfJoin）；零表 → ErrNoTables。

## 7. 并发

目录与统计注册后只读；选择器状态为每次调用私有（掩码→最优计划的表、计数器），
多协程并发 Select 共享同一目录安全，-race 干净。

## 8. 包划分

stats（统计与选择率）、cost（公式与默认值）、catalog（登记/异常检测/行读计数）、
plan（节点与 DP）、explain（逐行渲染）、cmd/demo（验收演示）。
