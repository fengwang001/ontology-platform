# 设计推导

## 1. 末尾换行策略（空输入与只含换行）

规范化体 B(x)：展开行尾、删行尾空白，但不动末尾换行。策略只作用于 B：
- Keep：输出 B，末尾原样。
- EnsureOne：若 x=="" 输出 ""；否则剥掉 B 末尾全部 `\n` 后补恰好一个 `\n`。
- TrimEmpty：若 x=="" 输出 ""；否则剥掉末尾全部空行只保留一个 `\n`（有末尾换行时）；x 本不以换行结尾则只删不补。

样例（B 与 Keep 相同）：

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

空输入选 `""`：三种策略都是幂等函数 F，需 F(F(""))=F("")；若选 `"\n"`，则空→`\n` 可幂等，但“空文档被凭空加字节”，且 EnsureOne 的“一个末尾换行”语义对没有任何行的文档无意义，故选 `""`。其余选择均满足 N(N(x))=N(x)：EnsureOne 先剥后补；TrimEmpty 的输出最多只以单个 `\n` 结尾（或无换行），再应用自身不动；Keep 的输出不含 `\r` 与行尾空白，再规范化是不动点。

## 2. 被删字节的 ToOut

ToOrig(o) 是满射到“存活原文字节”的非减函数；ToOut 是其单侧逆：存活字节映射到其输出位置；被删区间内的原偏移 i 映射到“紧随该删除区间之后的存活输出位置”（删除位于输出末尾时取输出长度）。
- 行尾空白全在 `\n` 前，被删后光标应落在换行符**之前**（行尾逻辑位置），故 ToOut(i)=该 `\n` 的输出偏移；与“之后”相比，之前保持 ToOut 非减且不跨过换行（跨换行会跳到下一行行首）。
- `\r\n` 中 `\r` 删除、`\n` 存活，ToOut(i of \r)=`\n` 的输出偏移，与 ToOut(i of \n) 相同。ToOrig 对该输出偏移给出 `\n` 的原偏移，于是 ToOut(ToOrig(o))=o（被删点不在 ToOrig 值域，逆等式只需对输出偏移成立）。

边界说明（鸽巢）：输出严格长于原文只可能发生在 EnsureOne 对无末尾换行的非空输入追加 1 字节时；此时两个相邻输出边共享原终点，恒等式对所有输出偏移 o∈[0,len) 与原偏移像点成立（实现中追加区间左右边都映射到原终点）。

## 3. 空白缓冲超限

未决空白必须缓冲：提前输出违反第 5 条（再次规范化时它变成行尾空白会被删）；提前丢弃违反第 4 条（切点不同结果不同，且可能误删行内空白）。强制判定为“非行尾”同样违反幂等——缓冲长度与切分方式相关，N(x) 会依赖配置阈值而不再是不动点。因此选择**拒绝**：返回带原偏移的具名错误 `ErrWSPending`，已输出内容与映射保留，进入终态，之后 Write 返回 `ErrClosed`。拒绝不改变任何已提交字节，第 4/5/6 条不受影响。

## 4. par 拼接

切点只切字节。每段独立处理时，段尾最多携带一个待定 `\r` 和一串未决空白（长度 ≤ 缓冲上限，有上界），段首不知道这些前缀。做法：段 k 规范化其原始切片得到 map_k；再按 k=1..K-1 修正——从段 k-1 的输出中回滚其尾部未提交部分（待定 `\r` 已按行内字符 `\r` 输出、或未决空白已被 Close 当行内输出），把这段“尾部原字节前缀 + 段 k 原切片”重新喂给规范化器得到 seam 结果，替换两段衔接处。上界：回滚的原字节数 ≤ 1+WSMax，每道缝代价为常数规模。坐标平移：段 k 原区间加 `sum(len(raw[0:k]))`，输出区间加拼接后的输出前缀长；各段 span 平移后按序合并（相邻同性质区间自动坍缩，故无删除的纯 `\n` 文本区间数为常数）。全部 K 段先用 K 个 goroutine 算出段内结果；末尾策略在拼接后的整体上最后施加（只改尾部，等价于整体一次处理）。
