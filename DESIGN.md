# 带补偿与断点续跑的 DAG 工作流编排器 — 设计推导

包依赖单向：`graph` → `journal` → `step`(依赖 journal) → `policy`(依赖 step) → `orchestrate`。
时间只走注入时钟（接口仅 `Sleep(d)`）；状态只在进程内存，journal 是进程内字节缓冲（用帧编码模拟"落盘"，
因此半条记录与真实文件上的 torn write 同构）。

## 1. 逆拓扑序补偿（语义 2、3、4）

边 `A→B` 表示 "A 是 B 的依赖"：执行拓扑序为 A 先 B 后（B 可能观察/消费 A 建立的资源）。
补偿是执行的逆操作，因此必须沿依赖方向反向、按逆拓扑序进行：B 先补偿，A 后补偿。

**为什么必须等 B 补偿"完成"再补偿 A（不能只先发起，也不能并发）：**
补偿函数撤销的是本步骤建立的资源，而该资源可能正被下游步骤的补偿所依赖。
并发反例：A=创建数据库连接并建表，B=在表中写入数据并拿到句柄，C=基于数据发报表，C 失败。
若 A、B 的补偿并发，A 可能先 `DROP TABLE`/断连，而 B 的补偿此刻还要用该句柄删除它写入的那批行，
于是 B 的补偿报"表不存在/连接已关闭"而失败——一次本该成功的清理被并发顺序破坏，甚至留下脏行。
实现上：补偿按 `topo` 的逆序逐步骤执行，前一步补偿（含其重试）返回后才开始下一步；
层内执行并发、补偿严格串行，顺序因此可被精确断言。

**恰好一次**：只有 journal 中存在 `ExecDone` 的步骤才进入补偿集合；失败步（只有 ExecStart/ExecFail）
与从未调度的步骤直接跳过。每步补偿前由状态机保证只经历一次 `Compensated` 迁移，
故"补偿次数"恒为 0 或 1。某步补偿失败不中止循环，错误聚合成 `CompensationErrors`（step→err），
其余步骤照常补偿。

## 2. 崩溃点枚举、journal 形态与半条记录（语义 5、6）

每条记录的写入点只有一个函数 `appendRec`，它有两个可注入崩溃钩子：**写前崩**（帧尚未进入缓冲）
与**写后崩**（帧已进入缓冲，但在它被赋予任何内存语义之前）。记录种类：
`ExecStart(k) / ExecDone(k) / ExecFail(k) / CompStart / CompDone / CompFail`。

逐个崩溃点的形态与恢复：

| 崩溃点 | journal 形态 | 重放判定与恢复 |
|---|---|---|
| ExecStart 前 | 无该帧 | 步骤仍 Pending，重新调度执行。 |
| ExecStart 后、动作前 | 有 Start(k)，无 Done/Fail | Start 不代表成功：状态 Running，恢复时重跑该步（幂等动作）。 |
| ExecDone 前 | 有 Start，无 Done | 同上，重跑；Done 才是"已完成不得重跑"的唯一凭据。 |
| ExecDone 后 | 有完整 Start+Done | 状态 Done，恢复时**绝不重跑**，执行计数不增。 |
| ExecFail 前/后 | 有 Start，Fail 可有可无 | 无 Done 即未成功：重试策略恢复（Fail 记录 attempt，可继续重试或进入补偿）。 |
| CompStart 前 | 无补偿帧 | 该步重新补偿一次。 |
| CompStart 后/CompDone 前 | 有 CompStart 无 CompDone | 补偿未证实完成，重放后重补一次（补偿幂等）。 |
| CompDone 后 | 有完整 CompStart+Done | 状态 Compensated，不再补偿。 |
| CompFail 前/后 | Fail 可有可无 | 补偿未成功，恢复时重试该步补偿并重新聚合错误。 |

**半条记录识别**：帧格式固定为 `[4B 总长(大端)][payload][4B CRC32(payload)]`。
重放时：读不满 4B 长度 → 丢弃尾部；声明长度超出剩余缓冲 → 丢弃尾部；CRC 不匹配 → 丢弃该帧及其后全部字节
（崩溃只可能发生在文件尾，前缀必完整）。被丢弃的尾部恰好是没写完的那一帧，状态由其前面的完整帧决定，
因此丢弃后状态自洽、确定。重放是 `[]byte → 纯状态` 的折叠：同一份 journal 折叠任意多次结果逐字节相同。

## 3. 线性重放与节点访问计数（语义 9）

重放分两趟：(a) **一次**遍历图节点，构建 `id → 可变状态槽` 的索引（这是唯一触碰图的地方，
节点访问数恰为 N，与 L 无关）；(b) 顺序折叠 L 条记录，每条只做 `slot[id]` 的 O(1) 状态迁移，
不再碰图。故节点访问次数上界 = N（另有一次拓扑层计算，也是 O(N+E)、只与图有关），
不随 L 增长。测试用 N 固定、L=100 与 L=10000 两组（同一两步图用不同 MaxRetries 产生不同轨迹长度），
断言两次重放 `replayNodeVisits` 相等。

## 4. 对外只有两种终态（语义 8）

编排器用一把 `sync.RWMutex` 保护"阶段+每步状态+计数"。终态只有 `Complete`（全部步骤 Done）
与 `Compensated`（补偿流程跑完：Done 的步骤均已补偿，可能附带 CompensationErrors）。
**临界区边界**：调度器在锁外并发执行动作（拿动作结果的那一刻才短暂加锁写状态+追加 journal）；
一旦某步终局失败，置阶段为内部 `compensating`（非终态），随后的整个补偿循环持锁进行——
因此查询要么看到失败发生前的运行快照（所有已完成步仍为 Done 的自洽快照），要么在补偿全部结束后
看到 `Compensated` 终态，永远观察不到"一部分 Done 一部分 Compensated"。`Snapshot()` 取一次
RLock 拷贝全部读数，连续两次必完全相同，且不推进任何状态。

超限（语义 10）：三个可判定哨兵错误 `ErrTooManySteps / ErrJournalFull / ErrConcurrency`，
均在任何状态被修改之前返回（构造期校验图规模；append 前校验长度，超限帧不入缓冲）。
