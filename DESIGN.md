# 条件请求前置判定器 — 设计推导

## 包划分与依赖方向

- `etag`：实体标签解析 + 强/弱两种比较，零依赖。
- `httpdate`：IMF-fixdate 时间戳（`Mon, 02 Jan 2006 15:04:05 GMT`）解析，零依赖。
- `precond`：求值顺序与结果裁决，单向依赖以上两包，反向依赖不存在。

## 1. 为什么 If-Match 用强比较、If-None-Match 用弱比较

If-Match 服务于条件写与断点续传：客户端要确认自己持有的字节与服务端
**逐字节一致**，才能安全地做增量更新或拼接后续分片。强比较要求双方都非弱
标签且 opaque-tag 完全相等；任一方带 `W/` 前缀即判不等。

If-None-Match 服务于缓存再验证：中间代理可能对表示做无损变换（重新压缩、
转码），字节变了但语义等价，弱标签正是为此存在，故用弱比较。

反例：若两边都用弱比较，断点续传时代理把响应重新压缩了一遍，字节流已经不
同，弱比较却仍判定"同一个表示"，客户端会把新字节拼到旧内容尾部，得到损坏
的文件，且双方都以为续传成功。同理，If-Match 用弱比较会让条件写在表示被
变换后误判通过，造成 lost update。

## 2. 为什么 If-Match 存在时忽略 If-Unmodified-Since

RFC 7232 §6 定义的是**优先级**而非叠加：先判 If-Match，它缺失时才轮到
If-Unmodified-Since。两个头部表达同一层意图（"我基于哪个版本操作"），ETag
版本比秒级时间戳版本更精确，显式给出 ETag 就意味着客户端放弃了时间戳判断。

反例（叠加判定出错）：资源 ETag 为 `"v2"`，最后修改于 T2；客户端发
`If-Match: "v2"` 与 `If-Unmodified-Since: T1`（T1 < T2，例如客户端保存了
旧的时间戳）。规范结果：If-Match 匹配 → 继续执行。叠加结果：T2 晚于 T1 →
前置条件失败。两者相反，叠加版让精确的 ETag 判断被粗糙的时间戳误杀。

## 3. 为什么同一个"匹配"在读方法与写方法下结果不同

If-None-Match 匹配表示"客户端已有的就是当前的"。对 GET/HEAD，正确响应是
304 Not Modified：让客户端复用缓存副本，省掉消息体。对 PUT/POST/DELETE 等
写方法，客户端的意图是"仅当资源不是我认为的那个版本时才动手"（典型如
`If-None-Match: *` 的"仅当不存在才创建"），匹配意味着前置条件不成立，必须
以 412 中止，否则会发生重复创建或覆盖他人更新。两种结果必须可区分：304 不
含消息体、指示缓存复用；412 指示本次操作被拒绝。

## 4. 相等时刻的归属推导

HTTP 日期只有秒级精度，"相等"实际覆盖一整秒。If-Modified-Since 的典型取值
就是上次响应的 Last-Modified，相等是最常见的"没动过"情形；若把相等算作已
修改，每次缓存再验证都会回全量响应，缓存机制失效。故规范把相等归入"未修
改"：只有 `ModTime.After(since)` 才算已修改。对称地，If-Unmodified-Since
相等算未修改、条件通过。两条方向相反的规则共用同一个 `After` 判断，只是
取反位置不同。

## 其他决策

- 解析失败的条件头部按缺失处理（忽略该头部），与 RFC 7232 的 "MUST ignore"
  一致；空列表（只有逗号）属于语法错误，同样走忽略路径。
- 注入时钟仅用于排除未来日期（日期晚于 now 的条件头部无效）；不带任何条件
  头部时保证零次调用。
- If-Modified-Since 不按请求方法门控（RFC 7232 仅限 GET/HEAD），按任务规格
  对所有方法统一求值。
- 求值是纯函数：输入按值传递，不修改任何传入结构，同输入必得同结果。
