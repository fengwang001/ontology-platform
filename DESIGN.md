# 结构化日志脱敏与一致性采样设计

## 边界与模型
- 记录由级别、追踪 ID 和嵌套字段组成；字段值仅支持映射、数组、字符串、数字、布尔、null。
- 映射为 `map[string]any`，规范序列化用 JSON，映射键按编码顺序排序，作为“非目标字段逐字节不变”的基线。
- 构造记录时遍历对象身份；映射重复出现即构成循环引用并拒绝，数组可重复包含同值但不构成循环。
- 嵌套深度从根映射计为 1，超过 32 拒绝；深度计数同时限制解析与脱敏遍历，避免栈失控。
- 空记录合法；空字符串值合法。

## 路径消歧
- 路径段以点分隔，例如 `a.b.c` 表示根键 `a` 下的键 `b` 下的键 `c`。
- 字段名可直接包含点号；为与嵌套路径消歧，反斜杠转义点：`a\.b` 表示根键字面量 `a.b`，`x.a\.b` 表示 `x` 下的 `a.b`。
- 反斜杠本身写作 `\\`；未转义的点永远是分隔符。
- 路径任一层不是映射，归类为类型不符，不匹配后续段。
- 路径不存在与类型不符均不是错误：规则只是不生效。
- 数组不作为命名路径段；数组内每个元素继续递归，因此规则可作用于数组内嵌套映射。
- 同一规则路径若同时能解释为字面量键和嵌套路径，优先匹配字面量键；这是唯一的确定性解释。

## 规则编译
- 规则包含字段路径、方式（替换/哈希/截断）和截断长度；编译期校验未知方式、非法路径、非正截断长度。
- 同一路径只能有一条有效规则；替换与哈希等冲突在编译期返回冲突错误，错误中包含两条规则。
- 规则集编译为路径 trie，并记录每个 trie 节点对应的规则。
- 处理记录时沿字段树和 trie 同步下行；每个字段最多触发一次当前节点和下行查找，因此路径匹配次数为 O(字段数+记录常数)，与规则总数无关。
- trie 命中由包内计数器记录，测试通过导出的调试读取函数观察；生产逻辑不依赖该计数器。

## 脱敏语义
- 替换：输出固定串 `[REDACTED]`，不保留原长度。
- 哈希：对 UTF-8 字节计算 SHA-256，输出十六进制；同值稳定、不同值强抗碰撞、结果不等于明文。
- 截断：保留前 N 个 rune，追加 `…(truncated)`；不足 N 时不追加。
- 脱敏沿映射和数组递归执行；字符串值若与任意已命中原值相等，也按同一规则处理，从而覆盖跨路径、数组和引用式重复值。
- 未命中字段不得修改；命中字段只替换其叶子值。以规范 JSON 比较所有未命中路径。

## 一致性采样
- 采样目标是保留完整追踪链，而不是保留固定比例的独立记录。
- 若决策使用每条记录的随机数、到达序号或时间，同一追踪 ID 的不同记录会得到不同结果，链必然可能断裂。
- 因此决策函数只能输入追踪 ID、采样率和规则版本：对追踪 ID 计算一次确定性 FNV-1a 哈希，将哈希映射到固定区间。
- 单次决策哈希计算恰好一次，时间 O(len(traceID))，额外状态 O(1)；规则集固定后结果与记录内容、顺序、时间、并发无关。
- 采样率 1 全保留；0 全丢弃；`(0,1)` 间按确定性哈希区间保留。
- 追踪 ID 缺失或为空串时没有可归并的链，采用保守策略：非 error 丢弃，error 强制保留并标记链不完整。
- error 及以上级别绕过采样丢弃决策；若其链采样结果为丢弃，输出记录附加 `_incomplete_trace=true`。
- 完整保留链中的 error 不打标记；被丢弃链中的非 error 不输出。

## 落盘格式与损坏分类
- 文件头为魔数 `ONTLOG01`（8 字节）+ 版本 uint8 + 保留 3 字节 + CRC32 uint32（小端），共 16 字节；头 CRC 覆盖前 12 字节。
- 每条记录帧为：体长度 uint32（4 字节，小端）+ JSON 体 + CRC32 uint32（4 字节，小端）；CRC 覆盖长度前缀和 JSON 体。
- 读取先验证头，再按帧恢复；返回已确认记录和可判定错误。
- 截断分类：短于 16 为头部不完整；头 CRC 错为头部损坏；长度字段不足 4 为长度前缀不完整；声明体不足为记录体不完整；体够但 CRC 字段不足或 CRC 错为 CRC 不匹配。
- 最大可恢复前缀由损坏点前已完整校验的帧决定。

## 并发与错误
- 规则集、采样器和脱敏器不可变或无共享修改；计数器使用原子操作。
- 并发工作协程只处理独立记录，输出按稳定的输入记录 ID 收集；因此集合及顺序都与串行一致。
- 循环引用、深度超限、规则冲突、文件损坏分别提供哨兵错误，均支持 `errors.Is`。
