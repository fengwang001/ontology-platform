# 设计推导

## 0. 映射模型（span）

输出按字节切成不相交、有序、全覆盖的段，每段是三种之一：
copy（`orig` 与 `out` 等长，`ToOrig` 恒等）、delete（只占原文）、insert（只占输出）。
相邻同类 copy 合并。
- `ToOrig(o)`：二分定位输出段；delete 不可达；insert 段（合成的最终换行）
  取其左邻原文位置（即该行最后一个非删字节之后）；否则 `o - out0 + orig0`。
- `ToOut(i)` 是「使 `ToOrig(o) >= i` 成立的最小输出偏移 o」（lower_bound）。
  即映射落在 i 处或越过 i 的第一个输出点。两函数因此互逆：
  `ToOut(ToOrig(o)) == o`，且都单调不减；定义域/值域都含终点 `len`。

## 1. 末尾换行策略

先规范主体（行尾空白已删），再看原文末尾换行个数 n：
- `TailKeep`：保留 n 个。
- `TailOne`：无尾换行则补一个；有则裁成一个。
- `TailTrim`：有则裁成一个；无则保持无（区别在无尾非空输入）。

| 输入 \ 策略 | TailKeep | TailOne | TailTrim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

**空输入选择 `""`**：若 TailOne 对空输入输出 `"\n"`，二次规范化得到 `"\n"`，
幂等也成立，但规范化不再保持「空→空」这一长度零界，且会凭空产生原文中不存在的
行，光标映射退化为一个 insert 段。选 `""` 更自然：三种策略对空输入一致，
且输出恒为自身。TailOne 对「非空无尾」输入（如 `"a"`）补 `"\n"`：补出的内容
自身规范后仍是一个换行，故幂等成立。三个策略的输出都不含 `\r` 与行尾空白，
再次规范化无任何变化，幂等成立。

## 2. 被删字节的 ToOut

以 `"a \n"`（删除一个尾随空格）为例：
copy a(o0)、delete 空格(i1)、copy `\n`(o1)。
`ToOut(1)=1`、`ToOut(2)=1`：被删空白与随后的换行映射到**同一个输出点**，
即换行符 `o=1` 之前的位置（光标停在行尾、换行符前），符合编辑器直觉
（被删空白就在行尾，光标不会被甩到下一行）。
`\r\n`：`\r` 删除、`\n` 保留，`ToOut(crPos)=ToOut(nlPos)=nl` 的输出偏移，
同理落在换行符前。规则即 §0 的 lower_bound：被删区间
`[i0,i1)` 全部映射到「删除之后第一个非删原文字节」的输出偏移；
若删除发生在流末（如末尾空白），映射到输出终点。

## 3. 空白缓冲超限

未判定空白必须缓冲：提前输出则之后出现 `\n` 时无法删除（违 §2/§5）；
提前删除则之后出现普通字符时无法恢复（违 §4）。两种「事后补救」都会让结果
依赖切分点。故选择**拒绝**：超过 `MaxPending` 立即返回可判定哨兵错误
`ErrPendingLimit`（带已消费原文偏移），已输出内容保留，进入终态
（再写返回 `ErrClosed`）。拒绝结果本身不再要求幂等（调用方不会把拒绝当成功输出），
而所有成功完成的规范化仍满足 §4/§5/§6。输出总上限 `MaxOutput` 同理：
试图越过即 `ErrOutputLimit`（成功路径不受影响，语义全部保留）。

## 4. par 拼接修正

每段以「保留待定状态」模式跑：段尾悬挂 `\r` 不结算（留给下一段开头的 `\n`
配对）；段尾未判定空白保留输出（因为本段不知道后面是不是行尾）。
各段先按字节偏移切成 `[0,k1),[k1,k2)…`（偏移都是边界），K 个 goroutine 跑
`norm`，得到 `(body, map)` 与一个**签名**：段末是否悬挂 `\r`；去掉悬挂 `\r` 后
尾部未断空白长度 w（只有空白时 w 为该串长度）；以及首字节是否 `\n`。

从右向左决定段尾空白去留：虚拟 EOL = 右邻签名（是换行；仅空白签名视作「右邻
决定」并向右传递）。则：
1. 若本段签名为悬挂 `\r` 且右段首字节是 `\n`：右段删其首字节（映射删该
   `\n`？否——规范语义是删 `\r`）：从本段同时删掉末尾 `\r` 的原文与输出各 1
   （`DropTail(1)`），右段保留 `\n`，即合并成一个 `\r\n`。
2. 若本段（删除可能的悬挂 `\r` 后）尾部 w 个字节全是空白、且右邻是换行签名：
   从本段同时删除尾部 w 个原文与输出（`DropTail(w)`）。
3. 否则保留。

映射拼接：段内段的 `orig/out` 坐标加上本段起点的全局平移（Builder.Merge 注入）；
删除用「原文输出各减同样多」的 tail drop，坐标体系始终一致。
修正范围有上界：每个切点最多回看一个段；被删空白连续跨越切点时，修正量计入
右段、右段又被其右邻判定，整段输入每字节至多被检查一次，总代价 O(n)。
最后在拼接结果上统一施加全局 TailPolicy（用一次 tail 重写完成，见 span.Builder）。
