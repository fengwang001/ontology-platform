# DESIGN

## 1. hunk 头零计数时的行号

统一格式头（形如旧侧逗号新侧的 at-at 头）中 a/c 是该 hunk 在旧/新文件里的首行行号（1 起），b/d 是覆盖行数；
覆盖 0 行时没有"首行"，GNU 取该空区间前一行的行号（0 表示文件开头之前）。
推导三例：x→yx 在文件开头插入，旧侧前一行不存在得 -0,0，新侧首行为第 1 行得 +1；
abc→abXc 在第 2 行后插入，旧侧前一行为第 2 行得 -2,0，新侧首行为第 3 行得 +3；
a→空 删除第 1 行，旧侧首行为 1 得 -1，新侧空区间在文件开头、前一行不存在得 +0,0。
所以坐标 = 空区间前的行数（0 起）。Apply 把插入点放在"第 a 行之后"：a=0 在文件最前，a=2 在第 2、3 行之间。

| 旧侧 | 书写 | Apply 解释 |
|---|---|---|
| b=0 | "-a,0"，a=前一行行号（首行之前为 0） | 不消耗旧行；插入点在第 a 行之后 |
| b=1 | "-a"（省略 ,1） | 消耗第 a 行这一行 |
| b>=2 | "-a,b" | 消耗第 a..a+b-1 行 |

## 2. hunk 合并阈值

结论：两 hunk 间隔未改动行 g，g <= 2C 合并，g >= 2C+1 分开。
各自上下文为 C 行时中间共用未改动行；合并成一个 hunk 最多浪费 2C 行上下文（g=2C 时恰好全部共用，无浪费），
g=2C+1 时必有 1 行纯属冗余上下文，GNU -U C 选分开。
反例 C=1、两改动间 g=2（如 a X Y b 中改 X 与 Y 之间隔 2 行）：阈值取 C 会被错误分开（少了应有上下文）；
阈值取 2C+1 会被错误合并（多出一行上下文）；GNU 实测为一个合并 hunk，故阈值 2C。

## 3. 并列最短脚本的选择

Myers 每条 furthest-reaching D 路径上，同一步先尝试"向下（删除）"再尝试"向右（插入）"，
得到确定性、删优先（delete-before-insert）的脚本。例 ab→ba 脚本为 "-a"、" b"、"+a"
（而非 "+b"、" a"、"-b"）：hunk 内同位置的减行排在加行之前。重复输入因此逐字节一致。

## 4. 偏移后后续 hunk 的查找

前一 hunk 以偏移 k（实际起点减记录起点）成功后，后续 hunk 的查找中心整体平移 k：
后续 hunk 在目标文件中的真实位移等于前序全部 hunk 的（插入数减删除数）累积值；
用平移后的中心 ±F 做窗口，候选仍按"距中心最近、同距靠前"选取。
不累积平移会让靠前的小偏移把后面本可命中的 hunk 误判为超出 F。

## 5. 其他

计数器 = Myers 在对角线上逐行 snake 比较的总步数；D<=上限时该和约为 (N+M)(D+1)，断言取 4 倍余量。
错误四类：ErrMalformed/ErrContext/ErrOutOfFuzz/ErrTooDifferent，均为哨兵错误。
