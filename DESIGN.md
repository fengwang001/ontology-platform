# 设计推导

## 1. 非法单元切分规则

统一规则：从首字节确定期望长度 L（2/3/4）。首字节本身必被吞。
- 第 2 字节不在该首字节的合法区间（含非 10xxxxxx）→ 整个单元只有首字节 1 个，下一字节重新解析。
- 第 2 字节合法、但其后（第 3/4 字节位置）出现非连续字节（非 10xxxxxx）→ 单元 = 已见的连续字节，到此为止（非连续字节重解析）。
- 流结束时仍欠字节 → 单元 = 全部残留（一次 FFFD）。

验证样例：`F0 90 80 41`（F0 第2字节须 90..9F，90 合法；41 非连续→单元 F0 90 80，41 正常）；`E0 80 80`（E0 第2字节须 A0..BF，80 非法→3 单元）；`ED A0 80`（ED 须 80..9F，A0 非法→3）；`C0 AF`（C0 永不合法→2）；`F4 90 80 80`（F4 须 80..8F，90 非法→4）；`E2 82`+EOF（第2合法，欠第3→1 单元）；`80 80`（连续字节各自成单元→2）。

## 2. 区间与吞字节表

| 首字节 | 第2字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 仅首字节 |
| E0 | A0..BF | 仅首字节 |
| E1..EC | 80..BF | 仅首字节 |
| ED | 80..9F | 仅首字节 |
| EE..EF | 80..BF | 仅首字节 |
| F0 | 90..BF | 仅首字节 |
| F1..F3 | 80..BF | 仅首字节 |
| F4 | 80..8F | 仅首字节 |
| 80..BF | —（连续字节） | 1（自身成单元） |
| C0 C1 F5..FF | 无 | 1（自身成单元） |

第2字节合法后，后续位置出现非连续字节：吞掉「首字节+已见连续字节」；EOF 欠字节：吞掉全部残留。

## 3. 切分缓存

UTF-8 最长 4 字节，缓存硬上限 3。C0/C1/F5..FF 在首字节到达即可判定非法；第2字节到达即可用表判定 E0/ED/F0/F4 越界（非最短、代理区、越界 U+110000 均在此排除）；之后每见到非连续字节立刻终结单元，无需等流结束。只有「连续字节数 < L 且输入耗尽」要等 Close，此时一次 FFFD。UTF-16 缓存硬上限 3（BOM 探测至多 2 字节 + 高代理后 2 字节）。

## 4. par 切点对齐

段 i 名义起点 b_i（b_0=0）。真实起点：i=0 为 0；i>0 从 b_i 向前至多回退 3 字节找单元边界——扫到首字节（非 10xxxxxx）即停在它处。段解析区间 [a_i, a_{i+1})，末端遇到非连续字节自然停止，不越界。段 i 抑制首字节偏移 < b_i 的单元，末段全收。单元长度 ≤4，回退 ≤3 必找到首字节：C3|切点|80 80 回退到 C3，前段输出 C3 80 一个非法单元，后段把另一个 80 当连续字节，与单线程一致。回看的 ≤3 字节只额外检查一次，总检查次数 ≤ n + 8·常数。

## 5. 上限断点

Write 只在完整标量输出后记账；缓存中的欠字节不算已消费，返回 n 严格等于已输出标量占用的输入字节数。超限立即停在标量边界（代理对整体保留），返回 ErrOutputLimit；调用方把未消费字节（含残留）喂新实例即逐字节续传。

## 6. 复杂度

每个输入字节至多在「读入」与「缓存前缀重验」中检查 2 次；stream 以非导出字段 checks 计数，par 汇总各段 checks 与边界扫描。单实例非并发安全。
