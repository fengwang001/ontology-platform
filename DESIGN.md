# 设计推导：Java Properties 解析/回写

## 第 3 条：续行只含空白时会怎样

`Properties` 读物理行后，若行尾是奇数个 `\`，则丢掉该反斜杠与行终止符，
继续读下一物理行，并对该续行**先剥掉前导空白**（空格/制表/换页）再拼接。

推导：拼接前的唯一处理是「跳过行首空白」。若下一物理行只含空白（或为空行），
跳过前导空白后什么也不剩，且空白行**不以 `#`/`!` 作为第一个非空白字符**，
所以它不是注释，而是贡献零个字符。结论：

- `k=v\` + 换行 + 空行/空白行（后面无内容）→ `k`→`v`；该空白行整行消失，
  不会插入空格，也不会终止逻辑行（链可继续，直到出现带非空白字符的行）。
- 依据：这正是「续行首部空白被剥掉」规则对全空白行的退化情形——
  全行都是「首部空白」，于是整行被剥光。注释判定只看非空白首字符，
  全空白行没有非空白字符，因此永不构成注释。

## 第 6 条：Store 各字符为什么必须/不必转义

Load 时反斜杠是转义引导符，所以键值中的字面 `\` 必须写成 `\\`；
字面换行/回车/制表/换页写成 `\n`/`\r`/`\t`/`\f`，否则会破坏行结构
（`\n` 若直接出现在值里会被当作行终止符，续行语义又会丢前导空白，无法还原）。

- 键（写在每行最前）：第一个非空白字符为 `#`/`!` 的行会被整行当注释，
  故键以 `#`/`!` 开头必须转义（`\#`/`\!`）；值永远在分隔符之后，行首注释
  规则够不到它，故值中的 `#`/`!`（含开头）都不必转义。
- 键中的 `=`/`:`/空白是键值分隔符，未转义会切断键，故必须转义；
  值里 `=`/`:` 不是分隔符（分隔符只认第一个），值中间的空白也原样保留，
  故值中间的空格不必转义。
- 值开头的空白：续行/逻辑行拼接后，解析在找分隔符时跳过的是键侧空白；
  分隔符之后还会跳过一段前导空白作为「分隔符两侧空白」。因此值的第一个
  空格会被吃掉，必须写成 `\ `；值尾部空白本就保留，不必转义。

非 ASCII 原样以 UTF-8 输出（不写 `\uXXXX`）。

## 语义与测试对照表

| 语义条目 | 钉住它的测试函数 |
| --- | --- |
| 1 分隔符 | （实现后补填） |
| 2 注释 | |
| 3 续行/空白续行 | |
| 4 转义与 \\u 行列号 | |
| 5 重复键顺序 | |
| 6 回写特殊字符 | |
| 7 1000 组往返 | |
| 计数器复杂度 | |
