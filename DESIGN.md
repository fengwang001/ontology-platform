# RLE 设计推导

唯一合法编码由 `Encode` 产生：最长游程；次数仅在 ≥2 时以无前导零十进制写出；数字与 `\` 加转义。
严格解码器只接受满足不变量 `Encode(Decode(t)) == t` 的输入，据此拒绝：

1. 显式次数 1，如 `1a`：解码得 `a`，重编码为 `a` ≠ `1a`。
2. 次数 0，如 `0a`：解码得空串（或零个 a），重编码为空串 ≠ `0a`。
3. 前导零，如 `01a`、`00a`：值为 1/0，重编码分别是 `a`/空，均与原文不同。
4. 相邻同符号游程，如 `2a3a`：解码为 `aaaaa`，最长游程重编码为 `5a` ≠ `2a3a`。
5. `\` 后跟非数字非反斜杠，如 `\a`：无可解码符号；若按字面 `a` 处理则重编码为 `a` ≠ `\a`。
6. 末尾孤立 `\`：转义无字符，t 不是完整游程序列。
7. 末尾只有次数无符号，如 `12`：缺符号，不是完整游程。
8. 非法 UTF-8：符号必须是合法码点；合法输出只含原始码点与 ASCII 结构符，不可能重编码出这些字节。
9. 次数溢出 `MaxCount`（=`math.MaxInt64`，如 20 位 `999…`）：重编码虽仍是同串，但输出规模无法实际表示；选择拒绝而非静默溢出。

## 大次数取舍

选**拒绝超上限**：上限 `runs.MaxCount = math.MaxInt64`。一次游程重复 MaxInt64 个码点本身已超出任何可寻址输出，更大的次数没有可实现语义；逐位解析时一旦超过上限即返回带偏移的 `ErrCountTooLarge`，绝不静默回绕。输出字节数另设可配置上限（默认 1 GiB），超限时 `ErrOutputLimit`，解码不按次数预分配，逐游程用 `strings.Repeat` 增量产出，故 `99999999999999999999a` 在分配前即被拒绝。

## 语义—测试对照

| 语义 | 钉住它的测试 |
| --- | --- |
| 1 往返（含数字/反斜杠/多字节/长游程） | `TestRoundTrip`（含随机循环） |
| 2 规范形式与严格拒绝清单 | `TestStrictReject`（表驱动，逐条偏移与错误类别） |
| 3 大次数与输出上限 | `TestBigCount`、`TestOutputLimit` |
| 4 最长游程、不做规范化 | `TestEncodeRuns`、`TestRoundTrip` |
| 5 任意切分点流式一致 | `TestStreamSplits`（遍历所有切分点循环） |
| 计数器 == 输入字节数 | `TestExaminedCounter`（1 MB/1 字节一段） |
