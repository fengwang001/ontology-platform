# DESIGN.md：只读快照一致性导出器

## 1. 目标与不变量
- 导出生成「某一时刻」的只读快照；导出期间底层键值可被并发读写。
- 快照结果必须等价于在版本水位 v 瞬间读到的全部键值，导出文件支持按块续传与完整性校验。
- 四类失败可用 `errors.Is` 区分：清单不完整、块头不完整、块体不完整、CRC 不匹配；另加快照过期。

## 2. 存储与版本水位
`store` 用 `map[string][]byte` 保存当前值，一把 `sync.RWMutex` 保护；每次 `Put` 让版本号 +1。
创建快照时记录当前版本 v，并在锁内复制当时的键集合（排序后的键切片）。键集合固定，
值则按下述写时复制机制读取。只支持覆盖写（不提供删除）；空键与空值均合法，且与「键不存在」天然可区分。

## 3. 写时复制的触发点（为什么导出不会读到新值）
普通读直接读当前 map。问题出在：快照打开后、导出尚未遍历到某键时，该键被覆盖。
若直接覆写底层值，导出到该键时读到新值，快照就跨时刻了。
正确触发点在**写入路径**：store 在持有写锁、真正改 map 之前，先回调所有打开的快照。
每个快照独立判断：若该键在自己的保留表里没有记录，就把「快照版本可见的旧值」拷贝进自己的保留表；
已有记录则不动（保留表存的永远是快照时刻可见的那一份，连续多次写只保留第一次的旧值）。
导出读取顺序为：先查本快照保留表，命中则用保留值；否则读当前值（该键自快照以来未被写，当前值即 v 时刻值）。
因此即使导出到第 50 个键时第 80 个键被改写，第 80 个键也已在写入瞬间保留旧值，导出仍是旧值。

## 4. 保留值何时释放：为什么按最老快照版本判断
保留值必须在「还可能被某个打开的快照读取」期间存活。一个旧值只对水位不早于该值旧版本
且尚未关闭的快照有意义。快照集合中真正的约束者是**最老的打开快照**：任何早于最老快照
产生的旧值都不可能被现存快照需要；而晚于最老快照的覆盖，其旧值可能被该最老快照读取。
因此资源存活边界 = 最老打开快照的版本水位。本实现把该规则具体化为「每个快照一张独立保留表」：
表只含本快照水位之后、关闭之前被覆盖键的旧值；`Close` 后整张表丢弃，计数归零。
两个重叠快照各持各的表，只有两个都 Close，所有保留值才全部释放。
内存只与「导出期间被改写的不同键数」成正比，与总键数无关（10 万键只改 100 个 => 至多 100 条）。

## 5. 导出顺序无关性
总校验和定义为：对快照固定的键集合按字典序排序后，对每个 `len(key)||key||len(val)||val`
依次写入 SHA-256。该哈希只依赖内容集合，与块的实际遍历/写入顺序无关。
所以字典序与逆序两种导出产生的文件字节布局可以不同，但清单中的总校验和相同、内容集合相同。
每个键在一次导出中恰好读取一次（先查保留表再决定），读取次数严格等于键数。

## 6. 文件格式、分块与内存上限
文件由连续自描述块组成，每块：
`MAGIC(4) | blockSeq uint32 BE | recCount uint32 BE | bodyLen uint32 BE | body | crc32 IEEE(头字段+body)`。
记录体编码 `keyLen uvarint | key | valLen uvarint | val`，块按记录顺序打包，不拆分单条记录。
导出只保留一块大小的复用缓冲，驻留内存峰值 <= 一块大小（计数器在每次缓冲分配时取 max 断言）。
块大小为 0 返回可判定错误。块大小大于总数据量时自然只有一块；空存储为 0 块。

## 7. 续传与「快照过期必须拒绝」
导出状态清单（与数据同目录的 sidecar：快照版本、水位键列表、键数、块大小、下一起始块）。
每完整写完一块即落盘并原子更新 sidecar 的「下一起始块」。续传时先校验快照：
快照必须仍打开且版本/键数与 sidecar 一致，否则返回 `ErrSnapshotStale`，**绝不删除已导出部分**。
论证：若快照已关闭，保留值已释放，只能读到当前数据；用它补齐缺口会拼出跨时刻文件，
总校验即使偶然通过也不代表一致性。过期后唯一安全动作是拒绝，保留现场供全量重导。
校验通过时，从第 k 块截断数据文件后续写；对任意 k（1..块数）逐一遍历，结果与一次性导出逐字节相同。

## 8. 完整性校验与截断分类
校验器顺序解析：magic 不符即错误；header 不足 16 字节 => `ErrTruncatedHeader`；
body 不足 => `ErrTruncatedBody`；body 够但 CRC 4 字节缺失 => `ErrTruncatedManifest`（结尾/清单不完整）；
有 CRC 但不匹配 => `ErrCRCMismatch`。全部块读完后重建 blockSeq 集合：乱序/缺失指名块号，
再校验总校验和。逐字节截断（1..len-1）全部落入上述四类之一。

## 9. 并发
store 读写锁保证 map 安全；快照保留表用自身 mutex 保护，注册/注销在 store 锁内进行，
写入回调在 store 写锁内读取旧值切片（值不可变），快照侧只做一次加锁拷贝，避免重入死锁。
`Close` 与导出竞争：导出持有快照活跃引用，关闭等待导出结束；过期读取返回可判定错误，
不会把半截文件当成完整文件。
