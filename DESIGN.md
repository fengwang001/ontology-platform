# 游标分页器设计

目标：在可并发增删的有序数据集上，用不透明游标做正向/反向翻页，保证不重不漏。

## 1. 复合键边界的推导

排序键 (Key float64, ID string)，ID 唯一。

- 反例 A（游标只记排序值 v，下一页取 `Key > v`）：10 行中 6 行 Key 同为 2.0，
  每页 3 行。第一页取到第 3 行（Key=2.0），游标记 v=2.0；第二页取 `Key > 2.0`，
  余下 3 行 Key=2.0 的行被整批跳过 —— 漏。
- 反例 B（取 `Key >= v`）：第二页从第一页最后一行重新开始，该行再次出现 —— 重。
- 正解：ID 唯一使 (Key, ID) 构成**严格全序**（先比 Key，相等再比 ID）。游标记录
  上一页最后一行的完整复合键 (v, id)，下一页取**严格大于**该复合键的行。同值行
  靠 ID 分出先后，既不漏也不重。空游标（零值）表示从头开始。

## 2. 反向翻页规则

反向不是把正向结果倒序。规则：比较方向反过来（取复合键**严格小于**游标的行），
取其中**最大**的 n 行，页内仍按正向（升序）排列。每页结果携带两个游标：
`Next`（方向=正向，锚在页尾行）与 `Prev`（方向=反向，锚在页首行）。
于是从第三页的 `Prev` 游标反向翻一页，恰好得到第二页且逐行相同。
方向由游标自带标记约束：正向游标做反向翻页（或反之）报 `ErrDirectionMismatch`。

## 3. 并发增删下的语义承诺

不做快照。翻页以游标复合键为锚（keyset），不依赖行是否仍存在：

- 插入到已翻过区间的行允许不出现；
- 已返回过的行绝不再次出现（后续页一律严格大于已返回的最大复合键）；
- 未被删除的行在其复合键区间内绝不跳过（全序 + 二分定位下界）；
- 游标指向的行被删除后仍可续翻（按复合键比较，不查行）。

翻页是纯函数：同一游标并发翻页，各协程得到逐行相同的页；增删用 RWMutex 保护。

## 4. 游标格式（不透明字节串）

布局：`magic(1) | version(1) | dir(1) | key uint64 BE(8) | idLen(1) | id | crc32(4)`。
方向码：正向 0xA5、反向 0x5A，互为按位取反（汉明距离 8），单比特翻转必非法。
解码顺序：空串→零游标（从头，合法）；长度不足→`ErrTruncated`；方向非法→
`ErrBadDirection`；idLen 与总长不符→`ErrTruncated`；magic/version/CRC 不符→
`ErrChecksum`。四类错误（含 `ErrDirectionMismatch`）均可 `errors.Is` 区分。
编解码 O(1)：定长头 + 一次字符串转换，分配次数为常数。

## 5. 效率约束

每页 = 一次二分定位（约 ceil(log2 N) 次比较）+ 顺序取 n 行（不再比较）。
page 包内置非导出计数器记录单次翻页比较次数，N=10000、n=20 时实测约 14 次，
远低于上界 4*(20+ceil(log2 10000))=136，绝不从头全扫。
