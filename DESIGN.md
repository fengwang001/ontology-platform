# 并行任务图调度与失败传播 — 设计推导

## 1. 四类最终状态的判定

每个任务终态恰为四类之一：

| 状态 | 判定依据 | 原因/错误 |
|---|---|---|
| Success | 在传播发生前已执行完并返回 nil | 无 |
| Failed | 自己执行并返回错误，或 panic 被捕获 | 自身原始错误（panic 标注为 panic） |
| Skipped | 从未开始；且其某个祖先 Failed | 指向「最早失败祖先」 |
| Canceled | 已经开始执行，因其他任务失败被 ctx 取消；或取消后返回结果 | 指向触发取消的失败任务 |

关键区分：是否**已经开始**。开始判定以 goroutine 实际调用任务函数为准；
Skipped 是永远没拿到运行资格，Canceled 是拿到过、跑了一半被撤。

「最早失败祖先」确定性规则：在 Failed 祖先集合中取 ID 字典序最小者
（`A→B→C`、A 失败时，C 的原因指向 A 而非 B）。多任务同时失败时各自独立，
报告保留全部 Failed 任务；下游的原因分别指向自己那条链上最早的失败。

## 2. 快速失败 vs 尽力而为

- FailFast：首个失败落定即触发传播——取消所有在跑任务（Canceled），
  其余含失败祖先或已不可达的任务全部 Skipped。
- BestEffort：不取消在跑任务；只要某任务的**全部**祖先都不 Failed，
  它仍可调度执行；一旦有失败祖先即 Skipped。互不相关的旁支继续跑完。
- 两种模式下 Success 一经落定永不改标；失败之后新完成的旁支成功
  在 FailFast 中按 Canceled 计（取消先于其完成），BestEffort 中为 Success。

## 3. 调度模型与复杂度

单事件循环 + worker 信号量：就绪堆按 ID 升序；维护剩余入度 `indeg`。
任务完成时只沿其出边递减后继入度（每边恰好处理一次），并入堆。
就绪判定 = 一次入堆/出堆决策，总计 ≤ 2V+E 次，满足 ≤ 4(V+E)。
并发计数用 `running int` + 容量 C 的信号量；记录峰值 `peakRunning`（非导出）。

取消传播在事件循环内完成，与完成事件串行，无数据竞争。
取消后迟到的完成包一律丢弃：终态以取消标记时刻为准。
为避免 goroutine 泄漏，事件循环收尾时仍排空所有 worker 包（任务函数必须返回）。

## 4. 环检测

执行前 DFS 三色标记，遇灰点回溯出一条边边真实、首尾闭合的路径；
自环（u→u）直接报 `[u u]`。重复加边幂等（邻接去重）。
拓扑分层：Kahn 算法，每层是当前入度为 0 的节点，层内按 ID 升序。

## 5. panic 与状态可区分性

exec 层 `recover`，把 panic 包装为带标记的 error（`errors.Is` 可判
`ErrPanic`），附原始值。四类状态用枚举区分；Skipped/Canceled 的原因
统一为 `*FailCause{RootID, Kind}`，同样可 `errors.Is` 到对应哨兵。

## 6. 确定性

ID 全部已知、就绪堆 ID 升序、根因取字典序最小、报告按 ID 字典序渲染，
故报告逐字节确定，与边的插入顺序、实际完成时序、并发度无关。

## 7. 包划分（5 包 + demo）

graph（建图/环/分层）、fail（状态枚举/原因/传播判定）、
exec（panic 安全执行）、sched（事件循环调度器/计数/取消）、
report（按 ID 排序的文本与结构化报告）、cmd/demo（逐条判定）。
