# RLE 编解码器设计说明

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。据此推导拒绝清单。

## 严格解码拒绝清单（逐条推导）

1. 显式次数 1（`1a`）：解码得 `a`，再编码为 `a` != `1a`。
2. 次数 0（`0a`、`00a`）：解码得空串，再编码为空串 != 原串；且合法次数只有 `1`（省略）或 `>=2`。
3. 前导零（`01a`、`012a`）：解码得 `a`/12 个 a，规范形式是 `a`/`12a` != 原串。
4. 相邻游程同符号（`2a3a`）：解码得 5 个 a，最长游程编码为 `5a` != `2a3a`；故流式输出需保留一个待发游程做前瞻。
5. 非法转义（`\b`）：转义只允许数字与反斜杠；接受它则解码出的 `b` 再编码为裸 `b` != `\b`。
6. 末尾孤立 `\`：游程必须以符号结束，缺少符号无法构成完整游程。
7. 末尾只有次数（`12`）：同上，缺少符号。
8. 非法 UTF-8（含末尾不完整多字节序列）：无法对应合法码点序列；若替换为 U+FFFD，再编码结果与原字节不同。

错误彼此可区分（哨兵错误），并在 `DecodeError.Offset` 给出字节偏移。

## 大次数取舍

选择**拒绝超过 `runs.MaxCount`（`math.MaxInt`）的次数**（`ErrCountTooLarge`）。
理由：Encode 的输入是 Go 字符串，其字节长度（因而任何游程长度）不超过 MaxInt，
所以规范编码中的次数永远不超过该值；超过它的 t 不可能是规范形式。
逐位累加时一旦越界即拒绝，绝不静默溢出。
解码不按次数预分配：输出有可配置的字节预算（默认 64 MiB，流式可配置），
超限在产出任何字节前返回可判定的 `ErrOutputLimit`。

## 语义—测试对照表

| 第三节语义 | 钉住它的测试函数 |
| --- | --- |
| 1 往返 | （待补） |
| 2 规范形式与严格解码 | （待补） |
| 3 大次数 | （待补） |
| 4 最长游程/不规范化 | （待补） |
| 5 跨切分点一致 | （待补） |
