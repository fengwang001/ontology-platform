# 设计推导

## 2. 代理对的错误偏移

规则：把高代理 `\uXXXX` 视为「未完成单元」，不立即报错，继续读下一个单元。

- 下一个单元是低代理：组合成一个码点，偏移不适用。
- 下一个单元不是低代理（普通字符、简单转义、别的 `\uXXXX`）或紧跟结尾引号：错误指向
  「紧跟高代理的那个单元的起点」；高代理后面直接是引号时指向该引号。
- 输入在高代理后结束（EOF）：错误指向高代理反斜杠的起点。

理由：严格语法里高代理不是独立词法单元，它和后继共同构成一个「代理对单元」。
偏移按「导致失败成为事实的第一个字节」给出：高代理后字节尚未读到时无法判定，
故 EOF 只能落在起点；一旦读到非低代理的后继，该后继的首字节就是破坏配对的位置。
因此 `"\uD83Dx"` 指向 `x`，`"\uD83DA"` 指向 `A`，`"\uDE00\uD83D"` 指向 `\uD83D`
（低代理没有前导高代理，在其自身起点即失败）。
`\uD83DA` 不能解释成「替换字符加 A」：JSON 没有「宽松替换」规则，孤立代理是非法序列；
且这里根本没有第 5 个十六进制位，`A` 是字面量内容。静默替换会让非法输入与合法输入
（真正的 U+FFFD 后接 A）产生相同结果，破坏解码的确定性与编码单射。

## 4. 最小转义与唯一性

RFC 8259 强制未转义控制符 U+0000–U+001F 不得出现，因此必须转义；U+007F 不是控制符
（DEL 不在该区间，RFC 对其无任何限制），原样输出合法。`/` 仅在允许的短转义列表中、
并非必须转义；非 ASCII 码点（含 U+2028/U+2029）以合法 UTF-8 原样出现同样合法。
故只转义 `"`、`\`、U+0000–U+001F，其中 5 个有短形式，其余固定 `\u00XX` 小写。

唯一性/单射：每个被转义字符只有一种表示（短形式优先，其余固定 4 位小写十六进制，
不足补零），其余字节原样；转义形式以 `\` 开头，而 `\` 自身必被转义，输出中的每个 `\`
都唯一对应一个转义序列起始，两种来源的码点不会碰撞。因此 Encode(s) 唯一，且
Decode(Encode(s)) 可无歧义还原 s，Encode 是单射。
