# CSV 方言解析器设计推导

## 1. 空行与单列空值

记录仅在遇到分隔记录的换行时产出；「上一条记录刚结束又立刻遇到 `\n`」的字符流无法表示任何数据字段，
因此**空行一律跳过**，不是一条记录（`"\n\n"` 只有一条记录，最后换行不产记录）。
判据：见到 `\n` 时若本条记录尚未开始（无任何字段，包括无前导引号），即为空行。
`"\r\n\r\n"` 同理由 CR 态在 `\n` 时判定跳过；孤立 `\r` 即使位于记录起点也是 `ErrLonelyCR`。

于是单列里「值为空的记录」必须写成 `""`：空文件与只有空白行的流都不含记录，若空值写成裸空，
其字节流就是换行本身，将被空行规则吞掉，丢失这条记录。所以回写器在**列数为 1 且字段为空**时必须加引号；
多列的空字段无歧义，保持裸空。

## 2. par 的切点问题

每段 `[o_i, o_{i+1})`，令 worker i 额外处理一个**前重叠字节** `o_i-1`（i>0），即输入 `[o_i-1, o_{i+1})`。
worker 并行跑**两个假设**机：

- OUT：种子为「字段开始」，解释「切点在引号外」；
- IN：种子为「引号字段内，且前重叠字节是前一引号」——即重叠字节 `x` 先按引号态处理：
  若 x 是 `"`，首字节再读 `"` 得「待定期」（配对成 `""`）；若 x 非 `"` 则按普通内容处理。
  这一台同时覆盖「引号内」与「从 `""` 中间切开」两种 IN 情形，不需要第三台。

拼接从左到右：段 i 的真实解释由段 i-1 终止时是否在引号内决定，在 i>0 时其前重叠字节必然是
前一段 worker 已处理过的尾字节，OUT/IN 两态恰为该字节之后的全部可能，故选中的解释与流式机逐字节同状态。
段内产出的、位于重叠位置的事件（cell 起点/记录号/字段号/错误）一律丢弃；跨边界未完成字段与
前一段 tail 合并（值拼接、引号标记与起点偏移取前段）。坐标换算：全局偏移 = 段逻辑起点 + 段内偏移
（IN 首字节为重叠字节，其输出不产记录）；全局记录号/字段号 = 段内编号 + 前段累计计数；错误的
记录号、字段号、偏移同样按此平移。落在 `\r` 与 `\n` 之间时：OUT 种子为字段开始会把 CR 记为错误，
但真实状态由前段决定为 CR 待定时选 IN（重叠字节 `\r` 在引号内是内容，`\n` 正确闭合），结果不变。

代价：每字节至多在 OUT、IN 两机各处理一次，重叠字节为常数 K 个，故处理次数 ≤ 2N+O(K)。

## 3. CR 待定状态与流末判定

未引号字段中 `\r` 不立刻定性：进入 CR 待定期，下一字节为 `\n` 则结束记录（行尾 `\r\n`，不写入值），
为逗号则逗号结束字段（`\r` 计入字段值，随后报 `ErrLonelyCR`？否——见下）：
本方言规定**未引号字段内出现裸 `\r` 即错误**，故 CR 待定后：`\n`→行尾；其他任意字节→`ErrLonelyCR`
（指向该 `\r`，逗号/`"`/EOF 同罪）。引号内 `\r` 永远是内容（包括其后非 `\n`），`\r\n` 原样两字节保留。
半包续传：CR 待定是显式状态，`Feed` 边界停在 `\r` 完全合法；`Close` 时处于该态即 `ErrLonelyCR`。
par：见第 2 节，重叠方案保证 `\r` 与其后的 `\n` 落在同一 worker 的同一解释中。

## 4. 上限的「立刻」

字段计数器只对**字段内容字节**自增：未引号态的普通字节；引号态的普通字节与每对 `""` 中的后一个
`"`（转义成一个引号字符）。开引号、闭引号、逗号、换行不计数。每个内容字节在追加前先判断
`count >= MaxFieldBytes`，超限立即 `ErrFieldTooLarge`（此时 `""` 的第一个引号不计数，第二个到达时拒绝），
不缓冲整条记录。字段数在每产出一个字段时检查 `MaxFieldsPerRecord`；记录数在每产出一条记录时检查
`MaxRecords`。超限即终态，之后 `Feed`/`Close` 返回同一错误，已产出记录保留。

## 5. 状态转移表

输入类别：`,` = C，`\n` = N，`\r` = R，`"` = Q，其他 = X，EOF = E。
动作：em 发字段，er 发记录，err 报错（括号内哨兵）。

| 状态 | C | N | R | Q | X | E |
|---|---|---|---|---|---|---|
| 字段开始 S | em,空字段→S | 空行跳过→S | 待定期 CR | 开引号→F | 值+1→B | 无记录正常结束 |
| 未引号 B | em→S | em,er→S | 字段挂起→CR | (ErrQuoteInBare) | 值+1→B | em,er,正常 |
| 引号内 F | 值+1→F | 值+1→F | 值+1→F | →G | 值+1→F | (ErrUnclosedQuote) |
| 引号刚闭 G | em→S | em,er→S | em→CR | 值+1(引号)→F | (ErrCharsAfterQuote) | em,er,正常 |
| CR 待定 CR | (ErrLonelyCR) | 挂起字段 em,er；若记录未开始则跳过 | (ErrLonelyCR) | (ErrLonelyCR) | (ErrLonelyCR) | (ErrLonelyCR) |

补充：S 遇 Q 后在引号闭合前一切按 F；进入字段（开引号或写入字节）置「记录已开始」；
CR 待定期挂起的字段不提前发出，待 `\n` 到达再发，保证错误时不留半截记录。

## 6. 回写规则与并发约定

回写必要引号：值含 `,` `"` `\r` `\n`，或第 1 节的单列空字段；被解析为带引号的字段原样保持引号
（以保证引号标记往返）；带引号值内部 `"` → `""`。行尾统一写 `\n`——引号字段内的 `\r\n` 属值内容
已原样保留并被引号包住，记录分隔符本身不承载 CR。**规范输入**定义：记录分隔符仅用 `\n`、
仅在上述必要情形加引号、无空行、末尾无多余换行；其上 `Write(Parse(x)) == x`。
单个流式 `*table.Parser` 非并发安全（文档明示）；`par` 内部 K 个 goroutine 仅写各自结果，
主 goroutine 顺序拼接，无共享可变状态。
