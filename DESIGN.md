# DESIGN

## 1. 流格式（全部 LEB128 无符号变长整数）
| 记录 | 字节布局 |
|---|---|
| 流头 | `4C 37 01`（魔数 "L7" + 版本 1，定长 3 字节） |
| 字面量 | tag=0, varint n, n 个原文字节 |
| 回指 | tag=1, varint distance(≥1), varint length(≥3) |
| 刷新 | tag=2（无负载） |
| 流尾 | tag=3, varint 原文总长, varint CRC32(IEEE) |

varint 最多 10 字节；第 10 字节只允许低 6 位，否则 64 位溢出。

## 2. 包与依赖
wire（无依赖）→ window（环形缓冲）→ match（哈希链，持有 window）；
enc 依赖 wire/match/window；dec 依赖 wire/window。依赖单向。

## 3. 关键推导
### 3.1 Write 切法无关
匹配决策需要看到「未来」字节，而 Write 边界处未来未知。结论：两次
Flush/Close 之间的输入整体缓存为一个 segment，不在 Write 中产出任何
压缩字节；决策只在 segment 封闭（Flush/Close）时对完整缓冲做贪心最长
匹配。因此任何 Write 切法给出的候选字节序列完全相同，输出逐字节相同。
Flush 的语义就是强制封闭当前 segment：它必须打断可能跨入未来的匹配，
立即输出该 segment 的全部记录；之后 segment 的匹配仍可经持久 window
回指此前历史。连续第二次 Flush 无新字节，segment 为空，故零输出。

### 3.2 重叠回指（distance < length）
块复制 `dst=src; copy(out, src)` 中 src 与 dst 重叠时，标准 copy 语义
虽可向前覆盖，但取历史窗口的一次性快照复制会在第二轮模式重复时取错。
结论：解压器逐字节复制，第 k 个字节取「当前输出序列距尾部 distance」
处：距离内取历史窗口，越过距离后取本次刚写出的字节，自然重复模式。
长度受输出上限约束（超限先拒绝），最坏 4MB 量级，逐字节循环可接受。

### 3.3 两类距离错误
distance=0 无意义，单列；distance>已输出长度：流本身引用了从未存在
的字节（可能是字典缺失/流拼接错误）；distance>window 容量：引用逻辑
上存在但已滑出窗口、无法取回，二者成因不同必须可区分。压缩器只对
哈希链中 `距离≤window.Len()≤容量` 的候选发回指，故绝不可能产生超窗
回指。并行块以「上一块末尾 ≤容量 字节」预置 matcher：解压端该字典
就是此前块已输出且仍在窗口内的字节，「已输出字节数」含义不变，只是
跨刷新标记保留窗口。

### 3.4 并行确定性
块 i 的编码输入仅 = 块 i 原文 + 块 i-1 原文末尾（容量内），不读任何
其他块、不读其他块的编码结果，也不共享可变状态（每 worker 独立
matcher/window），故与 worker 数和完成顺序无关。汇编按块号顺序拼接：
块间放刷新标记（使每块边界可独立还原），最后一块后放流尾；刷新不清
空解压窗口，跨块回指因此合法。workers=1 走相同函数，输出必然一致。

### 3.5 复杂度
哈希链每位置只沿链走至多 maxChain 个候选，故总考察 ≤ maxChain×字节；
链槽按位置模容量环形复用，配合距离校验丢弃过期头，每字节代价不随窗口
增长。全同字节在第 4 字节起即命中距离 1 的长回指。
