# 设计推导

## 1. 末尾换行策略与样例表

三种策略只在 `Close` 时对“输出末尾”做一次决定，且决定后的结果仍满足“无 `\r`、无行尾空白”。
- Preserve：不改末尾。`"\n"`→`"\n"`，`"\n\n"`→`"\n\n"`（原样的重复换行属于空行，予以保留）。
- EnsureOne：末尾必须恰好一个 `\n`；末尾有若干 `\n` 就保留一个、没有就追加一个。
- TrimBlank：删掉全部末尾空行后保留一个换行；等价于 EnsureOne，但**空输入输出空串**（没有任何行可结束，强行加换行会让空输入产出内容）。
幂等：三种策略的输出都以 0 或 1 个 `\n` 结尾且无行尾空白，再规范化时行尾处理删除 0 个空白，末尾决策为不动点。EnsureOne 对空输入取 `"\n"`：`N("\n")="\n"`，是不动点；取 `""` 同样是不动点，二者都满足幂等，差异只是“空文件是否强制产生一行”，本实现选 `"\n"`（语义即“确保至少一行已结束的行”）。

| 输入 | Preserve | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

不变量：`ToOrig` 单调不减；对每个输出偏移 o，`ToOut(ToOrig(o))==o`；`ToOut` 单调不减；定义域都是 `[0,len]`。
设被删区间 `[a,b)`（b 处开始保留内容，如 `\n`），其输出区间退化为点 p。
- `ToOrig(p)=a`（逆像取删除区起点）。若取 b，则删除区内部 i∈[a,b) 的 ToOut 无处可取（取 b 会迫使 i=b-1 的 ToOut≥b>p，与输出长度矛盾）。故 `ToOut(i)=p` 对所有 i∈[a,b)，且 `ToOut(b)=p+len(保留)`。
- `\r\n` 中被删的 `\r`（a）与其后保留的 `\n`（b）：p 即 `\n` 的输出偏移，`ToOut(a)=p`。
结论：被删空白里的光标与 `\r\n` 的 `\r` 都映射到**行尾换行符之前**（换行符的输出起点），符合编辑器把被删尾随空白内光标定位到换行前的直觉。
纯插入（EnsureOne 对空输入追加 `\n`，原文区间退化为点 a=b=n）：`ToOrig(p)=ToOrig(p+1)=n`，无法同时满足 `ToOut(n)=p` 与 `=p+1`。取 `ToOut(n)=p+1`（EOF 锚到 EOF），插入点 o=p 为该不变量唯一不可达位；这是任何非降整数映射在输出严格变长时的必然例外（鸽巢原理），文档明确此一处。

## 3. 缓冲超限选择

空白串必须缓冲才能判定是否在行尾。两种处理：
- 强制判定为“非行尾”并输出：后续若真出现行尾，已输出的空白无法撤回，直接违反语义 2（且幂等失效），不可取。
- 拒绝：不产出与最终规范化不一致的内容，已输出前缀保持有效；返回带原文偏移的可判定错误并进入终态，之后 Write 返回终态错误。语义 4/5/6 对成功完成的输入不受影响（超限输入的结果定义为错误）。
选择拒绝（`ErrWhitespaceOverflow`）。输出总字节上限同理：先于追加检查，超限即 `ErrOutputLimit`，不写部分字节。

## 4. par 拼接

段开头不知道前段末尾是否留待定 `\r` 或未判定空白，故不能各段独立 Close。做法：一次 O(n) 顺序的“边界预扫”确定每段拥有的原文区间 `[s,e)`：
- 前段末为 `\r` 且本段首字节为 `\n`：该 `\n` 归前段（CRLF 成对）。
- 从本段临时末向后：跳过行尾空白（空格/制表符）后若紧跟行尾，则把空白与该换行（CRLF 含两字节）整体纳入本段——待定空白与其判定依据同段。
- 看不到行尾的尾部空白归下一段起点，由下段内容判定；一路延续到 EOF 则并入最后一段。仅最后一段按用户末尾策略 Close，其余段按 Preserve 处理，且其拥有区间在下一非空行的行首处结束，切点干净。
修正范围有上界：CRLF 最多吸收 1 字节；空白吸收到最近换行（纯空白无换行的极端输入退化为顺序处理，仍正确，总扫描 O(n)）。
各 worker 输出与映射在段内坐标：映射区间端点整体平移（orig 加 s，out 加前段输出长度），相邻同斜率区间合并。`\r|\n`、空白中间、“空白后是 `\r`”三类切点均由吸收规则覆盖。
