# 设计推导

## 2. 代理对错误偏移

解码规则把 `\uHIGH\uLOW` 看成一个不可分割的词法产生式：高代理本身不是 Unicode 标量值，只有后面紧跟低代理时才合成一个码点。

- 单独遇到低代理：它所在的 `\u` 起点就是错误起点；低代理不能作为一个独立码点。
- 已读到高代理后，任何不能完成“紧跟的低代理转义”的输入都在高代理 `\u` 起点报错：错误属于整个代理对产生式，而不是后面的普通字节。
- 因此 `"\uD83Dx"`、`"\uD83DA"` 都报高代理起点；`"\uDE00\uD83D"` 先遇到低代理，报第一个 `\uDE00` 起点。

`\uD83DA` 不能解释成“替换字符加 A”：严格解码器不得把非法转义改写成 U+FFFD；且 JSON 的 `\u` 固定消费四位十六进制，消费完 `D83D` 后看到的是普通字符 `A`，不是第二个 `\u`。高代理期待尚未满足，故只能拒绝。

## 4. 最小转义与唯一性

JSON 字符串中必须转义的只有引号、反斜杠和 U+0000–U+001F：引号会结束字符串，反斜杠会引入转义，控制字符被 RFC 8259 禁止以未转义形式出现。

U+001F 属于上述控制字符范围，所以必须转义；U+007F（DEL）不是 RFC 8259 定义的控制字符，也没有定界含义，可作为合法 UTF-8 原样输出。`/`、非 ASCII、U+2028/U+2029 同理保持原样。

编码器对每个码点只走两条互斥路径：允许集合中的字节原样输出，不允许字符使用唯一短转义或固定小写 `\u00XX`。各转义前缀互不冲突，解码时原样段与转义段可无歧义切分，所以 `Encode` 是单射；同一输入每次都得到完全相同的字节，因此输出唯一。
