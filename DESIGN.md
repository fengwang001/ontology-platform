# DESIGN

## 1. 末尾换行策略

先把行尾与行尾空白规范化为核心流 C（行尾一律 `\n`，行尾空白已删，末尾换行原样）。

- Keep：输出 C，不增删末尾换行。
- EnsureOne：若 C 不以 `\n` 结尾则追加一个合成 `\n`；空输入输出 `\n`。
- TrimBlank：删掉 C 末尾全部 `\n`；若仍有字节则补一个合成 `\n`，否则输出空。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

幂等论证：三种策略的输出都不含 `\r`、不含行尾空白。Keep 是恒等投影；EnsureOne 的
输出必以 `\n` 结尾，第二次应用不再追加；TrimBlank 的输出要么为空，要么恰以“非换行
内容 + 单个 `\n`”结尾，第二次应用删除零个、补回同一个。EnsureOne 对空输入若选
`""` 也幂等，但会出现 `""` 与 `"\n"` 两个相邻不动点且无法表达“空文件也要一个换行”；
选 `"\n"` 使“以 `\n` 结尾”无例外，故取后者。

## 2. 被删字节的 ToOut

映射记录为区间：保留(orig 长=out 长)、删除(orig 长>0, out 长 0)、合成(orig 长 0)。
被删字节（行尾空白、`\r\n` 中的 `\r`）位于一个删除区间 `[a,b)`，其输出坐标是该区间
边界处的同一个点 p，故 ToOut(i)=p（i∈[a,b]），落在终止 `\n` 的输出下标，即
**换行符之前**。理由：ToOut 单调不减要求整串被删字节取同一值；取 p（换行前）后
ToOrig(p) 指向下一个保留字节 `\n` 的原下标，ToOut 再作用仍回到 p，满足
ToOut(ToOrig(o))=o。取换行之后会让“光标在被删空白里”越过行边界，编辑器将跳到
下一行行首，故不取。单独 `\r` 转成的 `\n` 是保留区间（原坐标即该 `\r`）。

插入的合成 `\n`（orig 长 0）使 |out|>|orig|，此时要让所有输出位置严格满足
ToOut(ToOrig(o))=o 在数学上不可能：o=合成位置与 o=lenOut 经 ToOrig 都只能到
lenOrig，而 ToOut(lenOrig) 只能有一个值。取 ToOrig(合成)=lenOrig、
ToOut(lenOrig)=lenOut（总长度互映，两映射处处单调不减）；恒等式对所有“源自原
文”的输出位置（Keep 策略下为全部位置）逐点成立。

## 3. 空白缓冲超限

强制判定为“非行尾”立即输出：若随后真到行尾，已输出空白无法收回，违反语义 2（行尾
空白删除）与语义 5（输出不含行尾空白、幂等），故不可取。选择**拒绝**：待定空白串
长度超过上限时返回具名错误 ErrTrailingRunTooLong（带原文偏移），已输出前缀保留，
实例进入终态，后续 Write 返回 ErrClosed。拒绝不产生结果，故不违反 4/5/6；截断到
报错前的输入重新整体规范化，恰等于保留的前缀。

## 4. par 拼接

段内 Close 无法感知段首/段尾的待定 `\r` 与空白，故不直接规范化整段。对每段求：
h=到第一个行尾（含）为止的前缀，b=最后一个 `\n` 之后的后缀；中间 core 起止都在
确定的行边界上，可独立规范化。交界处把 b_j 与 h_{j+1} 拼成“行边界起、行边界止”
的块独立规范化（待定 `\r|\n`、空白串均落在该块内，被一次看全）；首块 h0 从流首
起，尾块 b_{K-1} 到流末并应用末尾策略。K=1 时整块处理。

修正范围上界：每个交界块只依赖相邻两段，涉及段数 ≤2；总扫描量每字节 ≤常数次。
其字节长度可跨一整行（长空白行），不保证常数字节，但与 K、切点无关。各块 span
先按“块内原文偏移→全局原文偏移、块内输出偏移→累计全局输出偏移”平移，再由
span.Append 合并相邻同类区间。
