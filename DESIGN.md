# 设计推导（本体只含流式行尾/空白规范化与偏移映射）

## 1. 核心模型
规范化输出是一串「行」，每行 = 去尾空白后的内容 + 一个 `\n`，最后统一应用末尾换行策略。
因此三种策略都只作用于输出尾部连续 `\n`：
- Keep：不动（`""→""`；有内容则保留其原始换行个数）。
- Ensure：非空且不以 `\n` 结尾则补一个 `\n`；空输入仍为 `""`。
- Trim：删掉全部末尾空行；若结果非空则确保恰好一个 `\n`（仅含空白的输入主体为空，故仍为 `""`）。

| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等性：规范化结果永不以空白结尾（Trim/Ensure 末字节必为 `\n`，Keep 的内容部分本就不含尾空白），
故第二次规范化不再删除任何东西；Ensure 对空输入若改成输出 `"\n"` 仍幂等，但会凭空造行，
破坏「空输入长度 0」的直觉与映射，故选 `""`。

## 2. 被删字节的 ToOut
把删除点表示为输出中的零宽区间：删除区间映射到「删除之后、下一内容之前」的同一输出偏移 d。
对 `\r\n` 中的 `\r`，其后是输出 `\n`，故 `\r` 映射到该 `\n` 的偏移（光标落到换行符处）；
被删的行尾空白位于 `\n` 之前，其映射目标也是该 `\n` 的偏移。两类删除在输出里都贴在行边界。
规则：区间 [a,b) 被删，d 为紧随其后输出字节的偏移（末尾删除则 d=len(out)），
ToOut(i)=d（a<=i<=b）。ToOrig(d) 取该处存活字节（末点取 len(orig)），
于是 ToOut(ToOrig(o))=o；两函数均单调不减，且非删除点严格一一对应。

## 3. 空白缓冲超限
强制把超长空白判定为「非行尾」并输出，会违反语义 2（若其后真来行尾，该空白应被删）
与语义 4（同输入按不同切分喂入，缓冲压力不同会得到不同输出），故不可取。
选择拒绝：返回具名哨兵错误 ErrWhitespaceBuffer（带已消费原文偏移），已输出内容保留，
规范化器进入终态（此后 Write/Close 返回 ErrClosed）。语义 4 不受影响——超限是
「原文前缀属性」（该串空白长度），与切分方式无关；语义 6 的映射覆盖到拒绝点为止，状态可解释。

## 4. par 拼接
各段用「片段模式」规范化：Close 不丢弃也不终结尾部待定状态，而是把尾部状态
（无待定 / 待定 \r / 一串待定空白）显式返回，主体输出与本地 span 照常产出。
段按顺序拼接，状态修正只发生在两段交界：
- 前段待定 \r：若后段首字节为 \n，该 \n 被吸收（删 1 字节），\r→\n；
  否则待定 \r 先产出 \n，再处理后段。
- 前段待定空白：交给后段首字节判定——为行尾则前段整串空白删除，否则原样输出；
  后段继续暴露自己的待定状态。空白串长度有上界（WhitespaceLimit，默认 1MiB），
  故跨段搬运的待定数据有界；每段只与紧邻下一段交互一次。
本地 span 平移（orig += 段全局起点，out += 全局已输出长度）后并入全局 Builder；
交界修正以小段 append 表达，删除即零宽区间。全部主体合并后再统一施加末尾策略
（见第 1 节尾部增删规则），因此结果与单流处理逐字节相同。
