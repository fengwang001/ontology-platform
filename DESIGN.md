# 熔断与舱壁调用保护器 — 设计推导

## 1. 分层与顺序

请求进入后顺序固定为：**熔断 → 舱壁 → 超时 → 真实调用**。

- 熔断在前：下游已知故障时直接快速失败，不申请并发额度、不排队，给故障下游恢复留出容量；
  若舱壁在前，连"拒绝"都要排队占名额，故障时队列被无效请求塞满，健康下游的排队公平性也被污染。
- 舱壁居中：只允许"熔断器认为值得一试"的请求竞争 N 个在途名额与 Q 个等待名额。
- 超时最贴近真实调用：只管一次调用的时限。

## 2. 统计口径：被熔断拒绝不计失败率

熔断拒绝是保护器自身行为，不是下游的新观测。若把拒绝计入失败：
打开 → 拒绝被记失败 → 失败率恒为 100% → 冷却后探测条件（样本窗口）仍被拒绝样本占满，
形成"一旦打开永远打不开"的自锁。因此：

- 熔断窗口只记录**真实调用**结果（成功 / 可重试失败 / 超时）；
- 熔断拒绝单独计为 `RejectedByBreaker`，不进窗口、不算失败率；
- 舱壁拒绝同理单独计 `RejectedByBulkhead`，属于容量事件而非下游质量事件；
- 全局恒等式：`总=成功+失败+熔断拒绝+舱壁拒绝`，`真实=总-两类拒绝`。

据此，打开后任意多次被拒不改变窗口，半开后一次成功即可回到关闭。

## 3. 不可重试错误是否计入熔断

**计入失败统计，但不进熔断窗口（不触发熔断）**。不可重试失败通常是确定性的
（4xx、参数错误），重试无意义；熔断要表达的是"下游暂时不可用、稍后可能恢复"，
让确定性错误触发熔断会误伤并连累其他请求。故窗口仅接纳 Retryable / Timeout。

## 4. 四条路径必须归还额度

额度在"真实调用开始前"获取，归还必须 defer 覆盖全部出口：
正常返回、错误返回、超时（ctx deadline）、panic（recover 转失败）。
任一路径漏还都会让在途计数永久偏高，最终 N 个名额全部泄漏、舱壁退化为全拒。

## 5. 舱壁语义

- N 个在途名额（带缓冲 channel 信号量），在途数恒 ≤ N，另记非导出历史峰值；
- Q 个等待名额：`acquire` 在信号量与"等待空位"上同时 select，
  第 N+Q+1 个请求立即得到 `ErrBulkheadFull`（时钟未被推进即可判定）；
- 等待中 ctx 取消：退出等待并归还等待空位，报 `ErrBulkheadCanceled`；
- N 必须 >0（N=1 即串行），Q ≥ 0；非法配置构造报错。

## 6. 熔断状态机

- 关闭：窗口内 连续失败≥阈值 或 (样本≥下限 且 失败率>阈值) → 打开，记录打开时刻与初始冷却；
- 打开：仅读时钟，经过冷却 → 半开（创建时重置探测计数）；时钟回拨不得提前迁移；
- 半开：并发放行恰好 Probe 个探测（名额用带缓冲 channel），其余直接拒绝；
  探测全部成功 → 关闭并清空窗口；任一失败 → 立即打开，冷却 = min(冷却×2, 上限)，
  加倍只随该次迁移发生一次；迁移在锁内判定，保证并发下只发生一次。
- 阈值（失败数/率/冷却/探测数）≤0 时构造报错。

## 7. 包划分

`classify`（四类可 `errors.Is` 判定错误）、`bulkhead`、`timeout`（时钟接口 + 真实时钟）、
`breaker`（注入 `Now()` 时钟）、`stat`（原子计数、口径快照与自洽检查、Protector 串联五层）、
`cmd/demo`（无参无网逐条判定，≤20 行输出）。

## 8. 错误模型

`ErrRetryable`、`ErrNonRetryable`、`ErrTimeout` 为调用结果类别；
`ErrBreakerOpen`、`ErrBulkheadFull`、`ErrBulkheadCanceled`、`ErrClockBackward`
为保护器可判定拒绝，彼此可用 `errors.Is` 区分。
