# 设计推导

定长参数：窗口容量 W(默认 32768)、候选链上限 L、最长匹配 M=65535、最小匹配 3。

## 流格式（全部 LEB128 无符号变长整数；记录首字节低 3 位为标签）

| 记录 | 布局 |
|---|---|
| 流头 | 魔数 `"LZ7"`(3B) + 版本(1B=1) + W(varint) |
| 字面量 | tag=0；len(varint)；len 个原始字节 |
| 回指 | tag=1；dist(varint,≥1)；mlen(varint,≥1) |
| 刷新 | tag=2；无载荷 |
| 流尾 | tag=3；原始总长(varint)；FNV-1a64 校验和(varint) |

记录起始处读 1 字节 tag；tag 只允许 0..3，否则报结构类错误。
整数最长 10 字节且第 10 字节仅允许最低 1 位，否则超长/溢出。

## 推导 1：压缩结果与 Write 切法无关

压缩器在位置 p 的决策只允许依赖“p 之前全部历史 + 从 p 起最终会收到的全部
后续字节”，不允许依赖“本次 Write 在何处结束”。做法：维护未定缓冲 pend，
每次只定稿信息已齐全的前缀：在 p 用 hash 链在窗口（≤p 的历史）贪心找最长
匹配，并在 p+1 处存在更长匹配时放弃 p 的匹配（lazy 一步）。若 p 之后还可能
到来新字节、当前匹配尚未失配也未达 M，则该匹配“可能继续延长”，不能定稿。
只有三种情况确定匹配结束：①出现失配字节；②达到 M；③Flush/Close 强制边界。
因此 Write 只追加字节并循环定稿已可定稿前缀，边界不产生任何决策，切法自然
无关；Flush 以情况③强制结束当前匹配并立即发刷新，保证已输出压缩字节恰好可
还原截至此刻的全部输入。

## 推导 2：重叠回指的复制

dist<len 时源区间与目标区间重叠。块复制（copy/append 展开）对重叠向前复制无
定义：dist=1 时正确结果是该字节重复 len 次，块复制会把“尚未生成的新字节”当
旧历史，正确性不被保证。解压器统一按 `out[t]=out[t-dist]` 逐字节生成、t 递增，
右侧可引用本轮刚写出的字节，语义即周期延拓，dist=1/2/3 任意长度均正确。单字节
循环每字节一次数组索引，对解压 O(n) 总复杂度只增加常数因子且零额外分配，可接受。

## 推导 3：两类距离错误

“距离超过已输出字节数”指流前缀合法但历史尚不足（如第一条记录就 dist=5）；
“距离超过窗口容量”指 dist>W——即使已输出很多也永远取不到，属结构性非法。
二者来源不同故分开报错。压缩器只从环形窗口已插入位置取候选，候选绝对距离
≤W，因而绝不产生后者；dist=0 也不可能（候选必在当前位置之前）。并行块以
“上一块原文末尾 W 字节”预置本块窗口：这些字节处于解压器已输出历史中，“已
输出字节数”仍从流起点累计，判距规则完全相同，无需特殊语义。

## 推导 4：并行确定性

块 i 的匹配器初始窗口 = 上一块原文末尾 W 字节（不足则全部），匹配范围是本块
原文；故其输出只依赖 block[i-1]、block[i] 两段确定输入，与完成顺序、其他块
结果无关。匹配器无随机状态（hash 链只依赖字节内容）、参数固定，同一切块下
结果唯一，与 workers 无关。衔接：块间不发刷新、不重置流，token 流首尾相接，
流头只写一次、流尾只在最后写；预置字典不是“新输出”，解压端无需块标记，单一
流式解压器直接消费同一 token 流即可还原全文。
