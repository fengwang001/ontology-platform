# 设计推导

## 1. 非法单元

采用最长合法前缀：首字节决定期望长度；后续字节须在 `80..BF`，第二字节还须满足下表。任一字节非法，则已收到的“合法前缀 + 当前非法字节”为一个单元；首字节非法时只吞该字节。未收满且流结束时，剩余整个前缀为一个截断错误，只替换一次。

因此 `F0 90 80 41` 中前 3 字节是等待尾字节的合法前缀，`41` 使 4 字节成一个单元；`E0 80 80` 的第二字节非法吞 2 字节，第三字节单独非法；`E2 82`+EOF 只吞未完成前缀 2 字节。

| 首字节 | 第二字节区间 | 非法第二字节吞 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 2 |
| `E0` | `A0..BF` | 2 |
| `E1..EC` | `80..BF` | 2 |
| `ED` | `80..9F` | 2 |
| `EE..EF` | `80..BF` | 2 |
| `F0` | `90..BF` | 2 |
| `F1..F3` | `80..BF` | 2 |
| `F4` | `80..8F` | 2 |
| `80..BF` | 不适用 | 1 |
| `C0 C1 F5..FF` | 不适用 | 1 |

## 2. 流式缓存

UTF-8 最长 4 字节，只需缓存首字节和至多 2 个后续字节，共 3 字节；但“非法第二字节”一到达立即连同首字节丢弃，`F0 90 80 41` 中 `41` 到达即判定。UTF-16 对齐前缓存 1 字节，代理状态缓存 2 字节；切到 8-bit 输入时 BOM 缓存 3 字节。

## 3. 并行切点

每段从切点向前回看至多 3 字节，找到一个可能由该点开始新字符的位置：切点字节不是 continuation，或前一字节不是可能跨边界的首字节。段 worker 从该“重叠起点”解析；输出只保留起始于切点或之后的标量/错误单元。若 worker 末尾拿到的完整单元起点早于切点，说明它已被前一段消费，输出层也跳过。`C3|80 80`：左段消费 `C3 80`，右段从 `80` 重扫出一个非法 continuation，第二个 `80` 再成一个非法单元。

## 4. 上限断点

输出在写入每个完整标量前检查容量；容量不足则不再检查后续字节，返回超限和本次已落盘标量对应的消费数。尚未落盘字节留在缓存，`Pending()` 返回它们；调用方用新实例并追加这些字节继续。统计中的已消费输入只含已落盘标量和已终止错误单元，缓存不计，因而守恒式在任意时刻成立。
