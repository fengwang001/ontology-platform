# 严格模式流式 Base64 设计

## 规范尾部推导

每个 Base64 字符承载 6 bit；四个字符组成 24 bit 块。

- 无填充：4 个字符全部有效，解码出 3 字节，24 bit 均有明文含义。
- 一个 `=`：前 3 个字符承载 18 bit，只解码 2 字节；第 3 个字符最低 2 bit 没有对应明文，必须为 `00`。
- 两个 `=`：前 2 个字符承载 12 bit，只解码 1 字节；第 2 个字符最低 4 bit 没有对应明文，必须为 `0000`。

因此，判定非规范尾部只看最后一个数据字符的未使用低位：

- `QUI=` 中 `I=001000`，末 2 bit 为 `00`，合法。
- `QUJ=` 中 `J=001001`，末 2 bit 为 `01`，拒绝。
- `QQ==` 中第二个 `Q=010000`，末 4 bit 为 `0000`，合法。
- `QR==` 中 `R=010001`，末 4 bit 为 `0001`；宽松截断会得到 `A`，但该比特不由明文字节决定，故严格拒绝。

这些零比特是规范编码器把明文字节移入 24 bit 块后自然留下的结果。允许非零值会使多个不同输入串解码成同一字节串，破坏“任意字节串的编码结果唯一”以及 `Encode(Decode(y)) == y`。填充形状也必须唯一：填充只能组成末尾组的 `x...=` 或 `xx==`，不得出现在流中其他位置。

## 语义与测试对照

| 语义 | 钉住该语义的测试函数 |
| --- | --- |
| 1. 标准字母表、填充与 MIME 换行 | 待测试实现后填写 |
| 2. 规范形式尾部 | 待测试实现后填写 |
| 3. 换行位置规则 | 待测试实现后填写 |
| 4. 五类错误与字节偏移 | 待测试实现后填写 |
| 5. 跨切分点一致 | 待测试实现后填写 |
| 6. 编码解码往返 | 待测试实现后填写 |
| 7. 输出上限与终态 | 待测试实现后填写 |

复杂度：解码器每个输入字节在进入函数时计入一次非导出计数器；字节进入状态机后不再重新扫描，因而 1 MB 逐字节输入的检查计数恰为输入字节数。
