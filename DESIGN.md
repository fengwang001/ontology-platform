# 严格 Base64 规范推导

一个 Base64 字符承载 6 bit，4 个字符共 24 bit。

- 无填充：4 个字符产出 3 字节，24 bit 全部有效，任何位都必须参与解码。
- `xx==`：前 2 个字符的 12 bit 中，只有前 8 bit 是 1 字节；第 2 个字符的低 4 bit 没有容量。
- `xxx=`：前 3 个字符的 18 bit 中，只有前 16 bit 是 2 字节；第 3 个字符的低 2 bit 没有容量。

严格模式要求所有“没有容量承载数据”的尾部位必须为零：

- `xx==` 检查第 2 个字符值的低 4 bit，即 `v1 & 0b1111 == 0`。
- `xxx=` 检查第 3 个字符值的低 2 bit，即 `v2 & 0b0011 == 0`。

因此 `QQ==` 中第二个 `Q` 的值为 16、低 4 bit 为 0，合法；`QR==` 中 `R` 的值为 17、低 4 bit 非零，必须拒绝。宽松解码器丢弃这些位后也得到 `A`，但这说明多段文本可映射到同一字节串。严格模式将无容量位固定为零后，每个字节串只有唯一规范编码，保证 `Decode(Encode(x)) == x` 与 `Encode(Decode(y)) == y`。

填充同时只能按整体长度使用：末组为 `xxxx`、`xxx=`、`xx==` 三种形态之一；缺填充、多填充、在末组之前出现 `=`，均不是另一字节串的规范编码。

## 语义与测试对照

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 标准字母表与填充 | 待补 |
| 规范形式 | 待补 |
| 换行 | 待补 |
| 五类错误及偏移 | 待补 |
| 跨切分点一致 | 待补 |
| 往返 | 待补 |
| 输出上限 | 待补 |
