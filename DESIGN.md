# DESIGN

## 1. 流格式（全部无符号 LEB128 varint，最多 10 字节）

| 记录 | 布局（按顺序） |
|---|---|
| 流头 | 魔数 `0x4C 0x5A`("LZ")，varint 版本=1 |
| 字面量段 | tag=0，varint 长度 n≥1，随后 n 个原文字节 |
| 回指 | tag=1，varint 距离 d≥1，varint 长度 l≥3 |
| 刷新标记 | tag=2（无负载，含义：截至此处可独立解码） |
| 流尾 | tag=3，varint 原文总长，varint FNV-1a 64 校验和 |

校验和对原文逐字节计算：h=14695981039346656037；每字节 `h=(h^b)*1099511628211 mod 2^64`。

## 2. 常量

窗口容量 W 与最大匹配长均为 65536，最小匹配 3，链长上限默认 64（均可配）。

## 3. 四处推导

### 3.1 与 Write 切法无关

压缩是贪心：位置 p 的输出只取决于「p 之前全部历史」与「p 起最长匹配长度 L(p)」。
判定 p 是否有匹配，必须能看到 p 之后最多 W 个字节；只要 p 后面还可能来字节，
L(p) 就可能继续变长。因此 Write 只追加进内部缓冲，不做任何决策；
仅当位置 p 之后已有 W 个确定字节时才定稿 p（或匹配已达上限 W，则再长也无法表示，
可提前定稿）。定稿后整段历史移入环形窗口，哈希链只插入已定稿位置，
故候选集合也与切法无关。Flush 要求此刻全部输入可被解码，于是它强制把
「后不足 W 字节」的尾部按无未来数据定稿（匹配到缓冲末尾即止）——即强制结束
当前匹配并输出刷新标记；Flush 之后恢复缓冲规则，历史仍在窗口中，可继续回指。

### 3.2 重叠回指的复制

d<l 时目标区与源区重叠。块复制（memmove 语义之外的"先拷整段"）会让后段读到
尚未写出的旧数据；正确语义是 dst[i]=dst[i-d] 按 i 递增逐字节进行，
效果是把长度 d 的模式周期延展。实现上用「按已复制量分块」的循环而非整段拷贝：
每轮复制 `min(d, 剩余)` 个字节，此时源是窗口里早已确定的 d 个字节，
用切片拷贝完成，轮数为 ceil(l/d)；d=1 时退化为逐字节，
但总拷贝量始终是 l，摊还 O(l)，4MB 同字节输入也无额外复杂度，可接受。

### 3.3 两种距离越界

「d>已输出字节数」：流引用了从未存在过的历史，属损坏（预置字典也救不了，
因为字典本应是"已输出"的前缀）。「d>窗口容量 W」：即使历史存在也无法表示，
压缩器受格式上限约束绝不可能产生它——每个回指发出时都断言 d≤W。
两者必须区分：前者可能是截断/拼接错误，后者说明流来自不兼容的更大窗口配置。
并行块预置字典不改变含义：块 k 的压缩器把上一块末尾 W 字节当作绝对位置
0..d0-1 的已输出历史喂入窗口与哈希链，块起点逻辑位置 d0；而流是连续的，
解压器此刻已真实输出了含该字典的全部前缀，故它看到的「已输出字节数」
天然 ≥ d0，同一条 d 校验规则无需特化。

### 3.4 并行确定性

块 k 压缩所需状态 = 窗口（恰为上一块原文末尾 W 字节）+ 空哈希链 + 本块原文，
与块 k-2 及更早块无关（W 之外的字节任何回指都无法引用），
也与其他块的压缩结果、完成顺序无关，是纯函数，故 workers=1/2/4/8 输出一致。
衔接无需刷新标记：块边界不承诺独立解码，压缩流仍是头 + 全部记录 + 单一流尾；
仅各块内部按 3.1 强制定稿尾部（不发 tag=2，纯内部动作），拼接记录即可。
