# 严格模式流式 Base64 设计

## 规范形式（第 2 条）的推导

Base64 把 3 个字节（24 bit）编码为 4 个 6-bit 字符。输入长度不是 3 的倍数时，
最后一组只有 1 或 2 个有效字节：

- 剩 1 字节：用 2 个 sextet（12 bit）装 8 bit，**低 4 bit 闲置**，补 `==`。
- 剩 2 字节：用 3 个 sextet（18 bit）装 16 bit，**低 2 bit 闲置**，补 `=`。

宽松解码器直接丢弃闲置比特，于是同一个字节串 `A` 有 16 种写法
（`QQ==`、`QR==`、...、`Q/==`）：第 2 个 sextet 的低 4 bit 任意取值，
解码结果都一样。也就是说在宽松解码下，「对任意字节串，编码结果唯一」
这一性质被破坏了。

严格模式要恢复这个性质，就必须只接受编码器真正会产出的那个串，
即接受集合恰好等于 Encode 的值域。编码器永远把闲置比特写成 0，
因此统一规则为：

> **最后一组中未被有效字节使用的比特必须全为 0。**

判定用到的比特：

- 尾部为 `==`：第 2 个 sextet 的**低 4 bit**（`v[1] & 0x0F`）。
- 尾部为 `=`：第 3 个 sextet 的**低 2 bit**（`v[2] & 0x03`）。

样例验证：`Q`=16=`0b010000`，低 4 bit 为 0，故 `QQ==` 合法；
`R`=17=`0b010001`，低 4 bit 为 1，故 `QR==` 拒绝。
`I`=8=`0b001000`，低 2 bit 为 0，故 `QUI=` 合法；
`J`=9=`0b001001`，低 2 bit 为 1，故 `QUJ=` 拒绝。

推论：严格解码下 `Decode(Encode(x)) == x`，且对被接受的 `y` 有
`Encode(Decode(y)) == y`；填充只能出现在整个流的末尾、总长必须是
4 的倍数，都是「接受集 = Encode 值域」的直接推论。

## 语义与测试对照表

（待测试写完后补充）
