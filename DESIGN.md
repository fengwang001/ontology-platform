# DESIGN — 统一格式 diff/patch

## 1. hunk 头零计数时的行号

统一定义：`a`/`c` 是该 hunk 覆盖区域**前一个已存在行的 1 基行号**（区域为空则取 0），`b`/`d` 是覆盖行数。
插入不覆盖任何旧行：在文件开头插入 → 前面没有行 → `-0,0`（样例 1）；在第 2 行后插入 → `-2,0`（样例 2）。
所以 `-0,0`/`-2,0` 是“前一行行号”，不是插入位置；插入位置 = 前一行之后（即第 `a` 行之后）。
删除后新文件无覆盖行：`+0,0`，其 `c=0` 同理（样例 3 `@@ -1 +0,0 @@`，b=1 省略 `,1`）。
`Apply` 解析：hunk 的删除/上下文消耗旧行；空区域（b=0）不消耗行，插入点即旧文件第 `a` 行之后。

| b 值 | 书写 `-a,b` | a 的取值 | Apply 解释 |
|---|---|---|---|
| b=0 | `-a,0`（显式） | 区域前一行号，开头为 0 | 第 a 行之后插入，不删除 |
| b=1 | `-a`（省略 ,1） | 覆盖的那一行号 | 匹配/删除第 a 行 |
| b=3 | `-a,b` | 首行号 | 从第 a 行起匹配 b 行 |

## 2. hunk 合并阈值

两 hunk 间未改动行数为 g。结论：**g ≤ 2C 合并，g > 2C 分开**。
C=3 时 GNU 行为：间隔 6 行公共行时合并（合并后上下文行不重叠，各 hunk 恰好带 3 行），间隔 7 行时分开（中间留 1 行不在任何 hunk 中）。
若阈值取 C：g=4..6 本应一个 hunk 却被拆开，与 GNU 不符。取 2C+1：g=7 被合并，公共区没有余量放满两组各 C 行上下文（7 < 3+1+3），也与 GNU 不符。故阈值恰为 2C。

## 3. 并列最短脚本的选择（确定性）

Myers O(ND) 贪心：每层 d 沿 k 从大到小扫描，k 的转移取
`k == d (或 V[k+1] < V[k-1])` 时向下走（删除一行），否则向右走（插入一行）；
即“同分时优先删除（down）”，回溯得到的脚本在每个等长点把 `-` 行排在 `+` 行之前。
`ab→ba`：d=0 k0 x=0；d=1 k=1 down→x=1，k=-1 右→x=0；d=2 k=0 取 k+1 分支 x=1，
蛇行到 (2,2)。脚本为 `-a  b  +a`（先删 a 再插 a，而非先插 b 再删 b）。同输入 100 次结果逐字节一致。

## 4. 偏移应用后后续 hunk 的平移

第 i 个 hunk 记录起点为 r_i，实际命中位置 p_i，偏移 δ_i = p_i − r_i。
第 i+1 个 hunk 的查找起点平移为 r_{i+1} + δ_i：已应用的 hunk 在旧坐标系中实际移动了 δ_i 行，
后续 hunk 必须以“上一 hunk 实际落点”为基准，否则两处都漂移时会找错或错过最近候选。
每个 hunk 仍在各自（已平移的预期位置 ±F 行内搜索；多候选取 |实际位置−预期位置| 最小，相同距离取靠前。

## 5. 其它

行切分保留原行尾（`\n`/`\r\n`/末行无尾），拼接即原文。补丁用 `\n` 作补丁自身行尾；
`\ No newline at end of file` 行标记前一内容行原文件无行尾，应用时据此去掉该行行尾。
错误四类互不重叠：`ErrMalformed`（含行数不符、非法行首）、`ErrContext`、`ErrOutOfRange`、`ErrTooDifferent`；
资源上限错误归类为 `ErrMalformed`（补丁本身不可接受）。应用全部 hunk 成功后才一次性替换（原子）。
