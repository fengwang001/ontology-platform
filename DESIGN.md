# 设计推导

## 代理对错误偏移（语义 2）

规则：高代理 `\uD800–\uDBFF` 必须由紧邻的低代理 `\uDC00–\uDFFF` 接续。
任何配对失败，偏移一律指向“使配对失败的那个 `\u` 单元的反斜杠”。

- `"\uD83D"`：其后本应有单元却没有，违规单元缺位 → 指向高代理起点（偏移 1）。
- `"\uD83Dx"` / `"\uD83DA"`：高代理后紧跟的不是 `\u` 单元；把错误记在
  违规者（`x`/`A`）会与“非法字符/控制字符”错误混淆，故仍记在高代理起点（1）。
- `"\uDE00"`：低代理不得单独出现 → 指向低代理起点（1）。
- `"\uDE00\uD83D"`：第一个单元是孤立低代理，立即拒绝，偏移指向它（1），
  不继续解释后面的高代理。

为什么 `"\uD83DA"` 不能是“U+FFFD + A”：高代理不是“坏字节”，它是一个
合法的转义，其语义被 RFC 8259 钉死为“与下一个单元配对”。静默替换会让
两个不同输入（`"\uD83DA"` 与 `"\uFFFDA"`）解码出同一字符串，破坏严格性
与往返唯一性；且 `A` 是普通内容，按配对规则高代理在看到它的瞬间即违约。

## 最小转义与唯一性（语义 4）

RFC 8259 要求转义的只有 `"`、`\` 与未转义控制字符 U+0000–U+001F。
U+001F 是最后一个 C0 控制字符，必须转义；U+007F 属于 DEL 而非 C0，
JSON 未要求转义，且它是合法 UTF-8 内容字节，原样输出即可被严格接受。
U+2028/U+2029、其他非 ASCII 同理：非控制、合法 UTF-8，原样输出。
短形式 `\b\f\n\r\t` 用于五个有缩写的控制字符，其余控制字符统一 `\u00XX`
（小写），`/` 永不转义。

单射性：编码器按固定顺序逐码点输出，每码点的输出形态由其值唯一决定，
因此 `Encode` 是确定性的、输出唯一。解码无歧义（每个字面形态只对应一个
码点，且未转义字节按 UTF-8 唯一解码），故 `Decode(Encode(s)) == s`；
又因输出是每个码点的最小唯一形态，`Encode(Decode(y)) == y`。

## 语义—测试对照

（表在实现完成后补齐）
