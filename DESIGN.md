# jstr 设计推导

## 第 2 条：代理对错误偏移

规则：偏移指向「使非法性得以确定的那个单元的起点」，分两种情形。

1. 低代理（DC00–DFFF）不能开启任何代理对，读到该单元本身即可
   判定非法，与后续无关 → 偏移指向该 \u 转义自己的反斜杠。
   故 "\uDE00" 与 "\uDE00\uD83D" 都报在偏移 1。
2. 高代理（D800–DBFF）开启一个「潜在合法」的对，流式解码器必须
   缓冲等待下一个单元，无法当场报错 → 偏移指向使配对失败的后续单元
   起点："\uD83D" 中结尾引号在偏移 7，报 7；"\uD83Dx" / "\uD83DA"
   中 x / A 不是反斜杠，报其偏移 7；"\uD83D\u0041"（后接完整 \u
   转义但不是低代理）报第二个 \u 的反斜杠。

理由：错误在「能确定的最早时刻」产生，偏移指向「导致失败的输入」
而非「无辜的等待者」；该规则确定、与切分方式无关。

## 为什么 "\uD83DA" 不能解释成「U+FFFD + A」

若孤立高代理被替换成 U+FFFD，则 "\uFFFDA" 与 "\uD83DA" 解码结果
相同，Decode 不再单射，输入信息被静默销毁；且替换边界任意
（"\uD83D\u0041" 是否也算 U+FFFD+A？）。RFC 8259 把孤立代理列为
互操作隐患，严格解码器唯一无歧义、可流式实现的策略是拒绝。

## 第 4 条：最小转义与唯一性

U+0000–U+001F 必须转义：RFC 8259 规定这些码点未转义出现即非法，
不转义则输出不可被任何合法解析器接受。U+007F 不在该区间，JSON 文法
允许原样出现，故不必转义；/、非 ASCII、U+2028/U+2029 同理。

唯一性：每个码点的输出写法唯一——" 与 \ 各只有一种转义；控制字符
按固定表得唯一短形式或 \u00xx（小写、定长）；其余码点原样。故
Encode 输出唯一。单射：输出按反斜杠分段可唯一还原——短形式字母、
\u 后四位、原样字节三类互不相交（原样段不含 " \ 及控制字符），
不同输入必产生不同输出。

## 语义 ↔ 测试对照

（待测试写完后补充）
