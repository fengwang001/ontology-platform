# DESIGN

统一格式差分/补丁器的设计推导（对应题面第三节）。结论均已用 GNU diffutils 3.10 实证核对。

## 1. 零计数时的行号（`-a,0` / `+c,0`）

样例：`x\n`→`y\nx\n` 得 `-0,0 +1`；`a b c`→`a b X c` 得 `-2,0 +3`；`a\n`→空 得 `-1 +0,0`。
推导：计数 ≥1 时，起始号是「区间第一行的 1 基行号」。计数为 0 时区间为空，不存在
「第一行」，于是 GNU 退而记录**插入点前一行的行号**（在文件最前插入时前一行的行号
为 0）。三个样例分别对应「前一行是第 0 行 / 第 2 行 / 新文件第 0 行」，完全吻合。
所以这个数字描述的是锚点（已有行），而不是「将要插入的位置」——插入的内容还不存在，
没有行号可指。`Apply` 的解释：`-a,0` 表示插入点在老文件第 `a` 行之后（0 基下标 `a`）。

| 情形 | 头的写法 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一行行号（文件首为 0） | 插入点在老文件第 a 行之后 |
| b=1 | `-a`（省略 `,1`） | 区间从第 a 行起，含 1 行 |
| b≥2 | `-a,b` | 区间从第 a 行起，含 b 行 |

## 2. hunk 合并阈值 = 2C

每段改动前后各带 C 行上下文。两段改动间隔 g 行未改动行时，前 hunk 的尾部上下文与
后 hunk 的头部上下文合计要覆盖 2C 行：g ≤ 2C 时上下文相接或重叠，只能合并；g > 2C
时中间至少剩 1 行不属于任何上下文，必须分开。实测（C=3）：g=6 合并、g=7 拆分；
C=0：g=0 合并、g=1 拆分，与 `diff -U C` 完全一致。
若阈值取 C：C=3、g=5 时 GNU 输出 1 个 hunk，按 C 会拆成 2 个，矛盾。
若阈值取 2C+1：C=3、g=7 时 GNU 输出 2 个 hunk（第 9 行无归属），按 2C+1 会合并，矛盾。

## 3. 并列最短脚本的选择：删除优先

Myers 每步在「向下（插入）」与「向右（删除）」之间选择。本实现仅当 `V[k-1] < V[k+1]`
时取插入，相等时取删除，即**同 D 并列时优先删除**。效果：`-` 行排在 `+` 行之前。
例 `a b`→`b a` 生成 `-a`、` b`、`+a`（先删 a、保留 b、末尾插 a），与 GNU diff 一致。
该规则由算法常数决定，与输入数据无关，因此同一输入的补丁文本逐字节确定。

## 4. 偏移应用后后续 hunk 的平移

补丁里的行号都是相对**原始旧文件**的。目标文件中，第 i 个 hunk 若以偏移 k 应用成功，
说明目标内容相对补丁记录位置整体位移了 k；同时每个已应用 hunk 把后续内容推移
`newCount-oldCount` 行。两者都会改变后续 hunk 在目标中的实际位置，因此查找起点必须
平移：`base = OldStart-1 + Σ(new-old) + Σk`（`OldCount=0` 时 `OldStart-1` 换成
`OldStart`）。每应用一个 hunk 就把它的 `k` 与行数差累加进平移量，否则后续 hunk 会在
错误位置附近搜索，导致误配或漏配。
