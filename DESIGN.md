# 设计推导

## 1. 非法单元切分（统一规则）

首字节决定期望长度 L 与第二字节合法区间；第 3..L 字节须为 80..BF。

- 首字节非法（80..BF、C0 C1、F5..FF）：吞 1 字节，输出 1 个 FFFD。
- 解码途中某必需字节非法（第二字节越界，或其后任一字节不是 80..BF）：从首字节起**已收下的全部字节**整体算 1 个非法单元（1 个 FFFD）；那个非法字节不吞，回炉重新当首字节。
- 已收下的前缀仍合法但遇 EOF：整个前缀算 1 个非法单元（替换模式 1 个 FFFD，严格模式报截断）。

逐条验证：`F0 90 80 41` 中 90∈[90,BF]、80∈80..BF，直到 41 非续字节，F0..80 整体 1 单元，41 回炉成 A → FFFD 41。`E0 80 80` 第二字节 80 越界，E0 单成单元，两个 80 各成单元 → 3。`ED A0 80`（A0 越 80..9F）同理 3。`C0 AF` 两个非法首字节 → 2。`F4 90..` 第二字节越界，F4 一个 + 三个续字节 → 4。`E2 82`+EOF 整体未完成前缀 → 1。`80 80` 两个孤立续字节 → 2。

## 2. 首字节与第二字节区间表

| 首字节 | L | 第二字节区间 | 非法时吞掉 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 见下 |
| E0 | 3 | A0..BF（防非最短） | 见下 |
| E1..EC | 3 | 80..BF | 见下 |
| ED | 3 | 80..9F（防代理） | 见下 |
| EE..EF | 3 | 80..BF | 见下 |
| F0 | 4 | 90..BF（防非最短） | 见下 |
| F1..F3 | 4 | 80..BF | 见下 |
| F4 | 4 | 80..8F（防 >10FFFF） | 见下 |
| 80..BF | — | 无（首字节非法） | 1 字节 |
| C0 C1 F5..FF | — | 无（首字节非法） | 1 字节 |

多字节行的"吞掉"：第二字节越界吞 1（首字节）；第 3/4 字节非续字节时吞"首字节起已收字节"（2 或 3）；EOF 吞整个前缀；越界字节本身一律回炉。

## 3. 缓存上限、即时判定与检查次数

最长序列 4 字节，只需缓存首字节 + 至多 2 个已确认续字节（≤3 字节）。看到非法必需字节的当刻即判非法（不等 EOF）；仅"前缀合法但缺字节"才等待，EOF 补 1 个 FFFD。解码器为状态机，每字节检查一次，回炉字节最多再检查一次，故检查次数 ≤ 2N，与切分方式无关（1 字节一段也不回扫）。

## 4. par 切点对齐

段 k（k≥1）从切点 b 回看 ≤3 字节：若 data[b-1] 为续字节，则把连续续字节及其前一首字节划给本段（realStart 前移）；否则 realStart=b。段 0..k-1 丢弃从 realStart 起的全部产出与统计（字节仍计入该段已消费，再由统计聚合时扣除重叠），跨界单元只在段 k 完整解析一次。段 0 结束时残留前缀不输出（交下段）。例：C3|80 80 → realStart 移到 C3，下段解出 é 后第二个 80 成 1 非法单元，与单线程相同。每界仅回看常数字节，检查次数 ≤ N+8·常数。

## 5. 上限断点

编码输出标量前先查 len(out)+编码长度 ≤ limit，不足即停：该标量不编码、其输入字节不消费。Write 返回的 n 只含已完整输出标量对应的输入字节；切分缓存中的前缀字节不算已消费（调用方重传 p[n:]）。新实例续传后拼接与不设上限逐字节一致。
