# 可撤销批量重命名与冲突解析器 — 设计

## 0. 模型
- 命名空间 `name.Space`：名字集合（有序快照），仅校验名字不含 NUL；空串合法，`/` 等路径符按普通字符处理。
- 请求 `Req{Old,New}`；一步原子基本操作把一个现有名改成不存在的名（否则覆盖，禁止）。
- 五个包：`name`(集合/校验/锁)、`plan`(冲突/排序)、`cycle`(破环)、`apply`(执行/日志/回滚)、`undo`(逆序撤销/截断分类)。

## 1. 顺序推导（拓扑序）
依赖：请求 `x→y` 若 `y` 是某个尚未执行请求的旧名，则必须等 `y→*` 先搬走，否则会覆盖它。
即建依赖边 `y -> x`（y 的请求先于 x）。图中每个顶点入度、出度至多 1：
- 连通分量只能是「链」或「纯环」。
- 链从汇点（其新名无人占用/无人再搬）开始执行：`{a→b,b→c}` 得 `b→c` 后 `a→b`，每步目标名均不存在。
- 纯环无拓扑序，必须破环（第 2 节）。
- 同层可并行的多个起点按「旧名字典序」排序；链整体按各自最小名字典序处理，保证结果对构造顺序无关且确定。

## 2. 破环通用做法与临时名数量
对环 `v0→v1→v2→…→v(n-1)→v0`：任选一个顶点（取字典序最小，保证确定性）做
`v0→T, v(n-1)→v0, v(n-2)→v(n-1), …, v1→v2, T→v1`（T=临时名）。
环上其余 n-1 个顶点不变，每个环只引入 **1 个**临时名；不同环各取一个，故临时名数 == 环数。
T 由固定前缀 `.rename-tmp-` 加序号生成，每次校验 T 不在「初始现有名 ∪ 全部请求新名」中，冲突则序号递增重试，
超过界（尝试覆盖所有候选且均被占）返回可判定错误 `ErrNoFreeTemp`，绝不覆盖数据。

## 3. 四类冲突（均可用 errors.Is 区分；检测先于任何修改）
1. `ErrTargetExists`：New 已在命名空间中，且 New 不是本批某请求的 Old（不会被搬走）。
2. `ErrDupTarget`：两个不同请求的 New 相同；错误中给出两个 Old。
3. `ErrDupSource`：同一 Old 出现两次；错误中给出两个 New。
4. `ErrMissingSource`：Old 不在命名空间中。
检测只对初始快照进行，全部通过后才允许执行，冲突时空间逐元素不变。

## 4. 自环语义
`a→a` 定义为 **无操作（noop）**：不冲突、不产生执行步骤、不需要撤销。重复 Old 检测先剔除自环（自环 Old 不参与重复判定）。

## 5. 执行、失败回滚与撤销
- 执行全程持有 `Space` 锁（`Lock`），期间任何外部 `Rename` 阻塞；测试用超时断言阻塞（文档定义为阻塞语义）。
- 每步先断言目标名不存在再移动；注入失败时把已执行步按逆序用日志记录反向移动，自动回滚到与执行前逐元素相同。
- 日志（临时目录，每批一个文件）：魔数头 `RENLOG1\n`；帧 `uint32 payloadLen | payload(JSON:old,new,dir) | uint32 CRC32(payload)`；
  每帧后追加空帧头（4 字节 0）作为 trailer。前向记录 `dir=1`，回滚逆序写 `dir=-1`；故成功日志正向撤销时逆序并交换 old/new。

## 6. 截断分类与最大可恢复前缀
在字节位置 p（1..len-1）截断：
- `p < 9`：`ErrHeaderIncomplete`（头部不完整），可撤销 0 步。
- 落在完整帧区之后、trailer(4B 0) 之内：`ErrRecordIncomplete`，该 trailer 仅为定界符，可撤销 = 已完整帧数。
- 落在某帧的长度头/载荷内（长度头声明值超出尾部）：`ErrRecordIncomplete`（记录不完整）。
- 帧载荷完整但其后 CRC 被截（0..3 字节）：`ErrCRC`（CRC 不匹配/缺失）；该帧不可信，恢复其之前的完整帧前缀。
恢复规则：按从后往前丢弃坏帧/不可信帧，得到「最大可恢复前缀」，逆序撤销之，并报告无法撤销的步骤。
重复撤销：成功撤销后日志删除（标记完成）；对同一日志再次撤销为空操作。

## 7. 复杂度
冲突检测与排序只用 map/数组常数次访问，与「请求数 + 名字数」线性；`plan` 用非导出计数器 `lookups`
统计 map 查找，断言 5 万请求时 `lookups <= 4*(请求数+涉及名字数)`。

## 8. 边界
空请求集：0 步成功，空日志，撤销无操作。单请求：直接一步。请求数远大于名字数（大量重复 Old）→ ErrDupSource。
