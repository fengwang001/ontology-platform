# LZ77 流式压缩设计

## 流格式（wire 包）

所有整数均为 uvarint（7bit/字节，小端序，最长 10 字节且第 10 字节 ≤1，否则报变长整数错）。

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 4 字节 `O L Z 1` + 版本 1 字节 `0x01` |
| 字面量段 | tag=0, 长度 n, 随后 n 个原始字节 |
| 回指 | tag=1, 距离 dist(≥1), 长度 len |
| 刷新标记 | tag=2（无负载） |
| 流尾 | tag=3, 原文总长, 原文 FNV-1a-64 校验和 |

流尾之后不允许有任何字节。校验和逐字节滚动累加（`h=(h^b)*prime`），无需缓存全文。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

问题：在 Write 边界上，当前位置的最长匹配可能延伸到尚未到达的字节；字面量也可能因后续字节凑成 ≥3 的匹配而非字面量。若按到达即输出，切法不同结果就不同。

推导：一个位置 p 的决定（字面量或回指）只依赖 `[p-windowCap, dataEnd)` 的字节。若最佳匹配在 `dataEnd` 处结束（`p+len == dataEnd`），更多输入可能延长它——决定必须推迟；若匹配在 `dataEnd` 之前结束，失配字节已知，决定是最终的。同理，若任何候选匹配触及 `dataEnd`（哪怕长度 <3），字面量决定也可能被推翻，必须推迟。匹配长度设上限 64KiB：达到上限的匹配不可能再延长，可立即提交。可用字节不足 3 时也无法排除未来匹配，同样推迟。

结论：编码器缓存未决字节，仅提交「不触及数据末尾」的决定；提交的记号序列因此只是 `(原文, Flush 偏移)` 的函数，与 Write 切法无关。代价是缓冲区有界（windowCap + 64KiB + 少量）。推论：`Flush` 必须强制结束当前匹配/字面量段——它把「数据末尾」人为定在 Flush 点，让所有未决决定以 final 语义提交，并发出刷新标记；这也是 Flush 后解压器能还原全部已输入数据的原因。

## 推导二：重叠回指的解压复制（第 2 条）

距离 < 长度时，回指的源区间与目标区间重叠：逐字节向前复制时，刚写出的字节立即成为后续源字节（如 dist=3,len=30 还原 `abcabc…`）。一次性 `copy` 源区间是复制前的快照，重叠部分拿到的是旧内容，结果错误。

结论：分块指数增长复制。每轮从窗口取 `n = min(剩余, 窗口现存量 - dist + 1)` 字节（首轮 n≥1 恒成立），追加到输出与窗口后，新字节成为合法历史，下一轮 n 至少翻倍（dist=1 时 1,2,4,…）。语义与逐字节复制完全一致，块数为 O(log len)，不退化成逐字节循环。

## 推导三：「距离超过已输出」与「距离超过窗口」为何区分（第 7 条）

二者约束来源不同：「超过已输出」是流本身的逻辑错误（引用不存在的历史）；「超过窗口容量」是格式对解码器资源的承诺（解码器只保留 windowCap 字节，超出即无法取回）。压缩器的不变量是 `dist ≤ min(已输出, windowCap)`，因此合法流两者都不会触发；但损坏流可能只违反其一（如 dist=5、已输出=2、容量=64K 只违反前者；dist=1M、已输出=2M、容量=64K 只违反后者），必须分别报告。并行块压缩的预置字典不改变「已输出」的含义：字典就是上一块原文的尾部，解码时那些字节已经真实输出过，块内回指在解码器看来就是普通合法回指，校验口径不变。

## 推导四：并行块压缩的确定性（第 9 条）

块 i 的压缩记号 = 纯函数 `(块 i 原文, 块 i-1 原文末尾 windowCap 字节, 配置)`。预置字典取自上一块的**原文**而非其压缩结果，因此不依赖其他块的压缩产物、完成顺序或 workers 数量；worker 只把结果写入各自槽位，最后按块序拼接，故 workers=1/2/4/8 输出逐字节相同。块间无需刷新标记：记录自定界，解码器对块边界无感知；字典回指的距离 ≤ windowCap 且 ≤ 已输出，天然通过解码校验。流头写一次，流尾的总长与校验和对整份原文计算。

## 复杂度

哈希链匹配器：每个位置只入链一次（O(1) 摊还），每次查询最多考察 maxChain 个候选，候选考察总数 ≤ maxChain × 输入字节数，与窗口规模无关（`match` 内非导出计数器 `candidates` 实测，见 AUDIT.md）。单实例压缩器/解压器均非并发安全；并行压缩仅在块间并发。
