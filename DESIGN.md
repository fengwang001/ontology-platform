推导（第三节）

1) 与 Write 切法无关
压缩器始终保留至少一个"未决字节"：只有当位置 p 之后还能看到一个字节时，才对 p 做匹配
决策。若在 p 找到匹配且当前长度未达上限，决策可能被后续字节延长，故推迟；下次写入再看。
一旦 p 处匹配长度不能再增长（达到最大长度 32768，或下一个字节打破匹配），或 p 最终不
能匹配，才提交：发字面量段（p 之前积累的未决字节）或发回指。决策只依赖"已写入压缩器
的全部字节"，与这些字节分几次 Write 到达无关，故输出逐字节相同。Flush/Close 强制把未决
字节全部裁决（Flush 同时发刷新标记），所以 Flush 时匹配必须终止——这也是 Flush 的承诺。

2) 重叠回指解压
距离 d < 长度 l 时，块复制 src=out[n-d:n] 与 dst=out[n:] 重叠，memmove 语义下后写的
位置会读到刚复制出的新字节，语义恰为"按距离 d 周期重复"。本实现直接逐字节 out[i]=
out[i-d]（i 递增），简单且天然正确；每字节一次环形窗口读取，l 次索引，成本与块复制同阶，
不退化复杂度。

3) "距离超过已输出" vs "距离超过窗口容量"
前者是流的时序错误：回指指向尚未产生的字节，任何合法压缩器都不可能产生，截断/伪造流才
会出现。后者是配置错误：回指指向比窗口更早、已被环形缓冲覆盖丢弃的历史；合法压缩器只
在窗口内找候选，也不可能产生。二者成因不同，必须分别报错并带字节偏移。并行预置字典只
是在压缩第二块前把上一块尾部 WindowCap 字节装进窗口，解压端则因为流是连续的、这些字典
字节本就是此前已输出的真实前缀——"已输出字节数"含义不变（随流单调增长），无需额外信号。

4) 并行确定性
每块只读取 data 中固定对齐的区间；除本块原文外只需要上一块末尾 WindowCap 字节作预置
字典，不读取任何其他块的压缩输出，也不共享可变匹配器状态（每 goroutine 独立压缩器），
故完成顺序不影响结果。衔接方式：第 0 块输出流头+记录，后续块只输出记录（其回指自然落入
解压端已输出的前缀），最后一块输出流尾；块间不需要刷新标记，刷新标记只表示"此前全部输入
此刻可被还原"，块边界不作该承诺。

流格式记录布局（整数均为无符号 LEB128 变长整数）
记录            | 布局
流头            | 魔数 0x4F 0x4E 0x54 0x31，版本 0x01，varint(windowCap)
字面量段 tag=0  | tag(0)，varint(长度 n>=1)，n 个原始字节
回指   tag=1    | tag(1)，varint(distance-1)（d>=1），varint(length-minMatch)（minMatch=3）
刷新   tag=2    | tag(2)
流尾   tag=3    | tag(3)，varint(原文总长)，varint(FNV-1a 64 校验和)，末字节 0x00
空输入同样产生 流头…流尾 的非空合法流；0 字节输入给解码器一律判截断。

参数：minMatch=3，maxMatch=32768，哈希链哈希=(b0*131^2+b1*131+b2)&0xffff，表头 65536，
候选链上限可配置（默认 64）。压缩器/解压器单实例非并发安全。
