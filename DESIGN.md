# 只读快照一致性导出器 — 设计推导

## 1. 模型
- `store`：并发键值存储。每次 `Put(k,v)` 产生单调递增版本号；`GetAt(k,ver)` 返回 `ver` 时可见值，删除用墓碑表示，与“键不存在”区分（首版本前不存在）。
- `snapshot`：`New()` 记录水位版本 `ver`，并向 store 注册。导出全程只经快照读。
- 单文件布局：`MANIFEST(len[4] manifest-json) || (header[12] || body)*`；header=`index BE uint32 || bodyLen BE uint32 || crc32-IEEE(body) BE uint32`。

## 2. 写时复制（COW）的触发点
导出进行中若直接覆盖，导出读到第 80 个键时它已是新值，快照被污染。
正确触发点在 store 的 `Put` 内、**覆盖生效之前**：遍历所有活跃快照，只要存在快照水位 `>= 该键当前版本`（即该键旧值对快照可见），就把当前 `(value, version)` 复制进该快照的保留表，再写入新值。
导出读键时：保留表命中则用保留值；否则读当前存储值。因此“导出到第 50 键时第 80 键被改写”，第 80 键仍读出快照时刻旧值。新插入键当前版本为 0，不被任何旧快照保留，快照遍历键集时用记录的版本水位过滤掉高版本新键。

## 3. 保留值释放：为什么按最老快照判断
保留表只在“至少一个仍需看到旧值的活跃快照”存在时必要。store 维护活跃快照按版本排序，最老快照水位最低、可见的历史区间最长：
- 一个旧值若对最老快照都不再可见（键版本 > 最老水位，说明该旧值诞生于最老快照之后），则对更新的所有快照同样不可见，无人需要；
- 反之只要最老快照仍活跃，其水位之后被覆盖的键都可能被它读到，必须保留。
故 `Put` 只需与最老快照水位比较一次即可决定是否 COW，且保留表挂在每个快照上。快照 `Close` 时注销并清空自己的保留表；两个重叠快照各自只保留自己需要的项，两者都 Close 后保留计数归零。Close 与进行中导出用 WaitGroup 同步：Close 等待在途读取结束，之后再导出得到 `ErrSnapshotClosed`，不会产出半截“完整”文件。

## 4. 内存与读取约束
- 保留项数 = 导出窗口内“对该快照可见且被覆盖”的不同键数，故与被改写键数成正比、与总键数无关；删键计数同样受限。
- 每个键只在其所属块读取一次（块内先收集 entry 再编码），读取次数严格等于键数。
- 一次只组装一个块：读一块 entry → 编码 → 写盘 → 丢弃，驻留峰值为一块（加定长头）。

## 5. 导出顺序无关性
清单总校验和不依赖块/遍历顺序：对“键的规范编码 `klen BE32 || key || vlen BE32 || value`”按**键字典序排序**后逐段 CRC32 累加。字典序与逆序导出产生的块划分可不同，但总校验和相同；逐字节一致性校验则逐块重算 body CRC 并重算有序总校验和。

## 6. 分块、续传与快照过期
- 确定性切块：先快照读全部 entry 的规范编码并按键序拼为逻辑字节流（分块不额外读键），按固定 `blockSize` 切为 body（末块可短，空存储为 1 个空体块）。
- 续传：解析现有 manifest 与已落盘块，校验已有块的 index 连续与 body CRC；随后只重写第 k 块及之后。**续传必须重新传入同一快照句柄**：若它已 Close，立即返回 `ErrSnapshotClosed`，不动已有字节，杜绝用当前数据拼出跨时刻文件。
- 单块大于数据量正常；`blockSize==0` 返回 `ErrBlockSize`。

## 7. 截断分类（四类可判定错误）
对文件逐字节截断，按长度落点分类：
1. 未含完整 manifest 区 → `ErrManifestTruncated`，从块 0 重来；
2. 停在某块 12 字节头内 → `ErrHeaderTruncated`，从该块续；
3. 有完整头但 body 不足 `bodyLen` → `ErrBodyTruncated`，从该块续；
4. body 完整但重算 CRC 不符（翻转/脏写）→ `ErrCRC`，从该块续。
纯截断本身不制造 CRC 错（body 完整即原样），CRC 类用等长字节损坏单独构造；四类均可用 `errors.Is` 区分。乱序/缺块由块 index 序列定位并报出具体块号。
