# DESIGN — 近似最近邻分桶检索器（随机超平面 LSH）

模块 `ontology`，仅标准库。包划分：`vec`（向量/距离/校验）、`hyper`（超平面族与签名）、
`bucket`（签名到 ID 的桶表）、`search`（候选生成+精排 TopK）、`persist`（落盘/读回/恢复）、
`cmd/demo`（演示判定）。

## 1. 签名计算规则

- 每张表 t 有 b 个超平面，法向量 n_i 属于 R^d，分量取自标准正态（`rand.NormFloat64`）。
- 向量 v 的签名为 b 位整数：`bit_i = 1 当且仅当 dot(n_i, v) > 0`，否则为 0。
- 内积恰为 0 的约定：取 0。因此零向量（合法输入）与任何超平面内积恒为 0，
  签名恒为全 0，所有零向量落在同一桶，互相可达，语义自洽。
- 几何含义：bit_i 记录 v 在超平面哪一侧；两向量签名汉明距离越小，夹角越可能小。

## 2. 可复现性

- 每个超平面族由显式种子生成：`hyper.New(dim, bits, seed)` 内部用
  `rand.New(rand.NewSource(seed))` 顺序消费随机流，不依赖全局随机源与时间。
- 第 i 张表（0 起）的种子为 `seed+i`：表数从 L 增到 L+1 时前 L 张表逐字节不变
  （候选集合是嵌套超集），这是召回随 L 单调不降的构造性保证。
- 同一种子流顺序产生 bits*dim 个法向量分量，因此 bits=8 的族是 bits=12 的族的前缀：
  12 位签名的低 8 位即 8 位签名。这是候选数随 b 单调不增的构造性保证。
- 同种子两次建索引，全部签名逐字节相同（测试逐向量断言）；不同种子必不同。

## 3. 位数 b、表数 L 与候选数、召回的关系

设两向量夹角为 theta，随机超平面把它们分到同侧的概率为 `p = 1 - theta/pi`
（近邻 p 大，远点 p 小）。单表 b 位签名完全相同的概率为 `p^b`；
L 张独立表下，两向量至少在一表同桶（成为候选）的概率：

```
P(candidate) = 1 - (1 - p^b)^L
```

定性推导：

- b 增大：`p^b` 对近邻、远点都指数衰减，但远点（p 小）衰减更快，桶被切得更细，
  每桶期望向量数约 N/2^b 指数下降，所以候选数随 b 单调下降（本实现以前缀构造保证
  严格不增）；同时近邻同桶概率也下降，召回随 b 下降，精排距离计算次数随之减少。
- L 增大：`1-(1-p^b)^L` 对任意 p>0 严格递增，所以召回随 L 单调不降（本实现以
  嵌套表构造保证）；候选数近似线性放大（去重后次线性），精排成本上升。
- 权衡：b 控制每桶粒度（精度/成本），L 控制重试次数（召回）。实践上先按候选预算
  `N/2^b * L 远小于 N` 选 b，再加大 L 至召回达标。本题取 d=32、N=5000、b=8、L=8：
  期望候选约 5000/256*8 约 156，远小于 500（10% 上限），实测见 FINDINGS.md。

## 4. 复杂度约束（可证明）

- 建索引哈希计算次数：每向量在每表算 b 次内积，恰为 `N*L*b`，计数器精确断言。
- 精排距离计算次数 = 去重后候选数，测试断言在 N=5000 上平均每查询不超过 500
  （10%），否则即退化为暴力扫描。退化情形（全部向量相同，全落一桶）候选数等于 N，
  此时召回必为 1，单独断言，不适用 10% 上界。

## 5. 持久化格式（persist）

```
头部(30B): magic "OLSH" | ver u16 | dim u32 | bits u32 | tables u32 | nvecs u32 | seed i64
超平面区:  tables * bits * dim 个 float64（小端）
桶表区:    每表 nbuckets u32，每桶 sig u64 | cnt u32 | cnt 个 u32 id
尾部:      CRC32(IEEE) u32，覆盖此前全部字节
```

- 读回按区解析，截断点落在哪个区就报哪类错：`ErrTruncHeader` / `ErrTruncPlanes` /
  `ErrTruncBuckets` / `ErrCRC`（`errors.Is` 可判定）。结构校验先于 CRC：法向量全零
  （退化，无法划分）报 `ErrDegenerate` 并指出第几张表第几位。
- 恢复（`Recover`）忽略 CRC，顺序解析出最大可恢复前缀：头部完整后，逐表要求
  超平面与桶表都完整才纳入；桶表中不小于 nvecs 的悬挂 ID 一律剔除并计数，
  保证恢复结果自洽。

## 6. 并发与边界语义

- 索引视图（超平面族+桶表切片）整体放在 `atomic.Pointer` 里，建表先完整构建再原子
  发布：查询要么看到旧视图要么看到新视图，永远看不到半成品表。查询只读，可多协程并发。
- 边界：空向量集（合法，查询返回空）；单向量；K>N 返回全部；NaN/±Inf 向量在建索引时
  拒绝并计入 `Skipped()`；查询向量维度不符返回 `vec.ErrDim`（报期望/实际维度）；
  维度为 1 合法；零向量合法（签名全 0，见第 1 节）。
