推导约定：路径按字节 `/` 切段，空串路径是**零段**；非空路径切出的首尾/连续 `/` 产生真实存在的空段（`x/` → `["x",""]`）。普通模式段恰好消费一个路径段；`**` 段是「零或多个整段」标记，本身不做段内匹配。不做任何规范化。

## 1. `x/**` 与 `**/x` 的边界（同一条规则）
规则只有一句：`**` 可消费 0 个或多个**任意整段（含空段）**，其余段各消费 1 段。
- `x/**` 对 `x`：`x` 消费 1 段，`**` 消费 0 段 → 匹配。对 `x/`：`**` 消费尾部空段 → 匹配。对 `x/y`：消费 `y` → 匹配。
- `**/x` 对 `x`：`**` 消费 0 段 → 匹配。对 `/x`（段 `["","x"]`）：`**` 消费首部空段、`x` 消费 `x` → 匹配。
- `a/**/b` 对 `a/b`：中间 `**` 消费 0 段，同一条规则，无特例。

## 2. 空串路径
空串 = 零段，连「空段」都不存在。`*` 是普通段，必须消费一个段（该段可以是空段，如 `x/` 的尾段），但零段时无段可消费 → `*` 不匹配空串；`?` 需一个码点也不匹配；`**` 可消费 0 段 → 匹配空串；空模式编译为零段序列，恰匹配零段路径。即「`*` 匹配空的段，不匹配没有段」。

## 3. 无回溯算法
段内用线性 glob：顺序比对，遇 `*` 只记录最后一个星号的回退点（模式位、输入位），失配时回到该点把输入多吞一个码点重试。只记最后一个 `*` 正确：若某分割让前缀经更早星号扩展后匹配，则在到达更晚星号处，把更早星号多吞的码点改由更晚的 `*` 吞掉（二者吞的都是任意码点序列），后缀不变，故更早回退点不产生新解。
跨段不能照搬：`**` 的转移粒度是整段，其后随的普通段要求**整段精确**匹配，贪婪吞段无法用「多吞一个再试同一后缀」局部回退（一个模式段对输入段要么整段成要么整段败，且多个 `**` 与普通段交替），单回退点覆盖不了所有消费分割。故用段级可达集合：状态为模式段位置集合，读入一个路径段后，普通段在段匹配成功时推进 1 格，`**` 格可停留（再吞一段）或推进 1 格（吞 0 段）。每个（模式段,输入段）对至多做一次段内匹配，总量 O(模式长度×路径码点数)，无指数回溯。

## 4. 规则集版本一致
规则集编译成不可变快照（规则切片+版本号），由 `atomic.Pointer` 持有；每次查询/`Explain` 只 `Load` 一次并全程引用该快照，结论、序号、原文必属同一版本。替换时先把全部新规则编译成功才 `Store`；任一失败直接返回错误、不 `Store`，旧版本原封不动。

## 结果表
| 模式＼路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `*` | 否 | 是 | 否 | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `x/**` | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
