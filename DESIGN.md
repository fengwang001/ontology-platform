# DESIGN

## 非法单元

统一规则：先把一个首字节当作其声明长度 `n` 的候选；读第二字节时立即检查其是否属于该首字节允许的区间，非法则只吞掉首字节，下一字节重新成单元；若第二字节合法、后续出现非法续接字节，则保留首两字节并只吞掉那个非法字节；续接字节缺失到流结束才吞掉此前的合法前缀。因此 `F0 90 80 41` 的第二字节 `90` 对 `F0` 合法，`41` 是非法续接，整体合法前缀为一个单元；`E0 80 80` 的 `80` 对 `E0` 非法，先吞 `E0`，剩下两个续接字节各自成单元。`C0 AF` 同理为两个单元；`ED A0 80`、`F4 90 80 80` 第二字节越界后，剩余续接字节各自成单元。`E2 82` 到流结束仍是一个合法前缀，故只有一个单元。

| 首字节 | 第二字节合法区间 | 第二字节非法 | 续接非法 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 吞 1 | 吞 1 |
| `E0` | `A0..BF` | 吞 1 | 吞 1 |
| `E1..EC` | `80..BF` | 吞 1 | 吞 1 |
| `ED` | `80..9F` | 吞 1 | 吞 1 |
| `EE..EF` | `80..BF` | 吞 1 | 吞 1 |
| `F0` | `90..BF` | 吞 1 | 吞 1 |
| `F1..F3` | `80..BF` | 吞 1 | 吞 1 |
| `F4` | `80..8F` | 吞 1 | 吞 1 |
| `80..BF` | 无 | 吞 1 | — |
| `C0 C1 F5..FF` | 无 | 吞 1 | — |

## 切分缓存

状态机只保存 `need`、已收到长度和已解码值，最多等 4 个字节，外部待决缓存硬上限为 3。首字节非法、孤立续接字节、以及第二字节落在非法区间时立刻产出错误；第二字节合法后，每个错误续接字节也立刻产出。只有合法续接字节数量不足时才等待下一次 `Write` 或 `Close`，所以半个合法字符不会被提前替换；`Close` 时缓存中若有待决前缀，按一个截断/替换单元处理。UTF-16 最多缓存 1 个奇数字节，再加一个已确认高代理等低代理，形成高代理后第二个 code unit 立即可决。

## 并行切点

每段先把原切点向前找安全边界：UTF-8 从切点前至多 3 字节寻找 ASCII 或“其后续接数量已完整落在本段前”的合法首字节，找不到则以切点为界；因此 `C3|<80>80` 中 `C3 80` 交给前段，后者只解析第二个 `80`，单元总数与单线程相同。`F0 90|<80>` 也把完整声明前缀留给前段，非法续接字节留给下一段，事件起始偏移仍是整个输入的绝对偏移。被前一段吞掉的尾巴由该段继续持有；下一段从安全边界开始，两段既不重叠产出也不漏字节。UTF-16 切点先对齐到 code unit，若切点前偶数位置是高代理，则再向前退 2 字节，使该高代理连同后续 code unit 属于同一段。UTF-16 总回看不超过 2，UTF-8 不超过 3，检查次数为“字节一次主检查 + 有限对齐检查”。

## 上限断点

`Write` 先把上次缓存和新数据视为一个逻辑输入，但返回的 `n` 是本次新增输入中已可归属于已产出标量、BOM 或终态错误的字节数；仍等待后续的缓存字节不计为 `n`，调用方下次必须原样再次包含它们。产出前先检查完整标量的输出长度，空间不足则不写任何半个字符，保留已输出内容并返回输出超限；UTF-16 代理对作为一个不可分割的 4 字节单元。调用方可从返回的绝对消费偏移开新实例并续写，前缀 BOM 只在偏移 0 识别，因此拼接结果与一次转码一致。
