# Quoted-Printable 设计

## 推导

1. 解码器遇到软换行 `=\r\n` 时只删除这三个编码字符，因此软换行前的普通空格仍是解码出的普通空格。
2. 行末空白规则只针对解码后紧跟硬换行或输入结束的空白；软换行不是输入换行。故空格可直接放在软换行前，不能写 `=20`，否则解码会得到多余转义对应的同一空格，虽仍可往返，但不是最小转义。
3. 但编码器无法预知当前硬行末尾之后是否还会触发软换行。为满足硬行末尾空白必须转义，采用整逻辑行（两个硬换行之间）编码：预先只确定硬行末空白并转义，之后按 76 列放软换行。
4. 每个输入字节先编码成 token：1 个字符的安全字节，或 3 个字符的 `=XX`；空格、制表符在硬行末转义。
5. 软换行前先检查 `当前列+token长度>76`；超界则输出 `=\r\n`。因此 token 原子放置，`=XX` 永不会跨行。
6. 空白在硬行末转义后变为 3 字符 token；软换行可能自然落在它前面，但不会落在其内部。

## 往返等式

定义 `N(x)`：把 x 中每个独立 `\n` 和 `\r\n` 规范为 `\r\n`，单独 `\r` 保留。则：

`Decode(Encode(x)) == N(x)`

空串往返为空串；编码输出始终通过严格解码器。

## 最小转义

除 `=`、非安全字节、硬行末空格/制表符外，编码器均输出原字节；软换行只在行长约束需要时加入。因此除这三类法定转义和必要软换行外，没有多余转义。

严格合法的 y 可能含额外转义；`Encode(Decode(y))` 会将这些可选转义还原为可打印原字节，也可能改变合法但非规范的软换行位置；与 y 的非规范差异只来自这两类可选编码。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 转义与换行规范 | TODO |
| 2 76 字符行长 | TODO |
| 3 行末空白与原子 token | TODO |
| 4 严格错误与偏移 | TODO |
| 5 任意切分一致 | TODO |
| 6 往返 | TODO |
| 7 幂等/最小转义 | TODO |
| 计数器复杂度 | TODO |
