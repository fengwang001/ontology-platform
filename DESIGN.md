# CSV 解析器设计

包依赖单向：cell → lexer → table → writer(cell)、par(lexer,table)。

## 状态机
状态：S=字段开始，B=未引号字段中，Q=引号字段中，E=引号中刚见引号，C=CR 待定。字节类：`"` , `,` , `\r` , `\n` , 其他 o。

| 状态 | `"` | `,` | `\r` | `\n` | o |
|---|---|---|---|---|---|
| S | Q（记引号） | 发空字段→S | C | 空行：不发记录→S | B |
| B | ErrQuote | 发字段→S | C | 发字段发记录→S | B |
| Q | E | 累积 | 累积(\r原样) | 累积(\n原样) | 累积 |
| E | Q（累积1个"） | 发字段→S | C(闭合后) | 发字段发记录→S | ErrAfterQuote |
| C | ErrBareCR(指向前一\r) | ErrBareCR | ErrBareCR | 发字段发记录→S | ErrBareCR |

E 行的 `\r`：字段已闭合，走与 B 下 `\r` 相同的 C 逻辑。C 下再遇 `\r`：上一 `\r` 已确认为孤立错误（ErrBareCR），不可静默；实现中 C 不缓存 `\r` 而是在进入 C 前已把该字节计入字段长度（引号已闭合/裸字段），下一非`\n`字节在 Close 或 Feed 时立即在该前一 `\r` 偏移报 ErrBareCR。EOF：S 且无待发字段=正常结束（末尾换行不产空记录；文件无尾换行也合法）；Q/E=ErrUnclosedQuote；C=ErrBareCR；其余=发最后字段与记录。

## 推导1：空行与单列空值
空行（`\n\n`、起始 `\n`、CRLF 版）选择**跳过**，不作为记录。理由：RFC4180 末尾换行本就不产生记录，若空行算"一个未引号空字段的记录"，则无法区分"文件尾换行"与"末尾真有一条空值记录"。跳过空行后，单列表中真正的空值记录只能靠引号表达：`""\n`。回写器对单列表唯一字段为空（Quoted=true）写 `""`；多列空字段写空即可（逗号已表达其存在）。不加引号会丢数据：`\n`（或文件以无尾换行结束时的空前缀）会被解析为空行跳过。

规范输入定义：每条记录以 `\n` 结束；引号字段内可含 `,` `"` `\r` `\n`，`\r\n` 原样；空行跳过；无孤立 `\r`；单列空值必须写 `""`。回写器统一以 `\n` 结尾，故 `Write(Parse(x))==x` 只对规范输入成立；往返（表相等+引号标记相同）对一切解析成功的表成立（引号标记来自 cell.Quoted，含逗号/引号/\r/\n 或单列空值时才加引号）。

## 推导2：par 切点
段长 N/K（K=1 时整段）。每段（首段除外）在两种起始假设下各跑一遍：O=起点在引号外（合成一个未引号空字段承接，start=base），I=起点在引号内（start=base，合成引号字段）。段内字节照跑，不做 EOF 判定；错误带绝对偏移（base+段内偏移）与假设下的局部记录/字段坐标。
从前段真实末状态确定本段假设：前段 Q→用 I；其余→用 O。两种边界特例由拼接器看边界字节解决：(a) 前段 C：边界字节是 `\n` 则行尾成立（本段 O 结果首字段剔除——该字节已在前段消费，本段以 base+1 起算重跑 O），否则在前段 `\r` 偏移报 ErrBareCR；流末 C 在 Close 判定。(b) 前段 E 且边界字节是 `"`：`""` 转义跨段，改以 I+E（引号内刚见引号）起始假设重跑该段，仅在该情形发生；测试输入不构造每段都命中的情形，故常态计数 ≤2N+常数（每段 ≤2 遍，每遍消费每字节一次，加常数边界重跑）。
拼接：O 段第一个 cell（合成承接字段）与前段末记录的悬挂尾字段按偏移合并——内容取原文区间重切、引号标记以前段为准、偏移并集；随后所有 cell 与记录坐标加上前段累计的真实记录数/字段偏移；I 段首字段同样并入前段悬挂字段。错误的记录号=前段已发记录数+段内编号，字段号同理换算；字节偏移本就是绝对偏移。终止错误只在被选中假设的路径上生效。

## 推导3：CR 待定
C 不改变已消费事实：`\r` 字节已计入字段长度与偏移。半包时 C 可跨 Feed 挂起（无数据即无判定）；`\n` 到达→CRLF 行尾；其他字节→在挂起的 `\r` 偏移报 ErrBareCR（引号字段内 `\r` 留在 Q 累积，是内容，不进 C）。par 中切点在 `\r` 后：见推导2(a)，由边界字节裁决；切点在 `\r\n` 中间即此情形。流在 `\r` 结束：Close 报 ErrBareCR（引号内除外）。

## 推导4：上限的"立刻"
MaxFieldBytes 按字段内容字节计数：引号字段内每字节（含逗号、\r、\n）+1，`""` 两个输入字节只令内容 +1（在 E→Q 动作时计 1，而非 2）。每消费一个内容字节后立即比较，第一个超限字节到达即 ErrFieldTooLong（偏移指向该字节），不缓冲整条记录。MaxFields：每发一个字段立即计数比较。MaxRecords：每发一条记录后计数。终态后 Feed/Close 返回同一错误。

单一流式 Lexer 实例非并发安全；par 内部每段独立 Lexer，K 个 goroutine 无共享写。
