# 设计推导

## 末尾换行

- `Keep`：规范已识别换行，但不增删末尾空行；空输入仍为 `""`。
- `EnsureOne`：任何输出都以恰好一个 `\n` 结束，因此空输入也映射为 `"\n"`。
- `TrimOne`：删除末尾连续空行，再保证恰好一个 `\n`；空输入同样为 `"\n"`。

幂等点在“策略输出再规范化”：`EnsureOne("")="\n"` 再次仍为 `"\n"`；若空输入给 `""`，再次会变 `"\n"`，不幂等。`TrimOne` 先删后补，重复执行只剩一个换行。三种策略输出均无 `\r` 和行尾空白。

| 输入 | Keep | EnsureOne | TrimOne |
|---|---|---|---|
| `""` | `""` | `"\n"` | `"\n"` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 被删字节映射

区间采用左闭右开。删除区间不产生输出，且 `ToOut` 必须单调不减，所以其位置映射到“右侧最近保留字节”的输出位置；没有右侧保留字节时映射到输出终点。`\r\n` 的 `\r` 右侧是 `\n`，故映射到输出 `\n`；行尾空白右侧是换行，也映射到该换行。

因此光标位于被删空白中会落到行尾换行符，而不是换行后下一行。反向查询每个输出偏移只返回保留字节：`ToOut(ToOrig(o))=o`。删除点内部不能满足 `ToOrig(ToOut(i))=i`，这是压缩映射的必然信息损失。追加的末尾换行没有原文字节，取插入点终点作为来源哨兵；查询 `len(out)` 合法。

## 缓冲超限

选择拒绝。若强制把未判定空白当作非行尾输出，而下一字节恰好是 `\r` 或 `\n`，就会输出本应删除的行尾空白；再规范化时这些空白又被删，违反幂等；错误的映射也无法事后表示“这些字节曾存在”。

拒绝返回包含原文偏移的可判定错误，缓冲内容、输出和映射保持在该运行开始前的已确认状态。写缓冲本身超限时报下一字节偏移；随后 Close 或 Write 返回终态错误，避免重复副作用。

## 并行拼接

切点允许任意字节。每个 worker 先把切点向两侧移动到“完整行边界”：候选起点若看到前一字节是 `\r` 或空格/制表符，则退到该行起始；候选终点向前扩展到下一个已确认行尾之后。单调修正后各段拥有完整行，重叠只出现在极少数零长度回退；退化为单段处理。

段内先规范化，但只有最末段应用末尾策略；非末段视为“后有更多输入”，末尾内容全部保留。输出按实际采用区间拼接；段映射先裁剪到采用的原文区间，再把原文和输出坐标整体平移。修正上界来自待定状态：`\r\n` 只回看 1 字节，空白必须扩展到当前空白串起点和其后行尾；若一行超过缓冲上限，单段边界无法合法切分，使用包含该行的更大连续区间，仍不需要 worker 间共享可变状态。
