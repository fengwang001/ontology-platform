# 多租户公平队列调度器 — 设计推导

## 1. 为什么是 `vt += c / w` 而不是 `c * w`

目标：长期运行下，租户 i 分得的执行代价（服务量）占比收敛到 `w_i / Σw`。

把调度器看成流体系：设租户 i 累计获得服务 `S_i(t)`，定义其虚拟时间
`v_i = S_i / w_i`（已获服务按权重归一化）。每次选择 `v_i` 最小者服务，
等价于不断把所有活跃租户的 `v_i` 拉平；稳态下 `v_i ≈ v_j`，即
`S_i / w_i = S_j / w_j`，于是 `S_i : S_j = w_i : w_j`，份额正比于权重。

离散化：执行一个代价为 c 的任务，服务量 S_i 增加 c，故
`v_i += c / w_i`。若误用 `c * w_i`：权重大的租户 vt 增长更快、更晚被选中，
稳态变为 `S_i * w_i = S_j * w_j`，即 `S_i ∝ 1/w_i`——份额与权重成反比，
恰好颠倒。因此必须除以权重。

## 2. 空闲租户重新加入时的 vt 抬升

定义系统虚拟时间 `V(t) = min{ v_i : i 活跃（队列非空）}`。堆顶即 V(t)。

问题：租户 X 空闲期间不参与调度，其 `v_X` 停在旧值。期间租户 Y 执行了
n 个代价 c 的任务，`V` 已推进到约 `v_X + n*c/w_Y`。X 重新提交时若沿用旧
`v_X`，它将一直是 vt 最小者，连续独占直到追上 V：需要 k 步使
`k*c/w_X ≈ n*c/w_Y`，即 `k ≈ n * w_X / w_Y`。等权重时 k ≈ n = 1000 步，
这就是"空闲后独占"bug。

正确做法：租户队列由空转非空（或新租户首次入队）时，令
`v_X = max(v_X, V(t))`。抬升后 X 与当前活跃租户站在同一起跑线，最迟在
"活跃租户数"步内被服务一次，之后按权重公平竞争。空闲期间 X 没有排队
任务，本就不该累积"债权"，抬升丢弃的正是这笔不该存在的债权。

## 3. 代价为 0 的任务

代价 0 合法（如纯信号任务），但若 `vt += 0`，该租户 vt 不推进，将永远
是堆顶、无限独占。处理：记账时代价取下界 `eff = max(cost, minCost)`，
`minCost = 1`。于是每个任务至少推进 `1/w_i > 0`，零代价任务流也会每步
让出堆顶，独占不可能。业务统计仍按原始 cost 记账，只有 vt 用 eff。

## 4. 同 vt 并列

堆的比较键为 `(vt, tenantID)` 字典序：vt 小者优先，vt 相等按租户 ID
字典序。完全序保证同一输入流产生唯一调度序列，可复现。

## 5. 数值表示：float64

vt 与代价用 float64。53 位尾数：10 万任务、单任务代价 ≤ 1e6、权重
≥ 1e-6 时 vt 总量级 ≤ 1e17，远未溢出；相对误差 ~1e-16，对 5% 的
公平性断言无影响。未选有理数：big.Rat 每步代价高且 API 重，精度收益
在本场景可忽略。权重极大（1e6）与极小（1e-6）并存时 c/w ≤ 1e12，
同样安全。

## 6. 复杂度

选择 = 堆 pop + （租户仍非空则）push。pop 下沉每层 2 次比较、push 上浮
每层 1 次，单次选择比较次数 ≤ `3*ceil(log2 n)`，测试上界取
`4*ceil(log2 n)`。堆中只放非空租户，空闲租户不占选择代价。

## 7. 错误与边界

哨兵错误，`errors.Is` 可区分：
- `ErrInvalidWeight`：权重 ≤ 0 或非有限值；
- `ErrQueueFull`：租户队列达到上限，仅拒绝本次入队，不影响其他租户；
- `ErrTenantBusy`：租户仍有排队任务时拒绝移除（任务去向：不被丢弃，
  必须先取空或显式清空，本实现选择拒绝）；
- `ErrEmpty`：无可调度任务。

## 8. 并发与确定性

调度器内一把互斥锁串行化所有入队/出队。并发提交时"提交顺序"定义为
获取入队锁的串行化顺序；因此确定性仅对固定提交顺序的任务流保证：
同一确定性流跑任意多次，调度序列逐元素相同。出队操作（pop+vt 推进+
回堆）在锁内原子完成，故每个任务恰好被取出一次。
