# 设计说明

## 代理对的错误偏移

解码器不做“替换字符 + 回溯”恢复。UTF-16 高代理项本身不对应 Unicode 标量值；读到 `\uD800`–`\uDBFF` 时，该转义序列立即产生“必须紧跟低代理项转义”的约束。

因此错误偏移采用最早失败点：

- 孤立高代理项：指向高代理项转义的反斜杠。其后是结尾引号、普通字符（如 `A`）、非法转义、短十六进制、另一个高代理项或非低代理项，均不改变该位置。
- 未处于高代理项之后的低代理项：指向该低代理项转义的反斜杠；所以 `"\uDE00\uD83D"` 指向第一个 `\u`。
- 高代理项后遇到的单元本身也非法时，仍指向高代理项起点；因为从那里开始的代理对构造已经确定失败，且该位置便于调用者修正整个构造。

`"\uD83DA"` 不能解释为“U+FFFD 后接 A”：高代理项不是可解码字符，后面的 `A` 也不是低代理项转义；严格解码器没有证据可把同一输入重解释为两个独立词法单元。静默替换还会破坏错误位置的确定性与往返性质。

## 最小转义与唯一性

RFC 8259 要求转义引号、反斜杠和 U+0000–U+001F。U+007F 虽属于 ASCII 控制区的常见称呼，但不是上述 C0 控制字符，可作为合法 UTF-8 原样出现；U+001F 必须转义则是因为它落在明确受禁的 U+0000–U+001F。非 ASCII 码点（包括 U+2028/U+2029）若为合法 UTF-8，也一律原样输出。

编码映射是：

- `"` 与 `\` 分别唯一映射到 `\"`、`\\`；
- `\b \f \n \r \t` 唯一使用五个短形式；
- 其他 U+0000–U+001F 唯一使用小写 `\u00XX`；
- 其他合法 UTF-8 字节逐字节复制。

这些源字符集合互不相交，转义形式也互不相交；严格解码器又能把每个输出词法单元还原为唯一原字符，所以 `Encode` 是单射。算法在每个位置只有一种规定输出，故规范输出唯一。
