# DESIGN

## 1. 非法单元切分规则（从 7 个样例推导）
规则与 WHATWG 编码标准一致，样例是其必然结果：
1. 首字节非法（`80..BF`、`C0 C1 F5..FF`）→ 吞 1 字节。
2. 多字节首字节后必须紧跟规定长度-1 个连续字节；每收一个就立刻判定：
   - 第 2 字节不在首字节允许区间（见下表）→ 吞首字节 1 个，其余字节重新解析；
   - 第 3/4 字节不是 `80..BF` → 同样只吞首字节，其余重解析。
3. 流结束时前缀合法但未收满 → 残留字节整体算 1 个非法单元（故 `E2 82`+EOF=1 个，不是 2 个）。
验证：`F0 90 80 41` 中 `90` 在 F0 区间（`90..BF`）合法，`80` 是合法续字节，`41` 打断 →
吞 `F0 90 80` 三个为 1 单元，`41` 重解析为合法，得 `FFFD 41`。
`E0 80 80`：E0 要求第 2 字节 `A0..BF`，`80` 非法 → 吞 E0；`80`、`80` 各自为首字节非法 → 3 个。
`ED A0 80`：ED 要求 `80..9F`，`A0` 非法 → 吞 ED，两续字节各 1 → 3 个。
`C0 AF`/`F4 90 80 80` 同理逐个重解析。

| 首字节 | 第2字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1（其余重解析） |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | —（首字节即非法） | 1 |
| C0 C1 F5..FF | — | 1 |
后续位置（第 3/4 字节）合法区间一律 `80..BF`，非法时仍只吞首字节。

## 2. 缓存上限与即时放弃
UTF-8 最长 4 字节，缓存只需存「首字节 + 已确认合法的续字节」，硬上限 **3** 字节
（F0+2 个续字节，等第 4 个）。首字节 `C2..DF/E0..EF/F0..F4` 先入缓存；
第 2 字节到达即按上表判定：越界立刻吞掉首字节并把第 2 字节交回主循环（不等到 EOF）；
第 3、4 字节同理（非 `80..BF` 即吞首字节、当前字节重解析）。故绝不提前输出 FFFD，
也不会回扫。UTF-16 缓存上限 2 字节（半个 code unit）外加 1 个高代理标记（0 字节）。

## 3. par 切点对齐
切点 b 可能落在字符/非法单元中间。回看最多 3 字节到 `a=b-3`，从 a 解析；
规则：完整结束于 b 之前（start+len<=b）的单元归上段；跨过 b 的单元归下段，
上段在其 start 处停止输出。前看 3 字节保证下段 `[a,end]` 能解析所有跨越 b 的单元
（最大单元 4 字节，b-3 之后起始的单元必跨 b 或完全在 b 之后）。
例：`C3 | 80 80`（| 为切点）：上段在 C3 处停（C3 与首个 80 成的单元跨切点，归下段），
下段从 C3 开始产出 1 合法 + 1 FFFD，总数与单线程一致。切在非法单元中间时该单元
整体归下段，上段在其首字节处停，故非法单元不重复也不遗漏。各段窗口字节至多被
「本主区 + 一次邻接重叠」检查 2 次；每段额外常数（≤6）字节，K 段总量 ≤ N+8C。
BOM 只在第 0 段开头识别。

## 4. 输出上限断点
`Write` 以标量为原子：编码前若 `len(out)+encLen > limit`，立即返回 `ErrOutputLimit`，
当前标量及之后字节全部**未消费**（返回的 n 只含此前标量的字节）。缓存在状态机内的
半截前缀属于已消费输入（受字节守恒），调用方续传时先取 `Tail()`（含该半截前缀）
再拼上 `p[n:]` 喂入新实例；两实例输出拼接 == 不设限一次转完。
