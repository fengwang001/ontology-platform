# DESIGN

## 代理对错误偏移

- `\uXXXX` 是一个 6 字节编码单元；高代理只声明“半个码点”，其后必须紧跟以低代理开头的下一个完整单元。
- 高代理后是普通字符或短转义（如 `"\uD83Dx"`、`"\uD83D\""`）时，错误偏移指向高代理之后那个不合格单元的起点。
- 高代理后仍以 `\u` 开头但不是低代理时，错误偏移也指向该 `\u` 的起点；如 `"\uDE00\uD83D"` 在 offset 6 拒绝第二个单元。
- 若高代理后输入结束，没有可定位的后继单元，则偏移指向孤立高代理自身的起点。
- `"\uD83DA"` 不能变成“替换字符加 A”：在反斜杠转义模式中，`A` 不能终结本转义；高代理已承诺读取下一个编码单元，而 `A` 既不是新转义也不能并入旧转义，必须在该位置拒绝。静默替换也违反严格解码。

## 最小转义与唯一性

- JSON 字符串只需阻止字面量终止符 `"`、转义引导符 `\` 和必须转义的未转义控制字符 U+0000–U+001F。
- U+001F 是 RFC 强制转义的最后一个控制字符；U+007F 属于 DEL 而非该范围，合法且可原样输出。`/` 与所有非 ASCII（含 U+2028、U+2029）也可原样输出。
- 每个输入码点都有且只有一条编码分支：五个短控制符用固定短形式；其余控制符用固定四位小写 `\u00XX`；`"`、`\` 用各自固定转义；其他字节/码点原样复制。
- 分支集合互不重叠且没有条件选择，因此同一输入只有一个输出；输出中的转义前缀又能唯一恢复原码点，未转义字节保持不变，所以 Encode 是单射。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待补 | 待补 |
