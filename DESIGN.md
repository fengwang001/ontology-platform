# RLE 编解码器设计

## 第 2 条：严格解码拒绝清单的推导

不变量：凡被接受的 t，必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。
编码器只会产出：最长游程；次数 1 省略；次数 >= 2 时无前导零十进制；
数字与反斜杠一律以 `\` 转义。凡不可能是编码器产物的输入，必须拒绝：

- 显式次数 1（`1a`）：编码器对次数 1 省略次数，只会产出 `a`。
  `Encode(Decode("1a")) == "a" != "1a"`，故拒绝（ErrCountOne，偏移指向数字）。
- 次数 0（`0a`）：编码器永不产出 0 次游程；`0a` 解码为 `a`，
  再编码得 `a != 0a`，故拒绝（ErrCountZero）。
- 前导零（`01a`、`007a`）：编码器写次数无符号无前导零，只会产出 `1a`/`7a`，
  故拒绝（ErrLeadingZero）。
- 相邻游程同符号（`2a3a`）：编码器取最长游程，只会产出 `5a`，
  故拒绝（ErrAdjacentSame，偏移指向前一游程符号）。
- `\` 后既非数字也非反斜杠（`\a`）：编码器只转义数字与 `\`，
  永不产出 `\a`；若解码为 `a`，再编码得 `a != \a`，故拒绝（ErrBadEscape）。
- 末尾孤立 `\`：编码器写反斜杠必成对（`\\`），孤立 `\` 不可能是产物，
  故拒绝（ErrTrailingBackslash）。
- 末尾只有次数没有符号（`12`）：游程 = 次数 + 符号，缺一不可，
  故拒绝（ErrTrailingCount）。
- 非法 UTF-8：题面限定符号是 Unicode 码点，且往返仅对合法 UTF-8 成立；
  接受则解码结果不是合法字符串，往返无从谈起，故拒绝（ErrInvalidUTF8）。

所有错误为带字节偏移的具名类型 `DecodeError`，可用 errors.Is 区分；
偏移一律指向违规内容的首字节（截断的多字节字符指向其首字节）。

## 第 3 条：大次数的取舍

选择**支持**任意大的次数：次数用 `math/big.Int` 解析（`runs.ParseCount`），
不存在整数溢出，自然杜绝「溢出后静默得到错误结果」。
但解码输出字节数受 Decoder 的可配置上限 `Limit` 约束（默认 64 MiB）：
每发射一个游程前先检查 `已写 + 次数*符号宽度 > Limit`，超限返回
ErrOutputLimit（偏移指向该游程符号）。因此内存占用以 Limit 为上界，
与次数本身无关，不会按次数比例分配内存。

## 测试对照表（最后补齐）

（待补）
