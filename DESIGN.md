# LZ77 自定义流格式设计（包 ontology）

## 1. 记录字节布局（全部为无符号 LEB128 变长整数）

| 记录 | 布局 |
| --- | --- |
| 流头 | 魔数 `0x4C 0x5A`（LZ）、版本=1（varint）、窗口容量 W（varint） |
| 字面量段 | 标记=0（varint）、长度 n（varint）、随后 n 个原始字节 |
| 回指 | 标记=1、距离 d（≥1，varint）、长度 l（≥3，varint） |
| 刷新标记 | 标记=2（单独一个 varint 字节） |
| 流尾 | 标记=3、原文总长 L（varint）、64 位 FNV-1a 校验和（varint） |

标记都是单字节 varint；刷新与流尾不携带数据。流尾后多字节即错误。

## 2. 第三节四处推导

### 2.1 切法无关（语义 3）
在位置 p 找到的最长匹配长度 ℓ 可能随未来字节继续增长，只有确认不可能再增长时才能输出。压缩器把 Write 仅作为追加：维护逻辑缓冲 b 与贪心游标 pos，Find 只在“可确定”前缀 `b[:len(b)-1]` 上工作——匹配触到当前缓冲末端（p+ℓ==len(b)）则悬置，等下次 Write；未被悬置覆盖的确定前缀立即编码输出。任何切法在同一累计偏移处面对的已知字节集合相同，故输出的（字面量/回指）序列逐字节相同。Flush 时不再有未来字节，触端匹配也不可能增长，强制把全部悬置前缀冲出——所以 Flush 是匹配的自然终点，且不跨越 Flush 保留悬置。

### 2.2 重叠回指复制（语义 2）
d<l 时目标尚未完全产生：固定源区间的块复制会把旧字节重复复制（d=1 时本应是 run-length 展开）。正确语义是逐字节 `out=append(out, out[len(out)-d])`，每轮源都可能是上一轮新产出的字节。该循环均摊 O(l)、无额外分配（append 不缩容），与解压整体 O(n) 同阶，故直接采用逐字节循环。

### 2.3 两种距离越界（语义 7）
“距离>已输出”引用尚未产生的字节（时间越界，任何配置下都不可能来自本压缩器），先检查；“距离>窗口容量”引用早已存在但超出流头声明保留范围的字节（容量越界），两者必须可区分。压缩器只保留最近 W 字节，Find 只返回 d≤W 且 d≤已编码字节数的候选，故绝不产生后者。并行块以“上一块末尾 W 字节”为预置字典：解压时这些字节恰好是解码器已输出内容（前一块先入流），“d≤已输出”天然成立，含义不变，无需特殊通道。

### 2.4 并行确定性（语义 9）
块 k 的压缩输入只有：本块原文 + 上一块末尾 ≤W 字节（预置进 window/matcher 哈希链，但不计入本块输出）。不读任何块的压缩结果、不依赖完成顺序；固定切分与确定性算法 ⇒ workers=1/2/4/8 输出逐字节相同。衔接：每块（除最后一块）后写刷新标记，最后由统一流尾收尾；刷新只标记边界、无输出动作，回指可跨越它（窗口不清空）。

## 3. 错误模型

`CorruptError{Offset, Err}` 包装哨兵：ErrHeader/ErrZeroDistance/ErrDistanceTooFar/ErrWindow/ErrVarint/ErrLength/ErrChecksum/ErrTrailing；不完整输入在 Close 时为 ErrTruncated。偏移为压缩流字节偏移。超限为 ErrOutputLimit，实例进入终态并粘滞返回。

## 4. 复杂度

4 字节键直接索引哈希头表 + 每位置前驱环形数组；Find 沿链最多走 chainLimit 个候选，链上位置超出 W 即止，Insert O(1)。最坏每字节 O(chainLimit)；全同字节串每字节考察数趋于 chainLimit，与规模无关。
