# 设计：基于版本向量的多副本冲突检测器

## 1. 数据模型与约定
- 每个副本有不可为空的副本 ID；向量是 `replicaID -> uint64` 的稀疏映射，缺失分量按 0 解释。
- 本地写入 `(key, value)` 时递增本副本分量，形成该条目的新版本向量。
- 所有状态仅保存在进程内存；实现只使用 Go 标准库。
- 未知副本 ID：构造注册表时必须登记；比较按缺失 0 处理，同步/编解码校验时报 `ErrUnknownReplica`。
- 兄弟版本是同一键下互不可比的条目；值仅作去重和确定性排序。

## 2. 四种偏序关系的推导
取两个向量分量并集。对每个 ID，缺失补 0。设 `less=存在 a[id]<b[id]`，`greater=存在 a[id]>b[id]`：
- `!less && !greater`：所有分量相等，关系为 `Equal`。
- `less && !greater`：每个分量不大于且至少一个更小，`a` 因果在 `b` 前，为 `Less`。
- `!less && greater`：每个分量不小于且至少一个更大，`a` 因果在 `b` 后，为 `Greater`。
- `less && greater`：一边有更小、另一边有更大，二者互不知晓，为 `Concurrent`。

因此并发不能定义成“不相等”；例如 `{A:1,B:0}` 与 `{A:0,B:1}` 同时存在两个方向。`{A:1}` 与 `{A:1,B:0}` 因 B 缺失补 0，完全相等。比较只遍历并集，最大读取/比较次数为 `2*|并集|`。

## 3. 存储、冲突与同步
- 每个副本为每个键保存一组尚不能互相支配的兄弟条目。
- 合并新条目 `x`：若现有条目严格大于 `x`，忽略；若现有条目严格小于 `x`，删除旧条目；否则 `x` 成为并发兄弟。
- 同步 X 到 Y：先校验源向量中的 ID；计数器小于已见源水位时报 `ErrCounterRollback`。条目逐个按上述规则并入。
- 双向交换律通过两个方向依次合并实现。重复同步是幂等的，因为已支配版本会被忽略，相等版本值相同则不重复添加。
- 两副本分别写同一键后双向同步，双方都保留两个并发兄弟；输出按值字典序规范化，保证逐元素一致。

## 4. 兄弟上限与确定性
每个存储有 `maxSiblings`。合并后超限时，按 `(向量, 值)` 的确定性顺序排序：向量比较优先，`Equal` 再比较值；保留最大的指定数量，丢弃最旧/最弱前缀。上限为 1 时即最后写入者胜出：有因果关系则因果较新者胜；纯并发时按该总顺序选择确定赢家。丢弃次数累计在存储中。

## 5. 收敛性
每条同步消息带源副本的当前向量水位。三个副本之间每一对至少双向同步一次后，每个本地写入都会传播到所有副本。冲突合并是交换律、结合律、幂等的集合归约；兄弟再按同一确定性顺序裁剪。因此任意满足覆盖条件的同步顺序产生逐字节相同的键、值、向量、丢弃计数和整体哈希。

## 6. 裁剪退役副本的安全性
退役 ID 集合 R 后，从仍活跃条目向量中删除 R。安全条件：对所有仍存活条目向量的每一对，裁剪前关系必须等于仅按活跃 ID 比较的关系。
删除分量只会消除“更小”或“更大”的证据：若某对原本是严格偏序，而严格性的唯一证据来自退役分量，裁剪后可能变成相等或并发；原本并发也可能因一个方向证据消失而变成偏序。因此任一对关系变化都拒绝裁剪并返回 `ErrUnsafePrune`。相等关系在被删分量均为 0 时仍安全。

## 7. 故障与边界
- 计数器回退：同步源某 ID 低于已观察水位，返回 `ErrCounterRollback`。
- 溢出：`math.MaxUint64` 再递增返回 `ErrOverflow`，不回绕。
- 序列化：二进制包含固定头、ID/计数分量、条目值和 CRC；未知 ID 返回 `ErrUnknownReplica`。
- 截断分类：长度小于固定头为 `ErrTruncatedHeader`；头声明的任一分量/值不足为 `ErrTruncatedComponent`；结构完整但 CRC 不符为 `ErrCRCMismatch`。四类错误均可用 `errors.Is` 区分。
- 零副本不能注册向量；单副本的任意两个版本只可能相等或严格可比；全零向量相等；同一副本连续写入严格递增。
