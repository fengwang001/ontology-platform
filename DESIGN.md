# DESIGN

## 1. 非法单元切分规则（WHATWG 最大子部分，自己写出）

从首字节确定期望长度 n；逐个读后续字节，只有续字节 80..BF 才被纳入。遇到第一个非续字节立刻终止本单元：该字节不被吞掉，退回作为下一单元首字节。在流结束前形成的非最短/越界/代理区序列整体为一个非法单元（吞 n 个字节）；合法前缀在流结束仍不足 n 个字节时，残留整体为一个非法单元（只 1 个 FFFD）。

对照样例：`F0 90 80 41` 首字节 F0 要 4 字节，第三字节 41 非续字节→单元为 `F0 90 80`，吞 3 字节、1 个 FFFD，41 另起。`E0 80 80`：E0 要求第二字节 A0..BF，80 越界→首单元仅 E0；随后 80 是孤立续字节吞 1；再一个 80 吞 1→3 个。`ED A0 80`：ED 第二字节须 80..9F，A0（代理区）越界→ED 单独成单元，A0/80 各为孤立续字节。`C0 AF`：C0 长度 2 但 C0/C1 永远非最短→C0 一个，AF 一个。`F4 90 80 80`：F4 第二字节须 80..8F，90（>10FFFF）越界→F4 一个，其后三个续字节各一个。`E2 82`+EOF：E2 要 3 字节，残留 2 字节是合法前缀被截断→1 个。`80 80` 两个孤立续字节→2 个。

第二字节区间表（第二字节合法则继续读满；非法时只吞当前已纳入字节数）：

| 首字节 | 长度 | 第二字节区间 | 非法时吞 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 1（仅首字节） |
| E0 | 3 | A0..BF | 1 |
| E1..EC | 3 | 80..BF | 1 |
| ED | 3 | 80..9F | 1 |
| EE..EF | 3 | 80..BF | 1 |
| F0 | 4 | 90..BF | 1 |
| F1..F3 | 4 | 80..BF | 1 |
| F4 | 4 | 80..8F | 1 |
| 80..BF | 1 | —（孤立续字节） | 1 |
| C0 C1 F5..FF | 1 | —（永不合法） | 1 |

长度不足时（EOF/截断）吞掉全部已纳入字节（1..3），只 1 个 FFFD。

## 2. 切分缓存上限与立即放弃等待

最长字符 4 字节，首字节已定性，故最多再缓存 3 个字节；缓存字节数 ≤3。看到第一个不满足区间的后续字节即可立即判非法并把它退回（如 E0 后跟 80，立刻把 E0 定为非法单元，不等 EOF）；长度已满但码点非最短/代理/越界同样立即判。只有「字节合法、前缀可延长但还差字节」才继续等。

## 3. par 切点对齐

对 K 段，第 i>0 段起点为 floor(i·n/K)。回看：UTF-8 从起点最多退 3 字节，找到一个 lead 字节，若该 lead 被其应有的续字节完整覆盖（从 lead 到起点的字节数达到期望长度），则字符已完整属于上段，起点不动；否则把起点退到该 lead，让本段接管此未完成单元。例：切点在 C3 后、下段以 80 80 起：C3 期望 2 字节且后面恰好 1 个续字节→完整归上段，第二、第三个 80 是两个孤立续字节归下段，单元总数与整体一致。UTF-16LE/BE 最多退 1 字节对齐 code unit；若对齐后高代理落在段尾（奇 code unit），再把它划给下段（退 2 字节），保证代理对不被切断。各段独立流式转码，顺序拼接；内部段以「不补截断 FFFD」收尾（stream 暴露 InteriorFinish），末段正常 Close。中段不含跨段尾巴，统计与整体天然一致。

## 4. 输出上限断点（语义 9）

Write 内部逐单元提交；提交前检查剩余容量，不足则进入终态 ErrLimit（不再提交）。返回的已消费 n = 已提交单元覆盖的输入字节（含已处理 BOM 字节）；尚未提交的尾巴最多 3 字节（UTF-8）或 3 字节（UTF-16：1 对齐字节+代理对）仍在缓存，调用方可经 Unread() 取回，连同 p[n:] 一起喂给新实例（Unread 即该尾巴，不重复喂）。BOM 不产生输出，其预算为 0，不触发超限。UTF-16 代理对两 code unit 一起检查、一起提交，不会输出半个代理对。

## 5. 检查次数计数

每个输入字节被首检计 1 次；一个单元遇非法后续字节时，该后续字节「退回」会被再检查 1 次（最多一次，它作为新单元首字节后不再退回）。故检查次数 ≤ 2×输入字节数，且与切分粒度无关（缓存字节不重扫）。par 每段独立检查一次，对齐回看每段 ≤3 字节（UTF-16 ≤3），总计 ≤ n + 8·C。
