# 差分同步校验与增量传输器 — 设计推导

## 1. 弱/强校验和的分工（缺一不可的推导）

- 弱校验和 `Weak`：32 位纯加法和 `W(i) = sum(src[i..i+bs]) mod 2^32`。
  作用：在源端每个偏移上 O(1) 滚动筛选「可能相同」的窗口。
- 强校验和 `Strong`：SHA-256。作用：确认弱命中是否真为内容相同。
- 只用弱校验和：加法和存在碰撞（交换块内两字节和不变），会把内容不同
  的块误判为可复用，补丁应用后结果与源端不一致且不报错（错误复用）。
- 只用强校验和：每个偏移都要算一次 SHA-256，代价为 O(n*bs) 且无法滚动，
  失去滚动筛选的意义；弱校验和的价值就是把强校验次数压到弱命中次数以内。
- 结论：弱负责快速筛选，强负责确认；强校验只在弱命中时计算。

## 2. 滚动推进公式

- 从偏移 i 推进到 i+1：`W(i+1) = W(i) - src[i] + src[i+bs]`（mod 2^32）。
- 正确性：窗口恰好移出 src[i]、移入 src[i+bs]，其余项不变。
- 测试保证：对随机数据的每个偏移，滚动结果与从头重算完全相等。

## 3. 复杂度与计数器

- 扫描代价：初始窗口 bs 次加法 + 每次推进 2 次基本运算 + 每次命中后
  重开窗口 bs 次，命中次数不超过 n/bs，总计不超过 3n，小于 4n。
- `chunk` 内置非导出原子计数器：弱校验基本运算次数、强校验计算次数；
  测试断言弱运算不超过 4 倍数据长度、强校验次数不超过弱命中次数。

## 4. 签名格式（sig，目标端生成）

    magic "OSS1" | blockSize u32 | dataLen u64 | count u32 |
    count x { weak u32 | strong [32]byte }

只覆盖完整块（末块不满不进签名），块大小为 0 返回可判定错误。

## 5. 补丁格式（diff 生成，patch 消费）

    头部 52B: magic "OSP1" | blockSize u32 | srcLen u64 | srcStrong [32]B | opCount u32
    指令序列: Reuse = 0x00 index u32 | Data = 0x01 len u32 + len 字节字面量
    尾部: CRC32(以上全部字节) u32

- 扫描源数据：每个偏移先查弱校验和表，命中再算强校验和确认；
  确认则发 Reuse 并跳过整块，否则发 1 字节字面量并滚动推进一格；
  连续字面量合并为一条 Data。同输入同输出（确定性），无 map 迭代序影响。

## 6. 截断补丁不得部分应用的实现方式

- `verify.Parse` 先把整份补丁完整解码并分类错误：
  头部不足 → ErrHeaderTruncated；指令头/参数不足 → ErrOpTruncated；
  Data 负载不足 → ErrDataTruncated；尾部 CRC 缺失或不符 → ErrCRCMismatch。
- `patch.Apply` 顺序：完整 Parse → CRC 校验 → Reuse 块号越界检查
  （ErrBlockRange）→ 在新切片上构建结果 → 结果长度与整体强校验和
  比对（不符 → ErrStateMismatch）→ 全部通过才返回。
- 目标数据从不被原地修改；任何一步失败都直接返回错误，目标端保持
  应用前逐字节相同。四类截断错误均可用 errors.Is 区分。

## 7. 故障语义

- 签名生成后目标数据被改动：复用块取到错误字节，最终整体强校验和
  与头部 srcStrong 不符 → ErrStateMismatch，不产出错误数据。
- 块大小为 0：chunk.ErrBlockSize，可判定错误。
