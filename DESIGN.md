# DESIGN

## 1. 空输入与末尾策略

三策略：Preserve（保留原样）、EnsureOne（非空则末尾恰一个 `\n`）、TrimAll（删掉全部末尾空行；无换行的最后一行不补）。
推导：N 的结果绝不含 `\r`、不含行尾空白；再规范化时不存在空白/`\r` 可删，只需末尾策略是幂等算子。Preserve 不动末尾、EnsureOne 强制恰一个、TrimAll 强制零个尾部空行，三者对“已规范”文本都是幂等的。
空输入是特例：EnsureOne 若输出 `\n`，则 N("")≠""，但 N("\n")=`\n`，仍幂等；我选 **N("")=""**（不凭空合成字节，映射无需伪造区间），幂等显然。空白行被删除后不产生 `\n`：EnsureOne 对非空原文仍补一个，故 "  \r\n"→"\n" 且 N("\n")="\n"。

| 输入 | Preserve | EnsureOne | TrimAll |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

`ToOrig` 只输出“存活”位置；`ToOut` 对全部原文偏移有定义，且要求 `ToOut(ToOrig(o))==o`、两者单调不减。
删除区间 `[a,b)` 映射到其**紧邻的下一个存活输出位置** d（被删区间内及左端点都取 d），除非它位于 EOF（取输出长度）。
`\r\n`：`\n` 存活在 o，则 `\r` 的 ToOut=o（不是 o+1）——满足 ToOrig(o) 是 `\n`、ToOut(ToOrig(o))=o。
行尾空白 `abc   \n`：空白取 `\n` 的输出位置（换行**之前**）。编辑器光标落在被删空白里应停在换行前（逻辑行尾）；落在 `\r` 上停在 `\n` 上。

## 3. 缓冲超限

方案：拒绝 vs 强制判为“非行尾”。强制输出会吐出空白，若下一字节恰好是换行，这些空白就永久出现在输出中，违反语义 2 与幂等；拒绝则不产生字节、可判定、不破坏任何不变量，但拒绝后若继续 Write 可能让“后续换行”改变含义，故**拒绝即进入终态**（同 NUL/输出超限/Close 后写），保留已输出内容，错误带当前待判串起点的原文偏移。判定与切分无关：计数器按累计字节判断，在任何喂入方式下同偏移触发。

## 4. par 拼接

每段独立用 Preserve 的“开放关闭”规范化：Close 不施加末尾策略，并暴露 pendingCR 与尾部待判空白。
顺序合并时维护两个 carry：待定 `\r` 与一段已输出的尾部空白（以输入坐标记录）。
- 待定 `\r`：看下一段首字节，`\n` 则配对删除（1 原字节→0），否则补一个输出 `\n`（合成）。修正只需 1 字节前瞻，有上界。
- 尾部空白：取下一非空段首行。首行是纯空白行（`[ \t\r\n]*` 截断于首个内部 `\n`）则该空白整串删除；否则原样保留。被删空白串可能任意长，但只是一次后缀裁切 + 区间平移，O(长度) 一次完成。
映射平移：段内 `span` 段坐标加段起点偏移即全局原文坐标；输出坐标加“此前已确认的输出长度”。被裁掉的尾部区间在其结局确定后再提交，因此无需回改已提交区间。最后一段再施加全局末尾策略。修正范围在段数上有上界，空白裁切总长 O(n)。
