# DESIGN: 基于版本向量的多副本冲突检测器

状态仅存进程内存，只用标准库。副本集合在构造 Store 时登记；向量是 `map[string]uint64`。

## 1. 四种偏序关系与判定算法

设并集副本集 U = keys(X) ∪ keys(Y)，缺失分量按 0 处理。定义

- `less` = 存在 r∈U 使 X[r] < Y[r]
- `greater` = 存在 r∈U 使 X[r] > Y[r]

只需一次并集遍历即可同时算出两个布尔量，判定表：

| less | greater | 关系 Rel | 含义 |
|---|---|---|---|
| false | false | Eq | 相等（逐分量，缺失即 0） |
| true  | false | Lt | X 严格小于 Y，因果在前 |
| false | true  | Gt | X 严格大于 Y，因果在后 |
| true  | true  | Cc | 并发，互不可比 |

要点：并发**不是“不相等”**。`{A:1,B:0}` vs `{A:0,B:1}` 两个方向的分量都存在，less 与 greater 同时为真 → Cc；
而 `{A:1,B:0}` vs `{A:2,B:0}` 只有 less → Lt。
只检查“所有分量 ≤”只能得到 Lt/Eq/Cc 三类的混合，必须额外检查“存在更大分量”才能把 Gt 与 Cc 分开。
复杂度 O(|U|)，每个并集分量恰好做两次比较（对 X、对 Y 的取值），比较次数 ≤ 2|U|，实现中用计数器断言。

## 2. 缺失分量语义

缺失分量恒等于 0：`{A:1}` 与 `{A:1,B:0}` 在 U={A,B} 上逐分量相等 → Eq。
因此向量内部不存储 0 分量（Marshal 时跳过 0），相等性只取决于并集上的数值。
例外策略：**未知副本 ID**（向量里出现 Store 未登记的 id）是故障而非缺失分量——比较本身仍按并集+0 语义工作（纯函数），
但 Store/Sync 入口用登记集校验，命中即返回 `ErrUnknownReplica`，文档与测试固定此选择。

## 3. 同步合并规则（逐键）

对同一键，用版本向量关系合并入方条目 S 与本地条目 L：

- Gt（S>L）：覆盖；Lt：忽略；Eq：无操作；Cc：保留两个兄弟版本（按 VV 规范化键去重）。

兄弟集合上限 N（构造给定）：超限按确定性规则丢弃“最旧”——保留 (VV规范化串, 值) 字典序最大的前 N 个，
丢弃计入 evict 计数。N=1 时退化为最后写入者胜：Cc 也只留字典序最大者，结果与同步方向无关，保证收敛。
本地写：副本 r 写键 k 时取该键当前兄弟 VV 的逐分量 max，再把 r 分量 +1；计数已为 math.MaxUint64 时返回 `ErrOverflow`，不回绕。

## 4. 收敛性

合并函数对兄弟集合是“集合并 + 统一排序截断”，交换、结合、幂等。
故任意图上只要每对副本至少同步一次（双向各一次），所有副本持有的 (键→兄弟集合) 相同；
同步顺序、重复同步、同步方向均不影响最终状态。测试枚举 24 种边顺序并对规范化 JSON 取 SHA-256 断言全等。

## 5. 裁剪安全性（prune）

退役副本集合 R：先把向量中 R 的分量删去，再比较裁剪前后所有副本“所有键的所有兄弟 VV”两两关系。
判定条件：若存在一对向量，其关系在裁剪前后发生变化（含 Eq↔其他、Lt/Gt/Cc 互变），则整次裁剪拒绝并返回 `ErrUnsafePrune`；
关系全部保持才提交。零向量删掉 R 后仍为零向量，恒安全。直觉：被删分量若是区分因果的唯一证据（如 {A:1} 与 {B:1} 在只留 A 时变成 Lt/0），裁剪就会改写历史，必须拒绝。

## 6. 计数器回退

每个副本保存对每个对端“最近一次见到的该对端自身时钟分量”水印。同步前检查对端向量中 peer 自身分量：
小于水印 → `ErrRollback`（时钟回拨/状态回滚），拒绝本次同步；首次见到只记录。其余副本分量可正常增减，不触发。

## 7. 序列化与截断分类

VV 二进制格式：`"VV1"` magic(3) + n(uint32 BE) + n×[idLen(uint16 BE)、id UTF-8、cnt(uint64 BE)] + CRC32(IEEE, 覆盖此前全部字节, uint32 BE)。
条目格式同理：`"ENT1"` + keyLen(uint16)、key + VV 段 + valLen(uint32)、value + CRC32。
对编码结果从长度 0 到 len-1 逐字节截断，循环分类为三类可判定错误：
长度 < 头部固定长（7 / 9 等）→ `ErrHeaderShort`；头部声明未读完 → `ErrComponentShort`；结构读完但 CRC 区缺失/不符 → `ErrCRCMismatch`。
四类错误（再加 `ErrRollback`、`ErrUnknownReplica`、`ErrOverflow`、`ErrUnsafePrune`）均为哨兵错误，用 `errors.Is` 区分。

## 8. 边界

零副本：无登记副本时不能写入（`ErrUnknownReplica`），空操作合法。单副本：任意两版本来自同一计数器链，总可比（Eq/Lt/Gt），无 Cc。
全零向量：缺失即 0，相互 Eq。同一副本连续写：自身分量严格递增，关系严格 Lt。N=1：见第 3 节 LWW 规则。
