# 设计：带转义的 RLE 编解码器

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 拒绝清单的逐条推导（若接受，哪个 t 违反不变量）

- 显式次数 1：`1a` 解码为 `a`，重编码为 `a` ≠ `1a`（次数 1 必须省略）。
- 次数 0：`0a` 无论解为空串还是 `a`，重编码分别是空串、`a`，均 ≠ `0a`。
- 前导零：`01a` 解码为 `a`，重编码 `a` ≠ `01a`；规范次数无前导零。
- 相邻同符号：`2a3a` 解码为 `aaaaa`，重编码为 `5a` ≠ `2a3a`；故 Encode 必须产
  最长游程，Decode 必须拒绝任何非最长拆分（含 `aa`、`2aa` 等形式）。
- `\` 后非数字非 `\`：`\q` 中 `q` 本无需转义，任何合法串的重编码都不会出现 `\q`。
- 末尾孤立 `\`：`a\` 的转义未完成，合法串重编码绝不会以 `\` 结尾。
- 末尾只有次数：`a2` 的次数无符号可附着，重编码不会以裸次数结尾。
- 非法 UTF-8：Decode 产出必须是合法 UTF-8 串，而合法串的重编码必为合法
  UTF-8，故含非法字节的 t 不可能等于任何重编码结果。
- 次数溢出 uint64：`99999999999999999999a` 的次数无法精确表示，任何截断都使
  重编码 ≠ t，故拒绝而非静默算错。

每类错误是可判定哨兵错误（`errors.Is`），包在 `*rle.Error` 中带字节偏移：
偏移指出错构造的首字节；坏转义指 `\` 之后那一字节；输出超限指超限游程的首字节。

## 大次数的取舍

次数以 uint64 解析，逐位累加、溢出即拒（`ErrCountOverflow`，不静默回绕）。
解码输出字节上限可配置（`NewDecoder(w, maxOutput)`；`Decode` 用
`DefaultMaxOutput` = 1 GiB）。发射游程前先检查 `count*len(sym)` 是否超剩余
额度，超限返回 `ErrOutputLimit`，因此内存占用从不与次数成正比；上限之内的
大次数（如 `1000000a`）正常解码。

## 语义 ↔ 测试对照

（待测试写完后补齐）
