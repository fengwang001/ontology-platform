# 带转义的 RLE 编解码器设计

不变量：严格解码器接受的每个 t 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 拒绝清单的逐条推导

每条给出：若接受它，哪个 t 违反不变量。

1. 显式次数 1：`Decode("1a")="a"`，而 `Encode("a")="a" != "1a"`。次数 1 的规范形式是省略。
2. 次数 0：`Decode("0a")=""`（零次重复），`Encode("")="" != "0a"`。编码器永不写 0。
3. 前导零：`Decode("01a")="a"`，`Encode("a")="a" != "01a"`。规范十进制无前导零。
4. 相邻同符号游程：`Decode("2a3a")="aaaaa"`，`Encode("aaaaa")="5a" != "2a3a"`。
   Encode 必须合成最长游程，否则本条的拒绝失去依据，唯一性不成立。
5. `\` 后非数字非反斜杠：若把 `"\a"` 当 `"a"` 接受，`Encode("a")="a" != "\a"`。
   编码器只在数字或 `\` 前写 `\`，故 `\` 后只能是这两类。
6. 末尾孤立 `\`：`\` 是转义引导符，任何 `Encode(s)` 中 `\` 后必跟数字或 `\`，
   不存在 s 使 `Encode(s)` 以孤立 `\` 结尾，故 `"a\"` 之类必拒。
7. 末尾只有次数：Encode 输出的每个游程以符号结尾，不存在 s 使 `Encode(s)`
   以数字结尾，故 `"a3"` 必拒（`"0"`/`"1"`/`"01"` 同时命中 1-3 条，优先报次数错误）。
8. 非法 UTF-8：Decode 须返回原字节，但 Encode 按码点重编码，非法字节会变成
   U+FFFD（EF BF BD），`Encode(Decode(t)) != t`。合法编码的 U+FFFD 本身不受限。

每一类对应一个独立哨兵错误，包在 `*rle.DecodeError` 中并带字节偏移 `Off`。

## 大次数的取舍（第 3 条）

选择**支持**任意大次数：格式本身未给次数设界，拒绝合法规范编码会破坏唯一性承诺。

- 次数用 `math/big.Int` 解析（`runs.ParseCount`），不可能整数溢出。
- 不按次数预分配内存；只在确认额度足够后才逐符文追加输出。
- 解码输出总字节上限可配置：`NewDecoder(limit)`；`Decode` 用 `DefaultMaxOutput`。
- 次数 x 符号宽 超出剩余额度时返回 `ErrTooLarge`（可 `errors.Is` 判定），偏移指向游程起点。

## 测试对照表

（待补）
