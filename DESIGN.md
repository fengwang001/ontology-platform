# 设计推导

## 1. 零计数时的 hunk 行号
unified diff 用 1 基、半开区间：`-a,b` 表示旧文件第 a..a+b-1 行（共 b 行）。
当 b=0（纯插入）时区间为空，a 表示"插入点之前最后一行的旧行号"，即插入发生在
旧行 a 与 a+1 之间；文件开头没有前一行，取 a=0；文件末尾追加取 a=旧文件总行数。
样例：文件首插入无前行→`-0,0`；在旧第 2、3 行之间插入→`-2,0`；删尽时新侧同理
写 `+0,0`，Apply 解析这种头时在 0 基切片下标 a 处插入。

| 旧侧 | 书写 | Apply 插入点（0 基） |
|---|---|---|
| b=0 | `-a,0`，a=前一行行号（无前行为0） | 删 0 行，在切片下标 a 处插入 |
| b=1 | `-a`（省略 `,1`） | 从下标 a-1 起替换 1 行 |
| b>=2 | `-a,b` | 从下标 a-1 起替换 b 行 |

## 2. hunk 合并阈值
hunk 从首处改动向上扩 C 行上下文、末处改动向下扩 C 行。两 hunk 的上下文窗口
相邻或重叠（间隔 g <= 2C）时 GNU diff 合并；g=2C 时中间恰好放下 C+C 行，
两窗口相接，合并；g=2C+1 时中间剩 1 行不属于任一窗口，分开。故阈值为 **2C**。
C=1 时 g=2 合并、g=3 分开（如 `A B C D` 中改 A 与 D）。若取阈值 C 则 g=2 会
被错误拆开；取 2C+1 则 g=3 会被错误合并，均与 `diff -U C` 不一致。

## 3. Myers 并列最短路径的选择
正向 Myers 每条 furthest-reaching 路径按 k 扫描。对 `a b -> b a`（D=2）：
先下移（删除）到达旧 a、新空；再斜向匹配 b；再上移（插入）匹配 a，得到
确定性排列 `-a`、` b`、`+a`。本实现固定**同一 D 层先尝试下移（删除优先）**
（k 从大到小扫描时 down 分支优先），因此并列脚本一律"删除行排在前、插入行
排在后"；相同输入产生逐字节相同补丁。

## 4. 偏移传播
hunk i 以偏移 delta_i（应用位置减记录位置，按旧文件行计）成功后，hunk i+1
的查找起点平移量 = 此前所有已应用 hunk 的 delta 之和，再叠加各 hunk 净增减
（newLen-oldLen）造成的自然位移。理由：后续 hunk 的记录行号相对原始文件，
而目标文本已被前序 hunk 整体改动；平移后"记录位置"才与当前文本对齐，F 行
模糊窗口围绕平移后的位置上下搜索。

## 5. 计数口径
计数器统计对角线蛇行的逐行比较次数（含失败比较）。Myers 每 d 层每条 k 只沿
一条蛇前进，总量 O((N+M)D)；带距离上限 Dmax 时在 d>Dmax 立即返回
ErrTooDifferent，计数器同样有界。
