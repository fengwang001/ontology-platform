# 随机超平面 LSH 分桶检索器设计

## 1. 签名计算规则

固定维度 d，第 l 张表用独立种子 seed_l 生成 b 个法向量 h_{l,j}∈R^d，
每个分量由标准正态分布采样（Box-Muller），RNG 为带显式种子的
splitmix64，无全局状态。向量 x 的第 l 个签名为 b 位整数：

  bit j = 1 当且仅当 <x, h_{l,j}> > 0；
  内积恰好为 0（含 x 为零向量、或数值上严格等于 0）时 bit 取 0。

约定 ">0 取 1，否则取 0" 保证零向量签名为全 0，确定且可复现。
每个向量在每张表只算 b 次内积，故哈希计算次数恒为 N·L·b。

## 2. 碰撞概率与召回推导

对单位方向 u、v，法向量方向均匀随机时，sign(<u,h>)=sign(<v,h>)
的概率 p=1-θ/π，θ 为 u、v 夹角。两位签名完全相等（b 位全部一致）
的概率为 p^b。查询 x 与某近邻 y 至少在一张表同桶（成为候选）的概率：

  P_hit = 1 - (1 - p^b)^L。

候选数期望（含查询自身及非近邻碰撞）：

  E[C] ≈ 1 + Σ_{y≠x} p(θ_y)^b，单表；多表取并集，
  E[C_L] ≤ 1 + Σ_y [1-(1-p(θ_y)^b)^L]，随 L 饱和于 N。

### 位数 b 的权衡

- b 增大：同桶条件更严格，p^b 对所有 θ>0 都下降，桶更细、
  候选数 E[C] 单调下降；但近邻命中概率 p^b 也下降，召回下降。
- b 减小：桶粗，候选多、召回高，b→0 时全部同桶退化为暴力扫描。

### 表数 L 的权衡

- L 增大：P_hit=1-(1-p^b)^L 对每个近邻单调不降，故召回单调不降；
  候选数随 L 上升但有上界 N，边际收益递减。
- L=1 时召回可能偏低，L=8 接近饱和。测试以 L=1/2/4/8 实测
  召回单调不降，以 b=4/8/12 实测候选数单调下降。

## 3. 检索流程

1. 建索引：校验并清洗向量（NaN/±Inf 拒绝并计入跳过数），
   对每张表算签名，写入 签名→[]ID。索引对象构建后不可变，
   通过原子指针整体发布：查询要么看到全部 L 张表，要么一张都看不到
   （表数为 0 的空索引），不会读到半成品。
2. 查询：对每张表算查询签名，取所有同桶向量 ID 的并集为候选；
   计数器记录进入精排的去重候选数；对候选算精确欧氏距离，返回 TopK。
   只读索引可被多协程并发查询。
3. 召回核对：另给暴力精确 TopK 作基准，召回=|近似∩精确|/K。

## 4. 可复现性

法向量只依赖显式种子：RNG 用种子初始化的 splitmix64，采样顺序固定
（逐表、逐位、逐分量 Box-Muller）。同种子两次建索引产生的法向量、
因而每个向量每张表的签名逐字节相同；不同种子以高概率产生不同法向量，
签名存在差异。索引不依赖 map 迭代序（落盘时排序）。

## 5. 持久化文件格式

全部小端，自描述：

  头(36B): magic "LSHIDX1"(8) version u16 dim u32 bits u8 tables u8
          numVectors u64 numBuckets u64 dataLen u64
  超平面区: 每张表 seed u64（法向量由种子重放生成，避免存浮点）
  桶表区: 桶数=u32；每桶 sig u64、count u32、count 个 id u32，按 sig 排序
  尾: 上述全部字节的 CRC32(u32)

截断点按字节区间分类：头不完整 / 超平面参数不完整 / 桶表不完整 /
CRC 不匹配。恢复时解析到的最后一个完整桶为最大可恢复前缀，
桶内未知 ID 被剔除并计数，恢复结果保证无悬挂 ID。
全零法向量（退化超平面）读回时检出，定位到第几张表第几位。
