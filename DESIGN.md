规则约定
- 空行（两个换行紧邻，`\n\n` 或 `\r\n\r\n`）跳过，不产生记录。
- 单列记录的唯一字段为空（逗号分隔体系下无歧义的"值为空"）时，规范表示是 `""`。
- 记录以 `\n`/`\r\n` 结束；末尾最后一个换行不产生额外空记录；末尾无换行合法。
- 规范输入：UTF-8 字节；每条记录（含最后一条）以 `\n` 结束；无空行；空单值字段写 `""`；
  含 `,`、`"`、`\r`、`\n` 或带引号标记的字段以引号包裹，`"` 转义为 `""`；引号字段内 CRLF 原样。

推导一：空行与单列空值
单列表中记录只有一个字段。若把空行当成"未引号空字段的记录"，那么两个换行相邻就无法与
"一条空值记录后正常换行"区分：写回 `\n\n` 再解析仍得两条记录，而写一条空值记录用
裸空（`\n` 紧邻另一个记录换行）又与空行不可区分——必然丢数据或增数据。因此空行必须
跳过；单列空值记录必须以加引号空字段 `""` 写出，引号标记把它与"无内容的行"区分开。
多列记录里某个字段为空不会产生整行空白（存在逗号），用未引号空即可。

推导二：par 切点
切点必须落在"引号外的记录边界之后"（记录终止符之后的 0 长度位置）。预扫描一遍字节，
仅用 inQuote/quoteSeen/CR-pending 三态标记所有引号外的记录边界；K-1 个目标切点取其后
第一个安全边界（找不到则并入相邻段）。每个 worker 从安全边界单趟解析，偏移按段基址
全局化，记录号加前缀记录数，因此与流式结果逐位相同。若预扫描自身遇语法错误，直接退回
单线程流式解析（此时仍正确、仍含全部已产出记录；最坏输入退化为顺序解析）。预扫描加
K 次 worker 单趟 = 每字节至多约 2 次处理。CR 落在段尾的情形不可能发生：段尾必是
记录边界（CRLF 中安全点在 LF 之后），故不存在跨段的 CR 待定。

推导三：CR 待定
未引号字段见到 `\r` 时进入 crPending：不立即发射，下一字节是 `\n` 则行尾结束记录，
是其他字节则在 `\r` 自身位置报裸 CR 错误，EOF 则在该 `\r` 处报裸 CR。状态保存在
lexer 对象中，跨 Feed 自然续传（`\r` 与 `\n` 分属两次 Feed 也正确）。引号字段内
`\r` 是普通内容字节。par 中安全切点永远在完整行尾之后，worker 起点不会处于 crPending。

推导四：上限"立刻"
字段计数器累计"字段值字节"：未引号字段逐字节计；引号字段内普通字节计 1，`""` 在
quoteSeen 上确认是转义时合计计 1（开引号本身不计）。每接收一个值字节立刻比较上限，
第一个超限字节即报 ErrFieldTooLong，不缓冲整记录。字段数在每个字段开始（Field 回调）
时计数并立刻比较 ErrTooManyFields；记录数在记录终止回调时计数并比较 ErrTooManyRecords。
进入终态后 Feed/Close 恒返回同一错误指针。

状态转移表（字节类别：c=逗号 q=" r=CR n=LF o=其他；动作：F=字段单元 R=记录终止
S=字段开始 P=字段值字节；错误：1裸引号 2引号后非法字符 3引号未闭合 4裸CR）

| 状态       | c              | q            | r             | n        | o            | EOF    |
|------------|----------------|--------------|---------------|----------|--------------|--------|
| start      | F空→start      | →quoted      | →crPending    | 跳过空行 | P→unquoted   | 结束   |
| unquoted   | F→start        | 错误1@本字节 | →crPending    | F,R→start| P→unquoted   | F,R结束|
| quoted     | P(,)→quoted    | →qSeen       | P(CR)→quoted  | P(NL)→q  | P→quoted     | 错误3  |
| qSeen      | F→start        | P(")→quoted  | →crPendingQ   | R→start  | 错误2@本字节 | R 结束 |
| crPending  | 错误4@CR(回填) | 错误4@CR     | 错误4@CR      | R→start  | 错误4@CR     | 错误4@CR|
| crPendingQ | 错误4@CR       | 错误4@CR     | 错误4@CR      | R→start  | 错误4@CR     | 错误4@CR|

crPendingQ = 引号字段刚闭合后遇 CR（该 CR 属行尾待定，不属字段内容）。
lexer 单实例非并发安全；多个实例可并发使用；par 内 K 个 goroutine 只读各自分片。

错误坐标：字节偏移从 0 起全局计数；记录号从 1 起（含尚未终止的当前记录）；字段号从 1
起（Field 回调分配）。错误实现为具名 lexer.Error 包裹哨兵，errors.Is 可判定。
