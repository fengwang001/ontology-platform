# DESIGN — JSON 字符串字面量严格编解码器

包划分：`esc`（单个转义序列与代理对判定，无依赖）→ `jstr`（`Decode`/`Encode`/流式 `Writer`，依赖 `esc`）。
`Encode` 签名为 `func Encode(s string) ([]byte, error)`：输入含非法 UTF-8 时返回 `*jstr.Error{Kind: KindUTF8}`，不静默替换。

## 第 2 条：代理对错误偏移的推导

事实：高代理 `\uD800–\uDBFF` 本身是一个语法完好的 `\uXXXX` 单元，它的"错"只在后续单元
无法与其配对时才发生；低代理 `\uDC00–\uDFFF` 则在任何"期望普通单元"的位置上本身就是非法的。
因此规则定为：**错误偏移指向使配对失败的那个单元的首字节**——
- 孤立/领头低代理：指向它自己的反斜杠（该单元自身不可配对，如 `"\uDE00"`、`"\uDE00\uD83D"` 偏移 1）；
- 高代理后未跟合法低代理：指向高代理结束之后、低代理转义本应开始的那个字节
  （`"\uD83D"` 偏移 7 指向 `"`；`"\uD83Dx"`/`"\uD83DA"` 偏移 7；`"\uD83D\u0041"` 偏移 7，即第二个 `\`）。
理由：指向高代理起点会冤枉一个合法单元且丢失"扫描实际失败点"的信息；指向失败单元首字节
精确定位了需要修复的位置，且两类失败（自身非法 / 缺席配对）用同一规则覆盖。

`"\uD83DA"` 不能解释为「U+FFFD + `A`」：RFC 8259 的语法里不存在孤立代理这一合法值，
高代理后必须紧跟 `\u` 低代理。若静默替换为 U+FFFD，则 `"\uD83DA"` 与 `"\uFFFDA"` 两个不同
字面量解码出同一字符串——解码不再单射，错误输入混入值域，第 5 条往返性质随之崩塌；
且替换字符抹掉了原始字节信息，调用方无法区分"数据本就是 U+FFFD"与"数据已损坏"。

## 第 4 条：最小转义与唯一性的推导

RFC 8259 的 `unescaped` 产生式为 `%x20-21 / %x23-5B / %x5D-10FFFF`：U+0000–U+001F 被显式
排除，故**必须**转义，否则输出不是合法 JSON；U+007F 落在 `%x5D-10FFFF` 内，任何合规解析器
都必须接受其原样出现，故**不必**转义。`/`、非 ASCII、U+2028/U+2029 同理合法，一律原样。
于是每个码点的输出拼写由确定性划分唯一决定：`"`/`\` → 加反斜杠；五个控制字符 → 短形式；
其余控制字符 → `\u00XX`（小写）；其余一切 → 恒等。无任何选择点 ⇒ 输出**唯一**。
单射：第 5 条给出 `Decode(Encode(s)) == s`，即 Decode 是 Encode 的左逆，有左逆者必单射；
等价地，输出可被确定性反转义唯一还原，故不同输入不会产生相同输出。

## 测试对照表

（待实现完成后补齐）
