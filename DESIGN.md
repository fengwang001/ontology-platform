# 设计：带溢出的哈希聚合器

目标：输入行 (分组键, 数值) 超内存预算时，按哈希分区把数据溢出到本地临时目录，再逐分区回读聚合。分区数构造时固定，与数据规模无关。

## 1. 溢出什么：部分聚合状态，而非原始行

- 溢出原始行：写盘量正比于输入行数 N；热点键（大量重复键）时磁盘写入毫无缩减；回读必须逐行重新聚合，回读代价也是 O(N)。
- 溢出部分聚合状态：写盘量正比于驻留分组数，而驻留分组数 ≤ 预算 B，每次溢出写盘 ≤ B 条记录；且每次溢出至少腾出一个空位才能再触发下一次，故全程写盘记录总数 ≤ 不同键数 ≤ N，通常远小于 N。回读时直接合并状态，无需重新逐行聚合。
- 结论：选部分聚合状态。代价是状态必须可编码、可合并，且合并顺序影响浮点和的位模式（见第 3 节），用固定合并顺序解决。

## 2. 同一分区多次溢出仍正确的原因

预算反复被打破时，同一分区会溢出多次，产生多个溢出片段（段文件）。正确性依赖两条：

1. 可合并且贡献不重叠：每个输入行的贡献只进入当时内存中的状态；溢出把该状态整体落盘并从内存清除，后续同键行从空状态重新累积。因此任一行的贡献恰好落在一个片段（或最终的内存余量）中，片段之间贡献互不重叠。
2. 每个片段恰好合并一次：回读时按段序号收集该分区**全部**片段，与内存余量一起各合并一次。漏掉任何一段就少算（本题最易错点），重复合并则 Count/Sum 会重复计数。实现上用「写出行数 == 读回行数」计数器守这条。

由于合并是针对不相交贡献集的并操作，只要「全部片段各合并一次」，结果与溢出次数无关。

## 3. 合并的代数性质：Min/Max 与 Count/Sum 不同

- Count：整数加法，结合律、交换律成立且精确；不幂等。
- Sum：浮点加法。IEEE 754 加法一般不满足结合律（舍入），仅在无舍入（如整数值、2 的幂次刻度）时精确结合。因此实现固定合并顺序（段序号升序 → 内存余量；分区按编号升序）保证确定性；测试用整数值断言与全内存聚合逐位相同。
- Min/Max：min/max 运算，结合律、交换律、**幂等**均成立，且不受浮点舍入影响。
- 关键差异：Min/Max 幂等（重复合并同一状态结果不变），Count/Sum 不幂等（重复合并会重复计数）。所以「每段恰好一次」对 Count/Sum 是硬约束，对 Min/Max 是宽松约束；统一按硬约束实现。

## 4. 分区文件格式（自描述）

- 头 20 字节：magic(4)="HAG1" | partID(uint32 LE) | segIdx(uint32 LE) | recordCount(uint64 LE)。recordCount 使「齐记录边界的截断」也可判定（期望记录数未到）。
- 每条记录：len(uint32 LE) | payload(len 字节) | CRC32-IEEE(payload)(uint32 LE)。payload 为 acc 编码的 (键, Count, Sum, Min, Max)。
- 写出：先写 `part-%04d-seg-%06d.tmp`，成功后原子改名为无 `.tmp` 后缀；启动时扫描目录清理残留 `.tmp`（写盘崩溃的半截文件），绝不读入。

## 5. 截断分类（截到字节 t 时）

- t < 20：头部不完整（ErrHeader）。
- t 落在某记录的 [起点, 起点+4)：长度前缀不完整（ErrLengthPrefix）；含「齐边界截断但 recordCount 未到」的 EOF 情形。
- t 落在 [起点+4, 起点+4+len)：行体不完整（ErrBody）。
- t 落在 [起点+4+len, 起点+8+len) 或 CRC 校验值不符：CRC 不匹配（ErrCRC）。
- 四类各为独立哨兵错误，可用 errors.Is 区分；错误携带分区号与字节偏移，聚合必须中止，不得输出残缺结果。

## 6. 资源与复杂度约束（可证明）

- 驻留分组数 ≤ B：插入新键前先判断，若驻留数已达 B 则先溢出当前最大分区再插入，故峰值永不超过 B；非导出计数器记录历史峰值供测试断言。
- 每行只哈希分区一次、只被处理一次（入表）；溢出记录总数 ≤ 不同键数 ≤ N（第 1 节），回读每条恰好一次，故总行处理次数 ≤ 2N。
- 分区数构造时固定，不随分组数增长。

## 7. 并发与确定性

回读阶段每分区一个协程，聚合状态互不共享；结果按分区编号升序合并（不依赖完成顺序），输出按键排序。因此无论回读完成顺序如何，分组顺序与每组 Sum 位模式逐字节相同。

## 8. 边界语义

零行→空结果；单行；同键多行；分组数 == B 不溢出、== B+1 恰好溢出一次；空串键合法；NaN 拒绝并计入跳过数；±Inf 参与 Sum 与 Min/Max；-0.0 在入表前归一化为 +0.0（±0 视为相等，保证 Min/Max/Sum 位模式稳定）。
