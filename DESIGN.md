# 设计推导（DESIGN）

区间统一记为 `I=[L,R)`，左闭右开：点 `p` 属于 `I` 当且仅当 `L<=p<R`。端点类型为 `int64`。

## 1. 相接算不算重叠；点查归属

左闭右开下 `[1,3)` 的点集是 `{x|1<=x<3}`，`[3,5)` 是 `{x|3<=x<5}`，交集为空。
定义交为 `[max(L1,L2), min(R1,R2))`，相交非空要求 `maxL < minR`。相接时 `maxL==minR`，
交是零长度区间，故**相接不重叠**。朴素线性扫描的定义即“与被查区间交集非空”，两者逐元一致。
点查 `3`：`3<3` 对第一个不成立，故不命中 `[1,3)`；`3>=3` 命中 `[3,5)`。
等价地 `Overlaps(I, Point(p))` 与点查使用同一谓词，不漏不多。

## 2. 零长度区间 `[2,2)`

它的点集为空。三条结论因此自洽：
1. **能插入**：`L<=R` 即合法（`L>R` 才报错），作为多重集元素存在，计入数量、可删除。
2. **点查永不命中**：`2<=p<2` 对任何 `p` 都不成立。
3. **重叠查询永不命中它**：对任意 `J`，`max(2,J.L) < min(2,J.R)` 不可能成立
   （交集至多退化为 `[x,x)`）。注意：零长度区间即便“嵌在”`[1,5)` 内也不命中——它不含任何点。

朴素扫描用同一交集谓词，结果一致。零长度区间仅作为已记账的实体存在。

## 3. 结果排序规则

要求“重复执行逐位相同”且“与插入顺序无关”，所以排序键不得依赖任何插入序状态
（如自增序号、指针地址、map 遍历序）。可作键的只有区间本身与载荷。采用全序：
`(L 升序, R 升序, Payload 升序)`。三者完全相同的两个元素是多重集中不可区分的副本，
交换它们不改变元组序列，因此序列逐位确定，与插入/删除次序无关。

## 4. 删除哪一个副本

`Delete(I)` 在键序树上找到第一个匹配 `(L,R,Payload)` 的节点删除。其余副本键完全相同，
删除后剩余多重集与“删任意一个副本”的剩余多重集恒等；查询输出是排序后的元组序列，
故结果不随删哪个、按什么插入次序而变。删三次空后再删返回“不存在”错误。

## 关系对照表（左闭右开；“零长度”指参与方为 `[x,x)`）

| 关系 | 示例 | Overlaps | 说明 |
|---|---|---|---|
| 相离 | [1,2) 与 [3,4) | 否 | maxL > minR |
| 相接 | [1,3) 与 [3,5) | 否 | maxL==minR，交集零长度 |
| 部分重叠 | [1,4) 与 [3,6) | 是 | maxL < minR |
| 包含 | [1,10) 与 [3,5) | 是 | 小者点集非空 |
| 完全相同 | [2,5) 与 [2,5) | 是 | 多重集允许并存 |
| 零长度 | [2,2) 与任意 | 否 | 点集为空，点查同样不命中 |

## 数据结构

AVL 平衡 BST，键序即上面的全序；每节点额外记 `maxR=max(R, 左.maxR, 右.maxR)`，
在插入回溯、删除回溯、每次旋转后重新计算。查询剪枝：左子树 `maxR<=q.L` 时整棵不相交；
右子树在 `q.R<=node.L`（重叠）或 `node.L>p`（点查）时跳过。计数器只记被访问（判过剪枝）
的节点数，存于 query 包非导出字段。
