# DESIGN

## 推导 1：压缩结果与 Write 切法无关
压缩器把距上次 Flush 之后的输入**全部缓存在 pending 段**，Write 只追加字节，不做任何编码决策。
决策只在 Flush/Close 时对“完整 pending 段”扫描一次：在位置 i，匹配器返回窗口内最长匹配 (d,l)，
l>=3 出发匹配记录并前进 l，否则输出 1 个字面量并继续。同一段字节无论被切成多少段 Write，
扫描输入完全一致，窗口与哈希链状态也一致（都只依赖已提交前缀），故输出逐字节相同。
Flush 强制结束当前匹配：把 pending 全量编码后追加 Flush 标记并清空 pending；解压器见到标记前，
字面量/匹配记录已足以还原全部输入。pending 为空时 Flush 是恒等操作，故连续第二次 Flush 零字节。

## 推导 2：重叠回指 (dist < length)
块复制语义是 memmove 语义下取“复制前”的源，当 d<l 时源区与目的区重叠，一次性 copy 得到的是
源窗口的前 d 字节重复截断，而非 `abcabc...` 的周期延展。正解是**逐字节向前** `out[i]=out[i-d]`。
实现不退化成纯循环：每轮 `n=min(剩余, d)`，`copy(out[len:][:n], out[len-d:][:n])`。
内建 copy 允许重叠且为 memmove 语义；每轮至少复制 d 字节，轮数 ceil(l/d)，总复制字节数恰好 l，
d=1 时直接逐字节 append（小 copy 的调用成本高于逐字节，两条路径语义相同）。

## 推导 3：两类“距离越界”为何可区分
dist==0：编码上无意义（不能指向当前字节）。dist>windowCap：回指超出环形窗口能保存的历史，
压缩器永远不会产生——匹配器只在窗口现存槽位中取候选，并行预置字典也只装入 min(W,len) 字节。
dist>已输出：语义上指向“尚未产生的输出”，例如窗口 4096 但解压才输出 10 字节时 dist=100。
两者先后检查（先 0，再已输出，再窗口）给出可区分错误与偏移。
并行预置字典不改变“已输出”含义：块 i 压缩前把块 i-1 末尾 W 字节装入窗口，而流式解压器在解
块 i 之前已经逐字节输出过块 0..i-1 的全部原文，环形窗口中恰好有同样的历史，故无需带外字典。

## 推导 4：并行块压缩的确定性与衔接
块 i 的压缩输入 = 本块原文 + 窗口初始态；窗口初始态只由块 i-1 原文末尾 W 字节唯一决定
（哈希链/窗口不跨块保留其他状态），与别的块的压缩结果、goroutine 完成顺序无关。
故各块可并发压缩，主 goroutine 按块序号收集并拼接即对 workers∈{1,2,4,8} 逐字节相同。
块之间**不需要刷新标记**：块边界只决定匹配器从哪里开始，匹配引用的历史在解码器窗口中已存在；
仅整个流开头写一次头、结尾写一次尾。

## 字节布局（整数均为 LE-B128 变长，小端，≤10 字节）
| 记录 | 布局 |
|---|---|
| 流头 | `4C 37 01`（魔数 "L7" + 版本 1） |
| 字面量段 | tag=0, varint n, 随后 n 个原文字节 |
| 回指 | tag=1, varint dist(>=1), varint length(>=3) |
| 刷新标记 | tag=2（无负载） |
| 流尾 | tag=3, varint 原文总长, varint FNV-1a32 校验和 |
