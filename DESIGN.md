# 可撤销批量重命名器 — 设计推导

## 模型

命名空间 `N` 是一个名字集合（map）。一批请求 `R = {old_i → new_i}` 表示把
`old_i` 搬到 `new_i`。单步执行 `o→n` 的语义：要求 `o ∈ N` 且 `n ∉ N`，然后
`N := (N \ {o}) ∪ {n}`；若 `n ∈ N` 则发生**覆盖**，属于数据损坏，必须杜绝。

## 排序规则推导

执行 `o→n` 时 `n` 必须空闲。`n` 若不空闲，要么它是外部名（冲突，直接拒绝），
要么它是某请求 `q` 的旧名 —— 此时只要 `q` 先执行完，`n` 就被搬走了。于是：

> 若 `new(p) == old(q)` 且 `p ≠ q`，则 `q` 必须先于 `p` 执行。

把每个请求看作节点、上述关系看作有向边 `p → q`（p 依赖 q），合法执行顺序
就是该图的**拓扑序**。对无环部分，「新名是某未执行请求的旧名 ⇒ 那个请求先
走」的贪心即拓扑排序；同层（入度同时为 0）按旧名字典序取，保证确定性。
正确性归纳：拓扑序中执行 `o→n` 时，若 `n` 是某请求旧名，该请求必已执行
（否则存在反向依赖违反拓扑序），故 `n` 已被搬走；若 `n` 不是任何旧名，则
`n ∉ N` 由冲突检测保证。因此**每一步目标名都不存在，全程无覆盖**。

## 环与破环

依赖图是函数图（每个节点出度 ≤ 1：一个旧名至多是一个请求的新名，否则触发
第三类冲突）。函数图的有向环都是简单环，环上请求互为「新名即对方旧名」，
任何顺序都会在最后一步覆盖，**必须借临时名**。

通用做法：对每个环 `v0→v1→…→vk→v0`，取环上字典序最小的旧名 `v0`（确定性），
把请求 `v0→v1` 拆成两步：`v0→T` 与 `T→v1`，其中 `T` 是临时名。原依赖边
`pred(v0)→(v0→v1)` 变为 `pred(v0)→(v0→T)`，而 `T→v1` 依赖环上前驱
`vk→v0`（因为 `v1` 是请求 `v1→v2` 的旧名）。拆边后环被打破，全图无环，拓
扑排序给出合法顺序。例 `{a→b,b→c,c→a}`：拆 `a→b` 得 `a→T, T→b`，拓扑序
`b→c, c→a, a→T, T→b`，逐步验证无覆盖。

**临时名数量论证**：每个环只需拆一条边即可使其无环（环去掉一条边即成链），
且一个临时名只服务一个环，故临时名数**恰好等于环的个数**。测试用 10 个互
不相交的环精确断言计数为 10。

**临时名生成**：形如 `~tmp-<counter>`，生成时校验不在 `N`、不在任何请求的
旧名/新名集合中，冲突则递增计数器重试；重试超过上限返回可判定错误
（`cycle.ErrNoTempName`），绝不覆盖数据。测试预占一批临时名仍成功。

## 四类冲突的判定条件（全部在任何修改之前完成）

1. `ErrTargetExists`：`new_i ∈ N` 且 `new_i` 不等于任何请求的 `old_j`
   （即不会被搬走）→ 拒绝整批，报出该名。
2. `ErrDuplicateTarget`：`new_i == new_j`（i≠j）→ 报出两个旧名。
3. `ErrDuplicateSource`：`old_i == old_j`（i≠j）→ 报出两个新名。
4. `ErrMissingSource`：`old_i ∉ N` → 指出该名。

检测只读 `N`，检测到任何冲突即整体拒绝，`N` 逐元素不变（测试断言）。
四者用 `errors.Is` 区分。请求数远大于名字数时大量重复旧名必触发第 3 类。

## 自环语义

`a→a` 定义为**无操作**（不是冲突）：执行前后 `N` 不变，不产生步骤、不写日
志记录。理由：`a` 已存在且目标即自身，视为幂等成功最符合重命名工具直觉；
若当冲突会误伤「批量里混入恒等项」的正常用法。

## 执行、日志与撤销

`apply` 持命名空间互斥锁执行整批（并发修改被阻塞至批次结束，文档定义此行
为并由测试验证）。每步先断言目标名不存在（防御性），成功后向日志追加一条
记录。第 k 步注入失败时，已执行的 k-1 步按逆序立即回滚，`N` 逐元素复原。

日志格式（全部大端）：
- 头部 12 字节：magic `RNLOG001`(8) + 记录数 u32(4)；
- 每条记录 9 字节帧：`[len:u16][crc32:u32][kind:u8][oldLen:u8][newLen:u8]`
  后接 `old`、`new` 字节，crc 覆盖 kind 起的内容；
- 尾部 12 字节：magic `RNEND001`(8) + crc32 u32(4)，crc 覆盖此前全部字节。

撤销按日志逆序执行 `new→old`。逆序处理时若某步的旧名仍被后续步骤占用，
则该步无法撤销（更早的步骤不可能为其腾出名字：若更早步骤的目标名等于
它，则拓扑序矛盾），跳过并计入无法撤销数。

幂等：环形批次是同一集合上的置换，撤销前后名字集合完全相同，基于状态
无法区分「未撤销」与「已撤销」，因此采用**日志消费**语义：撤销完成后把
日志重写为剩余未撤销的记录（完全撤销时重写为 0 条记录的空日志），同一
日志重复撤销即为无操作。

截断分类（截到 t 字节，设头 12、记录区结束于 E、总长 L=E+12）：
- `t < 12` → `undo.ErrBadHeader`（头部不完整）；
- `12 ≤ t < E` → `undo.ErrTruncated`（记录不完整：帧头或记录体被切断）；
- `t = E`（记录完整但无尾部）→ `undo.ErrTruncated`；
- `E < t < L` → `undo.ErrCRCMismatch`（尾部被截：识别为尾部 magic 前缀
  或 crc 不全，完整性无法确认）。
撤销按**最大可恢复前缀**进行，报告已撤销步数与无法撤销的步数；三类错误均
可用 `errors.Is` 区分。

## 边界

空请求集：合法，零步骤。单请求：直接执行。空串名字：合法。名字含 `/`、`\`：
按普通字符处理，不做任何路径解释。复杂度：冲突检测与排序只做哈希表查找，
总查找次数 ≤ `4*(请求数+涉及名字数)`，由 `plan` 非导出计数器在测试中验证。
