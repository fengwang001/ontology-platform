# DESIGN：.properties 解析与回写

## 第 3 条推导：续行中遇到空行/纯空白行

依据 `Properties.load` 规范与 LineReader 拼行循环：物理行先计数行尾反斜杠，
奇数则丢弃换行符、再读下一物理行，并**先跳过新行的全部前导空白**。

- 下一物理行为**空行（只有 `\n`）**：拼行循环立即结束，逻辑行在已拼接内容处终止。
  例：`k=v\` + 空行 + `w=x` => `k`->`v`，且 `w=x` 作为新逻辑行解析。
- 下一物理行**只含空白**（空格/制表）：空白剥离规则把整行内容剥成空串，
  本行贡献 0 个字符；由于它本身不是空行读取，循环继续，再读下一行
  （该行前导空白同样被剥）。例：`k=v\` + `   \n` + `  w` => `k`->`vw`。

结论：空白续行被**整体吞掉且不终止续行链**，空行才终止。这正是「续行首部
空白被剥掉」规则作用于「整行都是空白」时的退化情形——剥完什么都不剩。

## 第 6 条推导：Store 各字符是否必须转义

load 的关键规则：逻辑行行首空白被跳过；键扫描时裸空白/`=`/`:` 结束键；
分隔符后、值开头的空白被跳过；值中间与值尾部空白保留；`#`/`!` 只在**逻辑行
第一个非空白字符**处触发注释；反斜杠是转义前缀；裸 `\n`/`\r` 切分物理行。

固定输出格式 `escapedKey + "=" + escapedValue + "\n"`，逐字符推导：

| 字符 | 键 | 值 | 理由 |
|---|---|---|---|
| `\` | 必转 | 必转 | 转义前缀，裸出现会吞掉下一字符 |
| `\n \r \t \f` | 必转 | 必转 | 裸换行断行；其余空白字符在键中会终止键 |
| `=` `:` | 必转 | 不转 | 只在键扫描阶段被当作分隔符，值中是字面量 |
| 空白 | 全部必转 | 仅首字符必转 | 键中任意裸空白结束键；值只有开头空白会被跳过，中间/尾部保留 |
| `#` `!` | 仅首字符必转 | 不转 | 注释只认逻辑行行首；值中、键中部均为字面量 |
| 非 ASCII | 原样 | 原样 | UTF-8 直接输出，不写 `\uXXXX` |

空键写 `=value`（裸 `=` 前空键合法）；空值写 `key=`。

## 语义 -> 测试函数对照表

（实现完成后补齐）
