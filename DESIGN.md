# 可撤销批量重命名：设计推导

## 1. 模型
命名空间是一个字符串集合（`map[string]struct{}`），所有名字都合法：空串合法，
`/` 等路径分隔符按普通字符处理。请求是有序对 `old -> new`。

## 2. 四类冲突（全部在任何修改之前检测）
1. 目标已存在：`new` 当前在集合中，且 `new` 不是本批某个请求的 `old`
   （即它不会被搬走）。错误 `ErrTargetExists`。
2. 重复目标：两个不同请求的 `new` 相同（自环 `a->a` 的目标不参与重复统计）。
   错误 `ErrDuplicateTarget`，携带两个 `old`。
3. 重复旧名：同一 `old` 出现两次。错误 `ErrDuplicateSource`，携带两个 `new`。
4. 旧名不存在：`old` 不在集合中。错误 `ErrMissingSource`。
四类错误可用 `errors.Is` 区分；检测阶段只读集合，因此命名空间逐元素不变。

## 3. 排序规则（拓扑序推导）
把每个请求看成有向边 `old -> new`。执行 `old->new` 时，`new` 必须当前不存在。
若 `new` 恰好是另一条边的起点（该名字尚未搬走），则必须先执行那条边。
即：边 `u->v` 依赖于以 `v` 为起点的边。于是得到依赖「先执行 `v->w`，再执行
`u->v`」。对依赖图做拓扑排序即安全顺序。例：`{a->b,b->c}` 中 `a->b` 依赖
`b->c`，故顺序为 `b->c` 然后 `a->b`，任何一步的目标名都不存在，无覆盖。

无入边约束的起点（入度 0 的请求）先执行；同层按 `old` 名字字典序，保证确定性。

## 4. 环的破解
有向环上的请求相互等待，拓扑序不存在，任何直接顺序都会覆盖。通用做法：对每个
环只挑一条边 `u->v`，改为
1. `u -> tmp`（腾出 `u`，`tmp` 是全新临时名）；
2. 沿环逆向执行其余边（`... -> ...`、`v -> u`），此时每个目标名都已腾空；
3. `tmp -> v`。
二元环 `{a->b,b->a}`：`a->tmp, b->a, tmp->b`。
三元环 `{a->b,b->c,c->a}`：`a->tmp, c->a, b->c, tmp->b`。
每个环恰好引入 1 个临时名（先搬走的那一个节点），环数即临时名数。
临时名生成：取形如 `.rename.tmp-<n>` 的候选，要求它既不在当前集合中，也不与
本批任何 `old/new` 相同；冲突则 `n++` 重试；超过上界返回可判定错误
`ErrTempExhausted`，绝不覆盖数据。

## 5. 复杂度
冲突检测与排序只用哈希表查找 + Kahn 入度算法，总代价 O(请求数 + 涉及名字数)。
包内非导出计数器记录集合/表查找次数；预算 `4*(R+N)`。破环为每条环边做常数
次工作，临时名数 = 环数。

## 6. 执行、日志与撤销
执行持命名空间写锁，期间任何并发 `Rename/Remove` 阻塞（demo 另验证 TryLock
拒绝语义）。每步先写日志（fsync 到临时目录文件）再改集合，保证可撤销。
日志格式：魔数头 `RENLOG1\n`（8 字节）；每条记录为
`uvarint(len) | old 字节 | new 字节（各带 uvarint 长度前缀）| CRC32-IEEE(载荷)
| uvarint(len)（尾长度，供截断分类）`。
撤销按日志逆序执行反向重命名。
截断点分类（在 EOF 处判定）：
- 连首/尾 uvarint 长度都无法完整读出：`ErrHeaderTruncated`（仅首记录）/
  `ErrRecordTruncated`；
- 长度字段完整但载荷或 CRC 被切：CRC 区域缺失 -> `ErrCRCMismatch`，
  载荷缺失 -> `ErrRecordTruncated`；
- 长度完整且 CRC 算出但不匹配：`ErrCRCMismatch`。
撤销恢复「最大可恢复前缀」，报告前缀步数与无法撤销的尾部步数；损坏日志保留，
不再删除。完整日志撤销成功后删除日志文件，再次撤销为无操作（幂等）。

## 7. 故障注入
`Executor` 接受 `failBefore(step)` 钩子；在第 k 步实际修改前返回错误时，已执行
步骤逆序撤销，命名空间逐元素还原。

## 8. 边界语义
- 空请求集：合法，计划为空，执行与撤销均为无操作。
- 单请求：直接一步。
- 自环 `a->a`：定义为**无操作**（旧名即新名，无覆盖风险），不参与冲突统计与
  排序，执行时零步、日志不记录。
- 名字为空串：合法成员。
- 路径分隔符：普通字符。
- 大量重复旧名：被「重复旧名」冲突在检测阶段整批拒绝。
