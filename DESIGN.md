# 规范化器设计推导

规范化只做两件事：任意行尾 -> `\n`；删除每行末尾的 ` ` 与 `\t`（含无换行的末行）。

## 1. 末尾换行策略（推导）

要求 N(N(x))=N(x) 且不插入原文没有的字节（否则双向互逆 `ToOut(ToOrig(o))=o`
无法为合成字节给出合法原像），故三种策略都只能删除、不能新增：
- Keep：保留末尾换行原状（"" 仍为 ""）。
- EnsureOne：原文以换行结尾则只保留 1 个；原文无换行则不补换行（补 `\n` 会
  产生无原像的合成字节；且空串补 `\n` 后再规范化仍可能需要补，规则被迫自指）。
- TrimEmpty：删掉全部末尾空行；非空内容保留恰好一个结尾换行；空串仍为空串。
  （纯空白行已先变空行，故 "  \r\n" 属"全是空行"。）

| 输入 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

三策略输出都只含 `\n` 且无行尾空白，故再规范化为空操作，幂等成立。

## 2. 被删字节的 ToOut（推导）

映射以"边界"定义：输出侧 o=0..L，原文侧 i=0..N。已保留字节从 0 起一一对应。
被删游程（行尾空白、被删 `\r`）把原文的多个边界压到同一输出边界：
- 单调不减 + `ToOut(ToOrig(o))=o` 要求该公共值必须是仍有原像的 o。
- 行尾空白（"abc  \n" 的两个空格）：空白前后的保留边界都是换行前位置 3，
  故游程内所有 i 取 3（换行**之前**，光标停在新行行尾，符合编辑器直觉）。
- `"\r\n"` 的 `\r`：保留的是 `\n`，`\r` 前边界与 `\n` 前边界相同，
  故 ToOut(i_of_\r)=ToOrig 中 `\n` 的位置；单独 `\r` 不删，变为 `\n`，1:1。

## 3. 缓冲超限（推导）

选择**拒绝**。空白游程尚未见行尾/EOF 时无法决定去留：强制输出会把可能的行尾
空白吐出，若后续真为行尾则违反语义 2/5（输出出现行尾空白且不可幂等），且字节
已离开状态机，违反 4/6 的映射一致性。拒绝则输出与映射都停在最后确定位置，
状态可解释（已输出前缀 = 已确定前缀的规范化结果），4/5/6 均不被破坏。
哨兵错误 `ErrPendingLimit` 带原文起始偏移；之后 Write 进入终态返回 `ErrClosed`。

## 4. par 拼接（推导）

各段独立规范化时段首状态干净（无遗留 `\r`/空白），得到 out、映射和段尾
pending（一串空白 + 可选一个待配 `\r`）。顺序合并段 k 与段 k+1：
1) 段 k 的待配 `\r` 看段 k+1 首字节：是 `\n` 则配成 `\r\n`（删 `\r`，放 `\n`），
否则 `\r` 单独成行（变 `\n`）；
2) 段 k 的待判空白接到段 k+1 首行内容前：其后首字节为行尾 -> 全删，
否则整体保留；修正只触及"上一段 pending + 下一段第一个确定字节"，修正范围
≤ pendingLimit + 1，与段长无关，有上界。
各段映射是仿射分段（seg: 原 [a,b)-> 输出 [c,d)），按段原始起点平移 a、
输出累积长度平移 c 即得全局表。全部段为纯删除/保留/1:1 转换，无合成字节，
故拼接结果与单次流式处理逐字节相同；最后再整体套用一次末尾策略。
