# JSON 字符串严格编解码设计

## 代理对错误偏移

规则：错误先归因于不能被语法接受的最小单元，且该单元的起点是偏移。

- 单独低代理：它本身不能开启代理对，偏移指向低代理 `\u` 的起点，如 `"\uDE00"` 的 1。
- 高代理后缺少第二个转义：高代理是合法对的前半，但后续单元缺失；EOF 时偏移取高代理起点，因为错误集合从那里开始，如 `"\uD83D"` 的 1。
- 高代理后字节不是 `\uXXXX`：后面那个字节/单元不能完成对，偏移指向它的起点；`"\uD83Dx"`、`"\uD83DA"` 都指向 7。
- 第二个单元不是低代理：偏移指向第二个 `\u` 的起点，如 `"\uDE00\uD83D"` 中第二个 `\u` 为 7；这是“后续不合格单元起点”规则。
- 若输入在高代理后直接结束，没有后续单元可指向，才回退指向高代理起点。

`"\uD83DA"` 不能解释成 U+FFFD 加 `A`：RFC 8259 只定义高代理后紧跟低代理；`A` 不是第二个 `\uXXXX`，且严格解码禁止用替换字符掩盖结构错误。

## 最小转义与唯一性

规则：编码只转义会破坏 JSON 字符串或不允许裸出现的码点：引号、反斜杠、U+0000–U+001F；其他字节（含 `/`、U+007F、U+2028/U+2029）在 UTF-8 合法时原样输出。

U+001F 必须转义，因为 RFC 8259 明确禁止字符串中出现未转义控制字符；U+007F 虽是控制语义字符，但不在 U+0000–U+001F 集合，也不是定界符或转义符，所以合法且不必转义。

短转义只用于固定的五码点，其他控制字符唯一映射到小写 `\u00XX`。任意码点的编码形式由自身唯一决定；合法字面量解码后再编码时，短转义、四位十六进制转义和裸 UTF-8 三类表示分别回到唯一的规范形式，因此 `Encode` 单射且输出唯一。
