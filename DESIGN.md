# 带转义文本 RLE：设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`（合法编码唯一）。

## 拒绝清单（接受即违反不变量）

1. 显式次数 1：接受 `1a` 则 `Decode("1a")="a"`，而 `Encode("a")="a" != "1a"`。
2. 次数 0：`0a` 解码为空，`Encode("")="" != "0a"`；0 次游程也不可能由 Encode 产生。
3. 前导零：`02a` 规范化为 `2a`，自身不等于编码结果；`0a` 同时落入第 2 条，故零单列。
4. 相邻同符号：`2a3a` 解码为 "aaaaa"，`Encode` 按最长游程合成为 `5a`，输入被改写。
5. `\` 后非数字/非反斜杠：如 `\x`，该符号无规范转义形式；也可能与多字节边界冲突。
6. 末尾孤立 `\`：`a\` 缺符号，且 `\` 只能作为转义引导，不是完整游程。
7. 末尾只有次数：`12` 没有符号，根本不是完整游程。
8. 非法 UTF-8：符号必须是 Unicode 码点；非法字节既无 Encode 形式，也可能制造歧义。

以上八类返回彼此可区分的哨兵/具名错误，并携带违规起始字节偏移。

## 大次数取舍

选择**支持任意大次数**：`runs.Count` 内部用 `math/big`，十进制解析逐位累加，永不溢出。
解码不按次数预分配：输出按块（固定缓冲重复符号）惰性写入，并维护已输出字节数；
配置上限 `WithMaxOut(n)`，超限返回可判定的 `ErrOutputLimit`。默认上限 64 MiB。
因此 `999…999a` 不会溢出、也不会在解码前分配等量内存。

## 流式一致性

解码器是逐字节状态机：次数数字缓冲、`\` 转义态、多字节码点按 UTF-8 首字节长度逐位组包，
每个字节恰好检查一次（非导出计数 == 输入字节数）。一个游程延迟到「下一游程符号已确认」
或 `Close` 时才提交，从而 `2a3a` 必须看到 `3` 后的符号才能判定相邻同符号。

## 语义—测试对照表

<!-- TABLE -->

<!-- END -->
