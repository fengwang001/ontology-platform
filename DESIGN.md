# 设计推导：可撤销批量重命名与冲突解析器

## 1. 模型与边界语义
- 命名空间 N 为名字集合；合法名字为任意字符串：空串合法，路径分隔符按普通字符处理。
- 自环 a→a 定义为无操作（no-op）：保留在计划中产生空步骤，不参与冲突与图分析。
- 空请求集合法，Apply 为无操作。

## 2. 四类冲突（检测在任何修改之前；用 errors.Is 区分）
- ErrSrcMissing：旧名 s 当前不在 N。
- ErrDupSrc：同一旧名出现两次（自环除外），报出两个新名。
- ErrDupDst：两个请求指向同一新名（去掉自环），报出两个旧名。
- ErrDstExists：新名 t 已在 N，且 t 不是本批某请求的旧名（不会被搬走）。
全部基于 map：先收集 src→dst 与 dst→srcs，再线性扫描。

## 3. 执行顺序推导（无环部分）
请求 a→b 会写入 b。若 b 是请求 b→c 的旧名，必须等 b→c 搬走后才能执行 a→b，否则覆盖。
功能图中边 u→v 表示「u 依赖指向 u 的边先执行」。算法：对函数边做反向拓扑——出度为 0
的终点先执行，逐步剥离；同层按旧名字典序排列，保证确定性（打乱构造顺序结果不变）。
{a→b,b→c}：b 出度为 0，先 b→c 再 a→b，每一步目标名都不存在。

## 4. 环的破解
功能图每节点出度至多 1，分支结构为「一条链汇入环或终点」，环两两不相交。
对环选字典序最小代表 x，引入 1 个临时名 t：先 x→t；再沿环反向顺序执行其余边
（每步目标已腾空）；最后 t→(x 的原目标)。
- 二元环 a→b,b→a（代表 a）：a→t, b→a, t→b。
- 三元环 a→b,b→c,c→a（代表 a）：a→t, c→a, b→c, t→b。
临时名数 = 环数：破环必须先腾出至少一个节点（≥1）；而一个 t 在环内按序复用、同一时刻
只存放一个节点即足够（≤1），故每环恰 1 个。入链在环破解后按反向拓扑执行。

## 5. 临时名生成
候选形如 `.rename-tmp-<n>`，n 从 0 递增；校验候选不在当前 N、不等于任何请求的旧名或
新名，冲突即 n++ 重试。名字空间有限，必能找到；不会覆盖数据。

## 6. 复杂度
冲突检测与排序均为 O(R+N)：map 完成去重与存在性判定，Kahn 剥离每节点每边一次。
plan 包以非导出 lookups 计数 map 查找，断言 lookups <= 4*(R+涉及名字数)。

## 7. 并发、执行与撤销日志
- Namespace 持 RWMutex，Apply 持写锁执行；并发修改被阻塞，TryLock 可判定为拒绝。
- 撤销日志写临时目录：头部 `RLOG1`+uint32(步数)；每条记录为
  uint32(载荷长度)+JSON 载荷+uint32(CRC32-IEEE)，顺序追加。
- 注入第 k 步失败：已执行步骤按日志逆序回滚，N 逐元素复原。
- undo 解析「最大可恢复前缀」并逆序撤销；对同一日志再次撤销为无操作（幂等），
  并报告无法撤销的步骤。
- 截断/损坏分类（errors.Is）：ErrLogHeader 头部不完整；ErrLogRecord 记录长度或 JSON
  载荷字节不全；ErrLogCRC 记录完整但 CRC 不匹配（含字节损坏）。

## 8. 撤销完整性
成功执行后撤销，N 与执行前按集合逐元素相同（顺序无关）。
