# 分层时间轮设计推导

记 tick=T，当前时刻 now，截止时刻 D=now+d，目标时刻 to，tick 序号 `tick(t)=⌊t/T⌋`。

## 1. 落槽取整

不变量：触发时刻 F 满足 `D ≤ F < D+T`。定时器在 tick 序号 q 的槽里，只有推进到 tick 序号 ≥ q 才触发，槽 q 最早在时刻 q·T 被处理，故需 `q·T ≥ D`，即 `q ≥ ⌈D/T⌉`；取最小值 `q=⌈D/T⌉` 时触发窗口 `[q·T, (q+1)·T)` 恰落在 `[D, D+T)` 内，是唯一满足第 1 条的公式。

对比（T=10ms）：

| 公式 | now=13,d=10 (D=23) | now=13,d=7 (D=20) | now=10,d=10 (D=20) |
|---|---|---|---|
| A `⌊now/T⌋+⌈d/T⌉` | q=2, F∈[20,30) 早触发 ✗ | q=2, F∈[20,30) ✓ | q=2, F∈[20,30) ✓ |
| B `⌈(now+d)/T⌉`（采用） | q=3, F∈[30,40) ✓ | q=2, F∈[20,30) ✓ | q=2, F∈[20,30) ✓ |
| C `⌊(now+d)/T⌋+1` | q=3 ✓ | q=3, F∈[30,40) 晚 2 tick ✗ | q=3, 晚 2 tick ✗ |

A 在 `⌊now/T⌋·T + ⌈d/T⌉·T < D`（now、d 都带余数）时早触发；C 在 D 恰为 T 的倍数时多等一个 tick。`d≤0` 时 D≤now，不进轮，进立即队列，下一次 Advance 触发（第 2 条）。

## 2. 降级保序

层 l 的槽覆盖 tick 区间 `[s·w^l, (s+1)·w^l)`（w=每层槽数）。推进越过槽 s 时，槽内条目按 `⌈D/T⌉` 重新落槽：能进层 l-1 的立即进，否则留在原层。高层槽内条目的 D 彼此可差近 `w^l` 个 tick，**槽内插入顺序与 (D, 注册序) 无关**，因此「保持插入顺序」无法直接给出第 3 条。结论：同一批触发（一次 Advance 内到期的全部条目，含立即队列、底层槽、溢出堆）统一按 `(D, seq)` 稳定排序后依次触发，seq 为全局单调注册序号。于是「同 D 一个直接落低层、一个从高层降级」时按 seq 即 After 调用顺序触发，与经过几层无关；排序只作用于本次到期的 K 个条目，代价 O(K log K)，不计入槽访问。

## 3. 推进的跳跃

不逐 tick 走。每层维护「非空槽」有序集合（Go 用 `[]int` 有序槽号 + 二分，槽内条目带绝对槽号校验，过期槽号整槽跳过）。`Advance(to)`：令 `a=⌊to/T⌋`，对层 l 从 L-1 到 0，处理绝对槽号区间 `(base_l, ⌊a/w^l⌋]` 内的非空槽（base_l 为上次推进到的槽号）；层 0 的槽即到期，层 l>0 的槽按第 2 节降级（降入层 l-1 时若目标槽号 ≤ 层 l-1 本次边界，会被同一轮层 l-1 的扫描自然处理，无需递归）。超出最高层覆盖 `w^L` 的条目进小顶堆（按 ⌈D/T⌉），Advance 末尾弹出 ≤a 的重新落槽。每个条目每次 Advance 至多被移动一次（每被访问必降到更低层或触发），故**单次 Advance 访问槽数 ≤ L·w + 本次触发数**，与跳过的 tick 数无关。

## 4. 回调中新注册

设本次 Advance 目标 to。回调里 `After` 的新定时器若 `D ≤ to`：选「下次 Advance 触发」。论证：若选本次触发，则 d≤0 的回调可无限自我再注册（D=now≤to 恒成立），单次 Advance 永不终止，违反「不得无限循环」；且本次触发批次在进入回调前已按 (D,seq) 排定，中途插入会破坏第 3 条顺序。选「下次」时：D≤now 进立即队列、now<D≤to 正常落槽，两者都在**下一次** Advance 触发，触发时刻 F≥D 且 F<D+T（下一次 Advance 的目标 ≥ 槽号·T），第 1 条仍成立；d≤0 链每次 Advance 只消耗已排定的有限批次，必然终止。
