# DESIGN

## 1. 零计数 hunk 头行号

统一 diff 头 `@@ -a,b +c,d @@` 中 a/c 表示该 hunk 覆盖区间第一行的 1 基行号；计数为 0（纯插入/纯删除且区间为空）时，空区间无法取“第一行”，GNU 规则改为取该插入/删除点之前最后一个已存在行的行号，即“前一行行号”；插入点在第 a 行与第 a+1 行之间（0 基偏移 a 处）。
- 文件开头插入：没有前一行 → a=0，插入点 0 基偏移 0。样例 x→y,x（C=0）得 `-0,0 +1`。
- 第 2 行后插入：前一行是第 2 行 → a=2，插入点 0 基偏移 2。样例得 `-2,0 +3`。
- 删除整个文件：删除点之后再无行，c 取删除前最后行号；删到空时新文件为空 → c=0，删除覆盖旧第 1 行。样例 a→空（C=3）得 `-1 +0,0`。

Apply 解析：b=0 时不消费旧行，把 hunk 的插入行写入旧偏移 a 处；d=0 时从旧偏移 a 开始消费删除行、不产出新行。

| b | 书写 | Apply 解释 |
|---|------|-----------|
| 0 | `-a,0`，a=插入点前一行行号（可为0） | 不消费旧行；插入点 = 0 基偏移 a |
| 1 | `-a`（省略 ,1） | 消费旧文件第 a 行 1 行 |
| ≥2 | `-a,b` | 从第 a 行起消费 b 行（含上下文/删除） |

## 2. hunk 合并阈值

阈值为 **2C**：相邻两改动间未改动行数 g ≤ 2C 合并，g > 2C（即 ≥ 2C+1）分开。
每个 hunk 两侧最多各带 C 行上下文。若 g ≤ 2C，则两 hunk 上下文窗口在中间重叠或相接（前段尾 C 行 + 后段头 C 行 ≥ g），不存在“两窗口都不覆盖”的行，分开会产生重叠/相邻 hunk，故 GNU 合并；g = 2C+1 时两窗口间恰好剩 1 行双方都不覆盖，GNU 分开。C=0 时阈值 0：相邻改动之间至少有 1 行未改动即 g≥1>0，全部独立成 hunk。
反例：阈值取 C 时，C=3、g=4（≤6）的 GNU 单 hunk 会被错误拆开；取 2C+1 时，C=3、g=7（>6）GNU 已拆成两个 hunk，却会被错误合并。

## 3. Myers 并列最短路径选择

采用前向 Myers O(ND)（V[k]，按 D 递增），蛇形延伸含逐行比较，计数记入非导出计数器。
每条对角线 k 的前驱为 k-1（向下=插入）与 k+1（向右=删除）。并列规则固定为：k == -D 或（非 k==D 且 V[k-1] < V[k+1]）时取自 k-1（插入），否则取自 k+1（删除）——即两前驱 x 相等这一唯一并列情形下选择“向右=删除”，与 GNU diff 默认一致。
影响 `a b`→`b a`（D=2）：删除优先路径产出 `-a`、空格 b、`+a`（先删后插）；插入优先会产出 `+b`、空格 a、`-b`。本实现固定前者，重复 100 次逐字节一致。回溯时由 (x,y) 与前驱坐标比较判定该步是插入还是删除。

## 4. 偏移后后续 hunk 的查找起点

前一 hunk 以偏移 +k（实际应用位置相对其记录位置）成功后，后续 hunk 的查找窗口**整体平移 k**：记录起点 oldStart 改为 oldStart+k，半径仍为 F。
原因：偏移 k 说明目标文本相对补丁制作时的文本整体多出/少了 k 行（前序上下文变化所致），后续 hunk 面对的是同一份被位移后的文本，其真实位置同步位移；不平移会系统性偏离，按 hunk 长度平移则会越过真实位置。搜索仍在 [start+k-F, start+k+F] 内取与记录位置距离最近、同距取最前者。

## 5. 其他约定

- `lines`：按行切分保留原行尾（LF/CRLF/末尾无换行）；空输入为 0 行；Join 原样拼回。
- 无换行：文件末行无 LF 时，其内容行后输出 `\ No newline at end of file`；仅末尾换行变化（a ↔ a 加 LF）也产生补丁（内容行相同、改行尾）。
- 资源上限：补丁总字节数、hunk 数在解析期判定；超限即拒绝，零状态变化。
- 四类错误：ErrMalformed（格式，带补丁行号）、ErrContext（上下文不匹配）、ErrOffset（超出偏移范围）、ErrTooDifferent（差异过大），彼此可用 errors.Is 区分。
