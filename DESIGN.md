# 设计推导

规范化 = 核心 N*（行尾统一为 `\n`、删行尾空白、保留空行）后再施加末尾策略。

## 1. 空输入与末尾换行策略
`ToOut(ToOrig(o))==o` 要求每个输出字节都能在原文找到落点。EnsureOne 若对 `""`
补一个合成 `\n`，该字节没有任何原文字节可映射，互逆式失败；且 `N("\n")=="\n"`
也无法回退。故核心结果为空（空串或纯空白）时，三种策略一律输出 `""`；非空时
Keep=原样，EnsureOne=去掉末尾空行后补恰好一个 `\n`，TrimKeepOne=删全部末尾空行
再补一个 `\n`。N 后无 `\r`、无尾空白，故后两者是不动点。

| 输入 | Keep | EnsureOne | TrimKeepOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut
设输出边界 b 处为行尾 `\n`：前行保留内容止于 b-1，`\r` 与行尾空白被删。由单调
不减：被删 i 夹在 `ToOut(内容)=b-1` 与 `ToOut(\n)=b` 之间，只能取 b-1 或 b；两种
取法都不破坏互逆式。再由 `ToOrig(o)=min{i:ToOut(i)>=o}` 与编辑器直觉：光标在被删
行尾空白里，逻辑位置是“行内容末尾、换行符之前”，即输出偏移 b（`\n` 之前）。
故取右归并：被删字节映射到其后第一个保留字节的输出偏移；流末被删则映射到输出终点。
`\r\n` 的 `\r` 映射到该 `\n` 的偏移 b；`ToOrig(b)` 仍精确落在 `\n` 上（`\n` 是
该值最小的非删除来源），互逆与单调均成立。

## 3. 空白缓冲超限
选择拒绝。若“强制按非行尾输出”，之后若真出现行尾，输出会含行尾空白，违反语义
2/5；且在空白中间切分时提前量不同会产出不同结果，违反语义 4。拒绝则：待定空白
长度只取决于原文内容，超限点的原文偏移与切分无关，错误可判定、可复现；已输出
内容保留，实例进入终态，Map 仍可查，不违反 4/5/6 任何一条。

## 4. par 拼接修正
各段独立 N* 后，段间唯一不确定的是边界，且只需看 k 段尾部与 k+1 段首字节：
(a) k 尾 `\r` 且 k+1 首字节为 `\n`：独立处理出两个 `\n`，拼接时删前一个；
(b) k 尾部一串 `[ \t]` 且 k+1 首字节为 `\n` 或 `\r`（真正行尾落在下一段）：独立
处理保留了空白，拼接时把这串改为删除。其余首字节（普通字节、空白、空段）无需修正。
每条边界至多 (a)(b) 各一次编辑；`\r` 只回看 1 字节；空白串虽可能长，但作为一个
整体只在这一条边界被处理一次，每个原文字节至多参与一条边界，故修正量有上界。
span 坐标平移：段内 Seg 的 orig/out 起点各加“此前各段原始长度和 / 输出长度和”；
(a) 删输出令其后所有段输出偏移减 1，(b) 把保留段改删除段（区间数不变），在合并
后的有序表上做这两次常数规模编辑即可，结果与单流逐字节处理完全相同。
