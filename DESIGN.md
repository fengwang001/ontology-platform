# DESIGN

## 代理对错误偏移（语义 2）

`\uXXXX` 是 UTF-16 码元（code unit），不是码点。解码以码元为最小单位推进；
每个 `\uXXXX` 起点是唯一稳定锚点，与流式切分方式无关。规则：
- 高代理 D800–DBFF 之后，下一码元必须是低代理 DC00–DFFF；否则在**高代理起点**报错（EOF 亦然）。
- 低代理在没有待匹配高代理时出现，在**该低代理起点**报错。

故 `"\uD83D"`、`"\uD83Dx"`、`"\uD83DA"` 都指向 `\uD83D` 起点；`"\uDE00\uD83D"` 指向首个 `\uDE00`。
选高代理起点而非后续单元：根源是高代理"发出永远无法兑现的承诺"，且该偏移落在
已确定的字节上，两码元之间任意切分都不变。
`"\uD83DA"` 不能读作 U+FFFD+'A'：严格模式禁止静默替换；A 是普通 BMP 字符而非低代理，
无法配对，整个序列必须拒绝。

## 最小转义、单射与唯一性（语义 4）

RFC 8259 只要求转义 `"`、`\` 与未转义即非法的 C0 控制符 U+0000–U+001F。
U+001F 必须转义，因为未转义控制字符非法；U+007F 是 DEL，不属于 C0、可原样出现，故不转义。
规则：`"`、`\` 固定短形式；`\b\f\n\r\t` 用短形式；其余控制符用 `\u00XX`（小写）；
`/`、非 ASCII（含 U+2028/U+2029）、U+007F 一律原样。
唯一性：每个码点的输出确定（短形式唯一，非 ASCII 只有一种 UTF-8 编码）。
单射：反斜杠一定由编码产生，其后续形式的值只可能是引号、反斜杠或控制符，
这些字节绝不以原样形式出现，解码器可无歧义逐字节恢复，故不同输入不可能同输出。

## 测试对照

（最后补齐）
