# 设计推导

## 1. 零计数 hunk 头的行号（语义 4）

规则：`-a,b` 中 `a` 是该 hunk 覆盖范围**前一行的旧文件行号**（1 基），覆盖为空时即“上一条未被改动的上下文行”的行号；文件开头没有上一行时为 0。新侧同理用新文件行号。

- 在 `x\n` 前插入 `y\n`（C=0）：旧侧 0 行，其上一条上下文不存在，故 `-0,0`；新侧覆盖第 1 行，故 `+1`（省略 `,1`）。
- `a\nb\nc\n` 在第 3 行前插入 `X\n`（C=0）：旧侧空覆盖前有第 2 行，故 `-2,0`；新侧覆盖第 3 行，故 `+3`。
- 删除唯一行 `a\n`（C=3）：旧侧覆盖第 1 行 → `-1`；新侧空覆盖前没有行 → `+0,0`。

Apply 解释：`a` 是“锚点行”（其内容必须匹配），插入点在该行**之后**；`a=0` 时插入点在文件最前。

| 旧侧行数 | 头书写 | Apply 插入/匹配点 |
|---|---|---|
| b=0 | `-a,0` | 锚点为第 a 行，插入点在其后；a=0 → 文件开头 |
| b=1 | `-a`（省略 ,1） | 匹配第 a 行 |
| b≥2 | `-a,b` | 匹配第 a..a+b-1 行 |

## 2. hunk 合并阈值（语义 5）

GNU `diff -U C`：两段上下文各需 C 行；间隔 g 行未改动时，
- g ≤ 2C：两侧上下文需求重叠（间隔正好 2C 行时上下文恰好相邻），合并；
- g ≥ 2C+1：中间至少能留出 1 行不属于任一 hunk 的“安静行”，分开。

故阈值是 **2C**（g≤2C 合并，g≥2C+1 分开）。反例 C=1，改动行 1 与行 4 间隔 g=2：GNU 输出 `@@ -1,4 +1,4 @@` 单 hunk。若阈值取 C：g=2>C 会错误地拆成两个；取 2C+1：g=3 也会错误合并（而 GNU 在 g=3 时拆分为两个 3 行 hunk）。

## 3. 并列最短脚本的选择（语义 3）

Myers 同层沿 k 扫描时，插入来自 k+1、删除来自 k-1。本实现规定：**优先删除**，即取 `max(down, right)` 且两者相等时选择 down（删除）。效果：`a b → b a` 的脚本先删后插，hunk 内删除行整体排在插入行之前。此规则全局固定 ⇒ 输出逐字节确定。

## 4. 偏移后后续 hunk 的平移（语义 7）

第 i 个 hunk 记录起点为 h_i，实际在 h_i+d_i 处应用；后续 hunk 的查找中心 = 其记录起点 + Σ(ins_j−del_j)，其中求和包含此前所有已应用 hunk 的实际净行变化（含偏移 d_j 造成的位置差）。原因：补丁是单一最终文本的变换，前置 hunk 已改变后续内容的坐标；偏移本身也是坐标修正，同样必须传递。模糊窗口 F 只吸收“目标文本相对补丁基底”的残差，不重复吸收已应用 hunk 的确定位移。
