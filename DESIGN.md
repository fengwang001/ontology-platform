# 分片扇出部分失败合并器设计

## 1. 问题
把一个聚合查询扇出到 N 个分片，部分分片超时/出错/损坏/重复返回。
目标：合并五种聚合（Count、Sum、Min、Max、TopK），并对每个结果标注可信度。
只用标准库；分片由可注入接口模拟；状态仅在进程内存。

## 2. 数据模型
- `Record{ID string, V float64}`：分片返回的条目；缺字段视为零值。
- 分片以流的方式逐块返回，用于模拟「超时」「重复块」「损坏」。
- 每个分片还能独立提供自己的**上界** `Bound()`（本分片任意条目的最大可能得分），
  该值在查询失败时仍可取，用于 TopK 前缀证明。
- 分片 ID 可为空串，仍是合法 ID。

## 3. 五种聚合在部分失败下的推导

设成功分片贡献的集合为 S，失败（缺失）分片为 M，真实全集为 U = S ∪ M。

- **Count**：count(U) = count(S) + count(M) ≥ count(S)，且 count(M) ≥ 0。
  所以成功部分是**下界**：真实 Count ≥ 当前值，标注 `≥ v`；全成功为精确。
- **Sum**：对非负得分，sum(U) = sum(S) + sum(M) ≥ sum(S)。
  同理真实 Sum ≥ 当前值，标注 `≥ v`；全成功精确。
- **Min**：min(U) = min(min(S), min(M)) ≤ min(S)。
  所以真实 Min **不大于**当前值：标注 `≤ v`（单边界，方向与 Count 相反）。
  注意：缺失分片可能含更小值，绝不能说「≥」。
- **Max**：max(U) = max(max(S), max(M)) ≥ max(S)。
  所以真实 Max **不小于**当前值：标注 `≥ v`。
- **TopK**：见下。

### 3.1 TopK 可信前缀条件（完整论证）

记缺失分片上界之和 B = Σ_{m∈M} bound(m)，它是缺失分片**最多**能贡献的总分
（任何来自缺失分片的条目得分 x 都满足 x ≤ B：单条得分 ≤ 所在分片 bound ≤ B）。

当前（成功部分）排序后的第 j 名得分 c_j。考虑「当前前 j 名中的某条目 x，c_x = c_j」
能否被挤出最终 TopK：
- 缺失分片最多带来若干条目，每条得分 ≤ B。
- 若 c_x > B，则任何缺失条目都严格小于 x；x 至少排在所有缺失条目之前。
- 同时 x 在成功部分中位于前 j，因此全集中严格大于 x 的条目至多 j−1 个，
  x 必在最终 TopK 中。

因此，当前列表中所有「得分严格大于 B」的条目，其**入选** TopK 是确定的。
由于 TopK 按得分降序、ID 升序排列，满足条件的条目必构成列表的一个连续前缀
（得分不达标处之后的条目得分更小，也不可能达标）。

**可信前缀长度** p = #{当前条目 i : score_i > B}，截断到 K。
- p = K：当前整个 TopK 入选确定；但缺失分片可能在前缀内部插入同分条目，
  故标注「入选确定，排名可能变化」。
- p = 0：没有任何条目能保证入选（缺失分片上界很大时必然如此）。
全成功时 B = 0（无缺失分片），所有得分 > 0 者确定；结合无缺失事实，
直接标注精确，不降级。

## 4. 扇出与资源约束
- 容量为 C 的信号量（带缓冲 channel）保证同时在飞请求 ≤ C；
  非导出字段记录历史峰值（进入临界区时更新）。
- `context.WithTimeout` 给整体截止时间；截止后信号量获取处直接判超时，
  不再发起新请求；在飞请求通过 ctx 取消。
- 每个分片独立收集块；按 `(ID,V)` 去重，重复块不重复计数。
- 流结束时若去重后条数 != 分片声称条数，判该分片「损坏」，其数据不并入。

## 5. 故障与状态
分片状态枚举：OK / Timeout / Error / Corrupt。
- 永不返回 → ctx 截止 → Timeout；查询返回错误 → Error；
  声称条数不符 → Corrupt；重复块 → 去重后仍为 OK。
- 全部失败（或零分片）→ 返回可判定错误；
  全部分片成功但无数据（空结果）→ 合法零值结果，二者可区分。

## 6. 确定性与边界
- TopK 排序：得分降序，ID 升序打破并列，与到达顺序无关。
- 报告中缺失清单、每分片状态均按分片 ID 排序后输出，保证逐字节确定。
- K 大于条目数：返回全部条目；单分片正常工作；空 ID 合法。
- 四种故障用状态枚举 + sentinel error，可 `errors.Is` 区分。
