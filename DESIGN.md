# 1. 非法单元切分规则（Unicode 最大子部分）
续字节 C=80..BF；首字节声明长度 n。规则：
(a) 非法首字节(C0,C1,F5..FF,或孤立的 80..BF)吞 1 个。
(b) 从首字节起，只要后随字节仍是合法续字节、且未发现范围违规，就把它算作本序列的一部分。
(c) 位置 1（第二字节）非法（非续字节，或落入首字节禁止区间）：非法单元仅含首字节(1)，该字节回喂重处理。
    位置 ≥2 遇到非续字节：非法单元含此前全部已积累字节，该字节回喂重处理。
(d) 流结束时合法的未完成前缀算一个非法单元（截断另算）。
校验：F0 90 80 41→第三字节 80 在 F0 须 90..BF，回喂点 41 单字节并入，F0 90 80 合吞 1 单元，41 重处理。
E0 80 80：E0 第二字节须 A0..BF，80 触发，回喂剩 [80 80] 两字节各吞 1 → 3。ED A0 80 同理（ED 须 80..9F）。
C0 AF：C0 非法首吞 1，AF 再吞 1 → 2。F4 90…（F4 须 80..8F）→4。E2 82+EOF 是一个合法前缀→1（不是 2）。

第二字节合法区间表：
| 首字节 | 第二字节区间 | 第二字节非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1（仅首字节，回喂） |
| E0 | A0..BF | 1（回喂） |
| E1..EC | 80..BF | 1（回喂） |
| ED | 80..9F | 1（回喂） |
| EE..EF | 80..BF | 1（回喂） |
| F0 | 90..BF | 1（仅首字节，回喂） |
| F1..F3 | 80..BF | 1（仅首字节，回喂） |
| F4 | 80..8F | 1（仅首字节，回喂） |
| 80..BF | 无（独立非法） | 1 |
| C0 C1 F5..FF | 无（越界/非最短） | 1 |
第 3/4 字节一律要求 80..BF；位置≥2 遇非续字节时本序列整体结束（吞掉全部已积累字节），该字节回喂。

# 2. 缓存上限与即时判定
合法 UTF-8 最长 4 字节，缓存≤3；UTF-16 单元 2 字节、代理对 4 字节，缓存≤3。
判定时机：看到位置非法的续字节（类别/范围不对）立刻判非法并回喂，不等流结束；
只有“尚未看到终止字节”的前缀等 Close，此时它是合法前缀→截断，与非法字节错误区分。

# 3. par 切点对齐
段从 cut 起解析，但把起点回退到“最后一个可能在切点后仍开着单元”的位置：k 取 0..3 中第一个使
前缀为合法 UTF-8 前缀(或 UTF-16 偶数长度前缀)且不含非法终止的点；找不到则不回退。
窗口配置：仅输出起点偏移∈[cut,nextCut) 的单元（最后一段含 EOF）。重叠区 [cut-3,cut) 交下段重看；
跨切点单元的起点必落在这 3 字节内，故恰被一段认领。例 C3|80 80：k=1，段1从 C3 起认 C3 80 合法、
第二个 80 非法，与单流相同。重叠≤3 字节/切点，为常数。

# 4. 超限断点
Write 仅在完整单元输出后推进消费位置；下一单元会越限时输出 sentinel ErrLimit 及
LimitError{ResumeAt}，本次 n=0 且实例终态。ResumeAt=已完整输出部分之后偏移
（切分缓存中的字节未消费，仍属未完成单元；BOM 亦须完整）。新实例从 ResumeAt 续传，
拼接与无限流逐字节相同。

# 5. 检查次数
每字节进状态机计一次；回喂重处理≤1 次（最大子部分并入），总量≤2N，与切分无关。
par 每切点多重看≤3 字节，总量≤N+8·C（K≤8，C=6）。

# 6. 并发
单个 stream.Transcoder 非并发安全；par 的 K 个 goroutine 各持独立实例，无共享可变状态。
