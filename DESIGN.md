# DESIGN

## 1. 记录字节布局

所有整数用无符号小端 base-128 varint；类型标签在 varint 最低 3 位。

| 记录 | 标签 | 后续字段 |
|---|---|---|
| StreamHeader | magic+version | 固定 6 字节：`LZ77\x02\x01` |
| Literal | tag=0 | count(varint), 紧跟 count 个原文字节 |
| Match | tag=1 | distance-1(varint), maxLen-1(varint) |
| Flush | tag=2 | 无 |
| End | tag=3 | totalOriginal(varint), checksum(varint, IEEE CRC32) |

tag 与其它位共用一个 varint：低位是标签，高位是该记录的载荷值。

## 2. Write 切法无关（语义 3、4）

压缩器收到的数据先进入累积缓冲，不立刻输出。最小匹配需要 hash 的 3 字节，且一个位置的最长匹配只有在拿到其后的下一个未决字节时才确定：若当前匹配仍能延长，当前位置不是匹配终点。因此只处理“倒数第二个可用字节之前”的位置，保留至少 2 个尾字节；遇到匹配终止时才确定前一段字面量/匹配。任何 Write 切法得到相同的无限流前缀，所以压缩输出逐字节相同。Flush 强制把全部尾部按其最终匹配结果排空并写 Flush 记录，因而解码器能还原截至该点的全部输入；没有新输入时没有新记录，第二次 Flush 为空。Close 排空尾部后写 End。

## 3. 重叠回指（语义 2）

distance<length 时，被复制的“历史”后半段其实是同一条匹配刚输出的字节。一次性 `dst=src[:length]` 环形取片会反复读到旧数据或越界，只有按输出下标 `out[i]=out[i-distance]` 逐字节推进，或使用长度为 distance 的重复周期，才能得到正确的 RLE/周期展开。解码器维护环形窗口，逐字节从 `(write-distance)` 读出再写入；距离为 1 时即自复制。热路径上这只是一次有界环形读+写，复杂度 O(length)，无需更复杂的块展开。

## 4. 两类越界距离（语义 7、9）

“距离>已输出字节数”表示流引用了尚未产生的数据，在空输出开头就可能出现；“距离>窗口容量”表示即使流以后继续，引用也永远不可能被窗口保留。压缩器只从当前窗口已有位置选候选，因此 window>0 时绝不会产生超过容量的距离。并行块中，块 n 的窗口预置入上一块末尾 `windowCap` 字节。预置字节在该块编码前已进入窗口和解压端同一预置字典，它们逻辑上属于“此前已处理原文”，因此判定距离的分母仍是“字典长度+本块已输出”，含义未变；它不是当前解码器最终 Output 的字节，最终流的前一块输出保证这些字节真实存在。

## 5. 并行确定性（语义 9）

块 i 的压缩器只读取本块原文和上一块原文末尾窗口容量字节，输入完全确定；哈希链、候选上限、匹配长度规则都是纯函数，不读取共享状态或完成顺序。worker 仅负责返回该块的 token 编码，主 goroutine 按固定块序拼接，因此 1/2/4/8 workers 输出逐字节相同。流只写一次 Header；块之间不写 Flush：上一块末尾窗口通过预置字典成为下一块上下文，普通 Match 即可跨块；最后一块排空后写一次 End，CRC 与总长度由主 goroutine 汇总。

## 6. 复杂度

每个位置沿哈希链最多考察 `chainLimit` 个候选，计数器记录全部候选；因此总考察数 ≤ chainLimit×输入字节，与窗口大小无关。匹配最长 258 字节；全同输入每 258 字节产生一条 Match，4 MiB 输出远小于 64 KiB。

## 7. 并发与状态

单个 Encoder/Decoder 实例不要求并发安全；`CompressParallel` 的各 worker 使用独立 window/matcher，通过 channel 返回不可变块结果，主 goroutine 顺序消费。所有状态仅在进程内存。
