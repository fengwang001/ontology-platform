# DESIGN：半包续传 / 并行切分 CSV（RFC4180 方言）

包：`cell`→`lexer`→`table`、`writer`→`cell`、`par`→`lexer,table`，依赖单向。

## 1. 空行与单列空值的推导
记录边界 = LF（CRLF 归一为一个边界）。一行内一个字段事件都没有（两 LF 相邻、CRLFCRLF、开头空行）就是**空行，跳过**。
一行恰好一个字段：若该字段被引号写过（`""` 或任何引号字段），它是**一条单列记录**；否则 `a\n\nb` 里的空行无法与“单列空值记录”区分，二者是同一字节序列。
推论（第 5 条往返）：单列表中某记录唯一字段为空，回写**必须写成 `""`**；写裸空串会变成空行，再解析时被跳过，记录丢失。多列记录中的空字段无歧义，裸写。
规范输入：记录以 LF 结尾（末行也有 LF）；仅在必要时加引号；单列空值写 `""`；无被跳过的空行。对规范输入 `Write(Parse(x))==x`。

## 2. par 切点推导
切点可能落在引号字段、`""`、CRLF 中间，段起点词法状态未知。方案：第 0 段以“字段开始+引号外”启动；其余每段各跑**两遍**：
- H0 引号外：若切点前一字节是 `\r`，从 CR 待定态启动，否则从字段开始态；
- H1 引号内：从“引号字段中”启动（其前的 `"` 属上一段，不重复计）。
从左到右扫描：上一段被选中运行的**终态**决定下一段选哪个假设（引号内→H1，否则 H0）。两假设都消费整段同样字节，每字节至多两遍，与切点是否落在长引号字段无关，计数 ≤2N+O(K)。
拼接后字段偏移天然是全局坐标（Cell 存绝对偏移）；记录号按拼接顺序重排，错误记录号 = 出错段之前的完整记录数 + 段内序号。终态/错误逐段传播，与流式同一状态机同源，结果完全相同。

## 3. CR 待定态
未引号态见 `\r` 进 CR 待定，不产出、不计行尾；见 `\n` → 一个记录边界；见其它字节（含 EOF）→ 孤立 CR 错误，位置在该 CR。
半包：`\r` 后流暂停时状态保留，`Close()` 判孤立 CR；`\r\n` 被切两半时下一次 Feed 的 `\n` 归一成一边界。par：CR 待定只可能在引号外，作为 H0 起始态之一；H1 中 `\r` 只是内容，从无待定。

## 4. 上限“立刻”
字段字节计数：未引号态逐字节 +1；引号态对数据字节 +1，转义 `""` 在第二引号到达时 +1（挂起在 Q1，判定不晚于任何真实数据字节）。计数到达上限的当字节立即返回 `ErrFieldTooLarge`，不缓冲整记录。字段数在字段事件产生时 +1 立即检查；记录数在记录闭合时 +1 立即检查。错误后终态，后续 Feed 返回同一错误。

## 5. 状态转移表
状态：S0 字段开始/未引号中；Q 引号字段中；Q1 引号内刚见引号；CR 行尾CR待定。类别：逗号、引号、LF、CR、其它 O。

| 状态 | `,` | `"` | LF | CR | O | EOF |
|---|---|---|---|---|---|---|
| S0 | 发字段;留S0 | →Q | 发边界→S0 | →CR | 追加→S0 | 挂起字段;ok |
| Q | 追加 | →Q1 | 追加 | 追加 | 追加 | ErrUnclosedQuote |
| Q1 | 发字段→S0 | 追加引号→Q | 发字段+边界→S0 | 发字段→CR | ErrBareQuote | 发字段;ok |
| CR | ErrBareCR | ErrBareCR | 发边界→S0 | ErrBareCR | ErrBareCR | ErrBareCR |

四类错误：ErrBareQuote（未引号字段见 `"`；Q1 后遇非法字节）、ErrUnclosedQuote（EOF 于 Q）、ErrBareCR（CR 后非 LF）、ErrColumnCount（table 层）。EOF 于 S0 仅当本字节段曾产生字段内容/引号事件才挂起最后字段；纯空段不产生，故末尾 LF 不产生空记录、空行交由 table 识别跳过。

## 6. 并发
`par` 用 K 个 goroutine + WaitGroup，结果按段索引归并，无共享可变状态。单一流式解析器非并发安全。
