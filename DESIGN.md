# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 Encode(Decode(t)) == t（合法编码唯一）。

## 严格解码拒绝清单（由不变量逐条推导）

1. 显式次数 1（如 `1a`）：Decode 得 `a`，Encode 省略 1 得 `a` != `1a`。
2. 次数 0（如 `0a`）：游程语义要求次数 >=1；0 个符号无法解码，拒绝。
3. 前导零（如 `01a`、`00a`）：Encode 用无前导零十进制，重编码为 `a`/`0a`（后者本身又非法），必不等于原文。
4. 相邻同符号游程（如 `2a3a`）：Decode 得 5 个 a，Encode 作最长游程得 `5a` != `2a3a`。
5. `\` 后非数字非 `\`（如 `\x`）：编码端只对数字和 `\` 转义，该序列不可能由 Encode 产生。
6. 末尾孤立 `\`：缺少被转义字符，游程无符号，不是完整编码。
7. 末尾只有次数（如 `12`）：游程 = 次数 + 符号，缺符号，无法成游程。
8. 非法 UTF-8：合法输入经 range 产生的输出只含合法 UTF-8，非法序列不可能是 Encode 的结果；原样符号也必须是合法码点。

每类对应独立哨兵错误（见 rle 包），错误带字节偏移，便于彼此区分与定位。

## 大次数取舍

选择「支持到 int 上限（64 位平台即 MaxInt64），超出拒绝」。

- 理由：Decode 输出是 Go string，单个 string 与进程内存本就不可能容纳超过 MaxInt 字节；
  再大的次数即便接受也无法产出对应结果，没有可表达的输出。
- 数字逐位累加，溢出或超过 runs.MaxCount 即返回 ErrCountTooLarge（偏移指向次数首字节），
    绝不静默回绕。
- 解码不按次数预分配；输出按有界分块写出，累计输出字节超过可配置上限（默认 1<<30）
    即返回 ErrOutputLimitExceeded（偏移指向触发游程起点），故 `9999...a` 不会导致 OOM。

## 语义—测试对照表

（实现完成后补）
