# DESIGN

## 1. 空行与单列空值
记录以行尾为界。两个 `\n` 相邻时，中间那条记录没有任何分隔符，字段数为 1、值为空。
两种候选：跳过空行 / 作为一条记录。若跳过，则单列表里「`a\n\nb`」与「`a\nb`」不可区分，
且截断到「`a\n`」与「`a\n\n`」前缀性被破坏。故选：**空行 = 一条含 1 个未引号空字段的记录**；
仅流末尾最后一个换行不产生记录（「已开始」标志：见字段、逗号或换行后才有记录）。
回写：单列空值若不写成 `""`，输出只能是零字节，它与「无记录」不可区分，
故**单列表的空字段必须 `""`**（多列空字段写零字节，逗号本身已标记其存在）。
**规范输入**：仅用 `\n` 行尾、引号最小化（仅在含 `,` `"` `\r` `\n` 或单列空值时加引号）、
非空记录无尾随换行的文本；解析器对引号与 `\r\n` 原样保留，`Write` 不改变已显式表达的引号标记。

## 2. par 切点
切点可落在引号字段、`""`、`\r\n` 中间，worker 无法自证起始状态。
方案：**先单趟轻量定界扫描**（仅状态转移，4 状态，不缓冲、不报错，不记为处理次数），
得到每个切点前的起始状态（字段外 / 引号内 / 引号后 / CR 待定）；再把该状态作为
该段 worker 的初始状态并行跑完整 lexer（唯一一次完整处理）。因此每字节完整处理恰 1 次，
加上 O(n) 定界扫描，总量 ≤ 2n+O(K)，长引号字段不重扫。拼接：段尾未发字段（引号内或
CR 待定挂起）与下段首字段拼接字节、合并引号标记，偏移取首段起点；无挂起时直接衔接。
错误坐标：段内 record/field 号是相对段首所处记录的；全局 = 段首已完成记录数 + 段内数。
段首在引号字段中间时，该字段字段号 = 段首所在记录已发字段数 + 1。字节偏移直接加段起点，
不依赖段内重标号。终止错误只在最后一段按真实起始状态产生。

## 3. CR 待定
字段外或未引号字段末见 `\r`：不报错也不断行，进入 CR 待定，字段先不发出。
续传：下一字节 `\n` → 行成立，发字段与记录；其余字节 → 该 `\r` 是孤立 CR，
在其偏移处报 BareCR，随后字节正常处理（错误即终态，无歧义）。
流在 `\r` 处结束：同样是孤立 CR，`Close` 报 BareCR（不是行尾，故挂起字段仍属未完成记录）。
引号内 `\r` 永远是普通内容，不定待。par：定界扫描与 worker 同规则，切点正落在 `\r` 后时，
下一段以 CR 待定起始，`\n` 跨段同样成立。

## 4. 上限「立刻」
字段字节数按**解码后的内容字节**计数：引号内普通字节 +1；转义对 `""` 合计 +1（一个内容字节），
在第二个引号到达时入账，故超限在**第 max+1 个内容字节到达的那一刻**报错，错误偏移即该字节，
无需缓冲整字段。字段数：每发出一个字段后立即与上限比较，
第 max+1 个字段的起始字节（触发它的逗号偏移）即错误点。记录数：每完成一条记录
（换行字节处）立即比较，第 max+1 条的换行偏移即错误点。par 中 assembler 用同一时机检查，
故流式与并行错误点逐位相同。

## 5. 状态转移表（F=字段外 U=未引号中 Q=引号中 A=引号刚闭 C=CR待定）
| 状态 | `,` | `"` | `\n` | `\r` | 其他 | EOF |
|---|---|---|---|---|---|---|
| F | 发空字段→F | →Q | 发空字段+记录→F | 发空字段,→C | →U | 无记录则空 |
| U | 发字段→F | ErrQuote | 发字段+记录→F | 挂起→C | 留U | 发字段+记录 |
| Q | 留Q | →A | 留Q | 留Q | 留Q | ErrUnclosedQuote |
| A | →F(新字段) | 留Q(转义) | 发字段+记录→F | ErrTrailingQuote | ErrTrailingQuote | 发字段+记录 |
| C | (续字节)ErrBareCR | ErrBareCR | 行成立→F | (被替换) | ErrBareCR | ErrBareCR |

## 6. 并发
`par` K goroutine 仅经 channel/切片汇合，`-race` 下无共享写；流式 Lexer 单实例非并发安全，
多实例独立。错误为哨兵：`ErrBareQuote`(引号位置非法)、`ErrTrailingQuote`、
`ErrUnclosedQuote`、`ErrColumnMismatch`、`ErrFieldTooLong`、`ErrFieldCount`、
`ErrRecordCount`、`ErrClosed`，均包装带偏移/记录号/字段号的具名类型。
