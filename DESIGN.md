# DESIGN：流式行尾/空白规范化与双向偏移映射

## 1. 末尾换行策略：空输入与只含换行的输入

三种策略 `Keep`（保留原样）、`One`（恰好一个换行）、`Trim`（删末尾空行但保留一个换行）。
约定：空输入输出 `""`（不凭空造字节；`One("")` 也取 `""`，因为 `N("\n")="\n"` 已使
`""→"\n"` 与 `""→""` 都满足幂等，取后者可避免无原文可对应的合成字节）。
`One`：末尾 `\n` 数 ≥1 则截成 1；=0 且非空则补 1。`Trim`：输出全为换行则为 `""`；
否则截掉末尾换行后再保留恰好一个；内容后原本没有换行则补一个。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

幂等：输出只含 `\n` 且无行尾空白；再次规范化不删任何空白，换行数在三策略下均为不动点。

## 2. 被删字节的 ToOut

映射为左闭右开区间，段只有三类：等长保留段、删除段（有原长出、无输出长）、
等长重映射段（`\r→\n`、借用的空白字节→合成 `\n`）。
`ToOrig(o)` 对保留/重映射段做线性插值，端点取总长。`ToOut(i)` 用 `origEnd` 二分：
落在保留/重映射段内线性插值；落在删除段 `[a,b)→p` 上时取 `p`（该段的输出起点，
即“下一个存活字节的位置”）。例 `"a \n"`：空白 i=1 → o=1，即换行符之前；
`"a\r\n"` 中 `\r` 与 `\n` 的原文偏移都映射到同一个输出 `\n` 的位置，之后再递增。
编辑器把光标放在被删空白里时，落在**行尾换行符之前**（仍在该行），符合用户直觉。
此规则保证 `ToOrig`、`ToOut` 均单调不减，且 `ToOut(ToOrig(o))==o`。
`One`/`Trim` 对无空白又无换行的输入（如 `"a"`）必须合成一个 `\n`，此时不存在可借用的
被删字节，该合成字节映射到原文终点：`ToOrig` 仍单调，但这一个输出位置不满足互逆
（终点与合成点共享同一原文偏移），这是“输出比原文长”时不可避免的唯一例外，
有行尾空白可借用时无此例外。

## 3. 空白缓冲超限：选择拒绝

若强制把未决空白判为“非行尾”输出，后续真出现行尾时无法收回，直接违反语义 2、5；
拒绝则只影响调用方的后续写入，已输出内容与映射保持有效。故选择拒绝：
待判定空白串长度超过 `WSLimit` 时返回具名错误 `ErrWSLimit`（带当前原文偏移），
进入终态，缓冲区间起点可由状态解释；之后写入返回 `ErrClosed`。输出总长超 `OutLimit`
同理在真正写出前返回 `ErrOutLimit`。

## 4. par 的拼接与修正范围

每段用 Open 模式独立规范化：段末只可能挂两种未定状态——待定 `\r`（1 字节）或一段
未定空白（长度任意）。拼接时只看**相邻段交界的那一个字节**即可裁决左段：
左挂 `\r`：右段首字节是 `\n` 则删除该 `\r`（右段照常输出那一个 `\n`），否则 `\r→\n`；
左挂空白：右段首字节是 `\r/\n` 则删除整串，否则整串保留输出。末段按 EOF 裁决
（`\r→\n`、空白删除）。修正只需回看左段尾部的一次状态，不回溯段内，故修正范围有上界
（1 个 `\r` 或 1 串空白，且空白串本就在有界缓冲内）。各段子映射表的段坐标加
`(段原文起点, 段输出起点)` 平移后合并；末尾换行策略在合并后的整体输出/映射上执行一次，
因此切点落在 `\r|\n`、空白中间、空白与 `\r` 之间结果都与单流一致。
