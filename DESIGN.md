# 设计推导

## 1. 记录字节布局（wire）

整数一律用无符号 LEB128（varint）；校验和为 FNV-1a 64 位。

| 记录 | 布局 |
|---|---|
| 流头 | `0x4F 0x4E` ("ON")，varint 版本=1，varint 窗口容量，varint 链长上限 |
| 字面量段 | tag=0（独立 varint），varint n，随后 n 个明文字节 |
| 回指 | tag=1（独立 varint），varint 距离(distance-1)，varint 长度(length-1) |
| 刷新标记 | tag=2（独立 varint，无载荷） |
| 流尾 | tag=3（独立 varint），varint 原文总长，varint FNV-1a 校验和 |

tag 是独立的 1 字节 varint（取值 0..3，其余值判为非法记录）；记录边界即
varint 结束位置，故任意切分点都能安全续解析。

## 2. Write 切法无关性（语义 3）

`Write` 只把字节追加到未决缓冲 `pend`，不立刻出记录；匹配搜索在缓冲上运行，
采用贪心：从偏移 0 起，用 match 包取「从当前位置开始」的最长匹配，匹配则立刻
输出回指并前进，否则前进一字节、累积为字面量。关键：搜索时**允许匹配延伸到
pend 的最后一个字节**——因为“从位置 i 开始的最长匹配”只依赖位置 i 及之后的
原文，与将来还会不会再来字节无关（再来字节只会让 i 之前的字面量更早结束，不会
改变从 i 起的匹配结果）。于是一旦位置 i 已可检查，其编码就被永久确定，与 Write
切分无关；未决的仅是 pend 末尾尚未扫描的位置。`Flush`/`Close` 必须把 pend
全部扫描到末尾并强制结束当前记录（再写刷新标记/流尾），因为此刻保证不会再有
后续字节，匹配不能再跨过边界等待。

## 3. 重叠回指的解压复制（语义 2）

块复制在 distance<length 时会读到尚未被新数据覆盖的缓冲，得到错误结果。
本实现逐字节复制：`out=append(out, out[len(out)-dist])`。输出缓冲由 Go 切片
承载，append 摊还 O(1)，每字节一次索引+一次拷贝，O(length) 且无额外分配热路径；
总输出量受输出上限约束，故逐字节循环在复杂度上完全可接受。

## 4. 两种距离越界（语义 7）

distance 必须同时满足 `<=窗口容量` 与 `<=已输出字节数`。前者是格式/配置错误
（回指超出压缩端承诺的窗口，任何流都无法凭该配置满足），后者是时序错误（距离
合法但引用了比流起点更早、尚未存在的字节）。压缩端只在 window 中登记实际出现
过的字节，且 window 容量即头部声明值，故二者都不可能由本压缩器产生。并行块的
预置字典（上一块末尾至多窗口容量字节）在块压缩器初始化时就放入 window，因此
“可引用历史”的语义不变；字典不计入本块输出，解压器看到的仍是单一流，已输出
字节数自然包含字典对应的原文——“已输出”含义未变。

## 5. 并行块压缩的确定性（语义 9）

每块压缩器是无共享的独立状态机：输入只有「本块原文」+「预置字典」（上一块末尾
窗口容量字节），哈希链、字面量、回指全部由二者确定，与 goroutine 调度、完成
顺序、其他块无关；块大小固定使切块唯一，故 workers=1/2/4/8 输出逐字节相同。
衔接：非首块压缩器不写流头；除最后一块外，每块编码完自身全部输入后写一个刷新
标记（语义 4，保证按块可还原边界）；最后一块写流尾（总长、全流校验和）。结果
是普通单流，流式解压器无需感知分块。

## 6. 复杂度

哈希链每位置至多追踪链长上限个候选；计数器记录考察总数，故
`考察数 ≤ 链长上限 × 输入字节数`。全同字节链上候选长度单调不减，实际每位置
O(链长)，与窗口/输入规模无关。
