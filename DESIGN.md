# 只读快照一致性导出器 — 设计推导

## 1. 一致性的威胁与写时复制（COW）触发点

导出读取是惰性、分批进行的。若写入直接覆盖 `store`，那么导出晚些读到的键
会取到快照时刻之后的新值，最终文件横跨多个时刻，不再一致。

推导：快照句柄 `snap` 建立时记录版本水位 `v0`。此后任意 `Put(k,new)` 在覆盖
前必须先把 **v0 可见的旧值** 保存进 `snap.retained[k]`；删除同理保留墓碑。
读取键时按序：`retained[k]` 存在则用它，否则读 `store` 当前值。于是无论写入
发生在导出读取该键之前还是之后，该键呈现的永远是 v0 的值。
键集合同理：快照建立时记录 v0 存在的键集（惰性收集：当前键集 ∪ 被改写过的
保留键），新插入的键 `retained` 中无记录、当前键集会多出——需要排除 v0 后
新增键，因此写入新键时也要给存活快照登记「v0 不存在」墓碑。

内存开销 = O(导出期间被改写的不同键数)，与总键数无关。

## 2. 保留值何时释放：为什么按最老快照判断

重叠快照 s1(水位 v10) 与 s2(v20)。若只为最老快照 s1 保留 v10 旧值，
则 k 在 v25 再被改写时 s2 水位值（v15 的值）无人保留，s2 也会被污染。
所以正确规则是：每次写入前，store 遍历**全部存活快照**，把「该快照水位下
当前可见值」复制进该快照自己的保留表（每键只保留第一次，即最早前值；
v0 后新增键登记「不存在」墓碑）。各快照保留表互相独立。

由此自然得到按最老快照判断的释放下界：键 k 的 v10 旧值只可能被最老快照
需要；只要最老快照存活，覆盖链上它水位可见的值就必须仍被保留。快照
Close 只丢弃自己的保留表；两快照重叠时同键副本分处两表，必须两个都关闭、
两张表都清空，该旧值的保留计数才归零。注册表移除后，更晚的写入不再为其
复制任何内容。

## 3. 导出顺序无关性

清单总校验和定义为规范化内容哈希：所有 KV 按 key 字典序排列，依次喂入
`crc32.IEEE`（与块切分、遍历顺序均无关）。因此字典序、逆序或任意顺序导出，
块的字节切分不同，但清单总校验和必然相同；逐键比对规范化序列亦相同。

## 4. 文件格式与续传

单文件，块依次排列，末尾追加清单尾：
- 块头 16B：magic `EBLC`(4) + index uint32 BE + payloadLen uint32 BE +
  crc32(header|payload) uint32 BE；后接 payload。
- payload = 若干记录：klen uint32 BE + vlen uint32 BE(0xFFFFFFFF 表墓碑)
  + key + value。
- 清单尾：`EMNF` + uint32 长度 + JSON{Version, ChunkSize, NChunks,
  ChunkCRCs[], TotalCRC}。

导出前清单未知 NChunks，故清单在全部块写完后落盘。续传时扫描已有块：
块 index 连续、CRC 正确、payload 记录可解码，则保留至最后一个完整块，
从其后继续；半截尾块截掉重写。块大小为 0 返回 `ErrInvalidChunkSize`。

## 5. 四类可判定错误（errors.Is）

- `ErrManifestIncomplete`：尾部清单缺失/被截断；
- `ErrHeaderIncomplete`：某块头不足 16B 或 magic 错；
- `ErrBodyIncomplete`：头声明的 payload 不足或记录跨块被截；
- `ErrCRC`：头/体在场但 CRC32 不符。
另有 `ErrChunkOutOfOrder`（含错位号）、`ErrChunkMissing`（含缺号）、
`ErrSnapshotClosed`（快照已过期）。
截断分析：清单尾被切掉即清单不完整；清单完整但文件在块中间结束，按切点
落在头区/体区分类；CRC 不符只可能由内容损坏产生（纯截断不会造成 CRC 不符，
用翻转字节构造一例）。

## 6. 快照过期为何必须拒绝续传

续传继续写入的块必须仍取自 v0。快照 Close 后 retained 已释放，再读只能得到
当前 store（混合时刻），拼出的文件前半 v0、后半 vNow，总校验和即使偶然可
伪造，语义上也不一致。因此 Resume 首先校验快照存活，否则返回
`ErrSnapshotClosed`，且不触碰已导出文件（已导出部分原样保留）。

## 7. 并发与资源

store 用 RWMutex；snapshot.retained 用 RWMutex，关闭置 closed 并等待在途读
导出（WaitGroup）后完成，杜绝关闭与读取竞争产出半截文件。导出读取计数在
snapshot 内（Get 一次记一次，恰为键数）。块内存峰值统计每次缓冲分配的最大值，
断言 ≤ 一块大小。`-race` 下并发 4 导出互不干扰。
