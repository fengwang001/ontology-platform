# 带转义文本 RLE：规范形式推导

不变量：严格解码器接受的任意 t 必须满足 `Encode(Decode(t)) == t`。

## 拒绝清单（逐条推导）

1. 显式次数 1（`1a`）：规范编码对 1 次游程省略次数，Encode 输出 `a` != `1a`。
2. 次数 0（`0a`）：游程长度至少 1，0 不表示任何合法游程，编码器永不产生。
3. 前导零（`01a`、`00a`）：规范十进制无前导零；且解码后 Encode 得 `a`，不等于输入。
4. 相邻同符号游程（`2a3a`）：Encode 按最长游程切分，应合并为 `5a`。
5. `\x` 中 x 非数字非 `\`：合法符号只有数字或 `\` 需要转义，其他码点按原样写出，Encode 会去掉该反斜杠。
6. 末尾孤立 `\`：其后缺少被转义字符，无法构成游程。
7. 末尾只有次数（`12`）：游程必须以符号收尾，次数后无符号则不完整。
8. 非法 UTF-8：解码输出是 Go string；原样字节经 Encode 会被当作 RuneError 重编码，字节级往返不成立。

各类错误分别用哨兵错误表示，并由携带字节偏移的错误类型包装，彼此可判定、可定位。

## 大次数取舍

选择**支持**：次数按十进制逐位累加进 `math/big.Int`，永不溢出；默认输出上限
`DefaultMaxOutput = 1<<30`，`Decoder.MaxOutput` 可配置（0 表示不限）。输出按小块
（每块 64 KiB）写出并在每块前检查剩余配额，绝不预分配与次数成正比的内存，超限返回
`ErrOutputLimit`。选择上限值的理由：1 GiB 足以覆盖正常文本，同时让恶意输入在占用
可控内存前被拒绝。

## 语义—测试对照表

（最后补齐）
