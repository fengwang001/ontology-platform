# 设计推导

## 续行的下一物理行只含空白（语义 3）

依据 `java.util.Properties` 的 `LineReader.readLine()`：

1. 行尾为奇数个 `\` 时，末尾 `\` 被移除，并置 `skipWhiteSpace=true`，
   下一物理行开头的空格 / `\t` / `\f` 全部跳过，随后才追加到 `lineBuf`。
2. 若下一物理行整行为空或只有空白，跳过前导空白后没有任何字节进入 `lineBuf`；
   到达该行行尾时按普通行尾处理——直接返回此前累积的逻辑行，不产生空值、不另起属性。
3. 退化情形：若该空白行自身又以奇数个 `\` 结尾，则续行链继续延伸到再下一行。

结论：空白续行被"吸收"，对逻辑行贡献为空串。它与"续行首部空白被剥掉"是同一条规则：
剥掉前导空白后剩下空串，拼接空串等于不拼接。因此 `k=v\` + `\n   ` => `k` -> `v`。
另注意注释只在逻辑行首判定一次，故续行里的 `#`、`!` 是普通字符。

## Store 只转义必要字符（语义 6）

以"Load(Store(m)) == m 且不引入多余反斜杠"为约束，逐字符对照 Load 规则：

- 键：行首空白会被忽略，所以前导空格须 `\ `；未转义的空格 / `\t` / `\f` / `=` / `:`
  是分隔符，故键中这些字符全部转义；键首字符为 `#` 或 `!` 会使整行变注释，仅该位置须转义；
  `\n` `\r` 会截断行、`\` 会吞掉下一字符，必须转义。
- 值：分隔符定位完成后，从首个非空白字符起的全部字节（含尾部空白）原样保留到行尾。
  因此值中间与末尾的空格、以及任意位置的 `# ! = :` 都不必转义；
  但值开头的空格 / `\t` / `\f` 会被"分隔符后空白吃掉"规则吞掉，必须转义（只转义第一个，
  反斜杠本身即成为首个非空白字符）；`\n` `\r` 与 `\` 同键，必须转义。
- 以 `#`/`!` 开头的是键才危险，因为注释判定只发生在逻辑行首；值永远位于分隔符之后，
  不可能处于行首位置，故值无需转义它们。
- 非 ASCII 字符不触发上述任何规则，按 UTF-8 原样输出即可。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | TestSeparators（表驱动） |
| 2 注释 | TestComments（表驱动） |
| 3 续行（含空白续行） | TestContinuation（表驱动） |
| 4 转义与 \\u 行列号 | TestEscapes / TestUnicodeError |
| 5 重复键覆盖与首次序 | TestDuplicateKeys |
| 6 Store 必要转义 | TestStoreEscape / TestRoundTrip |
| 7 千组随机往返 | TestRoundTrip（循环 1000 组） |
| 计数器 ≤2N | logical.TestCounterBound |
