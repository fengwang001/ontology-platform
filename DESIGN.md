# CSV 方言解析器设计（module ontology）

状态：S0 字段起点；S1 未引号字段中；S2 引号字段中；S3 引号内刚见引号（闭合或转移待定）；S4 行尾 CR 待定。

## 状态转移表（输入类别：`,` `\n` `\r` `"` 其他）

| 状态 | `,` | `\n` | `\r` | `"` | 其他 |
|---|---|---|---|---|---|
| S0 | 发空字段→S0 | 关记录(无悬挂则跳过空行)→S0 | →S4 | 开引号字段(Start=本偏移)→S2 | 置值→S1 |
| S1 | 发字段→S0 | 发字段+关记录→S0 | →S4 | **ErrBareQuote** | 追加→S1 |
| S2 | 追加`,`→S2 | 追加`\n`→S2 | 追加`\r`→S2 | →S3 | 追加→S2 |
| S3 | 发字段→S0 | 发字段+关记录→S0 | →S4 | 追加`"`→S2 | **ErrTrailing** |
| S4 | **ErrLoneCR** | 关记录(无悬挂跳过)→S0 | **ErrLoneCR** | **ErrLoneCR** | **ErrLoneCR** |

EOF：S0 正常（无悬挂）；S1/S3 发悬挂字段并关记录；S4 报 ErrLoneCR；S2 报 ErrUnclosed。字段/记录上限、字节计数在“追加/发字段”动作点立即执行。

## 推导一：空行与单列空值

不变量要求“末尾换行不产生额外空记录”，而 `a\n\nb\n` 中间的空行若算记录无法与“最后换行”区分语义。故选择：**空行一律跳过**（S0 见到 `\n` 且无悬挂字段；`\r\n` 同理）。于是单列记录 `x\n` 只有一条记录；单列空值只能来自 `""`。回写时单列表的空字段若不加引号写成空串，重新解析得到零条记录（被当空行跳过），数据丢失；因此 writer 规则之一：**单列表中唯一字段值为空串时必须写 `""`**。多列中的未引号空字段照常写空串（`,` 已提供结构，不会丢）。解析器产出的引号标记须保留：`""` 回写仍为 `""`。

## 推导二：par 切点

切点可落在任意字节，段 worker 仅凭本段无法知道入口状态。做法：先做一遍**串行分类扫描**（一个只跟踪状态的 lexer，不攒值），在每个切点记录入口 Seed{state, recNo, fieldNo, pendingStart, quoted 标记}；随后 K 个 goroutine 各带 Seed 解析自己的段（S2/S3 时携带前置悬挂值用于长度判断），从左到右拼接事件。分类扫描每字节处理 1 次、K 个分段解析合计 N 次，总处理次数 ≤ N + N + 常量 = 2N+常量，与长引号字段无关，无回扫。段内事件坐标是段内偏移，拼接时统一加段起点；记录号加 Seed 中的已完成记录数。段内错误同理换算成全局字节偏移/记录号/字段号，与流式位置一致（字节偏移从 0、记录字段从 1）。

## 推导三：CR 待定

未引号字段遇 `\r`：可能是 `\r\n` 行尾，也可能是非法孤立 `\r`，必须进入 S4 等下一字节。半包续传时 S4 是普通持久状态，可跨 `Feed` 暂停；下一字节为 `\n` 则按行尾处理，否则（含流结束）报 ErrLoneCR。引号内不产生 S4，`\r` 直接成为内容且原样保留。par 切点在 `\r\n` 之间时，分类扫描把入口 Seed.state=S4 传给下一段，段首字节决定行尾或错误；切点恰在段末时 S4 作为出口 Seed 继续向右传递。

## 推导四：上限“立刻”

引号内 `""` 代表一个引号字符，因此转义只在 S3 见 `"` 回到 S2 时计 **1** 字节（非 2）。字段长度计数点 = 每次向当前字段追加内容（S1 的其他字节；S2 的逗号/LF/CR/其他各 1；S3→S2 的转义引号 1），在追加**之前**与 MaxFieldBytes 比较，第一个超限字节到达即 ErrFieldTooLarge，不缓冲整字段。字段数在每次发出字段时比较 MaxFields；记录数在每次关记录时比较 MaxRecords。错误后 parser 进入终态，永久缓存该 error。

## 规范输入（canonical）

writer 输出：记录间 `\n`，无尾随换行；空输入写空串。字段在下列任一情形加引号：值含 `,` `"` `\r` `\n`；值为空且该记录只有 1 个字段；原解析标记为加引号（如 `""`）。引号转义为 `""`。此集合外的串不被加引号，故规范输入回写逐字节不变。

## 备注

单个流式 Parser 实例非并发安全；独立实例互不共享状态。`lexer`/`par` 各持有非导出计数器 `n`，经 `BytesProcessed()` 读取。
