# 设计推导

## 1. 零计数时 hunk 头的行号

统一格式头 `@@ -a,b +c,d @@` 中 `a,c` 是 1 基的起始行，`b,d` 是计数；
删除/替换时该块在旧文件占第 `a..a+b-1` 行。纯插入（`b=0`）旧文件里
没有行可编号，GNU 取「插入点前一行」的行号：插入点位于旧行 `a` 之后、
行 `a+1` 之前。

- 文件头插入（前 0 行）：前一行不存在，行号记 0，即 `-0,0`。
- 插在第 2 行后：前一行是第 2 行，即 `-2,0`（样例 a,b,c→a,b,X,c）。
- 删除整文件（`d=0`）：新文件没有行，最后一个旧行号为 1，写 `-1`，
  写 `+0,0`。

Apply 解释：`b=0` 时把插入点放在旧文件下标（0 基）`a` 处，即新行插在
旧行 `a-1` 与旧行 `a` 之间；`a=0` 表示插到开头。`d=0` 同理删除点位于
新文件下标 `c` 处。

| 情形 | 书写 | 起始 a | Apply 插入/删除点（0 基下标） |
|---|---|---|---|
| b=0（纯插入） | `-a,0`（a 可为 0） | 前一行行号 | 在旧行下标 a 之前插入 |
| b=1 | `-a`（省略 ,1） | 该行行号 | 从旧行下标 a-1 起匹配 1 行 |
| b≥2 | `-a,b` | 首行行号 | 从旧行下标 a-1 起匹配 b 行 |

## 2. hunk 合并阈值

相邻两段改动之间有 g 行未改动行。前一段的尾部上下文最多取 C 行，后一段
的头部上下文最多取 C 行。两段上下文不重叠（即 g > 2C）时分两个 hunk；
g ≤ 2C 时两段上下文相接或重叠，合并成一个 hunk。故阈值为 **2C**。

例（C=3）：g=6 时分两个 hunk，各带 3 行上下文；若阈值取 C，则 g=4
也会分开，与 GNU 输出的单个 hunk 不符；若取 2C+1，则 g=7 被合并而
上下文会需要 4 行，超出 -U 3。g=2C 合并、g=2C+1 分开，与 GNU 一致。

## 3. 并列最短脚本的选择

Myers 前向 V 算法在同一条对角线上选 k 时，统一规则：`k == -d` 或
（`k != d` 且 `V[k-1] < V[k+1]`）时从 k-1 来，即**向下（删除）优先**，
否则向右（插入）。因此 `a b → b a` 的脚本是先删 `a`、再插 `a`：
`-a` ` b` `+a`，hunk 内 `-` 行排在对应 `+` 行之前。该规则对同一输入
恒定，补丁文本逐字节确定。

## 4. 偏移后后续 hunk 的平移

hunk i 记录的旧起始为 r_i，实际匹配位置为 p_i，偏移 o_i = p_i - r_i。
该 hunk 应用后文本的累计漂移 delta（插入行数−删除行数）只影响之后的
新文件行号；但旧文件侧后续 hunk 的预期记录位置 r_{i+1} 与 hunk i 位于
同一（旧）坐标系，因此其查找中心平移 **o_i**（不是 delta）：
中心 = r_{i+1} + o_i，在 ±F 行内搜索。原因：匹配只依赖旧文本，偏移是
由上下文定位误差造成的坐标系位移，会原样传递给后续 hunk。

## 复杂度

edit 用 Myers O(ND)：计数器 steps 每走一步横/纵边 +1，snake 每行
比较 +1。D≤上限时 steps ≤ (2D+1)(D+1)/2 + 每次 snake ≤ M+N 的推进量，
整体上界 4(N+M)(D+1) 由测试钉住。
