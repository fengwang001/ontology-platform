# DESIGN

## 关键推导

1. **Write 切法无关**：压缩器只在看到后续失配字节时提交记录。当前最长匹配若抵达已知缓冲区末尾，就继续缓存；下一批数据只会延长它或由下一个字节证明其结束。无匹配起点不会因未来历史而变成有匹配，因为候选只来自该起点之前。因此已提交记录只取决于累计字节流。`Flush` 强制把末尾匹配视为结束并写刷新标记；标记只承诺解码可见性，不丢弃窗口。
2. **重叠回指**：距离为 `d`、长度为 `l` 且 `l>d` 时，后段要复制的内容本身尚不存在；必须每复制一个字节就让它进入历史，再复制下一个字节。块复制会读到陈旧缓冲。实现直接逐字节 `for`；解压端每字节还有限额、窗口和校验职责，此循环不会成为额外算法复杂度。
3. **两类距离错误**：距离为 0 无意义。`距离>窗口容量` 违反编码器永不引用容量外历史的不变量；`距离<=容量但>已输出字节数` 说明记录引用了尚不存在的位置，故必须分开。压缩器只插入、查找当前位置之前的位置，不可能产生超容量或超当前长度的距离。并行块把上一块尾部原文预置进匹配器窗口；解压器不重置，那些字节本就是此前已输出历史，所以“已输出”含义不变。
4. **并行确定性**：块 `i` 的候选只在“本块原文 + 上一块原文末尾窗口容量”内，哈希链结果不读取其他块，也不读取 goroutine 完成顺序；压缩函数是纯函数。按固定块顺序拼接记录即可，块间不加刷新标记，解码器窗口自然延续；仅流首写头、流尾写一次总长度和校验和。

## 字节布局

| 记录 | 布局 |
| --- | --- |
| 流头 | 魔数 `LZ77O`，版本 `1` |
| 字面量 | `uvarint(0)` + `uvarint(长度)` + 原始字节 |
| 回指 | `uvarint(1)` + `uvarint(距离)` + `uvarint(长度)` |
| 刷新 | `uvarint(2)` |
| 流尾 | `uvarint(3)` + `uvarint(原文总长)` + `uvarint(CRC32-IEEE)` |

所有整数为无符号 LEB128；读取器在第 10 字节后仍未终止或值溢出 `uint64` 时拒绝。
