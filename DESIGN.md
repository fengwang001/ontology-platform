# 只读快照一致性导出器设计

## 1. 版本存储与快照水位
`store` 中每个键保存 `(value, version)`，版本号单调递增；`Put` 用一把锁串行化版本推进。
`snapshot.Snapshot(View)` 记录创建时的版本水位 `base`：快照只能看到 `version <= base` 的值。

## 2. 写时复制的触发点（为什么导出中写不污染快照）
关键推导：导出是懒遍历，快照创建时**不**复制全量数据（否则内存与总键数成正比）。
因此每个在版本 `v > base` 到达的 `Put(k)`，在覆盖 store 当前值之前，必须先判断：
store 中现存的 `(old, version <= base)` 正是快照 `base` 时刻可见的值。
于是把 `k -> old` 复制进该快照私有的保留表，再覆盖 store。
快照读取 `k` 时先查保留表，命中则返回旧值，否则读 store；这保证导出进行到任意位置，
被延迟读取的键仍是快照时刻的值（测试：导出到第 50 键时改写第 80 键，结果仍是旧值）。
保留表是每个快照私有的：两个重叠快照水位不同，各自需要的旧值不同，不能共用。

## 3. 保留值何时释放（为什么按最老快照判断）
`Put` 只需判断「最老活跃快照」：若其水位 `< 新版本`，则旧值对它（以及更新的活跃快照）
可能可见，需保留；若一个键已在某快照保留表中，后续 Put 不覆盖该条目（保留水位前最后旧值）。
关闭某快照即丢弃其私有保留表。两个重叠快照各自持有独立条目，
只有**两个都关闭**后，该键为快照保留的内存才全部归零。
开销只与「导出期间被改写的不同键数」成正比，与总键数无关。
存在与否用存在位表达；空值/空键均为合法数据。

## 4. 关闭与导出的竞争
快照用 `sync.WaitGroup` 记录在途导出，开始 `Add`、结束 `Done`；`Close` 先标记过期再 `Wait`。
续传必须重新持有同一快照：若已 `Close`，返回 `ErrSnapshotExpired` 且**不触碰**已导出文件，
避免用当前数据拼出跨时刻文件。要么导出完整结束，要么得到可判定错误，不会把半截文件误认完整。

## 5. 分块文件格式（单文件，块自描述）
文件 = 清单区 + 若干块。清单区：`"ONEX"` + 4 字节清单区长度，随后定长、零填充的 JSON 清单。
每块定长头 `block(u32) dataLen(u32) dataCRC(u32) headerCRC(u32)`，随后 `dataLen` 字节负载与 4 字节 `CRC32(头+负载)`。
负载为定长记录 `klen(u32) vlen(u32) key value`；块只在记录边界切分，故一块驻留 ≤ 一块大小。
清单含版本水位、块大小、每块 `offset/length/crc`、已完成块数与最终 SHA-256（完成前为空）。

## 6. 续传
清单头给出每块 `offset/length`，`Done` 记录已完整落盘块数。续传从 `Done` 起，
按相同记录划分重写后续块（幂等、可覆盖半截块），全部完成后回填 SHA-256。
每个键在一次导出中只经快照读取一次（计数器据此断言）。

## 7. 导出顺序无关性
最终 SHA-256 在校验阶段对「按键排序后的定长记录」规范化计算，与导出遍历顺序
（字典序/逆序）无关，故两种顺序总校验和相同、内容集合相同。

## 8. 校验与四类截断
校验清单与每块头/体/CRC，再核对块号连续、偏移一致与最终 SHA-256。
逐字节截断 1..len-1 判定为：清单不完整 / 块头不完整 / 块体不完整 / CRC 不匹配；
其中块尾 1..3 字节截断落在 CRC 拖车，归为 CRC 不匹配。乱序/缺失按清单逐块定位具体块号。
错误用哨兵值，`errors.Is` 可区分；`blockSize==0` 返回 `ErrInvalidBlockSize`。

## 9. 边界
空存储（0 块，仅清单，SHA-256 为空记录规范化值）、单键、块大于总量（单块）、
全键改写（保留值=键数）、空键、空值均覆盖；并发依赖锁与每快照私有表，`-race` 干净。
