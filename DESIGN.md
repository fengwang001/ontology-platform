# DESIGN

## 1. 空行与单列空值
零字节行（两个记录结束符之间没有任何字节，`\n\n` 或 `\r\n\r\n`，含文件开头）**跳过**，不产生记录。
因此单列空记录无法用裸空表示（会被当成空行丢掉），规范写法必须是 `""`。
多列中的空字段由逗号定位，可裸写（`a,,b`）。
**规范输入**定义：无被跳过的空行；引号遵循最小必要；单列空记录写 `""`；每条记录以 `\n` 结束；引号字段内字节原样。
回写器加引号当且仅当：值含 `,` `"` `\r` `\n`，或单列记录中的空值，或该 Cell 本就带引号标记
（最后一条保证任意 T 的引号标记往返；规范输入中它与「必要」恰好重合，故逐字节往返）。

## 2. par 切点
每段在两种入口假设下各跑一遍同一状态机：F=字段边界外（fieldStart），Q=引号字段内（quoted）。
K 段共处理 ≤2N 字节，无回扫。实际入口只有五种：fresh、openUnquoted、quoted、quoteSeen、crPending。
- fresh→F；quoted→Q。
- quoteSeen→Q：在事件流意义下两状态等价（quoted 遇 `"` 得 quoteSeen 不产出；quoteSeen 遇 `"` 产出一个 `"`；
  对任意引号串，成对消费后产出与终态相同；遇分隔符两者都闭合字段；遇其他字节都在同一偏移报 TextAfterQuote）。
- openUnquoted→F，仅首字节需修补：首字节是 `"` 立即报 QuoteInUnquoted；首字节是行结束且跨段字段已有内容，
  把 F 的 blankLine 替换为「闭合字段+记录结束」；其余字节两状态完全一致。
- crPending→F，仅首字节需修补：`\n` 且字段非空时同上替换；其它字节立即在 CR 偏移报 LoneCR。
段从左到右串行裁决入口，故拼接 == 单线程（同一确定性状态机、同一字节序列、同一入口状态）。
错误位置：事件按段内字节下标标注，播放时加段起点 lo；记录号/字段号由播放计数器（已结束记录数+1、本记录已结束字段数+1）现场给出。

## 3. CR 待定
未引号态遇 `\r` 不入值，记 crPending 与 CR 偏移。下一字节：`\n`→记录结束；其它（含 EOF、`"`）→在 CR 偏移报 LoneCR。
半包：状态跨 Feed 保留即可。par：切点恰在 CR 后时即上文 crPending 修补；流结束仍 crPending 按 EOF 分支报错。
引号态 `\r` 是普通内容。

## 4. 上限「立刻」
字段配额按**解码后的值字节**计数：普通字节 +1；引号态 `""` 在第二个引号到达时 +1（此时字符才成形，
故第一个超限字节到达即拒，不缓冲整字段）。每次 +1 前检查 MaxFieldBytes；字段数在字段闭合事件时检查
MaxFields；记录数在记录闭合事件时检查 MaxRecords。触发即终态，错误被粘住。

## 5. 状态转移表（S=字段开始 U=未引号中 Q=引号中 Q'=引号后 P=CR待定）
| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| S | 结束空字段/开新字段,S | 开引号字段,Q | 空行:跳过,S | P(偏移记下) | 值+1,U |
| U | 结束字段/开新字段,S | 错 QuoteInUnquoted | 结束记录,S | P | 值+1,U |
| Q | 值+`,` Q | Q'(不产出) | 值+`\n` Q | 值+`\r` Q | 值+1,Q |
| Q' | 结束字段/开新字段,S | 值+`"` Q | 结束记录,S | P | 错 TextAfterQuote |
| P | 错 LoneCR(CR偏移) | 错 LoneCR(CR偏移) | 结束记录,S | 错 LoneCR(CR偏移) | 错 LoneCR(CR偏移) |

P 只有紧跟 `\n` 才成行尾；其它任何字节（含逗号、EOF）都在 CR 偏移报 LoneCR。
EOF：Q/Q' 错 UnclosedQuote；P 错 LoneCR；其余 flush 当前记录（无内容且无字段则无记录）。
