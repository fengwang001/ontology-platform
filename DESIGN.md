# 带溢出的哈希聚合器 — 设计推导

## 1. 溢出什么：原始行 vs 部分聚合状态

内存哈希表 `key -> acc.State` 超预算时，两种方案：

- **溢出原始行**：回读时要重新逐行聚合，回读阶段的内存峰值仍是该分区的
  全部分组状态；且每行携带完整键，写盘字节数随输入行数线性增长，
  高基数场景下 I/O 量 = 输入量。
- **溢出部分聚合状态**：每个键只写一条 `(key, Count, Sum, Min, Max)`，
  写盘字节数随「不同键数」增长；同一键的百万行在内存里已压成一条。
  回读时只需与后续同键状态做 O(1) 合并。

**选择：溢出部分聚合状态。** 理由：① 聚合的本质是折叠（fold），中间态
对同键任意多行具有压缩性，溢出它不改变渐近内存，只付出一次顺序写；
② 溢出原始行在「同一分区被反复溢出」时会把同一批行写盘多次，I/O 随
溢出次数放大，而部分状态每次只写当前驻留的增量折叠结果，总量仍受
「键数 × 溢出次数」约束且每段都更小；③ 正确性可归结为代数性质（见下）。

## 2. 同一分区多次溢出为何仍然正确

聚合状态构成一个**交换 monoid**（对每种聚合分别验证，见 FINDINGS.md 表①）：

- `Merge` 满足交换律与结合律（Sum 的浮点 caveat 见第 3 节），
  且存在单位元（Count=0 的空状态，Min=+Inf、Max=-Inf 语义上不出现，
  因为空状态从不落盘——只有真实见过的行才产生状态）。
- 因此一个键的全部输入行可以按**任意切分**成若干段，每段先各自折叠成
  部分状态，再按段的先后顺序合并，结果等价于一次性折叠全部行。
- 同一分区第 1、2、3… 次溢出写出的正是第 1、2、3… 段输入的折叠结果；
  内存里剩下的状态是最后一段。回读时**按溢出时间顺序**把各段与内存
  剩余状态依次 `Merge`，即还原完整折叠。漏掉任何一段都会少算，
  所以分区文件按段追加、回读按段序合并是正确性的关键不变量。

## 3. Min/Max 与 Count/Sum 的合并差异

- **Count/Sum 是加法合并**：`Count = c1+c2`、`Sum = s1+s2`。合并会
  「累积」，同一段状态被合并两次就会重复计数——所以每个溢出段必须
  恰好读回一次（计数器断言写出行数 == 读回行数）。
- **Min/Max 是取极合并**：`Min = min(m1,m2)`、`Max = max(M1,M2)`。
  合并**幂等**：同一段重复合并不改变结果，且与行的到达顺序完全无关。
- 浮点结合律：Count 是整数加法，精确满足结合律；Min/Max 是选择运算，
  精确满足结合律与幂等；Sum 的浮点加法仅在**无舍入**（如本测试用的
  小整数取值）时满足结合律。工程上这意味着：溢出路径与全内存路径的
  Sum 仅在精确算术下保证逐位相同，测试据此构造数据并用
  `math.Float64bits` 断言；Count/Min/Max 则无条件逐位相同。

## 4. 资源与复杂度约束（可证明）

- 驻留分组数：插入新键前先检查 `len(table) == budget`，是则先溢出
  一个分区再插入，故峰值 ≤ budget（计数器 `PeakResident` 记录）。
- 分区数 P 构造时固定，与数据规模无关；溢出文件数 ≤ P × 溢出次数。
- 每行至多入表一次；每个溢出段恰好读回一次；溢出记录数 ≤ 输入行数
  （每条溢出记录至少折叠了一行新输入），故总行处理次数 ≤ 2N。
- 受害分区选择「驻留键数最多」的分区，摊还每次溢出释放最多空间。

## 5. 分区文件格式与故障语义

```
header: magic(4) | version(2) | partition(4) | recordCount(8)   自描述
record: uvarint(bodyLen) | body | crc32(body)(4)                逐行
```

- recordCount 在写完记录后回填（先写占位，rename 前 Seek 回填），
  使「干净边界上的截断」也能被识别为长度前缀不完整。
- 写出：先写 `*.tmp`，`close` 后 `rename` 为 `*.spill`（原子）；
  启动时清理目录内所有 `*.tmp`（半截写崩溃残留），且运行实例只读
  自己 runID 前缀的段文件，绝不把临时文件当有效分区。
- 读回按偏移分类错误：头部不完整 / 长度前缀不完整 / 行体不完整 /
  CRC 不匹配（CRC 尾 4 字节不全视为不可校验，归入 CRC 类），
  四类可用 `errors.Is` 区分，错误携带分区号与字节偏移。
- 任何读回错误都让聚合中止并报错，绝不输出缺组的结果。

## 6. 并发与确定性

- 回读按分区并行（每协程一个分区，结果写入各自槽位，无共享状态）。
- 合并顺序固定为「分区号升序 → 段内按写出先后 → 内存剩余状态」，
  与协程完成顺序无关；输出按键排序。故任意打乱回读完成顺序，
  每组 Sum 位模式与分组顺序逐字节相同。

## 7. 边界语义

- NaN：拒绝并计入 `Skipped`（NaN 会破坏 Min/Max 与 Sum 的可比性）。
- ±Inf：合法，参与 Sum 与 Min/Max（+Inf 与 -Inf 同键时 Sum 得 NaN，
  属 IEEE 正常语义，不拒绝）。
- ±0.0 视为相等：比较前把 -0 归一为 +0；Go 的 `math.Min/Max` 对
  混合符号零的取舍本身确定，Sum 中 `+0 + -0 = +0`。
- 分组数 == 预算不溢出；== 预算 + 1 恰好溢出一次；空串键合法。
