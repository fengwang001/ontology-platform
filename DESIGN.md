# 分片扇出查询的部分失败合并器 — 设计推导

## 模型

- 查询扇出到 N 个分片；分片 i 返回若干记录 `{ID, Value}`（Value 为非负分数），并自报条数 `Claimed`。
- 每个分片能给出**上界** `b_i`：该分片对任意单一 ID 的总贡献不超过 `b_i`。
- 聚合语义：Count = 总记录数；Sum = 总分数；Min/Max = 单条记录极值；TopK = 按 ID 跨分片求和后的前 K 名（并列按 ID 升序）。
- 分片结果只有「成功 / 失败」两种；失败分片的数据完全不可用，但其上界 `b_i` 仍已知（构造时给定）。

## 部分失败下五种聚合的可信度推导

设成功分片集合为 S，缺失分片集合为 M，缺失上界之和 `B = Σ_{i∈M} b_i`。

### Count / Sum：下界（≥）

记录数与分数均非负，缺失分片只可能**增加**总数，不可能减少。
因此成功部分的结果 `v` 满足：真实值 ≥ v，即 `v` 是**下界**，标注「≥ v」。
区间估计：Count/Sum ∈ [v, +∞)。缺得越多区间越宽，但方向不变。

### Min / Max：方向相反的单边界

- Min：缺失分片可能含有**更小**的值，所以当前最小值 `v` 只保证「真实 Min ≤ v」，
  `v` 是真实 Min 的**上界**。区间：(-∞, v]（在分数非负模型下为 [0, v]）。
- Max：缺失分片可能含有**更大**的值，所以当前最大值 `v` 只保证「真实 Max ≥ v」，
  `v` 是真实 Max 的**下界**。区间：[v, +∞)。
- 两者方向相反：Min 给上界、Max 给下界。写反即错。

### TopK：可信前缀

按 ID 汇总得分，取前 K。缺失分片可能把分数加到任意 ID 上，所以：

1. 即使只缺一个分片，当前第 K 名也可能被某个「藏在缺失分片里的挑战者」挤掉，
   因此**整个列表不能整体标注可信**。
2. 但单个条目可以认证：挑战者最坏情况是「当前已知得分为 0、缺失分片把全部
   缺失质量 B 都堆到它头上」，其真实得分至多为 B。
3. 故当前得分**严格大于 B** 的条目，任何此类挑战者都无法超越它；它前面只有
   当前排它前面的条目（名次可能挪动），其**入选 TopK 是确定的，排名不确定**。
4. 由于列表按得分降序，「得分 > B」的条目必然构成一个**前缀**。
   定义**可信前缀长度 j** = 满足 score > B 的头部条目数（截断到 K）。
   - B 很小（缺失分片上界小）→ j = K，整个 TopK 成员资格确定；
   - B 很大 → j = 0，列表完全不可信。
5. 全部成功时 M 为空、B = 0，j = K 且排名也确定，整体标注「精确」。

### 全部成功

M 为空时五种聚合均无任何不确定性，必须标注「精确」，不得降级。

## 资源约束

- `fanout` 用容量为 limit 的信号量限制并发；非导出原子计数器记录历史最大
  在飞数，经 `Peak()` 暴露供测试断言。
- 整体截止由 `context.WithTimeout` 实现：获取信号量前先检查 `ctx.Err()`，
  截止后不再发起新请求；在飞请求通过 ctx 取消，`Run` 在截止后迅速返回。

## 故障注入与检出

- 超时：分片挂起直到 ctx 取消 → 状态 `Timeout`，其数据缺失但其余分片照常合并。
- 损坏：`Claimed != len(Records)` → 状态 `Corrupt`，整片判失败，坏数据不并入。
- 重复返回：同一分片 ID 在输入中出现多次（重试叠加）→ 按 ID 去重，首个结果生效，
  Count/Sum 与只出现一次时逐位相同。
- 全部失败：无任何成功分片 → 返回哨兵错误 `ErrAllFailed`（`errors.Is` 可判定），
  绝不返回「零值 + 精确」。
- 零分片 → 哨兵错误 `ErrNoShards`。全部返回空结果 ≠ 全部失败：前者是精确的成功。

## 确定性

扇出结果按输入下标回填（不按到达顺序），合并对 TopK 做全排序（得分降序、
ID 升序打破并列），缺失清单排序后输出，因此与到达顺序无关。
