# DESIGN：分片扇出查询的部分失败合并器

## 0. 数据模型与前提

- 记录 `Record{ID, Value}`；分片结果 `Result{ShardID, Claimed, Records, Bound, Err}`。
- 五种聚合定义在同一批记录上：Count=记录条数；Sum=ΣValue；Min/Max=Value 极值；
  TopK=按 Value 降序、并列按 ID 升序的前 K 条。
- 前提：Value ≥ 0（计分语义）。Count 的下界结论恒成立；Sum 的下界结论依赖此前提，
  因为缺失分片的贡献非负，成功部分必然不大于真实总和。Min/Max/TopK 的结论不依赖该前提。
- `Bound` 是分片事先声明的「本分片可能出现的最极端值」上界，分片失败时仍可用于推导。

## 1. 五种聚合在部分失败下的可信度推导

设成功分片集合为 S，失败（缺失）分片集合为 M。

### Count / Sum：下界（真实值 ≥ 当前值）

Count 与 Sum 都是对分片结果的单调累加，且每个分片的贡献非负（条数 ≥ 0，Value ≥ 0）。
缺失分片只会使总量变大或不变，绝不会变小。因此成功部分的合并值是真实值的下界：
标注为「≥ 当前值」（方向 AtLeast）。缺失越多界越松，但方向不变。

### Min / Max：方向相反的单边界

- Min：当前值 m 是 S 上的最小值。M 中可能含有比 m 更小的值，所以真实最小值只会
  不大于 m。m 是真实 Min 的上界，标注「真实 Min ≤ m」（方向 AtMost）。
- Max：对称地，M 中可能含更大的值，真实最大值只会不小于当前值。当前值是真实 Max
  的下界，标注「真实 Max ≥ m」（方向 AtLeast）。
- 方向写反即逻辑错误：Min 的部分结果永远不可能给出下界，因为未到的分片可以无限小。
- 若成功分片全部返回空结果（HasMinMax=false），Min/Max 无任何可说的，标注 Uncertain。

### TopK：整体不可信，但存在可信前缀

- 整体不可信：即使只缺一个分片，该分片中只要存在一条值介于当前第 K 名与第 K-1 名
  之间的记录，当前第 K 名就会被挤出榜单。因此缺失任意分片时不能标注整个列表精确。
- 可信前缀条件：令 S = 所有缺失分片 Bound 之和。对当前 TopK 中值为 v 的条目 e，
  若 v > S，则 e 必定入选真实 TopK：
  - 在「Bound 约束单条记录最大值」的解释下：任何缺失记录的值 ≤ max(Bound) ≤ S < v，
    要把 e 挤出前 K 需要 K 条值大于 v（或等值且 ID 更小）的记录，缺失分片拿不出。
  - 在「Bound 约束分片贡献总量」的更弱解释下：缺失分片能制造的记录值之和也不超过
    S < v，同样无法超越 e。取上界之和作为阈值在两种解释下都安全（偏保守）。
- 因此 confidence 返回可信前缀长度 j = 当前 TopK 中满足 v > S 的最长前缀；
  前缀内条目「入选确定，但内部排名不确定」（缺失记录可能插入它们之间）。
- 边界：缺失上界很小（S 小于第 K 名的值）则 j = K；上界很大（S 不小于第 1 名）则 j = 0。

### 全部成功

M 为空时五种聚合一律标注 Exact（精确），不得降级；TopK 的可信前缀即整个列表。

## 2. 资源约束（可证明）

- 扇出用容量为 maxConcurrent 的信号量，任一时刻在飞请求不超过上限；inFlight 原子计数，
  峰值用 CAS 循环更新到非导出字段 peak，测试经 Peak() 读取。
- 整体截止由 context.WithTimeout 派生；发射循环每轮先查 ctx.Err()，截止后不再发起
  新请求（goroutine 内 Query 前再查一次，关闭检查与发射之间的竞态窗口）；在飞请求持有
  同一 ctx，被取消后返回，Wait 在截止后极短窗口内返回。

## 3. 故障分类（errors.Is / 状态枚举可区分）

- Err 为 context.DeadlineExceeded → 状态 Timeout。
- Claimed != len(Records) → 状态 Corrupt，该分片判失败，坏数据不并入。
- 其他 Err → 状态 Error。
- 同一 ShardID 重复到达：按 ID 去重，先到为准，不得重复计数。
- 全部失败 → 哨兵错误 ErrAllFailed；零分片 → ErrNoShards；
  全部成功但皆空结果 → 正常返回 Count=0（HasMinMax=false），与全部失败可区分。

## 4. 确定性

- Sum 在「Value 降序、ID 升序」排序后的条目上累加，消除浮点加法顺序依赖。
- TopK 比较器是全序（并列按 ID 升序），与到达顺序无关。
- 成功/缺失分片清单均按 ShardID 排序后输出，保证逐字节一致。
