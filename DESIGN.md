# 统一差分：设计推导

## 1. 零计数 hunk 头的行号（语义 4）

统一格式里 `-a,b` 描述旧文件中的一段：该段从旧文件第 `a` 行开始，共 `b` 行。
b=1 时省略 `,1`；只有纯插入时 b=0，此时“该段”为空，没有“第一行”可言。
统一规则：**b=0 时 a = 插入点前一行的旧文件行号；插入点在文件开头时 a=0**。

- `x\n` → `y\nx\n`（C=0）：新行插在文件开头，前面没有旧行，故 `-0,0`；新文件从第 1 行开始计数 `+1`。
- `a\nb\nc\n` → `a\nb\nX\nc\n`（C=0）：X 插在旧第 2 行之后、旧第 3 行之前，前一旧行是第 2 行，故 `-2,0 +3`。
- `a\n` → 空（C=3）：旧段 1 行按省略规则写 `-1`；新文件为空段，其起始计数为 0，故 `+0,0`。

即 `-0,0` 与 `-2,0` 的数字都不是“将要插入的位置”（那是 1 或 3），而是定位空段所用的“前一行行号”。
Apply 读 `-a,0` 时把插入点放在旧游标第 a 行**之后**（a=0 即在文件开头插入），随后继续匹配上下文。

| 旧侧 | 书写 | 旧段含义 | Apply 的插入/替换点 |
|---|---|---|---|
| b=0 | `-a,0`（a 可为 0） | 空段，a=插入点前一旧行行号 | 旧游标对齐 a 行之后，只插入不删除 |
| b=1 | `-a` | 旧文件第 a 行这 1 行 | 删除/替换第 a 行 |
| b≥2 | `-a,b` | 旧文件第 a 行起 b 行 | 删除/替换该区间 |

新侧 `+c,d` 规则完全对称（d=0 时 c=插入点前一新行行号）。

## 2. Hunk 合并阈值（语义 5）

每个改动向前后各取 C 行上下文，覆盖旧文件区间 `[p-C, p+C]` 与 `[q-C, q+C]`，
p、q 是改动起始行，g=q-p-1 为间隔未改动行数。两段能放进同一个 hunk，
当且仅当区间不脱节：`p+C+1 >= q-C`，即 **g ≤ 2C 合并，g ≥ 2C+1 分开**。阈值是 **2C**。

C=0 时阈值 0：改动不相邻必分开。C=1、g=2 时必须合并（相邻上下文只输出一次）：
若误用阈值 C（g≤1 才合并），g=2 会被多切一个 hunk，与 `diff -U1` 不符；
若误用 2C+1（g≤3 合并），g=3 时两段上下文间还空 1 行，`diff -U1` 输出两个 hunk，被错误粘合。

## 3. 并列最短脚本的选择（语义 3）

Myers 按 D=0,1,2… 枚举路径，首次命中终点即最短。每个 D 内 k 从大到小扫描，
递推 `V[k] = max(下行(删除)后的x, 右行(插入)后的x)`；x 相等的并列**取下行（删除优先）**。
因此 `a b` → `b a` 的脚本固定为 `-a`、` b`、`+a`，同 D 上“先插后删”的路径永不出现。
补丁文本因此逐字节确定；hunk 内同位置删除行整体排在插入行之前，公共行夹在中间。

## 4. 偏移后后续 hunk 的查找起点（语义 7）

在记录位置 ±F 行窗口内寻找候选；候选到记录位置的距离取绝对值，最小者胜，并列取靠前者。
前一 hunk 以偏移 s（实际起点 − 记录起点）应用后，其后所有行号整体平移 s，
故后续 hunk 的搜索中心 = 记录起点 + s，窗口为该中心 ±F。不平移会让已应用 hunk 的位移
累积成误报“超出偏移范围”；平移量只取**已成功 hunk 的净位移**（插入−删除），与 hunk 间原间隔无关。

## 5. 其他约定

- 行模型：每个逻辑行保留原行尾（`\n`/`\r\n`/无），字节拼接还原；空文本是 0 行。
- 无末尾换行：对应正文行后输出 `\ No newline at end of file`，仅换行差异也产生删/插一对行。
- 计数器 = 对角线前进步 + snake 内逐行比较步；有 D 界时只跑 d≤D，同样满足 4(N+M)(D+1)。
- 四类错误哨兵：`edit.ErrTooDifferent`；`udiff.ErrFormat`（带补丁行号）；
  `patch.ErrContext`、`patch.ErrOffset`；资源超限为可判定的具名错误。
