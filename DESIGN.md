# RLE 编解码器设计

格式：游程 = 可选十进制次数 + 符号；数字与 `\` 须转义；次数 1 省略。

## 严格解码拒绝清单的推导

不变量：被接受的 t 必须满足 `Encode(Decode(t)) == t`（合法编码唯一）。
逐条看：若接受该类输入，哪个 t 违反不变量——

1. 显式次数 1：`Decode("1a")="a"`，而 `Encode("a")="a" != "1a"`。拒绝。
2. 次数 0：`Decode("0a")=""`，`Encode("")="" != "0a"`。拒绝。
3. 前导零：`Decode("01a")="a"`，`Encode("a")="a" != "01a"`。拒绝。
4. 相邻同符号游程：`Decode("2a3a")="aaaaa"`，`Encode("aaaaa")="5a" != "2a3a"`。
   必须看到下一个游程的符号才能判定，故状态机保留上一符号。
5. `\` 后非数字非反斜杠：`\a` 无合法解码值——若解为 `a`，`Encode("a")="a"`；
   若解为字面值 `\a`，`Encode("\\a")="\\\\a"`，均不等于 `\a`。拒绝。
6. 末尾孤立 `\`：`a\` 若解为 `a` 或 `a\`，编码分别为 `a`、`a\\`，均不等于 `a\`。
7. 末尾只有次数：`12` 的任何解码值的编码都以符号结尾，不可能等于 `12`。
8. 非法 UTF-8：码点序列无定义，range 会把坏字节变成 U+FFFD，
   重编码无法还原原字节，往返不成立。拒绝。

每类对应一个哨兵错误（`errors.Is` 可判定），包在 `*rle.Error` 里带字节偏移。

## 大次数的取舍

选择**支持**任意大次数：次数以十进制串读入、`math/big.Int` 表示，无溢出。
但解码输出总字节数受上限约束（`NewDecoder(w, maxOut)` 可配，`Decode` 用
`DefaultMaxOutput`）。游程写出前先判定 `n <= (maxOut-written)/size`，
超限返回 `ErrTooLarge`；写出按 8 KiB 块循环，绝不分配与次数成正比的内存。

## 语义与测试对照

（待测试写完后补齐）
