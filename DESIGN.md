# 可分裂二维网格索引设计

## 1. 边界归属
- 格与查询矩形都采用左闭右开：`[x0,x1) × [y0,y1)`。
- 点 `(x,y)` 属于格，当且仅当 `x0 <= x < x1` 且 `y0 <= y < y1`。
- 查询命中同样要求 `qx0 <= x < qx1` 且 `qy0 <= y < qy1`。
- 因此左、下边界点命中；右、上边界点不命中；四个角按两条轴分别应用同一规则。
- 分裂线为 `mx=(x0+x1)/2`、`my=(y0+y1)/2`。四个子格为 SW/SE/NW/NE：
  `SW=[x0,mx)×[y0,my)`，`SE=[mx,x1)×[y0,my)`，
  `NW=[x0,mx)×[my,y1)`，`NE=[mx,x1)×[my,y1)`。
- 分裂线上的点只进入坐标为闭边界的一侧：`x=mx` 进东侧，`y=my` 进北侧。
- `+0.0` 与 `-0.0` 按 IEEE 比较结果视为同一坐标；`NaN` 不满足任何归属，拒绝插入并计数；`±Inf` 拒绝插入。
- 退化查询 `x0=x1` 或 `y0=y1` 是合法矩形，但不存在点能同时满足 `a<=v<a`，结果为空。

## 2. 分裂与终止
- 叶格点数超过容量 8 时尝试四分；原有四个象限的点重新插入子格，分裂格自身不再持有数据点。
- 中点用 `x0/2+x1/2` 计算，降低 `x1-x0` 溢出为 Inf 的风险；只有 `x0<mx<x1` 且 `y0<my<y1` 才可分。
- 若所有点坐标相同，每次分裂后这些点仍全落在同一个子格；子格会再次超容量并继续分裂。
- 若不设终止条件，浮点坐标最终无法产生更细中点时会无限递归；深度上限 128 是第二道防线。
- 到达不可分或深度 128 的叶格允许超容量，1000 个同坐标点形成一条有限深链且不丢点。
- 删除后不自动合并：合并需移动子树并重新公布根版本，阈值附近的反复增删会造成分裂/合并抖动；本任务只要求删除正确性。

## 3. 查询剪枝
- 查询矩形与格相离：跳过，整格不访问其点。
- 查询矩形完全包含格：整取格内所有点，不逐点判定。
- 二者仅相交：下钻；叶格逐点做左闭右开判定。
- 非导出计数器记录访问格数和逐点判定点数，供包内测试与 demo 读取。

## 4. 持久化与恢复
- 文件由自描述头和逐格记录组成；小端编码，头和每条记录各带 IEEE CRC32。
- 头含魔数、版本、根边界、总记录数、总点数和 NaN 跳过数。
- 记录含格 ID、父 ID、象限、深度、边界、分裂标志、点数、四个子格 ID 和点数据。
- 读回校验：头 CRC；记录头 CRC；点载荷长度；点载荷 CRC；父子引用；子格必须在父格内部。
- 截断恢复只接受坏记录之前已完整校验的记录前缀；结构错误指出格 ID 与越界维。
- 保存先写 `*.tmp`、fsync 后原子 rename；加载时发现遗留临时文件则判定为崩溃产物并清理。

## 5. 并发
- `Grid` 持有 `sync.RWMutex`：查询持有 RLock，插入/删除/分裂/落盘持有 Lock。
- 分裂期间不会发布半构造子树；查询看到的要么是旧叶格，要么是完整新子树。
- 因而并发查询不会读到移动中的点；每次结果都是某个已提交状态的一致子集，粗锁也简化了计数与删除。
