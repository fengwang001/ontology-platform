# 设计推导

## 末尾换行
- 策略只删除、不插入：保留原样 `Keep`；`One` 把末尾连续空行压成一个 `\n`，因此无尾换行的空输入仍是 `""`；`Trim` 只删最后一个换行，即删除末尾全部空行但保留一个换行。
- 若给空输入插入 `\n`，该输出位置没有原文字节可由 `ToOrig` 指向，必然破坏 `ToOut(ToOrig(o))=o`；选择 `""` 则三策略对空输入都幂等。

| 输入 | Keep | One | Trim |
| --- | --- | --- | --- |
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 被删字节的 ToOut
- 映射函数只能单调不减；输出区间必须由保留字节覆盖，才能保证每个输出位置逆映射后再映射回自身。
- 因此被删字节不能落在下一个保留输出之后：删除尾空白、`\r\n` 的 `\r`、末尾裁掉的 `\n` 时，`ToOut(i)` 都取该删除段左侧的输出位置。
- 光标放在被删尾空白中，会定位到本行换行符之前；这正是删除发生的位置，而不是跳到下一行。

## 空白缓冲上限
- 选择拒绝：空格/Tab 只有看到行尾或 EOF 才能判定，强制当“非行尾”会在后续真出现换行时产生错误输出，破坏跨切分一致和幂等。
- 超限点报原文偏移，立即返回 `ErrWhitespaceLimit`，缓冲与已输出均保留；进入终态后再写返回 `ErrClosed`。

## 并行拼接
- K 段以“开放结尾”处理：段尾未定 `\r` 和尾空白不提前判定。前一段尾部只可能携带 1 个 `\r` 或一串空白，修正只看后一段开头及一个决定字符。
- 空白上限给该串长度上界；开放段只决定到尾空白前。拼接时按全局字符决定保留或删除，开放段 mappers 以原始偏移和已决定输出偏移依次平移连接。
- 最后一段闭合，再统一做一次全局末尾策略；`span.Builder` 可从输出尾部回卷，保证映射与整体处理一致。
