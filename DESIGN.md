# 统一 diff 设计推导（依赖方向 lines→edit→hunk→udiff→patch，无反向依赖）

## 1. 零计数 hunk 头的行号规则（语义 4）
hunk 头数字为「起点的前一行行号」。a 是旧侧 hunk 第一行的前一行（1 基），b 是旧侧覆盖行数；c、d 同理。
- 旧文件开头插入：旧侧无任何行，起点前一行不存在→`a=0`；新侧第一行是第 1 行→`c=1`：`@@ -0,0 +1 @@`。
- 在旧第 2 行后、旧第 3 行前插入（C=0）：插入点前一行是旧第 2 行→`-2,0`；新侧该插入行成为新第 3 行→`+3`。
- 删除唯一的旧第 1 行（C=3）：旧侧第一行是第 1 行→`-1`；新侧为空，起点前一行是新第 0 行→`+0,0`。
所以数字是「前一行行号」而非插入位置；`Apply` 对 `b=0` 把插入点放在旧文件第 a 行之后（a=0 即文件开头），对 `d=0` 把删除点解释为新坐标仅供显示，应用只看旧侧。

| 旧侧书写 | 含义 | Apply 插入/删除点（旧文件，0 基偏移） |
|---|---|---|
| `-a,0` | 0 行：纯插入 | 在第 a 行之后；a=0 → 偏移 0 |
| `-a`（即 `-a,1`，省略 ,1） | 1 行，行号 a | 偏移 a-1 起 1 行 |
| `-a,b` b≥2 | b 行，首行行号 a | 偏移 a-1 起 b 行 |

## 2. hunk 合并阈值（语义 5）
设两处改动之间的未改动行数为 g。实测 GNU diff -U C：C=3 时 g=5 合并、g=6 分开；C=2 时 g=4 合并、g=5 分开；C=1 时 g=2 合并、g=3 分开。故**合并 iff g ≤ 2C**（GNU 源码的 `gap <= 2*C`）。C 会过早切分（C=3 时 g=5 本该合并却切开）；2C+1 会过晚合并（g=2C+1 时 GNU 已分开）。

## 3. Myers 并列最短脚本的选择（语义 3）
回溯时在同一 (k,D) 上若「从 k-1 来（插入）」与「从 k+1 来（删除）」等长，**优先删除**（k+1 分支优先），随后沿对角线吃下 snake。效果：`a b → b a` 产生 `-a`/` b`/`+a` 排列，即删除块整体排在可并列的插入块之前；同一对输入因此逐字节确定。

## 4. 偏移与后续 hunk 平移（语义 7）
每个 hunk 在记录位置 ±F 行窗口内，找上下文与删除行精确匹配点，多个候选取 |偏移| 最小，同距取靠前。hunk i 以位移 o 应用后，其后每个 hunk 的查找基准随累计位移平移：hunk j 的搜索中心 = 记录位置 + Σ_{i<j}oᵢ，窗口仍为 ±F。原因：前一处净增删 o 行使其后所有内容整体平移 o 行；不平移会让本应命中的后续 hunk 误报超范围（patch(1) 的 offset 行为）。
