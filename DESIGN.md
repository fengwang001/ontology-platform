# 调度器设计推导

## 1. 多把锁释放后的优先级

有效优先级为 `eff(t)=max(base(t), 所有 t 直接或间接阻塞任务的 base)`。L 持 m1、m2，等待者基础优先级分别为 10、7，则释放 m1 前 `eff(L)=10`；释放后 m1 的等待链消失，但 m2 仍在，故 `eff(L)=7`，不能恢复为 1。

反例：L 先持有 m1、m2；H(10) 等 m1，X(7) 等 m2；L 释放 m1 时若恢复 1，大量 M(5) 可连续运行，X 与经 m2 间接依赖 L 的任务仍被反转。因此释放必须删除该锁贡献后重算最大值。

## 2. 降级传播何时停止

等待边 `w -> h` 表示等待者 w 被持有者 h 阻塞。每个任务保存：基础优先级、自身作为等待者贡献给持有者的阈值、自己持有锁收到的最大贡献。一次插入、删除或阈值变化只沿持有者边传播。

节点重算值若与旧值相同即可停止：该节点给其持有者的阈值本来就等于旧值，因而其上游的所有输入和输出都不会变化；其兄弟等待者提供的更大贡献仍保留。

“降到基础优先级就停止”是错的：节点可能还持有另一把有等待者的锁，或仍被另一条等待边支持。降到基础值会沿链过度撤销，而未降到基础值但从 10 变为 7 时又必须继续通知持有者。

## 3. 持锁执行 Done

选择报错并强制释放全部锁，不让等待者永久阻塞。Done 使任务进入 `Aborted`，返回可判定的 `ErrLockedDone`；所有锁按队列顺序交接，任务可通过最后结果中的 `aborted=true` 和错误识别异常退出，下一持有者不继承该状态。

## 4. Unlock 交接与抢占

一条 Unlock 的顺序：校验持有者；选队首；删除旧等待边并让队首成为新持有者；为新持有者重建“它若正等别的锁”的等待边；删除该锁给旧持有者的贡献，并沿旧持有者上游降级；沿新持有者的等待边向上继承；任务 PC 前进。

交接发生在同一临界状态更新中，不会有无主且有人等待的可观察状态。步末重新按有效优先级选任务，因此获得锁的等待者若高于刚执行 Unlock 的任务，下一步立即运行；这不违反无界反转，反而保证被唤醒的最高优先级者先完成其临界区。

## 5. 继承效果对照

| 实现 | M=1，H 等待步数 | M=100，H 等待步数 |
|---|---:|---:|
| 不继承 | 受 M 工作量影响，可能 > L 剩余临界区+常数 | 随 M 总工作量无界增长 |
| 传递继承 | <= L 剩余临界区+常数 | <= L 剩余临界区+常数 |

复杂度用增量边和每把锁的最大贡献维护；无关任务不在传播集合中，释放单锁只需修改该锁聚合与受影响等待链。
