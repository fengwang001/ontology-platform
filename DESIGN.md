# 设计与推导

## 1. 零计数时 hunk 头行号的规则

头中数字的常规定义：`-a,b` 的 a 是该 hunk 在旧文件中**第一行**的 1 基行号。当 b=0 时
hunk 在旧文件里不占任何行，不存在“第一行”，GNU diff 取**插入点之前最近一行的行号**，
插入点在文件开头时该值为 0。

- `x` → `y,x`（C=0）：插入点在开头，前面有 0 行 → `-0,0`；新侧第一行是 y → `+1`（,1 省略）。
- `a,b,c` → `a,b,X,c`（C=0）：X 插在第 2 行之后，前面有 2 行 → `-2,0`；新侧第一行是第 3 行 → `+3`。
- `a` → 空（C=3）：旧侧删除第 1 行 → `-1`；新侧 0 行，删除点之前有 0 行 → `+0,0`。

统一规则：**计数为 0 的一侧，起点数字等于该 hunk 在该侧的插入/删除点之前已有行数**
（开头为 0，末尾删除同样为 0）。Apply 看到 b=0 时不消费旧行，把插入点放在“第 a 行之后”
（a=0 即文件开头）；d=0 时删除点同理。

| 旧侧计数 | 书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`，a=插入点前的行数（开头为 0） | 不消费旧行；在第 a 行之后插入 |
| b=1 | `-a`（省略 `,1`） | 消费第 a 行这一行 |
| b≥2 | `-a,b` | 消费第 a 到第 a+b−1 行 |

## 2. hunk 合并阈值

两段改动间有 g 行未改动行。前一 hunk 向后最多带 C 行上下文，后一 hunk 向前最多带 C 行，
共可覆盖 2C 行。当 g ≤ 2C 时，两段上下文相邻或重叠，GNU diff 输出一个 hunk；
g = 2C+1 时中间必有一行落在两段上下文之外，GNU diff 输出两个 hunk。
**阈值为 2C：g ≤ 2C 合并，g ≥ 2C+1 分开。**

反例（C=1，改动 X、Y 间隔 g 行）：g=2 时两行分别是 X 后上下文与 Y 前上下文，必须合并。
若阈值取 C，g=2 就会被分开，与 GNU 不符；若阈值取 2C+1，g=3 也会合并，
但 3 个间隔行只有 2 个上下文位置，中间行无处安放，GNU 在此输出两个 hunk，亦不符。

## 3. 并列最短脚本的选择

Myers 回溯到对角线 k 时，若向下（插入）与向右（删除）到达同一 x（k=0、D 为偶数的并列），
固定选择**删除优先**（条件 `V[k-1] < V[k+1]` 不成立时走删除）。因此 `a b` → `b a`
的脚本固定为 `-a`、` b`、`+a`：hunk 内被删行排在前，公共行居中，插入行排在后。
同一输入运行任意次脚本字节一致。

## 4. 偏移后后续 hunk 的查找起点

hunk 头记录的是**原文件坐标**。设前面各 hunk 净增行数（插入行−删除行）累计为 Δ，
上一个 hunk 实测偏移为 k（实际起点减去其当时的查找中心），则下一个 hunk 的查找中心为

`中心 = 记录的旧起点 + Δ + k`

Δ 始终累加（精确应用时 k=0 也要加，因为净增行改变了后续坐标），k 沿用上一个实测值
（GNU patch 的 offset carry）：文件整体平移时后续 hunk 直接出现在预期附近；
若该中心 ±F 内无匹配则该 hunk 失败，距离仍按相对该中心计算。

## 5. 计数器口径

`diagSteps` 在每次对角线选择（删/插一步）及 snake 中每次逐行比较时递增。
每条 d 路径上各对角线 snake 比较的 (x,y) 互不重合，故每层 ≤ N+M，
总计数 ≤ (D+1)(N+M) + (D+1)² ≤ 4(N+M)(D+1)。距离上限触发时同样满足。
