# LZ77 自定格式：设计推导

## 流格式记录布局

流头固定 `LZ77` 魔数 4 字节 + 版本 `1` 单字节；其后为记录流，末尾恰一条流尾。
CRC32 按 IEEE 多项式对「原文」连续计算（块压缩时每块带累积值接力），定长 4 字节大端。
varint 为无符号 LEB128：最多 10 字节，第 10 字节只允许最低位；越界报 `ErrVarInt`。

| 记录 | tag | 后续字段（均为 varint，除 CRC） | 说明 |
|---|---|---|---|
| 字面量 | 0 | 长度 n；紧接 n 个明文字节 | 连续字面量合并为一条 |
| 回指 | 1 | 距离 dist（≥1）；长度 length（≥3） | 重叠时按字节向输出前方复制 |
| 刷新 | 2 | 无 | 解压器把已还原字节整体交给输出 |
| 流尾 | 3 | 原文总长 length；CRC32（4 字节大端） | 流尾后多字节报 `ErrTrailing` |

## 推导一：输出与 Write 切法无关（语义 3、4）

最长匹配可能跨过任意 `Write` 边界，因此压缩器不能在边界提交任何决定。
维护「未决位置」`p`：仅当其后已有 `maxMatch` 个字节或流已结束（flush/close），
位置 `p` 的最长匹配才被最终确定。尾部不足 `maxMatch` 字节整体缓存，不发任何记录。
由于匹配判定只看「窗口历史 + 确定长度的后续原文」，与这段字节由几次 `Write`
拼出无关，故记录序列逐字节相同。`Flush` 的语义是强制让所有未决位置变为确定：
把尾部缓存按「流结束」处理（确定匹配、发字面量、发刷新标记），之后重置未决
窗口——新写入的字节从空历史重新匹配，但仍允许回指 flush 前已留在滑窗中的历史
（滑窗不随 flush 清空，故 flush 后的匹配可跨过 flush 点）。无新输入再 Flush
时，刷新标记上一轮已发，输出 0 字节。

## 推导二：重叠回指的复制（语义 2）

块复制（如 `copy` 语义）允许目标落在源区间内，Go 的 `copy(dst, src)` 按字节
顺序、逐字节前进，恰等价于 LZ77「每复制一个字节，它立刻成为新的历史」的定义：
dist<length 时后面的字节会复制到刚刚复制出的字节，周期串被正确展开。
实现上对 `d >= n` 走一次整段 copy；对 `d < n` 用逐字节（实际上也是对单字节
窗口的 copy）循环。最坏情形为超长回指，逐字节总量等于输出字节数，属线性开销，
且写出前逐字节累加输出计数，天然配合输出上限的提前拦截，故可接受。

## 推导三：两种越界距离为何区分（语义 7）

`dist > 已输出字节数`：引用了从未存在过的字节，任何合法流、任何预置字典都不
可能产生，属伪造引用。`dist > 窗口容量` 但不大于已输出：引用的字节确实曾经
输出，只是压缩方宣称的记忆窗口太小、已滑出——这说明压缩器/解压器窗口配置不
一致，是另一类故障。压缩器只在窗口内取候选，故绝不可能产生后者（单块压缩
时）。并行预置字典改变了「已输出字节数」：块字典是逻辑历史的一部分但尚未通过
本块记录输出。解压器对预置字典的处理是把它视为窗口初始内容并把「已输出」
计数也预置为字典长度；流式格式本身不含字典，字典只存在于压缩端（由上块原文
取得），解压器解压时实际逐块还原了上块全部字节，因此线上解压器看到的已输出
数自然包含上块，两类越界的区分与错误判断不变。

## 推导四：并行块的确定性与衔接（语义 9）

块 k 的匹配器输入 = 块 k 原文 + 固定窗口算法，其唯一外部信息是块 k-1 原文
（取其末尾至多窗口容量字节作预置字典）；它不读取其他块的记录、CRC、完成时刻，
哈希链是纯函数式的（插入/查找无随机源、无共享可变状态），故每块输出只依赖
「本块原文 + 上一块原文」，与 worker 数、调度顺序无关。衔接：各块编码结果按
块序拼接进同一流头之后；块间不插刷新标记——记录流对解压器是连续的，块 k 的
回指距离可落入块 k-1（这些字节已在解压输出/窗口中）。仅在调用方显式 Flush 的
累计偏移处才有刷新标记，故 workers=1/2/4/8 记录逐字节相同；流尾记录块 k 的
累积 CRC 与总长。
