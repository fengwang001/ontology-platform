# 可分裂二维网格索引 — 设计推导

包划分：`geom`（点/矩形）→ `cell`（四叉树节点）→ `grid`（插入/删除/定位/并发）
→ `query`（剪枝范围查询与计数器）→ `persist`（落盘/恢复/校验）→ `cmd/demo`。

## 1. 坐标系与边界归属（左闭右开）

每个格是 `[x0,x1) × [y0,y1)`；查询矩形同样左闭右开。
归属判定：`x0 <= x && x < x1 && y0 <= y && y < y1`。

- 点在格内：属于本格。
- 左/下边界（`x==x0`、`y==y0`，含 `+0/-0`，IEEE 下相等）：属于本格。
- 右/上边界（`x==x1`、`y==y1`）：不属于本格，属于坐标更大的相邻格。
- 四角：左下 `(x0,y0)` 属本格；左上、右下、右上都不属本格（上/右边排除），右上点属右上邻格。
- 退化矩形（`x0==x1` 或 `y0==y1`，线/点）：严格不等式使任何点都不命中，合法但结果为空。
- 查询边界：点落在查询左/下边 → 命中；落在右/上边 → 不命中。
- `NaN`：任何比较皆 false，插入时拒绝并计入 `Skipped()`；`±Inf` 同样拒绝（要求坐标有限）。

## 2. 分裂与重新归属

叶子格点数 `> 容量` 时分裂为 4 子格，中点 `mx=(x0+x1)/2, my=(y0+y1)/2`。
象限编码 `q = (x>=mx ? 1:0) | (y>=my ? 2:0)`：0=SW，1=SE，2=NW，3=NE。

分裂线归属：西/南子格范围是 `[lo,mid)`，中点被其上界排除；东/北子格是
`[mid,hi)`，中点被其下界包含。故 `x==mx` 进东侧子格、`y==my` 进北侧子格，
中心点进 NE。这是左闭右开划分 `[lo,mid) ∪ [mid,hi)` 的唯一归属。
原有点全部按此规则下沉一次，随后对仍超载的子格递归分裂。
下沉是「从父点集移除、按象限加入子格」的单步操作，不丢不重：
每个点恰好落入一个子格（象限划分是 `[x0,mx)∪[mx,x1)` 的完备互斥划分）。

### 2.1 全同坐标为何会无限递归，如何终止

若所有点坐标相同，任一分裂后它们全部进入同一个子格，该子格仍然超载，
朴素递归永不停止。终止条件（两选一命中即停为「饱和叶」，不再分裂）：

1. 坐标不可再分：某轴 `(lo+hi)/2` 在 float64 下舍入回 `lo`（或 `hi`），
   即 `mx<=lo || mx>=hi`。两轴都不可再分时，该格已是该坐标处可表示的最小格，
   再分也产生不出更小范围，必须停；只有一轴可再分时仍按四子格分裂
   （另一轴两子格边界相等，点都走下界侧，结构合法、归属仍唯一）。
2. 深度上限 `maxDepth=100`：双保险，防止任何未预料的浮点情形导致栈增长。

饱和叶可持有超过容量的点；点查、范围查询、落盘均正常工作。

## 3. 删除不合并

删除只从叶格移除点。子格总数降到阈值以下也不合并，理由：
合并需把四子格点上提到父并丢弃子格，紧接的插入又会触发分裂，
在阈值附近产生「分裂—合并」抖动与写放大；本任务明确不要求合并。
代价仅为稀疏树多占空节点（有界），查询剪枝不受影响。

## 4. 查询剪枝与计数器

递归访问格 `c` 与查询矩形 `r`：相离（`r.x1<=c.x0 || r.x0>=c.x1 || y 同理`）跳过；
`r` 完全包含 `c`（含边界，因半开：点 `< c.x1 <= r.x1` 必满足 `< r.x1`）→
整取该格全部点（含其子树），逐点判定数 +0；相交且为叶 → 逐点判定；
相交且已分裂 → 递归四子格。计数器：`CellsVisited`、`PointsTested`。

## 5. 并发

`grid` 用单一 `sync.RWMutex`：查询持读锁，插入/删除持写锁。
分裂（移动点）与查询互斥，查询看到的永远是某次提交后的完整快照，
移动中的点不可能被看到，因此不重不漏；多查询彼此并发。

## 6. 落盘格式（小端，自描述，全部 CRC32(IEEE)）

- 文件头 50B：magic `ONGRID01`(8) + ver u16 + cap u32 + 根矩形 4×f64 + 头CRC u32。
- 格记录按前序（根在前）：len u32 + payload（矩形 4×f64 + depth u16 +
  flags u8 + npt u32 + nchild u8 + npt×(id u64,x f64,y f64)）+ 该记录 CRC u32。
  flags：bit0=已分裂，bit1=饱和叶。nchild 给出直接子格数（0 或 4）。
- 文件尾 12B：`FOOT` + 记录总数 u32 + 全部格记录字节的 CRC32。
- 截断分类：<50B 头不完整；记录定长头（len+40B 元数据）读不齐 → 记录头不完整；
  点数据区读不齐 → 点数据不完整；CRC 缺失/不符或尾块不完整 → CRC 不匹配。
  任何情况下返回「最大可恢复前缀」（所有自身完整且 CRC 正确、结构校验通过的格）。
- 结构不变量：父标记分裂却缺子格 → 报错并指出父格；子格矩形超出父格
  （x0/x1 或 y0/y1 越界）→ 报错指出子格序与越界维度。
- 崩溃安全：写 `name.tmp` → fsync → 原子 rename；加载时忽略并清理残留 tmp。
