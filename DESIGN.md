# 设计推导

## 1. 非法单元切分规则（由七样例推导）

规则：从首字节 B 起按「最大合法序列」吞字节：首字节非法（80..BF / C0 C1 / F5..FF）只吞 1；
合法首字节吞掉它期望的所有后续字节，但每个后续字节必须落在该位置的合法区间内，一旦某位置
字节非法，该非法位置之后的期望续字节**重新当作新首字节解析**，已吞部分整体记为 1 个非法单元。

- `F0 90 80 41`：F0 要求 3 续字节，第 2 字节 90 合法，第 3 字节 80 不在 F0 第 3 位区间
  （90..BF），F0 90 80 为 1 单元；41 合法 → 1 FFFD + A。
- `E0 80 80`：E0 第 2 字节须 A0..BF，80 非法，E0 80 为 1 单元；剩余 80 各为首字节非法 →
  共 3 FFFD。
- `ED A0 80`：ED 第 2 字节须 80..9F（A0..BF 是高代理编码），ED A0 为 1 单元，剩 80 1 单元
  → 3 FFFD。`C0 AF`：C0 本身非法吞 1，AF 再吞 1 → 2 FFFD（非最短形式同理：任何位置非法即止）。
- `F4 90 80 80`：F4 第 2 字节须 80..8F（90..BF 越界 >10FFFF），F4 90 为 1 单元，后两 80 各 1
  → 4 FFFD。`E2 82`+EOF：E2 期望的第 3 字节缺失，残留的整个合法前缀计 1 单元 → 1 FFFD。
- `80 80`：两个非法首字节 → 2 FFFD。

第二字节合法区间表（其余续字节位置：除首字节受限外均为 80..BF）：

| 首字节 | 第2字节 | 非法时吞 | 首字节 | 第2字节 | 非法时吞 |
|---|---|---|---|---|---|
| C2..DF | 80..BF | 2（首+非法续） | E0 | A0..BF | 2 |
| E1..EC | 80..BF | 3 | ED | 80..9F | 3 |
| EE..EF | 80..BF | 3 | F0 | 90..BF | 2 |
| F1..F3 | 80..BF | 4 | F4 | 80..8F | 2 |
| 80..BF | — | 1 | C0 C1 F5..FF | — | 1 |

## 2. 切分缓存上限与提前放弃

UTF-8 最长 4 字节，因此只需缓存「首字节 + 至多 2 个已验证续字节」=3 字节。机器不缓存原始
字节而缓存解析状态：need（还欠几字节）、cp（已累加码点）、start（单元在流中的起始偏移）。
看到「位置非法」的字节时立刻完成非法单元并把该字节重投一次（每字节最多被检查 2 次：作为
非法续 1 次、作为新首字节 1 次），无需等到 EOF。只有 need>0 且真 EOF 才是截断。UTF-16
缓存：半个码元（1 字节，奇数长度）或一个孤立高代理（2 字节），EOF 时为截断。

## 3. par 的切点对齐

UTF-8 切 [s,e)：从 s 最多回看 3 字节找首字节 L（非 80..BF 者）。若 L 存在，段只报告「起点
≥ s」的单元（前缀 [L,s) 仅喂给机器定状态，其单元属上段）；段尾需续读最多 3 字节，单元起
点 < e 的全部吞掉，起点 ≥ e 的留给下段。L 不存在（s 前 3 字节全是续字节）则段首先跳过直到
首个非续字节，跳过部分属上段已开始的非法单元。末段才允许把残缺当截断/FFFD。

关键例：切点在 C3|80 80。下段回看 L=C3，前缀 C3 喂入定状态（期待续），首报起点 s=80，
于是 C3 80 合并为合法字符且归上段（其起点 <s）；第二个 80 起点 ≥s，是孤立续字节。非法数
与单线程一致，因为每个单元恰好被「包含其首字节的段」计数一次。回看/续读各 ≤3 字节，
UTF-16 同理各回看/续读 ≤2 字节（代理对需 4）。

## 4. 输出上限断点

Write 在输出一个标量**前**检查剩余额度；放不下则保持终态，返回 n=本次输入中已完全解决的
字节数，缓存里的字节不计入 n。`Pos()` 给出下一个未解决字节的全流偏移（含缓存），调用方用
Pos 作为新实例的续传起点。因此跨上限拼接与一次转完逐字节相同：每个标量要么整进旧实例，
要么整进新实例，UTF-16 代理对作为一个单元检查。
