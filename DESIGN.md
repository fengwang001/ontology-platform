# 统一 diff 设计推导

## 1. 零计数 hunk 头的行号（第二节第 4 条）

样例：`x\n`→`y\nx\n` 得 `-0,0`；`a b c`→`a b X c` 得 `-2,0`；`a\n`→空 得 `+0,0`。
共同点是计数为 0 的一侧在该 hunk 里**不消费任何行**，数字只可能表示「插入点之前
保留了多少行」：例一插入发生在文件开头（前面 0 行）→ `-0,0`；例二 `X` 插在第 2
行之后（前面 2 行）→ `-2,0`；例三新文件为空，删除点之前 0 行 → `+0,0`。
所以该数字是**前一行的行号**（= 之前保留的行数），而不是 1-based 的插入位置：
若把它解释成「将要插入的位置」，例一会得到 `-1,0`（位置 1），与 GNU 输出矛盾；
而「前一行行号」在插入点位于文件开头时自然给出 0，三个样例全部吻合。
`Apply` 的解释：`b==0` 时插入点放在第 `a` 行之后（0-based 下标 `a`）；`b>0` 时
起点为 0-based 下标 `a-1`。

| 情形 | 头部写法 | Apply 对插入点/起点的解释 |
| --- | --- | --- |
| b=0 | `-a,0`（`,0` 必须显式写），a=之前保留行数 | 插入点=0-based 下标 a（第 a 行之后） |
| b=1 | `-a`（省略 `,1`），a=首行行号 | 起点=0-based 下标 a-1 |
| b≥2 | `-a,b`，a=首行行号 | 起点=0-based 下标 a-1，共 b 行 |

## 2. hunk 合并阈值（第二节第 5 条）

每个改动块向前/向后各要 C 行上下文。两段改动相隔 g 行未改动行时，前者的尾部
上下文（C 行）与后者的头部上下文（C 行）合计 2C 行：g ≤ 2C 时两段上下文重叠或
恰好相接，GNU 把两段合并；g = 2C+1 时中间还剩 1 行谁也覆盖不到，GNU 拆开。
故阈值为 **2C**。反证：若阈值取 C，则 g=C+1..2C 时本算法拆开，但 GNU 输出的是
单个 hunk（如 C=1、g=2 时两段改动连同 2 行间隔落在一个 hunk 里）；若阈值取
2C+1，则 g=2C+1 时本算法合并，但 GNU 输出两个 hunk（中间那行不属于任何上下
文）。两者都与 `diff -U C` 不符。

## 3. 最短脚本的并列选择（第二节第 3 条）

Myers 前向搜索中，k 线上一步可来自 k-1（右移=删除）或 k+1（下移=插入）。
本实现在 `V[k-1] >= V[k+1]` 时取删除，即**平局优先删除**。回溯时同一改动块内
所有删除先于插入被记录，渲染出的 hunk 里 `-` 行全部排在 `+` 行之前，与 GNU
diff 一致。例：`a b`→`b a`（D=2），本实现输出 `-a`、` b`、`+a`（先删 a、在 b
后插 a），而非插入优先的 `+b`、` a`、`-b` 形态。规则固定，故输出确定。

## 4. 偏移应用后后续 hunk 的查找起点（第三节第 4 条）

补丁里的行号都是**原文件坐标**。第 i 个 hunk 记录位置 p_i，实际在偏移 k_i 处
命中；应用后它使后续文本净增 s_i = new_i - old_i 行。对第 i+1 个 hunk，目标
文本相对原坐标的总漂移 = 之前各 hunk 的命中偏移与净行数变化之和，故维护
shift：查找中心 = p_{i+1} + shift，命中后 `shift += k_i + s_i`。不平移会把查
找中心放在已失效的原坐标上，前面 hunk 每净增/删一行都会无谓消耗后续 hunk 的
偏移预算 F，甚至导致误拒。
