# 设计推导

## 1. `x/**` 与边界推导

路径按字面 `/` 切段，不做任何规范化。`**` 段匹配「零个或多个**整段**」，
这里「整段」含相邻 `/` 之间的空段（故 `x/` 的尾段为空段）。模式与路径都切成段
序列后做对齐：普通段必须恰好对齐一个路径段；`**` 段可消耗任意数量（含零）
路径段。由此一条规则统一解释全部边界：

- `x/**`：模式 `x`,`**`。`x` 对齐 `x` 后，`**` 消耗零段 => 匹配 `x`；
  `x/` 切为 `x`,`` ，`**` 消耗一个空段 => 匹配 `x/`；`x/y` => 匹配。
- `**/x`：`**` 消耗零段、`x` 对齐末段 => 匹配 `x`；`/x` 切为 ``,`x`，
  `**` 消耗前导空段 => 匹配 `/x`。
- `a/**/b` 匹配 `a/b`：中间 `**` 消耗零段，与上同一条零段规则。

## 2. 空串路径推导

空串路径切成单段 `[""]`。`*` 是**段内**原子，匹配一个段内零或多个码点，
故 `*` 匹配该空段（空串路径）。`?` 需恰好一个码点，不匹配。`**` 是整段
通配符，可消耗零或多个路径段；路径非空时至少有一个段 `[""]`，`**` 消耗
它 => `**` 也匹配空串路径。空模式只匹配空串，是字面相等。

## 3. 无回溯算法

- **段内**：逐码点扫描，遇 `*` 记录「模式星后位置 + 当前路径位置」为唯一
  回退点；失配时回退，并把路径位置推进 1（让星号多吃一个码点）。只保留
  **最后一个**星号即够：更早的星号 `s1` 之后还有较新的星号 `s2`，任何
  「`s1` 多吃一点」的对齐，都等价于让 `s1`、`s2` 之间那段先对齐成功、再由
  `s2` 多吃；即较早星号的一切回退选择都能被较新星号的回退模拟，不会漏解。
  故线性、无指数回溯。
- **跨段 `**`**：段是离散对齐单位，普通段要求「恰好一个」，不能像码点那样
  平滑地「多吃一个码点」，且多个 `**` 之间隔着必须恰好对齐的普通段，较早
  `**` 的选择会改变中间普通段的落点，无法被较新 `**` 的回退统一模拟。
  因此段级用**可达集合 DP**：`reach[i]` 表示处理到模式第 i 段的可达位置
  集合（路径段下标），每段做一次集合传播，`**` 生成从该点到末段的全部后缀
  位置。总代价 = 模式段数 × 路径段数，整体 O(模式长度 × 路径长度)。

## 4. 规则集版本一致

`Set` 内只放一个不可变的 `*ruleset` 指针，用 `atomic.Pointer[ruleset]` 持有。
每次查询先 `Load()` 一次快照指针，之后 `Match`/`Explain` 全程只用该指针，
故一次查询只看到某一个版本，序号与原文必属同一版本。替换时先**离线**编译
整个新规则集：任一条失败则丢弃新集合、原子指针不动，旧版本完全不受影响；
全部成功后单次 `Store` 完成热替换。

## 5. 匹配结果总表

| 模式 ＼ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**`   | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| `**/x`   | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `*`      | 是 | 是 | 是(空段) | 否(两段) | 否 | 否 | 否 |
| `**`     | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
