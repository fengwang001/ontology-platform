# 设计推导

## 末尾换行
规范化先得到：CRLF/CR/LF 都为 `\n`，每行尾的空格、制表符删除。三种策略只检查最终连续 `\n`：

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

EnsureOne 若向空输入插入 `\n`，输出有 0、1 两个偏移而原始终点只有 0；为同时满足 `ToOut(ToOrig(o))=o` 和单调，不可能给两个输出偏移不同的原像。因此本实现不凭空制造行边界：已有边界时折叠成一个，没有边界时不插入。TrimBlank 删除末尾空行后保留最后一个边界；规范化后它与 EnsureOne 的字节结果相同，差异仅在策略语义。所有输出再处理时，尾部 `\n` 数已是 0 或 1，故幂等。

## 被删字节映射
设输出在边界 `p` 删除一段原文字节后继续。逆映射只能把 `p` 指向删除段后的第一个保留字节；若删除段位于 EOF，则指向输出和原文的共同终点。为使 `ToOut(ToOrig(p))=p`，删除段内每个原文字节的 `ToOut` 都取 `p`，删除段前的字节取小于 `p` 的值，之后取大于 `p` 的值，单调性成立。CRLF 中 LF 产生该 `\n`，所以 CR 的 `ToOut` 等于 LF/换行符偏移；行尾空白也取同一偏移。编辑器光标因此停在换行边界之前，而不是跳到下一行。

## 缓冲超限
强制把待定空白输出成普通空白，会在后续读到换行时产生行尾空白，直接违反“输出无行尾空白”和幂等；事后收回又会改变已经给出的输出偏移。故选择拒绝：超限错误带导致超限的原文偏移，已输出字节保留，写入端进入终态；更小截断输入不会包含该字节，因而截断语义仍确定。

## 并行拼接
每段独立做“非终态规范化”：CR 后是否跟 LF、末尾空白后是否为行尾均不在段内判定，结果携带 pending CR 偏移和 pending 空白区间。拼接顺序读取相邻原始字节：下一段首字节为 LF 时，前一 CR 删除，由 LF 生成换行；否则前一 CR 生成一个换行。下一段解析出的首字节为行尾时，累计的跨段空白删除；为普通字节时，按恒等区间补输出；下一段仍全是空白则继续累计。待定空白总长受同一缓冲上限约束，CR 至多一个，所以修正范围有上界。所有段内区间先加该段原始/输出基址，再追加；删除 CR、删除或补写空白只改边界处的短区间。终态策略最后只作用于完整结果的连续尾部。
