# 设计推导

## 1. 零计数 hunk 头的行号

统一格式把 hunk 描述为旧文件区间 `[a, a+b-1]`、新文件区间 `[c, c+d-1]`，二者均为
**1 基行号**，`b/d` 是该区间的行数。当 `b=0` 时区间为空，其起点等价地写成
「前一行的行号」：

- 文件开头插入：前面没有行，故 a=0，即 `-0,0 +1`（样例 1）。
- 在旧第 2 行与第 3 行之间插入：前一行是旧第 2 行，故 `-2,0 +3`（样例 2）。
- 旧文件唯一一行被删尽：空区间是 `[2,1]`，a=1，故 `-1 +0,0`（样例 3）。

`Apply` 的解释：旧侧删除行为空时，匹配窗口为空，插入点放在**旧侧第 a 行之后**
（a=0 即文件开头；a=len 即文件末尾）。新侧同理：空插入点为新第 c 行之后。

| b | 头书写 | Apply 解释 |
|---|---|---|
| 0 | `-a,0`，a=前一行行号（开头为 0） | 不匹配删除行；插入点在第 a 行后 |
| 1 | `-a`（省略 `,1`） | 删除旧第 a 行；替换从第 a 行开始 |
| ≥2 | `-a,b` | 删除旧第 a..a+b-1 行，首行号为 a |

新侧 d 的三种情形完全对称。

## 2. hunk 合并阈值

GNU diff 每个 hunk 至多在改动两端各保留 C 行上下文。两段相邻改动相隔 g 行未改动行：
合并时这 g 行全部保留；分开时前一段末尾留 C 行、后一段开头留 C 行。

- g ≤ C：不合并就连 C 行上下文都放不下 → 必须合并。
- g = 2C：正好分成 C+C，上下文总数等于 g，合并也只保留 g 行 → **分界点，GNU 选择合并**。
- g = 2C+1：分开只保留 2C 行、中间丢弃 1 行；合并会多保留 1 行 → 必须分开。

故阈值为 **2C**：`g ≤ 2C` 合并，`g > 2C` 分开。取 C 则 g=2C 被错误分开（与 GNU
不符）；取 2C+1 则 g=2C+1 被错误合并（多出 GNU 不会输出的一行上下文）。

## 3. Myers 并列最短路径的选择

Myers 逐层 D、逐对角线 k 展开；同一 D 上由 k+1（向下，删除）与 k-1（向右，插入）
汇合。约定优先走删除（向下）：`prev[k+1] >= prev[k-1]` 时取删除分支。回溯得到的
脚本中，并列情形下删除块排在插入块之前（如 `a b → b a` 输出 `-a` 行在 `+a` 行前）。
snake 推进吃掉的相等行输出为上下文行。该选择固定，故同一输入的补丁文本逐字节确定。

## 4. 偏移应用后后续 hunk 的起点

前一 hunk 以偏移 +k（实际匹配位置 = 记录位置 + k）应用成功后，它给其后所有行带来
净位移 =（本 hunk 插入行数 − 删除行数）。后续 hunk 的查找起点平移
`k + (ins − del)`：`ins − del` 是 hunk 自身内容造成的结构性位移（即便零偏移也存在），
`k` 是该 hunk 在旧文本中错位的位置，之后的 hunk 通常整体带着同样的错位。平移后仍在
起点上下 F 行窗口内取候选，多候选取离起点最近、同距取靠前者。
