# 条件请求前置判定器 — 设计推导

三个包单向依赖：`precond` → `etag`、`httpdate`；后两者互不依赖、不依赖 `precond`。

## 1. 为什么 If-Match 用强比较、If-None-Match 用弱比较

`If-Match` 的用途是「仅当目标还是我以为的那个字节序列时才执行」，
典型场景是断点续传（Range + If-Match）与乐观并发写。弱标签 `W/"x"` 只承诺
语义等价，不承诺字节一致。若 `If-Match` 用弱比较，客户端拿着 `W/"x"` 的
缓存去续传，而服务端资源已是字节不同的 `"x"`（语义等价但内容偏移全变），
判定却通过，续传会把新旧两段字节拼成损坏文件。故 `If-Match` 必须强比较：
任一方是弱标签即判不等。

`If-None-Match` 的用途是「内容没变就别重发/别覆盖」，只关心语义等价，
`W/"x"` 与 `"x"` 应视为同一实体，故用弱比较。若两边都用弱比较，上述
断点续传场景就会放行字节不一致的资源，产生静默数据损坏。

## 2. 为什么 If-Match 存在时忽略 If-Unmodified-Since

RFC 7232 规定：头字段之间存在覆盖关系，`If-Match` 优先于
`If-Unmodified-Since`，`If-None-Match` 优先于 `If-Modified-Since`；
且 `If-Match`/`If-Unmodified-Since` 组整体先于另一组求值。原因：两类
前置条件表达的是同一意图（「资源没变才执行」），强校验（ETag）比弱校验
（秒级时间戳）更精确，两者叠加等于用不精确的条件去收紧精确的条件。

反例：资源 ETag 为 `"a"`，最后修改时间 T2；客户端发
`If-Match: "a"` 且 `If-Unmodified-Since: T1`（T1 < T2，时钟漂移或
客户端缓存了旧时间戳）。叠加判定会因时间条件失败而返回 412，但规范
要求忽略时间条件、ETag 匹配即放行——叠加给出了与规范相反的结果。
求值顺序因此为（RFC 7232 §6）：

1. `If-Match` 存在 → 强比较，不匹配即前置条件失败；通过则继续往下，
   且 `If-Unmodified-Since` 被整体忽略；
2. 否则 `If-Unmodified-Since` 存在 → 时间判定，失败即前置条件失败；
3. `If-None-Match` 存在 → 弱比较，匹配则读方法未修改 / 写方法失败；
   不匹配则继续执行，且 `If-Modified-Since` 被整体忽略；
4. 否则 `If-Modified-Since` 存在 → 未修改则返回未修改；
5. 以上都未命中 → 继续执行。

## 3. 为什么同一「匹配」在读/写方法下结果不同

`If-None-Match` 匹配表示「你已有的就是最新的」。对 GET/HEAD，服务端
无需重发实体，返回 304 Not Modified 让客户端用缓存即可——这不是错误。
对 PUT/POST 等写方法，匹配意味着「你想覆盖的正是当前版本」，但
`If-None-Match` 的语义是「仅当不存在时才写」，故必须拒绝并返回
412 Precondition Failed。两者对客户端是截然不同的信号（用缓存 vs
操作被拒绝），所以判定器必须产出可区分的两种结果。

## 4. 相等时刻的归属推导

HTTP 时间戳精度为 1 秒，「最后修改时刻 ≤ 时间戳」时，时间戳无法证明
资源在其之后又被修改过。`If-Modified-Since: T` 的语义是「若晚于 T
修改过才重新执行」，相等时无法断定已修改，保守归为**未修改**（继续用
缓存），即「最后修改 > T 才算已修改」。`If-Unmodified-Since: T` 的语义
是「仅当未晚于 T 修改才执行」，相等同样无法断定已修改，故归为未修改、
**通过**。两条规则共享同一比较：`modified = lastModified.After(T)`，
`If-Modified-Since` 在 modified 时继续、否则未修改；`If-Unmodified-Since`
在 modified 时失败、否则通过。

## 解析失败即忽略

条件头部解析失败（非法 ETag 列表、非法日期）时按「该头部不存在」处理，
而不是整体报错：头部可能由中间件生成，客户端不应因一个无法理解的头部
而失去整个请求。时间解析对四类错误（非 GMT 后缀、非法月份名、不存在的
日期、多余空白）返回彼此可区分的错误，便于定位，但判定层一律吞掉。
