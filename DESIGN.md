# 设计推导

## 末尾换行策略

先计算基础规范化：统一 EOL、删行尾空白，其余字符原样。设输入经基础规范化后为 `b`。

- `Keep`：输出 `b`，空输入仍为空；幂等因为基础规范化幂等。
- `EnsureOne`：空输入输出 `"\n"`，避免规范化器凭空删字节；否则删除末尾所有空行后补一个 `"\n"`。再次运行时已有且仅有一个末尾换行。
- `TrimBlank`：空输入仍为 `""`（没有行可终止）；非空则删除末尾全部空行后补一个 `"\n"`。非空结果总以换行结束，再次运行不变。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 被删字节的 ToOut

把源区间到输出区间看成左闭右开的单调分段映射。删除区间本身无输出位置；为保证单调且不跨过换行，区间内偏移映射到“删除开始时的输出位置”。因此行尾空白映射到换行符之前；`"\r\n"` 的 `\r` 也映射到输出换行符之前。EOF 处删除的空白映射到当前输出终点。输出偏移只指向保留区间，满足 `ToOut(ToOrig(o)) == o`。

EnsureOne 对空输入追加的 `\n` 无源偏移：其起点映射到源 0；该位置的正向查询折叠回 0。非空补换行时，追加区间从“最后保留字节的起点”开始记录，保证逆映射成立；删除的尾部空白仍映射到删除发生处。

## 缓冲与上限

空白在看到换行前不能判定：若是行尾要删除，若是普通空白要保留，所以必须保存原始待判定片段。超限选择拒绝而非强制输出：强制输出后若下一字符是换行，输出和映射将无法撤回，违反跨切分、幂等、双向映射。拒绝返回带源偏移的哨兵错误，已输出保留；之后写入或关闭返回终态错误，状态仍可从已输出、待判定片段和偏移解释。输出上限同理拒绝，保证结果要么是合法完整前缀语义，要么不产生伪造输出。

## par 拼接

每段独立做不应用末尾策略的流式规范化，得到稳定前缀、尾部 carry（可能含待定 CR 或空白）和段内 span。顺序拼接时，只把上一段 carry 与下一段原文重新送入一个小型 normalizer，丢弃该段从 0 开始算出的临时结果；之后保留该段稳定输出。carry 上界为一个 CR 加一串空白，空白长度受缓冲上限约束，所以修正量有界。段映射先平移源/输出坐标，再删除被修正段前缀对应的旧区间，接入重算映射。最后一段的 carry 在 Close 时应用全局末尾策略。K 个独立 normalizer 由 goroutine 运行，顺序协程只做有界修正。
