# 设计推导

## 1. 空行与单列空值
不变量：末尾换行不产生记录（否则单记录文件无法终止于换行）。
因此解析时只有"见到分隔符/结束符才落字段、见到行尾才落记录"。
空输入 → 0 记录；`\n` 是"一条空记录的终止符"，其末尾换行不额外产生记录 → 1 条空记录；
`a\n\n` 中第二个 `\n` 终止一条空记录 → `a` 与空记录各一条，即**空行 = 含一个未引号空字段的记录，不跳过**。
跳过空行会让 `a\n\n` 丢数据，故选不跳过。
回写矛盾：单列表中一条空记录，不加引号写成 `` 时会得到空串：`a\n\nb` 与 `a\n` + 空记录 + `\nb` 无法区分
（空记录没有自己的字节）。故 writer 对"单列、唯一字段为空"必须写 `""`，字段变为有引号的非空串。
规范输入定义：不含孤立 CR；行尾一律 `\n`；非末尾记录间恰好一个换行；单列空字段写成 `""`；
其余字段仅在含 `,` `"` `\r` `\n` 时加引号，`"` 双写。规范输入可逐字节往返。

## 2. par 切点
切点可落在任意字节。worker 不知道起点真实状态，采用**双假设**：
假设 OUT（起点在引号外，处于新字段开始态）与 IN（起点在引号字段内）各完整解析本段。
前一段的真实结束态决定下一段取哪个结果：OUT 结束于引号外（含 CR 待定，见 3）→ 选 OUT；
结束于引号内（quoted 或刚见引号）→ 选 IN。段 0 以 OUT 起。从左到右即可唯一确定全部状态，
等价于把同一条确定状态机在切点处打断，故事件流与单线程逐字节相同。
两个边界特例：起点处于"刚见引号"（引号计数奇偶在段首悬空）时，IN 结果从段首扫描连续引号串，
结合前段结束态判定悬空引号属于闭合还是转义，必要时丢弃/续跑前缀（该续跑只触碰连续引号，
不计入长字段重扫）；起点处于 CR 待定时只需重放段首 1 字节（见 3）。
每个 cell 都带全局字节偏移，worker 直接给段内偏移加段起点；记录号/字段号是拼接事件计数，
只在拼接时赋全局序号；错误同样携带段内偏移，加段基址即全局偏移，记录号/字段号随拼接计数得出。
每字节被两个假设各处理一次 ≤ 2N，特例重放为每段常数。

## 3. CR 待定
未引号态见 `\r` 不能立即区分 `\r\n`（行尾）与孤立 `\r`（非法内容），进入 crPending：
下一字节是 `\n` → 落记录；其他字节 → 在该 `\r` 偏移报 ErrBareCR（孤立 CR 在未引号字段中非法）。
半包续传时 crPending 可挂起任意久：Feed 边界处保留状态与字段起点偏移即可。
流在 `\r` 结束：Close 时仍在 crPending → 视为孤立 CR 报错（没有等到 `\n`）。
引号字段内 `\r` 是普通内容，无待定状态。par 切点落在 `\r`/`\n` 之间时，前段以 crPending 结束，
后段 OUT 假设只消费段首那一字节做二选一判定，之后与 OUT 正常结果对齐。

## 4. 上限的"立刻"
单字段长度按**语义字节数**计：未引号字段每字节 +1；引号字段内每内容字节 +1，`""` 对整体贡献 1
（一个引号字符）。计数在字节进入对应状态的同一动作里递增并比较 MaxFieldBytes，
第一个超限字节到达即返回 ErrFieldTooLong，不等字段闭合。MaxFields 在逗号落字段时计数比较；
MaxRecords 在记录落定时比较。出错即终态，Feed/Close 永久返回同一错误（已产出记录保留在表中）。

## 5. 状态转移表
列：状态 × 字节类别（`,` | `"` | `\n` | `\r` | 其他）。

| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| fieldStart(新字段) | 落未引号空字段→fieldStart | →quoted | 落字段+落记录→fieldStart | →crPending | 起始未引号字段→bare |
| bare(未引号中) | 落字段→fieldStart | ErrBareQuote | 落字段+落记录 | →crPending | 追加字节 |
| quoted(引号中) | 内容 | →qPending | 内容 | 内容 | 内容 |
| qPending(刚见引号) | 落引号字段 | ErrQuoteAfterClose | 落字段+落记录 | →crPending(字段已闭合) | ErrQuoteAfterClose |
| crPending(CR待定) | 落字段+落记录 | ErrBareCR(于挂起CR偏移) | 落字段+落记录 | 同上(先报) | ErrBareCR(于挂起CR偏移) |

Close：quoted/qPending 在引号中结束（qPending 视为已闭合，落字段）按上表处理；
bare/fieldStart/crPending 结束 → crPending 报 ErrBareCR，其余正常落字段落记录。
错误类型：ErrBareQuote、ErrQuoteAfterClose、ErrUnclosedQuote、ErrBareCR 四类彼此独立；
另有 ErrFieldTooLong、ErrTooManyFields、ErrTooManyRecords、ErrTerminal。
