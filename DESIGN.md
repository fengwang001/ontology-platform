# 设计推导：text/event-stream 增量解析器

包依赖单向：`linescan`、`field` 互不依赖，`stream` 依赖二者。

## 1. CRLF 跨块时为何不会变成两个空行

`linescan` 的切分状态机只有两个状态：

- `normal`：正常累积字节。遇 `\n` → 派发当前行并回到 `normal`；遇 `\r` →
  派发当前行并进入 `cr`；其它字节 → 累积。
- `cr`：上一个字符是悬挂的 `\r`，它已经完成过一次“行尾派发”。此时遇
  `\n` → 判定为 CRLF 的尾字节，**吞掉并回到 `normal`，不再派发**；遇
  `\r` → 旧 CR 已成行尾，再派发一次空行并留在 `cr`；其它字节 → 派发空行
  （`\r` 之后另起一行），再把该字节作为新行首字符累积。

块边界落在 `\r` 与 `\n` 之间时，`\r` 随前块触发一次派发并把状态留在
`cr`，后块首个字节 `\n` 被吞掉，整段只产生一次行尾，故
`\r\n` 前后为空内容时只得到一个空行，而不是两个。

## 2. 切分点无关需要保留哪些状态

不变量：解析器状态只由“已经喂入的完整字节前缀”决定，与该前缀被切成
多少块、块边界落在哪无关。

跨块必须保留的状态：

- 行扫描器中的半行缓冲 `buf`，以及悬挂 CR 标志 `cr`；
- 事件聚合缓冲：`data`（含已出现过 data 行的标志）、`event`（含是否被
  设置的标志）；
- 持久重连状态：`lastEventID`、`retry`；
- 流首 BOM 是否已检查的标志。

逐字节喂入等价于切块的最细粒度；每来一个字节都按第 1 节的同一转移表
推进。任意切块方式下，块内依次应用相同转移，到达的状态与逐字节结果逐
步相同（归纳：前缀相同 → 状态相同；追加相同字节序列 → 状态相同）。
因此任意切分点产生的行序列、进而事件序列完全相同。

## 3. 字段推导与第 4 条：只有注释的块为何不派发

`field.Parse`：首字节是 `:` -> 注释（`Comment=true`）；无冒号 -> 整行为名、
值为空；有冒号 -> 冒号左侧为名，右侧去掉**恰好一个**前导空格（没有空格
则原样，两个空格则保留一个）。空行在进入字段解析前由 `stream` 直接识别。

派发判定：空行到来时，仅当“出现过 data 行”或“设置过 event 名”才派发。
注释行与未知字段既不写 data 也不设 event，所以只有注释的块不派发。若
错误派发空事件，下游会凭空收到一次回调（数据为零字节、事件名为默认
`message`），把心跳/注释节奏误判为真实业务事件，破坏事件流语义。

## 4. 第 9 条：超限为何必须已聚合状态零变化

`Feed` 先在**临时副本**上扫描：任一逻辑行超过行上限返回 `ErrLineTooLong`；
任何时刻 data 累积超过事件数据上限返回 `ErrDataTooLong`。两个错误是不同
哨兵值，可 `errors.Is` 区分。只有全部扫描成功，才把扫描结果提交到接收方。

截断后继续会让事件序列依赖块内“先看到多少字节”：同一字节流在恰好超限
处切块与不切块时，截断保留的前缀不同，后续字段可能落到被截断的事件或新
事件里，派发出的事件内容随切分点变化——直接破坏第 2 条“任意切分点事件
序列完全相同”的断言。原子拒绝保证：错误等价于这次 `Feed` 从未发生。
