# 严格模式流式 Base64 设计

## 规范尾部的推导

一个 4 字符组解码出 24 比特。无填充时 4 个 6 比特值全部承载数据；
有填充时，只使用其中若干个 6 比特值，剩余比特必须全部为 0。

- `XX==`：只用第 1 个值的 6 比特与第 2 个值的高 2 比特，凑成 1 字节；
  第 2 个值的**低 4 比特**必须为 0。
- `XXX=`：用前两个值（12 比特）与第 3 个值的高 4 比特，凑成 2 字节；
  第 3 个值的**低 2 比特**必须为 0。

因此判定「非规范尾部」只看两个位置：
`XX==` 看第 2 个值的低 4 位；`XXX=` 看第 3 个值的低 2 位。
非零即拒绝。`QR==` 中 `R` 的值为 17，低 4 位为 1，故拒绝；
而 `QQ==` 中 `Q` 值为 16，低 4 位为 0，合法，输出 `A`。

这与编码结果唯一的关系：若允许那些尾比特非零，同一个字节串就存在
多个合法编码（如 `A` 可写成 `QQ==` 或 `QR==` 等）。强制尾比特为 0，
正是 Encode 产生的形式，从而「任意字节串 ↔ 其编码结果」一一对应，
`Encode(Decode(y)) == y` 才可能成立。

## 语义与测试对照表

<!-- 测试完成后补齐 -->
