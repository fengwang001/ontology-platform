# 设计说明

严格解码器只接受自身就是规范编码的文本，不变量为 `Encode(Decode(t)) == t`。

1. 显式次数 `1`：`1a` 解码为 `a`，但 `Encode(a)="a"`，不是 `1a`。
2. 次数 `0`：`0a` 不对应任何游程；即使解为空串，其规范编码也是空串。
3. 前导零：`01a` 若按 1 次解码会得到 `a`，规范编码是 `a`；十进制写法也因此不唯一。
4. 相邻同符号：`2a3a` 解码为 `aaaaa`，规范编码是 `5a`。
5. 非法转义：`\b` 中的反斜杠不可能由编码器产生；反斜杠后只允许数字或反斜杠。
6. 末尾孤立反斜杠：`a\` 没有转义符号，解码结果若取 `a\` 之外的任何值，重新编码都不会得到原串。
7. 只有次数没有符号：`12` 不是完整游程；不存在能重新编码出该尾巴的字符串。
8. 非法 UTF-8：编码中的未转义符号必须是 Unicode 码点，非法字节不可能由合法字符串的编码产生。

大次数选择“支持任意十进制精度，但限制展开输出”：`runs` 用 `math/big` 逐位累加，杜绝固定宽整数溢出；`rle` 不按次数预分配，只在符号边界检查 `已输出字节数+次数×码点字节数`，超过可配置上限立即返回可判定错误。默认上限为 64 MiB；负数上限表示不限制。

## 语义测试对照

| 语义 | 测试函数 |
| --- | --- |
| 往返与规范编码 | 待实现 |
| 严格拒绝清单与偏移 | 待实现 |
| 大次数与输出上限 | 待实现 |
| 最长游程且不规范化 | 待实现 |
| 跨切分点与字节计数 | 待实现 |
