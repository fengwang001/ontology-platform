# .properties 解析器/回写器设计

## 空白续行的推导（第 3 条）
- 依据：JDK `Properties.LineReader.readLine` 在每个物理行尾判断前一个字节是否为
  奇数游程的反斜杠；若是，丢弃该反斜杠，并以 `skipWhiteSpace=true` 的状态继续读
  下一物理行——续行只承诺「剥掉下一行的前导空白」，不承诺任何其他处理。
- 推导：若被接上的那一物理行只含空白或为空，前导空白剥离后它贡献 0 个字符；
  而它自己的行尾没有奇数反斜杠，逻辑行就此结束，续行链**不会**越过空白行延伸。
- 结论：`k=v\` + 换行 + `   ` + 换行 + `w=x` → `k`→`v`、`w`→`x`；文件末尾以单个
  反斜杠结尾时同理，该反斜杠被丢弃（`k=v\`(EOF) → `k`→`v`）。
- 与「续行首部空白被剥掉」的关系：空白续行不是特例，而是同一条剥离规则的必然
  推论——剥完即空，空行无续行符，链自然终止；是否续行只看各物理行自身的行尾。

## 回写转义的推导（第 6 条）
- 键必须转义：`\`（转义符本身）；`=`、`:`（分隔符，否则截断键）；所有空白
  （空格/制表/换页会结束键，换行/回车会结束整行）；行首的 `#`/`!`（否则整行被
  当作注释）。键中间的 `#`/`!` 不必转义。
- 值必须转义：`\`、换行、回车，以及**开头**的空格/制表/换页。
- 值中间的空格不必转义：load 在过分隔符后只跳过一次前导空白，此后到行尾的全部
  字符（含尾部空白）都属于值；值开头的空格必须转义，否则会被那次跳过吃掉。
- 值中的 `#`/`!` 不必转义：注释判定只看逻辑行第一个非空白字符，而值永远不在
  行首（前面至少有分隔符）；键可能出现在行首，故键首的 `#`/`!` 必须转义。
- 非 ASCII 按 UTF-8 原样输出，不写成 `XXXX`（load 对非 ASCII 字节无特殊处理，
  原样读回即可逐项相等）。

## 语义与测试对照表
（实现完成后补齐）
