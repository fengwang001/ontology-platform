# 游标分页器设计

## 1. 复合游标边界的推导

排序键是 `(score float64, id string)`，按字典序定义全序：先比 score，相等再比 id。
id 唯一，所以该全序在数据集上无并列，任意两行可严格比较。

**反例 A（只记 score，用 `>`）**：十行中六行 score 相同（如都为 1.0，id 为 r0..r5）。
第一页取 r0,r1,r2 后游标记 `v=1.0`；下一页取 `score > 1.0`，r3,r4,r5 被整批跳过 —— 漏。

**反例 B（只记 score，用 `>=`）**：第一页取 r0,r1,r2 后游标记 `v=1.0`；
下一页取 `score >= 1.0`，r0,r1,r2 再次出现 —— 重。

**正确做法**：游标记录上一页最后一行的完整复合键 `(v, id)`，下一页边界为复合键的
**严格大于**：`(score, id) > (v, cursorID)`，即 `score > v || (score == v && id > cursorID)`。
因为 id 唯一，等于游标键的行恰有一行（即上一页最后一行本身），严格大于既不漏掉同
score 的后续行，也不会重复已返回行。反向对称：边界为复合键的**严格小于**。

## 2. 反向翻页规则

反向取页不是把正向结果倒序。规则：

- 比较方向反转：取复合键**严格小于**游标键的行；
- 取数方向反转：取满足条件行中**最大的 size 行**（即紧邻游标之前的一段）；
- **页内顺序仍按正向（升序）排列**，因此反向翻一页得到的页与正向对应页逐行相同；
- 续翻游标取本页**第一行**的复合键（正向续翻取最后一行）。

空游标表示端点：正向空游标 = 从头开始；反向空游标 = 从末尾开始。

## 3. 并发增删下的语义承诺

不做快照隔离：翻页过程中若有行被插入到**已翻过**的区间，允许它不出现；
插入到未翻过区间的行，会在后续页中按其复合键位置出现。承诺两条：

1. **不重**：已返回过的行绝不再出现 —— 边界是严格大于/小于已返回页的端点复合键；
2. **不漏**：未被删除的行不会因其复合键区间被跳过而丢失 —— 每页边界连续衔接，
   区间 `(上一页末键, 本页末键]` 内现存行恰好是本页内容。

删除安全：游标只携带复合键的值，不持有行的引用；指向已删除行的游标仍以
`(score, id)` 值定位，可继续翻页。

## 4. 效率约束

- 每次翻页先取一次一致性快照（读锁下拷贝切片头数组），之后为纯函数：
  二分定位 O(log N) 次比较，再逐行校验边界至多 size 次。
  单次翻页比较数上界 `4 * (size + ceil(log2 N))`，绝不从头全扫。
- 游标编解码 O(1)：定长头（方向 1B + score 8B + id 长度 2B）+ id 字节 + CRC32 4B，
  解码分配次数为常数，与数据集规模无关。

## 5. 游标格式与故障分类

```
[0]        方向标记：0xC1=正向 0xC2=反向
[1:9]      score 的 IEEE754 位，大端
[9:11]     id 长度 uint16，大端
[11:11+n]  id 字节
末尾 4B    CRC32-IEEE（覆盖此前全部字节）
```

解码校验顺序：长度下限与 id 长度字段一致性（`ErrTruncated`）→ 方向标记合法性
（`ErrDirection`）→ CRC 校验（`ErrChecksum`）。三类错误均为哨兵错误，可用
`errors.Is` 区分；跨方向复用（正游标用于反向翻页）同样报 `ErrDirection`。
单比特翻转不改变长度，故全部变体落入「方向非法」或「校验失败」两类，
任何变体都不会被接受（统计见 FINDINGS.md）。

## 6. 并发模型

`row.Store` 用 `sync.RWMutex` 保护有序切片；增删在写锁内二分定位后插入/移除。
翻页只读：取快照后无锁计算，无隐藏推进状态。同一游标被多协程并发使用时，
各自得到的页逐行相同（翻页是纯函数）。`-race` 必须干净。
