# CSV 解析器设计

## 推导

### 1. 空行与单列空值
空行（两个行尾相邻，含开头行）**跳过**：否则无法与「无换行的空输入」区分，且 RFC 4180 不含空记录。因此单列记录要表达唯一的空字段，必须写成 `""`（加引号）；若写成空字节，写出的只是空行，再读回会被跳过，丢一条记录。故回写器在「单列表且字段值为空」时必须加引号。

### 2. par 切点
切点可落在引号内，本段无法自行判态。每段以两种假设各解析一遍：A 起点在引号外、行首；B 起点在引号字段内、延续前段字段。另取段首前一字节推导 CR 待定。从左至右：首段选 A；此后由前段结尾真实状态决定本段选 A 或 B（B 时用前段首字段元信息做种子），并把该段 A 假设产生的「行首空字段」按是否真空行丢弃。各事件偏移一律用全局绝对偏移；段内错误的记录号加前段记录数、字段号在跨字段时加 1。每字节至多算两遍，计数 ≤ 2N+C。段长 0 时不跑 worker，状态原样传递。

### 3. CR 待定
bare/qseen 下遇 `\r` 进入 `cr`：不发事件、不结束记录；下一字节为 `\n` 则按行尾处理（字段保持原始 `\r`?——不，`\r` 属行尾，不入字段），为普通字节则报孤立 CR。半包时 `\r` 留在待定态等下一 Feed；EOF 仍在 `cr` 则按行尾正常结束（容忍无尾换行的对称：`\r` 流尾视为记录终止）。par 中切点在 `\r`、`\n` 之间时，前段挂起态为 cr+lf 期待，后段首字节 `\n` 即终结该记录，与单线程同。

### 4. 上限「立刻」
字段字节数按**解码后字符**计：普通字节 +1，引号字段内 `""` 整体 +1 且在第一引号到达时即计。每来一个计数单位立即与上限比较，超限即在当前字节偏移报错，不缓冲整记录；随后进入终态，Feed/Close 恒返回同一错误。

## 状态转移表

状态：S=字段开始 B=未引号字段 Q=引号字段 E=引号内见引号(待判) C=CR待定。事件：f 字段完成, r 记录完成。

| 状态 | `,` | `"` | `\r` | `\n` | 其他 |
|---|---|---|---|---|---|
| S | f→S | →Q | →C | f,r→S | →B |
| B | f→S | 错误:裸引号 | →C | f,r→S | 追加 |
| Q | 追加 | 追加,""计1 | 追加(内容) | 追加(内容) | 追加 |
| E | f→S | 追加→Q | →C | f,r→S | 错误:引号后杂符 |
| C | 错误:孤立CR | 错误:孤立CR | 错误:孤立CR | f,r→S | 错误:孤立CR |

EOF：S 且行首→无记录（纯空/尾换行）；S→f,r；B/E→f,r；Q→错误:引号未闭合；C→f,r（容忍）。
错误均带字节偏移、记录号、字段号（从 1 起，偏移从 0 起）。单个流式解析器非并发安全。
