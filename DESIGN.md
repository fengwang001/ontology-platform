# DESIGN：严格模式流式 Base64

## 第 2 条「规范形式」的推导

Base64 把 3 个字节（24 bit）编码为 4 个字符（4×6 bit）。当输入长度不是 3 的倍数时，
末尾会剩下 1 或 2 个字节（8 或 16 bit），不是 6 的整数倍，于是最后一个编码字符的
6 bit 里有一部分比特**不承载任何输入数据**：

- 剩 1 字节（8 bit）：第 2 个字符只用高 2 bit，**低 4 bit 空闲**，形如 `XX==`。
- 剩 2 字节（16 bit）：第 3 个字符只用高 4 bit，**低 2 bit 空闲**，形如 `XXX=`。

编码器按 RFC 4648 必须把这些空闲比特置 0。但宽松解码器只取高位、直接丢弃低位，
所以 `QQ==`（Q=16，低 4 bit 为 0）和 `QR==`（R=17，低 4 bit 为 1）都解出 `A`。
这意味着：**如果接受 `QR==`，则同一个字节串 `A` 存在两个不同的合法编码**
（`QQ==` 与 `QR==`，事实上低 4 bit 有 16 种取值），编码不再唯一，
`Encode(Decode(y)) == y` 不再成立，编码结果也无法做相等性比较/签名/去重。

由此推出统一规则（规范形式，canonical form）：

> **严格模式只接受编码函数值域中的串**：尾组空闲比特必须全 0。
> 具体判定只看两处——`XX==` 组检查第 2 个字符 6 bit 值的**低 4 bit**，
> `XXX=` 组检查第 3 个字符 6 bit 值的**低 2 bit**；非 0 即拒绝。

配套规则（同源于「编码结果唯一」）：

- 填充只能出现在整个流的末尾，且数量必须恰好补齐到 4 的倍数：
  `QQ`（长度非 4 倍数）、`QQ=`（填充不足）、`QQ===`（填充过多）均拒绝。
- 填充之后不允许再出现任何数据字符：`QQ==QQ==` 拒绝。
- 空输入合法：空串是空字节串的唯一编码。

这样严格解码恰好是严格编码的左逆，对任意字节串编码结果唯一，
往返性质 `Decode(Encode(x)) == x` 与 `Encode(Decode(y)) == y` 同时成立。

## 语义 ↔ 测试对照表

（待测试写完后补齐）
