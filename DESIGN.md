# 设计推导

全局单调序号 `next`（从 1 开始），`head = next-1` 为最新序号。环内现存条数 `live`，
故 `oldest = head-live+1`（空环时无定义）。序号只增、不复用，槽位映射 `slot(seq)=(seq-1)%N`。

## 1. 满与空如何区分

若只比较「写入位置」与「读取位置」是否重合，二者都会落在同一个槽，无法区分。
因此不靠位置重合，而显式维护现存条数 `live`（0..N）：

- 一条没写：`live=0`，此时空，无可读记录。
- 刚好写满 N 条：`live=N`，位置再次重合，但满。
重合在两种状态都会出现，`live` 给出唯一判定：`live=0 ⇔ 空`，`live=N ⇔ 满`，无歧义。
追加时若 `live<N` 则 `live++`，写满后再追加转为覆盖：`oldest++`、`live` 保持 N。

## 2. 掉队的判定边界（差 N 与差 N+1）

读者持「下一条想读的序号」`want`。可读区间为闭区间 `[oldest, head]`。
判定只看序号、不看槽位，因此绕圈不会把过期槽位误当新数据：

- `want >= oldest` 且 `want <= head`：可读；
- `want > head`：追上写入者（暂时无新记录，不是掉队）；
- `want < oldest`：已掉队，覆盖的条数 `missed = oldest - want > 0`。

读者 `want = head-N+1`（与最旧一条重合）时，它「恰好落后 N 条」：
该槽仍是活数据（`want == oldest`），可读，不算掉队。再覆盖一条后 oldest 前移，
同一读者变成 `want = oldest-1`，即「差 N+1 条」：它要的那条已被覆盖，判掉队，
`missed=1`。**分界：落后 N 条仍可读，落后 N+1 条掉队；「即将被覆盖的那一条」在被覆盖
之前不算掉队，覆盖完成的同一刻才算。** 与第 1 条一致：满环（N 条全活）仍是可读状态，
满环上的下一次追加才制造掉队。

## 3. 恢复到哪一条：读到还是跳过

恢复把 `want` 重置为当前 `oldest`。不变量 3 要求「恢复后读取序号连续、不再重复报掉队」。
掉队错误已声明 `[want, old)` 这些序号永久丢失。若恢复后跳过 oldest 从 `oldest+1` 开始，
则 oldest 既没在丢段里、也永远读不到，序号序列出现空洞，违反连续；若从 oldest 之前开始，
则会再次 `want < oldest` 而重复报掉队。故恢复后 **第一次 Read 必须读到 oldest 本身**，
之后随 head 连续前进，不再报错。

## 4. 注销最慢读者后最慢落后量立即重算

落后量语义是「当前存活读者中落后写入者最多的量」，与逐读者核对的结果必须一致（不变量 5）。
若注销后保留历史值，该值将不再对应任何存活读者，与逐读者核对必然不符。
故注销即刻从统计结构删除该读者并重算最慢值；所有活跃读者都追上时最慢为 0。

## 增量维护最慢落后量

每次 Append 都让全部读者落后量 +1，无法逐个改堆。改用「目标序号」建最小堆：堆顶是
`want` 最小的读者，即最慢读者；`slowest = max(0, head - heap.top.want)`。Append 不动堆，
仅 Read/注册/恢复/注销做一次 O(log R) 堆调整，查询只读堆顶（访问 1 条记录）。
掉队读者立即出堆、计入掉队计数，不参与最慢落后量。

## 位置关系对照表（读者 want 相对环 [oldest, head]，live 条）

| 条件 | 状态 | Read 结果 |
|---|---|---|
| live=0（环空，任意 want） | 空 | ErrNoNewRecord |
| want > head | 追上写入者，可读但暂无数据 | ErrNoNewRecord |
| oldest <= want <= head | 可读 | 返回序号 want 的记录 |
| want = head-N+1 = oldest（差 N） | 恰好落后 N，仍可读 | 返回最旧记录 |
| want < oldest（差 N+1 起） | 已掉队 | FellBehindError，missed=oldest-want |
| live=N 且下一次 Append | 满→覆盖，旧 oldest 被覆写 | 持有旧 want 的读者此后掉队 |
