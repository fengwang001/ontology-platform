# 严格模式 Base64：规范尾部的推导

Base64 以 4 个 sextet（每个 6 bit）为一个量子（quantum），量子携带 24 bit，
即 0~3 字节。只有末量子允许携带 1 或 2 字节，并用 `=` 占满 4 格。

设末量子的四个 sextet 为 s0 s1 s2 s3。

- 3 字节（无填充）：24 bit 全部有效，三种余数的编码都合法且唯一。
- 2 字节，形状 `XY Z=`（即 s0 s1 s2 `=`）：前 16 bit 有效，
  落在 s2 高 4 位；因此 s2 的**低 2 位必须为 0**，否则这 2 bit
  不被输出却可以随意取值，同一字节串就会对应多个编码。
  例：`QUI=` 中 s2=`I`(8)，8 的低 2 位为 0，合法；
  `QUJ=` 中 s2=`J`(9)，低 2 位为 1，拒绝。
- 1 字节，形状 `XY==`：前 8 bit 有效，落在 s1 高 2 位；
  因此 s1 的**低 4 位必须为 0**。
  例：`QQ==` 中 s1=`Q`(16)，低 4 位为 0，合法；
  `QR==` 中 s1=`R`(17)，低 4 位非 0，宽松解码同样得到 `A`
  却不是编码 `A` 的产物，故拒绝。

统一规则：**被填充抹掉的那些 bit 必须全部为 0**（判定比特就是
8n mod 6 之后剩余的低位：1 字节查 s1 的低 4 位，2 字节查 s2 的低 2 位）。
这保证 `Encode` 是从字节串到编码串的单射：每个 x 只有唯一规范编码，
`Decode(Encode(x))=x`，且严格接受的 y 满足 `Encode(Decode(y))=y`。

其余规则：填充数只能是 0/1/2 个且仅在整个流末尾；总长度必须为 4 的倍数；
换行（`\n` 或 `\r\n`）仅 MIME 模式下、且只在量子边界（第 4 格之后）。

## 语义—测试对照表

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1 标准字母表与 `=` 填充、76 列 MIME 换行、末行无换行 | （收尾时补） |
| 2 规范尾部（全部样例与非规范 bit 拒绝） | （收尾时补） |
| 3 换行位置规则（MIME 开关/边界/`\r`/空白） | （收尾时补） |
| 4 五类错误可区分且带字节偏移 | （收尾时补） |
| 5 任意切分点逐字节一致（含 `\r|\n`、`=|=`） | （收尾时补） |
| 6 往返（余数 0/1/2，MIME 开/关，含空串） | （收尾时补） |
| 7 输出上限：组边界停下、保留已输出、终态 | （收尾时补） |
