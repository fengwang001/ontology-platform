# 严格模式 Base64：规范尾部的推导

3 字节输入占 24 bit，正好编码为 4 个 6-bit 字符，无歧义。余数情形需要补 0 到 24 bit：

- 1 字节（8 bit）→ 2 个字符承载 12 bit，后补 4 个 0 bit，后接 `==`。
  例：`A` = 0x41 → 010000 01**0000** → `QQ==`。第 2 个字符只有高 4 bit 是数据，
  低 2 bit 必须为 0。`QR==` 的 `R`(17) = 010001，低 2 bit 为 01，携带了输入
  中不存在的信息，必须拒绝。
- 2 字节（16 bit）→ 3 个字符承载 18 bit，后补 2 个 0 bit，后接 `=`。
  例：`AB` → `QUI=`；`QUJ=` 第 3 字符 `J`(9) = 0010**01**，最低 1 bit 必须为 0。

**统一规则**：补位区间内的每一个 bit 都必须为 0。即检查「最后一个非填充字符」的低位：
1 字节余数组检查第 2 字符低 2 bit（`c2&0x3 == 0`）；2 字节余数组检查第 3 字符
低 1 bit（`c3&0x1 == 0`）。

**与编码唯一性的关系**：若允许补位 bit 非 0，同一字节串有多个合法编码
（`A` 既可写成 `QQ==` 也可写成 `QR==`、`QS==`…），编码映射不再是双射，
`Encode(Decode(y)) == y` 也无法成立。补位 bit 全 0 使「字节串 ↔ 严格编码」
一一对应，拒绝正是恢复唯一性的必要条件。

填充其余规则：无填充 / 单 `=` / 三 `=` 一律非法；`=` 只允许出现在整个流的
末尾（即出现填充后，除关闭外不得再出现任何字符）。

## 语义—测试对照

| 语义条目 | 测试函数 |
|---|---|
| 1 标准字母表/填充/MIME 76 列 | （测试阶段补全） |
| 2 规范尾部样例 | |
| 3 换行位置规则 | |
| 4 五类错误可区分且带偏移 | |
| 5 任意切分点一致 | |
| 6 往返（含 MIME 两种设置） | |
| 7 输出上限在组边界停于终态 | |
| 计数器 == 输入字节数 | |
