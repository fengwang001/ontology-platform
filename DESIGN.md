# 推导与结论（规则即 WHATWG 式"首字节+第二字节窗口"状态机，由样例反推）

## 1. 非法单元切分（七个样例 → 一条规则）

状态机停在 ground。首字节按窗口要求取第二字节：第二字节合法则继续吃
continuation（每个 80..BF），其中任一字节缺失（EOF）/不是 continuation
时，**已吃前缀整体算一个非法单元**；第二字节非法，则首字节单独成单元，
该第二字节回到 ground 重新判定。ground 上 80..BF、C0 C1 F5..FF 各吞 1。

- `F0 90 80 41`：F0 窗口 90..BF，90 合法；期待 cont 却见 41 → 前缀 F0 90 80 是 1 单元，41 合法。
- `E0 80 80`：E0 窗口 A0..BF，80 非法 → E0 单独；余下 80、80 在 ground 各吞 1，共 3。
- `ED A0 80`：ED 窗口 80..9F，A0 非法 → ED + A0 + 80 各 1，共 3（代理区拒绝）。
- `C0 AF`：C0 非法首字节吞 1，AF ground 吞 1，共 2（非最短形式）。
- `F4 90 80 80`：F4 窗口 80..8F，90 非法 → F4 + 90 + 80 + 80 各 1，共 4（越界）。
- `E2 82`+EOF：82 在 E1..EC 窗口 80..BF 内合法，EOF 缺第 3 字节 → 已吃前缀整体 1 单元（不是 2）。
- `80 80`：ground stray continuation 各吞 1，共 2。

第二字节窗口本质：E0 排除 80..9F（拒绝 0000..07FF 的非最短 3 字节）；
ED 排除 A0..BF（拒绝 D800..DFFF 代理）；F0 要求 90..（拒绝非最短 4 字节）；
F4 只允许 ..8F（拒绝 >10FFFF）。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞 |
|---|---|---|
| C2..DF | 80..BF | 1（首字节自己） |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | —（ground stray） | 1 |
| C0 C1 F5..FF | —（非法首字节） | 1 |

注：第二字节合法、但后续 continuation 缺失/非法时，单元长度=已吃前缀（2..4）。

## 2. 缓存上限与"立即不等 EOF"

最长合法序列 4 字节：看到首字节后最多再缓存 3 字节（任意时刻待解析字节 ≤3，
`pendingCap=3`）。立即判非法、不必等 EOF 的时机：
(a) ground 见 80..BF / C0 C1 / F5..FF；(b) 首字节后第二字节落窗口外
（如 E0 80、ED A0、F0 80、F4 90）；(c) 合法窗口后见到非 continuation
（如 F0 90 80 41 见 41）。只有"前缀合法、后续字节尚未来"才必须等待；
EOF 时残留即截断/1 个非法单元。重试字节（被退回 ground 的字节）当次 Write
内消化，不跨 Write 缓存。

## 3. par 的切点对齐（任意字节偏移）

每段不假设起点对齐，先**向回看常数字节**重建状态：UTF-8 回看上一段最多
3 字节。起点处若处于"某首字节的未完成窗口"中，则跨切点的那个单元由**结束
它的那段**负责（统计按单元完成位置归属），该段从 ground 重放必要前缀但不
输出其开头部分；起点前已经完整结束的单元绝不重放。

- `C3 | 80 80`：段0 有 pending C3、未见续字节即结束，不产出；段1 从 ground
  开始：80 是 stray（单元1），80 是 stray（单元2）。单元归属看完成点，C3
  在段1 解析 80 时被一起吞为同一单元 → 合计 2 单元，与单线程一致。
- 回看常数（≤3 UTF-8 字节 / ≤3 UTF-16 字节）即常数开销，不整段重扫。
UTF-16：奇字节切点需回看 1 字节对齐 code unit；高代理跨切点时回看 1 code
unit；落在孤立低代理前照常从 ground 判定。BOM 只由段0 的流起点逻辑处理。

## 4. 第 9 条上限断点

输出前先判断"本标量编码后是否超 `MaxOut`"：超限则在标量前停下，返回
`ErrOutputLimit`，已输出标量与已返回的 `n` 严格对应。`n` 只含已被完整单元
（含其吞掉字节）消费的输入字节；尚在 pending 缓存里的字节**不算** `n`，
调用方把 `input[n:]`（含这些字节）喂给新实例。拼接因此逐字节等于无上限
一次转完。每字节在解码中只被检查一次（重试字节计第二次，常量上界），
故检查次数 ≤ 2×输入字节数；par 额外 ≤ 8×3 的回看常数。
