# 设计推导

## 3. 只含空白的续行

`Properties.readLine` 先保留行尾反斜杠前的全部字符，再把下一个物理行交给同一个循环处理。
新行开头若是空白，续行状态下只执行“跳过前导空白”；空白被耗尽后若立刻遇到 LF/CR/EOF，
本行没有任何非空白片段可追加，当前逻辑行就在该换行符处结束。
因此 `k=a\` 后接只含空白的一行，再接 `b=c` 时，结果是 `k=a` 与 `b=c`；
空白行不是“把空白追加为值”，而是其前导空白被剥离后贡献空片段。
空行连空白也没有，同样贡献空片段并结束续行。
这说明“剥离续行首部空白”发生在追加内容之前，且剥离不会吞掉终止逻辑行的行结束符。

## 6. Store 的最小必要转义

回写按首次出现顺序逐行输出 `key=value`，并追加结尾换行。
解析键时，行首空白会被忽略，未转义的 `#`/`!` 会让整行成为注释，
未转义的 `=`、`:`、空白会成为键值分隔符；所以键必须转义前导空白、首字符 `#`/`!`、
以及 `=`、`:` 和空白，键中的反斜杠、制表、换行、回车、换页也必须转义。
值位于分隔符之后，其中的 `#`、`!`、`=`、`:` 和中间空格没有语法含义，因此不必转义；
但值的第一个空格若直接放在 `=` 后，会与分隔符后的前导空白一同被吃掉，所以值开头空白必须写成 `\ `。
值开头的制表/换页同属会被跳过的空白，也要转义；尾部空白可原样保留。
反斜杠及控制字符按解析器能唯一还原的形式转义；非 ASCII 原样输出为 UTF-8。

## 测试对照表

（实现测试后补齐）
