# 设计推导

## 流格式（所有整数均为小端 uvarint）

| 记录 | 字节布局 |
|---|---|
| 流头 | magic "ONTO1" + varint windowSize |
| 字面量 | tag=0 + varint n + n 个原始字节 |
| 回指 | tag=1 + varint distance + varint length |
| 刷新 | tag=2（无负载） |
| 流尾 | tag=3 + varint originalLen + varint checksum(FNV-1a 64, 原文) |

tag 直接作为每记录首字节（不是位域）；记录连续排列，块间无需刷新标记。

## 推导一：压缩与 Write 切法无关

匹配器只能对“已知且其后仍有足够字节可比较”的位置取最长匹配。压缩器维护一个待决缓冲，
边界规则：对缓冲中除最后 maxMatch-1 个字节外的前缀做压缩，末尾至多 maxMatch-1
字节留待下次 Write（未来字节可能延长匹配）。这样任何位置的匹配长度只取决于最终完整输入，
与它跨过多少次 Write 无关。Flush 语义要求解压器立刻得到全部已交输入，因此 Flush 强制
把待决字节全部按“无未来字节”处理（匹配立即定长、剩余转字面量），再追加一条刷新记录；
无新输入时压缩器处于“已刷新”干净状态，第二次 Flush 不输出任何字节。

## 推导二：重叠回指的复制

距离 d、长度 L 且 d<L 时，源区与目标区重叠。一次性块复制会读到尚未写出或残留旧值；正确
语义是逐字节向前复制：out[o+k]=out[o+k-d]，读第 k 字节时它必已写出（k-d<k）。实现采用
逐字节循环：输出路径本身就要逐字节做 FNV 校验与上限检查，循环次数等于输出字节数，无额外
渐近开销；非重叠情形同样正确，不做特化。

## 推导三：两类“距离越界”

“距离超过已输出字节数”指流此刻可回指的逻辑历史长度；“距离超过窗口容量”是配置承诺的物理
上界。窗口 64KiB 的流在只输出 10 字节后出现 distance=100 属前者；输出 1MB 后 distance=
100000 属后者。压缩器只从窗口取候选且仅注册已进入窗口的位置，故绝不产生超窗口距离。并行
块预置字典把上一块末尾 W 字节放入窗口：它既是已输出历史（解压端按全局输出计数）也占窗口
容量，因此“已输出字节数”含义不变——解压端始终以累计输出长度为唯一历史长度，不知晓块边界。

## 推导四：并行块压缩的确定性

块 k 压缩时窗口只预置 data[本块起点-W:]（上一块末尾至多 W 字节），候选位置全部来自已落入
窗口的部分；匹配决策只依赖本块原文与上一块原文，不依赖任何块的压缩结果或 goroutine 完成
顺序。哈希链是确定算法，故 workers=1/2/4/8 对同一分块产生逐字节相同的记录序列。流上顺序
拼接各块记录即可：回指距离以全局历史为参照，解压端窗口天然跨块连续，块间不需要刷新标记
（刷新只用于流式 Flush 语义）。

## 复杂度

哈希链每位置至多追踪 chainLimit 个候选；计数器记录全部考察候选数，上界
chainLimit×输入字节数。全同字节串每位置考察数被链长上限钉住，与窗口大小无关。
