# JSON 字符串字面量严格编解码器 — 设计推导

包划分：`esc`（单个转义序列与代理对判定，零依赖）← `jstr`（字面量编解码与流式解码）。
错误一律为 `*jstr.Error{Kind, Off}`，`Kind` 可用 `errors.Is` 对照哨兵，`Off` 为字面量内字节偏移（开引号为 0）。

## 第 2 条：代理对错误偏移的推导

候选位置只有两个：高代理 `\uXXXX` 的起点，或其后那个不合格单元的起点。规则取**前者**：

- 高代理是「义务的产生者」：`\uD83D` 一旦完整读出，就产生一个必须紧跟 `\uDC00–\uDFFF` 的未决义务；后续无论来什么（`x`、`A`、另一个高代理、闭引号、EOF），失败原因都相同——该高代理无法成对。义务在流式状态下于任意切分点都可能悬置，唯一在一切切分方式下都已确定、且与后续消耗多少字节无关的位置，就是高代理自身的起点。
- `"\uD83D"`（紧跟闭引号）根本没有「后面那个单元」可指，若规则指向后者则此情形无定义；指向高代理起点则六种非法样例（孤立高、孤立低、`\uD83Dx`、`\uD83DA`、顺序颠倒、高后跟非低代理）全部统一覆盖。
- 孤立低代理 `\uDE00` 是另一种错误：它在**自己**的位置上就不合法（前面没有高代理），故偏移指向它自己的 `\` 起点。顺序颠倒 `"\uDE00\uD83D"` 在读出 `\uDE00` 时即报于偏移 1，根本不再读第二个单元。

`"\uD83DA"` 不能解释成「U+FFFD + `A`」：JSON 的 `\u` 恰取 4 位十六进制，`\uD83D` 已是完整转义，其值 U+D83D 不是 Unicode 标量，永远不能出现在任何良构 UTF-8/UTF-16 流中；RFC 8259 未定义任何替换行为。若静默换成 U+FFFD，则 `"\uD83DA"` 与 `"\uFFFDA"` 解码结果相同，Decode 不再单射、往返性质（第 5 条）被破坏，且错误被吞掉，违背严格性。故必须拒绝，偏移 = 高代理的 `\` 起点。

## 第 4 条：最小转义与唯一性的推导

RFC 8259 的 `unescaped` 产生式为 `%x20-21 / %x23-5B / %x5D-10FFFF`：必须转义的只有 `"`、`\` 与 U+0000–U+001F。U+001F 落在强制区间内，原样出现即非法字面量；U+007F（DEL）不在区间内，原样出现合法——「最小」即只转义强制集合，故 U+007F、`/`、非 ASCII、U+2028/U+2029 一律原样。

唯一性：每个输入码点只对应一种输出形式（8 个短转义、其余控制字符 `\u00XX` 小写、其余原样），映射是函数、无选择点，故输出唯一。单射：转义序列以 `\` 引导且定长（2 或 6 字节），`\` 绝不原样出现，故分词无歧义，Decode 是 Encode 的左逆；若 `Encode(a)=Encode(b)` 则 `a=Decode(Encode(a))=Decode(Encode(b))=b`。非法 UTF-8 输入直接判错（`Encode` 返回 `error`），不参与上述映射。

## 测试对照表（最后补齐）
