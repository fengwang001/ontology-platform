# 带租期的分布式序列号分配器 — 设计推导

## 1. 模型
多进程共享目录中的持久计数器 C。实例按「段长」从 C 批量租用号段 [s,s+n)，
本地逐号分发，租约带到期时间。包：clock（注入时钟）/ durable（持久计数器）/
lease（号段租约）/ alloc（本地分发+续租）/ audit（不变量自检）。

## 2. 持久化必须先于分发（崩溃不重号推导）
租到 [s,s+n) 时，必须先把持久值推进到 s+n（fsync+原子改名落盘成功），
然后才允许发出任何一个号。
反证「边分发边推进」：若持久值只记录「已发到哪」，序列
t0 取段[100,200)；t1 发出 100..149（在途响应，客户端可能已收到 149）；
t2 持久值仅推进到 150；t3 进程崩溃；t4 重启，读到 C=150，
从 150 继续发。于是 149（崩溃前已收到）与之后某号冲突 → 重号。
根因：分发（对客户端可见）与推进（崩溃恢复依据）不是同一原子事件，
崩溃窗口内已发出的号无法回滚。因此采用「整段预推进」：
测试场景「租到[100,200)、发到150崩溃」重启后首号必 ≥ 200。
代价是作废号段 [150,200) 成为空洞——不保证无空洞，只保证唯一/不回退。

## 3. 租约到期语义
租约含 start、length、expires（逻辑时钟纳秒）。now < expires 才有效；
now ≥ expires 时剩余号立即作废，下一次分配重新租用新段。
旧段剩余号永不再发（产生空洞）。TTL=0 的语义：租约在取得的同一时刻即到期，
故强制 length=1：取段→落盘→发出，每次分配恰好一次持久写；不允许 TTL<0。

## 4. 时钟回拨
时钟是唯一的时间来源且必须单调。每次读钟先与上次比较，now < last 即回拨，
返回 ErrClockMovedBack，拒绝一切分配——否则回拨可让 expires>now 而「复活」
已作废段，破坏 §3。回拨不改变持久值。

## 5. 段长自适应
维护 cur（当前段长），min≤cur≤max。每次成功续租：
段在到期前耗尽（快路径续租）→ cur=min(cur*2,max)（启动首次也翻倍）；
段到了期仍有余量（下次分配时才发现过期）→ cur=max(cur/2,min)。
TTL=0 时不调整，恒为 1。确定性序列：快路径续租 6 次 1000→…→64000；
随后时钟跳过到期、低速续租 6 次 → 回落至 1000。

## 6. 持久计数器格式、原子推进与损坏拒绝
文件布局（共 21 字节，大端）：magic(1)=0xA7，value(8 uint64)，
reserved(8，0)，crc(4，IEEE CRC32 over 前 17 字节)。
推进 = 写临时文件 → fsync → rename 原子替换 → fsync 目录。
跨实例串行化：同目录 flock 排他锁；进程内用 Mutex。
首次启动（文件不存在）→ 从配置起点建文件，不视为损坏。
损坏时绝不允许从 0 重新开始（否则与历史已发号大规模冲突），一律拒绝启动。
按截断长度 L（1..20）分类：L≤8 头部不完整 ErrCorruptHeader
（magic+value 未满；文件根本未形成一条记录）；
9≤L≤16 值/保留区不完整 ErrCorruptValue；
17≤L≤20 CRC 区不完整或校验不符 ErrCorruptCRC。三类可用 errors.Is 区分；
另有 ErrInvalidLength（n≤0）、ErrCounterOverflow（s+n 溢出）、
ErrWriteFailed（注入写失败，临时文件失败则旧文件与内存值不变）。

## 7. 性能与资源约束
10 万号、段长 1000：恰好 100 次持久写（要求 ≤120，留余量）。
本地快路径：取号=读钟比较+游标自增+余量比较，O(1)，用操作计数器断言 ≤常数。

## 8. 全局不变量（audit 自检）
唯一性：flock 串行化整段预推进 ⇒ 任意两段不相交 ⇒ 跨实例号互不重复。
单调性：单实例游标只增，且新段 start ≥ 旧段 end。
空洞：作废段 [consumed,end) 长度 = end-consumed，来源仅两类——
崩溃丢弃（进程死亡时未发完的段）与到期作废。audit 汇总空洞区间
总数与总长度，精确等式：holes == Σ作废余量 + Σ崩溃段整段未耗量。

## 9. 边界
length≤0 → ErrInvalidLength；length=1 → 每号一次持久写；
s+n>2^64 或越过配置上限前 → ErrCounterOverflow，拒绝，不回绕；
TTL=0 见 §3；起点在首次建文件时生效。
