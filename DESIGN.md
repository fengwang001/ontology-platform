# 排序合并连接（SMJ）溢出与回退执行器 — 设计

## 1. 范围与模型
- 两路输入 `L`、`R` 均已按连接键 `key` 升序；做等值内连接。
- 连接本身单协程推进；仅「溢出写盘」与「重扫」可能发生并发。
- 内存阈值 `T`：右侧同键组驻留行数达到 `T` 即溢出到临时目录的自描述文件。

## 2. 行与溢出文件格式（row / spillgrp）
- 行编码（流式）：`keylen(u32 LE) | key bytes | vallen(u32 LE) | val bytes`，空串合法。
- 溢出文件 = 头 + 若干帧。
- 头（变长）：`magic(8)="SMJSPILL" | version(1)=1 | groupKeyLen(u32) | groupKey | nrows(u32)`；头长 `hLen = 8+1+4+len(groupKey)+4`。
- 帧：`length(u32 LE) | row 编码 | crc32(u32 LE, IEEE)`；`length` 仅统计行编码字节。
- 读回逐帧校验：任一帧损坏都返回该帧之前可恢复的行数（半截行一律不算）。

## 3. 核心推导：右侧组起点的两种表示
设右侧当前键组的行序列为 `g[0..m-1]`，左侧该键每个左行都要对整组重扫一次。

- **全内存**：组起点 = 切片下标 `0`。重扫 = 迭代器下标重置为 `0`，O(1)。
- **已溢出**：帧变长（`length` 前缀 + 变长行 + CRC），**不能**用 `行号 × 固定长度` 推算位置，否则一旦出现变长行，后几行会从错误字节开始读，典型症状为：前几行匹配正常，后续行读出错位或半截行。正确做法是**记录字节偏移**：解析完头部后 `groupStart = hLen`；重扫 = `Seek(groupStart, 0)`，随后顺序按 `length` 取帧。
- 组起点在组完整落盘（状态 `sealed`）后才有效；重扫前必须显式等待该状态。

## 4. 主循环、输出顺序与逆序检出（smj）
- 每次读一侧的连续同键行为一组：比较两键，小组丢弃（内连接无匹配），等键则逐左行重扫右组做笛卡尔展开，然后两侧同时进入下一键。
- 输出顺序定义：按连接键升序；同键内按「左行到达序」为主序、「右行到达序」为次序。打乱输入同键行相对顺序会改变结果；同一份输入重复执行必须逐字节一致。
- 排序前提校验：任一侧新读到的键小于上一个键即报 `ErrUnsorted`，并给出该侧第一处逆序位置（从零计的流内行下标）。

## 5. 重扫读出行数等式（复杂度上界）
设左行数 `L`、右行数 `R`。两侧行各被读取一次；被匹配到的溢出右组在每个左行时重扫。

`reads = L + R + Σ_匹配的溢出右组 (左组行数 × 右组行数)`

求和项即「每个左行 × 其匹配到的右组大小」之和（全内存重扫不计入磁盘读出）。等式精确成立即证明没有多余的全表重扫；驻留峰值由 `spillgrp` 的非导出计数器记录，上界为 `T`。

## 6. 状态机、并发与故障注入（spillgrp）
- 状态：`building → spilling → sealed`；用 `sync.Cond`+互斥量保护。`Rescan` 在未 `sealed` 时阻塞等待，绝不靠 sleep；写完并 fsync 后置 sealed。
- 截断点分类（长度从 1 逐字节截到 `size-1`，按读取位置分类）：`[1,hLen-1]`→`ErrHeader`；落在帧 `length` 4 字节内→`ErrLength`；落在行体内→`ErrTruncated`；落在 CRC 4 字节内（含体后 CRC 全缺）→`ErrCRC`。
- 截断文件读出最大可恢复前缀后，连接必须中止报错（`smj` 透传），不得给出少行结果。
- 输入流中途报错（`stream.ErrStream`）：保留已输出结果，并上报已消费到的键。
- Seek 失败可注入：经 `Seeker` 接口包装，返回 `ErrSeek` 并指出键与该组。
- 错误均为哨兵，用 `errors.Is` 判定。
