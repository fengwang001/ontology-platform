# 条件请求前置判定设计

## 1. ETag 为什么区分强比较与弱比较

弱 ETag 只保证两个表示在语义上等价，不保证字节完全相同；强 ETag 才承诺表示字节级一致。

`If-Match` 常用于写入、断点续传或并发控制。客户端提交的是它曾见过的具体版本，服务器必须确认当前字节版本仍是那个版本。因此 `If-Match` 使用强比较：只要任一方是 `W/` 弱标签，即使 opaque 字符串相同，也不能认为是同一个可安全续传的字节版本。

如果 `If-Match` 也使用弱比较，那么 `"v1"` 会匹配 `W/"v1"`。假设旧响应在重新生成时语义不变，但压缩方式、分块边界或字节填充发生变化，断点续传就会把旧字节偏移接到新字节流上，产生损坏文件，却被服务器误判为前置条件通过。

`If-None-Match` 主要回答“客户端缓存的语义版本是否仍是当前版本”。语义未变即可返回 304，所以它使用弱比较：`"x"` 与 `W/"x"`、两个 `W/"x"` 都算同一语义版本。

## 2. 为什么存在 If-Match 时忽略 If-Unmodified-Since

`If-Match` 与 `If-Unmodified-Since` 同属“阻止作用于错误版本”的前置条件，但 ETag 比时间更精确。规范规定当 `If-Match` 出现时，由它单独裁决，`If-Unmodified-Since` 不参与。

若叠加为“两者都必须通过”，会得到规范外的失败。例如资源当前 ETag 确实等于 `If-Match` 列表中的标签，但它的 `Last-Modified` 因重新编码、元数据归一化或时钟舍入而晚于 `If-Unmodified-Since`：单独按 `If-Match` 应继续执行，叠加判定却会错误地返回前置条件失败。

同理，缓存新鲜度这一组中 `If-None-Match` 出现时忽略 `If-Modified-Since`。两组之间先裁决阻止写入的 `If-Match` / `If-Unmodified-Since`，再裁决缓存新鲜度。

## 3. If-None-Match 匹配为什么随方法变化

`If-None-Match` 匹配表示客户端提到的版本仍然存在。

对 GET/HEAD，这正是缓存仍有效，请求无需传输表示，结果应为“未修改”。对 PUT、PATCH、POST、DELETE 等方法，客户端通常试图“仅在该版本不存在时执行”，当前仍存在意味着并发版本冲突，结果应为“前置条件失败”。

因此匹配事实相同，但方法语义不同：读方法产生可缓存的 304 类结果，写方法产生 412 类结果；判定器必须返回两个可区分结果，而不能复用同一个错误。

## 4. 修改时刻相等时归属于哪边

`If-Modified-Since: T` 询问“资源是否在 T 之后发生修改”。这个条件严格对应 `Last-Modified > T`，不是 `>= T`。因此当 `Last-Modified == T` 时，没有“晚于该时刻”的修改，结论是未修改。

`If-Unmodified-Since: T` 询问“资源自 T 以来是否保持未修改”。`Last-Modified <= T` 即通过；相等时资源最后修改时刻没有超过客户端给出的边界，因此通过。

HTTP 日期只有秒级精度。解析严格限定为 `Mon, 02 Jan 2006 15:04:05 GMT`，并通过日期算术拒绝不存在的日期。无法解析的条件头部只使该头部失效，判定继续使用其他有效头部；所有时间都来自输入或注入时钟，判定器不读取系统时钟。
