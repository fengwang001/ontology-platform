# DESIGN.md：带溢出的哈希聚合器

## 1. 需求与不变量

对输入流 `Row{Key string, V float64}` 做分组聚合，输出每组 `Count/Sum/Min/Max`。
内存驻留分组数有硬预算 `B`，分区数 `P` 在构造时固定，与数据规模无关。必须保证：

- 峰值驻留分组数 `<= B`；
- 每行至多被处理两次（一次入表、一次回读）；
- 溢出写盘的记录数 == 回读读回的记录数；
- 任一损坏/写失败都中止并报错，绝不输出缺组结果。

## 2. 溢出什么：原始行 vs 部分聚合状态

方案 A：溢出原始行，回读后重新聚合。方案 B：溢出**当前部分聚合状态**（每组一条
`{Key, Count, Sum, Min, Max}`），回读后与内存状态、其它溢出片段做合并。

选择 **B**。理由：

1. 状态大小与行数无关，只与**不同分组数**有关。同键行越多，B 的写盘量越小，
   最坏情况（全部 key 不同）才退化为每行一条，且此时每条记录等价于一行原始行，
   不会比 A 更差。
2. 回读阶段做的是"状态合并"而非逐行累加，CPU 与临时空间都更省。
3. 正确性要求聚合状态的合并算子满足结合律（见第 3 节），本四种聚合全部满足，
   因此把聚合过程切成任意段、按任意顺序合并，最终状态与一次性全内存聚合等价。

## 3. 同一分区被多次溢出为何仍正确

设某 key 的完整聚合是 `f(r1..rn)`，按时间/位置切成片段
`s1=f(r1..ra), s2=f(...), s3=...`。只要存在结合性的合并算子 ⊕ 使
`f(X∪Y) = f(X) ⊕ f(Y)`，则
`f(全部) = s1 ⊕ s2 ⊕ s3 ⊕ 内存剩余`，与加括号顺序无关。

一个分区在第 1、2、3 次预算打破时被分别写成 3 个片段文件，回读时必须把
**全部片段**和内存中该分区剩余状态一起 ⊕ 合并；漏掉任一片段就是漏掉该片段
覆盖的全部行。实现上每次溢出生成独立段文件 `partNNN-SSSS.agg`，回读把该
分区所有段全部读完，逐一并入一个空状态。

### 四种聚合的合并规则（Min/Max 与 Count/Sum 不同）

- Count：`c = c1+c2`，整数加法，严格结合、交换。
- Sum：`s = s1+s2`，浮点加法。数学上结合，但 IEEE754 下**换顺序可能改末位**。
  本设计中同一个 key 的状态只来自"同分区的若干段 + 内存"，段的并集划分固定，
  且合并按**段序号升序**确定性进行；并发只发生在分区之间（key 集合互不相交），
  所以分区完成顺序不影响任一 key 的加法顺序，Sum 位模式逐字节确定。
- Min/Max：不是数值相加，而是**选择**：`min(a,b)`（±0.0 视为相等、NaN 不进入，
  因 NaN 在入表前被拒绝）。`min/min` 天然结合且交换；空状态以"尚未设置"哨兵
  并入，任一方未设置直接取另一方，保证 `min(x,空)=x`。

## 4. 数据格式与内存预算

分区文件自描述：头 11 字节 = magic(4)=`AGGF`、version(1)、partition(2,大端)、
nrec(4)；其后每条记录 = 长度前缀 4 字节大端 + 载荷 + CRC32(IEEE)4 字节。
载荷 = keylen(2) + key + Count(8) + Sum(8) + Min(8) + Max(8)，固定 32 字节 + key。

哈希用 FNV-1a 32 位，`p = fnv32a(key) % P`。驻留分组数超过 B 时，选出当前
内存中**行数最多**的分区（平手取分区号最小，保证确定性）整区溢出，删其内存
状态。计数器：峰值驻留数、溢出次数、每分区溢出次数、写出/读回记录数、
处理行数（AddRow 与读回并入各计 1，跳过的 NaN 不计）、跳过 NaN 数。

## 5. 故障模型

- 写盘：先写 `*.tmp`，`f.Sync` 后 `rename` 为正式段；崩溃留下的 `*.tmp` 在
  store 打开时被识别并删除，绝不参与读回。
- 写失败可注入（第 k 次新建/写段时返回错误）；聚合中止时清空目录中本实例的
  全部临时与正式段（残留计数为 0）。
- 读回逐字节校验：头不完整 / 长度前缀不完整 / 行体不完整 / CRC 不匹配，
  四类错误用 `errors.Is` 区分，错误信息含分区号与字节偏移；任一段损坏即整体
  中止，不产出结果。

## 6. 并发

每分区一个 goroutine 回读并得到独立 map，互不共享；主协程按固定分区顺序
收集并按键排序输出。可注入"分区完成顺序"（用 channel 闸门）做确定性测试，
输出与 Sum 位模式不随完成顺序变化。

## 7. 边界

零行输出空；空串为合法 key；NaN 拒绝并计入 `SkippedNaN`；±Inf 正常参与
（Sum 为 Inf、Min/Max 比较成立）；+0.0/-0.0 同键，Min/Max 视为相等。
