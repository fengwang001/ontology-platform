# 多租户配额公平队列调度器 — 设计推导

仅用标准库，状态在进程内存。包：`task`、`tenant`、`sched`、`admit`、`stat`、`cmd/demo`。

## 1. 为什么 vt 推进是 c/权重

租户 i 维护虚拟时间 `vt_i`，执行代价 c 后 `vt_i += c / w_i`；调度器总选 `vt` 最小的非空租户。

把虚拟时间理解为“该租户消耗的**人均配额**”：租户在真实时间内执行的总代价 C_i，
则 `vt_i ≈ C_i / w_i`。“选最小 vt”等价于优先补偿“实际代价占自己权重份额最少”的租户。
长期连续占用下所有非空租户的 vt 必须相等，故 `C_i / w_i = C_j / w_j`，即
`C_i : C_j = w_i : w_j`，份额与权重成正比（权重 1:3 ⇒ 执行次数 1:3）。
若用 `c*w_i`，连续占用条件变成 `C_i*w_i = C_j*w_j`，份额与权重**成反比**，方向错误。

## 2. 空闲租户重新加入：vt 必须抬升

系统虚拟时间 `sysVT` = 当前所有**非空**租户 vt 的最小值（堆顶；堆为空时保持旧值，单调不减）。
租户从“空”转为“非空”（首次入队也算）时：

```
vt_i = max(vt_i, sysVT)
```

推导：X 空闲时不参与调度，其 vt 停在旧值；若 Y 在此期间执行了代价 S，
则 Y 的 vt 前进 S/w_Y，而 X 落后同样幅度。不抬升时，X 重新提交后会被连续选中，
直到自己的 vt 追平 S 的差距：单位代价任务下要连续独占约 `S / c0 * w_X/w_Y` 步
（等权等代价时就是 S 步，本题 1000 步），长期空闲者反而获得巨大优先券。
抬到 max 后 X 与 Y 站在同一起跑线，靠 ID 字典序决定这一步谁先，立刻恢复公平交织。

## 3. 代价为 0 与并列规则

- 代价 0 合法（仅用于记账边界），但记账代价取 `max(c, MinCost)`，`MinCost=1`。
  否则 0 代价任务使 vt 不推进，该租户在堆顶无限自循环、独占调度器。实际代价统计仍记 0。
- 堆中 `(vt, 租户ID)` 排序：vt 小者优先，vt 相同按 ID 字典序，保证逐元素可复现。

## 4. 数据类型与参数校验

- vt、权重用 `float64`：权重极大/极小并存时 `1e308/1e-308` 不溢出（float64 到 ±1.8e308），
  1e5 次调度的累计精度误差远小于 5% 的验收阈值，比手写有理数简单。
- 权重 `<= 0`、NaN、Inf ⇒ `ErrInvalidWeight`；租户不存在 ⇒ `ErrUnknownTenant`；
  每租户队列超限 ⇒ `ErrQueueFull`；租户仍有排队任务时移除 ⇒ `ErrTenantBusy`。
  四类哨兵错误用 `errors.Is` 判定。移除策略选择**拒绝**（不清空任务，任务不丢）。

## 5. 背压、复杂度、并发

- `admit.Controller` 持有每租户上限 L：`Submit` 在锁内查 `sched.QueueLen`，超限返回
  `ErrQueueFull`；限额按租户独立，A 超限不影响 B（B 有自己的计数）。
- 选择用最小堆（`container/heap`），单次选择 = Pop 下沉（每层 ≤2 次比较）+
  租户仍非空时 Push 上浮（每层 1 次），比较数 ≤ 3·ceil(log2(n))，小于验收上界
  `4·ceil(log2(n))`。堆中只有非空租户：空租户出队后即出堆，不占选择代价。
- `sched` 内部一把 `sync.Mutex` 保护 map/堆/vt；`admit` 一把锁保护限额判定。
  并发下每个任务恰好被取出一次：入队与出队都在临界区内完成。

## 6. 确定性与时钟

同一份确定性提交流，堆排序只依赖 `(vt, ID)`，无随机、无 map 迭代参与选择，
调度序列逐元素相同（20 次一致）。并发提交时，“提交顺序”定义为获取 `admit`/`sched`
入队锁的串行化顺序，即锁把并发请求确定化后的顺序。
时钟可注入：`sched.WithClock`（默认 `time.Now`），用于记录出队时刻，不参与公平选择。
