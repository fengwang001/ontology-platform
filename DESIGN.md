# 属性级权限过滤器 — 设计推导

范围：按角色授予列可见性；当过滤谓词引用不可见列时如何处置；行裁剪与可审计报告。
不实现存储、索引、查询解析、HTTP、认证。状态全部在进程内存。

## 一、不可见列出现在谓词里：三种做法的推导

设角色 R 可见 `{id, name, dept}`，`secret` 不可见。攻击者知道列名 `secret`，想推出某行的 `secret` 值。

### 甲：不可见列的比较当作假

谓词里 `secret = 1` 恒按 false 求值，查询照常执行。

泄露路径：探针 `NOT (secret = 1)`。按甲它被改写成 `NOT false = true`，于是**所有行都被返回**，
包括真实满足 `secret = 1` 的行。攻击者再发 `secret = 1`（恒空集）。两个探针一正一反，
答案被确定性互补地改写：攻击者由此确认 (1) 该列**存在但被隐藏**（列不存在会报未知列错误，
两者可区分），(2) 系统在做谓词改写。更糟的是答案在说谎：真实满足条件的行从 `NOT` 形式被
返回，攻击者若从可见列关联出候选值 v，用 `secret = v` / `NOT (secret = v)` 成对查询即可
把「该列被隐藏且结果被改写」变成可观察事实，过滤机制本身成为泄露信道。

### 乙：不可见列当作 NULL 参与三值逻辑

谓词里 `secret` 恒按 NULL 求值。

泄露路径：探针 `secret IS NULL`。按乙它恒为 TRUE，返回全部行——`IS NULL` 直接变成
「该列存在且被隐藏」的确认探针，存在性泄露。配合 `secret IS NOT NULL`（恒假，空集）与
`secret = 1`（UNKNOWN，被 WHERE 过滤，空集），三个探针即可完整刻画系统的改写规则，
「不可见」与「值真的为空」被混为一谈，语义同样说谎。

### 丙：整条查询拒绝执行并指出列名与路径（本题采用）

谓词引用任何一个对该角色不可见的列，整条查询返回可判定错误
（`filter.ErrInvisibleColumn`，可用 `errors.Is` 判定），错误携带列名及其在谓词树中的路径，
不返回任何行。

论证：拒绝时返回的信息（列名、路径、错误码）只依赖**调用方自己提交的谓词**和**公开的
权限策略**，不依赖任何数据行的内容。因此错误本身不携带关于 `secret` 值的任何比特；
而查询不执行，结果集自然为零泄露。甲、乙的共同病根是「替不可见列编造一个值再求值」，
任何编造的值都会让结果集依赖一个攻击者可探测的谎言；丙不编造、不求值，是唯一不泄露的做法。

## 二、OR 短路例外：常量折叠后消失的分支不算被读取

例：`TRUE OR (secret = 1)`。无论 `secret` 取何值，结果恒为 TRUE，`secret` 实际未被读取。
若仍拒绝，则把「语法上出现」与「语义上被读取」混为一谈，会误伤无害查询。

**定义**：先做常量折叠，再对折叠后的树做不可见列检查。折叠规则：
- `AND`：任一支折叠为 `false` → 整体 `false`；折叠为 `true` 的支删除； 空 → `true`；独支 → 该支。
- `OR`：任一支折叠为 `true` → 整体 `true`；折叠为 `false` 的支删除；空 → `false`；独支 → 该支。
- `NOT`：作用于常量 → 取反。
- 折叠中被消除的子树视为**未被读取**，其中的不可见列不再触发拒绝；
  折叠后仍残留的不可见列引用 → 整条拒绝。

**理由**：折叠是纯语义保持的重写（对任意行，折叠前后谓词真值相同）。被消除的子树在任何
求值策略下都不影响结果，攻击者无法从结果差异中反推其中的列值；而残留的引用必然影响结果，
必须拒绝。例：`TRUE OR (secret = 1)` 放行；`FALSE AND (secret = 1)` 放行；
`FALSE OR (secret = 1)`、`TRUE AND (secret = 1)` 折叠后 `secret` 残留 → 拒绝。

## 三、行裁剪与报告

- 结果行只保留可见列；不可见列**从 map 中删除键**，不置零值、不置 NULL，
  使「不可见」与「值为空」可区分。
- 报告（`report.Report`）记录：被裁列名集合（字典序）、被拒绝的谓词引用
  （列名 + 路径，同列合并、路径全列）、整条查询是否被拒绝。
  `Canonical()` 输出排序后的确定字节，与构造顺序无关。

## 四、复杂度约束

- 谓词检查为单遍遍历：`Filter.visited` 计数等于折叠后树的节点总数，不重复遍历。
- 行裁剪拷贝计数 `Filter.copies` 每拷贝一个可见列条目 +1，恰等于可见列数 v，
  上界 4v 显然成立，与总行数无关（1000 列 5 可见时 copies = 5 ≤ 20）。

## 五、穷举验证的定义

三列 `{a, b, c}`，可见性 8 种组合 × 谓词形态 4 种 = 32 种。模板引用列：
`=` 引用 {a}；`NOT` 引用 {a}；`AND` 引用 {a,b}；`OR` 引用 {a,b,c}。
判定定义：折叠后树中仍引用任何不可见列 → 拒绝并指名，否则放行。
期望：= 放 4 拒 4；NOT 放 4 拒 4；AND 放 2 拒 6；OR 放 1 拒 7；合计放 11 拒 21。

## 六、错误分类

三类错误可用 `errors.Is` 区分：
`policy.ErrUnknownRole`（未知角色）、`filter.ErrInvisibleColumn`（谓词引用不可见列）、
`filter.ErrInvalidPredicate`（谓词结构非法，如空列名、空合取、nil 子节点）。
