# DESIGN — 可撤销的批量重命名与冲突解析器

仅标准库。五个包：`name` / `plan` / `cycle` / `apply` / `undo`，外加 `cmd/demo`。

## 1. 模型与自环语义

命名空间是一个名字集合（无序集合，用 map 表示）。每个请求 `old -> new`：
- 空串是**合法**名字；路径分隔符当普通字符，不做任何文件语义解释。
- 自环 `a -> a` 定义为**无操作（no-op）**：它不搬运任何名字，不参与图、不占临时名、不产生执行步骤，但允许出现。

## 2. 四类冲突（均在任何修改之前判定，可用 errors.Is 区分）

1. 旧名不存在：`old` 不在命名空间。
2. 同一旧名出现两次：同一 `old` 映射到两个不同 `new`（自环与另一目标也属此类；两个相同请求算重复，亦按此类拒绝）。
3. 两个请求指向同一新名：两个不同 `old` 的 `new` 相等。
4. 目标名已存在且不在本批被搬走：`new` 在命名空间中，且没有任何请求的 `old` 等于它。

任一冲突即拒绝整批；先检测后执行，命名空间逐元素不变。

## 3. 执行顺序的推导（拓扑序）

把每个非自环请求看成有向边 `old -> new`。执行 `x->y` 会先占用名字 `y`，因此：
- 若 `y` 同时是某个**尚未执行**请求的旧名，则执行 `x->y` 会覆盖还没搬走的 `y`。
- 即 `x->y` 必须等所有旧名为 `y` 的请求先执行。依赖：`x` 依赖 `y`，先做 `y->*` 再做 `x->y`。

实现上对边按「新名 → 旧名」建反邻接，用 Kahn 拓扑：入度为「该旧名的新名是否又是别人的旧名」。
`{a->b,b->c}`：b 无入边先执行 `b->c`，再执行 `a->b`。同层按名字字典序，保证确定性。

## 4. 环的破解（cycle 包）

无冲突批的功能图中每个节点入度/出度 ≤ 1。Kahn 处理完入度非零节点后，剩余即若干互不相交的环。
一个环无论什么顺序都有一步的目标名仍被占用，故每环引入**恰好一个**临时名 t：
- 对环 `v0->v1, v1->v2, ..., v{k-1}->v0`，替换为
  `v0->t, v{k-1}->v0, v{k-2}->v{k-1}, ..., v1->v2, t->v1`。
- 二元环 `{a->b,b->a}` => `a->t, b->a, t->b`；三元环同理，每步执行前目标名必不存在。

临时名数量 = 环数（每环一个），可由计数器精确断言。
临时名必须不在：现有名字集合、所有目标名、已选临时名中。冲突则重试
（`tmp-0`、`tmp-1`…）；重试超过上限仍全被占用则返回可判定错误，绝不覆盖数据。

## 5. 复杂度

冲突检测与排序只做 map 查找/插入，代价 O(R+N)（R=请求数，N=涉及名字数）。
`plan` 用非导出计数器统计 map 查找次数；正常批总量 ≤ 4(R+N)。

## 6. apply：执行、加锁、失败注入与日志

`Executor` 持命名空间的写锁，整批执行期间任何并发修改被阻塞。
每步执行前校验目标名不存在（防覆盖）；注入钩子可让第 k 步失败，
失败后对已完成步骤**逆序自动回滚**，命名空间恢复到执行前。
日志写本地临时目录文件：头部魔数+版本+步骤数，其后每条记录
`[8字节长度][JSON(old,new,index)][CRC32]`，追加刷盘。

## 7. undo：日志解析、截断三分类与幂等

按字节流式解析。三类可 errors.Is 区分的错误：
- 头部不完整：文件短于固定头长度。
- 记录不完整：声明长度超出剩余字节（含长度字段被截断）。
- CRC 不匹配：长度完整但校验和不符。
损坏点之后的记录不可恢复；按**最大可恢复前缀**逆序撤销，并报告无法撤销的步数。
同一日志撤销两次：第二次为无操作（幂等）。

## 8. 并发与确定性

执行全程持锁；另用可超时的探测测试断言并发改名被阻塞。
请求以构造顺序打乱 20 次，排序结果只依赖请求集合（同层字典序），步骤序列逐元素相同。

## 9. 边界

空请求集（成功、零步）、单请求、自环（无操作）、空串名字、含分隔符名字、
请求数远大于名字数（大量重复旧名被第 2 类冲突拦截）。
