# 设计推导：空白续行 与 Store 转义

## 第 3 条：续行的下一物理行只含空白或为空

`java.util.Properties` 拼接逻辑行时，对每一物理行先跳过起始空白，再逐字符扫描；
遇到行尾反斜杠后丢弃该反斜杠与行结束符，并把“下一物理行去掉前导空白后”的内容追加到当前逻辑行。

若下一物理行只含空白（或为空行），剥掉前导空白后得到的是**空串**：

- 追加空串不改变当前键值行的内容；
- 该空行本身没有反斜杠，续行链立即终止，不会继续吞掉再下一行。

结论：`k=a\` ＋ LF ＋ `   `（空白行）＋ LF ＋ `x=y` 得到 `k`→`a`、`x`→`y`，
空白续行不产生任何值内容，也不会把 `x=y` 卷进来。
依据：续行首部空白的剥离是**整行性**的——整行的空白都属于“前导空白”，
全被剥掉后剩余长度为 0；规则上并不存在“保底保留一个空格”，所以空白行等价于不存在的一段。

## 第 6 条：Store 只转义必要字符

回写文本会再次被 `load` 解析，因此只需转义会改变再解析结果的字符。

键（出现在行首）：
- 首字符是空白、`#`、`!`：否则该行会被当作行首空白或注释，必须转义。
- 行内的 `=`、`:`、空格：否则会被识别成键/值分隔符，必须转义。
- `\`：否则会与后一字符组成新转义；制表/换行/回车/换页：输出控制字符会破坏物理行或被错误解释。

值（出现在分隔符之后，行中间不引入注释）：
- 只有**开头**的空白必须转义：分隔符之后的空白会被当作“分隔符周围空白”吃掉；
  一旦值已经开始（已输出第一个非空白字符），其后的空格处于“值中间”，不会再被当作分隔符空白，
  故值中间空格（连同 `#`、`!`、`=`、`:`）都无需转义，行内 `#` 也不构成注释。
- `\` 与控制字符（`\t \n \r \f`）同样必须转义。
- 非 ASCII 一律原样 UTF-8 输出（语义只要求再解析相等）。
- 空值写成 `key=`；空键/空值都合法；首字符选择用 `\ ` 转义前导空格。

## 7 条语义 → 测试函数对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | `props.TestParse` |
| 2 注释 | `logical.TestNext` |
| 3 续行（含空白续行、奇数反斜杠） | `logical.TestNext` |
| 4 转义与 `\u` 行列号错误 | `props.TestParse` / `props.TestUnicodeErrorPosition` |
| 5 重复键覆盖且保持首次顺序 | `props.TestDuplicateKeyOrder` |
| 6 回写转义规则 | `props.TestStoreSpecial` |
| 7 1000 组随机往返 | `props.TestRoundTripRandom` |
| 复杂度（检查计数 ≤2n） | `logical.TestExaminedBytesBound` |
