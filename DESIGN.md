# 设计推导

## 1. Write 切法无关
压缩器把当前 flush/close 区间内尚未成为确定记录的数据保存在连续内存。位置 i 只有在窗口中找到候选，并确认其后第一个不匹配字节后，才能确定匹配结束；因此匹配长度按原始字节比较，可跨越任意 Write。i 到区间末不足一个前瞻字节时不提前输出字面量，等下次 Write、Flush 或 Close 再判定。Flush 没有未来字节，必须强制提交当前记录；它只改变提交时刻，不改变同区间内此前字节的决策。

## 2. 重叠回指
距离 d、长度 L 且 L>d 时，源序列是输出流自身的后缀；例如 d=1 是重复末字节。逐字节执行 out[t]=out[t-d]，每步能读到刚生成的字节。一次性 src:=out[end-d:end] 块复制只保存旧后缀，周期串会出错。解压器采用逐字节循环；它执行的是实际必须生成的输出字节，配合输出上限，时间与输出规模线性，不会额外膨胀。

## 3. 两类距离错误
“距离超过已输出字节数”表示引用了尚不存在的历史，流即使窗口无限也不合法。“距离超过窗口容量”表示历史存在但压缩器声明的引用范围不允许。先判 0，再判已输出，最后判窗口，可分别给出稳定错误。压缩器只在窗口实际保留且候选绝对位置早于当前位置时发匹配，故不会产生超窗口引用。并行块预置上一块末尾字节时，这些字节已进入解压器历史和窗口，“已输出字节数”的规则不变。

## 4. 并行确定性
块 k 的输入只有本块原文和块 k-1 末尾至多窗口容量字节；匹配器不读取共享可变状态，也不等待其他块完成。各 worker 使用固定窗口容量、链长、块边界和哈希链规则，因此纯函数结果确定，workers 只影响执行顺序。块之间不跨边界发回指；每块在本块记录前写块边界标记，解压器无状态动作，仅把它作为可连接点。最后统一写一个流尾。

## 流格式
| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 `LZ7`、版本 `1` |
| 块边界 | 标签 `0`，后续为本块记录 |
| 字面量 | 标签 `1`；uvarint 长度 n；n 个原文字节 |
| 回指 | 标签 `2`；uvarint(distance)；uvarint(length)，distance>=1，length>=3 |
| 刷新 | 标签 `3`，表示截至此处的数据可独立还原 |
| 流尾 | 标签 `4`；uvarint(原文总长度)；uvarint(FNV-1a 64 校验和) |

所有长度、距离、偏移、总长度和校验和均用 Go binary.PutUvarint 同款变长整数；解码器接受完整编码，超过 10 字节或第 10 字节溢出 uint64 即错误。
