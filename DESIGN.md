# DESIGN: 结构化日志脱敏与一致性采样

仅标准库；状态在进程内存；输出落盘。包：record / rule / mask / sample / sink / cmd/demo。

## 1. 一致性采样推导

目标：同一 trace 链的记录要么全留要么全丢。
- 反例：对每条记录独立掷骰 → 同链决策不同 → 链断裂，且重跑结果不同。
- 结论：决策函数只能依赖 traceID：`keep = hash64(traceID) % denom < keepNumerator`。
  与记录内容、到达顺序、时间无关；FNV-1a 64 位确定性哈希，单次决策恰好计算 1 次哈希（计数器证明 O(1)）。
- 采样率：`rate=0` 全丢，`rate=1` 全留。
- 无 traceID / 空串：无法与任何链关联，按“独立单条链”处理——对规范化键 `"\x00<no-trace>"` 做同样哈希（rate=0.5 时随机留/丢，但同一条记录重复决策一致；0 全丢、1 全留）。

## 2. error 强制保留与链不完整标记

级别 error 及以上无条件保留，即使其链被采样丢弃。此时该记录带 `incomplete=true`：
链上其余非 error 记录已被丢弃，仅凭此记录看不到完整链。标记当且仅当
`level>=Error && !chainKept`；链被保留时 error 记录不打标记。

## 3. 字段路径与点号消歧

路径表达式 `a.b.c` 逐段映射嵌套；消歧规则：
- 反斜杠转义：`\.` 表示键名字面量点，`\\` 表示字面量反斜杠。字段名 `a.b` 写作 `a\.b`。
- 匹配顺序：对每个 map，先用当前路径的**完整剩余串**在当前层查一次“字面键”（允许含点的真实键），再按未转义点分段下钻。
  即 `a.b`：先查键 `"a.b"`；不存在再查 `a` → `b`。真实键优先，消除嵌套歧义。
- 路径某层不是 map（例如是字符串/数组）：该路径不匹配（不报错）。
- 路径不存在：不匹配。
- 数组按元素递归（路径不能用数字下标选择元素；数组内所有映射/标量均递归）。

## 4. 脱敏

三种方式：
- replace：整值替换为固定串（不保留长度）。
- hash：`sha256(value)` 的十六进制；同值同结果、不同值不同（概率 2^-256）、不可逆；结果恒不等于原值。
- truncate：保留前 N 个 rune，发生截断时加标记后缀 `"…<truncated>"`；N>=len 时原值不变。

彻底性：两遍处理。① 路径规则（trie 精确路径）处理规则命中处；② 被配置为“按值匹配”的敏感值，对整棵树做一次递归，凡标量值相等（含数组元素、被其它字段引用处）全部处理。
非目标字段逐字节不变：只改命中的 map 值槽位，结构/键顺序不动。

## 5. 预编译与复杂度

规则集编译为路径 trie：一次记录遍历中每个 map 层只在 trie 上走一步，匹配代价
O(记录字段数 + 常数)，与规则数无关。计数器 matchLookups 记录 trie 查表次数；
100 规则 × 1e5 记录时上界 `记录数*(字段数+常数)`。同路径两种不同方式（如 replace 与 hash）
在编译期报 ErrConflict，指出冲突的两条规则。

## 6. 记录与安全

record 用 `map[string]any` 嵌套表示。NewRecord 构造时：
- 深度上限 MaxDepth=32，超限 ErrTooDeep 并计数，不递归到底（防栈溢出）。
- 遍历带祖先指针集合，自引用映射检出 ErrCycle（不死循环）。
级别 Debug<Info<Warn<Error<Fatal。

## 7. 落盘格式与损坏分类

文件 = 魔数 `"ONTLOGv1\n"`(9B) 头 + 若干帧；帧 = `[4B 大端长度 n][n 字节 JSON][4B CRC32-IEEE(JSON)]`。
读取分类（errors.Is 可区分）：
- ErrHeader：头 9B 不完整。
- ErrLenPrefix：不足 4B 长度前缀。
- ErrBody：声明 n 字节未到齐（记录体不完整）。
- ErrCRC：体完整但 CRC 不符。
恢复：逐帧顺序解析；遇 ErrCRC 停止（后续帧边界不可信），此前完整帧全部恢复；
对文件做 1..len-1 逐字节截断时，恢复数等于“最大完整前缀”所含帧数。

## 8. 并发

规则集/trie 只读；采样器仅含不可变配置（计数器 atomic）；mask 对每条记录独立。
多 goroutine 处理结果按记录输入序汇总后与串行结果逐元素相同。

## 9. 错误判别

哨兵错误：ErrConflict / ErrTooDeep / ErrCycle / ErrHeader / ErrLenPrefix / ErrBody / ErrCRC，
全部可用 errors.Is 区分。
