# DESIGN（CSV RFC4180 方言：半包续传 + 并行切分）

## 0. 约定
记录号=物理行号（从 1，`\n` 计一次行尾，`\r\n` 同一行尾）；字段号从 1；字节偏移从 0。
`Cell.Off`=首内容字节偏移（空字段=逗号/行尾所在位置）；`Cell.End`=末内容字节偏移+1（空字段=Off）。

## 1. 空行与单列空值
空行（`\n`、`\r\n` 或连续行尾）**跳过，不产生记录**。空文档无记录；末尾换行不产生记录（只靠 `recOpen` 决定是否发记录）。
由此单列表里空值记录只能写成 `""`：不加引号的空行被定义为“无记录”，若 writer 把单列空字段写成 ``（无换行）再 parse 得到空文档，数据丢失。故单列且唯一字段为空（Quoted 真假都一样）时必须写 `""`。
**规范输入**：记录间恰好一个 `\n`，末尾无 `\n`，非空文档；行尾只用 `\n`；除上述单列空值等“不加引号则往返失败”的情形外都用最小引号。`Write` 输出即规范形式，故 `Parse(Write(T))` 等 T，`Write(Parse(x))==x` 对规范 x 成立。

## 2. par 切点：双假设
每段独立跑两遍状态机，分别假设段起点在 **引号外(OUT)** 与 **引号内(Q)**。每遍产出：最终状态、事件流（App 带源偏移）、错误。
自左向右拼接：前段确定其结束时真实状态（QQUOTE 携带“待决引号”，CROUT 携带“待决 CR”），据此选本段 OUT 或 Q 结果：
- 前段结束在引号外：选 OUT 结果，其首个引号外行尾之前内容归跨段开字段。
- 前段结束在 Q 内：选 Q 结果；前段以 QQUOTE 结束且段首为 `"` 时，两者合成转义 `""`，拼接时按事件源偏移裁掉一个合成引号，无需重扫。
计数=两遍 N + 每段至多 1 个合成引号 + O(K)，切点落在多长引号字段里都不重扫。
段内只做字段字节数超限检查（事件带源偏移）；字段数/记录数在拼接回放时按全局计数检查。错误坐标=段内坐标+段基偏移；记录号=段内行号+前段行尾总数。错误即终态。

## 3. CR 待定（CROUT）
OUT 见 `\r` 进 CROUT，不发任何东西，续传/切点都只是状态携带。下一字节 `\n`→行尾（CR 不计入字段）；其他→孤立 CR 错误（偏移指向该 `\r`）；EOF→同样错误。Q 内 `\r` 只是内容。par 中前段以 CROUT 结束时，由拼接器用 OUT 的两种首字节裁决（CR→`\n` 消解 / CR→错误），常数开销。

## 4. 第 7 条“立刻”
逐字节驱动，字段长度按**逻辑内容字符**计：普通字节 +1；Q 内 `""` 整体 +1（QQUOTE 再见 `"` 回 Q 时计这一个字符）。接纳下一字符前比上限，第一个超限字节到达即 `ErrFieldTooLong`（源偏移即错误偏移），不缓冲整条记录。字段数在字段开始时检查；记录数在行尾/EOF 提交时检查。终态后 Feed/Close 返回同一错误。

## 5. 状态转移表
状态：S=字段开始, U=未引号中, Q=引号中, QQ=引号中刚见引号, CR=行尾CR待定。输入类：`,` `"` `\r` `\n` 普通 P。

| 状态 | , | " | \r | \n | P |
|---|---|---|---|---|---|
| S | 发空字段→S | 进 Q | 进 CR | 发空字段+行尾→S | 进U,App |
| U | 发字段→S | **ErrBareQuote** | 进 CR | 发字段+行尾→S | App |
| Q | App | 进 QQ | App | App | App |
| QQ | 发字段→S | App(一个引号)→Q | **ErrAfterQuote** | **ErrAfterQuote** | **ErrAfterQuote** |
| CR | 同S | 同S | **ErrLoneCR** | 消解为行尾（CR 丢弃） | **ErrLoneCR** |

EOF：S/U 且 recOpen→发字段（不补行尾）；Q/QQ→`ErrUnclosedQuote`；CR→`ErrLoneCR`；S 且 !recOpen→正常空结束。
四类语法错误：`ErrBareQuote`、`ErrAfterQuote`、`ErrUnclosedQuote`、table 层 `ErrFieldCount`；另 `ErrLoneCR` 可判定。

## 6. 并发
par 起 K goroutine，各自独立机器、结果写独立下标，主 goroutine 顺序拼接；无共享可变状态，`-race` 干净。单个流式 Parser 非并发安全（文档明示）。
