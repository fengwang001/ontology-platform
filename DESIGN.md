# 游标分页器（keyset pagination）设计

## 1. 数据模型与全序

行 = `(Score float64, ID string)`，ID 全局唯一。行上定义全序：
先比 Score，Score 相等再比 ID（字符串字典序）。记作 `<k`，
`(s,a) <k (t,b) iff s<t || (s==t && a<b)`。数据集按此全序排列。

## 2. 为什么游标必须是复合键

反例：六行 Score 同为 1.0，ID = a..f，每页 3 行。
- 游标只记 Score=1.0，下页取 `Score > 1.0`：d/e/f 被整批跳过（漏）。
- 下页取 `Score >= 1.0`：a/b/c 再次出现（重）。
任何只依赖 Score 的单值谓词都无法把页边界定位到同分序列内部，
因为等号无法区分同分的不同行。

正确做法：游标记录本页最后一行的复合键 `(s,id)`，下页取严格大于：
`(Score,ID) >k (s,id)`，即
`Score>s || (Score==s && ID>id)`。
严格大于天然排除最后一行本身，ID 又能在同分序列内部推进，
故不重不漏。首页游标为空字节串，谓词为恒真。

## 3. 反向翻页规则

反向不是「正向取页再逆序」（那会得到倒序的页）。规则：
1. 边界键不变，但比较方向取反，取该键**之前**的行；
2. 取数方向取反（从边界向更小键方向取 limit 行）；
3. 取回后页内仍按正向 `<k` 排列。
设正向页为 P1,P2,P3…，每页返回首尾两个游标；以第 n 页**首游标**
反向取一页，得到的恰好是第 n-1 页，且逐行顺序相同。

## 4. 并发增删下的语义

数据用切片按全序存放，RWMutex 保护，读写均在锁内完成；
翻页是纯函数：输入 `(快照可见数据, 游标, limit, 方向)`，无推进状态，
同一游标并发调用得到逐行相同的结果。

不提供快照隔离，只承诺 keyset 两条不变量：
- **不重**：已返回过的行不会因后续翻页再次出现。游标锚定的是
  复合键位置而非行身份；谓词是严格半开区间，边界行自身永不返回。
  插入到已翻过区间（键 < 游标）的行落在谓词之外，故不出现、
  也不会把旧行推回来。
- **不漏（相对当前数据）**：任何当前仍存在、且键落在
  「当前游标 → 方向终点」开区间内的行，必在后续某页出现。
  删除只移除行本身，不改变其余行的键序；游标不依赖锚点行存在，
  锚点行被删后以键值继续定位（O(log n) 二分）。

## 5. 游标编码（不透明、O(1)）

字节布局：`dir(1) | idLen(2,big) | score(8,big, math.Float64bits) |
id(idLen) | crc32(4,big)`，空字节串 = 从头开始（无方向）。
CRC32-IEEE 覆盖其前面全部字节。解码顺序：
长度/前缀截断 → `ErrTruncated`；dir 字节非 0/1 → `ErrBadDirection`；
校验和不符 → `ErrChecksum`；方向与请求不符 → `ErrDirectionMismatch`。
解码只做定长切分与一次散列，分配为常数（实测 allocs/op 为常数）。

## 6. 效率

取页 = 二分定位边界 `O(log n)` + 顺序取 limit 行，比较次数上界
`log2(n) + limit`（含定位探测）。10000 行、每页 20 行时
上界 `20+ceil(log2 10000)=34`，验收线 `4*34=136`。
计数器在 page.Result 中导出 `Comparisons`，每次键比较 +1。

## 7. 包划分

`row`（Row/比较）、`cursor`（编解码+错误哨兵）、
`page`（并发安全 Store + 双向取页 + 计数）、`audit`（不重不漏自检）、
`cmd/demo`（八条判定 + 总计）。测试按包合并为一个表驱动文件。
