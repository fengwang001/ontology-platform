# 设计：结构化日志字段脱敏与一致性采样

## 1. 数据模型（record）

记录为 `Record{Level, TraceID, Fields map[string]Value}`，字段值为
`Str|Num|Bool|Map|Arr` 五类；数组元素仍是任意 Value，故规则可作用于
「数组内嵌套映射」。构造时做 DFS 校验：深度从根映射计为 1，超过
`MaxDepth(32)` 返回 `ErrDepthExceeded`；用访问指针集合检出自引用映射
（同一 `map` 指针重复出现）返回 `ErrCycle`。两类错误均可 `errors.Is`。

## 2. 路径消歧规则（关键）

字段名允许含点号（名字就叫 `a.b`）。解析点分路径 `a.b.c` 时，在每一层
映射上**先查整段剩余字符串是否为一个精确键，再按点号下钻**，即「精确
全名优先于逐层下钻」。因此：
- 映射同时有键 `a.b` 和嵌套 `a -> b` 时，路径 `a.b` 命中精确键；
- 不存在精确全名时才尝试切分点号逐层进入；
- 某层值不是映射（是字符串/数组等）→ 路径不可达，规则不生效，不报错；
- 路径不存在 → 不生效，不报错。
规则一律在编译期验证路径非空。

## 3. 规则集编译（rule）
预编译为两类索引：
- 路径表：以精确点分路径为键（数组内映射以其元素路径归一），查一条
  字段只需在它自己的完整路径上做一次 map 查找，匹配次数为 O(字段数)，
  与规则数无关；
- 值模式表：已编译 `regexp.Regexp` 的线性小表（按模式名分组）。
编译期校验：正则可解析；同一路径不允许两条动作不同的规则（替换 vs
哈希等），否则返回 `ErrConflict` 并指出两条规则。
包内计数器 `pathLookups` 记录每条记录实际路径查找次数。

## 4. 脱敏（mask）
三种动作及不变量：
- 替换：固定串 `***`，不保留原长度；
- 哈希：FNV-1a 64 十六进制，同值同结果、异值异结果、输出不等于原值
（不可逆外观）；
- 截断：保留前 N 个 rune，追加 `…` 标注截断；长度不足 N 时不加标注。
DFS 遍历全部字段（含各数组元素、被多处引用的同一 map）。路径规则在
完整路径上判定；值模式规则对每个字符串值判定。命中值模式的同一敏感串
无论出现在多少处都被处理；非目标字段原样返回。

## 5. 一致性采样（sample，核心推导）
要求同一条追踪链的记录要么全留要么全丢。若对每条记录独立掷骰，同一
TraceID 的各次随机结果不同，链必断；按计数器、时间或到达顺序取模会让
结果随输入顺序/时刻漂移，重复运行不可复现。因此决策只能是 TraceID 的
**确定性函数**：`h = FNV64(TraceID)`，`keep = h%1000 < rate*1000`。
决策与记录内容、顺序、时间、并发均无关，单次决策恰好 1 次哈希
（计数器 `hashCalls` 断言）。
空 TraceID 视为一个固定虚拟链（键 `"\x00no-trace"`），同样确定性取舍，
使无追踪记录之间决策一致；rate=0 全丢、rate=1 全留。
级别 >= error 强制保留：若该链本应丢弃，则记录打 `ChainIncomplete`
标记（其余同链普通记录仍丢弃）；链本就保留时不打标记。

## 6. 落盘格式与损坏分类（sink）
文件 = 头 `ONTLOG1\n`（8 字节，自描述魔数+版本）+ 若干帧；每帧
`[4 字节大端长度 n][n 字节 JSON 记录体][4 字节 CRC32(IEEE, 覆盖体)]`。
读取时按截断点分类（错误均可 `errors.Is`）：
短于 8 字节 → `ErrHeaderTruncated`；长度前缀不足 4 字节 →
`ErrLenTruncated`；声明长度超出剩余字节 → `ErrBodyTruncated`；
CRC 不符 → `ErrCRCMismatch`。读取在首个损坏帧停止，恢复数即此前完整
帧数（最大可恢复前缀）。Writer 用 mutex 保证并发安全。
