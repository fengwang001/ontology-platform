# 设计推导

## 表示约定

模式与路径都按同一规则切成「段 + 边界标志」：`Split` 返回首段前是否有 `/`（leading）、末段后是否有 `/`（trailing）；`strings.Split` 不折叠 `//`、不处理 `.`/`..`。边界标志必须逐位相等，`/` 永不出现在任何段内（模式里的 `\/` 转义为字面 `/` 码点，见下注）。

## 推导一：`x/**`、`**/x` 与 `a/**/b`

`**` 的定义是「零个或多个**整段**」。把模式按段切成 token 序列，`**` 是一个能消费 0..n 个段的 token，其余段 token 必须恰好消费一段。

- `x/**` 的 token 为 `x, **`：`**` 消费 0 段时剩段序列恰为 `x`，故匹配 `x`；但 `x/` 的段序列是 `x` 且 trailing 置位，而模式 trailing 不置位——边界是不同字符串，不匹配；匹配 `x/y`、`x/y/z`。
- `**/x` 的 token 为 `**, x`：`**` 消费 0 段时段序列为 `x`，故匹配 `x`；`/x` 的 leading 置位而模式没有，不匹配（模式写 `/**/x` 才匹配）。
- `a/**/b` 的 token 为 `a, **, b`：`**` 消费 0 段得到 `a,b`，即 `a/b`；消费 1、2 段得到 `a/x/b`、`a/x/y/b`。三条结论由同一条「0..n 整段」规则推出，无特例。
- 边界注：`x/` 与 `x` 是不同输入，任何模式都不能同时匹配两者，除非其自身边界标志随 `**` 伸缩——trailing 属于模式的**固定**边界，不随 `**` 伸缩，故 `x/**` 不匹配 `x/`。

## 推导二：空串路径

段序列视角下，空串是「0 个段、leading=false、trailing=false」。

- `?` 与普通字符类都要求恰好消费一段，不匹配。
- `*` 是**段内**通配符，它必须附着在某一段上消费该段内的码点；段数为 0 时没有段可附着，故 `*`（以及 `a*`、`**b` 这类非整段 `**`）不匹配空串。
- 整段 `**` 消费「0 个或多个段」，0 段合法，且模式 leading/trailing 均为 false，故整段模式 `**` 匹配空串。
- 空模式同样是 0 段、无边界，只匹配空串。

## 推导三：无回溯算法

段内匹配用经典「记录回退点」的线性扫描：扫描器 `(pi, si)`，普通原子与路径码点不等时，若存在星号回退点则回到该星号，并让它多消费一个码点；每个回退点只记**最后一个** `*` 的位置。

为什么只记最后一个 `*` 不漏匹配：设两个星号 `p*`（旧）、`q*`（新），若某次匹配失败发生在新星号之后，把多出的路径码点交给新星号消费严格不劣于交给旧星号——因为旧星号与新星号之间的片段是固定的，它一旦在某位置对齐成功，把该固定片段的对齐点整体右移等价于让新星号扩大消费区间；旧星号唯一能而新星号不能的情形（固定片段无法对齐）已被「回退后重扫固定片段」覆盖。形式化：存在匹配时取新星号消费区间最短的那一个匹配，新星号多消费只会进入同样的重扫，因此旧星号回退点被支配。每对 `(pi,si)` 至多被线性扫描常数次访问，故段内 O(模式原子数 × 段码点数)。

该论证不能照搬到 `**`：被支配的前提是「星号之间是**按码点逐字符对齐的固定串**」。`**` 之间的片段是整段，而每段的段内匹配本身带有 `*` 分支选择；一个 `**` 取多少**段**会改变后续每段模式与路径段的配对关系（段不能被拆给两个模式段），不存在「码点级固定前缀」保证新 `**` 支配旧 `**`。因此跨段采用 DP：状态集是「已匹配到的模式 token 下标」集合，逐路径段转移，`**` 转移为保留自身并前进一步，普通段转移需段内匹配成功。状态数 ≤ token 数 × 段数，每步段内匹配 O(原子×码点)，总计 O(模式长度 × 路径长度)，无指数回溯。

## 推导四：规则集版本一致

规则编译产物是不可变快照；`Set` 用 `atomic.Pointer[snapshot]` 持有当前快照。查询先取一次指针，命中判定与 `Explain` 全部基于该指针，故一次查询只看到一个版本，序号与原文必属同版。替换先在锁外完整编译新快照；任一条规则编译失败或超上限即直接返回错误，旧指针不写入、旧快照原样保留。编译成功后单次 `Store` 原子切换。

## 匹配结果表

| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | 否 | 是 | 否 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是（须 `/**/x`，此列写 `**/x` 则否） | 否 | 否 | 否 |

注：`**/x` 对 `/x` 为**否**（leading 不同）。

| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `*` | 否 | 是 | 否 | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 否 | 否 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
