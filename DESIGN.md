# 多租户配额公平队列调度器 — 设计推导

## 1. 模型
租户 t 权重 w>0；任务代价 c>=0。每租户一条 FIFO 队列，调度器从
非空租户中选虚拟时间 vt 最小者执行其队首任务。

## 2. 为什么执行后 vt += c/w，而不是 c*w
公平目标是「单位权重获得相同服务量」。设租户 i 已执行总代价 S_i，
其虚拟时间 v_i = S_i / w_i（按权重归一化的已获服务）。
- 执行代价 c 后 S_i += c，故 v_i += c/w_i。
- 调度总选 v 最小者，即总选「单位权重已获服务最少」的租户；两租户
  i,j 切换点为 S_i/w_i = S_j/w_j，即 S_i/S_j = w_i/w_j，服务量与
  权重成正比。等代价任务下权重 1:3 的两租户执行次数比趋于 1:3。
- 若改用 v_i += c*w_i，权重越大 vt 涨得越快、被选越少，份额与权重
  成反比，错误。

## 3. 空闲租户重新加入：vt 抬升
问题：X 空闲期间 Y 执行 N=1000 个等代价任务，系统归一化时间前进约
Δ = N*c/w_y。若 X 仍带旧值 v_x（v_x < v_y）入队，选最小者会一直
选 X，直到 v_x + k*c/w_x >= v_y，即 X 连续独占
k = (v_y-v_x)*w_x/c 步；等权重等代价下 k=N=1000，Y 被饿死千步。
正确做法：队列由空转非空（新到达或重新提交）时
vt = max(vt, sysVT)。sysVT 为当前系统虚拟时间（最近一次被选租户的
vt；全体空闲/初始时为 0，单调不减）。抬升后 X、Y 同起跑线，按权重
公平交替：X 的任务在很少（常数）步内执行，Y 也持续被调度不饿死。

## 4. 零代价任务
c=0 时 c/w=0，该租户 vt 永不推进而无限独占。处理：有效代价
c' = max(c, ε)，ε = 1e-3。零代价任务合法，但每次执行仍令 vt 前进
ε/w > 0，其他租户必然在有限步内追平：相对单位代价租户，零代价租户
最多连续执行 1/ε = 1000 步（有界，可实测），不再无限独占。统计实际
份额时仍按任务原始代价（0）记账，不污染份额度量。

## 5. 数值表示与溢出
vt 用 float64：10 万级调度下精度远优于 5% 偏差要求，并自然兼容极大/
极小权重。为防极端权重下增量成为 Inf，加法饱和到 1e150，结果非有限
时同样钳制，保证比较始终良定义、不出现 NaN/Inf。

## 6. 并列打破
按 (vt, 租户 ID) 字典序：vt 相同取 ID 字典序最小者，调度序列可复现。

## 7. 数据结构与复杂度
非空租户放入 container/heap 最小堆，键为 (vt, ID)。选择 = 弹出堆顶后
按新 vt 重新入堆，O(log n)，n 为非空租户数；空闲租户不在堆中，不占
比较代价。堆的 Less 每次比较给包内计数器 +1，单次选择开始前清零。
1000 租户时断言上界 4*ceil(log2(1000)) = 40。

## 8. 背压与边界
- 每租户队列上限 cap：超限返回 ErrQueueFull，该租户状态不变；各租户
  队列与上限独立，不影响其他租户入队。
- 权重 <=0 返回 ErrBadWeight；代价 <0 返回 ErrBadCost。
- 移除租户时若仍有排队任务，拒绝并返回 ErrTenantActive，任务不去向
  不明；队列排空后方可移除。
- 租户未注册返回 ErrUnknownTenant。均为哨兵错误，errors.Is 可区分。

## 9. 并发与确定性
调度器用一把 sync.Mutex 保护队列、堆与账本，配合 sync.Cond 在入队或
关闭时唤醒消费者。堆顶弹出即移除：每个任务恰好被一个消费者取出，不丢
不重。同一逻辑提交顺序 ⇒ 同一调度序列；并发提交时「提交顺序」以各
提交者获取入队锁后的串行化顺序为准（确定性测试在单协程固定顺序提交）。

## 10. 包划分
task（任务值）、tenant（队列+权重账本）、sched（堆选择与并发调度器）、
admit（上限背压）、stat（份额统计与偏差）、cmd/demo（逐条判定演示）。
