# 通配符模式匹配器设计

范围：把含 `%`（任意串）与 `_`（恰好一个码点）的模式匹配到目标字符串，
对标 SQL LIKE。不做正则、查询解析、索引、存储。状态全部在进程内存。

## 1. 单锚点回溯：为什么线性就够

朴素递归对每个 `%` 枚举它消费 0..n 个码点的所有可能，分支随 `%`
个数指数增长（`%a%a...%b` 配一串 `a` 是典型恶性用例）。

推导：把模式看成 token 序列，`%` 是唯一能"多吃"文本的 token。
假设扫描到某处失配，且最近的 `%` 记为 S，它当时消费到文本位置 starI。
若存在一种成功对齐，其中某个**更早**的 `%` 多消费了一段文本 X，
那么因为 `%` 的语言是 Σ\*（任意串），X 同样可以被 S 消费——
S 与更早 `%` 之间没有必须落在 X 里的字面量约束会被破坏
（若有字面量约束，失配早就发生在推进到 S 之前）。
所以"让更早的锚点多消费"永远可以改写为"让最近锚点多消费"，
**只需记住最近一个 `%` 的位置 starJ 与它消费到的文本位置 starI**。

算法（无递归）：
- 指针 i（文本码点）、j（模式 token），锚点 (starJ, starI)，初值 -1。
- token 为字面量或 `_` 且当前码点匹配：i++, j++。
- token 为 `%`：记录 starJ=j、starI=i，j++（先按消费 0 个尝试）。
- 否则失配：若 starJ≥0，回 j=starJ+1，starI++，i=starI
  （让最近的 `%` 多消费一个码点，重试其后缀）；否则失败。
- 模式用尽时，文本也恰好用尽才成功；否则按上一条回退。

不漏解：对最近锚点 starI 的取值按 0,1,2,... 递增枚举，覆盖该 `%`
所有可能消费量；更早锚点无需重访（上述可改写性）。归纳可得：
若存在任何对齐使整体匹配，必在某次 starI 推进后被找到。

步数上界：starI 单调不减、至多推进 n 次（n=文本码点数）；
两次回退之间 j 至多前进 m 步（m=token 数）。
循环迭代总数 ≤ (n+1)(m+1)+n ≤ 4·m·n（m,n≥1），线性，无指数项。
每次迭代 O(1)，故总时间 O(m·n)，空间 O(1)（除码点切片）。

## 2. 相邻 `%` 折叠

规则：解析时把连续出现的 `%` 折叠为一个。
语义不变的理由：`%` 匹配 Σ\*，两个相邻 `%` 匹配 Σ\*·Σ\* = Σ\*
（任意串切成两段仍是任意串，反之任意串都可切成两段）。
归纳到 k 个相邻 `%` 同理。折叠是保语言变换，
`%%%a` 与 `%a` 匹配完全相同的字符串集合，且 token 数变少。

## 3. 码点而非字节

SQL LIKE 的 `_` 语义是"恰好一个字符"。Go 的字符单位是码点（rune）：
- 模式与文本先整体校验 UTF-8 合法性，再解码为 []rune 比较。
- `_` 消费恰好一个码点，因此四字节表情（如 U+1F600）被 `_` 匹配，
  而按字节算会被错判为 4 个单位。
- 非法 UTF-8 不做 U+FFFD 静默替换：替换会把不同输入映射到同一
  内部表示，破坏可判定性与确定性。解析/匹配时返回可判定错误，
  用 `errors.Is` 归类，并用 `errors.As` 取出侧别（pattern/text）
  与出错字节偏移。

## 4. 错误分类（三类，errors.Is 可区分）

- ErrInvalidEscape：`\` 后跟非 `%_\\` 字符，报模式内字节位置。
- ErrTrailingEscape：模式以单个 `\` 结尾。
- ErrInvalidUTF8：模式或文本含非法 UTF-8，报侧别与字节偏移。

## 5. 包划分

- pattern：解析、转义、折叠、规范化长度读取。
- budget：步数计数器（内部字段非导出）与上限判定。
- match：单锚点迭代匹配，逐步经 budget 计数。
- audit：朴素递归参考实现 + 固定种子随机对拍。
- cmd/demo：逐条打印 OK/FAIL 的可运行演示。
