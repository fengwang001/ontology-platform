# DESIGN

## 1. 空输入/只含换行（末尾策略）
三策略：Keep=保留原样；One=无内容则空，否则删全部末尾`\n`后补恰好一个；Trim=删末尾空行（空白行）再补一个，全空则空。
幂等：One 对空输入取`""`而非`"\n"`；若取`"\n"`，N("")!=""且二次规范化虽稳定，但与“无内容”语义冲突，且截断遍历在0字节处产生字节。取`""`则 N("")=""。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut
映射记录两类区间：1:1 直映区间；删除区间 `[a,b):orig, [o,o):out`。插入区间（One/Trim 补的`\n`）记 `[n,n):orig,[o,o+1):out`，仅 ToOrig 用到。
ToOut(i)：删除区间内所有 i 映射到该区间输出起点 o（“后一个输出字节的起点”=换行符位置）。理由：ToOut 单调不减，且不能映射到区间内部。`\r\n`的`\r`与尾随空白都位于换行符之前，ToOut=`\n`的输出偏移——光标落在行尾换行符**之前**（即换行位置），不会跳到下一行。
ToOrig(o)：落在 1:1 区间直接对应；删除空区间输出点 o 对应删除区间起点 a；插入点对应插入锚点。故对所有输出偏移 o，ToOut(ToOrig(o))==o。

## 3. 缓冲超限
待定空白可能在行尾（应删）或行中（应留），二者结论相反。
强制判定为“非行尾”并输出：若下一字节真是行尾，则输出含行尾空白，违反语义2/幂等5；且切分点落在空白串中间与否结果不同，违反4。拒绝则不产生任何错误输出，4/5/6 均不被破坏（拒绝是可判定错误 ErrSpaceLimit，带原文偏移，进入终态，状态可解释：已输出内容保留）。故选择**拒绝**。
输出超限同理：在写入前判断，超限返回 ErrOutputLimit，不写入，终态。

## 4. par 拼接与有界修正
各段独立 norm（Keep 策略，不补末尾换行）。段边界只有两种待定状态，修正扫描范围有界：
- CRLF 跨切分：若切点前最后原文字节是`\r`（段尾输出以`\n`结尾，因该`\r`已生成`\n`）且下段首字节是`\n`，则下段首字节是同一 CRLF 的另一半——丢弃该字节（删除点）。
- 空白跨切分：行尾空白串可在切点任意处断开。拼接时从段尾向前、下段头向后扫一段连续空白：
  - 下段空白之后紧跟行尾：这些空白（含上段尾保留的）全行尾，全部删除；
  - 下段空白之后是非空白字节：全部保留。
  `\r\n`被拆时“段尾`\r`生成`\n`、段头`\n`丢弃”，空白判定中把段尾`\n`视为行尾。
有界：只需从边界双向扫描到首个非空白/行尾即止，每字节至多被相邻边界扫一次，总 O(n)，不随空白串长度膨胀。
映射平移：每段 span 表的 orig/out 坐标加段全局基址；被丢弃的段头字节在合并时转为删除区间；段表 entry 顺序天然有序，直接串接。最后对全局输出应用 Keep/One/Trim 末尾策略（One/Trim 追加的`\n`为插入区间）。

## 5. 复杂度
span 用有序 entry 切片（直映 run-length + 删除点）。无删除纯`\n`文本只产生 1 个直映区间。ToOut/ToOrig 二分 entry，计数器 probes 记录访问区间数，断言 ≤ 2·log2(N)+4。
