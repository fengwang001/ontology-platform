# DESIGN

## 推导 1：空输入 / 纯换行 × 三种末尾策略

策略：`Keep`=规范化全部行尾与空白，不动末尾结构；`EnsureOne`=末尾恰有一个 `\n`；`TrimAll`=删除全部末尾空行（含全部尾 `\n`）后，仅当原文存在非空白内容且以行尾结束时保留一个 `\n`。

EnsureOne 对**任意**输入（含空）保证输出以 `\n` 结尾（空→`"\n"`）。若选空→`""`，则 `""` 与 `"\n"` 同为不动点使「恰好一个」不变量在空输入上不成立，且 N(N(""))=N("")="" 表面幂等却无法与「确保一个」自洽；选 `""→"\n"` 后 `"\n"→"\n"`，闭包干净。TrimAll 下 `"\n"`、`"\n\n"`、`"  \r\n"` 的唯一内容行是空行，整行删除→`""`。

| 输入 | Keep | EnsureOne | TrimAll |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 推导 2：被删字节的 ToOut

由 ToOut(ToOrig(o))==o，被删区间不能作为任何 ToOrig 的值。设原文删除区间 D=[a,b)（尾随空白或 `\r\n` 中的 `\r`），其后保留的 `\n` 映射到输出 p。取 **ToOut(i)=p，i∈[a,b)**：被删字节映射到行尾 `\n` 的位置，即换行符之前的边界；光标落在被删空白中时跳到行尾换行之前，符合直觉。ToOrig(p)=该 `\n` 的原文偏移，ToOrig 在删除间隙内不可见（输出没有对应字节），故不违反映射。ToOut(lenOrig)=lenOut，ToOrig(lenOut)=lenOrig；两函数均单调不减（删除区间取常值平台）。

## 推导 3：空白缓冲超限

强制把未决空白输出，若其后真出现行尾则无法撤回，直接违反语义 2/4/5。故选择**拒绝**：挂起的 `[ \t]` 字节数超 `MaxPending` 时返回具名 `ErrPendingOverflow`（带原文偏移），已输出保留，实例进入终态（再写返回 `ErrClosed`）。挂起量只取决于内容本身（行内连续空白长度），与切分方式无关，同一字节在任意喂入方式下触发，满足语义 4。输出超 `MaxOutput` 返回 `ErrOutputOverflow` 同样终态。

## 推导 4：par 拼接

跨段状态只有两类：①段尾待定 `\r`（上界 1 字节）；②段尾挂起空白（一段连续 `[ \t]+`，长度上界即段长）。做法：切点处不裸切段内容——各 worker 用独立 norm 完整处理其字节段并 Close，拼接器对每个段边界做一次**有界重放**：取出左段尾部至多「待定 `\r` + 全部挂起空白」与右段开头同样数量的边界字节，用一个新 norm 状态机从「换行已提交」态重放这一小片，用重放结果替换两段在该片上的输出与映射。重放总量每边界一次、合计 O(输入)。映射平移：右段 span 的 orig 坐标加段原文起点、out 坐标加前段已提交输出长度，再用重放区的新区间替换边界旧区间；区间只在删除点产生，纯 `\n` 文本区间数为常数。
