# DESIGN：流式行尾与空白规范化

## 0. 算法总览

字节流经过三个串行判定：`eol` 判定 `\r\n`/`\r`/`\n`（前一字符是 `\r` 时下一字符待定）；
`ws` 判定一串空白是否在行尾（只有看到 `\n` 或流结束才知道）；`norm` 消费判定结果，
`\r` 删除、`\n` 输出，行尾空白删除，其余原样输出。`span` 用三类区间记录映射：
identity(orig,out,n) 一一对应、delete(origStart,origEnd) 仅原文区间、
insert(outStart,outEnd) 仅输出区间（策略补的末尾 `\n`）。
区间按位置有序，`ToOrig`/`ToOut` 二分（`sort.Search`）。

## 1. 末尾换行策略（样例表）

`Keep` 不动；`EnsureOne`：输出以换行结尾当且仅当输出非空（空输入仍为空）；
`TrimBlank`：删掉全部末尾换行，剩余非空则补一个 `\n`，全空则为空。

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

幂等：`EnsureOne("")==""`，故补换行规则必须是"非空才补"；
若空输入也补 `"\n"`，则 N("")="\n" 而 N("\n")="\n"，N(N(""))=N("\n")="\n"≠""，破坏幂等。
`TrimBlank` 对非空文本的不动点是"恰好一个末尾换行"，与 `EnsureOne` 同；
对"仅若干换行"整体坍缩为 `""`，再次规范化仍为 `""`。空输入三种策略都输出 `""`，
从而全部满足 N(N(x))=N(x)，且输出天然无 `\r`、无行尾空白。

## 2. 被删字节的 ToOut

映射是保序的满射：多个被删原文字节折叠到同一个输出位置。
删除区间没有输出字节，故被删位置 i 的 ToOut 取"它之后下一个保留内容在输出中的位置"（右靠），
即紧随该删除区间的输出偏移。删除区间内所有 i 映射到同一 o，且 o 是某输出区间起点，
`ToOrig(o)` 回到删除区间后的原文起点，再 ToOut 仍得 o（该点不在删除区间内部），互逆成立；
两函数均单调不减。编辑器把光标放在被删行尾空白或 `\r\n` 的 `\r` 上时，右靠意味着光标跳到
**换行符之后（下一行起点）**；换行符本身保留，视觉行尾仍可达。

## 3. 缓冲超限选择

两种方案：强制把待定空白判定为"非行尾"并输出，会违反语义 2（后续若出现换行，
本应删除的空白无法收回）、破坏幂等与跨切分一致（切分点不同，强制输出时机不同）。
拒绝（可判定错误）不改变任何输出与映射规则，只在写入瞬间以带原文偏移的
`ErrPendingLimit` 终止；已输出保留，进入终态，语义 4/5/6 不受影响。故选择**拒绝**。
输出上限同理：超出立即 `ErrOutputLimit`（带原文偏移），已输出保留，终态。

## 4. par 拼接（窗口法）

段头有两个未知：前段末尾待定 `\r`、未判定空白。第 k 段（owned 区间 [b_k,b_{k+1})）
规范化时向**前**回看 W=PendingLimit+3 字节作为上下文（首段不回看）。
这个上界足够：待定空白至多 PendingLimit 个，再加 `\r` 与随后的 `\n` 共 2 字节；
任何决定"owned 区间第一字节如何处理"的信息都落在前 W 字节内。
每个 goroutine 对窗口 [b_k-W, b_{k+1}) 建独立 norm，Close 后拿到相对窗口的输出与 span，
按 orig 坐标把输出与映射区间裁剪到 [b_k, b_{k+1})（末段保留策略插入的 insert 区间），
再把 orig/out 起点平移到全局坐标后拼接。裁剪点取自区间边界，平移后仍有序、无缝无重；
K 个 goroutine 只读共享输入、只写各自结果，`-race` 干净。总工作量 O(n+KW)。
