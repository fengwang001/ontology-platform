# 快照读可见性判定器 — 设计

## 核心推导：为什么只看提交时刻不够

朴素规则：「写入事务 T 的提交号 < 快照水位 ⇒ 版本可见」。

水位只保证「提交号小于它的提交发生在过去」，但提交号分配与快照
创建并不同步：快照创建时仍活跃的事务 T 会进入活跃集，而 T 随后
提交时仍可能拿到小于水位的提交号（号已预留或水位被粗时钟推进）。

反例构造：
- T1 在快照创建时仍未提交，故进入活跃集 active={T1}
- 快照水位 water=10
- T1 之后提交，拿到提交号 5（5 < 10）

按朴素规则 T1 写的版本可见 —— 读者看到了取快照那一刻尚未提交
的数据，快照读的一致性被破坏。

正确规则（本实现）：

    可见 ⟺ T 已提交 ∧ commitSeq(T) < water
         ∧ (T ∉ active ∨ T 是快照自身事务)

即「提交号小于水位 且 写入事务不在快照的活跃集中」；快照自身事
务的写入对自己恒可见。边界：commitSeq == water 不可见（左闭右
开）；已回滚事务永不可见。

## 复杂度

单次判定 = 1 次事务表查找 + 至多 1 次活跃集查找，O(1)，不遍历事务表。
