# 多副本缓存失效传播与回源协调器 — 设计推导

包依赖单向：`version ← entry ← single ← replica`，`version ← bus ← replica`。
状态全部在进程内存；时间只来自注入时钟 `replica.Clock`；只用标准库。

## 1. 为什么"丢弃旧版本通知"能让乱序投递收敛（第 1、8 条）

每个条目维护一个关键量 `knownVer`：已持有数据的版本与已应用通知版本
的**最大值**。不变式是：`knownVer` 单调不减。

- 应用通知 `nv` 的规则只有一条：`nv < knownVer` 时丢弃（什么都不做），
  `nv >= knownVer` 时把 `knownVer` 提升为 `nv` 并使数据失效。
- 因此 `knownVer` 是"对通知版本集合取 max"的在线折叠。`max` 满足交换律、
  结合律、幂等律：对同一集合，无论按什么顺序、重复多少次折叠，结果都是
  集合的最大值。顺序投递 `v1,v2,v3` 与乱序投递 `v3,v1,v2` 的末态
  `knownVer` 都是 `max(v1,v2,v3)`，且数据是否有效只取决于
  "持有数据版本 == knownVer"，所以末态逐字段相同。
- 重复通知幂等是同一论证的推论：`max(S, x, x) = max(S, x)`。第二次投递
  同一通知时 `nv == knownVer` 且条目已处于失效态，无任何状态迁移，
  「实际失效次数」只在真实状态迁移时 +1，故重复投递不增加计数。
- 多副本收敛（第 8 条）：各副本的通知子集可能不同（有丢失），但每个副本
  的 `knownVer` 都是"它收到的通知版本 ∪ 它回源拿到的后端版本"的 max。
  存活时长到期后（左闭右开，`now == expiresAt` 即过期）每个副本都会重新
  回源，而后端是单一事实源、其版本只增不减，于是一轮过期后所有副本都拿到
  后端当前版本 `V`，`knownVer` 全部收敛到 `V`，可见状态一致。
  测试用 8 副本 + 每副本独立的随机丢弃/重复/乱序投递方案验证。

## 2. 回源结果的过期判定（第 6 条）

判定方式：回源完成时，在**副本互斥锁内**比较
`结果版本 rv` 与 `条目当前 knownVer`，`rv < knownVer` 则丢弃结果，
条目回到"失效待回源"。

- 为什么不能用"回源开始时的版本"：回源进行中可能到达更高版本的通知，
  开始时的版本对此完全不可见，旧结果会被写入缓存，用旧值覆盖新版本。
- 为什么不能只看"回源结束时的版本"而不在飞行期间记录通知：飞行期间到达
  的通知若不被记录到 `knownVer`，结束时就没有任何东西可比较——`rv`
  自身就是可疑的旧值，不能自证新鲜。必须把"期间到达的最高失效版本"
  持续累积在条目上，结束时才有判定基准。
- 临界区边界：`ApplyNotification`（提升 `knownVer`）与 `CompleteFetch`
  （比较 `rv` 与 `knownVer` 并写入）都在同一把 `replica.mu` 下执行，
  二者不可交错；loader 调用本身在锁外进行（慢键不阻塞其他键与通知）。
  因此不存在"比较完之后、写入之前又进来一条通知"的窗口：
  要么通知先落地（结果被判旧丢弃），要么写入先完成（通知随后使新值失效，
  单调性仍保持）。

## 3. 为什么失败不能缓存（第 5 条）

失败没有任何版本信息：一次后端错误不代表任何版本的数据状态。若把失败
写进缓存：

- 它会伪装成"有效数据"被后续读命中，直到 TTL 到期——直接违反第 5 条
  "失败后立刻重试必须真正再回源一次"（重试会被缓存拦截，回源计数不再
  增长）；
- 它没有版本，无法参与第 1 条的单调比较：后续通知到达时无法判定这个
  "失败值"是新是旧，`knownVer` 不变式被破坏；
- 它会把瞬时故障放大成 TTL 时长的错误可见性，违反"回源失败不污染"。

所以失败时条目只回到"可重新回源"态（`Hole`/`Stale`），所有等待者拿到
同一个错误，单飞调用当场解散，下一次读必然重新触发回源。

## 4. 索引结构与检查数上界（第 9 条）

索引就是一张 `map[string]*entry.Entry`，以键为索引。`ApplyNotification`
只做一次哈希查找定位受影响条目，检查数恰好为 1，与缓存中条目总数 N
无关；测试用 N=100 与 N=10000 两组对照，断言每条通知的检查数都是 1。
最坏情况上界：每次应用 O(1)（哈希表期望 O(1)，不遍历任何其他条目；
通知创建新条目时触发的"过期条目淘汰"见下节，它只在缓存满时发生，
是容量管理路径，不在正常通知路径上）。

## 5. 条目数超限的淘汰策略（第 10 条）

策略：**先淘汰已过期条目，仍满则拒绝**。

- 需要新建条目（首次读取或未知键通知）且缓存已满时，先按注入时钟删除
  所有 `now >= expiresAt` 的条目；腾出位置则继续，否则返回
  `ErrEntriesFull`，不改动任何已有状态。
- 为什么这不破坏第 1 条的版本单调性：被淘汰的只是**已过期**条目，而
  过期条目按第 3 条本来就不可服务——任何读都会触发重新回源。删除它
  不改变任何可观察行为，只丢失一个"已失效的事实"。活条目（未过期）
  永不被淘汰，它们的 `knownVer` 单调性原样保留。
- 被淘汰键的 `knownVer` 被遗忘是否安全：安全。之后该键的旧通知最多
  创建一个待回源条目（不携带任何数据），不会复活旧值；下一次回源从
  后端拿到的是当前版本，单调性由后端版本的单调性兜底。拒绝路径则
  什么都不改，平凡地保持单调性。
- 三类上限（条目数 / 单飞并发 / 总线队列）分别返回
  `ErrEntriesFull` / `single.ErrInflightFull` / `bus.ErrQueueFull`
  三个可用 `errors.Is` 两两区分的哨兵错误，且拒绝均为原子无副作用。
