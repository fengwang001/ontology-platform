# DESIGN

## 1. 末尾换行策略（空输入与只含换行）

规范化分两步：先做行尾/空白规范化（此时不增删末尾换行），再按策略调整结尾。

- `Keep`：不调整。空输入→`""`。
- `EnsureOne`：**仅当**结果非空且末尾不是 `\n` 时补一个 `\n`。空输入→`""`（不凭空制造内容；若选 `""→"\n"` 虽也幂等，但会把零字节文件改成有内容，编辑器场景不可接受）。幂等：调整后的输出必以 `\n` 结尾，第二次不再触发补写。
- `TrimBlank`：先删掉结尾全部 `\n`（末尾空行），结果为空则 `""`，否则补恰好一个 `\n`。因此无换行内容 `"abc"`→`"abc\n"`（"保留一个换行"）。幂等：输出恒为空或以单个 `\n` 结尾且最后一行非空。

样例表：

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

映射表只存"保留段"（runs）：每段记录 `[origStart,origEnd)` 与 `outStart`，段内等长线性；runs 之间的原文空隙即被删字节（行尾空白、`\r`、TrimBlank 删掉的末尾 `\n`）。

`ToOrig(o)`：二分找到含 `o` 的 run，线性换算；`o==len(out)` 取最后保留点或 0。
`ToOut(i)`：落在 run 内则线性；落在被删间隙则取**间隙右沿**，即下一 run 的 `outStart`（间隙在末尾时为 `len(out)`）。

推导：不变量要求 `ToOrig` 单调不减且 `ToOut(ToOrig(o))==o`。设间隙后第一个输出偏移为 `o`，则 `ToOrig(o)` 是下一 run 的起点，其 `ToOut` 必须恰为 `o`；间隙内任一原偏移 `i`，其像不能 `<o`（破坏单调与互逆）也不能 `>o`（与该起点的像冲突），唯一选择是 `o`。于是光标落在被删行尾空白或 `\r\n` 的 `\r` 上时，映射到**换行符本身的位置**（行尾、换行符之前），不会越过换行跳到下一行。

插入例外：`ToOut` 像集至多 `origLen+1` 个点；含插入的输出（策略给无结尾换行的内容补 `\n`）有 `outLen=origLen+1`，端点全互逆需 `outLen+1` 个不同像，鸽巢原理排除之。故插入的末尾换行与原文终点等同（`ToOrig(p)=origLen`、`ToOut(origLen)=p`），互逆等式对所有 `o<outLen` 成立；仅插入情形两个终端位置合一。

## 3. 空白缓冲超限：选择拒绝

备选"强制判定为非行尾并输出"必然违反幂等（第 5 条）：空白是否在行尾只有看到后续字节才知道；若强制输出而流实际在该行结束，输出含行尾空白，第二次 Close 才删除，结果不同。故选择拒绝：返回具名错误 `ErrWhitespaceLimit{OrigOffset}`（=该行起点偏移），已输出全部是已判定前缀并保留，映射只覆盖该前缀，实例进入终态（再写得 `ErrClosed`）。默认上限 0 表示无限，第 4 条逐切分一致性不受影响；小上限只在"同一超长空白行"触发，与切分方式无关。

## 4. par 拼接

切点 `c` 处未决状态至多两种：紧邻的待定 `\r`（1 字节），以及其前一串未判定空白。令每个内部起点
`l_k = c_k 之前最后一个 '\n' 的下一个位置`（没有则 0）。
`\n` 一定终结其所在行（无论前面是否挂着 `\r`），所以 `l_k` 必为确定行首；worker 区间 `[l_k, l_{k+1})`（末段到 n）恰好首尾相接、互不重叠地 partition 原文，输出直接拼接；内部段以 `Keep` Close（确定点关闭，无待定 `\r`/空白），仅末段施加用户的末尾策略。映射拼接：每段 runs 的 orig/out 坐标分别平移 `l_k` 与已拼接输出长度。

修正范围上界：有限空白上限 W 下，切点左侧 W+2 字节内必出现 `\n`（W 个空白 + `\r\n` 两字节），否则单线程也会在同一偏移报 `ErrWhitespaceLimit`；W=0（无限）时回退线性搜到上一个 `\n`（无删除点的长行本身是一个 run）。输出上限按已拼接前缀长度顺序计费，严格 NUL 以全局原文偏移判定，二者结果与分段无关。
