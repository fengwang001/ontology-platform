# 设计推导

## 1. 空行与单列空值
空字节流与一个尾随换行都表示“零条记录”。若空行算记录，则 `""`（空输入）与 `"\n"` 无法区分且往返歧义；故**空行（连续换行 / 首部换行）一律跳过，不计记录号**。空行定义：一条记录结束后在产生任何字段字节前再次遇到行尾。单列 `""` 与 `,""` 中逗号后的空则是真实的未引号空字段。
回写器必须 `Write(Parse("")) == ""` 且让单列表中的空字段能回读。结论：记录间与末尾统一写 `\n`（无“无换行末记录”表示），且**单列表的唯一空字段必须写成 `""`**——否则 `x` 为空时 `Write(T)` 为空字节，回读成零条记录，数据丢失。多列中的空字段无需引号（逗号已定位它）。规范输入定义：无空行、CRLF 只出现在引号字段内、字段仅在必要时被引号包裹、每条记录以 `\n` 结束；此集合上 `Write(Parse(x)) == x` 逐字节成立。

## 2. par 切点
切点可落在任意字节，段首状态有 6 种可能（字段起始S0/未引号中SU/引号中SQ/引号中刚见引号SQP/未引号CR待定CRU/引号中CR待定CRQ）。方案：段 0 从 S0 整段解析；段 k>0 先按两种**入口假设**各解析一遍——H_OUT（字段起始；其待续字段初值为空）与 H_IN（引号字段中；待续字段初值为空）。段末真实状态可直接判定：段末若在引号内则真实入口是 H_IN，否则 H_OUT。该判定无需看左邻：引号配对奇偶完全由“段起始是否在引号内”决定，两遍正好覆盖。首字节若恰为 `"` 的歧义段（H_OUT 下是开引号、H_IN 下是转义引号），以段末判定为准，丢弃另一遍。
段内解析产生两类事件：已闭合字段（含引号标记与偏移）、记录结束；段末未闭合字段作为“待续字段”（值+起始偏移+引号标记）交给拼接。拼接规则：下段首字段与上段待续字段合并；下段若一开始就闭合出空字段（入口在字段中却立刻遇到逗号/换行），用待续字段替换之。偏移：段内坐标加段起点字节偏移即全局偏移；记录号/字段号在拼接后按事件顺序从 1 编号。错误同样是事件，只重定位字节偏移；待续字段错误（超限等）以段事件流为准，不会重复。复杂度：每段两遍 + 首字节常数修正 ⇒ 处理次数 ≤ 2N+C。

## 3. CR 待定
未引号字段中见到 `\r` 不能立刻判行尾（可能是孤立 `\r` 错误），故进入 CRU：下一字节为 `\n` 则行尾；为其他字节则以该 `\r` 的偏移报孤立 CR 错误（`\r` 不属于字段内容）。引号字段内 `\r` 是内容，进入 CRQ：下字节 `\n` 作为 CRLF 原样并入；否则单 `\r` 保留。半包续传时 CRU/CRQ 是可暂停状态，等下一字节；`Close` 时停在 CRU → 孤立 CR（流在 `\r` 处结束），停在 CRQ → 字段内保留单个 `\r` 并正常闭合。par 段末停在 CRU/CRQ：该状态原样作为段末状态传给下一段作为入口假设继续，规则与续传完全一致；末段 `Close` 语义同上。

## 4. “立刻”上限
字段值按**逻辑字符**计数：未引号每字节 +1；引号字段内 `""` 作为一个引号字符 +1，普通字节 +1，开/闭引号本身不计。每并入一个逻辑字符前先比较 `MaxFieldBytes`，第一个超限字节即返回 `ErrFieldTooLong`，不缓冲剩余字节。记录字段数在每次字段闭合时计数，达到 `MaxFieldsPerRecord` 后再闭合一个即报错；记录数在每条记录结束时计数，达到 `MaxRecords` 后下一条记录结束即报错。出错即终态，后续 `Feed` 返回 `ErrTerminal`，已产出记录保留。

## 5. 状态转移表
字节类别：c=逗号 q=`"` r=`\r` n=`\n` o=其他字节。动作：emit=闭合当前字段 EOL=行尾（空行则跳过） append=并入内容(+1计数字节) Q=并入引号字符(+1)。
| 状态 | c | q | r | n | o |
|---|---|---|---|---|---|
| S0 字段起始 | emit 空字段,SU | →SQ | →CRU | EOL(空行,无字段) | append,SU |
| SU 未引号中 | emit,SU | ErrBareQuote | →CRU | EOL | append,SU |
| SQ 引号中 | append | →SQP | append,CRQ | append | append |
| SQP 刚见引号 | emit 闭合,SU | append Q,SQ | ErrQuoteClosed | ErrQuoteClosed | ErrQuoteClosed |
| CRU 未引号CR待定 | 孤立CR Err(再处理c) | 孤立CR Err(再处理q) | 孤立CR Err(再处理r) | EOL（CRLF） | 孤立CR Err(再处理o) |
| CRQ 引号CR待定 | append `\r`+c,SU* | append `\r`+Q,SQ | append `\r`+r,CRQ | append `\r\n`,SQ | append `\r`+o,SQ |

*CRQ 行在下一字节确定后先并入挂起的 `\r`，再按下一字节动作；逗号/换行在引号内即内容，不闭合。SQP 遇 `\r`/`\n`：闭合引号后行尾前出现异常，按 ErrQuoteClosed，偏移在该字节。EOF：SQ/SQP 未闭合→ErrUnclosedQuote；CRU→ErrBareCR；其余 flush。
