# 设计推导（unified diff）

包依赖单向：`lines → edit → hunk → udiff → patch`（patch 另依赖 lines）。

## 1. 零计数时 hunk 头的行号

统一格式头 `@@ -a,b +c,d @@` 恒定义为：片段覆盖旧文件第 a..a+b-1 行（共 b 行，1 基），新文件第 c..c+d-1 行。

纯插入时旧侧 b=0，区间为空，`a` 取「插入点之前的旧行号」：插在文件开头则前一行不存在，取 0（样例 `x\n`→`y\nx\n` 得 `-0,0 +1`）；插在第 2、3 行之间则 a=2（样例得 `-2,0 +3`，新侧首行是第 3 行）。
新侧 d=0（纯删除）同理 c=「删除点之后的新行号」：文件删空时后续行不存在，c=0（`a\n`→空 得 `-1 +0,0`，b=1 按规则省略 `,1`）。
Apply 读到 b=0：在旧文件第 a 行**之后**插入（a=0 即在文件最前）；d=0：在新侧第 c 行**之后**放置结果内容，c=0 即最前。

| 旧侧 | 书写 | Apply 解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一旧行行号（可为 0） | 在第 a 行后插入，a=0 → 文件首 |
| b=1 | `-a`（省略 `,1`） | 替换/删除旧第 a 行 |
| b≥2 | `-a,b` | 覆盖旧第 a 至 a+b-1 行 |

## 2. hunk 合并阈值

阈值 g ≤ **2C** 合并，g > 2C 分开（g = 两改动段之间未改动行数）。
每个 hunk 带最多 C 行上下文。若 g > 2C，两 hunk 上下文各取 C 行后中间仍剩 g−2C ≥ 1 行不显示，必须分开；
g = 2C 时上下文恰好相接（共 2C 行），GNU diff 合并为一个 hunk；g = 2C+1 时中间有 1 行无法显示，分开。
反例：阈值取 C 会把 g=2C（本应相连的上下文）错误拆开；取 2C+1 会在 g=2C+1 时强行合并，
中间那 1 行无处可放（要么少 1 行上下文，要么多出 1 行 ` ` 行），与 `diff -U C` 不一致。

## 3. 并列最短脚本的选择

采用前向 Myers（Ukkonen/V 数组，保存每层 V 快照回溯）。对角序号 k = x−y，同层 k 从 −d 到 d 步进 2。
每个 k 上取前驱时：**向下（删除）优先**，仅当向上（插入）严格更优时才向上（k=−d 必向上，k=d 必向下）。
故 `a b → b a` 输出 `-a`、` b`、`+a`（先删后插），与 GNU diff 一致；hunk 内同深度的 `-` 行排在 `+` 行前。
计数器记录每次对角线推进（含 snake 内逐行相等比较）的总步数，回溯不计。

## 4. 偏移后后续 hunk 的平移

第 i 个 hunk 记录起点为 r_i，实际在 r_i+δ 处应用成功，则第 i+1 个 hunk 的查找中心平移为
r_(i+1)+δ：前一个 hunk 实际增删的净行数 = 头中 (d−b)，与其记录值相同（整体平移不改变增删数），
所以后续所有行号统一平移 δ。搜索窗口仍为中心 ±F；多候选取 |实际−中心| 最小，并列取靠前（较小行号）。
