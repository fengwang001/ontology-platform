# 设计推导

## 1. 非法单元切分规则（WHATWG 最大子部）

在首字节处按"期望长度"读序列：第二字节若不在其首字节专属合法区间内，该首字节独立成一个非法单元（吞 1 字节，故 `F0 90 80 41` 只替换 1 次，`90 80 41` 重新解析）；
第二字节合法但后续字节不是 10xxxxxx：缺几个尾就吞几个（`E2 82`+EOF 缺 1 个尾 → 1 个 FFFD），遇到非续字节立即终止、该非续字节留作新首字节（不提前替换）。
首字节 80..BF / C0 C1 / F5..FF 本身非法，吞 1 字节。于是 `E0 80 80`（第二字节非法→1，剩 80 80 各 1）、`ED A0 80`（代理区）、`C0 AF`、`F4 90 80 80`（越界）均为 3 或 4 个 FFFD。

| 首字节 | 第二字节合法区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 吞首字节 1 |
| E0 | A0..BF | 吞首字节 1 |
| E1..EC | 80..BF | 吞首字节 1 |
| ED | 80..9F | 吞首字节 1 |
| EE..EF | 80..BF | 吞首字节 1 |
| F0 | 90..BF | 吞首字节 1 |
| F1..F3 | 80..BF | 吞首字节 1 |
| F4 | 80..8F | 吞首字节 1 |
| 80..BF | 无（首字节即非法） | 1 |
| C0 C1 F5..FF | 无（首字节即非法） | 1 |

区间同时排除非最短（E0 拒 <A0、F0 拒 <90）、代理（ED 拒 >9F）与越界（F4 拒 >8F）。

## 2. 切分缓存上限与立即判废

UTF-8 最长 4 字节，已确认首字节+合法第二字节后只差后续字节，故只需缓存"未完成前缀"至多 3 字节；看到字节即可判定的情形（首字节非法、第二字节越界、尾位出现非续字节）立刻产出非法单元，绝不等 EOF。
UTF-16 每个 code unit 2 字节，缓存至多 1 个奇数字节；再缓存至多 1 个高代理（2 字节），故总残留 ≤3 字节。只有 Close 时残留才判截断。

## 3. par 切点对齐

第 k 段起点可能落在单元内部：从起点向回看——UTF-8 连续跳过续字节 10xxxxxx（至多 3 个），再看其前一个首字节声明的长度是否覆盖段起点；UTF-16 按全局偏移奇偶对齐到 2 字节边界。
若起点被前一个单元覆盖，则把解析起点再前移到该单元起点；即"前一段只处理在段内完整结束的单元，尾巴（含被切开的非法单元）整体交给下一段"。
每个单元的统计（合法字节/吞掉字节/非法数）记在其**起点所在段**，于是各段统计之和与单线程流完全一致。
`C3|80 80`：段二回看发现 80 被段一的 C3 覆盖，起点前移到 C3，作为一个完整合法字符解析；末尾独立的 80 单独成非法单元，非法数恰为 1。回看为有界常数（UTF-8 ≤4、UTF-16 ≤2），故 K=8 时检查次数 ≤ N+8·常数。

## 4. 输出上限断点

Write 输出以标量（UTF-16 以整个代理对）为最小单位；若下一个标量会越限，则不输出它，返回哨兵 ErrLimit，已输出内容与统计不动。
已返回的已消费字节数 n 只对应已输出标量：因切分缓存中的半成品从未被确认，n 可能小于 len(p)。错误附带绝对 ResumeOffset（未输出单元在整个输入流的起点），调用方从该偏移重喂给新实例，拼接逐字节等于无上限结果。
EOF 残留在 Close 才处理，与上限无关；缓存字节数恒 ≤ 第 2 节上限。
