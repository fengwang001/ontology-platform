# DESIGN — unified diff 差分/渲染/补丁

## 1. 零计数时的行号（题面第 4 条）

GNU hunk 头 `-a,b` 中 `a` 是**该 hunk 覆盖的旧侧区域第一行的 1 基行号**；
该区域由 `b` 行组成（全为 `-/ `），纯插入时 `b=0`、区域为空，
空区域夹在旧文件第 `a` 行与第 `a+1` 行之间，故 `a` 是「插入点前一行的行号」，
文件开头前一行的行号取 0。

- `x` → `y,x`：插入点在第 0 行之前 → `-0,0`；新侧区域非空，首行号 1 → `+1`。
- `a,b,c` → `a,b,X,c`：插入点在旧第 2 行之后 → `-2,0`；新侧首行号 3 → `+3`。
- 删光 `a`：旧侧首行号 1 → `-1`；新侧空，插入点在新第 0 行前 → `+0,0`。

Apply 解析：`b=0` 时匹配位置 = 旧行 `a` 之后（`a=0` 即文件头）；`d=0` 同理。

| 旧侧书写 | b | 含义 | Apply 区域起点（0 基索引） |
|---|---|---|---|
| `-0,0` | 0 | 文件开头纯插入 | 索引 0 |
| `-k,0` (k≥1) | 0 | 在旧第 k 行后插入 | 索引 k |
| `-a` | 1 | 旧第 a 行这一行（`,1` 省略） | 索引 a−1 |
| `-a,b` (b≥2) | b | 旧第 a..a+b−1 行 | 索引 a−1 |

## 2. hunk 合并阈值（题面第 5 条）

结论：**g ≤ 2C 合并，g ≥ 2C+1 分开**。
设两段改动间未改动行 g。各自向外取 C 行上下文：需要 `2C` 行才能同时满足两边
（C 行给上 hunk 尾部、C 行给下 hunk 头部）。
- g = 2C：上下文恰好不重叠，合并后中间 g 行全部可见；分开则两 hunk 间
  无任何分隔行，GNU 输出为单个 hunk，故阈值不能小于 2C。
- g = 2C+1：分开后两 hunk 之间恰好留 1 行不在补丁中，与 `diff -U C` 一致；
  若阈值取 2C+1 则该行会被吞进 hunk，输出多一行上下文，不一致。
- 取 C 更不对：g=C+1 时 GNU 仍合并（上下文可重叠共享），取 C 会错误分开。

## 3. Myers 并列最短路径的选择（题面第 3 条）

固定规则：每条 furthest-frontier 递推中 k 相同（并列）时**优先删除**
（取 k+1 的前驱，即先走向下的删除边），再走 snake。
确定性还包括：相同字节行按内容全等比较、hunk 内严格按编辑脚本顺序输出。
例 `a b` → `b a`（D=2）：优先删除得 `-a / 'b' / +a`，
即 `-` 行排在 `+` 行之前；优先插入会得 `+b / 'a' / -b`。本实现固定为前者。

## 4. 偏移后的后续 hunk（题面第 7 条）

hunk i 以偏移 δ（实际匹配位置 − 记录位置，带符号）应用后，
hunk i+1 的查找中心平移 δ：前 hunk 在原文中净删 r 行、净插 p 行，
其后所有行整体平移 p−r，而 δ 已隐含此前所有净增减；以前一 hunk
应用后的新坐标为基准，记录起点 + δ 正是期望值。平移是全量平移，
F 窗口在新中心上下各 F 行；多候选取 |实际偏移| 最小，相同取靠前。
应用在工作副本上进行，任一 hunk 失败则丢弃副本。

## 5. 其他约定

- 行保留原行尾（`\n`/`\r\n`/无）；仅末尾换行变化时，该行渲染为
  紧跟 `\ No newline at end of file` 的一对 `-`/`+`。
- 计数器：每次沿对角线逐行比较（含成功与失败的那一次）计 1。
- 解析严格：头计数与实际行数必须与 b/d 恰好一致；`\` 标记行合法且不计入 b/d。
