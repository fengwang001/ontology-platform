# 带溢出的哈希聚合器 — 设计推导

## 1. 溢出什么：原始行 vs 部分聚合状态

候选 A「溢出原始行」：把选中分区的原始输入行写盘，回读时重新聚合。
候选 B「溢出部分聚合状态」：把选中分区已聚合的中间状态 `(Count, Sum, Min, Max)` 写盘，回读时与后续同键状态合并。

本题选 **B**，论证：

- 磁盘占用有界：每个键在磁盘上只占一条定长状态，而 A 的溢出文件随该分区行数线性增长，可能远超内存表本身。
- 回读成本低：B 回读的是已压缩状态，重新聚合代价为 O(分区内不同键数)，A 为 O(分区内行数)。
- 与内存表同构：内存里存的本来就是 `map[key]State`，溢出只是把 map 的一部分序列化，回读合并与在内存中继续 `Add` 走同一套 `Merge` 代数，无需第二套「重新聚合」代码路径，少一类不一致风险。
- A 的唯一优点是直观；但在「同一分区多次溢出」时 A 要管理追加写的行流，B 只需多段定长状态文件，合并规则统一。

## 2. 同一分区多次溢出为何仍然正确

设键 k 的输入值按到达序为 v1..vn。内存状态机满足：任意时刻内存中的
State = fold(Add, 某段连续子序列)。溢出即把当前 State 快照写盘并清空内存态，
后续行再从零累积。最终 k 的结果 =

  Merge(seg1, Merge(seg2, ... Merge(segK, memResidual)))

其中 seg1..segK 是第 1..K 次溢出的快照，memResidual 是内存剩余状态。
只要 Merge 满足：

- 封闭性：两个 State 合并仍是 State；
- 结合律：Merge(Merge(a,b),c) = Merge(a,Merge(b,c))；
- 单位元：Count=0 的空态 e 满足 Merge(e,s)=Merge(s,e)=s；
- 与 Add 一致：Merge(fold(v1..vi), fold(vi+1..vn)) = fold(v1..vn)；

则按任意切分点分段、按固定顺序合并，结果等价于一次性全内存聚合。
因此**回读时必须收齐该分区的全部溢出段**（段文件按序号命名、按序读取），
漏掉任何一段等价于丢掉一段连续输入子序列，必然少算——这是本题最易错点。
实现上段文件命名为 `p<分区>-<序号>.part`，读回时 glob 该分区全部段、按序号排序。

## 3. Min/Max 与 Count/Sum 在合并上的差异

- Count：合并 = 整数相加，单位元 0，严格满足结合律与交换律。
- Sum：合并 = 浮点相加，单位元 0.0。**数学上**满足结合律，但 IEEE-754
  浮点加法有舍入，不同分组顺序可能产生不同位模式。因此实现固定合并顺序
  （分区号升序 → 段序号升序 → 内存残余最后），保证逐次运行逐位可复现；
  与全内存参考结果做逐位比对时，测试数据取整数值 float64（加法精确，
  结合律严格成立）。
- Min/Max：合并 = 对两个候选取 min/max，单位元分别是 +Inf/-Inf（实现里用
  Count=0 的空态统一充当单位元）。满足结合律与交换律，且无舍入问题，
  任意合并顺序结果逐位相同。这与 Sum 的关键差异：Min/Max 是**选择**运算
  （幂等、无精度损失），Sum 是**累积**运算（受浮点舍入影响）。
- ±0.0：Add 时把 -0.0 归一化为 +0.0，Min/Max/Sum 不再区分二者。
- NaN 在输入侧拒绝（计入跳过数），±Inf 合法参与全部四种聚合。

## 4. 内存与复杂度约束

- 驻留上限：插入新键前若 `len(table) == budget`，先溢出受害分区
  （优先新键所属分区，若其无驻留键则选驻留键最多的分区），再插入，
  故任意时刻 `len(table) <= budget`；非导出计数器记录历史峰值。
- 每行处理 ≤ 2 次：入表一次；若被溢出，回读合并一次。计数器
  `Processed = 输入行数 + 回读条数 <= 2N`，且 `Written == ReadBack`。
- 分区数构造时固定，与数据规模无关；溢出文件数 <= 分区数 × 每分区溢出次数。

## 5. 分区文件格式与故障语义

```
header(16B): magic u32 | version u8 | pad 3B | partID u32 | entryCount u32
entry*:      bodyLen u32 | body | crc32(body) u32
body:        keyLen u32 | key | count i64 | sum f64 | min f64 | max f64
```

- 截断分类：偏移 < 16 → 头部不完整；落在某 entry 的 4B 长度前缀内 → 长度前缀
  不完整；落在 body 内 → 行体不完整；落在 4B CRC 内或校验值不符 → CRC 不匹配。
  四类错误为不同哨兵错误，`errors.Is` 可区分，错误携带分区号与字节偏移。
- 写盘：先写 `*.part.tmp` 再 `rename` 为 `*.part`；启动时清理目录内残留
  `*.tmp`（视为崩溃残骸，绝不读入）；读路径只 glob `*.part`。
- 写失败可注入第 k 次写失败；聚合中止并删除本次已产生的全部分区文件，
  残留计数为 0。

## 6. 并发与确定性

回读阶段每分区一个 goroutine，只读各自段文件，聚合状态互不共享；
合并阶段在单线程内按「分区号升序、段序号升序」的固定顺序进行，
与回读完成顺序无关，故输出分组顺序与 Sum 位模式逐字节确定。
`-race` 干净：goroutine 间仅通过按分区号定址的结果切片 + WaitGroup 交接。
