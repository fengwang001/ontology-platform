# 设计推导

## 1. 空行与单列空值
空行不跳过，而是"一条只含一个未引号空字段的记录"：两个相邻行尾之间完成了一条 1 字段记录。
单列空记录的唯一字段若裸写为空，在"流末换行不产生额外记录、无尾换行也合法"的规则下，
无法同时表达引号标记与尾部形态差异。因此规范输入（canonical）定义为：每条记录含最后一条都以
LF 结尾；单列表中的空字段必须写作 `""`。这样 `Parse(Write(T))` 对任意成功解析的 T 逐字段相等
且 Quoted 标记相同：非空字段的 Quoted 仅在含 `,` `"` `\r` `\n` 时才必要（这些字符在裸写时改变
词法），单列空字段的引号是第 4 条推出的必要情形；多列空字段裸写无歧义，不必引号。
规范输入 x（全尾 LF、单列空字段为 `""`、只在必要处引号）满足 `Write(Parse(x))==x` 逐字节。

## 2. par 切点
切点可落任意字节。每 worker 把本段（末段含 EOF，非末段额外只读右邻 1 字节做裁决）在两种初始
假设下各解析一遍：H0=段首为 FStart；H1=段首在 QField（开引号此前已消费）。结果含完整事件、
段末状态(FStart/Mid/QField/QPend/CR)与挂起字段。协调器从段0 H0 起，按上段真实末态选择：
引号内(QField/QPend)取 H1，否则 H0；拼接修正三处：(a) 上段挂起字段与本段首字段首尾相接；
(b) 段内已关闭记录直接采纳；(c) QPend/CR 跨界由右邻首字节裁决（该字节仅裁决、不重复发事件）。
字节偏移 worker 直接用全局绝对偏移；记录号=前缀完整记录数+段内序号；字段号=当前记录前缀字段数
+段内序号。故错误坐标即全局坐标。

## 3. CR 待定
未引号字段中见 `\r` 进 CR 态暂不发字段：次字节 `\n`→行尾(CRLF，CR 不入值)；其他→孤立 CR 错。
半包：CR 为段末字节则挂起，下次 Feed 首字节裁决，Close 仍在 CR 态→孤立 CR 错（偏移指 CR）。
par：左 worker 多吃右段首字节裁决，末段 EOF 时判孤立 CR。引号内 CR 是内容，无待定态。

## 4. 上限"立刻"
未引号字段每内容字节 +1；引号字段每数据字节 +1（转义对 `""` 在见第二个引号确认为一个 `"`
字符时 +1）。追加前比较，第一个超限字节即错，不缓冲整字段。字段数在每字段发射时检查；记录数
在每记录发射前检查。

## 状态转移表（+ 追加值, f 发字段, r 发记录）
| 状态\输入 | `,` | `"` | `\n` | `\r` | 其他 | EOF |
|---|---|---|---|---|---|---|
| FStart | f,→Mid | →QField | f,r | →CR | +,→Mid | 发挂起字段 |
| Mid | f | Err裸引号 | f,r | →CR | + | 发挂起字段 |
| QField | + | →QPend | + | + | + | Err未闭合 |
| QPend | f | +引号,→QField | f,r(CR 后\n 为CRLF) | f,r | Err闭合后字符 | 发挂起字段 |
| CR | f,r 再处理逗号 | Err | f,r | Err孤立CR | Err孤立CR | Err孤立CR |
