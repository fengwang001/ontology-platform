# SSE 增量解析器设计推导

三个包单向依赖：`linescan`（字节→行）← `field`（行→字段）← `stream`（字段→事件/重连状态）。

## 1. CRLF 与块边界落在 CR/LF 之间

`linescan` 用一个 1 字节的 CR 挂起状态 `crPending`，行尾只有在确认下一个字节不是 `\n` 后才真正发射：

```
正常态 --看到 \r--> 挂起态（记住上一行，不发射）
挂起态 --看到 \n--> 正常态，发射一次行（吃掉 LF）
挂起态 --看到 \r--> 正常态发射一行，再回挂起态（\r\r 是两个行尾）
挂起态 --看到普通字节--> 正常态发射一行，再正常处理该字节
挂起态 --块结束--> 保持挂起；下一 Feed 的首字节继续判定
```

因此 `\r\n` 只产生一次行尾；`\r` 在某块末尾、`\n` 在下一块首字节时，状态跨块保留，仍然只算一次，绝无两个空行。

## 2. 切分点无关的不变量

切分只改变 `Feed` 的调用次数，不改变字节顺序。要保证结果不变，跨调用必须保留全部"未决断言"：

- 半行缓冲 `pending`（尚未见行尾的字节）；
- `crPending`：最后一个字节是否为待决的 `\r`；
- BOM 前缀匹配长度（BOM 的三个字节也可能跨块）；
- 上层 `stream` 的事件缓冲（data/event/id/retry）。

不变量：**第 n 次 Feed 返回后，解析器的内部状态只取决于"已喂入字节的拼接"，与切块方式无关**。证明用归纳法：初始状态相同；每次处理单个字节是纯状态迁移、输出只依赖该字节与当前状态，故把一次大 Feed 拆成任意小 Feed 后，逐字节到达的 (状态, 输出) 序列一致，最终状态与输出序列也一致。测试对同一输入枚举 chunk=1..len 验证事件序列全等。

## 4. 为什么只有注释的块不能派发

派发条件是「数据缓冲非空 *或* 设置过 event 名」。注释行（`:` 开头）与未知字段都不触碰这两个缓冲。若空行无条件派发，心跳注释块（`:keepalive\n\n`）会让下游观察到一个 `data==""` 的合成消息事件——下游无法区分它与真实的 `data\n\n` 空数据事件，于是心跳、填充被误当业务消息处理。所以注释只复位判定、不产生事件；派发后清空事件缓冲，但 `lastEventID` 属于连接级状态，必须保留。

## 9. 超限拒绝为什么必须零状态改变

超限（单行 > MaxLineBytes、单事件 data > MaxDataBytes）返回两个可判定的独立错误，并保证已聚合状态不变。实现上用快照回滚：出错时恢复 data/event/hasEvent 与下层扫描器状态。

若改成"截断后继续"，则同一逻辑输入在不同切块点下，超限发生的位置不同（例如限制 4 字节，`abcdef\n` 按 3+3 与按 5+1 切块会在不同调用点越界），截断保留下来的字节数也不同，最终事件内容随切块方式变化——直接破坏第 2 条"任意切分点事件序列完全相同"的断言。全有或全无的拒绝使输出（错误 + 不变状态）对切块方式同样不可区分。
