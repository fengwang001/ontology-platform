# Set-Cookie 解析与适用性判定器 — 设计推导

包依赖单向：`attr`（解析）← `jar`，`match`（域/路径匹配）← `jar`，`attr` 与 `match` 互不依赖。
时间只走注入时钟 `func() time.Time`，状态全在进程内存，不碰网络与文件系统。

## 1. 默认路径推导（语义第 2 条）

规则（与 RFC 6265 §5.1.4 一致）：取来源 URL 的路径，若不以 `/` 开头则默认路径为 `/`；
否则找到路径中**最后一个** `/`，若它是首字符（下标 0）则默认路径为 `/`，
否则取它之前的子串。

- `/a/b/c`：最后一个 `/` 在下标 4，前缀 `/a/b`。
- `/a`：最后一个 `/` 在下标 0（首字符），没有"目录部分"可截，落到 `/`。
- `/`：同上，推出 `/`。

为什么 `/a` 推出 `/` 而不是 `/a`：默认路径的语义是"设置者所在的目录"。
`/a` 本身就是根目录下的一个资源，它的目录是 `/`；若把资源自身当目录，
等于让该资源冒充一个路径前缀去匹配 `/a/...` 下所有兄弟资源，越权放大了作用域。
只有路径里确实存在第二个 `/`（即确有目录层级）时，截出来的才是真实目录。

## 2. 路径匹配不是前缀匹配（语义第 3 条）

朴素前缀匹配下，cookie 路径 `/a` 会命中请求路径 `/ab`：`strings.HasPrefix("/ab","/a")` 为真。
越权场景：站点把 `/a` 分给应用甲、`/ab` 分给应用乙（如 `/admin` 与 `/admin-backup`，
或 `/app` 与 `/apple`）。甲在 `/a` 下设的会话 cookie 会被发往乙的 `/ab`，
乙可读取/重放甲的会话；反之乙也能用同名 cookie 覆盖甲的会话（会话固定攻击）。
因此匹配规则必须是：请求路径 == cookie 路径，或请求路径以 cookie 路径为前缀
**且**（cookie 路径以 `/` 结尾，或请求路径在边界处的下一字符是 `/`）。
`/a` 匹配 `/a`、`/a/`、`/a/b`，不匹配 `/ab`。

## 3. Domain 越权判据（语义第 4 条）

无 `Domain` 属性：host-only，只匹配来源主机自身，子域不匹配。
有 `Domain`：忽略前导点后记为 d，判据为——**来源主机必须等于 d 或是 d 的子域**
（即 host == d 或 host 以 "."+d 结尾），否则拒绝。这推出两类必拒：

- 异域越权：`b.example.com` 设 `Domain=example.org`，host 不以 d 结尾，拒绝。
- 上级/公共后缀越权：`b.example.com` 设 `Domain=com`，`b.example.com` 以 `.com` 结尾，
  形式上通过第一条判据，但 `com` 是单标签域（公共后缀级），若放行，
  该 cookie 会被发往 `com` 下所有站点，等于跨站注入。
  因此附加判据：**d 必须含至少一个 `.`**（多标签），单标签一律拒绝。
  这是公共后缀检查的保守近似：不联网、不内置 PSL，单标签必为公共后缀级。

## 4. 覆盖时保留最早创建时间（语义第 6、8 条）

发送顺序规则：路径长者优先；路径等长时创建时间早者优先。
身份是 (name, domain, path) 三元组，同身份再设是覆盖。

若覆盖时刷新创建时间，则"谁最后刷新谁排最后"：两个等长路径的 cookie
A（先建）、B（后建），本应是 A 在 B 前；但只要 B 被重新设置一次，
顺序就翻转为 B 在 A 前。刷新是服务端可主动触发的（重发同名 Set-Cookie 即可），
于是发送顺序被远端任意操纵，且同一批 cookie 的相对顺序随时间漂移、不可复现。
保留最早创建时间则顺序只取决于"首次出现"这一不可篡改的历史事实，
覆盖只更新值与过期，排序稳定、可断言。
