# 设计推导

## 1. 零计数 hunk 头的行号（题面§4）

三个样例：`x\n`→`y\nx\n` 得 `-0,0`；`a\nb\nc\n`→`a\nb\nX\nc\n` 得 `-2,0`；`a\n`→空得 `+0,0`。
假设数字是"将要插入的位置"（1-based 新行行号）：例 2 插入在第 3 行之前，应写 `-3,0`，
与样例 `-2,0` 矛盾。假设数字是"插入点之前已有的行数"（即前一行的 1-based 行号，文件
开头为 0）：例 1 插入点之前 0 行 → `-0,0` ✓；例 2 之前 2 行 → `-2,0` ✓；例 3 新文件
为空，位置之前 0 行 → `+0,0` ✓。三例全部吻合，且与 GNU diff 一致。

结论：计数为 0 时数字 = 该侧插入点之前的行数；计数 > 0 时数字 = 首行的 1-based 行号。
`Apply` 解释：count>0 → 起始下标 = start-1；count==0 → 插入点下标 = start（第 start 行之后）。

| 情形 | 头写法 | 数字含义 | Apply 插入点 |
|---|---|---|---|
| b=0 | `-a,0`（`,0` 不省略） | a = 插入点前的旧行数 | 下标 a（第 a 行之后插入） |
| b=1 | `-a`（省略 `,1`） | a = 该行 1-based 行号 | 下标 a-1 起替换 1 行 |
| b≥2 | `-a,b` | a = 首行 1-based 行号 | 下标 a-1 起替换 b 行 |

## 2. hunk 合并阈值 = 2C（题面§5）

设两段改动下标为 i、j，夹 g = j-i-1 行未改动行。第一段 hunk 尾部上下文延伸到 i+C，
第二段头部上下文从 j-C 开始。两 hunk 能分开，当且仅当两段上下文之间至少隔着一行既
不属于前者也不属于后者的行：j-C > i+C+1 ⟺ g > 2C。故 g ≤ 2C 合并，阈值 T = 2C。

* 若 T = C：取 C=3、g=4。两侧上下文各伸 3 行，3+3=6 ≥ 4，区域重叠，GNU diff 合并；
  T=C 却拆分，不一致。
* 若 T = 2C+1：取 g = 2C+1。两段上下文之间恰好剩 1 行不属于任何 hunk，GNU diff 输出
  两个 hunk（该行不出现在输出中）；T=2C+1 却合并，不一致。

## 3. 最短脚本的并列选择（题面§3）

Myers 每步在 k = k-1（右移=删除）与 k = k+1（下移=插入）之间选 V 值更大者；本实现规定
V[k-1] == V[k+1] 时取删除（条件写成 `V[k-1] < V[k+1]` 才选插入）。效果：`a b`→`b a`
的脚本为 `-a`、` b`、`+a`，即先删后插，与 GNU diff 一致；替换块在 hunk 内呈现为全部
`-` 行在前、`+` 行在后。该规则固定，故同一输入的补丁文本逐字节确定。

## 4. 偏移应用后后续 hunk 的起点（题面§7）

补丁记录的位置相对原文件。已应用的 hunk 累计增删 Δ = Σ(new-old) 行，后续 hunk 的记录
位置必须加 Δ 才对应当前文本，否则前面每增删一行后面全部失配。此外，若前一 hunk 实际
命中位置相对修正后位置偏移了 k（说明目标文件被第三方整体平移），同一平移大概率也作
用于后续区域，故后一 hunk 的查找中心 = 记录位置 + Δ + k_prev，再在 ±F 内取最近匹配
（距离相同取靠前者）作精修。k_prev 只是初猜，精修仍由逐候选精确匹配保证正确。
