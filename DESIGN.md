# 随机超平面 LSH 分桶检索器 — 设计与推导

## 1. 模型

- 固定维度 `d` 的 `float64` 向量；索引阶段每条向量分配整数 ID。
- 随机超平面 LSH（Charhar / SimHash 式）。第 `l` 张表有 `b` 个单位无关的随机法向量
  `w[l][j]`，每个分量由显式种子 `seed + l*b + j` 派生的 RNG 用标准正态生成。
- 签名是 `b` 位整数。第 `j` 位规则：
  - `dot(x, w) > 0` → 位 `j = 1`
  - `dot(x, w) < 0` → 位 `j = 0`
  - `dot(x, w) == 0`（含零向量）→ 按位索引 `j` 归一化：`(j 为偶数取 0, 奇数取 1)`，
    即零向量签名为位掩码 `0b0101...`。该约定仅依赖签名位下标，确定性、可复现，
    避免“零内积全取 0”让零向量与特定半球向量系统性共桶造成偏置。
- 每次点积分量乘加即记一次哈希计算，故哈希计算次数恰为 `N × L × b`，由计数器精确断言。

## 2. 位数 b、表数 L 与候选数、召回的关系

两向量夹角为 `θ` 时，单个超平面给二者相同位的概率是

  `p = 1 - θ/π`

（超平面法向均匀球面分布）。

**单表同桶概率**：`p^b`。b 个位必须全部一致，b 越大同桶越难，桶越细：
- 候选数随 b 单调下降（每次多一位相当于再做一次 1/2 量级划分）；
- 近邻（θ 小、p 接近 1）同桶概率仍较高，远邻（θ≈π/2, p≈1/2）同桶概率≈`2^{-b}`，
  被快速滤掉。

**L 表取并集**：一条近邻在至少一张表同桶的概率为

  `P_hit(L) = 1 - (1 - p^b)^L`

L 增大时 `P_hit` 单调上升趋于 1，故召回随 L 单调不降；代价是候选并集变大
  （期望候选数 `≈ N(1-(1-2^{-b})^L)`，同维度均匀数据）。

**权衡结论**：b 调节“桶粗细/单表过滤强度”（b↑ 候选↓ 召回↓），L 调节“重试次数”
（L↑ 召回↑ 候选↑）。用小 b 多表获得高召回，用大 b 少表获得高过滤性。

## 3. 检索

1. 对查询在每张表算签名，取各表该签名桶的 ID 并集（跨表去重）；
2. 仅对候选计算精确欧氏距离并精排，取 TopK；候选为空时结果为空；
3. 计数器记录“精排阶段算过距离的向量数”（去重后），5000 向量上必须 ≤ 500。
4. 暴力精确检索器计算全量距离作为召回基准；召回 =
   `|近似Top10 ∩ 精确Top10| / 10`，平局按 ID 次序打破，保证两法一致。

## 4. 索引与并发

- 建索引在私有结构中逐表完成，全部表构建好后一次性原子替换对外索引指针
  （整体发布）；查询只看到“全部表可见”或“全部不可见”，不读半成品表。
- 发布后索引只读，多协程并发查询无需加锁；`-race` 下干净。

## 5. 持久化格式（自描述，小端序）

```
"LSHB" | u16 版本=1 | u16 维度 d | u32 表数 L | u32 位数 b
| u64 种子 seed | u64 向量数 N
| L*b 个法向量：每个 [d]float64
| N 条：u32 id | [d]float64
| L 张桶表：每张 u32 桶数，随后每桶 u64 签名 | u32 条目数 | [u32 id...]
| u32 IEEE CRC32（覆盖此前全部字节）
```

- 严格加载：CRC 不符 → `ErrCRC`；长度不足按落点分类为
  `ErrHeader`（头）/ `ErrFamilies`（法向量）/ `ErrBuckets`（桶表）。
- 最大可恢复前缀：按字段顺序容忍尾部截断，返回截断点前完整解析出的索引与
  `Recovered{DroppedRefs, Err}`；桶表中引用了不存在向量 ID 的条目被剔除并计数，
  保证无悬挂 ID。
- 法向量全零退化 → `ErrDegenerate`，错误中包装第几张表第几位（从 1 起），
  可用 `errors.Is` 判定；维度不符 → `ErrDimMismatch`（带期望/实际维度）。
- 向量含 NaN 或 ±Inf 在建索引阶段被拒绝并计入 `Skipped`；零向量合法；维度 1 合法。

## 6. 可复现性

所有随机性来自显式 `int64` 种子；法向量由 `rand.New(rand.NewSource(seed))`
按表、位、维度固定顺序生成（Go 顶层源在同版本下确定）。同种子两次建索引，
法向量与每个向量的签名（即签名表）逐字节相同；不同种子以高概率产生不同签名，
测试断言两者存在差异。
