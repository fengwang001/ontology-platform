# 设计推导：只读快照一致性导出器

模块 `ontology`，仅标准库。包划分：`store`（多版本键值）、`snapshot`（快照与写时复制）、
`export`（分块导出与续传）、`manifest`（清单与文件格式）、`verify`（校验）、`cmd/demo`。

## 1. 写时复制：触发点与释放条件

**问题**：导出进行中键被改写，若直接覆盖，导出读到新值，快照不再是某一时刻的一致视图。

**模型**：`store` 是单调多版本存储，每次 `Set` 产生新版本号（水位线）。快照 = 打开时刻的
版本水位 `v`。键 `k` 对快照可见的值 = 版本 <= `v` 的最新值。

**触发点推导**：设写入把 `k` 从版本 `a` 推进到 `b`（`a < b`）。对任意打开的快照 `s`（版本 `v_s`）：

- 若 `v_s < a`：`k` 在快照时刻不存在或可见值更老，而更老的值已在更早写入时被保留（归纳不变式）；
- 若 `a <= v_s < b`：旧值（版本 `a`）正是 `s` 可见的值，覆盖后丢失，**必须在覆盖前保留**；
- 若 `v_s >= b`：不可能，因为快照版本 <= 当前版本 < `b`。

所以触发点唯一确定：**`store.Set` 覆盖前**，对所有满足 `1 <= a <= v_s` 的打开快照 `s`，
把 `(k, 旧值)` 记入 `s` 的保留集（已保留则跳过，首次覆盖保留的就是快照可见值）。快照读取
时**优先查保留集**，未命中再按版本读 `store`。不变式：保留集与版本化存储足以重建快照视图。

**释放条件推导**：保留值存在的唯一理由是被某个打开的快照引用。快照 `s` 关闭后，没有任何
读者能以版本 `v_s` 观察数据，故 `s` 的保留集可整体释放。等价地（共享保留池实现），一条
保留值可释放当且仅当**最老打开快照的版本**越过它：最老快照是最保守的读者，它都不需要了，
更新的快照更不需要。两个重叠快照时，关闭较新的那个不影响最老水位，旧值必须继续保留；
只有两个都关闭，水位彻底越过，才全部释放。测试据此断言。

**开销**：保留集大小 <= 导出期间被改写的不同键数，与总键数无关。

## 2. 导出顺序无关性

快照视图是一个**纯函数**：版本到键值映射的部分函数，与读取顺序无关——每次 `Get` 的结果
只取决于版本水位与保留集，不依赖此前读过哪些键。因此按任意键序导出，得到的**键值对集合**
相同。分块边界与逐块 CRC 随顺序变化，但总校验和定义为对全部键的 `CRC32(key‖0x00‖value)`
取 **XOR**：XOR 满足交换律与结合律，故总校验和是集合的函数，与顺序无关。测试用字典序与
逆序断言总校验和相同。

## 3. 快照过期时拒绝续传

续传的语义是「继续完成**同一时刻**的快照导出」。已写出的前 `k-1` 块来自版本 `v` 的快照；
若原快照已关闭，其保留集已释放，续传只能读到当前数据（版本 `v' > v`），拼出的文件前段
属于时刻 `v`、后段属于时刻 `v'`，是跨时刻的不一致文件，且总校验和仍可能碰巧合法——
**静默产出错误结果比报错更糟**。因此 `Resume` 首先校验：快照未关闭且快照版本等于清单
版本，否则返回可判定的 `ErrSnapshotExpired`，且**不触碰**已导出的部分文件（它是合法的
中间产物）。关闭与导出竞争时，导出要么在关闭前完成（结果正确），要么读到
`ErrSnapshotClosed` 而中止并清理临时文件——先写 `.tmp` 再 rename，最终路径上绝不出现
被当成完整文件的半截文件。

## 4. 文件格式与续传

文件布局：`[magic "ONS1"][manifestLen u32][manifest JSON][chunk]*`，
块：`[index u32][keyCount u32][bodyLen u32][crc32 u32][body]`，
块体：`([keyLen u16][key][valLen u32][value])*`。

清单在文件头且自描述全部块的边界与 CRC，故：截断点落在清单区则清单不完整；落在块头
16 字节内则块头不完整；落在块体则块体不完整；字节被改写则 CRC 不匹配。续传时从清单
读出全部块的计划，顺序扫描文件中完整且 CRC 正确的块，截断到第一个不完整块处，从该
块号继续追加。每块自描述（带块号），乱序或缺失在校验时按期望块号与实际块号指名报错。

## 5. 资源约束

- 保留值个数 <= 导出期间被改写的键数（见第 1 节）；
- 每个键恰好 `Get` 一次（`snapshot` 计数器断言）；
- 导出驻留内存不超过当前一块的缓冲（写满即落盘），清单只存每块的定长元数据。
