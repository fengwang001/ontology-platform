# DESIGN

统一格式 diff/patch 设计推导。包依赖单向：`lines ← edit ← hunk ← udiff ← patch`。

## 1. 零计数 hunk 头的行号（语义 4）

设 `pos` 为改动在文件中的 0 基插入点（= 其前未受影响行数）。统一规则：
`count ≥ 1` 时 `start = pos+1`（首个被覆盖行的 1 基行号）；`count = 0` 时 `start = pos`。
样例：`x → y x` 是在旧第 1 行前插入，pos=0 → `-0,0`；`a b c → a b X c` 是在旧第
2 行后插入，pos=2 → `-2,0`；`a → 空` 中新侧 count=0、pos=0 → `+0,0`。
为什么数字是「前一行的行号」而非「将要插入的位置」：count=0 时没有「首个被覆盖行」，
若写 pos+1（1 基）会暗示覆盖了下一行；GNU 改写插入点之前最后一行的行号，文件首插入
时为 0。Apply 解释：count=0 → 插入点 = start（在该行之后，0 基下标 start）；
count≥1 → 首个覆盖行 0 基下标 = start−1。

| b | 头部写法 | Apply 的插入/覆盖点 |
| --- | --- | --- |
| b=0 | `-pos,0`（pos=前行行号） | 第 pos 行之后（0 基下标 pos） |
| b=1 | `-a`（省略 `,1`），a=pos+1 | 下标 a−1 起覆盖 1 行 |
| b≥2 | `-a,b`，a=pos+1 | 下标 a−1 起覆盖 b 行 |

## 2. hunk 合并阈值（语义 5）

阈值 = **2C**。每处改动向前后各要 C 行上下文；两改动间隔 g 行时，g ≤ 2C 则前一段
尾部上下文与后一段头部上下文相接或重叠，必须合成一个 hunk；g > 2C 则两段上下文之间
还剩 g−2C ≥ 1 行不属于任何 hunk，必须分开。C=3：g=6 → 单 hunk（6 行全作上下文），
g=7 → 两个 hunk。若取 C：g=4 时 GNU 仍给单 hunk（上下文重叠），取 C 会错误拆分；
若取 2C+1：g=7 时 GNU 给两个 hunk，取 2C+1 会错误合并，且无法决定中间行归属。

## 3. 并列最短脚本的选择（语义 3）

Myers 在 `V[k-1] ≥ V[k+1]` 时取右移（删除），否则取下移（插入），即并列时**优先删除**。
效果：同一位置既删又插时，hunk 内 `-` 行排在 `+` 行之前。例：`a b → b a` 得到
`-a`、` b`、`+a`，与 GNU diff 一致；优先插入会得到相反的 +/- 排列，同样最短但渲染不同。
该规则固定，故同一输入重复 100 次补丁文本逐字节相同。

## 4. 偏移后的后续 hunk（语义 7 后续）

要平移，平移量为累计 `k + Δ`。前一个 hunk 记录位置为 base、实际在 `pos = base + k`
处匹配，且净增 `Δ = 新行数 − 旧行数`：偏移 k 通常源于目标文件在该处之前已有增删
（例如前面多了 3 行，后续所有行整体后移 3），故对后续 hunk 同样成立；长度差 Δ 则是
本次拼接后其后行的坐标变化。后续 hunk 查找中心 = 记录位置 + 累计(k+Δ)。例：hunk1
记录第 10 行、以 +3 应用且多插 2 行，hunk2 记录第 20 行则从第 25 行开始在 ±F 内查找。
匹配仍是逐行精确校验，平移只改变查找中心，不会造成错配；不平移则在重复行多的文件里
可能选错候选或误判超出范围，与 GNU patch 的 offset 累积行为不一致。
