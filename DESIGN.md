# LZ77 自定流式格式设计

## 记录布局（tag 为前缀 varint）

| 记录 | tag | 后续字段 | 语义 |
|---|---|---|---|
| 流头 | 1 | version=1, windowCap | 必须为首记录 |
| 字面量 | 2 | length, bytes | 连续 length 个原文字节 |
| 回指 | 3 | distance, length | 从已输出历史 distance 处复制 length 字节 |
| 刷新 | 4 | 无 | 表示此前输入完整可还原 |
| 流尾 | 5 | total, checksum | total=原文总长，checksum=IEEE CRC32 |

整数均为无符号 LEB128 varint：每字节低 7 位负载，最高位为续位；超过 10 字节或第 10 字节使 64 位溢出即报错。

## 推导一：压缩结果与 Write 切法无关

压缩器维护“窗口 + 待决缓存”。只在缓存至少含 `3` 字节时，在**下一起始位置**发起一次贪心最长匹配；找到则把匹配前的所有字节作为字面量记录，再整体跳过该匹配；找不到则只确定 1 个字面量字节再前进。关键点：一次 `Write` 绝不直接产生记录，只追加到待决缓存；记录仅由统一的排空循环产生。因此同一字节在成为“下一起始位置”时，无论它来自哪次 Write，其右侧可见字节集合都等于“截至当前总写入”的同一前缀，选择确定相同。

`Flush` 必须强制结束当前未决区间：把剩余缓存全部按字面量输出并写刷新标记。因为调用方承诺此刻不会立刻提供下一字节，等待会违反“此刻即可完整还原”；而强制结束只影响刷新点，不改变窗口，之后匹配仍可跨过刷新边界回指旧数据。连续两次 Flush 之间无输入时没有新记录可产出，故第二枚刷新标记必须省略（dirty 标志控制）。

## 推导二：重叠回指

距离 d、长度 L、d<L 时，源区间与目标区间重叠。块复制读到的可能是尚未复制的旧数据；例如 d=1，结果应不断复制同一个字节。正解是按输出序号 k 读 `out[n-d+(k mod d)]`，等价于逐字节 `out = append(out, out[len(out)-d])`。实现采用窗口内的环形逐字节 append；长度在写入前先与输出上限比较，不预分配 L 字节，因此 d=1 的最坏情形每次仅常数工作，且天然支持解压炸弹提前拒绝。

## 推导三：两类“距离过大”

“distance > 已输出字节数”表示流引用了尚不存在的输出，是语义截断/损坏；“distance > windowCap”表示即便将来输出更多也永不可达，违反头声明的窗口容量。压缩器只向窗口插入已处理字节，回指候选全部来自当前窗口，距离必 ≤ windowCap；故正常流不可能产生后者，但解压器必须独立检查，防止恶意流。

并行块中，下一块以上一块末尾 windowCap 字节为预置字典。该字典属于该块逻辑流的历史：解压器在连续流里此前已实际输出这些字节，所以“已输出字节数”是全局累计值；块边界不重置窗口、不产生特殊记录。

## 推导四：并行确定性

块 k 的匹配器状态只读取两块原文：`data[k*bs:(k+1)*bs]` 与上一块末尾 ≤windowCap 字节；不读取其他块的输出、哈希或完成顺序。压缩为纯函数，且按固定块索引收集结果，故 workers=1/2/4/8 仅改变调度，字节结果完全一致。块间不插刷新标记：流头写一次，各块只贡献普通记录，最后写唯一流尾；线性拼接本身就是合法流。
