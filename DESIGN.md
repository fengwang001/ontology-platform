# 批量导入幂等与断点续传器 — 设计推导

目标：把大批次记录导入目标存储；崩溃后可续传；同批次重复导入不产生重复数据。
仅标准库；进度与数据写本地临时目录；写入按业务键幂等。

## 1. 进度粒度的推导

- 每条写入都按业务键幂等：`Write(k, v)` 在 `k` 已存在且值相同的时候是
  无副作用的成功（返回 existed=true），值不同才报唯一键冲突。
- 推论一：重放任意已写过的记录是安全的，因此进度**不需要**精确到最后一条
  已写记录，只需记录「保证已全部落库的前缀区间」。崩溃点之后、最后一条进度
  记录之前的记录，重放一遍即可收敛。
- 推论二：进度可以粗粒度。每写满 N 条刷一次进度文件（一次小文件写），
  崩溃时最多回退 N 条重放，重写数 ≤ N，与已写总量无关。若每写一条刷一次，
  每条记录都伴随一次 fsync 级开销，吞吐塌方；若写入不幂等，则崩溃点后的
  部分状态无法安全重放，才被迫逐条刷进度。本题幂等前提使粗粒度成立。
- 粒度选择：N 是可调参数（demo/测试用 4096 或 10）。进度文件只保存
  区间列表，已写区间在顺序写下自动合并为单区间，文件大小 O(1)。

## 2. 进度文件格式与可恢复前缀

文本行格式，便于逐字节截断分类：

```
HDR <batchID> <total> <committed 0|1>\n
IVL <start> <end>\n        # 半开区间，可多条
CRC <crc32(此前全部字节)>\n
```

加载时按「最大可恢复前缀」解释，截断点分三类可判定错误：

- 头部不完整：首行被截断或字段不全 → 进度为空（等价从头开始）。
- 区间记录不完整：某条 IVL 行被截断 → 丢弃该行及其后内容，保留之前区间。
- CRC 不匹配/缺失：前缀恰好完整或 CRC 行被截 → 头部与区间仍可用，
  仅校验失败，按已解析区间恢复（理由见第 4 节：最终以存储为准）。

进度文件不存在视为从头开始，不是错误。

## 3. 重复导入的幂等判定

- Commit 阶段把 `committed=1` 写入进度文件并释放占用标记。
- 同一批次再次 Import：Prepare 读到 committed=1，直接返回
  `(alreadyDone=true, err=nil)`，不执行任何写入，存储逐字节不变。
- 判定「已完成」只认进度文件中的 committed 标志，与存储内容无关；
  判定「已完成多少」只认存储实际内容（见下）。

## 4. 为什么以存储为准、不以进度为准

进度文件是易损的：崩溃可能留下截断文件，外部干预可能让进度超前于存储
（声称写到 5 万而存储只有 3 万）。若续传盲信进度，缺口将永久丢失。
因此 Prepare 阶段把进度区间与存储逐一核对：沿区间扫描，遇到第一个
存储中不存在的键即截断区间，得到有效 frontier，从 frontier 续传。
进度只起「跳过已确认前缀」的加速作用，正确性完全由存储内容决定；
这也让第 2 节中 CRC 失配时仍采用已解析区间成为安全选择。

## 5. 部分批次（在途）语义

- 未 Commit 的批次：已写记录对 reconcile 可见，但计入「在途」，
  不计入「已提交」统计。
- reconcile 一遍线性扫描输出三段：已写入区间（manifest 下标）、
  缺口区间（精确到端点）、多余记录（存储有而清单无）。
- 复杂度：一趟扫清单（n 次 Has）+ 一趟扫存储（≤n+extra 次访问），
  总访问 ≤ 3n，无 O(n²)。

## 6. 并发与占用标记

- 每批次一个 `<id>.lock` 文件，内容为本批次占有的过期时间戳，
  `O_CREATE|O_EXCL` 创建保证互斥；第二个导入器立即得到 ErrBusy。
- 崩溃接管：时钟可注入；锁文件时间戳过期后允许删除旧锁并重试创建，
  实现安全接管。持有者仍活跃则拒绝。（生产实现应周期性续约，此处从简。）
- 不同批次锁文件不同，可并发；store 内部以互斥锁保护，`-race` 干净。

## 7. 边界语义

- 零记录批次：Write/Verify 空转，Commit 置 committed=1，重复导入返回已完成。
- 单记录批次：正常流程。业务键为空串合法。批次 ID 空串拒绝。
- 同批次内业务键重复：Validate 拒绝整批，错误携带重复键与两处下标。

## 8. 阶段编排

Prepare（校验清单→占锁→加载并核对进度）→ Write（跳过已确认区间，
逐条幂等写，每 N 条刷进度）→ Verify（线性确认全部键落库）→
Commit（写 committed=1，释放锁）。内存中同时驻留记录数为 1（逐条生成、
写后即弃），有峰值计数器可断言。
