# DESIGN

## 流格式记录布局

| 记录 | 首字节 | 后续字段（均为无符号 LEB128 varint，≤10 字节） |
|---|---|---|
| 流头 | `L Z 7 7` 魔数 | 版本(=1,varint) |
| 字面量 | tag=0 | 长度 n，后随 n 个原文字节 |
| 回指 | tag=1 | 距离 d（1..窗口容量），长度 l（≥3） |
| 刷新 | tag=2 | 无 |
| 流尾 | tag=3 | 原文总长，CRC-32(IEEE) 校验和 |

## 推导 1：压缩与 Write 切法无关

匹配器只能利用**已到达**的字节，而「最长匹配」只在看到其后第一个不匹配字节时才确定。
因此压缩器缓存一个未决区：位置 p 若找到匹配 [p,p+l)，只有当 p+l 严格落在
已缓冲区内（即知道匹配终止字节）才输出该回指；若匹配一直延伸到缓冲区末尾，
则挂起，等待下一次 Write。字面量立即落定，回指延迟落定。挂起位置之前的所有
位置都已落定，挂起位置之后尚未检查，故任何切法产出的 token 流相同。
Flush/Close 强制把匹配按当前长度落定——边界后再无字节，当前长度就是最终长度，
并写刷新标记。无新输入时直接返回，故连续 Flush 第二次零字节。

## 推导 2：重叠回指复制

d<l 时，目标区间与源区间重叠。一次性块复制（如 copy）要求源不被自身覆盖：
距离 1、长度 30 应复制同一字节 30 次，块复制会读到刚写入的混合内容而错。
语义是「逐字节向前复制」：out[t]=out[t-d]，t 递增。实现用单字节循环。
每字节最多循环输出上限次，解压本身就是 O(输出长度)，故该循环不增加渐近复杂度，
可接受；d>=l 时也走同一循环，代码路径统一。

## 推导 3：两类「距离过大」

「距离超过已输出字节数」：d>总已输出（含预置字典已落地部分），引用了尚不存在
的字节，是逻辑截断/伪造错误。「距离超过窗口容量」：d>配置容量，即便历史足够长
也超出解码器保留范围，是协议/参数错误。压缩器只从固定容量窗口取候选，故合法流
绝不可能产生后者；流被喂给更小窗口的解码器时才会触发。并行块预置字典是上一块
原文，压缩时它已进入窗口；解压时上一块输出也早已写入输出缓冲，故「已输出字节数」
语义不变，仍用累计输出计数，不需特殊标记。

## 推导 4：并行块的确定性

第 i 块压缩只读取两块数据：本块原文与上一块原文末尾（窗口容量，作预置字典）。
哈希链只由这两段原文决定，匹配结果与其他块、与 goroutine 完成顺序无关；
块大小与字典固定，故 workers=1/2/4/8 的逐块字节完全一致，按块序拼接即可。
每块在边界写一个刷新标记收尾：它只是语义同步点，解压器照常消费、不产生字节，
因此输出仍是单一合法流；流头写一次，流尾写一次（总长与 CRC 覆盖全文）。
