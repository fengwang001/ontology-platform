# DESIGN：自定 LZ77 流（五包：wire/window/match/enc/dec）

## 1. 流格式（全部无符号 LEB128 变长整数；记录按到达顺序排列）

| 记录 | 标签字节 | 后续字段 |
|---|---|---|
| 流头 | `0x01` | 魔数4B（`LZ77ONTO` 前4字节 `10 27 57 37`）、版本=`1`、窗口容量、链长上限 |
| 字面量段 | `0x02` | 长度 n、n 个原始字节 |
| 回指 | `0x03` | 距离(1..窗口容量)、长度(>=3) |
| 刷新 | `0x04` | 无（边界标记，解压忽略） |
| 流尾 | `0x05` | 原文总长、FNV-1a 64 位校验和 |

流尾之后出现任意字节即“多余字节”。长度为 0 的输入仍写头+尾，故流非空；长度 0 的流是截断。

## 2. Write 切法无关（语义3）与 Flush（语义4）

对每个锚点 p 只有在“该位置的判定被一个已知字节封顶”后才输出：
- 剩余 <3B：不能形成长度3的匹配，全部留待将来，暂不输出。
- 找到匹配 [p,p+l)：仅当缓冲区在匹配之后还有一个字节（封顶字节）时才确认该匹配最长；
  否则（匹配一直延伸到缓冲区末尾）保留不输出——下次 Write 可能延长它，也可能因新字节
  把它变成更长/同样长度。这样任何切法得到的判定只取决于累计原文，输出逐字节相同。
- 无匹配：该字节可立即作为字面量（将来不可能回溯改变它），但与后续字面量合并成段。
Flush 强制以已知数据结尾完成所有判定（匹配到达末尾也视为封顶并输出），然后写刷新标记。
因此 Flush 后所有输入可被还原；再次 Flush 前无新输入、无未决内容，产生 0 字节。
Flush 不清窗口，之后匹配仍可跨越 Flush 边界回指旧数据。

## 3. 重叠回指（语义2）

距离 d<长度 l 时，源区间与目标区间重叠：一次性 `copy(dst, window[d-len:])` 只给出来自旧
历史的一段，会重复错误模式。正确语义是“已复制的字节立刻成为后续源字节”。实现逐字节
`out=append(out, out[len(out)-d])`，环形窗口同步追加；成本 O(l)，与该段输出长度同阶，
不会产生额外复杂度，故接受逐字节循环。

## 4. 两类距离越界（语义7）与预置字典（语义9）

“距离>窗口容量”违反协议上限（窗口根本不保留那么久的字节），流头已声明容量，故是
格式错误，压缩器永不产生；“距离>已输出字节数”是流内容与进度矛盾（截断/伪造）。二者
分开报。并行块每块带预置字典：块0窗口为空；块 n（n>=1）用块 n-1 原文末尾 min(容量,
len) 字节预置。解码顺序与编码一致，这些预置字节在该块第一个记录时已存在于输出缓冲，
“已输出字节数”按解码进程自然包含它们，语义不变。块间只需普通记录衔接；不强制刷新
（刷新仅服务 Flush 的可还原点承诺，确定性拼接不需要它）。

## 5. 并行确定性（语义9）

每块压缩是纯函数 f(本块原文, 上一块原文, 固定容量, 固定链上限)：哈希链只由预置字典与
本块字节构造，不读其他块、不读共享可变状态、不依赖 goroutine 完成顺序。各 goroutine 用
各自独立的 window/matcher，按块序号收集 body 后顺序拼接：一个流头 + 全部块 body + 一个
流尾。故 workers=1/2/4/8 输出逐字节相同，且仍是可流式解开的单流。

## 6. 复杂度

哈希链：head[hash] 记最新位置，prev[pos mod 容量] 记同哈希前驱。每位置至多沿链走
链长上限步，候选考察总数 <= 链长上限×输入字节数，与窗口大小无关；全同字节每位置考察
数收敛到 min(链上限, 可达深度)，不随规模增长。计数器为非导出字段，测试经
`CandidatesInspected()` 读取。
