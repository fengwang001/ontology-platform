# 设计推导

规范化：`\r\n`、`\r`、`\n` 统一为 `\n`；删除每行末连续空格/制表符（含纯空白行，行保留）。

## 1. 末尾策略
- Keep：Close 前不增删。
- EnsureOne：输入非空且不以 `\n` 结尾则追加一个 `\n`（锚点在原文 len 处，纯插入，不对应原文字节）。
- TrimBlank：删尾部多余空行但保留一个换行，即“去掉末尾多余的 `\n`，最终若非空恰保留一个 `\n`”。
  与 EnsureOne 唯一差别：输入不以 `\n` 结尾时 TrimBlank 不追加。

样例表（策略 → 输出）：

| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

空输入讨论：EnsureOne 对空输入有 `"\n"` 与 `""` 两种选择。
选 `"\n"` 时，为让 `N("\n")=="\n"`，规则必须带条件（如“为空才加”），但插入的换行
无原文锚点，映射 `ToOrig(0)` 无来源，且 Keep 也无法保持互逆。
选 `""`：空集→空集，追加规则带“非空”条件，输出已满足自身不动，三策略都幂等，且每段输出都锚定原文。
故三策略对空输入统一输出 `""`。

## 2. 被删字节的 ToOut
映射用“锚点事件”（Delete：原文删 n、输出不动；Insert：输出插 n、原文不动）加各自身份区间。
查询：o/i 落在事件跨度内时取该事件后、最近的“非删除输出位置 / 非删除原文位置”。
- `\r\n` 的 `\r` 是 Delete(1)：`ToOut(i(CR)) = ToOut(i(LF))`，即输出 `\n` 的偏移。
  于是 `ToOrig(o(\n))=i(LF)` 有唯一来源，且 `ToOut(ToOrig(o))==o`。
- 末尾空白是 Delete(k)：空白中任意 i 映射到行尾换行的输出偏移（换行存在时）。
  光标放在被删空白里 → 落在换行符“之前的内容末端”，即换行起点（其前）；
  无换行（文件末）时落在输出终点。两侧映射均单调不减，互逆等式对所有输出位置成立。

## 3. 空白缓冲超限
候选 A 强制判定为“非行尾”并输出：该串本应被删，却进入输出，违反语义 2、5（再次规范化仍会删，
不幂等）且不可修复。候选 B 拒绝：返回带原文偏移的可判定错误，已输出前缀保留；
已提交的前缀本身是合法规范化结果，后续语义 4/6 不受影响，截断到拒绝点之前也自洽。
故选 B（拒绝）。默认上限 1<<20。

## 4. par 拼接
每段独立规范化，段间仅可能冲突于段尾 CR 与段尾待定空白：
- 段 k 末字节为 `\r`、段 k+1 首字节为 `\n`：段 k 多产生一个换行，回放时去掉该 Insert(1)，
  让 `\n` 作为 CRLF 的输出（删除段 k+1 的 CR Delete 不存在，因其首字节是 `\n` 身份拷贝）。
  具体：段 k 的尾部 CR 事件由“Insert 一个 \n”改为 Delete（并入下一段 LF 前），即跳过该输出字节，
  下一段首个 `\n` 身份输出成为唯一换行。
- 段 k 末尾空白被删、但段 k+1 首字节是正文/空白起行（不以行尾开头）：该空白应保留，
  回放时把该 Delete 还原为身份输出（含下一段开头被判为非行尾的空白）。
判定只需看相邻两段各至多 MaxPending 个尾部待定空白字节 + 1 个 CR，故修正范围有上界，
与 K、输入长度无关。映射表为段内事件列表，回放时坐标加段原始起点（原文）与累计输出长度（输出）
平移，重建成一张全局 Map。
