# DESIGN

## 1. 零计数 hunk 头的行号规则（语义第 4 条）

三个样例：`-0,0`（在文件最前面插入）、`-2,0`（在第 2 行后插入）、`+0,0`（新文件为空）。
若把数字解释成「将要插入的位置」，样例 1 应是 `-1,0` 而非 `-0,0`，矛盾。
若解释成「插入点之前最后一行的行号」：样例 1 插入点之前没有行 → 0；样例 2 插入点
之前是第 2 行 `b` → 2；样例 3 新文件没有任何行 → 0。三个样例全部吻合。
结论：**count==0 时数字 = 插入点之前最后一行的 1 基行号（文件开头为 0）；
count>=1 时数字 = 区间首行的 1 基行号**。`Apply` 对 `-a,0` 把插入点放在 0 基下标 a
处（即「第 a 行之后」），对 `-a,b`(b>=1) 从下标 a-1 起替换 b 行。

| b     | 头写法 `-a,b`        | Apply 对插入点的解释   |
|-------|----------------------|------------------------|
| b = 0 | `-a,0`，a=前一行行号 | 在 0 基下标 a 处插入   |
| b = 1 | `-a`（省略 `,1`）    | 从下标 a-1 起替换 1 行 |
| b ≥ 2 | `-a,b`               | 从下标 a-1 起替换 b 行 |

## 2. hunk 合并阈值 = 2C（语义第 5 条）

设两段改动之间夹着 g 行未改动行。前一段的 hunk 会向后带 C 行尾上下文，后一段会向前
带 C 行首上下文。g ≤ 2C 时两段上下文重叠（g<2C）或恰好相接（g=2C），中间没有任何
一行「不属于任何上下文」，GNU diff 此时输出一个合并 hunk；g = 2C+1 时中间有 1 行
两侧上下文都够不到，GNU diff 输出两个 hunk。故阈值 = 2C。
- 若阈值取 C：g=C+1（如 C=3、g=4）时两段上下文已重叠，GNU 合并，我们却拆开 → 不一致。
- 若阈值取 2C+1：g=2C+1 时 GNU 拆开（中间那 1 行无上下文可依），我们却合并 → 不一致。

## 3. 最短脚本的并列选择（语义第 3 条）

Myers 前向贪心在同一 D 上，当 `V[k-1] == V[k+1]` 时两条路同为最短。本实现规定：
**并列时优先删除**（`V[k-1] >= V[k+1]` 走右移=删除，否则走下移=插入），回溯沿用同一
规则，故结果确定。效果：同一段「替换」里 `-` 行排在 `+` 行之前。例：`a b` → `b a`
得到 `-a`、`␣b`、`+a`；若优先插入则会得到 `+b`、`␣a`、`-b`，二者同为最短但文本不同。

## 4. 偏移应用后的后续 hunk（语义第 7 条）

维护两个量：`delta` = 已应用 hunk 带来的净行数变化（插入-删除之和），`lastOff` =
上一个 hunk 实际落点相对「记录位置+delta」的偏移。第 i+1 个 hunk 的查找中心 =
`记录位置 + delta + lastOff`。理由：delta 是结构性必选项——前面 hunk 增删了行，
后续内容在新文本中的位置必然整体平移；lastOff 是启发项——目标文件若在头部多/少了
k 行，这一位移通常贯穿全文（GNU patch 同样用上一个 hunk 的偏移作下一个的首猜）。
在该中心 ±F 内找精确匹配，多个候选取距中心最近、同距取靠前。
