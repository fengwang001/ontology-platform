# 设计推导

## 1. 免重算的堆键

任务 i 的有效优先级 `p_i = base_i + (now - t_i)/K`（有理数精确值，K>0）。
比较两任务 i,j：`p_i > p_j`
⇔ `(base_i - base_j) + ((t_j - t_i)/K) > 0`（now 已消去）
⇔ `base_i*K - t_i > base_j*K - t_j`。
因此相对次序与 now 无关。定义整型键 `key = base*K - enqueueTick`：
key 大者有效优先级大，先出；key 相等则 t 小（入队更早）先出；再相等 id 升序。
时钟推进不改堆，杜绝「每刻全量重算/重建堆」。

不溢出范围：要求 `|base|*K + |t| <= math.MaxInt64`，由
`K <= 10^6`、`|base| <= 10^12`、`|t| <= 10^15` 保证（乘积 10^18、加 10^15 < 9.3e18）。
New 与 Push/Advance 越界返回 ErrRange。

float64 反例：K=1，a: base=2^53+1,t=0 → 浮点键 2^53+1；
b: base=2^53,t=0 → 键 2^53。2^53 以上相邻整数无法表示，
两键被舍入成同一个 float64，有效优先级相差恰好 1 却被判相等，
若再按 id 取舍，新任务可排到老任务前面。故禁用 float64。

## 2. 饥饿上界

低优先级 L：入队刻 0，base 0。高优先级 H：base=Δ（Δ>=1），每刻来一个，每刻 Pop 一次。
L 在刻 w 与到达刻 r 的 H 比较有效优先级：
L 胜出 ⇔ w/K > Δ+(w-r)/K ⇔ r > ΔK。
即到达刻 r=1..ΔK-1 的 H（key=ΔK-r>0）共 ΔK-1 个排在 L 之前；
r=ΔK 的 H 与 L 键相等（key=0），并列时 L（刻0）入队更早，故 L 胜、H 留队。
每刻 Pop 一个：刻 1..ΔK-1 依次弹出 r=1..ΔK-1 的 H（ΔK-1 次），
刻 ΔK：先推入 r=ΔK 的 H，随后 Pop 因并列规则返回 L。
L 从入队刻 0 到出队刻 ΔK，等待恰好 **ΔK** 刻，此即上界且可恰好达到。
若并列不看入队刻（如 id 序），该 H 可能压过 L，上界变为 ΔK+1。
恰好达到上界的构造：K=2，Δ=3，ΔK=6；刻0 Push L(base0)；
刻1..6 每刻 Advance(1)→Push 一个 H(base3)→Pop，前 5 次得 H，
刻 6 的 Pop 返回 L（与该刻 H 并列、入队更早），等待刻数 = 6-0 = 6 = ΔK。
