# DESIGN: 结构化日志脱敏与一致性采样

## 1. 包划分
- `record`：记录=级别+追踪ID+嵌套字段(map[string]any/slice/标量)；JSON 编解码、深度与环检测。
- `rule`：规则按字段路径匹配（脱敏方式 replace/hash/truncate），预编译成路径前缀树；记录规则匹配次数的非导出计数器。
- `mask`：对一份记录执行规则集，输出脱敏后的深拷贝；replace/hash/truncate 三语义。
- `sample`：按追踪ID做确定性采样，error 及以上强制保留。
- `sink`：文件=自描述头 + 每记录 4 字节大端长度前缀 + JSON 体 + CRC32(IEEE)。

## 2. 一致性采样推导
同一追踪链可能被切成多条记录、以任意顺序到达。若每条记录独立掷骰，
同链记录决策会不同，链断裂。要求“只依赖追踪ID、与记录内容/顺序/时间无关”，
唯一可行做法是对追踪ID做确定性哈希，取 `hash % denom < numer` 决定留弃。
因此：相同追踪ID ⇒ 永远相同决策；一次决策只做一次哈希，O(1)。
无追踪ID（空串）策略：视为“无链”，按整条记录自身内容做一次确定性哈希决策
（error 仍强制保留），不与任何真实链共享桶；文档固定该策略。
采样率 0：全部丢弃（error 除外）；采样率 1：全部保留。

## 3. 链不完整标记
error 及以上无条件保留。若其追踪链本应被采样丢弃，该 error 记录是链上唯一幸存者，
输出打 `incomplete_chain=true`。标记仅出现在“链决策=丢弃 且 级别强制保留”时；
链决策保留或非 error 均不打标。

## 4. 嵌套路径与点号消歧
路径是段序列而非字符串切分：编译时先按字面量在当前映射中查找整串键，
未命中再按点拆分逐级下钻。即**字面键优先于嵌套解释**：
字段名 `a.b` 与嵌套 `a`→`b` 同时存在时，规则 `a.b` 命中字面键。
路径不存在：跳过，不报错。中途某层不是 map：无法继续，跳过该规则。
数组：路径段为十进制数字时下钻 slice（越界跳过）。
最大深度 32：超过拒绝（ErrDepth），不深递归入栈。构造时自引用 map 检出
ErrCycle（访问栈集合判定）。

## 5. 脱敏语义与彻底性
- replace：固定串 `***`，不保留长度。
- hash：SHA-256 十六进制前 16 字符；同值同结果、不同值不同、不可逆且不等于原值。
- truncate：保留前 N 个 rune，加后缀 `…(+k)` 标注被截字符数；N 按 rune 计。
彻底性：规则编译时对每条路径规则登记其目标路径；执行期第一遍沿路径命中并记录
命中处的字面值，第二遍全树扫描，凡字符串等于任一已登记敏感值（其他路径、数组元素、
被引用值）一律同样处理。非目标字段：深拷贝后仅改目标，序列化逐字节比对保证其余不变。
同一路径冲突（既 replace 又 hash 等）编译期报 ErrConflict，指出冲突的两条规则索引。

## 6. 复杂度
路径树每字段一次查找，匹配计数上界 `记录数*(字段数+常数C)`，
C 为按值规则数（哈希表 O(1)/字段），避免 记录数*规则数*字段数。
采样决策哈希次数恒为 1（计数器断言）。

## 7. 落盘格式与损坏分类
文件 = 魔数 `ONTLOG1\n`(8B) + 保留头(8B) + 记录帧序列（固定头共 16B）；
帧 = uint32BE 体长度 + JSON 体 + uint32BE CRC32(体)。
截断分类：
- 头部不完整：文件短于 16B。
- 长度前缀不完整：剩余 <4 字节。
- 记录体不完整：剩余 < 4+len 或 < 4+len+4。
- CRC 不匹配：帧完整但校验不符。
恢复记录数=首个坏帧前的完整好帧数（最大可恢复前缀）。
四类错误 ErrCycle/ErrDepth/ErrConflict/ErrCorrupt 均为哨兵，errors.Is 可区分。

## 8. 并发
规则集/采样器构建后只读；mask 在记录深拷贝上工作；sink 互斥串行化写入。
并发结果按追踪ID+序号排序后与串行结果逐元素相同。
