# 严格模式 Base64：非规范尾部推导

## 分组与比特

标准 Base64 把每 3 字节（24 bit）切成 4 个 6-bit 值，用 RFC 4648 字母表编码。
输入长度不是 3 的倍数时，最后一组不足 3 字节：

- 1 字节 → 输出 2 个字符 + `==`：第 1 字符是该字节高 6 位，第 2 字符只用了低 2 位，
  即第 2 字符的低 4 位是无数据比特，必须为 0。
- 2 字节 → 输出 3 个字符 + `=`：第 3 字符只用了第 2 字节低 4 位里的高 2 位，
  即第 3 字符的低 2 位是无数据比特，必须为 0。

因此「非规范尾部」只看两个比特位置：末组第 2 字符 6-bit 值的低 4 位
（1 字节情形），或末组第 3 字符 6-bit 值的低 2 位（2 字节情形）。

## 为什么 `QR==` 必须拒绝

`Q=16`、`R=17`。两者高 6 位相同（=16），解出字节 `0x41='A'`；
`R` 的低 4 位是 `1`，而这 4 位是被填充比特，规范要求为 0。
宽松解码器丢弃这 4 个 bit，于是 `QQ==` 与 `QR==`（乃至 16 个不同的第 2 字符）
都解出同一字节串。严格模式拒绝它，恰好保证：

- **对任意字节串 x，Encode(x) 唯一**（无数据比特固定为 0，填充位置由长度唯一决定）；
- 编码是字节串到合法字符串子集的**双射**，故对任何被接受的 y，
  `Encode(Decode(y)) == y` 也成立。

统一规则：末组 pad 数必须是 0/1/2；有 pad 时被 pad 掉的无数据比特必须全为 0；
`=` 只能出现在整个流的最后一组。`QQ`/`QQ=`（长度不合法）、`QQ===`（组内 pad
位置错）、`QQ==QQ==`（填充后仍有数据）由长度规则与填充位置规则拒绝。

## 测试对照表（第 2 节 7 条语义）

（最后补）
