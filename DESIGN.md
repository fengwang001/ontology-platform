# 设计与推导

## 1. 零计数 hunk 头的行号规则

统一格式里 `a` 是 1 基旧文件起始行，范围是 `[a, a+b-1]`；b=0 时区间为空，
空区间表示「插在第 a 行之后」。要让该记号对任意插入点唯一，GNU 规定
**a 取插入点前一行的行号（0 表示在文件头）**，而不是插入位置本身。

- 在文件头插入：前面没有行 → a=0，即 `-0,0 +1`。
- 在第 k 行之后插入：a=k，如旧文件 `a\nb\nc\n`、在 `b` 后插 `X` → `-2,0 +3`。
- 旧文件整体删除：新侧范围为空，c 取删除点前一行号；删唯一行 → `@@ -1 +0,0 @@`。

Apply 解析 b=0 时把插入点放在旧文件第 a 行之后（a=0 即开头）。

| 旧侧 | 书写 | a 的含义 | Apply 插入/删除点 |
|---|---|---|---|
| b=0 | `-a,0`（必写 ,0） | 前一行行号，0=文件头 | 第 a 行之后 |
| b=1 | `-a`（省略 ,1） | 该行行号 | 第 a 行 |
| b≥2 | `-a,b` | 首行行号 | 第 a 行起 b 行 |

## 2. hunk 合并阈值

相邻两改动间未改动行数为 g，每个 hunk 上下各取 C 行上下文。合并后中间上下文为
g 行；分开则前 hunk 的后 C 行与后 hunk 的前 C 行会重叠或相接。当且仅当
**g ≤ 2C** 时两段上下文重叠（或在 g=2C 时恰好相接），GNU 将其合并为一个 hunk；
g = 2C+1 时中间至少有 1 行不属于任何 hunk，必须分开。阈值即 **2C**。

反例（C=3）：若阈值取 C=3，则 g=4（≤2C）本应合并却被分开，与 `diff -U3`
不符（4 行上下文可被两侧各 3 行完整覆盖）；若取 2C+1=7，则 g=7 时中间有
7-6=1 行空隙，GNU 输出两个 hunk，取 7 会错误合并。测试钉住 g=6 合并、g=7 分开。

## 3. Myers 并列最短路径的选择

Myers 每条对角线上保留到达 (x,y) 的最大 x。D 为奇数步、同一对角线可由
「先向下（删除）经 k-1」或「先向右（插入）经 k+1」到达时，本实现固定
**删除优先**（k 从 -D 递增，端点比较取下标较大的前驱：负 k 取插入前驱、
非负 k 取删除前驱）。因此 `a b → b a` 的脚本固定输出 `-a`、` b`、`+a`
（删除排在插入前），100 次生成逐字节一致。回放时同序产出 `-`/`+` 行。

## 4. 偏移后后续 hunk 的查找起点

hunk i 记录的旧起点为 s_i，实际在偏移 d_i 处应用。维护「游标」
cur_i = s_i + 此前所有 d_j（已应用 hunk 在当前文本中的位置），并以它为中心
±F 搜索。平移量正是此前所有偏移之和：每个 hunk 的成功应用都把它之后的所有
行整体平移 d_j（插入多于删除为正）。不叠加则后续 hunk 在整体移位的文档上
必然失配；叠加后等价于 GNU patch 的「已应用 hunk 推动后续行号」。

## 5. 其余约定

- 行：按 LF 切分，行尾（LF/CRLF/无）随行保留；解析补丁按 LF 切，正文去首标记
  后拼回，`\r` 视为内容，保证 CRLF 逐字节往返。
- 复杂度：Myers O(ND)，计数器只累加每条对角线端点延伸 snake 的逐行比较步数。
- 错误四类：`ErrMalformed`（格式）、`ErrContext`（上下文不匹配）、
  `ErrOutOfRange`（超出偏移）、`ErrTooDifferent`（差异过大），彼此可判定。
- 并发：Store 以单 mutex 串行化「校验+替换+版本+日志」，提交日志记录成功顺序。
