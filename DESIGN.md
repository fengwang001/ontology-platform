# 设计推导

## 1. 非法单元切分规则（字节级状态机，"最短错误恢复"）

首字节决定期望长度 n。第二字节不在其合法区间时，首字节自成一个非法单元（吞 1），
随后每个续字节 80..BF 各成一个非法单元（吞 1）；期望未满即 EOF 时，已缓冲字节合为一个单元。
故 `F0 90 80 41`：F0 第二字节区间 90..BF，90 非法 → F0 吞 1，80 吞 1，41 合法；
`E0 80 80`：E0 区间 A0..BF，80 非法 → E0/80/80 各吞 1。规则统一为：

- 首字节非法（80..BF、C0、C1、F5..FF）：吞 1。
- 首字节合法但第二字节非法：首字节吞 1；其后每个 80..BF 各吞 1；遇非续字节则它作为
  新序列起点重新解析（不被吞）。
- 已读到合法第二字节后，第 3/4 字节非续字节：已缓冲的 2 或 3 字节合成 1 个单元（吞 2/3），
  当前非续字节重新解析。越界判定（代理区/超 U+10FFFF）前移到第二字节区间实现。
- EOF 残留：任意未完成前缀合成 1 个非法单元。

`ED A0 80`：ED 区间 80..9F，A0 非法 → ED/A0/80 各吞 1。`C0 AF`：C0 首字节非法吞 1，
AF 非续（作为新起点）也非法吞 1。`F4 90 80 80`：F4 区间 80..8F，90 非法 → 四字节各吞 1。
`E2 82`+EOF：第二字节合法，EOF 残留 2 字节 → 1 单元。`80 80`：各吞 1。

| 首字节 | 第二字节合法区间 | 非法时吞 |
|---|---|---|
| C2..DF | 80..BF | 1 |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | 非法首字节 | 1 |
| C0 C1 F5..FF | 非法首字节 | 1 |

## 2. 缓存上限与立即判定

最长合法序列 4 字节，缓存 ≤3 字节。看到首字节即知期望长度与第二字节区间：第二字节一到
即可判定，不必等 EOF；其后最多再等 2 个续字节，第 3/4 字节非续时立即把缓冲合成单元并让
当前字节重新解析。UTF-16 缓存 ≤3：1 字节奇偶对齐 + 1 个高代理代码单元。

## 3. par 切点对齐

段 k 逻辑边界为 cut[k]。UTF-8 起始对齐：从 cut 向左跳过最多 3 个连续续字节（80..BF），
若再前一字节是 C2..F4 引导字节则再退 1（共 ≤4）。起点必为某单元首字节，切点所属单元
整体落在一段内，不被重复解析。UTF-16：先退到偶偏移（≤1），若前一代码单元为高代理再退 2（≤3）。
每段只保留单元首偏移 ∈ [cut[k-1], cut[k]) 的单元：对 lo 用前缀抑制（首偏移 < lo 的丢弃），
对 hi，对齐已让切点前整单元归本段（首偏移 < hi 保留），下段抑制区不会再取，故单元数恒等。
例 `C3 | 80 80`：段 0 取 C3 80；段 1 从 C3 起解析但抑制 C3 单元，首个 80 成独立单元，总数不变。

## 4. 上限断点

每个完整标量输出前检查容量；放不下则该标量（含整个代理对/多字节）完全不输出，Write 返回
的 n 只到上一已输出标量的物理末尾（非法字节随其 FFFD 同进同退）。解码缓存中的字节未离开
解码器，不算已消费；调用方把 data[n:] 喂给新实例即可，拼接与无限流逐字节相同。
