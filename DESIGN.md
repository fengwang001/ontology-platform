# 设计推导

## 末尾换行策略与空输入
规范化的输出字母表只有「内容行以 \n 结尾」。三种策略作用于 Close 时的尾部：
- Keep：保留原文尾部结构（不增不减 \n）。
- EnsureOne：末尾恰好一个 \n；但**空输入输出 ""**——把「无行」文档强行造出行既违反直觉，且 N("")="\n" 后需 N("\n")="\n" 虽幂等却会让空文档产生字节；选 N("")="" 时对任何非空 x 末尾规整为一个 \n，N 作用于已规范结果不变，仍幂等。
- TrimEmpty：删除全部尾部空行；若删完为空则输出 ""，否则保留最后一个非空行的一个 \n。

| 输入 \ 策略 | Keep | EnsureOne | TrimEmpty |
|---|---|---|---|
| "" | "" | "" | "" |
| "\n" | "\n" | "\n" | "" |
| "\n\n" | "\n\n" | "\n" | "" |
| "  \r\n" | "\n" | "\n" | "\n" |

幂等论证：规范输出只含「非 CR、无行尾空白、以 \n 结束」的行，再规范化不删任何字节；三策略各自幂等——Keep 不动尾部、EnsureOne 的输出尾部为 0 或 1 个 \n、TrimEmpty 不动非空行尾的单个 \n。

## 被删字节的 ToOut
约束：ToOrig 单调不减；对所有输出偏移 o 有 ToOut(ToOrig(o))==o。删去区间 [a,b) 是一次坍缩：其后输出位置整体前移，区间内无输出点。设删区间后第一个存活字节的输出位置为 p（行尾 \n；若删区间在文件末尾则 p=len(out)），则 i∈[a,b) 时 ToOut(i)=p。被删行尾空白与 \r\n 中的 \r 都映射到该行 \n；文件末尾被删字节映射到输出终点。区间左闭右开，o=p 指向 \n，ToOut 回到 p，互逆成立；两函数均阶梯式单调不减。光标点在被删空白中时落在换行符前的边界，不会跳到下一行。

## 空白缓冲超限（第 8 条）
选择**拒绝**：待定空白串长度达到上限+1 时返回携带原文偏移的 ErrWSBuffer。强制输出会把「后来确实在行尾」的空白永久写入，违反第 2、5 条；拒绝在任意切分方式下都在同一逻辑空白串的同一字节触发，不违反 4/5/6。拒绝后进入终态，已输出内容与映射保留，再写返回 ErrClosed。输出超限时同理立即返回 ErrOutputLimit（带原文偏移）并保留已输出内容。

## par 的拼接（第 9 条）
按字节切点切 K 段，每段独立跑同一状态机（不含末尾策略），得到「已决输出+残态」。残态只有三种且有界：待定 CR（1 字节）、待定行尾空白（≤ 上限+1）、无。顺序拼接时把前段残态与后段首字节重新判定：\r|\n 合为一个行尾（删 \r 出 \n）；\r 后非 \n 则独立成行；残空白遇行尾整体删除，否则原样吐出。映射平移：段内输出坐标加「前序已决输出总长」，原文坐标加「段起点偏移」。接缝修正只触及「前段残态+后段首字节」，空白残态被同一上限截断，故修正范围有上界。末尾策略只在拼接完成后施加一次。worker 间无共享，-race 安全。

## 复杂度（span）
映射仅在坍缩点分段，纯保留合并为一段，10MB 无删除文本区间数恒为 1。ToOrig/ToOut 用 sort.Search 二分；非导出计数器 probe 记录单次查询检查的区间数，断言 ≤ 2·log2(n)+4。
