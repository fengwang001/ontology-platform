# DESIGN — 自定格式 LZ77 流式压缩

## 流格式（wire 包，整数一律 Uvarint 变长编码，≤10 字节）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `0x4C 0x5A 0x01`（魔数 "LZ" + 版本 1） |
| 字面量段 | `0x01` + uvarint(长度 n) + n 字节原文 |
| 回指 | `0x02` + uvarint(距离 d≥1) + uvarint(长度 l≥4) |
| 刷新标记 | `0x03`（无负载，仅对齐 Flush 语义） |
| 流尾 | `0x04` + uvarint(原文总长) + uvarint(FNV-1a64 校验和) |

## 推导一：压缩结果与 Write 切法无关（第 3 条）

- 问题：在 Write 边界上，位置 q 的最长匹配可能延伸到尚未到达的字节；提前发出会被后续数据「打脸」。
- 规则：q 处匹配长度为 L 时，只有当 `q+L < 已到达字节数`（匹配后还有字节，说明匹配确实断在失配处）或 `L == MaxMatchLen`（达到硬上限，不可能更长）时才发出该回指；否则把整个未决尾部缓存起来等更多数据。字面量在发现位置 q 无匹配时即确定（匹配只依赖 q 之前的历史与 q 处字节，与未来无关），但字面量段只在遇到回指或 Flush/Close 时才落盘，因此段边界也只取决于字节流。
- 缓存上界：未决匹配满足 `到达数-frontier < MaxMatchLen`，否则它必然触上限被发出，故缓存 < MaxMatchLen（1MiB），有界。
- 哈希链插入也用确定性前沿：位置 p 只有在分词前沿越过 `p+4` 时才入链（哈希需要 4 字节），即 `ins+4 <= q` 时插入 ins。该规则只依赖分词前沿 q，不依赖到达时刻，因此候选集合与 Write 切法无关。窗口环形容量取 `Window+MaxMatchLen`，使「距离 ≤ Window」成为唯一淘汰条件，淘汰边界不随到达量移动。
- 对 Flush 的含义：Flush 时不能再等「后面的字节」，必须强制结束当前匹配（按已到达字节截断发出）并冲刷字面量段，再写刷新标记。因此 Flush 偏移是输出的一部分：同一输入、同一组 Flush 偏移 ⇒ 输出逐字节相同。

## 推导二：距离 < 长度的重叠复制（第 2 条）

- 一次性块复制（`copy(dst, dst[len-d:])`）假设源区域已冻结；d < l 时源与目标重叠，块复制只会重复拷贝旧内容，得不到 `abc→abcabc…` 的语义。
- LZ77 的定义是逐字节向前复制：每写一个字节，它立刻成为后续回指的源。本实现解压时按 `b := win.Last(d); append(b)` 循环。
- 正确性论证：第 i 次迭代读的是「当前输出末尾倒数第 d 个字节」，归纳可得输出恰为周期 d 的延拓。
- 性能论证：解压输出本身就是 Ω(l) 字节，逐字节循环是 O(l)，渐近上不退化；无需为省常数引入分块特判，故接受逐字节循环。

## 推导三：「距离超过已输出」与「距离超过窗口」（第 7 条）

- 「超过窗口容量」是格式/资源限制：解压器用固定容量环形窗口，距离 > 容量的字节已被淘汰，物理上取不回。
- 「超过已输出」是流合法性限制：回指只能指向已还原的历史，指向未来即流损坏。二者来源不同、修复方式不同（调大窗口 vs 流坏了），必须可区分。
- 压缩器为何绝不产生「超过窗口」：匹配器链遍历时 `q-cand > maxDist` 即断链，maxDist=窗口容量，故发出的距离 ≤ 容量。
- 并行块压缩中，预置字典是上一块原文尾部，这些字节在全局输出中恰好紧邻本块之前，「已输出字节数」的含义不变；距离相对全局输出依然 ≤ 已输出、≤ 容量，解压器无需感知块边界。

## 推导四：并行块压缩的确定性（第 9 条）

- 块 i 的压缩 = f(字典_i, 块_i 原文)。字典_i 取自「上一块原文末尾 ≤ 窗口容量字节」，即原始输入的切片，不依赖任何块的压缩结果、不依赖调度顺序；因此每个块的记录序列是纯函数，workers=1/2/4/8 结果逐字节相同。
- 衔接：各块记录直接按块序拼接进同一条流，块间不需要刷新标记（刷新标记只服务于流式 Flush 承诺）；回指天然跨越块边界指向字典。流头写一次，流尾的总长与校验和覆盖全部输入。
- 哈希链与窗口在每个块内独立构建（以字典预置），块间无共享可变状态，故 `-race` 干净。
