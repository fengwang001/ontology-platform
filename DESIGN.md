# 设计：可撤销批量重命名与冲突解析

## 1. 模型与名字合法性
- 命名空间 `Namespace` 是内存中字符串名字的集合（`map[string]struct{}` + `sync.RWMutex`）。
- **任何字符串均合法**：空串 `""` 是合法名字；`/`、`\` 等路径分隔符按普通字符处理，不做路径语义解释。

## 2. 四类冲突（检测先于任何修改，全部用 map 查找完成）
- `ErrTargetExists`：新名当前存在，且它不是本批某个请求的旧名（不会被搬走）。
- `ErrDuplicateTarget`：两个不同请求指向同一新名。
- `ErrDuplicateSource`：同一旧名出现两次（报出两个新名）。
- `ErrSourceMissing`：旧名当前在命名空间中不存在。
- 检测通过后才允许执行；任一冲突出现时集合保持原样。

## 3. 自环语义
`a→a` 定义为**无操作**：不冲突、不产生步骤。重复出现两次 `a→a` 属于“同一旧名出现两次”，按
`ErrDuplicateSource` 处理。

## 4. 执行顺序的推导（拓扑序）
把每个非自环请求视作节点，节点 r 的旧名 `old(r)`、新名 `new(r)`。
若 `new(r1) == old(r2)`，则 r1 要写入 r2 当前占据的位置，必须等 r2 先搬走，于是得到约束
**r2 → r1（r2 先执行）**。
- `{a→b, b→c}`：约束 `b→c` 先于 `a→b`，故顺序为先 `b→c` 后 `a→b`；每步目标位均为空，无覆盖。
- 这正是依赖图的拓扑序：入度为 0 的节点（其目标名不是任何待执行节点的旧名）先执行，
  同层按名字字典序（以 `old|new` 为键）保证确定性，用 Kahn 算法 + 小顶堆实现。
- 所有 map 查找均为均摊 O(1)，检测与排序总代价 O(请求数 + 涉及名字数)。

## 5. 破环的通用做法
拓扑排序耗尽但仍有节点，则剩下的每个弱连通分量都是一个有向环（每个旧名/新名在环内
恰好出现一次）。对每个环 `x1→x2, x2→x3, …, xn→x1` 借**一个**临时名 `t`，展开为：
`x1→t, xn→x(n-1 的源)…`，具体为 `x1→t, xn→x(n-1 名字位), …, x2→x1, t→x2`，共 n+1 步：
1. `x1 → t`（腾空 x1）
2. `xn → x(n-1)`，沿环逆序逐个前移（二元环时即 `x2 → x1`）
3. `t → xn`（临时名落位）

每一步执行前其目标名都不存在：t 初始不存在；环内名字按逆序腾空后才写入。

**临时名数量 = 环数**：每个环恰好需要一个临时名，环之间互不相交，无需共享。

**临时名生成**：以固定前缀 `".rename-tmp-"` 递增序号生成；每个候选必须既不在现有名字集合中，
也不是本批任何目标名；冲突则序号自增重试。若连续大量候选（上限 10000）全部被占用，
返回可判定错误 `ErrTempNameExhausted`，绝不覆盖数据。

## 6. 执行、撤销与日志
- 执行持命名空间写锁，串行应用计划步骤；另设执行器互斥锁禁止两批并发执行。
- 并发的外部修改在执行期间阻塞在同一把 `RWMutex` 上（语义：阻塞，不允许并发改集合）。
- 每步成功后向日志追加一条记录：`旧名 \n 新名 \n` 的长度前缀帧（binary 大端）+ CRC32(IEEE)。
  文件头为魔数+版本，帧格式：`[4:oldLen][old][4:newLen][new][4:crc32]`，CRC 覆盖 old/new 字节。
- 某步失败（故障注入）：已执行步骤按日志**逆序撤销**，集合恢复如初，返回带注入点的错误。

## 7. 撤销与截断分类（三类错误可 errors.Is）
- `ErrHeaderIncomplete`：文件短于固定文件头，头部不完整。
- `ErrRecordIncomplete`：某帧长度前缀声明的字节（含 CRC）读不全。
- `ErrCRC`：帧完整但 CRC32 不匹配。
- 撤销按“最大可恢复前缀”进行：完整且 CRC 正确的帧逆序撤销；一旦帧不完整/CRC 错，
  其后所有帧均不可撤销，返回 `ErrCorruptLog`（包裹具体分类），并报告无法撤销的步数。
- 幂等：日志末尾完整撤销后写入“已撤销”结束标记；再次撤销直接无操作（`ErrAlreadyUndone`）。

## 8. 包结构（.go 文件上限 9 个：6 实现 + 3 测试）
- `name`：命名空间、锁、合法性与快照；- `plan`：四类冲突、计数器、Kahn 拓扑序、环提取；
- `cycle`：环展开、临时名生成与重试；- `apply`：加锁执行、日志、失败注入与自动回滚；
- `undo`：日志解析、三类截断分类、最大可恢复前缀逆序撤销、幂等；- `cmd/demo`：逐条判定演示。
  测试表驱动并按包合并：`plan_test.go`（name+plan+cycle）、`apply_test.go`、`undo_test.go`。
