# 设计推导

## 代理对错误偏移（语义 2）

把字面量看成一个「码点单元」序列，每个未转义字节组或每个 `\uXXXX` 是一个单元。
高代理（D800–DBFF）不单独构成码点，它声明「下一个单元必须是低代理」。

规则：**错误偏移指向第一个不能被接受的单元的起点**；高代理本身不是错误，只是一条待满足的
约束；约束在它后面那个单元被看到（或字面量结束）时才裁决。

- `"😀"`：D83D+DE00 成对，起点是高代理的 `\`（该单元起点），组合成 U+1F600。
- `"\uD83D"`：末尾只有高代理；缺低代理，偏移 = 高代理单元起点（失败的组合起点）。
- `"\uDE00"`：低代理没有前置高代理；偏移 = 该低代理单元起点。
- `"\uD83Dx"`：`x` 不是低代理单元，偏移 = `x` 的位置；`"\uD83DA"` 同理，偏移 = `A`。
  这也解释了它为何不能是「替换字符 + A」：严格模式下遇到非法序列必须整体拒绝（RFC 8259
  禁止未配对代理），不存在「替换后继续」的分支；一旦容忍，错误位置与恢复策略都不再唯一。
- `"\uDE00\uD83D"`：第一个低代理即非法，偏移 = 第一个 `\uDE00` 起点，失败立刻裁决，
  不会延后到第二个单元。

## 最小转义、单射与唯一性（语义 4）

RFC 8259 对字符串内容的强制要求只有：引号 `"` 与反斜杠 `\` 必须转义，未转义的
U+0000–U+001F 禁止出现。因此 Encode 只转义这三类：

- `"`→`\"`，`\`→`\\`；
- `\b \f \n \r \t` 用规定短形式；
- 其余控制字符用 `\u00XX`（固定 4 位小写十六进制，无歧义）。

U+001F 必须转义因为它落在「未转义控制字符被禁止」区间；U+007F 不是控制字符（DEL，
在 C0 区之外），JSON 允许原样出现，故不转义。`/`、非 ASCII（含 U+2028/U+2029）同理原样。

输出唯一：每个码点的输出形式由上述规则完全确定，没有可选择的写法（`/` 不转义、非 ASCII
不用 `\u`、十六进制固定小写 4 位）。单射：输出是逐码点可逆的前缀无歧义编码——普通字节原样，
转义一律以 `\` 开头而原始 `\` 必为 `\\`，解码器可唯一还原，故不同输入必得到不同输出。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 五类错误可区分且带偏移 | `TestDecodeErrorKinds` |
| 2 代理对组合/拒绝/偏移 | `TestSurrogates` |
| 3 非法 UTF-8 双向拒绝 | `TestInvalidUTF8` |
| 4 最小转义编码 | `TestEncodeMinimal` |
| 5 往返（双向） | `TestRoundTrip` |
| 6 任意切分点一致 + 计数 ≤ 2n | `TestChunking` |
