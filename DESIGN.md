# 设计推导

## 非法单元

从首字节确定期望长度与合法范围；每次先判定第二字节：
- 首字节非法（80..BF、C0、C1、F5..FF）：该字节单独成非法单元。
- 第二字节缺失（EOF）：只把已收到的首字节（或合法前缀）记为一个非法单元；因此 E2 82+EOF 是一个。
- 第二字节存在但越界：只吞首字节，第二字节重新解析；故 F0 90 80 41 中 F0 成一个，90、80 各成一个，41 合法。
- 第二字节合法、后续字节缺失：已收前缀整体成一个非法单元。
- 第二字节合法、后续某延续字节非法或越界：从首字节到该非法字节前整体成一个，非法字节重新解析；所以 E0 80 80、ED A0 80 为三个，C0 AF 为两个，F4 90... 为四个。

| 首字节 | 第二字节区间 | 非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1；EOF 吞已收前缀 |
| E0 | A0..BF | 1；EOF 吞已收前缀 |
| E1..EC | 80..BF | 1；EOF 吞已收前缀 |
| ED | 80..9F | 1；EOF 吞已收前缀 |
| EE..EF | 80..BF | 1；EOF 吞已收前缀 |
| F0 | 90..BF | 1；EOF 吞已收前缀 |
| F1..F3 | 80..BF | 1；EOF 吞已收前缀 |
| F4 | 80..8F | 1；EOF 吞已收前缀 |
| 80..BF | 无 | 1 |
| C0 C1 F5..FF | 无 | 1 |

第三、四字节均须 80..BF；若第二字节已合法而后续失败，吞掉当前已收的 2 或 3 字节前缀。

## 切分缓存

最长 UTF-8 字符 4 字节，缓存最多 3 字节；UTF-16 最多缓存高代理加 1 个奇数字节，也为 3 字节。UTF-8 首字节期望长度非法立刻成单元；第二字节非法时立刻只拒绝首字节；只有“首字节+合法第二字节（+合法第三字节）”可等待下一字节或 EOF。因此任何字节最多作为输入检查一次，不按切分回扫。

## 并行切点

每段拥有 [start,end)，end 为全局切点。若 end-1 是 UTF-8 延续字节 80..BF，前一段回退 start，最多回退到连续延续前缀前的首字节（上限 3）；后一段从同一首字节开始，前一段额外读取该首字节作为只读哨兵以终止等待。切点后若不是合法延续，哨兵不会被吞，所有权不变。例：C3|80 80 回退到 C3，前段只拥有空范围并用 C3 终止；后段拥有 C3 80 80，得到 1 个合法字符和 1 个非法延续。非法单元不会在两段各计一次。

## 输出断点

Write 先保留无法判定的尾部到缓存；缓存字节尚未消费。每次只在一个完整标量编码成功后增加 consumed，consumed 始终是本次 p 中已形成完整输出的字节数。输出空间不足时不写半个标量，已输出内容不动，返回 ErrOutputLimit；调用方把 p[consumed:] 传给新实例，残留在旧缓存的字节仍包含在该切片中。
