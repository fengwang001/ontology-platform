# RLE 设计推导

不变量：严格解码器接受的每个 t 都满足 `Encode(Decode(t)) == t`。据此推导必须拒绝的输入。

## 拒绝清单（接受则构造出违反不变量的 t）

1. 显式次数 1，如 `1a`：解码得 `a`，再编码为 `a` ≠ `1a`（次数 1 必须省略）。
2. 次数 0，如 `0a`：解码得空串；空串编码为空 ≠ `0a`，且游程次数必须 ≥1。
3. 前导零，如 `01a`、`00a`：规范十进制无前导零，再编码为 `1a`（该形式本身也非法）≠ 原文。
4. 相邻同符号游程，如 `2a3a`：`Encode` 取最长游程，再编码为 `5a` ≠ `2a3a`。
5. 非法转义 `\x`：唯一允许的转义是数字与反斜杠；其他码点原样写出，再编码不产生该 `\`。
6. 末尾孤立 `\`：转义缺少被转义字符，不是任何游程的完整编码。
7. 末尾只有次数无符号，如 `12`：游程必须以符号结尾，不可解析为完整编码。
8. 非法 UTF-8：符号是码点；非法字节序列不可能由 `Encode`（输入恒为合法 UTF-8）产生。

各类返回彼此可判定的哨兵错误（`errors.Is`），错误类型携带字节偏移。

## 大次数取舍

支持但设输出上限：次数用 uint64 累加，`n*10+d` 溢出即报“次数溢出”，绝不静默回绕。
同时不按次数预分配内存，仅按实际产出的字节增长。`Decoder.MaxBytes`（默认 1<<30）
为解码输出总字节上限，游程 n 满足 n*UTF8Len(sym) 超过剩余额度即报“输出超限”，
且该检查用不溢出的乘法完成。超限不写入任何输出，错误可用 `errors.Is` 判定。
故大次数输入 O(输入长度) 判定，不会崩溃。

## 测试对照表

| 语义（第三节） | 钉住的测试函数 |
| --- | --- |
| 1 往返（含数字/反斜杠/多字节/长游程/随机） | TestRoundTrip / TestRandomRoundTrip |
| 2 规范形式与严格解码（8 类错误与偏移） | TestStrictRejects |
| 3 大次数（溢出、超限、不按次数分配） | TestLargeCount |
| 4 最长游程、组合字符不规范化 | TestLongestRuns / TestCombining |
| 5 跨切分点一致（逐字节、含 2a3a） | TestStreamingAllSplits |
| 复杂度：检查字节计数 == 输入字节数 | TestByteCounter |
