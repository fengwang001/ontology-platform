# 设计推导

## UTF-8 非法单元
规则采用字节级前缀机：首字节声明长度；每收到第二字节先用其首字节专属区间判定。第二字节非法时，只吞首字节+第二字节；后续连续字节非法时，各吞一个字节。合法长度未在流结束前满足时，只吞已经缓存的整个前缀。
因此 `F0 90 80 41` 在第二字节即判非法，吞 2 字节，`80 41` 继续解析；`E0 80 80` 第二字节非法吞 2，最后续字节再吞 1；`C0 AF` 同理；`E2 82`+EOF 是未完成合法前缀，整体只替换一次。

| 首字节 | 第二字节区间 | 非法时吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 2 |
| `E0` | `A0..BF` | 2 |
| `E1..EC` | `80..BF` | 2 |
| `ED` | `80..9F` | 2 |
| `EE..EF` | `80..BF` | 2 |
| `F0` | `90..BF` | 2 |
| `F1..F3` | `80..BF` | 2 |
| `F4` | `80..8F` | 2 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |
第三/四字节必须在 `80..BF`；此时非法只吞当前这一个字节，已确认前缀不回退。标量还必须在 `[0,0xD7FF]∪[0xE000,0x10FFFF]`，第二字节区间已排除非最短、代理区与越界。

## 切分缓存
UTF-8 最长 4 字节，缓存硬上限为 3：新首字节到来前，旧残缺前缀必须能在首字节或第二字节被判定；第三、四字节非法时只吞当前字节，不继续等待。看到非法首字节、非法第二字节、或合法前缀后遇到非续字节，可立刻放弃等待；只有“已通过检查但长度不足”的 1..3 字节可缓存。UTF-16 缓存硬上限为 3：一个待配对高代理加一个输入尾字节。

## par 切点对齐
每个单元归属于包含其首字节的全局区间。段起点先向前最多回看 3 字节：若找到会覆盖起点的 UTF-8 首字节，就从该首字节解析并跳过非本段单元；否则从起点解析。段尾再多读若干字节补完在本段开始的单元。若回看位置前 1 字节不是续字节，或前 3 个位置都没有首字节，可立即确定无跨段首字节，所以对齐是常数成本。
`C3|80 80` 中，下一段回看发现 `C3`，解析完整 `C3 80` 但因首字节属于上段而跳过，随后第二个 `80` 独立成非法单元；上段遇边界停止，不会提前替换。全局只有最后一段能看到 EOF，故截断也只判定一次。

## 超限断点
编码器在写入一个新事件前预留其最大输出空间；空间不足则不解析、不输出，返回输出超限和 `ResumeOffset`。因此返回的 `n` 只包含已形成事件的输入字节；切分缓存中的字节不算本次消费，保留到下次 `Write`。UTF-16 增补字符先拿到两个码元位置后才编码代理对，绝不留下半个代理对。
