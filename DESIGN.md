# 多租户配额公平队列调度器 — 设计推导

## 1. 为什么 vt 增量是 c/权重 而不是 c*权重

目标：长期运行下，租户 i 获得的执行代价速率正比于其权重 w_i，即对任意两个持续有积压的租户 i、j，有 S_i / S_j = w_i / w_j（S 为累计执行代价）。

定义租户的虚拟时间为「归一化服务量」：vt_i = S_i / w_i。每次为 i 执行一个代价 c 的任务，S_i 增加 c，故 vt_i += c / w_i。调度时选择 vt 最小者，等价于永远服务「归一化服务量最少」的租户，于是各积压租户的 vt 被拉平：vt_i ≈ vt_j ⟹ S_i / w_i ≈ S_j / w_j ⟹ S_i / S_j ≈ w_i / w_j。这正是目标份额。

反证 c*权重：若 vt_i += c * w_i，则权重越大的租户 vt 涨得越快、越不被选中，份额与权重成反比，恰好倒置。直觉：权重是「应得份额」，应放在分母把服务量折算成欠账；放在分子会把奖励变成惩罚。

## 2. 空闲租户重新加入时的 vt 抬升

维护系统虚拟时间 sysVT = 最近一次被选中租户在选中时刻的 vt。由于每次选最小 vt，sysVT 单调不减。

问题：租户 X 空闲期间 vt 停在旧值 v0，而其他租户把 sysVT 推进到 v0 + D。若 X 直接以 v0 重新入堆，它的 vt 是全场最小，会被连续选中，直到 vt 追上 sysVT：需要服务 D 的归一化量，即连续独占约 D * w_X / c 步。例：w 均为 1、c 为 1、Y 在 X 空闲期执行 1000 个任务，则 D = 1000，X 重新提交后将连续独占约 1000 步，Y 被饿死 1000 步。

正确做法：X 从空转为非空（或首次到达）时，令 vt_X = max(vt_X, sysVT)。含义：空闲期间 X 没有「被欠账」，它错过的服务不属于它；抬升到 sysVT 后 X 与其他租户站在同一起跑线，按 (vt, ID) 正常交替，X 的任务在 1~2 步内被执行，Y 不受挤压。抬升取 max 而非直接赋值，是为了不降低已高于 sysVT 的 vt（理论上不会发生，但保持幂等安全）。

## 3. 代价为 0 的任务

零代价任务合法，但若 vt 增量为 0/w = 0，该租户 vt 不推进，将永远是最小 vt，无限独占调度器。处理：设最小有效代价下界 MinCost = 1，计费时代价取 max(c, MinCost)。这样零代价任务仍推进 vt（按代价 1 计），份额行为退化为等代价情形，不会独占。负代价同样被钳到下界。

## 4. 同虚拟时间的并列打破

堆的比较键为 (vt, tenantID) 二元组：vt 相等时按租户 ID 字典序小者优先。比较键全序且确定性，给定相同任务流，调度序列逐元素可复现。

## 5. 数值表示与溢出

vt 用 float64。权重合法区间 (0, +inf)，构造时拒绝 w <= 0 与 NaN。float64 上限约 1.8e308，即使 w = 1e-9、c = 1e6、执行 1e9 步，vt 也仅约 1e24，远不溢出；53 位尾数在 1e5~1e9 步量级下份额误差远低于 5% 的验收阈值。故选 float64 而非有理数（有理数在标准库下需 big.Rat，每步堆比较代价高且无必要）。

## 6. 复杂度

选择用 container/heap 实现的二叉堆，元素为 (vt, ID)。单次选择 = 弹出堆顶 + （该租户仍有任务时）压回，比较次数上界约 2*ceil(log2(n)) + ceil(log2(n)) < 4*ceil(log2(n))，n 为堆中非空租户数。sched 内置非导出计数器统计单次 Next 的 Less 调用次数供测试断言。堆中只放非空租户，空闲租户不占选择代价。

## 7. 背压、移除与错误

每租户队列上限 limit：超限入队返回 ErrQueueFull，且只影响该租户。权重 <= 0 或 NaN：构造返回 ErrInvalidWeight。队列非空时移除租户：拒绝并返回 ErrTenantNotEmpty（任务去向 = 留在原租户队列，先排空再移除）。空调度返回 ErrEmpty。四类错误均为哨兵错误，可用 errors.Is 区分。

## 8. 并发与确定性

admit.Scheduler 用一把互斥锁串行化 Submit/Next。并发下「提交顺序」定义为获取入队锁的串行化顺序：任务序号在锁内分配，因此同一批并发提交重跑时序号分配可能不同，但任一单次运行内每个任务恰好被取出一次；而固定提交顺序的确定性任务流（单线程重放）调度序列逐元素相同。时钟以 func() time.Time 注入，仅在入队时给任务打时间戳，不影响调度决策。
