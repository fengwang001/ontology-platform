# 组提交顺序保证器设计

## 1. 目标与边界
并发写请求攒批、一次落盘一次同步；每条返回自己的全局序号与错误。
只用标准库；WAL 写本地临时目录。包：req / batcher / wal / commit / recover。

## 2. 序号分配时机（推导）
候选 A：到达即分配 → 落盘失败时序号永久空洞，回放无法重连续号。
候选 B：落盘成功后才分配 → 序号无法在落盘时写入记录，
且多条并发无法在落盘前确定彼此先后，调用方拿不到“我排在谁后面”。
规则（采用）：**批次组装完成、调用落盘之前**为该批一次性分配连续序号
`[base, base+n)`，并写入每条记录。落盘（含同步）成功才提交计数器
`base += n`；失败则整批序号作废、计数器不前进（回滚），批内全部请求收到
失败错误。因此第 3 批序号紧接第 1 批，无空洞。

## 3. 批次原子性（批级 CRC，非条级）
磁盘格式（小端序）：
- 文件头（仅一次）：魔数 `OWALv1\x00`（8 字节）。
- 每批：`批长 uint32 | 起始序号 uint64 | 条目数 uint32 |
  [ 条目长 uint32 | 载荷 ... ]... | CRC32 uint32`。
CRC32(IEEE) 覆盖“批长之后、CRC 之前”的整批字节。校验整批，
任何一条损坏都使整批 CRC 不符 → 整批丢弃，绝不只丢坏条目。
单条超过批字节上限：单独成批（批上限对它不适用），不拒绝；
因为拒绝会破坏“每条都有序号”的语义，拆分则破坏请求原子性。

## 4. leader 选择与唤醒（不串台）
提交组以 mutex + 条件变量组织：每个在途批次是一个 group，
含条目切片（每条带唯一调用方标识）与共享结果槽。
同一时刻只有一个 leader：第一个把请求挂入 group 的 goroutine 成为
该 group 的 leader，负责攒批（条件等待触发）、分配序号、落盘、同步、
写结果、广播；后来者为 follower，只追加并等待该 group 完成。
若 follower 的条目会使字节超限，它先关闭当前 group（触发立即提交）
并成为下一个 group 的 leader。唤醒后每 goroutine 只读取自己追加位置
对应槽位（按指针/下标一一对应），故绝不串台。

## 5. 攒批触发（三者先到）
条数达 MaxCount；或加入后字节将超过 MaxBytes（先成批，
该条进入下一批；超大单条直接独批）；或等待达 MaxWait（注入时钟）。
MaxWait=0 时每次立即成批（退化为逐条，配合 MaxCount=1 同理）。

## 6. 故障分类与回放
截断点分类（确定性，errors.Is）：
- 文件头不足 8 字节：ErrHeaderIncomplete。
- 批头（长/序号/条数，16 字节）不足：ErrBatchHeaderIncomplete。
- 条目长前缀或载荷不足：ErrEntryIncomplete。
- 声明字节齐但 CRC 不符（含 CRC 尾被截断）：ErrCRCMismatch。
回放恢复到“最后一个完整且 CRC 正确的批次”，其后忽略，序号连续无空洞。
落盘写一半崩溃 = 条目不完整或 CRC 不符 → 整批不可见；
下次序号从最后完整批之后继续。同步注入失败 → 整批错误 + 序号回滚。

## 7. 关闭语义
零请求 Close：只写 8 字节文件头（回放得 0 批，下一起始序号为 1）。
Close 与攒批竞争：在途请求要么并入最后一批成功落盘，
要么收到 ErrClosed；绝不“成功但未落盘”。

## 8. 复杂度与并发
同步次数 = 落盘批次数 ≪ 请求数（上限 100、1 万请求时 ≤ 200，
留 2× 余量给字节/超时触发）。序号区间按批次连续不重叠，
批内条目顺序与序号一致；全部路径经 -race 验证。
