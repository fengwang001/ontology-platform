# 分片扇出查询的部分失败合并器 — 设计推导

## 1. 模型与记号

- 分片集合 `S = {1..n}`，成功集 `O`，缺失集 `M`，`m = |M|`。
- 每条记录 `Record{ID, Count, Sum, Min, Max, TopK}`：分片做本地部分聚合，
  合并器只合并部分聚合，不逐条搬运原始数据。部分字段为零值即该字段缺席。
- 分片另给 `Bound`：其 TopK 任意条目可能达到的最大得分（上界）。
- 真实全量结果记为 `*`，成功部分算出的值记为带 `o` 下标。

## 2. 五种聚合在部分失败下还能说什么

### Count / Sum —— 单侧下界

所有分片贡献非负。缺失分片只可能再增加总量：

```
Count* = Σ_O c_i + Σ_M c_i ≥ Σ_O c_i = Count_o
Sum*   = Σ_O s_i + Σ_M s_i ≥ Σ_O s_i = Sum_o
```

故成功部分是**下界**，标注「≥ 当前值」，区间 `[当前值, +∞)`。
缺失越多下界越松，但标注形式不变。全部成功时为精确值。

### Min —— 真实最小值只可能更小

缺失分片可能含更小值：`Min* = min(min_O, min_M) ≤ min_O`。
故只能断言「真实 Min ≤ 当前值」，区间 `(-∞, 当前值]`。

### Max —— 真实最大值只可能更大

`Max* = max(max_O, max_M) ≥ max_O`，断言「真实 Max ≥ 当前值」，
区间 `[当前值, +∞)`。

注意 Min/Max 方向相反：Min 给出的是**上界**（真实值不超过它），
Max 给出的是**下界**（真实值不低于它）。

### TopK —— 可信前缀

设缺失分片上界之和 `B = Σ_{i∈M} Bound_i`（缺失分片最多还能塞进
TopK 的最高得分上限；每个缺失分片至多贡献若干个不超过其 Bound 的条目，
能挤入当前榜单的威胁上限以 B 刻画）。

合并后的当前榜单按 (得分降序, ID 升序) 为 `e_1..e_K`。
缺失数据最多能插入得分为 B 的竞争者。考察当前第 j 名：

- 若 `score(e_j) > B`，则任何缺失条目得分都严格小于它，它必在
  全量 TopK 之内（但前面可能插入条目，故**名次可能改变**，只确定入选）。
- 若 `score(e_j) ≤ B`，缺失条目可能把它挤出，不能确定。

由于榜单按得分降序，条件对前缀单调。可信前缀长度：

```
P = max { j : 对所有 t ≤ j, score(e_t) > B } = 最长的严格大于 B 的前缀长
```

边界：
- `M = ∅ ⇒ B = 0 ⇒ P = K`（全部成功，整表精确）。
- 当前条目数 < K 时，只对存在的条目判定；空集 M 下少于 K 条也算精确，
  因为真实总数本来就不足 K。
- B 很大（≥ 榜首得分）⇒ P = 0；B 很小（< 第 K 名得分）⇒ P = K。
- 前缀条目「入选确定、排名不确定」；前缀之后全部不可信。

## 3. 状态与错误

每分片状态枚举：`OK / Timeout / Corrupt / Duplicate / Failed`。
可判定错误（哨兵错误，`errors.Is` 区分）：`ErrNoShards`、
`ErrAllFailed`、`ErrCorrupt`、`ErrTimeout`、`ErrDuplicate`。
- 记录数与声称条数不符 → Corrupt，坏数据不并入。
- 同分片第二次结果 → Duplicate，忽略第二次，计数不重复。
- 所有分片返回空结果是合法精确结果（Count=0 等），与全失败可区分：
  全失败时状态无 OK 且返回 `ErrAllFailed`；全空时全部 OK，标注精确。

## 4. fanout 资源约束

- 信号量限并发 ≤ C；`inflight` 计数器在获取槽位后自增、释放前自减，
  非导出峰值 `peak` 记录历史最大值。
- 整体 `ctx` 带截止时间：`ctx.Done` 后不再获取槽位/发起请求；
  已在飞请求携带派生 ctx 被取消。`Wait` 在所有在飞 goroutine 退出后返回。
- 截止后到达的结果一律不计入 `started`。

## 5. 确定性

- 合并只依赖成功分片集合，与到达顺序无关；TopK 并列按 ID 升序。
- 缺失分片清单按分片 ID 升序排序；报告用结构序列化，逐字节可复现。
- 去重以分片 ID 为键（空串是合法 ID，只有一个分片可占用它）。

## 6. 包划分（5 个包 + demo，共 9 个 .go 文件）

`shard`（接口/假分片/校验）、`fanout`（并发+截止）、
`combine`（五种聚合合并）、`confidence`（标注+区间+可信前缀）、
`report`（报告+判定错误）、`cmd/demo`。
测试按包合并为表驱动：`fanout` 内部测试 1 个文件；
`combine` 与 `shard` 合 1 个 `combine_test`（同目录外部包）；
`confidence` 与 `report` 合 1 个 `report_test`。

## 7. demo 判定项（每条一行 OK/FAIL，≤20 行）

1 全成功五聚合精确；2 Count 下界；3 Sum 下界；4 Min 方向；
5 Max 方向；6 TopK 前缀=K（小上界）；7 TopK 前缀=0（大上界）；
8 并发峰值≤上限；9 重复返回不重复计数；10 全失败返回错误；
11 打乱顺序逐字节一致；12 超时出部分结果与缺失清单；
13 损坏分片判失败且不并入。
