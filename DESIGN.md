# 事件流增量解析器设计推导

## 1. 块边界落在 `\r` 与 `\n` 之间

行切分状态机只有两个状态：`inLine`（行内累积）与 `afterCR`（刚读到 `\r`，行已产出，但下一个字节身份未定）。

- `inLine` 读到 `\n`：产出当前行，留在 `inLine`。
- `inLine` 读到 `\r`：产出当前行，迁移到 `afterCR`。
- `afterCR` 读到 `\n`：这是上一个 `\r` 的延续（`\r\n` 是一个行尾），丢弃该字节，迁回 `inLine`。**不产出行**。
- `afterCR` 读到其他字节：说明 `\r` 是独立行尾，该字节属于新行，迁入 `inLine` 并正常处理。

关键推导：`\r` 一旦读到就立即产出行并进入 `afterCR`，于是 `\r\n` 的 `\n` 无论与 `\r` 同块还是跨块，都走同一条「丢弃」分支。若 `\r` 是流的最后一个字节，流结束即可，无需再产出空行——行已在读到 `\r` 时产出。因此 `\r\n` 永远只算一个行尾，不可能被当成两个空行。

## 2. 「切分点无关」的不变量

跨块必须保留的状态（linescan 层）：未完成的半行缓冲 `buf`、状态位 `afterCR`。stream 层另保留：当前事件聚合缓冲、`lastEventID`、重连间隔、是否已消费流首 BOM。

不变量：**任意时刻，解析器的全部状态只是「已消费字节前缀」的函数**，与这些字节被分成几块喂入无关。论证：对字节流做归纳。喂入一个字节时，状态迁移只依赖当前状态与该字节（状态机无隐藏输入）；把同一段字节切成两块顺序喂入，等价于连续应用同样的迁移函数序列，复合结果不变。因此对同一字节串的任意切分，喂完后状态相同，期间产出的行序列相同；行序列相同则 field 解析与事件派发序列相同。遍历所有切分点（1 字节、2 字节……整块）即是对该归纳的穷举验证。

## 3. 为什么「只有注释的块」不能派发空事件

派发判据是「数据缓冲非空或设置过 event 名」。注释行（`:` 开头）按规范被忽略，不触碰任何缓冲；未知字段同理。若空行无条件派发，下游会在每个注释块后收到一个 `data` 为空、`event` 为空的「幽灵事件」：对下游而言这与一个真实的空数据事件不可区分，会触发空消息回调、推进 UI/业务状态，甚至因为空事件不带 `id` 而干扰「最后事件 id」的语义。心跳注释的存在意义恰恰是「保活但不产生语义」，所以判据必须看缓冲而非看空行。

## 4. 超限为什么必须状态不变、不能「截断后继续」

第 9 条要求超限返回可判定错误且已聚合状态不变，理由：错误要能被调用方判定为「这条流已损坏」，调用方据此丢弃整条流并用 `lastEventID` 重连；若解析器内部偷偷改了聚合状态，重连前的查询值就不再可信。

若改成「截断后继续」，会破坏第 2 条的断言：切分点无关性依赖「状态只是已消费前缀的函数」。截断后，已聚合内容取决于超限发生在哪一块（切块不同，截断点不同，剩余字节被当成新行还是续行也不同），同一字节串在不同切分下会派发出不同事件序列，不变量直接失效。所以超限必须是「拒绝该次喂入、状态零变化」，让错误成为唯一出口。
