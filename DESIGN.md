
# DESIGN: float64 最短往返十进制编解码器

仅做一件事：float64 -> 最短十进制文本 -> float64，往返逐位相同
（用 `math.Float64bits` 比对），且文本尽量短。只用标准库；
核心代码不调用 `strconv.FormatFloat` / `strconv.ParseFloat`。

## 1. 精确模型

每个普通 float64 等于 `x = (-1)^s * m * 2^e`：

- 正规数：`m = 2^52 + frac`，`e = exp - 1023 - 52`
- 次正规数：`m = frac`，`e = 1 - 1023 - 52 = -1074`
- `bits` 包负责从 `math.Float64bits` 取出 sign / biased exp / frac。

所以 x 是一个精确有理数 `num/den`：e>=0 时为 `m*2^e`，e<0 时为
`m / 2^(-e)`。用 `big.Rat` 表示，全程无浮点误差。

## 2. 最短判据：逐位递增 + 精确往返检查

- 17 位十进制一定能往返：任意两个不同 float64 之间的相对间距至少
  约 2^-53 ≈ 1.11e-16，而 17 位十进制数的相对舍入半径为 5e-17，
  小于该间距，故 17 位十进制串落在唯一的一个 float64 舍入区间内。
- 15 位不保证：15 位的半径 5e-15 可大于两个 float64 的间距，可能
  同时进入相邻两个 float64 的舍入区间。如 `0.1` 的真实值是
  `0.100000000000000005551...`，"0.1" 能往返，但 15 位并非对所有
  数都够。
- 正确算法：从 1 位有效数字开始，生成该长度下最接近 x 的十进制
  数 D，**用精确整数算术把 D 舍入回 float64**，比较其 bits 是否
  等于 x；相等即答案，否则加一位，最多 17 位必成功。

为什么不用浮点回代：用 float64 乘除重建 D 再比较会引入第二次舍入
误差，可能把"精确上不属于 x 舍入区间"的 D 误判为可往返（测试构造
了一个 16 位串：浮点乘除回代 `==x` 成立，但精确比较不等）。

## 3. 精确十进制 -> float64（`parse`）

输入符号、有效数字串 digits、十进制指数 k，表示
`v = sign * digits * 10^k`。全程 `big.Int`：

- k>=0：`N = digits * 5^k * 2^k`，D=1。
- k<0 ：`N = digits`，`D = 5^(-k) * 2^(-k)`，即 v=N/D。

把 N/D 与 float64 二进制边界比较：放大到相同量级后按
round-half-to-even 选出最近的尾数与阶码；次正规、上溢
（返回 ErrExpOverflow）同样精确处理。无浮点中间值。

## 4. 十进制数字的生成（`dec`）

量级 `10^q <= x < 10^(q+1)` 用精确比较确定（先估算再校正，杜绝
log 误差）。对位数 n，取四舍五入后的前 n 位数字组成 D（精确
`big.Rat`），调用第 3 节的精确转换判等。包内非导出计数器 `checks`
记录精确检查次数；0.1 在第 3 次内成功（1、2、3 位）。

## 5. 文本组装规则（`fmtf`，确定性）

设有效数字 a1..an，首位 a1 的权为 10^q：

- `-6 <= q <= 20`：定点形式。q>=0 时数字放整数部分并按需补零与
  小数点；q<0 时为 `0.00..digits`。
- 否则：科学形式 `d.dddde±QQ`，指数至少两位（`1e+21`、`1e-7`）。
- `-0` 输出 `-0`；+0 输出 `0`。

边界两侧各两值：q=20（定点，如 1e20）与 q=21（`1e+21`）；
q=-6（定点，如 1e-6）与 q=-7（`1e-7`）。

## 6. 错误策略

NaN / ±Inf 没有自然的最短十进制文本（任何十进制串都解析为有限
数），静默产出会伪造语义，故 `Encode` 返回 `ErrNonFinite`。
`parse` 四类错误可用 `errors.Is` 区分：`ErrEmpty`、`ErrSyntax`
（非法字符，错误信息给出字节位置）、`ErrExpOverflow`、
`ErrTooManyDigits`。
