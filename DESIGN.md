# 熔断与舱壁隔离调用保护器 — 设计推导

分层顺序（自外向内）：`stat 计数 → breaker.Allow → bulkhead.Execute → timeout.Do(真实调用) → breaker.Report / stat 落账`。

## 1. 熔断计数只算「真实调用」的推导

设失败率 = 失败数 / 样本数。若把被熔断拒绝计入失败：

- 熔断打开后，每个请求都被拒且都计为失败，失败率恒为 100%；
- 打开 → 半开的判定若依赖失败率回落，则永远不满足，熔断锁死；
- 即使靠冷却进入半开，样本里仍混着大量「拒绝伪失败」，一次真实失败就再次打开。

结论：拒绝是保护器自己的行为，不包含任何下游健康信息，信息量为零，不得进入
失败率分子与分母。口径定为：**只有通过了熔断门且拿到舱壁额度、真正执行了的
调用，其结果（成功 / 可重试失败 / 超时）才进入熔断统计**；被熔断拒绝、被舱壁
拒绝只计入 `stat` 的独立计数，不进熔断样本。测试：打开后 1000 次拒绝，失败率
统计不变；冷却后一次探测成功即关闭。

## 2. 熔断置于舱壁之前的论证

- 熔断在前：下游已知故障时，请求在 O(1) 内被拒，不占用并发额度、不进等待
  队列，额度留给熔断半开后的探测调用；拒绝路径零排队。
- 舱壁在前：熔断要拒绝的请求也得先排队 / 占额度。下游故障期间舱壁队列被
  注定失败的请求塞满，恢复后的探测请求反而拿不到额度；拒绝延迟从 O(1) 退
  化为排队时长。

故规定**熔断在前**。测试：熔断打开期间发起调用，舱壁在途占用恒为 0。

## 3. 四条路径都必须归还额度

额度是计数资源，获取与归还必须配对。调用结果有四条出口：成功、失败（可判
定错误）、超时、panic。任何一条漏还，额度单调泄漏至 0，此后所有请求被永久
拒绝——这是不可恢复的静默故障，比下游故障本身更严重。实现上用 `defer` 在
获取成功后立即登记归还，panic 由包装器 `recover` 转为普通错误，保证 defer
链完整执行。测试：四条路径各跑 1000 次，结束后可用额度回到满值 N。

## 4. 不可重试错误是否计入熔断

定义：**不计入**。论证：

- 熔断器测量的是「下游是否健康」。不可重试错误（如参数非法）是调用方问题，
  下游处理正常，重试无意义；计入会让健康下游被误熔断。
- 可重试错误与超时都可能是下游过载 / 故障的表现，必须计入。
- 不可重试错误仍是「真实调用」的结果，计入 `stat` 的失败分类统计（口径等式
  不受影响），只是不进熔断触发条件。

接口：`classify.Of(err)` 给出三分类，`Kind.Trips()` 决定是否触发熔断计数。

## 5. 熔断状态机口径

- 关闭 → 打开：连续失败数 ≥ 阈值；或（样本数 ≥ 下限 且 失败率 ≥ 阈值）。
  两个条件独立，任一满足即打开。
- 打开 → 半开：`now - openedAt ≥ 当前冷却时长`。`now < openedAt` 判为时钟
  回拨：返回可判定错误，状态不变。
- 半开：最多 `Probes` 个探测并发通过，其余立即拒绝。
- 半开 → 关闭：探测成功数达到 `Probes`，冷却重置为基础值。
- 半开 → 打开：任一可触发失败立即打开，冷却 ×2（封顶 `MaxCooldown`）。
- 并发去重：状态迁移只由第一个满足条件的调用触发，迁移计数器断言只迁移
  一次，冷却不会重复加倍。
- 打开期间到达的迟到 `Report`（开门前已放行的在途调用）忽略，避免重复触发。

## 6. 统计口径自洽

`stat` 计数：总请求数、真实调用数、被熔断拒绝数、被舱壁拒绝数、成功数、
失败数（按 可重试 / 不可重试 / 超时 细分）。任意操作序列后恒成立：

- 总请求数 = 成功数 + 失败数 + 被熔断拒绝数 + 被舱壁拒绝数
- 真实调用数 = 总请求数 − 被熔断拒绝数 − 被舱壁拒绝数（= 成功数 + 失败数）
- Σ 按类别细分失败数 = 失败数

每个请求进入保护器时计总请求数；在熔断 / 舱壁两个关口恰有一处被计为拒绝，
通过两关者恰好在成功或失败处落账一次，故等式由「每个请求恰好落账一次」
直接推出。

## 7. 四类可判定错误（`errors.Is`）

- `breaker.ErrOpen`：熔断拒绝（含半开探测额度满）
- `bulkhead.ErrFull`：舱壁额度与等待队列均满，立即拒绝
- `classify.ErrTimeout`：单次调用超时（`timeout.Do` 包装返回）
- `breaker.ErrClockBackward`：冷却判定时检测到时钟回拨
