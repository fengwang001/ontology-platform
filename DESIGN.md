# CSV 解析设计

## 推导与结论

1. **空行与单列空值**：空行若作为记录，与“无记录的空文件”“列数未知”冲突；因此空行一律跳过。单列记录的唯一字段为空时，未引号写法会变成空行并被跳过，故必须写成 `""`。引号为空字段保留了“此字段存在且带引号”的语义。
2. **并行切点**：每段先按“段首在引号外”和“段首在引号内”各跑一次；从左到右，若前段实际结束于引号内，后段采用内假设，否则采用外假设。为让闭合引号也能在段首恢复，除首段外携带前一段最后 1 字节作为重叠前缀。拼接时丢弃偏移小于段起点的事件；开字段的值在普通内容边界去掉重叠字节，闭合引号则保留其转义语义。段内坐标加段起点；记录/字段数由事件中的分隔符、行尾、字段开始事件全局累加。错误随对应假设的路径产生，并按同一偏移规则换算。
3. **CR 待定**：未引号或闭合引号后见 `\r` 进入 CR 待定，不立即结束记录。下一字节是 `\n` 才产生行尾；否则报孤立 CR。半包时该状态可暂停续传；流在 `\r` 结束时 Close 判错。并行切点把待定 CR 作为重叠前缀交给下段，上段不发记录；若段为末段则按流末孤立 CR 处理。引号内 CR 是普通内容。
4. **即时上限**：字段每收到一个输出字节即计 1，`""` 在解析到第二个引号时输出一个引号字节；在追加该字节前检查字段上限，故第一个超限字节到达即失败。分隔符到达即先计数再检查字段数；行尾完成时先计数记录再检查记录数。错误后状态机终态，后续 Feed 返回同一错误。

**规范输入**：UTF-8 字节按原样处理；空文件无表；记录用 `\n` 结束，最后记录可有/无换行；空行不是记录；仅必要时加引号：字段含逗号、引号、CR、LF，或单列记录中的空字段；原本带引号的空/非空字段保留引号标记，保证逐字节往返。

## 状态转移表

| 状态 | 逗号 | `"` | CR | LF | 其他 |
|---|---|---|---|---|---|
| 字段开始 | 空字段；下一字段开始 | 入引号字段 | 入 CR 待定 | 行尾/空行 | 入未引号字段 |
| 未引号字段中 | 字段结束；下一字段开始 | 裸引号错 | 入 CR 待定 | 行尾 | 追加字节 |
| 引号字段中 | 追加 | 入“刚见引号” | 追加 | 追加 | 追加 |
| 刚见引号 | 字段结束；下一字段开始 | 追加一个引号；回引号中 | 入 CR 待定 | 行尾 | 闭合后杂散字符错 |
| CR 待定 | 首字节若非 LF：孤立 CR 错；否则行尾，重复 LF 另起空行 | 同左 | 同左 | 行尾 | 同左 |

续传边界还包括 EOF：引号中 EOF 报未闭合；CR 待定 EOF 报孤立 CR；其余状态允许无换行结束。每个字段保存原文字节半开区间 `[Start,End)`；引号字段的区间包含外层引号。
