# 分层时间轮设计推导

记 tick 单位 `t`,绝对 tick 序号 `k(x)=⌈x/t⌉`,槽触发时刻取槽区间右端点 `(k+1)t`(语义1要求 `D≤T<D+t`)。

## 1. 落槽取整
截止 `D=now+d`。候选公式:F1 `k(D)=⌈(now+d)/t⌉`;F2 `⌈now/t⌉+⌈d/t⌉`;F3 `⌊(now+d)/t⌋`。
触发时刻 T(k)=`(k+1)t`,推进时钟恰好到 T(k),要求 `D≤T(k)<D+t`。
- 验算 (now=13,d=10,t=10,ms):D=23。F1⇒k=3,T=30∈[23,33) 合法;F2⇒2+1=3 合法;F3⇒k=2,T=20<D 早触发,违规。
- now=10,d=10,D=20:F1⇒k=2,T=30∈[20,30);F2⇒1+1=2;F3⇒k=2,T=30。
- now=19,d=1,D=20:F1⇒k=2,T=30;F2⇒2+1=3,T=40,晚两个 tick(40≥D+t=30),违规;F3⇒k=2,T=30。

| 输入(ms) | F1 触发/判定 | F2 触发/判定 | F3 触发/判定 |
|---|---|---|---|
| now13 d10 | 30 合法 | 30 合法 | 20 早,违规 |
| now10 d10 | 30 合法 | 30 合法 | 30 合法 |
| now19 d1 | 30 合法 | 40 晚,违规 | 30 合法 |

唯一全程合法公式:**槽序号 `⌈(now+d)/tick⌉`,触发时刻取槽区间右端点**。实现用整数 `unixNano`,`k=(unixNano+tick-1)/tick`。

## 2. 降级保序
每个定时器带单调递增 `seq`(组内全局)。槽内双向链表按 `(deadline,seq)` 有序并入(同 deadline 的 seq 天然递增)。降级时把高层槽整链按下标键并入低层槽:直接落入低层者与高层降下者混排后,同 deadline 一律按 seq 升序,故语义3成立。逐个头插会反转 seq,被测试钉死。

## 3. 大跳跃
每层维护非空槽位集(位图 bitset)。推进 `[cur,target]` 时:level0 只枚举区间内非空槽;level i≥1 只在游标跨过层边界 `b`(b ≡ 0 mod S^i)时处理槽 `b/S^i mod S`。一次 Advance 中每个 `(level,slot)` 至多访问一次,故
**访问槽数 ≤ L·S + F**(L 层数,S 每层槽数,F 触发数;F 项容纳触发后被回调新注册并再次访问的 0 层槽),与跳过 tick 数无关。超出最高层范围者进最小堆(键 `deadline,seq`),仅当其 deadline tick 进入范围才回填。

## 4. 回调中新注册
选择:**d>0 且截止 ≤ target 的正常定时器在本次触发;回调里注册的 d≤0 定时器一律推迟到下次 Advance**。论证:语义2要求 d≤0 不同步执行;若回调内 d≤0 在本次触发,回调可不断 `After(0)` 自调用造成无限循环。推迟后它在下次 Advance 开头以当时时钟触发,`D≤T<D+t` 仍满足(进当前 tick 槽),且回调链每轮 Advance 至多一层,必然终止。
