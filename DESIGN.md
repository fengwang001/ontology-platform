# Ontology 属性级权限设计

模块：`ontology`，Go 1.26。授权三元组 `(主体, 属性, 读|写)`，按调用者主体逐属性判定；无权限即彻底不可见/不可写（不脱敏、不返回假值）。

## 推导点 1：默认拒绝 vs 默认允许

问题：某属性对某主体没有任何显式授权时，默认可读/可写吗？

- 口径 A：默认允许。后果：漏授权即泄露（新属性上线、新员工入职时自动放行）；「撤销授权」也无法真正收回——忘了删某条 grant 就一直放行。安全依赖"每条授权都记得正确配置"，违反 fail-closed。
- 口径 B：默认拒绝。后果：安全（无显式授权即不可见/不可写）；但读投影时可能把「必填属性」从对象里抹掉，投影对象不再满足 schema。
- 最终选择：**默认拒绝**。schema 必填约束的处置——被投影掉的必填属性使对象降级为「部分可见」视图，而不是「非法对象」：schema 约束只在写入与存储完整性上强制，读视图允许缺必填列。存储层的对象始终完整；投影只影响本次读取的返回值，永不回写。

## 推导点 2：写入含无权限字段——拒绝 vs 静默忽略

问题：调用者对属性 `b` 无写权限，却提交含 `b` 的写入，怎么处理？

- 口径 A：静默忽略 `b`，只写有权限的部分。后果：调用者以为 `b` 已写入、实际被丢弃；下次读回发现 `b` 没变——静默数据丢失 / 静默降级，且无法与"确实写成功"区分。
- 口径 B：整条拒绝。后果：fail-closed，调用者立即知道哪些字段没写进去。
- 最终选择：**创建和更新都整条拒绝**。创建（整对象）含任一无权限字段 → 拒绝；更新（部分字段补丁）含任一无权限字段 → **也拒绝**，哨兵错误必须指出字段名（`errors.Is(err, ErrWriteDenied)`）。先全量校验再物化：任一字段无权限则本次写入零字段落地（物化数为 0），绝不部分写入。

## 推导点 3：谓词引用不可见列

问题：查询谓词引用了调用者无读权限的列，如 `WHERE secret > 10`，怎么处理？

- 口径 A：把不可见列当 NULL 继续求值。后果：`NOT(secret > 10)` 对所有不可见行恒真——通过布尔取反把「secret 是否 > 10」泄露给无权限者（NOT 探针）。
- 口径 B：把不可见列当假值继续求值。后果：`secret IS NULL` 对不可见行恒真，同样能逐行探测 secret 是否存在/取值（IS NULL 探针）。
- 最终选择：**谓词一旦引用任一无读权限的列，整条查询拒绝**，哨兵错误指出列名（`errors.Is(err, ErrPredicateDenied)`），不做任何 NULL/假值/默认值代换，也不降级为"只查可见行"。谓词列的可见性检查先于任何行求值。

## 授权模型（acl）

- 主体：用户或组（`Subject{ID, Group}`）。组有成员，成员可以是用户也可以是组（嵌套）。
- 授权为三元组，动作读/写。`Grant` = 显式允许，`Revoke` = 显式拒绝（不是删除记录），无记录 = 默认拒绝。
- 直接授权优先：主体自身有显式条目（允许或拒绝）时覆盖组结论；自身无条目时，取其所在全部组（含嵌套祖先）授权的并集：任一组允许即允许，否则拒绝。

## 求值与投影（access / project）

- `access`：每个主体一个 Checker，惰性且仅一次地展开组闭包（BFS + visited，去重），之后判定任意多 `(属性, 动作)` 都复用该闭包，展开访问数不随判定属性数增长。
- `project`：按排序后的属性键构造新对象，仅拷贝可见键。不可见键**缺席**（不是零值、不是掩码），因此零值与真实缺失可区分。排序 + 新 map 保证同主体同对象任意遍历顺序投影逐字节确定。

## 写时强制与组装（api）

- 唯一入口 `Service`：`Create` / `Update` / `Get` / `Query`。
- Create 先做参数校验（必填列齐全），再逐字段查写权限；Update 对补丁中每个字段查写权限。任一不过返回带字段名的哨兵错误，且物化计数为 0（成功时为整对象全量物化，无中间态）。
- Query 先校验谓词引用列全部对调用者可见，再对结果逐对象做读投影（部分可见视图允许缺必填列）。
- 哨兵错误：`ErrWriteDenied`、`ErrPredicateDenied`、`ErrInvalidArgument`，均可用 `errors.Is` 区分。

## 跨模块不变量（非导出计数器佐证）

- 读投影确定性：`project.Projector.removed` 计数被移除属性；同输入两次投影结果 `reflect.DeepEqual`。
- 授权并集可判定：`access.Checker.expansions` 计数组展开访问；100 / 10000 两档组规模下，判定 N 个属性时展开只发生一次（计数 = 闭包大小，与 N 无关）。
- 写时零静默丢弃：`api.Service.materialized` 计数成功物化次数；任一无权限字段的写入后计数不增，错误可定位字段名。
