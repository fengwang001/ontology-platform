# 设计说明

## 3. 只含空白的续行

物理行尾为奇数个 `\` 时，Properties 只删除最后一个 `\` 和行终止符，再读取下一物理行。
续行接入前会跳过其首部空白；若下一物理行只含空白，这些空白就是全部续行内容，跳过後接入空串。
此时上一行末尾的续行符已经消失，逻辑行不再以 `\` 结尾，所以续接立即结束，不会再穿透到第三行。
例如 `k=a\`、空白行、`b=c` 的结果是 `k=a` 与 `b=c`。
这不是“空行被忽略”：该空白物理行已被续行消费；“剥离首部空白”同时解释了它为何贡献零个字符。

## 6. Store 的最小必要转义

Load 先识别逻辑行，再在键中寻找第一个未转义的 `=`、`:` 或空白分隔符。
因此键必须转义反斜杠、实际换行、所有 `=`/`:` 和空白；首字符为 `#` 或 `!` 时也必须转义，否则整行会在键解析前被判定为注释。
键中的 `#`/`!` 若不在首字符则无需转义。
值的分隔已经确定，所以值中的 `=`、`:`、`#`、`!` 和内部空白都是普通字符；只有首个值字符若是空白会被“吃掉”，故只需转义开头的第一个空白，后续空白自然成为值。
反斜杠和 `\n`、`\r` 在键值中都必须转义，避免改变转义或物理行结构。
内部制表符/换页符可原样保留；非 ASCII 按 UTF-8 原样写出。每行写 `key=value\n`，空键、空值也能由 Load 还原。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
| 1. 分隔符与空白 | `TestDelimitersAndWhitespace` |
| 2. 注释 | `TestComments` |
| 3. 续行与空白续行 | `TestContinuations` |
| 4. 转义与 `\uXXXX` 行列号 | `TestEscapesAndUnicodeErrors` |
| 5. 重复键覆盖与首次顺序 | `TestDuplicateKeyOrder` |
| 6. Store 特殊字符 | `TestStoreSpecialCharacters` |
| 7. 1000 组随机往返 | `TestRandomRoundTrip` |
