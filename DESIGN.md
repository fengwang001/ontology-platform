# LZ77 流式压缩器设计推导

## 1. 流格式（所有整数为 LE Base128 uvarint；dist/len 编码为值-1）

| 记录 | 字节布局 | 说明 |
|---|---|---|
| 流头 | `"LZX1"`(4) + winCap(varint) + chainCap(varint) | winCap 为窗口容量 |
| 字面量段 | `10xxxxxx`（计数1..63）+ 原始字节 | 连续字面量；计数内联，0 不出现 |
| 短回指 | `00llllll` + dist(varint) | len=1..63 |
| 中回指 | `01llllll` + lenExt(varint) + dist(varint) | len=64..(63+2^35)，值-64 |
| 刷新标记 | `0x80` | 边界：解压器此前输入已全部可还原 |
| 流尾 | `0xC0` + origLen(varint) + crc32IEEE(varint) | CRC32 覆盖全部原文 |

字面量计数上限 63，故编码器每 63 字节必须可决策，缓存上限有界。

## 2. 四处推导

### 2.1 与 Write 切法无关（语义3）

在位置 p 找到长度 L 的回指只需要检查未来 maxMatch 个字节；若 p+maxMatch
之前数据已全部到达，该决策与未来数据无关（贪心最优前缀，输出只依赖
p 起的已知字节）。因此 Write 时保留最后 maxMatch 个已收字节不处理
（挂起区），其余字节按确定贪心编码；任何调用切法下同一累计位置的
已知内容相同，故字节级输出相同。Flush/Close 强制把挂起区排空，
此时必须终结当前匹配，因为没有未来字节可延长它，决策依然确定。

### 2.2 重叠回指的复制（语义2）

d=3、len=30 时目标区与源区重叠：`dst[i]=dst[i-d]` 必须在写出 i 时
读取当时的目标缓冲（含刚复制的字节），形成周期重复。一次性
`copy(dst, src-window区)` 若 src 与 dst 为同一底层数组的重叠段，
Go copy 语义是前向逐字节拷贝，恰好正确；但显式按“源在历史中固定
不动”的块拷贝会错。实现对每次回指执行 `for i:=0;i<len;i++ {
out=append(out, out[len(out)-d]) }`：append 读的是含新字节的同一
切片，d=1 即 RLE。每字节一步、无额外分配，4MB 级开销可接受。

### 2.3 两种距离越界的区别（语义7）

dist>winCap：格式本身不可能（匹配器只查窗口），属结构性非法数据，
即使流很短也必报错，与当前进度无关。dist≤winCap 但 dist>已输出
字节数：引用了尚不存在的字节，错误依赖解压进度（截断流也可能呈现
此状），故分列两个哨兵。压缩器在 winCap 容量环形窗内查找，窗口
中不存在 dist>winCap 的候选，故绝不产出前者。并行块预置字典：块
编码器启动时先把上一块末尾 ≤winCap 字节灌入窗口与哈希链，但这些
字节并不在本块压缩记录里重新产出；流整体仍是一条流，字典字节是
更早块已输出的原文，“已输出字节数”按整条流单调计数，语义不变。

### 2.4 并行块压缩的确定性（语义9）

块 k 的匹配器状态只由“本块原文 + 上一块末尾 winCap 字节（预置
字典）”决定，不读取块 k-1 的任何编码结果，故块间无编码依赖、无
完成顺序依赖，各 goroutine 产出的记录序列确定且相同。衔接不需要
刷新标记：各块产出的是同一条记录序列的连续子段（字面量段不跨块
合并），按块序拼接后加统一流头与一条流尾（origLen 与 CRC 均为
全原文的确定函数），即为一条普通合法流。workers 只改变执行顺序，
输出逐字节相同。

## 3. 复杂度

哈希链：3 字节哈希桶 + 每位置一指针的链，每位置沿链至多 chainCap
个候选；候选计数 ≤ chainCap×输入字节。长匹配达到 maxMatch 即停，
全同字节每位置考察恒为 chainCap，不随规模增长。
