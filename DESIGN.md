# 推导一：ErrNever 的搜索上界

事实1（公历 400 年循环）：格里历 400 年内有 97 个闰年；400 年总天数
= 400*365 + 97 = 146097 = 7*20871，是 7 的整数倍。故 400 年后月、日、星期
完全相同（年只差 400），2 月 29 日在 400 年块内的分布也逐块相同。
事实2：cron 可行集合由 (月,日,星期) 三元组决定，与年份无关（年不是字段），
触发时刻在公历 400 年周期上循环。
证明（充分性）：设 S 为全部合法触发日期。任取 t，区间 [t, t+400年] 覆盖连续
400 个公历年；任何连续 400 个公历年恰含 97 个闰年且天数为 7 的倍数，星期
结构与标准块相同，故 S 非空时该区间必含 S 中至少一个日期。所以向后扫描
400 年仍无候选日期即可判定 ErrNever；Prev 对称（向前 400 年）。
为什么 28 年不够：28 年周期只在“每 4 年都闰”的区段成立，整百年不闰会打断
它——2100 不是闰年，且跨过它的 28 年含 10227 天（非 7 倍数），星期排布错位。
反例：表达式 `0 0 29 2 MON`，起点 t=2072-03-01 00:00 UTC。
2072-02-29 是最近一个“闰日且星期一”，但已在 t 之前；其后 2076…2096 的闰日
都不是星期一，2100 又不是闰年。按 28 年上界只搜到 2100-03-01 → 误报 ErrNever。
正确答案是 **2128-02-29 00:00 UTC**（该日为星期一，距起点约 56 年）。

# 推导二：FireOnce 最晚时刻的常数次求法

需要 (W, now] 内最晚触发时刻 s，不能逐个 Next（跳跃可达数十年）。
步骤（≤3 次 calc 调用）：
1. s1 = calc.Prev(expr, now)：不晚于 now 的最晚触发时刻。
2. 若 s1.After(W)，排定 s1，W 推进到 now；否则区间内无触发，什么都不做。
（影子/Stats 判断“有无”时可再算 s0 = calc.Prev(expr, W)，比较 s0==s1。）
Prev 的两种边界情形：
- t 本身恰为触发时刻：Prev 返回 t（“不晚于 t”取等号）。这是第 3 条
  Prev(Next(t))=Next(t) 的要求；FireOnce 用严格比较 s1.After(W) 排除 W 自身，
  区间 (W, now] 的开界因此正确。
- t 带秒/纳秒：Prev 忽略秒以下部分（结果秒纳秒恒为 0），返回 ≤t 的整分点；
  Next 返回严格晚于 t 的整分点。二者都不在秒级取舍候选，保证往返恒等式对
  任意 t 成立。
FireAll 需区间内全部时刻，仍逐个 Next，但靠“候选日跳跃 + 日内位扫描”保证
每次推进的工作量有界（见 AUDIT.md 实测计数）。
