## 1. 未完成计数：何时 +1 / -1

结论：**派生被接受时（入队前）+1；任务到达终态（执行完 / 跳过 / 失败）时 -1，且在任务函数返回之后**。

错误规则是「开始执行时 +1，函数返回时 -1」。提前终止交错（1 个 worker）：
1. 任务 A 被接受，开始执行（此时才 +1）。2. A 派生 B：B 入队，因还没开始执行不计数；A 函数返回并 -1 → 计数为 0。
3. Wait 此刻观测到计数 0、所有 worker 空闲 → 误判终止。4. B 才被取出执行，任务实际尚未跑完。
正确规则下 B 在入队前已 +1，步骤 3 计数为 1，Wait 不返回。根任务同理必须在入队前计数，否则根任务入队前计数恒为 0。

## 2. StealHalf 取整：⌈n/2⌉

| n | ⌊n/2⌋ 偷走 | ⌈n/2⌉ 偷走 | 后果 |
|---|---|---|---|
| 0 | 0 | 0 | 无任务可偷 |
| 1 | 0 | 1 | floor：唯一（长）任务永远偷不走，其它 worker 饿死；ceil：可偷走 |
| 2 | 1 | 1 | 相同 |
| 3 | 1 | 2 | ceil 再平衡更快 |

floor 在 n=1 偷 0 个：根任务在某 worker 一次性派生 8 个屏障任务，若它自己留 1 个并阻塞在屏障，该任务永远不可偷 → 死锁。
ceil 在 n=2 时两 worker 互偷看似 ping-pong 活锁；但本执行器规定**本地队列非空绝不发起窃取、先执行本地任务**，互偷不可能发生。故选 ⌈n/2⌉。
钉死测试：`TestStealRoundingBarrier`——8 任务/8 worker 屏障，floor 规则在对齐调度下死锁，ceil 通过。

## 3. 取消如何到达被偷走的任务

不遍历队列：Cancel 必须只与子组数有关（第四节），任务可积压 10 万个，遍历队列会使其与任务数线性相关。
方案：取消只在组树打标记（BFS 子组，访问节点数有计数器）。任务**出队后、函数体调用前**逐父链检查：任一祖先已取消则跳过并 -1，否则固定「未取消」判定再调用 fn——这一行就是「已开始的照常跑完」分界：
`if g.cancelledAncestor() != nil { 跳过 } else { fn() }`。检查祖先数 ≤ 树深（深度 3/30 两档计数器钉住）。
向已取消组 Spawn：在执行器入口同步查祖先链，直接记跳过并返回（不入队、不占 pending）；被拒绝计数只给过载/关闭。

## 4. 本地队列满：转移一半，失败则就地执行

拒绝会使递归展开在无全局过载时假性失败；选「转移一半到随机受害者再入队」，仍满则**就地执行**。
就地执行是有界尾调用式退化：只在所有本地队列饱和（过载边缘）才发生，fn 派生的子任务优先转移，递归深度只在持续全局饱和时增长，与 DFS 同阶且不会无界（每层先弹当前任务再入子任务，饱和由转移消解）。配合 MaxPending，栈与内存都有界。
