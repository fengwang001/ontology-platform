# Quoted-Printable 设计推导（RFC 2045 严格子集）

## 1. 行末空白（规则 3-1）

空格/制表符先按"待决空白"缓存，不立即提交：
- 其后是硬换行（`\n`，含 `\r\n`）或输入结束 → 该行末空白必须 `=20`/`=09`。
- 其后是任意其他字节 → 该空白不在行末，原样提交（如 `"a b" → "a b"`）。
因为只缓存最后的"空白段"（空白之间互不清空），决策只看紧邻的下一字符，每字节检查一次。

## 2. `=XX` 不被拆开（规则 3-2）

把已输出单元（原字节或 `=XX`）视为整体 token。提交前先看 `col + len(token)`：
- `> 76`：先输出软换行 `=\r\n`（其 `=` 计入 76），`col` 归零，再放 token。
故 74 列后遇到 `=3D`：74+3=77 > 76，先软换行，整枚 `=3D` 落新行。
边界：token 恰好让列到 76 时先挂起（margin），只有真的还有后续字节才补软换行，
因此 76 个可打印字符后直接结束的行**不**多余换行（规则 6 的 75/76/77 覆盖）。

## 3. 软换行恰好插在空格之后，该空格是否编码？

**不编码，保留原空格。** 解码时 `=\r\n` 整段被删除，该空格后面紧跟的是新行首字节，
而非行尾；解码拼回后空格前后字符相邻，空格不在任何行末。若改成 `=20` 反而多做了
一次转义（违反最小转义）。编码器在软换行前以原字节 flush 待决空白，且只在 76 列内
放不下下一 token 时才换，因此该空格与其后的内容最终同属一段连续文本。

## 4. 往返等式（规则 6 的准确形式）

定义 `N(x)` 为换行规范化：把 x 中每个不被前导 `\r` 跟随的 `\n` 替换为 `\r\n`，
单独的 `\r` 保留为普通字节。则对任意字节串 x：

    Decode(Encode(x)) == N(x)

即仅有的差异是裸 `\n` 变 `\r\n`；二进制字节、孤立 `\r`（编为 `=0D`）、行末空白
（编为 `=20`/`=09`）逐字节还原。编码产物恒被严格解码器接受。

## 5. 最小转义（规则 7）

编码器只在三种情形输出 `=XX`：(a) 字节不是 33–126 的可打印 ASCII（`=` 除外）；
(b) `=` 自身；(c) 按第 1 节被判为行末空白的空白。其余合法字节（含行内空白）一律
原样。因此 `Encode(Decode(y))` 相对 y 的变化只可能是 y 里"可打印字符被多余转义"
（如 `=41`→`A`）；行长/软换行位置由字节内容唯一决定，规范化后一致。

## 6. 语义 → 测试对照表

| 语义 | 钉住它的测试函数 |
| --- | --- |
| 1 转义与换行规范化 | `TestEscapeAndNewlines` |
| 2 76 列上限（含软换行 `=`） | `TestLineLimit75_76_77` |
| 3 行末空白 / token 不拆 / 软换行前空格 | `TestTrailingWhitespace`、`TestTokenNotSplit`、`TestSpaceBeforeSoftBreak` |
| 4 五类严格错误与偏移 | `TestDecodeErrors` |
| 5 跨切分点一致 | `TestAllSplits` |
| 6 往返 | `TestRoundTrip` |
| 7 幂等/最小转义 | `TestIdempotentMinimal` |
| 计数器 ≤2n | `TestCheckBudget` |
