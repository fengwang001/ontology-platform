# DESIGN

## 1. Write 边界为何不影响输出（语义 3/4）

压缩器维护两部分历史：窗口 W（已最终提交的字节）与待决缓冲 P（最近一次 Write 之后尚未决策的字节）。贪心 LZ77 在位置 i 必须看到「当前匹配无法再延长」才能提交一个 token。
规则：处理到 i 时，若匹配在 P 的最后一个可用字节仍未失配，则该 token 悬置——因为下一次 Write 可能继续延长它。仅在三种情况下强制决策：(a) 匹配失配（长度可定）；(b) 匹配达最大长度；(c) Flush/Close。
因此单字节 Write 与整段 Write 走的是同一循环，区别只在悬置 token 何时被后续数据接上，提交内容逐字节一致。Flush 强制结束当前匹配（语义 4 的边界），之后窗口含全部已输入字节，后续匹配仍可回指它们。连续 Flush 无新输入：没有悬置 token、没有字面量，刷新标记按「上一记录后是否已有数据」去重，第二次 Flush 直接返回空。

## 2. 重叠回指（语义 2）

距离 d < 长度 L 时源区与目标区重叠：块复制语义读出的是旧内容，而正确语义是 RLE 式展开（d=1 即重复同一字节）。
解压对回指按 `out = append(out, out[len(out)-int(d)])` 逐字节 append，每次重新用最新长度取索引；d=1 自然退化为重复。论证可接受：每字节一次 append，总工作量恰为输出字节数 O(n)，无额外放大，且 Go append 不会因自引用产生别名问题。

## 3. 两类越界距离（语义 7）

「d > 已输出字节数」是流本身在该偏移处不可满足（连字典都没有）；「d ≤ 已输出但 d > 窗口容量」是流引用了窗口策略承诺之外的历史，二者成因不同，必须分成两个哨兵错误，均在 Varint 读完后、复制前判定，并记录当前流字节偏移。
压缩器只从环形窗口取候选，d ≤ windowCap 恒成立，且候选存在于已提交窗口，故正常流绝不触发后者。并行块预置字典：块内匹配可回指「上一块末尾 ≤ 窗口容量」字节；这些字节真实出现在解压输出里——块流只是顺序拼接，解压器窗口自然累积，「已输出字节数」含义不变，无需特殊标记。

## 4. 并行确定性（语义 9）

块 k 的匹配器只读取：块 k 原文 + 块 k-1 末尾 windowCap 字节。其哈希链初始化只依赖这段预置字典，不读任何 token、不读其他块结果；同配置压缩函数为纯函数，故任意 worker 数与完成顺序产出逐字节相同。
衔接：每块压缩前预置字典但不发射任何记录；块间直接拼接记录，拼接点是自然边界（块内最后 token 在块末被强制结束，不写刷新标记——刷新标记仅用于 API Flush 的同步语义）。按块序收集结果即可。

## 5. 流格式

变长整数：LEB128 无符号，最多 10 字节（第 10 字节仅最低 1 位有效，否则溢出）。

| 记录 | 字节布局 |
|---|---|
| 流头 | `4C 5A 37`("LZ7") + ver(uvarint=1) + windowCap(uvarint) |
| 字面量段 | tag=0(1bit,记录高位置0) + n(uvarint) + n 个原文字节 |
| 回指 | tag 高位=1；tag 低 7 位存 (length-MINLEN) 低 7 位，续位同 uvarint 续位规则；随后 distance(uvarint，≥1) |
| 刷新 | 单字节 tag：高位=1 且 length 字段=0x7F（保留值，MINLEN≥3 永不冲突） |
| 流尾 | `54 4C`("TL") + totalLen(uvarint) + crc32-IEEE(uvarint) |

字面量段 n=0 不发射；CRC 覆盖全部原文。
