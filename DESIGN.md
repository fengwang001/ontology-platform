# 谓词常量折叠与谓词下推 — 设计与推导

## 1. 三值逻辑（Kleene 强三值）真值表约定
- 值域 {T, F, U}（U=未知/NULL）。AND 取 min（F<U<T），OR 取 max，NOT 取反；
  比较：任一侧为 NULL 则 U；T 正常比较；其余 F。
- WHERE 过滤只保留 T，因此 U 与 F 的过滤效果相同。

## 2. 三条「看似成立、三值下不成立」的重写（推导）
1. `x = x` 不能折叠为 T：x=NULL 时结果为 U，不是 T。
2. `A OR NOT A`：A=U 时 NOT A=U，U OR U=U，不是 T（排中律失效）。
3. `A AND NOT A`：A=U 时 U AND U=U，不是 F（矛盾律失效）。
   对照：`NOT (A AND B) = NOT A OR NOT B`（德摩根）。逐格验证：两侧均为
   max(NOT a, NOT b) 按 Kleene 格展开，对 U 也成立，故三值下等价，可安全重写。

## 3. 何时可以按二值优化（安全位置条件推导）
设位置语境 `strict`：表达式的值是否会被「原样观察」。
- WHERE 顶层只看 T：U 与 F 同效，允许把「实际为 U 或 F」的子式写成 F，
  把「T 或 U」写成 T。
- NOT 之下：NOT U=U，而 NOT F=T，二者不再同效，禁止二值化。
- CASE 的 WHEN 条件按 WHERE 语义（只取 T），但其 THEN/ELSE 是值语境；
  本实现不含 CASE，仅需保证 NOT 之下严格。
- 规则：`strict` 初值 false（顶层非严格）；进入 AND/OR 的子项继承同一
  strict（顶层合取/析取项仍在 WHERE 内，非严格）；进入 NOT 子项置 strict=true。
  非严格位置允许把 `A AND NOT A -> F`、`A OR NOT A -> T` 折叠；
  严格位置禁止。`x=x` 在任何位置都不折叠（它产生 U，非严格位置也无 T 行，
  但为可预测起见本实现对该结构不做二值折叠，仅在 demo/测试中展示其不被折叠）。

## 4. 包结构（标准库，状态在内存）
- ast：节点（Const/Col/Cmp/Not/And/Or）、规范化 Print、表名收集。
- fold：单遍自底向上折叠（常量比较、双重否定、常量子树、安全位二值折叠）。
- push：逻辑计划 Scan/Filter/Join；把只引用单表的 Filter 条件下推到该 Scan；
  多表条件保留在 Join 上方；完成后自检无跨表引用。
- rules：Rule 列表 + Fixpoint 不动点迭代；非导出 rounds/visits 计数；
  规则按声明序固定执行（见 §7）。
- equiv：n 列域 {T,F,U}（小整数+NULL），穷举 3^n 赋值逐点比对。
- cmd/demo：九项验收判定。
- 等价验证两档：精确三值相等用于与位置无关的规则（常量求值、双重否定、
  德摩根、常量吸收）；过滤真值相等（T 与非 T）用于安全位二值折叠规则及整条
  WHERE 重写链——后者在 §3 意义下保语义，但不保持 U/F 的逐点区分。

## 5. 不动点终止性与复杂度
每条规则必须严格下降一个良基度量，定义元组：
M = (节点总数, NOT 节点数, 非常量叶子数)，按字典序比较。
- 常量求值：减少非常量叶子 / 节点；双重否定：减少 NOT 数；
- 德摩根（单向 NOT(AND)->OR(NOT..)）：NOT 数不变但把 NOT 推向叶子，
  以「NOT 节点深度之和」为附加分量，严格下降；
- 安全位矛盾/排中折叠：节点数下降。
故每轮（任一规则触发）M 严格下降，无无限序列 => 终止。
迭代轮数上界：每个节点至多贡献常数次下降，实测并断言 rounds ≤ 4*节点数。
节点访问：每轮常数遍，断言 visits ≤ rounds*节点数*4。
震荡检测：记录每轮输出的规范化串，出现重复（非不动点）即报可判定 ErrOscillation。

## 6. 下推正确性
下推只移动「仅引用单表 t」的合取项到 t 的 Scan；多表项保留。
单表条件在 Inner Join/单表 Filter 下与其原位置过滤等价（逐行：不满足条件的
t 行在 Join 结果中对应元组必被过滤）。下推后每个 Scan 谓词只引用本表，
由 push.SelfCheck 穷举扫描节点断言。

## 7. 确定性
规则集按切片声明顺序应用并在 Fixpoint 内排序固定；德摩根仅单向，集合中不含
互逆规则。测试将规则顺序随机打乱 20 次，内部仍排序，规范化打印结果须逐字节
一致；另注入一对互逆规则，断言第二轮即检出 ErrOscillation。

## 8. 边界
nil 树与空 AND/OR -> 可判定错误；列引用未知表 -> push 阶段可判定错误；
AND/OR 单子节点按退化处理（单子直接采用）；深度 1000 链以迭代（显式栈）
遍历，不递归下降；重复子表达式不做 CSE，仅各自求值。
