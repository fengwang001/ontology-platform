# 随机超平面 LSH 近似最近邻分桶检索器 — 设计文档

## 1. 数据与距离

- 向量：固定维度 `d` 的 `[]float64`，类型为 `vec.Vector`。
- 欧氏距离：`dist(x,y) = sqrt(Σ(xi-yi)^2)`；内积：`dot(x,y) = Σ xi*yi`。
- 维度不符的查询/插入返回 `ErrDimMismatch`（含期望、实际维度）。
- `NaN` 与 `±Inf` 均为非法分量：建索引时拒绝该向量，计入 `Skipped` 计数。

## 2. 签名计算规则

每张表 `l` 有 `b` 个随机单位/高斯超平面法向量 `h_lj ∈ R^d`。签名 `s_l(x)` 是
`b` 位串：

```
bit_j(x) = 1  当 dot(h_lj, x) >  0
         = 0  当 dot(h_lj, x) <= 0   // 包含内积恰为 0（零向量约定取 0）
```

内积恰为 0 的约定为 **0 位**（"落在/贴在超平面负侧"）。该约定对所有向量确定性
生效，因此零向量在每张表的签名恒为全 0，不会造成不确定行为。

随机数：以显式 `int64` 种子创建 `math/rand.New(rand.NewSource(seed))`，第
`(l,j)` 个法向量的第 `k` 个分量按固定顺序取 `rng.NormFloat64()`。Go 顶层源
保证同种子序列逐位相同，且生成顺序与插入数据无关，因此同种子重建的超平面、
签名表逐字节相同；不同种子以概率 1 产生不同法向量从而签名有差异。

## 3. 候选生成与精排

`L` 张表独立建桶（桶键 = 该表 `b` 位签名）。查询时取查询向量在每张表的签名
桶，合并候选 ID（跨表去重），再对候选逐一计算真实欧氏距离，取 TopK。

嵌套语义：`L2/L4/L8` 的表 0..L-1 与 `L1` 的表是同一批超平面（建一次 8 张，
子集复用），故表数增加只会向候选集做并集追加 —— 保证召回随 L 单调不降。

计数器：
- `HashCalcs`：建索引时每向量在每表每位计算一次内积，故恰为 `N×L×b`；
- `DistCalcs`：精排阶段实际计算欧氏距离的去重候选数（须 ≤ 0.1N）。

## 4. 位数 b、表数 L 与候选数、召回的关系（推导）

设查询 q 与其最近邻 x 的夹角为 θ，随机超平面给二者同一位的概率
`p(θ) = 1 - θ/π`（Charikar 符号 LSH）。

- 单表 b 位全部相同（x 与 q 落同桶）的概率：`P1 = p(θ)^b`。
- L 张独立表中至少一张同桶（召回近似，忽略精排平局）：
  `R = 1 - (1 - p(θ)^b)^L`。
- 单表每桶期望大小 ≈ `N / 2^b`（均匀极限），期望候选数
  `C ≈ N·(1 - (1 - 2^-b)^L)`。

定性结论：
- **b 增大** → 桶变细，单桶向量数约按 `2^-b` 衰减，候选数单调下降；但
  近邻 b 位全同的概率 `p^b` 也下降，召回随之降低。
- **L 增大** → 多一次独立"抓同桶"机会，召回单调上升趋近 1；候选数随之
  上升（多桶并集），上限为 N。
- 因此 (b 小, L 大) 高召回高代价；(b 大, L 小) 低代价低召回。工程上取
  折中（本实现实测 b=8、L=8 时 N=5000 候选 ≪ 500 而 top10 召回 ≥ 0.6）。

边界：所有向量相同 ⇒ 任意超平面上符号一致 ⇒ 同桶，候选 = N，召回恒为 1。

## 5. 并发可见性

索引采用原子快照指针：建索引在后台完成全部 `L` 张表后，一次
`atomic.Pointer[Snapshot]` Store 发布；查询只 Load 当前快照。未完成的表
永不可见（全有或全无）。索引不可变，并发查询与构建无数据竞争。

## 6. 落盘格式（自描述 + CRC32）

全部大端，布局：

```
[固定头 22B] "LSH1"(4) | version u16 | d u32 | b u8 | L u8
             | payloadLen u32 | dataLen u32
[超平面区]   表数 u32, 每张: bits u32, dim u32, 紧接 bits*dim 个 float64
[桶表区]     表数 u32, 每张: 桶数 u32, 每桶 sig u64 | nIDs u32 | ids...(u32)
[元信息]     dim u32 | inserted u64 | skipped u64
[向量区]     可选: 恢复校验所需 N*d 个 float64（自描述，读回即可自洽校验）
[CRC32]      IEEE，覆盖以上全部字节，u32
```

固定头声明 `payloadLen`（超平面+桶表+元信息长度）与 `dataLen`（向量区长度），
故每个字节区间都可严格分类截断：

- `n < 22` 或 `n < 22+payloadLen+dataLen` 之前的字节落在固定头/超平面/
  桶表/向量区 → `ErrHeaderTruncated` / `ErrHyperTruncated` /
  `ErrBucketTruncated` / `ErrBucketTruncated`（向量区为桶表自洽数据的一部分）；
- 主体完整但缺尾部 4 字节 CRC → `ErrCRCTruncated`；
- 长度完整但 CRC 不符 → `ErrCRCMismatch`。

恢复：截断时返回已解码的最大完整前缀（最后一张不完整桶丢弃），并剔除引用
不存在向量 ID 的桶项（计数 `DroppedIDs`），保证恢复结果无悬挂 ID。超平面
法向量为全零时返回 `ErrDegenerateHyper{Table,Bit}`。

## 7. 暴力基准

`search.BruteForce` 对全部向量算欧氏距离取精确 TopK，作为召回率地面真值。
