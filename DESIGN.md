# 设计推导

## 第 3 条：续行接到“只含空白/空行”会怎样

`java.util.Properties` 的物理行读取循环里，行尾为奇数个 `\` 时：
丢弃该 `\` 与行终止符，然后**先剥掉下一物理行首部的空白**（Java 是一个
`while` 跳过空格、制表、换页），再把其余字符接上。

- 下一物理行有可见字符：如 `k=v\` + `   w` → 剥掉 3 个空格得 `vw`。
- 下一物理行**只含空白或为空**：剥空白的循环直接走到行尾，没有任何字符
  被追加；该行终止逻辑行（空物理行不是续行）。因此
  `k=v\` + 空行 → `k`→`v`，且该空行不会被重复计入。
  依据：JDK `LineReader.readLine` 的连续行处理是“跳过前导空白后读取行其余
  部分”，空白被吞是“首部剥离”规则的极端情形（剥掉了全部内容），而不是
  把空行当作新一条逻辑行。续行链 `a\\\n   \\\n  b` 中，纯空白行同样被整条
  消费并参与链，其后的 `b` 仍接上。

## 第 6 条：Store 需要转义哪些字符

回写以 `key=value\n` 为固定格式，Load 端规则决定最小转义集。

- 键：`\` 永远要转义（否则改变语义）；首个前导空白必须转义（行首空白会被
  跳过，`\ ` 只转第一个即可，其后空格安全）；`#`、`!` 仅在**键首**必须转义
  （键首的 `#`/`!` 会使整行被当注释，行中则无注释语义）；`=`、`:`、空白是
  分隔符，出现在键中必须转义。
- 值：`\` 必须转义；制表/换行/回车/换页转义（换行断行、`\t` 等控制符必须
  编码）；**开头**的空格必须写成 `\ `——Load 会吃掉分隔符后的前导空白；
  值中间与结尾的空格永不转义：Load 对值只剥前导、保留其余。`#`、`!`、
  `=`、`:` 在值中无分隔/注释作用（分隔符只在找键时识别一次），不必转义。
- 非 ASCII 原样 UTF-8 输出，不用 `\uXXXX`。

## 语义—测试对照表

| 语义 | 测试函数 |
|---|---|
| 1 分隔符 | （收尾填写） |
| 2 注释 | （收尾填写） |
| 3 续行 | （收尾填写） |
| 4 转义 | （收尾填写） |
| 5 重复键顺序 | （收尾填写） |
| 6 回写 | （收尾填写） |
| 7 往返随机 | （收尾填写） |
| 复杂度计数 | （收尾填写） |
