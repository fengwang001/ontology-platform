# 设计推导（UTF-8 ⇄ UTF-16 流式转码器）

## 1. 非法单元切分规则（由七样例推导）

规则（每个首字节独立处理）：
1. 首字节落在 `80..BF`、`C0 C1`、`F5..FF`：吞 1 字节，出 1 个 U+FFFD。
2. 长度 2/3/4 的首字节：先缓存首字节，用第二字节的合法区间（见下表）判定。
   - 第二字节落在非法区间：只吞掉首字节（1 个 U+FFFD），第二字节重新当新首字节。
   - 第二字节合法：进入续收，逐字节收剩余续字节；续字节缺失或不是 `80..BF` 时，已收前缀整体为 1 个非法单元，越界字节重新当新首字节。

验证样例：`F0 90 80 41`——`90` 不在 `F0` 的区间 `[90,BF]`？它恰在区间内；此时 `90` 合法、进入续收，第三字节 `80` 对四字节序列合法，第四字节 `41` 非法，已收的 `F0 90 80` 整体为 1 个非法单元，`41` 重处理为 `A` → `FFFD 41`。`E0 80 80`：第二字节 `80∉[A0,BF]`，吞 `E0`；`80 80` 各为孤立续字节 → 三个。`ED A0 80`：`A0∉[80,9F]`，吞 `ED`，后两个 `A0 80` 中 `A0` 孤立、`80` 孤立 → 三个。`C0 AF`：`C0` 首字节非法吞 1，`AF` 孤立 → 两个。`F4 90 80 80`：`90∉[80,8F]` 吞 `F4`；`90 80 80` 中 `90` 孤立吞 1，剩 `80 80` 两个孤立 → 四个。`E2 82`+EOF：第二字节合法、续字节缺失，整个残前缀 1 个单元 → 一个。`80 80`：两个孤立 → 两个。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 吞首 1，第二字节重处理 |
| `E0` | `A0..BF` | 同上 |
| `E1..EC` | `80..BF` | 同上 |
| `ED` | `80..9F` | 同上 |
| `EE..EF` | `80..BF` | 同上 |
| `F0` | `90..BF` | 同上 |
| `F1..F3` | `80..BF` | 同上 |
| `F4` | `80..8F` | 同上 |
| `80..BF` | 无（孤立续字节） | 吞 1 |
| `C0 C1 F5..FF` | 无（非法首字节） | 吞 1 |

## 2. 缓存上限与提前放弃

缓存只存当前未完成前缀。四字节字符最多缓存 3 字节（首字节 + 至多 2 个续字节；第四字节一到即出结果），硬上限 3。第二字节区间检查使我们看到第二字节即可判定 `E0 80..9F`、`ED A0..BF`、`F0 80..8F`、`F4 90..BF` 非法，不必等流结束；`C0/C1/F5..FF/80..BF` 作首字节则零等待。只有“前缀已合法但缺续字节”才必须等到下一字节或 EOF。

## 3. par 切点对齐

切点任意。每段独立跑流式转码，段间只交缓存、不跳过任何字节：段 i 结束时若残留未完成前缀（合法字符中间，或第二字节已合法后的残前缀），把 pending 原封作为下一段开头喂入；孤立续字节/非法首字节在段尾立即出 FFFD，不交接。切点后的字节成为下一段新输入，每个输入字节只在一段里被当“新字节”检查。

非法单元数因此不变：单元归属只取决于“当前 pending 是否存在”。切点只可能切断 pending（整包转交，下一段看到相同字节序列，决策相同）或切在边界（pending 为空，下一段独立重放开头）。例：`C3 | 80 80`——上段 pending=`C3`，下段从 `C3 80 80` 开始：`C3 80` 合法成 U+00C0，第二个 `80` 孤立，与单线程一致；若误把切点后续字节跳过或各自当孤立，会得到 FFFD×3。第 0 段独享 BOM 识别；严格模式下段尾 pending 不算截断（段走“转交而非收尾”路径），错误偏移按段起始全局偏移重算。

## 4. 上限断点（语义 9）

输出回调返回容量不足时回滚到上一个标量边界：不提交该标量输出；已喂进解析器但尚未产出标量的字节留作 pending。`Write` 返回的 n 等于已产出标量所对应的、已离开本实例的字节数；pending 不算已消费。续传输入为 `Pending()` 后接 `p[n:]`，拼起来与无限额逐字节相同。UTF-16 代理对作为一个标量整体提交，不出现半代理。

## 5. 检查次数记账

每个“作为新字节进入状态机”的字节计一次；因非法第二字节而重处理的字节在下一轮再计一次，最坏每字节 2 次（孤立首字节后紧跟被它连累重处理的字节），故单流 ≤ 2N，且不因 Write 切碎而回扫（不重放 pending：状态编码为 need/lo/hi 与已收首、第二字节）。`par` 中每字节恰属于一段；段间转交的 pending（UTF-8 ≤3、UTF-16 ≤2）在下一段重放一次，总量 ≤ N + 8·maxPending。
