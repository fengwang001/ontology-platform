# DESIGN - 可撤销批量重命名与冲突解析器

## 模型

命名空间是名字的集合（`name.Namespace`，内存 map + 互斥锁）。一批请求
`r_i = (old_i -> new_i)` 全部先校验、再排序、再执行；任何冲突都在修改前报出。

## 一、排序规则的推导

执行 `old->new` 的前提：`new` 此刻不存在，否则覆盖。若 `new` 本身是另一个
尚未执行请求 `s` 的 `old`，则必须先执行 `s` 把名字腾出来。于是得到依赖边：

- `r` 依赖于 `s` 当且仅当 `r.new == s.old`（`s` 必须先执行）。

因为合法批次中所有 `old` 互不相同（见冲突三），每个请求至多有一个前驱，
依赖图是**函数图**：若干条链 + 若干个环。链头（入度 0）即「`new` 不是任何
请求的 `old`」的请求。排序规则：

1. 按 `old` 字典序依次取每个链头，从链头沿「`next = newMap[cur.old]`」
   走到尾，依次输出。例 `{a->b, b->c}`：`b->c` 的 `new=c` 不是任何人的
   `old`，是链头，先执行；再走回 `a->b`。中途任何时刻目标名均不存在。
2. 链全部输出后仍未访问的请求必在环上，交给破环流程。

同层多链按链头 `old` 字典序、环按最小 `old` 字典序处理，故同一批请求无论
构造顺序如何，步骤序列逐元素相同（确定性）。整个过程只做常数次 map 查找：
建 oldMap/newMap 各 1 次/请求，存在性检查 4 次/请求，链走查 1 次/请求，
故查找总数 <= 4*(请求数 + 涉及名字数)，由非导出计数器 `lookups` 验证。

## 二、破环的通用做法与临时名数量

环上每个请求的 `new` 都是环内另一请求的 `old`，任何排序都会覆盖，必须借
环外临时名 `t`。设环为 `old_i -> new_i`（`new_i == old_{i+1 mod k}`）：

1. `old_0 -> t`（腾出 `old_0`）；
2. `i` 从 `k-1` 递减到 `1`：`old_i -> new_i`（`new_i == old_{i+1}` 已空）；
3. `t -> new_0`（`new_0 == old_1` 已空）。

每步目标名都不存在，共 `k+1` 步。每个环只需**一个**临时名（第 1 步借出、
第 3 步归还），环之间互不相交（`old`/`new` 各自唯一），故临时名总数恰好
等于环的个数。临时名取 `\x00tmp<N>`，`N` 从 0 递增，生成前校验不与现有
名、目标名、已用临时名冲突，冲突则 `N++` 重试；命名空间有限故必然终止。

## 三、四类冲突的判定条件（全部在任何修改之前）

设 `oldMap`/`newMap` 为本批请求的 old/new 集合：

1. `ErrTargetExists`：`ns.Has(new)` 且 `new` 不在 `oldMap`（目标存在且本批不搬走）。
2. `ErrDuplicateTarget`：两请求 `new` 相同，报出两个 `old`。
3. `ErrDuplicateSource`：两请求 `old` 相同，报出两个 `new`。
4. `ErrSourceMissing`：`!ns.Has(old)`，报出该名。

四类各对应一个哨兵错误，可 `errors.Is` 区分。`plan.Plan` 是纯函数，只读
命名空间，检测到冲突时命名空间逐元素不变。

## 四、边界语义

- 空请求集：合法，0 步。单请求：1 步。
- 自环 `a->a`：**无操作**。源存在时合法、产生 0 步；源不存在仍报
  `ErrSourceMissing`。自环不参与依赖图（否则退化成一元环）。
- 空串是合法名字；`/` 等路径分隔符按普通字符处理。名字合法性仅要求
  是合法 UTF-8（`name.Valid`）。
- 请求数远大于名字数 ⇒ 必然出现重复 `old`，被第三类冲突拦下。

## 五、执行、日志与撤销

`apply.Execute` 持命名空间锁执行整批（并发修改会被阻塞至批次结束），每步
成功后向临时目录日志追加一条记录。第 k 步失败（测试注入 `failAt`）时，已
执行的步骤**就地逆序回滚**（`new->old`），日志截回只剩头部。

日志格式：8 字节头 `ONTLOG01`，随后每条记录
`u16 oldLen | u16 newLen | old | new | crc32(本记录前四段)`。
截断分类（`undo.Classify`，逐字节可判定）：

- 切点 < 8 或头部损坏：`ErrHeaderIncomplete`；
- 切点落在记录的长度/名字载荷内：`ErrRecordIncomplete`；
- 载荷完整但切点落在 4 字节 CRC 内（或 CRC 值不符）：`ErrCRCMismatch`。

`undo.Undo` 读日志、取最大可恢复前缀，逆序执行 `new->old`；截断处之后的
步骤无法撤销，在 `Report.FirstLost` 报告。幂等性：撤销前检查 `new` 是否仍
存在，不存在则跳过，故同一日志撤销两次，第二次是无操作。
