# 设计推导

规范化定义（先做行内规范化，再由末尾策略决定尾部）：
E(x)：每个行尾 `\r\n`/`\r`/`\n` 都变成 `\n`（`\r\r\n`→`\n\n`）；删除每行末尾（含最后一行，当其后无换行时）连续的空格/制表符；NUL 与非法 UTF-8 原样保留。
尾部策略只作用于结尾的换行串：Preserve 不动；EnsureOne 把结尾换行串替换为单个 `\n`，无内容则输出 `""`；TrimBlank 删掉结尾全部空行（连同其换行），再保证一个 `\n`，无内容输出 `""`。

## 1. 空输入与只含换行
关键决策：EnsureOne 对**空**输入输出 `""` 而非 `"\n"`。若空→`"\n"`，则 N("")="\n" 而 N("\n")="\n"，N(N(""))="\n"≠N("")=""，破坏幂等；选 `""` 则三种策略都是「无内容→空」，幂等平凡成立。

| 输入 | Preserve | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""`（删空行；无内容，不补） |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

TrimBlank 对 `"\n"` 输出 `""`：删空行后无剩余内容，遵循「无内容→空」；再次规范化仍为 `""`，幂等成立。

## 2. 被删字节的 ToOut
要求：ToOut 单调不减；对任意输出位置 o，ToOut(ToOrig(o))=o。ToOrig 必须把每个输出字节映到其自身，故它不能取被删字节；被删位置 i 的像只能取其相邻的某个**保留**输出位置。
方案：被删字节映到「紧跟删除区间之后的第一个保留输出位置」；若删除区间后无保留字节（尾部），映到输出长度（终点）。
因此 `\r\n` 的 `\r` 与行尾空白都映到该行 `\n` 的输出位置；编辑器把光标置于被删空白时，落点在**换行符处（行末/下行起点）**，选择映到换行「之后」会把光标推过下行行首，错乱。

## 3. 空白缓冲超限（第 8 条）
延迟空白在看到行尾/EOF 前无法判定。两个选项：
- 拒绝：返回带原文偏移的可判定错误，已输出内容与映射保留，进入终态。幂等、互逆（第 5/6 条）对「成功结果」仍成立；错误状态可解释、可重放（调大上限重试）。不违反任何一条。
- 强制判定为非行尾并输出：真实行尾随后到达时，这些空白已无法删除，输出含行尾空白，违反第 2、5 条（结果不再幂等），且不同缓冲上限产生不同输出，违反第 4 条。
选择**拒绝**（`ErrWSOverflow`）。输出字节上限同理立即拒绝（`ErrOutputLimit`）。

## 4. par 拼接
按字节边界切成 K 段，难点：段首可能继承前段末尾的待定 `\r`（`\r|\n`）或跨段的行尾空白串。
观察：任何紧跟已确认行尾（输出 `\n`）之后的位置都是**干净点**——无待定 `\r`（行尾已归一）、无待定空白（空白必属于当前行，行尾已结束）。
算法：对每段找其内最后一个 `\n`（原始偏移）；切点取「不超过段边界的最近干净点」，即相邻两段最后 `\n` 位置的较小者（首块从 0，末块到 len）。由此得到 K 个块，每块以干净点起、以干净点（或 len）止，块独立用 norm 处理，首块与中间块用 Preserve，仅末块应用用户策略（结尾换行只影响最后一块）。
拼接：块输出直接相连；各块 span 加 (origBase,outBase) 平移后 merge（相邻同斜率锚点合并）。
修正范围有上界：跨边界重放的只是「边界到本侧最近 `\n`」的字节，即最后/第一行，≤ maxLine 而非段长。
非法 UTF-8 切点安全：切点在字节偏移，块各自独立解码，非法字节原样通过，切点两侧互不影响。
