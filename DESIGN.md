# 基于版本向量的多副本冲突检测器设计

## 1. 数据模型

- `Vector`：副本 ID 到 `uint64` 计数器的映射；某副本连续本地写入时仅自己的分量递增。
- `Entry`：键、值、写入者 ID、版本向量。键的一组版本按确定性顺序保存为兄弟。
- `Store`：一个副本拥有已登记副本集合、每个键的兄弟版本集合和兄弟上限 `cap`。
- 状态只在进程内存；只使用标准库。

## 2. 偏序四种关系的判定

取两个向量分量键的并集；缺失分量按 0 处理。遍历并集时维护：

- `less`：存在某分量 `a[i] < b[i]`。
- `greater`：存在某分量 `a[i] > b[i]`。

四种互斥结果：

1. 二者皆否：`Equal`，所有有效分量相同。
2. 仅 `less`：`Before`，A 严格因果在 B 前。
3. 仅 `greater`：`After`，A 严格因果在 B 后。
4. 二者皆是：`Concurrent`，二者互不可比。

因此并发必须同时观察到“更小分量”和“更大分量”。仅判断“所有分量 `<=`”无法区分 `Before` 与 `Equal`，也不能直接给出 `Concurrent`。

缺失分量不影响偏序：`{A:1}` 等价于 `{A:1,B:0}`，因为零计数器没有描述任何事件。比较只遍历两向量并集，比较次数上界为 `2*|union|`，不遍历全部历史副本。

## 3. 本地写入、合并与冲突

本地写入新值时，写入者分量加一；加一前必须检查 `math.MaxUint64`，溢出返回错误，禁止回绕。

同步 `X -> Y` 时按版本向量合并同一键：

- `Before`：新条目因果在前，忽略。
- `After`：新条目因果在后，覆盖旧集合。
- `Equal`：幂等无操作；等值向量只保留一个确定性值。
- `Concurrent`：并入兄弟集合，规范化去重并按 `(vector 的字典序, value)` 排序。

若兄弟数超过上限，确定性丢弃排序中最旧（最小）的兄弟，使最新的最大兄弟保留；`cap=1` 时退化为最后写入者胜。每次丢弃累加可读取计数。该规则使任意双向同步轮次收敛：同一因果集合在所有副本上产生同一有序集合，重复执行不再改变状态。

## 4. 裁剪退役副本的安全性

裁剪只允许删除向量中的退役 ID。对裁剪域内任意向量对，先记录原关系，再删除退役 ID 并重算：

- 所有关系保持不变：裁剪安全。
- 任一向量对关系改变：拒绝整个裁剪，返回可判定错误，不产生半更新。

直观上，被删分量若曾用来区分“只有更小分量”和“同时有更小与更大分量”，它仍承载活跃副本可见的因果信息；此时不得退役。裁剪安全检查与活跃副本的定义绑定。

## 5. 故障模型

- 计数器回退：同一存储再次观察到某写入者键的计数器小于已知值，返回 `ErrCounterRegression`。
- 未知副本 ID：外部向量或条目的写入者不在注册表时返回 `ErrUnknownReplica`；这使“未登记 ID”区别于普通缺失零分量。
- 溢出：递增前检查上界，返回 `ErrOverflow`。
- 序列化：二进制格式含魔数、固定元数据、向量、条目、CRC32。解码逐段校验，截断返回 `ErrTruncatedHeader`、`ErrTruncatedComponent` 或 `ErrCRCMismatch`，四类错误均支持 `errors.Is`。

## 6. 边界语义

零副本只允许空向量；单副本中任意两个版本可比；全零向量互相相等；同一副本连续写入严格递增。空存储同步为空，兄弟上限必须至少为 1。
