# 多租户配额公平队列调度器 — 设计与推导

## 1. 模型

- 任务 `Task{Tenant, Cost, Seq}`：租户 ID、代价（≥0）、全局限号。
- 每个租户一条 FIFO 队列，权重 `w>0`，各自维护虚拟时间 `vt`（float64）。
- 选择规则：从所有**非空**租户中取 `vt` 最小者，执行其队首任务；
  `vt` 相同按租户 ID 字典序打破并列（可复现）。
- 非空租户存于最小堆，选择为 O(log n)，空租户不在堆中（不占选择代价）。

## 2. 为什么推进量是 c/w 而不是 c*w

设租户 i 的权重 w_i。它每执行代价 c，虚拟时间增加 Δ_i = c / w_i。
选择器总是服务 vt 最小的租户，即让「虚拟时间落后最多」者先跑。

长流稳态下，所有活跃租户 vt 几乎相等（相差不超过一个最大任务），
因此 Δ 相等意味着各租户以相同的虚拟时间速率前进：

  实际代价速率 r_i 满足 r_i / w_i = r_j / w_j ⇒ r_i : r_j = w_i : w_j。

即权重 1:3 的两租户长期执行次数比为 1:3。若用 c*w，
则 r_i * w_i 相等 ⇒ r_i ∝ 1/w_i，权重越大反而分得越少，与配额语义相反。

## 3. 空闲租户重新加入：vt 抬升

若租户 X 离开时 vt=0，系统在其空闲期间由其他租户推进到全局虚拟时间 G。
X 以 vt=0 回来时会成为绝对最小值，调度器必须连续服务它，直到它的 vt
追上 G，独占步数约为 G * w_X / c（题目场景约 1000 步），既造成突发独占，
也让空闲成为套利手段。

做法：租户队列从空变非空（新到达/重新加入）时

  vt = max(vt, G)，   G = 当前所有非空租户 vt 的最小值（系统虚拟时间）。

（若此刻系统无其他非空租户，G 取所有已知 vt 的最大值，保证单调不退步。）
抬升后 X 从当前水位参与公平竞争：它不会立刻插队，也不会被饿死，
按权重份额逐步获得服务。租户首次加入 vt 初始为 0，同样经此抬升。

## 4. 零代价任务

c=0 时 Δ=0，租户 vt 不推进，会无限占据堆顶。处理：对有效代价设最小下界 ε：

  Δ = max(c, ε) / w，   ε = 1e-12。

零代价任务合法且免费记账（实际代价仍按 0 统计），但 vt 至少前进 ε/w。
ε 不能取机器精度：若 ε 过小，一批零成本任务累计推进量仍近似 0，会整批
独占。取 ε=1e-3（单位代价尺度）：零成本租户每跑一个前进 ε/w，
其零成本流与正常流交替获得服务（约各占一半）而非 100% 独占；
而 1000 个零成本任务累计才 1 个 vt 单位，对正常任务长期份额的扭曲
小于 0.1%。

## 5. 数值与溢出

vt 使用 float64。Δ = c/w：w 极小时 Δ 大但只做加法与比较，
1e5 次调度量级下远在 float64 安全范围内，无 int 乘法溢出；
w 极大时 Δ 趋小，由 ε 下界兜底。

## 6. 背压、移除与错误

- 每租户队列容量上限；超限返回 `ErrQueueFull`，拒绝入队且不影响其他租户。
- 权重 ≤0：构造租户时返回 `ErrBadWeight`。
- 向未注册/已移除租户提交：`ErrNoSuchTenant`。
- 移除非空队列租户：返回 `ErrQueueNotEmpty`，任务保留不动（不静默丢弃）；
  队列排空后才可移除。
- 四类错误均为哨兵错误，`errors.Is` 可区分。

## 7. 并发与确定性

调度器内一把互斥锁保护租户表、堆、账本与统计；条件变量在任务可用时
唤醒等待者。提交与取出分别串行化。确定性任务流（固定提交顺序与代价）
下调度序列唯一；并发提交时，「提交顺序」定义为获取入队锁的串行化顺序，
锁顺序即 Seq 顺序，故结果仍可复现（同串行化顺序 ⇒ 同序列）。
统计追踪每个租户执行任务数与实际代价和；公平性度量 = 实际代价占比与
权重占比的相对偏差。`stat` 仅在锁内更新，读取返回快照。
