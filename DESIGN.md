# 自然排序：设计与推导

## 比较规则（分层，靠前的层优先）
1. 用 chunk 包从左到右切出数字段 D（ASCII 0-9）与非数字段 N，两串同步逐段比较。
2. 一段位置上：串先结束者最小；否则 D < N（固定标签序）。
3. N 对 N：逐字节按字节序比较，首个不同字节定序。
4. D 对 D：不转整数，比较「去前导零后的规范串」——先比长度，长度相同再比字典序（如 "009"→"9"，"010"→"10"，长者大）。
5. 前导零个数**不在这里比**，按出现顺序记入延迟序列 Z。
6. 两串所有段的主比较全相等后，再字典序比较 Z（每段少零者小，如 (0,1)<(1,0)）；仍相等则原串相同（唯一返回 0 的情形）。

## 第3条推导：前导零为何必须延迟
- 若在数字段立刻比零数：a01 与 a1 数值相等，零多者大，于是 `a01b > a1c`，与题设 `a01b < a1c` 矛盾。
- 题设说明：数值相等的数字段应视为「主比较相等」，后续内容（b<c）先决定；零数只在整串主比较打平时兜底。
- 演算 `a01b` vs `a1c`：N "a"="a"；D 规范 "1"="1"（Z 记 1 vs 0，挂起）；N "b" vs "c"，b<c ⇒ **a01b < a1c**，Z 不再查阅。
- 演算 `a1b01` vs `a01b1`："a"="a"；D "1"="1"（Z 0 vs 1）；"b"="b"；D "1"="1"（Z 1 vs 0）；主比较全平，比 Z=(0,1) vs (1,0)，首位 0<1 ⇒ **a1b01 < a01b1**。
- 同理 `a1<a01<a001`（Z=0<1<2）；`x0<x00<x000`（Z=1<2<3）且三者规范串 "0" 小于 x1 的 "1" ⇒ `x000<x1`。

## 第4条：严格全序论证
定义键 K(s)=(P(s),Z(s))：P 为段主键序列（结束 < D(规范串) < N(字节串)，D 内按长度再字典序，N 按字节序）；Z 为各数字段零数序列。
每层字母表均全序 ⇒ 序列的字典序为严格全序；两个全序的字典序拼接 (P,Z) 仍是严格全序，故反对称、传递。
K 单射：P 给出段种类与规范内容，Z 唯一还原每段的零填充，拼接即原串 ⇒ K(s)=K(t) 当且仅当 s=t。

## 其余要点
- 不溢出：数字段只做长度/字节比较，40 位、40 万位同理。
- 非 ASCII：≥0x80 的字节一律进 N 段按字节比较；全角数字是多字节序列，不匹配 ASCII 0-9；不做 rune 解码故不 panic，字节序仍是全序。
- 复杂度：单次前向扫描，数字段的跳零与读规范一次融合完成；每字节至多检查 2 次，计数器 ≤ 2(len(a)+len(b))。

## 语义—测试对照表
<!-- 最后回填 -->
