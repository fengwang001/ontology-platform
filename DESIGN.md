# LZ77 自定流式压缩设计

## 1. 流格式（全部变长整数，uint64，LEB128；varint >10 字节或第 10 字节溢出即非法）

| 记录 | 标签 | 后续字段 | 含义 |
|---|---|---|---|
| 流头 | `0xA0` | ver(uvarint=1), cap(uvarint) | 魔数 1 字节、版本、窗口容量 |
| 字面量 | `0x01` | n(uvarint), 原始 n 字节 | 长度 ≤ MaxMatch |
| 回指 | `0x02` | dist(uvarint), length(uvarint) | 1≤dist≤cap，length≤MaxMatch(258) |
| 刷新 | `0x03` | 无 | 此前所有输入在解码器已可见 |
| 流尾 | `0x04` | total(uvarint), checksum(uvarint) | 原文总长、FNV-1a 64 校验和 |

魔数错误与版本错误为两个哨兵；未知标签、标签越界各自报错，错误位置是该记录首字节偏移。

## 2. 推导一：压缩结果与 Write 切法无关（语义 3/4）

问题：在 `Write` 边界不知道未来字节，最长匹配可能跨过边界；若直接把边界处的匹配
按当前缓冲截断，1 字节一段与整段写入会得到不同结果。

推导：维护「已决位置」`d` 与未决缓冲。每轮在 `buf[d:]` 处找匹配，且只接受
满足 `d+m+tail ≤ len(buf)`（`tail=1`，仿 gzip lazy 的最小前瞻）的匹配；
字面量同样要求 `d+tail < len(buf)`。即：**任何到达当前缓冲末端的匹配都悬置
（open match），不提交**。下一批字节到来后：
- open match 可延长 → 替换为更长匹配，继续悬置；
- 在其后发现更长匹配（lazy 评估，只看一步）→ 发射一个字面量，再走新匹配；
- open match 结束 → 发射它。
决策只依赖「已到达的完整字节序列」这一个状态，与它们由几次 `Write` 到达无关，
故输出逐字节相同。

对 Flush 的结论：悬置本质是「用未来信息做决定」，Flush 要求此刻必须形成边界，
故 Flush 强制提交 open match（按当前长度，允许贴缓冲末端）并发射刷新标记；
它因此是唯一的非确定性隐藏来源，语义只要求在相同累计偏移 Flush 时一致。
连续两次 Flush 之间无新输入时没有任何可提交内容，第二次零字节（用 dirty 标志）。

## 3. 推导二：重叠回指的解压（语义 2）

设回指 dist<length。块复制 `copy(out[w:w+n], out[w-d:w-d+n])` 在 Go 中保证
源随目标增长而被更新，可正确产出周期展开；但该保证依赖 copy 逐字节前向实现，
语义不直观。结论：**统一逐字节前向复制** `out[w]=out[w-d]`。
单条回指长度上限 258，循环 ≤258 次，O(n) 总复杂度，无性能退化；
既正确处理 dist=1/2/3 的周期展开，也覆盖普通不重叠情形。

## 4. 推导三：两种「距离越界」的区分（语义 7）

解码到回指 (d,n) 时存在两道独立的界：
1. d > 已输出字节数：引用了从未产生过的数据。即使历史未被丢弃也不存在，
   属「引用不存在内容」，对任何 cap 都非法 → ErrDistBeyondOutput。
2. d ≤ 已输出但 d > cap：内容确实输出过，已滑出窗口，按本格式协议不允许引用
   → ErrDistBeyondWindow。检查顺序固定（先 1 后 2），两错误可区分。

压缩器侧：插入窗口前做容量截断，只在窗口现存历史中找候选，匹配长度 ≤ MaxMatch，
故绝不可能产生情形 2；候选距离 ≤ cap 且 ≤ 已输出，情形 1 也不可能。
并行块：块 k 以「上一块原文末尾 cap 字节」预置窗口后独立压缩；解码是一条连续
流，块边界没有任何标记，预置字典字节在解码器处就是此前真实输出的前缀字节，
因此「已输出字节数」含义不变，两种检查规则不变。

## 5. 推导四：并行块压缩的确定性（语义 9）

第 k 块的压缩器状态 = 空窗口预置 dict=block[k-1] 末尾 cap 字节 + 块原文；
其输入只含本块与上一块原文，不读取任何其他块的压缩结果，也不感知完成顺序。
纯函数 f(block[k-1], block[k]) 无共享可变状态，goroutine 数只影响调度。
衔接：整流只写一次流头；块 0 从头压缩，块 k≥1 用预置字典压缩；各块均不写
流头/流尾/刷新标记，按固定块序拼接，末尾写一次流尾。故无需块间刷新标记，
workers=1/2/4/8 输出逐字节相同，且产出的就是第 5 条可流式解开的普通流。

## 6. 复杂度

哈希链：每位置 3 字节哈希入头表，链指针环内最近优先；候选链长度上限 chainCap
（构造校验 >0）。复杂度测试的「考察数」定义为每位置 min(链长,chainCap) 的链步
数；全同字节且匹配已达 MaxMatch 时立即停止，故每字节考察 O(1)，与输入规模无关。

## 7. 其他

单实例非并发安全；独立实例之间无共享状态。校验和对原文增量计算（FNV-1a 64）。
解压炸弹：解析出 length/literal n 后，在复制任何字节前检查 total+count>limit。
