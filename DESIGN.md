# 设计推导

## 末尾换行策略

策略为 `Keep`、`EnsureOne`、`TrimOne`。先规范每个行尾并删除行尾空白，再只处理“最后一个非空内容之后”的连续换行。

- `Keep`：保留原有结尾换行数量；没有换行则不添加。
- `EnsureOne`：存在一个或多个结尾换行时折叠成一个；没有换行则不添加。
- `TrimOne`：删除最后一个非空内容后的全部空行，但保留一个换行；没有非空内容或没有换行时不添加。

空输入选择输出空串。若 `EnsureOne("")="\n"`，再规范化会得到 `"\n"`，虽幂等，但该换行没有任何原文字节可承载，和“每个输出偏移都能逆映射到原文偏移”冲突。选择空串后三种策略都满足 `N(N(x))=N(x)`。

| 输入 | Keep | EnsureOne | TrimOne |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 被删字节的映射

映射端点采用左闭右开区间。输出位置 `o` 是两个字节之间或终点；`ToOrig(o)` 返回其对应原文端点。原文端点分两类：存活端点必须互逆，删除点可映射到其右侧最近的存活输出端点。

因此 `\r\n` 中的 `\r` 和行尾空白均映射到删除串右侧：普通行尾为该行 `\n` 的输出位置；流末尾最终被删除的空白映射到输出终点。这样两条函数单调不减，且所有存活输出位置仍满足 `ToOut(ToOrig(o))=o`。编辑器光标落在被删空白中时落在换行符之前；删除点本身不是输出中的光标位置。

## 缓冲与输出上限

未判定的 `[ \t]+` 必须缓存；选择“超限时拒绝”，因为强制把行尾空白当普通空白输出后，下一字节若为行尾，已经无法收回，会违反跨切分点一致和幂等。拒绝返回带原文起点的 `ErrWhitespaceLimit`，保留已输出内容，后续写入返回 `ErrClosed`。输出超出 `OutputLimit` 立即返回 `ErrOutputLimit`；严格 NUL 返回带偏移的 `*NULPolicyError`。

## 并行拼接

每段从本地 0 开始独立规范化。切点在 `\r` 与 `\n` 之间时，将该 `\r` 交给后段：前段输入截到 `len-1`，后段输入改为从该偏移开始；这是每处接缝至多 1 字节的修正。

切点在行尾空白中时，前段会在其 EOF 把空白删掉，而后段若以空白开头，这些空白可能属于行中间。故对每个非末段在其副本末尾追加哨兵字节 `x` 后再 `Close`，使空白不能在 EOF 被判为行尾；随后从输出中删除哨兵，并丢弃覆盖该哨兵的最后映射项。末段不追加，以保持全局 EOF 语义。随后再应用上述 `\r|\n` 修正。映射项先各自平移原文和输出基址，再拼接；删除映射可平移并独立成立。修正范围有上界：每处接缝只检查末字节/首字节及一个哨兵，复杂度为 O(K)，不随空白串长度增长。
