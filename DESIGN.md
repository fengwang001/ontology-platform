# 采样剖析与热点归因器 — 设计推导

包划分：`stack`（栈规范化）、`tree`（调用树聚合）、`attrib`（热点归因）、
`sampler`（固定间隔采样）、`dump`（落盘/读回）、`cmd/demo`（演示）。

## 1. self 与 total

一次采样得到一条根→叶的帧路径，沿路径每个节点命中 1 次。

- self：只计「样本恰好终止于该节点」的次数。每条样本恰好给一个叶节点 +1。
- total：计「该节点出现在样本路径上」的次数，即自身 self 加全部后代 total。

两条恒等式：

1. 所有节点 self 之和 == 总采样数 N（每条样本贡献恰好 1 个 self）。
2. 所有节点 total 之和 >= N，一般严格大于 N（祖先被自身路径上的后代重复计入）。

实现：插入一条路径时，途经节点 total++，仅终止节点 self++。

## 2. 递归帧的函数级归因

递归函数 F 可在同一条路径出现多次（多个 F 节点，互为祖先/后代）。
函数级 self = 所有 F 节点 self 之和（叶子唯一，无重复）。
函数级 total **不能**把所有 F 节点的 total 相加：后代 F 的整棵子树已包含在
祖先 F 的子树内，相加会重复计数。

规则：函数 F 的 total 只统计**不被路径上另一个 F 包含的最外层 F 节点**，
即遍历每条样本路径时，对函数名 F 只在第一次（最浅）出现处计 1。

栈 `A→F→G→F→H` 建树（以终止于 H 的样本计）：

| 节点 | self | total |
|---|---|---|
| A | 0 | 1 |
| A/F(外) | 0 | 1 |
| A/F/G | 0 | 1 |
| A/F/G/F(内) | 0 | 1 |
| A/F/G/F/H | 1 | 1 |

F 节点级 total 之和 = 2（错误）；F 函数级 total = 1（只取最外层）。

## 3. 深度截断

栈规范化 `Normalize(frames, maxDepth)`：相邻连续重复帧先合并（去重），
长度 > maxDepth 时只保留前 maxDepth 帧，末尾打截断标记（Truncated=true），
并令截断节点成为该样本的终止节点（接收 self）。被丢弃的深度数累加到
可读的 `TruncatedSamples`（按样本条数计），绝不静默丢弃整条样本。
长度 == maxDepth 不截断；空栈拒绝并计入 InvalidSamples。

## 4. 复杂度

- 插入沿路径走，每帧一次 map 查找，比较次数 == 深度；与既有节点总数无关。
  `insertCmp` 计数，上界：样本数 × 深度 × 4（含 map 判定等常量开销）。
- 归因是「树快照一遍 DFS 扫描 + sort.Slice 一次排序」，不重建树；
  `rebuilds` 计数器恒为 0。

## 5. 采样器

可注入 `Clock`（Now/Sleep 等价的 tick 驱动）与 `Source`（返回当前栈；
返回 (nil,false) 表示本次丢样）。固定间隔触发一次。

- 忙窗口丢样：Dropped++。恒等式：成功入树样本数（self 之和）+ Dropped
  == 应采样次数 Attempts。
- 时钟回拨/异常间隔（nextTick 早于上次）：跳过该 tick，Anomalous++，
  不产生负数/巨大间隔，不破坏 self 恒等式。
- Stop 幂等（sync.Once + 关闭通道），Wait 等待采样协程退出。

## 6. 并发

tree 内 `sync.RWMutex`：插入持写锁在一次临界区内沿整条路径完成
（total/self 与父链接同批可见）；归因/快照持读锁，看不到半更新。

## 7. dump 文件格式

小端二进制，自描述：

- 头（40 字节）：魔数 `PROFv1\n`(7) + 保留(1)=0 + 版本 u16(2) +
  maxDepth u32(4) + 节点数 u32(4) + 采样总数 u64(8) +
  截断样本 u64(8) + 头 CRC32 u32(4)，共 40。
- 记录序列，每条：深度 u32(4) + 截断标记 u8(1) + self u64(8) +
  total u64(8) + 名长 u32(4) + 名字 UTF-8。为前序 DFS，第 i 条记录深度
  决定其父节点，天然支持最大可恢复前缀。
- 文件尾：整段（头+全部记录）的 CRC32 u32(4)。

截断点分类（`errors.Is`）：`ErrShortHeader`（<40 或头 CRC 坏）、
`ErrShortRecord`（头部完整但某条记录字节不全）、`ErrCRC`（记录完整但尾 CRC
不匹配）。恢复：返回完整记录构成的最大前缀，并保证其按深度成链
（不出现缺父的子节点；不完整父记录之后的记录一律不纳入）。
