# 组提交顺序保证器 设计

## 1. 包划分
- `req`：请求 `Request{ID,Payload}`、结果 `Result{ID,Seq,Err}` 与哨兵错误。
- `batcher`：单 goroutine 攒批；触发条件为条数、字节、等待时长三者先到；单条超限时单独成批。
- `wal`：批次追加写 + fsync；自描述头、逐条长度前缀、批级 CRC32 与读回。
- `commit`：组提交编排，唯一 leader 落盘，等待者凭各自 channel 收自己的结果。
- `recover`：扫描日志到最后一个完整批次，遇坏批整批丢弃并停止。
- `cmd/demo`：临时目录端到端演示。

## 2. 序号分配时机与回滚
到达即分配会在落盘失败时留下空洞；成功后才分配又无法表达调用顺序。
规则：批次在收集结束的同一临界区取一段连续序号（先占号），随后落盘；
落盘或同步失败则把计数器回滚到本批首号，整批作废，批内每条返回
`ErrSync`/`ErrWrite`。回滚在单线程序号所有者中发生，期间后续批次已持有更大号
但尚未写盘；为严格满足“第 3 批紧接第 1 批”，提交循环串行处理批次，
前批未决不启动后批，失败回滚后下一批取号即连续无空洞。

## 3. 批次原子性
文件布局：文件头魔数+版本(8B)，之后每批
`magic(4) count(4) dataLen(4) crc(4) | 条目 [len(4)|payload]*`。
CRC 覆盖“条目区全部字节”，是批级校验而非条级校验。
读回先校验头界与长度，再整体校验 CRC；任一不符：
头截断、批头截断、条目截断、CRC 不符四类可判定错误，整批不可见并停止。
因此一批内第 3 条损坏会丢弃全批；已 fsync 的前批与后续有效批（只可能出现在
重新打开追加前；损坏尾部之后无有效数据）语义清晰。

## 4. leader 与唤醒
请求 goroutine 只把 `{req, resultCh}` 投递到队列后阻塞读自己的 channel。
提交 goroutine 是唯一 leader：取批、编号、写 WAL、fsync，然后按数组位置
逐条向对应 channel 发送结果。等待者从不共享结果槽，故不串台。

## 5. 资源与复杂度
每批一次 `Write`（可由注入 writer 聚合）加一次 `Sync`；10000 请求/上限100
理论最多 100 批，测试允许 <=200 以吸收关闭尾批与触发抖动。
攒批器字节按 `4+len(payload)` 预计算；单条超限时立即单独成批，绝不拒绝。
时钟用可注入 `Timer`，测试不依赖真实睡眠。

## 6. 故障与关闭
注入 writer 可返回写错误、sync 错误；sync 失败即整批错误并回滚序号。
`Close` 与请求竞争：关闭后队列不再收请求，在途请求要么被吸入最后一批并
成功落盘，要么收到 `ErrClosed`；成功结果只在 fsync 返回后发送，故不存在
“成功但未落盘”。

## 7. 边界
零请求 Close：只写 8B 文件头，无批次（定义为有头文件，回放为 0 批）。
空载荷合法；上限 1 退化为逐条；等待 0 立即成批；超大单条单独成批。
