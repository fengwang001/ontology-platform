# 只追加日志：段索引与范围回放 设计

## 1. 总体结构

日志目录下每段两个文件：`seg_XXXXX.log`（段数据）、`seg_XXXXX.idx`（稀疏索引，可删除）。
段号为 10 进制零填充，按段号即按时间排序。

- 段头（28 字节，定长）：魔数 4B(`ONSE`)、版本 1B(=1)、首序号 8B(uint64 BE)、事件数 8B(uint64 BE)、段头 CRC32 4B(校验前 21B)、保留 3B(0)。
- 事件帧：`len(4B uint32 BE) | payload(len B) | crc32-IEEE over payload(4B)`。空载荷合法（len=0）。
- 事件序号由首序号与段内位置隐式决定：第 k 条（k 从 0）序号 = 首序号 + k；帧内不存序号。
- 段头事件数在 Close 时回写（同偏移原地覆盖前 28 字节），读者必须以「读到的合法帧数」为最终事实。

## 2. 稀疏索引与定位推导

索引文件：头 `OSIX`+版本 1B；之后每条锚点 `seq 8B BE | offset 8B BE`，offset 为该事件帧长度前缀的字节偏移。
首事件必为锚点；此后每 N 条一个锚点。

定位回放 `[from,to]` 时选择**不大于 from 的最大锚点**，从该帧顺序扫描：

- 若选「数值最接近 from」的锚点，它可能 > from（from 落在锚点后半个区间）。
  回放只能顺序向前，无法从更大序号倒回，会漏掉锚点前、from 之后的事件，故禁止。
- 选 <= from 的最大锚点后，跳过数 `from-anchorSeq ∈ [0,N-1]`，严格小于 N。
- from 小于首锚点（即本段首序号）时该段无锚点可用：跨段场景改从更早段定位；
  全段扫描场景从段头开始。首锚点 == 段首事件，跳过 0。

## 3. 索引可从段完整重建

锚点只含 (序号, 偏移)：偏移来自段内帧的字节位置（扫描帧即可得到），
序号来自段头首序号 + 已读帧数。两者都能从段文件重算，索引不存放任何「仅写入时可知」的信息
（不存长度、载荷、时间戳、写入机状态）。因此删除 idx 后扫描全部合法帧重建，
与原文件逐字节相同（定长大端编码、锚点选择规则确定）。

## 4. 范围回放与跨段

按段号升序打开段，读段头得 (first,count)，要求相邻段满足
`prev.first + prev.count == next.first`，否则报 `ErrSequenceGap`（缺口区间
`[prev.first+prev.count, next.first-1]`）。

边界语义：`from>to` → `ErrInvalidRange`；`from` 小于最小序号 → 钳到最小序号（从头，不报错）；
`to` 大于最大序号 → 回放到末尾（不报错）。计数器：`skipped`=定位阶段丢弃事件数
（必然 < N）；`bytesRead`=实际读取字节数（含段头与所在锚点区间，上界为
区间事件编码长度之和 + 一个锚点区间字节数）。

## 5. 损坏分类与修复

逐字节截断（截断长度 t 从 1 到 size-1，读取以快照长度为界）：

- `t < 28`：`ErrTruncatedHeader`（头部不完整）。
- 已完整帧数 k 之后：尾余 `<4` → `ErrTruncatedLength`；`<4+len` → `ErrTruncatedBody`；
  `<8+len` → `ErrTruncatedCRC`；完整但 crc 不符 → `ErrCRCMismatch`（翻转字节注入，非截断）。

修复：截到最后一个合法帧边界（帧尾偏移），回写段头事件数=实际可读条数，
随后顺序读不再报错。索引损坏/不一致（锚点偏移处解不出合法帧或 CRC/序号不符）时
删除并重建索引、回退全段扫描定位，报告标记 `IndexInvalid=true`。四类错误用独立哨兵，
支持 `errors.Is`。

## 6. 并发

Writer 的追加与 Reader 的打开/读取由目录级 RWMutex 协调；Reader 打开时取文件大小快照，
只读快照内字节。解析帧前校验快照内剩余字节 >= 4+len+4 且 CRC 正确，
进行中的半帧永远不会被当作完整事件。回放永远看到某个一致的前缀。
