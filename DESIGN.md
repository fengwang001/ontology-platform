# 有预算的图遍历器：设计推导

## 1. 预算的计量单位：访问的节点数

本题规定预算以「访问的节点数」计量。若以「展开的边数」计量，截断点会随图的
存储形态漂移：同一张逻辑图，邻接表里重复边是否去重、自环是否保留、边数组
是否排序压缩，都会改变「展开 k 条边」实际覆盖到的节点集合；甚至同一节点
出度的表示差异（并行边计 1 条还是 n 条）都会让同一预算落在不同的逻辑位置。
节点数则与存储形态无关：无论边如何存放，「访问了 k 个不同节点」在逻辑图上
是唯一确定的，因此截断点可复现、可续传。

## 2. 截断确定性：必须固定的三处顺序

1. 出边顺序：邻接表按目标 ID 字典序排序（graph 包在插入时维护），展开同一
   节点时边的考察顺序固定，与 map 迭代顺序无关。
2. 同层入队顺序：frontier 是显式的 FIFO 结构（就绪队列加展开游标队列），
   父节点按访问先后排列，同一父节点的子节点按出边顺序逐个产生；绝不遍历
   map 来决定下一个访问谁。
3. 预算消耗口径：只有「首次标记 visited」才消耗 1 单位预算；弹出重复候选、
   考察指向已访问节点的边都不消耗。三条合起来，同图同起点同预算的两次遍历
   逐元素相同。

## 3. 续点必须包含什么

BFS 的瞬时状态 = 已访问集合 + 整个 frontier（不是最后一个节点）。只记最后
访问的节点会丢失同层尚未访问的兄弟节点与尚未展开的父节点余边，无法续传。
因此续点包含：

- Visited：已访问节点（按访问序），用于去重与恢复访问集合；
- Ready：已产生但尚未访问的候选节点（FIFO）；
- Pending：展开游标列表（节点, 下一条待考察出边下标），FIFO。

初始续点为 Ready=[start]；Ready 与 Pending 均空即「已完成」。

## 4. 懒展开：峰值队列上界

朴素 BFS 访问节点时把全部出边一次性入队，星形图（1 中心 + 1 万叶子）在
预算 100 下峰值队列会达到 1 万。本设计改为懒展开：游标队列中每个游标每次
只产生一个候选进入 Ready，访问一个节点才为其建立游标。任意时刻
len(Ready)+len(Pending) <= 2*已访问数 + 1，故峰值队列是 O(预算) 而非
O(出度)，实测远小于 4*预算 的上界。每条边被其游标恰好考察一次，考察边数
不超过已访问节点出度之和。

## 5. 续点编码与篡改分类

编码：magic(3B) | visited 数与 ID 列表 | ready 数与 ID 列表 | pending 数与
(ID, 下标) 列表 | CRC32(前全部字节)，字符串用 uvarint 长度前缀，整体确定。
解码按三段分类，均可用 errors.Is 区分：

1. 结构解析（magic、长度前缀、边界）：失败报 ErrIncomplete；
2. 语义校验（续点引用的节点必须存在于图中）：失败报 ErrUnknownNode；
3. 完整性校验（CRC32）：失败报 ErrChecksum。

按此顺序，逐比特翻转必然落入其一：长度区翻转多为结构错误，ID 字节翻转若
得到图中不存在的 ID 则为未知节点，其余（含 CRC 自身）归为校验失败。任何
变体都不会被误接受。

## 6. 故障与并发

- 续传时发现队列或游标引用的节点已从图中删除：返回包装了 ErrNodeGone 的
  错误并指名该节点，绝不静默跳过（否则分段等价于一次的承诺失效）；初始
  续点的起点不存在则报 ErrStartMissing。
- 遍历是纯函数：Walk 只读图、复制续点后再推进，无隐藏状态；多协程对同一
  图与同一续点并发续传，结果逐元素相同、计数器互不串台，-race 干净。
