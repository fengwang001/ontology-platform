# 设计推导：计数草图与频繁项检测器

结构：`d × w` 计数矩阵，键 x 经 d 个哈希各落一行一格；`Add(x,n)` 把 d 格各加 n，
`Estimate(x)` 取 d 格最小值。每格是「映射到该格的所有键的真实计数之和」，故每格 ≥ 真实计数，
最小值也 ≥ 真实计数：绝不低估。

## 1. 能不能支持减计数 Sub(x,n)？——不能

调用方能拿到的只有 `Estimate(x)`，它是**含碰撞水分的上界**，不是真实计数。
具体操作序列（取 d=1、w=2，键 `x`、`y` 恰好哈希到同一格，该格记为 C）：

1. `Add(x,1)`：C=1。x 真实计数 1。
2. `Add(y,1)`：C=2。y 真实计数 1。
3. `Estimate(x)` 返回 **2**（真实只有 1，多出的 1 是 y 的碰撞贡献）。
4. 调用方执行 `Sub(x, Estimate(x))` 即 `Sub(x,2)`：C=0。
5. `Estimate(y)` 返回 **0**，而 y 的真实计数是 **1**。0 < 1，不变量 1 被破坏。

根因：格子里存的是多个键计数的混合和，x 的真实贡献与 y 的贡献不可分离；
减去「估计值」会把别人的计数一起抹掉。即使约定只减真实计数在数学上安全
（每格减 n ≤ true(x) 后仍 ≥ 其余键计数之和），草图也**无法核验**调用方报的是真实值。
结论：**不提供 `Sub`**。调用方想「撤销」时：(a) 保留输入日志重建草图；或
(b) 按时间窗各建一个草图，合并用 `Merge`（逐格加，精确），过期窗口整体弃用，不做逐键减。

## 2. 保守更新是否保持「绝不低估」？——是，但它破坏合并同构

保守更新：`Add(x,n)` 先取 d 格最小值 m，只把小于 m+n 的格子提到 m+n。
不变量 1 论证：更新前每格 ≥ true(x)（归纳假设），故 m ≥ true(x)；
更新后每格 ≥ m+n ≥ true(x)+n = 新的 true(x)；其余键的格子只增不减。归纳成立，**仍绝不低估**。
但不变量 2 被破坏，具体序列（d=2，键 a、b 在第 0 行碰撞、第 1 行不碰撞，各加 1）：

- 合并喂入 `[a,b]`：a 使两格为 1；b 的最小值 m=0（第 1 行空格），只把空格提到 1，
  第 0 行碰撞格**保持 1**。
- 分开喂 `A=[a]`、`B=[b]` 再逐格相加：第 0 行碰撞格 = 1+1 = **2**。

1 ≠ 2：逐格加合并不是保守更新的同态。换逐格取 max 也救不了（同一键拆到两条流各计 1，
max 合并得 1，真实是 2）。结论：主草图**不用保守更新**，`Add` 一律逐格加 n，以保证不变量 2。

## 3. (ε,δ) 与 (w,d) 的换算

采用 `w = ⌈e/ε⌉`，`d = ⌈ln(1/δ)⌉`。常数来历：某行中 x 的期望高估 ≤ N/w
（其余 N 次插入各有 1/w 概率撞进同一格）；Markov 不等式给出
Pr[该行高估 ≥ εN] ≤ N/(w·εN) = 1/(wε) ≤ 1/e（取 w ≥ e/ε，e 是 Markov 界的最优底数）。
d 行相互独立，估计值=min 仅当 d 行全失败才失守：Pr ≤ (1/e)^d = e^(−d) ≤ δ（取 d ≥ ln(1/δ)）。
反向：调用方直接给 (w,d) 时，只能表述为「高估 ≤ (e/w)·N 的概率 ≥ 1 − e^(−d)」，
即 ε=e/w、δ=e^(−d)。因为 w、d 向上取整过，这是**保守**表述；不得声称比它更紧的 ε。

## 4. HeavyHitters 的阈值比谁？——比 Estimate

判定规则：`Estimate(x) ≥ θ`（θ=φ·N）即入选。推导：不变量 1 给出 Estimate(x) ≥ true(x)，
故 true(x) ≥ θ ⟹ Estimate(x) ≥ θ，真实超阈值的键必然入选，零漏报（不变量 4）；
多报（估计值虚高入选）是被允许的。若改用「Estimate(x) − εN ≥ θ」：一个真实计数
true=θ+1、估计值被碰撞抬高不足 εN 的键会被剔除——错误落在**漏报**一侧，直接违反不变量 4。
