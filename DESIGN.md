# 带撤回的增量聚合视图维护器设计

## 1. 模型与包边界

- `change`：一条变更包含单调版本号、操作、记录 ID、原组、原值，以及更新目标组/目标值；空组名合法，`HasGroup=false` 表示缺失。
- `agg`：聚合器族，统一支持插入、删除、重算，并显式报告删除是否依赖组成员。
- `journal`：日志文件先写自描述头，再写 `uint32` 帧长、变更字节、`CRC32-IEEE`；追加采用 `Write+Sync`，读取长度前缀后只读一帧。
- `view`：维护 `map[group]members[id]value`、每族聚合器、已应用最大版本和拒绝计数。
- `audit`：按变更流重放成员集合，再全量构建五族聚合，与视图快照逐组、逐聚合器比较。
- `cmd/demo`：组合上述包，在临时目录构造崩溃、截断、乱序和并发场景并打印判定。

## 2. 删除可撤回性推导

设组内成员为多重集合 `M`，删除值为 `x`。

- Count 的状态是 `|M|`，删除贡献恒为 1，因此 `Count(M-x)=Count(M)-1`；只需标量状态。
- Sum 的状态是 `ΣM`，线性运算满足 `Σ(M-x)=ΣM-x`；只需标量状态。
- Min 的状态只是当前最小值。若 `x != min(M)`，最小值不变；若 `x == min(M)`，标量不能说明是否有同值副本，也不能反推出次小值，故必须扫描成员。
- Max 与 Min 对称：只有删除值不等于当前最大值时可直接保留，否则必须扫描成员。
- DistinctCount 的标量只保存不同值个数。删除某值后，无法从计数知道该值是否仍被其他成员持有，因此删除时必须回到成员多重集合。

因此接口保留两阶段信息：`Delete(x) bool` 报告该聚合器是否需要重算，`NeedsMembers()` 是静态能力声明。只有返回需要的聚合器扫描成员；Count/Sum 始终不扫描，Min/Max 仅极值被删时扫描，DistinctCount 对每次删除扫描。扫描入参限定为目标组当前成员，因此访问数上界是该组成员数，不能也不必读取其他组。

## 3. Apply、Recompute、Commit

1. Apply：校验组存在、值非 NaN、版本合法；将记录变更作用于暂存成员表。更新等价于从旧组旧值删除旧 ID，再向新组新值插入该 ID。
2. Recompute：收集删除受影响的组与聚合器，在暂存成员上生成新聚合值；空结果标记删除整组。
3. Commit：在单一写临界区把暂存成员和聚合结果换成正式状态，然后推进最大版本。

读者在 Commit 后一次性拿到 map 快照，不会看到同一组 Count 已变、Sum 未变。写事务串行化，Recompute 期间到达的同组写入在锁后执行，不会丢失。

## 4. 版本、幂等与拒绝

设已应用最大版本为 `V`，输入版本为 `v`：

- `v < V`：返回版本回退错误，拒绝计数增加，不写日志、不改状态。
- `v == V`：规范化比较全部业务字段；相同则视为重复投递，直接成功且状态逐字段不变；不同则版本冲突拒绝。
- `v > V`：校验通过后先追加日志，再执行三阶段维护，成功后 `V=v`。

缺失组名与 NaN 在日志追加前拒绝；空串组名保留。重放时按同样规则过滤，所以截断日志的完整前缀能确定性恢复到同一状态。

## 5. 崩溃恢复与截断分类

正常路径先持久化日志再改内存。恢复时从文件头顺序重放完整帧：

- 字节数小于固定头：头部不完整。
- 固定头后不足 4 字节：长度前缀不完整；相对已知完整日志在帧边界截断也归入此类，因为下一帧前缀缺失。
- 前缀声明长度大于剩余字节：记录体不完整。
- 成帧完整但 CRC32 不等：CRC 不匹配。

重放遇到任何不完整/损坏帧即停止并返回可判定错误；此前完整帧已经构建，损坏帧绝不生效。测试在 Apply 后、Recompute 中途、Commit 前三个钩子制造进程态丢失，再仅依靠日志重建。

## 6. 浮点与边界

Sum 比较使用 `math.Float64bits`，禁止把误差容限当作一致。NaN 没有相等语义，插入即拒绝；`+0` 与 `-0` 作为成员键视为同一个值，Min/Max 比较也按数值相等处理。组内最后成员删除后删除组节点，查询返回 `(零值,false)`，而不是 `Count=0`。
