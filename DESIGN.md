# 设计推导

记号：N 为规范化器；策略 K=保留原样，E=确保恰好一个换行，T=删全部末尾空行但保留一个换行。
规范化主体（与策略无关）：`\r\n`、`\r` 均输出 `\n`（故 `\r\r\n`→`\n\n`）；删除每行尾部
空格/制表符（缓冲到看见行尾或 EOF 才能判定，纯空白行→空行）；其余字节（含 NUL、非法 UTF-8）
原样输出。策略只作用于"结尾连续 `\n` 的个数 m"。

## 1. 空输入与末尾换行策略

先对主体输出定义 m（结尾 `\n` 个数）与 prefix（去掉末尾空行后的前缀）。策略：
- K：不动。空→空，`\n`→`\n`，`\n\n`→`\n\n`，`"  \r\n"`（尾部空白删除）→`\n`。
- E：去掉全部末尾 `\n` 后恒补一个 `\n`，**空输入输出 `\n`**。空输入若输出 `""`，则 N 把
  无内容文件变成有内容文件的逆方向不自洽，但幂等其实两种选择各自都成立（`""`→`""` 是幂等的；
  选 `""` 仅使"确保一个"名不副实）。选 `\n`：N 的像中没有空串，再作用一次仍 `\n`，幂等；
  且语义统一为"恰好一个末尾换行"。代价：空文件被写入一个换行，文档明示。
- T：prefix 为空（整个文件全是 `\n`，含空文件）→ 输出 `""`（没有需要保留换行的"最后一行"）；
  否则去掉全部末尾 `\n` 再补一个。幂等：第二次主体输出 m=1 且 prefix 非空 → 同样 `prefix+\n`；
  而 `""`→`""`。

| 输入 | K 保留 | E 恰好一个 | T 删空行留一个 |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut

映射是阶梯函数，段为 [origStart,origEnd)→[outStart,outEnd)，长度可分别为 0（纯插入/纯删除）。
不变量：f=ToOrig 单调不减且 f 满射到"保留字节的原偏移"，要求 ToOut(f(o))=o。
对被删原偏移 i（行尾空白、`\r\n` 中的 `\r`）：i 位于保留点 a（前段最后保留字节）与
b（换行 `\n`，保留）之间。设换行输出偏移为 o。若 ToOut(i)=o+1，则 f(o)=b，
ToOut(f(o))=o 仍成立，但阶梯会在删除区间内先跳到 o+1 再在 b 处退回 o，破坏 ToOut 单调不减。
故**删除区间内所有 i 统一映射到 o**（左侧锚定，输出换行符的位置）。端点：ToOut(len(orig))=
len(out)；ToOrig(len(out))=len(orig)（终点恒锚文件尾，不回退到最后一个保留字节）。
编辑器语义：光标置于被删尾部空白中，映射到换行符位置 = 视觉上行尾、换行符**之前**
（偏移 o 的光标在第 o 字节之前），符合"光标留在行尾"的直觉。

## 3. 缓冲超限（第 8 条）

强制把待定空白判定为"非行尾"并输出：若后续真出现行尾，已输出的空白无法收回，违反第 2 条，
且输出含行尾空白→破坏第 5 条幂等。拒绝则不产生任何输出与映射变更，仅返回可判定错误
（ErrTrailingSpaceLimit，带该空白串起始原文偏移），规范化器进入终态；之后 Write 返回
ErrClosed。拒绝不改变任何已成立的字节结果，第 4/5/6 条不受影响。**选择拒绝**。
输出上限同理：超限即 ErrOutputLimit（带原文偏移）并终态。NUL 严格模式：ErrNUL 带偏移、终态、
已输出保留。

## 4. par 拼接

每段独立用 K 策略规范化（段无全局末尾语义；最终策略只在拼接后对全局结果作用一次）。跨段边界
只有三种待定状态，且都由"上一段最后一个字节"决定，故修正范围有上界（仅边界处 O(1) 段）：
1. 上段末为 `\r`、下段首为 `\n`：上段独立处理已把悬垂 `\r` 在其 EOF 输出成 `\n`，而全局下
   应二者合并为一个 `\n`——丢弃上段尾的那个输出 `\n` 及其映射段（该 `\r` 改判为被删字节，
   锚点在下段 `\n` 的输出位置）。
2. 上段末为悬垂空白（其后在下段才见到行尾）：K 处理上段时该空白在其 EOF 被判非行尾而保留，
   本就正确；拼接后若下段首字节构成行尾，则该空白实为行尾空白须删除——删除该空白对应的输出
   段与映射段（连续空白作为一个区间，定位 O(1)）。
3. 其余边界：直接拼接。
映射拼接：各段映射段的 orig/out 坐标分别加段全局 orig 偏移与累计输出偏移；删除的边界段直接
剔除；切点任意（1 字节段、`\r|\n`、空白中间）时每段只产生上述 O(1) 修正，故结果与切点无关，
等于单流结果。最后对拼接结果应用末尾策略（其实现本身是"主体输出 + 尾部调整"，映射追加零长/
删除段）。
