# 熔断与舱壁调用保护器 — 设计推导

## 1. 分层与调用顺序

调用路径：`stat.Protector.Do → breaker 判定 → bulkhead 拿额度 → timeout 限时执行 → classify 归类 → 回填统计`。

**熔断必须在舱壁之前**。熔断打开意味着下游已被判定故障，此时继续排队、占用下游（实际是本机等待队列）的并发名额毫无收益：被拒请求会无谓地填满舱壁等待队列，挤掉熔断恢复后半开探测的名额，并放大故障期延迟。熔断在前时，打开状态下请求在常数时间被拒，舱壁计数恒为 0。反之“舱壁在前则连拒绝也要排队”，违背快速失败。

## 2. 熔断计数口径（核心）

记总请求 R、真实调用 A、熔断拒绝 B、舱壁拒绝 H、成功 S、失败 F。

**熔断拒绝绝不计入失败率**。若把 B 计为失败，熔断打开后分母里所有观测都是“失败”，失败率被自身拒绝锁死在 100%，即使下游已恢复，冷却后半开前的统计仍永远满足打开条件，形成“一旦打开永远打不开”的死锁。正确口径：失败率只用真实调用的结果计算，即分子分母都只来自穿过两道闸门、真正接触下游的调用。半开成功后连续成功计数可清零，故一次成功即可回关闭。

统计等式（任意操作序列后成立）：
- R = S + F + B + H（每个请求恰好四种结局之一）
- A = R − B − H（真实调用即未被任一闸门拒绝者）
- F = F_可重试 + F_不可重试 + F_超时（失败按类别不重不漏）

## 3. 错误分类

四类可用 `errors.Is` 区分的错误：`ErrRetryable`、`ErrNonRetryable`、`ErrTimeout`、以及闸门错误 `ErrBreakerOpen`/`ErrBulkheadRejected`。超时单独成类（可重试但观测含义不同）。未知错误默认按可重试处理（保守假设瞬时故障）。

**不可重试错误不计入熔断失败计数，但计入统计 F**。理由：熔断描述的是“下游此刻是否健康”，4xx/参数错误/业务拒绝说明下游活着且正确响应，是调用方的问题；把它计为故障会让一个有 bug 的调用方把健康的下游熔断掉。熔断的触发只数 ErrRetryable 与 ErrTimeout；半开期收到不可重试结果同样视为“下游活着”，按探测成功处理。

## 4. 舱壁语义

容量 N、等待队列 Q：在途真实调用 ≤ N；第 N+Q+1 个请求立即返回 ErrBulkheadRejected，注入时钟不推进（无等待）。等待者被 ctx 取消时必须出队并唤醒后继，名额不泄漏。用 channel + mutex 维护 FIFO 队列与峰值计数 peak（非导出）。

**成功、失败、超时、panic 四条路径都必须释放额度**。释放放在 `defer`：panic 被 recover 转成错误，但 defer 照样执行；任何一条路径漏还，可用额度单调下降直至为 0，之后所有请求被拒且不可恢复。panic 在 timeout 执行边界被 recover，包装为可重试失败，绝不击穿保护器。

## 5. 熔断状态机

- 关闭→打开：连续失败 ≥ 阈值，或 失败率超阈值且真实调用样本数 ≥ 最小样本（两条件独立实现）。
- 打开→半开：Now ≥ openAt + cooldown；冷却加倍只在“半开失败回退”时发生，d=min(2d, max)。
- 半开→关闭：probeSlots 个探测全部返回非熔断计失败（成功或不可重试）。
- 半开→打开：任一探测失败（可重试/超时），冷却加倍。
- 半开期并发：仅 probeSlots 个并发探测可通过，其余直接 ErrBreakerOpen；探测全部归还槽位后才允许下一批。

**时钟回拨**：打开时记录 openedAt 与到期时刻；判定用单调的“到期时刻”比较，Now 小于已观测过的时间（回拨）时不迁移、返回 ErrBreakerOpen。迁移在 mutex 内完成，并发触发只迁移一次（迁移计数 transitions），冷却不会重复加倍。

## 6. 配置与边界

N、Q、失败阈值、连续阈值、probeSlots、cooldown ≤ 0 一律构造报错。N=1 退化为串行，语义不变。时钟通过 Clock 接口（Now）注入，测试用可控时钟。

## 7. 文件与包

classify（分类）、bulkhead（舱壁）、timeout（限时+panic 捕获）、breaker（状态机）、stat（组合保护器+口径校验），共 5 个包加 cmd/demo；每文件 < 200 行。
