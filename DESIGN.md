# 设计推导

## 3. 只含空白的续行

`Properties.load` 的续行不是由“行类型”决定，而是在扫描键值时看到行尾反斜杠后读取下一物理行。
它先剥掉下一物理行首部的空白（空格、制表、换页），再从第一个剩余字符继续扫描。
因此，若下一物理行为空或只含空白，首部空白剥离后没有任何剩余字符，当前逻辑行立即结束；
不会再透过这行的行尾继续读取第三行，之后的物理行重新作为新逻辑行解析。
这条规则与“续行首部空白被剥掉”是同一动作的两面：空白是连接点处要跳过的分隔，而不是键值内容；
当它耗尽整行时，连接点之后不存在可拼接的内容。

## 6. Store 的最小转义

Load 先跳过逻辑行首部空白，再把首个未转义的空格、制表、换页、`=` 或 `:` 当作键值分隔；
`#`、`!` 只在逻辑行首部第一个非空白位置表示注释。反斜杠会启动转义，裸换行或回车会切断物理行。

因此键必须转义：

- 任意位置的空白、`=`、`:`：否则会被识别成键值分隔；
- 任意位置的反斜杠、换行、回车：否则会改变转义或物理行结构；
- 仅开头的 `#`、`!`：键中其余位置的二者没有注释含义，无需转义；
- 仅开头的空白已被上一条覆盖；其他无法安全原样出现的 ASCII 控制字符用 `\uXXXX`。

值位于分隔符之后。解析器只会吞掉分隔符后的一段前导空白，一旦遇到非空白字符，后续空格和值尾空白均保留；
所以值只需转义第一个字符若是空白，内部空格、制表、换页及首尾的 `#`、`!` 均可原样输出。
值中的 `=`、`:` 也不是分隔符，无需转义。反斜杠、换行、回车仍必须转义；非 ASCII 字符直接按 UTF-8 输出。

## 语义与测试对照

| 语义 | 测试函数 |
| --- | --- |
