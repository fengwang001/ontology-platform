# 设计推导

## 1. UTF-8 非法单元

规则是“最大合法前缀”：首字节非法或遇孤立续字节，立即吞 1 字节；合法首字节后，只连续收集它仍可能需要的续字节。一旦出现非续字节、越界第二字节，或到达应有长度，单元立即结束；流结束时，未达到应有长度的已收集字节整体算一个单元。每个完整非法单元替换成一个 U+FFFD。

因此 `F0 90 80 41` 先收集 `F0 90 80`，遇到 `41` 结束，故 3 字节只换一次；`E0 80 80` 中第二字节已非法，单元为 `E0 80`，余下 `80` 独立；`ED A0 80` 同理；`C0 AF` 两个非法首字节；`F4 90 80 80` 是 `F4 90` 加两个孤立续字节；`E2 82`+EOF 是一个二字节截断前缀；`80 80` 是两个孤立续字节。

| 首字节 | 第二字节合法区间 | 第二字节非法 | 续字节不足 |
|---|---:|---:|---:|
| `C2..DF` | `80..BF` | 吞首字节 | 吞已收全部 |
| `E0` | `A0..BF` | 吞已收 2 字节 | 吞已收全部 |
| `E1..EC` | `80..BF` | 吞已收 2 字节 | 吞已收全部 |
| `ED` | `80..9F` | 吞已收 2 字节 | 吞已收全部 |
| `EE..EF` | `80..BF` | 吞已收 2 字节 | 吞已收全部 |
| `F0` | `90..BF` | 吞已收 2 字节 | 吞已收全部 |
| `F1..F3` | `80..BF` | 吞已收 2/3 字节 | 吞已收全部 |
| `F4` | `80..8F` | 吞已收 2/3 字节 | 吞已收全部 |
| `80..BF` | 无 | 吞 1 字节 | 不适用 |
| `C0 C1 F5..FF` | 无 | 吞 1 字节 | 不适用 |

## 2. 流缓存

最长 UTF-8 为 4 字节，所以只需缓存首字节后最多 3 个续字节；UTF-16 只需缓存 1 个尾字节或 1 个高代理码元。合法首字节可先等待；第二字节落在非法区间可立刻判错，无需 EOF；`E1..EF` 的普通前缀需等满长度，遇到非续字节也立刻结束。EOF 时缓存整体是一个截断非法单元。

## 3. 并行切点

对切点 `c`，最多回看 3 字节，寻找“末端恰在 `c` 前、且预期长度越过 `c`”的合法 UTF-8 前缀；找到就把该前缀的起点作为本段起点，否则从 `c` 开始。各段按这些对齐点形成不重叠区间，顺序使用普通流式解码器。`C3|80 80` 会把 `C3` 留给后段，后段解出合法 `C3 80`，再处理独立 `80`；切点在非法单元中部时，最大合法前缀同样整体后移，因而非法单元吞掉的字节不会被拆成两个。回看固定 3 字节，不重扫整段。

## 4. 上限与断点

解码器在接收能结束一个源标量的最后字节前，先检查目标方向该标量所需最大空间：UTF-8 目标预留 3 字节（替换符长度），合法标量实际为 1..4；UTF-16 目标预留 4 字节，防止代理对输出一半。空间不足即在标量边界停止并返回输出超限，尚未结束的缓存字节不计入 `Write` 的返回值。调用方可用 `Pending()` 取这些字节，新建实例后从 `Pending()+未传输入` 继续；已输出标量与消费字节一一对应，故拼接结果幂等。
