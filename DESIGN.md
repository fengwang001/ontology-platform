# 严格 Base64 规范尾部

标准 Base64 把每 3 个输入字节编码为 4 个 6 比特字符。

- 3 字节余数为 0：组内 24 个信息比特全部来自输入，无 `=`。
- 余数为 2：前 3 个字符承载 18 个输入比特；第 3 个字符只用高 4 位，低 2 位必须为 `00`，第 4 位固定为 `=`。
- 余数为 1：前 2 个字符承载 12 个输入比特；第 2 个字符只用高 2 位，低 4 位必须为 `0000`，后两位固定为 `=`。

因此 `QR==` 虽被宽松解码成 `A`，但 `R` 的 6 比特值是 17，其二进制为 `010001`；作为末组第 2 字符时低 4 位为 `0001`，不满足必须为 `0000` 的规范要求。规范编码对同一个 `A` 只产生 `QQ==`，因为 `Q=16=010000` 的低 4 位为零。

统一规则：先检查填充形状必须恰好是无填充、一个末尾 `=` 或两个末尾 `=`；再检查没有输入对应的“残余比特”是否全部为零。一个尾 `=` 检查第 3 字符低 2 位；两个尾 `=` 检查第 2 字符低 4 位。该规则拒绝把不可见的填充比特用于携带额外信息，因而保证每个字节串的标准编码唯一，也保证严格接受的字符串重新编码后逐字符不变。

## 测试对照

| 语义 | 测试函数 |
| --- | --- |
| 待实现后补充 | 待实现后补充 |
