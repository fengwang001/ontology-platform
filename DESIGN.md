# 设计推导：带补偿与断点续跑的 DAG 工作流编排器

## 0. 总览与数据结构

- 五个包依赖单向：`graph → journal → step → policy → orchestrate`（箭头为"被依赖"方向，
  即 `orchestrate` 依赖全部，`graph` 不依赖任何包）。
- 记录帧：`[4 字节大端长度][4 字节 CRC32][JSON 载荷]`。帧长度/CRC/EOF 三者可判定
  半条记录：长度声明后字节不足、CRC 不符，或尾部恰好残留长度前缀，一律视为"写到一半"，
  从该字节起全部截断丢弃（帧后不允许尾随垃圾）。
- 记录 JSON：`{Seq, Step, Phase, Attempt}`，`Seq` 由 journal 单调分配，是唯一真相。
- 重放只做 `map[step]Record` 的折叠（每步保留序号最大的一条），**不触碰图**；
  折叠完成后由编排器对每个 step 访问一次图（取层数），上界 O(N)，与轨迹长度 L 无关。
- 注入时钟：`Clock` 仅暴露 `Now()`/`Sleep(d)`，测试用手动时钟即时推进；代码不出现
  `time.Now`/`time.After`/`time.AfterFunc`。

## 1. 第 2 条：为什么逆拓扑序、为什么必须串行等完成

依赖边 A→B 读作"B 的存在依赖 A 的产出"：A 先建资源，B 在其上使用。补偿是执行的逆操作，
因此必须按执行的反方向、即逆拓扑序（Kahn 分层的层号从大到小，同层按拓扑索引降序，
保证 B 在 A 之前）。

必须等 B 的补偿**完成**再补偿 A，因为 B 的补偿动作可能需要 A 建立的资源仍然存在。
反例：A="建数据库表"，B="写入行/建外键子表"。若 A、B 的补偿并发：
- A 的补偿（DROP TABLE）先于 B 的补偿（DELETE 行）执行 → 删一张仍被子对象引用/仍含
  B 数据的表：外键约束报错，或 B 的数据变成孤儿泄漏；
- 更一般地，A 先释放资源后，B 的补偿句柄失效，B 补偿必然失败。
所以补偿全程串行、前一个返回（无论成败）才开始下一个；失败也记录后继续，最后聚合。

## 2. 第 5、6 条：崩溃点枚举与半条记录

每条记录写入有两个钩子点，钩子返回错误即"崩溃"：

1. **写前崩**：字节尚未追加，journal 形态完全不变。重放看不到该记录，状态停留在上一条
   记录之后——即该步骤停在 Executing（或上一阶段），恢复时重新执行该步骤
   （Executing 没有 Completed 记录，视为未完成，重跑合法；完成的步骤有 Completed 记录，绝不重跑）。
2. **写后崩（完整帧）**：帧字节已全部落"盘"。重放正常折叠，步骤精确推进一个阶段，
   恢复从下一阶段继续。
3. **写半崩**：钩子在追加中途截断缓冲（注入 torn 写），尾部为不完整帧：长度声明后字节
   不足，或 CRC 不符。重放顺序扫描，遇到第一个坏帧即停止并丢弃该帧及之后所有字节
   （崩溃后不会再有新帧，其后不可能有合法记录）。判定依据纯粹是长度/CRC/EOF，确定性、
   与内存无关；截断后前面的完整帧仍然自洽，状态等价于"写前崩"或"上一记录之后"。

恢复只实例化一个新编排器、把同一份 journal 字节重放，不读取任何旧内存；
`Replay` 是纯折叠，同一份字节重放任意次结果完全相同（幂等）。
测试遍历：构造一次完整运行，枚举出每条记录的写前、写后两个点，再对每条记录做写半截断，
逐点重放续跑，断言各步骤执行计数相对无崩溃基线不增、终态一致。

## 3. 第 9 条：线性重放与节点访问计数

重放策略：单遍扫描字节流，逐条 JSON 折叠进 `states map[string]Record`，O(L) 时间、
O(N) 空间，全程不访问图节点（计数器 `replayNodeTouches` 不增长）。
扫描结束后，编排器仅在分发恢复状态时对至多 N 个步骤各读一次图中层数，
故节点访问次数上界为 N，**与 L 无关**。
测试固定 N，用同一图重放 L=100 与 L=10000（长轨迹来自步骤的多次重试记录），
断言两次 `replayNodeTouches` 相等且等于 N，即不随 L 线性增长。

## 4. 第 8 条：只有两种对外终态与临界区

编排器内部维护可变的实时状态（含补偿中间态），另持一个已"发布"的不可变快照
 `published`，受同一把 `sync.RWMutex` 保护。

- 正常执行期：步骤进入/完成时在**短临界区**内更新实时状态并刷新快照
  （此时对外可见的是非终态的进行中状态，允许）。
- 终局失败一旦确定：编排器取消其他在途步骤，然后在**补偿大临界区**内依次执行全部补偿，
  期间实时状态可能出现"部分已补偿"，但快照只在两种时刻发布：全部补偿动作结束后
  一次性发布 `AllCompensated`（含每步最终态与聚合的补偿失败），或全部成功后发布
  `AllComplete`。查询全程走 RLock 读快照：补偿期间读到的是失败前最后一个非终态快照，
  绝不会读到混合终态。
- 因此对外终态只有 `AllComplete` / `AllCompensated` 两种；补偿失败聚合为
  `CompensationErrors`（步骤→错误），不改变终态种类。
- 补偿也写 journal（Compensating/Compensated），所以崩溃发生在补偿阶段同样可续跑：
  已 Compensated 的步骤不补第二次，满足补偿恰好一次。
