# DESIGN — CSV 解析器设计推导

方言：逗号分隔；记录以 `\n` 或 `\r\n` 结束；引号字段内 `""` 表示一个字面引号，字段内逗号/`\n`/`\r\n` 原样保留。

## 1. 空行与单列空值（第三节）

空行 = 一条「只含一个未加引号空字段」的记录，不跳过。理由：`\n\n` 与 `a\n\nb` 中间那一行若被跳过，则 `Parse(Write(T))` 无法还原它（writer 不知道它存在），违反第 5 条往返不变量；而「一个空字段」恰好是 lexer 在 FieldStart 状态直接遇到 `\n` 的自然产物，无需特判。

回写器推论：单列表中某条记录的唯一字段为空时，**必须**写成 `""` 而不是空行。因为规范形式每条记录以 `\n` 结尾（见下），若写成空行则解析回来得到「未加引号空字段」，引号标记丢失，违反「引号标记相同」。同理，任何带引号标记的字段（含 `""`）回写时必须带引号，否则标记丢失。

**规范输入定义**（`Write(Parse(x)) == x` 逐字节成立的 x）：每条记录以 `\n` 结尾（含最后一条）；未加引号字段不含 `, " \r \n`；字段仅当含这四者之一、或带引号标记、或为空且是记录唯一字段时才加引号；引号字段内 `"` 一律写成 `""`。空输入解析为空表，写回为空。
**规范输入定义**（`Write(Parse(x)) == x` 逐字节成立的 x）：每条记录以 `\n` 结尾（含最后一条）；未加引号字段不含 `, " \r \n`；字段仅当含这四者之一、或带引号标记、或为空且是记录唯一字段时才加引号；引号字段内 `"` 一律写成 `""`。空输入解析为空表，写回为空。

## 2. par 的切点问题（第三节）

切点落在引号字段内部时，下一段无法自知状态。方案：**双假设候选 + 顺序拼接**。段 i（i≥1）的 worker 用同一份共享只读 buf 解析两遍：候选 A 假设段首处于「字段开始」，候选 Q 假设段首处于「引号字段内部」（段首字节即内容）。每个候选记录：完整字段（值/引号标记/段内偏移）、记录结束事件、结尾状态、以及段首那个未闭合字段的「前缀」（Q 假设下段首属于上一段的字段）。段 0 起点状态已知（字段开始），只算一遍。

拼接器从左到右维护真实边界状态 S（含未闭合字段的已积累内容）：对边界 i 选候选 C =（S 在引号内 ? Q : A）。合并规则按 S 细分：字段开始→C 的前缀自立为新字段；未引号字段中→前缀追加到当前字段；引号字段中→前缀追加，引号标记继承；CR 待定→看段首字节：`\n` 则结束字段与记录并跳过该字节，否则报孤立 `\r` 错误；引号待定→看段首字节：`"` 则追加一个字面引号并回到引号内（跳过），`,`/`\n` 则闭合字段（Q 候选会多产一个空首字段，丢弃之），其他报「闭合后非分隔符」。每字节最多被 A、Q 各处理一次 ⇒ 总量 ≤ 2n。

坐标换算：worker 只产段内偏移，拼接器加段基址得全局字节偏移；记录号/字段号由拼接器在消费记录结束事件时统一分配，因此错误三元组与单线程一致。错误按全局偏移取最小者胜出（同偏移时先发生的候选优先）。

## 3. CR 待定状态（第三节）

未引号字段末尾见 `\r`：可能是 `\r\n` 行尾，也可能是孤立 `\r`（错误），必须看下一字节。半包续传：lexer 进入 CRPending 状态停住，`\r` 不计入字段值；下次 Feed 的首字节为 `\n` 则结束字段与记录，否则报孤立 `\r` 错误（偏移指向 `\r`）。`Close()` 时仍处于 CRPending ⇒ 按孤立 `\r` 报错。par 切点：边界状态为 CRPending 时按第 2 节的合并规则由拼接器用段首字节判定，等价于续传。

## 4. 上限的「立刻」（第三节）

引号字段内 `""` 计 1 个字面字节：lexer 在 QuotePending 状态见到第二个 `"` 时向值追加一个字节，此时值长度 +1，立即与 MaxFieldBytes 比较，超限即返回 ErrFieldTooLarge 并进入终态——不等待字段闭合，不缓冲整条记录。未引号字段每字节追加时同样即时检查。MaxFields 在开启本记录第 MaxFields+1 个字段时立刻拒绝；MaxRecords 在第 MaxRecords+1 条记录的第一个字段开始时立刻拒绝。超限后已产出的完整记录保留，后续 Feed 返回同一错误。
引号字段内 `""` 计 1 个字面字节：lexer 在 QuotePending 状态见到第二个 `"` 时向值追加一个字节，此时值长度 +1，立即与 MaxFieldBytes 比较，超限即返回 ErrFieldTooLarge 并进入终态——不等待字段闭合，不缓冲整条记录。未引号字段每字节追加时同样即时检查。MaxFields 在开启本记录第 MaxFields+1 个字段时立刻拒绝；MaxRecords 在第 MaxRecords+1 条记录的第一个字段开始时立刻拒绝。超限后已产出的完整记录保留，后续 Feed 返回同一错误。

## 状态转移表

| 状态 \ 输入 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| FieldStart | 产出空字段 | →InQuoted | 产出空字段+记录结束 | →CRPending | →InUnquoted |
| InUnquoted | 产出字段 | 错：未引号字段含引号 | 产出字段+记录结束 | →CRPending | 追加 |
| InQuoted | 追加 | →QuotePending | 追加 | 追加 | 追加 |
| QuotePending | 产出字段 | 追加字面引号→InQuoted | 产出字段+记录结束 | 错：闭合后非分隔符 | 错：闭合后非分隔符 |
| CRPending | 错：孤立`\r` | 错：孤立`\r` | 产出字段+记录结束→FieldStart | 错：孤立`\r` | 错：孤立`\r` |

EOF：FieldStart→正常结束；InUnquoted→产出字段+记录结束；InQuoted→错：引号未闭合；QuotePending→产出字段+记录结束；CRPending→错：孤立`\r`。

## 其他约定

- 单实例非并发安全；par 内部 K 个 goroutine 只读共享 buf，结果经 channel 汇总，无共享可写状态。
- 空输入 → 空表；列数以第一条记录为准，后续不一致报 ErrColumnCount。
