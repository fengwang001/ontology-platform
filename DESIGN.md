# DESIGN：只追加日志的段索引与范围回放

## 1. 盘上格式

- 段文件 `seg-NNNN.log`：32 字节固定头 + 逐事件帧。
- 头（32B）：魔数 `ONTLSEG1`(8)、版本 u32be、首序号 u64be、事件数 u32be、CRC32(头其余 28B, u32be)。事件数在追加中滞后，权威值以扫描为准。
- 帧 = 载荷长度 u32be + 载荷 + CRC32(长度与载荷, u32be)，长度前缀允许 0（空载荷合法）。
- 索引 `seg-NNNN.idx`：魔数 `ONTLIDX1`(8) + 版本 u32be + 间隔 N u32be + 锚点记录；锚点 = 序号 u64be + 字节偏移 u64be（16B，定长）。首事件必为锚点，之后每 N 条一个。
- 锚点偏移直接来自段文件帧起点，不包含任何只在写入时可知的状态。

## 2. 定位规则推导：为何取「不大于 from 的最大锚点」

锚点只覆盖它自己及其后的事件，顺序扫描只能向后推进。设锚点序号为 a：

- 若选「最接近 from 的锚点」且 a > from，从 a 开始扫描会永久漏掉 [from, a) 的事件，且没有任何手段倒回。
- 若 a <= from，从 a 所在字节顺序读，跳过 seq < from 的事件即可精确到达 from。
- 因此唯一合法选择是 floor anchor：`max{a | a <= from}`。
- 若 from <= 首锚点序号（首事件），floor anchor 即首锚点，从头扫描；from < 最小序号不报错，等价于从最小序号开始（边界语义见 §6）。
- 用上索引后跳过事件数 = from - a ∈ [0, N-1]，严格小于 N。

三种 from 位置：from 恰为锚点（跳过 0）、两锚点之间（跳过 1..N-1）、from <= 首锚点（从头，跳过 0，仍命中首锚点）。

## 3. 索引可丢弃、可逐字节重建

重建只需顺序扫描段文件：从头开始，每读一帧，`frameIndex % N == 0` 时记录 `(seq, 帧起始偏移)`。
理由：序号在帧载荷内（event 自描述：seq u64be + payload），偏移是扫描位置，间隔 N 存于索引头并可在重建时沿用；三者都来自段文件本身与确定性规则。
故重建产物与原索引逐字节相同（魔数、版本、N、记录顺序与内容全部确定）。索引不含时间戳、PID 之类写入期信息。

## 4. 段边界与跨段回放

段头记录首序号 first 与事件数 count，段尾序号 = first+count-1。打开目录按文件名排序，断言相邻段
`prev.first + prev.count == cur.first`，否则报告缺口 `[prev.first+prev.count, cur.first-1]`。
回放按段顺序接力，用 `[from,to]` 与每段实际序号区间相交，天然不重不漏。

## 5. 索引失效与回退

定位到锚点偏移后必须校验：该偏移处应解出长度前缀、帧体且 CRC 正确、且首帧序号等于锚点序号。
任一不成立（典型：偏移被改到帧中间）即判定索引损坏：丢弃锚点、从段头后做全段顺序扫描定位，
结果仍正确，报告标记 indexStale。故障用哨兵错误区分，可 `errors.Is`：
`ErrHeaderIncomplete / ErrLenPrefixIncomplete / ErrBodyIncomplete / ErrCRC`；
另有 `ErrInvalidRange`（from>to）、`ErrSeqGap`（序号不连续）、`ErrIndexStale`（索引失效标注）。

截断分类（L = 32 + Σ frameLen，截断到 t 字节）：

- t < 32：头部不完整。
- 32 <= t < L：按帧游标，t 落在帧前 4B 内 → 长度前缀不完整；落在后续但不足整帧 → 事件体不完整（CRC 缺失也属此类）；帧完整但 CRC 值不符（需翻转字节，纯截断不会产生）→ CRC 不匹配。
- t 恰为完整边界：正常 EOF，无错误。

repair 截断段：扫描至最大可恢复前缀，把头里事件数改成实际条数，截去残尾；修正后顺序读不再报错。

## 6. 边界语义

空日志（0 段或 0 事件）：回放得 0 事件；单事件正常；`from > to` → ErrInvalidRange；
`from < minSeq` → 从 minSeq 开始（不报错）；`to > maxSeq` → 放到段尾（不报错）；空载荷合法。

## 7. 复杂度与并发

定位跳过事件数 < N（计数器 `skipped`，非导出）。读取字节计数 `bytesRead` 仅来自起点段锚点帧到终点段末帧：
区间内事件编码总长 + 至多一个锚点间隔（N 帧）的前导。
并发：写者经 *os.File 追加，整帧写就后才对读者可见；读者打开独立只读句柄扫描一致前缀，
半帧/残尾统一表现为可判定截断错误并被回放当作段尾处理，故读者只见连续前缀。-race 须干净。

## 8. 包划分

event（帧编解码）、segment（头/追加读/截断分类/修复）、sparse（索引读写/floor 查找/重建）、
replay（范围定位回放+计数器）、repair（目录校验/缺口/索引失效回退/重建）、cmd/demo（判定演示）。
