# 多租户配额公平队列调度器设计

仅用标准库；调度状态存进程内存；时钟通过 `tenant.Clock` 接口注入（测试用步进时钟）。

## 包划分

- `task`：任务值（租户 ID、代价、提交序号、提交时刻）。
- `tenant`：租户记录：FIFO 队列、权重、虚拟时间账本、空闲重入抬升。
- `sched`：以最小堆按 `(vt, id)` 选择非空租户；互斥锁保护；可选 `Cond` 等待。
- `admit`：在 `sched.Scheduler` 之上做每租户队列上限、阻塞式提交、移除租户数语义。
- `stat`：份额统计与公平性度量（实际代价占比、相对偏差、连续独占步数）。
- `cmd/demo`：九项验收判定。

## 1. 为什么 vt 增量是 c/权重

租户 t 每执行代价 c，`vt_t += c/w_t`；调度器总选 vt 最小者。

设两租户权重 w1=1、w2=3，长期被服务的代价为 C1、C2。公平要求二者 vt 交替领先且
终值相近：`C1/w1 ≈ C2/w2`，即 `C1:C2 = w1:w2 = 1:3`。服务量与权重成正比，正是
加权公平份额。若改成 `c*w`，则 `C1*w1 ≈ C2*w2`，得到 `C1:C2 = w2:w1 = 3:1`，
份额与权重成反比，权重越大反而拿得越少，语义颠倒。故必须用除法。

## 2. 空闲租户重新加入：vt 抬升

租户从空变非空时（首次到达或队列耗尽后再次到达），令
`vt = max(vt, 系统当前最小 vt)`，即抬到「当前系统虚拟时间」。

不抬升：X 空闲期间 Y 执行 1000 个等代价任务（w=1），Y 的 vt 前进 1000，而 X 的 vt
停在旧值，落后 1000。X 重新提交一个任务后，因 vt 最小会连续中选；每执行一步 X 追
1、Y 停 1，需要约 1000 步（若 X 任务足够多）才能追平——这期间 X 独占、Y 被饿死。
抬升后 X 的 vt 与 Y 平齐，按 `(vt,id)` 平局规则二者交替，X 至多 1 步即被服务，且
Y 立即继续获得份额。正在被服务的活跃租户不抬升（其 vt 本就在推进）。系统无活跃
租户（全空）时基准取 0。

## 3. 同 vt 并列

堆比较键为 `(vt, tenantID)`：vt 小者优先；vt 相等时 ID 字典序小者优先。浮点严格用
`==` 比较即可（同值任务经相同运算得到相同位模式），保证序列逐元素可复现。

## 4. 零代价任务

令虚拟推进 `d = c/w`，当 `d < minVCost` 时取 `d = minVCost`（实现里 `minVCost=1e-2`，
对应正常代价 1 的 1/100）。零代价任务因此仍令 vt 缓慢前进，最多连续中选
约 `1/minVCost = 100` 次后必然让给同权租户，不会无限独占；实际记账代价仍记 0。

## 5. 权重与数值

vt 用 float64：1e5 次调度、正常代价下 vt 量级约 1e5，离 1e308 极远；增量是除法，
权重越大增量越小，不会溢出。权重必须为正且有限；`w<=0`、NaN、Inf 一律拒绝
（ErrBadWeight）。另设实用安全区间 [1e-9,1e9]：超出时报 ErrBadWeight，从源头杜绝
除出 1e308 量级增量后再累加导致的精度/溢出问题。极大（1e9）与极小（1e-9）并存时，
两者 vt 增量分别为 1e-9 与 1e9，均为有限值且可比较。

## 6. 背压与移除

四类哨兵错误，`errors.Is` 可判：`ErrQueueFull`（该租户达上限，其他租户不受影响）、
`ErrBadWeight`、`ErrUnknownTenant`（提交/取任务于未注册或已移除租户）、
`ErrExists`。移除租户：其排队任务不丢弃也不重投，随 `Remove` 返回给调用方并从调度
器注销；移除后对该 ID 的提交报 ErrUnknownTenant（重新注册需显式 Add）。

## 7. 并发

sched 内单个 `sync.Mutex` 保护租户 map 与堆，`sync.Cond` 支撑阻塞等待/唤醒；入队与
出队串行化点都在锁内，故每个任务恰好入堆一次、出堆一次，不丢不重。
并发提交时「提交顺序」= 各提交 goroutine 获取入队锁的串行化顺序；同批相同
`(vt,id)` 再用任务提交序号兜底 FIFO。`-race` 干净。

## 8. 复杂度

堆中只保留非空租户：入队（空→非空）一次 push，出队后仍非空则 Fix、变空则 Remove。
单次选择 = pop（≤⌈log2 n⌉ 次比较）+ 必要时 Fix（同量级），比较次数计数；
n=1000 时断言 < `4*ceil(log2 1000)` = 40。空闲租户不在堆中，不占选择代价。
