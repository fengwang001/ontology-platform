# DESIGN — RFC4180 方言 CSV 解析器

## 1. 空行与单列空值（推导）
记录由换行界定。`a\n\nb` 中两条换行夹住零字节：若把空行当记录，单列空值无法与「排版空行」区分，
且空行不含任何逗号，不是列结构的一部分。**选择：空行一律跳过**（不产生记录），无论单列还是多列；
判定：换行到达时本记录尚未产生任何字段，则丢弃。代价：单列「值为空的记录」若写成裸空（`\n`）会被
当空行丢弃。**故 writer 必须把单列空值写成 `""`**：加引号后字段存在，记录不再是空行；反之裸空
`""` 往返后引号标记丢失，不逐字节相等。多列空字段无需引号（逗号已证明字段存在）。

## 2. par 切点（推导）
切点可在任意字节，下一段 worker 无法自证是否处于引号内。令每段对首字节做**两种假设**各解析一遍：
H0=引号外、H1=引号内。段间只需左段末尾输出二值状态 Q（末字节处理后是否在引号内）：段 i 真实起点
在引号内 ⇔ 段 i−1 结束在引号内（引号态只能由段内字节改变）。故段 0 用 H0，此后按前段 Q 选 H0/H1，
结果唯一确定。裸字段被切断时 H0 首字段带 `cont=true`，驱动者挂到前段挂起字段并丢弃其内容副本；
H1 不产生新记录。每段仅算 2 遍，总处理次数 ≤ 2N+常数。段内错误偏移加段起点；记录号加段前确认
记录数；含错误段回退全局顺序解析定位（错误路径，不计入复杂度）。

## 3. CR 待定（推导）
裸字段中 `\r` 仅当其后为 `\n` 时是行尾，否则孤立 CR 错误；故见到 `\r` 进 C 态，不判定。续传：
`\r` 可落在某次 `Feed` 末尾，状态保留，下字节 `\n`→行尾、其它→`ErrLoneCR`；`Close` 仍挂起→
`ErrLoneCR`。par：`\r\n` 被切开时左段只输出「CR 挂起」标志（与 Q 并列），右段首字节 `\n` 则按
行尾并入、否则在切点偏移记 `ErrLoneCR`。引号内 `\r` 是普通内容，不挂起。

## 4. 上限「立刻」（推导）
字段按**解码后字符**计数：裸字节各计 1；引号内普通字节计 1，`""` 在「见引号后再见引号」时折叠为
1 个引号（第二字节不计数）。每字节处理同时累加比较，超限即在该字节偏移返回 `ErrFieldTooLong`。
字段数在字段闭合时计；记录数在记录闭合时计；同样立即比较。错误粘滞：解析器进终态，后续 Feed/Close
返回同一错误。

## 5. 状态转移表
状态：F=字段首(未引号) B=裸字段中 Q=引号字段中 A=引号中刚见引号 C=行尾CR待定。
输入：`,` `"` `\n` `\r` 其它 o。动作：emit=发字段；rec=发记录；skip=空行丢弃。

| 状态 | `,` | `"` | `\n` | `\r` | o |
|---|---|---|---|---|---|
| F | emit,→F | →Q | 空行→F(skip) | →C | →B |
| B | emit,→F | ErrBareQuote | emit+rec,→F | →C | →B |
| Q | Q | →A | Q | Q | Q |
| A | emit,→F | →Q(折叠"") | emit+rec,→F | emit+rec,→F | ErrQuoteClosed |
| C | ErrLoneCR(偏移=\r) | 同左 | emit+rec,→F | —(保留) | ErrLoneCR |

EOF：Q→ErrUnclosedQuote；C→ErrLoneCR；F/B/A 有字段在途→发末记录（A 先 emit）。
字段偏移 `[start,end)` 含引号本身；空字段 start=end=所在字节偏移（`""` 取开引号位置）。

## 6. 规范输入（canonical）
`\n` 行尾、无空行；必需引号才加（含 `,` `"` `\r` `\n`，或单列唯一空字段）；`""` 仅出现在必需处。
并发：单流式解析器实例非并发安全；`par.Parse` 内部 goroutine 间仅交接不可变段结果，无共享写。
