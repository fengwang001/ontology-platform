# DESIGN：带路径语义的通配模式

路径与模式均按 `/` 切成段；不做任何规范化，`""` 切成一个空段 `[""]`，`"x/"` 切成 `["x",""]`。

## 1. `x/**`、`**/x` 的边界推导

唯一规则：`**` 整段消费连续的 **k≥0 个整段**（空段也是段），k 可为 0，位置可在首/中/尾。
- `x/**` 对 `x`：`**` 取 k=0，命中；对 `x/`：k=1（消费尾空段），命中；对 `x/y`：k=1，命中。
- `**/x` 对 `x`：k=0，命中；对 `/x`：路径为 `["","x"]`，k=1，命中。
- `a/**/b` 对 `a/b`：中间 `**` 取 k=0。同一规则的第三个位置，无需特判。

## 2. 空串路径推导

`""` 是「一个零码点的空段」。段内 `*` 匹配同段 0 个或多个码点，故匹配 0 个 → `*` 命中 `""`。
`**` 消费 k≥0 个整段，对 `[""]` 取 k=1 → `**` 命中 `""`。`?` 恰好要 1 个码点，空段没有 → 不命中。
空模式为「零原子的一个空段」，段匹配要求路径段也为空，故只匹配空串。

## 3. 无回溯算法

段内：线性通配扫描，两个指针加「最后一个 `*` 的位置与其消费到的文本位置」。失败时只回退到最后一个
`*`，令它多吃 1 个码点再试。正确性不变量：模式前缀 `Q*` 已匹配文本前缀 `T[0:m]` 时，它能匹配
**任意** `T[0:m+k]`（k≥0，星号吸收）。因此整体命中 ⟺ 最后一个 `*` 之后的「无星后缀」在某个
≥m 的起始位置命中；算法按 m,m+1,… 逐个尝试，无遗漏，且每轮必有一个文本位置前进，总步数 O(模式+路径)。
遇到新的 `*` 时建立更强的同形不变量，旧星记录被覆盖：旧星多吃只可能把后续整体右移，仍落在新星的
尝试区间内，故无需保留旧回退点。

该论证不能直接照搬到 `**`：段内模型中被吸收者是同类原子（码点），前缀延长 O(1)；而 `**` 吸收的单位
与分隔符耦合——取 k=0 时 `a/**/b` 中的 `/**/` 坍缩为 `/`，文本不是「`**` 可吸收原子组成的平坦
序列」，平坦序列上的位置/回退点无法表达「零段且分隔符消失」；且一次「前进一格」是一次变长子匹配。
故跨段改为段级 NFA 状态集合：状态为模式段下标，按路径段逐段推进；`**` 段既可停留（消费一段）也可
零段跳过（闭包）。每段对每个状态至多尝试一次段匹配，O(模式段×路径段) 次段匹配，段内线性，
总体 O(模式长度×路径长度)，天然处理零段与连续空段。

## 4. 规则集版本一致

当前规则集整体存于 `atomic.Pointer[version]`；查询开始时只读一次指针，结论、`Explain` 的序号/原文、
版本号全部取自同一快照。替换先在普通变量中编译全部规则，任一失败立即返回错误、不写指针 → 旧版本
原样保留；全部成功后加互斥锁（串行化替换者并分配单调版本号）换入新快照。

## 5. 结果表（✓ 命中 / ✗ 不命中）

| 模式 \\ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | ✗ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ |
| `**/x` | ✗ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ |
| `*` | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| `**` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `a/**/b` | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |

## 6. 错误与上限

四类可判定错误（带模式字节偏移）：未闭合类 `class.KindUnclosed`、空类 `class.KindEmpty`（`[]` 首
`]` 后再无闭合 `]`；`[]a]` 的首 `]` 为字面量）、颠倒区间 `class.KindReversed`、末尾单反斜杠
`syntax.ErrTrailingEscape`。超限错误：模式字节数、`**` 段数、规则条数，零值用默认上限。
