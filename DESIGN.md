# DESIGN: Ontology 对象图遍历器

模块：`graph`（有向图 + 三色环检测）、`traverse`（遍历序）、`bounded`（深度/数量截断三态）、`api`（唯一对外入口）。

## 遍历序选择：BFS

- 可选口径 A：DFS。深度是「路径依赖」的——同一节点沿不同路径首次到达的深度不同，
  `MaxDepth(d)` 的语义随遍历顺序漂移；且配合 Limit 时截断点不可解释。
- 可选口径 B：BFS。首次到达深度 == 最短距离，`MaxDepth(d)` 精确等于「最短距离 <= d」，
  与去重（每节点只输出一次）天然兼容，截断点按层推进、可解释。
- 最终选择：**BFS**。三个方向 `out/in/both` 仅改变邻居集合（both = 出 ∪ 入，同层内去重）。

## 推导 1：环检测的三色标记（灰 vs 黑）

- 可选口径 A：DFS 中「遇到任何已访问节点（灰或黑）即报环」。
  后果：菱形依赖 A->B、A->C、B->D、C->D 中，D 先经 B 完成（黑），再经 C 到达时已黑，
  被误判为环。即把「被两条路径到达」误报成「环」，误报率随扇入上升。
- 可选口径 B：仅当边指向**灰**节点（在当前递归栈上）才报环；指向**黑**节点（已在别的
  路径彻底完成）不报。灰->灰 边意味着存在从栈上节点回到栈上祖先的路径，即真环；
  黑节点不可能再参与新环（若它与当前路径成环，它在完成前就该被发现）。
  后果：菱形返回 false，真环 A->B->C->A 返回 true，且每条边至多被考察常数次。
- 最终选择：**口径 B**。白=未访问、灰=在栈上、黑=已完成；`HasCycle` 对全图每个白色
  节点启动 DFS（覆盖非连通图）。
- 复杂度不变量：非导出计数器 `edgeExams` 记录边考察次数，每次调用清零；一次完整
  运行每条边恰好考察 1 次（提前命中环则更少），断言 `edgeExams <= V + E`，
  对 V=100 与 V=10000 两档在 `graph` 包内测试中验证。

## 推导 2：limit 截断与环的交互

- 可选口径 A：「已输出节点数 == n」即记 `LimitCut`。
  后果：可达节点恰好 == n 时，遍历其实自然走完，却被误报为截断——把
  `Complete` 误报成 `LimitCut`。
- 可选口径 B：输出满 n 个即停止输出，此时检查「遍历中实际遇到（已发现入队）
  但未输出的节点数」：> 0 记 `LimitCut`，== 0 说明恰好走完，记 `Complete`。
  后果：恰好 == n 判定正确；截断点落在环上时（环把 limit 占满），环上尚未输出
  的节点已在 BFS 队列中，被计入「未展开」，正确报 `LimitCut`。
- 最终选择：**口径 B**。判定依据是停止时刻的队列剩余，而非输出计数。
- 不变量：`输出节点数 + 未展开节点数 == 从起始可达的节点数`，环上每节点只贡献
  一次。为此 `LimitCut` 停止输出后，仍以只读方式继续扩散（不再输出、不再受
  depth/limit 约束）统计剩余可达节点，保证等式在三种终止态下都成立。
  `bounded` 以非导出字段 `out`、`unexpanded` 记录，包内测试断言该等式。

## 推导 3：深度上限与环的交互

- 可选口径 A：不去重，节点每到达一次就输出一次。
  后果：环 A->B->C->A 上 A 在深度 0、3、6... 无限出现，输出发散；即使加深度上限，
  同一节点重复输出也违反「对象集合」语义。
- 可选口径 B：节点**只输出一次**（按首次到达深度，BFS 下即最短距离）；深度语义为
  「最短距离 <= d 的节点都要被访问到」。发现新节点时若其深度 > d，计入「未展开」
  且不再入队（BFS 首次发现即最短深度，故不存在「以后会更浅到达」）。
  后果：d >= 环上最长最短距离时环被完整走完，记 `Complete`，不无限循环；
  d 更小时，深度 d 处仍有未展开的边（有节点因深度被剪），记 `DepthCut`；
  深度 d 处无未展开边（被剪的边只指向已访问节点）则仍为 `Complete`。
- 最终选择：**口径 B**。判定依据是「深度 d 处是否还有未展开的边」，而非「图里
  是否存在环」——环本身不是截断理由。

## 截断三态的表示

- 可选口径 A：自定义枚举 + 字符串比较。后果：调用方无法用语义化方式判定，易退化成
  字符串匹配。
- 可选口径 B：哨兵错误。`Complete = nil`，`DepthCut = ErrDepthCut`，
  `LimitCut = ErrLimitCut`，调用方用 `errors.Is` 区分。
- 最终选择：**口径 B**。三态互斥由构造保证：`LimitCut` 只在「输出满 n 且队列非空」
  的分支返回；`DepthCut` 只在自然走完后按未展开计数判定；两条路径不交叉。

## MaxDepth / Limit <= 0 的语义（api 参数校验）

- 可选口径 A：<= 0 一律报错。后果：零值 `Request{}` 不可用，调用方必须显式填两个
  上限才能做「不限」遍历。
- 可选口径 B：**0 = 不施加该约束**（零值可用），**负数 = 非法参数**报错。
- 最终选择：**口径 B**。起始节点不存在、非法 dir、MaxDepth/Limit < 0 均返回参数
  错误；`api.Traverse` 是唯一对外入口。
