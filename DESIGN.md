# 多版本可见性存储：读视图与增量回收 — 设计推导

## 1. 可见性规则（需求 4）的推导

快照隔离要求读事务在开始点 `P` 之后与整个系统"冻结"：重复读必须得到同一答案。
设版本 `v` 由事务 `t(v)` 提交。冻结语义给出两个直觉条件：

1. `t(v)` 必须在快照建立**之前**已完成提交，否则快照期间 `v` 会"凭空出现"，破坏可重复读。
  - 事务号单调且在提交点获得（见 `txid`：下一号在提交时才取），因此"提交时刻 < 建快照时刻"等价于 `t(v) < P`。
  - 取闭区间 `t(v) <= P` 是错的：`P` 是建快照那一瞬间"下一个将被发出的号"，与建快照同临界区发出的提交号恰好等于 `P`，它在逻辑时刻上与快照**同时或之后**。故边界必须**左闭右开** `[0, P)`，`t(v) == P` 不可见。
2. 建快照时仍活跃（未提交）的事务集合 `A` 里的号，即使数值 `< P`，其提交结果也不得进入本快照（否则就是读未提交）。

两条件合并：`v` 对快照 `S=(P,A)` 可见 ⇔ `committed(v) && t(v) < P && t(v) ∉ A`。
同一版本链按从事务号大到小扫描，取第一个满足条件的版本：它就是该快照下键的当前值（可能是删除标记）。

读己之写是唯一例外：事务号为 `self` 的未提交版本，对 `self` 自己可见，对 `P,A` 判定不参与的任何其他快照都不可见；回滚时整条事务的版本从链上摘除，于是没有任何残留可被回收器或扫描误判。

## 2. 回收安全条件与增量策略（需求 5、6）

设当前所有活跃快照的建点最小值为 `W = min P`（无快照时取 `+∞`，并由 store 保持水位只升不降）。
任何现存快照可见的版本，其 `t` 必 `< W`（注意不是 `<=`，呼应第 1 节的开区间）。
因此版本 `v` 可回收当且仅当：

- **遮蔽**：同键存在更新版本 `u`（`t(u) > t(v)`）已提交，且 `t(u) < W`。此时对每个现存快照，扫描都会先命中 `u`（或更右的版本），`v` 永不可能被选中；删除标记同样按此规则被它右侧版本遮蔽。
- 未提交版本只在事务结束（提交/回滚）后处理：提交后按上条判定；回滚则直接摘除，绝不回收成"可见历史"。

**增量数据结构**：回收器维护一个以"屏障号"`b = 链上右数第二个已提交版本的 t` 为键的最小堆，只登记**发生过追加**的键。
推进时给出新水位 `W'`，反复弹出 `b < W'` 的键并实际考察该键；考察后若链仍长，用新的 `b` 重新入堆。
没有可回收版本的键其 `b >= W'`，永不出堆，因此一次推进只触碰真正有版本可回收的键，**不遍历全部键**。
考察数上界：`O(本轮弹出键上被审视的版本总数)`，与"有回收机会的键的版本数"同阶，与总键数 `N` 无关；
`N=100` 与 `N=10000` 且只有少量键多版本时，两次考察数相等即证明不随 `N` 线性增长（计数器 `examined` 为非导出字段）。

## 3. 写入崩溃点枚举（需求 8）

一次写提交在逻辑上分为四段，钩子可在每段之后注入崩溃：

- **P0（取号前）**：什么都没写。重启后只有未完成写者，恢复 = 回滚，事务完全不可见。
- **P1（版本入槽后、索引前）**：槽位存在但没有任何索引/链指向它，且事务号尚未发出。重启恢复时按"写者登记"找到孤儿槽并丢弃。槽位不可达 ⇒ 完全不可见。
- **P2（链/索引挂上后、提交前）**：链上节点 `committed=false`、不在任何快照的 `[0,P)` 已提交集合中，且写者仍登记为活跃。重启恢复将其按未提交事务整条摘除。其他快照看不到，自己也随重启死亡 ⇒ 无半可见，不会出现"索引指向无体槽位"（P1 已证明反向也不可能）。
- **P3（提交号已发出并置 committed 后）**：事务号与提交标志在同一临界区内发布；恢复看到的是完整已提交版本 ⇒ 完全可见。

关键不变量：**槽位与索引的挂接在同一互斥临界区完成**，崩溃只会发生在临界区之外的注入点，故"索引指向空槽"与"有槽无索引"都不会成为持久中间态；恢复以写者登记表为权威，未提交即回滚。测试用循环遍历全部 4 个点。

## 4. 水位推进与新快照的竞态（需求 7）

store 用一把互斥锁串行化三件事：建快照、提交、回收推进。临界区边界如下：

- **建快照**：在锁内读取"已发出的最大号"得 `P`，拷贝当前活跃写者号集合得 `A`，登记快照，然后才释放锁。锁外任何提交号都 `>= P`，由开区间规则天然不可见。
- **推进回收**：锁内先重算 `W = min(活跃快照的 P)`（无快照为 `+∞`），且对外水位取 `max(old, W)` 保证**只升不降**；再以该水位调用回收器弹出堆、删版本，全程持锁。

两种交错都被排除：
- 快照先建：它已在活跃集合中，`W <= P`，等于 `P` 的号不 `< W`，其可能看到的版本不会被回收。
- 回收先推：版本已删，但后建快照的 `P` 更大，按可见性规则它本就只应看到遮蔽该旧版本的新版本，读结果与"旧版本还在"时逐字节一致。

故"刚开始的快照读到已回收版本"不可能发生：要么它建在回收前并参与压低 `W`，要么建在回收后而从规则上不需要那些版本。
