# 设计推导

## 1. UTF-8 非法单元切分规则

解码按「最长合法前缀」贪心：从首字节进入，后续字节必须落在该位置合法区间。
- 续字节 `80..BF` 在位置1却不合法（E0/ED/F0/F4 特殊区间）：只吞首字节，该续字节退回重新解码。非法性由前两字节即可判定（非最短/代理/越界）。
- 非续字节出现在需要续字节的位置：已缓存的全部合法前缀构成一个非法单元，该字节退回重新解码。
- 流结束残留合法前缀：整个前缀是一个截断单元，输出一个 FFFD。

样例：`F0 90 80 41` 中 90/80 均在合法区间，41 不是续字节 → 三字节前缀整体一个单元、41 重解析；`E0 80 80` 中 80 不满足 E0 要求的 A0..BF → E0 一个单元，80 退回，两个孤立续字节各一个，共三个；`ED A0 80` 同理（A0 暴露代理）；`C0 AF` 中 C0 永不合法，两个单元；`F4 90 80 80` 中 90 越界，F4 一个+三个孤立续字节；`E2 82`+EOF 是缺第 3 字节的合法前缀，整吞为一个截断单元；`80 80` 是两个孤立续字节。

## 2. 首字节 → 第二字节区间表

| 首字节 | 字节2区间 | 字节2非法吞掉 | 后续位置非法吞掉 |
|---|---|---|---|
| C2..DF | 80..BF | 1，字节2退回 | — |
| E0 | A0..BF | 1，字节2退回 | 前缀全吞，违例字节退回 |
| E1..EC | 80..BF | 1，字节2退回 | 前缀全吞，违例字节退回 |
| ED | 80..9F | 1，字节2退回（A0..BF 为代理） | 前缀全吞，违例字节退回 |
| EE..EF | 80..BF | 1，字节2退回 | 前缀全吞，违例字节退回 |
| F0 | 90..BF | 1，字节2退回（80..8F 非最短） | 前缀全吞，违例字节退回 |
| F1..F3 | 80..BF | 1，字节2退回 | 前缀全吞，违例字节退回 |
| F4 | 80..8F | 1，字节2退回（90..BF 越界） | 前缀全吞，违例字节退回 |
| 80..BF | 无（孤立续字节） | 1 | — |
| C0 C1 F5..FF | 无（永不合法） | 1，下字节正常解析 | — |

位置 3/4 统一要求 80..BF；EOF 处合法前缀整吞。

## 3. 切分缓存上限与提前判定

最长序列 4 字节，解码器只保存「已验证前缀」（首字节+至多 2 个续字节），缓存硬上限 3 字节。字节 2 落在非法区间、或任意位置遇到非续字节时立刻判非法，绝不等待 EOF；只有各字节均合法但序列未完成才跨 Write 等待，Close 时按截断处理。因此切分点落在合法多字节字符中间时不会提前输出 FFFD。

## 4. par 切点对齐

段 i 原始区间 [raw[i], raw[i+1])。除段 0 外，从候选起点 S 向回看至多 3 字节：遇到 80..BF 继续；遇到首字节 L（长度 need）时，若 L 与 S 之间续字节数 < need-1，则该序列跨切点，本段真正起点改为 L；否则起点即 S；遇到 ASCII/非法首字节停止，起点即 S。a[0]=0，a[K]=n 并单调夹紧。段 i 实际只处理 [a[i], a[i+1])：跨切点的前缀整个交给右段，左段裁掉这些字节（不产生截断 FFFD），故合法字符跨切点仍拼成原字符；最后一段在 EOF 才可能截断。`C3|80 80`：右段回看发现 C3 前续字节数 0 < need-1=1，认领 C3，左段为空；右段解析 C3 80 成字符、80 为非法，与单线程「字符+FFFD」逐字节相同。`E2 82|`（右段为空，S=n）不认领，左段 Close 输出一个截断 FFFD。回看常数 ≤3，检查次数 ≤ N+8·常数。UTF-16 同理：奇数起点回退 1 字节对齐代码单元；若回退后的 2 字节是高代理且右段非空，再回退 2 字节（总回看 ≤3）。

## 5. 输出上限断点

每个完整标量输出前检查长度；会越限时把解码器回滚到处理该字节前的快照（已验证前缀原样保留），返回 LimitError{Prefix: 解码器前缀 + 未处理输入}，n 只计已输出标量覆盖的字节；缓存前缀不算已消费。调用方用新实例以 Prefix 续传，拼接与不限长一致；UTF-16 代理对整体检查。

## 6. 检查次数

每个物理字节进入解码器计数一次；被退回重解析的字节来自解码器内部状态，不重读物理字节。故流式检查数 ≤ N+常数；par ≤ N+8·常数。
