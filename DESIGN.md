# 设计推导（UTF-8⇄UTF-16 流式转码，WHATWG 风格替换）

## 1. 非法单元切分（七个样例的统一规则）

读首字节 L，知道需要续接字节数 n（L∈C2..DF→1，E0..EF→2，F0..F4→3）。依次检查后续字节：
- 第二字节越界（见第 5 节）：**只吞 L 这一个字节**，该字节即一个非法单元，其后字节全部重新当首字节解析。
- 第二字节合法、但第 k(k≥3) 个续接字节不是 80..BF：吞掉「L 加已读的合法续接字节」（2..4 个），非法续接字节退回重解析。
- L 本身非法（80..BF / C0 C1 / F5..FF）：单独一个非法单元。

`F0 90 80 41`：F0 第二字节合法区间是 90..BF，90 合法；第三字节 80 合法；第 4 字节 41 非续接，吞 F0 90 80 三个→1 个 FFFD，41 重解析。`E0 80 80`：E0 第二字节须 A0..BF，80 越界→只吞 E0；两个 80 各自孤立→共 3 个。`ED A0 80`（A0 在 ED 的 80..9F 之外）、`C0 AF`（C0 非法）、`F4 90 80 80`（F4 第二字节须 80..8F，90 越界→只吞 F4，90/80/80 各孤立）同理。`E2 82` 遇流结束：两个字节组成「未完成的合法前缀」，整体算一个截断单元→1 个 FFFD，故不是两个。

## 2. 缓存上限与何时立即放弃等待

增量解码器只缓存「未完成的合法前缀」：最长的合法首字节 F0..F4 需要 3 个续接，故缓存硬上限 **3 字节**（UTF-16 为 1 个 code unit 或 1 个奇字节）。看到第二字节越界（E0 收到 80..9F、ED 收到 A0..BF、F0 收到 80..8F、F4 收到 90..BF 等）即可立刻判 L 非法并退回当前字节，**不必等流结束**；只有「续接字节一直合法却没凑齐」才需缓存到 Close。

## 3. par 切点对齐

段 i 的首字节绝对偏移为 cut。在它前面取至多 3 个字节（u8 用 `TailLen` 精确取「已是续接尾的字节数+1」，u16 取 1 个 code unit）作 priming 重扫；流式子解码器是移位寄存器，喂入这段前缀后状态必与原流到达 cut 时一致。事件携带**起始绝对偏移**：只保留 `[cut, nextCut)` 内起始的单元事件。上一段在 cut 右侧没有任何残留需要传递——cut 处未完成的前缀由下一段重扫重建。`C3|80 80`：下一段 priming 取 `C3 80`，事件起始于 cut-1 被丢弃，但第二个 80 作为孤立续接（起始=cut+1）被保留，故只产生 1 个新非法单元，与单线程「C3 80 合成字符、80 孤立」完全相同。截断判定只有最后一段在全局 EOF 做。检查次数 = 每字节至多 1 次正扫 + 每段至多 3 字节回看 ≤ n+8×3。

## 4. 第 9 条断点

超限在输出一个完整标量之前判定：已完整输出的标量保留，半个代理对/半个字符绝不写出。`Write` 返回的 n 是**已被完整单元消费**的输入字节；停在某合法单元之后、下一单元（含缓存续接）之前。残留在切分缓存里的字节不算已消费，连同后续字节留给调用方传给新实例。新实例从该字节起以空缓存解析，拼接逐字节等于无上限一次转完。

## 5. 首字节 → 第二字节合法区间 / 非法时吞掉字节数

| 首字节 | 第二字节区间 | 越界吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1（仅 L） |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | —（孤立续接） | 1 |
| C0 C1 F5..FF | —（非法首字节） | 1 |

第二字节合法而第 3/4 续接非法时吞 2（E 类）或 3（F 类）；流结束未凑齐时缓存字节整体为 1 个截断单元。
