# 严格模式 Base64 — 设计说明

## 第 2 条「规范形式」的推导

编码是 3 字节到 4 个 6-bit 字符的映射。输入末尾不足 3 字节时：

- 剩 1 字节（8 bit）：用 2 个字符，第 2 个字符只承载高 4 bit，
  其 sextet 的低 2 bit 不承载任何数据，编码器恒写 0，再补 `==`。
- 剩 2 字节（16 bit）：用 3 个字符，第 3 个字符只承载高 2 bit，
  其 sextet 的低 4 bit 不承载任何数据，编码器恒写 0，再补 `=`。

若解码器接受这些比特非 0 的输入，则同一字节串会对应多个"合法"编码
（`QQ==` 与 `QR==` 都解出 `A`），编码不再唯一，解码也不再是编码的逆。
严格模式要求「对任意字节串，编码结果唯一」，即 Decode 是 Encode 的
精确逆，因此一切编码器不可能产出的字节串都必须拒绝。

判定用的比特（sextet 为字符在字母表中的 6-bit 序号）：

- 尾组形如 `xx==`：检查第 2 个字符 sextet 的低 2 bit（`v & 3`）。
- 尾组形如 `xxx=`：检查第 3 个字符 sextet 的低 4 bit（`v & 15`）。

样例验证：`QR==` 中 R=17=0b010001，低 2 bit=01，非 0，拒绝；
`QUJ=` 中 J=9=0b001001，低 4 bit=1001，非 0，拒绝；
`QQ==` 与 `QUI=` 对应比特全 0，接受。

统一规则：严格解码只接受编码器可能产出的字节串——填充只能出现在
整个流的末尾且数量正确（0/1/2 个 `=`）、总长为 4 的倍数、尾部空闲
比特全 0、换行只出现在组之间（仅 MIME 模式）。

## 语义与测试对照

（待测试写完后补充）
