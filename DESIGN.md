# Bitemporal AsOf 查询器 — 设计推导

包：`interval` `record` `store` `asof` + `cmd/demo`，仅标准库，状态在内存。

## 1. 两条时间轴不对称

- 每条记录携带两个左闭右开区间：有效区间 `V=[vs,ve)`（事实在现实世界成立的时间），
  事务区间 `T=[ts,te)`（数据库“知道”该事实的时间）。
- **事务时间只追加**：`T` 只能在其起点之后被“截止”（把 `te` 从无穷改成当前时刻），
  起点与事实内容永不修改。理由：`T` 记录的是“我们当时相信什么”。若在 t2 更正时直接
  修改 t1 那条记录的内容或起点，则 `AsOf(·, t1)` 将返回 120，历史被重写，审计与“当时
  查询结果”无法复现。更正的正确动作是：① 把旧记录的 `te` 截止到 t2；② 追加一条新记录，
  其 `T=[t2,∞)`、`V` 可任意（**有效时间可回填**：新记录的 `vs` 可以远早于 t2，甚至早于
  全部已有事务时间，因为“现在更正过去的事实”是合法的）。
- 因此同一有效时刻 2020：`AsOf(2020,t1)=100`、`AsOf(2020,t2)=120`。单时间轴模型无法
  同时保留两个答案。

## 2. 逻辑删除

删除不是抹除，而是“事实在某有效时刻终止”：把覆盖该点的记录截止为 `V=[vs,vd)`，其 `T`
  同时截止到删除事务时刻 `td`，不追加新值。于是删除前的历史仍可见
  （`AsOf(v<vd, t>=td)` 得原值），删除后为“不存在”（`AsOf(v>=vd, t>=td)`）。
  查询返回 `(value, ok)`，`ok=false` 与值恰好为零值由 `ok` 区分。

## 3. 矩形不相交不变量

把一条记录看成 `V×T` 矩形。对同一键，任意两条记录的矩形内部不相交（端点可共用，因为
区间左闭右开）。写入/更正维持方式：设新事务时刻为 `t`、目标有效区间为 `V'`。

1. 先收集所有 `T` 仍为 `[_,∞)`（live）且与 `V'` 矩形相交的记录；
2. 每条的 `T` 截止为 `[ts,t)`；
3. 其有效区间在 `V'` 之外若有残余（`vs<vs'` 或 `ve'>ve`），以同样内容、`T=[t,∞)` 保留；
4. 追加新记录 `(V', [t,∞), 新值)`。

历史记录（`te` 有限）永不触碰，且新矩形的 `T=[t,∞)` 与历史的 `te<=t` 不相交，故不变量
归纳成立。store 提供非导出 `checkInvariant()` 遍历全部同键记录对做断言。

## 4. 无穷远终点与空区间

用零值 `time.Time{}` 表示 `+∞`：`contains` 中终点为零值即视为无限大；有限终点为常规
`<` 比较，行为与有限值一致。区间 `[a,b)` 要求 `a` 非零、`a<b`；`a==b` 为空区间，构造时
拒绝（`ErrEmptyInterval`，可用 `errors.Is` 判定）。查询时刻落在起点命中、落在终点不命中。

## 5. 数据结构与复杂度

`store`：`map[key][]Record`，仅按键定位版本链，写入持互斥锁、查询持读锁（`sync.RWMutex`）。
查询只遍历该键版本链：1000 键 × 每键 10 版 = 10000 条时，点查检查数 ≤ 链长 ≤ `4*10`。
更正只触及 live 且相交的旧记录（截止 + 至多 2 条残余），故触及数 ≤ 该键版本数 + 2。
`asof` 持有非导出计数器 `checked`，记录本次查询检查过的记录数。

## 6. 三类错误（errors.Is 可区分）

- `ErrEmptyInterval`：空（或零起点）区间；
- `ErrStaleTransactionTime`：更正/删除所用事务时刻不晚于该键已有最新事务时刻（追加性）；
- `ErrNotFound`：读取/删除一个不存在的键。

并发更正时事务时刻由 store 内单调时钟（微秒）加盖，天然全序，保证不相交不变量。
