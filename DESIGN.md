# DESIGN

## 1. 非法单元切分规则（推导）

目标规则（WHATWG 风格，统一样例）：首字节 b1 宣布长度 L，第二字节 b2 若在合法区间外，
则**只吞 b1**；b2 合法而后续字节非法（非续字节、越界值），则吞掉已读到的最长合法前缀
（含 b1）；流在等待的续字节处结束，已缓存字节算 1 个单元。

首字节表（区间含端点）：

| 首字节 | L | b2 合法区间 | b2 非法时吞 | 前缀合法后中断时吞 |
|---|---|---|---|---|
| C2..DF | 2 | 80..BF | 1 | 1 |
| E0 | 3 | A0..BF | 1 | 1/2（已读几吞几） |
| E1..EC | 3 | 80..BF | 1 | 1/2 |
| ED | 3 | 80..9F | 1 | 1/2 |
| EE..EF | 3 | 80..BF | 1 | 1/2 |
| F0 | 4 | 90..BF | 1 | 1/2/3 |
| F1..F3 | 4 | 80..BF | 1 | 1/2/3 |
| F4 | 4 | 80..8F | 1 | 1/2/3 |
| 80..BF | 1 | — | 1（孤立续字节） | — |
| C0 C1 F5..FF | 1 | — | 1（永不合法） | — |

b2 判定同时排除非最短（E0<A0、F0<90）与越界（ED>9F、F4>8F）。
样例复核：`F0 90 80 41`：b2=90 合法、b3=80 为合法前缀、b4=41 非续字节 → 吞 F0 90 80
（1 个单元），41 重解析；`E0 80 80`：E0 的 b2=80 非法→吞 E0；80、80 各为孤立续字节→共
3 个。`ED A0 80`：A0>9F→吞 ED，续字节各孤立→3。`C0 AF`：C0 吞 1，AF 孤立→2。
`F4 90 80 80`：F4 的 b2=90 非法→吞 F4，余 3 个续字节各孤立→4。`E2 82`+EOF：b2 合法、
b3 缺失→吞已读 2 字节、1 个单元。`80 80`：各孤立→2。

## 2. 缓存上限与立即判定点

UTF-8 最长 4 字节，等待中的首字节最多再挂 3 个续字节，故缓存硬上限 3。收到 b2 即可判
定 b2 区间是否合法（非法立刻吐 1 个单元、只吞首字节，不等 EOF）；进入「等续字节」态后
每收到一个字节：非 80..BF 即立刻吐单元并让该字节重新参与解析；合法则继续等。仅在
Close 仍有等待时才是截断（与非法可区分）。UTF-16 最长 2 个 code unit（4 字节），缓存
上限 1 个 code unit（待配低代理），另在首 2 字节用于 BOM/字节序判定；奇数字节 Close 为
截断。

## 3. par 切点对齐

起点对齐：从切点 i 向前看至多 3 字节，吞掉紧邻 i 的全部 80..BF（它们归上一段单元），
再看前一个字节：若为多字节首字节且其长度可能跨过切点（E0/F0 还要确认 b2 合法区间），
则该首字节也归上段，start 再前移。于是每段从一个确定的单元边界开始，段 0 恒从 0 起。
重叠只由「回看」产生，不重扫段体。

尾部：除最后一段外，每段输入额外附加切点后至多 4 字节（lookahead，UTF-16 为对齐到偶数
+2 字节/code unit）。段内解析到超过本段原始末尾的字节时不发标量（lookahead 仅供判定）：
这样切点在非法单元中（如 `C3|80 80`：下段在 80 处对齐后以孤立续字节起，lookahead 让上
段把 `C3 80` 判成 1 个非法单元；下段首个 80 落在 lookahead 不输出 → 全局恰 1 个单元）、
或在 `F0 90 80|41` 中都能与单流给出相同单元数与输出。统计只计「归属本起点」的字节。
严格模式取各段错误中起始偏移最小者，输出拼到该错误所在单元之前；偏移按段真实起点+段
内位置换算为全流偏移。K=1..8 与任意切点均覆盖。

## 4. 上限断点

发射一个标量前先判定输出长度是否超 MaxOut：超界则该标量及其后续字节一律不动（缓存中
的多字节前缀字节同样不算已消费），Write 返回的 n = 本轮已发射标量对应的输入字节数
（含这些标量所属字节），返回 ErrOutputLimit（终态）。调用方用原输入[n:]续传新实例即
可逐字节拼出无上限结果；UTF-16 代理对作为整体判定，绝不只发高代理。

## 5. 检查次数

每个输入字节在主扫描检查 1 次；仅被缓存的合法前缀（≤3 字节）在新 Write 中再检查 1 次，
故碎切 ≤ 2N。par 的回看/前看每段 ≤ 8 字节（UTF-16 同量级，常数 C=32），检查数
≤ N + K·C。计数器在 stream 内为非导出字段 checks，经 Checks() 读出。
