# 设计推导

## 流格式

多字节整数一律 LEB128 无符号变长整数。记录序列：流头 → 若干记录 → 流尾。

| 记录 | 字节布局 |
|---|---|
| 流头 | 魔数 `0x4F 0x4E`（"ON"）、版本 `0x01`、窗口容量 varint、链长上限 varint |
| 字面量段 | 标记 `0x00`、长度 n varint、随后 n 个原文字节 |
| 回指 | 标记 `0x01`、距离 varint（≥1）、长度 varint（≥3） |
| 刷新标记 | 单字节 `0x02`，无负载 |
| 流尾 | 标记 `0x03`、原文总长 varint、CRC32(IEEE) 校验和 varint |

标记字节是记录的第一个字节，其前一条记录（若有）必定已完整解析，故切分无歧义。

## 推导一：压缩结果与 Write 切法无关

在任意位置 p，匹配器判断「从 p 起能否延长匹配」需要检查位置 p+len 处的字节；若该字节尚未收到，就无法断定当前匹配已结束。因此压缩器维护一个待决输入缓冲：除 Flush/Close 外，新字节只追加进缓冲，不立即做决策。
贪心压缩的处理单位是一次「决块」：两次决策点之间收到的全部字节（Flush 时为全部待决字节；Close 时为剩余全部字节）。压缩算法是对一条完整字节序列（历史窗口 + 决块）的纯函数：从决块起点逐位贪心，字面量与匹配完全由字节内容决定，与该序列此前被分几次 Write 追加无关，故输出逐字节相同。
Flush 代表调用方此刻需要「全部已交输入可还原」的承诺，所以它强制终结尚未定论的匹配，把待决缓冲整体压成一个决块并追加 `0x02`；连续两次 Flush 间无新字节则没有任何记录可输出。

## 推导二：距离 < 长度的重叠复制

一次性块复制 `copy(out, out[i-d:i-d+l])` 的源区间按复制开始时的旧内容固定；而 LZ77 语义是源随输出增长——刚复制出的字节立即成为后续字节的来源（如距离 1 表示整段重复同一字节）。Go 内建 `copy` 在源与目标重叠时结果不能依赖。
实现：长度较短时逐字节 `out = append(out, out[n-d])`，索引每次读取的都是已产出切片，天然满足前向复制；长度较大时以 d 为周期分块，每块复制一段不与源重叠的切片（先复制前 d 字节、它们又成为下一块的源，等价展开）。两种路径结果一致；逐字节路径仅用于小长度，成本可接受。

## 推导三：「距离超过已输出」与「距离超过窗口」为何不同

已输出字节数随解压推进单调增长，窗口容量是流头声明的固定上限。距离 d 必须同时满足 d ≤ 已输出（历史里确实存在该字节）与 d ≤ 窗口容量（压缩器只可能在该范围内引用）。对任意流，d 介于二者之间是可达状态（解压早期、已输出尚少），故必须是两类可区分错误。
压缩器只在容量为 W 的窗口里做匹配，窗口内不存在的字节不可能成为候选，因此 d > W 不可能由本压缩器产生；出现它说明流损坏或流头被篡改。
并行块的预置字典是上一块原文的真实字节：压缩时写入匹配器窗口，解压时这些字节也确已由上一块解压产出。二者看到的历史一致，「已输出字节数」含义不变，无需特殊标记。

## 推导四：并行压缩的确定性与衔接

块 k 的压缩只读取块 k 原文与块 k-1 原文末尾至多 W 字节（预置字典），不读取任何块的压缩结果、不等待其他块完成；各块任务相互独立，可任意并发、任意顺序执行，输出字节只由「本块原文 + 固定算法参数」决定，故 workers=1/2/4/8 输出逐字节相同。
衔接：块边界处不写刷新标记。块 k 允许的回指距离最大 W，而其可引用的跨块历史正是上一块尾部 W 字节；流式解压时这些字节已在解压窗口内，直接拼接记录即可正确解开，仅流首写一次流头、流末写一次流尾。
