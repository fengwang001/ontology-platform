# 多租户配额公平队列调度器 — 设计推导

## 1. 为什么是 `vt += c / 权重` 而不是 `c * 权重`

目标：长期内租户 i 获得的执行代价份额正比于权重 w_i。
调度规则是「每轮选 vt 最小者」，因此所有活跃租户的 vt 会被拉到近似相等
（记为系统虚拟时间 S）。设租户 i 在 vt 前进 Δ 的过程中执行的累计代价为 C_i，
由 vt += c/w_i 得 C_i = Δ · w_i，即单位虚拟时间换得的执行代价正比于 w_i，
于是 C_i / ΣC_j = w_i / Σw_j，份额精确正比于权重。
若改用 c·w_i，则 C_i = Δ / w_i，权重越大份额越小，与配额语义完全相反；
且高权重租户 vt 冲得快、被低频选中，单调性也错。故取 c/w_i。

## 2. 空闲租户重新加入的 vt 抬升

系统虚拟时间 S 定义为「最近一次被选中租户出队时的 vt」（活跃租户 vt 的下界近似）。
租户 X 空闲期间，其他租户继续执行，S 单调前进；X 的 vt 却停在旧值 vt_X ≪ S。
若 X 重新提交时不修正，它会在每次选择中都是最小者，直到 vt_X 追上 S：
独占步数 ≈ (S − vt_X) · w_X / c。例：Y（w=1）以代价 1 执行 1000 步后 S≈1000，
X（w=1）重新提交 1 个任务，若不抬升，X 将连续独占约 1000 步才轮到 Y——
既让 X 拿到远超配额的突发，也长期饿死 Y。
正确做法：租户由空转为非空（或首次到达）时，令 vt = max(vt, S)。
抬升后 X 至多与活跃者并列，按并列规则在 1 步内被服务一次，随后回到公平轮转；
Y 的既有 vt 优势不被抹掉，也不会被饿死。

## 3. 代价为 0 的任务

代价 0 合法，但若按 c/w 计费 vt 不前进，该租户永远是最小者，无限独占。
处理：计费代价取下界 MinCost = 1（与典型单位代价同量级），
即 billed = max(c, 1)。vt 每步严格前进，零代价任务按单位代价参与公平轮转，
既不独占也不免费（避免用零代价任务套利绕过配额）。

## 4. 同 vt 并列

比较键为 (vt, 租户ID) 字典序：vt 相同取 ID 较小者。
堆中不存在完全相等的键，调度序列对同一输入流完全可复现。

## 5. 数值表示：float64

vt 用 float64。10 万步、代价 O(1)、权重极端并存（如 1e-9 与 1e9）时，
单步增量最大 c/w ≈ 1e9，累计 ≈ 1e14，远低于 float64 精确整数上限 2^53 ≈ 9e15，
不溢出、不丢精度。相比有理数（分子分母随步数膨胀、需周期约分）更简单且足够精确。

## 6. 复杂度

选择用二叉堆（container/heap）：单次「弹出最小 + 重插入」比较次数 O(log n)，
实测上界取 4·⌈log2 n⌉（下沉每层约 2 次比较 + 上浮每层约 1 次）。
堆中只放非空租户，空闲租户不占选择代价。

## 7. 背压、移除与并发

- 每租户队列上限：超限返回 ErrQueueFull，只影响该租户。
- 权重 ≤ 0：AddTenant 返回 ErrInvalidWeight。
- 有排队任务时移除租户：拒绝，返回 ErrTenantNotEmpty（任务留在原租户，去向明确）。
- 操作不存在的租户：ErrTenantNotFound。四类错误均可 errors.Is 区分。
- 并发：admit.Queue 用一把互斥锁串行化 Submit/Next；并发提交时，
  「提交顺序」定义为获取入队锁的串行化顺序，确定性只对单线程提交流承诺。
