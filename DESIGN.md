# Quoted-Printable 编解码器设计

## 推导一：行末空白

规则：空格与制表符原样输出，但若位于一行末尾（紧跟换行或输入结束）必须写成
`=20` / `=09`。推导：解码端把 `=XX` 还原为字节，而行尾裸空白在传输中会被
网关剥掉（RFC 2045 也允许解码器忽略行尾空白），所以行尾空白只有转义才能
存活。样例：`"a \n"` → `"a=20\r\n"`；`"a b"` → `"a b"`（空格后还有内容）；
`"a "`（输入结束）→ `"a=20"`。实现上需要向后看一个字节才能判定空格是否
在行末，因此编码器对空格/制表符缓存一个待决字节，见到后继再落盘。

## 推导二：软换行不得拆开 `=XX`

规则：每行（不含 `\r\n`）最多 76 字符，软换行 `=\r\n` 的 `=` 计入 76。插入
软换行时不得把 `=XX` 拆到两行。推导：解码器按 `=` 后跟两个十六进制字符
解析，若 `=3` 留在行尾、`D` 落到下一行行首，则 `=3\r\n` 不是合法软换行
（`3` 不是 `\r`），严格解码器必须报错，编码器就产出了自己无法解码的输出。
所以换行决策必须以「下一个输出单元」为粒度：单元是 1 字符（可打印字节）
或 3 字符（`=XX`）。样例：行内已有 74 字符、下一单元是 `=3D`（3 字符），
74+3=77>76，先插 `=\r\n` 再输出 `=3D`。

## 推导三：软换行前的空格算不算行末空白

结论：算，必须写成 `=20`。理由：严格解码器（以及 RFC 2045）把 `=\r\n`
整体删除，即软换行前的那个 `=` 被吃掉；若空格原样留在 `=` 之前，删除软
换行后空格变成「行末未转义空白」，严格解码器按第 4 条必须报错，且宽松
传输也可能剥掉它。因此编码器在插入软换行前，若行尾有待决空格/制表符，
先把它落盘为 `=20`/`=09`，再插 `=\r\n`。这样软换行插入点对解码器不可见，
往返成立。

## 往返等式的准确形式

令 N(x) 为换行规范化：`\r\n` → `\n`，孤立 `\r` 保留为普通字节。则对任意
字节串 x：`Decode(Encode(x)) == N(x)`。特别地，x 中的 `\n` 往返后变成
`\r\n`；孤立 `\r` 被转义为 `=0D`，往返后仍是 `\r`。且 `Encode(x)` 必被
严格解码器接受。

## 最小转义

编码器只转义三类字节：(a) 可打印 ASCII 33–126 之外的字节（空格、制表符
除外）；(b) `=` 本身；(c) 行末的空格/制表符。其余一律原样输出，因此是
最小转义：对严格解码器接受的任意 y，`Encode(Decode(y))` 与 y 的差异只
可能来自 y 中「可打印字符被多余地转义」（如 `=41` vs `A`），解码再编码后
多余转义被消除，输出是 y 的最小转义规范形。

## 测试对照表

（待补）
