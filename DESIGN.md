# 设计推导

## 1. 空行与单列空值

空行（两个 `\n` 相邻，或开头就是 `\n`）**跳过，不产生记录**。若把它当作「一条单字段、值为空的记录」，则
单列文件无法区分「空行（排版空隙）」与「真正的空数据行」，且 `a\n\n` 会平白多出一条记录。

因此单列空值记录必须写成 `""`（加引号的空字段）。规则：单列表回写时，唯一字段为空（无论引号标记）都写 `""`；
多列表中未引号空字段写空串即可（逗号已提供结构）。规范输入定义：记录间无空行；空值除单列表外一律未引号；
行尾用 `\n`；末记录带换行；引号字段只在必要时加引号。则 `Write(Parse(x)) == x` 逐字节成立。

## 2. par 切点

每段 worker 无法自证起点是否在引号内。令每个 worker 按两种假设各解析一次：
H0=起点在引号外字段起始态，H1=起点在引号字段内（有活动单元）。协调器从段 0 的真实状态 FS 出发，
串行衔接：段 i 的真实进入态为 H0 或 H1 之一，直接取对应假设的事件流与结束态。进入态若为刚见引号（QS）/
CR 待定（CR）这两个「待定态」，二者分别只是 QU 与 BA 的加一字符上下文：取最接近假设（H1/H0）的事件流，
仅按首字节边界语义修正（首字节若恰为闭合引号/LF，把「追加」改为「提交」），结束态相同。故每字节至多处理
2 遍，无反复重扫。事件携带段内偏移，拼接时加段基址即得全局偏移；记录号/字段号按被采纳事件流的累计计数。
错误是「首错误即终态」：采纳流中第一个错误即全局首错误，坐标同样加段基址。

## 3. CR 待定

未引号字段见 `\r` 进入 CR 态：下一字节是 `\n` 则为 CRLF 行尾；其他字节则该 `\r` 是孤立 CR，报
ErrBareCR（偏移指向 `\r`）。半包续传中 CR 态跨 Feed 保持。par 中 CR 态只能出现在段边界，按第 2 节的
待定态修正处理。流在 `\r` 处结束（Close）：按孤立 CR 报错（其后不是 `\n`）。引号字段内 `\r` 是普通内容，
不产生待定态（`\r\n` 原样保留）。

## 4. 上限的「立刻」

字段字节计数对进入字段值的每个字节自增，`""` 转义中的两个引号只产生一个值字节，计 1。计数在写入该字节
之前与上限比较：第 (max+1) 个值字节到达的那个输入字节即报错，不等记录闭合。字段数在每个新单元出现前比较；
记录数在每条记录提交前比较。触发后置终态，错误粘滞，后续 Feed 返回同一错误。

## 5. 状态转移表

状态：FS 字段开始；BA 未引号字段；QU 引号字段；QS 引号内刚见引号；CR 行尾 CR 待定；
QC 引号闭合后 CR 待定（`"x"\r`）。类别：`,` `"` `\n` `\r` 其他 X。
动作：C 提交单元；R 提交记录；a 追加值字节；q 追加一个引号值字节。

| 状态 | `,` | `"` | `\n` | `\r` | X |
|---|---|---|---|---|---|
| FS | C→FS | →QU | 空行:无→FS | →CR | a→BA |
| BA | C→FS | ErrQuote | R→FS | →CR | a→BA |
| QU | a | →QS | a | a | a |
| QS | C→FS | q→QU | R→FS | →QC | ErrAfterQuote |
| CR | ErrBareCR | ErrBareCR | R→FS | ErrBareCR | ErrBareCR |
| QC | ErrAfterQuote | ErrAfterQuote | R→FS | ErrAfterQuote | ErrAfterQuote |

Close：FS 态无活动内容不产出（末尾换行/空文件）；其余非错误态先 C 再 R；QU 报 ErrUnclosedQuote；
CR/QC 在 EOF 分别报 ErrBareCR、ErrAfterQuote。空行（FS 遇换行）不提交记录。
