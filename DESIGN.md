# 严格模式流式 Base64：规范尾部推导

一组 4 个字符 `c0 c1 c2 c3` 携带 24 个比特：
`v0,v1,v2,v3` 各 6 bit，拼成 `v0<<18|v1<<12|v2<<6|v3`，
再切成 3 字节。解码只取前 8/16/24 bit，尾部是若干「不被使用」的比特。

关键事实：编码时，最后一个量子里所有数据不足的比特恒为 0。
因此被接受的量子，其未使用比特必须全为 0；否则该量子不可能由任何字节串编码得到。

- 无填充（3 字节）：24 bit 全被使用，无未用比特，任何合法字符组都规范。
- 一个 `=`（2 字节，`c3='='`）：只用 `v0,v1,v2` 的高 16 bit，
  `v2` 的低 2 bit 未被使用 → 必须 `v2 & 0b11 == 0`。
  `QUI=`：v2='I'=8，`8&3==0`，合法；`QUJ=`：v2='J'=9，`9&3==1`，拒绝。
- 两个 `=`（1 字节，`c2=c3='='`）：只用 `v0,v1` 的高 8 bit，
  `v1` 的低 4 bit 未被使用 → 必须 `v1 & 0b1111 == 0`。
  `QQ==`：v1='Q'=16，`16&15==0`，合法；`QR==`：v1='R'=17，`16&15==1`，拒绝。

结论：非规范尾部的判定只看最后一个被使用量子的「后缀未用比特」——
两填充查 `v1` 低 4 位，一填充查 `v2` 低 2 位，必须全 0。
这保证映射是单射的：宽松解码器丢弃这些比特，于是多个不同量子
（如 `QQ==` 与 `QR==`）会解出同一字节串，规范编码不再唯一；
强制未用比特为 0 后，每个字节串恰有一个编码，即
`Encode(Decode(y)) == y` 对所有被接受的 y 成立。

## 测试对照表

<!-- 实现完成后补齐：7 条语义 -> 测试函数名 -->
