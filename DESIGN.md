# DESIGN：自定义流式 LZ77 压缩格式

所有整数用标准 varint（LEB128，最多 10 字节；第 10 字节只允许低 1 位）。魔数固定 3 字节 `L17`，版本 varint（当前 1）。

## 流格式记录布局

| 记录 | 首字节 | 后续字段 | 语义 |
|---|---|---|---|
| 流头 | 魔数 `L17` | varint 版本=1, varint 窗口容量 | 每流恰好 1 个，在最前 |
| 字面量 | 0 | varint 长度 n，随后 n 字节原文 | n≥1 |
| 回指 | 1 | varint 距离(≥1), varint 长度(≥3) | 允许距离<长度（重叠复制） |
| 刷新 | 2 | 无 | 提交点；连续两个无新数据时第二个不产生字节 |
| 流尾 | 3 | varint 原文总长, varint CRC32(IEEE) | 流尾后不得有任何字节 |

## 推导一：压缩结果为何与 Write 切法无关（语义 3、4）

最长匹配的终点依赖"下一个不匹配字节"，而该字节可能在后续 Write 中才到达。
因此压缩器维护一个 pending 缓冲区，只处理 `len(pending)-1` 个起始位置：对位置 i，
必须同时拿到 i+1 的判定（是否有更长匹配）才能决定 i 输出字面量还是回指；
而回指的长度一直延伸到第一次失配，所以一个匹配的长度只有在失配字节（或 Flush/EOF）
到达后才确定。具体规则：
- 新数据追加进 pending；扫描时保留最后 1 字节不判决。
- 对每个可判决位置做哈希链查询；当前匹配必须等下一位置的查询结果出现（可能更长）才发出，
  即贪婪匹配的"延迟一位置发射"：匹配一旦发出，其长度按当时已到达数据确定，
  后续字节到达只延长"尚未发射"的匹配，永不改写已发射记录。
由于判决只依赖累计字节序列本身，与字节分几次 Write 到达无关，故输出逐字节相同。
- Flush：强制判决全部 pending（包括最后一字节），发射刷新记录并落盘；
  因此解码器可还原截至 Flush 的全部输入。窗口/哈希链不清空，故之后仍可回指历史。
- 无新数据的 Flush：所有输入此前均已判决并落盘，直接返回，不产生字节。

## 推导二：重叠回指的解压复制（语义 2）

块复制在长度>距离时会读到目标区尚未初始化的内容。正确语义是 RLE 式递推：
`out[j]=out[j-dist]`，每读一个字节就先写回输出，随后位置读到的可能就是刚写出的字节
（周期为 dist）。实现采用逐字节循环，长度上限为窗口容量（默认 64KiB），
单条记录开销可接受，天然保证正确性，无需特意向后分块。距离 1 即整段重复同一字节。

## 推导三：两类"距离越界"为何区分（语义 7）

- 距离 > 窗口容量：流本身与声明的窗口矛盾，任何长度的合法前缀都不可能产生它；
  压缩器在插入位置 p 只可能引用窗口内位置（距离≤容量），故正常压缩绝不可能产出。
- 距离 ≤ 容量但 > 已输出字节数：可能出现在损坏的短前缀上（窗口够大但历史还没那么长）。
二者是不同不变量，必须分别报错。并行块的预置字典字节在解压侧就是此前真实输出的字节，
"已输出字节数"含义不变；压缩器预置字典长度恒为 min(窗口容量, 已处理字节数)，
故引用的距离既不超窗口也不超"当时已输出"。

## 推导四：并行块压缩的确定性（语义 9）

按固定 blockSize 切块。块 k 以块 k-1 原文末尾至多窗口容量字节作为预置字典
（填入窗口与哈希链）后，独立压缩本块原文；块边界强制判决全部 pending，
但不发射刷新记录（块边界不是 Flush 语义边界），块 k 的回指可越过块边界。
流中只写一次流头，各块记录顺序拼接，最后统一写一个流尾（总长、CRC）。
每块的输出只由"本块原文 + 上一块原文末尾窗口"决定，与 worker 数、调度顺序、
其他块结果无关；块内用的正是串行压缩器同一套贪婪/延迟判决逻辑，故
workers=1/2/4/8 输出逐字节相同。并发只共享只读输入，无共享可变状态，race 干净。

## 解压设计

解码器维护读偏移（错误均带字节偏移）、记录状态机、逐字节 varint 解码
（>10 字节或第 10 字节>1 报超长/溢出）、环形窗口、已输出计数与上限。
回指先校验距离（先窗口后已输出），再校验长度是否使总量超上限，全部在写出前，
然后逐字节复制。流尾校验长度与 CRC；流尾后再出现字节报多余字节。
EOF 时未走到流尾即截断；零字节输入也判截断（与"空原文的合法非空流"区分）。
任何错误后进入终态，后续 Write 返回同一错误。
