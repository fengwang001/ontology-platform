# 游标分页器设计推导

数据集：按复合键 `(排序值 Key, 唯一 ID)` 升序排列的有序集合，支持并发增删。
序关系：`a < b` 当且仅当 `a.Key < b.Key`，或 `a.Key == b.Key 且 a.ID < b.ID`。
该序是全序（ID 唯一保证任何两行可比较），这是全部推导的前提。

## 一、为什么游标必须是复合键 (Key, ID)

反例 1（只记排序值 + 严格大于）：十行中六行 Key 相同（如 Key=5 的
`a,b,c,d,e,f`），每页三行。第一页返回 `..., a, b`，游标记 `v=5`。
下一页取 `Key > 5`，则 `c,d,e,f` 被整批跳过 —— **漏**。

反例 2（只记排序值 + 大于等于）：同一数据集，下一页取 `Key >= 5`，
则 `a,b` 再次出现在第二页 —— **重**。

结论：排序值不唯一时，任何只含排序值的边界都必然漏或重。正确做法是
游标记录上一页最后一行的完整复合键 `(Key, ID)`，下一页边界为复合键
**严格大于**：`Key > v`，或 `Key == v 且 ID > id`。因为序是全序，
严格大于既不漏（后续行全在满足侧的区间里）也不重（已返回行全部严格
小于边界）。游标编码为不透明字节串：方向标记 + Key(8B) + ID 长度 +
ID + CRC32 校验。

## 二、反向翻页规则

反向不是「正向结果倒序」。规则：比较方向与取数方向同时反转，页内顺序
仍按正向（升序）排列。即：以游标复合键为上界取 **严格小于** 该键的
最后 n 行，再按升序输出。因此反向游标指向页首行，继续反向翻页时取
「严格小于页首」的区间。正向翻到第三页后用其反向游标翻一页，得到的
正是第二页且逐行相同（区间 `(页2首, 页3首)` 内最后 n 行即第二页全体，
顺序不变）。

## 三、并发增删下的语义承诺

不做快照隔离：翻页进行中插入到**已翻过区间**的行允许永不出现；插入到
**未翻过区间**的行会按其复合键位置自然出现。承诺两条硬保证：

1. 已返回过的行绝不再次返回（边界是严格大于已返回最大复合键）；
2. 未被删除的行不会在其复合键区间内被跳过（二分定位 + 顺序扫描，
   扫描区间由复合键界定而非物理位置）。

删除安全：游标只承载复合键值，不持有行的引用或下标，故指向已删除行的
游标仍可按「严格大于该复合键」继续翻页。

## 四、效率上界

单次翻页 = 一次二分定位（≤ ceil(log2 N) 次行比较）+ 顺序扫描本页
（≤ n 次行比较），比较总数 ≤ `4*(n + ceil(log2 N))`，绝不从头全扫。
游标编解码为定长头部 + ID 拷贝，分配次数为常数，与数据集规模无关。

## 五、故障与并发

- 解码校验顺序：方向标记非法 → 长度字段不自洽（字段不完整）→ CRC 校验
  失败；空字节串合法，表示从头（正向）或从尾（反向）开始。
- 正向游标用于反向翻页（或反之）报方向错误，不静默处理。
- 翻页是纯函数：游标不可变、无隐藏推进状态，同一游标多协程并发翻页
  结果逐行相同；数据集用 RWMutex 保护，`-race` 干净。
