# JSON 字符串字面量严格编解码器 — 设计推导

包划分：`esc`（单个转义序列判定，无依赖）← `jstr`（Decode/Encode/流式 Write+Close）。
Decode 直接复用流式解码器（一次 Write + Close），因此一次性与任意切分的结果天然逐位一致。

## 第 2 条：代理对错误偏移的推导

候选规则：(a) 指向高代理 `\u` 的起点；(b) 指向其后那个不合格单元的起点。
取 **(a) 的加强版：偏移永远指向"第一个无法完成配对的码元"的反斜杠**——
孤立低代理指向它自己；高代理未被低代理紧随（含 EOF、跟非 `\u`、跟的 `\u` 不是低代理）指向该高代理的起点。
理由：
1. 规则的全函数性：`"\uD83D"` 在 EOF 处失败时根本不存在"后面的单元"，(b) 无定义，(a) 总有定义。
2. 流式安全：判定只依赖已确定的事实（"这个高代理未被合法配对"），不依赖后续还会来多少字节，切分点不影响偏移。
3. 语义准确：被违反的约束是"高代理必须紧随低代理"（RFC 8259 §7 的 12 字符形式），违约主体是高代理本身。

`"\uD83DA"` 为什么不能解码成「U+FFFD + `A`」：
- 语法上不存在该产生式：高代理后唯一合法后继是 `\uDC00`–`\uDFFF`，`A` 不是 `\`，整个字面量无合法解析。
- 替换会破坏单射：`"\uD83DA"` 与 `"\uFFFDA"` 将解码为同一字符串，往返性质（第 5 条）不再成立。
- 静默替换会掩盖数据损坏，与"严格解码、五类错误可区分"的目标直接冲突。

## 第 4 条：最小转义与唯一性的推导

RFC 8259 的 `unescaped = %x20-21 / %x23-5B / %x5D-10FFFF`：
- U+0000–U+001F 不在 `unescaped` 内 → **必须**转义（语法强制，无选择）。
- `"` 与 `\` 是定界符/转义引导符 → 必须转义。
- U+007F 落在 `%x5D-10FFFF` 内 → 原样合法；`/`、非 ASCII、U+2028/U+2029 同理。
  即：U+001F 是"语法禁止原样出现"，U+007F 只是"风格上可疑"，严格解析器无权要求转义后者。

唯一性：每个码点的输出由确定性查表决定（5 个短转义 / 其余控制符 `\u00xx` 小写 /
`"`、`\` 加反斜杠 / 其余原样），无任何自由选择 ⇒ 同一输入输出唯一（范式）。
单射：码点→转义形式的映射本身单射（短形式、`\u00xx`、原样三类值域互不重叠，
且 `"`、`\` 的转义形式不会与任何原样输出混淆），拼接后 Decode 可逐码点逆转，
故 `Encode(a)==Encode(b)` ⇒ `a==b`。结合 Decode 是 Encode 的左逆，得第 5 条往返。

## 语义 ↔ 测试对照表

（待测试完成后填写）
