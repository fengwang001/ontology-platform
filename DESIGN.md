第 3 条：续行行只含空白或为空时的行为

依据 readLine 的控制流：续行触发后进入下一物理行，行首空白被“无条件跳过”
（跳过空白时不改变 precedingBackslash，而续行连接发生在该标志为 true 时）。
若该行除空白外没有任何字符，读取循环遇到行终止符时 precedingBackslash 已为
false（只有反斜杠会翻转它），于是走“结束逻辑行”分支，而不是再次续行。
结论：空白续行行贡献 0 个字符并立即终结当前逻辑行；其后的物理行是一条新逻辑行，
空白行不会被“吸收”，也不会继续拼接。与“首部空白剥离”的关系：剥离发生在追加之前，
剥光后没有字符可追加，行终止符按普通换行处理；因此 k=a\ + LF + 全空白行 + LF + z
得到 k→a，z 另起一行。EOF 同理（无终止符时逻辑行在空白耗尽处结束）。

第 6 条：Store 的转义推导（只转义必要字符）

解析器识别三类特殊构造：反斜杠转义、键值分隔符、行首注释。据此逐项推导：
1) 反斜杠本身：任何位置都必须写成 \\，否则会和后一字符组成转义序列；全部翻倍后
   行尾反斜杠个数恒为偶数，不会意外形成续行。
2) \t \n \r \f：控制字符无法原样表示（换行会截断逻辑行），必须转义。
3) 键中的分隔符与空白：未转义的 '=' ':' 及空白（空格/Tab/\f）在键中会充当分隔符，
   故键内这些字符无论出现在哪里都要转义。
4) 键首的 '#' '!'：注释只在逻辑行第一个非空白字符处判定。键首若为空白，该空白已按
   规则 3 转义，后面的 '#' 不再位于行首；因此 '#'/'!' 仅在“键的第一个字符”时转义。
5) 值开头的空白必须转义：解析器在分隔符两侧跳过空白，不转义就会被吃掉；一旦值已经
   开始，中间与结尾的空格都是普通字符（样例明确保留值尾空白），无需转义。
6) 值中的 '#'/'!' '=' ':' 永不是注释也不是分隔符（分隔符只取第一个、注释只在行首），
   一律原样输出。非 ASCII 以 UTF-8 原样输出。
7) 空键直接写 '='；空值只写键与 '='。

语义 → 测试函数对照表（实现完成后回填）

| 语义 | 测试函数 |
|---|---|
| 1 分隔符与空白 | TBD |
| 2 注释 | TBD |
| 3 续行与空白续行 | TBD |
| 4 转义与 uXXXX 行列号 | TBD |
| 5 重复键首次顺序 | TBD |
| 6 Store 必要转义 | TBD |
| 7 1000 组随机往返 | TBD |
| 附：字节检查计数 ≤2N | TBD |
