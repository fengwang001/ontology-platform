# DESIGN - JSON 字符串字面量严格编解码器

## 第 2 条推导：代理对错误偏移指向哪里

规则：**凡代理对错误，偏移一律指向「无法完成配对的第一个 `\uXXXX` 单元的起始字节」（即它的反斜杠）**。

- `"\uD83D"`（孤立高代理）：错误在 offset 1。高代理在读到后续单元之前尚未构成错误；
  当输入结束或后续单元不合格时，真正坏掉的 token 是这个高代理本身，后续字节只是证据。
  若规则指向「后面那个不合格单元」，则 `"\uD83D"`（紧跟引号或输入结束）根本不存在那个单元，
  偏移将无处可指——规则必须对「无后续单元」与「后续单元不合格」给出同一答案。
- `"\uDE00"`（孤立低代理）：低代理自身永远不能开启一个对，错误即其自身起点 offset 1。
- `"\uD83Dx"`、`"\uD83DA"`：高代理期待 `\u` 却得到别的字节，错误仍指高代理起点 offset 1，
  与孤立情形同一规则、同一偏移，调用方定位方式一致。
- `"\uDE00\uD83D"`（顺序颠倒）：第一个单元是低代理，无法开启配对，错误在 offset 1。

为什么 `"\uD83DA"` 不能解释成「一个替换字符加 `A`」：严格解码器不做静默替换。
孤立代理不是 Unicode 标量值，无法编码为合法 UTF-8 输出；替换成 U+FFFD 会
(a) 静默丢失信息、掩盖数据损坏；(b) 让两个不同字面量解码为同一字符串，
Decode 不再单射，往返性质崩塌。RFC 8259 只对配对的代理给出语义，
孤立代理的行为「不可预测」，严格器唯一自洽的选择是拒绝。

## 第 4 条推导：最小转义与唯一性

- 为什么 U+007F 不必转义而 U+001F 必须：RFC 8259 的 unescaped 产生式为
  `%x20-21 / %x23-5B / %x5D-10FFFF`——文法只禁止裸写 `"`、`\` 与 `%x00-1F`。
  U+001F 落在禁集内，必须转义；U+007F(DEL) 不在禁集内，可合法原样出现。
  最小转义 = 恰好转义文法强制集，多一个少一个都破坏「最小」。
- 输出唯一：每个 rune 的输出形式由确定性规则唯一决定
  （在短转义表则短形式；其余控制字符 `\u00xx` 小写；此外一律原样），
  无任何自由选择，故同一输入只有一种输出。
- 单射：裸输出字节绝不含 `"`、`\` 与控制字符，每个转义以 `\` 开头且定长
  （2 或 6 字节），故输出可被唯一切分回逐 rune 的块，不同输入必得不同输出；
  等价地，`Decode(Encode(s)) == s` 给出左逆，直接蕴含 Encode 单射。

## 语义 → 测试对照表

（待测试写完后补齐）
