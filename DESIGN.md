# 熔断与舱壁隔离调用保护器 — 设计推导

## 0. 结构

`classify`（错误分类）→ `timeout`（单次时限）→ `bulkhead`（并发额度+等待队列）→
`breaker`（状态机）→ `stat`（统计口径）→ `guard`（组合：熔断在前、舱壁在后）→ `cmd/demo`。
调用链：`guard.Do` = 熔断判定 → 舱壁取额度 → timeout 执行 → 分类统计并回馈熔断。

## 1. 推导：被熔断拒绝的调用不得计入失败率

设失败率 = 失败数 / 真实调用数。若把「被熔断拒绝」也算作失败或算入分母：

- 熔断打开后，每次拒绝都抬高失败分子或污染分母口径，失败率被自己的拒绝锁定在阈值之上；
- 冷却到期进入半开的条件若依赖失败率回落，则永远不满足 → 一旦打开永远无法恢复（死锁式熔断）。

正确口径：熔断器只对**真实到达下游的调用**（成功/失败/超时/panic）计数；
被拒绝的调用只进入 `stat` 的「被熔断拒绝数」，既不进失败分子也不进真实调用分母。
这样打开期间统计冻结，冷却到期后仅凭半开探测的真实结果决定去留：
半开探测全部成功 → 关闭；任一失败 → 重新打开。失败率只反映下游真实健康度。

## 2. 推导：熔断必须置于舱壁之前

两种顺序对比：

- **熔断在前**：下游已知故障（熔断打开）时，请求在第一道门立即以可判定错误返回，
  不占用并发额度、不进等待队列；舱壁额度全部留给仍可能成功的调用路径。
- **舱壁在前**：熔断打开期间每个注定被拒的请求仍要取额度、甚至排队，
  拒绝被延迟到出队之后，既浪费额度又把「拒绝」降级为「排队后拒绝」，
  高并发下等待队列被无效请求塞满，真正可执行的调用反而被挤掉。

故规定：先熔断、后舱壁。推论（可测）：熔断打开时舱壁在途占用恒为 0。

## 3. 推导：四条路径都必须归还额度

额度是有限资源（N）。获取额度后的退出路径只有四条：成功、失败、超时、panic。
任一路径漏还，额度单调泄漏；泄漏累计 N 次后可用额度为 0，舱壁从此全拒（静默死亡）。
因此归还必须挂在 `defer` 上、与结果无关：panic 经 `recover` 转为失败错误后同样走 defer 归还。
测试在四路径各跑 1000 次后断言可用额度回满，即「无泄漏」的充要证据。

## 4. 定义与论证：不可重试错误是否计入熔断

定义：**不计入**熔断失败计数（不触发打开），但计入 `stat` 的失败数（按类别细分）。
论证：熔断器测量的是「下游是否健康」。不可重试错误（如参数非法、权限不足）由调用方
决定，重放结果不变，与下游容量/健康无关；若计入，调用方 bug 会把熔断器刷开，
让无辜的其他调用方被一起拒绝（误伤面扩大）。可重试错误与超时反映下游处理能力，
计入熔断。超时同时是可判定错误（`errors.Is` 可区分）。

## 5. 熔断状态机精确语义

- 关闭→打开：连续失败数 ≥ 阈值，**或**（样本数 ≥ 下限 且 失败率 > 阈值）。两条件独立，分别测试。
- 打开→半开：`now ≥ openedAt + cooldown`（注入时钟判定）。
- 半开→关闭：半开期 `Probes` 个探测全部成功（并发探测数 ≤ Probes，超出立即拒绝）。
- 半开→打开：任一探测失败立即打开，且 `cooldown = min(2*cooldown, maxCooldown)`；回到关闭时 cooldown 复位为基准值。
- 时钟回拨：`now < openedAt` 视为回拨，返回可判定错误 `ErrClockRollback`，状态保持打开不变（不得提前半开）。
- 并发迁移：所有状态迁移在互斥锁内完成；并发失败只触发一次「关闭→打开」，冷却只加倍一次（迁移计数器断言）。

## 6. 舱壁精确语义

- 额度 N：在途真实调用数恒 ≤ N；内部非导出峰值计数器记录历史峰值（`Peak()` 只读暴露）。
- 队列上限 Q：第 N+Q+1 个请求立即以 `ErrFull` 拒绝，不睡眠不轮询（注入时钟不推进可证）。
- 等待中被 ctx 取消：离开队列、不持有任何名额，名额立即可被他人获取。
- 不同下游各自持有独立 `Bulkhead` 实例，互不影响。

## 7. 统计口径（自洽等式）

任意操作序列后恒成立：

1. `总请求数 = 成功数 + 失败数 + 被熔断拒绝数 + 被舱壁拒绝数`
2. `真实调用数 = 总请求数 − 被熔断拒绝数 − 被舱壁拒绝数 = 成功数 + 失败数`
3. `Σ 按类别失败数（可重试/不可重试/超时） = 失败数`

每个请求在 `guard.Do` 中只走一条终态分支、只记一次账，是等式成立的构造性保证。

## 8. 四类可判定错误（`errors.Is`）

`breaker.ErrOpen`（熔断拒绝）、`bulkhead.ErrFull`（舱壁满）、`timeout.ErrTimeout`（调用超时）、
`breaker.ErrClockRollback`（时钟回拨）。panic 被 recover 转为普通失败错误（可重试类）。
