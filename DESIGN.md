# DESIGN

包依赖单向：`wire`、`window` 独立；`match`→`window`；`enc`→`wire`+`match`+`window`；`dec`→`wire`+`window`。

## 流格式（所有整数均为 uvarint/LEB128，超 10 字节或第 10 字节 >1 即非法）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4F 4C 5A`("OLZ") + uvarint 版本(=1) |
| 字面量段 | `00` + uvarint n + n 字节原文 |
| 回指 | `01` + uvarint dist + uvarint len |
| 刷新标记 | `02` |
| 流尾 | `03` + uvarint 原文总长 + uvarint FNV-1a/64 校验和 |

回指语义：从已输出末尾向前 dist 字节处，逐字节向前复制 len 字节（dist<len 时环回重复）。

## 推导 1：压缩与 Write 切法无关（语义 3）

编码器维护绝对位置 `pos`（下一个待编码字节）与窗口末尾 `frontier`。对 `pos` 的决定分三类：
(a) 可用字节 `frontier-pos < 3`：连哈希都算不出，且未来字节可能让此处产生匹配，**挂起**；
(b) 找到最长匹配且 `len == min(frontier-pos, maxMatch)` 且未达 `maxMatch` 上限：匹配一路延伸到数据边界，未来字节可能延长它，**挂起**；
(c) 其余（失配结束、达到 `maxMatch` 上限、无匹配输出字面量）：结果只依赖已到达字节，**立即定案**。
挂起时 `pos` 不动，等更多数据或 Flush/Close 后重试；重试时从 `pos` 重新跑 `Find`，等价于一次性看到全部数据，故定案序列只是「原文 + Flush 偏移」的纯函数，与 Write 切分无关。字面量段只在发出回指、达到固定上限或 Flush/Close 时切分，切分点同样只由数据决定。
对 Flush 的含义：Flush 承诺「截至目前的输入可全部被还原」，而挂起的匹配依赖未来字节，所以 **Flush 必须把当前边界当最终边界强制定案**（结束当前匹配、吐出字面量、写刷新标记）；否则要么违背承诺，要么 Flush 后无法继续挂起。

## 推导 2：重叠回指的解压复制（语义 2）

一次性块复制（memmove 语义）在 `dist < len` 时拷的是复制前的旧内容，得到的是「旧窗口 + 空洞」，而 LZ77 语义是边复制边产生新内容：`abc`+dist3/len30 应得 `abcabc…`。本实现让解压器把每个字节写入环形窗口后立刻可被读取，下一个读取自然看到刚写入的字节，环回重复由窗口不变量自动成立。复杂度：输出本身即 Ω(len)，逐字节复制每字节 O(1)，已是渐近最优；环形缓冲边界使任何块复制最多省常数，逐字节循环在此可接受且不退化正确性。

## 推导 3：「距离超已输出」与「距离超窗口」为何区分（语义 7）

窗口容量是**格式层**限制（解压器环形缓冲只能回看 W 字节），已输出是**历史层**限制（不能引用不存在的字节）。流早期 `total < W` 时二者不同：`dist ∈ (total, W]` 不越窗口但无历史。压缩器侧：匹配器候选位置 `c` 必满足 `pos-W ≤ c < pos`，故压缩器**绝不可能**产生 `dist > W`；而损坏流两种错误都可能出现，必须分别可判定。并行块压缩中「已输出」含义**不变**：预置字典 = 上一块末尾 ≤W 字节，恰等于解压器解到本块时已输出的字节，dist 两类检查语义与串行一致。

## 推导 4：并行块压缩的确定性（语义 9）

块 i 的压缩输入只有 `data[start:end]` 与预置字典 `data[start-W:start]`（上一块原文末尾），二者都是输入的纯函数；不读其他块的压缩结果，不依赖完成顺序。编码器对每块以 final 语义收尾（强制结束边界匹配、吐字面量），产物只是「字面量/回指」记录序列；worker 池只做调度，记录按块号定序拼接，块间插入刷新标记（末块后接流尾，流尾对整份原文计总长与校验和）。解压器看到的记录流与串行编码器产出同构，故 workers 取 1/2/4/8 输出逐字节相同。
