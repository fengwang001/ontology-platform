# 熔断与舱壁调用保护器 — 设计推导

## 1. 包划分与依赖方向
- `classify`：错误分类（可重试/不可重试/超时），无依赖。
- `bulkhead`：每下游独立的 N 个在途额度 + Q 个等待位；内部含历史峰值计数。
- `timeout`：单次调用时限，`context.WithTimeout` 包裹，panic 在此层捕获。
- `breaker`：关闭/打开/半开状态机，时钟为接口可注入。
- `stat`：聚合计数器 + `Protector`（编排三层，顺序见 §3）。
- `cmd/demo`：逐条判定演示。
依赖方向：`stat → {breaker,bulkhead,timeout,classify}`，无环。

## 2. 被拒绝调用不计入失败率
记窗口内真实调用样本 R、失败 F，则失败率 = F/R。被熔断拒绝的请求是保护器
自身行为，不携带任何关于下游当前健康度的新信息：若把拒绝计入 F，则
打开后拒绝越多 F/R 越接近 1，冷却到期进入半开且一次探测成功后，旧的高
失败率仍压在窗口里，无法回到关闭——状态机被自己的拒绝锁死。
因此真实调用数恒等于 `总请求 − 熔断拒绝 − 舱壁拒绝`；失败率与连续失败
只由真实调用的结果驱动。舱壁拒绝同理：它反映的是本地并发压力而非下游
故障，也不进熔断计数，只单独计入「被舱壁拒绝数」。

## 3. 熔断置于舱壁之前
顺序固定为：先判熔断，再取舱壁额度，再做真实调用。
- 熔断在前：下游已知故障时请求立即返回，不占用 N 个宝贵在途额度，故障
  期间额度可留给半开探测与恢复流量；打开态下舱壁占用恒为 0。
- 若舱壁在前：连「明知必败」的拒绝也要先排队占名额，故障期等待队列被
  垃圾请求填满，恢复请求反而进不来。故本题强制熔断在前。

## 4. 四条路径都必须归还额度
额度在「真实调用即将开始」时取得，调用结束只有四种出口：成功返回、
业务失败、超时（ctx deadline）、panic。任一出口漏还，N 个额度会逐步
泄漏至 0，此后所有请求都因舱壁满而被拒且无法自愈。因此用
`defer bh.Release()` 与 `defer recover()` 保证：panic 被捕获后转化为
一次失败结果（归入失败统计与熔断），再归还额度。

## 5. 不可重试错误是否计入熔断
计入，但作为失败的一个子类。论证：熔断的目的是在下游持续不能正常服务
时停止打它；不可重试错误（如 4xx 式确定性拒绝）同样说明「此刻调用不会
成功」，连续出现意味着下游在异常地拒绝服务，应触发保护。统计上按
类别细分（可重试/不可重试/超时/panic）各类失败数，其和等于总失败数；
熔断只关心「是否失败」，类别用于上报与排障。

## 6. 状态机
- 关闭：真实失败累加连续失败数 Cf 与窗口样本；连续失败达 `Fails`，或
  样本数 ≥ `MinSamples` 且失败率 > `Rate`，→ 打开。
- 打开：直接拒绝；`now ≥ openedAt + cooldown` 时 → 半开（重置冷却）。
- 半开：仅 `Probes` 个探测可取得在途名额，其余直接拒绝；探测全部成功
  → 关闭（清零计数与冷却）；任一失败 → 打开，冷却 = min(冷却×2, 上限)。
- 所有迁移在互斥锁内完成并对迁移计数加一；加倍冷却只在该次迁移发生，
  并发失败不得重复加倍。
- 时钟回拨：打开态检查时若 `now` 小于上次判定时间，返回可判定错误
  `ErrClockBacktrack`，状态与冷却均不变。

## 7. 舱壁语义
在途计数 ≤ N，内部记录历史峰值。Acquire：有空位立即占；否则等待位
< Q 时入队等待（可被 ctx.Done 移除并归还等待名额）；等待位也满则
立即返回 `ErrBulkheadRejected`，不等待、不推进时钟。不同下游各自
持有独立 Bulkhead，互不可见。

## 8. 统计口径（不变量）
1. 总请求 = 成功 + 失败 + 熔断拒绝 + 舱壁拒绝
2. 真实调用 = 总请求 − 熔断拒绝 − 舱壁拒绝
3. Σ各分类失败数 = 失败数
每个请求恰落入四类出口之一，计数在同一编排函数内串行完成，故不变量
对任意操作序列成立。

## 9. 错误体系
哨兵错误：`ErrBreakerOpen`、`ErrBulkheadRejected`、`ErrClockBacktrack`、
`ErrPanic`，均可用 `errors.Is` 判定；业务错误经 classify 包装为
可重试/不可重试/超时三类。配置阈值 ≤ 0 时构造返回错误。
