# 分词器设计（lex / words）

## 第 2 条推导：双引号内反斜杠何时是转义
POSIX 2.2.3：双引号内反斜杠仅在后跟 `$`、`` ` ``、`"`、`\`、换行时是转义符。依据：
双引号内仍具特殊含义的字符只有这五个——`$`/`` ` `` 触发展开、`"` 结束引号、`\` 自身、
换行用于续行；反斜杠的职责只是"取消下一个字符的特殊含义"，对其余字符无特殊含义可
取消，故原样保留（`"a\b"`→`a\b`）。本工程不做展开，但转义规则不变：`"\$"`→`$`。

## 第 4 条推导：空词
词（token）由"是否开始过"判定，而非"是否有字节"：引号对即使为空也构成 token 的一
部分，并与相邻无引号部分拼接；纯空白只分隔、不产生 token。结论：`""''`→1 个词（空
串，两个空引号拼进同一词）；`a""`→1 个词 `a`；`a '' b`→3 个词 `a`、``、`b`；
`\<换行>` 单独一行→0 个词：反斜杠-换行是续行（POSIX 2.2.1），分词前被删除，不构成
token，也不会"开始"一个词。

## 状态转移表（状态 × 字节类别 → 下一状态 / 动作）
| 状态 | 字节 | 下一状态 | 动作 |
|---|---|---|---|
| N 普通 | 空白 | N | 词已开则 End |
| N | `'` / `"` | S / D | Begin，记开引号偏移 |
| N | `\` | EN | 记反斜杠偏移 |
| N | 其他 | N | Begin；Emit |
| S 单引号 | `'` / 其他（含 `\`） | N / S | — / Emit |
| D 双引号 | `"` / `\` / 其他 | N / ED / D | — / — / Emit |
| EN 转义待定 | 换行 / 其他 | N | 续行无输出 / Begin；Emit |
| ED 双引号内待定 | `$` `` ` `` `"` `\` / 换行 / 其他 | D | Emit / 续行 / Emit `\`+该字节 |
| EOF | S / D,ED / EN / N | — | ErrSingle@开引号 / ErrDouble@开引号 / ErrEscape@反斜杠 / 词开则 End |

## 测试对照表
| 语义 | 钉住它的测试 |
|---|---|
| 1 单引号全字面 | TestSplit/sq_* |
| 2 双引号五种转义与保留 | TestSplit/dq_* |
| 3 无引号转义与续行 | TestSplit/unquoted_* |
| 4 拼接与空词 | TestSplit/concat_*、empty_*、continuation_only |
| 5 三类错误与偏移 | TestSplitErrors |
| 6 任意切分点一致 | TestAllSplitPoints |
| 7 往返与 Quote 写法 | TestRoundTrip、TestQuote |
| 计数器线性（==字节数） | lex.TestStepCountIsLinear |
