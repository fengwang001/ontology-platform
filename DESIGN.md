# DESIGN

## 1. 零计数时的 hunk 头行号

统一规则：`a` = hunk 覆盖区间**前一行**的 1 基旧文件行号；没有前一行时为 0。
即 hunk 覆盖的旧行是 `(a+1 .. a+b)`。新侧 `c` 同理。

- `x` → `yx`：纯插入，旧区间为空且其前没有行，故 `a=0,b=0`；新区间为第 1 行，`c=1,d=1` → `-0,0 +1`。
- `abc` → `abXc`：在旧第 2 行与第 3 行之间插入 X，空旧区间的前一行是旧第 2 行 → `a=2,b=0`；新侧只有 X 一行 → `+3`。
- `a\n` → 空：旧区间为第 1 行（`a=1,b=1`，省略 `,1`），新区间为空且其前无行 → `c=0,d=0`。

数字是「前一行行号」而非「插入位置」，是因为 unified diff 行号一律从 1 开始，必须用 0 这个越界哨兵表达「文件开头之前」。
Apply 时插入点 = 第 `a` 行之后（`a=0` 即文件开头；`a` 等于旧文件总行数即文件末尾）。

| 旧侧情况 | 书写 | 含义 / Apply 插入点 |
|---|---|---|
| b=0 | `-a,0` | 不消费旧行；插入点在旧第 a 行之后（a=0 → 文件头） |
| b=1 | `-a`（省略 ,1） | 替换/删除旧第 a 行；插入点在旧第 a 行处 |
| b≥2 | `-a,b` | 消费旧第 a..a+b-1 行；插入点在旧第 a+b-1 行处 |

b=0 时 a 是「前一行行号」，b≥1 时 a 是「首行行号」，相差 1 是 0 计数的特殊约定。

## 2. hunk 合并阈值

相邻两段改动之间未改动行数为 g，每侧上下文 C 行。**阈值 g ≤ 2C 时合并**（GNU diff -U C 规则：两个 hunk 的上下文区间重叠或相邻即合并；共享上下文条件为 g ≤ 2C）。
C=3：g=6 合并（前 hunk 吃到后 3 行、后 hunk 吃到前 3 行，上下文刚好相接无重复），g=7 分开。
若阈值取 C：g=4（< 2C+1，GNU 会合并）会被错误分开；取 2C+1：g=7（GNU 中两 hunk 间还剩 1 行独立上下文，分开）会被错误合并。

## 3. Myers 并列最短路径的选择

每一步 d 对每条对角线 k 选择前驱时：`k == -d` 或（`k != d` 且 V[k-1] < V[k+1]）取向下（插入）前驱，否则取向右（删除）前驱。
即**并列时优先删除**（`ab → ba` 先产出 `-a` 再 `+a`，删除行排在插入行之前）。此选择固定写入实现，保证确定性。
计数器只统计前向搜索中 snake 的逐行对角前进步（含比较失败那一次），回溯不计。

## 4. 偏移后后续 hunk 的查找

第 i 个 hunk 在记录行号外偏移 oi 行应用成功后，后续 hunk 的查找起点平移「该 hunk 成功位置相对于其头声明位置的累计位移」：新起点 = 头声明起点 + Σoi（oi 为实际落点减声明落点，按旧文件行坐标）。
因为偏移只改变行的绝对坐标、不改变 hunk 之间的相对间距；在平移后的起点 ±F 内搜索，最近优先、同距靠前。
