# DESIGN

## 1. 零计数 hunk 头行号

统一规则：`-a,b` 的 `a` = hunk 首个旧行的 1 基旧文件行号；`b=0`（无旧行，纯插入）时没有“首个旧行”，`a` 取插入点前一行的旧行号，插入点在文件开头则为 0。

- `x` → 开头插入 `y`：插入点前无旧行，故 `-0,0`；新侧第 1 行是 y，`+1`。
- `a b c` → `a b X c`：X 插在 b 之后，插入点前一行是第 2 行，故 `-2,0 +3`。
- 删除全部 `a`：hunk 含旧行 1（b=1 省略），删除点位于第 0 行之后，故 `-1 +0,0`。

| 旧侧 b | 头部书写 | Apply 的解释 |
|---|---|---|
| b=0 | `-a,0`，a=插入点前一行行号（开头为 0） | 新内容插在旧第 a 行之后（a=0 即开头） |
| b=1 | `-a`（省略 ,1） | 从旧第 a 行起取 1 行替换 |
| b≥2 | `-a,b` | 从旧第 a 行起取 b 行替换 |

## 2. hunk 合并阈值

阈值是 2C：两段改动间隔 g 条未改动行，g ≤ 2C 时合并。

反例（C=1）：阈值若为 C=1，则 g=2 时会分两个 hunk，但 GNU diff -U1 给一个含 2 行公共上下文的单 hunk；阈值若为 2C+1=3，则 g=3 时 GNU diff -U1 分两个 hunk，取 3 却会合并。取 2C：g=2 合并、g=3 分开，与 GNU 一致。C=0 时阈值 0，有公共行即分开。

## 3. Myers 并列选择

并列时删除优先：k == -d 或（k != d 且 down 端 ≥ right 端）时从 k+1 对角线下来（删旧行），否则从 k-1 对角线右行（插新行）。因此 `a b → b a` 输出 `-a`、` b`、`+a`，删除整段排在插入之前（delete-before-insert）。选择固定，保证逐字节确定性。

## 4. 偏移后后续 hunk 的查找起点

前一 hunk 以偏移 δ（实际起点 − 记录起点）应用后，后一 hunk 的记录起点整体平移 δ 再作查找中心。因为前一 hunk 使其后内容整体位移 δ；不平移会让记录位置系统性偏离真实位置。窗口仍为平移后起点上下各 F 行，候选最近、等距靠前者胜。

## 5. 行模型

每行保留原始字节（含行尾），含 \n、\r\n、无尾三种；拼接即原文。相等比较含行尾，故仅 CRLF↔LF 或末尾换行差异也产生非空脚本。

## 6. 复杂度

edit 维护非导出计数器，仅在 snake 逐行比较成功前进一步计数；上界 ≤ 4(N+M)(D+1)。编辑距离超配置上限立即返回“差异过大”。
