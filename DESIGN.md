# RLE 设计说明

不变量：严格解码器接受的每个 `t` 都满足 `Encode(Decode(t)) == t`。据此推导必须拒绝的情形：

1. **显式次数 1**（如 `1a`）：Decode 得 `a`，Encode 省略次数 1 得 `a != 1a`，故拒绝。
2. **次数 0**（如 `0a`）：游程次数恒 ≥1；Encode 永不会产生 0，且 `a` 重复 0 次为空，无对应编码，故拒绝。
3. **前导零**（如 `01a`、`00a`）：规范十进制无前导零，Encode(`a`)→`a`、Encode(11 个 a)→`11a`，任何 Encode 结果都不含前导零；`0` 单独即情形 2，故拒绝。
4. **相邻游程同符号**（如 `2a3a`）：Decode 得 5 个 a，Encode 按最长游程合并得 `5a != 2a3a`；因此一个符号必须「看到下一个游程的符号」后才能提交输出，故拒绝。
5. **非法转义**（`\x`，x 非数字非 `\`）：转义只用于数字与反斜杠；Decode 无法确定该符号，且任何 Encode 结果不含此形式，故拒绝。
6. **末尾孤立 `\`**：转义序列不完整，无符号可解码，故拒绝。
7. **末尾只有次数**（如 `12`、`a2` 末尾）：每个游程必须以符号收尾，次数后无符号不是合法编码，故拒绝。
8. **非法 UTF-8**：符号是 Unicode 码点，非法字节序列无法成为任何 Encode 输出；跨 Write 的不完整序列需缓冲续接，最终仍不完整即拒绝。

以上错误均为彼此可区分的哨兵错误，包装在带**字节偏移**（出错 token 在总输入中的起始字节）的错误类型中。

## 大次数取舍：拒绝

选择**拒绝**而非 big.Int 支持，理由：
- 输出大小必须与次数成正比（游程就是重复展开），次数超过 `int` 上限时输出本身无法在内存中表示，接受也无法交付结果。
- 上限取 `math.MaxInt`（64 位平台为 9223372036854775807）：这是切片/字符串长度的天然上限。
- 累加数字时逐位做溢出检查（`n > (MaxInt-d)/10` 即拒绝），**先拒绝再继续**，绝不发生静默回绕。
- 解码**流式推进**：读取次数时不分配与次数成比例的内存；仅在游程提交（符号已确认合法）前按累计输出字节做配额检查，超限返回可判定的 `ErrOutputLimit`，默认上限 64 MiB，可通过 `NewDecoder` 配置。
