# 句柄表设计推导

句柄编码：`uint64 = [tableID:16位][代号gen:20位][槽位slot:28位]`。
tableID 由全局原子计数器从 1 开始分配；槽位代号从 1 开始递进，上限 `MaxGen = 2^20 - 1`。

## 1. 代号回绕怎么办（核心推导）

**若允许回绕，不变量 1 必被破坏。** 具体操作序列（容量为 1 的表）：

1. `Insert` → 发出 `h1 = (tid, gen=1, slot=0)`；
2. `Remove(h1)` → 槽位 0 代号递进为 2；
3. `Insert` → 发出 `h2 = (tid, gen=2, slot=0)`；`Remove(h2)` → 代号递进为 3；
4. 如此重复 `MaxGen-1` 轮，槽位 0 的代号从 `MaxGen` 回绕到 1；
5. 此时老句柄 `h1` 的代号字段（1）与槽位当前代号（1）相等，`Get(h1)` 校验通过——老句柄复活，再 `Insert` 后 `h1` 将静默读到新对象的值。不变量 1 被破坏。

结论：回绕意味着任意老句柄至多经 `MaxGen` 次复用就会复活，与「永不复活」直接矛盾，**故代号不得回绕**。

**处理办法：代号触顶即退役（exhausted）。** 槽位代号到达 `MaxGen` 后：不再递进、不再回空闲链表、不再发放句柄；对匹配该末代代号的访问返回 `ErrSlotExhausted`，代号不匹配的访问返回 `ErrStale`。

- 不破坏不变量 1：代号在 `Remove` 时递进、`Insert` 时按当前代号发放，故末代（`MaxGen`）句柄确实会发出；但它被 `Remove` 后槽位即退役，状态恒为 `Exhausted`，再访问命中状态检查返回 `ErrSlotExhausted`；更早的老句柄代号必然小于 `MaxGen`，与槽位代号不等，返回 `ErrStale`。两类句柄都永不再校验通过。
- 不破坏不变量 3：把退役槽位列为与「在用 / 空闲」并列的第三类，等式改为 `Len + Free + Exhausted == Cap`，由自检方法逐槽核验。

## 2. 代号起点与零值句柄（由不变量 5 推导）

零值句柄是 `uint64` 全零。要让它永不有效，编码中至少一个字段必须保证非零：

- tableID 从 **1** 开始分配 ⇒ 任何真实表发出的句柄都含非零 tableID ⇒ 编码必非零；
- 代号从 **1** 开始（而非 0）⇒ 「槽位 0 的第 0 代」这个编码 `(tid, 0, 0)` 对应的句柄从未被发出过，不会与零值混淆；即使伪造，校验时代号与槽位当前代号（≥1）不等，判失效。

两道防线各自独立成立：零值因 tableID=0 被 `ErrZeroHandle` 拦截；「第 0 代」编码因代号从 1 起步而永不合法。

## 3. 空闲链表：头插还是尾插

两者都是 O(1)。区别在于代号耗尽速度：

- **头插（LIFO）**：刚释放的槽位立刻被下次 `Insert` 复用。容量 k 的表在持续插入/删除下，全部复用压力集中在同一个槽位上，其代号每次操作 +1，约 `MaxGen` 次操作后该槽位退役，随后热点顺次烧毁其余槽位，整表容量持续流失。
- **尾插（FIFO）**：复用压力均摊到 k 个槽位，单槽代号每 k 次操作才 +1，首个槽位退役需要约 `k × MaxGen` 次操作。

**选择尾插。** 这与第 1 条的「耗尽即退役」一致：退役是不可逆的容量损失，应尽量推迟；尾插把耗尽时间放大了 k 倍。

## 4. 同一失效句柄连用两次

两次必须返回**相同**的错误（`ErrStale`）。推导：不变量 4 要求被拒操作不得改变任何可观测状态。若第二次返回可区分的「已处理过」错误，表就必须额外记录「该句柄已被查询过一次」这一事实——这本身就是可观测状态的变化，违反不变量 4。因此失效句柄的每次访问都无状态、等结果。
