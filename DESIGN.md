# RLE 严格编解码设计

不变量：严格解码器接受的每个 t，都满足 `Encode(Decode(t)) == t`。

## 拒绝清单推导（给出偏移的 `DecodeError`，哨兵彼此可区分）

1. 显式次数 1（`1a`）：解码为 a，重编码为 `a`，与 `1a` 不同。
2. 次数 0（`0a`）：空游程无对应符号输出；且次数 0 不是规范写法，拒绝。
3. 前导零（`01a`、`00a`）：规范十进制无前导零，重编码得到 `a`/`0`，不等。
4. 相邻同符号游程（`2a3a`）：解码为 5 个 a，重编码为 `5a`，不等。
5. 非法转义（`\x`）：反斜杠后只能是数字或反斜杠，其他字符无合法解码。
6. 末尾孤立 `\`（`a\`）：转义不完整，无完整游程。
7. 末尾只有次数（`12`）：游程必须以符号收尾，无符号无法输出。
8. 非法 UTF-8（`\xff` 等）：符号是 Unicode 码点，非法字节无对应码点。
9. 次数超上限：见下节。

## 大次数取舍：拒绝（上限 MaxCount = math.MaxInt）

按字节累加解析次数，每步检查 `n > (MaxInt-d)/10`，超限在首位数字的偏移返回
ErrCountTooLarge，绝不静默溢出。拒绝的理由：一次游程即需 MaxInt+1 个输出，
在 64 位平台无法由 Go 字符串容纳；支持它没有可表示的结果。解码输出总量受
可配置上限限制（默认 64 MiB，`WithMaxOutput`），超限返回 ErrOutputLimit，
且只按已确定的游程扩张内存，绝不预先按次数分配。

## 语义与测试对照
