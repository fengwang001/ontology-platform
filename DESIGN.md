# DESIGN

## 第3条推导：续行接上的行只含空白或为空

依据 `java.util.Properties` 的 `LineReader.readLine`：读到行尾奇数个反斜杠时，
先消费掉其中一个（`len -= 1`），再读下一物理行，并逐个跳过其前导空白
（skipWhiteSpace）。若下一行全是空白或为空：所有字符被跳过，随后遇到的换行
触发"当前逻辑行结束"——因为累积缓冲非空（如 `k=v`），且行尾反斜杠已被消费，
不再续行。结论：`k=v\` + 空白行 → 逻辑行就是 `k=v`，即 `k`→`v`；该空白行
凭空消失，既不产生空逻辑行，也不让续行链继续。这与"续行首部空白被剥掉"是
同一条规则的极端情形：剥光后没有内容可接，而反斜杠在上一行已被消费，续行
链自然终止。例外：若该行为"空白+奇数个反斜杠"（如 `  \`），剥掉前导空白后
剩下的反斜杠会重新触发续行——因为奇偶判断发生在剥离之后。

## 第6条推导：Store 的转义规则（只转义必要字符）

由解析侧反推：键的结束标志是"未转义的 `=`、`:` 或空白（空格/制表/换页）"；
注释判定只看逻辑行第一个非空白字符；值在分隔符之后原样保留，解析器只在
分隔符后跳过一次空白，进入值后不再剥离任何字符（尾部空白也保留）。
- 键：`\` 必须转义（否则被当作转义前缀）；`=`、`:`、空格、`\t`、`\f` 必须
  转义（否则提前结束键）；`\n`、`\r` 必须转义（无法出现在一个物理行内）；
  键首的 `#`、`!` 必须转义（否则整行被当注释）。键中间的 `#`/`!` 不必转义。
- 值：只需转义 `\`、`\n`、`\r`，以及**开头**的空白。值中间的空格不必转义，
  因为解析器进入值后不再吃空白；值开头的空白必须转义，否则会被"分隔符后
  跳空白"那一步吃掉。值不需要转义 `#`/`!`（注释判定发生在拆键值之前且只
  看行首），也不需要转义 `=`/`:`（分隔符查找在键结束时已完成）。
- 非 ASCII 按 UTF-8 原样输出（对应 `store(Writer)` 的 escUnicode=false）。

## 语义 ↔ 测试对照表

（待测试写完后补齐）
