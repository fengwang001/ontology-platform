# DESIGN — 熔断与舱壁隔离的调用保护器

## 0. 分层与调用链

请求路径：`Protector.Do` → 熔断判定 → 舱壁取额度 → 时限内执行真实调用 → 统计落账。
包依赖：`breaker` → {`bulkhead`, `timeout`, `stat`, `classify`}，后四者互不依赖（`stat` 仅依赖 `classify` 的错误类别）。

## 1. 推导：被熔断拒绝的调用不得计入失败率

设失败率 `r = 失败数 / 真实调用数`，熔断打开条件含 `r >= 阈值`。
反设把「被熔断拒绝」也计为失败：在打开状态下，每一次拒绝都使失败数 +1，
而真实调用数为 0，于是 `r` 被自己的拒绝单调推向 100%，打开条件永远成立——
即使下游已经恢复，熔断也永远无法闭合（自我锁死）。
更根本地：拒绝发生在调用之前，不携带任何关于下游健康度的信息，
不能作为评估下游的样本。
**口径**：只有真正执行了 `fn` 的调用才产生成功/失败样本；
熔断拒绝与舱壁拒绝只计入各自的拒绝计数，不进入失败率分子分母。
推论：打开期间统计中 `真实调用数` 与 `失败数` 冻结不变，
冷却结束进入半开后，一次成功探测即可闭合（测试构造「打开后连续 1000 次被拒」验证）。

## 2. 论证：熔断置于舱壁之前

两种顺序对比：
- **熔断在前**：打开期间拒绝是纯内存状态判断，不占用并发额度、不进等待队列，
  舱壁占用恒为 0（测试断言）；下游恢复后额度全部可用于真实流量。
- **舱壁在前**：注定被拒的请求也要先排队/占额度。故障期间 N 个额度与 Q 个
  排队名额被「等死」的请求占满，熔断恢复瞬间真实请求反而排不进队；
  且排队请求持有上游连接，放大上游资源消耗。
结论：熔断是「免费拒绝」，必须放在「昂贵准入」（额度+队列）之前。
代价是熔断判定成为每请求热点，用互斥锁内 O(1) 判断即可，成本可忽略。

## 3. 论证：额度必须在成功/失败/超时/panic 四条路径上归还

额度总量 N 固定，只借不还则可用额度单调递减至 0，此后所有请求被舱壁
拒绝——保护器自身退化为永久拒绝器，且无任何外部信号可恢复。
四条路径逐一分析：
- 成功/失败：`fn` 正常返回，释放显而易见；
- 超时：`timeout.Do` 在截止时间先返回，但 `fn` 的 goroutine 可能仍在跑，
  额度归保护器所有、与 `fn` 生命周期解耦，超时返回时必须释放；
- panic：若不 recover，恐慌沿栈上穿会跳过普通返回路径，必须在 `fn`
  最近的边界 recover 转为 error，使 defer 释放链正常展开。
实现：获取额度成功后立即 `defer Release()`，四条路径汇聚到同一 defer。
测试在四条路径上各跑 1000 次，断言在途数归零、额度回满。

## 4. 定义与论证：不可重试错误计入熔断

**定义**：熔断统计的是「调用是否成功」，与失败是否可重试无关；
可重试、不可重试、超时三类错误全部计入失败。
论证：
1. 熔断器保护的是下游与系统整体，不是重试调度器（本题明确不实现重试）。
   不可重试的失败（如参数错误 4xx）同样消耗下游资源、同样表示本次调用
   未完成；持续发生时仍需熔断止损。
2. 若不计入，缺陷性或攻击性的不可重试流量可无限击穿到下游。
3. 错误分类的作用体现在统计细分（`stat` 按类别计数）与调用方决策，
   而非改变熔断口径。未知错误默认按可重试归类（保守视为瞬时故障）。

## 5. 熔断状态机精确语义

- 关闭 → 打开（二选一，均实现并分别测试）：
  a) 连续失败数 >= `ConsecutiveFailures`；
  b) 样本数 >= `MinSamples` 且 失败率 >= `FailureRate`。
  窗口为「自上次闭合起」的累计计数，打开时清零。
- 打开 → 半开：`now - openedAt >= cooldown`（注入时钟），该请求占第一个探测名额。
- 半开 → 关闭：`HalfOpenProbes` 个探测全部成功。
- 半开 → 打开：任一探测失败立即打开，`cooldown = min(2*cooldown, MaxCooldown)`；
  闭合时冷却重置为基准值。仅半开→打开这一条路径加倍，杜绝重复加倍。
- 半开并发：同时在途探测 <= `HalfOpenProbes`，超出立即拒绝（`ErrOpen`）。
- 并发迁移：所有状态迁移在互斥锁内完成并计数，N 个协程并发触发失败时
  迁移恰发生一次、冷却恰加倍一次（计数器断言）。
- 时钟回拨：打开状态下 `now < openedAt` 返回 `ErrClockBackward`，状态不变；
  `now` 落在 `[openedAt, openedAt+cooldown)` 内返回 `ErrOpen`，
  回拨不可能导致提前半开。
- 探测名额泄漏防护：熔断放行后若舱壁拒绝（未形成真实调用），
  调 `OnAbort` 归还探测名额，不计成功也不计失败。

## 6. 统计口径自洽

`stat.Recorder` 在入口记 `Total`，每个请求恰好落入唯一终态：
`Success` / `Failure(kind)` / `BreakerRejected` / `BulkheadRejected`
（等待中被上游取消按未形成真实调用归入舱壁拒绝）。
由「每请求恰一个终态」直接推出任意操作序列后：
- `Total = Success + Failure + BreakerRejected + BulkheadRejected`
- `Real = Total - BreakerRejected - BulkheadRejected`
- `FailureRetryable + FailureNonRetryable + FailureTimeout = Failure`

## 7. 四类可判定错误（errors.Is）

- `breaker.ErrOpen`：熔断拒绝（含半开探测名额满）
- `breaker.ErrClockBackward`：时钟回拨
- `bulkhead.ErrFull`：舱壁队列满，立即拒绝
- `timeout.ErrPanic`：调用 panic 被捕获转为失败
（超时经 `context.DeadlineExceeded` 判定，归入 `classify.Timeout`。）
