# 可分裂二维网格索引 — 设计推导

## 1. 边界归属（唯一规则）

- 单元格为左闭右开 `R=[x0,x1)×[y0,y1)`。包含：`x0<=x<x1 && y0<=y<y1`。
- 分裂线取 `mx=(x0+x1)/2, my=(y0+y1)/2`，四象限（子格仍左闭右开）：
  - SW `[x0,mx)×[y0,my)`；SE `[mx,x1)×[y0,my)`；
  - NW `[x0,mx)×[my,y1)`；NE `[mx,x1)×[my,y1)`。
- 点恰在分裂线 `x==mx`：归入东格（SE/NE，由 y 决定）；`y==my`：归入北格；
  两者同时（中心）→ NE。因左闭右开，每个点恰入一个子格，不重不丢。
- 父格四个角：左下/左上归本格（边界）；右上 `(x1,y1)` 不在任何格内；
  等于上界 x1 或 y1 的点不属于本格（世界框本身左闭右开）。
- 查询矩形 `Q` 同样左闭右开，命中判据 `Q.x0<=p.x<Q.x1 && Q.y0<=p.y<Q.y1`：
  - 点在左/下边界 → 命中；点在右/上边界 → 不命中；退化为线/点（x0==x1 等）
    → 任何点都不命中（区间为空），属合法输入。
- 格与查询的关系：相离 `x1<=Q.x0 || x0>=Q.x1 || y1<=Q.y0 || y0>=Q.y1` 跳过；
  完全包含（`Q.x0<=x0 && x1<=Q.x1` 且 y 同理；右侧等号安全，因点严格 `<x1`）
  → 整取，格内全部点直接进结果、不逐点判；其余为相交 → 递归，叶格逐点判。

## 2. 分裂终止条件（推导）

- 触发：叶格点数 > cap 且「至少一个维度可再分」（`mx>x0 && mx<x1`，y 同理）。
  若某维退化（`x0==x1`），只在可分的一维做中线，另一维整体保留。
- 全同坐标点：设点 p 恰在 `mx`（浮点加法/2 常见情形），则每次都落 NE，NE 再次
  超容、再分裂、p 仍走同一支 → 无限递归。终止条件：若全部点都挤入同一子格
  （中线对这批点不可再分），放弃分叉，回退为不分叉的超载叶格；此外深度达到
  maxDepth=32（约 1e-9 相对精度，兜底防浮点怪值）后也不再分。结论：全同 1000
  点停在一个超载叶格，查询照常逐点返回。
- 点带唯一 ID：完全同坐标也可区分、删除与计数；`±0` 视为相等（IEEE 754 本就
  `+0==-0`，不另作处理）。

## 3. 不合并的理由

删除后子树总点数 <= cap 时**不**合并。合并需把四子格点重新上提并原子替换，
插入/删除抖动于阈值附近时会反复 split/merge（昂贵且需协调并发）；本索引只
承诺查询复杂度不随删除恶化——空枝查询仅 O(树深)，碎片化代价有限，故以简单性
与稳定性为先，不做合并。

## 4. 并发与一致性

`grid` 用 `sync.RWMutex`：查询持读锁、插入/删除持写锁；分裂在写锁内完成
「下沉全部点 + 原子挂载子指针」。查询看到的要么是分裂前整格、要么是分裂后
完整四格，点不会同时存在两层或消失；结果即某一已提交快照下矩形内点的精确集合。

## 5. 输入校验

`NaN`、`±Inf`、世界框外（含等于右上界）的点拒绝插入，计数在
`grid.Rejected()` 中可读回。

## 6. 持久化格式

- 头：魔数 `ONTG`、版本 u16=1、世界框 4×float64(LE)、cap u32、maxDepth u32，共 52B。
- 先序遍历，逐格记录：tag u8(0=叶/1=分裂)、边界 4×f64、深度 u32、
  点数 u32、点载荷(ID u64,x,y f64 ×n)、记录体 CRC32(u32)。
  分裂格后紧跟四棵子树；重建时 tag 决定子树是否必须存在（结构不变量）。
- 截断分类：头 <52B = 头部不完整；落在记录头(33B)/点载荷内 = 对应不完整；
  落在 CRC 4B 或缺 CRC = CRC 不匹配。恢复：逐记录先序解析，完整且 CRC 合法
  才提交，遇第一条坏记录即停（其后无法定位，即最大可恢复前缀）；若分裂父缺子，
  父降级为叶（仅保留自身载荷点），保证树完整。
- 结构校验：tag=1 却缺子格 → 报父格；子格边界不被父格包含 → 报子格及越界维。
- 写盘：写 `name.tmp`、fsync 后 `os.Rename` 原子改名；恢复时识别并清理残留
  `*.tmp`。
