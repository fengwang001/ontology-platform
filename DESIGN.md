# DESIGN

自定格式 LZ77 流式压缩。包依赖单向：`wire` ← `enc`/`dec`，`window` ← `match`/`dec`，`match` ← `enc`。

## 流格式记录布局

| 记录 | 字节布局 |
| --- | --- |
| 流头 | `4C 5A 01`（魔数 'L''Z' + 版本 1），定长 3 字节 |
| 字面量段 | `varint(1) varint(n)` + n 字节原文 |
| 回指 | `varint(2) varint(dist) varint(len)` |
| 刷新标记 | `varint(3)`，无负载 |
| 流尾 | `varint(4) varint(原文总长) varint(FNV-1a/64 校验和)` |

varint 为 little-endian base-128（同 `encoding/binary.Uvarint`），最多 10 字节，第 10 字节不得大于 1。

## 推导一：压缩结果与 Write 切法无关（第 3 条）

`Write` 返回后调用方可能继续写，当前末尾的字面量段可能变长、末尾位置的最长匹配可能
被后续字节延长。因此**在 Write 边界上，没有任何一个输出决定是安全的**：多缓存 1 字节
就可能改变前一个位置的最长匹配。结论：未决字节 = 自上次 Flush 以来的**全部**输入，
压缩器把它们攒在 `pend` 里，一个记录都不输出。只有 Flush/Close 是决策点——此时调用方
承诺「此刻之前的输入必须可还原」，匹配才能定界。输出因此只是「累计输入 + Flush 偏移
序列」的函数，与 Write 切法无关。对 Flush 的推论：Flush 必须**强制结束**当前匹配与字面
量段（在 Flush 偏移处截断，哪怕再等一个字节可能得到更长匹配），否则 Flush 的承诺无法
兑现；Flush 后保留窗口容量的历史尾部作字典，后续匹配可回指 Flush 之前的数据。

## 推导二：重叠回指的解压复制（第 2 条）

距离 < 长度时，块复制（一次性 memmove len 字节）的源区间包含**尚未写出**的字节，结果
错误；逐字节向前复制则把最近 dist 字节周期性地重现，正是 LZ77 语义（dist=3,len=30 还
原为 `abc` 重复 10 次）。本实现用环形窗口逐字节复制：每次 `Append` 是 O(1) 摊还，复制
总长 O(len)，与任何块复制同渐近阶；且解压器本来就要把每个输出字节喂给窗口与校验和，
循环体没有多余开销。故逐字节循环在此可接受，无需分块倍增优化。

## 推导三：「距离超过已输出」与「距离超过窗口」为何是两个错误（第 7 条）

窗口容量是**资源限制**（环形缓冲能记住多远），已输出字节数是**流合法性**（不能引用还
不存在的字节）。二者独立：流早期 dist≤cap 但 dist>已输出；流后期小窗口下 dist≤已输出
但 dist>cap。压缩器绝不可能产生后者：匹配器只在距当前位置 `cap` 以内走候选链
（`pos-p <= cap` 守卫），发出的 dist 必然 ≤ cap。并行块压缩中，预置字典是上一块原文
尾部，它在流里先于本块记录被解压器真实输出，所以「已输出字节数」的含义不变——字典
不是隐藏状态，只是解压器已经产出过的历史。

## 推导四：并行块压缩的确定性（第 9 条）

块 i 的压缩输出 = f（块 i 原文，块 i-1 末尾窗口容量字节，配置）。三者都是**原始输入**
的切片，不依赖任何块的压缩结果、完成顺序或调度。因此 workers 取任意值输出逐字节相同。
衔接方式：流 = 流头 + 各块记录顺序拼接 + 流尾。记录自定界（每个 varint 字段长度已知、
字面量自带长度），块间**不需要**刷新标记；块 i+1 的回指指向块 i 尾部，对解压器就是普
通的「回指已输出字节」。流尾的总长与校验和覆盖完整原文，在所有块完成后计算。
