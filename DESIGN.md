# 严格 RLE 设计

不变量：严格解码器接受的每个 `t` 都满足 `Encode(Decode(t)) == t`。

1. 显式次数 `1`：接受 `1a` 会解出 `a`，但 `Encode("a") == "a"`，不是 `1a`。
2. 次数 `0`：游程必须至少含一个符号；`0a` 不是任何非空游程的规范编码。
3. 前导零：接受 `01a` 会解出 `a`，规范结果是 `a`；`00a` 等十进制也不合规。
4. 相邻同符号：接受 `2a3a` 会解出五个 `a`，编码器必须输出最长游程 `5a`。
5. 非法转义：`\x` 若按 `x` 接受，规范编码是 `x`；转义只允许数字和反斜杠。
6. 末尾孤立 `\`：转义符后没有符号，不构成完整游程。
7. 末尾只有次数：`12` 缺少符号，不构成完整游程；空输入才编码为空。
8. 非法 UTF-8：解码结果必须是合法字符串；若替换成 RuneError，再编码也不会等于原字节。

大次数选择“支持任意精度次数”，次数用 `math/big.Int` 累加和比较，不经过 `int64`，
因而不会静默溢出。解码器先只保存次数和一个码点，并在实际写出前比较可配置的输出总字节
上限；超限即返回可判定错误，绝不按次数预分配内存。

待补：语义与测试函数对照表。
