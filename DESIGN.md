# 设计推导

## 非法单元

统一采用 WHATWG“最长合法前缀”规则：首字节决定期望长度；先检查第二字节是否在该首字节允许区间。若第二字节非法，首字节单独成一个非法单元，其后字节重新从自身解析；之后连续字节只要不是当前序列期待的 `80..BF`，当前序列已读字节合为一个非法单元，并从那个字节重新解析。续字节自己出现时各占一个单元；截断时已缓存的一个合法前缀只占一个单元。

因此 `F0 90 80 41` 中 `41` 不是续字节，`F0 90 80` 合成一个单元；`E0 80 80` 的首字节后第二字节非法，之后两个字节又各自非法；`C0 AF`、`ED A0 80` 同理首字节先行；`E2 82` 结束时是一个未完成合法前缀。该规则保证每输入字节只检查一次，切分缓存只需保存“一个首字节 + 最多 3 个续字节”，即 3 字节。看到第二字节越界即可立即放弃首字节；看到期望序列中的非续字节即可结算旧前缀并重新处理该字节，无需等流结束。

| 首字节 | 第二字节合法区间 | 非法时吞掉字节 |
|---|---:|---:|
| `C2..DF` | `80..BF` | 1；合法前缀至 EOF 吞已缓存长度 |
| `E0` | `A0..BF` | 1 |
| `E1..EC` | `80..BF` | 1 |
| `ED` | `80..9F` | 1 |
| `EE..EF` | `80..BF` | 1 |
| `F0` | `90..BF` | 1 |
| `F1..F3` | `80..BF` | 1 |
| `F4` | `80..8F` | 1 |
| `80..BF` | 无 | 1 |
| `C0 C1 F5..FF` | 无 | 1 |

## 切分与 par

流式解码器持久保存未完成序列；后续字节到达后沿用同一状态，所以任何切分均得到同一事件流。`par` 对 UTF-8 只允许在字节边界切工：每段先做“起点对齐”，从切点向前最多找 3 字节；若找到尚未完成的 UTF-8 首字节，则该前缀归上一段最后一个事件，本段从其后的第一个完整边界开始；否则本段从切点开始。起点若是续字节或非法首字节，按正常非法单元解析。

切点前的 3 字节上限足以覆盖最长 4 字节 UTF-8。上一段不把尾字节“移交”给下一段，而是用包含重叠前缀的本地扫描确定边界，再只把边界后的输入交给本段；重叠字节只在上一段计数。这样 `C3|80 80` 中首段结束 `C3`，下段从 `80` 开始，得到一个截断事件和一个非法续字节，和单线程一致；事件数、吞字节数不因切点改变。UTF-16 切点向偶数字节对齐，重叠最多 1 字节；高/低代理边界由上段完整保留。

## 上限断点

输出按完整标量生成；写输出缓冲前先检查剩余容量，容量不足立即返回超限错误，不写入半个 UTF-8 字节或半个代理。`Write` 只返回已经形成完整输出事件的输入字节数；当前事件尚未形成而留在解码缓存中的字节不算消费。调用方以返回值续传，新实例得到同一事件流；BOM 只在首个实例处理。

## 检查次数

解码器每次输入只把每个新字节推进状态机一次；缓存字节不随后续分片重复扫描，计数器只在字节进入状态机时递增。`par` 每个主体字节计一次，起点对齐最多回看 3 个 UTF-8 字节或 1 个 UTF-16 字节，故总额外检查不超过 `K×3`。
