# 熔断 + 舱壁调用保护器：设计推导

范围：只做单层调用包装，顺序固定为 `breaker -> bulkhead -> timeout -> 真实调用`，
统计放进程内存，时钟可注入。不做重试、退避调度、抖动。

## 1. 被熔断拒绝的调用不计入失败率

失败率的语义是「真实下游调用中失败的比例」，它回答的是「下游现在是否健康」。
被熔断挡住的请求根本没有触达下游，不携带任何关于下游健康度的新信息。
若把拒绝计入失败：打开期间拒绝越多，失败率越被自身拒绝锁定在接近 100%，
形成正反馈，半开探测即便成功，积累的样本仍被海量拒绝稀释，熔断器无法回到关闭。
因此口径固定为：

- 真实调用数 = 总请求 - 熔断拒绝 - 舱壁拒绝；
- 成功/失败只统计真实调用；
- 熔断拒绝、舱壁拒绝是两个独立的出口，互不重叠。

推论：打开后连续 1000 次被拒，失败计数零增长；半开后一次成功即可关闭。

## 2. 熔断置于舱壁之前

若舱壁在前：熔断器已经打开（下游已知故障）时，请求仍要先占用一个并发额度、
甚至在等待队列里排队，再被熔断拒绝——等于用真实故障的下游的额度去服务必败请求，
占满队列后连半开探测都可能被自己的拒绝流量堵在队列后，且等待毫无意义。

熔断在前则下游已知故障时零成本立即拒绝，舱壁额度占用恒为 0；
只有熔断器认为「值得真实尝试」（关闭或半开拿到探测名额）的请求才进入舱壁。
代价：熔断判定本身必须是 O(1) 的内存操作（加锁读计数器），这在本设计中成立。

## 3. 四条路径都必须归还额度

舱壁额度是可重用资源，获取与释放必须严格配对。调用的出口只有四条：
正常返回成功、返回错误（含不可重试）、超时被 ctx 取消、panic 被捕获。
任一路径漏掉释放，泄漏都是永久性的（计数器单调下降且无补偿机制），
跑满 N 次后舱壁永久退化为全拒。因此释放放在 `defer` 中，
panic 在最内层被 `recover` 转成错误后仍沿 defer 正常释放。

## 4. 不可重试错误是否计入熔断

计入失败，但不参与熔断。不可重试错误（如 4xx 参数错误）是确定性失败：
它是真实调用，按统计口径必须计入「真实失败」，否则
`总 = 成功 + 失败 + 两类拒绝` 不闭合。但它通常由请求自身缺陷引起，
重试到同一故障实例不会成功，更不代表下游不可用——一批坏请求不应熔断健康下游。
故：可重试错误与超时计入熔断失败计数；不可重试错误对熔断状态机中立
（既不增连续失败也不打断它，因为成功才表示下游恢复）。

## 5. 熔断状态机

- 关闭：记录滚动窗口成功/失败数；连续失败达阈值，或失败率超阈值且样本数达下限
  （下限排除小样本噪声），任一满足即打开。
- 打开：拒绝一切请求；冷却到期（注入时钟 now >= openAt+cooldown）允许首个请求
  转为半开。时钟回拨使 now 变小只会延后到期，绝不提前；读取到回拨返回错误且
  状态保持打开。
- 半开：仅允许配置数量的探测并发通过（探测名额即半开期并发上限），其余直接拒绝；
  全部探测成功 -> 关闭并清零计数；任一探测失败 -> 立即回到打开，冷却翻倍，
  翻倍有上限。状态迁移在互斥锁内完成，迁移计数器单调，保证只迁移一次、
  只加倍一次。

## 6. 舱壁语义

N 个并发额度 + Q 个等待位，合计 N+Q 个 channel 槽位作为队列信号量。
Acquire：有空槽立即占位（在途 < N 时立即晋升执行，否则排队）；
无空槽立即返回「队列满」错误（判定前不取时间、不等待，故时钟不会被推进）。
排队者用一个无缓冲晋升信号量等待：真实额度释放时关闭/唤醒一个等待者；
ctx 取消时等待者退出并释放其队列槽位。非导出计数器记录在途历史峰值。
每个下游一个独立 Bulkhead 实例，互不共享，故 X 耗尽不影响 Y。

## 7. 统计自洽

每个请求最终四选一出口（成功 / 真实失败 / 熔断拒绝 / 舱壁拒绝），
由包装器在唯一出口点计数，互斥锁保护。由此恒成立：

- `总请求 = 成功 + 失败 + 熔断拒绝 + 舱壁拒绝`
- `真实调用 = 总请求 - 熔断拒绝 - 舱壁拒绝 = 成功 + 失败`
- `失败 = 可重试失败 + 不可重试失败 + 超时失败 + panic 失败`

四类错误分别为哨兵错误 ErrRetryable / ErrNonRetryable / ErrTimeout /
ErrRejected（熔断拒绝与舱壁拒绝用独立哨兵，errors.Is 可区分；
队列满、回拨、阈值非法等均为可判定错误）。

## 8. 包划分（每个 .go 文件 ≤ 200 行）

classify（分类）、bulkhead（舱壁）、timeout（时限+recover）、
breaker（状态机）、stat（计数器+口径校验+组装包装器）、cmd/demo（判定演示）。
