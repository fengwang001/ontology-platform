# 设计推导

## 1. 空行与单列空值

空行（两换行相邻，含 `\r\n\r\n`）选择为**跳过，不产生记录**。理由：它不含任何定界动作，
只是行间空白；文件末尾换行也同理不产生尾记录。

推导：单列 CSV 里记录 `x` 写为 `x\n`。若空值记录也写为空字节（`""` 不加引号），则
`a\n\nb\n` 无法区分「记录 a、空记录、记录 b」与「a、空行、b」，往返丢数据。故单列空值
**必须**写为 `""`。多列空字段不会有此歧义（逗号确定记录存在），故不加引号。回写器规则：
仅当 `cell.Quoted` 为真才加引号；解析器只在「单列记录的唯一空字段」处把 `Quoted` 置真，
从而 `Write(Parse(x))` 对规范输入逐字节相等。**规范输入**：记录以 `\n` 结束，末记录有换行，
无空行，引号仅出现在必要处（逗号/引号/CR/LF，或上述单列空值）。

## 2. par 切点（双假设）

每段分别以「起点在引号外 (O)」「起点在引号内 (I)」两初始状态各跑一遍（I 假设：已在引号字段，
且字段起点在段外）。从左到右确定真相：前一段选定跑法的末状态只有 outside/quoted 两种
（`\r` 待定会在拼接时处理：下一段首字节非 `\n` 则该 `\r` 为孤立 CR 错误），据此选本段
O 或 I，故选定结果与单线程一致（状态机确定性）。拼接：O 段首字段与前段悬空字段合并；
I 段首的幻影字段同样并入；首记录序号、字段号按前段计数偏移重基。字节偏移为绝对量直接正确；
记录号/字段号加前段已完成数；段首幻影字段的错误再加 1 个字段偏移。无重扫：每段两遍即 2N。

## 3. CR 待定状态

未引号字段见到 `\r` 进入 crPending，**不发字段**。续传：下一 `\n` 正常行尾；其他字节（含
EOF）= 孤立 `\r` 错误，位置记在该 `\r`，后续字节照常消费。par：`\r` 在段尾时两段都无法定，
拼接点按下段首字节判定（上段 O 末 crPending 且下段首字节非 `\n` → 报错，错误坐标用上段
计数器，且 I 跑法的幻影字段丢弃）。引号内 `\r` 永远是内容，无待定。

## 4. 上限的「立刻」

引号字段维护「逻辑字节数」：普通字节 +1，转义对 `""` 仅在第二引号处 +1（第一引号是转义前导，
不是内容）。字段缓冲每加 1 前先判限，超限即错，不缓冲整条记录。字段数在见到逗号的第一字节
即计数检查；记录数在记录完成瞬间检查。终态后 `Feed` 永久返回同一错误。

## 5. 状态转移表

列：`,` `\n` `\r` `"` 其他。动作：C=累积, F=完成字段, R=完成记录, N=空行跳过。

| 状态 | , | \n | \r | " | 其他 |
|---|---|---|---|---|---|
| fieldStart | F,新字段 | N(若记录未开始)/R | crPending | →quoted | C→unquoted |
| unquoted | F,新字段 | R | crPending | 错误:裸引号 | C |
| quoted | C | C | C | →qseen | C |
| qseen | F,新字段 | R | crPending | C(内容引号),→quoted | 错误:引号后杂字符 |
| crPending | (见 §3) 孤立CR错误后按unquoted遇',' | F,R 后回 fieldStart | (见 §3) | (见 §3) | (见 §3) |

crPending 遇任意字节 b：先定「孤立 CR」错误（位置为该 CR），再把 b 按 fieldStart 规则处理。
EOF 处理：qseen→字段结束；quoted→未闭合错误；crPending→孤立 CR 错误；fieldStart 且记录未开始
→无记录；否则发出末字段/末记录。
