# 严格模式 Base64：规范尾部推导

一个 4 字符组承载 24 bit（每字符 6 bit），正常编码 3 字节。
流末不足 3 字节时用填充补齐，于是出现「填充组」：

- 1 字节 + `==`：只用 2 个字符 = 12 bit，其中 8 bit 是真实数据，
  多出 **4 个备用比特**，即第 2 个字符 6 bit 值的**低 4 位**。
- 2 字节 + `=`：用 3 个字符 = 18 bit，其中 16 bit 是真实数据，
  多出 **2 个备用比特**，即第 3 个字符 6 bit 值的**低 2 位**。

**统一规则：填充组内所有备用比特必须为 0。**

推导：
- `QQ==`：Q=16=`010000`，备用位 `0000`，合法，解出 `A`。
- `QR==`：R=17=`010001`，备用位 `0001≠0`，拒绝。
- `QUI=`：I=8=`001000`，备用位 `00`，合法，解出 `AB`。
- `QUJ=`：J=9=`001001`，备用位 `01≠0`，拒绝。

为什么必须拒绝非零备用比特：编码器对真实字节做移位拼装时，备用比特
**恒为 0**。若允许非 0 备用比特，则 `QQ==` 与 `QR==` 两个不同输入会解出
同一个字节串 `A`，解码丢失信息，导致「任意字节串的编码结果唯一」不成立
（即 Encode(Decode(y)) = y 被破坏）。要求备用比特为 0，等价于「被接受的 y
恰好且唯一地是某个字节串的编码结果」，编解码在合法集合上互为逆映射。

无填充（`QQ`）、填充数量错（`QQ=`/`QQ===`）与填充后出现数据
（`QQ==QQ==`）属填充位置/长度错误，与非规范尾部是不同类别。

## 语义 → 测试对照表

（实现完成后补全）
