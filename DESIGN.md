# 规范化器设计推导

## 1. 末尾换行策略（空输入推导）
定义 v=规范化主体（`\r\n`、`\r`→`\n`；删行尾空格/Tab；不含末尾策略）末尾 `\n` 的个数。
Keep 原样输出 v；EnsureOne 使末尾恰有 1 个 `\n`（v=0 则补，>1 则删到 1）；Trim 删光末尾空行，即 v→0。
EnsureOne 选「空输入也补成 `\n`」而非「空给空」：后者使 `N("")=""` 但 `N("x")="x\n"`，规则是「恰一个 \n」，空输出满足它却破坏"所有结果同形态"，且空集补一个 `\n` 后再规范化仍为 `\n`（幂等）；两种选择都幂等，选补一的不变量更简单（输出永远以 \n 结尾）。Trim 下空→空：没有行就没有换行。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `""` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `""` |

## 2. 被删字节的 ToOut
不变量：对输出偏移 o 有 `ToOut(ToOrig(o))==o`；两映射均单调不减。映射以「运行(run)」记录：每个 run=(origLen,outLen)，同长为恒等 run，不等为删除 run。故 ToOrig 总落在恒等 run 内，逆映射只要求被删字节映到**相邻**恒等 run 边界。
取规则：被删字节映到它右侧第一个存活字节的输出偏移；删除位于流末尾（右侧无存活字节）则映 `len(out)`。
- `\r\n` 的 `\r`：映到 `\n` 的位置（行边界），光标在 `\r` 上→换行符上，而非行首；
- 行尾空白：映到随后 `\n` 的位置，即换行符**之前/之上**，编辑器里等价于行尾，不会跳到下一行行首（映到下一行行首会越过换行，光标错位）；
- 末尾被删空白（无换行收尾）：映到 `len(out)`。
ToOrig 取「输出偏移 o 所在恒等 run 的对应原文偏移」；两端终点各自映终点。

## 3. 第 8 条缓冲超限：选「拒绝」
挂起空白超过 MaxPending 时返回可判定错误 `ErrPendingLimit`（错误类型携带原文偏移=已消费边界），已 flush 的输出与映射保留，实例进入终态（后续 Write 返回 `ErrClosed`）。
强制判定为「非行尾」并输出看似流式友好，但它依赖一个未来会被推翻的假设：随后若是 `\n`，正确结果应删除这些空白，强制输出无法回滚，直接违反幂等(5)与映射正确性(6)；随后若再来同样长空白还会重复超限、结果依赖切分方式违反(4)。拒绝只意味着"此上限下无法处理"，不改变任何理想结果，故不违反 4/5/6。输出超 MaxOutput 同理返回 `ErrOutputLimit`（偏移为将写入位置）后终态。NUL 严格模式返回 `ErrNUL`（带原文偏移），默认模式 NUL 原样通过；非法 UTF-8 始终原样通过。

## 4. par 拼接
独立段的唯一差异来源：切点落在 ①`\r|\n` ②一长串行尾空白中间 ③「空白后接 `\r`」。段末尾的悬垂 `\r` 在单段 Close 时被迫当成独行尾输出 `\n`；段末挂起空白被迫当成非行尾（保留）。
做法：每段用 Keep 策略规范化，得到运行表，并由段尾向后取至多 W+1 字节（W=挂起空白长度，受同一 MaxPending 约束）作为「悬垂窗口」。顺序拼接时维护两个全局 carry：悬停 `\r`（0/1 字节）与挂起空白串；读下段首字节即可裁决：
- 悬停 `\r` 遇 `\n`：把该段开头 `\n` 由恒等 run 改为删除 run（合并为一个 `\n`）；
- 挂起空白遇行尾：整条空白串改判删除（跨段的各段恒等 run 改为删除 run）；遇普通字节：全部保留（它们本就保留）；空白串可继续累加 carry；
裁决只需每段前 1 个新字节，故每段修正窗口 ≤ MaxPending+1 原文字节，上界与 K 无关。
映射拼接：段 i 全部 run 的坐标加段 i 的全局原文基址与已累计输出基址；删除↔恒等的改判只改变 run 长度（删除 run=(n,0)，恒等 run=(n,n)），不改变段间基址关系。全部 K 段裁决完后统一施加一次末尾策略（与单流 Close 相同），保证与 K 无关。NUL 偏移包装为全局偏移；任一 MaxPending/MaxOutput 失败即整体失败。
