# 带背压的流式聚合管线 — 设计

链路：`source -> parse -> aggregate -> sink`，外加 `ckpt`。多级有界队列，全部进程内存，仅标准库。

## 1. 「已消费位置」推导

每条 source 记录带单调偏移 `off`（首条为 1）。三个候选位置：

- **source 读取位置 r**：r 之后的记录可能已读出、压在某级队列里、尚未落盘。崩溃后从 r 重放 -> 在途记录丢失。错。
- **sink 落盘位置 s**：s 是最后落盘那批的偏移；此刻 s 之后可能还有记录已在聚合或已写临时文件。从 s 重放 -> 已落盘部分重复。错。
- 正确定义：**已消费位置 f = 所有「偏移 <= f 的记录都已 (a) 计入某组或坏记录数，且 (b) 这些结果已原子落盘，且 (c) 各级队列中不存在偏移 <= f 的在途项」的最大偏移。** 即 f 既是结果前缀，也是在途集合的水位线。

### 检查点时机：屏障（barrier）

每 N 条（checkpoint 间隔）插一个屏障标记，随记录流过 parse/aggregate。聚合器收到屏障前，先把偏移 <= 屏障的全部更新并入组表（屏障有序，此前更新必已收齐），再把 `{组表快照, bad, off}` 交给 sink；sink 写临时文件 -> fsync -> 原子 rename，成功后才把同一份状态用 ckpt 原子落盘（保留最近两份）。

关键：ckpt 写在 rename **之后**。rename 成功而 ckpt 未写 -> 重放该批（覆盖同一批输出），最多重复计算、不多不少；ckpt 写成功则输出必已在盘上。恢复只认 ckpt 里的 f，从 f+1 重读，因此不丢（队列里 >f 的会重放）也不重（<=f 的不会重放）。

sink 输出是「截至 f 的完整聚合快照」而非增量：每次按 key 排序重写整个输出文件，任一批次重放只产生与不崩溃时逐字节相同的前缀文件；末尾有 CRC32 校验行，半截临时文件（`*.tmp`）一律视为未完成并删除。

崩溃点安全性：source 读完队列非空 / 聚合中途 -> 最近屏障未提交，f 不动，重放；sink 写临时文件未改名 -> rename 未发生，旧输出+旧 f 仍有效，重放该批。

## 2. 背压与资源约束

- 阶段间全部用容量为 Q 的有界 channel。投递阻塞即天然背压：sink 慢 -> 聚合通道满 -> parse 通道满 -> source 的 `Read` 在发送处阻塞。
- 计数器（原子）：`inflight` = 当前所有队列在途条数，`maxInflight` 记历史峰值；`sourceBlocked` 记 source 发送阻塞发生次数。稳态断言 `maxInflight <= 各级容量之和`（2Q）；无背压则趋近总记录数。
- 组表为 map，创建第 `maxGroups+1` 个新 key 时返回哨兵错误 `ErrTooManyGroups`，管线停止；空串是合法 key；缺键记录计入 bad。

## 3. 故障处理

- 坏记录：parse 返回错误，计入 `bad`，不进任何组，管线继续。
- source 错误：已处理屏障照常量盘+写 ckpt，错误经 `Report` 如实上报。
- sink 半截：恢复先扫目录删除全部 `*.tmp`（逐字节截断的临时文件都没有正式名，必然被清），再读正式输出+校验和。
- ckpt 损坏：状态文件 JSON 内嵌 CRC32，写为 `ckpt.a/ckpt.b` 交替两份；读取时两份都试，校验失败那份跳过并标记 `fellback=true`。

## 4. 并发与优雅停止

每阶段一个 goroutine，由 `context` + `sync.Once` 控制：source/parse/agg/sink 串行管道，屏障保证 Stop 时先排空在途并提交最后屏障。重复 `Stop` 幂等；source 未启动时 Stop 只令其立即结束。测试在 Stop 后轮询 goroutine 数回到基线。

## 5. 包划分（.go 文件上限 9 个）

`source`（可注入内存源：速率/报错/提前结束）、`parse`（行 -> Record）、`sink`（临时文件+rename+CRC、清理）、`ckpt`（双份状态+回退）、`stage`（阶段骨架与管线编排/背压计数）、`cmd/demo`（验收）。测试按包合并、表驱动。
