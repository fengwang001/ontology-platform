# 设计推导

流格式：每条记录首字节 tag。0=字面量(后跟 uvarint n、n 字节)；1=回指(uvarint dist、uvarint length)；2=刷新标记；
3=流尾(uvarint 原文总长、uvarint CRC32-IEEE)。流头为魔数 `LZ77v1`(5 字节)+uvarint 窗口容量。uvarint 为小端 7 位/字节
标准 base128，限 10 字节且 64 位不溢出（第 10 字节只允许低 1 位）。

## 1 与切法无关的压缩
压缩器维护 w=32768 的环形窗口和最多 maxMatch=258 个「未决字节」。每次只处理一个位置 pos，且仅当其后至少还有
maxMatch 个已收字节（或到达强制结束点：Flush/Close）才在 pos 做决策：查哈希链、取窗口内不超过 maxMatch 的最长匹配。
决策只依赖「pos 之前已入窗的字节」与「pos..pos+maxMatch 的内容」，二者都是输入前缀的确定函数，与 Write 边界无关；
匹配长度统一封顶 maxMatch，故未来多到的数据不会改变已发记录。一个位置发字面量则前进一步；发匹配则其覆盖的位置全部
入哈希链后跳过。Flush 是强制边界：把所有未决字节按上述同样规则耗尽（已知后续暂无数据，最长匹配自然到已收末尾为止），
发刷新标记，此时解码器已能还原全部输入；边界前后哈希链与窗口连续，故之后仍可回指旧数据。Close 同样耗尽后发流尾。
第二次 Flush 时没有新位置被决策、无新记录，故零字节。

## 2 重叠回指的复制
块复制 dst:=src[:n] 在 d<n 时会读到尚未搬好的目标自身，结果错误（如 d=1 应复制成同一字节重复 n 次）。
实现对每个回指按字节 `out[len]=out[len-d]` 复制；该固定模式编译器易优化，且复制总量等于输出量 O(n)，最坏输入
4MB 同字节串约 4 万次回指、共 4MB 次赋值，开销可接受，不需要特化展开。

## 3 两类距离错误
「超过已输出字节数」说明引用的是从未存在过的偏移，流在任意窗口配置下都非法；「超过窗口容量」说明数据曾经存在但
已滑出当前窗口，只在本窗口配置下非法。二者必须分开报。压缩器只在环形窗保留的最近 w 字节中找候选，dist<=w，
且 dist<=pos，故正常流两者都不会发生。并行块以「上一块原文的最后 w 字节预置窗口」：对解压序列而言这些字节就是
本块记录之前已输出的字节（同一线性输出），所以「已输出字节数」语义不变，仍包含预置字典长度。

## 4 并行确定性
第 k 块只需要本块原文与上一块原文（后者仅取末尾 w 字节建窗与哈希链），不需要任何块的压缩记录或其他块状态，
因此所有块可任意并发、任意顺序完成。每块由与流式完全相同的位置决策算法产出 0..多条记录：字面量/回指，块间
不需要刷新标记——记录是位置无关的，顺序拼接后接流尾即得到一条合法流（块边界处的匹配只能落在预置字典或本块内，
长度封顶保证不会「需要看到看到下一块」）。worker 数只改变调度，块划分与每块输出恒定，故字节恒定。
空输入也输出流头+流尾；解码器按消费偏移报告所有错误。
