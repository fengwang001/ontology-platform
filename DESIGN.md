# DESIGN — properties 解析器/回写器推导

## 第 3 条：只含空白（或为空）的续行

推导（对照 `java.util.Properties` 的 `LineReader.readLine`）：续行发生后，读
下一物理行时 `skipWhiteSpace=true` 且 `appendedLineBegin=true`。前者剥掉行
首空白；后者使「紧跟的换行符不被当作空行跳过」，而是走正常 EOL 分支：此时
缓冲区已有内容（`len>0`）且行尾不是奇数反斜杠，于是逻辑行就此结束。

结论：一个只含空白或为空、且自身不以奇数反斜杠结尾的续行，被当前逻辑行
**整个吞掉**——贡献零个字符，并**终止**该逻辑行；它不会被重新解释为一个
空行分隔符。例：`k=a\` + `   ` + `x=y` → `k`→`a`、`x`→`y`。若该行剥掉首部
空白后剩下 `\`（如 `   \`），它自身又是奇数反斜杠续行，继续往下接。

与「续行首部空白被剥掉」的关系：两者是同一条规则。剥空白对每条续行都发
生；当续行全是空白时，剥完即为空——「空贡献 + 终止逻辑行」只是该规则的
特例，而不是另一条规则。EOF 时奇数结尾的反斜杠同样被丢弃（`k=v\`→`v`）。

## 第 6 条：Store 只转义必要的字符

键（key）必须转义：`\`（否则被当作转义引导符）、`=` `:`（否则成为分隔符）、
空格/`\t`/`\f`（键扫描遇到未转义空白即结束，故**键中所有**空白都要转义）、
`\n` `\r`（行终止符无法字面出现）、**仅当位于键首**的 `#`/`!`（否则整行被
判为注释）。键中其他位置的 `#`/`!` 不必转义：注释判定只看逻辑行第一个非
空白字符。

值（value）必须转义：`\`、`\n`、`\r`，以及**仅开头**的空白。值开头空白必
须转义，因为 Load 在分隔符之后会跳过所有字面空白才作为值起点，字面前导空
格会被吃掉；写成 `\ ` 后首字符是反斜杠，跳过停止，反转义还原出空格。值
**中间**的空格不必转义：值起点一旦确定，行尾之前的一切（含空格、`=`、`:`、
`#`、`!`）都按字面取值，无任何特殊含义；值尾部空白也因此原样保留。以
`#`/`!` 开头的**键**必须转义而值不必：注释判定只看逻辑行首字符，而值永远
不会出现在行首——即使键为空，Store 也会写出 `=`，行首是 `=` 而非值。

非 ASCII 字符原样以 UTF-8 写出；Load 只特殊处理 ASCII 字符，多字节序列直
通，往返成立，无需 `\uXXXX`。

## 语义 ↔ 测试对照表

（待测试写完后补齐）
