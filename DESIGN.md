# DESIGN

## 1. hunk 头零计数的行号规则（题面第 4 条）

样例：旧 `x\n` 新 `y\nx\n`（C=0）→ 头 `-0,0 +1`；旧 `a\nb\nc\n` 新 `a\nb\nX\nc\n`（C=0）→ 头 `-2,0 +3`；旧 `a\n` 新 ``（C=3）→ 头 `-1 +0,0`。

推导：纯插入在旧文件中不占任何行，故旧侧计数 b=0。此时行号 a 无法指到「被覆盖的行」，
唯一自洽的读法是「插入点之前最后一行的行号」：插到文件头时前面没有行，记 0；
插到第 2 行之后时记 2。它**不是**「将要插入的位置」（否则文件头插入会写成 -1，
第 2 行后会写成 -3，与样例矛盾）。对称地，纯删除使新侧 d=0，c 记删除点之前最后一行的新行号。
规则：计数为 0 时，行号 = 该侧插入点**前一行**的行号；计数 ≥1 时，行号 = 区间首行行号（1 起）。
`Apply` 解析 `-a,0` 时把插入点放在旧文本第 a 行之后（a=0 即文件最前）。

| b 值 | 头写法 | Apply 的插入点解释 |
|---|---|---|
| b=0 | `-a,0`，a=前一行行号（文件头为 0） | 第 a 行之后插入 |
| b=1 | `-a`（省略 `,1`） | 从第 a 行起覆盖 1 行 |
| b≥2 | `-a,b` | 从第 a 行起覆盖 b 行 |

## 2. hunk 合并阈值（题面第 5 条）

结论：两段改动之间的未改动行数 g ≤ 2C 时合并，g ≥ 2C+1 时分开。

推导：每段改动独立成 hunk 时，后一段需要自带 C 行前导上下文，前一段需要 C 行尾部上下文，
两者合计最多容纳 2C 行公共行；g ≤ 2C 时两个 hunk 的上下文窗口必然重叠或相接，
GNU diff 会把相接的窗口并成一个 hunk（中间 g 行全部作为上下文行写出）。
反例验证：C=1、g=2（如 1 2 X 4 5 Y 7 8）若阈值取 C=1 则会分成两个 hunk，
但 `diff -U1` 实际输出单个 hunk（中间 4、5 为上下文）；若阈值取 2C+1=3，
则 g=3 时会被合并，但 `diff -U1` 对 g=3 输出两个 hunk（前段尾部 1 行 + 后段前导 1 行，
中间还剩 1 行无法共享）。故阈值恰为 2C。

## 3. 最短脚本的并列选择（题面第 3 条）

Myers 在同一 D 上有多条最短路径（如 `a b`→`b a`，D=2 时「删 a 插 a」与「插 b 删 b」等价）。
规定：**向前搜索时并列取删除（x 增），回溯时并列取删除优先**，净效果是同一 hunk 内
所有 `-` 行排在所有 `+` 行之前（先删后插）。该规则固定写入算法，故同一输入重复
生成 100 次输出逐字节相同。若改为插入优先，hunk 内会变成 `+` 行在前，二者都是
最短脚本但文本不同；本实现选定删除优先并以此为确定性的唯一来源。

## 4. 偏移应用后后续 hunk 的查找起点（题面第 7 条）

结论：前一个 hunk 以偏移 +k 应用成功后，后一个 hunk 的期望位置要**跟着平移 +k**
（累计所有已成功 hunk 的偏移之和），再在新起点上下 F 行内查找。

理由：补丁各 hunk 的行号都相对同一份原始文本。目标文本相对原文整体平移了 k 行时，
第一个 hunk 靠偏移找到位置；若后续 hunk 不平移，它会在原始行号附近查找，
而真实内容已随同一平移移走，必然失配或误配到别处。后续 hunk 继承累计偏移，
等价于承认「目标 = 原文 + 整体平移」这一最常见情形，同时仍在 ±F 内精确匹配，
不降低正确性。失败仍触发整体原子拒绝，与是否平移无关。
