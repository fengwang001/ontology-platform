# DESIGN

## 模型
流式状态机：逐字节扫描，维护 `pendingWS`（末尾空格/Tab，延迟到行尾或 EOF 判定）与 `pendingCR`（`\r` 待定：下一字节是 `\n` 则合成一个行尾，否则单独行尾）。
输出只产生 `\n`。NUL 与非法 UTF-8 原样透传；严格模式遇 NUL 立即返回带原文偏移的错误。

## 推导 1：空输入 / 末尾策略
三策略：Keep=保留原样；Ensure=恰好一个末尾换行；Trim=删掉所有末尾空行但保留一个换行。
推导约束是幂等：规范化结果已无 `\r`、无行尾空白，故再次规范化时只有“末尾空行”会影响结果。
| 输入 | Keep | Ensure | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |
Ensure 对空输入选 `""`：若选 `"\n"`，则 N(`""`)=`"\n"` 但 N(`"\n"`)=`"\n"`，N(N(`"""))=`"\n"`≠N(`""`)，违反幂等；选 `""` 则不动非空结果，空映射空，幂等成立。
Trim 先删尾部全部 `\n`（空输入仍是空），再在“原本至少一个字节”时补恰好一个 `\n`。

## 推导 2：被删字节的 ToOut
映射要求 ToOrig 单调不减且 ToOut(ToOrig(o))=o。被删原文字节没有对应输出位置，
故把整段连续删除（行尾空白、`\r\n` 的 `\r`）映射到“删除点后一个保留位置”的输出偏移：
即该删段结束后首个保留字节的输出偏移；删段位于末尾时取输出长度。
这使 ToOut 单调不减；空白内每个偏移都映射到行尾 `\n` 的偏移（换行符之前），
符合编辑器语义：光标落在已删空白里，应停在该行换行符前，而非跳到下一行。
ToOrig 只对实际存在的输出偏移定义，逆映射成立。

## 推导 3：缓冲超限
`pendingWS` 尚未判定：强制输出会把行尾空白当成行中空白，若其后真有行尾则输出含行尾空白，违反语义 2 与幂等（5）。
故选择拒绝：返回带原文偏移的可判定错误（`ErrWSLimit`），已输出内容保留，进入终态，后续写入返回 `ErrClosed`。
`\r` 只缓冲 1 字节，不设限。输出超总量上限立即 `ErrOutLimit`，同样终态。

## 推导 4：par 拼接
按任意切点分段，各段独立以 Keep 规范化。切点只能制造两类跨段状态：末尾待定 `\r` 与未判定空白。
对每个切点，从左段尾部取“重叠窗”：跳过空格/Tab，再吞一个 `\r`（窗起点为该序列首，否则取段末）。
窗范围至多为 `WSLimit+1`，故修正范围有上界。
把窗原文与右段前缀（从右段头到右段首个行尾后的位置）合并后重新规范化，
替换两段在该重叠原轴上的输出；全局输出即“段输出与窗输出按原轴排序拼接”，再整体施加一次末尾策略。
映射不直接合并表：由拼接得到的输出，与各段 span 导出的连续区间（orig 区间/out 区间/长度）
按原轴求交、重放为一个全局 span，坐标按原轴累加平移；区间数只随删除点增长。
重叠窗在原轴排序后合并，相邻/重叠窗自动并为一段。
