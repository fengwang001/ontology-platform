# 属性级权限过滤器设计

## 目标与边界

按角色的可见列集合过滤谓词与结果行；不实现存储、索引、解析、HTTP 或认证。

## 不可见列进入谓词的三种处理

### 甲：把不可见比较当作假

会泄露。探针 `NOT (secret = 1)`：内部比较恒假，取反后恒真，攻击者可取得本应被过滤的行。

### 乙：把不可见列当作 NULL 参与三值逻辑

仍会泄露存在性，并让 `IS NULL` 成为探针。攻击者若知道 `secret` 非 NULL，正常查询会选中行；替换为 NULL 后结果变化，即可推断存在性；`secret IS NULL` 被直接求值为真也会放行。

### 丙：整条查询拒绝

谓词静态分析若引用该角色不可见的列，立即返回可判定错误，报告列名和树路径。数据库不观察、不返回任何行，因此不能从行数、选择结果或布尔结果反推列的存在与值。本实现采用丙。

## OR 短路例外

定义：在拒绝判定前对谓词做常量折叠。若某支不可见列比较位于 `OR` 分支中，且该分支折叠后可证明为常量真或常量假，则该引用不拒绝；常量真使整个 `OR` 恒真，常量假使该分支被删除，运行时都不会读取该列。

限制：只有在其最小 `OR` 祖先中被上述方式消除的引用才可豁免。顶层 `AND`、`NOT` 中的常量分支若仍携带不可见列，不适用 OR 例外；无法证明常量真假的分支一律拒绝。这个定义宁可保守，也不把未发生的读取误判为已读取。

## 谓词与折叠

节点：常量、列比较（`=`、`<>`、`<`、`<=`、`>`、`>=`、`IS NULL`）、`AND`、`OR`、`NOT`。路径是从根开始的字段名与子节点下标，例如 `OR[0]/NOT[0]`。

常量折叠遵循三值逻辑以正确处理比较产生的 NULL，但安全判定不把不可见列当作 NULL：未知列引用保留为不可证明表达式。比较含 nil 常量时，除 `IS NULL` 外结果为 NULL；`NOT NULL = NULL`；`AND/OR` 使用 SQL 三值逻辑。

拒绝时收集全部不可见引用（同一列只产生一个列名条目，但路径全部保留），不因第一个错误停止。

## 行裁剪

结果行只复制可见列，生成新 map；不可见列既不复制，也不写零值或 NULL，使“不可见”和“可见但值为空”可区分。复制按排序后的可见列进行，便于复杂度计数与确定性报告。

## 复杂度

谓词安全检查、折叠和裁剪在同一遍后序遍历完成，每个节点计数一次。行裁剪只遍历可见列：一千列、五列可见时只进行五次取值和五次写入。

## 报告

报告包含：删除列名集合（字典序）、拒绝引用（列名及全部路径）、是否整条拒绝。报告由有序输入生成 JSON，因此打乱输入构造顺序仍逐字节相同。

## 错误

三类错误用哨兵错误区分：`ErrInvisibleColumn`（安全拒绝）、`ErrInvalidPredicate`（结构或操作符非法）、`ErrUnknownRole`（策略中无角色）。

## 穷举定义

三列模板对每列独立选择可见/不可见，共八种；形态为 `a=1`、`NOT(a=1)`、`(a=1 AND b=2)`、`(a=1 OR b=2)`（第三列用于空集/全集边界）。定义：比较形态引用不可见即拒绝；NOT 同理；AND 任一分支引用不可见即拒绝；OR 中没有常量可消除的不可见分支也拒绝；常量谓词与无过滤总是放行。
