# 组提交与顺序保证器设计

## 1. 目标与边界

把并发到来的写请求攒成批，一次落盘一次 `fsync`，按批次内位置给每个调用方返回
全局连续序号与错误。只用标准库，日志写本地临时目录。五个核心包：
`req`（请求/结果模型）、`batcher`（攒批）、`wal`（编解码与落盘）、
`commit`（组提交编排与序号）、`recover`（崩溃回放）。

## 2. 日志格式（自描述）

文件头：`"ONTW"` 4 字节 magic + 1 字节版本，共 5 字节。零请求 Close 也创建
仅含文件头的文件（不产生空批次），回放得到 0 个完整批次。

批次布局（小端）：

```
[TotalLen u32][SeqBase u64][Count u32]([Len u32][Data])... [CRC32 u32]
```

- `TotalLen` 含自身在内的整批字节数，使读侧可在 CRC 失败后按长度跳过、重新对齐。
- 逐条 `Len` 长度前缀，空载荷（`Len=0`）合法。
- `CRC32`（IEEE）覆盖从 `SeqBase` 到最后一条数据末尾的全部字节，**批级而非条级**。

批大小估计：`20 + Σ(4+len(Data))`（20 = 4+8+4 头 + 4 CRC）。

## 3. 序号分配时机（核心推导）

三种候选：
1. 到达即分配：落盘失败会留下永久序号空洞，违背“全局连续”。
2. 落盘成功后分配：等待者无法在落盘期间获知先后，且成功回执与提交两阶段易分裂。
3. **批次组装完成、落盘之前一次性分配**：取当前计数器为 `SeqBase`，
   按批次内切片位置分配 `SeqBase+i`，随后一次落盘。

失败规则（本题规则）：
- 落盘（写或同步）失败 → 整批不可提交，**序号整批作废，计数器回滚为 `SeqBase`**，
  由唯一的编排协程执行，回滚无需 CAS。
- 成功 → 计数器已在 `SeqBase+n`，下一批紧接其后，故“第 2 批失败、第 3 批序号
  紧接第 1 批”。
- 批内条目顺序即序号顺序，回放据此校验区间连续不重叠。

## 4. 批次原子性

原子性靠“批级 CRC + 整批可见性判定”：
- 回放时固定头/任一条目/CRC 任一部分缺失或 CRC 不符 → **整批丢弃**，
  绝不只丢坏条目；中段坏批用 `TotalLen` 跳过，前后批仍可见。
- 文件尾部的半个批次（落盘中途崩溃）同样整批不可见，下次写入从最后一个
  完整批次的 `SeqBase+Count` 继续。

截断点四类可判定错误（`errors.Is`）：
- `ErrHeaderIncomplete`：文件头不足 5 字节。
- `ErrBatchHeaderIncomplete`：不足 4 字节读 `TotalLen`，或不足 20 字节最小批头。
- `ErrEntryIncomplete`：批头完整但某条 `Len/Data` 在声明范围内读不全。
- `ErrBatchCRC`：全部条目字节在位但 CRC 缺失/不符（整批不可验证即丢弃）。
另：`req.ErrClosed` 表示关闭后/关闭竞争中未被收留的请求。

## 5. 攒批触发（先到先得）

单后台协程拥有批次，三个条件任一满足即 flush：
- 条数达 `MaxCount`；
- 批编码字节达 `MaxBytes`（再加入下一条会超限即 flush）；
- 距首条到达达 `MaxWait`（注入 `Clock` 便于测试；`MaxWait=0` 表示每条立即成批）。

**超大单条**：`4+len(Data)+20 > MaxBytes` 的请求不被拒绝：先 flush 当前批，
再让它单独成批（仅此情形允许单批超 `MaxBytes`），计数器如实记录其字节数。

## 6. leader 选择与唤醒不串台

不做竞选：攒批协程是每个批次唯一且固定的 leader，负责分配序号与落盘；
调用协程只在自己的 `Request.done chan Result` 上阻塞。leader 落盘后严格按
批次切片下标 i 投递 `Result{Seq: SeqBase+i}` 到第 i 个请求自己的 channel，
物理上不存在结果串台的可能。关闭时 Close 等待 leader 排空最后一批：
已入队请求要么进入最后一批并落盘成功，要么 Submit 直接收到 `ErrClosed`，
不存在“成功但未落盘”。

## 7. 资源与复杂度

同步次数 = 批次数（每批恰好一次 fsync），10000 请求、上限 100 ⇒ ≤200。
攒批入队 O(1)；计数器记录历史最大批字节数，断言不越界（超大单批除外，见 §5）。

## 8. 故障注入与并发

`wal.Writer` 提供同步钩子（第 N 次 sync 返回错误）。半截写入由测试直接把
编码后的批次切短写入文件模拟。全部共享状态只在单 leader 协程访问，
调用方通过 channel 收结果，`-race` 下无数据竞争。
