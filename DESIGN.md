# design notes

## 第 3 条：续行行只含空白或为空

依据 `java.util.Properties` 的读行循环（`LineReader.readLine`）：遇到行尾 `\` 时
丢弃该反斜杠、递增物理行号，随后无条件执行前导空白跳过
（`skipLF` 之后 `while isWhiteSpace` 循环），直到再次遇到非空白或行结束。

推导：若下一物理行为空或全是空白，跳过循环会一直走到该行行尾，
于是“续行首部空白被剥掉”剥掉的不是有限个前导空白，而是整行；
读取再次回到续行判定点时该行已无内容，逻辑行就此结束，
等价于该空白行完全不贡献任何字符（也不会再带出更下一行）。
结论：`k=a\` + 换行 + 空行 → `k`→`a`；`k=a\` + 换行 + `   ` → 同样 `k`→`a`。
即空白续行规则就是“首部空白剥离”规则在“整行皆空白”时的退化情形，
并非额外规则；注释判定发生在空白剥离之前，所以它也不是注释。

## 第 6 条：Store 转义规则推导

Load 的判定顺序是：行首空白被丢 → `#!` 判注释 → 键中遇裸 `=`/`:`/空白即分隔符
→ 值前空白被丢 → 值尾空白保留。转义只需让重新解析时避开这些判定。

键：首个字符是空白（行首空白会被丢）、`#`、`!`（整行变注释）时必须转义；
键中任意空白、`=`、`:` 必须转义，否则被当分隔符；
反斜杠必须转义（否则被读成转义引导符）；`\n \r \t \f` 转义。
值：只有开头的空白必须转义（分隔符后空白会被丢），中间和结尾空格原样
（值内不再找分隔符，故中间空格、`=`、`:`、`#`、`!` 都无需转义）；
反斜杠与四个控制字符同样必须转义；`#`/`!` 在值位永远不是注释，无需转义。
非 ASCII 原样 UTF-8 输出（Load 默认按 Reader 读，即 UTF-8 解码）。
统一用 `key=value` 输出，空键空值也成立（`=v` 得空键；`k=` 得空值）。

## 语义—测试对照表

| 语义 | 测试函数 |
| --- | --- |
| 1 分隔符与空白 | `props.TestParse` |
| 2 注释 | `props.TestParse` |
| 3 续行（含空白续行） | `props.TestParse` |
| 4 转义与 `\u` 行列号 | `props.TestParse` / `props.TestUnicodeError` |
| 5 重复键首见顺序 | `props.TestOrder` |
| 6 回写最小转义 | `props.TestStoreRoundTrip` |
| 7 1000 组随机往返 | `props.TestRandomRoundTrip` |
| 复杂度 ≤2n 计数 | `logical.TestCheckedBytes` |
