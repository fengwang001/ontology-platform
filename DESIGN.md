# RLE 设计说明

不变量：严格解码接受的每个 `t` 都满足 `Encode(Decode(t)) == t`，即编码结果唯一（规范形式）。

## 必须拒绝的输入（逐条推导）

1. 显式次数 `1`（如 `1a`）：次数 1 按格式必须省略，接受后再编码得到 `a`，与原串不同。
2. 次数 `0`（如 `0a`）：游程至少含一个符号；接受后要么输出空、再编码得空串，与原串不同，要么产生非规范形式。
3. 前导零（如 `01a`）：格式要求十进制无前导零，再编码得到 `1a`（且仍非法），不可能等于原串。
4. 相邻游程符号相同（如 `2a3a`）：解码后是 5 个 `a`，`Encode` 按最长游程合并为 `5a`，与原串不同；必须看到下一个游程首符号才能判定，因此流式解码要保留上一游程直到确认符号不同。
5. `\` 后既非数字也非 `\`（如 `\a`）：符号只有 ASCII 数字与 `\` 需要转义；其他码点按格式必须原样写出，`\a` 不可能是规范编码（且转义无定义）。
6. 末尾孤立 `\`：缺少被转义字符，不是完整游程；接受再编码也不可能产生该尾随 `\`。
7. 末尾只有次数没有符号（如 `12`）：游程 = 次数 + 符号，缺符号，编码无法表示这一残缺形式。
8. 非法 UTF-8：符号是 Unicode 码点，非法字节不构成任何码点；`Decode` 返回字符串，无法经 `Encode` 还原出该非法字节序列（`Encode` 只产合法 UTF-8 与 `\`/数字）。

上述每一类返回不同哨兵错误，包装为带字节偏移的错误类型，偏移从 0 计、指向引发错误的字节（数字类错误指向次数首字节）。

## 大次数取舍

选择**不限次数位数、用 `math/big` 累积**：拒绝则需人为规定上限，而格式本身允许任意无前导零十进制。
解码绝不按次数预分配内存，而是在输出上限检查通过后分小块重复写入符号，因此 `99999999999999999999a` 不会引发巨型分配。
输出总字节上限可配置（`Decoder` 选项，默认 1 GiB）；继续输出会超限时返回哨兵错误 `ErrOutputLimit`，偏移指向当前游程首字节，任何切分方式下判定位置一致。

## 语义—测试对照表

| 语义（第三节） | 测试函数 |
| --- | --- |
| 1 往返 | （测试阶段补填） |
| 2 规范形式/严格解码 | （测试阶段补填） |
| 3 大次数与输出上限 | （测试阶段补填） |
| 4 最长游程、不做规范化 | （测试阶段补填） |
| 5 跨切分点一致 | （测试阶段补填） |
| 四 输入字节检查计数 | （测试阶段补填） |
