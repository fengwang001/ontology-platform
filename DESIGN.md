# 媒体类型内容协商设计

## 1. 为什么先比 q，再比特异性

q 值表达客户端显式偏好，特异性只在偏好相同时消除范围大小的歧义。
若先比特异性，会把客户端降权甚至拒绝的精确类型错误选中。

反例：

- Accept: `text/*;q=0.2, text/plain;q=0.1`
- 候选：`text/plain`, `text/html`

先比特异性会选 `text/plain`，但它的 q 只有 `0.1`；正确答案是 `text/html`，
因为它由 `text/*` 接受且 q 为 `0.2`。因此排序键必须是：先 q，后特异性。

特异性三级固定为：

1. 类型与子类型都精确；
2. 仅类型精确、子类型为 `*`；
3. `*/*`。

参数也属于特异性：参数集合不同就不匹配，而不是弱匹配。

## 2. q=0 与非法 q

`q=0` 是明确拒绝，不是低优先级。否则在：

- Accept: `text/plain;q=0`
- 候选：`text/plain`

时，协商器会把唯一候选当成“最低但可接受”返回，使客户端收到明确不要的类型。
因此任何由 q=0 项匹配的候选都不得入选；没有其他候选时返回“无法接受”。

q 缺省为 1；q 只接受 `0`、`1`，或最多三位小数的 0 到 1 之间数字。
`q=1.5`、`q=abc`、`q=0.1234` 均非法。

本实现选择**丢弃非法 Accept 项并继续处理同头部中的其他项**。理由是 HTTP
内容协商应尽量利用其他合法偏好，一项拼写错误不应让整个请求失去可服务性；
如果全部项都非法且没有任何可接受范围，结果自然是“无法接受”。非法 q 的解析
错误仍可由单项解析函数返回，错误包含该项从 0 开始的序号。

## 3. 同分为什么采用服务端顺序

q 与特异性相同表示客户端对多个类型没有表达进一步偏好。此时应让服务端决定
默认表示形式，因为候选清单顺序就是服务端成本、兼容性或默认体验的显式声明。

因此选择算法不读取 Accept 的同分顺序，只扫描服务端候选并保留第一个达到当前
最佳分数的候选。交换 Accept 项顺序结果不变；交换候选顺序才改变同分结果。

## 4. 缺失与空串为什么必须区分

Accept 缺失表示客户端没有发送偏好。HTTP 的常规默认是接受所有类型，即
`*/*;q=1`，所以应允许服务端按自身顺序选择。

空串是客户端显式发送了一个空的接受列表，语义是不接受任何媒体范围。若把它
规范化成 `*/*`，空 Accept 会错误地返回候选；若把缺失也当成空值，则未表达
偏好的普通请求会被全部拒绝。

因此 API 使用 `accept *string`：nil 表示缺失并按 `*/*` 处理；非 nil 空串表示
明确不接受任何类型，返回可判定的 `ErrNotAcceptable`。
