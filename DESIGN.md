# DESIGN — JSON 字符串字面量严格编解码器

包划分：`esc`（单个转义序列与代理对判定，无依赖）← `jstr`（Decode/Encode/流式 Decoder）。
`Encode` 签名定为 `Encode(s string) ([]byte, error)`：输入含非法 UTF-8 时返回哨兵错误
`jstr.ErrInvalidUTF8`，绝不静默替换为 U+FFFD。

## 第 2 条推导：代理对错误偏移

规则：**偏移指向使输入首次不可恢复的那个码元的起点**——流式解码器无需前瞻即可报错的位置。

- 孤立低代理（`"\uDE00"`、顺序颠倒的 `"\uDE00\uD83D"`）：低代理出现时没有任何待配对的高代理，
  它自身就是违规码元 → 偏移指向**它自己的 `\u` 转义起点**（`"\uDE00"` 报偏移 1）。
  解码器在读完它的 4 位十六进制时即可判定，无需看后面。
- 高代理后未跟合法低代理：高代理本身语法合法，错误在于配对失败，失败发生在后一个码元 →
  - 下一字节不是 `\`（`"\uD83Dx"`、`"\uD83DA"`、`"\uD83D"` 的收尾引号）→ 偏移指向**该字节**（偏移 7）；
  - 存在第二个 `\uXXXX` 但不是低代理（`"\uD83D\uD83D"`）→ 偏移指向**第二个转义的起点**（偏移 7）。

为什么 `"\uD83DA"` 不能解释成「U+FFFD + `A`」：
1. 代理码点不是 Unicode 标量值，根本不存在对应的 UTF-8 编码，输出字符串无法合法携带它，
   解码器只能在「拒绝」与「捏造数据」之间选择；
2. U+FFFD 替换是针对**非法字节序列**的转码约定，而此输入是良构 ASCII——转义语法合法，
   只是语义上不可编码，没有可"清洗"的对象；
3. 捏造会破坏单射：`"\uD83DA"` 与 `"\uFFFDA"` 将解码为同一字符串，往返性质（第 5 条）不再成立。
   严格解码器必须拒绝。

## 第 4 条推导：最小转义与唯一性

RFC 8259 的 `unescaped = %x20-21 / %x23-5B / %x5D-10FFFF`：语法上**必须**转义的只有
U+0000–U+001F（控制字符，不在 unescaped 内，任何合规解析器都拒绝裸控制字符）与
`"`、`\`（元字符，裸出现会被误解析为定界符/转义符）。U+007F 落在 `%x5D-10FFFF` 内，
裸输出对所有合规解析器都合法，转义它不增加可解析性，故不转义；U+001F 是控制字符，
不转义则输出不是合法 JSON，故必须转义。`/`、非 ASCII、U+2028/U+2029 同理合法，一律原样。

单射且唯一：编码逐 rune 进行，每个 rune 的输出形式**唯一确定**（5 个短转义 / `\u00XX` 小写 /
原样，三类 token 两两不交且各自自定界），无任选分支（不转义 `/`、不用大写十六进制、
不转义非 ASCII），故 Encode 是函数（输出唯一）；token 集合自定界使拼接可唯一解码，
不同输入必得不同输出（单射）。

## 测试对照表

| 语义 | 钉住它的测试函数 |
|---|---|
| 1. 五类错误可区分且带偏移 | TODO |
| 2. 代理对组合与校验 | TODO |
| 3. 非法 UTF-8 两个方向拒绝 | TODO |
| 4. 最小转义编码 | TODO |
| 5. 往返 | TODO |
| 6. 跨切分点一致 | TODO |
