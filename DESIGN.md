# 三态熔断器设计

## 1. 包与依赖

`outcome`（结果分类）→ `tally`（定容桶计数）→ `state`（三态机）→ `breaker`（门面，注入 `now func() int64`，毫秒）。
依赖单向，代码内不出现任何 `time.Now/After/Sleep`。

## 2. 配置

`Quota`：半开在途探测硬上限（>0）。`CooldownMS`：冷却毫秒数（>0）。
`Window`：闭合态统计窗口容量（>0）。`FailureRate`(0,1]：断开阈值比率。
`MinSamples`(>=1)：允许断开的最小样本数。
哨兵错误 `ErrBadQuota/ErrBadCooldown/ErrBadWindow/ErrNilFunc` 互不相同。

## 3. 推导

### 3.1 半开判定（与配额自洽）

配额 Q 是**同时在途**上限，不是成功所需次数。规则：探测成功且在途归零时，
只有「本代成功数 == 已发出探测数 == Q」才回闭合；探测**首次失败**立即重新断开。
- Q=1：唯一探测成功→成功数1=Q→闭合；失败→立即断开，均一步终结。
- Q=3：最多放 3 个在途；3 个全成才闭合，任一失败立刻断开。
- 若来的调用不足 Q（如只来 2 个且都成功）：在途归零但成功数 2<Q，留在半开等待，
  不闭合也不死锁，后续调用继续占剩余配额直到凑满 Q。
每进入半开分配一个代次（generation）；状态跳走后迟到的旧代结果一律丢弃，
因此旧探测迟到的成功不会污染新一轮半开。

### 3.2 冷却边界

采用「到期即生效」左闭右开：断开区间为 `[openAt, openAt+CooldownMS)`。
`now - openAt >= CooldownMS`（即 `now >= openAt+CooldownMS`，含恰好相等）时进入半开。
故冷却满恰好那个 tick 已属半开。每次 `Call`/状态查询入口惰性评估，无需后台定时器。

### 3.3 统计窗口语义

选**按最近 N 次调用（计数窗口，环形桶）**，不按时间。
时间窗口在「长时间无调用后一次失败」时仍会保留远古样本、比率含义随密度漂移；
计数窗口只看最近 N 次真实结果，语义稳定。为防样本太少误断开，
另设 `MinSamples`：窗口内真实调用数不足 MinSamples 时，即使失败率超阈值也不断开。
被拒调用根本不写入 tally（见 3.4），故不影响比率。

### 3.4 半开中途被拒的调用

被拒（配额已满/仍在冷却）不代表下游意见，由不变量 3「被拒既不算成功也不算失败」：
它们不写入 tally、不占配额、不计入半开本代成功/失败，不是「半开期的一次结果」。
半开判定只统计真正发出的探测。

### 3.5 复杂度

tally 维护 `sumOK/sumFail/size` 增量计数：写入时仅访问新桶（+被挤出的旧桶），
`FailureRate()` 除读取聚合计数外只读一个最旧桶做窗口自洽校验，访问桶数恒 ≤ 1
（空窗为 0），与容量无关。访问数记于非导出字段，不进公开接口。

## 4. 状态转移表

| 当前状态 | 事件 | 新状态 | 时刻 |
|---|---|---|---|
| 闭合 | 真实成功 | 闭合 | now |
| 闭合 | 真实失败且 size>=MinSamples 且 fail/size>=FailureRate | 断开 | now=openAt |
| 闭合 | 真实失败但未达断开条件 | 闭合 | now |
| 断开 | 任意 Call 且 now < openAt+Cooldown | 断开（拒绝，不发调用） | — |
| 断开 | 任意 Call 且 now >= openAt+Cooldown | 半开（代次+1，配额/成功清零），再按半开受理 | now |
| 半开 | 受理：在途<配额 | 半开（在途+1，真正发出） | now |
| 半开 | 受理：在途==配额 | 半开（拒绝） | — |
| 半开 | 探测失败（代次匹配） | 断开（openAt=该结果到达时刻，在途-1） | now |
| 半开 | 探测成功，在途归零且成功数==Q | 闭合（tally 全量清空） | now |
| 半开 | 探测成功，其余情况 | 半开（在途-1，成功+1） | now |
| 任意 | 旧代迟到结果 | 忽略，状态不变 | — |

## 5. 并发与 panic

单 mutex 串行化决策；真实调用在锁外执行（否则配额=1 时死等），返回后带代次回锁结算。
panic 用 defer 在锁外 recover 前先在锁内归还在途计数，再 `panic(v)` 原样重抛。
