# 设计推导

## UTF-8 非法单元

采用一次只提交一个确定单元的规则：首字节非法，立即吞 1 字节；合法首字节看到第二字节不合法，只吞首字节，并重放第二字节；第二字节合法后，后续每个非法续接字节各吞 1；流末未满足期望长度，已缓存前缀合为 1 个非法单元。因此 `F0 90 80 41` 中第三字节不是续接，提交前缀 1 个，重放 `41`；`E0 80 80` 与 `ED A0 80` 在第二字节即失败，首字节 1 个，随后两个续接各 1 个。`C0 AF` 同理两个；`F4 90 80 80` 在第二字节失败，后续三字节各一个。`E2 82` 到流末仍是合法前缀，整体一个截断单元；`80 80` 两个非法首字节。

| 首字节 | 第二字节区间 | 非法第二字节吞掉 |
|---|---|---|
| `C2..DF` | `80..BF` | 1，重放第二字节 |
| `E0` | `A0..BF` | 1，重放第二字节 |
| `E1..EC` | `80..BF` | 1，重放第二字节 |
| `ED` | `80..9F` | 1，重放第二字节 |
| `EE..EF` | `80..BF` | 1，重放第二字节 |
| `F0` | `90..BF` | 1，重放第二字节 |
| `F1..F3` | `80..BF` | 1，重放第二字节 |
| `F4` | `80..8F` | 1，重放第二字节 |
| `80..BF` | 无 | 1（非法首字节） |
| `C0 C1 F5..FF` | 无 | 1（非法首字节） |

## 缓存与判定时机

UTF-8 最长合法前缀 4 字节，pending 上限 3：读首字节后即可等第二字节；第二字节不合法时立即提交首字节并重放当前字节。`E0/E1...` 等特殊第二字节区间当场排除非最短与越界，`ED` 当场排除 UTF-16 代理；第三、四字节只允许 `80..BF`。UTF-16 pending 上限 1 字节；确定 code unit 后，高代理最多再缓存 2 字节。BOM 只在全局偏移 0 的未决状态判定，被切开时仍留在 pending，因此与不切开等价。

## par 切点对齐

每段拥有“起始偏移落在本区间”的解码事件，但解析时回看前 3 字节（UTF-8）或前 4 字节（UTF-16）作上下文，并向后多读 6 或 4 字节。worker 只输出事件起点在自己半开区间内的事件。切点在字符中间时，事件属于首字节所在段，下段用上下文跳过其续接字节；切点在非法单元中间时同理，因为非法单元逐字节确定，尾段只会提交真正从切点开始的非法首字节。`C3|80 80` 中，双字节字符属于上段，下段从第二个 `80` 开始；单线程同样是一个合法字符加一个非法字节。

## 上限断点

`Write` 的已消费数只包含已产生输出或严格错误的完整事件起点对应字节；残留在 pending 的字节不算，调用方必须带上它们续传。处理一个事件前先确认完整目标编码字节可放入上限，否则停在该事件起点并返回超限错误，不写半个字符或半个代理对。续传实例从“返回偏移之后的全部输入”开始；拼上此前输出即等于无上限结果。
