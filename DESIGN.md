# 结构化日志脱敏与采样器 — 设计文档

## 1. 一致性采样：为什么决策只能由追踪 ID 决定

一条追踪链包含多条记录。若对每条记录独立掷骰（随机数、时间、到达顺序），同一条链上
必然有记录留下、有记录丢弃，链被切断，无法还原一次完整调用。

要求的不变量是：**同一条链的所有记录决策相同**。决策必须是一个纯函数，其唯一输入能
标识链，即追踪 ID：

```
keep = hash(traceID) / 2^64 < rate
```

- 使用 FNV-1a 64 位哈希，把 traceID 映射到 [0, 2^64)；与记录内容、时间、顺序均无关。
- 同一 traceID 在任何进程、任何时刻、重复任意次，结果一致；O(1)，每次恰好 1 次哈希。
- rate=0：阈值 0，全丢；rate=1：特判全留；中间值为确定性比例。
- 哈希只决定「链是否入样」，不涉及记录随机抽样，故链完整。

**无追踪 ID 的策略（定义）**：无 ID 的记录无法归属任何链，丢弃它会造成不可审计的永久
丢失，故按「宁可多留」**无条件保留**，且不打链不完整标记（它本就不属于任何链）。
**空串 ID** 是合法 ID，与普通 ID 一样参与哈希（因此可能被丢），二者必须区分对待。

## 2. 错误级别强制保留与「链不完整」标记

级别定义：debug=0、info=1、warn=2、error=3、fatal=4；error 及以上即 level>=3。

处理规则：

1. `Decide(traceID)` 给出链的入样结论。
2. 链入样：记录保留。
3. 链被丢弃且 level<3：记录丢弃。
4. **链被丢弃但 level>=3：记录强制保留**，并在记录上置 `ChainIncomplete=true`。

`ChainIncomplete` 标记**只**出现在第 4 种情形：链未入样、记录因高级别单独保留——此时
输出里有这条错误记录，却没有同链的上下文，链在输出中不完整。入样链的 error 记录保留但
不打标记；被丢链的普通记录也不打标记（它根本不出现）。

## 3. 嵌套路径与「字段名含点号」的消歧

规则路径按 `.` 分段，`*` 匹配任意单个键或一个数组下标。

消歧规则（定义）：**字面点号键等价于嵌套**。记录中的键 `a.b` 在匹配时视为两段路径
`a -> b`。因此规则 `a.b` 对以下两种存储都会命中：

- `{"a": {"b": v}}`（真嵌套）；
- `{"a.b": v}`（字面键名含点号）。

推论：

- 若同一条记录两种存储同时存在，两处值都被脱敏——规则作用于路径，不作用于存储形式。
- 规则 `a`（单段）**不**匹配字面键 `a.b`，因为该键代表两段路径。
- 某层不是映射（如路径要求继续深入但当前值是字符串/数组标量）：路径不可达，不命中，
  不报错。路径不存在：同样静默不命中。
- 数组：`a.*.b` 匹配数组每个元素映射里的 `b`；数字段 `a.0.b` 精确匹配下标 0。

实现上规则集预编译为段前缀 trie；遍历记录时维护 trie 节点 frontier（大小 ≤ 深度上限），
对每个键先按整键精确查，再按点分段下行，每层 O(1)。

## 4. 脱敏方式与彻底性

- **替换 Replace**：目标值整体替换为固定串，长度不保留（不保持原长）。
- **哈希 Hash**：对值的规范 JSON 做 SHA-256，输出十六进制串。同值同结果、不同值不同
  结果、不可逆（输出不包含原值，无法还原）。
- **截断 Truncate**：按 rune 保留前 N 个字符并追加固定截断标注；不足 N 时原样返回。

脱敏遍历整棵字段树，同一敏感值出现在不同路径、数组元素中都会处理。值模式规则（正则）
在编译期合并为一个带命名分组的大正则，对每个字符串标量只做一次匹配，命中即按该模式指
定方式脱敏。非目标字段不被触碰：JSON 键序确定，可逐字节比对。

## 5. 复杂度约束

- 规则 trie 预编译：每条记录单趟遍历，每字段的 trie 查询次数为常数（frontier 深度受上限
  32 约束）。非导出计数器 `matches` 每访问一个字段加 1，故
  `匹配次数 ≤ 记录数 × 字段数`，与规则数无关；100 规则 × 10 万记录由测试断言。
- 采样：单次决策恰好 1 次哈希计算（非导出计数器断言）。

## 6. 落盘格式与截断分类

文件格式（全部小端）：

```
魔数头 "ONTSINK\x01"（8 字节，自描述）
重复：长度(4) | JSON 记录体(length) | CRC32-IEEE(4，仅覆盖记录体)
```

读取按前缀恢复，能恢复多少恢复多少，错误可判定（errors.Is 区分）：

- 前缀不足 8 字节或魔数不符：`ErrHeaderIncomplete`；
- 余 <4 字节：长度前缀不完整 `ErrLengthIncomplete`；
- 余 <4+length：记录体不完整 `ErrBodyIncomplete`；
- 余 <4+length+4（CRC 缺失/被截）或 CRC 不符：`ErrCRCMismatch`（无法完成完整性校验
  即归此类）；
- 截断点恰在记录边界：无错误，恢复出的就是最大可恢复前缀。

## 7. 深度、循环引用与并发

- 嵌套深度（映射/数组均计数）上限 32：构造校验时超过即 `record.ErrDepth`，拒绝并计数，
  不递归栈溢出。
- 自引用/环：校验时维护「正在访问」的映射身份集合，重复进入即 `record.ErrCycle`；
  共享但无环（DAG）不误报。
- 四类故障 `errors.Is` 可区分：`record.ErrDepth`、`record.ErrCycle`、
  `rule.ErrConflict`（同路径冲突规则，编译期报错并指出两条规则）、sink 截断错误。
- 并发：编译后的规则集与采样器只读（计数器用 atomic）；`Apply` 就地修改的是调用方传入
  的记录副本，多协程并发处理结果与串行处理的输出集合逐元素相同，`-race` 干净。
