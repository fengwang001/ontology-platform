# 设计与推导

路径与模式都按 `/` 切成**段数组，保留空段**（不折叠 `//`，不处理 `.`）。
段内原子：字面码点、`?`（恰一码点）、`*`（段内任意零/多码点）、字符类（一个码点）。

## 1. `**` 边界推导

`**` 仅在独占一段时表示「零个或多个**整段**」，且「整段」与普通段完全同质，
**包括空段**（空段也是切分结果，规则 3 禁止折叠任何东西）。统一规则：

- `**` 吃掉路径段数组中连续 0..k 个段（含空段）；
- 其余模式段与剩余路径段一一对应、逐段匹配。

由此：`a/**/b` 匹配 `a/b`，因为 `**` 吃 0 段；`x/**` 匹配 `x`（吃 0 段，
末尾不要求 `/`）与 `x/`（吃 1 个空段）；`**/x` 匹配 `x`（吃 0 段）；
`**/x` 对 `/x`：切分分别为 `["**","x"]` 与 `["","x"]`，`**` 吃 1 个空段 `""`，
模式段 `x` 匹配路径段 `x` ⇒ 匹配。四个例子同一条规则，无特判。

## 2. 空串路径

`""` 切分为单空段 `[""]`。`*` 是段内原子，匹配同段零个或多个码点 ⇒ 匹配空段，
故 `*` 匹配 `""`；`?` 要求恰好一个码点，不匹配。`**` 按第 1 条吃整段：
模式 `["**"]`、路径 `[""]`，`**` 吃 1 个空段 ⇒ 匹配。即 `*` 与 `**` 都匹配空串路径；
空模式（0 个模式段）只匹配空串。

## 3. 无回溯算法

- 段内（无 `**`）：经典「最后一个星号回退」。维护 `starP/starQ`，失配时只回退到
  最近的 `*`：令它多吃一个码点，其后原子重新对齐。正确性：若某可行匹配让**更早**的星号
 扩张而最近星号不变即可成功，把两星之间被匹配的码点整体改划给最近的星号吞吃（`*…*`
  中前缀转移不改变先后顺序），仍得一可行匹配 ⇒ 最近星号可作唯一回退点。
  每步推进模式指针或令回退点单调右移，O(模式原子 × 路径码点)，无指数回溯。
- 跨段 `**` 不能照搬：段内星只在一条线性序列上吞字符，前缀转移保持顺序；`**` 吞的是
  **整段**，多颗 `**` 时其两侧模式段可在不同路径段上反复对齐，让「最后一个 `**`」吞掉
  前缀段会整段丢失中间段对模式段的约束，不保持可行性。改用 DP：可达模式段集合按路径段
  滚动；普通段逐段确认，`**` 段做闭包（吃 0 段或连续多段）。布尔矩阵至多
  模式段 × 路径段，每格一次段内线性匹配，总量 O(模式长 × 路径长)。

## 4. 规则集版本一致

新规则**先整体编译**为不可变快照（失败立即返回，旧集合不触），成功后用
`atomic.Pointer` 单次交换发布；查询开始时取一次快照指针，全程只用它。
故每次查询（含 `Explain`）看到的序号、原文、结论同属一个版本；替换原子，无半新半旧。

## 5. 边界结果表

| 模式 \ 路径 | `""` | `x` | `x/` | `/x` | `x/y` | `a/b` | `a/x/y/b` |
|---|---|---|---|---|---|---|---|
| `x/**` | 否 | 是 | 是 | 否 | 是 | 否 | 否 |
| `**/x` | 否 | 是 | 否 | 是 | 否 | 否 | 否 |
| `*` | 是 | 否 | 否 | 否 | 否 | 否 | 否 |
| `**` | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| `a/**/b` | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
