# 设计推导

## 非法单元
统一规则：从起点读“按首字节长度可达的最长合法前缀”；任一必需续字节不是 `80..BF`，当前起点是一个非法单元，已确定的坏续字节重新当下一个起点；若全部续字节存在但码点非标量、越界或非最短，则该整序列是非法单元。故 `F0 90 80 41` 只在第 3 字节坏，首 3 字节为一个单元，`41` 重试成新字符；`E0 80 80` 中第 2 字节必须是 `A0..BF`，第 1 个字节先成单元，余下 `80 80` 各为单元；`E2 82` 在 EOF 是合法未完成前缀，只有一个截断单元。

| 首字节 | 第二字节区间 | 第二字节非法吞掉 | 其他续字节非法 |
|---|---:|---:|---|
| `C2..DF` | `80..BF` | 1 | 无 / 2 |
| `E0` | `A0..BF` | 1 | 第3坏吞2 |
| `E1..EC` | `80..BF` | 1 | 第3坏吞2 |
| `ED` | `80..9F` | 1 | 第3坏吞2 |
| `EE..EF` | `80..BF` | 1 | 第3坏吞2 |
| `F0` | `90..BF` | 1 | 第3/4坏分别吞2/3 |
| `F1..F3` | `80..BF` | 1 | 第3/4坏分别吞2/3 |
| `F4` | `80..8F` | 1 | 第3/4坏分别吞2/3 |
| `80..BF` | 无 | 1 | — |
| `C0 C1 F5..FF` | 无 | 1（声明长度不等待） | — |

## 流式缓存
UTF-8 最长 4 字节：读到首字节后只需等待至多 3 个续字节；一旦某位置出现上面区间外字节，立即失败并把坏字节重试，不等到 EOF。EOF 仍缓存时，若是首字节或合法续字节前缀，替换模式输出一个 FFFD，严格模式报截断。UTF-16 最多缓存 1 字节；读到完整码元后，高代理最多再等待 1 个码元，低代理或非低代理立即完成非法单元，非低代理要重试。

## 并行对齐
段起点可在任意字节。每段从切点向前回看成前缀：UTF-8 回看至多 3 字节，UTF-16 奇数字节边界回看 1 字节。回看到的不完整前缀只用于状态校验，不输出、不计数；它跨过切点后由本段正常解析。已完整结束于切点前的单元不回看；UTF-8 前缀长度按其首字节声明长度与 3 取小，因此 `C3|80 80` 回看 `C3`，段内首单元会吃掉第一个 `80`，第二个 `80` 重试为独立非法单元，与单线程的 `C3 80` 合法、`80` 非法完全一致。首段独占 BOM。

## 上限断点
输出空间不足时，在读取下个标量前按最坏大小预留：UTF-8 最多 4 字节，UTF-16 最多 4 字节；高代理已解码时只预留 2 字节。成功编码后才推进输入、统计和输出。`Write` 只报告已经形成完整输出单元的输入字节；仍在解码缓存的字节返回为未消费。调用方把 `n=len(p)` 后剩余的旧尾字节接到新数据开头重传；新旧实例输出拼接，等于无限流结果。
