# 设计（CSV RFC4180 方言；不含 encoding/csv）

## 数据模型
- `cell.Cell{Value []byte; Quoted bool; Start,End int}`：`Quoted` 区分 `,` 与 `,"",`；Start=字段首字节（引号则为 `"`）偏移，End=字段末尾的独占偏移（含闭合引号）。
- 记录 `[][]cell.Cell`；空表合法；表头即第 1 条记录。

## 推导一：空行与单列空值
空行是**一条只含 1 个未加引号空字段的记录**（选择 A，不跳过）。理由：跳过会丢失行信息，且与“记录以行尾结束”的定义一致（零字段记录不存在——每条记录至少一个字段）。于是 `a\n\nb` 是 3 条记录，单列空值就是 `""` 之外的 `[]`。
往返矛盾：末尾换行不产生额外记录，故 `""`（零换行）解析为单列空记录，而 `""\n` 解析为空表。回写单列空值时若写裸空串：无尾换行则与 `""` 的**引号标记**不符，有尾换行则会被解析成空表（丢记录）。结论：**每条记录都以 `\n` 结尾（末行亦然）**；当一条记录是单列、字段空且 `Quoted` 时写 `""\n`，单列裸空值写 `\n`，二者可区分且无歧义（`\n` 前绝不会多出空记录）。空表=空字节。

## 状态机（状态×字节类 → 次态/动作/错误）
字节类：逗号 C、LF N、CR R、引号 Q、其他 D。错误：`ErrQuoteInBare`(Q 于 S/U)、`ErrBareQuote`(闭引号后非 C/N/R)、`ErrUnclosedQuote`(EOF 于 Q/QQ)、`ErrBareCR`(R 后非 N，引号内除外)。
| 现态 | C | N | R | Q | D |
|---|---|---|---|---|---|
| S 字段开始 | U,闭字段+开新字段 | U,空字段结记录 | CR | Q,开引号 | U,存字节 |
| U 未引号中 | U,闭字段+新S | U,闭字段结记录 | CR | **QuoteInBare** | U,存 |
| Q 引号中 | Q,存逗号 | Q,存LF | Q,存CR(原样) | QQ | Q,存 |
| QQ 刚见引号 | U,闭字段+新S | U,闭字段结记录 | CRf | U,闭字段 | **BareQuote** |
| CR 行尾CR待定 | 结算行尾→按S遇C | 结算→U | (裸R:先结算再**BareCR**) | 结算→**QuoteInBare** | 结算→U存 |
| CRf 引号后CR待定 | 结算→按S遇C | 结算→U | (裸R)**BareCR** | (裸R)**BareQuote** | (裸R)**BareQuote** |
EOF：U/S 结束当前字段+记录（空串即空记录，不丢）；CR/CRf=BareCR；Q/QQ=UnclosedQuote。字段首字节到达即发 fStart(off,quoted)，数据以 run 追加，逗号/换行闭字段。

## 推导二：半包续传
状态只有 5 个，全部留在结构体内；CR/CRf 是“待定”而非报错，下一 Feed 首字节再判：N→正常 CRLF 行尾，其他→在“该字节”处报 BareCR（先把挂起的行尾结算掉）。EOF 停留 CR/CRf 即 BareCR。切点落在 `""` 之间（停 QQ）或 `\r\n` 之间（停 CR）均由同一转移覆盖，故任意切分逐位相同。单实例**非并发安全**。

## 推导三：par 切点（双假设）
段 worker 无法知道起点是否在引号内，故每段只跑**两遍**：
- out 假设=字段开始；若切点位 `\r`（上字节）则以 CR 待定起步。
- in 假设=引号内；若上字节是 `"` 则以 QQ 起步（否则该 `"` 会被误吞）。
从左到右：段 0 真起点=out；段 k 的真起点由段 k-1 选中运行的结束态决定（Q/QQ→in；CR/CRf→待定型；其余→out），二者必与一个假设同态，直接采用其全部事件。拼接时若前段以开放字段结束（fStart 未闭）且后段以 fStart 续开，则丢弃后者重复的 fStart。双假设的起点设置把上一字节并入判定，故无边界误判。复杂度：每字节至多 2 次。
坐标：事件全局偏移=段首偏移+局部偏移；record/field 编号由组装器在拼接后的**全局事件流**上统一标注，错误再回填编号，故与单线程完全一致。

## 推导四：上限的“立刻”
三上限（字段字节数、记录字段数、总记录数）在组装器判定。字段计数=引号内**解码后**字节数，`""` 只算 1；每个数据 run 到达先判 `n+len > MaxFieldBytes`，即在 run 首字节偏移报 ErrFieldTooLarge 并终态，不等字段闭合、不缓冲整记录。第 fields+1 个 fStart 报 ErrTooManyFields；第 records+1 条记录结算报 ErrTooManyRecords。终态后 Feed/Close 返回同一错误。

## 四类语法错误的位置
QuoteInBare=非法 `"` 偏移；BareQuote=闭引号后第一个非法字节；UnclosedQuote=字段起始 `"` 偏移（EOF 处）；BareCR=该孤立 CR 偏移；列数=触发字节。错误带字节偏移(0 起)、记录号、字段号(1 起)。

## 回写“必要引号”与规范输入
含 `,` `"` `\r` `\n` 必须加引号；原本 Quoted 的空字段必须加（见推导一）。加引号时 `"`→`""`，CRLF 原样；列数由表保证。规范输入=行尾一律 LF（CRLF 仅作字段内容）、仅必要引号、每记录含尾 LF；对其 Write(Parse(x))==x。
