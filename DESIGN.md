# DESIGN

## 1. 零计数 hunk 头的行号（b=0 / d=0）

统一定义：`@@ -a,b +c,d @@` 中 a、c 都是 **1 基的旧/新起始行号**；count=0 表示
「在该行之后插入」，故 0 行段的行号 = 锚点行（前一行）的行号；文件开头没有前一行
时锚点号为 0。验证三样例：文件头插入时无锚点 → `-0,0 +1`（新段有 1 行，普通规则
c=1）；在旧第 2 行后插入、成为新第 3 行 → `-2,0 +3`；删除唯一行 → `-1 +0,0`
（旧段有 1 行 a=1，新文件为空、无锚点 → c=0）。`Apply` 看到 count=0 即把插入点放
在旧第 a 行**之后**（a=0 时在文件最前），绝不消费任何旧行。

| 旧段 | 书写 | Apply 解释（插入点在旧第 a 行之后，不消费旧行） |
| --- | --- | --- |
| b=0 | `-a,0`，a=锚点（前一行）号，文件首为 0 | 纯插入，a=0 即文件最前 |
| b=1 | `-a`（省略 `,1`） | 替换/删除旧第 a 行这 1 行 |
| b≥2 | `-a,b` | 作用于旧第 a..a+b-1 行 |

新段 `+c,d` 完全同构。

## 2. hunk 合并阈值

设两段改动之间相隔 g 行未改动行。各自取 C 行上下文后，中间剩余 g-2C 行；
g-2C ≤ 0（即 **g ≤ 2C**）时上下文重叠或相邻，必须合并；g ≥ 2C+1 时中间至少剩
1 行上下文之外的未改动行，必须分开。例：C=3，改动在第 1 行与第 8 行，g=6=2C，
两 hunk 上下文恰相接，GNU diff -U3 输出单个 hunk（第 1..8 行）；改为 g=7 时中间
剩 1 行，GNU 输出两个 hunk。阈值若取 C：g=6 会被错误拆开（多一个 hunk，头行号
不同）；若取 2C+1：g=7 会被错误合并（吞掉本应原样保留的第 4 行），均与 GNU 不符。

## 3. Myers 并列最短路径的选择

Myers 每层 D 对每个 k 取 `V[k] = max(V[k-1]+1(下行/插入), V[k+1](右行/删除))`。
本实现规定：**并列时优先右行（删除）**（先判定 `d > V[k+1]`，相等走 else）。因此
`a b → b a` 的脚本固定为 `-a / -b / +b / +a`（所有删除连续排在插入之前），而不是
插入先行的交错形式。回溯同样依据保存的 V 快照复现同一选择，保证确定性：同输入生成
100 次补丁逐字节相同。

## 4. 偏移应用与后续 hunk 的平移

每个 hunk 在记录的旧起始行附近 ±F 行窗口内，按「旧段完整匹配」找候选，距离取
`|实际起始-记录起始|`，最小者胜，并列取靠前。一个 hunk 以偏移 +k 命中后，后续
hunk 的**记录位置整体平移 +k**（记录的是相对同一旧文本的坐标，前一 hunk 多消费/
少消费 k 行后，后续内容在新坐标下同步移动），再在平移后的位置 ±F 内搜索；这样既
能跟随累积漂移，又不把搜索窗口无界放大。

## 5. 其余约定

- 行 = 内容 + 行尾（`\n`/`\r\n`/无），比较只看内容，拼接恢复原行尾；末行无换行用
  `\ No newline at end of file` 标记，渲染紧随对应行。
- 计数器：Myers 每次对角线 snake 内逐行相等比较与每步边移动各计 1，
  故总计 ≤ (D+1)(2M+2D+1)+D ≤ 4(N+M)(D+1)。
