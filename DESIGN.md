# 设计文档：内存倒排索引与布尔短语查询引擎

## 1. 包划分与依赖方向

`tokenize` → `postings` → `dict` → `index` → `query`，单向依赖，无反向。
`index` 持有唯一一把 `sync.RWMutex`：写操作（Add/Update/Delete）持写锁，
查询经 `index.ReadView` 在持读锁的闭包内完整执行，因此一次查询只看到某一个
完整索引状态（不变量 5）。`postings`/`dict` 的公开方法本身不加锁，其并发安全
由 `index` 的锁保证；它们的纯读方法（归并、查找）之间天然可并发。

## 2. 分词边界规则表

| 字符类别 | 归属 | 依据 |
|---|---|---|
| 空白（`unicode.IsSpace`） | 切开（分隔符） | 词的天然边界，所有检索系统共识 |
| 拉丁等字母（非 CJK 的 `unicode.IsLetter`） | 并入当前词 | 连续字母构成词 |
| 数字（`unicode.IsDigit`） | 并入当前词 | `abc123`、`v2` 这类标识符应整体可检索 |
| CJK（Han/平假名/片假名/Hangul） | 每字独立成 token | 无空格分词信息，单字 unigram 是可判定且不依赖外部词典的唯一选择 |
| 连字符 `-` | 词内且后随词字符时并入，否则切开 | `state-of-the-art` 应整体可检索；孤立 `-` 是标点 |
| 下划线 `_` | 词内且后随词字符时并入，否则切开 | `snake_case` 标识符应整体可检索 |
| 点 `.` | 永远切开 | 句末句号远多于词内点，切开是更安全的默认 |
| 其他符号 | 切开 | 默认分隔 |

另：大小写统一折叠为小写（`unicode.ToLower`），保证索引与查询一致。
token 上限 `MaxTermRunes = 64`（按 rune 计），超限返回 `ErrTermTooLong`，不静默截断。

## 3. 四处推导

### 3.1 位置语义与相邻判据

候选：字节偏移 或 token 序号。若用字节偏移，"相邻"要依赖两个 token 之间的
分隔符宽度，而分隔符宽度不唯一（`a b` 与 `a  b` 语义相同却偏移不同），判据
无法良定义。token 序号把"相邻"定义为**序号差恰好为 1**，与分隔符细节解耦，
且天然满足样例：`x a b c y` 中 `a,b,c` 序号为 1,2,3，差恒为 1 故命中；
`x a c b y` 中 `a` 后是 `c`，`a` 与 `b` 序号差为 2 故不命中；正文开头即
`a b c` 时序号为 0,1,2，同样命中。**结论：位置 = 从 0 开始的 token 序号；
`"t1 t2 ... tk"` 命中当且仅当存在位置 p 使 ti 出现在 p+i-1（i=1..k）。**

### 3.2 相关性排序的稳定性

约束：同一查询重复执行逐位相同；结果与插入顺序无关。推论：排序键必须是
**全序**，且键中**不得包含任何与插入顺序相关的成分**（如内部自增序号、
map 遍历序）。取排序键为 `(-score, docID)`：score 是文档固有内容（命中正
向子句的频次和，见下）的函数，docID 是调用方赋予的固有标识，二者都与插入
顺序无关；docID 唯一，故键构成全序，分数相同者按 docID 升序定序。两条约束
同时满足。**结论：按 score 降序，平分按 docID 升序。**

score 定义：term 子句贡献该 term 在文档中的出现次数；短语子句贡献短语在
文档中的相邻出现次数；AND 取交集、OR 取并集，分数各自相加；NOT 只过滤不
贡献分（被 NOT 排除的文档已不在结果中，剩余文档的分数不应因"不含某词"而
变化，否则 NOT 会扭曲相关性）。

### 3.3 NOT 单独出现的作用域

可组合性要求 NOT 是集合上的一元算子：`NOT S` 必须对任意集合 S 有定义，
包括 `NOT b` 单独出现。集合论中一元补算子需要论域，论域只能是"当前索引中
全部活跃文档"。又由不变量 3，已删除文档不得被任何查询命中，故论域取
`index` 中当前活跃 docID 集合（删除时同步移除），已删除文档天然不在其中。
**结论：`NOT b` = 全部活跃文档 − b 的 postings；空索引上 `NOT b` 为空集。**

### 3.4 更新时出现次数减少 / term 消失

索引中位置列表就是"该 term 在该文档的全部出现"，因此新正文出现 1 次时，
该 docID 下的位置列表必须**替换**为新列表（旧的 3 个位置作废），而不是增量
追加——追加会让已不存在的位置继续命中短语查询。某 term 在新正文中完全消
失时，从该 term 的 postings 中移除该 docID；若因此 postings 变空，由不变量
1 必须同时把该 term 从 dict 删除，否则前缀查找会返回查不到文档的幽灵 term。
**结论：Update 按 term 集合做三路差量——新增 term 执行插入、消失 term 执行
摘除（空则删 term）、共有 term 位置列表整体替换；全部变更在写锁内一次性
应用，应用阶段无失败路径（所有可失败校验在加锁前完成），故原子。**

## 4. 归并复杂度

`Intersect` 取较短一侧逐元素在较长一侧做二分查找，比较次数 O(m·log n)，
不对长侧全量扫描。比较计数器为 `postings` 包内非导出字段（`atomic.Int64`，
并发写安全），由包内白盒测试断言：短侧 3 个 docID，长侧 1000 与 100000 两
档的比较次数增长远小于 100 倍（理论约 3·10 次 vs 3·17 次）。计数器不出现在
任何公开接口；演示程序通过 `go test -run TestIntersectScaling -v` 子进程读数。

## 5. 故障与上限（均为可判定哨兵错误）

- `ErrDuplicateDoc`：Add 已存在的 docID，索引不变。
- `ErrDocNotFound`：Update/Delete 不存在的 docID，索引不变。
- `tokenize.ErrTermTooLong`：token 超 64 rune，本次 Add/Update 整体不生效。
- `index.ErrTooManyPositions`：单文档单 term 位置数超 1024，整体不生效。
- `index.ErrEmptyDocument`：空文档（零长度）；只含分隔符的文档则**成功**登记
  （存在但无 term，可被 NOT 命中），二者行为不同但都可判定。
- `query.ErrEmptyQuery`：空查询串；`query.ErrInvalidQuery`：语法错误。
