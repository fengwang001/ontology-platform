# 设计推导（CSV RFC4180 方言：半包续传 + 并行切分）

## 1. 空行与单列空值
空行（记录尚未产出任何字段即遇行尾）= **跳过**，不是一条记录；但「引号空字段 `""` 单独成行」必须保留为一条单列记录。
反例：若空行算未引号空记录，则 `a\n\n` 与 `a\n` 无法区分。回写器后果：单列表中唯一字段为空（必然是 `""`）的记录，若写成裸空串，其输出流末尾会变成空行而被跳过、或与上一记录的换行粘连导致丢记录；故该情形**必须加引号**（`"\n`）。空文件 = 0 条记录；末尾换行不产生额外记录；无换行结束的末记录合法。

## 2. par 切点
切点可能落在引号字段内、`""` 中间、`\r\n` 中间，段内无法自证引号态。方案：每段以两种入口假设（引号外 S0 / 引号内 S1）各跑一次纯函数扫描；段 0 入口为外。从左到右逐段选择：上段选中出口的「引号奇偶」决定本段选哪条假设（真双假设，固定两遍）。另需携带：上段出口是否 `\r` 待定、是否处于「刚见引号」。CR 在段尾：下一段首字节由选定结果先喂入旧待定帧。选定段内事件坐标 = 段基 + 局部偏移；记录号/字段号由**重放检查器**从段 0 起用真实累计序号统一盖戳，故错误坐标天然全局正确。引号奇偶把 `""`、字段内换行全部吸收，无反复重扫；总处理 ≤ 2N+O(K)。

## 3. CR 待定
未引号态见 `\r`：不立即判错，进入 CR 待定帧且不产出字段（`\r` 尚未确认归属）。续传下一字节：`\n`→确认行尾（终结当前字段+记录）；其它（含 EOF、引号、逗号）→孤立 CR 错误，位置在该 `\r`，错误即终态。引号态内 `\r` 是普通内容，无待定。流恰在 `\r` 结束：Finish 判孤立 CR。par 段尾 `\r` 同样只携带待定帧，由下段或 Finish 裁决，故切在 `\r\n` 中间结果不变。

## 4. 上限的「立刻」
字段字节在「进入值的那一刻」计数：未引号字节各 1；引号态中每个字面字节各 1，`""` 转义对只在第二引号确认后追加 1 个字节（即一对双引号=1 字节值）。故第 (MaxFieldBytes+1) 个值字节到达的同一次处理即报错，不缓冲整条记录。字段数在字段起始事件即计数；总记录数在新记录第一个字段起始（跳过空行在其前完成）时计数。错误即终态，后续 Feed/Close 返回同一错误。

## 5. 状态转移表
状态：F=字段开始 B=未引号 Q=引号内 A=引号后刚见引号 C=CR待定。字节类：`,` `"` `\r` `\n` 普通 EOF。
| 现态 | , | " | \r | \n | 普通 | EOF |
|---|---|---|---|---|---|---|
| F | 终空字段/新字段,→F | →Q | →C | 终字段+终记录,→F | 追加,→B | 无记录则空；否则终字段+终记录 |
| B | 终字段/新字段,→F | 错误 ErrBareQuote | →C | 终字段+终记录,→F | 追加,→B | 终字段+终记录 |
| Q | 追加 | →A | 追加,→Q | 追加,→Q | 追加,→Q | 错误 ErrUnclosedQuote |
| A | 终字段/新字段,→F | 追加一引号,→Q | 错误 ErrQuoteClose | 终字段+终记录,→F | 错误 ErrQuoteClose | 终字段+终记录 |
| C | 错误 ErrLoneCR | 同左 | 同左 | 终字段+终记录,→F | 错误 ErrLoneCR | 错误 ErrLoneCR |
错误位置均为当前字节绝对偏移；列数不一致由表组装层在字段起始/记录终结时判定 ErrFieldCount。单实例非并发安全；par 内部 goroutine 并发，实例间互不共享。
