# 可撤销批量重命名设计

## 1. 基本模型
- `name.Namespace` 用 `map[string]struct{}` 表示当前名字集合，用 `sync.RWMutex` 保护。
- 空字符串是合法名字；`/`、`\` 等路径分隔符只作为普通字符，因此所有字符串均合法。
- 请求为 `{Old, New}`；实际修改前先完整做冲突检测，再生成计划。

## 2. 四类冲突
1. `ErrMissing`：`Old` 当前不存在。
2. `ErrTargetExists`：`New` 已存在，且它不是本批某个会被搬走的旧名。
3. `ErrDuplicateTarget`：两个不同旧名请求同一个新名，错误记录两个旧名。
4. `ErrDuplicateSource`：同一旧名出现两次，错误记录两个新名。
自环 `a->a` 定义为合法无操作：它不读写字典，也不参与拓扑和环检测。

## 3. 顺序推导
每个请求是一条 `Old -> New` 边。若请求 `x->y` 的目标 `y` 也是另一个待执行请求的旧名，则必须先搬走 `y`，即 `y->z` 先于 `x->y`。
因此依赖规则是：`step.Dst == other.Src` 时，`other` 必须先执行。对该依赖图做拓扑排序。
例：`a->b, b->c` 中 `b->c` 先执行，之后 `a->b`，任何时刻目标名均不存在。
同一拓扑层按旧名字典序排列，保证输入打乱后步骤序列仍一致。

## 4. 环与临时名
合法请求中每个旧名出度至多一条，冲突检测也保证新名不重复，所以非自环连通分量只可能是链或简单环。
对环选字典序最小节点 `x`，令 `p(x)` 为其前驱：
1. `x -> tmp`；
2. 从 `p(x)` 开始沿前驱方向执行原请求：`p(x)->x`、`p(p(x))->p(x)`……；
3. 最后 `tmp -> New(x)`。
第一步腾出环中一个位置，随后每个原目标都已腾空；最后一步填入最初腾出的目标。每个独立环只需一个临时名，所以临时名数量等于环数。
临时名从固定前缀加序号生成；若命中现有名、目标名或已生成临时名，则递增重试。

## 5. 执行、回滚与撤销
- 执行期间持有命名空间写锁；其他修改会被阻塞，`TryLock` 可观测为拒绝。
- 每成功一步，把 `Src/Dst` 和载荷 CRC32 追加到临时目录日志，再执行下一步。
- 第 k 步失败或故障注入失败时，按日志逆序撤销已完成步骤，然后返回错误，不保留部分结果。
- 正常撤销也逆序执行；某条记录的 `Dst` 不存在且 `Src` 已存在时视为已撤销，故重复撤销幂等。

## 6. 日志格式与截断
文件为固定头 `ONTLOG1\n`，后接若干记录：
`uint32(载荷长度) + JSON载荷 + uint32(CRC32(载荷))`。
截断分类：
- 短于固定头：`ErrHeaderIncomplete`；
- 长度字段、载荷或 CRC 未写完整：`ErrRecordIncomplete`；
- 记录边界完整但 CRC 不符：`ErrCRC`。
撤销始终恢复损坏点之前的最大完整前缀，并报告无法撤销的步数。

## 7. 复杂度
冲突检测、建图、破环和拓扑均随请求数与涉及名字数线性遍历；包内计数器统计 map 查找，约束为不超过 `4*(请求数+涉及名字数)`。排序仅用于同层确定性输出。
