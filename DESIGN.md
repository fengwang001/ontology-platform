# 设计推导

## 1. 非法单元切分

解码器保存首字节、已接受值和还需续字节数。每个字节先按当前状态检查；非法时，**已缓存前缀**形成一个非法单元，触发出错的那个字节不吞掉，而是作为新首字节重新判定。

- `F0 90 80 41`：90、80 都是 F0 后的合法续字节；41 不是续字节，缓存的 `F0 90 80` 成一个单元，41 重排队后合法。
- `E0 80 80`：E0 后第二字节必须 ≥A0，故 E0 成一个单元；80 重排队，是孤立续字节；最后 80 同样孤立，共三个。
- `ED A0 80`：ED 后第二字节必须 ≤9F，A0 令 ED 成单元；A0、80 各自孤立，共三个。
- `C0 AF`：C0 非法成一个；AF 重排队成孤立续字节，共两个。
- `F4 90 80 80`：F4 后第二字节必须 ≤8F；F4、90、80、80 依次各成一个。
- `E2 82`+EOF：82 合法，流结束时整个未完成前缀成一个。
- `80 80`：两个孤立续字节。

| 首字节 | 第二字节合法区间 | 第二字节非法时吞掉 |
|---|---|---|
| C2..DF | 80..BF | 1（非法字节重排队） |
| E0 | A0..BF | 1 |
| E1..EC | 80..BF | 1 |
| ED | 80..9F | 1 |
| EE..EF | 80..BF | 1 |
| F0 | 90..BF | 1 |
| F1..F3 | 80..BF | 1 |
| F4 | 80..8F | 1 |
| 80..BF | 无 | 1 |
| C0 C1 F5..FF | 无 | 1 |

若非法发生在第 3/4 字节，已缓存的 2/3 字节成一个单元，出错字节重排队。

## 2. 流式缓存

UTF-8 最长 4 字节，已开始未完成的前缀最多 3 字节；UTF-16 只需 1 个奇字节和可选高代理状态。统一硬上限为 3 字节。非法 lead、ASCII 或新 lead 可立刻终结等待；只有合法续字节且仍缺字节时才继续缓存。

## 3. par 切点

从每个切点向前最多检查 3 字节，再向后检查有限续字节，确定切点所属单元的起点；该起点同时是上段终点和下段起点，段范围因此无重叠、无遗漏。`C3 | 80 80` 中 C3 的长度覆盖切点，下段从 C3 开始，先接受 C3,80，再把第二个 80 判为孤立续字节，与流式计数一致。落在非法单元中间时同样把整个前缀交给含其首字节的段；触发出错的重排队字节属于自己的段。

## 4. 超限断点

只在完整标量或完整非法单元完成后检查上限，绝不写半个标量或半个代理对。缓存中尚未判定的字节不算已消费；`Write` 返回的 n 截止到最后一个已完整处理字节。调用方用 `p[n:]` 续传到新实例，统计只随完整事件增长，所以拼接与不限流完全一致。
