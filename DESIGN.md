# 设计推导：Java `.properties` 解析/回写

## 第 3 条：只含空白（或空）的续行

对照 `java.util.Properties` 的 `LineReader#readLine`：进入续行后，
它先 `skipWhiteSpace()`（吃掉前导空白），再判断是否注释行，最后才拼接。
`skipWhiteSpace` 对**整条**全是空白的物理行，会一直吃到行尾并触发
`readLine` 取下一物理行——即这条空白行被整体跳过，续行关系继续传递。

结论：
- `a\` + `   ` + `b` → 逻辑行 `ab`（中间的空白行不贡献任何字符，
  也不终止续行链）。
- 这不是“保留空白”，而正是“续行首部空白被剥掉”这条规则作用于**整条行**
  的结果：剥首部空白剥到行尾仍未遇到非空白字符，该行自然没有可拼接内容，
  于是继续取下一行并再剥一次其首部空白。
- `k=v\`（文件结束、再无物理行）→ 结尾反斜杠丢弃，得 `k`→`v`。

## 第 6 条：Store 只转义必要字符

以“Load(Store(m)) 与 m 相等且顺序一致”为约束逐条推导：

键（key）：
- 行首的空白会被 Load 当行首空白忽略 → 键首空格/Tab/FF 前加 `\`。
- 键首是 `#`/`!` 会让整行变成注释 → 必须加 `\`。
- 键中间的 `=`、`:`、空格/Tab/FF 会被当作键值分隔符 → 全部加 `\`。
- 键中间的 `#`/`!` 不会变注释（注释只看行首）→ 原样。

值（value）：
- 分隔符只在键阶段寻找，故值里的 `=`、`:`、`#`、`!`（包括位于
  值首）都不必转义；值也不可能让整行变成注释（前面有键与分隔符）。
- 值**开头**的空白会被 Load 当“分隔符右侧空白”吃掉 → 只在值首第一个
  空白字符前加 `\`；中间的空白不参与任何规则，原样保留。

键与值共同：
- 反斜杠一律 `\\`（否则会被当成转义引导符）。
- `\n`/`\r` 会造成物理换行必须写成 `\n`/`\r`；`\t`/`\f` 为稳妥写成
  `\t`/`\f`（Load 能还原，且避免裸 Tab 在键中被当分隔符）。
- 非 ASCII 按 UTF-8 原样输出（Load 以 Reader 解码，不依赖 `\uXXXX`）。
- 空键时用 `=值` 表示（空键 + 分隔符）；空值写裸键或 `键=`，Load 皆得
  空值；本实现统一写 `键=`。

## 语义—测试对照表

| 第二节语义 | 钉住它的测试函数 |
|---|---|
| 1 分隔符与空白 | `TestSeparators` |
| 2 注释 | `TestComments` |
| 3 续行（含空白续行） | `TestContinuation` |
| 4 转义与 `\u` 行列号 | `TestEscapes` / `TestUnicodeErrorPosition` |
| 5 重复键首次顺序 | `TestDuplicateKeysOrder` |
| 6 回写特殊字符 | `TestStoreRoundTripSpecials` |
| 7 随机 1000 组往返 | `TestRandomRoundTrip` |
| 计数器 ≤2n | `TestScanBudget` |
