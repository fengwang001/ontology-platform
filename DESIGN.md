# 严格模式流式 Base64：规范尾部推导

## 推导（语义 2）

Base64 把每 3 字节（24 bit）切成 4 个 6 bit 值（记 s0..s3）。输入长度对 3 取余：

- 余 0：4 个 sextet 全部携带输入比特，无 `=`。
- 余 1：编码 1 字节（8 bit），只用 s0 全部 6 bit 与 s1 高 2 bit；
  输出形式 s0 s1 `=` `=`，s1 的**低 4 bit 必须为 0**。
- 余 2：编码 2 字节（16 bit），只用 s0、s1 全部 6 bit 与 s2 高 4 bit；
  输出形式 s0 s1 s2 `=`，s2 的**低 2 bit 必须为 0**。

验证样例：`QQ==` 中 s1=`Q`=16=`010000`，低 4 bit 全 0，合法（解码 0x41=`A`）；
`QR==` 中 s1=`R`=17=`010001`，**最低位是 1**，低 4 bit 非零，该比特不承载任何
输入信息却为 1 → 非规范，拒绝。`QUI=`：s2=`I`=8=`001000` 低 2 bit 为 0，合法；
`QUJ=`：s2=`J`=9=`001001` 低 2 bit 非零，拒绝。

统一规则（canonical）：一个 4 字符量子（quantum）合法当且仅当：
1. 无填充：4 个 sextet 都是普通字符；
2. `xx==`：后两位确为 `=`，且 s1 的低 4 bit 为 0（仅产出 1 字节）；
3. `xxx=`：末位为 `=`、第三位不是 `=`，且 s2 的低 2 bit 为 0（产出 2 字节）。
其余填充形式（`x=x=`、`=xx=`、`===x`、`xxx` 末尾缺填充等）一律拒绝。

与编码唯一性的关系：对任意字节串 x，其长度模 3 唯一确定填充形状，而每个填充量子
中未使用的低位若允许为任意值，则多个不同输入字符串会解码到同一 x（如 `QQ==` 与
`QR==` 都宽松解码为 `A`），`Decode` 不是单射，于是 `Encode(Decode(y)) != y`，
往返与“编码结果唯一”同时被破坏。要求未使用比特为 0 后，严格接受集合恰为
`{Encode(x)}`，`Encode` 与严格 `Decode` 互为逆映射。

流级别：填充只允许出现在整个流的最后一个量子（见 `QQ==QQ==` 拒绝）。
