# Properties 解析器设计说明

## 第 3 条：续行与“空白续行”推导

依据 `java.util.Properties` 的 `LineReader.readLine`（OpenJDK）：

1. 每读一个物理行，先剥掉行首空白；剥完为空则整行丢弃；首字符为 `#`/`!` 则整行是注释。
2. 从行尾向前看反斜杠：逐个 `\` 翻转 `precededByBackslash` 标志，只有停下时标志为真（奇数个）才续行；
   续行时去掉末尾一个 `\`，其余 `\` 原样保留。
3. 续行后读下一物理行，重复第 1 步：**该行走的是同一个分类流程**。

结论：续行接到的下一物理行若只含空白（或为空），它在第 1 步就被整行丢弃，
既不向逻辑行追加任何字符，也不剥离出“空内容”拼接；读取再下一物理行时 `\` 已被第 2 步去掉，
续行状态不复存在，因此再下一行作为**新逻辑行**重新分类（空行/注释/正常行）。
它与“续行首部空白被剥掉”是同一条规则的两面：新物理行总是先剥离行首空白再分类/拼接，
剥完为空时剥离的结果就是“没有内容可接”，逻辑行在该物理行处自然终止。
EOF 前仅剩一个 `\`：读不到下一物理行，该 `\` 同样被去掉后返回。

## 第 6 条：回写转义规则推导（只转义必要字符）

回写格式为每行 `key=value\n`，要保证再解析逐项还原，需对照“解析时哪些字符有特殊含义”：

- 反斜杠在解析时永远是转义/续行引导符：键值中任何 `\` 必须写成 `\\`；
  行尾值若以 `\` 结尾不转义会触发续行，故也必须转义。
- `\n`、`\r` 会切断物理行，必须写成 `\n`/`\r`（否则跨接结果错误）。
- `=`、`:`、空白只在“键到值的分隔”处有含义：键中的它们必须转义（`\=`、`\:`、`\ `），
  值中的 `=`/`:`/空白永远只是值内容，故不转义。
- **值开头的空格必须转义**：解析键值时，分隔符后的前导空白会被跳过（trim leading whitespace），
  不转义就会丢失；而值中间/结尾的空白不在跳过范围内（结尾空白保留），无需转义。
  值开头的制表符同理。
- **以 `#`/`!` 开头的键必须转义，值不必**：行首（剥行首空白后）首字符为 `#`/`!` 的整行是注释，
  键位于行首，必须写 `\#`/`\!`；值在第一个分隔符之后，行首判定永远到不了它，故无需转义。
  键开头的空白同理：会在剥行首空白阶段丢失，必须转义。
- 非 ASCII 字符没有任何解析特殊含义，按 UTF-8 原样输出，不写 `\uXXXX`。
- 空键：行首直接写 `=value`（`=` 前为空键）；空值：写 `key=`。

## 语义—测试对照表

| 语义条目 | 钉住它的测试函数 |
| --- | --- |
| 1 分隔符与空白 | `TestDelimiters` |
| 2 注释 | `TestComments` |
| 3 续行（含空白续行、EOF） | `TestContinuation` |
| 4 转义与 `\u` 错误行列号 | `TestEscapes` |
| 5 重复键覆盖、顺序按首次出现 | `TestDuplicateOrder` |
| 6 Store 转义与往返一致 | `TestStoreRoundtrip` |
| 7 1000 组随机往返 | `TestRandomRoundtrip` |
| 复杂度：字节检查计数 ≤ 2N | `TestByteCheckCounter` |
