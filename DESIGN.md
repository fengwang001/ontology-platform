# DESIGN

## 1. 非法单元切分规则（WHATWG 风格，统一样例）

首字节决定序列长度 `n`。从第二字节起要求 `80..BF`；但第二字节在
`E0/ED/F0/F4` 时有更窄区间（见下表）。一旦某字节失配：**若已经接受过
≥1 个续字节（或当前失配字节是该序列的第二字节且首字节本身越界类别不同），
当前失配字节回退，不吞**；否则把失配字节也吞掉。即"非法单元 = 合法前缀
+ 第一个非法续字节"，第二字节非法时整个首字节前缀作废，第二字节另起。

- `F0 90 80 41`：F0 要求 90..BF，90 合法→接受；80 失配→单元 `F0 90 80`
  （3 字节），41 回退另起 → 一个 FFFD + A。
- `E0 80 80`：E0 要求 A0..BF，第二字节 80 非法，单元只含 E0；后续两个 80
  各自是孤立续字节 → 共 3 个 FFFD。`ED A0 80` 同理（ED 要求 80..9F，A0
  非法 → ED 一个，A0、80 各一个）。
- `C0 AF`：C0 是"过长首字节"类（与 C1、F5..FF 同），首字节本身非法且长度
  视为 1，AF 另起 → 2 个。`F4 90 80 80`：F4 要求 80..8F，90 非法→F4 单独；
  90、80、80 各单独 → 4 个。
- `80 80`：孤立续字节每个长度 1 → 2 个。
- `E2 82`+EOF：E2 接受 82 后期待第三字节，EOF 时整个**合法前缀**（2 字节）
  合并成 1 个非法单元（不是 2 个）。

第二字节合法区间与失配吞字：

| 首字节 | n | 第二字节区间 | 第二字节非法时 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 吞首字节，失配字节另起 |
| E0 | 3 | A0..BF | 同 |
| E1..EC | 3 | 80..BF | 同 |
| ED | 3 | 80..9F | 同 |
| EE..EF | 3 | 80..BF | 同 |
| F0 | 4 | 90..BF | 同 |
| F1..F3 | 4 | 80..BF | 同 |
| F4 | 4 | 80..8F | 同 |
| 80..BF | 1 | ——（首字节即非法，吞 1） | |
| C0 C1 F5..FF | 1 | ——（首字节即非法，吞 1） | |

第 3 及以后的续字节只要求 80..BF，失配时失配字节回退另起（故 F0 90 80
吞 3 个、41 回退）。

## 2. 跨切分缓存

缓存里只保留"仍是某合法序列合法前缀"的字节，最长序列 4 字节，故最多缓存
**3 字节**（首字节已吃进）。看到第二字节超出该首字节区间、或后续续字节不在
80..BF 的那一刻立刻判非法，不等 EOF；只有"前缀一直合法但字节数不够"要拖到
`Close`，此时整个前缀合并为一个截断单元（替换 1 个 FFFD；严格模式为截断错误，
与非法字节错误是两种哨兵/具名类型）。

## 3. par 切点对齐

每个字节唯一属于"以某个首字节开头的那个单元"。段只在**单元首字节**处真正
开始解析。中间段从切点向左最多回看 3 个字节：若切点落在续字节中，向左找到
单元首字节；该单元归含首字节的上一段，本段从其后的下一个单元首字节开始。
这样每段独立转码、输出直接拼接。`C3 | 80 80`：切点在 C3 之后，单元 C3 80
归上段（首字节 C3 在上段），下段从第二个 80 开始，该 80 是孤立续字节 → 与
单流"1 个合法字符 + 1 个非法单元"完全相同。回看常数 ≤3，无整段重扫；
UTF-16 回看 1 字节对齐到偶偏移，并检查代理对首码元归属。只有第 0 段承认
BOM；只有最后一段在末尾做截断判定。严格模式取最早报错段。

## 4. 上限断点

输出超限时停在**已完整输出的最后一个标量**之后；`Write` 返回的 n 等于
"这些已输出标量在本次 p 中覆盖到的字节数"。切分缓存里尚未定性的字节**不算
已消费**：它们可能来自更早的 Write，超限返回后通过 `Pending()` 一并交回，
调用方把 `Pending()` 与本次未消费尾巴接到新实例头部续传，拼接逐字节等于
无限流。统计：合法字节 + 非法吞掉字节 + BOM 字节 == 输入总字节。
