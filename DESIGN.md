# 带撤回的增量聚合视图维护器设计

## 1. 范围与模型

基表变更以只增日志的形式到达，每条变更包含：操作类型、记录 ID、旧组/新组、旧值/新值与单调版本号。
视图只保存当前成员表和每个分组的聚合状态；日志在本地临时目录追加持久化。

支持三类操作：

- Insert：新记录进入一个组。
- Delete：已有记录离开一个组。
- Update：同一记录原子地离开旧组并进入新组；同组更新等价于先撤回旧值再加入新值。

## 2. 聚合撤回推导

设组内成员为多重集合 `M={(id,value)}`。

### Count

`Count(M)=|M|`。插入加一；删除时被删值不参与结果，可直接减一。
删除不需要枚举成员；组内成员数降为 0 时删除该组。

### Sum

`Sum(M)=Σvalue`。插入加 `value`；删除减同一个 `value`。
浮点结果仍可做该增量运算；审计比较使用 `math.Float64bits` 检查位级一致性。
删除不需要枚举成员。

### Min / Max

普通聚合状态只保存当前极值。删除非极值时，极值不变；若删除的成员正好等于当前极值，无法仅凭标量状态判断：

1. 组内是否还有另一个相同极值；
2. 若没有，剩余成员中的新极值是多少。

因此 Min/Max 的删除先做“候选撤回”；当被删值等于当前极值时，将该组标记为重算。
重算枚举当前组的剩余成员，分别取最小值或最大值。不得访问其他组。

### DistinctCount

结果是不同值的数量，仅保存集合即可正确插入，但删除某个值时必须确认该值是否仍被其他成员持有。
为了让删除保持真增量，内部保存 `distinctValue -> 引用次数`；引用计数为 1 时删除该值，否则仅减引用。
这使删除不需要访问“全部成员”，但仍明确暴露该聚合依赖成员引用信息；若外部只给标量聚合状态则必须重算。

## 3. 多阶段提交流程

一次变更按以下阶段执行：

1. Apply：校验版本与输入，在待提交影子状态中修改成员表和聚合状态，记录受影响组。
2. Recompute：只处理被标记的组，按当前剩余成员重新计算相关聚合，并累计重算次数与访问成员数。
3. Commit：在同一把视图互斥锁内发布影子状态、更新最大版本号、记录拒绝计数。

重算是例外：只有删除当前 Min/Max，或聚合器声明需要成员而增量引用信息不足时才发生。
Count 与 Sum 的重算次数始终为 0。

## 4. 版本与幂等

版本空间是全序整数。设视图已提交最大版本为 `last`：

- `version < last`：版本回退，返回可判定错误 `ErrVersionRollback`，不修改视图。
- `version == last`：重复应用，返回可判定错误 `ErrDuplicateVersion`；视图逐字段不变。
- `version > last`：正常提交，并将 `last` 推进到该版本。

乱序不是重排序缓冲问题；本设计要求调用方按日志顺序重放，因此任何回退或重复都拒绝并计数。
缺失分组键和 NaN 同样在 Apply 前拒绝，防止非法记录进入日志之后污染状态。

## 5. 日志格式与截断分类

文件以固定 magic/version 头开始，之后每条记录使用：

`[4 字节大端长度][序列化记录][4 字节 IEEE CRC32]`

重放按边界顺序读取并分类：

- 文件短于文件头：头部不完整。
- 文件头后剩余字节不足 4：长度前缀不完整。
- 长度声明的记录体或 CRC 字节不足：记录体不完整。
- 字节足够但 CRC 不匹配：CRC 不匹配。

CRC 之后若还有字节，继续解析下一条。重放遇到任一损坏边界即停止；已完整通过 CRC 的记录记录构成恢复结果，半截记录绝不生效。

## 6. 崩溃恢复

演示在三个可注入点制造恢复：

- Apply 中途：日志尚未追加完整变更；成员表与聚合状态均不发布。
- Recompute 中途：崩溃前状态可能已临时标记，但未 Commit；恢复时丢弃内存状态。
- Commit 之前：即使日志已 fsync，内存发布未完成；重启后仍从日志完整前缀重放。

三种恢复都从空视图重放日志完整前缀，因此与无崩溃运行的已确认前缀逐字段相同。

## 7. 并发与一致性

所有提交、重放和查询通过同一把 `sync.RWMutex` 串行化发布。
读操作在 RLock 下获取一致快照；提交只在全部组、全部聚合更新完成后释放写锁，因此不可能看到 Count 已改而 Sum 未改。
Recompute 与新写入同在写锁内，同组写入不会丢失。

## 8. 审计

审计器不读取增量聚合状态，只从视图当前成员表重新构造分组并全量计算所有聚合。
随后逐组比较：缺失组、多余组、Count/Sum/Min/Max/DistinctCount 均必须一致；浮点 Sum 比较 `math.Float64bits`。
