# 带租期的分布式序列号分配器 — 设计

## 1. 架构

至少五个包，依赖单向：`clock` ← `durable` ← `lease` ← `alloc` ← `audit`；`cmd/demo` 串联。

- `clock`：`Clock` 接口（`Now()`、`Sleep`），`RealClock` 与可注入的 `FakeClock`（手动推进，返回单调时间）。
- `durable`：目录内单个计数器文件 `counter.dat`，跨实例互斥用同目录锁文件 + `flock`。
- `lease`：号段 `[Start, Start+Len)` + 到期时刻；判定剩余量、是否到期。
- `alloc`：向 `durable` 批量续租，在本地 O(1) 分发；租期到期作废剩余号。
- `audit`：收集多实例分发记录，校验唯一性、实例内单调性、统计空洞区间。

## 2. 持久化必须先于分发（崩溃重号推导）

错误做法：租到 `[100,200)` 只把持久值推到 100，边分发边推进。
实例分发到 150 时崩溃，存在两类已离开进程边界的在途响应（150 之后的号可能已经写入
下游存储 / 返回给调用方）。重启后若持久值只反映"已分发位置 150"，新实例会再次租用
`[150,250)`，于是 150..199 被第二次发出 —— 重号。持久值无法精确记录"哪些在途响应
确实被对方收到"，故任何滞后提交都不安全。

正确做法：租段是一次原子的"领号区间"事务 —— 实例调用 `Advance(n)` 把持久计数器
从 `s` 原子推到 `s+n`，**fsync + 原子改名落盘成功后**才允许在本地发出 `[s,s+n)` 中
的任何号。崩溃后重放持久文件：值已经是 `s+n`，新段必从 `s+n` 起。崩溃时
`[已分发, s+n)` 成为空洞（可接受），但任何号至多被发出一次。

## 3. 租约到期作废语义

租约带 `expireAt`。分发前检查 `now >= expireAt`：到期则整段作废，剩余号一律不再
发出，立即租用新段。作废的剩余号计入空洞，绝不续用 —— 否则"丢失"的实例复活时会
与新租用者重号。租期 0 允许：每段只发一个号，下次分发时段恰好到期，必然重租。

## 4. 时钟回拨

`alloc` 记录上次观测时间。每次分配先取时钟：`now < lastSeen` 立即返回
`ErrClockRolledBack`，不触碰任何状态。回拨可能让"按墙上时间判定到期"的租约看似
复活，故拒绝是唯一安全选择。恢复到不早于 `lastSeen` 后再分配（当前段仍有效则
继续，无新增空洞）。

## 5. 段长自适应

段长 ∈ [Min, Max]，初值 Min。段被**耗尽**（高频信号）→ `size=min(Max, size+size/2)`；
段被**到期作废**（低频/失活信号）→ `size=max(Min, size/2)`。高频阶段段长迅速爬向
Max，持久写摊薄；低频阶段回落到 Min。段长 ≤ 0 为配置错误。段长 1 合法：每次分发
一次持久写。

## 6. 性能与资源

- 分发 10 万、段长 1000：恰好 100 次 `Advance`（≤120 的余量留给首段）。
- 本地快路径：一次取号 = 比较到期 + 边界判断 + 加法，O(1)，测试以操作计数断言。
- 续租路径在实例锁内完成；跨实例由 `flock` 串行化，8 协程/4 实例段互不重叠。

## 7. 持久文件格式与损坏拒绝启动

`counter.dat`（共 16 字节）：`[magic:4 "ONC1"][version:1=1][reserved:3][value:8 BE]
[crc32:4 IEEE, 覆盖前 12 字节]`。写入走 `counter.dat.tmp.<pid>` 临时文件 + fsync +
`rename`（原子替换）+ 目录 fsync。损坏时若"回退到 0 重新开始"，新段 `[0,..)` 与
历史已发号大面积重号，故必须拒绝启动。按截断长度分类：

- `< 12`：头部/值不完整（再细分 `<4` 魔数不完整、`<12` 值不完整）；
- `12..15`：值完整但 CRC 不完整 → CRC 不匹配类（长度 16 为完整文件，纯截断测试不含）。

三类错误 `ErrHeaderIncomplete` / `ErrValueIncomplete` / `ErrCRCMismatch` 可用
`errors.Is` 判定；写失败 `ErrWriteFailed`；越界 `ErrOverflow`；段非法 `ErrBadLease`。

## 8. 边界

- 首次启动无文件：从配置起点（默认 0）开始，不预先写文件，首次租用时创建。
- uint64 上界：`value + n < value`（或不足剩余空间）→ `ErrOverflow`，拒绝且不修改文件。
- 持久写失败：临时文件阶段失败不动原文件；调用方租段失败，不分发任何号。

## 9. 不变量

唯一性（跨实例，持久计数器是唯一领号仲裁者）、实例内严格单调（本地游标只增）、
空洞不禁止：空洞 = 作废段剩余 + 崩溃丢弃，audit 精确报区间数与总长度。
