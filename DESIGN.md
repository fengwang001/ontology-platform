# 设计推导

## 1. 末尾换行策略（空输入 / 纯换行）

规范化主体 N 先产出「行规范化」结果 y：行尾统一 `\n`、删行尾空白。策略只改结尾：
- Keep：原样，结果 = y。
- 关键约束（第 6 条）：ToOut 是定义在 origLen+1 个整点上的单调阶梯，
  ToOut(ToOrig(o))=o 要求其值域覆盖 outLen+1 个整点，故必须 outLen≤origLen
  ——任何策略都**不得增长**长度，否则插入的 `\n` 无原文字节可锚定，互逆律
  必破。故 EnsureOne 不凭空补行：空→空；末尾有连续 `\n` 裁到一个；否则原样。
- Trim：末尾连续 `\n` 裁到一个，但若整份 y 全是这些 `\n`（只有空行）则全删
  →空；y 不以 `\n` 结尾则不动。与 EnsureOne 在 `"\n\n"` 上可区分。两策略再
  作用于自身结果都不变，幂等成立。

| 输入 | Keep | EnsureOne | Trim |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `""` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 2. 被删字节的 ToOut

不变量：ToOrig 单调不减且 ToOut(ToOrig(o))=o。`\r\n`→`\n` 中 `\r` 被删，
输出唯一对应原 `\n`（行尾位置）。要让被删位置可回指且保持单调不减，被删
连续字节（一串行尾空白、`\r`）的 ToOut 必须等于「下一个保留字节的输出位
置」；若删除位于末尾（无后继），等于输出长度。故 ToOut 是阶梯函数：
- `\r\n` 的 `\r`：ToOut= `\n` 的输出偏移（换行符处）。
- 行尾空白串 `"  "` 后随 `\n`：两个空白都映射到 `\n` 的输出偏移，即
  换行符**之前**（编辑器光标落在行尾换行前，等于该行内容末尾）。
- 末尾被策略裁掉的 `\n`：映射到输出长度（终点）。
ToOrig 只在输出实占区间取值，因此互逆恒成立。

## 3. 空白缓冲超限

强制把未判定空白当行内空白输出：若后续真出现行尾，这些空白必须被删除，
但它们已输出——只能在映射里事后删除，使流式 Write 的已返回输出与最终输出
不一致，破坏「已输出内容保留」语义与截断遍历一致性（见语义 5、故障注入）。
故选**拒绝**：返回哨兵 `ErrBufferLimit`（OffsetError 带原文偏移），已输出内
容保留，实例进入终态，再写返回 ErrClosed，错误可判定、状态可解释。
输出总字节超限同理立即 ErrOutputLimit；严格模式 NUL 立即 ErrNUL。

## 4. par 拼接

各段不知道段尾是否是待定 `\r` 或未判定空白。做法：段 k 处理
`[start_k,end_k)` 时，把最后一个可能影响判定的「未决尾巴」c（最多
max(BufferLimit)+1 字节，即待定 `\r` 1 字节或一串空白）通过 handoff 交给
段 k+1：段 k 的定案输出只覆盖到尾巴之前，段 k+1 从 `c||自己的字节` 重放
（`CloseOpen` 不做末尾策略）。尾巴有上界（≤ 缓冲上限+1），故修正代价有界。
最后一段正常 Close（应用末尾策略）。映射坐标：每段构造时给 origBase，段内
原偏移加上 base；输出段拼接时平移 outBase；span 仅随删除点增长，
相邻段若映射连续则合并，保证区间数不随段数膨胀。

## 5. 复杂度

span 用有序段数组（保留段含 orig/out 双起点；删除段为零输出宽区间），
ToOrig/ToOut 各自二分（sort.Search），非导出计数器 lastProbes 记录本次
比较的区间数。无删除的纯 `\n` 文本只有 1 段。
