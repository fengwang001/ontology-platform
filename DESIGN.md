# 带撤回的增量聚合视图维护器 — 设计推导

## 1. 模型

变更流 `change.Change{Ver, Op, RecID, Group, Value, NewGroup, NewValue}`，
版本号 `Ver` 单调递增。基表记录以 `RecID` 唯一标识；组内成员记录为
`(RecID -> value)`。视图对每个分组同时维护 5 个聚合器的结果。

## 2. 撤回策略推导

删除值 `x` 时，新聚合 `f(S\{x})` 能否由旧状态 `z=f(S)` 推出？

- **Count**：`z=|S|` 是带符号可加量，`|S\{x}|=z-1`。删除恒可增量。
- **Sum**：`z=ΣS` 满足 `Σ(S\{x})=z-x`。删除恒可增量。
- **Min/Max**：状态只保存一个极值。若 `x≠z`（删的不是当前极值），结果
  不变，可增量；若 `x==z`，被删后新极值可能是其余任意元素，单值状态
  不含任何信息，必须遍历组内剩余成员重算。
- **DistinctCount**：去重计数需要知道 `x` 是否还有其他持有者。仅持计数
  无法回答，必须访问成员（或额外引用计数）；按题意统一走成员重算。

因此聚合器显式声明 `NeedsMembersOnDelete()`，view 在删除（含更新的删半
步）阶段据此标记该组进入 Recompute 阶段；其余删除走纯增量 Apply。
重算是例外：Min 仅在删到当前极值时触发（判据为删除时当前极值 == 被删
值），Count/Sum 永不触发。

## 3. 三阶段编排 Apply → Recompute → Commit

- **Apply（持写锁）**：幂等/回退校验、成员表增删、各聚合器增量更新，
  并记录受影响组与「需重算」标记。
- **Recompute（持写锁）**：对标记组，用该组成员集合重建聚合器，计数器
  累加触发次数与访问成员数。
- **Commit（持写锁）**：结果与 Apply 在同一把锁内原子落位，删空成员的
  组整体移除；随后释放锁并追加日志。读者只能看到变更前或变更后的完整
  状态，不可能看到 Count 已更而 Sum 未更的半更新。

## 4. 版本幂等与回退

视图保存 `maxVer`。`Ver<maxVer` 返回 `ErrVersionRollback`；`Ver==maxVer`
直接成功且不触碰任何状态字段（相同记录的重放天然幂等，判据就是版本号
相等）；`Ver>maxVer` 正常处理。缺失分组、NaN 值拒绝并计入 `Rejected`，
不进入视图与日志。

## 5. 日志与崩溃恢复

文件 = 自描述头（5B 魔数 `ONTJ\x01` + 8B 期望条数）+ 帧序列；帧 =
`uvarint(长度) ‖ 定长 body(128B) ‖ CRC32`（CRC 覆盖 body）。重放以「已
知名数」严格分类每个截断点：头不完整（<13B 或魔数不符）/ 长度前缀不完整
（下一帧只有不完整的 varint 字节，定长 body 使其只能断在长度首字节）/
记录体不完整（长度可读但 body+CRC 不足，或已完整帧数少于声明条数）/ CRC
不匹配（整帧完整但校验失败）。恢复 = 重放到第一个坏帧，视图恰等于完整前
缀的全量结果。崩溃点（Apply 中途、Recompute 中途、Commit 之前）均在日志
追加之前，重启重放后视图与未崩溃逐字段相同。

## 6. 包划分（6 个非测试 .go + 3 个测试文件 = 9）

`change` 编解码；`agg` 聚合器族与撤回声明；`journal` 追加/重放/分类；
`view` 分组状态与三阶段编排（含重算计数、拒绝计数）；`audit` 全量重算
逐组核对（Sum 用 `math.Float64bits`）；`cmd/demo` 验收演示。
