# 设计推导

## 1. 零计数 hunk 头的行号

GNU 规则：hunk 覆盖的旧行区间是半开区间 `[a, a+b)`，其中 `a` 是首行 1 基行号。
区间为空时 `a` 取「插入点前一行」的行号（无前一行则 0）。
- `x` → 在其前插入 `y`：插入点在第 1 行前，前一行行号 0，故 `-0,0 +1`。
- 在 `b`(第2行) 后、`c`(第3行) 前插入 `X`：插入点前一行是第 2 行，故 `-2,0 +3`。
- 删光唯一一行 `a`：新区间为空，插入点前无新行，故 `-1 +0,0`。

| 旧计数 | 书写 `-a,b` | Apply 的插入/锚点解释 |
|---|---|---|
| b=0 | `-a,0`（不省略 0） | 不删除；锚点在旧第 a 行与第 a+1 行之间（a=0 即文件头），在该处插入 |
| b=1 | `-a`（省略 `,1`） | 删除旧第 a 行，锚点为该行 |
| b≥2 | `-a,b` | 删除旧第 a..a+b-1 行，锚点为该区间 |

解析后插入点 = 该 hunk 上下文/删除行在原文中匹配区间的结尾；b=0 时锚点由前导（或尾随）上下文行确定。

## 2. hunk 合并阈值

阈值 = **2C**（相隔未改动行数 g ≤ 2C 合并，g ≥ 2C+1 分开）。
每个改动块左右各带 C 行上下文；两个块各自扩展 C 行后，中间剩余未改动行数为 g−2C。
取 C=1：若阈值为 C，则相邻改动隔 2 行上下文时 GNU 会把两个上下文行都保留并合并，C 会错误拆开；
若阈值为 2C+1，则相隔 3 行纯上下文时 GNU 输出两个独立 hunk（中间一行重复出现于两 hunk），2C+1 会错误合并。
故仅 2C 与 `diff -U C` 一致（g=2C 合并、g=2C+1 分开）。

## 3. 并列最短脚本的选择

Myers 的 V 迭代中固定取：可向下（删除）走时优先删除，否则向右（插入）；
即 `k == -d || (k != d && V[k-1] < V[k+1])` 时取 `V[k-1]`（删除优先）。
因此 `a b` → `b a` 先 `-a` 再 `+a`（删除行排在插入行前），同 D 的反向选择永不出现。
该选择贯穿 hunk 内行序：同一位置的改动统一呈 `-` 行块后接 `+` 行块。

## 4. 偏移后后续 hunk 的查找起点

要平移。hunk i 以偏移 `o` 应用后，记已应用块造成的净行数变化 `delta = 插入数-删除数`；
hunk i+1 的记录起点平移量为「此前所有已应用 hunk 的实际偏移与 delta 之和」中与位置相关的部分：
实现上每块用「匹配起点 − 记录旧起点」作为该块偏移，下一块期望起点 = 记录起点 + 前块 delta，
并在其 ±F 内搜索。因为前块插入/删除使后续行整体位移 delta，不补偿则同一改动会被误判为漂移。

## 其他约定

- 行：保留原行尾（`\n`/`\r\n`/无），空字节串切成 0 行。
- 无换行标记只可能出现在文件最后一行对应的 `-`/`+`/` ` 行之后。
- 计数器：每条 furthest-reaching D 迭代检查每个 k 记 1，snake 每比一对行记 1。
- 偏移候选多个时距离最小、并列取靠前；任一 hunk 失败则整体不生效。
