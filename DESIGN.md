# 设计推导

## 1. 非法单元切分规则（逐字节状态机）

规则：读首字节定长度 L 与第二字节区间 [lo,hi]。后续每来一个字节：
若不在 0x80..0xBF，当前前缀整体作为**一个**非法单元，该字节**退回**重新解析；
第二字节超出 [lo,hi] 同理（这就是 `E0 80`、`ED A0`、`F0 80`、`F4 90` 各成一单元）。
流结束时未凑满 L：已缓存前缀算**一个**单元（`E2 82`+EOF 只一个）。
故 `F0 90 80 41`：F0 要求区间 90..BF 且共 4 字节，80 是合法续字节，直到 41 非续字节，
整个 F0 90 80 成一个单元、41 退回；而 `E0 80 80`：E0 第二字节区间 A0..BF，80 立即判错，
前缀 E0 一个单元，80 退回→又一个、再 80→又一个，共三个。

| 首字节 | 长度 | 第二字节区间 | 非法时吞下 |
|---|---|---|---|
| C2..DF | 2 | 80..BF | 前缀（1 字节） |
| E0 | 3 | A0..BF | 前缀 |
| E1..EC | 3 | 80..BF | 前缀 |
| ED | 3 | 80..9F | 前缀 |
| EE..EF | 3 | 80..BF | 前缀 |
| F0 | 4 | 90..BF | 前缀 |
| F1..F3 | 4 | 80..BF | 前缀 |
| F4 | 4 | 80..8F | 前缀 |
| 80..BF | — | 无（孤立续字节） | 该 1 字节 |
| C0 C1 F5..FF | — | 无（永非法首字节） | 该 1 字节 |

UTF-16：高代理 D800..DBFF 后必须跟低代理 DC00..DFFF，否则高代理为一个非法单元，
下一单元（含非低代理的那个 code unit）退回重解析；孤立低代理自成一个非法单元。

## 2. 缓存上限与立即判错时机

UTF-8 最长 4 字节；缓存满 4 字节即出一个标量；首字节定长度后只需缓存「尚未凑满的前缀」，
最多 3 字节（首字节 + 至多 2 个续字节；见首字节即判错则为 0）。硬上限 = 3。
UTF-16 一个 code unit 2 字节、代理对最多缓存 1 个高代理 = 2 字节，统一上限 3。
立即判错（不必等 EOF）：首字节非法；第二字节越界；任何续字节不在 80..BF。
只有 EOF 时才需要把仍合法的前缀判为「截断」（严格模式 ErrTruncated，与 ErrIllegal 不同）。

## 3. par 切点对齐

切点任意。段 i 从切点 c 起向前回看至多 3 字节，判定 c 是否落在某单元内部：
在段 i-1 的末尾用有界扫描（≤3 字节）找「包含 c 的那个单元」的起点 a（找不到则 a=c）。
段 i-1 只负责 a 之前的字节，[a,c) 的尾巴随段 i 一起解析（段 i 起始偏移仍记为 a，
保证错误偏移是全局偏移）。这样任何输入字节恰被一个 worker 解析。
为何非法单元计数一致：每个单元（合法标量或非法单元）总以其首字节归属唯一 worker；
回看判定只依赖首字节与后续 1 字节（E0/ED/F0/F4 的特例在第二字节即可判定），≤3 足够。
例：段尾 `C3` 切点、下段头 `80 80`：`C3 80` 整体交给下段成一个标量，上段不产出；
若下段头是 `41 80`，则 C3 单独成非法单元归下段，41、80 各自再解析，计数与串行一致。
UTF-16：切点先对齐到偶数 code unit（奇数回退 1 字节），高代理跨切点时回看 2 字节带上它。
每 worker 以 midstream 模式构造（不识别 BOM），仅首个 worker 识别流首 BOM。

## 4. 上限断点

每个完整标量编码前检查 `len(out)+encLen > limit`，超限即返回 ErrLimit，已编码标量保留、
半标量绝不写出。返回的 n 只含「已交付并已产出标量的字节」；残留在解码缓存里的字节
不计入 n（等于未消费），调用方用 `Pending()` 取出它们拼到下次输入前。
故新实例 Write(append(pending, input[n:]...)) 后两段输出拼接与无限流逐字节相同。
ErrLimit 为终态错误，防止越过上限后继续写。
