# 设计推导

## 1. 空行与单列空值
空行（两个换行紧邻）取「一条只含一个未引号空字段的记录」：跳过会丢字节、破坏偏移与往返。
但**末尾换行**只是终止符，不产生额外记录：解析时把连续空记录挂起，下一个非空记录到来才补发；
`Close` 时丢弃挂起的空记录。规则：`a\n\n` => [a]（末尾空记录丢弃），`a\n\nb` => [a],[],[b]，
空串 => 无记录。单列表中记录唯一字段为空：内部空行写 `\n\n`（=空字段+换行）可往返；
但**最后**一条记录的空字段若写裸 `\n`，会被当成终止符而丢失该记录（`a\n\n` 只有 [a]），
故必须写 `""\n`（引号空字段），解析得到 Quoted=true 的空单元格，区别于裸空字段。
规范输入：CRLF 仅作行尾；引号仅在必要时使用；末尾单空字段用 `""`；以 `\n` 或 `\r\n` 结束。

## 2. par 切点
每段起点可能处于 5 种状态之一：FS（引号外字段首/逗号后）、B（引号外字段中）、QI（引号字段中）、
QS（引号中刚见引号）、CR（行尾 CR 待定）。FS 与 B 的转移规则可统一为 O（引号外，用 open 标记字段
是否已有内容；字段首遇 `"` 开引号合法，仅在 open 时遇 `"` 才错）；QS 与 QI 除「下一字节为 `"`」外
完全相同，单遍内并行推演两种解释。故每段只跑两遍：O 假设与 I（引号内）假设，每遍同一字节机处理
一次；O 机同时携带一个 QS 影子状态。按段从左到右：由上段真实结束状态选用本段对应变体（CR 结尾且
下段首字节 LF 时按行尾处理）。跨段单元格合并 Value、以最左段起点为起点；记录号/字段号/字节偏移 =
段基址 + 段内坐标（合成前缀字节偏移为负、计数剔除）。故事件流与单线程逐字节相同，错误坐标同法换算。

## 3. CR 待定
引号外见 `\r` 不立即判错也不立即结行，进入 CR 状态：续传时下一字节是 `\n` 则结行，其他字节则在
该 `\r` 位置报孤立 CR（ErrBareCR）；`Close` 停在 CR 状态同样报 ErrBareCR。引号内 `\r` 是内容；
引号闭合后处于 CR 状态（`"x"\r`）按引号外规则处理。par 切点落在 `\r` 与 `\n` 间：上段结束为 CR，
下段以 I 变体见 LF 仍按 CR-待定规则结行（引号内裸 LF 永远是内容，仅 CR 情形需区分，
故 I 机保留 CR 待定影子即可）。

## 4. 上限「立刻」
字段字节计数器在每个**逻辑字符**落进字段值时递增：引号外每字节 +1；引号内普通字节 +1，
`""` 转义在见到第二个 `"` 时计 1 个字符（第一个 `"` 只暂存于 QS 状态、不计）。计数器在字段
结束时清零。因此第 (max+1) 个字节到达的当次 `Feed` 即返回 ErrFieldTooLong，不缓冲整条记录。
字段数在发单元格时（含续传首块）即检查 ErrTooManyFields；记录数在发记录时检查 ErrTooManyRecords。
错误为终态：记录首个错误，后续 Feed/Close 返回同一错误。

## 5. 状态转移表
状态：OUT（引号外，open=字段是否已有内容）、QI（引号内）、QS（引号内刚见引号）、CR（CR 待定）。
动作：emit 发单元格、rec 发记录、sink 吸收进字段值。

| 状态 | `,` | `"` | `\n` | `\r` | 其他 |
|---|---|---|---|---|---|
| OUT(open=false) | emit | ->QI | emit+rec | ->CR | sink |
| OUT(open=true) | emit | ErrBareQuote | emit+rec | ->CR | sink |
| QI | sink | ->QS | sink | sink | sink |
| QS | emit | sink(`"`)->QI | emit+rec | ->CR | ErrQuoteJunk |
| CR | ErrBareCR 后按 OUT | 同左 | emit+rec | ErrBareCR 后 sink | 同左 |

CR 遇任意非 LF：先在该 `\r` 偏移报 ErrBareCR 后按普通字节处理；QI 遇 EOF 报 ErrUnclosedQuote
（位置为字段起点）。列数不一致由 table 在记录边界报 ErrColumnCount。单流式实例非并发安全；
par 内部并发安全。
