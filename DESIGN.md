# JSON 字符串严格编解码器 — 设计推导

## 代理对错误偏移（语义 2）

规则：
- 孤立低代理（`"\uDE00"`、`"\uDE00\uD83D"` 中的第一个单元）：报在**它自己的 `\` 处**。
- 高代理配对失败（后面不是 `\u`、不是低代理、或输入结束）：报在**高代理的 `\` 处**。

推导：
1. 读到高代理时，解码器已承诺一个 12 字节的复合 token（`\uXXXX\uXXXX`）；无法完成
   配对的责任单元是高代理本身。若指向后续字节，同一错误会因后续内容不同而得到
   不同偏移，规则不稳定、不可预测；指向高代理起点则与前瞻内容无关，唯一确定。
2. 低代理永远不能合法地开始一个 16 位单元，所以孤立/颠倒时它就是首个非法单元，
   报在它自己的起点。
3. `"\uD83DA"` 不能解释成「U+FFFD + `A`」：那样 `"\uD83DA"` 与 `"\uFFFDA"` 解码
   结果相同，Decode 不再单射，`Encode(Decode(y)) != y`，往返性质（语义 5）被破坏；
   且静默改写数据违反严格性。RFC 8259 §8.2 对孤立代理的行为未作规定，严格解码器
   唯一自洽的选择是拒绝。

## 最小转义与唯一性（语义 4）

RFC 8259 语法：`unescaped = %x20-21 / %x23-5B / %x5D-10FFFF`。
- 必须转义的只有 `"`(0x22)、`\`(0x5C)（结构性字符）和 U+0000–U+001F（不在
  unescaped 范围内，原样出现即非法）。
- U+007F 落在 `%x5D-10FFFF` 内，可原样出现，故最小转义不动它；U+001F 不在
  unescaped 内，故必须转义。`/`、非 ASCII、U+2028/U+2029 同理一律原样。
- 唯一性：每个源字符恰有一条确定性、上下文无关的编码规则（原样 / 短转义 /
  `\u00XX`），所以 Encode 是函数，输出唯一。
- 单射：不同字符映射到不同 token；短形式（`\b\f\n\r\t`）与 `\u00XX` 互不重叠，
  token 边界无歧义，故不存在两个不同输入产生同一输出。

## 测试对照表

（待补齐）
