# DESIGN
流格式（多字节整数一律无符号 LEB128 varuint）：

| 记录 | 字节布局 |
| --- | --- |
| 流头 HEAD | 魔数 `LZ7`(3B) + 版本(1B=1)，定长 4 字节 |
| 字面量 LIT | 标记 0x00 + 长度 n(varuint) + n 个原文字节，n>=1 |
| 回指 REF | 标记 0x01 + 距离 d(varuint) + 长度 l(varuint)，d>=1,l>=3 |
| 刷新 FLUSH | 标记 0x02，无字段 |
| 流尾 TAIL | 标记 0x03 + 原文总长(varuint) + CRC32-IEEE(varuint) |

校验和按原文字节顺序计算，压缩边吃边算、解压边重建边算。

## 1. 为何压缩结果与 Write 切法无关（语义 3、4）
贪心 LZ 中匹配是否命中只看历史窗口，候选链与切法无关；唯一歧义是当前匹配还会延长多少。
压缩器内部维护已决历史窗口与 pending 缓冲，守不变量：缓冲内除最后一字节外均可立即判决；
但若某匹配从其起点一路吃满到缓冲末端（start+matchLen == 缓冲总长），则不发任何记录，停在
该起点等待下一字节——此刻无法区分匹配真结束还是被 Write 边界截断。下次 Write 后从同一起点
重跑：延长则得到更长匹配，断开则上一长度即终值。判决只依赖「历史 + 一段连续输入」，与该段
由几次 Write 拼出无关，故输出逐字节确定。Flush 强制终止待定：把 pending 全部判决（末端匹配
按当前长度发出）再发 FLUSH，此后记录恰好覆盖全部已写输入；历史不清除，故之后匹配仍可回指
Flush 之前数据。压缩器记录自上次 Flush 后是否发出过东西；无新输入的第二次 Flush 零字节。

## 2. 重叠回指的复制（语义 2）
d<l 时源区与目的区重叠，一次性块复制会读到尚未写入的新缓冲或重复旧值，均错。正确语义是
逐字节向前复制 out[t]=out[t-d]、t 递增，新写字节可被后续复制读到，从而展开 abcabc…。实现
直接逐字节从环形窗口取：每追加一个字节，下次 At(d-1) 可能读到的正是本次刚追加的字节。
4MB 最坏约 4M 次单字节读写，成本低于压缩侧哈希比较，故逐字节循环即可，不做 run 特判。

## 3. 两种「距离过大」为何分开（语义 7、9）
d>已输出：引用从未存在过的位置，窗口再大也非法；已输出>=d>窗口容量：数据存在过但已滑出
固定窗口，按本格式不可引用。压缩器只把窗口内位置插入哈希链，故顺序压缩绝不可能产生 d>W。
两类由不同哨兵报告并附字节偏移。并行预置字典只改窗口初值、不改「已输出」：解压是单一流，
d 始终针对解压器真实已重建字节数判定；块内引用字典字节时这些字节已在更早块输出，仍合法，
流中无需声明字典。

## 4. 并行块压缩的确定性与衔接（语义 9）
切块按偏移固定；第 k 块压缩器的唯一输入是「本块原文 + 上一块末尾 W 字节预置字典」，
窗口/链仅由这段数据构造，与更早块、其他 goroutine、完成顺序无关，故 workers=1/2/4/8 各块
记录逐字节相同。衔接：各块 LIT/REF 记录顺序拼接，块间不发 FLUSH（FLUSH 仅表达用户边界）、
不发 TAIL；最后由编排者对全部原文统一发一次 TAIL（总长与校验和跨全部原文）。预置字典参与
块内绝对位置编号以保证距离换算正确，但既不输出也不计入该块记录覆盖字节数。
