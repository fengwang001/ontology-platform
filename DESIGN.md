# 带转义的 RLE 编解码器设计

## 严格解码的拒绝清单（由 Encode(Decode(t)) == t 逐条推导）

不变量：解码器接受的每个 t 必须等于其解码结果的再编码，否则合法编码不唯一。
以下每类若被接受，括号内给出违反不变量的具体 t：

1. 显式次数 1：`1a` 解码为 "a"，再编码为 `a` ≠ `1a`。
2. 次数 0：`0a` 解码为 ""，再编码为空串 ≠ `0a`。
3. 前导零：`01a` 解码为 "a"，再编码为 `a` ≠ `01a`。
4. 相邻同符号：`2a3a` 解码为 "aaaaa"，再编码为 `5a` ≠ `2a3a`。
5. `\` 后非数字非反斜杠：`\a` 若解码为 "a" 则再编码为 `a`，若解码为 "\a"
   则再编码为 `\\a`，两种解释都 ≠ `\a`，故无可接受的解码。
6. 末尾孤立 `\`：`a\` 解码为 "a" 或 "a\"，再编码为 `a` 或 `a\\`，均不等。
7. 末尾只有次数：`12` 没有符号，任何解码结果的再编码都不含结尾的 `12`。
8. 非法 UTF-8：码点无法确定，而 Encode 按码点切分，往返无定义。

每类对应一个彼此可区分的哨兵错误（ErrExplicitOne、ErrZeroCount、
ErrLeadingZero、ErrAdjacentSame、ErrBadEscape、ErrTrailingEscape、
ErrMissingSymbol、ErrInvalidUTF8），统一包进 *Error 并带字节偏移：
计数值类定位于游程起点，转义类定位于反斜杠，UTF-8 类定位于出错字节，
相邻同符号定位于后一游程的起点，次数缺符号定位于输入末尾。

## 大次数（第 3 条）的取舍

支持任意大的次数文本，不在解析层设次数上限：runs.Counter 以 limit+1 为
上限做饱和累加，绝不回绕溢出。解码输出总字节数设可配置上限（默认 1 GiB，
SetLimit 可改）；游程次数 × 符号宽度超出剩余额度时在输出前返回 ErrTooLarge，
内存占用只与已输出字节成正比。`99999999999999999999a` 得到 ErrTooLarge，
既不溢出静默出错，也不按次数预分配内存。

## 语义 ↔ 测试对照

（待测试写完后补充）
