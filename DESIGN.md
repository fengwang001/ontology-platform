# 事件流增量解析器设计推导

## 1. 块边界落在 `\r` 与 `\n` 之间

行切分状态机只有两个状态：`inLine`（正在累积一行）与 `afterCR`（上一个字节是 `\r`，行已产出，但还不确定它是不是 `\r\n` 的左半边）。

状态迁移（输入为单个字节 b）：

- `inLine` + `\r` → 产出当前行为完整行，进入 `afterCR`。
- `inLine` + `\n` → 产出当前行，留在 `inLine`。
- `inLine` + 其他 → 追加到行缓冲，留在 `inLine`。
- `afterCR` + `\n` → 丢弃该 `\n`（它是上一个 `\r` 的右半边），回到 `inLine`。
- `afterCR` + 其他 → 该字节属于新行，回到 `inLine` 并正常处理。

关键：`afterCR` 状态下**不产出任何行**，只做"吞掉一个 `\n`"的判定。因此 `\r\n` 无论落在同一块还是跨两块，都只产生一次行结束，绝不会被当成两个空行。流在 `afterCR` 状态结束时，挂起的 `\r` 已经是完整行尾，无需补发。

## 2. 切分点无关性

跨块必须保留的状态（不变量）：行缓冲 `buf`、状态位 `afterCR`、BOM 待决标志（流首三个字节未凑齐前不消费）、以及 stream 层的事件缓冲与 lastEventID/retry。

论证：状态机的输出只依赖「当前状态 + 下一个字节」，不依赖字节来自哪一次 `Write` 调用。设字节串 S 的两种切分产生调用序列 c1..cm 与 d1..dn，其拼接均为 S。对字节位置归纳：处理第 k 个字节前的状态由前 k-1 个字节唯一决定，与切块无关，故第 k 个字节的产出也相同。因此对任意切分点，产出的行序列相同；stream 层只消费行序列，故派发的事件序列也相同。BOM 判定同理：只在「尚未消费任何字节」的前缀上做，凑齐 3 字节前不产出，切分不改变字节序。

## 3. 为什么只有注释的块不能派发

派发条件是「数据缓冲非空或设置过 event 名」。注释行（`:` 开头）按规范被忽略，不进入任何缓冲；未知字段同理。若空行无条件派发，下游会在每个注释块后收到一个 data 为空、无事件名的"幽灵事件"，把保活注释误当成业务消息，触发空负载的处理逻辑，破坏「注释仅作保活」的契约。因此空行只是"尝试派发"的信号，是否真派发由缓冲内容决定。

## 4. 超限时为什么必须状态不变

上限检查在「追加之前」做：新行会使行缓冲超限、或新 data 会使事件数据缓冲超限时，直接返回错误并丢弃该输入，缓冲保持原值。若改成「截断后继续」，则同一字节串在不同切分点下，超限触发点前后的字节归属会不同：截断位置取决于块边界，后续字节被当作新内容继续聚合，导致第 2 条「任意切分点产出相同事件序列」不再成立——错误路径引入了切分点相关的分叉。拒绝且不变更状态，则错误发生前的状态是唯一确定的，调用方重喂或放弃都有确定语义；同时两类错误（行超限 / 数据超限）用可判定的哨兵错误区分，便于调用方分别处理。
