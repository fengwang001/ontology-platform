# DESIGN — 自定义 LZ77 流式压缩

## 流格式（wire 包）

所有多字节整数均为 Uvarint（≤10 字节，第 10 字节只能贡献 1 bit）。

| 记录 | 字节布局 |
| ---- | -------- |
| 流头 | `4C 5A 37 37`（"LZ77" 魔数）+ 1 字节版本 `01` |
| 字面量段 | `01` + uvarint N + N 个原始字节 |
| 回指 | `02` + uvarint 距离 D + uvarint 长度 L |
| 刷新标记 | `03`（无负载） |
| 流尾 | `04` + uvarint 原文总长 + uvarint FNV-1a32 校验和 |

## 推导 1：与 Write 切法无关（第 3 条）

压缩方在 `Write` 边界上无法判断当前匹配是否结束：边界后未到
的字节可能让匹配继续延长，也可能让一段字面量继续累积。任何
"看到就发"的策略都会把切法泄漏进输出。因此本设计让 `Write`
**只缓冲、不编码**：未决字节 = 自上次 `Flush` 以来的全部输入。
只有在 `Flush`/`Close` 时，缓冲区末尾才成为"确定不会再有后续
字节"的边界，此时对整段缓冲跑一次 LZ77：匹配可以延伸到缓冲
末尾，字面量段也在末尾才截断。因为编码决策只依赖字节流本身
与 Flush 偏移，而不依赖 Write 的切法，输出与切法无关。推论：
`Flush` 必须强制结束当前匹配与字面量段（缓冲末尾之后没有可
供延伸的字节），否则解压方拿不到"截至 Flush 的全部输入"。
代价是内存占用为两次 Flush 之间的输入量，由调用方用 Flush
控制；这是用内存换确定性的刻意取舍。

## 推导 2：重叠回指的复制语义（第 2 条）

距离 D < 长度 L 时，源区间与目标区间重叠：逐字节向前复制时，
第 k 个输出字节可能正是第 k-D 个输出字节（刚写出的）。一次性
`copy(dst, src)` 从旧快照拷贝，会把"应当是新写出的字节"错拷成
旧窗口内容（如 `abc`+回指(3,30)：块复制得到 `abcabc` 后剩余全错）。
本实现按"读窗口 → 追加 → 新字节立即可读"逐字节复制，天然正确。
可接受性：回指复制每字节 O(1)（环形缓冲一次取模寻址），总代价
O(输出量)，与任何解压器同阶；优化成按 D 周期块拷只是常数优化，
不值得为此引入正确性风险，故保留逐字节循环。

## 推导 3：两种距离错误为何区分（第 7 条）

"距离 > 窗口容量"是**编码器不变量**被破坏：匹配器只在窗口内搜
索，产生的距离恒 ≤ 窗口容量，出现即说明流被篡改或解码器配置
的窗口小于编码器。"距离 > 已输出字节数"是**流内逻辑**错误：
距离在窗口容量内、但历史还没那么长（如流首就回指）。二者诱
因不同（配置/篡改 vs 流内容错误），必须可区分。并行块压缩把
上一块末尾作为预置字典，但各块记录直接拼接进同一条流，解码
器看到的是连续输出，"已输出字节数"含义不变（= 全部已还原字
节），字典只是让编码器能引用这些字节，不引入新的距离语义。

## 推导 4：并行块压缩的确定性（第 9 条）

块 i 的编码输入只有：本块原文 + 上一块末尾 min(窗口容量, 块长)
字节（预置字典）。字典来自**原文**而非其他块的压缩结果，故每
块的输出与压缩顺序、worker 数、其他块是否完成无关；worker 只
改变调度，不改变任何块的输入，结果对 workers=1/2/4/8 逐字节
相同。衔接：回指距离在解码器看来就是"距当前输出末尾的字节数"，
与块边界无关，因此块间**不需要**刷新标记，记录直接首尾相接，
最后统一追加一个覆盖全文的流尾。空输入也照常产出流头+流尾。
