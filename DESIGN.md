# DESIGN

## 流记录布局

魔数为 `ONLZ`，版本为 1；窗口容量与所有整数均为无符号 LEB128。

| 记录 | 字节布局 |
|---|---|
| 流头 | `ONLZ`、版本 1、窗口容量 |
| 字面量 | tag=0、长度、连续原文 |
| 回指 | tag=1、距离、长度 |
| 刷新 | tag=2；无后续字段 |
| 流尾 | tag=3、原文总长、IEEE CRC-32 |

tag 与下一个 varint 合并为 `tag<<62|value`；字面量长度 0 时不另编码长度。

## Write 无关性

匹配器只对“已确认结束”的位置提交决策。每次处理都先保留当前后缀：可回指至少 3 字节，但新输入末尾的位置可能继续向右延长。
普通提交只处理到“除最后一个字节外”的位置：若该位置无匹配，就发出这个字面量并只推进 1；若匹配达到可读末尾，则把起点到匹配末尾全部保留，等待下一个 `Write` 或 `Flush`。
因此任何切法都会得到同一条待确认后缀；不会因边界提前截短匹配，也不会提前发出其后的字面量。
`Flush` 强制解释全部保留后缀：达到末尾的匹配按当前长度发出，无法匹配的尾字节作字面量；随后发刷新标记。没有新输入时没有后缀、也没有标记，故连续 Flush 零字节。

## 重叠回指

块复制会先从源区读出旧值；当 `distance<length` 时，目标尾部本应读到本次复制刚产生的字节，块复制却可能读到旧值或未定义数据。
解压维护固定容量环形窗口，回指按相对距离定位，然后循环 `length` 次：读取当前字节、输出、再写入窗口。循环中的窗口位置每步前进，所以距离 1/2/3 且长度很大时自然形成重复周期。
这里逐字节循环是可接受的：解压总工作与输出规模线性；窗口容量给内存上界，输出上限在读取 length 后、写任何字节前检查。

## 距离错误与并行字典

“超过已输出字节数”说明引用了尚不存在的历史；“超过窗口容量”说明引用存在但当前解码器状态不可能保留。二者成因不同，故先查容量再查已输出数量并返回不同哨兵错误。
合法压缩器只插入仍在环形窗口内的位置；查找候选时窗口也会过滤陈旧候选，所以不可能产生超过窗口容量的距离。
并行块使用上一块末尾不超过窗口容量的原文作为预置字典。这些字节虽不属于本块，但已经在前一块流记录中输出；对整个流的解压器而言，它们就在正常“已输出历史”里，语义没有改变。

## 并行确定性

第 `i` 块的匹配器只接收本块原文与第 `i-1` 块尾部原文；哈希链由该预置文本初始化，输出只取决于这两个字节串和固定配置，不读取共享全局状态、也不等待其他块完成。
worker 只返回本块的 body 记录（不含全局头/尾）。主 goroutine 按块序号拼接，块间不插刷新标记；前块输出的最后记录不会改变后块的字节编码，后块的回指由解压器自然落到前块历史。
因结果只按序号拼接，1/2/4/8 个 worker 的输出逐字节相同。
