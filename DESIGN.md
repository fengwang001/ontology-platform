# DESIGN：自定格式 LZ77 流式压缩

## 流格式（全部字段为无符号 LEB128 变长整数）

| 记录 | 字节布局 |
| --- | --- |
| 流头 | 魔数 `'O' 'N' 'T' 'L' 'Z'` + 版本`=1` + 窗口容量 |
| 字面量段 | 标签`0` + 长度 n + n 个原始字节 |
| 回指 | 标签`1` + 距离 dist(≥1) + 长度 len(≥3) |
| 刷新标记 | 标签`2`（无负载） |
| 流尾 | 标签`3` + 原文总长 + CRC64(ISO) |

记录只允许标签 0–3；CRC64 多项式为 ECMA 反射式 `0xC96C5795D7870F42`，初始/异或出均为 0。

## 推导一：压缩结果为何与 Write 切法无关

结论要求：匹配可以跨过任意 `Write` 边界，而边界处看不到未来字节。反证可知，若在 `Write` 中立即下决策，
单字节喂入时最长匹配永远只有 0（后面还没来），整段喂入却能得到长匹配，二者必然不同。任何固定
“延迟 k 字节”也不成立：全同字节串的匹配可任意长。要同时满足“不多输出”与“跨边界”，唯一稳妥做法是
在两次 `Flush` 之间**缓冲全部未决输入**，只在 `Flush`/`Close` 时对该缓冲整体跑同一个确定性编码器。
于是编码器看到的字节序列只等于“累计输入”，与切成几段无关；`Flush` 位置只决定在哪段缓冲之后插入
刷新标记，偏移相同则输出相同。代价：`Flush` 之间的数据在内存中（题面允许状态在进程内存）。
`Flush` 必须强制结束当前匹配：它要让已产出的压缩字节足以还原全部已交输入，不能把字节留成未决，
因此 `Flush` 压缩整块 pending、追加标签 2；若两次 Flush 间无新输入，编码器跑空缓冲、输出为空，
天然产生零字节。

## 推导二：重叠回指的复制

块复制 `out[len:len+l]=out[len-d:len-d+l]` 读到的是复制开始前的旧切片；当 dist<len 时，越过 dist
后读到的仍是旧数据而不是“刚写出的副本”，例如 dist=1 时整块都变成同一个旧字节，正确结果应为周期
重复。语义必须是“取第 dist 个最近的已输出字节，写出后它也进入历史”，即逐字节向前：
`out=append(out,out[len-dist])` 每轮重新按新长度索引。我直接采用逐字节 append：解压本身就是
每字节一次的流式过程，没有额外渐近开销（每输出字节 O(1)），且 append 复用输出容量，不产生
每字节分配，因此不存在“为正确性牺牲一个量级”。

## 推导三：距离的两种越界为何要分开

解压器在处理回指时按顺序检查：dist==0 → 非法回指；dist>窗口容量 → 超窗；dist>已输出字节数
→ 超历史。前两者只依赖流头/本记录，后者依赖执行历史，故是两类不同错误。“超过窗口容量”指声明的
dist 超过编码器承诺的环形容量，压缩器只在哈希链中引用现存、且距离≤窗口容量的历史位置，因此正常流
绝不会触发它（损坏流才会）；“超过已输出”则是流本身在当前执行点引用了尚不存在的字节。并行预置
字典**没有改变“已输出字节数”的含义**：块 i 以块 i-1 原文为字典进行匹配，但这些字典字节就是前一块
已经顺序解压出的真实输出，解压器只有一个连续输出缓冲，回指依旧满足 dist≤已输出总量；预置字典
只是压缩端的搜索起点，不是流格式里的隐藏状态。

## 推导四：并行块压缩的确定性与衔接

`CompressParallel(data, blockSize, workers)` 按固定偏移切块。块 i 的哈希链用块 i-1 的原文播种
（只取其中落在窗口容量内的尾部），因此块 i 的压缩结果只依赖 `block[i]` 与 `block[i-1]` 两块原文，
不读取任何其他块的压缩结果；workers 数与完成顺序只影响调度，按固定块序拼接后输出一致。流格式不需要
特殊衔接：单流头写一次，块 i 内部的回指在解压顺序上都指向此前已输出字节（上一块或本块更早处），
块间也不必插入刷新标记（块边界天然是字面量/回指边界）；最后统一写一个含总长与 CRC 的流尾。
`workers=1` 与 >1 走同一编码函数，保证四种 workers 逐字节相同。

## 复杂度

`match.Finder` 暴露非导出计数 `probed`（经 `Probed()` 读取）。候选按“最近优先”入链，并在
`l==winSize`（能引用的最长距离）时立即停止考察更深候选——全同字节的最近候选必给出最大值，
更深者不可能更优。故每位置考察数有常数上界 ≤ 链长上限，与输入规模无关；总计 ≤ 链长×字节数。

## 并发性

单个压缩器/解压器实例不是并发安全的；`CompressParallel` 每块使用独立 Finder/缓冲，仅在按序收集时
通过 channel 汇合，无共享可变状态、无 sleep。
