# 设计推导

## 1. 行尾与空白
`eol.Cursor` 只在 `\r` 上停留为待定；下一字节是 `\n` 时二者合一，否则 `\r` 立即成单独行尾。故 `\r\r\n` 第一个 `\r` 见到第二个 `\r` 即落定，第二个 `\r` 与 `\n` 合成第二个行尾 → `\n\n`。`ws` 只缓冲 `[ \t]`；见到行尾则整串删除，见到其他字节则整串原样吐出，EOF 同“见到行尾”。

## 2. 空输入/只含换行 × 三策略
策略：`Keep`（末尾原样）、`One`（末尾恰好一个换行，必要时追加）、`Trim`（删全部末尾空行再保留一个换行）。

| 输入 | Keep | One | Trim |
|---|---|---|---|
| `""` | `""` | `"\n"` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

幂等：三策略输出均无 `\r`、无行尾空白；Keep 保留末尾原样，再规范化不变；One 使末尾恰好一个 `\n`，对 `"\n"` 再作用仍为自身，空输入必须取 `"\n"`（取 `""` 则 One 非幂等，且空与非空无换行不可区分）；Trim 形如“正文+单个 `\n`”，空文档为 `""`（给空文档追加 `\n` 会让第二次结果变成 `"\n"`，破坏幂等）。

## 3. 被删字节的 ToOut
采用左闭右开区间与“锚点”模型：删除区间 D=[a,b) 把其后第一个生成字节（换行符，输出偏移 o）作为锚点；无后续输出（EOF 删除）时锚点为输出终点。

- 原文 `i<a`：`ToOut(i)=i-已删`；`i∈[a,b]`：`ToOut(i)=o`（被删空白、`\r\n` 的 `\r` 都落在下一个输出 `\n`）。
- `ToOrig(o)=b`（`\n` 回到对应原文起点）；锚点前的输出位置回到 `a`。

编辑器语义：光标在被删空白里时映射到行尾 `\n` 之前（紧贴上一行内容），而非下一行。左逆恒等与双向单调随之成立。

## 4. 缓冲超限
选择**拒绝**：待定空白超过 `WSCap` 立即返回带原文偏移的可判定错误 `ErrWSCap`，已输出保留并进入终态。强制判定为“非行尾”会把真实行尾空白提前输出成正文，之后遇 `\n` 无法撤回，违反幂等（5）与跨切分一致（4）；拒绝不产生结果，不违反 4/5/6。

## 5. par 拼接
边界只可能是：一个待定 `\r`，或一串未判定空白（其后可再跟一个 `\r`）。对边界 p 向前最多回溯 `WSCap+1`：跳过结尾连续 `[ \t]`，其前一字节若为 `\r` 再回退 1；该点前必为已定正文或行尾，之后每字节都能无前文重放。各段输入改为相邻重同步点之间，独立规范化后按序拼接；区间端点统一加“本段原文起点/已产生输出长度”平移。修正范围上界 `WSCap+1`，故有界。末段才施加末尾策略与 EOF 删空白；非末段用 Keep 且不 EOF。

## 6. 映射存储与复杂度
只存删除区间 `{origStart, origEnd, outAnchor}`（相邻的 Close 时合并），纯 `\n` 文本零区间。查询手写二分，比较次数计入非导出计数器：`ToOut` 二分累计删除量，`ToOrig` 二分锚点。单次 ≤ ⌈log₂(n+1)⌉+1 ≤ 2⌈log₂n⌉+4。
