# DESIGN：自定义 LZ77 流

## 流格式（全部小端 uvarint；记录=tag 0..2 + 载荷；tag 3 为 FLUSH）

| 记录 | 布局 | 说明 |
|---|---|---|
| 流头 | magic "LZ7" + ver=1(各1字节) | 魔数/版本错立刻拒绝 |
| 字面量 | tag=0, len(uvarint), len 字节原文 | len≥1；空段不允许 |
| 回指 | tag=1, dist(uvarint), length(uvarint) | dist≥1，length≥1 |
| 刷新 | tag=3（单字节） | 无新输入的重复 Flush 不发 |
| 流尾 | tag=2, origLen(uvarint), checksum(uvarint) | 流中唯一 tag=2，之后不得有字节 |

校验和：64 位多项式滚动哈希 h = h*P + b，P=0x100000001b3。可按块合并：
h(XY)=h(X)*P^|Y|+h(Y)，使并行块只需返回 (长度, 块哈希)。

## 推导1：为什么压缩结果与 Write 切法无关（语义3/4）

贪心 LZ 在位置 p 的动作只取决于「p 之前全部已确认字节」与「p 及之后的未来字节」，
其中未来只影响匹配长度，不影响「匹配是否存在」。Write 在任意位置切断未来，因此
压缩器不能在 Write 边界做决定。做法：维护逻辑缓冲区 = 已确认窗口 + pending 未决尾。
frontier 是第一个尚未决定的绝对位置：
1. frontier 处前瞻不足 3 字节 → 无法下结论，等更多 Write；
2. 调 Find：无匹配（或长度<3）→ 确认 frontier 为字面量，frontier++，该字节入窗口；
3. 命中匹配 → 进入「开放匹配」：dist 固定（Find 那一刻决定），每来新字节只比较
   (frontier+curLen) 这一个字节，相等则延长，不等才确认该回指、把整段拷入窗口。
dist 与匹配起点在第一次命中时确定，未来字节只会延长 length；无论字节是一次 Write
到达还是分 7 次到达，比较序列逐字节相同，故输出（含字面量段边界，见下）逐字节相同。
字面量同样缓到「下一条回指 / Flush / 流尾」才整体发出：段内只追加字面量，何时
Flush 只决定段的发出时刻而不改变其内容。Flush 的语义就是「把无法再等的未来强制
揭晓」：立即结束开放匹配、发出挂起字面量并写 FLUSH 标记；所以 Flush 后解压器能
还原截至此刻全部输入。无新输入时没有任何可确认内容，第二次 Flush 发 0 字节。

## 推导2：重叠回指的解压（语义2）

回指要求把 out 尾部之前 dist 的 length 个字节复制到 out 尾部。块复制
copy(dst, src[0:length]) 当 length>dist 时，目标尚未产生或被旧值填充。
正确语义是「每复制一个字节，它立刻成为后续复制的来源」：dst==src+dist（dist>0），
Go 内置 copy 对重叠切片只保证 memmove 语义（dst<src 时会错），故按 dist 为步长：
每次 copy(out[cur:], out[cur-dist:cur])（源是已存在前缀，最多 dist 字节），循环
ceil(length/dist) 次。每次至多复制窗口容量大小，常数为窗口/dist，dist=1 最坏退化为
逐字节——但这恰是题面允许的情形（距离1重复字节），且每次 write 本就按字节产生，
可接受，不额外引入复杂度。

## 推导3：两种「距离越界」为何分开（语义7）

- dist>已输出字节数：引用从未存在过的内容，任何流都无法解释，是流内部矛盾；
  解码器此刻输出长度 n 就是权威。
- dist≤已输出 但 >窗口容量：引用的是「真实存在过、但已滑出固定窗口」的历史。
  只要 enc/dec 窗口配置一致，压缩器永不会发出（Append 按容量淘汰，Find 候选只取
  存活位置）。独立报错能区分「窗口容量配置不匹配」与「流损坏」，故必须分两类。
并行预置字典没有改变含义：字典在解码该块第一字节之前就逐字节放进窗口与 n 计数，
回指时它就是「已输出」的一部分（origLen 上限含预置字节，解码主输入时上限减去
字典长度）。校验和只对真实原文算，不含字典。

## 推导4：并行块压缩的确定性（语义9）

块 i 是主输入 [i*B,(i+1)*B) 的压缩。其匹配器开始时窗口预置块 i-1 原文末尾至多 W
字节，哈希链只由「预置字典 + 本块已确认前缀」构成。因此块 i 的压缩是纯函数
f(本块原文, 上一块原文)，与其他块、goroutine 完成顺序无关；workers=1 走同一 f，
1/2/4/8 输出逐字节相同。块间衔接不需要 FLUSH：固定 B 使块边界天然已知，每块末尾
强制 Drain（等同 Flush 的决策：未来结束，确认全部开放匹配），后一块直接在预置
字典上继续发记录，解码器看到一条连续合法流。最后由编排者发一条流尾：origLen 为
总长；checksum 用第一节合并公式从各块哈希合成。

## 复杂度

哈希链每位置插入 O(1)，Find 沿链最多考察 chainMax 个候选，「考察位置数」≤
chainMax/位置。全同字节最坏也是 chainMax × 输入位置数，不随窗口规模增长。
