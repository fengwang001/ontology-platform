# 设计推导

## 关键推导

1. **切法无关**：压缩器把未形成稳定块的输入连续放入字节缓冲，而不在 `Write` 处发字面量。最长匹配只由“当前位置起的完整未决缓冲”和窗口中的确定历史决定；单次扫描消费到某个位置后，该位置只能被更后的位置作为历史引用，因此结果只依赖累计字节流。`Flush`/`Close` 把当前缓冲压成确定记录并清空待决状态；普通 `Flush` 追加刷新记录，随后新块仍可引用未滑出的历史。无新输入的第二次 `Flush` 没有待决状态，故零输出。
2. **重叠复制**：块复制在距离 `d<长度 n` 时会复制尚未前移的目标旧值，不能生成周期重复。解压器维护历史环，每次从环读出第 `d` 个字节、立即写回该字节，输出位置前移后该字节又成为新历史。循环每次只做一次定长索引读写，虽不是整块 `copy`，但没有回退、额外分配或每字节函数开销；距离不小于长度时仍走同一简单路径，边界最少且无特殊错误。
3. **两类距离错误**：距离超过窗口容量说明编码与本流声明的配置矛盾，压缩器永远不能发出：候选只来自保留最近 `cap` 字节的匹配器。距离不超过窗口但超过已输出，可能出现在窗口预置或流位置语义错误时，所以必须单独识别。并行块的预置字典不是压缩输出；解码顺序仍逐字节输出，“已输出”不把字典虚拟计入。能跨块引用，是因为上一块输出仍在解码器环中。
4. **并行确定性**：第零块只依赖自身；第 k 块的匹配器只预置第 k-1 块末尾至多窗口容量字节，再扫描本块。哈希链、候选上限和并列选择均为确定性规则，不依赖 goroutine 完成顺序或其他块的压缩结果。worker 只返回按块号排序的载荷与 CRC；流仍为一个全局头、若干普通记录、一个全局尾，不需要块刷新标记。

## 记录布局

| 记录 | 标记 | 后续字段 | 说明 |
| --- | --- | --- | --- |
| 流头 | `0x4C 0x5A 0x37 0x01` | varint 窗口、varint 链长 | 仅出现一次 |
| 字面量 | tag=0 | varint n，n 字节原文 | n≥1 |
| 回指 | tag=1 | varint distance，varint length | distance≥1，length≥3 |
| 刷新 | tag=2 | 无 | 块边界 |
| 流尾 | tag=3 | varint 原长，varint CRC-32(IEEE) | 最后记录，其后必须 EOF |

所有整数使用无符号 LEB128。读取器在 tag/varint/载荷任一字节缺失时返回截断；超过 10 字节或第 10 字节携带超出 64 位的位返回整数错误。错误携带自流起始的绝对偏移。
