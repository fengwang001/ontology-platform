# 严格模式流式 Base64 设计说明

## 规范形式的推导（语义 2）

一个 4 字符组承载 24 bit。末尾组带填充时，真实数据不足 24 bit：

- `XX==`：1 字节 = 8 bit，占用 2 个字符 = 12 bit，剩余 4 bit 无数据。
- `XXX=`：2 字节 = 16 bit，占用 3 个字符 = 18 bit，剩余 2 bit 无数据。

编码器（本实现与 RFC 4648 一致）总是把这些剩余比特写 0。由此推出
统一判定规则：**剩余比特必须全为 0**，即

- `XX==`：第 2 个字符的 6 bit 值的低 4 bit 必须为 0；
- `XXX=`：第 3 个字符的 6 bit 值的低 2 bit 必须为 0。

`QR==`：R = 17 = 0b010001，宽松解码仍得 0x41='A'，但低 4 bit =
0001 ≠ 0，说明它**不是任何字节串的编码结果**，严格模式必须拒绝。

与「编码唯一」的关系：若容忍非零剩余比特，`QQ==` 与 `QR==` 等
16 个（`XXX=` 情形为 4 个）不同文本会解码到同一字节串，
Encode(Decode(y)) ≠ y，编码不再是字节串到文本的单射。要求剩余
比特为 0 后，每个字节串恰好对应唯一合法文本，Decode(Encode(x))
= x 与 Encode(Decode(y)) = y（对合法 y）同时成立。

## 实现要点

- `b64`：字母表、单组编解码与合法性判定；`=` 只允许作组后缀且
  至多 2 个；剩余比特检查如上。
- `stream`：单遍状态机，每个输入字节恰好检查一次（非导出计数器
  `checks`）；`\r` 用 1 bit 状态挂起，使 `\r`/`\n` 可跨切分点；
  错误偏移为整个输入流中的绝对字节位置。
- 上限：组解码后若超限则不写入输出，报 `ErrTooLong` 并进入终态，
  已输出的完整组保留。

## 测试对照表

（待测试写完后补齐）
