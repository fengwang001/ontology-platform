# 设计推导

## 1. 输出模型
流被逐字节判定为三类原子 run（span.Run{Orig,Out}）：
- identity：Orig=Out=1，普通字节原样通过；
- delete：Orig>0,Out=0，被删内容（行尾空白或 `\r`）；
- insert：Orig=0,Out=1，末尾策略补的 `\n`。
映射按 run 累计，`\n` 永远是 identity（`\r\n` 拆成 delete 的 `\r` + identity 的 `\n`）。

## 2. 样例 × 策略
| 输入 | Keep | EnsureOne | TrimBlank |
|---|---|---|---|
| `""` | `""` | `""` | `""` |
| `"\n"` | `"\n"` | `"\n"` | `"\n"` |
| `"\n\n"` | `"\n\n"` | `"\n"` | `"\n"` |
| `"  \r\n"` | `"\n"` | `"\n"` | `"\n"` |

## 3. 空输入与幂等推导
EnsureOne 对空输入可选 `"\n"` 或 `""`：选 `"\n"` 则 N(空)=`"\n"`，但 N(`"\n"`)=`"\n"`，幂等成立；问题在于“无内容”被凭空造出一行，且截断空串与含换行串不可区分。
选 `""`：定义为“原文至少出现过一个行尾时，才保证末尾恰好一个 `\n`；无内容则保持空”。于是 N(空)=空，N(`"\n"`)=`"\n"`，N(`"\n\n"`)=`"\n"`，全部幂等。TrimBlank 同此定义，并额外在有内容时删尽尾部空行：`"\n\n"`→`"\n"`，`"a\n\n"`→`"a\n"`。Keep 原样（只做 EOL/空白规范化）。三者输出均不含 `\r` 与行尾空白，故 N(N(x))=N(x)。

## 4. 被删字节的 ToOut 推导
设 delete run 覆盖原文 [s,e)，其前一 identity/insert run 之后的输出位置为 p（即换行符 `\n` 的输出位置）。左闭右开且单调不减要求：
ToOut(s)=…=ToOut(e-1)=p，ToOut(e)=p+1（若 e 处是换行）。
依据 ToOut(ToOrig(o))=o：输出点 p（`\n`）必须映回原文 e，所以 p 不能属于被删区间；被删位置只能映到“删除区间之后第一个仍存在的输出点”，即 p。光标落在被删空白里时，映射到**换行符本身的位置（行尾换行之前的那一点，即换行左缘）**，编辑器把它吸附到行尾，不会越过换行到下一行。`\r\n` 中的 `\r` 同理映到 `\n`。终点 ToOut(lenOrig)=lenOut。

## 5. 缓冲超限
待定空白可能任意长（下一个 `\n` 之前）。拒绝：在写入导致缓冲超限时返回 ErrWSTooLarge（带原文偏移），已输出保留、进终态；不产生任何输出/映射，故不变量 4/5/6 不被触及，错误可判定。强制输出：必须先假定“非行尾”吐出空白，若之后真出现行尾，这些空白本该删除却已无法收回，违反 5（输出含行尾空白）与幂等；且映射已固化，无法同时满足 6。因此选择**拒绝**。输出超限同理在真正 append 前返回 ErrOutputLimit。

## 6. par 拼接
唯一能携带跨切点状态的是末尾的 `\r` 与连续空白；二者一旦遇到 identity 字节（非空白、非换行）即落定，遇到 `\n` 则整行结束。因此切点附近的“未决区域”只可能从该切点向前延伸到最近一个已落定的 `\n`（无 `\n` 时延伸到段首，有上界 MaxWS+1）。
做法：对每个切点找两侧最近 `\n` 界定的修正窗；相邻无 `\n` 分隔的切点把窗口合并。窗口外是各段独立、已落定的前缀/后缀，直接平移拼接；窗口内合并其原文区间后用一个 norm 重算。span 提供 SliceRuns(runs,origLo,origHi)：按累计 orig 坐标切出子区间 run（identity run 在边界切成两段），orig/out 分别减去基址即得段内坐标；重算窗口的 run 原样替换，坐标天然连续，映射逐位一致。修正范围每切点 ≤ 到最近 `\n` 的距离 + 合并项，总长 ≤ 8×(MaxWS+1)，有上界。
