# 最短往返十进制编解码器设计

目标：float64 与十进制文本互转，逐位往返（math.Float64bits 相等）且有效数字最少。

## 一、位数的正确判据：逐位递增 + 精确往返检查

- 十七位一定够：float64 尾数 53 位，相邻两值相对间隔不超过 2^-52 ≈ 2.22e-16，
  而十七位十进制有效数字的相对网格不超过 1e-16，故每个 float64 的舍入区间内
  必落有一个十七位十进制值；又 10^17 > 2^53，不同 float64 不会共享同一个
  十七位值。所以十七位必能往返。
- 十七位不是最短：0.1 的精确值是 0.1000000000000000055511151231257827…，
  但一位有效数字 0.1 就落在它的舍入区间内，最短是一位而非十七位。
- 十五位不一定够：10^15 < 2^53 ≈ 9.007e15，十五位十进制值的个数少于同一
  数量级上 float64 的个数，由鸽笼原理必有两个不同 float64 共享同一个十五位
  最近值，至少其一无法往返。故不存在固定的安全位数。
- 判据：从 n=1 起逐位递增，对每个 n 用精确算术判断「是否存在 n 位有效数字
  的十进制值落在 x 的舍入区间内」，第一个成立的 n 即答案。存在性检查等价于
  「解回来是否等于原值」，但全程不出现浮点回代。

## 二、精确算术做法（math/big，不用浮点验证）

- x 分解为 v = m * 2^e（m 为含隐含位的尾数整数，次正规数无隐含位）。
- 舍入区间 (lo, hi)：lo、hi 为 x 与左右邻居的中点，用 big.Rat 精确表示
  （整数乘 2 的幂）。尾数 m 为偶数时端点闭合（ties-to-even 归 x），奇数时
  开放。m == 2^52 且阶码大于 1 时下侧间隔减半，单独处理。
- 对给定的 n：取十进制步长 step = 10^k，使 v/step 落在 [10^(n-1), 10^n)
  （先用 math.Log10 估计 k，再用 big.Rat 比较精确修正）。计算
  d_min = 满足 d*step 在 lo 之上（按开闭决定是否可取等）的最小整数、
  d_max = 满足 d*step 在 hi 之下的最大整数。d_min <= d_max 即存在；取最接近
  v 的 d = clamp(round(v/step), d_min, d_max)，剥掉尾部零后即为答案。
- 解析方向同样精确：文本转大整数 M 与 10 的幂，组成 big.Rat，再手工做
  最近偶数舍入拼出 53 位尾数与阶码（含次正规与正负零），不用浮点乘除。
- 浮点回代（如 float64(整数)*10^k 再比较）会让十进制到二进制的舍入误差与
  反向误差互相抵消而误判相等；本实现全程有理数比较，杜绝二次误差。

## 三、指数形式的选择规则（确定、可复现）

设 E 为最高有效数字的十进制指数（v = D.DDD… * 10^E）：

- 当 -4 <= E <= 16 用定点形式（如 0.0001、123、10000000000000000）；
- 其余用指数形式 d[.ddd]e±XX（指数带符号、至少两位，如 1e+17、1e-05）。
- 规则只依赖 E，同一数值永远得到同一文本。

## 四、错误与边界约定

- NaN 与 ±Inf 没有对应的有限十进制文本，若静默映射为某字符串会丢失
  「这不是一个数」的语义并可能与合法文本冲突，故编码返回可判定错误。
- +0.0 与 -0.0 分别编为 0 与 -0，符号位参与编码，互不混同。
- parse 错误四类（errors.Is 可区分）：空串、非法字符（报字节位置）、
  指数溢出（调整后的十进制指数绝对值大于 400，或指数字面量超出 int32）、
  有效数字超过 17 位（超出本编码器输出域，无法保证按本规则往返）。
- dec 包内置非导出原子计数器记录精确往返检查次数（仅诊断用途，不影响
  结果；对 0.1 第一次检查即成功，计数为 1，不超过 3）。
