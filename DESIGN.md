# 设计推导（老化优先级队列）

## 1. 免重算的堆键

任务 a 的有效优先级 EP_a = base_a + (now - t_a)/K（t_a 为入队刻），越大越先出。
比较两任务 a,b（K>0）：
EP_a > EP_b  ⇔  base_a - t_a/K > base_b - t_b/K
⇔  K·base_a - t_a > K·base_b - t_b（乘以 K，不改变方向）。
定义整数键 key = t - K·base（int64）：EP_a > EP_b ⇔ key_a < key_b。
结论：两任务的相对次序只由 key 决定，与 now 无关，时钟推进无需重排堆，
最小堆按比较元组 (key, t, id) 升序即可（t 为入队刻）。id 唯一，故不存在
三元组全同的两个任务。

不溢出范围：K ≥ 1，|t| < 2^62 且 K·|base| < 2^62，则 |key| < 2^63，
int64 求差安全；K ≤ 0 或越界时构造返回 ErrRange。

float64 反例（now=1，K=1）：A（老）base=2^53+1、t=0，真值 EP_A=2^53+1；
B（新）base=2^53+2、t=1，真值 EP_B=2^53+1，二者并列，按 t 应由 A 先出。
float64 在 2^53 处 ULP=2：float64(2^53+1) 被舍成 2^53，而 2^53+2 可精确
表示，于是算出 EP_A=2^53、EP_B=2^53+2，错误地让新任务 B 排在老任务 A 前面。

## 2. 饥饿上界

设低任务 L：base_L=0、t_L=0，key_L=0；涌入高任务 H：base=Δ（Δ≥1），
H_s 在刻 s 入队，key_H(s)=s-KΔ，每刻 Push 一个、Pop 一次。
L 先于 H_s ⇔ key_L ≤ key_H(s)（s=KΔ 时并列，L 入队更早而先出）
⇔ 0 ≤ s-KΔ ⇔ s ≥ KΔ。故刻 0..KΔ-1 弹出 H_0..H_{KΔ-1}，刻 KΔ 的 Pop 中
H_{KΔ} 与 L 并列、L 先出：L 等待恰为 KΔ 刻，这是精确上界。
若并列时不按“入队更早者先出”，H_{KΔ} 可能先于 L，L 被推到刻 KΔ+1，
上界恶化成 KΔ+1——故并列规则是上界为 KΔ 的必要条件。
恰好达到上界的构造：K=2，Δ=2。刻 0 预置 L(base=0)；刻 s=0..4 各
Push H_s(base=2) 后 Pop 一次。H_0..H_3 的 key 为 -4,-3,-2,-1 依次先出，
刻 4 时 H_4 的 key=0 与 L 并列，L 凭 t=0 更早先出：等待恰为 4=KΔ 刻。
