package ontology

// Rank computes ROW_NUMBER / RANK / DENSE_RANK per partition.
// It is a stub until the implementation lands.
func Rank(rows []Row, opts Options) ([]Result, Stats) {
	_ = rows
	_ = opts
	return nil, Stats{}
}
