// Command demo exercises the jstr strict JSON string codec and prints
// one OK/FAIL line per check. Checks are filled in as packages land.
package main

import "fmt"

func main() {
	fmt.Println("demo: skeleton, checks pending")
}
