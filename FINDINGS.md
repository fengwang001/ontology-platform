# FINDINGS

## 表① 五个聚合器的撤回能力核对（agg 测试结论）

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
|---|---|---|---|
| Count | 是（+1） | 是（-1） | 否 |
| Sum | 是（+v） | 是（-v） | 否 |
| Min | 是 | 仅当被删值≠当前极值；删到极值返回 false | 是 |
| Max | 是 | 仅当被删值≠当前极值；删到极值返回 false | 是 |
| DistinctCount | 是 | 仅当该值还有其他持有者；删最后持有者返回 false | 是 |

## 表② 截断点分类（journal，200 帧逐字节截断，结论待重放测试回填）

| 字节位置区间 | 分类 | 重放后生效记录数 |
|---|---|---|
| 待回填 | 头部不完整 | 0 |
| 待回填 | 长度前缀不完整 | 待回填 |
| 待回填 | 记录体不完整 | 待回填 |
| CRC 不匹配（翻转字节示例） | CRC 不匹配 | 帧不生效 |
