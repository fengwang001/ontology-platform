# 实测结论

测试环境：Go 1.26.5 linux/arm64，`go test -count=1 ./...` 与 `-race` 全绿。

## 表 1：权重 1:3:6，10 万次调度（代价均为 1）

| 租户 | 权重 | 理论占比 | 实际占比 | 相对偏差 |
|------|------|----------|----------|----------|
| A    | 1    | 0.10     | 0.10000  | 0.0000%  |
| B    | 3    | 0.30     | 0.30000  | 0.0000%  |
| C    | 6    | 0.60     | 0.60000  | 0.0000%  |

逐租户相对偏差均 < 5%（实测为 0，因 100000 恰好被份额整除；非整除步数下
偏差也不超过一个任务步长的量级）。权重 1:3 两租户场景执行次数比收敛到
3.000 ± 5%。

## 表 2：空闲租户重新加入（X 空闲期间 Y 执行 1000 个任务）

| 实现                | X 重新加入后连续执行步数 | Y 的表现          |
|---------------------|--------------------------|-------------------|
| 抬升 vt 到 sysVT    | 1（随后与 Y 交替）       | 仅被推迟 1 步     |
| 不抬升 vt           | 1000（追上 vt_Y 才停）   | 被饿死 1000 步    |

不抬升时 X 的 vt 停在 1，Y 的 vt 已到 1000，X 以每步 +1 追赶，连续独占
1000 步（含 vt 相等时按 ID 字典序 X<Y 仍优先的一步）。抬升后 X 与 Y 同
水平，按 ID 并列规则 X 先走 1 步，之后正常交替。

## sched 组其他结论

- 单次选择比较次数：1000 个活跃租户实测 ≤ 30，上界 4·ceil(log2 1000)=40；
  1000 租户仅 5 个非空时堆长为 5，空闲租户不占选择代价。
- 同 vt 并列：按租户 ID 字典序，a,b,c 依次出队，与提交顺序无关。
- 零代价任务：按 MinCost=1 计费，200 步内零代价租户与普通租户各约一半，
  无独占。
- 权重 1e-9 与 1e9 并存：2 万步后 sysVT 有限（无溢出/NaN），极小权重租户
  仅被服务 1 次。
- 确定性：同一固定任务流 20 次重放，调度序列逐元素相同。

## admit 组结论

- 四类哨兵错误（ErrInvalidWeight / ErrInvalidCost / ErrQueueFull /
  ErrTenantNotEmpty）均可被 errors.Is 区分；另有无主租户 ErrUnknownTenant。
- 背压隔离：租户 a 队列满后入队返回 ErrQueueFull，租户 b 仍可正常入队、
  正常被调度。
- 并发：8 生产者 + 8 消费者、16 租户、10 万任务，`-race` 干净；每个任务
  恰好被取出一次，总取出数 = 总提交数，无重复序号。

## stat 组结论

- Shares/Expected/RelDeviation 表驱动验证：精确命中时偏差 0；4% 偏移、
  缺失租户（偏差 1）、空输入均符合预期。
- Tracker 时钟可注入：假时钟推进 1 分钟，LastServed 精确跟随注入时钟；
  Executed 返回副本，外部修改不影响内部状态。
