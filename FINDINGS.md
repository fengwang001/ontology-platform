# 实验结论

## 表 1：三种 from 位置的定位行为（锚点间隔 N，replay 实测后回填）

| from 位置 | 选中锚点 | 定位跳过事件数 | 是否用上索引 |
|---|---|---|---|
| 恰好等于某锚点序号 | 该锚点自身 | 0 | 是 |
| 落在两个锚点之间 | 不大于 from 的最大锚点 | 1..N-1（< N） | 是 |
| 小于本段首锚点（段首） | 无可用锚点 | 0（从段头扫） | 否（回退段首） |

200 个随机 from、N=128、10 万事件：跳过数上界实测见 replay 测试，全部严格 < 128。

## 表 2：500 事件段逐字节截断分类（文件长 5528B，头 28B，每帧 11B）

| 类别 | 字节区间（截断长度 t） | 结论 | repair 前条数 | repair 后条数 |
|---|---|---|---|---|
| 头部不完整 | 1..27 | ErrTruncatedHeader，整段不可用 | 0 | 0 |
| 长度前缀不完整 | 11k+29..11k+32（k=0..499，上限 5520） | ErrTruncatedLength | k | k |
| 事件体不完整 | 11k+33..11k+39（上限 5523） | ErrTruncatedBody | k | k |
| CRC 不完整 | 11k+40..11k+43（上限 5527） | ErrTruncatedCRC | k | k |
| 恰好整帧边界 | t=28+11k（k=1..500；5528 为完整文件不截断） | 帧流完整，段头 count 虚高 → ErrCountMismatch | 500(声明)/k(实读) | k |
| CRC 内容不符（非截断，翻转 1 字节） | 首帧体偏移 33 | ErrCRCMismatch，ValidEnd=28 | 0 | 0 |

逐字节循环（t=1..5527，共 5527 个截断点）全部命中上表，无未分类点；
repair 前后条数差值在 repair 包测试中断言。
