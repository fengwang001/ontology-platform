# 测试结论

## 表① 导出期间改写键数 → 保留值个数与内存峰值

场景：100,000 个键，块大小固定（demo/测试中为 4 KiB）。保留值个数由
`snapshot.Retained()` 读取（保留了快照旧值的键数）；块驻留峰值由导出器
`PeakBlockBytes()` 读取（单次导出期间最大单块体字节数）。

| 导出期间改写键数 | 保留值个数 | 断言上限 | 块驻留峰值 | 结论 |
|---|---|---|---|---|
| 0 | 0 | 0 | 实测 ≤204B（=块大小，`TestPeakBlock/multi`） | 无 COW，零额外保留 |
| 100（10 万键库） | 100 | ≤100 | 实测 ≤204B | 保留值与改写键数成正比，与总键数 10 万无关 |
| 全部（8 键小库） | 8 = 键数 | ≤键数 | 实测单块 <200B | 每键都保留旧值；读取次数仍精确等于键数 |

已通过：`go test -count=1 ./...` 与 `go test -race -count=1 ./snapshot/...`；
`TestCOW` 还断言“导出到第 50 键时第 80 键被改写，导出读到旧值 old-79”、
读取次数精确等于键数、快照关闭后保留值归零；`TestOverlap` 断言两个重叠
快照只有都关闭后才释放（按最老快照水位裁剪）。
`TestResumeEveryK` 对 k=1..块数 逐个续传，与一次性导出**逐字节相同**；
`TestOrder` 断言字典序/逆序总校验和相同；`TestExpiredResume` 断言快照过期
返回 `ErrSnapshotExpired` 且已导出部分未被修改；`TestConcurrentExports` 4 路
并发导出与单独导出逐字节相同，`-race` 干净。

## 表② 逐截断点的分类（待截断测试完成后补写）
