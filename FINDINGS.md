# FINDINGS

## posting 编解码

- 增量编码往返一致（空链 / 单文档 / 多文档大间隔均通过）。
- 损坏数据（截断 varint、零 delta、零计数）均返回 `posting.ErrCorrupt`。
- 重复 (doc,pos) 添加被拒绝并计数，测试构造 3 次重复，`Dups()==3`。

## 表一：短语命中（文本 / 短语 / 次数 / 起始位置）

| 文本 | 短语 | 命中次数 | 起始位置 |
| --- | --- | --- | --- |
| `A A A` | `"A A"` | 2 | [0, 1] |
| `A A A A` | `"A A"` | 3 | [0, 1, 2] |
| `A B A B A` | `"A B A"` | 2 | [0, 2] |
| `A B A` | `"A"`（长 1 退化） | 2 | [0, 2] |
| `A B` | `"A B C"`（超长） | 0 | [] |

## 复杂度实测

- 1 万 A + 1 万 B 查 `"A B"`：命中 10000 次，比较 40001 次，上界 4×20000=80000，达标。
- AND（链长 150/100/120）：比较 398 次，上界 4×100×3=1200，按最短链驱动达标。

## 表二：段截断分类（200 词段）

段文件总长 3226 字节（头 24 + 正文 3198 + CRC 4），逐字节截断 1..3225 的分类：

| 截断字节区间 | 分类结论 | 恢复出的词数 |
| --- | --- | --- |
| [1, 23] | 头部不完整 `ErrHeaderIncomplete` | 0 |
| [24, 3212] 中落在 term 字节处 | 词典不完整 `ErrDictIncomplete` | 0..199（完整 record 前缀） |
| [27, 3221] 中落在 postings 字节处 | 倒排链不完整 `ErrPostingsIncomplete` | 0..199（完整 record 前缀） |
| [3222, 3225] | CRC 不匹配 `ErrCRCMismatch` | 200（正文完整，仅 CRC 受损） |

- 词典与倒排链区间交错（record 交错布局），并集覆盖 [24, 3221]。
- 全部 3225 个截断点均被四类之一覆盖，无未分类错误。
- 恢复出的索引自洽：词典中每个词的倒排链都可完整解码，无悬挂指针。

## 合并 / 删除 / 并发

- 3 段 × 10 文档 × 3 词合并：ItemsRead == ItemsWritten == 90，每项规定读写各一次。
- 删除后立即查询（位图过滤）与删除后合并再查询，结果逐字节相同。
- 合并中途崩溃（残留 `*.tmp`）：旧段查询结果不变，重开目录时半截段被清理。
- 4 个查询 goroutine 与 5 次合并并发：结果始终一致、无重复文档，`-race` 干净。

## verify 自检

- 合法段通过；四类截断各取一例均被 `errors.Is` 正确分类。
- 位置乱序（可解码但违反不变量）被 `verify.ErrInvariant` 检出。
