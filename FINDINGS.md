# FINDINGS — 实测结论

## 表 1：权重 1:3:6，10 万次调度的代价占比

任务流：三租户各预提交 10 万个任务，单任务代价分别为 1/2/3，
连续调度 100000 次，按实际执行代价统计占比。

| 租户 | 权重 | 理论占比 | 实际占比 | 相对偏差 |
|------|------|----------|----------|----------|
| a    | 1    | 0.1000   | 0.1000   | 0.004%   |
| b    | 3    | 0.3000   | 0.3000   | <0.001%  |
| c    | 6    | 0.6000   | 0.6000   | <0.001%  |

结论：逐租户相对偏差均远小于 5% 上界，`vt += c/w` 的份额收敛得证。

## 表 2：空闲租户重新加入，抬升 vs 不抬升 vt

场景：X 空闲期间 Y（权重 1、代价 1）执行 1000 个任务，之后 X
（权重 1）提交 1 个任务，统计 X 的连续执行步数。

| 实现                | X 连续执行步数 | 行为                     |
|---------------------|----------------|--------------------------|
| 抬升 vt 到系统 V(t) | 1              | 1 步内被服务，随后公平竞争 |
| 不抬升              | 1001           | 独占约 n·w_X/w_Y 步追平    |

结论：不抬升时 X 独占步数 ≈ Y 在其空闲期间执行的任务数（1000 步），
与 DESIGN.md 第 2 节的推导 `k ≈ n·w_X/w_Y` 一致；抬升后 X 最迟
1 步内被服务且无法独占，Y 不被饿死。

## 其他测试结论

- 比较次数：1000 活跃租户单次选择实测 19 次，上界 4·ceil(log2 1000)=40。
- 堆中元素数恒等于非空租户数，空闲租户（测试混入 500 个）不进堆。
- 零代价任务按 minCost=1 记账，最大连续执行 1 步，无独占。
- 并发：10 万任务并发提交 + 8 消费者，取出总数=提交数、无重复序号，
  `-race` 干净。
- 确定性：同一固定任务流 20 次运行调度序列逐元素一致。
