# 实测结论

数据由 `go test -count=1 ./...`（含 `-race`）与 `go run ./cmd/demo` 实测，
环境 Go 1.26.5。测试均为表驱动；步数来自 `budget.Counter` 的确定性计数。

## 表 ① `%a%a…%b`（文本 = 20 个 `a`）

模式含 k 个 `%a` 再加 `%b`，必然失配（无 `b`）。上界 = `4 * 折叠后模式长度 * 20`。

| `%` 个数 | 折叠后长度 | 实测步数 | 线性上界 | 2^%数（朴素分支量级） | 结论 |
|---:|---:|---:|---:|---:|:--|
| 2  | 6  | 24 | 480  | 4    | 24 ≤ 480，线性 |
| 4  | 10 | 26 | 800  | 16   | 26 ≤ 800，线性 |
| 8  | 18 | 30 | 1440 | 256  | 30 ≤ 1440，线性 |

步数随 `%` 个数仅常数级增长（30 vs 朴素 2^8=256 量级），与单锚点 O(P*T) 推导一致。
同一对输入重复 1000 次步数完全相同（实测 30），无随机性、无缓存副作用。

## 表 ② 转义与边界八种情形

| # | 输入（模式 / 文本） | 期望 | 实际 |
|---:|:--|:--|:--|
| 1 | `\%` / `%` | 匹配（字面量 `%`） | 匹配 |
| 2 | `\_` / `_` | 匹配（字面量 `_`） | 匹配 |
| 3 | `\\` / `\` | 匹配（字面量 `\`） | 匹配 |
| 4 | `\a` / 任意 | 模式错误，字节位置 0 | ErrPattern @0 |
| 5 | `ab\` / 任意 | 尾随转义错误，字节位置 2 | ErrPattern @2 |
| 6 | 空模式 / 空串、`x` | 只匹配空串 | 空串匹配、`x` 不匹配 |
| 7 | `___` / `ab` | 长度不足，不匹配 | 不匹配 |
| 8 | `%` / `ab\xff` | 文本错误，侧别=文本，字节位置 2 | ErrText @2 |

补充对拍：固定种子 `0xC0FFEE` 生成 2000 组随机（模式 0..8 / 文本 0..10，
字符集 `a,b,%,_`），正式实现与朴素递归参考实现结论全部一致，复跑可重现；
`%%%a` 与 `%a` 在 7 组文本上匹配集合逐组相等且折叠后长度同为 2；
`caf_` 匹配 `café`、`_` 匹配四字节 `😀`；`-race` 下并发匹配各自步数互不串台。
