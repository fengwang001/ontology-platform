# TopK 同 ID 覆盖路径：行为与文档承诺不一致清单

复现实验基于当前实现（未改动任何实现文件），对应表征测试见
`overwrite_test.go` 的 `TestOverwriteCharacterization`。

涉及的文档承诺：

- `selector.go` 的 `Push` 注释：「同一 ID 后到的分数覆盖先到的分数，并立即重排」。
- 同文件 `Len` 注释：「内部当前持有的元素个数（恒不超过 K）」。
- `helpers.go` 的 `replace` 注释：「以去掉旧元素后剩余集合中的最差元素为截断阈值，
  ……否则该 ID 立即掉出 Top-K」。
- `compare.go`：分数相等（含 +0/-0）一律按 ID 字典序升序。

---

## 1. 非满容器下覆盖为更差分数，元素被误删（核心缺陷）

- 复现：`New(3, Desc)`；`Push("a",10)`、`Push("b",5)`；`Push("a",1)`
  （此时 `len=2 < K=3`，a 降为 1）。Asc 对称：`New(3,Asc)`，
  a:1、b:5 后 `Push("a",10)`。
- 实际输出：`Snapshot() == [{b 5}]`，`Len() == 1`；元素 a 消失，
  持有数从 2 降到 1（K=3 仍有空槽）。
- 应当输出：`[{b 5} {a 1}]`，`Len() == 2`。容器未满，第 3 个槽位是
  空位，任何非 NaN 分数都应入选，覆盖只应更新分数并重排。
- 根因：`replace`（`helpers.go`）不区分 `len == k` 与 `len < k`，
  一律取「去掉旧元素后剩余集合的最差元素」为截断阈值。该阈值只在满容器时
  等于真实的第 K 名；非满时真实阈值是 +∞（Desc）/-∞（Asc）的空槽，
  逻辑把「比现存最差元素差」错误地当成了「掉出 Top-K」。

## 2. 非满容器下覆盖为并列分数、且本 ID 字典序更大时被误删

- 复现：`New(3, Desc)`；`Push("z",5)`、`Push("a",5)`；`Push("z",5)`
  （覆盖后分数与阈值元素 a 的 5 并列，且 z > a）。
- 实际输出：`Snapshot() == [{a 5}]`，`Len() == 1`。
- 应当输出：`[{a 5} {z 5}]`，`Len() == 2`。并列打破规则只应决定
  排名先后，非满容器下不应导致淘汰；同 ID 覆盖后仍应是这两个元素在榜。
- 根因：同发现 1。保留条件 `rankLess(incoming, threshold)` 在并列时
  要求 incoming 的 ID 严格更小，z 不满足即走删除分支——此判定只适用于
  满容器下「与第 K 名争一个槽」的场景。

## 3. +0.0/-0.0 在非满覆盖路径上同样触发误删

- 复现：`New(3, Desc)`；`Push("z",0)`、`Push("a",0)`；
  `Push("z", -0.0)`。
- 实际输出：`Snapshot() == [{a +0}]`，`Len() == 1`。
- 应当输出：`[{a 0} {z -0}]`，`Len() == 2`。
- 根因：`rankLess` 正确地把 +0/-0 判等，随后走 ID 字典序；但该并列结果
  被喂给发现 1 中本不该存在的非满截断判定，z（ID 更大）被删。
  即符号零判等本身正确，误伤来自外层非满阈值逻辑。

## 4. len==1 的特判与非满一般情形自相矛盾

- 复现：`New(1, Desc)`；`Push("a",100)`；`Push("a",-999)`。
- 实际输出：`[{a -999}]`，`Len() == 1`（保留，分数更新为 -999）。
  对比发现 1：`New(3,Desc)` 中 `len==2` 时更差覆盖却被删。
- 应当输出：两者都应保留（单元素容器下当前实际结果恰好在语义上正确）；
  但同一条「非满容器覆盖更差」路径仅因元素个数不同就给出相反结果。
- 根因：`replace` 开头对 `len(hp.items)==1` 做了无条件就地替换的特判，
  绕过了阈值比较；该特判实际上是「非满容器总是保留」规则在单个元素时的
  偶然正确版本，却没有推广到 `1 < len < K`，形成行为断层。

## 5. 覆盖路径与新 ID 推入路径的满容器并列结果不一致（集合形态不同）

- 复现 A（覆盖）：`New(2,Desc)`；m:10、z:5；`Push("m",5)`，
  得 `[{m 5} {z 5}]`，`Len()==2`。
- 复现 B（新 ID）：同初始状态后 `Push("a",5)`（走 Push 满容器分支），
  得 `[{m 10} {a 5}]`，`Len()==2`。
- 实际输出：两条路径在「incoming 与最差元素并列且 ID 更小」时都保留
  incoming，但覆盖路径**不踢出阈值元素**（就地改值 + fix），
  最终留下 `{m,z}`；新 ID 路径则移除最差元素，留下 `{m,a}`。
- 应当输出：语义上「保留两个分数最高、并列时 ID 最小者」应对两种到达
  方式给出同一套规则；当前结果集取决于该分数是覆盖还是新推入。
- 根因：`replace` 命中保留分支时只做 `hp.items[pos]=incoming; fix`，
  没有复用 `Push` 满容器分支「remove 最差 + push incoming」的动作；
  覆盖路径缺少满容器下的最终再截断步骤。

## 6. 文档层面的内部张力

- `Push` 注释承诺「同一 ID……覆盖……并立即重排」，字面只要求就地更新；
  `replace` 注释又承诺非满场景也会「立即掉出 Top-K」。两处注释未提及
  容量前提，非满误删（发现 1-3）至少与 `Len`「持有数恒为当前元素个数」
  的直觉及滚动窗口「同 ID 反复更新不会凭空丢元素」的使用预期冲突。
- 满容器下覆盖更差掉出的行为已被
  `TestOverwriteWorseDropsOutOfTopK` 固化，本次按「只加测试、不改实现」
  要求，所有表征测试均断言当前真实行为并全部通过。

## 修复方向（仅记录，未实施）

- `replace` 开头区分：`len(hp.items) < t.k` 时无条件就地更新分数；
  仅 `len == k` 时才做阈值比较，且保留时与 `Push` 满容器分支共用
  「与最差元素比较、严格更优才替换」的同一截断动作。
