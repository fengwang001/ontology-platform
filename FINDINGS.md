# FINDINGS：测试结论记录

## 表① 五种请求形态的执行步骤与临时名

| 形态 | 请求 | 执行步骤序列 | 临时名数 |
| --- | --- | --- | --- |
| 链 | a→b, b→c | b→c；a→b | 0 |
| 二元环 | a→b, b→a | a→tmp；b→a；tmp→b | 1 |
| 三元环 | a→b, b→c, c→a | a→tmp；c→a；b→c；tmp→b | 1 |
| 目标已存在 | a→b（b 存在且不搬走） | 拒绝整批，无步骤 | 0 |
| 同名冲突 | a→x, b→x | 拒绝整批，无步骤 | 0 |

结论（`plan` 包测试）：

- 链按拓扑序执行，逐步快照断言中途无覆盖；环借 1 个临时名，k 元环产生 k+1 步。
- 10 个互不相交的三元环恰好产生 10 个临时名（临时名数 == 环数）。
- 预占 `\x00renametmp0..9` 后生成器重试到 `\x00renametmp10` 仍成功；
  全部候选被占时返回可判定的 `cycle.ErrNoTemp`，不覆盖数据。
- 四类冲突均可用 `errors.Is` 区分，且检测后命名空间逐元素未变。
- 5 万请求链的查找计数 ≤ 4×(请求数+涉及名字数)，实测 350000 ≤ 400004。
- 打乱构造顺序 20 次，步骤序列与临时名序列逐元素相同。
- 自环为无操作（检测前丢弃）；空请求集得空计划；大量重复旧名被
  `ErrDuplicateOld` 拦截；空串与含分隔符名字按普通字符处理。
