# 实验与测试结论

## 0. 进度记录

- row：编解码表驱动往返（空键、±0、±Inf、大整数键）逐位一致；
  NaN 被 Valid 拒绝；7 种坏 body 均报错。

## 1. 四种聚合：部分状态合并规则与结合性

| 聚合 | 部分状态 | 合并规则 | 结合律结论 |
|---|---|---|---|
| Count | n int64 | n₁+n₂ | 满足（整数加法） |
| Sum | s float64 | s₁+s₂ | 实数与 ±Inf 上满足；精确可表示数值下位相同 |
| Min | v+exists | 空取对方，否则取小者 | 满足，且幂等（min∘min=min） |
| Max | v+exists | 空取对方，否则取大者 | 满足，且幂等（max∘max=max） |

（测试结论待补）

## 2. 分区文件截断分类（字节区间）

（截断测试后补）
