# 实测结论

测试基线：段头 24 字节；记录 = 4 字节长度前缀 + 载荷 + 4 字节 CRC32。
截断实验用 500 事件、每条载荷 8 字节（记录 16 字节），文件全长 8024 字节。

## 表 1：三种 from 位置的定位（N = 128）

| from 位置 | 用例 | 跳过事件数 | 是否用上索引 |
|---|---|---|---|
| 恰等于锚点 | from = 256（锚点 256） | 0 | 是，直接落在锚点 |
| 两锚点之间 | from = 300（锚点 256） | 44 | 是，从锚点扫 44 条 |
| 小于首锚点 | from = 10 < firstSeq = 50 | 0 | 否，钳到段头后开始 |

三种位置跳过数均 < N；10 万事件、200 个随机 from 全部满足跳过数 < 128。

## 表 2：截断点分类与修复（500 事件段，全长 8024）

| 截断点字节区间 | 分类结论 | repair 前条数 | repair 后条数 |
|---|---|---|---|
| 1..23 | 头部不完整 ErrShortHeader | 头不可读 | 不可修复 |
| 24+16k .. 24+16k+3 | 长度前缀不完整 ErrShortLengthPrefix | 500 | k |
| 24+16k+4 .. 24+16k+11 | 事件体不完整 ErrShortBody | 500 | k |
| 24+16k+12 .. 24+16k+15 | CRC 不匹配 ErrCRCMismatch | 500 | k |
| 24+16k（记录边界） | 完整文件，无错误 | 500 | 500（无需修复） |

全部 8023 个截断点逐一分类通过；repair 截掉坏尾并把段头 count 改为实际可读
条数 k = (cut-24)/16，修复后顺序重读不再报错，前后差值 = 500 - k。

## 其他结论（随测试补充）

- event 编解码：空载荷合法；CRC 覆盖长度前缀+载荷，翻转任一字节必报
  ErrCRCMismatch；长度前缀超上限报 ErrBadLength。
- segment：追加写后段头 count 即时可见；相邻段满足 首序号+事件数==下一段
  首序号（10/1/250 三种段长验证）；坏 magic 与短头分别报 ErrBadMagic、
  ErrShortHeader。
- sparse：删除索引后从段重建，字节与原索引完全一致；Floor 对“小于首锚点 /
  等于锚点 / 锚点之间 / 超过末锚点”四种位置均给出正确的不大于锚点。
