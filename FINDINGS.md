# FINDINGS: 实测结论

测试基准：1000 条 `key-000000` 风格字符串，单块，K=16，块文件 3933 字节。
K 表基准：见表一（lookup 完成后填入）。

## 表二：逐字节截断分类（块文件长 3933B，截断点 1..3932 全覆盖）

| 分类 | 截断点字节区间 | 判定错误 | 恢复条目数 |
|---|---|---|---|
| 头部不完整 | [1, 15] | ErrHeaderIncomplete | 0 |
| 重启点表不完整 | [16, 267] | ErrRestartTable | 0 |
| 条目不完整 | [268, 3928] | ErrEntryIncomplete | 0..999（随截断点线性增长） |
| CRC 不匹配 | [3929, 3932] | ErrCRC | 1000（条目完整，仅校验和受损） |

- 全部 3932 个截断点均落入且仅落入上述四类之一，无未分类错误、无 panic。
- 每个截断点恢复出的最大前缀都通过严格有序断言（slices.IsSorted）。
- 截断落在条目区时恢复数 = 完整条目数；落在 CRC 的 4 字节内时条目
  全部恢复，仅报 CRC 错误。

## 故障注入结论（block 包测试）

- 前缀长度撒谎：把第 2 条 sharedLen 改为 0x7f（> 前一条 5B），Decode 与
  DecodeEntry 均报 ErrPrefixLen，无越界 panic。
- 重启点偏移错位：restart[1] +1 指向条目中间，DecodeEntry(5) 回退到
  块首顺序解压，结果 "e005" 正确，错误标注 ErrRestartOffset；全量
  Decode 同样检出且条目完整。
- 有序性：重复值、`ab` 后跟 `a` 均被 Encode 拒绝（ErrUnsorted）。
- 前缀按字节计：café/cafés 公共前缀 = 5 字节（码点口径为 4），测试锁定。

## 表一：K 的权衡（10000 条 `user:%08d`，blockSize=128，200 次随机采样）

| K | 字典总字节 | 取值平均解压次数 | 二分平均比较次数 |
|---|---|---|---|
| 1 | 194128 | 1.0 | 14.4 |
| 4 | 74628 | 2.5 | 13.8 |
| 16 | 45083 | 8.7 | 18.3 |
| 64 | 37709 | 31.9 | 39.3 |

- 取值平均解压 ≈ (K+1)/2（K=16 实测 8.7，理论 8.5），与推导一致。
- K=1 等价于不压缩，体积最大；K 从 1 到 64 体积降到约 1/5。
- 二分比较随 K 增大（区间线扫变长）；K=16 时实测最大 29 次，
  上界 4*ceil(log2(256))+16 = 48，余量充足。
- 10 万条、K=16 下 500 个随机 i 的取值解压次数全部 <= 16。

## 扫描与并发结论

- 100 块字典上前缀 "049" 命中 100 条，块解压计数器恰为 2：未命中块
  仅靠目录首末值就被跳过，空前缀才解压全部 100 块。
- UTF-8 前缀 café/日本 扫描正常（前缀按字节匹配，与块格式口径一致）。
- 8 协程并发 Get/Find/Scan 同时另一协程反复构建新字典：结果与串行
  一致，未完成的构建对查询不可见（查询只持有旧不可变字典），
  `-race` 干净。
- verify.Dict 对完好字典通过；篡改末块 1 字节后自检报 CRC 错误。
