# 实测结论

测试环境：Go 1.26.5 linux/arm64，`go test -count=1 ./...` 与 `-race` 全绿。

## 已完成组的结论

- event 编解码：空载荷 / nil 载荷 / 大二进制载荷均往返一致；不足 8 字节的输入全部判为 `ErrEventTruncated`。
- segment：5 组写读往返（单段 / 恰满轮转 / 非整除轮转 / 空载荷 / 单事件）全部一致；
  相邻段头均满足 `firstSeq + count == 下一段 firstSeq`；记录解码的四类截断错误可用 `errors.Is` 区分。

## 表 1：三种 from 位置的定位表现（N=128，段内连续序号）

| from 位置 | 锚点选择 | 跳过事件数 | 是否用上索引 |
| --- | --- | --- | --- |
| 恰好等于锚点序号 | 该锚点本身 | 0 | 是 |
| 位于两个锚点之间 | 不大于 from 的最大锚点 | from−锚点序号，实测 1..127，严格 <128 | 是 |
| 小于首锚点 | 无锚点可用，回退段头扫描 | 0（钳位到段首） | 否 |

补充：10 万事件、N=128、200 个随机 from，skipped 全部严格小于 128；
bytesRead == (区间条数 + skipped) × 记录长，且 ≤ 区间长度 + 一个锚点区间。

## 表 2：截断分类与修复（500 事件段，记录定长）

| 截断点字节区间 | 分类结论 | repair 前条数 | repair 后条数 |
| --- | --- | --- | --- |
| [1, 25]（头 26B 之内，共 25 点） | `ErrHeaderIncomplete` | — | — |
| 记录内偏移 0–3（每记录 4 点，共 2000 点） | `ErrLengthPrefixIncomplete` | — | — |
| 记录内偏移 4–26（每记录 23 点，共 11500 点） | `ErrBodyIncomplete` | — | — |
| 记录内偏移 27–30（每记录 4 点，共 2000 点） | `ErrCRCMismatch` | — | — |

说明：段 = 26B 头 + 500×31B 记录 = 15526B；逐字节截断 15525 个点全部正确分类。
修复示例：截断于 26+300×31+10，段头 count 由 500 改正为 300（差 200），
修复后顺序读 300 条不再报错，索引同步重建。
