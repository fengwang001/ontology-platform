# 实验结论

## 表① 写入失败注入（批次 100 条，粒度 N=10）

| 失败位置 k | 进度停留点 | 续传重写条数 | 最终结果比对 |
|---|---|---|---|
| 第 1 条（k=1） | 0（未刷过任何区间） | 0 | 与无失败运行逐字节相同 |
| 中间（k=50） | 40（停在失败点前的最后 flush） | 9（<=N=10） | 与无失败运行逐字节相同 |
| 最后一条（k=100） | 90 | 9（<=N=10） | 与无失败运行逐字节相同 |

另：10 万条批次在 k=70000 注入失败，进度停在 69000，续传重写 999 条（<=N=1000），
内存峰值 1000（=N），验证粗粒度进度上界成立。

## 表② 进度文件截断分类（样例文件 43 字节）

样例：`P1 b1 30\n`(0-8) `I 0 10\n`(9-15) `I 10 20\n`(16-23) `I 20 30\n`(24-31)
`E b19e9ed8\n`(32-42)。截断点 m 取遍 1..42，逐字节由 `TestTruncateEveryByte` 验证。

| 分类 | 截断点 m 的字节区间 | 回退后的进度（frontier） |
|---|---|---|
| 头部不完整 ErrHeader | [1,8] | 0（空进度，从头开始） |
| 区间记录不完整 ErrInterval | [10,15]→0；[17,23]→10；[25,31]→20 | 最后一条完整区间的端点 |
| CRC 不匹配 ErrCRC | m=9→0；m=16→10；m=24→20；m=32 与 [33,42]→30 | 全部完整区间的端点 |

结论：三类截断均可判定、可区分；恢复结果恒为「最大可恢复前缀」，
续传后存储与不崩溃时逐字节相同（由 importer 测试的逐字节比对覆盖）。
