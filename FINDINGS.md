# 实测结论

## 逐包测试

- vec：欧氏距离/内积数值正确；维度不符、空向量、NaN、±Inf 均可判定拒绝。
- hyper：签名与「内积>0 置位、≤0 清位」逐位一致（零向量恒为 0）；
  同种子签名完全一致、不同种子有差异；哈希计数精确等于 N×L×b。

## 表① b×L 候选数与 top10 召回率

（待 search 测试后填入实测矩阵）

## 表② 截断点字节区间、分类与恢复桶数

（待 persist 测试后填入实测区间）
