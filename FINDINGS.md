# FINDINGS

## 表① 六行同排序值时各页行 ID 与复合键边界

数据集：10 行，Key = [1,1,2,2,2,2,2,2,3,3]，ID = r00..r09（r02..r07 共 6 行 Key=2.0），
页大小 3，正向翻页。边界 = 该页游标锚定的复合键（页尾行）。

| 页 | 行 ID | 页尾复合键边界（下一页严格大于它） |
| --- | --- | --- |
| 1 | r00, r01, r02 | (2.0, r02) |
| 2 | r03, r04, r05 | (2.0, r05) |
| 3 | r06, r07, r08 | (3.0, r08) |
| 4 | r09 | (3.0, r09) |

结论：6 行 Key=2.0 的行跨三页被 ID 顺序切开，10 行恰好各出现一次，
拼接顺序与一次性全量排序完全一致；反向从第 3 页 Prev 游标翻一页，
逐行等于第 2 页（非倒序）。

## 表② 游标逐比特翻转的分类统计

样本游标：`{Dir:Forward, Key:2.5, ID:"r0042"}`，编码长 21 字节，共 168 个单比特变体。

| 分类 | 变体数 | 来源 |
| --- | --- | --- |
| 校验失败 ErrChecksum | 152 | magic/version/Key/ID/CRC 字段的比特翻转 |
| 字段不完整 ErrTruncated | 8 | idLen 字节的 8 个比特翻转（总长必然不符） |
| 方向标记非法 ErrBadDirection | 8 | 方向字节的 8 个比特翻转（0xA5/0x5A 汉明距 8） |
| 被误接受 | 0 | —— |

结论：168 个变体全部被拒绝且分类确定，无一被当成合法游标接受。
另测得单次解码分配次数 ≤2（常数），与游标内容长度无关。
