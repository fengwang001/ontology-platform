# FINDINGS：测试结论

## 表 1：星形图（1 中心 + 10000 叶子）资源占用

来源：`go test ./walk/ -run TestStarPeakQueue -v`

| 预算 | 已访问数 | 峰值队列 | 上界 4*预算 | 判定 |
|-----:|--------:|--------:|----------:|:----:|
|   10 |      10 |      10 |        40 | OK |
|  100 |     100 |     100 |       400 | OK |
| 1000 |    1000 |    1000 |      4000 | OK |

结论：出边逐条考察、子节点逐个入队，峰值 = 预算（队首帧 + 已入队叶子），
一万条出边从未一次性入队；已访问数精确等于预算。

## 表 2：续点逐比特翻转分类统计

来源：`go test ./resume/ -run TestBitFlipClassification -v`，
对 37 字节合法续点（共 296 比特）逐位翻转，无一被误接受。

| 分类 | 变体数 | 说明 |
|------|-------:|------|
| 校验失败 ErrChecksum | 152 | 头部 19 字节（magic/flags/计数/游标/crc）被改 |
| 字段不完整 ErrIncomplete | 117 | idLen 错乱导致截断/剩余/结构矛盾 |
| 引用不存在节点 ErrUnknownNode | 27 | ID 字节被改，结构与校验完好但图中无此节点 |
| 误接受 | 0 | — |

注：测试图节点 ID 两两比特汉明距离 >= 2，故单比特翻转 ID 必然落到
「不存在的节点」而不会被误认为另一合法节点。

## 其他已验证结论

- 同一输入重复遍历 20 次，访问序列与续点逐字节相同（walk）。
- 预算 n 拆成 a 与 n-a 两段（a 从 1 到 n-1 全切分点），拼接序列与
  一次遍历逐元素相同，且末段续点与一次遍历的续点一致（audit）。
- 预算为零返回空序列且续点逐字节等于初始续点；已完成续点再续传
  返回空序列不报错（walk）。
- 两段续传之间删除仍在队列中的节点，续传报 ErrUnknownNode 并指名
  该节点（walk/resume）。
- 16 协程并发续传同一续点结果逐元素相同，go test -race 干净（walk）。
