# FINDINGS — 实测结论

测试命令：`go test -count=1 ./...`（另跑过 `-race`，均通过）。

## 表 1：权重 1:3:6，10 万次调度（等代价 c=1，TestFairness100k）

| 租户 | 权重占比（理论） | 实际代价占比 | 相对偏差 |
|------|------------------|--------------|----------|
| p    | 0.10000          | 0.10000      | 0.00000  |
| q    | 0.30000          | 0.30000      | 0.00000  |
| r    | 0.60000          | 0.60000      | 0.00000  |

逐租户相对偏差均为 0（< 5% 阈值）：min-vt 选择把各租户 vt 拉平，
C_i/w_i 相等即 C_i ∝ w_i，与 DESIGN.md 第 1 节推导一致。

## 表 2：空闲租户重入（Y 先独占执行 1000 步，X 再带 100 个任务重入）

| 实现                | X 连续执行步数 | X 首个任务等待步数 | 后续 200 步内 Y 被执行次数 |
|---------------------|----------------|--------------------|----------------------------|
| 抬升 vt 到 sysVT    | 1              | 0（并列按 ID 序）  | 100                        |
| 不抬升（vt 停在 0） | 100            | 0                  | 100（前 100 步被 X 独占）  |

不抬升时 X 的 vt 落后 sysVT 约 1000，按 N=(v1-v0)*w/c 可独占约 1000 步；
X 只有 100 个任务，故 100 步全部连续独占。抬升后 X 与 Y 交替执行，Y 不饿死。

## 其他实测要点

- 1:3（及 2:5）权重的执行次数比例收敛，相对误差 < 5%（TestConvergence）。
- 1000 租户单次选择比较次数实测约 30，上界 4*ceil(log2 1000)=40 成立；
  堆中元素数 == 非空租户数，空闲租户不占选择代价（TestComparisonBound）。
- 零/负代价任务被 MinCost=1 下界兜住，与等权租户近似交替，不独占
  （TestZeroCostNoMonopoly）。
- 权重 1e6 与 1e-6 并存时 vt 保持有限，float64 无溢出（TestExtremeWeights）。
- 背压：每租户队列上限只拒绝超限租户，其他租户入队不受影响；有排队任务
  时移除返回 ErrTenantBusy，空租户可移除并重新注册（TestBackpressureIsolation）。
- 四类错误 ErrInvalidWeight / ErrQueueFull / ErrTenantBusy / ErrUnknownTenant
  均可用 errors.Is 区分（TestErrors）。
- 并发：8 生产 8 消费、10 万任务，取出总数 == 提交总数且序号无重复，
  `-race` 干净（TestConcurrentExactlyOnce）。
- 确定性：同一任务流 20 次重放，调度序列逐元素一致（TestDeterminism）；
  并发提交的「提交顺序」以获取入队锁的串行化顺序为准。
