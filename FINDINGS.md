# 实测结论

## 表① 四种聚合的部分状态合并规则与结合律

| 聚合 | 部分状态合并规则 | 结合律 | 备注 |
|---|---|---|---|
| Count | Count += 对方 Count（整数加法） | 成立（精确） | 不幂等，重复合并会重复计数 |
| Sum | Sum += 对方 Sum（浮点加法） | 仅无舍入时成立；IEEE 754 一般不满足 | 不幂等；固定合并顺序保证确定性，测试用整数值逐位比对 |
| Min | Min = min(双方 Min) | 成立 | 交换、幂等，不受浮点舍入影响 |
| Max | Max = max(双方 Max) | 成立 | 交换、幂等，不受浮点舍入影响 |

结论：Min/Max 的合并是幂等的选取运算，Count/Sum 是不幂等的累加运算；因此「同一溢出片段恰好合并一次」对 Count/Sum 是硬约束。`acc` 包测试（TestMergeAssociative）验证了整数值下 (a⊕b)⊕c == a⊕(b⊕c) 对四种聚合同时成立，这是同一分区多次溢出仍能合并出正确结果的代数基础。

## 表② 分区文件截断点分类

文件布局：头 [0,20)；第 i 条记录起点 sᵢ：长度前缀 [sᵢ,sᵢ+4)，行体 [sᵢ+4,sᵢ+4+Lᵢ)，CRC [sᵢ+4+Lᵢ,sᵢ+8+Lᵢ)。

| 截断点字节区间 | 分类结论 | 中止位置（偏移） |
|---|---|---|
| [0, 20) | 头部不完整 ErrHeader | 已读到的字节数（0..19） |
| [sᵢ, sᵢ+4) | 长度前缀不完整 ErrLengthPrefix | sᵢ |
| [sᵢ+4, sᵢ+4+Lᵢ) | 行体不完整 ErrBody | sᵢ+4 |
| [sᵢ+4+Lᵢ, sᵢ+8+Lᵢ) | CRC 不匹配 ErrCRC | sᵢ+4+Lᵢ |
| 记录边界（含文件尾）但 recordCount 未到 | 长度前缀不完整 ErrLengthPrefix | 边界偏移 |

实测（400 行文件逐字节截断 1..len-1，共 8019 个截断点）：头部 19、长度前缀 1600、行体 4800、CRC 1600，全部与预期分类一致；每个错误均携带分区号与偏移（TestTruncateClassify）。

## 测试结论（按组追加）

- acc：Add/Merge/编解码往返全部通过；空状态合并为恒等元；±Inf 参与 Sum 与 Min/Max；结合律在整数值下对四种聚合成立。
- hashpart：分区函数稳定且在界内；段文件写读往返一致；400 行文件逐字节截断全部正确分类（见表②）；翻转行体一字节必报 ErrCRC；启动时清理半截 .tmp 且绝不读入；注入第 2 次写失败后 Cleanup 残留为 0。
- hashagg：5 万不同分组、预算 500 下驻留峰值 ≤500 且确实溢出；写出行数 == 读回行数；行处理次数 ≤ 2N；单分区被溢出 3 次（及多分区多次）结果与全内存聚合逐位相同；分组数==预算不溢出、==预算+1 恰好一次；零行/单行/同键/空键/NaN/±Inf/±0 语义全部正确；截断段文件让 Finish 中止并报分区号与偏移；写失败注入后目录残留为 0；回读完成顺序打乱 20 次输出逐字节一致；`-race` 干净。
- verify：全内存参考聚合与结果核对、逐字节一致性、损坏检测均按表驱动通过。
