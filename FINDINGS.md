# FINDINGS - 测试结论记录

## 表 1：四种聚合的部分状态合并规则与结合律

| 聚合 | 部分状态合并规则 | 幂等 | 结合律结论（实测） |
|---|---|---|---|
| Count | c1 + c2（整数累加） | 否 | 严格满足（整数精确），任意次序合并结果一致 |
| Sum | s1 + s2（浮点累加） | 否 | 整数值 float64 下严格满足（中间和可精确表示）；任意浮点仅近似成立，故位级比对测试用整数值 |
| Min | min(m1, m2)（取极值） | 是 | 严格满足，对任意合法输入（含 ±Inf）成立 |
| Max | max(m1, m2)（取极值） | 是 | 严格满足，对任意合法输入（含 ±Inf）成立 |

- 幺元：Count/Sum 为 0，Min 为 +Inf，Max 为 -Inf；Zero() 即合并幺元，测试已验证。
- 差异要点：Count/Sum 非幂等，同一片段重复合并会翻倍，故每段溢出记录只读回一次；Min/Max 幂等，重复合并无害。
- 已知边界：同组同时含 +Inf 与 -Inf 时 Sum 为 NaN（IEEE 语义），Min/Max 仍正确；输入为 NaN 的行在入表前被拒绝。

## 表 2：分区文件截断点分类（待 hashpart 测试后填写）
