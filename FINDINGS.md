# 测试结论

## 表一：四类状态分布（图：A→B→C 加并行慢任务 S，A 失败）

| 模式 | A | B | C | S（慢任务） |
|---|---|---|---|---|
| 快速失败 | （待测） | （待测） | （待测） | （待测） |
| 尽力而为 | （待测） | （待测） | （待测） | （待测） |

## 表二：故障注入结论

| 注入 | 结论 |
|---|---|
| 环检测 | 通过：迭代 DFS 在执行前检出环，返回路径逐边存在于输入且首尾闭合；自环 x→x 同样检出（graph.TestGraph） |
| 任务 panic | 通过：`exec.Run` recover 后包装为 `*fail.PanicError`，`errors.Is(err, ErrPanic)` 成立且消息含原始值 "boom"，调度器不崩溃（demo 判定；报告层见 sched 测试） |
| 多任务同时失败 | （待测） |
| 取消后写回 | （待测） |
