# 测试结论

测试以表驱动循环覆盖；场景：数据集 V={5,1,3}（缺一个，缺失上界=2）；
缺一半为 {5,1}+{2} 两成功、两缺失（缺失上界各=100）。

## 1. 五种聚合 × 三种失败程度（15 格）

| 聚合 | 全部成功 | 缺一个分片 | 缺一半分片 |
|---|---|---|---|
| Count | 精确 `exact Count=3` | 下界 `Count >= 3`，区间 [3,+∞] | 下界 `Count >= 3`，区间 [3,+∞] |
| Sum | 精确 `exact Sum=9` | 下界 `Sum >= 9`，区间 [9,+∞] | 下界 `Sum >= 8`，区间 [8,+∞] |
| Min | 精确 `exact Min=1` | 单边界 `Min <= 1`，区间 [-∞,1] | 单边界 `Min <= 1`，区间 [-∞,1] |
| Max | 精确 `exact Max=5` | 单边界 `Max >= 5`，区间 [5,+∞] | 单边界 `Max >= 5`，区间 [5,+∞] |
| TopK(K=2/3) | 精确 `exact TopK[2]`，前缀=K | 入选确定 `prefix=2=K`，排名可能变 | 无保证 `TopK guaranteed prefix=0` |

结论：Min 的方向是「真实值 ≤ 当前值」，Max/Count/Sum 是「≥」，方向相反，未写反；
缺失上界小 → TopK 前缀=K，缺失上界大 → 前缀=0；全成功时五种聚合均为「精确」。

## 2. 四种故障注入的检出与分片状态

| 故障 | 检出方式 | 分片状态 | 对合并的影响 |
|---|---|---|---|
| 超时（永不返回） | 整体 deadline 到期，ctx 取消 | `timeout` | 列入缺失清单，数据不并入，给部分结果 |
| 损坏（条数不符） | 去重后条数 != 分片 `Claimed()` | `corrupt` | 该分片整份丢弃，不并入坏数据 |
| 重复返回（重试叠加） | 按 `(ID,V)` 键去重 | `ok` | Count/Sum 与只返回一次逐位相同 |
| 全部失败 | 成功分片数=0 | 各自 `timeout`/`error`/`corrupt` | `Run` 返回 `ErrAllFailed`，非零值精确 |

另：零分片返回 `ErrNoShards`；所有分片成功但无数据返回 Count=0 且标精确，
与「全部失败」可 `errors.Is` 区分。

## 3. 资源与确定性结论

- 200 分片、上限 8：历史峰值在 (0,8]（非导出计数器，`PeakInFlight()` 读取）。
- deadline 后不再发起：20 个 hang 分片、上限 3 时实际发起数恰为 3；
  `Run` 在 deadline+250ms 窗口内返回，在飞请求随 ctx 取消。
- 20 种打乱到达顺序下 `report.String()` 逐字节相同；TopK 并列按 ID 升序。
- `go test -race` 干净。
