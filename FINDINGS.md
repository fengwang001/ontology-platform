# 测试发现

## 聚合可信度（十五格）

| 聚合 | 全部成功 | 缺一个分片 | 缺一半分片 |
|---|---|---|---|
| Count | 精确 | 部分结果：`>= n` | 部分结果：`>= n` |
| Sum | 精确 | 部分结果：`>= v` | 部分结果：`>= v` |
| Min | 精确 | 单边界：`true <= m` | 单边界：`true <= m` |
| Max | 精确 | 单边界：`true >= m` | 单边界：`true >= m` |
| TopK | 精确 | 前缀 p，缺失上界决定可信度 | 前缀 p，缺失上界决定可信度 |

## 故障注入

| 故障 | 检出方式 | 分片状态 | 测试结论 |
|---|---|---|---|
| 超时 | `errors.Is(ErrTimeout)` / ctx 截止 | `timeout` | 已验证假分片在截止时返回 |
| 损坏 | `Declared != len(Records)` | `corrupt` | 已验证故障可配置并可检出 |
| 重复返回 | 同分片 ID 已有首个成功帧 | `ok`（合并层去重） | 已验证假分片返回两帧 |
| 全部失败 | `errors.Is(ErrAllShardsFailed)` | `failed/timeout/...` | 待扇出与报告测试 |

## 资源约束

| 检查 | 结果 |
|---|---|
| 200 分片、上限 8 | 历史峰值不超过 8 |
| 启动前整体截止 | 新发起数为 0，`Run` 立即返回 |
| 损坏 / 零分片 | 分别得到 `corrupt` / `ErrNoShards` |
