# FINDINGS — 测试结论记录

## 1. 错误分类与时限（classify / timeout）

- 三分类表驱动测试全部通过：普通错误→可重试；包装 `ErrNonRetryable`→不可
  重试；包装 `ErrTimeout` / `context.DeadlineExceeded`→超时。
- `Trips()` 口径：可重试与超时计入熔断，不可重试不计入。
- `timeout.Do`：20ms 时限下，阻塞调用按时返回 `ErrTimeout`（`errors.Is` 可
  判定，分类为超时）；fn 内 panic 被 recover 转为普通错误，包装器未击穿。

## 2. 熔断状态机迁移表

| 迁移 | 触发条件 | 测试用例 | 观察到的冷却时长序列 |
|---|---|---|---|
| （待 breaker 测试后填写） | | | |

## 3. 5 万次随机调用统计等式实测

（待 stat 测试后填写）

## 4. 舱壁隔离（bulkhead）

- 峰值：500 协程、N=8 时在途峰值恰好为 8，恒不超过额度。
- 队列：N=1、Q=1 时第 3 个请求立即返回 `ErrFull`（1s 守卫内返回，注入时钟
  读数不变）；等待者被 ctx 取消后队列名额归还，后续等待者可入队。
- 额度归还：成功 / 失败 / 超时 / panic 四路径各 1000 次后可用额度回到满值 8。
- 隔离：X（N=2）被占满后自身调用被拒，Y 的 100 次调用全部成功。
- 边界：N=1 时 100 并发最大在途为 1（串行）；`New` 对 n<1、q<0 报错。
- 修复记录：`Release` 曾先收通道再减计数，峰值计数瞬时虚高（观察到 14>8）；
  改为先减计数再收通道后，计数恒不超过额度。
