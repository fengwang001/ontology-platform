# 测试结论

数据集（六行 Score=1）：
`(0,z0) (1,a) (1,b) (1,c) (1,d) (1,e) (1,f) (2,q) (2,r) (3,x)`，
每页 3 行。游标记录上一页最后一行的复合键，下一页取严格复合大于它。

## 表① 各页行 ID 与复合键边界

| 页 | 行 ID（页内正向序） | 页末复合键（下一页边界，严格大于） |
|----|--------------------|------------------------------------|
| 1  | z0, a, b           | (1, b)                             |
| 2  | c, d, e            | (1, e)                             |
| 3  | f, q, r            | (2, r)                             |
| 4  | x                  | (3, x)，其后无行，结束             |

结论：Score=1 的 a..f 跨第 1–3 页，靠 ID 部分严格切分，六行恰好
各出现一次；10 行总访问顺序与一次性全量排序完全一致，不重不漏。

## 表② 游标逐比特翻转分类统计

合法游标长度 21 字节（id="id-ab"），每个方向 168 个变体，两方向
合计 336 个，循环逐比特翻转，无一被当成合法游标接受。

| 分类              | 变体数 | 误接受数 |
|-------------------|-------:|---------:|
| ErrChecksum       |    272 |        0 |
| ErrCursorMalformed|     48 |        0 |
| ErrBadDirection   |     16 |        0 |
| 合计              |    336 |        0 |

细分（单方向 168 个）：版本字节 8 → malformed；方向字节 8 →
badDirection；idLen 两字节 16 → malformed；其余 136 → checksum。

其余结论：正向第 3 页的 `Prev` 反向取页逐行复现第 2 页（c,d,e），
非倒序；一万行每页 20 行，二分定位比较 14 次，远低于上界
4·(20+14)=136；单次解码 1 次分配（常数）；`go test -race` 下
同游标 16 协程并发翻页结果逐行相同，增删协程无数据竞争。
