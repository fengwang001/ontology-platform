# FINDINGS

## 表 1：五个聚合器的增量能力核对结论

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 | 依据 |
|---|---|---|---|---|
| Count | 是 | 是 | 否 | S-1 与成员无关 |
| Sum | 是 | 是 | 否 | 精确和可逆减（big.Float 4096 位） |
| Min | 是 | 否 | 是 | 删到极值时标量状态无次小值信息 |
| Max | 是 | 否 | 是 | 与 Min 对偶 |
| DistinctCount | 是 | 否 | 是 | 需 value 引用计数判断是否仍有持有者 |

测试核对：`agg.decl` 判定通过；重算计数结论见文末「重算与审计」。

## 表 2：截断点分类（200 条变更的日志，总长 8808 字节）

记录布局：头 8 字节；每条 44 字节 = len(4)+body(36)+crc(4)，
body = 22 固定字节 + 组键 6 字节 + id 8 字节。第 i 条记录起始 s(i)=8+44i。

| 截断点字节区间 | 分类 | 重放后生效记录数 |
|---|---|---|
| [1, 7] | 头部不完整 | 0 |
| [s(i)+1, s(i)+3] | 长度前缀不完整 | i |
| [s(i)+4, s(i)+39] | 记录体不完整 | i |
| [s(i)+40, s(i)+43] | CRC 不匹配 | i |
| c = s(i)（干净前缀） | 无错误 | i |

测试核对：`journal.TestTruncation` 逐字节遍历 1..8807 全部截断点，
分类与生效记录数逐点断言通过；四类分类均被实际触发。
