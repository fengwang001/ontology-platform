Findings（随每组测试完成追加）

event 帧编解码：空/单条/大序号/二进制载荷往返一致；三类帧错误
(ErrShortLength / ErrShortFrame / ErrCRC) 均可由 errors.Is 区分。

表1 三种 from 位置（N=128，10 万事件）

| from 位置 | 跳过事件数 | 用上索引 |
| --- | --- | --- |
| 等于锚点（from=256，锚点 0,128,256...） | 0 | 是（锚点即起点） |
| 锚点之间（from=300，锚点 256） | 44（<N=128） | 是（二分定位 256） |
| 小于首锚点（from=5 / from=0） | 5 / 0 | 否（无锚点<=from，段首扫描） |

重建一致性：删 idx 后由段重建的索引与原索引 Marshal 字节相同
（N=8/128/1 多组断言通过）。

表2 500 事件段逐字节截断分类与 repair

| 字节区间（500 事件，H=32，帧首 32） | 分类 | repair 前条数 | repair 后条数 |
| --- | --- | --- | --- |
| [1,31] | ErrShortHeader 头部不完整 | 500(标称) | 0（无法读头） |
| [33,35]（每帧前 4B 的长度前缀内，遍历实测至 8520） | ErrShortLength | 500 | 已读条数 |
| [36,48]（seq/载荷/CRC 内，遍历实测至 8532） | ErrShortFrame | 500 | 已读条数 |
| 帧边界精确截断（500 处） | 无错误，已是干净前缀 | k | k（无需改） |
| 翻转一载荷字节（完整长度） | ErrCRC | 500 | 最大前缀 |

实测遍历 1..len-1 全部截断点均落入上述类别，无未分类；
repair 前后条数差值在 repair 包测试中断言（见后续追加）。
