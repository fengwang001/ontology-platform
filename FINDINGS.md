# FINDINGS — 实测结论

## 0. 可复现性（hyper）

- 同种子两次生成同维同位数的超平面族，4 个测试向量的签名逐一相等；异种子签名不同。测试：`hyper/hyper_test.go`。
- 哈希计算计数器精确等于 `向量数 × 位数`（单表），多表合计 `N × L × b`，不多不少。
- 零向量签名恒为全 1（内积恰为 0 归入正半空间），b=1/8/64 均验证。

## 1. 候选数与 top10 召回率实测矩阵

（数据：5000 向量 / 50 簇 / dims=32 / 100 查询，种子固定；候选数为每查询去重后的平均。）

每格为 `候选数 / 召回率`：

| b \ L | 1 | 2 | 4 | 8 |
|---------|----------------|----------------|----------------|----------------|
| 4  | 354.3 / 0.756 | 672.9 / 0.955 | 1246.8 / 0.997 | 2236.6 / 1.000 |
| 8  | 77.2 / 0.637  | 125.1 / 0.824 | 202.3 / 0.975  | 322.2 / 1.000  |
| 12 | 47.4 / 0.527  | 69.7 / 0.727  | 97.2 / 0.935   | 115.9 / 0.997  |

- 召回率：b=8、L=8 时 top10 召回 = 1.000 ≥ 0.6；每个 b 行内召回随 L 单调不降（表嵌套 ⇒ 候选集嵌套 ⇒ 确定性成立）。
- 候选数：每个 L 列内随 b 严格下降（如 L=8：2236.6 → 322.2 → 115.9），与 `N·2^-b·L` 的推导一致。
- 计数器：建索引哈希计算恰为 `5000×8×8=320000`；精排距离计算平均每查询 322.2 次 ≤ 500（10%·N）。
- 边界：空索引、单向量、K>N、全同向量（候选=全体、召回=1）、NaN/±Inf 拒绝并计数、零向量合法（签名全 1）、dims=1、维度不符报 `vec.ErrDim`，均通过。
- 并发：4 写 4 读并发 1000 次 Add / 2000 次 Query，`-race` 干净，无悬挂 ID。

## 2. 索引文件截断分类

测试配置：dims=4、bits=3、tables=2、count=6，文件全长 368 字节
（头 20B + 超平面 192B + 桶表 152B + CRC 4B）。对 1..367 全部截断点逐一断言分类：

| 截断点（字节） | 分类（`errors.Is`） | Recover 恢复桶数 |
|---|---|---|
| 1–19 | `ErrHeader` 头部不完整 | 不可恢复（报错） |
| 20–211 | `ErrHyper` 超平面参数不完整 | 不可恢复（报错） |
| 212–363 | `ErrBuckets` 桶表不完整 | 最大完整前缀，0–8 桶随截点递增 |
| 364–367 | `ErrCRC` CRC 缺失/不匹配 | 全部 8 桶 |

- 恢复自洽性：全部截断点下 `Recover` 结果中桶表引用的 ID 均 < count，无悬挂 ID。
- 悬挂剔除：把头里 count 改成 2 后 `Recover` 剔除全部 id≥2 的引用并计数（Dropped>0），剩余引用全部合法。
- 退化超平面：把 table 1 bit 2 的法向量清零，`Load` 报 `ErrDegenerate` 且消息含 `table 1 bit 2`。
- CRC 损坏：桶表区翻一字节，`Load` 报 `ErrCRC`；magic 翻一字节，报 `ErrHeader`。
