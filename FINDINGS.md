# FINDINGS — 测试结论

## ① 三种 from 位置的定位表现（锚点间隔 N）

| from 位置 | 起始扫描点 | 跳过事件数 | 用上索引 |
|---|---|---|---|
| 恰好等于锚点序号 | 该锚点 offset | 0 | 是 |
| 两锚点之间（距前锚点 d） | 前驱锚点 offset | d（恒 < N） | 是 |
| 小于首锚点（< 段首序号） | 段事件区起点（offset 28） | clamp 后为 0（语义：从头回放） | 否（无锚点可用） |

实测（`replay` 测试，N=4，三段各 16 事件）：from=16 跳过 0；from=18 跳过 2；from=0（小于最小序号）clamp 到头跳过 0。
10 万事件、N=128、200 个随机 from：跳过数全部严格 < 128，且结果与全量扫描一致。
字节上界实测（N=128，载荷 2 字节）：回放 10 条读取 296 字节 ≤ 10×10 + 128×20 = 2660。

## ② 截断点分类与 repair 效果（500 事件段，记录定长 20 字节，总长 10028）

| 截断点字节区间 | 分类结论 | 点数 | repair 前条数 | repair 后条数 |
|---|---|---|---|---|
| [1, 27] | 头部不完整 | 27 | —（不可修，头缺失） | — |
| 记录内偏移 0–3（每记录 4 点） | 长度前缀不完整 | 2000 | 500（头声明） | 截断前完整记录数 |
| 记录内偏移 4–19（每记录 16 点） | 事件体不完整 | 8000 | 500（头声明） | 截断前完整记录数 |
| 位翻转（非截断）注入事件体或 CRC 字段 | CRC 不匹配 | 2 例 | — | — |

逐字节遍历 1..10026 全部 10027 个截断点：每个都被分入上表三类之一，无未分类错误；截断不会产生 CRC 不匹配（字节缺失而非错误），CRC 类由位翻转覆盖。
修复实例：截断于 28+300×20+7，repair 前头声明 500 条，修复后 300 条，差值 200；修复后顺序重读无错误。

## 其他结论

- 删索引后由段文件重建，索引文件逐字节相同（`sparse`/`repair` 测试均断言）。
- 索引锚点偏移篡改为指向事件中部：锚点验证失败，自动回退全段扫描，结果正确且报告 `IndexInvalid=true`。
- 相邻段序号缺 10、11 时 `CheckGaps` 报 `ErrSeqGap` 并给出区间 [10,11]。
- 并发追加 + 4 路持续回放：每次回放均为连续无缺口前缀，`-race` 干净。
