# 测试结论记录

## 0. 滚动校验和（chunk 组）

- 在 n∈{1000,1024,999,64,65}、块大小 ∈{4,16,7,64} 的随机数据上，逐偏移断言「滚动推进 == 从头重算」，全部相等。
- 基本运算次数实测上界为 `块大小 + 2*(n-块大小)`，恒 ≤ 4n。
- 弱碰撞对 `{1,2,3,4}` 与 `{4,3,2,1}`：弱校验和同为 10，内容不同，SHA-256 不同——碰撞可构造且强校验可区分。

## 1. 六种源/目标关系下的补丁构成

夹具：块大小 8，基准数据 40 字节（5 块），固定随机种子，数字为实测（`TestRelations` 断言）。

| 关系 | 新数据字节数 | 复用块数 |
| --- | --- | --- |
| 相同 | 0 | 5 |
| 中间差一块 | 8（=1 块，≤2 倍块大小） | 4 |
| 目标为源的前缀 | 16（源多出的 2 块） | 3 |
| 源为目标的前缀 | 0 | 3 |
| 完全不同 | 40（整份） | 0 |
| 空（源为空） | 0 | 0 |

补充：目标为空时新数据 40、复用 0；两者皆空时均为 0。

其他 diff 组结论：弱碰撞块未被复用、结果与源一致；强校验次数 == 弱匹配次数；同一对数据 20 次 diff 补丁逐字节相同；8 协程并发 diff+apply 全部往返正确且 `-race` 干净。

## 2. 截断点分类（patch 组）

夹具：「中间差一块」补丁共 89 字节 = 头 52 + 指令区 5×5=25 + 数据段 8 + CRC 4。
测试从长度 1 遍历到 88，每个截断点都被分类且目标数据逐字节不变。

| 截断长度区间（字节） | 分类 | 目标端是否保持不变 |
| --- | --- | --- |
| 1–51 | ErrTruncatedHeader | 是 |
| 52–76 | ErrTruncatedInstr | 是 |
| 77–84 | ErrTruncatedData | 是 |
| 85–88 | ErrCRCMismatch | 是 |

其他 patch 组结论：复用块号越界（刚好越界/远超/负值）均报 `ErrBlockOutOfRange` 且不 panic、目标不变；签名生成后目标数据被改动时，`verify.Final` 报 `ErrChecksumMismatch`，不会产出错误数据。
