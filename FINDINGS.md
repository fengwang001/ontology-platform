# FINDINGS — 测试结论记录

## 表 ② 进度文件截断分类（先于表 ① 完成）

测试文件：批次 `b`，5 个区间行（`I 0 2` … `I 8 10`），全长 92 字节，
头部 `PGR1 b <crc>\n` 占 [0,15]，区间行依次起于 16/31/46/61/76。
对全部 91 个截断点（cut=1..91，保留前 cut 字节）逐一分类：

| 截断点区间 | 字节含义 | 分类 | 回退后进度 |
|---|---|---|---|
| 1–15 | 头部行内（无换行） | ErrHeaderIncomplete | 空进度（高水位 0） |
| 17–21, 32–36, 47–51, 62–66, 77–82 | 区间行 `I s e` 前缀内 | ErrIntervalIncomplete | 该行之前的完整区间 |
| 22–29, 37–44, 52–59, 67–74, 83–90 | 区间行 CRC 数字内（含空 CRC） | ErrCRCMismatch | 该行之前的完整区间 |
| 16, 30–31, 45–46, 60–61, 75–76, 91 | 行边界或完整行（缺末尾换行） | ClassOK | 区间数减少的正常回退 |

计数：OK=10，头部不完整=15，区间不完整=26，CRC 不匹配=40，合计 91。
全部截断点恢复结果均为原区间列表的前缀，高水位不回退到负值、不超过原值；
Open 会把截断文件物理重写为最大可恢复前缀，续传结果与不崩溃一致
（续传一致性见 importer 测试 TestImporter/truncated_progress_resume）。

## 表 ① 写入失败位置（n=10，粒度 N=3，故障注入第 k 次 Put）

| 失败位置 k | 进度停留点（已刷区间高水位） | 续传重写条数 | 最终结果比对 |
|---|---|---|---|
| 1（首条） | 0（无任何区间落盘） | 0 | 与无失败运行逐字节相同 |
| 5（中间） | 3（仅区间 [0,3) 落盘） | 1（区间 [3,4) 幂等重放） | 与无失败运行逐字节相同 |
| 10（末条） | 9（区间 [0,9) 落盘） | 0 | 与无失败运行逐字节相同 |

补充：10 万条在第 7 万条中断（N=1000），进度停在 69000，存储实际 69999 条，
续传重写 999 条（≤ N），工作缓冲峰值 1000（≤ N），未从头重扫；
进度超前于存储（50000 vs 30000）时以存储为准从 30000 续传，终态逐字节相同。
