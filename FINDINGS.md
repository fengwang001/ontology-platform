# FINDINGS — 实验记录与结论

## ① 递归栈 A→F→G→F→H（该栈采样 1 次）节点 self/total 与 F 函数级 total

| 节点（按路径） | self | total |
| --- | ---: | ---: |
| A | 0 | 1 |
| A→F（最外层 F） | 0 | 1 |
| A→F→G | 0 | 1 |
| A→F→G→F（内层 F） | 0 | 1 |
| A→F→G→F→H | 1 | 1 |

结论：两个 F 节点的 total 相加会得到 2，但内层 F 的采样集合是外层的子集，
重复计入。函数级 total 只取「祖先链无同名 F」的最外层节点：**FuncTotal(F) = 1**
（采样 n 次时为 n，demo 中 n=7 断言为 7，绝不为 14）。FuncSelf(F) = 0；
H 的 self=1，全树 Σself = 1 = 样本数。

## ② 落盘文件逐截断点的分类与恢复（样本：m→A→{BB×2,C}、m→D；文件共 156 字节）

头部固定 36 字节；5 条节点记录依次为
m(23)、A(23)、BB(24)、C(23)、D(23)；CRC 尾 4 字节，边界偏移
36→59→82→106→129→152→156。

| 截断后长度（字节） | 分类 | 可完整恢复的记录 | 恢复节点数 |
| --- | --- | --- | ---: |
| 1 – 35 | 头部不完整 ErrHeader | 无（头部无效则不恢复） | 0 |
| 36 – 58 | 节点记录不完整 ErrRecord | 无 | 0 |
| 59 – 81 | 节点记录不完整 ErrRecord | m | 1 |
| 82 – 105 | 节点记录不完整 ErrRecord | m, A | 2 |
| 106 – 128 | 节点记录不完整 ErrRecord | m, A, BB | 3 |
| 129 – 151 | 节点记录不完整 ErrRecord | m, A, BB, C | 4 |
| 152 – 155 | CRC 缺失/不匹配 ErrCRC | m, A, BB, C, D | 5 |

测试以循环逐字节（n=1…155）断言上述分类（errors.Is）与节点数；每个截断点恢复出的树
均满足 Σself == 已恢复节点样本数（如 129–151 时为 3），且按前序深度检查保证不出现
父节点缺失而子节点存在。完整文件 156 字节 Decode 成功，Σself=4，Dropped=137。
