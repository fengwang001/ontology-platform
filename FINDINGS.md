# 测试结论

## 表 1：六行同排序值时的分页（10 行，每页 3 行，正向）

数据集：d0..d5 的 score 均为 1.0，d6..d9 的 score 为 6.0..9.0。

| 页 | 行 ID | 页末复合键（续翻游标） | 本页行数 |
|----|-------|------------------------|----------|
| 1 | d0,d1,d2 | (1.0, d2) | 3 |
| 2 | d3,d4,d5 | (1.0, d5) | 3 |
| 3 | d6,d7,d8 | (8.0, d8) | 3 |
| 4 | d9 | (9.0, d9) | 1 |

结论：十行恰好各出现一次，顺序与一次性全量排序一致；第 1→2 页边界的
score 相同（都是 1.0），仅靠复合键中的 ID 分量区分，证明只记 score 的
游标在此处必然漏掉 d3..d5（`>`）或重复 d0..d2（`>=`）。

## 表 2：游标逐比特翻转的分类统计

合法游标 20 字节（方向 1 + score 8 + 长度 2 + id 5 + CRC 4），共 160 个
单比特翻转变体，全部被拒绝，无一被误接受：

| 分类 | 变体数 | 触发位置 |
|------|--------|----------|
| 字段不完整（ErrTruncated） | 16 | id 长度字段的 2 字节 |
| 方向标记非法（ErrDirection） | 8 | 方向字节的 8 个比特 |
| 校验失败（ErrChecksum） | 136 | score、id、CRC 各字节 |
| 被误接受 | 0 | — |

单比特翻转不改变字节串长度，故长度字段被翻转时解码出的 id 长度与实际
不符，落入「字段不完整」；方向字节 0xC1/0xC2 相距 2 比特，任何单比特
翻转都落到非法值；其余位置一律被 CRC32 拦下。

## 其他结论

- 反向翻页：正向翻到第 3 页后用该页的反向游标翻一页，得到 d3,d4,d5，
  与正向第 2 页逐行相同（非倒序）。
- 并发增删：翻到第 2 页后插入 a1,a2（已翻过区间）、b1,b2（未翻过区间）
  并删除 k4，后续页为 k5,k6,b1,k7,b2,k8,k9：已返回行无重复，b1,b2 按
  复合键顺序出现，k4 不出现。
- 比较上界：10000 行、每页 20 行，单次翻页比较 33 次，上界
  4*(20+ceil(log2 10000))=136，远未触及；绝不从头全扫。
- 解码分配：id 长 1/8/60 的游标，单次解码分配均 <= 1 次，与输入无关。
