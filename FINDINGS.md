# 测试结论记录

## 表一：A→B→C 加并行慢任务 S 的状态分布

（待 sched 测试完成后补充）

## 表二：故障注入结论

（待 sched 测试完成后补充）

## 分阶段结论

- graph 阶段：环检测对普通环与自环均返回首尾闭合、逐边可在输入图中验证的
  真实环路径；重复加边幂等（Edges 计数不增）；未知节点加边返回
  `ErrUnknownNode`（可 `errors.Is`）；1000 层链迭代拓扑无栈溢出。
