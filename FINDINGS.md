# FINDINGS

## ① 三种 from 位置（N=128，锚点在 128/256/…，段首 base=0）

| from 位置 | 例子 from | 命中锚点 seq | 定位跳过事件数 | 用上索引 |
|---|---|---|---|---|
| 恰好等于锚点 | 128 | 128 | 0 | 是 |
| 两锚点之间 | 200 | 128 | 72（<128） | 是 |
| 小于首锚点 | 50 | 无 | 50（<128） | 否，段首前缀扫描 |

另：from 大于末锚点（如 999）回退到末锚点 256；跳过数同样严格 < N。

## ② 500 事件段逐字节截断分类（载荷 10B，帧长 18B，头 32B）

| 截断字节区间 cut | 分类 | repair 前条数 | repair 后条数 |
|---|---|---|---|
| 1..31 | ErrHeaderTruncated 头部不完整 | 0（不可打开） | 0，隔离 |
| 第 k 帧起点 +1..+3（k=0..499） | ErrLengthTruncated 长度前缀不完整 | 500（头声明） | k |
| 第 k 帧起点 +4..+17 | ErrBodyTruncated 事件体/CRC 不完整 | 500（头声明） | k |

差值 = 500-k，逐 cut 循环断言；修复后顺序读到 io.EOF 不报错。

## ③ CRC 不匹配

纯尾部截断不会产生 CRC 不匹配（完整帧的 CRC 必正确）；CRC 类用翻转一帧内字节注入，
解帧返回 ErrCRCMismatch，`errors.Is` 可判。repair 同样以最大可恢复前缀截断恢复。
