# 测试结论

## 表① 50 条链 / 1000 条记录（每链 20 条，乱序；链 7 含 1 条 error）

| 采样率 | 保留链数 | 保留记录 | 丢弃记录 | 链不完整标记 | 链完整性 |
|---|---|---|---|---|---|
| 0   | 0/50 | 1（仅 1 条 error 强制保留） | 999 | 1（该 error） | 每链同留同弃 |
| 0.5 | 28/50 | 561（=28×20+1，含链7的强制 error） | 439 | 1（链7被采样丢弃，其 error 被强制保留） | 每链同留同弃 |
| 1   | 50/50 | 1000 | 0 | 0 | 全部保留 |

结论：同一 TraceID 的记录在乱序下链决策全一致，重复 20 次决策相同；标记仅出现在
「链被丢弃 + error 及以上被强制保留」的唯一一条记录上；率 0 时普通记录全丢而 error
仍保留，率 1 时无标记。单次决策哈希计数恰为 1。

## 表② 逐字节截断分类（4 条记录，文件共 242 字节；header 1..8）

帧布局：`[8,65)`、`[65,121)`、`[121,177)`、`[177,234)`，结束标记起始于 234。

| 字节区间（截断点 cut = 保留前 cut 字节） | 分类 | 可恢复记录数 |
|---|---|---|
| 1–7 | 头部不完整（`ErrHeader`） | 0 |
| 8–11 / 65–68 / 121–124 / 177–180 / 238–241 | 长度前缀不完整（`ErrLengthPrefix`，含帧边界与结束标记截断） | 0 / 1 / 2 / 3 / 4 |
| 12–64 / 69–120 / 125–176 / 181–233 | 记录体不完整（`ErrBodyTruncated`，含 CRC 尾部被切） | 0 / 1 / 2 / 3 |
| 帧体或 CRC 字节翻转（帧完整但校验不符） | CRC 不匹配（`ErrCRC`） | 0（停在坏帧） |

结论：每个截断点都被 `errors.Is` 归入四类之一；恢复记录数恒等于「结束位置 ≤ cut」
的完整帧数量（最大可恢复前缀）。帧边界处因 8 字节结束标记缺失而报长度前缀不完整，
从而保证 1..len-1 全部截断点均有明确分类。
