# FINDINGS — 测试结论实录

环境：go1.26.5 linux/arm64，`go test -count=1 ./...` 与 `-race` 全部通过。

## 测试组结论（按包追加）

### record
- 编解码往返：空记录、深度 1、嵌套映射、数组内嵌套映射、空串值、空 trace ID 六类均逐字节一致。
- 深度：嵌套恰为 32 层通过，33 层及以上返回 `ErrDepth`（可 `errors.Is` 判定），无栈溢出。
- 循环：map 自引用、map 间接互引、slice 自引用三类均返回 `ErrCycle`，且不会被误分类为 `ErrDepth`。
- 深拷贝：修改克隆体不影响原记录。

### rule + mask
- 冲突：同路径 replace+hash 在编译期报 `ErrConflict`，错误文本同时指出 rule #0 与 rule #1；
  同路径同动作去重通过；`"a.b"` 与 `a.b` 是不同路径，不冲突。
- 路径语义：规则 `a.b` 只命中嵌套 `a→b`、不命中字面字段 `a.b`；规则 `"a.b"` 反之；
  中间层为字符串、路径不存在时不匹配、不报错、不 panic。
- 三种方式不变量：替换为固定串（不保留长度）；哈希同值同结果、异值异结果、带 `sha256:` 前缀且不等于原值；
  截断按 rune 保留前 N 字符并追加 `…(truncated)`，短串与空串原样。
- 彻底性：值 `SECRET-1` 出现在嵌套映射、数组元素、顶层三处，被同一条值模式规则全部哈希，三处结果一致。
- 非目标字段：脱敏后非目标字段（含数字、数组其余元素）逐字节不变，输入记录本身未被改动。
- 复杂度：100 条规则 × 10 万条记录（每条 5 个字符串字段），路径匹配计数精确等于 500000 = 记录数 × 字段数，
  上界 记录数 × (字段数+1) 成立，与规则数无关。

## 表 ① 采样留存统计（50 链 × 20 条 = 1000 条，FNV-1a 确定性决策）

（待 sample 包测试完成后填写）

## 表 ② 截断点分类（逐字节 1..len-1）

（待 sink 包测试完成后填写）
