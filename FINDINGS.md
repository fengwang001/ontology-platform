# 实测结论

## sched 组（go test ./sched）

- 权重 1:3 与 1:3:6 等代价任务流，长期执行次数比例收敛到权重比，相对偏差 < 5%。
- 权重 1e6 与 1e-6 并存：100 步内 hi=99、lo=1，vt 保持有限（float64 无溢出）。
- 同 vt 并列按租户 ID 字典序，序列可复现；20 次重放逐元素一致。
- 1000 个活跃租户单次选择比较次数实测 ≤ 26，上界 4*ceil(log2 1000)=40；空闲租户不进堆。
- 零代价任务按 MinCost=1 计费，X 最大连续执行 1 步，不独占。

## 表 1：权重 1:3:6，10 万次调度的代价占比（待 stat 组测试后填入）

| 租户 | 权重占比 | 实际代价占比 | 相对偏差 |
| --- | --- | --- | --- |
| A | 10% | 待填 | 待填 |
| B | 30% | 待填 | 待填 |
| C | 60% | 待填 | 待填 |

## 表 2：空闲租户重新加入（X 空闲期间 Y 执行 1000 步，之后 X、Y 各排 1500 任务）

| 实现 | X 最大连续执行步数 | Y 首次被调度的步 |
| --- | --- | --- |
| vt 抬升到系统虚拟时间 | 2 | 2 |
| 不抬升（vt 停在旧值） | 1001 | 1001 |

不抬升时 X 独占约 1000 步，等于它空闲期间系统虚拟时间的推进量，与 DESIGN.md 第 2 节的推导一致。
