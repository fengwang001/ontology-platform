# FINDINGS

## 测试结论记录（按包）

- `row`：复合键 `(Score,ID)` 严格全序；`-0` 按 IEEE-754 位序排在 `+0` 之前，NaN/空 ID 被 `ErrInvalid` 拒绝。
- `cursor`：合法游标往返解码一致；截断归 `ErrIncomplete`；跨方向归 `ErrMismatch`；解码分配次数为有界常数（短/长游标均 ≤4，与长度无关）。
- `page`：见下方同值分页边界表（测试后回填）。
- `audit`：全量遍历自检（测试后回填）。

## ① 六行同排序值：每页行 ID 与复合键边界（每页 3 行，共 10 行）

待 `page` 包测试后回填。

## ② 游标比特翻转分类统计

样本游标：Forward，Last=(2.5,"node-9")，First=(2.5,"node-1")，编码长度 38 字节 = 304 比特；逐比特翻转（循环覆盖每一位）：

| 分类 | 变体数 | 被误接受 |
| --- | --- | --- |
| 校验失败 ErrChecksum | 256 | 无 |
| 字段不完整 ErrIncomplete（版本位 8 + 两个长度字段各 16） | 40 | 无 |
| 方向标记非法 ErrDirection（方向字节 8 位） | 8 | 无 |
| 合计 | 304 | 0 |
