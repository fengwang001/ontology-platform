# 实验结论

## 表① 五个聚合器撤回能力核对

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
|---|---|---|---|
| Count | 是 (+1) | 是 (-1) | 否 |
| Sum | 是 (+v) | 是 (-v) | 否 |
| Min | 是 | 仅当 v>当前Min；删到极值须重算 | 是（极值计数归零时） |
| Max | 是 | 仅当 v<当前Max；删到极值须重算 | 是（极值计数归零时） |
| DistinctCount | 是 | 否（基数不含持有信息） | 是（每次删除） |

对应测试：`agg/agg_test.go` 表驱动覆盖重复极值不重算、删唯一极值重算、删非极值不动。

## 表② Journal 截断点分类（200 条，头 8 字节，帧 44 字节）

每帧布局：偏移 0..3 长度前缀、4..39 记录体（36B）、40..43 CRC。
cut 为保留的前导字节数；下表按帧内偏移 `(cut-8) mod 44` 归类。

| 截断字节位置（cut） | 分类 | 重放后生效记录数 |
|---|---|---|
| 1..7（含空文件 0 同理） | 头部不完整 | 0 |
| 8+44k+1 .. 8+44k+3（k=0..199） | 长度前缀不完整 | k（不完整帧不生效） |
| 8+44k+4 .. 8+44k+39 | 记录体不完整 | k |
| 8+44k+40 .. 8+44k+43 | CRC 不匹配（CRC 尾部截断） | k |
| 8+44k（完整帧边界） | 无错误 | k |

另：长度/体完整但 CRC 字节被翻转 → CRC 不匹配，当前帧及其后均不生效。
对应测试：`journal/journal_test.go` 用单循环遍历 cut=1..8807 全部分类与生效条数。
