# FINDINGS

## chunk 包结论

- 滚动校验和在 len∈{64,100,257,33,5}、块大小∈{4,7,16,32,5} 的随机数据上
  逐偏移断言：滚动推进结果与从头重算完全相等。
- 碰撞实测：[1,0,0,1] 与 [0,1,1,0]（块大小 4）弱校验和相同（a=2,b=5），
  强校验和不同——证明弱校验和单独使用会导致错误复用。
- 运算计数：len=4096/bs=64 时滚动全程基本运算 8258 次 < 4×4096=16384，
  远优于逐偏移重算的 4096×64=262144。

## 表① 六种源/目标关系的补丁构成

测试场景：块大小 16，基准数据 8 块共 128 字节（TestRelations 实测）。

| 关系 | 新数据字节数 | 复用块数 |
| --- | --- | --- |
| 完全相同 | 0 | 8 |
| 中间差一块 | 16（≤ 2×16） | 7 |
| 目标为前缀（源多 10 字节） | 10 | 8 |
| 源为前缀（源取前 5 块） | 0 | 5 |
| 完全不同 | 128（整份） | 0 |
| 源为空 | 0 | 0 |

补充：块大小（64）大于数据长度（10）时，末块不满逻辑生效，新数据 0、
复用 1 块。所有场景应用结果均与源端逐字节一致，且强校验次数 ≤ 弱匹配次数。

## 表② 截断点分类

测试补丁：块大小 16、目标 128 字节、中间差一块，全长 101 字节
（头部 41 + 指令区 56 + CRC 4）。逐字节截断 1..100 共 100 个截断点，
每个都给出可判定错误，且应用失败后目标端逐字节不变（TestTruncation 实测）。

| 截断点字节区间 | 分类结论 | 目标端是否不变 |
| --- | --- | --- |
| [1, 41) | 头部不完整 ErrTruncatedHeader（40 点） | 不变 |
| [41, 61) ∪ [77, 97) | 指令不完整 ErrTruncatedInstruction（40 点） | 不变 |
| [61, 77) | 数据段不完整 ErrTruncatedData（16 点） | 不变 |
| [97, 101) | CRC 不匹配 ErrCRCMismatch（4 点） | 不变 |

补充：复用块号越界报 ErrBlockOutOfRange；签名后目标端数据被改动报
ErrChecksumMismatch。两类均不产出任何数据。
