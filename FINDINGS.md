# 实验结论

## ① 六种源/目标关系（blockSize=8，8 个块）

| 关系 | 新数据字节 | 复用块数 |
|---|---:|---:|
| 完全相同 | 0 | 8 |
| 中间差一块 | 8 | 7 |
| 目标为源前缀（目标 43B） | 24 | 5 |
| 源为目标前缀（源 43B） | 3 | 5 |
| 完全不同 | 64 | 0 |
| 两者皆空 | 0 | 0 |

注：前缀场景末块不满，未对齐尾部按字面传输；目标前缀时匹配点之后的
重对齐产生至多 3 个块的边界冗余，仍远小于整份数据。

## ② 截断点分类（截断长度 n，H=52；待补丁实际生成后回填区间）

| 字节区间 | 分类 | 目标是否保持不变 |
|---|---|---|
| 0 ≤ n < H | 头部不完整 ErrHeaderTruncated | 是 |
| H ≤ n < H+instLen | 指令不完整 ErrInstrTruncated | 是 |
| H+instLen ≤ n < H+instLen+dataLen | 数据段不完整 ErrDataTruncated | 是 |
| n = 全长但 CRC 损坏 | CRC 不匹配 ErrCRCMismatch | 是 |
