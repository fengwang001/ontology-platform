# FINDINGS — 实测结论

## chunk 包

- 滚动校验和：在 n∈{1000,4097,100,257,513}、bs∈{1,7,16,64,100} 的随机数据上，
  逐偏移断言滚动结果与从头重算完全相等，全部通过。
- 弱碰撞实测：交换块内两字节（如 [1,2,3,4] 与 [2,1,3,4]）弱校验和相同、
  内容不同，SHA-256 强校验和可区分 → 两级校验缺一不可被实测证实。
- 弱校验基本运算次数：滚动扫描 n 字节实测 ≤ 2n，满足 ≤ 4n 的上界
  （若每偏移重算则为 n*bs，实测对比见 demo 判定行）。

## 表 1：六种源/目标关系下的补丁构成

基准：块大小 64，基准数据 100 块 = 6400 字节，随机种子固定。

| 关系 | 新数据字节数 | 复用块数 |
|---|---|---|
| 相同 | 0 | 100 |
| 中间差一块 | 64（= 1 块，≤ 2*bs） | 99 |
| 目标为源前缀（源多 320B） | 320 | 100 |
| 源为目标前缀（源取前 320B） | 0 | 5 |
| 完全不同 | 6400（整份） | 0 |
| 空（源空 / 目标空） | 0 / 6400 | 0 / 0 |

- 确定性：同一（源、目标）连跑 20 次，补丁字节流逐字节相同。
- 复杂度：diff 全程弱校验基本运算实测 ≤ 3n（上界 4n）；强校验次数
  恰好等于弱命中次数（中间差一块场景：99 次强校验确认 + 若干碰撞探查）。
- 并发：8 协程对不同数据对并发 diff+apply，结果全部正确，-race 干净。

## 表 2：截断点分类（补丁总长 170 字节：52 头 + 5 复用 + 数据指令 + 4 复用 + 4 CRC）

| 截断点区间 | 分类 | 目标端是否保持不变 |
|---|---|---|
| [1, 51] | 头部不完整 ErrHeaderTruncated | 是 |
| [52, 81] | 指令不完整 ErrOpTruncated | 是 |
| [82, 145] | 数据段不完整 ErrDataTruncated | 是 |
| [146, 165] | 指令不完整 ErrOpTruncated | 是 |
| [166, 169] | CRC 不匹配 ErrCRCMismatch | 是 |

- 全部 169 个截断点逐一断言：Apply 必返回上述四类之一（errors.Is 可区分）、
  返回数据为 nil、目标端数据与应用前逐字节相同（不部分应用）。
- 指令越界：复用块号 99（目标仅 4 块）→ ErrBlockRange，不 panic。
- 签名与数据不符：签名后目标端改动 1 字节 → ErrStateMismatch，不产出错误数据。
