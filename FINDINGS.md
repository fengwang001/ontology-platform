# 实验结论

## 表 1：五个聚合器的增量撤回核对（测试 `agg/agg_test.go`）

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
| --- | --- | --- | --- |
| Count | 是（+1） | 是（-1，恒成立） | 否 |
| Sum | 是（+v） | 是（-v，恒成立） | 否 |
| Min | 是 | 仅删的不是当前极值时 | 删到当前极值时需要 |
| Max | 是 | 仅删的不是当前极值时 | 删到当前极值时需要 |
| DistinctCount | 是（引用计数） | 否：需确认值是否还有持有者 | 总是需要 |

结论：10 万插入 + 5000 删除场景中 Min 重算恰为 3、Count/Sum 为 0，
详见 view 测试回填后的记录。
