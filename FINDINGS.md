# FINDINGS — 实测结论

## 可复现性与计数（hyper/vec 测试组）

- 同种子（99）两次建族，3 个测试向量 × 4 表的签名逐一相同；种子 100 的签名存在差异。
- 零向量签名恒为全 0（内积恰为 0 取 0 位的约定），dim=1 族可正常签名。
- 哈希计数器精确：n×bits×tables 三组 (1,4,1)/(7,8,3)/(50,12,8) 实测 HashOps 分别为
  4 / 168 / 4800，与公式不多不少。
- 维度不符返回 `vec.DimError{Want,Got}` 且 `errors.Is(err, vec.ErrDimMismatch)` 成立；
  NaN、±Inf 分别被 `ErrNaN`、`ErrInf` 拒绝。

## 表一：b × L 候选数与 top10 召回实测矩阵

数据：5000 向量（100 簇，dim=32，种子 7），100 个留出查询，候选数=精排距离计算数的均值。

| b \ L | 1 | 2 | 4 | 8 |
|-------|---|---|---|---|
| 4  | 377.9 / 0.682 | 696.2 / 0.902 | 1257.3 / 0.988 | 2052.2 / 0.999 |
| 8  | 51.5 / 0.453  | 86.5 / 0.663  | 148.2 / 0.882  | 255.2 / 0.986  |
| 12 | 14.7 / 0.306  | 26.3 / 0.476  | 40.7 / 0.733   | 59.1 / 0.936   |

（单元格 = 平均候选数 / top10 召回率。）

- 召回随 L 单调不降（b=8：0.453 → 0.663 → 0.882 → 0.986），L=8 时 0.986 ≥ 0.6。
- 候选数随 b 单调下降（L=4：1257.3 → 148.2 → 40.7），与 `L·N·2^(−b)` 的推导一致。
- 精排距离计算数：b=8、L=8 时每查询均值 255.2 ≤ 500（5000 的 10%），未退化为暴力扫描。
- 边界语义实测：空索引返回空；单向量 K=10 返回 1 条；300 个全同向量全落一桶，
  候选数=300=总数，top10 全中（召回 1）；NaN/±Inf 被拒且 Skipped=3；零向量合法；
  dim=1 索引可建可查；并发建索引+查询 `-race` 干净，读者只见合法 id。

## 表二：索引文件截断点分类与恢复

测试索引：dim=4、bits=6、tables=3、nvec=50，文件总长 1982 字节，共 64 个桶。
对 1..1981 全部截断点逐一调用 `Load` 分类（循环覆盖，无展开用例）：

| 截断点字节区间 | 分类结论 | 截断点数 |
|---|---|---|
| [1, 22)        | 头部不完整（ErrHeader）      | 21   |
| [22, 598)      | 超平面参数不完整（ErrHyperplanes） | 576 |
| [598, 1978)    | 桶表不完整（ErrBuckets）     | 1380 |
| [1978, 1982)   | CRC 不匹配（ErrCRC）         | 4    |

恢复（`Recover`，只保留完整解码的桶，剔除 id ≥ nvec 的悬挂引用）：

| 截断位置（桶表区间） | 恢复桶数 |
|---|---|
| 25%  | 17 / 64 |
| 50%  | 31 / 64 |
| 75%  | 47 / 64 |
| 100% | 64 / 64 |

- 全部 1380 个桶表区间截断点恢复后均无悬挂 ID（逐点断言 id < nvec）。
- 人为把桶内某 id 改为 9999 并修正 CRC：恢复时剔除并计 DroppedIDs=1。
- 人为把表 1 第 2 位法向量置全零并修正 CRC：`Load` 报 `DegenerateError{Table:1, Bit:2}`，
  `errors.Is(err, ErrDegenerate)` 成立；四类截断错误同样均可 `errors.Is` 区分。
