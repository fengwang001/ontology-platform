# 测试结论

## 表 ① 导出窗口内改写比例 vs 保留值 / 块内存峰值

基准：100 000 个键，块大小 4096 字节；保留值为快照关闭前实测计数。

| 改写键数 | 保留值个数 | 块内存峰值（字节） |
|---|---:|---:|
| 0 | 0 | 待 export 组实测 |
| 100 | 100（=改写键数，与总键数无关） | 待 export 组实测 |
| 全部（50/50 小库用例） | 50（=键数） | 待 export 组实测 |

补充实测（snapshot 组）：读取次数 = 键数（100 000 次 Get，计数器精确相等）；
空串键、空值与键不存在可区分；墓碑后旧快照仍读旧值；Close 后 reads 返回 ErrClosed，保留计数归零；
两个重叠快照（版本 v1、v2）在 v1 Close 后 v2 的保留仍在，两者都 Close 后计数均归零。

## 表 ② 逐字节截断点分类与可续传起始块

待 export/verify 组测试后，用实际夹具（块长度、清单长度）回填字节区间。
