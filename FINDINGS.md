# FINDINGS — 测试结论记录

## 表一：点位置归属（格 `[0,4)×[0,4)`，左闭右开）

| 位置 | 示例坐标 | 归属结论 |
|---|---|---|
| 格内 | (2,2) | 属于该格 |
| 左边界 | (0,2) | 属于该格（左闭） |
| 右边界 | (4,2) | 不属于（右开），属右邻格 |
| 下边界 | (2,0) | 属于该格（下闭） |
| 上边界 | (2,4) | 不属于（上开），属上邻格 |
| 左下角 | (0,0) | 属于该格 |
| 右下角 | (4,0) | 不属于，属右邻格 |
| 左上角 | (0,4) | 不属于，属上邻格 |
| 右上角 | (4,4) | 不属于，属右上邻格 |
| 分裂线 | (2,1)/(1,2) | x 在中线进右子格，y 在中线进上子格 |

结论由 `geom.TestContains`、`cell.TestSplitReassign` 验证通过。

## 剪枝实测（`query.TestPruningBounds`，10 万均匀点、1% 面积查询）

命中 998 点；逐点判定 159（0.16%，上界 5%）；访问格 58 / 总格 21153
（0.27%，上界 10%）。整格包含查询逐点判定为 0（`query.TestWholeTake`）。
1000 个同坐标点分裂在深度上限处终止且全部可查
（`cell.TestIdenticalPointsTerminate`）。

## 表二：截断分类（待 persist 测试后填写）
