# 测试结论

## 表 ①：五种请求形态的步骤序列与临时名用量

| 形态 | 请求 | 执行步骤序列 | 临时名数 |
| --- | --- | --- | --- |
| 链 | a→b, b→c | b→c ; a→b | 0 |
| 二元环 | a→b, b→a | a→~tmp-0 ; b→a ; ~tmp-0→b | 1 |
| 三元环 | a→b, b→c, c→a | a→~tmp-0 ; c→a ; b→c ; ~tmp-0→b | 1 |
| 目标已存在 | a→x（x 占用且不搬走） | 整批拒绝（ErrTargetExists），0 步 | 0 |
| 同名冲突 | a→c, b→c | 整批拒绝（ErrDuplicateTarget），0 步 | 0 |

结论：链按「目标空闲者优先」的拓扑序执行；每个环恰好借 1 个临时名，
步数 = 环长 + 1；两类冲突在任何修改之前被拒绝。10 个互不相交的二元环
实测临时名恰为 10；5 万请求实测查找次数 ≤ 4×(请求数+名字数)；构造顺序
打乱 20 轮步骤序列逐元素一致；临时名 `~tmp-0..99` 被预占时自动改用
`~tmp-100`，全程无覆盖。

## 表 ②：日志截断点分类（待 undo 包测试后填写）
