# 测试结论

## ① 攒批触发条件（batcher 表驱动测试）

| 触发条件 | 配置 | 输入 | 批次大小 | 同步次数 |
|---|---|---|---|---|
| 条数上限 | maxCount=3 | 3×10B | [3]（30B） | 待 commit 组补 |
| 字节上限 | maxBytes=100 | 40B+40B+40B | [2,1]（80B,40B） | 待 commit 组补 |
| 等待超时 | maxWait（注入时钟） | 3×10B | [3]（30B） | 待 commit 组补 |

补充结论：

- 字节恰好等于上限（50B+50B，maxBytes=100）立即成批，不越界。
- 单条 200B > maxBytes=100：不拒绝，单独成批（唯一允许越上限的情形）。
- maxWait=0：每条立即成批，批大小恒为 1。
- 空批 Timer 为 nil，不会产生空批次。

## ② 截断点分类（待 recover 组补）
