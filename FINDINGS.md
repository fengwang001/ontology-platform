# Findings：同 ID 覆盖在非满容器下的行为偏差

测试对象：`selector.go` / `helpers.go` 中的 `TopK.Push` 与 `(*TopK).replace`。
复现测试：`overwrite_characterization_test.go`（characterization，全部通过）。

## 结论速览

`Push` 对**新 ID** 正确区分了“满 / 非满”：非满直接入选，只有满时才与堆顶最差元素
比较淘汰。但同 ID 覆盖走的是 `replace`，它**无条件套用“满容器淘汰”阈值**，
完全不看容器是否已满，导致非满容器下元素被误删。

## 1. 非满容器（len < K）覆盖为更差分数：元素被误删

- 复现（Desc，K=3）：`Push("a",100)`、`Push("b",50)`、`Push("a",1)`。
- 实际输出：`Len()==1`，`Snapshot()==[{b 50}]`；a 消失。
- 对称形态（Asc，K=3）：a:1、b:50、`Push("a",100)` 后同样只剩 b。
- 应当输出：`Len()==2`，`Snapshot()==[{b 50} {a 1}]`；
  容器有空位，覆盖只应更新分数并立即重排，不应产生任何淘汰。
- 根因：`helpers.go` 的 `replace` 先找出“去掉旧元素后剩余集合的最差元素”作阈值，
  新分数若不严格优于该阈值就 `remove(pos)`。该阈值只在**满容器有真实淘汰压力**
  时才有意义；`replace` 没有像 `Push` 新 ID 分支那样判断 `len(t.hp.items) < t.k`，
  非满时照样执行淘汰，白白丢掉一个名额。
- 加剧后果：误删后该 ID 从 `items`/`index` 消失，下次推送被当作全新元素；
  下游滚动窗口中指标分数短暂下滑即永久掉榜，且 `Len()` 可能长期小于 K，
  直到有足够新 ID 补齐（测试 `underfull_dropped_id_can_reenter` 钉住该后果）。

## 2. 非满容器覆盖为并列分数（含 +0.0/-0.0）：同样被误删

- 复现（Desc，K=3）：`Push("a",-0.0)`、`Push("b",5)`、`Push("a",+0.0)`。
- 实际输出：`Len()==1`，只剩 `{b 5}`；a 被删。
- 应当输出：`Len()==2`，a 以零分保留在榜（符号见第 4 条）。
- 根因：与第 1 条同处。并列时 `rankLess` 返回 false（保留分支进不去），
  于是落到删除分支；非满容器本不该有任何“截断”。

## 3. 满容器覆盖掉出后容器留洞，且并列去留比较与文档承诺相反

- 文档/注释承诺（`selector.go` 的 `Push` 注释、`helpers.go` 的 `replace` 注释）：
  “容器满时与最差元素比较、严格更优才替换；**并列时保留 ID 字典序更小者**”，
  语义是“保留当前分数下的前 K 个”。
- 复现 A（满，并列，覆盖 ID 更大），K=2，Desc：
  `Push("a",5)`、`Push("zzz",9)`、`Push("zzz",5)`。实际 `Len()==1`，只剩 a。
  按承诺并列应保留更小 ID 的 a，结果碰巧正确，但实现并非按承诺比较，
  且容器从满变非满（反例见 B）。
- 复现 B（满，并列，覆盖 ID 更小），K=2，Desc：
  `Push("a",5)`、`Push("aa",9)`、`Push("aa",5)`。实际 `Len()==1`，只剩 a，aa 被删。
  按承诺应保留更小 ID，即 `Snapshot()==[{aa 5} {a 5}]`、`Len()==2`。
- 根因：`replace` 用 `rankLess`（同分按 **ID 升序**）在剩余集合里挑 rank 最靠后的
  元素作阈值，再用 `rankLess(incoming, threshold)` 决定去留。去留取决于**阈值元素
  自身的 ID**：阈值 ID（a）比 incoming（aa）更小时，incoming 反而被判为“不够好”
  而删除；这不是“二者谁的 ID 更小”的比较。正确做法应与新 ID 满容器分支一致，用
  `heapLess`（堆顶=最差，同分 ID 更大者更差）比较 incoming 与堆顶并原位替换，
  满容器淘汰后长度恒为 K。
- 附带影响：满容器覆盖掉出后 `Len` 从 K 变 K-1，与新 ID 满容器分支
  （淘汰一个、补进一个，长度恒为 K）不一致；这也使第 1 条的误删在已满场景下
  被掩盖成“看起来合理的淘汰”。

## 4. +0.0 与 -0.0 判等，但覆盖是否替换符号位未定义

- 现状：`scoreLess`/`rankLess`/`heapLess` 都把 +0.0 与 -0.0 视为同分，
  Snapshot 顺序按 ID（见既有 `float_test.go`），这部分与注释一致。
- 但“覆盖”是否更新符号位没有约定：单元素非满路径（`replace` 的 `len==1`
  分支）无条件写入新值，`Push("a",+0)` 再 `Push("a",-0)` 实际存 `-0.0`；
  多元素非满路径则因第 2 条直接删除。
- 建议：文档明确覆盖是否替换符号位（建议替换：后到值整体生效），并在第 1～3 条
  修复后统一验证。

## 5. 单元素非满分支与多元素非满分支行为自相矛盾

- 复现：K=3，仅 `Push("a",5)` 后 `Push("a",1)`，实际 `Len()==1`、a 保留为 1；
  同样 K=3 但先有 `Push("b",50)` 再覆盖（第 1 条），a 被删。
- 应当：两种非满形态行为一致，都只更新分数、保留在榜。
- 根因：`replace` 对 `len==1` 特判为直接赋值，恰好绕过了错误的阈值淘汰；
  该特判反证阈值逻辑本意只服务于满容器淘汰。

## 6. NaN 覆盖：行为正确（对照项，无偏差）

- `Push("a",9)`、`Push("b",4)`、`Push("a",NaN)`：旧分数 9 保留，
  `Skipped()==1`，`Len()==2`，与 `Push` 注释及既有
  `TestNaNOverwriteKeepsOldScore` 一致，仅作对照记录。

## 修复方向（仅建议，本次未改实现）

1. `replace` 开头按 `len(t.hp.items) < t.k` 分流：非满时原位写入新分数并
   `fix`、同步 map，永不删除。
2. 满容器时复用新 ID 分支的判定：以堆顶（`heapLess` 意义上的最差元素）为阈值，
   incoming 严格更优才原位替换并 `fix`，否则保持旧值不变（长度恒为 K）。
3. 删除 `len==1` 特判，使其被非满分支统一覆盖。
