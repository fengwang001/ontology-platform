# 实验结论

## 表 1：请求形态 → 执行步骤序列与临时名数
| 形态 | 请求 | 执行步骤序列 | 临时名数 |
|---|---|---|---|
| 链 | a→b,b→c | b→c ; a→b | 0 |
| 二元环 | a→b,b→a | a→T ; b→a ; T→b | 1 |
| 三元环 | a→b,b→c,c→a | a→T ; c→a ; b→c ; T→b | 1 |
| 目标已存在 | a→b（b 存在且不搬走） | 拒绝整批，0 步 | 0 |
| 同名冲突 | a→x,b→x（或 a→x,a→y） | 拒绝整批，0 步 | 0 |

## plan 包已验证结论
- 拓扑序：链严格自汇点起；`{a→b,b→c}` 实测顺序 b→c,a→b。
- 四类冲突均可 `errors.Is` 判定；检测只对快照进行，命名空间零改动。
- 5 万请求 map 查找计数 ≤ `4*(请求数+名字数)`（实测远低于上界）。
- 自环为无操作；空串名与含 `/` 名按普通名字处理；空请求集 0 步成功。
