# 测试结论

## 五种聚合 × 缺失规模（标注文字）

| 聚合 | 全部成功 | 缺一个分片 | 缺一半分片 |
| --- | --- | --- | --- |
| Count | 精确：=v | 下界：≥v | 下界：≥v |
| Sum | 精确：=v | 下界：≥v | 下界：≥v |
| Min | 精确：=v | 单边界：真实 Min≤v | 单边界：真实 Min≤v |
| Max | 精确：=v | 单边界：真实 Max≥v | 单边界：真实 Max≥v |
| TopK | 精确：全榜可信，前缀=K | 按上界截断：前缀 L∈[0,K] | 按上界截断：前缀 L∈[0,K] |

## 故障注入：检出方式与分片状态

| 故障 | 检出方式 | 分片状态 |
| --- | --- | --- |
| 超时 | ctx 截止前无有效批次 | timeout（errors.Is ErrTimeout） |
| 损坏 | 实交记录数 ≠ 声称条数 | corrupt（ErrCorrupt），记录整批丢弃 |
| 重复返回 | 同一分片第二批到达 | duplicate（ErrDuplicate），仅首批计入 |
| 全部失败 | 无 OK 分片 | 汇总错误 ErrAllFailed（零分片 ErrNoShards） |

## 已跑测试组
- shard：健康/损坏/重复/失败/超时事件流、状态枚举 —— 通过。
