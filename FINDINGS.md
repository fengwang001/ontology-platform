# FINDINGS：Top-K 同 ID 覆盖路径的行为与文档承诺不一致项

复现基准：`New(K, dir)`；下列「实际输出」均已由
`replace_characterization_test.go` 中的 characterization tests 钉住并通过。
文档承诺来源：`selector.go` 的 `Push` 注释（"同一 ID 后到的分数覆盖先到的
分数，并立即重排"）、`Len` 注释（"当前持有的元素个数"）以及背景语义
（容器未不满时不存在真实淘汰，任何覆盖都应原地更新）。

## 1. 非满容器覆盖为更差分值：元素被误删

- 复现：`New(4, Desc)`；`Push("a",10) Push("b",20) Push("c",30)`；
  `Push("a",1)`（容器持有数 3 < K=4）。Asc 对称形态：a=30 覆盖为 40。
- 实际输出：`Snapshot = [c30 b20]`，`Len = 2`，ID `a` 消失，容器留下空位。
- 应当输出：`Snapshot = [c30 b20 a1]`，`Len = 3`，仅原地更新 a 的分数。
- 根因：`helpers.go` 的 `replace` 不区分满/非满容器，无条件以"去掉旧元素后
  剩余集合中的最差元素"为截断阈值；该阈值语义只在 `len == K`（必须淘汰一个
  才能维持 K 个）时成立。非满容器有空位，覆盖后的元素即使最差也应保留。

## 2. 非满容器覆盖为相同分数：元素同样被误删

- 复现：`New(4, Desc)`；a=10,b=20,c=30；再 `Push("a",10)`。
- 实际输出：`Snapshot = [c30 b20]`，`Len = 2`，a 被删除。
- 应当输出：`Snapshot = [c30 b20 a10]`，`Len = 3`，覆盖后分数不变、原地保留。
- 根因：同 #1。阈值 = b20，`rankLess(a10, b20)` 为 false，直接走删除分支；
  代码从未考虑"删除后仍有空位"这一情况。

## 3. 覆盖后分数等于截断阈值时：空位也不补，并列破坏被错误用于淘汰

- 复现（Desc）：`New(4, Desc)`；a=10,b=20,c=30；`Push("a",20)`。
  - 实际输出：`Snapshot = [c30 a20 b20]`，a 保留（`a < b` 并列胜）。
  - 同形态换成大 ID：z=10,b=20,c=30；`Push("z",20)` →
    `Snapshot = [c30 b20]`，**z 被删除，Len 从 3 掉到 2**。
  - Asc 对称：z=30 覆盖为 20 时同样被删。
  - `+0.0`/`-0.0` 与阈值 0 并列时完全同构：ID 小者留（分数判等，符号位
    不影响比较），ID 大者被删（测试中 `overwrite-to-negzero/poszero-*` 钉住）。
- 应当输出：非满容器下并列不构成淘汰理由，z 应保留为 `[c30 b20 z20]`。
- 根因：同 #1。并列打破规则（满容器从 K 与 K+1 名中选小 ID）被直接搬到
  非满容器，导致"字典序不够小"成为凭空踢出元素的理由。

## 4. 同一语义随持有数不连续：`len == 1` 特判掩盖缺陷

- 复现：`New(4, Desc)`；只 `Push("a",10)` 后 `Push("a",1)`（持有数 1）。
- 实际输出：`Snapshot = [a1]`，`Len = 1`，正确保留。
  再加入任何一个其他元素（持有数 ≥ 2）后做完全相同的更差覆盖：
  a 立即被删除（见 #1，测试 `occupants-1/2/3-*` 钉住这一跳变）。
- 应当输出：两种持有数下都应原地保留 a。
- 根因：`replace` 对 `len(hp.items) == 1` 单独特判直接写回；该特判恰好
  等价于"非满容器不淘汰"的正确语义，但只覆盖了单个元素这一种形态，
  使缺陷在持有数 1→2 处出现行为不连续，也让单元素冒烟测试无法暴露问题。

## 5. 满容器覆盖路径的阈值定义本身错误（被既有测试固化）

- 复现：`New(2, Desc)`；a=100,b=50；`Push("a",1)`。
- 实际输出：`Snapshot = [b50]`，`Len = 1`（既有
  `TestOverwriteWorseDropsOutOfTopK` 已将此钉为"期望"，Asc 同）。
- 应当输出：`Snapshot = [b50 a1]`，`Len = 2`。覆盖是对**已持有槽位**的原地
  更新，不占用第 K+1 个名额；满容器下覆盖也不应产生空位。正确做法是先写回
  新分数、再在同一集合内取最差者，而不是把被覆盖元素从候选中剔除后再比较。
- 根因：`replace` 把"覆盖"实现成"先删旧值，再按新元素挑战最差位置"，
  混用了 `Push` 的满容器淘汰路径（`selector.go` 中 worst = heap[0] 的比较
  只对真正的新 ID 成立）。由此被覆盖 ID 一旦成为写回后的最差元素就自删，
  即使所有 ID 互不相同且总数 ≤ K。

## 6. 代码注释与对外契约互相矛盾

- 位置：`helpers.go` 中 `replace` 的注释——"否则该 ID 立即掉出 Top-K"，
  声称覆盖可以使已在榜元素掉出；而 `selector.go` 的 `Push` 注释承诺
  覆盖只是"更新分数并立即重排"，未声明任何覆盖淘汰语义。
- 实际行为遵循前者（任意更差/并列覆盖都可能删除元素）；调用方依据后者
  无法预期 `Len` 在一次同 ID 覆盖后变小。
- 建议：对外契约明确为"同 ID 覆盖永不淘汰、只更新与重排"；修复点集中在
  `replace`：删除阈值截断分支，直接写回新值并 `fix` 堆序即可，满/非满
  容器行为随之统一。

## 已核对、与文档一致的行为（非缺陷）

- NaN 覆盖既有 ID：`Push` 入口先判 NaN，`skipped++` 后整体拒绝，旧分数
  保留、不进 `replace`（`full/underfull-nan-overwrite-*` 钉住）。
- `+0.0 == -0.0`：`rankLess`/`heapLess` 显式把双零判同分，并列按 ID 升序；
  正常入选路径与 `Snapshot` 排序一致。
- 新 ID 在满容器下严格更优才替换、并列留小 ID：与 `Push` 注释一致。
