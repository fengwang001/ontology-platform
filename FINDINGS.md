# FINDINGS：TTL-LRU 缓存驱逐与 TTL 边界行为分析

范围：`internal/ttlcache`（`cache.go` / `operations.go` / `entry.go` / `list.go`）。
以下每条均为「实现真实行为」与「注释/文档所承诺或暗示的语义」之间的不一致；
对应 characterization 测试见 `internal/ttlcache/characterization_test.go`（断言当前真实行为，全部通过）。

## 1. evict 按「写入最早」而非「过期最早」挑选过期项

- 注释承诺：`cache.go` 中 `evict` 注释为「优先驱逐写入时刻最早的过期项」，但「驱逐过期项」的语义直觉是清掉过期最久（`expireAt` 最小）的条目；长短 TTL 交错时两种口径给出相反顺序，注释未说明这一分歧。
- 复现输入：容量 2；t=0 `Put(early, ttl=100)`；t=50 `Put(late, ttl=10)`；推进到 t=100（两者均已过期，late 已过期 40ms、early 刚到期）；`Put(new)` 触发驱逐。
- 实际输出：`early` 被驱逐，`late`（已过期更久）保留。
- 应当输出：若按「过期最早」口径，应驱逐 `late`（`expireAt=60` 小于 `early.expireAt=100`）。
- 根因：`evict` 的比较谓词是 `e.createdAt < oldestExpired.createdAt`，比较的是写入时刻而非过期时刻 `createdAt + ttl`。各条目 TTL 不同，`createdAt` 最小不等于 `expireAt` 最小，于是「写入早但 TTL 长」的条目先于「写入晚但早已过期」的条目被驱逐。

## 2. 重复 Put 不刷新 TTL：刷新值不延寿，Put 成功时该项可能已过期

- 注释承诺：`Put` 注释明确「不刷新 TTL（过期时刻以首次写入为准）」，但「写入成功返回 nil」隐含该键当前可用；实际上重 Put 一个已过期（但尚未清理）的键会返回成功，而随后的 `Get` 立即按 miss 删除它。
- 复现输入：t=0 `Put(a, old, ttl=10)`；t=9 `Put(a, new, ttl=1000)`（重 Put）；推进到 t=10 后 `Get(a)`。
- 实际输出：t=10 `Get(a)` 返回 miss 并删除该键；重 Put 携带的 `ttl=1000` 被完全忽略。
- 应当输出：若重 Put 视为一次「写入」，过期时刻应顺延至 t=9+1000（或至少注释应警告「重 Put 不延寿，新值可能立即不可读」）。
- 根因：`operations.go` 中命中已存在键的分支只执行 `e.val = val` 与 `moveToFront(e)`，不更新 `createdAt`/`ttl`；`expireAt` 永远锚定首次写入。

## 3. 惰性单条驱逐：过期项长期占位，容量被尸体条目挤占

- 注释承诺：`Len` 注释「包含尚未被清理的过期项」、`evict` 注释「驱逐一个条目」，但没有任何文档说明「过期项可无限期占位、清理速率受后续写入节流」，调用方会以为容量全部可用于存活条目。
- 复现输入：容量 3；t=0 写入 a/b/c（ttl=10）；推进到 t=10（全部过期）；连续 `Put(d)`、`Put(e)`、`Put(f)`。
- 实际输出：过期后 `Len()` 仍为 3；每次 `Put` 只驱逐一个过期项，`Len` 始终保持 3，需 3 次 `Put` 才清完 a/b/c。期间缓存中存活条目数远少于容量。
- 应当输出：容量语义上更合理的是写入前清掉全部过期项（或至少清理到有空间为止），一次 `Put` 即可回收所有尸体条目。
- 根因：`Put` 仅在 `len(items) >= capacity` 时调用一次 `evict`，而 `evict` 每次只 `removeEntry` 一个条目；`Len` 又把过期项计入，使 `len(items)` 长期虚高，清理只能逐条惰性触发。

## 4. 同 createdAt 的过期项：驱逐头侧（MRU），与 LRU 直觉相反

- 注释承诺：链表约定「头为最近使用、尾为最久未使用」，驱逐语义暗示优先牺牲更久未用的条目；同刻冲突的 tie-break 行为完全无文档。
- 复现输入：容量 2；t=0 同时刻 `Put(a)`、`Put(b)`（createdAt 均为 0，b 为头/MRU）；推进到 t=10（同时过期）；`Put(c)` 触发驱逐。变体：过期前 `Get(a)` 把 a 提升为 MRU，则被驱逐的变成 a。
- 实际输出：头侧（最近使用）的过期项被驱逐，尾侧（最久未用）的保留。
- 应当输出：同刻冲突应驱逐尾侧（LRU）项，与「尾为最久未使用、优先淘汰」的约定一致。
- 根因：`evict` 头→尾扫描且用严格小于（`e.createdAt < oldestExpired.createdAt`）更新候选：同 `createdAt` 时后遇到的（靠尾侧）条目不会替换先遇到的（头侧）条目，于是头侧 MRU 项成为牺牲品。改为 `<=` 或从尾扫描即可翻转 tie-break。

## 5. 过期项在 Get / Delete / Len 三处口径不一致

- 注释承诺：三处各自注释分别说明了行为，但没有任何一处指出三者对同一个过期项给出互相矛盾的「存在性」答案。
- 复现输入：容量 2；t=0 `Put(a, ttl=10)`；推进到 t=10（a 已过期未清理）。
- 实际输出：`Get(a)` 返回 `("", false)` 视为不存在并立即删除（`Len` 归 0）；`Delete(a)` 返回 `true` 视为存在；`Len()` 返回 1 计入总数。
- 应当输出：同一时刻、同一条目，三个入口应对「是否存在」给出一致答案（例如统一视为不存在：`Delete` 返回 false、`Len` 不计入）；或至少在文档中显式声明这是有意的三态语义。
- 根因：`Get` 显式检查 `expiredAt` 并按 miss 处理；`Delete` 只查 `items` 映射、不检查过期；`Len` 直接返回 `len(items)` 不过滤。三条路径各自独立实现，没有统一的「过期即不存在」抽象。

## 备注

- 以上均为对当前行为的刻画与根因分析，未修改任何实现文件；修复方向（如改谓词为 `expireAt`、重 Put 刷新 TTL、批量清理、统一过期语义）需另行评审后再动实现。
