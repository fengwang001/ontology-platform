# 测试结论记录

## 表 1：三个崩溃时刻的恢复结论

（随集成测试补全）

## 表 2：临时文件逐字节截断结论

| 截断字节数 n | Verify 判定 | CleanupTemp | LatestOutput |
| --- | --- | --- | --- |
| 0 ≤ n ≤ 载荷全长（循环逐点，不展开） | 缺尾行/校验和不符，一律无效 | 删除 1 个 `*.tmp` | pos=-1，不被当成交付文件 |

- 正常提交：`*.tmp` rename 后不存在；尾行 `sha256=` 与载荷一致才有效。
- sink 在写文件前注入错误：不产生任何输出文件，错误如实返回。
- ckpt：两轮转文件；最新文件校验和不匹配时回退到上一个完好文件并标记
  `fellBack=true`；两个都坏时从 pos=0 重启；无文件即全新开始。

## 阶段性结论

- parse：表驱动 8 个切分点（含空键、缺 `=`、value 非数字、负数、空白），
  坏帧计数准确且 Parse 不返回错误，管线可继续。
