# 测试结论（每组测试完成后立即补入）

## 已完成分组

- change：10 条表驱动用例，覆盖三种 Op 校验、空串分组合法 / nil 拒绝、
  NaN 拒绝、版本非正拒绝；编解码往返位级一致；每个 payload 逐字节截断均
  返回 `ErrDecode`。
- agg：6 条场景表 × 5 个聚合器 + 声明表。删非极值时 Min/Max 不标记、
  删到极值标记；DistinctCount 每次删除标记；±0 重建后为同一 distinct 值。

## 表 ① 五个聚合器撤回核对

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
|---|---|---|---|
| Count | 是（n+1） | 是（n-1，恒成立） | 否 |
| Sum | 是（s+v） | 是（s-v，恒成立） | 否 |
| Min | 是 | 条件：仅删到当前极值需重算，删非极值无影响 | 条件需要 |
| Max | 是 | 条件：仅删到当前极值需重算，删非极值无影响 | 条件需要 |
| DistinctCount | 是（加入集合） | 否：状态不含持有计数，无法知道 v 是否仍被持有 | 是（每次删除） |

## 表 ② 逐字节截断分类（200 条等长记录）

（待 journal 组测试后填入）
