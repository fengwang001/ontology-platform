# 实验结论

## 表 ① 导出期间不同改写规模下的保留值与内存峰值

| 改写字节规模 | 改写键数 | 保留值个数 | 分块内存峰值 |
|---|---|---|---|
| 0 | 0 | 0 | ≤ 一块大小 |
| 100 / 100000 | 100 | 100 | ≤ 一块大小 |
| 全部键 | N（=键数） | N | ≤ 一块大小 |

结论：保留值个数恒等于“快照存活期间被覆盖的键数”，与总键数无关；
导出缓冲峰值恒为一块大小（单块场景即总数据量）。
（export 包测试落地后回填实测列。）

## 表 ② 截断点分类

| 字节区间 | 分类 | 可续传起始块 |
|---|---|---|
| 0 .. manifest 末尾-1 | 清单不完整 | 0（重新导出） |
| 整块之后 .. 下一块头 16B 内 | 块头不完整 | 当前块号 |
| 块头齐 .. 该块体末-1 | 块体不完整 | 当前块号 |
| 完整文件体内翻转 1 字节 | CRC 不匹配 | 当前块号（重写该块） |

（verify 包测试落地后回填实测字节区间。）
