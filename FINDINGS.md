# FINDINGS

## 十种输入组合的合并结果与冲突分类

由 `merge3/merge3_test.go` 的 TestMergeTable 验证（全部通过）。

| # | 输入组合 | 合并结果 | 冲突分类 |
|---|----------|----------|----------|
| 1 | 左改右未改 | 取左（改了的一侧） | 无冲突 |
| 2 | 两侧同值（1->2） | 结果为 2 | 无冲突 |
| 3 | 两侧异值（同字段） | 结果取左，冲突待解 | FieldValue |
| 4 | 左删右改 | 记录不复活 | DeleteModify（与 FieldValue 可区分） |
| 5 | 两侧同删 | 记录删除 | 无冲突 |
| 6 | 改不同字段（左 a 右 b） | a、b 都生效，c 保持祖先值 | 无冲突 |
| 7 | 同键新增同内容 | 采用该记录 | 无冲突 |
| 8 | 同键新增异内容 | 结果取左，冲突待解 | AddAdd |
| 9 | 祖先缺失（两侧都有） | 按两侧各自新增处理，同 7/8 | 同内容无冲突，异内容 AddAdd |
| 10 | 类型不一致（string vs int） | 结果取左，报告含两侧类型名 | TypeMismatch |

补充边界结论（同表验证）：三集合皆空得空结果零冲突；祖先为空
两侧各有内容全部按新增并入；某侧为空按「该侧删除全部记录」处理
（对侧未动则无冲突全删，对侧改过的记录各报一个 DeleteModify）；
空串键、空串字段名均合法；字段值为空串与字段不存在可区分，
一侧置空串、一侧删字段判为 FieldValue 冲突。

## 复杂度与确定性结论

- 3 个各 5 万键集合：查找计数 150000 = 3*|并集| <= 4*|并集|，通过。
- 报告阶段访问记录数 == 冲突数，不二次遍历全量记录，通过。
- 三集合插入顺序打乱 20 次，序列化输出逐字节相同，通过。

## 截断点分类结论

由 `serialize/serialize_test.go` 的 TestTruncation 逐点循环验证
（样例文件全长 179B，头 8B，payload 167B，CRC 4B）。

| 截断点区间 | 含义 | 分类 | 样例点数 |
|------------|------|------|----------|
| [1, 8) | 头部（magic+长度）不完整 | ErrHeaderIncomplete | 7 |
| [8, 8+167) | payload 不足头部声明长度 | ErrRecordIncomplete | 164 |
| [8+167, 179) | CRC 被切或缺失 | ErrCRCMismatch | 4 |

另：长度不变、逐字节翻转 payload 的 167 个点全部判为
ErrCRCMismatch（TestCorruption）；三类错误可用 errors.Is 互相区分。
同侧同键既删又改的变更集被 delta.Validate 拒绝，错误含该键名，
可用 errors.Is(err, delta.ErrContradictory) 判定。
