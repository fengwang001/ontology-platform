# 实测结论

## 表① 五个聚合器撤回策略核对（go test ./agg 全部通过）
| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 |
|---|---|---|---|
| Count | 是（+1） | 是（-1） | 否 |
| Sum | 是（+v） | 是（-v） | 否 |
| Min | 是 | 仅当被删值≠当前极值 | 是（命中极值才重算） |
| Max | 是 | 仅当被删值≠当前极值 | 是（命中极值才重算） |
| DistinctCount | 是 | 否（计数无法反映是否仍有持有者） | 是（删除即重算） |

## 表② 逐字节截断分类（200 条日志，全长 7496 字节；L 为截断后长度）
| 字节位置区间 | 分类 | 重放后生效记录条数 |
|---|---|---|
| L=1..5 | 头部不完整 ErrShortHeader | 0 |
| L=6 | 干净边界（仅完整头） | 0，无错误 |
| L=7..10 | 长度前缀不完整 ErrShortLength（首条；每条边界后 +0..3 复发） | 该点之前完整条数 |
| L=11..7491 | 记录体不完整 ErrShortBody（跨各帧体部区间） | 该点之前完整条数 |
| L=7492..7495 | CRC 不匹配 ErrCRC（末帧尾 4 字节；每帧尾 +0..3 复发） | 199（末帧未生效） |
| L=7458 等帧起点 | 干净边界，无错误 | 恰为完整前缀条数（如 199） |

遍历 L=1..7495 共 7495 个截断点，四类错误均至少出现一次；干净帧边界
返回无错误且生效条数=完整前缀条数。完整文件 L=7496 重放 200 条、无错误。
