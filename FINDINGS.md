# FINDINGS — 测试结论记录

## 0. posting 编解码

- 差分 varint 编码往返一致（空链/单文档/多文档/大间隔）；重复 (doc,pos) 被拒绝并计数；
  逆序添加报错；截断/尾随垃圾/零差分均能检出。

## 0.1 segment 写读与截断

- 200 词段（每词 5 文档 × 2 位置）写读往返一致；1..N-1 全部截点均落入四类之一且
  `errors.Is` 可区分；CRC 翻转一字节即报 `ErrCRCMismatch`。
- 恢复自洽：截断段恢复出的词典中每个词的倒排链都完整可读、文档号严格升序，
  不完整词被剔除，无悬挂指针。

## 1. 短语命中表（重叠计数，位置 0 起）

| 文本 | 短语 | 命中次数 | 命中位置(锚) |
|---|---|---|---|
| A A A | "A A" | 2 | 0, 1 |
| A A A A | "A A" | 3 | 0, 1, 2 |
| A B A B A | "A B A" | 2 | 0, 2 |
| x y x | "x"（长 1，退化为单词查询） | 2 | 0, 2 |
| A B | "A B C"（短语超长） | 0 | 无 |

## 1.1 复杂度断言

- 1 万 A + 1 万 B 查 "A B"：命中 10000，比较次数 <= 4*20000（实测约 2 万，线性）。
- AND 三链（100/150/200）：比较次数 <= 4*100*3=1200，最短链驱动 + galloping 跳跃。

## 1.2 merge 合并与故障

- 多路归并：无删除时 读==写==总项数（6/6/6）；删除 2 文档时 读 6、写 4，被删项清除。
- 删除后立即查询（位图过滤）与删除后合并再查询，结果逐条相同。
- 合并中途崩溃：残留 `merged.seg.tmp` 被 Recover 识别清理，旧段可用，
  查询结果与崩溃前逐字节相同。
- 并发：4 查询协程与 10 次 Compact 并发，结果恒等于一致快照，`-race` 干净。

## 1.3 verify 自检

- 完好段 CheckFile 通过；四类截断各取代表截点均被正确分类。
- 不变量自检可检出：词典乱序、文档号乱序、位置乱序、悬挂词；完好段无误报。

## 2. 段截断分类表（段文件全长 N=7220 字节，dictLen=3000）

| 截点区间(字节) | 分类 | 恢复词数 |
|---|---|---|
| 1–15 | 头部不完整 | 不可恢复 |
| 16–3015 | 词典不完整 | 不可恢复 |
| 3016–7215 | 倒排链不完整 | 0–199（随截点递增，如 3610→28，5000→94，7215→199） |
| 7216–7219 | CRC 不匹配 | 200（全部恢复） |
