# 实测结论

测试环境：go1.26.5 linux/arm64，`go test -count=1 ./...` 与 `-race` 全部通过。

## 表① 导出期间改写键数 vs 保留值个数 / 块缓冲内存峰值

总键数 100000，块大小 4096 字节。

| 导出期间改写键数 | 保留值个数 | 块缓冲峰值 |
| --- | --- | --- |
| 0 | 0 | ≤ 4096 |
| 100 | 100 | ≤ 4096 |
| 100000（全部） | 100000 | ≤ 4096 |

结论：保留值个数 == 导出期间被改写的键数，与总键数无关；快照关闭后归零；
两个重叠快照时，关掉一个后保留值仍在，两个都关闭后才释放。
块缓冲峰值不超过块大小（单条记录小于块大小时）。

## 快照与并发

- 快照读在键被覆盖后仍返回旧值；快照后新建的键对快照不可见。
- 空键合法；空值与键不存在可区分（`ok` 标志）。
- 4 写 4 读并发 + 反复开关快照，`-race` 干净，最终保留值归零。

## 导出与续传

- 读取次数 == 键数（1000 键导出实测 1000 次读，每键恰好一次）。
- 块缓冲峰值 ≤ 块大小（块 4096 字节时峰值实测不超 4096）。
- 字典序与逆序导出的 `TotalCRC` 相同（顺序无关性成立）。
- 导出到第 50 个键时改写第 80 个键，导出结果与无写入对照组逐字节相同。
- 200 键 / 块 128 字节共 29 块：对每个 k∈[1,29] 中断后续传，结果与一次性导出逐字节相同。
- 4 个并发导出（另有并发写入干扰）各自与单独导出逐字节相同，`-race` 干净。
- 边界：空存储导出 0 块合法；单键 / 块大于总数据量均为单块；块大小 ≤0 返回 `ErrChunkSizeZero`。
