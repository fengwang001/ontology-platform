# FINDINGS — 实测结论（随测试推进逐步补充）

测试环境：linux/arm64，go1.26.5，全部随机源显式播种，结果可复现。

## 0. 基础正确性（vec / hyper）

- `vec`：Dot/Dist 表驱动用例（正交/一般/一维/零向量）全部符合手算值；
  维度不符、NaN、±Inf 分别命中 `ErrDim`/`ErrNaN`/`ErrInf`（`errors.Is` 可判定）。
- `hyper`：同种子两族对 500 个随机向量的 8 位签名逐字节相同；异种子（42 vs 43）
  存在差异；零向量签名恒为 0（内积恒 0，按约定取 0 位）；bits=8 签名恒等于
  bits=12 签名的低 8 位（前缀性质，候选随 b 单调的构造保证）；哈希计数器精确
  等于 调用次数×位数。

## 1. b × L 候选数与 top10 召回率实测矩阵

数据：N=5000、d=32、64 簇聚类（簇心×4，簇内噪声 0.6）、100 个查询、种子固定。
每格为 `平均候选数 / 平均 top10 召回率`（候选数 = 精排距离计算次数，按查询平均）：

| b \ L | 1              | 2              | 4              | 8              |
|-------|----------------|----------------|----------------|----------------|
| 4     | 436.0 / 0.896  | 795.4 / 0.971  | 1348.4 / 0.999 | 2215.7 / 1.000 |
| 8     | 84.8 / 0.742   | 118.0 / 0.880  | 181.3 / 0.983  | 305.3 / 1.000  |
| 12    | 44.1 / 0.599   | 60.0 / 0.784   | 74.9 / 0.941   | 98.0 / 0.999   |

结论：

- 候选数在每个 L 下随 b 严格下降（前缀构造保证不增，实测严格），与 N/2^b 的指数
  缩放一致；召回率随 b 上升而下降。
- b=8 时召回率随 L 单调不降：0.742 → 0.880 → 0.983 → 1.000（嵌套表构造保证）。
- 主配置 b=8、L=8：平均候选 305.3 ≤ 500（N 的 10%），未退化为暴力扫描；
  召回率 1.000 ≥ 0.6。
- 建索引哈希计算次数精确等于 N×L×b = 5000×8×8 = 320000，计数器断言通过。
- 边界：空集/单向量/K>N/K=0/维度1/零向量均符合预期；NaN、±Inf 建索引时被拒并
  计入 Skipped=3；全同 50 个向量全落一桶，候选数=50=总数，召回=1。
- 并发：8 协程查询与 7 次 AddTables 并发，`-race` 干净，无非法 ID。

## 2. 索引文件截断分类与恢复

测试夹具：dim=4、bits=4、2 张表、40 个向量，索引文件共 702 字节。
对全部 701 个截断点逐一验证，分类与字节区间：

| 截断点区间   | 分类（errors.Is）      | 截断点数 | 恢复出的表/桶        |
|--------------|------------------------|----------|----------------------|
| [1, 30)      | ErrTruncHeader         | 29       | 头部不完整，不可恢复 |
| [30, 286)    | ErrTruncPlanes         | 256      | 0 表 / 0 桶          |
| [286, 698)   | ErrTruncBuckets        | 412      | 0~1 表 / 0~3 桶      |
| [698, 701]   | ErrCRC                 | 4        | 2 表 / 7 桶（全部）  |

代表点实测：cut=285（超平面区末）→ 0 表 0 桶；cut=291（桶表区初）→ 0 表 0 桶；
cut=602（第 2 张桶表内）→ 1 表 3 桶；cut=701（CRC 区）→ 2 表 7 桶全量。
恢复结果逐截断点断言自洽：TablesRecovered == len(Tables) == len(Planes)，
桶表中无 ≥ NVecs 的悬挂 ID；人为把一个 ID 改成 0xFFFFFFFF 后，Recover 剔除并
计数 DanglingIDs=1，恢复结果无残留。超平面法向量被改成全零时，Load 报
ErrDegenerate 并指出 "table 1 bit 2"（结构校验先于 CRC）。坏魔数报 ErrBadMagic，
末字节翻转报 ErrCRC。
