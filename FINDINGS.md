# FINDINGS

## 表① 五种请求形态：执行步骤序列与临时名数

| 形态 | 请求 | 执行步骤序列 | 临时名数 |
|---|---|---|---|
| 链 | a→b,b→c | b→c, a→b | 0 |
| 二元环 | a→b,b→a | a→tpm-0, b→a, tpm-0→b | 1 |
| 三元环 | a→b,b→c,c→a | a→tpm-0, c→a, b→c, tpm-0→b | 1 |
| 目标已存在 | a→x（x 存在且不搬走） | 拒绝整批，0 步 | 0 |
| 同名冲突 | a→c,b→c | 拒绝整批，0 步 | 0 |

已由 plan 组测试证实：链序为 b→c 后 a→b 且逐步无覆盖；两类冲突拒绝且命名空间
逐元素未变；5 万请求哈希查找计数 ≤ 4(R+N)；打乱 20 次步骤序列逐元素相同；
自环 a→a 为无操作；空串与 `x/y` 按普通名字参与排序。

cycle 组测试（逐步快照检查器）证实：二元环 3 步、三元环 4 步，每步目标名都不存在；
预占 tpm-0..tpm-999 后选用 tpm-1000，结果与占用集合共存且不覆盖；
10 个互不相交环恰好用 10 个临时名。

## 表② 日志截断点：字节区间 / 分类 / 可撤销步数

（undo 组测试后按逐字节遍历实测填入）
