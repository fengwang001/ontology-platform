# FINDINGS

## 测试结论（随实现滚动更新）

- interval：起点命中、终点不命中；空/倒置区间返回 `ErrEmpty`；`Forever` 与有限终点比较行为一致。
- store：更正把旧记录事务区间截止在 `tx2` 并追加新记录；删除把有效区间截断为 `[2020,2025)`；随机 1000 次操作与 100 协程 × 100 次并发更正后，同键矩形不相交不变量均成立。
- asof：同一有效时刻 2020，事务时刻 t1 得 100、t2 得 120；连续三次更正在三个事务时刻各取到 100/120/130；事务时刻早于首条记录返回不存在；同一查询重复 1000 次结果与检查数完全一致；并发读写 `-race` 干净。

## 表一：薪资更正场景（t1 写入 100，t2 更正为 120）

| 有效时刻 \ 事务时刻 | t1 | t2 |
|---|---|---|
| 2020（区间内） | 100 | 120 |
| 2022（区间外） | 不存在 | 不存在 |

## 表二：复杂度实测（1000 键 × 10 版本 = 10000 条）

| 指标 | 实测 | 上界 |
|---|---|---|
| 点查检查记录数 | 10 | 4 × 10 = 40 |
| 单次更正触及记录数 | 12 | 10 + 2 = 12 |
