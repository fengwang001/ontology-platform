# FINDINGS

## 表② 逐字节截断分类（文件 `2\ngreeting = "hello ${name}"\nname = "file"`，长 42 字节，截 1..41）

| 分类 | 字节区间（长度） | 结论 | 行号 |
| --- | --- | --- | --- |
| 行不完整 | 1..2、28..29 | 止于行边界，尚欠后续行 | 2 / 3 |
| 键不完整 | 3..11、30..34 | 最后一行无 `=` | 2 / 3 |
| 值不完整 | 12..27、35..41 | 有 `=` 但缺结尾引号 | 2 / 3 |

每个截断点均返回包装 `source.ErrTruncated` 的可判定错误；全长 42 为唯一合法点。
