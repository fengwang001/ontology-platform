# FINDINGS

## 表 ② 游标逐比特翻转分类统计

样本游标：`(Key=2.5, ID="row-0042", Forward)`，编码长 22 字节，共 176 个
单比特翻转变体。结论：**无一被误接受**，分类如下。

| 错误类别 | 变体数 | 来源字节 |
| --- | --- | --- |
| 方向标记非法 ErrDirection | 8 | 方向字节（1B × 8bit） |
| 字段不完整 ErrTruncated | 8 | ID 长度字节（1B × 8bit） |
| 校验失败 ErrChecksum | 160 | Key(8B)+ID(8B)+CRC(4B) 共 20B × 8bit |
| 合计 | 176 | 全部被拒，0 误接受 |

说明：方向标记取值 1/2，单比特翻转不可能互相转化，故方向字节翻转
必落入 ErrDirection；ID 长度字节翻转后总长必不自洽，必落入
ErrTruncated；其余字节翻转后长度自洽但 CRC 不符，必落入 ErrChecksum。

## 表 ① 六行同排序值时的分页边界

数据集：Key 为 `1,2,5,5,5,5,5,5,6,7`（六行 Key=5），ID 为 r00..r09，
每页 3 行正向翻页。结论：十行恰好各出现一次，顺序与全量排序一致。

| 页 | 行 ID（复合键） | 页末游标边界（严格大于） |
| --- | --- | --- |
| 1 | r00(1) r01(2) r02(5) | (5, r02) |
| 2 | r03(5) r04(5) r05(5) | (5, r05) |
| 3 | r06(5) r07(5) r08(6) | (6, r08) |
| 4 | r09(7) | (7, r09) |

关键观察：第 1、2 页边界排序值同为 5，仅靠 ID 分量区分；若游标只记
排序值，Key=5 的六行必然被漏掉或重复（见 DESIGN.md 反例）。
