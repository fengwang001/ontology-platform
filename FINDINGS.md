# 测试结论

## 表二：持久文件截断分类（文件全长 20 字节）

| 截断后长度 | 损坏位置 | 分类错误 | 结论 |
|---|---|---|---|
| 1–7 | magic 头部（8B）不完整 | `durable.ErrHeaderIncomplete` | 拒绝启动 |
| 8–15 | 计数值（8B）不完整 | `durable.ErrValueIncomplete` | 拒绝启动 |
| 16–19 | CRC32（4B）不完整 | `durable.ErrCRC` | 拒绝启动 |
| 20（CRC 被改） | CRC 校验失败 | `durable.ErrCRC` | 拒绝启动 |

逐字节遍历全部 19 个截断点，逐点断言：`Open` 返回对应分类错误且返回的
计数器为 nil——**没有任何一个截断点回退到 0 或配置起点**。损坏即拒启，
杜绝大规模重号。

## 持久写失败

注入写故障后 `Advance` 返回 `ErrWrite`，内存值与磁盘值均保持推进前的
状态（临时文件 + rename，无部分覆盖）；清除故障后可继续推进。

## 表一：三种故障情形下的首个新号与空洞

| 情形 | 场景设定 | 首个新号 | 空洞区间 | 空洞长度 |
|---|---|---|---|---|
| 崩溃于分发中途 | 租 [100,200)，发到 149 崩溃 | 200（= 已租段末尾） | [150,199] | 50 |
| 租约到期 | 租 [0,100)，发 10 个后时钟跨过到期点 | 100（新号段） | [10,99] | 90 |
| 时钟回拨 | 分配后时钟回拨 1s | 无（`alloc.ErrClockRollback`，拒绝分配） | 无 | 0 |

崩溃场景验证「持久化先于分发」：计数器在租用时已推进到 200，重启后从
200 起租，崩溃前已发出的号绝不重发。到期场景验证旧段剩余号（10–99）
在后续 100 次分配中全部未出现。回拨场景验证拒绝后时钟再跨过到期点，
下一号来自新段（64），过期租约未复活。

## 性能与自适应

- 段长 1000、分发 10 万：持久写 100 次（阈值 120），本地分发每号恰好
  1 次内存操作（`ops == 分发数`，O(1)）。
- 自适应：高频阶段（时钟不推进）段长 64→4096 封顶，采样均值 ≥ 3000；
  低频阶段（每次分配前推进 2h）段长减半回落，均值 ≤ 300，且高频均值
  ≥ 4 倍低频均值。
- 并发租用：8 协程 × 4 实例共享同一计数器，4 万个号零重复——
  `AdvanceBy` 单锁内「读值 + 推进」原子化，杜绝读改写竞态。

## 全局不变量（audit 自检）

- 4 实例共享同一持久计数器、并发各发 2.5 万号：10 万号零重复、各流严格
  单调递增。
- 空洞等式精确成立：构造「崩溃丢弃 50 + 租约作废 90」场景，
  `audit.Analyze` 报出空洞区间 2 个、总长度恰好 140 = 50 + 90。
