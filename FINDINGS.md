# FINDINGS

## 舱壁（bulkhead）测试结论

- 四路径归还：成功/失败/超时/panic 各 1000 次后 `Available=4`、`InFlight=0`，无泄漏；panic 经 `errors.Is` 命中 `classify.ErrPanic`。
- 峰值：N=8、500 协程实测 `Peak=8`；N=1、100 协程实测 `Peak=1`，退化为串行。
- 队列满：N=1、Q=2 时第 4 个请求立即返回 `RejectedError`，`At` 等于注入时钟固定值，时间未被推进。
- 取消：等待中 ctx 取消后 `waiting` 归 0，新请求可重新入队而非被拒。
- 隔离：X 额度被占满时 Y 100/100 成功。
- 构造：`Concurrency<=0` 或 `Queue<0` 均报错。

## 表一：熔断状态机五条迁移

| 迁移 | 触发条件 | 测试用例 | 观测冷却序列 |
|---|---|---|---|
| 关闭→打开 | 连续失败达阈值（2） | 连续失败打开→半开→一次成功关闭 | c0 |
| 关闭→打开 | 样本≥4 且失败率≥0.5（实测 3/4=0.75） | 失败率超阈值且样本达下限打开 | c0 |
| 打开→半开 | 注入时钟推进 ≥ cooldown | 同上两例；回拨时拒绝且状态不变 | c0 |
| 半开→关闭 | 探测成功数达 HalfOpenProbes（2 次全成功） | 连续失败打开→半开→一次成功关闭 | 重置回 c0 |
| 半开→打开 | 任一探测失败 | 半开失败冷却加倍并封顶 | c0→2c0→4c0→4c0（封顶） |

并发补充：100 协程闯半开实测通过数=3（=HalfOpenProbes）；50 协程并发上报失败实测迁移次数=1，冷却只加倍一次（2c0，未重复加倍）。

## 表二：5 万次随机调用后的统计等式实测

配置：8 协程、舱壁 N=6/Q=2、单次时限 1ms、熔断（连续 10 / 样本 50 率 0.35 / 冷却 1ms 封顶 4ms / 探测 2）。实测计数：总请求 50000，真实调用 174，熔断拒绝 36193，舱壁拒绝 13633，成功 92，失败 82（可重试 60、不可重试 7、超时 15）。

| 等式 | 左侧 | 右侧 | 结果 |
|---|---|---|---|
| 总请求 = 成功+失败+熔断拒绝+舱壁拒绝 | 50000 | 92+82+36193+13633=50000 | 成立 |
| 真实调用 = 总请求 − 熔断拒绝 − 舱壁拒绝 | 174 | 50000−36193−13633=174 | 成立 |
| 分类失败和 = 失败总数 | 60+7+15=82 | 82 | 成立 |

另：熔断打开后经 Executor 连续 1000 次被拒，实测 `BreakerReject` 增量=1000，`Real` 与 `Failed` 零增量，舱壁峰值零增长；冷却过半开后一次成功即关闭。
