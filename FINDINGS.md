# 实验与测试结论

## 0. 分阶段记录
- `name`：空串与含 `/` 名字均合法；自环为无操作；目标已存在/旧名缺失均被
  拒绝且集合不变；持锁期间并发 `Rename` 阻塞、`TryLock` 失败。
- `plan`：`{a->b,b->c}` 拓扑序为 `b->c,a->b`；四类冲突检测期间集合逐元素不变；
  20 次打乱后步骤序列完全一致；5 万请求查找次数实测 < 预算 `4*(R+N)`。

## 表 1：五种请求形态的执行步骤序列与临时名数

| 形态 | 输入 | 执行步骤序列 | 临时名数 |
| --- | --- | --- | --- |
| 链 | `a->b,b->c` | `b->c, a->b` | 0 |
| 二元环 | `a->b,b->a` | 待回填 | 1 |
| 三元环 | `a->b,b->c,c->a` | 待回填 | 1 |
| 目标已存在 | `a->x`（x 在集合且不搬走） | 拒绝，无步骤 | 0 |
| 同名冲突 | `a->x,b->x` | 拒绝，无步骤 | 0 |

## 表 2：日志截断点的字节区间、分类与可撤销步数

| 截断字节区间（相对完整日志） | 分类 | 可撤销步数 |
| --- | --- | --- |
| 待 apply/undo 测试后按实测区间回填 | 头部/记录/CRC 三类 | 待回填 |
