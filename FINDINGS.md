# 实测结论（随测试推进逐条补写）

## 超平面族（hyper）

- 同种子两次 `NewFamily` 生成的法向量逐分量相同，同一向量在每张表的签名逐位一致；
  不同种子法向量不同，非零向量签名存在差异（测试 `TestFamilyTable`）。
- 哈希计数：1 个向量在 4 表 8 位下计数恰为 32 = 1×4×8。
- 全零法向量被 `FromPlanes` 检出，错误可 `errors.Is(ErrZeroPlane)` 且定位到 (表, 位)。
- 零向量与任意法向量内积为 0，按约定（>0 才置 1）签名恒为 0，测试断言通过。

## 表 1：b × L 候选数与 top10 召回率矩阵（5000 向量 / 100 查询）

| b ＼ L | L=1 | L=2 | L=4 | L=8 |
|---|---|---|---|---|
| 4  | 待测 | 待测 | 待测 | 待测 |
| 8  | 待测 | 待测 | 待测 | 待测 |
| 12 | 待测 | 待测 | 待测 | 待测 |

（每格为「平均候选数 / 平均召回」，由 search 包测试实测后回填。）

## 表 2：截断点字节区间与分类、恢复桶数

| 类别 | 字节区间 | 分类 | 恢复桶数 |
|---|---|---|---|
| 头部不完整 | 待测 | ErrHeaderTrunc | 0 |
| 超平面参数不完整 | 待测 | ErrPlanesTrunc | 0 |
| 桶表不完整 | 待测 | ErrBucketsTrunc | 待测 |
| CRC 不匹配 | 待测 | ErrCRC | 待测 |

（由 persist 包逐字节截断测试实测后回填。）
