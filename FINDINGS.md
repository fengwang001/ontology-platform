# FINDINGS — 测试结论记录

## 0. 复杂度实测（segment）

- 100000 条目段（stride=16）点查比较数实测 ≤ 29，上界 4·⌈log2 100000⌉=68，余量充足。
- 条目自带 CRC：截断在条目中部时 DecodeAt 报「条目不完整」，最大可恢复前缀恰为完整条目，半截条目一条不留。

## 1. 三种键状态 × 四种层级分布的点查结论

| 层级分布 | 有值 | 已删除 | 从未写过 |
|---|---|---|---|
| L0 有值 + L1 有旧值 | 返回 L0 值（层小者胜） | — | — |
| L0 删除标记 + L1 旧值 | — | 返回已删除，旧值不复活 | — |
| 仅 L2 有该键 | 返回 L2 值 | — | — |
| 全层无该键 | — | — | 返回从未写过（与已删除严格区分） |

level.TestGetStates 已逐项验证；另验证 L0 同层序号大者胜、空串键合法、空字节串值≠删除标记。

## 2. 截断分类（500 条目段，全长 15712B，逐字节 1..15710 实测）

布局：头 [0,36)，条目区 [36,15036)（每条 30B），索引区 [15036,15708)，footer [15708,15712)。

| 截断后长度 n | 截断点数 | 分类 | 可恢复前缀条目数 |
|---|---|---|---|
| 1..35 | 35 | 头部不完整 | 0 |
| 36..15035 | 15000 | 条目不完整 | (n-36)/30（半截条目一条不留） |
| 15036..15707 | 672 | 索引段不完整 | 500（条目区完好） |
| 15708..15711 | 4 | CRC 不匹配 | 500（仅 footer 受损） |

verify.TestTruncationClassification 对全部 15710 个截断点逐一断言分类正确，抽样断言前缀条目数。

## 3. 合并与并发（compact）

- 合并 L0+L1（L2 仍有旧值）：删除标记保留，点查仍返回已删除（merged count=3）。
- 合并 L0+L1+L2（覆盖全部层）：删除标记被物理丢弃，点查变为从未写过，段体积变小（count=2）。
- 合并中途崩溃：对临时文件逐字节截断（全部截断点），恢复后 .tmp 被清理、旧段可用、读取与合并前逐字节相同。
- 合并与 4 路并发读同时进行：任意时刻点查均返回正确值，`-race` 干净。

## 4. 演示（cmd/demo）

`go run ./cmd/demo` 退出码 0，9 项判定全 OK：多版本胜出、三态区分、部分合并保留删除标记（count=3）、全层合并丢弃（count=2）、点查比较 14≤68、归并比较 6992≤24000 且驻留峰值 5≤5、四类截断各一例、崩溃恢复后读取一致、L1 重叠 [k,m] 被检出。
