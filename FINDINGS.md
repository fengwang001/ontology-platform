# 测试结论

## 逐组结论

- `name`：空串/`/`/反斜杠/NUL/Unicode 均合法；集合比对顺序无关；
  改名三类结果（成功、源缺失、目标存在，force 可覆盖）正确；持写锁时并发写阻塞，
  释放后继续。`go test -race` 通过。
- `cycle`：空闲时取 `.rename-tmp-0`；占用现有名 ∪ 目标名即重试；
  人为占满第一族 65536 个候选后自动换第二族成功；两族全占满返回
  `ErrNoTempName` 而非覆盖数据。
- `plan`：四类冲突均可 `errors.Is` 区分且检测后命名空间逐元素未变；
  链拓扑序正确、二/三元环各用 1 个临时名且逐步目标不存在；
  5 万请求 map 读次数 ≤ 4(R+N)；10 个不相交环临时名恰好 10；
  打乱 20 次步骤序列逐元素一致。
- `apply`：链/环/自环/空集执行后集合正确；撤销后与执行前逐元素相同；
  注入第 1、中间、最后一步失败，已执行步逆序回滚、命名空间复原、日志文件删除。

## 表① 五种请求形态的执行步骤与临时名数

| 形态 | 请求 | 执行步骤序列 | 临时名数 |
|------|------|--------------|----------|
| 链 | a→b,b→c | b→c, a→b | 0 |
| 二元环 | a→b,b→a | a→.rename-tmp-0, b→a, .rename-tmp-0→b | 1 |
| 三元环 | a→b,b→c,c→a | a→.rename-tmp-0, c→a, b→c, .rename-tmp-0→b | 1 |
| 目标已存在 | a→b（b 已存在且不搬走） | 拒绝整批（0 步） | 0 |
| 同名冲突 | a→c,b→c | 拒绝整批（0 步，报 a,b） | 0 |

## 表② 日志截断点分类（以实际成功日志字节布局为准）

| 字节区间 | 分类 | 可撤销步数 |
|----------|------|-----------|
| _待 undo 组补充_ | | |
