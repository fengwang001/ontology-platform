# 结论记录

## 表 1：五种请求形态的步骤序列与临时名使用

| 形态 | 请求 | 结果 / 执行步骤序列 | 临时名数 |
| --- | --- | --- | --- |
| 链 | a→b, b→c | b→c ; a→b（拓扑序，全程无覆盖） | 0 |
| 二元环 | a→b, b→a | a→tmp~0 ; b→a ; tmp~0→b（每步目标名均不存在） | 1 |
| 三元环 | a→b, b→c, c→a | a→tmp~0 ; c→a ; b→c ; tmp~0→b（每步目标名均不存在） | 1 |
| 目标已存在 | ns={x,a}, x→a | 拒绝：ErrTargetExists，命名空间逐元素未变 | 0 |
| 同名冲突 | ns={x,y}, x→a 与 y→a | 拒绝：ErrDuplicateTarget，命名空间逐元素未变 | 0 |

## 表 2：日志截断点分类

样本日志：3 步 `{a→x, b→y, c→z}`，头行 23 字节（`ONT1 00000003 <crc>\n`），
每条记录 6 字节（`hex(from) hex(to)\n`），总长 41 字节。对 L=1..40 逐字节截断：

| 截断长度 L（字节区间） | 分类 | 可恢复前缀（步） | 可撤销步数 / 无法撤销 |
| --- | --- | --- | --- |
| 1–22 | ErrHeaderIncomplete | 0 | 0 / 全部（总数未知） |
| 23（头行边界） | ErrCRCMismatch | 0 | 0 / {0,1,2} |
| 24–28（记录 0 中间） | ErrRecordIncomplete | 0 | 0 / {0,1,2} |
| 29（记录 0 边界） | ErrCRCMismatch | 1 | 1 / {1,2} |
| 30–34（记录 1 中间） | ErrRecordIncomplete | 1 | 1 / {1,2} |
| 35（记录 1 边界） | ErrCRCMismatch | 2 | 2 / {2} |
| 36–40（记录 2 中间） | ErrRecordIncomplete | 2 | 2 / {2} |

三类错误均 errors.Is 可区分；撤销按最大可恢复前缀逆序进行，缺失尾部步骤逐个报告。

## 备注

- plan 组：四类冲突均 errors.Is 可判定，检测前后命名空间快照逐元素相同（含空请求、
  自环无操作、空串名、含 `/` 名、重复旧名拦截「请求数远大于名字数」等边界）。
- 排序确定性：20 次打乱构造顺序，步骤序列与环划分逐元素相同。
- 复杂度：5 万请求链的查找计数低于 4×(请求数+涉及名字数) 上限。
- cycle 组：二元/三元环各借 1 个临时名且每步目标名不存在；tmp~0..99 被预先占用时
  自动重试到 tmp~100 成功；10 个互不相交的环恰好产生 10 个临时名（等于环数）。
- apply/undo 组：四种形态执行后撤销，命名空间逐元素还原；第 1/3/5 步注入失败均完整
  回滚且日志删除；41 字节日志的全部 40 个截断点都被正确分类；重复撤销幂等；
  并发修改在批次执行期间被阻塞、批次结束后生效。
