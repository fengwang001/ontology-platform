# 测试结论

## 表①：三种 from 位置（锚点间隔 N，段首锚点序号=段首序号）

| from 位置 | 选用锚点 | 跳过事件数 | 是否用上索引 |
| --- | --- | --- | --- |
| 恰好等于锚点序号 | 该锚点本身 | 0 | 是（直接定位） |
| 两个锚点之间 | 不大于 from 的最大锚点 | 1..N-1 | 是，跳过数严格 < N |
| 小于第一个锚点（< 段首序号） | 无（ErrEmpty） | 0（从段头后扫描） | 否，回退段头；跨段时改从更早段起 |

说明：「最接近 from」的锚点可能在 from 之后，从其后扫描会漏掉它之前、from 之后的事件，
故必须取「不大于 from 的最大锚点」。
