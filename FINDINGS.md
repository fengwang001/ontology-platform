# FINDINGS — 测试结论记录

## 表一：十种输入组合的合并结果与冲突分类

（全部经 `merge3.TestCombinations` 表驱动验证通过；另含 5 条边界用例）

| # | 输入组合 | 合并结果 | 冲突分类 |
|---|----------|----------|----------|
| 1 | 左改右未改 | 取左侧修改 | 无冲突 |
| 2 | 两侧同值修改 | 取该值 | 无冲突 |
| 3 | 两侧异值修改同字段 | 取左侧（已上报） | FieldValue |
| 4 | 左删右改 | 记录删除（已上报） | DeleteVsModify |
| 5 | 两侧同删 | 删除 | 无冲突 |
| 6 | 改不同字段 | 两修改都生效，其余字段保持祖先值 | 无冲突 |
| 7 | 两侧同键新增同内容 | 取该内容 | 无冲突 |
| 8 | 两侧同键新增异内容 | 取左侧（已上报） | AddAdd |
| 9 | 祖先缺失（两侧各自新增） | 按 #7/#8 处理 | 视内容是否一致 |
| 10 | 字段类型不一致 | 取左侧（已上报） | TypeMismatch（报告两侧类型） |

## 表二：截断点分类结论

对样例序列化输出（全长 69 字节）逐截断点 1..68 循环验证，全部落入三类且均可 `errors.Is` 判定：

| 截断区间 | 分类 | 截断点数 |
|----------|------|----------|
| 1–8 | 头部不完整 `ErrHeaderIncomplete` | 8 |
| 9–64 | 记录不完整 `ErrRecordIncomplete` | 56 |
| 65–68 | CRC 不匹配 `ErrCRCMismatch` | 4 |

另验证：破坏 CRC 字节、破坏负载首字节均判为 `ErrCRCMismatch`；落盘 `WriteFile`/`ReadFile` 往返一致。

## 附：doc 包测试结论

- 规范化编码与插入顺序无关；空串值与字段不存在在编码层可区分。
- 空串键、空串字段名合法；`Decode` 对全部截断点（循环 1..len-1）均报错。
- `Value` 相等性含类型：`Str("1") != Num(1)`。

## 附：merge3 包测试结论

- 边界语义：某侧为空 = 该侧删除祖先全部记录（快照语义，已按定义测试）。
- 确定性：三集合插入顺序打乱 20 次，规范化输出逐字节相同。
- 复杂度：3×5 万键合并，查找次数 150000 ≤ 4×50000；报告阶段访问数 = 冲突数。
- 矛盾输入：同侧同键既删既改，`Validate` 返回可 `errors.Is` 判定的 `ErrContradictory`。
