# FINDINGS — 测试结论（随各包测试完成逐步补充）

## chunk

- 滚动校验和在 300 字节随机数据、bs=16 上对全部 285 个偏移逐一与从头重算相等。
- 弱校验和基本运算实测 = 2L − bs（初始窗口 bs 次 + 每次推进 2 次），不超过 4L。
- 碰撞对 `[1 2 3 4]` / `[4 3 2 1]`：弱和同为 10，强和（SHA-256 前 16 字节）不同。
- 分块边界：空数据 0 块；整数倍无尾块；非整数倍末块不满；bs 大于数据长度时 1 个
  短块；bs 为 0 或负数返回 `chunk.ErrBlockSize`。

## 表 ① 六种源/目标关系下的补丁构成（bs=8）

| 关系               | 源/目标大小（字节） | 新数据字节数 | 复用块数 |
| ------------------ | ------------------- | ------------ | -------- |
| 完全相同           | 64 / 64             | 0            | 8        |
| 中间差一块         | 64 / 64             | 8（≤ 2·bs）  | 7        |
| 目标是源的前缀     | 80 / 48             | 32           | 6        |
| 源是目标的前缀     | 48 / 80             | 0            | 6        |
| 完全不同           | 40 / 40             | 40           | 0        |
| 空（源空/目标空/均空） | 0/64, 64/0, 0/0  | 0 / 64 / 0   | 0        |

## diff

- 弱碰撞对 `[1 2 3 4]`/`[4 3 2 1]`：弱命中后强校验不通过，0 复用、4 字节新数据，
  应用结果与源端逐字节一致——碰撞被强校验挡住。
- 强校验计算次数 ≤ 弱命中次数（中间差一块场景实测 8 = 8）。
- 同一（源，目标）连跑 20 次补丁逐字节相同；16 协程并发 diff 互不干扰，`-race` 干净。

## patch

- 测试补丁：目标 "AAAABBBBCCCC"（3 块），源 "AAAAXYBBBBZ"，总长 63B，头 36B。
- 全部 62 个截断点逐一验证：均返回可判定错误，且 `ApplyFile` 失败后目标文件
  逐字节保持不变（先写临时文件再 rename，失败路径不触碰目标）。
- 越界块号 999（修好 CRC）→ `ErrOutOfRange`；payload 翻转 1 字节 → `ErrCRCMismatch`；
  签名后目标被改动 → `ErrMismatch`；magic 损坏 → `ErrHeaderIncomplete`。

## 表 ② 截断点分类（补丁总长 63B，头 36B，CRC 4B）

| 截断点（保留字节数） | 分类                   | 目标端保持不变 |
| -------------------- | ---------------------- | -------------- |
| 1–35                 | 头部不完整             | 是             |
| 36–45, 48–57         | 指令不完整             | 是             |
| 46–47, 58            | 数据段不完整           | 是             |
| 59–62                | CRC 不匹配             | 是             |
