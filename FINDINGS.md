# 测试结论（每组测试通过后立即补写）

## 表一：`A→B→C` + 并行慢任务 S 在两种模式下的终态分布

图：A 立即失败；B 依赖 A；C 依赖 B；S 无依赖且运行很慢（A 失败时 S 正在跑）。

| 任务 | FastFail | 原因 | BestEffort | 原因 |
|------|----------|------|------------|------|
| A | Failed | 原始错误 | Failed | 原始错误 |
| B | Skipped | A | Skipped | A |
| C | Skipped | A（非 B） | Skipped | A（非 B） |
| S | Canceled（已开始过） | ctx 取消 | Success（无关分支跑完） | — |

（以上行为待 exec 集成测试落实验证后勾选）

## 表二：故障注入结论

| 注入 | 结论 | 状态 |
|------|------|------|
| 环（含自环） | 执行前检出，CycleError.Path 闭合且每条边均在输入中；自环同样检出 | 已验证（graph 表驱动 8 例 + 1000 层链不栈溢出，全部通过） |
| 任务 panic | 待 exec 测试 | 待验证 |
| 多任务同时失败 | 待 exec 测试 | 待验证 |
| 取消后写回 | 待 exec 测试 | 待验证 |
