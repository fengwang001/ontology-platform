# 实测结论

以下结论均由 `go test ./...` 中对应测试实际验证（时钟为注入的假时钟，序列确定）。

## 表一：三类异常情形下的首个新号与空洞

场景统一为：段长固定，租得 `[100,200)`（租期 10s，起点 t0）。

| 情形 | 注入序列 | 首个新号 | 空洞长度 | 判定 |
| --- | --- | --- | --- | --- |
| 崩溃于分发中途 | 租 `[100,200)`，发 100..149 后丢弃实例，同目录重启 | 200（= 已租段末尾） | 50（150..199 在途丢弃） | 绝不回退到 150，无重号 |
| 租约到期 | 租 `[0,10)` 发 0..2，时钟推进 11s 跨期 | 10（新段 `[10,20)` 起点） | 7（旧段剩余 3..9 永不出现） | 旧段作废，重新租用 |
| 时钟回拨 | 分配后把时钟回拨 1 小时 | 无（拒绝分配） | 0 | 返回 `ErrClockBackwards`，租约不复活 |

崩溃与作废产生的空洞可精确计量：`audit` 测得空洞总长度 == 作废号段剩余长度
之和 + 崩溃丢弃长度（实测 7 + 9 = 16，精确断言通过）。

## 表二：持久文件逐字节截断的分类（文件全长 16 字节）

布局：`[0,4) magic | [4,12) 值 | [12,16) CRC32`。对截断点 1..15 逐一验证：

| 截断后字节区间 | 分类 | errors.Is 判定 | 启动行为 |
| --- | --- | --- | --- |
| 1..3 | 头部不完整 | `ErrHeaderIncomplete` | 拒绝启动 |
| 4..11 | 值不完整 | `ErrValueIncomplete` | 拒绝启动 |
| 12..15 | CRC 缺失/不匹配 | `ErrCRCMismatch` | 拒绝启动 |

全部 15 个截断点均为「拒绝启动」，无一例外；绝不从 0 重建计数器（否则整段
历史号段会被重发）。另：翻转值区域任一字节 → `ErrCRCMismatch`，同样拒绝。

## 性能与资源实测

| 指标 | 实测 | 阈值 | 判定 |
| --- | --- | --- | --- |
| 10 万分发（maxSeg=1000）的持久写次数 | 111 | <= 120 | 通过（约为分发数的 1/900） |
| 本地分发操作数 | 每次恰 1 次（ops + 租用次数 == 分发总数） | 常数 | O(1) 成立 |
| 段长自适应（min=1, max=256） | 高频均段长 125.0，低频约 3.9 | 高频 > 4x 低频 | 差异显著 |
| 4 实例 x 2.5 万并发分发 | 10 万号零重复、实例内严格递增 | 零重复 | 通过 |
| 8 协程并发租用（各租 10） | 80 个号互不重叠、并集连续 | 无重叠 | 通过 |
