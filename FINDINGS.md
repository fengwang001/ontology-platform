# FINDINGS

## ① 六行同排序值：每页行 ID 与复合键边界

数据集 10 行：ID `t1..t4` 的 V=0，ID `a..f` 的 V=1（6 行同值），pageSize=3。
排序后复合键边界如下（`>` 表示下一页严格大于该复合键）：

| 页 | 行 ID（页内升序） | 末行复合键 (V,ID) | 下一页边界 |
|----|------------------|-------------------|------------|
| 1 | t1, t2, t3 | (0,"t3") | (V,ID) > (0,"t3") |
| 2 | t4, a, b | (1,"b") | (V,ID) > (1,"b") |
| 3 | c, d, e | (1,"e") | (V,ID) > (1,"e") |
| 4 | f | (1,"f") | (V,ID) > (1,"f") → 空 |

结论：10 行恰好各出现一次；同 V=1 的 6 行（a..f）由 ID 字典序在复合键上展开，
跨越第 2/3/4 页，无整批跳过，也无上一页行重复。

## ② 游标比特翻转分类统计

合法游标 `Encode((2.5,"row-1"), Forward)` 共 23 字节 = 184 个比特，逐比特翻转：

| 分类 | 变体数 | 被误接受 |
|------|--------|----------|
| ErrIncomplete（字段不完整/magic） | 8 | 0 |
| ErrDirection（方向标记非法） | 8 | 0 |
| ErrChecksum（校验失败） | 168 | 0 |
| 合计 | 184 | 0 |

说明：magic 字节的 8 位翻转全部落入 ErrIncomplete；dir 字节翻成 0 或 3 的 8 个位置
（2 个取值 × 8 位）落入 ErrDirection；其余 168 位翻转改变长度以外字段且方向仍合法，
落入 ErrChecksum。无任何变体被当成合法游标接受。
