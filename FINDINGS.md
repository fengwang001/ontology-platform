# FINDINGS

测试环境：go1.26.5 linux/arm64，`go test -count=1 ./...` 与 `-race` 全绿。

## 表① 写入失败注入（n=100，粒度 N=10，失败为一次性）

| 失败位置 k | 进度停留点 | 续传重写条数 | 最终结果比对 |
|---|---|---|---|
| 第 1 条 | 0（无区间） | 0 | 与无故障运行逐字节相同 |
| 中间（第 50 条） | 40 | 9 | 与无故障运行逐字节相同 |
| 最后一条（第 100 条） | 90 | 9 | 与无故障运行逐字节相同 |

结论：失败后进度停在失败点之前最后一个已刷新区间端点；续传从该点
继续，重写数 < N；最终结果与无故障运行一致。

## 表② 进度文件逐字节截断分类

样本文件 42B：`HDR b 10 0\n`（[0,11)）+ `IVL 0 5\n`、`IVL 7 9\n`
（[11,27)）+ `CRC <n>\n`（[27,42)）。遍历截断点 cut∈[1,41]：

| 截断点字节区间 | 分类结论 | 回退后的进度值 |
|---|---|---|
| [1,11)（头部行内） | 头部不完整 | 空进度，等价从头开始 |
| (11,19)、(19,27)（IVL 行内） | 区间记录不完整 | 截断点之前完整 IVL 行表示的区间 |
| cut=11、19（行边界）与 [27,42)（CRC 行） | CRC 缺失/不匹配 | 全部已解析区间（最终以存储为准） |

三类计数：头部 10、区间 14、CRC 17，合计 41 = 文件长度-1，全覆盖。
截断后续传结果与不崩溃时逐字节相同（由 importer 以存储为准保证）。
