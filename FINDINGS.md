# 测试发现记录

## 0. record 包

表驱动覆盖：空记录、nil 字段、深度 1、数组内嵌套映射、深度 16 接受、
深度 18 拒绝（`ErrTooDeep`）、自引用映射拒绝（`ErrCycle`）、JSON 往返字节一致。
结论：全部通过；超限与环在构造期检出，无栈溢出/死循环。

<!-- 后续表格在各组测试完成后立即补入 -->

## 1. rule 包

表驱动覆盖：路径切分（`a.b` / `a\.b` / `arr.0.k`）、字面点号键与嵌套路径
得到不同 Trie 节点、路径不存在/中段非映射均未命中、值规则精确查表、同路径或
同值两种方式编译期报 `ErrConflict`（含两条规则名）。
复杂度：100 条规则 × 10 万条记录 × 每记录 5 个路径探测，MatchCount=500000，
与规则数无关，满足 `记录数*(字段数+C)` 上界（C=8，上界 130 万）。

## 2. mask 包

表驱动覆盖三方式不变量：replace 输出固定串且长度改变；hash 同值相同、
异值不同、结果不含原值；truncate 保留前 3 字符并带 `...<truncated>`；空串值仍被替换。
覆盖性：值 `"SECRET"` 出现在 3 处（普通字段 / 嵌套映射 / 数组内映射引用），
脱敏后 3 处全部为 `REDACTED`，原串零残留；非目标字段（`keep`、`n`）序列化字节
与期望完全一致；路径规则只动目标路径；源记录不被修改（深拷贝隔离）。
