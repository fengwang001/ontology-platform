# FINDINGS — 测试结论记录

## 表 1：五种请求形态的执行步骤与临时名使用

| 请求形态 | 执行步骤序列 | 临时名数 |
| --- | --- | --- |
| 链 `{a→b, b→c}` | `b→c, a→b`（末端先行，全程无覆盖） | 0 |
| 二元环 `{a→b, b→a}` | `a→tmp, b→a, tmp→b` | 1 |
| 三元环 `{a→b, b→c, c→a}` | `a→tmp, c→a, b→c, tmp→b` | 1 |
| 目标已存在 `{a→b}`（b 存在且不搬走） | 拒绝整批，0 步，命名空间不变 | 0 |
| 同名冲突 `{a→c, b→c}` | 拒绝整批，0 步，报出旧名 a、b | 0 |

## 表 2：日志截断点分类（日志 = 16B 头 + bodyLen B 记录区 + 4B CRC，全长 L）

| 截断点字节区间 | 分类结论 | 可撤销步数 |
| --- | --- | --- |
| `[1, 15]` | 头部不完整 `ErrHeaderIncomplete` | 0（无法解析任何记录） |
| `[16, 16+bodyLen-1]` | 记录不完整 `ErrRecordIncomplete` | 截断点之前已完整的记录数 |
| `[16+bodyLen, L-1]` | CRC 不匹配 `ErrCRCMismatch` | 全部 count 步（记录区完整） |

## 逐组结论（随测试完成补充）

- name 包：空串与 `/`、`\` 均为合法普通字符，NUL 非法；增删查改与
  顺序无关的集合比对全部通过。
- cycle/plan：链末端先行、二元/三元环各借 1 个临时名且每步目标名均
  不存在；临时名同形名字被占 8 个仍成功；四类冲突均可 `errors.Is`
  区分且检测后命名空间逐元素不变；打乱 20 次步骤序列一致；10 个环
  恰好 10 个临时名；5 万请求 lookups=50 万 < 60 万上界。
- apply/undo：执行后撤销命名空间逐元素相同；第 1/中/末步注入失败均
  完整回滚；38 字节日志逐字节截断 37 个点，分类与表 2 完全一致，
  记录区截断按最大可恢复前缀撤销并报告 Lost；重复撤销幂等；批量
  执行期间并发 Add 被阻塞至批结束（race 通过）。
