# FINDINGS — 测试核对结论

## 表① 五个聚合器的撤回能力（`agg` 声明 × `view` 实测）

| 聚合器 | 插入可增量 | 删除可增量 | 删除需要成员 | 实测重算次数 |
| --- | --- | --- | --- | --- |
| Count | 是 | 是 | 否 | 0（10 万插入 + 5000 删除） |
| Sum | 是 | 是 | 否 | 0（同上） |
| Min | 是 | 仅当被删值≠当前极值 | 是 | 恰 3（仅 3 次删到当前 Min） |
| Max | 是 | 仅当被删值≠当前极值 | 是 | 0（该测试未删到当前 Max） |
| DistinctCount | 是 | 否 | 是 | 每次删除都重算（符合声明） |

重算扫描量：3 次 Min 重算共访问 100000+99999+99998=299997 条成员，
恰为三次触发时该组（且仅该组）的成员数，未扫描其他组。

## 表② 逐字节截断分类（200 条变更的日志，payload 30B，记录步长 38B，总长 7612B）

记 s(i)=12+38i 为第 i 条记录（0 起）的起始偏移，截断点 p 表示保留前 p 字节。

| 截断点字节区间 | 分类结论 | 重放后生效记录数 |
| --- | --- | --- |
| p ∈ [1, 11] | 头部不完整 | 0 |
| p ∈ [s(i)+1, s(i)+3] | 长度前缀不完整 | i |
| p ∈ [s(i)+4, s(i)+33] | 记录体不完整 | i |
| p ∈ [s(i)+34, s(i)+37] | CRC 不匹配 | i |
| p = s(i)（记录边界） | 合法短日志 | i |

遍历 p=1..7610 全部截断点，分类与生效条数均与上表一致；四类错误均为可判定
的哨兵错误。截断日志重放后，视图恰等于完整记录前缀的全量重算结果，半截记录
不生效。
