# FINDINGS

## 测试进展

- stack：空栈判 Invalid；相邻同帧去重；非相邻递归帧保留；恰达上限不截断、上限加一
  截断且末帧带标记；截断帧数正确；输入不被原地修改。全部通过。
- tree：Σself 恒等于总采样数；深 3 单样本 Σtotal=3 > Σself=1；递归栈产生 2 个 F
  节点；10 万条深 20 栈查找计数 2,000,000 ≤ 上界 8,000,000；空栈计入 invalid。通过。
- attrib：A→F→G→F→H 函数级 total(F)=1（最外层），self 累加但 total 不重复；
  零样本返回空切片；按 self/total 排序正确；排除 F 后子树 G 提升且 F 消失；
  归因重建次数恒为 0。通过。

## 表① 递归栈 A→F→G→F→H 的 self/total（单条样本）

| 节点路径 | self | total |
|---|---|---|
| A | 0 | 1 |
| A→F(外层) | 0 | 1 |
| A→F→G | 0 | 1 |
| A→F→G→F(内层) | 0 | 1 |
| A→F→G→F→H | 1 | 1 |

函数级 total(F) = 1（仅最外层 F），不是 1+1=2。（attrib 测试后复核）

## 表② 落盘截断点分类

（dump 测试后按真实字节区间填写）
