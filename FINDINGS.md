# 实测结论

## pattern 组（已完成）

- 相邻 `%` 折叠：`%%%a` 规范化后 2 个 token，与 `%a` 的 token 序列逐项相等。
- 三种转义 `\%` `\_` `\\` 均解析为单字面量 token。
- `\x` 报 ErrInvalidEscape（含字节位置），`\` 结尾报 ErrTrailingEscape。
- 非法 UTF-8 报 ErrInvalidUTF8，侧别 pattern，字节偏移正确。
- 三类错误经 errors.Is 两两可区分。

## budget 组（已完成）

- 计数字段非导出，包内白盒测试断言计数精确、上限拒绝越界步。
- limit<=0 表示不限；两个 Meter 实例互不串台。

## match 组（已完成）

- 单锚点迭代匹配，无递归；`_` 按码点消费（café、😀 用例通过）。
- 非法 UTF-8 文本报 side=text 与字节偏移；超限返回 budget.ErrExhausted。
- 重复 1000 次结果与步数完全一致；8 协程并发 -race 干净、步数不串台。
- 匹配不修改传入模式与文本。

## audit 组（已完成）

- 固定种子 42 生成 2000 组（模式, 文本），与朴素递归参考逐对一致。
- 同种子重跑序列完全一致，换种子序列改变，结果可复现。

## 表 1：`%a%a...%b` 步数 vs 上界（4·m·n，文本为 20 个 a）

| `%` 个数 k | 模式 token 数 m | 实测步数 | 上界 4·m·n | 结论 |
|---|---|---|---|---|
| 2 | 4 | 23 | 320 | 远低于上界 |
| 4 | 8 | 25 | 640 | 远低于上界 |
| 8 | 16 | 29 | 1280 | 远低于上界 |

步数随 k 近似线性（23→25→29），而朴素递归分支数为 2^k 量级。

## 表 2：转义与边界八情形

| 情形 | 输入 | 期望 | 实际 |
|---|---|---|---|
| `\%` 字面量 | 模式 `\%` vs 文本 `%` | 匹配 | 匹配 ✓ |
| `\_` 字面量 | 模式 `\_` vs 文本 `_` | 匹配 | 匹配 ✓ |
| `\\` 字面量 | 模式 `\\` vs 文本 `\` | 匹配 | 匹配 ✓ |
| 非法转义 | 模式 `\x` | ErrInvalidEscape@0 | ErrInvalidEscape@0 ✓ |
| 结尾反斜杠 | 模式 `abc\` | ErrTrailingEscape@3 | ErrTrailingEscape@3 ✓ |
| 空模式 | `""` vs `""` / `"a"` | 匹配 / 不匹配 | 匹配 / 不匹配 ✓ |
| `%` 任意串 | `%` vs `""` / `"abc"` | 匹配 / 匹配 | 匹配 / 匹配 ✓ |
| `_` 非空 | `_` vs `""` | 不匹配 | 不匹配 ✓ |
