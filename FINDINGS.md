# FINDINGS — 测试结论记录

## 表一：三种 from 位置的定位行为（N=128，100000 事件）

| from 位置 | 跳过事件数 | 是否用上索引 | 结论 |
|---|---|---|---|
| 恰等于锚点序号 | 0 | 是（锚点即起点） | 直接命中，零扫描 |
| 位于两锚点之间 | 7（恒 < N） | 是（取不大于 from 的最大锚点） | 200 个随机 from 全部 < 128 |
| 小于首锚点 | 0 | 否（无锚点可用，从头扫） | from 被钳到最小序号，不报错 |

## 表二：截断点分类与 repair 结果（500 事件段，记录定长 24 B，头 24 B）

| 截断字节区间 | 分类 | repair 前条数 | repair 后条数 |
|---|---|---|---|
| t ∈ [1, 23] | 头部不完整 | 不可修复（报错） | — |
| t = 24+24k | 完整边界（nil） | 无需修复 | k |
| t = 24+24k+r, r ∈ [1,3] | 长度前缀不完整 | 500 | k |
| t = 24+24k+r, r ∈ [4,19] | 事件体不完整 | 500 | k |
| t = 24+24k+r, r ∈ [20,23] | CRC 不匹配 | 500 | k |

## 其他结论（随测试完成补充）

- event 编解码：往返一致，短头/截断体错误可 `errors.Is` 区分（event 包测试通过）。
- 逐字节截断：12023 个截断点全部分类正确，四类错误均可 `errors.Is` 区分。
- repair 实例：cut=6055（第 252 条记录体内）→ 条数 500 改正为 251，差值 249，修复后顺序读无错。
- 索引重建：删除后由段重建，与原索引逐字节相同（sparse、repair 两处断言）。
- 索引篡改：锚点偏移 +3 指向记录中间，定位检出并回退全段扫描，结果正确且 IndexInvalid 标注存在。
- 序号缺口：构造缺失序号 500 的两段，CheckContinuity 报 ErrSeqGap 并指出缺口区间 [500,500]。
- 并发：5000 次追加与持续 ReplayPrefix 并发，每次回放均为连续无缺口前缀，`-race` 干净。
