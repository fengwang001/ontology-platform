# 凸多边形交集 NOTES

## 第三节推导（五行分步表，顶点均逆时针）

| # | A | B | 交集 |
|---|---|---|---|
| P1 | [0,4]×[0,4] | [2,6]×[2,6] | (2,2)(4,2)(4,4)(2,4) |
| P2 | [0,2]×[0,2] | [3,5]×[3,5] | 空（包围盒不相交，O(1) 判空） |
| P3 | (0,0)(4,0)(0,4) | [1,5]×[1,5] | (1,1)(3,1)(1,3) |
| P4 | [0,4]×[0,2] | [2,6]×[0,4] | (2,0)(4,0)(4,2)(2,2) |
| P5 | [0,4]×[0,4] | [4,8]×[0,4] | 空（仅共边 x=4，面积为零） |

- (甲) P1 交集为 (2,2)(4,2)(4,4)(2,4)。符号写反后，B 的右边 (6,2)→(6,6) 变成补半平面 x≥6，A 全部顶点 x≤4 被整体裁掉，交集错成**空**（同理上边补半平面 y≥6 也会裁空）。
- (乙) P2 交集为**空**。若不检测包围盒不相交/空集，裁剪后剩 0 个顶点，会把这 0 顶点的伪多边形当有效结果返回（Empty()=false，Vertices()=nil 却非空标志）。
- (丙) P5 交集只是线段 x=4、y∈[0,4]，面积为零 → 判**空**。若不判退化，会返回 2 顶点退化"多边形" (4,0)(4,4)，面积为零却被当成非空结果。

## 四条不变量（位置 + 钉住它的测试）

1. 与朴素裁剪一致：`cint.Intersect` 本身就是逐边朴素裁剪（cint/cint.go），测试 `TestNaiveConsistency`（api_test.go，随机凸多边形对对照无包围盒预判、无提前退出的朴素参照实现）。
2. 凸性自洽：半平面裁剪保凸（cvex.ClipLeft），结果顶点满足 A、B 全部半平面由裁剪定义保证；测试 `TestCoverage` 断言结果严格凸且顶点落在双方所有半平面内。
3. 覆盖正确：结果 ⊆ A∩B 由逐边裁剪保证；严格内部点必在结果内，测试 `TestCoverage` 网格采样断言。
4. 失败不留痕：`NewPolygon` 先校验后构造，非法输入返回 nil + 哨兵错误（api/api.go）；测试 `TestRejections`、`TestNoPartialResult`。
