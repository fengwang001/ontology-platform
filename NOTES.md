# Fisher-Yates 均匀洗牌推导

## 下标选择范围

正确做法（从后往前）：第 i 步（i = n-1 … 1）在 `[0, i]` 中选 j，交换 arr[i] 与 arr[j]。
等价地，从前往后则在 `[i, n)` 中选。每一步只能从「尚未确定位置」的部分里抽。

## 为什么「全范围 `[0, n)` 选」不均匀

- 错误实现每一步都在 n 个下标里选，共 n 步，产生 n^n 条等概率选择路径，每条概率 1/n^n。
- 但最终结果只有 n! 种排列。每个排列的概率 = 映射到它的路径数 / n^n。
- n>=3 时 n! 不能整除 n^n（例如 n=4：4^4=256，4!=24），由鸽巢原理，各排列对应的路径数不可能相同，
  于是某些排列概率翻倍、另一些为零；实测 n=4 时最大/最小频次比远超 5。

## 为什么「从 `[0, i]` 选」恰好均匀

- 第 i 步有 i+1 种选择，总路径数为 n·(n-1)·…·1 = n!，且每条路径与最终排列一一对应
  （每一步把选中元素固定到位置 i，交换序列唯一决定排列；给定排列，交换序列也被唯一回溯）。
- 故每种排列恰好一条路径，概率 1/n!。n=4 时 24 种排列各约 120000/24=5000 次，最大/最小比 <= 1.5。
- i 从 n-1 走到 1，共 n-1 步，因此随机源恰好被调用 n-1 次（不多不少）。

## 代码与测试索引

（实现完成后回填）
