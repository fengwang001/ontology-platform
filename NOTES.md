# Fenwick 树（BIT）笔记

## 为什么内部下标必须 1 起算

- Fenwick 树用 `lowbit(i) = i & -i` 决定下一跳：
  - 更新：`i += lowbit(i)`（向根传播）
  - 查询：`i -= lowbit(i)`（向前缀回溯）
- 若直接用 0 起算且不平移，第一次更新下标 0 时：
  - `lowbit(0) = 0 & -0 = 0`
  - 更新循环变成 `i += 0`，`i` 永远是 0，原地死循环；
  - 树的第 0 槽也不是任何 `lowbit` 跳转可达的有效节点。
- 结论：内部数组必须 1 起算、槽 0 闲置；对外 API 仍可 0 起算，
  入口统一做 `j := i + 1` 平移。`PrefixSum(-1)` 定义为返回 0（空前缀）。
