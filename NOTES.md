# NOTES — 支持分组键变更的增量 GROUP BY

## 八步推导（maxGroups=3，初始为空；视图只列 count>0 的组）

| 步 | 操作 | 行表（之后） | 视图（之后） | 输出 |
|---|---|---|---|---|
| 1 | Insert(1,a,5) | 1:(a,5) | a:(5,1) | +(a,5,1) |
| 2 | Insert(2,a,-5) | 1:(a,5) 2:(a,-5) | a:(0,2) | -(a,5,1) +(a,0,2) |
| 3 | Insert(3,b,7) | +3:(b,7) | a:(0,2) b:(7,1) | +(b,7,1) |
| 4 | Update(1,a,8) | 1→(a,8) | a:(3,2) b:(7,1) | -(a,0,2) +(a,3,2) |
| 5 | Update(2,b,3) | 2→(b,3) | a:(8,1) b:(10,2) | -(a,3,2) +(a,8,1) -(b,7,1) +(b,10,2) |
| 6 | Update(1,c,8) | 1→(c,8) | b:(10,2) c:(8,1) | -(a,8,1) +(c,8,1) |
| 7 | Update(3,b,7) | 不变 | 不变 | 无 |
| 8 | Delete(2) | 1:(c,8) 3:(b,7) | b:(7,1) c:(8,1) | -(b,10,2) +(b,7,1) |

- (甲) 第 5 步输出 `-(a,3,2) +(a,8,1) -(b,7,1) +(b,10,2)`。若改键 Update 只加新组、不从旧组撤回，第 5 步只剩 `-(b,7,1) +(b,10,2)`，第 6 步同样不撤 a；八步后下游视图错成 `a:(3,2) b:(7,1) c:(8,1)`——多出幻影组 a:(3,2)（正确视图无 a）。
- (乙) 若以 `sum==0` 判删组：第 2 步后 a=(0,2) 被误删，输出错成只有 `-(a,5,1)`（漏 `+(a,0,2)`，组从视图消失）。若 count 归零仍保留并输出 `+(g,0,0)`：第 6 步错成 `-(a,8,1) +(a,0,0) +(c,8,1)`，八步后视图有 3 组 `a:(0,0) b:(7,1) c:(8,1)`。
- (丙) 若 Update 一律=Delete+Insert 各自独立输出：第 4 步错成 `-(a,0,2) +(a,-5,1) -(a,-5,1) +(a,3,2)`（4 条，应 2 条）；第 7 步错成 `-(b,10,2) +(b,3,1) -(b,3,1) +(b,10,2)`（4 条，应「无」）。

## 四条不变量：保证位置与钉住测试

1. 与批量重算一致：`gagg.applyOne` 用 `gdelta.Entries` 的同一增量维护 aggs，count=0 即删组（gagg/gagg.go）；测试 `TestEightStep`、`TestRandomInvariants`。
2. 变更日志自洽：`gdelta.Entries` 旧组先于新组、组内先 `-` 后 `+`（gdelta/gdelta.go）；`api.checkLog` 逐条回放校验每个前缀；测试 `TestRandomInvariants`（内嵌回放）。
3. 最小输出：`gdelta.group` 的 before==after 短路、count>0 才输出（gdelta/gdelta.go）；测试 `TestRandomInvariants`（逐步断言 ≤4 条、同组至多一撤一加、视图无 count==0 组）。
4. 失败不留痕：`gagg.Apply` 全程在克隆的行表/聚合上操作，任一条出错即弃（gagg/gagg.go）；测试 `TestFailureAtomic`。
