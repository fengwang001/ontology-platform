# NOTES — t-digest 式近似分位数推导

## 九步推导（K=3，记法 mean:count）

1. `Add(1)` → `[1:1]` n=1
2. `Add(2)` → `[1:1, 2:1]` n=2
3. `Add(3)` → `[1:1, 2:1, 3:1]` n=3
4. `Add(4)` → `[1.5:2, 3:1, 4:1]` n=4（并 1:1+2:1，和 2 并列取下标最小）
5. `Add(5)` → `[1.5:2, 3.5:2, 5:1]` n=5（并 3:1+4:1，和 2 并列取下标最小）
6. `Add(6)` → `[1.5:2, 3.5:2, 5.5:2]` n=6（并 5:1+6:1）
7. `Add(7)` → `[1.5:2, 3.5:2, 6:3]` n=7（并 5.5:2+7:1，和 3 最小）
8. `Merge([9,10])` → `[2.5:4, 6:3, 9.5:2]` n=9（先并 9:1+10:1→9.5:2，再并 1.5:2+3.5:2→2.5:4）
9. `Retract(4)` → `[2.75:4, 22/3≈7.333:3, 10:1]` n=8（整体重算：exact={1,2,3,5,6,7,9,10} 升序重 Add）

(甲) 第 5 步正确并最小和对 3:1+4:1 → `[1.5:2, 3.5:2, 5:1]`，Q(0.5)（n=5，r=2 落在 3.5:2）= **3.5**；
错成「最大和合并」：并 1.5:2+3:1 → `[2:3, 4:1, 5:1]`，Q(0.5) 错成 **2**。

(乙) 第 4 步后 n=4、r=1.5 落在 hi=1 与 lo=2 之间：插值 1.5+(3−1.5)×0.5 = **2.25**；
不插值、直接取 int(r)=1 号秩所在质心 → 错成 **1.5**。

(丙) 第 9 步重算后 Q(0.5)（n=8、r=3.5 在 hi=3 与 lo=4 之间）= 2.75+(22/3−2.75)×0.5 = 121/24 ≈ **5.0417**；
错成「最近质心 count−1」→ `[2.5:3, 6:3, 9.5:2]`，Q(0.5) 错成 **6**。
必须重算：4 已与 1、2、3 一起坍缩进 2.5:4，质心里没有 4 的个体，只有从 exact 重算才能精确剔除。

## 四条不变量：代码位置 → 钉住它的测试

1. 误差有界：cent.Quantile 按秩段定位+插值（cent/cent.go），Count 精确、质心≤k 由 dg.Add/compress 保证 → TestErrorBoundVsExact / TestQuantileVisitsBoundedByK
2. 合并自洽：dg.Merge 只读 o 不改 o、compress 确定性（dg/dg.go）→ TestMergeSelfConsistent
3. 撤回即重算：dg.Retract 从 exact 升序重建（dg/dg.go）→ TestRetractIsRebuild
4. 失败不留痕：Retract 先校验后扣减、New/Quantile 前置校验（dg/dg.go、api/api.go）→ TestRejectedOpsLeaveNoTrace / TestSentinelErrorsDistinct
