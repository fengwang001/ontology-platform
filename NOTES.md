# NOTES — ontology-311：幂等生产者服务端序号校验推导

记号：窗=[(seq,位点)…]，只写本请求所在 (pid,分区) 的状态；「无态」=该分区无状态；ep=pid 7 当前 epoch。N=2，W=2。

| # | 请求 e/分区/seq | 结果 | 位点 | 本步后该分区 lastSeq/窗口 | ep |
|---|---|---|---|---|---|
| 1 | 0/0/0 | 追加 | 0 | last0 窗[(0,0)] | 0 |
| 2 | 0/1/0 | 追加 | 0 | p1 last0 窗[(0,0)] | 0 |
| 3 | 0/0/1 | 追加 | 1 | last1 窗[(0,0),(1,1)] | 0 |
| 4 | 0/0/1 | 重复 | 1 | last1 窗[(0,0),(1,1)] 不变 | 0 |
| 5 | 0/0/3 | 乱序错误 | 无 | last1 窗[(0,0),(1,1)] 不变 | 0 |
| 6 | 0/0/2 | 追加 | 2 | last2 窗[(1,1),(2,2)] | 0 |
| 7 | 0/0/0 | 重复已过期 | 无 | last2 窗[(1,1),(2,2)] 不变 | 0 |
| 8 | 1/1/0 | 追加 | 1 | p1 last0 窗[(0,1)]；p0 状态已清空 | 1 |
| 9 | 0/0/3 | 围栏错误 | 无 | p0 无态（不变） | 1 |
| 10 | 1/0/0 | 追加 | 3 | p0 last0 窗[(0,3)] | 1 |

最终日志：分区0 = (e0,s0)@0 (e0,s1)@1 (e0,s2)@2 (e1,s0)@3，共 4 条；分区1 = (e0,s0)@0 (e1,s0)@1，共 2 条。

(甲) 第4步=重复确认、返回位点1（成功，不追加）；第7步=重复已过期错误、无位点。若只认 seq==lastSeq+1：第4步 lastSeq=1、1≠2 → 乱序错误，生产者会误以为 seq1 没写成功（实际已落盘位点1）从而重发/上抛，重试幂等被破坏；第7步也被报成乱序错误（正确应为重复已过期）。
(乙) 升级 epoch 不清空状态：第8步 p1 旧态 last0 窗[(0,0)]，seq0 命中窗口 → 重复、位点0、不追加（正确：追加位点1）；第10步 p0 旧态 last2 窗[(1,1),(2,2)]，seq0 → 重复已过期错误、无位点（正确：追加位点3）。最终 p0=3 条、p1=1 条（正确各为 4、2 条）。
(丙) 状态按 pid 共用：第2步命中第1步留下的共用窗 → 重复、返回分区0 的位点0、不向分区1 追加（正确：分区1 追加位点0）；第10步共用态来自第8步 last0 窗[(0,分区1位点0)]，seq0 → 重复、返回分区1 的位点0、不追加。最终 p0=3 条、p1=1 条（正确 4、2）。回到正确的按 (pid,分区) 维护，若第9步跳过围栏直接按第4条：p0 已被清空无态，首条要求 seq=0，seq3 → 乱序错误、不追加。

## 不变量落点（代码位置 / 钉住的测试）

1. 与朴素参照一致：`broker.(*Broker).Produce` 严格按规则 1→2→3→4 顺序 return，epoch 清空用 `entry.epoch` 标签惰性失效（broker.go）；`api.(*API).SelfCheck` 内置朴素参照（升级时 for 循环逐个 delete、窗口线性查找）逐条对拍。钉住：`TestNaiveEquivalence`、`TestTenStepDerivation`（api/api_test.go）。
2. 恰好一次落盘：只有 `seqstate.Check` 返回 Append 后才在 Produce 内 `append(b.logs[...])`，重复走窗口旧位点直接返回（broker.go）；窗为连续最近 W 条，接受序必为 0,1,2…。钉住：`TestExactlyOnce`（broker/broker_test.go）、`TestConcurrentDuplicateProduce`（api/api_test.go）。
3. 围栏：`known && epoch<cur` 在任何写操作之前返回 `ErrFenced`（broker.go 第2步），其后无 append/无状态改动。钉住：`TestFence`。
4. 失败不留痕：四类错误分支全部位于 epochs/states/logs 写入之前；第3条 seq≠0 不写 epoch 表；惰性旧条目不命中时绝不 reset。钉住：`TestRejectLeavesNoTrace`（含 epoch 未升级、被拒后可继续使用）。
复杂度：`broker.Broker.checked`（非导出）记录最近一次 Produce 检查/修改的序号状态条目数；`broker/broker_test.go` 的 `TestEpochBumpLazy` 白盒直读该字段，m=100/1000/10000 下断言 ≤2，证明清空是 O(1) 惰性失效；该字段不经任何导出方法暴露。
