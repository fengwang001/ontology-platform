# ontology-749 全局最小割（Stoer-Wagner）

## 第三节推导：n=4，边 (0,1)=3、(1,2)=2、(0,2)=5、(2,3)=1

| 阶段 | 存活节点 | MAS 加入顺序 | (s,t) | 本阶段割值 |
|---|---|---|---|---|
| 1 | 0,1,2,3 | 0→2→1→3 | (1,3) | w(3→{0,1,2}) = 1 |
| 2 | 0, 1'={1,3}, 2 | 0→2→1' | (2,1') | w(1'→{0,2}) = 3+3 = 6 |
| 3 | 0, 2'={1,2,3} | 0→2' | (0,2') | w(2'→{0}) = 5+3 = 8 |

阶段 1 细节：A={0} 时 cur=[-,3,5,0]，取 2；cur[1]=3+2=5、cur[3]=0+1=1，取 1；最后取 3。
合并规则：t 并入 s，共同邻居边权相加——阶段 2 的 (1',2) = (1,2)2+(3,2)1 = 3；阶段 3 的 8 = (0,2)5+(0,1')3。
取最小：min(1, 6, 8) = **1**（即割出孤立侧 {3}，仅断边 (2,3)）。

- (甲) 只取最后阶段割值 → 返回 **8**（正确 1）。
- (乙) MAS 改取「最小」：阶段 1 顺序 0→3→1→2，(s,t)=(1,2)，割值 = 5+2+1 = **8**；
  阶段 2 顺序 0→3→1'，割值 9；阶段 3 割值 9 → 最终返回 **8**（正确 1）。
- (丙) 割值错取 s-t 边权：阶段 1 (1,3) 无边记 0 → **0**；阶段 2 (2,1')=3；阶段 3 = 8；
  取最小 → 最终返回 **0**（正确 1）。

## 四条不变量：保证位置 + 钉住测试

1. 下界正确：`sw.MinCut` 对每个阶段的割值取最小（`sw/sw.go` 主循环）→ `TestMinCutMatchesBruteForce`
2. 与朴素参照一致：同上测试，参照 = 枚举全部二划分取最小跨边权和 → `TestMinCutMatchesBruteForce`
3. 合并正确性：`sw.merge` 对共同邻居边权 `+=` 求和、删 s-t 边 → `TestMergeSumsWeights`
4. 失败不留痕：`wg.AddEdge`/`wg.New` 全部校验通过后才写状态 → `TestRejectedOpsKeepState`
