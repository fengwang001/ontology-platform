# Hamming(7,4) 推导与不变量

## 第三节推导：nibble 1011（值 11）

d1d2d3d4 = 1011；p1 = 1⊕0⊕1 = 0，p2 = 1⊕1⊕1 = 1，p3 = 0⊕1⊕1 = 0
→ 码字（位置 1..7 = p1 p2 d1 p3 d2 d3 d4）= **0110011**

七行分步表（每行只翻转码字的一位，再纠错）：

| 翻转位 | 翻转后码字 | (s1,s2,s3) | syndrome | 纠正后 d1d2d3d4 |
|---|---|---|---|---|
| 1 (p1) | 1110011 | (1,0,0) | 1 | 1011 |
| 2 (p2) | 0010011 | (0,1,0) | 2 | 1011 |
| 3 (d1) | 0100011 | (1,1,0) | 3 | 1011 |
| 4 (p3) | 0111011 | (0,0,1) | 4 | 1011 |
| 5 (d2) | 0110111 | (1,0,1) | 5 | 1011 |
| 6 (d3) | 0110001 | (0,1,1) | 6 | 1011 |
| 7 (d4) | 0110010 | (1,1,1) | 7 | 1011 |

- **(甲)** 奇校验 p_i = NOT(…)：p1p2p3 = 101 → 错码字 **1011011**（正确偶校验为 0110011）。
- **(乙)** 映射写反 syndrome = s1*4+s2*2+s3：翻 d1（位置 3）后伴随式 (1,1,0) 被错算成 **6** → 去翻位置 6（d3），码字 0100011→0100001，数据错成 **0001**（应还原 1011）。
- **(丙)** 单 nibble 1011 打包：0110011 大端补 0 → **0x66**；`Encode([])` 返回**空切片**（长 0）。双错翻 p1p2：0110011→0010011，伴随式 (1,1,0)、syndrome=3 → 错翻 d1，数据静默错成 **0011**。最小距离 3：双错使码字落在另一合法码字的距离-1 邻域内，伴随式非零却没有「双错」保留值，译码器只能按单错处理，故双错不可检出、静默出错。

## 第二节四条不变量（保证位置 / 钉住测试）

1. **与朴素参照一致**：`hamm/hamm.go` 的 `EncodeNibble`/`DecodeNibble` 按定义逐位实现；测试 `TestAgainstNaiveReference`（api_test.go，内嵌朴素参照逐字节比对）。
2. **切分点无关**：`api.Feeder` 只拼接字节、最终调同一个 `Decode`；测试 `TestChunkingInvariance`。
3. **确定性/单错可纠**：`Encode`/`Decode` 为无状态纯函数；测试 `TestDeterministicAndSingleError`（7 种单错全覆盖 + 重复编码逐字节相同）。
4. **失败不留痕**：`api.Decode` 先整体校验（截断/填充）再解码，出错返回 `nil`；测试 `TestFailureAtomicity`。
