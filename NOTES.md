# 简单多边形面积与重心 — 推导与不变量

## 第三节：L 形六行分步表（逆时针 v0..v5）

| 边 v_i→v_{i+1} | 叉积 c = x_i·y_{i+1} − x_{i+1}·y_i | (x_i+x_{i+1})·c | (y_i+y_{i+1})·c |
|---|---|---|---|
| (0,0)→(6,0) | 0·0−6·0 = 0 | 6·0 = 0 | 0·0 = 0 |
| (6,0)→(6,2) | 6·2−6·0 = 12 | 12·12 = 144 | 2·12 = 24 |
| (6,2)→(2,2) | 6·2−2·2 = 8 | 8·8 = 64 | 4·8 = 32 |
| (2,2)→(2,6) | 2·6−2·2 = 8 | 4·8 = 32 | 8·8 = 64 |
| (2,6)→(0,6) | 2·6−0·6 = 12 | 2·12 = 24 | 12·12 = 144 |
| (0,6)→(0,0) | 0·0−0·6 = 0 | 0·0 = 0 | 6·0 = 0 |
| 合计 | 40 | 264 | 264 |

- (甲) 面积 A = 40/2 = **20**（= 6×6 − 4×4 凹口，吻合）。顶点反转成顺时针后每一项叉积变号，鞋带和为 −40、面积 **−20**；按规则整体拒绝（哨兵错误 ErrNotCCW），不得取绝对值。
- (乙) 重心 = (264/(6·20), 264/(6·20)) = **(11/5, 11/5)**。若分母误写为 2A = 40，则错成 (264/40, 264/40) = **(33/5, 33/5)**（整整偏大 3 倍）。
- (丙) 顶点均值 = (16/6, 16/6) = **(8/3, 8/3)**；与正确重心每坐标差 11/5 − 8/3 = **−7/15**。顶点均值把 6 个顶点等权，完全不看边长与面积分布；重心是面积加权平均，凹口（缺掉的 4×4 块）把质量拉向左下，而等权顶点在凹角附近照样计票，故顶点均值系统性偏向右上。

## 第二节：四条不变量的落点

1. 与三角剖分一致：`meas.Centroid` 的矩累加即「原点扇形有向三角剖分、面积加权」的代数等价式；由 `TestTriangulationConsistent`（api 包，独立逐三角形有理数累加）钉住。
2. 平移不变：重心是仿射量，公式只含顶点坐标的齐次组合；由 `TestTranslationInvariant`（api 包，平移 (tx,ty) 后逐字段比对）钉住。
3. 面积非负且为真：`api.New` 拒绝非逆时针输入，`meas.Area` 返回 area2/2（恒正）；由 `TestAreaPositiveAndTrue` 钉住。
4. 失败不留痕：`api.New` 先完成全部校验，任一失败返回 `nil, err`，不构造任何部分结果；由 `TestRejectNoPartial` 钉住。
