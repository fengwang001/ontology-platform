# NOTES

## 第三节推导：P1(0,0) P2(4,0) P3(0,4) P4(4,4) P5(3,3) P6(2,1)

| 步 | 插入 | 三角形集合 |
|---|---|---|
| 1 | P1 | 无三角形 |
| 2 | P2 | 无三角形 |
| 3 | P3 | (P1,P2,P3) |
| 4 | P4 | (P1,P2,P3), (P3,P2,P4) |
| 5 | P5 | (P1,P3,P5), (P1,P5,P2), (P3,P5,P4), (P5,P2,P4) |
| 6 | P6 | (P1,P2,P6), (P2,P5,P6), (P1,P6,P3), (P6,P5,P3), (P3,P5,P4), (P5,P2,P4) |

(甲) P1..P4 共圆（圆心(2,2)，r²=8），共圆不翻转：对角线 **P2-P3**，集合 {(P1,P2,P3),(P3,P2,P4)}。若误判 `>=0` 则对角线错成 **P1-P4**，集合错成 {(P1,P2,P4),(P1,P4,P3)}。
(乙) 更正：P5=(3,3) 实际在 (P3,P2,P4) 严格内部（题面"不在任何三角形内部"不成立），且同时在 (P1,P2,P3) 外接圆严格内部（dist²=2<8），故两个三角形都须挖除，重连得 {(P1,P3,P5),(P1,P5,P2),(P3,P5,P4),(P5,P2,P4)}。若只按"点-三角形包含"重构，只挖 (P3,P2,P4)：错留 **(P1,P2,P3)**、错生 **(P3,P2,P5)**，非法边 P2-P3 被保留（应翻成 P1-P5）。
(丙) P6 在 (P1,P3,P5) 与 (P1,P5,P2) 外接圆严格内部，挖除这两个、重连得 {(P1,P2,P6),(P2,P5,P6),(P1,P6,P3),(P6,P5,P3),(P3,P5,P4),(P5,P2,P4)}。若只把包含三角形 (P1,P5,P2) 一分为三、不级联，非法边 **P1-P5** 被保留，正确应翻成 **P3-P6**，集合错成 {(P1,P2,P6),(P2,P5,P6),(P5,P1,P6),(P1,P3,P5),(P3,P5,P4),(P5,P2,P4)}。

## 四条不变量：保障位置与钉住测试

1. 与暴力重算一致：`tri/check.go` `SelfCheck` 对内置点序列做 O(n³) 空圆暴力比对；测试 `TestBruteForceConsistency`（随机多点集独立暴力对照）。
2. 三角剖分自洽（内部边恰两三角形共享、T=2n−2−h、面积均正）：`tri/check.go` `checkTris` 逐边逐三角形核验；测试 `TestSelfCheck`。
3. 局部 Delaunay：`tri/tri.go` `legalize` 仅在严格内含（>0）时翻转、共圆（=0）不翻；`checkTris` 对每条内部边复核 InCircle≤0；测试 `TestSelfCheck`。
4. 失败不留痕：`tri/tri.go` `Insert` 先做越界/重复/共线三项校验、全部通过后才变更任何状态；测试 `TestErrorsDistinctAndAtomic`。
