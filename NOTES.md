# 单调栈求下一个更大元素：推导与不变量

## 推导：新元素与栈顶相等时弹不弹（序列 [2,2,3]，栈内为下标，底→顶）

做法 A（栈顶值 < a[i] 才弹，相等不弹）：
- i=0：栈空，压 0 → 栈 [0]
- i=1：栈顶值 a[0]=2 == a[1]=2，不弹，压 1 → 栈 [0 1]
- i=2：a[2]=3，弹 1（ans[1]=2）、弹 0（ans[0]=2），压 2 → 栈 [2]
- 结束：栈内下标 2 无解 → ans[2]=-1。答案 A = [2, 2, -1]

做法 B（栈顶值 <= a[i] 即弹，相等也弹）：
- i=0：压 0 → 栈 [0]
- i=1：a[0]=2 <= a[1]=2，弹 0（ans[0]=1），压 1 → 栈 [1]
- i=2：弹 1（ans[1]=2），压 2 → 栈 [2]
- 结束：ans[2]=-1。答案 B = [1, 2, -1]

差异在下标 0：A 指向 2（跳过相等的下标 1），B 指向 1。
A 即「下一个严格更大」，B 即「下一个大于等于」。
本题采用 A：严格更大；无解用 None=-1，与合法下标 0 不混淆。

## 四条不变量的落实

1. 结果正确：mono.Scanner.step 弹出栈顶时即定答案（mono/mono.go），
   由 TestNextGreaterMatchesNaive（对照朴素双循环）与 TestSelfCheck 钉住。
2. 栈单调：step 的弹出循环保证栈内值自底向顶非递增（mono/mono.go），
   由 TestStackMonotonic 逐步钉住。
3. 无解可判定：None=-1（mono/mono.go、nge/nge.go），由 TestNoneSentinel 钉住。
4. 失败不留痕：Scan 先校验再计算且只读输入（mono/mono.go），由 TestRejections 钉住。
