# 两栈队列推导笔记

## 何时转移栈元素

- 结构：`in` 栈承接所有 Enqueue；`out` 栈承接所有 Dequeue/Peek。
- 规则：仅当 `out` 为空且需要出队时，才把 `in` 全部逐个 Pop 并 Push 进 `out`。
- 为什么不能每次 Dequeue 都倒：`in` 顶是最新元素，整体倒入 `out` 后 `out` 顶变成最旧
  元素，顺序才正确；但若 `out` 非空时再倒，新倒入的元素会压在更早入队的元素之上，
  旧元素被压住、最新元素反而先出，FIFO 被破坏。
- 为什么不能每次 Enqueue 都把 `out` 倒回 `in`：n 个元素会在两栈间反复横跳，单次操作
  退化为 O(n)，摊还代价也随操作数线性增长（见 check 包内 badQueue 的退化测试）。
- 摊还 O(1)：每个元素一生只被 Push 进 `in` 一次、转移移动一次、从 `out` Pop 一次；
  任意 m 次操作总移动次数 <= 入队总数 <= m，故摊还每次 <= 1（实测断言上界取 2）。

## 语义与测试位置（实现后回填）

- TBD
