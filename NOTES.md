# 左外连接物化视图增量维护 — 推导与不变量

## 八步分步表（RV 缺席记 nil；日志 `+`/`-` 为插入/撤回）

| 步 | 操作 | 本步输出的变更日志 | 操作后宽表（K 升序） |
|---|---|---|---|
| 1 | PutL(k1,10) | +(k1,10,nil) | k1=(10,nil) |
| 2 | PutL(k2,20) | +(k2,20,nil) | k1=(10,nil), k2=(20,nil) |
| 3 | PutR(k2,200)（补发） | -(k2,20,nil), +(k2,20,200) | k1=(10,nil), k2=(20,200) |
| 4 | PutR(k1,100)（补发） | -(k1,10,nil), +(k1,10,100) | k1=(10,100), k2=(20,200) |
| 5 | PutR(k3,300)（无左，仅存） | 无 | k1=(10,100), k2=(20,200) |
| 6 | PutL(k3,30) | +(k3,30,300) | k1=(10,100), k2=(20,200), k3=(30,300) |
| 7 | PutR(k2,250)（更新） | -(k2,20,200), +(k2,20,250) | k1=(10,100), k2=(20,250), k3=(30,300) |
| 8 | DelL(k1) | -(k1,10,100) | k2=(20,250), k3=(30,300)（右记录 k1=100 保留） |

**(甲)** 若第 3 步只发 `+(k2,20,200)` 而不先撤回 `-(k2,20,nil)`，下游同时存在 (k2,20,nil) 与 (k2,20,200) 两行，k2 出现两行且旧值残留。违反不变量 2（每个 K 至多一行、补发必须先撤回旧行）。

**(乙)** 第 5 步若按内连接/右流也物化处理，会错输出 `+(k3,?,300)`（左值不存在，只能编造 nil/零值），宽表多出一行 (k3,nil,300)。正确：不输出任何日志，仅保存 R 待用，宽表不变。本质差别：左连接以左流为驱动，右记录无左匹配时只是状态、不产生行；内连接则要求两侧同时存在才有行（且右流单独物化会让无左之右也成行，两者都错）。

**(丙)** 第 8 步后再 `PutL(k1,15)`：正确输出 `+(k1,15,100)`，宽表 k1=(15,100), k2=(20,250), k3=(30,300)。若 DelL 连右记录一起删，则错输出 `+(k1,15,nil)`，k1 的 RV 错成 nil。不变量 3 要求右记录留存，因为 DelL 只是左流撤回其键，右流中 k1=100 仍是当前事实；左连接语义是「左行消失」，不是「右流记录被删除」，否则右流状态被左流操作污染。

## 四条不变量：保证位置与钉住测试

1. **与批量重算一致**：`join/join.go` 的 PutL/PutR/DelL/DelR 同步维护左集（keys+lv）与 rstore，View() 由同一状态按 K 升序生成；测试 `join.TestBatchRecomputeRandom`、`join.TestEightSteps`。
2. **变更日志自洽**：`join/join.go` 的 emit 一律先 `-` 旧行再 `+` 新行，`-` 取自当前物化行；测试 `join.TestLogPrefixRandom`。
3. **右记录留存**：`join/join.go` DelL 只删左集、不触碰 rstore；测试 `api.TestRightRetainedAfterDelL`。
4. **失败不留痕**：`join/join.go` 各方法先校验（空 K、记录存在性）再改状态，校验失败直接返回哨兵错误；测试 `api.TestFailureAtomic`、`api.TestErrorsDistinct`。
