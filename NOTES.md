# NOTES — ontology-353 分层布隆去重

哈希位(m=8,k=2)：a{0,1} b{2,5} c{2,3} d{4,7} i{0,1} f{1,6}

| 步 | 键 | 切换 | 当前层位集(升序) | 历史层位集(升序) | 判定 |
|---|---|---|---|---|---|
| 1 | a | 否 | {0,1} | {} | 新 |
| 2 | b | 否 | {0,1,2,5} | {} | 新 |
| 3 | c | 是 | {2,3} | {0,1,2,5} | 新 |
| 4 | d | 否 | {2,3,4,7} | {0,1,2,5} | 新 |
| 5 | i | 是 | {} | {0,1,2,3,4,5,7} | 重复 |
| 6 | f | 否 | {1,6} | {0,1,2,3,4,5,7} | 新 |
| 7 | a | 否 | {1,6} | {0,1,2,3,4,5,7} | 重复 |
| 8 | b | 否 | {1,6} | {0,1,2,3,4,5,7} | 重复 |

(甲) 假阳性：历史层位 {0,1}（首层封存的 a）与 i 的两个哈希位完全碰撞，i 实际未见却判重复。若切换只清空不并入（历史恒空）：第 5 步 i 被错判成「新」（规则下正确输出为「重复」，该假阳性本被允许），第 7 步 a 被错判成「新」——a 第 1 步已判新写入，属假阴性、同一真新键两次输出「新」；违反不变量 1。
(乙) 用当前层覆盖历史层：第 5 步切换后历史层错成 {2,3,4,7}（位 0、1、5 被清除），第 8 步 b{2,5} 因位 5 已丢失、当前层 {1,6} 也无此位，被错判成「新」（假阴性）；历史位置位后被清除，违反不变量 3（历史层只增），并连带违反不变量 1。
(丙) 全序列后历史层累计 4 个键的位（第 1 层 a、b，第 2 层 c、d；i 是假阳性未写入，f 留在当前层）。正确 n=4：(1−(7/8)^8)^2≈43.1%；若错按单层 cap=2（n=2）：(1−(7/8)^4)^2≈17.1%，低估约 26 个百分点。

不变量 → 代码保证位置 → 钉住测试：
1. 无假阴性：`dedup.feedOne` 中 current、history 两层 Contains 双查，新键才写；`TestNoFalseNegatives`
2. 切换正确：`dedup.rotate` 先 `bf.Merge`（OR 进历史）后 `bf.Reset`（位清空、计数归零）；`TestSaturationSwitch`
3. 历史层只增/估值单调：`bf.Merge` 只 OR 不清位、`histN` 只累加、`EstimateFP` 按公式代入 current.Count()/histN；`TestEstimateFP`
4. 失败不留痕：`dedup.New` 构造前校验 m/cap，`dedup.Feed` 先全量校验所有 key 再逐条处理；`TestRejectedOpsNoTrace`
复杂度：非导出字段 `lastProbes` 记录最近一次判定检查的位位置数，每次判定 ≤2k=4；`TestProbeCountConstant`
