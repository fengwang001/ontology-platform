# 括号配平与最长有效子串
## 推导：栈存下标 + 基准值
(甲) 预置基准 `-1`；(乙) 不匹配的右括号把**自己的下标压栈**作新基准。基准=当前有效段起点的前一个下标，长度=当前下标-栈顶；段从下标 0 开始时 i-(-1)=i+1 才正确，故基准只能是 -1。
做法A（预置-1 + 不匹配压栈）走查 `")()())"`：

    i=0 ')' 弹-1 栈空→压0  栈[0]    最长0
    i=1 '(' 压1            栈[0,1]  最长0
    i=2 ')' 弹1, 2-0=2     栈[0]    最长2
    i=3 '(' 压3            栈[0,3]  最长2
    i=4 ')' 弹3, 4-0=4     栈[0]    最长4
    i=5 ')' 弹0 栈空→压5   栈[5]    最长4
    i=6 ')' 弹5 栈空→压6   栈[6]    最长4
    A 结果：start=1, length=4  ✓

做法B（不预置 + 不匹配清空）：

    i=0 ')' 栈空→清空      栈[]     最长0
    i=1 '(' 压1            栈[1]    最长0
    i=2 ')' 弹1 栈空，无栈顶可减，长度算不出  栈[]  最长0
    i=3 '(' 压3            栈[3]    最长0
    i=4 ')' 弹3 栈空，同样算不出            栈[]  最长0
    i=5,6 ')' 栈空→清空    栈[]     最长0
    B 结果：最长=0  ✗（i=2 清空丢基准，连 2 都算不出，更算不出 4）

结论：选做法A，预置 -1，不匹配的右括号下标压栈作新基准。
## 不变量 → 保证位置 → 钉住它的测试
1. 配平判定与计数语义一致、报首个出错位置：`balance/balance.go` IsBalanced 单遍栈；TestBalanceTable
2. 最长子串正确（含朴素对照）：`longest/longest.go` Longest；TestLongestTable（naive 仅在 longest_test.go，随机对照见 demo）
3. 并列最长取最左：Longest 仅严格大于才更新；TestLongestTable（`"())()"`→(0,2)）
4. 失败不留痕：Longest 先 Validate，出错即返零值、不碰输入；TestRejections
