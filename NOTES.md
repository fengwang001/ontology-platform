# 并查集推导笔记

## 按秩合并与路径压缩

朴素做法 Union 总把 y 的根挂到 x 的根下，依次合并 (0,1),(1,2),...,(n-2,n-1)
会得到一棵深 n-1 的链，Find 最坏 O(n)。

按秩合并：每棵树根维护秩 rank（高度上界），合并时把较小秩的根挂到较大秩的根下；
秩相等才任选一边挂载并把该根秩加 1。一个根的秩要增加，必须有两棵同秩树合并，
其节点数至少翻倍，因此秩为 r 的树根下至少有 2^r 个节点，故 r <= log2(n)，
树高 <= log2(n)，单次 Find 最坏 O(log n)。

路径压缩：Find 沿 parent 上溯时把沿途节点直接指向最终根，后续查找近乎 O(1)。
与按秩合并不冲突——压缩只缩短链，不会增加秩；秩只在合并时单调增加。
两者配合的均摊复杂度为反阿克曼级 O(alpha(n))（对任何实际 n 都是 <=5 的常数），
工程上即「近乎常数」；不使用压缩时按秩也保证 Find 跳数 <= log2(n)，
故任意时刻单次 Find 跳数 <= log2(n) <= 2·log2(n)+2。

## 语义与测试位置

（测试写完后回填：第二节各条的代码位置与测试函数名）
