# NOTES — PowMod 推导与不变量

## 八行分步表（square-and-multiply，乘法均为 mulmod）

| # | (base,exp,mod) | 归一化底数 | 逐步 | 结果 |
|---|---|---|---|---|
| 1 | (-2,3,5) | -2→3 | 3^1=3, 3^2=9%5=4, r=3*4=12%5 | **2** |
| 2 | (-3,2,7) | -3→4 | 4^2=16%7 | **2** |
| 3 | (-3,3,7) | -3→4 | 4^2=2, r=4*2=8%7 | **1** |
| 4 | (7,100,13) | 7 | 7^2=10,7^4=9,7^8=3,7^16=9,7^32=3,7^64=9; 100=64+32+4 → 9*3*9=243%13 | **9** |
| 5 | (-5,3,9) | -5→4 | 4^2=7, r=4*7=28%9 | **1** |
| 6 | (2,0,5) | 2 | r=1%5 | **1** |
| 7 | (5,0,1) | 0 | r=1%1 | **0** |
| 8 | (2,10,1) | 0 | r=1%1 | **0** |

## 三问

- (甲) 不归一化：Go `%` 截断取余，(-2)^3=-8，-8%5=**-3**（错）；正确值 **2**。
- (乙) 正确值 2^64 mod 1000000007 = **582344008**。朴素 int64：4294967296^2=2^64 静默回绕为 **0**，0%1000000007=**0**（错）。
- (丙) 正确值 **0**（mod==1 时恒 0）。若先判 exp==0 直接返回 1，错成 **1**。

## 四条不变量 → 代码位置 → 钉住它的测试

1. 与朴素参照一致：`pow/pow.go` 平方乘主循环 + `mod.Mulmod` 不溢出；测试 `TestAgainstNaiveBig`（exp≤12 全组合对拍 math/big）。
2. 模语义正确：`mod/mod.go` `Normalize` 归一化负 base，`PowMod` 入口 `r=1%m`；测试 `TestModSemantics`（结果∈[0,mod)、负 base==归一化后幂）。
3. 指数拆分正确：`pow/pow.go` 用 `mod.Mulmod` 组合 r 与底数；测试 `TestExponentSplit`（PowMod(e1+e2)==Mulmod(P(e1),P(e2))）。
4. 失败不留痕：`pow/pow.go` 入口三个哨兵错误 ErrModZero/ErrModNegative/ErrExpNegative，校验先于一切计算；测试 `TestSentinelErrorsDistinct`。
