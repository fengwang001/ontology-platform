# K-slack 乱序事件计数 NOTES

## 八步推导（K=3，key="a"，容忍窗口 [high-3, high]，左闭右闭）

| 步 | Seq | 之后 high | 判定 | 之后 Accepted("a") | 之后 Dropped() |
|---|---|---|---|---|---|
| 1 | 10 | 10 | 首个接受（high 由"无"设为 10） | 1 | 0 |
| 2 | 8  | 10 | 窗口内接受（8∈[7,10]，不推进） | 2 | 0 |
| 3 | 12 | 12 | 窗口内接受（12>10，推进 high） | 3 | 0 |
| 4 | 5  | 12 | 超窗丢弃（5 < 12-3=9） | 3 | 1 |
| 5 | 8  | 12 | 超窗丢弃（8 < 9） | 3 | 2 |
| 6 | 11 | 12 | 窗口内接受（11∈[9,12]，不推进） | 4 | 2 |
| 7 | 9  | 12 | 窗口内接受（9 == high-K，左闭边界） | 5 | 2 |
| 8 | 7  | 12 | 超窗丢弃（7 < 9） | 5 | 3 |

**(甲)** 第 7 步 `Seq=9 == high-K` 被**接受**（左闭）。若误写成 `Seq > high-K`（左开），第 7 步被判成**超窗丢弃**，最终 `Accepted("a")` 错成 **4**、`Dropped()` 错成 **4**（正确为 5 / 3）。

**(乙)** 若第 4 步把 `high` 覆盖成 5：第 5 步 `Seq=8 > 5` 被**接受**并推进 `high=8`；随后 11 接受推进 high=11、9 在 [8,11] 内接受、7<8 丢弃。八步后 `Accepted("a")` 错成 **6**（正确 5），`Dropped()` 错成 2。

**(丙)** K=3，`("a",100)` 后 `("b",1)`：全局 high 实现下 high=100，`("b",1)` 因 `1 < 100-3=97` 被错判**超窗丢弃** → `Accepted` 总数错成 **1**、`Dropped()` 错成 **1**（正确为 2 / 0，"b" 的首个事件应无条件接受）。违反第二节**不变量 1**（高水位须按 Key 各自维护，结果须与逐 Key 朴素重算一致）。

## 四条不变量的落点

1. **与朴素逐条重算一致**：`slack.Judge` 是纯函数判定，`wcount.applyOne` 逐条应用且按 Key 独立存 `high`；测试 `TestNaiveEquivalence`（多档 Key 规模 × 随机顺序对比朴素实现）。
2. **窗口边界自洽**：`slack.Judge` 中 `seq >= high-K` 接受、`seq < high-K` 丢弃，`high` 仅在 `seq > high` 时推进，丢弃路径不写 `high`；测试 `TestWindowBoundary`。
3. **累计性**：`accepted`/`dropped` 在 `wcount.applyOne` 中只有 `++`，无任何回写；测试 `TestMonotonic`。
4. **失败不留痕**：`wcount.Feed` 先整批校验（空 Key / 新 Key 超限）再统一应用，校验失败零写入；`api.New` 校验参数；测试 `TestFeedAtomic`、`TestBadParams`。
