# 伙伴分配器 NOTES

## 八步推导（N=5，池 [0,32)，初始空闲 [(0,32)]）

| 步 | 操作 | 结果 | 操作后空闲列表 (偏移,大小) |
|---|---|---|---|
| 1 | Alloc(4) | 返回 0（32→16→8→4 三次分裂） | (4,4) (8,8) (16,16) |
| 2 | Alloc(4) | 返回 4 | (8,8) (16,16) |
| 3 | Alloc(4) | 返回 8（分裂 (8,8)） | (12,4) (16,16) |
| 4 | Alloc(4) | 返回 12 | (16,16) |
| 5 | Free(4) | 伙伴 0 已分配，不合并 | (4,4) (16,16) |
| 6 | Free(8) | 伙伴 12 已分配，不合并 | (4,4) (8,4) (16,16) |
| 7 | Free(12) | 与伙伴 8 合并为 (8,8) | (4,4) (8,8) (16,16) |
| 8 | Free(0) | 级联：(0,8)→(0,16)→(0,32) | (0,32) |

(甲) 第 6 步的 (4,4) 与 (8,4) 地址相邻、大小相同，但**不是伙伴**：4 XOR 4 = 0 ≠ 8，且 4 不是 2^(2+1)=8 的倍数，二者并非同一父块分出。若误把「相邻等大」都合并，会多出非法块 **(4,8)**：起始偏移 4 不是其大小 8 的整数倍，违反伙伴结构不变量，之后经它分裂/合并出的块全部错位。
(乙) 若取整成「严格大于 n 的 2 的幂」，Alloc(4) 按 8 分配：第 1 步占 [0,8) 返回 0，第 2 步占 [8,16) 返回 8，第 3、4 步占 [16,24)、[24,32)。正确结果前四步应各占 [0,4)、[4,8)、[8,12)、[12,16)；错误做法 4 次 4 字节请求就耗尽整个 32 字节池。
(丙) 第 8 步若只合并一轮：(0,4)+(4,4)→(0,8) 即停，空闲列表错成 **(0,8) (8,8) (16,16)**，最大空闲块为 8；正确应级联到顶，最大空闲块为 32，即单个 (0,32)。

## 四条不变量：保障位置 / 钉住测试

1. 守恒不重叠：pool.Alloc/Free 成对维护已分配表与 buddy 空闲链表（分裂删父加双子、合并删双子加父），api.SelfCheck 逐项复核。测试 `TestConservation`。
2. 全释放回单块：buddy.Release 沿伙伴链级联合并，直到伙伴被占或到顶 [0,S)。测试 `TestFullReleaseReturnsToSingle`。
3. 结构合法：buddy 只按 2 的幂二等分、只与 off^size 的真伙伴合并，对齐性天然成立；SelfCheck 再复核。测试 `TestStructure`。
4. 失败不留痕：pool 先校验（ErrBadSize/ErrFull/ErrBadFree 三个哨兵）后改动，任一校验失败零写入。测试 `TestFailureAtomic`。
