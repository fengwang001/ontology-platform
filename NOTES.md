# NOTES

## 三、`s = "aabaa"` 逐字符构建表（每步新建节点及其 link）

| 步 | 字符 | 新建回文（长度） | link 指向的回文 |
|----|------|------------------|------------------|
| 1 | a | `"a"` (1) | `""`（偶根） |
| 2 | a | `"aa"` (2) | `"a"` |
| 3 | b | `"b"` (1) | `""`（偶根） |
| 4 | a | `"aba"` (3) | `"a"` |
| 5 | a | `"aabaa"` (5) | `"aa"` |

- **(甲)** 把「不同回文数」错当「总个数」：会算成 **5**（即不同回文数本身），而正确的总个数是 **9**（cnt 传播后各节点之和 4+2+1+1+1）。
- **(乙)** 只扩奇数长度中心：`"abba"` 里没有任何长度 ≥3 的奇回文，会算成 **1**（单字符），正确是 **4**（`"abba"` 整体，偶长度）。
- **(丙)** 不做 link 传播、只计节点「新建」次数：`"a"` 只在第 1 步新建一次，`Count("a")` 会算成 **1**，正确是 **4**（`aabaa`→`aa`→`a`、`aba`→`a` 两级传播累加）。

## 二、四条不变量：保证位置与钉住测试

1. **与朴素一致**：`pal.New` 逐字符 `add` 建树 + `query` 的 Distinct/Total/Longest 实现；测试 `TestAgainstNaive`。
2. **link 树结构**：`pal.add` 中 len==1 链到偶根、否则沿后缀链取 `next`（长度严格递减故无环）；测试 `TestLinkInvariants`。
3. **计数传播**：`pal.New` 末尾按 len 降序 `cnt[link[v]] += cnt[v]`；测试 `TestCountPropagation`。
4. **失败不留痕**：`api.New` 先校验再构建、`query.Count` 纯只读、错误均为哨兵错误；测试 `TestFailureAtomicity`。
