# NOTES

## 三、推导：`8 / 2 / 2 - 3` 按后序遍历构建 AST（共 7 节点）

| 步 | 构造的节点 | 运算符/字面量 | 左操作数值 | 右操作数值 | 本节点值 |
|----|-----------|--------------|-----------|-----------|---------|
| 1 | 叶子 | 8 | 无 | 无 | 8 |
| 2 | 叶子 | 2 | 无 | 无 | 2 |
| 3 | 内部节点 | / | 8 | 2 | 4 |
| 4 | 叶子 | 2 | 无 | 无 | 2 |
| 5 | 内部节点 | / | 4 | 2 | 2 |
| 6 | 叶子 | 3 | 无 | 无 | 3 |
| 7 | 内部节点 | - | 2 | 3 | -1 |

- (甲) 若 `term` 循环误为右结合：`8/2/2` 变 `8/(2/2)`——第 3 步算 `2/2=1`、第 5 步算 `8/1=8`，最终 `8/2/2-3` 错成 **5**（正确为 -1）。
- (乙) 若四则同优先级从左到右：`2+3*4` 变 `(2+3)*4`，错成 **20**（正确为 14）。
- (丙) 若 `/` 误用 floor：`-7/2` 错成 **-4**（正确向零截断为 -3）。

## 二、四条不变量的落点

1. 与两栈参照一致：`api.refEval`（显式优先级表 + 左结合）为参照；`TestAgainstReference` 随机表达式对拍钉住。
2. AST 与求值自洽：`api.Eval` 只读 `parse.Node`，不碰解析状态；`TestASTEvalConsistent` 钉住。
3. 左结合：`parse.expr`/`parse.term` 的 for 循环把新因子累积到左侧；`TestEvalTable`（含 `8/2/2-3`、`8-3-2`）钉住。
4. 失败不留痕：解析状态全在 `parser` 栈帧，求值无全局可变量（前瞻峰值计数仅成功时写、不影响结果）；`TestNoStateAfterFailure` 钉住。
