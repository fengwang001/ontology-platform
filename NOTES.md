# SCD2 历史区间维护笔记（inf = math.MaxInt64）

## 第三节：八步分步表（同一键 K，从空开始）
1. U(10,"a") 顺序新开：[10,inf)=a
2. U(30,"b") 顺序闭合+顺序新开：[10,30)=a;[30,inf)=b
3. D(50) 顺序闭合（不新开）：[10,30)=a;[30,50)=b
4. U(20,"c") 乱序拆分（落在[10,30)内）：[10,20)=a;[20,30)=c;[30,50)=b
5. U(30,"d") 替换同一时刻（U→U）：[10,20)=a;[20,30)=c;[30,50)=d
6. U(5,"f") 乱序插入（不落在任何行内部）+新开：[5,10)=f;[10,20)=a;[20,30)=c;[30,50)=d
7. D(20) 替换同一时刻（U→D，行消失）：[5,10)=f;[10,20)=a;[30,50)=d
8. U(40,"g") 乱序拆分（落在[30,50)内）：[5,10)=f;[10,20)=a;[30,40)=d;[40,50)=g

## (甲)(乙)(丙)
甲：Upsert 直接覆盖开放行而不闭合 → 第2步后只有 [10,inf)=b；AsOf(K,15) 错成 "b"/存在，正确是 "a"。
乙：无视 Eff 一律"闭合于 Eff+追加[Eff,inf)" → 第4步后 [10,30)=a;[30,50)=b;[20,inf)=c；[20,50) 被两行同时覆盖（[20,30) 是 a/c，[30,50) 是 b/c）；AsOf(K,55) 错成 "c"/存在，正确是不存在（D(50) 之后）。
丙：同一 Eff 保留先到者（第5、7步被忽略）→ [5,10)=f;[10,20)=a;[20,30)=c;[30,40)=b;[40,50)=g；AsOf(K,25) 错成 "c"（正确：不存在），AsOf(K,35) 错成 "b"（正确："d"）。

## 第二节四条不变量：保证位置 / 钉住测试
1 与批量重算一致：scd.go applyOne 的替换/拆分/闭合/新开只做点集的局部增量改写，等价 interval.Build 全量重算；TestInvariantRecompute 钉住，api.go SelfCheck 用独立 recompute 模型复核。
2 区间良构：interval.go Build/Split 只产出 from<to 的行且边界来自相邻变更点，scd.go 不产生跨行重叠；TestWellFormed 钉住（并发期由 TestConcurrentDifferentKeys 复核）。
3 顺序无关：scd.go 自由函数 search 仅按 Eff 二分定位，终态只取决于点集（同 Eff 后到替换）；TestOrderIndependence 用随机排列钉住。
4 失败不留痕：api.go Apply 持锁前先整批校验键非空/Eff 合法（不过即返回、零改动），scd.go Apply 在任何写入前先按批统计每键最终点数（同 Eff 替换不计数）超限即整批拒绝、通过才逐条并入；TestRejectionAtomic 钉住，四类哨兵由 TestApplyErrorSentinels 钉住。
