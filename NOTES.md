# 多项式求值 NOTES（Horner，int64 精确 + 溢出检出）

## 八行分步表（Horner：acc 从最高次系数起，逐步 acc=acc*x+coeff[i]）

1. `([1,2,3],2)`：acc=3 → 3·2+2=8 → 8·2+1=17 ⇒ **17**
2. `([5,-2,1],3)`：acc=1 → 1·3+(−2)=1 → 1·3+5=8 ⇒ **8**
3. `([0,0,1],10)`：acc=1 → 1·10+0=10 → 10·10+0=100 ⇒ **100**
4. `([1,1,1,1],-2)`：acc=1 → 1·(−2)+1=−1 → (−1)·(−2)+1=3 → 3·(−2)+1=−5 ⇒ **−5**
5. `([1,0,1],67108864)`：acc=1 → 1·67108864+0=67108864 → 67108864·67108864+1 ⇒ **4503599627370497**
6. `([],5)`：零多项式 ⇒ **0**
7. `([1],999)`：常数多项式，与 x 无关 ⇒ **1**
8. `([0,3037000500],3037000500)`：acc=3037000500 → 下一步 3037000500·3037000500 超 int64 ⇒ **溢出（乘法）**

## 三问

- (甲) 正确结果 **4503599627370497**（=2⁵²+1）。题目预期 float64 会丢 +1 得 4503599627370496；严格说 float64 有 53 位有效位、整数可精确表示到 2⁵³，故 2⁵²+1 恰好仍可精确表示，这一次 float64 不丢。真正的 +1 丢失发生在 2⁵³+1：**9007199254740993+0 被舍入为 9007199254740992**（demo 演示此真实舍入）。
- (乙) 3037000500²=9223372037000250000 > MaxInt64=9223372036854775807，应报**乘法溢出错误**。朴素 int64 相乘静默回绕成 **−9223372036709301616**（=9223372037000250000−2⁶⁴）；两个正数相乘得负数、且模 2⁶⁴ 后不等于真值，故可判定为错值。
- (丙) 大端误读为 `[3,2,1]`：3+2·2+1·4=**11**（正确为 17）。

## 四条不变量及其保证位置与钉住测试

1. 与 big 参照一致：`eval.Eval` 全程 int64 且每步检溢出，无溢出即精确；测试 `TestMatchesBigReference`（api_test.go，用 math/big 逐项累加对照）。
2. Horner≡逐项：循环不变式 acc=Σ coeff[i]·x^(i−k)；测试 `TestHornerEqualsTermwise`（api_test.go，先算 x^i 再逐项求和对照）。
3. 空/常数正确：`eval.Eval` 空切片直接返回 0，常数多项式不进循环；测试 `TestEmptyAndConstant`（api_test.go）。
4. 失败不留痕：`eval` 的 `mul`/`add` 先算后检、溢出即返回互不相同哨兵错误，调用方拿不到半成品；测试 `TestDistinctErrorsNoPartial`（api_test.go）。
