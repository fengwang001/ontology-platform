# Base64 推导与不变量

## "foobarb"（66 6F 6F 62 61 72 62，7 字节）分步编码

| 块 | 3 字节（不足补 0） | 4 个 6 位值（十进制） | 映射字符 |
|---|---|---|---|
| 1 | 66 6F 6F | 25, 38, 61, 47 | Z m 9 v |
| 2 | 62 61 72 | 24, 38, 5, 50 | Y m F y |
| 3 | 62 00 00 | 24, 32, —, — | Y g = = |

完整编码串：`Zm9vYmFyYg==`

**(甲)** `0x66` = `01100110` → 补 0 得 `011001 10(0000)` → 25(Z)、32(g)，编码为 `Zg==`，**两个** `=`。若误把「补齐到 4 字符」当「补齐到 3 字节」只补一个 `=`，得到 `Zg=`：长度 3 不是 4 的倍数，且少了一个 6 位组，非法。
**(乙)** `Zm9=` 长度 4 合法，但按规范**非法**：最后一个 6 位组 61 = `111101`，低 2 位是 `01`，应为 `00`。正确实现报「末尾未用位非零」哨兵错误，返回 `(nil, ErrTrailingBits)`；naive（不校验）会静默解出 `"fo"`。
**(丙)** naive 跳过长度校验、自动右补 `=`：`TWFu` 正常解出 `"Man"`；`TWE` 被补成 `TWE=` 后解出 `"Ma"`（`TWE=` 恰是 `"Ma"` 的合法编码，张冠李戴、静默出错）。正确实现应对 `TWE` 返回 `(nil, ErrLength)` 整体拒绝。

## 四条不变量及其保证位置与钉住测试

1. **往返一致**：`b64.Encode`/`b64.Decode` 严格互逆（b64/b64.go 的 `Encode`/`Decode`/`encodeBlock`/`decodeBlock`）；测试 `TestRoundTrip`（b64/b64_test.go）。
2. **规范输出**：`encodeBlock` 按余数补 0/1/2 个 `=` 且只输出字母表字符（b64/b64.go）；测试 `TestCanonicalPadding`。
3. **与朴素参照一致**：测试内手写教科书参照编码器逐字节比对；测试 `TestMatchesNaiveReference`。
4. **失败不留痕**：`Decode` 先做整体校验（长度/填充/字符/末尾未用位）再解码，出错返回 `(nil, 哨兵错误)`（b64/b64.go 的 `Decode`）；测试 `TestFaultInjection`。
