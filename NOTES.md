# NOTES

## 第三节推导（n=7，边 2→0 2→1 4→1 3→1 5→6 0→6 1→6）

初始入度：0:1 1:3 2:0 3:0 4:0 5:0 6:3，初始候选集 {2,3,4,5}。

| 步 | 输出 | 输出后候选集 | 本步被减为 0 的终点 |
|----|------|--------------|---------------------|
| 1 | 2 | {0,3,4,5} | 0 |
| 2 | 0 | {3,4,5} | 无 |
| 3 | 3 | {4,5} | 无 |
| 4 | 4 | {1,5} | 1 |
| 5 | 1 | {5} | 无 |
| 6 | 5 | {6} | 6 |
| 7 | 6 | {} | 无 |

拓扑序：2 0 3 4 1 5 6

- (甲) FIFO：初始队 [2,3,4,5]，输出 2 后 0 追加到队尾得 [3,4,5,0]，第 2 个输出错成 **3**（正确应为 **0**）。
- (乙) 最大编号优先：第 1 个输出错成 **5**（正确应为 **2**）。
- (丙) 加 0→2 后 0↔2 成环：正确 TopoSort 返回 `dag.ErrCycle`、不返回任何序列；不检测环的实现处理完 3,4,5 后卡死，静默返回部分序列 **[3 4 5]**。

## 四条不变量的保证位置与钉住测试

1. 合法拓扑序：节点只在入度减到 0 时才出堆输出（topo/topo.go `Sort` 主循环）；测试 `TestTopoOrderInvariants`（api/api_test.go）。
2. 与朴素参照一致：候选集用最小堆，每步取编号最小（topo/topo.go `intHeap`）；测试 `TestTopoOrderInvariants` 与测试内 `naive` 对拍。
3. 完整性/环检测：`len(out) != n` 时返回 `dag.ErrCycle` 且 nil 序列（topo/topo.go 末尾）；测试 `TestFaultInjection` 环用例。
4. 失败不留痕：`dag.AddEdge` 三项校验全部通过后才写状态（dag/dag.go）；测试 `TestFaultInjection` 断言 EdgeCount 不变、图仍可用。
