# 资源预留调度器 NOTES

## 八步推导（capacity=10，区间左闭右开）

| # | 操作 | 检查区间峰值 load | 判定 | 操作后已激活集 |
|---|---|---|---|---|
| 1 | Reserve(0,5,4) | 0（空集） | 接受（0+4≤10），id=1 | R1:[0,5)4 |
| 2 | Reserve(5,9,7) | 0（[5,9) 无人覆盖） | 接受（0+7≤10），id=2 | R1:[0,5)4 R2:[5,9)7 |
| 3 | Reserve(2,7,5) | 7（[2,5)=4，[5,7)=7） | 拒绝（7+5>10） | R1:[0,5)4 R2:[5,9)7 |
| 4 | Reserve(0,9,1) | 7（max(4,7)） | 接受（7+1≤10），id=4 | R1:[0,5)4 R2:[5,9)7 R4:[0,9)1 |
| 5 | Release(1) | — | 成功 | R2:[5,9)7 R4:[0,9)1 |
| 6 | Reserve(2,4,9) | 1（仅 R4 覆盖） | 接受（1+9=10≤10），id=6 | R2:[5,9)7 R4:[0,9)1 R6:[2,4)9 |
| 7 | Reserve(0,6,2) | 10（[2,4)=1+9） | 拒绝（10+2>10） | R2:[5,9)7 R4:[0,9)1 R6:[2,4)9 |
| 8 | Release(2) | — | 成功 | R4:[0,9)1 R6:[2,4)9 |

- (甲) 闭区间误判：R1 视为 [0,5]，点 5 处 load 错成 4，4+7=11>10，第 2 步被错判为**拒绝**（正确：峰值 0，接受）。
- (乙) 简单累加：4+7=11，11+1=12>10，第 4 步被错判为**拒绝**（正确：峰值 7，8≤10 接受）。
- (丙) 释放不彻底：[2,4) 峰值错成 R1+R4=5，5+9=14>10，第 6 步被错判为**拒绝**（正确：峰值 1，10≤10 接受）。

## 四条不变量的保证位置与钉住测试

1. 与朴素参照一致：`res.Core.CanAdd` 只对重叠候选求峰值（res/res.go）；测试 `TestNaiveConsistency`（api/api_test.go，随机序列对照逐点扫描）。
2. 容量不越界：接受前必过 `CanAdd` 峰值判定（rsv/rsv.go Reserve）；测试 `TestCapacityBound`。
3. 释放即失效：`Release` 同步从 `res.Core` 删除区间（rsv/rsv.go）；测试 `TestReleaseInvalidates`。
4. 失败不留痕：校验先于一切状态修改，拒绝路径无写（rsv/rsv.go）；测试 `TestFailureNoTrace` + `TestErrorsDistinct`。
