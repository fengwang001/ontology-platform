# 可复现有序跳表

## 层级提升的可复现方案（推导）

1. 跳表每次插入要决定新节点层级，经典做法是抛硬币（p=1/2 的几何分布）。
   若直接用 math/rand 且不播种，Go 会用随机运行时种子（Go 1.20+ 全局源自动随机播种），
   两次进程拿到的随机流不同，同一批 key 提升出的层级集合不同，
   于是 forward 指针结构不同；Range 走第 0 层顺序虽相同，但序列化结构字节不同——
   这正是事故中“两次构建结构抖动”的根因。
2. 结论：层级流必须是 seed 的确定性函数。本实现用 math/rand/v2.New(rand.NewPCG(seed, seed^0x9E))
   作为每个 SkipList 私有的 PRNG，插入顺序上第 i 次插入消费同一随机流：
   level=1，掷 coin=Uint64()&1，为 1 且未到 maxLevel 则升级。
   同 seed + 同 key + 同插入顺序 => 同层级序列 => 逐字节同构；不同 seed 的流不同 => 结构有差异。
3. Find/Delete 不消费随机流，因此构建后查询不影响可复现性。

## 语义代码位置与测试名（实现后回填）

TODO
