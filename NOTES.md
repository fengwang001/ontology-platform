# 增量物化视图：推导与不变量落点

## 1. 六步分步对照（分组 g，初始为空）

| # | 变更 | COUNT | SUM | MIN | MAX |
|---|---|---|---|---|---|
| 1 | Insert 5 | 1 | 5 | 5 | 5 |
| 2 | Insert 2 | 2 | 7 | 2 | 5 |
| 3 | Insert 9 | 3 | 16 | 2 | 9 |
| 4 | Retract 9 | 2 | 7 | 2 | 5 |
| 5 | Insert 2 | 3 | 9 | 2 | 5 |
| 6 | Retract 2 | 2 | 7 | 2 | 5 |

(甲) COUNT、SUM 只需「当前标量 + 本条增量」：它们是可分配聚合，存在二元合并算子 ⊕ 使 f(X∪Y)=f(X)⊕f(Y)，且撤回有逆元（-1 / -v）。
MIN、MAX 做不到：撤回当前极值时，标量里没有「次小/次大值」，也没有每个值还活着几份。判据：聚合值能由一个结合二元算子从单值结果合并得到（且撤回要求该算子可逆），才支持纯标量增量维护；MIN/MAX 的合并不可逆（无法从最大值还原其余元素）。

(乙) 第 4 步朴素 MAX（只存标量「当前最大值 9」）撤回后只能给出错误值 **0**（或被迫报「无」），正确值是 **5**。
「撤回值若等于当前极值就清空」的补救在**第 6 步**继续出错：第 4 步清空 → 第 5 步 Insert 2 得 MAX=2 → 第 6 步 Retract 2 又清空，但此时 {5,2} 仍活着，正确 MAX=5；重复值让「清空」把别的副本与无关值一起丢了。
最小附加状态：**值 → 存活份数的有序多重集**（AVL，每个不同值一个节点加频数字段）。MIN/MAX 即最左/最右节点；插入加频数，撤回减频数、归零才删节点，重复值天然正确；树高 O(log m)，每次 Apply 只访问 O(log m) 个已存值。它保留了全部值的有序存活信息，故充分且最小。

## 2. 四条不变量的保证位置与钉住测试

1. 逐步一致：`agg.(*Group).Apply`（agg/agg.go）四个本体随每条变更同更新，`api.(*View).Feed`（api/api.go）逐条落盘；钉住：`TestStepByStepRecompute`、`TestSixStepTable`。
2. 撤回是插入的逆：AVL `ins`/`del` 频数字段对称增减、COUNT/SUM 加减逆元（agg/agg.go）；钉住：`TestRetractInverse`。
3. 空分组语义：`Min.Value`/`Max.Value` 返回 `(0,false)`，`api.Quad.HasRange` 原样透传，绝不用 0 冒充；钉住：`TestEmptyGroup`。
4. 失败不留痕：`agg.(*Group).Apply` 先做合法性/溢出预检再变更任何本体；`api.(*View).Feed` 收集已应用步骤、失败时按逆事件回滚并删除本次新建分组；钉住：`TestAtomicReject`（三类错误互异由同函数断言），整体由 `TestSelfCheck` 复核。
