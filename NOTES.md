# Union-Find Notes

## 按秩合并与路径压缩

- 若 Union 总把 y 的根挂到 x 的根下，输入可依次制造单链子树；第 k 次 Find 可能经过 k 个父指针，最坏时间为 O(n)。
- 按秩合并只把秩较小的根挂到秩较大的根下；秩近似树高，只有两棵同秩树合并时新根秩才加一。
- 因此高度达到 h 至少需要 2^h 个元素，故 h <= floor(log2(n))；Find 最多跟随 O(log n) 个父指针。
- 路径压缩在 Find 时把沿途节点直接指向根，立即缩短后续查询；它不增加按秩定义的高度上界。
- 按秩合并让树始终保持低高度，路径压缩反复摊平实际访问路径；二者配合的 Find/Union 均摊复杂度为反阿克曼函数级，即 O(alpha(n))，实际接近常数。
- 测试钉住 n=2^16 的链式 Union：平衡实现 Find 跳数 <= 16；内联的“总挂到 x 根下”错误实现跳数 > 1000。
