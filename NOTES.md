# 滑动去重窗口 NOTES

## 八步推导（W=5，maxOpen=3）

| # | 事件 | 判定 | 该 ID 的 last | 保留集合(ID→last) | 淘汰 |
|---|------|------|---------------|--------------------|------|
| 1 | (X,10) | 接受（新 ID） | 10 | {X:10} | 无 |
| 2 | (X,15) | 接受（d=5>=W） | 15 | {X:15} | 无 |
| 3 | (Y,20) | 接受（新 ID） | 20 | {X:15,Y:20} | 无 |
| 4 | (Z,30) | 接受（新 ID） | 30 | {X:15,Y:20,Z:30} | 无 |
| 5 | (Y,24) | 重复（d=4<W） | 20（不变） | {X:15,Y:20,Z:30} | 无 |
| 6 | (W,40) | 接受（新 ID，表满） | 40 | {Y:20,Z:30,W:40} | 淘汰 X（last=15 最小） |
| 7 | (Y,25) | 接受（d=5>=W） | 25 | {Y:25,Z:30,W:40} | 无 |
| 8 | (Z,32) | 重复（d=2<W） | 30（不变） | {Y:25,Z:30,W:40} | 无 |

- (甲) d==W 属 `d>=W`，判**接受**，X.last=15。若错写成 `d<=W` 算窗口内，第 2 步错成**重复**，X.last 错留 **10**。
- (乙) 第 6 步淘汰 last 最小的 **X(15)**。若错淘汰 last 最大者，会错淘汰 **Z(30)**；第 8 步 (Z,32) 因 Z 无记录错成**接受**（正确为重复）。
- (丙) 第 5 步重复不刷新，Y.last 保持 **20**。若重复也刷新到 24，第 7 步 (Y,25) d=1<W 错成**重复**（正确为接受）。

## 四条不变量：保证位置与钉住它的测试

1. 窗口不重叠：`win.Duplicate` 半开区间判定 + `dedup.Table.Dedup` 仅接受时写 last；`TestWindowNonOverlap`。
2. 与朴素参照一致：流式与测试内朴素重放共用同一判定规则逐事件比对；`TestNaiveReplayConsistency`。
3. 淘汰正确：`dedup.Table.Dedup` 表满时选 last 最小者淘汰，重复/乱序路径不触碰表与 last；`TestEviction`、`TestEvictedIsNew`。
4. 失败不留痕：`api.New`/`api.Dedup` 先校验后动状态，哨兵错误互不相同；`TestRejectNoSideEffect`。
