# EWMA 初始化与撤回 — 推导与不变量

## 八步推导（alpha = 0.25）

| # | 操作 | hist（操作后） | EWMA（操作后） |
|---|------|----------------|----------------|
| 1 | Add(10) | [10] | 10（首值直接作种子） |
| 2 | Add(20) | [10,20] | 0.25·20+0.75·10 = 12.5 |
| 3 | Add(10) | [10,20,10] | 0.25·10+0.75·12.5 = 11.875 |
| 4 | Add(30) | [10,20,10,30] | 0.25·30+0.75·11.875 = 16.40625 |
| 5 | Retract(10) | [10,20,30]（删索引2） | 重算：10→12.5→16.875 |
| 6 | Add(40) | [10,20,30,40] | 0.25·40+0.75·16.875 = 22.65625 |
| 7 | Retract(20) | [10,30,40] | 重算：10→15→21.25 |
| 8 | Add(50) | [10,30,40,50] | 0.25·50+0.75·21.25 = 28.4375 |

- (甲) 第1步正确值 **10**；若用 0 种子 `0.25·10+0.75·0` 会错成 **2.5**。
- (乙) 第5步正确重算值 **16.875**；若用反向一步 `(16.40625−0.25·10)/0.75` 会错成 **18.541666…**（它撤销的是索引0的10，且浮点也不精确）。
- (丙) 若第5步误删最早一次（索引0），hist=[20,10,30]，重算 20→17.5→**20.625**，与正确的 16.875 不同。

## 四条不变量及代码落点

1. 与批量重算一致：`ema.EMA.Add` 的增量衰减与 `recompute` 用同一公式；撤回后一律走 `recompute` 从零重算。测试：`TestMatchesBatchRecompute`（随机序列对拍）。
2. 初始化规则：`Add` 在 `!init` 时直接 `value=v`，无 0 种子；空序列 `Initialized()==false`。测试：`TestFirstAddSeedsDirectly`。
3. 撤回精确：`Retract` 删 hist 中最后一次出现的 v 后整体重算，等价于该值从未加入。测试：`TestRetractExactEquivalence`。
4. 失败不留痕：`New` 先校验 alpha、`Retract` 先查空/找值，全部通过后才改状态。测试：`TestFailureLeavesNoTrace`。
