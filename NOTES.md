# NOTES

## 三、n=3 分步推导  A=[[0,1,1],[1,0,1],[1,1,0]]，b=[5,4,3]

| 步 | 该列候选绝对值 | 主元行/主元值 | 交换 | 步末状态 |
|---|---|---|---|---|
| k=0 | \|0\|,\|1\|,\|1\| | 行1，1（并列取下标小） | 换 r0↔r1 | A=[[1,0,1],[0,1,1],[0,1,-1]]，b=[4,5,-1] |
| k=1 | \|1\|,\|1\| | 行1，1 | 不换 | A=[[1,0,1],[0,1,1],[0,0,-2]]，b=[4,5,-6] |
| k=2 | \|-2\| | 行2，-2 | 不换 | 上三角完成 |
| 回代 x[2] | — | — | — | x[2]=-6/-2=3 |
| 回代 x[1],x[0] | — | — | — | x[1]=(5-1·3)/1=2；x[0]=(4-0·2-1·3)/1=1 |

- (甲) 交换只作用于 A：b 保持 [5,4,3] 参与消元，k=0 后 b=[5,4,-2]，k=1 后 b=[5,4,-6]，回代得错解 **x=[2,1,3]**（正确 [1,2,3]）。
- (乙) 不选主元直接用 A[0][0]=0 做主元：m=1/0=+Inf（float64 不 panic），随后 0·Inf=NaN 扩散到整列与 b，最终 **x 全为 NaN**——既不报奇异、也解不出正确结果。
- (丙) 回代减号写反成加号：x[2]=3 不变；**x[1]=(5+1·3)/1=8**；**x[0]=(4+0·8+1·3)/1=7**（正确 x[1]=2、x[0]=1）。

## 二、四条不变量的落点

1. 残差 ≤1e-9：`elim.Solve` 部分主元消元+逆序回代保证；测试 `api_test.TestResidual`。
2. 输入不改：`elim.Solve` 校验后先整体复制 a、b 再运算；测试 `api_test.TestInputUnchanged`。
3. 主元确定：`pivot.Pick` 用严格 `>` 更新最优，并列恒保留下标最小行；测试 `pivot.TestPickTieDeterministic`。
4. 失败不留痕：`elim.Solve` 先做全部维度/空系统校验再复制，奇异时直接返回哨兵错误，包内无可写全局状态（pivot 计数器只增不改逻辑且恒 0）；测试 `api_test.TestRejectNoSideEffect`。
