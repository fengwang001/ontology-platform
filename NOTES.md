# 版本驱动物化视图缓存：推导与不变量

## 八步分步表（记法 `Value@Version`；s0/s1/T = stamp(g0)/stamp(g1)/totalStamp）

| 步 | 操作 | (g0,a) | (g0,b) | (g1,c) | (g1,d) | s0/s1/T | 返回 | 缓存 |
|---|---|---|---|---|---|---|---|---|
| 1 | Write(g0,a,5) | 5@1 | 0@0 | 0@0 | 0@0 | 1/0/1 | 无 | — |
| 2 | ReadG(g0) | 5@1 | 0@0 | 0@0 | 0@0 | 1/0/1 | 5 | miss→g0={5,1} |
| 3 | Write(g0,b,3) | 5@1 | 3@1 | 0@0 | 0@0 | 2/0/2 | 无 | — |
| 4 | ReadG(g0) | 5@1 | 3@1 | 0@0 | 0@0 | 2/0/2 | 8 | miss(1≠2)→g0={8,2} |
| 5 | Write(g1,c,7) | 5@1 | 3@1 | 7@1 | 0@0 | 2/1/3 | 无 | — |
| 6 | ReadTotal() | 5@1 | 3@1 | 7@1 | 0@0 | 2/1/3 | 15 | miss→T={15,3} |
| 7 | Write(g0,b,10) | 5@1 | 10@2 | 7@1 | 0@0 | 3/1/4 | 无 | — |
| 8 | ReadTotal() | 5@1 | 10@2 | 7@1 | 0@0 | 3/1/4 | 22 | miss(3≠4)→T={22,4} |

- **(甲)** 第 8 步返回 **22**。若 Total 缓存永不失效，第 6 步算出 15 后不再校验，第 8 步错返回 **15**（漏掉第 7 步 b 从 3→10 的 +7）。
- **(乙)** 第 4 步应返回 **8**。若失效判定只看代表键 a 的 Version：第 3 步写的是 b，a.Version 仍为 1，与缓存戳相等被判新鲜，错返回第 2 步缓存的 **5**——组内其他记录的写被漏判。
- **(丙)** 反例序列：`Write(g1,c,7)`；`ReadG(g1)`=7（清标记）；`Write(g1,c,9)`（置脏）；`ReadG(g0)`（见脏重算 g0 并**清标记**）；`ReadG(g1)` → 标记已被 g0 的读清除，命中旧缓存，错返回 **7**（应为 9）。共享脏标记无法表达「g1 缓存仍过期」——任一视图的读都会把它清掉；版本号只增不减、读不清除，每个视图各自比对 stamp 才能精确判失效。

## 四条不变量的落点

1. **与批量重算一致**：`vcache` miss 时调 `ver.Store.SumGroup/SumTotal` 全量重算后刷新缓存 —— `TestViewMatchesBatch`。
2. **缓存新鲜性**：`vcache.readG/readTotal` 返回前以 `Stamp()` 校验并写入当前戳 —— `TestCacheFreshAfterRead`。
3. **版本单调**：`ver.Store.Write` 只做 `Version+=1` 且 stamp 增量 +1，无回退路径 —— `TestVersionMonotonic`。
4. **失败不留痕**：`ver.Store.Write` 先完成全部校验再改任何状态 —— `TestRejectLeavesState`。
