# Indexed Heap Notes

## DecreaseKey 为什么只能 sift-up

设下标从 1 开始：父下标 `i/2`，子下标 `2i`、`2i+1`。

- 把节点 x 的键由大变小：x 与两个子节点原本满足 x <= child，x 变小后仍满足，
  所以 x 不可能需要向下移动。
- x 变小后可能违反 x >= parent；反复与父节点比较并交换（sift-up）即可恢复，
  交换次数最多为树高 `floor(log2(n))`，故上界 `2·log2(n)+2`。
- 若 decrease 后改做 sift-down：x 会与更小的子节点交换，绕过仍比 x 大的父节点，
  父节点于是压在 x 上方，最小堆被破坏。

### 反例

堆 `[3,5,7,9,11,13]`，把 9 降到 1：

- 正确 sift-up：依次 1<->5、1<->3，得 `[1,3,7,5,11,13]`，Pop 序列
  `[1,3,5,7,11,13]`。
- 错误 sift-down：1 与子 11 交换，再与 13 交换，1 沉底；根仍为 3，
  Pop 首元素是 3 而不是 1，Pop 序列错误。

## 代码位置与测试

（实现完成后回填）
