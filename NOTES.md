# 版本驱动物化视图缓存：推导与不变量

## 八步分步表（`V@N` = Value@Version；s0/s1/tot = stamp(g0)/stamp(g1)/totalStamp）

初始：a=0@0 b=0@0 c=0@0 d=0@0，s0=0 s1=0 tot=0，缓存全空。

| # | 操作 | a | b | c | d | s0 | s1 | tot | 返回 | 缓存 |
|---|------|---|---|---|---|----|----|-----|------|------|
| 1 | Write(g0,a,5) | 5@1 | 0@0 | 0@0 | 0@0 | 1 | 0 | 1 | 无 | 不动缓存 |
| 2 | ReadG(g0) | 5@1 | 0@0 | 0@0 | 0@0 | 1 | 0 | 1 | 5 | 未命中→cacheG[g0]={5,1} |
| 3 | Write(g0,b,3) | 5@1 | 3@1 | 0@0 | 0@0 | 2 | 0 | 2 | 无 | 不动缓存 |
| 4 | ReadG(g0) | 5@1 | 3@1 | 0@0 | 0@0 | 2 | 0 | 2 | 8 | 未命中(1≠2)→{8,2} |
| 5 | Write(g1,c,7) | 5@1 | 3@1 | 7@1 | 0@0 | 2 | 1 | 3 | 无 | 不动缓存 |
| 6 | ReadTotal() | 5@1 | 3@1 | 7@1 | 0@0 | 2 | 1 | 3 | 15 | 未命中→cacheT={15,3} |
| 7 | Write(g0,b,10) | 5@1 | 10@2 | 7@1 | 0@0 | 3 | 1 | 4 | 无 | 不动缓存 |
| 8 | ReadTotal() | 5@1 | 10@2 | 7@1 | 0@0 | 3 | 1 | 4 | 22 | 未命中(3≠4)→{22,4} |

- **(甲)** 第 8 步应返回 **22**。若 Total 缓存「一次算出、永不失效」，第 6 步算出的 15 被直接保留，第 8 步错成 **15**（漏掉第 7 步 b 从 3→10 的 +7）。
- **(乙)** 第 4 步应返回 **8**。若失效判定只看代表键 a 的 Version：第 3 步写的是 b，a.Version 仍是 1，与缓存戳相等被判新鲜，第 4 步错返回旧值 **5**（漏掉 b 的 3）。组内任一记录写都使组变脏，必须看全组 Version 之和。
- **(丙)** 反例序列：`W(g1,c,7)`→`ReadG(g1)`=7（清标记）→`W(g0,a,5)`（置脏）→`ReadG(g1)`（重算 7，清标记）→`W(g1,c,9)`（置脏）→`ReadG(g0)`（重算 5，**清标记**）→`ReadG(g1)`：标记已被无关的 g0 读清除，命中旧缓存，错返回 **7**（应为 9）。共享脏标记被「任一读」清除，无法记住「哪个组脏」；版本号只增不减、读不清除，每组各自比对，互不抹掉对方的失效。

## 四条不变量：代码位置 → 钉住它的测试

1. 与批量重算一致：`vcache` 失效即整组/全表重算（vcache/vcache.go `ReadG`/`ReadTotal`）→ `TestViewMatchesBatch`
2. 缓存新鲜性：重算后以当前 stamp 刷新缓存（vcache/vcache.go）→ `TestCacheStampCurrent`
3. 版本单调：`ver.Write` 只做 `Version+=1`、stamp 只增（ver/ver.go）→ `TestVersionMonotonic`
4. 失败不留痕：`ver.Write` 先校验后变更，三类哨兵错误（ver/ver.go）→ `TestRejectedOpsLeaveState`
