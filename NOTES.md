# EDF 准入推导

设任务集在长度 L 的时间窗内被接受，任务 i 的周期为 Ti、执行量为 Ci。

1. 任务 i 在 [0,L) 释放 floor(L/Ti) 个作业，总需求为 floor(L/Ti)Ci；若存在可行调度，则总需求不能超过处理器供给 L，所以 sum(Ci/Ti) <= 1 是必要条件。
2. EDF 总是执行绝对截止期最早的就绪作业。若某个作业错过截止期，考察从最近空闲点到该截止期的忙等区间：其中所有被执行作业的截止期都不晚于错失作业，但总执行需求仍超过区间长度。周期等于截止期时，这些需求正来自各任务按周期释放的作业，推出 sum(Ci/Ti)>1，矛盾。因此 sum(Ci/Ti)<=1 是充分条件。
3. 固定优先级不按截止期动态选择任务，短周期任务可长期抢占长周期任务。RM 反例为 (1,2)、(2,5)、(2,7)，利用率 1/2+2/5+2/7=59/70<1；RM 中第三个任务在 t=7 前只得到 1 刻，错过截止期；EDF 不错失。
