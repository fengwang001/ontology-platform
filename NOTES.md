# 计数翻滚窗口 NOTES（size=5, lateness=2）

## 八步推导（元素按 (Pos,Val) 到达，同一 Key）

| # | 元素 | 之后 wm | 窗口 | 判定 | 本步触发 | 之后开放的窗口(cnt) |
|---|------|--------|------|------|----------|--------------------|
| 1 | (0,10) | 0 | 0 | 正常 | 无 | w0(1) |
| 2 | (1,20) | 1 | 0 | 正常 | 无 | w0(2) |
| 3 | (2,30) | 2 | 0 | 正常 | 无 | w0(3) |
| 4 | (3,40) | 3 | 0 | 正常 | 无 | w0(4) |
| 5 | (4,50) | 4 | 0 | 正常 | w0 sum=150 | 无 |
| 6 | (5,60) | 5 | 1 | 正常 | 无 | w1(1) |
| 7 | (9,90) | 9 | 1 | 正常 | 无 | w1(2) |
| 8 | (7,70) | 9 | 1 | 迟到被接受（7 >= 9-2） | 无 | w1(3) |

- (甲) 窗口 [0,5) 在第 5 步 (4,50) 到达时触发，聚合 150。若错写成 `cnt==size-1` 触发：第 4 步即以 (0,10)(1,20)(2,30)(3,40) 触发、聚合错成 100；第 5 步 (4,50) 因窗口已关闭被丢弃，最终该窗口聚合错成 100。
- (乙) (5,60) 应归窗口 1（floor(5/5)=1）。若错用 floor((Pos-1)/size)=0，它被归到已关闭的窗口 0 而遭丢弃：窗口 1 的 cnt 少 1（第 8 步后仅 2 而非 3），丢弃数 +1。
- (丙) 第 8 步接受阈值为 Pos >= wm-lateness = 9-2 = 7，7>=7 故被接受。若错写成严格大于 Pos > wm-lateness，7>7 不成立，(7,70) 被丢弃，窗口 1 的 cnt 少 1（停在 2）。

## 四条不变量：保证位置与钉住它的测试

1. 批量重算一致：`cwagg.Agg.Add` 仅在接受分支累加 `sum`、丢弃分支不碰聚合 → `api_test.TestBatchConsistency`。
2. 输出自洽：`cwagg` 的 `window.fired` 置位后拒绝再接受、每窗口只 append 一次 → `api_test.TestFireOnceAndLate`。
3. 水位单调+计数守恒：`cwagg.Agg.Add` 末尾 `max` 推进水位、`cnt` 触发后不再增 → `cwagg_test.TestWatermarkCount`。
4. 失败不留痕：`api.Feed` 先整批校验再应用、`New` 先校验参数 → `api_test.TestFeedRejectedAtomic`。
