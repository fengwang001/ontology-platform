# NOTES

## 推导（m=3,n=2；A=[[1,1],[1,2],[1,3]]，b=[2,3,5]，正确解 x=[1/3,3/2]）

1. Gram：[[3,6],[6,14]]（G00=1+1+1=3；G01=1·1+1·2+1·3=6；G11=1+4+9=14）
2. Aᵀb：[10,23]（c0=2+3+5=10；c1=1·2+2·3+3·5=23）
3. 消元（无主元）：行1−(6/3)·行0 → [[3,6],[0,2]]，右端 [10, 23−2·10=3]
4. 回代：x1=3/2；x0=(10−6·(3/2))/3=(10−9)/3=1/3 → x=[1/3,3/2]
5. 残差 b−A·x：A·x=[11/6,10/3,29/6]，故残差=[1/6,−1/3,1/6]，且 Aᵀr=[0,0]

- (甲) 只填上三角、下三角当 0：G=[[3,6],[0,14]] → x1=23/14，x0=(10−6·23/14)/3=(2/14)/3=**1/21**，解错成 **[1/21, 23/14]**
- (乙) c0 漏加最后一行：c0=1·2+1·3=**5**（应为 10）
- (丙) 回代 x0 漏减 G01·x1：x0=10/3=**10/3**（应为 1/3），解错成 **[10/3, 3/2]**

## 四条不变量（保证位置 / 钉住的测试）

1. 残差最小：`lsq/lsq.go` 的 SolveG 对完整对称 Gram 消元+回代解正规方程，`api.Solve` 调用它；由 `api/api_test.go` 的 **TestNaiveConsistency** 钉住（随机列满秩矩阵，逐项核验 |Aᵀ(Ax−b)|≤1e-9，并对扰动 δ 比较残差）。
2. Gram 对称：`gram/gram.go` 的 Gram 对 i≤j 算一次列点积后镜像写入 (i,j) 与 (j,i)，逐位相等；由 **TestGramSymmetric** 逐位钉住，并用 SolveG 走完整矩阵核验解一致。
3. 一次 Factor 多次 Solve：`api/api.go` 的 Factor 只在成功时把 Gram 存入缓存并原子 +1（gramCount），Solve 只复制缓存 Gram、不再计算；由 **TestFactorOnceGramCount** 钉住（m=100/1000/10000 个不同右端，计数恒为 1）。
4. 失败不留痕：`api/api.go` 的 Factor/Solve 全部先校验、最后一句才提交状态，秩亏在副本上探测，计数器仅成功路径 +1；由 **TestRejectedLeavesNoTrace** 钉住（四类哨兵互不相同，拒绝后缓存/计数不变且可正常继续）。
