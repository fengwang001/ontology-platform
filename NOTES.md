# NOTES — 带水位线的双流窗口连接

## 第三节推导：W=10, delay=3，同一 Key，事件序列 L@5 R@6 L@12 R@8 L@2 R@15

join 记为 `(窗口起点, LeftTS, RightTS)`；wm = max(wm, TS−3)，只进不退。

| # | 事件 | 之后 wm | 窗口 | 判定 | 本步 join | 之后仍保留 |
|---|------|--------|------|------|-----------|------------|
| 1 | L@5  | 2  | [0,10)  | 正常 | 无 | [0,10): L5 |
| 2 | R@6  | 3  | [0,10)  | 正常 | (0,5,6) | [0,10): L5,R6 |
| 3 | L@12 | 9  | [10,20) | 正常 | 无 | [0,10): L5,R6；[10,20): L12 |
| 4 | R@8  | 9  | [0,10)  | 正常 | (0,5,8) | [0,10): L5,R6,R8；[10,20): L12 |
| 5 | L@2  | 9  | [0,10)  | 正常 | (0,2,6), (0,2,8) | [0,10): L5,R6,R8,L2；[10,20): L12 |
| 6 | R@15 | 12 | [10,20) | 正常 | (10,12,15) | [10,20): L12,R15（[0,10) end=10≤12 已清除）|

总计 5 条 join：1+1+0+1+2（第 5 步）+1。

**(甲)** 完整重放（不增量）：第 5 步时 [0,10) 内 L{5,2}×R{6,8}，输出 **4** 条（正确 2）；各步输出 1+2+4+1=**8**（正确 5）。重复配对：`(0,5,6)` 在第 2、4、5 步共输出 3 次（多 2 次），`(0,5,8)` 在第 4、5 步共 2 次（多 1 次）。

**(乙)** delay=0, W=10，序列 L@5, R@10, R@5。正确实现：第 2 步后 wm=10，`end≤wm` 清除 [0,10)，保留 `{[10,20): R10}`；第 3 步 R@5 落入 [0,10)，wm=10≥end=10 → 迟到丢弃，0 条 join。错误实现（`wm>end`）：第 2 步后 [0,10) 未清除，保留 `{[0,10): L5, [10,20): R10}`；第 3 步 R@5 被接受，多输出 join `(0,5,5)`。

**(丙)** TS=−5, W=10：正确窗口为 **[−10,0)**（floor 除法 k=−1）；Go 向零截断 `−5/10==0` 错分为 **[0,10)**。序列 L@−5、R@5（delay=0）：正确实现两者分属 [−10,0) 与 [0,10)，输出 **0** 条；错误实现把两者都放进 [0,10)，多输出 join `(0,−5,5)`。

## 四条不变量：保证位置与钉住测试

1. **与批量叉积一致**：`wjoin.Feed` 增量配对 + `Joiner.Flush` 推平水位线；参照 `api.Batch`；测试 `TestFlushMatchesBatch`。
2. **无重复配对**：`wjoin.feedOne` 只把新事件与已保留的异侧事件配一次；测试 `TestNoDuplicateJoins`。
3. **水位线单调**：`wjoin.advance` 只取 max，丢弃事件同样先过 advance；测试 `TestWatermarkMonotonic`。
4. **失败不留痕**：`wjoin.Feed` 先整批校验、全部通过才改状态；测试 `TestRejectedFeedLeavesNoTrace`。
