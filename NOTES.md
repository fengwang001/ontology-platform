# 热点键检测与再分片 — 推导与不变量

## 第三节：S=2, T=3，八事件分步表

| 步 | 事件 | 落点 | 触发迁移 | cnt（按分片号升序） |
|---|---|---|---|---|
| 1 | (a,0) | 0 | 否 | [1,0] |
| 2 | (a,0) | 0 | 否 | [2,0] |
| 3 | (a,0) | 0 | 是：a 累计达 T=3，分配专属分片 2 | [3,0,0] |
| 4 | (b,1) | 1 | 否 | [3,1,0] |
| 5 | (a,0) | 2 | 否（a 已热点） | [3,1,1] |
| 6 | (a,0) | 2 | 否 | [3,1,2] |
| 7 | (c,1) | 1 | 否（c 累计仅 1） | [3,2,2] |
| 8 | (a,0) | 2 | 否 | [3,2,3] |

- (甲) 正确 `Counts() = [3,2,3]`。若触发事件直接落专属分片：基础分片 0 只计到 2、专属分片多计 1，错成 `[2,2,4]`。
- (乙) 若触发事件双写（基础+专属各计一次）：错成 `[3,2,4]`，总计数 9，多 1。
- (丙) 若阈值错写成 `> T`：a 的第 3 个事件累计为 3 不迁移，第 4 个事件（第 5 步）累计 4 > 3 才迁移，该事件仍落基础分片 0；之后第 6、8 步落专属。错成 `[4,2,2]`。

## 第二节：四条不变量的保证位置与钉住测试

1. 总量守恒：`api.Feed` 每个被接受的事件恰好执行一次 `cnt[dest]++`（`route.Router.Feed` 定唯一落点），无任何路径重复或跳过。测试 `TestCountsInvariant`（逐事件断言 Σcnt）。
2. 与朴素参照一致：`api_test.go` 内独立暴力模拟 `naiveCounts`，逐事件套路由规则，与 `Counts()` 比对。测试 `TestCountsInvariant`。
3. 迁移原子性：`route.Router.Feed` 先按当前 hot 状态定落点并计数，之后才可能置 hot/分配专属分片，触发事件天然落基础分片；hot 一旦置位永久保持。测试 `TestMigrationAtomicity`。
4. 失败不留痕：`api.Feed` 先对整批做全量校验（S/T/Base/Key），任一非法即整体返回哨兵错误，不触碰任何状态。测试 `TestRejectNoTrace`。
