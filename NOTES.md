# NOTES — 跳洞检测器推导（New(2), W=2）

十一步分步表（事件 Feed 之后的状态）：

| 步 | seq | H | seen | gaps | 备注 |
|---|---|---|---|---|---|
| 1 | 1 | 1 | {} | {} | 并入连续前缀 |
| 2 | 2 | 2 | {} | {} | 并入 |
| 3 | 3 | 3 | {} | {} | 并入 |
| 4 | 4 | 4 | {} | {} | 并入 |
| 5 | 6 | 4 | {6} | {} | 在途：6−5=1<W，5 未错过窗口，不判洞 |
| 6 | 9 | 7 | {9} | {5,7} | 判 5 洞→并入 6→判 7 洞；8 未错过窗口（9−8=1<2） |
| 7 | 2 | 7 | {9} | {5,7} | 忽略（2≤H），状态不变 |
| 8 | 3 | 7 | {9} | {5,7} | 忽略 |
| 9 | 6 | 7 | {9} | {5,7} | 忽略（6 已并入过） |
| 10 | 10 | 10 | {} | {5,7,8} | 判 8 洞→并入 9→并入 10 |
| 11 | 11 | 11 | {} | {5,7,8} | 并入 |

- **甲**（第5步 Feed(6)）：正确实现**不**判洞，`Gaps()=[]`——6−5=1<W=2，5 仍在乱序窗口；「见跳即报」（`seq>H+1` 就把 H+1..seq−1 全判洞）会在此刻**误报 5**。
- **乙**（第6步 Feed(9)）：正确 `gaps={5,7}`，**洞大小 2**；错误实现不排除已见乱序序号 6、也不排除尚未错过窗口的 8，会**多报 6 和 8**，错报成 `{5,6,7,8}`、**洞大小 4**。
- **丙**（第7–9步重复 2/3/6）：三者均 `≤H` 被忽略，`H=7`、`Gaps()={5,7}` **均不变**；若用固定容量 W=2 的环形缓冲存 seen 且不去重，插 2、再插 3 时把真正在途的 **9 挤出并误判 9 为洞**。

不变量保证位置 / 钉住的测试：

1. **三分互斥·朴素参照一致**：`det/det.go` 中 `(*Detector).Feed` 的收敛循环每轮只把 `H+1` 从 `seen` 删除或追加进 `gaps`，三者不重叠——钉于 `det` 包 `TestNaiveEquivalence`（逐事件对拍朴素全集参照）。
2. **乱序容忍·无早判**：仅当 `seq.Overdue(x,maxSeen,W)`（`maxSeen-x>=W`，减法写法防 `x+W` 溢出）成立才判洞，见 `seq/seq.go` 与 `det` 收敛分支 2——钉于 `TestNoEarlyGap`。
3. **重复不误报·无晚报**：`Feed` 开头 `seq.Covered(seq,H)` 命中即在任何写入前返回；`seen` 是 map 天然去重，`gaps` 只随 H 递增追加、每序号至多一次——钉于 `TestDuplicateIgnored`。
4. **失败不留痕**：非法序号 / 溢出在 `det.Feed` 任何写入前返回，非法窗口在 `api.New` 建状态前返回，三个哨兵错误互异——钉于 `api` 包 `TestWalkAndErrors`（非法序号/溢出、拒后无痕、仍可继续）与 `TestNewRejectsBadWindow`（非法窗口）。
