# 定名规则推导（第三节）

## 优先次序
`filename*`（扩展标记）胜出，`filename` 仅在没有扩展版时启用。
理由：RFC 6266 规定两者并存时接收方应使用 `filename*`；
扩展版经字符集解码，信息更完整。参数用有序切片传递，
选择时显式按标记分支，不依赖 map 遍历顺序，结果确定。

## 安全化处理次序（自上而下，每步确定）
1. 选定候选值：有 `filename*` 用其解码值；否则用 `filename`，
   并对它做一次防御性百分号解码（非法序列原样保留），
   使 `%2e%2e`、`%2f` 这类编码伪装在判定前现形。
2. 去掉首尾空白（unicode 空白）。
3. 删除控制字符（含 NUL、DEL）。
4. 删除路径分隔符 `/` 与 `\`。
5. 反复删除 `..` 子串直到不存在（`....`→空，`a..b`→`ab`）。
6. 结果为空或全是 `.` ⇒ 兜底名 `unnamed`（确定、非空、安全）。

## 不变量 3 如何满足
- 第 2 类剔除只删「不可落盘的成分」：首尾空白、控制字符、
  分隔符、点序列。两个输入若只在被删成分上不同（如 `a/b` 与
  `ab`、`..` 与 `%2e%2e`），撞名是规则允许的。
- 百分号解码是规范化而非丢失：`a%41b` 与 `aAb` 指向同一字节
  序列，视为同一名字的不同写法，归并不丢信息。
- 除上述两类外不做任何折叠：大小写、空格、分号、扩展名全部
  保留，故两个「所指字节不同」的名字定名结果仍不同。
- 无 filename 参数的输入不含任何定名信息，归入兜底名不算丢失。
