# NOTES — 简单多边形面积与重心

## 第三节推导：L 形 (0,0)(6,0)(6,2)(2,2)(2,6)(0,6)，逆时针

| 边 i→i+1 | 叉积 c=xᵢyᵢ₊₁−xᵢ₊₁yᵢ | (xᵢ+xᵢ₊₁)·c | (yᵢ+yᵢ₊₁)·c |
|---|---|---|---|
| (0,0)→(6,0) | 0·0−6·0 = 0 | 6·0 = 0 | 0·0 = 0 |
| (6,0)→(6,2) | 6·2−6·0 = 12 | 12·12 = 144 | 2·12 = 24 |
| (6,2)→(2,2) | 6·2−2·2 = 8 | 8·8 = 64 | 4·8 = 32 |
| (2,2)→(2,6) | 2·6−2·2 = 8 | 4·8 = 32 | 8·8 = 64 |
| (2,6)→(0,6) | 2·6−0·6 = 12 | 2·12 = 24 | 12·12 = 144 |
| (0,6)→(0,0) | 0·0−0·6 = 0 | 0·0 = 0 | 0·0 = 0 |
| **Σ** | **40** | **264** | **264** |

- (甲) 面积 A = Σc/2 = 40/2 = **20**（= 6·6 − 4·4，验证一致）。顺时针输入 Σc = −40，A = **−20**（负）；按规则整体拒绝，报「非逆时针」错误，**不得取绝对值**。
- (乙) 重心 Cx = 264/(6·20) = 264/120 = **11/5**，Cy = 264/120 = **11/5**。若分母误写成 2A=40：Cx = 264/40 = **33/5**，即 (33/5, 33/5)，恰为正确值的 3 倍。
- (丙) 顶点均值 = ((0+6+6+2+2+0)/6, …) = (16/6, 16/6) = **(8/3, 8/3)**。与正确重心差 8/3 − 11/5 = **7/15**。顶点均值把顶点等权，完全忽略边长与面积分布；重心是面积加权的一阶矩/面积，顶点密集处 ≠ 质量集中处，故二者一般不等。

## 第二节四条不变量：保证位置 + 钉住它的测试

1. 与三角剖分一致：meas 的一阶矩即「过原点三角形有向面积加权」的代数恒等变形（meas/meas.go 矩累加循环）；测试 `TestCentroidMatchesTriangulation`（api_test.go 内独立扇形三角剖分加权平均对照）。
2. 平移不变：重心公式只含边向量与顶点对的齐次二次组合，平移 (tx,ty) 后矩同步平移；测试 `TestCentroidTranslationInvariant`。
3. 面积非负且为真：api.New 拒绝非逆时针（poly.CCW），meas.Area 返回 |Σc|/2 的既约正有理数；测试 `TestAreaPositiveAndRejectsCW`。
4. 失败不留痕：api.New 先全部校验通过才构造对象，任何校验失败直接返回 nil+哨兵错误（api/api.go New）；测试 `TestRejectedInputLeavesNoPartialResult`。
