# Raft 任期与投票簿记 — 推导笔记

## 第三节：六步推导（n=3，多数派=2；初始 term=[0,0,0]，votedFor=[-1,-1,-1]）

| 步 | term | votedFor | Winner() |
|---|---|---|---|
| S1 StartElection(0) | [1,0,0] | [0,-1,-1] | 无 |
| S2 RequestVote(0,1)→节点1 | [1,1,0] | [0,0,-1] | 0 |
| S3 RequestVote(0,1)→节点2 | [1,1,1] | [0,0,0] | 0 |
| S4 RequestVote(1,1)→节点2 | [1,1,1] | [0,0,0] | 0 |
| S5 RequestVote(2,0)→节点0 | [1,1,1] | [0,0,0] | 0 |
| S6 StartElection(1) | [1,2,1] | [0,1,0] | 0 |

S4：节点 2 在任期 1 已投给 0，同任期拒绝改投。S5：t=0 < term[0]=1，过期任期拒绝。S6 后候选 1 只有自投 1 票，候选 0 仍握节点 0、2 两票。

- **(甲)** S5 正确实现：**拒绝**，状态不变（votedFor[0] 保持 0）。若见票就同意、不比较任期，votedFor[0] 会错成 **2**（正确应为 **0**）。
- **(乙)** S6 正确实现：term[1] 变为 **2**、votedFor[1] 变为 **1**。若忘了自增任期，term[1] 停在 1，而 votedFor[1] 在任期 1 已是 **0**（S2 投给了节点 0），同任期不可改票 → 节点 1 无法投给自己，也拉不到任何票（别人都在任期 1 投过 0），得票恒为 0，永远无法获胜。
- **(丙)** S4 正确实现：**拒绝**，votedFor[2] 保持 **0**。若允许同任期重复投票直接覆盖，votedFor[2] 会错成 **1**（正确应为 **0**），违反第二节**不变量 2**（每节点每任期至多一票，同任期不可更改）。

## 第二节：四条不变量的代码位置与钉住它们的测试

1. **与朴素重算一致**：`elect.Cluster.Winner` 用增量维护的 `votes` 计数判定多数派；测试 `TestWinnerMatchesNaive`（随机操作序列后逐个对比朴素重算）。
2. **每节点每任期至多一票**：`term.Book.RequestVote` 的 `t == Term` 分支只允许 `-1` 或同候选；测试 `TestOneVotePerTerm`。
3. **任期单调**：`term` 仅在 `StartElection`（+1）或更大 `t` 到达时提升，`votedFor` 只在此时被改写；测试 `TestTermMonotone`。
4. **失败不留痕**：`elect` 先校验（越界 / t<0 / 自投票三个互不相同的哨兵错误）再动状态，被拒操作零写入；测试 `TestRejectedOpsLeaveState`。
