# 单调队列滑动窗口最值：推导与不变量落实

## 推导：新元素等于队尾时，弹还是不弹（序列 [3,3,2]，w=2）

记候选为 (下标,值)。每步顺序：淘汰滑出窗口者 → 处理队尾 → 入队 → 取队首输出。

做法 A（等于也弹：队尾值 <= 新值即弹）：

  i=0: 入队          队列 [(0,3)]            窗口 [0,0] 输出 3
  i=1: 弹 (0,3)      队列 [(1,3)]            窗口 [0,1] 输出 3
  i=2: 2<3 不弹      队列 [(1,3),(2,2)]      窗口 [1,2] 输出 3

做法 B（等于不弹：仅队尾值 < 新值才弹）：

  i=0: 入队          队列 [(0,3)]            窗口 [0,0] 输出 3
  i=1: 3==3 不弹     队列 [(0,3),(1,3)]      窗口 [0,1] 输出 3
  i=2: (0,3) 滑出    队列 [(1,3),(2,2)]      窗口 [1,2] 输出 3

两者 Max() 输出相同。但 A 在 i=1 把 (0,3) 弹出时，下标 0 仍在即将输出的
窗口 [0,1] 内，该候选本次输出还要用到：Max 值仅靠 (1,3) 同值顶替才没错，
队首下标却不再是窗口内最左最大值位置，不变量 3“候选不早退”被破坏。B 只
弹严格更小者（被新元素支配、永不可能是未来最大值），过期一律按下标淘汰，
窗口内候选一个不少。结论：选 B——仅当队尾值严格小于新值才弹，相等保留。

## 四条不变量落实

1. 结果正确：slide.Maxes 逐元素 Feed 并取队首；TestMaxesMatchesNaive 对照朴素双循环。
2. 队列单调：mono.Push 弹掉队尾严格更小者，mono.Verify 校验单调性与队首下标；TestSelfCheck 每步钉住。
3. 候选不早退：相等不弹（见上），过期仅按左边界下标淘汰；TestThreeThreeTwo 钉住。
4. 失败不留痕：mono.Push 先验下标、slide.New/Maxes 先验 w，验过才改状态；TestPushRejectsNonIncreasing 与 TestErrorsDistinct 钉住。
