# 并行对齐快照点 — 推导与不变量

## 八步推导（P=3，maxPending=2；C=min(各分区事件数)，pending[p]=count[p]−C）

| 步 | Feed | 事件数(0,1,2) | C | pending(0,1,2) | 被拒 |
|---|---|---|---|---|---|
| 1 | 0 | (1,0,0) | 0 | (1,0,0) | 否 |
| 2 | 1 | (1,1,0) | 0 | (1,1,0) | 否 |
| 3 | 2 | (1,1,1) | 1 | (0,0,0) | 否 |
| 4 | 0 | (2,1,1) | 1 | (1,0,0) | 否 |
| 5 | 0 | (3,1,1) | 1 | (2,0,0) | 否 |
| 6 | 0 | (3,1,1) | 1 | (2,0,0) | **是** |
| 7 | 1 | (3,2,1) | 1 | (2,1,0) | 否 |
| 8 | 2 | (3,2,2) | 2 | (1,0,0) | 否 |

- (甲) 第 6 步被拒：若接受，事件数变 (4,1,1)，C 仍为 1，pending[0]=4−1=**3** > maxPending=**2**，故拒且状态不变。不做背压的实现第 6 步后 pending[0]=3，违反不变量 3（背压有界）。
- (乙) 错取 max(3,1,1) 得 C=**3**；分区 1、2 各自只有 1 条事件，读快照时会尝试读各自的第 2、3 条（下标 1、2），越界读不到。正确 C=min=**1**。
- (丙) 对齐判定式：cut[0]=cut[1]=…=cut[P−1]，且该共同值 = min(各分区事件数)。cut=(3,2,1) 各分量互不相等（3≠2≠1），且 min(3,2,1)=1≠3，校验器据此判不对齐。正确 cut 向量 = **(1,1,1)**。

## 四条不变量：代码位置 → 钉住它的测试

1. 对齐性：`cut.Tracker` 用 freq 频次表增量维护 min（cut/cut.go `Feed`）；`snap.Store.Snapshot` 每分区恰取前 `Cut` 条（snap/snap.go）。测试 `TestAlignmentInvariant`。
2. 与朴素参照一致：`Snapshot` 按分区序拼接各分区前 `Cut` 条（snap/snap.go）。测试 `TestNaiveEquivalence`。
3. 背压有界：`cut.Tracker.Feed` 先算投喂后新 pending，超限即拒、未触碰任何状态（cut/cut.go）。测试 `TestBackpressureBound`。
4. 失败不留痕：`snap.Store.Feed` 先校验分区号与空 Key，有错即返回、状态不变（snap/snap.go）。测试 `TestFailureNoTrace`。
