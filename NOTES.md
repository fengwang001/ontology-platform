# NOTES

初始视图 {k1:1,k2:2}，∅=nil 期望（要求键不存在）；累积状态=真实视图叠加本批前序事件。

| # | 事件 | 判定时累积状态(相关键) | 期望 | 是否继续 |
|---|---|---|---|---|
| 1 | Put(k3,30,∅) | k3 不存在 | ∅ 满足 | 继续，累积 k3=30 |
| 2 | Put(k1,10,&"1") | k1=1 | ="1" 满足 | 继续，累积 k1=10 |
| 3 | Del(k2,&"2") | k2=2 | ="2" 满足 | 继续，累积 k2 已删 |
| 4 | Put(k2,20,∅) | k2 不存在(事件3删) | ∅ 满足 | 继续，累积 k2=20 |
| 5 | Put(k4,40,∅) | k4 不存在 | ∅ 满足 | 继续，累积 k4=40 |
| 6 | Del(k3,∅) | k3=30(事件1建，未删) | ∅ 不满足 | 停止，整批拒绝 |
| 7 | Del(k4,∅) | — | 未检查 | — |
| 8 | Put(k5,50,∅) | — | 未检查 | — |

(甲) 第4步应**通过**：批内视角 k2 已被事件3删除，∅ 满足。只对照真实视图的实现读到真实 k2=2 仍存在，会在事件4报"nil 期望但键已存在"，把失败点从事件6错提前到事件4；若它还落地上一个前缀，视图错成 {k1:10,k2消失,k3:30}。
(乙) 事件1–5在各自累积状态下全部满足；事件6 要求 k3 不存在，而事件1已建 k3=30、其后无删除，故事件6是第一条失败，7/8 不再检查。"应用成功前缀"的错误实现会把视图改成 {k1:10,k2:20,k3:30,k4:40}；正确视图保持 {k1:1,k2:2}。
(丙) Put(k1,x,&"1") 对照真实 k1=1 满足，累积变 k1=x；Put(k1,y,&"x") 对照累积 k1=x 满足，整批通过，终态 k1=y。只对照真实视图的实现第二条读到 k1=1≠x 而错拒。印证**不变量2（批内顺序语义）**。

不变量（保证位置 / 钉住的测试函数）：
- I1 全或无：api/api.go `ApplyBatch`——err 立即返回、视图一字段不动；全过则在同一把写锁内依次应用 change set。`TestApplyBatchAllOrNothing`。
- I2 批内顺序：plan/plan.go `Rehearse` 的 delta 叠加层（命中叠加层即不再查可见视图）。`TestRehearsalOrdering`。
- I3 与朴素参照一致：api/api.go `SelfCheck` 内置确定性批+1000 条随机批逐批比对朴素逐事件应用器；api 测试 `TestNaiveReference`、`TestSelfCheck`。
- I4 失败不留痕且报第一条：plan/plan.go `Rehearse` 遇首条空键/期望失败即返回带 Index 的 BatchError，api 不改图。`TestSentinelErrors`。
- 复杂度：plan.Planner.lookups 为非导出字段，公开接口无任何读取途径；plan/plan_test.go `TestLookupCount`（m=100/1000/10000、批内1事件，断言 ≤事件数+1 且不随 m 增长）。
