# 跨分区会话键归并 — 推导与不变量

## 第三节：八步分步表（A=Append，C=Close）

| # | 操作 | s1 已见 Seq | s1 结果 | s2 已见 Seq | s2 结果 |
|---|------|------------|---------|------------|---------|
| 1 | A(s1,2,1) | {2} | 未冻结 | {} | 未冻结 |
| 2 | A(s2,1,3) | {2} | 未冻结 | {1} | 未冻结 |
| 3 | A(s1,1,5) | {1,2} | 未冻结 | {1} | 未冻结 |
| 4 | A(s1,4,8) | {1,2,4} | 未冻结 | {1} | 未冻结 |
| 5 | A(s1,3,2) | {1,2,3,4} | 未冻结 | {1} | 未冻结 |
| 6 | C(s1,4) | {1,2,3,4} | 5128 | {1} | 未冻结 |
| 7 | A(s1,5,9) | {1,2,3,4} | 5128（拒绝，不留痕） | {1} | 未冻结 |
| 8 | C(s2,1) | {1,2,3,4} | 5128 | {1} | 3 |

- **(甲)** `C(s1,4)` 后 s1 = **5128**（Seq 升序取值 5,1,2,8）。若按到达顺序拼接（1,5,8,2）会错成 **1582**。
- **(乙)** 归并键误用 `Seq`：第 3 步与第 2 步撞键（Seq=1 已存值 3），s1 的 5 被误判**冲突**而拒绝——s1 永久缺 Seq=1，第 6 步 `C(s1,4)` 也随之失败；若实现遇撞键改为覆盖，s2 的 3 被 5 顶掉，第 8 步 `C(s2,1)` 错得 **5**（正确为 3）。
- **(丙)** 第 7 步：s1 已关闭，应拒绝并报「已关闭」类哨兵错误，**不留痕**，结果保持 5128、已见集合不变。若不检查关闭照常拼接，会错成 **51289**。

## 第二节：四条不变量 → 保证位置 → 钉住它的测试

1. **批量一致**：`seg.Close` 按 Seq 升序折叠（`seg/seg.go`）；`api.SelfCheck` 不变量① + `TestBatchConsistency`。
2. **结果不变**：`closed` 后 result 只读，`Append`/`Close` 先判 closed（`seg/seg.go`）；`TestResultImmutability`。
3. **唯一/幂等**：`seg.Append` 同值幂等、异值 `ErrConflict`（`seg/seg.go`）；`TestUniqueIdempotent`。
4. **失败不留痕**：`mrg.Append`/`Close` 先校验后落状态，所有拒绝路径在写 map 前返回（`mrg/mrg.go`）；`TestFailureNoTrace`。
