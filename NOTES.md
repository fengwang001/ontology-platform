# MinHash 推导与不变量说明

## 八行分步表（p=11；A={1,4,7}，B={1,4,8,9}）

| 行 | 哈希函数 | 在集合元素上的取值 | sig[i]（最小值） |
|---|---|---|---|
| A,h1 | (2x+3)%11 | 1→5, 4→0, 7→6 | **0** |
| A,h2 | (3x+1)%11 | 1→4, 4→2, 7→0 | **0** |
| A,h3 | (5x+4)%11 | 1→9, 4→2, 7→6 | **2** |
| A,h4 | (7x+2)%11 | 1→9, 4→8, 7→7 | **7** |
| B,h1 | (2x+3)%11 | 1→5, 4→0, 8→8, 9→10 | **0** |
| B,h2 | (3x+1)%11 | 1→4, 4→2, 8→3, 9→6 | **2** |
| B,h3 | (5x+4)%11 | 1→9, 4→2, 8→0, 9→5 | **0** |
| B,h4 | (7x+2)%11 | 1→9, 4→8, 8→3, 9→10 | **3** |

- (甲) 相等位置只有 i=1（0==0），`Estimate(A,B)=1/4=0.25`；若错写成「不等位置数/k」则得 3/4=0.75。
- (乙) `sig_B[2]=min{4,2,3,6}=2`；误取最大值得 6，误取首个插入元素（1→4）得 4。
- (丙) 空集合签名应为 `[MaxUint64]*4`（+Inf）。若初值错设为 0，空签名变 `[0,0,0,0]`，会与任何 `sig[i]==0` 的非空集合（如 A 的 h1/h2 位）误判相等，空集 vs A 得 2/4=0.5 的假相似。

## 四条不变量 → 代码位置 → 钉住它的测试

1. 签名正确（含空集 MaxUint64）：`mh/mh.go` 的 `New`（初值 MaxUint64）与 `Add`（逐位取 min）；测试 `TestSignatureCorrect`。
2. 与朴素参照一致：`api/api.go` 的 `Estimate` 只数相等位；测试 `TestMatchesNaive`（逐元素重算对比）。
3. 确定性/对称：哈希无随机（`hash/hash.go` 纯函数），`Estimate` 只读；测试 `TestDeterministicAndSymmetric`。
4. 失败不留痕：所有校验先于任何写操作（`api.NewSketch`/`Estimate` 先验后动）；测试 `TestFailureLeavesNoTrace`。
