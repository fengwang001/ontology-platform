# NOTES

## 为什么安全性检查可以贪心

定义：need_i = max_i - alloc_i。安全 = 存在一个完成排列，使每个作业在完成前
其 need ≤ 当前可用量；作业完成后归还 alloc，可用量只增不减。

设 S 是当前「可完成」（need ≤ avail）的作业集合。
- 若存在安全序列 p1..pn，则 p1 ∈ S，且 p1 完成后状态仍安全（p2..pn 是其安全序列）。
- 任取 q ∈ S 且 q ≠ p1。q 完成使 avail 变成 avail+alloc_q ≥ avail（分量）。
  于是 p2..pn 中每一步当时满足的 need≤avail 关系仍然成立，只需把 q 从序列
  中移到最前：q, p1, ..., (跳过 q) ..., pn 仍是合法完成序列。
故「现在能完成哪个就完成哪个」不改变最终能否全部完成，挑的顺序不影响结论，
无需回溯：每轮扫描，找到一个可完成作业就完成（可用量单调增），
一轮至少完成一个，否则判不安全。

三作业两资源反例（total=(9,9)，avail=(4,4)，状态安全，可经
P2 申请(2,2)、P1 申请(3,3) 合法到达）：
P0 max=(6,6) alloc=(0,0) need=(6,6)；
P1 max=(7,7) alloc=(3,3) need=(4,4)；
P2 max=(8,8) alloc=(2,2) need=(6,6)。
P1 先完成：avail→(7,7)，P0、P2 随后完成，故安全。
P0 申请(1,1)：(1,1)≤avail=(4,4)，「只查可用量」会批准；
批准后 avail=(3,3)，三个 need 分别为 (5,5)/(4,4)/(6,6)，
无一 ≤(3,3)，全部卡死 → 银行家必须拒绝。

（待补：第二节语义索引 + 第四节实测值）
