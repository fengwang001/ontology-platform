# Indexed Binary Heap Notes

## decrease-key 调整方向

- 设元素旧键为 `k`，降到 `v < k`。降低前最小堆保证其两个子节点键都 `>= k`，故降低后仍有 `v < k <= 子节点键`，向子树的约束不会失效。
- 唯一可能失效的是与父节点之间的约束：父节点键可能满足 `v < parent <= k`。因此只需与父节点比较并反复 `sift-up`，直到父节点不大于该元素或到达根。
- `sift-down` 只检查子节点；降低后的键仍小于所有子节点键，所以它不会修复已经失效的祖先约束。若从被降低节点开始下沉，结果通常是原地不动，留下“父节点大于子节点”的破坏。
- 规定反例：堆数组为 `[3,5,7,9,11,13]`，把键 `9` 降到 `1`。正确 `sift-up` 得到 `[1,3,7,5,11,13]`，Pop 序列为 `[1,3,5,7,11,13]`。
- 错误地对下标 3 做 `sift-down` 时该节点是叶子，数组保持 `[3,5,7,1,11,13]`，其中父节点 `5` 大于子节点 `1`；Pop 序列变成 `[3,5,1,7,11,13]`。
