# VLQ 推导笔记

## 八行分步表（无符号 VLQ：7 位一组、最低有效组在前、bit7 为续位）
| 十进制 | 7 位组（低→高） | 各字节续位 | 十六进制字节 | 字节数 |
|---|---|---|---|---|
| 0 | [0] | 0 | 00 | 1 |
| 1 | [1] | 0 | 01 | 1 |
| 127 | [127] | 0 | 7F | 1 |
| 128 | [0, 1] | 1, 0 | 80 01 | 2 |
| 300 | [44, 2] | 1, 0 | AC 02 | 2 |
| 16383 | [127, 127] | 1, 0 | FF 7F | 2 |
| 16384 | [0, 0, 1] | 1, 1, 0 | 80 80 01 | 3 |
| 2097151 | [127, 127, 127] | 1, 1, 0 | FF FF 7F | 3 |

## (甲) 错误实现（每轮 emit (v&0x7f)|0x80、v>>=7，v==0 后再补 00）
128 → `80 81 00`（正确 `80 01`），300 → `AC 82 00`（正确 `AC 02`）：都多出末尾冗余 `00`，且最后一组数据字节被误置续位（`81`/`82` 应为 `01`/`02`）。错在循环以「右移之前 v>0」为继续条件，最后一组也置续位，循环外又无条件补 `00`。

## (乙) `80 00` 不做规范性检查
首字节低 7 位为 0、续位 1，次字节 `00`：解出值 0、消耗 2 字节。按规范 0 的最小编码是 `00`，`80 00` 属「长度>1 且末字节 00」的末尾冗余全 0 组，必须拒绝，返回哨兵错误 `ErrNonCanonical`。

## (丙) `40` 不做符号扩展
按无符号读得 64；规范上 bit6=1 为负，符号扩展后 = 64 − 128 = **−64**。`C0 00`：首字节低 7 位 0x40 且续位，次字节 `00` 余数为 0、bit6=0，按停止条件为正数 64；末字节 bit6=0 无需符号扩展，故该错误对正数 64 无影响。

## 四条不变量落点
1. 往返一致：`enc.EncodeUint/EncodeInt` 与 `enc.DecodeUint/DecodeInt` 互为逆；测试 `TestRoundTrip`。
2. 规范性固定点：`enc.DecodeUint/DecodeInt` 拒绝末尾冗余 `00`/`7F`；测试 `TestNonCanonicalRejected`。
3. 朴素参照一致：`TestEncodeUintVectors` 内 `naiveEncodeUint` 与 `EncodeUint` 全向量字节级比对。
4. 失败不留痕：`stream.Reader.ReadUint/ReadInt` 出错不推进 `pos`；测试 `TestFailureNoAdvance`。
