# NOTES — 带依赖排序的事务日志回放

## 一、八步推导（集合均按 id 升序列出）

| 步 | 操作 | 已登记 | 暂存 | 已回放 | Replay 返回 |
|---|---|---|---|---|---|
| 1 | Commit(3,5) | {3} | {3} | {} | — |
| 2 | Commit(5) | {3,5} | {} | {} | — |
| 3 | Commit(1,5) | {1,3,5} | {} | {} | — |
| 4 | Replay() | {1,3,5} | {} | {1,3,5} | [5 1 3] |
| 5 | Commit(2,1) | {1,2,3,5} | {} | {1,3,5} | — |
| 6 | Replay() | {1,2,3,5} | {} | {1,2,3,5} | [2] |
| 7 | Commit(7,2) | {1,2,3,5,7} | {} | {1,2,3,5} | — |
| 8 | Replay() | {1,2,3,5,7} | {} | {1,2,3,5,7} | [7] |

要点：步 1 依赖 5 未登记 → 3 暂存；步 2 登记 5 后 3 自动激活；步 4 可回放者反复取最小 id：5(无依赖)→1(5 已回放)→3(5 已回放)。

- (甲) 忽略依赖按 id 升序：第 4 步错返回 `[1 3 5]`；正确为 `[5 1 3]`。1、3 排在其依赖 5 之前，违反不变量 1（依赖先行/拓扑序）。
- (乙) 若「依赖未登记」=拒绝：第 1 步 `Commit(3,5)` 报错被拒、3 不登记；后续回放永久少掉事务 3（第 4 步只返回 `[1 5]`）。
- (丙) `Commit(2,1)` 使 2 暂存；再 `Commit(1,2)`：若只在 Commit 新事务时查环、激活暂存事务时不查，则登记 1 时 2 不在活动图、查不到环，1 登记成功并触发 2 激活，1↔2 互依赖进入回放图。之后 `Replay()` 中 1 等 2、2 等 1，入度永远 ≥1，两者永远不可回放：Replay 反复返回空，形成死锁（既不回放也不报错）。

## 二、四条不变量的保证位置与钉住测试

1. 依赖先行：`rpl.Replay` 只从就绪最小堆取入度 0 者，`gph.MarkReplayed` 递减入度 —— `TestEightSteps`/`TestTopoOrder`。
2. 可复现：`rpl` 用最小堆做 id 升序 tie-break，选择唯一 —— `TestReproducible`。
3. 暂存正确：`gph.Commit` 依赖未登记则暂存、登记齐后级联激活，不进就绪集 —— `TestStaging`。
4. 失败不留痕：`gph.Commit` 全部校验（非法 id/自依赖/重复/超限/成环）先于任何变更 —— `TestRejectedKeepsState`。
