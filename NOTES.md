# ontology-321 菱形依赖无毛刺传播

## 第三节推导

层级：A=0, E=0, B=1, C=1, D=2, F=3, G=3（G 的输入 A 层 0、D 层 2）。初始值：A=1, E=100, B=2, C=11, D=13, F=113, G=14。

朴素逐边推送（DFS，消费者按定义序：A→B,C,G；B→D；C→D；D→F,G）处理 `Apply{A:5}`，九行重算：

| # | 重算 | 读到的输入 | 值变化 | 继续推 |
|---|------|-----------|--------|--------|
| 1 | B | A=5 | 2→10 | 推 D |
| 2 | D | B=10, C=11(旧) | 13→21 | 推 F,G |
| 3 | F | D=21, E=100 | 113→121 | 推（无消费者） |
| 4 | G | A=5, D=21 | 14→26 | 推（无消费者） |
| 5 | C | A=5 | 11→15 | 推 D |
| 6 | D | B=10, C=15 | 21→25 | 推 F,G |
| 7 | F | D=25, E=100 | 121→125 | 推 |
| 8 | G | A=5, D=25 | 26→30 | 推 |
| 9 | G | A=5, D=25 | 30→30 | 不推 |

(甲) 分层分轮日志：R0 (A,1,5)；R1 (B,2,10),(C,11,15)；R2 (D,13,25)；R3 (F,113,125),(G,14,30)；派生节点共重算 5 次。朴素实现的中间值：D=21、F=121、G=26（正确结果里不存在）；派生节点变更共 8 条，D 出现 2 次。
(乙) BFS：队列初始 [B,C,G]；B=10、C=15 之后出队 G，读到 A=5(新)、D=13(旧)，得 G=18——这是第一个「部分新部分旧」的重算；之后 D 出队两次（25、25不变）、F=125、G=30，共重算 7 次。
(丙) 合并 `Apply{A:0,E:10}` 日志 7 条：(A,5,0),(E,100,10),(B,10,0),(C,15,10),(D,25,10),(F,125,20),(G,30,10)。拆成两次：先 `Apply{A:0}` 时 F 用旧 E=100 算出中间值 F=110，日志 6 条；再 `Apply{E:10}` 得 (E,100,10),(F,110,20) 2 条；合计 8 条，比合并多 1 条。

## 四条不变量落点

1. 全量重算一致：`prop.Engine.Apply` 按层级升序分轮、同轮按名序重算，等价于拓扑序全量重算 → `TestFullRecomputeConsistent`、`api.SelfCheck`。
2. 无毛刺：`prop` 中节点层级严格大于其所有输入，处理到该层时输入值已全部定稿；每节点每次 Apply 至多重算一次、仅值变才入日志 → `TestLogWellFormed`。
3. 值没变不传播：`prop.Apply` 源同值跳过、派生重算值不变不标记 changed，下游不会被调度 → `TestNoopApplyEmpty`。
4. 失败不留痕：`dag.New` 三阶段校验（定义非法→节点不存在→成环）先于任何状态构造；`prop.Apply` 先验完全部键再动值 → `TestRejectedKeepsState`、`TestFaultInjection`。
