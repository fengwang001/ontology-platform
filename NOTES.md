# Fisher-Yates 均匀性推导

- 正确算法从后向前处理：在第 `i` 步，只从 `[0,i]` 中均匀选择下标 `j`，交换 `arr[i]` 与 `arr[j]`。
- 第 `i` 步有 `i+1` 个选择；完整执行共有 `n*(n-1)*...*1=n!` 条等概率选择路径。
- 每条路径概率为 `1/n!`，且每条路径唯一确定一个排列；归纳可证每个最终排列恰有一条路径，所以概率恰为 `1/n!`。
- 错误算法每步都从完整范围 `[0,n)` 选择，`n` 步共有 `n^n` 条等概率路径。
- 若要仍得到均匀分布，每个排列必须对应同样多条路径，即 `n!` 必须整除 `n^n`。
- 对 `n>=3`，`n!` 含因子 `n-1`；当 `n=4` 时 `n-1=3` 不整除 `4^4=256`，所以 24 个排列不可能等概率。
- 因而错误算法会让部分排列对应更多路径、概率翻倍或更高，另一些排列概率为零或偏低。
- 正确范围随未处理前缀收缩；`n` 个元素只需最后 `n-1` 次交换，因此随机数调用次数精确为 `n-1`。

## 语义与测试索引

（实现测试后补充）
