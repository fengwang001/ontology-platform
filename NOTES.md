# 迟到丢弃与更新 NOTES（size=10, delay=3, lateness=5，wm=maxTS−3）

## 八行分步表（同一 Key；「留存」为该步后仍占内存的窗口:计数）

| # | TS | wm | 窗口 | 判定 | 本步输出 | 留存 |
|---|---|----|------|------|----------|------|
| 1 | 2  | -1 | [0,10)   | 正常计入 | 无 | [0,10):1 |
| 2 | 9  | 6  | [0,10)   | 正常计入 | 无 | [0,10):2 |
| 3 | 15 | 12 | [10,20)  | 迟到接受(12<15) | +[10,20):1；触发+[0,10):2 | [0,10):2, [10,20):1 |
| 4 | 4  | 12 | [0,10)   | 迟到接受(12<15) | -[0,10):2, +[0,10):3 | [0,10):3, [10,20):1 |
| 5 | 18 | 15 | [10,20)  | 正常计入 | 无（[0,10) 被清除：15>=10+5） | [10,20):2 |
| 6 | 5  | 15 | [0,10)   | 丢弃(15>=15) | 无 | [10,20):2 |
| 7 | 22 | 19 | [20,30)  | 正常计入 | 无 | [10,20):2, [20,30):1 |
| 8 | 7  | 19 | [0,10)   | 丢弃(19>=15) | 无 | 同上；Dropped=2 |

Flush 后补触发 +[10,20):2、+[20,30):1。最终视图：[0,10):3, [10,20):2, [20,30):1。

(甲) 第 6 步 wm=15 恰等于 end+lateness=15，按规则归属**丢弃**（`wm>=end+lat` 丢弃）。若误写成 `wm>end+lat` 才丢弃：TS=5 被接受、计数变 4 并打补丁，TS=7 仍丢弃 → [0,10) 视图错成 **4**（正确 3），丢弃数错成 **1**（正确 2）。
(乙) 迟到接受只改计数不发补丁：第 4 步后下游仍持旧值 2，[0,10) 最终视图错成 **2**（正确 3），违反不变量 1（与批量重算一致）。
(丙) 换序 2,4,9,15,18,22,5,7：第 4 步 [0,10) 已计到 3，触发时直接输出 +3，不再出现 -2/+3 更新补丁——中间日志不同、最终视图相同；差异来自**准点触发规则**（触发输出的是触发时刻的计数）。不变量 1 必须只取**被接受**的事件：TS=5、TS=7 被丢弃，若按全部事件重算 [0,10) 会错成 5。

## 四条不变量在代码中的落实

1. 批量一致：wagg 的触发/补丁/清除规则（sweep 与 apply）；测试 `TestFlushEqualsBatch`。
2. 日志自洽：wagg 迟到补丁先 `-emitted` 再 `+count`、触发至多一次（fired 标记）；测试 `TestLogPrefixes`。
3. 水位线单调：`win.Watermark.Advance` 只取 max，丢弃事件同样先推进；测试 `TestWatermarkMonotonic`。
4. 失败不留痕：`wagg.Feed` 先校验空 Key、maxOpen 受限时在克隆上试跑失败即弃；测试 `TestRejectNoTrace`。
