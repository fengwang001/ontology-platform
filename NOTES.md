# ontology-352：左外连接物化视图增量维护

## 八步推导（宽表按 K 升序，RV 缺席标 nil）

| 步 | 操作 | 本步输出日志 | 操作后宽表 |
|---|---|---|---|
| 1 | PutL(k1,10) | +(k1,10,nil) | k1:(10,nil) |
| 2 | PutL(k2,20) | +(k2,20,nil) | k1:(10,nil) k2:(20,nil) |
| 3 | PutR(k2,200)（补发） | -(k2,20,nil) +(k2,20,200) | k1:(10,nil) k2:(20,200) |
| 4 | PutR(k1,100)（补发） | -(k1,10,nil) +(k1,10,100) | k1:(10,100) k2:(20,200) |
| 5 | PutR(k3,300) | 无（左流无 k3，仅存 R 待用） | k1:(10,100) k2:(20,200) |
| 6 | PutL(k3,30) | +(k3,30,300) | k1:(10,100) k2:(20,200) k3:(30,300) |
| 7 | PutR(k2,250)（更新） | -(k2,20,200) +(k2,20,250) | k1:(10,100) k2:(20,250) k3:(30,300) |
| 8 | DelL(k1) | -(k1,10,100) | k2:(20,250) k3:(30,300)（R 中 k1=100 保留） |

(甲) 第 3 步若不先撤回，下游将同时存在 (k2,20,nil) 与 (k2,20,200) 两行同 K，宽表错成 k2 双行（旧行 (k2,20,nil) 残留）。违反不变量 2：每个 K 至多一行，改行必须先撤回旧行再插入新行。
(乙) 错实现会输出 +(k3,?,300)（或类似），宽表多出一条无左记录的 k3 行。正确输出为「无」，宽表不变；k3 到第 6 步 PutL 才物化，且直接带上已保存的 RV=300。本质差别：左连接由左流驱动，右流独有的记录永不物化；内连接则要求两侧同时存在才出行。
(丙) 再 PutL(k1,15)：正确输出 +(k1,15,100)，宽表 k1:(15,100) k2:(20,250) k3:(30,300)。若 DelL 把右记录一起删掉，此次会错输出 +(k1,15,nil)（RV 丢成 nil）。不变量 3 要求右记录留存，因为右流状态独立于左流成员关系，删除左行不得销毁右流已到达的数据，否则同 K 左记录再次插入时无法复用正确 RV。

## 不变量 → 代码位置 → 测试

1. 批量重算一致：`join.View` 按有序 keys 逐行拼 RV；`api.SelfCheck` 内 checkBatch 用 `join.Batch` 比对；测试 `TestBatchConsistency`。
2. 日志自洽：`join.PutL/PutR/DelR` 一律先 `-` 后 `+`；`api.SelfCheck` 内 checkLog 模拟下游逐条应用；测试 `TestLogSelfConsistency`、`TestEightStepTrace`。
3. 右记录留存：`join.DelL` 只删左键不碰 rstore；测试 `TestRightRetainedAfterDelL`。
4. 失败不留痕：各 op 先校验后改动，哨兵错误 `ErrEmptyKey/ErrNoLeft/ErrNoRight`；测试 `TestFailuresAtomic`。
