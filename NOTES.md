# NOTES

## 层级提升的可复现推导（第三节）

跳表每插入一个元素都要决定它的塔高（层数）。经典实现用
`math/rand` 抛硬币得到几何分布的层数。问题在于：若使用全局随机源
或不固定种子，层数取决于进程运行时的随机状态——同一批 key 两次
构建会得到不同的塔高分布，各层节点集合不同，结构序列化结果随之
抖动；这正是线上事故的根因（`Range` 结果虽仍有序，但底层结构
不可复现，依赖结构一致性的校验都会失败）。

要满足「同 seed 两次构建结构逐字节相同」，层数必须是确定性函数。
本实现采用 `Level(seed, key)`：将 key 与 seed 混合（乘黄金比例
常数异或后再做 xorshift 混合），取混合结果末尾零比特个数 +1
作为层数。这在数学上仍是 p=1/2 的几何分布（期望层数 2，查找
期望 O(log n)），但输出只取决于 (seed, key)，与插入顺序、进程
状态、时钟全部无关。因此同 seed 插入同一批 key 必然得到逐字节
相同的结构；不同 seed 改变哈希输入，塔高分布随之改变，结构必然
出现差异。该性质由测试 `TestSkipList/Reproducible` 钉住。
