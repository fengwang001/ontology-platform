# 基于计数的翻滚窗口（size=5, lateness=2 推导 + 不变量落点）

## 第三节：八步分步表（元素写作 (Pos,Val)，窗口 k=[k*5,(k+1)*5)）

| 步 | 元素 | 之后 wm | 窗口 | 判定 | 触发 | 之后开放的窗口(cnt) |
|---|---|---|---|---|---|---|
| 1 | (0,10) | 0 | 0 | 正常 | 无 | w0(1) |
| 2 | (1,20) | 1 | 0 | 正常 | 无 | w0(2) |
| 3 | (2,30) | 2 | 0 | 正常 | 无 | w0(3) |
| 4 | (3,40) | 3 | 0 | 正常 | 无 | w0(4) |
| 5 | (4,50) | 4 | 0 | 正常 | w0 触发，sum=150 | 无 |
| 6 | (5,60) | 5 | 1 | 正常 | 无 | w1(1) |
| 7 | (9,90) | 9 | 1 | 正常 | 无 | w1(2) |
| 8 | (7,70) | 9 | 1 | 迟到被接受（7>=9-2） | 无 | w1(3) |

- (甲) 窗口 [0,5) 在第 5 步 (4,50) 到达时触发，聚合 150。若错写成 cnt==size-1 即触发：第 4 步就以 (0,10)(1,20)(2,30)(3,40) 触发、聚合错成 100；第 5 步 (4,50) 因窗口已关闭被丢弃，该窗口最终聚合错成 100（少 50）。
- (乙) (5,60) 应归窗口 1（floor(5/5)=1）。若错用 floor((Pos-1)/size) 则归窗口 0；窗口 0 已关闭 → 元素被丢弃、丢弃数 +1，窗口 1 的 cnt 少 1（0 而非 1），窗口 1 可能永远凑不齐 size 而不触发。
- (丙) 接受阈值 Pos >= wm-lateness = 9-2 = 7，7>=7 故被接受。若错写成严格大于 Pos > 7，则 (7,70) 被丢弃、丢弃数 +1，窗口 1 的 cnt 少 1（2 而非 3）。

## 四条不变量：保证位置 + 钉住测试

1. 批量重算一致：`cwagg.(*Agg).Add` 仅在接受时 `cnt++`、`sum+=Val`，触发时输出当前 sum → 测试 `TestBatchRecompute`、`TestBehavior/section3`。
2. 输出自洽：触发即置 `closed`，之后该窗口一律拒绝（`Add` 中 closed 分支），每窗口至多输出一次 → 测试 `TestBehavior`（multi-window、dup-idempotent）、`TestBatchRecompute` 的重复触发检查。
3. 水位单调+计数守恒：`Add` 末尾 `wm=max(wm,Pos)`（丢弃也推进、重复投递不推进）；`cnt` 只在接受时 ++ 且 closed 后冻结 → 测试 `TestBehavior/late-boundary-inclusive`、`TestBehavior/dup-idempotent`。
4. 失败不留痕：`api.New` 先校验 size/lateness；`api.Feed` 先整批校验 Pos/Key 再逐条应用 → 测试 `TestRejectNoTrace`。
