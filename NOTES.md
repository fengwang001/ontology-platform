# 定名规则推导（第三节）

## 优先次序
- `filename*`（带扩展标记）优先于 `filename`；两者都没有时给兜底名。
- 理由：扩展形式能携带字符集与百分号编码，保真度更高；RFC 6266 也建议接收方优先采用它。
- 确定性：paramjoin 输出的是**有序切片**（按名字首次出现排序），`Decide` 顺序扫描、
  同类取首个出现者，完全不依赖 map 遍历顺序。

## 安全化次序（Sanitize，按序执行）
1. 防御性百分号解码：合法的 `%XX` 先还原成字节，非法序列原样保留。
   理由：下游若再做一次 URL 解码，`%2e%2e` 会变回 `..`，必须在定名前识别。
2. 剔除首尾空白（空格、制表、回车、换行）。
3. 整体为 ``、`.`、`..` ⇒ 兜底名 `unnamed`。
4. 逐字节编码：`/`、`\`、控制字符（<0x20、0x7F，含 NUL）编为大写 `%XX`；
   相邻点串（长度 ≥2 的 `...`）中每个 `.` 编为 `%2E`，保证结果不含 `..` 子串。
5. 结果为空 ⇒ 兜底名 `unnamed`。

## 第 3 条（不丢信息）如何满足
- 第 4 步的 `%XX` 编码是可逆变换，本身不产生任何碰撞。
- 可能撞名的只剩三类，且都属于第 2 条要求剔除/归一的部分：
  a. 解码后字节序列相同的输入（如 `a%2Fb` 与 `a/b`）——它们本就指同一串字节；
  b. 仅首尾空白不同的输入——首尾空白正是第 2 条要剔除的部分；
  c. 被整体剔除到兜底名的输入（如 `..` 与 `%2e%2e`）——剔除部分即两者的全部差异。
- 因此两个原本不同的输入若定出同名，其差异必落在上述被剔除部分之内。
