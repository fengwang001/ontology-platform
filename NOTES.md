# NOTES: 用两个栈实现队列

## 转移规则的推导

结构：`in` 栈只接收入队，`out` 栈只服务出队；`out` 的栈顶即队首。

- 为何不能每次 Dequeue 都倒：若 `out` 非空时仍把 `in` 倒入 `out`，
  新元素会压在 `out` 中更老的元素之上，栈顶变成较新的元素，
  先入队的元素反而后出，FIFO 顺序被破坏（退化成部分 LIFO）。
- 为何不能每次 Enqueue 都倒回：把 `out` 倒回 `in` 再压入，单次
  Enqueue 要移动全部 n 个元素，单次操作 O(n)，批量吞吐骤降，
  这正是线上事故的退化模式。
- 正确规则：Enqueue 只压 `in`；Dequeue/Peek 仅当 `out` 为空时才把
  `in` 整体倒入 `out`。`out` 非空时绝不动 `in`，老元素始终居栈顶。
- 摊还 O(1)：每个元素至多入 in 一次、随整体倒出一次、出 out 一次，
  合计常数次移动；n 次操作总移动 O(n)，摊还每次 O(1)。工程上按
  入 in 计 1、倒出计 1 统计移动数，摊还上界为 2。

## 语义与测试对照（代码位置 / 测试函数）

（待测试写完后补充）
