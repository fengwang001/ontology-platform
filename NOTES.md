# NOTES

## 第三节推导（size=10，左开右闭 (wm-10, wm]，过期条件 TS <= wm-10）

| 步 | 事件 TS:Val | wm | 入/出 | 本步过期 | sum | 窗口内保留 |
|---|---|---|---|---|---|---|
| 1 | 5:10 | 5 | 入 | 无 | 10 | {5:10} |
| 2 | 15:20 | 15 | 入 | 5:10 | 20 | {15:20} |
| 3 | 12:30 | 15 | 入 | 无 | 50 | {15:20, 12:30} |
| 4 | 12:40 | 15 | 入 | 无 | 90 | {15:20, 12:30, 12:40} |
| 5 | 25:50 | 25 | 入 | 15:20, 12:30, 12:40 | 50 | {25:50} |
| 6 | 15:60 | 25 | 出(丢弃) | 无 | 50 | {25:50} |
| 7 | 35:70 | 35 | 入 | 25:50 | 70 | {35:70} |

- (甲) TS=15 == wm-size=15，左开区间不含左边界，应**丢弃**；误用左闭 `TS >= wm-size` 会把它计入，第 6 步后 sum 错成 **110**（正确 50）。
- (乙) TS=5 == wm-size=5，过期条件是 `TS <= wm-size`，应**被过期**；误用严格小于 `TS < wm-size` 会留下它，第 2 步后 sum 错成 **30**（正确 20）。
- (丙) 同 TS=12 的两条是不同事件，都计入；误按 TS 去重只留后写（Val=40）时，第 4 步后 sum 错成 **60**（正确 90）。

## 四条不变量：保证位置与钉住它的测试

1. 与批量重算一致：`wagg.ingest` 用 `win.InWindow` 判定成员、`win.Expired` 过期；测试 `TestBatchEquivalence`（api_test，随机序列对拍批量重算）。
2. 窗口成员守恒：`wagg.ingest` 过期一个减一个 Val、入窗一个加一个 Val；`wagg.SelfCheck` 重加成员核对；测试 `TestMemberConservation`（wagg_test）。
3. 水位线单调：`wagg.ingest` 仅 `if e.TS > k.wm` 时推进（丢弃事件同规则）；测试 `TestWatermarkMonotonic`（wagg_test）。
4. 失败不留痕：`wagg.Apply` 先在克隆状态上试跑、全部成功才提交；测试 `TestRejectedLeavesNoTrace`（api_test）。
