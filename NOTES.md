# 布谷鸟哈希：推导与不变量

## 一、n=4 分步表（h1=x%4，h2=(3x+1)%4，`-` 为空槽）

| 键 | h1 | h2 | 被挤出 | 落点 | T1 | T2 |
|---|---|---|---|---|---|---|
| 0 | 0 | 1 | - | T1[0] | [0,-,-,-] | [-,-,-,-] |
| 1 | 1 | 0 | - | T1[1] | [0,1,-,-] | [-,-,-,-] |
| 2 | 2 | 3 | - | T1[2] | [0,1,2,-] | [-,-,-,-] |
| 3 | 3 | 2 | - | T1[3] | [0,1,2,3] | [-,-,-,-] |
| 4 | 0 | 1 | 0 | T1[0]；0→T2[1] | [4,1,2,3] | [-,0,-,-] |
| 5 | 1 | 0 | 1 | T1[1]；1→T2[0] | [4,5,2,3] | [1,0,-,-] |
| 6 | 2 | 3 | 2 | T1[2]；2→T2[3] | [4,5,6,3] | [1,0,-,2] |
| 7 | 3 | 2 | 3 | T1[3]；3→T2[2] | [4,5,6,7] | [1,0,3,2] |

**(甲)** Insert 4 挤出键 0，0 落到它的另一候选槽 `T2[h2(0)]=T2[1]`。若实现把被挤出的键直接丢弃，`Lookup(0)` 查 `T1[0]=4`、`T2[1]=空`，会误判 0「不存在」；0 的正确位置是 T2[1]。

**(乙)** 键 0、4、8：`h1` 全为 0、`h2` 全为 1（8%4=0，25%4=1）。三键只共享 T1[0] 与 T2[1] 两个槽，驱逐在两者之间无限往返成环。正确实现在驱逐次数超过 maxKicks 时返回 `ErrTableFull`，且两表保持插入前原样：T1=[4,5,6,7]、T2=[1,0,3,2]。

**(丙)** 若 `Lookup` 只查 T1，会被误判「不存在」的键：0（在 T2[1]）、1（在 T2[0]）、2（在 T2[3]）、3（在 T2[2]）。

## 二、四条不变量的保证位置与钉住它们的测试

1. **查找正确**：`cuckoo.Insert` 驱逐链终点必把键写入其 h1 或 h2 槽，`Delete` 命中即清槽 → `TestLookupAfterInsertDelete`。
2. **与朴素参照一致**：随机 Insert/Delete 序列后逐键比对 map[int]bool → `TestMatchesNaiveMap`。
3. **驱逐有界 + 失败不留痕**：`cuckoo.Insert` 在 T1/T2 副本上模拟驱逐，超限弃副本返回 `ErrTableFull`，原表未动 → `TestTableFullAtomic`。
4. **失败不留痕**：重复 Insert / 不存在键 / n<1 全部在写表之前返回哨兵错误 → `TestRejectedOpsNoSideEffect`。
