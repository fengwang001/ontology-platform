# 保序并行映射器：失败选择规则推导与语义索引

规则推导：Run 要与顺序执行语义一致且结果与失败时序无关。
顺序执行时下标 0..m-1 成功、m 失败，永远返回 m（首个失败下标）。
因此并行下必须返回"下标最小的失败"，而非"时间上最早到达的错误"。
下标 5 先失败时不能立刻返回：下标 3 仍在跑，可能也失败，那时正确结果是 3。
做法：只在收到下标 k 的结果且 [0,k] 全部到达时放行；k 失败则 emit 0..k-1 后返回 k 的错误。
此时所有 <k 的结果必已处理（否则无法判定 k 是最小失败），与到达顺序无关。
窗口：Put(i) 要求 i-next<=W；结果按序经 Next 放行，背压来自 Next。

## 语义 -> 代码位置 / 测试函数名

<!-- 测试完成后回填 -->

## 第四节实测

<!-- 窗口历史最大暂存数：实测回填 -->
