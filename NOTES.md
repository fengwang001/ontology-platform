# 最小包围圆 推导与不变量

## 一、五个点集逐步推导

| 点集 | 圆心 | r (r²) | 边界点 |
|---|---|---|---|
| S1={(0,0)} | (0,0) | 0 (0) | 1 个：(0,0) |
| S2={(0,0),(6,0)} | (3,0) | 3 (9) | 2 个：(0,0),(6,0) |
| S3={(0,0),(6,0),(0,8)} | (3,4) | 5 (25) | 2 个：(6,0),(0,8)（直角三角形，斜边为直径；(0,0) 恰在圆上） |
| S4={(0,0),(6,0),(1,1)} | (3,0) | 3 (9) | 2 个：(0,0),(6,0)（钝角在 (1,1)，最长边为直径） |
| S5={(0,0),(3,0),(6,0)} | (3,0) | 3 (9) | 2 个：(0,0),(6,0)（共线，两端点直径） |

- (甲) S2：圆心 (3,0)，r=3。误实现（圆心=第一个点、半径=完整距离）：圆心错成 (0,0)，r 错成 6（r²=36），是正确值的 2 倍。
- (乙) S4：r=3（r²=9），边界 (0,0),(6,0)。总取外接圆则错：外接圆圆心 (3,-2)，r²=13，r=√13≈3.606，比 3 大且圆心跑到点集下方。
- (丙) S5：圆心 (3,0)，r=3（r²=9）。直接求外接圆：三点共线使行列式为 0，除以零，圆心/半径无定义（程序崩溃或得到无穷大半径），结果完全错误。

## 二、四条不变量的保证位置与钉住测试

1. 与暴力枚举一致：`mec.Insert` 用 Welzl 重建，`mec.BruteForce` 枚举点对/三元组取最小覆盖圆；测试 `TestBruteForceConsistency`。
2. 最小性：Welzl 产出的圆与暴力枚举的最小者逐字段相等（`api.SelfCheck` 与上同一断言路径）；测试 `TestBruteForceConsistency`。
3. 覆盖正确：`circ.Circle.Contains` 判定 dist²≤r²，边界点 dist²==r²；测试 `TestCoverageBoundary`。
4. 失败不留痕：`api.Insert` 先校验（越界/重复）再改状态，`api.MinCircle` 空集直接返回 `ErrEmpty`；测试 `TestFailureNoTrace`。

计数器恒 1 由 `TestInsertChecksConstant` 钉住；并发只读一致由 `TestConcurrentReads` 钉住。
