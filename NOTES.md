# 增量物化视图聚合维护 — 推导与不变量

## 第三节推导：六步变更逐步对照表（分组 g 初始为空）

| 步 | 变更      | COUNT | SUM | MIN | MAX |
|----|-----------|-------|-----|-----|-----|
| 1  | Insert 5  | 1     | 5   | 5   | 5   |
| 2  | Insert 2  | 2     | 7   | 2   | 5   |
| 3  | Insert 9  | 3     | 16  | 2   | 9   |
| 4  | Retract 9 | 2     | 7   | 2   | 5   |
| 5  | Insert 2  | 3     | 9   | 2   | 5   |
| 6  | Retract 2 | 2     | 7   | 2   | 5   |

**(甲)** COUNT、SUM 只需「当前标量 + 本条增量」：新值 = 旧值 ± 1 / ± v，与集合其余内容无关。
MIN、MAX 做不到：撤回当前极值后，新极值取决于「还活着的整个多重集」，单个标量里没有这份信息。
判据：聚合函数须对每个元素**可减**（存在逆运算 ⊖，使 f(S) 与 x 可唯一推出 f(S\{x})）才能纯标量增量维护；COUNT/SUM 可减，MIN/MAX 不可减。

**(乙)** 朴素 MAX 只存标量 9：第 4 步 `Retract 9` 后它无从得知次大值——若报「无」是错的，若保留 9 也是错的；正确值是 **5**。
补救「撤回值等于当前最大值就清空」会被重复值击穿：第 5 步 `Insert 2` 后它仍报「无」（错，正确 MAX=5）；若改成「清空后以新插入值为准」，第 6 步 `Retract 2` 又会把还剩一份的 2 整份抹掉（错在第 6 步，2 应仍在集合中）。
**最小附加状态**：有序多重集（值 → 出现次数，本题用 treap）。撤回只把该值计数减一、减到零才删节点；极值从树的两端 O(log m) 取得。它恰好保存了「每个值还剩几份、彼此谁大谁小」这一撤回所需的全部信息，故充分且无冗余。

## 四条不变量：落点与钉住它们的测试

1. **逐步一致**：`agg.Group.Apply` 让 count/sum 与多重集同增同减（`agg/agg.go`）；测试 `TestStepwiseConsistency`。
2. **撤回是插入的逆**：多重集计数 +1/−1 对称、sum/count 加减对称（`agg/agg.go`）；测试 `TestRetractIsInverse`。
3. **空分组语义**：`Group.Value` 在 count==0 时返回 `OK=false`，MIN/MAX 报不存在而非 0（`agg/agg.go`）；测试 `TestEmptyGroupSemantics`。
4. **失败不留痕**：`Apply` 先做溢出/存在性检查、通过后才变更，任一检查失败直接返回（`agg/agg.go`、`api/api.go`）；测试 `TestFailureAtomicity`。
