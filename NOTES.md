# NOTES

## 一、推导（gap=10，到达序 TS=100,105,130,135,118,100）

| 步 | 事件 | 到达后的完整会话序列 |
|---|---|---|
| 1 | 100 | `[100,100]×1` |
| 2 | 105 | `[100,105]×2` |
| 3 | 130 | `[100,105]×2; [130,130]×1` |
| 4 | 135 | `[100,105]×2; [130,135]×2` |
| 5 | 118 | `[100,105]×2; [118,118]×1; [130,135]×2` |
| 6 | 100 | `[100,105]×3; [118,118]×1; [130,135]×2` |

（甲）118 距前会话 end=105 为 13，距后会话 start=130 为 12，**两侧都严格大于 gap=10**，故它够不着任何一边：必须在二分定位处**新建独立会话 [118,118] 且不归并**。朴素做法「并进最近会话再扩边」（12<13 选右侧）会得到 `[100,105]×2; [118,135]×3`：其中相邻事件 118→130 相差 12>gap 却被塞进同一会话，违反不变量 3「任意两个相邻会话的间隔严格大于 gap（否则它们本应是同一个会话）」对规范形的界定——该句的逆否即「凡相邻事件差 >gap 必须分属两个会话」，[118,135] 跨过了断点，不是合法会话；同时直接违反不变量 1（重算应为 3 个会话）。

（乙）第 6 步重复的 100 落在 [100,105] 区间内部：start、end 都不动，计数 2→3。重复时间戳只增计数，与会话边界是两件独立的事。不能依赖「事件按时间到达」：反例顺序 **100,130,105** 喂给「只与上一个到达事件比较」的实现——100 建会话，130 差 30>10 开新会话，105 只与「上一个」130 比（差 25>10）再开一个，得 `[100];[105];[130]` 三个会话，前两个间隔仅 5≤10；排序重算只有 `[100,105]×2;[130]×1`，划分不同。

## 二、不变量在代码中的保证位置与钉住的测试

1. 与全量重算一致：`sess.insert` 按 start 二分定位，仅向紧邻两侧按 `evt.ShouldMerge` 吸收（单点最多吸收左右各一个，既有不变式保证不会连锁）。测试：`TestSixStepSequence`、`TestOrderIndependence`。
2. 顺序无关：结果只取决于事件多重集；`sess.Set.Feed` 在每 Key 的切片副本上计算，全部成功后才提交。测试：`TestOrderIndependence`（循环生成多档规模随机序列与多种打乱顺序）。
3. 规范形：吸收的唯一判据是 `evt.ShouldMerge`（间隔 ≤gap 必合并），插入后相邻间隔恒严格 >gap，start 严格升序。测试：`TestCanonicalForm`、`TestSixStepSequence`。
4. 失败不留痕：三类哨兵错误 `evt.ErrInvalidEvent`、`sess.ErrBadGap`、`sess.ErrTooMany`；`sess.Add` 先经纯函数 `insert` 求结果、超限则不写回，`sess.Feed`/`api.Feed` 先全量校验再在副本上逐事件计算、任一失败整体不提交。测试：`TestRejectionsNoTrace`、`TestFeedAtomic`。

复杂度：非导出字段 `sess.Set.cmp` 记录最近一次 Add 实际比较过边界的已有会话数（≤2，加被吸收数），定位用 `sort.Search` 不计入；同包白盒测试 `TestComparisonBudget` 直接读字段，m=100/1000/10000 断言其为与 m 无关的常数。并发：`api.mu`（RWMutex），`TestConcurrentSnapshot` 钉住；自检：`api.SelfCheck` 对内置序列核验四条，`TestSelfCheck` 钉住。
