# NOTES

## 三、n=4 逐步推演（_ 表示空槽；h1=x%4，h2=(3x+1)%4）

| x | h1 | h2 | 被挤出的键及去向 | x 的落点 | 插入后 T1 | 插入后 T2 |
|---|---|---|---|---|---|---|
| 0 | 0 | 1 | 无 | T1[0] | [0,_,_,_] | [_,_,_,_] |
| 1 | 1 | 0 | 无 | T1[1] | [0,1,_,_] | [_,_,_,_] |
| 2 | 2 | 3 | 无 | T1[2] | [0,1,2,_] | [_,_,_,_] |
| 3 | 3 | 2 | 无 | T1[3] | [0,1,2,3] | [_,_,_,_] |
| 4 | 0 | 1 | 0 → T2[1]（0 的 h2 槽，空） | T1[0] | [4,1,2,3] | [_,0,_,_] |
| 5 | 1 | 0 | 1 → T2[0]（1 的 h2 槽，空） | T1[1] | [4,5,2,3] | [1,0,_,_] |
| 6 | 2 | 3 | 2 → T2[3]（2 的 h2 槽，空） | T1[2] | [4,5,6,3] | [1,0,_,2] |
| 7 | 3 | 2 | 3 → T2[2]（3 的 h2 槽，空） | T1[3] | [4,5,6,7] | [1,0,3,2] |

**甲**：Insert 4 挤出 0，0 搬到它的另一个候选槽 T2[h2(0)=1]。若直接丢弃，T1[0]=4、全表无 0，Lookup(0) 错误地得 false；正确位置是 T2[1]，Lookup 查 T1[0] 与 T2[1] 得 true。
**乙**：h1/h2：0→(0,1)，4→(0,1)，8→(0,1)（(8·3+1)%4=1）。三个键只有 T1[0]、T2[1] 两个候选槽，插入 8 的驱逐链 8→4→0→8 成环、永无空槽。kicks 超过 maxKicks 时立即返回 ErrTableFull，且回滚到插入前：T1=[4,5,6,7]，T2=[1,0,3,2]。
**丙**：只查 T1[h1] 会漏掉 0、1、2、3；正确位置分别为 0→T2[1]、1→T2[0]、2→T2[3]、3→T2[2]（4,5,6,7 在 T1 不受影响）。

## 二、四条不变量的保证位置与钉住测试

1. **查找正确**：`cuckoo/cuckoo.go` Insert 成功落位（含驱逐链每环搬到旧键另一候选槽）、Lookup 同时检查两候选槽 —— `TestInsertLookup` 钉住。
2. **与朴素参照一致**：`cuckoo/cuckoo.go` Insert/Delete 仅在成功时改 size 与槽位、Lookup 双槽判定 —— `TestNaiveReference`（确定性随机序列，逐键比对 map[int]bool）钉住。
3. **驱逐有界**：`cuckoo/cuckoo.go` Insert 中 kicks 计数、`kicks>maxKicks` 即失败、rollback 反向重放变更日志 —— `TestTableFull`（多档 maxKicks，失败后逐槽快照比对）钉住。
4. **失败不留痕**：`cuckoo/cuckoo.go` New 校验 n、Insert 前置重复检查、Lookup/Delete 哨兵错误，拒绝路径均先于任何改动或经回滚 —— `TestFailureNoTrace` 钉住。

另：非导出 `lastProbes`（atomic）恒为 2 由 `TestProbeCountO1`（m=100/1000/10000 多档）钉住；并发只读由 `TestConcurrentLookup`（`go test -race`）钉住。
