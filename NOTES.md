# LAG 增量维护 · 推导与不变量

## 八步推导（行序按 (Sort 升序, ID 升序)；NULL = 分区内无前驱，与 0 严格区分）

| 步 | 操作 | p 行序 | q 行序 | 本步输出的变更日志（按序） |
|---|---|---|---|---|
| 1 | I(1,p,10,5) | 1 | — | +(1,NULL) |
| 2 | I(2,p,30,8) | 1,2 | — | +(2,5) |
| 3 | I(3,p,20,7) | 1,3,2 | — | +(3,5) -(2,5) +(2,7) |
| 4 | I(4,q,15,9) | 1,3,2 | 4 | +(4,NULL) |
| 5 | I(5,p,20,6) | 1,3,5,2 | 4 | +(5,7) -(2,7) +(2,6) |
| 6 | D(3) | 1,5,2 | 4 | -(3,5) -(5,7) +(5,5) |
| 7 | I(6,p,5,5) | 6,1,5,2 | 4 | +(6,NULL) -(1,NULL) +(1,5) |
| 8 | D(1) | 6,5,2 | 4 | -(1,5) |

(甲) 第 3 步输出 `+(3,5) -(2,5) +(2,7)`；第 4 步输出 `+(4,NULL)`。若实现「只为新行输出 +、从不更新其他行」，第 3 步后 ID=2 的 LAG 错为 5（应为 7，即新前驱行 3 的 Val）。若实现忽略分区、全行同序列，第 4 步会把 4(Sort15) 插到 1(Sort10) 之后、3(Sort20) 之前，输出 `+(4,5) -(3,5) +(3,9)` 三条（正确只有 `+(4,NULL)` 一条）。

(乙) 第 6 步输出 `-(3,5) -(5,7) +(5,5)`。若实现「插入时正确更新后继、删除时不更新后继」，第 6 步后 ID=5 的 LAG 仍是 7——那是已被删除的行 ID=3 的 Val。第 8 步 D(1) 按正确规则只输出 1 条 `-(1,5)`：行 5 的新前驱变成行 6，其 Val=5 与行 5 的旧 LAG(=5) 相等，「值没变就不输出」。该错误实现在第 8 步也只输出 `-(1,5)`（它不更新后继），其最终视图 ID=5 为 7，与正确结果 5 不相同；但注意第 8 步的输出条目与正确实现一模一样——错误是第 6 步引入的。更危险的情形：若某实现第 6 步漏更新、第 8 步却又补发 `-(5,7) +(5,5)`，最终视图会恰好回到正确结果，只在最后比对就彻底漏掉第 6、7 步之后的错误视图。故不变量 1 必须写成「每一步之后」而不是「最后」。

(丙) 第 5 步 ID=5 与 ID=3 的 Sort 并列(20)，按 ID 升序打破：行序 1,3,5,2，输出 `+(5,7) -(2,7) +(2,6)`。若改按 ID 降序打破：行序变成 1,5,3,2，输出为 `+(5,5) -(3,5) +(3,6)`（行 2 的前驱仍是行 3，LAG 不变不输出）。

## 四条不变量：保证位置与钉住测试

1. 与批量重算一致：`api.Apply` 把每条 changelog 同步应用到 `a.view`，`lagview.View.Batch` 做全量重算，`SelfCheck` 逐步比对两者。测试：`TestViewMatchesBatchEveryStep`（多种子随机序列逐步比对 `Batch` 全量重算）、`TestEightStepChangelog`（八步精确变更日志）。
2. 变更日志自洽：`lagview.Insert/Delete` 只在 LAG 实际变化时对后继发 `-(旧)`、`+(新)`，且顺序固定。测试：`TestChangelogPrefixConsistency`（下游模型逐条核验每个前缀）。
3. 与插入顺序无关：定位只依赖 `(Sort,ID)` 二分（`lagord.Seq.Locate`），与到达顺序无关。测试：`TestInsertOrderIndependence`（多种子乱序重放）。
4. 失败不留痕：`api.check` 先对整批做纯校验（不触碰任何状态），全部通过后才落变更；四类拒绝各对应一个哨兵错误。测试：`TestRejections`（四类哨兵错误互不相同、被拒批次不产出任何日志条目且视图不变、之后可继续用）。
