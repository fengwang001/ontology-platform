# Ring Buffer 推导笔记

## 满/空判定的方案选择（第三节）

读写指针重合时，单凭指针无法区分「空」与「满」。

- 方案①牺牲一槽位：以 `(w+1)%cap == r` 判满，`w == r` 判空。
  空槽只用于区分两种重合状态，最多存放 cap-1 个元素。
  故 `New(4)` 实际容量只有 3：连续 Enqueue 第 4 次即满足
  `(w+1)%4 == r`（w=3 绕回为 0，r=0），错误地返回 ErrFull。
- 方案②额外维护 size 计数器：写前看 size==cap 判满，读写后更新 size。
  指针重合时用 size 消歧：size==0 为空，size==cap 为满。
  全部 cap 个槽位都可使用。

第二节第 1 条要求 `New(4)` 能连续成功 4 次、第 5 次才 ErrFull，
方案①在第 4 次就报满，直接违反该条，故必须采用方案②（size 计数器）。

## 语义条目与代码位置（完成后回填）

<!-- 待回填：第二节各条 -> 代码行与测试函数名 -->
