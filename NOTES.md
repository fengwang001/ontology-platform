# NOTES

## 第三节：五步裁剪推导（包围正方形 [-10^6,10^6]^2，顶点逆时针）

| 步 | 加入 | 裁剪后区域顶点（逆时针） |
|---|---|---|
| 1 | H1: -x ≤ 0 | (0,-1e6), (1e6,-1e6), (1e6,1e6), (0,1e6) |
| 2 | H2: -y ≤ 0 | (0,0), (1e6,0), (1e6,1e6), (0,1e6) |
| 3 | H3: x+y ≤ 6 | (0,0), (6,0), (0,6) |
| 4 | H4: x ≤ 4 | (0,0), (4,0), (4,2), (0,6) |
| 5 | H5: y ≤ 4 | (0,0), (4,0), (4,2), (2,4), (0,4) |

- (甲) 最终五顶点：(0,0),(4,0),(4,2),(2,4),(0,4)。若 H3 方向搞反成 x+y ≥ 6，最终错成三角形 (2,4),(4,2),(4,4)。
- (乙) 再加 H6: -x ≤ -5（x ≥ 5）：当前区域 x ≤ 4，与 x ≥ 5 平行相反，交集为空（无顶点）。不检测该情形的实现会把 H6 误判为"冗余不改变区域"，错误返回上一步的五边形 (0,0),(4,0),(4,2),(2,4),(0,4)。
- (丙) 若"在内"写成严格 x+y < 6：边界点 (6,0)、(0,6) 被判在外并丢弃，第 3 步结果错成退化单点 (0,0)（面积 0，后续步骤随之全错）。

## 第二节：四条不变量的保证位置与钉住测试

1. 与朴素裁剪一致：`hpi.Region.Add` 就是按到达顺序的 Sutherland–Hodgman 逐边裁剪，`api` 不做任何重排；测试 `TestNaiveConsistency`（api 包，随机序列对照朴素参考实现）。
2. 可行性自洽：裁剪只保留 Side ≤ 0 的点与边上交点（`hpi.clip`）；测试 `TestFeasibility`（hpi 包，顶点满足全部半平面 + 外部采样点至少违反一个）。
3. 凸性/逆时针/无重复：初始包围正方形逆时针，裁剪保序；`hpi.clip` 去重相邻重复点；测试 `TestConvexCCW`（hpi 包，叉积符号与面积符号断言）。
4. 失败不留痕：`api.Add` 先做全部校验（`ErrDegenerate`/`ErrOutOfRange`），通过后才触碰 `hpi` 状态；测试 `TestRejectedInputNoSideEffect`。

计数器：`hpi.Region.lastChecked`（非导出），冗余预判只查包围盒；测试 `hpi` 包内 `TestRedundantCostConstant`。
