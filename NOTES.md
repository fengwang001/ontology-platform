# Queue via Two Stacks

## 推导

设 `in` 负责入队，`out` 负责出队：

1. `Enqueue` 只压入 `in`，最新元素位于 `in` 栈顶。
2. `Dequeue` 优先弹出 `out`；只有 `out` 为空时，才把 `in` 中元素逐个弹出并压入 `out`。
3. 倒栈会反转顺序：若 `in` 自底向上是 `1,2,3`，倒空后 `out` 自顶向下就是 `1,2,3`，因此出队保持 FIFO。
4. 不能每次 `Dequeue` 都倒：`out` 非空时，其顶部仍是更早入队的元素；再把 `in` 倒到 `out` 会让新元素盖住旧元素，破坏 FIFO。
5. 不能在 `Enqueue` 时把 `out` 倒回 `in`：元素会随每次入队反复跨栈移动，单批操作可退化为 O(n)。
6. 只在 `out` 为空时倒栈时，每个元素至多经历入 `in`、移到 `out`、出 `out` 三次；总移动次数线性，摊还每次操作 O(1)，且转移次数摊还上界为 2。

## 代码与测试索引

待实现后补充。
