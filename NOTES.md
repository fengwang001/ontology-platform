# NOTES

## 第三节：低位在前打包推导（f0..f3，总宽 14 位）

| 字段 | 宽度 | 有符号 | 值 | 位范围 | 贡献（值<<偏移） |
|---|---|---|---|---|---|
| f0 | 3 | 否 | 5 (101) | [0,3) | 5<<0 = 5 |
| f1 | 5 | 否 | 18 (10010) | [3,8) | 18<<3 = 144 |
| f2 | 4 | 是 | -3 → 补码 1101 (=13) | [8,12) | 13<<8 = 3328 |
| f3 | 2 | 否 | 1 (01) | [12,14) | 1<<12 = 4096 |

打包结果：5+144+3328+4096 = **7573（0x1D95）**。

- (甲) 高位在前：在 14 位区域内逆序排布，偏移依次 11/6/2/0：5<<11 + 18<<6 + 13<<2 + 1<<0 = 10240+1152+52+1 = **11445（0x2CB5）**；正确的低位在前结果是 **7573（0x1D95）**。
- (乙) f0（3 位无符号）赋 9：超出 [0,7]，正确实现返回 **(0, ErrValueOverflow) 且不改任何状态**；naive 静默截断低 3 位，1001→001，存成 **1**。
- (丙) f2 解包：1101 符号位为 1，符号扩展得 **-3**；不做符号扩展（零扩展）得 **13**。

## 第二节四条不变量：保证位置 + 钉住测试

1. 往返一致：`bits/bits.go` 的 `PackFields`（补码位模式直接入字）与 `Extract`（bit w-1 为 1 则 `x |= ^mask` 符号扩展）；由 `TestRoundTrip` 钉住。
2. 无重叠无空洞：`bits/bits.go` 的 `PackFields` 中偏移 off 从 0 起逐字段 `off += w`、总宽 ≤64；`pack/pack.go` 的 `NewSchema` 记录同一组偏移；由 `TestSchemaLayout` 钉住。
3. 与朴素参照一致：`PackFields` 循环体就是教科书式 `word |= uint64(value) << off`；由 `TestNaiveReference` 逐向量与测试内独立手写参照比对钉住。
4. 失败不留痕：`PackFields` 先整表校验宽度/值域再置字；`SetBit`/`ClearBit` 先判越界再动字节；由 `TestRejectedPackNoTrace` 与 `TestBitmapErrorsNoTrace` 钉住（拒绝后继续正常使用）。

O(1) 计数器 `probe` 是 `pack` 包非导出字段，任何导出接口都读不到，仅包内测试 `TestBitLocationConstantTime` 直接读取。
