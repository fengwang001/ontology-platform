# 凸多边形直径（旋转卡壳）推导与不变量

## 第三节推导

五边形 v0(0,0) v1(5,1) v2(6,4) v3(3,6) v4(1,5)，逐边求对跖顶点（使 |Area2| 最大的顶点）：

| 边 (i→i+1) | 各候选面积（只列最大者） | 对跖顶点 | d²(v[i],对跖) | d²(v[i+1],对跖) |
|---|---|---|---|---|
| v0→v1 | v2:14, v3:27, v4:24 | v3 | 45 | 29 |
| v1→v2 | v3:11, v4:16, v0:14 | v4 | 32 | 26 |
| v2→v3 | v4:7, v0:24, v1:11 | v0 | 52 | 45 |
| v3→v4 | v0:9, v1:12, v2:7 | v1 | 29 | 32 |
| v4→v0 | v1:24, v2:26, v3:9 | v2 | 26 | 52 |

- (甲) 全部对跖点对中最大 d²=**52**，由 **(v0,v2)** 取得（边 v2→v3 与边 v4→v0 各枚举到一次，去重后同一对）。矩形 (0,0)(6,0)(6,4)(0,4)：直径 d²=6²+4²=**52**，并列 **两对 {0,2} 与 {1,3}**（两条对角线）；若实现用严格 `>` 只保留首个最大值，枚举顺序下先收 {0,2}，**漏报 {1,3}**。
- (乙) 误把直径当最长边：五条边 d² 为 26,10,13,5,26，会错成 **26**；正确应为 **52**。
- (丙) 推进条件写成 `<`：j 朝面积**减小**方向滑动，停在局部最小处，对跖枚举退化成「最近点」枚举，必漏 (v0,v2)；按本实现（j 初值 1）逐边模拟，各边得到的最大 d² 仅 26,10,13,5,26，最终错成 **26**。

## 四条不变量及其落点

1. 与暴力枚举一致：`rc.solve` 枚举全部对跖点对取 max（定理保证直径必在其中）；测试 `TestBruteForceConsistent`。
2. 直径在对跖对：返回值只来自 `rc.solve` 枚举出的对跖点对；测试 `TestPairsAreAntipodal`。
3. 并列完整：`rc.solve` 用 map 收集所有 d² 等于最大值的对（无序去重），不只留首对；测试 `TestTiesComplete`。
4. 失败不留痕：`api.New` 先完成全部校验，最后一步才整体赋值，出错时接收者原状态不变；测试 `TestRejectNoPartial`。
