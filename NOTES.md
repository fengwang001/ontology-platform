# NOTES

## s="ababa" (n=5) 五行 Z 表

| i | Z[i] | n-i | Z[i]==n-i（周期?） |
|---|---|---|---|
| 0 | 0 | 5 | 否（约定 Z[0]=0） |
| 1 | 0 | 4 | 否（a≠b） |
| 2 | 3 | 3 | 是 → p=2 是周期 |
| 3 | 0 | 2 | 否（a≠b） |
| 4 | 1 | 1 | 是 → p=4 是周期 |

- (甲) 判定误写 `Z[p] <= n-p`：恒真，p=1 被误判，最短周期错成 **1**；正确为 **2**。
- (乙) 额外要求 p 整除 n：2、4 均不整除 5，全被拒，错返回 **5**（或报"非周期"）；正确为 **2**。
- (丙) 从 n-1 递减取第一个满足者：先命中 p=4，错返回 **4**；正确为 **2**。

## 第二节四条不变量：保证位置 / 钉住的测试

1. 与朴素参照逐项一致：`zfn.go` 的 `New` 逐字符比较循环（Z-box 仅拷贝初值，匹配长度仍由真比较决定）；`zfn_test.go::TestZNaive`。
2. 结构自洽：`zfn.go` 只写 z[1..]（z[0] 留零值），循环条件 `i+z[i] < n` 保证不越界，失配即停保证最长；`zfn_test.go::TestZStructural`。
3. 最短周期正确：`period.go::Shortest` 按 p=1..n-1 升序、仅以 `==n-p` 判定，先中即最小，否则 n；`api_test.go::TestPeriodNaive`（对朴素枚举）。
4. 失败不留痕：`api.go::New` 先完成空串/非法 UTF-8 全部校验，再一次性赋值；查询方法先查 built；`api_test.go::TestErrorsAndNoTrace`。
