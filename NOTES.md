# NOTES — 平滑加权轮询推导与不变量

## 六行分步表（权重 [3,1,2]，W=6，初始 cw=(0,0,0)）

| 次 | 加权后 (cw0,cw1,cw2) | 选中 | 减 W 后 (cw0,cw1,cw2) |
|---|---|---|---|
| 1 | (3,1,2)  | 0 | (-3,1,2) |
| 2 | (0,2,4)  | 2 | (0,2,-2) |
| 3 | (3,3,0)  | 0 | (-3,3,0) |
| 4 | (0,4,2)  | 1 | (0,-2,2) |
| 5 | (3,-1,4) | 2 | (3,-1,-2) |
| 6 | (6,0,0)  | 0 | (0,0,0)  |

序列 0,2,0,1,2,0；计数 0→3、1→1、2→2，恰等于权重。

- (甲) 第 2 次平滑算法选 2；朴素展开 0,0,0,1,2,2 第 2 次选 0。
- (乙) 并列 (3,3,0) 取下标最小 → 选 0；若取下标最大 → 错选 1。
- (丙) 若减自身权重 w_i：落选者 cw 只增不减，服务器 1 永非最大 → 被饿死（6 次中 0 次）；错推 (3,1,2)→(0,1,2)→(3,2,2)→(3,3,4)，第 4 次加权 (6,4,6) 并列取小错选 0（应为 1）。

## 四条不变量

1. 公平性：每轮 cw+=w、选中者减 W —— `wrr/wrr.go` `Next`；测试 `TestFairnessAndSumCW`。
2. Σcw==0：每轮净增 Σw−W=0 —— `wrr/wrr.go` `Next` 的加/减；测试 `TestFairnessAndSumCW`（逐次断言 SumCW）。
3. 确定性：堆 Less 并列取下标小、无随机源 —— `wrr/wrr.go` `maxHeap.Less`；测试 `TestDeterminism`。
4. 失败不留痕：svc 先校验后改状态 —— `svc/svc.go` `New`/`SetWeight`；测试 `TestRejectedKeepsState`。
