# Elias gamma 流编码 — 推导与不变量

序列 [1,2,3,5,8] 五行分步表（偏移为 0 基比特位区间）：

| N | k | gamma 位串 | 位数 | 累积偏移 |
|---|---|---|---|---|
| 1 | 0 | `1` | 1 | 0–0 |
| 2 | 1 | `010` | 3 | 1–3 |
| 3 | 1 | `011` | 3 | 4–6 |
| 5 | 2 | `00101` | 5 | 7–11 |
| 8 | 3 | `0001000` | 7 | 12–18 |

拼接 = `1010011001010001000`（19 位），末字节低位补 5 个 0 → 字节 **A6 51 00**。解码依次得 1,2,3,5,8，剩余 5 个 0 为合法填充。

(甲) `0x80`(10000000) → **[1]**（首 1 解出 N=1，尾 7 个 0 全 0 到结尾，合法填充）；`0x01`(00000001) → **ErrTruncated**（数到 7 个前导 0 才遇 1，k=7，还需 7 位低位即耗尽）。若把「遇 1 后不足」误当填充，`0x01` 被错解成 **128**（2^7，低位补 0）；若把「全 0 到结尾」误当截断，`0x80` 被错拒成 **ErrTruncated**。

(乙) N=2^63：k=63，gamma 码 = 63 个前导 0 + 64 位二进制，共 **127 位**；正确实现遇 1 即判 **ErrOverflow**。无 k≤62 上限时 `1 << 63` 在 int64 回绕为 **-9223372036854775808**（math.MinInt64）。

(丙) 前导 0 错写成 k+1 个：N=1 → `01`，N=2 → `0010`，`Encode([1,2])` 错位串 = **`010010`**（补零后字节 0x48）；正确解码器按 0-1-0、0-1-0 切分，解出 **[2,2]** 而非 [1,2]。`Encode([])` 正确返回 **`[]byte{}`（长度 0，不是 [0x00]）**。

不变量（保证位置 / 钉住的测试，测试均在 gcode_test.go）：

1. 与朴素参照一致：`gcode.Encode`/writeCode 的逐位拼接与 `Decode`/`DecodeOne` 的逐位手解（bits 大端序）—— **TestReference**。
2. 切分点无关：`gcode.StreamDecoder` 的 Feed/End 跨块码字状态机（bits.StreamReader 游标不随分块重置）—— **TestStreamChunks**（遍历全部切点）。
3. 确定性/无前缀：`EncodeOne` 的「k 个 0 +(k+1) 位二进制」构造即无前缀码，Writer 纯追加无随机态 —— **TestDeterminism**（重复编码逐字节相同）与内部 **TestPrefixFree**（gamma 码两两非前缀，gcode 与朴素参照逐字节相同故等价传递）。
4. 失败不留痕：`gcode.Deccode` 先累积局部切片，出错 `return nil, err`，Reader 为每次调用新建 —— **TestAtomicFailure**（断言 nil 前缀且错误后可用）。

复杂度：`bits.Reader.lastChecks` 为非导出字段，`DecodeOne` 起始调 `ResetCheckCount`（只复位、不读出数值）；**TestComplexityCounter** 经 export_test 垫片断言解 N=2^40 恰检查 2*40+1=81 位，不随 m=100/1000/10000 增长。
