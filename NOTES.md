# Stoer-Wagner 全局最小割 — 推导与不变量

## 第三节推导：n=4，边 (0,1)=3、(1,2)=2、(0,2)=5、(2,3)=1

| 阶段 | MAS 加入顺序（从 0 开始，并列取小编号） | (s,t) | 本阶段割值 |
|---|---|---|---|
| 1 | 0 → 2(w5) → 1(w3+2=5) → 3(w1) | (1,3) | 1 |
| 2 | 0 → 2(w5) → {1,3}(w3+2+1=6) | (2,{1,3}) | 6 |
| 3 | 0 → {1,2,3}(w5+3=8) | (0,{1,2,3}) | 8 |

合并规则：阶段1 合并 1,3 → 超节点 {1,3}，对共同邻居 2 的边权相加 2+1=3；阶段2 合并 2 与 {1,3}。
全局最小割 = min(1, 6, 8) = **1**（割集为孤立点 3 的一侧）。

- **(甲)** 只取最后一个阶段 → 返回 **8**（正确应为 1）。
- **(乙)** MAS 改取「最小」：阶段1 顺序变为 0 → 3(w0) → 1(w3) → 2(w5+1+2=8)，割值错成 **8**；阶段2、3 各为 9、9 → 最终返回 **8**（正确应为 1）。
- **(丙)** 割值错算为 s-t 边权：阶段1 的 (1,3) 之间无边 → 错成 **0**；三阶段为 0、3、8 → 最终返回 **0**（正确应为 1）。

## 四条不变量（保证位置 / 钉住它的测试）

1. **下界正确**：`sw.MinCut` 对全部阶段割值取最小（sw/sw.go），由 `TestInvariantLowerBound`（api/api_test.go，枚举全部二划分逐一 ≥ 且最小值相等）钉住。
2. **与朴素参照一致**：同一组图上逐值等于「节点 0 到每个 t 的最小 s-t 割再取最小」，由 `TestInvariantSTReference`（api/api_test.go）钉住。
3. **合并正确性**：`sw.mergeInto` 中 `adj[keep][x] += wx`（sw/sw.go），共同邻居边权求和，由 `TestMergeSumsCommonNeighbor`（sw/sw_internal_test.go）钉住。
4. **失败不留痕**：`wg.AddEdge` 全部校验通过后才写入（wg/wg.go），`wg.New` 拒绝 n<2，由 `TestRejectedOpsLeaveStateUnchanged`（api/api_test.go）钉住。
