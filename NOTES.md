# MLFQ 防博弈推导

1. 若“每次被调度”都把本级已用配额重置为 0，作业可在 Q[0]-1 刻时 `Yield`。
2. 它回到最高级队尾后会再次得到 0..Q[0]-1 的完整预算；每轮都少跑 1 刻，因此 `used` 永远小于 Q[0]，永不降级。
3. 正确账是“作业在当前级别的累计已用刻数”：每运行 1 刻递增；`Yield` 只移动到队尾，不清账；用满配额才降级并清零；周期性提升回最高级时清零。
4. Q[0]=4 时，博弈作业每轮跑 3 刻：第 1 次被调度后 `used=3`，第 2 次再跑 1 刻即达到 4，在该次调度期间降到第 2 级；对照的“每次调度重置”实现则始终 `used<4`。
5. 任意已入队作业首次获得 CPU 前，每次有效 `Step` 都会让其最高级 FIFO 位置前进；在容量 `MaxJobs=10000` 下，从提交到首次运行的粗上界是 `MaxJobs-1=9999` 个有效刻，与作业总量有界、与是否跨越提升点无关。
