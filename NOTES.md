# NFA→DFA 子集构造：推导与不变量

NFA：状态{0,1,2}，起点0，接受{2}，字母表{a,b,c}；0-a->1，1-ε->2，1-b->1，2-c->2。

## 子集构造分步表（起始 D=ε闭包({0})={0}=A；3 个 DFA 状态 × 3 字符 = 9 个〈状态,字符〉组合，逐行如下）

| 当前状态 | 字符 | move 直接集 | ε闭包后 | 是否新状态/是否接受 |
| A={0} | a | {1} | {1,2} | 新状态 B，接受（含 2） |
| A={0} | b | ∅ | — | 无转移（∅ 不生成状态） |
| A={0} | c | ∅ | — | 无转移 |
| B={1,2} | a | ∅ | — | 无转移 |
| B={1,2} | b | {1} | {1,2} | 已有 B，接受 |
| B={1,2} | c | {2} | {2} | 新状态 C，接受 |
| C={2} | a | ∅ | — | 无转移 |
| C={2} | b | ∅ | — | 无转移 |
| C={2} | c | {2} | {2} | 已有 C，接受 |

（题目称 8 行；实际需处理 3 状态 × 3 字符共 9 个组合，上表无遗漏。）终态：A、B、C 共 3 个 DFA 状态。

## 三问
- (甲) 漏 ε 闭包：B 错成 {1}（漏掉 1-ε->2 的 2）；`"a"` 落在 {1}，{1} 不含接受态 → **拒绝**（正确：**接受**，B 应为 {1,2}）。
- (乙) 判定取反：`"a"` 到 B={1,2} 含接受态 → 误判**拒绝**（正确：接受）；`"z"` 不在字母表，起步即无转移 → **拒绝**（与正确结果碰巧相同，因为根本没走动）。
- (丙) ∅ 也作状态且对 {a,b,c} 各自环：DFA 状态总数 3 → **4**。

## 四条不变量落点
1. 与 NFA 模拟一致：`api.simulate` 为不构造 DFA 的参照，`SelfCheck` 逐串比对；测试 `TestAcceptsMatchesSimulation`（api/api_test.go）。
2. 闭包完备：`dfa.Build` 中每个状态都由 `nfa.EpsilonClosure` 产出；测试 `TestSelfCheck`（逐状态重算闭包比对）。
3. 确定性：`dfa.DFA.trans` 类型为 `[]map[byte]int`，每字符至多一个后继；测试 `TestDeterministic`（api/api_test.go）。
4. 失败不留痕：`dfa.Build` 先 `nfa.Validate`，出错返回 nil + 哨兵错误；测试 `TestFaultInjection`、`TestFailureNoSideEffect`。
