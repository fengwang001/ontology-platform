# NOTES
## 十三步推导（maxRows=3）

| # | 事件 | 结果 | 输出 | Buffered | 进行中缓冲 |
|---|---|---|---|---|---|
| 1 | BEGIN(1) | 接受 | 无 | 0 | 1:[] |
| 2 | BEGIN(2) | 接受 | 无 | 0 | 1:[],2:[] |
| 3 | R(2,a) | 接受 | 无 | 1 | 1:[],2:[a] |
| 4 | R(1,b) | 接受 | 无 | 2 | 1:[b],2:[a] |
| 5 | BEGIN(3) | 接受 | 无 | 2 | 1:[b],2:[a],3:[] |
| 6 | R(3,c) | 接受 | 无 | 3 | 1:[b],2:[a],3:[c] |
| 7 | R(2,d) | 拒绝·缓冲满 | 无 | 3 | 1:[b],2:[a],3:[c] |
| 8 | C(2) | 接受 | Txn{2,[a]} | 2 | 1:[b],3:[c] |
| 9 | R(1,e) | 接受 | 无 | 3 | 1:[b,e],3:[c] |
| 10 | X(3) | 接受 | 无 | 2 | 1:[b,e] |
| 11 | R(1,f) | 接受 | 无 | 3 | 1:[b,e,f] |
| 12 | C(1) | 接受 | Txn{1,[b,e,f]} | 0 | — |
| 13 | C(3) | 拒绝·未知事务 | 无 | 0 | — |

**(甲)** 按 BEGIN 顺序输出：第 8 步 C(2) 时 tx1 仍在进行中，tx2 滞留缓冲，**第 8 步无任何输出**；第 9 步 R(1,e) 时总账 3 已满（tx2 的 a 未释放），**被拒绝·缓冲满**；第 10 步 X(3) 释放 c 后总账降为 2，第 11 步 R(1,f) 被接受。最终输出为 `[Txn{1,[b,f]}, Txn{2,[a]}]`——顺序错（BEGIN 序而非 COMMIT 序，tx1 排到了 tx2 前面），且 tx1 丢失行 e。

**(乙)** 不缓冲直接透传：下游依次收到 a,b,c,d,e,f。其中 **c 属于已回滚的 tx3**（正确实现永不输出）；**d 在正确实现下根本不会被接受**（第 7 步缓冲满被拒）。

**(丙)** ROLLBACK 不释放总账：第 10 步后总账仍为 3，**第 11 步 R(1,f) 被拒·缓冲满**；tx1 最终输出错成 `[b,e]`（丢 f）；全部结束后 `Buffered()` 错成 **1**（c 泄漏）。第 13 步 C(3)：正确实现与该错误实现**都是拒绝·未知事务**（tx3 已回滚），两者一致。

## 四条不变量的落点
1. 与朴素参照一致：`api/selfcheck` 内朴素参照逐事件比对；测试 `TestNaiveConsistency`。
2. 原子性：`regroup.Feed` 的 COMMIT 分支整体搬走该 tx 缓冲、ROLLBACK 整体丢弃；测试 `TestAtomicity`。
3. 缓冲精确：`regroup` 总账 `total` 只在 ROW 接受 +1、COMMIT/ROLLBACK 释放；测试 `TestBufferedAccounting`。
4. 失败不留痕：`regroup.Feed` 先校验后改状态，四类哨兵错误任一触发即返回；测试 `TestFailureNoTrace`。
