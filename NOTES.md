# NOTES：流与版本表时态连接

版本简写 `10:A`、`22:墓`；vwm `-∞` 为初始水位；缓冲只列未释放事件。

| 步 | vwm | k 的全部版本 | 拒绝 | 缓冲 | 本步输出 |
|---:|----|----|----|----|----|
| 1 | -∞ | 10:A | 否 | — | 无 |
| 2 | -∞ | 10:A | 否 | e1@12 | 无 |
| 3 | -∞ | 10:A,20:B | 否 | e1@12 | 无 |
| 4 | -∞ | 10:A,20:B | 否 | e1@12,e2@25 | 无 |
| 5 | 11 | 10:A,20:B | 否 | e1@12,e2@25 | 无 |
| 6 | 11 | 10:A,12:C,20:B | 否 | e1,e2 | 无 |
| 7 | 11 | 10:A,12:C,20:B | 否 | e1,e2,e3@22 | 无 |
| 8 | 11 | 10:A,12:C,20:B,22:墓 | 否 | e1,e2,e3 | 无 |
| 9 | 22 | 10:A,12:C,20:B,22:墓 | 否 | e2@25 | e1(seq0,TS12)→C；e3(seq2,TS22)→无值 |
| 10 | 22 | 10:A,12:C,20:B,22:墓 | 是：迟到版本 ErrLateVersion | e2@25 | 无 |
| 11 | 22 | 10:A,12:C,20:B,22:墓 | 否 | e2@25 | e4(seq3,TS8)立即→无值 |
| 12 | 22 | 10:A,12:C,20:B,22:墓,30:D | 否 | e2@25 | 无 |
| 13 | 30 | 10:A,12:C,20:B,22:墓,30:D | 否 | — | e2(seq1,TS25)→无值（22:墓 生效） |

(甲) 第9步输出 e1→C、e3→无值。取"当前最新版"：第9步 e1 错→无值（22:墓），第13步 e2 错→D。AS OF 写成 `ValidFrom<TS`：e1 错→A、e3 错→B。释放写成 `TS<vwm`：第9步只放 e1(→C)，e3 推迟到第13步。
(乙) e2=无值、e3=无值（22:墓 覆盖 TS22/25）。墓碑跳过取更早版：e2→B、e3→B。迟到判定写成 `ValidFrom<vwm`：第10步 X@22 被接受并覆盖墓碑，e2 第13步错→X；e3 第9步已输出无值、重算变 X，违反不变量3。
(丙) 不缓冲、到达即按当时版本连接：e1→A（正C）、e2→B（正无值）、e3→B（正无值）、e4→无值（一致）；错的是 e1,e2,e3。正确输出先后 e1,e3,e4,e2（TS 12,22,8,25），按 TS 不单调：e4 迟到但 TS≤vwm 立即输出，插在第9步批与第13步之间。

不变量1（与朴素一致）：连接只在 `Feed` 立即分支与 `releaseLocked` 中、TS≤vwm 时经 `ver.AsOf` 完成；迟到版本在 `writeLocked` 入口被拒 → 终态恰为全部被接受变更。钉于 TestNaiveDifferential、TestThirteenSteps。
不变量2（恰好一次且有序）：Seq 仅在 `Feed` 接受时分配一次；缓冲事件只在 `releaseLocked` 移出并追加，按 (TS,Seq) 排序，谓词 TS≤vwm。钉于 TestNaiveDifferential、TestConcurrentFeed。
不变量3（已输出不可变）：`writeLocked` 拒 vf≤vwm；已输出 TS≤旧 vwm，其后被接受变更 vf>新 vwm≥TS，不可能影响其 AS OF。钉于 TestThirteenSteps（第10步拒、e3/e2 保持无值）。
不变量4（失败不留痕）：所有校验在持锁变更前返回——`Feed` 先查空键与缓冲容量再分配 Seq，`Watermark` 先查回退再赋值，版本写入先查空键/迟到再 Put。钉于 TestRejectedLeavesNoState。
