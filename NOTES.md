# NOTES: 布隆过滤器参数推导与实现索引

## k 的最优值推导

给定 m 位、n 个元素、k 个哈希函数，假阳性率约为

  f(k) = (1 - e^(-kn/m))^k

令 t = kn/m，对 g(k) = ln f(k) = k*ln(1 - e^(-t)) 关于 k 求导：

  g'(k) = ln(1 - e^(-t)) + t*e^(-t) / (1 - e^(-t))

令 g'(k) = 0，记 x = e^(-t)，则

  ln(1-x) = -t*x/(1-x)  =>  (1-x)*ln(1-x) = x*ln(x)

两边关于 x = 1/2 对称，故极小点在 x = 1/2，即 e^(-t) = 1/2，t = ln2：

  k* = (m/n)*ln2

此时 f = (1/2)^k*，代入反解 m = -n*ln(p) / (ln2)^2，k* = -ln(p)/ln2 = log2(1/p)。

为什么 k=1 或过大都更差：
- k=1：f = 1 - e^(-n/m)。n=10000、p=0.01 时 m/n≈9.59，f≈0.099，约为最优值 0.01 的 10 倍。
- k 过大：t=kn/m 增大使 e^(-t)→0，ln f ≈ -k*e^(-kn/m)→0⁻，f→1；且每个元素置位更多，位数组更快被填满。
- 由 g'(k) 的符号变化（k<k* 时 g'<0，k>k* 时 g'>0）可知 k* 是唯一极小点。

## 语义与代码位置索引

（测试写完后补充）
