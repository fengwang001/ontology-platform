# 分区保序路由器 NOTES

## 第三节：P=2 九步推导（分区0前缀和 10/40/80/140/230，分区1前缀和 20/70/140/220）

| 步 | 事件 | [C0,C1] | W() | PrefixTotal() |
|---|---|---|---|---|
| 1 | (0,0,10) | [1,0] | 0 | 0 |
| 2 | (1,0,20) | [1,1] | 1 | 10+20=30 |
| 3 | (0,1,30) | [2,1] | 1 | 30 |
| 4 | (0,2,40) | [3,1] | 1 | 30 |
| 5 | (1,1,50) | [3,2] | 2 | 40+70=110 |
| 6 | (0,3,60) | [4,2] | 2 | 110 |
| 7 | (1,2,70) | [4,3] | 3 | 80+140=220 |
| 8 | (1,3,80) | [4,4] | 4 | 140+220=360 |
| 9 | (0,4,90) | [5,4] | 4 | 360 |

- (甲) 第5步后 W=2、PrefixTotal=110。若错用「按到达顺序的全局累加器」得 10+20+30+40+50=150。差值 40 来自事件 (0,2,40)：它是分区0的第3条，超出 W=2 的已提交前缀（分区1当时只有2条），不该计入。
- (乙) 第6步后正确 W=2、PrefixTotal=110。若 W 错取 max 得 W=4，PrefixTotal=分区0前4条140+分区1全部(仅2条)70=210；多计入 (0,2,40) 与 (0,3,60)——分区0位点2、3的事件，分区1尚未对齐到该前缀。
- (丙) 另一交错：分区0五条先到 (0,0..4)，分区1四条后到 (1,0..3)，终值仍是 W=4、PrefixTotal=360。「容忍跨分区乱序」=终值只取决于各分区按位点排序后的事件集合，与到达交错无关。朴素参照必须按「分区内按位点、取前 W 条」陈述，因为 Pos 只在分区内有意义，跨分区不存在全局到达序号，按到达顺序陈述的参照在乱序下不唯一、不可判定。

## 不变量落实（代码位置 / 钉住它的测试）

1. 与朴素参照一致：route.Router.PrefixTotal 经 pofs 前缀和数组取前 W 条；测试 TestNaiveConsistency（随机交错对拍朴素实现）。
2. 按位点单调：W 是各分区计数的 min，计数只增，Val>=0 故前缀和只增；测试 TestMonotonicity。
3. 分区保序：pofs.Buffer.Append 强制 Pos==count；乱序终值不变由 TestInterleaveEquivalence 钉住。
4. 失败不留痕：route.Router.Append 先校验 Part/Pos/Val 全部通过才写 pofs，任一失败零写入；测试 TestFailureAtomicity（另含 TestPrefixTotalComplexity、TestConcurrentReads 钉第四、六节）。
