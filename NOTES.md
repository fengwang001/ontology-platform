# NOTES — 有界乱序事件时间重排缓冲

## 第三节推导（delay=3, maxBuffered=10；wm 列单位同 TS）

| 步 | 事件 | 前 wm | 判定 | 后 wm | 本步主输出 | 本步旁路 | 缓冲余 |
|---|---|---|---|---|---|---|---|
| 1 | a:5 | -inf | 缓冲 | 2 | — | — | a5 |
| 2 | b:3 | 2 | 缓冲 | 2 | — | — | b3 a5 |
| 3 | c:9 | 2 | 缓冲 | 6 | b3 a5 | — | c9 |
| 4 | d:5 | 6 | 迟到 | 6 | — | d5 | c9 |
| 5 | e:6 | 6 | 迟到 | 6 | — | e6 | c9 |
| 6 | f:9 | 6 | 缓冲 | 6 | — | — | c9 f9 |
| 7 | g:4 | 6 | 迟到 | 6 | — | g4 | c9 f9 |
| 8 | h:12 | 6 | 缓冲 | 9 | c9 f9 | — | h12 |
| 9 | i:12 | 9 | 缓冲 | 9 | — | — | h12 i12 |
| 10 | j:16 | 9 | 缓冲 | 13 | h12 i12 | — | j16 |

Flush：wm=+inf，释放 j:16。最终主输出 `b3 a5 c9 f9 h12 i12 j16`；旁路 `d5 e6 g4`。

- (甲) 迟到判定若写成 `TS < wm`：e:6 在 wm=6 时不算迟到 → 进缓冲，第 8 步随释放进主输出（该步主输出变 `e6 c9 f9`）。释放条件若写成 `TS < wm`：第 8 步主输出为空，c、f 推迟到第 10 步（wm=13 时与 h、i 一起释放）。
- (乙) 正确顺序：第 8 步 `c→f`、第 10 步 `h→i`（同 TS 按 Seq 升序）。若只按 TS 排序且同 TS 后到先出：第 8 步错成 `f→c`、第 10 步错成 `i→h`，(TS,Seq) 出现 (9,5)→(9,2) 的下降，违反不变量 2（主输出严格递增），进而违反不变量 1（与朴素参照的稳定排序不再逐条相同）。
- (丙) 迟到直接进主输出：完整主输出 `b3 a5 d5 e6 g4 c9 f9 h12 i12 j16`，首次 TS 下降在第 7 步 e:6→g:4（6→4）。若迟到直接丢弃：丢 d、e、g 三个事件。

## 四条不变量 → 代码位置 → 钉住它的测试

1. 与朴素参照一致：`api.Push` 严格按「判定→推进→释放」三步且判定用推进前 wm；`rbuf.Release` 按 (TS,Seq) 升序出前缀。测试：`TestSection3`、`TestNaiveReference`、`TestConcurrent`。
2. 主输出严格有序：`rbuf.Add` 二分插入保持 (TS,Seq) 全序，`Release` 只放 `TS<=wm` 的有序前缀。测试：`TestNaiveReference`（逐对断言 `order.Less`）、`TestBuffer`（释放顺序）。
3. 恰好一次：迟到进 `side`、其余进 `buf`，`Flush` 以 wm=+inf 放空缓冲，`seen` 去重。测试：`TestNaiveReference`（计数+唯一性+`buf.Len()==0`）、`TestConcurrent`。
4. 失败不留痕：`Push` 先校验空 ID/重复 ID，`rbuf.Add` 满时先返回 `ErrFull` 不做任何变更，全部校验先于序号分配。测试：`TestErrors`、`TestBuffer`（满时拒绝）；另由 `SelfCheck` 内置核验。
