# NOTES — KM 推导（矩阵 W：左0 [10,8,0]，左1 [10,0,0]，左2 [0,10,10]）

初始标号 l=各行最大=[10,10,10]，r=[0,0,0]。S=增广树内左，T=树内右；Δ=min(l[i]+r[j]-w[i][j])，i∈S，j∉T。

1. 增广左0：S={0}；紧边 0-右0（10=10），右0空 → 匹配 {0→0}；无需 Δ。
2. 增广左1：S={1}；紧边 1-右0，右0已配左0 → S={1,0}，T={0}。
   slack：(0,右1)=10-8=2，(0,右2)=10，(1,右1)=10，(1,右2)=10 → **Δ=2（右1）**。
   调整：l=[8,8,10]，r=[2,0,0]；边 0-右1 变紧（8+0=8），右1空 → 增广路 1-右0-0-右1；匹配 {0→1,1→0}。
3. 增广左2：S={2}；紧边 2-右1、2-右2；pop 右1(slack0，已配0)→左0入树，左0使右0 slack=0（8+2=10=w00）；pop 右0→左1入树；pop 右2(slack0，来自左2)空 → 路 2-右2。
   最终匹配 {0→1,1→0,2→2}=**[1,0,2]，总权 8+10+10=28**（最大）。

- **(甲) 贪心**（按左序取未占用右节点中权最大，并列取小编号）：左0→右0(10)，左1→右1(0)，左2→右2(10)，得 **20，[0,1,2]**；正确应为 28。
- **(乙) Δ 对全部左节点取 min**：步骤2第一次改标号时正确 **Δ=2**；错成 **0**——树外左2 有 l2+r1-w21=10+0-10=0。后果：标号不调整却把树外右节点当可达，扩出不存在的增广路，标号可行性被破坏，匹配非最大或死循环。
- **(丙) 误做最小权**：枚举 6 种排列，最小为 [2,1,0]：0+0+0=**0**；正确应为 28。

## 不变量（保证位置 / 钉住的测试）

1. **完美匹配**：`km.go` 的 `augment` 沿紧边交错路增广，mL/mR 始终互为逆映射；由 `TestSolveMatchesBruteForce` 钉住。
2. **最大权**：`km.go` 标号恒可行（l+r≥w），Δ 只降 S 内左标号、升 T 内右标号，可行性不变；紧边凑出完美匹配即最优；同测试对拍钉住。
3. **与朴素参照一致**：`km.Solve` 在表驱动用例（题三矩阵、负权、并列、随机矩阵）上与 n! 全排列枚举逐值、逐元素相同；`TestSolveMatchesBruteForce`。
4. **失败不留痕**：`bmg.go` `SetWeight` 先查越界、再查重复位，全部通过才写矩阵；`api.go` `Solve` 先 `Complete()` 全扫；`TestRejectionLeavesStateUnchanged` 钉住。
   字典序最小：`km.go` `lexMin` 只沿零 slack 边构成的交错圈旋转（保最优），逐位取最小；`TestSolveMatchesBruteForce` 对拍枚举的字典序最优排列。
   delta 计数：`km.go` 非导出字段 `deltaRChecks`（索引堆每次求 Δ 只 pop 1 个右节点）；`TestDeltaRChecksBounded` 用 n=100/1000/10000 断言每次 ≤2。
