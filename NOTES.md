# NOTES

## 第三节推导（演示参数 B=3, M=7, s="abab", 'a'=97, 'b'=98）

| i | P[i] | B^i mod 7 |
|---|------|-----------|
| 0  | 0 | 1 |
| 1  | 6 | 3 |
| 2  | 4 | 2 |
| 3  | 4 | 6 |
| 4  | 5 | 4 |

- (甲) `s[2..4)`="ab"：`(P[4]-P[2]*B^2) mod 7 = (5-4*2) mod 7 = -3 mod 7 = 4`，与逐字节重算 `(97*3+98) mod 7 = 389 mod 7 = 4` 一致，正确值是 **4**。若漏乘位移项写成 `P[4]-P[2]`，得 `5-4 = 1`，错算成 **1**。
- (乙) 原始差 `5 - 8 = -3`。Go 的 `%` 返回负余数，忘记调整到 `[0, M)` 会错返回 **-3**，正确应调整为 **4**（-3+7）。
- (丙) `s[0..3)`="aba"：`P[3]-P[0]*B^3 = 4-0 = 4`；`s[1..4)`="bab"：`P[4]-P[1]*B^3 = 5-6*6 = -31 ≡ 4 (mod 7)`。两者哈希都是 **4**，单哈希下 `Equal(0,3,1,4)` 会把不等的子串错判成 **true**。真实实现用双哈希（两个大素数模同时相等才判等），碰撞概率约 `1/(M1*M2)`，可忽略。

## 四条不变量落点

1. **Equal≡bytes.Equal**：`query.Equal` 先比长度再比双模哈希，`api.Equal` 先做区间校验；由 `TestEqualMatchesNaive` 钉住。
2. **子串哈希=逐字节重算**：`rhash.Table.Hash` 用 `(P[r]-P[l]*pow[r-l]) mod M` 并调正到 `[0,M)`；由 `TestHashMatchesRecompute` 钉住。
3. **LCP≡逐字节比较**：`query.LCP` 二分长度 + `Equal`；由 `TestLCPMatchesNaive` 钉住。
4. **失败不留痕**：`api` 先校验后执行，`New` 校验失败不赋值，所有查询只读；由 `TestRejectionLeavesState` 钉住。
