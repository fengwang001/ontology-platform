# 双输入算子检查点屏障对齐 — 推导与不变量

## 第三节：十步分步表（缓冲列 = c0 缓冲 / c1 缓冲；sum 只写非零键）

| 步 | 元素 | 本步后阻塞 | 本步后缓冲 | 本步输出片段 | 本步后 sum | 快照 |
|---|---|---|---|---|---|---|
| 1 | c0 x+1 | 无 | 空/空 | c0 x+1 | x=1 | 无 |
| 2 | c1 y+2 | 无 | 空/空 | c1 y+2 | x=1 y=2 | 无 |
| 3 | c0 B1 | c0 | 空/空 | 无 | x=1 y=2 | 无 |
| 4 | c0 x+10 | c0 | [x+10]/空 | 无 | x=1 y=2 | 无 |
| 5 | c1 x+3 | c0 | [x+10]/空 | c1 x+3 | x=4 y=2 | 无 |
| 6 | c0 y+20 | 同 | [x+10,y+20]/空 | 无 | x=4 y=2 | 无 |
| 7 | c1 y+4 | 同 | 同上 | c1 y+4 | x=4 y=6 | 无 |
| 8 | c1 B1 | 无 | 空/空 | B1, c0 x+10, c0 y+20 | x=14 y=26 | 快照1={x=4 y=6} |
| 9 | c1 x+5 | 无 | 空/空 | c1 x+5 | x=19 y=26 | 无 |
| 10 | c0 x+100 | 无 | 空/空 | c0 x+100 | x=119 y=26 | 无 |

- (甲) 第3步就快照：快照1={x=1 y=2}，漏掉 c1 x+3、c1 y+4（它们在 c0 屏障后、c1 屏障前到达）。恢复重放屏障后记录 x+10,y+20,x+100(c0)、x+5(c1)，得 sum={x=116 y=22}，正确应为 {x=119 y=26}。
- (乙) 不缓冲照常处理：第8步快照1={x=14 y=26}，多含屏障后的 c0 x+10、y+20。恢复时这两条又被重放一次，sum={x=129 y=46}，多算 x+10 与 y+20。
- (丙) LIFO 释放：第8步输出=B1, c0 y+20, c0 x+10；第10步后 sum 与正确实现相同（加法可交换）。违反不变量2（通道内保序：c0 的 y+20 到达晚于 x+10 却先出）。只比快照与 sum 发现不了，因为求和对顺序不敏感，错误只体现在输出流次序上。

## 第二节不变量 → 保证位置 → 钉住它的测试

1. 切割一致：align.(*state).process 在第二个屏障 n 被处理时调 align()，先快照再转发再重放 → TestSnapshotMatchesCut
2. 通道内保序：chanbuf.Chan 的 FIFO 缓冲 + align.(*state).replay 按到达序逐个弹出 → TestPerChannelOrder
3. 不丢不重：记录只经 (*state).process 进输出一次或经 Enqueue 进缓冲一次；State 即输出求和 → TestNoLossNoDup
4. 失败不留痕：align.(*Align).Push 先在克隆上应用、全部成功才换入；三类哨兵错误 → TestRejectLeavesNoTrace
