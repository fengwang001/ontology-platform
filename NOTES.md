# NOTES：增量前缀和视图

## 八步推导（N=16，从空视图开始；每行列：操作 → 存在的键:值 → 各键前缀和 → 本步受影响键数）

| # | 操作 | 键:值 | 前缀和 | 受影响 |
|---|------|-------|--------|--------|
| 1 | Put(3,5)  | 3:5 | P(3)=5 | 1（新键） |
| 2 | Put(7,-2) | 3:5, 7:-2 | P(3)=5, P(7)=3 | 1（新键；P(3) 不变） |
| 3 | Put(5,4)  | 3:5, 5:4, 7:-2 | 5, 9, 7 | 2（新键 5 + 键 7 变） |
| 4 | Put(3,8)  | 3:8, 5:4, 7:-2 | 8, 12, 10 | 3（差值+3 波及全部后缀） |
| 5 | Put(5,4)  | 不变 | 8, 12, 10 | 0（差值为 0） |
| 6 | Del(3)    | 5:4, 7:-2 | P(5)=4, P(7)=2 | 2（被删键不计） |
| 7 | Put(0,0)  | 0:0, 5:4, 7:-2 | 0, 4, 2 | 1（新键计 1；值为 0 不波及他人） |
| 8 | Put(7,-6) | 0:0, 5:4, 7:-6 | 0, 4, -2 | 1（仅键 7 变） |

**(甲)** 第 4 步后正确前缀和为 P(3)=8、P(5)=12、P(7)=10，受影响 3 个。若修改时把新值 8（而非差值 +3）加进累加结构：P(3)=13、P(5)=17、P(7)=15。按此错误实现跑完八步（累加器最终为 0:0, 3:5, 5:8, 7:-8），第 8 步后 Prefix(7) 错成 **5**，正确值是 **-2**。

**(乙)** 第 5 步返回 0、第 6 步返回 2、第 7 步返回 1。若错算成「操作后 >= k 的存在键个数」：第 5 步得 2、第 6 步得 2（恰好碰对）、第 7 步得 3。

**(丙)** 第 8 步后：Prefix(5)=4、Prefix(0)=0（值为 0 不是错误）、Prefix(6) 与 Prefix(3) 均返回「键不存在」错误。若对不存在的键直接返回 <=k 的和：Prefix(6) 错返回 4、Prefix(3) 错返回 0。

## 四条不变量与落点

1. **与朴素重算一致**：`psum` 用双 Fenwick（值树+计数树）增量维护，`psum.Put/Del/Prefix`；钉于 `TestSelfCheck`（api.SelfCheck 内逐步对拍朴素模型）。
2. **受影响数正确**：`psum.Put/Del` 用计数树算「>k（或 >=k）的存在键数」并按规则+1（新键）；钉于 `TestSelfCheck` 与 `TestAffectedCounts`。
3. **可逆**：Put 只加差值、Del 减去存储值，天然互逆；钉于 `TestSelfCheck`（不变量 3 段）与 demo 第 4 行。
4. **失败不留痕**：`psum.Put/Del/Prefix` 先按序校验（ErrBadParam→ErrOutOfRange→ErrNotFound→ErrTooMany）再改状态；钉于 `TestErrorPrecedence`、`TestFailureNoTrace`。

复杂度：`psum.View.last`（非导出）记录最近操作访问的 Fenwick 节点数，钉于 `TestVisitsLogarithmic`（<=8*(log2N+2)，与 m 无关）；并发由 `sync.RWMutex` 保证，钉于 `TestConcurrentReaders`（`go test -race`）。
