# NOTES

## 多输入失败时返回哪个错误

朴素顺序参照按 0,1,2,… 执行：下标 3 必然先于下标 5 执行，
因此若 3 会失败，参照在 3 处即失败，永远到不了 5。
为让并行结果与时序无关（第二节第 1、3 条），必须返回**下标最小的失败**，
而非时间上最早到达的失败。

收到第一个错误后不能立刻停止等待：更小下标仍在跑的输入也可能失败。
例：5 很快失败、3 较慢失败，立刻返回会错误地报 5。
规则：不再派发新输入、取消 ctx、排空在跑任务，最终取失败下标最小值 f；
顺序保证 <f 全部成功，故只 emit 0..f-1，f 及之后一律不 emit。

## 语义位置与测试（实现后回填）

- 保序：待回填
- 窗口上界：待回填
- 失败语义：待回填
- 错误包装：待回填
- 资源：待回填
- 复杂度实测：待回填
