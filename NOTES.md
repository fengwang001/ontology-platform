# Union-Find 设计笔记

## 1. 范围

仅实现属性等价关系的动态连通分量：合并、同分量判定、分量计数。状态在进程内存，仅用标准库。

## 2. 接口

- `uf`：`New(n)` / `Find(x)` / `Union(x,y)` / `Connected(x,y)` / `Count()`。
- `id`：元素下标类型 `Index` 与哨兵错误，委托 `uf`。
- `check`：朴素 BFS 参照与全部表驱动测试。

## 3. 推导：为什么必须按秩合并 + 路径压缩

朴素合并总把 `y` 的根挂到 `x` 的根下，树没有高度约束；链式 Union 后树退化成单链，
`Find` 从叶到根走 n 跳，退化为 O(n)。

按秩合并：每棵树维护秩 `r`（高度上界），合并时把秩小的根挂到秩大的根下；秩相等时
任选其一为根并把其秩加 1。一个秩为 r 的根至少持有 2^r 个节点（归纳：r 首次出现必由两棵
秩 r-1 的树合成，节点数 ≥ 2·2^(r-1) = 2^r），故 r ≤ log2(n)，树高 ≤ log2(n)，
Find 最坏 O(log n)。n=2^16 时任意 Find 跳数 ≤ 16。

路径压缩：Find 时把沿途节点直接指向根，后续查找近乎 O(1)。它不增加秩（秩只在合并时
变化），与按秩合并配合后，Union/Find 的均摊代价为反阿克曼函数 O(α(n))，对任何实际
n 都 ≤ 5，即“近乎常数”。保守钉测：随机 10000 次 Union 后任意 Find 跳数
≤ 2·log2(n)+2（秩界 2log n，路径压缩只会更短）。

对照实现（总是挂到 x 根下，无压缩）在同一链式序列下 Find 最深跳数 = n-1 > 1000。

## 4. 代码位置与测试索引

（测试完成后回填）
