# 推导：栈放什么、不匹配时怎么处理

A：预置基准 -1；'(' 压下标；')' 先弹栈，若栈空则压当前下标作新基准，否则长度=i-栈顶。
B：不预置基准；')' 无法匹配时直接清空栈。

对 `")()())"` 逐字符走查（栈为该字符处理完后的状态）：

| i | c | A：栈 | A：最长 | B：栈 | B：最长 |
|---|---|---|---|---|---|
| 起始 | - | [-1] | 0 | [] | 0 |
| 0 | ) | [0] | 0 | [] 清空 | 0 |
| 1 | ( | [0 1] | 0 | [1] | 0 |
| 2 | ) | [0]，2-0=2 | 2 | [] 弹空，无基准 | 0 |
| 3 | ( | [0 3] | 2 | [3] | 0 |
| 4 | ) | [0]，4-0=4 | 4 | [] | 0 |
| 5 | ) | [5] | 4 | [] 清空 | 0 |

A 得 4（子串 1..4）；B 在 i=2 即失去基准，i=4 时前段已被清空，
1..4 连不成一段，结果为 0。基准取 -1：长度=右边界 i 减去有效区
前一个位置，串首或空串时该位置正是 -1。不匹配的 ')' 必须压入而非
清空：它是下一有效区的基准，保留它后续配对段才能连续计数。

## 不变量：代码保证位置 / 钉住的测试

1. 配平与首个出错位置：`balance.go:Check`；`TestBalanceCheck`。
2. 子串真实且最长：`longest.go:scan`；`TestNaiveCrosscheck`（朴素枚举对照）。
3. 并列最长取最左：`longest.go:scan` 仅 `>` 时更新；`TestTieLeftmost`。
4. 失败不留痕：`New`/`Longest` 先校验后计算、不写输入；`TestErrors`（三类哨兵、拒绝后可复用）。
5. 单遍访问恰 n 次：`longest.go:scan` 的非导出 `visits`；`TestCharCountExactlyN`（1000/100000 × 四形态）。
6. 并发逐位一致：`TestConcurrentDeterministic`，`go test -race` 干净。
