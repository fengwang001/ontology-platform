# NOTES

规则：key = U^(1/w)，k=2，始终保留 key 最大的两个。

| 步 | key 计算 | 动作 | 之后样本（元素:key） |
|---|---|---|---|
| 1 A | 0.9^1 = 0.900 | 纳入（未满） | [A:0.900] |
| 2 B | 0.64^(1/2) = 0.800 | 纳入（未满） | [A:0.900, B:0.800] |
| 3 C | 0.75^1 = 0.750 | 丢弃（0.750 < 最小 0.800） | [A:0.900, B:0.800] |
| 4 D | 0.343^(1/3) = 0.700 | 丢弃（0.700 < 0.800） | [A:0.900, B:0.800] |
| 5 E | 0.1^1 = 0.100 | 丢弃（0.100 < 0.800） | [A:0.900, B:0.800] |

正确最终样本：{A, B}。

(甲) 错写 key=w：权重 1,2,1,3,1，步 4 D(3) 顶掉最小的 A(1)，得 {B, D}；正确 {A, B}。A 被错误逐出，D 被错误收进。
(乙) 错写 key=U：.9/.64/.75/.343/.1，步 3 时 C(.75) 顶掉 B(.64)，得 {A, C}；正确 {A, B}。根因：权重为 2 的 B 被权重 1 的 C 挤掉——1/w 次幂本应把 B 的键从 .64 抬到 .80 > .75。
(丙) 错成保留最小：A 纳入 → B 纳入 → C(.75) 逐 A(.9) → D(.7) 逐 B(.8) → E(.1) 逐 C(.75)，得 {D:0.700, E:0.100}；正确 {A, B}。

不变量在代码中的保证位置与钉住的测试：

1. 大小恒为 min(k,N)：wsampler.(*Sampler).Feed 只在未满时纳入、满后仅原位替换，计数器 n 只增到 k；TestInvariants。
2. 前 k 必留：wrs.(*Reservoir).Offer 的 len(slots)<k 分支无条件纳入；TestInvariants。
3. 与朴素离线参照一致：wrs.Key 用 math.Pow(u,1/w)，Offer 仅当新键严格大于最小键才替换；TestOfflineReference。
4. 失败不留痕：wsampler.New 拒 k<=0 与 nil rng；Feed 先整批校验（空值/权重<=0/U 越界）通过后才触碰任何状态；TestRejectionAtomic。
5. O(k)：wsampler.n 为非导出计数器且无任何导出访问途径，白盒 TestRetainedCounterOK 在 m=100/1000/10000 下逐元素断言 n==min(k,seen)、填满后恒为 k；并发只读由 TestConcurrentReaders 钉住。
