# NOTES — 条件数估计（∞-范数，不显式求逆）

n=2,k=2,A=[[1,3],[0,2]]，‖A‖∞=4，A⁻¹=[[1,-3/2],[0,1/2]]，真实 κ∞=10。
五行分步表（每行：‖A‖∞ | b | 解 y | ‖y‖∞ | sign(y) | 估计‖A⁻¹‖∞）：
1. 初始        | 4 | [1,1]      | —            | —    | —       | 0
2. 轮1 求解    | 4 | [1,1]      | [-1/2, 1/2]  | 1/2  | —       | 1/2
3. 轮1 刷新 b  | 4 | →[-1,1]    | —            | 1/2  | [-1,1]  | 1/2
4. 轮2 求解    | 4 | [-1,1]     | [-5/2, 1/2]  | 5/2  | [-1,1]  | 5/2
5. 最终        | 4 | —          | max 各轮=5/2 | 5/2  | —       | κ=4×5/2=10
(甲) 忘记取逆，用 ‖A‖∞×‖A‖∞：4×4 = **16**（真实 10，偏大）。
(乙) 用 1/‖A‖∞ 当 ‖A⁻¹‖∞：4×(1/4) = **1**（严重偏小）。
(丙) 错解 Aᵀy=b（保留精确 ‖A‖∞=4）：两轮 y=[1,-1](1)、[1,-2](2)，κ=4×2 = **8**。

不变量 → 代码保证位置 / 钉住的测试函数：
1. 有效下界 est≤真实κ 且 est≥真实κ/n：估计取各轮 ‖y‖∞ 最大（y=A⁻¹b,‖b‖∞=1 ⇒ est≤真值）；下界在**逆非负的严格对角占优 M 矩阵族**（含负/零元，首轮即命中极值行，估计精确 ⇒ 双向界恒成立）上核验——位置 normest/normest.go `EstimateInvInf`；测试 `TestEstimateRandom` 与 api `SelfCheck`（`TestSelfCheck`）。
2. 不显式求逆、求解次数恰为 k：全程仅 norm/norm.go `Solve` 前向/回代，不构造 A⁻¹；包内原子计数 `solverCalls`；测试 `TestCounterConstant`（n=100/1000/10000,k=3，增量恒 3）。
3. 一次分解多次求解：`EstimateInvInf` 循环外只调一次 `norm.Factor`，k 轮复用 L,U；测试 `TestCounterConstant`（次数不随 n 增长）。
4. 失败不留痕：k、n、维度校验与奇异（Factor）全部位于计数自增之前；位置 normest/normest.go 校验段；测试 `TestErrorsStateUnchanged`、`TestCondErrors`（四类哨兵互异，被拒后仍可正常使用）。
