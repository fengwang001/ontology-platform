# NOTES

## 第三节：八步推导

| 步 | 操作 | 已登记 | 暂存 | 已回放 | Replay 返回 |
|---|---|---|---|---|---|
| 1 | Commit(3,5) | {3} | {3} | {} | — |
| 2 | Commit(5) | {3,5} | {} | {} | — |
| 3 | Commit(1,5) | {1,3,5} | {} | {} | — |
| 4 | Replay() | {1,3,5} | {} | {1,3,5} | [5,1,3] |
| 5 | Commit(2,1) | {1,2,3,5} | {} | {1,3,5} | — |
| 6 | Replay() | {1,2,3,5} | {} | {1,2,3,5} | [2] |
| 7 | Commit(7,2) | {1,2,3,5,7} | {} | {1,2,3,5} | — |
| 8 | Replay() | {1,2,3,5,7} | {} | {1,2,3,5,7} | [7] |

- (甲) 忽略依赖按 id 升序：第 4 步错返回 **[1,3,5]**；正确为 **[5,1,3]**。1、3 排在依赖 5 之前，违反不变量 1（依赖先行）。
- (乙) 依赖未登记即拒绝：第 1 步 `Commit(3,5)` 报错，事务 **3 永远丢失**，第 4 步只返回 [5,1]，后续回放都少掉 3。
- (丙) `Commit(2,1)` 使 2 暂存（1 未登记）；`Commit(1,2)` 登记时图上仅 1→2 无环被接受，激活 2 时补边 2→1 成环。若激活不查环则漏检：1、2 互等对方先回放，**Replay 永远选不出二者（僵持/死锁），二者永不回放**。

## 第二节：四条不变量的保证位置与钉住测试

1. 依赖先行：`gph` 入度（未回放依赖数）+ 就绪最小堆，仅 `MarkReplayed` 降入度，`rpl.Replay` 只弹堆顶；测试 `TestEightSteps`。
2. 可复现：`gph` 就绪堆按最小 id 弹出，tie-break 唯一；测试 `TestReproducible`。
3. 暂存正确：`gph.Commit` 中依赖未登记则入 `staged`、依赖登记齐后 `activate`；测试 `TestStaging`。
4. 失败不留痕：`gph.Commit` 先校验（id/自依赖/重复/上限），并在克隆边集上查环通过后才落地；测试 `TestErrorsNoSideEffect`。
