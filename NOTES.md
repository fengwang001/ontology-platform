# 稀疏位点索引：推导与不变量

## 八行分步表（base=1000，indexInterval=100）

| 记录(位点/字节) | 物理位置 | 写前累计 | 建条目(>=100) | 写后累计 |
|---|---|---|---|---|
| 1000/60 | 0 | 0 | 否 | 60 |
| 1001/50 | 60 | 60 | 否 | 110 |
| 1003/40 | 110 | 110 | 是 (3,110) | 40 |
| 1004/60 | 150 | 40 | 否 | 100 |
| 1007/30 | 210 | 100 | 是 (7,210) | 30 |
| 1008/80 | 240 | 30 | 否 | 110 |
| 1010/20 | 320 | 110 | 是 (10,320) | 20 |
| 1012/50 | 340 | 20 | 否 | 70 |

完整索引：`[(3,110), (7,210), (10,320)]`

**(甲)** `Lookup(1004)`：floor 条目 (3,110)，从 110 扫 1003、1004 → 返回 `(1004,150)`，扫 2 条。`Lookup(1006)`：floor (3,110)，扫 1003、1004、1007 → 返回 `(1007,210)`，扫 3 条。若错取 ceiling（不小于目标的第一个条目 (7,210)），从 210 起扫，`Lookup(1004)` 会返回 `(1007,210)`，漏掉真正的 1004。

**(乙)** 条件错写成 `累计 > 100`：第 5 行累计恰为 100 不触发，第 6 行才触发 → 索引变 `[(3,110), (8,240)]`。此时 `Lookup(1007)` 的 floor 是 (3,110)，扫 1003、1004、1007 共 3 条；正确实现 floor 是 (7,210)，只扫 1 条。

**(丙)** 追加 `1013/40`：累计 70<100 不建条目，返回物理位置 390，索引不变。再追加位点 `1000+2147483648`：rel=2147483648 > int32 上限，整体拒绝（ErrRelOverflow），记录/索引/累计/字节数均不变。若截断成 int32，rel 变 -2147483648，且此时累计 110>=100，会写入条目 `(-2147483648, 430)`。`Lookup(999)` 若不查下界：rel=-1 无 floor 条目，从 0 扫 → 返回 `(1000,0)`，扫 1 条（本应报 ErrBelowBase）。

## 四条不变量落点

1. 与朴素参照一致：`segment.Lookup` 用 `idx.Floor` 定起点后顺序扫描；测试 `api.TestLookupMatchesNaive`。
2. 索引良构：条目只在 `segment.Append` 中按规则追加，天然随记录递增；测试 `api.TestIndexWellFormed`。
3. 与规则重算一致：同上唯一建索引点；测试 `api.TestIndexMatchesRecompute`。
4. 失败不留痕：`Append`/`Lookup` 先完成全部校验再改任何状态；测试 `segment.TestFailureNoTrace`。
