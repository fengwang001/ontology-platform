# Heartbeat 水位推进推导（delay=3）与不变量落点

| # | 事件 | 类型 | 贡献 | 迟到心跳 | 步后水位 |
|---|---|---|---|---|---|
| 1 | 数据(a,TS=5) | 数据 | 5−3=2 | 否 | 2 |
| 2 | 心跳(TS=12) | 心跳 | 12 | 否 | 12 |
| 3 | 数据(b,TS=8) | 数据 | 8−3=5 | 否 | 12 |
| 4 | 心跳(TS=9) | 心跳 | 9 | 是（9≤12） | 12 |
| 5 | 心跳(TS=20) | 心跳 | 20 | 否 | 20 |
| 6 | 数据(c,TS=14) | 数据 | 14−3=11 | 否 | 20 |
| 7 | 心跳(TS=17) | 心跳 | 17 | 是（17≤20） | 20 |
| 8 | 心跳(TS=25) | 心跳 | 25 | 否 | 25 |

最终水位 **25**，迟到心跳 **2** 个（第 4、7 个）。

- **(甲)** 心跳误按数据处理（贡献 TS−3）：心跳贡献依次为 9/6/17/14/22，第 8 步后错成 **22**；正确应是 **25**。
- **(乙)** 完全忽略心跳：只剩数据贡献 2、5、11 取 max，最终错成 **11**。空闲流没有数据事件到达，max 没有新输入，
  水位只能停在旧值；心跳是空闲期唯一的「时间已推进」承诺信号，忽略它下游会永远认为时间停滞。
- **(丙)** 迟到心跳直接 `wm=TS`（不取 max）：第 4 步水位从 12 **回退成 9**（正确仍为 12），违反**不变量 2（水位单调）**。

## 第二节四条不变量：保证位置与钉住测试

1. **与批量重算一致**：`wm/wm.go` 的 `Advance` 即 max 归并、`hbs/hbs.go` 逐事件喂入；`api` 的 `TestBatchRecomputeConsistency` 用批量重算比对。
2. **水位单调**：`wm/wm.go` 的 `Advance` 仅在贡献更大时替换；钉住测试 `TestWatermarkMonotonic`、`TestConcurrentAdvanceMonotonic`。
3. **心跳语义**：`hbs/hbs.go` 中 `Heartbeat` 贡献 TS 本身、`judgeLate` 命中只累加迟到计数；钉住测试 `TestEightStepSequence`。
4. **失败不留痕**：`api/api.go` 在调用 `hbs` 之前先校验并直接返回哨兵错误，状态一行未触；钉住测试 `TestRejectionLeavesNoTrace`。

迟到判定 O(1)：`hbs` 非导出字段 `lateChecks`（判定只比较标量水位，检查历史事件数恒 0），钉住测试 `TestLateCheckDoesNotRescanHistory`（m=100/1000/10000）。
