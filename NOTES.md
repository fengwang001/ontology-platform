# NOTES

## 八步推导（maxIntervals=8）

| # | 操作 | 变更日志 | 操作后并集 |
|---|---|---|---|
| 1 | Add(0,10) | `+[0,10)` | `[0,10)` |
| 2 | Add(10,20) | `-[0,10)` `+[0,20)` | `[0,20)` |
| 3 | Add(30,40) | `+[30,40)` | `[0,20)` `[30,40)` |
| 4 | Withdraw(15,35) | `-[0,20)` `+[0,15)` `-[30,40)` `+[35,40)` | `[0,15)` `[35,40)` |
| 5 | Add(15,35) | `-[0,15)` `-[35,40)` `+[0,40)` | `[0,40)` |
| 6 | Withdraw(20,25) | `-[0,40)` `+[0,20)` `+[25,40)` | `[0,20)` `[25,40)` |
| 7 | Add(20,25) | `-[0,20)` `-[25,40)` `+[0,40)` | `[0,40)` |
| 8 | Withdraw(0,40) | `-[0,40)` | （空） |

(甲) 只合并严格重叠、不合并邻接时：第 2 步后错成 2 段 `[0,10)` `[10,20)`（应为 1 段 `[0,20)`）；此后第 4 步撤 `[15,35)` 得 `[0,10)` `[10,15)` `[35,40)`，第 5 步 `Add(15,35)` 与任何段都不严格重叠（15==15、35==35 只贴边），错成 4 段 `[0,10)` `[10,15)` `[15,35)` `[35,40)`（应为 1 段 `[0,40)`）。
(乙) 不判空就输出残留段，会多输出两条非法条目：`+[35,20)`（`[0,20)` 的右残留为空）与 `+[30,15)`（`[30,40)` 的左残留为空），均 s>=e。
(丙) 只处理第一个相交区间就停：`[30,40)` 完全没被撤，最终残留 `[0,15)` `[30,40)`；正确应剩 `[0,15)` `[35,40)`，即 `[30,40)` 应被拆成只留 `[35,40)`。

## 四条不变量的落点

1. 与朴素重放一致：`union/union.go` 的 `Add`/`Withdraw` 与 `seg/seg.go` 的朴素参照 `NaiveAdd`/`NaiveWithdraw`（`api.naiveReplay` 逐步整体重算）实现同一套规则；测试 `TestReplayMatchesNaive`。
2. 变更日志自洽：`union/union.go` 只对当前 `u.segs` 中真实存在的段输出 `-`（端点原样拷贝）；测试 `TestChangelogSelfConsistent`。
3. 归并完备：`union/union.go` `Add` 合并全部接触段、`Withdraw` 残留段是原段子集；测试 `TestViewMaximal`。
4. 失败不留痕：`union/union.go` 先校验（`s>=e`、段数超限、无严格重叠）再改状态，被拒绝时未触碰 `u.segs`/`u.log`；测试 `TestRejectedOpsLeaveNoTrace`。
