# NOTES

## 第三节：六步交错推导（maxRetries=3，G1=[a+1,c+1]，G2=[b+5,c+10]）

| # | 动作 | 快照读到 | 校验结论 | 本步改动 | 视图(a,b,c) | 累计重试 |
|---|------|----------|----------|----------|-------------|----------|
| 1 | G1 快照 | a=0@v0, c=0@v0 | — | 无 | (0,0,0) | 0 |
| 2 | G2 快照 | b=0@v0, c=0@v0 | — | 无 | (0,0,0) | 0 |
| 3 | G1 校验+提交 | a@v0, c@v0 | 均未变 | a=1(v1), c=1(v1) | (1,0,1) | 0 |
| 4 | G2 校验+提交 | b@v0, c@v0 | c 已变 v0→v1，冲突 | 无（整批不应用） | (1,0,1) | 0 |
| 5 | G2 重新快照 | b=0@v0, c=1@v1 | — | 无 | (1,0,1) | 0 |
| 6 | G2 校验+提交 | b@v0, c@v1 | 均未变 | b=5(v1), c=11(v2) | (1,5,11) | 1 |

- (甲) 去掉版本校验：第 3 步后视图 (1,0,1)；第 4 步 G2 持旧快照 c=0 直接写 `c=0+10`，覆盖 G1 的 +1 → 视图 (1,5,10)。丢失更新发生在第 4 步、Key `c`；最终 c 错成 **10**（正确 11）。
- (乙) 逐 Key「校验→立即应用」且不回滚：第 4 步先把 **b** 应用掉（b=5），随后 c 冲突、整批重试时 b 被再加 5 → b 错成 **10**（正确 5）。
- (丙) 悲观锁按请求顺序加锁：G1 先锁 c（持有）、G2 先锁 a（持有）；G1 再申请 a 被 G2 持有、G2 再申请 c 被 G1 持有 → 循环等待死锁。死锁后双方均未提交，视图停在 **(a,c)=(0,0)**；正确实现按 Key 排序（都按 a→c）加锁，最终 **(a,c)=(3,3)**。

## 第二节：四条不变量的保证位置与钉住测试

1. 朴素重放一致：`commit.tryCommit` 在互斥锁内按字典序整体应用净增量 → `TestNaiveReplay`。
2. 无部分提交：`tryCommit` 同一把锁内先全量校验、全部通过才统一应用，冲突直接返回 → `TestAtomicNoPartial`。
3. 版本单调且恰 +1：仅 `cell.Cell.Apply` 内 `ver++`，只在成功提交路径调用 → `TestVersionPlusOne`。
4. 失败不留痕：`merge` 先于任何状态访问拒绝非法批；冲突/耗尽不改值、版本、retries（retries 仅在「重试后成功」时 +1）→ `TestRejectsNoTrace`、`TestContended`。
