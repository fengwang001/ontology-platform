# Fisher-Yates 洗牌推导

正确做法（从后往前）：第 i 步（i 从 n-1 递减到 1）在区间 [0,i] 内均匀选下标 j，交换 arr[i] 与 arr[j]。

为什么不能每步都从整个数组 [0,n) 选：每一步都有 n 种等概率选择，共 n 步，路径总数为 n^n 条，
每条概率 1/n^n；而不同排列只有 n! 种。若排列等概率，路径必须在 n! 个排列间均分，要求 n! 整除 n^n。
该整除一般不成立（n=3：27 不能被 6 整除；n=4：256 不能被 24 整除），于是某些排列对应路径偏多
（概率翻倍），某些为零，排列严重不均匀。

正确区间为何均匀：第 i 步在 [0,i] 的 i+1 个位置中等概率选择，总路径数 = n*(n-1)*...*2 = n!。
任意两条不同路径产生不同排列，且每种排列恰由一条路径产生（逆过程可从结果唯一还原每一步的 j），
故每种排列概率 = 1/n!，全部排列等概率。

随机数调用次数：恰好 n-1 次（i 从 n-1 到 1）；空数组与单元素为 0 次，原样返回。

## 语义与测试位置

（测试写完后补入）
