# Union-Find 推导与实现说明

## 平衡策略与复杂度推导

- 朴素合并（总是把 y 的根挂到 x 的根下）不限制树高：连续合并 (0,1),(1,2),...,(n-2,n-1)
  会得到一条长度 n-1 的链，`Find(n-1)` 必须沿父指针走到根，退化为 O(n)。
- 按秩合并：每个根维护秩（树高上界），合并时把秩较小的根挂到秩较大的根下；秩相等时任选一者，
  新根秩加 1。一棵树的秩从 k 升到 k+1，至少要再合并一棵秩同样为 k 的树，即其节点数至少翻倍。
  由 n 个节点起步，秩至多翻倍 log2(n) 次，故任何树的秩 ≤ log2(n)，单次 `Find` 跳数 O(log n)。
- 路径压缩：`Find` 递归找到根后，把沿途所有节点的父指针直接改为根，后续查找基本为 1 跳。
  压缩不增加秩；秩只作为「合并方向」的上界，单调不减。
- 按秩合并保证每次操作的树高上界，路径压缩反复摊平已走过的路径，两者配合后
  （逆阿克曼函数 α(n)，对一切实际 n 有 α(n) ≤ 5），均摊每次操作 O(α(n))，即近乎常数。
  工程上用更松的界钉住：路径跳数 ≤ 2·log2(n)+2（n=100000 时为 36）。

## 语义实现位置

（测试完成后补入）
