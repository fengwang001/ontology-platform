# NOTES

## 第三节推导：n=7，边 2→0 2→1 4→1 3→1 5→6 0→6 1→6

初始入度：0:1 1:3 2:0 3:0 4:0 5:0 6:3；初始候选 {2,3,4,5}。

| 步 | 输出 | 输出后候选集合 | 本步入度减为 0 的终点 |
|---|---|---|---|
| 1 | 2 | {0,3,4,5} | 0 |
| 2 | 0 | {3,4,5} | 无 |
| 3 | 3 | {4,5} | 无 |
| 4 | 4 | {1,5} | 1 |
| 5 | 1 | {5} | 无 |
| 6 | 5 | {6} | 6 |
| 7 | 6 | {} | 无 |

拓扑序：2 0 3 4 1 5 6。

- (甲) FIFO：初始队 [2,3,4,5]，输出 2 后 0 追加队尾 → [3,4,5,0]，第 2 个输出错成 **3**（正确应为 **0**）。
- (乙) 最大优先：首步候选 {2,3,4,5} 取最大，第 1 个输出错成 **5**（正确应为 **2**）。
- (丙) 加 0→2 后成环 0↔2：正确 `TopoSort` 返回哨兵错误 `ErrCycle`、不返回任何序列；若不检测环，处理完能处理的节点后静默返回部分序列 **[3,4,5]**（0,1,2,6 永远入度非 0）。

## 第二节四条不变量（代码位置 + 钉住它的测试）
1. 合法拓扑序：`topo/topo.go` `run()` 中终点入度减到 0 才入堆、出堆才输出，保证 `pos(u)<pos(v)`；测试 `TestTopoSortInvariants`（经 `verifyOrder`）、`api.SelfCheck`。
2. 与朴素参照一致：`topo/topo.go` `minHeap` 每步取编号最小者；测试 `TestMinFirstOrder`（精确序列）、`TestTopoSortInvariants`（对 `naiveKahn` 逐元素）。
3. 完整性/环检测：`topo/topo.go` 末尾 `len(order)!=len(indeg)` 时返回 `ErrCycle` 且 `nil` 序列；测试 `TestCycle`、`TestRejections`。
4. 失败不留痕：`dag/dag.go` `AddEdge` 三项校验全部先于任何写操作；`topo` 只在 `Snapshot` 副本上排序不碰原图；测试 `TestRejections`。
