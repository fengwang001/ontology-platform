# EDF 准入推导

1. 任务 i 每 Ti 刻请求 Ci 刻，长期需求率为 Ci/Ti，总利用率 U=ΣCi/Ti。
2. 必要性：若 U>1，单位时间的平均执行需求超过单处理器供给，任何调度器都必有任务错过截止期。
3. 充分性：截止期等于周期时，EDF 总是执行绝对截止期最早的就绪作业。
4. 若 EDF 在 t 首次错失，说明 [0,t] 内截止期不晚于 t 的作业总需求超过 t；EDF 的最早截止期规则不会为更晚截止期作业推迟这些作业。
5. 这些紧迫作业的需求按周期 C·⌈t/T⌉ 计入，而 U≤1 保证该需求界限不超过可用处理器时间；故不存在错失，U≤1 也是充分条件。
6. 固定优先级不具备该等价性。反例：(C,T)=(1,4)、(2,6)、(3,8)，U=1/4+1/3+3/8=23/24<1。
7. RM 下短周期任务优先：[0,8] 为 (1,4) 用 2 刻、为 (2,6) 的首个作业用 2 刻、在 6 刻又被新的 (2,6) 抢占；(3,8) 到截止期只执行 2 刻，因而错失。
8. EDF 先完成 6 刻截止期作业，再让 8 刻截止期作业在 7 刻前完成，同一任务集无错失。
9. 因此准入用有理数 big.Rat 计算 U，并以 U≤1 作为精确且原子的判定条件。
