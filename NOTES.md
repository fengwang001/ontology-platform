# NOTES — 批量事件时间对齐器

## 第三节推导（Key = "k"，A(-1) 视为无下限）

| # | 批 | TS | V | 迟到? | 本步后本批对齐时间 | 本步后 k 最终值 |
|---|---|----|---|-------|--------------------|------------------|
| 1 | 0 | 10 | 1 | 否（第 0 批无下限） | 10 | 1 |
| 2 | 0 | 5 | 2 | 否 | 5 | 1（10>5，V=1 保留） |
| 3 | 1 | 20 | 3 | 否（20≥A(0)=5） | 20 | 3 |
| 4 | 1 | 12 | 4 | 否（12≥5） | 12 | 3（20>12） |
| 5 | 2 | 8 | 5 | **迟到，丢弃**（8<A(1)=12） | 12（批内尚无被接受事件，沿用 A(1)） | 3 |
| 6 | 2 | 12 | 6 | 否（12=12，严格小于才迟到） | 12 | 3（12<20） |
| 7 | 3 | 25 | 7 | 否（25≥12） | 25 | 7 |
| 8 | 3 | 25 | 9 | 否 | 25 | 9（TS 平局，后到者胜） |

最终：A=[5,12,12,25]，Dropped=1，k=9。

- (甲) 错用批内最大 TS：A(1) 错成 20（正确 12）；第 2 批 (8,5)、(12,6) 都被误判迟到，丢弃数由正确值 1 错成 2。
- (乙) 错用最小 TS 胜：被接受事件中最小 TS=5 → k 错成 2（正确 9）。
- (丙) 平局错用先到者胜：第 3 批 (25,7) 先到 → k 错成 7（正确 9）。

## 四条不变量的落点

1. 与批量重算一致：`lww/lww.go` `Feed` 中 `ts >= e.ts` 才覆盖（最大 TS 胜、平局后到胜，等价于批内按 TS 升序应用）；测试 `TestInvariant1BatchRecompute`（api/api_test.go）。
2. 对齐时间单调且正确：`aln/aln.go` `Accept` 增量维护运行最小（每事件 1 次比较），`Close` 仅在有被接受事件时推进 prev、否则沿用；测试 `TestInvariant2AlignedMonotone`。
3. 丢弃与迟到一致：`lww.Feed` 先经 `aln.Late` 判定，迟到只增 dropped、不写 vals、不进 Accept；测试 `TestInvariant3DroppedConsistent`。
4. 失败不留痕：`api/api.go` `New`/`Feed` 先完成全部校验再做任何写入；测试 `TestInvariant4NoStateOnReject`。
