# Indexed Heap Notes

## Decrease-key 调整方向

最小堆中，节点的两个子节点键本就不小于该节点键。
将节点键变小后，它仍不会大于任一子节点，因此无需也不能 sift-down。
它只可能违反“子节点不小于父节点”：若新键小于父节点，就与父节点交换并重复 sift-up。

反例堆序列为 `[3,5,7,9,11,13]`，把键 9 降到 1 后应得到 `[3,5,7,1,11,13]` 的堆树。
错误地执行 sift-down 时，1 位于叶子，没有交换，树保持不变；随后 Pop 序列为
`[3,5,1,7,11,13]`，而正确序列必须是 `[1,3,5,7,11,13]`。

因此 decrease-key 只沿父链 sift-up，交换次数不超过树高 `floor(log2 n)`。
