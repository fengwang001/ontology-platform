# NOTES

## 一、八步推导（maxInFlight=3，key=K，next 初始 1；到达 5,2,4,1,3,6,7,2）
| 步 | Seq | 动作 | next | 缓冲 | 已发射 |
|---|---|---|---|---|---|
| 1 | 5 | 缓冲（5>1，余位） | 1 | [5] | [] |
| 2 | 2 | 缓冲 | 1 | [2,5] | [] |
| 3 | 4 | 缓冲（达上限 3） | 1 | [2,4,5] | [] |
| 4 | 1 | 发射 1 并级联放行 2 | 3 | [4,5] | [1,2] |
| 5 | 3 | 发射 3 并级联放行 4,5 | 6 | [] | [1,2,3,4,5] |
| 6 | 6 | 发射 6 | 7 | [] | [1..6] |
| 7 | 7 | 发射 7 | 8 | [] | [1..7] |
| 8 | 2 | 重复丢弃（2<next=8） | 8 | [] | [1..7] |

- (甲) 误写 `Seq<=next`：第 4 步 1<=1 被当重复丢弃，next 永停 1；缓冲满后第 5/6/7 步及第 8 步（2>1）全部背压拒绝。最终已发射错成 []，正确应为 [1,2,3,4,5,6,7]。
- (乙) 不做级联：第 4 步只发 1（正确应发 1,2）；2 滞留缓冲（next 已为 2 仍不放行），第 5 步 3 使缓冲超限被拒；第 8 步重达到的 2 触发发射值 2，但第 2 步缓冲的那份拷贝永不释放，3 已被拒。最终错成 [1,2]，正确 [1..7]。
- (丙) seq==next 时把缓冲全部按升序放行：第 4 步发 1,2,4,5，越过 3；第 5 步 3<next(=6) 被当重复丢弃，3 永久丢失。最终错成 [1,2,4,5,6,7]（有空洞），正确 [1..7]。

## 二、四条不变量的代码位置与钉住测试
1. 顺序保证（严格递增、步长 1、无空洞、至多一次）：`fifo/fifo.go` Feed 的级联循环仅在队首 `buf[head]==next` 时放行，否则立即停；重复 `seq<next` 不发射。钉：`TestFeedScenarios`、`TestConcurrentDistinctKeys`。
2. 与朴素参照一致（已发射==被接受序号的最长连续前缀排序；丢弃数==seq<next 次数）：`fifo/fifo.go` Feed 三分支 + `dropped` 仅在 seq<next 自增；`api` SelfCheck/测试用接受集合模型比对。钉：`TestOrderingMatchesNaive`、`TestSelfCheck`。
3. 背压上界（缓冲≤maxInFlight，超限整体拒绝不留痕）：`fifo/fifo.go` Feed 在插入前按活动长度 `len(head:)+1>max` 判错，先判错后改状态。钉：`TestBackpressureBound`、`TestFeedScenarios`。
4. 失败不留痕（空 key、非正 max、背压拒绝不改任何状态）：`api/api.go` New/Feed 先校验再动 dispatch；fifo 背压分支在任何写入前返回；三类哨兵错误互不相同。钉：`TestErrorsLeaveNoTrace`、`TestSelfCheck`。
