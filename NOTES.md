# NOTES — 分块矩阵乘法 (n=5, b=2)

A[3][k]=16+k；B[k][2]=k-2。k 维块：[0,2)、[2,4)、[4,5)（尾块，大小 1=5%2）

| k | 所属 k 块 | A[3][k] | B[k][2] | 本项乘积 | 部分和 |
|---|---|---|---|---|---|
| 0 | [0,2) | 16 | -2 | -32 | -32 |
| 1 | [0,2) | 17 | -1 | -17 | -49 |
| 2 | [2,4) | 18 | 0 | 0 | -49 |
| 3 | [2,4) | 19 | 1 | 19 | -30 |
| 4 | [4,5) 尾块 | 20 | 2 | 40 | 10 |

正确 C[3][2] = 10。

- (甲) 只处理完整块、跳过尾块 [4,5)：丢失 k=4 的项 40，结果错成 **-30**（正确 10）。
- (乙) 块内上界写成 `<=`：k=2 的项 (0) 与 k=4 的项 (40) 被左右两块各重复一次，结果错成 **50**。
- (丙) 误读成 B[j][k]，即 B[2][k]=2-k：16*2+17*1+18*0+19*(-1)+20*(-2) = 32+17+0-19-40，结果错成 **-10**。

## 四条不变量：保证位置 / 钉住的测试函数

1. 与朴素逐位相等：`mul/mul.go` 的 `Blocked`（块序 i块→k块→j块，块内 i→k→j，固定 (i,j) 的 k 严格 0..n-1 升序）；`TestBlockedMatchesNaive`（math.Float64bits 逐位比对，多档 n/b 含尾块与负数）。
2. 块覆盖完备：`blk/blk.go` 的 `Parts`（闭式 start=q*b, end=min(start+b,n)）；`TestPartsCoverage`（断言无重叠无缝隙、尾块大小=n%b）。
3. 位级稳定：`mul/mul.go` 为纯函数，无 map 遍历、无并发写、无随机；`TestBlockedDeterministic`（同输入重复调用结果 bits 全同）。
4. 失败不留痕：`api/api.go` 的 `validate` 在任何计算/`TailBlock` 调用之前整体校验，拒绝时不触碰任何计数器；`TestRejectionLeavesNoTrace`（拒绝后引擎结果仍与基线一致）。

O(1) 尾块定位：`blk/blk.go` 包级非导出原子计数器 `lookupEntries`，`TailBlock` 闭式求解、每次置 0；白盒 `TestTailLookupO1`（n=100/1000/10000, b=17 恒为 0）。
并发只读一致：`api/api.go` 的 `Mul` 对入站切片只读；`TestMulConcurrentEqual`（WaitGroup 并发，结果逐字节相同）。
