# NOTES — 幂迭代求最大特征值

## 一、四行分步表（A=[[3,1],[1,3]]，v0=[1,0]；λ 取归一化后当前向量的 Rayleigh 商）

| 步 | v | w=A·v | max\|w\| | v'=w/max\|w\| | λ |
|---|---|---|---|---|---|
| 1 | [1, 0] | [3, 1] | 3 | [1, 1/3] | 36/10 = 18/5 = 3.6 |
| 2 | [1, 1/3] | [10/3, 2] | 10/3 | [1, 3/5] | 132/34 = 66/17 ≈ 3.882353 |
| 3 | [1, 3/5] | [18/5, 14/5] | 18/5 | [1, 7/9] | 516/130 = 258/65 ≈ 3.969231 |
| 4 | [1, 7/9] | [34/9, 10/3] | 34/9 | [1, 15/17] | 2052/514 = 1026/257 ≈ 3.992218 |

第 1 步 λ 计算：v'ᵀAv' = 4，v'ᵀv' = 10/9，λ = 4/(10/9) = 3.6。

## 二、三问（第 1 步，正确 λ=3.6）

- (甲) 忘除 vᵀv：λ = v'ᵀAv' = **4**。
- (乙) 分子分母颠倒：λ = (10/9)/4 = 10/36 = **5/18 ≈ 0.277778**。
- (丙) 直接取归一化因子：λ = max|w| = **3**。

## 三、四条不变量：保证位置与钉住测试

1. 与朴素参照一致（A·v==λ·v，每项残差≤1e-9，λ 模最大）：`eigen.Iterate` 每轮一次 w=A·v，λ=vᵀw/vᵀv，驱动 |Δλ|<tol；λ 以 ε² 收敛而 v 只以 ε 收敛，故收敛后由 `polish` 继续同一迭代规则至残差 ‖A·v−λv‖∞≤1e-9 才返回；测试 `api_test.go::TestResidualAgainstNaive`。
2. 归一化确定（max|vᵢ|≡1，符号保留，逐位一致）：`eigen.Iterate` 取首个绝对值最大分量的带符号值 m，v'=w/m，全程无随机；测试 `api_test.go::TestNormalizationDeterminism`。
3. 输入不被修改：`power.MatVec` 只写新分配切片，`eigen.Iterate` 对 a、v0 全程只读（v、w 均为新切片）；测试 `api_test.go::TestInputsUnmodified`。
4. 失败不留痕（含计数器）：`eigen.Iterate` 先完成全部校验再增原子计数器 `matVecCalls`，拒绝路径零触碰；测试 `eigen/eigen_test.go::TestCounterDeltaAndRejected` 与 `api_test.go::TestSentinelErrorsDistinct`。
