# 新鲜期选取规则（第三节推导）

## 结论：优先级

`s-maxage` > `max-age` > `Expires` > 三者皆无（新鲜期 = 0）

## 推导

1. 必须确定且不依赖 map 遍历顺序：三个来源按固定线性优先级取第一个存在者，
   绝不并列、绝不随机。
2. 本缓存是**共享缓存**（多用户共用），这决定了 `s-maxage` 排第一：
   - `s-maxage` 的语义就是「仅对共享缓存生效的新鲜期」；私有缓存必须忽略它，
         共享缓存则以它为准。
   - 当 `s-maxage` 存在时，共享缓存**忽略** `max-age`，即使 `max-age=60` 与
     `s-maxage=600` 矛盾，也取 600。
3. 无 `s-maxage` 时取 `max-age`：`Cache-Control` 是 HTTP/1.1 指令，显式覆盖
   HTTP/1.0 遗留的 `Expires`，故二者同在时以 `max-age` 为准，`Expires` 被忽略。
4. 只有 `Expires` 时：新鲜期 = `Expires - tDate`，负值截 0。
   用 `tDate`（响应生成时刻）而非到达时刻，才能让「绝对过期时刻」换算出的
   新鲜期与基于年龄的公式口径一致。
5. 三者皆无：新鲜期 = 0，立即过期；规范禁止在没有显式新鲜度信息时
   （如无 `Last-Modified` 依据）做启发式猜测，本题也明确不许猜。
