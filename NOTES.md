# NOTES：DAG 最小路径覆盖推导

n=6，边：0→1、0→2、2→1、3→4、4→5。二分图边 L0-R1、L0-R2、L2-R1、L3-R4、L4-R5。
最大匹配 M=4：L0→R2、L2→R1、L3→R4、L4→R5。

| 节点 | 路径前驱（匹配入边） | 路径后继（匹配出边） | 角色 |
| --- | --- | --- | --- |
| 0 | 无 | 2 | 起点 |
| 1 | 2 | 无 | 终点 |
| 2 | 0 | 1 | 中间 |
| 3 | 无 | 4 | 起点 |
| 4 | 3 | 5 | 中间 |
| 5 | 4 | 无 | 终点 |

结论：M=4，覆盖数 6−4=2，路径 [0,2,1]、[3,4,5]；无孤立节点。
(甲) 每条匹配边各当一条路径、不合并：得 4 条（顶点 2、4 还被两条路径共享，违反顶点不相交）；正确应为 2 条。
(乙) 贪心（左节点按编号顺序、取编号最小未占右节点、不回溯）：L0 先占 R1，L2 唯一候选 R1 被占即失败 → M=3、覆盖数 6−3=3；正确应为 M=4、覆盖数 2。
(丙) 加孤立点 6（n=7，无边）：M=4 不变，覆盖数 7−4=3。若起点误取「无前驱且有后继」，孤立点 6 既无前驱也无后继被漏掉，只返回 2 条 [0,2,1]、[3,4,5]；正确应为 3 条。

## 四条不变量的保证位置与钉住的测试

1. 覆盖合法：mpc/mpc.go 的 buildPaths 只从 matchR==−1 的起点沿 matchL 走到 −1，链上每边都是登记边，「无前驱起点 + 唯一后继」划分保证每点恰出现一次；钉于 TestCoverLegality（随机 DAG 表驱动：真链、不相交、并集为全部 n 点、len(paths)==cover）。
2. 覆盖最小：mpc.maximumMatching 用 Kuhn 增广路算法求二分图最大匹配，Cover 返回 n−|M|；钉于 TestCoverMinimal（n≤7 枚举全部排列 ×2^(n−1) 切点，独立求出最小链划分数，逐图对比）。
3. 与朴素参照一致：cover 在 mpc.Cover 中直接由 n 减去匹配数得出；钉于 TestCoverMatchesFormula（测试内独立枚举所有匹配求最大 |M|，逐值核对 cover==n−|M|）。
4. 失败不留痕：dag/dag.go 的 AddEdge 先依次做范围、自环、重复校验，全部通过后才写 edges 与邻接表；钉于 TestRejectedOpsLeaveNoTrace（每类拒收后 EdgeCount 不变、Solve 结果不变）。

环错误由 dag.HasCycle（Kahn 入度法）在 mpc.Cover 入口判定，钉于 TestSentinelErrors；五个哨兵错误互异；probes 为 mpc 非导出字段、不经公开接口读出，O(1) 判定钉于 TestProbeCountConstant。
