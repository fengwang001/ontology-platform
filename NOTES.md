# NOTES — 多副本反熵读修复

## 八步推导（key="k"，副本 R0/R1/R2，条目写作 value@ver，空=∅）

| # | 操作 | R0 | R1 | R2 | winner | 回填副本 | repaired |
|---|------|----|----|----|--------|----------|----------|
| 1 | Put(k,"a",5,{0,1}) | a@5 | a@5 | ∅ | —（无读） | — | — |
| 2 | Read(k) | a@5 | a@5 | a@5 | a@5 | R2（空→(a,5)） | 1 |
| 3 | Put(k,"b",7,{1,2}) | a@5 | b@7 | b@7 | — | — | — |
| 4 | Put(k,"c",7,{2}) | a@5 | b@7 | c@7 | — | — | — |
| 5 | Read(k) | c@7 | c@7 | c@7 | c@7（7 并列，"c">"b"） | R0（落后）、R1（同版冲突） | 2 |
| 6 | Put(k,"d",4,{0}) | d@4 | c@7 | c@7 | — | — | — |
| 7 | Read(k) | c@7 | c@7 | c@7 | c@7 | R0（晚到旧写被纠正） | 1 |
| 8 | Read(k) | c@7 | c@7 | c@7 | c@7 | 无（已收敛） | 0 |

- (甲) 漏回填空副本：第 2 步 repaired 错成 **0**，R2 错留 **∅**（应为 (a,5)）。
- (乙) 严格 `>` 并列保留下标最小者：winner 错成 **"b"**；R0 被回填成 **(b,7)**，R2 的 (c,7) 被判冲突也回填成 **(b,7)**（正确应为 R0、R1 回填成 (c,7)）。
- (丙) 按到达时间判定：第 7 步错返回 **"d"**，并把 R1、R2 回填成 **(d,4)**，版本从 7 **降到 4**（违反只升不降）。

## 四条不变量：保证位置与钉住它的测试

1. 与朴素参照一致：winner 由 `rrep` 按 (ver,value) 大根堆定位（与 `rep.Winner` 朴素扫描同序），回填在 `rrep.Store.Read` 末尾统一执行 → `TestNaiveConsistency`、`TestEightStepTrace`。
2. 修复收敛：回填把空/落后/冲突副本一律改写为 winner（`rrep.Store.Read` 的修复循环）→ `TestEightStepTrace`（第 8 步 repaired==0）。
3. 只升不降：回填条件 `!Ok || Ver<w.Ver || (Ver==w.Ver && Value!=w.Value)` 只升或持平；winner 版本==读前最大版本由堆顶有效性保证 → `TestNoDowngrade`。
4. 失败不留痕：`api` 先做全部参数校验再调用 `rrep`，校验失败直接返回哨兵错误 → `TestRejections`（四类错误互不相同、状态不变、可继续用）。
