# DRR 笔记

## 赤字在 flow 变空时的处理

flow 被发空并离开活动轮转表时，剩余赤字必须清零，不能保留。

赤字只补偿“本轮已经轮到但尚未用完”的排队机会。flow 空闲期间不在活动表中，未参与轮转，也不应累积或携带轮次信用。若发空时保留 `r` 字节赤字，该 flow 之后在第一轮会先拥有旧 `r`，再得到 quantum `q`；由于最后一个数据块可造成至多 `MaxBlock-1` 字节的整包超额，它第一轮最多可发 `r+q+MaxBlock-1`。

正确实现中返回 flow 的初始赤字为 0，因此第一轮服务量上界是 `q+MaxBlock-1`，与空闲时长和其他 flow 在这期间服务了多少轮无关。

两个持续积压 flow 的任意已发送字节差满足 `|S_a-S_b| <= max(q_a,q_b)+MaxBlock-1`：任一时刻至多一个 flow 多拿一个 quantum，而双方停留状态的赤字均小于队首块大小。

## 语义与测试位置

待实现后补充。
