# 新鲜期（freshness lifetime）选取规则

候选来源有三个，按以下**确定次序**取第一个存在者，与 map 遍历顺序无关：

1. 响应指令 `s-maxage`（秒数）
2. 响应指令 `max-age`（秒数）
3. `Expires - tDate`（绝对时刻换算成相对新鲜期，负数截 0）
4. 三者都不存在 ⇒ 新鲜期为 **0**（立刻过期，不做任何启发式猜测）

## 为什么是这个次序

本题缓存是**共享缓存**（多个用户共用一份响应）。

- `s-maxage` 的 `s-` 即 shared（共享）：它存在时**只**对共享缓存生效，
  并显式覆盖 `max-age`，因此共享缓存必须最先采用它；它与 `max-age`
  矛盾时，以 `s-maxage` 为准。这正是「共享缓存」这一身份改变优先级的地方
  （私有缓存不看 `s-maxage`，会直接用 `max-age`）。
- 其次用 `max-age`：它是相对秒数，比 `Expires` 的绝对时刻更明确，
  且规范规定 `max-age` 出现即覆盖 `Expires`，故 `Expires` 排第三。
- `Expires` 是绝对时刻，时钟基准可能不同，只能退化为
  `Expires - tDate` 的相对新鲜期使用；差值为负截 0。
- 三者皆无时禁止启发式取值（如 LM-factor），新鲜期即 0，
  要求立刻再验证，行为保守且确定。
