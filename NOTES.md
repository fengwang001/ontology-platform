# Union-Find 推导笔记

## 为什么朴素合并会退化成链

Union 总把 y 的根挂到 x 的根下、不做任何平衡时，依次 Union(0,1), Union(1,2),
..., Union(n-2,n-1) 会得到 0<-1<-...<-n-1 的单链；对末端 Find 的跳数为 O(n)。

## 按秩合并：树高 <= log2(n)

每个根维护秩 rank（树高上界），合并时把秩小的根挂到秩大的根下；秩相等才任选其一
并把新根秩 +1。节点深度增加当且仅当它所在树的根秩增加，而秩 r 的树至少含 2^r 个
节点（归纳：r=0 时 1 个；两棵秩 r-1 的树合并才产生秩 r，含 2*2^(r-1)=2^r 个）。
故 rank <= log2(n)，Find 跳数（跟随父指针到根的次数）<= log2(n)。

## 路径压缩与均摊复杂度

Find 时把沿途节点直接指向根，后续查找近乎 O(1)。路径压缩不增加秩，秩只增不减，
继续充当高度上界。按秩合并 + 路径压缩的经典均摊分析给出每次操作
O(alpha(n))（反 Ackermann 函数，实际 <=5，即均摊接近常数）；工程上配合
跳数 <= 2*log2(n)+2 的有限界做钉测。

## 语义与测试对照（测试写完后回填）

| 语义 | 实现位置 | 测试函数 |
| --- | --- | --- |
