# NOTES：session window 增量维护推导

gap=10，到达序 100,105,130,135,118,100，`[s,e]×n` 为闭区间与计数：

| 步 | 事件 | 该 Key 的完整会话序列 |
|---|---|---|
| 1 | 100 | `[100,100]×1` |
| 2 | 105 | `[100,105]×2`（105−100=5≤10） |
| 3 | 130 | `[100,105]×2,[130,130]×1`（130−105=25>10） |
| 4 | 135 | `[100,105]×2,[130,135]×2` |
| 5 | 118 | `[100,105]×2,[118,118]×1,[130,135]×2` |
| 6 | 100 | `[100,105]×3,[118,118]×1,[130,135]×2` |

(甲) 步5 前：118 距前段 end=105 为 **13**、距后段 start=130 为 **12**，都 >10，谁也够不着，唯一合法操作是在两团中间**独立插入一团**。一般规则：按 TS 二分定位后向两侧圈「距离 ≤gap」的归并区间——够 0 团插入、够 1 团扩边、够 2 团三团合一（gap=15 时 118 双侧够得着：13、12 均≤15，六事件合成 `[100,135]×6`）。
朴素「并进最近会话（12<13 选后团）再扩边」第 5 步得 `[100,105]×2,[118,135]×3`，而排序重算为三团——直接违反**不变量 1**；它把相差 12>gap 的 118/130 锁进同一团，踩的是**不变量 3**「任意两个相邻会话的间隔严格大于 gap（否则它们本应是同一个会话）」的逆否：相差 >gap 必须分属不同会话。同一朴素算法在 gap=15 下结果两团相邻间隔 118−105=13≤15，构成该句的字面违反。
(乙) 步6 再来完全相同的 TS=100：start=100、end=105 都不动，仅计数 2→3。重复时间戳是多重集元素，「加不加计数」与「边界移不移」是两件独立的事。
反例（只与上一个**到达**事件比较的朴素实现）：到达序 `100,130,105`——105 与上一个到达的 130 差 25>10 被误开新团，得 3 团；排序重算 100,105,130 只有 2 团。故不变量 2 禁止依赖到达序：本实现只按 TS 的二分位置与区间谓词决定归并。

## 四条不变量：代码保证位置 / 钉住的测试函数

1. 与全量重算一致：`sess.addInto` 的归并区间合并（够得着的相邻团全部吃掉，`sess/sess.go`）；`sess.TestRecomputeEquivalence`（oracle 排序重算，逐会话断言）。
2. 顺序无关：`Add/AddAll` 只用 TS 二分结果与 `evt.Mergeable` 谓词、不读任何到达历史；`sess.TestRecomputeEquivalence`（每规格 8 轮 shuffle）与 `sess.TestSixSteps`。
3. 规范形（不重叠 / 相邻间隔严格 >gap / start 严格升序）：归并区间构造与切片重排；`sess.TestCanonicalForm`（20 组随机 gap 与内容逐条断言）。
4. 失败不留痕：`addInto` 先做完决策才改切片、`AddAll` 在 shadow 副本上模拟全部成功才整体提交、`api.Feed` 透传；`sess.TestRejectedLeavesNoTrace`、`api.TestNewBadGap`、`api.TestFeedAtomicity`。

复杂度：`sess.Set.lastCmp` 为非导出字段，仅计最近一次 Add 在归并区间两侧探测的会话数；`sess.TestCompareBudget`（m=100/1000/10000，断言 ≤ 归并数+2，不随 m 增长）。并发：`api.Window` 用 `sync.RWMutex`，Snapshot/SelfCheck 走 RLock；`api.TestConcurrentReaders`（32 goroutine、无 sleep、-race 干净）。
