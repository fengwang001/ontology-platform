# NOTES

## 八步推导（size=10 delay=3 lateness=5 early=2，同一 Key=K）
|步|事件|wm|窗口/归类|本步变更日志|之后计数|
|---|---|---|---|---|---|
|1|TS=2|-1|[0,10) 正常|无|[0,10)=1|
|2|TS=5|2|[0,10) 正常|+(K,[0,10),2) early|[0,10)=2|
|3|TS=9|6|[0,10) 正常|无|[0,10)=3|
|4|TS=15|12|本事件入[10,20)正常；水位推进使[0,10) on-time|+(K,[0,10),3)|[0,10)=3(fired),[10,20)=1|
|5|TS=4|12|[0,10) 迟到接受(wm<15)|-(K,[0,10),3) +(K,[0,10),4)|[0,10)=4|
|6|TS=12|12|[10,20) 正常|+(K,[10,20),2) early|[10,20)=2|
|7|TS=7|12|[0,10) 迟到接受(wm<15)|-(K,[0,10),4) +(K,[0,10),5)|[0,10)=5|
|8|TS=20|17|本事件入[20,30)正常(计1无快照)；wm≥15，[0,10)静默purge|无|[10,20)=2,[20,30)=1|

Flush 再输出 +(K,[10,20),2)、+(K,[20,30),1)；最终视图 [0,10)=5、[10,20)=2、[20,30)=1，丢弃数=0。

- (甲) early 若清零：第 4 步 on-time 错成 **1**（正确 3）；迟到修正后最终视图错成 **3**（正确 5）。
- (乙) 不记 fired 标记：第 5 步重复 on-time，多输出 **+(K,[0,10),3)**；违反**不变量 3**（on-time 每窗口至多一次）。
- (丙) on-time 即 purge：第 5、7 步状态已不存在，迟到事件均**丢弃**；最终视图 [0,10) 错成 **3**（正确 5），丢弃数错成 **2**（正确 0）。

## 四条不变量：保证位置 / 钉住的测试
1. 与批量重算一致：`wagg.go` 的 `advance`/`feedOne` 接受判定 + `mat` 增量物化；测试 `api/..._test.go::TestInvariants`。
2. 变更日志前缀自洽：`wagg.go::feedOne` late 固定先发旧值 `-cur` 再发 `+new`，`emit` 只追加；测试 `TestInvariants`。
3. on-time 至多一次 / wm 只进不退：`wagg.go` 的 `entry.fired`（`advance` 与 `feedOne`）与只增的 `maxTS`；测试 `TestTriggerSemantics`。
4. 失败不留痕：`wagg.go::Agg.Feed` 的 txn 快照 + 反向回滚（日志截断后重放重建 mat）；测试 `TestFeedRejectsAtomically`。
