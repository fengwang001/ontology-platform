# NOTES

## 三、推导：a="abc", b="yabd", k=2，完整 DP 表（行首为该行的 a[i-1]）

| i | a[i-1] | dp[i][0] | [1] | [2] | [3] | [4] |
|---|--------|----|----|----|----|----|
| 0 | —   | 0 | 1 | 2 | 3 | 4 |
| 1 | 'a' | 1 | 1 | 1 | 2 | 3 |
| 2 | 'b' | 2 | 2 | 2 | 1 | 2 |
| 3 | 'c' | 3 | 3 | 3 | 2 | 2 |

示例：`dp[1][1]`：'a'≠'y'，`min(dp[0][1]+1, dp[1][0]+1, dp[0][0]+1)=1`；`dp[3][4]`：'c'≠'d'，`min(dp[2][4]+1, dp[3][3]+1, dp[2][3]+1)=min(3,3,2)=2`。

- **(甲)** 替换代价错为 2：重算末行 `3 4 3 2 3`，得 `dp[3][4]=`**3**（正确 2；插入 'y' 1 步 + 替换被算成 2 步）。
- **(乙)** 边界错初始化为全 0：末行 `0 1 2 1 1`，得 `dp[3][4]=`**1**（正确 2；空前缀代价被白送，插入 'y' 没付钱）。
- **(丙)** 错读 `dp[n-1][m-1]=dp[2][3]`：读到 **1**（正确 2；那是 "ab"→"yab" 的距离，漏了末尾 'c'→'d' 的替换）。

## 七、四条不变量：保证位置 → 钉住它的测试

1. **与朴素一致**：`dist/dist.go` 的 band DP 只把 `|i-j|>k` 的单元置 inf（不影响 ≤k 的结果）→ `dist.TestDistanceVsNaive`（300+ 组输入与全表 DP 逐对对照）。
2. **脚本有效**：`ops/ops.go` 从 band 表回溯最小脚本，`ops.Apply` 可校验应用 → `api_test.TestEditScriptValid`（脚本长度=距离且应用后 a 变 b）。
3. **上限正确**：`dist.go` 末尾 `prev[m]>k` 才报 `ErrExceedsCap`，`|n-m|>k` 提前报 → `dist.TestCapBoundary`（k 从 0 到 d+1 逐档验证当且仅当）。
4. **失败不留痕**：`api.Calculator` 创建后不可变、每次调用工作区独立 → `api_test.TestFaultInjection`（三类错误互不相同，拒绝前后结果相同且 SelfCheck 复过）。
