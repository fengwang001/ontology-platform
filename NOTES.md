# NOTES — 支配树推导与不变量

## 第三节推导（n=7，边 0→1 0→6 1→2 1→6 2→3 2→4 3→5 4→5）

支配集：dom(0)={0} dom(1)={0,1} dom(2)={0,1,2} dom(3)={0,1,2,3} dom(4)={0,1,2,4} dom(5)={0,1,2,5} dom(6)={0,6}

| v | 直接前驱 | 前驱支配集的交集 | idom |
|---|---------|-----------------|------|
| 0 | —（入口） | — | 0 |
| 1 | {0} | {0} | 0 |
| 2 | {1} | {0,1} | 1 |
| 3 | {2} | {0,1,2} | 2 |
| 4 | {2} | {0,1,2} | 2 |
| 5 | {3,4} | {0,1,2,3}∩{0,1,2,4}={0,1,2} | 2 |
| 6 | {0,1} | {0}∩{0,1}={0} | 0 |

- (甲) DFS 从 0、邻接按编号升序：访问序 0→1→2→3→5→4→6，parent(6)=1，`idom(6)` 错成 **1**（正确 **0**）。DFS 父节点只记录「发现它的那一条路径」，但 0→6 直达边表明存在不经过 1 的路径；支配要求每条路径都经过，故父节点不一定是支配者。
- (乙) 只取第一个直接前驱（登记序 3→5 先于 4→5）：交集退化为 dom(3)，`idom(5)` 错成 **3**（正确 **2**），漏掉前驱 4 把「0→1→2→4→5」这条不经过 3 的路径丢了。
- (丙) `IDom(7)` 应返回 **-1**，`Dominates(0,7)` 应返回 **false**。若把 idom(7) 错设为 0，`Dominates(0,7)` 会误判 true——入口「支配」一个不可达节点，违反「b 不可达恒 false」，且 `Dominates(7,7)` 也会错成 true。

## 四条不变量落点

1. Dominates 定义正确：`dom/dom.go` 用支配树 DFS 进出区间 O(1) 判定；测试 `TestDominatesBruteForce`（删点可达性参照）。
2. idom 是最近支配者：`dom/dom.go` Compute（Cooper 交点算法）；测试 `TestIDomIsImmediate`。
3. 与朴素定点一致：测试 `TestIDomMatchesNaive`（随机图逐节点对比朴素交集迭代）。
4. 失败不留痕：`dgraph/dgraph.go` AddEdge 先全部校验通过才改状态；测试 `TestRejectedOpsKeepState`。
