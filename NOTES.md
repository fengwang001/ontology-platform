# 最长配平子串：栈的推导（以 `")()())"` 为例）

- 甲：扫描前栈预置基准 `-1`（串首之前的虚拟边界）。
- 乙：遇到无法匹配的 `)`，把它的下标**压栈**，它充当后续段的新基准；清空栈会丢掉基准。

做法一（预置 -1 + 不匹配时压入），长度公式 `i - top`：

| i | 字符 | 动作 | 栈（底→顶） | 当前最长 |
|---|------|------|-------------|----------|
| -1 | - | 预置基准 | `[-1]` | 0 |
| 0 | `)` | 无法匹配，压入 0 | `[-1,0]` | 0 |
| 1 | `(` | 压入 1 | `[-1,0,1]` | 0 |
| 2 | `)` | 弹出 1，`2-0=2` | `[-1,0]` | 2 |
| 3 | `(` | 压入 3 | `[-1,0,3]` | 2 |
| 4 | `)` | 弹出 3，`4-0=4` | `[-1,0]` | 4 |
| 5 | `)` | 无法匹配，压入 5 | `[-1,0,5]` | 4 |

结果：start=1，length=4（子串 `"()()"`）。

做法二（不预置 + 不匹配时清空，长度仍取 `i - top`）：

| i | 字符 | 动作 | 栈 | 当前最长 |
|---|------|------|----|----------|
| 0 | `)` | 清空 | `[]` | 0 |
| 1 | `(` | 压入 | `[1]` | 0 |
| 2 | `)` | 弹出后栈空，**无 top 可算** | `[]` | 0（本应为 2） |
| 3 | `(` | 压入 | `[3]` | 0 |
| 4 | `)` | 弹出后栈空，**无 top 可算** | `[]` | 0（本应为 4） |
| 5 | `)` | 清空 | `[]` | 0 |

做法二最终得 0，算不出 4：每次把配对的最后一个 `(` 弹尽后，栈中没有基准下标，长度无从计算；若改用 `i+1` 兜底，等于暗中重建一个基准，已不再是"清空"。基准取 `-1` 是因为它是"串首之前那一个假想的坏右括号"，于是从头有效的串（如 `"()()"`）也满足同一公式 `i - top`；真实的坏右括号被压入后，就成为后续有效段的新基准，基准永不丢失。

## 不变量在代码中的保证位置与钉住的测试

1. 配平判定正确：`balance.Checker.Check`（balance/balance.go）对 `scan` 事件单遍压栈/弹栈，右括号不匹配立即返回其下标，结束取栈底为首个未闭合 `(`；测试 `TestIsBalancedInvariant`。
2. 子串有效且最长：长度公式 `i - top` 保证有效（longest/longest.go 的 `Solver.Solve`），`Solver.SelfCheck` 复用 balance 复核子串，朴素枚举对照见 `TestLongestNaiveEquivalence`。
3. 并列最长取最左：仅当 `got > best`（严格大于）才更新起点，longest/longest.go 的 `Solver.Solve`；测试 `TestLeftmostTie`。
4. 失败不留痕：长度/非法字符由 `scan.Scanner.Events` 先判，出错 `Solve` 返回零值 `Result`，`Solver` 自身无字段状态；测试 `TestRejectedOperationsClean`。
