# 文件名定名规则

1. `filename*` 优先于 `filename`：扩展形式显式携带字符集，信息更精确；为空时再用普通形式。
2. 选择结果先做百分号归一化：普通 `filename` 中的 `%2e` 等按字节解码，避免用编码伪装路径片段；扩展值已在参数解码阶段完成这一步。
3. 去除首尾 Unicode 空白；随后只检查整体basename，不把名字拆成路径。
4. 若百分号字节不是合法 UTF-8，把该非法序列逐字节编码为 `%XX`，保证信息不丢且不产生半个字符。
5. 对实际危险字符做可逆百分号编码：`/`、`\`、NUL 与其他控制字符成为 `%XX`；字面 `%` 成为 `%25`。
6. 归一化后若整体为 `.` 或 `..`，或变为空串，使用确定兜底名 `_file`。
7. 确定性：只查固定的两个键，先 `filename*` 后 `filename`；不遍历 map 选名。
8. 信息保留：危险字符不是删除或统一替换成 `_`，而是带标记的可逆 `%XX` 编码，所以 `a/b`、`a%2Fb` 归一后相同（二者只在危险分隔符的编码表示上不同），但不会与 `a_b` 相撞。
9. 首尾空白是明确要剔除的部分；仅因此不同的名字允许相同。
10. `filename="%2e%2e"` 归一化为 `..`，与字面 `..` 是同一危险路径片段；两者整体都被替换成 `_file`，符合“只在需剔除部分上不同”的例外。
