# NOTES

## 第三节推导（pageSize=4，V0=Alloc([0,0,0,0]) → 页 A, ref=1）

| 步 | 操作 | A (ref/内容) | B | C | 本步结果 |
|---|---|---|---|---|---|
| 1 | Snapshot(V0)→V1 | 2/[0,0,0,0] | – | – | 共享，A+1 |
| 2 | Snapshot(V0)→V2 | 3/[0,0,0,0] | – | – | 共享，A+1 |
| 3 | Write(V1,0,9) | 2/[0,0,0,0] | 1/[9,0,0,0] | – | A 被共享→拷贝出 B，A−1，V1→B，写 B[0]=9 |
| 4 | Write(V0,0,5) | 1/[0,0,0,0] | 1/[9,0,0,0] | 1/[5,0,0,0] | A 仍被共享→拷贝出 C，A−1，V0→C，写 C[0]=5 |
| 5 | Release(V2) | 释放 | 1/[9,0,0,0] | 1/[5,0,0,0] | A 减到 0，释放 |
| 6 | Write(V0,1,7) | – | 1/[9,0,0,0] | 1/[5,7,0,0] | C 独占→原地写 C[1]=7 |
| 7 | Snapshot(V0)→V3 | – | 1/[9,0,0,0] | 2/[5,7,0,0] | 共享，C+1 |
| 8 | Read(V1,0)=9；Read(V0,0)=5 | – | 1/[9,0,0,0] | 2/[5,7,0,0] | 读各自私有页 |

- **(甲)** 第 3 步若原地写：A 变 [9,0,0,0]；Read(V0,0) 与 Read(V2,0) 都错误返回 9（正确应为 0）。
- **(乙)** 拷贝忘减旧页：第 3 步后 A 的 ref=3（正确 2）。第 4 步拷贝后 A=2，第 5 步 Release(V2) 后 A=1，永远到不了 0，A 泄漏。
- **(丙)** Release 无重复检测：第二次 Release(V1) 把已归零释放的 B 再减一次（ref 变 −1）；若 B 的槽位已被新页复用，会把无辜新页误 free，造成 use-after-free 与计数守恒崩坏。

## 第二节四条不变量及其保证位置与钉住测试

1. **引用计数守恒**：仅在 `mgmt` 的 Alloc/Snapshot/Write/Release 四处改计数，每处增减与 view 生灭一一对应；测试 `TestNaiveEquivalence`（逐 view 对拍计数 + 全释放后页数为 0）。
2. **与朴素参照一致**：`api.SelfCheck` 内置操作序列逐条对比朴素模拟；随机序列对拍在 `TestNaiveEquivalence`。
3. **写隔离**：`cow.Page` 写前 `Ref()>1` 必走 `Clone`；测试 `TestWriteIsolation`。
4. **失败不留痕**：所有校验（view 合法性、off 边界、len、maxPages）先于任何状态变更；测试 `TestFailureAtomicity`。
