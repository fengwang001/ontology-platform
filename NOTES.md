# 多项式求值 NOTES（Horner，int64 精确 + 溢出检出）

## 八行分步表（acc=0，自最高次起 acc=acc*x+coeff[i]）

1. `([1,2,3],2)`: 0*2+3=3; 3*2+2=8; 8*2+1=17 → **17**
2. `([5,-2,1],3)`: 0*3+1=1; 1*3+(-2)=1; 1*3+5=8 → **8**
3. `([0,0,1],10)`: 0*10+1=1; 1*10+0=10; 10*10+0=100 → **100**
4. `([1,1,1,1],-2)`: 0*-2+1=1; 1*-2+1=-1; -1*-2+1=3; 3*-2+1=-5 → **-5**
5. `([1,0,1],67108864)`: 0*x+1=1; 1*x+0=67108864; 67108864*x+1=2^52+1 → **4503599627370497**
6. `([],5)`: 无迭代 → **0**
7. `([1],999)`: 0*999+1=1 → **1**
8. `([0,3037000500],3037000500)`: 0*x+3037000500=3037000500; 3037000500*x=9223372037000250000 > MaxInt64=9223372036854775807 → **溢出（乘法）**

## 三问

- (甲) 正确结果 **4503599627370497**（=2^52+1）。题设预期的 float64 舍入答案：4503599627370496（丢 +1）。
  严格考据：2^52+1 < 2^53，仍在 float64 精确整数范围内，此输入其实不丢；首次丢 +1 发生在 2^53+1 → 9007199254740992（demo 用此点实测演示同一现象）。
- (乙) 应报**乘法溢出**（哨兵 ErrMulOverflow）。朴素 int64 不检溢出会静默回绕成 **-9223372036709301616**
  （=9223372037000250000−2^64）。判据：非零数的平方必为正，回绕值却是负数，且与 big 精确值 9223372037000250000 不符，故为错值。
- (丙) 系数读反（当成大端 [3,2,1]）：3 + 2*2 + 1*4 = **11**（正确值 17）。

## 四条不变量落点

1. 与朴素参照一致：`eval/eval.go` 的 Horner 循环与 big 逐项累加数学等价；测试 `TestEvalVsBigRef`（api/api_test.go）。
2. Horner≡逐项：同一循环，乘法分配律保证；测试 `TestHornerEquivTermwise`（api/api_test.go）。
3. 空/常数正确：`eval.Eval` 对 len==0 返回 0、len==1 不进入循环直接取 coeff[0]；测试 `TestEmptyZeroConstant`（api/api_test.go）。
4. 失败不留痕：`eval` 的 mul/add 检出溢出立即 `return 0, 哨兵错误`，不写任何状态；测试 `TestErrorsDistinctNoPartial`（api/api_test.go）。
