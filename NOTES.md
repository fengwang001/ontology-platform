# GK 分位数摘要 · 推导与不变量落实

## 六步推导（ε=0.25，2εn=0.5n；COMPRESS 条件 g_{i-1}+g_i+Δ_i ≤ 2εn）

| 步 | 操作 | 摘要 (v,g,Δ) |
|---|---|---|
| 1 | INSERT(10) | (10,1,0) |
| 2 | INSERT(20) | (10,1,0) (20,1,0) |
| 3 | INSERT(30) | (10,1,0) (20,1,0) (30,1,0) |
| 4 | INSERT(40) | (10,1,0) (20,1,0) (30,1,0) (40,1,0) |
| 5 | COMPRESS() | (10,2,0) (30,2,0) |
| 6 | INSERT(25) | (10,2,0) (25,1,1) (30,2,0) |

第 5 步细节（此时 n=4，2εn=2）：(30,1,0)+(40,1,0)：1+1+0=2≤2 合并为 (30,2,0)；(20,1,0)+(30,2,0)：1+2+0=3>2 不合；(10,1,0)+(20,1,0)：1+1+0=2≤2 合并为 (10,2,0)。

(甲) 落在 (10,2,0) 与 (30,2,0) 之间，Δ=g_i+Δ_i−1=2+0−1=1。若漏写 −1 则 Δ=2，g+Δ=1+2=3>2εn=2.5，违反带宽不变。
(乙) 若条件错成 <：三对之和都是 2，2<2 不成立，一个都不合并（仍 4 元组）；INSERT(25) 改落 (20,1,0)、(30,1,0) 之间，Δ=1+0−1=0（应为 1，错成 0）。
(丙) 第 6 步后 n=5，φn=3.5。rmax 序列 2,4,5：正确 QUERY(0.7)=25（首个 rmax≥3.5）。错用 rmin(2,3,5) 定位则返回 30。

## 四条不变量在代码中的落实

1. 结构（升序、无重复、Size=n）：gk/gk.go Insert 用 sort.Search 定位插入保持升序，seen 集合拒重，n 仅在插入成功时 ++。测试 api.TestStructure。
2. 带宽（g+Δ≤2εn）：gk.go Insert 中间元组 Δ=g_i+Δ_i−1、Compress 仅在和 ≤band 时合并；gk.BandOK 以 max(1,⌊2εn⌋) 校验（n<1/2ε 时 2εn<1≤g，取下界 1）。测试 api.TestBand。
3. 与朴素参照一致：quant/quant.go Query 取首个 rmax_i≥φn；测试 api.TestNaiveRef 对多档 m、升/降/随机序断言排名差 ≤εn。
4. 失败不留痕：所有校验先于任何变更（gk.Insert 先查 seen，quant.Query 先验 φ 与 n，api.New 先验 ε）。测试 api.TestRejectNoState。
