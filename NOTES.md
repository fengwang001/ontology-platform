# NOTES — 十进制字符串 → 精确有理数解析器

## 第三节推导：八行表（公式 I.F(R) = I + F/10^j + R/(10^j·(10^k−1))，结果既约）

| 输入 | 推导 | n/d |
|---|---|---|
| `3.14` | 314/100，约分 | 157/50 |
| `0.1` | 1/10 | 1/10 |
| `-2.5` | −25/10，约分 | −5/2 |
| `0.(3)` | 3/(10−1)=3/9，约分 | 1/3 |
| `0.1(6)` | (16−1)/(10·9)=15/90，约分 | 1/6 |
| `123` | 123/1 | 123/1 |
| `0.(142857)` | 142857/999999，约分 | 1/7 |
| `-0.0` | 0/10，零归一 | 0/1 |

**(甲)** `float64(0.1)` 的精确值是 `3602879701896397/2^55`——一个与 1/10 不相等的、巨大且未约分的假分数。原因：0.1 的二进制展开是无限循环的 0.0001100110011…，IEEE754 双精度只有 53 位尾数，必须舍入，故无法精确表示。「乘 10^k 取整」对 k=1 侥幸得到 1（舍入恰好弹回 1.0），纯属巧合，换 k 或换值即露馅。

**(乙)** 累加 `num = num*10 + 9` 到第 19 位时 `num = 9999999999999999999 > MaxInt64 = 9223372036854775807`，正确实现必须报可判定的 `ErrOverflow`；朴素 int64 会静默回绕成负数 `-8446744073709551617`，得到错误结果且毫无察觉。

**(丙)** 两者正确结果都应是 `0/1`。若不做「符号 + 零」归一，`-0.0` 的 `String()` 会输出非法形式 `-0/1`（`0.(0)` 若不归一还会输出 `0/9`）。

## 四条不变量：保证位置 + 钉住测试

1. **与朴素参照一致**：`api/api.go` 的 `refRat` 用 `big.Rat` 按 I.F(R) 公式建参照，`SelfCheck` 逐条比对；测试 `TestSelfCheck`、`TestParseAgainstBigRef`。
2. **既约且规范**：`conv/conv.go` 末尾 gcd 约分、保证 d>0、零一律归一 `0/1`；测试 `TestParseReducedNormalized`。
3. **有限与循环都精确**：`conv` 单趟整数累加 `num=num*10+d`（全程禁 float64），循环项 `num*(10^k−1)+R`；测试 `TestParseKnownValues`。
4. **失败不留痕**：`lex.Scan`/`conv.Convert` 为纯函数，`api.Parser` 无状态，错误一律哨兵错误；测试 `TestRejectedInputsLeaveNoTrace`。
