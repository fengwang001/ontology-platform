# Quickselect 推导

1. partition 后，pivot 的最终位置为 p，且左侧元素都小于 pivot，右侧元素都大于 pivot。
2. 排序后下标 k 的元素只可能有三种位置：p == k 时 pivot 就是第 k 小；p > k 时目标在左半区，仍按下标 k 查找。
3. p < k 时目标在右半区，仍按下标 k 查找；partition 只重排，不改变全局目标下标。
4. 不能比较 pivot 的值与 k：k 是下标，pivot 是元素值，二者量纲不同；同一值在不同数组中的排名也可能不同。
5. 平均随机 pivot 产生递归 C(n) = n + C(n/2) 量级，比较次数约 2n；测试断言 100000 个随机元素平均不超过 4n。
6. 语义位置与测试名：
