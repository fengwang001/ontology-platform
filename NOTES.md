# NOTES — Gauss-Jordan 推导：n=2，A=[[0,2],[1,1]]，增广 [A|I]

| 步 | 消元列 | 候选主元 | 选中行 | 交换 | 行变换后 [A | I] |
|---|---|---|---|---|---|
| 1 | — | — | — | — | [0 2\|1 0]; [1 1\|0 1]（初始） |
| 2 | k=0 | \|0\|=0，\|1\|=1 | 行1 | 交换行0,1 | [1 1\|0 1]; [0 2\|1 0] |
| 3 | k=0 | 主元=1 | 行1 | 否 | 行0÷1 归一化；下消元 m=A[1][0]/1=0，无操作：同步2 |
| 4 | k=1 | 候选仅 \|2\|=2 | 行1 | 否 | 行1÷2：[1 1\|0 1]; [0 1\|1/2 0] |
| 5 | k=1 | 主元=1 | 行1 | 否 | 上消元 行0-=1·行1：[1 0\|-1/2 1]; [0 1\|1/2 0] |

A⁻¹=[[-1/2,1],[1/2,0]]。核验 A·A⁻¹：行0=(0·-1/2+2·1/2, 0·1+2·0)=(1,0)；行1=(1·-1/2+1·1/2, 1·1+1·0)=(0,1) → I。

- (甲) 只消左半、不同步右半：右半始终是 I，A⁻¹ 错成单位阵 [[1,0],[0,1]]，**A⁻¹[0][1]=0**（应为 1）。
- (乙) 不选主元、拿 A[0][0]=0 归一化：**除零**——float64 下 1/0=+Inf、0/0=NaN，结果被 Inf/NaN 污染且不报错；若实现遇零对角线即判奇异，则是**误报奇异**（此阵可逆，本该交换行）。
- (丙) 漏上消元：终态右半为 [0 1; 1/2 0]，**A⁻¹[0][0]=0**（应为 -1/2）。

## 不变量：保证位置 / 钉住的测试

1. A·A⁻¹=I（逐项≤1e-9）：`gauss.Invert` 主循环对左右两半部同步做交换/归一化/乘减；钉于 api `TestInvProductIsIdentity`、`TestSelfCheck`。
2. 右半初始为精确单位阵、终态左半为 I：`gauss.go` 中 `r[i*n+i]=1` 初始化且每行变换两半部同做；钉于 gauss 白盒 `TestTwoByTwoMatchesDerivation`。
3. 输入逐字节不变：`gauss.Invert` 开头 `copy` 到新切片 `l`，原切片从不取地址写入；钉于 api `TestInputNotMutated`（含并发共享同一输入）。
4. 失败不留痕（含内部计数器）：空矩阵/维度错误在任何计算前返回；奇异时 `defer` 按本调用 did 原子回滚 `rowOps`；api 拒绝路径不碰任何计数器；钉于 gauss 白盒 `TestFailuresDoNotAdvanceCounter`、api `TestRejectedCallsDoNotChangeState`、`TestSelfCheck`。

附：对角阵 n=100/1000/10000 乘减恒 0 钉于 gauss 白盒 `TestDiagonalMatricesNeedNoRowOps`；并发逐字节一致钉于 api `TestConcurrentInvariance`（`-race`）。
