# 按分区并行、确定性归并的聚合 — 推导与不变量

## 第三节：六行分步表（maxPartitions=3，部分和按 key 升序）

| # | 记录 (P,K,V) | 归属 | P0 部分和 | P1 部分和 | P2 部分和 |
|---|--------------|------|-----------|-----------|-----------|
| 1 | (0,"a",5) | P0 | a=5 | — | — |
| 2 | (1,"a",1) | P1 | a=5 | a=1 | — |
| 3 | (2,"b",6) | P2 | a=5 | a=1 | b=6 |
| 4 | (0,"a",3) | P0 | a=8 | a=1 | b=6 |
| 5 | (1,"c",4) | P1 | a=8 | a=1,c=4 | b=6 |
| 6 | (2,"a",2) | P2 | a=8 | a=1,c=4 | a=2,b=6 |

- **(甲)** `Merge()` 后 **a=11**（8+1+2）、**b=6**、**c=4**。若同 key 跨分区「后写覆盖」且按 P0→P1→P2 合入，`a` 先被写 8、再被 1 覆盖、最后被 2 覆盖，错成 **a=2**。
- **(乙)** 按完成序 P2→P0→P1 直接拼接不重排：key 序列为 `a,b, a, a,c`，`a` 出现 **3 次**，依次为 **2、8、1**；下游取第一个 `a` 得 **2**（应为 11）。
- **(丙)** 求和既交换又结合：六个数按任何分组、任何顺序相加都是 11，所以与完成顺序无关。反例「后完成者覆盖」（非交换）：完成序 P0→P1→P2 时 `a`=P2 的 **2**；完成序 P2→P1→P0 时 `a`=P0 的 **8**——同一批记录两个结果，确定性被破坏。故归并必须用交换且结合的求和，并按 key 全序输出，才能保证逐字节相同。

## 第二节：四条不变量 → 代码位置 → 钉住它的测试

1. **与批量重算一致**：`merge.Merge` 最小堆多路归并、同 key 求和（merge/merge.go）；测试 `TestMergeEqualsBatchRecompute`。
2. **全序且与完成顺序无关**：`pagg.Sorted` 排序 + 堆按 key 弹出，与输入分区顺序无关；测试 `TestMergeDeterministicAcrossOrders`。
3. **每记录恰计一次**：`api.Add` 校验通过才恰好写一次 `pagg.Add`，归并只求和不覆盖；测试 `TestEachRecordCountedOnce`。
4. **失败不留痕**：`api.Add` 先校验后写，三个互不相同的哨兵错误；测试 `TestRejectedAddLeavesNoTrace`。
