# 可信前缀裁剪：推导笔记

## 裁剪方向：只能从近端向远端

信任只能从「与本机直连、确实由我配置的可信代理发出」这一跳开始建立。
近端可信，才相信它所报告的再外一层地址，逐跳外推。

反例（为什么不能从远端找「第一个不可信」）：
链路 `9.9.9.9, 1.2.3.4, 10.0.0.1`，可信清单 `10.0.0.0/8`。
真实情况是：可信代理 `10.0.0.1` 转发真实客户端 `1.2.3.4`，
而攻击者在最左端塞入伪造的 `9.9.9.9`。
从近端裁：`10.0.0.1` 可信 → 外一层 `1.2.3.4` 不可信，停止 ⇒ 客户端 `1.2.3.4`（正确）。
从远端找第一个不可信：`9.9.9.9` 立刻「命中」⇒ 客户端 `9.9.9.9`，
攻击者只用塞一条假记录就顶掉了真结果。

## 地址缺失跳

缺失（`for=unknown`、`for=_...`）意味着该跳的对端地址无法确认。
无法确认 ⇒ 无法判定其可信 ⇒ 信任链在此断裂，必须终止裁剪，
结果就是这一跳（地址缺失，调用方据此回退到直连对端地址）。
绝不允许把缺失跳「当成可信」继续往外。

## 两种头部同时出现的合并

两种头部都是「最左最远端、最右最近端」。自近端起按层对齐（zip）：
最近一层取两头部最右记录，再外一层取各自倒数第二，以此类推；
只存在于一种头部的层，另一头视为不存在，不得跳过。
一层「可信」当且仅当该层所有存在的记录地址都通过可信判定
（防止攻击者只污染一种头部就绕过）。某层出现第一个不可信/缺失时停止，
客户端地址取链头（链式优先，其次键值式）在该层的记录地址。

## 直连

没有任何跳记录时，客户端地址就是调用方给出的直连对端地址。
