# 合并水位与空闲分区 — 推导与不变量

## 一、九步分步表（New(3, idle=10, t0=0)；-∞ 表示 math.MinInt64）

| 步 | 分区水位 w0,w1,w2 | 本步空闲分区 | 候选值 | 合并水位 |
|---|---|---|---|---|
| 1 | 12,-∞,-∞ | 无 | -∞ | -∞ |
| 2 | 12,15,-∞ | p2（10-0=10，恰好） | 12 | 12 |
| 3 | 40,15,-∞ | p2 | 15 | 15 |
| 4 | 40,18,-∞ | p2 | 18 | 18 |
| 5 | 40,18,16 | p0（22-12=10，恰好） | 16 | 18 |
| 6 | 40,18,21 | p0 | 18 | 18 |
| 7 | 40,18,21 | p0、p1（30-20=10，恰好） | 21 | 21 |
| 8 | 40,18,21 | p0、p1、p2（36-26=10，恰好） | 无 | 21 |
| 9 | 40,30,21 | p0、p2 | 30 | 30 |

**(甲)** 若空闲分区仍参与取 min：九步合并依次 -∞,-∞,-∞,-∞,16,18,18,18,21，终值 21（正确 30；前四步被 p2 的 -∞ 卡死，此后恒偏低）。若空闲判定写成 now-last>idle（相等不算）：**第 2 步** p2 恰满 10 被误判活跃，候选含其 -∞，错成 **-∞（应 12）**；**第 7 步** p1 同理被误判活跃，候选 min(18,21)=18，错成 **18（应 21）**。（第 5、8 步边界处候选恰好不影响合并结果，故不错。）

**(乙)** 第 5 步正确合并水位 **18** = max(操作前 18, 候选 16)；若直接置为候选值不与旧值取 max，第 5 步错成 **16**，违反**不变量 2（合并水位只进不退）**，并连带违反不变量 1。

**(丙)** 第 8 步正确 **21**（全部空闲→无候选→保持不变）；若把全部空闲定义为取全部分区水位最大值：第 8 步错成 max(40,18,21)=**40**，第 9 步再 max(40,30)=**40**（应 30）。

## 二、四条不变量的代码位置与钉住测试

1. **朴素一致**：merge.go 中 peel(now) 用按 last 有序的活跃堆逐个定位新空闲分区（堆顶未空闲即停，不扫整表），并同步把它们移出「只含非空闲分区」的水位堆；advance() 读水位堆顶得候选值，末尾与旧合并值取 max，与朴素全扫描（api.go 的 naive.step 参照实现）逐拍同构；由 api_test.go 的 **TestNaiveRandom**（随机序列逐拍比对合并水位）和 **TestNineSteps**（钉死本表及第 2/5/8 步空闲集合位图）钉住。
2. **只进不退**：merge.go 的 advance() 仅在水位堆非空且堆顶 `c > m.merged` 时推进，全部空闲（堆空）直接返回、合并水位不动；由 **TestMonotonic**（含全部空闲、合法上报低于合并水位、空闲恢复三类场景）钉住。
3. **空闲边界精确**：pwm.go 的 Idle 用 `now >= last+idle`（相等即空闲，溢出安全）；被拒 Report 在 merge.go 校验段直接返回、触达不到 pwm 的状态写入；由 **TestIdleBoundary**（idle-1 非空闲、==idle 空闲、拒绝后 last 不刷新）与 **TestNineSteps** 的空闲位图钉住。
4. **失败不留痕**：merge.go 的 Report 先依次做分区越界、时钟回退、水位回退三项校验，全部通过后才置 clock 并 peel/写入；任何错误路径上无任何写操作；由 **TestRejectedNoTrace**（断言合并水位、时钟、空闲态、以及经后续空闲时点观测到的 last 均不变）钉住。

复杂度计数：merge.Manager.reads 为**非导出**字段，判空闲/求最小值时每逐个读取一个分区 +1，堆内上浮下沉比较不计；merge_test.go 的 **TestReadCounter** 白盒断言：无空闲/最小值变化的 Report 读数为与 m 无关的小常数，k 个分区转空闲的 Tick 读数 ≤ 2k+常数。
