# NOTES

## 第三节推导（n=4；边 01,12,02,13,03；rot0=[1,2,3], rot1=[0,3,2], rot2=[0,1], rot3=[1,0]）
规则：next(u→v) = v→pred_v(u)（pred=环序中紧挨 u 前面的邻居）。
| 起始有向边 | 面顶点序列 | 面长度 |
|---|---|---|
| 0→1 | 0,1,2 | 3 |
| 0→2 | 0,2,1,3 | 4 |
| 0→3 | 0,3,1 | 3 |
反向起始边落入同一 next 轨道，只是序列轮转；Σ长度=10=2E，每条有向边恰出现一次。
F=3；V-E+F=4-5+3=2=1+C（C=1），欧拉公式成立。
(甲) 若错取「后面的邻居」：0→1 在 rot1 取 0 的后继 3→1→3，在 rot3 取 1 的后继 0→3→0，在 rot0 取 3 的后继 1→回到 0→1，走出 **[0,1,3]**；正确应走出 **[0,1,2]**，面被换成另一个。
(乙) 两个不相交三角形 V=6,E=6,F=3,C=2：正确 6-6+3=3=1+2，成立；若假设连通硬套 =2，算出 3≠2，把合法嵌入**误判为不合法**（漏了 +C-1=1）。
(丙) 只数有限面会漏掉外平面 [0,2,1,3]：F'=2，4-5+2=1≠2，**误判为不合法**；正确 F=3，4-5+3=2=1+C，成立。

## 第二节四条不变量的落点
1. 面追踪完整（每面闭合、Σ长度=2E）：`face/face.go` 的 `traceOrbits` 用 visited 遍历全部 2E 条有向边、去闭合重复终点并累加长度；钉于 `face/face_test.go::TestFaceCoverage`。
2. 与朴素参照逐值一致：`face/face.go` 的 `next`（索引+双向环 O(1) 取前驱）；钉于 `face/face_test.go::TestNaiveReference`（`naive` 用切片线性扫描做对照）。
3. 欧拉公式含连通分量：`face/face.go` 的 `EulerHolds`，C 由 `compMap` BFS 邻接表得到，外轨道由 `mergeOuter` 合并；钉于 `face/face_test.go::TestEuler`（含不相交双三角形 C=2 用例）。
4. 失败不留痕：`pg/pg.go` 的 `AddEdge`/`SetRotation` 全部校验通过后才修改状态；钉于 `face/face_test.go::TestRejectionLeavesState`。
