# NOTES

七步推演（n=3, maxBuf=8），初始 L=[0,0,0]，buf=∅，dup=0：

| 步 | 到达 | 判定 | 本步交付（先后） | 步后 V_local | 步后缓冲 | dup |
|---|---|---|---|---|---|---|
| 1 | e [2,2,0] | 缓冲 | 无 | [0,0,0] | e | 0 |
| 2 | d [1,1,1] | 缓冲 | 无 | [0,0,0] | e,d | 0 |
| 3 | b [1,1,0] | 缓冲 | 无 | [0,0,0] | e,d,b | 0 |
| 4 | c [2,0,0] | 缓冲 | 无 | [0,0,0] | e,d,b,c | 0 |
| 5 | f [2,2,2] | 缓冲 | 无 | [0,0,0] | e,d,b,c,f | 0 |
| 6 | a [1,0,0] | 交付+级联 | a,c,b,e,d,f | [2,2,2] | ∅ | 0 |
| 7 | b [1,1,0] | 重复丢弃 | 无 | [2,2,2] | ∅ | 1 |

(甲) 只比发送方分量：第 2 步首次错交——d 的 V[2]=1=L[2]+1 即被交付，L=[0,0,1]；其因果前驱 a=[1,0,0]、b=[1,1,0] 均未交付。
(乙) 不级联：第 6 步只交付 a，L=[1,0,0]，buf=e,d,b,c,f；比正确实现少交付 c,b,e,d,f 共 5 条。
(丙) 正确处理：判重丢弃、dup=1、状态不变。若不判重，b 不可交付（1≠3）落入缓冲，7 步后 buf 剩 1 条，违反不变量 3（闭合集到达后缓冲未空；同时与不变量 1 的朴素参照不符）。

不变量 → 代码保证位置 / 钉住测试：

1. 与朴素参照一致：`cbuf` 每轮按 From 升序只探测键 (j, L[j]+1) 的候选、交付至不动点，选择规则与整表扫描相同 — `TestNaiveReferenceEquivalence`
2. 因果序：`vc.Deliverable` 要求 V[j]=L[j]+1 且其余分量 ≤ L — `TestCausalOrderInvariant`
3. 完备：`cbuf.cascade` 级联到无可交付 + 缓冲按 (from,seq) 索引 — `TestClosedSetCompleteness`
4. 失败不留痕：`api.New` 先校验参数；`api.Receive` 先 `vc.Legal`、再判重、满缓冲在插入前拒绝 — `TestRejectionLeavesNoTrace`
