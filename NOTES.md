# Shannon-Fano 推导与不变量

## 第三节推导（频次 A5 B2 C1 D1 E1，按频次降序+符号升序）

| # | 当前组 | 总频次 | 切分点 | 左(前缀+0) | 右(前缀+1) |
|---|---|---|---|---|---|
| 1 | A5 B2 C1 D1 E1 | 10 | A\|B（\|5-5\|=0） | A=5 → `0` | BCDE=5 → `1` |
| 2 | B2 C1 D1 E1 | 5 | B\|C（\|2-3\|=1 与 \|3-2\|=1 并列，取最左） | B=2 → `10` | CDE=3 → `11` |
| 3 | C1 D1 E1 | 3 | C\|D（\|1-2\|=1 与 \|2-1\|=1 并列，取最左） | C=1 → `110` | DE=2 → `111` |
| 4 | D1 E1 | 2 | D\|E（\|1-1\|=0） | D=1 → `1110` | E=1 → `1111` |

码表：A=`0` B=`10` C=`110` D=`1110` E=`1111`。
"AABCCD" → `0 0 10 110 110 1110` → 位串 `00101101101110`（14 位）→ 字节 `0x2D 0xB8`。

- (甲) 并列取最右：第 2 步改切 `BC|DE`，得 A=`0` B=`100` C=`101` D=`110` E=`111`。B/C/D/E 由 `10/110/1110/1111` 变为 `100/101/110/111`（B、C 变长，D、E 变短）。
- (乙) 误用「首次累计 >= total/2」：{A6 B4 C3 D1} total=14，累计 A=6<7、A+B=10>=7 → 顶层切 `AB|CD`，错码 A=`00` B=`01` C=`10` D=`11`；正确「差最小」切 `A|BCD`（|6-8|=2 最小），正解 A=`0` B=`10` C=`110` D=`111`。
- (丙) `Encode([])` 返回空切片（0 字节）；单符号 {A:1} 码字为空串，`Encode("AA")` 编成 0 位（0 字节）；{A:0,B:0} 构造报 `ErrInvalidFreq`。

## 第二节四条不变量

1. 与朴素参照一致：切分在 `sfano.split`（差最小、并列取最左），打包在 `sfstream.Encode`（大端）；测试 `TestTableMatchesNaive`、`TestEncodeMatchesNaive`。
2. 切分点无关：`sfstream.Feeder` 累积位缓冲后统一解码，与切块无关；测试 `TestFeedChunkAgnostic`。
3. 确定性/前缀自由：`sfano.Build` 排序键确定、码字按 `0/1` 分叉生成；测试 `TestDeterministicPrefixFree`。
4. 失败不留痕：`api.New/Encode/Decode` 出错路径先返回、不写任何状态（Codec 构建后只读）；测试 `TestFailureAtomic`。
