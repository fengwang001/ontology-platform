# EWMA 推导与不变量说明

## 一、分步推导（α=0.9, seed=0, 不校正, 观测 [10,20,10,20,10]）

递推 `s ← α·x + (1−α)·s_old`，即 `s ← 0.9·x + 0.1·s_old`，初始 s=0：

| 步 | x | s（该步之后） |
|---|---|---|
| 1 | 10 | 0.9·10 + 0.1·0 = **9** |
| 2 | 20 | 0.9·20 + 0.1·9 = **18.9** |
| 3 | 10 | 0.9·10 + 0.1·18.9 = **10.89** |
| 4 | 20 | 0.9·20 + 0.1·10.89 = **19.089** |
| 5 | 10 | 0.9·10 + 0.1·19.089 = **10.9089** |

**(甲)** 权重写反成 `s = 0.1·x + 0.9·s_old`：第1步 s=0.1·10=**1**（正确 9）；逐步得 s=1, 2.9, 3.61, 5.249，第5步 s=**5.7241**（正确 10.9089）。新样本权重从 0.9 错成 0.1，均值被历史（seed=0）拖住。

**(乙)** 种子错改为 seed=x₁=10：第1步 s=0.9·10+0.1·10=**10**（正确 seed=0 时为 9）。首观测被重复计入。

**(丙)** 第1步 s=9 < 10，偏置来自 seed=0 以权重 (1−α)ⁿ 混入。校正 `s/(1−(1−α)ⁿ)`：n=1 时 9/(1−0.1)=9/0.9=**10**，恰等于首观测。因 seed=0 时 s=α·Σ(1−α)ⁿ⁻ⁱxᵢ，各观测权重和恰为 1−(1−α)ⁿ，除以它即归一化；n=1 时唯一观测权重为 α，约去后正是 x₁。

## 二、四条不变量的保证位置与钉住测试

1. **范围不变**：`ewma/ewma.go` `Update` 中 s 是 x 与 s_old 的凸组合（α∈(0,1)），归纳即落在历史最值内；校正值 ≤s 同侧亦在区间内。测试 `TestRangeInvariant`。
2. **常数输入精确**：`ewma.go` `Value` 的校正分支 `s/(1−(1−α)ⁿ)`；不校正时闭式即 c·(1−(1−α)ⁿ)。测试 `TestConstantInputExact`。
3. **与闭式参照一致**：`Update` 递推展开即闭式 `α·Σ(1−α)ⁿ⁻ⁱxᵢ+(1−α)ⁿ·seed`。测试 `TestMatchesClosedForm`（测试内独立批量重算比对）。
4. **失败不留痕**：`api/api.go` 中 `Update`/`Value` 先校验（`ErrBadAlpha`/`ErrEmptyKey`/`ErrUnknownKey`）再触碰状态，校验失败零写入。测试 `TestRejectionLeavesNoTrace`。

复杂度：`ewma.go` 非导出字段 `lastReads` 恒置 1，测试 `TestValueReadsConstant`（同包内）断言不随 m 增长。并发：`series` 用 `sync.RWMutex`，测试 `TestConcurrentReadConsistency`。
