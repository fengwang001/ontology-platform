# NOTES

## 八步分步表（d2 = 欧氏平方距离）

| # | 操作 | 返回 | 关键 d2 / 并列 |
|---|---|---|---|
| 1 | Add(0,0) | 0 | 登记站点 0=(0,0) |
| 2 | Add(1,1) | 1 | 登记站点 1=(1,1) |
| 3 | Add(4,0) | 2 | 登记站点 2=(4,0) |
| 4 | Nearest(0,2) | 1 | s0 d2=4, s1 d2=2, s2 d2=20；s1 最小 |
| 5 | Nearest(1,0) | 0 | s0 d2=1, s1 d2=1 并列, s2 d2=9；取最小索引 0 |
| 6 | Add(0,2) | 3 | 登记站点 3=(0,2) |
| 7 | Nearest(0,2) | 3 | s3 自身 d2=0 |
| 8 | Within(1,0,1) | 0 | r2=1：d2 依次 1,2,9,5，无一 `<1`；s0 恰在圆上 |

- **甲**：正确返回 **1**（s1 的 d2=2 严格最小）。若误用曼哈顿距离：s0=|0|+|2|=2、s1=|1|+|1|=2、s2=4+2=6，s0/s1 并列取小索引，错误返回 **0**。
- **乙**：正确返回 **0**（s0、s1 的 d2 均为 1，并列取最小索引）。若用 `<=` 保留最后扫描到的最小者，s1 覆盖 s0，错误返回 **1**。
- **丙**：正确返回 **0**。若误写成 `d2 <= r2`，圆上 s0=(0,0)（d2=1）被多算，错成 **1**（仅多算站点 0）。

## 四条不变量（代码保证位置 / 钉住的测试函数）

1. 与朴素扫描一致：`sites/sites.go` 的 `Nearest` 螺旋外扩、按格子精确下界剪枝，比较逻辑与全扫相同；由 `TestNaiveConsistency` 钉住。
2. 距离精确：`nbr/nbr.go` 的 `Dist2` 用 int64 精确平方；`sites/sites.go` 的 `Within` 经 `nbr.Inside` 严格判 `d2 < r2`；由 `TestNBR`、`TestWithin` 钉住。
3. 并列稳定：`sites.go` 仅在 `d2 < best` 或 `d2 == best 且索引更小` 时更新，与遍历顺序无关；由 `TestEightSteps`、`TestNaiveConsistency` 钉住。
4. 失败不留痕：`api/api.go` 的 `Add` 先做越界/重复校验全部通过后才改状态，三个哨兵错误互异；由 `TestSentinelErrors` 钉住。
