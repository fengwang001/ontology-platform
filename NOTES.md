# NOTES

## 七步推导（sender S，maxBuffered 足够大）

| 步 | 到达seq | next旧值 | 判定 | 本步交付 | 缓冲 | 累计重复 |
|----|---------|----------|------|----------|------|----------|
| 1 | 0 | 0 | 交付 | 0 | {} | 0 |
| 2 | 2 | 1 | 入缓冲 | — | {2} | 0 |
| 3 | 1 | 1 | 交付+级联 | 1,2 | {} | 0 |
| 4 | 2 | 3 | 重复 | — | {} | 1 |
| 5 | 3 | 3 | 交付 | 3 | {} | 1 |
| 6 | 0 | 4 | 重复 | — | {} | 2 |
| 7 | 4 | 4 | 交付 | 4 | {} | 2 |

最终交付日志 = 0,1,2,3,4；累计重复 = 2。

- (甲) 不级联：2 永远留在缓冲，`next` 停在 2；此后 seq≥2 的到达全部入缓冲，交付卡死，缓冲无限增长直至超限报错。
- (乙) 漏判 `seq<next`：seq=2 落入「否则交付」分支被再次交付，日志出现两个 2，恰好一次被破坏。
- (丙) `==` 误判入缓冲：0 进缓冲而 `next` 永为 0，任何消息都交付不了，全部堆积到缓冲超限，整体阻塞。

## 四条不变量

1. 恰好一次：`seq/seq.go` 的 `Add` 中 `s<next` 与「缓冲内已有」只计重复不交付，仅 `s==next` 及级联循环追加日志；测试 `TestExactlyOnce`。
2. FIFO 保序：日志只在 `Add` 的 `==next` 分支与级联循环里按 `next` 严格递增追加；测试 `TestFIFOOrder`。
3. 与朴素参照一致：`api` 的 `SelfCheck` 与测试 `TestNaiveReference` 用随机到达序列对拍「去重后按 seq 升序」的朴素结果。
4. 失败不留痕：`rb/rb.go` 的 `Deliver` 先做全部校验（空 sender / 负 seq / 缓冲超限）再触碰状态，拒绝时不建 tracker、不改缓冲/日志/重复计数；测试 `TestFailureNoTrace`。
