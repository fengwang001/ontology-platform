# 序号回绕比较与单调展开：推导笔记

## 八行分步表（N=4，M=16，半圈=8；流 13 14 15 0 1 2 10 3）

| # | s | d=(s−last%16)&15 | 判定 | absolute | last 之后 |
|---|---|---|---|---|---|
| 1 | 13 | —（首个） | 首项 | 13 | 13 |
| 2 | 14 | 1 | Less | 14 | 14 |
| 3 | 15 | 1 | Less | 15 | 15 |
| 4 | 0 | 1 | Less | 16 | 16 |
| 5 | 1 | 1 | Less | 17 | 17 |
| 6 | 2 | 1 | Less | 18 | 18 |
| 7 | 10 | 8 | Incomparable | 错误 | 18（不变） |
| 8 | 3 | 1 | Less | 19 | 19 |

(甲) 第4步：d=(0−15)&15=(−15) mod 16=1，不是 15：模 M 减法自然回绕，−15≡1 (mod 16)。若朴素实现用直接无符号大小比较（0<15 判「倒退/旧」），合法回绕被错判为 Greater 拒绝，absolute 永远停在 15 无法前进，破坏不变量 1（与参照一致）与 3（绝对单调递增）。

(乙) 第7步 Cmp(2,10)：d=(10−2)&15=8=M/2，应判 Incomparable。若把 d==M/2 归入 Less，10 会被错展开为 last+8=18+8=26。圆周上 10 既在 2 前方 8 步也在后方 8 步，两个方向等距、是同一段弧，「前移 8」与「后移 8」根本无法区分，故必须报错拒绝。

(丙) Cmp(2,10) 的 d=8；Cmp(10,2) 的 d=(2−10)&15=8，两者都是半圈。若 d==M/2 判 Less，则 Cmp(2,10)=Less 且 Cmp(10,2)=Less，与「Less 当且仅当反向为 Greater」矛盾，违反不变量 2（反对称）。故半圈必须单列为一态。

## 四条不变量：保证位置 / 钉住测试

1. 与朴素参照一致：`api.SelfCheck` 用独立的线性扫描参照逐流比对；测试 `TestSelfCheck`、`TestFeedSequence`（钉住上表八行）。
2. 反对称：`sar.Cmp` 只由 d 与 M/2 的三向比较决定，且 d(b,a)=M−d(a,b)；测试 `TestCmpAntisymmetry`。
3. 绝对单调：`unwrap.Feed` 仅当 0<d<M/2 时 last+=d（d≥1），Equal 原样返回不前进；测试 `TestFeedMonotonic`。
4. 失败不留痕：`unwrap.Feed` 所有错误分支都在写 last 之前 return；测试 `TestFeedErrors`。
