# 净零折叠 NOTES

## 八行分步表（maxDepth=8，操作 +5 -5 -8 +8 +7 +2 -7 -2）

| 步 | 操作 | 是否抵消 | 未了结序列 | 净值 |
|---|---|---|---|---|
| 1 | +5 | 否（空序列） | [+5] | 5 |
| 2 | -5 | 抵消末尾 +5 | [] | 0 |
| 3 | -8 | 否（空序列） | [-8] | -8 |
| 4 | +8 | 抵消末尾 -8 | [] | 0 |
| 5 | +7 | 否（空序列） | [+7] | 7 |
| 6 | +2 | 否（末尾 +7 同号） | [+7,+2] | 9 |
| 7 | -7 | 否（末尾 +2 量值不等） | [+7,+2,-7] | 2 |
| 8 | -2 | 否（末尾 -7 同号） | [+7,+2,-7,-2] | 0 |

(甲) 第 7 步正确序列是 `[+7,+2,-7]`（只能与末尾 +2 比较，不抵消故追加）。若错误地在整条序列里搜反号等量对、把 -7 与最早的 +7 抵消，第 7 步会错成 `[+2]`。
(乙) 第 8 步后正确序列是 `[+7,+2,-7,-2]`（4 条；净值为 0 但无相邻反号等量对，无可再折叠）。若错误地「净值=0 就清空」，会错成 `[]`。
(丙) 同一多重集按 +7、-7、+2、-2 到达：+7→[+7]；-7 抵消→[]；+2→[+2]；-2 抵消→[]，结果为 `[]`，与上表第 8 步 `[+7,+2,-7,-2]` 不同，但两者净值都是 0。可见不变量 1/2（changelog 的具体内容）与到达顺序有关；不变量 3（Net 恒等于所有操作代数和、重放 changelog 回到 Net）与顺序无关，对任意到达顺序都成立。

## 四条不变量：保证位置与钉住测试

1. 与朴素重放一致：norm.Apply 的「只查末尾、抵消弹栈否则追加」即朴素反复抵消的不动点（栈式一遍等价）；测试 `TestChangelogMatchesNaive`（api/api_test.go，内含朴素参照实现 naiveFold 逐条比对）。
2. 折叠完备（无相邻反号等量对）：fold.Cancels 只在末尾命中时弹栈，追加前必与末尾比较，归纳保持；测试 `TestNoAdjacentCancellablePairs`。
3. 语义不变（重放=Net）：norm.Apply 中抵消时 net 不变（弹出 tail 且不入 op，tail+op=0），追加时 net+=op；测试 `TestReplayEqualsNet`。
4. 失败不留痕：norm.Apply 先做全部校验（k 合法/深度/溢出），任一失败在写状态前返回哨兵错误；测试 `TestRejectedOpsLeaveStateUnchanged`、`TestErrorsDistinct`。

自检入口：api 包 `SelfCheck()` 对内置序列核验上述四条；复杂度 O(1) 由 norm 非导出计数器保证，白盒测试 `TestTailCheckConstant`（norm/norm_test.go）；并发只读一致性由 `TestConcurrentReads` 钉住。
