# 跳变窗口计数 NOTES

## 第三节推导（size=8, hop=4, delay=2；窗口 [n*4, n*4+8)，wm=maxTS−2，wm>=end 触发）

| 步 | TS | wm | 落入窗口(2个) | 本步触发 | 已触发累计 |
|---|---|---|---|---|---|
| 1 | 6  | 4  | [0,8) [4,12)   | —           | — |
| 2 | 10 | 8  | [4,12) [8,16)  | [0,8) +1    | [0,8)=1 |
| 3 | 12 | 10 | [8,16) [12,20) | —           | [0,8)=1 |
| 4 | 14 | 12 | [8,16) [12,20) | [4,12) +2   | +[4,12)=2 |
| 5 | 16 | 14 | [12,20) [16,24)| —           | 同上 |
| 6 | 18 | 16 | [12,20) [16,24)| [8,16) +3   | +[8,16)=3 |
| 7 | 20 | 18 | [16,24) [20,28)| —           | 同上 |
| 8 | 22 | 20 | [16,24) [20,28)| [12,20) +4  | +[12,20)=4 |

Flush 后补触发：[16,24) +4（TS 16,18,20,22）、[20,28) +2（TS 20,22）。

- (甲) 退化为翻滚窗口 floor(TS/8)*8：[4,12) 错成 **0**（应 2，无任何 TS 落入 [4,8)）；[12,20) 错成 **2**（应 4，只装 TS=12,14，16/18 被错划到 [16,24)）。
- (乙) 右边界误取闭区间：TS=12 额外落入 [4,12)，TS=16 额外落入 [8,16)；[4,12) 错成 **3**（应 2），[8,16) 错成 **4**（应 3）。
- (丙) 触发写成 wm>end：第2步 [0,8) 推迟到第3步（wm=10）；第4步 [4,12) 推迟到第5步（wm=14）；第6步 [8,16) 推迟到第7步（wm=18）；第8步 [12,20) 需 wm>20，八步内永不触发，只能等 Flush。若事件流在第8步后、Flush 前结束，[12,20) 的 +4 永不输出，下游物化视图永久缺失该窗口。

## 四条不变量的保证位置与钉住测试

1. 与批量重算一致：`wagg.Agg.feedOne` 用 `win.Windows` 枚举全部 size/hop 个重叠窗口并懒创建计数；`Flush` 触发全部余留窗口。测试 `TestViewMatchesBatch`（api/api_test.go）。
2. 变更日志自洽：窗口触发即从堆弹出并删除计数（`wagg.Agg.advance`），每个 (Key,窗口) 只发一条 + 且为最终计数。测试 `TestChangelogExactlyOnce`。
3. 触发顺序==end 升序、水位线单调：待触发窗口放按 end 有序的最小堆（`wagg.pq`），弹序即 end 升序；wm=maxTS−delay 只进不退。测试 `TestTriggerOrderByEnd`。
4. 失败不留痕：`wagg.Agg.Feed` 先整批校验乱序再逐条应用；`wagg.New` 先校验参数；三类哨兵错误 ErrParam/ErrDelay/ErrOrder 互不相同。测试 `TestRejectLeavesNoTrace`、`TestSentinelErrors`。
