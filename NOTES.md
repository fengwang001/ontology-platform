# NOTES — ontology-302：加权蓄水池抽样 A-Res

k=2；us=[.30,.49,.81,.0625,.72,.64,.90,.25]；键=u^(1/W)，池满仅当新键严格高于堆顶才替换。

| 步 | u | 键 | 结果 | 池（排名高→低） | Consumed |
|---|---|---|---|---|---|
| 1 a W=1 | .30 | .3000 | 插入 | a:0.3000 | 1 |
| 2 b W=2 | .49 | .7000 | 插入 | b:0.7000,a:0.3000 | 2 |
| 3 c W=.5 | .81 | .6561 | 替换 a | b:0.7000,c:0.6561 | 3 |
| 4 d W=0 | 不取 | — | ErrZeroWeight | b:0.7000,c:0.6561 | 3 |
| 5 e W=4 | .0625 | .5000 | 未入选 | b:0.7000,c:0.6561 | 4 |
| 6 f W=1 | .72 | .7200 | 替换 c | f:0.7200,b:0.7000 | 5 |
| 7 g W=2 | .64 | .8000 | 替换 b | g:0.8000,f:0.7200 | 6 |
| 8 h W=.5 | .90 | .8100 | 替换 f | h:0.8100,g:0.8000 | 7 |

(甲) 键误写为 u·W：g 键=0.64×2=**1.28**（键竟可 >1，权重序被彻底扭曲）；全程重算最终错成 **g、b**（真解 h、g，h 被丢、b 反留）。
(乙) 误留键最小的 k 个：最终错成 **e:0.5000、a:0.3000**。
(丙) 把 W=0 当键 0 的普通元素：d 吞掉 .0625（键 0 仍不入选）；e←u=.72 键≈**.9212**，f←u=.64 键=.6400，g←u=.90 键≈**.9487**，h←u=.25 键=.0625；最终错成 **g、e**；Consumed 错成 **8**。正确实现 Consumed=**7**，随机源还剩 **.25** 未用。

不变量保证位置 / 钉测测试：

1. 与批量参照一致：`resv` 小顶堆按（键↓、到达序↑）全序只留 top-k，`api.Sample` 出堆按同全序排序 — TestSampleMatchesBatch。
2. 随机数守恒：游标只在 `api.Offer` 应用期推进、每接受一个元素恰好推进一，预校验失败不推进 — TestRandomConservation。
3. 蓄水池规模=min(k,接受数) 且 ID 互异：`resv.Offer` 容量控制 + `api.seen` 去重 — TestPoolSizeAndUniqueIDs。
4. 失败不留痕：`api.Offer` 全行预校验（ID→W=0→W 非法→源用尽）通过后才改池与游标 — TestRejectedBatchNoTrace。
