# 物化视图 DAG：级联失效与拓扑重算 — 推导与不变量

## 第三节推导

反向边（下游）：A→{C,D}，B→{C}，C→{E}，D→{E,F}。注册时非基视图（E,F,C,D）即置脏；脏是布尔标记。

| 步骤 | 脏集合 | A | B | C | D | E | F |
|---|---|---|---|---|---|---|---|
| 注册完 | {C,D,E,F} | 0 | 0 | 0 | 0 | 0 | 0 |
| Set(A,1) | {A,C,D,E,F} | 1 | 0 | 0 | 0 | 0 | 0 |
| Set(B,2) | {A,B,C,D,E,F} | 1 | 2 | 0 | 0 | 0 | 0 |
| Recompute | {} | 1 | 2 | 3 | 2 | 5 | 1 |
| Set(B,20) | {B,C,E} | 1 | 20 | 3 | 2 | 5 | 1 |
| Recompute | {} | 1 | 20 | 21 | 2 | 23 | 1 |
| Set(A,10) | {A,C,D,E,F} | 10 | 20 | 21 | 2 | 23 | 1 |
| Recompute | {} | 10 | 20 | 30 | 20 | 50 | 19 |

- (甲) 按注册序 A,B,E,F,C,D 重算脏集 {B,C,E}：E 排在 C 前，读到旧 C=3、D=2，E 错成 **5**（正确 23）。
- (乙) 只标直接下游：Set(A,10) 只脏 {A,C,D}，E、F 不重算，E 错成 **23**（应 50）、F 错成 **1**（应 19）。
- (丙) `AddView(N,[M])` 必须整体被拒（环 M→N→M，可判定错误），状态不变。若环检测只看已注册视图、忽略前向引用：M→N 边在 N 注册前未入图，加 N 时只见 N→M，环被放过；之后 Recompute 拓扑排序失败或按任意序求值得错值。不变量 1 的参照必须写「按拓扑序」：允许前向引用意味着注册序可让视图排在其依赖之前（如 E、F 先于 C、D），注册序不是合法求值序，结果会依赖注册先后而非依赖语义。

## 四条不变量：保证位置与钉住测试

1. **与批量重算一致**：`view.Store.Recompute` 只对脏集按拓扑序求值、干净视图保留原值，等价于全量拓扑重算；测试 `TestBatchConsistency`（随机操作序列对拍朴素全量重算）。
2. **拓扑序正确**：`dag.Graph.Topo` 对脏子图 Kahn 排序，`Recompute` 严格按该序求值；测试 `TestTopologicalFreshness`（fn 内记录读到的依赖值，断言是最新）。
3. **去重**：脏标记为 bool，`Recompute` 内每视图至多求值一次并计入非导出计数器 `last`；测试 `TestDedupSingleEval`、`TestEvalCountIndependentOfM`。
4. **失败不留痕**：`api` 先校验（空名/重名/未注册/非基）再落地，`view.Add`/`Recompute` 先查环与未解析依赖再改状态；测试 `TestFailureLeavesNoTrace`。
