# NOTES

## 第三节推导：B=6, T=2，六次 Append 分步表

| 步 | Append | 累计逻辑 | 缓冲(步后) | flush物理 | merge物理 | 累计物理 | 放大因子 |
|---|---|---|---|---|---|---|---|
| 1 | (a,1) | 2 | 2 | 0 | 0 | 0 | 0/2=0 |
| 2 | (b,1) | 4 | 4 | 0 | 0 | 0 | 0/4=0 |
| 3 | (c,1) | 6 | 0 | 6 | 0 | 6 | 6/6=1 |
| 4 | (d,12) | 9 | 3 | 0 | 0 | 6 | 6/9≈0.667 |
| 5 | (a,12) | 12 | 0 | 6 | 10 | 22 | 22/12≈1.833 |
| 6 | (e,1) | 14 | 2 | 0 | 0 | 22 | 22/14≈1.571 |

第 5 步：flush 出段 {d:12, a:12}(6 字节)，段数达 T=2，合并段 {a:1,b:1,c:1} 与 {d:12,a:12}，
同键留 seq 最大者 → {a:12, b:1, c:1, d:12}，合并段 = 3+2+2+3 = 10 字节。

- (甲) 第 5 步 merge 后 `Get("a")` = "12"；合并段 10 字节。若错留「第一个」版本：a→"1"，
  合并段错成 2+2+2+3=9 字节，累计物理错成 6+6+9=21，最终放大因子错成 21/14=1.5（且 Get("a") 错返回 "1"）。
- (乙) 最终放大因子 = 22/14 ≈ 1.571。若漏计 merge 物理字节：物理错成 12，放大因子错成 12/14 ≈ 0.857。
- (丙) 第 3 步缓冲恰为 6 = B，按 `>=` 规则 flush 发生，段 6 字节。若错写成 `buf > B`：第 3 步不 flush；
  第 4 步缓冲 6+3=9 > 6 才 flush，第一个段错成 9 字节（正确为第 3 步 flush 出 6 字节）。

## 第二节四条不变量的保证位置与钉住测试

1. 与朴素逐条重算一致：seglog.Get 先缓冲后段、各自从新到旧扫（seglog.go），rec.Merge 按 seq 留最新（rec.go）；测试 `TestGetMatchesNaive`。
2. 放大因子正确：flush/merge 各自把段字节累加进 phys（seglog.go flush/merge 两处），Amplification=phys/logical（api.go）；测试 `TestAmplificationAccounting`（含 22/14 全账）。
3. 段数有界：flush 后 `len(segs)>=T` 即合并为 1 段（seglog.go），存活段数 <= T-1；测试 `TestLiveSegmentsBounded`（seglog 包内，读非导出计数器）。
4. 失败不留痕：Append/New 先校验后改状态，校验失败直接返回哨兵错误（api.go）；测试 `TestFailuresLeaveNoTrace`。
