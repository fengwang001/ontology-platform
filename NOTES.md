# 物化视图乐观并发提交器 — 推导与不变量

## 六步确定交错（New(3)；G1 提交 [a+1,c+1]，G2 提交 [b+5,c+10]）

| 步 | 动作 | 快照读到 | 校验结论 | 本步改动 | 视图(a,b,c) | 累计重试 |
|---|---|---|---|---|---|---|
| 1 | G1 快照 | a=0@v0, c=0@v0 | — | 无 | (0,0,0) | 0 |
| 2 | G2 快照 | b=0@v0, c=0@v0 | — | 无 | (0,0,0) | 0 |
| 3 | G1 校验+提交 | — | a、c 版本均未变 | a=1@v1, c=1@v1 | (1,0,1) | 0 |
| 4 | G2 校验+提交 | — | c 已变 v0→v1，冲突 | 无（释放锁，重试） | (1,0,1) | 1 |
| 5 | G2 重新快照 | b=0@v0, c=1@v1 | — | 无 | (1,0,1) | 1 |
| 6 | G2 校验+提交 | — | b、c 均未变 | b=5@v1, c=11@v2 | (1,5,11) | 1 |

## 三问

- (甲) 去掉版本校验：步3后视图 (1,0,1)，步4后 (1,5,10)。第 4 步 Key `c` 产生丢失更新——G2 用过期快照 c=0 直接覆盖，G1 的 +1 丢失，`c` 错成 10（正确 11）。
- (乙) 逐 Key「校验→立即应用」且不回滚：第 4 步按字典序先校验 `b`（未变）并应用 b=5，随后 `c` 冲突、重试时 `b` 被再加一次，`b` 错成 10（正确 5）。
- (丙) 悲观锁按请求顺序加锁：G1 持 c 锁请求 a，G2 持 a 锁请求 c，循环等待死锁；视图停在 (a,c)=(0,0)。按 Key 排序后两边都按 a→c 加锁，无环，最终 (a,c)=(2,3)。

## 四条不变量落点

1. 朴素重放一致：`commit.tryCommit` 在单互斥锁内整体校验+应用，提交顺序即锁获取顺序；`TestNaiveReplay`（commit 包内测试）钉住。
2. 无部分提交：`tryCommit` 先全部 Key 校验通过才统一应用，冲突直接返回不写任何值；`TestSixStep`、`TestConcurrentSameKey`（commit 包内测试）钉住。
3. 版本单调恰 +1：版本只由 `cell.Apply` 改写且恰 +1，冲突/拒绝路径不写；`TestVersionExactlyPlusOne`（commit 包内测试）钉住。
4. 失败不留痕：`merge` 是加锁前的纯函数校验，ErrContended 路径不写值/版本/重试计数；`TestRejectionNoTrace`、`TestContendedNoTrace`（commit 包内测试）钉住。
