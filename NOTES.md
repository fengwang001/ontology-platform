# DAG 全局最长路径 · 推导与不变量落点

## 第三节推导：n=5，边 0→1:2 0→2:4 1→3:5 2→3:3 3→4:1 2→4:7 1→4:9

拓扑序 0,1,2,3,4；源 dist=0（空前缀），其余 -∞。

| 节点 | 各前驱候选 dist[p]+w | dist |
|---|---|---|
| 0 | （源，空前缀） | 0 |
| 1 | 0: 0+2=2 | 2 |
| 2 | 0: 0+4=4 | 4 |
| 3 | 1: 2+5=7；2: 4+3=7 | 7 |
| 4 | 3: 7+1=8；2: 4+7=11；1: 2+9=11 | 11 |

答案 = max dist = **11**；并列路径 [0,1,4]（2+9）与 [0,2,4]（4+7），取字典序最小 → **[0,1,4]**。

- (甲) 误取 min（最短路）：dist[1]=2, dist[2]=4, dist[3]=min(7,7)=7, dist[4]=min(8,11,11)=**8**（正确 11）。
- (乙) 固定从 0 出发、忽略其它源：加源 5→6:100 后仍返回 **11**（正确全局最长 **100**，路径 [5,6]）。
- (丙) 并列取字典序最大：返回 **[0,2,4]**（正确 **[0,1,4]**）。

## 第二节不变量落点

1. 路径合法：`lpath.Solve` 回溯只沿真实边（dist 一致 + good 剪枝，到达和恰为 dist）；`api.checkPath` 复核；测试 `TestSelfCheck`、`TestBruteForce`（路径逐元素等于朴素枚举出的合法路径）。
2. 最长：`lpath.Solve` 拓扑序 DP 对全部边取 max，前驱按 `max(dist,0)` 松弛（负 dist 时从该节点新起路径，否则漏掉非源起点）；测试 `TestBruteForce`（n=2..8 × 20 种子对拍）。
3. 与朴素参照一致：`api_test.brute` 枚举全部路径取最大总权，逐值比对；测试 `TestBruteForce`。
4. 失败不留痕：`wdag.AddEdge` 先完成全部校验（越界/自环/重复）再写入，`wdag.New` 拒绝非正 n；测试 `TestErrorsDistinctAndStateKept`（EdgeCount 不变且 Solve 仍可用）。
