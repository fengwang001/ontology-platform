# 弦图判定（MCS + 成团检查）推导与不变量
## 第三节推导：n=6，边 (0,1)(1,2)(0,2)(1,3)(3,4)(2,5)
邻接：0:{1,2} 1:{0,2,3} 2:{0,1,5} 3:{1,4} 4:{3} 5:{2}

MCS 分步表（每行：选中节点 → 选中后各未选节点权重）：

| 步 | 选中 | w1 | w2 | w3 | w4 | w5 |
|----|------|----|----|----|----|----|
| 1 | 0（全 0 取最小编号） | 1 | 1 | 0 | 0 | 0 |
| 2 | 1（w=1，与 2 并列取小） | – | 2 | 1 | 0 | 0 |
| 3 | 2（w=2 唯一最大） | – | – | 1 | 0 | 1 |
| 4 | 3（w=1，与 5 并列取小） | – | – | – | 1 | 1 |
| 5 | 4（w=1，与 5 并列取小） | – | – | – | – | 1 |
| 6 | 5 | – | – | – | – | – |

MCS 选中序 = 0,1,2,3,4,5 → **PEO = 逆序 = [5,4,3,2,1,0]**。
逐节点成团检查（N+ = PEO 中排在 v 之后的邻居）：5:{2}✓ 4:{3}✓ 3:{1}✓ 2:{0,1}，(0,1)有边✓ 1:{0}✓ 0:∅✓ → 全部成团，**该图是弦图**。

- **(甲)** 只做 MCS、跳过成团检查 ⇒ 任何图都判弦图。对 C4（0-1-2-3-0）误判为**弦图**；正确应为**非弦图**，其 PEO=[3,2,1,0] 中首个违规节点是 **3**（N+(3)={1,2}，(1,2) 无边）。
- **(乙)** 把 MCS 序原样当 PEO（不取逆）⇒ 六边图上节点 **1** 的 N+={2,3}，(2,3) 无边 ⇒ 误判「不成团」，把**弦图误判为非弦图**（正确：弦图）。
- **(丙)** 只查「与 PEO 紧邻下一节点相邻」⇒ C4 中 v=3 只查 (3,2) 有边即过，**漏检不相邻对 (1,2)** ⇒ 把 C4 **误判为弦图**（正确：非弦图）。

## 第二节四条不变量：保证位置 + 钉住它的测试
1. **PEO 合法性**：`mcs/mcs.go` 的 `Compute` 用懒删除堆按（权重降序、编号升序）逐步选出再整体取逆；测试 `TestPEOMatchesNaiveReference`（mcs 包，与 O(n²) 朴素全扫描参照逐图比对）。
2. **判定正确**：`mcs/mcs.go` 的 `check` 按 PEO 顺序逐节点对 N+(v) 两两查边并记录首个违规者；测试 `TestKnownGraphs`（已知结论图）+ `TestChordalVersusNaive`（随机图对拍）。
3. **与朴素参照一致**：判定主路径本身就是逐节点两两相邻朴素检查；测试 `TestCliqueCheckEqualsNaive`（同图同 PEO 下逐节点与独立朴素实现比对）。
4. **失败不留痕**：`ug/ug.go` 的 `AddEdge` 先完成全部校验再写邻接表，`api/api.go` 的 `New` 先校验 n；测试 `TestRejectedEdgesLeaveNoTrace`（api 包，验四种哨兵错误互异、被拒后 EdgeCount 不变且可继续加边）。

复杂度佐证：`mcs` 非导出字段 `probes` 记录每次选点检查的堆项数，测试 `TestProbeBoundIsolatedNodes`（m=100…10000 孤立点，每步 ≤2）证明是堆而非全表扫描；该字段不经任何导出接口暴露。
