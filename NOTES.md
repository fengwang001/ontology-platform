# NOTES：按时间分区的增量物化与滚动清理

## 第三节推导（size=10，R=3，Key 全为 "k"，窗口 [cur-2, cur]）

| 步 | TS | 桶k=floor(TS/10) | 该桶计数 | cur | Dropped |
|---|---|---|---|---|---|
| 1 | 5 | 0 | 1 | 0 | 0 |
| 2 | -12 | -2 | 1 | 0 | 0 |
| 3 | 0 | 0 | 2 | 0 | 0 |
| 4 | 10 | 1 | 1 | 1 | 1（清桶-2，弃1） |
| 5 | -1 | -1 | 1 | 1 | 1（-1≥cur-2，未迟到） |
| 6 | 20 | 2 | 1 | 2 | 2（清桶-1，弃1） |
| 7 | 9 | 0 | 3 | 2 | 2 |
| 8 | 30 | 3 | 1 | 3 | 5（清桶0，弃3） |

终态：保留桶 {1:1, 2:1, 3:1} 共 3 个，Dropped=5。

- **(甲)** 用 Go 截断除法：第2步 `-12/10=-1`（应 -2），第5步 `-1/10=0`（应 -1）。第5步的 TS=-1 错进桶 0，使第 7 步后桶 0 计数错成 **4**（正确 3）。
- **(乙)** 清理错写成 `k < cur-R`（窗口 [cur-3, cur]）：第8步后桶 0 仍被保留（0<0 不成立），保留桶 {0,1,2,3} 共 **4** 个；只清了桶 -2、-1 各 1 条，Dropped 错成 **2**（正确：3 桶、Dropped=5）。
- **(丙)** 第8步后 TS=-5 → k=-1 < cur-2=1，属**迟到事件**：直接丢弃、不重开已清理的桶，Dropped=**6**。若「重新打开已清理的桶」：桶 -1 被重建计数 1，保留桶错成 4 个 {-1,1,2,3}，Dropped 错留 5。因为迟到判定取决于事件到达时的 cur，同一批事件不同到达顺序会得到不同的被保留事件集，故结果依赖到达顺序；不变量 1 必须写成「只取被保留的事件」，否则视图无法对应任何批量重算结果。

## 四条不变量的保证位置与钉住测试

1. **与批量重算一致**：`tpart.Part.Add` 逐事件累加、`advance` 清理时把被清桶计数计入 dropped；测试 `api_test.TestBatchRecomputeConsistency`。
2. **桶归属正确**：`tbucket.FloorDiv` 对负 a 向下取整（非 Go 截断）；测试 `tpart_test.TestFloorDiv`。
3. **滚动与保留**：`tpart.advance` 中 cur 只进不退、`lo=cur-R+1` 单调推进并清 `<lo` 的桶；测试 `api_test.TestRetentionWindow`。
4. **失败不留痕**：`api.New` 先校验 size/R、`api.Feed` 先整批校验 Key 再落地；测试 `api_test.TestRejections`。
