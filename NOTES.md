# 支配树：第三节推导 + 不变量落点

## 推导（n=7，边 0→1 0→6 1→2 1→6 2→3 2→4 3→5 4→5）

支配集 dom(v)={v}∪⋂(前驱 dom)：dom(0)={0}，dom(1)={0,1}，dom(2)={0,1,2}，dom(3)={0,1,2,3}，
dom(4)={0,1,2,4}，dom(5)={0,1,2,5}，dom(6)={0,6}。

| 节点 | 直接前驱 | 前驱支配集的交集 | idom |
|---|---|---|---|
| 0 | 无（入口） | — | 0（定义） |
| 1 | {0} | {0} | 0 |
| 2 | {1} | {0,1} | 1 |
| 3 | {2} | {0,1,2} | 2 |
| 4 | {2} | {0,1,2} | 2 |
| 5 | {3,4} | dom(3)∩dom(4)={0,1,2} | 2 |
| 6 | {0,1} | dom(0)∩dom(1)={0} | 0 |

(甲) DFS 从 0、邻接升序的访问序为 0,1,2,3,5,4,6，parent(6)=1，idom(6) 会错成 **1**（正确 **0**）。DFS 父节点只记录「第一条到达路径」的前驱；0→6 直达边绕开了 1，支配要求**每条**路径都经过，必须取所有前驱支配集的交集。
(乙) 只取第一个直接前驱（登记序 3→5 先于 4→5），idom(5) 错成 **3**（正确 **2**）：3 只在经 3 的路径上，4→5 绕开了 3，故 3 不支配 5。
(丙) IDom(7) 应返回 **-1**，Dominates(0,7) 应返回 **false**。若错把 idom(7)=0，Dominates(0,7) 会错报 **true**——断言「0 到 7 的每条路径都过 0」，但根本不存在这样的路径；IDom(7) 也错报 0 而非哨兵 -1。

## 四条不变量：代码位置 + 钉住它的测试

1. 支配定义正确：`dom.Tree.Dominates` 用支配树 DFS 进出区间 O(1) 判定（dom/dom.go），区间由 CHK 定点算出的 idom 树生成；`TestDominatesBruteForce`（删点可达性对照定义）、`SelfCheck` 钉住。
2. idom 正确：CHK 的 intersect 沿 idom 链向上合并，保证取到最深支配者（dom/dom.go `Compute`）；`TestIDomTable`、`SelfCheck` 钉住。
3. 与朴素定点一致：`TestNaiveConsistency` 对多档随机图逐节点比对「前驱支配集交集」朴素定点结果。
4. 失败不留痕：`dgraph.AddEdge`/`dgraph.New` 先全部校验、通过后才改状态（dgraph/dgraph.go）；`TestErrorsDistinctAndStateUnchanged` 钉住。
