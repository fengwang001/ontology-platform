# 变更流「序号 ↔ 位点」双索引 NOTES

## 第三节推导（segmentSize=3，每分段首条记锚点，锚点=(Seq, 起始Offset)）

| 步 | Seq | len | 起始Offset | 结束Offset | 新增锚点 |
|----|-----|-----|-----------|-----------|----------|
| 1 | 1  | 10 | 0   | 10  | (1, 0)  |
| 2 | 2  | 15 | 10  | 25  | 无      |
| 3 | 5  | 10 | 25  | 35  | 无      |
| 4 | 9  | 20 | 35  | 55  | (9, 35) |
| 5 | 10 | 5  | 55  | 60  | 无      |
| 6 | 20 | 20 | 60  | 80  | 无      |
| 7 | 21 | 20 | 80  | 100 | (21, 80)|
| 8 | 22 | 20 | 100 | 120 | 无      |

- (甲) `LocateSeq(10)` = **55**：取最后 Seq≤10 的锚点 (9,35)，从 35 起扫 9@35、10@55。若错用「第一个 Seq≥s 的锚点」，取到 (21,80)，从 80 向后扫 Seq 21、22 永远到不了 10，**错报「不存在」**。
- (乙) `LocateSeq(9)` = **35**（目标恰为锚点 (9,35)）。若锚点错记为该分段首条的**结束** Offset，锚点变 (9,55)，`LocateSeq(9)` **错返回 55**。
- (丙) `LocateSeq(15)`：15 ∈ [1,22] 但从未出现 → 返回**「不存在」错误**。若错当成「返回最后 Seq≤s 的已存在事件」，会**错返回 Seq=10 的 Offset 55**。越界：目标早于首事件或晚于末事件（Seq<1 或 Seq>22，Offset 同理）时触发；不存在：落在 [首,末] 范围内但该 Seq/Offset 从未出现时触发。两者哨兵必须不同。

## 四条不变量（保证位置 + 钉住它的测试）

1. **与朴素参照一致**：`index/index.go` 的 `LocateSeq`/`LocateOffset` 用「最后一个 ≤ 目标的锚点 + 线性扫描」，与从 0 累加等价；测试 `TestLocateMatchesNaive`。
2. **锚点自洽**：`index/index.go` 的 `Verify` 逐锚点查 Seq、Offset 严格递增，并从前任锚点起逐条扫描核对 Offset；测试 `TestAnchorsConsistent`。
3. **损坏可判定且可恢复**：`Verify` 报 `ErrCorrupt`，`Rebuild` 清空锚点从头重扫日志重建；测试 `TestCorruptVerifyRebuild`。
4. **失败不留痕**：`index/index.go` 的 `New`/`Append` 先校验后变更，越界查询在扫描前返回，任何拒绝不改状态；测试 `TestRejectedAppendKeepsState`。
