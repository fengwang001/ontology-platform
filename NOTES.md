# NOTES

## 一、五对凸多边形交集（CCW；A 依次被 B 每条有向边的左半平面裁剪）
| | A | B | A∩B |
|---|---|---|---|
| P1 | (0,0)(4,0)(4,4)(0,4) | (2,2)(6,2)(6,6)(2,6) | (2,2)(4,2)(4,4)(2,4) |
| P2 | (0,0)(2,0)(2,2)(0,2) | (3,3)(5,3)(5,5)(3,5) | 空（包围盒不相交） |
| P3 | (0,0)(4,0)(0,4) | (1,1)(5,1)(5,5)(1,5) | (1,1)(3,1)(1,3) |
| P4 | (0,0)(4,0)(4,2)(0,2) | (2,0)(6,0)(6,4)(2,4) | (2,0)(4,0)(4,2)(2,2) |
| P5 | (0,0)(4,0)(4,4)(0,4) | (4,0)(8,0)(8,4)(4,4) | 空（共边 x=4，面积为 0） |

- (甲) P1=(2,2)(4,2)(4,4)(2,4)。符号写反后：边 (2,2)→(6,2) 保留 y≤2，A 先被裁成下半矩形；右边 (6,2)→(6,6) 的补半平面 x≥6 再把 A 整体裁光，交集错成**空**。
- (乙) P2=**空**。朴素裁剪第一条边 y≥3 就输出 0 个顶点；若不做包围盒/空集检测，会把这个 0 顶点列表（相切情形则是 1–2 顶点）包成“非空交集”返回。
- (丙) P5=**空**（零面积即空）。SH 原始输出为退化线段 (4,0)(4,0)(4,4)(4,4)，去重后是线段 (4,0)–(4,4)；不判零面积就会把它当交集返回。

## 二、四条不变量 → 代码保证位置 / 钉住的测试函数
1. 与朴素裁剪一致：`cint/cint.go` 的 Intersect 按 B 边序逐条调用 `cvex.Clip`；`TestNaiveConsistencyRandom`（随机凸包精确有理比对）、`TestFivePairs`。
2. 凸性自洽、顶点在 A、B 全部半平面内：`cvex/cvex.go` Clip 只保留 left≥0；`cint/cint.go` 顶点数<3 与零面积过滤；`TestCoverageLattice`（逐顶点核验）、`TestSelfCheck`。
3. 覆盖正确（⊆A、⊆B，严格内点必入结果）：bbox 预判只短路真不相交，裁剪含边界；`TestCoverageLattice`（格点严格内点必入结果）、`TestSelfCheck`（见证点严格在 A、B、结果三者内部）。
4. 失败不留痕：`api/api.go` NewPolygon 全部校验通过后才构造 Polygon，Intersect 算完才赋值；`TestSentinelErrors`、`TestNoPartialResult`。
- 复杂度：裁剪次数存于 `cint.Engine.clipCount`（非导出，公开接口不可读）；bbox 不相交即为 0，由 `TestBBoxEarlyOutZeroClips`（m=100/1000/10000）钉住。
