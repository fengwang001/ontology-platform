# NOTES

## 八步推导（maxInFlight=3，key=K，next 初始 1）

| 步 | Seq | 动作 | next | 缓冲 | 已发射 |
|---|---|---|---|---|---|
| 1 | 5 | 缓冲 | 1 | [5] | [] |
| 2 | 2 | 缓冲 | 1 | [2,5] | [] |
| 3 | 4 | 缓冲（恰好 3 条，未超） | 1 | [2,4,5] | [] |
| 4 | 1 | 发射 1，级联发射 2 | 3 | [4,5] | [1,2] |
| 5 | 3 | 发射 3，级联 4,5 | 6 | [] | [1,2,3,4,5] |
| 6 | 6 | 直接发射（==next） | 7 | [] | [1..6] |
| 7 | 7 | 直接发射（==next） | 8 | [] | [1..7] |
| 8 | 2 | Seq<next：重复丢弃 | 8 | [] | [1..7] |

（甲）第 4 步 `1<=1` 被误判为重复而丢弃；此后 `Seq==next` 的分支永远进不去，next 永停 1，没有任何事件能被发射（后续事件只会堆满缓冲后被背压拒绝，或缓冲内幂等），最终已发射 `[]`（正确应为 `[1..7]`）。

（乙）第 4 步只发射 `1`（正确应发射 `1,2`）；`2` 从第 2 步起卡在缓冲队首；第 5～7 步的 3/6/7 均因缓冲已满（3 条上限）被背压拒绝；直到第 8 步到达的 `2==next` 才把 2 发射出去。最终只发射 `[1,2]`，3..7 全缺。

（丙）第 4 步发射 1 后把缓冲整体升序放行，即发射 `1,2,4,5`；`3` 被越过，第 5 步到达时 next 已推进到 6，被判重复丢弃，最终 `[1,2,4,5,6,7]`，`3` 永久丢失（正确 `[1..7]`）。

## 四条不变量的代码落点与钉住测试

1. 顺序无空洞：`fifo/fifo.go` 的 Feed 仅 `Seq==next` 直接发射、级联只在队首 `buf[head]==next` 时逐条放行；由 `TestOrderingNoHoles`、`TestEightStepScenario` 钉住。
2. 与朴素参照一致：`fifo/fifo.go` 仅 `Seq<next` 分支累加 dropped，其余被接受序号必按序发射；由 `TestNaiveReference` 钉住。
3. 背压上界：`fifo/fifo.go` 插入前先判 `Buffered()+1 > max` 即原样拒绝；由 `TestBackpressureBound` 钉住。
4. 失败不留痕：`dispatch/dispatch.go` New 先判非正 max、Feed 先判空 key 后才取锁建态；`fifo/fifo.go` 背压拒绝先于插入（第 60 行），空 key 返回前不触碰 map；由 `TestRejectionLeavesNoTrace`、`TestSentinelErrorsDistinct` 钉住。

探针上界：`fifo/fifo.go` 非导出字段 `probes` + 有序缓冲队首指针（每次级联只看队首一条），由白盒测试 `TestProbeBound` 断言 ≤ m+常数。
