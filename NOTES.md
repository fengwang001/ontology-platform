# NOTES

## 三、分步推导（L=3，H=8；ReadChunk 快照={15:c}；EndChunk 输出 +(12,d),+(15,e)；Poll 输出 +(10,f),-(12),+(19,g)；最终 View={10:f,15:e,19:g}）

|LSN|条目|段|∈[10,20)|读前|修正|Poll|输出|
|---|---|---|---|---|---|---|---|
|1|+10 a|≤L|是|是|否|否|无|
|2|+15 b|≤L|是|是|否|否|无|
|3|+20 x|≤L|否|是|否|否|无|
|4|+15 c|(L,H]|是|是|是|否|无（被 LSN7 覆盖）|
|5|-10|(L,H]|是|是|是|否|无（快照无 10）|
|6|+12 d|(L,H]|是|否|是|否|+(12,d)|
|7|+15 e|(L,H]|是|否|是|否|+(15,e)|
|8|+20 y|(L,H]|否|否|否|否|无|
|9|+10 f|>H|是|否|否|是|+(10,f)|
|10|-12|>H|是|否|否|是|-(12)|
|11|+19 g|>H|是|否|否|是|+(19,g)|

（甲）EndChunk 输出 +(15,c)；Poll 输出 +(10,f),+(19,g)（视图中无 12，LSN10 不输出）；最终视图 {10:f,15:c,19:g}，键 15 错成 "c"，来自 LSN4（应为 LSN7 的 "e"）。
（乙）Poll 输出 +(15,c),+(12,d),+(15,e),+(10,f),-(12),+(19,g)；多出 +(15,c),+(12,d),+(15,e) 三条；键 15 先回退到旧值 "c" 再恢复 "e"；最终视图仍正确，不变量 1 发现不了，违反第 3 条（不重复不回退）。
（丙）闭区间 [10,20]：快照={15:c,20:x}，EndChunk 输出 +(12,d),+(15,e),+(20,y)，最终视图多出 (20,y)；左开 (10,20)：Poll 输出 -(12),+(19,g)，最终视图少 (10,f)。BeginChunk(20,30) 被接受（[20,30) 与 [10,20) 不相交）；BeginChunk(19,25) 被拒绝（[19,20) 相交）。

## 二、不变量落实位置与测试

1. 视图一致：chunk.go `EndChunk` 修正 + `Poll` 只应用已完成 chunk 且 LSN>H 的条目；测试 `TestSection3`、`TestConcurrent`。
2. 日志自洽：chunk.go `EndChunk` 按键升序输出、`Poll` 按 LSN 序、Delete 先查视图存在才输出；测试 `TestSection3`（精确输出含 `-(12)` 删存在键）、`TestConcurrent`；另由 demo 的 `SelfCheck` 行做影子重放逐前缀比对。
3. 不重复不回退：chunk.go `Poll` 过滤条件 `lsn > d.h`；测试 `TestSection3`（Poll 精确输出无 LSN≤H 重发）、`TestConcurrent`（输出总数比对）。
4. 失败不留痕：chunk.go 各操作先算后提交（克隆视图、超限整体拒绝、pos 不推进）；测试 `TestRejectNoTrace`。

另：第四节计数器为 chunk.go 非导出字段 `checked`，测试 `TestCheckedNotLinearInM`（同包直接读字段，不经导出接口）。
