# ontology-610 模幂 NOTES

## 一、八行分步表（每行独立，结果 ∈ [0, mod)）

| base | exp | mod | 归一化底数 | 平方乘关键步骤 | 结果 |
|---|---|---|---|---|---|
| -2 | 3 | 5 | 3 | 3²=9≡4；4·3=12≡2 | 2 |
| -3 | 2 | 7 | 4 | 4²=16≡2 | 2 |
| -3 | 3 | 7 | 4 | 4²≡2；2·4=8≡1 | 1 |
| 7 | 100 | 13 | 7 | 7¹²≡1，100=8·12+4；7²≡10，7⁴≡10²=100≡9 | 9 |
| -5 | 3 | 9 | 4 | 4²=16≡7；7·4=28≡1 | 1 |
| 2 | 0 | 5 | 2 | exp=0 且 mod>1 → 1 | 1 |
| 5 | 0 | 1 | 0 | mod=1 → 一切余 0 | 0 |
| 2 | 10 | 1 | 0 | mod=1 → 一切余 0 | 0 |

(甲) `PowMod(-2,3,5)`：不归一化时 (-2)³=-8，Go 的 `%` 保留被除数符号，-8%5=**-3**（错值）；正确值 **2**。
(乙) `(2^32)² mod 1000000007`：2^32≡294967268，294967268²=87005689191383824≡**582344008**（正确值）。朴素 int64 相乘 2^32·2^32=2^64 静默回绕为 **0**，得错值 **0**。
(丙) `PowMod(5,0,1)`：mod=1 时任何整数余 0，正确值 **0**。若先判 `exp==0` 直接返回 1，错成 **1**，且越出 `[0,1)`。

## 二、四条不变量落点

1. **朴素参照一致**：`pow.PowMod` 平方乘主循环（pow/pow.go）；`TestPowModNaiveReference` 用 math/big 参照对 exp≤12 逐条比对。
2. **模语义正确**：`mod.Normalize` 把 base 归一化到 [0,m)，循环内只经 `mod.MulMod`；`TestModSemantics` 钉住结果∈[0,mod)、负 base 等价、mod=1 恒 0。
3. **指数拆分正确**：平方乘按位拆分指数天然满足同态；`TestExponentSplit` 用 MulMod 组合两侧断言相等。
4. **失败不留痕**：`pow.PowMod` 入口三个分支分别返回 ErrZeroModulus/ErrNegativeModulus/ErrNegativeExponent；`TestErrorSentinels` 断言三者互不相同、不 panic、无半成品。
