# 全局窗口周期触发 NOTES

## 三、分步推导（period=3，元素依次 10,20,30,40,50,60,70,80）

| 步 | Val | 之后 sum | 之后 cnt | 触发快照 |
|----|-----|------|------|----------|
| 1 | 10 | 10  | 1 | 无 |
| 2 | 20 | 30  | 2 | 无 |
| 3 | 30 | 60  | 3 | (60, 3) |
| 4 | 40 | 100 | 4 | 无 |
| 5 | 50 | 150 | 5 | 无 |
| 6 | 60 | 210 | 6 | (210, 6) |
| 7 | 70 | 280 | 7 | 无 |
| 8 | 80 | 360 | 8 | 无 |

- (甲) 第 6 步正确快照 **(210, 6)**。若错成「触发即清零」，第 3 步后 sum/cnt 归 0，第 4–6 步重新累计 40+50+60，输出 **(150, 3)**：sum 少 60（丢了前 3 条之和）、cnt 少 3。
- (乙) 若错成 `cnt%3==1`：第 **1、4、7** 步触发，第一次快照 **(10, 1)**；正确实现应在第 **3、6** 步触发。
- (丙) 第 3 步正确快照 **(60, 3)**。若判定错放在加入元素之前，快照不含第 3 条，输出 **(30, 2)**（sum=10+20，cnt=2）。

## 二、四条不变量的落点

1. 批量重算一致：触发时快照直接取运行中的 sum/cnt，O(1) 不重扫（`gwin/gwin.go` Apply 触发分支）；测试 `TestBatchRecompute`。
2. 触发不重置：全系统只有 `gw.Acc.Add` 改 sum/cnt，`Fired` 是纯判定（`gw/gw.go`）；测试 `TestNoReset`。
3. 快照单调：只在 `cnt>0 && cnt%period==0` 时追加快照且 cnt 只增不减（`gwin/gwin.go` Apply）；测试 `TestMonotonic`。
4. 失败不留痕：`Apply` 先校验全部 key、再模拟各 key 触发数核对 maxSnap，全部通过后才落状态，任一拒绝整体不生效（`gwin/gwin.go`）；测试 `TestFailureAtomic`。
