# Rabin-Karp 滚动哈希笔记

## 碰撞回退与取模推导

1. 取整数字母表值 `v(c)`（此处 `v(c)=c-'a'+1`，base=10，mod=7），窗口哈希定义为
   `H(s) = (v(s0)*base^(m-1) + ... + v(s_{m-1})) mod mod`，
   追加字符用 `h = (h*base + v(c)) % mod` 增量更新。
2. `H(a)=H(b)` 只说明两子串哈希同余。不同子串可同余：`H("b")=2` 与 `H("aa")=11` 均同余 4（mod 7）。
   故哈希相等是匹配的必要条件、非充分条件；命中后必须回退逐字符验证，否则碰撞产生假阳性。
3. 滑窗移除最高位：`h' = h*base + v(in) - v(out)*base^m`。末项可能使括号内为负，
   Go 的 `%` 对负数保留负号，会得到负哈希并污染后续滚动。正确写法为先加一个 mod 再取模：
   `h = (h - v(out)*base^(m-1) % mod + mod) % mod`（等价处理均可），保证结果落在 `[0,mod)`。
4. 碰撞用例：text=`"aaaaaab"`、pattern=`"b"`，朴素答案 `[6]`。
   窗口 `"b"`(4) 与 `"aa"`(4) 哈希相等；验证函数只认逐字符全等，故仍返回 `[6]`。
   内联的“命中即成功”错误实现会在位置 0 误报，断言其结果含 0。
5. 复杂度：每窗口 O(1) 滚动；仅哈希命中触发 O(m) 验证，总验证字符数
   ≤ m·(出现次数 + 碰撞次数)，随机输入下接近 m·出现次数，不逐位置 O(m) 退化。

## 代码位置与测试对应（第二节语义）
<!-- 测试写完后补充 -->
