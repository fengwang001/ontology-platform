# FWW 去重寄存器 — 推导与不变量

## 六行分步表（同一 Key=K，写入记为 (Seq,Val)）

| 步 | 到达 | 本步输出 changelog | 生效值 | 累积丢弃 |
|---|---|---|---|---|
| 1 | (7,a) | +(K,7,a) | (7,a) | 0 |
| 2 | (3,b) | -(K,7,a) +(K,3,b) | (3,b) | 0 |
| 3 | (10,c) | 无 | (3,b) | 1 |
| 4 | (1,d) | -(K,3,b) +(K,1,d) | (1,d) | 1 |
| 5 | (5,e) | 无 | (1,d) | 2 |
| 6 | (8,f) | 无 | (1,d) | 3 |

(甲) 误实现为「最先到达者胜」：第 2 步 (3,b) **不会**撤回 (7,a)（该实现到达即定、永不撤回），(3,b) 被丢弃；最终生效值错成 **(7,"a")**，正确值是 **(1,"d")**。
(乙) LWW（保留最大 Seq）：第 3 步 (10,c) 输出 **-(K,7,a)** 和 **+(K,10,c)** 两条，生效值变 (10,c)；之后 1,3,5,8 全落败，最终错成 **(10,"c")**（正确 (1,"d")）。逐点相反：FWW 保留最小 Seq、更小者到达时撤回已物化的较大者、较大者到达时丢弃；LWW 保留最大 Seq、更大者到达时撤回较小者、较小者到达时丢弃——保留谁、撤回谁、丢弃谁全部相反。
(丙) 另一到达顺序：(1,d),(7,a),(3,b),(10,c),(5,e),(8,f)。最终生效值仍为 (1,"d")，但 changelog 只有一条 +(K,1,d)，撤回 **0** 次（原顺序 2 次），丢弃 5。差异来自「只有更小的 Seq 在较大者已物化之后到达才触发撤回」这条规则：撤回次数由到达顺序决定，终值不由到达顺序决定。不变量 1 必须写「按 Seq 取最小」而非「按到达取首个」：到达顺序是传输层的偶然（乱序、重试、并发），按到达取首个会让同一批写入因到达顺序不同而得到不同视图，必然与批量重算结果不一致。

## 四条不变量：保证位置与钉住它的测试

1. 与批量重算一致：`reg.Reg.Apply` 仅在 `seq < 当前Seq` 时替换（reg/reg.go），故终值即最小 Seq；测试 `TestViewMatchesBatch`（api/api_test.go）。
2. changelog 自洽：撤回记录由 `reg.Apply` 返回的旧 (Seq,Val) 原样生成，先 `-` 后 `+`（fww/fww.go）；测试 `TestChangelogConsistent`（fww/fww_test.go）。
3. 生效 Seq 单调不增：同 1，替换条件严格为更小 Seq（reg/reg.go）；测试 `TestSeqMonotone`（fww/fww_test.go）。
4. 失败不留痕：`fww.Feed` 先在克隆的寄存器表上试算整批，全部成功才提交状态（fww/fww.go）；测试 `TestFailureAtomic`（api/api_test.go）。
