# 按时间分区的增量物化与滚动清理 — 推导与不变量

## 八行分步表（size=10，R=3，Key 全为 "k"）

| # | TS  | 桶键 k | 该桶计数 | cur | Dropped |
|---|-----|--------|----------|-----|---------|
| 1 | 5   | 0      | 1        | 0   | 0       |
| 2 | -12 | -2     | 1        | 0   | 0       |
| 3 | 0   | 0      | 2        | 0   | 0       |
| 4 | 10  | 1      | 1        | 1   | 1（清桶 -2，丢 1） |
| 5 | -1  | -1     | 1        | 1   | 1       |
| 6 | 20  | 2      | 1        | 2   | 2（清桶 -1，丢 1） |
| 7 | 9   | 0      | 3        | 2   | 2       |
| 8 | 30  | 3      | 1        | 3   | 5（清桶 0，丢 3） |

最终保留桶 {1,2,3} 各计 1，共 3 个桶，Dropped=5。

- **(甲)** 用 Go 截断除法：`-12/10=-1`（错，应 -2）、`-1/10=0`（错，应 -1）。第 5 步事件错进桶 0，桶 0 在第 7 步计数错成 **4**（正确 3）。
- **(乙)** 清理错写成 `k < cur-R`：第 8 步后桶 0 被**错留**，保留 4 个桶 {0,1,2,3}；桶 -2 迟至第 6 步、桶 -1 迟至第 8 步才被清，Dropped 错成 **2**（正确 5）。
- **(丙)** 第 8 步后 `TS=-5` → 桶 -1 < cur-R+1=1，属**迟到事件**，直接丢弃，Dropped=**6**。若「重新打开已清理的桶」：保留桶错成 4 个 {-1,1,2,3}、Dropped 错留 5。同一事件在清理前到达会计数、之后到达被丢弃，故结果依赖到达顺序；不变量 1 必须写成「只取被保留（未被丢弃）的事件」，否则批量重算会把已被清理/迟到的事件也算进去。

## 四条不变量：保障位置与钉住它的测试

1. **与批量重算一致**：`tpart.Feed` 只累加窗口内事件、清理即计入丢弃；`api.batchRecompute` 只统计被保留事件。测试 `TestViewMatchesBatchRecompute`（api 包，随机序列多档 size/R）。
2. **桶归属正确**：`tbucket.FloorDiv` 在 `a<0 且 a%b!=0` 时 `q--`，负安全。测试 `TestFloorDiv`（tpart 测试文件内，含 -12/-1 等负例）。
3. **滚动与保留**：`cur` 仅在更大桶键到达时推进；`cleanup` 按有序桶键删除 `< cur-R+1` 前缀。测试 `TestRetentionBounds`（api 包，随机喂数后逐桶核对窗口）。
4. **失败不留痕**：`New`/`Feed` 先整体校验后落地，`Feed` 先验全批 Key 再逐条应用。测试 `TestRejectionLeavesState`（tpart 测试文件内）。
