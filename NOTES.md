# 视图依赖 DAG 按批拓扑增量刷新 — 推导与不变量

## 第三节推导（DAG：v1←b1，v2←b2，v3←v1+v2，v4←v3+b3）

| # | SetBase | 脏视图集合 | b1末值 | b2末值 | b3末值 |
|---|---------|-----------|----|----|----|
| 1 | b1=2 | {v1,v3,v4} | 2 | 0 | 0 |
| 2 | b3=7 | {v1,v3,v4} | 2 | 0 | 7 |
| 3 | b2=4 | {v1,v2,v3,v4} | 2 | 4 | 7 |
| 4 | b1=5 | {v1,v2,v3,v4} | 5 | 4 | 7 |
| 5 | b2=1 | {v1,v2,v3,v4} | 5 | 1 | 7 |
| 6 | b3=2 | {v1,v2,v3,v4} | 5 | 1 | 2 |
| 7 | b1=3 | {v1,v2,v3,v4} | 3 | 1 | 2 |
| 8 | b2=6 | {v1,v2,v3,v4} | 3 | 6 | 2 |
| 9 | Refresh | 刷新顺序 v1→v2→v3→v4 | 日志：-(v1,0)+(v1,3)；-(v2,0)+(v2,6)；-(v3,0)+(v3,9)；-(v4,0)+(v4,11) | | |

- (甲) 正确顺序：v1,v2,v3,v4（字典序拓扑）。若按树路径展开（b1→v1→v3→v4、b2→v2→v3→v4、b3→v4）：v3 被刷 **2 次**、v4 被刷 **3 次**；v4 首刷在 b1 路径末尾，此时 v2 未刷（旧值 0）、v3 刚刷成 v1+旧v2=3+0=3，故 v4=3+b3=**5**（正确值 11）。
- (乙) 只刷直接依赖者：脏集={v1,v2,v4}。一次 Refresh 后 v3 不刷**仍为 0**（应 9），v4=v3(0)+b3(2)=**2**（应 11）。第 2 轮 v3=9，第 3 轮 v4=11，**共需 3 轮**才收敛。
- (丙) 另一排列：b1=2, b1=5, b1=3, b2=4, b2=1, b2=6, b3=7, b3=2（每个基底的末次写入仍排在其所有写入最后）。第 2–8 行的脏集与末值列均不同（如第 2 行变为 {v1,v3,v4}/b1=5，第 4 行才首次出现全集）；第九行 Refresh 输出**相同**，最终视图**相同**。批内顺序无关由「同批同基底只末值生效」+「脏集是变更基底传递闭包之并（集合语义）」保证；跨批顺序有关由「Refresh 一次性消费当前累积批次并清空登记」保证——批次边界不同，各批看到的基底值就不同。

## 四条不变量的落点

1. 与全量重算一致：`rfr.Refresh` 按 `dag.Topo` 拓扑序用依赖当前值求和；测试 `TestFullRecomputeConsistency`（api/api_test.go）。
2. 每层恰好一次：脏集是集合（`dag.Dirty` 去重），`Topo` 每节点恰好发射一次；测试 `TestChangelogShape`。
3. 变更日志序合法：`dag.Topo` 字典序 Kahn，依赖未刷完不发射；测试 `TestChangelogShape`。
4. 失败不留痕：`api` 先校验后落笔、`dag.AddView` 成环整体回滚；测试 `TestErrorsAndNoTrace`。
