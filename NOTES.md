# NOTES — ontology-314 时间索引 Lookup

New(100,1000)，一次 Append：50 40 70 60 70 65 90 80 90 85（位点 100..109）

| off | TS | 迄今最大 | 入索引 | 追加后完整索引 |
|---|---|---|---|---|
| 100 | 50 | 50 | 是（首条必记） | (50,100) |
| 101 | 40 | 50 | 否 | (50,100) |
| 102 | 70 | 70 | 是（70>50） | (50,100),(70,102) |
| 103 | 60 | 70 | 否 | (50,100),(70,102) |
| 104 | 70 | 70 | 否（等于最大值不记） | (50,100),(70,102) |
| 105 | 65 | 70 | 否 | (50,100),(70,102) |
| 106 | 90 | 90 | 是（90>70） | (50,100),(70,102),(90,106) |
| 107 | 80 | 90 | 否 | (50,100),(70,102),(90,106) |
| 108 | 90 | 90 | 否（等于最大值不记） | (50,100),(70,102),(90,106) |
| 109 | 85 | 90 | 否 | (50,100),(70,102),(90,106) |

(甲) Lookup(45)=(100,true)，Lookup(85)=(106,true)。误在原始切片上 sort.Search：45 时 a[5]=65、a[2]=70 两次判真即收窄到下标 2 → 误返位点 102；85 时 a[5]=65 假、a[8]=90 真、a[7]=80 假 → 误返位点 108。

(乙) Lookup(65)=(102,true)（102 的 70 是第一条 >=65）。误做「<=t 的最后一条」会返位点 105（TS=65）；从 105 重读漏掉 102(70)、104(70)。

(丙) Lookup(80)=(106,true)，Lookup(91)=(110,false)（LEO）。只认精确相等：80 误返 107（漏掉 106 的 90）；超界返末条：91 误返 109，多读 109（TS=85<91，本不该重读）。

不变量与保证位置 / 钉住的测试：
1. 与朴素参照一致：tlog.go Lookup 委托 tidx.go 下界二分（首个 TS>=t 索引项恰是首条命中消息），TestNaiveReference 钉住。
2. 索引严格单调、项 TS=前缀最大：tidx.go Add 仅在 ts>末项 TS 时追加，TestIndexInvariants 钉住。
3. 查询单调：tidx.go Lookup 的 sort.Search 下界语义，TestLookupMonotonic 钉住。
4. 失败不留痕：tlog.go Append/New 先全部校验、后改任何状态，TestRejectedBatchLeavesNoTrace 钉住。

复杂度：tidx.go 非导出 probes 字段记录最近一次 Lookup 探查项数；白包测试 TestTidxProbeBound 直读字段，断言 probes <= 2*ceil(log2(m+1))+4（m=100/1000/10000）。
