# NOTES

记 A=`3e11fa47-71ca-11e1-9e33-c80aa9429562`，B=`8a94f357-aab4-11df-86ab-c80aa9429562`，maxIntervals=4。下表 Executed 列用 A/B 代全 UUID（=小写 UUID 原文）。

| 步 | 结果 | Executed() 规范文本 | 区间总数 | Missing 返回 |
|---|---|---|---|---|
| 1 | 成功 | A:1-5:7 | 2 | 无 |
| 2 | 成功 | A:1-7（6 把 1-5 与 7 相邻合并） | 1 | 无 |
| 3 | 成功 | A:1-12,B:10-12:20-25 | 3 | 无 |
| 4 | ErrRange（B:5-3 即 a>b），整体拒绝，A:13-14 不并入 | A:1-12,B:10-12:20-25 | 3 | 无 |
| 5 | 成功 | A:1-12,B:10-14:20-26 | 3 | 无 |
| 6 | 成功（合计 4，不超限） | A:1-12,B:10-14:16:18-26 | 4 | 无 |
| 7 | 成功（只读） | A:1-12,B:10-14:16:18-26 | 4 | A:13-15,B:1-9:15:17:27-30 |
| 8 | 成功（只读） | A:1-12,B:10-14:16:18-26 | 4 | A:13 |

（甲）只合重叠不合相邻：第 1 步后 `A:1-5:7`；第 2 步后 `A:1-5:6:7`；第 3 步并入后 A 有 1-5、6、7、8、9-12 共 5 段、B 有 2 段，合计 7>4，结果 ErrTooMany、整体拒绝，第 3 步后 Executed 仍为 `A:1-5:6:7`。
（乙）差集端点两处 off-by-one：第 7 步错成 `A:12-15,B:1-10:16-18:26-30`（10、12、16、18、26 被算成缺失）；第 8 步错成 `A:12-13`。
（丙）同 UUID 后条目覆盖前条目：第 6 步 `B:18-19` 被 `B:16` 覆盖，Executed 错成 `A:1-12,B:10-14:16:20-26`；第 7 步错成 `A:13-15,B:1-9:15:17-19:27-30`。

## 不变量在代码中的保证位置与钉住测试

1. 与朴素参照一致：差集端点由 ivl.Set.Subtract（二分定位+逐相交区间推进）保证，并集由 normalize 保证；测试 TestNaiveRandom（api_test.go，随机集合展开成整数集独立求差/并后逐字节比对）钉住。
2. 规范形唯一：ivl.normalize 排序并按「重叠或相邻」合并（MaxInt64 防溢出），ivl.Format/gtid.canonical 按 UUID 字典序、单点写 n；测试 TestCanonicalIdempotent（重复解析往返、多切分多顺序同输出）钉住。
3. 差集互补：api.Missing 为纯只读、Subtract 输出与已执行集无交；测试 TestDifferenceComplement（Apply 缺失集后再 Missing 为空且仅多出这些事务）钉住。
4. 失败不留痕：gtid.Parse 整体返回错误即不落库；api.Apply 先在副本上构造候选、区间合计 >max 才返回 ErrTooMany，全程不碰原状态；测试 TestRejectedApplyLeavesState（含空白/UUID/0/超限及第 4 步 A:13-14 不并入）钉住。
