# 大值溢出存储 · 推导与不变量（T=4）

## 八步分步表（计数 = 下一个待分配块号，从 1 起，永不复用）

| # | 操作 | a | b | c | 溢出块集合 | 计数 |
|---|------|---|---|---|-----------|------|
| 1 | Put a="1234" | 内联"1234" | — | — | {} | 1 |
| 2 | Put b="12345" | 内联 | →块1 | — | {1} | 2 |
| 3 | Put b="xy" | 内联 | 内联"xy" | — | {}（块1已回收） | 2 |
| 4 | Put c="12345" | 内联 | 内联 | →块2 | {2} | 3 |
| 5 | Put c="1234567890" | 内联 | 内联 | →块3 | {3}（块2已回收） | 4 |
| 6 | Get c → "1234567890" | 内联 | 内联 | →块3 | {3} | 4 |
| 7 | Del b | 内联 | — | →块3 | {3} | 4 |
| 8 | Put a="1234567" | →块4 | — | →块3 | {3,4} | 5 |

(甲) 第1步 len=4 ≤ T=4 → **内联**。若错写成 `len(v) >= T` 算溢出：第1步会错出一个本不该有的溢出块1（a→块1、集合{1}、计数2）。T=4 时长度 1–4 内联，≥5 溢出。
(乙) 第5步旧块2**被回收**，集合{3}。若忘回收 → 泄漏：块2成孤儿，溢出块数错成 2（{2,3}）。若顺序颠倒（先回收块2再改 ref[c]），两步间崩溃 → ref[c]→2 指向已删块，留下悬挂引用。
(丙) 崩溃留下孤儿块5，Recover 回收 **1** 块，返回 `(1, [], nil)`，集合回到 {3,4}。若误删被引用块（如3）→ 块数错成 1 且 c 变悬挂；若漏掉孤儿5 → 块数错成 3（{3,4,5}）。不留悬挂由 Put 铁律保证：**先写新块 → 再更新引用 → 最后回收旧块**，任意中间态引用都指向存在的块（只可能留孤儿，由 Recover 清）。

## 四条不变量 → 代码位置 → 钉住它的测试

1. 与朴素参照一致：`srec.Put/Get/Del` 严格按阈值归属与回收顺序；测试 `api_test.TestNaiveReplay`（多种子随机序列对拍朴素模型）。
2. 引用完整（无孤儿、无悬挂）：`srec.Put` 先写块后挂引用、`Del` 同步删块、`Recover` 扫孤儿验悬挂；测试 `api_test.TestIntegrity`、`srec.TestRecoverOrphan`。
3. 回收原子：`srec.Put` 溢出分支「写新块→更新ref→删旧块」顺序；测试 `api_test.TestReclaimAtomic`（反复更新大值块数恒 1、Recover 零孤儿）。
4. 失败不留痕：`srec.Put` 先校验上限再分配块号，`api` 层校验空 key/非法参数；测试 `api_test.TestFailureAtomic`（拒绝前后状态快照一致、三哨兵错误互异、拒绝后仍可正常使用）。
