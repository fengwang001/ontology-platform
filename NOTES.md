# 延迟物化推导笔记

## 七步分步表（基列 a=2,b=3,c=4；派生 d=a+b, e=d*2, f=c+e）

| 步 | 操作 | 触发计算(依赖序) | 返回 | 复用缓存 |
|---|---|---|---|---|
| 1 | Get(e) | d, e | e=10 | 无 |
| 2 | Get(d) | 无 | d=5 | d |
| 3 | Get(f) | f | f=14 | e |
| 4 | Set(a,5) | 无（仅失效 d,e,f） | — | — |
| 5 | Get(d) | d | d=8 | 无 |
| 6 | Get(f) | e, f | f=20 | d |
| 7 | Get(e) | 无 | e=16 | e |

- (甲) 惰性下 `New` 计算 0 个派生列。急切下 `New` 算 3 个（d,e,f），第 1 步 `Get(e)` 算 0 个；对比惰性（第 1 步算 2 个 d,e）：初始化多 3 次、第 1 步少 2 次。
- (乙) `Set(a,5)` 应失效 d,e,f（传递闭包）。若只失效直接依赖 d：第 6 步 `Get(f)` 命中第 3 步旧缓存，返回 14；正确应为 20（f=c+e=4+16）。
- (丙) 无缓存时第 2 步 `Get(d)` 重算 d 共 1 次（其依赖 a,b 为基列，不再触发派生计算）；正确应为 0 次。

## 四条不变量落点

1. 与朴素重算一致：`mat.Engine.Get` 递归求值、失效即清缓存，等价于每次从头重算；测试 `TestNaiveConsistency`。
2. 缓存一致：缓存只在 `setBase` 失效闭包时清除，写入缓存的值即按当前基列算出的值；测试 `TestCacheConsistency`。
3. 失效正确：`setBase` 用 `dep.Closure` 失效全部传递依赖，未依赖者不动；测试 `TestInvalidation`。
4. 失败不留痕：`New/Get/Set` 全部校验先于任何状态写；测试 `TestFailureAtomic`。
