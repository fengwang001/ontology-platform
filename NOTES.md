# Dijkstra 推导

每个节点保存当前最短距离 `dist[v]`，距离每次变小时将 `(v, dist[v])` 入堆。

堆中因此可能保留同一节点的多条记录。弹出 `(u, d)` 时：

- 若 `d > dist[u]`，该记录是旧距离产生的过期记录，必须跳过（lazy deletion）。
- 若 `d == dist[u]`，该记录代表当前最短距离；非负权下弹出即最终确定，可松弛出边。

不能用过期记录松弛邻居：它对应的旧路径已经被更短路径否定。若继续把旧距离 `d`
传播出去，等于把一个已被废弃、更大的“当前距离”写回图状态，会污染邻居的候选值；
更严重的错误写法还会用 `d` 覆盖节点已确定的最短距离，违反“节点一旦按最小键弹出，
非负权保证其距离不再改变”的贪心不变量，于是后续邻居可能得到错误的更大距离。

跳过过期记录后，每条边只在其源节点的有效记录上检查；有效弹出至多发生在一次最终
确定上，成功松弛次数因此不超过边数 `m`（实现中保留极小常数余量），不是
Bellman-Ford 的 `O(n·m)`。
