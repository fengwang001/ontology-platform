# ontology-501 迟到纠正重处理：推导与不变量

W=3，单 key，到达序：(5,10)(7,20)(6,15)(8,5)(5,99)(6,15)(6,30)(9,3)。窗口按首次到达序，新 Seq 入队、超 W 淘汰最早者；淘汰只出窗，Val 与 sum 保留。

| 步 | 事件 | sum | 窗口（首次到达序） | 类别 | 变更日志 |
|---|---|---|---|---|---|
| 1 | (5,10) | 10 | [5] | 新 | +10 |
| 2 | (7,20) | 30 | [5,7] | 新 | -10,+30 |
| 3 | (6,15) | 45 | [5,7,6] | 迟到新 | -30,+45 |
| 4 | (8,5) | 50 | [7,6,8]（淘汰5） | 新 | -45,+50 |
| 5 | (5,99) | 50 | [7,6,8] | 过期 | 无 |
| 6 | (6,15) | 50 | [7,6,8] | 重复 | 无 |
| 7 | (6,30) | 65 | [7,6,8]（顺序不动） | 纠正 | -50,+65 |
| 8 | (9,3) | 68 | [6,8,9]（淘汰7） | 新 | -65,+68 |

(甲) 5 已在第 4 步被淘汰出窗 → 过期纠正：拒绝，sum=50，无产出。不设窗口、任何 Seq 皆可纠正的错实现：50+(99-10)=**139**。
(乙) (6,15) 与现值相同 → 幂等 no-op：sum=50，无产出，窗口/stale 皆不动。不去重、把重复再累加一次的错实现：50+15=**65**。
(丙) 6 从未见过，虽小于当前 max=7，仍是迟到的**新数据**：接受，sum=45，产出 -30,+45。把一切乱序（seq<max）都拒绝的错实现：第 3 步被拒，sum 错停在 **30**。根本区别：迟到针对「从未见过的 Seq」（新信息，加法可交换、与到达序无关，必须收）；过期纠正针对「已见 Seq 改值」（旧值已随窗口淘汰而落定，拒）。分界是 Seq 曾否见过，而非 Seq 数值大小。

不变量（代码保证位置 / 钉住的测试函数）：
1. 与批量重算一致：`agg.Agg.Apply` 对 vals 做增量调整（新 Seq +=val、纠正 +=(new-old)、出窗只 delete win 不动 vals/sum）；TestBatchRecompute（多档随机序列对朴素重算）、TestEightStep。
2. 变更日志自洽：`api.API.Apply` 只发「首事件单 +」或「-,+」两种形状；`chlog.Log.Append` 强制 + 时键缺席、- 时恰撤现值；TestChangelogPrefixes（逐前缀用新 Log 重放）、TestSelfCheck。
3. 幂等：agg 重复分支不动 vals/sum/order/win，api 对其不发变更、不增 stale；TestIdempotent（每事件再投一次）、TestSelfCheck。
4. 失败不留痕：`api.New` 先校验 W，`api.Apply` 在加锁与建 key 之前拒绝空 key 与 seq<=0，三哨兵互异；TestRejectedNoTrace（拒绝前后 View/Stale 全等，之后仍可正常用）。
