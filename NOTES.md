# NOTES：原子批次物化器

## 一、八事件批的逐步推导（初始视图 {k1:1, k2:2}）

| # | 事件 | 批内累积状态（相关键） | 前置期望 | 是否满足 | 预演 |
|---|------|------------------------|----------|----------|------|
| 1 | Put(k3,30,∅) | k3 不存在（视图无、批内无前序） | 要求不存在 | 满足 | 继续，累积 k3=30 |
| 2 | Put(k1,10,&"1") | k1=1（取自真实视图） | 要求等于 "1" | 满足 | 继续，累积 k1=10 |
| 3 | Del(k2,&"2") | k2=2（取自真实视图） | 要求等于 "2" | 满足 | 继续，累积 k2=已删 |
| 4 | Put(k2,20,∅) | k2 不存在（第 3 步已删） | 要求不存在 | 满足 | 继续，累积 k2=20 |
| 5 | Put(k4,40,∅) | k4 不存在 | 要求不存在 | 满足 | 继续，累积 k4=40 |
| 6 | Del(k3,∅) | k3=30（第 1 步刚建立） | 要求不存在 | **不满足** | **停止，整批拒绝** |
| 7 | Del(k4,∅) | —（未评估） | — | — | 不执行 |
| 8 | Put(k5,50,∅) | —（未评估） | — | — | 不执行 |

- **(甲)** 第 4 步应当**通过**：判定基于批内累积状态，第 3 步已把 k2 删掉，"必须不存在"成立。只对照真实视图的实现会看到 k2=2 仍存在，误报"期望不存在但键已存在"的前置失败，把第 4 步当成第一条失败事件——整批被拒且返回了**错误的失败事件**（正确语义下首失败是第 6 步）。
- **(乙)** 第 6 步是第一条失败事件：nil 期望要求 k3 不存在，但批内第 1 步已令 k3=30。"应用成功前缀"的错误实现会把第 1–5 步落盘，视图错成 **{k1:10, k2:20, k3:30, k4:40}**；正确视图保持 **{k1:1, k2:2}** 逐键不变。
- **(丙)** 批 `Put(k1,x,&"1")` → `Put(k1,y,&"x")`：第 1 条对照视图 k1=1 通过，累积 k1=x；第 2 条对照累积状态 k1=x，期望 "x" 满足，最终 k1=y。只对照真实视图的实现第 2 条看到 k1=1≠"x" 会错拒。这印证不变量 2（批内顺序语义）。

## 二、四条不变量的保证位置与钉住测试

1. **全或无**：`plan.Rehearse` 先全程预演产出变更集，`api.ApplyBatch` 仅在 err==nil 时一次性替换视图；测试 `TestAllOrNothing`。
2. **批内顺序语义**：`plan.Rehearse` 内 overlay 先查、视图后查，前序 put/del 对后续可见；测试 `TestIntraBatchOrder`。
3. **与朴素参照一致**：`api.SelfCheck` 与 `TestMatchesNaiveReference` 用朴素逐事件应用器对拍多组随机批序列。
4. **失败不留痕**：`ApplyBatch` 出错路径不写视图，错误携带第一条失败事件序号；测试 `TestRejectedBatchLeavesNoTrace`（含空批/空键/前置失败三哨兵互异）。
