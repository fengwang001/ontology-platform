# 跳跃窗口计数 NOTES

## 九步推导（size=12, slide=4, n=size/slide=3，窗口记 [start,end)，只写 start）

| 步 | 操作 | 时钟 | 所属窗口 → 实际计入 | 输出 | 打开窗口(start:count) | 丢弃 |
|---|---|---|---|---|---|---|
| 1 | Add(-5) | -∞ | [-16,-4),[-12,0),[-8,4) → 全部 | 无 | -16:1 -12:1 -8:1 | 0 |
| 2 | Add(0) | -∞ | [-8,4),[-4,8),[0,12) → 全部 | 无 | -16:1 -12:1 -8:2 -4:1 0:1 | 0 |
| 3 | Add(-1) | -∞ | [-12,0),[-8,4),[-4,8) → 全部 | 无 | -16:1 -12:2 -8:3 -4:2 0:1 | 0 |
| 4 | Advance(0) | 0 | — | (K,[-16,-4),1) (K,[-12,0),2) | -8:3 -4:2 0:1 | 0 |
| 5 | Add(-3) | 0 | [-12,0),[-8,4),[-4,8) → 计入 [-8,4),[-4,8)（[-12,0) 已关） | 无 | -8:4 -4:3 0:1 | 0 |
| 6 | Add(-13) | 0 | [-24,-12),[-20,-8),[-16,-4) → 全已关，丢弃 | 无 | -8:4 -4:3 0:1 | 1 |
| 7 | Advance(4) | 4 | — | (K,[-8,4),4) | -4:3 0:1 | 1 |
| 8 | Add(8) | 4 | [0,12),[4,16),[8,20) → 全部 | 无 | -4:3 0:2 4:1 8:1 | 1 |
| 9 | Advance(12) | 12 | — | (K,[-4,8),3) (K,[0,12),2) | 4:1 8:1 | 1 |

**(甲)** TS=-5 属 [-16,-4),[-12,0),[-8,4)；TS=-1 属 [-12,0),[-8,4),[-4,8)。用 Go 截断：-5/4=-1→起点 -4，得 [-4,8),[-8,4),[-12,0)（多 [-4,8) 漏 [-16,-4)）；-1/4=0→起点 0，得 [0,12),[-4,8),[-8,4)（多 [0,12) 漏 [-12,0)）。第 4 步输出错成只有 (K,[-12,0),1)：漏掉 (K,[-16,-4),1)，且 [-12,0) 计数 1≠2。

**(乙)** TS=0 属 3 个：[-8,4),[-4,8),[0,12)。若当闭区间 [start,start+size]：第 2 步计入 4 个（多 [-12,0]），第 8 步计入 4 个（多 [-4,8]）。第 4 步输出错成 (K,[-16,-4],1),(K,[-12,0],3)（[-12,0] 多计 1）；第 9 步错成 (K,[-4,8],4),(K,[0,12],3)（各多计 1）。

**(丙)** 第 5 步计入 [-8,4),[-4,8)（[-12,0) 已关闭）。若把 TS<当前时钟 的事件整条丢弃：第 7 步输出错成 (K,[-8,4),3)；第 9 步错成 (K,[-4,8),2)；最终 Dropped() 错成 2（应为 1）。

## 四条不变量：保证位置与钉住它的测试

1. **与朴素参照一致**：`hagg.Add` 只计入 `end>clock` 的窗口、`Advance` 关闭即输出即清除 → `hagg/hagg_test.go: TestNaiveReference`。
2. **归属数精确**：`hop.Starts` 用 `floorDiv` 向下取整、恒返回 size/slide 个窗口；`hagg.Add` 按 `end>clock` 过滤、全关才 dropped++ → `hagg/hagg_test.go: TestAssignmentCounts`。
3. **输出有序且唯一**：`hagg` 用按 (end,key,start) 排序的堆弹出即有序；关闭即删 map 条目，`end<=clock` 的窗口永不再建 → `hagg/hagg_test.go: TestOrderedUnique`。
4. **失败不留痕**：所有校验（参数/回退/空 Key/maxOpen）先于任何状态变更，maxOpen 先算新增数再落盘 → `api/api_test.go: TestFailureAtomicity`。
