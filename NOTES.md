# KMP 流式匹配器：推导与不变量

## 一、P="abaaba" 前缀函数分步表（i 从 0 到 5）

| i | P[i] | 入行 j=π[i-1] | 比较与回退过程 | π[i] |
|---|------|---------------|----------------|------|
| 0 | a | —（定义） | π[0]=0 | 0 |
| 1 | b | 0 | P[0]='a'≠'b'，j 已为 0 无处可退 | 0 |
| 2 | a | 0 | P[0]='a'='a'，j→1 | 1 |
| 3 | a | 1 | P[1]='b'≠'a'，沿 π 回退 j=π[0]=0；再比 P[0]='a'='a'，j→1 | 1 |
| 4 | b | 1 | P[1]='b'='b'，j→2 | 2 |
| 5 | a | 2 | P[2]='a'='a'，j→3 | 3 |

正确 π = [0,0,1,1,2,3]。

**(甲)** π[3]=1。若失配时不沿 π 回退链、直接把 j 归 0 只试一次：i=3 时 P[1]='b'≠'a' 即归 0，得 π[3]=0。错误表 [0,0,1,0,0,1] 与正确值在第 3、4、5 位不同（0/0/1 对 1/2/3）。

**(乙)** P="aa"、文本 "aaa"：正确结束下标集合 {1,2}。匹配成功后把 j 归 0 的实现只报告 {1}，漏掉结束下标 2。

**(丙)** P="aa"、文本 "aaaa"：正确结束下标集合 {1,2,3}。不重叠实现（每次匹配后跳过整个模式长度）只报告 {1,3}，漏掉结束下标 2。

## 二、四条不变量的落点

1. **与朴素一致**：`match.Feed` 按 π 逐字节推进并记录结束下标（含重叠）；由 `TestNaiveConsistency`（match 包内）钉住。
2. **π 正确 / j 为已消费文本最长后缀==前缀**（匹配后按规则归约为 π[n-1] 的真前缀态）：`pfx.Compute`、`pfx.Step`；由 `TestPrefixFunction`、`TestStateInvariant` 钉住。
3. **均摊 O(1) 不回退重扫**：`match` 非导出字段 `cmps` 经 `pfx.Step` 的计数参数累加，每字节比较 ≤2 次均摊；由 `TestComparisonBound`（≤2·(m+n)）钉住。
4. **失败不留痕**：`match.Feed` 先在局部变量上模拟整批，超限则整体不提交（j/匹配/计数器/丢弃数均不变）；由 `TestFeedAtomicity` 钉住。
