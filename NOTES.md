# NFA→DFA 子集构造：推导与不变量

## 第三节：分步表（NFA：状态{0,1,2}，起点0，接受{2}；0-a->1，1-ε->2，1-b->1，2-c->2）

| # | 当前DFA状态 | 字符 | move直接集 | ε闭包后 | 新状态? | 接受? |
|---|---|---|---|---|---|---|
| 1 | A={0} | a | {1} | {1,2} | 新→B | 是(含2) |
| 2 | A={0} | b | ∅ | ∅ | 不生成 | - |
| 3 | A={0} | c | ∅ | ∅ | 不生成 | - |
| 4 | B={1,2} | a | ∅ | ∅ | 不生成 | - |
| 5 | B={1,2} | b | {1} | {1,2} | 已有(=B) | 是 |
| 6 | B={1,2} | c | {2} | {2} | 新→C | 是 |
| 7 | C={2} | a,b | ∅ | ∅ | 不生成 | - |
| 8 | C={2} | c | {2} | {2} | 已有(=C) | 是 |

- (甲) 漏 ε 闭包：B 错成 {1}（正确为 {1,2}）；`"a"` 停在 {1} 不含接受态 → 被**错误拒绝**（正确：**接受**，闭包 {1,2} 含 2）。
- (乙) 接受判定取反：`"a"` 到达 {1,2} 含接受态 → 被**错误拒绝**（正确接受）；`"z"` 在 A 上无转移 → **拒绝**（与正确结果相同，拒绝发生在接受判定之前）。
- (丙) ∅ 也算状态（且 a/b/c 自环）：DFA 状态从 **3** 个（A,B,C）变成 **4** 个（A,B,C,∅）。

## 第二节：四条不变量落点

1. 与 NFA 模拟一致：`dfa.Build` 与 `api.Simulate` 用同一条 ε闭包+move 规则（nfa.go `EpsilonClosure`、dfa.go `Build`）；测试 `TestEquivalenceVsSimulation`。
2. 闭包完备：只有 ε 闭包的结果才经 `intern` 成为状态（dfa.go `Build` 内 `EpsilonClosure` 调用点）；测试 `TestClosureComplete`。
3. 确定性：转移表 `map[byte]int` 每字符至多一个后继，集合经 `intern` 按内容去重（dfa.go `intern`）；测试 `TestDeterministic`。
4. 失败不留痕：`api.BuildAndMatch` 先 `Validate` 后 `Build`，出错不构造（api.go）；测试 `TestErrorInjection`。
