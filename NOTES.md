# 裁剪规则推导（第三节）

## 方向：只能从近端向远端裁

攻击者（真实客户端）可以在请求里任意伪造转发头部的左端（远端），
但无法伪造沿途代理追加在右端（近端）的记录。
若反过来从远端找「第一个不可信」，伪造者只需在最左塞一条假记录：
链式 `9.9.9.9, 1.2.3.4, 10.0.0.1`（可信 `10.0.0.0/8`）。
远端优先会把 `9.9.9.9` 当成客户端——一条假记录顶掉真结果。
从近端裁：`10.0.0.1` 可信被剥掉，停在 `1.2.3.4`，伪造的 `9.9.9.9`
永远轮不到被采纳。所以方向不能反。

## 地址缺失的跳必须终止裁剪

`for=unknown` / `for=_xyz` 表示该层代理拒绝透露上一跳。
若把缺失当可信继续往远端裁，伪造者只要注入一条 `unknown`，
就能让它左侧的伪造地址被采纳为「客户端」——等于开了一个绕过口。
因此缺失即不可信：在该跳终止，客户端地址记为未知，并报告终止位置。

## 两种头部同时出现的合并

合并序列 = 键值式跳（较远端） ++ 链式跳（较近端），各自内部保持
「最左最远端」的原次序。理由：

1. 链式（XFF 风格）几乎被所有 L7 负载均衡支持，包括紧邻本机的那
   一跳；键值式（Forwarded）通常只在更外侧的边缘层开启，只出现在
   键值式里的跳多半更远，故键值式排在远端侧。
2. 每条头部内部次序不变，从近端向远端逐跳裁剪的方向性依然成立。
3. 两种头部的跳全部进入合并序列，只出现在其中一种里的跳不会被跳过。
4. 同一层被两种头部重复记录时：可信跳被重复裁剪无害；不可信跳两份
   相邻，停在哪一份都得到同一地址，结果不变。
