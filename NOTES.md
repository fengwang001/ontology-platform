# INTERSECT ALL 增量视图：推导与不变量落点

## 第三节：八步分步表（值 "a"，m = min(l, r)）

| 步 | 操作 | l | r | m | 本步输出 |
|---|---|---|---|---|---|
| 1 | Apply(L,a,+1) | 1 | 0 | 0 | 无 |
| 2 | Apply(R,a,+1) | 1 | 1 | 1 | +(a) |
| 3 | Apply(R,a,+1) | 1 | 2 | 1 | 无 |
| 4 | Apply(L,a,+1) | 2 | 2 | 2 | +(a) |
| 5 | Apply(R,a,+1) | 2 | 3 | 2 | 无 |
| 6 | Apply(R,a,−1) | 2 | 2 | 2 | 无 |
| 7 | Apply(R,a,−1) | 2 | 1 | 1 | −(a) |
| 8 | Apply(L,a,−1) | 1 | 1 | 1 | 无 |

- (甲) 第 3 步 l=1, r:1→2，m 应为 **1**（min 不变）。若每次 +1 都无条件输出 +(a)，这一步会把 m 错推到 **2**；第 **3、5** 步（都是在 r>l 的较大侧加）会因此各多输出一条 +(a)。
- (乙) 第 6 步 l=2, r:3→2，m 应为 **2**。若每次 −1 都无条件输出 −(a)，这一步会把 m 错推到 **1**。对应规则「对较大一侧的增减（m 不变）不产生输出」（穿越才变）。
- (丙) 两侧各 Apply("<null>",+1) 后交集重数应为 **0**；若把 NULL 当普通相等值匹配，会错输出一条 **+(<null>)**（m 错为 1）。第 5 步后盈余 r−l=1，若把盈余重数差当交集输出，第 5 步会错输出 **一条 +(a)**（把 m 错推到 3），正确应输出 **无**。

## 第二节：四条不变量的代码落点与钉住它们的测试

1. 与批量重算一致：`inter.Table.View` 逐值返回 `mset.Counter.M()`（`null` 标记恒使 M=0），测试 `TestViewMatchesBatch` 对随机序列与批量 min(l,r) 逐一比对。
2. 变更日志自洽：`api.Engine.Apply` 把 m 的差值展开成逐条 ±1 的 `Change`；测试 `TestChangelogPrefix` 按每个前缀重放并校验每值重数等于 min(l,r) 且非负、每条 −(值) 恰撤回 1 个现存副本。
3. 穿越才变：`mset.Counter.Apply` 先取 oldM、改计数、再取 newM，返回差值（较大侧增减差值为 0 不产出）；测试 `TestCrossingOnly` 钉住八步序列的输出逐条等于上表。
4. 失败不留痕：`mset.Counter.Apply` 在校验（d≠0、计数不变负）通过前不改任何字段，`inter.Table.Apply` 先拒空串且出错时不落 map；测试 `TestRejectedNoEffect` 断言三类哨兵错误互不相同且拒后 View 不变、后续可正常用。
