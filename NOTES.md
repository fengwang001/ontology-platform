# 会话窗口合并 NOTES（gap=3，Key=K）

## 八行分步表（每步先 wm=max(wm,TS)；闭合判定 wm>end+3）

| 步 | TS | wm | 动作 | 事后 K 的全部会话（区间/开闭/计数） |
|---|---|---|---|---|
| 1 | 10 | 10 | 新建 | [10,10]开1 |
| 2 | 13 | 13 | 合并（13-10=3≤3） | [10,13]开2 |
| 3 | 20 | 20 | 新建；[10,13]闭合（20>16） | [10,13]闭2，[20,20]开1 |
| 4 | 16 | 20 | 丢弃（M 含已闭 [10,13]） | [10,13]闭2，[20,20]开1 |
| 5 | 23 | 23 | 合并（23-20=3≤3） | [10,13]闭2，[20,23]开2 |
| 6 | 17 | 23 | 反向合并（17≥20-3） | [10,13]闭2，[17,23]开3 |
| 7 | 25 | 25 | 合并（25≤23+3） | [10,13]闭2，[17,25]开4 |
| 8 | 11 | 25 | 丢弃（M 含已闭 [10,13]） | [10,13]闭2，[17,25]开4 |

(甲) 合并条件错写成 `TS-end<gap`：第 2 步 13-10=3 不满足 → 新建 [13,13]；第 5 步同理新建 [23,23]。最终错成 [10,10]闭1、[13,13]闭1、[17,20]闭2、[23,25]开2：TS=10 会话错成 [10,10]计1（应 [10,13]计2），TS=20 会话错成 [17,20]计2（应 [17,25]计4）。
(乙) 只做正向合并：第 6 步 17<20 被漏认 → 新建 [17,17]，wm=23>20 使其立即闭合；第 7 步得 [20,25]开3。最终 [17,25]计4 错成 [17,17]闭1 与 [20,25]开3。
(丙) 换序 20,16,10,13,23,17,25,11：wm 先到 20，[16,16]、[10,10] 新建即闭，13/17/11 落入闭会话邻域被丢弃，最终 [10,10]闭1、[16,16]闭1、[20,25]开3。差异来自「wm 只进不退 + M 含已闭则丢弃」：高 TS 先到提前封死后到事件。故不变量 1 只能取**被接受**事件——被丢事件不进任何会话，按全部事件重算（16、11 会被并入）永远对不上视图。

## 四条不变量落点

1. 与批量重算一致：合并集在 `swin.(*Engine).feedOne` 按 End 有序二分+连续窗口判定，与 `sess.Link` 的连通定义逐条对应；`api.SelfCheck` 与 `api_test.TestViewMatchesBatch` 钉住。
2. 闭合不变性：Closed 由 `wm>end+gap` 派生（`swin.View`），合并集一旦含已闭会话即丢弃该事件（`feedOne`），闭行保留可查、其三元组不再可变；`api_test.TestClosureImmutability` 钉住。
3. 丢弃与迟到一致：`feedOne` 中 M 成员满足 `wm>end+gap` 即丢且 `dropped++`，SelfCheck 用喂入前 View 独立逐步预测；`api_test.TestDropConsistency` 钉住。
4. 失败不留痕：`swin.(*Engine).Feed` 先校验空 Key，再在深拷贝上试跑整批，出错丢弃拷贝、不提交；`api_test.TestRejectAtomic` 钉住。
