# Golomb/Rice 编码：推导与不变量

## 第三节：M=5 分步表（b=ceil(log2 5)=3，lim=2^3-5=3）

| n | q | r | 商的一元码 | 余数截断二进制 | 完整码字 |
|---|---|---|-----------|---------------|---------|
| 0 | 0 | 0 | 0  | 00  | 000   |
| 2 | 0 | 2 | 0  | 10  | 010   |
| 4 | 0 | 4 | 0  | 111 | 0111  |
| 5 | 1 | 0 | 10 | 00  | 1000  |
| 9 | 1 | 4 | 10 | 111 | 10111 |

比特流 `000 010 0111 1000 10111`（19 位）末字节低位补 5 个 0 → 最终字节 **`09 E2 E0`**。

- (甲) r=0→`00`、r=1→`01`、r=2→`10`（r<lim=3，用 2 位）；r=3→`110`、r=4→`111`（r>=lim，写 r+lim，用 3 位）。若误用 Rice 式固定 3 位余数，n=2 会错成 `0`+`010`=**`0010`**（正确为 `010`）。
- (乙) 忘加 lim 时 r=4 直接写 3 位 `100`，n=4 错编成 `0`+`100`=**`0100`**（正确为 `0111`）；解码先读 2 位 `10`=2<3 即停，n=4 被错解成 **2**。
- (丙) `Encode([])` 返回空（0 字节）；`Encode([0])`（M=5）为 `000` 补零 → 单字节 **`0x00`**；`M<=0` 报 `ErrInvalidParam`，负元素报 `ErrNegative`。

## 第二节：四条不变量落点

1. 朴素参照一致：`golomb.EncodeOne`/`DecodeOne` 严格按给定规则实现；测试 `gstream.TestNaiveReference` 用独立手算参照逐字节/逐元素比对。
2. 切分点无关：`gstream.Stream.Feed` 只累积字节、`Decode` 统一解码；测试 `gstream.TestChunkedFeed` 多档切块比对一次性 Decode。
3. 确定性/自定界：`EncodeOne` 为纯函数、位写出顺序固定，一元码+截断余数可唯一解码；测试 `api.TestDeterministic`。
4. 失败不留痕：`gstream.Decoder.Decode` 遇错整体返回 `nil` 且不更新 `lastBits`；测试 `api.TestTruncatedNoPartial`。

注：末字节补 0 与尾部取 0 值的码字（全 0 比特）不可区分，解码遇「剩余比特全 0」即视为填充停止；自检与随机用例的末元素均取非 0。
