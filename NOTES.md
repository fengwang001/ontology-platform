# 括号配平与最长有效子串 — 推导与不变量

## 一、栈推导（输入 `")()())"`，下标 0..5：`) ( ) ( ) )`）

栈只存下标，有效长度 = 当前下标 - 栈顶。选择：**预置基准 -1；不匹配的 `)` 压入其下标**。

做法甲（预置 -1，不匹配 `)` 压入）：
初始栈 `[-1]` best0；i0 `)` 弹空→压0 栈`[0]` best0；i1 `(` 栈`[0,1]`；i2 `)` 栈`[0]` 长2-0=2 best2；i3 `(` 栈`[0,3]`；i4 `)` 栈`[0]` 长4-0=4 best4；i5 `)` 弹空→压5 栈`[5]`。结果 **start=1,len=4**。

做法乙（不预置，不匹配 `)` 清空，长度只算配对跨度 i-弹出下标+1）：
i0 `)` 清空；i1 压1；i2 弹1 得2；i3 压3；i4 弹3 只得 4-3+1=**2**；i5 清空。结果 best2，**算不出 4**。
错在 i4：弹出后栈空、没有基准，只能看到内层对 (3,4)，无法把 (1,2) 与 (3,4) 两段相邻配平拼成连续长度 4——清空把上一段终点（基准）丢了。

基准为何是 **-1**：子串从下标 0 开始且在 i 处配平时，长度必须是 i+1 = i-(-1)，故基准代表「起点前一个位置」；出现无法匹配的 `)` 后，新子串只能从其后开始，故把基准更新为该 `)` 的下标（即压栈，下次长度自然跨过它）。

## 二、四条不变量：保证位置 + 钉住的测试

1. 配平判定正确：`balance/balance.go` 的 `IsBalanced` 用下标栈，栈深≡逐字符计数，首个破坏点＝栈空遇 `)`（ExcessRight）或栈底未闭 `(`（UnclosedLeft）；钉住：`TestIsBalanced`。
2. 最长子串正确：`longest/longest.go` 的 `Longest` 单遍栈求 start/len，子串必配平且无更长者；钉住：`TestLongestVsNaive`（对照测试文件内朴素枚举 `naive`，并含 `SelfCheck` 正/反用例）。
3. 并列最长取最左：`longest/longest.go` 中仅当新长度**严格大于** best 才更新，故最小 start 先占；钉住：`TestLongestVsNaive` 的并列用例（`naive` 同为严格大于、最左优先）。
4. 失败不留痕：`IsBalanced`/`Longest` 出错路径立即 `return` 零值，不拼接半截子串、不修改入参（string 不可变且无写入）；钉住：`TestErrors`（含三类哨兵错误、零值结果、拒后可继续使用）。
