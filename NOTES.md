# 定名规则推导（第三节）

## 优先次序（不变量 1）
- `filename*`（带扩展标记）优先于 `filename`。
- 理由：扩展形式能携带字符集与百分号编码，是发送方信息更全的表态；RFC 6266/5987 也建议接收方优先采用扩展形式。
- 两者同时出现且内容不同时，固定选 `filename*`。`paramjoin.Join` 输出切片而非 map，`filename.Choose` 对两种形态显式二选一，结果与参数出现顺序、map 遍历顺序无关。

## 安全化次序（不变量 2）
`Sanitize` 按固定次序只做「丢弃 / 折叠」，从不改写字符：
1. 丢弃 `%`+两位十六进制 的三字节序列（防止下游再做一次百分号解码，得到分隔符或 `.`）。
2. 丢弃 `/`、`\`、NUL 与全部控制字符（`unicode.IsControl`）。
3. 折叠连续 `.` 为单个 `.`（保证结果中不出现 `..` 子串，单独 `.`/`..` 也不可能残留）。
4. 修剪首尾空白。
5. 结果为空串或恰为 `.` 时，定为兜底名 `unnamed%2e`。

## 不变量 3 如何满足
- 上述每步只删除字符、不替换字符：输出总是输入的一个有序子序列（再经点折叠与修剪）。
- 因此「两个输入定出同一名字」⟺「它们被保留的字符序列相同」⟺「两者仅在被剔除的字符上不同」，正好落在不变量 3 允许的豁免内。
- 兜底名 `unnamed%2e` 含有 `%2e` 序列，而任何真实输入在第 1 步都会被剔除该序列，故没有任何输入能「自然」定出兜底名；只有被剔到空（或纯点）的输入才落到它，这些输入彼此之间本就只差被剔除的部分，允许撞名（如 `%2e%2e` 与 `..`）。
