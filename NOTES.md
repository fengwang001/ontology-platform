# Union-Find Notes

## 平衡推导
- 若 `Union` 总把 `y` 的根挂到 `x` 的根下，恶意输入可让每棵新增树成为旧根的子节点，树高退化为 `n-1`，`Find` 变为 `O(n)`。
- 按秩合并把秩较小的根挂到秩较大的根下；秩只在两树秩相等并合并时增加一次。
- 归纳可证：秩为 `r` 的树至少有 `2^r` 个节点。单点秩为 0；秩升到 `r+1` 时，必须合并两棵秩为 `r`、各至少 `2^r` 个节点的树，故新树至少有 `2^(r+1)` 个节点。
- 因而秩满足 `2^r <= n`，树高至多为 `floor(log2(n))`，单次查找最多经过 `log2(n)` 条父子边。
- 路径压缩在 `Find` 时让沿途节点直接指向根；秩只作为上界而不会因压缩下降。它与按秩合并配合后的均摊复杂度是逆 Ackermann 函数 `O(alpha(n))`，对实际输入等价于接近常数。
- 测试用非导出跳数计数器钉住边界：正确实现的链式对抗序列路径不超过 16；总是挂到 `x` 根下的错误实现同序列路径超过 1000。

## 语义索引
- 待实现后补充第二节各条的代码位置与测试函数名。
