# CEP `A -> B within T`：推导与不变量

`T=5, maxPending=8`；队列列只写 Key `k` 的待匹配 A（TS），对写作 `(A.TS,B.TS)`。

| # | 事件 | 宽松后队列 | 宽松输出 | 严格输出 |
|---|---|---|---|---|
| 1 | k:A@1 | [1] | 无 | 无 |
| 2 | k:C@2 | [1] | 无 | 无 |
| 3 | k:B@3 | [] | (1,3) | 无（前事件是 C） |
| 4 | k:A@4 | [4] | 无 | 无 |
| 5 | k:A@6 | [4,6] | 无 | 无 |
| 6 | k:B@9 | [6] | (4,9)（9−4=5 闭区间） | (6,9) |
| 7 | k:B@11 | [] | (6,11)（11−6=5） | 无（前事件是 B） |
| 8 | k:A@12 | [12] | 无 | 无 |
| 9 | z:A@14 | [12]（z 自己有 [14]） | 无 | 无 |
| 10 | k:B@17 | [] | (12,17)（17−12=5） | (12,17) |

**甲**：宽松最终 `(1,3)(4,9)(6,11)(12,17)`；严格最终 `(6,9)(12,17)`。先滤掉无关事件再判连续会**多出 (1,3)**（B@3 在只含 A/B 的序列里紧挨 A@1）；按全局上一个事件判连续会**少掉 (12,17)**（全局前事件是 z:A@14，k 的 A@12 被顶掉；若再不校验同 Key 还会错配出 z:A@14→k:B@17）。
**乙**（开区间 `<T`、过期 `>=T`）：宽松变成 `(1,3)(6,9)`——B@9 时 A@4 因 9−4=5 先被过期移除，只剩 A@6；B@17 时 A@12 因 17−12=5 被移除，B 丢弃。严格变成只剩 `(6,9)`（17−12=5 不满足 `<5`，(12,17) 消失）。
**丙**（A 不消费、B 与队列中全部窗口内 A 配对）：输出 `(1,3)(4,9)(6,9)(6,11)(12,17)`；**A@6 被重复匹配**，分别配给 B@9 与 B@11，违反**不变量 3（不重复）**。

不变量（代码保证位置 / 钉住的测试函数）：

1. 与朴素参照一致：`Engine.feedOne` 的逐事件队首语义，对拍 `cepmatch_test.go` 内独立 O(n) 朴素参照 `naive`；`TestReferenceEquivalence`（40 随机种子×两模式×随机批边界）。
2. 匹配合法（同 Key、A 先到、`0<=d<=T`、严格无夹带）：`cepwin.InWindow`/`Expired` + `Engine` 的 `pending`/`last` 均按 Key 分桶；`TestMatchLegality`、`TestWindowAndOtherKey`。
3. 不重复：`feedOne` 宽松配对即队首出队、严格只看上一个事件；`TestNoReuse`、`TestTenEvents`。
4. 失败不留痕：`feedOne` 的每次变更写入 `batchLog`，`Feed` 遇错 `rollback` 逆序回放（队列/上一事件/匹配/探针全复原）；`TestRejectAtomic`（四类互异错误、拒后状态逐对相等且可继续）。

