# 分区裁剪：推导与不变量落点

## 第三节推导（谓词 p∈[20,50) 且 v∈[30,60)）

| id | p 范围 | v 统计 | 静态裁剪 | 动态裁剪 | 最终 |
|---|---|---|---|---|---|
| P0 | [0,10) | [10,20] | 裁：hi=10 <= P_lo=20 | （未走到） | 裁剪 |
| P1 | [10,20) | [30,40] | 裁：hi=20 <= P_lo=20（恰好相等也裁） | （未走到） | 裁剪 |
| P2 | [20,30) | [50,60] | 过：[20,30) 与 [20,50) 相交 | 过：[50,60] 与 [30,60) 相交（50<60 且 60>30） | 扫描 |
| P3 | [30,40) | [15,25] | 过 | 裁：max_v=25 < V_lo=30 | 裁剪 |
| P4 | [40,50) | [60,80] | 过 | 裁：min_v=60 >= V_hi=60（恰好相等也裁） | 裁剪 |

扫描集 = {P2}，裁剪集 = {P0,P1,P3,P4}。

- (甲) 静态写成 `hi<P_lo || lo>P_hi`：P1 的 hi=20 恰等于 P_lo=20，严格小于不成立 → P1 漏裁；扫描集错成 {P1,P2}（正确 {P2}）。
- (乙) 只做静态不做动态：P3、P4 的 p 范围都与谓词相交 → 多扫 P3、P4；扫描集错成 {P2,P3,P4}（正确 {P2}）。
- (丙) 动态写成 `max_v<=V_lo || min_v>V_hi`：P4 的 min_v=60 恰等于 V_hi=60，严格大于不成立 → P4 漏裁；扫描集错成 {P2,P4}（正确 {P2}）。

## 四条不变量：代码位置与钉住它的测试

1. 不漏：`part/part.go` 的 `StaticPrune`/`DynamicPrune`，被裁 = 任一维无交集（析取）；测试 `TestSoundness`（api/api_test.go）。
2. 剪枝最大化：`prune/prune.go` 的 `Query` 与暴力逐分区判定对拍，扫描集 == 两维都相交者；测试 `TestExact`（prune/prune_test.go）。
3. 顺序无关：两维条件相互独立、纯函数析取，先动态后静态重算比对；测试 `TestOrderIndependent`（prune/prune_test.go）。
4. 失败不留痕：`part.New`/`Table.Add`/`Table.Query` 全部先校验后写入，拒绝路径零副作用；测试 `TestRejectedOpsLeaveState`（api/api_test.go）。
