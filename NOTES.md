# CDC 双流 inner join 物化宽表 NOTES

## 八步推导（初始 L、R 皆空；宽表按 key 升序）

| # | 操作 | 输出变更日志 | 操作后宽表 |
|---|------|--------------|------------|
| 1 | PutL(k1,10) | 无 | (空) |
| 2 | PutL(k2,20) | 无 | (空) |
| 3 | PutR(k2,200) | +(k2,20,200) | (k2,20,200) |
| 4 | PutR(k1,100) | +(k1,10,100) | (k1,10,100),(k2,20,200) |
| 5 | PutR(k3,300) | 无 | (k1,10,100),(k2,20,200) |
| 6 | PutL(k3,30) | +(k3,30,300) | (k1,10,100),(k2,20,200),(k3,30,300) |
| 7 | PutR(k2,250) | -(k2,20,200),+(k2,20,250) | (k1,10,100),(k2,20,250),(k3,30,300) |
| 8 | DelL(k1) | -(k1,10,100) | (k2,20,250),(k3,30,300) |

- (甲) 不撤回旧行只发 `+(k2,20,250)`：下游 k2 同时挂着 (k2,20,200) 与 (k2,20,250) 两行。违反不变量 2（任一 key 至多一行）与不变量 1（比朴素重算多出一行 (k2,20,200)）。
- (乙) 若右流先到即报错/丢弃：第 5 步 `R[k3]=300` 丢失，第 6 步 `PutL(k3,30)` 发现 k3 不在 R 而无输出，宽表永久缺 (k3,30,300) 一行。
- (丙) 不判重则额外输出 `-(k2,20,250),+(k2,20,250)`：下游看到一次无意义的撤回+重建抖动（行内容没变，日志多出两条噪声），违反不变量 3 的幂等要求。

## 四条不变量：保证位置与钉住测试

1. 与朴素重算一致：`join` 每次变更由 `apply` 同步维护 wide 映射，`WideTable` 只读它（join/join.go）；测试 `TestWideTableMatchesNaiveJoin`。
2. 变更日志自洽：`jrow` 的 Put/Del 只在确认旧行存在时才先发 `-(旧值)` 再发 `+(新值)`（jrow/jrow.go）；测试 `TestChangelogPrefixesConsistent`。
3. 内连接且幂等：`jrow` 仅当 `InL && InR` 才产出 `+`；`join.PutL/PutR` 开头判重（值相同直接返回无输出）；测试 `TestInnerJoinAndIdempotent`。
4. 失败不留痕：`join` 每个操作先校验（空 key / Del 不存在 / 超容量 / maxKeys<=0）再改状态，哨兵错误 `ErrEmptyKey/ErrNotFound/ErrCapacity/ErrBadMaxKeys` 互不相同；测试 `TestRejectedOpsLeaveNoTrace`。
