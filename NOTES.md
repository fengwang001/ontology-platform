# Householder QR — NOTES

## 三、n=2 分步表（A=[[3,1],[4,1]]）

| k | x,‖x‖ | v(v[0]=1) | H | 作用后该列 |
|---|---|---|---|---|
| 0 | [3,4], 5 | [1,-2] | [[3/5,4/5],[4/5,-3/5]] | col0 → [5,0] |
| 0 | [1,1], √2 | 同一个 v（只构一次，作用 k 后所有列） | 同上 | col1 → [7/5,1/5] |
| 1 | [1/5], 1/5 | —（x[1:] 为空，跳过） | I | col 不变 [1/5] |
| 终 | — | — | Q=H0 | R=[[5,7/5],[0,1/5]]，Q=[[3/5,4/5],[4/5,-3/5]] |

- **(甲) 漏因子 2**：H=I−vvᵀ/(vᵀv)=[[4/5,2/5],[2/5,1/5]]，H·[3,4]=**[4,2]** → R[0][0]=4（应 5），R[1][0]=2（应 0）。
- **(乙) 符号相反**：v=x+‖x‖e₁=[8,4]→归一[1,1/2]，H=[[-3/5,-4/5],[-4/5,3/5]]，H·[3,4]=**[-5,0]** → R[0][0]=**-5**（应 +5）。
- **(丙) 漏作用后续列**：col1 保持 [1,1] → R[0][1]=**1**（应 7/5）。

## 二、不变量落点

1. **QR 还原 A（逐项 ≤1e-9）**：`qr/qr.go` `Factor` 在副本上原位消元得 R，Q 经 `applyQFromRight` 累乘 Q=∏Hₖ；测试 `TestReconstruction` 钉。
2. **QᵀQ=I、R 严格上三角**：每个 H 对称正交、累乘保正交；`hh.Apply` 只更新 k 行起的块、把对角线下消零；测试 `TestOrthogonalAndUpper` 钉。
3. **列不回头改**：`hh/hh.go` `Apply` 列循环为 `for j:=k; j<n`，前 k-1 列一个字节都不写；测试 `TestApplyPreservesEarlierColumns` 钉。
4. **失败不留痕**：`qr/qr.go` 三类哨兵校验先于任何修改；反射子数先记局部变量，成功后才 `atomic.Add`，输入仅读副本；测试 `TestRejectionLeavesState` 钉。

计数器（非导出，白盒同包读）：`TestTriangularZeroCount` 对 n=100/1000/10000 已上三角矩阵断言计数增量恒 0。并发：`TestConcurrentIdentical`（`go test -race`）。
