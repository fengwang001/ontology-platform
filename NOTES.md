# Indexed Binary Heap Notes

## decrease-key 只需要 sift-up

最小堆中每个节点的键不大于其两个子节点。把某节点的键改小后：

- 对它的子节点：它原先就不大于子节点，现在更小，所以与子节点的关系仍然成立。
- 对它的父节点：父节点原来可能小于或等于旧键；新键可能小于父节点，只有这一侧可能违规。

因此违规只能沿父链出现，只需不断与父节点比较、交换，即 sift-up。

反例：堆序列 `[3,5,7,9,11,13]`。把键 `9` 降到 `1`，得到 `[3,5,7,1,11,13]`。
正确做法是让 `1` 依次与 `5`、`3` 交换，得到 `[1,3,7,5,11,13]`。
若错误地做 sift-down：`1` 从下标 3 开始。按常见实现取子节点最小值时，它没有子节点，
会直接停止，堆顶仍是 `3`；某些自根或错误下标版本更会比较并交换无关节点。
无论哪种实现，随后 Pop 都不能得到 `[1,3,5,7,11,13]`；本仓库测试内联该错误 sift-down 版本钉住此结论。

## 代码与测试位置

（实现完成后补充）
