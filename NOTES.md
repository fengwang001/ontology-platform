# 几何哈希：规范坐标推导

T=(0,0),(2,0),(2,2),(0,2)，基(v0,v1)，d=(2,0)，q=4，perp(d)=(0,2)。

| 点 | 规范 (u,v) |
|---|---|
| (0,0) | (0,0) |
| (2,0) | (1,0) |
| (2,2) | (1,1) |
| (0,2) | (0,1) |

S=(5,5),(5,7),(3,7),(3,5)，对应基((5,5),(5,7))，d=(0,2)，q=4，四点同为 (0,0),(1,0),(1,1),(0,1)，故匹配成立。

(甲) 镜像 (0,0),(2,0),(2,-2),(0,-2) 在同一基下 v 全部取反：(0,0),(1,0),(1,-1),(0,-1)。按「不含反射」必须判不匹配；若把基两点顺序颠倒（无序基）或不对齐方向，v 的符号丢失，镜像与原像投出同样的票，必然假阳性。
(乙) 旋转 30° 时 (2,0)→(√3,1)，场景侧 (u,v) 含无理运算，与模板侧精确有理值做浮点精确相等永不成立 → 查不到 key、漏匹配（假阴性）。须固定步长（1/16）四舍五入且两侧一致；代码用整数分数精确四舍五入（gh.Quantize），无浮点误差。
(丙) 不设最小支持数时，单个噪声点偶然落格就出票，随机场景几乎必然假阳性。正确阈值为 k−2（基之外的剩余点数），出票后还须逐点精确有理验证才接受。

## 不变量：保证位置 / 钉住测试

1. 与穷举一致：哈希只产生候选，match.go 的 vote 对每个模板点算精确有理像、必须落在场景点且量化 key 命中该点模板桶；TestInvariantExhaustiveConsistency（match 包）。
2. 相似不变：规范坐标公式在 gh.BasisUV、精确量化在 gh.KeyOf（gh.go）；TestInvariantSimilarity（match 包）。
3. 无反射：vote 只按定向 perp 帧做 u·d+v·perp(d) 的复数乘，绝不取共轭（match.go）；TestMirrorRejected（api 包）。
4. 失败不留痕：api.go 的 NewTemplate/Match 先做完全部校验（五个互异哨兵错误）再构造/运行，失败即 nil；TestRejectedInputs（api 包）。

计数器为 match.go 中 session.votes（非导出字段），TestCounterQuadratic 在同包测试内读取并断言投票数 ≤ 4n²。
