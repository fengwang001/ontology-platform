# Union-Find 推导

1. 若每次 Union 都把 y 的根挂到 x 的根下，序列 Union(0,1), Union(1,2), ... 会形成深度 n-1 的链，末端 Find 为 O(n)。
2. 按秩合并把秩较小的根挂到秩较大的根下；秩是树高上界，只有两棵同秩树合并时新根秩才加 1。
3. 归纳可得：秩为 r 的树至少包含 2^r 个元素，所以任意树高 r <= floor(log2(n))，n=2^16 时高度不超过 16。
4. Find 路径压缩把查找沿途节点直接改挂到根；之后这些节点的查找近似 O(1)，且秩仍可作为保守上界。
5. 按秩合并保证树不会变高，路径压缩持续摊平长路径；二者配合后单次操作均摊 O(alpha(n))，alpha(n) 增长极慢，可视为接近常数。
6. 本实现额外用测试钉住工程界：随机 10000 次 Union 后，单次 Find 跳数 <= 2*log2(n)+2。
