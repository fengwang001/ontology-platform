# NOTES — 单字段二级索引增量维护

## 八步推导（空表起步；组内主键升序）

| 步 | 操作 | F=5 索引组 | F=8 索引组 | 本步返回 |
|---|---|---|---|---|
| 1 | Put(a,5) | [a] | [] | 成功 |
| 2 | Put(b,5) | [a b] | [] | 成功 |
| 3 | Put(c,8) | [a b] | [c] | 成功 |
| 4 | Put(a,8) | [b] | [a c] | 成功 |
| 5 | Del(b) | [] | [a c] | 成功 |
| 6 | Put(d,5) | [d] | [a c] | 成功 |
| 7 | Range(5,8) | [d] | [a c] | [d] |
| 8 | Eq(8) | [d] | [a c] | [a c] |

(甲) 只插新组、不删旧组：第 4 步后 `Eq(5)` 错返回 `[a b]`（a 残留在旧组），正确应为 `[b]`；违反不变量 2（更新原子：主键只出现在新 F 组）。
(乙) `Range(5,8)` 正确返回 `[d]`。误作闭区间 `[5,8]` 会错返回 `[a c d]`：多出 a、c，它们 F=8 不满足 F<8（hi 不含），不该出现。
(丙) 是**无操作**。若误当普通插入往 F=8 组追加重复项，此后 `Eq(8)` 错返回 `[a a c]`，正确应为 `[a c]`。

## 四条不变量：保证位置 / 钉住测试

1. 与批量重算一致 — ient 按 (F,PK) 全序有序存放、二分定位（ient/ient.go），tbl 写路径同步维护索引（tbl/tbl.go）；`api.TestBatchRecompute`。
2. 更新原子 — tbl/tbl.go Put 更新分支在同一写临界区内先删旧索引项再插新项，同 F 直接返回不动作；`api.TestUpdateAtomic`。
3. 范围语义 — ient/ient.go Range 下界二分定位 (lo,"")、扫描条件 F<hi、结果按 PK 升序；`api.TestRangeSemantics`。
4. 失败不留痕 — tbl/tbl.go Apply 先在记录表副本上整批模拟校验，全部通过后才落真写；`api.TestFailureNoTrace`。
