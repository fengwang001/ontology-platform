# Fibonacci(Zeckendorf) 编码：推导与不变量

序列 `[1,2,4,6,8]`；F(1..7)=1,2,3,5,8,13,21。位偏移从 1 计。

| N | Zeckendorf 下标 | 码位串 b1..bk+分隔符 | 位数 | 累积比特偏移 |
|---|---|---|---|---|
| 1 | {1} | `11` | 2 | 1–2 |
| 2 | {2} | `011` | 3 | 3–5 |
| 4 | {1,3} | `1011` | 4 | 6–9 |
| 6 | {1,4} | `10011` | 5 | 10–14 |
| 8 | {5} | `000011` | 6 | 15–20 |

拼接 = `11011101110011000011`，末字节补 4 个 0 ⇒ 字节 `DD CC 30`。解码：`11`→F1=1；`011`→F2=2；`1011`→F1+F3=1+3=4；`10011`→F1+F4=1+5=6；`000011`→F5=8；尾部 4 个 0 为填充。

(甲) 错定义 F1=1,F2=1,F3=2,F4=3,F5=5：4 贪心取 F4=3 再取 F2=1（下标 {4,2}，不相邻），k=4，码位串 `01011`。正确解码器按 F1=1,F2=2 求和：b2+b4=F2+F4=2+5=**7**（不是 4）；而 7=F4+F2=5+2，`01011` 正是正确定义下 **7** 的码。

(乙) 第 3 码 `1011` 结尾 `11` 落在第 8、9 位：第 8 位是字节 0 的 LSB、第 9 位是字节 1 的 MSB，即终止对**恰好跨字节边界**（第 4 码 `10011` 全在字节 1 内）。`Encode([1])`=`0xC0`=`11000000`。若按字节内扫描、字节末重置：码后 6 个填充 0 被当成一个无 `11` 的新码 ⇒ 误报 `ErrTruncated`，[1] 也解不出；若见单个 `1` 即当结束：第 1 位闭出一个空码（和为 0，非正），第 2 位再闭一个，随后 6 个 0 跑到结尾仍报截断 ⇒ 得到垃圾/报错，绝不是 `[1]`。

(丙) `0x03`=`00000011`：前 6 位 b1..b6=0，首个 `11` 在第 7、8 位 ⇒ k=7、b7=1 ⇒ 求和 F(7)=**21**。`0x01`=`00000001`：末位是孤立 `1`，到结尾无 `11` ⇒ `ErrTruncated`。`Encode([]int64{})` = 长度 0 的 `[]byte{}`。

## 四条不变量（保证位置 / 钉住测试）

1. 与朴素参照一致：`fcode.Encode`/`Decode` 与测试内朴素参照逐字节/逐元素比对——`TestNaiveReference`。
2. 切分点无关：`StreamDecoder.Feed` 增量维护待定位串、按逻辑位计数（fcode.go）——`TestFeedSegmentation`。
3. 确定性/无前缀：`EncodeOne` 固定置 bk 与分隔符，码内无相邻 1、终止 `11` 唯一——`TestDeterminismAndCodes`。
4. 失败不留痕：`Decode` 全失败返回 nil；`Feed` 在副本上试解、出错不提交（fcode.go）——`TestFailureLeavesNoState`。
哨兵三互异：`TestSentinelErrors`；尾码检查位不随 m 增长：`TestTailCheckBitsIndependentOfM`；并发：`TestConcurrentRoundTrip`；api 自检：`TestSelfCheck`。
