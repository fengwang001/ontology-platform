# 变更流位点回卷与倒退检测 — 推导与不变量

## 八步推导（threshold = 2^31 = 2147483648）

| # | r | 事件 | prev(后) | unwrapped(后) |
|---|---|------|----------|----------------|
| 1 | 100 | First | 100 | 100 |
| 2 | 200 | Forward | 200 | 200 |
| 3 | 4294967200 | Forward | 4294967200 | 4294967200 |
| 4 | 50 | Wrap | 50 | 4294967346 |
| 5 | 60 | Forward | 60 | 4294967356 |
| 6 | 40 | 倒退被拒 | 60 | 4294967356 |
| 7 | 4294967295 | Forward | 4294967295 | 8589934591 |
| 8 | 5 | Wrap | 5 | 8589934597 |

(甲) 第 4 步正确值 4294967346 = 4294967200 + (2^32−4294967200) + 50 = 4294967200+96+50；公式错成 `…+r−1`（少算 1）→ 4294967345；漏加 r → 4294967296。
(乙) 不设阈值时第 6 步（40 < 60，drop=20）被误判为 Wrap：unwrapped = 4294967356 + (2^32−60) + 40 = 8589934632（正确：拒绝并保持 4294967356）。
(丙) 用原始 uint32 比较：第 4 步 50<4294967200、第 8 步 5<4294967295 都会被误判成倒退异常。32 位原始值在回卷点必然回落、本身非单调，既无法区分「回卷」与「真倒退」，也不存在可核验的单调性；不变量 1 的参照必须使用回卷展开后的 64 位单调值。

注：推导取 threshold=2^31，但 `New` 合法域为 [1, 2^31)，代码中用最大合法值 (1<<31)-1；各步 drop 为 4294967150/20/4294967290，分类结果与 2^31 完全一致。

## 四条不变量：保证位置 / 钉住测试

1. 朴素一致：`eng.Feed` 每步仅经 `wrap.Classify`/`wrap.Unwrap` 增量提交，与离线逐步展开同源；`TestNaiveReference` 对多档随机序列逐步比对朴素参照。
2. 单调不减：`eng.Feed` 只在事件被接受时提交，Duplicate 增量 0、First/Forward/Wrap 增量为正，拒绝不提交；`TestMonotonic`。
3. 回卷正确性：`wrap.Classify` 的 `drop > threshold` 判定 + `wrap.Unwrap` 的 `pu+(2^32−prev)+r`；`TestWrapCorrectness`。
4. 失败不留痕：`eng.Feed` 先分类、试算，全部成功才写状态，`api.New` 先验阈值再构造；`TestFailureNoTrace`（含非法阈值/倒退/溢出/空态查询，且四者哨兵错误互不相同）。
