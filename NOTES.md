# Phaser 推导与不变量

## 三、分步推导（NewPhaser(2)，A=0、B=1，相位 0）

| 步 | 操作 | 之后相位 | party 集合 | 未到达集合 |
|---|---|---|---|---|
| 1 | A Arrive | 0 | {A,B} | {B} |
| 2 | Register → C | 0 | {A,B,C} | {B,C} |
| 3 | B Arrive | 0 | {A,B,C} | {C} |
| 4 | C Arrive | 1 | {A,B,C} | {A,B,C} |

- (甲) 第 3 步后**不推进**（C 未到达，未到达计数=1）。若 Register 让新 party 加入**下一相位**（不计入当前未到达）：第 3 步 B Arrive 使未到达降 0 → 相位变 **1**，第 4 步后相位仍为 **1**；C 正在到达的相位：正确实现为 **0**，错误实现为 **1**。
- (乙) 正确实现（先到达后注销）：A 到达使未到达降 0 → 推进到相位 1，再注销 A → 相位 **1**。若先注销后到达：注销 A 使未到达集合变空、触发一次推进（相位 1），随后 A 的到达又计入新相位、未到达再降 0 又推一次 → 相位 **2**。差在错误实现**多推一次**（注销提前清空了未到达集合，到达又被重复计入）。
- (丙) B 是最后到达者，其 Arrive 返回**推进前**的相位 **0**。若实现返回推进后的当前相位，B 会返回 **1**，与 A 的返回值 0 不一致（同一相位到达者返回值应相同）。

## 二、四条不变量：保证位置与钉住它的测试

1. **与朴素参照一致**：`api.SelfCheck` 内置逐操作比对真实 Phaser 与 naive 参照的相位/party/未到达快照；测试 `TestSelfCheck`。
2. **推进条件精确**：`phase.maybeAdvance` 是唯一推进点，仅在未到达计数降 0 且 party 数 > 0 时 +1；测试 `TestFourStepScenario`、`TestArriveChecksConstant`。
3. **相位单调**：相位仅由 `maybeAdvance` 每次 +1（终止置 -1 除外），无任何减赋；测试 `TestSelfCheck`（逐步断言单调且步长 ≤1）。
4. **失败不留痕**：`phase` 的 Register/Arrive/Deregister 全部先校验、后改状态，任一校验失败直接返回哨兵错误；测试 `TestRejectedOpsLeaveNoTrace`。
