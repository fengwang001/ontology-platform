# 读时一致性快照 — 推导与不变量

## 八步推导（初值 ver=0，历史为空）

| # | 操作 | 操作后 ver | hist[a] | hist[b] | 读结果 |
|---|------|-----------|---------|---------|--------|
| 1 | Write(a,10) | 1 | (1,10) | — | |
| 2 | Write(b,20) | 2 | (1,10) | (2,20) | |
| 3 | s1=Snapshot() | 2 | (1,10) | (2,20) | s1=2 |
| 4 | Write(a,15) | 3 | (1,10),(3,15) | (2,20) | |
| 5 | Read(s1,a) | 3 | (1,10),(3,15) | (2,20) | 10 |
| 6 | Write(b,25) | 4 | (1,10),(3,15) | (2,20),(4,25) | |
| 7 | Read(s1,b) | 4 | (1,10),(3,15) | (2,20),(4,25) | 20 |
| 8 | s2=Snapshot() | 4 | (1,10),(3,15) | (2,20),(4,25) | s2=4 |

- (甲) 第 5 步正确为 10（版本 1）。若不保留版本历史、只存最新值，会错成 15（读到版本 3 的值）。
- (乙) 第 7 步正确为 20：b 的版本 2 恰等于 s1=2，`<=` 含等于故可见。若误写成 `ver < snap`，版本 2 被排除、无更早版本，会错成 0。
- (丙) 若误用读取时刻最新版本（ver=4），`Read(s1,b)` 会错成 25（版本 4 的值）。同一快照若不钉住固定版本，a 读到时刻 T1、b 读到时刻 T2，各键来自不同时刻，拼不出任一真实存在过的一致状态，故不变量 2 要求同一快照跨键用同一固定版本。

## 四条不变量（保证位置 / 钉住它的测试）

1. 与朴素参照一致：`snap.Store.Read` 在按 ver 有序的历史上二分取 `ver<=snap` 的最大版本，等价于重放后写覆盖先写；`TestReadMatchesNaiveReplay`。
2. 快照隔离（跨键一致）：`Read` 只用入参 snap，经 `ver.Visible` 的 `<=` 比较，从不读当前 ver；`TestSnapshotIsolationCrossKey`。
3. 读不阻塞写、读不改状态：`Read/ReadCurrent` 只读、`Snapshot` 只增登记，唯 `Write` 递增 ver；`TestReadsDoNotMutate`。
4. 失败不留痕：`api` 层先校验后执行，哨兵错误互不相同，拒绝路径零写入；`TestRejectedOpsLeaveNoTrace`、`TestErrorsDistinct`。
