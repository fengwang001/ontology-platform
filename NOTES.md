# KMP 推导与不变量

## 第三节推导：P = "abaaba" 的前缀函数（六行分步表）

| i | P[i] | 进入时 j=π[i-1] | 比较与回退过程 | π[i] |
|---|------|----------------|----------------|------|
| 0 | a | —（边界） | 定义 π[0]=0 | 0 |
| 1 | b | 0 | 比 P[0]='a'≠'b'，j 已为 0 无处可退 | 0 |
| 2 | a | 0 | 比 P[0]='a'='a'，j→1 | 1 |
| 3 | a | 1 | 比 P[1]='b'≠'a'，沿 π 回退 j=π[0]=0（跳过 'b'）；再比 P[0]='a'='a'，j→1 | 1 |
| 4 | b | 1 | 比 P[1]='b'='b'，j→2 | 2 |
| 5 | a | 2 | 比 P[2]='a'='a'，j→3 | 3 |

正确 π = [0,0,1,1,2,3]。

- (甲) π[3]=**1**。失配后不沿 π 回退、只试一次就归 0：i=3 时 'a'≠P[1]='b' 直接 j=0 → π[3] 错成 **0**；错误实现 π=[0,0,1,0,0,1]，第 **3、4、5** 位与正确值不同（正确的 1,2,3 被错成 0,0,1）。
- (乙) P="aa"、文本 "aaa"：正确结束下标集合 **{1,2}**；匹配后 j 归 0 的实现只报 **{1}**，漏掉 2。
- (丙) P="aa"、文本 "aaaa"：正确集合 **{1,2,3}**；不重叠实现（匹配后跳过整个模式长度）只报 **{1,3}**，漏掉 2。

## 第二节四条不变量：保证位置 → 钉住它的测试

1. 与朴素一致：`match.Feed` 逐字节 `pfx.Advance`，j==len(P) 记结束下标后 j=π[n-1]；对照 `api.naiveEnds`。测试 `TestNaiveConsistency`。
2. π 正确、j 语义：`pfx.Compute`/`pfx.Advance`，j 恒为「已消费文本最长后缀==P 前缀」长度。测试 `TestPrefixFunction`。
3. 不回退重扫：`match.Matcher.cmps`（非导出）计匹配阶段比较次数，每字节均摊 O(1)。测试 `match.TestLinearComparisons`。
4. 失败不留痕：`match.Feed` 全程在局部变量模拟，超限则整体不提交；`api.New` 用哨兵错误拒绝。测试 `TestFeedAtomicOnOverflow`、`TestFaultInjection`。
