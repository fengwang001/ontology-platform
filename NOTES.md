# NOTES

## 三、`BinOp("+", IntLit(258), Var("ab"))` 编码分步表（小端）

| 字段 | 字节数 | 字节值(hex) |
|---|---|---|
| BinOp 标签(=5) | 1 | 05 |
| 运算符 +(码0) | 1 | 00 |
| IntLit 标签(=0) | 1 | 00 |
| int64 258 | 8 | 02 01 00 00 00 00 00 00 |
| Var 标签(=2) | 1 | 02 |
| 名字长度前缀 uint32(=2) | 4 | 02 00 00 00 |
| 名字 "ab" | 2 | 61 62 |

完整字节串(18B)：`05 00 00 02 01 00 00 00 00 00 00 02 02 00 00 00 61 62`

- (甲) `IntLit(2^32)`：标签1B+定宽8B=**9字节**，字节 `00 00 00 00 00 01 00 00 00`，解码值=**4294967296**。误用4字节uint32：2^32 mod 2^32 = **0**，被截断成0。
- (乙) `Var("")`：长度前缀=**0**（4B `00 00 00 00`），名字**0字节**。C风格`\0`终止：空串没有可写的名字字节，解码器遇0停会把**后续节点的标签等字节当作名字**一直读到碰巧的0，名字错成垃圾且整条流错位。
- (丙) `Var("a\x00b")`：长度前缀=**3**，名字字节 `61 00 62`。用strlen写长度会得到**1**，解码后名字错成 **"a"**，残留的 `62` 还会污染后续解码。

## 二、四条不变量的保证位置与钉住它的测试

1. 与朴素参照一致：`codec.Encode/Decode` 定长字段+长度前缀；测试 `codec.TestRoundTripNaive`（独立 naiveEncode 逐字节比对 + ast.Equal）。
2. 字节序一致：`codec.go` 全部走 `binary.LittleEndian`，IntLit恒8B、长度前缀恒4B；测试 `codec.TestByteWidths`（含 IntLit(2^32) 占9B、字节数可预测）。
3. 字符串按字节：`codec.go` Var 分支直接写 `[]byte(name)` 不转义；测试 `codec.TestStringBytes`（空串/`\x00`/0xff 非UTF-8）。
4. 失败不留痕：`codec.Decode` 出错即返回 nil+哨兵错误，无全局状态；测试 `codec.TestFaultInjection`、`api.TestRejectedThenOK`。
