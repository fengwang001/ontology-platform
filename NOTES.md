# 字节序编解码 NOTES

## 三、推导：8 字节 `01 02 03 04 05 06 07 08` 的六行解码表

| 宽度 | 字节序 | 取用字节 | 解出的值 (hex) |
|---|---|---|---|
| uint16 | 小端 | 01 02 | 0x0201 |
| uint16 | 大端 | 01 02 | 0x0102 |
| uint32 | 小端 | 01 02 03 04 | 0x04030201 |
| uint32 | 大端 | 01 02 03 04 | 0x01020304 |
| uint64 | 小端 | 01 02 03 04 05 06 07 08 | 0x0807060504030201 |
| uint64 | 大端 | 01 02 03 04 05 06 07 08 | 0x0102030405060708 |

- (甲) `0x01020304` 按大端写 uint32 → `01 02 03 04`；误用小端（或 LE 机器上写本机序）→ `04 03 02 01`。
- (乙) 把偏移 0 误当 uint64 小端读 8 字节 `01 00 00 00 02 00 00 00` → `0x0000000200000001` = 8589934593；正确按 uint32 小端读 → 1。
- (丙) `FF FF FF FF`：无符号 uint32 = 4294967295；有符号 int32 符号扩展 = -1。

## 二、四条不变量：保证位置与钉住测试

1. 往返一致：`codec.Encode`/`codec.Decode` 同序同宽互逆（codec/codec.go）→ `TestRoundTrip`
2. 序互逆：`ord.Swap16/32/64` 纯字节反转，BE 写 LE 读 = 反转（ord/ord.go）→ `TestSwapAndCrossOrder`
3. 与朴素参照一致：`ord.Put/Uint` 逐字节移位（ord/ord.go）→ `TestNaiveReference`
4. 失败不留痕：`codec.Decode` 先校验宽度与对齐再解码，拒绝即 `(nil, error)`（codec/codec.go）→ `TestRejectTotal`
