# ontology-323 一致采样：推导与不变量对照

## 第三节：十行分步表（New(2500,100)；h 初值 0，逐字节 h=h*31+b；bucket=h mod 10000；采 ⇔ bucket<r）

| # | 键 | h(key) 算式 | h | bucket | r=2500 采？ |
|---|---|---|---|---|---|
| 1 | gnj | 103 → 103*31+110=3303 → 3303*31+106 | 102499 | 2499 | ✓ |
| 2 | dzv | 100 → 100*31+122=3222 → 3222*31+118 | 100000 | 0 | ✓ |
| 3 | gnk | 103 → 3303 → 3303*31+107 | 102500 | 2500 | ✗（=r） |
| 4 | kcm | 107 → 107*31+99=3416 → 3416*31+109 | 106005 | 6005 | ✗ |
| 5 | dzv | 同 #2 | 100000 | 0 | ✓ |
| 6 | qjy | 113 → 113*31+106=3609 → 3609*31+121 | 112000 | 2000 | ✓ |
| 7 | a6m | 97 → 97*31+54=3061 → 3061*31+109 | 95000 | 5000 | ✗ |
| 8 | dzv | 同 #2 | 100000 | 0 | ✓ |
| 9 | SetRate(2000) | 新纳入 = [] | | 被移出 = [qjy(2000), gnj(2499)] | |
| 10 | SetRate(5000) | 新纳入 = [qjy(2000), gnj(2499), gnk(2500)] | | 被移出 = [] | |

- (甲) 八事件共保留 **5** 个（gnj、dzv×3、qjy）；dzv 三个事件**全留**。若每事件独立 25% 随机：dzv 三事件全留或全丢概率 = 0.25³+0.75³ = **0.4375**，违反不变量 2（同键全留全丢）。
- (乙) 错误实现 h mod M==0：r=2500(M=4) 采 {dzv,gnk,qjy,a6m}；r=2000(M=5) 采 {dzv,gnk,kcm,qjy,a6m}；r=5000(M=2) 采 {dzv,gnk,qjy,a6m}。**kcm** 在调低到 2000 时反被纳入、调高到 5000 时反被移出，违反单调性（不变量 3）。
- (丙) bucket=2500 是 **gnk**（r=2500 不采）；=2000 是 **qjy**（降到 2000 被移出）；=5000 是 **a6m**（升到 5000 仍不采）。若误写 bucket<=r：八事件保留 **6** 个（多 gnk）；SetRate(2000) 移出 [gnj,gnk]、新纳入 []；SetRate(5000) 新纳入 [gnj,gnk,a6m]、移出 []。

## 第二节四条不变量：代码位置 → 钉住它的测试

1. 与朴素参照一致：`samp/samp.go` Feed 逐事件走 `khash.Sampled`、SetRate 只二分扫 `[min(r1,r2), max(r1,r2))` 桶区间（与全量判定差集等价）→ `TestFeedMatchesNaive`、`TestSetRateMatchesNaive`。
2. 同键全留全丢、跨实例一致：判定是纯函数 `khash.Sampled(key,rate)`（`khash/khash.go`），只依赖键与采样率 → `TestCrossInstance`。
3. 单调：`bucket<r` 的键集随 r 单调扩张，SetRate 区间方向决定 added/removed 必有一空 → `TestMonotonic`。
4. 失败不留痕：`samp.Feed` 先整批校验（空键/maxKeys）再落状态，`SetRate` 先验 r 越界 → `TestNoTrace`。
