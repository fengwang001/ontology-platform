# Dijkstra 过期记录推导

## 不变量
- dist[v] 始终是 src 到 v 的一条真实可行路径长度（最短距离的上界）。
- 队列元素 (v, d) 只是「入队那一刻」dist[v] 的快照；每次成功松弛都入队，故同节点可出现多次。

## 为什么必须跳过过期记录（lazy deletion）
弹出 (v, d) 时分两种情况：
1. d > dist[v]：入队后 v 又被更短路径改进过，该快照已过期，必须 `continue` 跳过。
2. d == dist[v]：非负权贪心定理保证此时 d 即最终最短距离，v 从此确定。
   任何经其它未确定节点的绕行，其长度都不小于当前最小键 d，不可能再缩短 v。

不能用过期记录更新邻居的原因：
- 若把过期值当真并回写 dist[v]=d，会把已确定的更短距离「抬高」，
  再用错误距离松弛邻居，造成距离被错误抬高/反复抖动（本次线上事故）。
- 即便只做普通松弛而不回写，也破坏「每节点只在最终距离上处理一次」的不变量，
  节点被重复处理，工作量从 O((n+m) log n) 退化到 O(n·m)。
因此实现为：`if d > dist[v] { continue }`，只在 d == dist[v] 时扫描出边。

## 复杂度
松弛次数 = 非过期弹出时扫描的出边数；每节点至多非过期弹出一次，故总松弛次数 ≤ m。
测试在 n=1000, m=5000 的图上断言松弛次数 ≤ m（5000），排除 O(n·m) 退化。

## 语义落点（代码位置 / 测试函数名）
（实现完成后补）
