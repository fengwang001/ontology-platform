# NOTES — ontology-507 分区裁剪

谓词：p ∈ [20,50) 且 v ∈ [30,60)

| id | 静态裁剪（只看 p） | 动态裁剪（只看 v，仅静态通过者） | 最终 |
|---|---|---|---|
| P0 [0,10) v[10,20] | 裁：hi=10 ≤ Plo=20 | 不评（盒在 v 维亦不相交：20<30） | 裁剪 |
| P1 [10,20) v[30,40] | 裁：hi=20 ≤ 20（恰好等于） | 不评（v 维其实相交 [30,40]） | 裁剪（静态） |
| P2 [20,30) v[50,60] | 不裁：10?否，20≥50?否 | 不裁：60<30否、50≥60否，相交 [50,60) | 扫描 |
| P3 [30,40) v[15,25] | 不裁 | 裁：max_v=25 < Vlo=30 | 裁剪（动态） |
| P4 [40,50) v[60,80] | 不裁：lo=40≥50?否（p 维相交 [40,50)） | 裁：min_v=60 ≥ Vhi=60（恰好等于） | 裁剪（动态） |

正确扫描集 = {P2}。

- (甲) 静态比较写成 `hi_i<P_lo || lo_i>P_hi`：**P1 漏裁**（hi=20 不 <20；其 v 维与谓词相交，动态救不回），扫描集错成 **{P1,P2}**（应为 {P2}）。
- (乙) 只做静态不做动态：静态通过 P2/P3/P4，**P3、P4 多扫**，扫描集错成 **{P2,P3,P4}**（应为 {P2}）。
- (丙) 动态写成 `max_v<=V_lo || min_v>V_hi`：**P4 漏裁**（min=60 不 >60），扫描集错成 **{P2,P4}**（应为 {P2}）。

## 不变量 → 代码保证位置 → 钉住的测试

1. 不漏：仅当两维边界盒至少一维不相交才判裁 —— 判定式在 `part/part.go` 的 `StaticPruned`/`DynamicPruned` —— `TestSoundness`（逐分区核验盒相交）。
2. 剪枝最大化：`prune/prune.go` 的 `Prune`：二分取 p 相交窗 [i,j)，窗内逐区做 v 判定，与暴力双维相交枚举一致 —— `TestExactness`。
3. 顺序无关：两维判定各自只读本维、互不影响 —— `TestOrderIndependent`（先静后动 / 先动后静各算一遍，扫描集逐元素比对）。
4. 失败不留痕：`part.New` 先校验后返回；`prune.Prune` 先校验谓词并解析全部 id（未知即整体失败）再产出结果；`Add` 持锁后、任何变更前完成重复/重叠校验 —— `TestRejectedOpsLeaveNoTrace`。
5. 检查计数器为非导出结构体 `prune.eval` 的非导出字段 `checked`（每次查询新建、并发安全），任何导出接口都读不到 —— `TestCheckedSublinear`（同包白盒测试直接读该字段）。
