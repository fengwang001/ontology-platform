# Dijkstra 笔记

## 优先队列中的过期记录（lazy deletion）

节点 v 的距离每次被改进都会向堆中入队一条 (v, d)，因此同一节点在堆中可能有多条记录。

不变量：权重均非负时，弹出的记录 (u, d) 若满足 d == dist[u]，则 dist[u] 已是最终最短距离。
反证：若还存在更短路径，该路径必含一条"已确定节点 -> 未确定节点"的松弛边
（源点已确定，u 未确定），其候选距离 <= 更短路径长 < d，应早于 (u, d) 弹出，矛盾。

弹出 (u, d) 时若 d > dist[u]，说明 u 此后已被更短记录改进，本条是过期记录，必须跳过：
`if d > dist[u] { continue }`。
不能用它更新邻居：d+w >= dist[u]+w，正常松弛不会产生改进；而错误地"直接覆盖"
`dist[v] = d+w` 会把 v 已确定的正确距离抬高成旧路径长度，造成抖动，
违反"已确定节点距离不再变化"的贪心不变量。旧记录也不应重新标记 u。

## 实现位置与测试

（测试完成后补充）
