# DAG 全局最长路径 — 推导与不变量

## 第三节推导（n=5，拓扑序 0,1,2,3,4）

边：0→1(2) 0→2(4) 1→3(5) 2→3(3) 3→4(1) 2→4(7) 1→4(9)

| 节点 | 前驱贡献 dist[前驱]+权 | dist |
|---|---|---|
| 0 | （源，空前缀） | 0 |
| 1 | 0+2=2 | 2 |
| 2 | 0+4=4 | 4 |
| 3 | 2+5=7；4+3=7 | 7 |
| 4 | 7+1=8；4+7=11；2+9=11 | 11 |

答案：总权 11。并列路径 0→1→4 与 0→2→4（均 11），字典序最小为 **[0 1 4]**。

- (甲) 误取 min：dist[4]=min(8,11,11)=**8**（正确 11）。
- (乙) 加源 5、边 5→6 权 100：固定从 0 出发只返回 **11**；正确全局最长为 **100**（路径 [5 6]）。
- (丙) 并列取字典序最大会返回 **[0 2 4]**；正确应返回 **[0 1 4]**。

## 四条不变量的保证位置与钉住测试

0. 关键决定：负权下「dist 源记 0、其余 -inf」与「任意起点 + 朴素参照一致」冲突，
   故 `lpath/lpath.go` 让每个节点都提供权 0 空前缀（非负权时与规则写法逐值一致），
   答案取所有边 `dist[u]+w` 的最大值（保证 ≥1 条边）。
1. 路径合法：`lpath.Solve` 回溯时沿真实出边逐步选后继（`lpath/lpath.go`），`api` 用 `wdag.OutEdges` 核验相邻有边且权值求和等于总权；测试 `TestSelfCheck`、`TestSolvePathLegal`。
2. 最长：`lpath.Solve` 按拓扑序对全部入边取 max（`lpath/lpath.go`）；测试 `TestSolveAgainstNaive`（小图枚举全部路径逐值比对）。
3. 与朴素参照一致：`api.SelfCheck` 内置图逐一跑朴素枚举参照（`api/api.go`）；测试 `TestSolveAgainstNaive`、`TestSelfCheck`。
4. 失败不留痕：`wdag.AddEdge` 全部校验先于任何状态修改（`wdag/wdag.go`）；测试 `TestRejectedOpsKeepState`。
