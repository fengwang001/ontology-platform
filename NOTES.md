# 新鲜期（freshness lifetime）选取规则

响应里可能同时出现 `s-maxage`、`max-age` 与 `Expires`，三者冲突时必须给出
确定结果。本缓存是**共享缓存**，采用如下固定优先次序：

1. `s-maxage`
2. `max-age`
3. `Expires`（给出的新鲜期为 `Expires - tDate`，负值截 0）
4. 三者都不存在：新鲜期 = 0（立即过期，不做任何启发式猜测）

## 理由

- `s-maxage` 优先于 `max-age`：按 RFC 9111，`s-maxage` 专门用于共享缓存，
  其语义就是「在共享缓存中覆盖 `max-age` 与 `Expires`」。共享缓存面向多个
  用户，源站需要一条能强制共享缓存忽略更宽松的 `max-age`/`Expires` 的指令，
  否则共享缓存可能为其他用户多供一段时间的陈旧响应。正因为本缓存是共享缓存
  （而非单个用户的私有缓存），才必须让 `s-maxage` 生效且排在最前；私有缓存
  会忽略它。
- `max-age` 优先于 `Expires`：`max-age` 是较新的显式相对秒数，RFC 9111
  规定它出现时即覆盖 `Expires`。`Expires` 依赖绝对时钟，易受时钟偏差与错误
  日期影响，只作为没有 `max-age`/`s-maxage` 时的回退。
- 以上次序是纯字段优先级，与 map 遍历顺序无关，因此结果确定。
- 三者皆无时新鲜期为 0：题目禁止启发式新鲜期，响应立刻进入过期状态。
