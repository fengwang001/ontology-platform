# 稀疏位点索引 NOTES

## 第三节推导（base=1000, indexInterval=100）

| # | 记录(位点/字节) | 物理位置 | 写前累计 | 索引条目 | 写后累计 |
|---|---|---|---|---|---|
| 1 | 1000/60 | 0 | 0 | 无（0<100） | 60 |
| 2 | 1001/50 | 60 | 60 | 无（60<100） | 110 |
| 3 | 1003/40 | 110 | 110 | (3,110)（110>=100，清零） | 40 |
| 4 | 1004/60 | 150 | 40 | 无 | 100 |
| 5 | 1007/30 | 210 | 100 | (7,210)（100>=100，边界取等） | 30 |
| 6 | 1008/80 | 240 | 30 | 无 | 110 |
| 7 | 1010/20 | 320 | 110 | (10,320) | 20 |
| 8 | 1012/50 | 340 | 20 | 无 | 70 |

完整索引：`[(3,110), (7,210), (10,320)]`

- (甲) `Lookup(1004)`：floor(4)=(3,110)，从 110 扫 1003、1004 → 返回 `(1004,150)`，扫 2 条。`Lookup(1006)`：floor(6)=(3,110)，扫 1003/1004/1007 → 返回 `(1007,210)`，扫 3 条。若错取 ceiling(4)=(7,210)，从 210 起扫，第一条即 1007，`Lookup(1004)` 错返回 `(1007,210)`。
- (乙) 条件错写为 `累计 > 100`：第 5 行累计=100 不建条目（变 130），第 6 行 130>100 建 `(8,240)`；第 8 行累计=100 仍不建。索引变 `[(3,110), (8,240)]`。此时 `Lookup(1007)`：floor(7)=(3,110)，从 110 扫 1003/1004/1007 共 3 条；正确实现 floor(7)=(7,210)，只扫 1 条。
- (丙) 追加 `1013/40`：累计 70<100 不建条目，返回 pos=390，索引不变。再追加 `1000+2147483648`、10 字节：rel=2147483648>2147483647，正确实现整体拒绝（ErrOverflow），记录/索引/累计均不变。若直接截断 int32：累计 110>=100 会先建条目，rel 截断为 -2147483648，写入条目 `(-2147483648, 430)`。`Lookup(999)` 若不查下界：无 floor 条目从 0 扫，首条 1000>=999，错返回 `(1000,0)`；正确实现报 ErrBelowBase。

## 四条不变量的落实位置与钉住测试

1. 与朴素参照一致：`segment.Lookup` 先 floor 二分再从该物理位置顺序扫描；`api.SelfCheck` 内 `naiveLookup` 对 `[base, 最后位点+1]` 全区间比对；测试 `TestNaiveConsistency`。
2. 索引良构：`segment.Append` 只在记录写入前、以当前段字节数为 pos、当前 rel 建条目，rel/pos 随追加天然严格递增且必为某记录起点；`SelfCheck` 逐条核对；测试 `TestIndexWellFormed`。
3. 与规则重算一致：`api.recompute` 按追加规则对记录序列重放；`SelfCheck` 比对；测试 `TestIndexRecompute`。
4. 失败不留痕：`segment.Append`/`Lookup` 全部校验先于任何状态修改，哨兵错误可判定；`SelfCheck` 与测试 `TestFailureNoTrace` 比对拒绝前后 Records/Entries/Bytes 完全一致。
