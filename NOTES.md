# 按时间戳查位点 — 推导与不变量

## 十行分步表（base=100；仅当 TS 严格大于迄今最大才记索引项）

| Off | TS | 迄今最大 | 记索引? | 追加后完整索引 (TS,Off) |
|-----|----|----|--------|------------------------|
| 100 | 50 | 50 | 是（首条必记） | (50,100) |
| 101 | 40 | 50 | 否 | (50,100) |
| 102 | 70 | 70 | 是 | (50,100) (70,102) |
| 103 | 60 | 70 | 否 | (50,100) (70,102) |
| 104 | 70 | 70 | 否（等于不记） | (50,100) (70,102) |
| 105 | 65 | 70 | 否 | (50,100) (70,102) |
| 106 | 90 | 90 | 是 | (50,100) (70,102) (90,106) |
| 107 | 80 | 90 | 否 | (50,100) (70,102) (90,106) |
| 108 | 90 | 90 | 否（等于不记） | (50,100) (70,102) (90,106) |
| 109 | 85 | 90 | 否 | (50,100) (70,102) (90,106) |

最终索引：(50,100) (70,102) (90,106)，LEO=110。

- (甲) `Lookup(45)=(100,true)`，`Lookup(85)=(106,true)`。若对原始 TS 切片 `[50 40 70 60 70 65 90 80 90 85]` 直接 `sort.Search`：`Lookup(45)` 返回 **102**，`Lookup(85)` 返回 **108**（均错）。
- (乙) `Lookup(65)=(102,true)`。若做成「`TS<=t` 的最后一条」，返回 **105**；从 105 重读会漏掉 Off **102、104**（TS=70，>=65）。
- (丙) `Lookup(80)=(106,true)`，`Lookup(91)=(110,false)`。「恰好等于 t」实现 `Lookup(80)` 返回 **107**；「t 大于全部时返回最后一条位点」实现 `Lookup(91)` 返回 **109**，下游会多读 Off **109**（TS=85<91，本不该重读）。

## 四条不变量：保证位置与钉住它的测试

1. 与朴素参照一致：`tidx.Index.Add` 只记严格新最大、`tidx.Index.Lookup` 在索引上二分（tidx/tidx.go）；`TestNaiveConsistency`。
2. 索引严格单调且每项 TS 等于其位点及之前的最大 TS：`tidx.Index.Add` 的严格大于判断（tidx/tidx.go）；`TestIndexStrict`。
3. 查询单调：`Lookup` 语义为「首个 TS>=t」，t 越大命中位点越靠后（tlog/tlog.go `Log.Lookup`）；`TestQueryMonotonic`。
4. 失败不留痕：`tlog.Log.Append` 先整批校验（负 TS、容量）再改任何状态（tlog/tlog.go）；`TestFailureAtomic`。
