# AST 常量折叠 — 推导与不变量

## 六行分步表：`((x*1)+(y*0))+(3*4)`，自底向上单趟折叠

| 步 | 折叠的子表达式 | 应用规则 | 折叠后结果 |
|---|---|---|---|
| 1 | 叶节点 `x,1,y,0,3,4` | 规则1/2（字面量自身已折叠，Var 不折叠） | 原样 |
| 2 | `x*1` | 规则5（`e*1→e`，被消去的 `1` 纯） | `x` |
| 3 | `y*0` | 规则5（`e*0→0`，被消去的 `y` 不含 `/0`） | `0` |
| 4 | `x+0` | 规则5（`e+0→e`） | `x` |
| 5 | `3*4` | 规则4（两子节点均为 int 字面量） | `12` |
| 6 | `x+12` | 无规则适用（`x` 非字面量，`12` 非恒等元素） | `x+12`（最终结果） |

## 三问

- **(甲)** `1/0` 折叠后**保持原样**（规则4 例外）。若实现照常折叠 `/`：整数 `1/0` 无法算出值，强行求值会在折叠期 panic；即便算出某个"值"，也是把一次必然的运行时除零错误掩盖成正常常量，折叠前后"结果或错误"不再一致。
- **(乙)** `false && (1/0 > 0)` 折叠后为 **`false`**（规则6：左侧已决定结果，右侧不折叠不检查）。若实现先折叠右侧：会把 `1/0` 的除零提前暴露——折叠期 panic 或把本应短路为 `false` 的表达式变成错误，短路语义被破坏。
- **(丙)** `(1/0)*0` 折叠后**保持原样**（规则5 纯度守卫：被消去的 `1/0` 含右操作数恒为 0 的 `/`）。若实现无条件应用 `e*0→0`：结果错成 **`0`**——原表达式必然运行时除零，折叠后却返回正常值 `0`，错误被吞掉，违反语义保持。

## 四条不变量落点

1. **与朴素参照一致**：`fold.fold` 自底向上、`/0` 保留、恒等带纯度守卫（fold/fold.go）；测试 `TestRefConsistency`（api/api_test.go）。
2. **短路不越界**：`fold.bin` 的 `&&`/`||` 分支先折左侧、命中短路直接返回、右侧不进入折叠（fold/fold.go）；测试 `TestShortCircuit`（fold/fold_test.go）。
3. **除零保留**：规则4 例外 + `mayDivZero` 纯度守卫（fold/fold.go）；测试 `TestDivZeroKept`（fold/fold_test.go）。
4. **失败不留痕**：`api.validate` 三个互不相同哨兵错误 + `SelfCheck` 整体判定（api/api.go）；测试 `TestFaultInjection`（api/api_test.go）。
