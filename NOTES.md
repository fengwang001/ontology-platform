# Grapheme 分段推导与不变量落点

## 第三节推导：12 码点串的六簇表

各码点 UTF-8 字节长：e=1, U+0301=2, a=1, ZWJ=3, b=1, CR=1, LF=1, RI=4, c=1, 👍=4, 🏻=4。

| # | 码点 | Start | End | Runes | Bytes | 粘合依据 |
|---|------|-------|-----|-------|-------|----------|
| 1 | e, U+0301 | 0 | 3 | 2 | 3 | 规则3：Extend(0301) |
| 2 | a, ZWJ, b | 3 | 8 | 3 | 5 | 规则3：Extend(ZWJ)；规则2：ZWJ 粘 b |
| 3 | CR, LF | 8 | 10 | 2 | 2 | 规则1：CRLF |
| 4 | RI, RI | 10 | 18 | 2 | 8 | 规则4：到首个 RI 连续数=1 为奇 |
| 5 | c | 18 | 19 | 1 | 1 | 两侧均不满足任一条 |
| 6 | 👍, 🏻 | 19 | 27 | 2 | 8 | 规则3：emoji 修饰符 Extend |

- **(甲)** 逐 rune 一簇得 **12** 簇；错拆 5 对：e|0301（规则3）、a|ZWJ（规则3）、ZWJ|b（规则2）、CR|LF（规则1）、👍|🏻（规则3）。
- **(乙)** 簇4 起始错报 **7**（正确 10，差 3：0301 多 1 字节 + ZWJ 多 2 字节）；簇6 起始错报 **10**（正确 19，差 9：再累计两个 RI 各多 3 字节）。
- **(丙)** 三连 RI 正确切 **2** 簇（RI RI｜RI，第三处连续数=2 为偶须切）；「前一个是 RI 就不切」的朴素实现错成 **1** 簇。

## 四条不变量落点

1. **与朴素参照一致**：`grapheme.Decode` 单遍增量累加偏移；`api.SelfCheck` 内置朴素参照逐簇比对；测试 `TestAgainstNaive`。
2. **切分不多不少**：边界判定唯一入口 `seg.NoBreak`；测试 `TestBoundaryRules`、`api.SelfCheck`。
3. **偏移自洽**：`Decode` 内 `start`/`i` 增量累加、末簇 `End=len(s)`；测试 `TestOffsetChain`。
4. **失败不留痕**：`api` 先校验（空串/越界）且 `Decode` 遇非法 UTF-8 整体返回错误，均不写任何状态；测试 `TestFailures`。
