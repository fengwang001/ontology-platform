# 区间并集增量维护：推导与不变量落点

## 八步推导（maxIntervals=8，`+`/`-` 为变更日志条目）

| # | 操作 | 输出的变更日志 | 操作后并集 |
|---|---|---|---|
| 1 | Add(0,10) | `+[0,10)` | `[0,10)` |
| 2 | Add(10,20) | `-[0,10)` `+[0,20)` | `[0,20)` |
| 3 | Add(30,40) | `+[30,40)` | `[0,20)` `[30,40)` |
| 4 | Withdraw(15,35) | `-[0,20)` `+[0,15)` `-[30,40)` `+[35,40)` | `[0,15)` `[35,40)` |
| 5 | Add(15,35) | `-[0,15)` `-[35,40)` `+[0,40)` | `[0,40)` |
| 6 | Withdraw(20,25) | `-[0,40)` `+[0,20)` `+[25,40)` | `[0,20)` `[25,40)` |
| 7 | Add(20,25) | `-[0,20)` `-[25,40)` `+[0,40)` | `[0,40)` |
| 8 | Withdraw(0,40) | `-[0,40)` | （空） |

- (甲) 若只合并严格重叠、不合并邻接：第 2 步后错成 **2 段** `[0,10)` `[10,20)`（应为 1 段 `[0,20)`）；第 5 步后错成 **3 段** `[0,15)` `[15,35)` `[35,40)`（应为 1 段 `[0,40)`）。
- (乙) 第 4 步若不判残留段为空：`[0,20)` 的"右残留"多出非法条目 `+[35,20)`，`[30,40)` 的"左残留"多出非法条目 `+[30,15)`，两条都是 s>=e 的空段。
- (丙) 第 4 步若只处理第一个相交区间：`[30,40)` 完全不被处理、原样残留，最终并集错为 `[0,15)` `[30,40)`；正确应剩 `[0,15)` `[35,40)`（即 `[30,40)` 应被撤成 `[35,40)`）。

## 四条不变量的保证位置与钉住测试

1. **与朴素重放一致**：`union.Add/Withdraw` 用二分定位+局部前向扫描，合并/拆分语义与 `api` 内朴素参照 `naiveAdd/naiveWithdraw`（整体重算、O(n²)）逐步对拍；测试 `TestViewMatchesNaive`。
2. **变更日志自洽**：`union.Add` 先逐段输出被合并旧段的 `-` 再输出合并段 `+`，`Withdraw` 先 `-I` 再 `+` 非空残留段，`-` 的对象此刻必在并集中；测试 `TestChangelogSelfConsistent`。
3. **归并完备**：`union` 的不相交区间集按起点有序，Add 吞并全部接触段（含邻接）、Withdraw 残留段与邻段必留缝；测试 `TestMaximalInvariant`。
4. **失败不留痕**：`union.Add/Withdraw` 先做合法性与容量检查再改任何状态，哨兵错误 `ErrInvalid/ErrTooMany/ErrNotFound` 互不相同；测试 `TestRejectLeavesState`。
