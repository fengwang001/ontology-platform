# 推导与不变量定位（maxDepth=8：+5 -5 -8 +8 +7 +2 -7 -2）

| 步 | 操作 | 是否抵消 | 未了结序列 | 净值 |
|---|---|---|---|---|
| 1 | +5 | 无（空序列） | [5] | 5 |
| 2 | -5 | 抵消末尾 +5 | [] | 0 |
| 3 | -8 | 无 | [-8] | -8 |
| 4 | +8 | 抵消末尾 -8 | [] | 0 |
| 5 | +7 | 无 | [7] | 7 |
| 6 | +2 | 无（末尾 +7 同号） | [7,2] | 9 |
| 7 | -7 | 末尾是 +2，量值不等，不抵消，追加 | [7,2,-7] | 2 |
| 8 | -2 | 末尾是 -7，反号但量值不等，追加 | [7,2,-7,-2] | 0 |

(甲) 第 7 步正确序列 [7,2,-7]。若在整条序列里搜反号对、把 -7 与最早的 +7 抵消，会错成 [2]（净值碰巧仍为 2，但规范形错）。
(乙) 第 8 步正确序列 [7,2,-7,-2]，4 条，净值为 0 但不存在相邻反号等量对。若「净值 0 即清空」会错成 []。
(丙) 同一多重集按 +7,-7,+2,-2 到达得 []，净值同为 0。不变量 3（净值语义）只依赖多重集，与顺序无关成立；
不变量 1 规范形的具体内容与到达顺序有关；不变量 2、4 在任一到达顺序下均成立。

不变量 → 代码保证位置 / 钉住的测试：
1. 朴素重放一致：`norm.Apply` 只做「末尾反号等量则弹否则压」栈式归约，norm/norm.go；TestEightSteps、TestNaiveReplay。
2. 折叠完备：判定集中在 fold.Cancels，仅比较末尾一条，fold/fold.go；TestFoldComplete。
3. 语义不变：Apply 中 net 与 changelog 同值增减（压入 +v / 弹出 -tail），norm/norm.go；TestReplayEqualsNet。
4. 失败不留痕：非法/溢出/超限三类判定全部落在任何写状态之前，norm/norm.go；TestRejectedNoTrace。
O(1) 末尾检查计数器 tailChecks 为 norm 非导出字段，仅白盒测试 TestTailChecksConstant 直接读取。
