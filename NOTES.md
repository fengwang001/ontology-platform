# Elias delta 编码推导与不变量说明

## 一、序列 [1, 2, 4, 10, 16] 的分步表（delta = gamma(L) + 去最高位后 L-1 位）

| n | L（位数） | gamma(L) | 值层（L-1 位） | delta(n) |
|---|---|---|---|---|
| 1 | 1 | `1` | （空） | `1` |
| 2 | 2 | `010` | `0` | `0100` |
| 4 | 3 | `011` | `00` | `01100` |
| 10 | 4 | `00100` | `010` | `00100010` |
| 16 | 5 | `00101` | `0000` | `001010000` |

拼接 27 位：`10100011 00001000 10001010 000`，末字节补 5 个 0 → 字节 `A3 08 8A 00`。
解码：γ 得 L=1,2,3,4,5；再读 L-1 位低位 0,0,0,2,0；n=2^(L-1)+低位 → 1,2,4,10,16。

- (甲) 误把全位 `100` 写上：错码 = `011`+`100` = **`011100`**（正确 `01100`）。正确解码器读 γ 得 L=3，只再读 2 位 `10`，得 2^2+2 = **6**（末尾残留 1 位 `0`）。
- (乙) `Encode([1])` = `1` 后补 7 个 0 = **`0x80`**；`Encode([])` 返回**空字节切片（长度 0，nil）**。
- (丙) γ(64)：64=`1000000`（7 位），γ = 6 个 0 + `1000000` = **`0000001000000`（13 位）**；再接 63 位值层，码总长 **76 位**。不检查时构造 2^63+低位，超出 int64 上限 2^63-1，必须报 **ErrOverflow**。

## 二、四条不变量的保证位置与钉住测试

1. 与朴素参照一致：`dcode/dcode.go` 的 `writeGamma`/`EncodeOne`/`Encode`（先 γ(L) 后 L-1 位）与 `DecodeOne`（数 0 定 k，读 k 位 L，再读 L-1 位）；测试中独立实现朴素参照逐字节/逐元素比对。钉住：`TestNaiveReference`。
2. 切分点无关：`StreamDecoder.Feed` 仅追加字节、不触码边界，`Decode` 对完整缓冲一次性解码。钉住：`TestChunking`（逐切点循环）。
3. 确定性/无前缀：`Encode` 为无随机、无 map 遍历的纯函数；γ 无前缀 + 值层固定 L-1 位 ⇒ δ 无前缀。钉住：`TestDeterministic`（重复编码逐字节相同 + 全部唯一可解）。
4. 失败不留痕：`Decode` 先试解整流、出错即返回 nil（试解结果丢弃），不暴露任何前缀；哨兵错误互不相同，纯函数无内部状态。钉住：`TestFailuresNoPartial`。

复杂度计数器在 `bits.Reader.lastCodeBits`（非导出字段，仅 bits/dcode 内部经 StartCode/CodeBits 使用，不出现在 api 公开接口），`DecodeOne` 起止各打点一次。钉住：`TestDecodeCounter`。并发钉住：`TestConcurrent`。
