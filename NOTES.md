# Notes: 并查集平衡推导

## 问题

Union 时若总把 y 的根挂到 x 的根下且不做平衡，对手可按固定顺序合并，
使每个新根只增加一个叶子，结构退化为一条链，Find 最坏 O(n)。

## 按秩合并：树高 ≤ log2(n)

每个根维护秩 rank（上界树高）。合并两棵树时把秩小的根挂到秩大的根下：

- 秩不等：新树高由秩大的一方决定，树高不增。
- 秩相等：挂接后新根秩 +1，这是树高唯一增长的途径。

一个秩从 k 增到 k+1 的根，其子树规模至少翻倍（合并了两棵 ≥2^k 的树），
故秩 k 的树至少含 2^k 个节点；n 个节点时秩 ≤ log2(n)，Find 跳数 O(log n)。

## 路径压缩

Find 时递归把沿途节点直接指向根，后续查询近似 O(1)。
压缩不增加秩（秩只作上界），与按秩合并配合的均摊复杂度为
反阿克曼级 O(alpha(n))，对任意实际 n 都 ≤5，即“近乎常数”。

## 测试钉住

- `TestChainUnionHeight`：n=2^16 顺序链式 Union，`uf.LastFindHops()` ≤ 16。
- `TestBadImplDegenerates`：内联“总挂到 x 根下”的错误实现，同序列跳数 > 1000。
- `TestRandomHopBound`：固定随机种子 10000 次 Union，跳数 ≤ 2·log2(n)+2。
