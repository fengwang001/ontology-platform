# NOTES
十二步表：返值/错类 | 步后V | k1/k2/k3 的(版,字节) | m1/m2/m3 累计调用
1 W(k1,"a"): — |1| (1,a)/∅/∅ | 0,0,0
2 W(k2,"bad"): — |1| (1,a)/(1,bad)/∅ | 0,0,0
3 Upgrade(3): — |3| 同 | 0,0,0
4 W(k3,"c,d"): — |3| (1,a)/(1,bad)/(3,c,d) | 0,0,0
5 Read(k1): "a;2" |3| (3,a;2)/(1,bad)/(3,c,d) | 1,1,0
6 Read(k2): 迁移失败 |3| (3,a;2)/(1,bad)/(3,c,d) | 2,2,0
7 Upgrade(4): — |4| 同 | 2,2,0
8 Read(k1): 缺迁移 |4| (3,a;2)/(1,bad)/(3,c,d) | 2,2,0
9 Reg(m3): — |4| 同 | 2,2,0
10 Read(k1): "4:a;2" |4| (4,4:a;2)/(1,bad)/(3,c,d) | 2,2,1
11 Read(k3): "4:c,d" |4| (4,4:a;2)/(1,bad)/(4,4:c,d) | 2,2,2
12 Read(k1): "4:a;2" |4| 同 | 2,2,2
(甲) 5="a;2"，11="4:c,d"。总从v1重跑：11→"4:c;d;2"（m1追加,2、m2把,替成;、m3加前缀）；只跑末步：5→"a"（m2对"a"无替换）。
(乙) 写回字节但不更版：10="4:a;2;2"（对"a;2"重跑m1m2m3），12="4:4:a;2;2;2"。完全不写回：返值不变；计数 m1/m2/m3=4/4/3（正确=2/2/2；k1 在 10、12 步因仍是 v1 各重跑一次 m1/m2，12 步 m3 也多跑一次）。
(丙) 6后 k2=(1,"bad")；前缀先写回则 k2=(2,"bad,2")。8与9间插 Read(k2)：正确=缺迁移、0个迁移被调用；边跑边发现=迁移失败（m2的bad原始错），m1、m2 各多调用1次，m3未触达。
不变量1 朴素一致：kv.go 的 Read 只从存储版本起链、写回值即纯函数重算值；由 api_test.go 的 TestNaiveReference 钉住。
不变量2 每步至多一次：kv.go 的 flight 同键去重 + 成功后存储版本前移；由 TestMigrationWriteback、TestConcurrentReadDedup 钉住。
不变量3 无中间版本：kv.go 在链全部成功后于同一 mu 临界区整体替换(版本,字节)；由 TestFailureLeavesNoTrace 钉住。
不变量4 失败不留痕：schema.go 的 Register/Migrate 与 kv.go 的 Upgrade/Read 全部先校验后改态；由 TestAPIErrors 钉住。
