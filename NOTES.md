# Chang–Roberts 推导与不变量

## 八行分步表（环序 N1(3)→N2(7)→N3(2)→N4(5)→N1，FIFO 处理）

| # | 接收节点 | 收到ID(来源) | 比较 | 动作/去向 |
|---|---------|-------------|------|----------|
| 1 | N2 | 3 (N1 发起) | 3 < 7 | 丢弃 |
| 2 | N3 | 7 (N2 发起) | 7 > 2 | 转发 → N4 |
| 3 | N4 | 2 (N3 发起) | 2 < 5 | 丢弃 |
| 4 | N1 | 5 (N4 发起) | 5 > 3 | 转发 → N2 |
| 5 | N4 | 7 (N3 转发) | 7 > 5 | 转发 → N1 |
| 6 | N2 | 5 (N1 转发) | 5 < 7 | 丢弃 |
| 7 | N1 | 7 (N4 转发) | 7 > 3 | 转发 → N2 |
| 8 | N2 | 7 (N1 转发) | 7 == 7 | 宣告当选 |

胜者 N2（ID 7），总消息数 8（4 次发起 + 4 次转发）。

**(甲)** 宣告条件错写成「≥」：第 7 行 N1 收到 7 ≥ 自身 3，N1 会错误宣告自己当选；ID 7 被 N1 吞下不再转发，真正的最大者 N2 永远收不到自己的 ID，胜者错为 N1(3)，正确性被破坏。
**(乙)** 比较方向写反（小才转发、大则丢弃）等于选最小 ID：只有最小 ID 2 能绕环一周回到 N3，胜者变成 N3（ID 2）。
**(丙)** N4 后继未闭合（空/错）：第 5 行 N4 转发 ID 7 后消息进入空指针——要么 panic，要么消息丢失；ID 7 永远回不到 N2，没有任何节点能收到自己的 ID，选举无法终止。

## 四条不变量：保证位置与钉住它的测试

1. **正确性**：`elect.Elect` 严格按「大转发/小丢弃/等宣告」模拟，`api` 内以 `ring.Max` 朴素遍历为参照；测试 `TestElectMatchesNaiveMax`。
2. **终止性**：`elect.Elect` 中每条消息跳数 ≤ n 的守卫（非最大 ID 必在 ≤n 跳内遇更大者被丢弃）；测试 `TestElectMatchesNaiveMax`（多档规模必返回）。
3. **环闭合**：`ring.Add`/`ring.Remove` 维护 `next` 链与尾指针，末节点恒指向首节点；测试 `TestRingClosure`。
4. **失败不留痕**：`ring.Add`/`ring.Remove` 先校验（空/重复/不存在）后修改，拒绝路径零写入；测试 `TestRejectedOpsLeaveStateUnchanged`。
