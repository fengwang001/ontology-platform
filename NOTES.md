# 列级 LWW：推导与不变量落点

## 八步分步表（Key=R；`v@TS` 表示胜者值与时间戳，`T@TS` 表示墓碑，`-` 表示该列无任何条目）

| 步 | 操作 | c1 | c2 | c3 | 冲突 | View(R) |
|---|---|---|---|---|---|---|
| 1 | Put(R,c1,5,"a") | a@5 | - | - | 否 | {c1:a} |
| 2 | Put(R,c2,7,"x") | a@5 | x@7 | - | 否 | {c1:a, c2:x} |
| 3 | Put(R,c1,5,"b") | b@5 | x@7 | - | **是** | {c1:b, c2:x} |
| 4 | Put(R,c1,9,"c") | c@9 | x@7 | - | 否 | {c1:c, c2:x} |
| 5 | Del(R,c2,8) | c@9 | T@8 | - | 否 | {c1:c} |
| 6 | Put(R,c1,4,"old") | c@9（忽略） | T@8 | - | 否 | {c1:c} |
| 7 | Put(R,c3,6,"y") | c@9 | T@8 | y@6 | 否 | {c1:c, c3:y} |
| 8 | Del(R,c3,2) | c@9 | T@8 | y@6（忽略） | 否 | {c1:c, c3:y} |

- **(甲)** 平局两值取字典序大者：c1 正确为 **"b"**。若平局只保留旧值，c1 错成 **"a"**。
- **(乙)** Del 的 TS=2 < c3 现存 6，墓碑被忽略：c3 正确为 **"y"（仍在视图中）**。若 Del 无条件删除，c3 错成**缺席**。
- **(丙)** 列级删除只影响 c2：View(R) 里 **c1 仍在**（{c1:c}）。若是记录级 LWW，第 4 步后全 Key 最大 TS=9，第 5 步 Del(TS=8) 被整体忽略，c2 错留 **"x"**。

## 四条不变量：代码落点与钉住它的测试

1. **批量重算一致**：`cell.Apply` 只按 (TS, 平局固定规则) 裁定，与到达顺序无关 → `TestBatchEquiv`（随机序列 vs 朴素批量重算逐列比对）。
2. **列隔离**：`row.Row` 用 `map[col]cell.Cell`，Put/Del 只读写 `cols[col]` 一项 → `TestColumnIsolation`。
3. **墓碑语义**：`cell.Apply` 中墓碑与值按同一 TS 规则参赛（TS 更大才替换，平局墓碑胜），`row.View` 跳过墓碑列 → `TestTombstone`。
4. **失败不留痕**：`api.Put/Del` 先做全部参数校验、通过后才触碰状态 → `TestRejectNoTrace`（拒绝前后胜者、冲突集合逐项相同）。
