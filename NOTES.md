# A-Res 推导与不变量

## 第三节：八步分步表（k=2，key = u^(1/W)，us 按序取用）

| 步 | 元素 | 取用的 u | 键 | 结果 | 蓄水池（按排名） | Consumed |
|---|---|---|---|---|---|---|
| 1 | a W=1 | 0.30 | 0.3000 | 插入 | a:0.3000 | 1 |
| 2 | b W=2 | 0.49 | 0.7000 | 插入 | b:0.7000, a:0.3000 | 2 |
| 3 | c W=0.5 | 0.81 | 0.6561 | 替换 a | b:0.7000, c:0.6561 | 3 |
| 4 | d W=0 | 不取 | — | 错误：权重为 0 | b:0.7000, c:0.6561 | 3 |
| 5 | e W=4 | 0.0625 | 0.5000 | 未入选（0.5<0.6561） | b:0.7000, c:0.6561 | 4 |
| 6 | f W=1 | 0.72 | 0.7200 | 替换 c | f:0.7200, b:0.7000 | 5 |
| 7 | g W=2 | 0.64 | 0.8000 | 替换 b | g:0.8000, f:0.7200 | 6 |
| 8 | h W=0.5 | 0.9 | 0.8100 | 替换 f | h:0.8100, g:0.8000 | 7 |

**(甲)** 若 key=u·W：g 的键=0.64×2=**1.28**（>1，说明它已不是 (0,1) 上的合法键，权重被乘性扭曲而非指数变换）；轨迹中 b:0.98 始终留池、f:0.72 被 g 挤出、h:0.45 未入选，最终样本错成 **{g, b}**（正确为 {h, g}）。

**(乙)** 若保留键最小的 k 个：七个被接受元素的键中最小两个是 a:0.3000、e:0.5000，最终错成 **{a, e}**。

**(丙)** 若 W==0 当「键为 0、照常取数」：d 取 0.0625 键 0 未入选；e 取 0.72 键 0.72^(1/4)≈**0.9212** 替 c；f 取 0.64 键 **0.6400** 未入选（<b:0.7000）；g 取 0.9 键 **0.9487** 替 b；h 取 0.25 键 **0.0625** 未入选。最终错成 **{g, e}**，Consumed=**8**。正确实现 Consumed=**7**，随机源剩 **u=0.25** 未用。

## 第二节四条不变量：代码位置与钉住测试

1. 批量参照一致：`resv.(*Pool).Consider` 的排名全序+小顶堆替换，对照 `api.reference` 整体排序；测试 `TestBatchReference`。
2. 随机数守恒：`api.Offer` 两阶段——预检通过后每个有效行恰好 `rnd.Source.Next` 一次；测试 `TestBatchReference`、`TestConcurrentOffer`。
3. 规模=min(k,接受数) 且 ID 互异：`resv.Pool` 容量 k + `api.Sampler.ids` 已接受集合；测试 `TestBatchReference`。
4. 失败不留痕：`api.Offer` 第一阶段全量预检，任一行失败即整体返回、游标与池不变；测试 `TestRejectionNoTrace`。
