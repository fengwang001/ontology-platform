# NOTES

max=12 step=4 delay=2 maxOpen=2，同一 Key=K；`(a,b,c)` 表示输出 `(K,[a,b),c)`。

| 步 | wm | S / Emin | 裁决 | 本步输出 | 后仍开窗口 |
|---|---|---|---|---|---|
| 1 TS=-5 | -7 | -12 / -4 | 接受 | (-12,-8,0) | {-12} |
| 2 TS=1 | -1 | 0 / 4 | 接受 | (-12,-4,1) | {-12,0} |
| 3 TS=-2 | -1 | -12 / 0 | 接受 | 无 | {-12,0} |
| 4 TS=6 | 4 | 0 / 8 | 接受 | (-12,0,2),(0,4,1) | {0} |
| 5 TS=3 | 4 | 0 / 4 | 丢弃(wm=Emin) | 无 | {0} |
| 6 TS=11 | 9 | 0 / 12 | 接受 | (0,8,2) | {0} |
| 7 TS=12 | 10 | 12 / 16 | 接受 | 无 | {0,12} |
| 8 TS=19 | 17 | 12 / 20 | 接受 | (0,12,3),(12,16,1) | {12} |
| Flush | +∞ | — | — | (12,20,2),(12,24,2) | {} |

甲：正确 6 步=(0,8,2)，8 步=(0,12,3),(12,16,1)。翻滚窗口 6 步错成 (0,8,1)（[4,8) 内仅 TS=6）；8 步错成 (0,12,1)（[8,12) 内仅 TS=11）,(12,16,1)（此窗恰好相同）。
乙：TS=12 被闭区间归入 S=0、只计入未触发的末窗，S=12 不建状态；8 步错成 (0,12,4),(12,16,0)（空状态首窗计数 0 也输出）。
丙：向零截断使 TS=-5、-2 都并入 S=0；4 步错成仅 (0,4,3)；正确实现 S=-12 的三条记录 (-12,-8,0)、(-12,-4,1)、(-12,0,2) 整条消失（(0,4,1) 计数被篡成 3）。

## 不变量（保证位置 / 钉住测试）

1. 批量重算一致：后缀累计在 `cagg.(*Agg).Feed` 接受时写入，触发即定型、末窗后清除（cagg/cagg.go）/ `TestBatchRecompute`
2. 累积单调、末窗=接受总数：接受时对 j≥jMin 的全部子窗计数 +1，末窗覆盖全窗（cagg/cagg.go `Feed`）/ `TestMonotone`
3. 不早触发、wm 只进不退：先更新 maxTS 再裁决，`drain` 仅弹出 End≤wm 的堆顶（cagg/cagg.go）/ `TestWatermark`
4. 失败不留痕：cagg.Feed 先跑无副作用的 preflight（空 Key 与容量干跑，非法即整体拒绝、绝不落状态），三类哨兵错误（cagg/cagg.go，api/api.go 加锁转发）/ `TestRejectedBatchNoTrace`
