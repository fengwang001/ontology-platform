# 幂等生产者序号校验：推导与不变量

## 十步推导（N=2, W=2, pid=7；窗口写法 seq@位点；lastSeq/窗口指本请求所在 (pid,分区)）

| # | 请求 e/p/s | 判定 | 位点 | lastSeq | 窗口 | 当前 epoch |
|---|---|---|---|---|---|---|
| 1 | 0/0/0 | 追加（新 pid，规则3） | 0 | P0:0 | 0@0 | 0 |
| 2 | 0/1/0 | 追加（P1 无状态，seq=0） | 0 | P1:0 | 0@0 | 0 |
| 3 | 0/0/1 | 追加（seq=last+1） | 1 | P0:1 | 0@0,1@1 | 0 |
| 4 | 0/0/1 | 重复（在窗口） | 1 | P0:1 | 0@0,1@1 | 0 |
| 5 | 0/0/3 | 乱序错误（跳号） | 无 | P0:1 | 0@0,1@1 | 0 |
| 6 | 0/0/2 | 追加 | 2 | P0:2 | 1@1,2@2 | 0 |
| 7 | 0/0/0 | 重复已过期错误（≤last 且不在窗口） | 无 | P0:2 | 1@1,2@2 | 0 |
| 8 | 1/1/0 | 追加（升级 epoch，清空 pid7 全部分区状态） | 1 | P1:0 | 0@1 | 1 |
| 9 | 0/0/3 | 围栏错误（0<1） | 无 | P0：无状态 | 空 | 1 |
| 10 | 1/0/0 | 追加（P0 状态已清空，seq=0） | 3 | P0:0 | 0@3 | 1 |

最终日志：分区0 = (e0,s0),(e0,s1),(e0,s2),(e1,s0) 共4条；分区1 = (e0,s0),(e1,s0) 共2条。

(甲) 第4步=重复、返回位点1；第7步=重复已过期错误。若把重复一律当乱序错误：第4步返回乱序错误，生产者会误以为位点1那条根本没写成功，于是重发或报错（实际已落盘）；第7步也返回乱序错误，与跳号无法区分。
(乙) 升级只改 epoch 不清状态：第8步按 (7,P1) 旧状态判定，seq0 在窗口 → 重复、返回位点0，不追加；第10步 seq0≤last2 且不在窗口{1,2} → 重复已过期错误。最终分区0=3条、分区1=1条（正确为4、2）。
(丙) 状态按 pid 共用：第2步共享窗口里已有 seq0@0 → 重复、返回的是分区0的位点0，分区1不落盘；第10步共享状态 last=0、窗口{0@1} → 重复、返回位点1，不追加。最终分区0=3条、分区1=1条。回到正确实现但第9步不做围栏：P0 状态已在第8步清空，seq=3≠0 → 乱序错误，不追加。

## 四条不变量落实位置与钉住它的测试

1. 与朴素参照一致：`api.naive`（升级逐分区清空、窗口线性查找）逐请求比对 → `TestNaiveEquivalence`、`Server.SelfCheck`。
2. 恰好一次落盘：`broker.Produce` 只有判定通过才 append，重复走窗口拿旧位点 → `TestTenStep`、`TestConcurrentDuplicate`。
3. 围栏：`broker.Produce` 第2步先比 epoch 返回 `ErrFenced` → `TestFencing`。
4. 失败不留痕：所有错误路径在任何写操作之前 return → `TestRejectNoTrace`。
