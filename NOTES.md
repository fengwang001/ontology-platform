# Fenwick 树（BIT）笔记

## 为什么内部下标必须 1 起算

- 步长由 `lowbit(i) = i & -i`（最低位 1 对应的值）决定：
  更新 `i += lowbit(i)` 走向父节点，查询 `i -= lowbit(i)` 走向左侧聚合节点。
- 若下标 0 起算且不做平移，则第一次更新 i=0 时 `lowbit(0) = 0 & -0 = 0`，
  于是 `i += 0` 使循环变量原地不动，更新永远停在同一节点，进程卡死。
  同理 `i -= 0` 也无法让查询终止于 0。
- Fenwick 的节点语义建立在 “位置 i 负责长度 lowbit(i) 的区间” 上，
  lowbit(0)=0 意味着 0 号节点无合法区间，所以树本身不含 0 号位。
- 结论：内部数组下标必须 1..n；对外 API 保持 0 起算，
  在入口统一平移 `j := i + 1`，查询终止条件保持 `j > 0`。

## 语义与测试索引（代码完成后回填）

<!-- 语义条目 -> 代码位置 / 测试函数名 -->
