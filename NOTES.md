# 面追踪推导与不变量对照

## 第三节推导
n=4，E={(0,1),(1,2),(0,2),(1,3),(0,3)}；rot[0]=[1,2,3] rot[1]=[0,3,2] rot[2]=[0,1] rot[3]=[1,0]。
next(u→v)=v→w，w 为 rot[v] 中紧挨 u 前面的邻居（循环），即前驱。

| 起始有向边 | 走出的面（顶点序列） | 面长度 |
|---|---|---|
| 0→1 | 0→1→2→0 | 3 |
| 1→0 | 1→0→3→1 | 3 |
| 0→2 | 0→2→1→3→0 | 4 |

10 条有向边各被覆盖一次，Σ面长=10=2E。F=3；V−E+F=4−5+3=2=1+C（C=1），欧拉公式成立。

- (甲) 错取"后面"（后继）：0→1→3→0，走出面 [0,1,3]；正确应走出 [0,1,2]。
- (乙) 误用 V−E+F=2：6−6+3=3≠2，误判为不成立；正确公式 V−E+F=1+C，6−6+3=3=1+2（C=2），应判成立。
  （注：题给 F=3 是计嵌套的几何嵌入；纯环序追踪两个三角形得 F=4，嵌套关系不在环序中。）
- (丙) 只数内面漏外平面：F=2，4−5+2=1≠2，误判为不成立；正确 F=3，4−5+3=2=1+1，应判成立。

## 四条不变量落点
1. 面追踪完整（每条有向边恰属一面，Σ面长=2E）：face.Trace 用 visited 标记逐有向边出发，数学上每条有向边恰入一环；钉于 TestFaceCoverage 与 SelfCheck。
2. 与朴素参照一致：face.NaiveFaces 用扫描定位+环序列去重独立计数；钉于 TestMatchesNaive。
3. 欧拉公式 V−E+F=1+C：api.EulerHolds，C 由 face.Components 并查集统计；钉于 TestEuler（含亏格式嵌入反例与不连通情形）。
4. 失败不留痕：pg.AddEdge/SetRotation 全部校验通过后才写状态，四类哨兵错误互不相同；钉于 TestRejectKeepsState。
