# 基数估计器推导笔记

哈希（x=key 字节和，m=4，j=x%4，w=(x/4)%4+1）：a→(j1,w1) b→(2,1) c→(3,1) d→(0,2) e→(1,2) f→(2,2)

## 八步分步表（p=2，S=3）

| 步 | 操作 | 模式 | 稀疏集合（升序）/ 秩数组 [r0,r1,r2,r3] |
|---|---|---|---|
| 1 | Add(a) | 稀疏 | {a} |
| 2 | Add(b) | 稀疏 | {a,b} |
| 3 | Add(c) | 稀疏 | {a,b,c} |
| 4 | Add(d) | 稠密 | [2,1,1,1]（d=4==S+1，转稠密） |
| 5 | Add(e) | 稠密 | [2,2,1,1] |
| 6 | Remove(d) | 稠密 | [0,2,1,1] |
| 7 | Remove(e) | 稠密 | [0,1,1,1] |
| 8 | Add(f) | 稠密 | [0,1,2,1] |

(甲) 第 6 步后 [0,2,1,1]、第 7 步后 [0,1,1,1]。朴素 HLL（Remove 不递减、只增不减）第 6、7 步仍 [2,2,1,1]，第 8 步后错成 [2,2,2,1]；Card=16/1.25=12.8，高于正确值 16/2.25≈7.11（偏高）。被删键的高秩永不衰减，删得越多虚高越多，估计只涨不回落——这就是「撤回后估计发散」。

(乙) 误写成 d≥S：第 3 步 Add(c) 使 d=3==S，错误实现此时已转稠密。正确 Card 应为稀疏精确值 3；错误实现用 {a,b,c} 建 HLL 得秩 [0,1,1,1]，Card=16/2.5=6.4。违反不变量 3（须恰在 d==S+1 才转稠密），并连带破坏不变量 1（d≤S 时 Card 必须等于精确不同键数）。

(丙) 寄存器 1 中 e 秩 2、a 秩 1，删 e 后 r1 应回落到 1。若「删最大秩即清零」：第 7 步后错成 [0,0,1,1]，正确应为 [0,1,1,1]；Card=16/3≈5.33，低于正确值 16/2.5=6.4（偏低）。清零把仍存活的 a 的秩贡献一并抹掉，Add/Remove 不再互逆，估计同样发散（这次向下）；只有回落到现存次大秩，撤回才可逆、估计不发散。

## 四条不变量：保证位置与钉住测试

1. 与批量重算一致：est 稠密态按 (j,w) 桶计数（est/est.go 的 convert/Add/Remove），rank 由桶现算（hll/hll.go Ranks）→ TestBatchConsistency
2. 撤回可逆不发散：hll.Remove 桶计数减一、归零删桶，Ranks 取现存最大秩（hll/hll.go）→ TestRevokeReversible
3. 阈值一次性：est.Add 中仅 `len(refs)==S+1` 时 convert，dense 标志粘滞不回落（est/est.go）→ TestThresholdOnce
4. 失败不留痕：est.New/Add/Remove 全部先校验后改状态，拒绝路径零写入（est/est.go）→ TestFaultInjection
