# 裁剪规则推导（第三节）

## 方向：只能从最近端向远端逐跳裁

转发头部的远端部分可以被客户端伪造：最外层代理会把客户端自报的
头部内容原样追加进链路。假设真实链路是 `1.2.3.4 -> 10.0.0.1`（可信
清单 `10.0.0.0/8`），攻击者在请求里自报 `X-Forwarded-For: 9.9.9.9`，
到达本机时头部变成 `9.9.9.9, 1.2.3.4, 10.0.0.1`。若从远端扫描
「第一个不可信」，结果会是伪造的 `9.9.9.9`——伪造者塞一条假记录
就顶掉了真结果。从最近端裁：`10.0.0.1` 可信裁掉，`1.2.3.4` 不可信
即停，客户端是 `1.2.3.4`；伪造的 `9.9.9.9` 位于停点之外，永不生效。

## 缺失跳必须终止裁剪

`for=unknown` / `for=_xxx` 表示该跳刻意隐藏了身份，它不可被判定为
可信。若把缺失跳当可信继续往远端裁，攻击者只要让链路中某一层输出
混淆标识，就能让更远端（更容易伪造）的记录被采信。按「只裁可信跳」
原则，遇到缺失跳必须停在该跳，客户端地址记为缺失（未知）。

## 两种头部同时出现时的合并

合并序列 = 逗号链式的全部跳（较远端）+ 键值式的全部跳（较近端），
各自保持「最左最远端」的内部次序，裁剪仍从合并序列的右端（最近端）
单向进行。理由：任一合并方式只要 (a) 包含两种头部的全部跳、(b) 不
打乱各自内部次序、(c) 从最近端单向裁，就满足三条不变量且不会跳过
只出现在一种头部里的跳；链式（X-Forwarded-For 风格）通常由更外层
设施写入，键值式（RFC 7239 Forwarded）由较内层写入，故链式排远、
键值式排近。
