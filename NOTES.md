# NOTES

## 1. 八步推导（capacity=10；id 在容量检查前自增，故第 3 步拒绝耗掉 id 3，第 4 步为 R4）

| 步 | 操作 | 检查区间峰值 load | 判定 | 步后已激活预留集 |
|---|---|---|---|---|
| 1 | Reserve(0,5,4) | 0 | 接受 | R1:[0,5)4 |
| 2 | Reserve(5,9,7) | 0（x=5 处 R1 已不覆盖，左闭右开） | 接受 | R1:[0,5)4, R2:[5,9)7 |
| 3 | Reserve(2,7,5) | 7（[2,5)=4，[5,7)=7） | 拒绝（7+5=12>10，id3 耗弃） | R1, R2 |
| 4 | Reserve(0,9,1) | 7（峰值在 R2 段） | 接受（7+1=8≤10） | R1, R2, R4:[0,9)1 |
| 5 | Release(1) | — | 释放 | R2:[5,9)7, R4:[0,9)1 |
| 6 | Reserve(2,4,9) | 1（[2,4) 仅 R4 覆盖） | 接受（1+9=10，恰好） | R2, R4, R5:[2,4)9 |
| 7 | Reserve(0,6,2) | 10（[2,4) 上 R4+R5=1+9） | 拒绝（10+2=12>10，id6 耗弃） | R2, R4, R5 |
| 8 | Release(2) | — | 释放 | R4:[0,9)1, R5:[2,4)9 |

- (甲) 闭区间误判：x=5 被同时计入 R1 与新区间，峰值错成 4+7=**11**，误判**拒绝**（正确：峰值 0，接受）。
- (乙) 全量需求简单累加：4+7=11，再加 1 =**12**>10，误判**拒绝**（正确：按重叠求峰值 7，8≤10 接受）。
- (丙) 释放不彻底：R1 仍参与，[2,4) 峰值错成 R1(4)+R4(1)=**5**，加 9=**14**>10，误判**拒绝**（正确：峰值 1，=10 接受）。

## 2. 四条不变量：保证位置与钉住的测试

1. 与朴素参照一致：`rsv/manager.go` 的 Reserve 调 `res/res.go` 的 `Set.Peak`（AVL 剪枝定位重叠区间 + 端点事件扫描），`api/sched.go` 据峰值判定；钉于 `TestReserveMatchesNaiveSweep`。
2. 容量不越界：`api/sched.go` Reserve 中 `peak+need>capacity` 即拒且不入树，接受路径才 Add；钉于 `TestCapacityInvariant`。
3. 释放即失效：`rsv/manager.go` Release 经 byID 定位，`res/res.go` `Set.Remove` 从 AVL 物理删除节点；钉于 `TestReleaseRemovesReservation`。
4. 失败不留痕：容量、need、区间三类校验全部在任何状态变更之前返回哨兵错误，计数器/树/集合均不动；钉于 `TestRejectionLeavesNoTrace`。
