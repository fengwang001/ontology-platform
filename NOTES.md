# base32 推导与不变量

## 填充推导（第三节）
一组 5 字节 = 40 位 = 8 字符。最后一组剩 r 字节时只有 8r 个有效位，
有效字符数 c = ⌈8r/5⌉，其余 8−c 个位置补 `=`：

| 剩余字节 r | 有效位 8r | 有效字符 c | 填充 `=` 个数 |
|---|---|---|---|
| 1 | 8  | 2 | 6 |
| 2 | 16 | 4 | 4 |
| 3 | 24 | 5 | 3 |
| 4 | 32 | 7 | 1 |

合法填充个数集合：**{0, 1, 3, 4, 6}**（0 表示恰好整组、无填充）。
**2 和 5 永不出现**：c=6（即填充 2）需要 26–30 个有效位，c=3（即填充 5）
需要 11–15 个有效位，但 8r（r 为整数）只会落在 8/16/24/32，永远落不进
这两个区间。因此解码遇到填充 2 或 5 个时，输入必不可能是合法编码，
必须报错而非容忍（见 b32.Decode 的 ErrPaddingCount）。

## 四条不变量（第二节）落点
1. 往返恒等：b32.Encode 与 b32.Decode 按 5 字节/8 字符互逆分组
   （b32/b32.go），由 TestRoundTrip 钉住（长度 0–20 逐字节比对）。
2. 编码长度确定：b32.Encode 每组恰写 8 字符、按上表补 `=`
   （b32/b32.go Encode），由 TestRoundTrip 的长度与填充断言钉住。
3. 非法输入可判定：b32.Decode 的三个哨兵错误 ErrInvalidChar /
   ErrPaddingPosition / ErrPaddingCount（b32/b32.go），由 TestErrors 钉住。
4. 失败不留痕：b32.Decode 先整体校验再解码，出错返回 nil 且不改入参
   （b32/b32.go Decode），由 TestErrors 的返回值与入参断言钉住。
