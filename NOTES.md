# 微批触发翻滚窗口计数 — 推导与不变量

## 第三节推导（W=10，B=3，同一 Key=K，到达序 5,12,20,7,22,25,8,31,15,9）

| 批 | 事件 | 批后 wm | 本批发出的 + | 本批迟到 -/+ |
|---|---|---|---|---|
| 1 | 5,12,20 | 20 | +(K,[0,10),1)、+(K,[10,20),1) | 无 |
| 2 | 7,22,25 | 25 | 无 | -(K,[0,10),1)、+(K,[0,10),2) |
| 3 | 8,31,15 | 31 | +(K,[20,30),3) | -(K,[0,10),2)、+(K,[0,10),3)；-(K,[10,20),1)、+(K,[10,20),2) |
| Flush | 9 | +∞ | +(K,[30,40),1) | -(K,[0,10),3)、+(K,[0,10),4) |

最终视图：[0,10)=4、[10,20)=2、[20,30)=3、[30,40)=1，与按 floor(TS/W) 批量重算一致。

- (甲) 第 1 批触发时 [10,20) 计数=1（仅 TS=12；TS=20 在边界，归 [20,30)）。若误作右闭 [10,20]：[10,20) 错成 2，[20,30) 错成 0（少 1，正确应为 1）。
- (乙) TS=8 的 old=2（关窗时 1 + TS=7 贡献 1）。若恒用关窗值 1 当 old：TS=8 错发 -(…,1)/+(…,2)（应 -2/+3），TS=9 错发 -(…,1)/+(…,2)（应 -3/+4），下游 [0,10) 最终错成 2（正确应为 4）。
- (丙) [0,10) 的 + 共 4 条（关窗 1 条 + 迟到 delta 3 条）。若每次迟到都重发完整 + 且下游全累加：1+2+3+4=10（正确最终应为 4）。

## 四条不变量：保证位置与钉住测试

1. 与批量重算一致：`mbatch.trigger` 第 1 步把每个事件（含迟到）计入 `win.Index(TS)` 窗口；`api.View` 重放全部日志。测试 `TestViewMatchesBatchRecompute`。
2. 变更日志自洽：`mbatch.trigger` 第 3 步 `old` 取该 (Key,窗口) 当前计数，按到达序先发 `-old` 再发 `+(old+1)`。测试 `TestChangelogPrefixesConsistent`。
3. 窗口只关一次：`closed[k]` 标记，仅第 2 步对未关闭窗口发 `+`；迟到只走第 3 步 delta。测试 `TestWindowClosesOnce`。
4. 失败不留痕：`api.New` 先校验再建状态；`api.Feed` 整批预检 Key 后才落事件。测试 `TestRejectedOpsLeaveNoTrace`。
