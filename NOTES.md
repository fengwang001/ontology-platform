# 单调队列滑窗最值：推导与不变量

## 推导：新元素等于队尾时，弹还是不弹（序列 [3,3,2]，w=2）
记法：队列项为 (下标:值)；“过期”指下标 < 当前窗口左边界。

做法A：相等也弹队尾（队尾值 <= 新值即弹）
| 步 | 操作 | 过期淘汰 | 入队前弹出 | 队列内容 | 窗口输出 |
| 1 | push(0,3) | - | - | (0:3) | 3 |
| 2 | push(1,3) | - | 弹 (0:3) | (1:3) | 3 |
| 3 | push(2,2) | (1:3) 过期弹出 | - | (2:2) | 2 |

做法B：相等保留（只弹严格更小的队尾）
| 步 | 操作 | 过期淘汰 | 入队前弹出 | 队列内容 | 窗口输出 |
| 1 | push(0,3) | - | - | (0:3) | 3 |
| 2 | push(1,3) | - | - | (0:3)(1:3) | 3 |
| 3 | push(2,2) | (0:3) 过期弹出 | - | (1:3)(2:2) | 3 |

结论：两种做法 Max() 数值都正确，但做法A在第2步把仍在窗口内、且第3步
窗口 [1,2] 里还要充当最大值的候选 (0:3) 提前丢掉，违反不变量3“候选不早退”：
窗口内候选信息丢失，“队首必属于当前窗口”的推理（含自检）被架空。
做法B保留相等者，(1:3) 自然留到第3步成为队首，四条不变量全部成立。
选择：相等不弹，只弹严格更小的队尾。

## 不变量：保证位置 / 钉住它的测试
1. 结果正确：slide.Maxes 与朴素双循环一致 — slide.Maxes；TestMaxesAgainstNaive
2. 队列单调非严格递减、队首即窗口最大 — mono.Push 弹尾循环；TestMonotonicQueue
3. 候选不早退、过期必淘汰 — mono.Push 只弹更小者 + slide.Feed 按左边界淘汰；TestMonotonicQueue
4. 失败不留痕 — mono.Push/slide.New/Maxes 先校验后改状态；TestRejectedOpsNoSideEffect
自检：slide.SelfCheck 核验单调性、队首在窗内、队长 <= w；TestSelfCheck。
