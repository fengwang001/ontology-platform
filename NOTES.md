# 第三节推导：A=[0,3]×[0,3]，B=[2,5]×[2,5]（均逆时针）

## 十行分步表

| # | 对象 | 判定过程 | 结论 |
|---|------|----------|------|
| 1 | A 顶点 (0,0) 对 B | 射线法：不在 B 内 | 保留 |
| 2 | A 顶点 (3,0) 对 B | y=0<2，不在 B 内 | 保留 |
| 3 | A 顶点 (3,3) 对 B | 2<3<5 且 2<3<5，在 B 严格内部 | 丢弃 |
| 4 | A 顶点 (0,3) 对 B | x=0<2，不在 B 内 | 保留 |
| 5 | B 顶点 (2,2) 对 A | 0<2<3 且 0<2<3，在 A 严格内部 | 丢弃 |
| 6 | B 顶点 (5,2) 对 A | x=5>3，不在 A 内 | 保留 |
| 7 | B 顶点 (5,5) 对 A | 不在 A 内 | 保留 |
| 8 | B 顶点 (2,5) 对 A | y=5>3，不在 A 内 | 保留 |
| 9 | 交点 (3,2) | A 边 (3,0)-(3,3)（x=3）× B 边 (2,2)-(5,2)（y=2） | 新增 |
| 10 | 交点 (2,3) | A 边 (3,3)-(0,3)（y=3）× B 边 (2,5)-(2,2)（x=2） | 新增 |

(甲) 正确并集（逆时针 8 顶点）：(0,0)(3,0)(3,2)(5,2)(5,5)(2,5)(2,3)(0,3)；误做凸包则错成六顶点 (0,0)(3,0)(5,2)(5,5)(2,5)(0,3)，凹口被填平。
(乙) 漏算边交点则错成六顶点 (0,0)(3,0)(5,2)(5,5)(2,5)(0,3)，边 (3,0)-(5,2)、(2,5)-(0,3) 斜穿凹口、凹口错位；缺交点 (3,2)、(2,3)。
(丙) 符号写反：A 的 (3,3)（本在 B 内应丢弃）被错误保留；B 的 (2,2)（本在 A 内应丢弃）被错误保留。

## 四条不变量的落点

1. 面积守恒：`un.Union` 只保留「非严格在对方内部」的边界子边；对照交面积由 `api.rectInter`（独立实现）算出；测试 `TestInvariantArea`。
2. 结果自洽：`api.validate`（无自交/无相邻重复/逆时针/面积>0）由 `api.SelfCheck` 复用；测试 `TestInvariantShape`；顶点归属由 `api.pointsOK` 前半核验，`TestInvariantCoverage` 钉住。
3. 覆盖正确：`un.keptSegs` 的子边中点判定保证不增不减区域；采样点双向比对在 `api.pointsOK` 后半，`TestInvariantCoverage` 钉住。
4. 失败不留痕：`api.NewPolygon` 先 `validate` 通过才构造，拒绝时返回 nil 与哨兵错误；测试 `TestInvariantAtomicFailure`。
