# ontology-261 NOTES：第三节推导（n=3, maxBuf=8；缓冲集合按 From 升序列）

| 步 | 到达 | 本步判定 | 本步交付（先后） | V_local | 缓冲 | dup |
|---|---|---|---|---|---|---|
| 1 | e (1,[2,2,0]) | 缓冲：V[1]=2≠0+1 | 无 | [0,0,0] | {e} | 0 |
| 2 | d (2,[1,1,1]) | 缓冲：V[0]=1>local[0]=0 | 无 | [0,0,0] | {d,e} | 0 |
| 3 | b (1,[1,1,0]) | 缓冲：V[0]=1>0 | 无 | [0,0,0] | {b,d,e} | 0 |
| 4 | c (0,[2,0,0]) | 缓冲：V[0]=2≠0+1 | 无 | [0,0,0] | {b,c,d,e} | 0 |
| 5 | f (2,[2,2,2]) | 缓冲：V[2]=2≠0+1 | 无 | [0,0,0] | {b,c,d,e,f} | 0 |
| 6 | a (0,[1,0,0]) | 可交付→级联至不动点 | a,c,b,e,d,f | [2,2,2] | ∅ | 0 |
| 7 | b 再次 (1,[1,1,0]) | 重复：V[1]=1≤local[1]=2，丢弃 | 无 | [2,2,2] | ∅ | 1 |

第6步级联细节：交 a→[1,0,0]；可交付候选 c(From0)、b(From1)，取最小 From 交 c→[2,0,0]；交 b→[2,1,0]；候选 e(From1)、d(From2)，交 e→[2,2,0]；交 d→[2,2,1]；交 f→[2,2,2]。

**(甲)** 第 **2** 步首次错误交付：d 的发送方分量 1==0+1 即被交，V_local=[0,0,1]；其因果前驱 a(0,[1,0,0])、b(1,[1,1,0]) 均未交付（d.V[0]=1、d.V[1]=1 无对应已交消息）。
**(乙)** 不级联：第 6 步只交 a，V_local=[1,0,0]，缓冲 {b,c,d,e,f} 共 5 条；比正确实现少交 c、b、e、d、f 五条（全部滞留缓冲等不到触发）。
**(丙)** 正确处理：判重丢弃、dup=1、其他状态不变。若不判重：1≠local[1]+1=3 不可交付→落入缓冲且永不可交付；7 步后缓冲剩 **1** 条、b 交付 0 次，违反**不变量 3**（完备性：闭合集合到齐缓冲须空、每条恰好交付一次），并使**不变量 1**（与朴素参照不一致）连带失效。

## 不变量保证位置与钉住测试

1. **与朴素参照一致**：`cbuf.Receive` 级联每轮只按 `buf[j][local[j]+1]` 索引候选（每发送方至多一条），j 升序首中即交付，等价于整表扫描取最小 From，不动点相同 → `TestNaiveAgreement` 钉住。
2. **因果序/同源连续**：交付唯一入口是谓词 `vc.Deliverable`（m.V[j]==local[j]+1 且其余分量 ≤local），同源序号因此严格 +1 → `TestSevenStepScenario` 与 `TestCompletenessAnyOrder` 的因果序断言钉住。
3. **完备**：`cbuf.Receive` 交付后级联到无候选为止，重复由 local 与缓冲索引双重判定 → `TestCompletenessAnyOrder`、`TestConcurrentReceive` 钉住。
4. **失败不留痕**：`cbuf.Receive` 先 `vc.Validate` 后做任何写入；需缓冲时先查容量再插入，满则返回 `cbuf.ErrFull`，dup 路径只加计数 → `TestRejectLeavesNoTrace` 钉住。
