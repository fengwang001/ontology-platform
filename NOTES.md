# 迟到纠正重处理：推导与不变量

## 八步推导（W=3，同一 key；窗口 = 首次到达序 FIFO，容量 3）

| # | 事件(Seq,Val) | 类别 | sum | 窗口(首次到达序) | 产出 |
|---|---------------|------|-----|------------------|------|
| 1 | (5,10) | 新 | 10 | [5] | +(k,10) |
| 2 | (7,20) | 新 | 30 | [5,7] | -(k,10) +(k,30) |
| 3 | (6,15) | 迟到新 | 45 | [5,7,6] | -(k,30) +(k,45) |
| 4 | (8,5) | 新 | 50 | [7,6,8]（5 被淘汰） | -(k,45) +(k,50) |
| 5 | (5,99) | 过期（拒绝） | 50 | [7,6,8] | 无 |
| 6 | (6,15) | 重复（no-op） | 50 | [7,6,8] | 无 |
| 7 | (6,30) | 纠正 | 65 | [7,6,8]（顺序不变） | -(k,50) +(k,65) |
| 8 | (9,3) | 新 | 68 | [6,8,9]（7 被淘汰） | -(k,65) +(k,68) |

终态校验：{5:10, 6:30, 7:20, 8:5, 9:3} 求和 = 68，与增量一致。

- **(甲)** 第 5 步是**过期**：Seq 5 已见且改值，但它在第 4 步已被挤出窗口，拒绝，sum 保持 50、无产出、过期数+1。若不设窗口任意可纠正：sum = 50 + (99-10) = **139**（错）。
- **(乙)** 第 6 步是**重复**：Seq 6 已见且值相同，幂等 no-op，sum 保持 50、无产出。若不去重当新事件再累加：sum = 50 + 15 = **65**（错）。
- **(丙)** 第 3 步是**迟到**（新 Seq），照常接受，sum = 45。若把一切乱序都拒绝：sum = **30**（错，丢了 15）。根本区别：迟到是「从未见过的 Seq 带来的新数据」，加法可交换故必须收；过期纠正是「已见 Seq 的改值」且该 Seq 已出纠正窗口，改值会破坏已产出结果的稳定性，必须拒。

## 四条不变量：保证位置与钉住测试

1. **与批量重算一致**：`agg/agg.go` 用 `map[seq]val` 持集合、增量维护 sum，`api.View` 直读日志视图；测试 `TestBatchEquivalence`（多档规模+随机顺序对拍朴素模型）。
2. **变更日志自洽**：`chlog/chlog.go` `Log.Append` 撤回时校验被撤值恰等于该 key 当前值，否则报错；测试 `TestChangelogPrefix`（重放每个前缀）。
3. **幂等**：`agg/agg.go` `Apply` 对「已见 Seq 且同值」直接返回，不动任何字段；测试 `TestIdempotent`。
4. **失败不留痕**：`api/api.go` `Apply`/`New` 先做全部校验、通过前不触碰任何状态；测试 `TestRejectNoTrace`。
