# NOTES — 差分约束求解器

## 三、分步松弛推导（n=3；C1: x1−x0≤−2 即边 0→1 权−2；C2: x2−x1≤−3 即边 1→2 权−3）

超级源 s 的边 s→0/1/2 权 0 松弛后，初始距离 (x0,x1,x2)=(0,0,0)。每轮**先松 C2 再松 C1**：

| 轮 | 松 C2(1→2,−3) 后 | 松 C1(0→1,−2) 后 |
|---|---|---|
| 1 | (0, 0, −3) | (0, −2, −3) |
| 2 | (0, −2, −5) | (0, −2, −5) 不变 |
| 3 | 不变，收敛 | (0, −2, −5) |

收敛解 (0, −2, −5)，即正确赋值。

- **(甲)** 只跑一轮全量松弛：x2=**−3**（正确 −5）。C2 先被处理时 x1 还是 0，x2 只能取 0+(−3)=−3；x1 在同一轮稍后才被 C1 压到 −2，x2 已没机会用新的 x1 重新松弛。
- **(乙)** C2 的权 −3 误取反成 +3：轮1 x2=min(0, 0+3)=0；之后 x2=min(0, −2+3)=0，收敛 x2=**0**（正确 −5）。
- **(丙)** 再登记 C3: x0−x2≤−10（边 2→0 权−10）：环 0→1→2→0 权和 −2−3−10=−15<0，负环 → 系统不可行，正确的 Solve 返回哨兵错误 ErrInfeasible、不返回任何赋值。若实现不检测负环、固定跑 3 轮（每轮按 C2→C1→C3 顺序）：轮1 (−13,−2,−3)、轮2 (−15,−15,−5)、轮3 (−28,−17,−18)，返回「看似可行」的 (−28,−17,−18)；它违反 C1：x1−x0=−17−(−28)=11 ≰ −2（也违反 C2：x2−x1=−18−(−17)=−1 ≰ −3）。

## 二、四条不变量：保证位置 + 钉住测试

1. **可行**：`sp.Engine.Solve` 队列驱动松弛至收敛，收敛时所有边满足 d(v)≤d(u)+w（`sp/sp.go`）；测试 `api.TestSolveTable`、`api.TestMatchesNaive` 逐约束核验。
2. **点态最大**：x_i=d(s,i) 是最短路距离，任何可行 y 有 y_i≤d(s,i)（`sp/sp.go` `NewEngine` 的超级源边）；测试 `api.TestPointwiseMaximal`（紧性刻画：x_i≤0 且每个 x_i<0 都被某条紧约束钉住）。
3. **与朴素参照一致**：`api.Solve` 结果等于逐轮全量松弛的朴素 Bellman-Ford（`api/api.go` 内 `naiveSolve` 参照）；测试 `api.TestMatchesNaive` 逐元素比对。
4. **失败不留痕**：`dc.System.Add` 先校验后追加、负环时 `sp` 只读快照求解不改登记集（`dc/dc.go`、`api/api.go`）；测试 `api.TestRejectedNoTrace`。

另：长链下全量松弛趟数恒 0 由 `sp.TestZeroFullPassesOnChain`（白盒读非导出计数器）钉住；并发一致由 `api.TestConcurrentSolveIdentical` 钉住。
