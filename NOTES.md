# 变长字段长度前缀编解码 — 推导与不变量

## 第三节推导：四字段记录（小端 4 字节长度前缀 + 负载）

| 字段 | 负载 | 长度 | 长度前缀(hex) | 完整字节(hex) |
|---|---|---|---|---|
| 1 | "hi" | 2 | 02 00 00 00 | 02 00 00 00 68 69 |
| 2 | ""（空） | 0 | 00 00 00 00 | 00 00 00 00 |
| 3 | "world!" | 6 | 06 00 00 00 | 06 00 00 00 77 6f 72 6c 64 21 |
| 4 | "A" | 1 | 01 00 00 00 | 01 00 00 00 41 |

整条记录（26 字节）：`02 00 00 00 68 69 00 00 00 00 06 00 00 00 77 6f 72 6c 64 21 01 00 00 00 41`

- (甲) 空字段 = 4 字节前缀（值 0）+ 0 字节负载，即 `00 00 00 00`。若把长度 0 当非法报错，编码直接失败；若解码漏掉空字段，字段数 4→3，且后续字段逐一错位，与正确结果完全不同。
- (乙) 前缀声明 100、剩余仅 3 字节：正确实现整体失败，返回 `(nil, frame.ErrTruncated)`，不交付任何部分字段；naive 不检查越界会切片越界 panic（或读到缓冲外垃圾）。
- (丙) 第 2 字段应从偏移 6（=0+4+2）开始读。若错算成「字段起点+负载长」=0+2=2，从偏移 2 读到 4 字节 `00 00 68 69`，得长度 0x69680000=1768554496，远超剩余 20 字节，报截断错（ErrTruncated）；正确偏移 6 处读出 `00 00 00 00`，长度 0，即空字段。

## 四条不变量：保证位置与钉住测试

1. 往返一致：`frame.Encode` 逐字段写「前缀+负载」、`frame.Decode` 按「读长度→偏移+4+L」切分（空字段长度 0 照常成字段）；测试 `TestRoundTrip`。
2. 前缀自洽：`Decode`/`Reader.NextField` 每字段偏移严格 `+4+L`，天然无重叠无空洞、覆盖全缓冲；测试 `TestPrefixSelfConsistent`。
3. 与朴素参照一致：测试内 `naiveEncode` 手写 4 字节小端+负载，与 `Encode` 字节级比对；测试 `TestMatchesNaive`。
4. 失败不留痕：`Decode` 出错即 `return nil, err`；`Reader.SkipField` 校验通过才推进 `pos`；测试 `TestFailureAtomic`。
