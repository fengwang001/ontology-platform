# Hamming(7,4) 推导笔记

## 三、nibble 1011（值 11，d1d2d3d4=1011）

p1=d1⊕d2⊕d4=1⊕0⊕1=0，p2=d1⊕d3⊕d4=1⊕1⊕1=1，p3=d2⊕d3⊕d4=0⊕1⊕1=0
码字 p1p2d1p3d2d3d4 = **0110011**

七行表（每次只翻转第 i 位，syndrome=s1*1+s2*2+s3*4）：

| 翻转位 | 翻转后码字 | s1s2s3 | syndrome | 纠错后 d1d2d3d4 |
|---|---|---|---|---|
| 1 (p1) | 1110011 | 100 | 1 | 1011 |
| 2 (p2) | 0010011 | 010 | 2 | 1011 |
| 3 (d1) | 0100011 | 110 | 3 | 1011 |
| 4 (p3) | 0111011 | 001 | 4 | 1011 |
| 5 (d2) | 0110111 | 101 | 5 | 1011 |
| 6 (d3) | 0110001 | 011 | 6 | 1011 |
| 7 (d4) | 0110010 | 111 | 7 | 1011 |

- (甲) 奇校验 p1p2p3=NOT(0,1,0)=(1,0,1) → 错码字 **1011011**（正确 0110011）。
- (乙) 映射写反 syndrome=s1*4+s2*2+s3：翻 d1（位置3）得伴随式 (1,1,0)，被错算成 4+2+0=**6** → 去翻位置 6（d3:1→0），码字成 0100001，数据错成 **0001**（正确 1011）。
- (丙) 单 nibble 打包：0110011 低位补 0 → 01100110 = **0x66**；`Encode([])` 返回**空切片**（长 0，无错误）。双错：0110011 翻 p1、p2 → 1010011，伴随式 (s1,s2,s3)=(1,1,0)，syndrome=3 → 错翻位置 3（d1:1→0）→ 数据错成 **0011**。最小距离 3：任何双错都落在另一合法码字的距离 1 邻域内，伴随式必给出某个单错的解释，故双错必然被"纠正"成错数据，无法检出。

## 二、四条不变量的落实位置与钉住测试

1. 与朴素参照一致：`hamm.EncodeNibble`/`DecodeNibble` 按定义逐位实现，`hpack.Pack` 大端 7 位步进；测试 `TestAgainstNaive`。
2. 切分点无关：`hpack.Feeder` 只追加字节、不解释边界，最终对同一字节流解码；测试 `TestChunkedFeed`。
3. 确定性/单错可纠：全部为无状态纯函数；测试 `TestDeterministic`、`TestSingleErrorCorrection`（7 位循环全覆盖）。
4. 失败不留痕：`api.Decode` 先整体校验再解码，出错返回 `nil`，包内无可变状态；测试 `TestErrorPaths`。
