# NOTES：撤回流上的增量 Top-N（N=3，maxRows=16）

排序：Score 降序；同分 Key 按字节序升序。变更日志 − 先 + 后。

| 步 | 输入 | 变更日志 | 本步后 Top-N | 榜外存活行 |
|---|---|---|---|---|
| 1 | +(a,50) | +(a,50) | [a50] | — |
| 2 | +(b,70) | +(b,70) | [b70,a50] | — |
| 3 | +(c,50) | +(c,50) | [b70,a50,c50] | — |
| 4 | +(d,60) | −(c,50) +(d,60) | [b70,d60,a50] | [c50] |
| 5 | +(e,50) | 无 | [b70,d60,a50] | [c50,e50] |
| 6 | −(b,70) | −(b,70) +(c,50) | [d60,a50,c50] | [e50] |
| 7 | −(e,50) | 无 | [d60,a50,c50] | — |
| 8 | +(f,55) | −(c,50) +(f,55) | [d60,f55,a50] | [c50] |
| 9 | −(d,60) | −(d,60) +(c,50) | [f55,a50,c50] | — |
| 10 | +(g,45) | 无 | [f55,a50,c50] | [g45] |

(甲) 第 6 步由 c50 补位：榜外 c50 与 e50 同分，Key 升序 c<e，故 c 排前。只留 N 个候选时 c 在**第 4 步**被挤出即丢弃（e 第 5 步也未留存），第 6 步只能输出 −(b,70)，Top-N 变成 [d60,a50]（只有两行，无补位）。
(乙) 同分改 Key 降序：**第 4 步**首次不同，错成 −(a,50) +(d,60)（正确为 −(c,50) +(d,60)：降序下 50 分次序为 e>c>a，第 4 步被挤的是 a 而非 c，第 3 步日志同为 +(c,50)）；第 5 步输出 −(c,50) +(e,50)；第 7 步输出 −(e,50) +(a,50)。
(丙) 撤回不补位：第 6 步仅 −(b,70)（持 2 行），第 9 步仅 −(d,60)（持 2 行），第 10 步 +(g,45) 填坑；第 10 步后下游持有 [f55,a50,g45]，批量正确为 [f55,a50,c50]，违反**不变量 1**（且第 6、9 步行数 2≠min(3,存活数)=3，亦违反不变量 2）。

不变量保证位置与钉住的测试：

1. 与批量重算一致：`topn/topn.go` 中 insert/retract 只改有序 `rank.Index`，`Top()` 切前 min(N,len)，`diff` 由前后集合求变更；由 `TestTenStep`、`TestBatchRecompute` 钉住。
2. 变更日志自洽：`topn.go` 先快照旧 Top 再变异、`diff` 保证 − 先 + 后且每条输入至多各一条，`Apply` 失败时 `restore` 截断 log；由 `TestChangelogPrefix`（逐前缀回放）钉住。
3. 严格有序无重复 Key：`rank/rank.go` 的 `Before`（分降/Key 升）与 `seek` 二分不变式；由 `rank.TestOrdering` 与 `TestBatchRecompute`（逐行比对有序模型）钉住。
4. 失败不留痕：`api/api.go` 的 `New` 拒非法参数；`topn.go` 的 `Apply` 保存 live 与 log 长度快照，出错整体 `restore` 重建；由 `TestSentinelErrors`、`TestAtomicBatch` 钉住。
