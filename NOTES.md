# 差分约束求解器 NOTES

## 一、第三节推导

约束 C1: x1-x0<=-2（边 0->1 权 -2），C2: x2-x1<=-3（边 1->2 权 -3）；超级源 s->0/1/2 权 0，故初始距离全 0。每轮先松 C2 再松 C1：

| 轮次 | 松弛后事件 | x0 | x1 | x2 |
|---|---|---|---|---|
| 初始 | 源边生效 | 0 | 0 | 0 |
| 1 | C2: x2=0-3=-3；C1: x1=0-2=-2 | 0 | -2 | -3 |
| 2 | C2: x2=-2-3=-5；C1: 不变 | 0 | -2 | -5 |
| 3 | 无变化，收敛 | 0 | -2 | -5 |

**(甲)** 只跑一轮：x2=-3（正确 -5）。先处理 C2 时 x1 还是 0，x2 只被压到 -3；随后 C1 才把 x1 压到 -2，但本轮不会再回头重松 C2，x2 停留在一个偏大的不可行值。
**(乙)** C2 权取反成 +3：x2=min(0, x1+3)=min(0,-2+3)=0（正确 -5）。上界被放宽，最短路不再沿 1->2 传播。
**(丙)** 加 C3: x0-x2<=-10（边 2->0 权 -10）：环 0->1->2->0 权之和 -2-3-10=-15<0，负环，系统不可行，Solve 返回哨兵错误 ErrInfeasible、不返回赋值。若不检测负环、固定跑 3 轮（每轮序 C2,C1,C3）就返回：轮1 [-13,-2,-3]，轮2 [-15,-15,-5]，轮3 [-28,-17,-18]，返回 [-28,-17,-18]；它违反 C1：x1-x0=-17-(-28)=11 > -2，也违反 C2：x2-x1=-18-(-17)=-1 > -3（仅 C3 成立：-10<=-10）。

## 二、四条不变量

1. **可行**：sp.Solve 队列驱动松弛至不动点，返回的 dist 满足全部边；`api.SelfCheck` 内置序列逐约束核验，测试 `TestSolveTable`/`TestRandomConsistent` 钉住。
2. **点态最大**：解=超级源最短路，任何可行 y 满足 y_i<=dist_i（最短路归纳）；测试 `TestPointwiseMax`（加参考变量收紧任一变量 1 即不可行）钉住。
3. **与朴素参照一致**：`api.naiveBF` 逐轮全量松弛作参照，Solve 结果逐变量相等；`TestSolveTable`/`TestRandomConsistent` 比对。
4. **失败不留痕**：dc.Add 先校验后追加、sp 不碰注册表、负环时 Solve 返回 (nil, ErrInfeasible)；`TestErrorsStateIntact`（含 SelfCheck 调用）钉住。
