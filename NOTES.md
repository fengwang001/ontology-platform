# Indexed Binary Heap Notes

## Decrease-key 为什么只向上调整

设最小堆满足 `parent[i] <= child[i]`。
将位置 `i` 的键从 `old` 改为 `new < old` 后，它与两个孩子的关系只会更安全：
孩子原来就不小于 `old`，所以也一定不小于 `new`。

被破坏的只可能是它与父亲的关系：父节点仍可能满足
`new < parent <= old`。因此只需把该节点与较大的父亲持续交换（sift-up）；
每向上一步，原来较小的父亲下沉到一个原本由更小新键占据的位置，
而该位置两个孩子也都不会破坏最小堆关系。

若 decrease 后做 sift-down，可能把已经变小的节点和较大孩子交换：
大孩子被搬到父亲位置，反而大于新键所在的孩子位置，破坏最小堆。

反例堆序列为 `[3,5,7,9,11,13]`。把键 `9` 降到 `1` 后：
- 正确 sift-up 得到 `[1,3,7,5,11,13]`，Pop 序列为 `[1,3,5,7,11,13]`。
- 错误 sift-down：`1` 与孩子中较小者 `5` 交换，得到 `[3,1,7,5,11,13]`；
  根节点 `3 > 1`，Pop 首个元素错误地变成 `3`。

## 语义与测试索引

（测试完成后回填代码位置与测试函数名）
