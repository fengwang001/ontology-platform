# 推导与不变量说明

哈希（p=2, m=4）：x=字节和，j=x%4，w=(x/4)%4+1 → a(1,1) b(2,1) c(3,1) d(0,2) e(1,2) f(2,2)

## 八步分步表（S=3）

| 步 | 操作 | 模式 | 稀疏集合 / 秩数组 |
|---|---|---|---|
| 1 | Add(a) | 稀疏 | {a} |
| 2 | Add(b) | 稀疏 | {a,b} |
| 3 | Add(c) | 稀疏 | {a,b,c}（d=3≤S，不转） |
| 4 | Add(d) | 稠密 | [2,1,1,1]（d=4=S+1，转稠密） |
| 5 | Add(e) | 稠密 | [2,2,1,1] |
| 6 | Remove(d) | 稠密 | [0,2,1,1]（寄存器0空，r0→0） |
| 7 | Remove(e) | 稠密 | [0,1,1,1]（r1 回落次大秩 1） |
| 8 | Add(f) | 稠密 | [0,1,2,1] |

(甲) 朴素 HLL（Remove 不递减）：第 6、7 步后仍 [2,2,1,1]，第 8 步后错成 **[2,2,2,1]**（正确 [0,1,2,1]）。Card 错误值=16/(¼+¼+¼+½)=12.8，正确值=16/(1+½+¼+½)≈7.11，**偏高**。删键后秩不回落，估计只增不减、与真实基数脱节，即「撤回后估计发散」。

(乙) 阈值误写为 d≥S：第 3 步 Add(c) 使 d=3==S，错误实现此时已处**稠密**。此时 Card 正确值=3（稀疏精确），错误实现给 HLL 估计 16/(1+½+½+½)=**6.4**。违反不变量 3（恰在 d=S+1 转一次）与不变量 1（稀疏期 Card 须等于精确不同键数）。

(丙) 第 7 步删 e（秩2）后 r1 应回落到 **1**（a 的秩）。「删最大秩即清零」的实现第 7 步后错成 **[0,0,1,1]**，正确为 [0,1,1,1]。清零丢掉了同寄存器内 a 的贡献，使估计偏离「批量重算」结果；只有回落到现存次大秩，Ranks 才始终等于对当前集合重算的结果，撤回才不发散。

## 四条不变量的保证位置与钉住测试

1. 与批量重算一致：`hll` 按寄存器「秩→计数」多集增减、`Ranks()` 现取现存最大秩；`est` 转稠密时用全部不同键重建 sketch — `TestBatchConsistency`
2. 撤回可逆：桶计数归零即删、`Ranks()` 自然回落次大秩 — `TestRevokeReversible`
3. 阈值一次性：`est.Add` 仅在非稠密且 `len(refs)==S+1` 时转换，`dense` 标志只置不消 — `TestThresholdOnce`
4. 失败不留痕：所有参数校验先于任何变更，`hll.Remove` 先查桶后改 — `TestFailureAtomic`
