# NOTES — 平滑加权轮询

## 推导：权重 [3,1,2]，W=6，初始 cw=(0,0,0)，连续 6 次 Next

| # | 加权后 (cw0,cw1,cw2) | 选中 | 减 W 后 (cw0,cw1,cw2) |
|---|---|---|---|
| 1 | (3,1,2)  | 0 | (-3,1,2) |
| 2 | (0,2,4)  | 2 | (0,2,-2) |
| 3 | (3,3,0)  | 0 | (-3,3,0) |
| 4 | (0,4,2)  | 1 | (0,-2,2) |
| 5 | (3,-1,4) | 2 | (3,-1,-2) |
| 6 | (6,0,0)  | 0 | (0,0,0)  |

- (甲) 第 2 次平滑算法选 **2**；朴素展开法序列 `0,0,0,1,2,2` 第 2 次选 **0**。平滑把高权重的命中摊开了。
- (乙) 第 3 次 cw=(3,3,0)，0 与 1 并列最大，按「并列取下标最小」应选 **0**；若错取「下标最大」会选 **1**。
- (丙) 若每次减的是自身权重 w_i 而非 W：前 3 步为 (0,1,2)→(3,2,2)→(3,3,4)，第 4 步加权后 (6,4,6)，会错选 **2**（正确应为 1）；服务器 1（权重最小）的 cw 持续累积却长期轮不到，被**饿死**（推演 12 步内 cw1 涨到 12 仍未被选中）。

## 四条不变量：保证位置与钉住它的测试

1. **公平性**：`wrr.Next` 严格按「加 w_i → 取最大（并列取下标小）→ 减 W」执行，每 W 步回到 cw 全零、各选 w_i 次；测试 `TestFairness`、`TestSmoothSequence312`。
2. **Σcw_i == 0**：`wrr.Next` 每步加总 W 再减 W，`SetWeight` 把 cw 全重置为 0；测试 `TestSumCWInvariant`。
3. **确定性**：堆比较器 `wrr.idxHeap.Less` 在 cw 相等时按下标小者优先，无随机源；测试 `TestDeterministic`。
4. **失败不留痕**：`wrr.New`、`wrr.SetWeight` 先完成全部校验再写任何字段；测试 `TestFailureNoSideEffect`。

另：定位最大者的检查数上界由 `TestLocateChecksBounded` 钉住；并发公平与 Σcw 恒零由 `TestConcurrentFairness` 钉住。
