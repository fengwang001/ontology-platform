# 有理数规范化四则运算 — 推导与不变量

## 第三节：七步推导（每步之后的规范形式 n/d）

| 步 | 运算 | 规范形式 |
|---|---|---|
| 1 | `v = New(6, -8)` | `-3/4`（d<0 → 符号归一得 -6/8，gcd(6,8)=2 约分） |
| 2 | `v = Add(v, 5/6)` | `1/12`（lcm(4,6)=12：-9/12 + 10/12） |
| 3 | `v = Mul(v, -4/1)` | `-1/3`（-4/12 约分） |
| 4 | `v = Sub(v, 1/6)` | `-1/2`（lcm(3,6)=6：-2/6 - 1/6 = -3/6 约分） |
| 5 | `v = Div(v, -3/1)` | `1/6`（(-1/2)×(1/-3) = -1/-6，符号归一） |
| 6 | `v = Add(v, 1/6)` | `1/3`（2/6 约分） |
| 7 | `v = Mul(v, 0/5)` | `0/1`（0 的规范形式恒为 0/1） |

**(甲)** 带符号求 gcd：`gcd(6,-8)` 按欧几里得得 **-2**（6%-8=6，-8%6=-2，6%-2=0）。若先做符号归一（-6/8）再除以 g=-2，得 **3/-4**——分母为负的非法形式。正确规范形式是 `-3/4`。违反第二节**不变量 2**（分母恒为正、规范形式唯一）。

**(乙)** 第 2 步交叉相乘不约分：n = 3*6 + 5*4 = 38，d = 4*6 = 24，得非既约形式 **38/24**（gcd(38,24)=2；且把 -3/4 符号外提成 3/4，连值都错了）。正确规范形式是 `1/12`。

**(丙)** `2^62 + 2^62`：分子正确应为 **2^63 = 9223372036854775808**（超出 int64 最大值 2^63-1）。朴素 int64 相加静默回绕成 **-9223372036854775808**。正确实现检出溢出并报可判定错误 `ErrAddOverflow`。

## 第二节：四条不变量的保证位置与钉住测试

1. **与朴素参照一致**：`rat.Add/Sub/Mul/Div` 全部经 `num.Normalize` 约分归一；测试 `TestAgainstBigReference`（与 math/big.Rat 逐条对拍）。
2. **规范形式唯一**：`num.Normalize`（gcd 对绝对值取、d 恒正、0→0/1）；测试 `TestCanonicalForm`。
3. **加法交换律且通分精确**：`rat.Add` 用 lcm 通分 + `num.Add64` 溢出检出；测试 `TestAddCommutative`。
4. **失败不留痕**：`Rat` 不可变，运算先整体算完再构造，失败返回 nil+哨兵错误；测试 `TestFailureNoSideEffect`。
