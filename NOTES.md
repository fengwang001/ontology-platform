# Indexed Heap Notes

## decrease-key 后为何只能 sift-up

堆数组 [3,5,7,9,11,13]（下标 1 起，父 i 的孩子为 2i、2i+1）。
将值 9 的元素降到 1，得到 [3,5,7,1,11,13]。

降键前不变量成立：父键 <= 子键，所以该元素的两个孩子（若存在）
键本就 >= 9，必然仍 >= 新键 1，孩子一侧不会被破坏。
唯一可能被破坏的关系是它与父节点：父键 3 > 新键 1。
因此只需沿父链 sift-up，与父节点比较、交换，直到父键 <= 当前键。

sift-up 后：[1,5,3,9,11,13]（1 与 3 交换），Pop 序列正确。

错做 sift-down 的反例：堆 [1,4,3,9,5]，把值 9（叶节点）降到 0，
得 [1,4,3,0,5]。正确做法是 sift-up：0 先与父 4 换、再与 1 换，
成 [0,1,3,4,5]。若只做 sift-down，叶节点没有孩子，一步都不动，
与父节点 4 的违例（4 > 0）永久残留，Peek 错误返回 1 而非 0。
结论：降键只制造「子 < 父」型违例，只能向上修；sift-down 修的是
「父 > 子」，方向相反，无法消除该违例。

## 语义与测试位置

<!-- 回填：第二节各条的代码位置与测试函数名 -->
