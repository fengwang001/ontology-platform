# 传递闭包：逐 k 推导 + 不变量落点

## 第三节推导（n=5，边 0→1, 1→2, 2→0, 2→3, 3→4）

初始（仅直接边，对角线全 false）：0:{1} 1:{2} 2:{0,3} 3:{4} 4:{}

| k | 以 k 为中间节点后新增可达的 (i,j) |
|---|---|
| 0 | (2,1) |
| 1 | (0,2), (2,2) |
| 2 | (0,0), (0,3), (1,0), (1,1), (1,3) |
| 3 | (0,4), (1,4), (2,4) |
| 4 | 无 |

最终闭包矩阵（行 0..4，1=可达）：`0:11111  1:11111  2:11111  3:00001  4:00000`

- (甲) 自反过度（空路径也算可达）：`Reach(4,4)` 错判 true；正确为 false（4 不在任何环上）。
- (乙) 跳数 ≤2（不做完所有中间节点）：`Reach(0,4)` 错判 false；正确为 true（0→1→2→3→4 需 4 跳）。
- (丙) 跳过对角线更新：`Reach(2,2)` 错判 false；正确为 true（2→0→1→2 成环）。

## 四条不变量：代码位置 + 钉住它的测试

1. 定义正确（≥1 条边才可达）：`tc.Compute` 对角线初始 false、仅靠 k 传递合并置位，`tc/tc.go`；测试 `TestDefinitionEdgeCases`。
2. 与朴素参照一致：`tc.Compute` 的布尔 Floyd 与测试内逐点 DFS 参照逐 (i,j) 比对，`tc/tc.go`；测试 `TestClosureMatchesNaive`。
3. 传递性：k 循环跑满 0..n-1 保证闭包传递，`tc/tc.go`；测试 `TestTransitivity`。
4. 失败不留痕：`dg.AddEdge`/`dg.New` 先完成全部校验再写邻接，任一校验失败直接返回哨兵错误，`dg/dg.go`；测试 `TestErrorsLeaveNoTrace`。
