# NULL 分组语义下的增量计数视图 — 推导笔记

## 一、八步事件分步表（maxGroupLen=16；计数列为该事件之后的值，存在集 = 计数非 0 的组）

| 步 | 事件 | NULL 组 | "" 组 | "a" 组 | "b" 组 | 存在的组 |
|---|---|---|---|---|---|---|
| 1 | (∅,+2) | 2 | 0 | 0 | 0 | {∅} |
| 2 | ("",+1) | 2 | 1 | 0 | 0 | {∅,""} |
| 3 | ("a",+3) | 2 | 1 | 3 | 0 | {∅,"",a} |
| 4 | (∅,+1) | 3 | 1 | 3 | 0 | {∅,"",a} |
| 5 | ("b",+5) | 3 | 1 | 3 | 5 | {∅,"",a,b} |
| 6 | ("",-1) | 3 | 0 | 3 | 5 | {∅,a,b} |
| 7 | ("a",-2) | 3 | 0 | 1 | 5 | {∅,a,b} |
| 8 | (∅,-3) | 0 | 0 | 1 | 5 | {a,b} |

- (甲) 第 4 步后：NULL 组 = 3，空串组 = 1。若把 nil 归并到 "" 键：合并键累到 (∅,+2)+("",+1)+(∅,+1)=4，于是 `Count(nil)` 错成 4（应 3），`Count(&"")` 错成 4（应 1）。
- (乙) 第 6 步后空串组计数为 0，属「不存在」。若归零仍保留为存在：`Groups()` 会多列出空串组，值为 0。
- (丙) 第 8 步 NULL 组已归零移除；再 `Feed(∅,-1)` 应整体拒绝，报 `ErrNegativeCount`，且不留痕（所有组计数与存在集不变，之后可正常使用）。若允许负计数：`Count(nil)` 错成 -1。

## 二、四条不变量的保证位置与钉住它的测试

1. 与批量重算一致：`agg.Agg.Apply` 逐事件增量维护、`api.View.Feed` 全批提交；测试 `TestBatchEquivalence`（api/api_test.go，随机序列对比朴素重算）。
2. NULL 与空串不混：`grp.Normalize` 用 `null` 标志位区分 nil 与 ""，键身份按字符串值；测试 `TestNullVsEmpty`。
3. 计数非负且归零即删：`agg.Agg.Apply` 负值拒绝、归零 `delete`；`api.View.Groups` 只列 map 中现存键；测试 `TestNonNegativeZeroRemoval`。
4. 失败不留痕：`api.View.Feed` 先在克隆上试算、全部通过才换入，`api.New` 校验参数；测试 `TestFailureAtomicity`。
