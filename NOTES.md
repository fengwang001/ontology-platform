# 列级脱敏视图 NOTES

## 第三节推导：analyst 六行分步表（逐字符核对）

| # | 行.列 | 原始值 | 套用规则 | 掩码结果 |
|---|---|---|---|---|
| 1 | 行1.Name | `张`（长1） | 保留首字符，余→`*`；长1保留原值 | `张` |
| 2 | 行1.Card | `1234567890123456`（长16） | 保留末4 `3456`，前12个→`*` | `************3456` |
| 3 | 行1.Phone | `13800138000`（长11） | 保留末4 `8000`，前7个→`*` | `*******8000` |
| 4 | 行2.Name | `李四`（长2） | 保留首字符 `李`，余1个→`*` | `李*` |
| 5 | 行2.Card | `999`（长3<4） | 长度<4 全掩，一个不留 | `***` |
| 6 | 行2.Phone | `10086`（长5） | 保留末4 `0086`，前1个→`*` | `*0086` |

**(甲)** `999` 正确应掩成 `***`。若只做「保留末4」而无长度<4全掩分支，对长3字符串取末4得到原样 `999`——完整卡号泄露，违反不变量2（原始值不泄露）。
**(乙)** 漏配 `phone` 则行1 Phone 原样返回 `13800138000`（正确应为 `*******8000`），完整手机号泄露，违反不变量2与不变量3（角色隔离）。
**(丙)** 误写成「保留首4」则行1 Card 错成 `1234************`（正确应为 `************3456`），被错误保留的是 `1234`。

## 四条不变量：保证位置与钉住测试

1. **与规则一致**：`mask/mask.go` 的 `MaskColumn` 纯函数按 (role,column) 直接索引分派，只引用 `raw`；测试 `TestMaskRules`（mask 包，表驱动含 len 0/1/3/4/5/16 边界）。
2. **原始值不泄露**：`mask/mask.go` 的 `MaskName/MaskCard/MaskPhone` 仅用 `raw` 重建新串；测试 `TestNoLeak`（api 包，逐字符断言 analyst/none 输出不含被掩子串）。
3. **角色隔离**：`table/table.go` 的 `Read` 按角色逐列套掩码组装新结构；测试 `TestViewsPerRole`（api 包，三角色视图逐字段比对）。
4. **失败不留痕**：`table/table.go` 的 `Insert` 先校验 `ID<0` 再写入、`mask/mask.go` 先校验角色/列再分派，拒绝路径无任何写操作；测试 `TestRejectedNoStateChange`（api 包，拒绝前后视图逐字段相等且可继续正常使用）。
