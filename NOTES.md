# NOTES — 规范归一化推导与不变量

## 八输入分步推导（D=递归分解，NFD=分解+规范排序，NFC=NFD+规范组合）

| # | 输入 | D | NFD | NFC | 累计键数 |
|---|---|---|---|---|---|
| 1 | 00E9 | 0065 0301 | 0065 0301 | e+0301→**00E9** | 1 |
| 2 | 0065 0301 | 0065 0301 | 0065 0301 | e+0301→**00E9** | 1 |
| 3 | 0065 0301 0323 | 同左 | 0065 **0323 0301**（220<230 重排） | e+0323→1EB9；1EB9+0301 无表项 → **1EB9 0301** | 2 |
| 4 | 0065 0323 0301 | 同左 | 0065 0323 0301（已有序） | 同上 → **1EB9 0301** | 2 |
| 5 | 1100 1161 | 同左（Jamo 无表项） | 1100 1161（皆起始符） | L+V 算法组合 → **AC00** | 3 |
| 6 | AC00 | 算法分解→1100 1161 | 1100 1161 | L+V→**AC00** | 3 |
| 7 | 212B | →00C5→0041 030A | 0041 030A | A+030A→00C5（212B 组合排除）→ **00C5** | 4 |
| 8 | 00C5 | 0041 030A | 0041 030A | A+030A→**00C5** | 4 |

**(甲)** 输入 3、4 规范等价（NFD 同为 0065 0323 0301）。若不做规范排序：输入 3 分解得 0065 0301 0323，e+0301 先组合成 00E9，0323 留下，错键 = **00E9 0323**；输入 4 键 = 1EB9 0301，两键**不同**。
**(乙)** 输入 5、6 规范等价。若只查表、无韩文算法分解：输入 6 的 NFD 错成 **1 个码点**（AC00 自身，应为 2 个）；输入 5 键 = 1100 1161、输入 6 键 = AC00，**不再同键**。
**(丙)** 输入 7、8 规范等价。若 A+ring 优先还原「首次见到的原字符」：输入 7 的 NFC 键错成 **212B**，输入 8 键 = 00C5，**不再同键**。

## 四条不变量的保证位置与钉住测试

1. 与朴素参照一致：`norm/norm.go` 的 `NFD/NFC`；测试 `TestNaiveReference`（norm_test.go，固定种子随机对拍）。
2. 幂等且 NFC 与原文规范等价：`NFC` 先 NFD 再组合；测试 `TestIdempotent`。
3. 等价键一致、`Get` 精确返回等价类、`Distinct`=键数：`idx/idx.go` 以 NFC 为 map 键；测试 `TestEquivalenceKeys`。
4. 失败不留痕：`idx.Put` 先校验再改 map、`New` 先验参；测试 `TestFailureAtomic`（另 `TestErrorsDistinct` 钉四类哨兵错误互不相同）。
