# 按版本列条件 upsert 与删除墓碑 — 推导与不变量落点

## 十步分步表（R=10，maxKeys=100；行记为 值@版本）

| # | 事件 | cur | 判定 | G | 本步清除 | 存活行 | 墓碑 |
|---|------|-----|------|---|---------|--------|------|
| 1 | U k1 v5 a | 0 | 应用 | 5 | 无 | k1=a@5 | — |
| 2 | D k1 v8 | 5 | 应用 | 8 | 无 | — | k1=8 |
| 3 | U k1 v6 b | 8 | 忽略 | 8 | 无 | — | k1=8 |
| 4 | U k2 v12 c | 0 | 应用 | 12 | 无 | k2=c@12 | k1=8 |
| 5 | U k1 v8 d | 8 | 忽略 | 12 | 无 | k2=c@12 | k1=8 |
| 6 | D k3 v15 | 0 | 应用 | 15 | 无 | k2=c@12 | k1=8, k3=15 |
| 7 | U k2 v18 e | 12 | 应用 | 18 | k1（18−8=10≥10） | k2=e@18 | k3=15 |
| 8 | U k1 v7 f | 0 | 应用 | 18 | 无 | k1=f@7, k2=e@18 | k3=15 |
| 9 | U k3 v14 g | 15 | 忽略 | 18 | 无 | k1=f@7, k2=e@18 | k3=15 |
| 10 | U k4 v30 h | 0 | 应用 | 30 | k3（30−15=15≥10） | k1=f@7, k2=e@18, k4=h@30 | — |

**(甲)** 第 5 步 Ver=8=cur，严格大于不成立 → **忽略**。若写成 `Ver >= cur`：第 5 步应用，k1=d@8；第 8 步 v7<8 被忽略，最终 **k1=d@8**（正确为 k1=f@7）。
**(乙)** 删除不留墓碑：第 3 步 cur=0，**k1=b@6 被应用**；第 9 步 k3 的 U v14 也会应用，最终 **k3=g@14**（正确实现下 k3 最终无行无墓碑，键不存在）。
**(丙)** k1 墓碑在**第 7 步**被清除（G=18，18−8=10≥R=10）。第 8 步 cur=0 → **应用**，k1=f@7。第 8 步 G_before−R=18−10=8，Ver=7≤8 不满足第 2 条前提，故不违反。若到期判定写成 `G−t > R`：第 7 步 10>10 不清除，墓碑留到**第 10 步**（30−8=22>10）才清除；第 8 步被墓碑挡住，最终 **k1 无行无墓碑**。

## 四条不变量的落点

1. **版本只进不退**：`rule.ShouldApply` 严格 `>`，`store.ApplyOne` 只在判定通过时写行/墓碑，忽略只加计数；`TestVersionMonotonic` 钉住。
2. **与朴素参照一致**：`store.purge` 用 `rule.Expired`（G−t≥R）按版本有序清除，前提满足时清除不触及任何键的 cur；`TestNaiveConsistency` 钉住。
3. **删除不被旧写入复活**：D 分支建/升墓碑，`store.cur` 取行与墓碑版本之最大；`TestTombBlocksOldWrites` 钉住。
4. **失败不留痕**：`api.Apply` 全程在 `store.Clone` 的副本上执行，全部成功才换入；`TestFailureNoTrace` 钉住。
