# GK 分位数摘要 NOTES

## 第三节推导（ε=0.25，2ε=0.5，按题面规则逐步执行）

1. INSERT(10)：[(10,1,0)]（n=1）
2. INSERT(20)：[(10,1,0),(20,1,0)]（n=2）
3. INSERT(30)：[(10,1,0),(20,1,0),(30,1,0)]（n=3）
4. INSERT(40)：[(10,1,0),(20,1,0),(30,1,0),(40,1,0)]（n=4）
5. COMPRESS：[(10,2,0),(30,2,0)]（n=4，2εn=2；对(30,40)与(10,20)的 g+g+Δ=2≤2 合并，(20,30′) 和为 3 不合）
6. INSERT(25)：[(10,2,0),(25,1,1),(30,2,0)]（n=5）

- (甲) 落在 (10,2,0) 与 (30,2,0) 之间，Δ=g_i+Δ_i−1=2+0−1=**1**。漏写 −1 则 Δ=2，g+Δ=3 > 2εn=2.5，**违反带宽不变**。
- (乙) ≤ 错成 <：三对之和为 2,3,2，无一 <2 → **一个都不合并**；INSERT(25) 改落 (30,1,0) 前，Δ 错成 1+0−1=**0**。
- (丙) QUERY(0.7)，φn=3.5：正确=**25**（首个 rmax≥3.5：(25,1,1) 的 rmax=4）；错用 rmin → **30**（首个 rmin≥3.5：(30,2,0) 的 rmin=5）。

## 题面规则的两处自相矛盾及实现的修正（不修正则第二节不变量不可达）

1. **合并方向**：题面「g_{i−1}+=g_i、删第 i 个」使幸存元组保留**较小** v，rmin 不再是排名下界——上面第 6 步 (30,2,0) 的 rmin=5 已大于 rank(30)=4；且级联合并可把 1..8 并成 [(1,8,0)]，任意 φ 都返回最小值，误差随 n 无界。实现改为保留**较大** v（标准 GK，此时 rank(v_i)∈[rmin_i,rmax_i] 恒成立）：第 5、6 步变为 [(20,2,0),(40,2,0)]、[(20,2,0),(25,1,1),(40,2,0)]；(甲)(乙) 结论不变，(丙) 错值相应为 40、正确仍为 25。demo 打印的是修正后轨迹。
2. **精度**：band=2εn 加「首个 rmax≥φn」查询的误差界实为 2εn（rank∈[rmin,rmax]，区间宽 Δ≤2εn），不变量 3 的 ≤εn 数学上不可达。api 内部以 ε/2 运行 gk（有效 band ≤ εn），误差 ≤εn 成立；gk.Compress 仍按题面阈值 2εn 实现，供第三节复现。

## 四条不变量：保证位置 → 钉住测试

1. 结构（升序、无重、Size=n）：gk.Insert 二分定位 + api.Insert 的 seen 去重 → TestStructure
2. 带宽 g+Δ≤2εn（2εn<1 时按下界 1 判，否则题面第 1 步即自相矛盾）：gk.Compress 合并条件 + Insert 的 Δ 规则 → TestBand
3. 误差 ≤εn：quant.Query 首个 rmax 定位 + api 内部 ε/2（修正 2）→ TestNaiveConsistency
4. 失败不留痕：api 先校验后变更，四个哨兵错误互不相同 → TestFaultInjection

并发：api 读写锁、查询只读 → TestConcurrentQuery；查询复杂度：quant 非导出计数器 → TestQueryVisitedBounded
