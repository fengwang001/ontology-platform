# 新鲜期（freshness lifetime）选取规则推导

三条候选来源：响应里的 `s-maxage`、`max-age`，以及绝对时刻 `Expires`（其新鲜期为
`Expires - tDate`，负数截为 0）。

## 共享缓存这一前提为什么影响次序

本缓存是**共享缓存**（多个用户共用同一份缓存副本），而不是单个用户的私有缓存。
HTTP 缓存语义（RFC 9111 §4.2.2 / RFC 7234 §4.2.2）因此规定：

- `s-maxage` 名字中的 `s` 即 shared，它是**专门写给共享缓存**的新鲜期指令；一旦出现，
  共享缓存必须以它为准，忽略 `max-age` 与 `Expires`。这样源站才能用一个共享端的
  覆盖值，避免按更适合私有缓存或源端默认值的时长把陈旧内容分发给多个用户。
- 没有 `s-maxage` 时，显式的相对秒数 `max-age` 优先于 `Expires`：`max-age` 是
  HTTP/1.1 起引入的、语义无歧义的相对新鲜期；`Expires` 是 HTTP/1.0 遗留的绝对
  时刻，依赖时钟同步、会被 `Date` 偏差扭曲，故只作兜底。
- 三者都不存在时，新鲜期为 0，即立刻过期，不做启发式猜测（不实现
  Last-Modified 启发式放宽，行为确定）。

## 确定的优先次序（不依赖 map 遍历顺序）

1. `s-maxage`（仅共享缓存生效，最高优先）
2. `max-age`
3. `Expires - tDate`（负数截 0）
4. 都不存在 → 0

该次序为固定链，逐条短路取值，因此对同一组头部永远给出相同结果。
