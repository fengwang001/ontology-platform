# 滑动窗口 TopK 推导（N=5, K=2）

| 步 | 到达（滑出） | 窗口内各键 sum | TopK |
|---|---|---|---|
| 1 | (a,+6) | a=6 | (a,6) |
| 2 | (a,+4) | a=10 | (a,10) |
| 3 | (b,+9) | a=10,b=9 | (a,10),(b,9) |
| 4 | (c,+8) | a=10,b=9,c=8 | (a,10),(b,9) |
| 5 | (d,+7) | a=10,b=9,c=8,d=7 | (a,10),(b,9) |
| 6 | (e,+1)，滑出(a,+6) | a=4,b=9,c=8,d=7,e=1 | (b,9),(c,8) |
| 7 | (f,+8)，滑出(a,+4) | b=9,c=8,d=7,e=1,f=8 | (b,9),(c,8) |

甲：第 6 步正确为 (b,9),(c,8)——a 撤出、c 补位；「只减分不重算、sum 归 0 才剔除」会保留 a，错成 (b,9),(a,4)，c 漏位。
乙：第 7 步 c/f 并列 8，字典序 c 胜，正确 (b,9),(c,8)；「并列取最新到达者」错成 (b,9),(f,8)。
丙：误留 N+1=6 条时，第 6 步 (a,+6) 未滑出、a 仍为 10，TopK 错成 (a,10),(b,9)，c 不补位。

## 不变量在代码中的保证位置与钉住的测试

1. 与朴素参照一致：`topk.Model.Apply` 的补位/交换循环每次变更后比较界内根（最差成员）与界外根（最佳候补）交换，`TopK` 只弹成员堆；`SelfCheck` 七步序列逐步暴力比对。测试 `TestNaiveReference`。
2. 撤回补位：`topk.Model.resync` 只重定位本步受影响的键，交换循环仅在候补严格优于成员时互换，并列按键序。测试 `TestWithdrawFill`（钉第 6、7 步）。
3. 窗口内容正确：`wtop.Window.Add` 环形覆盖最旧条，`sums/count` 同增同减、count 归 0 即删键。测试 `TestWindowSums`。
4. 失败不留痕：`api.New` 先校验 N/K 再建状态；`api.Feed` 先整批校验空 Key 全部通过后才应用；哨兵 `ErrInvalidN/ErrInvalidK/ErrEmptyKey`。测试 `TestErrors`、`TestRejectedNoTrace`。
