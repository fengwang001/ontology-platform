# 翻滚窗口 + 水位线 + 允许迟到：推导与不变量

## 八行分步表（size=10, delay=3, lateness=5，同一 Key；wm = max(TS)−3，只进不退）

| # | TS | 事后 wm | 落入窗口 | 判定 | 本步输出 | 事后保留状态的窗口 |
|---|----|--------|----------|------|--------|--------------------|
| 1 | 2  | -1 | [0,10)   | 正常     | 无                        | [0,10):1 |
| 2 | 7  | 4  | [0,10)   | 正常     | 无                        | [0,10):2 |
| 3 | 13 | 10 | [10,20)  | 正常     | +(K,[0,10),2)（触发）      | [0,10):2, [10,20):1 |
| 4 | 9  | 10 | [0,10)   | 迟到接受 | -(K,[0,10),2); +(K,[0,10),3) | [0,10):3, [10,20):1 |
| 5 | 18 | 15 | [10,20)  | 正常     | 无（wm≥0+10+5，清除 [0,10)） | [10,20):2 |
| 6 | 4  | 15 | [0,10)   | 丢弃     | 无                        | [10,20):2 |
| 7 | 10 | 15 | [10,20)  | 正常     | 无                        | [10,20):3 |
| 8 | 23 | 20 | [20,30)  | 正常     | +(K,[10,20),3)（触发）     | [10,20):3, [20,30):1 |

- (甲) TS=13→[10,20)，其到达把 wm 推到 10，触发 [0,10) 输出 +2；TS=10→[10,20)，不触发任何窗口。若把窗口当闭区间 [start,end]：TS=10 会错落入 [0,10]（已于第 5 步清除）被丢弃，[10,20) 终值错成 2、丢弃数错成 2。若触发写成 wm>end：第 3 步 [0,10) 不触发、+2 缺失（迟至第 5 步 wm=15 才发，并与第 4 步迟到输出叠加出错）；第 7 步本就不触发，不受影响。
- (乙) 第 6 步 wm=15 = end+lateness=10+5，按「wm < end+lateness 才接受」判定为**丢弃**。若迟到/清除判定写成 wm > end+lateness：第 5 步不清除 [0,10)，第 6 步被接受 → 输出 -3;+4，最终视图 [0,10) 错成 4（正确为 3）。
- (丙) 改序 23,2,7,13,9,18,4,10：TS=23 先把 wm 推到 20，随后 2、7、9、4 全部因 wm≥end+lateness 被丢弃，最终视图只剩 [10,20):3、[20,30):1，没有 [0,10)。差异来自「水位线按到达顺序的最大 TS 单调推进 + 超迟到即丢弃」这条规则。不变量 1 必须写成「只取被接受的事件」：被丢弃事件永不进入视图，若按全部事件重算，任何发生过丢弃的流式结果都不可能等于批量结果。

## 四条不变量的落点

1. 批量一致：`wagg.feedOne` 只给被接受事件计数、`Flush` 把水位线推到 +∞ 触发全部 —— 测试 `TestFlushMatchesBatch`。
2. 日志自洽：`wagg.feedOne` 对已触发窗口先 `-` 旧值再 `+` 新值 —— 测试 `TestChangelogPrefixConsistency`。
3. 水位线单调：`wagg.feedOne` 取 `max(旧wm, TS-delay)`，丢弃事件同样推进 —— 测试 `TestWatermarkMonotonic`。
4. 失败不留痕：`wagg.Feed` 先克隆、全部成功才整体换入；参数/空 Key/maxOpen 三个哨兵错误互不相同 —— 测试 `TestFailureAtomicity`、`TestErrorsDistinct`。
