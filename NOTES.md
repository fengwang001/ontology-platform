# ontology-751 弦图判定 NOTES

## 第三节推导

图：n=6，边 (0,1)(1,2)(0,2)(1,3)(3,4)(2,5)。邻接：0:{1,2} 1:{0,2,3} 2:{0,1,5} 3:{1,4} 4:{3} 5:{2}

MCS 分步表（每行：选中节点 | 选中后各未选节点权重）：

1. 选 0（全 0 并列取最小编号）| 1:1 2:1 3:0 4:0 5:0
2. 选 1（权 1，与 2 并列取小）| 2:2 3:1 4:0 5:0
3. 选 2（权 2 唯一最大）| 3:1 4:0 5:1
4. 选 3（权 1，与 5 并列取小）| 4:1 5:1
5. 选 4（权 1，与 5 并列取小）| 5:1
6. 选 5（唯一剩余）| —

MCS 选中序 = [0 1 2 3 4 5]，PEO = 逆序 = [5 4 3 2 1 0]。

逐节点成团检查（N+ = PEO 中排在 v 之后的邻居）：5:{2}✓ 4:{3}✓ 3:{1}✓ 2:{0,1} 有边(0,1)✓ 1:{0}✓ 0:{}✓ → 弦图，FirstViolator=-1。

- (甲) C4（环 0-1-2-3-0）：MCS 序 [0 1 2 3]，PEO [3 2 1 0]。只做 MCS 不查成团 → 错判「弦图(true)」；正确：非弦图，首个违规节点是 3（N+(3)={0,2}，(0,2) 无边）。
- (乙) 六边图把选中序原样当 PEO=[0 1 2 3 4 5]：节点 1 的 N+={2,3}，(2,3) 无边 → 在节点 1 处误判「不成团」，把弦图错判成「非弦图」；正确：弦图。
- (丙) 只查「与 PEO 紧邻下一节点相邻」：C4 中 3-2、2-1、1-0 均相邻而全过，漏检 N+(3)={0,2} 中 (0,2) 这一对不相邻节点 → 把 C4 错判成「弦图」；正确：非弦图。

## 第二节不变量落点

1. PEO= MCS 选中序的逆序：`mcs.Solver.Compute` 用最大堆得选中序后整体取逆；钉于 TestPEOMatchesNaiveMCS。
2. 判定正确 + 首个违规：`mcs.Solver.Compute` 按 PEO 顺序逐节点对 N+ 两两查边，首个失败节点即 Violator；钉于 TestSixEdgeGraph、TestC4。
3. 与朴素参照一致：测试内 naiveMCS（全表扫描）+ naiveChordal（逐节点两两查）逐图对照；钉于 TestChordalMatchesNaive。
4. 失败不留痕：`ug.New`/`ug.AddEdge` 全部校验先于任何写入，四类哨兵错误互不相同；钉于 TestRejectedOpsLeaveStateUnchanged。
