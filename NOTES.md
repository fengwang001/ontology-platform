# Marzullo 时钟同步 — 推导与不变量

## 扫描线推导（f=1，K=3，需 ≥ K−f = 2 台一致）

区间：A[8,12]、B[10,12]、C[19,21]（均闭）。端点按位置升序，同一位置 L 在 R 前。

| # | 端点 | 位置 | 处理后 count | 候选共识区间变化 |
|---|------|------|--------------|------------------|
| 1 | L(A) | 8  | 1 | <2，无候选 |
| 2 | L(B) | 10 | 2 | 达到 2，进入候选，lo=10 |
| 3 | R(A) | 12 | 1 | 跌破 2，离开候选，hi=12 → 候选 [10,12] |
| 4 | R(B) | 12 | 0 | 不在候选中 |
| 5 | L(C) | 19 | 1 | <2，无候选 |
| 6 | R(C) | 21 | 0 | 不在候选中 |

最短共识区间 = **[10,12]**（闭，长度 2，唯一候选即最短）。

- **(甲)** 12 属于共识区间：闭区间含端点，A、B 的误差区间都含 12，该时刻仍有 2 台一致。若错写成右开 [10,12)，会丢掉 t=12 这个 A、B 恰好仍达成共识的端点时刻，CountAt(12) 也会由 2 错成 1。
- **(乙)** f=0 等价要求 3 台全一致：A∩B∩C = [10,12]∩[19,21] = ∅，扫描线 count 峰值只有 2 < 3，应报「无法共识」的可判定错误。与 f=1 的差别：f=1 容忍 C 一台出错，得 [10,12]；f=0 不容错，空交集、无结果。
- **(丙)** error=−1 → C 的区间为 [21,19]，lo>hi 倒置。必须按可判定错误（ErrNegativeError）整体拒绝、不得参与共识：倒置区间不含任何真实时刻，若参与会让扫描线在 21 处 +1、19 处 −1，计数彻底失真。

## 四条不变量 → 代码位置 → 钉住它的测试

1. 与朴素重算一致：`csync.Set.Consensus` 每次对当前排序端点重跑 `marz.Sweep`，无任何缓存 → `TestConsensusMatchesNaive`
2. 边界闭合：`marz.Sweep` 的 lo/hi 直接取端点位置，同位 L 先 R 后保证端点计入 → `TestBoundaryClosure`
3. CountAt 正确：`csync.Set.CountAt` 用二分算「L(≤t) − R(<t)」，与逐台判定等价 → `TestCountAtMatchesNaive`
4. 失败不留痕：`csync.Set.Add`/`api.New` 先全部校验、后变更，拒绝路径零写入 → `TestRejectedOpsLeaveStateUnchanged`
