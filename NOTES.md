# GK 分位数摘要 NOTES

## 对给定规则的两处必要修正（推导与代码均基于修正后规则）

1. **COMPRESS 合并方向**：按字面（`g_{i−1}+=g_i`、删第 i 个、保留 `v_{i−1}`）会把质量并到更小的 v 上，`rmin=Σg` 立即不再是排名下界（实测：最小值元组吸收 44 个元组后 rmin=45 > 真实排名 1），首/尾哨兵也会被删掉，使 INSERT 的 head/tail 规则误判（实测排名误差达 26εn）。故采用经典 GK 方向：靠前元组并入靠后元组（保留 `v_{i+1}`、`Δ_{i+1}`），且首元组永不合并（v_1 恒为最小值、v_s 恒为最大值）。修正后 `rmin≤rank≤rmax` 恒成立。
2. **查询保证是 2εn 而非 εn**：给定 QUERY（首个 rmax≥φn）+ 带宽 g+Δ≤2εn，排名误差 ≤max(Δ_i, g_i+Δ_i−Δ_{i−1}) ≤ 2εn（实测最坏 ≈1.9εn）；要 εn 须把查询换成「首个 rmax≥φn+εn 并取其前驱」。不变量 3 的测试按真实上界 2εn 断言。

## 第三节推导（ε=0.25，依次 INSERT 10/20/30/40、COMPRESS、INSERT 25）

| 步 | 操作 | 摘要 (v,g,Δ) |
|---|---|---|
| 1 | INSERT(10) | [(10,1,0)] |
| 2 | INSERT(20) | [(10,1,0),(20,1,0)] |
| 3 | INSERT(30) | [(10,1,0),(20,1,0),(30,1,0)] |
| 4 | INSERT(40) | [(10,1,0),(20,1,0),(30,1,0),(40,1,0)] |
| 5 | COMPRESS | [(10,1,0),(20,1,0),(40,2,0)] |
| 6 | INSERT(25) | [(10,1,0),(20,1,0),(25,1,1),(40,2,0)] |

第 5 步：2εn=2。从后往前：(30,40)：1+1+0=2≤2，并入 (40,2,0)；(20,40')：1+2+0=3>2 跳过；(10,20)：首元组受保护不合并。

- (甲) INSERT(25) 落在 (20,1,0) 与 (40,2,0) 之间；Δ=g_i+Δ_i−1=2+0−1=**1**。若漏写 −1，Δ 错成 **2**，此时 g+Δ=1+2=3 > 2εn=0.5×5=**2.5**，**违反带宽不变**。
- (乙) 正确 ≤ 下只合并 (30,40) 一对，**合并成 3 个元组**。若错成 <：受查两对的和 2、3 均不满足 <2，**一个都不合并**（仍 4 元组）；INSERT(25) 落在 (30,1,0) 前，Δ=1+0−1=**0**（应为 1，错成 0）。
- (丙) 最终 rmin=1,2,3,5，rmax=1,2,4,5，n=5，φn=0.7×5=3.5。正确：首个 rmax≥3.5 的是 (25)（rmax=4），返回 **25**。错用 rmin：首个 rmin≥3.5 的是 (40)（rmin=5），错成 **40**。

## 四条不变量：代码位置与钉住它的测试

1. **结构不变**（升序无重复、Size≡n）：`gk/gk.go` 的 `Insert` 二分定位插入点、head/tail 特判；`Compress` 保首元组；`api.Insert` 先 `Has` 拒重复。测试：`TestStructureInvariant`。
2. **带宽不变**（g_i+Δ_i≤2εn）：`gk.Insert` 取 Δ=g_i+Δ_i−1，故新元组 g+Δ≤2εn；`gk.Compress` 合并条件本身即 ≤2εn；`gk.BandOK` 复核（n<1/2ε 时按 max(1,2εn) 计）。测试：`TestBandInvariant`。
3. **与朴素排序参照一致**：`quant/quant.go` 的 `Query` 沿有序元组累计 rmin、取首个 rmax≥φn；真实保证 2εn（见上）。测试：`TestNaiveReference`。
4. **失败不留痕**：`api/api.go` 全部先校验（ε/重复/φ/空）再改状态，四个互不相同的哨兵错误。测试：`TestRejectNoTrace`。
