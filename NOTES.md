# NOTES：变更流上的外键约束物化

## 十三步推导（空库起步；哨兵：O=ErrOrphan子插父不存在 U=ErrParentInUse删被引用父 P=ErrNoParent删不存在父 C=ErrNoChild删不存在子）

| # | 操作 | 判定 | ViewP | ViewC |
|---|---|---|---|---|
| 1 | C.ins(k1,p1) | 拒 O | [] | {} |
| 2 | P.ins(p1) | 成 | [p1] | {} |
| 3 | C.ins(k1,p1) | 成 | [p1] | {k1:p1} |
| 4 | C.ins(k2,p2) | 拒 O（p2 不存在） | [p1] | {k1:p1} |
| 5 | P.del(p1) | 拒 U（k1 引用 p1） | [p1] | {k1:p1} |
| 6 | C.ins(k3,p1) | 成 | [p1] | {k1:p1,k3:p1} |
| 7 | C.del(k1) | 成 | [p1] | {k3:p1} |
| 8 | C.del(k2) | 拒 C（k2 从未存在） | [p1] | {k3:p1} |
| 9 | C.del(k3) | 成 | [p1] | {} |
| 10 | P.del(p1) | 成（引用计数=0） | [] | {} |
| 11 | C.ins(k3,p1) | 拒 O（p1 已删） | [] | {} |
| 12 | P.ins(p1) | 成 | [p1] | {} |
| 13 | C.ins(k3,p1) | 成 | [p1] | {k3:p1} |

**(甲)** 第5步应拒（ErrParentInUse），p1 此时被 k1 引用，状态不变。误实现为级联删子：第5步后 ViewC 错成 {}（k1 被误删）。误实现为静默悬垂：ViewP=[]、ViewC={k1:p1}，k1 指向已不存在的 p1，违反不变量2（永不悬空）。
**(乙)** 等待队列错实现会记住第4步的 k2 与第11步的 k3，在第12步 p1 到达时自动补挂：第13步后 ViewC 错成 {k2:p2, k3:p1}（k2 指向从未出现的 p2，悬垂）。正确视图只有 {k3:p1}。差异来自规则「被拒的子行不记忆：没有等待队列，父晚到后必须由上游重投」——k2 从未被重投。
**(丙)** 第11步应拒（ErrOrphan），属「父晚到」（父第12步才到，判定只看操作那一刻父是否存在）。第8步应拒（ErrNoChild）：k2 第4步被拒、不留痕，故「不存在」。若把被拒插入记成「半存在」，第8步会被误判为删除成功；且半存在行若计入视图，第4步起 ViewC 错成含 k2:p2 的悬垂行（违反不变量2、4），第8步后才碰巧回到 {k3:p1}。

## 四条不变量的保证位置与钉住测试

1. 与朴素参照一致：fk 只在判定成功后改 sch（fk.go 各操作先校验后落写）；测试 `TestInvariantNaiveReplay`（随机序列对拍朴素重放）。
2. 永不悬空：CIns 先查父存在、PDel 先查引用计数为 0（fk.go）；测试 `TestInvariantNoDangling`。
3. 引用计数正确：计数只在 CIns/CDel 成功路径 ±1（fk.go）；测试 `TestInvariantRefCount`。
4. 失败不留痕：所有拒绝分支在任何写之前 return（fk.go）；测试 `TestInvariantFailureAtomic`（拒绝前后视图逐字段相等）。
