# NOTES：十进制字符串 → 精确既约分数

## 第三节：八行表（I.F(R) = I + F/10^j + R/(10^j·(10^k−1))，约分后）

| 输入 | 推导要点 | n/d |
|---|---|---|
| `"3.14"` | 314/100，gcd=2 | `157/50` |
| `"0.1"` | 1/10 已既约 | `1/10` |
| `"-2.5"` | 25/10=5/2，符号入分子 | `-5/2` |
| `"0.(3)"` | 3/(10^1−1)=3/9 | `1/3` |
| `"0.1(6)"` | 1/10+6/(10·9)=15/90 | `1/6` |
| `"123"` | 无小数部分 | `123/1` |
| `"0.(142857)"` | 142857/999999，gcd=142857 | `1/7` |
| `"-0.0"` | 值为 0，归一 | `0/1` |

## 三问

- **(甲)** `float64(0.1)` 的精确值是 `3602879701896397/36028797018963968`（≠1/10）。0.1 不是 2 的幂次和，二进制下是无限循环小数 `0.0001100110011…`，53 位尾数装不下只能舍入，故任何「float 解析再乘 10^k 取整」都会继承该表示误差，得到巨大且未约分的假分数。
- **(乙)** 累加到第 19 个 9 时 `num` 应为 9999999999999999999 > MaxInt64(9223372036854775807)。正确实现：在 `num*10+9` 前做溢出检出，报哨兵错误 `ErrOverflow` 并整体失败。朴素 int64 会回绕成 `-8446744073709551617`，静默产出错误结果。
- **(丙)** 两者正确结果都是 `0/1`。若不做「符号+零」归一，`-0.0` 会得到 N=0 且带负号的形态，`String()` 输出非法的 `-0/1`（零不应带符号、分母必须为正）。

## 四条不变量的保证位置与钉住测试

1. **与 big 朴素参照一致**：`api.SelfCheck` 内置 `math/big` 逐条对照；测试 `TestAgainstBigRef`（api/api_test.go）。
2. **既约且规范**：`conv.Convert` 末尾 gcd 约分、d>0、零归一为 0/1；测试 `TestNormalized`。
3. **有限与循环都精确**：`conv.Convert` 全程 int64 整数运算、禁浮点；测试 `TestExactRepeating`。
4. **失败不留痕**：`conv.Convert` 无可观测全局状态（计数器为非导出内部仪表，不经公开接口可见），被拒输入整体失败且后续解析结果不变；测试 `TestRejectedLeavesNoState`。
