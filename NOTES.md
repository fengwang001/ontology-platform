# NOTES

推导：`A=[[0,1,1],[1,0,1],[1,1,0]]`，`b=[5,4,3]`（行主序），期望 `x=[1,2,3]`。

| 步 | 候选\|·\|序列 | 选中(行,值) | 交换 | 步末 A / b / x |
|---|---|---|---|---|
| k=0 | [0,1,1] | (1,1) | 换 0↔1（A 与 b 同换） | A=`[[1,0,1],[0,1,1],[0,1,-1]]`，b=`[4,5,-1]` |
| k=1 | i=1,2：[1,1] | (1,1)，并列取小 | 无 | 第2行 -= 第1行 → `[0,0,-2]`，b=`[4,5,-6]` |
| k=2 | [2] | (2,-2) | 无 | 上三角完成 |
| x[2] | — | -6/-2 | — | `x[2]=3` |
| x[1],x[0] | — | — | — | `x[1]=(5-1·3)/1=2`；`x[0]=(4-0·2-1·3)/1=1` |

（甲）只换 A 不换 b：k=0 消元后 b 仍从 `[5,4,3]` 起算，末态 b=`[5,4,-6]`，回代得 **x=[2,1,3]**（x0、x1 对调，错）。
（乙）不用主元：`A[0][0]=0`，Go float64 除以零不 panic，乘子 m=1/0=+Inf 污染第 1、2 行（U 出现 -Inf/NaN），回代终得 **x=[NaN,NaN,NaN]**；若另加零主元守卫则把非奇异矩阵**误报奇异**。均不可能照常解出。
（丙）回代减号写反成加号：`x[2]=3` 不变，**x[1]=(5+3)=8**，**x[0]=(4+0+3)=7**。

## 不变量（保证位置 / 钉住的测试）

1. 残差逐项 ≤1e-9：`elim/elim.go` 的 `Solve` 前向消元+逆序回代；`api/api_test.go::TestSolveResidual`。
2. 输入逐字节不改：`elim/elim.go` `Solve` 入口先整体复制 a、b 再运算；`api/api_test.go::TestInputNotModified`。
3. 并列最大恒取最小下标：`pivot/pivot.go` `Pick` 仅在严格 `>` 时更新选中行；`api/api_test.go::TestPivotTieSmallest`、`pivot/pivot_test.go::TestPick`。
4. 失败不留痕（含内部计数器）：`elim/elim.go` 先全部校验（n、维度）再复制，奇异在复制体上判定、计数器无写入；`api/api_test.go::TestRejectedLeavesNoTrace`。

复杂度（第四节）：找最大与判全零合并为同一趟，整轮消元后额外扫描恒 0、不随 n 增长；`pivot/pivot_test.go::TestExtraScanCounterZero`（n=100/1000 跑真实整轮、n=10000 跑等价 n 趟扫描）。
