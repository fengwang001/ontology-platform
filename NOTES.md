# Gauss-Jordan 求逆：推导与不变量

## 五行分步表：A = [[0,2],[1,1]]，增广 [A|I]，n=2

| 步 | 列k | 候选主元 | 主元行 | 交换 | 本步行变换后 [A|I] |
|---|---|---|---|---|---|
| 初始 | — | — | — | — | [0 2 \| 1 0] / [1 1 \| 0 1] |
| 1 | 0 | r0:\|0\|, r1:\|1\| | r1 | 是 | 换行后归一(÷1)：[1 1 \| 0 1] / [0 2 \| 1 0] |
| 2 | 0 | — | — | — | 下消元（r1 该列已为 0）、上消元（无行）：不变 |
| 3 | 1 | r1:\|2\| | r1 | 否 | 归一 r1÷2：[1 1 \| 0 1] / [0 1 \| 1/2 0] |
| 4 | 1 | — | — | — | 上消元 r0-=1·r1：[1 0 \| -1/2 1] / [0 1 \| 1/2 0] |

结果 A⁻¹ = [[-1/2, 1], [1/2, 0]]。核验 A·A⁻¹：行0=[0·(-1/2)+2·(1/2), 0·1+2·0]=[1,0]；行1=[1·(-1/2)+1·(1/2), 1·1+1·0]=[0,1] = I ✓

- (甲) 行变换只作用左半、右半 I 不同步 ⇒ 右半仍是单位阵，"A⁻¹"=I，A⁻¹[0][1] 错成 **0**（应为 1）。
- (乙) 不带主元直接用 A[0][0]=0 做主元 ⇒ 归一化除以 0，产生 NaN/Inf 并随消元污染全矩阵；**是除零（NaN/Inf），不是可判定的奇异错误**。
- (丙) 漏做上消元 ⇒ 右半=[[0,1],[1/2,0]] 被当作 A⁻¹，A⁻¹[0][0] 错成 **0**（应为 -1/2）。

## 四条不变量（保证位置 → 钉住它的测试）

1. A·A⁻¹==I（误差≤1e-9）：`gauss.eliminate` 对所有行变换同步作用左右两半 → `TestInvIdentity`（api 包表驱动多档矩阵）。
2. 单位阵精确、消元完左半恰为 I：归一化 d/d≡1、消元 f−f·1≡0 均 IEEE 精确；零元素跳过不污染 → `TestLeftHalfExactIdentity`、`TestExactInverse`。
3. 输入不被修改：`gauss.Invert` 开头 `copy` 出 L 后只在副本上运算 → `TestInputUnmodified`。
4. 失败不留痕：维度/空检查在任何写入前返回；乘减计数先记局部变量、成功才 `atomic.Add` → `TestFailureNoTrace`（gauss 白盒验计数器不变，api 验功能不变）。
