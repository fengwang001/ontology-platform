# 消元求行列式 NOTES

## 三、n=3 推导：A = [[0,1,1],[1,0,1],[1,1,0]]（det = 2）

| 行 | 列k | 候选主元 abs | 主元行 | 交换 | 符号累计 | 消元后矩阵 |
|---|---|---|---|---|---|---|
| 1 | 0 | 0,1,1（行0/1/2） | 行1（并列取小） | 换(0,1) | -1 | [1 0 1; 0 1 1; 0 1 -1] |
| 2 | 1 | 1,1（行1/2） | 行1（并列取小） | 无 | -1 | [1 0 1; 0 1 1; 0 0 -2] |
| 3 | 2 | 2（行2） | 行2 | 无 | -1 | 上三角，对角线 1,1,-2 |
| 4 | — | 主元乘积 | 1×1×(−2) = −2 | — | — | — |
| 5 | — | 最终 det | (−1)^1 × (−2) = 2 | — | — | — |

- (甲) 忘记交换符号翻转：det = (+1)×(−2) = **−2**（正确应为 2）。
- (乙) 不带主元、直接拿 A[0][0]=0 当主元：乘子 = 1/0 = +Inf，行运算产生 NaN 并传播到对角线，det 错成 **NaN**；若实现把零主元误判为奇异则错成 **0**。两者都错，正确为 2。
- (丙) 乘积循环上界写成 k<n−1（漏乘最后主元 −2）：det = (−1)×(1×1) = **−1**（正确应为 2）。

## 二、四条不变量落点

1. 与朴素参照一致：`elim.Det` 带部分主元消元；测试 `TestDetMatchesNaive`（elim/elim_test.go）。
2. 交换符号正确：`elim.Det` 内 `swaps` 计数，返回 `sign×对角积`；测试 `TestSwapSignParity`。
3. 输入不被修改：`elim.Det` 入口整体复制 `a` 后只在副本上运算；测试 `TestInputUnmodified`。
4. 失败不留痕：`elim.Det` 先做全部校验再进消元循环，计数器只在循环内递增；测试 `TestFailureAtomic`。

自检：`api.SelfCheck()` 对内置矩阵核验以上四条，返回首个违例错误。
