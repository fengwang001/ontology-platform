# 过滤索引增量维护 — 推导与不变量

谓词 P(row): `Score >= 50`（左闭）。索引：`Key → 命中行 ID 升序`。

## 第三节：六步推导（从空表开始）

| 步 | 操作 | 该行最新 Key/Score | 命中 旧→新 | 索引动作 | 本步之后索引 |
|---|---|---|---|---|---|
| 1 | Put(1,10,80) | id1: 10/80 | 无→命中 | 加入 10:[1] | 10→[1] |
| 2 | Put(2,10,30) | id2: 10/30 | 无→不中 | 无 | 10→[1] |
| 3 | Put(3,20,60) | id3: 20/60 | 无→命中 | 加入 20:[3] | 10→[1] 20→[3] |
| 4 | Put(1,10,49) | id1: 10/49 | 命中→不中 | 移除 10 下 id1 | 20→[3] |
| 5 | Put(2,10,50) | id2: 10/50 | 不中→命中(50 左闭) | 加入 10:[2] | 10→[2] 20→[3] |
| 6 | Put(3,10,70) | id3: 10/70 | 命中→命中,Key 20→10 | 迁移 20→10 | 10→[2,3] |

最终 `Lookup(10) = [2,3]`。

- **(甲)** 正确动作：从 Key 10 移除 id1。若「Score<50 时什么都不做、忘记移除」，id1 陈旧残留，最终 `Lookup(10)=[1,2,3]`，多出 id1。
- **(乙)** 误写成严格 `Score>50`：第 5 步 id2(Score=50) 不进索引，最终 `Lookup(10)=[3]`，少了 id2。
- **(丙)** 非过滤索引返回全表 Key=10 的行：最终 `Lookup(10)=[1,2,3]`，多出 id1(Score 49<50)；违反第二节**不变量 2（无陈旧条目：索引含 Score<50 的行）**。

## 第二节：四条不变量的保障位置与钉住测试

1. **与全量重建一致**：`ptab.Put/Delete` 的命中迁移 switch 对每次变更精确增量维护；`api.SelfCheck` 扫全表重建并逐 Key 比对。测试：`TestRebuildEquivalence`。
2. **无陈旧条目**：命中判定唯一入口 `pindex.Hit`(Score>=50)；命中翻不命中时 `ptab.Put` 必走 `Remove` 分支。测试：`TestSixStepTrace`（第 4 步）。
3. **Lookup 完备且升序**：`pindex.Add` 二分插入保序，`Lookup` 按 Key 直取该 Key 下全部 ID。测试：`TestLookupCompleteness`。
4. **失败不留痕**：`ptab.Put/Delete` 先校验（id<0 / score 越界 / Delete 不存在）后改任何状态。测试：`TestFaultInjection`。
