# 推导：n=3，八步操作（初始 (0,0,0)）

| # | 操作 | 操作后 (c0,c1,c2) | 结果 |
|---|---|---|---|
| 1 | Acquire(0) | (1,0,0) | 成功 |
| 2 | Acquire(2) | (1,0,1) | 成功 |
| 3 | Pick() | (1,0,1) | 返回 1（唯一最少） |
| 4 | Acquire(1) | (1,1,1) | 成功 |
| 5 | Pick() | (1,1,1) | 返回 0（并列取最小下标） |
| 6 | Release(0) | (0,1,1) | 成功 |
| 7 | Pick() | (0,1,1) | 返回 0（唯一最少） |
| 8 | Release(0) | (0,1,1) | 报错：连接下溢，状态不变 |

- (甲) 第 5 步三台并列，正确返回最小下标 **0**；若并列取最大下标，则返回 **2**。
- (乙) 第 7 步正确返回 **0**；若比较写反选连接数最多者，最多值 1 在下标 1、2 并列，取小得 **1**。
- (丙) 第 8 步 c0=0，正确行为报错且 (c0,c1,c2) 仍为 (0,1,1)；若不做下溢校验，c0 变成 **-1**，此后 Pick 的朴素扫描恒把 -1 当最少，**永远返回 0**，新流量全压到服务器 0。

## 四条不变量的落点与钉住测试

1. 与朴素参照一致：`lc/lc.go` 堆序 `Less` 按 (连接数, 下标) 字典序、`Pick` 直接返回堆顶；由 `TestPickMatchesNaiveReference` 钉住。
2. 计数非负：`svc/svc.go` 的 `Release` 在调用 `Decr` 前先校验 Count≥1；由 `TestCountsNonNegative` 钉住。
3. 守恒：`lc/lc.go` 的 `Incr/Decr` 严格 ±1，且 `svc/svc.go` 仅在校验通过后才调用它们；由 `TestConservation` 钉住。
4. 失败不留痕：`svc/svc.go` 的 `Acquire/Release` 先校验后改动，`api/api.go` 的 `New` 先校验后构造；由 `TestRejectedOpsLeaveNoTrace` 钉住。
