# NOTES

## 三、十一步推导（记录写法 `元素(A,R)`，`—`=不存在；集合只列存活元素；`″`=同上一步）

| 步 | A 记录 | A 集合 | B 记录 | B 集合 |
|---|---|---|---|---|
| 1 Add(A,x,5) | x(5,—) | {x} | — | {} |
| 2 Remove(B,x,5) | ″ | {x} | x(—,5) | {} |
| 3 Add(B,y,3) | ″ | {x} | x(—,5) y(3,—) | {y} |
| 4 Remove(B,z,9) | ″ | {x} | x(—,5) y(3,—) z(—,9) | {y} |
| 5 Merge(A,B) | x(5,5) y(3,—) z(—,9) | {y} | ″ | {y} |
| 6 Add(A,z,8) | x(5,5) y(3,—) z(8,9) | {y} | ″ | {y} |
| 7 Add(A,x,6) | x(6,5) y(3,—) z(8,9) | {x,y} | ″ | {y} |
| 8 Remove(A,y,7) | x(6,5) y(3,7) z(8,9) | {x} | ″ | {y} |
| 9 Add(B,y,6) | ″ | {x} | x(—,5) y(6,—) z(—,9) | {y} |
| 10 Merge(A,B) | x(6,5) y(6,7) z(8,9) | {x} | ″ | {y} |
| 11 Merge(B,A) | ″ | {x} | x(6,5) y(6,7) z(8,9) | {x} |

- (甲) 并列偏写（`A>=R` 即在集合）：第 5 步 A={x,y}（x 的 5>=5 被判活，正确为 {y}）；十一步后两副本仍同为 {x}，与正确结果相同（最终 x:6>5、y:6<7、z:8<9，偏向不再影响判定）。
- (乙) 整条覆盖式 Merge：第 5 步 A 记录=x(—,5) y(3,—) z(—,9)、集合 {y}（步 1 的 Add(A,x,5) 被 B 的 x(—,5) 整条盖掉）；第 10 步 A 记录=x(—,5) y(6,—) z(—,9)、集合 {y}——y 被错误加入（步 8 的 Remove(A,y,7) 被步 9 来的 y(6,—) 盖掉），x 被错误移除（步 7 的 Add(A,x,6) 被 B 的 x(—,5) 盖掉）。
- (丙) 未添加不记删除：步 2（Remove(B,x,5)）与步 4（Remove(B,z,9)）的删除被丢掉；第 5 步 A={x,y}；十一步后 A={x,z}（z 的 R=9 丢失，z(8,—) 被判活），正确应为 {x}。

## 四、四条不变量的落实位置与钉住它的测试

1. 朴素一致：`lww.Merge` 对每元素 A/R 独立取 max，`cluster.SyncAll` 全对全并入使各副本收敛到全量 max → `api_test.TestNaiveReference`。
2. 合并律：max 天然交换/结合/幂等，`Merge` 对 `dst==src` 直接返回、重复合并不再产生变更 → `lww.TestMergeLaws`。
3. 增量等价：每副本 `log` 记录全部变更、`last[src]` 记住上次序号，只并入 `log[last:]`；未变元素此前已取过 max 且记录单调不减 → `api_test.TestIncrementalEquivalence`、`lww.TestIncrementalChecked`。
4. 失败不留痕：所有校验（参数/空元素/非正 ts/容量）先于任何写入，Merge 先算容量再应用的两阶段写法 → `api_test.TestFailureAtomicity`。
