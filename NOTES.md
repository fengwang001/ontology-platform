# 裁剪规则推导（第三节）

## 方向性：只能从近端向远端裁
转发头部的左半部分是客户端自己就能写的：直连客户端可在请求里塞入任意
伪造记录，第一跳可信代理只会**追加**，不会替我们删除。因此只有从本机
直连对端起、连续可信的那段后缀才是可信代理写下的；越往左越不可信。
伪造示例：链式 `9.9.9.9, 1.2.3.4, 10.0.0.1`，可信 `10.0.0.0/8`。
`9.9.9.9` 是攻击者塞进头部的。若从远端找「第一个不可信」，结果会是
`9.9.9.9`——一条假记录就顶掉了真结果。正确做法：从最近的 `10.0.0.1`
（可信，裁掉）向远端走，遇 `1.2.3.4` 不可信即停，客户端是 `1.2.3.4`，
它左侧的 `9.9.9.9` 根本不被读取。

## 缺失地址的跳必须终止裁剪
`for=unknown` / `_xxx` 表示该跳刻意隐藏地址。无法判定它可信 ⇒ 不能裁
掉它；而它更远端的记录都由它或更左侧转发而来，可信度同样无从谈起。
故裁剪在该跳终止，客户端地址记为缺失，只报告终止位置（第几跳）。

## 两种头部的合并：右对齐拉链
两个头部描述同一条路径，且各自最右端都是最近一跳，故按**右对齐**
（近端对齐）逐位合并：同位置地址文本相同（大小写不敏感）⇒ 同一跳，
去重并合并来源标记；不同 ⇒ 两条都保留，约定链式记录更远、键值记录
更近（仅为确定性，不影响可信判定）；一侧用尽后另一侧余下记录依次接
到更远端。每个头部内部次序不变 ⇒ 只出现在一种头部里的跳不会被跳过；
裁剪仍从合并序列最近端开始 ⇒ 不变量 1 成立。

## 其余约定
- 跳号：合并序列从最远端起 1 计数；0 表示直连对端地址。
- 跳数上限按合并去重后的跳数计；超限即报错，不做部分裁剪。
- 无跳记录（直连）⇒ 客户端为调用方给出的对端地址。
- 网段包含判定逐跳短路，总次数不超过 n*m，首跳即停时不超过 m。
